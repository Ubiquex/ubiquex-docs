---
title: "aws_cloudtrail_insights_metric_data"
description: "`aws_cloudtrail_insights_metric_data` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudtrail_insights_metric_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_insights_metric_data"})

    		ubx.Data(cloudtrail.InsightsMetricData, "example", cloudtrail.InsightsMetricDataConfig{
    			EventName:   "example-event-name",
    			EventSource: "example",
    			InsightType: "example",
    			DataType:    "example",
    			EndTime:     "example",
    			MaxResults:  1,
    			Period:      1,
    			StartTime:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsMetricData } from "@ubx/sdk-aws/aws/data/cloudtrail/insights_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_insights_metric_data" });

      data(InsightsMetricData, "example", {
        eventName: "example-event-name",
        eventSource: "example",
        insightType: "example",
        dataType: "example",
        endTime: "example",
        maxResults: 1,
        period: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import InsightsMetricData, InsightsMetricDataConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_insights_metric_data")

        ubx.data(InsightsMetricData, "example", InsightsMetricDataConfig(
            event_name="example-event-name",
            event_source="example",
            insight_type="example",
            data_type="example",
            end_time="example",
            max_results=1,
            period=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="event_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="event_source" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="insight_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="period" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="trail_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_code" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="timestamps" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="trail_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="values" type="list(number)">
  Returned by the lookup.
</ResponseField>
