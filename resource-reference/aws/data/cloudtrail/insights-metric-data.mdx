---
title: "aws_cloudtrail_insights_metric_data"
description: "`aws_cloudtrail_insights_metric_data` reads real, reported CloudTrail Insights metric data points."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_insights_metric_data"})

    		ubx.Data(cloudtrail.InsightsMetricData, "example", cloudtrail.InsightsMetricDataConfig{
    			EventName:   "example-event-name",
    			EventSource: "example",
    			InsightType: "example",
    			DataType:    "example",
    			EndTime:     "example",
    			MaxResults:  1,
    			Period:      1,
    			StartTime:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsMetricData } from "@ubx/sdk-aws/aws/data/cloudtrail/insights_metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_insights_metric_data" });

      data(InsightsMetricData, "example", {
        eventName: "example-event-name",
        eventSource: "example",
        insightType: "example",
        dataType: "example",
        endTime: "example",
        maxResults: 1,
        period: 1,
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import InsightsMetricData, InsightsMetricDataConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_insights_metric_data")

        ubx.data(InsightsMetricData, "example", InsightsMetricDataConfig(
            event_name="example-event-name",
            event_source="example",
            insight_type="example",
            data_type="example",
            end_time="example",
            max_results=1,
            period=1,
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="data_type" type="string">
  The real data type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="event_name" type="string" required>
  The real name of the event this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="event_source" type="string" required>
  The real source that produced this event. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="insight_type" type="string" required>
  The real, discriminating type of the insight this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="period" type="number">
  The real time period this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="trail_name" type="string">
  The real name of the trail this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error_code" type="string">
  The real code identifying the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="timestamps" type="list(string)">
  The real timestamps included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="trail_arn" type="string">
  The real Amazon Resource Name (ARN) of the trail this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="values" type="list(number)">
  The real, primary values of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
