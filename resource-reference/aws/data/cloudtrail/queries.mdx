---
title: "aws_cloudtrail_queries"
description: "`aws_cloudtrail_queries` reads the real, saved queries run against a CloudTrail Lake event data store."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_queries"})

    		ubx.Data(cloudtrail.Queries, "example", cloudtrail.QueriesConfig{
    			EventDataStore: "example",
    			EndTime:        "example",
    			MaxResults:     1,
    			QueryStatus:    "example",
    			StartTime:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Queries } from "@ubx/sdk-aws/aws/data/cloudtrail/queries";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_queries" });

      data(Queries, "example", {
        eventDataStore: "example",
        endTime: "example",
        maxResults: 1,
        queryStatus: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import Queries, QueriesConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_queries")

        ubx.data(Queries, "example", QueriesConfig(
            event_data_store="example",
            end_time="example",
            max_results=1,
            query_status="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="event_data_store" type="string" required>
  The real CloudTrail event data store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_status" type="string">
  The real, current status of the query this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="queries" type="list(object)">
  The real queries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
