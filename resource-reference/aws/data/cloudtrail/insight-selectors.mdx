---
title: "aws_cloudtrail_insight_selectors"
description: "`aws_cloudtrail_insight_selectors` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudtrail_insight_selectors` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_insight_selectors"})

    		ubx.Data(cloudtrail.InsightSelectors, "example", cloudtrail.InsightSelectorsConfig{
    			EventDataStore: "example",
    			TrailName:      "example-trail-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightSelectors } from "@ubx/sdk-aws/aws/data/cloudtrail/insight_selectors";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_insight_selectors" });

      data(InsightSelectors, "example", {
        eventDataStore: "example",
        trailName: "example-trail-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import InsightSelectors, InsightSelectorsConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_insight_selectors")

        ubx.data(InsightSelectors, "example", InsightSelectorsConfig(
            event_data_store="example",
            trail_name="example-trail-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_data_store" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="trail_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="event_data_store_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="insight_selectors" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="insights_destination" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="trail_arn" type="string">
  Returned by the lookup.
</ResponseField>
