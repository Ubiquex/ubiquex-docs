---
title: "aws_cloudtrail_trail_status"
description: "`aws_cloudtrail_trail_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudtrail_trail_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_trail_status"})

    		ubx.Data(cloudtrail.TrailStatus, "example", cloudtrail.TrailStatusConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrailStatus } from "@ubx/sdk-aws/aws/data/cloudtrail/trail_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_trail_status" });

      data(TrailStatus, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import TrailStatus, TrailStatusConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_trail_status")

        ubx.data(TrailStatus, "example", TrailStatusConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_logging" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_cloud_watch_logs_delivery_error" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_cloud_watch_logs_delivery_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_delivery_attempt_succeeded" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_delivery_attempt_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_delivery_error" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_delivery_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_digest_delivery_error" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_digest_delivery_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_notification_attempt_succeeded" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_notification_attempt_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_notification_error" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_notification_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="start_logging_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stop_logging_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="time_logging_started" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="time_logging_stopped" type="string">
  Returned by the lookup.
</ResponseField>
