---
title: "aws_cloudtrail_query"
description: "`aws_cloudtrail_query` looks up one real CloudTrail Lake query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_query"})

    		ubx.Data(cloudtrail.Query, "example", cloudtrail.QueryConfig{
    			EventDataStore: "example",
    			QueryAlias:     "example",
    			RefreshId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Query } from "@ubx/sdk-aws/aws/data/cloudtrail/query";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_query" });

      data(Query, "example", {
        eventDataStore: "example",
        queryAlias: "example",
        refreshId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import Query, QueryConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_query")

        ubx.data(Query, "example", QueryConfig(
            event_data_store="example",
            query_alias="example",
            refresh_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_data_store" type="string">
  The real CloudTrail event data store this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_alias" type="string">
  The real alias assigned to this query. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="refresh_id" type="string">
  The real, unique identifier of the refresh this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delivery_s3_uri" type="string">
  The real, resolvable URI of the delivery s3 this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="delivery_status" type="string">
  The real, current status of the delivery this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  The real, human-readable message describing the error. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_data_store_owner_account_id" type="string">
  The real, unique identifier of the event data store owner account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="prompt" type="string">
  The real prompt used by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_id" type="string">
  The real, unique identifier of the query this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_statistics" type="object">
  &lt;p&gt;Gets metadata about a query, including the number of events that were matched, the total number of events scanned, the query run time in milliseconds, and the query's creation time.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="bytes_scanned" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="events_matched" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="events_scanned" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="execution_time_in_millis" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="query_status" type="string">
  The real, current status of the query this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_string" type="string">
  The real, raw query string this request runs. **(AI-inferred)** Returned by the lookup.
</ResponseField>
