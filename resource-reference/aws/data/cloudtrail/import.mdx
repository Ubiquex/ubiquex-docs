---
title: "aws_cloudtrail_import"
description: "`aws_cloudtrail_import` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudtrail_import` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudtrail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudtrail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudtrail_import"})

    		ubx.Data(cloudtrail.Import, "example", cloudtrail.ImportConfig{
    			ImportId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Import } from "@ubx/sdk-aws/aws/data/cloudtrail/import";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudtrail_import" });

      data(Import, "example", {
        importId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudtrail import Import, ImportConfig

    def describe():
        ubx.intent("look up aws_cloudtrail_import")

        ubx.data(Import, "example", ImportConfig(
            import_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="import_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="destinations" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="end_event_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="import_source" type="object">
  &lt;p&gt; The import source. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3" type="object" required>
      &lt;p&gt; The settings for the source S3 bucket. &lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="s3_bucket_access_role_arn" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="s3_bucket_region" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="s3_location_uri" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="import_statistics" type="object">
  &lt;p&gt; Provides statistics for the specified &lt;code&gt;ImportID&lt;/code&gt;. CloudTrail does not update import statistics in real-time. Returned values for parameters such as &lt;code&gt;EventsCompleted&lt;/code&gt; may be lower than the actual value, because CloudTrail updates statistics incrementally over the course of the import. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="events_completed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="failed_entries" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="files_completed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="prefixes_completed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="prefixes_found" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="import_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="start_event_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="updated_timestamp" type="string">
  Returned by the lookup.
</ResponseField>
