---
title: "aws_s3_control_caller_access_grants"
description: "`aws_s3_control_caller_access_grants` reads the real S3 Access Grants currently available to the calling principal."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_caller_access_grants"})

    		ubx.Data(s3_control.S3ControlCallerAccessGrants, "example", s3_control.S3ControlCallerAccessGrantsConfig{
    			AccountId:            "example",
    			AllowedByApplication: true,
    			GrantScope:           "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { S3ControlCallerAccessGrants } from "./local-sdk/aws/sdk/typescript/aws/data/s3_control/s3_control_caller_access_grants.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_caller_access_grants" });

      data(S3ControlCallerAccessGrants, "example", {
        accountId: "example",
        allowedByApplication: true,
        grantScope: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlCallerAccessGrants, S3ControlCallerAccessGrantsConfig

    def describe():
        ubx.intent("look up aws_s3_control_caller_access_grants")

        ubx.data(S3ControlCallerAccessGrants, "example", S3ControlCallerAccessGrantsConfig(
            account_id="example",
            allowed_by_application=True,
            grant_scope="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="allowed_by_application" type="bool">
  Whether this action is allowed by the application's own configuration. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="grant_scope" type="string">
  The real scope this grant applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="caller_access_grants_list" type="list(object)">
  The real caller access grantss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
