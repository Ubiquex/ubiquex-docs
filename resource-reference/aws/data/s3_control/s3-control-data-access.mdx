---
title: "aws_s3_control_data_access"
description: "`aws_s3_control_data_access` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_data_access` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_data_access"})

    		ubx.Data(s3_control.S3ControlDataAccess, "example", s3_control.S3ControlDataAccessConfig{
    			AccountId:       "example",
    			Permission:      "example",
    			Target:          "example",
    			AuditContext:    "example",
    			DurationSeconds: 7200,
    			Privilege:       "example",
    			TargetType:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlDataAccess } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_data_access";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_data_access" });

      data(S3ControlDataAccess, "example", {
        accountId: "example",
        permission: "example",
        target: "example",
        auditContext: "example",
        durationSeconds: 7200,
        privilege: "example",
        targetType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlDataAccess, S3ControlDataAccessConfig

    def describe():
        ubx.intent("look up aws_s3_control_data_access")

        ubx.data(S3ControlDataAccess, "example", S3ControlDataAccessConfig(
            account_id="example",
            permission="example",
            target="example",
            audit_context="example",
            duration_seconds=7200,
            privilege="example",
            target_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="audit_context" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="permission" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="privilege" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="target" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="target_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="credentials" type="object">
  &lt;p&gt;The Amazon Web Services Security Token Service temporary credential that S3 Access Grants vends to grantees and client applications. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expiration" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="secret_access_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="grantee" type="object">
  &lt;p&gt;The user, group, or role to which you are granting access. You can grant access to an IAM user or role. If you have added your corporate directory to Amazon Web Services IAM Identity Center and associated your Identity Center instance with your S3 Access Grants instance, the grantee can also be a corporate directory user or group.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="grantee_identifier" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="grantee_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="matched_grant_target" type="string">
  Returned by the lookup.
</ResponseField>
