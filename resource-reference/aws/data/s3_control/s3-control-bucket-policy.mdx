---
title: "aws_s3_control_bucket_policy"
description: "`aws_s3_control_bucket_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_bucket_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_bucket_policy"})

    		ubx.Data(s3_control.S3ControlBucketPolicy, "example", s3_control.S3ControlBucketPolicyConfig{
    			AccountId: "example",
    			Bucket:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlBucketPolicy } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_bucket_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_bucket_policy" });

      data(S3ControlBucketPolicy, "example", {
        accountId: "example",
        bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlBucketPolicy, S3ControlBucketPolicyConfig

    def describe():
        ubx.intent("look up aws_s3_control_bucket_policy")

        ubx.data(S3ControlBucketPolicy, "example", S3ControlBucketPolicyConfig(
            account_id="example",
            bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>
