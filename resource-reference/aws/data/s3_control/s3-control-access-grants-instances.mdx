---
title: "aws_s3_control_access_grants_instances"
description: "`aws_s3_control_access_grants_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_access_grants_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_grants_instances"})

    		ubx.Data(s3_control.S3ControlAccessGrantsInstances, "example", s3_control.S3ControlAccessGrantsInstancesConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessGrantsInstances } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_grants_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_access_grants_instances" });

      data(S3ControlAccessGrantsInstances, "example", {
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessGrantsInstances, S3ControlAccessGrantsInstancesConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_grants_instances")

        ubx.data(S3ControlAccessGrantsInstances, "example", S3ControlAccessGrantsInstancesConfig(
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_grants_instances_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
