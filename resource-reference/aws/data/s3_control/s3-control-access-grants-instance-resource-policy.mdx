---
title: "aws_s3_control_access_grants_instance_resource_policy"
description: "`aws_s3_control_access_grants_instance_resource_policy` reads the real resource-based policy attached to an S3 Access Grants instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_grants_instance_resource_policy"})

    		ubx.Data(s3_control.S3ControlAccessGrantsInstanceResourcePolicy, "example", s3_control.S3ControlAccessGrantsInstanceResourcePolicyConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { S3ControlAccessGrantsInstanceResourcePolicy } from "./local-sdk/aws/sdk/typescript/aws/data/s3_control/s3_control_access_grants_instance_resource_policy.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_s3_control_access_grants_instance_resource_policy",
      });

      data(S3ControlAccessGrantsInstanceResourcePolicy, "example", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessGrantsInstanceResourcePolicy, S3ControlAccessGrantsInstanceResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_grants_instance_resource_policy")

        ubx.data(S3ControlAccessGrantsInstanceResourcePolicy, "example", S3ControlAccessGrantsInstanceResourcePolicyConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization" type="string">
  The real AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
