---
title: "aws_s3_control_access_grants"
description: "`aws_s3_control_access_grants` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_access_grants` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_grants"})

    		ubx.Data(s3_control.S3ControlAccessGrants, "example", s3_control.S3ControlAccessGrantsConfig{
    			AccountId:         "example",
    			ApplicationArn:    "arn:aws:iam::123456789012:policy/example",
    			GrantScope:        "example",
    			GranteeIdentifier: "example",
    			GranteeType:       "example",
    			MaxResults:        1,
    			Permission:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessGrants } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_grants";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_access_grants" });

      data(S3ControlAccessGrants, "example", {
        accountId: "example",
        applicationArn: "arn:aws:iam::123456789012:policy/example",
        grantScope: "example",
        granteeIdentifier: "example",
        granteeType: "example",
        maxResults: 1,
        permission: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessGrants, S3ControlAccessGrantsConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_grants")

        ubx.data(S3ControlAccessGrants, "example", S3ControlAccessGrantsConfig(
            account_id="example",
            application_arn="arn:aws:iam::123456789012:policy/example",
            grant_scope="example",
            grantee_identifier="example",
            grantee_type="example",
            max_results=1,
            permission="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="application_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="grant_scope" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="grantee_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="grantee_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="permission" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_grants_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
