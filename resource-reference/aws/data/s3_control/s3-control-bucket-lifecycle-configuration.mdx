---
title: "aws_s3_control_bucket_lifecycle_configuration"
description: "`aws_s3_control_bucket_lifecycle_configuration` reads the real object lifecycle configuration of an S3 on Outposts bucket."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_bucket_lifecycle_configuration"})

    		ubx.Data(s3_control.S3ControlBucketLifecycleConfiguration, "example", s3_control.S3ControlBucketLifecycleConfigurationConfig{
    			AccountId: "example",
    			Bucket:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlBucketLifecycleConfiguration } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_bucket_lifecycle_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_bucket_lifecycle_configuration" });

      data(S3ControlBucketLifecycleConfiguration, "example", {
        accountId: "example",
        bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlBucketLifecycleConfiguration, S3ControlBucketLifecycleConfigurationConfig

    def describe():
        ubx.intent("look up aws_s3_control_bucket_lifecycle_configuration")

        ubx.data(S3ControlBucketLifecycleConfiguration, "example", S3ControlBucketLifecycleConfigurationConfig(
            account_id="example",
            bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rules" type="list(object)">
  The real rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
