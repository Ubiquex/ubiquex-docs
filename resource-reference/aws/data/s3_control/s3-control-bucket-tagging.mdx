---
title: "aws_s3_control_bucket_tagging"
description: "`aws_s3_control_bucket_tagging` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_bucket_tagging` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_bucket_tagging"})

    		ubx.Data(s3_control.S3ControlBucketTagging, "example", s3_control.S3ControlBucketTaggingConfig{
    			AccountId: "example",
    			Bucket:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlBucketTagging } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_bucket_tagging";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_bucket_tagging" });

      data(S3ControlBucketTagging, "example", {
        accountId: "example",
        bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlBucketTagging, S3ControlBucketTaggingConfig

    def describe():
        ubx.intent("look up aws_s3_control_bucket_tagging")

        ubx.data(S3ControlBucketTagging, "example", S3ControlBucketTaggingConfig(
            account_id="example",
            bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tag_set" type="list(object)">
  Returned by the lookup.
</ResponseField>
