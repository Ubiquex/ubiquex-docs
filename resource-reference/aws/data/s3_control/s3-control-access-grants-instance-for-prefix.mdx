---
title: "aws_s3_control_access_grants_instance_for_prefix"
description: "`aws_s3_control_access_grants_instance_for_prefix` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_access_grants_instance_for_prefix` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_grants_instance_for_prefix"})

    		ubx.Data(s3_control.S3ControlAccessGrantsInstanceForPrefix, "example", s3_control.S3ControlAccessGrantsInstanceForPrefixConfig{
    			AccountId: "example",
    			S3Prefix:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessGrantsInstanceForPrefix } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_grants_instance_for_prefix";

    export default stack("example", () => {
      intent({
        summary: "look up aws_s3_control_access_grants_instance_for_prefix",
      });

      data(S3ControlAccessGrantsInstanceForPrefix, "example", {
        accountId: "example",
        s3Prefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessGrantsInstanceForPrefix, S3ControlAccessGrantsInstanceForPrefixConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_grants_instance_for_prefix")

        ubx.data(S3ControlAccessGrantsInstanceForPrefix, "example", S3ControlAccessGrantsInstanceForPrefixConfig(
            account_id="example",
            s3_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="s3_prefix" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_grants_instance_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="access_grants_instance_id" type="string">
  Returned by the lookup.
</ResponseField>
