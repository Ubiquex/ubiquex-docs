---
title: "aws_s3_control_regional_buckets"
description: "`aws_s3_control_regional_buckets` reads the real S3 on Outposts buckets within a Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_regional_buckets"})

    		ubx.Data(s3_control.S3ControlRegionalBuckets, "example", s3_control.S3ControlRegionalBucketsConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    			OutpostId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlRegionalBuckets } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_regional_buckets";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_regional_buckets" });

      data(S3ControlRegionalBuckets, "example", {
        accountId: "example",
        maxResults: 1,
        outpostId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlRegionalBuckets, S3ControlRegionalBucketsConfig

    def describe():
        ubx.intent("look up aws_s3_control_regional_buckets")

        ubx.data(S3ControlRegionalBuckets, "example", S3ControlRegionalBucketsConfig(
            account_id="example",
            max_results=1,
            outpost_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_id" type="string">
  The real, unique identifier of the AWS Outpost this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="regional_bucket_list" type="list(object)">
  The real regional buckets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
