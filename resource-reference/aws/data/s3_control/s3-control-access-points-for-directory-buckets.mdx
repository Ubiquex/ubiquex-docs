---
title: "aws_s3_control_access_points_for_directory_buckets"
description: "`aws_s3_control_access_points_for_directory_buckets` reads the real S3 Access Points created for S3 Express One Zone directory buckets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_points_for_directory_buckets"})

    		ubx.Data(s3_control.S3ControlAccessPointsForDirectoryBuckets, "example", s3_control.S3ControlAccessPointsForDirectoryBucketsConfig{
    			AccountId:       "example",
    			DirectoryBucket: "example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { S3ControlAccessPointsForDirectoryBuckets } from "./local-sdk/aws/sdk/typescript/aws/data/s3_control/s3_control_access_points_for_directory_buckets.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_s3_control_access_points_for_directory_buckets",
      });

      data(S3ControlAccessPointsForDirectoryBuckets, "example", {
        accountId: "example",
        directoryBucket: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPointsForDirectoryBuckets, S3ControlAccessPointsForDirectoryBucketsConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_points_for_directory_buckets")

        ubx.data(S3ControlAccessPointsForDirectoryBuckets, "example", S3ControlAccessPointsForDirectoryBucketsConfig(
            account_id="example",
            directory_bucket="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="directory_bucket" type="string">
  The real S3 bucket used for the directory. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_point_list" type="list(object)">
  The real access points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
