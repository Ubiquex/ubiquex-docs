---
title: "aws_s3_control_storage_lens_configuration_tagging"
description: "`aws_s3_control_storage_lens_configuration_tagging` reads the real tags applied to an S3 Storage Lens configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_storage_lens_configuration_tagging"})

    		ubx.Data(s3_control.S3ControlStorageLensConfigurationTagging, "example", s3_control.S3ControlStorageLensConfigurationTaggingConfig{
    			AccountId: "example",
    			ConfigId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { S3ControlStorageLensConfigurationTagging } from "./local-sdk/aws/sdk/typescript/aws/data/s3_control/s3_control_storage_lens_configuration_tagging.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_s3_control_storage_lens_configuration_tagging",
      });

      data(S3ControlStorageLensConfigurationTagging, "example", {
        accountId: "example",
        configId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlStorageLensConfigurationTagging, S3ControlStorageLensConfigurationTaggingConfig

    def describe():
        ubx.intent("look up aws_s3_control_storage_lens_configuration_tagging")

        ubx.data(S3ControlStorageLensConfigurationTagging, "example", S3ControlStorageLensConfigurationTaggingConfig(
            account_id="example",
            config_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="config_id" type="string" required>
  The real, unique identifier of the config this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
