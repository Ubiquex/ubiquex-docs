---
title: "aws_s3_control_access_point_scope"
description: "`aws_s3_control_access_point_scope` reads the real scope (permissions and prefixes) restricting an S3 Access Point."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_point_scope"})

    		ubx.Data(s3_control.S3ControlAccessPointScope, "example", s3_control.S3ControlAccessPointScopeConfig{
    			AccountId: "example",
    			Name:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessPointScope } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_point_scope";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_access_point_scope" });

      data(S3ControlAccessPointScope, "example", {
        accountId: "example",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPointScope, S3ControlAccessPointScopeConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_point_scope")

        ubx.data(S3ControlAccessPointScope, "example", S3ControlAccessPointScopeConfig(
            account_id="example",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="scope" type="object">
  &lt;p&gt;You can use the access point scope to restrict access to specific prefixes, API operations, or a combination of both.&lt;/p&gt; &lt;p&gt;For more information, see &lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/userguide/access-points-directory-buckets-manage-scope.html"&gt;Manage the scope of your access points for directory buckets&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="permissions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="prefixes" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
