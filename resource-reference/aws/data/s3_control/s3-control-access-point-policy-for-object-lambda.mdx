---
title: "aws_s3_control_access_point_policy_for_object_lambda"
description: "`aws_s3_control_access_point_policy_for_object_lambda` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_access_point_policy_for_object_lambda` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_point_policy_for_object_lambda"})

    		ubx.Data(s3_control.S3ControlAccessPointPolicyForObjectLambda, "example", s3_control.S3ControlAccessPointPolicyForObjectLambdaConfig{
    			AccountId: "example",
    			Name:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessPointPolicyForObjectLambda } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_point_policy_for_object_lambda";

    export default stack("example", () => {
      intent({
        summary: "look up aws_s3_control_access_point_policy_for_object_lambda",
      });

      data(S3ControlAccessPointPolicyForObjectLambda, "example", {
        accountId: "example",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPointPolicyForObjectLambda, S3ControlAccessPointPolicyForObjectLambdaConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_point_policy_for_object_lambda")

        ubx.data(S3ControlAccessPointPolicyForObjectLambda, "example", S3ControlAccessPointPolicyForObjectLambdaConfig(
            account_id="example",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>
