---
title: "aws_s3_control_access_points"
description: "`data_aws_s3_control_access_points` looks up a real, already-existing `aws_s3_access_point`."
---

It creates a real, named network endpoint with its own access policy for an `aws_s3_bucket`, letting different applications reach the same bucket through different access points instead of managing one increasingly complex bucket policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_points"})

    		ubx.Data(s3_control.S3ControlAccessPoints, "example", s3_control.S3ControlAccessPointsConfig{
    			AccountId:      "example",
    			Bucket:         "example",
    			DataSourceId:   "example",
    			DataSourceType: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { S3ControlAccessPoints } from "./local-sdk/aws/sdk/typescript/aws/data/s3_control/s3_control_access_points.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_access_points" });

      data(S3ControlAccessPoints, "example", {
        accountId: "example",
        bucket: "example",
        dataSourceId: "example",
        dataSourceType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPoints, S3ControlAccessPointsConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_points")

        ubx.data(S3ControlAccessPoints, "example", S3ControlAccessPointsConfig(
            account_id="example",
            bucket="example",
            data_source_id="example",
            data_source_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bucket" type="string">
  The real name of the S3 bucket this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_source_id" type="string">
  The real, unique identifier of the data source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_source_type" type="string">
  The real, discriminating type of the data source this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_point_list" type="list(object)">
  The real access points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
