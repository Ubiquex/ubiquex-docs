---
title: "aws_s3_control_access_points_for_object_lambda"
description: "`aws_s3_control_access_points_for_object_lambda` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_access_points_for_object_lambda` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_points_for_object_lambda"})

    		ubx.Data(s3_control.S3ControlAccessPointsForObjectLambda, "example", s3_control.S3ControlAccessPointsForObjectLambdaConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessPointsForObjectLambda } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_points_for_object_lambda";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_access_points_for_object_lambda" });

      data(S3ControlAccessPointsForObjectLambda, "example", {
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPointsForObjectLambda, S3ControlAccessPointsForObjectLambdaConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_points_for_object_lambda")

        ubx.data(S3ControlAccessPointsForObjectLambda, "example", S3ControlAccessPointsForObjectLambdaConfig(
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="object_lambda_access_point_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
