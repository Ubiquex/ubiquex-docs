---
title: "aws_s3_control_access_points_for_object_lambda"
description: "`aws_s3_control_access_points_for_object_lambda` looks up a real S3 Object Lambda Access Point..."
---

`aws_s3_control_access_points_for_object_lambda` looks up a real S3 Object Lambda Access Point, transforming object data through a Lambda function on retrieval.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_points_for_object_lambda"})

    		ubx.Data(s3_control.S3ControlAccessPointsForObjectLambda, "example", s3_control.S3ControlAccessPointsForObjectLambdaConfig{
    			AccountId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { S3ControlAccessPointsForObjectLambda } from "./local-sdk/aws/sdk/typescript/aws/data/s3_control/s3_control_access_points_for_object_lambda.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_s3_control_access_points_for_object_lambda" });

      data(S3ControlAccessPointsForObjectLambda, "example", {
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPointsForObjectLambda, S3ControlAccessPointsForObjectLambdaConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_points_for_object_lambda")

        ubx.data(S3ControlAccessPointsForObjectLambda, "example", S3ControlAccessPointsForObjectLambdaConfig(
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="object_lambda_access_point_list" type="list(object)">
  The real object lambda access points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
