---
title: "aws_s3_control_access_point_configuration_for_object_lambda"
description: "`aws_s3_control_access_point_configuration_for_object_lambda` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_s3_control_access_point_configuration_for_object_lambda` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3_control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/s3_control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_s3_control_access_point_configuration_for_object_lambda"})

    		ubx.Data(s3_control.S3ControlAccessPointConfigurationForObjectLambda, "example", s3_control.S3ControlAccessPointConfigurationForObjectLambdaConfig{
    			AccountId: "example",
    			Name:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { S3ControlAccessPointConfigurationForObjectLambda } from "@ubx/sdk-aws/aws/data/s3_control/s3_control_access_point_configuration_for_object_lambda";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_s3_control_access_point_configuration_for_object_lambda",
      });

      data(S3ControlAccessPointConfigurationForObjectLambda, "example", {
        accountId: "example",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.s3_control import S3ControlAccessPointConfigurationForObjectLambda, S3ControlAccessPointConfigurationForObjectLambdaConfig

    def describe():
        ubx.intent("look up aws_s3_control_access_point_configuration_for_object_lambda")

        ubx.data(S3ControlAccessPointConfigurationForObjectLambda, "example", S3ControlAccessPointConfigurationForObjectLambdaConfig(
            account_id="example",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration" type="object">
  &lt;p&gt;A configuration used when creating an Object Lambda Access Point.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="allowed_features" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cloud_watch_metrics_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="supporting_access_point" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="transformation_configurations" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
