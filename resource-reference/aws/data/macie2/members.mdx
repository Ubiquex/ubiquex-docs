---
title: "aws_macie2_members"
description: "`aws_macie2_members` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_macie2_members` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_members"})

    		ubx.Data(macie2.Members, "example", macie2.MembersConfig{
    			MaxResults:     1,
    			OnlyAssociated: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Members } from "@ubx/sdk-aws/aws/data/macie2/members";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_members" });

      data(Members, "example", {
        maxResults: 1,
        onlyAssociated: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import Members, MembersConfig

    def describe():
        ubx.intent("look up aws_macie2_members")

        ubx.data(Members, "example", MembersConfig(
            max_results=1,
            only_associated="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="only_associated" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="members" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
