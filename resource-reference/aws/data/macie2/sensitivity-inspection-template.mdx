---
title: "aws_macie2_sensitivity_inspection_template"
description: "`aws_macie2_sensitivity_inspection_template` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_macie2_sensitivity_inspection_template` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_sensitivity_inspection_template"})

    		ubx.Data(macie2.SensitivityInspectionTemplate, "example", macie2.SensitivityInspectionTemplateConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SensitivityInspectionTemplate } from "@ubx/sdk-aws/aws/data/macie2/sensitivity_inspection_template";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_sensitivity_inspection_template" });

      data(SensitivityInspectionTemplate, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import SensitivityInspectionTemplate, SensitivityInspectionTemplateConfig

    def describe():
        ubx.intent("look up aws_macie2_sensitivity_inspection_template")

        ubx.data(SensitivityInspectionTemplate, "example", SensitivityInspectionTemplateConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="excludes" type="object">
  &lt;p&gt;Specifies managed data identifiers to exclude (not use) when performing automated sensitive data discovery. For information about the managed data identifiers that Amazon Macie currently provides, see &lt;a href="https://docs.aws.amazon.com/macie/latest/user/managed-data-identifiers.html"&gt;Using managed data identifiers&lt;/a&gt; in the &lt;i&gt;Amazon Macie User Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="managed_data_identifier_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="includes" type="object">
  &lt;p&gt;Specifies the allow lists, custom data identifiers, and managed data identifiers to include (use) when performing automated sensitive data discovery. The configuration must specify at least one custom data identifier or managed data identifier. For information about the managed data identifiers that Amazon Macie currently provides, see &lt;a href="https://docs.aws.amazon.com/macie/latest/user/managed-data-identifiers.html"&gt;Using managed data identifiers&lt;/a&gt; in the &lt;i&gt;Amazon Macie User Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="allow_list_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="custom_data_identifier_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="managed_data_identifier_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sensitivity_inspection_template_id" type="string">
  &lt;p&gt;The unique identifier for the sensitivity inspection template.&lt;/p&gt; Returned by the lookup.
</ResponseField>
