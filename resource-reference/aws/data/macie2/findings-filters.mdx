---
title: "aws_macie2_findings_filters"
description: "`data_aws_macie2_findings_filters` looks up a real, already-existing `aws_macie_findings_filter`."
---

It defines real custom criteria, severity, finding type, affected resource, that either narrows which Macie findings a query returns or automatically archives findings matching those criteria, so routine noise doesn't have to be triaged by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_findings_filters"})

    		ubx.Data(macie2.FindingsFilters, "example", macie2.FindingsFiltersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FindingsFilters } from "./local-sdk/aws/sdk/typescript/aws/data/macie2/findings_filters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_findings_filters" });

      data(FindingsFilters, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import FindingsFilters, FindingsFiltersConfig

    def describe():
        ubx.intent("look up aws_macie2_findings_filters")

        ubx.data(FindingsFilters, "example", FindingsFiltersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings_filter_list_items" type="list(object)">
  The real findings-filter list entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
