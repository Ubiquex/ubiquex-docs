---
title: "aws_macie2_resource_profile_artifacts"
description: "`aws_macie2_resource_profile_artifacts` reads the real, sampled objects backing a Macie resource profile's own sensitive data findings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_resource_profile_artifacts"})

    		ubx.Data(macie2.ResourceProfileArtifacts, "example", macie2.ResourceProfileArtifactsConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceProfileArtifacts } from "@ubx/sdk-aws/aws/data/macie2/resource_profile_artifacts";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_resource_profile_artifacts" });

      data(ResourceProfileArtifacts, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import ResourceProfileArtifacts, ResourceProfileArtifactsConfig

    def describe():
        ubx.intent("look up aws_macie2_resource_profile_artifacts")

        ubx.data(ResourceProfileArtifacts, "example", ResourceProfileArtifactsConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="artifacts" type="list(object)">
  The real artifacts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
