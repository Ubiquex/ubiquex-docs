---
title: "aws_macie2_sensitivity_inspection_templates"
description: "`aws_macie2_sensitivity_inspection_templates` reads the real sensitivity inspection templates configured for Macie."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_sensitivity_inspection_templates"})

    		ubx.Data(macie2.SensitivityInspectionTemplates, "example", macie2.SensitivityInspectionTemplatesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SensitivityInspectionTemplates } from "@ubx/sdk-aws/aws/data/macie2/sensitivity_inspection_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_sensitivity_inspection_templates" });

      data(SensitivityInspectionTemplates, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import SensitivityInspectionTemplates, SensitivityInspectionTemplatesConfig

    def describe():
        ubx.intent("look up aws_macie2_sensitivity_inspection_templates")

        ubx.data(SensitivityInspectionTemplates, "example", SensitivityInspectionTemplatesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sensitivity_inspection_templates" type="list(object)">
  The real Amazon Macie sensitivity inspection templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
