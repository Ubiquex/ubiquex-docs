---
title: "aws_macie2_sensitive_data_occurrences_availability"
description: "`aws_macie2_sensitive_data_occurrences_availability` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_macie2_sensitive_data_occurrences_availability` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_sensitive_data_occurrences_availability"})

    		ubx.Data(macie2.SensitiveDataOccurrencesAvailability, "example", macie2.SensitiveDataOccurrencesAvailabilityConfig{
    			FindingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SensitiveDataOccurrencesAvailability } from "@ubx/sdk-aws/aws/data/macie2/sensitive_data_occurrences_availability";

    export default stack("example", () => {
      intent({
        summary: "look up aws_macie2_sensitive_data_occurrences_availability",
      });

      data(SensitiveDataOccurrencesAvailability, "example", {
        findingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import SensitiveDataOccurrencesAvailability, SensitiveDataOccurrencesAvailabilityConfig

    def describe():
        ubx.intent("look up aws_macie2_sensitive_data_occurrences_availability")

        ubx.data(SensitiveDataOccurrencesAvailability, "example", SensitiveDataOccurrencesAvailabilityConfig(
            finding_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="finding_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="code" type="string">
  &lt;p&gt;Specifies whether occurrences of sensitive data can be retrieved for a finding. Possible values are:&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="reasons" type="list(string)">
  Returned by the lookup.
</ResponseField>
