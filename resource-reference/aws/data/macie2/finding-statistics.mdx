---
title: "aws_macie2_finding_statistics"
description: "`aws_macie2_finding_statistics` reads real, aggregated statistics (grouped by severity or type) of Macie findings."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_finding_statistics"})

    		ubx.Data(macie2.FindingStatistics, "example", macie2.FindingStatisticsConfig{
    			GroupBy:         "example",
    			FindingCriteria: map[string]any{"criterion": map[string]any{"key": "value"}},
    			Size:            1,
    			SortCriteria:    map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FindingStatistics } from "./local-sdk/aws/sdk/typescript/aws/data/macie2/finding_statistics.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_finding_statistics" });

      data(FindingStatistics, "example", {
        groupBy: "example",
        findingCriteria: { criterion: { key: "value" } },
        size: 1,
        sortCriteria: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import FindingStatistics, FindingStatisticsConfig

    def describe():
        ubx.intent("look up aws_macie2_finding_statistics")

        ubx.data(FindingStatistics, "example", FindingStatisticsConfig(
            group_by="example",
            finding_criteria={"criterion": {"key": "value"}},
            size=1,
            sort_criteria={"attribute_name": "example-attribute-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="finding_criteria" type="object">
  &lt;p&gt;Specifies, as a map, one or more property-based conditions that filter the results of a query for findings.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="criterion" type="map(object)">
      &lt;p&gt;Specifies a condition that defines a property, operator, and one or more values to filter the results of a query for findings. The number of values depends on the property and operator specified by the condition. For information about defining filter conditions, see &lt;a href="https://docs.aws.amazon.com/macie/latest/user/findings-filter-basics.html"&gt;Fundamentals of filtering findings&lt;/a&gt; in the &lt;i&gt;Amazon Macie User Guide&lt;/i&gt;.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="group_by" type="string" required>
  The real field results are grouped by. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="size" type="number">
  The real size of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_criteria" type="object">
  &lt;p&gt;Specifies criteria for sorting the results of a query that retrieves aggregated statistical data about findings.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      &lt;p&gt;The grouping to sort the results by. Valid values are:&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="counts_by_group" type="list(object)">
  The real counts by group. **(AI-inferred)** Returned by the lookup.
</ResponseField>
