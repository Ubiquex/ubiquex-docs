---
title: "aws_macie2_buckets"
description: "`aws_macie2_buckets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_macie2_buckets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_buckets"})

    		ubx.Data(macie2.Buckets, "example", macie2.BucketsConfig{
    			Criteria:     map[string]string{"managed-by": "ubx"},
    			MaxResults:   1,
    			SortCriteria: map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Buckets } from "@ubx/sdk-aws/aws/data/macie2/buckets";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_buckets" });

      data(Buckets, "example", {
        criteria: { "managed-by": "ubx" },
        maxResults: 1,
        sortCriteria: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import Buckets, BucketsConfig

    def describe():
        ubx.intent("look up aws_macie2_buckets")

        ubx.data(Buckets, "example", BucketsConfig(
            criteria={"managed-by": "ubx"},
            max_results=1,
            sort_criteria={"attribute_name": "example-attribute-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="criteria" type="map(object)">
  &lt;p&gt;Specifies, as a map, one or more property-based conditions that filter the results of a query for information about S3 buckets.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_criteria" type="object">
  &lt;p&gt;Specifies criteria for sorting the results of a query for information about S3 buckets.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="buckets" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
