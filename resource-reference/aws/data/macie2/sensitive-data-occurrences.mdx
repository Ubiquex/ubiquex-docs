---
title: "aws_macie2_sensitive_data_occurrences"
description: "`aws_macie2_sensitive_data_occurrences` reads real, specific occurrences of sensitive data detected within an S3 object."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_sensitive_data_occurrences"})

    		ubx.Data(macie2.SensitiveDataOccurrences, "example", macie2.SensitiveDataOccurrencesConfig{
    			FindingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SensitiveDataOccurrences } from "@ubx/sdk-aws/aws/data/macie2/sensitive_data_occurrences";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_sensitive_data_occurrences" });

      data(SensitiveDataOccurrences, "example", {
        findingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import SensitiveDataOccurrences, SensitiveDataOccurrencesConfig

    def describe():
        ubx.intent("look up aws_macie2_sensitive_data_occurrences")

        ubx.data(SensitiveDataOccurrences, "example", SensitiveDataOccurrencesConfig(
            finding_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="finding_id" type="string" required>
  The real, unique identifier of the finding this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="error" type="string">
  The real error encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sensitive_data_occurrences" type="map(list(object))">
  &lt;p&gt;Specifies a type of sensitive data reported by a finding and provides occurrences of the specified type of sensitive data.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;The status of a request to retrieve occurrences of sensitive data reported by a finding. Possible values are:&lt;/p&gt; Returned by the lookup.
</ResponseField>
