---
title: "aws_macie2_reveal_configuration"
description: "`aws_macie2_reveal_configuration` reads the real configuration controlling whether Macie can reveal actual samples of detected sensitive data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_reveal_configuration"})

    		ubx.Data(macie2.RevealConfiguration, "example", macie2.RevealConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RevealConfiguration } from "@ubx/sdk-aws/aws/data/macie2/reveal_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_reveal_configuration" });

      data(RevealConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import RevealConfiguration, RevealConfigurationConfig

    def describe():
        ubx.intent("look up aws_macie2_reveal_configuration")

        ubx.data(RevealConfiguration, "example", RevealConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration" type="object">
  &lt;p&gt;Specifies the status of the Amazon Macie configuration for retrieving occurrences of sensitive data reported by findings, and the Key Management Service (KMS) key to use to encrypt sensitive data that's retrieved. When you enable the configuration for the first time, your request must specify an KMS key. Otherwise, an error occurs.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      &lt;p&gt;The status of the configuration for retrieving occurrences of sensitive data reported by findings. Valid values are:&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="retrieval_configuration" type="object">
  &lt;p&gt;Provides information about the access method and settings that are used to retrieve occurrences of sensitive data reported by findings.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="external_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="retrieval_mode" type="string" required>
      &lt;p&gt;The access method to use when retrieving occurrences of sensitive data reported by findings. Valid values are:&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="role_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
