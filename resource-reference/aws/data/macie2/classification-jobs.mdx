---
title: "aws_macie2_classification_jobs"
description: "`aws_macie2_classification_jobs` looks up a real Macie sensitive data discovery job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_classification_jobs"})

    		ubx.Data(macie2.ClassificationJobs, "example", macie2.ClassificationJobsConfig{
    			FilterCriteria: map[string]any{"excludes": []map[string]any{{"comparator": "example"}}},
    			MaxResults:     1,
    			SortCriteria:   map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ClassificationJobs } from "./local-sdk/aws/sdk/typescript/aws/data/macie2/classification_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_classification_jobs" });

      data(ClassificationJobs, "example", {
        filterCriteria: { excludes: [{ comparator: "example" }] },
        maxResults: 1,
        sortCriteria: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import ClassificationJobs, ClassificationJobsConfig

    def describe():
        ubx.intent("look up aws_macie2_classification_jobs")

        ubx.data(ClassificationJobs, "example", ClassificationJobsConfig(
            filter_criteria={"excludes": [{"comparator": "example"}]},
            max_results=1,
            sort_criteria={"attribute_name": "example-attribute-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_criteria" type="object">
  &lt;p&gt;Specifies criteria for filtering the results of a request for information about classification jobs.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="excludes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="includes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_criteria" type="object">
  &lt;p&gt;Specifies criteria for sorting the results of a request for information about classification jobs.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      &lt;p&gt;The property to sort the results by. Valid values are:&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
