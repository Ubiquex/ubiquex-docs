---
title: "aws_macie2_classification_scopes"
description: "`aws_macie2_classification_scopes` reads the real classification scopes configured for Macie."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_classification_scopes"})

    		ubx.Data(macie2.ClassificationScopes, "example", macie2.ClassificationScopesConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClassificationScopes } from "@ubx/sdk-aws/aws/data/macie2/classification_scopes";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_classification_scopes" });

      data(ClassificationScopes, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import ClassificationScopes, ClassificationScopesConfig

    def describe():
        ubx.intent("look up aws_macie2_classification_scopes")

        ubx.data(ClassificationScopes, "example", ClassificationScopesConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="classification_scopes" type="list(object)">
  The real Amazon Macie classification scopes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
