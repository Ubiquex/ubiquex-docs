---
title: "aws_macie2_tags_for_resource"
description: "`aws_macie2_tags_for_resource` reads the real tags applied to a Macie resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_tags_for_resource"})

    		ubx.Data(macie2.TagsForResource, "example", macie2.TagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForResource } from "@ubx/sdk-aws/aws/data/macie2/tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_tags_for_resource" });

      data(TagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import TagsForResource, TagsForResourceConfig

    def describe():
        ubx.intent("look up aws_macie2_tags_for_resource")

        ubx.data(TagsForResource, "example", TagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  &lt;p&gt;A string-to-string map of key-value pairs that specifies the tags (keys and values) for an Amazon Macie resource.&lt;/p&gt; Returned by the lookup.
</ResponseField>
