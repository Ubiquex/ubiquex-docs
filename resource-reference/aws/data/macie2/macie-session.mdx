---
title: "aws_macie2_macie_session"
description: "`aws_macie2_macie_session` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_macie2_macie_session` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_macie_session"})

    		ubx.Data(macie2.MacieSession, "example", macie2.MacieSessionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MacieSession } from "@ubx/sdk-aws/aws/data/macie2/macie_session";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_macie_session" });

      data(MacieSession, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import MacieSession, MacieSessionConfig

    def describe():
        ubx.intent("look up aws_macie2_macie_session")

        ubx.data(MacieSession, "example", MacieSessionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="finding_publishing_frequency" type="string">
  &lt;p&gt;The frequency with which Amazon Macie publishes updates to policy findings for an account. This includes publishing updates to Security Hub and Amazon EventBridge (formerly Amazon CloudWatch Events). For more information, see &lt;a href="https://docs.aws.amazon.com/macie/latest/user/findings-monitor.html"&gt;Monitoring and processing findings&lt;/a&gt; in the &lt;i&gt;Amazon Macie User Guide&lt;/i&gt;. Valid values are:&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="service_role" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;The status of an Amazon Macie account. Valid values are:&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Returned by the lookup.
</ResponseField>
