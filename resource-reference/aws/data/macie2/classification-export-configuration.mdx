---
title: "aws_macie2_classification_export_configuration"
description: "`aws_macie2_classification_export_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_macie2_classification_export_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_classification_export_configuration"})

    		ubx.Data(macie2.ClassificationExportConfiguration, "example", macie2.ClassificationExportConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClassificationExportConfiguration } from "@ubx/sdk-aws/aws/data/macie2/classification_export_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_classification_export_configuration" });

      data(ClassificationExportConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import ClassificationExportConfiguration, ClassificationExportConfigurationConfig

    def describe():
        ubx.intent("look up aws_macie2_classification_export_configuration")

        ubx.data(ClassificationExportConfiguration, "example", ClassificationExportConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration" type="object">
  &lt;p&gt;Specifies where to store data classification results, and the encryption settings to use when storing results in that location. The location must be an S3 general purpose bucket.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3_destination" type="object">
      &lt;p&gt;Specifies an S3 bucket to store data classification results in, and the encryption settings to use when storing results in that bucket. The bucket must be an existing general purpose bucket. It can be a bucket in your own account or a bucket that another account owns. If another account owns the bucket, you must specify both the unique identifier for the account and the name of the bucket.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="expected_bucket_owner" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="key_prefix" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="kms_key_arn" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
