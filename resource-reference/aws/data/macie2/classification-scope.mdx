---
title: "aws_macie2_classification_scope"
description: "`aws_macie2_classification_scope` reads the real, account-wide S3 bucket exclusion scope for Macie's own automated discovery."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_classification_scope"})

    		ubx.Data(macie2.ClassificationScope, "example", macie2.ClassificationScopeConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClassificationScope } from "@ubx/sdk-aws/aws/data/macie2/classification_scope";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_classification_scope" });

      data(ClassificationScope, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import ClassificationScope, ClassificationScopeConfig

    def describe():
        ubx.intent("look up aws_macie2_classification_scope")

        ubx.data(ClassificationScope, "example", ClassificationScopeConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="name" type="string">
  &lt;p&gt;The name of the classification scope.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="s3" type="object">
  &lt;p&gt;Specifies the S3 buckets that are excluded from automated sensitive data discovery for an Amazon Macie account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="excludes" type="object" required>
      &lt;p&gt;Specifies the names of the S3 buckets that are excluded from automated sensitive data discovery.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket_names" type="list(string)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
