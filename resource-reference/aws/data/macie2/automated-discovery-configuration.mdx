---
title: "aws_macie2_automated_discovery_configuration"
description: "`aws_macie2_automated_discovery_configuration` reads the real, account-wide configuration of Macie's own automated sensitive data discovery."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_macie2_automated_discovery_configuration"})

    		ubx.Data(macie2.AutomatedDiscoveryConfiguration, "example", macie2.AutomatedDiscoveryConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutomatedDiscoveryConfiguration } from "@ubx/sdk-aws/aws/data/macie2/automated_discovery_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_macie2_automated_discovery_configuration" });

      data(AutomatedDiscoveryConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.macie2 import AutomatedDiscoveryConfiguration, AutomatedDiscoveryConfigurationConfig

    def describe():
        ubx.intent("look up aws_macie2_automated_discovery_configuration")

        ubx.data(AutomatedDiscoveryConfiguration, "example", AutomatedDiscoveryConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="auto_enable_organization_members" type="string">
  &lt;p&gt;Specifies whether to automatically enable automated sensitive data discovery for accounts that are part of an organization in Amazon Macie. Valid values are:&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="classification_scope_id" type="string">
  &lt;p&gt;The unique identifier the classification scope.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="disabled_at" type="string">
  &lt;p&gt;Specifies a date and time in UTC and extended ISO 8601 format.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="first_enabled_at" type="string">
  &lt;p&gt;Specifies a date and time in UTC and extended ISO 8601 format.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  &lt;p&gt;Specifies a date and time in UTC and extended ISO 8601 format.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="sensitivity_inspection_template_id" type="string">
  &lt;p&gt;The unique identifier for the sensitivity inspection template.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;The status of the automated sensitive data discovery configuration for an organization in Amazon Macie or a standalone Macie account. Valid values are:&lt;/p&gt; Returned by the lookup.
</ResponseField>
