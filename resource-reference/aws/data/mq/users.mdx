---
title: "aws_mq_users"
description: "`aws_mq_users` looks up a real Amazon MQ broker user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_users"})

    		ubx.Data(mq.Users, "example", mq.UsersConfig{
    			BrokerId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Users } from "@ubx/sdk-aws/aws/data/mq/users";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_users" });

      data(Users, "example", {
        brokerId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mq import Users, UsersConfig

    def describe():
        ubx.intent("look up aws_mq_users")

        ubx.data(Users, "example", UsersConfig(
            broker_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="broker_id" type="string" required>
  The real, unique identifier of the broker this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
