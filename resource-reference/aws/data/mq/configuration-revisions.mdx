---
title: "aws_mq_configuration_revisions"
description: "`aws_mq_configuration_revisions` reads the real, saved revisions of an Amazon MQ configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_configuration_revisions"})

    		ubx.Data(mq.ConfigurationRevisions, "example", mq.ConfigurationRevisionsConfig{
    			ConfigurationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationRevisions } from "@ubx/sdk-aws/aws/data/mq/configuration_revisions";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_configuration_revisions" });

      data(ConfigurationRevisions, "example", {
        configurationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mq import ConfigurationRevisions, ConfigurationRevisionsConfig

    def describe():
        ubx.intent("look up aws_mq_configuration_revisions")

        ubx.data(ConfigurationRevisions, "example", ConfigurationRevisionsConfig(
            configuration_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_id" type="string" required>
  The real, unique identifier of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="revisions" type="list(object)">
  The real revisions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
