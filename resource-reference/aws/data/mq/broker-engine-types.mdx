---
title: "aws_mq_broker_engine_types"
description: "`aws_mq_broker_engine_types` reads the real, available Amazon MQ broker engine types (ActiveMQ, RabbitMQ) and versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_broker_engine_types"})

    		ubx.Data(mq.BrokerEngineTypes, "example", mq.BrokerEngineTypesConfig{
    			EngineType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BrokerEngineTypes } from "./local-sdk/aws/sdk/typescript/aws/data/mq/broker_engine_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_broker_engine_types" });

      data(BrokerEngineTypes, "example", {
        engineType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mq import BrokerEngineTypes, BrokerEngineTypesConfig

    def describe():
        ubx.intent("look up aws_mq_broker_engine_types")

        ubx.data(BrokerEngineTypes, "example", BrokerEngineTypesConfig(
            engine_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_type" type="string">
  The real database or compute engine type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="broker_engine_types" type="list(object)">
  The real, discriminating types of broker engines this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
