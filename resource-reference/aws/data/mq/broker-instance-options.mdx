---
title: "aws_mq_broker_instance_options"
description: "`aws_mq_broker_instance_options` reads the real, available Amazon MQ broker instance types for a given engine."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_broker_instance_options"})

    		ubx.Data(mq.BrokerInstanceOptions, "example", mq.BrokerInstanceOptionsConfig{
    			EngineType:       "example",
    			HostInstanceType: "example",
    			StorageType:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BrokerInstanceOptions } from "./local-sdk/aws/sdk/typescript/aws/data/mq/broker_instance_options.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_broker_instance_options" });

      data(BrokerInstanceOptions, "example", {
        engineType: "example",
        hostInstanceType: "example",
        storageType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.mq import BrokerInstanceOptions, BrokerInstanceOptionsConfig

    def describe():
        ubx.intent("look up aws_mq_broker_instance_options")

        ubx.data(BrokerInstanceOptions, "example", BrokerInstanceOptionsConfig(
            engine_type="example",
            host_instance_type="example",
            storage_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_type" type="string">
  The real database or compute engine type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="host_instance_type" type="string">
  The real, discriminating type of the host instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="storage_type" type="string">
  The real, discriminating type of the storage this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="broker_instance_options" type="list(object)">
  The real, available options for the broker instance. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
