---
title: "aws_mq_brokers"
description: "`aws_mq_brokers` looks up a real Amazon MQ broker."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_brokers"})

    		ubx.Data(mq.Brokers, "example", mq.BrokersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Brokers } from "@ubx/sdk-aws/aws/data/mq/brokers";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_brokers" });

      data(Brokers, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mq import Brokers, BrokersConfig

    def describe():
        ubx.intent("look up aws_mq_brokers")

        ubx.data(Brokers, "example", BrokersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="broker_summaries" type="list(object)">
  The real, summarized brokers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
