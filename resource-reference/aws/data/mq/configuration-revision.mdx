---
title: "aws_mq_configuration_revision"
description: "`aws_mq_configuration_revision` looks up one real, saved revision of an Amazon MQ configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_configuration_revision"})

    		ubx.Data(mq.ConfigurationRevision, "example", mq.ConfigurationRevisionConfig{
    			ConfigurationId:       "example",
    			ConfigurationRevision: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationRevision } from "@ubx/sdk-aws/aws/data/mq/configuration_revision";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_configuration_revision" });

      data(ConfigurationRevision, "example", {
        configurationId: "example",
        configurationRevision: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mq import ConfigurationRevision, ConfigurationRevisionConfig

    def describe():
        ubx.intent("look up aws_mq_configuration_revision")

        ubx.data(ConfigurationRevision, "example", ConfigurationRevisionConfig(
            configuration_id="example",
            configuration_revision="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_id" type="string" required>
  The real, unique identifier of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="configuration_revision" type="string" required>
  The real revision of the configuration. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data" type="string">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
