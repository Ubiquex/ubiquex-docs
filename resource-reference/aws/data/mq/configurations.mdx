---
title: "aws_mq_configurations"
description: "`aws_mq_configurations` looks up a real Amazon MQ configuration, a set of broker engine properties."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_mq_configurations"})

    		ubx.Data(mq.Configurations, "example", mq.ConfigurationsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Configurations } from "@ubx/sdk-aws/aws/data/mq/configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_mq_configurations" });

      data(Configurations, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.mq import Configurations, ConfigurationsConfig

    def describe():
        ubx.intent("look up aws_mq_configurations")

        ubx.data(Configurations, "example", ConfigurationsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configurations" type="list(object)">
  The real configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
