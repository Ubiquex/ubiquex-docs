---
title: "aws_guardduty_findings"
description: "`aws_guardduty_findings` looks up a real GuardDuty finding, a detected threat."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_guardduty_findings"})

    		ubx.Data(guardduty.Findings, "example", guardduty.FindingsConfig{
    			DetectorId:   "example",
    			FindingIds:   []string{"example"},
    			SortCriteria: map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Findings } from "./local-sdk/aws/sdk/typescript/aws/data/guardduty/findings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_guardduty_findings" });

      data(Findings, "example", {
        detectorId: "example",
        findingIds: ["example"],
        sortCriteria: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.guardduty import Findings, FindingsConfig

    def describe():
        ubx.intent("look up aws_guardduty_findings")

        ubx.data(Findings, "example", FindingsConfig(
            detector_id="example",
            finding_ids=["example"],
            sort_criteria={"attribute_name": "example-attribute-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detector_id" type="string" required>
  The real, unique identifier of the detector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="finding_ids" type="list(string)" required>
  The real, unique identifiers of the findings this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_criteria" type="object">
  &lt;p&gt;Contains information about the criteria used for sorting findings.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  The real findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
