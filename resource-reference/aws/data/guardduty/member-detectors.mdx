---
title: "aws_guardduty_member_detectors"
description: "`aws_guardduty_member_detectors` reads the real detector configuration of a GuardDuty member account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_guardduty_member_detectors"})

    		ubx.Data(guardduty.MemberDetectors, "example", guardduty.MemberDetectorsConfig{
    			AccountIds: []string{"example"},
    			DetectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MemberDetectors } from "@ubx/sdk-aws/aws/data/guardduty/member_detectors";

    export default stack("example", () => {
      intent({ summary: "look up aws_guardduty_member_detectors" });

      data(MemberDetectors, "example", {
        accountIds: ["example"],
        detectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.guardduty import MemberDetectors, MemberDetectorsConfig

    def describe():
        ubx.intent("look up aws_guardduty_member_detectors")

        ubx.data(MemberDetectors, "example", MemberDetectorsConfig(
            account_ids=["example"],
            detector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)" required>
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="detector_id" type="string" required>
  The real, unique identifier of the detector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="member_data_source_configurations" type="list(object)">
  The real, configured Amazon GuardDuty data sources for member accounts. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unprocessed_accounts" type="list(object)">
  The real accounts that were not successfully processed. **(AI-inferred)** Returned by the lookup.
</ResponseField>
