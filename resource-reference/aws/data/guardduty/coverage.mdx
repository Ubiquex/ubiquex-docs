---
title: "aws_guardduty_coverage"
description: "`aws_guardduty_coverage` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_guardduty_coverage` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_guardduty_coverage"})

    		ubx.Data(guardduty.Coverage, "example", guardduty.CoverageConfig{
    			DetectorId:     "example",
    			FilterCriteria: map[string]any{"filter_criterion": []map[string]any{{"criterion_key": "example"}}},
    			MaxResults:     1,
    			SortCriteria:   map[string]any{"attribute_name": "example-attribute-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Coverage } from "@ubx/sdk-aws/aws/data/guardduty/coverage";

    export default stack("example", () => {
      intent({ summary: "look up aws_guardduty_coverage" });

      data(Coverage, "example", {
        detectorId: "example",
        filterCriteria: { filterCriterion: [{ criterionKey: "example" }] },
        maxResults: 1,
        sortCriteria: { attributeName: "example-attribute-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.guardduty import Coverage, CoverageConfig

    def describe():
        ubx.intent("look up aws_guardduty_coverage")

        ubx.data(Coverage, "example", CoverageConfig(
            detector_id="example",
            filter_criteria={"filter_criterion": [{"criterion_key": "example"}]},
            max_results=1,
            sort_criteria={"attribute_name": "example-attribute-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detector_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_criteria" type="object">
  &lt;p&gt;Represents the criteria used in the filter.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="filter_criterion" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_criteria" type="object">
  &lt;p&gt;Information about the sorting criteria used in the coverage statistics.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  Returned by the lookup.
</ResponseField>
