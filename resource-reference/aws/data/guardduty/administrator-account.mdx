---
title: "aws_guardduty_administrator_account"
description: "`aws_guardduty_administrator_account` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_guardduty_administrator_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_guardduty_administrator_account"})

    		ubx.Data(guardduty.AdministratorAccount, "example", guardduty.AdministratorAccountConfig{
    			DetectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AdministratorAccount } from "@ubx/sdk-aws/aws/data/guardduty/administrator_account";

    export default stack("example", () => {
      intent({ summary: "look up aws_guardduty_administrator_account" });

      data(AdministratorAccount, "example", {
        detectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.guardduty import AdministratorAccount, AdministratorAccountConfig

    def describe():
        ubx.intent("look up aws_guardduty_administrator_account")

        ubx.data(AdministratorAccount, "example", AdministratorAccountConfig(
            detector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detector_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="administrator" type="object">
  &lt;p&gt;Contains information about the administrator account and invitation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="invitation_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="invited_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="relationship_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
