---
title: "aws_guardduty_invitations_count"
description: "`aws_guardduty_invitations_count` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_guardduty_invitations_count` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_guardduty_invitations_count"})

    		ubx.Data(guardduty.InvitationsCount, "example", guardduty.InvitationsCountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InvitationsCount } from "@ubx/sdk-aws/aws/data/guardduty/invitations_count";

    export default stack("example", () => {
      intent({ summary: "look up aws_guardduty_invitations_count" });

      data(InvitationsCount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.guardduty import InvitationsCount, InvitationsCountConfig

    def describe():
        ubx.intent("look up aws_guardduty_invitations_count")

        ubx.data(InvitationsCount, "example", InvitationsCountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="invitations_count" type="number">
  Returned by the lookup.
</ResponseField>
