---
title: "aws_guardduty_members"
description: "`aws_guardduty_members` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_guardduty_members` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	guardduty "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/guardduty"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_guardduty_members"})

    		ubx.Data(guardduty.Members, "example", guardduty.MembersConfig{
    			DetectorId:     "example",
    			MaxResults:     1,
    			OnlyAssociated: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Members } from "@ubx/sdk-aws/aws/data/guardduty/members";

    export default stack("example", () => {
      intent({ summary: "look up aws_guardduty_members" });

      data(Members, "example", {
        detectorId: "example",
        maxResults: 1,
        onlyAssociated: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.guardduty import Members, MembersConfig

    def describe():
        ubx.intent("look up aws_guardduty_members")

        ubx.data(Members, "example", MembersConfig(
            detector_id="example",
            max_results=1,
            only_associated="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detector_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="only_associated" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="members" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
