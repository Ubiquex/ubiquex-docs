---
title: "aws_odb_autonomous_database_versions"
description: "`aws_odb_autonomous_database_versions` reads the real, available Oracle Autonomous Database software versions for Oracle Database@AWS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_autonomous_database_versions"})

    		ubx.Data(odb.AutonomousDatabaseVersions, "example", odb.AutonomousDatabaseVersionsConfig{
    			DbWorkload: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AutonomousDatabaseVersions } from "./local-sdk/aws/sdk/typescript/aws/data/odb/autonomous_database_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_autonomous_database_versions" });

      data(AutonomousDatabaseVersions, "example", {
        dbWorkload: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.odb import AutonomousDatabaseVersions, AutonomousDatabaseVersionsConfig

    def describe():
        ubx.intent("look up aws_odb_autonomous_database_versions")

        ubx.data(AutonomousDatabaseVersions, "example", AutonomousDatabaseVersionsConfig(
            db_workload="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="db_workload" type="string">
  The real, primary workload type this database is optimized for. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="autonomous_database_versions" type="list(object)">
  The real versions of the autonomous database included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
