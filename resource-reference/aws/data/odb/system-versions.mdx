---
title: "aws_odb_system_versions"
description: "`aws_odb_system_versions` reads the real, available Oracle Exadata system software versions for Oracle Database@AWS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_system_versions"})

    		ubx.Data(odb.SystemVersions, "example", odb.SystemVersionsConfig{
    			GiVersion:  "example",
    			Shape:      "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SystemVersions } from "./local-sdk/aws/sdk/typescript/aws/data/odb/system_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_system_versions" });

      data(SystemVersions, "example", {
        giVersion: "example",
        shape: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.odb import SystemVersions, SystemVersionsConfig

    def describe():
        ubx.intent("look up aws_odb_system_versions")

        ubx.data(SystemVersions, "example", SystemVersionsConfig(
            gi_version="example",
            shape="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gi_version" type="string" required>
  The real version of the gi. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shape" type="string" required>
  The real compute shape this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="system_versions" type="list(object)">
  The real versions of the system included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
