---
title: "aws_odb_db_system_shapes"
description: "`aws_odb_db_system_shapes` reads the real, available compute shapes for provisioning an Oracle Database@AWS DB System."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_db_system_shapes"})

    		ubx.Data(odb.DbSystemShapes, "example", odb.DbSystemShapesConfig{
    			AvailabilityZone:   "example",
    			AvailabilityZoneId: "example",
    			MaxResults:         1,
    			ShapeFamily:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DbSystemShapes } from "./local-sdk/aws/sdk/typescript/aws/data/odb/db_system_shapes.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_db_system_shapes" });

      data(DbSystemShapes, "example", {
        availabilityZone: "example",
        availabilityZoneId: "example",
        maxResults: 1,
        shapeFamily: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.odb import DbSystemShapes, DbSystemShapesConfig

    def describe():
        ubx.intent("look up aws_odb_db_system_shapes")

        ubx.data(DbSystemShapes, "example", DbSystemShapesConfig(
            availability_zone="example",
            availability_zone_id="example",
            max_results=1,
            shape_family="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="availability_zone" type="string">
  The real Availability Zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  The real, unique identifier of the availability zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shape_family" type="string">
  The real shape family. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="db_system_shapes" type="list(object)">
  The real Oracle DB System compute shapes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
