---
title: "aws_odb_gi_versions"
description: "`aws_odb_gi_versions` reads the real, available Oracle Grid Infrastructure versions for Oracle Database@AWS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_gi_versions"})

    		ubx.Data(odb.GiVersions, "example", odb.GiVersionsConfig{
    			MaxResults: 1,
    			Shape:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { GiVersions } from "./local-sdk/aws/sdk/typescript/aws/data/odb/gi_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_gi_versions" });

      data(GiVersions, "example", {
        maxResults: 1,
        shape: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.odb import GiVersions, GiVersionsConfig

    def describe():
        ubx.intent("look up aws_odb_gi_versions")

        ubx.data(GiVersions, "example", GiVersionsConfig(
            max_results=1,
            shape="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shape" type="string">
  The real compute shape this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="gi_versions" type="list(object)">
  The real versions of the gi included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
