---
title: "aws_odb_oci_onboarding_status"
description: "`aws_odb_oci_onboarding_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_odb_oci_onboarding_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_oci_onboarding_status"})

    		ubx.Data(odb.OciOnboardingStatus, "example", odb.OciOnboardingStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OciOnboardingStatus } from "@ubx/sdk-aws/aws/data/odb/oci_onboarding_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_oci_onboarding_status" });

      data(OciOnboardingStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.odb import OciOnboardingStatus, OciOnboardingStatusConfig

    def describe():
        ubx.intent("look up aws_odb_oci_onboarding_status")

        ubx.data(OciOnboardingStatus, "example", OciOnboardingStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="autonomous_database_oci_integration_iam_roles" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="existing_tenancy_activation_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="linked_oci_compartment_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="linked_oci_tenancy_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="new_tenancy_activation_link" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="oci_identity_domain" type="object">
  &lt;p&gt;Information about an Oracle Cloud Infrastructure (OCI) identity domain configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_setup_cloud_formation_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="oci_identity_domain_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="oci_identity_domain_resource_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="oci_identity_domain_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_reason" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p/&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="subscription_errors" type="list(object)">
  Returned by the lookup.
</ResponseField>
