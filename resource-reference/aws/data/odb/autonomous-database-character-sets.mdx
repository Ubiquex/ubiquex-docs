---
title: "aws_odb_autonomous_database_character_sets"
description: "`aws_odb_autonomous_database_character_sets` reads the real, available database character sets for provisioning an Oracle Database@AWS Autonomous Database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_autonomous_database_character_sets"})

    		ubx.Data(odb.AutonomousDatabaseCharacterSets, "example", odb.AutonomousDatabaseCharacterSetsConfig{
    			CharacterSetType: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutonomousDatabaseCharacterSets } from "@ubx/sdk-aws/aws/data/odb/autonomous_database_character_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_autonomous_database_character_sets" });

      data(AutonomousDatabaseCharacterSets, "example", {
        characterSetType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.odb import AutonomousDatabaseCharacterSets, AutonomousDatabaseCharacterSetsConfig

    def describe():
        ubx.intent("look up aws_odb_autonomous_database_character_sets")

        ubx.data(AutonomousDatabaseCharacterSets, "example", AutonomousDatabaseCharacterSetsConfig(
            character_set_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="character_set_type" type="string">
  The real, discriminating type of the character set this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="autonomous_database_character_sets" type="list(object)">
  The real autonomous database character sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
