---
title: "aws_odb_gi_minor_versions"
description: "`aws_odb_gi_minor_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_odb_gi_minor_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_gi_minor_versions"})

    		ubx.Data(odb.GiMinorVersions, "example", odb.GiMinorVersionsConfig{
    			GiVersion:          "example",
    			AvailabilityZone:   "example",
    			AvailabilityZoneId: "example",
    			MaxResults:         1,
    			ShapeFamily:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GiMinorVersions } from "@ubx/sdk-aws/aws/data/odb/gi_minor_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_gi_minor_versions" });

      data(GiMinorVersions, "example", {
        giVersion: "example",
        availabilityZone: "example",
        availabilityZoneId: "example",
        maxResults: 1,
        shapeFamily: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.odb import GiMinorVersions, GiMinorVersionsConfig

    def describe():
        ubx.intent("look up aws_odb_gi_minor_versions")

        ubx.data(GiMinorVersions, "example", GiMinorVersionsConfig(
            gi_version="example",
            availability_zone="example",
            availability_zone_id="example",
            max_results=1,
            shape_family="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="availability_zone" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="gi_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="shape_family" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="gi_minor_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
