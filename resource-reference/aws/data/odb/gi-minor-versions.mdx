---
title: "aws_odb_gi_minor_versions"
description: "`aws_odb_gi_minor_versions` reads the real, available Oracle Grid Infrastructure minor versions for Oracle Database@AWS."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_odb_gi_minor_versions"})

    		ubx.Data(odb.GiMinorVersions, "example", odb.GiMinorVersionsConfig{
    			GiVersion:          "example",
    			AvailabilityZone:   "example",
    			AvailabilityZoneId: "example",
    			MaxResults:         1,
    			ShapeFamily:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GiMinorVersions } from "@ubx/sdk-aws/aws/data/odb/gi_minor_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_odb_gi_minor_versions" });

      data(GiMinorVersions, "example", {
        giVersion: "example",
        availabilityZone: "example",
        availabilityZoneId: "example",
        maxResults: 1,
        shapeFamily: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.odb import GiMinorVersions, GiMinorVersionsConfig

    def describe():
        ubx.intent("look up aws_odb_gi_minor_versions")

        ubx.data(GiMinorVersions, "example", GiMinorVersionsConfig(
            gi_version="example",
            availability_zone="example",
            availability_zone_id="example",
            max_results=1,
            shape_family="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="availability_zone" type="string">
  The real Availability Zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  The real, unique identifier of the availability zone this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="gi_version" type="string" required>
  The real version of the gi. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shape_family" type="string">
  The real shape family. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="gi_minor_versions" type="list(object)">
  The real versions of the gi minor included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
