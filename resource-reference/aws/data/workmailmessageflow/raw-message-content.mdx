---
title: "aws_workmailmessageflow_raw_message_content"
description: "`aws_workmailmessageflow_raw_message_content` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workmailmessageflow_raw_message_content` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmailmessageflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workmailmessageflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workmailmessageflow_raw_message_content"})

    		ubx.Data(workmailmessageflow.RawMessageContent, "example", workmailmessageflow.RawMessageContentConfig{
    			MessageId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RawMessageContent } from "@ubx/sdk-aws/aws/data/workmailmessageflow/raw_message_content";

    export default stack("example", () => {
      intent({ summary: "look up aws_workmailmessageflow_raw_message_content" });

      data(RawMessageContent, "example", {
        messageId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workmailmessageflow import RawMessageContent, RawMessageContentConfig

    def describe():
        ubx.intent("look up aws_workmailmessageflow_raw_message_content")

        ubx.data(RawMessageContent, "example", RawMessageContentConfig(
            message_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="message_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="message_content" type="string">
  Returned by the lookup.
</ResponseField>
