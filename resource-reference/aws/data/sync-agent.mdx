---
title: "aws_data_sync_agent"
description: "`aws_data_sync_agent` activates a real DataSync agent, a virtual machine or hardware appliance deployed close to on-premises data..."
---

`aws_data_sync_agent` activates a real DataSync agent, a virtual machine or hardware appliance deployed close to on-premises data, that an `aws_data_sync_task` reads from or writes to as one endpoint of its transfer.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sync agent"})

    		ubx.Resource(data.SyncAgent, "ci-runner", data.SyncAgentConfig{
    			ActivationKey:     "example",
    			AgentName:         "example-agent-name",
    			SecurityGroupArns: []string{"example"},
    			SubnetArns:        []string{"example"},
    			Tags:              []map[string]any{{"key": "example"}},
    			VpcEndpointId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncAgent } from "./local-sdk/aws/sdk/typescript/aws/data/sync_agent.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sync agent" });

      resource(SyncAgent, "ci-runner", {
        activationKey: "example",
        agentName: "example-agent-name",
        securityGroupArns: ["example"],
        subnetArns: ["example"],
        tags: [{ key: "example" }],
        vpcEndpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data import SyncAgent, SyncAgentConfig

    def describe():
        ubx.intent("payments own sync agent")

        ubx.resource(SyncAgent, "ci-runner", SyncAgentConfig(
            activation_key="example",
            agent_name="example-agent-name",
            security_group_arns=["example"],
            subnet_arns=["example"],
            tags=[{"key": "example"}],
            vpc_endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_sync_agent, with activation key example, agent
    name example-agent-name, security group arns a set value, subnet
    arns a set value, tags a set value, vpc endpoint id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="activation_key" type="string">
  Activation key of the Agent. Optional.
</ResponseField>

<ResponseField name="agent_name" type="string">
  The name configured for the agent. Text reference used to identify the agent in the console. Optional.
</ResponseField>

<ResponseField name="security_group_arns" type="list(string)">
  The ARNs of the security group used to protect your data transfer task subnets. Optional.
</ResponseField>

<ResponseField name="subnet_arns" type="list(string)">
  The ARNs of the subnets in which DataSync will create elastic network interfaces for each data transfer task. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string">
  The ID of the VPC endpoint that the agent has access to. Optional.
</ResponseField>

## Output properties

<ResponseField name="agent_arn" type="string">
  The DataSync Agent ARN. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_type" type="string">
  The service endpoints that the agent will connect to. Computed by AWS after `ship`.
</ResponseField>
