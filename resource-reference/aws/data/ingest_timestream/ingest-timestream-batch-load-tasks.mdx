---
title: "aws_ingest_timestream_batch_load_tasks"
description: "`aws_ingest_timestream_batch_load_tasks` looks up a real Timestream batch load task, importing data from S3 into a table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ingest_timestream "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ingest_timestream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ingest_timestream_batch_load_tasks"})

    		ubx.Data(ingest_timestream.IngestTimestreamBatchLoadTasks, "example", ingest_timestream.IngestTimestreamBatchLoadTasksConfig{
    			MaxResults: 1,
    			TaskStatus: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IngestTimestreamBatchLoadTasks } from "@ubx/sdk-aws/aws/data/ingest_timestream/ingest_timestream_batch_load_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_ingest_timestream_batch_load_tasks" });

      data(IngestTimestreamBatchLoadTasks, "example", {
        maxResults: 1,
        taskStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ingest_timestream import IngestTimestreamBatchLoadTasks, IngestTimestreamBatchLoadTasksConfig

    def describe():
        ubx.intent("look up aws_ingest_timestream_batch_load_tasks")

        ubx.data(IngestTimestreamBatchLoadTasks, "example", IngestTimestreamBatchLoadTasksConfig(
            max_results=1,
            task_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="task_status" type="string">
  The real, current status of the task. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="batch_load_tasks" type="list(object)">
  The real Timestream batch load tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
