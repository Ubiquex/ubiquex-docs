---
title: "aws_ingest_timestream_databases"
description: "`aws_ingest_timestream_databases` looks up a real Timestream database."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ingest_timestream "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ingest_timestream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ingest_timestream_databases"})

    		ubx.Data(ingest_timestream.IngestTimestreamDatabases, "example", ingest_timestream.IngestTimestreamDatabasesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IngestTimestreamDatabases } from "./local-sdk/aws/sdk/typescript/aws/data/ingest_timestream/ingest_timestream_databases.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ingest_timestream_databases" });

      data(IngestTimestreamDatabases, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ingest_timestream import IngestTimestreamDatabases, IngestTimestreamDatabasesConfig

    def describe():
        ubx.intent("look up aws_ingest_timestream_databases")

        ubx.data(IngestTimestreamDatabases, "example", IngestTimestreamDatabasesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="databases" type="list(object)">
  The real databases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
