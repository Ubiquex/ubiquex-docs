---
title: "aws_ingest_timestream_tables"
description: "`aws_ingest_timestream_tables` looks up a real Timestream table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ingest_timestream "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ingest_timestream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ingest_timestream_tables"})

    		ubx.Data(ingest_timestream.IngestTimestreamTables, "example", ingest_timestream.IngestTimestreamTablesConfig{
    			DatabaseName: "example-database-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IngestTimestreamTables } from "./local-sdk/aws/sdk/typescript/aws/data/ingest_timestream/ingest_timestream_tables.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ingest_timestream_tables" });

      data(IngestTimestreamTables, "example", {
        databaseName: "example-database-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ingest_timestream import IngestTimestreamTables, IngestTimestreamTablesConfig

    def describe():
        ubx.intent("look up aws_ingest_timestream_tables")

        ubx.data(IngestTimestreamTables, "example", IngestTimestreamTablesConfig(
            database_name="example-database-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string">
  The real name of the database this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tables" type="list(object)">
  The real tables included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
