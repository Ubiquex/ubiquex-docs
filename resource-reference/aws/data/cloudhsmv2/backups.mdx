---
title: "aws_cloudhsmv2_backups"
description: "`aws_cloudhsmv2_backups` looks up a real CloudHSM v2 backup of an HSM cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudhsmv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudhsmv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudhsmv2_backups"})

    		ubx.Data(cloudhsmv2.Backups, "example", cloudhsmv2.BackupsConfig{
    			Filters:       map[string]any{"key": "value"},
    			MaxResults:    1,
    			Shared:        true,
    			SortAscending: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Backups } from "@ubx/sdk-aws/aws/data/cloudhsmv2/backups";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudhsmv2_backups" });

      data(Backups, "example", {
        filters: { key: "value" },
        maxResults: 1,
        shared: true,
        sortAscending: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudhsmv2 import Backups, BackupsConfig

    def describe():
        ubx.intent("look up aws_cloudhsmv2_backups")

        ubx.data(Backups, "example", BackupsConfig(
            filters={"key": "value"},
            max_results=1,
            shared=True,
            sort_ascending=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="map(list(string))">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shared" type="bool">
  Whether this object is shared. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_ascending" type="bool">
  Whether results are sorted in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backups" type="list(object)">
  The real backups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
