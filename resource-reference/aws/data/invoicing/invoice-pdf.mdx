---
title: "aws_invoicing_invoice_pdf"
description: "`aws_invoicing_invoice_pdf` reads a real, generated PDF document for an AWS invoice."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	invoicing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/invoicing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_invoicing_invoice_pdf"})

    		ubx.Data(invoicing.InvoicePdf, "example", invoicing.InvoicePdfConfig{
    			InvoiceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InvoicePdf } from "@ubx/sdk-aws/aws/data/invoicing/invoice_pdf";

    export default stack("example", () => {
      intent({ summary: "look up aws_invoicing_invoice_pdf" });

      data(InvoicePdf, "example", {
        invoiceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.invoicing import InvoicePdf, InvoicePdfConfig

    def describe():
        ubx.intent("look up aws_invoicing_invoice_pdf")

        ubx.data(InvoicePdf, "example", InvoicePdfConfig(
            invoice_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="invoice_id" type="string" required>
  The real, unique identifier of the invoice this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="invoice_pdf" type="object">
  &lt;p&gt; Invoice document data. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="document_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="document_url_expiration_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="invoice_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="supplemental_documents" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
