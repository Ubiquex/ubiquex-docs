---
title: "aws_invoicing_invoice_summaries"
description: "`data_aws_invoicing_invoice_summaries` looks up a real, already-existing `aws_invoicing_invoice_unit`."
---

It groups a real, mutually exclusive set of member accounts into one business entity, a subsidiary, a legal entity, a cost center, so AWS issues that entity its own separate invoice rather than one combined invoice for an entire AWS Organization. Reached general availability December 2024.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	invoicing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/invoicing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_invoicing_invoice_summaries"})

    		ubx.Data(invoicing.InvoiceSummaries, "example", invoicing.InvoiceSummariesConfig{
    			Selector:   map[string]any{"resource_type": "example"},
    			Filter:     map[string]any{"billing_period": map[string]any{"month": 1}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InvoiceSummaries } from "./local-sdk/aws/sdk/typescript/aws/data/invoicing/invoice_summaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_invoicing_invoice_summaries" });

      data(InvoiceSummaries, "example", {
        selector: { resourceType: "example" },
        filter: { billingPeriod: { month: 1 } },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.invoicing import InvoiceSummaries, InvoiceSummariesConfig

    def describe():
        ubx.intent("look up aws_invoicing_invoice_summaries")

        ubx.data(InvoiceSummaries, "example", InvoiceSummariesConfig(
            selector={"resource_type": "example"},
            filter={"billing_period": {"month": 1}},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt; Filters for your invoice summaries. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="billing_period" type="object">
      &lt;p&gt; The billing period for which you want to retrieve invoice-related documents. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="month" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="year" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="invoicing_entity" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="receiver_role" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="time_interval" type="object">
      &lt;p&gt; The time period that you want invoice-related documents for. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_date" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="start_date" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="selector" type="object" required>
  &lt;p&gt;Specifies the invoice summary.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="resource_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="invoice_summaries" type="list(object)">
  The real, summarized invoices included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
