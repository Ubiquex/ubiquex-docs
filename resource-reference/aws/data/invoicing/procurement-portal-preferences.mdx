---
title: "aws_invoicing_procurement_portal_preferences"
description: "`aws_invoicing_procurement_portal_preferences` reads the real procurement portal preferences configured for AWS invoicing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	invoicing "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/invoicing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_invoicing_procurement_portal_preferences"})

    		ubx.Data(invoicing.ProcurementPortalPreferences, "example", invoicing.ProcurementPortalPreferencesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProcurementPortalPreferences } from "@ubx/sdk-aws/aws/data/invoicing/procurement_portal_preferences";

    export default stack("example", () => {
      intent({ summary: "look up aws_invoicing_procurement_portal_preferences" });

      data(ProcurementPortalPreferences, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.invoicing import ProcurementPortalPreferences, ProcurementPortalPreferencesConfig

    def describe():
        ubx.intent("look up aws_invoicing_procurement_portal_preferences")

        ubx.data(ProcurementPortalPreferences, "example", ProcurementPortalPreferencesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="procurement_portal_preferences" type="list(object)">
  The real, configured AWS Marketplace procurement portal preferences. **(AI-inferred)** Returned by the lookup.
</ResponseField>
