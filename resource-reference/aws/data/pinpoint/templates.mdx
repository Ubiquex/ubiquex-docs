---
title: "aws_pinpoint_templates"
description: "`aws_pinpoint_templates` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_templates` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_templates"})

    		ubx.Data(pinpoint.Templates, "example", pinpoint.TemplatesConfig{
    			NextToken:    "example",
    			PageSize:     "example",
    			Prefix:       "example",
    			TemplateType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Templates } from "@ubx/sdk-aws/aws/data/pinpoint/templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_templates" });

      data(Templates, "example", {
        nextToken: "example",
        pageSize: "example",
        prefix: "example",
        templateType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import Templates, TemplatesConfig

    def describe():
        ubx.intent("look up aws_pinpoint_templates")

        ubx.data(Templates, "example", TemplatesConfig(
            next_token="example",
            page_size="example",
            prefix="example",
            template_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="next_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="prefix" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="template_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="templates_response" type="object">
  &lt;p&gt;Provides information about all the message templates that are associated with your Amazon Pinpoint account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="item" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
