---
title: "aws_pinpoint_campaign_versions"
description: "`data_aws_pinpoint_campaign_versions` looks up a real, already-existing `aws_pinpoint_campaign`."
---

It creates a real, scheduled or triggered messaging campaign targeting an `aws_pinpoint_app` segment. Campaigns are explicitly named among the engagement features AWS is retiring: the Pinpoint console and campaign resources become inaccessible after October 30, 2026, a date roughly two months from now, with AWS recommending Amazon Connect for continued engagement use cases.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_campaign_versions"})

    		ubx.Data(pinpoint.CampaignVersions, "example", pinpoint.CampaignVersionsConfig{
    			ApplicationId: "example",
    			CampaignId:    "example",
    			PageSize:      "example",
    			Token:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CampaignVersions } from "./local-sdk/aws/sdk/typescript/aws/data/pinpoint/campaign_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_campaign_versions" });

      data(CampaignVersions, "example", {
        applicationId: "example",
        campaignId: "example",
        pageSize: "example",
        token: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import CampaignVersions, CampaignVersionsConfig

    def describe():
        ubx.intent("look up aws_pinpoint_campaign_versions")

        ubx.data(CampaignVersions, "example", CampaignVersionsConfig(
            application_id="example",
            campaign_id="example",
            page_size="example",
            token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="campaign_id" type="string" required>
  The real, unique identifier of the campaign this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="token" type="string">
  An opaque pagination or continuation token from a previous response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="campaigns_response" type="object">
  &lt;p&gt;Provides information about the configuration and other settings for all the campaigns that are associated with an application.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="item" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
