---
title: "aws_pinpoint_apns_voip_channel"
description: "`aws_pinpoint_apns_voip_channel` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_apns_voip_channel` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_apns_voip_channel"})

    		ubx.Data(pinpoint.ApnsVoipChannel, "example", pinpoint.ApnsVoipChannelConfig{
    			ApplicationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApnsVoipChannel } from "@ubx/sdk-aws/aws/data/pinpoint/apns_voip_channel";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_apns_voip_channel" });

      data(ApnsVoipChannel, "example", {
        applicationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import ApnsVoipChannel, ApnsVoipChannelConfig

    def describe():
        ubx.intent("look up aws_pinpoint_apns_voip_channel")

        ubx.data(ApnsVoipChannel, "example", ApnsVoipChannelConfig(
            application_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="apnsvoip_channel_response" type="object">
  &lt;p&gt;Provides information about the status and settings of the APNs (Apple Push Notification service) VoIP channel for an application.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="default_authentication_method" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="has_credential" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="has_token_key" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_archived" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_by" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="platform" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="version" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
