---
title: "aws_pinpoint_in_app_messages"
description: "`aws_pinpoint_in_app_messages` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_in_app_messages` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_in_app_messages"})

    		ubx.Data(pinpoint.InAppMessages, "example", pinpoint.InAppMessagesConfig{
    			ApplicationId: "example",
    			EndpointId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InAppMessages } from "@ubx/sdk-aws/aws/data/pinpoint/in_app_messages";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_in_app_messages" });

      data(InAppMessages, "example", {
        applicationId: "example",
        endpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import InAppMessages, InAppMessagesConfig

    def describe():
        ubx.intent("look up aws_pinpoint_in_app_messages")

        ubx.data(InAppMessages, "example", InAppMessagesConfig(
            application_id="example",
            endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="endpoint_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="in_app_messages_response" type="object">
  &lt;p&gt;Get in-app messages response object.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="in_app_message_campaigns" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
