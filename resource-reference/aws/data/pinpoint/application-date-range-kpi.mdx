---
title: "aws_pinpoint_application_date_range_kpi"
description: "`aws_pinpoint_application_date_range_kpi` reads a real, computed KPI metric for a Pinpoint application over a date range."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_application_date_range_kpi"})

    		ubx.Data(pinpoint.ApplicationDateRangeKpi, "example", pinpoint.ApplicationDateRangeKpiConfig{
    			ApplicationId: "example",
    			KpiName:       "example-kpi-name",
    			EndTime:       "example",
    			NextToken:     "example",
    			PageSize:      "example",
    			StartTime:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationDateRangeKpi } from "@ubx/sdk-aws/aws/data/pinpoint/application_date_range_kpi";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_application_date_range_kpi" });

      data(ApplicationDateRangeKpi, "example", {
        applicationId: "example",
        kpiName: "example-kpi-name",
        endTime: "example",
        nextToken: "example",
        pageSize: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import ApplicationDateRangeKpi, ApplicationDateRangeKpiConfig

    def describe():
        ubx.intent("look up aws_pinpoint_application_date_range_kpi")

        ubx.data(ApplicationDateRangeKpi, "example", ApplicationDateRangeKpiConfig(
            application_id="example",
            kpi_name="example-kpi-name",
            end_time="example",
            next_token="example",
            page_size="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="kpi_name" type="string" required>
  The real name of the kpi this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_date_range_kpi_response" type="object">
  &lt;p&gt;Provides the results of a query that retrieved the data for a standard metric that applies to an application, and provides information about that query.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="end_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="kpi_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="kpi_result" type="object" required>
      &lt;p&gt;Provides the results of a query that retrieved the data for a standard metric that applies to an application, campaign, or journey.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="rows" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
