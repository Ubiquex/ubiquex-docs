---
title: "aws_pinpoint_segments"
description: "`aws_pinpoint_segments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_segments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_segments"})

    		ubx.Data(pinpoint.Segments, "example", pinpoint.SegmentsConfig{
    			ApplicationId: "example",
    			PageSize:      "example",
    			Token:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Segments } from "@ubx/sdk-aws/aws/data/pinpoint/segments";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_segments" });

      data(Segments, "example", {
        applicationId: "example",
        pageSize: "example",
        token: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import Segments, SegmentsConfig

    def describe():
        ubx.intent("look up aws_pinpoint_segments")

        ubx.data(Segments, "example", SegmentsConfig(
            application_id="example",
            page_size="example",
            token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="token" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="segments_response" type="object">
  &lt;p&gt;Provides information about all the segments that are associated with an application.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="item" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
