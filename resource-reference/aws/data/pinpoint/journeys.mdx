---
title: "aws_pinpoint_journeys"
description: "`aws_pinpoint_journeys` looks up a real Pinpoint journey, a multi-step, automated customer engagement workflow."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_journeys"})

    		ubx.Data(pinpoint.Journeys, "example", pinpoint.JourneysConfig{
    			ApplicationId: "example",
    			PageSize:      "example",
    			Token:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Journeys } from "@ubx/sdk-aws/aws/data/pinpoint/journeys";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_journeys" });

      data(Journeys, "example", {
        applicationId: "example",
        pageSize: "example",
        token: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import Journeys, JourneysConfig

    def describe():
        ubx.intent("look up aws_pinpoint_journeys")

        ubx.data(Journeys, "example", JourneysConfig(
            application_id="example",
            page_size="example",
            token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="token" type="string">
  An opaque pagination or continuation token from a previous response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="journeys_response" type="object">
  &lt;p&gt;Provides information about the status, configuration, and other settings for all the journeys that are associated with an application.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="item" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
