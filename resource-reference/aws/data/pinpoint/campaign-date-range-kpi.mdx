---
title: "aws_pinpoint_campaign_date_range_kpi"
description: "`aws_pinpoint_campaign_date_range_kpi` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pinpoint_campaign_date_range_kpi` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_campaign_date_range_kpi"})

    		ubx.Data(pinpoint.CampaignDateRangeKpi, "example", pinpoint.CampaignDateRangeKpiConfig{
    			ApplicationId: "example",
    			CampaignId:    "example",
    			KpiName:       "example-kpi-name",
    			EndTime:       "example",
    			NextToken:     "example",
    			PageSize:      "example",
    			StartTime:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CampaignDateRangeKpi } from "@ubx/sdk-aws/aws/data/pinpoint/campaign_date_range_kpi";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_campaign_date_range_kpi" });

      data(CampaignDateRangeKpi, "example", {
        applicationId: "example",
        campaignId: "example",
        kpiName: "example-kpi-name",
        endTime: "example",
        nextToken: "example",
        pageSize: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import CampaignDateRangeKpi, CampaignDateRangeKpiConfig

    def describe():
        ubx.intent("look up aws_pinpoint_campaign_date_range_kpi")

        ubx.data(CampaignDateRangeKpi, "example", CampaignDateRangeKpiConfig(
            application_id="example",
            campaign_id="example",
            kpi_name="example-kpi-name",
            end_time="example",
            next_token="example",
            page_size="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="campaign_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="kpi_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="next_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="campaign_date_range_kpi_response" type="object">
  &lt;p&gt;Provides the results of a query that retrieved the data for a standard metric that applies to a campaign, and provides information about that query.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="campaign_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="end_time" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="kpi_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="kpi_result" type="object" required>
      &lt;p&gt;Provides the results of a query that retrieved the data for a standard metric that applies to an application, campaign, or journey.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="rows" type="list(object)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
