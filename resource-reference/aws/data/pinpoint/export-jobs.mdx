---
title: "aws_pinpoint_export_jobs"
description: "`aws_pinpoint_export_jobs` reads the real jobs exporting endpoint definitions from a Pinpoint application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_export_jobs"})

    		ubx.Data(pinpoint.ExportJobs, "example", pinpoint.ExportJobsConfig{
    			ApplicationId: "example",
    			PageSize:      "example",
    			Token:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ExportJobs } from "./local-sdk/aws/sdk/typescript/aws/data/pinpoint/export_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_export_jobs" });

      data(ExportJobs, "example", {
        applicationId: "example",
        pageSize: "example",
        token: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import ExportJobs, ExportJobsConfig

    def describe():
        ubx.intent("look up aws_pinpoint_export_jobs")

        ubx.data(ExportJobs, "example", ExportJobsConfig(
            application_id="example",
            page_size="example",
            token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string" required>
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="token" type="string">
  An opaque pagination or continuation token from a previous response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_jobs_response" type="object">
  &lt;p&gt;Provides information about all the export jobs that are associated with an application or segment. An export job is a job that exports endpoint definitions to a file.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="item" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
