---
title: "aws_pinpoint_template_versions"
description: "`aws_pinpoint_template_versions` reads a real, saved version of a Pinpoint message template."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pinpoint_template_versions"})

    		ubx.Data(pinpoint.TemplateVersions, "example", pinpoint.TemplateVersionsConfig{
    			TemplateName: "example-template-name",
    			TemplateType: "example",
    			NextToken:    "example",
    			PageSize:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TemplateVersions } from "@ubx/sdk-aws/aws/data/pinpoint/template_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_pinpoint_template_versions" });

      data(TemplateVersions, "example", {
        templateName: "example-template-name",
        templateType: "example",
        nextToken: "example",
        pageSize: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pinpoint import TemplateVersions, TemplateVersionsConfig

    def describe():
        ubx.intent("look up aws_pinpoint_template_versions")

        ubx.data(TemplateVersions, "example", TemplateVersionsConfig(
            template_name="example-template-name",
            template_type="example",
            next_token="example",
            page_size="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="string">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_name" type="string" required>
  The real name of the template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="template_type" type="string" required>
  The real, discriminating type of the template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="template_versions_response" type="object">
  &lt;p&gt;Provides information about all the versions of a specific message template.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="item" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="next_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="request_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
