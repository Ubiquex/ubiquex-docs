---
title: "aws_route53_health_check_count"
description: "`data_aws_route53_health_check_count` looks up a real, already-existing `aws_route53_health_check`."
---

It creates a real, recurring health check against an endpoint, an IP address, domain, or CloudWatch alarm, that DNS failover routing and `aws_route53_record_set` health-check associations rely on to detect an unhealthy target.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_health_check_count"})

    		ubx.Data(route53.HealthCheckCount, "example", route53.HealthCheckCountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthCheckCount } from "@ubx/sdk-aws/aws/data/route53/health_check_count";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_health_check_count" });

      data(HealthCheckCount, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import HealthCheckCount, HealthCheckCountConfig

    def describe():
        ubx.intent("look up aws_route53_health_check_count")

        ubx.data(HealthCheckCount, "example", HealthCheckCountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="health_check_count" type="number">
  The real count of health checks. **(AI-inferred)** Returned by the lookup.
</ResponseField>
