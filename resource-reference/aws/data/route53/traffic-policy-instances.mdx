---
title: "aws_route53_traffic_policy_instances"
description: "`aws_route53_traffic_policy_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_traffic_policy_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_traffic_policy_instances"})

    		ubx.Data(route53.TrafficPolicyInstances, "example", route53.TrafficPolicyInstancesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrafficPolicyInstances } from "@ubx/sdk-aws/aws/data/route53/traffic_policy_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_traffic_policy_instances" });

      data(TrafficPolicyInstances, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import TrafficPolicyInstances, TrafficPolicyInstancesConfig

    def describe():
        ubx.intent("look up aws_route53_traffic_policy_instances")

        ubx.data(TrafficPolicyInstances, "example", TrafficPolicyInstancesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="hosted_zone_id_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="max_items" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_instance_name_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_instance_type_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>
