---
title: "aws_route53_tags_for_resources"
description: "`aws_route53_tags_for_resources` reads the real tags applied to multiple Route 53 resources at once."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_tags_for_resources"})

    		ubx.Data(route53.TagsForResources, "example", route53.TagsForResourcesConfig{
    			ResourceIds:  []string{"example"},
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TagsForResources } from "./local-sdk/aws/sdk/typescript/aws/data/route53/tags_for_resources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_tags_for_resources" });

      data(TagsForResources, "example", {
        resourceIds: ["example"],
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import TagsForResources, TagsForResourcesConfig

    def describe():
        ubx.intent("look up aws_route53_tags_for_resources")

        ubx.data(TagsForResources, "example", TagsForResourcesConfig(
            resource_ids=["example"],
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_ids" type="list(string)" required>
  The real, unique identifiers of the resources this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource_tag_sets" type="list(object)">
  The real resource tag sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
