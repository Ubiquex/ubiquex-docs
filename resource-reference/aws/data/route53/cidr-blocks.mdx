---
title: "aws_route53_cidr_blocks"
description: "`aws_route53_cidr_blocks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_cidr_blocks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_cidr_blocks"})

    		ubx.Data(route53.CidrBlocks, "example", route53.CidrBlocksConfig{
    			CollectionId: "example",
    			LocationName: "example-location-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CidrBlocks } from "@ubx/sdk-aws/aws/data/route53/cidr_blocks";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_cidr_blocks" });

      data(CidrBlocks, "example", {
        collectionId: "example",
        locationName: "example-location-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import CidrBlocks, CidrBlocksConfig

    def describe():
        ubx.intent("look up aws_route53_cidr_blocks")

        ubx.data(CidrBlocks, "example", CidrBlocksConfig(
            collection_id="example",
            location_name="example-location-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="collection_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="location_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cidr_blocks" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
