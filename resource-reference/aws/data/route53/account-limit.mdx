---
title: "aws_route53_account_limit"
description: "`aws_route53_account_limit` reads real, account-level Route 53 limits and current usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_account_limit"})

    		ubx.Data(route53.AccountLimit, "example", route53.AccountLimitConfig{
    			Type: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountLimit } from "@ubx/sdk-aws/aws/data/route53/account_limit";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_account_limit" });

      data(AccountLimit, "example", {
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import AccountLimit, AccountLimitConfig

    def describe():
        ubx.intent("look up aws_route53_account_limit")

        ubx.data(AccountLimit, "example", AccountLimitConfig(
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="type" type="string" required>
  The real, discriminating type of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  The real, total count of matching results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="limit" type="object">
  &lt;p&gt;A complex type that contains the type of limit that you specified in the request and the current value for that limit.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="value" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
