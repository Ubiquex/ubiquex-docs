---
title: "aws_route53_health_checks"
description: "`data_aws_route53_health_checks` looks up a real, already-existing `aws_route53_health_check`."
---

It creates a real, recurring health check against an endpoint, an IP address, domain, or CloudWatch alarm, that DNS failover routing and `aws_route53_record_set` health-check associations rely on to detect an unhealthy target.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_health_checks"})

    		ubx.Data(route53.HealthChecks, "example", route53.HealthChecksConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthChecks } from "@ubx/sdk-aws/aws/data/route53/health_checks";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_health_checks" });

      data(HealthChecks, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import HealthChecks, HealthChecksConfig

    def describe():
        ubx.intent("look up aws_route53_health_checks")

        ubx.data(HealthChecks, "example", HealthChecksConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="health_checks" type="list(object)">
  The real health checks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
