---
title: "aws_route53_reusable_delegation_set_limit"
description: "`aws_route53_reusable_delegation_set_limit` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_reusable_delegation_set_limit` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_reusable_delegation_set_limit"})

    		ubx.Data(route53.ReusableDelegationSetLimit, "example", route53.ReusableDelegationSetLimitConfig{
    			DelegationSetId: "example",
    			Type:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReusableDelegationSetLimit } from "@ubx/sdk-aws/aws/data/route53/reusable_delegation_set_limit";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_reusable_delegation_set_limit" });

      data(ReusableDelegationSetLimit, "example", {
        delegationSetId: "example",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import ReusableDelegationSetLimit, ReusableDelegationSetLimitConfig

    def describe():
        ubx.intent("look up aws_route53_reusable_delegation_set_limit")

        ubx.data(ReusableDelegationSetLimit, "example", ReusableDelegationSetLimitConfig(
            delegation_set_id="example",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="delegation_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="limit" type="object">
  &lt;p&gt;A complex type that contains the type of limit that you specified in the request and the current value for that limit.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="value" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
