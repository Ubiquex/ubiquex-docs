---
title: "aws_route53_checker_ip_ranges"
description: "`aws_route53_checker_ip_ranges` reads Route 53's own real, published IP address ranges used by its health checkers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_checker_ip_ranges"})

    		ubx.Data(route53.CheckerIpRanges, "example", route53.CheckerIpRangesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CheckerIpRanges } from "./local-sdk/aws/sdk/typescript/aws/data/route53/checker_ip_ranges.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_checker_ip_ranges" });

      data(CheckerIpRanges, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import CheckerIpRanges, CheckerIpRangesConfig

    def describe():
        ubx.intent("look up aws_route53_checker_ip_ranges")

        ubx.data(CheckerIpRanges, "example", CheckerIpRangesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="checker_ip_ranges" type="list(string)">
  The real IP address ranges Route 53 health checkers use. **(AI-inferred)** Returned by the lookup.
</ResponseField>
