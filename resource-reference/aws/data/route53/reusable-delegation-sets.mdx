---
title: "aws_route53_reusable_delegation_sets"
description: "`aws_route53_reusable_delegation_sets` looks up a real Route 53 reusable delegation set, a fixed set of name servers shared across multiple hosted zones."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_reusable_delegation_sets"})

    		ubx.Data(route53.ReusableDelegationSets, "example", route53.ReusableDelegationSetsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReusableDelegationSets } from "@ubx/sdk-aws/aws/data/route53/reusable_delegation_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_reusable_delegation_sets" });

      data(ReusableDelegationSets, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import ReusableDelegationSets, ReusableDelegationSetsConfig

    def describe():
        ubx.intent("look up aws_route53_reusable_delegation_sets")

        ubx.data(ReusableDelegationSets, "example", ReusableDelegationSetsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="delegation_sets" type="list(object)">
  The real delegation sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
