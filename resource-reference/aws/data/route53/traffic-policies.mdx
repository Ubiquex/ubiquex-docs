---
title: "aws_route53_traffic_policies"
description: "`aws_route53_traffic_policies` looks up a real Route 53 traffic policy, a reusable, complex routing configuration template."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_traffic_policies"})

    		ubx.Data(route53.TrafficPolicies, "example", route53.TrafficPoliciesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TrafficPolicies } from "./local-sdk/aws/sdk/typescript/aws/data/route53/traffic_policies.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_traffic_policies" });

      data(TrafficPolicies, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import TrafficPolicies, TrafficPoliciesConfig

    def describe():
        ubx.intent("look up aws_route53_traffic_policies")

        ubx.data(TrafficPolicies, "example", TrafficPoliciesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_id_marker" type="string">
  An opaque pagination token, requesting the next page of traffic policy results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_summaries" type="list(object)">
  The real, summarized traffic policys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
