---
title: "aws_route53_traffic_policy_instances_by_policy"
description: "`aws_route53_traffic_policy_instances_by_policy` reads the real traffic policy instances created from a specific Route 53 traffic policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_traffic_policy_instances_by_policy"})

    		ubx.Data(route53.TrafficPolicyInstancesByPolicy, "example", route53.TrafficPolicyInstancesByPolicyConfig{
    			TrafficPolicyId:      "example",
    			TrafficPolicyVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrafficPolicyInstancesByPolicy } from "@ubx/sdk-aws/aws/data/route53/traffic_policy_instances_by_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_traffic_policy_instances_by_policy" });

      data(TrafficPolicyInstancesByPolicy, "example", {
        trafficPolicyId: "example",
        trafficPolicyVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import TrafficPolicyInstancesByPolicy, TrafficPolicyInstancesByPolicyConfig

    def describe():
        ubx.intent("look up aws_route53_traffic_policy_instances_by_policy")

        ubx.data(TrafficPolicyInstancesByPolicy, "example", TrafficPolicyInstancesByPolicyConfig(
            traffic_policy_id="example",
            traffic_policy_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="traffic_policy_id" type="string" required>
  The real, unique identifier of the traffic policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="traffic_policy_version" type="number" required>
  The real version of the traffic policy. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hosted_zone_id_marker" type="string">
  An opaque pagination token, requesting the next page of hosted zone results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_truncated" type="bool">
  Whether the result was truncated and more real data is available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_instance_name_marker" type="string">
  An opaque pagination token, requesting the next page of traffic policy instance results by name. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_instance_type_marker" type="string">
  An opaque pagination token, requesting the next page of traffic policy instance results by type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traffic_policy_instances" type="list(object)">
  The real Route 53 traffic policy instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
