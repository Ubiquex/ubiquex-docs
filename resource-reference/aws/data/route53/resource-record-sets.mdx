---
title: "aws_route53_resource_record_sets"
description: "`aws_route53_resource_record_sets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_resource_record_sets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_resource_record_sets"})

    		ubx.Data(route53.ResourceRecordSets, "example", route53.ResourceRecordSetsConfig{
    			HostedZoneId:          "example",
    			StartRecordIdentifier: "example",
    			StartRecordName:       "example-start-record-name",
    			StartRecordType:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceRecordSets } from "@ubx/sdk-aws/aws/data/route53/resource_record_sets";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_resource_record_sets" });

      data(ResourceRecordSets, "example", {
        hostedZoneId: "example",
        startRecordIdentifier: "example",
        startRecordName: "example-start-record-name",
        startRecordType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import ResourceRecordSets, ResourceRecordSetsConfig

    def describe():
        ubx.intent("look up aws_route53_resource_record_sets")

        ubx.data(ResourceRecordSets, "example", ResourceRecordSetsConfig(
            hosted_zone_id="example",
            start_record_identifier="example",
            start_record_name="example-start-record-name",
            start_record_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="hosted_zone_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_record_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_record_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_record_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="is_truncated" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="max_items" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_record_identifier" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_record_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_record_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_record_sets" type="list(object)">
  Returned by the lookup.
</ResponseField>
