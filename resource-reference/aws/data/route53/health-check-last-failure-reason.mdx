---
title: "aws_route53_health_check_last_failure_reason"
description: "`aws_route53_health_check_last_failure_reason` reads the real, most recent failure reason reported by a Route 53 health check."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_health_check_last_failure_reason"})

    		ubx.Data(route53.HealthCheckLastFailureReason, "example", route53.HealthCheckLastFailureReasonConfig{
    			HealthCheckId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HealthCheckLastFailureReason } from "@ubx/sdk-aws/aws/data/route53/health_check_last_failure_reason";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_health_check_last_failure_reason" });

      data(HealthCheckLastFailureReason, "example", {
        healthCheckId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53 import HealthCheckLastFailureReason, HealthCheckLastFailureReasonConfig

    def describe():
        ubx.intent("look up aws_route53_health_check_last_failure_reason")

        ubx.data(HealthCheckLastFailureReason, "example", HealthCheckLastFailureReasonConfig(
            health_check_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="health_check_id" type="string" required>
  The real, unique identifier of the health check this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="health_check_observations" type="list(object)">
  The real Route 53 health check observations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
