---
title: "aws_secretsmanager_random_password"
description: "`aws_secretsmanager_random_password` reads a real, freshly generated random password from Secrets Manager."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	secretsmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/secretsmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_secretsmanager_random_password"})

    		ubx.Data(secretsmanager.RandomPassword, "example", secretsmanager.RandomPasswordConfig{
    			ExcludeCharacters:       "example",
    			ExcludeLowercase:        true,
    			ExcludeNumbers:          true,
    			ExcludePunctuation:      true,
    			ExcludeUppercase:        true,
    			IncludeSpace:            true,
    			PasswordLength:          1,
    			RequireEachIncludedType: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RandomPassword } from "./local-sdk/aws/sdk/typescript/aws/data/secretsmanager/random_password.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_secretsmanager_random_password" });

      data(RandomPassword, "example", {
        excludeCharacters: "example",
        excludeLowercase: true,
        excludeNumbers: true,
        excludePunctuation: true,
        excludeUppercase: true,
        includeSpace: true,
        passwordLength: 1,
        requireEachIncludedType: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.secretsmanager import RandomPassword, RandomPasswordConfig

    def describe():
        ubx.intent("look up aws_secretsmanager_random_password")

        ubx.data(RandomPassword, "example", RandomPasswordConfig(
            exclude_characters="example",
            exclude_lowercase=True,
            exclude_numbers=True,
            exclude_punctuation=True,
            exclude_uppercase=True,
            include_space=True,
            password_length=1,
            require_each_included_type=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="exclude_characters" type="string">
  The real characters excluded from this generated value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="exclude_lowercase" type="bool">
  Whether lowercase letters are excluded from this generated value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="exclude_numbers" type="bool">
  Whether numbers are excluded from this generated value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="exclude_punctuation" type="bool">
  Whether punctuation characters are excluded from this generated value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="exclude_uppercase" type="bool">
  Whether uppercase letters are excluded from this generated value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_space" type="bool">
  Whether the SageMaker space is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="password_length" type="number">
  The real length of the password. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="require_each_included_type" type="bool">
  The real, discriminating type of the require each included this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="random_password" type="string">
  The real, generated random password for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
