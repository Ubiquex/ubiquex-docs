---
title: "aws_secretsmanager_secret_version_ids"
description: "`aws_secretsmanager_secret_version_ids` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_secretsmanager_secret_version_ids` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	secretsmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/secretsmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_secretsmanager_secret_version_ids"})

    		ubx.Data(secretsmanager.SecretVersionIds, "example", secretsmanager.SecretVersionIdsConfig{
    			SecretId:          "example",
    			IncludeDeprecated: true,
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecretVersionIds } from "@ubx/sdk-aws/aws/data/secretsmanager/secret_version_ids";

    export default stack("example", () => {
      intent({ summary: "look up aws_secretsmanager_secret_version_ids" });

      data(SecretVersionIds, "example", {
        secretId: "example",
        includeDeprecated: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.secretsmanager import SecretVersionIds, SecretVersionIdsConfig

    def describe():
        ubx.intent("look up aws_secretsmanager_secret_version_ids")

        ubx.data(SecretVersionIds, "example", SecretVersionIdsConfig(
            secret_id="example",
            include_deprecated=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_deprecated" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  Returned by the lookup.
</ResponseField>
