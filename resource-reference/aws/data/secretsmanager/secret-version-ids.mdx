---
title: "aws_secretsmanager_secret_version_ids"
description: "`aws_secretsmanager_secret_version_ids` reads the real, saved version IDs of a Secrets Manager secret."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	secretsmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/secretsmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_secretsmanager_secret_version_ids"})

    		ubx.Data(secretsmanager.SecretVersionIds, "example", secretsmanager.SecretVersionIdsConfig{
    			SecretId:          "example",
    			IncludeDeprecated: true,
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SecretVersionIds } from "./local-sdk/aws/sdk/typescript/aws/data/secretsmanager/secret_version_ids.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_secretsmanager_secret_version_ids" });

      data(SecretVersionIds, "example", {
        secretId: "example",
        includeDeprecated: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.secretsmanager import SecretVersionIds, SecretVersionIdsConfig

    def describe():
        ubx.intent("look up aws_secretsmanager_secret_version_ids")

        ubx.data(SecretVersionIds, "example", SecretVersionIdsConfig(
            secret_id="example",
            include_deprecated=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_deprecated" type="bool">
  Whether deprecated items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  The real, unique identifier of the secret this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  The real versions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
