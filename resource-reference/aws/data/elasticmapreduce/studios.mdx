---
title: "aws_elasticmapreduce_studios"
description: "`aws_elasticmapreduce_studios` looks up a real EMR Studio, a managed IDE for developing and running EMR workloads."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_studios"})

    		ubx.Data(elasticmapreduce.Studios, "example", elasticmapreduce.StudiosConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Studios } from "@ubx/sdk-aws/aws/data/elasticmapreduce/studios";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_studios" });

      data(Studios, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import Studios, StudiosConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_studios")

        ubx.data(Studios, "example", StudiosConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="studios" type="list(object)">
  The real Nimble Studio studios included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
