---
title: "aws_elasticmapreduce_instance_groups"
description: "`aws_elasticmapreduce_instance_groups` reads the real instance groups (Master, Core, Task) configured on an EMR cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_instance_groups"})

    		ubx.Data(elasticmapreduce.InstanceGroups, "example", elasticmapreduce.InstanceGroupsConfig{
    			ClusterId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceGroups } from "@ubx/sdk-aws/aws/data/elasticmapreduce/instance_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_instance_groups" });

      data(InstanceGroups, "example", {
        clusterId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import InstanceGroups, InstanceGroupsConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_instance_groups")

        ubx.data(InstanceGroups, "example", InstanceGroupsConfig(
            cluster_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_groups" type="list(object)">
  The real instance groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
