---
title: "aws_elasticmapreduce_supported_instance_types"
description: "`aws_elasticmapreduce_supported_instance_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticmapreduce_supported_instance_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_supported_instance_types"})

    		ubx.Data(elasticmapreduce.SupportedInstanceTypes, "example", elasticmapreduce.SupportedInstanceTypesConfig{
    			ReleaseLabel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SupportedInstanceTypes } from "@ubx/sdk-aws/aws/data/elasticmapreduce/supported_instance_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_supported_instance_types" });

      data(SupportedInstanceTypes, "example", {
        releaseLabel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import SupportedInstanceTypes, SupportedInstanceTypesConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_supported_instance_types")

        ubx.data(SupportedInstanceTypes, "example", SupportedInstanceTypesConfig(
            release_label="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="release_label" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="supported_instance_types" type="list(object)">
  Returned by the lookup.
</ResponseField>
