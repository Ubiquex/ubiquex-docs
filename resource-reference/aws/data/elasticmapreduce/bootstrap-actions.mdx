---
title: "aws_elasticmapreduce_bootstrap_actions"
description: "`aws_elasticmapreduce_bootstrap_actions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticmapreduce_bootstrap_actions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_bootstrap_actions"})

    		ubx.Data(elasticmapreduce.BootstrapActions, "example", elasticmapreduce.BootstrapActionsConfig{
    			ClusterId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BootstrapActions } from "@ubx/sdk-aws/aws/data/elasticmapreduce/bootstrap_actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_bootstrap_actions" });

      data(BootstrapActions, "example", {
        clusterId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import BootstrapActions, BootstrapActionsConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_bootstrap_actions")

        ubx.data(BootstrapActions, "example", BootstrapActionsConfig(
            cluster_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bootstrap_actions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
