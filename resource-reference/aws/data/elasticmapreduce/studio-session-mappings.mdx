---
title: "aws_elasticmapreduce_studio_session_mappings"
description: "`aws_elasticmapreduce_studio_session_mappings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticmapreduce_studio_session_mappings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_studio_session_mappings"})

    		ubx.Data(elasticmapreduce.StudioSessionMappings, "example", elasticmapreduce.StudioSessionMappingsConfig{
    			IdentityType: "example",
    			StudioId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StudioSessionMappings } from "@ubx/sdk-aws/aws/data/elasticmapreduce/studio_session_mappings";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_studio_session_mappings" });

      data(StudioSessionMappings, "example", {
        identityType: "example",
        studioId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import StudioSessionMappings, StudioSessionMappingsConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_studio_session_mappings")

        ubx.data(StudioSessionMappings, "example", StudioSessionMappingsConfig(
            identity_type="example",
            studio_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="studio_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="session_mappings" type="list(object)">
  Returned by the lookup.
</ResponseField>
