---
title: "aws_elasticmapreduce_session_endpoint"
description: "`aws_elasticmapreduce_session_endpoint` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticmapreduce_session_endpoint` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_session_endpoint"})

    		ubx.Data(elasticmapreduce.SessionEndpoint, "example", elasticmapreduce.SessionEndpointConfig{
    			ClusterId: "example",
    			SessionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SessionEndpoint } from "@ubx/sdk-aws/aws/data/elasticmapreduce/session_endpoint";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_session_endpoint" });

      data(SessionEndpoint, "example", {
        clusterId: "example",
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import SessionEndpoint, SessionEndpointConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_session_endpoint")

        ubx.data(SessionEndpoint, "example", SessionEndpointConfig(
            cluster_id="example",
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auth_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="auth_token_expiration_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="credentials" type="object">
  &lt;p&gt;The credentials that you can use to connect to cluster endpoints. Credentials consist of a username and a password.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="username_password" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="password" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="username" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="endpoint" type="string">
  Returned by the lookup.
</ResponseField>
