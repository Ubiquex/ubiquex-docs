---
title: "aws_elasticmapreduce_release_labels"
description: "`aws_elasticmapreduce_release_labels` reads the real, available EMR release labels."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_release_labels"})

    		ubx.Data(elasticmapreduce.ReleaseLabels, "example", elasticmapreduce.ReleaseLabelsConfig{
    			Filters:    map[string]any{"application": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReleaseLabels } from "@ubx/sdk-aws/aws/data/elasticmapreduce/release_labels";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_release_labels" });

      data(ReleaseLabels, "example", {
        filters: { application: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import ReleaseLabels, ReleaseLabelsConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_release_labels")

        ubx.data(ReleaseLabels, "example", ReleaseLabelsConfig(
            filters={"application": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;The release label filters by application or version prefix.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="application" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="prefix" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="release_labels" type="list(string)">
  The real EMR release labels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
