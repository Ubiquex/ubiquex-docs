---
title: "aws_elasticmapreduce_release_label"
description: "`aws_elasticmapreduce_release_label` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticmapreduce_release_label` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_release_label"})

    		ubx.Data(elasticmapreduce.ReleaseLabel, "example", elasticmapreduce.ReleaseLabelConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReleaseLabel } from "@ubx/sdk-aws/aws/data/elasticmapreduce/release_label";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_release_label" });

      data(ReleaseLabel, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import ReleaseLabel, ReleaseLabelConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_release_label")

        ubx.data(ReleaseLabel, "example", ReleaseLabelConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="available_osreleases" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="release_label" type="string">
  Returned by the lookup.
</ResponseField>
