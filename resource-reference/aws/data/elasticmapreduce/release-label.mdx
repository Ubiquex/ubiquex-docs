---
title: "aws_elasticmapreduce_release_label"
description: "`aws_elasticmapreduce_release_label` reads real, detailed information about an EMR release label, including its own installed application versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_release_label"})

    		ubx.Data(elasticmapreduce.ReleaseLabel, "example", elasticmapreduce.ReleaseLabelConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReleaseLabel } from "./local-sdk/aws/sdk/typescript/aws/data/elasticmapreduce/release_label.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_release_label" });

      data(ReleaseLabel, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import ReleaseLabel, ReleaseLabelConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_release_label")

        ubx.data(ReleaseLabel, "example", ReleaseLabelConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="available_osreleases" type="list(object)">
  The real operating system releases available for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="release_label" type="string">
  The real, human-readable label of the release. **(AI-inferred)** Returned by the lookup.
</ResponseField>
