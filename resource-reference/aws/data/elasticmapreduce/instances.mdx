---
title: "aws_elasticmapreduce_instances"
description: "`aws_elasticmapreduce_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticmapreduce_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_instances"})

    		ubx.Data(elasticmapreduce.Instances, "example", elasticmapreduce.InstancesConfig{
    			ClusterId:          "example",
    			InstanceFleetId:    "example",
    			InstanceFleetType:  "example",
    			InstanceGroupId:    "example",
    			InstanceGroupTypes: []string{"example"},
    			InstanceStates:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Instances } from "@ubx/sdk-aws/aws/data/elasticmapreduce/instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_instances" });

      data(Instances, "example", {
        clusterId: "example",
        instanceFleetId: "example",
        instanceFleetType: "example",
        instanceGroupId: "example",
        instanceGroupTypes: ["example"],
        instanceStates: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import Instances, InstancesConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_instances")

        ubx.data(Instances, "example", InstancesConfig(
            cluster_id="example",
            instance_fleet_id="example",
            instance_fleet_type="example",
            instance_group_id="example",
            instance_group_types=["example"],
            instance_states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instance_fleet_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_fleet_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_group_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_group_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_states" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
