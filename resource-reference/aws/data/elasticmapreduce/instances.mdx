---
title: "aws_elasticmapreduce_instances"
description: "`aws_elasticmapreduce_instances` reads the real EC2 instances running within an EMR cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_instances"})

    		ubx.Data(elasticmapreduce.Instances, "example", elasticmapreduce.InstancesConfig{
    			ClusterId:          "example",
    			InstanceFleetId:    "example",
    			InstanceFleetType:  "example",
    			InstanceGroupId:    "example",
    			InstanceGroupTypes: []string{"example"},
    			InstanceStates:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Instances } from "@ubx/sdk-aws/aws/data/elasticmapreduce/instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_instances" });

      data(Instances, "example", {
        clusterId: "example",
        instanceFleetId: "example",
        instanceFleetType: "example",
        instanceGroupId: "example",
        instanceGroupTypes: ["example"],
        instanceStates: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import Instances, InstancesConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_instances")

        ubx.data(Instances, "example", InstancesConfig(
            cluster_id="example",
            instance_fleet_id="example",
            instance_fleet_type="example",
            instance_group_id="example",
            instance_group_types=["example"],
            instance_states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_fleet_id" type="string">
  The real, unique identifier of the instance fleet this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_fleet_type" type="string">
  The real, discriminating type of the instance fleet this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_group_id" type="string">
  The real, unique identifier of the instance group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_group_types" type="list(string)">
  The real, discriminating types of instance groups this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_states" type="list(string)">
  The real, current states of the instances included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instances" type="list(object)">
  The real instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
