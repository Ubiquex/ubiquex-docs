---
title: "aws_elasticmapreduce_steps"
description: "`aws_elasticmapreduce_steps` reads the real steps submitted to an EMR cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_steps"})

    		ubx.Data(elasticmapreduce.Steps, "example", elasticmapreduce.StepsConfig{
    			ClusterId:  "example",
    			StepIds:    []string{"example"},
    			StepStates: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Steps } from "@ubx/sdk-aws/aws/data/elasticmapreduce/steps";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_steps" });

      data(Steps, "example", {
        clusterId: "example",
        stepIds: ["example"],
        stepStates: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import Steps, StepsConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_steps")

        ubx.data(Steps, "example", StepsConfig(
            cluster_id="example",
            step_ids=["example"],
            step_states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="step_ids" type="list(string)">
  The real, unique identifiers of the steps this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="step_states" type="list(string)">
  The real, current states of the steps included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  The real, ordered steps included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
