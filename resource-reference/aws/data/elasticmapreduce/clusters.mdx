---
title: "aws_elasticmapreduce_clusters"
description: "`aws_elasticmapreduce_clusters` reads the real EMR clusters on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_clusters"})

    		ubx.Data(elasticmapreduce.Clusters, "example", elasticmapreduce.ClustersConfig{
    			ClusterStates: []string{"example"},
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Clusters } from "./local-sdk/aws/sdk/typescript/aws/data/elasticmapreduce/clusters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_clusters" });

      data(Clusters, "example", {
        clusterStates: ["example"],
        createdAfter: "example",
        createdBefore: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import Clusters, ClustersConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_clusters")

        ubx.data(Clusters, "example", ClustersConfig(
            cluster_states=["example"],
            created_after="example",
            created_before="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_states" type="list(string)">
  The real, current states of the clusters included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="clusters" type="list(object)">
  The real clusters included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
