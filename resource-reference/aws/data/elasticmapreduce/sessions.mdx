---
title: "aws_elasticmapreduce_sessions"
description: "`aws_elasticmapreduce_sessions` reads the real EMR interactive sessions on a cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticmapreduce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticmapreduce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticmapreduce_sessions"})

    		ubx.Data(elasticmapreduce.Sessions, "example", elasticmapreduce.SessionsConfig{
    			ClusterId:     "example",
    			MaxResults:    1,
    			SessionStates: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Sessions } from "./local-sdk/aws/sdk/typescript/aws/data/elasticmapreduce/sessions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticmapreduce_sessions" });

      data(Sessions, "example", {
        clusterId: "example",
        maxResults: 1,
        sessionStates: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticmapreduce import Sessions, SessionsConfig

    def describe():
        ubx.intent("look up aws_elasticmapreduce_sessions")

        ubx.data(Sessions, "example", SessionsConfig(
            cluster_id="example",
            max_results=1,
            session_states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_id" type="string" required>
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_states" type="list(string)">
  The real, current states of the sessions included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  The real sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
