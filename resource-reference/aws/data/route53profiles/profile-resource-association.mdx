---
title: "aws_route53profiles_profile_resource_association"
description: "`aws_route53profiles_profile_resource_association` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53profiles_profile_resource_association` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53profiles "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53profiles"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53profiles_profile_resource_association"})

    		ubx.Data(route53profiles.ProfileResourceAssociation, "example", route53profiles.ProfileResourceAssociationConfig{
    			ProfileResourceAssociationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileResourceAssociation } from "@ubx/sdk-aws/aws/data/route53profiles/profile_resource_association";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53profiles_profile_resource_association",
      });

      data(ProfileResourceAssociation, "example", {
        profileResourceAssociationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53profiles import ProfileResourceAssociation, ProfileResourceAssociationConfig

    def describe():
        ubx.intent("look up aws_route53profiles_profile_resource_association")

        ubx.data(ProfileResourceAssociation, "example", ProfileResourceAssociationConfig(
            profile_resource_association_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_resource_association_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="profile_resource_association" type="object">
  &lt;p&gt; The association between a Route 53 Profile and resources. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="creation_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="modification_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="owner_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="profile_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_properties" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
