---
title: "aws_route53profiles_profile_associations"
description: "`aws_route53profiles_profile_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53profiles_profile_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53profiles "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53profiles"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53profiles_profile_associations"})

    		ubx.Data(route53profiles.ProfileAssociations, "example", route53profiles.ProfileAssociationsConfig{
    			MaxResults: 1,
    			ProfileId:  "example",
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileAssociations } from "@ubx/sdk-aws/aws/data/route53profiles/profile_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53profiles_profile_associations" });

      data(ProfileAssociations, "example", {
        maxResults: 1,
        profileId: "example",
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53profiles import ProfileAssociations, ProfileAssociationsConfig

    def describe():
        ubx.intent("look up aws_route53profiles_profile_associations")

        ubx.data(ProfileAssociations, "example", ProfileAssociationsConfig(
            max_results=1,
            profile_id="example",
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="profile_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
