---
title: "aws_route53profiles_profile_associations"
description: "`aws_route53profiles_profile_associations` reads the real associations between a Route 53 Profile and VPCs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53profiles "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/route53profiles"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53profiles_profile_associations"})

    		ubx.Data(route53profiles.ProfileAssociations, "example", route53profiles.ProfileAssociationsConfig{
    			MaxResults: 1,
    			ProfileId:  "example",
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileAssociations } from "@ubx/sdk-aws/aws/data/route53profiles/profile_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53profiles_profile_associations" });

      data(ProfileAssociations, "example", {
        maxResults: 1,
        profileId: "example",
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53profiles import ProfileAssociations, ProfileAssociationsConfig

    def describe():
        ubx.intent("look up aws_route53profiles_profile_associations")

        ubx.data(ProfileAssociations, "example", ProfileAssociationsConfig(
            max_results=1,
            profile_id="example",
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string">
  The real, unique identifier of the profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="profile_associations" type="list(object)">
  The real associations for this profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>
