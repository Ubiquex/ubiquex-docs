---
title: "aws_route53profiles_profile_resource_associations"
description: "`aws_route53profiles_profile_resource_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53profiles_profile_resource_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53profiles "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53profiles"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53profiles_profile_resource_associations"})

    		ubx.Data(route53profiles.ProfileResourceAssociations, "example", route53profiles.ProfileResourceAssociationsConfig{
    			ProfileId:    "example",
    			MaxResults:   1,
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProfileResourceAssociations } from "@ubx/sdk-aws/aws/data/route53profiles/profile_resource_associations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53profiles_profile_resource_associations",
      });

      data(ProfileResourceAssociations, "example", {
        profileId: "example",
        maxResults: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53profiles import ProfileResourceAssociations, ProfileResourceAssociationsConfig

    def describe():
        ubx.intent("look up aws_route53profiles_profile_resource_associations")

        ubx.data(ProfileResourceAssociations, "example", ProfileResourceAssociationsConfig(
            profile_id="example",
            max_results=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="profile_resource_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
