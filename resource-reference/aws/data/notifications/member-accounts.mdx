---
title: "aws_notifications_member_accounts"
description: "`aws_notifications_member_accounts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_notifications_member_accounts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/notifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_notifications_member_accounts"})

    		ubx.Data(notifications.MemberAccounts, "example", notifications.MemberAccountsConfig{
    			NotificationConfigurationArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:                   1,
    			MemberAccount:                "example",
    			OrganizationalUnitId:         "example",
    			Status:                       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MemberAccounts } from "@ubx/sdk-aws/aws/data/notifications/member_accounts";

    export default stack("example", () => {
      intent({ summary: "look up aws_notifications_member_accounts" });

      data(MemberAccounts, "example", {
        notificationConfigurationArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        memberAccount: "example",
        organizationalUnitId: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.notifications import MemberAccounts, MemberAccountsConfig

    def describe():
        ubx.intent("look up aws_notifications_member_accounts")

        ubx.data(MemberAccounts, "example", MemberAccountsConfig(
            notification_configuration_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            member_account="example",
            organizational_unit_id="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="member_account" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="notification_configuration_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="organizational_unit_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="member_accounts" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
