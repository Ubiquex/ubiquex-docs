---
title: "aws_codebuild_reports"
description: "`aws_codebuild_reports` looks up one real CodeBuild report, generated from a build's own test or coverage results."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_reports"})

    		ubx.Data(codebuild.Reports, "example", codebuild.ReportsConfig{
    			Filter:     map[string]any{"status": "example"},
    			MaxResults: 1,
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Reports } from "@ubx/sdk-aws/aws/data/codebuild/reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_reports" });

      data(Reports, "example", {
        filter: { status: "example" },
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import Reports, ReportsConfig

    def describe():
        ubx.intent("look up aws_codebuild_reports")

        ubx.data(Reports, "example", ReportsConfig(
            filter={"status": "example"},
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt; A filter used to return reports with the status specified by the input &lt;code&gt;status&lt;/code&gt; parameter. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reports" type="list(string)">
  The real reports included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
