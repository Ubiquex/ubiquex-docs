---
title: "aws_codebuild_build_batches"
description: "`aws_codebuild_build_batches` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codebuild_build_batches` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_build_batches"})

    		ubx.Data(codebuild.BuildBatches, "example", codebuild.BuildBatchesConfig{
    			Filter:     map[string]any{"status": "example"},
    			MaxResults: 1,
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BuildBatches } from "@ubx/sdk-aws/aws/data/codebuild/build_batches";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_build_batches" });

      data(BuildBatches, "example", {
        filter: { status: "example" },
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import BuildBatches, BuildBatchesConfig

    def describe():
        ubx.intent("look up aws_codebuild_build_batches")

        ubx.data(BuildBatches, "example", BuildBatchesConfig(
            filter={"status": "example"},
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Specifies filters when retrieving batch builds.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ids" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
