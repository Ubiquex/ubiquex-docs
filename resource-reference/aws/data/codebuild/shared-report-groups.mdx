---
title: "aws_codebuild_shared_report_groups"
description: "`aws_codebuild_shared_report_groups` reads the real CodeBuild report groups shared with the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_shared_report_groups"})

    		ubx.Data(codebuild.SharedReportGroups, "example", codebuild.SharedReportGroupsConfig{
    			MaxResults: 1,
    			SortBy:     "example",
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SharedReportGroups } from "./local-sdk/aws/sdk/typescript/aws/data/codebuild/shared_report_groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_shared_report_groups" });

      data(SharedReportGroups, "example", {
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import SharedReportGroups, SharedReportGroupsConfig

    def describe():
        ubx.intent("look up aws_codebuild_shared_report_groups")

        ubx.data(SharedReportGroups, "example", SharedReportGroupsConfig(
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="report_groups" type="list(string)">
  The real report groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
