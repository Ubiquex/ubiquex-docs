---
title: "aws_codebuild_curated_environment_images"
description: "`aws_codebuild_curated_environment_images` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codebuild_curated_environment_images` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_curated_environment_images"})

    		ubx.Data(codebuild.CuratedEnvironmentImages, "example", codebuild.CuratedEnvironmentImagesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CuratedEnvironmentImages } from "@ubx/sdk-aws/aws/data/codebuild/curated_environment_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_curated_environment_images" });

      data(CuratedEnvironmentImages, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import CuratedEnvironmentImages, CuratedEnvironmentImagesConfig

    def describe():
        ubx.intent("look up aws_codebuild_curated_environment_images")

        ubx.data(CuratedEnvironmentImages, "example", CuratedEnvironmentImagesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="platforms" type="list(object)">
  Returned by the lookup.
</ResponseField>
