---
title: "aws_codebuild_sandboxes"
description: "`aws_codebuild_sandboxes` looks up one real CodeBuild sandbox, an interactive build environment session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_sandboxes"})

    		ubx.Data(codebuild.Sandboxes, "example", codebuild.SandboxesConfig{
    			MaxResults: 1,
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Sandboxes } from "@ubx/sdk-aws/aws/data/codebuild/sandboxes";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_sandboxes" });

      data(Sandboxes, "example", {
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import Sandboxes, SandboxesConfig

    def describe():
        ubx.intent("look up aws_codebuild_sandboxes")

        ubx.data(Sandboxes, "example", SandboxesConfig(
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ids" type="list(string)">
  The real, unique identifiers of the objects this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
