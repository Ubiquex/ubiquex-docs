---
title: "aws_codebuild_sandboxes_for_project"
description: "`aws_codebuild_sandboxes_for_project` reads the real sandbox sessions run for a CodeBuild project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_sandboxes_for_project"})

    		ubx.Data(codebuild.SandboxesForProject, "example", codebuild.SandboxesForProjectConfig{
    			ProjectName: "example-project-name",
    			MaxResults:  1,
    			SortOrder:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SandboxesForProject } from "@ubx/sdk-aws/aws/data/codebuild/sandboxes_for_project";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_sandboxes_for_project" });

      data(SandboxesForProject, "example", {
        projectName: "example-project-name",
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import SandboxesForProject, SandboxesForProjectConfig

    def describe():
        ubx.intent("look up aws_codebuild_sandboxes_for_project")

        ubx.data(SandboxesForProject, "example", SandboxesForProjectConfig(
            project_name="example-project-name",
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  The real name of the project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ids" type="list(string)">
  The real, unique identifiers of the objects this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
