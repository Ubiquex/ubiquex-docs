---
title: "aws_codebuild_builds_for_project"
description: "`aws_codebuild_builds_for_project` reads the real builds run for a CodeBuild project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_builds_for_project"})

    		ubx.Data(codebuild.BuildsForProject, "example", codebuild.BuildsForProjectConfig{
    			ProjectName: "example-project-name",
    			SortOrder:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BuildsForProject } from "./local-sdk/aws/sdk/typescript/aws/data/codebuild/builds_for_project.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_builds_for_project" });

      data(BuildsForProject, "example", {
        projectName: "example-project-name",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import BuildsForProject, BuildsForProjectConfig

    def describe():
        ubx.intent("look up aws_codebuild_builds_for_project")

        ubx.data(BuildsForProject, "example", BuildsForProjectConfig(
            project_name="example-project-name",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project_name" type="string" required>
  The real name of the project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ids" type="list(string)">
  The real, unique identifiers of the objects this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
