---
title: "aws_codebuild_command_executions_for_sandbox"
description: "`aws_codebuild_command_executions_for_sandbox` reads the real commands executed within a CodeBuild sandbox session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_command_executions_for_sandbox"})

    		ubx.Data(codebuild.CommandExecutionsForSandbox, "example", codebuild.CommandExecutionsForSandboxConfig{
    			SandboxId:  "example",
    			MaxResults: 1,
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommandExecutionsForSandbox } from "@ubx/sdk-aws/aws/data/codebuild/command_executions_for_sandbox";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_command_executions_for_sandbox" });

      data(CommandExecutionsForSandbox, "example", {
        sandboxId: "example",
        maxResults: 1,
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import CommandExecutionsForSandbox, CommandExecutionsForSandboxConfig

    def describe():
        ubx.intent("look up aws_codebuild_command_executions_for_sandbox")

        ubx.data(CommandExecutionsForSandbox, "example", CommandExecutionsForSandboxConfig(
            sandbox_id="example",
            max_results=1,
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sandbox_id" type="string" required>
  The real, unique identifier of the sandbox this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="command_executions" type="list(object)">
  The real command executions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
