---
title: "aws_codebuild_code_coverages"
description: "`aws_codebuild_code_coverages` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codebuild_code_coverages` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_code_coverages"})

    		ubx.Data(codebuild.CodeCoverages, "example", codebuild.CodeCoveragesConfig{
    			ReportArn:                 "arn:aws:iam::123456789012:policy/example",
    			MaxLineCoveragePercentage: 1,
    			MaxResults:                1,
    			MinLineCoveragePercentage: 1,
    			SortBy:                    "example",
    			SortOrder:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CodeCoverages } from "@ubx/sdk-aws/aws/data/codebuild/code_coverages";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_code_coverages" });

      data(CodeCoverages, "example", {
        reportArn: "arn:aws:iam::123456789012:policy/example",
        maxLineCoveragePercentage: 1,
        maxResults: 1,
        minLineCoveragePercentage: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import CodeCoverages, CodeCoveragesConfig

    def describe():
        ubx.intent("look up aws_codebuild_code_coverages")

        ubx.data(CodeCoverages, "example", CodeCoveragesConfig(
            report_arn="arn:aws:iam::123456789012:policy/example",
            max_line_coverage_percentage=1,
            max_results=1,
            min_line_coverage_percentage=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_line_coverage_percentage" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="min_line_coverage_percentage" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="report_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="code_coverages" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
