---
title: "aws_codebuild_source_credentials"
description: "`aws_codebuild_source_credentials` reads the real source provider credentials (such as a GitHub token) registered with CodeBuild."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codebuild_source_credentials"})

    		ubx.Data(codebuild.SourceCredentials, "example", codebuild.SourceCredentialsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SourceCredentials } from "@ubx/sdk-aws/aws/data/codebuild/source_credentials";

    export default stack("example", () => {
      intent({ summary: "look up aws_codebuild_source_credentials" });

      data(SourceCredentials, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codebuild import SourceCredentials, SourceCredentialsConfig

    def describe():
        ubx.intent("look up aws_codebuild_source_credentials")

        ubx.data(SourceCredentials, "example", SourceCredentialsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="source_credentials_infos" type="list(object)">
  The real, detailed information about this object's own source credentials. **(AI-inferred)** Returned by the lookup.
</ResponseField>
