---
title: "aws_data_zone_environment_profile"
description: "`aws_data_zone_environment_profile` is a real, reusable configuration template an `aws_data_zone_environment` gets provisioned from..."
---

`aws_data_zone_environment_profile` is a real, reusable configuration template an `aws_data_zone_environment` gets provisioned from, so every project's environment of a given kind is set up consistently rather than configured from scratch each time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own zone environment profile"})

    		ubx.Resource(data.ZoneEnvironmentProfile, "example", data.ZoneEnvironmentProfileConfig{
    			AwsAccountId:                   "example",
    			AwsAccountRegion:               "example",
    			DomainIdentifier:               "example",
    			EnvironmentBlueprintIdentifier: "example",
    			Name:                           "example",
    			ProjectIdentifier:              "example",
    			Description:                    "Managed by ubx.",
    			UserParameters:                 []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ZoneEnvironmentProfile } from "@ubx/sdk-aws/aws/data/zone_environment_profile";

    export default stack("example", () => {
      intent({ summary: "example own zone environment profile" });

      resource(ZoneEnvironmentProfile, "example", {
        awsAccountId: "example",
        awsAccountRegion: "example",
        domainIdentifier: "example",
        environmentBlueprintIdentifier: "example",
        name: "example",
        projectIdentifier: "example",
        description: "Managed by ubx.",
        userParameters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data import ZoneEnvironmentProfile, ZoneEnvironmentProfileConfig

    def describe():
        ubx.intent("example own zone environment profile")

        ubx.resource(ZoneEnvironmentProfile, "example", ZoneEnvironmentProfileConfig(
            aws_account_id="example",
            aws_account_region="example",
            domain_identifier="example",
            environment_blueprint_identifier="example",
            name="example",
            project_identifier="example",
            description="Managed by ubx.",
            user_parameters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_zone_environment_profile called "example", with
    aws account id example, aws account region example, domain
    identifier example, environment blueprint identifier example,
    project identifier example, description Managed by ubx., user
    parameters a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string" required>
  The AWS account in which the Amazon DataZone environment is created. Required.
</ResponseField>

<ResponseField name="aws_account_region" type="string" required>
  The AWS region in which this environment profile is created. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description of this Amazon DataZone environment profile. Optional.
</ResponseField>

<ResponseField name="domain_identifier" type="string" required>
  The ID of the Amazon DataZone domain in which this environment profile is created. Required.
</ResponseField>

<ResponseField name="environment_blueprint_identifier" type="string" required>
  The ID of the blueprint with which this environment profile is created. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of this Amazon DataZone environment profile. Required.
</ResponseField>

<ResponseField name="project_identifier" type="string" required>
  The identifier of the project in which to create the environment profile. Required.
</ResponseField>

<ResponseField name="user_parameters" type="list(object)">
  The user parameters of this Amazon DataZone environment profile. Optional.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of a user-defined parameter configured in the environment profile, used to match or reference a parameter declared by the associated environment blueprint when provisioning an environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Sets the actual value for a user parameter in the environment profile, which is supplied as the value when the environment is provisioned from this profile. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp of when this environment profile was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  The Amazon DataZone user who created this environment profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_id" type="string">
  The ID of the Amazon DataZone domain in which this environment profile is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="environment_blueprint_id" type="string">
  The ID of the blueprint with which this environment profile is created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of this Amazon DataZone environment profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_id" type="string">
  The identifier of the project in which to create the environment profile. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of when this environment profile was updated. Computed by AWS after `ship`.
</ResponseField>
