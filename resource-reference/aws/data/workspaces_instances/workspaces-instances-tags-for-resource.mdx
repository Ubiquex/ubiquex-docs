---
title: "aws_workspaces_instances_tags_for_resource"
description: "`aws_workspaces_instances_tags_for_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_instances_tags_for_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces_instances "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces_instances"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_instances_tags_for_resource"})

    		ubx.Data(workspaces_instances.WorkspacesInstancesTagsForResource, "example", workspaces_instances.WorkspacesInstancesTagsForResourceConfig{
    			WorkspaceInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspacesInstancesTagsForResource } from "@ubx/sdk-aws/aws/data/workspaces_instances/workspaces_instances_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_instances_tags_for_resource" });

      data(WorkspacesInstancesTagsForResource, "example", {
        workspaceInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces_instances import WorkspacesInstancesTagsForResource, WorkspacesInstancesTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_workspaces_instances_tags_for_resource")

        ubx.data(WorkspacesInstancesTagsForResource, "example", WorkspacesInstancesTagsForResourceConfig(
            workspace_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workspace_instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="list(object)">
  Returned by the lookup.
</ResponseField>
