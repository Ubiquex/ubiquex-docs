---
title: "aws_workspaces_instances_workspace_instances"
description: "`aws_workspaces_instances_workspace_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workspaces_instances_workspace_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspaces_instances "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workspaces_instances"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workspaces_instances_workspace_instances"})

    		ubx.Data(workspaces_instances.WorkspacesInstancesWorkspaceInstances, "example", workspaces_instances.WorkspacesInstancesWorkspaceInstancesConfig{
    			MaxResults:      1,
    			ProvisionStates: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkspacesInstancesWorkspaceInstances } from "@ubx/sdk-aws/aws/data/workspaces_instances/workspaces_instances_workspace_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_workspaces_instances_workspace_instances" });

      data(WorkspacesInstancesWorkspaceInstances, "example", {
        maxResults: 1,
        provisionStates: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workspaces_instances import WorkspacesInstancesWorkspaceInstances, WorkspacesInstancesWorkspaceInstancesConfig

    def describe():
        ubx.intent("look up aws_workspaces_instances_workspace_instances")

        ubx.data(WorkspacesInstancesWorkspaceInstances, "example", WorkspacesInstancesWorkspaceInstancesConfig(
            max_results=1,
            provision_states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="provision_states" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="workspace_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>
