---
title: "aws_es_domain_change_progress"
description: "`aws_es_domain_change_progress` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_es_domain_change_progress` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_domain_change_progress"})

    		ubx.Data(es.DomainChangeProgress, "example", es.DomainChangeProgressConfig{
    			DomainName: "example-domain-name",
    			ChangeId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainChangeProgress } from "@ubx/sdk-aws/aws/data/es/domain_change_progress";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_domain_change_progress" });

      data(DomainChangeProgress, "example", {
        domainName: "example-domain-name",
        changeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import DomainChangeProgress, DomainChangeProgressConfig

    def describe():
        ubx.intent("look up aws_es_domain_change_progress")

        ubx.data(DomainChangeProgress, "example", DomainChangeProgressConfig(
            domain_name="example-domain-name",
            change_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="change_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an Elasticsearch domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_progress_status" type="object">
  &lt;p&gt;The progress details of a specific domain configuration change.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="change_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="change_progress_stages" type="list(object)">
      &lt;p&gt;The list of progress stages of a specific domain configuration change.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="completed_properties" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="config_change_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="initiated_by" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_updated_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="pending_properties" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      &lt;p&gt;The overall status value of the domain configuration change.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_number_of_stages" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
