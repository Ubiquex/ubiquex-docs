---
title: "aws_es_reserved_elasticsearch_instance_offerings"
description: "`aws_es_reserved_elasticsearch_instance_offerings` reads a real, available offering for purchasing an Elasticsearch Reserved Instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_reserved_elasticsearch_instance_offerings"})

    		ubx.Data(es.ReservedElasticsearchInstanceOfferings, "example", es.ReservedElasticsearchInstanceOfferingsConfig{
    			MaxResults:                              1,
    			ReservedElasticsearchInstanceOfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedElasticsearchInstanceOfferings } from "@ubx/sdk-aws/aws/data/es/reserved_elasticsearch_instance_offerings";

    export default stack("example", () => {
      intent({
        summary: "look up aws_es_reserved_elasticsearch_instance_offerings",
      });

      data(ReservedElasticsearchInstanceOfferings, "example", {
        maxResults: 1,
        reservedElasticsearchInstanceOfferingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import ReservedElasticsearchInstanceOfferings, ReservedElasticsearchInstanceOfferingsConfig

    def describe():
        ubx.intent("look up aws_es_reserved_elasticsearch_instance_offerings")

        ubx.data(ReservedElasticsearchInstanceOfferings, "example", ReservedElasticsearchInstanceOfferingsConfig(
            max_results=1,
            reserved_elasticsearch_instance_offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt; Set this value to limit the number of results returned. &lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_elasticsearch_instance_offering_id" type="string">
  The real, unique identifier of the reserved elasticsearch instance offering this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt; Paginated APIs accepts NextToken input to returns next page results and provides a NextToken output in the response which can be used by the client to retrieve more results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_elasticsearch_instance_offerings" type="list(object)">
  The real Elasticsearch Reserved Instance offerings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
