---
title: "aws_es_vpc_endpoint_access"
description: "`aws_es_vpc_endpoint_access` reads which real principals have access to an Elasticsearch VPC endpoint."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_vpc_endpoint_access"})

    		ubx.Data(es.VpcEndpointAccess, "example", es.VpcEndpointAccessConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VpcEndpointAccess } from "./local-sdk/aws/sdk/typescript/aws/data/es/vpc_endpoint_access.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_vpc_endpoint_access" });

      data(VpcEndpointAccess, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.es import VpcEndpointAccess, VpcEndpointAccessConfig

    def describe():
        ubx.intent("look up aws_es_vpc_endpoint_access")

        ubx.data(VpcEndpointAccess, "example", VpcEndpointAccessConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an Elasticsearch domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="authorized_principal_list" type="list(object)">
  The real authorized principals included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt; Paginated APIs accepts NextToken input to returns next page results and provides a NextToken output in the response which can be used by the client to retrieve more results. &lt;/p&gt; Returned by the lookup.
</ResponseField>
