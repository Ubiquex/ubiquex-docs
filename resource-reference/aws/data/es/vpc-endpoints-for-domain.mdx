---
title: "aws_es_vpc_endpoints_for_domain"
description: "`aws_es_vpc_endpoints_for_domain` reads the real VPC endpoints connected to an Elasticsearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_vpc_endpoints_for_domain"})

    		ubx.Data(es.VpcEndpointsForDomain, "example", es.VpcEndpointsForDomainConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcEndpointsForDomain } from "@ubx/sdk-aws/aws/data/es/vpc_endpoints_for_domain";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_vpc_endpoints_for_domain" });

      data(VpcEndpointsForDomain, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import VpcEndpointsForDomain, VpcEndpointsForDomainConfig

    def describe():
        ubx.intent("look up aws_es_vpc_endpoints_for_domain")

        ubx.data(VpcEndpointsForDomain, "example", VpcEndpointsForDomainConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an Elasticsearch domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt; Paginated APIs accepts NextToken input to returns next page results and provides a NextToken output in the response which can be used by the client to retrieve more results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_summary_list" type="list(object)">
  The real, summarized VPC endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
