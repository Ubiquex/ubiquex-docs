---
title: "aws_es_compatible_elasticsearch_versions"
description: "`aws_es_compatible_elasticsearch_versions` reads the real Elasticsearch versions a domain can be upgraded to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_compatible_elasticsearch_versions"})

    		ubx.Data(es.CompatibleElasticsearchVersions, "example", es.CompatibleElasticsearchVersionsConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CompatibleElasticsearchVersions } from "@ubx/sdk-aws/aws/data/es/compatible_elasticsearch_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_compatible_elasticsearch_versions" });

      data(CompatibleElasticsearchVersions, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import CompatibleElasticsearchVersions, CompatibleElasticsearchVersionsConfig

    def describe():
        ubx.intent("look up aws_es_compatible_elasticsearch_versions")

        ubx.data(CompatibleElasticsearchVersions, "example", CompatibleElasticsearchVersionsConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string">
  &lt;p&gt;The name of an Elasticsearch domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compatible_elasticsearch_versions" type="list(object)">
  The real versions of the compatible elasticsearch included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
