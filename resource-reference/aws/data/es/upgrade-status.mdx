---
title: "aws_es_upgrade_status"
description: "`aws_es_upgrade_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_es_upgrade_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_upgrade_status"})

    		ubx.Data(es.UpgradeStatus, "example", es.UpgradeStatusConfig{
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UpgradeStatus } from "@ubx/sdk-aws/aws/data/es/upgrade_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_upgrade_status" });

      data(UpgradeStatus, "example", {
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import UpgradeStatus, UpgradeStatusConfig

    def describe():
        ubx.intent("look up aws_es_upgrade_status")

        ubx.data(UpgradeStatus, "example", UpgradeStatusConfig(
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  &lt;p&gt;The name of an Elasticsearch domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="step_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="upgrade_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="upgrade_step" type="string">
  Returned by the lookup.
</ResponseField>
