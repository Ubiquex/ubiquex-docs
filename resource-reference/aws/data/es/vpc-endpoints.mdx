---
title: "aws_es_vpc_endpoints"
description: "`aws_es_vpc_endpoints` looks up a real VPC endpoint connecting privately to an Elasticsearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_vpc_endpoints"})

    		ubx.Data(es.VpcEndpoints, "example", es.VpcEndpointsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcEndpoints } from "@ubx/sdk-aws/aws/data/es/vpc_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_vpc_endpoints" });

      data(VpcEndpoints, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import VpcEndpoints, VpcEndpointsConfig

    def describe():
        ubx.intent("look up aws_es_vpc_endpoints")

        ubx.data(VpcEndpoints, "example", VpcEndpointsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt; Paginated APIs accepts NextToken input to returns next page results and provides a NextToken output in the response which can be used by the client to retrieve more results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_summary_list" type="list(object)">
  The real, summarized VPC endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
