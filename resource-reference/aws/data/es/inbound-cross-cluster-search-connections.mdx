---
title: "aws_es_inbound_cross_cluster_search_connections"
description: "`aws_es_inbound_cross_cluster_search_connections` reads a real, incoming cross-cluster search connection request to an Elasticsearch domain."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_inbound_cross_cluster_search_connections"})

    		ubx.Data(es.InboundCrossClusterSearchConnections, "example", es.InboundCrossClusterSearchConnectionsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InboundCrossClusterSearchConnections } from "./local-sdk/aws/sdk/typescript/aws/data/es/inbound_cross_cluster_search_connections.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_es_inbound_cross_cluster_search_connections",
      });

      data(InboundCrossClusterSearchConnections, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.es import InboundCrossClusterSearchConnections, InboundCrossClusterSearchConnectionsConfig

    def describe():
        ubx.intent("look up aws_es_inbound_cross_cluster_search_connections")

        ubx.data(InboundCrossClusterSearchConnections, "example", InboundCrossClusterSearchConnectionsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt; Set this value to limit the number of results returned. &lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cross_cluster_search_connections" type="list(object)">
  The real OpenSearch cross-cluster search connections included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt; Paginated APIs accepts NextToken input to returns next page results and provides a NextToken output in the response which can be used by the client to retrieve more results. &lt;/p&gt; Returned by the lookup.
</ResponseField>
