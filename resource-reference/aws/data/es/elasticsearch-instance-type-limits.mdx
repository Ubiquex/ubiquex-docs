---
title: "aws_es_elasticsearch_instance_type_limits"
description: "`aws_es_elasticsearch_instance_type_limits` reads the real limits applicable to an Elasticsearch instance type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_elasticsearch_instance_type_limits"})

    		ubx.Data(es.ElasticsearchInstanceTypeLimits, "example", es.ElasticsearchInstanceTypeLimitsConfig{
    			ElasticsearchVersion: "example",
    			InstanceType:         "example",
    			DomainName:           "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticsearchInstanceTypeLimits } from "@ubx/sdk-aws/aws/data/es/elasticsearch_instance_type_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_elasticsearch_instance_type_limits" });

      data(ElasticsearchInstanceTypeLimits, "example", {
        elasticsearchVersion: "example",
        instanceType: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import ElasticsearchInstanceTypeLimits, ElasticsearchInstanceTypeLimitsConfig

    def describe():
        ubx.intent("look up aws_es_elasticsearch_instance_type_limits")

        ubx.data(ElasticsearchInstanceTypeLimits, "example", ElasticsearchInstanceTypeLimitsConfig(
            elasticsearch_version="example",
            instance_type="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string">
  &lt;p&gt;The name of an Elasticsearch domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen).&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="elasticsearch_version" type="string" required>
  The real version of the elasticsearch. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  The real instance type this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="limits_by_role" type="map(object)">
  &lt;p&gt; Map of Role of the Instance and Limits that are applicable. Role performed by given Instance in Elasticsearch can be one of the following: &lt;ul&gt; &lt;li&gt;data: If the given InstanceType is used as data node&lt;/li&gt; &lt;li&gt;master: If the given InstanceType is used as master node&lt;/li&gt; &lt;li&gt;ultra_warm: If the given InstanceType is used as warm node&lt;/li&gt; &lt;/ul&gt; &lt;/p&gt; Returned by the lookup.
</ResponseField>
