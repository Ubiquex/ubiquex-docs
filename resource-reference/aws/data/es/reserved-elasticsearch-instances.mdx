---
title: "aws_es_reserved_elasticsearch_instances"
description: "`aws_es_reserved_elasticsearch_instances` looks up a real, purchased Elasticsearch Reserved Instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_reserved_elasticsearch_instances"})

    		ubx.Data(es.ReservedElasticsearchInstances, "example", es.ReservedElasticsearchInstancesConfig{
    			MaxResults:                      1,
    			ReservedElasticsearchInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ReservedElasticsearchInstances } from "./local-sdk/aws/sdk/typescript/aws/data/es/reserved_elasticsearch_instances.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_reserved_elasticsearch_instances" });

      data(ReservedElasticsearchInstances, "example", {
        maxResults: 1,
        reservedElasticsearchInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.es import ReservedElasticsearchInstances, ReservedElasticsearchInstancesConfig

    def describe():
        ubx.intent("look up aws_es_reserved_elasticsearch_instances")

        ubx.data(ReservedElasticsearchInstances, "example", ReservedElasticsearchInstancesConfig(
            max_results=1,
            reserved_elasticsearch_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt; Set this value to limit the number of results returned. &lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_elasticsearch_instance_id" type="string">
  The real, unique identifier of the reserved elasticsearch instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt; Paginated APIs accepts NextToken input to returns next page results and provides a NextToken output in the response which can be used by the client to retrieve more results. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_elasticsearch_instances" type="list(object)">
  The real Elasticsearch Reserved Instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
