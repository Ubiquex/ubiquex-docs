---
title: "aws_es_domain_names"
description: "`aws_es_domain_names` reads the real names of the Elasticsearch domains on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	es "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/es"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_es_domain_names"})

    		ubx.Data(es.DomainNames, "example", es.DomainNamesConfig{
    			EngineType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainNames } from "@ubx/sdk-aws/aws/data/es/domain_names";

    export default stack("example", () => {
      intent({ summary: "look up aws_es_domain_names" });

      data(DomainNames, "example", {
        engineType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.es import DomainNames, DomainNamesConfig

    def describe():
        ubx.intent("look up aws_es_domain_names")

        ubx.data(DomainNames, "example", DomainNamesConfig(
            engine_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="engine_type" type="string">
  The real database or compute engine type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_names" type="list(object)">
  &lt;p&gt; Contains the list of Elasticsearch domain information.&lt;/p&gt; Returned by the lookup.
</ResponseField>
