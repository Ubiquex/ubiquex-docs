---
title: "aws_compute_optimizer_ecsservice_recommendations"
description: "`aws_compute_optimizer_ecsservice_recommendations` reads real, system-generated Compute Optimizer rightsizing recommendations for an ECS service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_ecsservice_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerEcsserviceRecommendations, "example", compute_optimizer.ComputeOptimizerEcsserviceRecommendationsConfig{
    			AccountIds:  []string{"example"},
    			Filters:     []map[string]any{{"name": "example-name"}},
    			MaxResults:  1,
    			ServiceArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerEcsserviceRecommendations } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_ecsservice_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_compute_optimizer_ecsservice_recommendations",
      });

      data(ComputeOptimizerEcsserviceRecommendations, "example", {
        accountIds: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
        serviceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerEcsserviceRecommendations, ComputeOptimizerEcsserviceRecommendationsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_ecsservice_recommendations")

        ubx.data(ComputeOptimizerEcsserviceRecommendations, "example", ComputeOptimizerEcsserviceRecommendationsConfig(
            account_ids=["example"],
            filters=[{"name": "example-name"}],
            max_results=1,
            service_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the services this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ecs_service_recommendations" type="list(object)">
  The real, recommended ECS service configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  The real errors encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
