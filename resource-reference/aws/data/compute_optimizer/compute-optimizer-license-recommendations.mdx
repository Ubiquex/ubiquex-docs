---
title: "aws_compute_optimizer_license_recommendations"
description: "`aws_compute_optimizer_license_recommendations` reads real, system-generated Compute Optimizer recommendations for optimizing licensed software costs..."
---

`aws_compute_optimizer_license_recommendations` reads real, system-generated Compute Optimizer recommendations for optimizing licensed software costs on an EC2 instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_license_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerLicenseRecommendations, "example", compute_optimizer.ComputeOptimizerLicenseRecommendationsConfig{
    			AccountIds:   []string{"example"},
    			Filters:      []map[string]any{{"name": "example-name"}},
    			MaxResults:   1,
    			ResourceArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerLicenseRecommendations } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_license_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_compute_optimizer_license_recommendations" });

      data(ComputeOptimizerLicenseRecommendations, "example", {
        accountIds: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
        resourceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerLicenseRecommendations, ComputeOptimizerLicenseRecommendationsConfig
    from ubx.aws.data.compute_optimizer.compute_optimizer_license_recommendations import ComputeOptimizerLicenseRecommendations_Filters

    def describe():
        ubx.intent("look up aws_compute_optimizer_license_recommendations")

        ubx.data(ComputeOptimizerLicenseRecommendations, "example", ComputeOptimizerLicenseRecommendationsConfig(
            account_ids=["example"],
            filters=[ComputeOptimizerLicenseRecommendations_Filters(name="example-name")],
            max_results=1,
            resource_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="errors" type="list(object)">
  The real errors encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="license_recommendations" type="list(object)">
  The real, recommended license configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
