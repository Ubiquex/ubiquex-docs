---
title: "aws_compute_optimizer_rdsdatabase_recommendations"
description: "`aws_compute_optimizer_rdsdatabase_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_compute_optimizer_rdsdatabase_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_rdsdatabase_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerRdsdatabaseRecommendations, "example", compute_optimizer.ComputeOptimizerRdsdatabaseRecommendationsConfig{
    			AccountIds:                []string{"example"},
    			Filters:                   []map[string]any{{"name": "example-name"}},
    			MaxResults:                1,
    			RecommendationPreferences: map[string]any{"cpu_vendor_architectures": []string{"example"}},
    			ResourceArns:              []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerRdsdatabaseRecommendations } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_rdsdatabase_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_compute_optimizer_rdsdatabase_recommendations",
      });

      data(ComputeOptimizerRdsdatabaseRecommendations, "example", {
        accountIds: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
        recommendationPreferences: { cpuVendorArchitectures: ["example"] },
        resourceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerRdsdatabaseRecommendations, ComputeOptimizerRdsdatabaseRecommendationsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_rdsdatabase_recommendations")

        ubx.data(ComputeOptimizerRdsdatabaseRecommendations, "example", ComputeOptimizerRdsdatabaseRecommendationsConfig(
            account_ids=["example"],
            filters=[{"name": "example-name"}],
            max_results=1,
            recommendation_preferences={"cpu_vendor_architectures": ["example"]},
            resource_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recommendation_preferences" type="object">
  &lt;p&gt;Describes the recommendation preferences to return in the response of a &lt;a&gt;GetAutoScalingGroupRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2InstanceRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2RecommendationProjectedMetrics&lt;/a&gt;, &lt;a&gt;GetRDSDatabaseRecommendations&lt;/a&gt;, and &lt;a&gt;GetRDSDatabaseRecommendationProjectedMetrics&lt;/a&gt; request.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="cpu_vendor_architectures" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="errors" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rds_dbrecommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>
