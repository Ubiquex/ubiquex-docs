---
title: "aws_compute_optimizer_effective_recommendation_preferences"
description: "`aws_compute_optimizer_effective_recommendation_preferences` reads the real, effective Compute Optimizer recommendation preferences applied to..."
---

`aws_compute_optimizer_effective_recommendation_preferences` reads the real, effective Compute Optimizer recommendation preferences applied to a resource, after resolving account and organization defaults.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_effective_recommendation_preferences"})

    		ubx.Data(compute_optimizer.ComputeOptimizerEffectiveRecommendationPreferences, "example", compute_optimizer.ComputeOptimizerEffectiveRecommendationPreferencesConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComputeOptimizerEffectiveRecommendationPreferences } from "./local-sdk/aws/sdk/typescript/aws/data/compute_optimizer/compute_optimizer_effective_recommendation_preferences.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_compute_optimizer_effective_recommendation_preferences",
      });

      data(ComputeOptimizerEffectiveRecommendationPreferences, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerEffectiveRecommendationPreferences, ComputeOptimizerEffectiveRecommendationPreferencesConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_effective_recommendation_preferences")

        ubx.data(ComputeOptimizerEffectiveRecommendationPreferences, "example", ComputeOptimizerEffectiveRecommendationPreferencesConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enhanced_infrastructure_metrics" type="string">
  Whether enhanced infrastructure metrics are enabled for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="external_metrics_preference" type="object">
  &lt;p&gt; Describes the external metrics preferences for EC2 rightsizing recommendations. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="source" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="look_back_period" type="string">
  The real time window results are computed over, looking back from now. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="preferred_resources" type="list(object)">
  The real, preferred resources for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="utilization_preferences" type="list(object)">
  The real, configured utilization preferences for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
