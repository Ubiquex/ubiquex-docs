---
title: "aws_compute_optimizer_lambda_function_recommendations"
description: "`aws_compute_optimizer_lambda_function_recommendations` reads real, system-generated Compute Optimizer rightsizing recommendations for a Lambda function."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_lambda_function_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerLambdaFunctionRecommendations, "example", compute_optimizer.ComputeOptimizerLambdaFunctionRecommendationsConfig{
    			AccountIds:   []string{"example"},
    			Filters:      []map[string]any{{"name": "example-name"}},
    			FunctionArns: []string{"example"},
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerLambdaFunctionRecommendations } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_lambda_function_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_compute_optimizer_lambda_function_recommendations",
      });

      data(ComputeOptimizerLambdaFunctionRecommendations, "example", {
        accountIds: ["example"],
        filters: [{ name: "example-name" }],
        functionArns: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerLambdaFunctionRecommendations, ComputeOptimizerLambdaFunctionRecommendationsConfig
    from ubx.aws.data.compute_optimizer.compute_optimizer_lambda_function_recommendations import ComputeOptimizerLambdaFunctionRecommendations_Filters

    def describe():
        ubx.intent("look up aws_compute_optimizer_lambda_function_recommendations")

        ubx.data(ComputeOptimizerLambdaFunctionRecommendations, "example", ComputeOptimizerLambdaFunctionRecommendationsConfig(
            account_ids=["example"],
            filters=[ComputeOptimizerLambdaFunctionRecommendations_Filters(name="example-name")],
            function_arns=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="function_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the functions this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lambda_function_recommendations" type="list(object)">
  The real, recommended Lambda function configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
