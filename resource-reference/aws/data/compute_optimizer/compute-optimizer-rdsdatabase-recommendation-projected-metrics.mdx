---
title: "aws_compute_optimizer_rdsdatabase_recommendation_projected_metrics"
description: "`aws_compute_optimizer_rdsdatabase_recommendation_projected_metrics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_compute_optimizer_rdsdatabase_recommendation_projected_metrics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_rdsdatabase_recommendation_projected_metrics"})

    		ubx.Data(compute_optimizer.ComputeOptimizerRdsdatabaseRecommendationProjectedMetrics, "example", compute_optimizer.ComputeOptimizerRdsdatabaseRecommendationProjectedMetricsConfig{
    			EndTime:                   "example",
    			Period:                    1,
    			ResourceArn:               "arn:aws:iam::123456789012:policy/example",
    			StartTime:                 "example",
    			Stat:                      "example",
    			RecommendationPreferences: map[string]any{"cpu_vendor_architectures": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerRdsdatabaseRecommendationProjectedMetrics } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_rdsdatabase_recommendation_projected_metrics";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_compute_optimizer_rdsdatabase_recommendation_projected_metrics",
      });

      data(ComputeOptimizerRdsdatabaseRecommendationProjectedMetrics, "example", {
        endTime: "example",
        period: 1,
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        startTime: "example",
        stat: "example",
        recommendationPreferences: { cpuVendorArchitectures: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerRdsdatabaseRecommendationProjectedMetrics, ComputeOptimizerRdsdatabaseRecommendationProjectedMetricsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_rdsdatabase_recommendation_projected_metrics")

        ubx.data(ComputeOptimizerRdsdatabaseRecommendationProjectedMetrics, "example", ComputeOptimizerRdsdatabaseRecommendationProjectedMetricsConfig(
            end_time="example",
            period=1,
            resource_arn="arn:aws:iam::123456789012:policy/example",
            start_time="example",
            stat="example",
            recommendation_preferences={"cpu_vendor_architectures": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recommendation_preferences" type="object">
  &lt;p&gt;Describes the recommendation preferences to return in the response of a &lt;a&gt;GetAutoScalingGroupRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2InstanceRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2RecommendationProjectedMetrics&lt;/a&gt;, &lt;a&gt;GetRDSDatabaseRecommendations&lt;/a&gt;, and &lt;a&gt;GetRDSDatabaseRecommendationProjectedMetrics&lt;/a&gt; request.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="cpu_vendor_architectures" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stat" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="recommended_option_projected_metrics" type="list(object)">
  Returned by the lookup.
</ResponseField>
