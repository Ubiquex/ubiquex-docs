---
title: "aws_compute_optimizer_idle_recommendations"
description: "`aws_compute_optimizer_idle_recommendations` reads real, system-generated Compute Optimizer recommendations flagging an idle resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_idle_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerIdleRecommendations, "example", compute_optimizer.ComputeOptimizerIdleRecommendationsConfig{
    			AccountIds:   []string{"example"},
    			Filters:      []map[string]any{{"name": "example-name"}},
    			MaxResults:   1,
    			OrderBy:      map[string]any{"dimension": "example"},
    			ResourceArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerIdleRecommendations } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_idle_recommendations";

    export default stack("example", () => {
      intent({ summary: "look up aws_compute_optimizer_idle_recommendations" });

      data(ComputeOptimizerIdleRecommendations, "example", {
        accountIds: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
        orderBy: { dimension: "example" },
        resourceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerIdleRecommendations, ComputeOptimizerIdleRecommendationsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_idle_recommendations")

        ubx.data(ComputeOptimizerIdleRecommendations, "example", ComputeOptimizerIdleRecommendationsConfig(
            account_ids=["example"],
            filters=[{"name": "example-name"}],
            max_results=1,
            order_by={"dimension": "example"},
            resource_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="object">
  &lt;p&gt;Describes how the recommendations are ordered.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimension" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="errors" type="list(object)">
  The real errors encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="idle_recommendations" type="list(object)">
  The real, recommended actions for idle resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
