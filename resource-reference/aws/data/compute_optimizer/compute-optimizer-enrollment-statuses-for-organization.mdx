---
title: "aws_compute_optimizer_enrollment_statuses_for_organization"
description: "`aws_compute_optimizer_enrollment_statuses_for_organization` reads the real Compute Optimizer enrollment status of every account within an AWS..."
---

`aws_compute_optimizer_enrollment_statuses_for_organization` reads the real Compute Optimizer enrollment status of every account within an AWS Organization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_enrollment_statuses_for_organization"})

    		ubx.Data(compute_optimizer.ComputeOptimizerEnrollmentStatusesForOrganization, "example", compute_optimizer.ComputeOptimizerEnrollmentStatusesForOrganizationConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerEnrollmentStatusesForOrganization } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_enrollment_statuses_for_organization";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_compute_optimizer_enrollment_statuses_for_organization",
      });

      data(ComputeOptimizerEnrollmentStatusesForOrganization, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerEnrollmentStatusesForOrganization, ComputeOptimizerEnrollmentStatusesForOrganizationConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_enrollment_statuses_for_organization")

        ubx.data(ComputeOptimizerEnrollmentStatusesForOrganization, "example", ComputeOptimizerEnrollmentStatusesForOrganizationConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_enrollment_statuses" type="list(object)">
  The real, current statuses of the account enrollments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
