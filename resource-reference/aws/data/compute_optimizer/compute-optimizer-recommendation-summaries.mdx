---
title: "aws_compute_optimizer_recommendation_summaries"
description: "`aws_compute_optimizer_recommendation_summaries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_compute_optimizer_recommendation_summaries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_recommendation_summaries"})

    		ubx.Data(compute_optimizer.ComputeOptimizerRecommendationSummaries, "example", compute_optimizer.ComputeOptimizerRecommendationSummariesConfig{
    			AccountIds: []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerRecommendationSummaries } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_recommendation_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_compute_optimizer_recommendation_summaries" });

      data(ComputeOptimizerRecommendationSummaries, "example", {
        accountIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerRecommendationSummaries, ComputeOptimizerRecommendationSummariesConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_recommendation_summaries")

        ubx.data(ComputeOptimizerRecommendationSummaries, "example", ComputeOptimizerRecommendationSummariesConfig(
            account_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
