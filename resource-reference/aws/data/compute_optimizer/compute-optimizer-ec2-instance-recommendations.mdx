---
title: "aws_compute_optimizer_ec2_instance_recommendations"
description: "`aws_compute_optimizer_ec2_instance_recommendations` reads real, system-generated Compute Optimizer rightsizing recommendations for an EC2 instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_ec2_instance_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerEc2InstanceRecommendations, "example", compute_optimizer.ComputeOptimizerEc2InstanceRecommendationsConfig{
    			AccountIds:                []string{"example"},
    			Filters:                   []map[string]any{{"name": "example-name"}},
    			InstanceArns:              []string{"example"},
    			MaxResults:                1,
    			RecommendationPreferences: map[string]any{"cpu_vendor_architectures": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComputeOptimizerEc2InstanceRecommendations } from "./local-sdk/aws/sdk/typescript/aws/data/compute_optimizer/compute_optimizer_ec2_instance_recommendations.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_compute_optimizer_ec2_instance_recommendations",
      });

      data(ComputeOptimizerEc2InstanceRecommendations, "example", {
        accountIds: ["example"],
        filters: [{ name: "example-name" }],
        instanceArns: ["example"],
        maxResults: 1,
        recommendationPreferences: { cpuVendorArchitectures: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerEc2InstanceRecommendations, ComputeOptimizerEc2InstanceRecommendationsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_ec2_instance_recommendations")

        ubx.data(ComputeOptimizerEc2InstanceRecommendations, "example", ComputeOptimizerEc2InstanceRecommendationsConfig(
            account_ids=["example"],
            filters=[{"name": "example-name"}],
            instance_arns=["example"],
            max_results=1,
            recommendation_preferences={"cpu_vendor_architectures": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recommendation_preferences" type="object">
  &lt;p&gt;Describes the recommendation preferences to return in the response of a &lt;a&gt;GetAutoScalingGroupRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2InstanceRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2RecommendationProjectedMetrics&lt;/a&gt;, &lt;a&gt;GetRDSDatabaseRecommendations&lt;/a&gt;, and &lt;a&gt;GetRDSDatabaseRecommendationProjectedMetrics&lt;/a&gt; request.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="cpu_vendor_architectures" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="errors" type="list(object)">
  The real errors encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instance_recommendations" type="list(object)">
  The real, recommended instance configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
