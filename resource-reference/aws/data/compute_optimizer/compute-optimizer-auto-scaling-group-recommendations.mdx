---
title: "aws_compute_optimizer_auto_scaling_group_recommendations"
description: "`aws_compute_optimizer_auto_scaling_group_recommendations` reads real, system-generated Compute Optimizer rightsizing recommendations for an Auto..."
---

`aws_compute_optimizer_auto_scaling_group_recommendations` reads real, system-generated Compute Optimizer rightsizing recommendations for an Auto Scaling group.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_auto_scaling_group_recommendations"})

    		ubx.Data(compute_optimizer.ComputeOptimizerAutoScalingGroupRecommendations, "example", compute_optimizer.ComputeOptimizerAutoScalingGroupRecommendationsConfig{
    			AccountIds:                []string{"example"},
    			AutoScalingGroupArns:      []string{"example"},
    			Filters:                   []map[string]any{{"name": "example-name"}},
    			MaxResults:                1,
    			RecommendationPreferences: map[string]any{"cpu_vendor_architectures": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerAutoScalingGroupRecommendations } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_auto_scaling_group_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_compute_optimizer_auto_scaling_group_recommendations",
      });

      data(ComputeOptimizerAutoScalingGroupRecommendations, "example", {
        accountIds: ["example"],
        autoScalingGroupArns: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
        recommendationPreferences: { cpuVendorArchitectures: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerAutoScalingGroupRecommendations, ComputeOptimizerAutoScalingGroupRecommendationsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_auto_scaling_group_recommendations")

        ubx.data(ComputeOptimizerAutoScalingGroupRecommendations, "example", ComputeOptimizerAutoScalingGroupRecommendationsConfig(
            account_ids=["example"],
            auto_scaling_group_arns=["example"],
            filters=[{"name": "example-name"}],
            max_results=1,
            recommendation_preferences={"cpu_vendor_architectures": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="auto_scaling_group_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the auto scaling groups this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recommendation_preferences" type="object">
  &lt;p&gt;Describes the recommendation preferences to return in the response of a &lt;a&gt;GetAutoScalingGroupRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2InstanceRecommendations&lt;/a&gt;, &lt;a&gt;GetEC2RecommendationProjectedMetrics&lt;/a&gt;, &lt;a&gt;GetRDSDatabaseRecommendations&lt;/a&gt;, and &lt;a&gt;GetRDSDatabaseRecommendationProjectedMetrics&lt;/a&gt; request.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="cpu_vendor_architectures" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="auto_scaling_group_recommendations" type="list(object)">
  The real, recommended Auto Scaling group configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  The real errors encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
