---
title: "aws_compute_optimizer_recommendation_export_jobs"
description: "`aws_compute_optimizer_recommendation_export_jobs` reads the real jobs exporting Compute Optimizer recommendations to S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_recommendation_export_jobs"})

    		ubx.Data(compute_optimizer.ComputeOptimizerRecommendationExportJobs, "example", compute_optimizer.ComputeOptimizerRecommendationExportJobsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			JobIds:     []string{"example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerRecommendationExportJobs } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_recommendation_export_jobs";

    export default stack("example", () => {
      intent({
        summary: "look up aws_compute_optimizer_recommendation_export_jobs",
      });

      data(ComputeOptimizerRecommendationExportJobs, "example", {
        filters: [{ name: "example-name" }],
        jobIds: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerRecommendationExportJobs, ComputeOptimizerRecommendationExportJobsConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_recommendation_export_jobs")

        ubx.data(ComputeOptimizerRecommendationExportJobs, "example", ComputeOptimizerRecommendationExportJobsConfig(
            filters=[{"name": "example-name"}],
            job_ids=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_ids" type="list(string)">
  The real, unique identifiers of the jobs this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommendation_export_jobs" type="list(object)">
  The real recommendation export jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
