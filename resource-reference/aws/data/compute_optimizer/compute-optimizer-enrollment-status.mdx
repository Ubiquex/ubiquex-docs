---
title: "aws_compute_optimizer_enrollment_status"
description: "`aws_compute_optimizer_enrollment_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_compute_optimizer_enrollment_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	compute_optimizer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/compute_optimizer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_compute_optimizer_enrollment_status"})

    		ubx.Data(compute_optimizer.ComputeOptimizerEnrollmentStatus, "example", compute_optimizer.ComputeOptimizerEnrollmentStatusConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeOptimizerEnrollmentStatus } from "@ubx/sdk-aws/aws/data/compute_optimizer/compute_optimizer_enrollment_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_compute_optimizer_enrollment_status" });

      data(ComputeOptimizerEnrollmentStatus, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.compute_optimizer import ComputeOptimizerEnrollmentStatus, ComputeOptimizerEnrollmentStatusConfig

    def describe():
        ubx.intent("look up aws_compute_optimizer_enrollment_status")

        ubx.data(ComputeOptimizerEnrollmentStatus, "example", ComputeOptimizerEnrollmentStatusConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="last_updated_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="member_accounts_enrolled" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_member_accounts_opted_in" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Returned by the lookup.
</ResponseField>
