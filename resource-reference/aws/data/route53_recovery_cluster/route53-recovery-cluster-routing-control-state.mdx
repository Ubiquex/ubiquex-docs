---
title: "aws_route53_recovery_cluster_routing_control_state"
description: "`aws_route53_recovery_cluster_routing_control_state` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_recovery_cluster_routing_control_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53_recovery_cluster "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53_recovery_cluster"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_recovery_cluster_routing_control_state"})

    		ubx.Data(route53_recovery_cluster.Route53RecoveryClusterRoutingControlState, "example", route53_recovery_cluster.Route53RecoveryClusterRoutingControlStateConfig{
    			RoutingControlArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Route53RecoveryClusterRoutingControlState } from "@ubx/sdk-aws/aws/data/route53_recovery_cluster/route53_recovery_cluster_routing_control_state";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53_recovery_cluster_routing_control_state",
      });

      data(Route53RecoveryClusterRoutingControlState, "example", {
        routingControlArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53_recovery_cluster import Route53RecoveryClusterRoutingControlState, Route53RecoveryClusterRoutingControlStateConfig

    def describe():
        ubx.intent("look up aws_route53_recovery_cluster_routing_control_state")

        ubx.data(Route53RecoveryClusterRoutingControlState, "example", Route53RecoveryClusterRoutingControlStateConfig(
            routing_control_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="routing_control_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="routing_control_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="routing_control_state" type="string">
  Returned by the lookup.
</ResponseField>
