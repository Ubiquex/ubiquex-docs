---
title: "aws_bcm_recommended_actions_recommended_actions"
description: "`aws_bcm_recommended_actions_recommended_actions` reads real, system-generated Billing and Cost Management recommended actions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bcm_recommended_actions "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bcm_recommended_actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bcm_recommended_actions_recommended_actions"})

    		ubx.Data(bcm_recommended_actions.BcmRecommendedActionsRecommendedActions, "example", bcm_recommended_actions.BcmRecommendedActionsRecommendedActionsConfig{
    			Filter:     map[string]any{"actions": []map[string]any{{"key": "example"}}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BcmRecommendedActionsRecommendedActions } from "@ubx/sdk-aws/aws/data/bcm_recommended_actions/bcm_recommended_actions_recommended_actions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_bcm_recommended_actions_recommended_actions",
      });

      data(BcmRecommendedActionsRecommendedActions, "example", {
        filter: { actions: [{ key: "example" }] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bcm_recommended_actions import BcmRecommendedActionsRecommendedActions, BcmRecommendedActionsRecommendedActionsConfig

    def describe():
        ubx.intent("look up aws_bcm_recommended_actions_recommended_actions")

        ubx.data(BcmRecommendedActionsRecommendedActions, "example", BcmRecommendedActionsRecommendedActionsConfig(
            filter={"actions": [{"key": "example"}]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Enables filtering of results based on specified action criteria. You can define multiple action filters to refine results using combinations of feature type, severity level, and recommendation type.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recommended_actions" type="list(object)">
  The real, recommended actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
