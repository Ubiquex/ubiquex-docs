---
title: "aws_data_signer_revocation_status"
description: "`aws_data_signer_revocation_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_signer_revocation_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_signer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_signer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_signer_revocation_status"})

    		ubx.Data(data_signer.DataSignerRevocationStatus, "example", data_signer.DataSignerRevocationStatusConfig{
    			CertificateHashes:  []string{"example"},
    			JobArn:             "arn:aws:iam::123456789012:policy/example",
    			PlatformId:         "example",
    			ProfileVersionArn:  "arn:aws:iam::123456789012:policy/example",
    			SignatureTimestamp: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSignerRevocationStatus } from "@ubx/sdk-aws/aws/data/data_signer/data_signer_revocation_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_signer_revocation_status" });

      data(DataSignerRevocationStatus, "example", {
        certificateHashes: ["example"],
        jobArn: "arn:aws:iam::123456789012:policy/example",
        platformId: "example",
        profileVersionArn: "arn:aws:iam::123456789012:policy/example",
        signatureTimestamp: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_signer import DataSignerRevocationStatus, DataSignerRevocationStatusConfig

    def describe():
        ubx.intent("look up aws_data_signer_revocation_status")

        ubx.data(DataSignerRevocationStatus, "example", DataSignerRevocationStatusConfig(
            certificate_hashes=["example"],
            job_arn="arn:aws:iam::123456789012:policy/example",
            platform_id="example",
            profile_version_arn="arn:aws:iam::123456789012:policy/example",
            signature_timestamp="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="certificate_hashes" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="platform_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="profile_version_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="signature_timestamp" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="revoked_entities" type="list(string)">
  Returned by the lookup.
</ResponseField>
