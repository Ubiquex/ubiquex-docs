---
title: "aws_frauddetector_model_versions"
description: "`aws_frauddetector_model_versions` looks up one real, trained version of a Fraud Detector model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_frauddetector_model_versions"})

    		ubx.Data(frauddetector.ModelVersions, "example", frauddetector.ModelVersionsConfig{
    			MaxResults:         1,
    			ModelId:            "example",
    			ModelType:          "example",
    			ModelVersionNumber: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelVersions } from "@ubx/sdk-aws/aws/data/frauddetector/model_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_frauddetector_model_versions" });

      data(ModelVersions, "example", {
        maxResults: 1,
        modelId: "example",
        modelType: "example",
        modelVersionNumber: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.frauddetector import ModelVersions, ModelVersionsConfig

    def describe():
        ubx.intent("look up aws_frauddetector_model_versions")

        ubx.data(ModelVersions, "example", ModelVersionsConfig(
            max_results=1,
            model_id="example",
            model_type="example",
            model_version_number="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_id" type="string">
  The real, unique identifier of the model this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_type" type="string">
  The real, discriminating type of the model this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_version_number" type="string">
  The real number of the model version. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="model_version_details" type="list(object)">
  The real, detailed information about the model version. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
