---
title: "aws_frauddetector_kmsencryption_key"
description: "`aws_frauddetector_kmsencryption_key` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_frauddetector_kmsencryption_key` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_frauddetector_kmsencryption_key"})

    		ubx.Data(frauddetector.KmsencryptionKey, "example", frauddetector.KmsencryptionKeyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KmsencryptionKey } from "@ubx/sdk-aws/aws/data/frauddetector/kmsencryption_key";

    export default stack("example", () => {
      intent({ summary: "look up aws_frauddetector_kmsencryption_key" });

      data(KmsencryptionKey, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.frauddetector import KmsencryptionKey, KmsencryptionKeyConfig

    def describe():
        ubx.intent("look up aws_frauddetector_kmsencryption_key")

        ubx.data(KmsencryptionKey, "example", KmsencryptionKeyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="kms_key" type="object">
  &lt;p&gt;The KMS key details.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_encryption_key_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
