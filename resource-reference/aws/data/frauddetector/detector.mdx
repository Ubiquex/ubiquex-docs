---
title: "aws_frauddetector_detector"
description: "`data_aws_frauddetector_detector` looks up a real, already-existing `aws_fraud_detector_detector`."
---

It was the real, top-level Amazon Fraud Detector resource combining an `aws_fraud_detector_event_type`, a trained model, and business rules to actually score real-time fraud risk. Amazon Fraud Detector closed to new customers on November 7, 2025; existing customers can continue using it, with AWS pointing to Amazon SageMaker, AutoGluon, or AWS WAF Fraud Control for new fraud-detection work.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_frauddetector_detector"})

    		ubx.Data(frauddetector.Detector, "example", frauddetector.DetectorConfig{
    			DetectorId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Detector } from "@ubx/sdk-aws/aws/data/frauddetector/detector";

    export default stack("example", () => {
      intent({ summary: "look up aws_frauddetector_detector" });

      data(Detector, "example", {
        detectorId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.frauddetector import Detector, DetectorConfig

    def describe():
        ubx.intent("look up aws_frauddetector_detector")

        ubx.data(Detector, "example", DetectorConfig(
            detector_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detector_id" type="string" required>
  The real, unique identifier of the detector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="detector_version_summaries" type="list(object)">
  The real, summarized detector versions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
