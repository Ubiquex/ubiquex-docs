---
title: "aws_frauddetector_lists_metadata"
description: "`aws_frauddetector_lists_metadata` reads real metadata about a Fraud Detector list, a reusable set of reference values used in a rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_frauddetector_lists_metadata"})

    		ubx.Data(frauddetector.ListsMetadata, "example", frauddetector.ListsMetadataConfig{
    			Name:       "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListsMetadata } from "@ubx/sdk-aws/aws/data/frauddetector/lists_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_frauddetector_lists_metadata" });

      data(ListsMetadata, "example", {
        name: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.frauddetector import ListsMetadata, ListsMetadataConfig

    def describe():
        ubx.intent("look up aws_frauddetector_lists_metadata")

        ubx.data(ListsMetadata, "example", ListsMetadataConfig(
            name="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lists" type="list(object)">
  The real lists included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
