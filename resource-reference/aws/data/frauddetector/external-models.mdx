---
title: "aws_frauddetector_external_models"
description: "`aws_frauddetector_external_models` looks up a real, external SageMaker model registered with Fraud Detector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_frauddetector_external_models"})

    		ubx.Data(frauddetector.ExternalModels, "example", frauddetector.ExternalModelsConfig{
    			MaxResults:    1,
    			ModelEndpoint: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExternalModels } from "@ubx/sdk-aws/aws/data/frauddetector/external_models";

    export default stack("example", () => {
      intent({ summary: "look up aws_frauddetector_external_models" });

      data(ExternalModels, "example", {
        maxResults: 1,
        modelEndpoint: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.frauddetector import ExternalModels, ExternalModelsConfig

    def describe():
        ubx.intent("look up aws_frauddetector_external_models")

        ubx.data(ExternalModels, "example", ExternalModelsConfig(
            max_results=1,
            model_endpoint="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="model_endpoint" type="string">
  The real endpoint this model is deployed to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="external_models" type="list(object)">
  The real external models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
