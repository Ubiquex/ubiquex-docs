---
title: "aws_uxc_account_customizations"
description: "`aws_uxc_account_customizations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_uxc_account_customizations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	uxc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/uxc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_uxc_account_customizations"})

    		ubx.Data(uxc.AccountCustomizations, "example", uxc.AccountCustomizationsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountCustomizations } from "@ubx/sdk-aws/aws/data/uxc/account_customizations";

    export default stack("example", () => {
      intent({ summary: "look up aws_uxc_account_customizations" });

      data(AccountCustomizations, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.uxc import AccountCustomizations, AccountCustomizationsConfig

    def describe():
        ubx.intent("look up aws_uxc_account_customizations")

        ubx.data(AccountCustomizations, "example", AccountCustomizationsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_color" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="visible_regions" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="visible_services" type="list(string)">
  Returned by the lookup.
</ResponseField>
