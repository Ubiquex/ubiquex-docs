---
title: "aws_aidevops_backlog_tasks"
description: "`aws_aidevops_backlog_tasks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aidevops_backlog_tasks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_backlog_tasks"})

    		ubx.Data(aidevops.BacklogTasks, "example", aidevops.BacklogTasksConfig{
    			AgentSpaceId: "example",
    			Filter:       map[string]any{"created_after": "example"},
    			Limit:        1,
    			Order:        "example",
    			SortField:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BacklogTasks } from "@ubx/sdk-aws/aws/data/aidevops/backlog_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_backlog_tasks" });

      data(BacklogTasks, "example", {
        agentSpaceId: "example",
        filter: { createdAfter: "example" },
        limit: 1,
        order: "example",
        sortField: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import BacklogTasks, BacklogTasksConfig

    def describe():
        ubx.intent("look up aws_aidevops_backlog_tasks")

        ubx.data(BacklogTasks, "example", BacklogTasksConfig(
            agent_space_id="example",
            filter={"created_after": "example"},
            limit=1,
            order="example",
            sort_field="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="object">
  &lt;p&gt;Filter criteria for listing backlog tasks, supporting time range, priority, status, and type filters.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="created_after" type="string">
      &lt;p&gt;Timestamp format used for backlog operations&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_before" type="string">
      &lt;p&gt;Timestamp format used for backlog operations&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="primary_task_id" type="string">
      &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="priority" type="list(string)">
      &lt;p&gt;List of backlog task priorities&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="list(string)">
      &lt;p&gt;List of backlog task statuses&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="task_type" type="list(string)">
      &lt;p&gt;List of backlog task types&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  &lt;p&gt;Sort order options&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="sort_field" type="string">
  &lt;p&gt;Fields available for sorting tasks&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for list operations (1-2048 characters)&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="tasks" type="list(object)">
  &lt;p&gt;List of backlog tasks&lt;/p&gt; Returned by the lookup.
</ResponseField>
