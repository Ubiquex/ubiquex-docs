---
title: "aws_aidevops_asset_content"
description: "`aws_aidevops_asset_content` reads the real content of an asset managed by an AI-driven DevOps agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_asset_content"})

    		ubx.Data(aidevops.AssetContent, "example", aidevops.AssetContentConfig{
    			AgentSpaceId: "example",
    			AssetId:      "example",
    			AssetVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetContent } from "@ubx/sdk-aws/aws/data/aidevops/asset_content";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_asset_content" });

      data(AssetContent, "example", {
        agentSpaceId: "example",
        assetId: "example",
        assetVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import AssetContent, AssetContentConfig

    def describe():
        ubx.intent("look up aws_aidevops_asset_content")

        ubx.data(AssetContent, "example", AssetContentConfig(
            agent_space_id="example",
            asset_id="example",
            asset_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="asset_id" type="string" required>
  &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="asset_version" type="number">
  The real version of the asset. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="content" type="object">
  &lt;p&gt;A zip file containing asset files&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="zip_file" type="string" required>
      &lt;p&gt;Zip file content as bytes&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="number">
  The real version identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
