---
title: "aws_aidevops_recommendation"
description: "`aws_aidevops_recommendation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aidevops_recommendation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_recommendation"})

    		ubx.Data(aidevops.Recommendation, "example", aidevops.RecommendationConfig{
    			AgentSpaceId:          "example",
    			RecommendationId:      "example",
    			RecommendationVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Recommendation } from "@ubx/sdk-aws/aws/data/aidevops/recommendation";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_recommendation" });

      data(Recommendation, "example", {
        agentSpaceId: "example",
        recommendationId: "example",
        recommendationVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import Recommendation, RecommendationConfig

    def describe():
        ubx.intent("look up aws_aidevops_recommendation")

        ubx.data(Recommendation, "example", RecommendationConfig(
            agent_space_id="example",
            recommendation_id="example",
            recommendation_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recommendation_id" type="string" required>
  &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="recommendation_version" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="recommendation" type="object">
  &lt;p&gt;Represents a recommendation with all its properties and metadata&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="additional_context" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="agent_space_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="content" type="object" required>
      &lt;p&gt;Content of a recommendation&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="spec" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="summary" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      &lt;p&gt;Timestamp format used for backlog operations&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="goal_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="goal_version" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="priority" type="string" required>
      &lt;p&gt;Priority level of a recommendation&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="rank_position" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ranked_at" type="string">
      &lt;p&gt;Timestamp format used for backlog operations&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="recommendation_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      &lt;p&gt;Status of a recommendation&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="task_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="title" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      &lt;p&gt;Timestamp format used for backlog operations&lt;/p&gt; Required lookup argument.
    </ResponseField>
    <ResponseField name="version" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
