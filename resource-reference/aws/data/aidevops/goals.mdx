---
title: "aws_aidevops_goals"
description: "`aws_aidevops_goals` reads the real goals configured for an AI-driven DevOps agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_goals"})

    		ubx.Data(aidevops.Goals, "example", aidevops.GoalsConfig{
    			AgentSpaceId: "example",
    			GoalType:     "example",
    			Limit:        1,
    			Status:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Goals } from "@ubx/sdk-aws/aws/data/aidevops/goals";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_goals" });

      data(Goals, "example", {
        agentSpaceId: "example",
        goalType: "example",
        limit: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import Goals, GoalsConfig

    def describe():
        ubx.intent("look up aws_aidevops_goals")

        ubx.data(Goals, "example", GoalsConfig(
            agent_space_id="example",
            goal_type="example",
            limit=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="goal_type" type="string">
  &lt;p&gt;Type of goal based on its origin&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;Possible states of a goal throughout its lifecycle&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="goals" type="list(object)">
  &lt;p&gt;List of goals&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for list operations (1-2048 characters)&lt;/p&gt; Returned by the lookup.
</ResponseField>
