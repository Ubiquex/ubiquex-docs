---
title: "aws_aidevops_pending_messages"
description: "`aws_aidevops_pending_messages` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aidevops_pending_messages` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_pending_messages"})

    		ubx.Data(aidevops.PendingMessages, "example", aidevops.PendingMessagesConfig{
    			AgentSpaceId: "example",
    			ExecutionId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PendingMessages } from "@ubx/sdk-aws/aws/data/aidevops/pending_messages";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_pending_messages" });

      data(PendingMessages, "example", {
        agentSpaceId: "example",
        executionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import PendingMessages, PendingMessagesConfig

    def describe():
        ubx.intent("look up aws_aidevops_pending_messages")

        ubx.data(PendingMessages, "example", PendingMessagesConfig(
            agent_space_id="example",
            execution_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  &lt;p&gt;Unique identifier for an agent space (allows alphanumeric characters and hyphens; 1-64 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="execution_id" type="string" required>
  &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="messages" type="list(object)">
  Returned by the lookup.
</ResponseField>
