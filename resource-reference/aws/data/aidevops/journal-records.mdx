---
title: "aws_aidevops_journal_records"
description: "`aws_aidevops_journal_records` reads the real, recorded activity journal of an AI-driven DevOps agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_journal_records"})

    		ubx.Data(aidevops.JournalRecords, "example", aidevops.JournalRecordsConfig{
    			AgentSpaceId: "example",
    			ExecutionId:  "example",
    			Limit:        1,
    			Order:        "example",
    			RecordType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { JournalRecords } from "./local-sdk/aws/sdk/typescript/aws/data/aidevops/journal_records.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_journal_records" });

      data(JournalRecords, "example", {
        agentSpaceId: "example",
        executionId: "example",
        limit: 1,
        order: "example",
        recordType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import JournalRecords, JournalRecordsConfig

    def describe():
        ubx.intent("look up aws_aidevops_journal_records")

        ubx.data(JournalRecords, "example", JournalRecordsConfig(
            agent_space_id="example",
            execution_id="example",
            limit=1,
            order="example",
            record_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="execution_id" type="string" required>
  &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  &lt;p&gt;Sort order options for journal operations&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="record_type" type="string">
  The real, discriminating type of the record this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for list operations (1-2048 characters)&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(object)">
  &lt;p&gt;List of journal records&lt;/p&gt; Returned by the lookup.
</ResponseField>
