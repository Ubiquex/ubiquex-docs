---
title: "aws_aidevops_asset_versions"
description: "`aws_aidevops_asset_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aidevops_asset_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_asset_versions"})

    		ubx.Data(aidevops.AssetVersions, "example", aidevops.AssetVersionsConfig{
    			AgentSpaceId: "example",
    			AssetId:      "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetVersions } from "@ubx/sdk-aws/aws/data/aidevops/asset_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_asset_versions" });

      data(AssetVersions, "example", {
        agentSpaceId: "example",
        assetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import AssetVersions, AssetVersionsConfig

    def describe():
        ubx.intent("look up aws_aidevops_asset_versions")

        ubx.data(AssetVersions, "example", AssetVersionsConfig(
            agent_space_id="example",
            asset_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="asset_id" type="string" required>
  &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for list operations (1-2048 characters)&lt;/p&gt; Returned by the lookup.
</ResponseField>
