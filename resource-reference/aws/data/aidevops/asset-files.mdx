---
title: "aws_aidevops_asset_files"
description: "`aws_aidevops_asset_files` reads the real files within an asset managed by an AI-driven DevOps agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_asset_files"})

    		ubx.Data(aidevops.AssetFiles, "example", aidevops.AssetFilesConfig{
    			AgentSpaceId: "example",
    			AssetId:      "example",
    			AssetVersion: 1,
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetFiles } from "@ubx/sdk-aws/aws/data/aidevops/asset_files";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_asset_files" });

      data(AssetFiles, "example", {
        agentSpaceId: "example",
        assetId: "example",
        assetVersion: 1,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import AssetFiles, AssetFilesConfig

    def describe():
        ubx.intent("look up aws_aidevops_asset_files")

        ubx.data(AssetFiles, "example", AssetFilesConfig(
            agent_space_id="example",
            asset_id="example",
            asset_version=1,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  The real, unique identifier of the agent space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="asset_id" type="string" required>
  &lt;p&gt;Generic resource identifier (allows alphanumeric characters, hyphens, and underscores; 1-128 characters)&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="asset_version" type="number">
  The real version of the asset. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for list operations (1-2048 characters)&lt;/p&gt; Returned by the lookup.
</ResponseField>
