---
title: "aws_aidevops_assets"
description: "`aws_aidevops_assets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_aidevops_assets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aidevops "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/aidevops"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_aidevops_assets"})

    		ubx.Data(aidevops.Assets, "example", aidevops.AssetsConfig{
    			AgentSpaceId:  "example",
    			AssetType:     "example",
    			MaxResults:    1,
    			UpdatedAfter:  "example",
    			UpdatedBefore: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Assets } from "@ubx/sdk-aws/aws/data/aidevops/assets";

    export default stack("example", () => {
      intent({ summary: "look up aws_aidevops_assets" });

      data(Assets, "example", {
        agentSpaceId: "example",
        assetType: "example",
        maxResults: 1,
        updatedAfter: "example",
        updatedBefore: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.aidevops import Assets, AssetsConfig

    def describe():
        ubx.intent("look up aws_aidevops_assets")

        ubx.data(Assets, "example", AssetsConfig(
            agent_space_id="example",
            asset_type="example",
            max_results=1,
            updated_after="example",
            updated_before="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_space_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="asset_type" type="string">
  &lt;p&gt;The type of asset (e.g. skill, artifact)&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="updated_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="updated_before" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for list operations (1-2048 characters)&lt;/p&gt; Returned by the lookup.
</ResponseField>
