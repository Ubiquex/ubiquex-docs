---
title: "aws_quicksight_space_permissions"
description: "`aws_quicksight_space_permissions` reads the real, resource-level permissions granted on a QuickSight space."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_space_permissions"})

    		ubx.Data(quicksight.SpacePermissions, "example", quicksight.SpacePermissionsConfig{
    			AwsAccountId: "example",
    			SpaceId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SpacePermissions } from "@ubx/sdk-aws/aws/data/quicksight/space_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_space_permissions" });

      data(SpacePermissions, "example", {
        awsAccountId: "example",
        spaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import SpacePermissions, SpacePermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_space_permissions")

        ubx.data(SpacePermissions, "example", SpacePermissionsConfig(
            aws_account_id="example",
            space_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="space_id" type="string" required>
  The real, unique identifier of the space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="space_arn" type="string">
  The real Amazon Resource Name (ARN) of the space this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
