---
title: "aws_quicksight_quick_sight_qsearch_configuration"
description: "`aws_quicksight_quick_sight_qsearch_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_quicksight_quick_sight_qsearch_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_quick_sight_qsearch_configuration"})

    		ubx.Data(quicksight.QuickSightQsearchConfiguration, "example", quicksight.QuickSightQsearchConfigurationConfig{
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QuickSightQsearchConfiguration } from "@ubx/sdk-aws/aws/data/quicksight/quick_sight_qsearch_configuration";

    export default stack("example", () => {
      intent({
        summary: "look up aws_quicksight_quick_sight_qsearch_configuration",
      });

      data(QuickSightQsearchConfiguration, "example", {
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import QuickSightQsearchConfiguration, QuickSightQsearchConfigurationConfig

    def describe():
        ubx.intent("look up aws_quicksight_quick_sight_qsearch_configuration")

        ubx.data(QuickSightQsearchConfiguration, "example", QuickSightQsearchConfigurationConfig(
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="qsearch_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>
