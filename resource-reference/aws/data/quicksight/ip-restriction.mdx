---
title: "aws_quicksight_ip_restriction"
description: "`aws_quicksight_ip_restriction` reads the real IP address allow list restricting access to a QuickSight account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_ip_restriction"})

    		ubx.Data(quicksight.IpRestriction, "example", quicksight.IpRestrictionConfig{
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IpRestriction } from "@ubx/sdk-aws/aws/data/quicksight/ip_restriction";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_ip_restriction" });

      data(IpRestriction, "example", {
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import IpRestriction, IpRestrictionConfig

    def describe():
        ubx.intent("look up aws_quicksight_ip_restriction")

        ubx.data(IpRestriction, "example", IpRestrictionConfig(
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enabled" type="bool">
  Whether this feature is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ip_restriction_rule_map" type="map(string)">
  The real, structured mapping of IP restriction rules for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_endpoint_id_restriction_rule_map" type="map(string)">
  The real, structured mapping of VPC endpoint ID restriction rules for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vpc_id_restriction_rule_map" type="map(string)">
  The real, structured mapping of VPC ID restriction rules for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
