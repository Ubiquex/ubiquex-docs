---
title: "aws_quicksight_iampolicy_assignments_for_user"
description: "`aws_quicksight_iampolicy_assignments_for_user` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_quicksight_iampolicy_assignments_for_user` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_iampolicy_assignments_for_user"})

    		ubx.Data(quicksight.IampolicyAssignmentsForUser, "example", quicksight.IampolicyAssignmentsForUserConfig{
    			AwsAccountId: "example",
    			Namespace:    "example",
    			UserName:     "example-user-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IampolicyAssignmentsForUser } from "@ubx/sdk-aws/aws/data/quicksight/iampolicy_assignments_for_user";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_iampolicy_assignments_for_user" });

      data(IampolicyAssignmentsForUser, "example", {
        awsAccountId: "example",
        namespace: "example",
        userName: "example-user-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import IampolicyAssignmentsForUser, IampolicyAssignmentsForUserConfig

    def describe():
        ubx.intent("look up aws_quicksight_iampolicy_assignments_for_user")

        ubx.data(IampolicyAssignmentsForUser, "example", IampolicyAssignmentsForUserConfig(
            aws_account_id="example",
            namespace="example",
            user_name="example-user-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="active_assignments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>
