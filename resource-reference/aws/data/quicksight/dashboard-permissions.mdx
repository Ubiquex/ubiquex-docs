---
title: "aws_quicksight_dashboard_permissions"
description: "`aws_quicksight_dashboard_permissions` reads the real, resource-level permissions granted on a QuickSight dashboard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_dashboard_permissions"})

    		ubx.Data(quicksight.DashboardPermissions, "example", quicksight.DashboardPermissionsConfig{
    			AwsAccountId: "example",
    			DashboardId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardPermissions } from "@ubx/sdk-aws/aws/data/quicksight/dashboard_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_dashboard_permissions" });

      data(DashboardPermissions, "example", {
        awsAccountId: "example",
        dashboardId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import DashboardPermissions, DashboardPermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_dashboard_permissions")

        ubx.data(DashboardPermissions, "example", DashboardPermissionsConfig(
            aws_account_id="example",
            dashboard_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dashboard_id" type="string" required>
  The real, unique identifier of the dashboard this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dashboard_arn" type="string">
  The real Amazon Resource Name (ARN) of the dashboard this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="link_sharing_configuration" type="object">
  &lt;p&gt;A structure that contains the configuration of a shareable link to the dashboard.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="permissions" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
