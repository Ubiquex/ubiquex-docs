---
title: "aws_quicksight_agent_permissions"
description: "`aws_quicksight_agent_permissions` reads the real, resource-level permissions granted on a QuickSight agent."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_agent_permissions"})

    		ubx.Data(quicksight.AgentPermissions, "example", quicksight.AgentPermissionsConfig{
    			AgentId:      "example",
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgentPermissions } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/agent_permissions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_agent_permissions" });

      data(AgentPermissions, "example", {
        agentId: "example",
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import AgentPermissions, AgentPermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_agent_permissions")

        ubx.data(AgentPermissions, "example", AgentPermissionsConfig(
            agent_id="example",
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_id" type="string" required>
  The real, unique identifier of the agent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
