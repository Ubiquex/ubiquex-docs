---
title: "aws_quicksight_space_resources"
description: "`aws_quicksight_space_resources` reads the real assets filed within a QuickSight space."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_space_resources"})

    		ubx.Data(quicksight.SpaceResources, "example", quicksight.SpaceResourcesConfig{
    			AwsAccountId: "example",
    			SpaceId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SpaceResources } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/space_resources.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_space_resources" });

      data(SpaceResources, "example", {
        awsAccountId: "example",
        spaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import SpaceResources, SpaceResourcesConfig

    def describe():
        ubx.intent("look up aws_quicksight_space_resources")

        ubx.data(SpaceResources, "example", SpaceResourcesConfig(
            aws_account_id="example",
            space_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="space_id" type="string" required>
  The real, unique identifier of the space this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="space_arn" type="string">
  The real Amazon Resource Name (ARN) of the space this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="space_resources" type="list(object)">
  The real resources allocated to this SageMaker space. **(AI-inferred)** Returned by the lookup.
</ResponseField>
