---
title: "aws_quicksight_dashboard_embed_url"
description: "`aws_quicksight_dashboard_embed_url` reads a real, generated, time-limited URL for embedding a QuickSight dashboard in an external application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_dashboard_embed_url"})

    		ubx.Data(quicksight.DashboardEmbedUrl, "example", quicksight.DashboardEmbedUrlConfig{
    			AwsAccountId:             "example",
    			DashboardId:              "example",
    			IdentityType:             "example",
    			AdditionalDashboardIds:   []string{"example"},
    			Namespace:                "example",
    			ResetDisabled:            true,
    			SessionLifetimeInMinutes: 1,
    			StatePersistenceEnabled:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DashboardEmbedUrl } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/dashboard_embed_url.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_dashboard_embed_url" });

      data(DashboardEmbedUrl, "example", {
        awsAccountId: "example",
        dashboardId: "example",
        identityType: "example",
        additionalDashboardIds: ["example"],
        namespace: "example",
        resetDisabled: true,
        sessionLifetimeInMinutes: 1,
        statePersistenceEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import DashboardEmbedUrl, DashboardEmbedUrlConfig

    def describe():
        ubx.intent("look up aws_quicksight_dashboard_embed_url")

        ubx.data(DashboardEmbedUrl, "example", DashboardEmbedUrlConfig(
            aws_account_id="example",
            dashboard_id="example",
            identity_type="example",
            additional_dashboard_ids=["example"],
            namespace="example",
            reset_disabled=True,
            session_lifetime_in_minutes=1,
            state_persistence_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="additional_dashboard_ids" type="list(string)">
  The real, unique identifiers of the additional dashboards this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dashboard_id" type="string" required>
  The real, unique identifier of the dashboard this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identity_type" type="string" required>
  The real type of this identity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reset_disabled" type="bool">
  Whether resetting this object is disabled. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_lifetime_in_minutes" type="number">
  The real, maximum lifetime of this session, in minutes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state_persistence_enabled" type="bool">
  Whether the state persistence is currently enabled. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="undo_redo_disabled" type="bool">
  Whether undo/redo is disabled for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="embed_url" type="string">
  The real, resolvable URL of the embed this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
