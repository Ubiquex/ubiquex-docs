---
title: "aws_quicksight_topic_refresh"
description: "`aws_quicksight_topic_refresh` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_quicksight_topic_refresh` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_topic_refresh"})

    		ubx.Data(quicksight.TopicRefresh, "example", quicksight.TopicRefreshConfig{
    			AwsAccountId: "example",
    			RefreshId:    "example",
    			TopicId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopicRefresh } from "@ubx/sdk-aws/aws/data/quicksight/topic_refresh";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_topic_refresh" });

      data(TopicRefresh, "example", {
        awsAccountId: "example",
        refreshId: "example",
        topicId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import TopicRefresh, TopicRefreshConfig

    def describe():
        ubx.intent("look up aws_quicksight_topic_refresh")

        ubx.data(TopicRefresh, "example", TopicRefreshConfig(
            aws_account_id="example",
            refresh_id="example",
            topic_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="refresh_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="topic_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="refresh_details" type="object">
  &lt;p&gt;The details about the refresh of a topic.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="refresh_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="refresh_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="refresh_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="request_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>
