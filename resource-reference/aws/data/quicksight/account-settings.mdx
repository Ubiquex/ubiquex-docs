---
title: "aws_quicksight_account_settings"
description: "`aws_quicksight_account_settings` reads the real, account-wide QuickSight settings, such as its own edition and default namespace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_account_settings"})

    		ubx.Data(quicksight.AccountSettings, "example", quicksight.AccountSettingsConfig{
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountSettings } from "@ubx/sdk-aws/aws/data/quicksight/account_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_account_settings" });

      data(AccountSettings, "example", {
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import AccountSettings, AccountSettingsConfig

    def describe():
        ubx.intent("look up aws_quicksight_account_settings")

        ubx.data(AccountSettings, "example", AccountSettingsConfig(
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_settings" type="object">
  &lt;p&gt;The Quick Sight settings associated with your Amazon Web Services account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="default_namespace" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="edition" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="notification_email" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="public_sharing_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="termination_protection_enabled" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
