---
title: "aws_quicksight_users_index_capacity"
description: "`aws_quicksight_users_index_capacity` reads the real, current capacity usage of a QuickSight account's own user index."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_users_index_capacity"})

    		ubx.Data(quicksight.UsersIndexCapacity, "example", quicksight.UsersIndexCapacityConfig{
    			AwsAccountId: "example",
    			Filters:      []map[string]any{{"total_capacity_bytes": map[string]any{"max_bytes": 1}}},
    			MaxResults:   1,
    			Namespace:    "example",
    			SortBy:       "example",
    			SortOrder:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UsersIndexCapacity } from "@ubx/sdk-aws/aws/data/quicksight/users_index_capacity";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_users_index_capacity" });

      data(UsersIndexCapacity, "example", {
        awsAccountId: "example",
        filters: [{ totalCapacityBytes: { maxBytes: 1 } }],
        maxResults: 1,
        namespace: "example",
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import UsersIndexCapacity, UsersIndexCapacityConfig
    from ubx.aws.data.quicksight.users_index_capacity import UsersIndexCapacity_Filters, UsersIndexCapacity_Filters_TotalCapacityBytes

    def describe():
        ubx.intent("look up aws_quicksight_users_index_capacity")

        ubx.data(UsersIndexCapacity, "example", UsersIndexCapacityConfig(
            aws_account_id="example",
            filters=[UsersIndexCapacity_Filters(total_capacity_bytes=UsersIndexCapacity_Filters_TotalCapacityBytes(max_bytes=1))],
            max_results=1,
            namespace="example",
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  &lt;p&gt;The field to sort user index capacity results by.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  &lt;p&gt;The sort order for user index capacity results.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
