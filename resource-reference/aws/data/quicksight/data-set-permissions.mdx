---
title: "aws_quicksight_data_set_permissions"
description: "`aws_quicksight_data_set_permissions` reads the real, resource-level permissions granted on a QuickSight dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_data_set_permissions"})

    		ubx.Data(quicksight.DataSetPermissions, "example", quicksight.DataSetPermissionsConfig{
    			AwsAccountId: "example",
    			DataSetId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataSetPermissions } from "@ubx/sdk-aws/aws/data/quicksight/data_set_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_data_set_permissions" });

      data(DataSetPermissions, "example", {
        awsAccountId: "example",
        dataSetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import DataSetPermissions, DataSetPermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_data_set_permissions")

        ubx.data(DataSetPermissions, "example", DataSetPermissionsConfig(
            aws_account_id="example",
            data_set_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="data_set_id" type="string" required>
  The real, unique identifier of the data set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_set_arn" type="string">
  The real Amazon Resource Name (ARN) of the data set this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
