---
title: "aws_quicksight_brand_assignment"
description: "`aws_quicksight_brand_assignment` reads which real QuickSight brand is currently assigned to an account or namespace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_brand_assignment"})

    		ubx.Data(quicksight.BrandAssignment, "example", quicksight.BrandAssignmentConfig{
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BrandAssignment } from "@ubx/sdk-aws/aws/data/quicksight/brand_assignment";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_brand_assignment" });

      data(BrandAssignment, "example", {
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import BrandAssignment, BrandAssignmentConfig

    def describe():
        ubx.intent("look up aws_quicksight_brand_assignment")

        ubx.data(BrandAssignment, "example", BrandAssignmentConfig(
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="brand_arn" type="string">
  The real Amazon Resource Name (ARN) of the brand this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
