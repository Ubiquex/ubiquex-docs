---
title: "aws_quicksight_self_upgrade_configuration"
description: "`aws_quicksight_self_upgrade_configuration` reads the real configuration controlling whether QuickSight Reader users can self-upgrade to Author."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_self_upgrade_configuration"})

    		ubx.Data(quicksight.SelfUpgradeConfiguration, "example", quicksight.SelfUpgradeConfigurationConfig{
    			AwsAccountId: "example",
    			Namespace:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SelfUpgradeConfiguration } from "@ubx/sdk-aws/aws/data/quicksight/self_upgrade_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_self_upgrade_configuration" });

      data(SelfUpgradeConfiguration, "example", {
        awsAccountId: "example",
        namespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import SelfUpgradeConfiguration, SelfUpgradeConfigurationConfig

    def describe():
        ubx.intent("look up aws_quicksight_self_upgrade_configuration")

        ubx.data(SelfUpgradeConfiguration, "example", SelfUpgradeConfigurationConfig(
            aws_account_id="example",
            namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  The real namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="self_upgrade_configuration" type="object">
  &lt;p&gt;The self-upgrade configuration for the Quick account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="self_upgrade_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
