---
title: "aws_quicksight_dashboard_snapshot_job"
description: "`aws_quicksight_dashboard_snapshot_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_quicksight_dashboard_snapshot_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_dashboard_snapshot_job"})

    		ubx.Data(quicksight.DashboardSnapshotJob, "example", quicksight.DashboardSnapshotJobConfig{
    			AwsAccountId:  "example",
    			DashboardId:   "example",
    			SnapshotJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardSnapshotJob } from "@ubx/sdk-aws/aws/data/quicksight/dashboard_snapshot_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_dashboard_snapshot_job" });

      data(DashboardSnapshotJob, "example", {
        awsAccountId: "example",
        dashboardId: "example",
        snapshotJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import DashboardSnapshotJob, DashboardSnapshotJobConfig

    def describe():
        ubx.intent("look up aws_quicksight_dashboard_snapshot_job")

        ubx.data(DashboardSnapshotJob, "example", DashboardSnapshotJobConfig(
            aws_account_id="example",
            dashboard_id="example",
            snapshot_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="dashboard_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="snapshot_job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot_configuration" type="object">
  &lt;p&gt;Describes the configuration of the dashboard snapshot.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="destination_configuration" type="object">
      &lt;p&gt;A structure that contains information on the Amazon S3 destinations of the generated snapshot.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="s3_destinations" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="file_groups" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="parameters" type="object">
      &lt;p&gt;A list of Quick Sight parameters and the list's override values.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="date_time_parameters" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="decimal_parameters" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="integer_parameters" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="string_parameters" type="list(object)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_configuration" type="object">
  &lt;p&gt;A structure that contains information about the users that the dashboard snapshot is generated for. Sensitive user information is excluded. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="anonymous_users" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
