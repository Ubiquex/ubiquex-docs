---
title: "aws_quicksight_session_embed_url"
description: "`aws_quicksight_session_embed_url` reads a real, generated, time-limited URL for embedding the full QuickSight console session in an external application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_session_embed_url"})

    		ubx.Data(quicksight.SessionEmbedUrl, "example", quicksight.SessionEmbedUrlConfig{
    			AwsAccountId:             "example",
    			EntryPoint:               "example",
    			SessionLifetimeInMinutes: 1,
    			UserArn:                  "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SessionEmbedUrl } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/session_embed_url.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_session_embed_url" });

      data(SessionEmbedUrl, "example", {
        awsAccountId: "example",
        entryPoint: "example",
        sessionLifetimeInMinutes: 1,
        userArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import SessionEmbedUrl, SessionEmbedUrlConfig

    def describe():
        ubx.intent("look up aws_quicksight_session_embed_url")

        ubx.data(SessionEmbedUrl, "example", SessionEmbedUrlConfig(
            aws_account_id="example",
            entry_point="example",
            session_lifetime_in_minutes=1,
            user_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entry_point" type="string">
  The real entry point. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_lifetime_in_minutes" type="number">
  The real, maximum lifetime of this session, in minutes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="embed_url" type="string">
  The real, resolvable URL of the embed this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
