---
title: "aws_quicksight_identity_context"
description: "`aws_quicksight_identity_context` reads real, contextual identity information for the caller's own QuickSight session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_identity_context"})

    		ubx.Data(quicksight.IdentityContext, "example", quicksight.IdentityContextConfig{
    			AwsAccountId:     "example",
    			UserIdentifier:   map[string]any{"email": "example"},
    			ContextRegion:    "example",
    			Namespace:        "example",
    			SessionExpiresAt: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IdentityContext } from "@ubx/sdk-aws/aws/data/quicksight/identity_context";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_identity_context" });

      data(IdentityContext, "example", {
        awsAccountId: "example",
        userIdentifier: { email: "example" },
        contextRegion: "example",
        namespace: "example",
        sessionExpiresAt: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import IdentityContext, IdentityContextConfig

    def describe():
        ubx.intent("look up aws_quicksight_identity_context")

        ubx.data(IdentityContext, "example", IdentityContextConfig(
            aws_account_id="example",
            user_identifier={"email": "example"},
            context_region="example",
            namespace="example",
            session_expires_at="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="context_region" type="string">
  The real, surrounding context region for this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_expires_at" type="string">
  The real timestamp this session expires. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_identifier" type="object" required>
  &lt;p&gt;A structure that contains information to identify a user.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="email" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="user_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="context" type="string">
  The real, structured context of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
