---
title: "aws_quicksight_folder_resolved_permissions"
description: "`aws_quicksight_folder_resolved_permissions` reads the real, effective permissions on a QuickSight folder after resolving any inherited grants."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_folder_resolved_permissions"})

    		ubx.Data(quicksight.FolderResolvedPermissions, "example", quicksight.FolderResolvedPermissionsConfig{
    			AwsAccountId: "example",
    			FolderId:     "example",
    			MaxResults:   1,
    			Namespace:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FolderResolvedPermissions } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/folder_resolved_permissions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_folder_resolved_permissions" });

      data(FolderResolvedPermissions, "example", {
        awsAccountId: "example",
        folderId: "example",
        maxResults: 1,
        namespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import FolderResolvedPermissions, FolderResolvedPermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_folder_resolved_permissions")

        ubx.data(FolderResolvedPermissions, "example", FolderResolvedPermissionsConfig(
            aws_account_id="example",
            folder_id="example",
            max_results=1,
            namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="folder_id" type="string" required>
  The real, unique identifier of the folder this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
