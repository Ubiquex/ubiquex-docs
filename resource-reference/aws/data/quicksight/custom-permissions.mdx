---
title: "aws_quicksight_custom_permissions"
description: "`data_aws_quicksight_custom_permissions` looks up a real, already-existing `aws_quick_sight_custom_permissions`."
---

It defines a real, named set of restricted capabilities, disabling the ability to share dashboards, for example, that can be assigned to specific QuickSight/Amazon Quick users or groups, layered on top of standard Author or Reader permissions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_custom_permissions"})

    		ubx.Data(quicksight.CustomPermissions, "example", quicksight.CustomPermissionsConfig{
    			AwsAccountId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomPermissions } from "@ubx/sdk-aws/aws/data/quicksight/custom_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_custom_permissions" });

      data(CustomPermissions, "example", {
        awsAccountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import CustomPermissions, CustomPermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_custom_permissions")

        ubx.data(CustomPermissions, "example", CustomPermissionsConfig(
            aws_account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_permissions_list" type="list(object)">
  The real custom permissionss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
