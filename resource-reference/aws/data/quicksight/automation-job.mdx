---
title: "aws_quicksight_automation_job"
description: "`aws_quicksight_automation_job` reads the real, current status of a QuickSight automation job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_automation_job"})

    		ubx.Data(quicksight.AutomationJob, "example", quicksight.AutomationJobConfig{
    			AutomationGroupId:    "example",
    			AutomationId:         "example",
    			AwsAccountId:         "example",
    			JobId:                "example",
    			IncludeInputPayload:  true,
    			IncludeOutputPayload: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutomationJob } from "@ubx/sdk-aws/aws/data/quicksight/automation_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_automation_job" });

      data(AutomationJob, "example", {
        automationGroupId: "example",
        automationId: "example",
        awsAccountId: "example",
        jobId: "example",
        includeInputPayload: true,
        includeOutputPayload: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import AutomationJob, AutomationJobConfig

    def describe():
        ubx.intent("look up aws_quicksight_automation_job")

        ubx.data(AutomationJob, "example", AutomationJobConfig(
            automation_group_id="example",
            automation_id="example",
            aws_account_id="example",
            job_id="example",
            include_input_payload=True,
            include_output_payload=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="automation_group_id" type="string" required>
  The real, unique identifier of the automation group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="automation_id" type="string" required>
  The real, unique identifier of the automation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include_input_payload" type="bool">
  Whether the input payload is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_output_payload" type="bool">
  Whether the output payload is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ended_at" type="string">
  The real timestamp this object ended. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="input_payload" type="string">
  The real, raw input payload of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="output_payload" type="string">
  The real, raw output payload of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="started_at" type="string">
  The real timestamp this object started. **(AI-inferred)** Returned by the lookup.
</ResponseField>
