---
title: "aws_quicksight_analysis_permissions"
description: "`aws_quicksight_analysis_permissions` reads the real, resource-level permissions granted on a QuickSight analysis."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_analysis_permissions"})

    		ubx.Data(quicksight.AnalysisPermissions, "example", quicksight.AnalysisPermissionsConfig{
    			AnalysisId:   "example",
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AnalysisPermissions } from "@ubx/sdk-aws/aws/data/quicksight/analysis_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_analysis_permissions" });

      data(AnalysisPermissions, "example", {
        analysisId: "example",
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import AnalysisPermissions, AnalysisPermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_analysis_permissions")

        ubx.data(AnalysisPermissions, "example", AnalysisPermissionsConfig(
            analysis_id="example",
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analysis_id" type="string" required>
  The real, unique identifier of the analysis this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analysis_arn" type="string">
  The real Amazon Resource Name (ARN) of the analysis this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
