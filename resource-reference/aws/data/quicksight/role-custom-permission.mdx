---
title: "aws_quicksight_role_custom_permission"
description: "`data_aws_quicksight_role_custom_permission` looks up a real, already-existing `aws_quick_sight_custom_permissions`."
---

It defines a real, named set of restricted capabilities, disabling the ability to share dashboards, for example, that can be assigned to specific QuickSight/Amazon Quick users or groups, layered on top of standard Author or Reader permissions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_role_custom_permission"})

    		ubx.Data(quicksight.RoleCustomPermission, "example", quicksight.RoleCustomPermissionConfig{
    			AwsAccountId: "example",
    			Namespace:    "example",
    			Role:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RoleCustomPermission } from "@ubx/sdk-aws/aws/data/quicksight/role_custom_permission";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_role_custom_permission" });

      data(RoleCustomPermission, "example", {
        awsAccountId: "example",
        namespace: "example",
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import RoleCustomPermission, RoleCustomPermissionConfig

    def describe():
        ubx.intent("look up aws_quicksight_role_custom_permission")

        ubx.data(RoleCustomPermission, "example", RoleCustomPermissionConfig(
            aws_account_id="example",
            namespace="example",
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  The real namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="role" type="string" required>
  The real IAM role this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_permissions_name" type="string">
  The real name of the custom permissions this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
