---
title: "aws_quicksight_key_registration"
description: "`aws_quicksight_key_registration` reads the real, customer-managed KMS keys registered for QuickSight's own encryption."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_key_registration"})

    		ubx.Data(quicksight.KeyRegistration, "example", quicksight.KeyRegistrationConfig{
    			AwsAccountId:   "example",
    			DefaultKeyOnly: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KeyRegistration } from "@ubx/sdk-aws/aws/data/quicksight/key_registration";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_key_registration" });

      data(KeyRegistration, "example", {
        awsAccountId: "example",
        defaultKeyOnly: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import KeyRegistration, KeyRegistrationConfig

    def describe():
        ubx.intent("look up aws_quicksight_key_registration")

        ubx.data(KeyRegistration, "example", KeyRegistrationConfig(
            aws_account_id="example",
            default_key_only=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="default_key_only" type="bool">
  Whether only the default encryption key is returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="key_registration" type="list(object)">
  The real, current key material registration status for this AWS KMS key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="qdata_key" type="object">
  &lt;p&gt;A structure that contains information about the &lt;code&gt;QDataKey&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="qdata_key_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="qdata_key_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
