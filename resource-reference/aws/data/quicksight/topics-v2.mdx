---
title: "aws_quicksight_topics_v2"
description: "`data_aws_quicksight_topics_v2` looks up a real, already-existing `aws_quick_sight_topic_v2`."
---

It defines a newer-generation version of an `aws_quick_sight_topic`, Amazon Quick's current natural-language topic configuration, superseding the original topic resource's schema for new topics.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_topics_v2"})

    		ubx.Data(quicksight.TopicsV2, "example", quicksight.TopicsV2Config{
    			AwsAccountId: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TopicsV2 } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/topics_v2.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_topics_v2" });

      data(TopicsV2, "example", {
        awsAccountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import TopicsV2, TopicsV2Config

    def describe():
        ubx.intent("look up aws_quicksight_topics_v2")

        ubx.data(TopicsV2, "example", TopicsV2Config(
            aws_account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="topic_summary_list" type="list(object)">
  The real topic summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
