---
title: "aws_quicksight_dashboards_qaconfiguration"
description: "`aws_quicksight_dashboards_qaconfiguration` reads the real Q&A (natural-language query) configuration enabled on a QuickSight dashboard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_dashboards_qaconfiguration"})

    		ubx.Data(quicksight.DashboardsQaconfiguration, "example", quicksight.DashboardsQaconfigurationConfig{
    			AwsAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardsQaconfiguration } from "@ubx/sdk-aws/aws/data/quicksight/dashboards_qaconfiguration";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_dashboards_qaconfiguration" });

      data(DashboardsQaconfiguration, "example", {
        awsAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import DashboardsQaconfiguration, DashboardsQaconfigurationConfig

    def describe():
        ubx.intent("look up aws_quicksight_dashboards_qaconfiguration")

        ubx.data(DashboardsQaconfiguration, "example", DashboardsQaconfigurationConfig(
            aws_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dashboards_qastatus" type="string">
  The real, current QuickSight Q status of the dashboards included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
