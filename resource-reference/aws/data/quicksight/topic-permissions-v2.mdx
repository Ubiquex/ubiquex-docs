---
title: "aws_quicksight_topic_permissions_v2"
description: "`aws_quicksight_topic_permissions_v2` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_quicksight_topic_permissions_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_topic_permissions_v2"})

    		ubx.Data(quicksight.TopicPermissionsV2, "example", quicksight.TopicPermissionsV2Config{
    			AwsAccountId: "example",
    			TopicId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopicPermissionsV2 } from "@ubx/sdk-aws/aws/data/quicksight/topic_permissions_v2";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_topic_permissions_v2" });

      data(TopicPermissionsV2, "example", {
        awsAccountId: "example",
        topicId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import TopicPermissionsV2, TopicPermissionsV2Config

    def describe():
        ubx.intent("look up aws_quicksight_topic_permissions_v2")

        ubx.data(TopicPermissionsV2, "example", TopicPermissionsV2Config(
            aws_account_id="example",
            topic_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="topic_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="permissions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="topic_arn" type="string">
  Returned by the lookup.
</ResponseField>
