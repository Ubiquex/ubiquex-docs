---
title: "aws_quicksight_topic_reviewed_answers"
description: "`aws_quicksight_topic_reviewed_answers` reads a real, verified answer an author has reviewed and approved for a QuickSight Q topic."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_topic_reviewed_answers"})

    		ubx.Data(quicksight.TopicReviewedAnswers, "example", quicksight.TopicReviewedAnswersConfig{
    			AwsAccountId: "example",
    			TopicId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopicReviewedAnswers } from "@ubx/sdk-aws/aws/data/quicksight/topic_reviewed_answers";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_topic_reviewed_answers" });

      data(TopicReviewedAnswers, "example", {
        awsAccountId: "example",
        topicId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import TopicReviewedAnswers, TopicReviewedAnswersConfig

    def describe():
        ubx.intent("look up aws_quicksight_topic_reviewed_answers")

        ubx.data(TopicReviewedAnswers, "example", TopicReviewedAnswersConfig(
            aws_account_id="example",
            topic_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="topic_id" type="string" required>
  The real, unique identifier of the topic this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="answers" type="list(object)">
  The real answers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="topic_arn" type="string">
  The real Amazon Resource Name (ARN) of the topic this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
