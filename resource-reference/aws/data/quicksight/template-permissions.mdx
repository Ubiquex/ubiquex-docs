---
title: "aws_quicksight_template_permissions"
description: "`aws_quicksight_template_permissions` reads the real, resource-level permissions granted on a QuickSight template."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_template_permissions"})

    		ubx.Data(quicksight.TemplatePermissions, "example", quicksight.TemplatePermissionsConfig{
    			AwsAccountId: "example",
    			TemplateId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TemplatePermissions } from "./local-sdk/aws/sdk/typescript/aws/data/quicksight/template_permissions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_template_permissions" });

      data(TemplatePermissions, "example", {
        awsAccountId: "example",
        templateId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import TemplatePermissions, TemplatePermissionsConfig

    def describe():
        ubx.intent("look up aws_quicksight_template_permissions")

        ubx.data(TemplatePermissions, "example", TemplatePermissionsConfig(
            aws_account_id="example",
            template_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="template_id" type="string" required>
  The real, unique identifier of the template this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="permissions" type="list(object)">
  The real permissions granted on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="template_arn" type="string">
  The real Amazon Resource Name (ARN) of the template this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
