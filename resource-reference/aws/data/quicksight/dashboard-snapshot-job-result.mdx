---
title: "aws_quicksight_dashboard_snapshot_job_result"
description: "`aws_quicksight_dashboard_snapshot_job_result` reads the real, generated result (a download URL) of a completed QuickSight dashboard snapshot job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_quicksight_dashboard_snapshot_job_result"})

    		ubx.Data(quicksight.DashboardSnapshotJobResult, "example", quicksight.DashboardSnapshotJobResultConfig{
    			AwsAccountId:  "example",
    			DashboardId:   "example",
    			SnapshotJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardSnapshotJobResult } from "@ubx/sdk-aws/aws/data/quicksight/dashboard_snapshot_job_result";

    export default stack("example", () => {
      intent({ summary: "look up aws_quicksight_dashboard_snapshot_job_result" });

      data(DashboardSnapshotJobResult, "example", {
        awsAccountId: "example",
        dashboardId: "example",
        snapshotJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.quicksight import DashboardSnapshotJobResult, DashboardSnapshotJobResultConfig

    def describe():
        ubx.intent("look up aws_quicksight_dashboard_snapshot_job_result")

        ubx.data(DashboardSnapshotJobResult, "example", DashboardSnapshotJobResultConfig(
            aws_account_id="example",
            dashboard_id="example",
            snapshot_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dashboard_id" type="string" required>
  The real, unique identifier of the dashboard this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="snapshot_job_id" type="string" required>
  The real, unique identifier of the snapshot job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error_info" type="object">
  &lt;p&gt;An object that contains information on the error that caused the snapshot job to fail.&lt;/p&gt; &lt;p&gt;For more information, see &lt;a href="https://docs.aws.amazon.com/quicksight/latest/APIReference/API_DescribeDashboardSnapshotJobResult.html"&gt;DescribeDashboardSnapshotJobResult API&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="error_message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="error_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_status" type="string">
  The real, current status of the job. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_id" type="string">
  The real, unique identifier AWS assigned to this API request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="result" type="object">
  &lt;p&gt;An object that provides information on the result of a snapshot job. This object provides information about the job, the job status, and the location of the generated file.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="anonymous_users" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="registered_users" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
