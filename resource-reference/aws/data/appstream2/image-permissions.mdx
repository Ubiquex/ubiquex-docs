---
title: "aws_appstream2_image_permissions"
description: "`aws_appstream2_image_permissions` reads which real AWS accounts an AppStream 2.0 image has been shared with."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_image_permissions"})

    		ubx.Data(appstream2.ImagePermissions, "example", appstream2.ImagePermissionsConfig{
    			Name:                "example",
    			MaxResults:          1,
    			SharedAwsAccountIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImagePermissions } from "@ubx/sdk-aws/aws/data/appstream2/image_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_image_permissions" });

      data(ImagePermissions, "example", {
        name: "example",
        maxResults: 1,
        sharedAwsAccountIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import ImagePermissions, ImagePermissionsConfig

    def describe():
        ubx.intent("look up aws_appstream2_image_permissions")

        ubx.data(ImagePermissions, "example", ImagePermissionsConfig(
            name="example",
            max_results=1,
            shared_aws_account_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="shared_aws_account_ids" type="list(string)">
  The real, unique identifiers of the shared aws accounts this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="shared_image_permissions_list" type="list(object)">
  The real shared image permissionss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
