---
title: "aws_appstream2_user_stack_associations"
description: "`aws_appstream2_user_stack_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appstream2_user_stack_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_user_stack_associations"})

    		ubx.Data(appstream2.UserStackAssociations, "example", appstream2.UserStackAssociationsConfig{
    			AuthenticationType: "example",
    			MaxResults:         1,
    			StackName:          "example-stack-name",
    			UserName:           "example-user-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserStackAssociations } from "@ubx/sdk-aws/aws/data/appstream2/user_stack_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_user_stack_associations" });

      data(UserStackAssociations, "example", {
        authenticationType: "example",
        maxResults: 1,
        stackName: "example-stack-name",
        userName: "example-user-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import UserStackAssociations, UserStackAssociationsConfig

    def describe():
        ubx.intent("look up aws_appstream2_user_stack_associations")

        ubx.data(UserStackAssociations, "example", UserStackAssociationsConfig(
            authentication_type="example",
            max_results=1,
            stack_name="example-stack-name",
            user_name="example-user-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stack_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_stack_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
