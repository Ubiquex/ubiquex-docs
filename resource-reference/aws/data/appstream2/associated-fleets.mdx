---
title: "aws_appstream2_associated_fleets"
description: "`aws_appstream2_associated_fleets` reads the real AppStream 2.0 fleets associated with a stack."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_associated_fleets"})

    		ubx.Data(appstream2.AssociatedFleets, "example", appstream2.AssociatedFleetsConfig{
    			StackName: "example-stack-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedFleets } from "@ubx/sdk-aws/aws/data/appstream2/associated_fleets";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_associated_fleets" });

      data(AssociatedFleets, "example", {
        stackName: "example-stack-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import AssociatedFleets, AssociatedFleetsConfig

    def describe():
        ubx.intent("look up aws_appstream2_associated_fleets")

        ubx.data(AssociatedFleets, "example", AssociatedFleetsConfig(
            stack_name="example-stack-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="stack_name" type="string" required>
  The real name of the CloudFormation stack this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
