---
title: "aws_appstream2_associated_stacks"
description: "`aws_appstream2_associated_stacks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appstream2_associated_stacks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_associated_stacks"})

    		ubx.Data(appstream2.AssociatedStacks, "example", appstream2.AssociatedStacksConfig{
    			FleetName: "example-fleet-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedStacks } from "@ubx/sdk-aws/aws/data/appstream2/associated_stacks";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_associated_stacks" });

      data(AssociatedStacks, "example", {
        fleetName: "example-fleet-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import AssociatedStacks, AssociatedStacksConfig

    def describe():
        ubx.intent("look up aws_appstream2_associated_stacks")

        ubx.data(AssociatedStacks, "example", AssociatedStacksConfig(
            fleet_name="example-fleet-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="names" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
