---
title: "aws_appstream2_software_associations"
description: "`aws_appstream2_software_associations` reads the real software associated with an AppStream 2.0 image or app block."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_software_associations"})

    		ubx.Data(appstream2.SoftwareAssociations, "example", appstream2.SoftwareAssociationsConfig{
    			AssociatedResource: "example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SoftwareAssociations } from "@ubx/sdk-aws/aws/data/appstream2/software_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_software_associations" });

      data(SoftwareAssociations, "example", {
        associatedResource: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import SoftwareAssociations, SoftwareAssociationsConfig

    def describe():
        ubx.intent("look up aws_appstream2_software_associations")

        ubx.data(SoftwareAssociations, "example", SoftwareAssociationsConfig(
            associated_resource="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="associated_resource" type="string" required>
  The real resource associated with this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="software_associations" type="list(object)">
  The real software associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
