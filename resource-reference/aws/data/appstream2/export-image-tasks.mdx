---
title: "aws_appstream2_export_image_tasks"
description: "`aws_appstream2_export_image_tasks` reads the real, current status of exporting an AppStream 2.0 image."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_export_image_tasks"})

    		ubx.Data(appstream2.ExportImageTasks, "example", appstream2.ExportImageTasksConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExportImageTasks } from "@ubx/sdk-aws/aws/data/appstream2/export_image_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_export_image_tasks" });

      data(ExportImageTasks, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import ExportImageTasks, ExportImageTasksConfig

    def describe():
        ubx.intent("look up aws_appstream2_export_image_tasks")

        ubx.data(ExportImageTasks, "example", ExportImageTasksConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="export_image_tasks" type="list(object)">
  The real EC2 image export tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
