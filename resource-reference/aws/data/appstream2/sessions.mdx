---
title: "aws_appstream2_sessions"
description: "`aws_appstream2_sessions` looks up a real AppStream 2.0 streaming session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_sessions"})

    		ubx.Data(appstream2.Sessions, "example", appstream2.SessionsConfig{
    			FleetName:          "example-fleet-name",
    			StackName:          "example-stack-name",
    			AuthenticationType: "example",
    			InstanceId:         "example",
    			Limit:              1,
    			UserId:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Sessions } from "@ubx/sdk-aws/aws/data/appstream2/sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_sessions" });

      data(Sessions, "example", {
        fleetName: "example-fleet-name",
        stackName: "example-stack-name",
        authenticationType: "example",
        instanceId: "example",
        limit: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import Sessions, SessionsConfig

    def describe():
        ubx.intent("look up aws_appstream2_sessions")

        ubx.data(Sessions, "example", SessionsConfig(
            fleet_name="example-fleet-name",
            stack_name="example-stack-name",
            authentication_type="example",
            instance_id="example",
            limit=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_type" type="string">
  The real, discriminating type of the authentication this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="fleet_name" type="string" required>
  The real name of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string">
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  The real name of the CloudFormation stack this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  &lt;p&gt;List of sessions.&lt;/p&gt; Returned by the lookup.
</ResponseField>
