---
title: "aws_appstream2_application_fleet_associations"
description: "`aws_appstream2_application_fleet_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_appstream2_application_fleet_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_application_fleet_associations"})

    		ubx.Data(appstream2.ApplicationFleetAssociations, "example", appstream2.ApplicationFleetAssociationsConfig{
    			ApplicationArn: "arn:aws:iam::123456789012:policy/example",
    			FleetName:      "example-fleet-name",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationFleetAssociations } from "@ubx/sdk-aws/aws/data/appstream2/application_fleet_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_application_fleet_associations" });

      data(ApplicationFleetAssociations, "example", {
        applicationArn: "arn:aws:iam::123456789012:policy/example",
        fleetName: "example-fleet-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import ApplicationFleetAssociations, ApplicationFleetAssociationsConfig

    def describe():
        ubx.intent("look up aws_appstream2_application_fleet_associations")

        ubx.data(ApplicationFleetAssociations, "example", ApplicationFleetAssociationsConfig(
            application_arn="arn:aws:iam::123456789012:policy/example",
            fleet_name="example-fleet-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="fleet_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_fleet_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
