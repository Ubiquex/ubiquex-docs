---
title: "aws_appstream2_entitled_applications"
description: "`aws_appstream2_entitled_applications` reads the real applications an AppStream 2.0 entitlement grants a user access to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	appstream2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/appstream2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appstream2_entitled_applications"})

    		ubx.Data(appstream2.EntitledApplications, "example", appstream2.EntitledApplicationsConfig{
    			EntitlementName: "example-entitlement-name",
    			StackName:       "example-stack-name",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EntitledApplications } from "./local-sdk/aws/sdk/typescript/aws/data/appstream2/entitled_applications.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_appstream2_entitled_applications" });

      data(EntitledApplications, "example", {
        entitlementName: "example-entitlement-name",
        stackName: "example-stack-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.appstream2 import EntitledApplications, EntitledApplicationsConfig

    def describe():
        ubx.intent("look up aws_appstream2_entitled_applications")

        ubx.data(EntitledApplications, "example", EntitledApplicationsConfig(
            entitlement_name="example-entitlement-name",
            stack_name="example-stack-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entitlement_name" type="string" required>
  The real name of the entitlement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  The real name of the CloudFormation stack this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entitled_applications" type="list(object)">
  The real applications this account is entitled to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
