---
title: "aws_kinesis_tags_for_stream"
description: "`aws_kinesis_tags_for_stream` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesis_tags_for_stream` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_tags_for_stream"})

    		ubx.Data(kinesis.TagsForStream, "example", kinesis.TagsForStreamConfig{
    			ExclusiveStartTagKey: "example",
    			Limit:                1,
    			StreamArn:            "arn:aws:iam::123456789012:policy/example",
    			StreamId:             "example",
    			StreamName:           "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForStream } from "@ubx/sdk-aws/aws/data/kinesis/tags_for_stream";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_tags_for_stream" });

      data(TagsForStream, "example", {
        exclusiveStartTagKey: "example",
        limit: 1,
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamId: "example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import TagsForStream, TagsForStreamConfig

    def describe():
        ubx.intent("look up aws_kinesis_tags_for_stream")

        ubx.data(TagsForStream, "example", TagsForStreamConfig(
            exclusive_start_tag_key="example",
            limit=1,
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_id="example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="exclusive_start_tag_key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="has_more_tags" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Returned by the lookup.
</ResponseField>
