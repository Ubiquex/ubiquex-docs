---
title: "aws_kinesis_shard_iterator"
description: "`aws_kinesis_shard_iterator` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesis_shard_iterator` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_shard_iterator"})

    		ubx.Data(kinesis.ShardIterator, "example", kinesis.ShardIteratorConfig{
    			ShardId:                "example",
    			ShardIteratorType:      "example",
    			StartingSequenceNumber: "example",
    			StreamArn:              "arn:aws:iam::123456789012:policy/example",
    			StreamId:               "example",
    			StreamName:             "example-stream-name",
    			Timestamp:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ShardIterator } from "@ubx/sdk-aws/aws/data/kinesis/shard_iterator";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_shard_iterator" });

      data(ShardIterator, "example", {
        shardId: "example",
        shardIteratorType: "example",
        startingSequenceNumber: "example",
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamId: "example",
        streamName: "example-stream-name",
        timestamp: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import ShardIterator, ShardIteratorConfig

    def describe():
        ubx.intent("look up aws_kinesis_shard_iterator")

        ubx.data(ShardIterator, "example", ShardIteratorConfig(
            shard_id="example",
            shard_iterator_type="example",
            starting_sequence_number="example",
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_id="example",
            stream_name="example-stream-name",
            timestamp="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="shard_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="shard_iterator_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="starting_sequence_number" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="timestamp" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="shard_iterator" type="string">
  Returned by the lookup.
</ResponseField>
