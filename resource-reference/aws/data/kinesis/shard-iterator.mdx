---
title: "aws_kinesis_shard_iterator"
description: "`aws_kinesis_shard_iterator` reads a real, generated shard iterator for reading records from a Kinesis Data Streams shard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_shard_iterator"})

    		ubx.Data(kinesis.ShardIterator, "example", kinesis.ShardIteratorConfig{
    			ShardId:                "example",
    			ShardIteratorType:      "example",
    			StartingSequenceNumber: "example",
    			StreamArn:              "arn:aws:iam::123456789012:policy/example",
    			StreamId:               "example",
    			StreamName:             "example-stream-name",
    			Timestamp:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ShardIterator } from "@ubx/sdk-aws/aws/data/kinesis/shard_iterator";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_shard_iterator" });

      data(ShardIterator, "example", {
        shardId: "example",
        shardIteratorType: "example",
        startingSequenceNumber: "example",
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamId: "example",
        streamName: "example-stream-name",
        timestamp: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import ShardIterator, ShardIteratorConfig

    def describe():
        ubx.intent("look up aws_kinesis_shard_iterator")

        ubx.data(ShardIterator, "example", ShardIteratorConfig(
            shard_id="example",
            shard_iterator_type="example",
            starting_sequence_number="example",
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_id="example",
            stream_name="example-stream-name",
            timestamp="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="shard_id" type="string" required>
  The real, unique identifier of the shard this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="shard_iterator_type" type="string" required>
  The real, discriminating type of the shard iterator this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="starting_sequence_number" type="string">
  The real number of the starting sequence. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string">
  The real Amazon Resource Name (ARN) of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_id" type="string">
  The real, unique identifier of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  The real name of the stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="timestamp" type="string">
  The real timestamp of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="shard_iterator" type="string">
  An opaque iterator identifying a real position within a Kinesis shard. **(AI-inferred)** Returned by the lookup.
</ResponseField>
