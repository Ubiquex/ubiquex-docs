---
title: "aws_kinesis_limits"
description: "`aws_kinesis_limits` reads real, account-level Kinesis Data Streams shard limits and current usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_limits"})

    		ubx.Data(kinesis.Limits, "example", kinesis.LimitsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Limits } from "@ubx/sdk-aws/aws/data/kinesis/limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_limits" });

      data(Limits, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import Limits, LimitsConfig

    def describe():
        ubx.intent("look up aws_kinesis_limits")

        ubx.data(Limits, "example", LimitsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="on_demand_stream_count" type="number">
  The real count of on demand streams. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="on_demand_stream_count_limit" type="number">
  The real, maximum number of on-demand streams allowed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="open_shard_count" type="number">
  The real count of open shards. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="shard_limit" type="number">
  The real, maximum number of Kinesis shards allowed. **(AI-inferred)** Returned by the lookup.
</ResponseField>
