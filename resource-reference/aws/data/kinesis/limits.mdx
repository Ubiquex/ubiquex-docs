---
title: "aws_kinesis_limits"
description: "`aws_kinesis_limits` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesis_limits` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_limits"})

    		ubx.Data(kinesis.Limits, "example", kinesis.LimitsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Limits } from "@ubx/sdk-aws/aws/data/kinesis/limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_limits" });

      data(Limits, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import Limits, LimitsConfig

    def describe():
        ubx.intent("look up aws_kinesis_limits")

        ubx.data(Limits, "example", LimitsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="on_demand_stream_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="on_demand_stream_count_limit" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="open_shard_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="shard_limit" type="number">
  Returned by the lookup.
</ResponseField>
