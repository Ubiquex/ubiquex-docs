---
title: "aws_kinesis_stream_summary"
description: "`aws_kinesis_stream_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesis_stream_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_stream_summary"})

    		ubx.Data(kinesis.StreamSummary, "example", kinesis.StreamSummaryConfig{
    			StreamArn:  "arn:aws:iam::123456789012:policy/example",
    			StreamId:   "example",
    			StreamName: "example-stream-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamSummary } from "@ubx/sdk-aws/aws/data/kinesis/stream_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_stream_summary" });

      data(StreamSummary, "example", {
        streamArn: "arn:aws:iam::123456789012:policy/example",
        streamId: "example",
        streamName: "example-stream-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import StreamSummary, StreamSummaryConfig

    def describe():
        ubx.intent("look up aws_kinesis_stream_summary")

        ubx.data(StreamSummary, "example", StreamSummaryConfig(
            stream_arn="arn:aws:iam::123456789012:policy/example",
            stream_id="example",
            stream_name="example-stream-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="stream_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="stream_description_summary" type="object">
  &lt;p&gt;Represents the output for &lt;a&gt;DescribeStreamSummary&lt;/a&gt; &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="consumer_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="encryption_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="enhanced_monitoring" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_record_size_in_ki_b" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="open_shard_count" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="retention_period_hours" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="stream_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="stream_creation_timestamp" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="stream_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="stream_mode_details" type="object">
      &lt;p&gt; Specifies the capacity mode to which you want to set your data stream. Currently, in Kinesis Data Streams, you can choose between an &lt;b&gt;on-demand&lt;/b&gt; capacity mode and a &lt;b&gt;provisioned&lt;/b&gt; capacity mode for your data streams. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="stream_mode" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stream_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="stream_status" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="warm_throughput" type="object">
      &lt;p&gt;Represents the warm throughput configuration on the stream. This is only present for On-Demand Kinesis Data Streams in accounts that have &lt;code&gt;MinimumThroughputBillingCommitment&lt;/code&gt; enabled.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="current_mi_bps" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="target_mi_bps" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
