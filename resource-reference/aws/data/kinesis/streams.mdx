---
title: "aws_kinesis_streams"
description: "`aws_kinesis_streams` looks up a real Kinesis Data Streams stream."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_streams"})

    		ubx.Data(kinesis.Streams, "example", kinesis.StreamsConfig{
    			ExclusiveStartStreamName: "example-exclusive-start-stream-name",
    			Limit:                    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Streams } from "@ubx/sdk-aws/aws/data/kinesis/streams";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_streams" });

      data(Streams, "example", {
        exclusiveStartStreamName: "example-exclusive-start-stream-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import Streams, StreamsConfig

    def describe():
        ubx.intent("look up aws_kinesis_streams")

        ubx.data(Streams, "example", StreamsConfig(
            exclusive_start_stream_name="example-exclusive-start-stream-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="exclusive_start_stream_name" type="string">
  The real name of the exclusive start stream this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="has_more_streams" type="bool">
  Whether additional real streams are available on a subsequent page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_names" type="list(string)">
  The real names of the streams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stream_summaries" type="list(object)">
  The real, summarized streams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
