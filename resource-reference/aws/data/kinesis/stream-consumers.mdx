---
title: "aws_kinesis_stream_consumers"
description: "`aws_kinesis_stream_consumers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesis_stream_consumers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_stream_consumers"})

    		ubx.Data(kinesis.StreamConsumers, "example", kinesis.StreamConsumersConfig{
    			StreamArn:               "arn:aws:iam::123456789012:policy/example",
    			MaxResults:              1,
    			StreamCreationTimestamp: "example",
    			StreamId:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamConsumers } from "@ubx/sdk-aws/aws/data/kinesis/stream_consumers";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_stream_consumers" });

      data(StreamConsumers, "example", {
        streamArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        streamCreationTimestamp: "example",
        streamId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis import StreamConsumers, StreamConsumersConfig

    def describe():
        ubx.intent("look up aws_kinesis_stream_consumers")

        ubx.data(StreamConsumers, "example", StreamConsumersConfig(
            stream_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            stream_creation_timestamp="example",
            stream_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stream_creation_timestamp" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="stream_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="consumers" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
