---
title: "aws_redshift_serverless_identity_center_auth_token"
description: "`aws_redshift_serverless_identity_center_auth_token` reads a real, temporary authentication token for a Redshift Serverless workgroup issued through..."
---

`aws_redshift_serverless_identity_center_auth_token` reads a real, temporary authentication token for a Redshift Serverless workgroup issued through IAM Identity Center trusted identity propagation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_serverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift_serverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_serverless_identity_center_auth_token"})

    		ubx.Data(redshift_serverless.RedshiftServerlessIdentityCenterAuthToken, "example", redshift_serverless.RedshiftServerlessIdentityCenterAuthTokenConfig{
    			WorkgroupNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftServerlessIdentityCenterAuthToken } from "@ubx/sdk-aws/aws/data/redshift_serverless/redshift_serverless_identity_center_auth_token";

    export default stack("example", () => {
      intent({
        summary: "look up aws_redshift_serverless_identity_center_auth_token",
      });

      data(RedshiftServerlessIdentityCenterAuthToken, "example", {
        workgroupNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_serverless import RedshiftServerlessIdentityCenterAuthToken, RedshiftServerlessIdentityCenterAuthTokenConfig

    def describe():
        ubx.intent("look up aws_redshift_serverless_identity_center_auth_token")

        ubx.data(RedshiftServerlessIdentityCenterAuthToken, "example", RedshiftServerlessIdentityCenterAuthTokenConfig(
            workgroup_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workgroup_names" type="list(string)" required>
  The real names of the workgroups included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="expiration_time" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="token" type="string">
  An opaque pagination or continuation token from a previous response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
