---
title: "aws_redshift_serverless_tracks"
description: "`aws_redshift_serverless_tracks` reads the real, available Redshift Serverless tracks."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_serverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift_serverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_serverless_tracks"})

    		ubx.Data(redshift_serverless.RedshiftServerlessTracks, "example", redshift_serverless.RedshiftServerlessTracksConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftServerlessTracks } from "@ubx/sdk-aws/aws/data/redshift_serverless/redshift_serverless_tracks";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_serverless_tracks" });

      data(RedshiftServerlessTracks, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_serverless import RedshiftServerlessTracks, RedshiftServerlessTracksConfig

    def describe():
        ubx.intent("look up aws_redshift_serverless_tracks")

        ubx.data(RedshiftServerlessTracks, "example", RedshiftServerlessTracksConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tracks" type="list(object)">
  The real tracks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
