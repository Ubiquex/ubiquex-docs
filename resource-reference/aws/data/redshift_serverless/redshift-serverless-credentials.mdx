---
title: "aws_redshift_serverless_credentials"
description: "`aws_redshift_serverless_credentials` reads real, temporary database credentials for a Redshift Serverless workgroup."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift_serverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift_serverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_serverless_credentials"})

    		ubx.Data(redshift_serverless.RedshiftServerlessCredentials, "example", redshift_serverless.RedshiftServerlessCredentialsConfig{
    			CustomDomainName: "example-custom-domain-name",
    			DbName:           "example-db-name",
    			DurationSeconds:  7200,
    			WorkgroupName:    "example-workgroup-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RedshiftServerlessCredentials } from "./local-sdk/aws/sdk/typescript/aws/data/redshift_serverless/redshift_serverless_credentials.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_serverless_credentials" });

      data(RedshiftServerlessCredentials, "example", {
        customDomainName: "example-custom-domain-name",
        dbName: "example-db-name",
        durationSeconds: 7200,
        workgroupName: "example-workgroup-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_serverless import RedshiftServerlessCredentials, RedshiftServerlessCredentialsConfig

    def describe():
        ubx.intent("look up aws_redshift_serverless_credentials")

        ubx.data(RedshiftServerlessCredentials, "example", RedshiftServerlessCredentialsConfig(
            custom_domain_name="example-custom-domain-name",
            db_name="example-db-name",
            duration_seconds=7200,
            workgroup_name="example-workgroup-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="custom_domain_name" type="string">
  The real custom domain name this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="db_name" type="string">
  The real name of the db this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="duration_seconds" type="number">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workgroup_name" type="string">
  The real name of the workgroup this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="db_password" type="string">
  The real, generated database password for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="db_user" type="string">
  The real database user this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiration" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_refresh_time" type="string">
  The real timestamp of the next refresh. **(AI-inferred)** Returned by the lookup.
</ResponseField>
