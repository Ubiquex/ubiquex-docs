---
title: "aws_billing_credits"
description: "`aws_billing_credits` reads the real promotional or service credits applied to an account's own billing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billing_credits"})

    		ubx.Data(billing.Credits, "example", billing.CreditsConfig{
    			AccountId:        "example",
    			StartDate:        "example",
    			EndDate:          "example",
    			PayerAccountFlag: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Credits } from "./local-sdk/aws/sdk/typescript/aws/data/billing/credits.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_billing_credits" });

      data(Credits, "example", {
        accountId: "example",
        startDate: "example",
        endDate: "example",
        payerAccountFlag: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.billing import Credits, CreditsConfig

    def describe():
        ubx.intent("look up aws_billing_credits")

        ubx.data(Credits, "example", CreditsConfig(
            account_id="example",
            start_date="example",
            end_date="example",
            payer_account_flag=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_date" type="string">
  The latest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="payer_account_flag" type="bool">
  Whether this account is the payer account. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string" required>
  The earliest real date to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="credits" type="list(object)">
  The real, accrued credits for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
