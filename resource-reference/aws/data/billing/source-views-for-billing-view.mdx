---
title: "aws_billing_source_views_for_billing_view"
description: "`aws_billing_source_views_for_billing_view` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_billing_source_views_for_billing_view` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billing_source_views_for_billing_view"})

    		ubx.Data(billing.SourceViewsForBillingView, "example", billing.SourceViewsForBillingViewConfig{
    			Arn:        "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SourceViewsForBillingView } from "@ubx/sdk-aws/aws/data/billing/source_views_for_billing_view";

    export default stack("example", () => {
      intent({ summary: "look up aws_billing_source_views_for_billing_view" });

      data(SourceViewsForBillingView, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.billing import SourceViewsForBillingView, SourceViewsForBillingViewConfig

    def describe():
        ubx.intent("look up aws_billing_source_views_for_billing_view")

        ubx.data(SourceViewsForBillingView, "example", SourceViewsForBillingViewConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="source_views" type="list(string)">
  Returned by the lookup.
</ResponseField>
