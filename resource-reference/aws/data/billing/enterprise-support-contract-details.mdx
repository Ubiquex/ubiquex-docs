---
title: "aws_billing_enterprise_support_contract_details"
description: "`aws_billing_enterprise_support_contract_details` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_billing_enterprise_support_contract_details` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billing_enterprise_support_contract_details"})

    		ubx.Data(billing.EnterpriseSupportContractDetails, "example", billing.EnterpriseSupportContractDetailsConfig{
    			BillingMonth: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnterpriseSupportContractDetails } from "@ubx/sdk-aws/aws/data/billing/enterprise_support_contract_details";

    export default stack("example", () => {
      intent({
        summary: "look up aws_billing_enterprise_support_contract_details",
      });

      data(EnterpriseSupportContractDetails, "example", {
        billingMonth: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.billing import EnterpriseSupportContractDetails, EnterpriseSupportContractDetailsConfig

    def describe():
        ubx.intent("look up aws_billing_enterprise_support_contract_details")

        ubx.data(EnterpriseSupportContractDetails, "example", EnterpriseSupportContractDetailsConfig(
            billing_month="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_month" type="string" required>
  &lt;p&gt;The billing month in YYYY-MM format. This must be a month in the past.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="additional_support_charge" type="list(object)">
  &lt;p&gt;A list of additional charges.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="additional_support_eligible_usage_spend" type="list(object)">
  &lt;p&gt;A list of additional charges.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="charged_payer_account_ids" type="list(object)">
  &lt;p&gt;A list of charge accounts.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="contract_payer_account_ids" type="list(object)">
  &lt;p&gt;A list of accounts that appear in the Enterprise Support Contract.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="is_contract_active" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pricing_plans" type="list(object)">
  &lt;p&gt;A list of pricing plans.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="support_allocation_method" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="support_prorate_start_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="support_reserved_instance_amortization_start_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="support_reserved_instance_treatment_method" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="support_savings_plans_amortization_start_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="support_savings_plans_treatment_method" type="string">
  Returned by the lookup.
</ResponseField>
