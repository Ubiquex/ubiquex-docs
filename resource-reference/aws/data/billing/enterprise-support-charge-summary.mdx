---
title: "aws_billing_enterprise_support_charge_summary"
description: "`aws_billing_enterprise_support_charge_summary` reads a real, computed Enterprise Support charge summary."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billing_enterprise_support_charge_summary"})

    		ubx.Data(billing.EnterpriseSupportChargeSummary, "example", billing.EnterpriseSupportChargeSummaryConfig{
    			BillingMonth: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnterpriseSupportChargeSummary } from "@ubx/sdk-aws/aws/data/billing/enterprise_support_charge_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_billing_enterprise_support_charge_summary" });

      data(EnterpriseSupportChargeSummary, "example", {
        billingMonth: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.billing import EnterpriseSupportChargeSummary, EnterpriseSupportChargeSummaryConfig

    def describe():
        ubx.intent("look up aws_billing_enterprise_support_charge_summary")

        ubx.data(EnterpriseSupportChargeSummary, "example", EnterpriseSupportChargeSummaryConfig(
            billing_month="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_month" type="string" required>
  &lt;p&gt;The billing month in YYYY-MM format. This must be a month in the past.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bill_date" type="string">
  The real date of the bill. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="billing_period_end_date" type="string">
  The real date of the billing period end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="billing_period_start_date" type="string">
  The real date of the billing period start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_estimated" type="bool">
  Whether this value is an estimate rather than an exact measurement. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="payer_account_id" type="string">
  The real, unique identifier of the payer account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="support_charge" type="string">
  The real, computed AWS Support charge. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="support_charge_percentage" type="string">
  The real, computed AWS Support charge, as a percentage. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="support_discount" type="string">
  The real discount applied to AWS Support charges. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="support_effective_pricing_plan" type="object">
  &lt;p&gt;A pricing plan for Enterprise Support billing.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="discount_applies_to_minimum_charge" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="end_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="minimum_charge" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="plan_discount_percent" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="pricing_plan_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tiered" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tiers" type="list(object)" required>
      &lt;p&gt;A list of pricing plan tiers.&lt;/p&gt; Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_support_charge" type="string">
  The real, total AWS Support charge. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_support_eligible_reserved_instance_spend" type="string">
  The real, total Reserved Instance spend eligible for AWS Support charge calculation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_support_eligible_savings_plan_spend" type="string">
  The real, total Savings Plans spend eligible for AWS Support charge calculation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_support_eligible_spend" type="string">
  The real, total spend eligible for AWS Support charge calculation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_support_eligible_usage_spend" type="string">
  The real, total usage spend eligible for AWS Support charge calculation. **(AI-inferred)** Returned by the lookup.
</ResponseField>
