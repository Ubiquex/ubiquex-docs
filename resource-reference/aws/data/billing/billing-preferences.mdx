---
title: "aws_billing_billing_preferences"
description: "`aws_billing_billing_preferences` reads the real, account-wide billing preferences, such as Savings Plans/Reserved Instance sharing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billing_billing_preferences"})

    		ubx.Data(billing.BillingPreferences, "example", billing.BillingPreferencesConfig{
    			Features:   []string{"example"},
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BillingPreferences } from "@ubx/sdk-aws/aws/data/billing/billing_preferences";

    export default stack("example", () => {
      intent({ summary: "look up aws_billing_billing_preferences" });

      data(BillingPreferences, "example", {
        features: ["example"],
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.billing import BillingPreferences, BillingPreferencesConfig
    from ubx.aws.data.billing.billing_preferences import BillingPreferences_Filters

    def describe():
        ubx.intent("look up aws_billing_billing_preferences")

        ubx.data(BillingPreferences, "example", BillingPreferencesConfig(
            features=["example"],
            filters=[BillingPreferences_Filters(name="example-name")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="features" type="list(string)" required>
  The real features included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="billing_preferences" type="list(object)">
  The real, configured billing preferences for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
