---
title: "aws_billing_enterprise_support_linked_account_charges"
description: "`aws_billing_enterprise_support_linked_account_charges` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_billing_enterprise_support_linked_account_charges` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billing_enterprise_support_linked_account_charges"})

    		ubx.Data(billing.EnterpriseSupportLinkedAccountCharges, "example", billing.EnterpriseSupportLinkedAccountChargesConfig{
    			BillingMonth: "example",
    			AccountId:    "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnterpriseSupportLinkedAccountCharges } from "@ubx/sdk-aws/aws/data/billing/enterprise_support_linked_account_charges";

    export default stack("example", () => {
      intent({
        summary: "look up aws_billing_enterprise_support_linked_account_charges",
      });

      data(EnterpriseSupportLinkedAccountCharges, "example", {
        billingMonth: "example",
        accountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.billing import EnterpriseSupportLinkedAccountCharges, EnterpriseSupportLinkedAccountChargesConfig

    def describe():
        ubx.intent("look up aws_billing_enterprise_support_linked_account_charges")

        ubx.data(EnterpriseSupportLinkedAccountCharges, "example", EnterpriseSupportLinkedAccountChargesConfig(
            billing_month="example",
            account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="billing_month" type="string" required>
  &lt;p&gt;The billing month in YYYY-MM format. This must be a month in the past.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="linked_account" type="list(object)">
  &lt;p&gt;A list of linked account charges.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
