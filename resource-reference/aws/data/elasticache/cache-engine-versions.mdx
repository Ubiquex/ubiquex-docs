---
title: "aws_elasticache_cache_engine_versions"
description: "`aws_elasticache_cache_engine_versions` reads the real, available ElastiCache engine (Redis/Valkey/Memcached) versions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_cache_engine_versions"})

    		ubx.Data(elasticache.CacheEngineVersions, "example", elasticache.CacheEngineVersionsConfig{
    			CacheParameterGroupFamily: "example",
    			DefaultOnly:               true,
    			Engine:                    "example",
    			EngineVersion:             "example",
    			MaxRecords:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CacheEngineVersions } from "./local-sdk/aws/sdk/typescript/aws/data/elasticache/cache_engine_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticache_cache_engine_versions" });

      data(CacheEngineVersions, "example", {
        cacheParameterGroupFamily: "example",
        defaultOnly: true,
        engine: "example",
        engineVersion: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import CacheEngineVersions, CacheEngineVersionsConfig

    def describe():
        ubx.intent("look up aws_elasticache_cache_engine_versions")

        ubx.data(CacheEngineVersions, "example", CacheEngineVersionsConfig(
            cache_parameter_group_family="example",
            default_only=True,
            engine="example",
            engine_version="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_parameter_group_family" type="string">
  The real cache parameter group family. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="default_only" type="bool">
  Whether the response is restricted to the default object only. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="engine" type="string">
  The real database or compute engine this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="engine_version" type="string">
  The real version of the database or compute engine this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cache_engine_versions" type="list(object)">
  The real versions of the cache engine included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
