---
title: "aws_elasticache_update_actions"
description: "`aws_elasticache_update_actions` reads the real, pending or applied service update actions for an ElastiCache cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_update_actions"})

    		ubx.Data(elasticache.UpdateActions, "example", elasticache.UpdateActionsConfig{
    			CacheClusterIds:           []string{"example"},
    			Engine:                    "example",
    			MaxRecords:                1,
    			ReplicationGroupIds:       []string{"example"},
    			ServiceUpdateName:         "example-service-update-name",
    			ServiceUpdateStatus:       []string{"example"},
    			ServiceUpdateTimeRange:    map[string]any{"end_time": "example"},
    			ShowNodeLevelUpdateStatus: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { UpdateActions } from "./local-sdk/aws/sdk/typescript/aws/data/elasticache/update_actions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticache_update_actions" });

      data(UpdateActions, "example", {
        cacheClusterIds: ["example"],
        engine: "example",
        maxRecords: 1,
        replicationGroupIds: ["example"],
        serviceUpdateName: "example-service-update-name",
        serviceUpdateStatus: ["example"],
        serviceUpdateTimeRange: { endTime: "example" },
        showNodeLevelUpdateStatus: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import UpdateActions, UpdateActionsConfig

    def describe():
        ubx.intent("look up aws_elasticache_update_actions")

        ubx.data(UpdateActions, "example", UpdateActionsConfig(
            cache_cluster_ids=["example"],
            engine="example",
            max_records=1,
            replication_group_ids=["example"],
            service_update_name="example-service-update-name",
            service_update_status=["example"],
            service_update_time_range={"end_time": "example"},
            show_node_level_update_status=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_cluster_ids" type="list(string)">
  The real, unique identifiers of the cache clusters this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="engine" type="string">
  The real database or compute engine this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replication_group_ids" type="list(string)">
  The real, unique identifiers of the replication groups this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_update_name" type="string">
  The real name of the service update this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_update_status" type="list(string)">
  The real, current status of the service update this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_update_time_range" type="object">
  &lt;p&gt;Filters update actions from the service updates that are in available status during the time range.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="show_node_level_update_status" type="bool">
  The real, current status of the show node level update this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="update_action_status" type="list(string)">
  The real, current status of the update action this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_actions" type="list(object)">
  The real, available update actions for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
