---
title: "aws_elasticache_allowed_node_type_modifications"
description: "`aws_elasticache_allowed_node_type_modifications` reads the real node types an ElastiCache cluster or replication group is currently allowed to scale to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_allowed_node_type_modifications"})

    		ubx.Data(elasticache.AllowedNodeTypeModifications, "example", elasticache.AllowedNodeTypeModificationsConfig{
    			CacheClusterId:     "example",
    			ReplicationGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AllowedNodeTypeModifications } from "@ubx/sdk-aws/aws/data/elasticache/allowed_node_type_modifications";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elasticache_allowed_node_type_modifications",
      });

      data(AllowedNodeTypeModifications, "example", {
        cacheClusterId: "example",
        replicationGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import AllowedNodeTypeModifications, AllowedNodeTypeModificationsConfig

    def describe():
        ubx.intent("look up aws_elasticache_allowed_node_type_modifications")

        ubx.data(AllowedNodeTypeModifications, "example", AllowedNodeTypeModificationsConfig(
            cache_cluster_id="example",
            replication_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_cluster_id" type="string">
  The real, unique identifier of the cache cluster this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replication_group_id" type="string">
  The real, unique identifier of the replication group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="scale_down_modifications" type="list(string)">
  The real, allowed scale-down modifications for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scale_up_modifications" type="list(string)">
  The real, allowed scale-up modifications for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
