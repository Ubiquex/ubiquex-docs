---
title: "aws_elasticache_engine_default_parameters"
description: "`aws_elasticache_engine_default_parameters` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticache_engine_default_parameters` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_engine_default_parameters"})

    		ubx.Data(elasticache.EngineDefaultParameters, "example", elasticache.EngineDefaultParametersConfig{
    			CacheParameterGroupFamily: "example",
    			Marker:                    "example",
    			MaxRecords:                1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EngineDefaultParameters } from "@ubx/sdk-aws/aws/data/elasticache/engine_default_parameters";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticache_engine_default_parameters" });

      data(EngineDefaultParameters, "example", {
        cacheParameterGroupFamily: "example",
        marker: "example",
        maxRecords: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import EngineDefaultParameters, EngineDefaultParametersConfig

    def describe():
        ubx.intent("look up aws_elasticache_engine_default_parameters")

        ubx.data(EngineDefaultParameters, "example", EngineDefaultParametersConfig(
            cache_parameter_group_family="example",
            marker="example",
            max_records=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_parameter_group_family" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="engine_defaults" type="object">
  &lt;p&gt;Represents the output of a &lt;code&gt;DescribeEngineDefaultParameters&lt;/code&gt; operation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cache_node_type_specific_parameters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cache_parameter_group_family" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parameters" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
