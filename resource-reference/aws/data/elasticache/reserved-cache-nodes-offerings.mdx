---
title: "aws_elasticache_reserved_cache_nodes_offerings"
description: "`aws_elasticache_reserved_cache_nodes_offerings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticache_reserved_cache_nodes_offerings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_reserved_cache_nodes_offerings"})

    		ubx.Data(elasticache.ReservedCacheNodesOfferings, "example", elasticache.ReservedCacheNodesOfferingsConfig{
    			CacheNodeType:                "example",
    			Duration:                     "example",
    			MaxRecords:                   1,
    			OfferingType:                 "example",
    			ProductDescription:           "example",
    			ReservedCacheNodesOfferingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReservedCacheNodesOfferings } from "@ubx/sdk-aws/aws/data/elasticache/reserved_cache_nodes_offerings";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticache_reserved_cache_nodes_offerings" });

      data(ReservedCacheNodesOfferings, "example", {
        cacheNodeType: "example",
        duration: "example",
        maxRecords: 1,
        offeringType: "example",
        productDescription: "example",
        reservedCacheNodesOfferingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import ReservedCacheNodesOfferings, ReservedCacheNodesOfferingsConfig

    def describe():
        ubx.intent("look up aws_elasticache_reserved_cache_nodes_offerings")

        ubx.data(ReservedCacheNodesOfferings, "example", ReservedCacheNodesOfferingsConfig(
            cache_node_type="example",
            duration="example",
            max_records=1,
            offering_type="example",
            product_description="example",
            reserved_cache_nodes_offering_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_node_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="duration" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offering_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product_description" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reserved_cache_nodes_offering_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="reserved_cache_nodes_offerings" type="list(object)">
  Returned by the lookup.
</ResponseField>
