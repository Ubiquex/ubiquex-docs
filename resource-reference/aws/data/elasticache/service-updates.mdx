---
title: "aws_elasticache_service_updates"
description: "`aws_elasticache_service_updates` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticache_service_updates` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_service_updates"})

    		ubx.Data(elasticache.ServiceUpdates, "example", elasticache.ServiceUpdatesConfig{
    			MaxRecords:          1,
    			ServiceUpdateName:   "example-service-update-name",
    			ServiceUpdateStatus: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceUpdates } from "@ubx/sdk-aws/aws/data/elasticache/service_updates";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticache_service_updates" });

      data(ServiceUpdates, "example", {
        maxRecords: 1,
        serviceUpdateName: "example-service-update-name",
        serviceUpdateStatus: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import ServiceUpdates, ServiceUpdatesConfig

    def describe():
        ubx.intent("look up aws_elasticache_service_updates")

        ubx.data(ServiceUpdates, "example", ServiceUpdatesConfig(
            max_records=1,
            service_update_name="example-service-update-name",
            service_update_status=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_update_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_update_status" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_updates" type="list(object)">
  Returned by the lookup.
</ResponseField>
