---
title: "aws_elasticache_cache_parameters"
description: "`aws_elasticache_cache_parameters` reads the real engine parameters of an ElastiCache parameter group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticache_cache_parameters"})

    		ubx.Data(elasticache.CacheParameters, "example", elasticache.CacheParametersConfig{
    			CacheParameterGroupName: "example-cache-parameter-group-name",
    			MaxRecords:              1,
    			Source:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CacheParameters } from "./local-sdk/aws/sdk/typescript/aws/data/elasticache/cache_parameters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticache_cache_parameters" });

      data(CacheParameters, "example", {
        cacheParameterGroupName: "example-cache-parameter-group-name",
        maxRecords: 1,
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticache import CacheParameters, CacheParametersConfig

    def describe():
        ubx.intent("look up aws_elasticache_cache_parameters")

        ubx.data(CacheParameters, "example", CacheParametersConfig(
            cache_parameter_group_name="example-cache-parameter-group-name",
            max_records=1,
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_parameter_group_name" type="string" required>
  The real name of the cache parameter group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="string">
  The real source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cache_node_type_specific_parameters" type="list(object)">
  The real parameters specific to this cache node type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
