---
title: "aws_apigateway_sdk_type"
description: "`aws_apigateway_sdk_type` looks up one real, available API Gateway SDK type (a target language or platform)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_sdk_type"})

    		ubx.Data(apigateway.SdkType, "example", apigateway.SdkTypeConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SdkType } from "@ubx/sdk-aws/aws/data/apigateway/sdk_type";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_sdk_type" });

      data(SdkType, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import SdkType, SdkTypeConfig

    def describe():
        ubx.intent("look up aws_apigateway_sdk_type")

        ubx.data(SdkType, "example", SdkTypeConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_properties" type="list(object)">
  The real, structured configuration properties of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="friendly_name" type="string">
  The real name of the friendly this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
