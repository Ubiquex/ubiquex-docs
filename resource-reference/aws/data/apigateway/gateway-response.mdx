---
title: "aws_apigateway_gateway_response"
description: "`aws_apigateway_gateway_response` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_gateway_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_gateway_response"})

    		ubx.Data(apigateway.GatewayResponse, "example", apigateway.GatewayResponseConfig{
    			ResponseType: "example",
    			RestApiId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GatewayResponse } from "@ubx/sdk-aws/aws/data/apigateway/gateway_response";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_gateway_response" });

      data(GatewayResponse, "example", {
        responseType: "example",
        restApiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import GatewayResponse, GatewayResponseConfig

    def describe():
        ubx.intent("look up aws_apigateway_gateway_response")

        ubx.data(GatewayResponse, "example", GatewayResponseConfig(
            response_type="example",
            rest_api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="response_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="default_response" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="response_parameters" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="response_templates" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_code" type="string">
  &lt;p&gt;The status code.&lt;/p&gt; Returned by the lookup.
</ResponseField>
