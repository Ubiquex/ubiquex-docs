---
title: "aws_apigateway_api_keys"
description: "`aws_apigateway_api_keys` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_api_keys` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_api_keys"})

    		ubx.Data(apigateway.ApiKeys, "example", apigateway.ApiKeysConfig{
    			CustomerId:    "example",
    			IncludeValues: true,
    			Limit:         1,
    			NameQuery:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiKeys } from "@ubx/sdk-aws/aws/data/apigateway/api_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_api_keys" });

      data(ApiKeys, "example", {
        customerId: "example",
        includeValues: true,
        limit: 1,
        nameQuery: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import ApiKeys, ApiKeysConfig

    def describe():
        ubx.intent("look up aws_apigateway_api_keys")

        ubx.data(ApiKeys, "example", ApiKeysConfig(
            customer_id="example",
            include_values=True,
            limit=1,
            name_query="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="customer_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_values" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_query" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="warnings" type="list(string)">
  Returned by the lookup.
</ResponseField>
