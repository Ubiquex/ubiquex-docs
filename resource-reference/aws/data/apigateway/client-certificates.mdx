---
title: "aws_apigateway_client_certificates"
description: "`aws_apigateway_client_certificates` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_client_certificates` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_client_certificates"})

    		ubx.Data(apigateway.ClientCertificates, "example", apigateway.ClientCertificatesConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClientCertificates } from "@ubx/sdk-aws/aws/data/apigateway/client_certificates";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_client_certificates" });

      data(ClientCertificates, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import ClientCertificates, ClientCertificatesConfig

    def describe():
        ubx.intent("look up aws_apigateway_client_certificates")

        ubx.data(ClientCertificates, "example", ClientCertificatesConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  Returned by the lookup.
</ResponseField>
