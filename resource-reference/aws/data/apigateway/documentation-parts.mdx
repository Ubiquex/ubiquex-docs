---
title: "aws_apigateway_documentation_parts"
description: "`aws_apigateway_documentation_parts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_documentation_parts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_documentation_parts"})

    		ubx.Data(apigateway.DocumentationParts, "example", apigateway.DocumentationPartsConfig{
    			RestApiId:      "example",
    			Limit:          1,
    			LocationStatus: "example",
    			NameQuery:      "example",
    			Path:           "/example/",
    			Type:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentationParts } from "@ubx/sdk-aws/aws/data/apigateway/documentation_parts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_documentation_parts" });

      data(DocumentationParts, "example", {
        restApiId: "example",
        limit: 1,
        locationStatus: "example",
        nameQuery: "example",
        path: "/example/",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import DocumentationParts, DocumentationPartsConfig

    def describe():
        ubx.intent("look up aws_apigateway_documentation_parts")

        ubx.data(DocumentationParts, "example", DocumentationPartsConfig(
            rest_api_id="example",
            limit=1,
            location_status="example",
            name_query="example",
            path="/example/",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="location_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name_query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="path" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  Returned by the lookup.
</ResponseField>
