---
title: "aws_apigateway_documentation_parts"
description: "`data_aws_apigateway_documentation_parts` looks up a real, already-existing `aws_api_gateway_documentation_part`."
---

It is one documented fragment, a description of a specific method, resource, or parameter, that rolls up into an `aws_api_gateway_documentation_version` for an `aws_api_gateway_rest_api`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_documentation_parts"})

    		ubx.Data(apigateway.DocumentationParts, "example", apigateway.DocumentationPartsConfig{
    			RestApiId:      "example",
    			Limit:          1,
    			LocationStatus: "example",
    			NameQuery:      "example",
    			Path:           "/example/",
    			Type:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentationParts } from "@ubx/sdk-aws/aws/data/apigateway/documentation_parts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_documentation_parts" });

      data(DocumentationParts, "example", {
        restApiId: "example",
        limit: 1,
        locationStatus: "example",
        nameQuery: "example",
        path: "/example/",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import DocumentationParts, DocumentationPartsConfig

    def describe():
        ubx.intent("look up aws_apigateway_documentation_parts")

        ubx.data(DocumentationParts, "example", DocumentationPartsConfig(
            rest_api_id="example",
            limit=1,
            location_status="example",
            name_query="example",
            path="/example/",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="location_status" type="string">
  The real, current status of the location this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_query" type="string">
  The real, free-text query matched against object names. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path" type="string">
  The real path of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The real, unique identifier of the REST API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  The real position of this object within its own ordered sequence. **(AI-inferred)** Returned by the lookup.
</ResponseField>
