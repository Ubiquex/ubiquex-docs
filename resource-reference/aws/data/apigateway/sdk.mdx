---
title: "aws_apigateway_sdk"
description: "`aws_apigateway_sdk` reads a real, generated client SDK for an API Gateway REST API stage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_sdk"})

    		ubx.Data(apigateway.Sdk, "example", apigateway.SdkConfig{
    			RestApiId:  "example",
    			SdkType:    "example",
    			StageName:  "example-stage-name",
    			Parameters: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Sdk } from "@ubx/sdk-aws/aws/data/apigateway/sdk";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_sdk" });

      data(Sdk, "example", {
        restApiId: "example",
        sdkType: "example",
        stageName: "example-stage-name",
        parameters: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import Sdk, SdkConfig

    def describe():
        ubx.intent("look up aws_apigateway_sdk")

        ubx.data(Sdk, "example", SdkConfig(
            rest_api_id="example",
            sdk_type="example",
            stage_name="example-stage-name",
            parameters={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="parameters" type="map(string)">
  The real, structured parameters of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The real, unique identifier of the REST API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sdk_type" type="string" required>
  The real, discriminating type of the sdk this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="stage_name" type="string" required>
  The real name of the stage this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="body" type="string">
  The real, raw body of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_disposition" type="string">
  The real Content-Disposition header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
