---
title: "aws_apigateway_rest_apis"
description: "`aws_apigateway_rest_apis` looks up a real API Gateway REST API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_rest_apis"})

    		ubx.Data(apigateway.RestApis, "example", apigateway.RestApisConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RestApis } from "@ubx/sdk-aws/aws/data/apigateway/rest_apis";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_rest_apis" });

      data(RestApis, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import RestApis, RestApisConfig

    def describe():
        ubx.intent("look up aws_apigateway_rest_apis")

        ubx.data(RestApis, "example", RestApisConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  The real position of this object within its own ordered sequence. **(AI-inferred)** Returned by the lookup.
</ResponseField>
