---
title: "aws_apigateway_export"
description: "`aws_apigateway_export` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_export` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_export"})

    		ubx.Data(apigateway.Export, "example", apigateway.ExportConfig{
    			ExportType: "example",
    			RestApiId:  "example",
    			StageName:  "example-stage-name",
    			Accepts:    "example",
    			Parameters: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Export } from "@ubx/sdk-aws/aws/data/apigateway/export";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_export" });

      data(Export, "example", {
        exportType: "example",
        restApiId: "example",
        stageName: "example-stage-name",
        accepts: "example",
        parameters: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import Export, ExportConfig

    def describe():
        ubx.intent("look up aws_apigateway_export")

        ubx.data(Export, "example", ExportConfig(
            export_type="example",
            rest_api_id="example",
            stage_name="example-stage-name",
            accepts="example",
            parameters={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accepts" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="export_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="stage_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="body" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="content_disposition" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  Returned by the lookup.
</ResponseField>
