---
title: "aws_apigateway_method"
description: "`aws_apigateway_method` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_method` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_method"})

    		ubx.Data(apigateway.Method, "example", apigateway.MethodConfig{
    			HttpMethod: "example",
    			ResourceId: "example",
    			RestApiId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Method } from "@ubx/sdk-aws/aws/data/apigateway/method";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_method" });

      data(Method, "example", {
        httpMethod: "example",
        resourceId: "example",
        restApiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import Method, MethodConfig

    def describe():
        ubx.intent("look up aws_apigateway_method")

        ubx.data(Method, "example", MethodConfig(
            http_method="example",
            resource_id="example",
            rest_api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="http_method" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="api_key_required" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="authorization_scopes" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="authorization_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="authorizer_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="method_integration" type="object">
  &lt;p&gt;Represents an &lt;code&gt;HTTP&lt;/code&gt;, &lt;code&gt;HTTP_PROXY&lt;/code&gt;, &lt;code&gt;AWS&lt;/code&gt;, &lt;code&gt;AWS_PROXY&lt;/code&gt;, or Mock integration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cache_key_parameters" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cache_namespace" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="connection_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="connection_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="content_handling" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="credentials" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="http_method" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="integration_responses" type="map(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="integration_target" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="passthrough_behavior" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="request_parameters" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="request_templates" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="response_transfer_mode" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timeout_in_millis" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="tls_config" type="object">
      &lt;p&gt;Specifies the TLS configuration for an integration.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="insecure_skip_verification" type="bool">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      &lt;p&gt;The integration type. The valid value is &lt;code&gt;HTTP&lt;/code&gt; for integrating an API method with an HTTP backend; &lt;code&gt;AWS&lt;/code&gt; with any Amazon Web Services service endpoints; &lt;code&gt;MOCK&lt;/code&gt; for testing without actually invoking the backend; &lt;code&gt;HTTP_PROXY&lt;/code&gt; for integrating with the HTTP proxy integration; &lt;code&gt;AWS_PROXY&lt;/code&gt; for integrating with the Lambda proxy integration. &lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="uri" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="method_responses" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="operation_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_models" type="map(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_parameters" type="map(bool)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="request_validator_id" type="string">
  Returned by the lookup.
</ResponseField>
