---
title: "aws_apigateway_model_template"
description: "`aws_apigateway_model_template` reads a real, sample mapping template generated from an API Gateway model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_model_template"})

    		ubx.Data(apigateway.ModelTemplate, "example", apigateway.ModelTemplateConfig{
    			ModelName: "example-model-name",
    			RestApiId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelTemplate } from "./local-sdk/aws/sdk/typescript/aws/data/apigateway/model_template.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_model_template" });

      data(ModelTemplate, "example", {
        modelName: "example-model-name",
        restApiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import ModelTemplate, ModelTemplateConfig

    def describe():
        ubx.intent("look up aws_apigateway_model_template")

        ubx.data(ModelTemplate, "example", ModelTemplateConfig(
            model_name="example-model-name",
            rest_api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="model_name" type="string" required>
  The real name of the model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The real, unique identifier of the REST API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="string">
  The real, current value of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
