---
title: "aws_apigateway_deployments"
description: "`aws_apigateway_deployments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_deployments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_deployments"})

    		ubx.Data(apigateway.Deployments, "example", apigateway.DeploymentsConfig{
    			RestApiId: "example",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Deployments } from "@ubx/sdk-aws/aws/data/apigateway/deployments";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_deployments" });

      data(Deployments, "example", {
        restApiId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import Deployments, DeploymentsConfig

    def describe():
        ubx.intent("look up aws_apigateway_deployments")

        ubx.data(Deployments, "example", DeploymentsConfig(
            rest_api_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  Returned by the lookup.
</ResponseField>
