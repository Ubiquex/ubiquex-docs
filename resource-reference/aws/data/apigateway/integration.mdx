---
title: "aws_apigateway_integration"
description: "`aws_apigateway_integration` looks up a real API Gateway integration, connecting a method to its own backend."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_integration"})

    		ubx.Data(apigateway.Integration, "example", apigateway.IntegrationConfig{
    			HttpMethod: "example",
    			ResourceId: "example",
    			RestApiId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Integration } from "@ubx/sdk-aws/aws/data/apigateway/integration";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_integration" });

      data(Integration, "example", {
        httpMethod: "example",
        resourceId: "example",
        restApiId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import Integration, IntegrationConfig

    def describe():
        ubx.intent("look up aws_apigateway_integration")

        ubx.data(Integration, "example", IntegrationConfig(
            http_method="example",
            resource_id="example",
            rest_api_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="http_method" type="string" required>
  The real HTTP method this object applies to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The real, unique identifier of the REST API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cache_key_parameters" type="list(string)">
  The real request parameters included in this object's own cache key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="cache_namespace" type="string">
  The real, unique identifier of this object's own API Gateway cache namespace. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connection_id" type="string">
  The real, unique identifier of the connection this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="connection_type" type="string">
  The real, discriminating type of the connection this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_handling" type="string">
  The real, configured handling for the request or response payload content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="credentials" type="string">
  The real, temporary security credentials issued for this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="integration_responses" type="map(object)">
  The real API Gateway integration responses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="integration_target" type="string">
  The real target this integration invokes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="passthrough_behavior" type="string">
  The real, configured behavior for requests without a matching content type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_parameters" type="map(string)">
  The real, structured parameters of this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="request_templates" type="map(string)">
  The real API Gateway request templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="response_transfer_mode" type="string">
  The real, negotiated transfer mode for this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="timeout_in_millis" type="number">
  The real, maximum duration this operation is allowed to run before timing out, in milliseconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tls_config" type="object">
  &lt;p&gt;Specifies the TLS configuration for an integration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="insecure_skip_verification" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  &lt;p&gt;The integration type. The valid value is &lt;code&gt;HTTP&lt;/code&gt; for integrating an API method with an HTTP backend; &lt;code&gt;AWS&lt;/code&gt; with any Amazon Web Services service endpoints; &lt;code&gt;MOCK&lt;/code&gt; for testing without actually invoking the backend; &lt;code&gt;HTTP_PROXY&lt;/code&gt; for integrating with the HTTP proxy integration; &lt;code&gt;AWS_PROXY&lt;/code&gt; for integrating with the Lambda proxy integration. &lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="uri" type="string">
  The real, resolvable URI of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
