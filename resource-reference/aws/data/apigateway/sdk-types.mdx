---
title: "aws_apigateway_sdk_types"
description: "`aws_apigateway_sdk_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigateway_sdk_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_sdk_types"})

    		ubx.Data(apigateway.SdkTypes, "example", apigateway.SdkTypesConfig{
    			Limit:    1,
    			Position: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SdkTypes } from "@ubx/sdk-aws/aws/data/apigateway/sdk_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_sdk_types" });

      data(SdkTypes, "example", {
        limit: 1,
        position: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import SdkTypes, SdkTypesConfig

    def describe():
        ubx.intent("look up aws_apigateway_sdk_types")

        ubx.data(SdkTypes, "example", SdkTypesConfig(
            limit=1,
            position="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="position" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>
