---
title: "aws_apigateway_method_response"
description: "`data_aws_apigateway_method_response` looks up a real, already-existing `aws_api_gateway_method`."
---

It defines one HTTP method (GET, POST, and similar) on an `aws_api_gateway_resource`, together with its own authorization, request validation, and backend integration, the real unit a REST API caller actually invokes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_method_response"})

    		ubx.Data(apigateway.MethodResponse, "example", apigateway.MethodResponseConfig{
    			HttpMethod: "example",
    			ResourceId: "example",
    			RestApiId:  "example",
    			StatusCode: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MethodResponse } from "./local-sdk/aws/sdk/typescript/aws/data/apigateway/method_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_method_response" });

      data(MethodResponse, "example", {
        httpMethod: "example",
        resourceId: "example",
        restApiId: "example",
        statusCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import MethodResponse, MethodResponseConfig

    def describe():
        ubx.intent("look up aws_apigateway_method_response")

        ubx.data(MethodResponse, "example", MethodResponseConfig(
            http_method="example",
            resource_id="example",
            rest_api_id="example",
            status_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="http_method" type="string" required>
  The real HTTP method this object applies to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The real, unique identifier of the REST API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="status_code" type="string" required>
  &lt;p&gt;The status code.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="response_models" type="map(string)">
  The real response models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="response_parameters" type="map(bool)">
  The real, structured response parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
