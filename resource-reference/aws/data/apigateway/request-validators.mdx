---
title: "aws_apigateway_request_validators"
description: "`data_aws_apigateway_request_validators` looks up a real, already-existing `aws_api_gateway_request_validator`."
---

It attaches to an `aws_api_gateway_method` and rejects malformed requests, missing required parameters or a body that doesn't match its `aws_api_gateway_model`, before they ever reach the backend integration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apigateway "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigateway"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigateway_request_validators"})

    		ubx.Data(apigateway.RequestValidators, "example", apigateway.RequestValidatorsConfig{
    			RestApiId: "example",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RequestValidators } from "./local-sdk/aws/sdk/typescript/aws/data/apigateway/request_validators.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigateway_request_validators" });

      data(RequestValidators, "example", {
        restApiId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apigateway import RequestValidators, RequestValidatorsConfig

    def describe():
        ubx.intent("look up aws_apigateway_request_validators")

        ubx.data(RequestValidators, "example", RequestValidatorsConfig(
            rest_api_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rest_api_id" type="string" required>
  The real, unique identifier of the REST API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="position" type="string">
  The real position of this object within its own ordered sequence. **(AI-inferred)** Returned by the lookup.
</ResponseField>
