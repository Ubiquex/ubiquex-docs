---
title: "aws_codepipeline_action_type"
description: "`aws_codepipeline_action_type` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codepipeline_action_type` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codepipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codepipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codepipeline_action_type"})

    		ubx.Data(codepipeline.ActionType, "example", codepipeline.ActionTypeConfig{
    			Category: "example",
    			Owner:    "example",
    			Provider: "example",
    			Version:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ActionType } from "@ubx/sdk-aws/aws/data/codepipeline/action_type";

    export default stack("example", () => {
      intent({ summary: "look up aws_codepipeline_action_type" });

      data(ActionType, "example", {
        category: "example",
        owner: "example",
        provider: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codepipeline import ActionType, ActionTypeConfig

    def describe():
        ubx.intent("look up aws_codepipeline_action_type")

        ubx.data(ActionType, "example", ActionTypeConfig(
            category="example",
            owner="example",
            provider="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_type" type="object">
  &lt;p&gt;The parameters for the action type definition that are provided when the action type is created or updated.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="executor" type="object" required>
      &lt;p&gt;The action engine, or executor, for an action type created for a provider, where the action is to be used by customers of the provider. The action engine is associated with the model used to create and update the action, such as the Lambda integration model.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="configuration" type="object" required>
          &lt;p&gt;The action engine, or executor, related to the supported integration model used to create and update the action type. The available executor types are &lt;code&gt;Lambda&lt;/code&gt; and &lt;code&gt;JobWorker&lt;/code&gt;.&lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="job_worker_executor_configuration" type="object">
              &lt;p&gt;Details about the polling configuration for the &lt;code&gt;JobWorker&lt;/code&gt; action engine, or executor.&lt;/p&gt; Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="polling_accounts" type="list(string)">
                  Optional lookup argument.
                </ResponseField>
                <ResponseField name="polling_service_principals" type="list(string)">
                  Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="lambda_executor_configuration" type="object">
              &lt;p&gt;Details about the configuration for the &lt;code&gt;Lambda&lt;/code&gt; action engine, or executor.&lt;/p&gt; Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="lambda_function_arn" type="string" required>
                  Required lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="job_timeout" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="policy_statements_template" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="object" required>
      &lt;p&gt;Specifies the category, owner, provider, and version of the action type.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="category" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="owner" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="provider" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="version" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="input_artifact_details" type="object" required>
      &lt;p&gt;Information about parameters for artifacts associated with the action type, such as the minimum and maximum artifacts allowed.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum_count" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="minimum_count" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_artifact_details" type="object" required>
      &lt;p&gt;Information about parameters for artifacts associated with the action type, such as the minimum and maximum artifacts allowed.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="maximum_count" type="number" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="minimum_count" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="permissions" type="object">
      &lt;p&gt;Details identifying the users with permissions to use the action type.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="allowed_accounts" type="list(string)" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="properties" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="urls" type="object">
      &lt;p&gt;Returns information about URLs for web pages that display to customers as links on the pipeline view, such as an external configuration page for the action type.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="configuration_url" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="entity_url_template" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="execution_url_template" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="revision_url_template" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
