---
title: "aws_codepipeline_pipelines"
description: "`aws_codepipeline_pipelines` looks up a real CodePipeline pipeline."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codepipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codepipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codepipeline_pipelines"})

    		ubx.Data(codepipeline.Pipelines, "example", codepipeline.PipelinesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Pipelines } from "@ubx/sdk-aws/aws/data/codepipeline/pipelines";

    export default stack("example", () => {
      intent({ summary: "look up aws_codepipeline_pipelines" });

      data(Pipelines, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codepipeline import Pipelines, PipelinesConfig

    def describe():
        ubx.intent("look up aws_codepipeline_pipelines")

        ubx.data(Pipelines, "example", PipelinesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipelines" type="list(object)">
  The real pipelines included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
