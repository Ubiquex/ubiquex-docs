---
title: "aws_codepipeline_action_types"
description: "`aws_codepipeline_action_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_codepipeline_action_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codepipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codepipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codepipeline_action_types"})

    		ubx.Data(codepipeline.ActionTypes, "example", codepipeline.ActionTypesConfig{
    			ActionOwnerFilter: "example",
    			RegionFilter:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ActionTypes } from "@ubx/sdk-aws/aws/data/codepipeline/action_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_codepipeline_action_types" });

      data(ActionTypes, "example", {
        actionOwnerFilter: "example",
        regionFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codepipeline import ActionTypes, ActionTypesConfig

    def describe():
        ubx.intent("look up aws_codepipeline_action_types")

        ubx.data(ActionTypes, "example", ActionTypesConfig(
            action_owner_filter="example",
            region_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_owner_filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="region_filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_types" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
