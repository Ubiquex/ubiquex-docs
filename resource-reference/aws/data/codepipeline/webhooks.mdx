---
title: "aws_codepipeline_webhooks"
description: "`aws_codepipeline_webhooks` looks up a real CodePipeline webhook, triggering a pipeline from an external source event."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codepipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/codepipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codepipeline_webhooks"})

    		ubx.Data(codepipeline.Webhooks, "example", codepipeline.WebhooksConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Webhooks } from "@ubx/sdk-aws/aws/data/codepipeline/webhooks";

    export default stack("example", () => {
      intent({ summary: "look up aws_codepipeline_webhooks" });

      data(Webhooks, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.codepipeline import Webhooks, WebhooksConfig

    def describe():
        ubx.intent("look up aws_codepipeline_webhooks")

        ubx.data(Webhooks, "example", WebhooksConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="webhooks" type="list(object)">
  The real webhooks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
