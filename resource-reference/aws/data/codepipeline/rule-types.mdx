---
title: "aws_codepipeline_rule_types"
description: "`aws_codepipeline_rule_types` reads the real, available CodePipeline rule types for a stage's own condition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	codepipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/codepipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_codepipeline_rule_types"})

    		ubx.Data(codepipeline.RuleTypes, "example", codepipeline.RuleTypesConfig{
    			RegionFilter:    "example",
    			RuleOwnerFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RuleTypes } from "./local-sdk/aws/sdk/typescript/aws/data/codepipeline/rule_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_codepipeline_rule_types" });

      data(RuleTypes, "example", {
        regionFilter: "example",
        ruleOwnerFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.codepipeline import RuleTypes, RuleTypesConfig

    def describe():
        ubx.intent("look up aws_codepipeline_rule_types")

        ubx.data(RuleTypes, "example", RuleTypesConfig(
            region_filter="example",
            rule_owner_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="region_filter" type="string">
  Restricts results to this AWS region. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rule_owner_filter" type="string">
  Restricts results to rules owned by this identity. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="rule_types" type="list(object)">
  The real, discriminating types of rules this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
