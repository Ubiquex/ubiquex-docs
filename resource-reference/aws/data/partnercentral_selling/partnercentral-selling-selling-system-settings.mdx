---
title: "aws_partnercentral_selling_selling_system_settings"
description: "`aws_partnercentral_selling_selling_system_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_partnercentral_selling_selling_system_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	partnercentral_selling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/partnercentral_selling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_partnercentral_selling_selling_system_settings"})

    		ubx.Data(partnercentral_selling.PartnercentralSellingSellingSystemSettings, "example", partnercentral_selling.PartnercentralSellingSellingSystemSettingsConfig{
    			Catalog: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PartnercentralSellingSellingSystemSettings } from "@ubx/sdk-aws/aws/data/partnercentral_selling/partnercentral_selling_selling_system_settings";

    export default stack("example", () => {
      intent({
        summary: "look up aws_partnercentral_selling_selling_system_settings",
      });

      data(PartnercentralSellingSellingSystemSettings, "example", {
        catalog: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.partnercentral_selling import PartnercentralSellingSellingSystemSettings, PartnercentralSellingSellingSystemSettingsConfig

    def describe():
        ubx.intent("look up aws_partnercentral_selling_selling_system_settings")

        ubx.data(PartnercentralSellingSellingSystemSettings, "example", PartnercentralSellingSellingSystemSettingsConfig(
            catalog="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource_snapshot_job_role_arn" type="string">
  Returned by the lookup.
</ResponseField>
