---
title: "aws_billingconductor_account_associations"
description: "`aws_billingconductor_account_associations` reads the real accounts associated with a Billing Conductor billing group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billingconductor "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/billingconductor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_billingconductor_account_associations"})

    		ubx.Data(billingconductor.AccountAssociations, "example", billingconductor.AccountAssociationsConfig{
    			BillingPeriod: "example",
    			Filters:       map[string]any{"account_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAssociations } from "@ubx/sdk-aws/aws/data/billingconductor/account_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_billingconductor_account_associations" });

      data(AccountAssociations, "example", {
        billingPeriod: "example",
        filters: { accountId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.billingconductor import AccountAssociations, AccountAssociationsConfig

    def describe():
        ubx.intent("look up aws_billingconductor_account_associations")

        ubx.data(AccountAssociations, "example", AccountAssociationsConfig(
            billing_period="example",
            filters={"account_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="billing_period" type="string">
  The real billing period this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;The filter on the account ID of the linked account, or any of the following:&lt;/p&gt; &lt;p&gt; &lt;code&gt;MONITORED&lt;/code&gt;: linked accounts that are associated to billing groups.&lt;/p&gt; &lt;p&gt; &lt;code&gt;UNMONITORED&lt;/code&gt;: linked accounts that are not associated to billing groups.&lt;/p&gt; &lt;p&gt; &lt;code&gt;Billing Group Arn&lt;/code&gt;: linked accounts that are associated to the provided Billing Group Arn.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="account_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="association" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="linked_accounts" type="list(object)">
  The real linked AWS accounts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
