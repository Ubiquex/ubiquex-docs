---
title: "aws_ivschat_logging_configurations"
description: "`data_aws_ivschat_logging_configurations` looks up a real, already-existing `aws_ivschat_logging_configuration`."
---

It streams a real `aws_ivschat_room`'s chat messages to a destination, CloudWatch Logs, Kinesis Data Firehose, or S3, for moderation review or analytics rather than messages existing only transiently in the room.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivschat "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ivschat"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ivschat_logging_configurations"})

    		ubx.Data(ivschat.LoggingConfigurations, "example", ivschat.LoggingConfigurationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoggingConfigurations } from "@ubx/sdk-aws/aws/data/ivschat/logging_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ivschat_logging_configurations" });

      data(LoggingConfigurations, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ivschat import LoggingConfigurations, LoggingConfigurationsConfig

    def describe():
        ubx.intent("look up aws_ivschat_logging_configurations")

        ubx.data(LoggingConfigurations, "example", LoggingConfigurationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="logging_configurations" type="list(object)">
  The real logging configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
