---
title: "aws_glue_entity_records"
description: "`aws_glue_entity_records` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_entity_records` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_entity_records"})

    		ubx.Data(glue.EntityRecords, "example", glue.EntityRecordsConfig{
    			EntityName:          "example-entity-name",
    			Limit:               1,
    			CatalogId:           "example",
    			ConnectionName:      "example-connection-name",
    			ConnectionOptions:   map[string]string{"managed-by": "ubx"},
    			DataStoreApiVersion: "example",
    			FilterPredicate:     "example",
    			OrderBy:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityRecords } from "@ubx/sdk-aws/aws/data/glue/entity_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_entity_records" });

      data(EntityRecords, "example", {
        entityName: "example-entity-name",
        limit: 1,
        catalogId: "example",
        connectionName: "example-connection-name",
        connectionOptions: { "managed-by": "ubx" },
        dataStoreApiVersion: "example",
        filterPredicate: "example",
        orderBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import EntityRecords, EntityRecordsConfig

    def describe():
        ubx.intent("look up aws_glue_entity_records")

        ubx.data(EntityRecords, "example", EntityRecordsConfig(
            entity_name="example-entity-name",
            limit=1,
            catalog_id="example",
            connection_name="example-connection-name",
            connection_options={"managed-by": "ubx"},
            data_store_api_version="example",
            filter_predicate="example",
            order_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="connection_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="connection_options" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="data_store_api_version" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="entity_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_predicate" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="selected_fields" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(any)">
  Returned by the lookup.
</ResponseField>
