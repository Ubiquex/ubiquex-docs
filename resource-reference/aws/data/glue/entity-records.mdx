---
title: "aws_glue_entity_records"
description: "`aws_glue_entity_records` reads the real data records for an entity exposed by a Glue connection's own native API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_entity_records"})

    		ubx.Data(glue.EntityRecords, "example", glue.EntityRecordsConfig{
    			EntityName:          "example-entity-name",
    			Limit:               1,
    			CatalogId:           "example",
    			ConnectionName:      "example-connection-name",
    			ConnectionOptions:   map[string]string{"managed-by": "ubx"},
    			DataStoreApiVersion: "example",
    			FilterPredicate:     "example",
    			OrderBy:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EntityRecords } from "./local-sdk/aws/sdk/typescript/aws/data/glue/entity_records.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_entity_records" });

      data(EntityRecords, "example", {
        entityName: "example-entity-name",
        limit: 1,
        catalogId: "example",
        connectionName: "example-connection-name",
        connectionOptions: { "managed-by": "ubx" },
        dataStoreApiVersion: "example",
        filterPredicate: "example",
        orderBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import EntityRecords, EntityRecordsConfig

    def describe():
        ubx.intent("look up aws_glue_entity_records")

        ubx.data(EntityRecords, "example", EntityRecordsConfig(
            entity_name="example-entity-name",
            limit=1,
            catalog_id="example",
            connection_name="example-connection-name",
            connection_options={"managed-by": "ubx"},
            data_store_api_version="example",
            filter_predicate="example",
            order_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connection_name" type="string">
  The real name of the connection this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connection_options" type="map(string)">
  The real, available options for the connection. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_store_api_version" type="string">
  The real version of the data store api. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="entity_name" type="string" required>
  The real name of the entity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_predicate" type="string">
  The real predicate used to filter this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number" required>
  The maximum number of results to return. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="selected_fields" type="list(string)">
  The real fields selected for this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(any)">
  The real records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
