---
title: "aws_glue_catalog_import_status"
description: "`aws_glue_catalog_import_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_catalog_import_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_catalog_import_status"})

    		ubx.Data(glue.CatalogImportStatus, "example", glue.CatalogImportStatusConfig{
    			CatalogId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogImportStatus } from "@ubx/sdk-aws/aws/data/glue/catalog_import_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_catalog_import_status" });

      data(CatalogImportStatus, "example", {
        catalogId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import CatalogImportStatus, CatalogImportStatusConfig

    def describe():
        ubx.intent("look up aws_glue_catalog_import_status")

        ubx.data(CatalogImportStatus, "example", CatalogImportStatusConfig(
            catalog_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="import_status" type="object">
  &lt;p&gt;A structure containing migration status information.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="import_completed" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="import_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="imported_by" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
