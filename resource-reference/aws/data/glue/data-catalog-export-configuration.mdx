---
title: "aws_glue_data_catalog_export_configuration"
description: "`aws_glue_data_catalog_export_configuration` reads the real configuration for exporting the Glue Data Catalog to an external metastore."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_catalog_export_configuration"})

    		ubx.Data(glue.DataCatalogExportConfiguration, "example", glue.DataCatalogExportConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataCatalogExportConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/glue/data_catalog_export_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_catalog_export_configuration" });

      data(DataCatalogExportConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataCatalogExportConfiguration, DataCatalogExportConfigurationConfig

    def describe():
        ubx.intent("look up aws_glue_data_catalog_export_configuration")

        ubx.data(DataCatalogExportConfiguration, "example", DataCatalogExportConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_configuration" type="object">
  &lt;p&gt;The encryption configuration for exported data catalog metadata.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sse_algorithm" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="export_setting" type="string">
  &lt;p&gt;The export setting for the data catalog.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="s3_table_bucket_arn" type="string">
  The real Amazon Resource Name (ARN) of the s3 table bucket this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;The current status of the data catalog export.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
