---
title: "aws_glue_column_statistics_task_runs"
description: "`aws_glue_column_statistics_task_runs` reads the real runs of a Glue column statistics generation task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_column_statistics_task_runs"})

    		ubx.Data(glue.ColumnStatisticsTaskRuns, "example", glue.ColumnStatisticsTaskRunsConfig{
    			DatabaseName: "example-database-name",
    			TableName:    "example-table-name",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ColumnStatisticsTaskRuns } from "@ubx/sdk-aws/aws/data/glue/column_statistics_task_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_column_statistics_task_runs" });

      data(ColumnStatisticsTaskRuns, "example", {
        databaseName: "example-database-name",
        tableName: "example-table-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import ColumnStatisticsTaskRuns, ColumnStatisticsTaskRunsConfig

    def describe():
        ubx.intent("look up aws_glue_column_statistics_task_runs")

        ubx.data(ColumnStatisticsTaskRuns, "example", ColumnStatisticsTaskRunsConfig(
            database_name="example-database-name",
            table_name="example-table-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="column_statistics_task_runs" type="list(object)">
  The real AWS Glue column statistics task runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
