---
title: "aws_glue_schema_by_definition"
description: "`aws_glue_schema_by_definition` looks up a real, registered schema version in the Glue Schema Registry, by its own definition content."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_schema_by_definition"})

    		ubx.Data(glue.SchemaByDefinition, "example", glue.SchemaByDefinitionConfig{
    			SchemaDefinition: "example",
    			SchemaId:         map[string]any{"registry_name": "example-registry-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SchemaByDefinition } from "./local-sdk/aws/sdk/typescript/aws/data/glue/schema_by_definition.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_schema_by_definition" });

      data(SchemaByDefinition, "example", {
        schemaDefinition: "example",
        schemaId: { registryName: "example-registry-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import SchemaByDefinition, SchemaByDefinitionConfig

    def describe():
        ubx.intent("look up aws_glue_schema_by_definition")

        ubx.data(SchemaByDefinition, "example", SchemaByDefinitionConfig(
            schema_definition="example",
            schema_id={"registry_name": "example-registry-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="schema_definition" type="string" required>
  The real, structured schema definition of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="schema_id" type="object" required>
  &lt;p&gt;The unique ID of the schema in the Glue schema registry.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="registry_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="schema_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="schema_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="created_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_format" type="string">
  The real format of the data. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_arn" type="string">
  The real Amazon Resource Name (ARN) of the schema this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema_version_id" type="string">
  The real, unique identifier of the schema version this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
