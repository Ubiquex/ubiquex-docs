---
title: "aws_glue_iterable_forms"
description: "`aws_glue_iterable_forms` reads a real, repeatable form (a list-type metadata field) within the Glue Data Catalog's business metadata layer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_iterable_forms"})

    		ubx.Data(glue.IterableForms, "example", glue.IterableFormsConfig{
    			AssetIdentifier:  "example",
    			IterableFormName: "example-iterable-form-name",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IterableForms } from "@ubx/sdk-aws/aws/data/glue/iterable_forms";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_iterable_forms" });

      data(IterableForms, "example", {
        assetIdentifier: "example",
        iterableFormName: "example-iterable-form-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import IterableForms, IterableFormsConfig

    def describe():
        ubx.intent("look up aws_glue_iterable_forms")

        ubx.data(IterableForms, "example", IterableFormsConfig(
            asset_identifier="example",
            iterable_form_name="example-iterable-form-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_identifier" type="string" required>
  The real, unique identifier of the asset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="iterable_form_name" type="string" required>
  The real name of the iterable form this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
