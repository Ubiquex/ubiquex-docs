---
title: "aws_glue_data_quality_model"
description: "`aws_glue_data_quality_model` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_data_quality_model` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_quality_model"})

    		ubx.Data(glue.DataQualityModel, "example", glue.DataQualityModelConfig{
    			ProfileId:   "example",
    			StatisticId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQualityModel } from "@ubx/sdk-aws/aws/data/glue/data_quality_model";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_quality_model" });

      data(DataQualityModel, "example", {
        profileId: "example",
        statisticId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataQualityModel, DataQualityModelConfig

    def describe():
        ubx.intent("look up aws_glue_data_quality_model")

        ubx.data(DataQualityModel, "example", DataQualityModelConfig(
            profile_id="example",
            statistic_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="statistic_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="completed_on" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="started_on" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
