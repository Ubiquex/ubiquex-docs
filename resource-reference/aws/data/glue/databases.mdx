---
title: "aws_glue_databases"
description: "`aws_glue_databases` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_databases` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_databases"})

    		ubx.Data(glue.Databases, "example", glue.DatabasesConfig{
    			AttributesToGet:   []string{"example"},
    			CatalogId:         "example",
    			MaxResults:        1,
    			ResourceShareType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Databases } from "@ubx/sdk-aws/aws/data/glue/databases";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_databases" });

      data(Databases, "example", {
        attributesToGet: ["example"],
        catalogId: "example",
        maxResults: 1,
        resourceShareType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Databases, DatabasesConfig

    def describe():
        ubx.intent("look up aws_glue_databases")

        ubx.data(Databases, "example", DatabasesConfig(
            attributes_to_get=["example"],
            catalog_id="example",
            max_results=1,
            resource_share_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attributes_to_get" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_share_type" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="database_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
