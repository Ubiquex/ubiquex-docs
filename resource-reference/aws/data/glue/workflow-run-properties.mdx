---
title: "aws_glue_workflow_run_properties"
description: "`aws_glue_workflow_run_properties` reads the real, run-scoped properties shared between jobs and crawlers within one run of a Glue workflow."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_workflow_run_properties"})

    		ubx.Data(glue.WorkflowRunProperties, "example", glue.WorkflowRunPropertiesConfig{
    			Name:  "example",
    			RunId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { WorkflowRunProperties } from "./local-sdk/aws/sdk/typescript/aws/data/glue/workflow_run_properties.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_workflow_run_properties" });

      data(WorkflowRunProperties, "example", {
        name: "example",
        runId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import WorkflowRunProperties, WorkflowRunPropertiesConfig

    def describe():
        ubx.intent("look up aws_glue_workflow_run_properties")

        ubx.data(WorkflowRunProperties, "example", WorkflowRunPropertiesConfig(
            name="example",
            run_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="run_id" type="string" required>
  The real, unique identifier of the run this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="run_properties" type="map(string)">
  The real, structured properties of this run. **(AI-inferred)** Returned by the lookup.
</ResponseField>
