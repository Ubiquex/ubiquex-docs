---
title: "aws_glue_data_quality_model_result"
description: "`aws_glue_data_quality_model_result` reads a real, computed result from a Glue Data Quality ML anomaly detection model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_quality_model_result"})

    		ubx.Data(glue.DataQualityModelResult, "example", glue.DataQualityModelResultConfig{
    			ProfileId:   "example",
    			StatisticId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataQualityModelResult } from "./local-sdk/aws/sdk/typescript/aws/data/glue/data_quality_model_result.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_quality_model_result" });

      data(DataQualityModelResult, "example", {
        profileId: "example",
        statisticId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataQualityModelResult, DataQualityModelResultConfig

    def describe():
        ubx.intent("look up aws_glue_data_quality_model_result")

        ubx.data(DataQualityModelResult, "example", DataQualityModelResultConfig(
            profile_id="example",
            statistic_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="profile_id" type="string" required>
  The real, unique identifier of the profile this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="statistic_id" type="string" required>
  The real, unique identifier of the statistic this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="completed_on" type="string">
  The real timestamp this object completed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="model" type="list(object)">
  The real model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
