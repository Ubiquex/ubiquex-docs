---
title: "aws_glue_tables"
description: "`aws_glue_tables` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_tables` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_tables"})

    		ubx.Data(glue.Tables, "example", glue.TablesConfig{
    			DatabaseName:         "example-database-name",
    			AttributesToGet:      []string{"example"},
    			AuditContext:         map[string]any{"additional_audit_context": "example"},
    			CatalogId:            "example",
    			Expression:           "example",
    			IncludeStatusDetails: true,
    			MaxResults:           1,
    			QueryAsOfTime:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Tables } from "@ubx/sdk-aws/aws/data/glue/tables";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_tables" });

      data(Tables, "example", {
        databaseName: "example-database-name",
        attributesToGet: ["example"],
        auditContext: { additionalAuditContext: "example" },
        catalogId: "example",
        expression: "example",
        includeStatusDetails: true,
        maxResults: 1,
        queryAsOfTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Tables, TablesConfig

    def describe():
        ubx.intent("look up aws_glue_tables")

        ubx.data(Tables, "example", TablesConfig(
            database_name="example-database-name",
            attributes_to_get=["example"],
            audit_context={"additional_audit_context": "example"},
            catalog_id="example",
            expression="example",
            include_status_details=True,
            max_results=1,
            query_as_of_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attributes_to_get" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="audit_context" type="object">
  &lt;p&gt;A structure containing the Lake Formation audit context.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="additional_audit_context" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="all_columns_requested" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="requested_columns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="expression" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_status_details" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_as_of_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="transaction_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="table_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
