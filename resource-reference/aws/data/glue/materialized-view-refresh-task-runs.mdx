---
title: "aws_glue_materialized_view_refresh_task_runs"
description: "`aws_glue_materialized_view_refresh_task_runs` reads the real runs refreshing a Glue Data Catalog materialized view."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_materialized_view_refresh_task_runs"})

    		ubx.Data(glue.MaterializedViewRefreshTaskRuns, "example", glue.MaterializedViewRefreshTaskRunsConfig{
    			CatalogId:    "example",
    			DatabaseName: "example-database-name",
    			MaxResults:   1,
    			TableName:    "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MaterializedViewRefreshTaskRuns } from "@ubx/sdk-aws/aws/data/glue/materialized_view_refresh_task_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_materialized_view_refresh_task_runs" });

      data(MaterializedViewRefreshTaskRuns, "example", {
        catalogId: "example",
        databaseName: "example-database-name",
        maxResults: 1,
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import MaterializedViewRefreshTaskRuns, MaterializedViewRefreshTaskRunsConfig

    def describe():
        ubx.intent("look up aws_glue_materialized_view_refresh_task_runs")

        ubx.data(MaterializedViewRefreshTaskRuns, "example", MaterializedViewRefreshTaskRunsConfig(
            catalog_id="example",
            database_name="example-database-name",
            max_results=1,
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string" required>
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string">
  The real name of the database this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string">
  The real name of the table this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="materialized_view_refresh_task_runs" type="list(object)">
  The real, refresh task runs for this materialized view. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
