---
title: "aws_glue_inbound_integrations"
description: "`data_aws_glue_inbound_integrations` looks up a real, already-existing `aws_glue_integration`."
---

It defines a real, zero-ETL integration from a source, Aurora, RDS, DynamoDB, to a target like Redshift or OpenSearch, replicating and keeping data in sync without a caller writing or scheduling any ETL job themselves. AWS Glue added CloudFormation and CDK support for zero-ETL integrations in November 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_inbound_integrations"})

    		ubx.Data(glue.InboundIntegrations, "example", glue.InboundIntegrationsConfig{
    			IntegrationArn: "arn:aws:iam::123456789012:policy/example",
    			MaxRecords:     1,
    			TargetArn:      "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InboundIntegrations } from "@ubx/sdk-aws/aws/data/glue/inbound_integrations";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_inbound_integrations" });

      data(InboundIntegrations, "example", {
        integrationArn: "arn:aws:iam::123456789012:policy/example",
        maxRecords: 1,
        targetArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import InboundIntegrations, InboundIntegrationsConfig

    def describe():
        ubx.intent("look up aws_glue_inbound_integrations")

        ubx.data(InboundIntegrations, "example", InboundIntegrationsConfig(
            integration_arn="arn:aws:iam::123456789012:policy/example",
            max_records=1,
            target_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="integration_arn" type="string">
  The real Amazon Resource Name (ARN) of the integration this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_arn" type="string">
  The real Amazon Resource Name (ARN) of the target this action applies to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="inbound_integrations" type="list(object)">
  The real inbound integrations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
