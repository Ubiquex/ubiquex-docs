---
title: "aws_glue_user_defined_functions"
description: "`aws_glue_user_defined_functions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_user_defined_functions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_user_defined_functions"})

    		ubx.Data(glue.UserDefinedFunctions, "example", glue.UserDefinedFunctionsConfig{
    			Pattern:      "example",
    			CatalogId:    "example",
    			DatabaseName: "example-database-name",
    			FunctionType: "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserDefinedFunctions } from "@ubx/sdk-aws/aws/data/glue/user_defined_functions";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_user_defined_functions" });

      data(UserDefinedFunctions, "example", {
        pattern: "example",
        catalogId: "example",
        databaseName: "example-database-name",
        functionType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import UserDefinedFunctions, UserDefinedFunctionsConfig

    def describe():
        ubx.intent("look up aws_glue_user_defined_functions")

        ubx.data(UserDefinedFunctions, "example", UserDefinedFunctionsConfig(
            pattern="example",
            catalog_id="example",
            database_name="example-database-name",
            function_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="function_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="pattern" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_defined_functions" type="list(object)">
  Returned by the lookup.
</ResponseField>
