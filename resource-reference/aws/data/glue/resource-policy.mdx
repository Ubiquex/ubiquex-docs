---
title: "aws_glue_resource_policy"
description: "`aws_glue_resource_policy` reads the real resource-based policy attached to a Glue Data Catalog resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_resource_policy"})

    		ubx.Data(glue.ResourcePolicy, "example", glue.ResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcePolicy } from "@ubx/sdk-aws/aws/data/glue/resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_resource_policy" });

      data(ResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_glue_resource_policy")

        ubx.data(ResourcePolicy, "example", ResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string">
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="create_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_hash" type="string">
  The real, cryptographic hash of this policy. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy_in_json" type="string">
  The real, JSON-formatted policy document. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
