---
title: "aws_glue_mapping"
description: "`aws_glue_mapping` reads a real, suggested schema mapping between a Glue ETL job's own source and target."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_mapping"})

    		ubx.Data(glue.Mapping, "example", glue.MappingConfig{
    			Source:   map[string]any{"database_name": "example-database-name"},
    			Location: map[string]any{"dynamo_db": []map[string]any{{"name": "example-name"}}},
    			Sinks:    []map[string]any{{"database_name": "example-database-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Mapping } from "@ubx/sdk-aws/aws/data/glue/mapping";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_mapping" });

      data(Mapping, "example", {
        source: { databaseName: "example-database-name" },
        location: { dynamoDb: [{ name: "example-name" }] },
        sinks: [{ databaseName: "example-database-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Mapping, MappingConfig

    def describe():
        ubx.intent("look up aws_glue_mapping")

        ubx.data(Mapping, "example", MappingConfig(
            source={"database_name": "example-database-name"},
            location={"dynamo_db": [{"name": "example-name"}]},
            sinks=[{"database_name": "example-database-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="location" type="object">
  &lt;p&gt;The location of resources.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dynamo_db" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="jdbc" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="s3" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sinks" type="list(object)">
  The real sinks included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source" type="object" required>
  &lt;p&gt;Specifies a table definition in the Glue Data Catalog.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="database_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="table_name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="mapping" type="list(object)">
  The real, structured mapping of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
