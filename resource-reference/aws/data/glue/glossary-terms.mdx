---
title: "aws_glue_glossary_terms"
description: "`aws_glue_glossary_terms` reads the real terms defined within a Glue Data Catalog business glossary."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_glossary_terms"})

    		ubx.Data(glue.GlossaryTerms, "example", glue.GlossaryTermsConfig{
    			GlossaryIdentifier: "example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GlossaryTerms } from "@ubx/sdk-aws/aws/data/glue/glossary_terms";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_glossary_terms" });

      data(GlossaryTerms, "example", {
        glossaryIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import GlossaryTerms, GlossaryTermsConfig

    def describe():
        ubx.intent("look up aws_glue_glossary_terms")

        ubx.data(GlossaryTerms, "example", GlossaryTermsConfig(
            glossary_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="glossary_identifier" type="string" required>
  The real, unique identifier of the glossary this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
