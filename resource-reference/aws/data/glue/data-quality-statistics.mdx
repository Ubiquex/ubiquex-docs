---
title: "aws_glue_data_quality_statistics"
description: "`aws_glue_data_quality_statistics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_data_quality_statistics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_quality_statistics"})

    		ubx.Data(glue.DataQualityStatistics, "example", glue.DataQualityStatisticsConfig{
    			MaxResults:      1,
    			ProfileId:       "example",
    			StatisticId:     "example",
    			TimestampFilter: map[string]any{"recorded_after": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQualityStatistics } from "@ubx/sdk-aws/aws/data/glue/data_quality_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_quality_statistics" });

      data(DataQualityStatistics, "example", {
        maxResults: 1,
        profileId: "example",
        statisticId: "example",
        timestampFilter: { recordedAfter: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataQualityStatistics, DataQualityStatisticsConfig

    def describe():
        ubx.intent("look up aws_glue_data_quality_statistics")

        ubx.data(DataQualityStatistics, "example", DataQualityStatisticsConfig(
            max_results=1,
            profile_id="example",
            statistic_id="example",
            timestamp_filter={"recorded_after": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="statistic_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="timestamp_filter" type="object">
  &lt;p&gt;A timestamp filter.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="recorded_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="recorded_before" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="statistics" type="list(object)">
  &lt;p&gt;A list of &lt;code&gt;StatisticSummary&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
</ResponseField>
