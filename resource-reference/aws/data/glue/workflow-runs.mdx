---
title: "aws_glue_workflow_runs"
description: "`data_aws_glue_workflow_runs` looks up a real, already-existing `aws_glue_workflow`."
---

It orchestrates a real, directed graph of Glue jobs, crawlers, and triggers as a single, coordinated ETL pipeline, so dependent steps run in the right order without a separate external scheduler.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_workflow_runs"})

    		ubx.Data(glue.WorkflowRuns, "example", glue.WorkflowRunsConfig{
    			Name:         "example",
    			IncludeGraph: true,
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkflowRuns } from "@ubx/sdk-aws/aws/data/glue/workflow_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_workflow_runs" });

      data(WorkflowRuns, "example", {
        name: "example",
        includeGraph: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import WorkflowRuns, WorkflowRunsConfig

    def describe():
        ubx.intent("look up aws_glue_workflow_runs")

        ubx.data(WorkflowRuns, "example", WorkflowRunsConfig(
            name="example",
            include_graph=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_graph" type="bool">
  Whether the related dependency graph is included in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="runs" type="list(object)">
  The real runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
