---
title: "aws_glue_data_quality_rulesets"
description: "`data_aws_glue_data_quality_rulesets` looks up a real, already-existing `aws_glue_data_quality_ruleset`."
---

It defines a real set of data quality rules, in Glue's own DQDL language, that can be evaluated against a table in an `aws_glue_catalog` to score completeness, accuracy, and consistency.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_quality_rulesets"})

    		ubx.Data(glue.DataQualityRulesets, "example", glue.DataQualityRulesetsConfig{
    			Filter:     map[string]any{"name": "example-name"},
    			MaxResults: 1,
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQualityRulesets } from "@ubx/sdk-aws/aws/data/glue/data_quality_rulesets";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_quality_rulesets" });

      data(DataQualityRulesets, "example", {
        filter: { name: "example-name" },
        maxResults: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataQualityRulesets, DataQualityRulesetsConfig
    from ubx.aws.data.glue.data_quality_rulesets import DataQualityRulesets_Filter

    def describe():
        ubx.intent("look up aws_glue_data_quality_rulesets")

        ubx.data(DataQualityRulesets, "example", DataQualityRulesetsConfig(
            filter=DataQualityRulesets_Filter(name="example-name"),
            max_results=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;The criteria used to filter data quality rulesets.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="created_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_before" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_modified_before" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_table" type="object">
      &lt;p&gt;An object representing an Glue table.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="database_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="table_name" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rulesets" type="list(object)">
  The real rule sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
