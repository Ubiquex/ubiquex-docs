---
title: "aws_glue_column_statistics_for_table"
description: "`aws_glue_column_statistics_for_table` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_column_statistics_for_table` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_column_statistics_for_table"})

    		ubx.Data(glue.ColumnStatisticsForTable, "example", glue.ColumnStatisticsForTableConfig{
    			ColumnNames:  []string{"example"},
    			DatabaseName: "example-database-name",
    			TableName:    "example-table-name",
    			CatalogId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ColumnStatisticsForTable } from "@ubx/sdk-aws/aws/data/glue/column_statistics_for_table";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_column_statistics_for_table" });

      data(ColumnStatisticsForTable, "example", {
        columnNames: ["example"],
        databaseName: "example-database-name",
        tableName: "example-table-name",
        catalogId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import ColumnStatisticsForTable, ColumnStatisticsForTableConfig

    def describe():
        ubx.intent("look up aws_glue_column_statistics_for_table")

        ubx.data(ColumnStatisticsForTable, "example", ColumnStatisticsForTableConfig(
            column_names=["example"],
            database_name="example-database-name",
            table_name="example-table-name",
            catalog_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="column_names" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="column_statistics_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  Returned by the lookup.
</ResponseField>
