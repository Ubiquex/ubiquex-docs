---
title: "aws_glue_column_statistics_for_partition"
description: "`aws_glue_column_statistics_for_partition` reads real, computed column statistics for one partition of a Glue Data Catalog table."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_column_statistics_for_partition"})

    		ubx.Data(glue.ColumnStatisticsForPartition, "example", glue.ColumnStatisticsForPartitionConfig{
    			ColumnNames:     []string{"example"},
    			DatabaseName:    "example-database-name",
    			PartitionValues: []string{"example"},
    			TableName:       "example-table-name",
    			CatalogId:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ColumnStatisticsForPartition } from "@ubx/sdk-aws/aws/data/glue/column_statistics_for_partition";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_column_statistics_for_partition" });

      data(ColumnStatisticsForPartition, "example", {
        columnNames: ["example"],
        databaseName: "example-database-name",
        partitionValues: ["example"],
        tableName: "example-table-name",
        catalogId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import ColumnStatisticsForPartition, ColumnStatisticsForPartitionConfig

    def describe():
        ubx.intent("look up aws_glue_column_statistics_for_partition")

        ubx.data(ColumnStatisticsForPartition, "example", ColumnStatisticsForPartitionConfig(
            column_names=["example"],
            database_name="example-database-name",
            partition_values=["example"],
            table_name="example-table-name",
            catalog_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="column_names" type="list(string)" required>
  The real names of the columns included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="partition_values" type="list(string)" required>
  The real values of the partition keys for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="column_statistics_list" type="list(object)">
  The real column statisticss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(object)">
  The real errors encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>
