---
title: "aws_glue_mltask_runs"
description: "`aws_glue_mltask_runs` reads the real runs of a Glue machine learning transform task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_mltask_runs"})

    		ubx.Data(glue.MltaskRuns, "example", glue.MltaskRunsConfig{
    			TransformId: "example",
    			Filter:      map[string]any{"started_after": "example"},
    			MaxResults:  1,
    			Sort:        map[string]any{"column": "example", "sort_direction": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MltaskRuns } from "@ubx/sdk-aws/aws/data/glue/mltask_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_mltask_runs" });

      data(MltaskRuns, "example", {
        transformId: "example",
        filter: { startedAfter: "example" },
        maxResults: 1,
        sort: { column: "example", sortDirection: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import MltaskRuns, MltaskRunsConfig
    from ubx.aws.data.glue.mltask_runs import MltaskRuns_Filter, MltaskRuns_Sort

    def describe():
        ubx.intent("look up aws_glue_mltask_runs")

        ubx.data(MltaskRuns, "example", MltaskRunsConfig(
            transform_id="example",
            filter=MltaskRuns_Filter(started_after="example"),
            max_results=1,
            sort=MltaskRuns_Sort(column="example", sort_direction="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;The criteria that are used to filter the task runs for the machine learning transform.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="started_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="started_before" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="task_run_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="object">
  &lt;p&gt;The sorting criteria that are used to sort the list of task runs for the machine learning transform.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="column" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="sort_direction" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transform_id" type="string" required>
  The real, unique identifier of the transform this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="task_runs" type="list(object)">
  The real AWS Glue task runs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
