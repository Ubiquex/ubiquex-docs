---
title: "aws_glue_connection_types"
description: "`data_aws_glue_connection_types` looks up a real, already-existing `aws_glue_connection`."
---

It stores the real, actual connection details, JDBC URL, credentials, network placement, an `aws_glue_crawler` or `aws_glue_job` uses to reach a data source outside the Glue Data Catalog itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_connection_types"})

    		ubx.Data(glue.ConnectionTypes, "example", glue.ConnectionTypesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectionTypes } from "@ubx/sdk-aws/aws/data/glue/connection_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_connection_types" });

      data(ConnectionTypes, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import ConnectionTypes, ConnectionTypesConfig

    def describe():
        ubx.intent("look up aws_glue_connection_types")

        ubx.data(ConnectionTypes, "example", ConnectionTypesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connection_types" type="list(object)">
  The real, discriminating types of connections this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
