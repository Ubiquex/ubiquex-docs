---
title: "aws_glue_dataflow_graph"
description: "`aws_glue_dataflow_graph` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_dataflow_graph` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_dataflow_graph"})

    		ubx.Data(glue.DataflowGraph, "example", glue.DataflowGraphConfig{
    			PythonScript: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataflowGraph } from "@ubx/sdk-aws/aws/data/glue/dataflow_graph";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_dataflow_graph" });

      data(DataflowGraph, "example", {
        pythonScript: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataflowGraph, DataflowGraphConfig

    def describe():
        ubx.intent("look up aws_glue_dataflow_graph")

        ubx.data(DataflowGraph, "example", DataflowGraphConfig(
            python_script="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="python_script" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dag_edges" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="dag_nodes" type="list(object)">
  Returned by the lookup.
</ResponseField>
