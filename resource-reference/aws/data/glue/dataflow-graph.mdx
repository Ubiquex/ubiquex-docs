---
title: "aws_glue_dataflow_graph"
description: "`aws_glue_dataflow_graph` reads a real, generated visual dataflow graph for a Glue ETL script."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_dataflow_graph"})

    		ubx.Data(glue.DataflowGraph, "example", glue.DataflowGraphConfig{
    			PythonScript: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataflowGraph } from "./local-sdk/aws/sdk/typescript/aws/data/glue/dataflow_graph.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_dataflow_graph" });

      data(DataflowGraph, "example", {
        pythonScript: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataflowGraph, DataflowGraphConfig

    def describe():
        ubx.intent("look up aws_glue_dataflow_graph")

        ubx.data(DataflowGraph, "example", DataflowGraphConfig(
            python_script="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="python_script" type="string">
  The real Python script content of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dag_edges" type="list(object)">
  The real edges of this directed acyclic graph (DAG). **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dag_nodes" type="list(object)">
  The real nodes of this directed acyclic graph (DAG). **(AI-inferred)** Returned by the lookup.
</ResponseField>
