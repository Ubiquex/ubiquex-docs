---
title: "aws_glue_custom_entity_types"
description: "`aws_glue_custom_entity_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_custom_entity_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_custom_entity_types"})

    		ubx.Data(glue.CustomEntityTypes, "example", glue.CustomEntityTypesConfig{
    			MaxResults: 1,
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomEntityTypes } from "@ubx/sdk-aws/aws/data/glue/custom_entity_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_custom_entity_types" });

      data(CustomEntityTypes, "example", {
        maxResults: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import CustomEntityTypes, CustomEntityTypesConfig

    def describe():
        ubx.intent("look up aws_glue_custom_entity_types")

        ubx.data(CustomEntityTypes, "example", CustomEntityTypesConfig(
            max_results=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_entity_types" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
