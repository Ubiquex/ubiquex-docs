---
title: "aws_glue_entity"
description: "`aws_glue_entity` looks up one real, discoverable entity exposed by a Glue connection's own native API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_entity"})

    		ubx.Data(glue.Entity, "example", glue.EntityConfig{
    			ConnectionName:      "example-connection-name",
    			EntityName:          "example-entity-name",
    			CatalogId:           "example",
    			DataStoreApiVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Entity } from "./local-sdk/aws/sdk/typescript/aws/data/glue/entity.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_entity" });

      data(Entity, "example", {
        connectionName: "example-connection-name",
        entityName: "example-entity-name",
        catalogId: "example",
        dataStoreApiVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Entity, EntityConfig

    def describe():
        ubx.intent("look up aws_glue_entity")

        ubx.data(Entity, "example", EntityConfig(
            connection_name="example-connection-name",
            entity_name="example-entity-name",
            catalog_id="example",
            data_store_api_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connection_name" type="string" required>
  The real name of the connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="data_store_api_version" type="string">
  The real version of the data store api. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="entity_name" type="string" required>
  The real name of the entity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fields" type="list(object)">
  The real fields included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
