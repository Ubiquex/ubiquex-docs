---
title: "aws_glue_asset"
description: "`aws_glue_asset` looks up a real, catalogued data asset within Glue Data Catalog's own Data Quality and lineage features."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_asset"})

    		ubx.Data(glue.Asset, "example", glue.AssetConfig{
    			Identifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Asset } from "@ubx/sdk-aws/aws/data/glue/asset";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_asset" });

      data(Asset, "example", {
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Asset, AssetConfig

    def describe():
        ubx.intent("look up aws_glue_asset")

        ubx.data(Asset, "example", AssetConfig(
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_type_id" type="string">
  The real, unique identifier of the asset type this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="attachments" type="map(object)">
  The real attachments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="forms" type="map(object)">
  The real forms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="glossary_terms" type="list(string)">
  The real DataZone glossary terms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="iterable_forms" type="map(object)">
  The real, iterable forms of this word or phrase. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
