---
title: "aws_glue_form_type"
description: "`aws_glue_form_type` looks up a real form type used to structure metadata in the Glue Data Catalog's business metadata layer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_form_type"})

    		ubx.Data(glue.FormType, "example", glue.FormTypeConfig{
    			Identifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FormType } from "./local-sdk/aws/sdk/typescript/aws/data/glue/form_type.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_form_type" });

      data(FormType, "example", {
        identifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import FormType, FormTypeConfig

    def describe():
        ubx.intent("look up aws_glue_form_type")

        ubx.data(FormType, "example", FormTypeConfig(
            identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schema" type="string">
  The real schema this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
