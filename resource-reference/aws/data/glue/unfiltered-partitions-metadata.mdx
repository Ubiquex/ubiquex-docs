---
title: "aws_glue_unfiltered_partitions_metadata"
description: "`aws_glue_unfiltered_partitions_metadata` reads the real, unfiltered metadata for the partitions of a Glue Data Catalog table..."
---

`aws_glue_unfiltered_partitions_metadata` reads the real, unfiltered metadata for the partitions of a Glue Data Catalog table, bypassing Lake Formation cell-level filtering.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_unfiltered_partitions_metadata"})

    		ubx.Data(glue.UnfilteredPartitionsMetadata, "example", glue.UnfilteredPartitionsMetadataConfig{
    			CatalogId:                "example",
    			DatabaseName:             "example-database-name",
    			SupportedPermissionTypes: []string{"example"},
    			TableName:                "example-table-name",
    			AuditContext:             map[string]any{"additional_audit_context": "example"},
    			Expression:               "example",
    			MaxResults:               1,
    			QuerySessionContext:      map[string]any{"additional_context": map[string]string{"key": "value"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UnfilteredPartitionsMetadata } from "@ubx/sdk-aws/aws/data/glue/unfiltered_partitions_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_unfiltered_partitions_metadata" });

      data(UnfilteredPartitionsMetadata, "example", {
        catalogId: "example",
        databaseName: "example-database-name",
        supportedPermissionTypes: ["example"],
        tableName: "example-table-name",
        auditContext: { additionalAuditContext: "example" },
        expression: "example",
        maxResults: 1,
        querySessionContext: { additionalContext: { key: "value" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import UnfilteredPartitionsMetadata, UnfilteredPartitionsMetadataConfig

    def describe():
        ubx.intent("look up aws_glue_unfiltered_partitions_metadata")

        ubx.data(UnfilteredPartitionsMetadata, "example", UnfilteredPartitionsMetadataConfig(
            catalog_id="example",
            database_name="example-database-name",
            supported_permission_types=["example"],
            table_name="example-table-name",
            audit_context={"additional_audit_context": "example"},
            expression="example",
            max_results=1,
            query_session_context={"additional_context": {"key": "value"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="audit_context" type="object">
  &lt;p&gt;A structure containing the Lake Formation audit context.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="additional_audit_context" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="all_columns_requested" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="requested_columns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="catalog_id" type="string" required>
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  The real name of the database this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="expression" type="string">
  The real expression this request evaluates. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_session_context" type="object">
  &lt;p&gt;A structure used as a protocol between query engines and Lake Formation or Glue. Contains both a Lake Formation generated authorization identifier and information from the request's authorization context.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="additional_context" type="map(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cluster_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="query_authorization_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="query_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="query_start_time" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  The real AWS region this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="segment" type="object">
  &lt;p&gt;Defines a non-overlapping region of a table's partitions, allowing multiple requests to be run in parallel.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="segment_number" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="total_segments" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="supported_permission_types" type="list(string)" required>
  The real permission types supported by this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  The real name of the table this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unfiltered_partitions" type="list(object)">
  The real, unfiltered partitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
