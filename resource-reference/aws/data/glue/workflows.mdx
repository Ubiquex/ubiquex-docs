---
title: "aws_glue_workflows"
description: "`data_aws_glue_workflows` looks up a real, already-existing `aws_glue_workflow`."
---

It orchestrates a real, directed graph of Glue jobs, crawlers, and triggers as a single, coordinated ETL pipeline, so dependent steps run in the right order without a separate external scheduler.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_workflows"})

    		ubx.Data(glue.Workflows, "example", glue.WorkflowsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workflows } from "@ubx/sdk-aws/aws/data/glue/workflows";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_workflows" });

      data(Workflows, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Workflows, WorkflowsConfig

    def describe():
        ubx.intent("look up aws_glue_workflows")

        ubx.data(Workflows, "example", WorkflowsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workflows" type="list(string)">
  The real workflows included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
