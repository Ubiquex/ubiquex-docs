---
title: "aws_glue_entities"
description: "`aws_glue_entities` reads the real, discoverable entities exposed by a Glue connection's own native API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_entities"})

    		ubx.Data(glue.Entities, "example", glue.EntitiesConfig{
    			CatalogId:           "example",
    			ConnectionName:      "example-connection-name",
    			DataStoreApiVersion: "example",
    			ParentEntityName:    "example-parent-entity-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Entities } from "@ubx/sdk-aws/aws/data/glue/entities";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_entities" });

      data(Entities, "example", {
        catalogId: "example",
        connectionName: "example-connection-name",
        dataStoreApiVersion: "example",
        parentEntityName: "example-parent-entity-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Entities, EntitiesConfig

    def describe():
        ubx.intent("look up aws_glue_entities")

        ubx.data(Entities, "example", EntitiesConfig(
            catalog_id="example",
            connection_name="example-connection-name",
            data_store_api_version="example",
            parent_entity_name="example-parent-entity-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="connection_name" type="string">
  The real name of the connection this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="data_store_api_version" type="string">
  The real version of the data store api. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parent_entity_name" type="string">
  The real name of the parent entity this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entities" type="list(object)">
  The real entities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
