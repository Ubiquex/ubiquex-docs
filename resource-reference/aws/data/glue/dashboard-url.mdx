---
title: "aws_glue_dashboard_url"
description: "`aws_glue_dashboard_url` reads a real, generated URL for the AWS Glue console dashboard."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_dashboard_url"})

    		ubx.Data(glue.DashboardUrl, "example", glue.DashboardUrlConfig{
    			ResourceId:    "example",
    			ResourceType:  "example",
    			RequestOrigin: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardUrl } from "@ubx/sdk-aws/aws/data/glue/dashboard_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_dashboard_url" });

      data(DashboardUrl, "example", {
        resourceId: "example",
        resourceType: "example",
        requestOrigin: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DashboardUrl, DashboardUrlConfig

    def describe():
        ubx.intent("look up aws_glue_dashboard_url")

        ubx.data(DashboardUrl, "example", DashboardUrlConfig(
            resource_id="example",
            resource_type="example",
            request_origin="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="request_origin" type="string">
  The real origin (source) of this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="url" type="string">
  The real, resolvable URL of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
