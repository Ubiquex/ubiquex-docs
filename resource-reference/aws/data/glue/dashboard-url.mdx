---
title: "aws_glue_dashboard_url"
description: "`aws_glue_dashboard_url` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_dashboard_url` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_dashboard_url"})

    		ubx.Data(glue.DashboardUrl, "example", glue.DashboardUrlConfig{
    			ResourceId:    "example",
    			ResourceType:  "example",
    			RequestOrigin: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardUrl } from "@ubx/sdk-aws/aws/data/glue/dashboard_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_dashboard_url" });

      data(DashboardUrl, "example", {
        resourceId: "example",
        resourceType: "example",
        requestOrigin: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DashboardUrl, DashboardUrlConfig

    def describe():
        ubx.intent("look up aws_glue_dashboard_url")

        ubx.data(DashboardUrl, "example", DashboardUrlConfig(
            resource_id="example",
            resource_type="example",
            request_origin="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="request_origin" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="url" type="string">
  Returned by the lookup.
</ResponseField>
