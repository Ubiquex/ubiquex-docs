---
title: "aws_glue_statements"
description: "`aws_glue_statements` reads the real, submitted statements within a Glue interactive session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_statements"})

    		ubx.Data(glue.Statements, "example", glue.StatementsConfig{
    			SessionId:     "example",
    			RequestOrigin: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Statements } from "./local-sdk/aws/sdk/typescript/aws/data/glue/statements.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_statements" });

      data(Statements, "example", {
        sessionId: "example",
        requestOrigin: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import Statements, StatementsConfig

    def describe():
        ubx.intent("look up aws_glue_statements")

        ubx.data(Statements, "example", StatementsConfig(
            session_id="example",
            request_origin="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="request_origin" type="string">
  The real origin (source) of this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="statements" type="list(object)">
  The real statements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
