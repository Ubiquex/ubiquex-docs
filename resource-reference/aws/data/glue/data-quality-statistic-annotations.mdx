---
title: "aws_glue_data_quality_statistic_annotations"
description: "`aws_glue_data_quality_statistic_annotations` reads a real, human annotation (normal or anomalous) applied to a Glue Data Quality statistic."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_quality_statistic_annotations"})

    		ubx.Data(glue.DataQualityStatisticAnnotations, "example", glue.DataQualityStatisticAnnotationsConfig{
    			MaxResults:      1,
    			ProfileId:       "example",
    			StatisticId:     "example",
    			TimestampFilter: map[string]any{"recorded_after": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DataQualityStatisticAnnotations } from "./local-sdk/aws/sdk/typescript/aws/data/glue/data_quality_statistic_annotations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_quality_statistic_annotations" });

      data(DataQualityStatisticAnnotations, "example", {
        maxResults: 1,
        profileId: "example",
        statisticId: "example",
        timestampFilter: { recordedAfter: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataQualityStatisticAnnotations, DataQualityStatisticAnnotationsConfig

    def describe():
        ubx.intent("look up aws_glue_data_quality_statistic_annotations")

        ubx.data(DataQualityStatisticAnnotations, "example", DataQualityStatisticAnnotationsConfig(
            max_results=1,
            profile_id="example",
            statistic_id="example",
            timestamp_filter={"recorded_after": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="profile_id" type="string">
  The real, unique identifier of the profile this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="statistic_id" type="string">
  The real, unique identifier of the statistic this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="timestamp_filter" type="object">
  &lt;p&gt;A timestamp filter.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="recorded_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="recorded_before" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="annotations" type="list(object)">
  The real annotations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
