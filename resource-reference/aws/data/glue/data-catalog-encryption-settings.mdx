---
title: "aws_glue_data_catalog_encryption_settings"
description: "`data_aws_glue_data_catalog_encryption_settings` looks up a real, already-existing `aws_glue_data_catalog_encryption_settings`."
---

It configures real, account-wide encryption defaults, at-rest KMS encryption and password encryption, for every table and connection in an `aws_glue_catalog`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data catalog encryption settings"})

    		ubx.Resource(glue.DataCatalogEncryptionSettings, "example", glue.DataCatalogEncryptionSettingsConfig{
    			CatalogId:                     "example",
    			DataCatalogEncryptionSettings: map[string]any{"connection_password_encryption": map[string]any{"kms_key_id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataCatalogEncryptionSettings } from "@ubx/sdk-aws/aws/glue/data_catalog_encryption_settings";

    export default stack("example", () => {
      intent({ summary: "example own data catalog encryption settings" });

      resource(DataCatalogEncryptionSettings, "example", {
        catalogId: "example",
        dataCatalogEncryptionSettings: {
          connectionPasswordEncryption: { kmsKeyId: "example" },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import DataCatalogEncryptionSettings, DataCatalogEncryptionSettingsConfig
    from ubx.aws.glue.data_catalog_encryption_settings import DataCatalogEncryptionSettings_DataCatalogEncryptionSettings, DataCatalogEncryptionSettings_DataCatalogEncryptionSettings_ConnectionPasswordEncryption

    def describe():
        ubx.intent("example own data catalog encryption settings")

        ubx.resource(DataCatalogEncryptionSettings, "example", DataCatalogEncryptionSettingsConfig(
            catalog_id="example",
            data_catalog_encryption_settings=DataCatalogEncryptionSettings_DataCatalogEncryptionSettings(connection_password_encryption=DataCatalogEncryptionSettings_DataCatalogEncryptionSettings_ConnectionPasswordEncryption(kms_key_id="example")),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_data_catalog_encryption_settings, with catalog id
    example, data catalog encryption settings a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  The real, unique identifier of the catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_catalog_encryption_settings" type="object">
  &lt;p&gt;Contains configuration information for maintaining Data Catalog security.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="connection_password_encryption" type="object">
      &lt;p&gt;The data structure used by the Data Catalog to encrypt the password as part of &lt;code&gt;CreateConnection&lt;/code&gt; or &lt;code&gt;UpdateConnection&lt;/code&gt; and store it in the &lt;code&gt;ENCRYPTED_PASSWORD&lt;/code&gt; field in the connection properties. You can enable catalog encryption or only password encryption.&lt;/p&gt; &lt;p&gt;When a &lt;code&gt;CreationConnection&lt;/code&gt; request arrives containing a password, the Data Catalog first encrypts the password using your KMS key. It then encrypts the whole connection object again if catalog encryption is also enabled.&lt;/p&gt; &lt;p&gt;This encryption requires that you set KMS key permissions to enable or restrict access on the password key according to your security requirements. For example, you might want only administrators to have decrypt permission on the password key.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aws_kms_key_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="return_connection_password_encrypted" type="bool" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encryption_at_rest" type="object">
      &lt;p&gt;Specifies the encryption-at-rest configuration for the Data Catalog.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_encryption_mode" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="catalog_encryption_service_role" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="sse_aws_kms_key_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
