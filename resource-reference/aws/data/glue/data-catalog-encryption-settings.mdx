---
title: "aws_glue_data_catalog_encryption_settings"
description: "`aws_glue_data_catalog_encryption_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glue_data_catalog_encryption_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glue_data_catalog_encryption_settings"})

    		ubx.Data(glue.DataCatalogEncryptionSettings, "example", glue.DataCatalogEncryptionSettingsConfig{
    			CatalogId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataCatalogEncryptionSettings } from "@ubx/sdk-aws/aws/data/glue/data_catalog_encryption_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_glue_data_catalog_encryption_settings" });

      data(DataCatalogEncryptionSettings, "example", {
        catalogId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glue import DataCatalogEncryptionSettings, DataCatalogEncryptionSettingsConfig

    def describe():
        ubx.intent("look up aws_glue_data_catalog_encryption_settings")

        ubx.data(DataCatalogEncryptionSettings, "example", DataCatalogEncryptionSettingsConfig(
            catalog_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_catalog_encryption_settings" type="object">
  &lt;p&gt;Contains configuration information for maintaining Data Catalog security.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="connection_password_encryption" type="object">
      &lt;p&gt;The data structure used by the Data Catalog to encrypt the password as part of &lt;code&gt;CreateConnection&lt;/code&gt; or &lt;code&gt;UpdateConnection&lt;/code&gt; and store it in the &lt;code&gt;ENCRYPTED_PASSWORD&lt;/code&gt; field in the connection properties. You can enable catalog encryption or only password encryption.&lt;/p&gt; &lt;p&gt;When a &lt;code&gt;CreationConnection&lt;/code&gt; request arrives containing a password, the Data Catalog first encrypts the password using your KMS key. It then encrypts the whole connection object again if catalog encryption is also enabled.&lt;/p&gt; &lt;p&gt;This encryption requires that you set KMS key permissions to enable or restrict access on the password key according to your security requirements. For example, you might want only administrators to have decrypt permission on the password key.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aws_kms_key_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="return_connection_password_encrypted" type="bool" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encryption_at_rest" type="object">
      &lt;p&gt;Specifies the encryption-at-rest configuration for the Data Catalog.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="catalog_encryption_mode" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="catalog_encryption_service_role" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="sse_aws_kms_key_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
