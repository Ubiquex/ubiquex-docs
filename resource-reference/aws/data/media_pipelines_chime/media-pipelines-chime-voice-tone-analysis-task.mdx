---
title: "aws_media_pipelines_chime_voice_tone_analysis_task"
description: "`aws_media_pipelines_chime_voice_tone_analysis_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_media_pipelines_chime_voice_tone_analysis_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	media_pipelines_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/media_pipelines_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_media_pipelines_chime_voice_tone_analysis_task"})

    		ubx.Data(media_pipelines_chime.MediaPipelinesChimeVoiceToneAnalysisTask, "example", media_pipelines_chime.MediaPipelinesChimeVoiceToneAnalysisTaskConfig{
    			Identifier:              "example",
    			VoiceToneAnalysisTaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediaPipelinesChimeVoiceToneAnalysisTask } from "@ubx/sdk-aws/aws/data/media_pipelines_chime/media_pipelines_chime_voice_tone_analysis_task";

    export default stack("example", () => {
      intent({
        summary: "look up aws_media_pipelines_chime_voice_tone_analysis_task",
      });

      data(MediaPipelinesChimeVoiceToneAnalysisTask, "example", {
        identifier: "example",
        voiceToneAnalysisTaskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.media_pipelines_chime import MediaPipelinesChimeVoiceToneAnalysisTask, MediaPipelinesChimeVoiceToneAnalysisTaskConfig

    def describe():
        ubx.intent("look up aws_media_pipelines_chime_voice_tone_analysis_task")

        ubx.data(MediaPipelinesChimeVoiceToneAnalysisTask, "example", MediaPipelinesChimeVoiceToneAnalysisTaskConfig(
            identifier="example",
            voice_tone_analysis_task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="voice_tone_analysis_task_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="voice_tone_analysis_task" type="object">
  &lt;p&gt;A representation of an asynchronous request to perform voice tone analysis on a media insights pipeline.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="updated_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="voice_tone_analysis_task_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="voice_tone_analysis_task_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
