---
title: "aws_media_pipelines_chime_speaker_search_task"
description: "`aws_media_pipelines_chime_speaker_search_task` reads the real, current status of a Chime SDK media pipeline speaker search task."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	media_pipelines_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/media_pipelines_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_media_pipelines_chime_speaker_search_task"})

    		ubx.Data(media_pipelines_chime.MediaPipelinesChimeSpeakerSearchTask, "example", media_pipelines_chime.MediaPipelinesChimeSpeakerSearchTaskConfig{
    			Identifier:          "example",
    			SpeakerSearchTaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MediaPipelinesChimeSpeakerSearchTask } from "@ubx/sdk-aws/aws/data/media_pipelines_chime/media_pipelines_chime_speaker_search_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_media_pipelines_chime_speaker_search_task" });

      data(MediaPipelinesChimeSpeakerSearchTask, "example", {
        identifier: "example",
        speakerSearchTaskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.media_pipelines_chime import MediaPipelinesChimeSpeakerSearchTask, MediaPipelinesChimeSpeakerSearchTaskConfig

    def describe():
        ubx.intent("look up aws_media_pipelines_chime_speaker_search_task")

        ubx.data(MediaPipelinesChimeSpeakerSearchTask, "example", MediaPipelinesChimeSpeakerSearchTaskConfig(
            identifier="example",
            speaker_search_task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="speaker_search_task_id" type="string" required>
  The real, unique identifier of the speaker search task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="speaker_search_task" type="object">
  &lt;p&gt;A representation of an asynchronous request to perform speaker search analysis on a media insights pipeline.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="speaker_search_task_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="speaker_search_task_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="updated_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
