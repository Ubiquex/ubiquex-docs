---
title: "aws_media_pipelines_chime_media_pipeline_kinesis_video_stream_pools"
description: "`aws_media_pipelines_chime_media_pipeline_kinesis_video_stream_pools` looks up a real Kinesis Video Stream pool used by a Chime SDK media pipeline."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media_pipelines_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/media_pipelines_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_media_pipelines_chime_media_pipeline_kinesis_video_stream_pools"})

    		ubx.Data(media_pipelines_chime.MediaPipelinesChimeMediaPipelineKinesisVideoStreamPools, "example", media_pipelines_chime.MediaPipelinesChimeMediaPipelineKinesisVideoStreamPoolsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MediaPipelinesChimeMediaPipelineKinesisVideoStreamPools } from "./local-sdk/aws/sdk/typescript/aws/data/media_pipelines_chime/media_pipelines_chime_media_pipeline_kinesis_video_stream_pools.ts";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_media_pipelines_chime_media_pipeline_kinesis_video_stream_pools",
      });

      data(MediaPipelinesChimeMediaPipelineKinesisVideoStreamPools, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.media_pipelines_chime import MediaPipelinesChimeMediaPipelineKinesisVideoStreamPools, MediaPipelinesChimeMediaPipelineKinesisVideoStreamPoolsConfig

    def describe():
        ubx.intent("look up aws_media_pipelines_chime_media_pipeline_kinesis_video_stream_pools")

        ubx.data(MediaPipelinesChimeMediaPipelineKinesisVideoStreamPools, "example", MediaPipelinesChimeMediaPipelineKinesisVideoStreamPoolsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="kinesis_video_stream_pools" type="list(object)">
  The real Kinesis Video Stream pools included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
