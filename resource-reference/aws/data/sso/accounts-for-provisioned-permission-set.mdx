---
title: "aws_sso_accounts_for_provisioned_permission_set"
description: "`aws_sso_accounts_for_provisioned_permission_set` reads the real accounts an IAM Identity Center permission set has been provisioned to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_accounts_for_provisioned_permission_set"})

    		ubx.Data(sso.AccountsForProvisionedPermissionSet, "example", sso.AccountsForProvisionedPermissionSetConfig{
    			InstanceArn:        "arn:aws:iam::123456789012:policy/example",
    			PermissionSetArn:   "arn:aws:iam::123456789012:policy/example",
    			MaxResults:         1,
    			ProvisioningStatus: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountsForProvisionedPermissionSet } from "@ubx/sdk-aws/aws/data/sso/accounts_for_provisioned_permission_set";

    export default stack("example", () => {
      intent({
        summary: "look up aws_sso_accounts_for_provisioned_permission_set",
      });

      data(AccountsForProvisionedPermissionSet, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        permissionSetArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        provisioningStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import AccountsForProvisionedPermissionSet, AccountsForProvisionedPermissionSetConfig

    def describe():
        ubx.intent("look up aws_sso_accounts_for_provisioned_permission_set")

        ubx.data(AccountsForProvisionedPermissionSet, "example", AccountsForProvisionedPermissionSetConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            permission_set_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            provisioning_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="permission_set_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the permission set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provisioning_status" type="string">
  The real, current status of the provisioning this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_ids" type="list(string)">
  The real, unique identifiers of the AWS accounts this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
