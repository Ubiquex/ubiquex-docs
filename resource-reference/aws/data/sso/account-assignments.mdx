---
title: "aws_sso_account_assignments"
description: "`aws_sso_account_assignments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sso_account_assignments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_account_assignments"})

    		ubx.Data(sso.AccountAssignments, "example", sso.AccountAssignmentsConfig{
    			AccountId:        "example",
    			InstanceArn:      "arn:aws:iam::123456789012:policy/example",
    			PermissionSetArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAssignments } from "@ubx/sdk-aws/aws/data/sso/account_assignments";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_account_assignments" });

      data(AccountAssignments, "example", {
        accountId: "example",
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        permissionSetArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import AccountAssignments, AccountAssignmentsConfig

    def describe():
        ubx.intent("look up aws_sso_account_assignments")

        ubx.data(AccountAssignments, "example", AccountAssignmentsConfig(
            account_id="example",
            instance_arn="arn:aws:iam::123456789012:policy/example",
            permission_set_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="permission_set_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_assignments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
