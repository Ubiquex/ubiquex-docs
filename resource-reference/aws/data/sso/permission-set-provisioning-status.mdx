---
title: "aws_sso_permission_set_provisioning_status"
description: "`data_aws_sso_permission_set_provisioning_status` looks up a real, already-existing `aws_sso_permission_set`."
---

It defines a real, reusable collection of IAM policies, managed, inline, and a permissions boundary, that an IAM Identity Center instance provisions as an assumable IAM role in each AWS account a user or group is assigned access to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_permission_set_provisioning_status"})

    		ubx.Data(sso.PermissionSetProvisioningStatus, "example", sso.PermissionSetProvisioningStatusConfig{
    			InstanceArn:                     "arn:aws:iam::123456789012:policy/example",
    			ProvisionPermissionSetRequestId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PermissionSetProvisioningStatus } from "@ubx/sdk-aws/aws/data/sso/permission_set_provisioning_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_permission_set_provisioning_status" });

      data(PermissionSetProvisioningStatus, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        provisionPermissionSetRequestId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import PermissionSetProvisioningStatus, PermissionSetProvisioningStatusConfig

    def describe():
        ubx.intent("look up aws_sso_permission_set_provisioning_status")

        ubx.data(PermissionSetProvisioningStatus, "example", PermissionSetProvisioningStatusConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            provision_permission_set_request_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provision_permission_set_request_id" type="string" required>
  The real, unique identifier of the provision permission set request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="permission_set_provisioning_status" type="object">
  &lt;p&gt;A structure that is used to provide the status of the provisioning operation for a specified permission set.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="failure_reason" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="permission_set_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="request_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
