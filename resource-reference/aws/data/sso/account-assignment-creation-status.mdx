---
title: "aws_sso_account_assignment_creation_status"
description: "`aws_sso_account_assignment_creation_status` reads the real, current status of assigning an IAM Identity Center permission set to an account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_account_assignment_creation_status"})

    		ubx.Data(sso.AccountAssignmentCreationStatus, "example", sso.AccountAssignmentCreationStatusConfig{
    			InstanceArn: "arn:aws:iam::123456789012:policy/example",
    			Filter:      map[string]any{"status": "example"},
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountAssignmentCreationStatus } from "./local-sdk/aws/sdk/typescript/aws/data/sso/account_assignment_creation_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_account_assignment_creation_status" });

      data(AccountAssignmentCreationStatus, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        filter: { status: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sso import AccountAssignmentCreationStatus, AccountAssignmentCreationStatusConfig

    def describe():
        ubx.intent("look up aws_sso_account_assignment_creation_status")

        ubx.data(AccountAssignmentCreationStatus, "example", AccountAssignmentCreationStatusConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            filter={"status": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;Filters the operation status list based on the passed attribute value.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_assignments_creation_status" type="list(object)">
  The real, current status of the account assignments creation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
