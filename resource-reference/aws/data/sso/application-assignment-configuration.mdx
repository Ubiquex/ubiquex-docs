---
title: "aws_sso_application_assignment_configuration"
description: "`aws_sso_application_assignment_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sso_application_assignment_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_application_assignment_configuration"})

    		ubx.Data(sso.ApplicationAssignmentConfiguration, "example", sso.ApplicationAssignmentConfigurationConfig{
    			ApplicationArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApplicationAssignmentConfiguration } from "@ubx/sdk-aws/aws/data/sso/application_assignment_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_application_assignment_configuration" });

      data(ApplicationAssignmentConfiguration, "example", {
        applicationArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import ApplicationAssignmentConfiguration, ApplicationAssignmentConfigurationConfig

    def describe():
        ubx.intent("look up aws_sso_application_assignment_configuration")

        ubx.data(ApplicationAssignmentConfiguration, "example", ApplicationAssignmentConfigurationConfig(
            application_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assignment_required" type="bool">
  Returned by the lookup.
</ResponseField>
