---
title: "aws_sso_application_session_configuration"
description: "`aws_sso_application_session_configuration` reads the real session configuration (sign-in and sign-out redirects) of an IAM Identity Center application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_application_session_configuration"})

    		ubx.Data(sso.ApplicationSessionConfiguration, "example", sso.ApplicationSessionConfigurationConfig{
    			ApplicationArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationSessionConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/sso/application_session_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_application_session_configuration" });

      data(ApplicationSessionConfiguration, "example", {
        applicationArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sso import ApplicationSessionConfiguration, ApplicationSessionConfigurationConfig

    def describe():
        ubx.intent("look up aws_sso_application_session_configuration")

        ubx.data(ApplicationSessionConfiguration, "example", ApplicationSessionConfigurationConfig(
            application_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="user_background_session_application_status" type="string">
  The real, current status of the user background session application this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
