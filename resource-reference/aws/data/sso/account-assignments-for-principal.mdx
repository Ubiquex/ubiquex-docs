---
title: "aws_sso_account_assignments_for_principal"
description: "`aws_sso_account_assignments_for_principal` reads the real account assignments granted to a given IAM Identity Center user or group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_account_assignments_for_principal"})

    		ubx.Data(sso.AccountAssignmentsForPrincipal, "example", sso.AccountAssignmentsForPrincipalConfig{
    			InstanceArn:   "arn:aws:iam::123456789012:policy/example",
    			PrincipalId:   "example",
    			PrincipalType: "example",
    			Filter:        map[string]any{"account_id": "example"},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountAssignmentsForPrincipal } from "./local-sdk/aws/sdk/typescript/aws/data/sso/account_assignments_for_principal.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_account_assignments_for_principal" });

      data(AccountAssignmentsForPrincipal, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        principalId: "example",
        principalType: "example",
        filter: { accountId: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sso import AccountAssignmentsForPrincipal, AccountAssignmentsForPrincipalConfig

    def describe():
        ubx.intent("look up aws_sso_account_assignments_for_principal")

        ubx.data(AccountAssignmentsForPrincipal, "example", AccountAssignmentsForPrincipalConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            principal_id="example",
            principal_type="example",
            filter={"account_id": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;A structure that describes a filter for account assignments.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  The real, unique identifier of the principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="principal_type" type="string" required>
  The real, discriminating type of the principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_assignments" type="list(object)">
  The real IAM Identity Center account assignments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
