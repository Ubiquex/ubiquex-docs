---
title: "aws_sso_customer_managed_policy_references_in_permission_set"
description: "`aws_sso_customer_managed_policy_references_in_permission_set` reads the real, customer-managed IAM policies attached to an IAM Identity Center..."
---

`aws_sso_customer_managed_policy_references_in_permission_set` reads the real, customer-managed IAM policies attached to an IAM Identity Center permission set.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_customer_managed_policy_references_in_permission_set"})

    		ubx.Data(sso.CustomerManagedPolicyReferencesInPermissionSet, "example", sso.CustomerManagedPolicyReferencesInPermissionSetConfig{
    			InstanceArn:      "arn:aws:iam::123456789012:policy/example",
    			PermissionSetArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomerManagedPolicyReferencesInPermissionSet } from "@ubx/sdk-aws/aws/data/sso/customer_managed_policy_references_in_permission_set";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_sso_customer_managed_policy_references_in_permission_set",
      });

      data(CustomerManagedPolicyReferencesInPermissionSet, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        permissionSetArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import CustomerManagedPolicyReferencesInPermissionSet, CustomerManagedPolicyReferencesInPermissionSetConfig

    def describe():
        ubx.intent("look up aws_sso_customer_managed_policy_references_in_permission_set")

        ubx.data(CustomerManagedPolicyReferencesInPermissionSet, "example", CustomerManagedPolicyReferencesInPermissionSetConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            permission_set_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="permission_set_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the permission set this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="customer_managed_policy_references" type="list(object)">
  The real, customer-managed IAM policies referenced by this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
