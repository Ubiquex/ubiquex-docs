---
title: "aws_sso_permission_sets_provisioned_to_account"
description: "`aws_sso_permission_sets_provisioned_to_account` reads the real IAM Identity Center permission sets provisioned to a given account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_permission_sets_provisioned_to_account"})

    		ubx.Data(sso.PermissionSetsProvisionedToAccount, "example", sso.PermissionSetsProvisionedToAccountConfig{
    			AccountId:          "example",
    			InstanceArn:        "arn:aws:iam::123456789012:policy/example",
    			MaxResults:         1,
    			ProvisioningStatus: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PermissionSetsProvisionedToAccount } from "./local-sdk/aws/sdk/typescript/aws/data/sso/permission_sets_provisioned_to_account.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_permission_sets_provisioned_to_account" });

      data(PermissionSetsProvisionedToAccount, "example", {
        accountId: "example",
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        provisioningStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sso import PermissionSetsProvisionedToAccount, PermissionSetsProvisionedToAccountConfig

    def describe():
        ubx.intent("look up aws_sso_permission_sets_provisioned_to_account")

        ubx.data(PermissionSetsProvisionedToAccount, "example", PermissionSetsProvisionedToAccountConfig(
            account_id="example",
            instance_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            provisioning_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioning_status" type="string">
  The real, current status of the provisioning this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permission_sets" type="list(string)">
  The real permission sets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
