---
title: "aws_sso_account_assignment_deletion_status"
description: "`aws_sso_account_assignment_deletion_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sso_account_assignment_deletion_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_account_assignment_deletion_status"})

    		ubx.Data(sso.AccountAssignmentDeletionStatus, "example", sso.AccountAssignmentDeletionStatusConfig{
    			AccountAssignmentDeletionRequestId: "example",
    			InstanceArn:                        "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountAssignmentDeletionStatus } from "@ubx/sdk-aws/aws/data/sso/account_assignment_deletion_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_account_assignment_deletion_status" });

      data(AccountAssignmentDeletionStatus, "example", {
        accountAssignmentDeletionRequestId: "example",
        instanceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import AccountAssignmentDeletionStatus, AccountAssignmentDeletionStatusConfig

    def describe():
        ubx.intent("look up aws_sso_account_assignment_deletion_status")

        ubx.data(AccountAssignmentDeletionStatus, "example", AccountAssignmentDeletionStatusConfig(
            account_assignment_deletion_request_id="example",
            instance_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_assignment_deletion_request_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_assignment_deletion_status" type="object">
  &lt;p&gt;The status of the creation or deletion operation of an assignment that a principal needs to access an account.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="failure_reason" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="permission_set_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="principal_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="principal_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="request_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="target_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
