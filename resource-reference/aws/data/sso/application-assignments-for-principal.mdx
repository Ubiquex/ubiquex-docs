---
title: "aws_sso_application_assignments_for_principal"
description: "`data_aws_sso_application_assignments_for_principal` looks up a real, already-existing `aws_sso_application_assignment`."
---

It grants a real user or group access to an `aws_sso_application`, the resource that actually authorizes that identity to sign in to the customer-managed application through IAM Identity Center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_application_assignments_for_principal"})

    		ubx.Data(sso.ApplicationAssignmentsForPrincipal, "example", sso.ApplicationAssignmentsForPrincipalConfig{
    			InstanceArn:   "arn:aws:iam::123456789012:policy/example",
    			PrincipalId:   "example",
    			PrincipalType: "example",
    			Filter:        map[string]any{"application_arn": "example"},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApplicationAssignmentsForPrincipal } from "./local-sdk/aws/sdk/typescript/aws/data/sso/application_assignments_for_principal.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_application_assignments_for_principal" });

      data(ApplicationAssignmentsForPrincipal, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        principalId: "example",
        principalType: "example",
        filter: { applicationArn: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sso import ApplicationAssignmentsForPrincipal, ApplicationAssignmentsForPrincipalConfig

    def describe():
        ubx.intent("look up aws_sso_application_assignments_for_principal")

        ubx.data(ApplicationAssignmentsForPrincipal, "example", ApplicationAssignmentsForPrincipalConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            principal_id="example",
            principal_type="example",
            filter={"application_arn": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;A structure that describes a filter for application assignments.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="application_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  The real, unique identifier of the principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="principal_type" type="string" required>
  The real, discriminating type of the principal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_assignments" type="list(object)">
  The real IAM Identity Center application assignments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
