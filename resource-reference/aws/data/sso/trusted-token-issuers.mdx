---
title: "aws_sso_trusted_token_issuers"
description: "`aws_sso_trusted_token_issuers` looks up a real IAM Identity Center trusted token issuer, used for OAuth token exchange."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sso_trusted_token_issuers"})

    		ubx.Data(sso.TrustedTokenIssuers, "example", sso.TrustedTokenIssuersConfig{
    			InstanceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TrustedTokenIssuers } from "@ubx/sdk-aws/aws/data/sso/trusted_token_issuers";

    export default stack("example", () => {
      intent({ summary: "look up aws_sso_trusted_token_issuers" });

      data(TrustedTokenIssuers, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sso import TrustedTokenIssuers, TrustedTokenIssuersConfig

    def describe():
        ubx.intent("look up aws_sso_trusted_token_issuers")

        ubx.data(TrustedTokenIssuers, "example", TrustedTokenIssuersConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="trusted_token_issuers" type="list(object)">
  The real IAM Identity Center trusted token issuers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
