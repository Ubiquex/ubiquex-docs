---
title: "aws_sustainability_estimated_water_allocation"
description: "`aws_sustainability_estimated_water_allocation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sustainability_estimated_water_allocation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sustainability "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sustainability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sustainability_estimated_water_allocation"})

    		ubx.Data(sustainability.EstimatedWaterAllocation, "example", sustainability.EstimatedWaterAllocationConfig{
    			TimePeriod:      map[string]any{"end": "example"},
    			AllocationTypes: []string{"example"},
    			FilterBy:        map[string]any{"dimensions": map[string]any{"key": "value"}},
    			Granularity:     "example",
    			GroupBy:         []string{"example"},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EstimatedWaterAllocation } from "@ubx/sdk-aws/aws/data/sustainability/estimated_water_allocation";

    export default stack("example", () => {
      intent({ summary: "look up aws_sustainability_estimated_water_allocation" });

      data(EstimatedWaterAllocation, "example", {
        timePeriod: { end: "example" },
        allocationTypes: ["example"],
        filterBy: { dimensions: { key: "value" } },
        granularity: "example",
        groupBy: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sustainability import EstimatedWaterAllocation, EstimatedWaterAllocationConfig

    def describe():
        ubx.intent("look up aws_sustainability_estimated_water_allocation")

        ubx.data(EstimatedWaterAllocation, "example", EstimatedWaterAllocationConfig(
            time_period={"end": "example"},
            allocation_types=["example"],
            filter_by={"dimensions": {"key": "value"}},
            granularity="example",
            group_by=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="allocation_types" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="object">
  &lt;p&gt;Filters environmental impact values by specific dimension values.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimensions" type="map(list(string))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="granularity" type="string">
  &lt;p&gt;Specifies the time period over which environmental impact data is aggregated.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="time_period" type="object" required>
  &lt;p&gt;Represents a duration of time defined by start and end timestamps.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  Returned by the lookup.
</ResponseField>
