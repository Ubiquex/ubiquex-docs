---
title: "aws_sustainability_estimated_water_allocation_dimension_values"
description: "`aws_sustainability_estimated_water_allocation_dimension_values` reads the real, available dimension values for filtering estimated water usage data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sustainability "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sustainability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sustainability_estimated_water_allocation_dimension_values"})

    		ubx.Data(sustainability.EstimatedWaterAllocationDimensionValues, "example", sustainability.EstimatedWaterAllocationDimensionValuesConfig{
    			Dimensions: []string{"example"},
    			TimePeriod: map[string]any{"end": "example", "start": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EstimatedWaterAllocationDimensionValues } from "@ubx/sdk-aws/aws/data/sustainability/estimated_water_allocation_dimension_values";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_sustainability_estimated_water_allocation_dimension_values",
      });

      data(EstimatedWaterAllocationDimensionValues, "example", {
        dimensions: ["example"],
        timePeriod: { end: "example", start: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sustainability import EstimatedWaterAllocationDimensionValues, EstimatedWaterAllocationDimensionValuesConfig
    from ubx.aws.data.sustainability.estimated_water_allocation_dimension_values import EstimatedWaterAllocationDimensionValues_TimePeriod

    def describe():
        ubx.intent("look up aws_sustainability_estimated_water_allocation_dimension_values")

        ubx.data(EstimatedWaterAllocationDimensionValues, "example", EstimatedWaterAllocationDimensionValuesConfig(
            dimensions=["example"],
            time_period=EstimatedWaterAllocationDimensionValues_TimePeriod(end="example", start="example"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dimensions" type="list(string)" required>
  The real, structured dimensions of this metric. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="time_period" type="object" required>
  &lt;p&gt;Represents a duration of time defined by start and end timestamps.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
