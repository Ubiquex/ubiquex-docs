---
title: "aws_sustainability_estimated_carbon_emissions"
description: "`aws_sustainability_estimated_carbon_emissions` reads real, estimated carbon emissions data for an account, from the Customer Carbon Footprint Tool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sustainability "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sustainability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sustainability_estimated_carbon_emissions"})

    		ubx.Data(sustainability.EstimatedCarbonEmissions, "example", sustainability.EstimatedCarbonEmissionsConfig{
    			TimePeriod:               map[string]any{"end": "example"},
    			EmissionsTypes:           []string{"example"},
    			FilterBy:                 map[string]any{"dimensions": map[string]any{"key": "value"}},
    			Granularity:              "example",
    			GranularityConfiguration: map[string]any{"fiscal_year_start_month": 1},
    			GroupBy:                  []string{"example"},
    			MaxResults:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EstimatedCarbonEmissions } from "@ubx/sdk-aws/aws/data/sustainability/estimated_carbon_emissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_sustainability_estimated_carbon_emissions" });

      data(EstimatedCarbonEmissions, "example", {
        timePeriod: { end: "example" },
        emissionsTypes: ["example"],
        filterBy: { dimensions: { key: "value" } },
        granularity: "example",
        granularityConfiguration: { fiscalYearStartMonth: 1 },
        groupBy: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sustainability import EstimatedCarbonEmissions, EstimatedCarbonEmissionsConfig

    def describe():
        ubx.intent("look up aws_sustainability_estimated_carbon_emissions")

        ubx.data(EstimatedCarbonEmissions, "example", EstimatedCarbonEmissionsConfig(
            time_period={"end": "example"},
            emissions_types=["example"],
            filter_by={"dimensions": {"key": "value"}},
            granularity="example",
            granularity_configuration={"fiscal_year_start_month": 1},
            group_by=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="emissions_types" type="list(string)">
  The real, discriminating types of emissionss this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="object">
  &lt;p&gt;Filters environmental impact values by specific dimension values.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimensions" type="map(list(string))">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="granularity" type="string">
  &lt;p&gt;Specifies the time period over which environmental impact data is aggregated.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="granularity_configuration" type="object">
  &lt;p&gt; Contains configuration for the fiscal year granularities (e.g., &lt;code&gt;YEARLY_FISCAL&lt;/code&gt;, &lt;code&gt;QUARTERLY_FISCAL&lt;/code&gt;. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="fiscal_year_start_month" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="group_by" type="list(string)">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="time_period" type="object" required>
  &lt;p&gt;Represents a duration of time defined by start and end timestamps.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
