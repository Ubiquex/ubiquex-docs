---
title: "aws_forecast_explainabilities"
description: "`aws_forecast_explainabilities` looks up a real Forecast explainability resource, attributing predicted values to their own contributing factors."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_explainabilities"})

    		ubx.Data(forecast.Explainabilities, "example", forecast.ExplainabilitiesConfig{
    			Filters:    []map[string]any{{"condition": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Explainabilities } from "@ubx/sdk-aws/aws/data/forecast/explainabilities";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_explainabilities" });

      data(Explainabilities, "example", {
        filters: [{ condition: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import Explainabilities, ExplainabilitiesConfig

    def describe():
        ubx.intent("look up aws_forecast_explainabilities")

        ubx.data(Explainabilities, "example", ExplainabilitiesConfig(
            filters=[{"condition": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="explainabilities" type="list(object)">
  The real Amazon Personalize explainability resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
