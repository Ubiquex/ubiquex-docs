---
title: "aws_forecast_forecast_export_jobs"
description: "`aws_forecast_forecast_export_jobs` looks up a real job exporting Forecast predictions to S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_forecast_export_jobs"})

    		ubx.Data(forecast.ForecastExportJobs, "example", forecast.ForecastExportJobsConfig{
    			Filters:    []map[string]any{{"condition": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ForecastExportJobs } from "@ubx/sdk-aws/aws/data/forecast/forecast_export_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_forecast_export_jobs" });

      data(ForecastExportJobs, "example", {
        filters: [{ condition: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import ForecastExportJobs, ForecastExportJobsConfig
    from ubx.aws.data.forecast.forecast_export_jobs import ForecastExportJobs_Filters

    def describe():
        ubx.intent("look up aws_forecast_forecast_export_jobs")

        ubx.data(ForecastExportJobs, "example", ForecastExportJobsConfig(
            filters=[ForecastExportJobs_Filters(condition="example")],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="forecast_export_jobs" type="list(object)">
  The real Amazon Forecast export jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
