---
title: "aws_forecast_forecasts"
description: "`aws_forecast_forecasts` looks up a real Forecast forecast, the generated predictions produced by a trained predictor."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_forecasts"})

    		ubx.Data(forecast.Forecasts, "example", forecast.ForecastsConfig{
    			Filters:    []map[string]any{{"condition": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Forecasts } from "./local-sdk/aws/sdk/typescript/aws/data/forecast/forecasts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_forecasts" });

      data(Forecasts, "example", {
        filters: [{ condition: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import Forecasts, ForecastsConfig

    def describe():
        ubx.intent("look up aws_forecast_forecasts")

        ubx.data(Forecasts, "example", ForecastsConfig(
            filters=[{"condition": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="forecasts" type="list(object)">
  The real forecasts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
