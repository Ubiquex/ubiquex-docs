---
title: "aws_forecast_monitor_evaluations"
description: "`aws_forecast_monitor_evaluations` reads the real, periodic accuracy evaluations of a Forecast predictor monitor."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_monitor_evaluations"})

    		ubx.Data(forecast.MonitorEvaluations, "example", forecast.MonitorEvaluationsConfig{
    			MonitorArn: "arn:aws:iam::123456789012:policy/example",
    			Filters:    []map[string]any{{"condition": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitorEvaluations } from "@ubx/sdk-aws/aws/data/forecast/monitor_evaluations";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_monitor_evaluations" });

      data(MonitorEvaluations, "example", {
        monitorArn: "arn:aws:iam::123456789012:policy/example",
        filters: [{ condition: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import MonitorEvaluations, MonitorEvaluationsConfig

    def describe():
        ubx.intent("look up aws_forecast_monitor_evaluations")

        ubx.data(MonitorEvaluations, "example", MonitorEvaluationsConfig(
            monitor_arn="arn:aws:iam::123456789012:policy/example",
            filters=[{"condition": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="monitor_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the monitor this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="predictor_monitor_evaluations" type="list(object)">
  The real predictor monitor evaluations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
