---
title: "aws_forecast_predictors"
description: "`aws_forecast_predictors` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_forecast_predictors` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_predictors"})

    		ubx.Data(forecast.Predictors, "example", forecast.PredictorsConfig{
    			Filters:    []map[string]any{{"condition": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Predictors } from "@ubx/sdk-aws/aws/data/forecast/predictors";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_predictors" });

      data(Predictors, "example", {
        filters: [{ condition: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import Predictors, PredictorsConfig

    def describe():
        ubx.intent("look up aws_forecast_predictors")

        ubx.data(Predictors, "example", PredictorsConfig(
            filters=[{"condition": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="predictors" type="list(object)">
  Returned by the lookup.
</ResponseField>
