---
title: "aws_forecast_what_if_analyses"
description: "`aws_forecast_what_if_analyses` looks up a real Forecast what-if analysis, testing a hypothetical change against a baseline forecast."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_what_if_analyses"})

    		ubx.Data(forecast.WhatIfAnalyses, "example", forecast.WhatIfAnalysesConfig{
    			Filters:    []map[string]any{{"condition": "example"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WhatIfAnalyses } from "@ubx/sdk-aws/aws/data/forecast/what_if_analyses";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_what_if_analyses" });

      data(WhatIfAnalyses, "example", {
        filters: [{ condition: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import WhatIfAnalyses, WhatIfAnalysesConfig

    def describe():
        ubx.intent("look up aws_forecast_what_if_analyses")

        ubx.data(WhatIfAnalyses, "example", WhatIfAnalysesConfig(
            filters=[{"condition": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="what_if_analyses" type="list(object)">
  The real Amazon Forecast what-if analyses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
