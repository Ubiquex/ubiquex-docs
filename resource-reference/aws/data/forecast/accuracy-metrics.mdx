---
title: "aws_forecast_accuracy_metrics"
description: "`aws_forecast_accuracy_metrics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_forecast_accuracy_metrics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	forecast "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/forecast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_forecast_accuracy_metrics"})

    		ubx.Data(forecast.AccuracyMetrics, "example", forecast.AccuracyMetricsConfig{
    			PredictorArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccuracyMetrics } from "@ubx/sdk-aws/aws/data/forecast/accuracy_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_forecast_accuracy_metrics" });

      data(AccuracyMetrics, "example", {
        predictorArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.forecast import AccuracyMetrics, AccuracyMetricsConfig

    def describe():
        ubx.intent("look up aws_forecast_accuracy_metrics")

        ubx.data(AccuracyMetrics, "example", AccuracyMetricsConfig(
            predictor_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="predictor_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auto_mloverride_strategy" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="is_auto_predictor" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="optimization_metric" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="predictor_evaluation_results" type="list(object)">
  Returned by the lookup.
</ResponseField>
