---
title: "aws_elasticfilesystem_file_system_policy"
description: "`aws_elasticfilesystem_file_system_policy` reads the real resource-based policy attached to an EFS file system."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticfilesystem "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticfilesystem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticfilesystem_file_system_policy"})

    		ubx.Data(elasticfilesystem.FileSystemPolicy, "example", elasticfilesystem.FileSystemPolicyConfig{
    			FileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FileSystemPolicy } from "@ubx/sdk-aws/aws/data/elasticfilesystem/file_system_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticfilesystem_file_system_policy" });

      data(FileSystemPolicy, "example", {
        fileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticfilesystem import FileSystemPolicy, FileSystemPolicyConfig

    def describe():
        ubx.intent("look up aws_elasticfilesystem_file_system_policy")

        ubx.data(FileSystemPolicy, "example", FileSystemPolicyConfig(
            file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file_system_id" type="string" required>
  The real, unique identifier of the file system this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
