---
title: "aws_elasticfilesystem_backup_policy"
description: "`aws_elasticfilesystem_backup_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticfilesystem_backup_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticfilesystem "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticfilesystem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticfilesystem_backup_policy"})

    		ubx.Data(elasticfilesystem.BackupPolicy, "example", elasticfilesystem.BackupPolicyConfig{
    			FileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupPolicy } from "@ubx/sdk-aws/aws/data/elasticfilesystem/backup_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticfilesystem_backup_policy" });

      data(BackupPolicy, "example", {
        fileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticfilesystem import BackupPolicy, BackupPolicyConfig

    def describe():
        ubx.intent("look up aws_elasticfilesystem_backup_policy")

        ubx.data(BackupPolicy, "example", BackupPolicyConfig(
            file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file_system_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_policy" type="object">
  &lt;p&gt;The backup policy for the file system used to create automatic daily backups. If status has a value of &lt;code&gt;ENABLED&lt;/code&gt;, the file system is being automatically backed up. For more information, see &lt;a href="https://docs.aws.amazon.com/efs/latest/ug/awsbackup.html#automatic-backups"&gt;Automatic backups&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
