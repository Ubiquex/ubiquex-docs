---
title: "aws_elasticfilesystem_tags_for_resource"
description: "`aws_elasticfilesystem_tags_for_resource` reads the real tags applied to an EFS resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticfilesystem "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticfilesystem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticfilesystem_tags_for_resource"})

    		ubx.Data(elasticfilesystem.TagsForResource, "example", elasticfilesystem.TagsForResourceConfig{
    			ResourceId: "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagsForResource } from "@ubx/sdk-aws/aws/data/elasticfilesystem/tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticfilesystem_tags_for_resource" });

      data(TagsForResource, "example", {
        resourceId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticfilesystem import TagsForResource, TagsForResourceConfig

    def describe():
        ubx.intent("look up aws_elasticfilesystem_tags_for_resource")

        ubx.data(TagsForResource, "example", TagsForResourceConfig(
            resource_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results used for pagination. Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Token used for pagination. Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
