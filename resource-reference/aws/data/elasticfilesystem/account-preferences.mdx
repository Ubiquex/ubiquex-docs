---
title: "aws_elasticfilesystem_account_preferences"
description: "`aws_elasticfilesystem_account_preferences` reads the real, account-wide EFS ID format preferences."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticfilesystem "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticfilesystem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticfilesystem_account_preferences"})

    		ubx.Data(elasticfilesystem.AccountPreferences, "example", elasticfilesystem.AccountPreferencesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AccountPreferences } from "./local-sdk/aws/sdk/typescript/aws/data/elasticfilesystem/account_preferences.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticfilesystem_account_preferences" });

      data(AccountPreferences, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticfilesystem import AccountPreferences, AccountPreferencesConfig

    def describe():
        ubx.intent("look up aws_elasticfilesystem_account_preferences")

        ubx.data(AccountPreferences, "example", AccountPreferencesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Max results used for pagination. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Token used for pagination. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_id_preference" type="object">
  &lt;p&gt;Describes the resource type and its ID preference for the user's Amazon Web Services account, in the current Amazon Web Services Region.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="resource_id_type" type="string">
      A preference indicating a choice to use 63bit/32bit IDs for all applicable resources. Optional lookup argument.
    </ResponseField>
    <ResponseField name="resources" type="list(string)">
      EFS resources to which a preference applies to. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
