---
title: "aws_elasticfilesystem_mount_target_security_groups"
description: "`aws_elasticfilesystem_mount_target_security_groups` reads the real security groups attached to an EFS mount target."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasticfilesystem "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticfilesystem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticfilesystem_mount_target_security_groups"})

    		ubx.Data(elasticfilesystem.MountTargetSecurityGroups, "example", elasticfilesystem.MountTargetSecurityGroupsConfig{
    			MountTargetId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MountTargetSecurityGroups } from "./local-sdk/aws/sdk/typescript/aws/data/elasticfilesystem/mount_target_security_groups.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elasticfilesystem_mount_target_security_groups",
      });

      data(MountTargetSecurityGroups, "example", {
        mountTargetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elasticfilesystem import MountTargetSecurityGroups, MountTargetSecurityGroupsConfig

    def describe():
        ubx.intent("look up aws_elasticfilesystem_mount_target_security_groups")

        ubx.data(MountTargetSecurityGroups, "example", MountTargetSecurityGroupsConfig(
            mount_target_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="mount_target_id" type="string" required>
  The real, unique identifier of the mount target this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="security_groups" type="list(string)">
  The real security groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
