---
title: "aws_elasticfilesystem_lifecycle_configuration"
description: "`aws_elasticfilesystem_lifecycle_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticfilesystem_lifecycle_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticfilesystem "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticfilesystem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticfilesystem_lifecycle_configuration"})

    		ubx.Data(elasticfilesystem.LifecycleConfiguration, "example", elasticfilesystem.LifecycleConfigurationConfig{
    			FileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LifecycleConfiguration } from "@ubx/sdk-aws/aws/data/elasticfilesystem/lifecycle_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticfilesystem_lifecycle_configuration" });

      data(LifecycleConfiguration, "example", {
        fileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticfilesystem import LifecycleConfiguration, LifecycleConfigurationConfig

    def describe():
        ubx.intent("look up aws_elasticfilesystem_lifecycle_configuration")

        ubx.data(LifecycleConfiguration, "example", LifecycleConfigurationConfig(
            file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file_system_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lifecycle_policies" type="list(object)">
  Returned by the lookup.
</ResponseField>
