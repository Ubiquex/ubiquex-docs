---
title: "aws_snowball_job_manifest"
description: "`aws_snowball_job_manifest` reads the real manifest file required to unlock a Snow Family device for a job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	snowball "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/snowball"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_snowball_job_manifest"})

    		ubx.Data(snowball.JobManifest, "example", snowball.JobManifestConfig{
    			JobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobManifest } from "@ubx/sdk-aws/aws/data/snowball/job_manifest";

    export default stack("example", () => {
      intent({ summary: "look up aws_snowball_job_manifest" });

      data(JobManifest, "example", {
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.snowball import JobManifest, JobManifestConfig

    def describe():
        ubx.intent("look up aws_snowball_job_manifest")

        ubx.data(JobManifest, "example", JobManifestConfig(
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="manifest_uri" type="string">
  The real, resolvable URI of the manifest this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
