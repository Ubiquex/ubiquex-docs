---
title: "aws_snowball_service_versions"
description: "`aws_snowball_service_versions` reads the real, available Snow Family service versions for a given feature."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	snowball "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/snowball"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_snowball_service_versions"})

    		ubx.Data(snowball.ServiceVersions, "example", snowball.ServiceVersionsConfig{
    			ServiceName: "example-service-name",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ServiceVersions } from "./local-sdk/aws/sdk/typescript/aws/data/snowball/service_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_snowball_service_versions" });

      data(ServiceVersions, "example", {
        serviceName: "example-service-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.snowball import ServiceVersions, ServiceVersionsConfig

    def describe():
        ubx.intent("look up aws_snowball_service_versions")

        ubx.data(ServiceVersions, "example", ServiceVersionsConfig(
            service_name="example-service-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  The real name of the service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dependent_services" type="list(object)">
  The real services this object depends on. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_versions" type="list(object)">
  The real versions of the service included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
