---
title: "aws_snowball_job_unlock_code"
description: "`aws_snowball_job_unlock_code` reads the real unlock code required to access a Snow Family device for a job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	snowball "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/snowball"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_snowball_job_unlock_code"})

    		ubx.Data(snowball.JobUnlockCode, "example", snowball.JobUnlockCodeConfig{
    			JobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobUnlockCode } from "@ubx/sdk-aws/aws/data/snowball/job_unlock_code";

    export default stack("example", () => {
      intent({ summary: "look up aws_snowball_job_unlock_code" });

      data(JobUnlockCode, "example", {
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.snowball import JobUnlockCode, JobUnlockCodeConfig

    def describe():
        ubx.intent("look up aws_snowball_job_unlock_code")

        ubx.data(JobUnlockCode, "example", JobUnlockCodeConfig(
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="unlock_code" type="string">
  The real code identifying the unlock. **(AI-inferred)** Returned by the lookup.
</ResponseField>
