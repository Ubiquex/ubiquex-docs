---
title: "aws_snowball_software_updates"
description: "`aws_snowball_software_updates` reads the real, available software updates for a Snow Family job's own device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	snowball "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/snowball"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_snowball_software_updates"})

    		ubx.Data(snowball.SoftwareUpdates, "example", snowball.SoftwareUpdatesConfig{
    			JobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SoftwareUpdates } from "@ubx/sdk-aws/aws/data/snowball/software_updates";

    export default stack("example", () => {
      intent({ summary: "look up aws_snowball_software_updates" });

      data(SoftwareUpdates, "example", {
        jobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.snowball import SoftwareUpdates, SoftwareUpdatesConfig

    def describe():
        ubx.intent("look up aws_snowball_software_updates")

        ubx.data(SoftwareUpdates, "example", SoftwareUpdatesConfig(
            job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="updates_uri" type="string">
  The real, resolvable URI of the updates this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
