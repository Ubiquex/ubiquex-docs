---
title: "aws_snowball_clusters"
description: "`aws_snowball_clusters` looks up a real Snow Family cluster, coordinating multiple devices for a large data transfer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	snowball "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/snowball"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_snowball_clusters"})

    		ubx.Data(snowball.Clusters, "example", snowball.ClustersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Clusters } from "@ubx/sdk-aws/aws/data/snowball/clusters";

    export default stack("example", () => {
      intent({ summary: "look up aws_snowball_clusters" });

      data(Clusters, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.snowball import Clusters, ClustersConfig

    def describe():
        ubx.intent("look up aws_snowball_clusters")

        ubx.data(Clusters, "example", ClustersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_list_entries" type="list(object)">
  The real cluster list entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
