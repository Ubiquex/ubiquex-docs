---
title: "aws_snowball_compatible_images"
description: "`aws_snowball_compatible_images` reads the real AMIs compatible with running as EC2 compute on a Snowball Edge device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	snowball "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/snowball"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_snowball_compatible_images"})

    		ubx.Data(snowball.CompatibleImages, "example", snowball.CompatibleImagesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CompatibleImages } from "@ubx/sdk-aws/aws/data/snowball/compatible_images";

    export default stack("example", () => {
      intent({ summary: "look up aws_snowball_compatible_images" });

      data(CompatibleImages, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.snowball import CompatibleImages, CompatibleImagesConfig

    def describe():
        ubx.intent("look up aws_snowball_compatible_images")

        ubx.data(CompatibleImages, "example", CompatibleImagesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compatible_images" type="list(object)">
  The real images compatible with this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
