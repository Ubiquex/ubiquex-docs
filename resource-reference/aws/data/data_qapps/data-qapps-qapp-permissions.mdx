---
title: "aws_data_qapps_qapp_permissions"
description: "`aws_data_qapps_qapp_permissions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_qapps_qapp_permissions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_qapp_permissions"})

    		ubx.Data(data_qapps.DataQappsQappPermissions, "example", data_qapps.DataQappsQappPermissionsConfig{
    			AppId:      "example",
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsQappPermissions } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_qapp_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_qapp_permissions" });

      data(DataQappsQappPermissions, "example", {
        appId: "example",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsQappPermissions, DataQappsQappPermissionsConfig

    def describe():
        ubx.intent("look up aws_data_qapps_qapp_permissions")

        ubx.data(DataQappsQappPermissions, "example", DataQappsQappPermissionsConfig(
            app_id="example",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="permissions" type="list(object)">
  &lt;p&gt;A list of permissions granted for a Amazon Q App.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Returned by the lookup.
</ResponseField>
