---
title: "aws_data_qapps_qapp_session_data"
description: "`aws_data_qapps_qapp_session_data` reads the real, current state data of a running Q App session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_qapp_session_data"})

    		ubx.Data(data_qapps.DataQappsQappSessionData, "example", data_qapps.DataQappsQappSessionDataConfig{
    			InstanceId: "example",
    			SessionId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsQappSessionData } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_qapp_session_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_qapp_session_data" });

      data(DataQappsQappSessionData, "example", {
        instanceId: "example",
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsQappSessionData, DataQappsQappSessionDataConfig

    def describe():
        ubx.intent("look up aws_data_qapps_qapp_session_data")

        ubx.data(DataQappsQappSessionData, "example", DataQappsQappSessionDataConfig(
            instance_id="example",
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_arn" type="string">
  The real Amazon Resource Name (ARN) of the session this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_data" type="list(object)">
  The real, raw data of the session. **(AI-inferred)** Returned by the lookup.
</ResponseField>
