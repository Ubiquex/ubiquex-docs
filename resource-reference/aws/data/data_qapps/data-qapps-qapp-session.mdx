---
title: "aws_data_qapps_qapp_session"
description: "`aws_data_qapps_qapp_session` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_qapps_qapp_session` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_qapp_session"})

    		ubx.Data(data_qapps.DataQappsQappSession, "example", data_qapps.DataQappsQappSessionConfig{
    			InstanceId: "example",
    			SessionId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsQappSession } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_qapp_session";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_qapp_session" });

      data(DataQappsQappSession, "example", {
        instanceId: "example",
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsQappSession, DataQappsQappSessionConfig

    def describe():
        ubx.intent("look up aws_data_qapps_qapp_session")

        ubx.data(DataQappsQappSession, "example", DataQappsQappSessionConfig(
            instance_id="example",
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="app_version" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="card_status" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="latest_published_app_version" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="session_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="session_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_is_host" type="bool">
  Returned by the lookup.
</ResponseField>
