---
title: "aws_data_qapps_categories"
description: "`aws_data_qapps_categories` reads the real categories available for organizing Q Apps within a Q Business application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_categories"})

    		ubx.Data(data_qapps.DataQappsCategories, "example", data_qapps.DataQappsCategoriesConfig{
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsCategories } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_categories";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_categories" });

      data(DataQappsCategories, "example", {
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsCategories, DataQappsCategoriesConfig

    def describe():
        ubx.intent("look up aws_data_qapps_categories")

        ubx.data(DataQappsCategories, "example", DataQappsCategoriesConfig(
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="categories" type="list(object)">
  The real categories included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
