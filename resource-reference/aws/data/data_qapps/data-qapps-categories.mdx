---
title: "aws_data_qapps_categories"
description: "`aws_data_qapps_categories` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_qapps_categories` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_categories"})

    		ubx.Data(data_qapps.DataQappsCategories, "example", data_qapps.DataQappsCategoriesConfig{
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsCategories } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_categories";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_categories" });

      data(DataQappsCategories, "example", {
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsCategories, DataQappsCategoriesConfig

    def describe():
        ubx.intent("look up aws_data_qapps_categories")

        ubx.data(DataQappsCategories, "example", DataQappsCategoriesConfig(
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="categories" type="list(object)">
  Returned by the lookup.
</ResponseField>
