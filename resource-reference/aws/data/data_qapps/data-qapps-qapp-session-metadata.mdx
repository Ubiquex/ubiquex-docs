---
title: "aws_data_qapps_qapp_session_metadata"
description: "`aws_data_qapps_qapp_session_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_qapps_qapp_session_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_qapp_session_metadata"})

    		ubx.Data(data_qapps.DataQappsQappSessionMetadata, "example", data_qapps.DataQappsQappSessionMetadataConfig{
    			InstanceId: "example",
    			SessionId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsQappSessionMetadata } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_qapp_session_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_qapp_session_metadata" });

      data(DataQappsQappSessionMetadata, "example", {
        instanceId: "example",
        sessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsQappSessionMetadata, DataQappsQappSessionMetadataConfig

    def describe():
        ubx.intent("look up aws_data_qapps_qapp_session_metadata")

        ubx.data(DataQappsQappSessionMetadata, "example", DataQappsQappSessionMetadataConfig(
            instance_id="example",
            session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="session_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="session_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="session_owner" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sharing_configuration" type="object">
  &lt;p&gt;The sharing configuration of an Amazon Q App data collection session.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="accept_responses" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="enabled" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="reveal_cards" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
