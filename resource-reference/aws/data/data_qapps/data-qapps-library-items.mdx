---
title: "aws_data_qapps_library_items"
description: "`aws_data_qapps_library_items` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_data_qapps_library_items` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_library_items"})

    		ubx.Data(data_qapps.DataQappsLibraryItems, "example", data_qapps.DataQappsLibraryItemsConfig{
    			InstanceId: "example",
    			CategoryId: "example",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsLibraryItems } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_library_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_library_items" });

      data(DataQappsLibraryItems, "example", {
        instanceId: "example",
        categoryId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsLibraryItems, DataQappsLibraryItemsConfig

    def describe():
        ubx.intent("look up aws_data_qapps_library_items")

        ubx.data(DataQappsLibraryItems, "example", DataQappsLibraryItemsConfig(
            instance_id="example",
            category_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="library_items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
