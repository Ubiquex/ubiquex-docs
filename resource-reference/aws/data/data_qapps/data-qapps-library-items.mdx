---
title: "aws_data_qapps_library_items"
description: "`aws_data_qapps_library_items` looks up one real Q App published to a Q Business application's own library."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data_qapps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/data_qapps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_data_qapps_library_items"})

    		ubx.Data(data_qapps.DataQappsLibraryItems, "example", data_qapps.DataQappsLibraryItemsConfig{
    			InstanceId: "example",
    			CategoryId: "example",
    			Limit:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataQappsLibraryItems } from "@ubx/sdk-aws/aws/data/data_qapps/data_qapps_library_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_data_qapps_library_items" });

      data(DataQappsLibraryItems, "example", {
        instanceId: "example",
        categoryId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.data_qapps import DataQappsLibraryItems, DataQappsLibraryItemsConfig

    def describe():
        ubx.intent("look up aws_data_qapps_library_items")

        ubx.data(DataQappsLibraryItems, "example", DataQappsLibraryItemsConfig(
            instance_id="example",
            category_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="category_id" type="string">
  The real, unique identifier of the category this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="library_items" type="list(object)">
  The real library items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
