---
title: "aws_evs_versions"
description: "`aws_evs_versions` reads the real, available VMware software versions supported by Elastic VMware Service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	evs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/evs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_evs_versions"})

    		ubx.Data(evs.Versions, "example", evs.VersionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Versions } from "@ubx/sdk-aws/aws/data/evs/versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_evs_versions" });

      data(Versions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.evs import Versions, VersionsConfig

    def describe():
        ubx.intent("look up aws_evs_versions")

        ubx.data(Versions, "example", VersionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="instance_type_esx_versions" type="list(object)">
  The real versions of the instance type esx included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vcf_versions" type="list(object)">
  The real versions of the vcf included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
