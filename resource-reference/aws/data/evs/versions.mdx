---
title: "aws_evs_versions"
description: "`aws_evs_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_evs_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	evs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/evs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_evs_versions"})

    		ubx.Data(evs.Versions, "example", evs.VersionsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Versions } from "@ubx/sdk-aws/aws/data/evs/versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_evs_versions" });

      data(Versions, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.evs import Versions, VersionsConfig

    def describe():
        ubx.intent("look up aws_evs_versions")

        ubx.data(Versions, "example", VersionsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="instance_type_esx_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vcf_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>
