---
title: "aws_greengrass_connector_definitions"
description: "`aws_greengrass_connector_definitions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrass_connector_definitions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_connector_definitions"})

    		ubx.Data(greengrass.ConnectorDefinitions, "example", greengrass.ConnectorDefinitionsConfig{
    			MaxResults: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectorDefinitions } from "@ubx/sdk-aws/aws/data/greengrass/connector_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_connector_definitions" });

      data(ConnectorDefinitions, "example", {
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import ConnectorDefinitions, ConnectorDefinitionsConfig

    def describe():
        ubx.intent("look up aws_greengrass_connector_definitions")

        ubx.data(ConnectorDefinitions, "example", ConnectorDefinitionsConfig(
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="definitions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
