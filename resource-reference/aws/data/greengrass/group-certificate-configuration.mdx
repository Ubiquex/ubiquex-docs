---
title: "aws_greengrass_group_certificate_configuration"
description: "`aws_greengrass_group_certificate_configuration` reads the real certificate rotation configuration of a Greengrass group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_group_certificate_configuration"})

    		ubx.Data(greengrass.GroupCertificateConfiguration, "example", greengrass.GroupCertificateConfigurationConfig{
    			GroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { GroupCertificateConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/greengrass/group_certificate_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_group_certificate_configuration" });

      data(GroupCertificateConfiguration, "example", {
        groupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import GroupCertificateConfiguration, GroupCertificateConfigurationConfig

    def describe():
        ubx.intent("look up aws_greengrass_group_certificate_configuration")

        ubx.data(GroupCertificateConfiguration, "example", GroupCertificateConfigurationConfig(
            group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificate_authority_expiry_in_milliseconds" type="string">
  The real, remaining validity period of this certificate authority's own certificate, in milliseconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="certificate_expiry_in_milliseconds" type="string">
  The real, remaining validity period of this certificate, in milliseconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>
