---
title: "aws_greengrass_device_definitions"
description: "`data_aws_greengrass_device_definitions` looks up a real, already-existing `aws_greengrass_device_definition`."
---

It registers real, local IoT devices, connected to an `aws_greengrass_group`'s core rather than directly to AWS IoT Core, that can communicate with the core and each other even when internet connectivity is lost. Part of Greengrass V1's maintenance-only lifecycle since June 2023.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_device_definitions"})

    		ubx.Data(greengrass.DeviceDefinitions, "example", greengrass.DeviceDefinitionsConfig{
    			MaxResults: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DeviceDefinitions } from "./local-sdk/aws/sdk/typescript/aws/data/greengrass/device_definitions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_device_definitions" });

      data(DeviceDefinitions, "example", {
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import DeviceDefinitions, DeviceDefinitionsConfig

    def describe():
        ubx.intent("look up aws_greengrass_device_definitions")

        ubx.data(DeviceDefinitions, "example", DeviceDefinitionsConfig(
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="definitions" type="list(object)">
  The real, structured definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
