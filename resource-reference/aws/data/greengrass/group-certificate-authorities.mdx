---
title: "aws_greengrass_group_certificate_authorities"
description: "`aws_greengrass_group_certificate_authorities` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrass_group_certificate_authorities` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_group_certificate_authorities"})

    		ubx.Data(greengrass.GroupCertificateAuthorities, "example", greengrass.GroupCertificateAuthoritiesConfig{
    			GroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupCertificateAuthorities } from "@ubx/sdk-aws/aws/data/greengrass/group_certificate_authorities";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_group_certificate_authorities" });

      data(GroupCertificateAuthorities, "example", {
        groupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import GroupCertificateAuthorities, GroupCertificateAuthoritiesConfig

    def describe():
        ubx.intent("look up aws_greengrass_group_certificate_authorities")

        ubx.data(GroupCertificateAuthorities, "example", GroupCertificateAuthoritiesConfig(
            group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="group_certificate_authorities" type="list(object)">
  Returned by the lookup.
</ResponseField>
