---
title: "aws_greengrass_subscription_definitions"
description: "`data_aws_greengrass_subscription_definitions` looks up a real, already-existing `aws_greengrass_subscription_definition`."
---

It defines real, local MQTT-style publish/subscribe routes between an `aws_greengrass_group`'s core, its devices, its Lambda functions, and the cloud, the actual message-routing rules the group runs on. Part of Greengrass V1's maintenance-only lifecycle since June 2023.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_subscription_definitions"})

    		ubx.Data(greengrass.SubscriptionDefinitions, "example", greengrass.SubscriptionDefinitionsConfig{
    			MaxResults: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SubscriptionDefinitions } from "@ubx/sdk-aws/aws/data/greengrass/subscription_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_subscription_definitions" });

      data(SubscriptionDefinitions, "example", {
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import SubscriptionDefinitions, SubscriptionDefinitionsConfig

    def describe():
        ubx.intent("look up aws_greengrass_subscription_definitions")

        ubx.data(SubscriptionDefinitions, "example", SubscriptionDefinitionsConfig(
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="definitions" type="list(object)">
  The real, structured definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
