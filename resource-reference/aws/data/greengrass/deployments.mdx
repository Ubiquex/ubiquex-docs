---
title: "aws_greengrass_deployments"
description: "`data_aws_greengrass_deployments` looks up a real, already-existing `aws_greengrass_v2_deployment`."
---

It pushes a real, defined set of `aws_greengrass_v2_component_version`s to a target group of Greengrass V2 core devices, with rollout and rollback configuration, the actual mechanism that gets software running on real edge devices.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_deployments"})

    		ubx.Data(greengrass.Deployments, "example", greengrass.DeploymentsConfig{
    			GroupId:    "example",
    			MaxResults: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Deployments } from "@ubx/sdk-aws/aws/data/greengrass/deployments";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_deployments" });

      data(Deployments, "example", {
        groupId: "example",
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import Deployments, DeploymentsConfig

    def describe():
        ubx.intent("look up aws_greengrass_deployments")

        ubx.data(Deployments, "example", DeploymentsConfig(
            group_id="example",
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deployments" type="list(object)">
  The real deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
