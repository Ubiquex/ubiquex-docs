---
title: "aws_greengrass_thing_runtime_configuration"
description: "`aws_greengrass_thing_runtime_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrass_thing_runtime_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_thing_runtime_configuration"})

    		ubx.Data(greengrass.ThingRuntimeConfiguration, "example", greengrass.ThingRuntimeConfigurationConfig{
    			ThingName: "example-thing-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ThingRuntimeConfiguration } from "@ubx/sdk-aws/aws/data/greengrass/thing_runtime_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_thing_runtime_configuration" });

      data(ThingRuntimeConfiguration, "example", {
        thingName: "example-thing-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import ThingRuntimeConfiguration, ThingRuntimeConfigurationConfig

    def describe():
        ubx.intent("look up aws_greengrass_thing_runtime_configuration")

        ubx.data(ThingRuntimeConfiguration, "example", ThingRuntimeConfigurationConfig(
            thing_name="example-thing-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="thing_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="runtime_configuration" type="object">
  Runtime configuration for a thing. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="telemetry_configuration" type="object">
      Configuration settings for running telemetry. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="configuration_sync_status" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="telemetry" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
