---
title: "aws_greengrass_resource_definition_versions"
description: "`aws_greengrass_resource_definition_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrass_resource_definition_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_resource_definition_versions"})

    		ubx.Data(greengrass.ResourceDefinitionVersions, "example", greengrass.ResourceDefinitionVersionsConfig{
    			ResourceDefinitionId: "example",
    			MaxResults:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceDefinitionVersions } from "@ubx/sdk-aws/aws/data/greengrass/resource_definition_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_resource_definition_versions" });

      data(ResourceDefinitionVersions, "example", {
        resourceDefinitionId: "example",
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import ResourceDefinitionVersions, ResourceDefinitionVersionsConfig

    def describe():
        ubx.intent("look up aws_greengrass_resource_definition_versions")

        ubx.data(ResourceDefinitionVersions, "example", ResourceDefinitionVersionsConfig(
            resource_definition_id="example",
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_definition_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  Returned by the lookup.
</ResponseField>
