---
title: "aws_greengrass_bulk_deployment_status"
description: "`aws_greengrass_bulk_deployment_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_greengrass_bulk_deployment_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_bulk_deployment_status"})

    		ubx.Data(greengrass.BulkDeploymentStatus, "example", greengrass.BulkDeploymentStatusConfig{
    			BulkDeploymentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BulkDeploymentStatus } from "@ubx/sdk-aws/aws/data/greengrass/bulk_deployment_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_bulk_deployment_status" });

      data(BulkDeploymentStatus, "example", {
        bulkDeploymentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import BulkDeploymentStatus, BulkDeploymentStatusConfig

    def describe():
        ubx.intent("look up aws_greengrass_bulk_deployment_status")

        ubx.data(BulkDeploymentStatus, "example", BulkDeploymentStatusConfig(
            bulk_deployment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bulk_deployment_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bulk_deployment_metrics" type="object">
  Relevant metrics on input records processed during bulk deployment. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="invalid_input_records" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="records_processed" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="retry_attempts" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bulk_deployment_status" type="string">
  The current status of the bulk deployment. Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="error_details" type="list(object)">
  A list of error details. Returned by the lookup.
</ResponseField>

<ResponseField name="error_message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  The key-value pair for the resource tag. Returned by the lookup.
</ResponseField>
