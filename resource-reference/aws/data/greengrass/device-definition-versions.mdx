---
title: "aws_greengrass_device_definition_versions"
description: "`data_aws_greengrass_device_definition_versions` looks up a real, already-existing `aws_greengrass_device_definition_version`."
---

It is a real, specific version of an `aws_greengrass_device_definition`, part of AWS IoT Greengrass V1's maintenance-only lifecycle since June 2023.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_device_definition_versions"})

    		ubx.Data(greengrass.DeviceDefinitionVersions, "example", greengrass.DeviceDefinitionVersionsConfig{
    			DeviceDefinitionId: "example",
    			MaxResults:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeviceDefinitionVersions } from "@ubx/sdk-aws/aws/data/greengrass/device_definition_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_device_definition_versions" });

      data(DeviceDefinitionVersions, "example", {
        deviceDefinitionId: "example",
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import DeviceDefinitionVersions, DeviceDefinitionVersionsConfig

    def describe():
        ubx.intent("look up aws_greengrass_device_definition_versions")

        ubx.data(DeviceDefinitionVersions, "example", DeviceDefinitionVersionsConfig(
            device_definition_id="example",
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_definition_id" type="string" required>
  The real, unique identifier of the device definition this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="versions" type="list(object)">
  The real versions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
