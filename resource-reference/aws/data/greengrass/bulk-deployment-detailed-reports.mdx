---
title: "aws_greengrass_bulk_deployment_detailed_reports"
description: "`aws_greengrass_bulk_deployment_detailed_reports` reads real, detailed per-group reports from a Greengrass bulk deployment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_bulk_deployment_detailed_reports"})

    		ubx.Data(greengrass.BulkDeploymentDetailedReports, "example", greengrass.BulkDeploymentDetailedReportsConfig{
    			BulkDeploymentId: "example",
    			MaxResults:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BulkDeploymentDetailedReports } from "./local-sdk/aws/sdk/typescript/aws/data/greengrass/bulk_deployment_detailed_reports.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_greengrass_bulk_deployment_detailed_reports",
      });

      data(BulkDeploymentDetailedReports, "example", {
        bulkDeploymentId: "example",
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import BulkDeploymentDetailedReports, BulkDeploymentDetailedReportsConfig

    def describe():
        ubx.intent("look up aws_greengrass_bulk_deployment_detailed_reports")

        ubx.data(BulkDeploymentDetailedReports, "example", BulkDeploymentDetailedReportsConfig(
            bulk_deployment_id="example",
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bulk_deployment_id" type="string" required>
  The real, unique identifier of the bulk deployment this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deployments" type="list(object)">
  The real deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
