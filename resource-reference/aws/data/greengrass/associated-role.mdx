---
title: "aws_greengrass_associated_role"
description: "`aws_greengrass_associated_role` reads the real IAM role associated with a Greengrass group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_associated_role"})

    		ubx.Data(greengrass.AssociatedRole, "example", greengrass.AssociatedRoleConfig{
    			GroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssociatedRole } from "@ubx/sdk-aws/aws/data/greengrass/associated_role";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_associated_role" });

      data(AssociatedRole, "example", {
        groupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import AssociatedRole, AssociatedRoleConfig

    def describe():
        ubx.intent("look up aws_greengrass_associated_role")

        ubx.data(AssociatedRole, "example", AssociatedRoleConfig(
            group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_id" type="string" required>
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="associated_at" type="string">
  The real timestamp this object was associated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
