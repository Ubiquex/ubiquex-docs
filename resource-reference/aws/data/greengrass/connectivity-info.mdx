---
title: "aws_greengrass_connectivity_info"
description: "`aws_greengrass_connectivity_info` reads the real network connectivity information (endpoints) of a Greengrass core device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	greengrass "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/greengrass"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_greengrass_connectivity_info"})

    		ubx.Data(greengrass.ConnectivityInfo, "example", greengrass.ConnectivityInfoConfig{
    			ThingName: "example-thing-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectivityInfo } from "@ubx/sdk-aws/aws/data/greengrass/connectivity_info";

    export default stack("example", () => {
      intent({ summary: "look up aws_greengrass_connectivity_info" });

      data(ConnectivityInfo, "example", {
        thingName: "example-thing-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.greengrass import ConnectivityInfo, ConnectivityInfoConfig

    def describe():
        ubx.intent("look up aws_greengrass_connectivity_info")

        ubx.data(ConnectivityInfo, "example", ConnectivityInfoConfig(
            thing_name="example-thing-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="thing_name" type="string" required>
  The real name of the AWS IoT thing this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connectivity_info" type="list(object)">
  The real network connectivity information for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message" type="string">
  The real, human-readable message describing this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
