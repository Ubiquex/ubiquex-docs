---
title: "aws_apigatewayv2_product_pages"
description: "`aws_apigatewayv2_product_pages` reads the real, published pages of an API Gateway v2 developer portal product."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_product_pages"})

    		ubx.Data(apigatewayv2.ProductPages, "example", apigatewayv2.ProductPagesConfig{
    			PortalProductId:        "example",
    			MaxResults:             "example",
    			ResourceOwnerAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProductPages } from "@ubx/sdk-aws/aws/data/apigatewayv2/product_pages";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_product_pages" });

      data(ProductPages, "example", {
        portalProductId: "example",
        maxResults: "example",
        resourceOwnerAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import ProductPages, ProductPagesConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_product_pages")

        ubx.data(ProductPages, "example", ProductPagesConfig(
            portal_product_id="example",
            max_results="example",
            resource_owner_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="portal_product_id" type="string" required>
  The real, unique identifier of the portal product this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="resource_owner_account_id" type="string">
  The real, unique identifier of the resource owner account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
