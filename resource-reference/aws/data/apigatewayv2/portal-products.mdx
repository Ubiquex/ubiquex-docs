---
title: "aws_apigatewayv2_portal_products"
description: "`data_aws_apigatewayv2_portal_products` looks up a real, already-existing `aws_api_gateway_v2_portal_product`."
---

It publishes a real API, or a curated group of operations from it, as a self-service product in an API Gateway developer portal, so external or internal developers can discover, subscribe to, and get credentials for it without a support ticket.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_portal_products"})

    		ubx.Data(apigatewayv2.PortalProducts, "example", apigatewayv2.PortalProductsConfig{
    			MaxResults:    "example",
    			ResourceOwner: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PortalProducts } from "@ubx/sdk-aws/aws/data/apigatewayv2/portal_products";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_portal_products" });

      data(PortalProducts, "example", {
        maxResults: "example",
        resourceOwner: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import PortalProducts, PortalProductsConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_portal_products")

        ubx.data(PortalProducts, "example", PortalProductsConfig(
            max_results="example",
            resource_owner="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  The real owner of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
