---
title: "aws_apigatewayv2_authorizers"
description: "`data_aws_apigatewayv2_authorizers` looks up a real, already-existing `aws_api_gateway_v2_authorizer`."
---

It attaches a Lambda-, JWT-, or IAM-based access check to an `aws_api_gateway_v2_api`, evaluated before a request reaches its integration, the v2 counterpart to a v1 REST API authorizer.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_authorizers"})

    		ubx.Data(apigatewayv2.Authorizers, "example", apigatewayv2.AuthorizersConfig{
    			ApiId:      "example",
    			MaxResults: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Authorizers } from "@ubx/sdk-aws/aws/data/apigatewayv2/authorizers";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_authorizers" });

      data(Authorizers, "example", {
        apiId: "example",
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import Authorizers, AuthorizersConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_authorizers")

        ubx.data(Authorizers, "example", AuthorizersConfig(
            api_id="example",
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
