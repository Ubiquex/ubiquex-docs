---
title: "aws_apigatewayv2_portal_product_sharing_policy"
description: "`aws_apigatewayv2_portal_product_sharing_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigatewayv2_portal_product_sharing_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_portal_product_sharing_policy"})

    		ubx.Data(apigatewayv2.PortalProductSharingPolicy, "example", apigatewayv2.PortalProductSharingPolicyConfig{
    			PortalProductId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PortalProductSharingPolicy } from "@ubx/sdk-aws/aws/data/apigatewayv2/portal_product_sharing_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_portal_product_sharing_policy" });

      data(PortalProductSharingPolicy, "example", {
        portalProductId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import PortalProductSharingPolicy, PortalProductSharingPolicyConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_portal_product_sharing_policy")

        ubx.data(PortalProductSharingPolicy, "example", PortalProductSharingPolicyConfig(
            portal_product_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="portal_product_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_document" type="string">
  Returned by the lookup.
</ResponseField>
