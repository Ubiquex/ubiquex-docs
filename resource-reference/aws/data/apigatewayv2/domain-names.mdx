---
title: "aws_apigatewayv2_domain_names"
description: "`data_aws_apigatewayv2_domain_names` looks up a real, already-existing `aws_api_gateway_domain_name_v2`."
---

It is the newer resource type for the same custom-domain concept as `aws_api_gateway_domain_name`, supporting multi-level base path mappings that the original resource type doesn't.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_domain_names"})

    		ubx.Data(apigatewayv2.DomainNames, "example", apigatewayv2.DomainNamesConfig{
    			MaxResults: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DomainNames } from "./local-sdk/aws/sdk/typescript/aws/data/apigatewayv2/domain_names.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_domain_names" });

      data(DomainNames, "example", {
        maxResults: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import DomainNames, DomainNamesConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_domain_names")

        ubx.data(DomainNames, "example", DomainNamesConfig(
            max_results="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="string">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
