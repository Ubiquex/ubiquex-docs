---
title: "aws_apigatewayv2_model_template"
description: "`aws_apigatewayv2_model_template` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_apigatewayv2_model_template` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_model_template"})

    		ubx.Data(apigatewayv2.ModelTemplate, "example", apigatewayv2.ModelTemplateConfig{
    			ApiId:   "example",
    			ModelId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelTemplate } from "@ubx/sdk-aws/aws/data/apigatewayv2/model_template";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_model_template" });

      data(ModelTemplate, "example", {
        apiId: "example",
        modelId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import ModelTemplate, ModelTemplateConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_model_template")

        ubx.data(ModelTemplate, "example", ModelTemplateConfig(
            api_id="example",
            model_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="model_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="string">
  Returned by the lookup.
</ResponseField>
