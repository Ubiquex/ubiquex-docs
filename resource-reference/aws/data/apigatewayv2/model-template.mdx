---
title: "aws_apigatewayv2_model_template"
description: "`data_aws_apigatewayv2_model_template` looks up a real, already-existing `aws_api_gateway_v2_model`."
---

It is a JSON Schema definition attached to an `aws_api_gateway_v2_api`, the v2 counterpart to a v1 REST API model, describing the expected shape of a request or response body.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_apigatewayv2_model_template"})

    		ubx.Data(apigatewayv2.ModelTemplate, "example", apigatewayv2.ModelTemplateConfig{
    			ApiId:   "example",
    			ModelId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelTemplate } from "./local-sdk/aws/sdk/typescript/aws/data/apigatewayv2/model_template.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_apigatewayv2_model_template" });

      data(ModelTemplate, "example", {
        apiId: "example",
        modelId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.apigatewayv2 import ModelTemplate, ModelTemplateConfig

    def describe():
        ubx.intent("look up aws_apigatewayv2_model_template")

        ubx.data(ModelTemplate, "example", ModelTemplateConfig(
            api_id="example",
            model_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="api_id" type="string" required>
  The real, unique identifier of the API this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="model_id" type="string" required>
  The real, unique identifier of the model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="value" type="string">
  The real, current value of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
