---
title: "aws_route53_recovery_control_config_routing_controls"
description: "`aws_route53_recovery_control_config_routing_controls` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_recovery_control_config_routing_controls` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53_recovery_control_config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53_recovery_control_config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_recovery_control_config_routing_controls"})

    		ubx.Data(route53_recovery_control_config.Route53RecoveryControlConfigRoutingControls, "example", route53_recovery_control_config.Route53RecoveryControlConfigRoutingControlsConfig{
    			ControlPanelArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Route53RecoveryControlConfigRoutingControls } from "@ubx/sdk-aws/aws/data/route53_recovery_control_config/route53_recovery_control_config_routing_controls";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53_recovery_control_config_routing_controls",
      });

      data(Route53RecoveryControlConfigRoutingControls, "example", {
        controlPanelArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53_recovery_control_config import Route53RecoveryControlConfigRoutingControls, Route53RecoveryControlConfigRoutingControlsConfig

    def describe():
        ubx.intent("look up aws_route53_recovery_control_config_routing_controls")

        ubx.data(Route53RecoveryControlConfigRoutingControls, "example", Route53RecoveryControlConfigRoutingControlsConfig(
            control_panel_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="control_panel_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="routing_controls" type="list(object)">
  Returned by the lookup.
</ResponseField>
