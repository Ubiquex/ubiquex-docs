---
title: "aws_route53_recovery_control_config_clusters"
description: "`aws_route53_recovery_control_config_clusters` looks up a real Recovery Control cluster..."
---

`aws_route53_recovery_control_config_clusters` looks up a real Recovery Control cluster, a highly available set of five Regional endpoints for toggling routing controls.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53_recovery_control_config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53_recovery_control_config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_recovery_control_config_clusters"})

    		ubx.Data(route53_recovery_control_config.Route53RecoveryControlConfigClusters, "example", route53_recovery_control_config.Route53RecoveryControlConfigClustersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Route53RecoveryControlConfigClusters } from "./local-sdk/aws/sdk/typescript/aws/data/route53_recovery_control_config/route53_recovery_control_config_clusters.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53_recovery_control_config_clusters" });

      data(Route53RecoveryControlConfigClusters, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53_recovery_control_config import Route53RecoveryControlConfigClusters, Route53RecoveryControlConfigClustersConfig

    def describe():
        ubx.intent("look up aws_route53_recovery_control_config_clusters")

        ubx.data(Route53RecoveryControlConfigClusters, "example", Route53RecoveryControlConfigClustersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="clusters" type="list(object)">
  The real clusters included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
