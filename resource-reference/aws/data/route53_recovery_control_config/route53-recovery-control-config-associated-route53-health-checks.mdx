---
title: "aws_route53_recovery_control_config_associated_route53_health_checks"
description: "`aws_route53_recovery_control_config_associated_route53_health_checks` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53_recovery_control_config_associated_route53_health_checks` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53_recovery_control_config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53_recovery_control_config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53_recovery_control_config_associated_route53_health_checks"})

    		ubx.Data(route53_recovery_control_config.Route53RecoveryControlConfigAssociatedRoute53HealthChecks, "example", route53_recovery_control_config.Route53RecoveryControlConfigAssociatedRoute53HealthChecksConfig{
    			RoutingControlArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Route53RecoveryControlConfigAssociatedRoute53HealthChecks } from "@ubx/sdk-aws/aws/data/route53_recovery_control_config/route53_recovery_control_config_associated_route53_health_checks";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_route53_recovery_control_config_associated_route53_health_checks",
      });

      data(Route53RecoveryControlConfigAssociatedRoute53HealthChecks, "example", {
        routingControlArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53_recovery_control_config import Route53RecoveryControlConfigAssociatedRoute53HealthChecks, Route53RecoveryControlConfigAssociatedRoute53HealthChecksConfig

    def describe():
        ubx.intent("look up aws_route53_recovery_control_config_associated_route53_health_checks")

        ubx.data(Route53RecoveryControlConfigAssociatedRoute53HealthChecks, "example", Route53RecoveryControlConfigAssociatedRoute53HealthChecksConfig(
            routing_control_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="routing_control_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="health_check_ids" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
