---
title: "aws_monitoring_metric_stream"
description: "`aws_monitoring_metric_stream` looks up one real CloudWatch metric stream, continuously exporting metrics to a Kinesis Data Firehose destination."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metric_stream"})

    		ubx.Data(monitoring.MetricStream, "example", monitoring.MetricStreamConfig{
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetricStream } from "@ubx/sdk-aws/aws/data/monitoring/metric_stream";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metric_stream" });

      data(MetricStream, "example", {
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import MetricStream, MetricStreamConfig

    def describe():
        ubx.intent("look up aws_monitoring_metric_stream")

        ubx.data(MetricStream, "example", MetricStreamConfig(
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="exclude_filters" type="list(object)">
  The real filters applied to exclude matching results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="firehose_arn" type="string">
  The real Amazon Resource Name (ARN) of the firehose this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="include_filters" type="list(object)">
  The real filters applied to restrict which items are included. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="include_linked_accounts_metrics" type="bool">
  Whether metrics from linked accounts are included in the response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_update_date" type="string">
  The real date of the last update. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="output_format" type="string">
  The real, requested output format of the response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="statistics_configurations" type="list(object)">
  The real statistics configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
