---
title: "aws_monitoring_alarm_mute_rule"
description: "`aws_monitoring_alarm_mute_rule` looks up one real CloudWatch alarm mute rule, temporarily suppressing notifications during planned maintenance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_alarm_mute_rule"})

    		ubx.Data(monitoring.AlarmMuteRule, "example", monitoring.AlarmMuteRuleConfig{
    			AlarmMuteRuleName: "example-alarm-mute-rule-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlarmMuteRule } from "@ubx/sdk-aws/aws/data/monitoring/alarm_mute_rule";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_alarm_mute_rule" });

      data(AlarmMuteRule, "example", {
        alarmMuteRuleName: "example-alarm-mute-rule-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import AlarmMuteRule, AlarmMuteRuleConfig

    def describe():
        ubx.intent("look up aws_monitoring_alarm_mute_rule")

        ubx.data(AlarmMuteRule, "example", AlarmMuteRuleConfig(
            alarm_mute_rule_name="example-alarm-mute-rule-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alarm_mute_rule_name" type="string" required>
  The real name of the alarm mute rule this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alarm_mute_rule_arn" type="string">
  The real Amazon Resource Name (ARN) of the alarm mute rule this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expire_date" type="string">
  The real date of the expire. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_timestamp" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="mute_targets" type="object">
  &lt;p&gt;Specifies which alarms an alarm mute rule applies to.&lt;/p&gt; &lt;p&gt;You can target up to 100 specific alarms by name. When a mute rule is active, the targeted alarms continue to evaluate metrics and transition between states, but their configured actions are muted.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="alarm_names" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mute_type" type="string">
  The real, discriminating type of the mute this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule" type="object">
  &lt;p&gt;Defines the schedule configuration for an alarm mute rule.&lt;/p&gt; &lt;p&gt;The rule contains a schedule that specifies when and how long alarms should be muted. The schedule can be a recurring pattern using cron expressions or a one-time mute window using at expressions.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="schedule" type="object" required>
      &lt;p&gt;Specifies when and how long an alarm mute rule is active.&lt;/p&gt; &lt;p&gt;The schedule uses either a cron expression for recurring mute windows or an at expression for one-time mute windows. When the schedule activates, the mute rule mutes alarm actions for the specified duration.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="duration" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="expression" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="timezone" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_date" type="string">
  The earliest real date to include in the results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
