---
title: "aws_monitoring_anomaly_detectors"
description: "`aws_monitoring_anomaly_detectors` looks up a real CloudWatch anomaly detector, modeling a metric's own expected value range over time."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_anomaly_detectors"})

    		ubx.Data(monitoring.AnomalyDetectors, "example", monitoring.AnomalyDetectorsConfig{
    			AnomalyDetectorIds:   []string{"example"},
    			AnomalyDetectorTypes: []string{"example"},
    			Dimensions:           []map[string]any{{"name": "example-name"}},
    			MaxResults:           1,
    			MetricName:           "example-metric-name",
    			Namespace:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AnomalyDetectors } from "@ubx/sdk-aws/aws/data/monitoring/anomaly_detectors";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_anomaly_detectors" });

      data(AnomalyDetectors, "example", {
        anomalyDetectorIds: ["example"],
        anomalyDetectorTypes: ["example"],
        dimensions: [{ name: "example-name" }],
        maxResults: 1,
        metricName: "example-metric-name",
        namespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import AnomalyDetectors, AnomalyDetectorsConfig

    def describe():
        ubx.intent("look up aws_monitoring_anomaly_detectors")

        ubx.data(AnomalyDetectors, "example", AnomalyDetectorsConfig(
            anomaly_detector_ids=["example"],
            anomaly_detector_types=["example"],
            dimensions=[{"name": "example-name"}],
            max_results=1,
            metric_name="example-metric-name",
            namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="anomaly_detector_ids" type="list(string)">
  The real, unique identifiers of the anomaly detectors this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="anomaly_detector_types" type="list(string)">
  The real, discriminating types of anomaly detectors this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="dimensions" type="list(object)">
  The real, structured dimensions of this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string">
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="anomaly_detectors" type="list(object)">
  The real anomaly detectors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
