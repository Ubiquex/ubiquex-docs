---
title: "aws_monitoring_alarms_for_metric"
description: "`aws_monitoring_alarms_for_metric` reads the real CloudWatch alarms currently watching a given metric."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_alarms_for_metric"})

    		ubx.Data(monitoring.AlarmsForMetric, "example", monitoring.AlarmsForMetricConfig{
    			MetricName:        "example-metric-name",
    			Namespace:         "example",
    			Dimensions:        []map[string]any{{"name": "example-name"}},
    			ExtendedStatistic: "example",
    			Period:            1,
    			Statistic:         "example",
    			Unit:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlarmsForMetric } from "@ubx/sdk-aws/aws/data/monitoring/alarms_for_metric";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_alarms_for_metric" });

      data(AlarmsForMetric, "example", {
        metricName: "example-metric-name",
        namespace: "example",
        dimensions: [{ name: "example-name" }],
        extendedStatistic: "example",
        period: 1,
        statistic: "example",
        unit: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import AlarmsForMetric, AlarmsForMetricConfig
    from ubx.aws.data.monitoring.alarms_for_metric import AlarmsForMetric_Dimensions

    def describe():
        ubx.intent("look up aws_monitoring_alarms_for_metric")

        ubx.data(AlarmsForMetric, "example", AlarmsForMetricConfig(
            metric_name="example-metric-name",
            namespace="example",
            dimensions=[AlarmsForMetric_Dimensions(name="example-name")],
            extended_statistic="example",
            period=1,
            statistic="example",
            unit="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dimensions" type="list(object)">
  The real, structured dimensions of this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="extended_statistic" type="string">
  The real, extended statistic (such as a percentile) computed for this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  The real namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number">
  The real time period this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="statistic" type="string">
  The real statistic (such as average or sum) computed for this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="unit" type="string">
  The real unit of measurement for this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_alarms" type="list(object)">
  The real CloudWatch metric alarms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
