---
title: "aws_monitoring_managed_insight_rules"
description: "`aws_monitoring_managed_insight_rules` reads the real, AWS-managed CloudWatch Contributor Insights rules available for a service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_managed_insight_rules"})

    		ubx.Data(monitoring.ManagedInsightRules, "example", monitoring.ManagedInsightRulesConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ManagedInsightRules } from "@ubx/sdk-aws/aws/data/monitoring/managed_insight_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_managed_insight_rules" });

      data(ManagedInsightRules, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import ManagedInsightRules, ManagedInsightRulesConfig

    def describe():
        ubx.intent("look up aws_monitoring_managed_insight_rules")

        ubx.data(ManagedInsightRules, "example", ManagedInsightRulesConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="managed_rules" type="list(object)">
  The real rules for the managed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
