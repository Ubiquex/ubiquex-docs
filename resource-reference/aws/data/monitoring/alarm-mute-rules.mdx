---
title: "aws_monitoring_alarm_mute_rules"
description: "`aws_monitoring_alarm_mute_rules` reads the real CloudWatch alarm mute rules on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_alarm_mute_rules"})

    		ubx.Data(monitoring.AlarmMuteRules, "example", monitoring.AlarmMuteRulesConfig{
    			AlarmName:  "example-alarm-name",
    			MaxRecords: 1,
    			Statuses:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AlarmMuteRules } from "./local-sdk/aws/sdk/typescript/aws/data/monitoring/alarm_mute_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_alarm_mute_rules" });

      data(AlarmMuteRules, "example", {
        alarmName: "example-alarm-name",
        maxRecords: 1,
        statuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import AlarmMuteRules, AlarmMuteRulesConfig

    def describe():
        ubx.intent("look up aws_monitoring_alarm_mute_rules")

        ubx.data(AlarmMuteRules, "example", AlarmMuteRulesConfig(
            alarm_name="example-alarm-name",
            max_records=1,
            statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alarm_name" type="string">
  The real name of the alarm this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="statuses" type="list(string)">
  The real, current statuses of the objects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alarm_mute_rule_summaries" type="list(object)">
  The real, summarized alarm mute rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
