---
title: "aws_monitoring_alarm_mute_rules"
description: "`aws_monitoring_alarm_mute_rules` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_monitoring_alarm_mute_rules` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_alarm_mute_rules"})

    		ubx.Data(monitoring.AlarmMuteRules, "example", monitoring.AlarmMuteRulesConfig{
    			AlarmName:  "example-alarm-name",
    			MaxRecords: 1,
    			Statuses:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlarmMuteRules } from "@ubx/sdk-aws/aws/data/monitoring/alarm_mute_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_alarm_mute_rules" });

      data(AlarmMuteRules, "example", {
        alarmName: "example-alarm-name",
        maxRecords: 1,
        statuses: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import AlarmMuteRules, AlarmMuteRulesConfig

    def describe():
        ubx.intent("look up aws_monitoring_alarm_mute_rules")

        ubx.data(AlarmMuteRules, "example", AlarmMuteRulesConfig(
            alarm_name="example-alarm-name",
            max_records=1,
            statuses=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alarm_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="statuses" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alarm_mute_rule_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
