---
title: "aws_monitoring_metric_data"
description: "`aws_monitoring_metric_data` reads real, reported time-series data points for a CloudWatch metric."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metric_data"})

    		ubx.Data(monitoring.MetricData, "example", monitoring.MetricDataConfig{
    			EndTime:           "example",
    			MetricDataQueries: []map[string]any{{"account_id": "example"}},
    			StartTime:         "example",
    			LabelOptions:      map[string]any{"timezone": "example"},
    			MaxDatapoints:     1,
    			ScanBy:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetricData } from "@ubx/sdk-aws/aws/data/monitoring/metric_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metric_data" });

      data(MetricData, "example", {
        endTime: "example",
        metricDataQueries: [{ accountId: "example" }],
        startTime: "example",
        labelOptions: { timezone: "example" },
        maxDatapoints: 1,
        scanBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import MetricData, MetricDataConfig
    from ubx.aws.data.monitoring.metric_data import MetricData_MetricDataQueries, MetricData_LabelOptions

    def describe():
        ubx.intent("look up aws_monitoring_metric_data")

        ubx.data(MetricData, "example", MetricDataConfig(
            end_time="example",
            metric_data_queries=[MetricData_MetricDataQueries(account_id="example")],
            start_time="example",
            label_options=MetricData_LabelOptions(timezone="example"),
            max_datapoints=1,
            scan_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="label_options" type="object">
  &lt;p&gt;This structure includes the &lt;code&gt;Timezone&lt;/code&gt; parameter, which you can use to specify your time zone so that the labels that are associated with returned metrics display the correct time for your time zone. &lt;/p&gt; &lt;p&gt;The &lt;code&gt;Timezone&lt;/code&gt; value affects a label only if you have a time-based dynamic expression in the label. For more information about dynamic expressions in labels, see &lt;a href="https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/graph-dynamic-labels.html"&gt;Using Dynamic Labels&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="timezone" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_datapoints" type="number">
  The real, maximum number of data points to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_data_queries" type="list(object)" required>
  The real CloudWatch metric data queries included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="scan_by" type="string">
  The real field results are scanned by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="messages" type="list(object)">
  The real messages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metric_data_results" type="list(object)">
  The real metric data results included in this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
