---
title: "aws_monitoring_otel_enrichment"
description: "`aws_monitoring_otel_enrichment` reads the real OpenTelemetry enrichment configuration applied to CloudWatch metrics."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_otel_enrichment"})

    		ubx.Data(monitoring.OtelEnrichment, "example", monitoring.OtelEnrichmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OtelEnrichment } from "@ubx/sdk-aws/aws/data/monitoring/otel_enrichment";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_otel_enrichment" });

      data(OtelEnrichment, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import OtelEnrichment, OtelEnrichmentConfig

    def describe():
        ubx.intent("look up aws_monitoring_otel_enrichment")

        ubx.data(OtelEnrichment, "example", OtelEnrichmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="status" type="string">
  &lt;p&gt;The status of OTel enrichment for the account.&lt;/p&gt; Returned by the lookup.
</ResponseField>
