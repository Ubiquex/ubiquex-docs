---
title: "aws_monitoring_alarms"
description: "`aws_monitoring_alarms` looks up a real CloudWatch alarm."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_alarms"})

    		ubx.Data(monitoring.Alarms, "example", monitoring.AlarmsConfig{
    			ActionPrefix:        "example",
    			AlarmNamePrefix:     "example",
    			AlarmNames:          []string{"example"},
    			AlarmTypes:          []string{"example"},
    			ChildrenOfAlarmName: "example-children-of-alarm-name",
    			MaxRecords:          1,
    			ParentsOfAlarmName:  "example-parents-of-alarm-name",
    			StateValue:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Alarms } from "@ubx/sdk-aws/aws/data/monitoring/alarms";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_alarms" });

      data(Alarms, "example", {
        actionPrefix: "example",
        alarmNamePrefix: "example",
        alarmNames: ["example"],
        alarmTypes: ["example"],
        childrenOfAlarmName: "example-children-of-alarm-name",
        maxRecords: 1,
        parentsOfAlarmName: "example-parents-of-alarm-name",
        stateValue: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import Alarms, AlarmsConfig

    def describe():
        ubx.intent("look up aws_monitoring_alarms")

        ubx.data(Alarms, "example", AlarmsConfig(
            action_prefix="example",
            alarm_name_prefix="example",
            alarm_names=["example"],
            alarm_types=["example"],
            children_of_alarm_name="example-children-of-alarm-name",
            max_records=1,
            parents_of_alarm_name="example-parents-of-alarm-name",
            state_value="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_prefix" type="string">
  The real S3 key prefix used for the action. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="alarm_name_prefix" type="string">
  The real S3 key prefix used for the alarm name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="alarm_names" type="list(string)">
  The real names of the alarms included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="alarm_types" type="list(string)">
  The real, discriminating types of alarms this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="children_of_alarm_name" type="string">
  The real name of the children of alarm this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_records" type="number">
  The maximum number of records to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="parents_of_alarm_name" type="string">
  The real name of the parents of alarm this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state_value" type="string">
  The real value of this object's own state. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="composite_alarms" type="list(object)">
  The real CloudWatch composite alarms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="log_alarms" type="list(object)">
  The real log-based alarms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metric_alarms" type="list(object)">
  The real CloudWatch metric alarms included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
