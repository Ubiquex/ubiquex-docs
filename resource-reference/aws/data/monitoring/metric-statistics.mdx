---
title: "aws_monitoring_metric_statistics"
description: "`aws_monitoring_metric_statistics` reads real, computed statistics (average, sum, min, max) for a CloudWatch metric over a time range."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metric_statistics"})

    		ubx.Data(monitoring.MetricStatistics, "example", monitoring.MetricStatisticsConfig{
    			EndTime:            "example",
    			MetricName:         "example-metric-name",
    			Namespace:          "example",
    			Period:             1,
    			StartTime:          "example",
    			Dimensions:         []map[string]any{{"name": "example-name"}},
    			ExtendedStatistics: []string{"example"},
    			Statistics:         []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetricStatistics } from "@ubx/sdk-aws/aws/data/monitoring/metric_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metric_statistics" });

      data(MetricStatistics, "example", {
        endTime: "example",
        metricName: "example-metric-name",
        namespace: "example",
        period: 1,
        startTime: "example",
        dimensions: [{ name: "example-name" }],
        extendedStatistics: ["example"],
        statistics: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import MetricStatistics, MetricStatisticsConfig

    def describe():
        ubx.intent("look up aws_monitoring_metric_statistics")

        ubx.data(MetricStatistics, "example", MetricStatisticsConfig(
            end_time="example",
            metric_name="example-metric-name",
            namespace="example",
            period=1,
            start_time="example",
            dimensions=[{"name": "example-name"}],
            extended_statistics=["example"],
            statistics=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dimensions" type="list(object)">
  The real, structured dimensions of this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="extended_statistics" type="list(string)">
  The real, extended statistics (such as percentiles) computed for this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  The real namespace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  The real time period this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="statistics" type="list(string)">
  The real, computed statistics included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="unit" type="string">
  The real unit of measurement for this value. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="datapoints" type="list(object)">
  The real, computed data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="label" type="string">
  The real, human-readable label of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
