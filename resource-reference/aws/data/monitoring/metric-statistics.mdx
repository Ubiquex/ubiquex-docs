---
title: "aws_monitoring_metric_statistics"
description: "`aws_monitoring_metric_statistics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_monitoring_metric_statistics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metric_statistics"})

    		ubx.Data(monitoring.MetricStatistics, "example", monitoring.MetricStatisticsConfig{
    			EndTime:            "example",
    			MetricName:         "example-metric-name",
    			Namespace:          "example",
    			Period:             1,
    			StartTime:          "example",
    			Dimensions:         []map[string]any{{"name": "example-name"}},
    			ExtendedStatistics: []string{"example"},
    			Statistics:         []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetricStatistics } from "@ubx/sdk-aws/aws/data/monitoring/metric_statistics";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metric_statistics" });

      data(MetricStatistics, "example", {
        endTime: "example",
        metricName: "example-metric-name",
        namespace: "example",
        period: 1,
        startTime: "example",
        dimensions: [{ name: "example-name" }],
        extendedStatistics: ["example"],
        statistics: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import MetricStatistics, MetricStatisticsConfig

    def describe():
        ubx.intent("look up aws_monitoring_metric_statistics")

        ubx.data(MetricStatistics, "example", MetricStatisticsConfig(
            end_time="example",
            metric_name="example-metric-name",
            namespace="example",
            period=1,
            start_time="example",
            dimensions=[{"name": "example-name"}],
            extended_statistics=["example"],
            statistics=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dimensions" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="extended_statistics" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="statistics" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="unit" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="datapoints" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="label" type="string">
  Returned by the lookup.
</ResponseField>
