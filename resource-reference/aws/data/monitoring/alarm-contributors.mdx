---
title: "aws_monitoring_alarm_contributors"
description: "`aws_monitoring_alarm_contributors` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_monitoring_alarm_contributors` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_alarm_contributors"})

    		ubx.Data(monitoring.AlarmContributors, "example", monitoring.AlarmContributorsConfig{
    			AlarmName: "example-alarm-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AlarmContributors } from "@ubx/sdk-aws/aws/data/monitoring/alarm_contributors";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_alarm_contributors" });

      data(AlarmContributors, "example", {
        alarmName: "example-alarm-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import AlarmContributors, AlarmContributorsConfig

    def describe():
        ubx.intent("look up aws_monitoring_alarm_contributors")

        ubx.data(AlarmContributors, "example", AlarmContributorsConfig(
            alarm_name="example-alarm-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alarm_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="alarm_contributors" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
