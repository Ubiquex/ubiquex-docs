---
title: "aws_monitoring_dataset"
description: "`aws_monitoring_dataset` reads a real, generated CloudWatch metrics dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_dataset"})

    		ubx.Data(monitoring.Dataset, "example", monitoring.DatasetConfig{
    			DatasetIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Dataset } from "./local-sdk/aws/sdk/typescript/aws/data/monitoring/dataset.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_dataset" });

      data(Dataset, "example", {
        datasetIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import Dataset, DatasetConfig

    def describe():
        ubx.intent("look up aws_monitoring_dataset")

        ubx.data(Dataset, "example", DatasetConfig(
            dataset_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_identifier" type="string" required>
  The real, unique identifier of the dataset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dataset_id" type="string">
  The real, unique identifier of the dataset this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS KMS key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
