---
title: "aws_monitoring_metric_widget_image"
description: "`aws_monitoring_metric_widget_image` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_monitoring_metric_widget_image` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metric_widget_image"})

    		ubx.Data(monitoring.MetricWidgetImage, "example", monitoring.MetricWidgetImageConfig{
    			MetricWidget: "example",
    			OutputFormat: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetricWidgetImage } from "@ubx/sdk-aws/aws/data/monitoring/metric_widget_image";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metric_widget_image" });

      data(MetricWidgetImage, "example", {
        metricWidget: "example",
        outputFormat: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import MetricWidgetImage, MetricWidgetImageConfig

    def describe():
        ubx.intent("look up aws_monitoring_metric_widget_image")

        ubx.data(MetricWidgetImage, "example", MetricWidgetImageConfig(
            metric_widget="example",
            output_format="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="metric_widget" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="output_format" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_widget_image" type="string">
  Returned by the lookup.
</ResponseField>
