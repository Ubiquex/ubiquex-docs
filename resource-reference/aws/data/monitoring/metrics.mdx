---
title: "aws_monitoring_metrics"
description: "`aws_monitoring_metrics` reads the real CloudWatch metrics published under a given namespace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metrics"})

    		ubx.Data(monitoring.Metrics, "example", monitoring.MetricsConfig{
    			Dimensions:            []map[string]any{{"name": "example-name"}},
    			IncludeLinkedAccounts: true,
    			MetricName:            "example-metric-name",
    			Namespace:             "example",
    			OwningAccount:         "example",
    			RecentlyActive:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Metrics } from "@ubx/sdk-aws/aws/data/monitoring/metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metrics" });

      data(Metrics, "example", {
        dimensions: [{ name: "example-name" }],
        includeLinkedAccounts: true,
        metricName: "example-metric-name",
        namespace: "example",
        owningAccount: "example",
        recentlyActive: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import Metrics, MetricsConfig
    from ubx.aws.data.monitoring.metrics import Metrics_Dimensions

    def describe():
        ubx.intent("look up aws_monitoring_metrics")

        ubx.data(Metrics, "example", MetricsConfig(
            dimensions=[Metrics_Dimensions(name="example-name")],
            include_linked_accounts=True,
            metric_name="example-metric-name",
            namespace="example",
            owning_account="example",
            recently_active="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dimensions" type="list(object)">
  The real, structured dimensions of this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_linked_accounts" type="bool">
  Whether results also include AWS Organizations member accounts linked to this account. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_name" type="string">
  The real name of the metric this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="namespace" type="string">
  The real namespace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owning_account" type="string">
  The real AWS account that owns this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recently_active" type="string">
  Whether this object has been recently active. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metrics" type="list(object)">
  The real metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="owning_accounts" type="list(string)">
  The real AWS accounts that own the objects included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
