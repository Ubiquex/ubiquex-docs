---
title: "aws_monitoring_metric_streams"
description: "`aws_monitoring_metric_streams` reads the real CloudWatch metric streams on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_metric_streams"})

    		ubx.Data(monitoring.MetricStreams, "example", monitoring.MetricStreamsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MetricStreams } from "@ubx/sdk-aws/aws/data/monitoring/metric_streams";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_metric_streams" });

      data(MetricStreams, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import MetricStreams, MetricStreamsConfig

    def describe():
        ubx.intent("look up aws_monitoring_metric_streams")

        ubx.data(MetricStreams, "example", MetricStreamsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="entries" type="list(object)">
  The real entries included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
