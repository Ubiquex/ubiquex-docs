---
title: "aws_monitoring_insight_rule_report"
description: "`aws_monitoring_insight_rule_report` reads a real, generated report from a CloudWatch Contributor Insights rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitoring "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/monitoring"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_monitoring_insight_rule_report"})

    		ubx.Data(monitoring.InsightRuleReport, "example", monitoring.InsightRuleReportConfig{
    			EndTime:             "example",
    			Period:              1,
    			RuleName:            "example-rule-name",
    			StartTime:           "example",
    			MaxContributorCount: 1,
    			Metrics:             []string{"example"},
    			OrderBy:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightRuleReport } from "@ubx/sdk-aws/aws/data/monitoring/insight_rule_report";

    export default stack("example", () => {
      intent({ summary: "look up aws_monitoring_insight_rule_report" });

      data(InsightRuleReport, "example", {
        endTime: "example",
        period: 1,
        ruleName: "example-rule-name",
        startTime: "example",
        maxContributorCount: 1,
        metrics: ["example"],
        orderBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.monitoring import InsightRuleReport, InsightRuleReportConfig

    def describe():
        ubx.intent("look up aws_monitoring_insight_rule_report")

        ubx.data(InsightRuleReport, "example", InsightRuleReportConfig(
            end_time="example",
            period=1,
            rule_name="example-rule-name",
            start_time="example",
            max_contributor_count=1,
            metrics=["example"],
            order_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_contributor_count" type="number">
  The real count of max contributors. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)">
  The real metrics included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order_by" type="string">
  The real field results are ordered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="period" type="number" required>
  The real time period this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  The real name of the rule this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregate_value" type="number">
  The real, aggregated value of this metric. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="aggregation_statistic" type="string">
  The real statistic (such as sum or average) used to aggregate this metric. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="approximate_unique_count" type="number">
  The real count of approximate uniques. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="contributors" type="list(object)">
  The real, top contributors included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="key_labels" type="list(string)">
  The real labels applied to this key. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metric_datapoints" type="list(object)">
  The real metric data points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
