---
title: "aws_finspace_api_users"
description: "`aws_finspace_api_users` looks up a real FinSpace user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	finspace_api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace_api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_api_users"})

    		ubx.Data(finspace_api.FinspaceApiUsers, "example", finspace_api.FinspaceApiUsersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FinspaceApiUsers } from "@ubx/sdk-aws/aws/data/finspace_api/finspace_api_users";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_api_users" });

      data(FinspaceApiUsers, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.finspace_api import FinspaceApiUsers, FinspaceApiUsersConfig

    def describe():
        ubx.intent("look up aws_finspace_api_users")

        ubx.data(FinspaceApiUsers, "example", FinspaceApiUsersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  The real users included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
