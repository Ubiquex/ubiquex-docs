---
title: "aws_finspace_api_permission_groups_by_user"
description: "`aws_finspace_api_permission_groups_by_user` reads the real FinSpace permission groups a given user belongs to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	finspace_api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace_api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_api_permission_groups_by_user"})

    		ubx.Data(finspace_api.FinspaceApiPermissionGroupsByUser, "example", finspace_api.FinspaceApiPermissionGroupsByUserConfig{
    			MaxResults: 1,
    			UserId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FinspaceApiPermissionGroupsByUser } from "./local-sdk/aws/sdk/typescript/aws/data/finspace_api/finspace_api_permission_groups_by_user.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_api_permission_groups_by_user" });

      data(FinspaceApiPermissionGroupsByUser, "example", {
        maxResults: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.finspace_api import FinspaceApiPermissionGroupsByUser, FinspaceApiPermissionGroupsByUserConfig

    def describe():
        ubx.intent("look up aws_finspace_api_permission_groups_by_user")

        ubx.data(FinspaceApiPermissionGroupsByUser, "example", FinspaceApiPermissionGroupsByUserConfig(
            max_results=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permission_groups" type="list(object)">
  The real permission groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
