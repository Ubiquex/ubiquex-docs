---
title: "aws_finspace_api_users_by_permission_group"
description: "`aws_finspace_api_users_by_permission_group` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_finspace_api_users_by_permission_group` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	finspace_api "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/finspace_api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_api_users_by_permission_group"})

    		ubx.Data(finspace_api.FinspaceApiUsersByPermissionGroup, "example", finspace_api.FinspaceApiUsersByPermissionGroupConfig{
    			MaxResults:        1,
    			PermissionGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FinspaceApiUsersByPermissionGroup } from "@ubx/sdk-aws/aws/data/finspace_api/finspace_api_users_by_permission_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_api_users_by_permission_group" });

      data(FinspaceApiUsersByPermissionGroup, "example", {
        maxResults: 1,
        permissionGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.finspace_api import FinspaceApiUsersByPermissionGroup, FinspaceApiUsersByPermissionGroupConfig

    def describe():
        ubx.intent("look up aws_finspace_api_users_by_permission_group")

        ubx.data(FinspaceApiUsersByPermissionGroup, "example", FinspaceApiUsersByPermissionGroupConfig(
            max_results=1,
            permission_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="permission_group_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="users" type="list(object)">
  Returned by the lookup.
</ResponseField>
