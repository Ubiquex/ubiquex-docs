---
title: "aws_finspace_api_permission_groups"
description: "`aws_finspace_api_permission_groups` looks up a real FinSpace permission group, controlling which users can access which datasets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	finspace_api "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/finspace_api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_finspace_api_permission_groups"})

    		ubx.Data(finspace_api.FinspaceApiPermissionGroups, "example", finspace_api.FinspaceApiPermissionGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FinspaceApiPermissionGroups } from "@ubx/sdk-aws/aws/data/finspace_api/finspace_api_permission_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_finspace_api_permission_groups" });

      data(FinspaceApiPermissionGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.finspace_api import FinspaceApiPermissionGroups, FinspaceApiPermissionGroupsConfig

    def describe():
        ubx.intent("look up aws_finspace_api_permission_groups")

        ubx.data(FinspaceApiPermissionGroups, "example", FinspaceApiPermissionGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number" required>
  The maximum number of results to return in a single page. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permission_groups" type="list(object)">
  The real permission groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
