---
title: "aws_ssm_incidents_related_items"
description: "`aws_ssm_incidents_related_items` reads the real related items (a runbook, a metric, a resource) attached to an Incident Manager incident."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_incidents "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_incidents"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_incidents_related_items"})

    		ubx.Data(ssm_incidents.SsmIncidentsRelatedItems, "example", ssm_incidents.SsmIncidentsRelatedItemsConfig{
    			IncidentRecordArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmIncidentsRelatedItems } from "@ubx/sdk-aws/aws/data/ssm_incidents/ssm_incidents_related_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_incidents_related_items" });

      data(SsmIncidentsRelatedItems, "example", {
        incidentRecordArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_incidents import SsmIncidentsRelatedItems, SsmIncidentsRelatedItemsConfig

    def describe():
        ubx.intent("look up aws_ssm_incidents_related_items")

        ubx.data(SsmIncidentsRelatedItems, "example", SsmIncidentsRelatedItemsConfig(
            incident_record_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_record_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the incident record this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="related_items" type="list(object)">
  The real items related to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
