---
title: "aws_ssm_incidents_response_plans"
description: "`aws_ssm_incidents_response_plans` looks up a real Incident Manager response plan, defining engagement and runbook actions for a new incident."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_incidents "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_incidents"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_incidents_response_plans"})

    		ubx.Data(ssm_incidents.SsmIncidentsResponsePlans, "example", ssm_incidents.SsmIncidentsResponsePlansConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmIncidentsResponsePlans } from "@ubx/sdk-aws/aws/data/ssm_incidents/ssm_incidents_response_plans";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_incidents_response_plans" });

      data(SsmIncidentsResponsePlans, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_incidents import SsmIncidentsResponsePlans, SsmIncidentsResponsePlansConfig

    def describe():
        ubx.intent("look up aws_ssm_incidents_response_plans")

        ubx.data(SsmIncidentsResponsePlans, "example", SsmIncidentsResponsePlansConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="response_plan_summaries" type="list(object)">
  The real, summarized response plans included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
