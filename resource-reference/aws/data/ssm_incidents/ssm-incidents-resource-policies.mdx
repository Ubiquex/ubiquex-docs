---
title: "aws_ssm_incidents_resource_policies"
description: "`aws_ssm_incidents_resource_policies` reads the real resource-based policies attached to an Incident Manager response plan."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_incidents "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_incidents"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_incidents_resource_policies"})

    		ubx.Data(ssm_incidents.SsmIncidentsResourcePolicies, "example", ssm_incidents.SsmIncidentsResourcePoliciesConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmIncidentsResourcePolicies } from "@ubx/sdk-aws/aws/data/ssm_incidents/ssm_incidents_resource_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_incidents_resource_policies" });

      data(SsmIncidentsResourcePolicies, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_incidents import SsmIncidentsResourcePolicies, SsmIncidentsResourcePoliciesConfig

    def describe():
        ubx.intent("look up aws_ssm_incidents_resource_policies")

        ubx.data(SsmIncidentsResourcePolicies, "example", SsmIncidentsResourcePoliciesConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_policies" type="list(object)">
  The real, resource-based access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
