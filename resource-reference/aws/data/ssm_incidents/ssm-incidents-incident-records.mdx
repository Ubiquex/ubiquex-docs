---
title: "aws_ssm_incidents_incident_records"
description: "`aws_ssm_incidents_incident_records` reads the real Incident Manager incident records on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_incidents "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_incidents"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_incidents_incident_records"})

    		ubx.Data(ssm_incidents.SsmIncidentsIncidentRecords, "example", ssm_incidents.SsmIncidentsIncidentRecordsConfig{
    			Filters:    []map[string]any{{"condition": map[string]any{"after": "example"}}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmIncidentsIncidentRecords } from "@ubx/sdk-aws/aws/data/ssm_incidents/ssm_incidents_incident_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_incidents_incident_records" });

      data(SsmIncidentsIncidentRecords, "example", {
        filters: [{ condition: { after: "example" } }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_incidents import SsmIncidentsIncidentRecords, SsmIncidentsIncidentRecordsConfig

    def describe():
        ubx.intent("look up aws_ssm_incidents_incident_records")

        ubx.data(SsmIncidentsIncidentRecords, "example", SsmIncidentsIncidentRecordsConfig(
            filters=[{"condition": {"after": "example"}}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="incident_record_summaries" type="list(object)">
  The real, summarized incident records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
