---
title: "aws_ssm_incidents_incident_findings"
description: "`aws_ssm_incidents_incident_findings` reads real, correlated findings (such as a related deployment) surfaced for an Incident Manager incident."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_incidents "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/ssm_incidents"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_incidents_incident_findings"})

    		ubx.Data(ssm_incidents.SsmIncidentsIncidentFindings, "example", ssm_incidents.SsmIncidentsIncidentFindingsConfig{
    			IncidentRecordArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmIncidentsIncidentFindings } from "@ubx/sdk-aws/aws/data/ssm_incidents/ssm_incidents_incident_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_incidents_incident_findings" });

      data(SsmIncidentsIncidentFindings, "example", {
        incidentRecordArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_incidents import SsmIncidentsIncidentFindings, SsmIncidentsIncidentFindingsConfig

    def describe():
        ubx.intent("look up aws_ssm_incidents_incident_findings")

        ubx.data(SsmIncidentsIncidentFindings, "example", SsmIncidentsIncidentFindingsConfig(
            incident_record_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_record_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the incident record this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="findings" type="list(object)">
  The real findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
