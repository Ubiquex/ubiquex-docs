---
title: "aws_iotsitewise_gateways"
description: "`aws_iotsitewise_gateways` looks up a real IoT SiteWise Edge gateway, collecting data from on-premises industrial equipment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_gateways"})

    		ubx.Data(iotsitewise.Gateways, "example", iotsitewise.GatewaysConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Gateways } from "@ubx/sdk-aws/aws/data/iotsitewise/gateways";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_gateways" });

      data(Gateways, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Gateways, GatewaysConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_gateways")

        ubx.data(Gateways, "example", GatewaysConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="gateway_summaries" type="list(object)">
  The real, summarized gateways included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
