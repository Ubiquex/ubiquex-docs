---
title: "aws_iotsitewise_project_assets"
description: "`aws_iotsitewise_project_assets` reads the real assets assigned to an IoT SiteWise Monitor project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_project_assets"})

    		ubx.Data(iotsitewise.ProjectAssets, "example", iotsitewise.ProjectAssetsConfig{
    			ProjectId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProjectAssets } from "@ubx/sdk-aws/aws/data/iotsitewise/project_assets";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_project_assets" });

      data(ProjectAssets, "example", {
        projectId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import ProjectAssets, ProjectAssetsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_project_assets")

        ubx.data(ProjectAssets, "example", ProjectAssetsConfig(
            project_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  The real, unique identifier of the project this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_ids" type="list(string)">
  The real, unique identifiers of the assets this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
