---
title: "aws_iotsitewise_action"
description: "`aws_iotsitewise_action` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotsitewise_action` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_action"})

    		ubx.Data(iotsitewise.Action, "example", iotsitewise.ActionConfig{
    			ActionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Action } from "@ubx/sdk-aws/aws/data/iotsitewise/action";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_action" });

      data(Action, "example", {
        actionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Action, ActionConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_action")

        ubx.data(Action, "example", ActionConfig(
            action_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_definition_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="action_payload" type="object">
  &lt;p&gt;The JSON payload of the action.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="string_value" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="execution_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resolve_to" type="object">
  &lt;p&gt;The detailed resource this execution summary resolves to.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="asset_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_resource" type="object">
  &lt;p&gt;The resource the action will be taken on. This can include asset-based resources and computation model resources.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="asset_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="computation_model_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
