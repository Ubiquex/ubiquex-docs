---
title: "aws_iotsitewise_interface_relationships"
description: "`aws_iotsitewise_interface_relationships` reads the real relationships between IoT SiteWise asset models and the interfaces they implement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_interface_relationships"})

    		ubx.Data(iotsitewise.InterfaceRelationships, "example", iotsitewise.InterfaceRelationshipsConfig{
    			InterfaceAssetModelId: "example",
    			MaxResults:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InterfaceRelationships } from "@ubx/sdk-aws/aws/data/iotsitewise/interface_relationships";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_interface_relationships" });

      data(InterfaceRelationships, "example", {
        interfaceAssetModelId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import InterfaceRelationships, InterfaceRelationshipsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_interface_relationships")

        ubx.data(InterfaceRelationships, "example", InterfaceRelationshipsConfig(
            interface_asset_model_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="interface_asset_model_id" type="string" required>
  The real, unique identifier of the interface asset model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="interface_relationship_summaries" type="list(object)">
  The real, summarized interface relationships included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
