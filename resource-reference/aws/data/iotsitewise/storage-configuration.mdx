---
title: "aws_iotsitewise_storage_configuration"
description: "`aws_iotsitewise_storage_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotsitewise_storage_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_storage_configuration"})

    		ubx.Data(iotsitewise.StorageConfiguration, "example", iotsitewise.StorageConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StorageConfiguration } from "@ubx/sdk-aws/aws/data/iotsitewise/storage_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_storage_configuration" });

      data(StorageConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import StorageConfiguration, StorageConfigurationConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_storage_configuration")

        ubx.data(StorageConfiguration, "example", StorageConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration_status" type="object">
  &lt;p&gt;Contains current status information for the configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="error" type="object">
      &lt;p&gt;Contains the details of an IoT SiteWise configuration error.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="message" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="disallow_ingest_null_na_n" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="disassociated_data_storage" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_update_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="multi_layer_storage" type="object">
  &lt;p&gt;Contains information about the storage destination.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="customer_managed_s3_storage" type="object" required>
      &lt;p&gt;Contains information about a customer managed Amazon S3 bucket.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="role_arn" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="s3_resource_arn" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="retention_period" type="object">
  &lt;p&gt;The number of days your data is kept in the hot tier. By default, your data is kept indefinitely in the hot tier.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="number_of_days" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="unlimited" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="storage_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="warm_tier" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="warm_tier_retention_period" type="object">
  &lt;p&gt;Set this period to specify how long your data is stored in the warm tier before it is deleted. You can set this only if cold tier is enabled.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="number_of_days" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="unlimited" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
