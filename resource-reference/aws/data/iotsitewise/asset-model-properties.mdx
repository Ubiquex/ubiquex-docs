---
title: "aws_iotsitewise_asset_model_properties"
description: "`aws_iotsitewise_asset_model_properties` reads the real properties defined on an IoT SiteWise asset model."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_asset_model_properties"})

    		ubx.Data(iotsitewise.AssetModelProperties, "example", iotsitewise.AssetModelPropertiesConfig{
    			AssetModelId:      "example",
    			AssetModelVersion: "example",
    			Filter:            "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetModelProperties } from "@ubx/sdk-aws/aws/data/iotsitewise/asset_model_properties";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_asset_model_properties" });

      data(AssetModelProperties, "example", {
        assetModelId: "example",
        assetModelVersion: "example",
        filter: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AssetModelProperties, AssetModelPropertiesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_asset_model_properties")

        ubx.data(AssetModelProperties, "example", AssetModelPropertiesConfig(
            asset_model_id="example",
            asset_model_version="example",
            filter="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_model_id" type="string" required>
  The real, unique identifier of the asset model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="asset_model_version" type="string">
  The real version of the asset model. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_model_property_summaries" type="list(object)">
  The real, summarized asset model propertys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
