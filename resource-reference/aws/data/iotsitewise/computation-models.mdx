---
title: "aws_iotsitewise_computation_models"
description: "`aws_iotsitewise_computation_models` looks up a real IoT SiteWise computation model, defining a formula computed across one or more assets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_computation_models"})

    		ubx.Data(iotsitewise.ComputationModels, "example", iotsitewise.ComputationModelsConfig{
    			ComputationModelType: "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputationModels } from "@ubx/sdk-aws/aws/data/iotsitewise/computation_models";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_computation_models" });

      data(ComputationModels, "example", {
        computationModelType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import ComputationModels, ComputationModelsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_computation_models")

        ubx.data(ComputationModels, "example", ComputationModelsConfig(
            computation_model_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="computation_model_type" type="string">
  The real, discriminating type of the computation model this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="computation_model_summaries" type="list(object)">
  The real, summarized computation models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
