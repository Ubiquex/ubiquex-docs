---
title: "aws_iotsitewise_search"
description: "`aws_iotsitewise_search` searches for real IoT SiteWise resources matching a query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_search"})

    		ubx.Data(iotsitewise.Search, "example", iotsitewise.SearchConfig{
    			SearchId:      "example",
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Search } from "./local-sdk/aws/sdk/typescript/aws/data/iotsitewise/search.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_search" });

      data(Search, "example", {
        searchId: "example",
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Search, SearchConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_search")

        ubx.data(Search, "example", SearchConfig(
            search_id="example",
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="search_id" type="string" required>
  &lt;p&gt;The unique identifier of a search, assigned by the service when the search is started.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="group_id" type="string">
  &lt;p&gt;A caller-supplied identifier used to group related searches together.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="query_statement" type="string">
  &lt;p&gt;A natural-language query describing the data to search for. Treated as sensitive and not logged.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="search_type" type="string">
  &lt;p&gt;The search strategy, which trades off latency against recall. &lt;code&gt;DEEP&lt;/code&gt; runs the full semantic and structured search for the highest-quality matches; &lt;code&gt;QUICK&lt;/code&gt; returns faster, lower-recall results. When &lt;code&gt;searchType&lt;/code&gt; is omitted on a request, the search defaults to &lt;code&gt;QUICK&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="started_at" type="string">
  The real timestamp this object started. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;The lifecycle status of a search.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="status_reason" type="string">
  The real, human-readable reason for the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>
