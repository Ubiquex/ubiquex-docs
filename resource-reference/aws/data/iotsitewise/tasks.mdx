---
title: "aws_iotsitewise_tasks"
description: "`aws_iotsitewise_tasks` reads the real, background tasks running within IoT SiteWise."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_tasks"})

    		ubx.Data(iotsitewise.Tasks, "example", iotsitewise.TasksConfig{
    			WorkspaceName: "example-workspace-name",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Tasks } from "@ubx/sdk-aws/aws/data/iotsitewise/tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_tasks" });

      data(Tasks, "example", {
        workspaceName: "example-workspace-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Tasks, TasksConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_tasks")

        ubx.data(Tasks, "example", TasksConfig(
            workspace_name="example-workspace-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;An opaque pagination token. Pass the value returned by a previous request to retrieve the next page of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="task_summaries" type="list(object)">
  &lt;p&gt;A list of task summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
