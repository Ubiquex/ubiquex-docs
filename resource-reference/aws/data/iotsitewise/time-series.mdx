---
title: "aws_iotsitewise_time_series"
description: "`aws_iotsitewise_time_series` looks up a real, unassociated time-series stream within IoT SiteWise, not yet tied to an asset property."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_time_series"})

    		ubx.Data(iotsitewise.TimeSeries, "example", iotsitewise.TimeSeriesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TimeSeries } from "@ubx/sdk-aws/aws/data/iotsitewise/time_series";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_time_series" });

      data(TimeSeries, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import TimeSeries, TimeSeriesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_time_series")

        ubx.data(TimeSeries, "example", TimeSeriesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="alias" type="string">
  The real, human-readable alias for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_id" type="string">
  The real, unique identifier of the asset this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_type" type="string">
  The real data type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="data_type_spec" type="string">
  The real, structured specification of this object's own data type. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="property_id" type="string">
  The real, unique identifier of the property this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="time_series_arn" type="string">
  The real Amazon Resource Name (ARN) of the time series this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="time_series_creation_date" type="string">
  The real date of the time series creation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="time_series_id" type="string">
  The real, unique identifier of the time series this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="time_series_last_update_date" type="string">
  The real date of the time series last update. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workspace_name" type="string">
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
