---
title: "aws_iotsitewise_enrichment_jobs"
description: "`aws_iotsitewise_enrichment_jobs` looks up a real IoT SiteWise data enrichment job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_enrichment_jobs"})

    		ubx.Data(iotsitewise.EnrichmentJobs, "example", iotsitewise.EnrichmentJobsConfig{
    			WorkspaceName: "example-workspace-name",
    			DatasetId:     "example",
    			EndDate:       "example",
    			JobType:       "example",
    			MaxResults:    1,
    			PropertyAlias: "example",
    			StartDate:     "example",
    			Status:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnrichmentJobs } from "@ubx/sdk-aws/aws/data/iotsitewise/enrichment_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_enrichment_jobs" });

      data(EnrichmentJobs, "example", {
        workspaceName: "example-workspace-name",
        datasetId: "example",
        endDate: "example",
        jobType: "example",
        maxResults: 1,
        propertyAlias: "example",
        startDate: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import EnrichmentJobs, EnrichmentJobsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_enrichment_jobs")

        ubx.data(EnrichmentJobs, "example", EnrichmentJobsConfig(
            workspace_name="example-workspace-name",
            dataset_id="example",
            end_date="example",
            job_type="example",
            max_results=1,
            property_alias="example",
            start_date="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_id" type="string">
  The real, unique identifier of the dataset this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_date" type="string">
  The latest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="job_type" type="string">
  &lt;p&gt;The type of enrichment job, derived from the job configuration union member&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="property_alias" type="string">
  The real, human-readable alias of the property this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string">
  The earliest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  &lt;p&gt;Status of an enrichment job throughout its lifecycle.&lt;/p&gt; &lt;p&gt;Status progression: PENDING → RUNNING → &#123;COMPLETED, FAILED, TIMED_OUT, CANCELLED&#125;&lt;/p&gt; &lt;ul&gt; &lt;li&gt;PENDING: Job has been accepted and is waiting to start processing&lt;/li&gt; &lt;li&gt;RUNNING: Job is actively processing video data to generate embeddings&lt;/li&gt; &lt;li&gt;COMPLETED: Job finished successfully; embeddings are available in IoT SiteWise&lt;/li&gt; &lt;li&gt;FAILED: Job encountered an error during processing&lt;/li&gt; &lt;li&gt;TIMED_OUT: Job exceeded the maximum processing time limit&lt;/li&gt; &lt;li&gt;CANCELLED: Job was cancelled via CancelEnrichmentJob&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Terminal states (job will not change status): COMPLETED, FAILED, TIMED_OUT, CANCELLED&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="time_series_id" type="string">
  The real, unique identifier of the time series this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  The real jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
