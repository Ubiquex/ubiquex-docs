---
title: "aws_iotsitewise_capture_data"
description: "`aws_iotsitewise_capture_data` reads real, captured time-series data submitted to IoT SiteWise."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_capture_data"})

    		ubx.Data(iotsitewise.CaptureData, "example", iotsitewise.CaptureDataConfig{
    			EndTime:        map[string]any{"time_in_seconds": 1},
    			StartTime:      map[string]any{"time_in_seconds": 1},
    			WorkspaceName:  "example-workspace-name",
    			FormatSettings: map[string]any{"frames_per_second": 1},
    			PropertyAlias:  "example",
    			TimeSeriesId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CaptureData } from "@ubx/sdk-aws/aws/data/iotsitewise/capture_data";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_capture_data" });

      data(CaptureData, "example", {
        endTime: { timeInSeconds: 1 },
        startTime: { timeInSeconds: 1 },
        workspaceName: "example-workspace-name",
        formatSettings: { framesPerSecond: 1 },
        propertyAlias: "example",
        timeSeriesId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import CaptureData, CaptureDataConfig
    from ubx.aws.data.iotsitewise.capture_data import CaptureData_EndTime, CaptureData_FormatSettings

    def describe():
        ubx.intent("look up aws_iotsitewise_capture_data")

        ubx.data(CaptureData, "example", CaptureDataConfig(
            end_time=CaptureData_EndTime(time_in_seconds=1),
            start_time=CaptureData_EndTime(time_in_seconds=1),
            workspace_name="example-workspace-name",
            format_settings=CaptureData_FormatSettings(frames_per_second=1),
            property_alias="example",
            time_series_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="object" required>
  &lt;p&gt;Contains a timestamp with optional nanosecond granularity.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="offset_in_nanos" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="time_in_seconds" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="format_settings" type="object">
  &lt;p&gt;Contains the output format configuration for video processing.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="frames_per_second" type="number">
      &lt;p&gt;A positive integer used for frames per second and video dimensions.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="height_in_pixels" type="number">
      &lt;p&gt;A positive integer used for frames per second and video dimensions.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="width_in_pixels" type="number">
      &lt;p&gt;A positive integer used for frames per second and video dimensions.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="property_alias" type="string">
  The real, human-readable alias of the property this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="object" required>
  &lt;p&gt;Contains a timestamp with optional nanosecond granularity.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="offset_in_nanos" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="time_in_seconds" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="time_series_id" type="string">
  The real, unique identifier of the time series this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="string">
  &lt;p&gt;A binary blob containing video data.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="data_type" type="string">
  &lt;p&gt;The allowed video data types.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for retrieving the next chunk of video data.&lt;/p&gt; Returned by the lookup.
</ResponseField>
