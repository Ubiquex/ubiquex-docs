---
title: "aws_iotsitewise_actions"
description: "`aws_iotsitewise_actions` reads the real IoT SiteWise actions on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_actions"})

    		ubx.Data(iotsitewise.Actions, "example", iotsitewise.ActionsConfig{
    			TargetResourceId:      "example",
    			TargetResourceType:    "example",
    			MaxResults:            1,
    			ResolveToResourceId:   "example",
    			ResolveToResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Actions } from "@ubx/sdk-aws/aws/data/iotsitewise/actions";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_actions" });

      data(Actions, "example", {
        targetResourceId: "example",
        targetResourceType: "example",
        maxResults: 1,
        resolveToResourceId: "example",
        resolveToResourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Actions, ActionsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_actions")

        ubx.data(Actions, "example", ActionsConfig(
            target_resource_id="example",
            target_resource_type="example",
            max_results=1,
            resolve_to_resource_id="example",
            resolve_to_resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resolve_to_resource_id" type="string">
  The real, unique identifier of the resolve to resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resolve_to_resource_type" type="string">
  The real, discriminating type of the resolve to resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_resource_id" type="string" required>
  The real, unique identifier of the target resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="target_resource_type" type="string" required>
  The real, discriminating type of the target resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_summaries" type="list(object)">
  The real, summarized actions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
