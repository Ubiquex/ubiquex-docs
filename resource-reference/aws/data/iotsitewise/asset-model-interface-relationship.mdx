---
title: "aws_iotsitewise_asset_model_interface_relationship"
description: "`aws_iotsitewise_asset_model_interface_relationship` reads a real relationship between an IoT SiteWise asset model and an interface it implements."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_asset_model_interface_relationship"})

    		ubx.Data(iotsitewise.AssetModelInterfaceRelationship, "example", iotsitewise.AssetModelInterfaceRelationshipConfig{
    			AssetModelId:          "example",
    			InterfaceAssetModelId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetModelInterfaceRelationship } from "@ubx/sdk-aws/aws/data/iotsitewise/asset_model_interface_relationship";

    export default stack("example", () => {
      intent({
        summary: "look up aws_iotsitewise_asset_model_interface_relationship",
      });

      data(AssetModelInterfaceRelationship, "example", {
        assetModelId: "example",
        interfaceAssetModelId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AssetModelInterfaceRelationship, AssetModelInterfaceRelationshipConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_asset_model_interface_relationship")

        ubx.data(AssetModelInterfaceRelationship, "example", AssetModelInterfaceRelationshipConfig(
            asset_model_id="example",
            interface_asset_model_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_model_id" type="string" required>
  The real, unique identifier of the asset model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="interface_asset_model_id" type="string" required>
  The real, unique identifier of the interface asset model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hierarchy_mappings" type="list(object)">
  The real hierarchy mappings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="property_mappings" type="list(object)">
  The real property mappings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
