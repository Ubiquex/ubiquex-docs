---
title: "aws_iotsitewise_queries"
description: "`aws_iotsitewise_queries` reads the real, saved SQL queries against IoT SiteWise's own data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_queries"})

    		ubx.Data(iotsitewise.Queries, "example", iotsitewise.QueriesConfig{
    			WorkspaceName: "example-workspace-name",
    			Filter:        "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Queries } from "@ubx/sdk-aws/aws/data/iotsitewise/queries";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_queries" });

      data(Queries, "example", {
        workspaceName: "example-workspace-name",
        filter: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Queries, QueriesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_queries")

        ubx.data(Queries, "example", QueriesConfig(
            workspace_name="example-workspace-name",
            filter="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  &lt;p&gt;Filter for ListQueries. Returns only queries with the specified status.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for each paginated request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;Pagination token for the query list.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="queries" type="list(object)">
  &lt;p&gt;A list of query summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
