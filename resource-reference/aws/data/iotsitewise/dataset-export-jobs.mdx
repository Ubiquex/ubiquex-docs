---
title: "aws_iotsitewise_dataset_export_jobs"
description: "`aws_iotsitewise_dataset_export_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotsitewise_dataset_export_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_dataset_export_jobs"})

    		ubx.Data(iotsitewise.DatasetExportJobs, "example", iotsitewise.DatasetExportJobsConfig{
    			WorkspaceName: "example-workspace-name",
    			Filter:        "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatasetExportJobs } from "@ubx/sdk-aws/aws/data/iotsitewise/dataset_export_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_dataset_export_jobs" });

      data(DatasetExportJobs, "example", {
        workspaceName: "example-workspace-name",
        filter: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import DatasetExportJobs, DatasetExportJobsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_dataset_export_jobs")

        ubx.data(DatasetExportJobs, "example", DatasetExportJobsConfig(
            workspace_name="example-workspace-name",
            filter="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  &lt;p&gt;Filter for ListDatasetExportJobs. ALL returns jobs in any status; otherwise returns jobs in the specified status.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum number of results to return for each paginated request.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="jobs" type="list(object)">
  &lt;p&gt;A list of dataset export job summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The token to be used for the next set of paginated results.&lt;/p&gt; Returned by the lookup.
</ResponseField>
