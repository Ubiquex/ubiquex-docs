---
title: "aws_iotsitewise_searches"
description: "`aws_iotsitewise_searches` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotsitewise_searches` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_searches"})

    		ubx.Data(iotsitewise.Searches, "example", iotsitewise.SearchesConfig{
    			WorkspaceName:       "example-workspace-name",
    			ListSearchesFilters: map[string]any{"group_id_filter": []string{"example"}},
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Searches } from "@ubx/sdk-aws/aws/data/iotsitewise/searches";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_searches" });

      data(Searches, "example", {
        workspaceName: "example-workspace-name",
        listSearchesFilters: { groupIdFilter: ["example"] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Searches, SearchesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_searches")

        ubx.data(Searches, "example", SearchesConfig(
            workspace_name="example-workspace-name",
            list_searches_filters={"group_id_filter": ["example"]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="list_searches_filters" type="object">
  &lt;p&gt;Optional filters for ListSearches. When multiple filters are set, a search must match all of them.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="group_id_filter" type="list(string)">
      &lt;p&gt;A filter on group identifier. Matches searches whose &lt;code&gt;groupId&lt;/code&gt; is any of the listed values.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="search_type_filter" type="list(string)">
      &lt;p&gt;A filter on search type. Matches searches whose &lt;code&gt;searchType&lt;/code&gt; is any of the listed values.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
    <ResponseField name="started_after" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="started_before" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_filter" type="list(string)">
      &lt;p&gt;A filter on search status. Matches searches whose status is any of the listed values.&lt;/p&gt; Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="search_summaries" type="list(object)">
  &lt;p&gt;A list of search summaries returned by ListSearches.&lt;/p&gt; Returned by the lookup.
</ResponseField>
