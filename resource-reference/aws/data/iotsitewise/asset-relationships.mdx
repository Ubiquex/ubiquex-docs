---
title: "aws_iotsitewise_asset_relationships"
description: "`aws_iotsitewise_asset_relationships` reads the real hierarchical relationships (parent/child) an IoT SiteWise asset participates in."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_asset_relationships"})

    		ubx.Data(iotsitewise.AssetRelationships, "example", iotsitewise.AssetRelationshipsConfig{
    			AssetId:       "example",
    			TraversalType: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetRelationships } from "@ubx/sdk-aws/aws/data/iotsitewise/asset_relationships";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_asset_relationships" });

      data(AssetRelationships, "example", {
        assetId: "example",
        traversalType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AssetRelationships, AssetRelationshipsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_asset_relationships")

        ubx.data(AssetRelationships, "example", AssetRelationshipsConfig(
            asset_id="example",
            traversal_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_id" type="string" required>
  The real, unique identifier of the asset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="traversal_type" type="string" required>
  The real, discriminating type of the traversal this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_relationship_summaries" type="list(object)">
  The real, summarized asset relationships included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
