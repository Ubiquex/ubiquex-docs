---
title: "aws_iotsitewise_dataset_data_segments"
description: "`aws_iotsitewise_dataset_data_segments` reads the real data segments within an IoT SiteWise dataset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_dataset_data_segments"})

    		ubx.Data(iotsitewise.DatasetDataSegments, "example", iotsitewise.DatasetDataSegmentsConfig{
    			DatasetId:      "example",
    			WorkspaceName:  "example-workspace-name",
    			DatasetVersion: "example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DatasetDataSegments } from "./local-sdk/aws/sdk/typescript/aws/data/iotsitewise/dataset_data_segments.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_dataset_data_segments" });

      data(DatasetDataSegments, "example", {
        datasetId: "example",
        workspaceName: "example-workspace-name",
        datasetVersion: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import DatasetDataSegments, DatasetDataSegmentsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_dataset_data_segments")

        ubx.data(DatasetDataSegments, "example", DatasetDataSegmentsConfig(
            dataset_id="example",
            workspace_name="example-workspace-name",
            dataset_version="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_id" type="string" required>
  The real, unique identifier of the dataset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="dataset_version" type="string">
  The real version of the dataset. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_segments" type="list(object)">
  The real data segments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
