---
title: "aws_iotsitewise_asset_composite_model"
description: "`aws_iotsitewise_asset_composite_model` looks up a real composite model attached to an IoT SiteWise asset, grouping related properties together."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_asset_composite_model"})

    		ubx.Data(iotsitewise.AssetCompositeModel, "example", iotsitewise.AssetCompositeModelConfig{
    			AssetCompositeModelId: "example",
    			AssetId:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AssetCompositeModel } from "./local-sdk/aws/sdk/typescript/aws/data/iotsitewise/asset_composite_model.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_asset_composite_model" });

      data(AssetCompositeModel, "example", {
        assetCompositeModelId: "example",
        assetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AssetCompositeModel, AssetCompositeModelConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_asset_composite_model")

        ubx.data(AssetCompositeModel, "example", AssetCompositeModelConfig(
            asset_composite_model_id="example",
            asset_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_composite_model_id" type="string" required>
  The real, unique identifier of the asset composite model this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="asset_id" type="string" required>
  The real, unique identifier of the asset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_definitions" type="list(object)">
  The real action definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_description" type="string">
  The real, free-text description of the asset composite model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_external_id" type="string">
  The real, unique identifier of the asset composite model external this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_name" type="string">
  The real name of the asset composite model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_path" type="list(object)">
  The real path of the asset composite model. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_properties" type="list(object)">
  The real, composite model properties of this AWS IoT SiteWise asset. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_summaries" type="list(object)">
  The real, summarized asset composite models included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_type" type="string">
  The real, discriminating type of the asset composite model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
