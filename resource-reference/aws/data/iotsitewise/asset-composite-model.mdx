---
title: "aws_iotsitewise_asset_composite_model"
description: "`aws_iotsitewise_asset_composite_model` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotsitewise_asset_composite_model` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_asset_composite_model"})

    		ubx.Data(iotsitewise.AssetCompositeModel, "example", iotsitewise.AssetCompositeModelConfig{
    			AssetCompositeModelId: "example",
    			AssetId:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetCompositeModel } from "@ubx/sdk-aws/aws/data/iotsitewise/asset_composite_model";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_asset_composite_model" });

      data(AssetCompositeModel, "example", {
        assetCompositeModelId: "example",
        assetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AssetCompositeModel, AssetCompositeModelConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_asset_composite_model")

        ubx.data(AssetCompositeModel, "example", AssetCompositeModelConfig(
            asset_composite_model_id="example",
            asset_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_composite_model_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="asset_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action_definitions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_external_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_path" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_properties" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="asset_composite_model_type" type="string">
  Returned by the lookup.
</ResponseField>
