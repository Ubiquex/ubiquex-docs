---
title: "aws_iotsitewise_bulk_import_jobs"
description: "`aws_iotsitewise_bulk_import_jobs` looks up a real IoT SiteWise bulk import job, loading historical time-series data from S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_bulk_import_jobs"})

    		ubx.Data(iotsitewise.BulkImportJobs, "example", iotsitewise.BulkImportJobsConfig{
    			Filter:        "example",
    			MaxResults:    1,
    			WorkspaceName: "example-workspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BulkImportJobs } from "@ubx/sdk-aws/aws/data/iotsitewise/bulk_import_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_bulk_import_jobs" });

      data(BulkImportJobs, "example", {
        filter: "example",
        maxResults: 1,
        workspaceName: "example-workspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import BulkImportJobs, BulkImportJobsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_bulk_import_jobs")

        ubx.data(BulkImportJobs, "example", BulkImportJobsConfig(
            filter="example",
            max_results=1,
            workspace_name="example-workspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string">
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_summaries" type="list(object)">
  The real, summarized jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
