---
title: "aws_iotsitewise_pipelines"
description: "`data_aws_iotsitewise_pipelines` looks up a real, already-existing `aws_io_tanalytics_pipeline`."
---

It is a real, ordered sequence of activities, filtering, enriching, transforming, that processes messages from an `aws_io_tanalytics_channel` and writes the result into an `aws_io_tanalytics_datastore`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_pipelines"})

    		ubx.Data(iotsitewise.Pipelines, "example", iotsitewise.PipelinesConfig{
    			WorkspaceName: "example-workspace-name",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Pipelines } from "./local-sdk/aws/sdk/typescript/aws/data/iotsitewise/pipelines.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_pipelines" });

      data(Pipelines, "example", {
        workspaceName: "example-workspace-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Pipelines, PipelinesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_pipelines")

        ubx.data(Pipelines, "example", PipelinesConfig(
            workspace_name="example-workspace-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;An opaque pagination token. Pass the value returned by a previous request to retrieve the next page of results.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_summaries" type="list(object)">
  &lt;p&gt;A list of pipeline summaries.&lt;/p&gt; Returned by the lookup.
</ResponseField>
