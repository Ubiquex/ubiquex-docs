---
title: "aws_iotsitewise_dataset_data_segment_relationships"
description: "`aws_iotsitewise_dataset_data_segment_relationships` reads the real relationships between an IoT SiteWise dataset and its own data segments."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_dataset_data_segment_relationships"})

    		ubx.Data(iotsitewise.DatasetDataSegmentRelationships, "example", iotsitewise.DatasetDataSegmentRelationshipsConfig{
    			DatasetId:     "example",
    			WorkspaceName: "example-workspace-name",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatasetDataSegmentRelationships } from "@ubx/sdk-aws/aws/data/iotsitewise/dataset_data_segment_relationships";

    export default stack("example", () => {
      intent({
        summary: "look up aws_iotsitewise_dataset_data_segment_relationships",
      });

      data(DatasetDataSegmentRelationships, "example", {
        datasetId: "example",
        workspaceName: "example-workspace-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import DatasetDataSegmentRelationships, DatasetDataSegmentRelationshipsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_dataset_data_segment_relationships")

        ubx.data(DatasetDataSegmentRelationships, "example", DatasetDataSegmentRelationshipsConfig(
            dataset_id="example",
            workspace_name="example-workspace-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_id" type="string" required>
  The real, unique identifier of the dataset this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workspace_name" type="string" required>
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_segment_relationship_summaries" type="list(object)">
  The real, summarized data segment relationships included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
