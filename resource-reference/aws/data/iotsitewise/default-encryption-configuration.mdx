---
title: "aws_iotsitewise_default_encryption_configuration"
description: "`aws_iotsitewise_default_encryption_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_iotsitewise_default_encryption_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_default_encryption_configuration"})

    		ubx.Data(iotsitewise.DefaultEncryptionConfiguration, "example", iotsitewise.DefaultEncryptionConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DefaultEncryptionConfiguration } from "@ubx/sdk-aws/aws/data/iotsitewise/default_encryption_configuration";

    export default stack("example", () => {
      intent({
        summary: "look up aws_iotsitewise_default_encryption_configuration",
      });

      data(DefaultEncryptionConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import DefaultEncryptionConfiguration, DefaultEncryptionConfigurationConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_default_encryption_configuration")

        ubx.data(DefaultEncryptionConfiguration, "example", DefaultEncryptionConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration_status" type="object">
  &lt;p&gt;Contains current status information for the configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="error" type="object">
      &lt;p&gt;Contains the details of an IoT SiteWise configuration error.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="code" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="message" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="encryption_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Returned by the lookup.
</ResponseField>
