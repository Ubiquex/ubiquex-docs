---
title: "aws_iotsitewise_assets"
description: "`aws_iotsitewise_assets` looks up a real IoT SiteWise asset, representing one physical piece of equipment or process."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_assets"})

    		ubx.Data(iotsitewise.Assets, "example", iotsitewise.AssetsConfig{
    			AssetModelId: "example",
    			Filter:       "example",
    			MaxResults:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Assets } from "@ubx/sdk-aws/aws/data/iotsitewise/assets";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_assets" });

      data(Assets, "example", {
        assetModelId: "example",
        filter: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Assets, AssetsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_assets")

        ubx.data(Assets, "example", AssetsConfig(
            asset_model_id="example",
            filter="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_model_id" type="string">
  The real, unique identifier of the asset model this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_summaries" type="list(object)">
  The real, summarized assets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
