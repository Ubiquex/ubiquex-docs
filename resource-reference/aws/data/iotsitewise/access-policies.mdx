---
title: "aws_iotsitewise_access_policies"
description: "`aws_iotsitewise_access_policies` looks up a real IoT SiteWise Monitor access policy, granting a user or group access to a portal or project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_access_policies"})

    		ubx.Data(iotsitewise.AccessPolicies, "example", iotsitewise.AccessPoliciesConfig{
    			IamArn:       "arn:aws:iam::123456789012:policy/example",
    			IdentityId:   "example",
    			IdentityType: "example",
    			MaxResults:   1,
    			ResourceId:   "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessPolicies } from "@ubx/sdk-aws/aws/data/iotsitewise/access_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_access_policies" });

      data(AccessPolicies, "example", {
        iamArn: "arn:aws:iam::123456789012:policy/example",
        identityId: "example",
        identityType: "example",
        maxResults: 1,
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AccessPolicies, AccessPoliciesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_access_policies")

        ubx.data(AccessPolicies, "example", AccessPoliciesConfig(
            iam_arn="arn:aws:iam::123456789012:policy/example",
            identity_id="example",
            identity_type="example",
            max_results=1,
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="iam_arn" type="string">
  The real Amazon Resource Name (ARN) of the iam this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="identity_id" type="string">
  The real, unique identifier of the identity this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="identity_type" type="string">
  The real type of this identity. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="access_policy_summaries" type="list(object)">
  The real, summarized access policys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
