---
title: "aws_iotsitewise_asset_property_aggregates"
description: "`aws_iotsitewise_asset_property_aggregates` reads real, aggregated (average, min, max) time-series values for an IoT SiteWise asset property."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_asset_property_aggregates"})

    		ubx.Data(iotsitewise.AssetPropertyAggregates, "example", iotsitewise.AssetPropertyAggregatesConfig{
    			AggregateTypes: []string{"example"},
    			EndDate:        "example",
    			Resolution:     "example",
    			StartDate:      "example",
    			AssetId:        "example",
    			MaxResults:     1,
    			PropertyAlias:  "example",
    			PropertyId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetPropertyAggregates } from "@ubx/sdk-aws/aws/data/iotsitewise/asset_property_aggregates";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_asset_property_aggregates" });

      data(AssetPropertyAggregates, "example", {
        aggregateTypes: ["example"],
        endDate: "example",
        resolution: "example",
        startDate: "example",
        assetId: "example",
        maxResults: 1,
        propertyAlias: "example",
        propertyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import AssetPropertyAggregates, AssetPropertyAggregatesConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_asset_property_aggregates")

        ubx.data(AssetPropertyAggregates, "example", AssetPropertyAggregatesConfig(
            aggregate_types=["example"],
            end_date="example",
            resolution="example",
            start_date="example",
            asset_id="example",
            max_results=1,
            property_alias="example",
            property_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregate_types" type="list(string)" required>
  The real, discriminating types of aggregates this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="asset_id" type="string">
  The real, unique identifier of the asset this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_date" type="string" required>
  The latest real date to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="property_alias" type="string">
  The real, human-readable alias of the property this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="property_id" type="string">
  The real, unique identifier of the property this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="qualities" type="list(string)">
  The real quality levels included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resolution" type="string" required>
  The real resolution of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_date" type="string" required>
  The earliest real date to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="time_ordering" type="string">
  The real order (ascending or descending) results are returned in by time. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregated_values" type="list(object)">
  The real, aggregated values included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
