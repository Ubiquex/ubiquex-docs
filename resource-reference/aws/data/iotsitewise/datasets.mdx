---
title: "aws_iotsitewise_datasets"
description: "`data_aws_iotsitewise_datasets` looks up a real, already-existing `aws_io_tanalytics_dataset`."
---

It runs a real, recurring SQL query or containerized analysis against an `aws_io_tanalytics_datastore` and materializes the result, the actual output a caller or downstream application reads insights from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iotsitewise "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/iotsitewise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_iotsitewise_datasets"})

    		ubx.Data(iotsitewise.Datasets, "example", iotsitewise.DatasetsConfig{
    			SourceType:  "example",
    			DatasetType: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Datasets } from "./local-sdk/aws/sdk/typescript/aws/data/iotsitewise/datasets.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_iotsitewise_datasets" });

      data(Datasets, "example", {
        sourceType: "example",
        datasetType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.iotsitewise import Datasets, DatasetsConfig

    def describe():
        ubx.intent("look up aws_iotsitewise_datasets")

        ubx.data(Datasets, "example", DatasetsConfig(
            source_type="example",
            dataset_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_type" type="string">
  The real, discriminating type of the dataset this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_type" type="string" required>
  The real type of the source this object was derived from. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dataset_summaries" type="list(object)">
  The real, summarized datasets included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workspace_name" type="string">
  The real name of the workspace this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
