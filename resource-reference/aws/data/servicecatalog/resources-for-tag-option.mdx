---
title: "aws_servicecatalog_resources_for_tag_option"
description: "`aws_servicecatalog_resources_for_tag_option` reads the real Service Catalog resources associated with a given TagOption."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_resources_for_tag_option"})

    		ubx.Data(servicecatalog.ResourcesForTagOption, "example", servicecatalog.ResourcesForTagOptionConfig{
    			TagOptionId:  "example",
    			PageSize:     1,
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcesForTagOption } from "@ubx/sdk-aws/aws/data/servicecatalog/resources_for_tag_option";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_resources_for_tag_option" });

      data(ResourcesForTagOption, "example", {
        tagOptionId: "example",
        pageSize: 1,
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ResourcesForTagOption, ResourcesForTagOptionConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_resources_for_tag_option")

        ubx.data(ResourcesForTagOption, "example", ResourcesForTagOptionConfig(
            tag_option_id="example",
            page_size=1,
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tag_option_id" type="string" required>
  The real, unique identifier of the tag option this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_details" type="list(object)">
  The real, detailed information about the resource. **(AI-inferred)** Returned by the lookup.
</ResponseField>
