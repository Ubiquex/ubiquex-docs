---
title: "aws_servicecatalog_stack_instances_for_provisioned_product"
description: "`aws_servicecatalog_stack_instances_for_provisioned_product` reads the real CloudFormation StackSet stack instances backing a Service Catalog..."
---

`aws_servicecatalog_stack_instances_for_provisioned_product` reads the real CloudFormation StackSet stack instances backing a Service Catalog provisioned product.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_stack_instances_for_provisioned_product"})

    		ubx.Data(servicecatalog.StackInstancesForProvisionedProduct, "example", servicecatalog.StackInstancesForProvisionedProductConfig{
    			ProvisionedProductId: "example",
    			AcceptLanguage:       "example",
    			PageSize:             1,
    			PageToken:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StackInstancesForProvisionedProduct } from "@ubx/sdk-aws/aws/data/servicecatalog/stack_instances_for_provisioned_product";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_servicecatalog_stack_instances_for_provisioned_product",
      });

      data(StackInstancesForProvisionedProduct, "example", {
        provisionedProductId: "example",
        acceptLanguage: "example",
        pageSize: 1,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import StackInstancesForProvisionedProduct, StackInstancesForProvisionedProductConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_stack_instances_for_provisioned_product")

        ubx.data(StackInstancesForProvisionedProduct, "example", StackInstancesForProvisionedProductConfig(
            provisioned_product_id="example",
            accept_language="example",
            page_size=1,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioned_product_id" type="string" required>
  The real, unique identifier of the provisioned product this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="stack_instances" type="list(object)">
  The real CloudFormation StackSet instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
