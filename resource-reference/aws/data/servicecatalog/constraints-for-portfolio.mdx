---
title: "aws_servicecatalog_constraints_for_portfolio"
description: "`aws_servicecatalog_constraints_for_portfolio` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_servicecatalog_constraints_for_portfolio` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_constraints_for_portfolio"})

    		ubx.Data(servicecatalog.ConstraintsForPortfolio, "example", servicecatalog.ConstraintsForPortfolioConfig{
    			PortfolioId:    "example",
    			AcceptLanguage: "example",
    			PageSize:       1,
    			PageToken:      "example",
    			ProductId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConstraintsForPortfolio } from "@ubx/sdk-aws/aws/data/servicecatalog/constraints_for_portfolio";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_constraints_for_portfolio" });

      data(ConstraintsForPortfolio, "example", {
        portfolioId: "example",
        acceptLanguage: "example",
        pageSize: 1,
        pageToken: "example",
        productId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ConstraintsForPortfolio, ConstraintsForPortfolioConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_constraints_for_portfolio")

        ubx.data(ConstraintsForPortfolio, "example", ConstraintsForPortfolioConfig(
            portfolio_id="example",
            accept_language="example",
            page_size=1,
            page_token="example",
            product_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="product_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="constraint_details" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  Returned by the lookup.
</ResponseField>
