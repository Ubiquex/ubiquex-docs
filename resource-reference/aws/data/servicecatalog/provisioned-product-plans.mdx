---
title: "aws_servicecatalog_provisioned_product_plans"
description: "`aws_servicecatalog_provisioned_product_plans` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_servicecatalog_provisioned_product_plans` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_provisioned_product_plans"})

    		ubx.Data(servicecatalog.ProvisionedProductPlans, "example", servicecatalog.ProvisionedProductPlansConfig{
    			AcceptLanguage:     "example",
    			AccessLevelFilter:  map[string]any{"key": "example"},
    			PageSize:           1,
    			PageToken:          "example",
    			ProvisionProductId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisionedProductPlans } from "@ubx/sdk-aws/aws/data/servicecatalog/provisioned_product_plans";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_provisioned_product_plans" });

      data(ProvisionedProductPlans, "example", {
        acceptLanguage: "example",
        accessLevelFilter: { key: "example" },
        pageSize: 1,
        pageToken: "example",
        provisionProductId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ProvisionedProductPlans, ProvisionedProductPlansConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_provisioned_product_plans")

        ubx.data(ProvisionedProductPlans, "example", ProvisionedProductPlansConfig(
            accept_language="example",
            access_level_filter={"key": "example"},
            page_size=1,
            page_token="example",
            provision_product_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="access_level_filter" type="object">
  &lt;p&gt;The access level to use to filter results.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="provision_product_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provisioned_product_plans" type="list(object)">
  Returned by the lookup.
</ResponseField>
