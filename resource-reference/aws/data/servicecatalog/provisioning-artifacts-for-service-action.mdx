---
title: "aws_servicecatalog_provisioning_artifacts_for_service_action"
description: "`aws_servicecatalog_provisioning_artifacts_for_service_action` reads the real provisioning artifacts a Service Catalog self-service action..."
---

`aws_servicecatalog_provisioning_artifacts_for_service_action` reads the real provisioning artifacts a Service Catalog self-service action is associated with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_provisioning_artifacts_for_service_action"})

    		ubx.Data(servicecatalog.ProvisioningArtifactsForServiceAction, "example", servicecatalog.ProvisioningArtifactsForServiceActionConfig{
    			ServiceActionId: "example",
    			AcceptLanguage:  "example",
    			PageSize:        1,
    			PageToken:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisioningArtifactsForServiceAction } from "@ubx/sdk-aws/aws/data/servicecatalog/provisioning_artifacts_for_service_action";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_servicecatalog_provisioning_artifacts_for_service_action",
      });

      data(ProvisioningArtifactsForServiceAction, "example", {
        serviceActionId: "example",
        acceptLanguage: "example",
        pageSize: 1,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ProvisioningArtifactsForServiceAction, ProvisioningArtifactsForServiceActionConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_provisioning_artifacts_for_service_action")

        ubx.data(ProvisioningArtifactsForServiceAction, "example", ProvisioningArtifactsForServiceActionConfig(
            service_action_id="example",
            accept_language="example",
            page_size=1,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_action_id" type="string" required>
  The real, unique identifier of the service action this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_artifact_views" type="list(object)">
  The real AWS Service Catalog provisioning artifact views included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
