---
title: "aws_servicecatalog_portfolio_share_status"
description: "`aws_servicecatalog_portfolio_share_status` reads the real, current status of sharing a Service Catalog portfolio with another account or organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_portfolio_share_status"})

    		ubx.Data(servicecatalog.PortfolioShareStatus, "example", servicecatalog.PortfolioShareStatusConfig{
    			PortfolioShareToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PortfolioShareStatus } from "./local-sdk/aws/sdk/typescript/aws/data/servicecatalog/portfolio_share_status.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_portfolio_share_status" });

      data(PortfolioShareStatus, "example", {
        portfolioShareToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import PortfolioShareStatus, PortfolioShareStatusConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_portfolio_share_status")

        ubx.data(PortfolioShareStatus, "example", PortfolioShareStatusConfig(
            portfolio_share_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="portfolio_share_token" type="string" required>
  The real, opaque portfolio share token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="organization_node_value" type="string">
  The real value of this organization node. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="portfolio_id" type="string">
  The real, unique identifier of the AWS Service Catalog portfolio this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="share_details" type="object">
  &lt;p&gt;Information about the portfolio share operation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="share_errors" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="successful_shares" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
