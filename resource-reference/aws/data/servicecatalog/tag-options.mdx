---
title: "aws_servicecatalog_tag_options"
description: "`aws_servicecatalog_tag_options` looks up a real Service Catalog TagOption, a predefined tag key-value pair that can be enforced on provisioned products."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_tag_options"})

    		ubx.Data(servicecatalog.TagOptions, "example", servicecatalog.TagOptionsConfig{
    			Filters:  map[string]any{"active": true},
    			PageSize: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagOptions } from "@ubx/sdk-aws/aws/data/servicecatalog/tag_options";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_tag_options" });

      data(TagOptions, "example", {
        filters: { active: true },
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import TagOptions, TagOptionsConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_tag_options")

        ubx.data(TagOptions, "example", TagOptionsConfig(
            filters={"active": True},
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;Filters to use when listing TagOptions.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="active" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="tag_option_details" type="list(object)">
  The real, detailed information about the tag option. **(AI-inferred)** Returned by the lookup.
</ResponseField>
