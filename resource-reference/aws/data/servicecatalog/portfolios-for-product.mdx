---
title: "aws_servicecatalog_portfolios_for_product"
description: "`aws_servicecatalog_portfolios_for_product` reads the real portfolios a Service Catalog product has been added to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_portfolios_for_product"})

    		ubx.Data(servicecatalog.PortfoliosForProduct, "example", servicecatalog.PortfoliosForProductConfig{
    			ProductId:      "example",
    			AcceptLanguage: "example",
    			PageSize:       1,
    			PageToken:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PortfoliosForProduct } from "./local-sdk/aws/sdk/typescript/aws/data/servicecatalog/portfolios_for_product.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_portfolios_for_product" });

      data(PortfoliosForProduct, "example", {
        productId: "example",
        acceptLanguage: "example",
        pageSize: 1,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import PortfoliosForProduct, PortfoliosForProductConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_portfolios_for_product")

        ubx.data(PortfoliosForProduct, "example", PortfoliosForProductConfig(
            product_id="example",
            accept_language="example",
            page_size=1,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_id" type="string" required>
  The real, unique identifier of the product this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="portfolio_details" type="list(object)">
  The real, detailed information about the portfolio. **(AI-inferred)** Returned by the lookup.
</ResponseField>
