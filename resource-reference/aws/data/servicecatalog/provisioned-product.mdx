---
title: "aws_servicecatalog_provisioned_product"
description: "`aws_servicecatalog_provisioned_product` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_servicecatalog_provisioned_product` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_provisioned_product"})

    		ubx.Data(servicecatalog.ProvisionedProduct, "example", servicecatalog.ProvisionedProductConfig{
    			Name:           "example",
    			AcceptLanguage: "example",
    			Id:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisionedProduct } from "@ubx/sdk-aws/aws/data/servicecatalog/provisioned_product";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_provisioned_product" });

      data(ProvisionedProduct, "example", {
        name: "example",
        acceptLanguage: "example",
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ProvisionedProduct, ProvisionedProductConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_provisioned_product")

        ubx.data(ProvisionedProduct, "example", ProvisionedProductConfig(
            name="example",
            accept_language="example",
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_watch_dashboards" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="provisioned_product_detail" type="object">
  &lt;p&gt;Information about a provisioned product.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="idempotency_token" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_provisioning_record_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_record_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_successful_provisioning_record_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="launch_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="product_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="provisioning_artifact_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
