---
title: "aws_servicecatalog_provisioning_parameters"
description: "`aws_servicecatalog_provisioning_parameters` reads the real, required parameters for launching a Service Catalog product."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_provisioning_parameters"})

    		ubx.Data(servicecatalog.ProvisioningParameters, "example", servicecatalog.ProvisioningParametersConfig{
    			AcceptLanguage:           "example",
    			PathId:                   "example",
    			PathName:                 "example-path-name",
    			ProductId:                "example",
    			ProductName:              "example-product-name",
    			ProvisioningArtifactId:   "example",
    			ProvisioningArtifactName: "example-provisioning-artifact-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisioningParameters } from "@ubx/sdk-aws/aws/data/servicecatalog/provisioning_parameters";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_provisioning_parameters" });

      data(ProvisioningParameters, "example", {
        acceptLanguage: "example",
        pathId: "example",
        pathName: "example-path-name",
        productId: "example",
        productName: "example-product-name",
        provisioningArtifactId: "example",
        provisioningArtifactName: "example-provisioning-artifact-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ProvisioningParameters, ProvisioningParametersConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_provisioning_parameters")

        ubx.data(ProvisioningParameters, "example", ProvisioningParametersConfig(
            accept_language="example",
            path_id="example",
            path_name="example-path-name",
            product_id="example",
            product_name="example-product-name",
            provisioning_artifact_id="example",
            provisioning_artifact_name="example-provisioning-artifact-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path_id" type="string">
  The real, unique identifier of the path this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="path_name" type="string">
  The real name of the path this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_id" type="string">
  The real, unique identifier of the product this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product_name" type="string">
  The real name of the product this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioning_artifact_id" type="string">
  The real, unique identifier of the provisioning artifact this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioning_artifact_name" type="string">
  The real name of the provisioning artifact this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="constraint_summaries" type="list(object)">
  The real, summarized constraints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_artifact_output_keys" type="list(object)">
  The real, unique keys of the provisioning artifact outputs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_artifact_outputs" type="list(object)">
  The real outputs of the provisioning artifact. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_artifact_parameters" type="list(object)">
  The real AWS Service Catalog provisioning artifact parameters included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="provisioning_artifact_preferences" type="object">
  &lt;p&gt;The user-defined preferences that will be applied during product provisioning, unless overridden by &lt;code&gt;ProvisioningPreferences&lt;/code&gt; or &lt;code&gt;UpdateProvisioningPreferences&lt;/code&gt;.&lt;/p&gt; &lt;p&gt;For more information on maximum concurrent accounts and failure tolerance, see &lt;a href="https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/stacksets-concepts.html#stackset-ops-options"&gt;Stack set operation options&lt;/a&gt; in the &lt;i&gt;CloudFormation User Guide&lt;/i&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="stack_set_accounts" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="stack_set_regions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tag_options" type="list(object)">
  The real, available options for the tag. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="usage_instructions" type="list(object)">
  The real, human-readable usage instructions for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
