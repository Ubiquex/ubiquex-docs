---
title: "aws_servicecatalog_organization_portfolio_access"
description: "`aws_servicecatalog_organization_portfolio_access` reads whether real AWS Organizations accounts have access to a shared Service Catalog portfolio."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_organization_portfolio_access"})

    		ubx.Data(servicecatalog.OrganizationPortfolioAccess, "example", servicecatalog.OrganizationPortfolioAccessConfig{
    			OrganizationNodeType: "example",
    			PortfolioId:          "example",
    			AcceptLanguage:       "example",
    			PageSize:             1,
    			PageToken:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrganizationPortfolioAccess } from "@ubx/sdk-aws/aws/data/servicecatalog/organization_portfolio_access";

    export default stack("example", () => {
      intent({
        summary: "look up aws_servicecatalog_organization_portfolio_access",
      });

      data(OrganizationPortfolioAccess, "example", {
        organizationNodeType: "example",
        portfolioId: "example",
        acceptLanguage: "example",
        pageSize: 1,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import OrganizationPortfolioAccess, OrganizationPortfolioAccessConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_organization_portfolio_access")

        ubx.data(OrganizationPortfolioAccess, "example", OrganizationPortfolioAccessConfig(
            organization_node_type="example",
            portfolio_id="example",
            accept_language="example",
            page_size=1,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_node_type" type="string" required>
  The real, discriminating type of the organization node this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  The real, unique identifier of the AWS Service Catalog portfolio this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_nodes" type="list(object)">
  The real organization nodes included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
