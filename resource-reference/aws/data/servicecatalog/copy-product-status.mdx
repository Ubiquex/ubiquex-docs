---
title: "aws_servicecatalog_copy_product_status"
description: "`aws_servicecatalog_copy_product_status` reads the real, current status of copying a Service Catalog product between accounts or Regions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_copy_product_status"})

    		ubx.Data(servicecatalog.CopyProductStatus, "example", servicecatalog.CopyProductStatusConfig{
    			CopyProductToken: "example",
    			AcceptLanguage:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CopyProductStatus } from "@ubx/sdk-aws/aws/data/servicecatalog/copy_product_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_copy_product_status" });

      data(CopyProductStatus, "example", {
        copyProductToken: "example",
        acceptLanguage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import CopyProductStatus, CopyProductStatusConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_copy_product_status")

        ubx.data(CopyProductStatus, "example", CopyProductStatusConfig(
            copy_product_token="example",
            accept_language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="copy_product_token" type="string" required>
  The real, opaque copy product token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="copy_product_status" type="string">
  The real, current status of the copy product this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_detail" type="string">
  The real, detailed information about the status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_product_id" type="string">
  The real, unique identifier of the target product this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
