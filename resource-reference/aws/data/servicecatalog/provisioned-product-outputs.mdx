---
title: "aws_servicecatalog_provisioned_product_outputs"
description: "`aws_servicecatalog_provisioned_product_outputs` reads the real CloudFormation stack outputs of a Service Catalog provisioned product."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_provisioned_product_outputs"})

    		ubx.Data(servicecatalog.ProvisionedProductOutputs, "example", servicecatalog.ProvisionedProductOutputsConfig{
    			AcceptLanguage:         "example",
    			OutputKeys:             []string{"example"},
    			PageSize:               1,
    			PageToken:              "example",
    			ProvisionedProductId:   "example",
    			ProvisionedProductName: "example-provisioned-product-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisionedProductOutputs } from "@ubx/sdk-aws/aws/data/servicecatalog/provisioned_product_outputs";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_provisioned_product_outputs" });

      data(ProvisionedProductOutputs, "example", {
        acceptLanguage: "example",
        outputKeys: ["example"],
        pageSize: 1,
        pageToken: "example",
        provisionedProductId: "example",
        provisionedProductName: "example-provisioned-product-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog import ProvisionedProductOutputs, ProvisionedProductOutputsConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_provisioned_product_outputs")

        ubx.data(ProvisionedProductOutputs, "example", ProvisionedProductOutputsConfig(
            accept_language="example",
            output_keys=["example"],
            page_size=1,
            page_token="example",
            provisioned_product_id="example",
            provisioned_product_name="example-provisioned-product-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="accept_language" type="string">
  The real, requested language for human-readable text in the response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="output_keys" type="list(string)">
  The real, unique keys of the outputs included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioned_product_id" type="string">
  The real, unique identifier of the provisioned product this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provisioned_product_name" type="string">
  The real name of the provisioned product this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="outputs" type="list(object)">
  The real outputs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
