---
title: "aws_config_delivery_channels"
description: "`aws_config_delivery_channels` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_delivery_channels` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_delivery_channels"})

    		ubx.Data(config.DeliveryChannels, "example", config.DeliveryChannelsConfig{
    			DeliveryChannelNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeliveryChannels } from "@ubx/sdk-aws/aws/data/config/delivery_channels";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_delivery_channels" });

      data(DeliveryChannels, "example", {
        deliveryChannelNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import DeliveryChannels, DeliveryChannelsConfig

    def describe():
        ubx.intent("look up aws_config_delivery_channels")

        ubx.data(DeliveryChannels, "example", DeliveryChannelsConfig(
            delivery_channel_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="delivery_channel_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="delivery_channels" type="list(object)">
  Returned by the lookup.
</ResponseField>
