---
title: "aws_config_custom_rule_policy"
description: "`aws_config_custom_rule_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_custom_rule_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_custom_rule_policy"})

    		ubx.Data(config.CustomRulePolicy, "example", config.CustomRulePolicyConfig{
    			ConfigRuleName: "example-config-rule-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomRulePolicy } from "@ubx/sdk-aws/aws/data/config/custom_rule_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_custom_rule_policy" });

      data(CustomRulePolicy, "example", {
        configRuleName: "example-config-rule-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import CustomRulePolicy, CustomRulePolicyConfig

    def describe():
        ubx.intent("look up aws_config_custom_rule_policy")

        ubx.data(CustomRulePolicy, "example", CustomRulePolicyConfig(
            config_rule_name="example-config-rule-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_rule_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_text" type="string">
  Returned by the lookup.
</ResponseField>
