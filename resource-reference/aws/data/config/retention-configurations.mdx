---
title: "aws_config_retention_configurations"
description: "`aws_config_retention_configurations` reads the real configuration history retention period configured for Config."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_retention_configurations"})

    		ubx.Data(config.RetentionConfigurations, "example", config.RetentionConfigurationsConfig{
    			RetentionConfigurationNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RetentionConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/config/retention_configurations.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_retention_configurations" });

      data(RetentionConfigurations, "example", {
        retentionConfigurationNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.config import RetentionConfigurations, RetentionConfigurationsConfig

    def describe():
        ubx.intent("look up aws_config_retention_configurations")

        ubx.data(RetentionConfigurations, "example", RetentionConfigurationsConfig(
            retention_configuration_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="retention_configuration_names" type="list(string)">
  The real names of the retention configurations included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="retention_configurations" type="list(object)">
  The real retention configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
