---
title: "aws_config_organization_config_rules"
description: "`data_aws_config_organization_config_rules` looks up a real, already-existing `aws_config_organization_config_rule`."
---

It deploys an `aws_config_config_rule` across every account in an AWS Organization at once, the org-wide counterpart to defining and rolling out the same rule to each member account individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_organization_config_rules"})

    		ubx.Data(config.OrganizationConfigRules, "example", config.OrganizationConfigRulesConfig{
    			Limit:                       1,
    			OrganizationConfigRuleNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrganizationConfigRules } from "./local-sdk/aws/sdk/typescript/aws/data/config/organization_config_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_organization_config_rules" });

      data(OrganizationConfigRules, "example", {
        limit: 1,
        organizationConfigRuleNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.config import OrganizationConfigRules, OrganizationConfigRulesConfig

    def describe():
        ubx.intent("look up aws_config_organization_config_rules")

        ubx.data(OrganizationConfigRules, "example", OrganizationConfigRulesConfig(
            limit=1,
            organization_config_rule_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_config_rule_names" type="list(string)">
  The real names of the organization config rules included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_config_rules" type="list(object)">
  The real rules for the organization config. **(AI-inferred)** Returned by the lookup.
</ResponseField>
