---
title: "aws_config_resource_evaluations"
description: "`aws_config_resource_evaluations` reads the real, individual rule evaluations from a proactive Config resource evaluation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_resource_evaluations"})

    		ubx.Data(config.ResourceEvaluations, "example", config.ResourceEvaluationsConfig{
    			Filters: map[string]any{"evaluation_context_identifier": "example"},
    			Limit:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceEvaluations } from "@ubx/sdk-aws/aws/data/config/resource_evaluations";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_resource_evaluations" });

      data(ResourceEvaluations, "example", {
        filters: { evaluationContextIdentifier: "example" },
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ResourceEvaluations, ResourceEvaluationsConfig

    def describe():
        ubx.intent("look up aws_config_resource_evaluations")

        ubx.data(ResourceEvaluations, "example", ResourceEvaluationsConfig(
            filters={"evaluation_context_identifier": "example"},
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;Returns details of a resource evaluation based on the selected filter.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="evaluation_context_identifier" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="evaluation_mode" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="time_window" type="object">
      &lt;p&gt;Filters evaluation results based on start and end times.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end_time" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="start_time" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_evaluations" type="list(object)">
  The real AWS Config resource evaluations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
