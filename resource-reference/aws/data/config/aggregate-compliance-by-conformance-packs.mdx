---
title: "aws_config_aggregate_compliance_by_conformance_packs"
description: "`aws_config_aggregate_compliance_by_conformance_packs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_aggregate_compliance_by_conformance_packs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_aggregate_compliance_by_conformance_packs"})

    		ubx.Data(config.AggregateComplianceByConformancePacks, "example", config.AggregateComplianceByConformancePacksConfig{
    			ConfigurationAggregatorName: "example-configuration-aggregator-name",
    			Filters:                     map[string]any{"account_id": "example"},
    			Limit:                       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AggregateComplianceByConformancePacks } from "@ubx/sdk-aws/aws/data/config/aggregate_compliance_by_conformance_packs";

    export default stack("example", () => {
      intent({
        summary: "look up aws_config_aggregate_compliance_by_conformance_packs",
      });

      data(AggregateComplianceByConformancePacks, "example", {
        configurationAggregatorName: "example-configuration-aggregator-name",
        filters: { accountId: "example" },
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import AggregateComplianceByConformancePacks, AggregateComplianceByConformancePacksConfig

    def describe():
        ubx.intent("look up aws_config_aggregate_compliance_by_conformance_packs")

        ubx.data(AggregateComplianceByConformancePacks, "example", AggregateComplianceByConformancePacksConfig(
            configuration_aggregator_name="example-configuration-aggregator-name",
            filters={"account_id": "example"},
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_aggregator_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;Filters the conformance packs based on an account ID, region, compliance type, and the name of the conformance pack.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="aws_region" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="compliance_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="conformance_pack_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregate_compliance_by_conformance_packs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
