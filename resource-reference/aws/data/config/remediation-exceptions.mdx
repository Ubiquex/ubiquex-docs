---
title: "aws_config_remediation_exceptions"
description: "`aws_config_remediation_exceptions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_remediation_exceptions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_remediation_exceptions"})

    		ubx.Data(config.RemediationExceptions, "example", config.RemediationExceptionsConfig{
    			ConfigRuleName: "example-config-rule-name",
    			Limit:          1,
    			ResourceKeys:   []map[string]any{{"resource_id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RemediationExceptions } from "@ubx/sdk-aws/aws/data/config/remediation_exceptions";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_remediation_exceptions" });

      data(RemediationExceptions, "example", {
        configRuleName: "example-config-rule-name",
        limit: 1,
        resourceKeys: [{ resourceId: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import RemediationExceptions, RemediationExceptionsConfig

    def describe():
        ubx.intent("look up aws_config_remediation_exceptions")

        ubx.data(RemediationExceptions, "example", RemediationExceptionsConfig(
            config_rule_name="example-config-rule-name",
            limit=1,
            resource_keys=[{"resource_id": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_rule_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_keys" type="list(object)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="remediation_exceptions" type="list(object)">
  Returned by the lookup.
</ResponseField>
