---
title: "aws_config_remediation_configurations"
description: "`aws_config_remediation_configurations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_remediation_configurations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_remediation_configurations"})

    		ubx.Data(config.RemediationConfigurations, "example", config.RemediationConfigurationsConfig{
    			ConfigRuleNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RemediationConfigurations } from "@ubx/sdk-aws/aws/data/config/remediation_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_remediation_configurations" });

      data(RemediationConfigurations, "example", {
        configRuleNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import RemediationConfigurations, RemediationConfigurationsConfig

    def describe():
        ubx.intent("look up aws_config_remediation_configurations")

        ubx.data(RemediationConfigurations, "example", RemediationConfigurationsConfig(
            config_rule_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_rule_names" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="remediation_configurations" type="list(object)">
  Returned by the lookup.
</ResponseField>
