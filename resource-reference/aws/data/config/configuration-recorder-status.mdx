---
title: "aws_config_configuration_recorder_status"
description: "`aws_config_configuration_recorder_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_configuration_recorder_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_configuration_recorder_status"})

    		ubx.Data(config.ConfigurationRecorderStatus, "example", config.ConfigurationRecorderStatusConfig{
    			Arn:                        "arn:aws:iam::123456789012:policy/example",
    			ConfigurationRecorderNames: []string{"example"},
    			ServicePrincipal:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationRecorderStatus } from "@ubx/sdk-aws/aws/data/config/configuration_recorder_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_configuration_recorder_status" });

      data(ConfigurationRecorderStatus, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        configurationRecorderNames: ["example"],
        servicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ConfigurationRecorderStatus, ConfigurationRecorderStatusConfig

    def describe():
        ubx.intent("look up aws_config_configuration_recorder_status")

        ubx.data(ConfigurationRecorderStatus, "example", ConfigurationRecorderStatusConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            configuration_recorder_names=["example"],
            service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="configuration_recorder_names" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_principal" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_recorders_status" type="list(object)">
  Returned by the lookup.
</ResponseField>
