---
title: "aws_config_organization_conformance_pack_statuses"
description: "`data_aws_config_organization_conformance_pack_statuses` looks up a real, already-existing `aws_config_organization_conformance_pack`."
---

It deploys an `aws_config_conformance_pack` across every account in an AWS Organization at once, the org-wide counterpart to rolling the same conformance pack out to each member account individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_organization_conformance_pack_statuses"})

    		ubx.Data(config.OrganizationConformancePackStatuses, "example", config.OrganizationConformancePackStatusesConfig{
    			Limit:                            1,
    			OrganizationConformancePackNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OrganizationConformancePackStatuses } from "./local-sdk/aws/sdk/typescript/aws/data/config/organization_conformance_pack_statuses.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_config_organization_conformance_pack_statuses",
      });

      data(OrganizationConformancePackStatuses, "example", {
        limit: 1,
        organizationConformancePackNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.config import OrganizationConformancePackStatuses, OrganizationConformancePackStatusesConfig

    def describe():
        ubx.intent("look up aws_config_organization_conformance_pack_statuses")

        ubx.data(OrganizationConformancePackStatuses, "example", OrganizationConformancePackStatusesConfig(
            limit=1,
            organization_conformance_pack_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_conformance_pack_names" type="list(string)">
  The real names of the organization conformance packs included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_conformance_pack_statuses" type="list(object)">
  The real, current statuses of the organization conformance packs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
