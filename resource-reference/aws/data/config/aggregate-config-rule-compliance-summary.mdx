---
title: "aws_config_aggregate_config_rule_compliance_summary"
description: "`data_aws_config_aggregate_config_rule_compliance_summary` looks up a real, already-existing `aws_config_config_rule`."
---

It evaluates a real AWS resource's configuration against a defined condition every time it's created, changed, or deleted, flagging the resource noncompliant and sending a notification the moment it violates the rule rather than requiring a caller to poll for drift.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_aggregate_config_rule_compliance_summary"})

    		ubx.Data(config.AggregateConfigRuleComplianceSummary, "example", config.AggregateConfigRuleComplianceSummaryConfig{
    			ConfigurationAggregatorName: "example-configuration-aggregator-name",
    			Filters:                     map[string]any{"account_id": "example"},
    			Limit:                       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AggregateConfigRuleComplianceSummary } from "./local-sdk/aws/sdk/typescript/aws/data/config/aggregate_config_rule_compliance_summary.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_config_aggregate_config_rule_compliance_summary",
      });

      data(AggregateConfigRuleComplianceSummary, "example", {
        configurationAggregatorName: "example-configuration-aggregator-name",
        filters: { accountId: "example" },
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.config import AggregateConfigRuleComplianceSummary, AggregateConfigRuleComplianceSummaryConfig

    def describe():
        ubx.intent("look up aws_config_aggregate_config_rule_compliance_summary")

        ubx.data(AggregateConfigRuleComplianceSummary, "example", AggregateConfigRuleComplianceSummaryConfig(
            configuration_aggregator_name="example-configuration-aggregator-name",
            filters={"account_id": "example"},
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_aggregator_name" type="string" required>
  The real name of the AWS Config configuration aggregator this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;Filters the results based on the account IDs and regions.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="aws_region" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregate_compliance_counts" type="list(object)">
  The real counts of aggregate compliances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="group_by_key" type="string">
  The real, unique key of the group by this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
