---
title: "aws_config_compliance_summary_by_config_rule"
description: "`aws_config_compliance_summary_by_config_rule` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_compliance_summary_by_config_rule` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_compliance_summary_by_config_rule"})

    		ubx.Data(config.ComplianceSummaryByConfigRule, "example", config.ComplianceSummaryByConfigRuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceSummaryByConfigRule } from "@ubx/sdk-aws/aws/data/config/compliance_summary_by_config_rule";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_compliance_summary_by_config_rule" });

      data(ComplianceSummaryByConfigRule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ComplianceSummaryByConfigRule, ComplianceSummaryByConfigRuleConfig

    def describe():
        ubx.intent("look up aws_config_compliance_summary_by_config_rule")

        ubx.data(ComplianceSummaryByConfigRule, "example", ComplianceSummaryByConfigRuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="compliance_summary" type="object">
  &lt;p&gt;The number of Config rules or Amazon Web Services resources that are compliant and noncompliant.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="compliance_summary_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="compliant_resource_count" type="object">
      &lt;p&gt;The number of Amazon Web Services resources or Config rules responsible for the current compliance of the item, up to a maximum number.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cap_exceeded" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="capped_count" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="non_compliant_resource_count" type="object">
      &lt;p&gt;The number of Amazon Web Services resources or Config rules responsible for the current compliance of the item, up to a maximum number.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cap_exceeded" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="capped_count" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
