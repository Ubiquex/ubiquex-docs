---
title: "aws_config_compliance_by_resource"
description: "`aws_config_compliance_by_resource` reads the real, current compliance status of a specific resource against its own applicable Config rules."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_compliance_by_resource"})

    		ubx.Data(config.ComplianceByResource, "example", config.ComplianceByResourceConfig{
    			ComplianceTypes: []string{"example"},
    			Limit:           1,
    			ResourceId:      "example",
    			ResourceType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComplianceByResource } from "./local-sdk/aws/sdk/typescript/aws/data/config/compliance_by_resource.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_compliance_by_resource" });

      data(ComplianceByResource, "example", {
        complianceTypes: ["example"],
        limit: 1,
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.config import ComplianceByResource, ComplianceByResourceConfig

    def describe():
        ubx.intent("look up aws_config_compliance_by_resource")

        ubx.data(ComplianceByResource, "example", ComplianceByResourceConfig(
            compliance_types=["example"],
            limit=1,
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="compliance_types" type="list(string)">
  The real compliance types this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compliance_by_resources" type="list(object)">
  The real AWS Config compliance results grouped by resource. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
