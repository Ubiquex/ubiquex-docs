---
title: "aws_config_conformance_pack_compliance_scores"
description: "`data_aws_config_conformance_pack_compliance_scores` looks up a real, already-existing `aws_config_conformance_pack`."
---

It bundles a real collection of `aws_config_config_rule`s and remediation actions into one deployable, monitorable unit, so an entire compliance standard's worth of rules gets rolled out and tracked together instead of one rule at a time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_conformance_pack_compliance_scores"})

    		ubx.Data(config.ConformancePackComplianceScores, "example", config.ConformancePackComplianceScoresConfig{
    			Filters:   map[string]any{"conformance_pack_names": []string{"example"}},
    			Limit:     1,
    			SortBy:    "example",
    			SortOrder: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConformancePackComplianceScores } from "@ubx/sdk-aws/aws/data/config/conformance_pack_compliance_scores";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_conformance_pack_compliance_scores" });

      data(ConformancePackComplianceScores, "example", {
        filters: { conformancePackNames: ["example"] },
        limit: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ConformancePackComplianceScores, ConformancePackComplianceScoresConfig

    def describe():
        ubx.intent("look up aws_config_conformance_pack_compliance_scores")

        ubx.data(ConformancePackComplianceScores, "example", ConformancePackComplianceScoresConfig(
            filters={"conformance_pack_names": ["example"]},
            limit=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;A list of filters to apply to the conformance pack compliance score result set. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="conformance_pack_names" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="conformance_pack_compliance_scores" type="list(object)">
  The real AWS Config conformance pack compliance scores included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
