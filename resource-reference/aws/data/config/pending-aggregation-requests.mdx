---
title: "aws_config_pending_aggregation_requests"
description: "`aws_config_pending_aggregation_requests` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_pending_aggregation_requests` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_pending_aggregation_requests"})

    		ubx.Data(config.PendingAggregationRequests, "example", config.PendingAggregationRequestsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PendingAggregationRequests } from "@ubx/sdk-aws/aws/data/config/pending_aggregation_requests";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_pending_aggregation_requests" });

      data(PendingAggregationRequests, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import PendingAggregationRequests, PendingAggregationRequestsConfig

    def describe():
        ubx.intent("look up aws_config_pending_aggregation_requests")

        ubx.data(PendingAggregationRequests, "example", PendingAggregationRequestsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pending_aggregation_requests" type="list(object)">
  Returned by the lookup.
</ResponseField>
