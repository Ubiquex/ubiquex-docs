---
title: "aws_config_compliance_by_config_rule"
description: "`data_aws_config_compliance_by_config_rule` looks up a real, already-existing `aws_config_config_rule`."
---

It evaluates a real AWS resource's configuration against a defined condition every time it's created, changed, or deleted, flagging the resource noncompliant and sending a notification the moment it violates the rule rather than requiring a caller to poll for drift.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_compliance_by_config_rule"})

    		ubx.Data(config.ComplianceByConfigRule, "example", config.ComplianceByConfigRuleConfig{
    			ComplianceTypes: []string{"example"},
    			ConfigRuleNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceByConfigRule } from "@ubx/sdk-aws/aws/data/config/compliance_by_config_rule";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_compliance_by_config_rule" });

      data(ComplianceByConfigRule, "example", {
        complianceTypes: ["example"],
        configRuleNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ComplianceByConfigRule, ComplianceByConfigRuleConfig

    def describe():
        ubx.intent("look up aws_config_compliance_by_config_rule")

        ubx.data(ComplianceByConfigRule, "example", ComplianceByConfigRuleConfig(
            compliance_types=["example"],
            config_rule_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="compliance_types" type="list(string)">
  The real compliance types this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="config_rule_names" type="list(string)">
  The real names of the AWS Config rules included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compliance_by_config_rules" type="list(object)">
  The real rules for the compliance by config. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
