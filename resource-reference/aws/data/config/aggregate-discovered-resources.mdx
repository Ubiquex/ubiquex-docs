---
title: "aws_config_aggregate_discovered_resources"
description: "`aws_config_aggregate_discovered_resources` reads real resources discovered by Config, aggregated across every account and Region in a Config aggregator."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_aggregate_discovered_resources"})

    		ubx.Data(config.AggregateDiscoveredResources, "example", config.AggregateDiscoveredResourcesConfig{
    			ConfigurationAggregatorName: "example-configuration-aggregator-name",
    			ResourceType:                "example",
    			Filters:                     map[string]any{"account_id": "example"},
    			Limit:                       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AggregateDiscoveredResources } from "@ubx/sdk-aws/aws/data/config/aggregate_discovered_resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_aggregate_discovered_resources" });

      data(AggregateDiscoveredResources, "example", {
        configurationAggregatorName: "example-configuration-aggregator-name",
        resourceType: "example",
        filters: { accountId: "example" },
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import AggregateDiscoveredResources, AggregateDiscoveredResourcesConfig
    from ubx.aws.data.config.aggregate_discovered_resources import AggregateDiscoveredResources_Filters

    def describe():
        ubx.intent("look up aws_config_aggregate_discovered_resources")

        ubx.data(AggregateDiscoveredResources, "example", AggregateDiscoveredResourcesConfig(
            configuration_aggregator_name="example-configuration-aggregator-name",
            resource_type="example",
            filters=AggregateDiscoveredResources_Filters(account_id="example"),
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_aggregator_name" type="string" required>
  The real name of the AWS Config configuration aggregator this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="object">
  &lt;p&gt;Filters the results by resource account ID, region, resource ID, and resource name.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="region" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_identifiers" type="list(object)">
  The real, unique identifiers of the resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
