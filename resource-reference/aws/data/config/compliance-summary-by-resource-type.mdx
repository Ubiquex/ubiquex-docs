---
title: "aws_config_compliance_summary_by_resource_type"
description: "`aws_config_compliance_summary_by_resource_type` reads a real, aggregated compliance summary of resources, grouped by resource type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_compliance_summary_by_resource_type"})

    		ubx.Data(config.ComplianceSummaryByResourceType, "example", config.ComplianceSummaryByResourceTypeConfig{
    			ResourceTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceSummaryByResourceType } from "@ubx/sdk-aws/aws/data/config/compliance_summary_by_resource_type";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_compliance_summary_by_resource_type" });

      data(ComplianceSummaryByResourceType, "example", {
        resourceTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ComplianceSummaryByResourceType, ComplianceSummaryByResourceTypeConfig

    def describe():
        ubx.intent("look up aws_config_compliance_summary_by_resource_type")

        ubx.data(ComplianceSummaryByResourceType, "example", ComplianceSummaryByResourceTypeConfig(
            resource_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compliance_summaries_by_resource_type" type="list(object)">
  The real, discriminating type of the compliance summaries by resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
