---
title: "aws_config_compliance_details_by_resource"
description: "`aws_config_compliance_details_by_resource` reads real, detailed compliance evaluation results for a specific resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_compliance_details_by_resource"})

    		ubx.Data(config.ComplianceDetailsByResource, "example", config.ComplianceDetailsByResourceConfig{
    			ComplianceTypes:      []string{"example"},
    			ResourceEvaluationId: "example",
    			ResourceId:           "example",
    			ResourceType:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComplianceDetailsByResource } from "@ubx/sdk-aws/aws/data/config/compliance_details_by_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_compliance_details_by_resource" });

      data(ComplianceDetailsByResource, "example", {
        complianceTypes: ["example"],
        resourceEvaluationId: "example",
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ComplianceDetailsByResource, ComplianceDetailsByResourceConfig

    def describe():
        ubx.intent("look up aws_config_compliance_details_by_resource")

        ubx.data(ComplianceDetailsByResource, "example", ComplianceDetailsByResourceConfig(
            compliance_types=["example"],
            resource_evaluation_id="example",
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="compliance_types" type="list(string)">
  The real compliance types this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_evaluation_id" type="string">
  The real, unique identifier of the resource evaluation this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="evaluation_results" type="list(object)">
  The real AWS Config evaluation results included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
