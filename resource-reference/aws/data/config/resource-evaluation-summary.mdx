---
title: "aws_config_resource_evaluation_summary"
description: "`aws_config_resource_evaluation_summary` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_config_resource_evaluation_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_resource_evaluation_summary"})

    		ubx.Data(config.ResourceEvaluationSummary, "example", config.ResourceEvaluationSummaryConfig{
    			ResourceEvaluationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceEvaluationSummary } from "@ubx/sdk-aws/aws/data/config/resource_evaluation_summary";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_resource_evaluation_summary" });

      data(ResourceEvaluationSummary, "example", {
        resourceEvaluationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ResourceEvaluationSummary, ResourceEvaluationSummaryConfig

    def describe():
        ubx.intent("look up aws_config_resource_evaluation_summary")

        ubx.data(ResourceEvaluationSummary, "example", ResourceEvaluationSummaryConfig(
            resource_evaluation_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_evaluation_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compliance" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="evaluation_context" type="object">
  &lt;p&gt;Use EvaluationContext to group independently initiated proactive resource evaluations. For example, CFN Stack. If you want to check just a resource definition, you do not need to provide evaluation context.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="evaluation_context_identifier" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="evaluation_mode" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="evaluation_start_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="evaluation_status" type="object">
  &lt;p&gt;Returns status details of an evaluation.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="failure_reason" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_details" type="object">
  &lt;p&gt;Returns information about the resource being evaluated.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="resource_configuration" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_configuration_schema_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="resource_type" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
