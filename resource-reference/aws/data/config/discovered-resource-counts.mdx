---
title: "aws_config_discovered_resource_counts"
description: "`aws_config_discovered_resource_counts` reads real, aggregated counts of resources discovered by Config, grouped by type."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_discovered_resource_counts"})

    		ubx.Data(config.DiscoveredResourceCounts, "example", config.DiscoveredResourceCountsConfig{
    			Limit:         1,
    			ResourceTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DiscoveredResourceCounts } from "./local-sdk/aws/sdk/typescript/aws/data/config/discovered_resource_counts.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_discovered_resource_counts" });

      data(DiscoveredResourceCounts, "example", {
        limit: 1,
        resourceTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.config import DiscoveredResourceCounts, DiscoveredResourceCountsConfig

    def describe():
        ubx.intent("look up aws_config_discovered_resource_counts")

        ubx.data(DiscoveredResourceCounts, "example", DiscoveredResourceCountsConfig(
            limit=1,
            resource_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_counts" type="list(object)">
  The real counts of resources included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_discovered_resources" type="number">
  The real, total number of resources discovered. **(AI-inferred)** Returned by the lookup.
</ResponseField>
