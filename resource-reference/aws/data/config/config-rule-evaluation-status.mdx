---
title: "aws_config_config_rule_evaluation_status"
description: "`data_aws_config_config_rule_evaluation_status` looks up a real, already-existing `aws_config_config_rule`."
---

It evaluates a real AWS resource's configuration against a defined condition every time it's created, changed, or deleted, flagging the resource noncompliant and sending a notification the moment it violates the rule rather than requiring a caller to poll for drift.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_config_rule_evaluation_status"})

    		ubx.Data(config.ConfigRuleEvaluationStatus, "example", config.ConfigRuleEvaluationStatusConfig{
    			ConfigRuleNames: []string{"example"},
    			Limit:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigRuleEvaluationStatus } from "@ubx/sdk-aws/aws/data/config/config_rule_evaluation_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_config_rule_evaluation_status" });

      data(ConfigRuleEvaluationStatus, "example", {
        configRuleNames: ["example"],
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ConfigRuleEvaluationStatus, ConfigRuleEvaluationStatusConfig

    def describe():
        ubx.intent("look up aws_config_config_rule_evaluation_status")

        ubx.data(ConfigRuleEvaluationStatus, "example", ConfigRuleEvaluationStatusConfig(
            config_rule_names=["example"],
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_rule_names" type="list(string)">
  The real names of the AWS Config rules included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="config_rules_evaluation_status" type="list(object)">
  The real, current status of the config rules evaluation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
