---
title: "aws_config_configuration_aggregators"
description: "`data_aws_config_configuration_aggregators` looks up a real, already-existing `aws_config_configuration_aggregator`."
---

It pulls real AWS Config configuration and compliance data from multiple accounts and Regions into one, centralized account and Region, so a caller gets one unified inventory and compliance view instead of checking each account and Region separately.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_config_configuration_aggregators"})

    		ubx.Data(config.ConfigurationAggregators, "example", config.ConfigurationAggregatorsConfig{
    			ConfigurationAggregatorNames: []string{"example"},
    			Limit:                        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationAggregators } from "@ubx/sdk-aws/aws/data/config/configuration_aggregators";

    export default stack("example", () => {
      intent({ summary: "look up aws_config_configuration_aggregators" });

      data(ConfigurationAggregators, "example", {
        configurationAggregatorNames: ["example"],
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.config import ConfigurationAggregators, ConfigurationAggregatorsConfig

    def describe():
        ubx.intent("look up aws_config_configuration_aggregators")

        ubx.data(ConfigurationAggregators, "example", ConfigurationAggregatorsConfig(
            configuration_aggregator_names=["example"],
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_aggregator_names" type="list(string)">
  The real names of the configuration aggregators included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration_aggregators" type="list(object)">
  The real AWS Config configuration aggregators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
