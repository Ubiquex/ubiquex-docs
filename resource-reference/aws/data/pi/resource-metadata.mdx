---
title: "aws_pi_resource_metadata"
description: "`aws_pi_resource_metadata` reads real metadata about a database resource monitored by Performance Insights."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_resource_metadata"})

    		ubx.Data(pi.ResourceMetadata, "example", pi.ResourceMetadataConfig{
    			Identifier:  "example",
    			ServiceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceMetadata } from "@ubx/sdk-aws/aws/data/pi/resource_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_resource_metadata" });

      data(ResourceMetadata, "example", {
        identifier: "example",
        serviceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import ResourceMetadata, ResourceMetadataConfig

    def describe():
        ubx.intent("look up aws_pi_resource_metadata")

        ubx.data(ResourceMetadata, "example", ResourceMetadataConfig(
            identifier="example",
            service_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="features" type="map(object)">
  The real features included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
