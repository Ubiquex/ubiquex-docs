---
title: "aws_pi_resource_metrics"
description: "`aws_pi_resource_metrics` reads real, reported Performance Insights metric data for a database resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_resource_metrics"})

    		ubx.Data(pi.ResourceMetrics, "example", pi.ResourceMetricsConfig{
    			EndTime:         "example",
    			Identifier:      "example",
    			MetricQueries:   []map[string]any{{"filter": map[string]string{"key": "value"}}},
    			ServiceType:     "example",
    			StartTime:       "example",
    			MaxResults:      1,
    			PeriodAlignment: "example",
    			PeriodInSeconds: 7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceMetrics } from "@ubx/sdk-aws/aws/data/pi/resource_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_resource_metrics" });

      data(ResourceMetrics, "example", {
        endTime: "example",
        identifier: "example",
        metricQueries: [{ filter: { key: "value" } }],
        serviceType: "example",
        startTime: "example",
        maxResults: 1,
        periodAlignment: "example",
        periodInSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import ResourceMetrics, ResourceMetricsConfig

    def describe():
        ubx.intent("look up aws_pi_resource_metrics")

        ubx.data(ResourceMetrics, "example", ResourceMetricsConfig(
            end_time="example",
            identifier="example",
            metric_queries=[{"filter": {"key": "value"}}],
            service_type="example",
            start_time="example",
            max_results=1,
            period_alignment="example",
            period_in_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_queries" type="list(object)" required>
  The real metric queries included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="period_alignment" type="string">
  The real alignment (such as calendar month) applied to this period. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="period_in_seconds" type="number">
  The real time period this request covers, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aligned_end_time" type="string">
  The real timestamp of the aligned end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="aligned_start_time" type="string">
  The real timestamp of the aligned start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="metric_list" type="list(object)">
  The real metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
