---
title: "aws_pi_available_resource_metrics"
description: "`aws_pi_available_resource_metrics` reads the real, available Performance Insights metrics for a database resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_available_resource_metrics"})

    		ubx.Data(pi.AvailableResourceMetrics, "example", pi.AvailableResourceMetricsConfig{
    			Identifier:  "example",
    			MetricTypes: []string{"example"},
    			ServiceType: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AvailableResourceMetrics } from "./local-sdk/aws/sdk/typescript/aws/data/pi/available_resource_metrics.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_available_resource_metrics" });

      data(AvailableResourceMetrics, "example", {
        identifier: "example",
        metricTypes: ["example"],
        serviceType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.pi import AvailableResourceMetrics, AvailableResourceMetricsConfig

    def describe():
        ubx.intent("look up aws_pi_available_resource_metrics")

        ubx.data(AvailableResourceMetrics, "example", AvailableResourceMetricsConfig(
            identifier="example",
            metric_types=["example"],
            service_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metric_types" type="list(string)" required>
  The real, discriminating types of metrics this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metrics" type="list(object)">
  The real metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
