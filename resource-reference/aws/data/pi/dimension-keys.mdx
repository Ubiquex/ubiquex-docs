---
title: "aws_pi_dimension_keys"
description: "`aws_pi_dimension_keys` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pi_dimension_keys` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_dimension_keys"})

    		ubx.Data(pi.DimensionKeys, "example", pi.DimensionKeysConfig{
    			EndTime:           "example",
    			GroupBy:           map[string]any{"group": "example"},
    			Identifier:        "example",
    			Metric:            "example",
    			ServiceType:       "example",
    			StartTime:         "example",
    			AdditionalMetrics: []string{"example"},
    			Filter:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DimensionKeys } from "@ubx/sdk-aws/aws/data/pi/dimension_keys";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_dimension_keys" });

      data(DimensionKeys, "example", {
        endTime: "example",
        groupBy: { group: "example" },
        identifier: "example",
        metric: "example",
        serviceType: "example",
        startTime: "example",
        additionalMetrics: ["example"],
        filter: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import DimensionKeys, DimensionKeysConfig

    def describe():
        ubx.intent("look up aws_pi_dimension_keys")

        ubx.data(DimensionKeys, "example", DimensionKeysConfig(
            end_time="example",
            group_by={"group": "example"},
            identifier="example",
            metric="example",
            service_type="example",
            start_time="example",
            additional_metrics=["example"],
            filter={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="additional_metrics" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="map(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="object" required>
  &lt;p&gt;A logical grouping of Performance Insights metrics for a related subject area. For example, the &lt;code&gt;db.sql&lt;/code&gt; dimension group consists of the following dimensions:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql.id&lt;/code&gt; - The hash of a running SQL statement, generated by Performance Insights.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql.db_id&lt;/code&gt; - Either the SQL ID generated by the database engine, or a value generated by Performance Insights that begins with &lt;code&gt;pi-&lt;/code&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql.statement&lt;/code&gt; - The full text of the SQL statement that is running, for example, &lt;code&gt;SELECT * FROM employees&lt;/code&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql_tokenized.id&lt;/code&gt; - The hash of the SQL digest generated by Performance Insights.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; &lt;note&gt; &lt;p&gt;Each response element returns a maximum of 500 bytes. For larger elements, such as SQL statements, only the first 500 bytes are returned.&lt;/p&gt; &lt;/note&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimensions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="group" type="string" required>
      A generic string type that forbids characters that could expose our service (or services downstream) to security risks around injections. Required lookup argument.
    </ResponseField>
    <ResponseField name="limit" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metric" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="partition_by" type="object">
  &lt;p&gt;A logical grouping of Performance Insights metrics for a related subject area. For example, the &lt;code&gt;db.sql&lt;/code&gt; dimension group consists of the following dimensions:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql.id&lt;/code&gt; - The hash of a running SQL statement, generated by Performance Insights.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql.db_id&lt;/code&gt; - Either the SQL ID generated by the database engine, or a value generated by Performance Insights that begins with &lt;code&gt;pi-&lt;/code&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql.statement&lt;/code&gt; - The full text of the SQL statement that is running, for example, &lt;code&gt;SELECT * FROM employees&lt;/code&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;db.sql_tokenized.id&lt;/code&gt; - The hash of the SQL digest generated by Performance Insights.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; &lt;note&gt; &lt;p&gt;Each response element returns a maximum of 500 bytes. For larger elements, such as SQL statements, only the first 500 bytes are returned.&lt;/p&gt; &lt;/note&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="dimensions" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="group" type="string" required>
      A generic string type that forbids characters that could expose our service (or services downstream) to security risks around injections. Required lookup argument.
    </ResponseField>
    <ResponseField name="limit" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="period_in_seconds" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aligned_end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="aligned_start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="keys" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="partition_keys" type="list(object)">
  Returned by the lookup.
</ResponseField>
