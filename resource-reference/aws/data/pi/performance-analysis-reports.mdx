---
title: "aws_pi_performance_analysis_reports"
description: "`aws_pi_performance_analysis_reports` looks up one real, generated Performance Insights performance analysis report."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_performance_analysis_reports"})

    		ubx.Data(pi.PerformanceAnalysisReports, "example", pi.PerformanceAnalysisReportsConfig{
    			Identifier:  "example",
    			ServiceType: "example",
    			ListTags:    true,
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PerformanceAnalysisReports } from "@ubx/sdk-aws/aws/data/pi/performance_analysis_reports";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_performance_analysis_reports" });

      data(PerformanceAnalysisReports, "example", {
        identifier: "example",
        serviceType: "example",
        listTags: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import PerformanceAnalysisReports, PerformanceAnalysisReportsConfig

    def describe():
        ubx.intent("look up aws_pi_performance_analysis_reports")

        ubx.data(PerformanceAnalysisReports, "example", PerformanceAnalysisReportsConfig(
            identifier="example",
            service_type="example",
            list_tags=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="list_tags" type="bool">
  The real list tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analysis_reports" type="list(object)">
  The real reports for the analysis. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
