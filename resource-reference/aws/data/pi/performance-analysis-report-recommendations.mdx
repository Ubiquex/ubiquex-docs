---
title: "aws_pi_performance_analysis_report_recommendations"
description: "`aws_pi_performance_analysis_report_recommendations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_pi_performance_analysis_report_recommendations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_performance_analysis_report_recommendations"})

    		ubx.Data(pi.PerformanceAnalysisReportRecommendations, "example", pi.PerformanceAnalysisReportRecommendationsConfig{
    			AnalysisReportId:  "example",
    			Identifier:        "example",
    			ServiceType:       "example",
    			MaxResults:        1,
    			RecommendationIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PerformanceAnalysisReportRecommendations } from "@ubx/sdk-aws/aws/data/pi/performance_analysis_report_recommendations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_pi_performance_analysis_report_recommendations",
      });

      data(PerformanceAnalysisReportRecommendations, "example", {
        analysisReportId: "example",
        identifier: "example",
        serviceType: "example",
        maxResults: 1,
        recommendationIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import PerformanceAnalysisReportRecommendations, PerformanceAnalysisReportRecommendationsConfig

    def describe():
        ubx.intent("look up aws_pi_performance_analysis_report_recommendations")

        ubx.data(PerformanceAnalysisReportRecommendations, "example", PerformanceAnalysisReportRecommendationsConfig(
            analysis_report_id="example",
            identifier="example",
            service_type="example",
            max_results=1,
            recommendation_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analysis_report_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="recommendation_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recommendations" type="list(object)">
  Returned by the lookup.
</ResponseField>
