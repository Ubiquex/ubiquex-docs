---
title: "aws_pi_dimension_key_details"
description: "`aws_pi_dimension_key_details` reads real, detailed Performance Insights data for a specific dimension key, such as one SQL query."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_dimension_key_details"})

    		ubx.Data(pi.DimensionKeyDetails, "example", pi.DimensionKeyDetailsConfig{
    			Group:               "example",
    			GroupIdentifier:     "example",
    			Identifier:          "example",
    			ServiceType:         "example",
    			RequestedDimensions: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DimensionKeyDetails } from "@ubx/sdk-aws/aws/data/pi/dimension_key_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_dimension_key_details" });

      data(DimensionKeyDetails, "example", {
        group: "example",
        groupIdentifier: "example",
        identifier: "example",
        serviceType: "example",
        requestedDimensions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import DimensionKeyDetails, DimensionKeyDetailsConfig

    def describe():
        ubx.intent("look up aws_pi_dimension_key_details")

        ubx.data(DimensionKeyDetails, "example", DimensionKeyDetailsConfig(
            group="example",
            group_identifier="example",
            identifier="example",
            service_type="example",
            requested_dimensions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group" type="string" required>
  The real group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="group_identifier" type="string" required>
  The real, unique identifier of the group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="requested_dimensions" type="list(string)">
  The real dimensions requested for this metric. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dimensions" type="list(object)">
  The real, structured dimensions of this metric. **(AI-inferred)** Returned by the lookup.
</ResponseField>
