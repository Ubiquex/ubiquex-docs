---
title: "aws_pi_available_resource_dimensions"
description: "`aws_pi_available_resource_dimensions` reads the real, available Performance Insights dimension groups for a database resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pi "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/pi"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_pi_available_resource_dimensions"})

    		ubx.Data(pi.AvailableResourceDimensions, "example", pi.AvailableResourceDimensionsConfig{
    			Identifier:        "example",
    			Metrics:           []string{"example"},
    			ServiceType:       "example",
    			AuthorizedActions: []string{"example"},
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AvailableResourceDimensions } from "@ubx/sdk-aws/aws/data/pi/available_resource_dimensions";

    export default stack("example", () => {
      intent({ summary: "look up aws_pi_available_resource_dimensions" });

      data(AvailableResourceDimensions, "example", {
        identifier: "example",
        metrics: ["example"],
        serviceType: "example",
        authorizedActions: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.pi import AvailableResourceDimensions, AvailableResourceDimensionsConfig

    def describe():
        ubx.intent("look up aws_pi_available_resource_dimensions")

        ubx.data(AvailableResourceDimensions, "example", AvailableResourceDimensionsConfig(
            identifier="example",
            metrics=["example"],
            service_type="example",
            authorized_actions=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authorized_actions" type="list(string)">
  The real actions this identity is authorized to perform. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(string)" required>
  The real metrics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  The real type of AWS service this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_dimensions" type="list(object)">
  The real dimensions of this metric. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
