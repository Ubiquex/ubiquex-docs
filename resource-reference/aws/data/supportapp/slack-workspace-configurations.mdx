---
title: "aws_supportapp_slack_workspace_configurations"
description: "`data_aws_supportapp_slack_workspace_configurations` looks up a real, already-existing `aws_support_app_slack_workspace_configuration`."
---

It authorizes a real Slack workspace for the AWS Support App, the prerequisite step before any `aws_support_app_slack_channel_configuration` within that workspace can be created.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	supportapp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/supportapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_supportapp_slack_workspace_configurations"})

    		ubx.Data(supportapp.SlackWorkspaceConfigurations, "example", supportapp.SlackWorkspaceConfigurationsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SlackWorkspaceConfigurations } from "@ubx/sdk-aws/aws/data/supportapp/slack_workspace_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_supportapp_slack_workspace_configurations" });

      data(SlackWorkspaceConfigurations, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.supportapp import SlackWorkspaceConfigurations, SlackWorkspaceConfigurationsConfig

    def describe():
        ubx.intent("look up aws_supportapp_slack_workspace_configurations")

        ubx.data(SlackWorkspaceConfigurations, "example", SlackWorkspaceConfigurationsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="slack_workspace_configurations" type="list(object)">
  The real Slack workspace configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
