---
title: "aws_agreement_marketplace_agreement_payment_request"
description: "`aws_agreement_marketplace_agreement_payment_request` looks up one real payment request under an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_payment_request"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementPaymentRequest, "example", agreement_marketplace.AgreementMarketplaceAgreementPaymentRequestConfig{
    			AgreementId:      "example",
    			PaymentRequestId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceAgreementPaymentRequest } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_agreement_payment_request";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_agreement_payment_request",
      });

      data(AgreementMarketplaceAgreementPaymentRequest, "example", {
        agreementId: "example",
        paymentRequestId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementPaymentRequest, AgreementMarketplaceAgreementPaymentRequestConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_payment_request")

        ubx.data(AgreementMarketplaceAgreementPaymentRequest, "example", AgreementMarketplaceAgreementPaymentRequestConfig(
            agreement_id="example",
            payment_request_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string" required>
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="payment_request_id" type="string" required>
  The real, unique identifier of the payment request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="charge_amount" type="string">
  The real, monetary amount charged. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="charge_id" type="string">
  The real, unique identifier of the charge this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="currency_code" type="string">
  The real currency this value is denominated in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
