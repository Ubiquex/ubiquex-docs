---
title: "aws_agreement_marketplace_billing_adjustment_requests"
description: "`aws_agreement_marketplace_billing_adjustment_requests` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_agreement_marketplace_billing_adjustment_requests` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_billing_adjustment_requests"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceBillingAdjustmentRequests, "example", agreement_marketplace.AgreementMarketplaceBillingAdjustmentRequestsConfig{
    			AgreementId:   "example",
    			AgreementType: "example",
    			Catalog:       "example",
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    			MaxResults:    1,
    			Status:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceBillingAdjustmentRequests } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_billing_adjustment_requests";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_billing_adjustment_requests",
      });

      data(AgreementMarketplaceBillingAdjustmentRequests, "example", {
        agreementId: "example",
        agreementType: "example",
        catalog: "example",
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceBillingAdjustmentRequests, AgreementMarketplaceBillingAdjustmentRequestsConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_billing_adjustment_requests")

        ubx.data(AgreementMarketplaceBillingAdjustmentRequests, "example", AgreementMarketplaceBillingAdjustmentRequestsConfig(
            agreement_id="example",
            agreement_type="example",
            catalog="example",
            created_after="example",
            created_before="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="agreement_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="catalog" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="created_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
