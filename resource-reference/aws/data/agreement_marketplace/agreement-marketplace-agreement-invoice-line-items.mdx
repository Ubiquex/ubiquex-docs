---
title: "aws_agreement_marketplace_agreement_invoice_line_items"
description: "`aws_agreement_marketplace_agreement_invoice_line_items` reads the real invoice line items billed under an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_invoice_line_items"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementInvoiceLineItems, "example", agreement_marketplace.AgreementMarketplaceAgreementInvoiceLineItemsConfig{
    			AgreementId:          "example",
    			GroupBy:              "example",
    			AfterIssuedTime:      "example",
    			BeforeIssuedTime:     "example",
    			InvoiceBillingPeriod: map[string]any{"month": 1},
    			InvoiceId:            "example",
    			InvoiceType:          "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgreementMarketplaceAgreementInvoiceLineItems } from "./local-sdk/aws/sdk/typescript/aws/data/agreement_marketplace/agreement_marketplace_agreement_invoice_line_items.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_agreement_invoice_line_items",
      });

      data(AgreementMarketplaceAgreementInvoiceLineItems, "example", {
        agreementId: "example",
        groupBy: "example",
        afterIssuedTime: "example",
        beforeIssuedTime: "example",
        invoiceBillingPeriod: { month: 1 },
        invoiceId: "example",
        invoiceType: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementInvoiceLineItems, AgreementMarketplaceAgreementInvoiceLineItemsConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_invoice_line_items")

        ubx.data(AgreementMarketplaceAgreementInvoiceLineItems, "example", AgreementMarketplaceAgreementInvoiceLineItemsConfig(
            agreement_id="example",
            group_by="example",
            after_issued_time="example",
            before_issued_time="example",
            invoice_billing_period={"month": 1},
            invoice_id="example",
            invoice_type="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after_issued_time" type="string">
  The real timestamp of the after issued. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="agreement_id" type="string" required>
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="before_issued_time" type="string">
  The real timestamp of the before issued. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="string" required>
  The real field results are grouped by. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="invoice_billing_period" type="object">
  &lt;p&gt;The billing period for an invoice, specified by month and year.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="month" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="year" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="invoice_id" type="string">
  The real, unique identifier of the invoice this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="invoice_type" type="string">
  The real, discriminating type of the invoice this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agreement_invoice_line_item_group_summaries" type="list(object)">
  The real, summarized agreement invoice line item groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
