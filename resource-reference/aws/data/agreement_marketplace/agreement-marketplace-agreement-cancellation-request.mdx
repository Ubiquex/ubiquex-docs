---
title: "aws_agreement_marketplace_agreement_cancellation_request"
description: "`aws_agreement_marketplace_agreement_cancellation_request` looks up one real request to cancel an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_cancellation_request"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementCancellationRequest, "example", agreement_marketplace.AgreementMarketplaceAgreementCancellationRequestConfig{
    			AgreementCancellationRequestId: "example",
    			AgreementId:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgreementMarketplaceAgreementCancellationRequest } from "./local-sdk/aws/sdk/typescript/aws/data/agreement_marketplace/agreement_marketplace_agreement_cancellation_request.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_agreement_cancellation_request",
      });

      data(AgreementMarketplaceAgreementCancellationRequest, "example", {
        agreementCancellationRequestId: "example",
        agreementId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementCancellationRequest, AgreementMarketplaceAgreementCancellationRequestConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_cancellation_request")

        ubx.data(AgreementMarketplaceAgreementCancellationRequest, "example", AgreementMarketplaceAgreementCancellationRequestConfig(
            agreement_cancellation_request_id="example",
            agreement_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_cancellation_request_id" type="string" required>
  The real, unique identifier of the agreement cancellation request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="agreement_id" type="string" required>
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reason_code" type="string">
  The real code identifying the reason. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
