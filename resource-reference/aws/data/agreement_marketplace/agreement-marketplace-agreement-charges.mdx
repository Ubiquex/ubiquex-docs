---
title: "aws_agreement_marketplace_agreement_charges"
description: "`aws_agreement_marketplace_agreement_charges` reads the real, billed charges under an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_charges"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementCharges, "example", agreement_marketplace.AgreementMarketplaceAgreementChargesConfig{
    			AgreementId:   "example",
    			AgreementType: "example",
    			Catalog:       "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgreementMarketplaceAgreementCharges } from "./local-sdk/aws/sdk/typescript/aws/data/agreement_marketplace/agreement_marketplace_agreement_charges.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_agreement_marketplace_agreement_charges" });

      data(AgreementMarketplaceAgreementCharges, "example", {
        agreementId: "example",
        agreementType: "example",
        catalog: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementCharges, AgreementMarketplaceAgreementChargesConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_charges")

        ubx.data(AgreementMarketplaceAgreementCharges, "example", AgreementMarketplaceAgreementChargesConfig(
            agreement_id="example",
            agreement_type="example",
            catalog="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string">
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="agreement_type" type="string">
  The real type of the agreement this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="catalog" type="string">
  The real catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
