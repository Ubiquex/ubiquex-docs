---
title: "aws_agreement_marketplace_agreement_payment_requests"
description: "`aws_agreement_marketplace_agreement_payment_requests` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_agreement_marketplace_agreement_payment_requests` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_payment_requests"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementPaymentRequests, "example", agreement_marketplace.AgreementMarketplaceAgreementPaymentRequestsConfig{
    			PartyType:     "example",
    			AgreementId:   "example",
    			AgreementType: "example",
    			Catalog:       "example",
    			MaxResults:    1,
    			Status:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceAgreementPaymentRequests } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_agreement_payment_requests";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_agreement_payment_requests",
      });

      data(AgreementMarketplaceAgreementPaymentRequests, "example", {
        partyType: "example",
        agreementId: "example",
        agreementType: "example",
        catalog: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementPaymentRequests, AgreementMarketplaceAgreementPaymentRequestsConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_payment_requests")

        ubx.data(AgreementMarketplaceAgreementPaymentRequests, "example", AgreementMarketplaceAgreementPaymentRequestsConfig(
            party_type="example",
            agreement_id="example",
            agreement_type="example",
            catalog="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="agreement_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="catalog" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="party_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
