---
title: "aws_agreement_marketplace_agreement_terms"
description: "`aws_agreement_marketplace_agreement_terms` reads the real, negotiated terms (pricing, duration) of an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_terms"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementTerms, "example", agreement_marketplace.AgreementMarketplaceAgreementTermsConfig{
    			AgreementId: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AgreementMarketplaceAgreementTerms } from "./local-sdk/aws/sdk/typescript/aws/data/agreement_marketplace/agreement_marketplace_agreement_terms.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_agreement_marketplace_agreement_terms" });

      data(AgreementMarketplaceAgreementTerms, "example", {
        agreementId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementTerms, AgreementMarketplaceAgreementTermsConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_terms")

        ubx.data(AgreementMarketplaceAgreementTerms, "example", AgreementMarketplaceAgreementTermsConfig(
            agreement_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string" required>
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accepted_terms" type="list(object)">
  The real, accepted terms and conditions for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
