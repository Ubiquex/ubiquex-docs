---
title: "aws_agreement_marketplace_agreement_cancellation_requests"
description: "`aws_agreement_marketplace_agreement_cancellation_requests` reads the real requests to cancel AWS Marketplace agreements."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_cancellation_requests"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementCancellationRequests, "example", agreement_marketplace.AgreementMarketplaceAgreementCancellationRequestsConfig{
    			PartyType:     "example",
    			AgreementId:   "example",
    			AgreementType: "example",
    			Catalog:       "example",
    			MaxResults:    1,
    			Status:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceAgreementCancellationRequests } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_agreement_cancellation_requests";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_agreement_marketplace_agreement_cancellation_requests",
      });

      data(AgreementMarketplaceAgreementCancellationRequests, "example", {
        partyType: "example",
        agreementId: "example",
        agreementType: "example",
        catalog: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementCancellationRequests, AgreementMarketplaceAgreementCancellationRequestsConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_cancellation_requests")

        ubx.data(AgreementMarketplaceAgreementCancellationRequests, "example", AgreementMarketplaceAgreementCancellationRequestsConfig(
            party_type="example",
            agreement_id="example",
            agreement_type="example",
            catalog="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string">
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="agreement_type" type="string">
  The real type of the agreement this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="catalog" type="string">
  The real catalog this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="party_type" type="string" required>
  The real, discriminating type of the party this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items" type="list(object)">
  The real, primary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
