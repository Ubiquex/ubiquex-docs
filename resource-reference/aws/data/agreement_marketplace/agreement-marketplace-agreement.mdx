---
title: "aws_agreement_marketplace_agreement"
description: "`aws_agreement_marketplace_agreement` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_agreement_marketplace_agreement` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreement, "example", agreement_marketplace.AgreementMarketplaceAgreementConfig{
    			AgreementId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceAgreement } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_agreement";

    export default stack("example", () => {
      intent({ summary: "look up aws_agreement_marketplace_agreement" });

      data(AgreementMarketplaceAgreement, "example", {
        agreementId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreement, AgreementMarketplaceAgreementConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement")

        ubx.data(AgreementMarketplaceAgreement, "example", AgreementMarketplaceAgreementConfig(
            agreement_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="acceptance_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="acceptor" type="object">
  &lt;p&gt;The details of the party accepting the agreement terms. This is commonly the buyer for &lt;code&gt;PurchaseAgreement&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="agreement_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="estimated_charges" type="object">
  &lt;p&gt;Estimated cost of the agreement.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="agreement_value" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="currency_code" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proposal_summary" type="object">
  &lt;p&gt;A summary of the proposal received from the proposer.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="offer_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="offer_set_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resources" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proposer" type="object">
  &lt;p&gt;Details of the party proposing the agreement terms,. This is commonly the seller for &lt;code&gt;PurchaseAgreement&lt;/code&gt;. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  Returned by the lookup.
</ResponseField>
