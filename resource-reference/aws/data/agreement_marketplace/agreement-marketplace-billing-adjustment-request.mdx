---
title: "aws_agreement_marketplace_billing_adjustment_request"
description: "`aws_agreement_marketplace_billing_adjustment_request` looks up one real billing adjustment request under an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_billing_adjustment_request"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceBillingAdjustmentRequest, "example", agreement_marketplace.AgreementMarketplaceBillingAdjustmentRequestConfig{
    			AgreementId:                "example",
    			BillingAdjustmentRequestId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceBillingAdjustmentRequest } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_billing_adjustment_request";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_billing_adjustment_request",
      });

      data(AgreementMarketplaceBillingAdjustmentRequest, "example", {
        agreementId: "example",
        billingAdjustmentRequestId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceBillingAdjustmentRequest, AgreementMarketplaceBillingAdjustmentRequestConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_billing_adjustment_request")

        ubx.data(AgreementMarketplaceBillingAdjustmentRequest, "example", AgreementMarketplaceBillingAdjustmentRequestConfig(
            agreement_id="example",
            billing_adjustment_request_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string" required>
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="billing_adjustment_request_id" type="string" required>
  The real, unique identifier of the billing adjustment request this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="adjustment_amount" type="string">
  The real amount this value was adjusted by. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="adjustment_reason_code" type="string">
  The real code identifying the adjustment reason. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="currency_code" type="string">
  The real currency this value is denominated in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="original_invoice_id" type="string">
  The real, unique identifier of the original invoice this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
