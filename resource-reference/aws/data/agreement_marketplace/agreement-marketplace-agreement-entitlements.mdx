---
title: "aws_agreement_marketplace_agreement_entitlements"
description: "`aws_agreement_marketplace_agreement_entitlements` reads the real usage entitlements granted by an AWS Marketplace agreement."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	agreement_marketplace "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/agreement_marketplace"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_agreement_marketplace_agreement_entitlements"})

    		ubx.Data(agreement_marketplace.AgreementMarketplaceAgreementEntitlements, "example", agreement_marketplace.AgreementMarketplaceAgreementEntitlementsConfig{
    			AgreementId: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgreementMarketplaceAgreementEntitlements } from "@ubx/sdk-aws/aws/data/agreement_marketplace/agreement_marketplace_agreement_entitlements";

    export default stack("example", () => {
      intent({
        summary: "look up aws_agreement_marketplace_agreement_entitlements",
      });

      data(AgreementMarketplaceAgreementEntitlements, "example", {
        agreementId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.agreement_marketplace import AgreementMarketplaceAgreementEntitlements, AgreementMarketplaceAgreementEntitlementsConfig

    def describe():
        ubx.intent("look up aws_agreement_marketplace_agreement_entitlements")

        ubx.data(AgreementMarketplaceAgreementEntitlements, "example", AgreementMarketplaceAgreementEntitlementsConfig(
            agreement_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agreement_id" type="string" required>
  The real, unique identifier of the agreement this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="agreement_entitlements" type="list(object)">
  The real AWS Marketplace agreement entitlements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
