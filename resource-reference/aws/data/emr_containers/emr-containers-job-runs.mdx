---
title: "aws_emr_containers_job_runs"
description: "`aws_emr_containers_job_runs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_emr_containers_job_runs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr_containers "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/emr_containers"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_emr_containers_job_runs"})

    		ubx.Data(emr_containers.EmrContainersJobRuns, "example", emr_containers.EmrContainersJobRunsConfig{
    			VirtualClusterId: "example",
    			Name:             "example",
    			CreatedAfter:     "example",
    			CreatedBefore:    "example",
    			MaxResults:       1,
    			States:           []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EmrContainersJobRuns } from "@ubx/sdk-aws/aws/data/emr_containers/emr_containers_job_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_emr_containers_job_runs" });

      data(EmrContainersJobRuns, "example", {
        virtualClusterId: "example",
        name: "example",
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        states: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.emr_containers import EmrContainersJobRuns, EmrContainersJobRunsConfig

    def describe():
        ubx.intent("look up aws_emr_containers_job_runs")

        ubx.data(EmrContainersJobRuns, "example", EmrContainersJobRunsConfig(
            virtual_cluster_id="example",
            name="example",
            created_after="example",
            created_before="example",
            max_results=1,
            states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="states" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="virtual_cluster_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="job_runs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
