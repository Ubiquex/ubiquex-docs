---
title: "aws_emr_containers_managed_endpoints"
description: "`aws_emr_containers_managed_endpoints` looks up a real EMR on EKS managed endpoint, an interactive Spark or Livy connection into a virtual cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr_containers "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/emr_containers"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_emr_containers_managed_endpoints"})

    		ubx.Data(emr_containers.EmrContainersManagedEndpoints, "example", emr_containers.EmrContainersManagedEndpointsConfig{
    			VirtualClusterId: "example",
    			CreatedAfter:     "example",
    			CreatedBefore:    "example",
    			MaxResults:       1,
    			States:           []string{"example"},
    			Types:            []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EmrContainersManagedEndpoints } from "@ubx/sdk-aws/aws/data/emr_containers/emr_containers_managed_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_emr_containers_managed_endpoints" });

      data(EmrContainersManagedEndpoints, "example", {
        virtualClusterId: "example",
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
        states: ["example"],
        types: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.emr_containers import EmrContainersManagedEndpoints, EmrContainersManagedEndpointsConfig

    def describe():
        ubx.intent("look up aws_emr_containers_managed_endpoints")

        ubx.data(EmrContainersManagedEndpoints, "example", EmrContainersManagedEndpointsConfig(
            virtual_cluster_id="example",
            created_after="example",
            created_before="example",
            max_results=1,
            states=["example"],
            types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="states" type="list(string)">
  The real, current states of the objects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="types" type="list(string)">
  The real, discriminating types this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="virtual_cluster_id" type="string" required>
  The real, unique identifier of the virtual cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="endpoints" type="list(object)">
  The real endpoints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
