---
title: "aws_emr_containers_job_templates"
description: "`aws_emr_containers_job_templates` looks up a real EMR on EKS job template, a saved, reusable job configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr_containers "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/emr_containers"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_emr_containers_job_templates"})

    		ubx.Data(emr_containers.EmrContainersJobTemplates, "example", emr_containers.EmrContainersJobTemplatesConfig{
    			CreatedAfter:  "example",
    			CreatedBefore: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EmrContainersJobTemplates } from "@ubx/sdk-aws/aws/data/emr_containers/emr_containers_job_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_emr_containers_job_templates" });

      data(EmrContainersJobTemplates, "example", {
        createdAfter: "example",
        createdBefore: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.emr_containers import EmrContainersJobTemplates, EmrContainersJobTemplatesConfig

    def describe():
        ubx.intent("look up aws_emr_containers_job_templates")

        ubx.data(EmrContainersJobTemplates, "example", EmrContainersJobTemplatesConfig(
            created_after="example",
            created_before="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="templates" type="list(object)">
  The real templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
