---
title: "aws_emr_containers_managed_endpoint_session_credentials"
description: "`aws_emr_containers_managed_endpoint_session_credentials` reads real, temporary credentials for connecting to an EMR on EKS managed endpoint."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	emr_containers "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/emr_containers"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_emr_containers_managed_endpoint_session_credentials"})

    		ubx.Data(emr_containers.EmrContainersManagedEndpointSessionCredentials, "example", emr_containers.EmrContainersManagedEndpointSessionCredentialsConfig{
    			CredentialType:           "example",
    			EndpointIdentifier:       "example",
    			ExecutionRoleArn:         "arn:aws:iam::123456789012:policy/example",
    			VirtualClusterIdentifier: "example",
    			ClientToken:              "example",
    			DurationInSeconds:        7200,
    			LogContext:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EmrContainersManagedEndpointSessionCredentials } from "./local-sdk/aws/sdk/typescript/aws/data/emr_containers/emr_containers_managed_endpoint_session_credentials.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_emr_containers_managed_endpoint_session_credentials",
      });

      data(EmrContainersManagedEndpointSessionCredentials, "example", {
        credentialType: "example",
        endpointIdentifier: "example",
        executionRoleArn: "arn:aws:iam::123456789012:policy/example",
        virtualClusterIdentifier: "example",
        clientToken: "example",
        durationInSeconds: 7200,
        logContext: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.emr_containers import EmrContainersManagedEndpointSessionCredentials, EmrContainersManagedEndpointSessionCredentialsConfig

    def describe():
        ubx.intent("look up aws_emr_containers_managed_endpoint_session_credentials")

        ubx.data(EmrContainersManagedEndpointSessionCredentials, "example", EmrContainersManagedEndpointSessionCredentialsConfig(
            credential_type="example",
            endpoint_identifier="example",
            execution_role_arn="arn:aws:iam::123456789012:policy/example",
            virtual_cluster_identifier="example",
            client_token="example",
            duration_in_seconds=7200,
            log_context="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_token" type="string">
  A real, caller-supplied token guaranteeing this request's own idempotency. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="credential_type" type="string" required>
  The real, discriminating type of the credential this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="duration_in_seconds" type="number">
  The real duration of this object, in seconds. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="endpoint_identifier" type="string" required>
  The real, unique identifier of the endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="execution_role_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the IAM execution role this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="log_context" type="string">
  The real, structured context around this log entry. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="virtual_cluster_identifier" type="string" required>
  The real, unique identifier of the virtual cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="credentials" type="object">
  &lt;p&gt;The structure containing the session token being returned.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="endpoint_credentials" type="object">
  &lt;p&gt;The structure containing the session token being returned.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="expires_at" type="string">
  The real timestamp this object expires. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
