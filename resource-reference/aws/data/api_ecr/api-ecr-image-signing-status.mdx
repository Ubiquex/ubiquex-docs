---
title: "aws_api_ecr_image_signing_status"
description: "`aws_api_ecr_image_signing_status` reads the real, current signing status of an image in an ECR repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_image_signing_status"})

    		ubx.Data(api_ecr.ApiEcrImageSigningStatus, "example", api_ecr.ApiEcrImageSigningStatusConfig{
    			ImageId:        map[string]any{"image_digest": "example"},
    			RepositoryName: "example-repository-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrImageSigningStatus } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_image_signing_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_image_signing_status" });

      data(ApiEcrImageSigningStatus, "example", {
        imageId: { imageDigest: "example" },
        repositoryName: "example-repository-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrImageSigningStatus, ApiEcrImageSigningStatusConfig

    def describe():
        ubx.intent("look up aws_api_ecr_image_signing_status")

        ubx.data(ApiEcrImageSigningStatus, "example", ApiEcrImageSigningStatusConfig(
            image_id={"image_digest": "example"},
            repository_name="example-repository-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_id" type="object" required>
  &lt;p&gt;An object with identifying information for an image in an Amazon ECR repository.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="image_digest" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_tag" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="signing_statuses" type="list(object)">
  The real, current statuses of the signings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
