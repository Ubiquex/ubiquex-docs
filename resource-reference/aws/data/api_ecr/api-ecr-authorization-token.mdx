---
title: "aws_api_ecr_authorization_token"
description: "`aws_api_ecr_authorization_token` reads a real, temporary authorization token for authenticating a Docker client to ECR."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_authorization_token"})

    		ubx.Data(api_ecr.ApiEcrAuthorizationToken, "example", api_ecr.ApiEcrAuthorizationTokenConfig{
    			RegistryIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrAuthorizationToken } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_authorization_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_authorization_token" });

      data(ApiEcrAuthorizationToken, "example", {
        registryIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrAuthorizationToken, ApiEcrAuthorizationTokenConfig

    def describe():
        ubx.intent("look up aws_api_ecr_authorization_token")

        ubx.data(ApiEcrAuthorizationToken, "example", ApiEcrAuthorizationTokenConfig(
            registry_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="registry_ids" type="list(string)">
  The real, unique identifiers of the registrys this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="authorization_data" type="list(object)">
  The real, raw data of the authorization. **(AI-inferred)** Returned by the lookup.
</ResponseField>
