---
title: "aws_api_ecr_lifecycle_policy_preview"
description: "`aws_api_ecr_lifecycle_policy_preview` reads a real, previewed result of applying an ECR lifecycle policy before enabling it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_lifecycle_policy_preview"})

    		ubx.Data(api_ecr.ApiEcrLifecyclePolicyPreview, "example", api_ecr.ApiEcrLifecyclePolicyPreviewConfig{
    			RepositoryName: "example-repository-name",
    			Filter:         map[string]any{"tag_status": "example"},
    			ImageIds:       []map[string]any{{"image_digest": "example"}},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiEcrLifecyclePolicyPreview } from "./local-sdk/aws/sdk/typescript/aws/data/api_ecr/api_ecr_lifecycle_policy_preview.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_lifecycle_policy_preview" });

      data(ApiEcrLifecyclePolicyPreview, "example", {
        repositoryName: "example-repository-name",
        filter: { tagStatus: "example" },
        imageIds: [{ imageDigest: "example" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrLifecyclePolicyPreview, ApiEcrLifecyclePolicyPreviewConfig

    def describe():
        ubx.intent("look up aws_api_ecr_lifecycle_policy_preview")

        ubx.data(ApiEcrLifecyclePolicyPreview, "example", ApiEcrLifecyclePolicyPreviewConfig(
            repository_name="example-repository-name",
            filter={"tag_status": "example"},
            image_ids=[{"image_digest": "example"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;The filter for the lifecycle policy preview.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="tag_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="image_ids" type="list(object)">
  The real, unique identifiers of the images this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="lifecycle_policy_text" type="string">
  The real, free-text content of the lifecycle policy. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="preview_results" type="list(object)">
  The real, previewed results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary" type="object">
  &lt;p&gt;The summary of the lifecycle policy preview request.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="expiring_image_total_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="transitioning_image_total_counts" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
