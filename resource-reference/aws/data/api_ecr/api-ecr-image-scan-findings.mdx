---
title: "aws_api_ecr_image_scan_findings"
description: "`aws_api_ecr_image_scan_findings` reads the real vulnerability findings from scanning an image in an ECR repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_image_scan_findings"})

    		ubx.Data(api_ecr.ApiEcrImageScanFindings, "example", api_ecr.ApiEcrImageScanFindingsConfig{
    			ImageId:        map[string]any{"image_digest": "example"},
    			RepositoryName: "example-repository-name",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrImageScanFindings } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_image_scan_findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_image_scan_findings" });

      data(ApiEcrImageScanFindings, "example", {
        imageId: { imageDigest: "example" },
        repositoryName: "example-repository-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrImageScanFindings, ApiEcrImageScanFindingsConfig

    def describe():
        ubx.intent("look up aws_api_ecr_image_scan_findings")

        ubx.data(ApiEcrImageScanFindings, "example", ApiEcrImageScanFindingsConfig(
            image_id={"image_digest": "example"},
            repository_name="example-repository-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_id" type="object" required>
  &lt;p&gt;An object with identifying information for an image in an Amazon ECR repository.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="image_digest" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_tag" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="image_scan_findings" type="object">
  &lt;p&gt;The details of an image scan.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enhanced_findings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="finding_severity_counts" type="map(number)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="findings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_scan_completed_at" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="vulnerability_source_updated_at" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="image_scan_status" type="object">
  &lt;p&gt;The current status of an image scan.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
