---
title: "aws_api_ecr_registry_scanning_configuration"
description: "`aws_api_ecr_registry_scanning_configuration` reads the real, account-wide image scanning configuration for an ECR registry."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_registry_scanning_configuration"})

    		ubx.Data(api_ecr.ApiEcrRegistryScanningConfiguration, "example", api_ecr.ApiEcrRegistryScanningConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrRegistryScanningConfiguration } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_registry_scanning_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_registry_scanning_configuration" });

      data(ApiEcrRegistryScanningConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrRegistryScanningConfiguration, ApiEcrRegistryScanningConfigurationConfig

    def describe():
        ubx.intent("look up aws_api_ecr_registry_scanning_configuration")

        ubx.data(ApiEcrRegistryScanningConfiguration, "example", ApiEcrRegistryScanningConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scanning_configuration" type="object">
  &lt;p&gt;The scanning configuration for a private registry.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rules" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="scan_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
