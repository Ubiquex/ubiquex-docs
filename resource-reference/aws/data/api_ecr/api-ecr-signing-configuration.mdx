---
title: "aws_api_ecr_signing_configuration"
description: "`aws_api_ecr_signing_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_ecr_signing_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_signing_configuration"})

    		ubx.Data(api_ecr.ApiEcrSigningConfiguration, "example", api_ecr.ApiEcrSigningConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrSigningConfiguration } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_signing_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_signing_configuration" });

      data(ApiEcrSigningConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrSigningConfiguration, ApiEcrSigningConfigurationConfig

    def describe():
        ubx.intent("look up aws_api_ecr_signing_configuration")

        ubx.data(ApiEcrSigningConfiguration, "example", ApiEcrSigningConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="registry_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="signing_configuration" type="object">
  &lt;p&gt;The signing configuration for a registry, which specifies rules for automatically signing images when pushed.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="rules" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
