---
title: "aws_api_ecr_registry_policy"
description: "`aws_api_ecr_registry_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_ecr_registry_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_registry_policy"})

    		ubx.Data(api_ecr.ApiEcrRegistryPolicy, "example", api_ecr.ApiEcrRegistryPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrRegistryPolicy } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_registry_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_registry_policy" });

      data(ApiEcrRegistryPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrRegistryPolicy, ApiEcrRegistryPolicyConfig

    def describe():
        ubx.intent("look up aws_api_ecr_registry_policy")

        ubx.data(ApiEcrRegistryPolicy, "example", ApiEcrRegistryPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="policy_text" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Returned by the lookup.
</ResponseField>
