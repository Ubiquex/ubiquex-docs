---
title: "aws_api_ecr_registry_policy"
description: "`aws_api_ecr_registry_policy` reads the real resource-based policy attached to an ECR registry, controlling cross-account replication permissions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_registry_policy"})

    		ubx.Data(api_ecr.ApiEcrRegistryPolicy, "example", api_ecr.ApiEcrRegistryPolicyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiEcrRegistryPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/api_ecr/api_ecr_registry_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_registry_policy" });

      data(ApiEcrRegistryPolicy, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrRegistryPolicy, ApiEcrRegistryPolicyConfig

    def describe():
        ubx.intent("look up aws_api_ecr_registry_policy")

        ubx.data(ApiEcrRegistryPolicy, "example", ApiEcrRegistryPolicyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="policy_text" type="string">
  The real, raw text of the policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
