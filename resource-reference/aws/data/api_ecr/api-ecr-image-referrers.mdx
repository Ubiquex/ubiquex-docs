---
title: "aws_api_ecr_image_referrers"
description: "`aws_api_ecr_image_referrers` reads the real OCI artifacts (such as a signature) that reference a given image in an ECR repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_image_referrers"})

    		ubx.Data(api_ecr.ApiEcrImageReferrers, "example", api_ecr.ApiEcrImageReferrersConfig{
    			RepositoryName: "example-repository-name",
    			SubjectId:      map[string]any{"image_digest": "example"},
    			Filter:         map[string]any{"artifact_status": "example"},
    			MaxResults:     1,
    			RegistryId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrImageReferrers } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_image_referrers";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_image_referrers" });

      data(ApiEcrImageReferrers, "example", {
        repositoryName: "example-repository-name",
        subjectId: { imageDigest: "example" },
        filter: { artifactStatus: "example" },
        maxResults: 1,
        registryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrImageReferrers, ApiEcrImageReferrersConfig

    def describe():
        ubx.intent("look up aws_api_ecr_image_referrers")

        ubx.data(ApiEcrImageReferrers, "example", ApiEcrImageReferrersConfig(
            repository_name="example-repository-name",
            subject_id={"image_digest": "example"},
            filter={"artifact_status": "example"},
            max_results=1,
            registry_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="object">
  &lt;p&gt;An object representing a filter on a &lt;a&gt;ListImageReferrers&lt;/a&gt; operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="artifact_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="artifact_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="subject_id" type="object" required>
  &lt;p&gt;An object that identifies an image subject.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="image_digest" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="referrers" type="list(object)">
  The real referrers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
