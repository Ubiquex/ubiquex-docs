---
title: "aws_api_ecr_pull_time_update_exclusions"
description: "`aws_api_ecr_pull_time_update_exclusions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_ecr_pull_time_update_exclusions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_pull_time_update_exclusions"})

    		ubx.Data(api_ecr.ApiEcrPullTimeUpdateExclusions, "example", api_ecr.ApiEcrPullTimeUpdateExclusionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrPullTimeUpdateExclusions } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_pull_time_update_exclusions";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_pull_time_update_exclusions" });

      data(ApiEcrPullTimeUpdateExclusions, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrPullTimeUpdateExclusions, ApiEcrPullTimeUpdateExclusionsConfig

    def describe():
        ubx.intent("look up aws_api_ecr_pull_time_update_exclusions")

        ubx.data(ApiEcrPullTimeUpdateExclusions, "example", ApiEcrPullTimeUpdateExclusionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="pull_time_update_exclusions" type="list(string)">
  Returned by the lookup.
</ResponseField>
