---
title: "aws_api_ecr_lifecycle_policy"
description: "`aws_api_ecr_lifecycle_policy` reads the real image lifecycle policy of an ECR repository, controlling automatic image expiration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_lifecycle_policy"})

    		ubx.Data(api_ecr.ApiEcrLifecyclePolicy, "example", api_ecr.ApiEcrLifecyclePolicyConfig{
    			RepositoryName: "example-repository-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrLifecyclePolicy } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_lifecycle_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_lifecycle_policy" });

      data(ApiEcrLifecyclePolicy, "example", {
        repositoryName: "example-repository-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrLifecyclePolicy, ApiEcrLifecyclePolicyConfig

    def describe():
        ubx.intent("look up aws_api_ecr_lifecycle_policy")

        ubx.data(ApiEcrLifecyclePolicy, "example", ApiEcrLifecyclePolicyConfig(
            repository_name="example-repository-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_evaluated_at" type="string">
  The real timestamp this object was last evaluated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="lifecycle_policy_text" type="string">
  The real, free-text content of the lifecycle policy. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
