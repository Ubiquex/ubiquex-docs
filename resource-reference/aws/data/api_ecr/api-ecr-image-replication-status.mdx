---
title: "aws_api_ecr_image_replication_status"
description: "`aws_api_ecr_image_replication_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_ecr_image_replication_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_image_replication_status"})

    		ubx.Data(api_ecr.ApiEcrImageReplicationStatus, "example", api_ecr.ApiEcrImageReplicationStatusConfig{
    			ImageId:        map[string]any{"image_digest": "example"},
    			RepositoryName: "example-repository-name",
    			RegistryId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrImageReplicationStatus } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_image_replication_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_image_replication_status" });

      data(ApiEcrImageReplicationStatus, "example", {
        imageId: { imageDigest: "example" },
        repositoryName: "example-repository-name",
        registryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrImageReplicationStatus, ApiEcrImageReplicationStatusConfig

    def describe():
        ubx.intent("look up aws_api_ecr_image_replication_status")

        ubx.data(ApiEcrImageReplicationStatus, "example", ApiEcrImageReplicationStatusConfig(
            image_id={"image_digest": "example"},
            repository_name="example-repository-name",
            registry_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="image_id" type="object" required>
  &lt;p&gt;An object with identifying information for an image in an Amazon ECR repository.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="image_digest" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="image_tag" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="registry_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="replication_statuses" type="list(object)">
  Returned by the lookup.
</ResponseField>
