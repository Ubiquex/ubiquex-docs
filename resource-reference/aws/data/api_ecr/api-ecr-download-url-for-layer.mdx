---
title: "aws_api_ecr_download_url_for_layer"
description: "`aws_api_ecr_download_url_for_layer` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_api_ecr_download_url_for_layer` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_download_url_for_layer"})

    		ubx.Data(api_ecr.ApiEcrDownloadUrlForLayer, "example", api_ecr.ApiEcrDownloadUrlForLayerConfig{
    			LayerDigest:    "example",
    			RepositoryName: "example-repository-name",
    			RegistryId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiEcrDownloadUrlForLayer } from "@ubx/sdk-aws/aws/data/api_ecr/api_ecr_download_url_for_layer";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_download_url_for_layer" });

      data(ApiEcrDownloadUrlForLayer, "example", {
        layerDigest: "example",
        repositoryName: "example-repository-name",
        registryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrDownloadUrlForLayer, ApiEcrDownloadUrlForLayerConfig

    def describe():
        ubx.intent("look up aws_api_ecr_download_url_for_layer")

        ubx.data(ApiEcrDownloadUrlForLayer, "example", ApiEcrDownloadUrlForLayerConfig(
            layer_digest="example",
            repository_name="example-repository-name",
            registry_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="layer_digest" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="download_url" type="string">
  Returned by the lookup.
</ResponseField>
