---
title: "aws_api_ecr_download_url_for_layer"
description: "`aws_api_ecr_download_url_for_layer` reads a real, generated download URL for an image layer stored in an ECR repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	api_ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/api_ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_api_ecr_download_url_for_layer"})

    		ubx.Data(api_ecr.ApiEcrDownloadUrlForLayer, "example", api_ecr.ApiEcrDownloadUrlForLayerConfig{
    			LayerDigest:    "example",
    			RepositoryName: "example-repository-name",
    			RegistryId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ApiEcrDownloadUrlForLayer } from "./local-sdk/aws/sdk/typescript/aws/data/api_ecr/api_ecr_download_url_for_layer.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_api_ecr_download_url_for_layer" });

      data(ApiEcrDownloadUrlForLayer, "example", {
        layerDigest: "example",
        repositoryName: "example-repository-name",
        registryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.api_ecr import ApiEcrDownloadUrlForLayer, ApiEcrDownloadUrlForLayerConfig

    def describe():
        ubx.intent("look up aws_api_ecr_download_url_for_layer")

        ubx.data(ApiEcrDownloadUrlForLayer, "example", ApiEcrDownloadUrlForLayerConfig(
            layer_digest="example",
            repository_name="example-repository-name",
            registry_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="layer_digest" type="string" required>
  The real, cryptographic digest of this container image layer. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="registry_id" type="string">
  The real, unique identifier of the registry this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  The real name of the repository this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="download_url" type="string">
  The real, resolvable URL of the download this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
