---
title: "aws_servicecatalog_appregistry_applications"
description: "`aws_servicecatalog_appregistry_applications` looks up a real AppRegistry application, grouping related resources and metadata."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog_appregistry "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog_appregistry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_appregistry_applications"})

    		ubx.Data(servicecatalog_appregistry.ServicecatalogAppregistryApplications, "example", servicecatalog_appregistry.ServicecatalogAppregistryApplicationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicecatalogAppregistryApplications } from "@ubx/sdk-aws/aws/data/servicecatalog_appregistry/servicecatalog_appregistry_applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_appregistry_applications" });

      data(ServicecatalogAppregistryApplications, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog_appregistry import ServicecatalogAppregistryApplications, ServicecatalogAppregistryApplicationsConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_appregistry_applications")

        ubx.data(ServicecatalogAppregistryApplications, "example", ServicecatalogAppregistryApplicationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="applications" type="list(object)">
  The real applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
