---
title: "aws_servicecatalog_appregistry_configuration"
description: "`aws_servicecatalog_appregistry_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_servicecatalog_appregistry_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog_appregistry "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog_appregistry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_appregistry_configuration"})

    		ubx.Data(servicecatalog_appregistry.ServicecatalogAppregistryConfiguration, "example", servicecatalog_appregistry.ServicecatalogAppregistryConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicecatalogAppregistryConfiguration } from "@ubx/sdk-aws/aws/data/servicecatalog_appregistry/servicecatalog_appregistry_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_servicecatalog_appregistry_configuration" });

      data(ServicecatalogAppregistryConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog_appregistry import ServicecatalogAppregistryConfiguration, ServicecatalogAppregistryConfigurationConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_appregistry_configuration")

        ubx.data(ServicecatalogAppregistryConfiguration, "example", ServicecatalogAppregistryConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="configuration" type="object">
  &lt;p&gt; Includes all of the AppRegistry settings. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="tag_query_configuration" type="object">
      &lt;p&gt; The definition of &lt;code&gt;tagQuery&lt;/code&gt;. Specifies which resources are associated with an application. &lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="tag_key" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
