---
title: "aws_servicecatalog_appregistry_attribute_groups"
description: "`aws_servicecatalog_appregistry_attribute_groups` looks up a real AppRegistry attribute group, a set of custom metadata attached to applications."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	servicecatalog_appregistry "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicecatalog_appregistry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_appregistry_attribute_groups"})

    		ubx.Data(servicecatalog_appregistry.ServicecatalogAppregistryAttributeGroups, "example", servicecatalog_appregistry.ServicecatalogAppregistryAttributeGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ServicecatalogAppregistryAttributeGroups } from "./local-sdk/aws/sdk/typescript/aws/data/servicecatalog_appregistry/servicecatalog_appregistry_attribute_groups.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_servicecatalog_appregistry_attribute_groups",
      });

      data(ServicecatalogAppregistryAttributeGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog_appregistry import ServicecatalogAppregistryAttributeGroups, ServicecatalogAppregistryAttributeGroupsConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_appregistry_attribute_groups")

        ubx.data(ServicecatalogAppregistryAttributeGroups, "example", ServicecatalogAppregistryAttributeGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute_groups" type="list(object)">
  The real attribute groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
