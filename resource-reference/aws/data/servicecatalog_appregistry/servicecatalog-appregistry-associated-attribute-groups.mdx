---
title: "aws_servicecatalog_appregistry_associated_attribute_groups"
description: "`aws_servicecatalog_appregistry_associated_attribute_groups` reads the real attribute groups associated with an AppRegistry application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog_appregistry "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/servicecatalog_appregistry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicecatalog_appregistry_associated_attribute_groups"})

    		ubx.Data(servicecatalog_appregistry.ServicecatalogAppregistryAssociatedAttributeGroups, "example", servicecatalog_appregistry.ServicecatalogAppregistryAssociatedAttributeGroupsConfig{
    			Application: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServicecatalogAppregistryAssociatedAttributeGroups } from "@ubx/sdk-aws/aws/data/servicecatalog_appregistry/servicecatalog_appregistry_associated_attribute_groups";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_servicecatalog_appregistry_associated_attribute_groups",
      });

      data(ServicecatalogAppregistryAssociatedAttributeGroups, "example", {
        application: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicecatalog_appregistry import ServicecatalogAppregistryAssociatedAttributeGroups, ServicecatalogAppregistryAssociatedAttributeGroupsConfig

    def describe():
        ubx.intent("look up aws_servicecatalog_appregistry_associated_attribute_groups")

        ubx.data(ServicecatalogAppregistryAssociatedAttributeGroups, "example", ServicecatalogAppregistryAssociatedAttributeGroupsConfig(
            application="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application" type="string" required>
  The real application this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="attribute_groups" type="list(string)">
  The real attribute groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
