---
title: "aws_osis_pipeline_blueprints"
description: "`data_aws_osis_pipeline_blueprints` looks up a real, already-existing `aws_osis_pipeline`."
---

It creates a real, fully managed OpenSearch Ingestion pipeline, a serverless Data Prepper configuration (in YAML) that filters, transforms, and routes data from a source such as S3 into an OpenSearch domain or collection without infrastructure to manage.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	osis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/osis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_osis_pipeline_blueprints"})

    		ubx.Data(osis.PipelineBlueprints, "example", osis.PipelineBlueprintsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PipelineBlueprints } from "./local-sdk/aws/sdk/typescript/aws/data/osis/pipeline_blueprints.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_osis_pipeline_blueprints" });

      data(PipelineBlueprints, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.osis import PipelineBlueprints, PipelineBlueprintsConfig

    def describe():
        ubx.intent("look up aws_osis_pipeline_blueprints")

        ubx.data(PipelineBlueprints, "example", PipelineBlueprintsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="blueprints" type="list(object)">
  The real blueprints included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
