---
title: "aws_osis_pipeline_endpoint_connections"
description: "`aws_osis_pipeline_endpoint_connections` reads the real VPC endpoint connections to an OpenSearch Ingestion pipeline."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	osis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/osis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_osis_pipeline_endpoint_connections"})

    		ubx.Data(osis.PipelineEndpointConnections, "example", osis.PipelineEndpointConnectionsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { PipelineEndpointConnections } from "./local-sdk/aws/sdk/typescript/aws/data/osis/pipeline_endpoint_connections.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_osis_pipeline_endpoint_connections" });

      data(PipelineEndpointConnections, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.osis import PipelineEndpointConnections, PipelineEndpointConnectionsConfig

    def describe():
        ubx.intent("look up aws_osis_pipeline_endpoint_connections")

        ubx.data(PipelineEndpointConnections, "example", PipelineEndpointConnectionsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipeline_endpoint_connections" type="list(object)">
  The real MediaConnect pipeline endpoint connections included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
