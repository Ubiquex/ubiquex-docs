---
title: "aws_osis_pipeline_change_progress"
description: "`aws_osis_pipeline_change_progress` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_osis_pipeline_change_progress` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	osis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/osis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_osis_pipeline_change_progress"})

    		ubx.Data(osis.PipelineChangeProgress, "example", osis.PipelineChangeProgressConfig{
    			PipelineName: "example-pipeline-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PipelineChangeProgress } from "@ubx/sdk-aws/aws/data/osis/pipeline_change_progress";

    export default stack("example", () => {
      intent({ summary: "look up aws_osis_pipeline_change_progress" });

      data(PipelineChangeProgress, "example", {
        pipelineName: "example-pipeline-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.osis import PipelineChangeProgress, PipelineChangeProgressConfig

    def describe():
        ubx.intent("look up aws_osis_pipeline_change_progress")

        ubx.data(PipelineChangeProgress, "example", PipelineChangeProgressConfig(
            pipeline_name="example-pipeline-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="pipeline_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_progress_statuses" type="list(object)">
  Returned by the lookup.
</ResponseField>
