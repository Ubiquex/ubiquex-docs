---
title: "aws_osis_pipelines"
description: "`data_aws_osis_pipelines` looks up a real, already-existing `aws_osis_pipeline`."
---

It creates a real, fully managed OpenSearch Ingestion pipeline, a serverless Data Prepper configuration (in YAML) that filters, transforms, and routes data from a source such as S3 into an OpenSearch domain or collection without infrastructure to manage.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	osis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/osis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_osis_pipelines"})

    		ubx.Data(osis.Pipelines, "example", osis.PipelinesConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Pipelines } from "@ubx/sdk-aws/aws/data/osis/pipelines";

    export default stack("example", () => {
      intent({ summary: "look up aws_osis_pipelines" });

      data(Pipelines, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.osis import Pipelines, PipelinesConfig

    def describe():
        ubx.intent("look up aws_osis_pipelines")

        ubx.data(Pipelines, "example", PipelinesConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pipelines" type="list(object)">
  The real pipelines included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
