---
title: "aws_amplify_backend_environments"
description: "`aws_amplify_backend_environments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_amplify_backend_environments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplify_backend_environments"})

    		ubx.Data(amplify.BackendEnvironments, "example", amplify.BackendEnvironmentsConfig{
    			AppId:           "example",
    			EnvironmentName: "example-environment-name",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackendEnvironments } from "@ubx/sdk-aws/aws/data/amplify/backend_environments";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplify_backend_environments" });

      data(BackendEnvironments, "example", {
        appId: "example",
        environmentName: "example-environment-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.amplify import BackendEnvironments, BackendEnvironmentsConfig

    def describe():
        ubx.intent("look up aws_amplify_backend_environments")

        ubx.data(BackendEnvironments, "example", BackendEnvironmentsConfig(
            app_id="example",
            environment_name="example-environment-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="environment_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backend_environments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
