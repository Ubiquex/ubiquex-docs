---
title: "aws_amplify_backend_environments"
description: "`aws_amplify_backend_environments` looks up a real Amplify backend environment, linking an app branch to a provisioned backend."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_amplify_backend_environments"})

    		ubx.Data(amplify.BackendEnvironments, "example", amplify.BackendEnvironmentsConfig{
    			AppId:           "example",
    			EnvironmentName: "example-environment-name",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BackendEnvironments } from "./local-sdk/aws/sdk/typescript/aws/data/amplify/backend_environments.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_amplify_backend_environments" });

      data(BackendEnvironments, "example", {
        appId: "example",
        environmentName: "example-environment-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.amplify import BackendEnvironments, BackendEnvironmentsConfig

    def describe():
        ubx.intent("look up aws_amplify_backend_environments")

        ubx.data(BackendEnvironments, "example", BackendEnvironmentsConfig(
            app_id="example",
            environment_name="example-environment-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  The real, unique identifier of the app this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="environment_name" type="string">
  The real name of the environment this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backend_environments" type="list(object)">
  The real Amplify backend environments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
