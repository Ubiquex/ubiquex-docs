---
title: "aws_kinesis_analytics_v2_applications"
description: "`aws_kinesis_analytics_v2_applications` looks up a real Managed Service for Apache Flink application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis_analytics_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis_analytics_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_analytics_v2_applications"})

    		ubx.Data(kinesis_analytics_v2.KinesisAnalyticsV2Applications, "example", kinesis_analytics_v2.KinesisAnalyticsV2ApplicationsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KinesisAnalyticsV2Applications } from "@ubx/sdk-aws/aws/data/kinesis_analytics_v2/kinesis_analytics_v2_applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_analytics_v2_applications" });

      data(KinesisAnalyticsV2Applications, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis_analytics_v2 import KinesisAnalyticsV2Applications, KinesisAnalyticsV2ApplicationsConfig

    def describe():
        ubx.intent("look up aws_kinesis_analytics_v2_applications")

        ubx.data(KinesisAnalyticsV2Applications, "example", KinesisAnalyticsV2ApplicationsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="application_summaries" type="list(object)">
  The real, summarized applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The name of the application.&lt;/p&gt; Returned by the lookup.
</ResponseField>
