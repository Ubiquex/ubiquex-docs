---
title: "aws_kinesis_analytics_v2_application_snapshots"
description: "`aws_kinesis_analytics_v2_application_snapshots` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kinesis_analytics_v2_application_snapshots` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesis_analytics_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kinesis_analytics_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kinesis_analytics_v2_application_snapshots"})

    		ubx.Data(kinesis_analytics_v2.KinesisAnalyticsV2ApplicationSnapshots, "example", kinesis_analytics_v2.KinesisAnalyticsV2ApplicationSnapshotsConfig{
    			ApplicationName: "example-application-name",
    			Limit:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KinesisAnalyticsV2ApplicationSnapshots } from "@ubx/sdk-aws/aws/data/kinesis_analytics_v2/kinesis_analytics_v2_application_snapshots";

    export default stack("example", () => {
      intent({ summary: "look up aws_kinesis_analytics_v2_application_snapshots" });

      data(KinesisAnalyticsV2ApplicationSnapshots, "example", {
        applicationName: "example-application-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kinesis_analytics_v2 import KinesisAnalyticsV2ApplicationSnapshots, KinesisAnalyticsV2ApplicationSnapshotsConfig

    def describe():
        ubx.intent("look up aws_kinesis_analytics_v2_application_snapshots")

        ubx.data(KinesisAnalyticsV2ApplicationSnapshots, "example", KinesisAnalyticsV2ApplicationSnapshotsConfig(
            application_name="example-application-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_name" type="string" required>
  &lt;p&gt;The name of the application.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  &lt;p&gt;A pagination token that can be used in a subsequent request.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="snapshot_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
