---
title: "aws_backup_report_jobs"
description: "`aws_backup_report_jobs` reads the real AWS Backup report generation jobs on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_report_jobs"})

    		ubx.Data(backup.ReportJobs, "example", backup.ReportJobsConfig{
    			ByCreationAfter:  "example",
    			ByCreationBefore: "example",
    			ByReportPlanName: "example-by-report-plan-name",
    			ByStatus:         "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ReportJobs } from "@ubx/sdk-aws/aws/data/backup/report_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_report_jobs" });

      data(ReportJobs, "example", {
        byCreationAfter: "example",
        byCreationBefore: "example",
        byReportPlanName: "example-by-report-plan-name",
        byStatus: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import ReportJobs, ReportJobsConfig

    def describe():
        ubx.intent("look up aws_backup_report_jobs")

        ubx.data(ReportJobs, "example", ReportJobsConfig(
            by_creation_after="example",
            by_creation_before="example",
            by_report_plan_name="example-by-report-plan-name",
            by_status="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="by_creation_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="by_creation_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="by_report_plan_name" type="string">
  The real name of the by report plan this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="by_status" type="string">
  The real, current status of the by this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="report_jobs" type="list(object)">
  The real report jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
