---
title: "aws_backup_indexed_recovery_points"
description: "`aws_backup_indexed_recovery_points` looks up a real recovery point indexed for AWS Backup's own file-level search."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_indexed_recovery_points"})

    		ubx.Data(backup.IndexedRecoveryPoints, "example", backup.IndexedRecoveryPointsConfig{
    			CreatedAfter:      "example",
    			CreatedBefore:     "example",
    			IndexStatus:       "example",
    			MaxResults:        1,
    			ResourceType:      "example",
    			SourceResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IndexedRecoveryPoints } from "./local-sdk/aws/sdk/typescript/aws/data/backup/indexed_recovery_points.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_indexed_recovery_points" });

      data(IndexedRecoveryPoints, "example", {
        createdAfter: "example",
        createdBefore: "example",
        indexStatus: "example",
        maxResults: 1,
        resourceType: "example",
        sourceResourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import IndexedRecoveryPoints, IndexedRecoveryPointsConfig

    def describe():
        ubx.intent("look up aws_backup_indexed_recovery_points")

        ubx.data(IndexedRecoveryPoints, "example", IndexedRecoveryPointsConfig(
            created_after="example",
            created_before="example",
            index_status="example",
            max_results=1,
            resource_type="example",
            source_resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="created_after" type="string">
  The earliest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="created_before" type="string">
  The latest real creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="index_status" type="string">
  The real, current status of the index. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="source_resource_arn" type="string">
  The real Amazon Resource Name (ARN) of the source resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="indexed_recovery_points" type="list(object)">
  The real indexed recovery points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
