---
title: "aws_backup_supported_resource_types"
description: "`aws_backup_supported_resource_types` reads the real AWS resource types AWS Backup currently supports protecting."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_supported_resource_types"})

    		ubx.Data(backup.SupportedResourceTypes, "example", backup.SupportedResourceTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SupportedResourceTypes } from "./local-sdk/aws/sdk/typescript/aws/data/backup/supported_resource_types.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_supported_resource_types" });

      data(SupportedResourceTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import SupportedResourceTypes, SupportedResourceTypesConfig

    def describe():
        ubx.intent("look up aws_backup_supported_resource_types")

        ubx.data(SupportedResourceTypes, "example", SupportedResourceTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="resource_types" type="list(string)">
  The real, discriminating types of AWS resources this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
