---
title: "aws_backup_backup_plan_from_json"
description: "`aws_backup_backup_plan_from_json` reads a real backup plan parsed from a JSON document."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_plan_from_json"})

    		ubx.Data(backup.BackupPlanFromJson, "example", backup.BackupPlanFromJsonConfig{
    			BackupPlanTemplateJson: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupPlanFromJson } from "@ubx/sdk-aws/aws/data/backup/backup_plan_from_json";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_plan_from_json" });

      data(BackupPlanFromJson, "example", {
        backupPlanTemplateJson: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupPlanFromJson, BackupPlanFromJsonConfig

    def describe():
        ubx.intent("look up aws_backup_backup_plan_from_json")

        ubx.data(BackupPlanFromJson, "example", BackupPlanFromJsonConfig(
            backup_plan_template_json="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_plan_template_json" type="string" required>
  The real, JSON-formatted AWS Backup plan template. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_plan" type="object">
  &lt;p&gt;Contains an optional backup plan display name and an array of &lt;code&gt;BackupRule&lt;/code&gt; objects, each of which specifies a backup rule. Each rule in a backup plan is a separate scheduled task and can back up a different selection of Amazon Web Services resources.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="advanced_backup_settings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_plan_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="rules" type="list(object)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="scan_settings" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
