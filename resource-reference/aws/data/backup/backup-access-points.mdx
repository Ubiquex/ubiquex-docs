---
title: "aws_backup_backup_access_points"
description: "`aws_backup_backup_access_points` looks up a real S3 Access Point used to restrict access to a backup vault."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_access_points"})

    		ubx.Data(backup.BackupAccessPoints, "example", backup.BackupAccessPointsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupAccessPoints } from "@ubx/sdk-aws/aws/data/backup/backup_access_points";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_access_points" });

      data(BackupAccessPoints, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupAccessPoints, BackupAccessPointsConfig

    def describe():
        ubx.intent("look up aws_backup_backup_access_points")

        ubx.data(BackupAccessPoints, "example", BackupAccessPointsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_access_points" type="list(object)">
  The real AWS Backup access points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
