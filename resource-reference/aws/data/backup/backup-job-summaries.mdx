---
title: "aws_backup_backup_job_summaries"
description: "`aws_backup_backup_job_summaries` reads a real, aggregated summary of AWS Backup jobs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_job_summaries"})

    		ubx.Data(backup.BackupJobSummaries, "example", backup.BackupJobSummariesConfig{
    			AccountId:       "example",
    			MaxResults:      1,
    			MessageCategory: "example",
    			ResourceType:    "example",
    			State:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupJobSummaries } from "@ubx/sdk-aws/aws/data/backup/backup_job_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_job_summaries" });

      data(BackupJobSummaries, "example", {
        accountId: "example",
        maxResults: 1,
        messageCategory: "example",
        resourceType: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupJobSummaries, BackupJobSummariesConfig

    def describe():
        ubx.intent("look up aws_backup_backup_job_summaries")

        ubx.data(BackupJobSummaries, "example", BackupJobSummariesConfig(
            account_id="example",
            max_results=1,
            message_category="example",
            resource_type="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="message_category" type="string">
  The real category of the message this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregation_period" type="string">
  The real time period results are aggregated over. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_job_summaries" type="list(object)">
  The real, summarized backup jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
