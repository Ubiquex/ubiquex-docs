---
title: "aws_backup_copy_job_summaries"
description: "`aws_backup_copy_job_summaries` reads a real, aggregated summary of AWS Backup copy jobs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_copy_job_summaries"})

    		ubx.Data(backup.CopyJobSummaries, "example", backup.CopyJobSummariesConfig{
    			AccountId:       "example",
    			MaxResults:      1,
    			MessageCategory: "example",
    			ResourceType:    "example",
    			State:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CopyJobSummaries } from "./local-sdk/aws/sdk/typescript/aws/data/backup/copy_job_summaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_copy_job_summaries" });

      data(CopyJobSummaries, "example", {
        accountId: "example",
        maxResults: 1,
        messageCategory: "example",
        resourceType: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import CopyJobSummaries, CopyJobSummariesConfig

    def describe():
        ubx.intent("look up aws_backup_copy_job_summaries")

        ubx.data(CopyJobSummaries, "example", CopyJobSummariesConfig(
            account_id="example",
            max_results=1,
            message_category="example",
            resource_type="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="message_category" type="string">
  The real category of the message this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregation_period" type="string">
  The real time period results are aggregated over. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="copy_job_summaries" type="list(object)">
  The real, summarized copy jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
