---
title: "aws_backup_backup_vault_notifications"
description: "`aws_backup_backup_vault_notifications` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_backup_backup_vault_notifications` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_vault_notifications"})

    		ubx.Data(backup.BackupVaultNotifications, "example", backup.BackupVaultNotificationsConfig{
    			BackupVaultName: "example-backup-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupVaultNotifications } from "@ubx/sdk-aws/aws/data/backup/backup_vault_notifications";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_vault_notifications" });

      data(BackupVaultNotifications, "example", {
        backupVaultName: "example-backup-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupVaultNotifications, BackupVaultNotificationsConfig

    def describe():
        ubx.intent("look up aws_backup_backup_vault_notifications")

        ubx.data(BackupVaultNotifications, "example", BackupVaultNotificationsConfig(
            backup_vault_name="example-backup-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_vault_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="backup_vault_events" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="snstopic_arn" type="string">
  Returned by the lookup.
</ResponseField>
