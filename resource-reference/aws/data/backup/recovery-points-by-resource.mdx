---
title: "aws_backup_recovery_points_by_resource"
description: "`aws_backup_recovery_points_by_resource` reads the real recovery points for a specific protected resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_recovery_points_by_resource"})

    		ubx.Data(backup.RecoveryPointsByResource, "example", backup.RecoveryPointsByResourceConfig{
    			ResourceArn:            "arn:aws:iam::123456789012:policy/example",
    			ManagedByAwsbackupOnly: true,
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecoveryPointsByResource } from "@ubx/sdk-aws/aws/data/backup/recovery_points_by_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_recovery_points_by_resource" });

      data(RecoveryPointsByResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        managedByAwsbackupOnly: true,
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RecoveryPointsByResource, RecoveryPointsByResourceConfig

    def describe():
        ubx.intent("look up aws_backup_recovery_points_by_resource")

        ubx.data(RecoveryPointsByResource, "example", RecoveryPointsByResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            managed_by_awsbackup_only=True,
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="managed_by_awsbackup_only" type="bool">
  Whether this object is restricted to management by AWS Backup only. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_points" type="list(object)">
  The real AWS Backup recovery points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
