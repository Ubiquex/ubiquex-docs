---
title: "aws_backup_backup_vault_access_policy"
description: "`aws_backup_backup_vault_access_policy` reads the real resource-based access policy attached to a Backup vault."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_vault_access_policy"})

    		ubx.Data(backup.BackupVaultAccessPolicy, "example", backup.BackupVaultAccessPolicyConfig{
    			BackupVaultName: "example-backup-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BackupVaultAccessPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/backup/backup_vault_access_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_vault_access_policy" });

      data(BackupVaultAccessPolicy, "example", {
        backupVaultName: "example-backup-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupVaultAccessPolicy, BackupVaultAccessPolicyConfig

    def describe():
        ubx.intent("look up aws_backup_backup_vault_access_policy")

        ubx.data(BackupVaultAccessPolicy, "example", BackupVaultAccessPolicyConfig(
            backup_vault_name="example-backup-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_name" type="string" required>
  The real name of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_vault_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
