---
title: "aws_backup_recovery_points_by_backup_vault"
description: "`aws_backup_recovery_points_by_backup_vault` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_backup_recovery_points_by_backup_vault` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_recovery_points_by_backup_vault"})

    		ubx.Data(backup.RecoveryPointsByBackupVault, "example", backup.RecoveryPointsByBackupVaultConfig{
    			BackupVaultName:          "example-backup-vault-name",
    			BackupVaultAccountId:     "example",
    			ByBackupPlanId:           "example",
    			ByCreatedAfter:           "example",
    			ByCreatedBefore:          "example",
    			ByParentRecoveryPointArn: "arn:aws:iam::123456789012:policy/example",
    			ByResourceArn:            "arn:aws:iam::123456789012:policy/example",
    			ByResourceType:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecoveryPointsByBackupVault } from "@ubx/sdk-aws/aws/data/backup/recovery_points_by_backup_vault";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_recovery_points_by_backup_vault" });

      data(RecoveryPointsByBackupVault, "example", {
        backupVaultName: "example-backup-vault-name",
        backupVaultAccountId: "example",
        byBackupPlanId: "example",
        byCreatedAfter: "example",
        byCreatedBefore: "example",
        byParentRecoveryPointArn: "arn:aws:iam::123456789012:policy/example",
        byResourceArn: "arn:aws:iam::123456789012:policy/example",
        byResourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RecoveryPointsByBackupVault, RecoveryPointsByBackupVaultConfig

    def describe():
        ubx.intent("look up aws_backup_recovery_points_by_backup_vault")

        ubx.data(RecoveryPointsByBackupVault, "example", RecoveryPointsByBackupVaultConfig(
            backup_vault_name="example-backup-vault-name",
            backup_vault_account_id="example",
            by_backup_plan_id="example",
            by_created_after="example",
            by_created_before="example",
            by_parent_recovery_point_arn="arn:aws:iam::123456789012:policy/example",
            by_resource_arn="arn:aws:iam::123456789012:policy/example",
            by_resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="backup_vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="by_backup_plan_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="by_created_after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="by_created_before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="by_parent_recovery_point_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="by_resource_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="by_resource_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_points" type="list(object)">
  Returned by the lookup.
</ResponseField>
