---
title: "aws_backup_scan_job_summaries"
description: "`aws_backup_scan_job_summaries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_backup_scan_job_summaries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_scan_job_summaries"})

    		ubx.Data(backup.ScanJobSummaries, "example", backup.ScanJobSummariesConfig{
    			AccountId:        "example",
    			MalwareScanner:   "example",
    			MaxResults:       1,
    			ResourceType:     "example",
    			ScanResultStatus: "example",
    			State:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScanJobSummaries } from "@ubx/sdk-aws/aws/data/backup/scan_job_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_scan_job_summaries" });

      data(ScanJobSummaries, "example", {
        accountId: "example",
        malwareScanner: "example",
        maxResults: 1,
        resourceType: "example",
        scanResultStatus: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import ScanJobSummaries, ScanJobSummariesConfig

    def describe():
        ubx.intent("look up aws_backup_scan_job_summaries")

        ubx.data(ScanJobSummaries, "example", ScanJobSummariesConfig(
            account_id="example",
            malware_scanner="example",
            max_results=1,
            resource_type="example",
            scan_result_status="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="malware_scanner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scan_result_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="aggregation_period" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_job_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
