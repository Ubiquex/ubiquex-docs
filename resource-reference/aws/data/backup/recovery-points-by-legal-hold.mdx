---
title: "aws_backup_recovery_points_by_legal_hold"
description: "`aws_backup_recovery_points_by_legal_hold` reads the real recovery points currently retained under a specific AWS Backup legal hold."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_recovery_points_by_legal_hold"})

    		ubx.Data(backup.RecoveryPointsByLegalHold, "example", backup.RecoveryPointsByLegalHoldConfig{
    			LegalHoldId: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecoveryPointsByLegalHold } from "@ubx/sdk-aws/aws/data/backup/recovery_points_by_legal_hold";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_recovery_points_by_legal_hold" });

      data(RecoveryPointsByLegalHold, "example", {
        legalHoldId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RecoveryPointsByLegalHold, RecoveryPointsByLegalHoldConfig

    def describe():
        ubx.intent("look up aws_backup_recovery_points_by_legal_hold")

        ubx.data(RecoveryPointsByLegalHold, "example", RecoveryPointsByLegalHoldConfig(
            legal_hold_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="legal_hold_id" type="string" required>
  The real, unique identifier of the legal hold this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_points" type="list(object)">
  The real AWS Backup recovery points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
