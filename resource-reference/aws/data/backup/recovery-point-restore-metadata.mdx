---
title: "aws_backup_recovery_point_restore_metadata"
description: "`aws_backup_recovery_point_restore_metadata` reads the real metadata needed to restore a specific AWS Backup recovery point."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_recovery_point_restore_metadata"})

    		ubx.Data(backup.RecoveryPointRestoreMetadata, "example", backup.RecoveryPointRestoreMetadataConfig{
    			BackupVaultName:      "example-backup-vault-name",
    			RecoveryPointArn:     "arn:aws:iam::123456789012:policy/example",
    			BackupVaultAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RecoveryPointRestoreMetadata } from "@ubx/sdk-aws/aws/data/backup/recovery_point_restore_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_recovery_point_restore_metadata" });

      data(RecoveryPointRestoreMetadata, "example", {
        backupVaultName: "example-backup-vault-name",
        recoveryPointArn: "arn:aws:iam::123456789012:policy/example",
        backupVaultAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RecoveryPointRestoreMetadata, RecoveryPointRestoreMetadataConfig

    def describe():
        ubx.intent("look up aws_backup_recovery_point_restore_metadata")

        ubx.data(RecoveryPointRestoreMetadata, "example", RecoveryPointRestoreMetadataConfig(
            backup_vault_name="example-backup-vault-name",
            recovery_point_arn="arn:aws:iam::123456789012:policy/example",
            backup_vault_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_account_id" type="string">
  The real AWS account ID that owns the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="backup_vault_name" type="string" required>
  The real name of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="recovery_point_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the AWS Backup recovery point this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_vault_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="restore_metadata" type="map(string)">
  The real, structured metadata of the restore. **(AI-inferred)** Returned by the lookup.
</ResponseField>
