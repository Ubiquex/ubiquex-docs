---
title: "aws_backup_restore_job_metadata"
description: "`aws_backup_restore_job_metadata` reads the real metadata associated with an AWS Backup restore job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_restore_job_metadata"})

    		ubx.Data(backup.RestoreJobMetadata, "example", backup.RestoreJobMetadataConfig{
    			RestoreJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RestoreJobMetadata } from "./local-sdk/aws/sdk/typescript/aws/data/backup/restore_job_metadata.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_restore_job_metadata" });

      data(RestoreJobMetadata, "example", {
        restoreJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RestoreJobMetadata, RestoreJobMetadataConfig

    def describe():
        ubx.intent("look up aws_backup_restore_job_metadata")

        ubx.data(RestoreJobMetadata, "example", RestoreJobMetadataConfig(
            restore_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="restore_job_id" type="string" required>
  The real, unique identifier of the restore job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metadata" type="map(string)">
  The real, structured metadata of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
