---
title: "aws_backup_restore_testing_plans"
description: "`data_aws_backup_restore_testing_plans` looks up a real, already-existing `aws_backup_restore_testing_plan`."
---

It schedules AWS Backup to automatically restore a sample of real recovery points on a recurring basis and validate the restore succeeded, catching a broken or unrestorable backup before an actual disaster does, rather than trusting an untested backup plan.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_restore_testing_plans"})

    		ubx.Data(backup.RestoreTestingPlans, "example", backup.RestoreTestingPlansConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RestoreTestingPlans } from "@ubx/sdk-aws/aws/data/backup/restore_testing_plans";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_restore_testing_plans" });

      data(RestoreTestingPlans, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RestoreTestingPlans, RestoreTestingPlansConfig

    def describe():
        ubx.intent("look up aws_backup_restore_testing_plans")

        ubx.data(RestoreTestingPlans, "example", RestoreTestingPlansConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="restore_testing_plans" type="list(object)">
  The real AWS Backup restore testing plans included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
