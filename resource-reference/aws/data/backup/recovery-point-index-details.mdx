---
title: "aws_backup_recovery_point_index_details"
description: "`aws_backup_recovery_point_index_details` reads the real, current status of indexing a recovery point for file-level search."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_recovery_point_index_details"})

    		ubx.Data(backup.RecoveryPointIndexDetails, "example", backup.RecoveryPointIndexDetailsConfig{
    			BackupVaultName:  "example-backup-vault-name",
    			RecoveryPointArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RecoveryPointIndexDetails } from "./local-sdk/aws/sdk/typescript/aws/data/backup/recovery_point_index_details.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_recovery_point_index_details" });

      data(RecoveryPointIndexDetails, "example", {
        backupVaultName: "example-backup-vault-name",
        recoveryPointArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RecoveryPointIndexDetails, RecoveryPointIndexDetailsConfig

    def describe():
        ubx.intent("look up aws_backup_recovery_point_index_details")

        ubx.data(RecoveryPointIndexDetails, "example", RecoveryPointIndexDetailsConfig(
            backup_vault_name="example-backup-vault-name",
            recovery_point_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_name" type="string" required>
  The real name of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="recovery_point_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the AWS Backup recovery point this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_vault_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="index_completion_date" type="string">
  The real date of the index completion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="index_creation_date" type="string">
  The real date of the index creation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="index_deletion_date" type="string">
  The real date of the index deletion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="index_status" type="string">
  The real, current status of the index. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="index_status_message" type="string">
  The real, human-readable message describing the index status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_resource_arn" type="string">
  The real Amazon Resource Name (ARN) of the source resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_items_indexed" type="number">
  The real, total number of items indexed. **(AI-inferred)** Returned by the lookup.
</ResponseField>
