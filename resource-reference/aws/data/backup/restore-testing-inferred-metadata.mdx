---
title: "aws_backup_restore_testing_inferred_metadata"
description: "`aws_backup_restore_testing_inferred_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_backup_restore_testing_inferred_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_restore_testing_inferred_metadata"})

    		ubx.Data(backup.RestoreTestingInferredMetadata, "example", backup.RestoreTestingInferredMetadataConfig{
    			BackupVaultName:      "example-backup-vault-name",
    			RecoveryPointArn:     "arn:aws:iam::123456789012:policy/example",
    			BackupVaultAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RestoreTestingInferredMetadata } from "@ubx/sdk-aws/aws/data/backup/restore_testing_inferred_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_restore_testing_inferred_metadata" });

      data(RestoreTestingInferredMetadata, "example", {
        backupVaultName: "example-backup-vault-name",
        recoveryPointArn: "arn:aws:iam::123456789012:policy/example",
        backupVaultAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RestoreTestingInferredMetadata, RestoreTestingInferredMetadataConfig

    def describe():
        ubx.intent("look up aws_backup_restore_testing_inferred_metadata")

        ubx.data(RestoreTestingInferredMetadata, "example", RestoreTestingInferredMetadataConfig(
            backup_vault_name="example-backup-vault-name",
            recovery_point_arn="arn:aws:iam::123456789012:policy/example",
            backup_vault_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_account_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="backup_vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="recovery_point_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="inferred_metadata" type="map(string)">
  Returned by the lookup.
</ResponseField>
