---
title: "aws_backup_protected_resources_by_backup_vault"
description: "`aws_backup_protected_resources_by_backup_vault` reads the real resources protected by recovery points within a specific Backup vault."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_protected_resources_by_backup_vault"})

    		ubx.Data(backup.ProtectedResourcesByBackupVault, "example", backup.ProtectedResourcesByBackupVaultConfig{
    			BackupVaultName:      "example-backup-vault-name",
    			BackupVaultAccountId: "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProtectedResourcesByBackupVault } from "@ubx/sdk-aws/aws/data/backup/protected_resources_by_backup_vault";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_protected_resources_by_backup_vault" });

      data(ProtectedResourcesByBackupVault, "example", {
        backupVaultName: "example-backup-vault-name",
        backupVaultAccountId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import ProtectedResourcesByBackupVault, ProtectedResourcesByBackupVaultConfig

    def describe():
        ubx.intent("look up aws_backup_protected_resources_by_backup_vault")

        ubx.data(ProtectedResourcesByBackupVault, "example", ProtectedResourcesByBackupVaultConfig(
            backup_vault_name="example-backup-vault-name",
            backup_vault_account_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_vault_account_id" type="string">
  The real AWS account ID that owns the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="backup_vault_name" type="string" required>
  The real name of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
