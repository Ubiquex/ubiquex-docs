---
title: "aws_backup_backup_access_points_by_resource"
description: "`aws_backup_backup_access_points_by_resource` reads the real S3 Access Points scoped to a specific protected resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_access_points_by_resource"})

    		ubx.Data(backup.BackupAccessPointsByResource, "example", backup.BackupAccessPointsByResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupAccessPointsByResource } from "@ubx/sdk-aws/aws/data/backup/backup_access_points_by_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_access_points_by_resource" });

      data(BackupAccessPointsByResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupAccessPointsByResource, BackupAccessPointsByResourceConfig

    def describe():
        ubx.intent("look up aws_backup_backup_access_points_by_resource")

        ubx.data(BackupAccessPointsByResource, "example", BackupAccessPointsByResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="backup_access_points" type="list(object)">
  The real AWS Backup access points included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
