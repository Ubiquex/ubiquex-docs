---
title: "aws_backup_restore_job"
description: "`aws_backup_restore_job` looks up one real AWS Backup restore job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_restore_job"})

    		ubx.Data(backup.RestoreJob, "example", backup.RestoreJobConfig{
    			RestoreJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RestoreJob } from "./local-sdk/aws/sdk/typescript/aws/data/backup/restore_job.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_restore_job" });

      data(RestoreJob, "example", {
        restoreJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RestoreJob, RestoreJobConfig

    def describe():
        ubx.intent("look up aws_backup_restore_job")

        ubx.data(RestoreJob, "example", RestoreJobConfig(
            restore_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="restore_job_id" type="string" required>
  The real, unique identifier of the restore job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_size_in_bytes" type="number">
  The real size of the backup, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_vault_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="completion_date" type="string">
  The real timestamp this object completed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="object">
  &lt;p&gt;Contains information about the restore testing plan that Backup used to initiate the restore job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="restore_testing_plan_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_resource_arn" type="string">
  The real Amazon Resource Name (ARN) of the created resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="deletion_status" type="string">
  The real, current status of the deletion this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="deletion_status_message" type="string">
  The real, human-readable message describing the deletion status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expected_completion_time_minutes" type="number">
  The real, expected time until this operation completes, in minutes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_parent" type="bool">
  Whether this object is a parent object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parent_job_id" type="string">
  The real, unique identifier of the parent job this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="percent_done" type="string">
  The real, current progress of this operation, as a percentage. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_point_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup recovery point this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_point_creation_date" type="string">
  The real date of the recovery point creation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_resource_arn" type="string">
  The real Amazon Resource Name (ARN) of the source resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="validation_status" type="string">
  The real, current status of the validation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="validation_status_message" type="string">
  The real, human-readable message describing the validation status. **(AI-inferred)** Returned by the lookup.
</ResponseField>
