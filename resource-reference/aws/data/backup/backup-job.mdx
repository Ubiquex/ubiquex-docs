---
title: "aws_backup_backup_job"
description: "`aws_backup_backup_job` looks up one real AWS Backup job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_backup_job"})

    		ubx.Data(backup.BackupJob, "example", backup.BackupJobConfig{
    			BackupJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupJob } from "@ubx/sdk-aws/aws/data/backup/backup_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_backup_job" });

      data(BackupJob, "example", {
        backupJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import BackupJob, BackupJobConfig

    def describe():
        ubx.intent("look up aws_backup_backup_job")

        ubx.data(BackupJob, "example", BackupJobConfig(
            backup_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="backup_job_id" type="string" required>
  The real, unique identifier of the backup job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_options" type="map(string)">
  The real, available options for the backup. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_size_in_bytes" type="number">
  The real size of the backup, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_type" type="string">
  The real, discriminating type of the backup this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_vault_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="backup_vault_name" type="string">
  The real name of the AWS Backup vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bytes_transferred" type="number">
  The real number of bytes transferred. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="child_jobs_in_state" type="map(number)">
  The real, current state of the child jobs in this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="completion_date" type="string">
  The real timestamp this object completed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="object">
  &lt;p&gt;Contains information about the backup plan and rule that Backup used to initiate the recovery point backup.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="backup_plan_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_plan_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_plan_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_plan_version" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_rule_cron" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_rule_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_rule_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_rule_timezone" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  The real Amazon Resource Name (ARN) of the encryption key this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expected_completion_date" type="string">
  The real date of the expected completion. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="initiation_date" type="string">
  The real date of the initiation. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_encrypted" type="bool">
  Whether this object is encrypted. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_parent" type="bool">
  Whether this object is a parent object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="message_category" type="string">
  The real category of the message this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="number_of_child_jobs" type="number">
  The real number of child jobs spawned by this job. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parent_job_id" type="string">
  The real, unique identifier of the parent job this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="percent_done" type="string">
  The real, current progress of this operation, as a percentage. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_point_arn" type="string">
  The real Amazon Resource Name (ARN) of the AWS Backup recovery point this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_point_lifecycle" type="object">
  &lt;p&gt;Specifies the time period, in days, before a recovery point transitions to cold storage or is deleted.&lt;/p&gt; &lt;p&gt;Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, on the console, the retention setting must be 90 days greater than the transition to cold after days setting. The transition to cold after days setting can't be changed after a backup has been transitioned to cold.&lt;/p&gt; &lt;p&gt;Resource types that can transition to cold storage are listed in the &lt;a href="https://docs.aws.amazon.com/aws-backup/latest/devguide/backup-feature-availability.html#features-by-resource"&gt;Feature availability by resource&lt;/a&gt; table. Backup ignores this expression for other resource types.&lt;/p&gt; &lt;p&gt;To remove the existing lifecycle and retention periods and keep your recovery points indefinitely, specify -1 for &lt;code&gt;MoveToColdStorageAfterDays&lt;/code&gt; and &lt;code&gt;DeleteAfterDays&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="delete_after_days" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="delete_after_event" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="move_to_cold_storage_after_days" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="opt_in_to_archive_for_supported_resources" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_name" type="string">
  The real name of the resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="start_by" type="string">
  The real, latest timestamp this task must start by. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  The real, human-readable message describing the current status. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vault_lock_state" type="string">
  The real, current state of the vault lock this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vault_type" type="string">
  The real, discriminating type of the vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
