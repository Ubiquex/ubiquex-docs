---
title: "aws_backup_scan_job"
description: "`aws_backup_scan_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_backup_scan_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_scan_job"})

    		ubx.Data(backup.ScanJob, "example", backup.ScanJobConfig{
    			ScanJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScanJob } from "@ubx/sdk-aws/aws/data/backup/scan_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_scan_job" });

      data(ScanJob, "example", {
        scanJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import ScanJob, ScanJobConfig

    def describe():
        ubx.intent("look up aws_backup_scan_job")

        ubx.data(ScanJob, "example", ScanJobConfig(
            scan_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="scan_job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="backup_vault_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="backup_vault_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="completion_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="continuous_scan_end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="continuous_scan_start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="object">
  &lt;p&gt;Contains identifying information about the creation of a scan job, including the backup plan and rule that initiated the scan.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="backup_plan_arn" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="backup_plan_id" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="backup_plan_version" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="backup_rule_id" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creation_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="malware_scanner" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="recovery_point_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_base_recovery_point_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_mode" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="scan_result" type="object">
  &lt;p&gt;Contains information about the results of a scan job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="scan_result_status" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scanner_role_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  Returned by the lookup.
</ResponseField>
