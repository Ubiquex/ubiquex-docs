---
title: "aws_backup_frameworks"
description: "`data_aws_backup_frameworks` looks up a real, already-existing `aws_backup_framework`."
---

It is a set of built-in, customizable AWS Backup Audit Manager controls, aligned to an organization's own compliance requirements, that automatically track backup activity and flag resources not yet compliant with those controls.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_frameworks"})

    		ubx.Data(backup.Frameworks, "example", backup.FrameworksConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Frameworks } from "./local-sdk/aws/sdk/typescript/aws/data/backup/frameworks.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_frameworks" });

      data(Frameworks, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup import Frameworks, FrameworksConfig

    def describe():
        ubx.intent("look up aws_backup_frameworks")

        ubx.data(Frameworks, "example", FrameworksConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="frameworks" type="list(object)">
  The real AWS Audit Manager frameworks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
