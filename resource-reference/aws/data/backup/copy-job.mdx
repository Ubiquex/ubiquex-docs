---
title: "aws_backup_copy_job"
description: "`aws_backup_copy_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_backup_copy_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_copy_job"})

    		ubx.Data(backup.CopyJob, "example", backup.CopyJobConfig{
    			CopyJobId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CopyJob } from "@ubx/sdk-aws/aws/data/backup/copy_job";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_copy_job" });

      data(CopyJob, "example", {
        copyJobId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import CopyJob, CopyJobConfig

    def describe():
        ubx.intent("look up aws_backup_copy_job")

        ubx.data(CopyJob, "example", CopyJobConfig(
            copy_job_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="copy_job_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="copy_job" type="object">
  &lt;p&gt;Contains detailed information about a copy job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="backup_size_in_bytes" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="child_jobs_in_state" type="map(number)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="completion_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="composite_member_identifier" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="copy_job_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_by" type="object">
      &lt;p&gt;Contains information about the backup plan and rule that Backup used to initiate the recovery point backup.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="backup_plan_arn" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_plan_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_plan_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_plan_version" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_rule_cron" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_rule_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_rule_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="backup_rule_timezone" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_by_backup_job_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="creation_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_backup_vault_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_encryption_key_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_recovery_point_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_recovery_point_lifecycle" type="object">
      &lt;p&gt;Specifies the time period, in days, before a recovery point transitions to cold storage or is deleted.&lt;/p&gt; &lt;p&gt;Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, on the console, the retention setting must be 90 days greater than the transition to cold after days setting. The transition to cold after days setting can't be changed after a backup has been transitioned to cold.&lt;/p&gt; &lt;p&gt;Resource types that can transition to cold storage are listed in the &lt;a href="https://docs.aws.amazon.com/aws-backup/latest/devguide/backup-feature-availability.html#features-by-resource"&gt;Feature availability by resource&lt;/a&gt; table. Backup ignores this expression for other resource types.&lt;/p&gt; &lt;p&gt;To remove the existing lifecycle and retention periods and keep your recovery points indefinitely, specify -1 for &lt;code&gt;MoveToColdStorageAfterDays&lt;/code&gt; and &lt;code&gt;DeleteAfterDays&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="delete_after_days" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="delete_after_event" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="move_to_cold_storage_after_days" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="opt_in_to_archive_for_supported_resources" type="bool">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="destination_vault_lock_state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="destination_vault_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="iam_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="is_parent" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="message_category" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="number_of_child_jobs" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="parent_job_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_backup_vault_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="source_recovery_point_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="state" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
