---
title: "aws_backup_global_settings"
description: "`aws_backup_global_settings` reads the real, account-wide global settings for AWS Backup."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_global_settings"})

    		ubx.Data(backup.GlobalSettings, "example", backup.GlobalSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GlobalSettings } from "@ubx/sdk-aws/aws/data/backup/global_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_global_settings" });

      data(GlobalSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import GlobalSettings, GlobalSettingsConfig

    def describe():
        ubx.intent("look up aws_backup_global_settings")

        ubx.data(GlobalSettings, "example", GlobalSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="global_settings" type="map(string)">
  The real settings of the global. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_update_time" type="string">
  The real timestamp of the last update. **(AI-inferred)** Returned by the lookup.
</ResponseField>
