---
title: "aws_backup_restore_jobs_by_protected_resource"
description: "`aws_backup_restore_jobs_by_protected_resource` reads the real restore jobs run against a specific protected resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_restore_jobs_by_protected_resource"})

    		ubx.Data(backup.RestoreJobsByProtectedResource, "example", backup.RestoreJobsByProtectedResourceConfig{
    			ResourceArn:                       "arn:aws:iam::123456789012:policy/example",
    			ByRecoveryPointCreationDateAfter:  "example",
    			ByRecoveryPointCreationDateBefore: "example",
    			ByStatus:                          "example",
    			MaxResults:                        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RestoreJobsByProtectedResource } from "@ubx/sdk-aws/aws/data/backup/restore_jobs_by_protected_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_restore_jobs_by_protected_resource" });

      data(RestoreJobsByProtectedResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        byRecoveryPointCreationDateAfter: "example",
        byRecoveryPointCreationDateBefore: "example",
        byStatus: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup import RestoreJobsByProtectedResource, RestoreJobsByProtectedResourceConfig

    def describe():
        ubx.intent("look up aws_backup_restore_jobs_by_protected_resource")

        ubx.data(RestoreJobsByProtectedResource, "example", RestoreJobsByProtectedResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            by_recovery_point_creation_date_after="example",
            by_recovery_point_creation_date_before="example",
            by_status="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="by_recovery_point_creation_date_after" type="string">
  The earliest real AWS Backup recovery point creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="by_recovery_point_creation_date_before" type="string">
  The latest real AWS Backup recovery point creation timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="by_status" type="string">
  The real, current status of the by this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="restore_jobs" type="list(object)">
  The real AWS Backup restore jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
