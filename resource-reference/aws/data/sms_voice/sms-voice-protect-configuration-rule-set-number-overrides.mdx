---
title: "aws_sms_voice_protect_configuration_rule_set_number_overrides"
description: "`aws_sms_voice_protect_configuration_rule_set_number_overrides` reads the real, per-number overrides of an End User Messaging Protect configuration's..."
---

`aws_sms_voice_protect_configuration_rule_set_number_overrides` reads the real, per-number overrides of an End User Messaging Protect configuration's own rule set.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_protect_configuration_rule_set_number_overrides"})

    		ubx.Data(sms_voice.SmsVoiceProtectConfigurationRuleSetNumberOverrides, "example", sms_voice.SmsVoiceProtectConfigurationRuleSetNumberOverridesConfig{
    			ProtectConfigurationId: "example",
    			Filters:                []map[string]any{{"name": "example-name"}},
    			MaxResults:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceProtectConfigurationRuleSetNumberOverrides } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_protect_configuration_rule_set_number_overrides";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_sms_voice_protect_configuration_rule_set_number_overrides",
      });

      data(SmsVoiceProtectConfigurationRuleSetNumberOverrides, "example", {
        protectConfigurationId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceProtectConfigurationRuleSetNumberOverrides, SmsVoiceProtectConfigurationRuleSetNumberOverridesConfig

    def describe():
        ubx.intent("look up aws_sms_voice_protect_configuration_rule_set_number_overrides")

        ubx.data(SmsVoiceProtectConfigurationRuleSetNumberOverrides, "example", SmsVoiceProtectConfigurationRuleSetNumberOverridesConfig(
            protect_configuration_id="example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="protect_configuration_id" type="string" required>
  The real, unique identifier of the protect configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="protect_configuration_arn" type="string">
  The real Amazon Resource Name (ARN) of the protect configuration this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rule_set_number_overrides" type="list(object)">
  The real, overridden rule set numbers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
