---
title: "aws_sms_voice_keywords"
description: "`aws_sms_voice_keywords` reads a real SMS keyword (such as STOP or HELP) configured for a phone number or sender ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_keywords"})

    		ubx.Data(sms_voice.SmsVoiceKeywords, "example", sms_voice.SmsVoiceKeywordsConfig{
    			OriginationIdentity: "example",
    			Filters:             []map[string]any{{"name": "example-name"}},
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SmsVoiceKeywords } from "./local-sdk/aws/sdk/typescript/aws/data/sms_voice/sms_voice_keywords.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_keywords" });

      data(SmsVoiceKeywords, "example", {
        originationIdentity: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceKeywords, SmsVoiceKeywordsConfig

    def describe():
        ubx.intent("look up aws_sms_voice_keywords")

        ubx.data(SmsVoiceKeywords, "example", SmsVoiceKeywordsConfig(
            origination_identity="example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="origination_identity" type="string" required>
  The real origination identity this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="keywords" type="list(string)">
  The real keywords included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="origination_identity_arn" type="string">
  The real Amazon Resource Name (ARN) of the origination identity this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
