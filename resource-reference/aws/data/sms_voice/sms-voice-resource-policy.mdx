---
title: "aws_sms_voice_resource_policy"
description: "`aws_sms_voice_resource_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sms_voice_resource_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_resource_policy"})

    		ubx.Data(sms_voice.SmsVoiceResourcePolicy, "example", sms_voice.SmsVoiceResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceResourcePolicy } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_resource_policy" });

      data(SmsVoiceResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceResourcePolicy, SmsVoiceResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_sms_voice_resource_policy")

        ubx.data(SmsVoiceResourcePolicy, "example", SmsVoiceResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_timestamp" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>
