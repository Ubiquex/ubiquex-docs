---
title: "aws_sms_voice_registration_section_definitions"
description: "`aws_sms_voice_registration_section_definitions` reads the real section definitions required for an End User Messaging registration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_registration_section_definitions"})

    		ubx.Data(sms_voice.SmsVoiceRegistrationSectionDefinitions, "example", sms_voice.SmsVoiceRegistrationSectionDefinitionsConfig{
    			RegistrationType: "example",
    			MaxResults:       1,
    			SectionPaths:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceRegistrationSectionDefinitions } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_registration_section_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_registration_section_definitions" });

      data(SmsVoiceRegistrationSectionDefinitions, "example", {
        registrationType: "example",
        maxResults: 1,
        sectionPaths: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceRegistrationSectionDefinitions, SmsVoiceRegistrationSectionDefinitionsConfig

    def describe():
        ubx.intent("look up aws_sms_voice_registration_section_definitions")

        ubx.data(SmsVoiceRegistrationSectionDefinitions, "example", SmsVoiceRegistrationSectionDefinitionsConfig(
            registration_type="example",
            max_results=1,
            section_paths=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registration_type" type="string" required>
  The real, discriminating type of the registration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="section_paths" type="list(string)">
  The real section paths included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registration_section_definitions" type="list(object)">
  The real, structured definitions for the sections on this registration. **(AI-inferred)** Returned by the lookup.
</ResponseField>
