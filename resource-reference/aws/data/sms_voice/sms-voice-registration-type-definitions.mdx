---
title: "aws_sms_voice_registration_type_definitions"
description: "`aws_sms_voice_registration_type_definitions` reads the real, available registration types for End User Messaging SMS/Voice."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_registration_type_definitions"})

    		ubx.Data(sms_voice.SmsVoiceRegistrationTypeDefinitions, "example", sms_voice.SmsVoiceRegistrationTypeDefinitionsConfig{
    			Filters:           []map[string]any{{"name": "example-name"}},
    			MaxResults:        1,
    			RegistrationTypes: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceRegistrationTypeDefinitions } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_registration_type_definitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_registration_type_definitions" });

      data(SmsVoiceRegistrationTypeDefinitions, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        registrationTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceRegistrationTypeDefinitions, SmsVoiceRegistrationTypeDefinitionsConfig
    from ubx.aws.data.sms_voice.sms_voice_registration_type_definitions import SmsVoiceRegistrationTypeDefinitions_Filters

    def describe():
        ubx.intent("look up aws_sms_voice_registration_type_definitions")

        ubx.data(SmsVoiceRegistrationTypeDefinitions, "example", SmsVoiceRegistrationTypeDefinitionsConfig(
            filters=[SmsVoiceRegistrationTypeDefinitions_Filters(name="example-name")],
            max_results=1,
            registration_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registration_types" type="list(string)">
  The real, discriminating types of registrations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registration_type_definitions" type="list(object)">
  The real, structured definitions for the types on this registration. **(AI-inferred)** Returned by the lookup.
</ResponseField>
