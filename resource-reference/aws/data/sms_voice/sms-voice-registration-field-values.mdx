---
title: "aws_sms_voice_registration_field_values"
description: "`aws_sms_voice_registration_field_values` reads the real, submitted field values of an End User Messaging registration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_registration_field_values"})

    		ubx.Data(sms_voice.SmsVoiceRegistrationFieldValues, "example", sms_voice.SmsVoiceRegistrationFieldValuesConfig{
    			RegistrationId: "example",
    			FieldPaths:     []string{"example"},
    			MaxResults:     1,
    			SectionPath:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceRegistrationFieldValues } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_registration_field_values";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_registration_field_values" });

      data(SmsVoiceRegistrationFieldValues, "example", {
        registrationId: "example",
        fieldPaths: ["example"],
        maxResults: 1,
        sectionPath: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceRegistrationFieldValues, SmsVoiceRegistrationFieldValuesConfig

    def describe():
        ubx.intent("look up aws_sms_voice_registration_field_values")

        ubx.data(SmsVoiceRegistrationFieldValues, "example", SmsVoiceRegistrationFieldValuesConfig(
            registration_id="example",
            field_paths=["example"],
            max_results=1,
            section_path="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="field_paths" type="list(string)">
  The real field paths included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="registration_id" type="string" required>
  The real, unique identifier of the registration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="section_path" type="string">
  The real path of the section. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registration_arn" type="string">
  The real Amazon Resource Name (ARN) of the registration this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="registration_field_values" type="list(object)">
  The real values supplied for the fields on this registration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_number" type="number">
  The real, numeric version of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
