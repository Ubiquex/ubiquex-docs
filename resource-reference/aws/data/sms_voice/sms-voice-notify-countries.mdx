---
title: "aws_sms_voice_notify_countries"
description: "`aws_sms_voice_notify_countries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sms_voice_notify_countries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_notify_countries"})

    		ubx.Data(sms_voice.SmsVoiceNotifyCountries, "example", sms_voice.SmsVoiceNotifyCountriesConfig{
    			Channels:   []string{"example"},
    			MaxResults: 1,
    			Tier:       "example",
    			UseCases:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceNotifyCountries } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_notify_countries";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_notify_countries" });

      data(SmsVoiceNotifyCountries, "example", {
        channels: ["example"],
        maxResults: 1,
        tier: "example",
        useCases: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceNotifyCountries, SmsVoiceNotifyCountriesConfig

    def describe():
        ubx.intent("look up aws_sms_voice_notify_countries")

        ubx.data(SmsVoiceNotifyCountries, "example", SmsVoiceNotifyCountriesConfig(
            channels=["example"],
            max_results=1,
            tier="example",
            use_cases=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channels" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tier" type="string">
  &lt;p&gt;The tier of a notify configuration.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;BASIC&lt;/code&gt; - Basic tier.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;ADVANCED&lt;/code&gt; - Advanced tier.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="use_cases" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="notify_countries" type="list(object)">
  Returned by the lookup.
</ResponseField>
