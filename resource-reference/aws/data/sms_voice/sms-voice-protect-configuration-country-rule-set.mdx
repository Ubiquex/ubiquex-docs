---
title: "aws_sms_voice_protect_configuration_country_rule_set"
description: "`aws_sms_voice_protect_configuration_country_rule_set` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sms_voice_protect_configuration_country_rule_set` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_protect_configuration_country_rule_set"})

    		ubx.Data(sms_voice.SmsVoiceProtectConfigurationCountryRuleSet, "example", sms_voice.SmsVoiceProtectConfigurationCountryRuleSetConfig{
    			NumberCapability:       "example",
    			ProtectConfigurationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceProtectConfigurationCountryRuleSet } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_protect_configuration_country_rule_set";

    export default stack("example", () => {
      intent({
        summary: "look up aws_sms_voice_protect_configuration_country_rule_set",
      });

      data(SmsVoiceProtectConfigurationCountryRuleSet, "example", {
        numberCapability: "example",
        protectConfigurationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceProtectConfigurationCountryRuleSet, SmsVoiceProtectConfigurationCountryRuleSetConfig

    def describe():
        ubx.intent("look up aws_sms_voice_protect_configuration_country_rule_set")

        ubx.data(SmsVoiceProtectConfigurationCountryRuleSet, "example", SmsVoiceProtectConfigurationCountryRuleSetConfig(
            number_capability="example",
            protect_configuration_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="number_capability" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="protect_configuration_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="country_rule_set" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="protect_configuration_arn" type="string">
  Returned by the lookup.
</ResponseField>
