---
title: "aws_sms_voice_account_limits"
description: "`aws_sms_voice_account_limits` reads real, account-level End User Messaging SMS/Voice limits and current usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_account_limits"})

    		ubx.Data(sms_voice.SmsVoiceAccountLimits, "example", sms_voice.SmsVoiceAccountLimitsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceAccountLimits } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_account_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_account_limits" });

      data(SmsVoiceAccountLimits, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceAccountLimits, SmsVoiceAccountLimitsConfig

    def describe():
        ubx.intent("look up aws_sms_voice_account_limits")

        ubx.data(SmsVoiceAccountLimits, "example", SmsVoiceAccountLimitsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account_limits" type="list(object)">
  The real, current account-level limits for this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
