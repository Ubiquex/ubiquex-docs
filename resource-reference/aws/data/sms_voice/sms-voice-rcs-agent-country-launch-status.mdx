---
title: "aws_sms_voice_rcs_agent_country_launch_status"
description: "`aws_sms_voice_rcs_agent_country_launch_status` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sms_voice_rcs_agent_country_launch_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_rcs_agent_country_launch_status"})

    		ubx.Data(sms_voice.SmsVoiceRcsAgentCountryLaunchStatus, "example", sms_voice.SmsVoiceRcsAgentCountryLaunchStatusConfig{
    			RcsAgentId:      "example",
    			Filters:         []map[string]any{{"name": "example-name"}},
    			IsoCountryCodes: []string{"example"},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceRcsAgentCountryLaunchStatus } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_rcs_agent_country_launch_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_rcs_agent_country_launch_status" });

      data(SmsVoiceRcsAgentCountryLaunchStatus, "example", {
        rcsAgentId: "example",
        filters: [{ name: "example-name" }],
        isoCountryCodes: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceRcsAgentCountryLaunchStatus, SmsVoiceRcsAgentCountryLaunchStatusConfig

    def describe():
        ubx.intent("look up aws_sms_voice_rcs_agent_country_launch_status")

        ubx.data(SmsVoiceRcsAgentCountryLaunchStatus, "example", SmsVoiceRcsAgentCountryLaunchStatusConfig(
            rcs_agent_id="example",
            filters=[{"name": "example-name"}],
            iso_country_codes=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="iso_country_codes" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rcs_agent_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="country_launch_status" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rcs_agent_arn" type="string">
  Returned by the lookup.
</ResponseField>
