---
title: "aws_sms_voice_spend_limits"
description: "`aws_sms_voice_spend_limits` reads the real, configured monthly spend limits for End User Messaging SMS/Voice."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_spend_limits"})

    		ubx.Data(sms_voice.SmsVoiceSpendLimits, "example", sms_voice.SmsVoiceSpendLimitsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SmsVoiceSpendLimits } from "./local-sdk/aws/sdk/typescript/aws/data/sms_voice/sms_voice_spend_limits.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_spend_limits" });

      data(SmsVoiceSpendLimits, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceSpendLimits, SmsVoiceSpendLimitsConfig

    def describe():
        ubx.intent("look up aws_sms_voice_spend_limits")

        ubx.data(SmsVoiceSpendLimits, "example", SmsVoiceSpendLimitsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="spend_limits" type="list(object)">
  The real spending limits configured for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
