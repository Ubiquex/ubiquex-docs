---
title: "aws_sms_voice_opted_out_numbers"
description: "`aws_sms_voice_opted_out_numbers` reads the real phone numbers that have opted out of receiving messages from a phone number or sender ID."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_opted_out_numbers"})

    		ubx.Data(sms_voice.SmsVoiceOptedOutNumbers, "example", sms_voice.SmsVoiceOptedOutNumbersConfig{
    			OptOutListName: "example-opt-out-list-name",
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceOptedOutNumbers } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_opted_out_numbers";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_opted_out_numbers" });

      data(SmsVoiceOptedOutNumbers, "example", {
        optOutListName: "example-opt-out-list-name",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceOptedOutNumbers, SmsVoiceOptedOutNumbersConfig

    def describe():
        ubx.intent("look up aws_sms_voice_opted_out_numbers")

        ubx.data(SmsVoiceOptedOutNumbers, "example", SmsVoiceOptedOutNumbersConfig(
            opt_out_list_name="example-opt-out-list-name",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="opt_out_list_name" type="string" required>
  The real name of the opt out list this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="opt_out_list_arn" type="string">
  The real Amazon Resource Name (ARN) of the opt out list this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="opted_out_numbers" type="list(string)">
  The real phone numbers that have opted out. **(AI-inferred)** Returned by the lookup.
</ResponseField>
