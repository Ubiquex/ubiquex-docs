---
title: "aws_sms_voice_phone_numbers"
description: "`aws_sms_voice_phone_numbers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_sms_voice_phone_numbers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_phone_numbers"})

    		ubx.Data(sms_voice.SmsVoicePhoneNumbers, "example", sms_voice.SmsVoicePhoneNumbersConfig{
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    			Owner:          "example",
    			PhoneNumberIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoicePhoneNumbers } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_phone_numbers";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_phone_numbers" });

      data(SmsVoicePhoneNumbers, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        owner: "example",
        phoneNumberIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoicePhoneNumbers, SmsVoicePhoneNumbersConfig

    def describe():
        ubx.intent("look up aws_sms_voice_phone_numbers")

        ubx.data(SmsVoicePhoneNumbers, "example", SmsVoicePhoneNumbersConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            owner="example",
            phone_number_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_ids" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="phone_numbers" type="list(object)">
  Returned by the lookup.
</ResponseField>
