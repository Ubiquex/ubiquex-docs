---
title: "aws_sms_voice_phone_numbers"
description: "`data_aws_sms_voice_phone_numbers` looks up a real, already-existing `aws_smsvoice_phone_number`."
---

It provisions a real phone number, long code, toll-free, or short code, for sending and receiving SMS, MMS, or voice messages through AWS End User Messaging.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_phone_numbers"})

    		ubx.Data(sms_voice.SmsVoicePhoneNumbers, "example", sms_voice.SmsVoicePhoneNumbersConfig{
    			Filters:        []map[string]any{{"name": "example-name"}},
    			MaxResults:     1,
    			Owner:          "example",
    			PhoneNumberIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SmsVoicePhoneNumbers } from "./local-sdk/aws/sdk/typescript/aws/data/sms_voice/sms_voice_phone_numbers.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_phone_numbers" });

      data(SmsVoicePhoneNumbers, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        owner: "example",
        phoneNumberIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoicePhoneNumbers, SmsVoicePhoneNumbersConfig

    def describe():
        ubx.intent("look up aws_sms_voice_phone_numbers")

        ubx.data(SmsVoicePhoneNumbers, "example", SmsVoicePhoneNumbersConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            owner="example",
            phone_number_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string">
  The real owner of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="phone_number_ids" type="list(string)">
  The real, unique identifiers of the phone numbers this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="phone_numbers" type="list(object)">
  The real phone numbers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
