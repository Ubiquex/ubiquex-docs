---
title: "aws_sms_voice_notify_templates"
description: "`aws_sms_voice_notify_templates` looks up a real message template for AWS End User Messaging Social notifications."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_notify_templates"})

    		ubx.Data(sms_voice.SmsVoiceNotifyTemplates, "example", sms_voice.SmsVoiceNotifyTemplatesConfig{
    			Filters:     []map[string]any{{"name": "example-name"}},
    			MaxResults:  1,
    			TemplateIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoiceNotifyTemplates } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_notify_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_notify_templates" });

      data(SmsVoiceNotifyTemplates, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        templateIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoiceNotifyTemplates, SmsVoiceNotifyTemplatesConfig

    def describe():
        ubx.intent("look up aws_sms_voice_notify_templates")

        ubx.data(SmsVoiceNotifyTemplates, "example", SmsVoiceNotifyTemplatesConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            template_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="template_ids" type="list(string)">
  The real, unique identifiers of the templates this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notify_templates" type="list(object)">
  The real notification templates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
