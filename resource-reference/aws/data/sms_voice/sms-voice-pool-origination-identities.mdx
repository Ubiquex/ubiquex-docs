---
title: "aws_sms_voice_pool_origination_identities"
description: "`aws_sms_voice_pool_origination_identities` reads the real phone numbers or sender IDs within an End User Messaging SMS/Voice pool."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sms_voice "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/sms_voice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_sms_voice_pool_origination_identities"})

    		ubx.Data(sms_voice.SmsVoicePoolOriginationIdentities, "example", sms_voice.SmsVoicePoolOriginationIdentitiesConfig{
    			PoolId:     "example",
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SmsVoicePoolOriginationIdentities } from "@ubx/sdk-aws/aws/data/sms_voice/sms_voice_pool_origination_identities";

    export default stack("example", () => {
      intent({ summary: "look up aws_sms_voice_pool_origination_identities" });

      data(SmsVoicePoolOriginationIdentities, "example", {
        poolId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.sms_voice import SmsVoicePoolOriginationIdentities, SmsVoicePoolOriginationIdentitiesConfig

    def describe():
        ubx.intent("look up aws_sms_voice_pool_origination_identities")

        ubx.data(SmsVoicePoolOriginationIdentities, "example", SmsVoicePoolOriginationIdentitiesConfig(
            pool_id="example",
            filters=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pool_id" type="string" required>
  The real, unique identifier of the pool this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="origination_identities" type="list(object)">
  The real origination identities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="pool_arn" type="string">
  The real Amazon Resource Name (ARN) of the pool this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
