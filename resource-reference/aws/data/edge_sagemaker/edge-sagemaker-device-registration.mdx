---
title: "aws_edge_sagemaker_device_registration"
description: "`aws_edge_sagemaker_device_registration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_edge_sagemaker_device_registration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	edge_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/edge_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_edge_sagemaker_device_registration"})

    		ubx.Data(edge_sagemaker.EdgeSagemakerDeviceRegistration, "example", edge_sagemaker.EdgeSagemakerDeviceRegistrationConfig{
    			DeviceFleetName: "example-device-fleet-name",
    			DeviceName:      "example-device-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EdgeSagemakerDeviceRegistration } from "@ubx/sdk-aws/aws/data/edge_sagemaker/edge_sagemaker_device_registration";

    export default stack("example", () => {
      intent({ summary: "look up aws_edge_sagemaker_device_registration" });

      data(EdgeSagemakerDeviceRegistration, "example", {
        deviceFleetName: "example-device-fleet-name",
        deviceName: "example-device-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.edge_sagemaker import EdgeSagemakerDeviceRegistration, EdgeSagemakerDeviceRegistrationConfig

    def describe():
        ubx.intent("look up aws_edge_sagemaker_device_registration")

        ubx.data(EdgeSagemakerDeviceRegistration, "example", EdgeSagemakerDeviceRegistrationConfig(
            device_fleet_name="example-device-fleet-name",
            device_name="example-device-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_fleet_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="device_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cache_ttl" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="device_registration" type="string">
  Returned by the lookup.
</ResponseField>
