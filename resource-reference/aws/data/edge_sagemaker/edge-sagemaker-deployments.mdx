---
title: "aws_edge_sagemaker_deployments"
description: "`aws_edge_sagemaker_deployments` reads the real, deployed model results reported by a SageMaker Edge Manager device."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	edge_sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/edge_sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_edge_sagemaker_deployments"})

    		ubx.Data(edge_sagemaker.EdgeSagemakerDeployments, "example", edge_sagemaker.EdgeSagemakerDeploymentsConfig{
    			DeviceFleetName: "example-device-fleet-name",
    			DeviceName:      "example-device-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EdgeSagemakerDeployments } from "./local-sdk/aws/sdk/typescript/aws/data/edge_sagemaker/edge_sagemaker_deployments.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_edge_sagemaker_deployments" });

      data(EdgeSagemakerDeployments, "example", {
        deviceFleetName: "example-device-fleet-name",
        deviceName: "example-device-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.edge_sagemaker import EdgeSagemakerDeployments, EdgeSagemakerDeploymentsConfig

    def describe():
        ubx.intent("look up aws_edge_sagemaker_deployments")

        ubx.data(EdgeSagemakerDeployments, "example", EdgeSagemakerDeploymentsConfig(
            device_fleet_name="example-device-fleet-name",
            device_name="example-device-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_fleet_name" type="string" required>
  The real name of the SageMaker device fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="device_name" type="string" required>
  The real name of the device this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="deployments" type="list(object)">
  The real deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
