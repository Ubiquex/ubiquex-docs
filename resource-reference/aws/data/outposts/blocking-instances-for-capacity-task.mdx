---
title: "aws_outposts_blocking_instances_for_capacity_task"
description: "`aws_outposts_blocking_instances_for_capacity_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_outposts_blocking_instances_for_capacity_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_blocking_instances_for_capacity_task"})

    		ubx.Data(outposts.BlockingInstancesForCapacityTask, "example", outposts.BlockingInstancesForCapacityTaskConfig{
    			CapacityTaskId:    "example",
    			OutpostIdentifier: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BlockingInstancesForCapacityTask } from "@ubx/sdk-aws/aws/data/outposts/blocking_instances_for_capacity_task";

    export default stack("example", () => {
      intent({
        summary: "look up aws_outposts_blocking_instances_for_capacity_task",
      });

      data(BlockingInstancesForCapacityTask, "example", {
        capacityTaskId: "example",
        outpostIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import BlockingInstancesForCapacityTask, BlockingInstancesForCapacityTaskConfig

    def describe():
        ubx.intent("look up aws_outposts_blocking_instances_for_capacity_task")

        ubx.data(BlockingInstancesForCapacityTask, "example", BlockingInstancesForCapacityTaskConfig(
            capacity_task_id="example",
            outpost_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_task_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="blocking_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
