---
title: "aws_outposts_catalog_items"
description: "`aws_outposts_catalog_items` reads the real, orderable Outposts catalog items."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_catalog_items"})

    		ubx.Data(outposts.CatalogItems, "example", outposts.CatalogItemsConfig{
    			Ec2FamilyFilter:        []string{"example"},
    			ItemClassFilter:        []string{"example"},
    			MaxResults:             1,
    			SupportedStorageFilter: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogItems } from "@ubx/sdk-aws/aws/data/outposts/catalog_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_catalog_items" });

      data(CatalogItems, "example", {
        ec2FamilyFilter: ["example"],
        itemClassFilter: ["example"],
        maxResults: 1,
        supportedStorageFilter: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import CatalogItems, CatalogItemsConfig

    def describe():
        ubx.intent("look up aws_outposts_catalog_items")

        ubx.data(CatalogItems, "example", CatalogItemsConfig(
            ec2_family_filter=["example"],
            item_class_filter=["example"],
            max_results=1,
            supported_storage_filter=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ec2_family_filter" type="list(string)">
  Restricts results to this EC2 instance family. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="item_class_filter" type="list(string)">
  Restricts results to this item class. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="supported_storage_filter" type="list(string)">
  Restricts results to this supported storage type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="catalog_items" type="list(object)">
  The real catalog items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
