---
title: "aws_outposts_asset_instances"
description: "`aws_outposts_asset_instances` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_outposts_asset_instances` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_asset_instances"})

    		ubx.Data(outposts.AssetInstances, "example", outposts.AssetInstancesConfig{
    			OutpostIdentifier:  "example",
    			AccountIdFilter:    []string{"example"},
    			AssetIdFilter:      []string{"example"},
    			AwsServiceFilter:   []string{"example"},
    			InstanceTypeFilter: []string{"example"},
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssetInstances } from "@ubx/sdk-aws/aws/data/outposts/asset_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_asset_instances" });

      data(AssetInstances, "example", {
        outpostIdentifier: "example",
        accountIdFilter: ["example"],
        assetIdFilter: ["example"],
        awsServiceFilter: ["example"],
        instanceTypeFilter: ["example"],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import AssetInstances, AssetInstancesConfig

    def describe():
        ubx.intent("look up aws_outposts_asset_instances")

        ubx.data(AssetInstances, "example", AssetInstancesConfig(
            outpost_identifier="example",
            account_id_filter=["example"],
            asset_id_filter=["example"],
            aws_service_filter=["example"],
            instance_type_filter=["example"],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="asset_id_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="aws_service_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="instance_type_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
