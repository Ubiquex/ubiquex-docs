---
title: "aws_outposts_outpost_supported_instance_types"
description: "`aws_outposts_outpost_supported_instance_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_outposts_outpost_supported_instance_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_outpost_supported_instance_types"})

    		ubx.Data(outposts.OutpostSupportedInstanceTypes, "example", outposts.OutpostSupportedInstanceTypesConfig{
    			OutpostIdentifier: "example",
    			AssetId:           "example",
    			MaxResults:        1,
    			OrderId:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutpostSupportedInstanceTypes } from "@ubx/sdk-aws/aws/data/outposts/outpost_supported_instance_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_outpost_supported_instance_types" });

      data(OutpostSupportedInstanceTypes, "example", {
        outpostIdentifier: "example",
        assetId: "example",
        maxResults: 1,
        orderId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import OutpostSupportedInstanceTypes, OutpostSupportedInstanceTypesConfig

    def describe():
        ubx.intent("look up aws_outposts_outpost_supported_instance_types")

        ubx.data(OutpostSupportedInstanceTypes, "example", OutpostSupportedInstanceTypesConfig(
            outpost_identifier="example",
            asset_id="example",
            max_results=1,
            order_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="order_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_types" type="list(object)">
  &lt;p&gt;Information about the instance types.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
