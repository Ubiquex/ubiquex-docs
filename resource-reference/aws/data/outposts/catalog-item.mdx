---
title: "aws_outposts_catalog_item"
description: "`aws_outposts_catalog_item` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_outposts_catalog_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_catalog_item"})

    		ubx.Data(outposts.CatalogItem, "example", outposts.CatalogItemConfig{
    			CatalogItemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CatalogItem } from "@ubx/sdk-aws/aws/data/outposts/catalog_item";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_catalog_item" });

      data(CatalogItem, "example", {
        catalogItemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import CatalogItem, CatalogItemConfig

    def describe():
        ubx.intent("look up aws_outposts_catalog_item")

        ubx.data(CatalogItem, "example", CatalogItemConfig(
            catalog_item_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="catalog_item_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="catalog_item" type="object">
  &lt;p&gt; Information about a catalog item. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="catalog_item_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="ec2_capacities" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="item_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="power_kva" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="supported_storage" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="supported_uplink_gbps" type="list(number)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="weight_lbs" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
