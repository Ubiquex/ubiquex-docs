---
title: "aws_outposts_capacity_tasks"
description: "`aws_outposts_capacity_tasks` reads the real capacity tasks run against an Outpost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_capacity_tasks"})

    		ubx.Data(outposts.CapacityTasks, "example", outposts.CapacityTasksConfig{
    			CapacityTaskStatusFilter: []string{"example"},
    			MaxResults:               1,
    			OutpostIdentifierFilter:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CapacityTasks } from "@ubx/sdk-aws/aws/data/outposts/capacity_tasks";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_capacity_tasks" });

      data(CapacityTasks, "example", {
        capacityTaskStatusFilter: ["example"],
        maxResults: 1,
        outpostIdentifierFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import CapacityTasks, CapacityTasksConfig

    def describe():
        ubx.intent("look up aws_outposts_capacity_tasks")

        ubx.data(CapacityTasks, "example", CapacityTasksConfig(
            capacity_task_status_filter=["example"],
            max_results=1,
            outpost_identifier_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_task_status_filter" type="list(string)">
  Restricts results to this capacity task status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier_filter" type="string">
  Restricts results to this AWS Outpost identifier. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_tasks" type="list(object)">
  The real EC2 capacity tasks included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
