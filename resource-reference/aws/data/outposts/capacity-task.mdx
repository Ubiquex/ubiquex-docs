---
title: "aws_outposts_capacity_task"
description: "`aws_outposts_capacity_task` looks up one real Outposts capacity task, rebalancing an Outpost's own instance family capacity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_capacity_task"})

    		ubx.Data(outposts.CapacityTask, "example", outposts.CapacityTaskConfig{
    			CapacityTaskId:    "example",
    			OutpostIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CapacityTask } from "./local-sdk/aws/sdk/typescript/aws/data/outposts/capacity_task.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_capacity_task" });

      data(CapacityTask, "example", {
        capacityTaskId: "example",
        outpostIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import CapacityTask, CapacityTaskConfig

    def describe():
        ubx.intent("look up aws_outposts_capacity_task")

        ubx.data(CapacityTask, "example", CapacityTaskConfig(
            capacity_task_id="example",
            outpost_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="capacity_task_id" type="string" required>
  The real, unique identifier of the capacity task this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  The real, unique identifier of the AWS Outpost this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="asset_id" type="string">
  The real, unique identifier of the asset this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="capacity_task_status" type="string">
  The real, current status of the capacity task this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="completion_date" type="string">
  The real timestamp this object completed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dry_run" type="bool">
  Whether this request is only validated, without actually being performed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failed" type="object">
  &lt;p&gt;The capacity tasks that failed.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="reason" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instances_to_exclude" type="object">
  &lt;p&gt;User-specified instances that must not be stopped. These instances will not appear in the list of instances that Amazon Web Services recommends to stop in order to free up capacity.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="account_ids" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instances" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="services" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="order_id" type="string">
  The real, unique identifier of the order this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="outpost_id" type="string">
  The real, unique identifier of the AWS Outpost this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="requested_instance_pools" type="list(object)">
  The real requested EC2 Fleet instance pools included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="task_action_on_blocking_instances" type="string">
  The real action taken on instances blocking this task. **(AI-inferred)** Returned by the lookup.
</ResponseField>
