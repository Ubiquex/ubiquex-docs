---
title: "aws_outposts_orderable_instance_types"
description: "`aws_outposts_orderable_instance_types` reads the real EC2 instance type combinations orderable for an Outposts order."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_orderable_instance_types"})

    		ubx.Data(outposts.OrderableInstanceTypes, "example", outposts.OrderableInstanceTypesConfig{
    			MaxResults:              1,
    			OutpostGenerationFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrderableInstanceTypes } from "@ubx/sdk-aws/aws/data/outposts/orderable_instance_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_orderable_instance_types" });

      data(OrderableInstanceTypes, "example", {
        maxResults: 1,
        outpostGenerationFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import OrderableInstanceTypes, OrderableInstanceTypesConfig

    def describe():
        ubx.intent("look up aws_outposts_orderable_instance_types")

        ubx.data(OrderableInstanceTypes, "example", OrderableInstanceTypesConfig(
            max_results=1,
            outpost_generation_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_generation_filter" type="string">
  Restricts results to this AWS Outpost generation. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_types" type="list(object)">
  The real instance types included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
