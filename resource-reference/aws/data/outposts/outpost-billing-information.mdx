---
title: "aws_outposts_outpost_billing_information"
description: "`aws_outposts_outpost_billing_information` reads real billing information for an Outpost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_outpost_billing_information"})

    		ubx.Data(outposts.OutpostBillingInformation, "example", outposts.OutpostBillingInformationConfig{
    			OutpostIdentifier: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { OutpostBillingInformation } from "./local-sdk/aws/sdk/typescript/aws/data/outposts/outpost_billing_information.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_outpost_billing_information" });

      data(OutpostBillingInformation, "example", {
        outpostIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import OutpostBillingInformation, OutpostBillingInformationConfig

    def describe():
        ubx.intent("look up aws_outposts_outpost_billing_information")

        ubx.data(OutpostBillingInformation, "example", OutpostBillingInformationConfig(
            outpost_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  The real, unique identifier of the AWS Outpost this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contract_end_date" type="string">
  The real date of the contract end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="payment_option" type="string">
  The real payment option this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="payment_term" type="string">
  The real payment term for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscriptions" type="list(object)">
  The real subscriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
