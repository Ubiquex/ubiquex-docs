---
title: "aws_outposts_outpost_instance_types"
description: "`aws_outposts_outpost_instance_types` reads the real EC2 instance types available on a specific Outpost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_outpost_instance_types"})

    		ubx.Data(outposts.OutpostInstanceTypes, "example", outposts.OutpostInstanceTypesConfig{
    			OutpostId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutpostInstanceTypes } from "@ubx/sdk-aws/aws/data/outposts/outpost_instance_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_outpost_instance_types" });

      data(OutpostInstanceTypes, "example", {
        outpostId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import OutpostInstanceTypes, OutpostInstanceTypesConfig

    def describe():
        ubx.intent("look up aws_outposts_outpost_instance_types")

        ubx.data(OutpostInstanceTypes, "example", OutpostInstanceTypesConfig(
            outpost_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_id" type="string" required>
  The real, unique identifier of the AWS Outpost this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_types" type="list(object)">
  &lt;p&gt;Information about the instance types.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) of the Outpost.&lt;/p&gt; Returned by the lookup.
</ResponseField>
