---
title: "aws_outposts_assets"
description: "`aws_outposts_assets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_outposts_assets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_outposts_assets"})

    		ubx.Data(outposts.Assets, "example", outposts.AssetsConfig{
    			OutpostIdentifier: "example",
    			AssetTypeFilter:   []string{"example"},
    			HostIdFilter:      []string{"example"},
    			MaxResults:        1,
    			StatusFilter:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Assets } from "@ubx/sdk-aws/aws/data/outposts/assets";

    export default stack("example", () => {
      intent({ summary: "look up aws_outposts_assets" });

      data(Assets, "example", {
        outpostIdentifier: "example",
        assetTypeFilter: ["example"],
        hostIdFilter: ["example"],
        maxResults: 1,
        statusFilter: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.outposts import Assets, AssetsConfig

    def describe():
        ubx.intent("look up aws_outposts_assets")

        ubx.data(Assets, "example", AssetsConfig(
            outpost_identifier="example",
            asset_type_filter=["example"],
            host_id_filter=["example"],
            max_results=1,
            status_filter=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="asset_type_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="host_id_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  &lt;p&gt;The maximum page size.&lt;/p&gt; Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assets" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  &lt;p&gt;The pagination token.&lt;/p&gt; Returned by the lookup.
</ResponseField>
