---
title: "aws_ssm_quicksetup_configurations"
description: "`aws_ssm_quicksetup_configurations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_quicksetup_configurations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_quicksetup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_quicksetup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_quicksetup_configurations"})

    		ubx.Data(ssm_quicksetup.SsmQuicksetupConfigurations, "example", ssm_quicksetup.SsmQuicksetupConfigurationsConfig{
    			ConfigurationDefinitionId: "example",
    			Filters:                   []map[string]any{{"key": "example"}},
    			ManagerArn:                "arn:aws:iam::123456789012:policy/example",
    			MaxItems:                  1,
    			StartingToken:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmQuicksetupConfigurations } from "@ubx/sdk-aws/aws/data/ssm_quicksetup/ssm_quicksetup_configurations";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_quicksetup_configurations" });

      data(SsmQuicksetupConfigurations, "example", {
        configurationDefinitionId: "example",
        filters: [{ key: "example" }],
        managerArn: "arn:aws:iam::123456789012:policy/example",
        maxItems: 1,
        startingToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_quicksetup import SsmQuicksetupConfigurations, SsmQuicksetupConfigurationsConfig

    def describe():
        ubx.intent("look up aws_ssm_quicksetup_configurations")

        ubx.data(SsmQuicksetupConfigurations, "example", SsmQuicksetupConfigurationsConfig(
            configuration_definition_id="example",
            filters=[{"key": "example"}],
            manager_arn="arn:aws:iam::123456789012:policy/example",
            max_items=1,
            starting_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_definition_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="manager_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="starting_token" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configurations_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
