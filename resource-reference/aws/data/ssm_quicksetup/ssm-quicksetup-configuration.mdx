---
title: "aws_ssm_quicksetup_configuration"
description: "`aws_ssm_quicksetup_configuration` reads the real, deployed configuration of a Systems Manager Quick Setup."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_quicksetup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_quicksetup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_quicksetup_configuration"})

    		ubx.Data(ssm_quicksetup.SsmQuicksetupConfiguration, "example", ssm_quicksetup.SsmQuicksetupConfigurationConfig{
    			ConfigurationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmQuicksetupConfiguration } from "@ubx/sdk-aws/aws/data/ssm_quicksetup/ssm_quicksetup_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_quicksetup_configuration" });

      data(SsmQuicksetupConfiguration, "example", {
        configurationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_quicksetup import SsmQuicksetupConfiguration, SsmQuicksetupConfigurationConfig

    def describe():
        ubx.intent("look up aws_ssm_quicksetup_configuration")

        ubx.data(SsmQuicksetupConfiguration, "example", SsmQuicksetupConfigurationConfig(
            configuration_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_id" type="string" required>
  The real, unique identifier of the configuration this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="account" type="string">
  The real AWS account this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="configuration_definition_id" type="string">
  The real, unique identifier of the configuration definition this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="string">
  The real, unique identifier of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_at" type="string">
  The real timestamp this object was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="manager_arn" type="string">
  The real Amazon Resource Name (ARN) of the manager this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  The real, structured parameters of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="region" type="string">
  The real AWS region this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status_summaries" type="list(object)">
  The real, summarized statuss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type_version" type="string">
  The real version of the type. **(AI-inferred)** Returned by the lookup.
</ResponseField>
