---
title: "aws_ssm_quicksetup_service_settings"
description: "`aws_ssm_quicksetup_service_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_quicksetup_service_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_quicksetup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_quicksetup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_quicksetup_service_settings"})

    		ubx.Data(ssm_quicksetup.SsmQuicksetupServiceSettings, "example", ssm_quicksetup.SsmQuicksetupServiceSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmQuicksetupServiceSettings } from "@ubx/sdk-aws/aws/data/ssm_quicksetup/ssm_quicksetup_service_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_quicksetup_service_settings" });

      data(SsmQuicksetupServiceSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_quicksetup import SsmQuicksetupServiceSettings, SsmQuicksetupServiceSettingsConfig

    def describe():
        ubx.intent("look up aws_ssm_quicksetup_service_settings")

        ubx.data(SsmQuicksetupServiceSettings, "example", SsmQuicksetupServiceSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="service_settings" type="object">
  &lt;p&gt;Settings configured for Quick Setup.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="explorer_enabling_role_arn" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
