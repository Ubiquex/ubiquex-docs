---
title: "aws_ssm_quicksetup_quick_setup_types"
description: "`aws_ssm_quicksetup_quick_setup_types` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_quicksetup_quick_setup_types` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_quicksetup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_quicksetup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_quicksetup_quick_setup_types"})

    		ubx.Data(ssm_quicksetup.SsmQuicksetupQuickSetupTypes, "example", ssm_quicksetup.SsmQuicksetupQuickSetupTypesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmQuicksetupQuickSetupTypes } from "@ubx/sdk-aws/aws/data/ssm_quicksetup/ssm_quicksetup_quick_setup_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_quicksetup_quick_setup_types" });

      data(SsmQuicksetupQuickSetupTypes, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_quicksetup import SsmQuicksetupQuickSetupTypes, SsmQuicksetupQuickSetupTypesConfig

    def describe():
        ubx.intent("look up aws_ssm_quicksetup_quick_setup_types")

        ubx.data(SsmQuicksetupQuickSetupTypes, "example", SsmQuicksetupQuickSetupTypesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="quick_setup_type_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
