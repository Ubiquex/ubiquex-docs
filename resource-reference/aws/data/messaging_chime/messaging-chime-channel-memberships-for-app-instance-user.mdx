---
title: "aws_messaging_chime_channel_memberships_for_app_instance_user"
description: "`aws_messaging_chime_channel_memberships_for_app_instance_user` reads the real Chime SDK Messaging channel memberships of a given app instance user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channel_memberships_for_app_instance_user"})

    		ubx.Data(messaging_chime.MessagingChimeChannelMembershipsForAppInstanceUser, "example", messaging_chime.MessagingChimeChannelMembershipsForAppInstanceUserConfig{
    			ChimeBearer:        "example",
    			AppInstanceUserArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannelMembershipsForAppInstanceUser } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channel_memberships_for_app_instance_user";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_messaging_chime_channel_memberships_for_app_instance_user",
      });

      data(MessagingChimeChannelMembershipsForAppInstanceUser, "example", {
        chimeBearer: "example",
        appInstanceUserArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelMembershipsForAppInstanceUser, MessagingChimeChannelMembershipsForAppInstanceUserConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channel_memberships_for_app_instance_user")

        ubx.data(MessagingChimeChannelMembershipsForAppInstanceUser, "example", MessagingChimeChannelMembershipsForAppInstanceUserConfig(
            chime_bearer="example",
            app_instance_user_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_user_arn" type="string">
  The real Amazon Resource Name (ARN) of the Amazon Chime app instance user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  The real Amazon Chime App Instance user ARN this request is authorized as. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel_memberships" type="list(object)">
  The real Amazon Chime channel memberships included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
