---
title: "aws_messaging_chime_channels"
description: "`aws_messaging_chime_channels` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_messaging_chime_channels` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channels"})

    		ubx.Data(messaging_chime.MessagingChimeChannels, "example", messaging_chime.MessagingChimeChannelsConfig{
    			AppInstanceArn: "arn:aws:iam::123456789012:policy/example",
    			ChimeBearer:    "example",
    			MaxResults:     1,
    			Privacy:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannels } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channels";

    export default stack("example", () => {
      intent({ summary: "look up aws_messaging_chime_channels" });

      data(MessagingChimeChannels, "example", {
        appInstanceArn: "arn:aws:iam::123456789012:policy/example",
        chimeBearer: "example",
        maxResults: 1,
        privacy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannels, MessagingChimeChannelsConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channels")

        ubx.data(MessagingChimeChannels, "example", MessagingChimeChannelsConfig(
            app_instance_arn="arn:aws:iam::123456789012:policy/example",
            chime_bearer="example",
            max_results=1,
            privacy="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="privacy" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channels" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
