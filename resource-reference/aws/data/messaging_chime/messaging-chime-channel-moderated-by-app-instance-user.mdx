---
title: "aws_messaging_chime_channel_moderated_by_app_instance_user"
description: "`aws_messaging_chime_channel_moderated_by_app_instance_user` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_messaging_chime_channel_moderated_by_app_instance_user` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channel_moderated_by_app_instance_user"})

    		ubx.Data(messaging_chime.MessagingChimeChannelModeratedByAppInstanceUser, "example", messaging_chime.MessagingChimeChannelModeratedByAppInstanceUserConfig{
    			AppInstanceUserArn: "arn:aws:iam::123456789012:policy/example",
    			ChannelArn:         "arn:aws:iam::123456789012:policy/example",
    			ChimeBearer:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannelModeratedByAppInstanceUser } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channel_moderated_by_app_instance_user";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_messaging_chime_channel_moderated_by_app_instance_user",
      });

      data(MessagingChimeChannelModeratedByAppInstanceUser, "example", {
        appInstanceUserArn: "arn:aws:iam::123456789012:policy/example",
        channelArn: "arn:aws:iam::123456789012:policy/example",
        chimeBearer: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelModeratedByAppInstanceUser, MessagingChimeChannelModeratedByAppInstanceUserConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channel_moderated_by_app_instance_user")

        ubx.data(MessagingChimeChannelModeratedByAppInstanceUser, "example", MessagingChimeChannelModeratedByAppInstanceUserConfig(
            app_instance_user_arn="arn:aws:iam::123456789012:policy/example",
            channel_arn="arn:aws:iam::123456789012:policy/example",
            chime_bearer="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_user_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="channel_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel" type="object">
  &lt;p&gt;Summary of the details of a moderated channel.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="channel_summary" type="object">
      &lt;p&gt;Summary of the details of a &lt;code&gt;Channel&lt;/code&gt;.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="channel_arn" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_message_timestamp" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="metadata" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="mode" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="privacy" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
