---
title: "aws_messaging_chime_channel_messages"
description: "`aws_messaging_chime_channel_messages` reads the real messages posted to a Chime SDK Messaging channel."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channel_messages"})

    		ubx.Data(messaging_chime.MessagingChimeChannelMessages, "example", messaging_chime.MessagingChimeChannelMessagesConfig{
    			ChannelArn:  "arn:aws:iam::123456789012:policy/example",
    			ChimeBearer: "example",
    			MaxResults:  1,
    			NotAfter:    "example",
    			NotBefore:   "example",
    			SortOrder:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MessagingChimeChannelMessages } from "./local-sdk/aws/sdk/typescript/aws/data/messaging_chime/messaging_chime_channel_messages.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_messaging_chime_channel_messages" });

      data(MessagingChimeChannelMessages, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        chimeBearer: "example",
        maxResults: 1,
        notAfter: "example",
        notBefore: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelMessages, MessagingChimeChannelMessagesConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channel_messages")

        ubx.data(MessagingChimeChannelMessages, "example", MessagingChimeChannelMessagesConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            chime_bearer="example",
            max_results=1,
            not_after="example",
            not_before="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  The real Amazon Chime App Instance user ARN this request is authorized as. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="not_after" type="string">
  The real timestamp after which this object is no longer valid. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="not_before" type="string">
  The real timestamp before which this object is not yet valid. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel_messages" type="list(object)">
  The real Amazon Chime channel messages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sub_channel_id" type="string">
  The real, unique identifier of the sub-channel this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
