---
title: "aws_messaging_chime_channels_associated_with_channel_flow"
description: "`aws_messaging_chime_channels_associated_with_channel_flow` reads the real channels a Chime SDK Messaging channel flow is applied to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channels_associated_with_channel_flow"})

    		ubx.Data(messaging_chime.MessagingChimeChannelsAssociatedWithChannelFlow, "example", messaging_chime.MessagingChimeChannelsAssociatedWithChannelFlowConfig{
    			ChannelFlowArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannelsAssociatedWithChannelFlow } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channels_associated_with_channel_flow";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_messaging_chime_channels_associated_with_channel_flow",
      });

      data(MessagingChimeChannelsAssociatedWithChannelFlow, "example", {
        channelFlowArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelsAssociatedWithChannelFlow, MessagingChimeChannelsAssociatedWithChannelFlowConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channels_associated_with_channel_flow")

        ubx.data(MessagingChimeChannelsAssociatedWithChannelFlow, "example", MessagingChimeChannelsAssociatedWithChannelFlowConfig(
            channel_flow_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_flow_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel flow this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channels" type="list(object)">
  The real channels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
