---
title: "aws_messaging_chime_messaging_streaming_configurations"
description: "`aws_messaging_chime_messaging_streaming_configurations` reads the real, real-time streaming configuration (to Kinesis) of a Chime SDK Messaging app..."
---

`aws_messaging_chime_messaging_streaming_configurations` reads the real, real-time streaming configuration (to Kinesis) of a Chime SDK Messaging app instance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_messaging_streaming_configurations"})

    		ubx.Data(messaging_chime.MessagingChimeMessagingStreamingConfigurations, "example", messaging_chime.MessagingChimeMessagingStreamingConfigurationsConfig{
    			AppInstanceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MessagingChimeMessagingStreamingConfigurations } from "./local-sdk/aws/sdk/typescript/aws/data/messaging_chime/messaging_chime_messaging_streaming_configurations.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_messaging_chime_messaging_streaming_configurations",
      });

      data(MessagingChimeMessagingStreamingConfigurations, "example", {
        appInstanceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeMessagingStreamingConfigurations, MessagingChimeMessagingStreamingConfigurationsConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_messaging_streaming_configurations")

        ubx.data(MessagingChimeMessagingStreamingConfigurations, "example", MessagingChimeMessagingStreamingConfigurationsConfig(
            app_instance_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the Amazon Chime app instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="streaming_configurations" type="list(object)">
  The real streaming configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
