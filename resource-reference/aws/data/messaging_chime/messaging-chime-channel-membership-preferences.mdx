---
title: "aws_messaging_chime_channel_membership_preferences"
description: "`aws_messaging_chime_channel_membership_preferences` reads the real notification preferences of a Chime SDK Messaging channel member."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channel_membership_preferences"})

    		ubx.Data(messaging_chime.MessagingChimeChannelMembershipPreferences, "example", messaging_chime.MessagingChimeChannelMembershipPreferencesConfig{
    			ChannelArn:  "arn:aws:iam::123456789012:policy/example",
    			ChimeBearer: "example",
    			MemberArn:   "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannelMembershipPreferences } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channel_membership_preferences";

    export default stack("example", () => {
      intent({
        summary: "look up aws_messaging_chime_channel_membership_preferences",
      });

      data(MessagingChimeChannelMembershipPreferences, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        chimeBearer: "example",
        memberArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelMembershipPreferences, MessagingChimeChannelMembershipPreferencesConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channel_membership_preferences")

        ubx.data(MessagingChimeChannelMembershipPreferences, "example", MessagingChimeChannelMembershipPreferencesConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            chime_bearer="example",
            member_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  The real Amazon Chime App Instance user ARN this request is authorized as. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="member_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the member this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="member" type="object">
  &lt;p&gt;The details of a user or bot.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="preferences" type="object">
  &lt;p&gt;The channel membership preferences for an &lt;code&gt;AppInstanceUser&lt;/code&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="push_notifications" type="object">
      &lt;p&gt;The channel membership preferences for push notification.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="allow_notifications" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="filter_rule" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
