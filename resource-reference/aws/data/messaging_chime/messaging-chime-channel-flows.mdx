---
title: "aws_messaging_chime_channel_flows"
description: "`aws_messaging_chime_channel_flows` looks up a real Chime SDK Messaging channel flow, a Lambda-backed processor applied to messages before delivery."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channel_flows"})

    		ubx.Data(messaging_chime.MessagingChimeChannelFlows, "example", messaging_chime.MessagingChimeChannelFlowsConfig{
    			AppInstanceArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannelFlows } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channel_flows";

    export default stack("example", () => {
      intent({ summary: "look up aws_messaging_chime_channel_flows" });

      data(MessagingChimeChannelFlows, "example", {
        appInstanceArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelFlows, MessagingChimeChannelFlowsConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channel_flows")

        ubx.data(MessagingChimeChannelFlows, "example", MessagingChimeChannelFlowsConfig(
            app_instance_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_instance_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the Amazon Chime app instance this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="channel_flows" type="list(object)">
  The real Amazon Chime channel flows included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
