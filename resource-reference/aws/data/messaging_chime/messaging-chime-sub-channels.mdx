---
title: "aws_messaging_chime_sub_channels"
description: "`aws_messaging_chime_sub_channels` reads the real sub-channels of an elastic Chime SDK Messaging channel, used to shard very large channels."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_sub_channels"})

    		ubx.Data(messaging_chime.MessagingChimeSubChannels, "example", messaging_chime.MessagingChimeSubChannelsConfig{
    			ChannelArn:  "arn:aws:iam::123456789012:policy/example",
    			ChimeBearer: "example",
    			MaxResults:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeSubChannels } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_sub_channels";

    export default stack("example", () => {
      intent({ summary: "look up aws_messaging_chime_sub_channels" });

      data(MessagingChimeSubChannels, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        chimeBearer: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeSubChannels, MessagingChimeSubChannelsConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_sub_channels")

        ubx.data(MessagingChimeSubChannels, "example", MessagingChimeSubChannelsConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            chime_bearer="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  The real Amazon Chime App Instance user ARN this request is authorized as. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sub_channels" type="list(object)">
  The real sub-channels included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
