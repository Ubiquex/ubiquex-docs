---
title: "aws_messaging_chime_channel_message_status"
description: "`aws_messaging_chime_channel_message_status` reads the real, current delivery status of a message posted to a Chime SDK Messaging channel."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	messaging_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/messaging_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_messaging_chime_channel_message_status"})

    		ubx.Data(messaging_chime.MessagingChimeChannelMessageStatus, "example", messaging_chime.MessagingChimeChannelMessageStatusConfig{
    			ChannelArn:   "arn:aws:iam::123456789012:policy/example",
    			ChimeBearer:  "example",
    			MessageId:    "example",
    			SubChannelId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MessagingChimeChannelMessageStatus } from "@ubx/sdk-aws/aws/data/messaging_chime/messaging_chime_channel_message_status";

    export default stack("example", () => {
      intent({ summary: "look up aws_messaging_chime_channel_message_status" });

      data(MessagingChimeChannelMessageStatus, "example", {
        channelArn: "arn:aws:iam::123456789012:policy/example",
        chimeBearer: "example",
        messageId: "example",
        subChannelId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.messaging_chime import MessagingChimeChannelMessageStatus, MessagingChimeChannelMessageStatusConfig

    def describe():
        ubx.intent("look up aws_messaging_chime_channel_message_status")

        ubx.data(MessagingChimeChannelMessageStatus, "example", MessagingChimeChannelMessageStatusConfig(
            channel_arn="arn:aws:iam::123456789012:policy/example",
            chime_bearer="example",
            message_id="example",
            sub_channel_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="channel_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the channel this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="chime_bearer" type="string" required>
  The real Amazon Chime App Instance user ARN this request is authorized as. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="message_id" type="string" required>
  The real, unique identifier of the message this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sub_channel_id" type="string">
  The real, unique identifier of the sub-channel this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="object">
  &lt;p&gt;Stores information about a message status.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="detail" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
