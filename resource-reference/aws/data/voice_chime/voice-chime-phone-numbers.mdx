---
title: "aws_voice_chime_phone_numbers"
description: "`aws_voice_chime_phone_numbers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_phone_numbers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_phone_numbers"})

    		ubx.Data(voice_chime.VoiceChimePhoneNumbers, "example", voice_chime.VoiceChimePhoneNumbersConfig{
    			FilterName:  "example-filter-name",
    			FilterValue: "example",
    			MaxResults:  1,
    			ProductType: "example",
    			Status:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimePhoneNumbers } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_phone_numbers";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_phone_numbers" });

      data(VoiceChimePhoneNumbers, "example", {
        filterName: "example-filter-name",
        filterValue: "example",
        maxResults: 1,
        productType: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimePhoneNumbers, VoiceChimePhoneNumbersConfig

    def describe():
        ubx.intent("look up aws_voice_chime_phone_numbers")

        ubx.data(VoiceChimePhoneNumbers, "example", VoiceChimePhoneNumbersConfig(
            filter_name="example-filter-name",
            filter_value="example",
            max_results=1,
            product_type="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter_value" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="phone_numbers" type="list(object)">
  Returned by the lookup.
</ResponseField>
