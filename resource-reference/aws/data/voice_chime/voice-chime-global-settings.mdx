---
title: "aws_voice_chime_global_settings"
description: "`aws_voice_chime_global_settings` reads the real, account-wide global settings (such as CDR bucket) for Amazon Chime Voice Connector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_global_settings"})

    		ubx.Data(voice_chime.VoiceChimeGlobalSettings, "example", voice_chime.VoiceChimeGlobalSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeGlobalSettings } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_global_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_global_settings" });

      data(VoiceChimeGlobalSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeGlobalSettings, VoiceChimeGlobalSettingsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_global_settings")

        ubx.data(VoiceChimeGlobalSettings, "example", VoiceChimeGlobalSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="voice_connector" type="object">
  &lt;p&gt;The Amazon Chime SDK Voice Connector settings. Includes any Amazon S3 buckets designated for storing call detail records.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cdr_bucket" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
