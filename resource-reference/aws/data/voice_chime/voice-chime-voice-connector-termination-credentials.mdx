---
title: "aws_voice_chime_voice_connector_termination_credentials"
description: "`aws_voice_chime_voice_connector_termination_credentials` reads the real SIP credentials configured for a Chime Voice Connector's own termination."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_termination_credentials"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorTerminationCredentials, "example", voice_chime.VoiceChimeVoiceConnectorTerminationCredentialsConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorTerminationCredentials } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_termination_credentials";

    export default stack("example", () => {
      intent({
        summary: "look up aws_voice_chime_voice_connector_termination_credentials",
      });

      data(VoiceChimeVoiceConnectorTerminationCredentials, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorTerminationCredentials, VoiceChimeVoiceConnectorTerminationCredentialsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_termination_credentials")

        ubx.data(VoiceChimeVoiceConnectorTerminationCredentials, "example", VoiceChimeVoiceConnectorTerminationCredentialsConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  The real, unique identifier of the Amazon Chime SDK Voice Connector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="usernames" type="list(string)">
  The real usernames included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
