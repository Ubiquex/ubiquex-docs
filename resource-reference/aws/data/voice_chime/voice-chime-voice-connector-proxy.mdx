---
title: "aws_voice_chime_voice_connector_proxy"
description: "`aws_voice_chime_voice_connector_proxy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_voice_connector_proxy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_proxy"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorProxy, "example", voice_chime.VoiceChimeVoiceConnectorProxyConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorProxy } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_proxy";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_voice_connector_proxy" });

      data(VoiceChimeVoiceConnectorProxy, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorProxy, VoiceChimeVoiceConnectorProxyConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_proxy")

        ubx.data(VoiceChimeVoiceConnectorProxy, "example", VoiceChimeVoiceConnectorProxyConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="proxy" type="object">
  &lt;p&gt;The proxy configuration for an Amazon Chime SDK Voice Connector.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="default_session_expiry_minutes" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="fall_back_phone_number" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_countries" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
