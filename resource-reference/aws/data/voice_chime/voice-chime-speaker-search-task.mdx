---
title: "aws_voice_chime_speaker_search_task"
description: "`aws_voice_chime_speaker_search_task` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_speaker_search_task` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_speaker_search_task"})

    		ubx.Data(voice_chime.VoiceChimeSpeakerSearchTask, "example", voice_chime.VoiceChimeSpeakerSearchTaskConfig{
    			SpeakerSearchTaskId: "example",
    			VoiceConnectorId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeSpeakerSearchTask } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_speaker_search_task";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_speaker_search_task" });

      data(VoiceChimeSpeakerSearchTask, "example", {
        speakerSearchTaskId: "example",
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeSpeakerSearchTask, VoiceChimeSpeakerSearchTaskConfig

    def describe():
        ubx.intent("look up aws_voice_chime_speaker_search_task")

        ubx.data(VoiceChimeSpeakerSearchTask, "example", VoiceChimeSpeakerSearchTaskConfig(
            speaker_search_task_id="example",
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="speaker_search_task_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="speaker_search_task" type="object">
  &lt;p&gt;A representation of an asynchronous request to perform speaker search analysis on a Voice Connector call.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="call_details" type="object">
      &lt;p&gt;The details of an Amazon Chime SDK Voice Connector call.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="is_caller" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="transaction_id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="voice_connector_id" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="speaker_search_details" type="object">
      &lt;p&gt;The details of a speaker search task.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="results" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="voiceprint_generation_status" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="speaker_search_task_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="speaker_search_task_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="started_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status_message" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="updated_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
