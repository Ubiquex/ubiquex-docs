---
title: "aws_voice_chime_sip_media_application_logging_configuration"
description: "`aws_voice_chime_sip_media_application_logging_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_sip_media_application_logging_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_sip_media_application_logging_configuration"})

    		ubx.Data(voice_chime.VoiceChimeSipMediaApplicationLoggingConfiguration, "example", voice_chime.VoiceChimeSipMediaApplicationLoggingConfigurationConfig{
    			SipMediaApplicationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeSipMediaApplicationLoggingConfiguration } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_sip_media_application_logging_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_voice_chime_sip_media_application_logging_configuration",
      });

      data(VoiceChimeSipMediaApplicationLoggingConfiguration, "example", {
        sipMediaApplicationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeSipMediaApplicationLoggingConfiguration, VoiceChimeSipMediaApplicationLoggingConfigurationConfig

    def describe():
        ubx.intent("look up aws_voice_chime_sip_media_application_logging_configuration")

        ubx.data(VoiceChimeSipMediaApplicationLoggingConfiguration, "example", VoiceChimeSipMediaApplicationLoggingConfigurationConfig(
            sip_media_application_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sip_media_application_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="sip_media_application_logging_configuration" type="object">
  &lt;p&gt;The logging configuration of a SIP media application.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enable_sip_media_application_message_logs" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
