---
title: "aws_voice_chime_voice_connector_groups"
description: "`aws_voice_chime_voice_connector_groups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_voice_connector_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_groups"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorGroups, "example", voice_chime.VoiceChimeVoiceConnectorGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorGroups } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_voice_connector_groups" });

      data(VoiceChimeVoiceConnectorGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorGroups, VoiceChimeVoiceConnectorGroupsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_groups")

        ubx.data(VoiceChimeVoiceConnectorGroups, "example", VoiceChimeVoiceConnectorGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="voice_connector_groups" type="list(object)">
  Returned by the lookup.
</ResponseField>
