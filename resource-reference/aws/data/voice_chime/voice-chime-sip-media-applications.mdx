---
title: "aws_voice_chime_sip_media_applications"
description: "`aws_voice_chime_sip_media_applications` looks up a real Chime SDK SIP media application, routing calls to a Lambda function for programmatic call control."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_sip_media_applications"})

    		ubx.Data(voice_chime.VoiceChimeSipMediaApplications, "example", voice_chime.VoiceChimeSipMediaApplicationsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeSipMediaApplications } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_sip_media_applications";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_sip_media_applications" });

      data(VoiceChimeSipMediaApplications, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeSipMediaApplications, VoiceChimeSipMediaApplicationsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_sip_media_applications")

        ubx.data(VoiceChimeSipMediaApplications, "example", VoiceChimeSipMediaApplicationsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sip_media_applications" type="list(object)">
  The real Amazon Chime SDK SIP media applications included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
