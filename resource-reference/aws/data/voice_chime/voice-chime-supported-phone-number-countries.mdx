---
title: "aws_voice_chime_supported_phone_number_countries"
description: "`aws_voice_chime_supported_phone_number_countries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_supported_phone_number_countries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_supported_phone_number_countries"})

    		ubx.Data(voice_chime.VoiceChimeSupportedPhoneNumberCountries, "example", voice_chime.VoiceChimeSupportedPhoneNumberCountriesConfig{
    			ProductType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeSupportedPhoneNumberCountries } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_supported_phone_number_countries";

    export default stack("example", () => {
      intent({
        summary: "look up aws_voice_chime_supported_phone_number_countries",
      });

      data(VoiceChimeSupportedPhoneNumberCountries, "example", {
        productType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeSupportedPhoneNumberCountries, VoiceChimeSupportedPhoneNumberCountriesConfig

    def describe():
        ubx.intent("look up aws_voice_chime_supported_phone_number_countries")

        ubx.data(VoiceChimeSupportedPhoneNumberCountries, "example", VoiceChimeSupportedPhoneNumberCountriesConfig(
            product_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="product_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="phone_number_countries" type="list(object)">
  Returned by the lookup.
</ResponseField>
