---
title: "aws_voice_chime_phone_number_settings"
description: "`aws_voice_chime_phone_number_settings` reads the real, account-wide default settings applied to new Amazon Chime SDK phone numbers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_phone_number_settings"})

    		ubx.Data(voice_chime.VoiceChimePhoneNumberSettings, "example", voice_chime.VoiceChimePhoneNumberSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VoiceChimePhoneNumberSettings } from "./local-sdk/aws/sdk/typescript/aws/data/voice_chime/voice_chime_phone_number_settings.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_phone_number_settings" });

      data(VoiceChimePhoneNumberSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimePhoneNumberSettings, VoiceChimePhoneNumberSettingsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_phone_number_settings")

        ubx.data(VoiceChimePhoneNumberSettings, "example", VoiceChimePhoneNumberSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="calling_name" type="string">
  The real name of the calling this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="calling_name_updated_timestamp" type="string">
  The real timestamp of the calling name updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>
