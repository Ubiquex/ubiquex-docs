---
title: "aws_voice_chime_proxy_sessions"
description: "`aws_voice_chime_proxy_sessions` looks up a real proxy session, masking two participants' own real phone numbers behind a Chime SDK proxy number."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_proxy_sessions"})

    		ubx.Data(voice_chime.VoiceChimeProxySessions, "example", voice_chime.VoiceChimeProxySessionsConfig{
    			VoiceConnectorId: "example",
    			MaxResults:       1,
    			Status:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VoiceChimeProxySessions } from "./local-sdk/aws/sdk/typescript/aws/data/voice_chime/voice_chime_proxy_sessions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_proxy_sessions" });

      data(VoiceChimeProxySessions, "example", {
        voiceConnectorId: "example",
        maxResults: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeProxySessions, VoiceChimeProxySessionsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_proxy_sessions")

        ubx.data(VoiceChimeProxySessions, "example", VoiceChimeProxySessionsConfig(
            voice_connector_id="example",
            max_results=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="voice_connector_id" type="string" required>
  The real, unique identifier of the Amazon Chime SDK Voice Connector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="proxy_sessions" type="list(object)">
  The real Amazon Chime SDK Voice Connector proxy sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
