---
title: "aws_voice_chime_voice_profile_domains"
description: "`aws_voice_chime_voice_profile_domains` looks up a real Chime SDK Voice Analytics voice profile domain, an isolated store of enrolled voice profiles."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_profile_domains"})

    		ubx.Data(voice_chime.VoiceChimeVoiceProfileDomains, "example", voice_chime.VoiceChimeVoiceProfileDomainsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VoiceChimeVoiceProfileDomains } from "./local-sdk/aws/sdk/typescript/aws/data/voice_chime/voice_chime_voice_profile_domains.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_voice_profile_domains" });

      data(VoiceChimeVoiceProfileDomains, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceProfileDomains, VoiceChimeVoiceProfileDomainsConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_profile_domains")

        ubx.data(VoiceChimeVoiceProfileDomains, "example", VoiceChimeVoiceProfileDomainsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="voice_profile_domains" type="list(object)">
  The real Amazon Chime SDK Voice Profile Domains included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
