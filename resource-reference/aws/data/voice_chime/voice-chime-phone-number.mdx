---
title: "aws_voice_chime_phone_number"
description: "`aws_voice_chime_phone_number` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_phone_number` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_phone_number"})

    		ubx.Data(voice_chime.VoiceChimePhoneNumber, "example", voice_chime.VoiceChimePhoneNumberConfig{
    			PhoneNumberId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimePhoneNumber } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_phone_number";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_phone_number" });

      data(VoiceChimePhoneNumber, "example", {
        phoneNumberId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimePhoneNumber, VoiceChimePhoneNumberConfig

    def describe():
        ubx.intent("look up aws_voice_chime_phone_number")

        ubx.data(VoiceChimePhoneNumber, "example", VoiceChimePhoneNumberConfig(
            phone_number_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="phone_number_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="phone_number" type="object">
  &lt;p&gt;A phone number used to call an Amazon Chime SDK Voice Connector.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="associations" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="calling_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="calling_name_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="capabilities" type="object">
      &lt;p&gt;The phone number capabilities for Amazon Chime SDK phone numbers, such as enabled inbound and outbound calling, and text messaging.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="inbound_call" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="inbound_mms" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="inbound_sms" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="outbound_call" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="outbound_mms" type="bool">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="outbound_sms" type="bool">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="country" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="deletion_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="e164_phone_number" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="order_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="phone_number_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="product_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="updated_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
