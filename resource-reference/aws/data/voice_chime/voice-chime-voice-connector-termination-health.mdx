---
title: "aws_voice_chime_voice_connector_termination_health"
description: "`aws_voice_chime_voice_connector_termination_health` reads the real, current health status of a Chime Voice Connector's own termination configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_termination_health"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorTerminationHealth, "example", voice_chime.VoiceChimeVoiceConnectorTerminationHealthConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorTerminationHealth } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_termination_health";

    export default stack("example", () => {
      intent({
        summary: "look up aws_voice_chime_voice_connector_termination_health",
      });

      data(VoiceChimeVoiceConnectorTerminationHealth, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorTerminationHealth, VoiceChimeVoiceConnectorTerminationHealthConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_termination_health")

        ubx.data(VoiceChimeVoiceConnectorTerminationHealth, "example", VoiceChimeVoiceConnectorTerminationHealthConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  The real, unique identifier of the Amazon Chime SDK Voice Connector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="termination_health" type="object">
  &lt;p&gt;The termination health details, including the source IP address and timestamp of the last successful &lt;code&gt;SIP OPTIONS&lt;/code&gt; message from your SIP infrastructure.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="source" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
