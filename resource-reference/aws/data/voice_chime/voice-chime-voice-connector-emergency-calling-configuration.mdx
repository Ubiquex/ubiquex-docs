---
title: "aws_voice_chime_voice_connector_emergency_calling_configuration"
description: "`aws_voice_chime_voice_connector_emergency_calling_configuration` reads the real emergency calling (E911) configuration of a Chime Voice Connector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_emergency_calling_configuration"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorEmergencyCallingConfiguration, "example", voice_chime.VoiceChimeVoiceConnectorEmergencyCallingConfigurationConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorEmergencyCallingConfiguration } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_emergency_calling_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_voice_chime_voice_connector_emergency_calling_configuration",
      });

      data(VoiceChimeVoiceConnectorEmergencyCallingConfiguration, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorEmergencyCallingConfiguration, VoiceChimeVoiceConnectorEmergencyCallingConfigurationConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_emergency_calling_configuration")

        ubx.data(VoiceChimeVoiceConnectorEmergencyCallingConfiguration, "example", VoiceChimeVoiceConnectorEmergencyCallingConfigurationConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  The real, unique identifier of the Amazon Chime SDK Voice Connector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="emergency_calling_configuration" type="object">
  &lt;p&gt;The emergency calling configuration details associated with an Amazon Chime SDK Voice Connector.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="dnis" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
