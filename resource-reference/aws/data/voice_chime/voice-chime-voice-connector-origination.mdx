---
title: "aws_voice_chime_voice_connector_origination"
description: "`aws_voice_chime_voice_connector_origination` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_voice_connector_origination` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_origination"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorOrigination, "example", voice_chime.VoiceChimeVoiceConnectorOriginationConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorOrigination } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_origination";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_voice_connector_origination" });

      data(VoiceChimeVoiceConnectorOrigination, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorOrigination, VoiceChimeVoiceConnectorOriginationConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_origination")

        ubx.data(VoiceChimeVoiceConnectorOrigination, "example", VoiceChimeVoiceConnectorOriginationConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="origination" type="object">
  &lt;p&gt;Origination settings enable your SIP hosts to receive inbound calls using your Amazon Chime SDK Voice Connector.&lt;/p&gt; &lt;note&gt; &lt;p&gt;The parameters listed below are not required, but you must use at least one.&lt;/p&gt; &lt;/note&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="disabled" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="routes" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
