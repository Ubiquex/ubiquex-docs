---
title: "aws_voice_chime_voice_connector_logging_configuration"
description: "`aws_voice_chime_voice_connector_logging_configuration` reads the real SIP and media logging configuration of a Chime Voice Connector."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_logging_configuration"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorLoggingConfiguration, "example", voice_chime.VoiceChimeVoiceConnectorLoggingConfigurationConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VoiceChimeVoiceConnectorLoggingConfiguration } from "./local-sdk/aws/sdk/typescript/aws/data/voice_chime/voice_chime_voice_connector_logging_configuration.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_voice_chime_voice_connector_logging_configuration",
      });

      data(VoiceChimeVoiceConnectorLoggingConfiguration, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorLoggingConfiguration, VoiceChimeVoiceConnectorLoggingConfigurationConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_logging_configuration")

        ubx.data(VoiceChimeVoiceConnectorLoggingConfiguration, "example", VoiceChimeVoiceConnectorLoggingConfigurationConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  The real, unique identifier of the Amazon Chime SDK Voice Connector this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="logging_configuration" type="object">
  &lt;p&gt;The logging configuration associated with an Amazon Chime SDK Voice Connector. Specifies whether SIP message logs can be sent to Amazon CloudWatch Logs.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enable_media_metric_logs" type="bool">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="enable_siplogs" type="bool">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
