---
title: "aws_voice_chime_voice_connector_external_systems_configuration"
description: "`aws_voice_chime_voice_connector_external_systems_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_voice_chime_voice_connector_external_systems_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_voice_connector_external_systems_configuration"})

    		ubx.Data(voice_chime.VoiceChimeVoiceConnectorExternalSystemsConfiguration, "example", voice_chime.VoiceChimeVoiceConnectorExternalSystemsConfigurationConfig{
    			VoiceConnectorId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeVoiceConnectorExternalSystemsConfiguration } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_voice_connector_external_systems_configuration";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_voice_chime_voice_connector_external_systems_configuration",
      });

      data(VoiceChimeVoiceConnectorExternalSystemsConfiguration, "example", {
        voiceConnectorId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeVoiceConnectorExternalSystemsConfiguration, VoiceChimeVoiceConnectorExternalSystemsConfigurationConfig

    def describe():
        ubx.intent("look up aws_voice_chime_voice_connector_external_systems_configuration")

        ubx.data(VoiceChimeVoiceConnectorExternalSystemsConfiguration, "example", VoiceChimeVoiceConnectorExternalSystemsConfigurationConfig(
            voice_connector_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="voice_connector_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="external_systems_configuration" type="object">
  &lt;p&gt;Contains information about an external systems configuration for a Voice Connector.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="contact_center_system_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_border_controller_types" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
