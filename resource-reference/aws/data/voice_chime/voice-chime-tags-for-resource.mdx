---
title: "aws_voice_chime_tags_for_resource"
description: "`aws_voice_chime_tags_for_resource` reads the real tags applied to an Amazon Chime SDK Voice resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_tags_for_resource"})

    		ubx.Data(voice_chime.VoiceChimeTagsForResource, "example", voice_chime.VoiceChimeTagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VoiceChimeTagsForResource } from "@ubx/sdk-aws/aws/data/voice_chime/voice_chime_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_tags_for_resource" });

      data(VoiceChimeTagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimeTagsForResource, VoiceChimeTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_voice_chime_tags_for_resource")

        ubx.data(VoiceChimeTagsForResource, "example", VoiceChimeTagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="list(object)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
