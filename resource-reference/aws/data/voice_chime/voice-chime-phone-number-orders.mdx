---
title: "aws_voice_chime_phone_number_orders"
description: "`aws_voice_chime_phone_number_orders` reads the real orders placed to provision new Amazon Chime SDK phone numbers."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	voice_chime "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/voice_chime"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_voice_chime_phone_number_orders"})

    		ubx.Data(voice_chime.VoiceChimePhoneNumberOrders, "example", voice_chime.VoiceChimePhoneNumberOrdersConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VoiceChimePhoneNumberOrders } from "./local-sdk/aws/sdk/typescript/aws/data/voice_chime/voice_chime_phone_number_orders.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_voice_chime_phone_number_orders" });

      data(VoiceChimePhoneNumberOrders, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.voice_chime import VoiceChimePhoneNumberOrders, VoiceChimePhoneNumberOrdersConfig

    def describe():
        ubx.intent("look up aws_voice_chime_phone_number_orders")

        ubx.data(VoiceChimePhoneNumberOrders, "example", VoiceChimePhoneNumberOrdersConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="phone_number_orders" type="list(object)">
  The real phone number orders included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
