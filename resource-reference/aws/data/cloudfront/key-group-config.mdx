---
title: "aws_cloudfront_key_group_config"
description: "`aws_cloudfront_key_group_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_key_group_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_key_group_config"})

    		ubx.Data(cloudfront.KeyGroupConfig, "example", cloudfront.KeyGroupConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KeyGroupConfig } from "@ubx/sdk-aws/aws/data/cloudfront/key_group_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_key_group_config" });

      data(KeyGroupConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import KeyGroupConfig, KeyGroupConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_key_group_config")

        ubx.data(KeyGroupConfig, "example", KeyGroupConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="key_group_config" type="object">
  &lt;p&gt;A key group configuration.&lt;/p&gt; &lt;p&gt;A key group contains a list of public keys that you can use with &lt;a href="https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html"&gt;CloudFront signed URLs and signed cookies&lt;/a&gt;.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="comment" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
