---
title: "aws_cloudfront_continuous_deployment_policy_config"
description: "`aws_cloudfront_continuous_deployment_policy_config` reads the real configuration of a CloudFront continuous deployment policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_continuous_deployment_policy_config"})

    		ubx.Data(cloudfront.ContinuousDeploymentPolicyConfig, "example", cloudfront.ContinuousDeploymentPolicyConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContinuousDeploymentPolicyConfig } from "@ubx/sdk-aws/aws/data/cloudfront/continuous_deployment_policy_config";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_continuous_deployment_policy_config",
      });

      data(ContinuousDeploymentPolicyConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import ContinuousDeploymentPolicyConfig, ContinuousDeploymentPolicyConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_continuous_deployment_policy_config")

        ubx.data(ContinuousDeploymentPolicyConfig, "example", ContinuousDeploymentPolicyConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="continuous_deployment_policy_config" type="object">
  &lt;p&gt;Contains the configuration for a continuous deployment policy.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="staging_distribution_dns_names" type="object" required>
      &lt;p&gt;The CloudFront domain name of the staging distribution.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="items" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="quantity" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="traffic_config" type="object">
      &lt;p&gt;The traffic configuration of your continuous deployment.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="single_header_config" type="object">
          &lt;p&gt;This configuration determines which HTTP requests are sent to the staging distribution. If the HTTP request contains a header and value that matches what you specify here, the request is sent to the staging distribution. Otherwise the request is sent to the primary distribution.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="header" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="value" type="string" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="single_weight_config" type="object">
          &lt;p&gt;Contains the percentage of traffic to send to a staging distribution.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="session_stickiness_config" type="object">
              &lt;p&gt;Session stickiness provides the ability to define multiple requests from a single viewer as a single session. This prevents the potentially inconsistent experience of sending some of a given user's requests to your staging distribution, while others are sent to your primary distribution. Define the session duration using TTL values.&lt;/p&gt; Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="idle_ttl" type="number" required>
                  Required lookup argument.
                </ResponseField>
                <ResponseField name="maximum_ttl" type="number" required>
                  Required lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="weight" type="number" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>
