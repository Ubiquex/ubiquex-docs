---
title: "aws_cloudfront_cloud_front_origin_access_identity_config"
description: "`aws_cloudfront_cloud_front_origin_access_identity_config` reads the real configuration of a CloudFront origin access identity."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_cloud_front_origin_access_identity_config"})

    		ubx.Data(cloudfront.CloudFrontOriginAccessIdentityConfig, "example", cloudfront.CloudFrontOriginAccessIdentityConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudFrontOriginAccessIdentityConfig } from "@ubx/sdk-aws/aws/data/cloudfront/cloud_front_origin_access_identity_config";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_cloud_front_origin_access_identity_config",
      });

      data(CloudFrontOriginAccessIdentityConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import CloudFrontOriginAccessIdentityConfig, CloudFrontOriginAccessIdentityConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_cloud_front_origin_access_identity_config")

        ubx.data(CloudFrontOriginAccessIdentityConfig, "example", CloudFrontOriginAccessIdentityConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_front_origin_access_identity_config" type="object">
  &lt;p&gt;Origin access identity configuration. Send a &lt;code&gt;GET&lt;/code&gt; request to the &lt;code&gt;/&lt;i&gt;CloudFront API version&lt;/i&gt;/CloudFront/identity ID/config&lt;/code&gt; resource.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="caller_reference" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="comment" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>
