---
title: "aws_cloudfront_distributions_by_realtime_log_config"
description: "`aws_cloudfront_distributions_by_realtime_log_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_distributions_by_realtime_log_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distributions_by_realtime_log_config"})

    		ubx.Data(cloudfront.DistributionsByRealtimeLogConfig, "example", cloudfront.DistributionsByRealtimeLogConfigConfig{
    			Marker:                "example",
    			MaxItems:              1,
    			RealtimeLogConfigArn:  "arn:aws:iam::123456789012:policy/example",
    			RealtimeLogConfigName: "example-realtime-log-config-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DistributionsByRealtimeLogConfig } from "@ubx/sdk-aws/aws/data/cloudfront/distributions_by_realtime_log_config";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_distributions_by_realtime_log_config",
      });

      data(DistributionsByRealtimeLogConfig, "example", {
        marker: "example",
        maxItems: 1,
        realtimeLogConfigArn: "arn:aws:iam::123456789012:policy/example",
        realtimeLogConfigName: "example-realtime-log-config-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionsByRealtimeLogConfig, DistributionsByRealtimeLogConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distributions_by_realtime_log_config")

        ubx.data(DistributionsByRealtimeLogConfig, "example", DistributionsByRealtimeLogConfigConfig(
            marker="example",
            max_items=1,
            realtime_log_config_arn="arn:aws:iam::123456789012:policy/example",
            realtime_log_config_name="example-realtime-log-config-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="realtime_log_config_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="realtime_log_config_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_list" type="object">
  &lt;p&gt;A distribution list.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
