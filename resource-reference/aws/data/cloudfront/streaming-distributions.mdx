---
title: "aws_cloudfront_streaming_distributions"
description: "`aws_cloudfront_streaming_distributions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_streaming_distributions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_streaming_distributions"})

    		ubx.Data(cloudfront.StreamingDistributions, "example", cloudfront.StreamingDistributionsConfig{
    			Marker:   "example",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamingDistributions } from "@ubx/sdk-aws/aws/data/cloudfront/streaming_distributions";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_streaming_distributions" });

      data(StreamingDistributions, "example", {
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import StreamingDistributions, StreamingDistributionsConfig

    def describe():
        ubx.intent("look up aws_cloudfront_streaming_distributions")

        ubx.data(StreamingDistributions, "example", StreamingDistributionsConfig(
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="streaming_distribution_list" type="object">
  &lt;p&gt;A streaming distribution list.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
