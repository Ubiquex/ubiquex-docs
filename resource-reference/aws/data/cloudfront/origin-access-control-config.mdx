---
title: "aws_cloudfront_origin_access_control_config"
description: "`aws_cloudfront_origin_access_control_config` reads the real configuration of a CloudFront origin access control (OAC)..."
---

`aws_cloudfront_origin_access_control_config` reads the real configuration of a CloudFront origin access control (OAC), the current mechanism for restricting direct access to an S3 or other origin.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_origin_access_control_config"})

    		ubx.Data(cloudfront.OriginAccessControlConfig, "example", cloudfront.OriginAccessControlConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OriginAccessControlConfig } from "@ubx/sdk-aws/aws/data/cloudfront/origin_access_control_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_origin_access_control_config" });

      data(OriginAccessControlConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import OriginAccessControlConfig, OriginAccessControlConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_origin_access_control_config")

        ubx.data(OriginAccessControlConfig, "example", OriginAccessControlConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="origin_access_control_config" type="object">
  &lt;p&gt;A CloudFront origin access control configuration.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="origin_access_control_origin_type" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="signing_behavior" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="signing_protocol" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
