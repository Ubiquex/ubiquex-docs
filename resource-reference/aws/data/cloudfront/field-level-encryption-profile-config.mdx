---
title: "aws_cloudfront_field_level_encryption_profile_config"
description: "`aws_cloudfront_field_level_encryption_profile_config` reads the real configuration of a CloudFront field-level encryption profile."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_field_level_encryption_profile_config"})

    		ubx.Data(cloudfront.FieldLevelEncryptionProfileConfig, "example", cloudfront.FieldLevelEncryptionProfileConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FieldLevelEncryptionProfileConfig } from "@ubx/sdk-aws/aws/data/cloudfront/field_level_encryption_profile_config";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_field_level_encryption_profile_config",
      });

      data(FieldLevelEncryptionProfileConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import FieldLevelEncryptionProfileConfig, FieldLevelEncryptionProfileConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_field_level_encryption_profile_config")

        ubx.data(FieldLevelEncryptionProfileConfig, "example", FieldLevelEncryptionProfileConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="field_level_encryption_profile_config" type="object">
  &lt;p&gt;A complex data type of profiles for the field-level encryption.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="caller_reference" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="comment" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="encryption_entities" type="object" required>
      &lt;p&gt;Complex data type for field-level encryption profiles that includes all of the encryption entities.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="items" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="quantity" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
