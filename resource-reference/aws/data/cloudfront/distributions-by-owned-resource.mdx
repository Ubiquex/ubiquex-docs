---
title: "aws_cloudfront_distributions_by_owned_resource"
description: "`aws_cloudfront_distributions_by_owned_resource` reads the real CloudFront distributions that own a given resource, such as a function or key group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distributions_by_owned_resource"})

    		ubx.Data(cloudfront.DistributionsByOwnedResource, "example", cloudfront.DistributionsByOwnedResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:      "example",
    			MaxItems:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DistributionsByOwnedResource } from "@ubx/sdk-aws/aws/data/cloudfront/distributions_by_owned_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_distributions_by_owned_resource" });

      data(DistributionsByOwnedResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionsByOwnedResource, DistributionsByOwnedResourceConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distributions_by_owned_resource")

        ubx.data(DistributionsByOwnedResource, "example", DistributionsByOwnedResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_list" type="object">
  &lt;p&gt;The list of distribution IDs and the Amazon Web Services accounts that they belong to.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
