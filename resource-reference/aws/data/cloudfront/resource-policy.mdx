---
title: "aws_cloudfront_resource_policy"
description: "`aws_cloudfront_resource_policy` reads the real resource-based policy attached to a CloudFront SaaS Manager resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_resource_policy"})

    		ubx.Data(cloudfront.ResourcePolicy, "example", cloudfront.ResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcePolicy } from "@ubx/sdk-aws/aws/data/cloudfront/resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_resource_policy" });

      data(ResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import ResourcePolicy, ResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_cloudfront_resource_policy")

        ubx.data(ResourcePolicy, "example", ResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy_document" type="string">
  The real, JSON-formatted policy document attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
