---
title: "aws_cloudfront_public_key_config"
description: "`aws_cloudfront_public_key_config` reads the real configuration of a public key registered with CloudFront."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_public_key_config"})

    		ubx.Data(cloudfront.PublicKeyConfig, "example", cloudfront.PublicKeyConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PublicKeyConfig } from "@ubx/sdk-aws/aws/data/cloudfront/public_key_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_public_key_config" });

      data(PublicKeyConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import PublicKeyConfig, PublicKeyConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_public_key_config")

        ubx.data(PublicKeyConfig, "example", PublicKeyConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_key_config" type="object">
  &lt;p&gt;Configuration information about a public key that you can use with &lt;a href="https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html"&gt;signed URLs and signed cookies&lt;/a&gt;, or with &lt;a href="https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/field-level-encryption.html"&gt;field-level encryption&lt;/a&gt;.&lt;/p&gt; &lt;p&gt;CloudFront supports signed URLs and signed cookies with RSA 2048 or ECDSA 256 key signatures. Field-level encryption is only compatible with RSA 2048 key signatures.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="caller_reference" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="comment" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="encoded_key" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
