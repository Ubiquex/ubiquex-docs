---
title: "aws_cloudfront_streaming_distribution_config"
description: "`aws_cloudfront_streaming_distribution_config` reads the real configuration of a CloudFront RTMP streaming distribution..."
---

`aws_cloudfront_streaming_distribution_config` reads the real configuration of a CloudFront RTMP streaming distribution, a retired AWS feature for Flash media streaming.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_streaming_distribution_config"})

    		ubx.Data(cloudfront.StreamingDistributionConfig, "example", cloudfront.StreamingDistributionConfigConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StreamingDistributionConfig } from "@ubx/sdk-aws/aws/data/cloudfront/streaming_distribution_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_streaming_distribution_config" });

      data(StreamingDistributionConfig, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import StreamingDistributionConfig, StreamingDistributionConfigConfig

    def describe():
        ubx.intent("look up aws_cloudfront_streaming_distribution_config")

        ubx.data(StreamingDistributionConfig, "example", StreamingDistributionConfigConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="etag" type="string">
  The real, current entity tag (ETag) of this object, used for optimistic concurrency. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="streaming_distribution_config" type="object">
  &lt;p&gt;The RTMP distribution's configuration information.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="aliases" type="object">
      &lt;p&gt;A complex type that contains information about CNAMEs (alternate domain names), if any, for this distribution.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="items" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="quantity" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="caller_reference" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="comment" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="enabled" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="logging" type="object">
      &lt;p&gt;A complex type that controls whether access logs are written for this streaming distribution.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="enabled" type="bool" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="prefix" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="price_class" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="s3_origin" type="object" required>
      &lt;p&gt;A complex type that contains information about the Amazon S3 bucket from which you want CloudFront to get your media files for distribution.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="origin_access_identity" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="trusted_signers" type="object" required>
      &lt;p&gt;A list of Amazon Web Services accounts whose public keys CloudFront can use to verify the signatures of signed URLs and signed cookies.&lt;/p&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="items" type="list(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="quantity" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
