---
title: "aws_cloudfront_distributions_by_cache_policy_id"
description: "`aws_cloudfront_distributions_by_cache_policy_id` reads the real CloudFront distributions using a given cache policy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distributions_by_cache_policy_id"})

    		ubx.Data(cloudfront.DistributionsByCachePolicyId, "example", cloudfront.DistributionsByCachePolicyIdConfig{
    			CachePolicyId: "example",
    			Marker:        "example",
    			MaxItems:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DistributionsByCachePolicyId } from "./local-sdk/aws/sdk/typescript/aws/data/cloudfront/distributions_by_cache_policy_id.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_distributions_by_cache_policy_id",
      });

      data(DistributionsByCachePolicyId, "example", {
        cachePolicyId: "example",
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionsByCachePolicyId, DistributionsByCachePolicyIdConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distributions_by_cache_policy_id")

        ubx.data(DistributionsByCachePolicyId, "example", DistributionsByCachePolicyIdConfig(
            cache_policy_id="example",
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cache_policy_id" type="string" required>
  The real, unique identifier of the cache policy this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_id_list" type="object">
  &lt;p&gt;A list of distribution IDs.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
