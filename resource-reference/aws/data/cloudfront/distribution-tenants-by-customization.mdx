---
title: "aws_cloudfront_distribution_tenants_by_customization"
description: "`aws_cloudfront_distribution_tenants_by_customization` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_distribution_tenants_by_customization` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distribution_tenants_by_customization"})

    		ubx.Data(cloudfront.DistributionTenantsByCustomization, "example", cloudfront.DistributionTenantsByCustomizationConfig{
    			CertificateArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:         "example",
    			MaxItems:       1,
    			WebAclarn:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DistributionTenantsByCustomization } from "@ubx/sdk-aws/aws/data/cloudfront/distribution_tenants_by_customization";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_distribution_tenants_by_customization",
      });

      data(DistributionTenantsByCustomization, "example", {
        certificateArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        maxItems: 1,
        webAclarn: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionTenantsByCustomization, DistributionTenantsByCustomizationConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distribution_tenants_by_customization")

        ubx.data(DistributionTenantsByCustomization, "example", DistributionTenantsByCustomizationConfig(
            certificate_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            max_items=1,
            web_aclarn="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="certificate_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="web_aclarn" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_tenant_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>
