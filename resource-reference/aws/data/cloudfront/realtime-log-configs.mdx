---
title: "aws_cloudfront_realtime_log_configs"
description: "`aws_cloudfront_realtime_log_configs` looks up a real CloudFront real-time log configuration, streaming request logs to Kinesis Data Streams."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_realtime_log_configs"})

    		ubx.Data(cloudfront.RealtimeLogConfigs, "example", cloudfront.RealtimeLogConfigsConfig{
    			Marker:   "example",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RealtimeLogConfigs } from "@ubx/sdk-aws/aws/data/cloudfront/realtime_log_configs";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_realtime_log_configs" });

      data(RealtimeLogConfigs, "example", {
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import RealtimeLogConfigs, RealtimeLogConfigsConfig

    def describe():
        ubx.intent("look up aws_cloudfront_realtime_log_configs")

        ubx.data(RealtimeLogConfigs, "example", RealtimeLogConfigsConfig(
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="realtime_log_configs" type="object">
  &lt;p&gt;A list of real-time log configurations.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
