---
title: "aws_cloudfront_distributions_by_connection_function"
description: "`aws_cloudfront_distributions_by_connection_function` reads the real CloudFront distributions using a given connection function."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distributions_by_connection_function"})

    		ubx.Data(cloudfront.DistributionsByConnectionFunction, "example", cloudfront.DistributionsByConnectionFunctionConfig{
    			ConnectionFunctionIdentifier: "example",
    			Marker:                       "example",
    			MaxItems:                     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DistributionsByConnectionFunction } from "@ubx/sdk-aws/aws/data/cloudfront/distributions_by_connection_function";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_distributions_by_connection_function",
      });

      data(DistributionsByConnectionFunction, "example", {
        connectionFunctionIdentifier: "example",
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionsByConnectionFunction, DistributionsByConnectionFunctionConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distributions_by_connection_function")

        ubx.data(DistributionsByConnectionFunction, "example", DistributionsByConnectionFunctionConfig(
            connection_function_identifier="example",
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_function_identifier" type="string" required>
  The real, unique identifier of the connection function this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_list" type="object">
  &lt;p&gt;A distribution list.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
