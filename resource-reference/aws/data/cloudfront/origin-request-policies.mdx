---
title: "aws_cloudfront_origin_request_policies"
description: "`aws_cloudfront_origin_request_policies` looks up a real CloudFront origin request policy, controlling which headers, cookies..."
---

`aws_cloudfront_origin_request_policies` looks up a real CloudFront origin request policy, controlling which headers, cookies, and query strings CloudFront forwards to the origin.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_origin_request_policies"})

    		ubx.Data(cloudfront.OriginRequestPolicies, "example", cloudfront.OriginRequestPoliciesConfig{
    			Marker:   "example",
    			MaxItems: 1,
    			Type:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OriginRequestPolicies } from "@ubx/sdk-aws/aws/data/cloudfront/origin_request_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_origin_request_policies" });

      data(OriginRequestPolicies, "example", {
        marker: "example",
        maxItems: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import OriginRequestPolicies, OriginRequestPoliciesConfig

    def describe():
        ubx.intent("look up aws_cloudfront_origin_request_policies")

        ubx.data(OriginRequestPolicies, "example", OriginRequestPoliciesConfig(
            marker="example",
            max_items=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="origin_request_policy_list" type="object">
  &lt;p&gt;A list of origin request policies.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
