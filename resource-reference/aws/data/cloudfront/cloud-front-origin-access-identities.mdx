---
title: "aws_cloudfront_cloud_front_origin_access_identities"
description: "`aws_cloudfront_cloud_front_origin_access_identities` looks up a real CloudFront origin access identity (OAI)..."
---

`aws_cloudfront_cloud_front_origin_access_identities` looks up a real CloudFront origin access identity (OAI), CloudFront's legacy mechanism for restricting direct access to an S3 origin.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_cloud_front_origin_access_identities"})

    		ubx.Data(cloudfront.CloudFrontOriginAccessIdentities, "example", cloudfront.CloudFrontOriginAccessIdentitiesConfig{
    			Marker:   "example",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CloudFrontOriginAccessIdentities } from "@ubx/sdk-aws/aws/data/cloudfront/cloud_front_origin_access_identities";

    export default stack("example", () => {
      intent({
        summary: "look up aws_cloudfront_cloud_front_origin_access_identities",
      });

      data(CloudFrontOriginAccessIdentities, "example", {
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import CloudFrontOriginAccessIdentities, CloudFrontOriginAccessIdentitiesConfig

    def describe():
        ubx.intent("look up aws_cloudfront_cloud_front_origin_access_identities")

        ubx.data(CloudFrontOriginAccessIdentities, "example", CloudFrontOriginAccessIdentitiesConfig(
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cloud_front_origin_access_identity_list" type="object">
  &lt;p&gt;Lists the origin access identities for CloudFront.Send a &lt;code&gt;GET&lt;/code&gt; request to the &lt;code&gt;/&lt;i&gt;CloudFront API version&lt;/i&gt;/origin-access-identity/cloudfront&lt;/code&gt; resource. The response includes a &lt;code&gt;CloudFrontOriginAccessIdentityList&lt;/code&gt; element with zero or more &lt;code&gt;CloudFrontOriginAccessIdentitySummary&lt;/code&gt; child elements. By default, your entire list of origin access identities is returned in one single page. If the list is long, you can paginate it using the &lt;code&gt;MaxItems&lt;/code&gt; and &lt;code&gt;Marker&lt;/code&gt; parameters.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
