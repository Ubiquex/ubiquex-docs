---
title: "aws_cloudfront_field_level_encryption_configs"
description: "`aws_cloudfront_field_level_encryption_configs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_field_level_encryption_configs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_field_level_encryption_configs"})

    		ubx.Data(cloudfront.FieldLevelEncryptionConfigs, "example", cloudfront.FieldLevelEncryptionConfigsConfig{
    			Marker:   "example",
    			MaxItems: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FieldLevelEncryptionConfigs } from "@ubx/sdk-aws/aws/data/cloudfront/field_level_encryption_configs";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_field_level_encryption_configs" });

      data(FieldLevelEncryptionConfigs, "example", {
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import FieldLevelEncryptionConfigs, FieldLevelEncryptionConfigsConfig

    def describe():
        ubx.intent("look up aws_cloudfront_field_level_encryption_configs")

        ubx.data(FieldLevelEncryptionConfigs, "example", FieldLevelEncryptionConfigsConfig(
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="field_level_encryption_list" type="object">
  &lt;p&gt;List of field-level encryption configurations.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
