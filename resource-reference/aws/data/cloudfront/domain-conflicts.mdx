---
title: "aws_cloudfront_domain_conflicts"
description: "`aws_cloudfront_domain_conflicts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cloudfront_domain_conflicts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_domain_conflicts"})

    		ubx.Data(cloudfront.DomainConflicts, "example", cloudfront.DomainConflictsConfig{
    			Domain:                          "example",
    			DomainControlValidationResource: map[string]any{"distribution_id": "example"},
    			Marker:                          "example",
    			MaxItems:                        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DomainConflicts } from "@ubx/sdk-aws/aws/data/cloudfront/domain_conflicts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_domain_conflicts" });

      data(DomainConflicts, "example", {
        domain: "example",
        domainControlValidationResource: { distributionId: "example" },
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DomainConflicts, DomainConflictsConfig

    def describe():
        ubx.intent("look up aws_cloudfront_domain_conflicts")

        ubx.data(DomainConflicts, "example", DomainConflictsConfig(
            domain="example",
            domain_control_validation_resource={"distribution_id": "example"},
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="domain_control_validation_resource" type="object" required>
  &lt;p&gt;The IDs for the distribution resources.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="distribution_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="distribution_tenant_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domain_conflicts" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>
