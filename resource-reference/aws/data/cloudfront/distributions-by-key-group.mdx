---
title: "aws_cloudfront_distributions_by_key_group"
description: "`aws_cloudfront_distributions_by_key_group` reads the real CloudFront distributions using a given key group for signed URLs or cookies."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distributions_by_key_group"})

    		ubx.Data(cloudfront.DistributionsByKeyGroup, "example", cloudfront.DistributionsByKeyGroupConfig{
    			KeyGroupId: "example",
    			Marker:     "example",
    			MaxItems:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DistributionsByKeyGroup } from "@ubx/sdk-aws/aws/data/cloudfront/distributions_by_key_group";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_distributions_by_key_group" });

      data(DistributionsByKeyGroup, "example", {
        keyGroupId: "example",
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionsByKeyGroup, DistributionsByKeyGroupConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distributions_by_key_group")

        ubx.data(DistributionsByKeyGroup, "example", DistributionsByKeyGroupConfig(
            key_group_id="example",
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key_group_id" type="string" required>
  The real, unique identifier of the key group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_id_list" type="object">
  &lt;p&gt;A list of distribution IDs.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="is_truncated" type="bool" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="items" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
