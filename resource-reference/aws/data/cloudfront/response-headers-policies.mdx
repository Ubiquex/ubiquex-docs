---
title: "aws_cloudfront_response_headers_policies"
description: "`aws_cloudfront_response_headers_policies` looks up a real CloudFront response headers policy..."
---

`aws_cloudfront_response_headers_policies` looks up a real CloudFront response headers policy, adding or overriding HTTP headers on the response CloudFront returns.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_response_headers_policies"})

    		ubx.Data(cloudfront.ResponseHeadersPolicies, "example", cloudfront.ResponseHeadersPoliciesConfig{
    			Marker:   "example",
    			MaxItems: 1,
    			Type:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResponseHeadersPolicies } from "@ubx/sdk-aws/aws/data/cloudfront/response_headers_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_response_headers_policies" });

      data(ResponseHeadersPolicies, "example", {
        marker: "example",
        maxItems: 1,
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import ResponseHeadersPolicies, ResponseHeadersPoliciesConfig

    def describe():
        ubx.intent("look up aws_cloudfront_response_headers_policies")

        ubx.data(ResponseHeadersPolicies, "example", ResponseHeadersPoliciesConfig(
            marker="example",
            max_items=1,
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="response_headers_policy_list" type="object">
  &lt;p&gt;A list of response headers policies.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="items" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="max_items" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="next_marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="quantity" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
