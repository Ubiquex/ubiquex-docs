---
title: "aws_cloudfront_distribution_tenants"
description: "`aws_cloudfront_distribution_tenants` looks up a real CloudFront distribution tenant..."
---

`aws_cloudfront_distribution_tenants` looks up a real CloudFront distribution tenant, a customer-specific configuration layered on top of a shared multi-tenant distribution.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cloudfront_distribution_tenants"})

    		ubx.Data(cloudfront.DistributionTenants, "example", cloudfront.DistributionTenantsConfig{
    			AssociationFilter: map[string]any{"connection_group_id": "example"},
    			Marker:            "example",
    			MaxItems:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DistributionTenants } from "./local-sdk/aws/sdk/typescript/aws/data/cloudfront/distribution_tenants.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_cloudfront_distribution_tenants" });

      data(DistributionTenants, "example", {
        associationFilter: { connectionGroupId: "example" },
        marker: "example",
        maxItems: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.cloudfront import DistributionTenants, DistributionTenantsConfig

    def describe():
        ubx.intent("look up aws_cloudfront_distribution_tenants")

        ubx.data(DistributionTenants, "example", DistributionTenantsConfig(
            association_filter={"connection_group_id": "example"},
            marker="example",
            max_items=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="association_filter" type="object">
  &lt;p&gt;Filter by the associated distribution ID or connection group ID.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="connection_group_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="distribution_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_items" type="number">
  The maximum number of items to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="distribution_tenant_list" type="list(object)">
  The real distribution tenants included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
