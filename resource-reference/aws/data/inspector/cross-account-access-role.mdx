---
title: "aws_inspector_cross_account_access_role"
description: "`aws_inspector_cross_account_access_role` reads the real IAM role Inspector Classic uses for cross-account access."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_cross_account_access_role"})

    		ubx.Data(inspector.CrossAccountAccessRole, "example", inspector.CrossAccountAccessRoleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CrossAccountAccessRole } from "./local-sdk/aws/sdk/typescript/aws/data/inspector/cross_account_access_role.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_cross_account_access_role" });

      data(CrossAccountAccessRole, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import CrossAccountAccessRole, CrossAccountAccessRoleConfig

    def describe():
        ubx.intent("look up aws_inspector_cross_account_access_role")

        ubx.data(CrossAccountAccessRole, "example", CrossAccountAccessRoleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="registered_at" type="string">
  The real timestamp this object was registered. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="valid" type="bool">
  Whether this object is currently valid. **(AI-inferred)** Returned by the lookup.
</ResponseField>
