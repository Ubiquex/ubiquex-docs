---
title: "aws_inspector_assessment_run_agents"
description: "`aws_inspector_assessment_run_agents` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector_assessment_run_agents` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_assessment_run_agents"})

    		ubx.Data(inspector.AssessmentRunAgents, "example", inspector.AssessmentRunAgentsConfig{
    			AssessmentRunArn: "arn:aws:iam::123456789012:policy/example",
    			Filter:           map[string]any{"agent_health_codes": []string{"example"}},
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssessmentRunAgents } from "@ubx/sdk-aws/aws/data/inspector/assessment_run_agents";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_assessment_run_agents" });

      data(AssessmentRunAgents, "example", {
        assessmentRunArn: "arn:aws:iam::123456789012:policy/example",
        filter: { agentHealthCodes: ["example"] },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import AssessmentRunAgents, AssessmentRunAgentsConfig

    def describe():
        ubx.intent("look up aws_inspector_assessment_run_agents")

        ubx.data(AssessmentRunAgents, "example", AssessmentRunAgentsConfig(
            assessment_run_arn="arn:aws:iam::123456789012:policy/example",
            filter={"agent_health_codes": ["example"]},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_run_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="object">
  &lt;p&gt;Contains information about an Amazon Inspector agent. This data type is used as a request parameter in the &lt;a&gt;ListAssessmentRunAgents&lt;/a&gt; action.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="agent_health_codes" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="agent_healths" type="list(string)" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_run_agents" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
