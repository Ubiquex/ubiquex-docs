---
title: "aws_inspector_assessment_report"
description: "`aws_inspector_assessment_report` reads a real, generated report from an Inspector Classic assessment run."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_assessment_report"})

    		ubx.Data(inspector.AssessmentReport, "example", inspector.AssessmentReportConfig{
    			AssessmentRunArn: "arn:aws:iam::123456789012:policy/example",
    			ReportFileFormat: "example",
    			ReportType:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { AssessmentReport } from "./local-sdk/aws/sdk/typescript/aws/data/inspector/assessment_report.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_assessment_report" });

      data(AssessmentReport, "example", {
        assessmentRunArn: "arn:aws:iam::123456789012:policy/example",
        reportFileFormat: "example",
        reportType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import AssessmentReport, AssessmentReportConfig

    def describe():
        ubx.intent("look up aws_inspector_assessment_report")

        ubx.data(AssessmentReport, "example", AssessmentReportConfig(
            assessment_run_arn="arn:aws:iam::123456789012:policy/example",
            report_file_format="example",
            report_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_run_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the assessment run this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="report_file_format" type="string" required>
  The real, requested file format of the report. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="report_type" type="string" required>
  The real, discriminating type of the report this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, resolvable URL of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
