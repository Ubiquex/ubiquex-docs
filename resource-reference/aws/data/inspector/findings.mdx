---
title: "aws_inspector_findings"
description: "`aws_inspector_findings` looks up a real Inspector Classic finding."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_findings"})

    		ubx.Data(inspector.Findings, "example", inspector.FindingsConfig{
    			FindingArns: []string{"example"},
    			Locale:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Findings } from "@ubx/sdk-aws/aws/data/inspector/findings";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_findings" });

      data(Findings, "example", {
        findingArns: ["example"],
        locale: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import Findings, FindingsConfig

    def describe():
        ubx.intent("look up aws_inspector_findings")

        ubx.data(Findings, "example", FindingsConfig(
            finding_arns=["example"],
            locale="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="finding_arns" type="list(string)" required>
  The real Amazon Resource Names (ARNs) of the findings this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="failed_items" type="map(object)">
  The real items that failed to process, in this batch request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="findings" type="list(object)">
  The real findings included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
