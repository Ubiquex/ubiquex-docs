---
title: "aws_inspector_rules_packages"
description: "`aws_inspector_rules_packages` reads the real, available Inspector Classic rules packages."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_rules_packages"})

    		ubx.Data(inspector.RulesPackages, "example", inspector.RulesPackagesConfig{
    			RulesPackageArns: []string{"example"},
    			Locale:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RulesPackages } from "./local-sdk/aws/sdk/typescript/aws/data/inspector/rules_packages.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_rules_packages" });

      data(RulesPackages, "example", {
        rulesPackageArns: ["example"],
        locale: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import RulesPackages, RulesPackagesConfig

    def describe():
        ubx.intent("look up aws_inspector_rules_packages")

        ubx.data(RulesPackages, "example", RulesPackagesConfig(
            rules_package_arns=["example"],
            locale="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="locale" type="string">
  The real locale this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rules_package_arns" type="list(string)" required>
  The real Amazon Resource Names (ARNs) of the rules packages this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="failed_items" type="map(object)">
  The real items that failed to process, in this batch request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rules_packages" type="list(object)">
  The real Amazon Inspector Classic rules packages included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
