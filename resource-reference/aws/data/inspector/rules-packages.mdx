---
title: "aws_inspector_rules_packages"
description: "`aws_inspector_rules_packages` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector_rules_packages` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_rules_packages"})

    		ubx.Data(inspector.RulesPackages, "example", inspector.RulesPackagesConfig{
    			RulesPackageArns: []string{"example"},
    			Locale:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RulesPackages } from "@ubx/sdk-aws/aws/data/inspector/rules_packages";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_rules_packages" });

      data(RulesPackages, "example", {
        rulesPackageArns: ["example"],
        locale: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import RulesPackages, RulesPackagesConfig

    def describe():
        ubx.intent("look up aws_inspector_rules_packages")

        ubx.data(RulesPackages, "example", RulesPackagesConfig(
            rules_package_arns=["example"],
            locale="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="locale" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rules_package_arns" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="failed_items" type="map(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rules_packages" type="list(object)">
  Returned by the lookup.
</ResponseField>
