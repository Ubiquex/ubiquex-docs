---
title: "aws_inspector_assessment_runs"
description: "`aws_inspector_assessment_runs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector_assessment_runs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_assessment_runs"})

    		ubx.Data(inspector.AssessmentRuns, "example", inspector.AssessmentRunsConfig{
    			AssessmentRunArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssessmentRuns } from "@ubx/sdk-aws/aws/data/inspector/assessment_runs";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_assessment_runs" });

      data(AssessmentRuns, "example", {
        assessmentRunArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import AssessmentRuns, AssessmentRunsConfig

    def describe():
        ubx.intent("look up aws_inspector_assessment_runs")

        ubx.data(AssessmentRuns, "example", AssessmentRunsConfig(
            assessment_run_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_run_arns" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_runs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failed_items" type="map(object)">
  Returned by the lookup.
</ResponseField>
