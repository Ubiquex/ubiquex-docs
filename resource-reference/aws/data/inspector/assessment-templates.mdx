---
title: "aws_inspector_assessment_templates"
description: "`aws_inspector_assessment_templates` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_inspector_assessment_templates` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_inspector_assessment_templates"})

    		ubx.Data(inspector.AssessmentTemplates, "example", inspector.AssessmentTemplatesConfig{
    			AssessmentTargetArns: []string{"example"},
    			Filter:               map[string]any{"duration_range": map[string]any{"max_seconds": 1}},
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AssessmentTemplates } from "@ubx/sdk-aws/aws/data/inspector/assessment_templates";

    export default stack("example", () => {
      intent({ summary: "look up aws_inspector_assessment_templates" });

      data(AssessmentTemplates, "example", {
        assessmentTargetArns: ["example"],
        filter: { durationRange: { maxSeconds: 1 } },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.inspector import AssessmentTemplates, AssessmentTemplatesConfig

    def describe():
        ubx.intent("look up aws_inspector_assessment_templates")

        ubx.data(AssessmentTemplates, "example", AssessmentTemplatesConfig(
            assessment_target_arns=["example"],
            filter={"duration_range": {"max_seconds": 1}},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="assessment_target_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="object">
  &lt;p&gt;Used as the request parameter in the &lt;a&gt;ListAssessmentTemplates&lt;/a&gt; action.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="duration_range" type="object">
      &lt;p&gt;This data type is used in the &lt;a&gt;AssessmentTemplateFilter&lt;/a&gt; data type.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="max_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="min_seconds" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name_pattern" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="rules_package_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="assessment_template_arns" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
