---
title: "aws_models_v2_lex_test_set_generation"
description: "`aws_models_v2_lex_test_set_generation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_test_set_generation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_test_set_generation"})

    		ubx.Data(models_v2_lex.ModelsV2LexTestSetGeneration, "example", models_v2_lex.ModelsV2LexTestSetGenerationConfig{
    			TestSetGenerationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexTestSetGeneration } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_test_set_generation";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_test_set_generation" });

      data(ModelsV2LexTestSetGeneration, "example", {
        testSetGenerationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexTestSetGeneration, ModelsV2LexTestSetGenerationConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_test_set_generation")

        ubx.data(ModelsV2LexTestSetGeneration, "example", ModelsV2LexTestSetGenerationConfig(
            test_set_generation_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="test_set_generation_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reasons" type="list(string)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="generation_data_source" type="object">
  &lt;p&gt;Contains information about the data source from which the test set is generated.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="conversation_logs_data_source" type="object">
      &lt;p&gt;The data source that uses conversation logs.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="bot_alias_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="bot_id" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="filter" type="object" required>
          &lt;p&gt;The selected data source to filter the conversation log.&lt;/p&gt; Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="end_time" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="input_mode" type="string" required>
              Required lookup argument.
            </ResponseField>
            <ResponseField name="start_time" type="string" required>
              Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="locale_id" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="storage_location" type="object">
  &lt;p&gt;Contains information about the location in which the test set is stored.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="s3_bucket_name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="s3_path" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="test_set_generation_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="test_set_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="test_set_name" type="string">
  Returned by the lookup.
</ResponseField>
