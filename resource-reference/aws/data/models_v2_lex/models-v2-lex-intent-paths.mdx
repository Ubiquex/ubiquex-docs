---
title: "aws_models_v2_lex_intent_paths"
description: "`aws_models_v2_lex_intent_paths` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_intent_paths` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_intent_paths"})

    		ubx.Data(models_v2_lex.ModelsV2LexIntentPaths, "example", models_v2_lex.ModelsV2LexIntentPathsConfig{
    			BotId:         "example",
    			EndDateTime:   "example",
    			IntentPath:    "example",
    			StartDateTime: "example",
    			Filters:       []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexIntentPaths } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_intent_paths";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_intent_paths" });

      data(ModelsV2LexIntentPaths, "example", {
        botId: "example",
        endDateTime: "example",
        intentPath: "example",
        startDateTime: "example",
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexIntentPaths, ModelsV2LexIntentPathsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_intent_paths")

        ubx.data(ModelsV2LexIntentPaths, "example", ModelsV2LexIntentPathsConfig(
            bot_id="example",
            end_date_time="example",
            intent_path="example",
            start_date_time="example",
            filters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="end_date_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="intent_path" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_date_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="node_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>
