---
title: "aws_models_v2_lex_custom_vocabulary_items"
description: "`aws_models_v2_lex_custom_vocabulary_items` reads a real custom vocabulary word registered for a Lex V2 bot locale, improving speech recognition accuracy."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_custom_vocabulary_items"})

    		ubx.Data(models_v2_lex.ModelsV2LexCustomVocabularyItems, "example", models_v2_lex.ModelsV2LexCustomVocabularyItemsConfig{
    			BotId:      "example",
    			BotVersion: "example",
    			LocaleId:   "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexCustomVocabularyItems } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_custom_vocabulary_items";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_custom_vocabulary_items" });

      data(ModelsV2LexCustomVocabularyItems, "example", {
        botId: "example",
        botVersion: "example",
        localeId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexCustomVocabularyItems, ModelsV2LexCustomVocabularyItemsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_custom_vocabulary_items")

        ubx.data(ModelsV2LexCustomVocabularyItems, "example", ModelsV2LexCustomVocabularyItemsConfig(
            bot_id="example",
            bot_version="example",
            locale_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="custom_vocabulary_items" type="list(object)">
  The real, custom vocabulary items included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
