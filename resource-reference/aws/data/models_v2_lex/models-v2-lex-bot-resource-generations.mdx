---
title: "aws_models_v2_lex_bot_resource_generations"
description: "`aws_models_v2_lex_bot_resource_generations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_bot_resource_generations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_resource_generations"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotResourceGenerations, "example", models_v2_lex.ModelsV2LexBotResourceGenerationsConfig{
    			BotId:      "example",
    			BotVersion: "example",
    			LocaleId:   "example",
    			MaxResults: 1,
    			SortBy:     map[string]any{"attribute": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexBotResourceGenerations } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_bot_resource_generations";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_resource_generations" });

      data(ModelsV2LexBotResourceGenerations, "example", {
        botId: "example",
        botVersion: "example",
        localeId: "example",
        maxResults: 1,
        sortBy: { attribute: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotResourceGenerations, ModelsV2LexBotResourceGenerationsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_resource_generations")

        ubx.data(ModelsV2LexBotResourceGenerations, "example", ModelsV2LexBotResourceGenerationsConfig(
            bot_id="example",
            bot_version="example",
            locale_id="example",
            max_results=1,
            sort_by={"attribute": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;Specifies the attribute and method by which to sort the generation request information.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="generation_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
