---
title: "aws_models_v2_lex_utterance_metrics"
description: "`aws_models_v2_lex_utterance_metrics` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_utterance_metrics` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_utterance_metrics"})

    		ubx.Data(models_v2_lex.ModelsV2LexUtteranceMetrics, "example", models_v2_lex.ModelsV2LexUtteranceMetricsConfig{
    			BotId:         "example",
    			EndDateTime:   "example",
    			Metrics:       []map[string]any{{"name": "example-name"}},
    			StartDateTime: "example",
    			Attributes:    []map[string]any{{"name": "example-name"}},
    			BinBy:         []map[string]any{{"name": "example-name"}},
    			Filters:       []map[string]any{{"name": "example-name"}},
    			GroupBy:       []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexUtteranceMetrics } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_utterance_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_utterance_metrics" });

      data(ModelsV2LexUtteranceMetrics, "example", {
        botId: "example",
        endDateTime: "example",
        metrics: [{ name: "example-name" }],
        startDateTime: "example",
        attributes: [{ name: "example-name" }],
        binBy: [{ name: "example-name" }],
        filters: [{ name: "example-name" }],
        groupBy: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexUtteranceMetrics, ModelsV2LexUtteranceMetricsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_utterance_metrics")

        ubx.data(ModelsV2LexUtteranceMetrics, "example", ModelsV2LexUtteranceMetricsConfig(
            bot_id="example",
            end_date_time="example",
            metrics=[{"name": "example-name"}],
            start_date_time="example",
            attributes=[{"name": "example-name"}],
            bin_by=[{"name": "example-name"}],
            filters=[{"name": "example-name"}],
            group_by=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attributes" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="bin_by" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="end_date_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(object)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_date_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  Returned by the lookup.
</ResponseField>
