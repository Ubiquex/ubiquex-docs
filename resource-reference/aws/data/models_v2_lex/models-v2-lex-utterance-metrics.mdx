---
title: "aws_models_v2_lex_utterance_metrics"
description: "`aws_models_v2_lex_utterance_metrics` reads real, aggregated utterance-level analytics metrics for a Lex V2 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_utterance_metrics"})

    		ubx.Data(models_v2_lex.ModelsV2LexUtteranceMetrics, "example", models_v2_lex.ModelsV2LexUtteranceMetricsConfig{
    			BotId:         "example",
    			EndDateTime:   "example",
    			Metrics:       []map[string]any{{"name": "example-name"}},
    			StartDateTime: "example",
    			Attributes:    []map[string]any{{"name": "example-name"}},
    			BinBy:         []map[string]any{{"name": "example-name"}},
    			Filters:       []map[string]any{{"name": "example-name"}},
    			GroupBy:       []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexUtteranceMetrics } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_utterance_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_utterance_metrics" });

      data(ModelsV2LexUtteranceMetrics, "example", {
        botId: "example",
        endDateTime: "example",
        metrics: [{ name: "example-name" }],
        startDateTime: "example",
        attributes: [{ name: "example-name" }],
        binBy: [{ name: "example-name" }],
        filters: [{ name: "example-name" }],
        groupBy: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexUtteranceMetrics, ModelsV2LexUtteranceMetricsConfig
    from ubx.aws.data.models_v2_lex.models_v2_lex_utterance_metrics import ModelsV2LexUtteranceMetrics_Metrics, ModelsV2LexUtteranceMetrics_Attributes, ModelsV2LexUtteranceMetrics_BinBy, ModelsV2LexUtteranceMetrics_Filters

    def describe():
        ubx.intent("look up aws_models_v2_lex_utterance_metrics")

        ubx.data(ModelsV2LexUtteranceMetrics, "example", ModelsV2LexUtteranceMetricsConfig(
            bot_id="example",
            end_date_time="example",
            metrics=[ModelsV2LexUtteranceMetrics_Metrics(name="example-name")],
            start_date_time="example",
            attributes=[ModelsV2LexUtteranceMetrics_Attributes(name="example-name")],
            bin_by=[ModelsV2LexUtteranceMetrics_BinBy(name="example-name")],
            filters=[ModelsV2LexUtteranceMetrics_Filters(name="example-name")],
            group_by=[ModelsV2LexUtteranceMetrics_Attributes(name="example-name")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="attributes" type="list(object)">
  The real, structured attributes of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="bin_by" type="list(object)">
  The real dimension results are grouped (binned) by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_date_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(object)">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(object)" required>
  The real metrics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_date_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
