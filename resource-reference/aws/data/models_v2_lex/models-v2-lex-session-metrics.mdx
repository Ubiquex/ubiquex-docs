---
title: "aws_models_v2_lex_session_metrics"
description: "`aws_models_v2_lex_session_metrics` reads real, aggregated session-level analytics metrics for a Lex V2 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_session_metrics"})

    		ubx.Data(models_v2_lex.ModelsV2LexSessionMetrics, "example", models_v2_lex.ModelsV2LexSessionMetricsConfig{
    			BotId:         "example",
    			EndDateTime:   "example",
    			Metrics:       []map[string]any{{"name": "example-name"}},
    			StartDateTime: "example",
    			BinBy:         []map[string]any{{"name": "example-name"}},
    			Filters:       []map[string]any{{"name": "example-name"}},
    			GroupBy:       []map[string]any{{"name": "example-name"}},
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexSessionMetrics } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_session_metrics.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_session_metrics" });

      data(ModelsV2LexSessionMetrics, "example", {
        botId: "example",
        endDateTime: "example",
        metrics: [{ name: "example-name" }],
        startDateTime: "example",
        binBy: [{ name: "example-name" }],
        filters: [{ name: "example-name" }],
        groupBy: [{ name: "example-name" }],
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexSessionMetrics, ModelsV2LexSessionMetricsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_session_metrics")

        ubx.data(ModelsV2LexSessionMetrics, "example", ModelsV2LexSessionMetricsConfig(
            bot_id="example",
            end_date_time="example",
            metrics=[{"name": "example-name"}],
            start_date_time="example",
            bin_by=[{"name": "example-name"}],
            filters=[{"name": "example-name"}],
            group_by=[{"name": "example-name"}],
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bin_by" type="list(object)">
  The real dimension results are grouped (binned) by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_date_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="list(object)">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="metrics" type="list(object)" required>
  The real metrics included in this result. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_date_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
