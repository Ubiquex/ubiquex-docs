---
title: "aws_models_v2_lex_bot_alias_replicas"
description: "`aws_models_v2_lex_bot_alias_replicas` reads the real, replicated copies of a Lex V2 bot alias in another Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_alias_replicas"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotAliasReplicas, "example", models_v2_lex.ModelsV2LexBotAliasReplicasConfig{
    			BotId:         "example",
    			ReplicaRegion: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexBotAliasReplicas } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_bot_alias_replicas.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_alias_replicas" });

      data(ModelsV2LexBotAliasReplicas, "example", {
        botId: "example",
        replicaRegion: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotAliasReplicas, ModelsV2LexBotAliasReplicasConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_alias_replicas")

        ubx.data(ModelsV2LexBotAliasReplicas, "example", ModelsV2LexBotAliasReplicasConfig(
            bot_id="example",
            replica_region="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replica_region" type="string" required>
  &lt;p&gt;The region that contains the replicated bots. Minimum value = 2, maximum value = 25.&lt;/p&gt; Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_alias_replica_summaries" type="list(object)">
  The real, summarized bot alias replicas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_region" type="string">
  &lt;p&gt;The region that contains the replicated bots. Minimum value = 2, maximum value = 25.&lt;/p&gt; Returned by the lookup.
</ResponseField>
