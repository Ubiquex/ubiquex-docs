---
title: "aws_models_v2_lex_bot_locales"
description: "`aws_models_v2_lex_bot_locales` looks up a real locale (language and region) configured for a Lex V2 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_locales"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotLocales, "example", models_v2_lex.ModelsV2LexBotLocalesConfig{
    			BotId:      "example",
    			BotVersion: "example",
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			SortBy:     map[string]any{"attribute": "example", "order": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexBotLocales } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_bot_locales";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_locales" });

      data(ModelsV2LexBotLocales, "example", {
        botId: "example",
        botVersion: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: { attribute: "example", order: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotLocales, ModelsV2LexBotLocalesConfig
    from ubx.aws.data.models_v2_lex.models_v2_lex_bot_locales import ModelsV2LexBotLocales_Filters, ModelsV2LexBotLocales_SortBy

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_locales")

        ubx.data(ModelsV2LexBotLocales, "example", ModelsV2LexBotLocalesConfig(
            bot_id="example",
            bot_version="example",
            filters=[ModelsV2LexBotLocales_Filters(name="example-name")],
            max_results=1,
            sort_by=ModelsV2LexBotLocales_SortBy(attribute="example", order="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;Specifies attributes for sorting a list of bot locales.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="bot_locale_summaries" type="list(object)">
  The real, summarized bot locales included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
