---
title: "aws_models_v2_lex_custom_vocabulary_metadata"
description: "`aws_models_v2_lex_custom_vocabulary_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_custom_vocabulary_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_custom_vocabulary_metadata"})

    		ubx.Data(models_v2_lex.ModelsV2LexCustomVocabularyMetadata, "example", models_v2_lex.ModelsV2LexCustomVocabularyMetadataConfig{
    			BotId:      "example",
    			BotVersion: "example",
    			LocaleId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexCustomVocabularyMetadata } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_custom_vocabulary_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_custom_vocabulary_metadata" });

      data(ModelsV2LexCustomVocabularyMetadata, "example", {
        botId: "example",
        botVersion: "example",
        localeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexCustomVocabularyMetadata, ModelsV2LexCustomVocabularyMetadataConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_custom_vocabulary_metadata")

        ubx.data(ModelsV2LexCustomVocabularyMetadata, "example", ModelsV2LexCustomVocabularyMetadataConfig(
            bot_id="example",
            bot_version="example",
            locale_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="custom_vocabulary_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  Returned by the lookup.
</ResponseField>
