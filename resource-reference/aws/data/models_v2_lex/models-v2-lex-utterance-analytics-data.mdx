---
title: "aws_models_v2_lex_utterance_analytics_data"
description: "`aws_models_v2_lex_utterance_analytics_data` reads real, per-utterance conversation analytics for a Lex V2 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_utterance_analytics_data"})

    		ubx.Data(models_v2_lex.ModelsV2LexUtteranceAnalyticsData, "example", models_v2_lex.ModelsV2LexUtteranceAnalyticsDataConfig{
    			BotId:         "example",
    			EndDateTime:   "example",
    			StartDateTime: "example",
    			Filters:       []map[string]any{{"name": "example-name"}},
    			MaxResults:    1,
    			SortBy:        map[string]any{"name": "example-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexUtteranceAnalyticsData } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_utterance_analytics_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_utterance_analytics_data" });

      data(ModelsV2LexUtteranceAnalyticsData, "example", {
        botId: "example",
        endDateTime: "example",
        startDateTime: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: { name: "example-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexUtteranceAnalyticsData, ModelsV2LexUtteranceAnalyticsDataConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_utterance_analytics_data")

        ubx.data(ModelsV2LexUtteranceAnalyticsData, "example", ModelsV2LexUtteranceAnalyticsDataConfig(
            bot_id="example",
            end_date_time="example",
            start_date_time="example",
            filters=[{"name": "example-name"}],
            max_results=1,
            sort_by={"name": "example-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="end_date_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;An object specifying the measure and method by which to sort the utterance data.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_date_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="utterances" type="list(object)">
  The real Amazon Lex utterances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
