---
title: "aws_models_v2_lex_slot_types"
description: "`aws_models_v2_lex_slot_types` looks up a real Lex V2 bot slot type, the set of valid values a slot can accept."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_slot_types"})

    		ubx.Data(models_v2_lex.ModelsV2LexSlotTypes, "example", models_v2_lex.ModelsV2LexSlotTypesConfig{
    			BotId:      "example",
    			BotVersion: "example",
    			LocaleId:   "example",
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			SortBy:     map[string]any{"attribute": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexSlotTypes } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_slot_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_slot_types" });

      data(ModelsV2LexSlotTypes, "example", {
        botId: "example",
        botVersion: "example",
        localeId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: { attribute: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexSlotTypes, ModelsV2LexSlotTypesConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_slot_types")

        ubx.data(ModelsV2LexSlotTypes, "example", ModelsV2LexSlotTypesConfig(
            bot_id="example",
            bot_version="example",
            locale_id="example",
            filters=[{"name": "example-name"}],
            max_results=1,
            sort_by={"attribute": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;Specifies attributes for sorting a list of slot types.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="slot_type_summaries" type="list(object)">
  The real, summarized slot types included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
