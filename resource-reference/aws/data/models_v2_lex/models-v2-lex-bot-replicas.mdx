---
title: "aws_models_v2_lex_bot_replicas"
description: "`aws_models_v2_lex_bot_replicas` reads the real, replicated copies of a Lex V2 bot in another Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_replicas"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotReplicas, "example", models_v2_lex.ModelsV2LexBotReplicasConfig{
    			BotId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexBotReplicas } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_bot_replicas.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_replicas" });

      data(ModelsV2LexBotReplicas, "example", {
        botId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotReplicas, ModelsV2LexBotReplicasConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_replicas")

        ubx.data(ModelsV2LexBotReplicas, "example", ModelsV2LexBotReplicasConfig(
            bot_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_replica_summaries" type="list(object)">
  The real, summarized bot replicas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_region" type="string">
  &lt;p&gt;The region that contains the replicated bots. Minimum value = 2, maximum value = 25.&lt;/p&gt; Returned by the lookup.
</ResponseField>
