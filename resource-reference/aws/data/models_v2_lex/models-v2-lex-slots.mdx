---
title: "aws_models_v2_lex_slots"
description: "`aws_models_v2_lex_slots` looks up a real Lex V2 bot slot, one piece of information an intent needs to be fulfilled."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_slots"})

    		ubx.Data(models_v2_lex.ModelsV2LexSlots, "example", models_v2_lex.ModelsV2LexSlotsConfig{
    			BotId:      "example",
    			BotVersion: "example",
    			IntentId:   "example",
    			LocaleId:   "example",
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			SortBy:     map[string]any{"attribute": "example", "order": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexSlots } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_slots";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_slots" });

      data(ModelsV2LexSlots, "example", {
        botId: "example",
        botVersion: "example",
        intentId: "example",
        localeId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: { attribute: "example", order: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexSlots, ModelsV2LexSlotsConfig
    from ubx.aws.data.models_v2_lex.models_v2_lex_slots import ModelsV2LexSlots_Filters, ModelsV2LexSlots_SortBy

    def describe():
        ubx.intent("look up aws_models_v2_lex_slots")

        ubx.data(ModelsV2LexSlots, "example", ModelsV2LexSlotsConfig(
            bot_id="example",
            bot_version="example",
            intent_id="example",
            locale_id="example",
            filters=[ModelsV2LexSlots_Filters(name="example-name")],
            max_results=1,
            sort_by=ModelsV2LexSlots_SortBy(attribute="example", order="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="intent_id" type="string" required>
  The real, unique identifier of the intent this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;Specifies attributes for sorting a list of bots.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="slot_summaries" type="list(object)">
  The real, summarized slots included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
