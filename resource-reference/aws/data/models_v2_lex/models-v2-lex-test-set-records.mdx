---
title: "aws_models_v2_lex_test_set_records"
description: "`aws_models_v2_lex_test_set_records` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_test_set_records` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_test_set_records"})

    		ubx.Data(models_v2_lex.ModelsV2LexTestSetRecords, "example", models_v2_lex.ModelsV2LexTestSetRecordsConfig{
    			TestSetId:  "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexTestSetRecords } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_test_set_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_test_set_records" });

      data(ModelsV2LexTestSetRecords, "example", {
        testSetId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexTestSetRecords, ModelsV2LexTestSetRecordsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_test_set_records")

        ubx.data(ModelsV2LexTestSetRecords, "example", ModelsV2LexTestSetRecordsConfig(
            test_set_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="test_set_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="test_set_records" type="list(object)">
  Returned by the lookup.
</ResponseField>
