---
title: "aws_models_v2_lex_bot_resource_generation"
description: "`aws_models_v2_lex_bot_resource_generation` looks up one real, generative-AI-assisted Lex V2 bot resource generation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_resource_generation"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotResourceGeneration, "example", models_v2_lex.ModelsV2LexBotResourceGenerationConfig{
    			BotId:        "example",
    			BotVersion:   "example",
    			GenerationId: "example",
    			LocaleId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexBotResourceGeneration } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_bot_resource_generation.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_resource_generation" });

      data(ModelsV2LexBotResourceGeneration, "example", {
        botId: "example",
        botVersion: "example",
        generationId: "example",
        localeId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotResourceGeneration, ModelsV2LexBotResourceGenerationConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_resource_generation")

        ubx.data(ModelsV2LexBotResourceGeneration, "example", ModelsV2LexBotResourceGenerationConfig(
            bot_id="example",
            bot_version="example",
            generation_id="example",
            locale_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="generation_id" type="string" required>
  The real, unique identifier of the generation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_date_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="failure_reasons" type="list(string)">
  The real, human-readable reasons for the failures. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="generated_bot_locale_url" type="string">
  The real, resolvable URL of the generated bot locale this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="generation_input_prompt" type="string">
  The real, input prompt used to generate this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="generation_status" type="string">
  The real, current status of the generation this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="model_arn" type="string">
  The real Amazon Resource Name (ARN) of the model this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
