---
title: "aws_models_v2_lex_imports"
description: "`aws_models_v2_lex_imports` reads the real imports of a Lex V2 bot or test set."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_imports"})

    		ubx.Data(models_v2_lex.ModelsV2LexImports, "example", models_v2_lex.ModelsV2LexImportsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			SortBy:     map[string]any{"attribute": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexImports } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_imports.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_imports" });

      data(ModelsV2LexImports, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: { attribute: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexImports, ModelsV2LexImportsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_imports")

        ubx.data(ModelsV2LexImports, "example", ModelsV2LexImportsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            sort_by={"attribute": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;Provides information for sorting a list of imports.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="bot_id" type="string">
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bot_version" type="string">
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="import_summaries" type="list(object)">
  The real, summarized imports included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="locale_id" type="string">
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
