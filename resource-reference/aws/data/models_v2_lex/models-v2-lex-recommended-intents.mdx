---
title: "aws_models_v2_lex_recommended_intents"
description: "`aws_models_v2_lex_recommended_intents` reads real, system-recommended intents generated from analyzing a Lex V2 bot's own conversation transcripts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_recommended_intents"})

    		ubx.Data(models_v2_lex.ModelsV2LexRecommendedIntents, "example", models_v2_lex.ModelsV2LexRecommendedIntentsConfig{
    			BotId:               "example",
    			BotRecommendationId: "example",
    			BotVersion:          "example",
    			LocaleId:            "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexRecommendedIntents } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_recommended_intents.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_recommended_intents" });

      data(ModelsV2LexRecommendedIntents, "example", {
        botId: "example",
        botRecommendationId: "example",
        botVersion: "example",
        localeId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexRecommendedIntents, ModelsV2LexRecommendedIntentsConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_recommended_intents")

        ubx.data(ModelsV2LexRecommendedIntents, "example", ModelsV2LexRecommendedIntentsConfig(
            bot_id="example",
            bot_recommendation_id="example",
            bot_version="example",
            locale_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_recommendation_id" type="string" required>
  The real, unique identifier of the bot recommendation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="bot_version" type="string" required>
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="summary_list" type="list(object)">
  The real summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
