---
title: "aws_models_v2_lex_aggregated_utterances"
description: "`aws_models_v2_lex_aggregated_utterances` reads real, aggregated utterance analytics reported by a Lex V2 bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_aggregated_utterances"})

    		ubx.Data(models_v2_lex.ModelsV2LexAggregatedUtterances, "example", models_v2_lex.ModelsV2LexAggregatedUtterancesConfig{
    			AggregationDuration: map[string]any{"relative_aggregation_duration": map[string]any{"time_dimension": "example", "time_value": 1}},
    			BotId:               "example",
    			LocaleId:            "example",
    			Filters:             []map[string]any{{"name": "example-name"}},
    			MaxResults:          1,
    			SortBy:              map[string]any{"attribute": "example", "order": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexAggregatedUtterances } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_aggregated_utterances";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_aggregated_utterances" });

      data(ModelsV2LexAggregatedUtterances, "example", {
        aggregationDuration: {
          relativeAggregationDuration: { timeDimension: "example", timeValue: 1 },
        },
        botId: "example",
        localeId: "example",
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: { attribute: "example", order: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexAggregatedUtterances, ModelsV2LexAggregatedUtterancesConfig
    from ubx.aws.data.models_v2_lex.models_v2_lex_aggregated_utterances import ModelsV2LexAggregatedUtterances_AggregationDuration, ModelsV2LexAggregatedUtterances_AggregationDuration_RelativeAggregationDuration, ModelsV2LexAggregatedUtterances_Filters, ModelsV2LexAggregatedUtterances_SortBy

    def describe():
        ubx.intent("look up aws_models_v2_lex_aggregated_utterances")

        ubx.data(ModelsV2LexAggregatedUtterances, "example", ModelsV2LexAggregatedUtterancesConfig(
            aggregation_duration=ModelsV2LexAggregatedUtterances_AggregationDuration(relative_aggregation_duration=ModelsV2LexAggregatedUtterances_AggregationDuration_RelativeAggregationDuration(time_dimension="example", time_value=1)),
            bot_id="example",
            locale_id="example",
            filters=[ModelsV2LexAggregatedUtterances_Filters(name="example-name")],
            max_results=1,
            sort_by=ModelsV2LexAggregatedUtterances_SortBy(attribute="example", order="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aggregation_duration" type="object" required>
  &lt;p&gt;Provides parameters for setting the time window and duration for aggregating utterance data.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="relative_aggregation_duration" type="object" required>
      &lt;p&gt;Specifies the time window that utterance statistics are returned for. The time window is always relative to the last time that the that utterances were aggregated. For example, if the &lt;code&gt;ListAggregatedUtterances&lt;/code&gt; operation is called at 1600, the time window is set to 1 hour, and the last refresh time was 1530, only utterances made between 1430 and 1530 are returned.&lt;/p&gt; &lt;p&gt;You can choose the time window that statistics should be returned for.&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;b&gt;Hours&lt;/b&gt; - You can request utterance statistics for 1, 3, 6, 12, or 24 hour time windows. Statistics are refreshed every half hour for 1 hour time windows, and hourly for the other time windows.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;b&gt;Days&lt;/b&gt; - You can request utterance statistics for 3 days. Statistics are refreshed every 6 hours.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;b&gt;Weeks&lt;/b&gt; - You can see statistics for one or two weeks. Statistics are refreshed every 12 hours for one week time windows, and once per day for two week time windows.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="time_dimension" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="time_value" type="number" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="locale_id" type="string" required>
  The real, unique identifier of the locale this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;Specifies attributes for sorting a list of utterances.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="aggregated_utterances_summaries" type="list(object)">
  The real, summarized aggregated utterancess included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="aggregation_last_refreshed_date_time" type="string">
  The real timestamp of the aggregation last refreshed date. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="aggregation_window_end_time" type="string">
  The real timestamp of the aggregation window end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="aggregation_window_start_time" type="string">
  The real timestamp of the aggregation window start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bot_alias_id" type="string">
  The real, unique identifier of the bot alias this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bot_version" type="string">
  The real version of the bot this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
