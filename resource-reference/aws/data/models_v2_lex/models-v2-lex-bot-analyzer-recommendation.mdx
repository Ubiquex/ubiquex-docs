---
title: "aws_models_v2_lex_bot_analyzer_recommendation"
description: "`aws_models_v2_lex_bot_analyzer_recommendation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_models_v2_lex_bot_analyzer_recommendation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_analyzer_recommendation"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotAnalyzerRecommendation, "example", models_v2_lex.ModelsV2LexBotAnalyzerRecommendationConfig{
    			BotAnalyzerRequestId: "example",
    			BotId:                "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexBotAnalyzerRecommendation } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_bot_analyzer_recommendation";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_analyzer_recommendation" });

      data(ModelsV2LexBotAnalyzerRecommendation, "example", {
        botAnalyzerRequestId: "example",
        botId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotAnalyzerRecommendation, ModelsV2LexBotAnalyzerRecommendationConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_analyzer_recommendation")

        ubx.data(ModelsV2LexBotAnalyzerRecommendation, "example", ModelsV2LexBotAnalyzerRecommendationConfig(
            bot_analyzer_request_id="example",
            bot_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_analyzer_request_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="bot_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_analyzer_recommendation_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="bot_analyzer_status" type="string">
  &lt;p&gt;The status of the bot analysis execution.&lt;/p&gt; &lt;p&gt;Valid values include:&lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Processing&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Available&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Failed&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Stopping&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt; &lt;code&gt;Stopped&lt;/code&gt; &lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
</ResponseField>

<ResponseField name="bot_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="locale_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
