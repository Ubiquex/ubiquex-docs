---
title: "aws_models_v2_lex_bot_aliases"
description: "`aws_models_v2_lex_bot_aliases` looks up a real Lex V2 bot alias, a named, versioned pointer used to deploy a bot."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_aliases"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotAliases, "example", models_v2_lex.ModelsV2LexBotAliasesConfig{
    			BotId:      "example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ModelsV2LexBotAliases } from "./local-sdk/aws/sdk/typescript/aws/data/models_v2_lex/models_v2_lex_bot_aliases.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_aliases" });

      data(ModelsV2LexBotAliases, "example", {
        botId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotAliases, ModelsV2LexBotAliasesConfig

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_aliases")

        ubx.data(ModelsV2LexBotAliases, "example", ModelsV2LexBotAliasesConfig(
            bot_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bot_alias_summaries" type="list(object)">
  The real, summarized bot aliass included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
