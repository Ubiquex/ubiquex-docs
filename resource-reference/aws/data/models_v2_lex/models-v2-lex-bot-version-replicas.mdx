---
title: "aws_models_v2_lex_bot_version_replicas"
description: "`aws_models_v2_lex_bot_version_replicas` reads the real, replicated copies of a Lex V2 bot version in another Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	models_v2_lex "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/models_v2_lex"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_models_v2_lex_bot_version_replicas"})

    		ubx.Data(models_v2_lex.ModelsV2LexBotVersionReplicas, "example", models_v2_lex.ModelsV2LexBotVersionReplicasConfig{
    			BotId:         "example",
    			ReplicaRegion: "example",
    			MaxResults:    1,
    			SortBy:        map[string]any{"attribute": "example", "order": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ModelsV2LexBotVersionReplicas } from "@ubx/sdk-aws/aws/data/models_v2_lex/models_v2_lex_bot_version_replicas";

    export default stack("example", () => {
      intent({ summary: "look up aws_models_v2_lex_bot_version_replicas" });

      data(ModelsV2LexBotVersionReplicas, "example", {
        botId: "example",
        replicaRegion: "example",
        maxResults: 1,
        sortBy: { attribute: "example", order: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.models_v2_lex import ModelsV2LexBotVersionReplicas, ModelsV2LexBotVersionReplicasConfig
    from ubx.aws.data.models_v2_lex.models_v2_lex_bot_version_replicas import ModelsV2LexBotVersionReplicas_SortBy

    def describe():
        ubx.intent("look up aws_models_v2_lex_bot_version_replicas")

        ubx.data(ModelsV2LexBotVersionReplicas, "example", ModelsV2LexBotVersionReplicasConfig(
            bot_id="example",
            replica_region="example",
            max_results=1,
            sort_by=ModelsV2LexBotVersionReplicas_SortBy(attribute="example", order="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bot_id" type="string" required>
  The real, unique identifier of the bot this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replica_region" type="string" required>
  &lt;p&gt;The region that contains the replicated bots. Minimum value = 2, maximum value = 25.&lt;/p&gt; Required lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="object">
  &lt;p&gt;The sort category for the version replicated bots.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="attribute" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="order" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="bot_version_replica_summaries" type="list(object)">
  The real, summarized bot version replicas included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="source_region" type="string">
  &lt;p&gt;The region that contains the replicated bots. Minimum value = 2, maximum value = 25.&lt;/p&gt; Returned by the lookup.
</ResponseField>
