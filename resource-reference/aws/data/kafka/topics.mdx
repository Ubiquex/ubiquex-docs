---
title: "aws_kafka_topics"
description: "`aws_kafka_topics` reads the real Kafka topics on an MSK cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_topics"})

    		ubx.Data(kafka.Topics, "example", kafka.TopicsConfig{
    			ClusterArn:      "arn:aws:iam::123456789012:policy/example",
    			MaxResults:      1,
    			TopicNameFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Topics } from "@ubx/sdk-aws/aws/data/kafka/topics";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_topics" });

      data(Topics, "example", {
        clusterArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        topicNameFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import Topics, TopicsConfig

    def describe():
        ubx.intent("look up aws_kafka_topics")

        ubx.data(Topics, "example", TopicsConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            topic_name_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="topic_name_filter" type="string">
  Restricts results to this topic name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="topics" type="list(object)">
  The real topics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
