---
title: "aws_kafka_configuration_revision"
description: "`aws_kafka_configuration_revision` looks up one real, saved revision of an MSK cluster configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_configuration_revision"})

    		ubx.Data(kafka.ConfigurationRevision, "example", kafka.ConfigurationRevisionConfig{
    			Arn:      "arn:aws:iam::123456789012:policy/example",
    			Revision: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConfigurationRevision } from "./local-sdk/aws/sdk/typescript/aws/data/kafka/configuration_revision.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_configuration_revision" });

      data(ConfigurationRevision, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        revision: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import ConfigurationRevision, ConfigurationRevisionConfig

    def describe():
        ubx.intent("look up aws_kafka_configuration_revision")

        ubx.data(ConfigurationRevision, "example", ConfigurationRevisionConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            revision=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="revision" type="number" required>
  The real revision of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_time" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="server_properties" type="string">
  The real, structured properties of this server. **(AI-inferred)** Returned by the lookup.
</ResponseField>
