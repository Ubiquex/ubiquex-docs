---
title: "aws_kafka_clusters_v2"
description: "`aws_kafka_clusters_v2` looks up a real MSK cluster, either provisioned or Serverless."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_clusters_v2"})

    		ubx.Data(kafka.ClustersV2, "example", kafka.ClustersV2Config{
    			ClusterNameFilter: "example",
    			ClusterTypeFilter: "example",
    			MaxResults:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ClustersV2 } from "./local-sdk/aws/sdk/typescript/aws/data/kafka/clusters_v2.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_clusters_v2" });

      data(ClustersV2, "example", {
        clusterNameFilter: "example",
        clusterTypeFilter: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import ClustersV2, ClustersV2Config

    def describe():
        ubx.intent("look up aws_kafka_clusters_v2")

        ubx.data(ClustersV2, "example", ClustersV2Config(
            cluster_name_filter="example",
            cluster_type_filter="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_name_filter" type="string">
  Restricts results to clusters matching this name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="cluster_type_filter" type="string">
  Restricts results to this cluster type. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_info_list" type="list(object)">
  The real cluster infos included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
