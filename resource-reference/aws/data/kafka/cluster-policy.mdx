---
title: "aws_kafka_cluster_policy"
description: "`aws_kafka_cluster_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kafka_cluster_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_cluster_policy"})

    		ubx.Data(kafka.ClusterPolicy, "example", kafka.ClusterPolicyConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ClusterPolicy } from "@ubx/sdk-aws/aws/data/kafka/cluster_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_cluster_policy" });

      data(ClusterPolicy, "example", {
        clusterArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import ClusterPolicy, ClusterPolicyConfig

    def describe():
        ubx.intent("look up aws_kafka_cluster_policy")

        ubx.data(ClusterPolicy, "example", ClusterPolicyConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="current_version" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>
