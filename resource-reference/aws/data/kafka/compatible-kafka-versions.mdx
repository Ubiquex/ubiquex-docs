---
title: "aws_kafka_compatible_kafka_versions"
description: "`aws_kafka_compatible_kafka_versions` reads the real Kafka versions an MSK cluster can be upgraded to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_compatible_kafka_versions"})

    		ubx.Data(kafka.CompatibleKafkaVersions, "example", kafka.CompatibleKafkaVersionsConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CompatibleKafkaVersions } from "./local-sdk/aws/sdk/typescript/aws/data/kafka/compatible_kafka_versions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_compatible_kafka_versions" });

      data(CompatibleKafkaVersions, "example", {
        clusterArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import CompatibleKafkaVersions, CompatibleKafkaVersionsConfig

    def describe():
        ubx.intent("look up aws_kafka_compatible_kafka_versions")

        ubx.data(CompatibleKafkaVersions, "example", CompatibleKafkaVersionsConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_arn" type="string">
  The real Amazon Resource Name (ARN) of the cluster this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compatible_kafka_versions" type="list(object)">
  The real versions of the compatible kafka included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
