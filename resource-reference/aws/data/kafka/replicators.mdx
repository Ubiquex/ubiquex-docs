---
title: "aws_kafka_replicators"
description: "`aws_kafka_replicators` looks up a real MSK Replicator, continuously replicating topics between two MSK clusters."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_replicators"})

    		ubx.Data(kafka.Replicators, "example", kafka.ReplicatorsConfig{
    			MaxResults:           1,
    			ReplicatorNameFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Replicators } from "./local-sdk/aws/sdk/typescript/aws/data/kafka/replicators.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_replicators" });

      data(Replicators, "example", {
        maxResults: 1,
        replicatorNameFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import Replicators, ReplicatorsConfig

    def describe():
        ubx.intent("look up aws_kafka_replicators")

        ubx.data(Replicators, "example", ReplicatorsConfig(
            max_results=1,
            replicator_name_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="replicator_name_filter" type="string">
  Restricts results to this replicator name. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replicators" type="list(object)">
  The real MSK Replicator replicators included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
