---
title: "aws_kafka_configuration_revisions"
description: "`aws_kafka_configuration_revisions` reads the real, saved revisions of an MSK cluster configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_configuration_revisions"})

    		ubx.Data(kafka.ConfigurationRevisions, "example", kafka.ConfigurationRevisionsConfig{
    			Arn:        "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigurationRevisions } from "@ubx/sdk-aws/aws/data/kafka/configuration_revisions";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_configuration_revisions" });

      data(ConfigurationRevisions, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import ConfigurationRevisions, ConfigurationRevisionsConfig

    def describe():
        ubx.intent("look up aws_kafka_configuration_revisions")

        ubx.data(ConfigurationRevisions, "example", ConfigurationRevisionsConfig(
            arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="revisions" type="list(object)">
  The real revisions of this object included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
