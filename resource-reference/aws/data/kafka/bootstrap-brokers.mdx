---
title: "aws_kafka_bootstrap_brokers"
description: "`aws_kafka_bootstrap_brokers` reads the real bootstrap broker connection strings of an MSK cluster."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_bootstrap_brokers"})

    		ubx.Data(kafka.BootstrapBrokers, "example", kafka.BootstrapBrokersConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BootstrapBrokers } from "./local-sdk/aws/sdk/typescript/aws/data/kafka/bootstrap_brokers.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_bootstrap_brokers" });

      data(BootstrapBrokers, "example", {
        clusterArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import BootstrapBrokers, BootstrapBrokersConfig

    def describe():
        ubx.intent("look up aws_kafka_bootstrap_brokers")

        ubx.data(BootstrapBrokers, "example", BootstrapBrokersConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the cluster this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bootstrap_broker_string" type="string">
  The real, connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_ipv6" type="string">
  The real, IPv6 connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_public_sasl_iam" type="string">
  The real, public IAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_public_sasl_scram" type="string">
  The real, public SCRAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_public_tls" type="string">
  The real, public TLS connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_sasl_iam" type="string">
  The real, IAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_sasl_iam_ipv6" type="string">
  The real, IPv6 IAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_sasl_scram" type="string">
  The real, SCRAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_sasl_scram_ipv6" type="string">
  The real, IPv6 SCRAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_tls" type="string">
  The real, TLS connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_tls_ipv6" type="string">
  The real, IPv6 TLS connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_vpc_connectivity_sasl_iam" type="string">
  The real, VPC-connectivity IAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_vpc_connectivity_sasl_scram" type="string">
  The real, VPC-connectivity SCRAM-authenticated SASL connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="bootstrap_broker_string_vpc_connectivity_tls" type="string">
  The real, VPC-connectivity TLS connection string for this MSK cluster's own bootstrap brokers. **(AI-inferred)** Returned by the lookup.
</ResponseField>
