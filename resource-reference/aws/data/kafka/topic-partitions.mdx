---
title: "aws_kafka_topic_partitions"
description: "`aws_kafka_topic_partitions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_kafka_topic_partitions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kafka "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/kafka"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_kafka_topic_partitions"})

    		ubx.Data(kafka.TopicPartitions, "example", kafka.TopicPartitionsConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/example",
    			TopicName:  "example-topic-name",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TopicPartitions } from "@ubx/sdk-aws/aws/data/kafka/topic_partitions";

    export default stack("example", () => {
      intent({ summary: "look up aws_kafka_topic_partitions" });

      data(TopicPartitions, "example", {
        clusterArn: "arn:aws:iam::123456789012:policy/example",
        topicName: "example-topic-name",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.kafka import TopicPartitions, TopicPartitionsConfig

    def describe():
        ubx.intent("look up aws_kafka_topic_partitions")

        ubx.data(TopicPartitions, "example", TopicPartitionsConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/example",
            topic_name="example-topic-name",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="topic_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="partitions" type="list(object)">
  Returned by the lookup.
</ResponseField>
