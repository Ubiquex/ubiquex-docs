---
title: "aws_gamelift_ec2_instance_limits"
description: "`aws_gamelift_ec2_instance_limits` reads the real, current EC2 instance limits and usage for GameLift fleets in a Region."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_ec2_instance_limits"})

    		ubx.Data(gamelift.Ec2InstanceLimits, "example", gamelift.Ec2InstanceLimitsConfig{
    			Ec2InstanceType: "example",
    			Location:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Ec2InstanceLimits } from "@ubx/sdk-aws/aws/data/gamelift/ec2_instance_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_ec2_instance_limits" });

      data(Ec2InstanceLimits, "example", {
        ec2InstanceType: "example",
        location: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import Ec2InstanceLimits, Ec2InstanceLimitsConfig

    def describe():
        ubx.intent("look up aws_gamelift_ec2_instance_limits")

        ubx.data(Ec2InstanceLimits, "example", Ec2InstanceLimitsConfig(
            ec2_instance_type="example",
            location="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ec2_instance_type" type="string">
  The real, discriminating type of the ec2 instance this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="location" type="string">
  The real location this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ec2_instance_limits" type="list(object)">
  The real, current EC2 instance limits for this account. **(AI-inferred)** Returned by the lookup.
</ResponseField>
