---
title: "aws_gamelift_container_group_definition_versions"
description: "`data_aws_gamelift_container_group_definition_versions` looks up a real, already-existing `aws_game_lift_container_group_definition`."
---

It is the real, versioned blueprint an `aws_game_lift_container_fleet` deploys from, which container images to run per instance and how, game-server containers plus any supporting per-instance containers like logging or background services.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_container_group_definition_versions"})

    		ubx.Data(gamelift.ContainerGroupDefinitionVersions, "example", gamelift.ContainerGroupDefinitionVersionsConfig{
    			Name:  "example",
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContainerGroupDefinitionVersions } from "@ubx/sdk-aws/aws/data/gamelift/container_group_definition_versions";

    export default stack("example", () => {
      intent({
        summary: "look up aws_gamelift_container_group_definition_versions",
      });

      data(ContainerGroupDefinitionVersions, "example", {
        name: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import ContainerGroupDefinitionVersions, ContainerGroupDefinitionVersionsConfig

    def describe():
        ubx.intent("look up aws_gamelift_container_group_definition_versions")

        ubx.data(ContainerGroupDefinitionVersions, "example", ContainerGroupDefinitionVersionsConfig(
            name="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="container_group_definitions" type="list(object)">
  The real GameLift container group definitions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
