---
title: "aws_gamelift_fleet_utilization"
description: "`aws_gamelift_fleet_utilization` reads the real, current game session utilization of a GameLift fleet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_fleet_utilization"})

    		ubx.Data(gamelift.FleetUtilization, "example", gamelift.FleetUtilizationConfig{
    			FleetIds: []string{"example"},
    			Limit:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FleetUtilization } from "./local-sdk/aws/sdk/typescript/aws/data/gamelift/fleet_utilization.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_fleet_utilization" });

      data(FleetUtilization, "example", {
        fleetIds: ["example"],
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import FleetUtilization, FleetUtilizationConfig

    def describe():
        ubx.intent("look up aws_gamelift_fleet_utilization")

        ubx.data(FleetUtilization, "example", FleetUtilizationConfig(
            fleet_ids=["example"],
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_ids" type="list(string)">
  The real, unique identifiers of the fleets this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fleet_utilization" type="list(object)">
  The real, current utilization of this fleet. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
