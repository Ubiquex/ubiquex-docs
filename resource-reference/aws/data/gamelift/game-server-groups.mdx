---
title: "aws_gamelift_game_server_groups"
description: "`data_aws_gamelift_game_server_groups` looks up a real, already-existing `aws_game_lift_game_server_group`."
---

It runs GameLift FleetIQ on top of a real, self-managed EC2 Auto Scaling group, optimizing for Spot Instance savings while protecting instances that are still hosting active game sessions from early termination.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_game_server_groups"})

    		ubx.Data(gamelift.GameServerGroups, "example", gamelift.GameServerGroupsConfig{
    			Limit: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GameServerGroups } from "@ubx/sdk-aws/aws/data/gamelift/game_server_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_game_server_groups" });

      data(GameServerGroups, "example", {
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import GameServerGroups, GameServerGroupsConfig

    def describe():
        ubx.intent("look up aws_gamelift_game_server_groups")

        ubx.data(GameServerGroups, "example", GameServerGroupsConfig(
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="game_server_groups" type="list(object)">
  The real game server groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
