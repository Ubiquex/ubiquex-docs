---
title: "aws_gamelift_compute_access"
description: "`aws_gamelift_compute_access` reads real, temporary credentials for accessing a compute resource within a GameLift fleet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_compute_access"})

    		ubx.Data(gamelift.ComputeAccess, "example", gamelift.ComputeAccessConfig{
    			ComputeName: "example-compute-name",
    			FleetId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ComputeAccess } from "./local-sdk/aws/sdk/typescript/aws/data/gamelift/compute_access.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_compute_access" });

      data(ComputeAccess, "example", {
        computeName: "example-compute-name",
        fleetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import ComputeAccess, ComputeAccessConfig

    def describe():
        ubx.intent("look up aws_gamelift_compute_access")

        ubx.data(ComputeAccess, "example", ComputeAccessConfig(
            compute_name="example-compute-name",
            fleet_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="compute_name" type="string" required>
  The real name of the compute resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fleet_id" type="string" required>
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compute_arn" type="string">
  The real Amazon Resource Name (ARN) of the compute this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="container_identifiers" type="list(object)">
  The real, unique identifiers of the containers this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="credentials" type="object">
  &lt;p&gt;Amazon Web Services account security credentials that allow interactions with Amazon GameLift Servers resources. The credentials are temporary and valid for a limited time span. You can request fresh credentials at any time. &lt;/p&gt; &lt;p&gt; Amazon Web Services security credentials consist of three parts: an access key ID, a secret access key, and a session token. You must use all three parts together to authenticate your access requests.&lt;/p&gt; &lt;p&gt;You need Amazon Web Services credentials for the following tasks: &lt;/p&gt; &lt;ul&gt; &lt;li&gt; &lt;p&gt;To upload a game server build directly to Amazon GameLift Servers S3 storage using &lt;code&gt;CreateBuild&lt;/code&gt;. To get access for this task, call &lt;a href="https://docs.aws.amazon.com/gamelift/latest/apireference/API_RequestUploadCredentials.html"&gt;https://docs.aws.amazon.com/gamelift/latest/apireference/API_RequestUploadCredentials.html&lt;/a&gt;.&lt;/p&gt; &lt;/li&gt; &lt;li&gt; &lt;p&gt;To remotely connect to an active Amazon GameLift Servers fleet instances. To get remote access, call &lt;a href="https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetComputeAccess.html"&gt;https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetComputeAccess.html&lt;/a&gt;.&lt;/p&gt; &lt;/li&gt; &lt;/ul&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="access_key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="secret_access_key" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="session_token" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fleet_arn" type="string">
  The real Amazon Resource Name (ARN) of the fleet this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target" type="string">
  The real object this action targets. **(AI-inferred)** Returned by the lookup.
</ResponseField>
