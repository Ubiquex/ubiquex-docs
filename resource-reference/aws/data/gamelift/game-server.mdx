---
title: "aws_gamelift_game_server"
description: "`aws_gamelift_game_server` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gamelift_game_server` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_game_server"})

    		ubx.Data(gamelift.GameServer, "example", gamelift.GameServerConfig{
    			GameServerGroupName: "example-game-server-group-name",
    			GameServerId:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GameServer } from "@ubx/sdk-aws/aws/data/gamelift/game_server";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_game_server" });

      data(GameServer, "example", {
        gameServerGroupName: "example-game-server-group-name",
        gameServerId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import GameServer, GameServerConfig

    def describe():
        ubx.intent("look up aws_gamelift_game_server")

        ubx.data(GameServer, "example", GameServerConfig(
            game_server_group_name="example-game-server-group-name",
            game_server_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="game_server_group_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="game_server_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="game_server" type="object">
  &lt;p&gt;Properties describing a game server that is running on an instance in a game server group. &lt;/p&gt; &lt;p&gt;A game server is created by a successful call to &lt;code&gt;RegisterGameServer&lt;/code&gt; and deleted by calling &lt;code&gt;DeregisterGameServer&lt;/code&gt;. A game server is claimed to host a game session by calling &lt;code&gt;ClaimGameServer&lt;/code&gt;. &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="claim_status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="connection_info" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="game_server_data" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="game_server_group_arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="game_server_group_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="game_server_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="instance_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_claim_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_health_check_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="registration_time" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="utilization_status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
