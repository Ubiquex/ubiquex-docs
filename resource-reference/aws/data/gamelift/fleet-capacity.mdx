---
title: "aws_gamelift_fleet_capacity"
description: "`aws_gamelift_fleet_capacity` reads the real, current instance capacity of a GameLift fleet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_fleet_capacity"})

    		ubx.Data(gamelift.FleetCapacity, "example", gamelift.FleetCapacityConfig{
    			FleetIds: []string{"example"},
    			Limit:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetCapacity } from "@ubx/sdk-aws/aws/data/gamelift/fleet_capacity";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_fleet_capacity" });

      data(FleetCapacity, "example", {
        fleetIds: ["example"],
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import FleetCapacity, FleetCapacityConfig

    def describe():
        ubx.intent("look up aws_gamelift_fleet_capacity")

        ubx.data(FleetCapacity, "example", FleetCapacityConfig(
            fleet_ids=["example"],
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_ids" type="list(string)">
  The real, unique identifiers of the fleets this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fleet_capacity" type="list(object)">
  The real, current capacity of this fleet. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
