---
title: "aws_gamelift_fleet_deployments"
description: "`aws_gamelift_fleet_deployments` reads the real deployments run against a GameLift fleet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_fleet_deployments"})

    		ubx.Data(gamelift.FleetDeployments, "example", gamelift.FleetDeploymentsConfig{
    			FleetId: "example",
    			Limit:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetDeployments } from "@ubx/sdk-aws/aws/data/gamelift/fleet_deployments";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_fleet_deployments" });

      data(FleetDeployments, "example", {
        fleetId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import FleetDeployments, FleetDeploymentsConfig

    def describe():
        ubx.intent("look up aws_gamelift_fleet_deployments")

        ubx.data(FleetDeployments, "example", FleetDeploymentsConfig(
            fleet_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_id" type="string">
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fleet_deployments" type="list(object)">
  The real fleet deployments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
