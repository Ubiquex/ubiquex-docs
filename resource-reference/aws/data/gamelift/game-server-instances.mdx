---
title: "aws_gamelift_game_server_instances"
description: "`aws_gamelift_game_server_instances` reads the real EC2 instances registered within a GameLift FleetIQ game server group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_game_server_instances"})

    		ubx.Data(gamelift.GameServerInstances, "example", gamelift.GameServerInstancesConfig{
    			GameServerGroupName: "example-game-server-group-name",
    			InstanceIds:         []string{"example"},
    			Limit:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GameServerInstances } from "@ubx/sdk-aws/aws/data/gamelift/game_server_instances";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_game_server_instances" });

      data(GameServerInstances, "example", {
        gameServerGroupName: "example-game-server-group-name",
        instanceIds: ["example"],
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import GameServerInstances, GameServerInstancesConfig

    def describe():
        ubx.intent("look up aws_gamelift_game_server_instances")

        ubx.data(GameServerInstances, "example", GameServerInstancesConfig(
            game_server_group_name="example-game-server-group-name",
            instance_ids=["example"],
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="game_server_group_name" type="string" required>
  The real name of the game server group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="instance_ids" type="list(string)">
  The real, unique identifiers of the instances this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="game_server_instances" type="list(object)">
  The real GameLift FleetIQ game server instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
