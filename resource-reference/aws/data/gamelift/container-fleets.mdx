---
title: "aws_gamelift_container_fleets"
description: "`aws_gamelift_container_fleets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gamelift_container_fleets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_container_fleets"})

    		ubx.Data(gamelift.ContainerFleets, "example", gamelift.ContainerFleetsConfig{
    			ContainerGroupDefinitionName: "example-container-group-definition-name",
    			Limit:                        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContainerFleets } from "@ubx/sdk-aws/aws/data/gamelift/container_fleets";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_container_fleets" });

      data(ContainerFleets, "example", {
        containerGroupDefinitionName: "example-container-group-definition-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import ContainerFleets, ContainerFleetsConfig

    def describe():
        ubx.intent("look up aws_gamelift_container_fleets")

        ubx.data(ContainerFleets, "example", ContainerFleetsConfig(
            container_group_definition_name="example-container-group-definition-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_group_definition_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="container_fleets" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
