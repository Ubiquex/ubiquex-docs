---
title: "aws_gamelift_game_session_details"
description: "`aws_gamelift_game_session_details` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gamelift_game_session_details` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_game_session_details"})

    		ubx.Data(gamelift.GameSessionDetails, "example", gamelift.GameSessionDetailsConfig{
    			AliasId:       "example",
    			FleetId:       "example",
    			GameSessionId: "example",
    			Limit:         1,
    			Location:      "example",
    			StatusFilter:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GameSessionDetails } from "@ubx/sdk-aws/aws/data/gamelift/game_session_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_game_session_details" });

      data(GameSessionDetails, "example", {
        aliasId: "example",
        fleetId: "example",
        gameSessionId: "example",
        limit: 1,
        location: "example",
        statusFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import GameSessionDetails, GameSessionDetailsConfig

    def describe():
        ubx.intent("look up aws_gamelift_game_session_details")

        ubx.data(GameSessionDetails, "example", GameSessionDetailsConfig(
            alias_id="example",
            fleet_id="example",
            game_session_id="example",
            limit=1,
            location="example",
            status_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alias_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="fleet_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="game_session_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="location" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="game_session_details" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
