---
title: "aws_gamelift_compute_auth_token"
description: "`aws_gamelift_compute_auth_token` reads a real, temporary authentication token for a GameLift Anywhere compute resource to connect to the GameLift service."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_compute_auth_token"})

    		ubx.Data(gamelift.ComputeAuthToken, "example", gamelift.ComputeAuthTokenConfig{
    			ComputeName: "example-compute-name",
    			FleetId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ComputeAuthToken } from "@ubx/sdk-aws/aws/data/gamelift/compute_auth_token";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_compute_auth_token" });

      data(ComputeAuthToken, "example", {
        computeName: "example-compute-name",
        fleetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import ComputeAuthToken, ComputeAuthTokenConfig

    def describe():
        ubx.intent("look up aws_gamelift_compute_auth_token")

        ubx.data(ComputeAuthToken, "example", ComputeAuthTokenConfig(
            compute_name="example-compute-name",
            fleet_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="compute_name" type="string" required>
  The real name of the compute resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fleet_id" type="string" required>
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="auth_token" type="string">
  The real, opaque auth token. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="compute_arn" type="string">
  The real Amazon Resource Name (ARN) of the compute this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="expiration_timestamp" type="string">
  The real timestamp of the expiration. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="fleet_arn" type="string">
  The real Amazon Resource Name (ARN) of the fleet this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
