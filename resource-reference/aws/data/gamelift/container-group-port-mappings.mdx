---
title: "aws_gamelift_container_group_port_mappings"
description: "`aws_gamelift_container_group_port_mappings` reads the real port mappings configured for a GameLift container group definition."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_container_group_port_mappings"})

    		ubx.Data(gamelift.ContainerGroupPortMappings, "example", gamelift.ContainerGroupPortMappingsConfig{
    			ContainerGroupType: "example",
    			FleetId:            "example",
    			ContainerName:      "example-container-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ContainerGroupPortMappings } from "@ubx/sdk-aws/aws/data/gamelift/container_group_port_mappings";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_container_group_port_mappings" });

      data(ContainerGroupPortMappings, "example", {
        containerGroupType: "example",
        fleetId: "example",
        containerName: "example-container-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import ContainerGroupPortMappings, ContainerGroupPortMappingsConfig

    def describe():
        ubx.intent("look up aws_gamelift_container_group_port_mappings")

        ubx.data(ContainerGroupPortMappings, "example", ContainerGroupPortMappingsConfig(
            container_group_type="example",
            fleet_id="example",
            container_name="example-container-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="container_group_type" type="string" required>
  The real, discriminating type of the container group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="container_name" type="string">
  The real name of the container this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="fleet_id" type="string" required>
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="compute_name" type="string">
  The real name of the compute resource this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="container_group_definition_arn" type="string">
  The real Amazon Resource Name (ARN) of the container group definition this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="container_group_port_mappings" type="list(object)">
  The real port mappings for this GameLift container group. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="fleet_arn" type="string">
  The real Amazon Resource Name (ARN) of the fleet this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="instance_id" type="string">
  The real, unique identifier of the instance this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  The real location this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
