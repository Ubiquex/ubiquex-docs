---
title: "aws_gamelift_fleet_port_settings"
description: "`aws_gamelift_fleet_port_settings` reads the real inbound port permissions configured for a GameLift fleet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_fleet_port_settings"})

    		ubx.Data(gamelift.FleetPortSettings, "example", gamelift.FleetPortSettingsConfig{
    			FleetId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FleetPortSettings } from "@ubx/sdk-aws/aws/data/gamelift/fleet_port_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_fleet_port_settings" });

      data(FleetPortSettings, "example", {
        fleetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import FleetPortSettings, FleetPortSettingsConfig

    def describe():
        ubx.intent("look up aws_gamelift_fleet_port_settings")

        ubx.data(FleetPortSettings, "example", FleetPortSettingsConfig(
            fleet_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_id" type="string" required>
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="fleet_arn" type="string">
  The real Amazon Resource Name (ARN) of the fleet this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="inbound_permissions" type="list(object)">
  The real inbound permissions configured for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  The real location this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="update_status" type="string">
  The real, current status of the update this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
