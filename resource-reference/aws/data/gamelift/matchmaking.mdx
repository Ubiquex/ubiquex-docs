---
title: "aws_gamelift_matchmaking"
description: "`aws_gamelift_matchmaking` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gamelift_matchmaking` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_matchmaking"})

    		ubx.Data(gamelift.Matchmaking, "example", gamelift.MatchmakingConfig{
    			TicketIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Matchmaking } from "@ubx/sdk-aws/aws/data/gamelift/matchmaking";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_matchmaking" });

      data(Matchmaking, "example", {
        ticketIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import Matchmaking, MatchmakingConfig

    def describe():
        ubx.intent("look up aws_gamelift_matchmaking")

        ubx.data(Matchmaking, "example", MatchmakingConfig(
            ticket_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ticket_ids" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ticket_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
