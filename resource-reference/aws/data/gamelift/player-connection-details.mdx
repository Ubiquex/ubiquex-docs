---
title: "aws_gamelift_player_connection_details"
description: "`aws_gamelift_player_connection_details` reads the real connection details for placing a player into a GameLift Realtime Servers session."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_player_connection_details"})

    		ubx.Data(gamelift.PlayerConnectionDetails, "example", gamelift.PlayerConnectionDetailsConfig{
    			GameSessionId: "example",
    			PlayerIds:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PlayerConnectionDetails } from "@ubx/sdk-aws/aws/data/gamelift/player_connection_details";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_player_connection_details" });

      data(PlayerConnectionDetails, "example", {
        gameSessionId: "example",
        playerIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import PlayerConnectionDetails, PlayerConnectionDetailsConfig

    def describe():
        ubx.intent("look up aws_gamelift_player_connection_details")

        ubx.data(PlayerConnectionDetails, "example", PlayerConnectionDetailsConfig(
            game_session_id="example",
            player_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="game_session_id" type="string" required>
  The real, unique identifier of the game session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="player_ids" type="list(string)" required>
  The real, unique identifiers of the players this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="player_connection_details" type="list(object)">
  The real, detailed information about the player connection. **(AI-inferred)** Returned by the lookup.
</ResponseField>
