---
title: "aws_gamelift_game_session_log_url"
description: "`aws_gamelift_game_session_log_url` reads a real, generated download URL for a GameLift game session's own server logs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_game_session_log_url"})

    		ubx.Data(gamelift.GameSessionLogUrl, "example", gamelift.GameSessionLogUrlConfig{
    			GameSessionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { GameSessionLogUrl } from "./local-sdk/aws/sdk/typescript/aws/data/gamelift/game_session_log_url.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_game_session_log_url" });

      data(GameSessionLogUrl, "example", {
        gameSessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import GameSessionLogUrl, GameSessionLogUrlConfig

    def describe():
        ubx.intent("look up aws_gamelift_game_session_log_url")

        ubx.data(GameSessionLogUrl, "example", GameSessionLogUrlConfig(
            game_session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="game_session_id" type="string" required>
  The real, unique identifier of the game session this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pre_signed_url" type="string">
  The real, resolvable URL of the pre signed this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
