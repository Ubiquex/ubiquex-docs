---
title: "aws_gamelift_game_session_log_url"
description: "`aws_gamelift_game_session_log_url` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_gamelift_game_session_log_url` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_game_session_log_url"})

    		ubx.Data(gamelift.GameSessionLogUrl, "example", gamelift.GameSessionLogUrlConfig{
    			GameSessionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GameSessionLogUrl } from "@ubx/sdk-aws/aws/data/gamelift/game_session_log_url";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_game_session_log_url" });

      data(GameSessionLogUrl, "example", {
        gameSessionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import GameSessionLogUrl, GameSessionLogUrlConfig

    def describe():
        ubx.intent("look up aws_gamelift_game_session_log_url")

        ubx.data(GameSessionLogUrl, "example", GameSessionLogUrlConfig(
            game_session_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="game_session_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="pre_signed_url" type="string">
  Returned by the lookup.
</ResponseField>
