---
title: "aws_gamelift_scaling_policies"
description: "`aws_gamelift_scaling_policies` looks up a real auto-scaling policy configured on a GameLift fleet."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_gamelift_scaling_policies"})

    		ubx.Data(gamelift.ScalingPolicies, "example", gamelift.ScalingPoliciesConfig{
    			FleetId:      "example",
    			Limit:        1,
    			Location:     "example",
    			StatusFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScalingPolicies } from "@ubx/sdk-aws/aws/data/gamelift/scaling_policies";

    export default stack("example", () => {
      intent({ summary: "look up aws_gamelift_scaling_policies" });

      data(ScalingPolicies, "example", {
        fleetId: "example",
        limit: 1,
        location: "example",
        statusFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.gamelift import ScalingPolicies, ScalingPoliciesConfig

    def describe():
        ubx.intent("look up aws_gamelift_scaling_policies")

        ubx.data(ScalingPolicies, "example", ScalingPoliciesConfig(
            fleet_id="example",
            limit=1,
            location="example",
            status_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="fleet_id" type="string" required>
  The real, unique identifier of the fleet this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="location" type="string">
  The real location this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status_filter" type="string">
  Restricts results to objects matching this status. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="scaling_policies" type="list(object)">
  The real scaling policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
