---
title: "aws_identitystore_group_memberships_for_member"
description: "`aws_identitystore_group_memberships_for_member` reads the real group memberships of a given member in an Identity Store."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identitystore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/identitystore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_identitystore_group_memberships_for_member"})

    		ubx.Data(identitystore.GroupMembershipsForMember, "example", identitystore.GroupMembershipsForMemberConfig{
    			IdentityStoreId: "example",
    			MemberId:        map[string]any{"user_id": "example"},
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupMembershipsForMember } from "@ubx/sdk-aws/aws/data/identitystore/group_memberships_for_member";

    export default stack("example", () => {
      intent({ summary: "look up aws_identitystore_group_memberships_for_member" });

      data(GroupMembershipsForMember, "example", {
        identityStoreId: "example",
        memberId: { userId: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.identitystore import GroupMembershipsForMember, GroupMembershipsForMemberConfig
    from ubx.aws.data.identitystore.group_memberships_for_member import GroupMembershipsForMember_GroupMemberships_MemberId

    def describe():
        ubx.intent("look up aws_identitystore_group_memberships_for_member")

        ubx.data(GroupMembershipsForMember, "example", GroupMembershipsForMemberConfig(
            identity_store_id="example",
            member_id=GroupMembershipsForMember_GroupMemberships_MemberId(user_id="example"),
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="identity_store_id" type="string" required>
  The real, unique identifier of the IAM Identity Center identity store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="member_id" type="object" required>
  &lt;p&gt;An object containing the identifier of a group member.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="user_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="group_memberships" type="list(object)">
  The real group memberships included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
