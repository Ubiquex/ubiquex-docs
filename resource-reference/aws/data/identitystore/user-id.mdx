---
title: "aws_identitystore_user_id"
description: "`aws_identitystore_user_id` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_identitystore_user_id` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identitystore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/identitystore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_identitystore_user_id"})

    		ubx.Data(identitystore.UserId, "example", identitystore.UserIdConfig{
    			AlternateIdentifier: map[string]any{"external_id": map[string]any{"id": "example"}},
    			IdentityStoreId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserId } from "@ubx/sdk-aws/aws/data/identitystore/user_id";

    export default stack("example", () => {
      intent({ summary: "look up aws_identitystore_user_id" });

      data(UserId, "example", {
        alternateIdentifier: { externalId: { id: "example" } },
        identityStoreId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.identitystore import UserId, UserIdConfig

    def describe():
        ubx.intent("look up aws_identitystore_user_id")

        ubx.data(UserId, "example", UserIdConfig(
            alternate_identifier={"external_id": {"id": "example"}},
            identity_store_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alternate_identifier" type="object" required>
  &lt;p&gt;A unique identifier for a user or group that is not the primary identifier. This value can be an identifier from an external identity provider (IdP) that is associated with the user, the group, or a unique attribute.&lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="external_id" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="issuer" type="string">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="unique_attribute" type="object">
      Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="attribute_path" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="attribute_value" type="any">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="user_id" type="string">
  Returned by the lookup.
</ResponseField>
