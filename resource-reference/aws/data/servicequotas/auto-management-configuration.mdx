---
title: "aws_servicequotas_auto_management_configuration"
description: "`aws_servicequotas_auto_management_configuration` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_servicequotas_auto_management_configuration` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicequotas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/servicequotas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_servicequotas_auto_management_configuration"})

    		ubx.Data(servicequotas.AutoManagementConfiguration, "example", servicequotas.AutoManagementConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutoManagementConfiguration } from "@ubx/sdk-aws/aws/data/servicequotas/auto_management_configuration";

    export default stack("example", () => {
      intent({
        summary: "look up aws_servicequotas_auto_management_configuration",
      });

      data(AutoManagementConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.servicequotas import AutoManagementConfiguration, AutoManagementConfigurationConfig

    def describe():
        ubx.intent("look up aws_servicequotas_auto_management_configuration")

        ubx.data(AutoManagementConfiguration, "example", AutoManagementConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="exclusion_list" type="map(list(object))">
  Returned by the lookup.
</ResponseField>

<ResponseField name="notification_arn" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="opt_in_level" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="opt_in_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="opt_in_type" type="string">
  Returned by the lookup.
</ResponseField>
