---
title: "aws_lambda_account_settings"
description: "`aws_lambda_account_settings` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_lambda_account_settings` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_lambda_account_settings"})

    		ubx.Data(lambda.AccountSettings, "example", lambda.AccountSettingsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountSettings } from "@ubx/sdk-aws/aws/data/lambda/account_settings";

    export default stack("example", () => {
      intent({ summary: "look up aws_lambda_account_settings" });

      data(AccountSettings, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.lambda_ import AccountSettings, AccountSettingsConfig

    def describe():
        ubx.intent("look up aws_lambda_account_settings")

        ubx.data(AccountSettings, "example", AccountSettingsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="account_limit" type="object">
  &lt;p&gt;Limits that are related to concurrency and storage. All file and storage sizes are in bytes.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="code_size_unzipped" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="code_size_zipped" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="concurrent_executions" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_code_size" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="unreserved_concurrent_executions" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="account_usage" type="object">
  &lt;p&gt;The number of functions and amount of storage in use.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="function_count" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_code_size" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
