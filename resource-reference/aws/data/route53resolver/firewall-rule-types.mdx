---
title: "aws_route53resolver_firewall_rule_types"
description: "`aws_route53resolver_firewall_rule_types` reads the real, available rule types for a Route 53 Resolver DNS Firewall rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_rule_types"})

    		ubx.Data(route53resolver.FirewallRuleTypes, "example", route53resolver.FirewallRuleTypesConfig{
    			MaxResults: 1,
    			RuleType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallRuleTypes } from "@ubx/sdk-aws/aws/data/route53resolver/firewall_rule_types";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_rule_types" });

      data(FirewallRuleTypes, "example", {
        maxResults: 1,
        ruleType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallRuleTypes, FirewallRuleTypesConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_rule_types")

        ubx.data(FirewallRuleTypes, "example", FirewallRuleTypesConfig(
            max_results=1,
            rule_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="rule_type" type="string">
  &lt;p&gt;The variant name of a rule type. Known values are &lt;code&gt;FirewallAdvancedContentCategory&lt;/code&gt;, &lt;code&gt;FirewallAdvancedThreatCategory&lt;/code&gt;, &lt;code&gt;DnsThreatProtection&lt;/code&gt;, and &lt;code&gt;PartnerThreatProtection&lt;/code&gt;. Returned in the &lt;code&gt;RuleType&lt;/code&gt; field of a &lt;a&gt;FirewallRuleTypeDefinition&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_rule_types" type="list(object)">
  The real, discriminating types of firewall rules this request is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
