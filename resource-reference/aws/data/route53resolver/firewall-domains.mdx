---
title: "aws_route53resolver_firewall_domains"
description: "`aws_route53resolver_firewall_domains` reads the real domains within a Route 53 Resolver DNS Firewall domain list."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_domains"})

    		ubx.Data(route53resolver.FirewallDomains, "example", route53resolver.FirewallDomainsConfig{
    			FirewallDomainListId: "example",
    			MaxResults:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallDomains } from "@ubx/sdk-aws/aws/data/route53resolver/firewall_domains";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_domains" });

      data(FirewallDomains, "example", {
        firewallDomainListId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallDomains, FirewallDomainsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_domains")

        ubx.data(FirewallDomains, "example", FirewallDomainsConfig(
            firewall_domain_list_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_domain_list_id" type="string" required>
  The real, unique identifier of the firewall domain list this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="domains" type="list(string)">
  The real domains included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
