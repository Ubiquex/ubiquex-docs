---
title: "aws_route53resolver_firewall_rules"
description: "`aws_route53resolver_firewall_rules` reads the real rules within a Route 53 Resolver DNS Firewall rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_rules"})

    		ubx.Data(route53resolver.FirewallRules, "example", route53resolver.FirewallRulesConfig{
    			FirewallRuleGroupId: "example",
    			Action:              "example",
    			MaxResults:          1,
    			Priority:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FirewallRules } from "./local-sdk/aws/sdk/typescript/aws/data/route53resolver/firewall_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_rules" });

      data(FirewallRules, "example", {
        firewallRuleGroupId: "example",
        action: "example",
        maxResults: 1,
        priority: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallRules, FirewallRulesConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_rules")

        ubx.data(FirewallRules, "example", FirewallRulesConfig(
            firewall_rule_group_id="example",
            action="example",
            max_results=1,
            priority=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action" type="string">
  The real action this object describes. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="firewall_rule_group_id" type="string" required>
  The real, unique identifier of the firewall rule group this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="priority" type="number">
  The real priority of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_rules" type="list(object)">
  The real rules for the firewall. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
