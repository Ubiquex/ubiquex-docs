---
title: "aws_route53resolver_resolver_rule_policy"
description: "`data_aws_route53resolver_resolver_rule_policy` looks up a real, already-existing `aws_route53_resolver_resolver_rule`."
---

It defines a real forwarding rule that sends DNS queries for a specific domain out through an `aws_route53_resolver_resolver_endpoint` to a specified on-premises or other DNS server, rather than resolving them through Route 53's own default resolution.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_rule_policy"})

    		ubx.Data(route53resolver.ResolverRulePolicy, "example", route53resolver.ResolverRulePolicyConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResolverRulePolicy } from "./local-sdk/aws/sdk/typescript/aws/data/route53resolver/resolver_rule_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_rule_policy" });

      data(ResolverRulePolicy, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverRulePolicy, ResolverRulePolicyConfig

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_rule_policy")

        ubx.data(ResolverRulePolicy, "example", ResolverRulePolicyConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resolver_rule_policy" type="string">
  The real resolver rule policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
