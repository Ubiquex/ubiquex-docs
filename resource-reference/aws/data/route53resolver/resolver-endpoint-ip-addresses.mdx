---
title: "aws_route53resolver_resolver_endpoint_ip_addresses"
description: "`data_aws_route53resolver_resolver_endpoint_ip_addresses` looks up a real, already-existing `aws_route53_resolver_resolver_endpoint`."
---

It creates a real inbound or outbound DNS resolver endpoint in a VPC, the resource that lets on-premises DNS resolvers forward queries into that VPC, or lets the VPC forward queries out to on-premises DNS servers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_endpoint_ip_addresses"})

    		ubx.Data(route53resolver.ResolverEndpointIpAddresses, "example", route53resolver.ResolverEndpointIpAddressesConfig{
    			ResolverEndpointId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverEndpointIpAddresses } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_endpoint_ip_addresses";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53resolver_resolver_endpoint_ip_addresses",
      });

      data(ResolverEndpointIpAddresses, "example", {
        resolverEndpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverEndpointIpAddresses, ResolverEndpointIpAddressesConfig

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_endpoint_ip_addresses")

        ubx.data(ResolverEndpointIpAddresses, "example", ResolverEndpointIpAddressesConfig(
            resolver_endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resolver_endpoint_id" type="string" required>
  The real, unique identifier of the resolver endpoint this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ip_addresses" type="list(object)">
  The real IP addresses included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
