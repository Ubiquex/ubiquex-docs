---
title: "aws_route53resolver_resolver_query_log_config_associations"
description: "`data_aws_route53resolver_resolver_query_log_config_associations` looks up a real, already-existing..."
---

`data_aws_route53resolver_resolver_query_log_config_associations` looks up a real, already-existing `aws_route53_resolver_resolver_query_logging_config_association`. It associates a VPC with an `aws_route53_resolver_resolver_query_logging_config`, the resource that actually turns query logging on for that specific VPC's DNS traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_query_log_config_associations"})

    		ubx.Data(route53resolver.ResolverQueryLogConfigAssociations, "example", route53resolver.ResolverQueryLogConfigAssociationsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			SortBy:     "example",
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverQueryLogConfigAssociations } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_query_log_config_associations";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_route53resolver_resolver_query_log_config_associations",
      });

      data(ResolverQueryLogConfigAssociations, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverQueryLogConfigAssociations, ResolverQueryLogConfigAssociationsConfig
    from ubx.aws.data.route53resolver.resolver_query_log_config_associations import ResolverQueryLogConfigAssociations_Filters

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_query_log_config_associations")

        ubx.data(ResolverQueryLogConfigAssociations, "example", ResolverQueryLogConfigAssociationsConfig(
            filters=[ResolverQueryLogConfigAssociations_Filters(name="example-name")],
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolver_query_log_config_associations" type="list(object)">
  The real Route 53 Resolver query log configuration associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The real count of totals. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_filtered_count" type="number">
  The real count of total filtereds. **(AI-inferred)** Returned by the lookup.
</ResponseField>
