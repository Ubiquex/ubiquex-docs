---
title: "aws_route53resolver_resolver_endpoints"
description: "`aws_route53resolver_resolver_endpoints` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53resolver_resolver_endpoints` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_endpoints"})

    		ubx.Data(route53resolver.ResolverEndpoints, "example", route53resolver.ResolverEndpointsConfig{
    			Filters: []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverEndpoints } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_endpoints";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_endpoints" });

      data(ResolverEndpoints, "example", {
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverEndpoints, ResolverEndpointsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_endpoints")

        ubx.data(ResolverEndpoints, "example", ResolverEndpointsConfig(
            filters=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_results" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="resolver_endpoints" type="list(object)">
  Returned by the lookup.
</ResponseField>
