---
title: "aws_route53resolver_firewall_configs"
description: "`aws_route53resolver_firewall_configs` reads the real DNS Firewall configurations on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_configs"})

    		ubx.Data(route53resolver.FirewallConfigs, "example", route53resolver.FirewallConfigsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallConfigs } from "@ubx/sdk-aws/aws/data/route53resolver/firewall_configs";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_configs" });

      data(FirewallConfigs, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallConfigs, FirewallConfigsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_configs")

        ubx.data(FirewallConfigs, "example", FirewallConfigsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_configs" type="list(object)">
  The real firewall configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
