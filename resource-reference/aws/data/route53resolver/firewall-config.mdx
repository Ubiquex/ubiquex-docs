---
title: "aws_route53resolver_firewall_config"
description: "`aws_route53resolver_firewall_config` reads the real DNS Firewall configuration of a VPC."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_config"})

    		ubx.Data(route53resolver.FirewallConfig, "example", route53resolver.FirewallConfigConfig{
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FirewallConfig } from "./local-sdk/aws/sdk/typescript/aws/data/route53resolver/firewall_config.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_config" });

      data(FirewallConfig, "example", {
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallConfig, FirewallConfigConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_config")

        ubx.data(FirewallConfig, "example", FirewallConfigConfig(
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_config" type="object">
  &lt;p&gt;Configuration of the firewall behavior provided by DNS Firewall for a single VPC from Amazon Virtual Private Cloud (Amazon VPC). &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="firewall_fail_open" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="owner_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
