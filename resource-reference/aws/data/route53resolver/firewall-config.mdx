---
title: "aws_route53resolver_firewall_config"
description: "`aws_route53resolver_firewall_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53resolver_firewall_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_config"})

    		ubx.Data(route53resolver.FirewallConfig, "example", route53resolver.FirewallConfigConfig{
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallConfig } from "@ubx/sdk-aws/aws/data/route53resolver/firewall_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_config" });

      data(FirewallConfig, "example", {
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallConfig, FirewallConfigConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_config")

        ubx.data(FirewallConfig, "example", FirewallConfigConfig(
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_config" type="object">
  &lt;p&gt;Configuration of the firewall behavior provided by DNS Firewall for a single VPC from Amazon Virtual Private Cloud (Amazon VPC). &lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="firewall_fail_open" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="owner_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
