---
title: "aws_route53resolver_resolver_rule_associations"
description: "`data_aws_route53resolver_resolver_rule_associations` looks up a real, already-existing `aws_route53_resolver_resolver_rule_association`."
---

It associates a real `aws_route53_resolver_resolver_rule` with a specific VPC, the resource that actually turns the rule's forwarding behavior on for that VPC's DNS queries.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_rule_associations"})

    		ubx.Data(route53resolver.ResolverRuleAssociations, "example", route53resolver.ResolverRuleAssociationsConfig{
    			Filters: []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverRuleAssociations } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_rule_associations";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_rule_associations" });

      data(ResolverRuleAssociations, "example", {
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverRuleAssociations, ResolverRuleAssociationsConfig
    from ubx.aws.data.route53resolver.resolver_rule_associations import ResolverRuleAssociations_Filters

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_rule_associations")

        ubx.data(ResolverRuleAssociations, "example", ResolverRuleAssociationsConfig(
            filters=[ResolverRuleAssociations_Filters(name="example-name")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolver_rule_associations" type="list(object)">
  The real Route 53 Resolver rule associations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
