---
title: "aws_route53resolver_resolver_config"
description: "`data_aws_route53resolver_resolver_config` looks up a real, already-existing `aws_route53_resolver_resolver_config`."
---

It configures a real, account-level Resolver setting, most commonly whether autodefined DNS rules for the AWS-provided DNS in a VPC are enabled, distinct from the per-endpoint settings on an `aws_route53_resolver_resolver_endpoint`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_config"})

    		ubx.Data(route53resolver.ResolverConfig, "example", route53resolver.ResolverConfigConfig{
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverConfig } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_config" });

      data(ResolverConfig, "example", {
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverConfig, ResolverConfigConfig

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_config")

        ubx.data(ResolverConfig, "example", ResolverConfigConfig(
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resolver_config" type="object">
  &lt;p&gt;A complex type that contains information about a Resolver configuration for a VPC.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="autodefined_reverse" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="owner_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
