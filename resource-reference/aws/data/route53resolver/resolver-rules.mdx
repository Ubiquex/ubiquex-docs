---
title: "aws_route53resolver_resolver_rules"
description: "`data_aws_route53resolver_resolver_rules` looks up a real, already-existing `aws_route53_resolver_resolver_rule`."
---

It defines a real forwarding rule that sends DNS queries for a specific domain out through an `aws_route53_resolver_resolver_endpoint` to a specified on-premises or other DNS server, rather than resolving them through Route 53's own default resolution.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_rules"})

    		ubx.Data(route53resolver.ResolverRules, "example", route53resolver.ResolverRulesConfig{
    			Filters: []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverRules } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_rules";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_rules" });

      data(ResolverRules, "example", {
        filters: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverRules, ResolverRulesConfig
    from ubx.aws.data.route53resolver.resolver_rules import ResolverRules_Filters

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_rules")

        ubx.data(ResolverRules, "example", ResolverRulesConfig(
            filters=[ResolverRules_Filters(name="example-name")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolver_rules" type="list(object)">
  The real rules for the resolver. **(AI-inferred)** Returned by the lookup.
</ResponseField>
