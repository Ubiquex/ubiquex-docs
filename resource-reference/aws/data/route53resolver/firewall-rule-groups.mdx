---
title: "aws_route53resolver_firewall_rule_groups"
description: "`aws_route53resolver_firewall_rule_groups` looks up a real Route 53 Resolver DNS Firewall rule group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_rule_groups"})

    		ubx.Data(route53resolver.FirewallRuleGroups, "example", route53resolver.FirewallRuleGroupsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallRuleGroups } from "@ubx/sdk-aws/aws/data/route53resolver/firewall_rule_groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_rule_groups" });

      data(FirewallRuleGroups, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallRuleGroups, FirewallRuleGroupsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_rule_groups")

        ubx.data(FirewallRuleGroups, "example", FirewallRuleGroupsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_rule_groups" type="list(object)">
  The real firewall rule groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
