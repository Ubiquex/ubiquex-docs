---
title: "aws_route53resolver_resolver_configs"
description: "`data_aws_route53resolver_resolver_configs` looks up a real, already-existing `aws_route53_resolver_resolver_config`."
---

It configures a real, account-level Resolver setting, most commonly whether autodefined DNS rules for the AWS-provided DNS in a VPC are enabled, distinct from the per-endpoint settings on an `aws_route53_resolver_resolver_endpoint`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_configs"})

    		ubx.Data(route53resolver.ResolverConfigs, "example", route53resolver.ResolverConfigsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResolverConfigs } from "@ubx/sdk-aws/aws/data/route53resolver/resolver_configs";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_configs" });

      data(ResolverConfigs, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverConfigs, ResolverConfigsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_configs")

        ubx.data(ResolverConfigs, "example", ResolverConfigsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolver_configs" type="list(object)">
  The real Route 53 Resolver configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
