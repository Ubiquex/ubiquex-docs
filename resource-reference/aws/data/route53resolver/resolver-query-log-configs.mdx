---
title: "aws_route53resolver_resolver_query_log_configs"
description: "`data_aws_route53resolver_resolver_query_log_configs` looks up a real, already-existing `aws_route53_resolver_resolver_query_logging_config`."
---

It configures real logging of DNS queries resolved through an `aws_route53_resolver_resolver_endpoint` or a VPC's default resolver, to a CloudWatch Logs group, S3 bucket, or Kinesis Data Firehose.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_resolver_query_log_configs"})

    		ubx.Data(route53resolver.ResolverQueryLogConfigs, "example", route53resolver.ResolverQueryLogConfigsConfig{
    			Filters:    []map[string]any{{"name": "example-name"}},
    			MaxResults: 1,
    			SortBy:     "example",
    			SortOrder:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResolverQueryLogConfigs } from "./local-sdk/aws/sdk/typescript/aws/data/route53resolver/resolver_query_log_configs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_resolver_query_log_configs" });

      data(ResolverQueryLogConfigs, "example", {
        filters: [{ name: "example-name" }],
        maxResults: 1,
        sortBy: "example",
        sortOrder: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import ResolverQueryLogConfigs, ResolverQueryLogConfigsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_resolver_query_log_configs")

        ubx.data(ResolverQueryLogConfigs, "example", ResolverQueryLogConfigsConfig(
            filters=[{"name": "example-name"}],
            max_results=1,
            sort_by="example",
            sort_order="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="list(object)">
  Real filter criteria restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_by" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  The real order (ascending or descending) results are sorted in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resolver_query_log_configs" type="list(object)">
  The real Route 53 Resolver query log configurations included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The real count of totals. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_filtered_count" type="number">
  The real count of total filtereds. **(AI-inferred)** Returned by the lookup.
</ResponseField>
