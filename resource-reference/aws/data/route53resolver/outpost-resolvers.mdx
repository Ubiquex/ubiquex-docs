---
title: "aws_route53resolver_outpost_resolvers"
description: "`aws_route53resolver_outpost_resolvers` looks up a real Route 53 Resolver on an AWS Outpost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_outpost_resolvers"})

    		ubx.Data(route53resolver.OutpostResolvers, "example", route53resolver.OutpostResolversConfig{
    			MaxResults: 1,
    			OutpostArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OutpostResolvers } from "@ubx/sdk-aws/aws/data/route53resolver/outpost_resolvers";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_outpost_resolvers" });

      data(OutpostResolvers, "example", {
        maxResults: 1,
        outpostArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import OutpostResolvers, OutpostResolversConfig

    def describe():
        ubx.intent("look up aws_route53resolver_outpost_resolvers")

        ubx.data(OutpostResolvers, "example", OutpostResolversConfig(
            max_results=1,
            outpost_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  The real Amazon Resource Name (ARN) of the outpost this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="outpost_resolvers" type="list(object)">
  The real Route 53 Resolver on Outposts resolvers included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
