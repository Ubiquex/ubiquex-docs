---
title: "aws_route53resolver_firewall_rule_group_associations"
description: "`aws_route53resolver_firewall_rule_group_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_route53resolver_firewall_rule_group_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_rule_group_associations"})

    		ubx.Data(route53resolver.FirewallRuleGroupAssociations, "example", route53resolver.FirewallRuleGroupAssociationsConfig{
    			FirewallRuleGroupId: "example",
    			MaxResults:          1,
    			Priority:            1,
    			Status:              "example",
    			VpcId:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FirewallRuleGroupAssociations } from "@ubx/sdk-aws/aws/data/route53resolver/firewall_rule_group_associations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_route53resolver_firewall_rule_group_associations",
      });

      data(FirewallRuleGroupAssociations, "example", {
        firewallRuleGroupId: "example",
        maxResults: 1,
        priority: 1,
        status: "example",
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallRuleGroupAssociations, FirewallRuleGroupAssociationsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_rule_group_associations")

        ubx.data(FirewallRuleGroupAssociations, "example", FirewallRuleGroupAssociationsConfig(
            firewall_rule_group_id="example",
            max_results=1,
            priority=1,
            status="example",
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="firewall_rule_group_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="priority" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_rule_group_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
