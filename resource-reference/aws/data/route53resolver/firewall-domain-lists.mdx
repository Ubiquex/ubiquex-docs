---
title: "aws_route53resolver_firewall_domain_lists"
description: "`aws_route53resolver_firewall_domain_lists` looks up a real Route 53 Resolver DNS Firewall domain list."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	route53resolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53resolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53resolver_firewall_domain_lists"})

    		ubx.Data(route53resolver.FirewallDomainLists, "example", route53resolver.FirewallDomainListsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { FirewallDomainLists } from "./local-sdk/aws/sdk/typescript/aws/data/route53resolver/firewall_domain_lists.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53resolver_firewall_domain_lists" });

      data(FirewallDomainLists, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.route53resolver import FirewallDomainLists, FirewallDomainListsConfig

    def describe():
        ubx.intent("look up aws_route53resolver_firewall_domain_lists")

        ubx.data(FirewallDomainLists, "example", FirewallDomainListsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="firewall_domain_lists" type="list(object)">
  The real Route 53 Resolver DNS Firewall domain lists included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
