---
title: "aws_ssm_sap_database"
description: "`aws_ssm_sap_database` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_sap_database` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_sap "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_sap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_sap_database"})

    		ubx.Data(ssm_sap.SsmSapDatabase, "example", ssm_sap.SsmSapDatabaseConfig{
    			ApplicationId: "example",
    			ComponentId:   "example",
    			DatabaseArn:   "arn:aws:iam::123456789012:policy/example",
    			DatabaseId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmSapDatabase } from "@ubx/sdk-aws/aws/data/ssm_sap/ssm_sap_database";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_sap_database" });

      data(SsmSapDatabase, "example", {
        applicationId: "example",
        componentId: "example",
        databaseArn: "arn:aws:iam::123456789012:policy/example",
        databaseId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_sap import SsmSapDatabase, SsmSapDatabaseConfig

    def describe():
        ubx.intent("look up aws_ssm_sap_database")

        ubx.data(SsmSapDatabase, "example", SsmSapDatabaseConfig(
            application_id="example",
            component_id="example",
            database_arn="arn:aws:iam::123456789012:policy/example",
            database_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="component_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="database" type="object">
  &lt;p&gt;The SAP HANA database of the application registered with AWS Systems Manager for SAP.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="application_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="arn" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="component_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="connected_component_arns" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="credentials" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="database_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="database_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="database_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="last_updated" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="primary_host" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="sqlport" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
