---
title: "aws_ssm_sap_databases"
description: "`aws_ssm_sap_databases` reads the real databases within an SAP application registered with Systems Manager for SAP."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm_sap "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_sap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_sap_databases"})

    		ubx.Data(ssm_sap.SsmSapDatabases, "example", ssm_sap.SsmSapDatabasesConfig{
    			ApplicationId: "example",
    			ComponentId:   "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SsmSapDatabases } from "./local-sdk/aws/sdk/typescript/aws/data/ssm_sap/ssm_sap_databases.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_sap_databases" });

      data(SsmSapDatabases, "example", {
        applicationId: "example",
        componentId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_sap import SsmSapDatabases, SsmSapDatabasesConfig

    def describe():
        ubx.intent("look up aws_ssm_sap_databases")

        ubx.data(SsmSapDatabases, "example", SsmSapDatabasesConfig(
            application_id="example",
            component_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="application_id" type="string">
  The real, unique identifier of the application this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="component_id" type="string">
  The real, unique identifier of the component this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="databases" type="list(object)">
  The real databases included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
