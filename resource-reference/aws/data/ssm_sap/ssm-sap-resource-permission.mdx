---
title: "aws_ssm_sap_resource_permission"
description: "`aws_ssm_sap_resource_permission` reads the real resource-based permission attached to a Systems Manager for SAP application."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ssm_sap "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_sap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_sap_resource_permission"})

    		ubx.Data(ssm_sap.SsmSapResourcePermission, "example", ssm_sap.SsmSapResourcePermissionConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			ActionType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SsmSapResourcePermission } from "./local-sdk/aws/sdk/typescript/aws/data/ssm_sap/ssm_sap_resource_permission.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_sap_resource_permission" });

      data(SsmSapResourcePermission, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        actionType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_sap import SsmSapResourcePermission, SsmSapResourcePermissionConfig

    def describe():
        ubx.intent("look up aws_ssm_sap_resource_permission")

        ubx.data(SsmSapResourcePermission, "example", SsmSapResourcePermissionConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            action_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_type" type="string">
  The real type of this action. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
