---
title: "aws_ssm_sap_resource_permission"
description: "`aws_ssm_sap_resource_permission` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_sap_resource_permission` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_sap "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_sap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_sap_resource_permission"})

    		ubx.Data(ssm_sap.SsmSapResourcePermission, "example", ssm_sap.SsmSapResourcePermissionConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    			ActionType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmSapResourcePermission } from "@ubx/sdk-aws/aws/data/ssm_sap/ssm_sap_resource_permission";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_sap_resource_permission" });

      data(SsmSapResourcePermission, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
        actionType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_sap import SsmSapResourcePermission, SsmSapResourcePermissionConfig

    def describe():
        ubx.intent("look up aws_ssm_sap_resource_permission")

        ubx.data(SsmSapResourcePermission, "example", SsmSapResourcePermissionConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
            action_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="action_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>
