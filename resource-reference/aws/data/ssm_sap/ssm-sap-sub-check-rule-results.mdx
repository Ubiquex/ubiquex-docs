---
title: "aws_ssm_sap_sub_check_rule_results"
description: "`aws_ssm_sap_sub_check_rule_results` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ssm_sap_sub_check_rule_results` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm_sap "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ssm_sap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ssm_sap_sub_check_rule_results"})

    		ubx.Data(ssm_sap.SsmSapSubCheckRuleResults, "example", ssm_sap.SsmSapSubCheckRuleResultsConfig{
    			SubCheckResultId: "example",
    			MaxResults:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SsmSapSubCheckRuleResults } from "@ubx/sdk-aws/aws/data/ssm_sap/ssm_sap_sub_check_rule_results";

    export default stack("example", () => {
      intent({ summary: "look up aws_ssm_sap_sub_check_rule_results" });

      data(SsmSapSubCheckRuleResults, "example", {
        subCheckResultId: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ssm_sap import SsmSapSubCheckRuleResults, SsmSapSubCheckRuleResultsConfig

    def describe():
        ubx.intent("look up aws_ssm_sap_sub_check_rule_results")

        ubx.data(SsmSapSubCheckRuleResults, "example", SsmSapSubCheckRuleResultsConfig(
            sub_check_result_id="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sub_check_result_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="rule_results" type="list(object)">
  Returned by the lookup.
</ResponseField>
