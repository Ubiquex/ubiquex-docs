---
title: "aws_elasticloadbalancing_instance_health"
description: "`aws_elasticloadbalancing_instance_health` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elasticloadbalancing_instance_health` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticloadbalancing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elasticloadbalancing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticloadbalancing_instance_health"})

    		ubx.Data(elasticloadbalancing.InstanceHealth, "example", elasticloadbalancing.InstanceHealthConfig{
    			LoadBalancerName: "example-load-balancer-name",
    			Instances:        []map[string]any{{"instance_id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InstanceHealth } from "@ubx/sdk-aws/aws/data/elasticloadbalancing/instance_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticloadbalancing_instance_health" });

      data(InstanceHealth, "example", {
        loadBalancerName: "example-load-balancer-name",
        instances: [{ instanceId: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticloadbalancing import InstanceHealth, InstanceHealthConfig

    def describe():
        ubx.intent("look up aws_elasticloadbalancing_instance_health")

        ubx.data(InstanceHealth, "example", InstanceHealthConfig(
            load_balancer_name="example-load-balancer-name",
            instances=[{"instance_id": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instances" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="load_balancer_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="instance_states" type="list(object)">
  Returned by the lookup.
</ResponseField>
