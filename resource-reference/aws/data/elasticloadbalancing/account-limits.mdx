---
title: "aws_elasticloadbalancing_account_limits"
description: "`aws_elasticloadbalancing_account_limits` reads real, account-level Classic Load Balancer limits and current usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticloadbalancing "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticloadbalancing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticloadbalancing_account_limits"})

    		ubx.Data(elasticloadbalancing.AccountLimits, "example", elasticloadbalancing.AccountLimitsConfig{
    			Marker:   "example",
    			PageSize: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountLimits } from "@ubx/sdk-aws/aws/data/elasticloadbalancing/account_limits";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticloadbalancing_account_limits" });

      data(AccountLimits, "example", {
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticloadbalancing import AccountLimits, AccountLimitsConfig

    def describe():
        ubx.intent("look up aws_elasticloadbalancing_account_limits")

        ubx.data(AccountLimits, "example", AccountLimitsConfig(
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="limits" type="list(object)">
  The real, current limits for this service. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
