---
title: "aws_elasticloadbalancing_load_balancers"
description: "`data_aws_elasticloadbalancing_load_balancers` looks up a real, already-existing `aws_elastic_load_balancing_load_balancer`."
---

It creates a Classic Load Balancer, ELB's original, legacy load balancing resource, layer 4 and basic layer 7 routing, superseded for new deployments by `aws_elastic_load_balancing_v2_load_balancer`'s own Application and Network Load Balancer types.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticloadbalancing "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elasticloadbalancing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elasticloadbalancing_load_balancers"})

    		ubx.Data(elasticloadbalancing.LoadBalancers, "example", elasticloadbalancing.LoadBalancersConfig{
    			LoadBalancerNames: []string{"example"},
    			Marker:            "example",
    			PageSize:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LoadBalancers } from "@ubx/sdk-aws/aws/data/elasticloadbalancing/load_balancers";

    export default stack("example", () => {
      intent({ summary: "look up aws_elasticloadbalancing_load_balancers" });

      data(LoadBalancers, "example", {
        loadBalancerNames: ["example"],
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elasticloadbalancing import LoadBalancers, LoadBalancersConfig

    def describe():
        ubx.intent("look up aws_elasticloadbalancing_load_balancers")

        ubx.data(LoadBalancers, "example", LoadBalancersConfig(
            load_balancer_names=["example"],
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_names" type="list(string)">
  The real names of the load balancers included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="load_balancer_descriptions" type="list(object)">
  The real, detailed load balancer descriptions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
