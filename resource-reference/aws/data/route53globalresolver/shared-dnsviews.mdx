---
title: "aws_route53globalresolver_shared_dnsviews"
description: "`aws_route53globalresolver_shared_dnsviews` reads the real DNS views shared with a Route 53 Global Resolver."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53globalresolver "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/route53globalresolver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_route53globalresolver_shared_dnsviews"})

    		ubx.Data(route53globalresolver.SharedDnsviews, "example", route53globalresolver.SharedDnsviewsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SharedDnsviews } from "@ubx/sdk-aws/aws/data/route53globalresolver/shared_dnsviews";

    export default stack("example", () => {
      intent({ summary: "look up aws_route53globalresolver_shared_dnsviews" });

      data(SharedDnsviews, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.route53globalresolver import SharedDnsviews, SharedDnsviewsConfig

    def describe():
        ubx.intent("look up aws_route53globalresolver_shared_dnsviews")

        ubx.data(SharedDnsviews, "example", SharedDnsviewsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dns_views" type="list(object)">
  The real DNS views included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
