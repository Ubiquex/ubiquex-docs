---
title: "aws_redshift_data_sessions"
description: "`aws_redshift_data_sessions` reads the real, active sessions on a Redshift cluster or workgroup, via the Redshift Data API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_sessions"})

    		ubx.Data(redshift_data.RedshiftDataSessions, "example", redshift_data.RedshiftDataSessionsConfig{
    			ClusterIdentifier: "example",
    			Database:          "example",
    			MaxResults:        1,
    			RoleLevel:         true,
    			SessionId:         "example",
    			Status:            "example",
    			WorkgroupName:     "example-workgroup-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftDataSessions } from "@ubx/sdk-aws/aws/data/redshift_data/redshift_data_sessions";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_sessions" });

      data(RedshiftDataSessions, "example", {
        clusterIdentifier: "example",
        database: "example",
        maxResults: 1,
        roleLevel: true,
        sessionId: "example",
        status: "example",
        workgroupName: "example-workgroup-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_data import RedshiftDataSessions, RedshiftDataSessionsConfig

    def describe():
        ubx.intent("look up aws_redshift_data_sessions")

        ubx.data(RedshiftDataSessions, "example", RedshiftDataSessionsConfig(
            cluster_identifier="example",
            database="example",
            max_results=1,
            role_level=True,
            session_id="example",
            status="example",
            workgroup_name="example-workgroup-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="database" type="string">
  The real database this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="role_level" type="bool">
  The real level of the role. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="session_id" type="string">
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workgroup_name" type="string">
  The real name of the workgroup this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sessions" type="list(object)">
  The real sessions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
