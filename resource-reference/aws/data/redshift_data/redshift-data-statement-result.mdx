---
title: "aws_redshift_data_statement_result"
description: "`aws_redshift_data_statement_result` reads the real result rows of a SQL statement executed via the Redshift Data API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_statement_result"})

    		ubx.Data(redshift_data.RedshiftDataStatementResult, "example", redshift_data.RedshiftDataStatementResultConfig{
    			Id:              "example",
    			WaitTimeSeconds: 7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftDataStatementResult } from "@ubx/sdk-aws/aws/data/redshift_data/redshift_data_statement_result";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_statement_result" });

      data(RedshiftDataStatementResult, "example", {
        id: "example",
        waitTimeSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_data import RedshiftDataStatementResult, RedshiftDataStatementResultConfig

    def describe():
        ubx.intent("look up aws_redshift_data_statement_result")

        ubx.data(RedshiftDataStatementResult, "example", RedshiftDataStatementResultConfig(
            id="example",
            wait_time_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="wait_time_seconds" type="number">
  The real, maximum time to wait for a message to arrive, in seconds, for long polling. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="column_metadata" type="list(object)">
  The real, structured metadata of the column. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="records" type="list(list(object))">
  The real records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_num_rows" type="number">
  The real, total number of rows in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
