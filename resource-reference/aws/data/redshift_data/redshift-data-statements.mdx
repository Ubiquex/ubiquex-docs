---
title: "aws_redshift_data_statements"
description: "`aws_redshift_data_statements` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_data_statements` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_statements"})

    		ubx.Data(redshift_data.RedshiftDataStatements, "example", redshift_data.RedshiftDataStatementsConfig{
    			ClusterIdentifier: "example",
    			Database:          "example",
    			MaxResults:        1,
    			RoleLevel:         true,
    			StatementName:     "example-statement-name",
    			Status:            "example",
    			WorkgroupName:     "example-workgroup-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftDataStatements } from "@ubx/sdk-aws/aws/data/redshift_data/redshift_data_statements";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_statements" });

      data(RedshiftDataStatements, "example", {
        clusterIdentifier: "example",
        database: "example",
        maxResults: 1,
        roleLevel: true,
        statementName: "example-statement-name",
        status: "example",
        workgroupName: "example-workgroup-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_data import RedshiftDataStatements, RedshiftDataStatementsConfig

    def describe():
        ubx.intent("look up aws_redshift_data_statements")

        ubx.data(RedshiftDataStatements, "example", RedshiftDataStatementsConfig(
            cluster_identifier="example",
            database="example",
            max_results=1,
            role_level=True,
            statement_name="example-statement-name",
            status="example",
            workgroup_name="example-workgroup-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="role_level" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="statement_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workgroup_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="statements" type="list(object)">
  Returned by the lookup.
</ResponseField>
