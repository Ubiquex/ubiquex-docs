---
title: "aws_redshift_data_statements"
description: "`aws_redshift_data_statements` reads the real SQL statements executed via the Redshift Data API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_statements"})

    		ubx.Data(redshift_data.RedshiftDataStatements, "example", redshift_data.RedshiftDataStatementsConfig{
    			ClusterIdentifier: "example",
    			Database:          "example",
    			MaxResults:        1,
    			RoleLevel:         true,
    			StatementName:     "example-statement-name",
    			Status:            "example",
    			WorkgroupName:     "example-workgroup-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RedshiftDataStatements } from "./local-sdk/aws/sdk/typescript/aws/data/redshift_data/redshift_data_statements.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_statements" });

      data(RedshiftDataStatements, "example", {
        clusterIdentifier: "example",
        database: "example",
        maxResults: 1,
        roleLevel: true,
        statementName: "example-statement-name",
        status: "example",
        workgroupName: "example-workgroup-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_data import RedshiftDataStatements, RedshiftDataStatementsConfig

    def describe():
        ubx.intent("look up aws_redshift_data_statements")

        ubx.data(RedshiftDataStatements, "example", RedshiftDataStatementsConfig(
            cluster_identifier="example",
            database="example",
            max_results=1,
            role_level=True,
            statement_name="example-statement-name",
            status="example",
            workgroup_name="example-workgroup-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="database" type="string">
  The real database this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="role_level" type="bool">
  The real level of the role. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="statement_name" type="string">
  The real name of the statement this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="workgroup_name" type="string">
  The real name of the workgroup this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="statements" type="list(object)">
  The real statements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
