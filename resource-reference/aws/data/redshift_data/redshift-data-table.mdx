---
title: "aws_redshift_data_table"
description: "`aws_redshift_data_table` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_redshift_data_table` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/redshift_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_table"})

    		ubx.Data(redshift_data.RedshiftDataTable, "example", redshift_data.RedshiftDataTableConfig{
    			Database:          "example",
    			ClusterIdentifier: "example",
    			ConnectedDatabase: "example",
    			DbUser:            "example",
    			MaxResults:        1,
    			Schema:            "example",
    			SecretArn:         "arn:aws:iam::123456789012:policy/example",
    			Table:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftDataTable } from "@ubx/sdk-aws/aws/data/redshift_data/redshift_data_table";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_table" });

      data(RedshiftDataTable, "example", {
        database: "example",
        clusterIdentifier: "example",
        connectedDatabase: "example",
        dbUser: "example",
        maxResults: 1,
        schema: "example",
        secretArn: "arn:aws:iam::123456789012:policy/example",
        table: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_data import RedshiftDataTable, RedshiftDataTableConfig

    def describe():
        ubx.intent("look up aws_redshift_data_table")

        ubx.data(RedshiftDataTable, "example", RedshiftDataTableConfig(
            database="example",
            cluster_identifier="example",
            connected_database="example",
            db_user="example",
            max_results=1,
            schema="example",
            secret_arn="arn:aws:iam::123456789012:policy/example",
            table="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cluster_identifier" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="connected_database" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="database" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="db_user" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="schema" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="table" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="workgroup_name" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="column_list" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="table_name" type="string">
  Returned by the lookup.
</ResponseField>
