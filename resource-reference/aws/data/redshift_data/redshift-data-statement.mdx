---
title: "aws_redshift_data_statement"
description: "`aws_redshift_data_statement` reads the real, current status of one SQL statement executed via the Redshift Data API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift_data "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/redshift_data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_redshift_data_statement"})

    		ubx.Data(redshift_data.RedshiftDataStatement, "example", redshift_data.RedshiftDataStatementConfig{
    			Id:              "example",
    			WaitTimeSeconds: 7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RedshiftDataStatement } from "@ubx/sdk-aws/aws/data/redshift_data/redshift_data_statement";

    export default stack("example", () => {
      intent({ summary: "look up aws_redshift_data_statement" });

      data(RedshiftDataStatement, "example", {
        id: "example",
        waitTimeSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.redshift_data import RedshiftDataStatement, RedshiftDataStatementConfig

    def describe():
        ubx.intent("look up aws_redshift_data_statement")

        ubx.data(RedshiftDataStatement, "example", RedshiftDataStatementConfig(
            id="example",
            wait_time_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="wait_time_seconds" type="number">
  The real, maximum time to wait for a message to arrive, in seconds, for long polling. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="cluster_identifier" type="string">
  The real, unique identifier of the cluster this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="database" type="string">
  The real database this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="db_user" type="string">
  The real database user this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="duration" type="number">
  The real duration of this object, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="error" type="string">
  The real error encountered while processing this request. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="execution_mode" type="string">
  The real mode this execution runs in. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="has_result_set" type="bool">
  The real has result set. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_parameters" type="list(object)">
  The real, structured parameters of this query. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="query_string" type="string">
  The real, raw query string this request runs. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="redshift_pid" type="number">
  The real Redshift process identifier (PID) for this session. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="redshift_query_id" type="number">
  The real, unique identifier of the redshift query this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="result_format" type="string">
  The real, requested format of this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="result_rows" type="number">
  The real rows included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="result_size" type="number">
  The real size of the result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  The real Amazon Resource Name (ARN) of the Secrets Manager secret this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="session_id" type="string">
  The real, unique identifier of the session this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sub_statements" type="list(object)">
  The real, nested policy statements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="workgroup_name" type="string">
  The real name of the workgroup this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
