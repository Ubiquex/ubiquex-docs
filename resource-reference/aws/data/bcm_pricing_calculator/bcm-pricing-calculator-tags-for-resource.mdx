---
title: "aws_bcm_pricing_calculator_tags_for_resource"
description: "`aws_bcm_pricing_calculator_tags_for_resource` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_bcm_pricing_calculator_tags_for_resource` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bcm_pricing_calculator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/bcm_pricing_calculator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_bcm_pricing_calculator_tags_for_resource"})

    		ubx.Data(bcm_pricing_calculator.BcmPricingCalculatorTagsForResource, "example", bcm_pricing_calculator.BcmPricingCalculatorTagsForResourceConfig{
    			Arn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BcmPricingCalculatorTagsForResource } from "@ubx/sdk-aws/aws/data/bcm_pricing_calculator/bcm_pricing_calculator_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_bcm_pricing_calculator_tags_for_resource" });

      data(BcmPricingCalculatorTagsForResource, "example", {
        arn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.bcm_pricing_calculator import BcmPricingCalculatorTagsForResource, BcmPricingCalculatorTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_bcm_pricing_calculator_tags_for_resource")

        ubx.data(BcmPricingCalculatorTagsForResource, "example", BcmPricingCalculatorTagsForResourceConfig(
            arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  Returned by the lookup.
</ResponseField>
