---
title: "aws_glacier_job"
description: "`aws_glacier_job` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glacier_job` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_job"})

    		ubx.Data(glacier.Job, "example", glacier.JobConfig{
    			AccountId: "example",
    			JobId:     "example",
    			VaultName: "example-vault-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Job } from "@ubx/sdk-aws/aws/data/glacier/job";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_job" });

      data(Job, "example", {
        accountId: "example",
        jobId: "example",
        vaultName: "example-vault-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import Job, JobConfig

    def describe():
        ubx.intent("look up aws_glacier_job")

        ubx.data(Job, "example", JobConfig(
            account_id="example",
            job_id="example",
            vault_name="example-vault-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="action" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="archive_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="archive_sha256_tree_hash" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="archive_size_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="completed" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="completion_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="inventory_retrieval_parameters" type="object">
  &lt;p&gt;Describes the options for a range inventory retrieval job.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="end_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="format" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="limit" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="marker" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_date" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inventory_size_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="job_output_path" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="output_location" type="object">
  &lt;p&gt;Contains information about the location where the select job results are stored.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="s3" type="object">
      &lt;p&gt;Contains information about the location in Amazon S3 where the select job results are stored.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="access_control_list" type="list(object)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="bucket_name" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="canned_acl" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="encryption" type="object">
          &lt;p&gt;Contains information about the encryption used to store the job results in Amazon S3. &lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="encryption_type" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="kmscontext" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="kmskey_id" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="prefix" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="storage_class" type="string">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="tagging" type="map(string)">
          Optional lookup argument.
        </ResponseField>
        <ResponseField name="user_metadata" type="map(string)">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="retrieval_byte_range" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="select_parameters" type="object">
  &lt;p&gt;Contains information about the parameters used for a select.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="expression" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="expression_type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="input_serialization" type="object">
      &lt;p&gt;Describes how the archive is serialized.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="csv" type="object">
          &lt;p&gt;Contains information about the comma-separated value (CSV) file to select from.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="comments" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="field_delimiter" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="file_header_info" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="quote_character" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="quote_escape_character" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="record_delimiter" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_serialization" type="object">
      &lt;p&gt;Describes how the select output is serialized.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="csv" type="object">
          &lt;p&gt;Contains information about the comma-separated value (CSV) file that the job results are stored in.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="field_delimiter" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="quote_character" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="quote_escape_character" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="quote_fields" type="string">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="record_delimiter" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sha256_tree_hash" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="snstopic" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_code" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status_message" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="tier" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vault_arn" type="string">
  Returned by the lookup.
</ResponseField>
