---
title: "aws_glacier_vaults"
description: "`aws_glacier_vaults` looks up a real S3 Glacier vault."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_vaults"})

    		ubx.Data(glacier.Vaults, "example", glacier.VaultsConfig{
    			AccountId: "example",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Vaults } from "@ubx/sdk-aws/aws/data/glacier/vaults";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_vaults" });

      data(Vaults, "example", {
        accountId: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import Vaults, VaultsConfig

    def describe():
        ubx.intent("look up aws_glacier_vaults")

        ubx.data(Vaults, "example", VaultsConfig(
            account_id="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vault_list" type="list(object)">
  The real vaults included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
