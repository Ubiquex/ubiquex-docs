---
title: "aws_glacier_parts"
description: "`aws_glacier_parts` reads the real, individual parts already uploaded as part of an S3 Glacier multipart upload."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_parts"})

    		ubx.Data(glacier.Parts, "example", glacier.PartsConfig{
    			AccountId: "example",
    			UploadId:  "example",
    			VaultName: "example-vault-name",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Parts } from "@ubx/sdk-aws/aws/data/glacier/parts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_parts" });

      data(Parts, "example", {
        accountId: "example",
        uploadId: "example",
        vaultName: "example-vault-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import Parts, PartsConfig

    def describe():
        ubx.intent("look up aws_glacier_parts")

        ubx.data(Parts, "example", PartsConfig(
            account_id="example",
            upload_id="example",
            vault_name="example-vault-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="upload_id" type="string" required>
  The real, unique identifier of the upload this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  The real name of the vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="archive_description" type="string">
  The real, free-text description of the archive. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="multipart_upload_id" type="string">
  The real, unique identifier of the multipart upload this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="part_size_in_bytes" type="number">
  The real size of the part size in, in bytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="parts" type="list(object)">
  The real upload parts included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="vault_arn" type="string">
  The real Amazon Resource Name (ARN) of the vault this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
