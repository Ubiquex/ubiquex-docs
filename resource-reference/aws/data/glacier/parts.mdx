---
title: "aws_glacier_parts"
description: "`aws_glacier_parts` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glacier_parts` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_parts"})

    		ubx.Data(glacier.Parts, "example", glacier.PartsConfig{
    			AccountId: "example",
    			UploadId:  "example",
    			VaultName: "example-vault-name",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Parts } from "@ubx/sdk-aws/aws/data/glacier/parts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_parts" });

      data(Parts, "example", {
        accountId: "example",
        uploadId: "example",
        vaultName: "example-vault-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import Parts, PartsConfig

    def describe():
        ubx.intent("look up aws_glacier_parts")

        ubx.data(Parts, "example", PartsConfig(
            account_id="example",
            upload_id="example",
            vault_name="example-vault-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="upload_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="archive_description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="creation_date" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="multipart_upload_id" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="part_size_in_bytes" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="parts" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="vault_arn" type="string">
  Returned by the lookup.
</ResponseField>
