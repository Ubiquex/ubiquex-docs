---
title: "aws_glacier_multipart_uploads"
description: "`aws_glacier_multipart_uploads` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glacier_multipart_uploads` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_multipart_uploads"})

    		ubx.Data(glacier.MultipartUploads, "example", glacier.MultipartUploadsConfig{
    			AccountId: "example",
    			VaultName: "example-vault-name",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MultipartUploads } from "@ubx/sdk-aws/aws/data/glacier/multipart_uploads";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_multipart_uploads" });

      data(MultipartUploads, "example", {
        accountId: "example",
        vaultName: "example-vault-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import MultipartUploads, MultipartUploadsConfig

    def describe():
        ubx.intent("look up aws_glacier_multipart_uploads")

        ubx.data(MultipartUploads, "example", MultipartUploadsConfig(
            account_id="example",
            vault_name="example-vault-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="uploads_list" type="list(object)">
  Returned by the lookup.
</ResponseField>
