---
title: "aws_glacier_multipart_uploads"
description: "`aws_glacier_multipart_uploads` reads the real, in-progress multipart uploads to an S3 Glacier vault."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_multipart_uploads"})

    		ubx.Data(glacier.MultipartUploads, "example", glacier.MultipartUploadsConfig{
    			AccountId: "example",
    			VaultName: "example-vault-name",
    			Limit:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MultipartUploads } from "./local-sdk/aws/sdk/typescript/aws/data/glacier/multipart_uploads.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_multipart_uploads" });

      data(MultipartUploads, "example", {
        accountId: "example",
        vaultName: "example-vault-name",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import MultipartUploads, MultipartUploadsConfig

    def describe():
        ubx.intent("look up aws_glacier_multipart_uploads")

        ubx.data(MultipartUploads, "example", MultipartUploadsConfig(
            account_id="example",
            vault_name="example-vault-name",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  The real name of the vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="uploads_list" type="list(object)">
  The real uploadss included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
