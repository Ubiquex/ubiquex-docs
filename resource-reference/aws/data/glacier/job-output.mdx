---
title: "aws_glacier_job_output"
description: "`aws_glacier_job_output` reads the real output of a completed S3 Glacier job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_job_output"})

    		ubx.Data(glacier.JobOutput, "example", glacier.JobOutputConfig{
    			AccountId: "example",
    			JobId:     "example",
    			VaultName: "example-vault-name",
    			Range:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobOutput } from "@ubx/sdk-aws/aws/data/glacier/job_output";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_job_output" });

      data(JobOutput, "example", {
        accountId: "example",
        jobId: "example",
        vaultName: "example-vault-name",
        range: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import JobOutput, JobOutputConfig

    def describe():
        ubx.intent("look up aws_glacier_job_output")

        ubx.data(JobOutput, "example", JobOutputConfig(
            account_id="example",
            job_id="example",
            vault_name="example-vault-name",
            range="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  The real, unique identifier of the job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="range" type="string">
  The real byte range requested for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  The real name of the vault this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accept_ranges" type="string">
  Whether this object accepts byte-range requests. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="archive_description" type="string">
  The real, free-text description of the archive. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="body" type="string">
  The real, raw body of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="checksum" type="string">
  The real checksum of this object's own content. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_range" type="string">
  The real Content-Range header value for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
