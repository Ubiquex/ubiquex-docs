---
title: "aws_glacier_job_output"
description: "`aws_glacier_job_output` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_glacier_job_output` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_job_output"})

    		ubx.Data(glacier.JobOutput, "example", glacier.JobOutputConfig{
    			AccountId: "example",
    			JobId:     "example",
    			VaultName: "example-vault-name",
    			Range:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { JobOutput } from "@ubx/sdk-aws/aws/data/glacier/job_output";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_job_output" });

      data(JobOutput, "example", {
        accountId: "example",
        jobId: "example",
        vaultName: "example-vault-name",
        range: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import JobOutput, JobOutputConfig

    def describe():
        ubx.intent("look up aws_glacier_job_output")

        ubx.data(JobOutput, "example", JobOutputConfig(
            account_id="example",
            job_id="example",
            vault_name="example-vault-name",
            range="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="job_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="range" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="accept_ranges" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="archive_description" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="body" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="checksum" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="content_range" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="number">
  Returned by the lookup.
</ResponseField>
