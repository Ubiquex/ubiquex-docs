---
title: "aws_glacier_provisioned_capacity"
description: "`aws_glacier_provisioned_capacity` reads the real, provisioned expedited retrieval capacity units purchased for an S3 Glacier account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glacier "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/glacier"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_glacier_provisioned_capacity"})

    		ubx.Data(glacier.ProvisionedCapacity, "example", glacier.ProvisionedCapacityConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProvisionedCapacity } from "@ubx/sdk-aws/aws/data/glacier/provisioned_capacity";

    export default stack("example", () => {
      intent({ summary: "look up aws_glacier_provisioned_capacity" });

      data(ProvisionedCapacity, "example", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.glacier import ProvisionedCapacity, ProvisionedCapacityConfig

    def describe():
        ubx.intent("look up aws_glacier_provisioned_capacity")

        ubx.data(ProvisionedCapacity, "example", ProvisionedCapacityConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the AWS account this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="provisioned_capacity_list" type="list(object)">
  The real provisioned capacitys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
