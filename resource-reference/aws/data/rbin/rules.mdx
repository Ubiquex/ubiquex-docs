---
title: "aws_rbin_rules"
description: "`data_aws_rbin_rules` looks up a real, already-existing `aws_rbin_rule`."
---

It creates a real Recycle Bin retention rule that intercepts deletion of a matching resource, an EBS snapshot or EBS-backed AMI, retaining it in the bin for a configured period so it can be recovered instead of being permanently deleted immediately.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	rbin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/rbin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_rbin_rules"})

    		ubx.Data(rbin.Rules, "example", rbin.RulesConfig{
    			ResourceType:        "example",
    			ExcludeResourceTags: []map[string]any{{"resource_tag_key": "example"}},
    			LockState:           "example",
    			MaxResults:          1,
    			ResourceTags:        []map[string]any{{"resource_tag_key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Rules } from "./local-sdk/aws/sdk/typescript/aws/data/rbin/rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_rbin_rules" });

      data(Rules, "example", {
        resourceType: "example",
        excludeResourceTags: [{ resourceTagKey: "example" }],
        lockState: "example",
        maxResults: 1,
        resourceTags: [{ resourceTagKey: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.rbin import Rules, RulesConfig

    def describe():
        ubx.intent("look up aws_rbin_rules")

        ubx.data(Rules, "example", RulesConfig(
            resource_type="example",
            exclude_resource_tags=[{"resource_tag_key": "example"}],
            lock_state="example",
            max_results=1,
            resource_tags=[{"resource_tag_key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="exclude_resource_tags" type="list(object)">
  The real exclude resource tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="lock_state" type="string">
  The real, current state of the lock this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_tags" type="list(object)">
  The real tags applied to the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The real, discriminating type of AWS resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  The real rules included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
