---
title: "aws_ce_savings_plans_purchase_recommendation_generation"
description: "`aws_ce_savings_plans_purchase_recommendation_generation` reads the real, current status of generating a fresh Cost Explorer Savings Plans purchase..."
---

`aws_ce_savings_plans_purchase_recommendation_generation` reads the real, current status of generating a fresh Cost Explorer Savings Plans purchase recommendation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ce_savings_plans_purchase_recommendation_generation"})

    		ubx.Data(ce.SavingsPlansPurchaseRecommendationGeneration, "example", ce.SavingsPlansPurchaseRecommendationGenerationConfig{
    			GenerationStatus:  "example",
    			PageSize:          1,
    			RecommendationIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SavingsPlansPurchaseRecommendationGeneration } from "./local-sdk/aws/sdk/typescript/aws/data/ce/savings_plans_purchase_recommendation_generation.ts";

    export default stack("example", () => {
      intent({
        summary: "look up aws_ce_savings_plans_purchase_recommendation_generation",
      });

      data(SavingsPlansPurchaseRecommendationGeneration, "example", {
        generationStatus: "example",
        pageSize: 1,
        recommendationIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.ce import SavingsPlansPurchaseRecommendationGeneration, SavingsPlansPurchaseRecommendationGenerationConfig

    def describe():
        ubx.intent("look up aws_ce_savings_plans_purchase_recommendation_generation")

        ubx.data(SavingsPlansPurchaseRecommendationGeneration, "example", SavingsPlansPurchaseRecommendationGenerationConfig(
            generation_status="example",
            page_size=1,
            recommendation_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="generation_status" type="string">
  The real, current status of the generation this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="recommendation_ids" type="list(string)">
  The real, unique identifiers of the recommendations this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="generation_summary_list" type="list(object)">
  The real generation summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
