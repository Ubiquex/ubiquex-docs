---
title: "aws_ce_commitment_purchase_analyses"
description: "`aws_ce_commitment_purchase_analyses` reads the real, generated analyses recommending a Reserved Instance or Savings Plan purchase."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ce_commitment_purchase_analyses"})

    		ubx.Data(ce.CommitmentPurchaseAnalyses, "example", ce.CommitmentPurchaseAnalysesConfig{
    			AnalysisIds:    []string{"example"},
    			AnalysisStatus: "example",
    			PageSize:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CommitmentPurchaseAnalyses } from "@ubx/sdk-aws/aws/data/ce/commitment_purchase_analyses";

    export default stack("example", () => {
      intent({ summary: "look up aws_ce_commitment_purchase_analyses" });

      data(CommitmentPurchaseAnalyses, "example", {
        analysisIds: ["example"],
        analysisStatus: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ce import CommitmentPurchaseAnalyses, CommitmentPurchaseAnalysesConfig

    def describe():
        ubx.intent("look up aws_ce_commitment_purchase_analyses")

        ubx.data(CommitmentPurchaseAnalyses, "example", CommitmentPurchaseAnalysesConfig(
            analysis_ids=["example"],
            analysis_status="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="analysis_ids" type="list(string)">
  The real, unique identifiers of the analysiss this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="analysis_status" type="string">
  The real, current status of the analysis this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="analysis_summary_list" type="list(object)">
  The real analysis summarys included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
