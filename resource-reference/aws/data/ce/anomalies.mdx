---
title: "aws_ce_anomalies"
description: "`aws_ce_anomalies` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_ce_anomalies` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ce "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/ce"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_ce_anomalies"})

    		ubx.Data(ce.Anomalies, "example", ce.AnomaliesConfig{
    			DateInterval: map[string]any{"start_date": "example"},
    			Feedback:     "example",
    			MaxResults:   1,
    			MonitorArn:   "arn:aws:iam::123456789012:policy/example",
    			TotalImpact:  map[string]any{"numeric_operator": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Anomalies } from "@ubx/sdk-aws/aws/data/ce/anomalies";

    export default stack("example", () => {
      intent({ summary: "look up aws_ce_anomalies" });

      data(Anomalies, "example", {
        dateInterval: { startDate: "example" },
        feedback: "example",
        maxResults: 1,
        monitorArn: "arn:aws:iam::123456789012:policy/example",
        totalImpact: { numericOperator: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.ce import Anomalies, AnomaliesConfig

    def describe():
        ubx.intent("look up aws_ce_anomalies")

        ubx.data(Anomalies, "example", AnomaliesConfig(
            date_interval={"start_date": "example"},
            feedback="example",
            max_results=1,
            monitor_arn="arn:aws:iam::123456789012:policy/example",
            total_impact={"numeric_operator": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="date_interval" type="object" required>
  &lt;p&gt;The time period for an anomaly. &lt;/p&gt; Required lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_date" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_date" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="feedback" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="monitor_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="total_impact" type="object">
  &lt;p&gt;Filters cost anomalies based on the total impact. &lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="end_value" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="numeric_operator" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="start_value" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

## Result properties

<ResponseField name="anomalies" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_page_token" type="string">
  Returned by the lookup.
</ResponseField>
