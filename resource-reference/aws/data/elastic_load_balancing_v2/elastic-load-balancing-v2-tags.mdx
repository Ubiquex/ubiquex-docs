---
title: "aws_elastic_load_balancing_v2_tags"
description: "`aws_elastic_load_balancing_v2_tags` reads the real tags applied to a Load Balancer resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_tags"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2Tags, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2TagsConfig{
    			ResourceArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2Tags } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_elastic_load_balancing_v2_tags" });

      data(ElasticLoadBalancingV2Tags, "example", {
        resourceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2Tags, ElasticLoadBalancingV2TagsConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_tags")

        ubx.data(ElasticLoadBalancingV2Tags, "example", ElasticLoadBalancingV2TagsConfig(
            resource_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arns" type="list(string)" required>
  The real Amazon Resource Names (ARNs) this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tag_descriptions" type="list(object)">
  The real, described tags included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
