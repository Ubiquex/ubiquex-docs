---
title: "aws_elastic_load_balancing_v2_target_health"
description: "`aws_elastic_load_balancing_v2_target_health` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elastic_load_balancing_v2_target_health` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_target_health"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2TargetHealth, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2TargetHealthConfig{
    			TargetGroupArn: "arn:aws:iam::123456789012:policy/example",
    			Include:        []string{"example"},
    			Targets:        []map[string]any{{"availability_zone": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2TargetHealth } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_target_health";

    export default stack("example", () => {
      intent({ summary: "look up aws_elastic_load_balancing_v2_target_health" });

      data(ElasticLoadBalancingV2TargetHealth, "example", {
        targetGroupArn: "arn:aws:iam::123456789012:policy/example",
        include: ["example"],
        targets: [{ availabilityZone: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2TargetHealth, ElasticLoadBalancingV2TargetHealthConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_target_health")

        ubx.data(ElasticLoadBalancingV2TargetHealth, "example", ElasticLoadBalancingV2TargetHealthConfig(
            target_group_arn="arn:aws:iam::123456789012:policy/example",
            include=["example"],
            targets=[{"availability_zone": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="target_group_arn" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="targets" type="list(object)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="target_health_descriptions" type="list(object)">
  Returned by the lookup.
</ResponseField>
