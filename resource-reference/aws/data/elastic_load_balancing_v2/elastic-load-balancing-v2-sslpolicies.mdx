---
title: "aws_elastic_load_balancing_v2_sslpolicies"
description: "`aws_elastic_load_balancing_v2_sslpolicies` reads the real, available SSL/TLS security policies for a Load Balancer listener."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_sslpolicies"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2Sslpolicies, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2SslpoliciesConfig{
    			LoadBalancerType: "example",
    			Marker:           "example",
    			Names:            []string{"example"},
    			PageSize:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2Sslpolicies } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_sslpolicies";

    export default stack("example", () => {
      intent({ summary: "look up aws_elastic_load_balancing_v2_sslpolicies" });

      data(ElasticLoadBalancingV2Sslpolicies, "example", {
        loadBalancerType: "example",
        marker: "example",
        names: ["example"],
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2Sslpolicies, ElasticLoadBalancingV2SslpoliciesConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_sslpolicies")

        ubx.data(ElasticLoadBalancingV2Sslpolicies, "example", ElasticLoadBalancingV2SslpoliciesConfig(
            load_balancer_type="example",
            marker="example",
            names=["example"],
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_type" type="string">
  The real, discriminating type of the load balancer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ssl_policies" type="list(object)">
  The real ssl policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
