---
title: "aws_elastic_load_balancing_v2_load_balancers"
description: "`aws_elastic_load_balancing_v2_load_balancers` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elastic_load_balancing_v2_load_balancers` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_load_balancers"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2LoadBalancers, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2LoadBalancersConfig{
    			LoadBalancerArns: []string{"example"},
    			Marker:           "example",
    			Names:            []string{"example"},
    			PageSize:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2LoadBalancers } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_load_balancers";

    export default stack("example", () => {
      intent({ summary: "look up aws_elastic_load_balancing_v2_load_balancers" });

      data(ElasticLoadBalancingV2LoadBalancers, "example", {
        loadBalancerArns: ["example"],
        marker: "example",
        names: ["example"],
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2LoadBalancers, ElasticLoadBalancingV2LoadBalancersConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_load_balancers")

        ubx.data(ElasticLoadBalancingV2LoadBalancers, "example", ElasticLoadBalancingV2LoadBalancersConfig(
            load_balancer_arns=["example"],
            marker="example",
            names=["example"],
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_arns" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="load_balancers" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>
