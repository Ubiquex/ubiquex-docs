---
title: "aws_elastic_load_balancing_v2_trust_store_revocation_content"
description: "`aws_elastic_load_balancing_v2_trust_store_revocation_content` reads the real certificate revocation list (CRL) content of a Load Balancer mutual TLS..."
---

`aws_elastic_load_balancing_v2_trust_store_revocation_content` reads the real certificate revocation list (CRL) content of a Load Balancer mutual TLS trust store.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_trust_store_revocation_content"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2TrustStoreRevocationContent, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2TrustStoreRevocationContentConfig{
    			RevocationId:  1,
    			TrustStoreArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2TrustStoreRevocationContent } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_trust_store_revocation_content";

    export default stack("example", () => {
      intent({
        summary:
          "look up aws_elastic_load_balancing_v2_trust_store_revocation_content",
      });

      data(ElasticLoadBalancingV2TrustStoreRevocationContent, "example", {
        revocationId: 1,
        trustStoreArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2TrustStoreRevocationContent, ElasticLoadBalancingV2TrustStoreRevocationContentConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_trust_store_revocation_content")

        ubx.data(ElasticLoadBalancingV2TrustStoreRevocationContent, "example", ElasticLoadBalancingV2TrustStoreRevocationContentConfig(
            revocation_id=1,
            trust_store_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="revocation_id" type="number" required>
  The real, unique identifier of the revocation this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="trust_store_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the trust store this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="location" type="string">
  The real location this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
