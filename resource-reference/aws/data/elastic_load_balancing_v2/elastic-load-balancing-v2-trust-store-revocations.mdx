---
title: "aws_elastic_load_balancing_v2_trust_store_revocations"
description: "`aws_elastic_load_balancing_v2_trust_store_revocations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elastic_load_balancing_v2_trust_store_revocations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_trust_store_revocations"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2TrustStoreRevocations, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2TrustStoreRevocationsConfig{
    			TrustStoreArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:        "example",
    			PageSize:      1,
    			RevocationIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2TrustStoreRevocations } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_trust_store_revocations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elastic_load_balancing_v2_trust_store_revocations",
      });

      data(ElasticLoadBalancingV2TrustStoreRevocations, "example", {
        trustStoreArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        pageSize: 1,
        revocationIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2TrustStoreRevocations, ElasticLoadBalancingV2TrustStoreRevocationsConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_trust_store_revocations")

        ubx.data(ElasticLoadBalancingV2TrustStoreRevocations, "example", ElasticLoadBalancingV2TrustStoreRevocationsConfig(
            trust_store_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            page_size=1,
            revocation_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="revocation_ids" type="list(number)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="trust_store_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="trust_store_revocations" type="list(object)">
  Returned by the lookup.
</ResponseField>
