---
title: "aws_elastic_load_balancing_v2_listeners"
description: "`aws_elastic_load_balancing_v2_listeners` looks up a real Load Balancer listener, checking for connection requests on a specific port and protocol."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_listeners"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2Listeners, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2ListenersConfig{
    			ListenerArns:    []string{"example"},
    			LoadBalancerArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:          "example",
    			PageSize:        1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2Listeners } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_listeners";

    export default stack("example", () => {
      intent({ summary: "look up aws_elastic_load_balancing_v2_listeners" });

      data(ElasticLoadBalancingV2Listeners, "example", {
        listenerArns: ["example"],
        loadBalancerArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2Listeners, ElasticLoadBalancingV2ListenersConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_listeners")

        ubx.data(ElasticLoadBalancingV2Listeners, "example", ElasticLoadBalancingV2ListenersConfig(
            listener_arns=["example"],
            load_balancer_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="listener_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the listeners this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="load_balancer_arn" type="string">
  The real Amazon Resource Name (ARN) of the load balancer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="listeners" type="list(object)">
  The real load balancer listeners included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
