---
title: "aws_elastic_load_balancing_v2_listener_certificates"
description: "`aws_elastic_load_balancing_v2_listener_certificates` reads the real TLS certificates attached to a Load Balancer listener."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_listener_certificates"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2ListenerCertificates, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2ListenerCertificatesConfig{
    			ListenerArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:      "example",
    			PageSize:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2ListenerCertificates } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_listener_certificates";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elastic_load_balancing_v2_listener_certificates",
      });

      data(ElasticLoadBalancingV2ListenerCertificates, "example", {
        listenerArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2ListenerCertificates, ElasticLoadBalancingV2ListenerCertificatesConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_listener_certificates")

        ubx.data(ElasticLoadBalancingV2ListenerCertificates, "example", ElasticLoadBalancingV2ListenerCertificatesConfig(
            listener_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="listener_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the listener this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="certificates" type="list(object)">
  The real certificates included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
