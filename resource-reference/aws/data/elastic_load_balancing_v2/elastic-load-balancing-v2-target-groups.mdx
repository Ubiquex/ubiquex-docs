---
title: "aws_elastic_load_balancing_v2_target_groups"
description: "`aws_elastic_load_balancing_v2_target_groups` looks up a real Load Balancer target group, routing traffic to a set of registered targets."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_target_groups"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2TargetGroups, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2TargetGroupsConfig{
    			LoadBalancerArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:          "example",
    			Names:           []string{"example"},
    			PageSize:        1,
    			TargetGroupArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ElasticLoadBalancingV2TargetGroups } from "./local-sdk/aws/sdk/typescript/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_target_groups.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_elastic_load_balancing_v2_target_groups" });

      data(ElasticLoadBalancingV2TargetGroups, "example", {
        loadBalancerArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        names: ["example"],
        pageSize: 1,
        targetGroupArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2TargetGroups, ElasticLoadBalancingV2TargetGroupsConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_target_groups")

        ubx.data(ElasticLoadBalancingV2TargetGroups, "example", ElasticLoadBalancingV2TargetGroupsConfig(
            load_balancer_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            names=["example"],
            page_size=1,
            target_group_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_arn" type="string">
  The real Amazon Resource Name (ARN) of the load balancer this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="names" type="list(string)">
  The real, given names of the objects this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="target_group_arns" type="list(string)">
  The real Amazon Resource Names (ARNs) of the target groups this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target_groups" type="list(object)">
  The real target groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
