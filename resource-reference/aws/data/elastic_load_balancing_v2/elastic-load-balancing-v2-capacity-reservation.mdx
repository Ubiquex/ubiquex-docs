---
title: "aws_elastic_load_balancing_v2_capacity_reservation"
description: "`aws_elastic_load_balancing_v2_capacity_reservation` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elastic_load_balancing_v2_capacity_reservation` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_capacity_reservation"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2CapacityReservation, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2CapacityReservationConfig{
    			LoadBalancerArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2CapacityReservation } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_capacity_reservation";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elastic_load_balancing_v2_capacity_reservation",
      });

      data(ElasticLoadBalancingV2CapacityReservation, "example", {
        loadBalancerArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2CapacityReservation, ElasticLoadBalancingV2CapacityReservationConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_capacity_reservation")

        ubx.data(ElasticLoadBalancingV2CapacityReservation, "example", ElasticLoadBalancingV2CapacityReservationConfig(
            load_balancer_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="load_balancer_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="capacity_reservation_state" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="decrease_requests_remaining" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="minimum_load_balancer_capacity" type="object">
  &lt;p&gt;The minimum capacity for a load balancer.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="capacity_units" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
