---
title: "aws_elastic_load_balancing_v2_trust_store_associations"
description: "`aws_elastic_load_balancing_v2_trust_store_associations` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_elastic_load_balancing_v2_trust_store_associations` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic_load_balancing_v2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/elastic_load_balancing_v2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_elastic_load_balancing_v2_trust_store_associations"})

    		ubx.Data(elastic_load_balancing_v2.ElasticLoadBalancingV2TrustStoreAssociations, "example", elastic_load_balancing_v2.ElasticLoadBalancingV2TrustStoreAssociationsConfig{
    			TrustStoreArn: "arn:aws:iam::123456789012:policy/example",
    			Marker:        "example",
    			PageSize:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ElasticLoadBalancingV2TrustStoreAssociations } from "@ubx/sdk-aws/aws/data/elastic_load_balancing_v2/elastic_load_balancing_v2_trust_store_associations";

    export default stack("example", () => {
      intent({
        summary: "look up aws_elastic_load_balancing_v2_trust_store_associations",
      });

      data(ElasticLoadBalancingV2TrustStoreAssociations, "example", {
        trustStoreArn: "arn:aws:iam::123456789012:policy/example",
        marker: "example",
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.elastic_load_balancing_v2 import ElasticLoadBalancingV2TrustStoreAssociations, ElasticLoadBalancingV2TrustStoreAssociationsConfig

    def describe():
        ubx.intent("look up aws_elastic_load_balancing_v2_trust_store_associations")

        ubx.data(ElasticLoadBalancingV2TrustStoreAssociations, "example", ElasticLoadBalancingV2TrustStoreAssociationsConfig(
            trust_store_arn="arn:aws:iam::123456789012:policy/example",
            marker="example",
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="trust_store_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="trust_store_associations" type="list(object)">
  Returned by the lookup.
</ResponseField>
