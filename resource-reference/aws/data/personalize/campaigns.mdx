---
title: "aws_personalize_campaigns"
description: "`aws_personalize_campaigns` looks up a real Personalize campaign, a deployed solution version serving real-time recommendations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_campaigns"})

    		ubx.Data(personalize.Campaigns, "example", personalize.CampaignsConfig{
    			MaxResults:  1,
    			SolutionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Campaigns } from "@ubx/sdk-aws/aws/data/personalize/campaigns";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_campaigns" });

      data(Campaigns, "example", {
        maxResults: 1,
        solutionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import Campaigns, CampaignsConfig

    def describe():
        ubx.intent("look up aws_personalize_campaigns")

        ubx.data(Campaigns, "example", CampaignsConfig(
            max_results=1,
            solution_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="solution_arn" type="string">
  The real Amazon Resource Name (ARN) of the solution this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="campaigns" type="list(object)">
  The real campaigns included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
