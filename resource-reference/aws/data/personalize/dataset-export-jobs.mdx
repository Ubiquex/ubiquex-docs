---
title: "aws_personalize_dataset_export_jobs"
description: "`aws_personalize_dataset_export_jobs` looks up a real Personalize job exporting a dataset to S3."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_dataset_export_jobs"})

    		ubx.Data(personalize.DatasetExportJobs, "example", personalize.DatasetExportJobsConfig{
    			DatasetArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatasetExportJobs } from "@ubx/sdk-aws/aws/data/personalize/dataset_export_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_dataset_export_jobs" });

      data(DatasetExportJobs, "example", {
        datasetArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import DatasetExportJobs, DatasetExportJobsConfig

    def describe():
        ubx.intent("look up aws_personalize_dataset_export_jobs")

        ubx.data(DatasetExportJobs, "example", DatasetExportJobsConfig(
            dataset_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_arn" type="string">
  The real Amazon Resource Name (ARN) of the dataset this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="dataset_export_jobs" type="list(object)">
  The real Amazon Personalize dataset export jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
