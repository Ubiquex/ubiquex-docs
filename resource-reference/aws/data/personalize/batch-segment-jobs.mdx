---
title: "aws_personalize_batch_segment_jobs"
description: "`aws_personalize_batch_segment_jobs` looks up a real Personalize batch segment job, generating a segmented list of users for a campaign."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_batch_segment_jobs"})

    		ubx.Data(personalize.BatchSegmentJobs, "example", personalize.BatchSegmentJobsConfig{
    			MaxResults:         1,
    			SolutionVersionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BatchSegmentJobs } from "./local-sdk/aws/sdk/typescript/aws/data/personalize/batch_segment_jobs.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_batch_segment_jobs" });

      data(BatchSegmentJobs, "example", {
        maxResults: 1,
        solutionVersionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import BatchSegmentJobs, BatchSegmentJobsConfig

    def describe():
        ubx.intent("look up aws_personalize_batch_segment_jobs")

        ubx.data(BatchSegmentJobs, "example", BatchSegmentJobsConfig(
            max_results=1,
            solution_version_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="solution_version_arn" type="string">
  The real Amazon Resource Name (ARN) of the solution version this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="batch_segment_jobs" type="list(object)">
  The real Amazon Personalize batch segment jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
