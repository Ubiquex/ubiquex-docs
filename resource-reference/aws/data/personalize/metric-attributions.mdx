---
title: "aws_personalize_metric_attributions"
description: "`data_aws_personalize_metric_attributions` looks up a real, already-existing `aws_personalize_metric_attribution`."
---

It configures real, ongoing export of business metrics, click-through rate, conversion, tied back to an `aws_personalize_solution`'s recommendations, so their real-world impact can be measured outside Personalize itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_metric_attributions"})

    		ubx.Data(personalize.MetricAttributions, "example", personalize.MetricAttributionsConfig{
    			DatasetGroupArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { MetricAttributions } from "./local-sdk/aws/sdk/typescript/aws/data/personalize/metric_attributions.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_metric_attributions" });

      data(MetricAttributions, "example", {
        datasetGroupArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import MetricAttributions, MetricAttributionsConfig

    def describe():
        ubx.intent("look up aws_personalize_metric_attributions")

        ubx.data(MetricAttributions, "example", MetricAttributionsConfig(
            dataset_group_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_group_arn" type="string">
  The real Amazon Resource Name (ARN) of the dataset group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metric_attributions" type="list(object)">
  The real Amazon Personalize metric attributions included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
