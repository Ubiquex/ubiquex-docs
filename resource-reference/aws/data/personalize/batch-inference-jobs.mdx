---
title: "aws_personalize_batch_inference_jobs"
description: "`aws_personalize_batch_inference_jobs` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_personalize_batch_inference_jobs` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_batch_inference_jobs"})

    		ubx.Data(personalize.BatchInferenceJobs, "example", personalize.BatchInferenceJobsConfig{
    			MaxResults:         1,
    			SolutionVersionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BatchInferenceJobs } from "@ubx/sdk-aws/aws/data/personalize/batch_inference_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_batch_inference_jobs" });

      data(BatchInferenceJobs, "example", {
        maxResults: 1,
        solutionVersionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import BatchInferenceJobs, BatchInferenceJobsConfig

    def describe():
        ubx.intent("look up aws_personalize_batch_inference_jobs")

        ubx.data(BatchInferenceJobs, "example", BatchInferenceJobsConfig(
            max_results=1,
            solution_version_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="solution_version_arn" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="batch_inference_jobs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
