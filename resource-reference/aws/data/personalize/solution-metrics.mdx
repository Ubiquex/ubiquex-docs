---
title: "aws_personalize_solution_metrics"
description: "`data_aws_personalize_solution_metrics` looks up a real, already-existing `aws_personalize_solution`."
---

It trains a real recommendation model against the data in an `aws_personalize_dataset_group`, using either a built-in Personalize recipe or AutoML, the resource that actually produces the model a deployed campaign serves recommendations from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_solution_metrics"})

    		ubx.Data(personalize.SolutionMetrics, "example", personalize.SolutionMetricsConfig{
    			SolutionVersionArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SolutionMetrics } from "@ubx/sdk-aws/aws/data/personalize/solution_metrics";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_solution_metrics" });

      data(SolutionMetrics, "example", {
        solutionVersionArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import SolutionMetrics, SolutionMetricsConfig

    def describe():
        ubx.intent("look up aws_personalize_solution_metrics")

        ubx.data(SolutionMetrics, "example", SolutionMetricsConfig(
            solution_version_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="solution_version_arn" type="string" required>
  The real Amazon Resource Name (ARN) of the solution version this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="metrics" type="map(number)">
  The real metrics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
