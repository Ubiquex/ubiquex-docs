---
title: "aws_personalize_data_deletion_jobs"
description: "`aws_personalize_data_deletion_jobs` looks up a real Personalize job deleting specific users' own data from a dataset group."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personalize "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/personalize"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_personalize_data_deletion_jobs"})

    		ubx.Data(personalize.DataDeletionJobs, "example", personalize.DataDeletionJobsConfig{
    			DatasetGroupArn: "arn:aws:iam::123456789012:policy/example",
    			MaxResults:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DataDeletionJobs } from "@ubx/sdk-aws/aws/data/personalize/data_deletion_jobs";

    export default stack("example", () => {
      intent({ summary: "look up aws_personalize_data_deletion_jobs" });

      data(DataDeletionJobs, "example", {
        datasetGroupArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.personalize import DataDeletionJobs, DataDeletionJobsConfig

    def describe():
        ubx.intent("look up aws_personalize_data_deletion_jobs")

        ubx.data(DataDeletionJobs, "example", DataDeletionJobsConfig(
            dataset_group_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_group_arn" type="string">
  The real Amazon Resource Name (ARN) of the dataset group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data_deletion_jobs" type="list(object)">
  The real data deletion jobs included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
