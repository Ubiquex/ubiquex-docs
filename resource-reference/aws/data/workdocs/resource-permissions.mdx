---
title: "aws_workdocs_resource_permissions"
description: "`aws_workdocs_resource_permissions` reads the real, shared permissions on a WorkDocs document or folder."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_resource_permissions"})

    		ubx.Data(workdocs.ResourcePermissions, "example", workdocs.ResourcePermissionsConfig{
    			ResourceId:          "example",
    			AuthenticationToken: "example",
    			Limit:               1,
    			PrincipalId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourcePermissions } from "@ubx/sdk-aws/aws/data/workdocs/resource_permissions";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_resource_permissions" });

      data(ResourcePermissions, "example", {
        resourceId: "example",
        authenticationToken: "example",
        limit: 1,
        principalId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import ResourcePermissions, ResourcePermissionsConfig

    def describe():
        ubx.intent("look up aws_workdocs_resource_permissions")

        ubx.data(ResourcePermissions, "example", ResourcePermissionsConfig(
            resource_id="example",
            authentication_token="example",
            limit=1,
            principal_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  The real, opaque authentication token used to authenticate this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="principal_id" type="string">
  The real, unique identifier of the principal this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="principals" type="list(object)">
  The real IAM principals included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
