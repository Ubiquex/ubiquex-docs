---
title: "aws_workdocs_document_path"
description: "`aws_workdocs_document_path` reads the real folder path of a WorkDocs document."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_document_path"})

    		ubx.Data(workdocs.DocumentPath, "example", workdocs.DocumentPathConfig{
    			DocumentId:          "example",
    			AuthenticationToken: "example",
    			Fields:              "example",
    			Limit:               1,
    			Marker:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { DocumentPath } from "./local-sdk/aws/sdk/typescript/aws/data/workdocs/document_path.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_document_path" });

      data(DocumentPath, "example", {
        documentId: "example",
        authenticationToken: "example",
        fields: "example",
        limit: 1,
        marker: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import DocumentPath, DocumentPathConfig

    def describe():
        ubx.intent("look up aws_workdocs_document_path")

        ubx.data(DocumentPath, "example", DocumentPathConfig(
            document_id="example",
            authentication_token="example",
            fields="example",
            limit=1,
            marker="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  The real, opaque authentication token used to authenticate this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  The real, unique identifier of the document this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fields" type="string">
  The real fields included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="path" type="object">
  &lt;p&gt;Describes the path information of a resource.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="components" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
