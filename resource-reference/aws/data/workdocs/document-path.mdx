---
title: "aws_workdocs_document_path"
description: "`aws_workdocs_document_path` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workdocs_document_path` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_document_path"})

    		ubx.Data(workdocs.DocumentPath, "example", workdocs.DocumentPathConfig{
    			DocumentId:          "example",
    			AuthenticationToken: "example",
    			Fields:              "example",
    			Limit:               1,
    			Marker:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentPath } from "@ubx/sdk-aws/aws/data/workdocs/document_path";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_document_path" });

      data(DocumentPath, "example", {
        documentId: "example",
        authenticationToken: "example",
        fields: "example",
        limit: 1,
        marker: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import DocumentPath, DocumentPathConfig

    def describe():
        ubx.intent("look up aws_workdocs_document_path")

        ubx.data(DocumentPath, "example", DocumentPathConfig(
            document_id="example",
            authentication_token="example",
            fields="example",
            limit=1,
            marker="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="fields" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="marker" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="path" type="object">
  &lt;p&gt;Describes the path information of a resource.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="components" type="list(object)">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
