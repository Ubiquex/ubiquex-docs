---
title: "aws_workdocs_folder_contents"
description: "`aws_workdocs_folder_contents` reads the real documents and subfolders within a WorkDocs folder."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_folder_contents"})

    		ubx.Data(workdocs.FolderContents, "example", workdocs.FolderContentsConfig{
    			FolderId:            "example",
    			AuthenticationToken: "example",
    			Include:             "example",
    			Limit:               1,
    			Order:               "example",
    			Sort:                "example",
    			Type:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FolderContents } from "@ubx/sdk-aws/aws/data/workdocs/folder_contents";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_folder_contents" });

      data(FolderContents, "example", {
        folderId: "example",
        authenticationToken: "example",
        include: "example",
        limit: 1,
        order: "example",
        sort: "example",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import FolderContents, FolderContentsConfig

    def describe():
        ubx.intent("look up aws_workdocs_folder_contents")

        ubx.data(FolderContents, "example", FolderContentsConfig(
            folder_id="example",
            authentication_token="example",
            include="example",
            limit=1,
            order="example",
            sort="example",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  The real, opaque authentication token used to authenticate this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="folder_id" type="string" required>
  The real, unique identifier of the folder this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  The real order results are returned in. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The real field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="documents" type="list(object)">
  The real documents included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="folders" type="list(object)">
  The real folders included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
