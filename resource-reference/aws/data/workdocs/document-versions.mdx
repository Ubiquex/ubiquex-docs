---
title: "aws_workdocs_document_versions"
description: "`aws_workdocs_document_versions` reads the real, saved versions of a WorkDocs document."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_document_versions"})

    		ubx.Data(workdocs.DocumentVersions, "example", workdocs.DocumentVersionsConfig{
    			DocumentId:          "example",
    			AuthenticationToken: "example",
    			Fields:              "example",
    			Include:             "example",
    			Limit:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentVersions } from "@ubx/sdk-aws/aws/data/workdocs/document_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_document_versions" });

      data(DocumentVersions, "example", {
        documentId: "example",
        authenticationToken: "example",
        fields: "example",
        include: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import DocumentVersions, DocumentVersionsConfig

    def describe():
        ubx.intent("look up aws_workdocs_document_versions")

        ubx.data(DocumentVersions, "example", DocumentVersionsConfig(
            document_id="example",
            authentication_token="example",
            fields="example",
            include="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  The real, opaque authentication token used to authenticate this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  The real, unique identifier of the document this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="fields" type="string">
  The real fields included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="document_versions" type="list(object)">
  The real versions of the document included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
