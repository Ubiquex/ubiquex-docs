---
title: "aws_workdocs_document_versions"
description: "`aws_workdocs_document_versions` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workdocs_document_versions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_document_versions"})

    		ubx.Data(workdocs.DocumentVersions, "example", workdocs.DocumentVersionsConfig{
    			DocumentId:          "example",
    			AuthenticationToken: "example",
    			Fields:              "example",
    			Include:             "example",
    			Limit:               1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DocumentVersions } from "@ubx/sdk-aws/aws/data/workdocs/document_versions";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_document_versions" });

      data(DocumentVersions, "example", {
        documentId: "example",
        authenticationToken: "example",
        fields: "example",
        include: "example",
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import DocumentVersions, DocumentVersionsConfig

    def describe():
        ubx.intent("look up aws_workdocs_document_versions")

        ubx.data(DocumentVersions, "example", DocumentVersionsConfig(
            document_id="example",
            authentication_token="example",
            fields="example",
            include="example",
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="document_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="fields" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="document_versions" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
