---
title: "aws_workdocs_current_user"
description: "`aws_workdocs_current_user` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workdocs_current_user` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_current_user"})

    		ubx.Data(workdocs.CurrentUser, "example", workdocs.CurrentUserConfig{
    			AuthenticationToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CurrentUser } from "@ubx/sdk-aws/aws/data/workdocs/current_user";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_current_user" });

      data(CurrentUser, "example", {
        authenticationToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import CurrentUser, CurrentUserConfig

    def describe():
        ubx.intent("look up aws_workdocs_current_user")

        ubx.data(CurrentUser, "example", CurrentUserConfig(
            authentication_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="user" type="object">
  &lt;p&gt;Describes a user.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="email_address" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="given_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="locale" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="modified_timestamp" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="organization_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="recycle_bin_folder_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="root_folder_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="storage" type="object">
      &lt;p&gt;Describes the storage for a user.&lt;/p&gt; Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="storage_rule" type="object">
          &lt;p&gt;Describes the storage for a user.&lt;/p&gt; Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="storage_allocated_in_bytes" type="number">
              Optional lookup argument.
            </ResponseField>
            <ResponseField name="storage_type" type="string">
              Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="storage_utilized_in_bytes" type="number">
          Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="surname" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="time_zone_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="username" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
