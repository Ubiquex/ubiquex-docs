---
title: "aws_workdocs_groups"
description: "`aws_workdocs_groups` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workdocs_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_groups"})

    		ubx.Data(workdocs.Groups, "example", workdocs.GroupsConfig{
    			SearchQuery:         "example",
    			AuthenticationToken: "example",
    			Limit:               1,
    			OrganizationId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Groups } from "@ubx/sdk-aws/aws/data/workdocs/groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_groups" });

      data(Groups, "example", {
        searchQuery: "example",
        authenticationToken: "example",
        limit: 1,
        organizationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import Groups, GroupsConfig

    def describe():
        ubx.intent("look up aws_workdocs_groups")

        ubx.data(Groups, "example", GroupsConfig(
            search_query="example",
            authentication_token="example",
            limit=1,
            organization_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search_query" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
