---
title: "aws_workdocs_resources"
description: "`aws_workdocs_resources` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workdocs_resources` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_resources"})

    		ubx.Data(workdocs.Resources, "example", workdocs.ResourcesConfig{
    			AuthenticationToken: "example",
    			CollectionType:      "example",
    			Limit:               1,
    			UserId:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Resources } from "@ubx/sdk-aws/aws/data/workdocs/resources";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_resources" });

      data(Resources, "example", {
        authenticationToken: "example",
        collectionType: "example",
        limit: 1,
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import Resources, ResourcesConfig

    def describe():
        ubx.intent("look up aws_workdocs_resources")

        ubx.data(Resources, "example", ResourcesConfig(
            authentication_token="example",
            collection_type="example",
            limit=1,
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="authentication_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="collection_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="documents" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="folders" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>
