---
title: "aws_workdocs_activities"
description: "`aws_workdocs_activities` reads real, recorded user activity events within a WorkDocs organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_activities"})

    		ubx.Data(workdocs.Activities, "example", workdocs.ActivitiesConfig{
    			ActivityTypes:             "example",
    			AuthenticationToken:       "example",
    			EndTime:                   "example",
    			IncludeIndirectActivities: true,
    			Limit:                     1,
    			OrganizationId:            "example",
    			ResourceId:                "example",
    			StartTime:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Activities } from "./local-sdk/aws/sdk/typescript/aws/data/workdocs/activities.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_activities" });

      data(Activities, "example", {
        activityTypes: "example",
        authenticationToken: "example",
        endTime: "example",
        includeIndirectActivities: true,
        limit: 1,
        organizationId: "example",
        resourceId: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import Activities, ActivitiesConfig

    def describe():
        ubx.intent("look up aws_workdocs_activities")

        ubx.data(Activities, "example", ActivitiesConfig(
            activity_types="example",
            authentication_token="example",
            end_time="example",
            include_indirect_activities=True,
            limit=1,
            organization_id="example",
            resource_id="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="activity_types" type="string">
  The real, discriminating types of activitys this request is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="authentication_token" type="string">
  The real, opaque authentication token used to authenticate this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_indirect_activities" type="bool">
  Whether indirect activities are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string">
  The real, unique identifier of the AWS Organizations organization this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  The real, unique identifier of the user this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_activities" type="list(object)">
  The real user activities included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
