---
title: "aws_workdocs_activities"
description: "`aws_workdocs_activities` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_workdocs_activities` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workdocs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/workdocs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_workdocs_activities"})

    		ubx.Data(workdocs.Activities, "example", workdocs.ActivitiesConfig{
    			ActivityTypes:             "example",
    			AuthenticationToken:       "example",
    			EndTime:                   "example",
    			IncludeIndirectActivities: true,
    			Limit:                     1,
    			OrganizationId:            "example",
    			ResourceId:                "example",
    			StartTime:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Activities } from "@ubx/sdk-aws/aws/data/workdocs/activities";

    export default stack("example", () => {
      intent({ summary: "look up aws_workdocs_activities" });

      data(Activities, "example", {
        activityTypes: "example",
        authenticationToken: "example",
        endTime: "example",
        includeIndirectActivities: true,
        limit: 1,
        organizationId: "example",
        resourceId: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.workdocs import Activities, ActivitiesConfig

    def describe():
        ubx.intent("look up aws_workdocs_activities")

        ubx.data(Activities, "example", ActivitiesConfig(
            activity_types="example",
            authentication_token="example",
            end_time="example",
            include_indirect_activities=True,
            limit=1,
            organization_id="example",
            resource_id="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="activity_types" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="authentication_token" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_indirect_activities" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="marker" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="user_activities" type="list(object)">
  Returned by the lookup.
</ResponseField>
