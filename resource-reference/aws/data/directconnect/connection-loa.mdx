---
title: "aws_directconnect_connection_loa"
description: "`aws_directconnect_connection_loa` reads the real Letter of Authorization (LOA-CFA) document for a Direct Connect connection."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_connection_loa"})

    		ubx.Data(directconnect.ConnectionLoa, "example", directconnect.ConnectionLoaConfig{
    			ConnectionId:   "example",
    			LoaContentType: "example",
    			ProviderName:   "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ConnectionLoa } from "./local-sdk/aws/sdk/typescript/aws/data/directconnect/connection_loa.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_connection_loa" });

      data(ConnectionLoa, "example", {
        connectionId: "example",
        loaContentType: "example",
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import ConnectionLoa, ConnectionLoaConfig

    def describe():
        ubx.intent("look up aws_directconnect_connection_loa")

        ubx.data(ConnectionLoa, "example", ConnectionLoaConfig(
            connection_id="example",
            loa_content_type="example",
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_id" type="string" required>
  The real, unique identifier of the connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="loa_content_type" type="string">
  The real, discriminating type of the loa content this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="provider_name" type="string">
  The real name of the provider this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="loa" type="object">
  &lt;p&gt;Information about a Letter of Authorization - Connecting Facility Assignment (LOA-CFA) for a connection.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="loa_content" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="loa_content_type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
