---
title: "aws_directconnect_virtual_gateways"
description: "`aws_directconnect_virtual_gateways` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_directconnect_virtual_gateways` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_virtual_gateways"})

    		ubx.Data(directconnect.VirtualGateways, "example", directconnect.VirtualGatewaysConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualGateways } from "@ubx/sdk-aws/aws/data/directconnect/virtual_gateways";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_virtual_gateways" });

      data(VirtualGateways, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import VirtualGateways, VirtualGatewaysConfig

    def describe():
        ubx.intent("look up aws_directconnect_virtual_gateways")

        ubx.data(VirtualGateways, "example", VirtualGatewaysConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="virtual_gateways" type="list(object)">
  Returned by the lookup.
</ResponseField>
