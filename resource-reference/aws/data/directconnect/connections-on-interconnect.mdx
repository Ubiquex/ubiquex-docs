---
title: "aws_directconnect_connections_on_interconnect"
description: "`aws_directconnect_connections_on_interconnect` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_directconnect_connections_on_interconnect` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_connections_on_interconnect"})

    		ubx.Data(directconnect.ConnectionsOnInterconnect, "example", directconnect.ConnectionsOnInterconnectConfig{
    			InterconnectId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectionsOnInterconnect } from "@ubx/sdk-aws/aws/data/directconnect/connections_on_interconnect";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_connections_on_interconnect" });

      data(ConnectionsOnInterconnect, "example", {
        interconnectId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import ConnectionsOnInterconnect, ConnectionsOnInterconnectConfig

    def describe():
        ubx.intent("look up aws_directconnect_connections_on_interconnect")

        ubx.data(ConnectionsOnInterconnect, "example", ConnectionsOnInterconnectConfig(
            interconnect_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="interconnect_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connections" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
