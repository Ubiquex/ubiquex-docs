---
title: "aws_directconnect_direct_connect_gateway_attachments"
description: "`aws_directconnect_direct_connect_gateway_attachments` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_directconnect_direct_connect_gateway_attachments` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_direct_connect_gateway_attachments"})

    		ubx.Data(directconnect.DirectConnectGatewayAttachments, "example", directconnect.DirectConnectGatewayAttachmentsConfig{
    			DirectConnectGatewayId: "example",
    			MaxResults:             1,
    			VirtualInterfaceId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DirectConnectGatewayAttachments } from "@ubx/sdk-aws/aws/data/directconnect/direct_connect_gateway_attachments";

    export default stack("example", () => {
      intent({
        summary: "look up aws_directconnect_direct_connect_gateway_attachments",
      });

      data(DirectConnectGatewayAttachments, "example", {
        directConnectGatewayId: "example",
        maxResults: 1,
        virtualInterfaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import DirectConnectGatewayAttachments, DirectConnectGatewayAttachmentsConfig

    def describe():
        ubx.intent("look up aws_directconnect_direct_connect_gateway_attachments")

        ubx.data(DirectConnectGatewayAttachments, "example", DirectConnectGatewayAttachmentsConfig(
            direct_connect_gateway_id="example",
            max_results=1,
            virtual_interface_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direct_connect_gateway_id" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="virtual_interface_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="direct_connect_gateway_attachments" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
