---
title: "aws_directconnect_customer_metadata"
description: "`aws_directconnect_customer_metadata` reads real, agreement metadata about a Direct Connect customer account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_customer_metadata"})

    		ubx.Data(directconnect.CustomerMetadata, "example", directconnect.CustomerMetadataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { CustomerMetadata } from "./local-sdk/aws/sdk/typescript/aws/data/directconnect/customer_metadata.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_customer_metadata" });

      data(CustomerMetadata, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import CustomerMetadata, CustomerMetadataConfig

    def describe():
        ubx.intent("look up aws_directconnect_customer_metadata")

        ubx.data(CustomerMetadata, "example", CustomerMetadataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="agreements" type="list(object)">
  The real agreements included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="nni_partner_type" type="string">
  The real, discriminating type of the nni partner this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
