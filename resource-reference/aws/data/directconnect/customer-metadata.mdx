---
title: "aws_directconnect_customer_metadata"
description: "`aws_directconnect_customer_metadata` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_directconnect_customer_metadata` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_customer_metadata"})

    		ubx.Data(directconnect.CustomerMetadata, "example", directconnect.CustomerMetadataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomerMetadata } from "@ubx/sdk-aws/aws/data/directconnect/customer_metadata";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_customer_metadata" });

      data(CustomerMetadata, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import CustomerMetadata, CustomerMetadataConfig

    def describe():
        ubx.intent("look up aws_directconnect_customer_metadata")

        ubx.data(CustomerMetadata, "example", CustomerMetadataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="agreements" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="nni_partner_type" type="string">
  Returned by the lookup.
</ResponseField>
