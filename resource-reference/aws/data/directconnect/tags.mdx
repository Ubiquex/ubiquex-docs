---
title: "aws_directconnect_tags"
description: "`aws_directconnect_tags` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_directconnect_tags` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_tags"})

    		ubx.Data(directconnect.Tags, "example", directconnect.TagsConfig{
    			ResourceArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Tags } from "@ubx/sdk-aws/aws/data/directconnect/tags";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_tags" });

      data(Tags, "example", {
        resourceArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import Tags, TagsConfig

    def describe():
        ubx.intent("look up aws_directconnect_tags")

        ubx.data(Tags, "example", TagsConfig(
            resource_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arns" type="list(string)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="resource_tags" type="list(object)">
  Returned by the lookup.
</ResponseField>
