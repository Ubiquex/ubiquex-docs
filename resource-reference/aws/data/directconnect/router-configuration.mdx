---
title: "aws_directconnect_router_configuration"
description: "`aws_directconnect_router_configuration` reads a real, generated router configuration template for a Direct Connect virtual interface."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_router_configuration"})

    		ubx.Data(directconnect.RouterConfiguration, "example", directconnect.RouterConfigurationConfig{
    			VirtualInterfaceId:   "example",
    			RouterTypeIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RouterConfiguration } from "@ubx/sdk-aws/aws/data/directconnect/router_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_router_configuration" });

      data(RouterConfiguration, "example", {
        virtualInterfaceId: "example",
        routerTypeIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import RouterConfiguration, RouterConfigurationConfig

    def describe():
        ubx.intent("look up aws_directconnect_router_configuration")

        ubx.data(RouterConfiguration, "example", RouterConfigurationConfig(
            virtual_interface_id="example",
            router_type_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="router_type_identifier" type="string">
  The real, unique identifier of the router type this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="virtual_interface_id" type="string" required>
  The real, unique identifier of the AWS Direct Connect virtual interface this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="customer_router_config" type="string">
  The real configuration of the customer router. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="router" type="object">
  &lt;p&gt;Information about the virtual router.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="platform" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="router_type_identifier" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="software" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="vendor" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="xslt_template_name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="xslt_template_name_for_mac_sec" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_interface_name" type="string">
  The real name of the virtual interface this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
