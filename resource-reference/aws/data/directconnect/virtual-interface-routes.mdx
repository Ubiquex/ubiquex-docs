---
title: "aws_directconnect_virtual_interface_routes"
description: "`aws_directconnect_virtual_interface_routes` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_directconnect_virtual_interface_routes` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_virtual_interface_routes"})

    		ubx.Data(directconnect.VirtualInterfaceRoutes, "example", directconnect.VirtualInterfaceRoutesConfig{
    			Filters:    map[string]any{"address_family": "example"},
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VirtualInterfaceRoutes } from "@ubx/sdk-aws/aws/data/directconnect/virtual_interface_routes";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_virtual_interface_routes" });

      data(VirtualInterfaceRoutes, "example", {
        filters: { addressFamily: "example" },
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import VirtualInterfaceRoutes, VirtualInterfaceRoutesConfig

    def describe():
        ubx.intent("look up aws_directconnect_virtual_interface_routes")

        ubx.data(VirtualInterfaceRoutes, "example", VirtualInterfaceRoutesConfig(
            filters={"address_family": "example"},
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="object">
  &lt;p&gt;The filters used to limit the routes returned by &lt;a&gt;ListVirtualInterfaceRoutes&lt;/a&gt;.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="address_family" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="as_path" type="list(number)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="cidrs" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="communities" type="list(string)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="route_direction" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="routes" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="virtual_interface_id" type="string">
  Returned by the lookup.
</ResponseField>
