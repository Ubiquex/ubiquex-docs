---
title: "aws_directconnect_loa"
description: "`aws_directconnect_loa` reads the real Letter of Authorization (LOA-CFA) document for a Direct Connect resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/directconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_directconnect_loa"})

    		ubx.Data(directconnect.Loa, "example", directconnect.LoaConfig{
    			ConnectionId: "example",
    			ProviderName: "example-provider-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Loa } from "@ubx/sdk-aws/aws/data/directconnect/loa";

    export default stack("example", () => {
      intent({ summary: "look up aws_directconnect_loa" });

      data(Loa, "example", {
        connectionId: "example",
        providerName: "example-provider-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.directconnect import Loa, LoaConfig

    def describe():
        ubx.intent("look up aws_directconnect_loa")

        ubx.data(Loa, "example", LoaConfig(
            connection_id="example",
            provider_name="example-provider-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_id" type="string" required>
  The real, unique identifier of the connection this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provider_name" type="string">
  The real name of the provider this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="loa_content" type="string">
  The real content of the loa. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="loa_content_type" type="string">
  The real, discriminating type of the loa content this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>
