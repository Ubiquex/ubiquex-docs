---
title: "aws_backup_search_search_job_results"
description: "`aws_backup_search_search_job_results` reads the real result rows of an AWS Backup Search job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup_search "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup_search"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_search_search_job_results"})

    		ubx.Data(backup_search.BackupSearchSearchJobResults, "example", backup_search.BackupSearchSearchJobResultsConfig{
    			SearchJobIdentifier: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { BackupSearchSearchJobResults } from "./local-sdk/aws/sdk/typescript/aws/data/backup_search/backup_search_search_job_results.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_search_search_job_results" });

      data(BackupSearchSearchJobResults, "example", {
        searchJobIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.backup_search import BackupSearchSearchJobResults, BackupSearchSearchJobResultsConfig

    def describe():
        ubx.intent("look up aws_backup_search_search_job_results")

        ubx.data(BackupSearchSearchJobResults, "example", BackupSearchSearchJobResultsConfig(
            search_job_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="search_job_identifier" type="string" required>
  The real, unique identifier of the search job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
