---
title: "aws_backup_search_search_job_backups"
description: "`aws_backup_search_search_job_backups` reads the real backups matched by an AWS Backup Search job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup_search "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup_search"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_search_search_job_backups"})

    		ubx.Data(backup_search.BackupSearchSearchJobBackups, "example", backup_search.BackupSearchSearchJobBackupsConfig{
    			SearchJobIdentifier: "example",
    			MaxResults:          1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupSearchSearchJobBackups } from "@ubx/sdk-aws/aws/data/backup_search/backup_search_search_job_backups";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_search_search_job_backups" });

      data(BackupSearchSearchJobBackups, "example", {
        searchJobIdentifier: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup_search import BackupSearchSearchJobBackups, BackupSearchSearchJobBackupsConfig

    def describe():
        ubx.intent("look up aws_backup_search_search_job_backups")

        ubx.data(BackupSearchSearchJobBackups, "example", BackupSearchSearchJobBackupsConfig(
            search_job_identifier="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="search_job_identifier" type="string" required>
  The real, unique identifier of the search job this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  The real, primary results of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
