---
title: "aws_backup_search_tags_for_resource"
description: "`aws_backup_search_tags_for_resource` reads the real tags applied to an AWS Backup Search resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup_search "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/backup_search"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_backup_search_tags_for_resource"})

    		ubx.Data(backup_search.BackupSearchTagsForResource, "example", backup_search.BackupSearchTagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BackupSearchTagsForResource } from "@ubx/sdk-aws/aws/data/backup_search/backup_search_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_backup_search_tags_for_resource" });

      data(BackupSearchTagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.backup_search import BackupSearchTagsForResource, BackupSearchTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_backup_search_tags_for_resource")

        ubx.data(BackupSearchTagsForResource, "example", BackupSearchTagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
