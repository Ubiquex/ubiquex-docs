---
title: "aws_cassandra_streams_records"
description: "`aws_cassandra_streams_records` reads real, captured change-data records from a Keyspaces (Cassandra) table stream."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cassandra_streams "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/cassandra_streams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cassandra_streams_records"})

    		ubx.Data(cassandra_streams.CassandraStreamsRecords, "example", cassandra_streams.CassandraStreamsRecordsConfig{
    			ShardIterator: "example",
    			MaxResults:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CassandraStreamsRecords } from "@ubx/sdk-aws/aws/data/cassandra_streams/cassandra_streams_records";

    export default stack("example", () => {
      intent({ summary: "look up aws_cassandra_streams_records" });

      data(CassandraStreamsRecords, "example", {
        shardIterator: "example",
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cassandra_streams import CassandraStreamsRecords, CassandraStreamsRecordsConfig

    def describe():
        ubx.intent("look up aws_cassandra_streams_records")

        ubx.data(CassandraStreamsRecords, "example", CassandraStreamsRecordsConfig(
            shard_iterator="example",
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="shard_iterator" type="string" required>
  An opaque iterator identifying a real position within a Kinesis shard. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="change_records" type="list(object)">
  The real change records included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="iterator_description" type="object">
  &lt;p&gt;Provides information about the current iterator.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="iterator_position" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="next_shard_iterator" type="string">
  An opaque iterator identifying the next real position within the Kinesis shard. **(AI-inferred)** Returned by the lookup.
</ResponseField>
