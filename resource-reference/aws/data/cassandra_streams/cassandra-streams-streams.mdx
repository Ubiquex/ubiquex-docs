---
title: "aws_cassandra_streams_streams"
description: "`aws_cassandra_streams_streams` reads the real change-data streams enabled on Keyspaces (Cassandra) tables."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cassandra_streams "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cassandra_streams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cassandra_streams_streams"})

    		ubx.Data(cassandra_streams.CassandraStreamsStreams, "example", cassandra_streams.CassandraStreamsStreamsConfig{
    			KeyspaceName: "example-keyspace-name",
    			MaxResults:   1,
    			TableName:    "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CassandraStreamsStreams } from "@ubx/sdk-aws/aws/data/cassandra_streams/cassandra_streams_streams";

    export default stack("example", () => {
      intent({ summary: "look up aws_cassandra_streams_streams" });

      data(CassandraStreamsStreams, "example", {
        keyspaceName: "example-keyspace-name",
        maxResults: 1,
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cassandra_streams import CassandraStreamsStreams, CassandraStreamsStreamsConfig

    def describe():
        ubx.intent("look up aws_cassandra_streams_streams")

        ubx.data(CassandraStreamsStreams, "example", CassandraStreamsStreamsConfig(
            keyspace_name="example-keyspace-name",
            max_results=1,
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="keyspace_name" type="string">
  The real name of the Amazon Keyspaces keyspace this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="table_name" type="string">
  The real name of the table this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="streams" type="list(object)">
  The real streams included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
