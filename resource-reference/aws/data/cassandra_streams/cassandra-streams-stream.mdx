---
title: "aws_cassandra_streams_stream"
description: "`aws_cassandra_streams_stream` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_cassandra_streams_stream` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cassandra_streams "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/cassandra_streams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_cassandra_streams_stream"})

    		ubx.Data(cassandra_streams.CassandraStreamsStream, "example", cassandra_streams.CassandraStreamsStreamConfig{
    			StreamArn:   "arn:aws:iam::123456789012:policy/example",
    			MaxResults:  1,
    			ShardFilter: map[string]any{"shard_id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CassandraStreamsStream } from "@ubx/sdk-aws/aws/data/cassandra_streams/cassandra_streams_stream";

    export default stack("example", () => {
      intent({ summary: "look up aws_cassandra_streams_stream" });

      data(CassandraStreamsStream, "example", {
        streamArn: "arn:aws:iam::123456789012:policy/example",
        maxResults: 1,
        shardFilter: { shardId: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.cassandra_streams import CassandraStreamsStream, CassandraStreamsStreamConfig

    def describe():
        ubx.intent("look up aws_cassandra_streams_stream")

        ubx.data(CassandraStreamsStream, "example", CassandraStreamsStreamConfig(
            stream_arn="arn:aws:iam::123456789012:policy/example",
            max_results=1,
            shard_filter={"shard_id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="shard_filter" type="object">
  &lt;p&gt;A filter used to limit the shards returned by a &lt;code&gt;GetStream&lt;/code&gt; operation.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="shard_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stream_arn" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="creation_request_date_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="keyspace_name" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="shards" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stream_label" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stream_status" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="stream_view_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="table_name" type="string">
  Returned by the lookup.
</ResponseField>
