---
title: "aws_xray_encryption_config"
description: "`aws_xray_encryption_config` reads the real, account-wide encryption configuration for X-Ray trace data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_encryption_config"})

    		ubx.Data(xray.EncryptionConfig, "example", xray.EncryptionConfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { EncryptionConfig } from "./local-sdk/aws/sdk/typescript/aws/data/xray/encryption_config.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_encryption_config" });

      data(EncryptionConfig, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import EncryptionConfig, EncryptionConfigConfig

    def describe():
        ubx.intent("look up aws_xray_encryption_config")

        ubx.data(EncryptionConfig, "example", EncryptionConfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="encryption_config" type="object">
  &lt;p&gt;A configuration document that specifies encryption configuration settings.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
