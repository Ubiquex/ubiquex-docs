---
title: "aws_xray_encryption_config"
description: "`aws_xray_encryption_config` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_xray_encryption_config` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_encryption_config"})

    		ubx.Data(xray.EncryptionConfig, "example", xray.EncryptionConfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EncryptionConfig } from "@ubx/sdk-aws/aws/data/xray/encryption_config";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_encryption_config" });

      data(EncryptionConfig, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import EncryptionConfig, EncryptionConfigConfig

    def describe():
        ubx.intent("look up aws_xray_encryption_config")

        ubx.data(EncryptionConfig, "example", EncryptionConfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="encryption_config" type="object">
  &lt;p&gt;A configuration document that specifies encryption configuration settings.&lt;/p&gt; Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="key_id" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
