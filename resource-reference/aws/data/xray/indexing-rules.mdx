---
title: "aws_xray_indexing_rules"
description: "`aws_xray_indexing_rules` reads the real indexing rules controlling which X-Ray trace attributes are searchable."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_indexing_rules"})

    		ubx.Data(xray.IndexingRules, "example", xray.IndexingRulesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { IndexingRules } from "./local-sdk/aws/sdk/typescript/aws/data/xray/indexing_rules.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_indexing_rules" });

      data(IndexingRules, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import IndexingRules, IndexingRulesConfig

    def describe():
        ubx.intent("look up aws_xray_indexing_rules")

        ubx.data(IndexingRules, "example", IndexingRulesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="indexing_rules" type="list(object)">
  The real rules for the indexing. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
