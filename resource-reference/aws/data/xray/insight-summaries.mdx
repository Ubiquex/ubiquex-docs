---
title: "aws_xray_insight_summaries"
description: "`aws_xray_insight_summaries` reads a real, summarized listing of X-Ray Insights."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_insight_summaries"})

    		ubx.Data(xray.InsightSummaries, "example", xray.InsightSummariesConfig{
    			EndTime:    "example",
    			StartTime:  "example",
    			GroupArn:   "arn:aws:iam::123456789012:policy/example",
    			GroupName:  "example-group-name",
    			MaxResults: 1,
    			States:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InsightSummaries } from "./local-sdk/aws/sdk/typescript/aws/data/xray/insight_summaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_insight_summaries" });

      data(InsightSummaries, "example", {
        endTime: "example",
        startTime: "example",
        groupArn: "arn:aws:iam::123456789012:policy/example",
        groupName: "example-group-name",
        maxResults: 1,
        states: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import InsightSummaries, InsightSummariesConfig

    def describe():
        ubx.intent("look up aws_xray_insight_summaries")

        ubx.data(InsightSummaries, "example", InsightSummariesConfig(
            end_time="example",
            start_time="example",
            group_arn="arn:aws:iam::123456789012:policy/example",
            group_name="example-group-name",
            max_results=1,
            states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="group_arn" type="string">
  The real Amazon Resource Name (ARN) of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_name" type="string">
  The real name of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="states" type="list(string)">
  The real, current states of the objects included in this result. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="insight_summaries" type="list(object)">
  The real, summarized insights included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
