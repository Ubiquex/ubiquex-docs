---
title: "aws_xray_insight_summaries"
description: "`aws_xray_insight_summaries` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_xray_insight_summaries` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_insight_summaries"})

    		ubx.Data(xray.InsightSummaries, "example", xray.InsightSummariesConfig{
    			EndTime:    "example",
    			StartTime:  "example",
    			GroupArn:   "arn:aws:iam::123456789012:policy/example",
    			GroupName:  "example-group-name",
    			MaxResults: 1,
    			States:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightSummaries } from "@ubx/sdk-aws/aws/data/xray/insight_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_insight_summaries" });

      data(InsightSummaries, "example", {
        endTime: "example",
        startTime: "example",
        groupArn: "arn:aws:iam::123456789012:policy/example",
        groupName: "example-group-name",
        maxResults: 1,
        states: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import InsightSummaries, InsightSummariesConfig

    def describe():
        ubx.intent("look up aws_xray_insight_summaries")

        ubx.data(InsightSummaries, "example", InsightSummariesConfig(
            end_time="example",
            start_time="example",
            group_arn="arn:aws:iam::123456789012:policy/example",
            group_name="example-group-name",
            max_results=1,
            states=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="group_arn" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_results" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="states" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="insight_summaries" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>
