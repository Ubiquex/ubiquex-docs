---
title: "aws_xray_insight_impact_graph"
description: "`aws_xray_insight_impact_graph` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_xray_insight_impact_graph` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_insight_impact_graph"})

    		ubx.Data(xray.InsightImpactGraph, "example", xray.InsightImpactGraphConfig{
    			EndTime:   "example",
    			InsightId: "example",
    			StartTime: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightImpactGraph } from "@ubx/sdk-aws/aws/data/xray/insight_impact_graph";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_insight_impact_graph" });

      data(InsightImpactGraph, "example", {
        endTime: "example",
        insightId: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import InsightImpactGraph, InsightImpactGraphConfig

    def describe():
        ubx.intent("look up aws_xray_insight_impact_graph")

        ubx.data(InsightImpactGraph, "example", InsightImpactGraphConfig(
            end_time="example",
            insight_id="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="insight_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_graph_end_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="service_graph_start_time" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="services" type="list(object)">
  Returned by the lookup.
</ResponseField>
