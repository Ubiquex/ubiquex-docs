---
title: "aws_xray_insight_impact_graph"
description: "`aws_xray_insight_impact_graph` reads the real service graph showing which downstream services an X-Ray Insight impacted."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_insight_impact_graph"})

    		ubx.Data(xray.InsightImpactGraph, "example", xray.InsightImpactGraphConfig{
    			EndTime:   "example",
    			InsightId: "example",
    			StartTime: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { InsightImpactGraph } from "./local-sdk/aws/sdk/typescript/aws/data/xray/insight_impact_graph.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_insight_impact_graph" });

      data(InsightImpactGraph, "example", {
        endTime: "example",
        insightId: "example",
        startTime: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import InsightImpactGraph, InsightImpactGraphConfig

    def describe():
        ubx.intent("look up aws_xray_insight_impact_graph")

        ubx.data(InsightImpactGraph, "example", InsightImpactGraphConfig(
            end_time="example",
            insight_id="example",
            start_time="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="insight_id" type="string" required>
  The real, unique identifier of the insight this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_graph_end_time" type="string">
  The real timestamp of the service graph end. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="service_graph_start_time" type="string">
  The real timestamp of the service graph start. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="services" type="list(object)">
  The real AWS services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
