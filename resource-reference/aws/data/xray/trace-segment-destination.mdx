---
title: "aws_xray_trace_segment_destination"
description: "`aws_xray_trace_segment_destination` reads the real, configured destination (X-Ray or CloudWatch Logs) traces are sent to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_trace_segment_destination"})

    		ubx.Data(xray.TraceSegmentDestination, "example", xray.TraceSegmentDestinationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TraceSegmentDestination } from "@ubx/sdk-aws/aws/data/xray/trace_segment_destination";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_trace_segment_destination" });

      data(TraceSegmentDestination, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import TraceSegmentDestination, TraceSegmentDestinationConfig

    def describe():
        ubx.intent("look up aws_xray_trace_segment_destination")

        ubx.data(TraceSegmentDestination, "example", TraceSegmentDestinationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="destination" type="string">
  The real destination of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
