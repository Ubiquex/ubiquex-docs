---
title: "aws_xray_trace_graph"
description: "`aws_xray_trace_graph` reads the real service graph for one specific X-Ray trace."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_trace_graph"})

    		ubx.Data(xray.TraceGraph, "example", xray.TraceGraphConfig{
    			TraceIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TraceGraph } from "./local-sdk/aws/sdk/typescript/aws/data/xray/trace_graph.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_trace_graph" });

      data(TraceGraph, "example", {
        traceIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import TraceGraph, TraceGraphConfig

    def describe():
        ubx.intent("look up aws_xray_trace_graph")

        ubx.data(TraceGraph, "example", TraceGraphConfig(
            trace_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="trace_ids" type="list(string)" required>
  The real, unique identifiers of the traces this request is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="services" type="list(object)">
  The real AWS services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
