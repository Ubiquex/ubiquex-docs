---
title: "aws_xray_sampling_targets"
description: "`aws_xray_sampling_targets` reads the real, computed sampling rates a service should apply for a given X-Ray sampling rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_sampling_targets"})

    		ubx.Data(xray.SamplingTargets, "example", xray.SamplingTargetsConfig{
    			SamplingStatisticsDocuments:      []map[string]any{{"borrow_count": 1}},
    			SamplingBoostStatisticsDocuments: []map[string]any{{"anomaly_count": 1}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SamplingTargets } from "@ubx/sdk-aws/aws/data/xray/sampling_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_sampling_targets" });

      data(SamplingTargets, "example", {
        samplingStatisticsDocuments: [{ borrowCount: 1 }],
        samplingBoostStatisticsDocuments: [{ anomalyCount: 1 }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import SamplingTargets, SamplingTargetsConfig
    from ubx.aws.data.xray.sampling_targets import SamplingTargets_SamplingStatisticsDocuments, SamplingTargets_SamplingBoostStatisticsDocuments

    def describe():
        ubx.intent("look up aws_xray_sampling_targets")

        ubx.data(SamplingTargets, "example", SamplingTargetsConfig(
            sampling_statistics_documents=[SamplingTargets_SamplingStatisticsDocuments(borrow_count=1)],
            sampling_boost_statistics_documents=[SamplingTargets_SamplingBoostStatisticsDocuments(anomaly_count=1)],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sampling_boost_statistics_documents" type="list(object)">
  The real documents for the sampling boost statistics. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sampling_statistics_documents" type="list(object)" required>
  The real documents for the sampling statistics. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_rule_modification" type="string">
  The real timestamp this rule was last modified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sampling_target_documents" type="list(object)">
  The real documents for the sampling target. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unprocessed_boost_statistics" type="list(object)">
  The real, unprocessed relevance-boost statistics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="unprocessed_statistics" type="list(object)">
  The real, unprocessed statistics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
