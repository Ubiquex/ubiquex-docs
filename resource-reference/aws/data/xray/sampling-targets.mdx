---
title: "aws_xray_sampling_targets"
description: "`aws_xray_sampling_targets` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_xray_sampling_targets` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_sampling_targets"})

    		ubx.Data(xray.SamplingTargets, "example", xray.SamplingTargetsConfig{
    			SamplingStatisticsDocuments:      []map[string]any{{"borrow_count": 1}},
    			SamplingBoostStatisticsDocuments: []map[string]any{{"anomaly_count": 1}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SamplingTargets } from "@ubx/sdk-aws/aws/data/xray/sampling_targets";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_sampling_targets" });

      data(SamplingTargets, "example", {
        samplingStatisticsDocuments: [{ borrowCount: 1 }],
        samplingBoostStatisticsDocuments: [{ anomalyCount: 1 }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import SamplingTargets, SamplingTargetsConfig

    def describe():
        ubx.intent("look up aws_xray_sampling_targets")

        ubx.data(SamplingTargets, "example", SamplingTargetsConfig(
            sampling_statistics_documents=[{"borrow_count": 1}],
            sampling_boost_statistics_documents=[{"anomaly_count": 1}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sampling_boost_statistics_documents" type="list(object)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sampling_statistics_documents" type="list(object)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_rule_modification" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="sampling_target_documents" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="unprocessed_boost_statistics" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="unprocessed_statistics" type="list(object)">
  Returned by the lookup.
</ResponseField>
