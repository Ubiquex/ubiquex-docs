---
title: "aws_xray_retrieved_traces_graph"
description: "`aws_xray_retrieved_traces_graph` reads the real service graph generated from an X-Ray Transaction Search retrieval job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_retrieved_traces_graph"})

    		ubx.Data(xray.RetrievedTracesGraph, "example", xray.RetrievedTracesGraphConfig{
    			RetrievalToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { RetrievedTracesGraph } from "./local-sdk/aws/sdk/typescript/aws/data/xray/retrieved_traces_graph.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_retrieved_traces_graph" });

      data(RetrievedTracesGraph, "example", {
        retrievalToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import RetrievedTracesGraph, RetrievedTracesGraphConfig

    def describe():
        ubx.intent("look up aws_xray_retrieved_traces_graph")

        ubx.data(RetrievedTracesGraph, "example", RetrievedTracesGraphConfig(
            retrieval_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="retrieval_token" type="string" required>
  The real, opaque retrieval token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="retrieval_status" type="string">
  The real, current status of the retrieval this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="services" type="list(object)">
  The real AWS services included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
