---
title: "aws_xray_resource_policies"
description: "`data_aws_xray_resource_policies` looks up a real, already-existing `aws_xray_resource_policy`."
---

It attaches a real resource-based access policy to X-Ray, controlling which principals, including from other accounts, can send trace data into it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_resource_policies"})

    		ubx.Data(xray.ResourcePolicies, "example", xray.ResourcePoliciesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { ResourcePolicies } from "./local-sdk/aws/sdk/typescript/aws/data/xray/resource_policies.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_resource_policies" });

      data(ResourcePolicies, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import ResourcePolicies, ResourcePoliciesConfig

    def describe():
        ubx.intent("look up aws_xray_resource_policies")

        ubx.data(ResourcePolicies, "example", ResourcePoliciesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="resource_policies" type="list(object)">
  The real, resource-based access policies attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
