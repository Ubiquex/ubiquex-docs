---
title: "aws_xray_time_series_service_statistics"
description: "`aws_xray_time_series_service_statistics` reads real, time-series statistics (latency, error rate) for a service in the X-Ray service graph."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_time_series_service_statistics"})

    		ubx.Data(xray.TimeSeriesServiceStatistics, "example", xray.TimeSeriesServiceStatisticsConfig{
    			EndTime:                  "example",
    			StartTime:                "example",
    			EntitySelectorExpression: "example",
    			ForecastStatistics:       true,
    			GroupArn:                 "arn:aws:iam::123456789012:policy/example",
    			GroupName:                "example-group-name",
    			Period:                   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { TimeSeriesServiceStatistics } from "./local-sdk/aws/sdk/typescript/aws/data/xray/time_series_service_statistics.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_time_series_service_statistics" });

      data(TimeSeriesServiceStatistics, "example", {
        endTime: "example",
        startTime: "example",
        entitySelectorExpression: "example",
        forecastStatistics: true,
        groupArn: "arn:aws:iam::123456789012:policy/example",
        groupName: "example-group-name",
        period: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import TimeSeriesServiceStatistics, TimeSeriesServiceStatisticsConfig

    def describe():
        ubx.intent("look up aws_xray_time_series_service_statistics")

        ubx.data(TimeSeriesServiceStatistics, "example", TimeSeriesServiceStatisticsConfig(
            end_time="example",
            start_time="example",
            entity_selector_expression="example",
            forecast_statistics=True,
            group_arn="arn:aws:iam::123456789012:policy/example",
            group_name="example-group-name",
            period=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="entity_selector_expression" type="string">
  The real expression used to select entities for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="forecast_statistics" type="bool">
  The real, computed statistics for this forecast. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_arn" type="string">
  The real Amazon Resource Name (ARN) of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_name" type="string">
  The real name of the group this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="period" type="number">
  The real time period this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="contains_old_group_versions" type="bool">
  The real versions of the contains old group included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="time_series_service_statistics" type="list(object)">
  The real, computed time-series service statistics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
