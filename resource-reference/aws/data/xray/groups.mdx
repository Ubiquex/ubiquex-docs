---
title: "aws_xray_groups"
description: "`data_aws_xray_groups` looks up a real, already-existing `aws_xray_group`."
---

It defines a real, named filter expression over X-Ray trace data, so traces matching specific criteria, an error rate threshold or a particular service, can be viewed and alarmed on as their own group rather than sifted out of the account's full trace volume by hand.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_groups"})

    		ubx.Data(xray.Groups, "example", xray.GroupsConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Groups } from "@ubx/sdk-aws/aws/data/xray/groups";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_groups" });

      data(Groups, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import Groups, GroupsConfig

    def describe():
        ubx.intent("look up aws_xray_groups")

        ubx.data(Groups, "example", GroupsConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="groups" type="list(object)">
  The real groups included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
