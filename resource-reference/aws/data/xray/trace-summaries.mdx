---
title: "aws_xray_trace_summaries"
description: "`aws_xray_trace_summaries` reads a real, summarized listing of X-Ray traces matching a filter expression."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_trace_summaries"})

    		ubx.Data(xray.TraceSummaries, "example", xray.TraceSummariesConfig{
    			EndTime:          "example",
    			StartTime:        "example",
    			FilterExpression: "example",
    			Sampling:         true,
    			SamplingStrategy: map[string]any{"name": "example-name"},
    			TimeRangeType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TraceSummaries } from "@ubx/sdk-aws/aws/data/xray/trace_summaries";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_trace_summaries" });

      data(TraceSummaries, "example", {
        endTime: "example",
        startTime: "example",
        filterExpression: "example",
        sampling: true,
        samplingStrategy: { name: "example-name" },
        timeRangeType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.xray import TraceSummaries, TraceSummariesConfig

    def describe():
        ubx.intent("look up aws_xray_trace_summaries")

        ubx.data(TraceSummaries, "example", TraceSummariesConfig(
            end_time="example",
            start_time="example",
            filter_expression="example",
            sampling=True,
            sampling_strategy={"name": "example-name"},
            time_range_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_time" type="string" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_expression" type="string">
  The real expression used to filter this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sampling" type="bool">
  The real, configured sampling behavior for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sampling_strategy" type="object">
  &lt;p&gt;The name and value of a sampling rule to apply to a trace summary.&lt;/p&gt; Optional lookup argument.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="value" type="number">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="start_time" type="string" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="time_range_type" type="string">
  The real, discriminating type of the time range this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approximate_time" type="string">
  The real timestamp of the approximate. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="trace_summaries" type="list(object)">
  The real, summarized traces included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="traces_processed_count" type="number">
  The real count of traces processeds. **(AI-inferred)** Returned by the lookup.
</ResponseField>
