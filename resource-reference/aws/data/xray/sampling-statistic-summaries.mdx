---
title: "aws_xray_sampling_statistic_summaries"
description: "`aws_xray_sampling_statistic_summaries` reads real, aggregated statistics on how X-Ray's own sampling rules are being applied."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_xray_sampling_statistic_summaries"})

    		ubx.Data(xray.SamplingStatisticSummaries, "example", xray.SamplingStatisticSummariesConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { SamplingStatisticSummaries } from "./local-sdk/aws/sdk/typescript/aws/data/xray/sampling_statistic_summaries.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_xray_sampling_statistic_summaries" });

      data(SamplingStatisticSummaries, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.xray import SamplingStatisticSummaries, SamplingStatisticSummariesConfig

    def describe():
        ubx.intent("look up aws_xray_sampling_statistic_summaries")

        ubx.data(SamplingStatisticSummaries, "example", SamplingStatisticSummariesConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sampling_statistic_summaries" type="list(object)">
  The real, summarized sampling statistics included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
