---
title: "aws_vpc_lattice_tags_for_resource"
description: "`aws_vpc_lattice_tags_for_resource` reads the real tags applied to a VPC Lattice resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc_lattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/vpc_lattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_vpc_lattice_tags_for_resource"})

    		ubx.Data(vpc_lattice.VpcLatticeTagsForResource, "example", vpc_lattice.VpcLatticeTagsForResourceConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcLatticeTagsForResource } from "@ubx/sdk-aws/aws/data/vpc_lattice/vpc_lattice_tags_for_resource";

    export default stack("example", () => {
      intent({ summary: "look up aws_vpc_lattice_tags_for_resource" });

      data(VpcLatticeTagsForResource, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.vpc_lattice import VpcLatticeTagsForResource, VpcLatticeTagsForResourceConfig

    def describe():
        ubx.intent("look up aws_vpc_lattice_tags_for_resource")

        ubx.data(VpcLatticeTagsForResource, "example", VpcLatticeTagsForResourceConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(string)">
  The real tags applied to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
