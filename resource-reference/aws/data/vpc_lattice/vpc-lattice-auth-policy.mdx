---
title: "aws_vpc_lattice_auth_policy"
description: "`aws_vpc_lattice_auth_policy` is a real, live lookup against AWS, generated from aws's own real schema -- read-only, never created or destroyed."
---

`aws_vpc_lattice_auth_policy` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against AWS's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc_lattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/vpc_lattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_vpc_lattice_auth_policy"})

    		ubx.Data(vpc_lattice.VpcLatticeAuthPolicy, "example", vpc_lattice.VpcLatticeAuthPolicyConfig{
    			ResourceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcLatticeAuthPolicy } from "@ubx/sdk-aws/aws/data/vpc_lattice/vpc_lattice_auth_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_vpc_lattice_auth_policy" });

      data(VpcLatticeAuthPolicy, "example", {
        resourceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.vpc_lattice import VpcLatticeAuthPolicy, VpcLatticeAuthPolicyConfig

    def describe():
        ubx.intent("look up aws_vpc_lattice_auth_policy")

        ubx.data(VpcLatticeAuthPolicy, "example", VpcLatticeAuthPolicyConfig(
            resource_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_identifier" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="policy" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Returned by the lookup.
</ResponseField>
