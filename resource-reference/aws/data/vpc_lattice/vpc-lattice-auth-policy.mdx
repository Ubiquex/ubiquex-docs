---
title: "aws_vpc_lattice_auth_policy"
description: "`aws_vpc_lattice_auth_policy` reads the real authentication and authorization policy attached to a VPC Lattice service or service network."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	vpc_lattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/vpc_lattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_vpc_lattice_auth_policy"})

    		ubx.Data(vpc_lattice.VpcLatticeAuthPolicy, "example", vpc_lattice.VpcLatticeAuthPolicyConfig{
    			ResourceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { VpcLatticeAuthPolicy } from "./local-sdk/aws/sdk/typescript/aws/data/vpc_lattice/vpc_lattice_auth_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_vpc_lattice_auth_policy" });

      data(VpcLatticeAuthPolicy, "example", {
        resourceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.vpc_lattice import VpcLatticeAuthPolicy, VpcLatticeAuthPolicyConfig

    def describe():
        ubx.intent("look up aws_vpc_lattice_auth_policy")

        ubx.data(VpcLatticeAuthPolicy, "example", VpcLatticeAuthPolicyConfig(
            resource_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_identifier" type="string" required>
  The real, unique identifier of the resource this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
