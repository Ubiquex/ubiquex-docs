---
title: "aws_vpc_lattice_resource_policy"
description: "`aws_vpc_lattice_resource_policy` reads the real resource-based policy attached to a VPC Lattice resource."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc_lattice "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/vpc_lattice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_vpc_lattice_resource_policy"})

    		ubx.Data(vpc_lattice.VpcLatticeResourcePolicy, "example", vpc_lattice.VpcLatticeResourcePolicyConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VpcLatticeResourcePolicy } from "@ubx/sdk-aws/aws/data/vpc_lattice/vpc_lattice_resource_policy";

    export default stack("example", () => {
      intent({ summary: "look up aws_vpc_lattice_resource_policy" });

      data(VpcLatticeResourcePolicy, "example", {
        resourceArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.vpc_lattice import VpcLatticeResourcePolicy, VpcLatticeResourcePolicyConfig

    def describe():
        ubx.intent("look up aws_vpc_lattice_resource_policy")

        ubx.data(VpcLatticeResourcePolicy, "example", VpcLatticeResourcePolicyConfig(
            resource_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_arn" type="string" required>
  The real Amazon Resource Name (ARN) this lookup is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="policy" type="string">
  The real access policy attached to this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
