---
title: "aws_appconfigdata_latest_configuration"
description: "`aws_appconfigdata_latest_configuration` reads the real, latest deployed configuration content an AppConfig client currently receives."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfigdata "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/appconfigdata"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_appconfigdata_latest_configuration"})

    		ubx.Data(appconfigdata.LatestConfiguration, "example", appconfigdata.LatestConfigurationConfig{
    			ConfigurationToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LatestConfiguration } from "@ubx/sdk-aws/aws/data/appconfigdata/latest_configuration";

    export default stack("example", () => {
      intent({ summary: "look up aws_appconfigdata_latest_configuration" });

      data(LatestConfiguration, "example", {
        configurationToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.appconfigdata import LatestConfiguration, LatestConfigurationConfig

    def describe():
        ubx.intent("look up aws_appconfigdata_latest_configuration")

        ubx.data(LatestConfiguration, "example", LatestConfigurationConfig(
            configuration_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="configuration_token" type="string" required>
  The real, opaque configuration token. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration" type="string">
  The real, structured configuration of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="content_type" type="string">
  The real MIME content type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_poll_configuration_token" type="string">
  The real, opaque next poll configuration token. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_poll_interval_in_seconds" type="number">
  The real, recommended interval before polling again, in seconds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="version_label" type="string">
  The real, human-readable label of this object's own version. **(AI-inferred)** Returned by the lookup.
</ResponseField>
