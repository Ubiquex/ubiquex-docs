---
title: "aws_airflow_environments"
description: "`aws_airflow_environments` looks up a real Managed Workflows for Apache Airflow (MWAA) environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	airflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/airflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_airflow_environments"})

    		ubx.Data(airflow.Environments, "example", airflow.EnvironmentsConfig{
    			MaxResults: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Environments } from "@ubx/sdk-aws/aws/data/airflow/environments";

    export default stack("example", () => {
      intent({ summary: "look up aws_airflow_environments" });

      data(Environments, "example", {
        maxResults: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.airflow import Environments, EnvironmentsConfig

    def describe():
        ubx.intent("look up aws_airflow_environments")

        ubx.data(Environments, "example", EnvironmentsConfig(
            max_results=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="max_results" type="number">
  The maximum number of results to return in a single page. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="environments" type="list(string)">
  The real environments included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
