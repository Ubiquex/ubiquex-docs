---
title: "aws_events_connections"
description: "`data_aws_events_connections` looks up a real, already-existing `aws_events_connection`."
---

It stores the real authorization credentials, API key, OAuth, basic auth, that an `aws_events_api_destination` uses to actually authenticate its calls, kept separate so the same credentials can be reused across multiple destinations.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	events "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/data/events"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_events_connections"})

    		ubx.Data(events.Connections, "example", events.ConnectionsConfig{
    			ConnectionState: "example",
    			Limit:           1,
    			NamePrefix:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { Connections } from "./local-sdk/aws/sdk/typescript/aws/data/events/connections.ts";

    export default stack("example", () => {
      intent({ summary: "look up aws_events_connections" });

      data(Connections, "example", {
        connectionState: "example",
        limit: 1,
        namePrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.data.events import Connections, ConnectionsConfig

    def describe():
        ubx.intent("look up aws_events_connections")

        ubx.data(Connections, "example", ConnectionsConfig(
            connection_state="example",
            limit=1,
            name_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="connection_state" type="string">
  The real, current state of the connection this lookup is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Restricts results to objects whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="connections" type="list(object)">
  The real connections included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>
