---
title: "aws_events_replays"
description: "`aws_events_replays` reads the real EventBridge replays run against an archive."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	events "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/data/events"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up aws_events_replays"})

    		ubx.Data(events.Replays, "example", events.ReplaysConfig{
    			EventSourceArn: "arn:aws:iam::123456789012:policy/example",
    			Limit:          1,
    			NamePrefix:     "example",
    			State:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Replays } from "@ubx/sdk-aws/aws/data/events/replays";

    export default stack("example", () => {
      intent({ summary: "look up aws_events_replays" });

      data(Replays, "example", {
        eventSourceArn: "arn:aws:iam::123456789012:policy/example",
        limit: 1,
        namePrefix: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.data.events import Replays, ReplaysConfig

    def describe():
        ubx.intent("look up aws_events_replays")

        ubx.data(Replays, "example", ReplaysConfig(
            event_source_arn="arn:aws:iam::123456789012:policy/example",
            limit=1,
            name_prefix="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="event_source_arn" type="string">
  The real Amazon Resource Name (ARN) of the event source this object was derived from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Restricts results to objects whose name starts with this prefix. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="next_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="replays" type="list(object)">
  The real EventBridge event replays included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
