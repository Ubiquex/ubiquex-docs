---
title: "aws_events_rule"
description: "`data_aws_events_rule` looks up a real, already-existing `aws_events_rule`."
---

It matches real events flowing through an `aws_events_event_bus` against a defined pattern, or fires on a schedule, and routes matching events to one or more real targets, a Lambda function, an SQS queue, a Step Functions state machine.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	events "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/events"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rule"})

    		ubx.Resource(events.Rule, "example", events.RuleConfig{
    			Name:               "example",
    			Description:        "Managed by ubx.",
    			EventBusName:       "example-event-bus-name",
    			EventPattern:       "example",
    			RoleArn:            "arn:aws:iam::123456789012:policy/example",
    			ScheduleExpression: "example",
    			State:              "example",
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Rule } from "@ubx/sdk-aws/aws/events/rule";

    export default stack("example", () => {
      intent({ summary: "example own rule" });

      resource(Rule, "example", {
        name: "example",
        description: "Managed by ubx.",
        eventBusName: "example-event-bus-name",
        eventPattern: "example",
        roleArn: "arn:aws:iam::123456789012:policy/example",
        scheduleExpression: "example",
        state: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.events import Rule, RuleConfig
    from ubx.aws.events.rule import Rule_Tags

    def describe():
        ubx.intent("example own rule")

        ubx.resource(Rule, "example", RuleConfig(
            name="example",
            description="Managed by ubx.",
            event_bus_name="example-event-bus-name",
            event_pattern="example",
            role_arn="arn:aws:iam::123456789012:policy/example",
            schedule_expression="example",
            state="example",
            tags=[Rule_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_events_rule called "example", with description Managed
    by ubx., event bus name example-event-bus-name, event pattern
    example, role arn arn:aws:iam::123456789012:policy/example,
    schedule expression example, state example, tags a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string" required>
  The real, given name of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="arn" type="string">
  The real Amazon Resource Name (ARN) of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_by" type="string">
  The real identity that created this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_bus_name" type="string">
  The real name of the event bus this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="event_pattern" type="string">
  The real, JSON-formatted event pattern used to match events. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="managed_by" type="string">
  The real identity or service that manages this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="role_arn" type="string">
  The real Amazon Resource Name (ARN) of the IAM role this lookup is scoped to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="schedule_expression" type="string">
  The real schedule expression (rate or cron) this object runs on. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
