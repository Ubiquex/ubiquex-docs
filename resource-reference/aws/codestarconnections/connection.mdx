---
title: "aws_code_star_connections_connection"
description: "`aws_code_star_connections_connection` is the pre-rename CloudFormation resource type for what's now `aws_code_connections_connection`..."
---

`aws_code_star_connections_connection` is the pre-rename CloudFormation resource type for what's now `aws_code_connections_connection`, AWS CodeStar Connections having been renamed AWS CodeConnections in March 2024. Existing connections and this resource type continue to work; the old APIs stopped being available after April 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestarconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/codestarconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connection"})

    		ubx.Resource(codestarconnections.Connection, "example", codestarconnections.ConnectionConfig{
    			ConnectionName: "example-connection-name",
    			HostArn:        "arn:aws:iam::123456789012:policy/example",
    			ProviderType:   "example",
    			Tags:           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Connection } from "@ubx/sdk-aws/aws/codestarconnections/connection";

    export default stack("example", () => {
      intent({ summary: "example own connection" });

      resource(Connection, "example", {
        connectionName: "example-connection-name",
        hostArn: "arn:aws:iam::123456789012:policy/example",
        providerType: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codestarconnections import Connection, ConnectionConfig
    from ubx.aws.codestarconnections.connection import Connection_Tags

    def describe():
        ubx.intent("example own connection")

        ubx.resource(Connection, "example", ConnectionConfig(
            connection_name="example-connection-name",
            host_arn="arn:aws:iam::123456789012:policy/example",
            provider_type="example",
            tags=[Connection_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_code_star_connections_connection, with connection name
    example-connection-name, host arn
    arn:aws:iam::123456789012:policy/example, provider type example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connection_name" type="string" required>
  The name of the connection. Connection names must be unique in an AWS user account. Required.
</ResponseField>

<ResponseField name="host_arn" type="string">
  The host arn configured to represent the infrastructure where your third-party provider is installed. You must specify either a ProviderType or a HostArn. Optional.
</ResponseField>

<ResponseField name="provider_type" type="string">
  The name of the external provider where your third-party code repository is configured. You must specify either a ProviderType or a HostArn. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the tags applied to a connection. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS CodeStar Connections connection, used to categorize and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a tag attached to the AWS CodeStar Connections connection, which can be used to categorize and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="connection_arn" type="string">
  The Amazon Resource Name (ARN) of the connection. The ARN is used as the connection reference when the connection is shared between AWS services. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connection_status" type="string">
  The current status of the connection. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  The name of the external provider where your third-party code repository is configured. For Bitbucket, this is the account ID of the owner of the Bitbucket repository. Computed by AWS after `ship`.
</ResponseField>
