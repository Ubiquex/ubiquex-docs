---
title: "aws_codestarconnections_host"
description: "Real, generated bindings for `aws_codestarconnections_host`."
---

`aws_codestarconnections_host` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codestarconnections"

    cfg := codestarconnections.HostConfig{
        Name: "example-name",
        ProviderEndpoint: "example",
        ProviderType: "example",
    }
    ubx.Resource(codestarconnections.Host, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Host } from
      "jsr:@ubx/sdk-aws/aws/codestarconnections/host";

    resource(Host, "example", {
      name: "example",
      providerEndpoint: "example",
      providerType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codestarconnections import Host, HostConfig

    cfg = HostConfig(
        name="example",
        provider_endpoint="example",
        provider_type="example",
    )
    ubx.resource(Host, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codestarconnections_host with name,
provider_endpoint, provider_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_endpoint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
