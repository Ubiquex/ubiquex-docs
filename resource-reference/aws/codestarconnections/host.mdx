---
title: "aws_codestarconnections_host"
description: "Real, generated bindings for `aws_codestarconnections_host`."
---

`aws_codestarconnections_host` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codestarconnections "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codestarconnections"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own host"})

    		ubx.Resource(codestarconnections.Host, "ci-runner", codestarconnections.HostConfig{
    			Name:             "ci-runner",
    			ProviderEndpoint: "example",
    			ProviderType:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Host } from "jsr:@ubx/sdk-aws/aws/codestarconnections/host";

    export default stack("payments", () => {
      intent({ summary: "payments own host" });

      resource(Host, "ci-runner", {
        name: "ci-runner",
        providerEndpoint: "example",
        providerType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codestarconnections import Host, HostConfig

    def describe():
        ubx.intent("payments own host")

        ubx.resource(Host, "ci-runner", HostConfig(
            name="ci-runner",
            provider_endpoint="example",
            provider_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codestarconnections_host called "ci-runner", with
    provider endpoint example, provider type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_endpoint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
