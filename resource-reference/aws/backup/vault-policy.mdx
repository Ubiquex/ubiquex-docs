---
title: "aws_backup_vault_policy"
description: "Real, generated bindings for `aws_backup_vault_policy`."
---

`aws_backup_vault_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/backup"

    cfg := backup.VaultPolicyConfig{
        BackupVaultName: "example-backup-vault-name",
        Policy: "example",
    }
    ubx.Resource(backup.VaultPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VaultPolicy } from
      "jsr:@ubx/sdk-aws/aws/backup/vault_policy";

    resource(VaultPolicy, "example", {
      backupVaultName: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import VaultPolicy, VaultPolicyConfig

    cfg = VaultPolicyConfig(
        backup_vault_name="example",
        policy="example",
    )
    ubx.resource(VaultPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_vault_policy with backup_vault_name, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="backup_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="backup_vault_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
