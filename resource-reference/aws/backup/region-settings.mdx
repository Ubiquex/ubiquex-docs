---
title: "aws_backup_region_settings"
description: "Real, generated bindings for `aws_backup_region_settings`."
---

`aws_backup_region_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"

    cfg := backup.RegionSettingsConfig{
        ResourceTypeOptInPreference: map[string]string{"key": "value"},
        ResourceTypeManagementPreference: map[string]string{"key": "value"},
    }
    ubx.Resource(backup.RegionSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegionSettings } from
      "jsr:@ubx/sdk-aws/aws/backup/region_settings";

    resource(RegionSettings, "example", {
      resourceTypeOptInPreference: { key: "value" },
      resourceTypeManagementPreference: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import RegionSettings, RegionSettingsConfig

    cfg = RegionSettingsConfig(
        resource_type_opt_in_preference={"key": "value"},
        resource_type_management_preference={"key": "value"},
    )
    ubx.resource(RegionSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_region_settings with
resource_type_opt_in_preference,
resource_type_management_preference.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type_management_preference" type="map(bool)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type_opt_in_preference" type="map(bool)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type_management_preference" type="map(bool)">
  Optional; if omitted, computed by AWS.
</ResponseField>
