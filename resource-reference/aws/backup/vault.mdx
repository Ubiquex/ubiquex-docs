---
title: "aws_backup_vault"
description: "Real, generated bindings for `aws_backup_vault`."
---

`aws_backup_vault` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"

    cfg := backup.VaultConfig{
        Name: "example-name",
        ForceDestroy: true,
        KmsKeyArn: "example",
    }
    ubx.Resource(backup.Vault, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Vault } from "jsr:@ubx/sdk-aws/aws/backup/vault";

    resource(Vault, "example", {
      name: "example",
      forceDestroy: true,
      kmsKeyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import Vault, VaultConfig

    cfg = VaultConfig(
        name="example",
        force_destroy=True,
        kms_key_arn="example",
    )
    ubx.resource(Vault, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_vault with name, force_destroy, kms_key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="recovery_points" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
