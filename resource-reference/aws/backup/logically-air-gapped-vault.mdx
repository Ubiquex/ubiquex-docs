---
title: "aws_backup_logically_air_gapped_vault"
description: "Real, generated bindings for `aws_backup_logically_air_gapped_vault`."
---

`aws_backup_logically_air_gapped_vault` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/backup"

    cfg := backup.LogicallyAirGappedVaultConfig{
        MaxRetentionDays: 1,
        MinRetentionDays: 1,
        Name: "example-name",
        EncryptionKeyArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(backup.LogicallyAirGappedVault, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogicallyAirGappedVault } from
      "jsr:@ubx/sdk-aws/aws/backup/logically_air_gapped_vault";

    resource(LogicallyAirGappedVault, "example", {
      maxRetentionDays: 1,
      minRetentionDays: 1,
      name: "example",
      encryptionKeyArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import (
        LogicallyAirGappedVault,
        LogicallyAirGappedVaultConfig,
    )

    cfg = LogicallyAirGappedVaultConfig(
        max_retention_days=1,
        min_retention_days=1,
        name="example",
        encryption_key_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(LogicallyAirGappedVault, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_logically_air_gapped_vault with
max_retention_days, min_retention_days, name, encryption_key_arn,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="encryption_key_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_retention_days" type="number" required>
  Required.
</ResponseField>

<ResponseField name="min_retention_days" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
