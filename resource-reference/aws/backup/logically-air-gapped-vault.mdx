---
title: "aws_backup_logically_air_gapped_vault"
description: "Real, generated bindings for `aws_backup_logically_air_gapped_vault`."
---

`aws_backup_logically_air_gapped_vault` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own logically air gapped vault"})

    		ubx.Resource(backup.LogicallyAirGappedVault, "ci-runner", backup.LogicallyAirGappedVaultConfig{
    			MaxRetentionDays: 1,
    			MinRetentionDays: 1,
    			Name:             "ci-runner",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogicallyAirGappedVault } from "jsr:@ubx/sdk-aws/aws/backup/logically_air_gapped_vault";

    export default stack("payments", () => {
      intent({ summary: "payments own logically air gapped vault" });

      resource(LogicallyAirGappedVault, "ci-runner", {
        maxRetentionDays: 1,
        minRetentionDays: 1,
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import LogicallyAirGappedVault, LogicallyAirGappedVaultConfig

    def describe():
        ubx.intent("payments own logically air gapped vault")

        ubx.resource(LogicallyAirGappedVault, "ci-runner", LogicallyAirGappedVaultConfig(
            max_retention_days=1,
            min_retention_days=1,
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_logically_air_gapped_vault called "ci-runner",
    with max retention days 1, min retention days 1, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="encryption_key_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_retention_days" type="number" required>
  Required.
</ResponseField>

<ResponseField name="min_retention_days" type="number" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
