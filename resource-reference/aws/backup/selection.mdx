---
title: "aws_backup_selection"
description: "Real, generated bindings for `aws_backup_selection`."
---

`aws_backup_selection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/backup"

    cfg := backup.SelectionConfig{
        IamRoleArn: "example",
        Name: "example-name",
        PlanId: "example",
        NotResources: []string{"example"},
        Resources: []string{"example"},
    }
    ubx.Resource(backup.Selection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Selection } from
      "jsr:@ubx/sdk-aws/aws/backup/selection";

    resource(Selection, "example", {
      iamRoleArn: "example",
      name: "example",
      planId: "example",
      notResources: ["example"],
      resources: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import Selection, SelectionConfig

    cfg = SelectionConfig(
        iam_role_arn="example",
        name="example",
        plan_id="example",
        not_resources=["example"],
        resources=["example"],
    )
    ubx.resource(Selection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_selection with iam_role_arn, name, plan_id,
not_resources, resources.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="not_resources" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="plan_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resources" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="not_resources" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
