---
title: "aws_backup_selection"
description: "Real, generated bindings for `aws_backup_selection`."
---

`aws_backup_selection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own selection"})

    		ubx.Resource(backup.Selection, "ci-runner", backup.SelectionConfig{
    			IamRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			Name:       "ci-runner",
    			PlanId:     "example",
    			Resources:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Selection } from "jsr:@ubx/sdk-aws/aws/backup/selection";

    export default stack("payments", () => {
      intent({ summary: "payments own selection" });

      resource(Selection, "ci-runner", {
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        planId: "example",
        resources: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import Selection, SelectionConfig

    def describe():
        ubx.intent("payments own selection")

        ubx.resource(Selection, "ci-runner", SelectionConfig(
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            plan_id="example",
            resources=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_selection called "ci-runner", with iam role arn
    arn:aws:iam::123456789012:policy/ci, plan id example, resources a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="not_resources" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="plan_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resources" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="not_resources" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
