---
title: "aws_backup_plan"
description: "`aws_backup_plan` defines a backup policy, schedule, lifecycle, and target `aws_backup_vault`..."
---

`aws_backup_plan` defines a backup policy, schedule, lifecycle, and target `aws_backup_vault`, that gets applied to AWS resources through an `aws_backup_selection`, the real, centrally managed alternative to writing custom per-service backup scripts.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own plan"})

    		ubx.Resource(backup.Plan, "ci-runner", backup.PlanConfig{
    			BackupPlan:     map[string]any{"backup_plan_name": "example-backup-plan-name"},
    			BackupPlanTags: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Plan } from "./local-sdk/aws/sdk/typescript/aws/backup/plan.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own plan" });

      resource(Plan, "ci-runner", {
        backupPlan: { backupPlanName: "example-backup-plan-name" },
        backupPlanTags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.backup import Plan, PlanConfig

    def describe():
        ubx.intent("payments own plan")

        ubx.resource(Plan, "ci-runner", PlanConfig(
            backup_plan={"backup_plan_name": "example-backup-plan-name"},
            backup_plan_tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_plan, with backup plan a set value, backup plan
    tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_plan" type="object" required>
  Provides the backup plan configuration, including the plan name and the set of backup rules that define the backup schedule, lifecycle, and vault. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advanced_backup_settings" type="list(object)">
      Specifies backup options for specific resource types, such as enabling Windows VSS for EC2 instances, by associating a resource type with backup option key-value pairs. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="backup_options" type="any">
          Specifies the key-value pairs that configure advanced backup settings for the associated resource type, such as enabling Windows VSS for Windows workloads via a key like `WindowsVSS` set to `enabled`. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_type" type="string">
          Specifies the AWS resource type (such as EC2, RDS, or DynamoDB) to which these advanced backup settings apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="backup_plan_name" type="string" required>
      The user-defined name for the backup plan, used to identify it in AWS Backup and required when creating the plan. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="backup_plan_rule" type="list(object)" required>
      Specifies the list of backup rules, each defining the backup schedule, lifecycle (transition to cold storage or expiration), and target backup vault for the AWS Backup plan. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="completion_window_minutes" type="number">
          Specifies the time window, in minutes, within which the backup job must complete; if the job exceeds this duration, AWS Backup cancels it. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="copy_actions" type="list(object)">
          Configures the copy actions, each of which copies a backup created by this rule to a separate destination Backup vault (optionally applying a different lifecycle). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="destination_backup_vault_arn" type="string">
              Specifies the Amazon Resource Name (ARN) of the backup vault to which the backup copy is sent when a backup rule's copy action is executed. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="lifecycle" type="object">
              For each copy action in a backup plan rule, this lifecycle object specifies when the copied recovery point transitions to cold storage (MoveToColdStorageAfterDays) and when it expires and is deleted (DeleteAfterDays). Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="delete_after_days" type="number">
                  For a copy action in a backup plan rule, this specifies the number of days after the copied recovery point is created before it is permanently deleted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="move_to_cold_storage_after_days" type="number">
                  For each copied recovery point, the number of days after creation when AWS Backup moves it to cold storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="opt_in_to_archive_for_supported_resources" type="bool">
                  When set to true, this boolean opts the backup copy created by this copy action into transitioning to the archive storage tier for supported resources, based on the lifecycle's archive-after days setting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_continuous_backup" type="bool">
          Specifies whether to enable continuous backup for the rule, which provides point-in-time recovery (PITR) with a retention window of up to 35 days. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="index_actions" type="list(object)">
          Configures backup indexing for the rule by specifying the resource types whose backups will be indexed, enabling fast search and query of backup contents. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="resource_types" type="list(string)">
              Defines the AWS resource types to index for backup search (for example, S3), enabling item-level querying and recovery from the backup. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="lifecycle" type="object">
          Defines when backups expire and optionally move to cold storage, via DeleteAfterDays and MoveToColdStorageAfterDays settings for the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="delete_after_days" type="number">
              Specifies the number of days after creation that a recovery point is automatically deleted. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="move_to_cold_storage_after_days" type="number">
              Specifies the number of days after creation that a backup recovery point is moved to cold storage, allowing cheaper storage for older backups. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="opt_in_to_archive_for_supported_resources" type="bool">
              When set to true, this boolean enables automatic archiving of backups to cold storage for resource types that support archiving, as part of the backup plan rule's lifecycle configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="recovery_point_tags" type="any">
          Specifies the key-value tags that AWS Backup applies to recovery points created by this backup plan rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rule_name" type="string">
          A human-readable name for the backup rule that uniquely identifies it within the backup plan. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scan_actions" type="list(object)">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="malware_scanner" type="string">
              Enables or disables the malware scan action on backups created under this backup plan rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scan_mode" type="string">
              Specifies the mode for scanning backups, with value 'ON' to enable malware scanning or 'OFF' to disable it for the associated backup rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schedule_expression" type="string">
          Defines the schedule for the backup rule using a cron or rate expression, such as `cron(0 5 * * ? *)` or `rate(12 hours)`. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="schedule_expression_timezone" type="string">
          Specifies the timezone (e.g., America/New_York or Etc/UTC) in which the backup rule's schedule expression is evaluated, determining when the backup is scheduled to run. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_window_minutes" type="number">
          The number of minutes before the scheduled backup start time that AWS Backup can wait before starting the backup, providing a window to accommodate resource availability or conflicts. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_backup_vault" type="string">
          The name of the backup vault in which the backups created by this rule are stored. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_logically_air_gapped_backup_vault_arn" type="string">
          The ARN of the logically air-gapped backup vault that this backup plan rule targets, causing backups to be stored in an immutable, ransomware-protected vault for the rule's retention period. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scan_settings" type="list(object)">
      Specifies the scanning configuration used by AWS Backup to run malware detection on supported backup resources (such as S3) when backups are created under this backup plan. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="malware_scanner" type="string">
          Indicates whether AWS Backup malware scanning is enabled for the backup plan, with accepted string values 'TRUE' or 'FALSE' that determine if newly created backups are scanned for potentially malicious software. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="resource_types" type="list(string)">
          Lists the AWS resource types (e.g., 'Aurora', 'RDS') that are selected for malware scanning under the backup plan's scan settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scanner_role_arn" type="string">
          The ARN of the IAM role that AWS Backup uses to scan backups for sensitive data, typically via Amazon Macie, when vault scanning is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="backup_plan_tags" type="any">
  Tags (key-value pairs) to attach to the backup plan for cost allocation, access control, and resource management in AWS Backup. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="backup_plan_arn" type="string">
  The Amazon Resource Name (ARN) of the backup plan, uniquely identifying the resource across AWS, such as arn:aws:backup:region:account:backup-plan:plan-id. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="backup_plan_id" type="string">
  The unique identifier assigned by AWS Backup when the backup plan is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="version_id" type="string">
  The version identifier of the backup plan, which changes each time the plan is updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
