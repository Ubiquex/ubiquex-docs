---
title: "aws_backup_global_settings"
description: "Real, generated bindings for `aws_backup_global_settings`."
---

`aws_backup_global_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own global settings"})

    		ubx.Resource(backup.GlobalSettings, "ci-runner", backup.GlobalSettingsConfig{
    			GlobalSettings: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalSettings } from "jsr:@ubx/sdk-aws/aws/backup/global_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own global settings" });

      resource(GlobalSettings, "ci-runner", {
        globalSettings: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import GlobalSettings, GlobalSettingsConfig

    def describe():
        ubx.intent("payments own global settings")

        ubx.resource(GlobalSettings, "ci-runner", GlobalSettingsConfig(
            global_settings={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_global_settings, with global settings a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="global_settings" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
