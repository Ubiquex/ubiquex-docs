---
title: "aws_backup_global_settings"
description: "Real, generated bindings for `aws_backup_global_settings`."
---

`aws_backup_global_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/backup"

    cfg := backup.GlobalSettingsConfig{
        GlobalSettings: map[string]string{"key": "value"},
    }
    ubx.Resource(backup.GlobalSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GlobalSettings } from
      "jsr:@ubx/sdk-aws/aws/backup/global_settings";

    resource(GlobalSettings, "example", {
      globalSettings: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import GlobalSettings, GlobalSettingsConfig

    cfg = GlobalSettingsConfig(
        global_settings={"key": "value"},
    )
    ubx.resource(GlobalSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_global_settings with global_settings.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="global_settings" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
