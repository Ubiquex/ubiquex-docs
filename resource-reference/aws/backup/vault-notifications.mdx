---
title: "aws_backup_vault_notifications"
description: "Real, generated bindings for `aws_backup_vault_notifications`."
---

`aws_backup_vault_notifications` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vault notifications"})

    		ubx.Resource(backup.VaultNotifications, "ci-runner", backup.VaultNotificationsConfig{
    			BackupVaultEvents: []string{"example"},
    			BackupVaultName:   "example-backup-vault-name",
    			SnsTopicArn:       "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VaultNotifications } from "jsr:@ubx/sdk-aws/aws/backup/vault_notifications";

    export default stack("payments", () => {
      intent({ summary: "payments own vault notifications" });

      resource(VaultNotifications, "ci-runner", {
        backupVaultEvents: ["example"],
        backupVaultName: "example-backup-vault-name",
        snsTopicArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import VaultNotifications, VaultNotificationsConfig

    def describe():
        ubx.intent("payments own vault notifications")

        ubx.resource(VaultNotifications, "ci-runner", VaultNotificationsConfig(
            backup_vault_events=["example"],
            backup_vault_name="example-backup-vault-name",
            sns_topic_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_vault_notifications, with backup vault events a
    set value, backup vault name example-backup-vault-name, sns topic
    arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_vault_events" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="backup_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="backup_vault_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
