---
title: "aws_backup_vault_notifications"
description: "Real, generated bindings for `aws_backup_vault_notifications`."
---

`aws_backup_vault_notifications` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"

    cfg := backup.VaultNotificationsConfig{
        BackupVaultEvents: []string{"example"},
        BackupVaultName: "example-backup-vault-name",
        SnsTopicArn: "example",
    }
    ubx.Resource(backup.VaultNotifications, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VaultNotifications } from
      "jsr:@ubx/sdk-aws/aws/backup/vault_notifications";

    resource(VaultNotifications, "example", {
      backupVaultEvents: ["example"],
      backupVaultName: "example",
      snsTopicArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import (
        VaultNotifications,
        VaultNotificationsConfig,
    )

    cfg = VaultNotificationsConfig(
        backup_vault_events=["example"],
        backup_vault_name="example",
        sns_topic_arn="example",
    )
    ubx.resource(VaultNotifications, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_vault_notifications with
backup_vault_events, backup_vault_name, sns_topic_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="backup_vault_events" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="backup_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="backup_vault_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
