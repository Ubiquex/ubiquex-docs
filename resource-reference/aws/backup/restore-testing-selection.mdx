---
title: "aws_backup_restore_testing_selection"
description: "Real, generated bindings for `aws_backup_restore_testing_selection`."
---

`aws_backup_restore_testing_selection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own restore testing selection"})

    		ubx.Resource(backup.RestoreTestingSelection, "ci-runner", backup.RestoreTestingSelectionConfig{
    			IamRoleArn:             "arn:aws:iam::123456789012:policy/ci",
    			Name:                   "ci-runner",
    			ProtectedResourceType:  "example",
    			RestoreTestingPlanName: "example-restore-testing-plan-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RestoreTestingSelection } from "jsr:@ubx/sdk-aws/aws/backup/restore_testing_selection";

    export default stack("payments", () => {
      intent({ summary: "payments own restore testing selection" });

      resource(RestoreTestingSelection, "ci-runner", {
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        protectedResourceType: "example",
        restoreTestingPlanName: "example-restore-testing-plan-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import RestoreTestingSelection, RestoreTestingSelectionConfig

    def describe():
        ubx.intent("payments own restore testing selection")

        ubx.resource(RestoreTestingSelection, "ci-runner", RestoreTestingSelectionConfig(
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            protected_resource_type="example",
            restore_testing_plan_name="example-restore-testing-plan-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_restore_testing_selection called "ci-runner",
    with iam role arn arn:aws:iam::123456789012:policy/ci, protected
    resource type example, restore testing plan name
    example-restore-testing-plan-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protected_resource_arns" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protected_resource_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="restore_metadata_overrides" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="restore_testing_plan_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="validation_window_hours" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="protected_resource_arns" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="restore_metadata_overrides" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="validation_window_hours" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>
