---
title: "aws_backup_vault_lock_configuration"
description: "Real, generated bindings for `aws_backup_vault_lock_configuration`."
---

`aws_backup_vault_lock_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/backup"

    cfg := backup.VaultLockConfigurationConfig{
        BackupVaultName: "example-backup-vault-name",
        ChangeableForDays: 1,
        MaxRetentionDays: 1,
    }
    ubx.Resource(backup.VaultLockConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VaultLockConfiguration } from
      "jsr:@ubx/sdk-aws/aws/backup/vault_lock_configuration";

    resource(VaultLockConfiguration, "example", {
      backupVaultName: "example",
      changeableForDays: 1,
      maxRetentionDays: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import (
        VaultLockConfiguration,
        VaultLockConfigurationConfig,
    )

    cfg = VaultLockConfigurationConfig(
        backup_vault_name="example",
        changeable_for_days=1,
        max_retention_days=1,
    )
    ubx.resource(VaultLockConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_vault_lock_configuration with
backup_vault_name, changeable_for_days, max_retention_days.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="backup_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="changeable_for_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_retention_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="min_retention_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="backup_vault_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
