---
title: "aws_backup_restore_testing_plan"
description: "Real, generated bindings for `aws_backup_restore_testing_plan`."
---

`aws_backup_restore_testing_plan` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/backup"

    cfg := backup.RestoreTestingPlanConfig{
        Name: "example-name",
        ScheduleExpression: "example",
        ScheduleExpressionTimezone: "example",
        StartWindowHours: 1,
    }
    ubx.Resource(backup.RestoreTestingPlan, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RestoreTestingPlan } from
      "jsr:@ubx/sdk-aws/aws/backup/restore_testing_plan";

    resource(RestoreTestingPlan, "example", {
      name: "example",
      scheduleExpression: "example",
      scheduleExpressionTimezone: "example",
      startWindowHours: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.backup import (
        RestoreTestingPlan,
        RestoreTestingPlanConfig,
    )

    cfg = RestoreTestingPlanConfig(
        name="example",
        schedule_expression="example",
        schedule_expression_timezone="example",
        start_window_hours=1,
    )
    ubx.resource(RestoreTestingPlan, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_backup_restore_testing_plan with name,
schedule_expression, schedule_expression_timezone,
start_window_hours.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule_expression" type="string" required>
  Required.
</ResponseField>

<ResponseField name="schedule_expression_timezone" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_window_hours" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule_expression_timezone" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_window_hours" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
