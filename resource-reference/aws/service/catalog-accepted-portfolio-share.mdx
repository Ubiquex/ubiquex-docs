---
title: "aws_service_catalog_accepted_portfolio_share"
description: "`aws_service_catalog_accepted_portfolio_share` accepts a real `aws_service_catalog_portfolio_share` from another account..."
---

`aws_service_catalog_accepted_portfolio_share` accepts a real `aws_service_catalog_portfolio_share` from another account, the resource that actually makes the shared portfolio's products usable in the accepting account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog accepted portfolio share"})

    		ubx.Resource(service.CatalogAcceptedPortfolioShare, "ci-runner", service.CatalogAcceptedPortfolioShareConfig{
    			PortfolioId:    "example",
    			AcceptLanguage: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogAcceptedPortfolioShare } from "./local-sdk/aws/sdk/typescript/aws/service/catalog_accepted_portfolio_share.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog accepted portfolio share" });

      resource(CatalogAcceptedPortfolioShare, "ci-runner", {
        portfolioId: "example",
        acceptLanguage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.service import CatalogAcceptedPortfolioShare, CatalogAcceptedPortfolioShareConfig

    def describe():
        ubx.intent("payments own catalog accepted portfolio share")

        ubx.resource(CatalogAcceptedPortfolioShare, "ci-runner", CatalogAcceptedPortfolioShareConfig(
            portfolio_id="example",
            accept_language="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_catalog_accepted_portfolio_share, with
    portfolio id example, accept language example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="accept_language" type="string">
  The language code. Optional.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  The portfolio identifier. Required.
</ResponseField>
