---
title: "aws_service_catalog_accepted_portfolio_share"
description: "`aws_service_catalog_accepted_portfolio_share` accepts a real `aws_service_catalog_portfolio_share` from another account..."
---

`aws_service_catalog_accepted_portfolio_share` accepts a real `aws_service_catalog_portfolio_share` from another account, the resource that actually makes the shared portfolio's products usable in the accepting account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own catalog accepted portfolio share"})

    		ubx.Resource(servicecatalog.AcceptedPortfolioShare, "example", servicecatalog.AcceptedPortfolioShareConfig{
    			PortfolioId:    "example",
    			AcceptLanguage: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AcceptedPortfolioShare } from "@ubx/sdk-aws/aws/servicecatalog/accepted_portfolio_share";

    export default stack("example", () => {
      intent({ summary: "example own catalog accepted portfolio share" });

      resource(AcceptedPortfolioShare, "example", {
        portfolioId: "example",
        acceptLanguage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog.accepted_portfolio_share import AcceptedPortfolioShare, AcceptedPortfolioShareConfig

    def describe():
        ubx.intent("example own catalog accepted portfolio share")

        ubx.resource(AcceptedPortfolioShare, "example", AcceptedPortfolioShareConfig(
            portfolio_id="example",
            accept_language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_catalog_accepted_portfolio_share, with
    portfolio id example, accept language example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="accept_language" type="string">
  The language code. Optional.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  The portfolio identifier. Required.
</ResponseField>
