---
title: "aws_service_catalog_service_action_association"
description: "`aws_service_catalog_service_action_association` associates a real `aws_service_catalog_service_action` with a specific..."
---

`aws_service_catalog_service_action_association` associates a real `aws_service_catalog_service_action` with a specific `aws_service_catalog_cloud_formation_product` and provisioning artifact, the resource that actually makes the action available on that product.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog service action association"})

    		ubx.Resource(service.CatalogServiceActionAssociation, "ci-runner", service.CatalogServiceActionAssociationConfig{
    			ProductId:              "example",
    			ProvisioningArtifactId: "example",
    			ServiceActionId:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogServiceActionAssociation } from "./local-sdk/aws/sdk/typescript/aws/service/catalog_service_action_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog service action association" });

      resource(CatalogServiceActionAssociation, "ci-runner", {
        productId: "example",
        provisioningArtifactId: "example",
        serviceActionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.service import CatalogServiceActionAssociation, CatalogServiceActionAssociationConfig

    def describe():
        ubx.intent("payments own catalog service action association")

        ubx.resource(CatalogServiceActionAssociation, "ci-runner", CatalogServiceActionAssociationConfig(
            product_id="example",
            provisioning_artifact_id="example",
            service_action_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_catalog_service_action_association, with
    product id example, provisioning artifact id example, service
    action id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="product_id" type="string" required>
  The ID (or identifier) of the AWS Service Catalog product to which the self-service action is associated. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="provisioning_artifact_id" type="string" required>
  The ID of the provisioning artifact (product version) that the service action is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="service_action_id" type="string" required>
  The ID of the AWS Service Catalog service action that this association attaches to the specified provisioning artifact. Required. **(AI-inferred)**
</ResponseField>
