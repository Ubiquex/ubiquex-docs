---
title: "aws_service_catalog_app_registry_resource_association"
description: "`aws_service_catalog_app_registry_resource_association` attaches a real AWS resource, most commonly a CloudFormation stack..."
---

`aws_service_catalog_app_registry_resource_association` attaches a real AWS resource, most commonly a CloudFormation stack, to an `aws_service_catalog_app_registry_application`, the resource that actually associates deployed infrastructure with the application it belongs to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog app registry resource association"})

    		ubx.Resource(service.CatalogAppRegistryResourceAssociation, "ci-runner", service.CatalogAppRegistryResourceAssociationConfig{
    			Application:  "example",
    			Resource:     "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogAppRegistryResourceAssociation } from "./local-sdk/aws/sdk/typescript/aws/service/catalog_app_registry_resource_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog app registry resource association" });

      resource(CatalogAppRegistryResourceAssociation, "ci-runner", {
        application: "example",
        resource: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.service import CatalogAppRegistryResourceAssociation, CatalogAppRegistryResourceAssociationConfig

    def describe():
        ubx.intent("payments own catalog app registry resource association")

        ubx.resource(CatalogAppRegistryResourceAssociation, "ci-runner", CatalogAppRegistryResourceAssociationConfig(
            application="example",
            resource="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_catalog_app_registry_resource_association,
    with application example, resource example, resource type
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application" type="string" required>
  The name or the Id of the Application. Required.
</ResponseField>

<ResponseField name="resource" type="string" required>
  The name or the Id of the Resource. Required.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The type of the CFN Resource for now it's enum CFN_STACK. Required.
</ResponseField>

## Output properties

<ResponseField name="application_arn" type="string">
  The ARN of the AWS Service Catalog AppRegistry application that this resource association links the associated resource to, which is automatically set by AWS and read-only. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The Amazon Resource Name (ARN) of the resource associated with the AppRegistry application, returned by AWS Service Catalog AppRegistry after the association is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
