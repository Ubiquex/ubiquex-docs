---
title: "aws_service_catalog_portfolio_principal_association"
description: "`aws_service_catalog_portfolio_principal_association` grants a real IAM principal, user, group, or role, access to an `aws_service_catalog_portfolio`..."
---

`aws_service_catalog_portfolio_principal_association` grants a real IAM principal, user, group, or role, access to an `aws_service_catalog_portfolio`, the resource that actually authorizes that principal to browse and launch its products.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own catalog portfolio principal association"})

    		ubx.Resource(servicecatalog.PortfolioPrincipalAssociation, "example", servicecatalog.PortfolioPrincipalAssociationConfig{
    			PrincipalType:  "example",
    			AcceptLanguage: "example",
    			PortfolioId:    "example",
    			PrincipalArn:   "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PortfolioPrincipalAssociation } from "@ubx/sdk-aws/aws/servicecatalog/portfolio_principal_association";

    export default stack("example", () => {
      intent({ summary: "example own catalog portfolio principal association" });

      resource(PortfolioPrincipalAssociation, "example", {
        principalType: "example",
        acceptLanguage: "example",
        portfolioId: "example",
        principalArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog.portfolio_principal_association import PortfolioPrincipalAssociation, PortfolioPrincipalAssociationConfig

    def describe():
        ubx.intent("example own catalog portfolio principal association")

        ubx.resource(PortfolioPrincipalAssociation, "example", PortfolioPrincipalAssociationConfig(
            principal_type="example",
            accept_language="example",
            portfolio_id="example",
            principal_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_catalog_portfolio_principal_association, with
    principal type example, accept language example, portfolio id
    example, principal arn arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="accept_language" type="string">
  The language code. Optional.
</ResponseField>

<ResponseField name="portfolio_id" type="string">
  The portfolio identifier. Optional.
</ResponseField>

<ResponseField name="principal_arn" type="string">
  The ARN of the principal (user, role, or group). Optional.
</ResponseField>

<ResponseField name="principal_type" type="string" required>
  The principal type. The supported value is IAM if you use a fully defined Amazon Resource Name (ARN), or IAM_PATTERN if you use an ARN with no accountID, with or without wildcard characters. Required.
</ResponseField>
