---
title: "aws_service_discovery_instance"
description: "Real, generated bindings for `aws_service_discovery_instance`."
---

`aws_service_discovery_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/service"

    cfg := service.DiscoveryInstanceConfig{
        Attributes: map[string]string{"key": "value"},
        InstanceId: "example",
        ServiceId: "example",
    }
    ubx.Resource(service.DiscoveryInstance, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DiscoveryInstance } from
      "jsr:@ubx/sdk-aws/aws/service/discovery_instance";

    resource(DiscoveryInstance, "example", {
      attributes: { key: "value" },
      instanceId: "example",
      serviceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.service import (
        DiscoveryInstance,
        DiscoveryInstanceConfig,
    )

    cfg = DiscoveryInstanceConfig(
        attributes={"key": "value"},
        instance_id="example",
        service_id="example",
    )
    ubx.resource(DiscoveryInstance, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_service_discovery_instance with attributes,
instance_id, service_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attributes" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
