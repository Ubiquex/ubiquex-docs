---
title: "aws_service_discovery_instance"
description: "Real, generated bindings for `aws_service_discovery_instance`."
---

`aws_service_discovery_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own discovery instance"})

    		ubx.Resource(service.DiscoveryInstance, "ci-runner", service.DiscoveryInstanceConfig{
    			Attributes: map[string]string{"managed-by": "ubx"},
    			InstanceId: "example",
    			ServiceId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DiscoveryInstance } from "jsr:@ubx/sdk-aws/aws/service/discovery_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own discovery instance" });

      resource(DiscoveryInstance, "ci-runner", {
        attributes: { "managed-by": "ubx" },
        instanceId: "example",
        serviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.service import DiscoveryInstance, DiscoveryInstanceConfig

    def describe():
        ubx.intent("payments own discovery instance")

        ubx.resource(DiscoveryInstance, "ci-runner", DiscoveryInstanceConfig(
            attributes={"managed-by": "ubx"},
            instance_id="example",
            service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_discovery_instance, with attributes a set
    value, instance id example, service id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attributes" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
