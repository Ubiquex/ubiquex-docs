---
title: "aws_service_discovery_instance"
description: "`aws_service_discovery_instance` registers a real, discoverable endpoint, an IP address or ARN plus custom attributes..."
---

`aws_service_discovery_instance` registers a real, discoverable endpoint, an IP address or ARN plus custom attributes, under an `aws_service_discovery_service`, the actual instance that discovery queries against that service return.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own discovery instance"})

    		ubx.Resource(service.DiscoveryInstance, "example", service.DiscoveryInstanceConfig{
    			InstanceAttributes: "example",
    			ServiceId:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DiscoveryInstance } from "@ubx/sdk-aws/aws/service/discovery_instance";

    export default stack("example", () => {
      intent({ summary: "example own discovery instance" });

      resource(DiscoveryInstance, "example", {
        instanceAttributes: "example",
        serviceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.service import DiscoveryInstance, DiscoveryInstanceConfig

    def describe():
        ubx.intent("example own discovery instance")

        ubx.resource(DiscoveryInstance, "example", DiscoveryInstanceConfig(
            instance_attributes="example",
            service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_service_discovery_instance, with instance attributes
    example, service id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="instance_attributes" type="any" required>
  This field is a required map of key-value pairs that define the instance's attributes for AWS Cloud Map service discovery, including standard keys like AWS_INSTANCE_IPV4, AWS_INSTANCE_IPV6, and AWS_INSTANCE_PORT, as well as any custom attributes you need to associate with the instance. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="service_id" type="string" required>
  The ID of the AWS Cloud Map service that this instance is registered with, used as the parent service for the instance's health and discovery configuration. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="instance_id" type="string">
  The unique identifier assigned by AWS Cloud Map to the instance when it is registered within the service. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
