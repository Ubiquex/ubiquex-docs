---
title: "aws_ses_mail_manager_addon_instance"
description: "`aws_ses_mail_manager_addon_instance` deploys a real, running instance of a third-party email security add-on, spam or malware scanning..."
---

`aws_ses_mail_manager_addon_instance` deploys a real, running instance of a third-party email security add-on, spam or malware scanning, subscribed through `aws_ses_mail_manager_addon_subscription`, into a Mail Manager `aws_ses_mail_manager_rule_set`'s processing pipeline.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mail manager addon instance"})

    		ubx.Resource(ses.MailManagerAddonInstance, "ci-runner", ses.MailManagerAddonInstanceConfig{
    			AddonSubscriptionId: "example",
    			Tags:                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MailManagerAddonInstance } from "./local-sdk/aws/sdk/typescript/aws/ses/mail_manager_addon_instance.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mail manager addon instance" });

      resource(MailManagerAddonInstance, "ci-runner", {
        addonSubscriptionId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ses import MailManagerAddonInstance, MailManagerAddonInstanceConfig

    def describe():
        ubx.intent("payments own mail manager addon instance")

        ubx.resource(MailManagerAddonInstance, "ci-runner", MailManagerAddonInstanceConfig(
            addon_subscription_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_mail_manager_addon_instance, with addon
    subscription id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="addon_subscription_id" type="string" required>
  The ID of the addon subscription that this addon instance is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the list of tag objects (key and value) to assign to the SES Mail Manager add-on instance for metadata management and cost allocation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the SES Mail Manager Addon Instance, used for metadata and resource organization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag attached to the AWS SES Mail Manager add-on instance, used for organization, cost allocation, and access control as an arbitrary string. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="addon_instance_arn" type="string">
  The Amazon Resource Name (ARN) assigned by AWS that uniquely identifies this Mail Manager add-on instance. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="addon_instance_id" type="string">
  The unique identifier assigned to this addon instance by AWS SES Mail Manager. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="addon_name" type="string">
  The name of the add-on (such as Spamhaus) that this addon instance provides, determined by the addon subscription and returned as a read-only attribute. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
