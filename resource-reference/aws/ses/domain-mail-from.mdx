---
title: "aws_ses_domain_mail_from"
description: "Real, generated bindings for `aws_ses_domain_mail_from`."
---

`aws_ses_domain_mail_from` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ses"

    cfg := ses.DomainMailFromConfig{
        Domain: "example",
        MailFromDomain: "example",
        BehaviorOnMxFailure: "example",
    }
    ubx.Resource(ses.DomainMailFrom, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainMailFrom } from
      "jsr:@ubx/sdk-aws/aws/ses/domain_mail_from";

    resource(DomainMailFrom, "example", {
      domain: "example",
      mailFromDomain: "example",
      behaviorOnMxFailure: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import DomainMailFrom, DomainMailFromConfig

    cfg = DomainMailFromConfig(
        domain="example",
        mail_from_domain="example",
        behavior_on_mx_failure="example",
    )
    ubx.resource(DomainMailFrom, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_domain_mail_from with domain, mail_from_domain,
behavior_on_mx_failure.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="behavior_on_mx_failure" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mail_from_domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
