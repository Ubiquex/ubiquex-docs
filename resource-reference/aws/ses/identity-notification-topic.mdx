---
title: "aws_ses_identity_notification_topic"
description: "Real, generated bindings for `aws_ses_identity_notification_topic`."
---

`aws_ses_identity_notification_topic` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ses"

    cfg := ses.IdentityNotificationTopicConfig{
        Identity: "example",
        NotificationType: "example",
        IncludeOriginalHeaders: true,
        TopicArn: "example",
    }
    ubx.Resource(ses.IdentityNotificationTopic, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityNotificationTopic } from
      "jsr:@ubx/sdk-aws/aws/ses/identity_notification_topic";

    resource(IdentityNotificationTopic, "example", {
      identity: "example",
      notificationType: "example",
      includeOriginalHeaders: true,
      topicArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import (
        IdentityNotificationTopic,
        IdentityNotificationTopicConfig,
    )

    cfg = IdentityNotificationTopicConfig(
        identity="example",
        notification_type="example",
        include_original_headers=True,
        topic_arn="example",
    )
    ubx.resource(IdentityNotificationTopic, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_identity_notification_topic with identity,
notification_type, include_original_headers, topic_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity" type="string" required>
  Required.
</ResponseField>

<ResponseField name="include_original_headers" type="bool">
  Optional.
</ResponseField>

<ResponseField name="notification_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="topic_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
