---
title: "aws_ses_identity_notification_topic"
description: "Real, generated bindings for `aws_ses_identity_notification_topic`."
---

`aws_ses_identity_notification_topic` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity notification topic"})

    		ubx.Resource(ses.IdentityNotificationTopic, "ci-runner", ses.IdentityNotificationTopicConfig{
    			Identity:               "example",
    			NotificationType:       "example",
    			IncludeOriginalHeaders: true,
    			TopicArn:               "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityNotificationTopic } from "jsr:@ubx/sdk-aws/aws/ses/identity_notification_topic";

    export default stack("payments", () => {
      intent({ summary: "payments own identity notification topic" });

      resource(IdentityNotificationTopic, "ci-runner", {
        identity: "example",
        notificationType: "example",
        includeOriginalHeaders: true,
        topicArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ses import IdentityNotificationTopic, IdentityNotificationTopicConfig

    def describe():
        ubx.intent("payments own identity notification topic")

        ubx.resource(IdentityNotificationTopic, "ci-runner", IdentityNotificationTopicConfig(
            identity="example",
            notification_type="example",
            include_original_headers=True,
            topic_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_identity_notification_topic, with identity
    example, notification type example, include original headers
    true, topic arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity" type="string" required>
  Required.
</ResponseField>

<ResponseField name="include_original_headers" type="bool">
  Optional.
</ResponseField>

<ResponseField name="notification_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="topic_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
