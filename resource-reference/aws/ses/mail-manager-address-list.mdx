---
title: "aws_ses_mail_manager_address_list"
description: "`aws_ses_mail_manager_address_list` defines a real, managed list of email addresses or domains that an `aws_ses_mail_manager_rule_set` or..."
---

`aws_ses_mail_manager_address_list` defines a real, managed list of email addresses or domains that an `aws_ses_mail_manager_rule_set` or `aws_ses_mail_manager_traffic_policy` can reference to allow or block matching senders or recipients.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mail manager address list"})

    		ubx.Resource(ses.MailManagerAddressList, "ci-runner", ses.MailManagerAddressListConfig{
    			AddressListName: "example-address-list-name",
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MailManagerAddressList } from "./local-sdk/aws/sdk/typescript/aws/ses/mail_manager_address_list.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mail manager address list" });

      resource(MailManagerAddressList, "ci-runner", {
        addressListName: "example-address-list-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ses import MailManagerAddressList, MailManagerAddressListConfig

    def describe():
        ubx.intent("payments own mail manager address list")

        ubx.resource(MailManagerAddressList, "ci-runner", MailManagerAddressListConfig(
            address_list_name="example-address-list-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_mail_manager_address_list, with address list name
    example-address-list-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="address_list_name" type="string">
  Sets the name of the address list, which identifies it in the SES Mail Manager console and API. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the key-value tags to attach to the SES Mail Manager address list resource, enabling organization, identification, and cost allocation within AWS. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag attached to the SES Mail Manager address list, used to store arbitrary metadata for resource identification, cost allocation, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="address_list_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this address list within AWS SES Mail Manager. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="address_list_id" type="string">
  The unique identifier assigned by AWS to the address list resource, generated upon creation and used to reference this address list in Mail Manager policies and configurations. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
