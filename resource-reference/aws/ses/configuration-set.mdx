---
title: "aws_ses_configuration_set"
description: "Real, generated bindings for `aws_ses_configuration_set`."
---

`aws_ses_configuration_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"

    cfg := ses.ConfigurationSetConfig{
        Name: "example-name",
        ReputationMetricsEnabled: true,
        SendingEnabled: true,
    }
    ubx.Resource(ses.ConfigurationSet, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConfigurationSet } from
      "jsr:@ubx/sdk-aws/aws/ses/configuration_set";

    resource(ConfigurationSet, "example", {
      name: "example",
      reputationMetricsEnabled: true,
      sendingEnabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import (
        ConfigurationSet,
        ConfigurationSetConfig,
    )

    cfg = ConfigurationSetConfig(
        name="example",
        reputation_metrics_enabled=True,
        sending_enabled=True,
    )
    ubx.resource(ConfigurationSet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_configuration_set with name,
reputation_metrics_enabled, sending_enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reputation_metrics_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="sending_enabled" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_fresh_start" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
