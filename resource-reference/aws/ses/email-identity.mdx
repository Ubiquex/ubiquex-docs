---
title: "aws_ses_email_identity"
description: "Real, generated bindings for `aws_ses_email_identity`."
---

`aws_ses_email_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own email identity"})

    		ubx.Resource(ses.EmailIdentity, "ci-runner", ses.EmailIdentityConfig{
    			Email: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EmailIdentity } from "jsr:@ubx/sdk-aws/aws/ses/email_identity";

    export default stack("payments", () => {
      intent({ summary: "payments own email identity" });

      resource(EmailIdentity, "ci-runner", {
        email: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ses import EmailIdentity, EmailIdentityConfig

    def describe():
        ubx.intent("payments own email identity")

        ubx.resource(EmailIdentity, "ci-runner", EmailIdentityConfig(
            email="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_email_identity, with email example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
