---
title: "aws_ses_email_identity"
description: "Real, generated bindings for `aws_ses_email_identity`."
---

`aws_ses_email_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"

    cfg := ses.EmailIdentityConfig{
        Email: "example",
    }
    ubx.Resource(ses.EmailIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EmailIdentity } from
      "jsr:@ubx/sdk-aws/aws/ses/email_identity";

    resource(EmailIdentity, "example", {
      email: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import EmailIdentity, EmailIdentityConfig

    cfg = EmailIdentityConfig(
        email="example",
    )
    ubx.resource(EmailIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_email_identity with email.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
