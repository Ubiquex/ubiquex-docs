---
title: "aws_ses_dedicated_ip_pool"
description: "`aws_ses_dedicated_ip_pool` groups real dedicated sending IP addresses under an `aws_ses_configuration_set`..."
---

`aws_ses_dedicated_ip_pool` groups real dedicated sending IP addresses under an `aws_ses_configuration_set`, so a chosen subset of an account's dedicated IPs is used for a particular category of email, keeping sending reputations isolated between categories.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dedicated ip pool"})

    		ubx.Resource(ses.DedicatedIpPool, "ci-runner", ses.DedicatedIpPoolConfig{
    			PoolName:    "example-pool-name",
    			ScalingMode: "example",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DedicatedIpPool } from "./local-sdk/aws/sdk/typescript/aws/ses/dedicated_ip_pool.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own dedicated ip pool" });

      resource(DedicatedIpPool, "ci-runner", {
        poolName: "example-pool-name",
        scalingMode: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ses import DedicatedIpPool, DedicatedIpPoolConfig

    def describe():
        ubx.intent("payments own dedicated ip pool")

        ubx.resource(DedicatedIpPool, "ci-runner", DedicatedIpPoolConfig(
            pool_name="example-pool-name",
            scaling_mode="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_dedicated_ip_pool, with pool name
    example-pool-name, scaling mode example, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="pool_name" type="string">
  The name of the dedicated IP pool. Optional.
</ResponseField>

<ResponseField name="scaling_mode" type="string">
  Specifies whether the dedicated IP pool is managed or not. The default value is STANDARD. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags (keys and values) associated with the dedicated IP pool. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (e.g., 'Environment') of a tag assigned to this SES dedicated IP pool, used to categorize or identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag assigned to the Amazon SES dedicated IP pool, providing user-defined metadata for resource organization and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
