---
title: "aws_ses_event_destination"
description: "Real, generated bindings for `aws_ses_event_destination`."
---

`aws_ses_event_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"

    cfg := ses.EventDestinationConfig{
        ConfigurationSetName: "example-configuration-set-name",
        MatchingTypes: []string{"example"},
        Name: "example-name",
        Enabled: true,
    }
    ubx.Resource(ses.EventDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventDestination } from
      "jsr:@ubx/sdk-aws/aws/ses/event_destination";

    resource(EventDestination, "example", {
      configurationSetName: "example",
      matchingTypes: ["example"],
      name: "example",
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import (
        EventDestination,
        EventDestinationConfig,
    )

    cfg = EventDestinationConfig(
        configuration_set_name="example",
        matching_types=["example"],
        name="example",
        enabled=True,
    )
    ubx.resource(EventDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_event_destination with configuration_set_name,
matching_types, name, enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="configuration_set_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="matching_types" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
