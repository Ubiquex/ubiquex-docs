---
title: "aws_ses_domain_dkim"
description: "Real, generated bindings for `aws_ses_domain_dkim`."
---

`aws_ses_domain_dkim` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ses"

    cfg := ses.DomainDkimConfig{
        Domain: "example",
    }
    ubx.Resource(ses.DomainDkim, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainDkim } from
      "jsr:@ubx/sdk-aws/aws/ses/domain_dkim";

    resource(DomainDkim, "example", {
      domain: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import DomainDkim, DomainDkimConfig

    cfg = DomainDkimConfig(
        domain="example",
    )
    ubx.resource(DomainDkim, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_domain_dkim with domain.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="dkim_tokens" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
