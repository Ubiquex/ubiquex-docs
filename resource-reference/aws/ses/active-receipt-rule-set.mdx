---
title: "aws_ses_active_receipt_rule_set"
description: "Real, generated bindings for `aws_ses_active_receipt_rule_set`."
---

`aws_ses_active_receipt_rule_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own active receipt rule set"})

    		ubx.Resource(ses.ActiveReceiptRuleSet, "ci-runner", ses.ActiveReceiptRuleSetConfig{
    			RuleSetName: "example-rule-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ActiveReceiptRuleSet } from "jsr:@ubx/sdk-aws/aws/ses/active_receipt_rule_set";

    export default stack("payments", () => {
      intent({ summary: "payments own active receipt rule set" });

      resource(ActiveReceiptRuleSet, "ci-runner", {
        ruleSetName: "example-rule-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ses import ActiveReceiptRuleSet, ActiveReceiptRuleSetConfig

    def describe():
        ubx.intent("payments own active receipt rule set")

        ubx.resource(ActiveReceiptRuleSet, "ci-runner", ActiveReceiptRuleSetConfig(
            rule_set_name="example-rule-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_active_receipt_rule_set, with rule set name
    example-rule-set-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_set_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
