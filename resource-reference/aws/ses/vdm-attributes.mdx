---
title: "aws_ses_vdm_attributes"
description: "`aws_ses_vdm_attributes` configures account-wide Virtual Deliverability Manager settings, engagement metrics tracking and optimized shared delivery..."
---

`aws_ses_vdm_attributes` configures account-wide Virtual Deliverability Manager settings, engagement metrics tracking and optimized shared delivery, that apply across every `aws_ses_configuration_set` unless a configuration set overrides them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vdm attributes"})

    		ubx.Resource(ses.VdmAttributes, "example", ses.VdmAttributesConfig{
    			DashboardAttributes: map[string]any{"engagement_metrics": "example"},
    			GuardianAttributes:  map[string]any{"optimized_shared_delivery": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VdmAttributes } from "@ubx/sdk-aws/aws/ses/vdm_attributes";

    export default stack("example", () => {
      intent({ summary: "example own vdm attributes" });

      resource(VdmAttributes, "example", {
        dashboardAttributes: { engagementMetrics: "example" },
        guardianAttributes: { optimizedSharedDelivery: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ses import VdmAttributes, VdmAttributesConfig
    from ubx.aws.ses.vdm_attributes import VdmAttributes_DashboardAttributes, VdmAttributes_GuardianAttributes

    def describe():
        ubx.intent("example own vdm attributes")

        ubx.resource(VdmAttributes, "example", VdmAttributesConfig(
            dashboard_attributes=VdmAttributes_DashboardAttributes(engagement_metrics="example"),
            guardian_attributes=VdmAttributes_GuardianAttributes(optimized_shared_delivery="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_vdm_attributes, with dashboard attributes a set
    value, guardian attributes a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dashboard_attributes" type="object">
  Preferences regarding the Dashboard feature. Optional.
  <Expandable title="properties">
    <ResponseField name="engagement_metrics" type="string">
      Whether emails sent from this account have engagement tracking enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="guardian_attributes" type="object">
  Preferences regarding the Guardian feature. Optional.
  <Expandable title="properties">
    <ResponseField name="optimized_shared_delivery" type="string">
      Whether emails sent from this account have optimized delivery algorithm enabled. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="vdm_attributes_resource_id" type="string">
  Unique identifier for this resource Computed by AWS after `ship`.
</ResponseField>
