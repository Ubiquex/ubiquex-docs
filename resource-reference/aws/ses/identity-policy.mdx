---
title: "aws_ses_identity_policy"
description: "Real, generated bindings for `aws_ses_identity_policy`."
---

`aws_ses_identity_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"

    cfg := ses.IdentityPolicyConfig{
        Identity: "example",
        Name: "example-name",
        Policy: "example",
    }
    ubx.Resource(ses.IdentityPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityPolicy } from
      "jsr:@ubx/sdk-aws/aws/ses/identity_policy";

    resource(IdentityPolicy, "example", {
      identity: "example",
      name: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import IdentityPolicy, IdentityPolicyConfig

    cfg = IdentityPolicyConfig(
        identity="example",
        name="example",
        policy="example",
    )
    ubx.resource(IdentityPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_identity_policy with identity, name, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
