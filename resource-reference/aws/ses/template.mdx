---
title: "aws_ses_template"
description: "Real, generated bindings for `aws_ses_template`."
---

`aws_ses_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ses"

    cfg := ses.TemplateConfig{
        Name: "example-name",
        Html: "example",
        Subject: "example",
    }
    ubx.Resource(ses.Template, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Template } from "jsr:@ubx/sdk-aws/aws/ses/template";

    resource(Template, "example", {
      name: "example",
      html: "example",
      subject: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import Template, TemplateConfig

    cfg = TemplateConfig(
        name="example",
        html="example",
        subject="example",
    )
    ubx.resource(Template, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_template with name, html, subject.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="html" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subject" type="string">
  Optional.
</ResponseField>

<ResponseField name="text" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
