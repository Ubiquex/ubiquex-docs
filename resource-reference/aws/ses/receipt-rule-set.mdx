---
title: "aws_ses_receipt_rule_set"
description: "`aws_ses_receipt_rule_set` creates a real, named..."
---

`aws_ses_receipt_rule_set` creates a real, named, ordered container of `aws_ses_receipt_rule`s under SES's original (pre-Mail-Manager) receiving pipeline; only one rule set can be active for an account at a time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ses "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own receipt rule set"})

    		ubx.Resource(ses.ReceiptRuleSet, "example", ses.ReceiptRuleSetConfig{
    			RuleSetName: "example-rule-set-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReceiptRuleSet } from "@ubx/sdk-aws/aws/ses/receipt_rule_set";

    export default stack("example", () => {
      intent({ summary: "example own receipt rule set" });

      resource(ReceiptRuleSet, "example", {
        ruleSetName: "example-rule-set-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ses import ReceiptRuleSet, ReceiptRuleSetConfig

    def describe():
        ubx.intent("example own receipt rule set")

        ubx.resource(ReceiptRuleSet, "example", ReceiptRuleSetConfig(
            rule_set_name="example-rule-set-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ses_receipt_rule_set, with rule set name
    example-rule-set-name.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="rule_set_name" type="string">
  The name of the rule set. Optional.
</ResponseField>
