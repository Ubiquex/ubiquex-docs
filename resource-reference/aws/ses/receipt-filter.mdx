---
title: "aws_ses_receipt_filter"
description: "Real, generated bindings for `aws_ses_receipt_filter`."
---

`aws_ses_receipt_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"

    cfg := ses.ReceiptFilterConfig{
        Cidr: "example",
        Name: "example-name",
        Policy: "example",
    }
    ubx.Resource(ses.ReceiptFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ReceiptFilter } from
      "jsr:@ubx/sdk-aws/aws/ses/receipt_filter";

    resource(ReceiptFilter, "example", {
      cidr: "example",
      name: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import ReceiptFilter, ReceiptFilterConfig

    cfg = ReceiptFilterConfig(
        cidr="example",
        name="example",
        policy="example",
    )
    ubx.resource(ReceiptFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_receipt_filter with cidr, name, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
