---
title: "aws_ses_domain_identity"
description: "Real, generated bindings for `aws_ses_domain_identity`."
---

`aws_ses_domain_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ses"

    cfg := ses.DomainIdentityConfig{
        Domain: "example",
    }
    ubx.Resource(ses.DomainIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DomainIdentity } from
      "jsr:@ubx/sdk-aws/aws/ses/domain_identity";

    resource(DomainIdentity, "example", {
      domain: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ses import DomainIdentity, DomainIdentityConfig

    cfg = DomainIdentityConfig(
        domain="example",
    )
    ubx.resource(DomainIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ses_domain_identity with domain.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="verification_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>
