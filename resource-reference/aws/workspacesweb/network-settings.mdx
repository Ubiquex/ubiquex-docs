---
title: "aws_workspacesweb_network_settings"
description: "Real, generated bindings for `aws_workspacesweb_network_settings`."
---

`aws_workspacesweb_network_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"

    cfg := workspacesweb.NetworkSettingsConfig{
        SecurityGroupIds: []string{"example"},
        SubnetIds: []string{"example"},
        VpcId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(workspacesweb.NetworkSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NetworkSettings } from
      "jsr:@ubx/sdk-aws/aws/workspacesweb/network_settings";

    resource(NetworkSettings, "example", {
      securityGroupIds: ["example"],
      subnetIds: ["example"],
      vpcId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workspacesweb import (
        NetworkSettings,
        NetworkSettingsConfig,
    )

    cfg = NetworkSettingsConfig(
        security_group_ids=["example"],
        subnet_ids=["example"],
        vpc_id="example",
        tags={"key": "value"},
    )
    ubx.resource(NetworkSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workspacesweb_network_settings with
security_group_ids, subnet_ids, vpc_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="network_settings_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
