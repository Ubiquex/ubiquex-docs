---
title: "aws_workspacesweb_browser_settings"
description: "Real, generated bindings for `aws_workspacesweb_browser_settings`."
---

`aws_workspacesweb_browser_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own browser settings"})

    		ubx.Resource(workspacesweb.BrowserSettings, "ci-runner", workspacesweb.BrowserSettingsConfig{
    			BrowserPolicy:               "example",
    			AdditionalEncryptionContext: map[string]string{"managed-by": "ubx"},
    			CustomerManagedKey:          "example",
    			Tags:                        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BrowserSettings } from "jsr:@ubx/sdk-aws/aws/workspacesweb/browser_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own browser settings" });

      resource(BrowserSettings, "ci-runner", {
        browserPolicy: "example",
        additionalEncryptionContext: { "managed-by": "ubx" },
        customerManagedKey: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import BrowserSettings, BrowserSettingsConfig

    def describe():
        ubx.intent("payments own browser settings")

        ubx.resource(BrowserSettings, "ci-runner", BrowserSettingsConfig(
            browser_policy="example",
            additional_encryption_context={"managed-by": "ubx"},
            customer_managed_key="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_browser_settings, with browser policy
    example, additional encryption context a set value, customer
    managed key example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="browser_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="customer_managed_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="browser_settings_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
