---
title: "aws_workspacesweb_ip_access_settings"
description: "Real, generated bindings for `aws_workspacesweb_ip_access_settings`."
---

`aws_workspacesweb_ip_access_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ip access settings"})

    		ubx.Resource(workspacesweb.IpAccessSettings, "ci-runner", workspacesweb.IpAccessSettingsConfig{
    			DisplayName:                 "example-display-name",
    			AdditionalEncryptionContext: map[string]string{"managed-by": "ubx"},
    			CustomerManagedKey:          "example",
    			Description:                 "Managed by ubx.",
    			Tags:                        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpAccessSettings } from "jsr:@ubx/sdk-aws/aws/workspacesweb/ip_access_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own ip access settings" });

      resource(IpAccessSettings, "ci-runner", {
        displayName: "example-display-name",
        additionalEncryptionContext: { "managed-by": "ubx" },
        customerManagedKey: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import IpAccessSettings, IpAccessSettingsConfig

    def describe():
        ubx.intent("payments own ip access settings")

        ubx.resource(IpAccessSettings, "ci-runner", IpAccessSettingsConfig(
            display_name="example-display-name",
            additional_encryption_context={"managed-by": "ubx"},
            customer_managed_key="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_ip_access_settings, with display name
    example-display-name, additional encryption context a set value,
    customer managed key example, description Managed by ubx., tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="customer_managed_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ip_access_settings_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
