---
title: "aws_workspacesweb_user_access_logging_settings"
description: "Real, generated bindings for `aws_workspacesweb_user_access_logging_settings`."
---

`aws_workspacesweb_user_access_logging_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user access logging settings"})

    		ubx.Resource(workspacesweb.UserAccessLoggingSettings, "ci-runner", workspacesweb.UserAccessLoggingSettingsConfig{
    			KinesisStreamArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserAccessLoggingSettings } from "jsr:@ubx/sdk-aws/aws/workspacesweb/user_access_logging_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own user access logging settings" });

      resource(UserAccessLoggingSettings, "ci-runner", {
        kinesisStreamArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import UserAccessLoggingSettings, UserAccessLoggingSettingsConfig

    def describe():
        ubx.intent("payments own user access logging settings")

        ubx.resource(UserAccessLoggingSettings, "ci-runner", UserAccessLoggingSettingsConfig(
            kinesis_stream_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_user_access_logging_settings, with
    kinesis stream arn arn:aws:iam::123456789012:policy/ci, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="kinesis_stream_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_access_logging_settings_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
