---
title: "aws_workspacesweb_identity_provider"
description: "Real, generated bindings for `aws_workspacesweb_identity_provider`."
---

`aws_workspacesweb_identity_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"

    cfg := workspacesweb.IdentityProviderConfig{
        IdentityProviderDetails: map[string]string{"key": "value"},
        IdentityProviderName: "example-identity-provider-name",
        IdentityProviderType: "example",
        PortalArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(workspacesweb.IdentityProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityProvider } from
      "jsr:@ubx/sdk-aws/aws/workspacesweb/identity_provider";

    resource(IdentityProvider, "example", {
      identityProviderDetails: { key: "value" },
      identityProviderName: "example",
      identityProviderType: "example",
      portalArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workspacesweb import (
        IdentityProvider,
        IdentityProviderConfig,
    )

    cfg = IdentityProviderConfig(
        identity_provider_details={"key": "value"},
        identity_provider_name="example",
        identity_provider_type="example",
        portal_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(IdentityProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workspacesweb_identity_provider with
identity_provider_details, identity_provider_name,
identity_provider_type, portal_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="identity_provider_details" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="identity_provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="identity_provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="portal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="identity_provider_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
