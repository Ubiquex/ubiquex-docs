---
title: "aws_workspacesweb_identity_provider"
description: "Real, generated bindings for `aws_workspacesweb_identity_provider`."
---

`aws_workspacesweb_identity_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity provider"})

    		ubx.Resource(workspacesweb.IdentityProvider, "ci-runner", workspacesweb.IdentityProviderConfig{
    			IdentityProviderDetails: map[string]string{"managed-by": "ubx"},
    			IdentityProviderName:    "example-identity-provider-name",
    			IdentityProviderType:    "example",
    			PortalArn:               "arn:aws:iam::123456789012:policy/ci",
    			Tags:                    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityProvider } from "jsr:@ubx/sdk-aws/aws/workspacesweb/identity_provider";

    export default stack("payments", () => {
      intent({ summary: "payments own identity provider" });

      resource(IdentityProvider, "ci-runner", {
        identityProviderDetails: { "managed-by": "ubx" },
        identityProviderName: "example-identity-provider-name",
        identityProviderType: "example",
        portalArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import IdentityProvider, IdentityProviderConfig

    def describe():
        ubx.intent("payments own identity provider")

        ubx.resource(IdentityProvider, "ci-runner", IdentityProviderConfig(
            identity_provider_details={"managed-by": "ubx"},
            identity_provider_name="example-identity-provider-name",
            identity_provider_type="example",
            portal_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_identity_provider, with identity
    provider details a set value, identity provider name
    example-identity-provider-name, identity provider type example,
    portal arn arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="identity_provider_details" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="identity_provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="identity_provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="portal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="identity_provider_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
