---
title: "aws_workspacesweb_session_logger"
description: "Real, generated bindings for `aws_workspacesweb_session_logger`."
---

`aws_workspacesweb_session_logger` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/workspacesweb"

    cfg := workspacesweb.SessionLoggerConfig{
        AdditionalEncryptionContext: map[string]string{"key": "value"},
        CustomerManagedKey: "example",
    }
    ubx.Resource(workspacesweb.SessionLogger, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SessionLogger } from
      "jsr:@ubx/sdk-aws/aws/workspacesweb/session_logger";

    resource(SessionLogger, "example", {
      additionalEncryptionContext: { key: "value" },
      customerManagedKey: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workspacesweb import (
        SessionLogger,
        SessionLoggerConfig,
    )

    cfg = SessionLoggerConfig(
        additional_encryption_context={"key": "value"},
        customer_managed_key="example",
    )
    ubx.resource(SessionLogger, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workspacesweb_session_logger with
additional_encryption_context, customer_managed_key.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="customer_managed_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_logger_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
