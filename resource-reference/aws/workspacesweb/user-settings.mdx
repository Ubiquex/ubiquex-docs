---
title: "aws_workspacesweb_user_settings"
description: "Real, generated bindings for `aws_workspacesweb_user_settings`."
---

`aws_workspacesweb_user_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user settings"})

    		ubx.Resource(workspacesweb.UserSettings, "ci-runner", workspacesweb.UserSettingsConfig{
    			CopyAllowed:                 "example",
    			DownloadAllowed:             "example",
    			PasteAllowed:                "example",
    			PrintAllowed:                "example",
    			UploadAllowed:               "example",
    			AdditionalEncryptionContext: map[string]string{"managed-by": "ubx"},
    			CustomerManagedKey:          "example",
    			DisconnectTimeoutInMinutes:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserSettings } from "jsr:@ubx/sdk-aws/aws/workspacesweb/user_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own user settings" });

      resource(UserSettings, "ci-runner", {
        copyAllowed: "example",
        downloadAllowed: "example",
        pasteAllowed: "example",
        printAllowed: "example",
        uploadAllowed: "example",
        additionalEncryptionContext: { "managed-by": "ubx" },
        customerManagedKey: "example",
        disconnectTimeoutInMinutes: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import UserSettings, UserSettingsConfig

    def describe():
        ubx.intent("payments own user settings")

        ubx.resource(UserSettings, "ci-runner", UserSettingsConfig(
            copy_allowed="example",
            download_allowed="example",
            paste_allowed="example",
            print_allowed="example",
            upload_allowed="example",
            additional_encryption_context={"managed-by": "ubx"},
            customer_managed_key="example",
            disconnect_timeout_in_minutes=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_user_settings, with copy allowed
    example, download allowed example, paste allowed example, print
    allowed example, upload allowed example, additional encryption
    context a set value, customer managed key example, disconnect
    timeout in minutes 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="copy_allowed" type="string" required>
  Required.
</ResponseField>

<ResponseField name="customer_managed_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="deep_link_allowed" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="disconnect_timeout_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="download_allowed" type="string" required>
  Required.
</ResponseField>

<ResponseField name="idle_disconnect_timeout_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="paste_allowed" type="string" required>
  Required.
</ResponseField>

<ResponseField name="print_allowed" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="upload_allowed" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deep_link_allowed" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_settings_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
