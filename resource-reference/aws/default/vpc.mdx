---
title: "aws_default_vpc"
description: "Real, generated bindings for `aws_default_vpc`."
---

`aws_default_vpc` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	default_ "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/default_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc"})

    		ubx.Resource(default_.Vpc, "ci-runner", default_.VpcConfig{
    			AssignGeneratedIpv6CidrBlock: true,
    			EnableDnsHostnames:           true,
    			EnableDnsSupport:             true,
    			ForceDestroy:                 true,
    			Ipv6IpamPoolId:               "example",
    			Ipv6NetmaskLength:            1,
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Vpc } from "jsr:@ubx/sdk-aws/aws/default/vpc";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc" });

      resource(Vpc, "ci-runner", {
        assignGeneratedIpv6CidrBlock: true,
        enableDnsHostnames: true,
        enableDnsSupport: true,
        forceDestroy: true,
        ipv6IpamPoolId: "example",
        ipv6NetmaskLength: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.default import Vpc, VpcConfig

    def describe():
        ubx.intent("payments own vpc")

        ubx.resource(Vpc, "ci-runner", VpcConfig(
            assign_generated_ipv6_cidr_block=True,
            enable_dns_hostnames=True,
            enable_dns_support=True,
            force_destroy=True,
            ipv6_ipam_pool_id="example",
            ipv6_netmask_length=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_default_vpc, with assign generated ipv6 cidr block
    true, enable dns hostnames true, enable dns support true, force
    destroy true, ipv6 ipam pool id example, ipv6 netmask length 1,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="assign_generated_ipv6_cidr_block" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_dns_hostnames" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_dns_support" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_network_address_usage_metrics" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block_network_border_group" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_ipam_pool_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="ipv6_netmask_length" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cidr_block" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_network_acl_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_security_group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dhcp_options_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enable_network_address_usage_metrics" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="existing_default_vpc" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_tenancy" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipv6_association_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipv6_cidr_block" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block_network_border_group" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="main_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
