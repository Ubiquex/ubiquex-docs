---
title: "aws_default_subnet"
description: "Real, generated bindings for `aws_default_subnet`."
---

`aws_default_subnet` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	default_ "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/default_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subnet"})

    		ubx.Resource(default_.Subnet, "ci-runner", default_.SubnetConfig{
    			AvailabilityZone:                        "example",
    			AssignIpv6AddressOnCreation:             true,
    			CustomerOwnedIpv4Pool:                   "example",
    			EnableDns64:                             true,
    			EnableResourceNameDnsARecordOnLaunch:    true,
    			EnableResourceNameDnsAaaaRecordOnLaunch: true,
    			ForceDestroy:                            true,
    			Ipv6Native:                              true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Subnet } from "jsr:@ubx/sdk-aws/aws/default/subnet";

    export default stack("payments", () => {
      intent({ summary: "payments own subnet" });

      resource(Subnet, "ci-runner", {
        availabilityZone: "example",
        assignIpv6AddressOnCreation: true,
        customerOwnedIpv4Pool: "example",
        enableDns64: true,
        enableResourceNameDnsARecordOnLaunch: true,
        enableResourceNameDnsAaaaRecordOnLaunch: true,
        forceDestroy: true,
        ipv6Native: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.default import Subnet, SubnetConfig

    def describe():
        ubx.intent("payments own subnet")

        ubx.resource(Subnet, "ci-runner", SubnetConfig(
            availability_zone="example",
            assign_ipv6_address_on_creation=True,
            customer_owned_ipv4_pool="example",
            enable_dns64=True,
            enable_resource_name_dns_a_record_on_launch=True,
            enable_resource_name_dns_aaaa_record_on_launch=True,
            force_destroy=True,
            ipv6_native=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_default_subnet, with availability zone example, assign
    ipv6 address on creation true, customer owned ipv4 pool example,
    enable dns64 true, enable resource name dns a record on launch
    true, enable resource name dns aaaa record on launch true, force
    destroy true, ipv6 native true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="assign_ipv6_address_on_creation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="availability_zone" type="string" required>
  Required.
</ResponseField>

<ResponseField name="customer_owned_ipv4_pool" type="string">
  Optional.
</ResponseField>

<ResponseField name="enable_dns64" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_resource_name_dns_a_record_on_launch" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_resource_name_dns_aaaa_record_on_launch" type="bool">
  Optional.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_native" type="bool">
  Optional.
</ResponseField>

<ResponseField name="map_customer_owned_ip_on_launch" type="bool">
  Optional.
</ResponseField>

<ResponseField name="map_public_ip_on_launch" type="bool">
  Optional.
</ResponseField>

<ResponseField name="private_dns_hostname_type_on_launch" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cidr_block" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enable_lni_at_device_index" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="existing_default_subnet" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_cidr_block_association_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="outpost_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="private_dns_hostname_type_on_launch" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
