---
title: "aws_default_network_acl"
description: "Real, generated bindings for `aws_default_network_acl`."
---

`aws_default_network_acl` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/default"

    cfg := default.NetworkAclConfig{
        DefaultNetworkAclId: "example",
        SubnetIds: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(default.NetworkAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NetworkAcl } from
      "jsr:@ubx/sdk-aws/aws/default/network_acl";

    resource(NetworkAcl, "example", {
      defaultNetworkAclId: "example",
      subnetIds: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.default import NetworkAcl, NetworkAclConfig

    cfg = NetworkAclConfig(
        default_network_acl_id="example",
        subnet_ids=["example"],
        tags={"key": "value"},
    )
    ubx.resource(NetworkAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_default_network_acl with default_network_acl_id,
subnet_ids, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_network_acl_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
