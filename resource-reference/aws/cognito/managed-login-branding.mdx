---
title: "aws_cognito_managed_login_branding"
description: "Real, generated bindings for `aws_cognito_managed_login_branding`."
---

`aws_cognito_managed_login_branding` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"

    cfg := cognito.ManagedLoginBrandingConfig{
        ClientId: "example",
        UserPoolId: "example",
        Settings: "example",
        UseCognitoProvidedValues: true,
    }
    ubx.Resource(cognito.ManagedLoginBranding, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ManagedLoginBranding } from
      "jsr:@ubx/sdk-aws/aws/cognito/managed_login_branding";

    resource(ManagedLoginBranding, "example", {
      clientId: "example",
      userPoolId: "example",
      settings: "example",
      useCognitoProvidedValues: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        ManagedLoginBranding,
        ManagedLoginBrandingConfig,
    )

    cfg = ManagedLoginBrandingConfig(
        client_id="example",
        user_pool_id="example",
        settings="example",
        use_cognito_provided_values=True,
    )
    ubx.resource(ManagedLoginBranding, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_managed_login_branding with client_id,
user_pool_id, settings, use_cognito_provided_values.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="settings" type="string">
  Optional.
</ResponseField>

<ResponseField name="use_cognito_provided_values" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="managed_login_branding_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="settings_all" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="use_cognito_provided_values" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
