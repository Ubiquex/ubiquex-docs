---
title: "aws_cognito_managed_login_branding"
description: "Real, generated bindings for `aws_cognito_managed_login_branding`."
---

`aws_cognito_managed_login_branding` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managed login branding"})

    		ubx.Resource(cognito.ManagedLoginBranding, "ci-runner", cognito.ManagedLoginBrandingConfig{
    			ClientId:   "example",
    			UserPoolId: "example",
    			Settings:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedLoginBranding } from "jsr:@ubx/sdk-aws/aws/cognito/managed_login_branding";

    export default stack("payments", () => {
      intent({ summary: "payments own managed login branding" });

      resource(ManagedLoginBranding, "ci-runner", {
        clientId: "example",
        userPoolId: "example",
        settings: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import ManagedLoginBranding, ManagedLoginBrandingConfig

    def describe():
        ubx.intent("payments own managed login branding")

        ubx.resource(ManagedLoginBranding, "ci-runner", ManagedLoginBrandingConfig(
            client_id="example",
            user_pool_id="example",
            settings="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_managed_login_branding, with client id
    example, user pool id example, settings example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="asset" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="bytes" type="string">
    </ResponseField>
    <ResponseField name="category" type="string" required>
    </ResponseField>
    <ResponseField name="color_mode" type="string" required>
    </ResponseField>
    <ResponseField name="extension" type="string" required>
    </ResponseField>
    <ResponseField name="resource_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="settings" type="string">
  Optional.
</ResponseField>

<ResponseField name="use_cognito_provided_values" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="asset" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="bytes" type="string">
    </ResponseField>
    <ResponseField name="category" type="string" required>
    </ResponseField>
    <ResponseField name="color_mode" type="string" required>
    </ResponseField>
    <ResponseField name="extension" type="string" required>
    </ResponseField>
    <ResponseField name="resource_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="managed_login_branding_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="settings_all" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="use_cognito_provided_values" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
