---
title: "aws_cognito_log_delivery_configuration"
description: "Real, generated bindings for `aws_cognito_log_delivery_configuration`."
---

`aws_cognito_log_delivery_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"

    cfg := cognito.LogDeliveryConfigurationConfig{
        UserPoolId: "example",
    }
    ubx.Resource(cognito.LogDeliveryConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogDeliveryConfiguration } from
      "jsr:@ubx/sdk-aws/aws/cognito/log_delivery_configuration";

    resource(LogDeliveryConfiguration, "example", {
      userPoolId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        LogDeliveryConfiguration,
        LogDeliveryConfigurationConfig,
    )

    cfg = LogDeliveryConfigurationConfig(
        user_pool_id="example",
    )
    ubx.resource(LogDeliveryConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_log_delivery_configuration with
user_pool_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
