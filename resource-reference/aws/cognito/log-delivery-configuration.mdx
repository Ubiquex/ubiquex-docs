---
title: "aws_cognito_log_delivery_configuration"
description: "Real, generated bindings for `aws_cognito_log_delivery_configuration`."
---

`aws_cognito_log_delivery_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log delivery configuration"})

    		ubx.Resource(cognito.LogDeliveryConfiguration, "ci-runner", cognito.LogDeliveryConfigurationConfig{
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogDeliveryConfiguration } from "jsr:@ubx/sdk-aws/aws/cognito/log_delivery_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own log delivery configuration" });

      resource(LogDeliveryConfiguration, "ci-runner", {
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import LogDeliveryConfiguration, LogDeliveryConfigurationConfig

    def describe():
        ubx.intent("payments own log delivery configuration")

        ubx.resource(LogDeliveryConfiguration, "ci-runner", LogDeliveryConfigurationConfig(
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_log_delivery_configuration, with user pool id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
