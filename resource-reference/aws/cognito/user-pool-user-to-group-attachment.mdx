---
title: "aws_cognito_user_pool_user_to_group_attachment"
description: "`aws_cognito_user_pool_user_to_group_attachment` is the real, actual membership grant linking an `aws_cognito_user_pool_user` to..."
---

`aws_cognito_user_pool_user_to_group_attachment` is the real, actual membership grant linking an `aws_cognito_user_pool_user` to an `aws_cognito_user_pool_group`. Neither existing on its own makes a user a group member, this attachment does.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user pool user to group attachment"})

    		ubx.Resource(cognito.UserPoolUserToGroupAttachment, "example", cognito.UserPoolUserToGroupAttachmentConfig{
    			GroupName:  "example-group-name",
    			UserPoolId: "example",
    			Username:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPoolUserToGroupAttachment } from "./local-sdk/aws/sdk/typescript/aws/cognito/user_pool_user_to_group_attachment.ts";

    export default stack("example", () => {
      intent({ summary: "example own user pool user to group attachment" });

      resource(UserPoolUserToGroupAttachment, "example", {
        groupName: "example-group-name",
        userPoolId: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserPoolUserToGroupAttachment, UserPoolUserToGroupAttachmentConfig

    def describe():
        ubx.intent("example own user pool user to group attachment")

        ubx.resource(UserPoolUserToGroupAttachment, "example", UserPoolUserToGroupAttachmentConfig(
            group_name="example-group-name",
            user_pool_id="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_pool_user_to_group_attachment, with group
    name example-group-name, user pool id example, username example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="group_name" type="string" required>
  The name of the Cognito user pool group to which the user is attached. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The unique ID of the Cognito user pool that contains the group and user to be attached. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="username" type="string" required>
  The username of the Cognito user to attach to the specified group. Required. **(AI-inferred)**
</ResponseField>
