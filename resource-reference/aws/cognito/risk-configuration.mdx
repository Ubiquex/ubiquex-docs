---
title: "aws_cognito_risk_configuration"
description: "Real, generated bindings for `aws_cognito_risk_configuration`."
---

`aws_cognito_risk_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cognito"

    cfg := cognito.RiskConfigurationConfig{
        UserPoolId: "example",
        ClientId: "example",
    }
    ubx.Resource(cognito.RiskConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RiskConfiguration } from
      "jsr:@ubx/sdk-aws/aws/cognito/risk_configuration";

    resource(RiskConfiguration, "example", {
      userPoolId: "example",
      clientId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        RiskConfiguration,
        RiskConfigurationConfig,
    )

    cfg = RiskConfigurationConfig(
        user_pool_id="example",
        client_id="example",
    )
    ubx.resource(RiskConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_risk_configuration with user_pool_id,
client_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="client_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
