---
title: "aws_cognito_risk_configuration"
description: "Real, generated bindings for `aws_cognito_risk_configuration`."
---

`aws_cognito_risk_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own risk configuration"})

    		ubx.Resource(cognito.RiskConfiguration, "ci-runner", cognito.RiskConfigurationConfig{
    			UserPoolId: "example",
    			ClientId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RiskConfiguration } from "jsr:@ubx/sdk-aws/aws/cognito/risk_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own risk configuration" });

      resource(RiskConfiguration, "ci-runner", {
        userPoolId: "example",
        clientId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import RiskConfiguration, RiskConfigurationConfig

    def describe():
        ubx.intent("payments own risk configuration")

        ubx.resource(RiskConfiguration, "ci-runner", RiskConfigurationConfig(
            user_pool_id="example",
            client_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_risk_configuration, with user pool id example,
    client id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="client_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
