---
title: "aws_cognito_user_pool_identity_provider"
description: "`aws_cognito_user_pool_identity_provider` federates an `aws_cognito_user_pool` with a real external identity source, Google, Facebook, SAML 2.0..."
---

`aws_cognito_user_pool_identity_provider` federates an `aws_cognito_user_pool` with a real external identity source, Google, Facebook, SAML 2.0, or another OIDC provider, mapping that provider's claims into the standardized JWT the user pool issues to an app.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user pool identity provider"})

    		ubx.Resource(cognito.UserPoolIdentityProvider, "example", cognito.UserPoolIdentityProviderConfig{
    			ProviderDetails:  "example",
    			ProviderName:     "example-provider-name",
    			ProviderType:     "example",
    			UserPoolId:       "example",
    			AttributeMapping: "example",
    			IdpIdentifiers:   []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPoolIdentityProvider } from "@ubx/sdk-aws/aws/cognito/user_pool_identity_provider";

    export default stack("example", () => {
      intent({ summary: "example own user pool identity provider" });

      resource(UserPoolIdentityProvider, "example", {
        providerDetails: "example",
        providerName: "example-provider-name",
        providerType: "example",
        userPoolId: "example",
        attributeMapping: "example",
        idpIdentifiers: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserPoolIdentityProvider, UserPoolIdentityProviderConfig

    def describe():
        ubx.intent("example own user pool identity provider")

        ubx.resource(UserPoolIdentityProvider, "example", UserPoolIdentityProviderConfig(
            provider_details="example",
            provider_name="example-provider-name",
            provider_type="example",
            user_pool_id="example",
            attribute_mapping="example",
            idp_identifiers=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_pool_identity_provider, with provider
    details example, provider name example-provider-name, provider
    type example, user pool id example, attribute mapping example,
    idp identifiers a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="attribute_mapping" type="any">
  Maps user pool attribute names to the corresponding attribute names from the external identity provider (IdP) for this user pool identity provider. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="idp_identifiers" type="list(string)">
  Specifies a list of custom identifiers for the identity provider that are included in the user's identities claim, enabling your application to distinguish which IdP a user used to sign in. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="provider_details" type="any" required>
  A map of provider-specific configuration settings (e.g., client_id, client_secret, and authorize_scopes for OIDC, or MetadataURL for SAML) that define how the identity provider integrates with the user pool. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  The name of the identity provider (e.g., Google, Facebook, LoginWithAmazon, or a custom OIDC/SAML provider name) that uniquely identifies this provider within the user pool. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  The identity provider type, such as 'Facebook', 'Google', 'LoginWithAmazon', 'SignInWithApple', 'OIDC', or 'SAML'. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The unique identifier of the Amazon Cognito user pool to which this identity provider configuration is attached. Required. **(AI-inferred)**
</ResponseField>
