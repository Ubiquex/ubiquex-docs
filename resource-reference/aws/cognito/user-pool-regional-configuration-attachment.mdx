---
title: "aws_cognito_user_pool_regional_configuration_attachment"
description: "`aws_cognito_user_pool_regional_configuration_attachment` configures an `aws_cognito_user_pool`'s real, Region-specific settings..."
---

`aws_cognito_user_pool_regional_configuration_attachment` configures an `aws_cognito_user_pool`'s real, Region-specific settings, including its email delivery configuration, SES sender address, configuration set, distinct from the user pool's own core identity and authentication settings.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user pool regional configuration attachment"})

    		ubx.Resource(cognito.UserPoolRegionalConfigurationAttachment, "ci-runner", cognito.UserPoolRegionalConfigurationAttachmentConfig{
    			UserPoolId:         "example",
    			EmailConfiguration: map[string]any{"configuration_set": "example"},
    			LambdaConfig:       map[string]any{"create_auth_challenge": "example"},
    			SmsConfiguration:   map[string]any{"eums_sms": map[string]any{"caller_arn": "example"}},
    			Status:             "example",
    			UserPoolTags:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPoolRegionalConfigurationAttachment } from "./local-sdk/aws/sdk/typescript/aws/cognito/user_pool_regional_configuration_attachment.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own user pool regional configuration attachment",
      });

      resource(UserPoolRegionalConfigurationAttachment, "ci-runner", {
        userPoolId: "example",
        emailConfiguration: { configurationSet: "example" },
        lambdaConfig: { createAuthChallenge: "example" },
        smsConfiguration: { eumsSms: { callerArn: "example" } },
        status: "example",
        userPoolTags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserPoolRegionalConfigurationAttachment, UserPoolRegionalConfigurationAttachmentConfig

    def describe():
        ubx.intent("payments own user pool regional configuration attachment")

        ubx.resource(UserPoolRegionalConfigurationAttachment, "ci-runner", UserPoolRegionalConfigurationAttachmentConfig(
            user_pool_id="example",
            email_configuration={"configuration_set": "example"},
            lambda_config={"create_auth_challenge": "example"},
            sms_configuration={"eums_sms": {"caller_arn": "example"}},
            status="example",
            user_pool_tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_pool_regional_configuration_attachment,
    with user pool id example, email configuration a set value,
    lambda config a set value, sms configuration a set value, status
    example, user pool tags example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="email_configuration" type="object">
  Configures the email delivery settings for the Cognito user pool, including the sender address, reply-to address, and the Amazon SES configuration set or source ARN used to send emails. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="configuration_set" type="string">
      Specifies the name of the Amazon SES configuration set that Amazon Cognito uses when sending email messages for the user pool's email configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email_sending_account" type="string">
      Specifies whether Cognito uses its default email account (COGNITO_DEFAULT) or a developer-provided Amazon SES account (DEVELOPER) to send emails for this regional user pool configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="from" type="string">
      The sender email address that Amazon Cognito uses to send email notifications from the user pool, as part of the attached email configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="reply_to_email_address" type="string">
      The email address that Amazon Cognito uses as the reply-to address for emails sent to users from the user pool. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_arn" type="string">
      The ARN of a verified email address or domain in Amazon SES that Amazon Cognito uses as the sender for emails sent from the user pool's email configuration. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lambda_config" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="create_auth_challenge" type="string">
      Specifies the ARN of the Lambda function that Amazon Cognito invokes to create an authentication challenge during custom authentication flows. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="custom_email_sender" type="object">
      Configures a custom email sender Lambda function for the Cognito user pool, using the specified Lambda ARN and version to handle outgoing email messages instead of Cognito's default email service. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          The ARN of the Lambda function that Cognito invokes as the custom email sender to send email messages for the user pool. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          Specifies the version (V1_0 or V2_0) of the AWS Lambda function used as the custom email sender in the user pool's email configuration. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_message" type="string">
      The ARN of the Lambda function that Amazon Cognito invokes for the CustomMessage trigger, which customizes verification codes and welcome messages sent to users. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="custom_smssender" type="object">
      Configures a custom AWS Lambda function to be used as the SMS sender for this Cognito User Pool regional configuration, specifying the Lambda function's ARN and version. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          The ARN of the Lambda function that Cognito invokes to send custom SMS messages when using the CustomSMSSender Lambda trigger for the user pool's Lambda configuration. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          The version qualifier (e.g., V1_0) of the Lambda function used as the custom SMS sender for the user pool's SMS configuration. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="define_auth_challenge" type="string">
      The ARN of an AWS Lambda function that serves as the DefineAuthChallenge trigger, enabling custom authentication challenge definition for the user pool. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="inbound_federation" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          The ARN of the Lambda function that Amazon Cognito invokes for the inbound federation trigger in the regional configuration, used to process or customize claims for federated identity provider sign-ins. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kmskey_id" type="string">
      Specifies the KMS key ID or ARN that Amazon Cognito uses to encrypt the code delivery messages to users, within the lambda_config block of this regional configuration attachment. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="post_authentication" type="string">
      The ARN of a Lambda function that Amazon Cognito invokes after a user is successfully authenticated, enabling custom logic like auditing or post-login workflows. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="post_confirmation" type="string">
      The ARN of a Lambda function that Amazon Cognito invokes after a user confirms their account, used to run custom post-confirmation logic such as creating a user profile or sending a welcome message. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_authentication" type="string">
      The ARN of an AWS Lambda function that Amazon Cognito invokes before user authentication, allowing you to customize the authentication flow. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_sign_up" type="string">
      The ARN of an AWS Lambda function that Amazon Cognito invokes before sign-up registration, used to customize the pre-sign-up trigger behavior. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_token_generation" type="string">
      The ARN of the Lambda function that Amazon Cognito invokes for the pre-token generation trigger, allowing customization of the token before it is issued during authentication. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_token_generation_config" type="object">
      Configuration for the pre token generation Lambda trigger, specifying the ARN and version of the Lambda function that Amazon Cognito invokes before generating tokens for authentication. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          Specifies the Amazon Resource Name (ARN) of the Lambda function that Amazon Cognito invokes before generating the ID token or access token, allowing you to customize claims or add/remove attributes. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          Specifies the version (e.g., V1_0 or V2_0) of the Lambda function used for the pre-token generation trigger, which determines the event payload format and expected behavior. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_migration" type="string">
      The ARN of the Lambda function that is invoked for the Cognito user migration trigger, which allows existing users to be migrated into the user pool when they sign in. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="verify_auth_challenge_response" type="string">
      The ARN of the Lambda function that Amazon Cognito invokes to verify a user's answer to a custom authentication challenge for the user pool region configuration. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sms_configuration" type="object">
  Specifies the SMS message configuration (e.g., SNS caller ARN and external ID) used by the user pool to send SMS verification codes and MFA messages. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="eums_sms" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="caller_arn" type="string" required>
          The ARN of the IAM role that Amazon Cognito assumes to send SMS messages through Amazon SNS for this regional user pool configuration. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="configuration_set_name" type="string">
          Name of the Amazon SNS configuration set used to track and manage SMS messages sent by the user pool for this regional configuration, enabling message delivery status and event logging. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="external_id" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="in_entity_id" type="string">
          The `in_entity_id` field specifies the India-specific DLT entity ID that is passed as the `AWS.SNS.SMS.EntityId` SMS attribute when Cognito sends SMS messages for this user pool's regional configuration, satisfying TRAI compliance for Indian phone numbers. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="in_template_id" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="origination_identity" type="string">
          Specifies the SMS origination identity (e.g., a sender ID or origination number) used by this regional SMS configuration when Cognito sends SMS messages for the associated user pool. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="region" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_id" type="string">
      The external ID to include in the IAM role trust policy that authorizes Amazon Cognito to send SMS messages through Amazon SNS, preventing the confused deputy problem. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sns_caller_arn" type="string">
      The ARN of the IAM role that Amazon Cognito assumes to send SMS messages through Amazon SNS for this user pool's regional SMS configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sns_region" type="string">
      The AWS region where Amazon SNS is configured to send SMS messages for the user pool's SMS configuration. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The status of the replica. Set to ACTIVE or INACTIVE. Optional.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_pool_tags" type="any">
  A map of key-value tags to assign to the Amazon Cognito user pool associated with this regional configuration attachment. Optional. **(AI-inferred)**
</ResponseField>
