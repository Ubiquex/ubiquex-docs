---
title: "aws_cognito_user_pool_resource_server"
description: "`aws_cognito_user_pool_resource_server` defines a real API an `aws_cognito_user_pool` can issue OAuth access tokens scoped to..."
---

`aws_cognito_user_pool_resource_server` defines a real API an `aws_cognito_user_pool` can issue OAuth access tokens scoped to, the mechanism behind machine-to-machine authorization and fine-grained API access control in Cognito rather than a single, undifferentiated access token.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user pool resource server"})

    		ubx.Resource(cognito.UserPoolResourceServer, "example", cognito.UserPoolResourceServerConfig{
    			Identifier: "example",
    			Name:       "example",
    			UserPoolId: "example",
    			Scopes:     []map[string]any{{"scope_description": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPoolResourceServer } from "@ubx/sdk-aws/aws/cognito/user_pool_resource_server";

    export default stack("example", () => {
      intent({ summary: "example own user pool resource server" });

      resource(UserPoolResourceServer, "example", {
        identifier: "example",
        name: "example",
        userPoolId: "example",
        scopes: [{ scopeDescription: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserPoolResourceServer, UserPoolResourceServerConfig
    from ubx.aws.cognito.user_pool_resource_server import UserPoolResourceServer_Scopes

    def describe():
        ubx.intent("example own user pool resource server")

        ubx.resource(UserPoolResourceServer, "example", UserPoolResourceServerConfig(
            identifier="example",
            name="example",
            user_pool_id="example",
            scopes=[UserPoolResourceServer_Scopes(scope_description="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_pool_resource_server called "example",
    with identifier example, user pool id example, scopes a set
    value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identifier" type="string" required>
  The unique identifier for the resource server, typically a URI (e.g., https://my-api.example.com), used by clients to reference the resource server and its scopes in OAuth flows. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  A friendly name for the resource server, used to identify it within the user pool. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="scopes" type="list(object)">
  Defines the OAuth 2.0 scopes that this resource server exposes to client apps, each scope specifying a name and a description. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="scope_description" type="string">
      A human-readable description that explains the purpose or permissions of the OAuth 2.0 scope defined for the user pool resource server. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="scope_name" type="string">
      The scope_name defines the unique name of the OAuth 2.0 scope that clients can request for authorization from this resource server. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The unique ID of the user pool that this resource server is associated with. Required. **(AI-inferred)**
</ResponseField>
