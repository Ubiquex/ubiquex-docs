---
title: "aws_cognito_identity_pool_provider_principal_tag"
description: "Real, generated bindings for `aws_cognito_identity_pool_provider_principal_tag`."
---

`aws_cognito_identity_pool_provider_principal_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity pool provider principal tag"})

    		ubx.Resource(cognito.IdentityPoolProviderPrincipalTag, "ci-runner", cognito.IdentityPoolProviderPrincipalTagConfig{
    			IdentityPoolId:       "example",
    			IdentityProviderName: "example-identity-provider-name",
    			PrincipalTags:        map[string]string{"managed-by": "ubx"},
    			UseDefaults:          true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityPoolProviderPrincipalTag } from "jsr:@ubx/sdk-aws/aws/cognito/identity_pool_provider_principal_tag";

    export default stack("payments", () => {
      intent({ summary: "payments own identity pool provider principal tag" });

      resource(IdentityPoolProviderPrincipalTag, "ci-runner", {
        identityPoolId: "example",
        identityProviderName: "example-identity-provider-name",
        principalTags: { "managed-by": "ubx" },
        useDefaults: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import IdentityPoolProviderPrincipalTag, IdentityPoolProviderPrincipalTagConfig

    def describe():
        ubx.intent("payments own identity pool provider principal tag")

        ubx.resource(IdentityPoolProviderPrincipalTag, "ci-runner", IdentityPoolProviderPrincipalTagConfig(
            identity_pool_id="example",
            identity_provider_name="example-identity-provider-name",
            principal_tags={"managed-by": "ubx"},
            use_defaults=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_identity_pool_provider_principal_tag, with
    identity pool id example, identity provider name
    example-identity-provider-name, principal tags a set value, use
    defaults true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="identity_provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="use_defaults" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
