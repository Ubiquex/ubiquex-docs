---
title: "aws_cognito_identity_pool_provider_principal_tag"
description: "Real, generated bindings for `aws_cognito_identity_pool_provider_principal_tag`."
---

`aws_cognito_identity_pool_provider_principal_tag` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"

    cfg := cognito.IdentityPoolProviderPrincipalTagConfig{
        IdentityPoolId: "example",
        IdentityProviderName: "example-identity-provider-name",
        PrincipalTags: map[string]string{"key": "value"},
        UseDefaults: true,
    }
    ubx.Resource(
        cognito.IdentityPoolProviderPrincipalTag,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityPoolProviderPrincipalTag } from
      "jsr:@ubx/sdk-aws/aws/cognito/identity_pool_provider_principal_tag";

    resource(IdentityPoolProviderPrincipalTag, "example", {
      identityPoolId: "example",
      identityProviderName: "example",
      principalTags: { key: "value" },
      useDefaults: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        IdentityPoolProviderPrincipalTag,
        IdentityPoolProviderPrincipalTagConfig,
    )

    cfg = IdentityPoolProviderPrincipalTagConfig(
        identity_pool_id="example",
        identity_provider_name="example",
        principal_tags={"key": "value"},
        use_defaults=True,
    )
    ubx.resource(IdentityPoolProviderPrincipalTag, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_identity_pool_provider_principal_tag with
identity_pool_id, identity_provider_name, principal_tags,
use_defaults.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="identity_provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="use_defaults" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
