---
title: "aws_cognito_user_in_group"
description: "Real, generated bindings for `aws_cognito_user_in_group`."
---

`aws_cognito_user_in_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user in group"})

    		ubx.Resource(cognito.UserInGroup, "ci-runner", cognito.UserInGroupConfig{
    			GroupName:  "example-group-name",
    			UserPoolId: "example",
    			Username:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserInGroup } from "jsr:@ubx/sdk-aws/aws/cognito/user_in_group";

    export default stack("payments", () => {
      intent({ summary: "payments own user in group" });

      resource(UserInGroup, "ci-runner", {
        groupName: "example-group-name",
        userPoolId: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserInGroup, UserInGroupConfig

    def describe():
        ubx.intent("payments own user in group")

        ubx.resource(UserInGroup, "ci-runner", UserInGroupConfig(
            group_name="example-group-name",
            user_pool_id="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_in_group, with group name
    example-group-name, user pool id example, username example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="username" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
