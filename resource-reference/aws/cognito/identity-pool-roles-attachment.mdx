---
title: "aws_cognito_identity_pool_roles_attachment"
description: "Real, generated bindings for `aws_cognito_identity_pool_roles_attachment`."
---

`aws_cognito_identity_pool_roles_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity pool roles attachment"})

    		ubx.Resource(cognito.IdentityPoolRolesAttachment, "ci-runner", cognito.IdentityPoolRolesAttachmentConfig{
    			IdentityPoolId: "example",
    			Roles:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityPoolRolesAttachment } from "jsr:@ubx/sdk-aws/aws/cognito/identity_pool_roles_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own identity pool roles attachment" });

      resource(IdentityPoolRolesAttachment, "ci-runner", {
        identityPoolId: "example",
        roles: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import IdentityPoolRolesAttachment, IdentityPoolRolesAttachmentConfig

    def describe():
        ubx.intent("payments own identity pool roles attachment")

        ubx.resource(IdentityPoolRolesAttachment, "ci-runner", IdentityPoolRolesAttachmentConfig(
            identity_pool_id="example",
            roles={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_identity_pool_roles_attachment, with identity
    pool id example, roles a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="roles" type="map(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
