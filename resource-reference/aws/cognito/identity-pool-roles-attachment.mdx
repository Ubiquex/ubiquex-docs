---
title: "aws_cognito_identity_pool_roles_attachment"
description: "Real, generated bindings for `aws_cognito_identity_pool_roles_attachment`."
---

`aws_cognito_identity_pool_roles_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"

    cfg := cognito.IdentityPoolRolesAttachmentConfig{
        IdentityPoolId: "example",
        Roles: map[string]string{"key": "value"},
    }
    ubx.Resource(cognito.IdentityPoolRolesAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityPoolRolesAttachment } from
      "jsr:@ubx/sdk-aws/aws/cognito/identity_pool_roles_attachment";

    resource(IdentityPoolRolesAttachment, "example", {
      identityPoolId: "example",
      roles: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        IdentityPoolRolesAttachment,
        IdentityPoolRolesAttachmentConfig,
    )

    cfg = IdentityPoolRolesAttachmentConfig(
        identity_pool_id="example",
        roles={"key": "value"},
    )
    ubx.resource(IdentityPoolRolesAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_identity_pool_roles_attachment with
identity_pool_id, roles.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="roles" type="map(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
