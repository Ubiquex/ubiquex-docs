---
title: "aws_cognito_user_group"
description: "Real, generated bindings for `aws_cognito_user_group`."
---

`aws_cognito_user_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cognito"

    cfg := cognito.UserGroupConfig{
        Name: "example-name",
        UserPoolId: "example",
        Description: "example",
        Precedence: 1,
    }
    ubx.Resource(cognito.UserGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserGroup } from
      "jsr:@ubx/sdk-aws/aws/cognito/user_group";

    resource(UserGroup, "example", {
      name: "example",
      userPoolId: "example",
      description: "example",
      precedence: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import UserGroup, UserGroupConfig

    cfg = UserGroupConfig(
        name="example",
        user_pool_id="example",
        description="example",
        precedence=1,
    )
    ubx.resource(UserGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_user_group with name, user_pool_id,
description, precedence.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="precedence" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
