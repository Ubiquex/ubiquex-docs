---
title: "aws_cognito_user_pool_replica"
description: "`aws_cognito_user_pool_replica` creates a real, secondary copy of an `aws_cognito_user_pool` in another AWS Region for multi-Region failover..."
---

`aws_cognito_user_pool_replica` creates a real, secondary copy of an `aws_cognito_user_pool` in another AWS Region for multi-Region failover, supporting a growing subset of authentication and session operations locally depending on whether the replica is active or inactive.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user pool replica"})

    		ubx.Resource(cognito.UserPoolReplica, "ci-runner", cognito.UserPoolReplicaConfig{
    			RegionName:           "example-region-name",
    			UserPoolId:           "example",
    			UserPoolTagsAtCreate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPoolReplica } from "./local-sdk/aws/sdk/typescript/aws/cognito/user_pool_replica.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own user pool replica" });

      resource(UserPoolReplica, "ci-runner", {
        regionName: "example-region-name",
        userPoolId: "example",
        userPoolTagsAtCreate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserPoolReplica, UserPoolReplicaConfig

    def describe():
        ubx.intent("payments own user pool replica")

        ubx.resource(UserPoolReplica, "ci-runner", UserPoolReplicaConfig(
            region_name="example-region-name",
            user_pool_id="example",
            user_pool_tags_at_create="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_pool_replica, with region name
    example-region-name, user pool id example, user pool tags at
    create example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="region_name" type="string" required>
  Specifies the AWS region where the Cognito user pool replica is created, which must be different from the primary pool's region. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  The ID of the original user pool in the primary region that this replica is created from, which is required to set up the cross-region replica. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_tags_at_create" type="any">
  Sets the initial tags on the user pool replica at creation, which are key-value pairs that help manage, identify, and categorize the resource. Optional. **(AI-inferred)**
</ResponseField>
