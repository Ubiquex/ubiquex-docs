---
title: "aws_cognito_user_pool"
description: "`aws_cognito_user_pool` is a real, standalone user directory and OIDC identity provider, sign-up, sign-in, and federation with third-party IdPs..."
---

`aws_cognito_user_pool` is a real, standalone user directory and OIDC identity provider, sign-up, sign-in, and federation with third-party IdPs, that issues JWTs directly to an app, independent of whether an `aws_cognito_identity_pool` is ever used alongside it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user pool"})

    		ubx.Resource(cognito.UserPool, "example", cognito.UserPoolConfig{
    			AccountRecoverySetting:     map[string]any{"recovery_mechanisms": []map[string]any{{"name": "example-name"}}},
    			AdminCreateUserConfig:      map[string]any{"allow_admin_create_user_only": true},
    			AliasAttributes:            []string{"example"},
    			AutoVerifiedAttributes:     []string{"example"},
    			DeletionProtection:         "example",
    			DeviceConfiguration:        map[string]any{"challenge_required_on_new_device": true},
    			EmailAuthenticationMessage: "example",
    			EmailAuthenticationSubject: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPool } from "./local-sdk/aws/sdk/typescript/aws/cognito/user_pool.ts";

    export default stack("example", () => {
      intent({ summary: "example own user pool" });

      resource(UserPool, "example", {
        accountRecoverySetting: { recoveryMechanisms: [{ name: "example-name" }] },
        adminCreateUserConfig: { allowAdminCreateUserOnly: true },
        aliasAttributes: ["example"],
        autoVerifiedAttributes: ["example"],
        deletionProtection: "example",
        deviceConfiguration: { challengeRequiredOnNewDevice: true },
        emailAuthenticationMessage: "example",
        emailAuthenticationSubject: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.cognito import UserPool, UserPoolConfig

    def describe():
        ubx.intent("example own user pool")

        ubx.resource(UserPool, "example", UserPoolConfig(
            account_recovery_setting={"recovery_mechanisms": [{"name": "example-name"}]},
            admin_create_user_config={"allow_admin_create_user_only": True},
            alias_attributes=["example"],
            auto_verified_attributes=["example"],
            deletion_protection="example",
            device_configuration={"challenge_required_on_new_device": True},
            email_authentication_message="example",
            email_authentication_subject="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user_pool, with account recovery setting a set
    value, admin create user config a set value, alias attributes a
    set value, auto verified attributes a set value, deletion
    protection example, device configuration a set value, email
    authentication message example, email authentication subject
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_recovery_setting" type="object">
  Configures the account recovery methods (such as email verification, SMS, or admin-only) that users can use to regain access to their accounts in the Cognito user pool. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="recovery_mechanisms" type="list(object)">
      Specifies the ordered list of account recovery mechanisms (like verified email or phone number) with priorities that determine how a user can recover their account in this user pool. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the recovery mechanism, which must be one of `verified_email`, `verified_phone_number`, or `admin_only` and identifies the method (email, phone, or admin) used for user account recovery. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="number">
          This number determines the order in which the recovery mechanism is attempted for user account recovery, with lower values having higher precedence and being tried first. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="admin_create_user_config" type="object">
  Configuration for the admin create user flow, controlling whether only administrators can create users and customizing the invitation message sent to new users. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="allow_admin_create_user_only" type="bool">
      When true, only administrators are allowed to create user accounts in the user pool, disabling self-registration by end users. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="invite_message_template" type="object">
      Configures the email subject, email body, and SMS body used in the invitation message sent to a new user when an administrator creates that user's account. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="email_message" type="string">
          Specifies the body of the email message that is sent as an invitation to a new user when an administrator creates the user in the user pool, allowing placeholders such as &#123;username&#125; and &#123;####&#125; for the temporary password/verification code. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email_subject" type="string">
          The subject line for the email invitation message sent to a newly created user when an administrator creates the user account in the user pool. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="smsmessage" type="string">
          The SMS message body that is sent to a user when an admin creates their account, containing placeholders for the username and the temporary password. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="unused_account_validity_days" type="number">
      Sets the number of days that a user account created by an administrator remains valid without any sign-in activity, after which it expires and the user must reset their password. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="alias_attributes" type="list(string)">
  Specifies the list of user attributes (such as email, phone_number, or preferred_username) that can be used as alternate sign-in aliases for the user pool. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="auto_verified_attributes" type="list(string)">
  Specifies the list of user attributes (such as email or phone_number) that Cognito automatically attempts to verify during user sign-up, marking them as verified when the verification process succeeds. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="deletion_protection" type="string">
  Indicates whether deletion protection is enabled for the user pool, with allowed values of ACTIVE or INACTIVE to prevent accidental deletion. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="device_configuration" type="object">
  Specifies device configuration settings for the user pool, determining whether a new device must pass a challenge and whether devices are remembered only when the user explicitly chooses to keep them signed in. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="challenge_required_on_new_device" type="bool">
      If true, a challenge is required when a user attempts to sign in from a device that has not been previously recognized, as part of the user pool's device tracking and remembered device settings. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="device_only_remembered_on_user_prompt" type="bool">
      When true, a user's device is remembered only if the user explicitly chooses to remember it during sign-in; when false, devices are automatically remembered regardless of user prompt. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="email_authentication_message" type="string">
  Specifies the email message body that Amazon Cognito sends to users when they need to authenticate using a one-time passcode (OTP) delivered to their email address, supporting the &#123;####&#125; placeholder for the actual code. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="email_authentication_subject" type="string">
  Specifies the subject line of the email message that Amazon Cognito sends when users use email as an authentication factor (email MFA). Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="email_configuration" type="object">
  Configures the email delivery settings for the user pool, specifying the source ARN, reply-to address, and delivery method used for messages such as verification codes and account notifications. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="configuration_set" type="string">
      The name of the Amazon SES configuration set to use when sending emails from the user pool. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email_sending_account" type="string">
      Specifies which email account Amazon Cognito uses to send emails, either Cognito's default account ('COGNITO_DEFAULT') or a developer-configured Amazon SES account ('DEVELOPER'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="from" type="string">
      The sender email address that Amazon Cognito uses in the 'From' field of emails it sends to users, which must be verified in Amazon Simple Email Service (SES). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="reply_to_email_address" type="string">
      The email address that recipients of Cognito notification emails can reply to, configured in the email configuration of the user pool. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_arn" type="string">
      The Amazon Resource Name (ARN) of a verified email address in Amazon SES that Cognito uses as the sender for user pool emails. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="email_verification_message" type="string">
  The custom email message that AWS Cognito sends to users to verify their email address, where you can include the verification code placeholder &#123;####&#125;. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="email_verification_subject" type="string">
  The subject line of the email that is sent to users when they need to verify their email address. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="enabled_mfas" type="list(string)">
  A list of the multi-factor authentication (MFA) methods enabled for the Cognito user pool, where each item is either 'SMS_MFA' or 'SOFTWARE_TOKEN_MFA'. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="issuer_configuration" type="object">
  Specifies the issuer configuration for the user pool, which defines the custom issuer ID and issuer URL that Cognito uses for tokens. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="type" type="string">
      The type field in issuer_configuration selects whether the user pool issues tokens with a custom issuer URL (provided via the associated issuer field) or uses the default Cognito issuer, thereby controlling the issuer claim. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="key_configuration" type="object">
  Defines the AWS KMS key configurations used for encrypting custom email and SMS sender messages in the Cognito user pool, specifying the KMS key ID and provider for each custom sender. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key_type" type="string">
      Defines the cryptographic key type (RSA or ECC) used for signing the user pool's JSON Web Tokens (JWTs). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kms_key_arn" type="string">
      The ARN of an AWS KMS customer-managed key that Amazon Cognito uses to encrypt user pool data at rest instead of the default AWS-owned encryption key. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lambda_config" type="object">
  Configures AWS Lambda triggers for user pool workflows, allowing you to specify Lambda function ARNs for events such as pre-signup, post-confirmation, and custom message, among others. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="create_auth_challenge" type="string">
      The ARN of the Lambda function invoked when Cognito needs to create a custom authentication challenge as part of the user pool's custom authentication flow. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="custom_email_sender" type="object">
      Configuration for a custom email sender Lambda trigger, specifying the ARN and version of the Lambda function that handles sending email notifications for the user pool. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          The Amazon Resource Name (ARN) of the Lambda function that Cognito invokes to send custom email messages for the user pool. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          The version of the AWS Lambda function to invoke for custom email sending, which must be set to 'V1_0'. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom_message" type="string">
      ARN of the Lambda function Amazon Cognito invokes to customize email and SMS messages for user verification, invitation, and account confirmation. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="custom_smssender" type="object">
      The configuration for a custom SMS sender Lambda trigger, specifying the Lambda function ARN and version used to send SMS messages for user verification and account recovery. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          The ARN of the Lambda function invoked by Amazon Cognito for custom SMS sender triggers to send SMS messages. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          Specifies the version of the AWS Lambda function to invoke for the custom SMS sender trigger, where 'V1_0' is the only currently supported value. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="define_auth_challenge" type="string">
      The ARN of the Lambda function invoked for the Define Auth Challenge trigger, which determines whether a custom authentication challenge is required and defines the challenge presented to the user. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="inbound_federation" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kmskey_id" type="string">
      The KMS key ID to use for encrypting the Lambda environment variables of the user pool's Lambda trigger functions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="post_authentication" type="string">
      The ARN of a Lambda function that Amazon Cognito invokes after a user is authenticated, enabling custom post-authentication logic such as logging or modifying the authentication response. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="post_confirmation" type="string">
      The Amazon Resource Name (ARN) of the Lambda function that Amazon Cognito triggers after a user confirms their account, enabling custom post-confirmation workflows. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_authentication" type="string">
      The ARN of the Lambda function invoked by the pre-authentication trigger for the user pool. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_sign_up" type="string">
      The ARN of an AWS Lambda function that Amazon Cognito invokes as a pre sign-up trigger, allowing you to customize or validate the sign-up flow before the user is created. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_token_generation" type="string">
      The ARN of an AWS Lambda function that Amazon Cognito invokes before generating identity and access tokens, allowing you to customize token claims. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_token_generation_config" type="object">
      Specifies the ARN and version of the Lambda function to invoke on the pre token generation trigger, enabling customization of access token and ID token claims. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda_arn" type="string">
          Specifies the Amazon Resource Name (ARN) of the Lambda function that Amazon Cognito invokes for the pre token generation trigger, allowing customization of identity and access token claims before issuance. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_version" type="string">
          Specifies the version of the pre token generation Lambda trigger to use, with 'V2_0' enabling the newer behavior that supports adding or modifying claims in the token. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_migration" type="string">
      The ARN of an AWS Lambda function that Amazon Cognito invokes to migrate a user from an external user directory into the user pool when the user attempts to sign in for the first time. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="verify_auth_challenge_response" type="string">
      The Amazon Resource Name (ARN) of a Lambda function that AWS Cognito invokes to verify a user's response to a custom auth challenge, enabling custom verification logic during authentication. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="mfa_configuration" type="string">
  Specifies whether multi-factor authentication (MFA) is enabled for the user pool, with valid values 'OFF', 'ON', or 'OPTIONAL'. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="policies" type="object">
  Configures the password policy for the user pool, specifying rules for password length, required character types, and temporary password validity duration. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="password_policy" type="object">
      Specifies the password policy for the user pool, including rules for minimum length, required character types (uppercase, lowercase, numbers, symbols), and temporary password validity, which must be satisfied by users when they set or change their passwords. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="minimum_length" type="number">
          Specifies the minimum number of characters a user password must contain in the Cognito user pool, enforcing a length requirement with a default of 8 characters. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="password_history_size" type="number">
          Specifies the number of previous passwords that Amazon Cognito remembers for each user, preventing the reuse of those passwords when a user changes their password. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="require_lowercase" type="bool">
          Specifies whether user passwords must contain at least one lowercase letter, with true enforcing the requirement and false making it optional. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="require_numbers" type="bool">
          A Boolean that specifies whether passwords in the user pool must include at least one numeric character (0-9) when users set or change their password. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="require_symbols" type="bool">
          Specifies whether passwords in the user pool must contain at least one symbol (non-alphanumeric character). Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="require_uppercase" type="bool">
          Specifies whether passwords in the user pool must contain at least one uppercase letter (A-Z). Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="temporary_password_validity_days" type="number">
          The number of days a temporary password generated for a user (e.g., by an admin or during a password reset) remains valid before the user must change it. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sign_in_policy" type="object">
      Defines the sign-in policy for the Cognito user pool, which dictates the allowed sign-in methods (such as username, email, or phone number) and whether sign-in aliases are enabled. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="allowed_first_auth_factors" type="list(string)">
          Specifies the list of authentication factors (such as PASSWORD, EMAIL_OTP, SMS_OTP, or WEB_AUTHN) that are allowed as the first step in the sign-in flow for the user pool. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schema" type="list(object)">
  Specifies the list of schema attributes (standard and custom) for the user pool, including each attribute's data type, whether it is required or mutable, and any validation constraints. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="attribute_data_type" type="string">
      Specifies the data type of the user pool schema attribute, which must be one of String, Number, DateTime, or Boolean and determines how the attribute's value is stored and validated. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="developer_only_attribute" type="bool">
      Indicates whether the schema attribute is a developer-only custom attribute that cannot be modified by end users and is only settable through the Cognito admin/developer APIs. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mutable" type="bool">
      This boolean property specifies whether the user pool schema attribute can be changed after the user profile is created. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Specifies the name of the user pool schema attribute, such as a built-in or custom attribute like 'email' or 'my_custom_attr'. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="number_attribute_constraints" type="object">
      Specifies the minimum and maximum numeric values allowed for a schema attribute of type Number in the user pool schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_value" type="string">
          Sets the inclusive maximum numeric value allowed for a custom Number-type schema attribute in the user pool, so that submitted values above this limit are rejected. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_value" type="string">
          Defines the lower bound for the numeric value of a number-type custom attribute in the user pool's schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="required" type="bool">
      Specifies whether this user pool schema attribute is required for users signing up. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="string_attribute_constraints" type="object">
      Configures the minimum and maximum character length limits for a string attribute in the user pool's schema definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_length" type="string">
          Maximum number of characters allowed in a string attribute value, used to define the upper bound for string schema attributes in the user pool. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_length" type="string">
          The minimum number of characters that a string attribute value must contain, enforced for the Cognito user pool schema attribute. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sms_authentication_message" type="string">
  The customizable SMS message text that Amazon Cognito sends to users for authentication (such as MFA code delivery), which must include the &#123;####&#125; placeholder for the verification code. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="sms_configuration" type="object">
  Configures the Amazon SNS settings used by the user pool to send SMS messages for user verification and authentication, including the SNS caller ARN and external ID required for Cognito to publish SMS messages. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="eums_sms" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="caller_arn" type="string" required>
          Required.
        </ResponseField>
        <ResponseField name="configuration_set_name" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="external_id" type="string">
          The external ID that AWS Cognito includes when it assumes the IAM role for sending SMS messages, preventing confused-deputy attacks and must match the external ID on the role's trust policy. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="in_entity_id" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="in_template_id" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="origination_identity" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="region" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="external_id" type="string">
      An arbitrary string that must match the `sts:ExternalId` condition in the IAM role's trust policy that Cognito assumes to send SMS messages via Amazon SNS, providing a security control against the confused deputy problem. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sns_caller_arn" type="string">
      The ARN of the IAM role that Amazon Cognito assumes to send SMS messages via Amazon SNS for the user pool. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sns_region" type="string">
      Specifies the AWS Region in which Amazon SNS is used for sending SMS messages from the user pool. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sms_verification_message" type="string">
  The SMS message template sent to users when they verify their phone number, which can include the verification code placeholder &#123;####&#125;. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_attribute_update_settings" type="object">
  Defines the user pool's attribute update settings, specifically the list of user attributes that require verification before a user can update them. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="attributes_require_verification_before_update" type="list(string)" required>
      Specifies which user attributes (such as email or phone_number) require verification before a user can update them in the Cognito user pool, and if any attribute is listed, that attribute's change is blocked until the new value is verified via a verification code or token. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_pool_add_ons" type="object">
  Configures the advanced security features for the user pool, specifically the advanced security mode (e.g., ENFORCED, AUDIT, or OFF) for threat protection. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="advanced_security_additional_flows" type="object">
      Specifies additional authentication flows, such as custom auth, to be included in the advanced security threat protection settings for the user pool. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_auth_mode" type="string">
          Determines the AWS Cognito Advanced Security mode (AUDIT or ENFORCED) for custom authentication flows, overriding the pool-level advanced security mode. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="advanced_security_mode" type="string">
      Specifies the advanced security mode for the user pool, which can be OFF, AUDIT, or ENFORCED, enabling features like compromised credential detection and account takeover protection. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_pool_name" type="string">
  Specifies a unique name for the user pool within the AWS region, which may contain letters, numbers, and underscores. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_tags" type="any">
  The user_pool_tags field specifies a map of key-value tags to attach to the Cognito user pool for metadata, cost allocation, and resource management. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_tier" type="string">
  Specifies the feature tier of the Cognito user pool (Lite, Standard, or Enterprise), which determines the available features and pricing for the pool. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="username_attributes" type="list(string)">
  Defines the list of user attributes (such as email or phone number) that can be used as the username for signing in to the user pool. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="username_configuration" type="object">
  A configuration object that controls whether usernames are case-sensitive (via the CaseSensitive property) for the user pool. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="case_sensitive" type="bool">
      Determines whether usernames in the user pool are case-sensitive, meaning 'User' and 'user' would be considered distinct when true, and the same when false. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="verification_message_template" type="object">
  Configures the templates for verification messages (email/SMS) sent to users, such as account verification or password reset, including subject, body, and the default email delivery method. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="default_email_option" type="string">
      Specifies whether the default email verification message uses a confirmation link or a confirmation code, with valid values CONFIRM_WITH_LINK or CONFIRM_WITH_CODE. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email_message" type="string">
      Specifies the body of the email message sent to users for verification purposes (such as account or email/phone verification), which can include the verification code placeholder &#123;####&#125;. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email_message_by_link" type="string">
      The custom email message body, including a placeholder for the verification link, that Amazon Cognito sends to users when email verification uses a link instead of a code. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email_subject" type="string">
      The subject line used in the email verification message sent to users when their email address is being verified. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email_subject_by_link" type="string">
      The subject line for the verification email sent to users when they verify their email address or phone number using a link (as opposed to a verification code). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sms_message" type="string">
      The SMS message content sent to users for verification, which can include the &#123;####&#125; placeholder that is replaced with the actual verification code. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_authn_factor_configuration" type="string">
  Configures WebAuthn as a second factor for the user pool, specifying the relying party ID, relying party name, user verification preference, and timeout for passwordless sign-in with security keys. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="web_authn_relying_party_id" type="string">
  The Relying Party ID for WebAuthn passkey authentication, which specifies the domain (e.g., 'example.com') that the user pool associates with registered passkeys. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="web_authn_user_verification" type="string">
  Specifies the user verification requirement for WebAuthn passkeys in the user pool, determining whether a user must complete an additional verification step (such as biometric or PIN) when using a passkey, with allowed values 'required', 'preferred', or 'discouraged'. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Cognito user pool, typically in the form arn:aws:cognito-idp:region:account-id:userpool/user-pool-id. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="provider_name" type="string">
  ProviderName is a read-only attribute that returns the name of the Amazon Cognito user pool, which identifies the user pool as the provider. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="provider_url" type="string">
  The AWS-created URL for the user pool, typically formatted as https://cognito-idp.&lt;region&gt;.amazonaws.com/&lt;pool_id&gt;, which clients use to make authentication requests to this user pool. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_pool_id" type="string">
  The unique identifier assigned by AWS to the user pool. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
