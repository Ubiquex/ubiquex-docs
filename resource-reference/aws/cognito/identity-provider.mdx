---
title: "aws_cognito_identity_provider"
description: "Real, generated bindings for `aws_cognito_identity_provider`."
---

`aws_cognito_identity_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"

    cfg := cognito.IdentityProviderConfig{
        ProviderDetails: map[string]string{"key": "value"},
        ProviderName: "example-provider-name",
        ProviderType: "example",
        UserPoolId: "example",
        AttributeMapping: map[string]string{"key": "value"},
        IdpIdentifiers: []string{"example"},
    }
    ubx.Resource(cognito.IdentityProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityProvider } from
      "jsr:@ubx/sdk-aws/aws/cognito/identity_provider";

    resource(IdentityProvider, "example", {
      providerDetails: { key: "value" },
      providerName: "example",
      providerType: "example",
      userPoolId: "example",
      attributeMapping: { key: "value" },
      idpIdentifiers: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        IdentityProvider,
        IdentityProviderConfig,
    )

    cfg = IdentityProviderConfig(
        provider_details={"key": "value"},
        provider_name="example",
        provider_type="example",
        user_pool_id="example",
        attribute_mapping={"key": "value"},
        idp_identifiers=["example"],
    )
    ubx.resource(IdentityProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_identity_provider with provider_details,
provider_name, provider_type, user_pool_id, attribute_mapping,
idp_identifiers.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attribute_mapping" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="idp_identifiers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="provider_details" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="attribute_mapping" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
