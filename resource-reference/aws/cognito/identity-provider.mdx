---
title: "aws_cognito_identity_provider"
description: "Real, generated bindings for `aws_cognito_identity_provider`."
---

`aws_cognito_identity_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity provider"})

    		ubx.Resource(cognito.IdentityProvider, "ci-runner", cognito.IdentityProviderConfig{
    			ProviderDetails: map[string]string{"managed-by": "ubx"},
    			ProviderName:    "example-provider-name",
    			ProviderType:    "example",
    			UserPoolId:      "example",
    			IdpIdentifiers:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityProvider } from "jsr:@ubx/sdk-aws/aws/cognito/identity_provider";

    export default stack("payments", () => {
      intent({ summary: "payments own identity provider" });

      resource(IdentityProvider, "ci-runner", {
        providerDetails: { "managed-by": "ubx" },
        providerName: "example-provider-name",
        providerType: "example",
        userPoolId: "example",
        idpIdentifiers: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import IdentityProvider, IdentityProviderConfig

    def describe():
        ubx.intent("payments own identity provider")

        ubx.resource(IdentityProvider, "ci-runner", IdentityProviderConfig(
            provider_details={"managed-by": "ubx"},
            provider_name="example-provider-name",
            provider_type="example",
            user_pool_id="example",
            idp_identifiers=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_identity_provider, with provider details a set
    value, provider name example-provider-name, provider type
    example, user pool id example, idp identifiers a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attribute_mapping" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="idp_identifiers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="provider_details" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="provider_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provider_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="attribute_mapping" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
