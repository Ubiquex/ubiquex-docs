---
title: "aws_cognito_identity_pool"
description: "Real, generated bindings for `aws_cognito_identity_pool`."
---

`aws_cognito_identity_pool` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity pool"})

    		ubx.Resource(cognito.IdentityPool, "ci-runner", cognito.IdentityPoolConfig{
    			IdentityPoolName:               "example-identity-pool-name",
    			AllowClassicFlow:               true,
    			AllowUnauthenticatedIdentities: true,
    			DeveloperProviderName:          "example-developer-provider-name",
    			OpenidConnectProviderArns:      []string{"example"},
    			SamlProviderArns:               []string{"example"},
    			SupportedLoginProviders:        map[string]string{"managed-by": "ubx"},
    			Tags:                           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityPool } from "jsr:@ubx/sdk-aws/aws/cognito/identity_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own identity pool" });

      resource(IdentityPool, "ci-runner", {
        identityPoolName: "example-identity-pool-name",
        allowClassicFlow: true,
        allowUnauthenticatedIdentities: true,
        developerProviderName: "example-developer-provider-name",
        openidConnectProviderArns: ["example"],
        samlProviderArns: ["example"],
        supportedLoginProviders: { "managed-by": "ubx" },
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import IdentityPool, IdentityPoolConfig

    def describe():
        ubx.intent("payments own identity pool")

        ubx.resource(IdentityPool, "ci-runner", IdentityPoolConfig(
            identity_pool_name="example-identity-pool-name",
            allow_classic_flow=True,
            allow_unauthenticated_identities=True,
            developer_provider_name="example-developer-provider-name",
            openid_connect_provider_arns=["example"],
            saml_provider_arns=["example"],
            supported_login_providers={"managed-by": "ubx"},
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_identity_pool, with identity pool name
    example-identity-pool-name, allow classic flow true, allow
    unauthenticated identities true, developer provider name
    example-developer-provider-name, openid connect provider arns a
    set value, saml provider arns a set value, supported login
    providers a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_classic_flow" type="bool">
  Optional.
</ResponseField>

<ResponseField name="allow_unauthenticated_identities" type="bool">
  Optional.
</ResponseField>

<ResponseField name="developer_provider_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_pool_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="openid_connect_provider_arns" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="saml_provider_arns" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="supported_login_providers" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
