---
title: "aws_cognito_resource_server"
description: "Real, generated bindings for `aws_cognito_resource_server`."
---

`aws_cognito_resource_server` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource server"})

    		ubx.Resource(cognito.ResourceServer, "ci-runner", cognito.ResourceServerConfig{
    			Identifier: "example",
    			Name:       "ci-runner",
    			UserPoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceServer } from "jsr:@ubx/sdk-aws/aws/cognito/resource_server";

    export default stack("payments", () => {
      intent({ summary: "payments own resource server" });

      resource(ResourceServer, "ci-runner", {
        identifier: "example",
        name: "ci-runner",
        userPoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import ResourceServer, ResourceServerConfig

    def describe():
        ubx.intent("payments own resource server")

        ubx.resource(ResourceServer, "ci-runner", ResourceServerConfig(
            identifier="example",
            name="ci-runner",
            user_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_resource_server called "ci-runner", with
    identifier example, user pool id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="scope_description" type="string" required>
    </ResponseField>
    <ResponseField name="scope_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="scope_description" type="string" required>
    </ResponseField>
    <ResponseField name="scope_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope_identifiers" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>
