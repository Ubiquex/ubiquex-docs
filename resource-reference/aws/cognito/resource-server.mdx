---
title: "aws_cognito_resource_server"
description: "Real, generated bindings for `aws_cognito_resource_server`."
---

`aws_cognito_resource_server` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cognito"

    cfg := cognito.ResourceServerConfig{
        Identifier: "example",
        Name: "example-name",
        UserPoolId: "example",
    }
    ubx.Resource(cognito.ResourceServer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceServer } from
      "jsr:@ubx/sdk-aws/aws/cognito/resource_server";

    resource(ResourceServer, "example", {
      identifier: "example",
      name: "example",
      userPoolId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cognito import (
        ResourceServer,
        ResourceServerConfig,
    )

    cfg = ResourceServerConfig(
        identifier="example",
        name="example",
        user_pool_id="example",
    )
    ubx.resource(ResourceServer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cognito_resource_server with identifier, name,
user_pool_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope_identifiers" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>
