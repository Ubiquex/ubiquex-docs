---
title: "aws_cognito_identity_pool_principal_tag"
description: "`aws_cognito_identity_pool_principal_tag` maps an `aws_cognito_identity_pool`'s real claims from one specific identity provider into IAM principal tags..."
---

`aws_cognito_identity_pool_principal_tag` maps an `aws_cognito_identity_pool`'s real claims from one specific identity provider into IAM principal tags on a user's temporary AWS STS session, enabling attribute-based access control policies keyed on those tags.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own identity pool principal tag"})

    		ubx.Resource(cognito.IdentityPoolPrincipalTag, "example", cognito.IdentityPoolPrincipalTagConfig{
    			IdentityPoolId:       "example",
    			IdentityProviderName: "example-identity-provider-name",
    			PrincipalTags:        "example",
    			UseDefaults:          true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityPoolPrincipalTag } from "@ubx/sdk-aws/aws/cognito/identity_pool_principal_tag";

    export default stack("example", () => {
      intent({ summary: "example own identity pool principal tag" });

      resource(IdentityPoolPrincipalTag, "example", {
        identityPoolId: "example",
        identityProviderName: "example-identity-provider-name",
        principalTags: "example",
        useDefaults: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito.identity_pool_principal_tag import IdentityPoolPrincipalTag, IdentityPoolPrincipalTagConfig

    def describe():
        ubx.intent("example own identity pool principal tag")

        ubx.resource(IdentityPoolPrincipalTag, "example", IdentityPoolPrincipalTagConfig(
            identity_pool_id="example",
            identity_provider_name="example-identity-provider-name",
            principal_tags="example",
            use_defaults=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_identity_pool_principal_tag, with identity
    pool id example, identity provider name
    example-identity-provider-name, principal tags example, use
    defaults true.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity_pool_id" type="string" required>
  The unique identifier of the Cognito identity pool to which these principal tags are associated. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="identity_provider_name" type="string" required>
  The name of the identity provider (such as 'Facebook', 'Google', or a custom provider) that this principal tag configuration applies to. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="principal_tags" type="any">
  A map of key-value pairs that define the principal tags to attach to the identity pool's principal for the specified identity provider. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="use_defaults" type="bool">
  Indicates whether to use the default principal tag mappings for the identity provider, causing Cognito to automatically apply its standard tags (such as 'role') instead of the custom principal tags you specify. Optional. **(AI-inferred)**
</ResponseField>
