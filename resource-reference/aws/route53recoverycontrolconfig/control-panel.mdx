---
title: "aws_route53recoverycontrolconfig_control_panel"
description: "Real, generated bindings for `aws_route53recoverycontrolconfig_control_panel`."
---

`aws_route53recoverycontrolconfig_control_panel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53recoverycontrolconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoverycontrolconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own control panel"})

    		ubx.Resource(route53recoverycontrolconfig.ControlPanel, "ci-runner", route53recoverycontrolconfig.ControlPanelConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/ci",
    			Name:       "ci-runner",
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ControlPanel } from "jsr:@ubx/sdk-aws/aws/route53recoverycontrolconfig/control_panel";

    export default stack("payments", () => {
      intent({ summary: "payments own control panel" });

      resource(ControlPanel, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53recoverycontrolconfig import ControlPanel, ControlPanelConfig

    def describe():
        ubx.intent("payments own control panel")

        ubx.resource(ControlPanel, "ci-runner", ControlPanelConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53recoverycontrolconfig_control_panel called
    "ci-runner", with cluster arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_control_panel" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_control_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
