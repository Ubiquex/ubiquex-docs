---
title: "aws_route53recoverycontrolconfig_control_panel"
description: "Real, generated bindings for `aws_route53recoverycontrolconfig_control_panel`."
---

`aws_route53recoverycontrolconfig_control_panel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoverycontrolconfig"

    cfg := route53recoverycontrolconfig.ControlPanelConfig{
        ClusterArn: "example",
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        route53recoverycontrolconfig.ControlPanel,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ControlPanel } from
      "jsr:@ubx/sdk-aws/aws/route53recoverycontrolconfig/control_panel";

    resource(ControlPanel, "example", {
      clusterArn: "example",
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoverycontrolconfig import (
        ControlPanel,
        ControlPanelConfig,
    )

    cfg = ControlPanelConfig(
        cluster_arn="example",
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(ControlPanel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoverycontrolconfig_control_panel with
cluster_arn, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_control_panel" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_control_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
