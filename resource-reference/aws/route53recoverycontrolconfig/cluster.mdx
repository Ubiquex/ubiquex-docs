---
title: "aws_route53recoverycontrolconfig_cluster"
description: "Real, generated bindings for `aws_route53recoverycontrolconfig_cluster`."
---

`aws_route53recoverycontrolconfig_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoverycontrolconfig"

    cfg := route53recoverycontrolconfig.ClusterConfig{
        Name: "example-name",
        NetworkType: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        route53recoverycontrolconfig.Cluster,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Cluster } from
      "jsr:@ubx/sdk-aws/aws/route53recoverycontrolconfig/cluster";

    resource(Cluster, "example", {
      name: "example",
      networkType: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoverycontrolconfig import (
        Cluster,
        ClusterConfig,
    )

    cfg = ClusterConfig(
        name="example",
        network_type="example",
        tags={"key": "value"},
    )
    ubx.resource(Cluster, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoverycontrolconfig_cluster with name,
network_type, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="network_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cluster_endpoints" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="endpoint" type="string">
    </ResponseField>
    <ResponseField name="region" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
