---
title: "aws_route53recoverycontrolconfig_routing_control"
description: "Real, generated bindings for `aws_route53recoverycontrolconfig_routing_control`."
---

`aws_route53recoverycontrolconfig_routing_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/route53recoverycontrolconfig"

    cfg := route53recoverycontrolconfig.RoutingControlConfig{
        ClusterArn: "example",
        Name: "example-name",
        ControlPanelArn: "example",
    }
    ubx.Resource(
        route53recoverycontrolconfig.RoutingControl,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RoutingControl } from
      "jsr:@ubx/sdk-aws/aws/route53recoverycontrolconfig/routing_control";

    resource(RoutingControl, "example", {
      clusterArn: "example",
      name: "example",
      controlPanelArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoverycontrolconfig import (
        RoutingControl,
        RoutingControlConfig,
    )

    cfg = RoutingControlConfig(
        cluster_arn="example",
        name="example",
        control_panel_arn="example",
    )
    ubx.resource(RoutingControl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoverycontrolconfig_routing_control with
cluster_arn, name, control_panel_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="control_panel_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="control_panel_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
