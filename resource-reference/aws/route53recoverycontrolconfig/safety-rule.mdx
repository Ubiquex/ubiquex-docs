---
title: "aws_route53recoverycontrolconfig_safety_rule"
description: "Real, generated bindings for `aws_route53recoverycontrolconfig_safety_rule`."
---

`aws_route53recoverycontrolconfig_safety_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/route53recoverycontrolconfig"

    cfg := route53recoverycontrolconfig.SafetyRuleConfig{
        ControlPanelArn: "example",
        Name: "example-name",
        WaitPeriodMs: 1,
        AssertedControls: []string{"example"},
        GatingControls: []string{"example"},
    }
    ubx.Resource(
        route53recoverycontrolconfig.SafetyRule,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SafetyRule } from
      "jsr:@ubx/sdk-aws/aws/route53recoverycontrolconfig/safety_rule";

    resource(SafetyRule, "example", {
      controlPanelArn: "example",
      name: "example",
      waitPeriodMs: 1,
      assertedControls: ["example"],
      gatingControls: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53recoverycontrolconfig import (
        SafetyRule,
        SafetyRuleConfig,
    )

    cfg = SafetyRuleConfig(
        control_panel_arn="example",
        name="example",
        wait_period_ms=1,
        asserted_controls=["example"],
        gating_controls=["example"],
    )
    ubx.resource(SafetyRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53recoverycontrolconfig_safety_rule with
control_panel_arn, name, wait_period_ms, asserted_controls,
gating_controls.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="asserted_controls" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="control_panel_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="gating_controls" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_controls" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="wait_period_ms" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
