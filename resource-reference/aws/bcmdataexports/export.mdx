---
title: "aws_bcmdataexports_export"
description: "Real, generated bindings for `aws_bcmdataexports_export`."
---

`aws_bcmdataexports_export` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bcmdataexports "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bcmdataexports"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own export"})

    		ubx.Resource(bcmdataexports.Export, "ci-runner", bcmdataexports.ExportConfig{
    			Tags: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Export } from "jsr:@ubx/sdk-aws/aws/bcmdataexports/export";

    export default stack("payments", () => {
      intent({ summary: "payments own export" });

      resource(Export, "ci-runner", {
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bcmdataexports import Export, ExportConfig

    def describe():
        ubx.intent("payments own export")

        ubx.resource(Export, "ci-runner", ExportConfig(
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bcmdataexports_export, with tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
