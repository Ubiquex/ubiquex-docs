---
title: "aws_bcmdataexports_export"
description: "Real, generated bindings for `aws_bcmdataexports_export`."
---

`aws_bcmdataexports_export` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bcmdataexports"

    cfg := bcmdataexports.ExportConfig{
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(bcmdataexports.Export, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Export } from
      "jsr:@ubx/sdk-aws/aws/bcmdataexports/export";

    resource(Export, "example", {
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bcmdataexports import Export, ExportConfig

    cfg = ExportConfig(
        tags={"key": "value"},
    )
    ubx.resource(Export, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bcmdataexports_export with tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
