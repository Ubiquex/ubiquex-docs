---
title: "aws_agent_registry_registry_record"
description: "`aws_agent_registry_registry_record` represents the metadata for one individual resource, an agent, a tool, an MCP server..."
---

`aws_agent_registry_registry_record` represents the metadata for one individual resource, an agent, a tool, an MCP server, published into an `aws_agent_registry_registry`. It's the actual catalog entry a consumer discovers and resolves, not the underlying resource itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	agent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/agent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registry registry record"})

    		ubx.Resource(agent.RegistryRegistryRecord, "ci-runner", agent.RegistryRegistryRecordConfig{
    			Descriptors:   map[string]any{"a2a_agent_card": map[string]any{"data": "example"}},
    			Name:          "ci-runner",
    			RecordType:    "example",
    			RegistryId:    "example",
    			Description:   "Managed by ubx.",
    			DisplayName:   "example-display-name",
    			RecordVersion: "example",
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryRegistryRecord } from "./local-sdk/aws/sdk/typescript/aws/agent/registry_registry_record.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own registry registry record" });

      resource(RegistryRegistryRecord, "ci-runner", {
        descriptors: { a2aAgentCard: { data: "example" } },
        name: "ci-runner",
        recordType: "example",
        registryId: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        recordVersion: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.agent import RegistryRegistryRecord, RegistryRegistryRecordConfig

    def describe():
        ubx.intent("payments own registry registry record")

        ubx.resource(RegistryRegistryRecord, "ci-runner", RegistryRegistryRecordConfig(
            descriptors={"a2a_agent_card": {"data": "example"}},
            name="ci-runner",
            record_type="example",
            registry_id="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            record_version="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_agent_registry_registry_record called "ci-runner",
    with descriptors a set value, record type example, registry id
    example, description Managed by ubx., display name
    example-display-name, record version example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the registry record. Optional.
</ResponseField>

<ResponseField name="descriptors" type="object" required>
  The typed set of descriptors for a registry record. Exactly one descriptor field is populated based on the record type. Required.
  <Expandable title="properties">
    <ResponseField name="a2a_agent_card" type="object">
      The A2A agent card descriptor, populated when the record type is AGENT. Optional.
      <Expandable title="properties">
        <ResponseField name="data" type="string">
          Descriptor payload data. Optional.
        </ResponseField>
        <ResponseField name="data_schema_version" type="string">
          Version of the descriptor type schema. Optional.
        </ResponseField>
        <ResponseField name="source" type="object">
          The source configuration that defines where descriptor content is retrieved from. Optional.
          <Expandable title="properties">
            <ResponseField name="from_url" type="object">
              URL-based descriptor source configuration, with credential provider configurations for authenticated URL retrieval. Optional.
              <Expandable title="properties">
                <ResponseField name="credential_provider_configurations" type="list(object)">
                  The credential providers used to authenticate when fetching descriptor content from the source URL. Optional.
                  <Expandable title="properties">
                    <ResponseField name="credential_provider" type="object">
                      The object that specifies which credential provider to use and its configuration (such as the provider type and provider-specific settings) for securely obtaining the authentication credentials needed to fetch the agent card from the given source URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="iam_credential_provider" type="object">
                          Configuration for an IAM-based credential provider used to authenticate requests to the source URL from which the A2A agent card is fetched, specifying the IAM role or access keys to use. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="region" type="string">
                              The AWS Region used by the IAM credential provider when authenticating requests to fetch the agent card from the specified URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="role_arn" type="string">
                              The ARN of the IAM role that the IAM credential provider assumes to authenticate and access the source URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="service" type="string">
                              Optional; if omitted, computed by AWS.
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="oauth_credential_provider" type="object">
                          Optional; if omitted, computed by AWS.
                          <Expandable title="properties">
                            <ResponseField name="custom_parameters" type="any">
                              Optional; if omitted, computed by AWS.
                            </ResponseField>
                            <ResponseField name="grant_type" type="string">
                              The OAuth 2.0 grant type used by this credential provider to obtain an access token when fetching from the source URL, such as client_credentials. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="provider_arn" type="string">
                              Optional; if omitted, computed by AWS.
                            </ResponseField>
                            <ResponseField name="scopes" type="list(string)">
                              Defines the list of OAuth 2.0 scopes to request when the OAuth credential provider obtains an access token to access the external URL source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="credential_provider_type" type="string">
                      Selects the authentication protocol (such as basic authentication, OAuth, or API key) used by the credential provider configuration to fetch the agent card from the remote URL in the source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="url" type="string" required>
                  URL source for descriptor content. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="agent_skills_definition" type="object">
      The agent skills definition descriptor, populated when the record type is SKILL. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_data" type="object">
          Additional data associated with an agent skills definition descriptor. Optional.
          <Expandable title="properties">
            <ResponseField name="skill_md" type="object">
              Markdown-format descriptor containing an agent skills document. Optional.
              <Expandable title="properties">
                <ResponseField name="data" type="string">
                  Descriptor payload data. Optional.
                </ResponseField>
                <ResponseField name="data_schema_version" type="string">
                  Version of the descriptor type schema. Optional.
                </ResponseField>
                <ResponseField name="source" type="object">
                  Source configuration for a SkillMd document. Unlike MCP/A2A sources, SkillMd does not support credential providers. Optional.
                  <Expandable title="properties">
                    <ResponseField name="from_url" type="object">
                      URL-based source for SkillMd content (sync is skipped; content is provided inline via Data). Optional.
                      <Expandable title="properties">
                        <ResponseField name="url" type="string" required>
                          URL source for the SkillMd document. Required.
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="data" type="string">
          Descriptor payload data. Optional.
        </ResponseField>
        <ResponseField name="data_schema_version" type="string">
          Version of the descriptor type schema. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="custom" type="object">
      The custom descriptor, populated when the record type is CUSTOM. Optional.
      <Expandable title="properties">
        <ResponseField name="data" type="string">
          Descriptor payload data. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mcp_server" type="object">
      The MCP server descriptor, populated when the record type is MCP. Optional.
      <Expandable title="properties">
        <ResponseField name="additional_data" type="object">
          Additional data associated with an MCP server descriptor. Optional.
          <Expandable title="properties">
            <ResponseField name="tools" type="object">
              The MCP tools descriptor. Optional.
              <Expandable title="properties">
                <ResponseField name="data" type="string">
                  Descriptor payload data. Optional.
                </ResponseField>
                <ResponseField name="data_schema_version" type="string">
                  Version of the tools descriptor schema. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="data" type="string">
          Descriptor payload data. Optional.
        </ResponseField>
        <ResponseField name="data_schema_version" type="string">
          Version of the descriptor type schema. Optional.
        </ResponseField>
        <ResponseField name="source" type="object">
          The source configuration that defines where descriptor content is retrieved from. Optional.
          <Expandable title="properties">
            <ResponseField name="from_url" type="object">
              URL-based descriptor source configuration, with credential provider configurations for authenticated URL retrieval. Optional.
              <Expandable title="properties">
                <ResponseField name="credential_provider_configurations" type="list(object)">
                  The credential providers used to authenticate when fetching descriptor content from the source URL. Optional.
                  <Expandable title="properties">
                    <ResponseField name="credential_provider" type="object">
                      Optional; if omitted, computed by AWS.
                      <Expandable title="properties">
                        <ResponseField name="iam_credential_provider" type="object">
                          Specifies the IAM credential provider settings, including the IAM role to assume, for authenticating to an MCP server accessed via its URL in the registry record's descriptor. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="region" type="string">
                              Specifies the AWS region used by the IAM credential provider when authenticating requests to fetch the MCP server definition from the source URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="role_arn" type="string">
                              The Amazon Resource Name (ARN) of the IAM role that the service assumes to generate temporary credentials for authenticating to the MCP server endpoint when using the IAM credential provider in the URL source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="service" type="string">
                              The AWS service name used by the IAM credential provider to sign requests when authenticating to the MCP server accessed via the URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="oauth_credential_provider" type="object">
                          Specifies the OAuth 2.0-based credential provider configuration used to authenticate when connecting to the MCP server sourced from a URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="custom_parameters" type="any">
                              Specifies a dynamic map of additional custom parameters to be included in the OAuth token request made by the credential provider for the MCP server sourced from a URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="grant_type" type="string">
                              Specifies the OAuth 2.0 grant type (e.g., client_credentials or authorization_code) used by the credential provider to obtain an access token when establishing a connection to the MCP server specified by the source URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="provider_arn" type="string">
                              Optional; if omitted, computed by AWS.
                            </ResponseField>
                            <ResponseField name="scopes" type="list(string)">
                              Specifies the OAuth 2.0 scopes to request from the MCP server's authentication endpoint, defining the specific permissions the agent will be granted when connecting via the configured URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="credential_provider_type" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="url" type="string" required>
                  URL source for descriptor content. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="display_name" type="string">
  The human-readable display name of the registry record. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the registry record. Required.
</ResponseField>

<ResponseField name="record_type" type="string" required>
  The type of the registry record. Required.
</ResponseField>

<ResponseField name="record_version" type="string">
  The version of the registry record. Optional.
</ResponseField>

<ResponseField name="registry_id" type="string" required>
  The identifier of the registry containing the record. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to assign to the registry record. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the registry record was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="record_arn" type="string">
  The Amazon Resource Name (ARN) of the registry record. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="record_id" type="string">
  The unique identifier of the registry record. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="registry_arn" type="string">
  The Amazon Resource Name (ARN) of the registry containing the record. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The lifecycle status of the registry record. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the registry record was last updated. Computed by AWS after `ship`.
</ResponseField>
