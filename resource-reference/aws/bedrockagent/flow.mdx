---
title: "aws_bedrockagent_flow"
description: "Real, generated bindings for `aws_bedrockagent_flow`."
---

`aws_bedrockagent_flow` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrockagent"

    cfg := bedrockagent.FlowConfig{
        ExecutionRoleArn: "example",
        Name: "example-name",
        CustomerEncryptionKeyArn: "example",
        Description: "example",
    }
    ubx.Resource(bedrockagent.Flow, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Flow } from "jsr:@ubx/sdk-aws/aws/bedrockagent/flow";

    resource(Flow, "example", {
      executionRoleArn: "example",
      name: "example",
      customerEncryptionKeyArn: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagent import Flow, FlowConfig

    cfg = FlowConfig(
        execution_role_arn="example",
        name="example",
        customer_encryption_key_arn="example",
        description="example",
    )
    ubx.resource(Flow, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagent_flow with execution_role_arn, name,
customer_encryption_key_arn, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="customer_encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
