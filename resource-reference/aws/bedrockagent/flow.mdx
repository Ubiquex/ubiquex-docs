---
title: "aws_bedrockagent_flow"
description: "Real, generated bindings for `aws_bedrockagent_flow`."
---

`aws_bedrockagent_flow` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own flow"})

    		ubx.Resource(bedrockagent.Flow, "ci-runner", bedrockagent.FlowConfig{
    			ExecutionRoleArn:         "arn:aws:iam::123456789012:policy/ci",
    			Name:                     "ci-runner",
    			CustomerEncryptionKeyArn: "arn:aws:iam::123456789012:policy/ci",
    			Description:              "Managed by ubx.",
    			Tags:                     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Flow } from "jsr:@ubx/sdk-aws/aws/bedrockagent/flow";

    export default stack("payments", () => {
      intent({ summary: "payments own flow" });

      resource(Flow, "ci-runner", {
        executionRoleArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        customerEncryptionKeyArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagent import Flow, FlowConfig

    def describe():
        ubx.intent("payments own flow")

        ubx.resource(Flow, "ci-runner", FlowConfig(
            execution_role_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            customer_encryption_key_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagent_flow called "ci-runner", with execution
    role arn arn:aws:iam::123456789012:policy/ci, customer encryption
    key arn arn:aws:iam::123456789012:policy/ci, description Managed
    by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="customer_encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
