---
title: "aws_bedrockagent_prompt"
description: "Real, generated bindings for `aws_bedrockagent_prompt`."
---

`aws_bedrockagent_prompt` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagent"

    cfg := bedrockagent.PromptConfig{
        Name: "example-name",
        CustomerEncryptionKeyArn: "example",
        DefaultVariant: "example",
    }
    ubx.Resource(bedrockagent.Prompt, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Prompt } from
      "jsr:@ubx/sdk-aws/aws/bedrockagent/prompt";

    resource(Prompt, "example", {
      name: "example",
      customerEncryptionKeyArn: "example",
      defaultVariant: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagent import Prompt, PromptConfig

    cfg = PromptConfig(
        name="example",
        customer_encryption_key_arn="example",
        default_variant="example",
    )
    ubx.resource(Prompt, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagent_prompt with name,
customer_encryption_key_arn, default_variant.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="customer_encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_variant" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
