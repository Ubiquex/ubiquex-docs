---
title: "aws_bedrockagent_knowledge_base"
description: "Real, generated bindings for `aws_bedrockagent_knowledge_base`."
---

`aws_bedrockagent_knowledge_base` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrockagent"

    cfg := bedrockagent.KnowledgeBaseConfig{
        Name: "example-name",
        RoleArn: "example",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(bedrockagent.KnowledgeBase, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KnowledgeBase } from
      "jsr:@ubx/sdk-aws/aws/bedrockagent/knowledge_base";

    resource(KnowledgeBase, "example", {
      name: "example",
      roleArn: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagent import (
        KnowledgeBase,
        KnowledgeBaseConfig,
    )

    cfg = KnowledgeBaseConfig(
        name="example",
        role_arn="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(KnowledgeBase, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagent_knowledge_base with name, role_arn,
description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_reasons" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
