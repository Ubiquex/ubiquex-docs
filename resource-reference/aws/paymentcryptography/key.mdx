---
title: "aws_paymentcryptography_key"
description: "Real, generated bindings for `aws_paymentcryptography_key`."
---

`aws_paymentcryptography_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/paymentcryptography"

    cfg := paymentcryptography.KeyConfig{
        Exportable: true,
        DeletionWindowInDays: 1,
        Enabled: true,
    }
    ubx.Resource(paymentcryptography.Key, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Key } from
      "jsr:@ubx/sdk-aws/aws/paymentcryptography/key";

    resource(Key, "example", {
      exportable: true,
      deletionWindowInDays: 1,
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.paymentcryptography import Key, KeyConfig

    cfg = KeyConfig(
        exportable=True,
        deletion_window_in_days=1,
        enabled=True,
    )
    ubx.resource(Key, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_paymentcryptography_key with exportable,
deletion_window_in_days, enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deletion_window_in_days" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="exportable" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="key_check_value_algorithm" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deletion_window_in_days" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key_check_value" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key_check_value_algorithm" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_origin" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
