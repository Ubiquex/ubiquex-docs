---
title: "aws_paymentcryptography_key_alias"
description: "Real, generated bindings for `aws_paymentcryptography_key_alias`."
---

`aws_paymentcryptography_key_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/paymentcryptography"

    cfg := paymentcryptography.KeyAliasConfig{
        AliasName: "example-alias-name",
        KeyArn: "example",
    }
    ubx.Resource(paymentcryptography.KeyAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KeyAlias } from
      "jsr:@ubx/sdk-aws/aws/paymentcryptography/key_alias";

    resource(KeyAlias, "example", {
      aliasName: "example",
      keyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.paymentcryptography import (
        KeyAlias,
        KeyAliasConfig,
    )

    cfg = KeyAliasConfig(
        alias_name="example",
        key_arn="example",
    )
    ubx.resource(KeyAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_paymentcryptography_key_alias with alias_name,
key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="alias_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
