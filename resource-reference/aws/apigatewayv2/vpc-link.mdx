---
title: "aws_apigatewayv2_vpc_link"
description: "Real, generated bindings for `aws_apigatewayv2_vpc_link`."
---

`aws_apigatewayv2_vpc_link` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc link"})

    		ubx.Resource(apigatewayv2.VpcLink, "ci-runner", apigatewayv2.VpcLinkConfig{
    			Name:             "ci-runner",
    			SecurityGroupIds: []string{"example"},
    			SubnetIds:        []string{"example"},
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcLink } from "jsr:@ubx/sdk-aws/aws/apigatewayv2/vpc_link";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc link" });

      resource(VpcLink, "ci-runner", {
        name: "ci-runner",
        securityGroupIds: ["example"],
        subnetIds: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigatewayv2 import VpcLink, VpcLinkConfig

    def describe():
        ubx.intent("payments own vpc link")

        ubx.resource(VpcLink, "ci-runner", VpcLinkConfig(
            name="ci-runner",
            security_group_ids=["example"],
            subnet_ids=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apigatewayv2_vpc_link called "ci-runner", with
    security group ids a set value, subnet ids a set value, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
