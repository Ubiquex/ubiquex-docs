---
title: "aws_apigatewayv2_api_mapping"
description: "Real, generated bindings for `aws_apigatewayv2_api_mapping`."
---

`aws_apigatewayv2_api_mapping` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api mapping"})

    		ubx.Resource(apigatewayv2.ApiMapping, "ci-runner", apigatewayv2.ApiMappingConfig{
    			ApiId:         "example",
    			DomainName:    "example-domain-name",
    			Stage:         "example",
    			ApiMappingKey: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiMapping } from "jsr:@ubx/sdk-aws/aws/apigatewayv2/api_mapping";

    export default stack("payments", () => {
      intent({ summary: "payments own api mapping" });

      resource(ApiMapping, "ci-runner", {
        apiId: "example",
        domainName: "example-domain-name",
        stage: "example",
        apiMappingKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigatewayv2 import ApiMapping, ApiMappingConfig

    def describe():
        ubx.intent("payments own api mapping")

        ubx.resource(ApiMapping, "ci-runner", ApiMappingConfig(
            api_id="example",
            domain_name="example-domain-name",
            stage="example",
            api_mapping_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apigatewayv2_api_mapping, with api id example, domain
    name example-domain-name, stage example, api mapping key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="api_mapping_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stage" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
