---
title: "aws_apigatewayv2_routing_rule"
description: "Real, generated bindings for `aws_apigatewayv2_routing_rule`."
---

`aws_apigatewayv2_routing_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own routing rule"})

    		ubx.Resource(apigatewayv2.RoutingRule, "ci-runner", apigatewayv2.RoutingRuleConfig{
    			DomainName: "example-domain-name",
    			Priority:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoutingRule } from "jsr:@ubx/sdk-aws/aws/apigatewayv2/routing_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own routing rule" });

      resource(RoutingRule, "ci-runner", {
        domainName: "example-domain-name",
        priority: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigatewayv2 import RoutingRule, RoutingRuleConfig

    def describe():
        ubx.intent("payments own routing rule")

        ubx.resource(RoutingRule, "ci-runner", RoutingRuleConfig(
            domain_name="example-domain-name",
            priority=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apigatewayv2_routing_rule, with domain name
    example-domain-name, priority 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_rule_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="routing_rule_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
