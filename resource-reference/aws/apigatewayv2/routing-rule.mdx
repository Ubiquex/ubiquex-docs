---
title: "aws_apigatewayv2_routing_rule"
description: "Real, generated bindings for `aws_apigatewayv2_routing_rule`."
---

`aws_apigatewayv2_routing_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/apigatewayv2"

    cfg := apigatewayv2.RoutingRuleConfig{
        DomainName: "example-domain-name",
        Priority: 1,
    }
    ubx.Resource(apigatewayv2.RoutingRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RoutingRule } from
      "jsr:@ubx/sdk-aws/aws/apigatewayv2/routing_rule";

    resource(RoutingRule, "example", {
      domainName: "example",
      priority: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.apigatewayv2 import RoutingRule, RoutingRuleConfig

    cfg = RoutingRuleConfig(
        domain_name="example",
        priority=1,
    )
    ubx.resource(RoutingRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_apigatewayv2_routing_rule with domain_name,
priority.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_rule_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="routing_rule_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
