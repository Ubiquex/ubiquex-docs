---
title: "aws_apigatewayv2_authorizer"
description: "Real, generated bindings for `aws_apigatewayv2_authorizer`."
---

`aws_apigatewayv2_authorizer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorizer"})

    		ubx.Resource(apigatewayv2.Authorizer, "ci-runner", apigatewayv2.AuthorizerConfig{
    			ApiId:                          "example",
    			AuthorizerType:                 "example",
    			Name:                           "ci-runner",
    			AuthorizerCredentialsArn:       "arn:aws:iam::123456789012:policy/ci",
    			AuthorizerPayloadFormatVersion: "example",
    			AuthorizerUri:                  "example",
    			EnableSimpleResponses:          true,
    			IdentitySources:                []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Authorizer } from "jsr:@ubx/sdk-aws/aws/apigatewayv2/authorizer";

    export default stack("payments", () => {
      intent({ summary: "payments own authorizer" });

      resource(Authorizer, "ci-runner", {
        apiId: "example",
        authorizerType: "example",
        name: "ci-runner",
        authorizerCredentialsArn: "arn:aws:iam::123456789012:policy/ci",
        authorizerPayloadFormatVersion: "example",
        authorizerUri: "example",
        enableSimpleResponses: true,
        identitySources: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigatewayv2 import Authorizer, AuthorizerConfig

    def describe():
        ubx.intent("payments own authorizer")

        ubx.resource(Authorizer, "ci-runner", AuthorizerConfig(
            api_id="example",
            authorizer_type="example",
            name="ci-runner",
            authorizer_credentials_arn="arn:aws:iam::123456789012:policy/ci",
            authorizer_payload_format_version="example",
            authorizer_uri="example",
            enable_simple_responses=True,
            identity_sources=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apigatewayv2_authorizer called "ci-runner", with api
    id example, authorizer type example, authorizer credentials arn
    arn:aws:iam::123456789012:policy/ci, authorizer payload format
    version example, authorizer uri example, enable simple responses
    true, identity sources a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authorizer_credentials_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="authorizer_payload_format_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="authorizer_result_ttl_in_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="authorizer_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authorizer_uri" type="string">
  Optional.
</ResponseField>

<ResponseField name="enable_simple_responses" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_sources" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="authorizer_result_ttl_in_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
