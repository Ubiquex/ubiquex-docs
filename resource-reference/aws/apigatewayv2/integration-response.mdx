---
title: "aws_api_gateway_v2_integration_response"
description: "`aws_api_gateway_v2_integration_response` transforms the response an `aws_api_gateway_v2_integration` returns before it goes back to the caller..."
---

`aws_api_gateway_v2_integration_response` transforms the response an `aws_api_gateway_v2_integration` returns before it goes back to the caller, used on WebSocket APIs where response mapping isn't handled automatically the way it is for HTTP APIs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apigatewayv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/apigatewayv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gateway v2 integration response"})

    		ubx.Resource(apigatewayv2.IntegrationResponse, "example", apigatewayv2.IntegrationResponseConfig{
    			ApiId:                       "example",
    			IntegrationId:               "example",
    			IntegrationResponseKey:      "example",
    			ContentHandlingStrategy:     "example",
    			ResponseParameters:          "example",
    			ResponseTemplates:           "example",
    			TemplateSelectionExpression: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IntegrationResponse } from "@ubx/sdk-aws/aws/apigatewayv2/integration_response";

    export default stack("example", () => {
      intent({ summary: "example own gateway v2 integration response" });

      resource(IntegrationResponse, "example", {
        apiId: "example",
        integrationId: "example",
        integrationResponseKey: "example",
        contentHandlingStrategy: "example",
        responseParameters: "example",
        responseTemplates: "example",
        templateSelectionExpression: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apigatewayv2.integration_response import IntegrationResponse, IntegrationResponseConfig

    def describe():
        ubx.intent("example own gateway v2 integration response")

        ubx.resource(IntegrationResponse, "example", IntegrationResponseConfig(
            api_id="example",
            integration_id="example",
            integration_response_key="example",
            content_handling_strategy="example",
            response_parameters="example",
            response_templates="example",
            template_selection_expression="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_api_gateway_v2_integration_response, with api id
    example, integration id example, integration response key
    example, content handling strategy example, response parameters
    example, response templates example, template selection
    expression example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  The API identifier. Required.
</ResponseField>

<ResponseField name="content_handling_strategy" type="string">
  Supported only for WebSocket APIs. Specifies how to handle response payload content type conversions. Supported values are ``CONVERT_TO_BINARY`` and ``CONVERT_TO_TEXT``, with the following behaviors: ``CONVERT_TO_BINARY``: Converts a response payload from a Base64-encoded string to the corresponding binary blob. ``CONVERT_TO_TEXT``: Converts a response payload from a binary blob to a Base64-encoded string. If this property is not defined, the response payload will be passed through from the integration response to the route response or method response without modification. Optional.
</ResponseField>

<ResponseField name="integration_id" type="string" required>
  The integration ID. Required.
</ResponseField>

<ResponseField name="integration_response_key" type="string" required>
  The integration response key. Required.
</ResponseField>

<ResponseField name="response_parameters" type="any">
  A key-value map specifying response parameters that are passed to the method response from the backend. The key is a method response header parameter name and the mapped value is an integration response header value, a static value enclosed within a pair of single quotes, or a JSON expression from the integration response body. The mapping key must match the pattern of ``method.response.header.&#123;name&#125;``, where name is a valid and unique header name. The mapped non-static value must match the pattern of ``integration.response.header.&#123;name&#125;`` or ``integration.response.body.&#123;JSON-expression&#125;``, where ``&#123;name&#125;`` is a valid and unique response header name and ``&#123;JSON-expression&#125;`` is a valid JSON expression without the ``$`` prefix. Optional.
</ResponseField>

<ResponseField name="response_templates" type="any">
  The collection of response templates for the integration response as a string-to-string map of key-value pairs. Response templates are represented as a key/value map, with a content-type as the key and a template as the value. Optional.
</ResponseField>

<ResponseField name="template_selection_expression" type="string">
  The template selection expression for the integration response. Supported only for WebSocket APIs. Optional.
</ResponseField>

## Output properties

<ResponseField name="integration_response_id" type="string">
  The automatically generated identifier for this integration response, assigned by AWS when the resource is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
