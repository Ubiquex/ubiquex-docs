---
title: "aws_sim_space_weaver_simulation"
description: "`aws_sim_space_weaver_simulation` ran a real, large-scale spatial simulation..."
---

`aws_sim_space_weaver_simulation` ran a real, large-scale spatial simulation, distributing simulated entities like vehicles or pedestrians across a managed compute cluster. AWS SimSpace Weaver is now fully discontinued: it closed to new customers on May 20, 2025, and AWS ended support entirely on May 20, 2026, a date roughly three months in the past. This resource describes a service no longer operable rather than a current one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	simspaceweaver "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/simspaceweaver"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own space weaver simulation"})

    		ubx.Resource(simspaceweaver.Simulation, "example", simspaceweaver.SimulationConfig{
    			Name:               "example",
    			RoleArn:            "arn:aws:iam::123456789012:policy/example",
    			MaximumDuration:    "example",
    			SchemaS3Location:   map[string]any{"bucket_name": "example-bucket-name"},
    			SnapshotS3Location: map[string]any{"bucket_name": "example-bucket-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Simulation } from "@ubx/sdk-aws/aws/simspaceweaver/simulation";

    export default stack("example", () => {
      intent({ summary: "example own space weaver simulation" });

      resource(Simulation, "example", {
        name: "example",
        roleArn: "arn:aws:iam::123456789012:policy/example",
        maximumDuration: "example",
        schemaS3Location: { bucketName: "example-bucket-name" },
        snapshotS3Location: { bucketName: "example-bucket-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.simspaceweaver.simulation import Simulation, SimulationConfig

    def describe():
        ubx.intent("example own space weaver simulation")

        ubx.resource(Simulation, "example", SimulationConfig(
            name="example",
            role_arn="arn:aws:iam::123456789012:policy/example",
            maximum_duration="example",
            schema_s3_location={"bucket_name": "example-bucket-name"},
            snapshot_s3_location={"bucket_name": "example-bucket-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sim_space_weaver_simulation called "example", with
    role arn arn:aws:iam::123456789012:policy/example, maximum
    duration example, schema s3 location a set value, snapshot s3
    location a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="maximum_duration" type="string">
  The maximum running time of the simulation. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the simulation. Required.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Role ARN. Required.
</ResponseField>

<ResponseField name="schema_s3_location" type="object">
  Specifies the Amazon S3 location (bucket and key) of the JSON schema that defines the entities and components for the simulation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="bucket_name" type="string" required>
      The Schema S3 bucket name. Required.
    </ResponseField>
    <ResponseField name="object_key" type="string" required>
      This is the schema S3 object key, which includes the full path of "folders" from the bucket root to the schema. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="snapshot_s3_location" type="object">
  The Amazon S3 bucket and key location of a simulation snapshot that SimSpace Weaver uses to initialize the simulation from a saved state. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="bucket_name" type="string" required>
      The Schema S3 bucket name. Required.
    </ResponseField>
    <ResponseField name="object_key" type="string" required>
      This is the schema S3 object key, which includes the full path of "folders" from the bucket root to the schema. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="describe_payload" type="string">
  Json object with all simulation details Computed by AWS after `ship`.
</ResponseField>
