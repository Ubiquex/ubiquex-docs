---
title: "aws_proxy_protocol_policy"
description: "Real, generated bindings for `aws_proxy_protocol_policy`."
---

`aws_proxy_protocol_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	proxy "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/proxy"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own protocol policy"})

    		ubx.Resource(proxy.ProtocolPolicy, "ci-runner", proxy.ProtocolPolicyConfig{
    			InstancePorts: []string{"example"},
    			LoadBalancer:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProtocolPolicy } from "jsr:@ubx/sdk-aws/aws/proxy/protocol_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own protocol policy" });

      resource(ProtocolPolicy, "ci-runner", {
        instancePorts: ["example"],
        loadBalancer: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.proxy import ProtocolPolicy, ProtocolPolicyConfig

    def describe():
        ubx.intent("payments own protocol policy")

        ubx.resource(ProtocolPolicy, "ci-runner", ProtocolPolicyConfig(
            instance_ports=["example"],
            load_balancer="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_proxy_protocol_policy, with instance ports a set
    value, load balancer example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_ports" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
