---
title: "aws_proxy_protocol_policy"
description: "Real, generated bindings for `aws_proxy_protocol_policy`."
---

`aws_proxy_protocol_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/proxy"

    cfg := proxy.ProtocolPolicyConfig{
        InstancePorts: []string{"example"},
        LoadBalancer: "example",
    }
    ubx.Resource(proxy.ProtocolPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProtocolPolicy } from
      "jsr:@ubx/sdk-aws/aws/proxy/protocol_policy";

    resource(ProtocolPolicy, "example", {
      instancePorts: ["example"],
      loadBalancer: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.proxy import ProtocolPolicy, ProtocolPolicyConfig

    cfg = ProtocolPolicyConfig(
        instance_ports=["example"],
        load_balancer="example",
    )
    ubx.resource(ProtocolPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_proxy_protocol_policy with instance_ports,
load_balancer.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_ports" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
