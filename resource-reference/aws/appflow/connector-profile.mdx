---
title: "aws_appflow_connector_profile"
description: "Real, generated bindings for `aws_appflow_connector_profile`."
---

`aws_appflow_connector_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connector profile"})

    		ubx.Resource(appflow.ConnectorProfile, "ci-runner", appflow.ConnectorProfileConfig{
    			ConnectionMode: "example",
    			ConnectorType:  "example",
    			Name:           "ci-runner",
    			ConnectorLabel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectorProfile } from "jsr:@ubx/sdk-aws/aws/appflow/connector_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own connector profile" });

      resource(ConnectorProfile, "ci-runner", {
        connectionMode: "example",
        connectorType: "example",
        name: "ci-runner",
        connectorLabel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appflow import ConnectorProfile, ConnectorProfileConfig

    def describe():
        ubx.intent("payments own connector profile")

        ubx.resource(ConnectorProfile, "ci-runner", ConnectorProfileConfig(
            connection_mode="example",
            connector_type="example",
            name="ci-runner",
            connector_label="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appflow_connector_profile called "ci-runner", with
    connection mode example, connector type example, connector label
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="connector_label" type="string">
  Optional.
</ResponseField>

<ResponseField name="connector_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="credentials_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
