---
title: "aws_wisdom_message_template_version"
description: "`aws_wisdom_message_template_version` publishes a real, immutable version of an `aws_wisdom_message_template`'s content..."
---

`aws_wisdom_message_template_version` publishes a real, immutable version of an `aws_wisdom_message_template`'s content, letting a reference to it pin to a specific, unchanging version rather than the template's editable draft.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wisdom "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wisdom"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own message template version"})

    		ubx.Resource(wisdom.MessageTemplateVersion, "example", wisdom.MessageTemplateVersionConfig{
    			MessageTemplateArn:           "arn:aws:iam::123456789012:policy/example",
    			MessageTemplateContentSha256: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MessageTemplateVersion } from "./local-sdk/aws/sdk/typescript/aws/wisdom/message_template_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own message template version" });

      resource(MessageTemplateVersion, "example", {
        messageTemplateArn: "arn:aws:iam::123456789012:policy/example",
        messageTemplateContentSha256: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wisdom import MessageTemplateVersion, MessageTemplateVersionConfig

    def describe():
        ubx.intent("example own message template version")

        ubx.resource(MessageTemplateVersion, "example", MessageTemplateVersionConfig(
            message_template_arn="arn:aws:iam::123456789012:policy/example",
            message_template_content_sha256="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wisdom_message_template_version, with message template
    arn arn:aws:iam::123456789012:policy/example, message template
    content sha256 example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="message_template_arn" type="string" required>
  The unqualified Amazon Resource Name (ARN) of the message template. Required.
</ResponseField>

<ResponseField name="message_template_content_sha256" type="string">
  The content SHA256 of the message template. Optional.
</ResponseField>

## Output properties

<ResponseField name="message_template_version_arn" type="string">
  The unqualified Amazon Resource Name (ARN) of the message template version. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="message_template_version_number" type="number">
  Current version number of the message template. Computed by AWS after `ship`.
</ResponseField>
