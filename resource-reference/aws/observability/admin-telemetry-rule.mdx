---
title: "aws_observability_admin_telemetry_rule"
description: "`aws_observability_admin_telemetry_rule` defines a real rule specifying which AWS resources, by type and selection criteria..."
---

`aws_observability_admin_telemetry_rule` defines a real rule specifying which AWS resources, by type and selection criteria, should have telemetry collection enabled and how, giving CloudWatch Observability Admin a way to audit and enforce telemetry configuration across an account.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	observability "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own admin telemetry rule"})

    		ubx.Resource(observability.AdminTelemetryRule, "ci-runner", observability.AdminTelemetryRuleConfig{
    			Rule:     map[string]any{"resource_type": "example"},
    			RuleName: "example-rule-name",
    			Tags:     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AdminTelemetryRule } from "./local-sdk/aws/sdk/typescript/aws/observability/admin_telemetry_rule.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own admin telemetry rule" });

      resource(AdminTelemetryRule, "ci-runner", {
        rule: { resourceType: "example" },
        ruleName: "example-rule-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.observability import AdminTelemetryRule, AdminTelemetryRuleConfig

    def describe():
        ubx.intent("payments own admin telemetry rule")

        ubx.resource(AdminTelemetryRule, "ci-runner", AdminTelemetryRuleConfig(
            rule={"resource_type": "example"},
            rule_name="example-rule-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_observability_admin_telemetry_rule, with rule a set
    value, rule name example-rule-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="rule" type="object" required>
  The telemetry rule Required.
  <Expandable title="properties">
    <ResponseField name="all_regions" type="bool">
      When true, the rule is replicated to all supported regions Optional.
    </ResponseField>
    <ResponseField name="allow_field_updates" type="bool">
      When true, configuration drift in managed telemetry resources will be detected and remediated for resource-level fields. Optional.
    </ResponseField>
    <ResponseField name="destination_configuration" type="object">
      The destination configuration for telemetry data Optional.
      <Expandable title="properties">
        <ResponseField name="cloudtrail_parameters" type="object">
          Telemetry parameters for Cloudtrail Optional.
          <Expandable title="properties">
            <ResponseField name="advanced_event_selectors" type="list(object)" required>
              Create fine-grained selectors for AWS CloudTrail management and data. Required.
              <Expandable title="properties">
                <ResponseField name="field_selectors" type="list(object)">
                  Specifies the list of field selectors that define which CloudTrail events match each advanced event selector, by comparing event fields such as eventName, readOnly, or resources against configured values for the telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="ends_with" type="list(string)">
                      Specifies the list of substrings that the selected event field's value must end with for the CloudTrail advanced event selector to match and the telemetry rule to forward the event. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="equals" type="list(string)">
                      Specifies the list of values that the selected CloudTrail event field must match exactly for an event to be included in the advanced event selector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="field" type="string">
                      Specifies the name of a CloudTrail event field (e.g., eventCategory, eventName, resources.type) used by an advanced event selector to filter which telemetry events are delivered to CloudTrail. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="not_ends_with" type="list(string)">
                      Specifies a list of string suffixes that the field value must not end with, excluding events where the corresponding CloudTrail field matches any of these suffixes from being forwarded by the telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="not_equals" type="list(string)">
                      In a CloudTrail advanced event selector, not_equals specifies a list of values that the evaluated field must not equal for an event to be included in the telemetry rule's CloudTrail destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="not_starts_with" type="list(string)">
                      Defines a list of prefix strings such that if the value of the selected event field begins with any of these prefixes, that event is excluded from the telemetry rule's CloudTrail-based selection. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="starts_with" type="list(string)">
                      In the telemetry rule's CloudTrail destination configuration, this list sets the prefix values for the field selector so that only events whose field value starts with one of these strings are included by the advanced event selector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  A user-defined name for an advanced event selector, used to identify the selector within the CloudTrail configuration for the Observability Admin telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_pattern" type="string">
          Pattern for telemetry data destination Optional.
        </ResponseField>
        <ResponseField name="destination_type" type="string">
          Type of telemetry destination Optional.
        </ResponseField>
        <ResponseField name="elbload_balancer_logging_parameters" type="object">
          Telemetry parameters for ELB/NLB Load Balancer Logs Optional.
          <Expandable title="properties">
            <ResponseField name="field_delimiter" type="string">
              A delimiter to delineate log fields Optional.
            </ResponseField>
            <ResponseField name="output_format" type="string">
              Specifies the output format for the load balancer access logs delivered to the telemetry sink, with valid values 'plain_text' or 'parquet'. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kms_key_arn" type="string">
          The Amazon Resource Name (ARN) of the customer-managed AWS KMS key used to encrypt the destination log groups specified in the Telemetry Rule. Optional.
        </ResponseField>
        <ResponseField name="log_delivery_parameters" type="object">
          Parameters for log delivery configuration Optional.
          <Expandable title="properties">
            <ResponseField name="log_types" type="list(string)">
              Types of logs to deliver Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retention_in_days" type="number">
          Number of days to retain the telemetry data in the specified destination Optional.
        </ResponseField>
        <ResponseField name="vpcflow_log_parameters" type="object">
          Telemetry parameters for VPC Flow logs Optional.
          <Expandable title="properties">
            <ResponseField name="log_format" type="string">
              The fields to include in the flow log record. If you omit this parameter, the flow log is created using the default format. Optional.
            </ResponseField>
            <ResponseField name="max_aggregation_interval" type="number">
              The maximum interval of time, in seconds, during which a flow of packets is captured and aggregated into a flow log record. Default is 600s. Optional.
            </ResponseField>
            <ResponseField name="traffic_type" type="string">
              The type of traffic captured for the flow log. Default is ALL Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="waflogging_parameters" type="object">
          Telemetry parameters for WAF v2 Web ACL Optional.
          <Expandable title="properties">
            <ResponseField name="log_type" type="string">
              The type of logs to generate for WAF. Optional.
            </ResponseField>
            <ResponseField name="logging_filter" type="object">
              Default handling for logs that don't match any of the specified filtering conditions. Optional.
              <Expandable title="properties">
                <ResponseField name="default_behavior" type="string">
                  The behavior required of the filter. Optional.
                </ResponseField>
                <ResponseField name="filters" type="list(object)">
                  A list of filters to be applied. Optional.
                  <Expandable title="properties">
                    <ResponseField name="behavior" type="string">
                      Specifies whether to keep or drop WAF logs that match this filter, with allowed values 'KEEP' or 'DROP'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="conditions" type="list(object)">
                      Defines the list of conditions for a WAF logging filter, where each condition matches on either a web ACL action (such as ALLOW or BLOCK) or a label name to determine which traffic is included in or excluded from the telemetry destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="action_condition" type="object">
                          A condition that matches web requests based on the action taken by AWS WAF (such as ALLOW, BLOCK, or COUNT), used within a logging filter in the WAF logging parameters to determine which requests are logged. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="action" type="string">
                              Specifies the action value (e.g., ALLOW, BLOCK, COUNT) that the condition matches to filter WAF log entries. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="label_name_condition" type="object">
                          Defines the exact WAF label name a request must contain for the logging filter to include the log, evaluated as one of the conditions in the filter's list for the WAF logging parameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_name" type="string">
                              The name of the AWS WAF label that must be present on a request for the logging filter condition to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="requirement" type="string">
                      Specifies whether the WAF logging filter condition is satisfied as a match (IS) or not match (IS_NOT) when applying the filter's keep/drop behavior to logs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="redacted_fields" type="list(object)">
              Fields not to be included in the logs. Optional.
              <Expandable title="properties">
                <ResponseField name="method" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="query_string" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  This field is the single header redaction specification, which contains the header name to be redacted from AWS WAF logs, ensuring that header's values are excluded from telemetry log output. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header that AWS WAF should redact from logs for this single-header redaction field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="regions" type="list(string)">
      List of AWS region codes where the rule should be replicated Optional.
    </ResponseField>
    <ResponseField name="resource_type" type="string" required>
      Resource Type associated with the Telemetry Rule Required.
    </ResponseField>
    <ResponseField name="selection_criteria" type="string">
      Selection Criteria on resource level for rule application Optional.
    </ResponseField>
    <ResponseField name="telemetry_source_types" type="list(string)">
      The telemetry source types for a telemetry rule. Optional.
    </ResponseField>
    <ResponseField name="telemetry_type" type="string" required>
      Telemetry Type associated with the Telemetry Rule Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  The name of the telemetry rule Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag used to label an AWS::ObservabilityAdmin::TelemetryRule resource, enabling resource organization and cost tracking. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="region_statuses" type="list(object)">
  Per-region replication status of the rule Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="region" type="string">
      The AWS Region code (e.g., us-east-1) where the telemetry rule is applied or reported, as part of the RegionStatuses list. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_arn" type="string">
      For the corresponding region in the region status list, this attribute contains the Amazon Resource Name (ARN) of the telemetry rule deployed in that region. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      Indicates whether the telemetry rule is active or inactive in the corresponding region, reflecting the rule's operational state for that region. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rule_arn" type="string">
  The arn of the telemetry rule Computed by AWS after `ship`.
</ResponseField>
