---
title: "aws_observability_admin_organization_telemetry_rule"
description: "`aws_observability_admin_organization_telemetry_rule` applies a real `aws_observability_admin_telemetry_rule`-style telemetry configuration across..."
---

`aws_observability_admin_organization_telemetry_rule` applies a real `aws_observability_admin_telemetry_rule`-style telemetry configuration across every account in an AWS Organization at once, rather than one account at a time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	observability "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own admin organization telemetry rule"})

    		ubx.Resource(observability.AdminOrganizationTelemetryRule, "example", observability.AdminOrganizationTelemetryRuleConfig{
    			Rule:     map[string]any{"resource_type": "example"},
    			RuleName: "example-rule-name",
    			Tags:     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AdminOrganizationTelemetryRule } from "./local-sdk/aws/sdk/typescript/aws/observability/admin_organization_telemetry_rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own admin organization telemetry rule" });

      resource(AdminOrganizationTelemetryRule, "example", {
        rule: { resourceType: "example" },
        ruleName: "example-rule-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.observability import AdminOrganizationTelemetryRule, AdminOrganizationTelemetryRuleConfig

    def describe():
        ubx.intent("example own admin organization telemetry rule")

        ubx.resource(AdminOrganizationTelemetryRule, "example", AdminOrganizationTelemetryRuleConfig(
            rule={"resource_type": "example"},
            rule_name="example-rule-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_observability_admin_organization_telemetry_rule, with
    rule a set value, rule name example-rule-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="rule" type="object" required>
  The telemetry rule Required.
  <Expandable title="properties">
    <ResponseField name="all_regions" type="bool">
      When true, the rule is replicated to all supported regions Optional.
    </ResponseField>
    <ResponseField name="allow_field_updates" type="bool">
      When true, configuration drift in managed telemetry resources will be detected and remediated for resource-level fields. Optional.
    </ResponseField>
    <ResponseField name="destination_configuration" type="object">
      The destination configuration for telemetry data Optional.
      <Expandable title="properties">
        <ResponseField name="cloudtrail_parameters" type="object">
          Telemetry parameters for Cloudtrail Optional.
          <Expandable title="properties">
            <ResponseField name="advanced_event_selectors" type="list(object)" required>
              Create fine-grained selectors for AWS CloudTrail management and data. Required.
              <Expandable title="properties">
                <ResponseField name="field_selectors" type="list(object)">
                  Specifies the list of field selectors (e.g., eventCategory, eventName, or resource type) used to filter which CloudTrail events are matched by each advanced event selector in the telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="ends_with" type="list(string)">
                      Defines the list of strings that the value of the referenced field (e.g., event name, resource type) must end with for the event to match the advanced event selector in a CloudTrail telemetry destination configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="equals" type="list(string)">
                      In the CloudTrail advanced event selectors of the destination configuration for this organization telemetry rule, the 'equals' field on a field selector defines the list of exact string values that the corresponding CloudTrail event attribute (e.g., eventName, readOnly) must match for the event to be delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="field" type="string">
                      Specifies the CloudTrail event field name (such as 'eventCategory', 'eventName', or 'resources.type') used by the advanced event selector to filter which events are included for delivery to the configured destination in this organization telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="not_ends_with" type="list(string)">
                      Specifies the list of values that the event field must not end with for the advanced event selector to match, filtering CloudTrail events in the destination configuration of the organization telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="not_equals" type="list(string)">
                      In the CloudTrail parameters of the telemetry rule, this list defines the values that the selected event field must not equal for the advanced event selector to match the event for telemetry collection. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="not_starts_with" type="list(string)">
                      Specifies a list of string prefixes that, when matched at the beginning of the selected event field's value, cause the event to be excluded from delivery, so only events whose field value does not start with any of these strings are delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="starts_with" type="list(string)">
                      Within a CloudTrail advanced event selector's field selector, this list defines the prefixes that the selected event field's value must start with for the telemetry rule to forward matching events to CloudTrail. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the advanced event selector, an optional user-defined label used to identify the selector within CloudTrail configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="destination_pattern" type="string">
          Pattern for telemetry data destination Optional.
        </ResponseField>
        <ResponseField name="destination_type" type="string">
          Type of telemetry destination Optional.
        </ResponseField>
        <ResponseField name="elbload_balancer_logging_parameters" type="object">
          Telemetry parameters for ELB/NLB Load Balancer Logs Optional.
          <Expandable title="properties">
            <ResponseField name="field_delimiter" type="string">
              A delimiter to delineate log fields Optional.
            </ResponseField>
            <ResponseField name="output_format" type="string">
              The output format for the load balancer access logs delivered to the configured destination, specifying whether the logs are written as text (gzip compressed) or in Parquet format. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kms_key_arn" type="string">
          The Amazon Resource Name (ARN) of the customer-managed AWS KMS key used to encrypt the destination log groups specified in the Telemetry Rule. Optional.
        </ResponseField>
        <ResponseField name="log_delivery_parameters" type="object">
          Parameters for log delivery configuration Optional.
          <Expandable title="properties">
            <ResponseField name="log_types" type="list(string)">
              Types of logs to deliver Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="retention_in_days" type="number">
          Number of days to retain the telemetry data in the specified destination Optional.
        </ResponseField>
        <ResponseField name="vpcflow_log_parameters" type="object">
          Telemetry parameters for VPC Flow logs Optional.
          <Expandable title="properties">
            <ResponseField name="log_format" type="string">
              The fields to include in the flow log record. If you omit this parameter, the flow log is created using the default format. Optional.
            </ResponseField>
            <ResponseField name="max_aggregation_interval" type="number">
              The maximum interval of time, in seconds, during which a flow of packets is captured and aggregated into a flow log record. Default is 600s. Optional.
            </ResponseField>
            <ResponseField name="traffic_type" type="string">
              The type of traffic captured for the flow log. Default is ALL Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="waflogging_parameters" type="object">
          Telemetry parameters for WAF v2 Web ACL Optional.
          <Expandable title="properties">
            <ResponseField name="log_type" type="string">
              The type of logs to generate for WAF. Optional.
            </ResponseField>
            <ResponseField name="logging_filter" type="object">
              Default handling for logs that don't match any of the specified filtering conditions. Optional.
              <Expandable title="properties">
                <ResponseField name="default_behavior" type="string">
                  The behavior required of the filter. Optional.
                </ResponseField>
                <ResponseField name="filters" type="list(object)">
                  A list of filters to be applied. Optional.
                  <Expandable title="properties">
                    <ResponseField name="behavior" type="string">
                      Determines whether log records that match the filter's condition are kept (KEEP) or dropped (DROP) from the WAF logging output. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="conditions" type="list(object)">
                      Specifies the match conditions (such as AWS WAF action, label, or country) applied to each log record to determine whether it is kept or dropped by this logging filter for WAF log delivery. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="action_condition" type="object">
                          Defines a condition that matches the WAF action (such as ALLOW, BLOCK, or COUNT) of a web request, used to determine whether the logging filter keeps or drops the log entry. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="action" type="string">
                              The action (such as ALLOW, BLOCK, or COUNT) taken by AWS WAF on the matched request, used as the condition for filtering WAF log entries. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="label_name_condition" type="object">
                          Represents the label name matching condition used to filter WAF logs, with properties for the label name and comparison operator. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="label_name" type="string">
                              Specifies the exact WAF label name (e.g., a label added by a rule or rule group) that a web request must carry for this label-name condition in the logging filter to match and be included in the telemetry destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="requirement" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="redacted_fields" type="list(object)">
              Fields not to be included in the logs. Optional.
              <Expandable title="properties">
                <ResponseField name="method" type="string">
                  This field indicates that the HTTP method (such as GET or POST) is redacted (omitted) from AWS WAF logs in the telemetry rule's WAF logging destination configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  Configures a single HTTP header to redact from AWS WAF logging output, using a field-match type that identifies the header by its name. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header whose value will be redacted from the WAF logging data for this organization telemetry rule's destination configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="regions" type="list(string)">
      List of AWS region codes where the rule should be replicated Optional.
    </ResponseField>
    <ResponseField name="resource_type" type="string" required>
      Resource Type associated with the Organization Telemetry Rule Required.
    </ResponseField>
    <ResponseField name="scope" type="string">
      Selection Criteria on scope level for rule application Optional.
    </ResponseField>
    <ResponseField name="selection_criteria" type="string">
      Selection Criteria on resource level for rule application Optional.
    </ResponseField>
    <ResponseField name="telemetry_source_types" type="list(string)">
      The telemetry source types for a telemetry rule. Optional.
    </ResponseField>
    <ResponseField name="telemetry_type" type="string" required>
      Telemetry Type associated with the Organization Telemetry Rule Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  The name of the organization telemetry rule Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="region_statuses" type="list(object)">
  Per-region replication status of the rule Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="region" type="string">
      The AWS region code (e.g., us-east-1) identifying the region whose telemetry rule status is reported in this region_statuses entry. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_arn" type="string">
      The Amazon Resource Name (ARN) of the telemetry rule created for a specific region, as reported in the region status map of the organization telemetry rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      The current status of the organization telemetry rule in the associated AWS region, indicating whether the rule is active for that region. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rule_arn" type="string">
  The arn of the organization telemetry rule Computed by AWS after `ship`.
</ResponseField>
