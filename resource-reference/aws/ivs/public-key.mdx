---
title: "aws_ivs_public_key"
description: "`aws_ivs_public_key` registers a real public key IVS validates participant tokens for an `aws_ivs_stage` against..."
---

`aws_ivs_public_key` registers a real public key IVS validates participant tokens for an `aws_ivs_stage` against, the Real-Time Streaming counterpart to `aws_ivs_playback_key_pair`'s own channel-playback authorization.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ivs "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ivs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own public key"})

    		ubx.Resource(ivs.PublicKey, "example", ivs.PublicKeyConfig{
    			Name:              "example",
    			PublicKeyMaterial: "example",
    			Tags:              []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PublicKey } from "@ubx/sdk-aws/aws/ivs/public_key";

    export default stack("example", () => {
      intent({ summary: "example own public key" });

      resource(PublicKey, "example", {
        name: "example",
        publicKeyMaterial: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ivs import PublicKey, PublicKeyConfig
    from ubx.aws.ivs.public_key import PublicKey_Tags

    def describe():
        ubx.intent("example own public key")

        ubx.resource(PublicKey, "example", PublicKeyConfig(
            name="example",
            public_key_material="example",
            tags=[PublicKey_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ivs_public_key called "example", with public key
    material example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string">
  Name of the public key to be imported. The value does not need to be unique. Optional.
</ResponseField>

<ResponseField name="public_key_material" type="string">
  The public portion of a customer-generated key pair. This field is required to create the AWS::IVS::PublicKey resource. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs that contain metadata for the asset model. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value associated with a tag key attached to the AWS IVS public key, used for organizing or identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Key-pair identifier. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Key-pair identifier. Computed by AWS after `ship`.
</ResponseField>
