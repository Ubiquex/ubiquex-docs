---
title: "aws_synthetics_group_association"
description: "Real, generated bindings for `aws_synthetics_group_association`."
---

`aws_synthetics_group_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/synthetics"

    cfg := synthetics.GroupAssociationConfig{
        CanaryArn: "example",
        GroupName: "example-group-name",
    }
    ubx.Resource(synthetics.GroupAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GroupAssociation } from
      "jsr:@ubx/sdk-aws/aws/synthetics/group_association";

    resource(GroupAssociation, "example", {
      canaryArn: "example",
      groupName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.synthetics import (
        GroupAssociation,
        GroupAssociationConfig,
    )

    cfg = GroupAssociationConfig(
        canary_arn="example",
        group_name="example",
    )
    ubx.resource(GroupAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_synthetics_group_association with canary_arn,
group_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="canary_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
