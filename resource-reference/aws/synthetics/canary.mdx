---
title: "aws_synthetics_canary"
description: "Real, generated bindings for `aws_synthetics_canary`."
---

`aws_synthetics_canary` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own canary"})

    		ubx.Resource(synthetics.Canary, "ci-runner", synthetics.CanaryConfig{
    			ArtifactS3Location:     "example",
    			ExecutionRoleArn:       "arn:aws:iam::123456789012:policy/ci",
    			Handler:                "example",
    			Name:                   "ci-runner",
    			RuntimeVersion:         "example",
    			DeleteLambda:           true,
    			FailureRetentionPeriod: 1,
    			S3Bucket:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Canary } from "jsr:@ubx/sdk-aws/aws/synthetics/canary";

    export default stack("payments", () => {
      intent({ summary: "payments own canary" });

      resource(Canary, "ci-runner", {
        artifactS3Location: "example",
        executionRoleArn: "arn:aws:iam::123456789012:policy/ci",
        handler: "example",
        name: "ci-runner",
        runtimeVersion: "example",
        deleteLambda: true,
        failureRetentionPeriod: 1,
        s3Bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.synthetics import Canary, CanaryConfig

    def describe():
        ubx.intent("payments own canary")

        ubx.resource(Canary, "ci-runner", CanaryConfig(
            artifact_s3_location="example",
            execution_role_arn="arn:aws:iam::123456789012:policy/ci",
            handler="example",
            name="ci-runner",
            runtime_version="example",
            delete_lambda=True,
            failure_retention_period=1,
            s3_bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_synthetics_canary called "ci-runner", with artifact s3
    location example, execution role arn
    arn:aws:iam::123456789012:policy/ci, handler example, runtime
    version example, delete lambda true, failure retention period 1,
    s3 bucket example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="artifact_s3_location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="delete_lambda" type="bool">
  Optional.
</ResponseField>

<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="failure_retention_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="handler" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="runtime_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_bucket" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="start_canary" type="bool">
  Optional.
</ResponseField>

<ResponseField name="success_retention_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="zip_file" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_location_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeline" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="created" type="string">
    </ResponseField>
    <ResponseField name="last_modified" type="string">
    </ResponseField>
    <ResponseField name="last_started" type="string">
    </ResponseField>
    <ResponseField name="last_stopped" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
