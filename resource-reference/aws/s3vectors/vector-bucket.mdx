---
title: "aws_s3vectors_vector_bucket"
description: "Real, generated bindings for `aws_s3vectors_vector_bucket`."
---

`aws_s3vectors_vector_bucket` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3vectors "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3vectors"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vector bucket"})

    		ubx.Resource(s3vectors.VectorBucket, "ci-runner", s3vectors.VectorBucketConfig{
    			VectorBucketName: "example-vector-bucket-name",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VectorBucket } from "jsr:@ubx/sdk-aws/aws/s3vectors/vector_bucket";

    export default stack("payments", () => {
      intent({ summary: "payments own vector bucket" });

      resource(VectorBucket, "ci-runner", {
        vectorBucketName: "example-vector-bucket-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3vectors import VectorBucket, VectorBucketConfig

    def describe():
        ubx.intent("payments own vector bucket")

        ubx.resource(VectorBucket, "ci-runner", VectorBucketConfig(
            vector_bucket_name="example-vector-bucket-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3vectors_vector_bucket, with vector bucket name
    example-vector-bucket-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="encryption_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
    </ResponseField>
    <ResponseField name="sse_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vector_bucket_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encryption_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
    </ResponseField>
    <ResponseField name="sse_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vector_bucket_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
