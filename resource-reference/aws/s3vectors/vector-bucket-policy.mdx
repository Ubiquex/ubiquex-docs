---
title: "aws_s3vectors_vector_bucket_policy"
description: "Real, generated bindings for `aws_s3vectors_vector_bucket_policy`."
---

`aws_s3vectors_vector_bucket_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3vectors"

    cfg := s3vectors.VectorBucketPolicyConfig{
        Policy: "example",
        VectorBucketArn: "example",
    }
    ubx.Resource(s3vectors.VectorBucketPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VectorBucketPolicy } from
      "jsr:@ubx/sdk-aws/aws/s3vectors/vector_bucket_policy";

    resource(VectorBucketPolicy, "example", {
      policy: "example",
      vectorBucketArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3vectors import (
        VectorBucketPolicy,
        VectorBucketPolicyConfig,
    )

    cfg = VectorBucketPolicyConfig(
        policy="example",
        vector_bucket_arn="example",
    )
    ubx.resource(VectorBucketPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3vectors_vector_bucket_policy with policy,
vector_bucket_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vector_bucket_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
