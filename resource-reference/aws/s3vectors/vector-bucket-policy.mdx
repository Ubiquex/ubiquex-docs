---
title: "aws_s3_vectors_vector_bucket_policy"
description: "`aws_s3_vectors_vector_bucket_policy` attaches a real resource-based access policy to an `aws_s3_vectors_vector_bucket`..."
---

`aws_s3_vectors_vector_bucket_policy` attaches a real resource-based access policy to an `aws_s3_vectors_vector_bucket`, controlling which principals can create, query, or manage vector indexes within it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3vectors "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/s3vectors"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own vectors vector bucket policy"})

    		ubx.Resource(s3vectors.VectorBucketPolicy, "example", s3vectors.VectorBucketPolicyConfig{
    			Policy:           "example",
    			VectorBucketArn:  "arn:aws:iam::123456789012:policy/example",
    			VectorBucketName: "example-vector-bucket-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VectorBucketPolicy } from "@ubx/sdk-aws/aws/s3vectors/vector_bucket_policy";

    export default stack("example", () => {
      intent({ summary: "example own vectors vector bucket policy" });

      resource(VectorBucketPolicy, "example", {
        policy: "example",
        vectorBucketArn: "arn:aws:iam::123456789012:policy/example",
        vectorBucketName: "example-vector-bucket-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3vectors.vector_bucket_policy import VectorBucketPolicy, VectorBucketPolicyConfig

    def describe():
        ubx.intent("example own vectors vector bucket policy")

        ubx.resource(VectorBucketPolicy, "example", VectorBucketPolicyConfig(
            policy="example",
            vector_bucket_arn="arn:aws:iam::123456789012:policy/example",
            vector_bucket_name="example-vector-bucket-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3_vectors_vector_bucket_policy, with policy example,
    vector bucket arn arn:aws:iam::123456789012:policy/example,
    vector bucket name example-vector-bucket-name.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="policy" type="any" required>
  A policy document containing permissions to add to the specified vector bucket. In IAM, you must provide policy documents in JSON format. However, in CloudFormation you can provide the policy in JSON or YAML format because CloudFormation converts YAML to JSON before submitting it to IAM. Required.
</ResponseField>

<ResponseField name="vector_bucket_arn" type="string">
  The Amazon Resource Name (ARN) of the vector bucket. Optional.
</ResponseField>

<ResponseField name="vector_bucket_name" type="string">
  The name of the vector bucket Optional.
</ResponseField>
