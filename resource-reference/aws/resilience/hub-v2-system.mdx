---
title: "aws_resilience_hub_v2_system"
description: "`aws_resilience_hub_v2_system` represents a real business application under the next-generation AWS Resilience Hub model (relaunched May 2026)..."
---

`aws_resilience_hub_v2_system` represents a real business application under the next-generation AWS Resilience Hub model (relaunched May 2026), the top-level resource that `aws_resilience_hub_v2_service`s and `aws_resilience_hub_v2_user_journey`s are organized under and that an `aws_resilience_hub_v2_policy` is ultimately assessed against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resilience "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resilience"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub v2 system"})

    		ubx.Resource(resilience.HubV2System, "example", resilience.HubV2SystemConfig{
    			Name:           "example",
    			Description:    "Managed by ubx.",
    			KmsKeyId:       "example",
    			SharingEnabled: true,
    			Tags:           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubV2System } from "./local-sdk/aws/sdk/typescript/aws/resilience/hub_v2_system.ts";

    export default stack("example", () => {
      intent({ summary: "example own hub v2 system" });

      resource(HubV2System, "example", {
        name: "example",
        description: "Managed by ubx.",
        kmsKeyId: "example",
        sharingEnabled: true,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.resilience import HubV2System, HubV2SystemConfig

    def describe():
        ubx.intent("example own hub v2 system")

        ubx.resource(HubV2System, "example", HubV2SystemConfig(
            name="example",
            description="Managed by ubx.",
            kms_key_id="example",
            sharing_enabled=True,
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resilience_hub_v2_system called "example", with
    description Managed by ubx., kms key id example, sharing enabled
    true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the system. Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The KMS key ID for encrypting system data. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the system. Required.
</ResponseField>

<ResponseField name="sharing_enabled" type="bool">
  Whether the system is enabled to be shared with other members of the Organization. Only applicable if the system owner is a management account or delegated admin. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags assigned to the system. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the system was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="system_arn" type="string">
  The ARN of the system. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="system_id" type="string">
  The system ID. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the system was last updated. Computed by AWS after `ship`.
</ResponseField>
