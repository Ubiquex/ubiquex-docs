---
title: "aws_resilience_hub_v2_policy"
description: "`aws_resilience_hub_v2_policy` defines a real, modular, composable resiliency policy under the next-generation AWS Resilience Hub model..."
---

`aws_resilience_hub_v2_policy` defines a real, modular, composable resiliency policy under the next-generation AWS Resilience Hub model, reusable and assignable across multiple `aws_resilience_hub_v2_service`s at once rather than one application at a time. This next-generation model, with generative-AI-powered failure mode analysis and organization-wide reporting, launched in May 2026, roughly three months ago, so behavior here is verified live rather than assumed from general knowledge.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resilience "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resilience"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub v2 policy"})

    		ubx.Resource(resilience.HubV2Policy, "example", resilience.HubV2PolicyConfig{
    			Name:            "example",
    			AvailabilitySlo: map[string]any{"target": 1},
    			DataRecovery:    map[string]any{"time_between_backups_in_minutes": 1},
    			Description:     "Managed by ubx.",
    			KmsKeyId:        "example",
    			MultiAz:         map[string]any{"disaster_recovery_approach": "example"},
    			MultiRegion:     map[string]any{"disaster_recovery_approach": "example"},
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubV2Policy } from "./local-sdk/aws/sdk/typescript/aws/resilience/hub_v2_policy.ts";

    export default stack("example", () => {
      intent({ summary: "example own hub v2 policy" });

      resource(HubV2Policy, "example", {
        name: "example",
        availabilitySlo: { target: 1 },
        dataRecovery: { timeBetweenBackupsInMinutes: 1 },
        description: "Managed by ubx.",
        kmsKeyId: "example",
        multiAz: { disasterRecoveryApproach: "example" },
        multiRegion: { disasterRecoveryApproach: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.resilience import HubV2Policy, HubV2PolicyConfig

    def describe():
        ubx.intent("example own hub v2 policy")

        ubx.resource(HubV2Policy, "example", HubV2PolicyConfig(
            name="example",
            availability_slo={"target": 1},
            data_recovery={"time_between_backups_in_minutes": 1},
            description="Managed by ubx.",
            kms_key_id="example",
            multi_az={"disaster_recovery_approach": "example"},
            multi_region={"disaster_recovery_approach": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resilience_hub_v2_policy called "example", with
    availability slo a set value, data recovery a set value,
    description Managed by ubx., kms key id example, multi az a set
    value, multi region a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="availability_slo" type="object">
  Defines the availability service level objective (SLO) for the policy, specifying the target uptime percentage (e.g., 99.99%) that the application must meet to be considered resilient. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="target" type="number">
      Availability target percentage. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_recovery" type="object">
  Defines the data recovery objectives for the policy, specifying the targeted Recovery Point Objective (RPO) and Recovery Time Objective (RTO) in seconds for restoring data after a disruption. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="time_between_backups_in_minutes" type="number">
      Time between backups in minutes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the policy. Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The KMS key ID for encrypting policy data. Optional.
</ResponseField>

<ResponseField name="multi_az" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="disaster_recovery_approach" type="string">
      Multi-AZ disaster recovery approach. Optional.
    </ResponseField>
    <ResponseField name="rpo_in_minutes" type="number">
      Recovery Point Objective in minutes. Optional.
    </ResponseField>
    <ResponseField name="rto_in_minutes" type="number">
      Recovery Time Objective in minutes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="multi_region" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="disaster_recovery_approach" type="string">
      Multi-Region disaster recovery approach. Optional.
    </ResponseField>
    <ResponseField name="rpo_in_minutes" type="number">
      Recovery Point Objective in minutes. Optional.
    </ResponseField>
    <ResponseField name="rto_in_minutes" type="number">
      Recovery Time Objective in minutes. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the policy. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags assigned to the policy. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair attached to the AWS Resilience Hub v2 policy, used to label and categorize the policy for management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="associated_service_count" type="number">
  The number of services associated with this policy. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp when the policy was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_arn" type="string">
  The ARN of the policy. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the policy was last updated. Computed by AWS after `ship`.
</ResponseField>
