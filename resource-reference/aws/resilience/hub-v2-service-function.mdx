---
title: "aws_resilience_hub_v2_service_function"
description: "`aws_resilience_hub_v2_service_function` registers a real, specific function or component, a Lambda function, a container..."
---

`aws_resilience_hub_v2_service_function` registers a real, specific function or component, a Lambda function, a container, within an `aws_resilience_hub_v2_service`, giving the next-generation Resilience Hub model visibility down to individual pieces of a deployable unit rather than only the unit as a whole.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resilience "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resilience"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub v2 service function"})

    		ubx.Resource(resilience.HubV2ServiceFunction, "ci-runner", resilience.HubV2ServiceFunctionConfig{
    			Criticality: "example",
    			Name:        "ci-runner",
    			ServiceArn:  "arn:aws:iam::123456789012:policy/ci",
    			Description: "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubV2ServiceFunction } from "./local-sdk/aws/sdk/typescript/aws/resilience/hub_v2_service_function.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own hub v2 service function" });

      resource(HubV2ServiceFunction, "ci-runner", {
        criticality: "example",
        name: "ci-runner",
        serviceArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.resilience import HubV2ServiceFunction, HubV2ServiceFunctionConfig

    def describe():
        ubx.intent("payments own hub v2 service function")

        ubx.resource(HubV2ServiceFunction, "ci-runner", HubV2ServiceFunctionConfig(
            criticality="example",
            name="ci-runner",
            service_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resilience_hub_v2_service_function called "ci-runner",
    with criticality example, service arn
    arn:aws:iam::123456789012:policy/ci, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="criticality" type="string" required>
  The criticality of the service function. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the service function. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the service function. Required.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  The ARN of the parent service. Required.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the service function was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_count" type="number">
  The number of resources associated with this function. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_function_id" type="string">
  The server-generated service function ID. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source" type="string">
  The source of the service function. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the service function was last updated. Computed by AWS after `ship`.
</ResponseField>
