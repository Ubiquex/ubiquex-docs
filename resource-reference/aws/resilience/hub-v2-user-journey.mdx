---
title: "aws_resilience_hub_v2_user_journey"
description: "`aws_resilience_hub_v2_user_journey` describes a real, critical business path, the sequence of services and steps a user actually depends on..."
---

`aws_resilience_hub_v2_user_journey` describes a real, critical business path, the sequence of services and steps a user actually depends on, through an `aws_resilience_hub_v2_system`, letting resilience be assessed against outcomes that matter to users rather than only individual services in isolation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resilience "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resilience"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub v2 user journey"})

    		ubx.Resource(resilience.HubV2UserJourney, "ci-runner", resilience.HubV2UserJourneyConfig{
    			Name:             "ci-runner",
    			SystemIdentifier: "example",
    			Description:      "Managed by ubx.",
    			PolicyArn:        "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubV2UserJourney } from "./local-sdk/aws/sdk/typescript/aws/resilience/hub_v2_user_journey.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own hub v2 user journey" });

      resource(HubV2UserJourney, "ci-runner", {
        name: "ci-runner",
        systemIdentifier: "example",
        description: "Managed by ubx.",
        policyArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.resilience import HubV2UserJourney, HubV2UserJourneyConfig

    def describe():
        ubx.intent("payments own hub v2 user journey")

        ubx.resource(HubV2UserJourney, "ci-runner", HubV2UserJourneyConfig(
            name="ci-runner",
            system_identifier="example",
            description="Managed by ubx.",
            policy_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resilience_hub_v2_user_journey called "ci-runner",
    with system identifier example, description Managed by ubx.,
    policy arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the user journey. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the user journey. Required.
</ResponseField>

<ResponseField name="policy_arn" type="string">
  The ARN of the resilience policy to associate with this user journey. Optional.
</ResponseField>

<ResponseField name="system_identifier" type="string" required>
  The system ARN or system ID that owns this user journey. Required.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the user journey was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the user journey was last updated. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_journey_id" type="string">
  The server-generated user journey ID. Computed by AWS after `ship`.
</ResponseField>
