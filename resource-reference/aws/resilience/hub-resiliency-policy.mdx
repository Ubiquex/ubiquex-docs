---
title: "aws_resilience_hub_resiliency_policy"
description: "`aws_resilience_hub_resiliency_policy` defines real recovery time and recovery point objectives that an `aws_resilience_hub_app` is assessed against..."
---

`aws_resilience_hub_resiliency_policy` defines real recovery time and recovery point objectives that an `aws_resilience_hub_app` is assessed against, the target Resilience Hub measures the application's actual architecture against to produce a resiliency score.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resiliencehub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/resiliencehub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hub resiliency policy"})

    		ubx.Resource(resiliencehub.ResiliencyPolicy, "example", resiliencehub.ResiliencyPolicyConfig{
    			Policy:                 map[string]any{"az": map[string]any{"rpo_in_secs": 1}},
    			PolicyName:             "example-policy-name",
    			Tier:                   "example",
    			DataLocationConstraint: "example",
    			PolicyDescription:      "example",
    			Tags:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResiliencyPolicy } from "@ubx/sdk-aws/aws/resiliencehub/resiliency_policy";

    export default stack("example", () => {
      intent({ summary: "example own hub resiliency policy" });

      resource(ResiliencyPolicy, "example", {
        policy: { az: { rpoInSecs: 1 } },
        policyName: "example-policy-name",
        tier: "example",
        dataLocationConstraint: "example",
        policyDescription: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.resiliencehub.resiliency_policy import ResiliencyPolicy, ResiliencyPolicyConfig

    def describe():
        ubx.intent("example own hub resiliency policy")

        ubx.resource(ResiliencyPolicy, "example", ResiliencyPolicyConfig(
            policy={"az": {"rpo_in_secs": 1}},
            policy_name="example-policy-name",
            tier="example",
            data_location_constraint="example",
            policy_description="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resilience_hub_resiliency_policy, with policy a set
    value, policy name example-policy-name, tier example, data
    location constraint example, policy description example, tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_location_constraint" type="string">
  Data Location Constraint of the Policy. Optional.
</ResponseField>

<ResponseField name="policy" type="object" required>
  Configures the resiliency policy rules per workload component, specifying the recovery point objective (RPO) and recovery time objective (RTO) in seconds for each named component. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="az" type="object" required>
      Failure Policy. Required.
      <Expandable title="properties">
        <ResponseField name="rpo_in_secs" type="number" required>
          RPO in seconds. Required.
        </ResponseField>
        <ResponseField name="rto_in_secs" type="number" required>
          RTO in seconds. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hardware" type="object" required>
      Failure Policy. Required.
      <Expandable title="properties">
        <ResponseField name="rpo_in_secs" type="number" required>
          RPO in seconds. Required.
        </ResponseField>
        <ResponseField name="rto_in_secs" type="number" required>
          RTO in seconds. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="region" type="object">
      Failure Policy. Optional.
      <Expandable title="properties">
        <ResponseField name="rpo_in_secs" type="number" required>
          RPO in seconds. Required.
        </ResponseField>
        <ResponseField name="rto_in_secs" type="number" required>
          RTO in seconds. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="software" type="object" required>
      Failure Policy. Required.
      <Expandable title="properties">
        <ResponseField name="rpo_in_secs" type="number" required>
          RPO in seconds. Required.
        </ResponseField>
        <ResponseField name="rto_in_secs" type="number" required>
          RTO in seconds. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_description" type="string">
  Description of Resiliency Policy. Optional.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  Name of Resiliency Policy. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

<ResponseField name="tier" type="string" required>
  Resiliency Policy Tier. Required.
</ResponseField>

## Output properties

<ResponseField name="policy_arn" type="string">
  Amazon Resource Name (ARN) of the Resiliency Policy. Computed by AWS after `ship`.
</ResponseField>
