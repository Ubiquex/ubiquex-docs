---
title: "aws_outposts_capacity_task"
description: "Real, generated bindings for `aws_outposts_capacity_task`."
---

`aws_outposts_capacity_task` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	outposts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/outposts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own capacity task"})

    		ubx.Resource(outposts.CapacityTask, "ci-runner", outposts.CapacityTaskConfig{
    			OutpostIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CapacityTask } from "jsr:@ubx/sdk-aws/aws/outposts/capacity_task";

    export default stack("payments", () => {
      intent({ summary: "payments own capacity task" });

      resource(CapacityTask, "ci-runner", {
        outpostIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.outposts import CapacityTask, CapacityTaskConfig

    def describe():
        ubx.intent("payments own capacity task")

        ubx.resource(CapacityTask, "ci-runner", CapacityTaskConfig(
            outpost_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_outposts_capacity_task, with outpost identifier
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="asset_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="order_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="outpost_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="task_action_on_blocking_instances" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="asset_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="capacity_task_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="completion_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="order_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="task_action_on_blocking_instances" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
