---
title: "aws_pcaconnector_scep_challenge"
description: "`aws_pcaconnector_scep_challenge` creates a real, static SCEP password that a device or mobile device management system presents to..."
---

`aws_pcaconnector_scep_challenge` creates a real, static SCEP password that a device or mobile device management system presents to an `aws_pcaconnector_scep_connector` to authenticate its certificate enrollment request.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pcaconnector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pcaconnector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scep challenge"})

    		ubx.Resource(pcaconnector.ScepChallenge, "ci-runner", pcaconnector.ScepChallengeConfig{
    			ConnectorArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScepChallenge } from "./local-sdk/aws/sdk/typescript/aws/pcaconnector/scep_challenge.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own scep challenge" });

      resource(ScepChallenge, "ci-runner", {
        connectorArn: "arn:aws:iam::123456789012:policy/ci",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.pcaconnector import ScepChallenge, ScepChallengeConfig

    def describe():
        ubx.intent("payments own scep challenge")

        ubx.resource(ScepChallenge, "ci-runner", ScepChallengeConfig(
            connector_arn="arn:aws:iam::123456789012:policy/ci",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pcaconnector_scep_challenge, with connector arn
    arn:aws:iam::123456789012:policy/ci, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connector_arn" type="string" required>
  The Amazon Resource Name (ARN) of the connector that this SCEP challenge belongs to, required to create the challenge under that connector. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="challenge_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the SCEP challenge within AWS Private CA Connector for SCEP, assigned by AWS when the challenge is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
