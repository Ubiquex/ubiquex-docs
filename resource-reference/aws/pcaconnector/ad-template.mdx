---
title: "aws_pcaconnector_ad_template"
description: "`aws_pcaconnector_ad_template` defines a real certificate template, mirroring an AD certificate template's own settings..."
---

`aws_pcaconnector_ad_template` defines a real certificate template, mirroring an AD certificate template's own settings, that an `aws_pcaconnector_ad_connector` issues certificates against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pcaconnector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pcaconnector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ad template"})

    		ubx.Resource(pcaconnector.AdTemplate, "ci-runner", pcaconnector.AdTemplateConfig{
    			ConnectorArn:                  "arn:aws:iam::123456789012:policy/ci",
    			Definition:                    map[string]any{"template_v2": map[string]any{"certificate_validity": map[string]any{"renewal_period": map[string]any{"period": 1}}}},
    			Name:                          "ci-runner",
    			ReenrollAllCertificateHolders: true,
    			Tags:                          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AdTemplate } from "./local-sdk/aws/sdk/typescript/aws/pcaconnector/ad_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own ad template" });

      resource(AdTemplate, "ci-runner", {
        connectorArn: "arn:aws:iam::123456789012:policy/ci",
        definition: {
          templateV2: { certificateValidity: { renewalPeriod: { period: 1 } } },
        },
        name: "ci-runner",
        reenrollAllCertificateHolders: true,
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.pcaconnector import AdTemplate, AdTemplateConfig

    def describe():
        ubx.intent("payments own ad template")

        ubx.resource(AdTemplate, "ci-runner", AdTemplateConfig(
            connector_arn="arn:aws:iam::123456789012:policy/ci",
            definition={"template_v2": {"certificate_validity": {"renewal_period": {"period": 1}}}},
            name="ci-runner",
            reenroll_all_certificate_holders=True,
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pcaconnector_ad_template called "ci-runner", with
    connector arn arn:aws:iam::123456789012:policy/ci, definition a
    set value, reenroll all certificate holders true, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connector_arn" type="string" required>
  The Amazon Resource Name (ARN) of the AWS Private CA Connector for Active Directory connector to which this template belongs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="definition" type="object" required>
  The definition object specifies the certificate template's validity, private key attributes, and flags, which are required by the PCA Connector for Active Directory to create and manage the template. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="template_v2" type="object">
      Specifies the v2 configuration for the Active Directory connector template, including certificate validity, extensions, key usage, private key attributes, and subject name flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="certificate_validity" type="object">
          Specifies the validity period of certificates issued using this template, including a numeric period and its unit (days, months, or years). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="renewal_period" type="object">
              Specifies how long before certificate expiration the issued certificates are automatically renewed, expressed as a period type (e.g., DAYS) and a numeric value. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="period" type="number">
                  The numeric duration for the renewal period of certificates issued under this template, interpreted according to the associated renewal_period type (e.g., DAYS or MONTHS). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period_type" type="string">
                  Specifies the time unit (e.g., DAYS, MONTHS, YEARS) for the renewal period, which determines how far in advance certificates are renewed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="validity_period" type="object">
              Specifies the duration and unit of time that certificates issued using this Private CA Connector for AD template remain valid. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="period" type="number">
                  The numeric length of the certificate validity period, interpreted in units of the associated validity period type (e.g., days, months, or years) for certificates issued from this PCA Connector AD template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period_type" type="string">
                  Specifies the unit of time (DAYS, MONTHS, or YEARS) for the certificate validity period in the PCA Connector for AD template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enrollment_flags" type="object">
          Defines enrollment behavior flags for the certificate template, such as requiring user interaction, including symmetric algorithms, and removing invalid certificates from the personal store, which are applied during certificate enrollment. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_key_reuse_on_nt_token_keyset_storage_full" type="bool">
              Enables or disables the reuse of an existing cryptographic key when the NT token keyset storage is full, which is an enrollment flag for the certificate template in AWS Private CA Connector for Active Directory. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_symmetric_algorithms" type="bool">
              When set to true, this flag includes symmetric algorithms in the certificate enrollment request for this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="no_security_extension" type="bool">
              When true, this enrollment flag causes the CA to omit the security extension from certificates issued by the template; when false, the security extension is included. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="remove_invalid_certificate_from_personal_store" type="bool">
              This boolean flag controls whether the certificate template instructs the CA to automatically remove an invalid certificate from the user's personal certificate store after enrollment. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_interaction_required" type="bool">
              Specifies whether certificate enrollment requires the user to interact with the client, such as entering a PIN or approving the request, in the Active Directory environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="extensions" type="object">
          Defines the certificate extension settings, such as application policies and key usage, that are included on certificates issued with this Active Directory connector template. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="application_policies" type="object">
              Specifies the application policies (extended key usage) that define the intended uses of certificates issued with this template, such as client authentication or code signing. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="critical" type="bool">
                  Indicates whether the application policies extension is marked as critical in the Active Directory certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="policies" type="list(object)">
                  Specifies the list of application policies (such as Smart Card Logon, Code Signing, or custom OIDs) that are included in the Application Policies extension of the certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="policy_object_identifier" type="string">
                      Specifies the object identifier (OID) of an application policy used in the certificate template, such as a numeric OID like 1.3.6.1.5.5.7.3.1 for server authentication. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="policy_type" type="string">
                      Sets the application policy type for the certificate, selecting a predefined OID such as ANY_APPLICATION or CLIENT_AUTHENTICATION, or CUSTOM to require a corresponding PolicyObjectIdentifier. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key_usage" type="object">
              Defines the key usage extension in the template, specifying the intended cryptographic purposes of the certificate's public key, such as digital signature, key encipherment, data encipherment, and whether the extension is critical. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="critical" type="bool">
                  Specifies whether the key usage extension is marked critical in the certificate template, which determines whether clients must strictly enforce the key usage restrictions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="usage_flags" type="object">
                  Specifies the set of key usage purposes (such as digital signature, key encipherment, or data encipherment) that the certificate's public key is authorized for in the key usage extension of a version 2 Active Directory template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_encipherment" type="bool">
                      Indicates whether the key can be used for data encipherment, permitting the encryption of arbitrary data with the certificate's public key. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="digital_signature" type="bool">
                      Whether the key usage extension includes the digitalSignature purpose, allowing certificates issued under this template to be used for digital signatures such as authentication or message integrity. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_agreement" type="bool">
                      Specifies whether the certificate key can be used for key agreement, such as in Diffie-Hellman key exchange, as part of the KeyUsage extension in the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_encipherment" type="bool">
                      Indicates whether the key usage extension permits the certificate's public key to be used for key encipherment, i.e., encrypting symmetric keys or other data for key transport. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="non_repudiation" type="bool">
                      Indicates whether the certificate's key can be used for non-repudiation, which proves the authenticity and integrity of a message or transaction in the key usage extension of the AD connector template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="general_flags" type="object">
          Configures the general flags for the v2 certificate template, such as AutoEnrollment, MachineType, NonexpiringCertificates, and UserType. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="auto_enrollment" type="bool">
              When enabled, this flag allows certificates issued from the template to be automatically enrolled by Active Directory clients, enabling auto-enrollment without requiring manual certificate requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="machine_type" type="bool">
              Specifies whether the certificate template is intended for machine (computer) accounts rather than user accounts in the Active Directory environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="private_key_attributes" type="object">
          Defines the private key attributes for the certificate template version 2, including the required minimum key length, key specification (e.g., RSA or ECDSA), and the allowed cryptographic service providers. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="crypto_providers" type="list(string)">
              Specifies the list of allowed cryptographic service providers (CSPs) that can be used to generate and store the private key for certificates issued from this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_spec" type="string">
              Specifies whether the private key generated by the template is intended for key exchange (encryption) or digital signatures. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="minimal_key_length" type="number">
              Specifies the minimum length in bits (e.g., 1024, 2048, or 4096) of the private key that certificates requested from this PCA Connector AD template must have. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="private_key_flags" type="object">
          Specifies the private key flags for the v2 template definition, determining whether the certificate private key can be exported and which Windows client versions are supported. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="client_version" type="string">
              Specifies the Windows Server client version (WIN_SERVER_2008 or WIN_SERVER_2012) for the private key flags in the v2 template definition, which determines the key algorithm and security settings for certificates issued by the Active Directory connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exportable_key" type="bool">
              Indicates whether the private key associated with the certificate can be exported, controlling the exportable key flag in the certificate template's private key flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strong_key_protection_required" type="bool">
              Specifies whether the private key must use strong key protection, requiring the user to enter a password each time they access or use the key. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subject_name_flags" type="object">
          Controls the subject name flags for the V2 certificate template, determining which fields (such as common name, directory path, DNS, email, and IP) are required or included as subject alternative names in certificates issued by the Active Directory connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="require_common_name" type="bool">
              Requires the Common Name (CN) component to be present in the subject name for certificates issued by this connector template when set to true. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_directory_path" type="bool">
              When enabled, this flag requires the certificate's subject name to include the full directory path (distinguished name) of the AD object, causing certificate requests that do not use a subject name with the directory path to be rejected. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_dns_as_cn" type="bool">
              When enabled, requires the certificate's subject common name (CN) to be set to the DNS name of the principal, as part of the subject name flags for the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_email" type="bool">
              Specifies whether the subject name for certificates issued from this template must include the email address of the principal. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_directory_guid" type="bool">
              Specifies whether the subject alternative name (SAN) must include the directory GUID of the object for the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_dns" type="bool">
              Indicates whether the certificate must include a DNS name in the subject alternative name (SAN) extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_domain_dns" type="bool">
              When enabled, includes the domain DNS attribute of the Active Directory entity in the certificate's subject alternative name (SAN). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_email" type="bool">
              If true, the certificate template requires an email address to be included in the subject alternative name (SAN) of issued certificates. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_spn" type="bool">
              Specifies whether the template requires that a Service Principal Name (SPN) be present in the certificate's subject alternative name (SAN). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_upn" type="bool">
              Specifies whether the subject alternative name (SAN) of the certificate must include the user principal name (UPN). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="superseded_templates" type="list(string)">
          This field lists the names of existing Active Directory certificate templates that this template supersedes, so that certificates issued from the superseded templates are replaced by certificates from this new template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="template_v3" type="object">
      Defines the v3 template definition for the AWS Private CA Connector for Active Directory, including settings for certificate validity, key usage, extensions, and enrollment flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="certificate_validity" type="object">
          Specifies the validity period and renewal period for certificates issued from this template, including the duration and its unit (e.g., days, months, years). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="renewal_period" type="object">
              Specifies how long before certificate expiration that the certificate will be renewed, using a period defined by a numeric value and time unit (such as days). Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="period" type="number">
                  Defines the numeric duration of the renewal period, which combined with the sibling 'type' field specifies how long before certificate expiration a certificate can be renewed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period_type" type="string">
                  The unit of time (e.g., DAYS, WEEKS, MONTHS, YEARS) used to measure the renewal period for the certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="validity_period" type="object">
              Defines the numeric duration and unit of time (days, months, or years) during which certificates issued using this Active Directory connector template remain valid. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="period" type="number">
                  The numeric duration of the certificate validity period in a template v3 definition, interpreted with its associated unit type (e.g., days, months, or years) to determine how long issued certificates remain valid. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period_type" type="string">
                  The unit of time (DAYS, MONTHS, or YEARS) used with the numeric period value to define the certificate validity duration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enrollment_flags" type="object">
          Defines the enrollment flag settings for this v3 template, controlling behaviors such as inclusion of basic constraints and security extensions on certificates issued to AD principals. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_key_reuse_on_nt_token_keyset_storage_full" type="bool">
              When set to true, this flag allows certificate enrollment to reuse an existing key pair when the NT token keyset storage is full, preventing enrollment failure due to storage capacity. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_symmetric_algorithms" type="bool">
              Setting this to true includes symmetric algorithms in the certificate request for the AD CS template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="no_security_extension" type="bool">
              When set to true, this enrollment flag omits the security extension from certificates issued by this template, which is commonly needed for smart card logon and certificate-based authentication in Active Directory. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="remove_invalid_certificate_from_personal_store" type="bool">
              When enabled, this enrollment flag removes invalid certificates from the user's personal certificate store during enrollment, as specified by the REMOVE_INVALID_CERTIFICATE_FROM_PERSONAL_STORE flag in the AWS PCA Connector for AD template definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_interaction_required" type="bool">
              Specifies whether the user must be prompted to interact during certificate enrollment (rather than enrolling silently) when using this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="extensions" type="object">
          Configures the certificate extensions applied by the template, including application policies and key usage settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="application_policies" type="object">
              Defines the Application Policies (Extended Key Usage) extension for the certificate template, specifying a list of application policy object identifiers (OIDs) that determine the certificate's intended purposes in the resulting certificates. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="critical" type="bool">
                  Indicates whether the application policies extension is critical, requiring certificates to be rejected if a client does not recognize this extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="policies" type="list(object)">
                  Defines the list of application policies (extended key usage purposes) that certificates issued from this template will support, where each policy item is specified by either an object identifier (OID) or a built-in application policy type. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="policy_object_identifier" type="string">
                      Specifies the object identifier (OID) of an application policy (extended key usage) to include in the certificate template's Application Policies extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="policy_type" type="string">
                      Specifies the type of application policy (enhanced key usage) for the template, selecting either a predefined usage such as CLIENT_AUTHENTICATION or CODE_SIGNING, or CUSTOM when the policy object must include a separate custom policy OID. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key_usage" type="object">
              Specifies the X.509 KeyUsage extension, including a critical flag and usage flags (digital signature, key encipherment, data encipherment, key agreement, key certificate sign, CRL sign, encipher only, decipher only) that define the valid cryptographic purposes of the certificate's public key. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="critical" type="bool">
                  Whether the KeyUsage X.509 certificate extension is marked as critical in the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="usage_flags" type="object">
                  Identifies which key usage flags (for example, digital signature, key encipherment, and key agreement) will be included in the certificate's KeyUsage extension for the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_encipherment" type="bool">
                      Determines whether the key usage extension includes the DataEncipherment flag, authorizing the certificate's public key to be used for data encryption. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="digital_signature" type="bool">
                      A boolean that, when set to true, enables the digitalSignature key usage flag, allowing the certificate's public key to be used for verifying digital signatures. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_agreement" type="bool">
                      Indicates whether the key usage extension includes the Key Agreement (keyAgreement) flag in the certificate template's usage flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_encipherment" type="bool">
                      Indicates whether the key can be used for key encipherment, enabling a public key to encrypt symmetric keys or other data for key transport. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="non_repudiation" type="bool">
                      Indicates whether the key usage extension includes the non-repudiation flag, enabling the key to be used to verify digital signatures that provide non-repudiation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="general_flags" type="object">
          Specifies the general flags for the certificate template, including MachineType and UserType booleans that determine whether the template is intended for machine or user certificate enrollment in the Active Directory environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="auto_enrollment" type="bool">
              Indicates whether the certificate template is enabled for automatic certificate enrollment for users or computers. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="machine_type" type="bool">
              Specifies whether this certificate template is configured for machine (computer) principals in Active Directory, enabling computer authentication via issued certificates. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="hash_algorithm" type="string">
          Specifies the hash algorithm (SHA256, SHA384, or SHA512) used to sign certificates issued from this Active Directory connector template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="private_key_attributes" type="object">
          Defines private key attributes such as minimum key length, key specification (RSA or ECC), and cryptographic providers for the certificate template version 3 in AWS Private CA Connector for Active Directory. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="algorithm" type="string">
              Specifies the private key algorithm (RSA or ECDSA) used for the certificate template in AWS Private CA Connector for Active Directory. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="crypto_providers" type="list(string)">
              Specifies the list of cryptographic service providers (CSPs) and key storage providers (KSPs) allowed for private key generation in this Active Directory certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_spec" type="string">
              Determines the cryptographic algorithm (RSA or ECC) used for private keys generated for certificates issued under this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_usage_property" type="object">
              Defines the key usage property for the private key in a version 3 PCA Connector for AD template, specifying the certificate's intended key usages (such as digital signature or key encipherment) through either property flags or a property type. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="property_flags" type="object">
                  Defines the set of key usage flags (Decrypt, KeyAgreement, KeyEncipherment, and Sign) that determine which cryptographic operations are permitted for the private key in the certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="decrypt" type="bool">
                      When true, the certificate template's private key is allowed to be used for decryption operations, influencing the key usage extension in issued certificates. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_agreement" type="bool">
                      Determines whether the private key can be used for key agreement (e.g., Diffie-Hellman) in the key usage extension of the Active Directory certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sign" type="bool">
                      Specifies whether the private key can be used for digital signature operations (the Digital Signature bit in the KeyUsage extension of the certificate template). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="property_type" type="string">
                  Specifies whether the key usage property for the private key allows all key usages (KEY_USAGE_PROPERTY_TYPE_ALL) or a custom set defined via the associated property flags (KEY_USAGE_PROPERTY_TYPE_CUSTOM). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="minimal_key_length" type="number">
              Specifies the minimum key size in bits (e.g., 2048, 3072, or 4096) that the private key must have for certificates issued from this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="private_key_flags" type="object">
          Controls the private key flags for the v3 certificate template, including the client version that determines the minimum supported Windows client for certificate enrollment. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="client_version" type="string">
              Specifies the minimum Windows Server client version (e.g., WINDOWS_SERVER_2008, WINDOWS_SERVER_2012) that the private key is compatible with for this v3 template's private key flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exportable_key" type="bool">
              Indicates whether the private key associated with the certificate issued by this template can be exported. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_alternate_signature_algorithm" type="bool">
              Specifies whether the private key must support the alternate signature algorithm, which is required for CNG (Cryptography Next Generation) keys in this v3 template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strong_key_protection_required" type="bool">
              If enabled, the certificate template requires strong key protection for the private key, meaning the user must interact with the key (e.g., enter a password) before it can be used. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subject_name_flags" type="object">
          Specifies the subject name flags for the template v3, which control whether common name, directory path, DNS as CN, email, and auto-enrollment are included or required in the certificate's subject name. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="require_common_name" type="bool">
              Specifies whether the certificate subject must include a common name (CN) when using template v3 in the PCA Connector for Active Directory template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_directory_path" type="bool">
              Indicates whether the certificate subject name must include the directory path (e.g., the object's distinguished name path) for the principal, as part of the subject name flags in the version 3 template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_dns_as_cn" type="bool">
              When true, forces the certificate's subject common name (CN) to be populated with the DNS name from the certificate request, corresponding to the RequireDNSAsCN flag in AWS PCA Connector for AD template subject name flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_email" type="bool">
              When set to true, the certificate subject name must include the requester's email address (the Email field) in the subject name. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_directory_guid" type="bool">
              Specifies whether the certificate's subject alternative name (SAN) must include the Active Directory directory GUID (objectGUID) of the enrolled subject. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_dns" type="bool">
              Indicates whether certificates issued by this template must include a DNS name in the subject alternative name (SAN) extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_domain_dns" type="bool">
              Indicates whether the domain DNS name must be included in the certificate's subject alternative name (SAN). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_email" type="bool">
              Specifies whether the issued certificate's subject alternative name (SAN) must include an email address. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_spn" type="bool">
              If true, the certificate template requires the subject alternative name (SAN) to include the Service Principal Name (SPN) of the certificate subject. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_upn" type="bool">
              When enabled, requires the certificate's Subject Alternative Name (SAN) to include the User Principal Name (UPN) of the AD user or computer. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="superseded_templates" type="list(string)">
          Specifies the list of existing Active Directory certificate template names that this v3 template supersedes, preventing those older templates from being used for new certificate requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="template_v4" type="object">
      Defines the configuration for an Active Directory certificate template using schema version 4, including settings for certificate validity, enrollment flags, private key attributes, key usage, and extensions. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="certificate_validity" type="object">
          Specifies the validity period of certificates issued from this template, including the numeric duration and its unit (days, months, or years), as defined in the version 4 template definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="renewal_period" type="object">
              Specifies the duration (as a period and time unit) before a certificate's expiration when the CA connector automatically renews the certificate for templates defined in this CertificateValidity object. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="period" type="number">
                  The numerical value of the renewal period's duration, interpreted according to the associated unit type (such as DAYS, MONTHS, or YEARS) defined in the renewal_period's 'type' field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period_type" type="string">
                  Specifies the unit of time (DAYS, WEEKS, MONTHS, or YEARS) used for the renewal period of the certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="validity_period" type="object">
              Specifies how long the certificate is valid, expressed as a numerical period and a unit type such as days, months, or years. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="period" type="number">
                  Specifies the numeric magnitude of the certificate validity period, which combined with the associated type (e.g., DAYS, MONTHS, YEARS) determines how long certificates issued from this template remain valid. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period_type" type="string">
                  Specifies the time unit (DAYS, MONTHS, or YEARS) for the validity period of certificates issued using this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enrollment_flags" type="object">
          Defines the enrollment flags for the v4 certificate template, controlling options such as whether user interaction is required during enrollment, whether auto-enrollment is permitted, and whether the security extension is included in issued certificates. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_key_reuse_on_nt_token_keyset_storage_full" type="bool">
              Enables certificate enrollment to reuse an existing key pair when the NT token keyset storage is full rather than failing. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_symmetric_algorithms" type="bool">
              Indicates whether symmetric algorithms are included in the certificate enrollment request for this template, allowing the client to use symmetric key algorithms for encryption. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="no_security_extension" type="bool">
              When true, the certificate issued by the private CA connector omits the security extension in its enrollment flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="remove_invalid_certificate_from_personal_store" type="bool">
              When enabled, this enrollment flag removes certificates that are invalid or have expired from the personal certificate store of the enrolling user or computer during certificate enrollment. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_interaction_required" type="bool">
              Indicates whether the certificate requester must be present and interact during enrollment, such as by selecting a cryptographic service provider, when using this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="extensions" type="object">
          Defines the certificate extensions (such as key usage and application policies) that will be included in certificates issued by the private CA based on this AD connector template. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="application_policies" type="object">
              Specifies the application policies extension for the certificate template, defining a list of policy OIDs that indicate the certificate's intended purposes and whether the extension is marked critical. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="critical" type="bool">
                  Indicates whether the application policies extension is marked as critical in the certificate template, causing a certificate with this extension to be rejected if a relying party does not recognize the application policy OIDs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="policies" type="list(object)">
                  The list of application policy objects (each containing an object identifier) that defines the extended key usage purposes for certificates issued using this template, such as client or server authentication. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="policy_object_identifier" type="string">
                      The object identifier (OID) that identifies the application policy (such as server authentication) to include in the certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="policy_type" type="string">
                      Specifies the type of application policy to include, such as 'ANSI_X9_57' for a standard X.509 policy or 'OMIT' to exclude the policy from the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key_usage" type="object">
              Defines the key usage extension for certificates issued by this template, specifying the permitted cryptographic purposes (such as digital signature or key encipherment) for the certificate's public key. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="critical" type="bool">
                  Specifies whether the key usage extension is marked as critical in the certificate template, requiring strict enforcement by certificate consumers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="usage_flags" type="object">
                  Specifies the key usage flag values (such as DigitalSignature, KeyEncipherment, DataEncipherment, KeyAgreement, NonRepudiation, etc.) that define the intended cryptographic purposes of the certificate's public key in the KeyUsage extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="data_encipherment" type="bool">
                      Specifies whether the data encipherment key usage flag is set, indicating that the certificate's public key can be used to encrypt data directly. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="digital_signature" type="bool">
                      Indicates whether the Digital Signature key usage flag is enabled for certificates issued by this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_agreement" type="bool">
                      Indicates whether the key can be used for key agreement (such as Diffie-Hellman key exchange) in the certificate's key usage extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_encipherment" type="bool">
                      Indicates whether the key can be used for key encipherment, a key usage extension flag in the Active Directory certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="non_repudiation" type="bool">
                      When true, the certificate's key usage extension includes the non-repudiation bit, allowing the key to be used for digital signatures that provide non-repudiation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="general_flags" type="object">
          Defines the general flags for the v4 certificate template, including whether auto-enrollment is enabled and whether the template applies to machine or user certificates. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="auto_enrollment" type="bool">
              Indicates whether the certificate template supports automatic enrollment, enabling client computers to automatically request and renew certificates based on this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="machine_type" type="bool">
              Determines whether the certificate template is issued to computer (machine) principals, as opposed to user principals. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="hash_algorithm" type="string">
          Specifies the hash algorithm (such as SHA256, SHA384, or SHA512) used for signing certificates issued from this Private CA Connector for Active Directory template. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="private_key_attributes" type="object">
          Defines the private key cryptographic configuration for the certificate template, including the key algorithm, key usage spec, minimal key length, and allowed cryptographic service providers used during AD certificate enrollment. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="algorithm" type="string">
              Specifies the cryptographic algorithm for the private key, with allowed values of RSA, ECDSA_P256, ECDSA_P384, or ECDSA_P521. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="crypto_providers" type="list(string)">
              Specifies the list of allowed cryptographic service providers (CSPs) or key storage providers (KSPs) that can be used for the private key in this Active Directory template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_spec" type="string">
              Specifies the cryptographic algorithm and key size for the private key, such as RSA_2048 or ECDSA_P256. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_usage_property" type="object">
              Specifies the key usage extension properties, including the set of key usage flags (such as digital signature, key encipherment, or key agreement) and whether the extension is marked critical, for the private key attributes of this template version 4 definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="property_flags" type="object">
                  Defines the key usage property flags (Decrypt, KeyAgreement, KeyEncipherment, and Sign) for the certificate template, controlling which private key operations the certificate is allowed to perform. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="decrypt" type="bool">
                      Indicates whether the private key can be used for decryption, controlling the 'Allow private key to be used for decryption' setting in the Active Directory certificate template's key usage flags. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="key_agreement" type="bool">
                      For an Active Directory certificate template definition, this boolean specifies whether the private key's key usage property includes the Key Agreement flag, allowing the key to be used for protocols like Diffie-Hellman key exchange. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="sign" type="bool">
                      Specifies whether the certificate key can be used for cryptographic signing (digital signature), enabling the certificate to sign data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="property_type" type="string">
                  Sets the key usage property type to 'ALL', which permits all key usages for the private key in the Active Directory connector template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="minimal_key_length" type="number">
              Specifies the minimum size, in bits, of the private key that can be used to generate certificates for this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="private_key_flags" type="object">
          Specifies the private key flags for the v4 template, defining the minimum client version and whether private keys are stored in the TPM or in software. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="client_version" type="string">
              Specifies the Windows client version that the private key flags apply to, controlling the minimum operating system version that can use the certificate template in the AD forest. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exportable_key" type="bool">
              Indicates whether the private key can be exported from the certificate store after issuance, corresponding to the 'Allow export of private key' option in Windows certificate templates. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_alternate_signature_algorithm" type="bool">
              Indicates whether the private key generated by this template uses an alternate signature algorithm, which is required for compatibility with certain client operating systems. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_same_key_renewal" type="bool">
              This boolean flag specifies whether renewals must use the same key pair as the original certificate, enforcing key continuity for certificates issued under this template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="strong_key_protection_required" type="bool">
              Determines whether the private key associated with this AD connector template requires strong key protection, meaning a user must provide a password or other approval before the key can be used. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use_legacy_provider" type="bool">
              Specifies whether to use the legacy cryptographic service provider (CSP) instead of the Cryptography API: Next Generation (CNG) provider for the private key. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subject_name_flags" type="object">
          Specifies which subject name components -- such as common name, directory path, DNS as common name, email, SPN, and UPN -- are required in the certificate's subject name for this template v4. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="require_common_name" type="bool">
              Requires that the certificate subject include a common name (CN) when this subject name flag is set. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_directory_path" type="bool">
              When true, this flag requires the certificate subject name to include the full directory path (Distinguished Name) of the Active Directory object. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_dns_as_cn" type="bool">
              When enabled, requires the certificate subject's common name (CN) to be set to the DNS name, as part of the SubjectNameFlagsV4 settings for the AD connector template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="require_email" type="bool">
              When set to true, this flag requires that the user's email address appear in the certificate's subject name for the Active Directory template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_directory_guid" type="bool">
              Specifies whether the subject alternative name (SAN) of the certificate must include the directory GUID of the AD object, enabling directory GUID-based identity in the certificate's SAN extension. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_dns" type="bool">
              When true, this flag requires the certificate's subject alternative name (SAN) to include the DNS name in the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_domain_dns" type="bool">
              Specifies whether the subject alternative name (SAN) must include the domain DNS name of the subject in the certificate template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_email" type="bool">
              When true, this flag requires that the subject alternative name (SAN) of the issued certificate include the email address of the enrollee. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_spn" type="bool">
              Indicates whether the certificate's subject alternative name (SAN) must include a Service Principal Name (SPN) for the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="san_require_upn" type="bool">
              When true, this flag requires that the certificate's Subject Alternative Name (SAN) includes the User Principal Name (UPN) of the Active Directory principal requesting the certificate, which is often required for smart card or other authentication scenarios. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="superseded_templates" type="list(string)">
          Specifies the list of existing certificate template names that this template supersedes, causing the CA connector to associate this new template with those older templates for replacement in Active Directory. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  A user-defined name for the certificate template, which must be unique within the connector and identifies the template in AWS Private CA Connector for Active Directory. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="reenroll_all_certificate_holders" type="bool">
  When set to true, triggers reissuance of certificates to all existing certificate holders when the template is updated or modified. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="template_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Private CA Connector for Active Directory template managed by this resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
