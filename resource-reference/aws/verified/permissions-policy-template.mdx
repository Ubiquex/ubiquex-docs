---
title: "aws_verified_permissions_policy_template"
description: "`aws_verified_permissions_policy_template` defines a real, reusable Cedar policy template with placeholders for principal and resource..."
---

`aws_verified_permissions_policy_template` defines a real, reusable Cedar policy template with placeholders for principal and resource, within an `aws_verified_permissions_policy_store`, so many similar policies (grant this specific user access to this specific document) can be instantiated from one template instead of writing each one out individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	verified "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verified"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own permissions policy template"})

    		ubx.Resource(verified.PermissionsPolicyTemplate, "example", verified.PermissionsPolicyTemplateConfig{
    			PolicyStoreId: "example",
    			Statement:     "example",
    			Name:          "example",
    			Description:   "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PermissionsPolicyTemplate } from "./local-sdk/aws/sdk/typescript/aws/verified/permissions_policy_template.ts";

    export default stack("example", () => {
      intent({ summary: "example own permissions policy template" });

      resource(PermissionsPolicyTemplate, "example", {
        policyStoreId: "example",
        statement: "example",
        name: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.verified import PermissionsPolicyTemplate, PermissionsPolicyTemplateConfig

    def describe():
        ubx.intent("example own permissions policy template")

        ubx.resource(PermissionsPolicyTemplate, "example", PermissionsPolicyTemplateConfig(
            policy_store_id="example",
            statement="example",
            name="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_verified_permissions_policy_template called "example",
    with policy store id example, statement example, description
    Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Specifies an optional text description for the policy template, used to provide context about the template's intended purpose. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="policy_store_id" type="string" required>
  The ID of the policy store in which this policy template is created, associating the template with a specific Cedar policy store. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="statement" type="string" required>
  The Cedar policy statement, written in the Cedar policy language, that defines the authorization rules for this policy template, optionally using placeholders like ?principal and ?resource. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="policy_template_id" type="string">
  The unique identifier (UUID) assigned by Amazon Verified Permissions to the policy template, used to reference the template in policies and API operations. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
