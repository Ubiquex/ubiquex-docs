---
title: "aws_aps_workspace"
description: "`aws_aps_workspace` creates a Prometheus-compatible workspace, the top-level container Amazon Managed Service for Prometheus ingests, stores..."
---

`aws_aps_workspace` creates a Prometheus-compatible workspace, the top-level container Amazon Managed Service for Prometheus ingests, stores, and serves metrics from, replicated across three Availability Zones and retained for 150 days by default.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	aps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/aps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workspace"})

    		ubx.Resource(aps.Workspace, "ci-runner", aps.WorkspaceConfig{
    			AlertManagerDefinition:    "example",
    			Alias:                     "example",
    			KmsKeyArn:                 "arn:aws:iam::123456789012:policy/ci",
    			LoggingConfiguration:      map[string]any{"log_group_arn": "example"},
    			QueryLoggingConfiguration: map[string]any{"destinations": []map[string]any{{"cloud_watch_logs": map[string]any{"log_group_arn": "example"}}}},
    			Tags:                      []map[string]any{{"key": "example"}},
    			WorkspaceConfiguration:    map[string]any{"limits_per_label_sets": []map[string]any{{"label_set": []map[string]any{{"name": "example-name"}}}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Workspace } from "./local-sdk/aws/sdk/typescript/aws/aps/workspace.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own workspace" });

      resource(Workspace, "ci-runner", {
        alertManagerDefinition: "example",
        alias: "example",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
        loggingConfiguration: { logGroupArn: "example" },
        queryLoggingConfiguration: {
          destinations: [{ cloudWatchLogs: { logGroupArn: "example" } }],
        },
        tags: [{ key: "example" }],
        workspaceConfiguration: {
          limitsPerLabelSets: [{ labelSet: [{ name: "example-name" }] }],
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.aps import Workspace, WorkspaceConfig

    def describe():
        ubx.intent("payments own workspace")

        ubx.resource(Workspace, "ci-runner", WorkspaceConfig(
            alert_manager_definition="example",
            alias="example",
            kms_key_arn="arn:aws:iam::123456789012:policy/ci",
            logging_configuration={"log_group_arn": "example"},
            query_logging_configuration={"destinations": [{"cloud_watch_logs": {"log_group_arn": "example"}}]},
            tags=[{"key": "example"}],
            workspace_configuration={"limits_per_label_sets": [{"label_set": [{"name": "example-name"}]}]},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_aps_workspace, with alert manager definition example,
    alias example, kms key arn arn:aws:iam::123456789012:policy/ci,
    logging configuration a set value, query logging configuration a
    set value, tags a set value, workspace configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alert_manager_definition" type="string">
  The AMP Workspace alert manager definition data Optional.
</ResponseField>

<ResponseField name="alias" type="string">
  AMP Workspace alias. Optional.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  KMS Key ARN used to encrypt and decrypt AMP workspace data. Optional.
</ResponseField>

<ResponseField name="logging_configuration" type="object">
  Logging configuration Optional.
  <Expandable title="properties">
    <ResponseField name="log_group_arn" type="string">
      CloudWatch log group ARN Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="query_logging_configuration" type="object">
  Query logging configuration Optional.
  <Expandable title="properties">
    <ResponseField name="destinations" type="list(object)" required>
      The destinations configuration for query logging Required.
      <Expandable title="properties">
        <ResponseField name="cloud_watch_logs" type="object">
          Specifies the CloudWatch Logs log group that receives query logs from the Amazon Managed Prometheus workspace. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="log_group_arn" type="string">
              The ARN of the CloudWatch Logs log group to which the Amazon Managed Service for Prometheus workspace sends its query logs. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="filters" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="qsp_threshold" type="number">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key applied to an Amazon Managed Service for Prometheus workspace, used to categorize and manage the workspace for purposes such as environment, team, or cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace_configuration" type="object">
  Workspace configuration Optional.
  <Expandable title="properties">
    <ResponseField name="limits_per_label_sets" type="list(object)">
      An array of label set and associated limits Optional.
      <Expandable title="properties">
        <ResponseField name="label_set" type="list(object)">
          Defines the list of label names (e.g., ["job", "team"]) that group time series into sets, and the workspace configuration applies the associated maximum active series limit per unique set of label values. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of a label set in the workspace configuration's limits_per_label_sets section, used to identify a group of labels for applying per-label-set series limits in Amazon Managed Prometheus. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The maximum number of active time series allowed for the label set defined in this Amazon Managed Prometheus workspace configuration limit, enforced per label set. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="limits" type="object">
          Specifies the query limits (for example, maximum number of samples and maximum query time) applied to a particular label set within the Amazon Managed Service for Prometheus workspace configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_series" type="number">
              The maximum number of unique time series (combinations of metric name and label values) that a per-label-set limit allows to be active in the workspace, beyond which incoming samples for that label set are rejected and the limit is enforced. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="out_of_order_time_window_in_seconds" type="number">
      The time window in seconds for accepting out-of-order samples Optional.
    </ResponseField>
    <ResponseField name="retention_period_in_days" type="number">
      How many days that metrics are retained in the workspace Optional.
    </ResponseField>
    <ResponseField name="rule_query_offset_in_seconds" type="number">
      Duration in seconds to offset rule evaluation queries into the past Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Workspace arn. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="prometheus_endpoint" type="string">
  AMP Workspace prometheus endpoint Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="workspace_id" type="string">
  Required to identify a specific APS Workspace. Computed by AWS after `ship`.
</ResponseField>
