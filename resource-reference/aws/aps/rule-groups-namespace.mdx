---
title: "aws_aps_rule_groups_namespace"
description: "`aws_aps_rule_groups_namespace` holds a set of Prometheus alerting and recording rules inside an `aws_aps_workspace`..."
---

`aws_aps_rule_groups_namespace` holds a set of Prometheus alerting and recording rules inside an `aws_aps_workspace`, the same PromQL-based rule mechanism open-source Prometheus uses, managed without the caller having to run and scale their own Alertmanager.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	aps "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/aps"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rule groups namespace"})

    		ubx.Resource(aps.RuleGroupsNamespace, "ci-runner", aps.RuleGroupsNamespaceConfig{
    			Data:      "example",
    			Name:      "ci-runner",
    			Workspace: "example",
    			Tags:      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleGroupsNamespace } from "./local-sdk/aws/sdk/typescript/aws/aps/rule_groups_namespace.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own rule groups namespace" });

      resource(RuleGroupsNamespace, "ci-runner", {
        data: "example",
        name: "ci-runner",
        workspace: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.aps import RuleGroupsNamespace, RuleGroupsNamespaceConfig

    def describe():
        ubx.intent("payments own rule groups namespace")

        ubx.resource(RuleGroupsNamespace, "ci-runner", RuleGroupsNamespaceConfig(
            data="example",
            name="ci-runner",
            workspace="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_aps_rule_groups_namespace called "ci-runner", with
    data example, workspace example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data" type="string" required>
  The RuleGroupsNamespace data. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The RuleGroupsNamespace name. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Amazon Managed Service for Prometheus rule groups namespace, used to categorize and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value for a tag assigned to the AWS Prometheus rule groups namespace, used to organize, identify, and manage the resource through cost allocation and operational metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workspace" type="string" required>
  Required to identify a specific APS Workspace associated with this RuleGroupsNamespace. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The RuleGroupsNamespace ARN. Computed by AWS after `ship`.
</ResponseField>
