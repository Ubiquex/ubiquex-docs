---
title: "aws_inspector_v2_code_security_integration"
description: "`aws_inspector_v2_code_security_integration` connects Amazon Inspector to a real source-code platform, GitHub or GitLab..."
---

`aws_inspector_v2_code_security_integration` connects Amazon Inspector to a real source-code platform, GitHub or GitLab, so Inspector's Code Security capability can scan up to 100,000 real repositories' default branches for vulnerabilities directly in the source, shifting detection left of a deployed, running resource. Launched June 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspectorv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/inspectorv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own code security integration"})

    		ubx.Resource(inspectorv2.CodeSecurityIntegration, "example", inspectorv2.CodeSecurityIntegrationConfig{
    			Name:                     "example",
    			CreateIntegrationDetails: map[string]any{"gitlab_self_managed": map[string]any{"access_token": "example", "instance_url": "example"}},
    			Tags:                     "example",
    			Type:                     "example",
    			UpdateIntegrationDetails: map[string]any{"github": map[string]any{"code": "example", "installation_id": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CodeSecurityIntegration } from "@ubx/sdk-aws/aws/inspectorv2/code_security_integration";

    export default stack("example", () => {
      intent({ summary: "example own code security integration" });

      resource(CodeSecurityIntegration, "example", {
        name: "example",
        createIntegrationDetails: {
          gitlabSelfManaged: { accessToken: "example", instanceUrl: "example" },
        },
        tags: "example",
        type: "example",
        updateIntegrationDetails: {
          github: { code: "example", installationId: "example" },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.inspectorv2 import CodeSecurityIntegration, CodeSecurityIntegrationConfig
    from ubx.aws.inspectorv2.code_security_integration import CodeSecurityIntegration_CreateIntegrationDetails, CodeSecurityIntegration_CreateIntegrationDetails_GitlabSelfManaged, CodeSecurityIntegration_UpdateIntegrationDetails, CodeSecurityIntegration_UpdateIntegrationDetails_Github

    def describe():
        ubx.intent("example own code security integration")

        ubx.resource(CodeSecurityIntegration, "example", CodeSecurityIntegrationConfig(
            name="example",
            create_integration_details=CodeSecurityIntegration_CreateIntegrationDetails(gitlab_self_managed=CodeSecurityIntegration_CreateIntegrationDetails_GitlabSelfManaged(access_token="example", instance_url="example")),
            tags="example",
            type="example",
            update_integration_details=CodeSecurityIntegration_UpdateIntegrationDetails(github=CodeSecurityIntegration_UpdateIntegrationDetails_Github(code="example", installation_id="example")),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_inspector_v2_code_security_integration called
    "example", with create integration details a set value, tags
    example, type example, update integration details a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="create_integration_details" type="object">
  Specifies the event type and API call used to create the Inspector v2 code security integration, defining when and how the integration is established for a repository. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="gitlab_self_managed" type="object" required>
      Provides the configuration details for integrating AWS Inspector code security with a self-managed GitLab instance, including the necessary connection and authentication information. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_token" type="string" required>
          The personal access token for the self-managed GitLab instance that AWS Inspector uses to authenticate and access repositories for code security scanning. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_url" type="string" required>
          The base URL of the self-managed GitLab instance that AWS Inspector connects to for code security integration. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Code Security Integration name Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

<ResponseField name="type" type="string">
  Specifies the type of code security integration to configure, such as GITHUB or GITLAB, indicating which source code repository service to connect to Inspector for vulnerability scanning. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="update_integration_details" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="github" type="object">
      The GitHub-specific configuration object within the update integration details, which defines repository-level scan and encryption settings for the AWS Inspector V2 code security integration. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="string" required>
          Required.
        </ResponseField>
        <ResponseField name="installation_id" type="string" required>
          The unique identifier of the GitHub App installation within the GitHub organization or account that AWS Inspector V2 uses to access and scan repositories. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="gitlab_self_managed" type="object">
      This object holds the settings needed to update the integration with a self-managed GitLab repository, including the instance URL and authentication credentials. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="auth_code" type="string" required>
          The personal access token (auth code) required for AWS Inspector v2 to authenticate to the self-managed GitLab instance when updating the code security integration. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Code Security Integration ARN Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="authorization_url" type="string">
  Authorization URL for OAuth flow Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Creation timestamp Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_at" type="string">
  Last update timestamp Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The read-only integration status, automatically set to 'ENABLED' or 'DISABLED' to indicate whether Amazon Inspector code security scanning is active for the account. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="status_reason" type="string">
  Reason for the current status Computed by AWS after `ship`.
</ResponseField>
