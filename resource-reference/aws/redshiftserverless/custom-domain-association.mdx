---
title: "aws_redshiftserverless_custom_domain_association"
description: "Real, generated bindings for `aws_redshiftserverless_custom_domain_association`."
---

`aws_redshiftserverless_custom_domain_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshiftserverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshiftserverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom domain association"})

    		ubx.Resource(redshiftserverless.CustomDomainAssociation, "ci-runner", redshiftserverless.CustomDomainAssociationConfig{
    			CustomDomainCertificateArn: "arn:aws:iam::123456789012:policy/ci",
    			CustomDomainName:           "example-custom-domain-name",
    			WorkgroupName:              "example-workgroup-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomDomainAssociation } from "jsr:@ubx/sdk-aws/aws/redshiftserverless/custom_domain_association";

    export default stack("payments", () => {
      intent({ summary: "payments own custom domain association" });

      resource(CustomDomainAssociation, "ci-runner", {
        customDomainCertificateArn: "arn:aws:iam::123456789012:policy/ci",
        customDomainName: "example-custom-domain-name",
        workgroupName: "example-workgroup-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshiftserverless import CustomDomainAssociation, CustomDomainAssociationConfig

    def describe():
        ubx.intent("payments own custom domain association")

        ubx.resource(CustomDomainAssociation, "ci-runner", CustomDomainAssociationConfig(
            custom_domain_certificate_arn="arn:aws:iam::123456789012:policy/ci",
            custom_domain_name="example-custom-domain-name",
            workgroup_name="example-workgroup-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshiftserverless_custom_domain_association, with
    custom domain certificate arn
    arn:aws:iam::123456789012:policy/ci, custom domain name
    example-custom-domain-name, workgroup name
    example-workgroup-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="custom_domain_certificate_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workgroup_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="custom_domain_certificate_expiry_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
