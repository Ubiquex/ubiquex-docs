---
title: "aws_connect_contact_flow_module_alias"
description: "`aws_connect_contact_flow_module_alias` creates a real named pointer to a specific `aws_connect_contact_flow_module_version`..."
---

`aws_connect_contact_flow_module_alias` creates a real named pointer to a specific `aws_connect_contact_flow_module_version`, letting a calling contact flow be redirected to a new module version without changing the reference it calls.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own contact flow module alias"})

    		ubx.Resource(connect.ContactFlowModuleAlias, "example", connect.ContactFlowModuleAliasConfig{
    			ContactFlowModuleId:      "example",
    			ContactFlowModuleVersion: 1,
    			Name:                     "example",
    			Description:              "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContactFlowModuleAlias } from "@ubx/sdk-aws/aws/connect/contact_flow_module_alias";

    export default stack("example", () => {
      intent({ summary: "example own contact flow module alias" });

      resource(ContactFlowModuleAlias, "example", {
        contactFlowModuleId: "example",
        contactFlowModuleVersion: 1,
        name: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect.contact_flow_module_alias import ContactFlowModuleAlias, ContactFlowModuleAliasConfig

    def describe():
        ubx.intent("example own contact flow module alias")

        ubx.resource(ContactFlowModuleAlias, "example", ContactFlowModuleAliasConfig(
            contact_flow_module_id="example",
            contact_flow_module_version=1,
            name="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_contact_flow_module_alias called "example",
    with contact flow module id example, contact flow module version
    1, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="contact_flow_module_id" type="string" required>
  The identifier of the contact flow module (ARN) this alias is tied to. Required.
</ResponseField>

<ResponseField name="contact_flow_module_version" type="number" required>
  The version number of the contact flow module this alias points to. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the alias. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the alias. Required.
</ResponseField>

## Output properties

<ResponseField name="alias_id" type="string">
  The unique identifier of the alias. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="contact_flow_module_alias_arn" type="string">
  The identifier of the contact flow module alias (ARN). This is constructed from the ContactFlowModuleArn and AliasId. Computed by AWS after `ship`.
</ResponseField>
