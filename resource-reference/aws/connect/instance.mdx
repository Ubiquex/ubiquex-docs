---
title: "aws_connect_instance"
description: "Real, generated bindings for `aws_connect_instance`."
---

`aws_connect_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance"})

    		ubx.Resource(connect.Instance, "ci-runner", connect.InstanceConfig{
    			IdentityManagementType:       "example",
    			InboundCallsEnabled:          true,
    			OutboundCallsEnabled:         true,
    			AutoResolveBestVoicesEnabled: true,
    			ContactFlowLogsEnabled:       true,
    			ContactLensEnabled:           true,
    			DirectoryId:                  "example",
    			EarlyMediaEnabled:            true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "jsr:@ubx/sdk-aws/aws/connect/instance";

    export default stack("payments", () => {
      intent({ summary: "payments own instance" });

      resource(Instance, "ci-runner", {
        identityManagementType: "example",
        inboundCallsEnabled: true,
        outboundCallsEnabled: true,
        autoResolveBestVoicesEnabled: true,
        contactFlowLogsEnabled: true,
        contactLensEnabled: true,
        directoryId: "example",
        earlyMediaEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import Instance, InstanceConfig

    def describe():
        ubx.intent("payments own instance")

        ubx.resource(Instance, "ci-runner", InstanceConfig(
            identity_management_type="example",
            inbound_calls_enabled=True,
            outbound_calls_enabled=True,
            auto_resolve_best_voices_enabled=True,
            contact_flow_logs_enabled=True,
            contact_lens_enabled=True,
            directory_id="example",
            early_media_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_instance, with identity management type
    example, inbound calls enabled true, outbound calls enabled true,
    auto resolve best voices enabled true, contact flow logs enabled
    true, contact lens enabled true, directory id example, early
    media enabled true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auto_resolve_best_voices_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="contact_flow_logs_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="contact_lens_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="directory_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="early_media_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_management_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="inbound_calls_enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="instance_alias" type="string">
  Optional.
</ResponseField>

<ResponseField name="multi_party_conference_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="outbound_calls_enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_role" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
