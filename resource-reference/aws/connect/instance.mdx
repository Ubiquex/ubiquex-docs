---
title: "aws_connect_instance"
description: "`aws_connect_instance` provisions the real, top-level container for a contact center, telephony, chat, and every other Connect resource, agents..."
---

`aws_connect_instance` provisions the real, top-level container for a contact center, telephony, chat, and every other Connect resource, agents, queues, contact flows, attaches to it. As of this session, AWS now uses "Amazon Connect" as the name of a broader portfolio of agentic business-function solutions, and refers to the contact center product this resource belongs to as "Amazon Connect Customer" (or simply Customer) in its own documentation, the two names used interchangeably during the transition.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance"})

    		ubx.Resource(connect.Instance, "ci-runner", connect.InstanceConfig{
    			Attributes:             map[string]any{"inbound_calls": true},
    			IdentityManagementType: "example",
    			DirectoryId:            "example",
    			InstanceAlias:          "example",
    			Tags:                   []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "./local-sdk/aws/sdk/typescript/aws/connect/instance.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own instance" });

      resource(Instance, "ci-runner", {
        attributes: { inboundCalls: true },
        identityManagementType: "example",
        directoryId: "example",
        instanceAlias: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.connect import Instance, InstanceConfig

    def describe():
        ubx.intent("payments own instance")

        ubx.resource(Instance, "ci-runner", InstanceConfig(
            attributes={"inbound_calls": True},
            identity_management_type="example",
            directory_id="example",
            instance_alias="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_instance, with attributes a set value,
    identity management type example, directory id example, instance
    alias example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attributes" type="object" required>
  Defines feature configuration flags for the Amazon Connect instance as key-value pairs (e.g., enabling or disabling inbound/outbound calls, contact lens, or auto-resolve best voices). Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="auto_resolve_best_voices" type="bool">
      Boolean flag which enables AUTO_RESOLVE_BEST_VOICES on an instance. Optional.
    </ResponseField>
    <ResponseField name="contact_lens" type="bool">
      Boolean flag which enables CONTACT_LENS on an instance. Optional.
    </ResponseField>
    <ResponseField name="contactflow_logs" type="bool">
      Boolean flag which enables CONTACTFLOW_LOGS on an instance. Optional.
    </ResponseField>
    <ResponseField name="early_media" type="bool">
      Boolean flag which enables EARLY_MEDIA on an instance. Optional.
    </ResponseField>
    <ResponseField name="enhanced_chat_monitoring" type="bool">
      Boolean flag which enables ENHANCED_CHAT_MONITORING on an instance. Optional.
    </ResponseField>
    <ResponseField name="enhanced_contact_monitoring" type="bool">
      Boolean flag which enables ENHANCED_CONTACT_MONITORING on an instance. Optional.
    </ResponseField>
    <ResponseField name="high_volume_out_bound" type="bool">
      Boolean flag which enables HIGH_VOLUME_OUTBOUND on an instance. Optional.
    </ResponseField>
    <ResponseField name="inbound_calls" type="bool" required>
      Mandatory element which enables inbound calls on new instance. Required.
    </ResponseField>
    <ResponseField name="message_streaming" type="bool">
      Boolean flag which enables MESSAGE_STREAMING on an instance. Optional.
    </ResponseField>
    <ResponseField name="multi_party_chat_conference" type="bool">
      Boolean flag which enables MULTI_PARTY_CHAT_CONFERENCE on an instance. Optional.
    </ResponseField>
    <ResponseField name="multi_party_conference" type="bool">
      Boolean flag which enables MULTI_PARTY_CONFERENCE on an instance. Optional.
    </ResponseField>
    <ResponseField name="outbound_calls" type="bool" required>
      Mandatory element which enables outbound calls on new instance. Required.
    </ResponseField>
    <ResponseField name="use_custom_ttsvoices" type="bool">
      Boolean flag which enables USE_CUSTOM_TTS_VOICES on an instance. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="directory_id" type="string">
  Existing directoryId user wants to map to the new Connect instance. Optional.
</ResponseField>

<ResponseField name="identity_management_type" type="string" required>
  Specifies the type of directory integration for new instance. Required.
</ResponseField>

<ResponseField name="instance_alias" type="string">
  Alias of the new directory created as part of new instance creation. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag applied to the Amazon Connect instance, used to categorize and manage the resource in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  An instanceArn is automatically generated on creation based on instanceId. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Timestamp of instance creation logged as part of instance creation. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  An instanceId is automatically generated on creation and assigned as the unique identifier. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="instance_status" type="string">
  Specifies the creation status of new instance. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_role" type="string">
  Service linked role created as part of instance creation. Computed by AWS after `ship`.
</ResponseField>
