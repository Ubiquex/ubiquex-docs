---
title: "aws_connect_user_hierarchy_structure"
description: "Real, generated bindings for `aws_connect_user_hierarchy_structure`."
---

`aws_connect_user_hierarchy_structure` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user hierarchy structure"})

    		ubx.Resource(connect.UserHierarchyStructure, "ci-runner", connect.UserHierarchyStructureConfig{
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserHierarchyStructure } from "jsr:@ubx/sdk-aws/aws/connect/user_hierarchy_structure";

    export default stack("payments", () => {
      intent({ summary: "payments own user hierarchy structure" });

      resource(UserHierarchyStructure, "ci-runner", {
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import UserHierarchyStructure, UserHierarchyStructureConfig

    def describe():
        ubx.intent("payments own user hierarchy structure")

        ubx.resource(UserHierarchyStructure, "ci-runner", UserHierarchyStructureConfig(
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_user_hierarchy_structure, with instance id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
