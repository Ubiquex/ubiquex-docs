---
title: "aws_connect_queue"
description: "Real, generated bindings for `aws_connect_queue`."
---

`aws_connect_queue` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/connect"

    cfg := connect.QueueConfig{
        HoursOfOperationId: "example",
        InstanceId: "example",
        Name: "example-name",
        Description: "example",
        MaxContacts: 1,
    }
    ubx.Resource(connect.Queue, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Queue } from "jsr:@ubx/sdk-aws/aws/connect/queue";

    resource(Queue, "example", {
      hoursOfOperationId: "example",
      instanceId: "example",
      name: "example",
      description: "example",
      maxContacts: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.connect import Queue, QueueConfig

    cfg = QueueConfig(
        hours_of_operation_id="example",
        instance_id="example",
        name="example",
        description="example",
        max_contacts=1,
    )
    ubx.resource(Queue, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_connect_queue with hours_of_operation_id,
instance_id, name, description, max_contacts.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="hours_of_operation_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="max_contacts" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="quick_connect_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="queue_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
