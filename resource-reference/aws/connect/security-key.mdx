---
title: "aws_connect_security_key"
description: "`aws_connect_security_key` registers a real public key with an `aws_connect_instance`, used to validate the authenticity of requests..."
---

`aws_connect_security_key` registers a real public key with an `aws_connect_instance`, used to validate the authenticity of requests, such as those from an embedded contact control panel, that the instance receives.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own security key"})

    		ubx.Resource(connect.SecurityKey, "example", connect.SecurityKeyConfig{
    			InstanceId: "example",
    			Key:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityKey } from "./local-sdk/aws/sdk/typescript/aws/connect/security_key.ts";

    export default stack("example", () => {
      intent({ summary: "example own security key" });

      resource(SecurityKey, "example", {
        instanceId: "example",
        key: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.connect import SecurityKey, SecurityKeyConfig

    def describe():
        ubx.intent("example own security key")

        ubx.resource(SecurityKey, "example", SecurityKeyConfig(
            instance_id="example",
            key="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_security_key, with instance id example, key
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="instance_id" type="string" required>
  Amazon Connect instance identifier Required.
</ResponseField>

<ResponseField name="key" type="string" required>
  A valid security key in PEM format. Required.
</ResponseField>

## Output properties

<ResponseField name="association_id" type="string">
  An associationID is automatically generated when a storage config is associated with an instance Computed by AWS after `ship`.
</ResponseField>
