---
title: "aws_connect_instance_storage_config"
description: "Real, generated bindings for `aws_connect_instance_storage_config`."
---

`aws_connect_instance_storage_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance storage config"})

    		ubx.Resource(connect.InstanceStorageConfig, "ci-runner", connect.InstanceStorageConfigConfig{
    			InstanceId:   "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceStorageConfig } from "jsr:@ubx/sdk-aws/aws/connect/instance_storage_config";

    export default stack("payments", () => {
      intent({ summary: "payments own instance storage config" });

      resource(InstanceStorageConfig, "ci-runner", {
        instanceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import InstanceStorageConfig, InstanceStorageConfigConfig

    def describe():
        ubx.intent("payments own instance storage config")

        ubx.resource(InstanceStorageConfig, "ci-runner", InstanceStorageConfigConfig(
            instance_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_instance_storage_config, with instance id
    example, resource type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="association_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
