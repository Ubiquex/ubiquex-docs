---
title: "aws_connect_instance_storage_config"
description: "Real, generated bindings for `aws_connect_instance_storage_config`."
---

`aws_connect_instance_storage_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"

    cfg := connect.InstanceStorageConfigConfig{
        InstanceId: "example",
        ResourceType: "example",
    }
    ubx.Resource(connect.InstanceStorageConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstanceStorageConfig } from
      "jsr:@ubx/sdk-aws/aws/connect/instance_storage_config";

    resource(InstanceStorageConfig, "example", {
      instanceId: "example",
      resourceType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.connect import (
        InstanceStorageConfig,
        InstanceStorageConfigConfig,
    )

    cfg = InstanceStorageConfigConfig(
        instance_id="example",
        resource_type="example",
    )
    ubx.resource(InstanceStorageConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_connect_instance_storage_config with instance_id,
resource_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="association_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
