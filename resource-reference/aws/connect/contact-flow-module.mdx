---
title: "aws_connect_contact_flow_module"
description: "`aws_connect_contact_flow_module` is a real, reusable block of logic that can be embedded inside multiple `aws_connect_contact_flow`s..."
---

`aws_connect_contact_flow_module` is a real, reusable block of logic that can be embedded inside multiple `aws_connect_contact_flow`s, so shared routing logic, a business-hours check, for instance, is maintained once rather than duplicated across every flow that needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own contact flow module"})

    		ubx.Resource(connect.ContactFlowModule, "example", connect.ContactFlowModuleConfig{
    			Content:                         "example",
    			InstanceArn:                     "arn:aws:iam::123456789012:policy/example",
    			Name:                            "example",
    			Description:                     "Managed by ubx.",
    			ExternalInvocationConfiguration: map[string]any{"enabled": true},
    			Settings:                        "example",
    			State:                           "example",
    			Tags:                            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContactFlowModule } from "./local-sdk/aws/sdk/typescript/aws/connect/contact_flow_module.ts";

    export default stack("example", () => {
      intent({ summary: "example own contact flow module" });

      resource(ContactFlowModule, "example", {
        content: "example",
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        name: "example",
        description: "Managed by ubx.",
        externalInvocationConfiguration: { enabled: true },
        settings: "example",
        state: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.connect import ContactFlowModule, ContactFlowModuleConfig

    def describe():
        ubx.intent("example own contact flow module")

        ubx.resource(ContactFlowModule, "example", ContactFlowModuleConfig(
            content="example",
            instance_arn="arn:aws:iam::123456789012:policy/example",
            name="example",
            description="Managed by ubx.",
            external_invocation_configuration={"enabled": True},
            settings="example",
            state="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_contact_flow_module called "example", with
    content example, instance arn
    arn:aws:iam::123456789012:policy/example, description Managed by
    ubx., external invocation configuration a set value, settings
    example, state example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="content" type="string" required>
  The content of the contact flow module in JSON format. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the contact flow module. Optional.
</ResponseField>

<ResponseField name="external_invocation_configuration" type="object">
  Defines the external invocation configuration of the flow module resource Optional.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Specifies whether the flow module resource is enabled for external invocation Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The identifier of the Amazon Connect instance (ARN). Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the contact flow module. Required.
</ResponseField>

<ResponseField name="settings" type="string">
  The schema of the settings for contact flow module in JSON Schema V4 format. Optional.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the contact flow module. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  One or more tags. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to an Amazon Connect contact flow module for resource metadata and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag applied to the Amazon Connect contact flow module, used for organizing, managing, and identifying the resource within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="contact_flow_module_arn" type="string">
  The identifier of the contact flow module (ARN). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the contact flow module. Computed by AWS after `ship`.
</ResponseField>
