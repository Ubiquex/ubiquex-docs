---
title: "aws_connect_contact_flow_module"
description: "Real, generated bindings for `aws_connect_contact_flow_module`."
---

`aws_connect_contact_flow_module` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own contact flow module"})

    		ubx.Resource(connect.ContactFlowModule, "ci-runner", connect.ContactFlowModuleConfig{
    			InstanceId:  "example",
    			Name:        "ci-runner",
    			ContentHash: "example",
    			Description: "Managed by ubx.",
    			Filename:    "example",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContactFlowModule } from "jsr:@ubx/sdk-aws/aws/connect/contact_flow_module";

    export default stack("payments", () => {
      intent({ summary: "payments own contact flow module" });

      resource(ContactFlowModule, "ci-runner", {
        instanceId: "example",
        name: "ci-runner",
        contentHash: "example",
        description: "Managed by ubx.",
        filename: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import ContactFlowModule, ContactFlowModuleConfig

    def describe():
        ubx.intent("payments own contact flow module")

        ubx.resource(ContactFlowModule, "ci-runner", ContactFlowModuleConfig(
            instance_id="example",
            name="ci-runner",
            content_hash="example",
            description="Managed by ubx.",
            filename="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_contact_flow_module called "ci-runner", with
    instance id example, content hash example, description Managed by
    ubx., filename example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="content_hash" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="filename" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="contact_flow_module_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="content" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
