---
title: "aws_connect_integration_association"
description: "`aws_connect_integration_association` connects an `aws_connect_instance` to a real, external third-party application through Amazon AppIntegrations..."
---

`aws_connect_integration_association` connects an `aws_connect_instance` to a real, external third-party application through Amazon AppIntegrations, the mechanism behind third-party apps in the agent workspace and Amazon Q in Connect's own knowledge sources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own integration association"})

    		ubx.Resource(connect.IntegrationAssociation, "example", connect.IntegrationAssociationConfig{
    			InstanceId:      "example",
    			IntegrationArn:  "arn:aws:iam::123456789012:policy/example",
    			IntegrationType: "example",
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IntegrationAssociation } from "@ubx/sdk-aws/aws/connect/integration_association";

    export default stack("example", () => {
      intent({ summary: "example own integration association" });

      resource(IntegrationAssociation, "example", {
        instanceId: "example",
        integrationArn: "arn:aws:iam::123456789012:policy/example",
        integrationType: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import IntegrationAssociation, IntegrationAssociationConfig
    from ubx.aws.connect.integration_association import IntegrationAssociation_Tags

    def describe():
        ubx.intent("example own integration association")

        ubx.resource(IntegrationAssociation, "example", IntegrationAssociationConfig(
            instance_id="example",
            integration_arn="arn:aws:iam::123456789012:policy/example",
            integration_type="example",
            tags=[IntegrationAssociation_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_integration_association, with instance id
    example, integration arn
    arn:aws:iam::123456789012:policy/example, integration type
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="instance_id" type="string" required>
  Amazon Connect instance identifier Required.
</ResponseField>

<ResponseField name="integration_arn" type="string" required>
  ARN of Integration being associated with the instance Required.
</ResponseField>

<ResponseField name="integration_type" type="string" required>
  Specifies the integration type to be associated with the instance Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags used to organize, track, or control access for this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Connect integration association, used to categorize or identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="integration_association_id" type="string">
  Identifier of the association with Connect Instance Computed by AWS after `ship`.
</ResponseField>
