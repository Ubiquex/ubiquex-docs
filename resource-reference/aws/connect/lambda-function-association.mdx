---
title: "aws_connect_lambda_function_association"
description: "Real, generated bindings for `aws_connect_lambda_function_association`."
---

`aws_connect_lambda_function_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lambda function association"})

    		ubx.Resource(connect.LambdaFunctionAssociation, "ci-runner", connect.LambdaFunctionAssociationConfig{
    			FunctionArn: "arn:aws:iam::123456789012:policy/ci",
    			InstanceId:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LambdaFunctionAssociation } from "jsr:@ubx/sdk-aws/aws/connect/lambda_function_association";

    export default stack("payments", () => {
      intent({ summary: "payments own lambda function association" });

      resource(LambdaFunctionAssociation, "ci-runner", {
        functionArn: "arn:aws:iam::123456789012:policy/ci",
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import LambdaFunctionAssociation, LambdaFunctionAssociationConfig

    def describe():
        ubx.intent("payments own lambda function association")

        ubx.resource(LambdaFunctionAssociation, "ci-runner", LambdaFunctionAssociationConfig(
            function_arn="arn:aws:iam::123456789012:policy/ci",
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_lambda_function_association, with function arn
    arn:aws:iam::123456789012:policy/ci, instance id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="function_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
