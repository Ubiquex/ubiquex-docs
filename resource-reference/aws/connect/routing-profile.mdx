---
title: "aws_connect_routing_profile"
description: "Real, generated bindings for `aws_connect_routing_profile`."
---

`aws_connect_routing_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"

    cfg := connect.RoutingProfileConfig{
        DefaultOutboundQueueId: "example",
        Description: "example",
        InstanceId: "example",
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(connect.RoutingProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RoutingProfile } from
      "jsr:@ubx/sdk-aws/aws/connect/routing_profile";

    resource(RoutingProfile, "example", {
      defaultOutboundQueueId: "example",
      description: "example",
      instanceId: "example",
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.connect import (
        RoutingProfile,
        RoutingProfileConfig,
    )

    cfg = RoutingProfileConfig(
        default_outbound_queue_id="example",
        description="example",
        instance_id="example",
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(RoutingProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_connect_routing_profile with
default_outbound_queue_id, description, instance_id, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_outbound_queue_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="routing_profile_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
