---
title: "aws_connect_phone_number_contact_flow_association"
description: "Real, generated bindings for `aws_connect_phone_number_contact_flow_association`."
---

`aws_connect_phone_number_contact_flow_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own phone number contact flow association"})

    		ubx.Resource(connect.PhoneNumberContactFlowAssociation, "ci-runner", connect.PhoneNumberContactFlowAssociationConfig{
    			ContactFlowId: "example",
    			InstanceId:    "example",
    			PhoneNumberId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PhoneNumberContactFlowAssociation } from "jsr:@ubx/sdk-aws/aws/connect/phone_number_contact_flow_association";

    export default stack("payments", () => {
      intent({ summary: "payments own phone number contact flow association" });

      resource(PhoneNumberContactFlowAssociation, "ci-runner", {
        contactFlowId: "example",
        instanceId: "example",
        phoneNumberId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import PhoneNumberContactFlowAssociation, PhoneNumberContactFlowAssociationConfig

    def describe():
        ubx.intent("payments own phone number contact flow association")

        ubx.resource(PhoneNumberContactFlowAssociation, "ci-runner", PhoneNumberContactFlowAssociationConfig(
            contact_flow_id="example",
            instance_id="example",
            phone_number_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_phone_number_contact_flow_association, with
    contact flow id example, instance id example, phone number id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="contact_flow_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="phone_number_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
