---
title: "aws_connect_bot_association"
description: "Real, generated bindings for `aws_connect_bot_association`."
---

`aws_connect_bot_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/connect"

    cfg := connect.BotAssociationConfig{
        InstanceId: "example",
    }
    ubx.Resource(connect.BotAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BotAssociation } from
      "jsr:@ubx/sdk-aws/aws/connect/bot_association";

    resource(BotAssociation, "example", {
      instanceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.connect import (
        BotAssociation,
        BotAssociationConfig,
    )

    cfg = BotAssociationConfig(
        instance_id="example",
    )
    ubx.resource(BotAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_connect_bot_association with instance_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
