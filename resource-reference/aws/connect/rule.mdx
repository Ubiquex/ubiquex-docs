---
title: "aws_connect_rule"
description: "`aws_connect_rule` automates a real action, generating an EventBridge event, sending an email, creating a task, the moment a defined condition is met..."
---

`aws_connect_rule` automates a real action, generating an EventBridge event, sending an email, creating a task, the moment a defined condition is met, a metric threshold breach, a Contact Lens finding, an evaluation completion, rather than a supervisor watching dashboards and reacting manually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rule"})

    		ubx.Resource(connect.Rule, "example", connect.RuleConfig{
    			Actions:            map[string]any{"assign_contact_category_actions": []string{"example"}},
    			Function:           "example",
    			InstanceArn:        "arn:aws:iam::123456789012:policy/example",
    			Name:               "example",
    			PublishStatus:      "example",
    			TriggerEventSource: map[string]any{"event_source_name": "example-event-source-name"},
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Rule } from "./local-sdk/aws/sdk/typescript/aws/connect/rule.ts";

    export default stack("example", () => {
      intent({ summary: "example own rule" });

      resource(Rule, "example", {
        actions: { assignContactCategoryActions: ["example"] },
        function: "example",
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        name: "example",
        publishStatus: "example",
        triggerEventSource: { eventSourceName: "example-event-source-name" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.connect import Rule, RuleConfig

    def describe():
        ubx.intent("example own rule")

        ubx.resource(Rule, "example", RuleConfig(
            actions={"assign_contact_category_actions": ["example"]},
            function="example",
            instance_arn="arn:aws:iam::123456789012:policy/example",
            name="example",
            publish_status="example",
            trigger_event_source={"event_source_name": "example-event-source-name"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_rule called "example", with actions a set
    value, function example, instance arn
    arn:aws:iam::123456789012:policy/example, publish status example,
    trigger event source a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="object" required>
  A list of actions to be run when the rule is triggered. Required.
  <Expandable title="properties">
    <ResponseField name="assign_contact_category_actions" type="list(any)">
      This action will assign contact category when a rule is triggered. Optional.
    </ResponseField>
    <ResponseField name="assign_sla_actions" type="list(object)">
      This action will assign an SLA when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="case_sla_configuration" type="object">
          This object defines the Service Level Agreement (SLA) configuration that is assigned to a case when the Assign SLA action is executed in the Amazon Connect rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field_id" type="string">
              Specifies the unique identifier of the case field that holds the SLA target date/time value when executing the assign SLA action on a Connect case. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the Service Level Agreement (SLA) to assign to the case when this rule's assign SLA action is executed. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="target_field_values" type="list(object)">
              Specifies a list of case field values (field identifier and value pairs) that are applied to the case when the SLA assignment action is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="string_value" type="string">
                  Specifies the string value for a target field that is set on a case when the AssignSla rule action executes, as part of the case SLA configuration's target field values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="target_sla_minutes" type="number">
              The number of minutes that defines the target service level agreement (SLA) for the case when the assign-sla action is invoked. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              This field specifies the time unit for the case SLA's duration in the assigned SLA action, using values such as `HOURS` or `DAYS`. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sla_assignment_type" type="string">
          Determines whether the service level agreement (SLA) is assigned as a custom percentage or based on the queue's existing SLA configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="create_case_actions" type="list(object)">
      This action will create a case when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="fields" type="list(object)">
          Defines the case fields and their assigned values for the case created by this CreateCase action, using each field's identifier and value. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The unique identifier of the Amazon Connect Cases field that the create-case action sets to the specified value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="object">
              The value assigned to the case field (keyed by the sibling 'key' property) in an Amazon Connect Cases CreateCase action, which can be a static string or a dynamic contact attribute reference like &#123;&#123;Contact.Attributes.attributeName&#125;&#125;. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="boolean_value" type="bool">
                  Specifies the boolean value to assign to a case field in the CreateCase action of an Amazon Connect rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="double_value" type="number">
                  This field holds the numeric value of a case field when creating a case through the create case action of an Amazon Connect rule, used when the field's data type is a number (double). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="empty_value" type="any">
                  Set the empty_value block inside a create-case action field's value to signal that the new case's corresponding field should be created with a null/empty value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_value" type="string">
                  The string value to assign to a case field when a Create Case action runs in an Amazon Connect rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="template_id" type="string">
          The ID of the Amazon Connect Cases template that defines the structure of the case created by this rule action. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="end_associated_tasks_actions" type="list(any)">
      This action will end associated tasks when a rule is triggered. Optional.
    </ResponseField>
    <ResponseField name="event_bridge_actions" type="list(object)">
      This action will send event bridge notification when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The unique name for this EventBridge action within the Connect rule, used to identify the action when multiple actions are configured. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="send_notification_actions" type="list(object)">
      The action will send notification when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="content" type="string">
          The body text of the notification message sent when the rule triggers, such as the content of an email or chat message for the recipient. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="content_type" type="string">
          Specifies the MIME type of the notification content (for example, 'text/plain' or 'text/html'), determining how the message body is formatted for recipients of the notification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="delivery_method" type="string">
          Specifies the delivery channel for the notification, such as EMAIL or SMS, in a send-notification action of an AWS Connect rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="exclusion" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="user_arns" type="list(string)">
              List of Amazon Connect user ARNs to exclude from receiving the notification. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_tags" type="any">
              A list of key/value user tag pairs that define an exclusion filter; Amazon Connect excludes users with any of these tags from receiving the notification sent by this SEND_NOTIFICATION action. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="recipient" type="object">
          Specifies the recipient of the notification sent by this send notification action, allowing you to target users, queues, or a contact in the Amazon Connect contact flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="user_arns" type="list(string)">
              The ARNs of Amazon Connect users who should receive the notification when this rule triggers. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_tags" type="any">
              Specifies the user tags used to select recipients for the SendNotification action, such that only users matching all provided key-value tag pairs receive the notification. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subject" type="string">
          The subject line for the email notification sent by this action, used when the delivery method is EMAIL. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="submit_auto_evaluation_actions" type="list(object)">
      This action will submit an auto contact evaluation when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="evaluation_form_arn" type="string">
          The ARN of the Amazon Connect evaluation form that is automatically submitted for the contact when this rule action triggers. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="task_actions" type="list(object)">
      This action will generate a task when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="contact_flow_arn" type="string">
          The Amazon Resource Name (ARN) of the Amazon Connect contact flow that the task action invokes to create and initiate the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          The description of the task to create when this rule is triggered, providing context for the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the task that Amazon Connect creates when this rule's task action is invoked. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="references" type="any">
          The references field defines a map of key-value pairs attached to the outbound task action created by the rule, supplying task-specific contextual data (e.g., customer identifiers or case numbers) that the contact flow and agents can utilize. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="update_case_actions" type="list(object)">
      This action will update a case when a rule is triggered. Optional.
      <Expandable title="properties">
        <ResponseField name="fields" type="list(object)">
          Specifies the list of case fields, each with a field ID and value, to update when the rule action triggers, allowing dynamic updates to Amazon Connect Cases data. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="string">
              The unique identifier of the Amazon Connect Cases field that the update case action sets to the corresponding value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="object">
              Specifies the typed value (e.g., string, number, boolean, or empty) to assign to the case field when the update-case action executes, matching the field's data type in the associated Amazon Connect Cases field. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="boolean_value" type="bool">
                  When an Amazon Connect rule updates a case, this boolean supplies the true/false value to set for the selected case field in the update-case action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="double_value" type="number">
                  Specifies the double-precision numeric value to assign to a case field in an update-case action within an Amazon Connect rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="empty_value" type="any">
                  Set to true to clear the case field's value in the update case action, effectively setting it to an empty/null state. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="string_value" type="string">
                  When an Amazon Connect rule executes an update-case action, this field specifies the literal string to assign to the target case field within the fields.value object. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="function" type="string" required>
  The conditions of the rule. Required.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The Amazon Resource Name (ARN) of the instance. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the rule. Required.
</ResponseField>

<ResponseField name="publish_status" type="string" required>
  The publish status of the rule. *Allowed values*: ``DRAFT`` | ``PUBLISHED`` Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags used to organize, track, or control access for this resource. For example, &#123; "tags": &#123;"key1":"value1", "key2":"value2"&#125; &#125;. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag that can be attached to an Amazon Connect rule to organize and manage the rule within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a key-value tag applied to the Amazon Connect rule, which you can use to organize, identify, and manage the rule alongside other AWS resources. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trigger_event_source" type="object" required>
  The name of the event source. Required.
  <Expandable title="properties">
    <ResponseField name="event_source_name" type="string" required>
      The name of the event source. Required.
    </ResponseField>
    <ResponseField name="integration_association_arn" type="string">
      The Amazon Resource Name (ARN) of the integration association. ``IntegrationAssociationArn`` is required if ``TriggerEventSource`` is one of the following values: ``OnZendeskTicketCreate`` | ``OnZendeskTicketStatusUpdate`` | ``OnSalesforceCaseCreate`` Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="rule_arn" type="string">
  The Amazon Resource Name (ARN) of the Amazon Connect rule, uniquely identifying it within your AWS account. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
