---
title: "aws_connect_approved_origin"
description: "`aws_connect_approved_origin` allow-lists a real domain that is permitted to embed an `aws_connect_instance`'s contact control panel through the Amazon..."
---

`aws_connect_approved_origin` allow-lists a real domain that is permitted to embed an `aws_connect_instance`'s contact control panel through the Amazon Connect Streams API, the mechanism that lets a custom CRM or web app host Connect's own softphone UI.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own approved origin"})

    		ubx.Resource(connect.ApprovedOrigin, "example", connect.ApprovedOriginConfig{
    			InstanceId: "example",
    			Origin:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApprovedOrigin } from "@ubx/sdk-aws/aws/connect/approved_origin";

    export default stack("example", () => {
      intent({ summary: "example own approved origin" });

      resource(ApprovedOrigin, "example", {
        instanceId: "example",
        origin: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import ApprovedOrigin, ApprovedOriginConfig

    def describe():
        ubx.intent("example own approved origin")

        ubx.resource(ApprovedOrigin, "example", ApprovedOriginConfig(
            instance_id="example",
            origin="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_approved_origin, with instance id example,
    origin example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="instance_id" type="string" required>
  Amazon Connect instance identifier Required.
</ResponseField>

<ResponseField name="origin" type="string" required>
  Domain name to be added to the allowlist of instance Required.
</ResponseField>
