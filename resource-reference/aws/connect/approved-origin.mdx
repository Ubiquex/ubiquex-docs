---
title: "aws_connect_approved_origin"
description: "`aws_connect_approved_origin` allow-lists a real domain that is permitted to embed an `aws_connect_instance`'s contact control panel through the Amazon..."
---

`aws_connect_approved_origin` allow-lists a real domain that is permitted to embed an `aws_connect_instance`'s contact control panel through the Amazon Connect Streams API, the mechanism that lets a custom CRM or web app host Connect's own softphone UI.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own approved origin"})

    		ubx.Resource(connect.ApprovedOrigin, "ci-runner", connect.ApprovedOriginConfig{
    			InstanceId: "example",
    			Origin:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApprovedOrigin } from "./local-sdk/aws/sdk/typescript/aws/connect/approved_origin.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own approved origin" });

      resource(ApprovedOrigin, "ci-runner", {
        instanceId: "example",
        origin: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.connect import ApprovedOrigin, ApprovedOriginConfig

    def describe():
        ubx.intent("payments own approved origin")

        ubx.resource(ApprovedOrigin, "ci-runner", ApprovedOriginConfig(
            instance_id="example",
            origin="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_approved_origin, with instance id example,
    origin example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="instance_id" type="string" required>
  Amazon Connect instance identifier Required.
</ResponseField>

<ResponseField name="origin" type="string" required>
  Domain name to be added to the allowlist of instance Required.
</ResponseField>
