---
title: "aws_connect_data_lake_association"
description: "`aws_connect_data_lake_association` associates a real AWS Glue Data Catalog database with an `aws_connect_instance`..."
---

`aws_connect_data_lake_association` associates a real AWS Glue Data Catalog database with an `aws_connect_instance`, making that instance's own contact center data queryable through Connect's own data lake integration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data lake association"})

    		ubx.Resource(connect.DataLakeAssociation, "example", connect.DataLakeAssociationConfig{
    			DataSetId:       "example",
    			InstanceId:      "example",
    			TargetAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataLakeAssociation } from "@ubx/sdk-aws/aws/connect/data_lake_association";

    export default stack("example", () => {
      intent({ summary: "example own data lake association" });

      resource(DataLakeAssociation, "example", {
        dataSetId: "example",
        instanceId: "example",
        targetAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect.data_lake_association import DataLakeAssociation, DataLakeAssociationConfig

    def describe():
        ubx.intent("example own data lake association")

        ubx.resource(DataLakeAssociation, "example", DataLakeAssociationConfig(
            data_set_id="example",
            instance_id="example",
            target_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_data_lake_association, with data set id
    example, instance id example, target account id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_set_id" type="string" required>
  The identifier of the analytics data set. Required.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  The identifier of the Amazon Connect instance Required.
</ResponseField>

<ResponseField name="target_account_id" type="string">
  The identifier of the target account Optional.
</ResponseField>

## Output properties

<ResponseField name="resource_share_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS Resource Access Manager share Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_share_id" type="string">
  The AWS Resource Access Manager share ID Computed by AWS after `ship`.
</ResponseField>
