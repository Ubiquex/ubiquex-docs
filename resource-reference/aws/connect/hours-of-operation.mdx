---
title: "aws_connect_hours_of_operation"
description: "`aws_connect_hours_of_operation` defines a real, named schedule, open days and hours..."
---

`aws_connect_hours_of_operation` defines a real, named schedule, open days and hours, that an `aws_connect_contact_flow` checks to route a contact differently outside business hours instead of every flow hardcoding its own time logic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own hours of operation"})

    		ubx.Resource(connect.HoursOfOperation, "example", connect.HoursOfOperationConfig{
    			Config:                    []map[string]any{{"day": "example"}},
    			InstanceArn:               "arn:aws:iam::123456789012:policy/example",
    			Name:                      "example",
    			TimeZone:                  "example",
    			ChildHoursOfOperations:    []map[string]any{{"name": "example-name"}},
    			Description:               "Managed by ubx.",
    			HoursOfOperationOverrides: []map[string]any{{"effective_from": "example"}},
    			ParentHoursOfOperations:   []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { HoursOfOperation } from "./local-sdk/aws/sdk/typescript/aws/connect/hours_of_operation.ts";

    export default stack("example", () => {
      intent({ summary: "example own hours of operation" });

      resource(HoursOfOperation, "example", {
        config: [{ day: "example" }],
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        name: "example",
        timeZone: "example",
        childHoursOfOperations: [{ name: "example-name" }],
        description: "Managed by ubx.",
        hoursOfOperationOverrides: [{ effectiveFrom: "example" }],
        parentHoursOfOperations: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.connect import HoursOfOperation, HoursOfOperationConfig

    def describe():
        ubx.intent("example own hours of operation")

        ubx.resource(HoursOfOperation, "example", HoursOfOperationConfig(
            config=[{"day": "example"}],
            instance_arn="arn:aws:iam::123456789012:policy/example",
            name="example",
            time_zone="example",
            child_hours_of_operations=[{"name": "example-name"}],
            description="Managed by ubx.",
            hours_of_operation_overrides=[{"effective_from": "example"}],
            parent_hours_of_operations=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_hours_of_operation called "example", with
    config a set value, instance arn
    arn:aws:iam::123456789012:policy/example, time zone example,
    child hours of operations a set value, description Managed by
    ubx., hours of operation overrides a set value, parent hours of
    operations a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="child_hours_of_operations" type="list(object)">
  List of child hours of operations. Optional.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="config" type="list(object)" required>
  Configuration information for the hours of operation: day, start time, and end time. Required.
  <Expandable title="properties">
    <ResponseField name="day" type="string">
      Specifies the day of the week (e.g., MONDAY, TUESDAY) that this hours of operation configuration applies to in the AWS Connect HoursOfOperation resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="end_time" type="object">
      The time at which the hours of operation end for a given day, specified as an object with hours and minutes in a 24-hour format. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="hours" type="number">
          Specifies the hour (0-23) in 24-hour format at which the operating hours end for the configured day. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="minutes" type="number">
          The minutes component of the time of day when the hours of operation configuration ends, used together with the end_time hour to define the daily closing time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="start_time" type="object">
      The start_time field specifies the time in HH:MM format (24-hour) when the hours of operation begin for the configured day. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="hours" type="number">
          The hour of the day (0-23) at which the hours of operation configuration starts. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="minutes" type="number">
          The minute component (0-59) of the start time for a given day's hours of operation configuration in Amazon Connect. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the hours of operation. Optional.
</ResponseField>

<ResponseField name="hours_of_operation_overrides" type="list(object)">
  One or more hours of operation overrides assigned to an hour of operation. Optional.
  <Expandable title="properties">
    <ResponseField name="effective_from" type="string">
      Specifies the starting timestamp (in ISO 8601 format) from which this hours of operation override takes effect. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="effective_till" type="string">
      Specifies the end date and time (in ISO 8601 format) for which this hours of operation override remains in effect, after which the standard hours of operation schedule applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hours_of_operation_override_id" type="string">
      This field provides a unique, user-defined identifier for a specific hours of operation override entry within the hours_of_operation_overrides list, allowing each override to be individually referenced and managed. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="override_config" type="list(object)">
      The override config defines the alternative schedule of days and times that overrides the default hours of operation during a specified date/time window. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="day" type="string">
          Specifies the day of the week (for example, MONDAY) to which this hours-of-operation override applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="end_time" type="object">
          Specifies the time of day, in 24-hour HH:MM format, when the override hours of operation end for the corresponding day in this override configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="hours" type="number">
              Specifies the hour of the day (0-23) in the override's end time, determining when the custom hours of operation override ends. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="minutes" type="number">
              The minute component (0-59) of the override's end time, specifying the minute at which this hours of operation override stops applying. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="start_time" type="object">
          In an Amazon Connect hours of operation override configuration, the start_time field defines the beginning of an active time interval, specified in 24-hour HH:MM format (e.g., '09:00'). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="hours" type="number">
              The hour of the day (0-23) when the override start time begins, used in AWS Connect hours of operation overrides. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="minutes" type="number">
              The minute component (0-59) of the time of day when this hours of operation override starts. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="override_description" type="string">
      Provides a human-readable description for an hours of operation override, enabling administrators to label special schedules such as holidays or one-off events. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="override_name" type="string">
      The display name assigned to a specific date override (e.g., a holiday) within an Amazon Connect hours of operation configuration, used to identify the custom schedule for that date. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="override_type" type="string">
      Indicates the type of override, either HOLIDAY or OVERRIDE, determining how the specific-date hours override the weekly configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="recurrence_config" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="recurrence_pattern" type="object">
          Configures the recurrence pattern (e.g., daily, weekly, or monthly interval and specific days) for applying the hours-of-operation override across a repeating schedule defined in the recurrence configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="by_month" type="list(number)">
              Specifies the months (1-12) during which this hours-of-operation override recurrence pattern is active, used in combination with other recurrence fields to define a monthly scheduling rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="by_month_day" type="list(number)">
              Defines the days of the month (1-31) on which the hours-of-operation override recurs, used when the recurrence pattern is set to a monthly interval. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="by_weekday_occurrence" type="list(number)">
              Specifies the occurrence(s) of the weekdays within a month (e.g., 1 for first, 2 for second, -1 for last) on which this hours of operation override recurrence applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="frequency" type="string">
              The frequency unit for the recurrence pattern of this override (e.g., daily, weekly, or monthly), defining how often the override schedule repeats. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interval" type="number">
              The number of time units (e.g., weeks or months, based on the recurrence pattern's frequency) between each occurrence of the recurring hours of operation override, such as 2 for every other week. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The identifier of the Amazon Connect instance. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the hours of operation. Required.
</ResponseField>

<ResponseField name="parent_hours_of_operations" type="list(object)">
  List of parent hours of operations. Optional.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  One or more tags. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key part of a tag for the Amazon Connect hours of operation, used to organize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag key-value pair attached to the Amazon Connect hours of operation resource, used for resource categorization and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="time_zone" type="string" required>
  The time zone of the hours of operation. Required.
</ResponseField>

## Output properties

<ResponseField name="hours_of_operation_arn" type="string">
  The Amazon Resource Name (ARN) for the hours of operation. Computed by AWS after `ship`.
</ResponseField>
