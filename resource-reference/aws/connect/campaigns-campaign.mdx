---
title: "aws_connect_campaigns_campaign"
description: "`aws_connect_campaigns_campaign` defines a real, high-volume outbound campaign, appointment reminders, telemarketing, debt collection..."
---

`aws_connect_campaigns_campaign` defines a real, high-volume outbound campaign, appointment reminders, telemarketing, debt collection, run through an `aws_connect_instance`. Outbound Campaigns V1 isn't supported in every Region; `aws_connect_campaigns_v2_campaign` is the newer, multi-channel version AWS points new campaigns toward.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	connect "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/connect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own campaigns campaign"})

    		ubx.Resource(connect.CampaignsCampaign, "example", connect.CampaignsCampaignConfig{
    			ConnectInstanceArn: "arn:aws:iam::123456789012:policy/example",
    			DialerConfig:       map[string]any{"agentless_dialer_config": map[string]any{"dialing_capacity": 1}},
    			Name:               "example",
    			OutboundCallConfig: map[string]any{"connect_contact_flow_arn": "example"},
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CampaignsCampaign } from "@ubx/sdk-aws/aws/connect/campaigns_campaign";

    export default stack("example", () => {
      intent({ summary: "example own campaigns campaign" });

      resource(CampaignsCampaign, "example", {
        connectInstanceArn: "arn:aws:iam::123456789012:policy/example",
        dialerConfig: { agentlessDialerConfig: { dialingCapacity: 1 } },
        name: "example",
        outboundCallConfig: { connectContactFlowArn: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.connect import CampaignsCampaign, CampaignsCampaignConfig

    def describe():
        ubx.intent("example own campaigns campaign")

        ubx.resource(CampaignsCampaign, "example", CampaignsCampaignConfig(
            connect_instance_arn="arn:aws:iam::123456789012:policy/example",
            dialer_config={"agentless_dialer_config": {"dialing_capacity": 1}},
            name="example",
            outbound_call_config={"connect_contact_flow_arn": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_connect_campaigns_campaign called "example", with
    connect instance arn arn:aws:iam::123456789012:policy/example,
    dialer config a set value, outbound call config a set value, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connect_instance_arn" type="string" required>
  Amazon Connect Instance Arn Required.
</ResponseField>

<ResponseField name="dialer_config" type="object" required>
  The possible types of dialer config parameters Required.
  <Expandable title="properties">
    <ResponseField name="agentless_dialer_config" type="object">
      Agentless Dialer config Optional.
      <Expandable title="properties">
        <ResponseField name="dialing_capacity" type="number">
          Allocates dialing capacity for this campaign between multiple active campaigns. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="predictive_dialer_config" type="object">
      Predictive Dialer config Optional.
      <Expandable title="properties">
        <ResponseField name="bandwidth_allocation" type="number" required>
          The bandwidth allocation of a queue resource. Required.
        </ResponseField>
        <ResponseField name="dialing_capacity" type="number">
          Allocates dialing capacity for this campaign between multiple active campaigns. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="progressive_dialer_config" type="object">
      Progressive Dialer config Optional.
      <Expandable title="properties">
        <ResponseField name="bandwidth_allocation" type="number" required>
          The bandwidth allocation of a queue resource. Required.
        </ResponseField>
        <ResponseField name="dialing_capacity" type="number">
          Allocates dialing capacity for this campaign between multiple active campaigns. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  Amazon Connect Campaign Name Required.
</ResponseField>

<ResponseField name="outbound_call_config" type="object" required>
  The configuration used for outbound calls. Required.
  <Expandable title="properties">
    <ResponseField name="answer_machine_detection_config" type="object">
      The configuration used for answering machine detection during outbound calls Optional.
      <Expandable title="properties">
        <ResponseField name="await_answer_machine_prompt" type="bool">
          Enables detection of prompts (e.g., beep after after a voicemail greeting) Optional.
        </ResponseField>
        <ResponseField name="enable_answer_machine_detection" type="bool" required>
          Flag to decided whether outbound calls should have answering machine detection enabled or not Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connect_contact_flow_arn" type="string" required>
      The identifier of the contact flow for the outbound call. Required.
    </ResponseField>
    <ResponseField name="connect_queue_arn" type="string">
      The queue for the call. If you specify a queue, the phone displayed for caller ID is the phone number specified in the queue. If you do not specify a queue, the queue defined in the contact flow is used. If you do not specify a queue, you must specify a source phone number. Optional.
    </ResponseField>
    <ResponseField name="connect_source_phone_number" type="string">
      The phone number associated with the Amazon Connect instance, in E.164 format. If you do not specify a source phone number, you must specify a queue. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  One or more tags. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Amazon Connect Campaigns campaign, used to categorize the resource for cost allocation, ownership, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The string value paired with each tag key on the Amazon Connect campaigns campaign, used for identifying, organizing, and managing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Amazon Connect Campaign Arn Computed by AWS after `ship`.
</ResponseField>
