---
title: "aws_shield_protection_health_check_association"
description: "Real, generated bindings for `aws_shield_protection_health_check_association`."
---

`aws_shield_protection_health_check_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own protection health check association"})

    		ubx.Resource(shield.ProtectionHealthCheckAssociation, "ci-runner", shield.ProtectionHealthCheckAssociationConfig{
    			HealthCheckArn:     "arn:aws:iam::123456789012:policy/ci",
    			ShieldProtectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProtectionHealthCheckAssociation } from "jsr:@ubx/sdk-aws/aws/shield/protection_health_check_association";

    export default stack("payments", () => {
      intent({ summary: "payments own protection health check association" });

      resource(ProtectionHealthCheckAssociation, "ci-runner", {
        healthCheckArn: "arn:aws:iam::123456789012:policy/ci",
        shieldProtectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.shield import ProtectionHealthCheckAssociation, ProtectionHealthCheckAssociationConfig

    def describe():
        ubx.intent("payments own protection health check association")

        ubx.resource(ProtectionHealthCheckAssociation, "ci-runner", ProtectionHealthCheckAssociationConfig(
            health_check_arn="arn:aws:iam::123456789012:policy/ci",
            shield_protection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_shield_protection_health_check_association, with
    health check arn arn:aws:iam::123456789012:policy/ci, shield
    protection id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="health_check_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shield_protection_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
