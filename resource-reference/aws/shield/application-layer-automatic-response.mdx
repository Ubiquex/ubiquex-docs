---
title: "aws_shield_application_layer_automatic_response"
description: "Real, generated bindings for `aws_shield_application_layer_automatic_response`."
---

`aws_shield_application_layer_automatic_response` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/shield"

    cfg := shield.ApplicationLayerAutomaticResponseConfig{
        Action: "example",
        ResourceArn: "example",
    }
    ubx.Resource(
        shield.ApplicationLayerAutomaticResponse,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ApplicationLayerAutomaticResponse } from
      "jsr:@ubx/sdk-aws/aws/shield/application_layer_automatic_response";

    resource(ApplicationLayerAutomaticResponse, "example", {
      action: "example",
      resourceArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.shield import (
        ApplicationLayerAutomaticResponse,
        ApplicationLayerAutomaticResponseConfig,
    )

    cfg = ApplicationLayerAutomaticResponseConfig(
        action="example",
        resource_arn="example",
    )
    ubx.resource(ApplicationLayerAutomaticResponse, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_shield_application_layer_automatic_response with
action, resource_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
