---
title: "aws_shield_application_layer_automatic_response"
description: "Real, generated bindings for `aws_shield_application_layer_automatic_response`."
---

`aws_shield_application_layer_automatic_response` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application layer automatic response"})

    		ubx.Resource(shield.ApplicationLayerAutomaticResponse, "ci-runner", shield.ApplicationLayerAutomaticResponseConfig{
    			Action:      "example",
    			ResourceArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationLayerAutomaticResponse } from "jsr:@ubx/sdk-aws/aws/shield/application_layer_automatic_response";

    export default stack("payments", () => {
      intent({ summary: "payments own application layer automatic response" });

      resource(ApplicationLayerAutomaticResponse, "ci-runner", {
        action: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.shield import ApplicationLayerAutomaticResponse, ApplicationLayerAutomaticResponseConfig

    def describe():
        ubx.intent("payments own application layer automatic response")

        ubx.resource(ApplicationLayerAutomaticResponse, "ci-runner", ApplicationLayerAutomaticResponseConfig(
            action="example",
            resource_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_shield_application_layer_automatic_response, with
    action example, resource arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
