---
title: "aws_shield_protection_group"
description: "Real, generated bindings for `aws_shield_protection_group`."
---

`aws_shield_protection_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own protection group"})

    		ubx.Resource(shield.ProtectionGroup, "ci-runner", shield.ProtectionGroupConfig{
    			Aggregation:       "example",
    			Pattern:           "example",
    			ProtectionGroupId: "example",
    			Members:           []string{"example"},
    			ResourceType:      "example",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProtectionGroup } from "jsr:@ubx/sdk-aws/aws/shield/protection_group";

    export default stack("payments", () => {
      intent({ summary: "payments own protection group" });

      resource(ProtectionGroup, "ci-runner", {
        aggregation: "example",
        pattern: "example",
        protectionGroupId: "example",
        members: ["example"],
        resourceType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.shield import ProtectionGroup, ProtectionGroupConfig

    def describe():
        ubx.intent("payments own protection group")

        ubx.resource(ProtectionGroup, "ci-runner", ProtectionGroupConfig(
            aggregation="example",
            pattern="example",
            protection_group_id="example",
            members=["example"],
            resource_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_shield_protection_group, with aggregation example,
    pattern example, protection group id example, members a set
    value, resource type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aggregation" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="members" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="pattern" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protection_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protection_group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
