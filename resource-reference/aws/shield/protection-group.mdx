---
title: "aws_shield_protection_group"
description: "Real, generated bindings for `aws_shield_protection_group`."
---

`aws_shield_protection_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/shield"

    cfg := shield.ProtectionGroupConfig{
        Aggregation: "example",
        Pattern: "example",
        ProtectionGroupId: "example",
        Members: []string{"example"},
        ResourceType: "example",
    }
    ubx.Resource(shield.ProtectionGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProtectionGroup } from
      "jsr:@ubx/sdk-aws/aws/shield/protection_group";

    resource(ProtectionGroup, "example", {
      aggregation: "example",
      pattern: "example",
      protectionGroupId: "example",
      members: ["example"],
      resourceType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.shield import (
        ProtectionGroup,
        ProtectionGroupConfig,
    )

    cfg = ProtectionGroupConfig(
        aggregation="example",
        pattern="example",
        protection_group_id="example",
        members=["example"],
        resource_type="example",
    )
    ubx.resource(ProtectionGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_shield_protection_group with aggregation, pattern,
protection_group_id, members, resource_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aggregation" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="members" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="pattern" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protection_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protection_group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
