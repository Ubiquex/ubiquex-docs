---
title: "aws_shield_protection_group"
description: "`aws_shield_protection_group` groups real `aws_shield_protection`-covered resources together so Shield Advanced evaluates and reports on their combined..."
---

`aws_shield_protection_group` groups real `aws_shield_protection`-covered resources together so Shield Advanced evaluates and reports on their combined traffic pattern as one unit, useful when resources share load or are meant to be assessed together rather than individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own protection group"})

    		ubx.Resource(shield.ProtectionGroup, "example", shield.ProtectionGroupConfig{
    			Aggregation:       "example",
    			Pattern:           "example",
    			ProtectionGroupId: "example",
    			Members:           []string{"example"},
    			ResourceType:      "example",
    			Tags:              []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProtectionGroup } from "@ubx/sdk-aws/aws/shield/protection_group";

    export default stack("example", () => {
      intent({ summary: "example own protection group" });

      resource(ProtectionGroup, "example", {
        aggregation: "example",
        pattern: "example",
        protectionGroupId: "example",
        members: ["example"],
        resourceType: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.shield.protection_group import ProtectionGroup, ProtectionGroupConfig

    def describe():
        ubx.intent("example own protection group")

        ubx.resource(ProtectionGroup, "example", ProtectionGroupConfig(
            aggregation="example",
            pattern="example",
            protection_group_id="example",
            members=["example"],
            resource_type="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_shield_protection_group, with aggregation example,
    pattern example, protection group id example, members a set
    value, resource type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aggregation" type="string" required>
  Defines how AWS Shield combines resource data for the group in order to detect, mitigate, and report events. * Sum - Use the total traffic across the group. This is a good choice for most cases. Examples include Elastic IP addresses for EC2 instances that scale manually or automatically. * Mean - Use the average of the traffic across the group. This is a good choice for resources that share traffic uniformly. Examples include accelerators and load balancers. * Max - Use the highest traffic from each resource. This is useful for resources that don't share traffic and for resources that share that traffic in a non-uniform way. Examples include Amazon CloudFront and origin resources for CloudFront distributions. Required.
</ResponseField>

<ResponseField name="members" type="list(string)">
  The Amazon Resource Names (ARNs) of the resources to include in the protection group. You must set this when you set `Pattern` to `ARBITRARY` and you must not set it for any other `Pattern` setting. Optional.
</ResponseField>

<ResponseField name="pattern" type="string" required>
  The criteria to use to choose the protected resources for inclusion in the group. You can include all resources that have protections, provide a list of resource Amazon Resource Names (ARNs), or include all resources of a specified resource type. Required.
</ResponseField>

<ResponseField name="protection_group_id" type="string" required>
  The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it. Required.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The resource type to include in the protection group. All protected resources of this type are included in the protection group. Newly protected resources of this type are automatically added to the group. You must set this when you set `Pattern` to `BY_RESOURCE_TYPE` and you must not set it for any other `Pattern` setting. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  One or more tag key-value pairs for the Protection object. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag attached to the AWS Shield Protection Group, used to store custom metadata that helps identify, organize, and manage the protection group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="protection_group_arn" type="string">
  The ARN (Amazon Resource Name) of the protection group. Computed by AWS after `ship`.
</ResponseField>
