---
title: "aws_shield_proactive_engagement"
description: "Real, generated bindings for `aws_shield_proactive_engagement`."
---

`aws_shield_proactive_engagement` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own proactive engagement"})

    		ubx.Resource(shield.ProactiveEngagement, "ci-runner", shield.ProactiveEngagementConfig{
    			Enabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProactiveEngagement } from "jsr:@ubx/sdk-aws/aws/shield/proactive_engagement";

    export default stack("payments", () => {
      intent({ summary: "payments own proactive engagement" });

      resource(ProactiveEngagement, "ci-runner", {
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.shield import ProactiveEngagement, ProactiveEngagementConfig

    def describe():
        ubx.intent("payments own proactive engagement")

        ubx.resource(ProactiveEngagement, "ci-runner", ProactiveEngagementConfig(
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_shield_proactive_engagement, with enabled true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
