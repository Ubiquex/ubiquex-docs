---
title: "aws_shield_proactive_engagement"
description: "Real, generated bindings for `aws_shield_proactive_engagement`."
---

`aws_shield_proactive_engagement` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"

    cfg := shield.ProactiveEngagementConfig{
        Enabled: true,
    }
    ubx.Resource(shield.ProactiveEngagement, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProactiveEngagement } from
      "jsr:@ubx/sdk-aws/aws/shield/proactive_engagement";

    resource(ProactiveEngagement, "example", {
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.shield import (
        ProactiveEngagement,
        ProactiveEngagementConfig,
    )

    cfg = ProactiveEngagementConfig(
        enabled=True,
    )
    ubx.resource(ProactiveEngagement, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_shield_proactive_engagement with enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
