---
title: "aws_shield_subscription"
description: "Real, generated bindings for `aws_shield_subscription`."
---

`aws_shield_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"

    cfg := shield.SubscriptionConfig{
        AutoRenew: "example",
        SkipDestroy: true,
    }
    ubx.Resource(shield.Subscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Subscription } from
      "jsr:@ubx/sdk-aws/aws/shield/subscription";

    resource(Subscription, "example", {
      autoRenew: "example",
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.shield import Subscription, SubscriptionConfig

    cfg = SubscriptionConfig(
        auto_renew="example",
        skip_destroy=True,
    )
    ubx.resource(Subscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_shield_subscription with auto_renew, skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auto_renew" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="auto_renew" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
