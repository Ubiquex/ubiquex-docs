---
title: "aws_shield_protection"
description: "Real, generated bindings for `aws_shield_protection`."
---

`aws_shield_protection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/shield"

    cfg := shield.ProtectionConfig{
        Name: "example-name",
        ResourceArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(shield.Protection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Protection } from
      "jsr:@ubx/sdk-aws/aws/shield/protection";

    resource(Protection, "example", {
      name: "example",
      resourceArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.shield import Protection, ProtectionConfig

    cfg = ProtectionConfig(
        name="example",
        resource_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(Protection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_shield_protection with name, resource_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
