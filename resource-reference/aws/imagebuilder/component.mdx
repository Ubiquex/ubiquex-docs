---
title: "aws_imagebuilder_component"
description: "Real, generated bindings for `aws_imagebuilder_component`."
---

`aws_imagebuilder_component` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own component"})

    		ubx.Resource(imagebuilder.Component, "ci-runner", imagebuilder.ComponentConfig{
    			Name:                "ci-runner",
    			Platform:            "example",
    			Version:             "example",
    			ChangeDescription:   "example",
    			Description:         "Managed by ubx.",
    			KmsKeyId:            "example",
    			SkipDestroy:         true,
    			SupportedOsVersions: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Component } from "jsr:@ubx/sdk-aws/aws/imagebuilder/component";

    export default stack("payments", () => {
      intent({ summary: "payments own component" });

      resource(Component, "ci-runner", {
        name: "ci-runner",
        platform: "example",
        version: "example",
        changeDescription: "example",
        description: "Managed by ubx.",
        kmsKeyId: "example",
        skipDestroy: true,
        supportedOsVersions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.imagebuilder import Component, ComponentConfig

    def describe():
        ubx.intent("payments own component")

        ubx.resource(Component, "ci-runner", ComponentConfig(
            name="ci-runner",
            platform="example",
            version="example",
            change_description="example",
            description="Managed by ubx.",
            kms_key_id="example",
            skip_destroy=True,
            supported_os_versions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_imagebuilder_component called "ci-runner", with
    platform example, version example, change description example,
    description Managed by ubx., kms key id example, skip destroy
    true, supported os versions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="change_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="data" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="platform" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="supported_os_versions" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="uri" type="string">
  Optional.
</ResponseField>

<ResponseField name="version" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="date_created" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encrypted" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
