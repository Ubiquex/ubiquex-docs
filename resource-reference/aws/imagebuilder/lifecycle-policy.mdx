---
title: "aws_imagebuilder_lifecycle_policy"
description: "Real, generated bindings for `aws_imagebuilder_lifecycle_policy`."
---

`aws_imagebuilder_lifecycle_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lifecycle policy"})

    		ubx.Resource(imagebuilder.LifecyclePolicy, "ci-runner", imagebuilder.LifecyclePolicyConfig{
    			ExecutionRole: "example",
    			Name:          "ci-runner",
    			ResourceType:  "example",
    			Description:   "Managed by ubx.",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LifecyclePolicy } from "jsr:@ubx/sdk-aws/aws/imagebuilder/lifecycle_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own lifecycle policy" });

      resource(LifecyclePolicy, "ci-runner", {
        executionRole: "example",
        name: "ci-runner",
        resourceType: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.imagebuilder import LifecyclePolicy, LifecyclePolicyConfig

    def describe():
        ubx.intent("payments own lifecycle policy")

        ubx.resource(LifecyclePolicy, "ci-runner", LifecyclePolicyConfig(
            execution_role="example",
            name="ci-runner",
            resource_type="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_imagebuilder_lifecycle_policy called "ci-runner", with
    execution role example, resource type example, description
    Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
