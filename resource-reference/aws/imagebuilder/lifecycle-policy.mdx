---
title: "aws_image_builder_lifecycle_policy"
description: "`aws_image_builder_lifecycle_policy` automatically deprecates or deletes an `aws_image_builder_image_pipeline`'s older real output images on a defined..."
---

`aws_image_builder_lifecycle_policy` automatically deprecates or deletes an `aws_image_builder_image_pipeline`'s older real output images on a defined schedule, real cleanup that keeps old, unpatched AMIs from accumulating indefinitely.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	imagebuilder "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/imagebuilder"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own lifecycle policy"})

    		ubx.Resource(imagebuilder.LifecyclePolicy, "example", imagebuilder.LifecyclePolicyConfig{
    			ExecutionRole:     "example",
    			Name:              "example",
    			PolicyDetails:     []map[string]any{{"action": map[string]any{"include_resources": map[string]any{"amis": true}}}},
    			ResourceSelection: map[string]any{"recipes": []map[string]any{{"name": "example-name"}}},
    			ResourceType:      "example",
    			Description:       "Managed by ubx.",
    			Status:            "example",
    			Tags:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LifecyclePolicy } from "@ubx/sdk-aws/aws/imagebuilder/lifecycle_policy";

    export default stack("example", () => {
      intent({ summary: "example own lifecycle policy" });

      resource(LifecyclePolicy, "example", {
        executionRole: "example",
        name: "example",
        policyDetails: [{ action: { includeResources: { amis: true } } }],
        resourceSelection: { recipes: [{ name: "example-name" }] },
        resourceType: "example",
        description: "Managed by ubx.",
        status: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.imagebuilder import LifecyclePolicy, LifecyclePolicyConfig
    from ubx.aws.imagebuilder.lifecycle_policy import LifecyclePolicy_PolicyDetails, LifecyclePolicy_PolicyDetails_Action, LifecyclePolicy_PolicyDetails_Action_IncludeResources, LifecyclePolicy_ResourceSelection, LifecyclePolicy_ResourceSelection_Recipes

    def describe():
        ubx.intent("example own lifecycle policy")

        ubx.resource(LifecyclePolicy, "example", LifecyclePolicyConfig(
            execution_role="example",
            name="example",
            policy_details=[LifecyclePolicy_PolicyDetails(action=LifecyclePolicy_PolicyDetails_Action(include_resources=LifecyclePolicy_PolicyDetails_Action_IncludeResources(amis=True)))],
            resource_selection=LifecyclePolicy_ResourceSelection(recipes=[LifecyclePolicy_ResourceSelection_Recipes(name="example-name")]),
            resource_type="example",
            description="Managed by ubx.",
            status="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_image_builder_lifecycle_policy called "example", with
    execution role example, policy details a set value, resource
    selection a set value, resource type example, description Managed
    by ubx., status example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the lifecycle policy. Optional.
</ResponseField>

<ResponseField name="execution_role" type="string" required>
  The execution role of the lifecycle policy. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the lifecycle policy. Required.
</ResponseField>

<ResponseField name="policy_details" type="list(object)" required>
  The policy details of the lifecycle policy. Required.
  <Expandable title="properties">
    <ResponseField name="action" type="object">
      Defines the lifecycle action (such as deleting the resource or creating a snapshot) that Image Builder performs on the resources selected by the policy's rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include_resources" type="object">
          Specifies the resource types (e.g., AMIs, container images, and snapshots) that the lifecycle policy action applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="amis" type="bool">
              Determines whether the lifecycle policy action applies to Amazon Machine Images (AMIs) in the resource set. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="containers" type="bool">
              Specifies whether container images are included as a resource type for this lifecycle policy action. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="snapshots" type="bool">
              Specifies whether snapshots are included as a resource type that the lifecycle policy action (such as deletion) applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the lifecycle action to apply to matching images when the policy conditions are met, with allowed values of 'DELETE' to remove the image or 'DEPRECATE' to mark it as deprecated. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="exclusion_rules" type="object">
      Defines exclusion criteria for the lifecycle policy, allowing resources to be omitted from policy actions based on attributes such as whether they are shared or public, the AWS Regions they reside in, or matching tags. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="amis" type="object">
          Defines AMI-specific exclusion rules that identify AMIs to exempt from lifecycle actions based on shared AWS accounts and tag conditions. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_public" type="bool">
              When set to true, the lifecycle policy excludes AMIs that are public (publicly shared) from the lifecycle action, preventing them from being expired or deregistered. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="last_launched" type="object">
              Defines an exclusion criterion based on how recently an AMI was last used to launch an EC2 instance, where a value and unit (e.g., DAYS) set a window such that AMIs launched within that recent period are excluded from the lifecycle policy's actions. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="unit" type="string">
                  Specifies the time unit (DAYS, WEEKS, MONTHS, or YEARS) used with the numeric value to define the last-launched age threshold for excluding an AMI from the lifecycle policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The numeric threshold (e.g., number of days) since an AMI was last launched, used by the last_launched exclusion rule to determine which AMIs are excluded from the lifecycle policy action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="regions" type="list(string)">
              Specifies a list of AWS Regions in which an AMI must also be present to be excluded from the lifecycle policy's delete or deprecate actions; if the AMI exists in any of the listed regions, it will not be affected by the policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shared_accounts" type="list(string)">
              The list of AWS account IDs that an AMI must be shared with for the AMI to be excluded from the lifecycle policy's action (for example, deregistration). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tag_map" type="any">
              Specifies a map of tag keys and values used to identify AMIs that should be excluded from the lifecycle policy's actions, meaning any image with exactly matching tags is not affected by the defined lifecycle rules. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tag_map" type="any">
          Defines a map of tag keys and values used to identify matching Image Builder resources that should be excluded from the lifecycle policy's actions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="filter" type="object">
      Defines the selection criteria (such as age, count, tag values, or retain-at-least threshold) that determine which resources the lifecycle policy actions apply to. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="retain_at_least" type="number">
          Sets the minimum number of resources (e.g., images) that must be retained, ensuring that at least this many resources are kept even when a count-based lifecycle filter would otherwise expire more. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Indicates the filter type used to select resources for the lifecycle policy action, such as `age`, `tag`, or `schedule`. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="unit" type="string">
          The unit of time (such as days or months) used with the filter's value to define the retention period for the lifecycle policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="number">
          Specifies the numeric threshold (e.g., the number of days for an AGE filter, or the number of resources for a COUNT filter) at which the lifecycle policy triggers its configured action. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_selection" type="object" required>
  The resource selection for the lifecycle policy. Required.
  <Expandable title="properties">
    <ResponseField name="recipes" type="list(object)">
      The recipes to select. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Specifies the name of an Image Builder recipe (image or container recipe) that the lifecycle policy's resource selection targets, so the policy applies to images built from that recipe. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="semantic_version" type="string">
          The semantic version of the Image Builder recipe to match in the lifecycle policy's resource selection, identifying the specific recipe version (e.g., 1.0.0) that the policy applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tag_map" type="any">
      The Image Builder resources to select by tag. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The resource type of the lifecycle policy. Required.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the lifecycle policy. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  The tags associated with the lifecycle policy. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the lifecycle policy. Computed by AWS after `ship`.
</ResponseField>
