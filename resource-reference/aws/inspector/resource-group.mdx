---
title: "aws_inspector_resource_group"
description: "Real, generated bindings for `aws_inspector_resource_group`."
---

`aws_inspector_resource_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/inspector"

    cfg := inspector.ResourceGroupConfig{
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(inspector.ResourceGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceGroup } from
      "jsr:@ubx/sdk-aws/aws/inspector/resource_group";

    resource(ResourceGroup, "example", {
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.inspector import (
        ResourceGroup,
        ResourceGroupConfig,
    )

    cfg = ResourceGroupConfig(
        tags={"key": "value"},
    )
    ubx.resource(ResourceGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_inspector_resource_group with tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
