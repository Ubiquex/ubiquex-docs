---
title: "aws_inspector_v2_filter"
description: "`aws_inspector_v2_filter` suppresses, or routes to a specific notification, real Inspector v2 findings matching defined criteria, severity..."
---

`aws_inspector_v2_filter` suppresses, or routes to a specific notification, real Inspector v2 findings matching defined criteria, severity, resource type, package name, cutting down alert noise from findings a caller has already triaged or determined aren't actionable.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v2 filter"})

    		ubx.Resource(inspector.V2Filter, "ci-runner", inspector.V2FilterConfig{
    			FilterAction:   "example",
    			FilterCriteria: map[string]any{"aws_account_id": []map[string]any{{"comparison": "example"}}},
    			Name:           "ci-runner",
    			Description:    "Managed by ubx.",
    			Tags:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2Filter } from "./local-sdk/aws/sdk/typescript/aws/inspector/v2_filter.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own v2 filter" });

      resource(V2Filter, "ci-runner", {
        filterAction: "example",
        filterCriteria: { awsAccountId: [{ comparison: "example" }] },
        name: "ci-runner",
        description: "Managed by ubx.",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.inspector import V2Filter, V2FilterConfig

    def describe():
        ubx.intent("payments own v2 filter")

        ubx.resource(V2Filter, "ci-runner", V2FilterConfig(
            filter_action="example",
            filter_criteria={"aws_account_id": [{"comparison": "example"}]},
            name="ci-runner",
            description="Managed by ubx.",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_inspector_v2_filter called "ci-runner", with filter
    action example, filter criteria a set value, description Managed
    by ubx., tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Findings filter description. Optional.
</ResponseField>

<ResponseField name="filter_action" type="string" required>
  The action taken on findings that match the filter criteria, either 'NONE' to do nothing or 'SUPPRESS' to suppress the findings. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="filter_criteria" type="object" required>
  The filter criteria object, which specifies the conditions (e.g., on finding fields like severity, resource tags, or EC2 instance properties) that determine which Inspector findings match the filter. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="aws_account_id" type="list(object)">
      This filter criterion matches findings based on the AWS account ID that owns the resource, using a list of string filter objects that compare the account ID against specified values (e.g., EQUALS). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS or NOT_EQUALS) used to evaluate the AWS account ID against the filter's value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The 12-digit AWS account ID that the filter criterion compares against, using the specified comparison operator, to include or exclude findings originating from that account. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="code_vulnerability_detector_name" type="list(object)">
      Filter criterion that matches findings based on the name of the security detector (for example, Amazon CodeGuru Security) that identified the code vulnerability. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the code vulnerability detector name against the filter value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The detector name string that this Inspector V2 filter condition compares against the code vulnerability detector name when evaluating findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="code_vulnerability_detector_tags" type="list(object)">
      This filter criterion matches Inspector V2 findings based on the detector tags of a code vulnerability (such as CWE tags), using the specified string comparison operators (for example, EQUALS or NOT_EQUALS) and values. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the operator (such as EQUALS, NOT_EQUALS, or PREFIX) that determines how the code vulnerability detector tag value is compared to the filter's expected value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The tag value to match against the code vulnerability detector's tags when filtering Inspector V2 findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="code_vulnerability_file_path" type="list(object)">
      Filters Inspector v2 findings based on the file path where a code vulnerability was detected, allowing multiple string comparison conditions (e.g., equals, contains) to include or exclude matching findings. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS or NOT_EQUALS) used to compare the code vulnerability file path against the value in this filter criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The file path of the code vulnerability (e.g., a source file path) used as the value for the filter criterion, matched according to the associated comparison operator. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="component_id" type="list(object)">
      The filter_criteria.component_id field is a list of string filter conditions (comparison and value) that match Inspector v2 findings based on the identifier of the vulnerable software component (e.g., package name or library) associated with the finding. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator (e.g., EQUALS, NOT_EQUALS) used to match the filter against the component ID value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the component ID value to match when filtering findings, used to identify findings associated with a particular software component. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="component_type" type="list(object)">
      In an AWS Inspector v2 filter, this list of string filters selects findings based on the type of vulnerable component (such as an operating system package or an application dependency) using the configured comparator and value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the component type of findings in the Inspector V2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          This value specifies the type of component to match when filtering Amazon Inspector findings, such as 'OS' (operating system) or 'PACKAGE' (software package). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ec2_instance_image_id" type="list(object)">
      Filters findings by the Amazon Machine Image (AMI) ID of the associated EC2 instance, using a list of comparison objects with operators and values. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator (such as EQUALS or NOT_EQUALS) used to match the EC2 instance image ID against the filter value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the Amazon Machine Image (AMI) ID of an EC2 instance that findings must match when this ec2_instance_image_id filter criterion is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ec2_instance_subnet_id" type="list(object)">
      Defines a list of string comparison filters on the EC2 instance's subnet ID, used to include or exclude Inspector V2 findings from the filter based on the subnet where the instance resides. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator used to match the EC2 instance's subnet ID against the filter value, such as EQUALS or NOT_EQUALS. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The subnet ID (e.g., subnet-12345678) that an EC2 instance must be associated with to match the Inspector v2 filter criterion, compared against the instance's subnet ID. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ec2_instance_vpc_id" type="list(object)">
      Filters findings by the VPC ID of the EC2 instance, where each object in the list defines a comparison operator and a VPC ID value that must match for the finding to be included. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (EQUALS, PREFIX, NOT_EQUALS, or NOT_PREFIX) used to match findings against the EC2 instance VPC ID value in this Inspector v2 filter criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the VPC ID that an EC2 instance must be associated with for the AWS Inspector v2 filter to match it. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ecr_image_architecture" type="list(object)">
      Filters findings by the CPU architecture of the ECR container image (e.g., amd64 or arm64) using a comparison operator and value to include or exclude matches. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS or NOT_EQUALS) used when matching the ECR image architecture of a finding against the configured filter value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the CPU architecture value (such as X86_64 or ARM64) of the ECR image to match when filtering findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ecr_image_hash" type="list(object)">
      Defines a list of filter conditions that match findings based on the SHA256 digest (hash) of the associated Amazon ECR container image, where each condition includes an image hash value and a comparison operator (such as EQUALS or NOT_EQUALS) to evaluate against the finding's image hash. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (such as EQUALS) used to match the ECR image hash against the specified value in the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the exact ECR image hash (SHA256 digest) value that an image must have for the filter to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ecr_image_pushed_at" type="list(object)">
      Filters findings to include only ECR images whose push date falls within one of the specified date ranges, with each object in the list containing a start and end timestamp. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_inclusive" type="number">
          Sets the inclusive upper bound timestamp for an ECR image push date range, so only images pushed at or before this time match the filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_inclusive" type="number">
          The inclusive start timestamp (in Unix epoch seconds) for filtering findings by when the associated ECR image was pushed. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ecr_image_registry" type="list(object)">
      Filters Inspector2 findings based on the ECR image registry (typically the AWS account ID of the registry) using the specified comparison and value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS) used to match the ECR image registry against the configured value for the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Sets the ECR image registry (such as an AWS account ID or registry hostname) that the filter criteria matches against when filtering Inspector v2 findings by ECR image source. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ecr_image_repository_name" type="list(object)">
      When creating or updating an AWS Inspector V2 filter, this field defines the ECR image repository name condition as a list of criterion objects (each with a comparison operator and value) to include or exclude findings based on the source ECR repository name. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the operator used to compare the ECR image repository name field against the specified value, such as EQUALS or NOT_EQUALS. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the exact ECR image repository name value that the Inspector v2 filter compares against when evaluating findings based on the ecrImageRepositoryName criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ecr_image_tags" type="list(object)">
      Filters findings by matching the provided comparison (e.g., EQUALS) and string against the Amazon ECR image tag associated with the container image. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator used to evaluate the ECR image tag against the filter's value, such as EQUALS or PREFIX. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the ECR image tag value to match when filtering findings, combined with the comparator to include or exclude images with that tag. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="epss_score" type="list(object)">
      Specifies a list of numeric ranges used to filter findings by their Exploit Prediction Scoring System (EPSS) score, matching findings whose EPSS value falls within at least one defined lower and upper boundary. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lower_inclusive" type="number">
          The lower inclusive bound of the EPSS score range used to match findings against the filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="upper_inclusive" type="number">
          Defines the inclusive upper bound of the EPSS score range used to filter Inspector v2 findings, retaining only those with an EPSS score at or below this value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="exploit_available" type="list(object)">
      Specifies the exploit availability condition for the filter, using a list of string filter objects with comparison and value to match whether a known exploit exists (e.g., YES or NO) for a finding. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match findings based on whether an exploit is available for a vulnerability. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value to match against the exploit availability status of an Inspector v2 finding, used to filter for findings where a public exploit is or is not available (e.g., 'YES' or 'NO'). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="finding_arn" type="list(object)">
      Filters findings by their Amazon Resource Name (ARN), using string filter conditions to include or exclude findings with specific ARNs. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The operator (e.g., EQUALS or NOT_EQUALS) used to compare the finding ARN against the specified value(s) to determine if a finding matches the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the Amazon Resource Name (ARN) of a finding that this Inspector v2 filter criterion matches to include or exclude findings with that exact ARN. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="finding_status" type="list(object)">
      Filters findings based on their current status (for example, ACTIVE, CLOSED, or SUPPRESSED) using the specified comparison operator and status value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the finding status value in this filter criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The finding status value (e.g., ACTIVE, SUPPRESSED, CLOSED) that the filter criteria matches against for this Inspector v2 filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="finding_type" type="list(object)">
      Specifies the finding type values (such as PACKAGE_VULNERABILITY or CODE_VULNERABILITY) to match when filtering findings, using a comparison operator like EQUALS or NOT_EQUALS. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS) used to match the finding type against the values you provide in the filter criteria for AWS Inspector v2. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          This value specifies the Inspector finding type that the filter matches, with allowed values including 'PACKAGE_VULNERABILITY' and 'NETWORK_REACHABILITY'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="first_observed_at" type="list(object)">
      Filters findings by the date and time they were first observed, defined as a list of DateFilter objects each specifying an inclusive start and end timestamp. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_inclusive" type="number">
          The inclusive end timestamp (in Unix epoch seconds) that limits the filter to findings first observed at or before this time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_inclusive" type="number">
          The inclusive start timestamp (Unix epoch seconds) of the window used to match findings whose first observed time falls on or after this value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fix_available" type="list(object)">
      A list of BooleanFilter conditions that match findings based on whether a fix is available for the vulnerability, using a comparison operator (e.g., EQUALS) against a boolean value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Sets the comparison operator (such as EQUALS or NOT_EQUALS) for the fix_available filter condition, controlling how a finding's fix availability status is matched against the specified value (e.g., TRUE or FALSE). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value to match for the fix_available criterion, such as 'YES' or 'NO', to filter findings based on whether a fix is available for the vulnerability. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="inspector_score" type="list(object)">
      This list of number filter objects defines the criteria for matching findings by their Amazon Inspector risk score, allowing numeric comparisons like EQUALS or GREATER_THAN against a specified score. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lower_inclusive" type="number">
          Sets the lower inclusive threshold (0-100) for the Inspector score that a finding must meet to match the filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="upper_inclusive" type="number">
          The inclusive upper bound for the Inspector score range, so resources with an Inspector score less than or equal to this value are included in the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda_function_execution_role_arn" type="list(object)">
      Defines a list of StringFilter objects that match Inspector findings by the execution role ARN of the associated Lambda function, using comparison operators like EQUALS or PREFIX to include or exclude findings based on that ARN. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator used to match the Lambda function's execution role ARN against the specified value(s) in the Inspector v2 filter criteria, such as EQUALS, NOT_EQUALS, PREFIX, or NOT_PREFIX. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the ARN of the Lambda function's execution role, so that only findings associated with Lambda functions using this role match the filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda_function_last_modified_at" type="list(object)">
      Specifies a list of date-range conditions that match findings for Lambda functions whose last modified timestamp falls within the given start and end times. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_inclusive" type="number">
          The inclusive upper bound, as a Unix timestamp in seconds, for filtering AWS Lambda functions by their last modification time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_inclusive" type="number">
          Defines the inclusive lower bound (as an epoch timestamp) of the range used by the Inspector v2 filter to match findings based on when the associated Lambda function was last modified. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda_function_layers" type="list(object)">
      Specifies a list of string filter conditions that match findings based on the layers associated with the affected Lambda function. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS) used to match a Lambda function layer against the filter value, determining whether the resource meets this filter condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The string value that specifies the ARN of an AWS Lambda layer, used to match findings from Lambda functions that use that layer. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda_function_name" type="list(object)">
      Specifies a list of string filter conditions that match findings based on the name of the associated Lambda function in an Amazon Inspector filter criteria. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS or NOT_EQUALS) used to match the Lambda function name against the filter value in this Inspector v2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value used with the comparison operator to match a Lambda function name when filtering Amazon Inspector v2 findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda_function_runtime" type="list(object)">
      Filters findings based on the runtime identifier of the associated Lambda function (e.g., 'nodejs14.x', 'python3.9') using a comparison operator (e.g., EQUALS, NOT_EQUALS) and a value specified in each list item. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS or NOT_EQUALS) used to match the Lambda function runtime against the filter's value(s) in the Inspector v2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The Lambda runtime identifier (such as python3.9 or nodejs18.x) that the AWS Inspector v2 filter compares against the runtime of a Lambda function to determine inclusion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_observed_at" type="list(object)">
      Within the filter_criteria block of an AWS Inspector v2 filter, last_observed_at is a list of objects that lets you specify a start and end timestamp to match findings by when the finding was last observed. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_inclusive" type="number">
          Specifies the inclusive end of the date range (as a Unix timestamp) for when a finding was last observed, used to filter Inspector v2 findings by their last observed time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_inclusive" type="number">
          Filters findings to include only those last observed at or after this inclusive epoch timestamp (in Unix seconds) within the last_observed_at date/time range. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_protocol" type="list(object)">
      Filters findings based on the network protocol (e.g., TCP, UDP) involved in network reachability findings, using comparison operators such as EQUALS to match the specified protocol values. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (such as EQUALS) used to match the network protocol of a finding against the filter's configured value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The network protocol (such as TCP, UDP, or ICMP) to match when filtering findings, used in the Inspector v2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port_range" type="list(object)">
      Matches findings based on the open network port range (begin_port to end_port) observed on the resource. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="begin_inclusive" type="number">
          The inclusive lower bound of the port range used to match network connections in the Inspector v2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="end_inclusive" type="number">
          The inclusive upper bound of the port range used to match findings where the network port associated with the finding falls within the specified range. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="related_vulnerabilities" type="list(object)">
      Filters findings by the identifiers of related vulnerabilities, using string comparison operators to include or exclude findings whose related Vulnerabilities list matches the specified value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          The comparison operator used to match related vulnerability IDs in the Inspector v2 filter criteria, such as EQUALS or NOT_EQUALS. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the vulnerability identifier (such as a CVE) to match when filtering Inspector v2 findings by related vulnerability. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_id" type="list(object)">
      The `resource_id` filter criterion matches findings whose affected resource identifier (such as an EC2 instance ID or ECR repository ARN) equals the specified value according to the chosen comparison operator. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the resource ID against the filter's expected value in an Amazon Inspector v2 filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value to match for the resource ID condition in the Inspector filter criteria, used to filter findings based on the resource's identifier. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_tags" type="list(object)">
      Defines the resource tag conditions used to filter Inspector findings, specifying tag keys, values, and comparison operators that resources must match to be included or excluded. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Defines the comparison operator (e.g., EQUALS or NOT_EQUALS) used when matching a resource's tag value against the specified key in the resource_tags filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string">
          The key of the resource tag that the Inspector V2 filter uses to match against resource tags when determining which resources are included in or excluded from the filter's findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The tag value to match for the resource tag filter, used to include or exclude resources based on their tags. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_type" type="list(object)">
      Defines one or more string comparison conditions that filter findings by the resource type (such as EC2, ECR, or Lambda) associated with the finding. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the resource type against the value in this filter criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The resource type value to match in an Inspector V2 filter, such as AWS_EC2_INSTANCE or AWS_ECR_CONTAINER_IMAGE, used with the StringFilter comparison when filtering findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="severity" type="list(object)">
      Specifies the severity levels (e.g., Critical, High, Medium, Low, Informational) to include or exclude in the Inspector v2 findings filter, using comparison and value objects. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS) used to match the severity of findings against the filter's severity value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The severity value to match in the filter criterion, provided as an uppercase string such as 'HIGH' or 'CRITICAL'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="title" type="list(object)">
      The title criterion in the filter criteria, which matches findings by their title using a comparison operator and one or more string values. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the string comparison operator (e.g., EQUALS, NOT_EQUALS, PREFIX, NOT_PREFIX) used to match the finding title against the configured value in the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value to match against the title of an Inspector V2 finding when the filter's title criterion is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updated_at" type="list(object)">
      The updated_at filter criteria item specifies a date range (with start and end times) used to match findings based on when their findings record was last updated. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="end_inclusive" type="number">
          The inclusive end (as a Unix epoch timestamp) of the time range used to filter Inspector v2 findings by their last updated time. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_inclusive" type="number">
          Specifies the inclusive start of the Unix epoch timestamp range used to filter resources by their last update time, so only resources updated at or after this time match. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vendor_severity" type="list(object)">
      Filters findings by the severity level assigned by the vendor, using a list of string comparison filters to include or exclude findings with specific vendor severity values. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the vendor-severity value against the filter criterion. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          This value specifies a vendor-reported severity level (such as CRITICAL, HIGH, or MEDIUM) that the Inspector v2 filter matches against findings to include or exclude them. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vulnerability_id" type="list(object)">
      Specifies the vulnerability identifier (such as a CVE ID) to include or exclude in the filter results, using comparison operators and a value for matching. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS) used to match a vulnerability's CVE identifier against the filter value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The vulnerability ID (for example, a CVE identifier) that the filter matches against findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vulnerability_source" type="list(object)">
      The vulnerabilitySource criteria filters Inspector v2 findings to those whose vulnerability data originates from a specified source (such as NVD) using one or more string comparison filters. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="comparison" type="string">
          Specifies the comparison operator (e.g., EQUALS, NOT_EQUALS) used to match the vulnerability source in the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The vulnerability source identifier, such as 'NVD' or 'CNVD', that the Inspector v2 filter will match against when evaluating findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vulnerable_packages" type="list(object)">
      Filters findings based on the properties of the vulnerable package, such as its name, version, architecture, epoch, release, or source layer hash, using a list of package filter objects. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="architecture" type="object">
          Filters findings by the CPU architecture of the vulnerable package, using a comparison operator and value such as 'x86_64' or 'arm64'. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              Defines the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the vulnerable package's architecture against the specified value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              Specifies the exact CPU architecture string (e.g., 'x86_64','arm64') used to match vulnerable package findings in the Inspector v2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="epoch" type="object">
          Specifies the numeric filter condition (comparison and value) for the epoch component of a vulnerable package's version, used to match AWS Inspector V2 findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="lower_inclusive" type="number">
              The lower inclusive bound for the epoch value of a vulnerable package version to match in an Inspector v2 filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="upper_inclusive" type="number">
              The upper inclusive bound for the package epoch value used to filter Inspector findings on vulnerable packages. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="file_path" type="object">
          Specifies the file-path matching condition for filtering findings by vulnerable package location, using a string comparison operator (e.g., EQUALS) and the path value to include or exclude. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              The comparison operator (e.g., EQUALS, NOT_EQUALS, PREFIX) used to match the vulnerable package's file path against the specified value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The exact file path string used as the comparison value when filtering findings based on the vulnerable package's file path. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="object">
          Specifies a filter condition on the name of the vulnerable software package in an Amazon Inspector finding, allowing you to match findings where the package name equals or does not equal a specified string value using a comparison operator. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              The comparison operator that determines how the vulnerable package name is matched (e.g., EQUALS, NOT_EQUALS) in the AWS Inspector v2 filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              This field specifies the exact or pattern-matching value for the vulnerable package name (e.g., 'openssl') used in the Inspector v2 filter criteria to include or exclude findings based on the package name. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="release" type="object">
          Specifies the release string (such as the distro-specific package release suffix) of the vulnerable package to match when defining the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              When building an Amazon Inspector V2 filter, this field sets the string comparison operator (such as EQUALS, PREFIX, or NOT_EQUALS) used to match the vulnerable package's release value against the value you specify, thereby controlling which findings are included or excluded. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              Specifies the release version of a vulnerable package that findings must match for the filter to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_lambda_layer_arn" type="object">
          The source_lambda_layer_arn criterion filters findings to include only those vulnerable packages that originate from a specified AWS Lambda layer, matched by its ARN. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              Specifies the comparison operator used to match the source Lambda layer ARN of a vulnerable package when filtering Inspector v2 findings (e.g., EQUALS or NOT_EQUALS). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              Specifies the Amazon Resource Name (ARN) of the Lambda layer that the vulnerable package originates from, used to filter Inspector v2 findings to only those associated with that layer. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="source_layer_hash" type="object">
          Specifies the source layer hash of a vulnerable container image layer to match when filtering Inspector findings, part of the vulnerable packages filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              Specifies the comparison operator (e.g., EQUALS or NOT_EQUALS) used to match the source layer hash of a vulnerable package when filtering Inspector v2 findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The source_layer_hash.value field sets the string value for the source layer hash of a vulnerable package, used to match findings against the specific container image layer SHA that contains the vulnerable package. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="version" type="object">
          Configures the version filter for vulnerable packages, using a comparison operator (such as EQUALS) and a value to match the package version in Inspector v2 findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison" type="string">
              Defines the comparison operator (e.g., EQUALS, NOT_EQUALS, GREATER_THAN) used to match the vulnerable package version against a specified value in the filter criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              The value of the version condition for the vulnerable package filter, specifying the exact package version (e.g., '1.2.3') to match in Inspector V2 findings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  Findings filter name. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  Tags assigned to the Inspector V2 filter, used for organizing and identifying the filter resource in AWS cost allocation and management. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Findings filter ARN. Computed by AWS after `ship`.
</ResponseField>
