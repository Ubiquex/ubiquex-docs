---
title: "aws_inspector_assessment_template"
description: "`aws_inspector_assessment_template` defined the real rules packages and duration for a security assessment run against..."
---

`aws_inspector_assessment_template` defined the real rules packages and duration for a security assessment run against an `aws_inspector_assessment_target` in Amazon Inspector Classic. Reaches full end of support May 20, 2026, alongside the rest of Inspector Classic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own assessment template"})

    		ubx.Resource(inspector.AssessmentTemplate, "example", inspector.AssessmentTemplateConfig{
    			AssessmentTargetArn:       "arn:aws:iam::123456789012:policy/example",
    			DurationInSeconds:         7200,
    			RulesPackageArns:          []string{"example"},
    			AssessmentTemplateName:    "example-assessment-template-name",
    			UserAttributesForFindings: []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AssessmentTemplate } from "@ubx/sdk-aws/aws/inspector/assessment_template";

    export default stack("example", () => {
      intent({ summary: "example own assessment template" });

      resource(AssessmentTemplate, "example", {
        assessmentTargetArn: "arn:aws:iam::123456789012:policy/example",
        durationInSeconds: 7200,
        rulesPackageArns: ["example"],
        assessmentTemplateName: "example-assessment-template-name",
        userAttributesForFindings: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.inspector import AssessmentTemplate, AssessmentTemplateConfig
    from ubx.aws.inspector.assessment_template import AssessmentTemplate_UserAttributesForFindings

    def describe():
        ubx.intent("example own assessment template")

        ubx.resource(AssessmentTemplate, "example", AssessmentTemplateConfig(
            assessment_target_arn="arn:aws:iam::123456789012:policy/example",
            duration_in_seconds=7200,
            rules_package_arns=["example"],
            assessment_template_name="example-assessment-template-name",
            user_attributes_for_findings=[AssessmentTemplate_UserAttributesForFindings(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_inspector_assessment_template, with assessment target
    arn arn:aws:iam::123456789012:policy/example, duration in seconds
    7200, rules package arns a set value, assessment template name
    example-assessment-template-name, user attributes for findings a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="assessment_target_arn" type="string" required>
  The ARN of the AWS Inspector assessment target that this template will run assessments against. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="assessment_template_name" type="string">
  A user-defined name for the assessment template, which must be unique within the AWS Region and is used to identify the template in the Inspector console and API; if omitted, AWS generates a default name. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="duration_in_seconds" type="number" required>
  Specifies the duration (in seconds) for which the Inspector assessment run will be executed. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="rules_package_arns" type="list(string)" required>
  The list of Amazon Inspector rules package ARNs that define the security checks run against the assessment targets. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_attributes_for_findings" type="list(object)">
  Specifies user-defined key-value pairs (tags) that are assigned to every finding generated by the assessment template, enabling you to filter or categorize findings by these attributes. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined key-value pair that is assigned to findings generated by this assessment template. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined attribute (key-value pair) attached to findings generated by this assessment template, used to categorize or filter findings in Amazon Inspector. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the assessment template. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
