---
title: "aws_inspector_assessment_template"
description: "Real, generated bindings for `aws_inspector_assessment_template`."
---

`aws_inspector_assessment_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/inspector"

    cfg := inspector.AssessmentTemplateConfig{
        Duration: 1,
        Name: "example-name",
        RulesPackageArns: []string{"example"},
        TargetArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(inspector.AssessmentTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AssessmentTemplate } from
      "jsr:@ubx/sdk-aws/aws/inspector/assessment_template";

    resource(AssessmentTemplate, "example", {
      duration: 1,
      name: "example",
      rulesPackageArns: ["example"],
      targetArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.inspector import (
        AssessmentTemplate,
        AssessmentTemplateConfig,
    )

    cfg = AssessmentTemplateConfig(
        duration=1,
        name="example",
        rules_package_arns=["example"],
        target_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(AssessmentTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_inspector_assessment_template with duration, name,
rules_package_arns, target_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="duration" type="number" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rules_package_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
