---
title: "aws_inspector_v2_code_security_scan_configuration"
description: "`aws_inspector_v2_code_security_scan_configuration` defines real scanning rules..."
---

`aws_inspector_v2_code_security_scan_configuration` defines real scanning rules, applied by default to every new repository or scoped to specific ones by tag, under an `aws_inspector_v2_code_security_integration`, up to three default configurations per project.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	inspector "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/inspector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v2 code security scan configuration"})

    		ubx.Resource(inspector.V2CodeSecurityScanConfiguration, "ci-runner", inspector.V2CodeSecurityScanConfigurationConfig{
    			Name:          "ci-runner",
    			Configuration: map[string]any{"rule_set_categories": []string{"example"}},
    			Level:         "example",
    			ScopeSettings: map[string]any{"project_selection_scope": "example"},
    			Tags:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2CodeSecurityScanConfiguration } from "./local-sdk/aws/sdk/typescript/aws/inspector/v2_code_security_scan_configuration.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own v2 code security scan configuration" });

      resource(V2CodeSecurityScanConfiguration, "ci-runner", {
        name: "ci-runner",
        configuration: { ruleSetCategories: ["example"] },
        level: "example",
        scopeSettings: { projectSelectionScope: "example" },
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.inspector import V2CodeSecurityScanConfiguration, V2CodeSecurityScanConfigurationConfig

    def describe():
        ubx.intent("payments own v2 code security scan configuration")

        ubx.resource(V2CodeSecurityScanConfiguration, "ci-runner", V2CodeSecurityScanConfigurationConfig(
            name="ci-runner",
            configuration={"rule_set_categories": ["example"]},
            level="example",
            scope_settings={"project_selection_scope": "example"},
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_inspector_v2_code_security_scan_configuration called
    "ci-runner", with configuration a set value, level example, scope
    settings a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration" type="object">
  Configuration is an object that specifies how Inspector v2 performs code security scans, including the scan mode (continuous or on a schedule) and any associated scheduling details such as daily start time. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="continuous_integration_scan_configuration" type="object">
      Configures continuous integration scan behavior for Inspector code security scans, including whether auto-scan is enabled and which scan type (full or diff) to apply when scanning code from CI systems. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="supported_events" type="list(string)" required>
          Specifies the continuous integration events (such as push or pull_request) that trigger an Inspector v2 code security scan for the associated repository. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="periodic_scan_configuration" type="object">
      Defines the schedule for recurring code security scans, specifying the interval (e.g., daily or weekly) at which AWS Inspector automatically rescans the configured repository. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="frequency" type="string">
          Specifies the recurrence interval (e.g., daily, weekly, or monthly) at which AWS Inspector periodically runs code security scans for the configured resources. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="frequency_expression" type="string">
          The cron expression that determines when periodic code security scans are triggered by AWS Inspector V2. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rule_set_categories" type="list(string)" required>
      The list of rule set categories (valid values: `SECURITY` and `CODE_QUALITY`) that the code security scan configuration uses to select which security and code-quality rule groups are applied during scans. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="level" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Code Security Scan Configuration name Optional.
</ResponseField>

<ResponseField name="scope_settings" type="object">
  Determines the set of programming languages and their associated severity scan modes (e.g., HIGH or CRITICAL) that limit which code security findings are generated by the scan configuration. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="project_selection_scope" type="string">
      For the AWS Inspector v2 code security scan configuration, this field specifies the project selection scope, which determines whether the scan applies to all available projects or only to a chosen subset of projects. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  Assigns customizable tags to the CodeSecurityScanConfiguration resource to help identify, organize, and manage it within AWS Inspector V2. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Code Security Scan Configuration ARN Computed by AWS after `ship`.
</ResponseField>
