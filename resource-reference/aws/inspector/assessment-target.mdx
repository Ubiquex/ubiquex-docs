---
title: "aws_inspector_assessment_target"
description: "Real, generated bindings for `aws_inspector_assessment_target`."
---

`aws_inspector_assessment_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/inspector"

    cfg := inspector.AssessmentTargetConfig{
        Name: "example-name",
        ResourceGroupArn: "example",
    }
    ubx.Resource(inspector.AssessmentTarget, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AssessmentTarget } from
      "jsr:@ubx/sdk-aws/aws/inspector/assessment_target";

    resource(AssessmentTarget, "example", {
      name: "example",
      resourceGroupArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.inspector import (
        AssessmentTarget,
        AssessmentTargetConfig,
    )

    cfg = AssessmentTargetConfig(
        name="example",
        resource_group_arn="example",
    )
    ubx.resource(AssessmentTarget, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_inspector_assessment_target with name,
resource_group_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_group_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
