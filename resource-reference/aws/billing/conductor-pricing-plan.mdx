---
title: "aws_billing_conductor_pricing_plan"
description: "`aws_billing_conductor_pricing_plan` bundles one or more `aws_billing_conductor_pricing_rule`s into the pricing..."
---

`aws_billing_conductor_pricing_plan` bundles one or more `aws_billing_conductor_pricing_rule`s into the pricing an `aws_billing_conductor_billing_group`'s member accounts see, letting an organization define custom, marked-up, or discounted rates for chargeback and showback separate from AWS's own real bill.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own conductor pricing plan"})

    		ubx.Resource(billing.ConductorPricingPlan, "example", billing.ConductorPricingPlanConfig{
    			Name:            "example",
    			Description:     "Managed by ubx.",
    			PricingRuleArns: []string{"example"},
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConductorPricingPlan } from "@ubx/sdk-aws/aws/billing/conductor_pricing_plan";

    export default stack("example", () => {
      intent({ summary: "example own conductor pricing plan" });

      resource(ConductorPricingPlan, "example", {
        name: "example",
        description: "Managed by ubx.",
        pricingRuleArns: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.billing import ConductorPricingPlan, ConductorPricingPlanConfig

    def describe():
        ubx.intent("example own conductor pricing plan")

        ubx.resource(ConductorPricingPlan, "example", ConductorPricingPlanConfig(
            name="example",
            description="Managed by ubx.",
            pricing_rule_arns=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_billing_conductor_pricing_plan called "example", with
    description Managed by ubx., pricing rule arns a set value, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The user-defined name for the pricing plan, required when creating the plan in AWS Billing Conductor and used for identification and display. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="pricing_rule_arns" type="list(string)">
  The ARNs of the pricing rules to associate with this pricing plan. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key portion of a user-defined tag assigned to the AWS Billing Conductor pricing plan, used to identify and categorize the plan within the resource's tag set. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag key-value pair attached to the AWS Billing Conductor pricing plan, used to organize, track, or control access to the plan. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Pricing Plan ARN Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="number">
  Creation timestamp in UNIX epoch time format Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="number">
  Latest modified timestamp in UNIX epoch time format Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="size" type="number">
  Number of associated pricing rules Computed by AWS after `ship`.
</ResponseField>
