---
title: "aws_billing_view"
description: "Real, generated bindings for `aws_billing_view`."
---

`aws_billing_view` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/billing"

    cfg := billing.ViewConfig{
        Name: "example-name",
        Description: "example",
        SourceViews: []string{"example"},
    }
    ubx.Resource(billing.View, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { View } from "jsr:@ubx/sdk-aws/aws/billing/view";

    resource(View, "example", {
      name: "example",
      description: "example",
      sourceViews: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.billing import View, ViewConfig

    cfg = ViewConfig(
        name="example",
        description="example",
        source_views=["example"],
    )
    ubx.resource(View, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_billing_view with name, description, source_views.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source_views" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="billing_view_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="derived_view_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_view_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="view_definition_last_updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
