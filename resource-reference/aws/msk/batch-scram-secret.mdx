---
title: "aws_msk_batch_scram_secret"
description: "`aws_msk_batch_scram_secret` associates a real batch of Secrets Manager SASL/SCRAM secrets with an `aws_msk_cluster`..."
---

`aws_msk_batch_scram_secret` associates a real batch of Secrets Manager SASL/SCRAM secrets with an `aws_msk_cluster`, enabling username/password authentication for clients connecting to that cluster.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own batch scram secret"})

    		ubx.Resource(msk.BatchScramSecret, "ci-runner", msk.BatchScramSecretConfig{
    			ClusterArn:    "arn:aws:iam::123456789012:policy/ci",
    			SecretArnList: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BatchScramSecret } from "./local-sdk/aws/sdk/typescript/aws/msk/batch_scram_secret.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own batch scram secret" });

      resource(BatchScramSecret, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        secretArnList: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.msk import BatchScramSecret, BatchScramSecretConfig

    def describe():
        ubx.intent("payments own batch scram secret")

        ubx.resource(BatchScramSecret, "ci-runner", BatchScramSecretConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            secret_arn_list=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_batch_scram_secret, with cluster arn
    arn:aws:iam::123456789012:policy/ci, secret arn list a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cluster_arn" type="string" required>
  The ARN of the Amazon MSK cluster to which the batch of SASL/SCRAM secrets will be associated. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="secret_arn_list" type="list(string)">
  The list of AWS Secrets Manager secret ARNs to associate with the MSK cluster for SCRAM authentication. Optional. **(AI-inferred)**
</ResponseField>
