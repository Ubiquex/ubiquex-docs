---
title: "aws_msk_serverless_cluster"
description: "Real, generated bindings for `aws_msk_serverless_cluster`."
---

`aws_msk_serverless_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"

    cfg := msk.ServerlessClusterConfig{
        ClusterName: "example-cluster-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(msk.ServerlessCluster, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServerlessCluster } from
      "jsr:@ubx/sdk-aws/aws/msk/serverless_cluster";

    resource(ServerlessCluster, "example", {
      clusterName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import (
        ServerlessCluster,
        ServerlessClusterConfig,
    )

    cfg = ServerlessClusterConfig(
        cluster_name="example",
        tags={"key": "value"},
    )
    ubx.resource(ServerlessCluster, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_serverless_cluster with cluster_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_sasl_iam" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cluster_uuid" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
