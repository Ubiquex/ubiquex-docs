---
title: "aws_msk_cluster_policy"
description: "Real, generated bindings for `aws_msk_cluster_policy`."
---

`aws_msk_cluster_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/msk"

    cfg := msk.ClusterPolicyConfig{
        ClusterArn: "example",
        Policy: "example",
    }
    ubx.Resource(msk.ClusterPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ClusterPolicy } from
      "jsr:@ubx/sdk-aws/aws/msk/cluster_policy";

    resource(ClusterPolicy, "example", {
      clusterArn: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import ClusterPolicy, ClusterPolicyConfig

    cfg = ClusterPolicyConfig(
        cluster_arn="example",
        policy="example",
    )
    ubx.resource(ClusterPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_cluster_policy with cluster_arn, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="current_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
