---
title: "aws_msk_cluster_policy"
description: "`aws_msk_cluster_policy` attaches a real resource-based access policy to an `aws_msk_cluster`, controlling which principals..."
---

`aws_msk_cluster_policy` attaches a real resource-based access policy to an `aws_msk_cluster`, controlling which principals, including from other accounts, can connect to and manage it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster policy"})

    		ubx.Resource(msk.ClusterPolicy, "ci-runner", msk.ClusterPolicyConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/ci",
    			Policy:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterPolicy } from "./local-sdk/aws/sdk/typescript/aws/msk/cluster_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster policy" });

      resource(ClusterPolicy, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        policy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.msk import ClusterPolicy, ClusterPolicyConfig

    def describe():
        ubx.intent("payments own cluster policy")

        ubx.resource(ClusterPolicy, "ci-runner", ClusterPolicyConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_cluster_policy, with cluster arn
    arn:aws:iam::123456789012:policy/ci, policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_arn" type="string" required>
  The arn of the cluster for the resource policy. Required.
</ResponseField>

<ResponseField name="policy" type="any" required>
  A policy document containing permissions to add to the specified cluster. Required.
</ResponseField>

## Output properties

<ResponseField name="current_version" type="string">
  The current version of the policy attached to the specified cluster Computed by AWS after `ship`.
</ResponseField>
