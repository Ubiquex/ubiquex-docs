---
title: "aws_msk_topic"
description: "Real, generated bindings for `aws_msk_topic`."
---

`aws_msk_topic` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"

    cfg := msk.TopicConfig{
        ClusterArn: "example",
        Name: "example-name",
        PartitionCount: 1,
        ReplicationFactor: 1,
        Configs: "example",
    }
    ubx.Resource(msk.Topic, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Topic } from "jsr:@ubx/sdk-aws/aws/msk/topic";

    resource(Topic, "example", {
      clusterArn: "example",
      name: "example",
      partitionCount: 1,
      replicationFactor: 1,
      configs: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import Topic, TopicConfig

    cfg = TopicConfig(
        cluster_arn="example",
        name="example",
        partition_count=1,
        replication_factor=1,
        configs="example",
    )
    ubx.resource(Topic, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_topic with cluster_arn, name, partition_count,
replication_factor, configs.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configs" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="partition_count" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_factor" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="configs_actual" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
