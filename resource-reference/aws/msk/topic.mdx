---
title: "aws_msk_topic"
description: "Real, generated bindings for `aws_msk_topic`."
---

`aws_msk_topic` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own topic"})

    		ubx.Resource(msk.Topic, "ci-runner", msk.TopicConfig{
    			ClusterArn:        "arn:aws:iam::123456789012:policy/ci",
    			Name:              "ci-runner",
    			PartitionCount:    1,
    			ReplicationFactor: 1,
    			Configs:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Topic } from "jsr:@ubx/sdk-aws/aws/msk/topic";

    export default stack("payments", () => {
      intent({ summary: "payments own topic" });

      resource(Topic, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        partitionCount: 1,
        replicationFactor: 1,
        configs: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.msk import Topic, TopicConfig

    def describe():
        ubx.intent("payments own topic")

        ubx.resource(Topic, "ci-runner", TopicConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            partition_count=1,
            replication_factor=1,
            configs="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_topic called "ci-runner", with cluster arn
    arn:aws:iam::123456789012:policy/ci, partition count 1,
    replication factor 1, configs example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configs" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="partition_count" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_factor" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="configs_actual" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
