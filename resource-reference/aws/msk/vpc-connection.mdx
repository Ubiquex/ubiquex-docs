---
title: "aws_msk_vpc_connection"
description: "Real, generated bindings for `aws_msk_vpc_connection`."
---

`aws_msk_vpc_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"

    cfg := msk.VpcConnectionConfig{
        Authentication: "example",
        ClientSubnets: []string{"example"},
        SecurityGroups: []string{"example"},
        TargetClusterArn: "example",
        VpcId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(msk.VpcConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VpcConnection } from
      "jsr:@ubx/sdk-aws/aws/msk/vpc_connection";

    resource(VpcConnection, "example", {
      authentication: "example",
      clientSubnets: ["example"],
      securityGroups: ["example"],
      targetClusterArn: "example",
      vpcId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import VpcConnection, VpcConnectionConfig

    cfg = VpcConnectionConfig(
        authentication="example",
        client_subnets=["example"],
        security_groups=["example"],
        target_cluster_arn="example",
        vpc_id="example",
        tags={"key": "value"},
    )
    ubx.resource(VpcConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_vpc_connection with authentication,
client_subnets, security_groups, target_cluster_arn, vpc_id,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_subnets" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
