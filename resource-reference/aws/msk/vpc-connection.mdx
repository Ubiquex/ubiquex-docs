---
title: "aws_msk_vpc_connection"
description: "Real, generated bindings for `aws_msk_vpc_connection`."
---

`aws_msk_vpc_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc connection"})

    		ubx.Resource(msk.VpcConnection, "ci-runner", msk.VpcConnectionConfig{
    			Authentication:   "example",
    			ClientSubnets:    []string{"example"},
    			SecurityGroups:   []string{"example"},
    			TargetClusterArn: "arn:aws:iam::123456789012:policy/ci",
    			VpcId:            "example",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcConnection } from "jsr:@ubx/sdk-aws/aws/msk/vpc_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc connection" });

      resource(VpcConnection, "ci-runner", {
        authentication: "example",
        clientSubnets: ["example"],
        securityGroups: ["example"],
        targetClusterArn: "arn:aws:iam::123456789012:policy/ci",
        vpcId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.msk import VpcConnection, VpcConnectionConfig

    def describe():
        ubx.intent("payments own vpc connection")

        ubx.resource(VpcConnection, "ci-runner", VpcConnectionConfig(
            authentication="example",
            client_subnets=["example"],
            security_groups=["example"],
            target_cluster_arn="arn:aws:iam::123456789012:policy/ci",
            vpc_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_vpc_connection, with authentication example,
    client subnets a set value, security groups a set value, target
    cluster arn arn:aws:iam::123456789012:policy/ci, vpc id example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_subnets" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
