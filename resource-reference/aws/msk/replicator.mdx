---
title: "aws_msk_replicator"
description: "Real, generated bindings for `aws_msk_replicator`."
---

`aws_msk_replicator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"

    cfg := msk.ReplicatorConfig{
        ReplicatorName: "example-replicator-name",
        ServiceExecutionRoleArn: "example",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(msk.Replicator, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Replicator } from "jsr:@ubx/sdk-aws/aws/msk/replicator";

    resource(Replicator, "example", {
      replicatorName: "example",
      serviceExecutionRoleArn: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import Replicator, ReplicatorConfig

    cfg = ReplicatorConfig(
        replicator_name="example",
        service_execution_role_arn="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(Replicator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_replicator with replicator_name,
service_execution_role_arn, description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replicator_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="current_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
