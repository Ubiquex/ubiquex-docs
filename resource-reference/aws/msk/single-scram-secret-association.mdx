---
title: "aws_msk_single_scram_secret_association"
description: "Real, generated bindings for `aws_msk_single_scram_secret_association`."
---

`aws_msk_single_scram_secret_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/msk"

    cfg := msk.SingleScramSecretAssociationConfig{
        ClusterArn: "example",
        SecretArn: "example",
    }
    ubx.Resource(msk.SingleScramSecretAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SingleScramSecretAssociation } from
      "jsr:@ubx/sdk-aws/aws/msk/single_scram_secret_association";

    resource(SingleScramSecretAssociation, "example", {
      clusterArn: "example",
      secretArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import (
        SingleScramSecretAssociation,
        SingleScramSecretAssociationConfig,
    )

    cfg = SingleScramSecretAssociationConfig(
        cluster_arn="example",
        secret_arn="example",
    )
    ubx.resource(SingleScramSecretAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_single_scram_secret_association with
cluster_arn, secret_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
