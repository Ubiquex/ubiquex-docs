---
title: "aws_msk_single_scram_secret_association"
description: "Real, generated bindings for `aws_msk_single_scram_secret_association`."
---

`aws_msk_single_scram_secret_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own single scram secret association"})

    		ubx.Resource(msk.SingleScramSecretAssociation, "ci-runner", msk.SingleScramSecretAssociationConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/ci",
    			SecretArn:  "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SingleScramSecretAssociation } from "jsr:@ubx/sdk-aws/aws/msk/single_scram_secret_association";

    export default stack("payments", () => {
      intent({ summary: "payments own single scram secret association" });

      resource(SingleScramSecretAssociation, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        secretArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.msk import SingleScramSecretAssociation, SingleScramSecretAssociationConfig

    def describe():
        ubx.intent("payments own single scram secret association")

        ubx.resource(SingleScramSecretAssociation, "ci-runner", SingleScramSecretAssociationConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            secret_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_single_scram_secret_association, with cluster arn
    arn:aws:iam::123456789012:policy/ci, secret arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
