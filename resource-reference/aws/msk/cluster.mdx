---
title: "aws_msk_cluster"
description: "Real, generated bindings for `aws_msk_cluster`."
---

`aws_msk_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster"})

    		ubx.Resource(msk.Cluster, "ci-runner", msk.ClusterConfig{
    			ClusterName:         "example-cluster-name",
    			KafkaVersion:        "example",
    			NumberOfBrokerNodes: 1,
    			EnhancedMonitoring:  "example",
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Cluster } from "jsr:@ubx/sdk-aws/aws/msk/cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster" });

      resource(Cluster, "ci-runner", {
        clusterName: "example-cluster-name",
        kafkaVersion: "example",
        numberOfBrokerNodes: 1,
        enhancedMonitoring: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.msk import Cluster, ClusterConfig

    def describe():
        ubx.intent("payments own cluster")

        ubx.resource(Cluster, "ci-runner", ClusterConfig(
            cluster_name="example-cluster-name",
            kafka_version="example",
            number_of_broker_nodes=1,
            enhanced_monitoring="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_cluster, with cluster name example-cluster-name,
    kafka version example, number of broker nodes 1, enhanced
    monitoring example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enhanced_monitoring" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kafka_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="number_of_broker_nodes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_ipv6" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_public_sasl_iam" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_public_sasl_scram" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_public_tls" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_sasl_iam" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_sasl_iam_ipv6" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_sasl_scram" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_sasl_scram_ipv6" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_tls" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_tls_ipv6" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_vpc_connectivity_sasl_iam" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_vpc_connectivity_sasl_scram" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bootstrap_brokers_vpc_connectivity_tls" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cluster_uuid" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="current_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="customer_action_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_mode" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="zookeeper_connect_string" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="zookeeper_connect_string_tls" type="string">
  Computed by AWS after `ship`.
</ResponseField>
