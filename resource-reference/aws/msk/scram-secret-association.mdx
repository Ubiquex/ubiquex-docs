---
title: "aws_msk_scram_secret_association"
description: "Real, generated bindings for `aws_msk_scram_secret_association`."
---

`aws_msk_scram_secret_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	msk "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scram secret association"})

    		ubx.Resource(msk.ScramSecretAssociation, "ci-runner", msk.ScramSecretAssociationConfig{
    			ClusterArn:    "arn:aws:iam::123456789012:policy/ci",
    			SecretArnList: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScramSecretAssociation } from "jsr:@ubx/sdk-aws/aws/msk/scram_secret_association";

    export default stack("payments", () => {
      intent({ summary: "payments own scram secret association" });

      resource(ScramSecretAssociation, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        secretArnList: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.msk import ScramSecretAssociation, ScramSecretAssociationConfig

    def describe():
        ubx.intent("payments own scram secret association")

        ubx.resource(ScramSecretAssociation, "ci-runner", ScramSecretAssociationConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            secret_arn_list=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_msk_scram_secret_association, with cluster arn
    arn:aws:iam::123456789012:policy/ci, secret arn list a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn_list" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
