---
title: "aws_msk_scram_secret_association"
description: "Real, generated bindings for `aws_msk_scram_secret_association`."
---

`aws_msk_scram_secret_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/msk"

    cfg := msk.ScramSecretAssociationConfig{
        ClusterArn: "example",
        SecretArnList: []string{"example"},
    }
    ubx.Resource(msk.ScramSecretAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ScramSecretAssociation } from
      "jsr:@ubx/sdk-aws/aws/msk/scram_secret_association";

    resource(ScramSecretAssociation, "example", {
      clusterArn: "example",
      secretArnList: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.msk import (
        ScramSecretAssociation,
        ScramSecretAssociationConfig,
    )

    cfg = ScramSecretAssociationConfig(
        cluster_arn="example",
        secret_arn_list=["example"],
    )
    ubx.resource(ScramSecretAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_msk_scram_secret_association with cluster_arn,
secret_arn_list.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn_list" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
