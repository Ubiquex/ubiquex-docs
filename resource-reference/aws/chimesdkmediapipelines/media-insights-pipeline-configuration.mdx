---
title: "aws_chimesdkmediapipelines_media_insights_pipeline_configuration"
description: "Real, generated bindings for `aws_chimesdkmediapipelines_media_insights_pipeline_configuration`."
---

`aws_chimesdkmediapipelines_media_insights_pipeline_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	chimesdkmediapipelines "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/chimesdkmediapipelines"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own media insights pipeline configuration"})

    		ubx.Resource(chimesdkmediapipelines.MediaInsightsPipelineConfiguration, "ci-runner", chimesdkmediapipelines.MediaInsightsPipelineConfigurationConfig{
    			Name:                  "ci-runner",
    			ResourceAccessRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MediaInsightsPipelineConfiguration } from "jsr:@ubx/sdk-aws/aws/chimesdkmediapipelines/media_insights_pipeline_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own media insights pipeline configuration" });

      resource(MediaInsightsPipelineConfiguration, "ci-runner", {
        name: "ci-runner",
        resourceAccessRoleArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.chimesdkmediapipelines import MediaInsightsPipelineConfiguration, MediaInsightsPipelineConfigurationConfig

    def describe():
        ubx.intent("payments own media insights pipeline configuration")

        ubx.resource(MediaInsightsPipelineConfiguration, "ci-runner", MediaInsightsPipelineConfigurationConfig(
            name="ci-runner",
            resource_access_role_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_chimesdkmediapipelines_media_insights_pipeline_configuration
    called "ci-runner", with resource access role arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_access_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
