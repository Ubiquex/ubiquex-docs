---
title: "aws_main_route_table_association"
description: "Real, generated bindings for `aws_main_route_table_association`."
---

`aws_main_route_table_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	main_ "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/main_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own route table association"})

    		ubx.Resource(main_.RouteTableAssociation, "ci-runner", main_.RouteTableAssociationConfig{
    			RouteTableId: "example",
    			VpcId:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RouteTableAssociation } from "jsr:@ubx/sdk-aws/aws/main/route_table_association";

    export default stack("payments", () => {
      intent({ summary: "payments own route table association" });

      resource(RouteTableAssociation, "ci-runner", {
        routeTableId: "example",
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.main import RouteTableAssociation, RouteTableAssociationConfig

    def describe():
        ubx.intent("payments own route table association")

        ubx.resource(RouteTableAssociation, "ci-runner", RouteTableAssociationConfig(
            route_table_id="example",
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_main_route_table_association, with route table id
    example, vpc id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="original_route_table_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
