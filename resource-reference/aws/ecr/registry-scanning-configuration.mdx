---
title: "aws_ecr_registry_scanning_configuration"
description: "Real, generated bindings for `aws_ecr_registry_scanning_configuration`."
---

`aws_ecr_registry_scanning_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ecr"

    cfg := ecr.RegistryScanningConfigurationConfig{
        ScanType: "example",
    }
    ubx.Resource(ecr.RegistryScanningConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegistryScanningConfiguration } from
      "jsr:@ubx/sdk-aws/aws/ecr/registry_scanning_configuration";

    resource(RegistryScanningConfiguration, "example", {
      scanType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecr import (
        RegistryScanningConfiguration,
        RegistryScanningConfigurationConfig,
    )

    cfg = RegistryScanningConfigurationConfig(
        scan_type="example",
    )
    ubx.resource(RegistryScanningConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecr_registry_scanning_configuration with scan_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scan_type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
