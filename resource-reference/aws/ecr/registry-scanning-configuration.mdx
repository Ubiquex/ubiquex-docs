---
title: "aws_ecr_registry_scanning_configuration"
description: "Real, generated bindings for `aws_ecr_registry_scanning_configuration`."
---

`aws_ecr_registry_scanning_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own registry scanning configuration"})

    		ubx.Resource(ecr.RegistryScanningConfiguration, "ci-runner", ecr.RegistryScanningConfigurationConfig{
    			ScanType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryScanningConfiguration } from "jsr:@ubx/sdk-aws/aws/ecr/registry_scanning_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own registry scanning configuration" });

      resource(RegistryScanningConfiguration, "ci-runner", {
        scanType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecr import RegistryScanningConfiguration, RegistryScanningConfigurationConfig

    def describe():
        ubx.intent("payments own registry scanning configuration")

        ubx.resource(RegistryScanningConfiguration, "ci-runner", RegistryScanningConfigurationConfig(
            scan_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_registry_scanning_configuration, with scan type
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scan_type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
