---
title: "aws_ecr_replication_configuration"
description: "Real, generated bindings for `aws_ecr_replication_configuration`."
---

`aws_ecr_replication_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"

    cfg := ecr.ReplicationConfigurationConfig{
    }
    ubx.Resource(ecr.ReplicationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ReplicationConfiguration } from
      "jsr:@ubx/sdk-aws/aws/ecr/replication_configuration";

    resource(ReplicationConfiguration, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecr import (
        ReplicationConfiguration,
        ReplicationConfigurationConfig,
    )

    cfg = ReplicationConfigurationConfig(
    )
    ubx.resource(ReplicationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecr_replication_configuration.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
