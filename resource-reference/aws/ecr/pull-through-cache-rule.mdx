---
title: "aws_ecr_pull_through_cache_rule"
description: "Real, generated bindings for `aws_ecr_pull_through_cache_rule`."
---

`aws_ecr_pull_through_cache_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ecr"

    cfg := ecr.PullThroughCacheRuleConfig{
        EcrRepositoryPrefix: "example",
        UpstreamRegistryUrl: "example",
        CredentialArn: "example",
        CustomRoleArn: "example",
    }
    ubx.Resource(ecr.PullThroughCacheRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PullThroughCacheRule } from
      "jsr:@ubx/sdk-aws/aws/ecr/pull_through_cache_rule";

    resource(PullThroughCacheRule, "example", {
      ecrRepositoryPrefix: "example",
      upstreamRegistryUrl: "example",
      credentialArn: "example",
      customRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecr import (
        PullThroughCacheRule,
        PullThroughCacheRuleConfig,
    )

    cfg = PullThroughCacheRuleConfig(
        ecr_repository_prefix="example",
        upstream_registry_url="example",
        credential_arn="example",
        custom_role_arn="example",
    )
    ubx.resource(PullThroughCacheRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecr_pull_through_cache_rule with
ecr_repository_prefix, upstream_registry_url, credential_arn,
custom_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="credential_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="custom_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="ecr_repository_prefix" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="upstream_registry_url" type="string" required>
  Required.
</ResponseField>

<ResponseField name="upstream_repository_prefix" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
