---
title: "aws_ecr_lifecycle_policy"
description: "Real, generated bindings for `aws_ecr_lifecycle_policy`."
---

`aws_ecr_lifecycle_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"

    cfg := ecr.LifecyclePolicyConfig{
        Policy: "example",
        Repository: "example",
    }
    ubx.Resource(ecr.LifecyclePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LifecyclePolicy } from
      "jsr:@ubx/sdk-aws/aws/ecr/lifecycle_policy";

    resource(LifecyclePolicy, "example", {
      policy: "example",
      repository: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecr import LifecyclePolicy, LifecyclePolicyConfig

    cfg = LifecyclePolicyConfig(
        policy="example",
        repository="example",
    )
    ubx.resource(LifecyclePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecr_lifecycle_policy with policy, repository.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
