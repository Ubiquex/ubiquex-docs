---
title: "aws_ecr_account_setting"
description: "Real, generated bindings for `aws_ecr_account_setting`."
---

`aws_ecr_account_setting` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account setting"})

    		ubx.Resource(ecr.AccountSetting, "ci-runner", ecr.AccountSettingConfig{
    			Name:  "ci-runner",
    			Value: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountSetting } from "jsr:@ubx/sdk-aws/aws/ecr/account_setting";

    export default stack("payments", () => {
      intent({ summary: "payments own account setting" });

      resource(AccountSetting, "ci-runner", {
        name: "ci-runner",
        value: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecr import AccountSetting, AccountSettingConfig

    def describe():
        ubx.intent("payments own account setting")

        ubx.resource(AccountSetting, "ci-runner", AccountSettingConfig(
            name="ci-runner",
            value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_account_setting called "ci-runner", with value
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="value" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
