---
title: "aws_ecr_pull_time_update_exclusion"
description: "`aws_ecr_pull_time_update_exclusion` excludes a real, specific pull request, most commonly from an automated scanner or CI system..."
---

`aws_ecr_pull_time_update_exclusion` excludes a real, specific pull request, most commonly from an automated scanner or CI system, from updating an `aws_ecr_repository` image's own last-pulled timestamp, keeping that timestamp meaningful for real usage-based lifecycle policies.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own pull time update exclusion"})

    		ubx.Resource(ecr.PullTimeUpdateExclusion, "example", ecr.PullTimeUpdateExclusionConfig{
    			PrincipalArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PullTimeUpdateExclusion } from "@ubx/sdk-aws/aws/ecr/pull_time_update_exclusion";

    export default stack("example", () => {
      intent({ summary: "example own pull time update exclusion" });

      resource(PullTimeUpdateExclusion, "example", {
        principalArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecr import PullTimeUpdateExclusion, PullTimeUpdateExclusionConfig

    def describe():
        ubx.intent("example own pull time update exclusion")

        ubx.resource(PullTimeUpdateExclusion, "example", PullTimeUpdateExclusionConfig(
            principal_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_pull_time_update_exclusion, with principal arn
    arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="principal_arn" type="string" required>
  Principal arn that should not update image pull times. Required.
</ResponseField>
