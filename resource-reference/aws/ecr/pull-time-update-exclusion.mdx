---
title: "aws_ecr_pull_time_update_exclusion"
description: "Real, generated bindings for `aws_ecr_pull_time_update_exclusion`."
---

`aws_ecr_pull_time_update_exclusion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ecr"

    cfg := ecr.PullTimeUpdateExclusionConfig{
        PrincipalArn: "example",
    }
    ubx.Resource(ecr.PullTimeUpdateExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PullTimeUpdateExclusion } from
      "jsr:@ubx/sdk-aws/aws/ecr/pull_time_update_exclusion";

    resource(PullTimeUpdateExclusion, "example", {
      principalArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ecr import (
        PullTimeUpdateExclusion,
        PullTimeUpdateExclusionConfig,
    )

    cfg = PullTimeUpdateExclusionConfig(
        principal_arn="example",
    )
    ubx.resource(PullTimeUpdateExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ecr_pull_time_update_exclusion with principal_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
