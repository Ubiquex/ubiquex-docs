---
title: "aws_ecr_pull_time_update_exclusion"
description: "Real, generated bindings for `aws_ecr_pull_time_update_exclusion`."
---

`aws_ecr_pull_time_update_exclusion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pull time update exclusion"})

    		ubx.Resource(ecr.PullTimeUpdateExclusion, "ci-runner", ecr.PullTimeUpdateExclusionConfig{
    			PrincipalArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PullTimeUpdateExclusion } from "jsr:@ubx/sdk-aws/aws/ecr/pull_time_update_exclusion";

    export default stack("payments", () => {
      intent({ summary: "payments own pull time update exclusion" });

      resource(PullTimeUpdateExclusion, "ci-runner", {
        principalArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecr import PullTimeUpdateExclusion, PullTimeUpdateExclusionConfig

    def describe():
        ubx.intent("payments own pull time update exclusion")

        ubx.resource(PullTimeUpdateExclusion, "ci-runner", PullTimeUpdateExclusionConfig(
            principal_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_pull_time_update_exclusion, with principal arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
