---
title: "aws_ecr_repository_creation_template"
description: "Real, generated bindings for `aws_ecr_repository_creation_template`."
---

`aws_ecr_repository_creation_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own repository creation template"})

    		ubx.Resource(ecr.RepositoryCreationTemplate, "ci-runner", ecr.RepositoryCreationTemplateConfig{
    			AppliedFor:         []string{"example"},
    			Prefix:             "example",
    			CustomRoleArn:      "arn:aws:iam::123456789012:policy/ci",
    			Description:        "Managed by ubx.",
    			ImageTagMutability: "example",
    			LifecyclePolicy:    "example",
    			RepositoryPolicy:   "example",
    			ResourceTags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RepositoryCreationTemplate } from "jsr:@ubx/sdk-aws/aws/ecr/repository_creation_template";

    export default stack("payments", () => {
      intent({ summary: "payments own repository creation template" });

      resource(RepositoryCreationTemplate, "ci-runner", {
        appliedFor: ["example"],
        prefix: "example",
        customRoleArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        imageTagMutability: "example",
        lifecyclePolicy: "example",
        repositoryPolicy: "example",
        resourceTags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecr import RepositoryCreationTemplate, RepositoryCreationTemplateConfig

    def describe():
        ubx.intent("payments own repository creation template")

        ubx.resource(RepositoryCreationTemplate, "ci-runner", RepositoryCreationTemplateConfig(
            applied_for=["example"],
            prefix="example",
            custom_role_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            image_tag_mutability="example",
            lifecycle_policy="example",
            repository_policy="example",
            resource_tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_repository_creation_template, with applied for a
    set value, prefix example, custom role arn
    arn:aws:iam::123456789012:policy/ci, description Managed by ubx.,
    image tag mutability example, lifecycle policy example,
    repository policy example, resource tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="applied_for" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="custom_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_tag_mutability" type="string">
  Optional.
</ResponseField>

<ResponseField name="lifecycle_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="prefix" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="resource_tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
