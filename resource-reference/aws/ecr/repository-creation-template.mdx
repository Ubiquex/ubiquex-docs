---
title: "aws_ecr_repository_creation_template"
description: "`aws_ecr_repository_creation_template` defines the real settings, access policy, tag immutability, encryption, lifecycle rules..."
---

`aws_ecr_repository_creation_template` defines the real settings, access policy, tag immutability, encryption, lifecycle rules, applied automatically to repositories ECR creates on a caller's behalf during pull-through cache or replication, matched to a specific repository-name prefix rather than every auto-created repository defaulting to ECR's own bare settings.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own repository creation template"})

    		ubx.Resource(ecr.RepositoryCreationTemplate, "example", ecr.RepositoryCreationTemplateConfig{
    			AppliedFor:                         []string{"example"},
    			Prefix:                             "example",
    			CustomRoleArn:                      "arn:aws:iam::123456789012:policy/example",
    			Description:                        "Managed by ubx.",
    			EncryptionConfiguration:            map[string]any{"encryption_type": "example"},
    			ImageTagMutability:                 "example",
    			ImageTagMutabilityExclusionFilters: []map[string]any{{"image_tag_mutability_exclusion_filter_type": "example"}},
    			LifecyclePolicy:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RepositoryCreationTemplate } from "./local-sdk/aws/sdk/typescript/aws/ecr/repository_creation_template.ts";

    export default stack("example", () => {
      intent({ summary: "example own repository creation template" });

      resource(RepositoryCreationTemplate, "example", {
        appliedFor: ["example"],
        prefix: "example",
        customRoleArn: "arn:aws:iam::123456789012:policy/example",
        description: "Managed by ubx.",
        encryptionConfiguration: { encryptionType: "example" },
        imageTagMutability: "example",
        imageTagMutabilityExclusionFilters: [{
          imageTagMutabilityExclusionFilterType: "example",
        }],
        lifecyclePolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.ecr import RepositoryCreationTemplate, RepositoryCreationTemplateConfig

    def describe():
        ubx.intent("example own repository creation template")

        ubx.resource(RepositoryCreationTemplate, "example", RepositoryCreationTemplateConfig(
            applied_for=["example"],
            prefix="example",
            custom_role_arn="arn:aws:iam::123456789012:policy/example",
            description="Managed by ubx.",
            encryption_configuration={"encryption_type": "example"},
            image_tag_mutability="example",
            image_tag_mutability_exclusion_filters=[{"image_tag_mutability_exclusion_filter_type": "example"}],
            lifecycle_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_repository_creation_template, with applied for a
    set value, prefix example, custom role arn
    arn:aws:iam::123456789012:policy/example, description Managed by
    ubx., encryption configuration a set value, image tag mutability
    example, image tag mutability exclusion filters a set value,
    lifecycle policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="applied_for" type="list(string)" required>
  A list of enumerable Strings representing the repository creation scenarios that this template will apply towards. The supported scenarios are PULL_THROUGH_CACHE, REPLICATION, and CREATE_ON_PUSH Required.
</ResponseField>

<ResponseField name="custom_role_arn" type="string">
  The ARN of the role to be assumed by Amazon ECR. Amazon ECR will assume your supplied role when the customRoleArn is specified. When this field isn't specified, Amazon ECR will use the service-linked role for the repository creation template. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description associated with the repository creation template. Optional.
</ResponseField>

<ResponseField name="encryption_configuration" type="object">
  The encryption configuration for the repository. This determines how the contents of your repository are encrypted at rest. By default, when no encryption configuration is set or the ``AES256`` encryption type is used, Amazon ECR uses server-side encryption with Amazon S3-managed encryption keys which encrypts your data at rest using an AES256 encryption algorithm. This does not require any action on your part. For more control over the encryption of the contents of your repository, you can use server-side encryption with KMSlong key stored in KMSlong (KMS) to encrypt your images. For more information, see [Amazon ECR encryption at rest](https://docs.aws.amazon.com/AmazonECR/latest/userguide/encryption-at-rest.html) in the *Amazon Elastic Container Registry User Guide*. Optional.
  <Expandable title="properties">
    <ResponseField name="encryption_type" type="string" required>
      The encryption type to use. Required.
    </ResponseField>
    <ResponseField name="kms_key" type="string">
      If you use the KMS or KMS_DSSE encryption type, specify the CMK to use for encryption. The alias, key ID, or full ARN of the CMK can be specified. The key must exist in the same Region as the repository. If no key is specified, the default AWS managed CMK for Amazon ECR will be used. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="image_tag_mutability" type="string">
  The tag mutability setting for the repository. If this parameter is omitted, the default setting of ``MUTABLE`` will be used which will allow image tags to be overwritten. If ``IMMUTABLE`` is specified, all image tags within the repository will be immutable which will prevent them from being overwritten. Optional.
</ResponseField>

<ResponseField name="image_tag_mutability_exclusion_filters" type="list(object)">
  A list of filters that specify which image tags are excluded from the repository creation template's image tag mutability setting. Optional.
  <Expandable title="properties">
    <ResponseField name="image_tag_mutability_exclusion_filter_type" type="string">
      Specifies the type of filter for image tag mutability exclusions, where 'ALL' matches every tag and 'REGEX' uses a regular expression pattern in the corresponding value field. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_tag_mutability_exclusion_filter_value" type="string">
      This field specifies the tag pattern (such as a regex or wildcard expression) that identifies image tags excluded from the repository creation template's image tag mutability policy, allowing those tags to be overwritten even in immutable repositories. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="lifecycle_policy" type="string">
  The lifecycle policy to use for repositories created using the template. Optional.
</ResponseField>

<ResponseField name="prefix" type="string" required>
  The repository namespace prefix associated with the repository creation template. Required.
</ResponseField>

<ResponseField name="repository_policy" type="string">
  The repository policy to apply to repositories created using the template. A repository policy is a permissions policy associated with a repository to control access permissions. Optional.
</ResponseField>

<ResponseField name="resource_tags" type="list(object)">
  The metadata to apply to the repository to help you categorize and organize. Each tag consists of a key and an optional value, both of which you define. Tag keys can have a maximum character length of 128 characters, and tag values can have a maximum length of 256 characters. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag that this repository creation template automatically applies to newly created Amazon ECR repositories. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair in the ResourceTags list of the AWS ECR Repository Creation Template, used to add metadata for identification, categorization, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The creation timestamp (in ISO 8601 format) recorded by AWS ECR when this repository creation template was first created, provided as a read-only computed attribute. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp indicating when the AWS ECR repository creation template was last updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
