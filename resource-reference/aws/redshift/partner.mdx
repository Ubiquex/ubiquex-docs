---
title: "aws_redshift_partner"
description: "Real, generated bindings for `aws_redshift_partner`."
---

`aws_redshift_partner` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own partner"})

    		ubx.Resource(redshift.Partner, "ci-runner", redshift.PartnerConfig{
    			AccountId:         "example",
    			ClusterIdentifier: "example",
    			DatabaseName:      "example-database-name",
    			PartnerName:       "example-partner-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Partner } from "jsr:@ubx/sdk-aws/aws/redshift/partner";

    export default stack("payments", () => {
      intent({ summary: "payments own partner" });

      resource(Partner, "ci-runner", {
        accountId: "example",
        clusterIdentifier: "example",
        databaseName: "example-database-name",
        partnerName: "example-partner-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import Partner, PartnerConfig

    def describe():
        ubx.intent("payments own partner")

        ubx.resource(Partner, "ci-runner", PartnerConfig(
            account_id="example",
            cluster_identifier="example",
            database_name="example-database-name",
            partner_name="example-partner-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_partner, with account id example, cluster
    identifier example, database name example-database-name, partner
    name example-partner-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="partner_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>
