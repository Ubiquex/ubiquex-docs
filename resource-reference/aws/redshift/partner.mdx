---
title: "aws_redshift_partner"
description: "Real, generated bindings for `aws_redshift_partner`."
---

`aws_redshift_partner` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"

    cfg := redshift.PartnerConfig{
        AccountId: "example",
        ClusterIdentifier: "example",
        DatabaseName: "example-database-name",
        PartnerName: "example-partner-name",
    }
    ubx.Resource(redshift.Partner, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Partner } from "jsr:@ubx/sdk-aws/aws/redshift/partner";

    resource(Partner, "example", {
      accountId: "example",
      clusterIdentifier: "example",
      databaseName: "example",
      partnerName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import Partner, PartnerConfig

    cfg = PartnerConfig(
        account_id="example",
        cluster_identifier="example",
        database_name="example",
        partner_name="example",
    )
    ubx.resource(Partner, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_partner with account_id,
cluster_identifier, database_name, partner_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="partner_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>
