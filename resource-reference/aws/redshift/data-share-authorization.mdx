---
title: "aws_redshift_data_share_authorization"
description: "Real, generated bindings for `aws_redshift_data_share_authorization`."
---

`aws_redshift_data_share_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/redshift"

    cfg := redshift.DataShareAuthorizationConfig{
        ConsumerIdentifier: "example",
        DataShareArn: "example",
        AllowWrites: true,
    }
    ubx.Resource(redshift.DataShareAuthorization, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DataShareAuthorization } from
      "jsr:@ubx/sdk-aws/aws/redshift/data_share_authorization";

    resource(DataShareAuthorization, "example", {
      consumerIdentifier: "example",
      dataShareArn: "example",
      allowWrites: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import (
        DataShareAuthorization,
        DataShareAuthorizationConfig,
    )

    cfg = DataShareAuthorizationConfig(
        consumer_identifier="example",
        data_share_arn="example",
        allow_writes=True,
    )
    ubx.resource(DataShareAuthorization, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_data_share_authorization with
consumer_identifier, data_share_arn, allow_writes.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_writes" type="bool">
  Optional.
</ResponseField>

<ResponseField name="consumer_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="data_share_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="managed_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="producer_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
