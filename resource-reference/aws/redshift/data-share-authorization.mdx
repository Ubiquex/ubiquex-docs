---
title: "aws_redshift_data_share_authorization"
description: "Real, generated bindings for `aws_redshift_data_share_authorization`."
---

`aws_redshift_data_share_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data share authorization"})

    		ubx.Resource(redshift.DataShareAuthorization, "ci-runner", redshift.DataShareAuthorizationConfig{
    			ConsumerIdentifier: "example",
    			DataShareArn:       "arn:aws:iam::123456789012:policy/ci",
    			AllowWrites:        true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataShareAuthorization } from "jsr:@ubx/sdk-aws/aws/redshift/data_share_authorization";

    export default stack("payments", () => {
      intent({ summary: "payments own data share authorization" });

      resource(DataShareAuthorization, "ci-runner", {
        consumerIdentifier: "example",
        dataShareArn: "arn:aws:iam::123456789012:policy/ci",
        allowWrites: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import DataShareAuthorization, DataShareAuthorizationConfig

    def describe():
        ubx.intent("payments own data share authorization")

        ubx.resource(DataShareAuthorization, "ci-runner", DataShareAuthorizationConfig(
            consumer_identifier="example",
            data_share_arn="arn:aws:iam::123456789012:policy/ci",
            allow_writes=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_data_share_authorization, with consumer
    identifier example, data share arn
    arn:aws:iam::123456789012:policy/ci, allow writes true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allow_writes" type="bool">
  Optional.
</ResponseField>

<ResponseField name="consumer_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="data_share_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="managed_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="producer_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
