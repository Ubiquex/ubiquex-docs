---
title: "aws_redshift_data_share_consumer_association"
description: "Real, generated bindings for `aws_redshift_data_share_consumer_association`."
---

`aws_redshift_data_share_consumer_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"

    cfg := redshift.DataShareConsumerAssociationConfig{
        DataShareArn: "example",
        AllowWrites: true,
        AssociateEntireAccount: true,
    }
    ubx.Resource(
        redshift.DataShareConsumerAssociation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DataShareConsumerAssociation } from
      "jsr:@ubx/sdk-aws/aws/redshift/data_share_consumer_association";

    resource(DataShareConsumerAssociation, "example", {
      dataShareArn: "example",
      allowWrites: true,
      associateEntireAccount: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import (
        DataShareConsumerAssociation,
        DataShareConsumerAssociationConfig,
    )

    cfg = DataShareConsumerAssociationConfig(
        data_share_arn="example",
        allow_writes=True,
        associate_entire_account=True,
    )
    ubx.resource(DataShareConsumerAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_data_share_consumer_association with
data_share_arn, allow_writes, associate_entire_account.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_writes" type="bool">
  Optional.
</ResponseField>

<ResponseField name="associate_entire_account" type="bool">
  Optional.
</ResponseField>

<ResponseField name="consumer_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="consumer_region" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_share_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="managed_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="producer_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
