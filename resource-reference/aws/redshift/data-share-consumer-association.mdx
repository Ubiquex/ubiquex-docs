---
title: "aws_redshift_data_share_consumer_association"
description: "Real, generated bindings for `aws_redshift_data_share_consumer_association`."
---

`aws_redshift_data_share_consumer_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data share consumer association"})

    		ubx.Resource(redshift.DataShareConsumerAssociation, "ci-runner", redshift.DataShareConsumerAssociationConfig{
    			DataShareArn:           "arn:aws:iam::123456789012:policy/ci",
    			AllowWrites:            true,
    			AssociateEntireAccount: true,
    			ConsumerArn:            "arn:aws:iam::123456789012:policy/ci",
    			ConsumerRegion:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataShareConsumerAssociation } from "jsr:@ubx/sdk-aws/aws/redshift/data_share_consumer_association";

    export default stack("payments", () => {
      intent({ summary: "payments own data share consumer association" });

      resource(DataShareConsumerAssociation, "ci-runner", {
        dataShareArn: "arn:aws:iam::123456789012:policy/ci",
        allowWrites: true,
        associateEntireAccount: true,
        consumerArn: "arn:aws:iam::123456789012:policy/ci",
        consumerRegion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import DataShareConsumerAssociation, DataShareConsumerAssociationConfig

    def describe():
        ubx.intent("payments own data share consumer association")

        ubx.resource(DataShareConsumerAssociation, "ci-runner", DataShareConsumerAssociationConfig(
            data_share_arn="arn:aws:iam::123456789012:policy/ci",
            allow_writes=True,
            associate_entire_account=True,
            consumer_arn="arn:aws:iam::123456789012:policy/ci",
            consumer_region="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_data_share_consumer_association, with data
    share arn arn:aws:iam::123456789012:policy/ci, allow writes true,
    associate entire account true, consumer arn
    arn:aws:iam::123456789012:policy/ci, consumer region example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_writes" type="bool">
  Optional.
</ResponseField>

<ResponseField name="associate_entire_account" type="bool">
  Optional.
</ResponseField>

<ResponseField name="consumer_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="consumer_region" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_share_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="managed_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="producer_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
