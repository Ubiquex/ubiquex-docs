---
title: "aws_redshift_usage_limit"
description: "Real, generated bindings for `aws_redshift_usage_limit`."
---

`aws_redshift_usage_limit` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own usage limit"})

    		ubx.Resource(redshift.UsageLimit, "ci-runner", redshift.UsageLimitConfig{
    			Amount:            1,
    			ClusterIdentifier: "example",
    			FeatureType:       "example",
    			LimitType:         "example",
    			BreachAction:      "example",
    			Period:            "example",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UsageLimit } from "jsr:@ubx/sdk-aws/aws/redshift/usage_limit";

    export default stack("payments", () => {
      intent({ summary: "payments own usage limit" });

      resource(UsageLimit, "ci-runner", {
        amount: 1,
        clusterIdentifier: "example",
        featureType: "example",
        limitType: "example",
        breachAction: "example",
        period: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import UsageLimit, UsageLimitConfig

    def describe():
        ubx.intent("payments own usage limit")

        ubx.resource(UsageLimit, "ci-runner", UsageLimitConfig(
            amount=1,
            cluster_identifier="example",
            feature_type="example",
            limit_type="example",
            breach_action="example",
            period="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_usage_limit, with amount 1, cluster
    identifier example, feature type example, limit type example,
    breach action example, period example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="amount" type="number" required>
  Required.
</ResponseField>

<ResponseField name="breach_action" type="string">
  Optional.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="feature_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="limit_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="period" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
