---
title: "aws_redshift_integration"
description: "`aws_redshift_integration` creates a real, zero-ETL integration that continuously replicates data from a source such as an RDS or Aurora cluster into..."
---

`aws_redshift_integration` creates a real, zero-ETL integration that continuously replicates data from a source such as an RDS or Aurora cluster into Amazon Redshift, so it can be queried for analytics within seconds of being written.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own integration"})

    		ubx.Resource(redshift.Integration, "example", redshift.IntegrationConfig{
    			SourceArn:                   "arn:aws:iam::123456789012:policy/example",
    			TargetArn:                   "arn:aws:iam::123456789012:policy/example",
    			AdditionalEncryptionContext: "example",
    			IntegrationName:             "example-integration-name",
    			KmskeyId:                    "example",
    			Tags:                        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Integration } from "./local-sdk/aws/sdk/typescript/aws/redshift/integration.ts";

    export default stack("example", () => {
      intent({ summary: "example own integration" });

      resource(Integration, "example", {
        sourceArn: "arn:aws:iam::123456789012:policy/example",
        targetArn: "arn:aws:iam::123456789012:policy/example",
        additionalEncryptionContext: "example",
        integrationName: "example-integration-name",
        kmskeyId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.redshift import Integration, IntegrationConfig

    def describe():
        ubx.intent("example own integration")

        ubx.resource(Integration, "example", IntegrationConfig(
            source_arn="arn:aws:iam::123456789012:policy/example",
            target_arn="arn:aws:iam::123456789012:policy/example",
            additional_encryption_context="example",
            integration_name="example-integration-name",
            kmskey_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_integration, with source arn
    arn:aws:iam::123456789012:policy/example, target arn
    arn:aws:iam::123456789012:policy/example, additional encryption
    context example, integration name example-integration-name,
    kmskey id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="additional_encryption_context" type="any">
  An optional set of non-secret key -- value pairs that contains additional contextual information about the data. Optional.
</ResponseField>

<ResponseField name="integration_name" type="string">
  The name of the integration. Optional.
</ResponseField>

<ResponseField name="kmskey_id" type="string">
  An KMS key identifier for the key to use to encrypt the integration. If you don't specify an encryption key, the default AWS owned KMS key is used. Optional.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  The Amazon Resource Name (ARN) of the database to use as the source for replication Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key portion of a user-defined tag attached to the AWS Redshift Integration resource, used for organization, cost tracking, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  The Amazon Resource Name (ARN) of the Redshift data warehouse to use as the target for replication Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time (UTC) when the integration was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="integration_arn" type="string">
  The Amazon Resource Name (ARN) of the integration. Computed by AWS after `ship`.
</ResponseField>
