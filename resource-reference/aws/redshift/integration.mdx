---
title: "aws_redshift_integration"
description: "Real, generated bindings for `aws_redshift_integration`."
---

`aws_redshift_integration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own integration"})

    		ubx.Resource(redshift.Integration, "ci-runner", redshift.IntegrationConfig{
    			IntegrationName:             "example-integration-name",
    			SourceArn:                   "arn:aws:iam::123456789012:policy/ci",
    			TargetArn:                   "arn:aws:iam::123456789012:policy/ci",
    			AdditionalEncryptionContext: map[string]string{"managed-by": "ubx"},
    			Description:                 "Managed by ubx.",
    			Tags:                        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Integration } from "jsr:@ubx/sdk-aws/aws/redshift/integration";

    export default stack("payments", () => {
      intent({ summary: "payments own integration" });

      resource(Integration, "ci-runner", {
        integrationName: "example-integration-name",
        sourceArn: "arn:aws:iam::123456789012:policy/ci",
        targetArn: "arn:aws:iam::123456789012:policy/ci",
        additionalEncryptionContext: { "managed-by": "ubx" },
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import Integration, IntegrationConfig

    def describe():
        ubx.intent("payments own integration")

        ubx.resource(Integration, "ci-runner", IntegrationConfig(
            integration_name="example-integration-name",
            source_arn="arn:aws:iam::123456789012:policy/ci",
            target_arn="arn:aws:iam::123456789012:policy/ci",
            additional_encryption_context={"managed-by": "ubx"},
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_integration, with integration name
    example-integration-name, source arn
    arn:aws:iam::123456789012:policy/ci, target arn
    arn:aws:iam::123456789012:policy/ci, additional encryption
    context a set value, description Managed by ubx., tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="integration_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
