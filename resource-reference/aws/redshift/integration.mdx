---
title: "aws_redshift_integration"
description: "Real, generated bindings for `aws_redshift_integration`."
---

`aws_redshift_integration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"

    cfg := redshift.IntegrationConfig{
        IntegrationName: "example-integration-name",
        SourceArn: "example",
        TargetArn: "example",
        AdditionalEncryptionContext: map[string]string{"key": "value"},
        Description: "example",
    }
    ubx.Resource(redshift.Integration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Integration } from
      "jsr:@ubx/sdk-aws/aws/redshift/integration";

    resource(Integration, "example", {
      integrationName: "example",
      sourceArn: "example",
      targetArn: "example",
      additionalEncryptionContext: { key: "value" },
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import Integration, IntegrationConfig

    cfg = IntegrationConfig(
        integration_name="example",
        source_arn="example",
        target_arn="example",
        additional_encryption_context={"key": "value"},
        description="example",
    )
    ubx.resource(Integration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_integration with integration_name,
source_arn, target_arn, additional_encryption_context,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="integration_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
