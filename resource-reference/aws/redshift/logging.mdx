---
title: "aws_redshift_logging"
description: "Real, generated bindings for `aws_redshift_logging`."
---

`aws_redshift_logging` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own logging"})

    		ubx.Resource(redshift.Logging, "ci-runner", redshift.LoggingConfig{
    			ClusterIdentifier:  "example",
    			BucketName:         "example-bucket-name",
    			LogDestinationType: "example",
    			LogExports:         []string{"example"},
    			S3KeyPrefix:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Logging } from "jsr:@ubx/sdk-aws/aws/redshift/logging";

    export default stack("payments", () => {
      intent({ summary: "payments own logging" });

      resource(Logging, "ci-runner", {
        clusterIdentifier: "example",
        bucketName: "example-bucket-name",
        logDestinationType: "example",
        logExports: ["example"],
        s3KeyPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import Logging, LoggingConfig

    def describe():
        ubx.intent("payments own logging")

        ubx.resource(Logging, "ci-runner", LoggingConfig(
            cluster_identifier="example",
            bucket_name="example-bucket-name",
            log_destination_type="example",
            log_exports=["example"],
            s3_key_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_logging, with cluster identifier example,
    bucket name example-bucket-name, log destination type example,
    log exports a set value, s3 key prefix example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="log_destination_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="log_exports" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
