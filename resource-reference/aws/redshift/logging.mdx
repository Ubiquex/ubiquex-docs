---
title: "aws_redshift_logging"
description: "Real, generated bindings for `aws_redshift_logging`."
---

`aws_redshift_logging` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"

    cfg := redshift.LoggingConfig{
        ClusterIdentifier: "example",
        BucketName: "example-bucket-name",
        LogDestinationType: "example",
    }
    ubx.Resource(redshift.Logging, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Logging } from "jsr:@ubx/sdk-aws/aws/redshift/logging";

    resource(Logging, "example", {
      clusterIdentifier: "example",
      bucketName: "example",
      logDestinationType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import Logging, LoggingConfig

    cfg = LoggingConfig(
        cluster_identifier="example",
        bucket_name="example",
        log_destination_type="example",
    )
    ubx.resource(Logging, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_logging with cluster_identifier,
bucket_name, log_destination_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="log_destination_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="log_exports" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
