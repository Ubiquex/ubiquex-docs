---
title: "aws_redshift_snapshot_copy"
description: "Real, generated bindings for `aws_redshift_snapshot_copy`."
---

`aws_redshift_snapshot_copy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/redshift"

    cfg := redshift.SnapshotCopyConfig{
        ClusterIdentifier: "example",
        DestinationRegion: "example",
        ManualSnapshotRetentionPeriod: 1,
        RetentionPeriod: 1,
    }
    ubx.Resource(redshift.SnapshotCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SnapshotCopy } from
      "jsr:@ubx/sdk-aws/aws/redshift/snapshot_copy";

    resource(SnapshotCopy, "example", {
      clusterIdentifier: "example",
      destinationRegion: "example",
      manualSnapshotRetentionPeriod: 1,
      retentionPeriod: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import SnapshotCopy, SnapshotCopyConfig

    cfg = SnapshotCopyConfig(
        cluster_identifier="example",
        destination_region="example",
        manual_snapshot_retention_period=1,
        retention_period=1,
    )
    ubx.resource(SnapshotCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_snapshot_copy with cluster_identifier,
destination_region, manual_snapshot_retention_period,
retention_period.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="manual_snapshot_retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_copy_grant_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="manual_snapshot_retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>
