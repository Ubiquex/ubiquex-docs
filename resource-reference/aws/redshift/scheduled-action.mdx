---
title: "aws_redshift_scheduled_action"
description: "Real, generated bindings for `aws_redshift_scheduled_action`."
---

`aws_redshift_scheduled_action` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/redshift"

    cfg := redshift.ScheduledActionConfig{
        IamRole: "example",
        Name: "example-name",
        Schedule: "example",
        Description: "example",
        Enable: true,
    }
    ubx.Resource(redshift.ScheduledAction, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ScheduledAction } from
      "jsr:@ubx/sdk-aws/aws/redshift/scheduled_action";

    resource(ScheduledAction, "example", {
      iamRole: "example",
      name: "example",
      schedule: "example",
      description: "example",
      enable: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import (
        ScheduledAction,
        ScheduledActionConfig,
    )

    cfg = ScheduledActionConfig(
        iam_role="example",
        name="example",
        schedule="example",
        description="example",
        enable=True,
    )
    ubx.resource(ScheduledAction, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_scheduled_action with iam_role, name,
schedule, description, enable.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enable" type="bool">
  Optional.
</ResponseField>

<ResponseField name="end_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="iam_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule" type="string" required>
  Required.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
