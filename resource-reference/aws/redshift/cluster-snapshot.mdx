---
title: "aws_redshift_cluster_snapshot"
description: "Real, generated bindings for `aws_redshift_cluster_snapshot`."
---

`aws_redshift_cluster_snapshot` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster snapshot"})

    		ubx.Resource(redshift.ClusterSnapshot, "ci-runner", redshift.ClusterSnapshotConfig{
    			ClusterIdentifier:             "example",
    			SnapshotIdentifier:            "example",
    			ManualSnapshotRetentionPeriod: 1,
    			Tags:                          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterSnapshot } from "jsr:@ubx/sdk-aws/aws/redshift/cluster_snapshot";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster snapshot" });

      resource(ClusterSnapshot, "ci-runner", {
        clusterIdentifier: "example",
        snapshotIdentifier: "example",
        manualSnapshotRetentionPeriod: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import ClusterSnapshot, ClusterSnapshotConfig

    def describe():
        ubx.intent("payments own cluster snapshot")

        ubx.resource(ClusterSnapshot, "ci-runner", ClusterSnapshotConfig(
            cluster_identifier="example",
            snapshot_identifier="example",
            manual_snapshot_retention_period=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_cluster_snapshot, with cluster identifier
    example, snapshot identifier example, manual snapshot retention
    period 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="manual_snapshot_retention_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
