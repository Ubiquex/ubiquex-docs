---
title: "aws_redshift_authentication_profile"
description: "Real, generated bindings for `aws_redshift_authentication_profile`."
---

`aws_redshift_authentication_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"

    cfg := redshift.AuthenticationProfileConfig{
        AuthenticationProfileContent: "example",
        AuthenticationProfileName: "example-authentication-profile-name",
    }
    ubx.Resource(redshift.AuthenticationProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AuthenticationProfile } from
      "jsr:@ubx/sdk-aws/aws/redshift/authentication_profile";

    resource(AuthenticationProfile, "example", {
      authenticationProfileContent: "example",
      authenticationProfileName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import (
        AuthenticationProfile,
        AuthenticationProfileConfig,
    )

    cfg = AuthenticationProfileConfig(
        authentication_profile_content="example",
        authentication_profile_name="example",
    )
    ubx.resource(AuthenticationProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_authentication_profile with
authentication_profile_content, authentication_profile_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication_profile_content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authentication_profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
