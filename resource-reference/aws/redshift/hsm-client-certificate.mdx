---
title: "aws_redshift_hsm_client_certificate"
description: "Real, generated bindings for `aws_redshift_hsm_client_certificate`."
---

`aws_redshift_hsm_client_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hsm client certificate"})

    		ubx.Resource(redshift.HsmClientCertificate, "ci-runner", redshift.HsmClientCertificateConfig{
    			HsmClientCertificateIdentifier: "example",
    			Tags:                           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HsmClientCertificate } from "jsr:@ubx/sdk-aws/aws/redshift/hsm_client_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own hsm client certificate" });

      resource(HsmClientCertificate, "ci-runner", {
        hsmClientCertificateIdentifier: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import HsmClientCertificate, HsmClientCertificateConfig

    def describe():
        ubx.intent("payments own hsm client certificate")

        ubx.resource(HsmClientCertificate, "ci-runner", HsmClientCertificateConfig(
            hsm_client_certificate_identifier="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_hsm_client_certificate, with hsm client
    certificate identifier example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="hsm_client_certificate_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hsm_client_certificate_public_key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
