---
title: "aws_redshift_endpoint_authorization"
description: "Real, generated bindings for `aws_redshift_endpoint_authorization`."
---

`aws_redshift_endpoint_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint authorization"})

    		ubx.Resource(redshift.EndpointAuthorization, "ci-runner", redshift.EndpointAuthorizationConfig{
    			Account:           "example",
    			ClusterIdentifier: "example",
    			ForceDelete:       true,
    			VpcIds:            []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointAuthorization } from "jsr:@ubx/sdk-aws/aws/redshift/endpoint_authorization";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint authorization" });

      resource(EndpointAuthorization, "ci-runner", {
        account: "example",
        clusterIdentifier: "example",
        forceDelete: true,
        vpcIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import EndpointAuthorization, EndpointAuthorizationConfig

    def describe():
        ubx.intent("payments own endpoint authorization")

        ubx.resource(EndpointAuthorization, "ci-runner", EndpointAuthorizationConfig(
            account="example",
            cluster_identifier="example",
            force_delete=True,
            vpc_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_endpoint_authorization, with account example,
    cluster identifier example, force delete true, vpc ids a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_delete" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_ids" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="allowed_all_vpcs" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grantee" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grantor" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
