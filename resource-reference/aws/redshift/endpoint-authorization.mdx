---
title: "aws_redshift_endpoint_authorization"
description: "Real, generated bindings for `aws_redshift_endpoint_authorization`."
---

`aws_redshift_endpoint_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"

    cfg := redshift.EndpointAuthorizationConfig{
        Account: "example",
        ClusterIdentifier: "example",
        ForceDelete: true,
        VpcIds: []string{"example"},
    }
    ubx.Resource(redshift.EndpointAuthorization, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointAuthorization } from
      "jsr:@ubx/sdk-aws/aws/redshift/endpoint_authorization";

    resource(EndpointAuthorization, "example", {
      account: "example",
      clusterIdentifier: "example",
      forceDelete: true,
      vpcIds: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.redshift import (
        EndpointAuthorization,
        EndpointAuthorizationConfig,
    )

    cfg = EndpointAuthorizationConfig(
        account="example",
        cluster_identifier="example",
        force_delete=True,
        vpc_ids=["example"],
    )
    ubx.resource(EndpointAuthorization, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_redshift_endpoint_authorization with account,
cluster_identifier, force_delete, vpc_ids.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_delete" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_ids" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="allowed_all_vpcs" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grantee" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grantor" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
