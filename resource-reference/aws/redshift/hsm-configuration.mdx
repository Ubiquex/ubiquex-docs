---
title: "aws_redshift_hsm_configuration"
description: "Real, generated bindings for `aws_redshift_hsm_configuration`."
---

`aws_redshift_hsm_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hsm configuration"})

    		ubx.Resource(redshift.HsmConfiguration, "ci-runner", redshift.HsmConfigurationConfig{
    			Description:                "Managed by ubx.",
    			HsmConfigurationIdentifier: "example",
    			HsmIpAddress:               "example",
    			HsmPartitionName:           "example-hsm-partition-name",
    			HsmPartitionPassword:       "example",
    			HsmServerPublicCertificate: "example",
    			Tags:                       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HsmConfiguration } from "jsr:@ubx/sdk-aws/aws/redshift/hsm_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own hsm configuration" });

      resource(HsmConfiguration, "ci-runner", {
        description: "Managed by ubx.",
        hsmConfigurationIdentifier: "example",
        hsmIpAddress: "example",
        hsmPartitionName: "example-hsm-partition-name",
        hsmPartitionPassword: "example",
        hsmServerPublicCertificate: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import HsmConfiguration, HsmConfigurationConfig

    def describe():
        ubx.intent("payments own hsm configuration")

        ubx.resource(HsmConfiguration, "ci-runner", HsmConfigurationConfig(
            description="Managed by ubx.",
            hsm_configuration_identifier="example",
            hsm_ip_address="example",
            hsm_partition_name="example-hsm-partition-name",
            hsm_partition_password="example",
            hsm_server_public_certificate="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_hsm_configuration, with description Managed
    by ubx., hsm configuration identifier example, hsm ip address
    example, hsm partition name example-hsm-partition-name, hsm
    partition password example, hsm server public certificate
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hsm_configuration_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hsm_ip_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hsm_partition_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hsm_partition_password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="hsm_server_public_certificate" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
