---
title: "aws_redshift_event_subscription"
description: "Real, generated bindings for `aws_redshift_event_subscription`."
---

`aws_redshift_event_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event subscription"})

    		ubx.Resource(redshift.EventSubscription, "ci-runner", redshift.EventSubscriptionConfig{
    			Name:            "ci-runner",
    			SnsTopicArn:     "arn:aws:iam::123456789012:policy/ci",
    			Enabled:         true,
    			EventCategories: []string{"example"},
    			Severity:        "example",
    			SourceIds:       []string{"example"},
    			SourceType:      "example",
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventSubscription } from "jsr:@ubx/sdk-aws/aws/redshift/event_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own event subscription" });

      resource(EventSubscription, "ci-runner", {
        name: "ci-runner",
        snsTopicArn: "arn:aws:iam::123456789012:policy/ci",
        enabled: true,
        eventCategories: ["example"],
        severity: "example",
        sourceIds: ["example"],
        sourceType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import EventSubscription, EventSubscriptionConfig

    def describe():
        ubx.intent("payments own event subscription")

        ubx.resource(EventSubscription, "ci-runner", EventSubscriptionConfig(
            name="ci-runner",
            sns_topic_arn="arn:aws:iam::123456789012:policy/ci",
            enabled=True,
            event_categories=["example"],
            severity="example",
            source_ids=["example"],
            source_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_event_subscription called "ci-runner", with
    sns topic arn arn:aws:iam::123456789012:policy/ci, enabled true,
    event categories a set value, severity example, source ids a set
    value, source type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="event_categories" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="severity" type="string">
  Optional.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="source_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="customer_aws_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
