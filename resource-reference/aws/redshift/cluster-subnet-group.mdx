---
title: "aws_redshift_cluster_subnet_group"
description: "`aws_redshift_cluster_subnet_group` defines a real set of VPC subnets that an `aws_redshift_cluster` is allowed to be placed into."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster subnet group"})

    		ubx.Resource(redshift.ClusterSubnetGroup, "ci-runner", redshift.ClusterSubnetGroupConfig{
    			Description: "Managed by ubx.",
    			SubnetIds:   []string{"example"},
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterSubnetGroup } from "./local-sdk/aws/sdk/typescript/aws/redshift/cluster_subnet_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster subnet group" });

      resource(ClusterSubnetGroup, "ci-runner", {
        description: "Managed by ubx.",
        subnetIds: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.redshift import ClusterSubnetGroup, ClusterSubnetGroupConfig

    def describe():
        ubx.intent("payments own cluster subnet group")

        ubx.resource(ClusterSubnetGroup, "ci-runner", ClusterSubnetGroupConfig(
            description="Managed by ubx.",
            subnet_ids=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_cluster_subnet_group, with description
    Managed by ubx., subnet ids a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string" required>
  The description of the parameter group. Required.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  The list of VPC subnet IDs Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The list of tags for the cluster parameter group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag applied to the Redshift cluster subnet group, used for identifying and organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="cluster_subnet_group_name" type="string">
  This name must be unique for all subnet groups that are created by your AWS account. If costumer do not provide it, cloudformation will generate it. Must not be "Default". Computed by AWS after `ship`.
</ResponseField>
