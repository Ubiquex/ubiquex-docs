---
title: "aws_redshift_namespace_registration"
description: "Real, generated bindings for `aws_redshift_namespace_registration`."
---

`aws_redshift_namespace_registration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own namespace registration"})

    		ubx.Resource(redshift.NamespaceRegistration, "ci-runner", redshift.NamespaceRegistrationConfig{
    			ConsumerIdentifier:            "example",
    			NamespaceType:                 "example",
    			ProvisionedClusterIdentifier:  "example",
    			ServerlessNamespaceIdentifier: "example",
    			ServerlessWorkgroupIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NamespaceRegistration } from "jsr:@ubx/sdk-aws/aws/redshift/namespace_registration";

    export default stack("payments", () => {
      intent({ summary: "payments own namespace registration" });

      resource(NamespaceRegistration, "ci-runner", {
        consumerIdentifier: "example",
        namespaceType: "example",
        provisionedClusterIdentifier: "example",
        serverlessNamespaceIdentifier: "example",
        serverlessWorkgroupIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import NamespaceRegistration, NamespaceRegistrationConfig

    def describe():
        ubx.intent("payments own namespace registration")

        ubx.resource(NamespaceRegistration, "ci-runner", NamespaceRegistrationConfig(
            consumer_identifier="example",
            namespace_type="example",
            provisioned_cluster_identifier="example",
            serverless_namespace_identifier="example",
            serverless_workgroup_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_namespace_registration, with consumer
    identifier example, namespace type example, provisioned cluster
    identifier example, serverless namespace identifier example,
    serverless workgroup identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="consumer_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="provisioned_cluster_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="serverless_namespace_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="serverless_workgroup_identifier" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
