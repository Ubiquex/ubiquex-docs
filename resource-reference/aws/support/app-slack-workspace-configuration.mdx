---
title: "aws_support_app_slack_workspace_configuration"
description: "`aws_support_app_slack_workspace_configuration` authorizes a real Slack workspace for the AWS Support App..."
---

`aws_support_app_slack_workspace_configuration` authorizes a real Slack workspace for the AWS Support App, the prerequisite step before any `aws_support_app_slack_channel_configuration` within that workspace can be created.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	support "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/support"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own app slack workspace configuration"})

    		ubx.Resource(support.AppSlackWorkspaceConfiguration, "example", support.AppSlackWorkspaceConfigurationConfig{
    			TeamId:    "example",
    			VersionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppSlackWorkspaceConfiguration } from "@ubx/sdk-aws/aws/support/app_slack_workspace_configuration";

    export default stack("example", () => {
      intent({ summary: "example own app slack workspace configuration" });

      resource(AppSlackWorkspaceConfiguration, "example", {
        teamId: "example",
        versionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.support import AppSlackWorkspaceConfiguration, AppSlackWorkspaceConfigurationConfig

    def describe():
        ubx.intent("example own app slack workspace configuration")

        ubx.resource(AppSlackWorkspaceConfiguration, "example", AppSlackWorkspaceConfigurationConfig(
            team_id="example",
            version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_support_app_slack_workspace_configuration, with team
    id example, version id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="team_id" type="string" required>
  The team ID in Slack, which uniquely identifies a workspace. Required.
</ResponseField>

<ResponseField name="version_id" type="string">
  An identifier used to update an existing Slack workspace configuration in AWS CloudFormation. Optional.
</ResponseField>
