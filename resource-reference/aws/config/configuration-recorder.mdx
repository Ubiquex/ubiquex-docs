---
title: "aws_config_configuration_recorder"
description: "Real, generated bindings for `aws_config_configuration_recorder`."
---

`aws_config_configuration_recorder` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own configuration recorder"})

    		ubx.Resource(config.ConfigurationRecorder, "ci-runner", config.ConfigurationRecorderConfig{
    			RoleArn: "arn:aws:iam::123456789012:policy/ci",
    			Name:    "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationRecorder } from "jsr:@ubx/sdk-aws/aws/config/configuration_recorder";

    export default stack("payments", () => {
      intent({ summary: "payments own configuration recorder" });

      resource(ConfigurationRecorder, "ci-runner", {
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import ConfigurationRecorder, ConfigurationRecorderConfig

    def describe():
        ubx.intent("payments own configuration recorder")

        ubx.resource(ConfigurationRecorder, "ci-runner", ConfigurationRecorderConfig(
            role_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_configuration_recorder called "ci-runner", with
    role arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
