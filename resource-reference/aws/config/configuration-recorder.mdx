---
title: "aws_config_configuration_recorder"
description: "Real, generated bindings for `aws_config_configuration_recorder`."
---

`aws_config_configuration_recorder` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"

    cfg := config.ConfigurationRecorderConfig{
        RoleArn: "example",
        Name: "example-name",
    }
    ubx.Resource(config.ConfigurationRecorder, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConfigurationRecorder } from
      "jsr:@ubx/sdk-aws/aws/config/configuration_recorder";

    resource(ConfigurationRecorder, "example", {
      roleArn: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.config import (
        ConfigurationRecorder,
        ConfigurationRecorderConfig,
    )

    cfg = ConfigurationRecorderConfig(
        role_arn="example",
        name="example",
    )
    ubx.resource(ConfigurationRecorder, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_config_configuration_recorder with role_arn, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
