---
title: "aws_config_configuration_recorder_status"
description: "Real, generated bindings for `aws_config_configuration_recorder_status`."
---

`aws_config_configuration_recorder_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/config"

    cfg := config.ConfigurationRecorderStatusConfig{
        IsEnabled: true,
        Name: "example-name",
    }
    ubx.Resource(config.ConfigurationRecorderStatus, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConfigurationRecorderStatus } from
      "jsr:@ubx/sdk-aws/aws/config/configuration_recorder_status";

    resource(ConfigurationRecorderStatus, "example", {
      isEnabled: true,
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.config import (
        ConfigurationRecorderStatus,
        ConfigurationRecorderStatusConfig,
    )

    cfg = ConfigurationRecorderStatusConfig(
        is_enabled=True,
        name="example",
    )
    ubx.resource(ConfigurationRecorderStatus, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_config_configuration_recorder_status with
is_enabled, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
