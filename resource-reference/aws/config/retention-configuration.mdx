---
title: "aws_config_retention_configuration"
description: "Real, generated bindings for `aws_config_retention_configuration`."
---

`aws_config_retention_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"

    cfg := config.RetentionConfigurationConfig{
        RetentionPeriodInDays: 1,
    }
    ubx.Resource(config.RetentionConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RetentionConfiguration } from
      "jsr:@ubx/sdk-aws/aws/config/retention_configuration";

    resource(RetentionConfiguration, "example", {
      retentionPeriodInDays: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.config import (
        RetentionConfiguration,
        RetentionConfigurationConfig,
    )

    cfg = RetentionConfigurationConfig(
        retention_period_in_days=1,
    )
    ubx.resource(RetentionConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_config_retention_configuration with
retention_period_in_days.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period_in_days" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
