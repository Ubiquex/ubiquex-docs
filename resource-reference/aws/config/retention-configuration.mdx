---
title: "aws_config_retention_configuration"
description: "Real, generated bindings for `aws_config_retention_configuration`."
---

`aws_config_retention_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own retention configuration"})

    		ubx.Resource(config.RetentionConfiguration, "ci-runner", config.RetentionConfigurationConfig{
    			RetentionPeriodInDays: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RetentionConfiguration } from "jsr:@ubx/sdk-aws/aws/config/retention_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own retention configuration" });

      resource(RetentionConfiguration, "ci-runner", {
        retentionPeriodInDays: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import RetentionConfiguration, RetentionConfigurationConfig

    def describe():
        ubx.intent("payments own retention configuration")

        ubx.resource(RetentionConfiguration, "ci-runner", RetentionConfigurationConfig(
            retention_period_in_days=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_retention_configuration, with retention period
    in days 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period_in_days" type="number" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
