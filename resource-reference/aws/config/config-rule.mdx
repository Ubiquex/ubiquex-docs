---
title: "aws_config_config_rule"
description: "Real, generated bindings for `aws_config_config_rule`."
---

`aws_config_config_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own config rule"})

    		ubx.Resource(config.ConfigRule, "ci-runner", config.ConfigRuleConfig{
    			Name:                      "ci-runner",
    			Description:               "Managed by ubx.",
    			InputParameters:           "example",
    			MaximumExecutionFrequency: "example",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigRule } from "jsr:@ubx/sdk-aws/aws/config/config_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own config rule" });

      resource(ConfigRule, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        inputParameters: "example",
        maximumExecutionFrequency: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import ConfigRule, ConfigRuleConfig

    def describe():
        ubx.intent("payments own config rule")

        ubx.resource(ConfigRule, "ci-runner", ConfigRuleConfig(
            name="ci-runner",
            description="Managed by ubx.",
            input_parameters="example",
            maximum_execution_frequency="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_config_rule called "ci-runner", with
    description Managed by ubx., input parameters example, maximum
    execution frequency example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="input_parameters" type="string">
  Optional.
</ResponseField>

<ResponseField name="maximum_execution_frequency" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
