---
title: "aws_config_configuration_aggregator"
description: "Real, generated bindings for `aws_config_configuration_aggregator`."
---

`aws_config_configuration_aggregator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/config"

    cfg := config.ConfigurationAggregatorConfig{
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(config.ConfigurationAggregator, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConfigurationAggregator } from
      "jsr:@ubx/sdk-aws/aws/config/configuration_aggregator";

    resource(ConfigurationAggregator, "example", {
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.config import (
        ConfigurationAggregator,
        ConfigurationAggregatorConfig,
    )

    cfg = ConfigurationAggregatorConfig(
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(ConfigurationAggregator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_config_configuration_aggregator with name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
