---
title: "aws_config_aggregate_authorization"
description: "Real, generated bindings for `aws_config_aggregate_authorization`."
---

`aws_config_aggregate_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own aggregate authorization"})

    		ubx.Resource(config.AggregateAuthorization, "ci-runner", config.AggregateAuthorizationConfig{
    			AccountId:           "example",
    			AuthorizedAwsRegion: "example",
    			Region:              "example",
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AggregateAuthorization } from "jsr:@ubx/sdk-aws/aws/config/aggregate_authorization";

    export default stack("payments", () => {
      intent({ summary: "payments own aggregate authorization" });

      resource(AggregateAuthorization, "ci-runner", {
        accountId: "example",
        authorizedAwsRegion: "example",
        region: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import AggregateAuthorization, AggregateAuthorizationConfig

    def describe():
        ubx.intent("payments own aggregate authorization")

        ubx.resource(AggregateAuthorization, "ci-runner", AggregateAuthorizationConfig(
            account_id="example",
            authorized_aws_region="example",
            region="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_aggregate_authorization, with account id
    example, authorized aws region example, region example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authorized_aws_region" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
