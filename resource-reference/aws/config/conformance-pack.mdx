---
title: "aws_config_conformance_pack"
description: "Real, generated bindings for `aws_config_conformance_pack`."
---

`aws_config_conformance_pack` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own conformance pack"})

    		ubx.Resource(config.ConformancePack, "ci-runner", config.ConformancePackConfig{
    			Name:                "ci-runner",
    			DeliveryS3Bucket:    "example",
    			DeliveryS3KeyPrefix: "example",
    			TemplateBody:        "example",
    			TemplateS3Uri:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConformancePack } from "jsr:@ubx/sdk-aws/aws/config/conformance_pack";

    export default stack("payments", () => {
      intent({ summary: "payments own conformance pack" });

      resource(ConformancePack, "ci-runner", {
        name: "ci-runner",
        deliveryS3Bucket: "example",
        deliveryS3KeyPrefix: "example",
        templateBody: "example",
        templateS3Uri: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import ConformancePack, ConformancePackConfig

    def describe():
        ubx.intent("payments own conformance pack")

        ubx.resource(ConformancePack, "ci-runner", ConformancePackConfig(
            name="ci-runner",
            delivery_s3_bucket="example",
            delivery_s3_key_prefix="example",
            template_body="example",
            template_s3_uri="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_conformance_pack called "ci-runner", with
    delivery s3 bucket example, delivery s3 key prefix example,
    template body example, template s3 uri example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delivery_s3_bucket" type="string">
  Optional.
</ResponseField>

<ResponseField name="delivery_s3_key_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_body" type="string">
  Optional.
</ResponseField>

<ResponseField name="template_s3_uri" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
