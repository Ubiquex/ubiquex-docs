---
title: "aws_config_delivery_channel"
description: "Real, generated bindings for `aws_config_delivery_channel`."
---

`aws_config_delivery_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"

    cfg := config.DeliveryChannelConfig{
        S3BucketName: "example-s3-bucket-name",
        Name: "example-name",
        S3KeyPrefix: "example",
    }
    ubx.Resource(config.DeliveryChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DeliveryChannel } from
      "jsr:@ubx/sdk-aws/aws/config/delivery_channel";

    resource(DeliveryChannel, "example", {
      s3BucketName: "example",
      name: "example",
      s3KeyPrefix: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.config import (
        DeliveryChannel,
        DeliveryChannelConfig,
    )

    cfg = DeliveryChannelConfig(
        s3_bucket_name="example",
        name="example",
        s3_key_prefix="example",
    )
    ubx.resource(DeliveryChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_config_delivery_channel with s3_bucket_name, name,
s3_key_prefix.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
