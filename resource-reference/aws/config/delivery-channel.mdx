---
title: "aws_config_delivery_channel"
description: "Real, generated bindings for `aws_config_delivery_channel`."
---

`aws_config_delivery_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own delivery channel"})

    		ubx.Resource(config.DeliveryChannel, "ci-runner", config.DeliveryChannelConfig{
    			S3BucketName: "example-s3-bucket-name",
    			Name:         "ci-runner",
    			S3KeyPrefix:  "example",
    			S3KmsKeyArn:  "arn:aws:iam::123456789012:policy/ci",
    			SnsTopicArn:  "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeliveryChannel } from "jsr:@ubx/sdk-aws/aws/config/delivery_channel";

    export default stack("payments", () => {
      intent({ summary: "payments own delivery channel" });

      resource(DeliveryChannel, "ci-runner", {
        s3BucketName: "example-s3-bucket-name",
        name: "ci-runner",
        s3KeyPrefix: "example",
        s3KmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
        snsTopicArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import DeliveryChannel, DeliveryChannelConfig

    def describe():
        ubx.intent("payments own delivery channel")

        ubx.resource(DeliveryChannel, "ci-runner", DeliveryChannelConfig(
            s3_bucket_name="example-s3-bucket-name",
            name="ci-runner",
            s3_key_prefix="example",
            s3_kms_key_arn="arn:aws:iam::123456789012:policy/ci",
            sns_topic_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_delivery_channel called "ci-runner", with s3
    bucket name example-s3-bucket-name, s3 key prefix example, s3 kms
    key arn arn:aws:iam::123456789012:policy/ci, sns topic arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_key_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
