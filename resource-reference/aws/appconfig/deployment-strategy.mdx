---
title: "aws_appconfig_deployment_strategy"
description: "Real, generated bindings for `aws_appconfig_deployment_strategy`."
---

`aws_appconfig_deployment_strategy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appconfig"

    cfg := appconfig.DeploymentStrategyConfig{
        DeploymentDurationInMinutes: 1,
        GrowthFactor: 1,
        Name: "example-name",
        ReplicateTo: "example",
        Description: "example",
        FinalBakeTimeInMinutes: 1,
    }
    ubx.Resource(appconfig.DeploymentStrategy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DeploymentStrategy } from
      "jsr:@ubx/sdk-aws/aws/appconfig/deployment_strategy";

    resource(DeploymentStrategy, "example", {
      deploymentDurationInMinutes: 1,
      growthFactor: 1,
      name: "example",
      replicateTo: "example",
      description: "example",
      finalBakeTimeInMinutes: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appconfig import (
        DeploymentStrategy,
        DeploymentStrategyConfig,
    )

    cfg = DeploymentStrategyConfig(
        deployment_duration_in_minutes=1,
        growth_factor=1,
        name="example",
        replicate_to="example",
        description="example",
        final_bake_time_in_minutes=1,
    )
    ubx.resource(DeploymentStrategy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appconfig_deployment_strategy with
deployment_duration_in_minutes, growth_factor, name,
replicate_to, description, final_bake_time_in_minutes.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deployment_duration_in_minutes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="final_bake_time_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="growth_factor" type="number" required>
  Required.
</ResponseField>

<ResponseField name="growth_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replicate_to" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
