---
title: "aws_appconfig_deployment_strategy"
description: "Real, generated bindings for `aws_appconfig_deployment_strategy`."
---

`aws_appconfig_deployment_strategy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own deployment strategy"})

    		ubx.Resource(appconfig.DeploymentStrategy, "ci-runner", appconfig.DeploymentStrategyConfig{
    			DeploymentDurationInMinutes: 7200,
    			GrowthFactor:                1,
    			Name:                        "ci-runner",
    			ReplicateTo:                 "example",
    			Description:                 "Managed by ubx.",
    			FinalBakeTimeInMinutes:      1,
    			GrowthType:                  "example",
    			Tags:                        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DeploymentStrategy } from "jsr:@ubx/sdk-aws/aws/appconfig/deployment_strategy";

    export default stack("payments", () => {
      intent({ summary: "payments own deployment strategy" });

      resource(DeploymentStrategy, "ci-runner", {
        deploymentDurationInMinutes: 7200,
        growthFactor: 1,
        name: "ci-runner",
        replicateTo: "example",
        description: "Managed by ubx.",
        finalBakeTimeInMinutes: 1,
        growthType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appconfig import DeploymentStrategy, DeploymentStrategyConfig

    def describe():
        ubx.intent("payments own deployment strategy")

        ubx.resource(DeploymentStrategy, "ci-runner", DeploymentStrategyConfig(
            deployment_duration_in_minutes=7200,
            growth_factor=1,
            name="ci-runner",
            replicate_to="example",
            description="Managed by ubx.",
            final_bake_time_in_minutes=1,
            growth_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appconfig_deployment_strategy called "ci-runner", with
    deployment duration in minutes 7200, growth factor 1, replicate
    to example, description Managed by ubx., final bake time in
    minutes 1, growth type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deployment_duration_in_minutes" type="number" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="final_bake_time_in_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="growth_factor" type="number" required>
  Required.
</ResponseField>

<ResponseField name="growth_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replicate_to" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
