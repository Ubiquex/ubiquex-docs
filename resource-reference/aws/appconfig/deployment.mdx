---
title: "aws_appconfig_deployment"
description: "Real, generated bindings for `aws_appconfig_deployment`."
---

`aws_appconfig_deployment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own deployment"})

    		ubx.Resource(appconfig.Deployment, "ci-runner", appconfig.DeploymentConfig{
    			ApplicationId:          "example",
    			ConfigurationProfileId: "example",
    			ConfigurationVersion:   "example",
    			DeploymentStrategyId:   "example",
    			EnvironmentId:          "example",
    			Description:            "Managed by ubx.",
    			KmsKeyIdentifier:       "example",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Deployment } from "jsr:@ubx/sdk-aws/aws/appconfig/deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own deployment" });

      resource(Deployment, "ci-runner", {
        applicationId: "example",
        configurationProfileId: "example",
        configurationVersion: "example",
        deploymentStrategyId: "example",
        environmentId: "example",
        description: "Managed by ubx.",
        kmsKeyIdentifier: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appconfig import Deployment, DeploymentConfig

    def describe():
        ubx.intent("payments own deployment")

        ubx.resource(Deployment, "ci-runner", DeploymentConfig(
            application_id="example",
            configuration_profile_id="example",
            configuration_version="example",
            deployment_strategy_id="example",
            environment_id="example",
            description="Managed by ubx.",
            kms_key_identifier="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appconfig_deployment, with application id example,
    configuration profile id example, configuration version example,
    deployment strategy id example, environment id example,
    description Managed by ubx., kms key identifier example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configuration_profile_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configuration_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deployment_strategy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="environment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deployment_number" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
