---
title: "aws_image_builder_distribution_configuration"
description: "`aws_image_builder_distribution_configuration` defines which real Regions and accounts an `aws_image_builder_image_pipeline`'s output gets copied and..."
---

`aws_image_builder_distribution_configuration` defines which real Regions and accounts an `aws_image_builder_image_pipeline`'s output gets copied and shared to once built, rather than a caller manually copying a new AMI everywhere it's needed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	image "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/image"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own builder distribution configuration"})

    		ubx.Resource(image.BuilderDistributionConfiguration, "example", image.BuilderDistributionConfigurationConfig{
    			Distributions: []map[string]any{{"ami_distribution_configuration": map[string]any{"name": "example-name"}}},
    			Name:          "example",
    			Description:   "Managed by ubx.",
    			Tags:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BuilderDistributionConfiguration } from "./local-sdk/aws/sdk/typescript/aws/image/builder_distribution_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own builder distribution configuration" });

      resource(BuilderDistributionConfiguration, "example", {
        distributions: [{ amiDistributionConfiguration: { name: "example-name" } }],
        name: "example",
        description: "Managed by ubx.",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.image import BuilderDistributionConfiguration, BuilderDistributionConfigurationConfig

    def describe():
        ubx.intent("example own builder distribution configuration")

        ubx.resource(BuilderDistributionConfiguration, "example", BuilderDistributionConfigurationConfig(
            distributions=[{"ami_distribution_configuration": {"name": "example-name"}}],
            name="example",
            description="Managed by ubx.",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_image_builder_distribution_configuration called
    "example", with distributions a set value, description Managed by
    ubx., tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The description of the distribution configuration. Optional.
</ResponseField>

<ResponseField name="distributions" type="list(object)" required>
  The distributions of the distribution configuration. Required.
  <Expandable title="properties">
    <ResponseField name="ami_distribution_configuration" type="object">
      Defines the configuration for distributing the built image as an Amazon Machine Image (AMI), including AMI tags, launch permissions (e.g., user groups or organization ARNs), and an optional AMI name prefix. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ami_tags" type="any">
          The tags to apply to the AMI when it is distributed to the target region. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          The description of the AMI distribution configuration, providing a human-readable explanation of the distribution settings for the AMI. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="kms_key_id" type="string">
          The customer-managed AWS KMS key ID used to encrypt the AMI volumes created from this distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="launch_permission_configuration" type="object">
          Specifies the AWS accounts and user groups that are granted launch permissions for the distributed AMI, effectively controlling who can launch the AMI created by this distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="organization_arns" type="list(string)">
              The list of ARNs of AWS Organizations organizational units (OUs) that this AMI distribution will be shared with via launch permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="organizational_unit_arns" type="list(string)">
              Specifies the ARNs of the AWS Organizations organizational units (OUs) to which launch permissions for the distributed AMI are granted, so that all accounts within those OUs can launch the image. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_groups" type="list(string)">
              Specifies the AWS user groups that will be granted launch permissions for the distributed AMI. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_ids" type="list(string)">
              Lists the AWS account IDs that are granted permission to launch the AMI created by the distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The name assigned to the AMI produced by this distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_account_ids" type="list(string)">
          Specifies the AWS account IDs to which the built AMI is shared after distribution. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_distribution_configuration" type="object">
      Configures how a container image is distributed, specifying the target container repository and optional container tags for each distribution in an Image Builder distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="container_tags" type="list(string)">
          The tags to apply to the container image (e.g., an ECR image) when it is distributed to the target AWS account and region. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          A user-defined description for the container distribution configuration, used to provide additional context or identify the purpose of these distribution settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_repository" type="object">
          Specifies the destination container repository (e.g., an Amazon ECR repository) to which Image Builder delivers container images built by this distribution configuration, including the repository name and service type. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="repository_name" type="string">
              The name of the container repository (e.g., within Amazon ECR) where the built container image is distributed by this distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="service" type="string">
              The service that the target repository belongs to, which must be 'ECR' (Amazon Elastic Container Registry) for container images distributed by Image Builder. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fast_launch_configurations" type="list(object)">
      Configures fast launch settings for Windows AMIs in this distribution, including enabling fast launch, snapshot configuration, and target accounts. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="account_id" type="string">
          Specifies the AWS account ID for which the distribution's fast launch configuration is enabled, allowing that account to use the AMI with faster launch times. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          This boolean enables or disables fast launch for the distribution, which pre-provisions snapshots to speed up instance launches. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="launch_template" type="object">
          The launch template configuration that specifies the EC2 launch template (by ID or name) used when launching instances from the fast-launch enabled Windows AMI in this distribution. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="launch_template_id" type="string">
              The ID of the EC2 launch template that Image Builder uses to configure pre-provisioned snapshots for fast launch of the distributed AMI. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="launch_template_name" type="string">
              The name of the EC2 launch template that defines the configuration for fast launching Windows AMIs in this distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="launch_template_version" type="string">
              Specifies the version of the EC2 launch template to use for the fast launch configuration, allowing you to pin to a specific version or use the default/latest version. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_parallel_launches" type="number">
          Specifies the maximum number of parallel EC2 instances that can be launched for testing the AMI as part of the fast launch configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="snapshot_configuration" type="object">
          Configures the pre-provisioned snapshots for a Windows fast launch setup, defining the target number of snapshots to retain for faster instance launching. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="target_resource_count" type="number">
              Specifies the target number of pre-provisioned snapshots to retain for fast launching Windows instances in this distribution configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="launch_template_configurations" type="list(object)">
      The launch template configurations specify the EC2 launch templates that Image Builder uses when distributing the AMI to specific AWS accounts, including the launch template ID and whether to set it as the default version. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="account_id" type="string">
          The AWS account ID to which this launch template configuration applies, so that the specified launch template is used for image builds in that account. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="launch_template_id" type="string">
          The ID of the EC2 launch template used by Image Builder when distributing the image to a target region, as defined in the launch template configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="set_default_version" type="bool">
          Indicates whether to set the specified EC2 launch template as the default launch template for the target AWS account when this launch template configuration is used by the Image Builder distribution. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="license_configuration_arns" type="list(string)">
      Specifies the AWS License Manager license configuration ARNs to apply to the AMI when it is distributed to the target region, enabling licensing compliance and usage tracking for that distribution. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="region" type="string">
      The AWS Region to which this distribution configuration applies, enabling per-region settings for the output AMIs. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ssm_parameter_configurations" type="list(object)">
      Lists the Systems Manager (SSM) parameter name-value pairs used to configure the EC2 instances launched from the image within this distribution, each object defining a parameter name and its value. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ami_account_id" type="string">
          The ID of the AWS account that owns the AMI to be distributed to the specified Systems Manager parameter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="data_type" type="string">
          Data type (String or StringList) of the SSM parameter that Image Builder creates in the target account when the AMI is distributed. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="parameter_name" type="string">
          The name of the AWS Systems Manager parameter that Image Builder updates with the AMI ID when the distribution configuration is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the distribution configuration. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  The tags associated with the component. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the distribution configuration. Computed by AWS after `ship`.
</ResponseField>
