---
title: "aws_game_lift_container_group_definition"
description: "`aws_game_lift_container_group_definition` is the real, versioned blueprint an `aws_game_lift_container_fleet` deploys from..."
---

`aws_game_lift_container_group_definition` is the real, versioned blueprint an `aws_game_lift_container_fleet` deploys from, which container images to run per instance and how, game-server containers plus any supporting per-instance containers like logging or background services.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own container group definition"})

    		ubx.Resource(gamelift.ContainerGroupDefinition, "example", gamelift.ContainerGroupDefinitionConfig{
    			Name:                          "example",
    			OperatingSystem:               "example",
    			TotalMemoryLimitMebibytes:     1,
    			TotalVcpuLimit:                1,
    			ContainerGroupType:            "example",
    			GameServerContainerDefinition: map[string]any{"container_name": "example-container-name", "image_uri": "example", "server_sdk_version": "example"},
    			SourceVersionNumber:           1,
    			SupportContainerDefinitions:   []map[string]any{{"container_name": "example-container-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContainerGroupDefinition } from "@ubx/sdk-aws/aws/gamelift/container_group_definition";

    export default stack("example", () => {
      intent({ summary: "example own container group definition" });

      resource(ContainerGroupDefinition, "example", {
        name: "example",
        operatingSystem: "example",
        totalMemoryLimitMebibytes: 1,
        totalVcpuLimit: 1,
        containerGroupType: "example",
        gameServerContainerDefinition: {
          containerName: "example-container-name",
          imageUri: "example",
          serverSdkVersion: "example",
        },
        sourceVersionNumber: 1,
        supportContainerDefinitions: [{ containerName: "example-container-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.gamelift import ContainerGroupDefinition, ContainerGroupDefinitionConfig
    from ubx.aws.gamelift.container_group_definition import ContainerGroupDefinition_GameServerContainerDefinition, ContainerGroupDefinition_SupportContainerDefinitions

    def describe():
        ubx.intent("example own container group definition")

        ubx.resource(ContainerGroupDefinition, "example", ContainerGroupDefinitionConfig(
            name="example",
            operating_system="example",
            total_memory_limit_mebibytes=1,
            total_vcpu_limit=1,
            container_group_type="example",
            game_server_container_definition=ContainerGroupDefinition_GameServerContainerDefinition(container_name="example-container-name", image_uri="example", server_sdk_version="example"),
            source_version_number=1,
            support_container_definitions=[ContainerGroupDefinition_SupportContainerDefinitions(container_name="example-container-name")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_game_lift_container_group_definition called "example",
    with operating system example, total memory limit mebibytes 1,
    total vcpu limit 1, container group type example, game server
    container definition a set value, source version number 1,
    support container definitions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="container_group_type" type="string">
  The scope of the container group Optional.
</ResponseField>

<ResponseField name="game_server_container_definition" type="object">
  Specifies the information required to run game servers with this container group Optional.
  <Expandable title="properties">
    <ResponseField name="container_name" type="string" required>
      A descriptive label for the container definition. Container definition names must be unique with a container group definition. Required.
    </ResponseField>
    <ResponseField name="depends_on" type="list(object)">
      A list of container dependencies that determines when this container starts up and shuts down. For container groups with multiple containers, dependencies let you define a startup/shutdown sequence across the containers. Optional.
      <Expandable title="properties">
        <ResponseField name="condition" type="string">
          Determines the condition (such as START, COMPLETE, SUCCESS, or HEALTHY) that the referenced dependency container must satisfy before the game server container is allowed to start. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="container_name" type="string">
          The name of the container that the game server container depends on, used to define the startup order between containers in the container group. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_override" type="list(object)">
      The environment variables to pass to a container. Optional.
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the environment variable whose value is being overridden for the game server container defined in this GameLift container group. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Specifies the value for an environment variable override applied to the game server container at runtime, replacing any default value defined in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image_uri" type="string" required>
      Specifies the image URI of this container. Required.
    </ResponseField>
    <ResponseField name="linux_capabilities" type="object">
      A set of Linux capabilities that are added to a container's default Docker configuration. For more detailed information, see the capabilities(7) Linux manual page. Optional.
      <Expandable title="properties">
        <ResponseField name="include" type="list(string)">
          The list of Linux capabilities to add to the container's default configuration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mount_points" type="list(object)">
      A list of mount point configurations to be used in a container. Optional.
      <Expandable title="properties">
        <ResponseField name="access_level" type="string">
          Specifies whether the mounted volume is read-only or read-write, using values like READ_ONLY or READ_WRITE. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="container_path" type="string">
          The file path inside the game server container where the mounted volume is accessible, as specified in the mount point configuration for the container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_path" type="string">
          Specifies the file path on the underlying EC2 instance that the container mount point references, allowing the container to access data stored on the instance's filesystem. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port_configuration" type="object">
      Defines the ports on a container. Optional.
      <Expandable title="properties">
        <ResponseField name="container_port_ranges" type="list(object)" required>
          Specifies one or more ranges of ports on a container. Required.
          <Expandable title="properties">
            <ResponseField name="from_port" type="number">
              Specifies the starting port in a contiguous range of ports that the game server container uses for incoming game traffic, as part of a container port range for the container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="protocol" type="string">
              The network protocol (TCP or UDP) for the container port range, used for routing traffic to the game server container. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="to_port" type="number">
              The ending port number in the inclusive range of ports that the game server container uses for inbound game traffic, as part of the container port configuration for the GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resolved_image_digest" type="string">
      The digest of the container image. Optional.
    </ResponseField>
    <ResponseField name="server_sdk_version" type="string" required>
      The version of the server SDK used in this container group Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  A descriptive label for the container group definition. Required.
</ResponseField>

<ResponseField name="operating_system" type="string" required>
  The operating system of the container group Required.
</ResponseField>

<ResponseField name="source_version_number" type="number">
  A specific ContainerGroupDefinition version to be updated Optional.
</ResponseField>

<ResponseField name="support_container_definitions" type="list(object)">
  A collection of support container definitions that define the containers in this group. Optional.
  <Expandable title="properties">
    <ResponseField name="container_name" type="string">
      The name assigned to the support container, which serves as a unique identifier within the container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="depends_on" type="list(object)">
      Defines which other containers in the same container group must reach a specified condition (e.g., START, COMPLETE, SUCCESS, HEALTHY) before this support container is started. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="condition" type="string">
          Defines the required condition (e.g., START, COMPLETE, SUCCESS, HEALTHY) on the referenced container that must be met before the support container in the GameLift container group definition is started. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="container_name" type="string">
          The name of a container in the same container group that this support container depends on, used to define startup order. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="environment_override" type="list(object)">
      A list of environment variable overrides applied to the support container when it runs, each specifying the variable name and value to replace any value defined in the container image, for a GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          Specifies the name of the environment variable to override in the support container's runtime configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value portion of an environment variable override applied to a support container in the GameLift container group definition, replacing the container image's default environment variable value. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="essential" type="bool">
      Indicates whether the support container is essential, meaning that if an essential support container stops or fails, the entire container group is stopped. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="health_check" type="object">
      Specifies the container health check configuration, including the command to run, interval, timeout, retries, and start period, used by Amazon GameLift to determine whether the support container is healthy. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="command" type="list(string)">
          The command to run inside the support container to perform a health check, specified as a list of strings representing the executable and its arguments. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="interval" type="number">
          Specifies the time interval (in seconds) between successive health check runs for this support container in the GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="retries" type="number">
          The retries field specifies the number of consecutive failed health checks after which the container is considered unhealthy in the GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="start_period" type="number">
          The number of seconds that the health check waits after the support container starts before beginning its health checks in a GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="timeout" type="number">
          The amount of time (in seconds) that GameLift waits for the container health check command to return before considering the health check failed and marking the container unhealthy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image_uri" type="string">
      The URI of the container image that this support container in the GameLift container group definition will pull and run. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="linux_capabilities" type="object">
      Configures the Linux kernel capabilities (additional or dropped) for the support container, controlling which privileged operations the container is allowed to perform. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="include" type="list(string)">
          Specifies the list of Linux capabilities to add (include) for the support container, granting the container these additional Linux privileges on top of its default capabilities. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="memory_hard_limit_mebibytes" type="number">
      Sets the maximum memory (in mebibytes) that the support container is allowed to use, enforcing a hard limit on its memory consumption. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mount_points" type="list(object)">
      Defines the mount points for the support container, specifying the container path and the source volume to attach for shared storage within the GameLift container group. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="access_level" type="string">
          Specifies whether the mounted container path is accessible as read-only (READ_ONLY) or read-write (READ_WRITE) to the support container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="container_path" type="string">
          Specifies the path inside the support container where a mounted volume is accessible, used to define the container's mount point for persistent storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="instance_path" type="string">
          Specifies the absolute file system path on the host EC2 instance that serves as the source volume for a mount point in a support container definition within an AWS GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="port_configuration" type="object">
      Defines the port range and protocol (e.g., TCP or UDP) that the support container exposes for inbound traffic in the GameLift container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="container_port_ranges" type="list(object)">
          Specifies an inclusive range of container ports (from a starting port to an ending port) that a support container in an Amazon GameLift container group definition exposes for game traffic or other networking purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="from_port" type="number">
              Specifies the starting (lower) port number in a contiguous range of ports that this support container is configured to use within its port configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="protocol" type="string">
              The network protocol (TCP or UDP) used by the container port range in the GameLift container group. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="to_port" type="number">
              Specifies the upper (inclusive) bound of the port range that this support container can use for its network traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resolved_image_digest" type="string">
      The resolved image digest for the support container, computed from the image URI to uniquely identify the exact container image version used by the container group definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="vcpu" type="number">
      The number of vCPU units allocated to this support container in the GameLift container group definition, which sets the CPU resource limit for that container. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag applied to an Amazon GameLift container group definition, enabling resource organization and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_memory_limit_mebibytes" type="number" required>
  The total memory limit of container groups following this definition in MiB Required.
</ResponseField>

<ResponseField name="total_vcpu_limit" type="number" required>
  The total amount of virtual CPUs on the container group definition Required.
</ResponseField>

<ResponseField name="version_description" type="string">
  The description of this version Optional.
</ResponseField>

## Output properties

<ResponseField name="container_group_definition_arn" type="string">
  The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift container group resource and uniquely identifies it across all AWS Regions. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example "1469498468.057"). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  A string indicating ContainerGroupDefinition status. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  A string indicating the reason for ContainerGroupDefinition status. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_number" type="number">
  The version of this ContainerGroupDefinition Computed by AWS after `ship`.
</ResponseField>
