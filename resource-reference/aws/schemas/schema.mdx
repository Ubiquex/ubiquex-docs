---
title: "aws_schemas_schema"
description: "Real, generated bindings for `aws_schemas_schema`."
---

`aws_schemas_schema` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	schemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/schemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own schema"})

    		ubx.Resource(schemas.Schema, "ci-runner", schemas.SchemaConfig{
    			Content:      "example",
    			Name:         "ci-runner",
    			RegistryName: "example-registry-name",
    			Type:         "example",
    			Description:  "Managed by ubx.",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Schema } from "jsr:@ubx/sdk-aws/aws/schemas/schema";

    export default stack("payments", () => {
      intent({ summary: "payments own schema" });

      resource(Schema, "ci-runner", {
        content: "example",
        name: "ci-runner",
        registryName: "example-registry-name",
        type: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.schemas import Schema, SchemaConfig

    def describe():
        ubx.intent("payments own schema")

        ubx.resource(Schema, "ci-runner", SchemaConfig(
            content="example",
            name="ci-runner",
            registry_name="example-registry-name",
            type="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_schemas_schema called "ci-runner", with content
    example, registry name example-registry-name, type example,
    description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_modified" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>
