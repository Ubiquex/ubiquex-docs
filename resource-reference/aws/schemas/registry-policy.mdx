---
title: "aws_schemas_registry_policy"
description: "Real, generated bindings for `aws_schemas_registry_policy`."
---

`aws_schemas_registry_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/schemas"

    cfg := schemas.RegistryPolicyConfig{
        Policy: "example",
        RegistryName: "example-registry-name",
    }
    ubx.Resource(schemas.RegistryPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RegistryPolicy } from
      "jsr:@ubx/sdk-aws/aws/schemas/registry_policy";

    resource(RegistryPolicy, "example", {
      policy: "example",
      registryName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.schemas import (
        RegistryPolicy,
        RegistryPolicyConfig,
    )

    cfg = RegistryPolicyConfig(
        policy="example",
        registry_name="example",
    )
    ubx.resource(RegistryPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_schemas_registry_policy with policy, registry_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
