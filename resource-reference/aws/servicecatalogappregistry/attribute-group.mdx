---
title: "aws_servicecatalogappregistry_attribute_group"
description: "Real, generated bindings for `aws_servicecatalogappregistry_attribute_group`."
---

`aws_servicecatalogappregistry_attribute_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalogappregistry "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalogappregistry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attribute group"})

    		ubx.Resource(servicecatalogappregistry.AttributeGroup, "ci-runner", servicecatalogappregistry.AttributeGroupConfig{
    			Attributes:  "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AttributeGroup } from "jsr:@ubx/sdk-aws/aws/servicecatalogappregistry/attribute_group";

    export default stack("payments", () => {
      intent({ summary: "payments own attribute group" });

      resource(AttributeGroup, "ci-runner", {
        attributes: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalogappregistry import AttributeGroup, AttributeGroupConfig

    def describe():
        ubx.intent("payments own attribute group")

        ubx.resource(AttributeGroup, "ci-runner", AttributeGroupConfig(
            attributes="example",
            name="ci-runner",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalogappregistry_attribute_group called
    "ci-runner", with attributes example, description Managed by
    ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attributes" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
