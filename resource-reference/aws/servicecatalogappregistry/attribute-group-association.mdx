---
title: "aws_servicecatalogappregistry_attribute_group_association"
description: "Real, generated bindings for `aws_servicecatalogappregistry_attribute_group_association`."
---

`aws_servicecatalogappregistry_attribute_group_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalogappregistry "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalogappregistry"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attribute group association"})

    		ubx.Resource(servicecatalogappregistry.AttributeGroupAssociation, "ci-runner", servicecatalogappregistry.AttributeGroupAssociationConfig{
    			ApplicationId:    "example",
    			AttributeGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AttributeGroupAssociation } from "jsr:@ubx/sdk-aws/aws/servicecatalogappregistry/attribute_group_association";

    export default stack("payments", () => {
      intent({ summary: "payments own attribute group association" });

      resource(AttributeGroupAssociation, "ci-runner", {
        applicationId: "example",
        attributeGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalogappregistry import AttributeGroupAssociation, AttributeGroupAssociationConfig

    def describe():
        ubx.intent("payments own attribute group association")

        ubx.resource(AttributeGroupAssociation, "ci-runner", AttributeGroupAssociationConfig(
            application_id="example",
            attribute_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalogappregistry_attribute_group_association,
    with application id example, attribute group id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  ID of the application. Required.
</ResponseField>

<ResponseField name="attribute_group_id" type="string" required>
  ID of the attribute group to associate with the application. Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
