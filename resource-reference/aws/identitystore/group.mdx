---
title: "aws_identitystore_group"
description: "Real, generated bindings for `aws_identitystore_group`."
---

`aws_identitystore_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/identitystore"

    cfg := identitystore.GroupConfig{
        DisplayName: "example-display-name",
        IdentityStoreId: "example",
        Description: "example",
    }
    ubx.Resource(identitystore.Group, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Group } from "jsr:@ubx/sdk-aws/aws/identitystore/group";

    resource(Group, "example", {
      displayName: "example",
      identityStoreId: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.identitystore import Group, GroupConfig

    cfg = GroupConfig(
        display_name="example",
        identity_store_id="example",
        description="example",
    )
    ubx.resource(Group, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_identitystore_group with display_name,
identity_store_id, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="external_ids" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="issuer" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
