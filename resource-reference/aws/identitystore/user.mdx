---
title: "aws_identitystore_user"
description: "Real, generated bindings for `aws_identitystore_user`."
---

`aws_identitystore_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identitystore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/identitystore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user"})

    		ubx.Resource(identitystore.User, "ci-runner", identitystore.UserConfig{
    			DisplayName:       "example-display-name",
    			IdentityStoreId:   "example",
    			UserName:          "example-user-name",
    			Locale:            "example",
    			Nickname:          "example",
    			PreferredLanguage: "example",
    			ProfileUrl:        "example",
    			Timezone:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "jsr:@ubx/sdk-aws/aws/identitystore/user";

    export default stack("payments", () => {
      intent({ summary: "payments own user" });

      resource(User, "ci-runner", {
        displayName: "example-display-name",
        identityStoreId: "example",
        userName: "example-user-name",
        locale: "example",
        nickname: "example",
        preferredLanguage: "example",
        profileUrl: "example",
        timezone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.identitystore import User, UserConfig

    def describe():
        ubx.intent("payments own user")

        ubx.resource(User, "ci-runner", UserConfig(
            display_name="example-display-name",
            identity_store_id="example",
            user_name="example-user-name",
            locale="example",
            nickname="example",
            preferred_language="example",
            profile_url="example",
            timezone="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_identitystore_user, with display name
    example-display-name, identity store id example, user name
    example-user-name, locale example, nickname example, preferred
    language example, profile url example, timezone example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="locale" type="string">
  Optional.
</ResponseField>

<ResponseField name="nickname" type="string">
  Optional.
</ResponseField>

<ResponseField name="preferred_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="profile_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timezone" type="string">
  Optional.
</ResponseField>

<ResponseField name="title" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="external_ids" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="issuer" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
