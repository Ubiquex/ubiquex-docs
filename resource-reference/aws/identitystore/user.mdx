---
title: "aws_identity_store_user"
description: "A complete, runnable aws_identity_store_user program, in every SDK language."
---

`aws_identity_store_user` -- real, typed bindings generated directly from
the real AWS CloudFormation resource registry, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identitystore "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/identitystore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user"})

    		ubx.Resource(identitystore.User, "example", identitystore.UserConfig{
    			IdentityStoreId: "example",
    			Name:            map[string]any{"family_name": "example-family-name"},
    			Addresses:       []map[string]any{{"country": "example"}},
    			Birthdate:       "example",
    			DisplayName:     "example-display-name",
    			Emails:          []map[string]any{{"primary": true}},
    			Locale:          "example",
    			NickName:        "example-nick-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "@ubx/sdk-aws/aws/identitystore/user";

    export default stack("example", () => {
      intent({ summary: "example own user" });

      resource(User, "example", {
        identityStoreId: "example",
        name: { familyName: "example-family-name" },
        addresses: [{ country: "example" }],
        birthdate: "example",
        displayName: "example-display-name",
        emails: [{ primary: true }],
        locale: "example",
        nickName: "example-nick-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.identitystore import User, UserConfig
    from ubx.aws.identitystore.user import User_Name, User_Addresses, User_Emails

    def describe():
        ubx.intent("example own user")

        ubx.resource(User, "example", UserConfig(
            identity_store_id="example",
            name=User_Name(family_name="example-family-name"),
            addresses=[User_Addresses(country="example")],
            birthdate="example",
            display_name="example-display-name",
            emails=[User_Emails(primary=True)],
            locale="example",
            nick_name="example-nick-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_identity_store_user called "a set value", with
    identity store id example, addresses a set value, birthdate
    example, display name example-display-name, emails a set value,
    locale example, nick name example-nick-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="addresses" type="list(object)">
  A list of addresses associated with the user. Optional.
  <Expandable title="properties">
    <ResponseField name="country" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="formatted" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="locality" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="postal_code" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="primary" type="bool">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="region" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="street_address" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="birthdate" type="string">
  The user's birthdate in YYYY-MM-DD format. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  A string containing the name of the user for display. Optional.
</ResponseField>

<ResponseField name="emails" type="list(object)">
  A list of email addresses associated with the user. Optional.
  <Expandable title="properties">
    <ResponseField name="primary" type="bool">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  The globally unique identifier for the identity store. Required.
</ResponseField>

<ResponseField name="locale" type="string">
  The geographical region or location of the user. Optional.
</ResponseField>

<ResponseField name="name" type="object">
  The name of the user. Optional.
  <Expandable title="properties">
    <ResponseField name="family_name" type="string">
      The family name of the user. Optional.
    </ResponseField>
    <ResponseField name="formatted" type="string">
      A string containing a formatted version of the name for display. Optional.
    </ResponseField>
    <ResponseField name="given_name" type="string">
      The given name of the user. Optional.
    </ResponseField>
    <ResponseField name="honorific_prefix" type="string">
      The honorific prefix of the user. Optional.
    </ResponseField>
    <ResponseField name="honorific_suffix" type="string">
      The honorific suffix of the user. Optional.
    </ResponseField>
    <ResponseField name="middle_name" type="string">
      The middle name of the user. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="nick_name" type="string">
  An alternate name for the user. Optional.
</ResponseField>

<ResponseField name="phone_numbers" type="list(object)">
  A list of phone numbers associated with the user. Optional.
  <Expandable title="properties">
    <ResponseField name="primary" type="bool">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="photos" type="list(object)">
  A list of photos associated with the user. Optional.
  <Expandable title="properties">
    <ResponseField name="display" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="primary" type="bool">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="preferred_language" type="string">
  The preferred language of the user. Optional.
</ResponseField>

<ResponseField name="profile_url" type="string">
  A URL associated with the user. Optional.
</ResponseField>

<ResponseField name="roles" type="list(object)">
  A list of roles associated with the user. Optional.
  <Expandable title="properties">
    <ResponseField name="primary" type="bool">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timezone" type="string">
  The time zone for the user. Optional.
</ResponseField>

<ResponseField name="title" type="string">
  The title of the user. Optional.
</ResponseField>

<ResponseField name="user_name" type="string">
  A unique string used to identify the user. Optional.
</ResponseField>

<ResponseField name="user_type" type="string">
  A string indicating the type of user. Optional.
</ResponseField>

<ResponseField name="website" type="string">
  The user's personal website or blog URL. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The date and time the user was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  The identifier of the user or system that created the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time the user was last updated. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_by" type="string">
  The identifier of the user or system that last updated the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_id" type="string">
  The identifier for a user in the identity store. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_status" type="string">
  The current status of the user account. Computed by AWS after `ship`.
</ResponseField>
