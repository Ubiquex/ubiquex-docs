---
title: "aws_identitystore_user"
description: "Real, generated bindings for `aws_identitystore_user`."
---

`aws_identitystore_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/identitystore"

    cfg := identitystore.UserConfig{
        DisplayName: "example-display-name",
        IdentityStoreId: "example",
        UserName: "example-user-name",
        Locale: "example",
        Nickname: "example-nickname",
    }
    ubx.Resource(identitystore.User, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { User } from "jsr:@ubx/sdk-aws/aws/identitystore/user";

    resource(User, "example", {
      displayName: "example",
      identityStoreId: "example",
      userName: "example",
      locale: "example",
      nickname: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.identitystore import User, UserConfig

    cfg = UserConfig(
        display_name="example",
        identity_store_id="example",
        user_name="example",
        locale="example",
        nickname="example",
    )
    ubx.resource(User, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_identitystore_user with display_name,
identity_store_id, user_name, locale, nickname.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="display_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="locale" type="string">
  Optional.
</ResponseField>

<ResponseField name="nickname" type="string">
  Optional.
</ResponseField>

<ResponseField name="preferred_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="profile_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timezone" type="string">
  Optional.
</ResponseField>

<ResponseField name="title" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="external_ids" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
    </ResponseField>
    <ResponseField name="issuer" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
