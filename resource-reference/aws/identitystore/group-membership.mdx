---
title: "aws_identitystore_group_membership"
description: "Real, generated bindings for `aws_identitystore_group_membership`."
---

`aws_identitystore_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identitystore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/identitystore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group membership"})

    		ubx.Resource(identitystore.GroupMembership, "ci-runner", identitystore.GroupMembershipConfig{
    			GroupId:         "example",
    			IdentityStoreId: "example",
    			MemberId:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupMembership } from "jsr:@ubx/sdk-aws/aws/identitystore/group_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own group membership" });

      resource(GroupMembership, "ci-runner", {
        groupId: "example",
        identityStoreId: "example",
        memberId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.identitystore import GroupMembership, GroupMembershipConfig

    def describe():
        ubx.intent("payments own group membership")

        ubx.resource(GroupMembership, "ci-runner", GroupMembershipConfig(
            group_id="example",
            identity_store_id="example",
            member_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_identitystore_group_membership, with group id example,
    identity store id example, member id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="membership_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
