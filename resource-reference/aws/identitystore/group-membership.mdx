---
title: "aws_identitystore_group_membership"
description: "Real, generated bindings for `aws_identitystore_group_membership`."
---

`aws_identitystore_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/identitystore"

    cfg := identitystore.GroupMembershipConfig{
        GroupId: "example",
        IdentityStoreId: "example",
        MemberId: "example",
    }
    ubx.Resource(identitystore.GroupMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GroupMembership } from
      "jsr:@ubx/sdk-aws/aws/identitystore/group_membership";

    resource(GroupMembership, "example", {
      groupId: "example",
      identityStoreId: "example",
      memberId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.identitystore import (
        GroupMembership,
        GroupMembershipConfig,
    )

    cfg = GroupMembershipConfig(
        group_id="example",
        identity_store_id="example",
        member_id="example",
    )
    ubx.resource(GroupMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_identitystore_group_membership with group_id,
identity_store_id, member_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="membership_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
