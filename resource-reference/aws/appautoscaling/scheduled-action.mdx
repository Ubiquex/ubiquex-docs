---
title: "aws_appautoscaling_scheduled_action"
description: "Real, generated bindings for `aws_appautoscaling_scheduled_action`."
---

`aws_appautoscaling_scheduled_action` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appautoscaling"

    cfg := appautoscaling.ScheduledActionConfig{
        Name: "example-name",
        ResourceId: "example",
        ScalableDimension: "example",
        Schedule: "example",
        ServiceNamespace: "example-service-namespace",
        EndTime: "example",
        StartTime: "example",
    }
    ubx.Resource(appautoscaling.ScheduledAction, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ScheduledAction } from
      "jsr:@ubx/sdk-aws/aws/appautoscaling/scheduled_action";

    resource(ScheduledAction, "example", {
      name: "example",
      resourceId: "example",
      scalableDimension: "example",
      schedule: "example",
      serviceNamespace: "example",
      endTime: "example",
      startTime: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appautoscaling import (
        ScheduledAction,
        ScheduledActionConfig,
    )

    cfg = ScheduledActionConfig(
        name="example",
        resource_id="example",
        scalable_dimension="example",
        schedule="example",
        service_namespace="example",
        end_time="example",
        start_time="example",
    )
    ubx.resource(ScheduledAction, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appautoscaling_scheduled_action with name,
resource_id, scalable_dimension, schedule, service_namespace,
end_time, start_time.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="end_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  Required.
</ResponseField>

<ResponseField name="schedule" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="timezone" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
