---
title: "aws_appautoscaling_policy"
description: "Real, generated bindings for `aws_appautoscaling_policy`."
---

`aws_appautoscaling_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appautoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appautoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own policy"})

    		ubx.Resource(appautoscaling.Policy, "ci-runner", appautoscaling.PolicyConfig{
    			Name:              "ci-runner",
    			ResourceId:        "example",
    			ScalableDimension: "example",
    			ServiceNamespace:  "example",
    			PolicyType:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Policy } from "jsr:@ubx/sdk-aws/aws/appautoscaling/policy";

    export default stack("payments", () => {
      intent({ summary: "payments own policy" });

      resource(Policy, "ci-runner", {
        name: "ci-runner",
        resourceId: "example",
        scalableDimension: "example",
        serviceNamespace: "example",
        policyType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appautoscaling import Policy, PolicyConfig

    def describe():
        ubx.intent("payments own policy")

        ubx.resource(Policy, "ci-runner", PolicyConfig(
            name="ci-runner",
            resource_id="example",
            scalable_dimension="example",
            service_namespace="example",
            policy_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appautoscaling_policy called "ci-runner", with
    resource id example, scalable dimension example, service
    namespace example, policy type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="alarm_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
