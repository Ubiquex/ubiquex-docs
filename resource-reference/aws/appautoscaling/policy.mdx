---
title: "aws_appautoscaling_policy"
description: "Real, generated bindings for `aws_appautoscaling_policy`."
---

`aws_appautoscaling_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appautoscaling"

    cfg := appautoscaling.PolicyConfig{
        Name: "example-name",
        ResourceId: "example",
        ScalableDimension: "example",
        ServiceNamespace: "example-service-namespace",
        PolicyType: "example",
    }
    ubx.Resource(appautoscaling.Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Policy } from
      "jsr:@ubx/sdk-aws/aws/appautoscaling/policy";

    resource(Policy, "example", {
      name: "example",
      resourceId: "example",
      scalableDimension: "example",
      serviceNamespace: "example",
      policyType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appautoscaling import Policy, PolicyConfig

    cfg = PolicyConfig(
        name="example",
        resource_id="example",
        scalable_dimension="example",
        service_namespace="example",
        policy_type="example",
    )
    ubx.resource(Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appautoscaling_policy with name, resource_id,
scalable_dimension, service_namespace, policy_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="alarm_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
