---
title: "aws_appautoscaling_target"
description: "Real, generated bindings for `aws_appautoscaling_target`."
---

`aws_appautoscaling_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appautoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appautoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target"})

    		ubx.Resource(appautoscaling.Target, "ci-runner", appautoscaling.TargetConfig{
    			MaxCapacity:       1,
    			MinCapacity:       1,
    			ResourceId:        "example",
    			ScalableDimension: "example",
    			ServiceNamespace:  "example",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Target } from "jsr:@ubx/sdk-aws/aws/appautoscaling/target";

    export default stack("payments", () => {
      intent({ summary: "payments own target" });

      resource(Target, "ci-runner", {
        maxCapacity: 1,
        minCapacity: 1,
        resourceId: "example",
        scalableDimension: "example",
        serviceNamespace: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appautoscaling import Target, TargetConfig

    def describe():
        ubx.intent("payments own target")

        ubx.resource(Target, "ci-runner", TargetConfig(
            max_capacity=1,
            min_capacity=1,
            resource_id="example",
            scalable_dimension="example",
            service_namespace="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appautoscaling_target, with max capacity 1, min
    capacity 1, resource id example, scalable dimension example,
    service namespace example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_capacity" type="number" required>
  Required.
</ResponseField>

<ResponseField name="min_capacity" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required.
</ResponseField>

<ResponseField name="suspended_state" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="dynamic_scaling_in_suspended" type="bool">
    </ResponseField>
    <ResponseField name="dynamic_scaling_out_suspended" type="bool">
    </ResponseField>
    <ResponseField name="scheduled_scaling_suspended" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="suspended_state" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="dynamic_scaling_in_suspended" type="bool">
    </ResponseField>
    <ResponseField name="dynamic_scaling_out_suspended" type="bool">
    </ResponseField>
    <ResponseField name="scheduled_scaling_suspended" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
