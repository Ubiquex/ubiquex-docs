---
title: "aws_fis_experiment_template"
description: "`aws_fis_experiment_template` defines a real chaos engineering experiment for AWS Fault Injection Service, which actions to inject..."
---

`aws_fis_experiment_template` defines a real chaos engineering experiment for AWS Fault Injection Service, which actions to inject, terminating instances, throttling an API, spiking CPU, against which real targets, and what stop conditions abort the experiment automatically if things go wrong.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own experiment template"})

    		ubx.Resource(fis.ExperimentTemplate, "ci-runner", fis.ExperimentTemplateConfig{
    			Description:                   "Managed by ubx.",
    			RoleArn:                       "arn:aws:iam::123456789012:policy/ci",
    			StopConditions:                []map[string]any{{"source": "example"}},
    			Targets:                       "example",
    			Actions:                       "example",
    			ExperimentOptions:             map[string]any{"account_targeting": "example"},
    			ExperimentReportConfiguration: map[string]any{"outputs": map[string]any{"experiment_report_s3_configuration": map[string]any{"bucket_name": "example-bucket-name"}}},
    			LogConfiguration:              map[string]any{"log_schema_version": 1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExperimentTemplate } from "./local-sdk/aws/sdk/typescript/aws/fis/experiment_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own experiment template" });

      resource(ExperimentTemplate, "ci-runner", {
        description: "Managed by ubx.",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        stopConditions: [{ source: "example" }],
        targets: "example",
        actions: "example",
        experimentOptions: { accountTargeting: "example" },
        experimentReportConfiguration: {
          outputs: {
            experimentReportS3Configuration: { bucketName: "example-bucket-name" },
          },
        },
        logConfiguration: { logSchemaVersion: 1 },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.fis import ExperimentTemplate, ExperimentTemplateConfig

    def describe():
        ubx.intent("payments own experiment template")

        ubx.resource(ExperimentTemplate, "ci-runner", ExperimentTemplateConfig(
            description="Managed by ubx.",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            stop_conditions=[{"source": "example"}],
            targets="example",
            actions="example",
            experiment_options={"account_targeting": "example"},
            experiment_report_configuration={"outputs": {"experiment_report_s3_configuration": {"bucket_name": "example-bucket-name"}}},
            log_configuration={"log_schema_version": 1},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_fis_experiment_template, with description Managed by
    ubx., role arn arn:aws:iam::123456789012:policy/ci, stop
    conditions a set value, targets example, actions example,
    experiment options a set value, experiment report configuration a
    set value, log configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="any">
  The actions for the experiment. Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  A description for the experiment template. Required.
</ResponseField>

<ResponseField name="experiment_options" type="object">
  Specifies experiment-wide options for the template, such as account targeting (single vs. multi-account) and the empty target resolution mode (fail or skip). Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="account_targeting" type="string">
      The account targeting setting for the experiment template. Optional.
    </ResponseField>
    <ResponseField name="empty_target_resolution_mode" type="string">
      The target resolution failure mode for the experiment template. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="experiment_report_configuration" type="object">
  Configures the delivery of the experiment report for the FIS experiment template, specifying the S3 destination bucket and prefix as well as the report format and schema version for the generated experiment results. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="data_sources" type="object">
      Specifies the CloudWatch dashboards that are included as data sources in the experiment report generated by this FIS experiment template. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cloud_watch_dashboards" type="list(object)">
          Specifies the CloudWatch dashboards to include as data sources in the experiment report generated by the FIS experiment template. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="dashboard_identifier" type="string">
              Specifies the Amazon Resource Name (ARN) of the CloudWatch dashboard to include as a data source in the experiment report. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="outputs" type="object" required>
      Specifies the output destinations for the experiment report, such as an S3 bucket or a CloudWatch Logs log group, where the FIS experiment report is delivered. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="experiment_report_s3_configuration" type="object" required>
          Configures the S3 bucket and optional key prefix where the executed experiment's report output files are delivered. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_name" type="string" required>
              The name of the S3 bucket to which AWS FIS delivers the experiment report output file. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="prefix" type="string">
              The key prefix used to store the experiment report output files within the specified S3 bucket. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="post_experiment_duration" type="string">
      The duration of time after the experiment ends during which data is collected and included in the experiment report, capturing post-experiment metrics such as recovery or rollback effects. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pre_experiment_duration" type="string">
      The length of time before the experiment's start that is captured in the experiment report, specified as an ISO 8601 duration (for example, PT10M). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="log_configuration" type="object">
  Specifies where to deliver experiment logs, including the destination CloudWatch Logs group and/or S3 bucket for storing the FIS experiment's activity and results. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cloud_watch_logs_configuration" type="object">
      Specifies the CloudWatch Logs destination for FIS experiment logs, including the ARN of the log group where logs are delivered. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="log_group_arn" type="string" required>
          The ARN of the Amazon CloudWatch Logs log group where the experiment template sends its logs. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log_schema_version" type="number" required>
      Specifies the schema version for the FIS experiment logs, which currently supports only version 1. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="s3_configuration" type="object">
      Configures the Amazon S3 destination (bucket and optional prefix) where AWS FIS stores the logs of experiment executions. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="bucket_name" type="string" required>
          The name of the S3 bucket that AWS FIS uses to store experiment report and execution logs. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix" type="string">
          Specifies the prefix to prepend to the log file names when experiment logs are delivered to the configured S3 bucket. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The Amazon Resource Name (ARN) of an IAM role that grants the AWS FIS service permission to perform service actions on your behalf. Required.
</ResponseField>

<ResponseField name="stop_conditions" type="list(object)" required>
  One or more stop conditions. Required.
  <Expandable title="properties">
    <ResponseField name="source" type="string">
      The service source type for the stop condition, such as 'aws:cloudwatch:alarm', which defines the CloudWatch alarm that stops the experiment when it enters the ALARM state. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The Amazon Resource Name (ARN) of the CloudWatch alarm that, when in alarm state, stops the experiment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  Specifies a map of key-value pairs to tag the AWS FIS experiment template, enabling you to organize, identify, and control access to the template using resource tags. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="targets" type="any" required>
  The targets for the experiment. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
