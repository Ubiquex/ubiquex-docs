---
title: "aws_fis_experiment_template"
description: "Real, generated bindings for `aws_fis_experiment_template`."
---

`aws_fis_experiment_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/fis"

    cfg := fis.ExperimentTemplateConfig{
        Description: "example",
        RoleArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(fis.ExperimentTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ExperimentTemplate } from
      "jsr:@ubx/sdk-aws/aws/fis/experiment_template";

    resource(ExperimentTemplate, "example", {
      description: "example",
      roleArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.fis import (
        ExperimentTemplate,
        ExperimentTemplateConfig,
    )

    cfg = ExperimentTemplateConfig(
        description="example",
        role_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(ExperimentTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_fis_experiment_template with description, role_arn,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
