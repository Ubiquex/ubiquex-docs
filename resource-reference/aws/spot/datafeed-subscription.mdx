---
title: "aws_spot_datafeed_subscription"
description: "Real, generated bindings for `aws_spot_datafeed_subscription`."
---

`aws_spot_datafeed_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	spot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/spot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own datafeed subscription"})

    		ubx.Resource(spot.DatafeedSubscription, "ci-runner", spot.DatafeedSubscriptionConfig{
    			Bucket: "example",
    			Prefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatafeedSubscription } from "jsr:@ubx/sdk-aws/aws/spot/datafeed_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own datafeed subscription" });

      resource(DatafeedSubscription, "ci-runner", {
        bucket: "example",
        prefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.spot import DatafeedSubscription, DatafeedSubscriptionConfig

    def describe():
        ubx.intent("payments own datafeed subscription")

        ubx.resource(DatafeedSubscription, "ci-runner", DatafeedSubscriptionConfig(
            bucket="example",
            prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_spot_datafeed_subscription, with bucket example,
    prefix example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
