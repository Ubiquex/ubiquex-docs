---
title: "aws_spot_datafeed_subscription"
description: "Real, generated bindings for `aws_spot_datafeed_subscription`."
---

`aws_spot_datafeed_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/spot"

    cfg := spot.DatafeedSubscriptionConfig{
        Bucket: "example",
        Prefix: "example",
    }
    ubx.Resource(spot.DatafeedSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DatafeedSubscription } from
      "jsr:@ubx/sdk-aws/aws/spot/datafeed_subscription";

    resource(DatafeedSubscription, "example", {
      bucket: "example",
      prefix: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.spot import (
        DatafeedSubscription,
        DatafeedSubscriptionConfig,
    )

    cfg = DatafeedSubscriptionConfig(
        bucket="example",
        prefix="example",
    )
    ubx.resource(DatafeedSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_spot_datafeed_subscription with bucket, prefix.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
