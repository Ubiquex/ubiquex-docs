---
title: "aws_grafana_workspace_service_account_token"
description: "Real, generated bindings for `aws_grafana_workspace_service_account_token`."
---

`aws_grafana_workspace_service_account_token` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/grafana"

    cfg := grafana.WorkspaceServiceAccountTokenConfig{
        Name: "example-name",
        SecondsToLive: 1,
        ServiceAccountId: "example",
        WorkspaceId: "example",
    }
    ubx.Resource(
        grafana.WorkspaceServiceAccountToken,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WorkspaceServiceAccountToken } from
      "jsr:@ubx/sdk-aws/aws/grafana/workspace_service_account_token";

    resource(WorkspaceServiceAccountToken, "example", {
      name: "example",
      secondsToLive: 1,
      serviceAccountId: "example",
      workspaceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.grafana import (
        WorkspaceServiceAccountToken,
        WorkspaceServiceAccountTokenConfig,
    )

    cfg = WorkspaceServiceAccountTokenConfig(
        name="example",
        seconds_to_live=1,
        service_account_id="example",
        workspace_id="example",
    )
    ubx.resource(WorkspaceServiceAccountToken, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_grafana_workspace_service_account_token with name,
seconds_to_live, service_account_id, workspace_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="seconds_to_live" type="number" required>
  Required.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expires_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_account_token_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
