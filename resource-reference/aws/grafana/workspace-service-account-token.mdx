---
title: "aws_grafana_workspace_service_account_token"
description: "Real, generated bindings for `aws_grafana_workspace_service_account_token`."
---

`aws_grafana_workspace_service_account_token` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	grafana "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/grafana"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workspace service account token"})

    		ubx.Resource(grafana.WorkspaceServiceAccountToken, "ci-runner", grafana.WorkspaceServiceAccountTokenConfig{
    			Name:             "ci-runner",
    			SecondsToLive:    7200,
    			ServiceAccountId: "example",
    			WorkspaceId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkspaceServiceAccountToken } from "jsr:@ubx/sdk-aws/aws/grafana/workspace_service_account_token";

    export default stack("payments", () => {
      intent({ summary: "payments own workspace service account token" });

      resource(WorkspaceServiceAccountToken, "ci-runner", {
        name: "ci-runner",
        secondsToLive: 7200,
        serviceAccountId: "example",
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.grafana import WorkspaceServiceAccountToken, WorkspaceServiceAccountTokenConfig

    def describe():
        ubx.intent("payments own workspace service account token")

        ubx.resource(WorkspaceServiceAccountToken, "ci-runner", WorkspaceServiceAccountTokenConfig(
            name="ci-runner",
            seconds_to_live=7200,
            service_account_id="example",
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_grafana_workspace_service_account_token called
    "ci-runner", with seconds to live 7200, service account id
    example, workspace id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="seconds_to_live" type="number" required>
  Required.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expires_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_account_token_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
