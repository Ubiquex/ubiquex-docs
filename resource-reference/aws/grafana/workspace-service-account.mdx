---
title: "aws_grafana_workspace_service_account"
description: "Real, generated bindings for `aws_grafana_workspace_service_account`."
---

`aws_grafana_workspace_service_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/grafana"

    cfg := grafana.WorkspaceServiceAccountConfig{
        GrafanaRole: "example",
        Name: "example-name",
        WorkspaceId: "example",
    }
    ubx.Resource(grafana.WorkspaceServiceAccount, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WorkspaceServiceAccount } from
      "jsr:@ubx/sdk-aws/aws/grafana/workspace_service_account";

    resource(WorkspaceServiceAccount, "example", {
      grafanaRole: "example",
      name: "example",
      workspaceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.grafana import (
        WorkspaceServiceAccount,
        WorkspaceServiceAccountConfig,
    )

    cfg = WorkspaceServiceAccountConfig(
        grafana_role="example",
        name="example",
        workspace_id="example",
    )
    ubx.resource(WorkspaceServiceAccount, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_grafana_workspace_service_account with
grafana_role, name, workspace_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="grafana_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
