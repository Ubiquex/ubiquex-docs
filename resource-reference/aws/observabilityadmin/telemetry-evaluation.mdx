---
title: "aws_observabilityadmin_telemetry_evaluation"
description: "Real, generated bindings for `aws_observabilityadmin_telemetry_evaluation`."
---

`aws_observabilityadmin_telemetry_evaluation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	observabilityadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own telemetry evaluation"})

    		ubx.Resource(observabilityadmin.TelemetryEvaluation, "ci-runner", observabilityadmin.TelemetryEvaluationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TelemetryEvaluation } from "jsr:@ubx/sdk-aws/aws/observabilityadmin/telemetry_evaluation";

    export default stack("payments", () => {
      intent({ summary: "payments own telemetry evaluation" });

      resource(TelemetryEvaluation, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.observabilityadmin import TelemetryEvaluation, TelemetryEvaluationConfig

    def describe():
        ubx.intent("payments own telemetry evaluation")

        ubx.resource(TelemetryEvaluation, "ci-runner", TelemetryEvaluationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_observabilityadmin_telemetry_evaluation, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="failure_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
