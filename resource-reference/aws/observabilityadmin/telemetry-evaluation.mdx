---
title: "aws_observabilityadmin_telemetry_evaluation"
description: "Real, generated bindings for `aws_observabilityadmin_telemetry_evaluation`."
---

`aws_observabilityadmin_telemetry_evaluation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"

    cfg := observabilityadmin.TelemetryEvaluationConfig{
    }
    ubx.Resource(
        observabilityadmin.TelemetryEvaluation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TelemetryEvaluation } from
      "jsr:@ubx/sdk-aws/aws/observabilityadmin/telemetry_evaluation";

    resource(TelemetryEvaluation, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.observabilityadmin import (
        TelemetryEvaluation,
        TelemetryEvaluationConfig,
    )

    cfg = TelemetryEvaluationConfig(
    )
    ubx.resource(TelemetryEvaluation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_observabilityadmin_telemetry_evaluation.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="failure_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
