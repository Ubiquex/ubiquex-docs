---
title: "aws_observabilityadmin_telemetry_pipeline"
description: "Real, generated bindings for `aws_observabilityadmin_telemetry_pipeline`."
---

`aws_observabilityadmin_telemetry_pipeline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"

    cfg := observabilityadmin.TelemetryPipelineConfig{
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        observabilityadmin.TelemetryPipeline,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TelemetryPipeline } from
      "jsr:@ubx/sdk-aws/aws/observabilityadmin/telemetry_pipeline";

    resource(TelemetryPipeline, "example", {
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.observabilityadmin import (
        TelemetryPipeline,
        TelemetryPipelineConfig,
    )

    cfg = TelemetryPipelineConfig(
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(TelemetryPipeline, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_observabilityadmin_telemetry_pipeline with name,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
