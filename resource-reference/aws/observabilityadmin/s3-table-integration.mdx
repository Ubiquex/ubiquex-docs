---
title: "aws_observabilityadmin_s3_table_integration"
description: "Real, generated bindings for `aws_observabilityadmin_s3_table_integration`."
---

`aws_observabilityadmin_s3_table_integration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"

    cfg := observabilityadmin.S3TableIntegrationConfig{
        RoleArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        observabilityadmin.S3TableIntegration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { S3TableIntegration } from
      "jsr:@ubx/sdk-aws/aws/observabilityadmin/s3_table_integration";

    resource(S3TableIntegration, "example", {
      roleArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.observabilityadmin import (
        S3TableIntegration,
        S3TableIntegrationConfig,
    )

    cfg = S3TableIntegrationConfig(
        role_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(S3TableIntegration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_observabilityadmin_s3_table_integration with
role_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="destination_table_bucket_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
