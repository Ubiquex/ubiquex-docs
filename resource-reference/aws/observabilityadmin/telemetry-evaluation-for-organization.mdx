---
title: "aws_observabilityadmin_telemetry_evaluation_for_organization"
description: "Real, generated bindings for `aws_observabilityadmin_telemetry_evaluation_for_organization`."
---

`aws_observabilityadmin_telemetry_evaluation_for_organization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import obse "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"

    cfg := obse.TelemetryEvaluationForOrganizationConfig{
        AllRegions: true,
        Regions: []string{"example"},
    }
    ubx.Resource(
        obse.TelemetryEvaluationForOrganization,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TelemetryEvaluationForOrganization } from
      "jsr:@ubx/sdk-aws/aws/observabilityadmin/telemetry_evaluation_for_organization";

    resource(TelemetryEvaluationForOrganization, "example", {
      allRegions: true,
      regions: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.observabilityadmin import (
        TelemetryEvaluationForOrganization,
        TelemetryEvaluationForOrganizationConfig,
    )

    cfg = TelemetryEvaluationForOrganizationConfig(
        all_regions=True,
        regions=["example"],
    )
    ubx.resource(TelemetryEvaluationForOrganization, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision
aws_observabilityadmin_telemetry_evaluation_for_organization with
all_regions, regions.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="all_regions" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regions" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="failure_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
