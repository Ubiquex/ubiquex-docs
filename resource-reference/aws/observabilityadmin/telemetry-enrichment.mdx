---
title: "aws_observabilityadmin_telemetry_enrichment"
description: "Real, generated bindings for `aws_observabilityadmin_telemetry_enrichment`."
---

`aws_observabilityadmin_telemetry_enrichment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"

    cfg := observabilityadmin.TelemetryEnrichmentConfig{
    }
    ubx.Resource(
        observabilityadmin.TelemetryEnrichment,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TelemetryEnrichment } from
      "jsr:@ubx/sdk-aws/aws/observabilityadmin/telemetry_enrichment";

    resource(TelemetryEnrichment, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.observabilityadmin import (
        TelemetryEnrichment,
        TelemetryEnrichmentConfig,
    )

    cfg = TelemetryEnrichmentConfig(
    )
    ubx.resource(TelemetryEnrichment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_observabilityadmin_telemetry_enrichment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="aws_resource_explorer_managed_view_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
