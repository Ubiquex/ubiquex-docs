---
title: "aws_observabilityadmin_telemetry_enrichment"
description: "Real, generated bindings for `aws_observabilityadmin_telemetry_enrichment`."
---

`aws_observabilityadmin_telemetry_enrichment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	observabilityadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/observabilityadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own telemetry enrichment"})

    		ubx.Resource(observabilityadmin.TelemetryEnrichment, "ci-runner", observabilityadmin.TelemetryEnrichmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TelemetryEnrichment } from "jsr:@ubx/sdk-aws/aws/observabilityadmin/telemetry_enrichment";

    export default stack("payments", () => {
      intent({ summary: "payments own telemetry enrichment" });

      resource(TelemetryEnrichment, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.observabilityadmin import TelemetryEnrichment, TelemetryEnrichmentConfig

    def describe():
        ubx.intent("payments own telemetry enrichment")

        ubx.resource(TelemetryEnrichment, "ci-runner", TelemetryEnrichmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_observabilityadmin_telemetry_enrichment, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="aws_resource_explorer_managed_view_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
