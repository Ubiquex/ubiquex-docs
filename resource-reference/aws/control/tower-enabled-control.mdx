---
title: "aws_control_tower_enabled_control"
description: "`aws_control_tower_enabled_control` turns on a real, high-level governance rule, preventive, detective, or proactive..."
---

`aws_control_tower_enabled_control` turns on a real, high-level governance rule, preventive, detective, or proactive, against an organizational unit or account in an `aws_control_tower_landing_zone`, the actual mechanism that keeps an environment from drifting away from its intended security and compliance baseline.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tower enabled control"})

    		ubx.Resource(control.TowerEnabledControl, "ci-runner", control.TowerEnabledControlConfig{
    			ControlIdentifier: "example",
    			TargetIdentifier:  "example",
    			Parameters:        []map[string]any{{"key": "example"}},
    			Tags:              []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TowerEnabledControl } from "./local-sdk/aws/sdk/typescript/aws/control/tower_enabled_control.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own tower enabled control" });

      resource(TowerEnabledControl, "ci-runner", {
        controlIdentifier: "example",
        targetIdentifier: "example",
        parameters: [{ key: "example" }],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.control import TowerEnabledControl, TowerEnabledControlConfig

    def describe():
        ubx.intent("payments own tower enabled control")

        ubx.resource(TowerEnabledControl, "ci-runner", TowerEnabledControlConfig(
            control_identifier="example",
            target_identifier="example",
            parameters=[{"key": "example"}],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_control_tower_enabled_control, with control identifier
    example, target identifier example, parameters a set value, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="control_identifier" type="string" required>
  Arn of the control. Required.
</ResponseField>

<ResponseField name="parameters" type="list(object)">
  Parameters to configure the enabled control behavior. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of a parameter to supply when enabling the AWS Control Tower control, which identifies the specific configuration option being set. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="any">
      Assigns the value for a Control Tower enabled-control parameter key (e.g., the tag key or allowed region list) that configures the behavior of the control being enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A set of tags to assign to the enabled control. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Control Tower enabled control, enabling you to organize and identify the control with custom metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_identifier" type="string" required>
  Arn for Organizational unit to which the control needs to be applied Required.
</ResponseField>
