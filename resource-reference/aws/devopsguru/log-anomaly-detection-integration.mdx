---
title: "aws_dev_ops_guru_log_anomaly_detection_integration"
description: "`aws_dev_ops_guru_log_anomaly_detection_integration` enables real log-based anomaly detection for an `aws_dev_ops_guru_resource_collection`..."
---

`aws_dev_ops_guru_log_anomaly_detection_integration` enables real log-based anomaly detection for an `aws_dev_ops_guru_resource_collection`, letting DevOps Guru correlate unusual log patterns alongside its own existing metric-based anomaly detection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devopsguru "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/devopsguru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ops guru log anomaly detection integration"})

    		ubx.Resource(devopsguru.LogAnomalyDetectionIntegration, "example", devopsguru.LogAnomalyDetectionIntegrationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogAnomalyDetectionIntegration } from "@ubx/sdk-aws/aws/devopsguru/log_anomaly_detection_integration";

    export default stack("example", () => {
      intent({ summary: "example own ops guru log anomaly detection integration" });

      resource(LogAnomalyDetectionIntegration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devopsguru.log_anomaly_detection_integration import LogAnomalyDetectionIntegration, LogAnomalyDetectionIntegrationConfig

    def describe():
        ubx.intent("example own ops guru log anomaly detection integration")

        ubx.resource(LogAnomalyDetectionIntegration, "example", LogAnomalyDetectionIntegrationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dev_ops_guru_log_anomaly_detection_integration, with .
    ```
  </Tab>
</Tabs>

## Input properties



## Output properties

<ResponseField name="account_id" type="string">
  User account id, used as the primary identifier for the resource Computed by AWS after `ship`.
</ResponseField>
