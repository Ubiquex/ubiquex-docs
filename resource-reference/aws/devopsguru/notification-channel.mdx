---
title: "aws_devopsguru_notification_channel"
description: "Real, generated bindings for `aws_devopsguru_notification_channel`."
---

`aws_devopsguru_notification_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/devopsguru"

    cfg := devopsguru.NotificationChannelConfig{
    }
    ubx.Resource(devopsguru.NotificationChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NotificationChannel } from
      "jsr:@ubx/sdk-aws/aws/devopsguru/notification_channel";

    resource(NotificationChannel, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.devopsguru import (
        NotificationChannel,
        NotificationChannelConfig,
    )

    cfg = NotificationChannelConfig(
    )
    ubx.resource(NotificationChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_devopsguru_notification_channel.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
