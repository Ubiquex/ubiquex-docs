---
title: "aws_dev_ops_guru_resource_collection"
description: "`aws_dev_ops_guru_resource_collection` scopes which real AWS resources, identified by CloudFormation stack or by tag..."
---

`aws_dev_ops_guru_resource_collection` scopes which real AWS resources, identified by CloudFormation stack or by tag, DevOps Guru actually analyzes CloudWatch metrics and CloudTrail events for, so anomaly detection covers the resources that matter rather than every resource in the account indiscriminately.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devopsguru "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/devopsguru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own resource collection"})

    		ubx.Resource(devopsguru.ResourceCollection, "example", devopsguru.ResourceCollectionConfig{
    			ResourceCollectionFilter: map[string]any{"cloud_formation": map[string]any{"stack_names": []string{"example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceCollection } from "@ubx/sdk-aws/aws/devopsguru/resource_collection";

    export default stack("example", () => {
      intent({ summary: "example own resource collection" });

      resource(ResourceCollection, "example", {
        resourceCollectionFilter: { cloudFormation: { stackNames: ["example"] } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devopsguru import ResourceCollection, ResourceCollectionConfig
    from ubx.aws.devopsguru.resource_collection import ResourceCollection_ResourceCollectionFilter, ResourceCollection_ResourceCollectionFilter_CloudFormation

    def describe():
        ubx.intent("example own resource collection")

        ubx.resource(ResourceCollection, "example", ResourceCollectionConfig(
            resource_collection_filter=ResourceCollection_ResourceCollectionFilter(cloud_formation=ResourceCollection_ResourceCollectionFilter_CloudFormation(stack_names=["example"])),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dev_ops_guru_resource_collection, with resource
    collection filter a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="resource_collection_filter" type="object" required>
  Information about a filter used to specify which AWS resources are analyzed for anomalous behavior by DevOps Guru. Required.
  <Expandable title="properties">
    <ResponseField name="cloud_formation" type="object">
      CloudFormation resource for DevOps Guru to monitor Optional.
      <Expandable title="properties">
        <ResponseField name="stack_names" type="list(string)">
          An array of CloudFormation stack names. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Tagged resources for DevOps Guru to monitor Optional.
      <Expandable title="properties">
        <ResponseField name="app_boundary_key" type="string">
          The tag key (AppBoundaryKey) that DevOps Guru uses to define the application boundary for selecting which tagged AWS resources are included in the resource collection. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tag_values" type="list(string)">
          Specifies the list of tag values for the tag key in the parent `tags` filter, used to match which AWS resources are included in the DevOps Guru resource collection. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="resource_collection_type" type="string">
  The type of ResourceCollection Computed by AWS after `ship`.
</ResponseField>
