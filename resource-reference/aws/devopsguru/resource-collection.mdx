---
title: "aws_devopsguru_resource_collection"
description: "Real, generated bindings for `aws_devopsguru_resource_collection`."
---

`aws_devopsguru_resource_collection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/devopsguru"

    cfg := devopsguru.ResourceCollectionConfig{
        Type: "example",
    }
    ubx.Resource(devopsguru.ResourceCollection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceCollection } from
      "jsr:@ubx/sdk-aws/aws/devopsguru/resource_collection";

    resource(ResourceCollection, "example", {
      type: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.devopsguru import (
        ResourceCollection,
        ResourceCollectionConfig,
    )

    cfg = ResourceCollectionConfig(
        type="example",
    )
    ubx.resource(ResourceCollection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_devopsguru_resource_collection with type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
