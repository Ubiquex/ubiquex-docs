---
title: "aws_devopsguru_resource_collection"
description: "Real, generated bindings for `aws_devopsguru_resource_collection`."
---

`aws_devopsguru_resource_collection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devopsguru "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/devopsguru"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource collection"})

    		ubx.Resource(devopsguru.ResourceCollection, "ci-runner", devopsguru.ResourceCollectionConfig{
    			Type: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ResourceCollection } from "jsr:@ubx/sdk-aws/aws/devopsguru/resource_collection";

    export default stack("payments", () => {
      intent({ summary: "payments own resource collection" });

      resource(ResourceCollection, "ci-runner", {
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devopsguru import ResourceCollection, ResourceCollectionConfig

    def describe():
        ubx.intent("payments own resource collection")

        ubx.resource(ResourceCollection, "ci-runner", ResourceCollectionConfig(
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_devopsguru_resource_collection, with type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloudformation" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="stack_names" type="list(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="app_boundary_key" type="string" required>
    </ResponseField>
    <ResponseField name="tag_values" type="list(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="cloudformation" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="stack_names" type="list(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="app_boundary_key" type="string" required>
    </ResponseField>
    <ResponseField name="tag_values" type="list(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>
