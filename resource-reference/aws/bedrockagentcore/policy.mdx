---
title: "aws_bedrockagentcore_policy"
description: "Real, generated bindings for `aws_bedrockagentcore_policy`."
---

`aws_bedrockagentcore_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.PolicyConfig{
        Name: "example-name",
        PolicyEngineId: "example",
        Description: "example",
        ValidationMode: "example",
    }
    ubx.Resource(bedrockagentcore.Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Policy } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/policy";

    resource(Policy, "example", {
      name: "example",
      policyEngineId: "example",
      description: "example",
      validationMode: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import Policy, PolicyConfig

    cfg = PolicyConfig(
        name="example",
        policy_engine_id="example",
        description="example",
        validation_mode="example",
    )
    ubx.resource(Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_policy with name,
policy_engine_id, description, validation_mode.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_engine_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="validation_mode" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="policy_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
