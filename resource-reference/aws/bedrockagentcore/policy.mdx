---
title: "aws_bedrock_agent_core_policy"
description: "`aws_bedrock_agent_core_policy` defines a real, individual Cedar policy statement within an `aws_bedrock_agent_core_policy_engine`..."
---

`aws_bedrock_agent_core_policy` defines a real, individual Cedar policy statement within an `aws_bedrock_agent_core_policy_engine`, controlling which tool an agent may call, under which conditions, with which arguments.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own agent core policy"})

    		ubx.Resource(bedrockagentcore.Policy, "example", bedrockagentcore.PolicyConfig{
    			Definition:      map[string]any{"cedar": map[string]any{"statement": "example"}},
    			Name:            "example",
    			PolicyEngineId:  "example",
    			Description:     "Managed by ubx.",
    			EnforcementMode: "example",
    			ValidationMode:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Policy } from "@ubx/sdk-aws/aws/bedrockagentcore/policy";

    export default stack("example", () => {
      intent({ summary: "example own agent core policy" });

      resource(Policy, "example", {
        definition: { cedar: { statement: "example" } },
        name: "example",
        policyEngineId: "example",
        description: "Managed by ubx.",
        enforcementMode: "example",
        validationMode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore.policy import Policy, PolicyConfig

    def describe():
        ubx.intent("example own agent core policy")

        ubx.resource(Policy, "example", PolicyConfig(
            definition={"cedar": {"statement": "example"}},
            name="example",
            policy_engine_id="example",
            description="Managed by ubx.",
            enforcement_mode="example",
            validation_mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_agent_core_policy called "example", with
    definition a set value, policy engine id example, description
    Managed by ubx., enforcement mode example, validation mode
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="definition" type="object" required>
  The definition structure for policies. Encapsulates different policy formats. Required.
  <Expandable title="properties">
    <ResponseField name="cedar" type="object">
      A Cedar policy statement within the AgentCore Policy system. Optional.
      <Expandable title="properties">
        <ResponseField name="statement" type="string" required>
          The Cedar policy statement that defines the authorization logic. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="policy" type="object">
      A policy statement within the AgentCore Policy system. Optional.
      <Expandable title="properties">
        <ResponseField name="statement" type="string" required>
          The policy statement. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable description of the policy's purpose and functionality. Optional.
</ResponseField>

<ResponseField name="enforcement_mode" type="string">
  Whether the policy contributes to the enforce decision returned to Gateway. LOG_ONLY policies are still evaluated but their decisions are observed only, allowing customers to validate a policy against real traffic before promoting it. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The customer-assigned immutable name for the policy. Must be unique within the policy engine. Required.
</ResponseField>

<ResponseField name="policy_engine_id" type="string" required>
  The identifier of the policy engine which contains this policy. Required.
</ResponseField>

<ResponseField name="validation_mode" type="string">
  The validation mode for the policy. Determines how Cedar analyzer validation results are handled. Optional.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the policy was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_arn" type="string">
  The Amazon Resource Name (ARN) of the policy. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_id" type="string">
  The unique identifier for the policy. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reasons" type="list(string)">
  Additional information about the policy status. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the policy was last updated. Computed by AWS after `ship`.
</ResponseField>
