---
title: "aws_bedrockagentcore_agent_runtime_endpoint"
description: "Real, generated bindings for `aws_bedrockagentcore_agent_runtime_endpoint`."
---

`aws_bedrockagentcore_agent_runtime_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agent runtime endpoint"})

    		ubx.Resource(bedrockagentcore.AgentRuntimeEndpoint, "ci-runner", bedrockagentcore.AgentRuntimeEndpointConfig{
    			AgentRuntimeId: "example",
    			Name:           "ci-runner",
    			Description:    "Managed by ubx.",
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AgentRuntimeEndpoint } from "jsr:@ubx/sdk-aws/aws/bedrockagentcore/agent_runtime_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own agent runtime endpoint" });

      resource(AgentRuntimeEndpoint, "ci-runner", {
        agentRuntimeId: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import AgentRuntimeEndpoint, AgentRuntimeEndpointConfig

    def describe():
        ubx.intent("payments own agent runtime endpoint")

        ubx.resource(AgentRuntimeEndpoint, "ci-runner", AgentRuntimeEndpointConfig(
            agent_runtime_id="example",
            name="ci-runner",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagentcore_agent_runtime_endpoint called
    "ci-runner", with agent runtime id example, description Managed
    by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="agent_runtime_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="agent_runtime_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="agent_runtime_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="agent_runtime_endpoint_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="agent_runtime_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
