---
title: "aws_bedrockagentcore_agent_runtime_endpoint"
description: "Real, generated bindings for `aws_bedrockagentcore_agent_runtime_endpoint`."
---

`aws_bedrockagentcore_agent_runtime_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrockagentcore"

    cfg := bedrockagentcore.AgentRuntimeEndpointConfig{
        AgentRuntimeId: "example",
        Name: "example-name",
        AgentRuntimeVersion: "example",
        Description: "example",
    }
    ubx.Resource(
        bedrockagentcore.AgentRuntimeEndpoint,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AgentRuntimeEndpoint } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/agent_runtime_endpoint";

    resource(AgentRuntimeEndpoint, "example", {
      agentRuntimeId: "example",
      name: "example",
      agentRuntimeVersion: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        AgentRuntimeEndpoint,
        AgentRuntimeEndpointConfig,
    )

    cfg = AgentRuntimeEndpointConfig(
        agent_runtime_id="example",
        name="example",
        agent_runtime_version="example",
        description="example",
    )
    ubx.resource(AgentRuntimeEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_agent_runtime_endpoint with
agent_runtime_id, name, agent_runtime_version, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="agent_runtime_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="agent_runtime_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="agent_runtime_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="agent_runtime_endpoint_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="agent_runtime_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
