---
title: "aws_bedrockagentcore_memory_strategy"
description: "Real, generated bindings for `aws_bedrockagentcore_memory_strategy`."
---

`aws_bedrockagentcore_memory_strategy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own memory strategy"})

    		ubx.Resource(bedrockagentcore.MemoryStrategy, "ci-runner", bedrockagentcore.MemoryStrategyConfig{
    			MemoryId:               "example",
    			Name:                   "ci-runner",
    			Namespaces:             []string{"example"},
    			Type:                   "example",
    			Description:            "Managed by ubx.",
    			MemoryExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MemoryStrategy } from "jsr:@ubx/sdk-aws/aws/bedrockagentcore/memory_strategy";

    export default stack("payments", () => {
      intent({ summary: "payments own memory strategy" });

      resource(MemoryStrategy, "ci-runner", {
        memoryId: "example",
        name: "ci-runner",
        namespaces: ["example"],
        type: "example",
        description: "Managed by ubx.",
        memoryExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import MemoryStrategy, MemoryStrategyConfig

    def describe():
        ubx.intent("payments own memory strategy")

        ubx.resource(MemoryStrategy, "ci-runner", MemoryStrategyConfig(
            memory_id="example",
            name="ci-runner",
            namespaces=["example"],
            type="example",
            description="Managed by ubx.",
            memory_execution_role_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagentcore_memory_strategy called "ci-runner",
    with memory id example, namespaces a set value, type example,
    description Managed by ubx., memory execution role arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="memory_execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="memory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace_templates" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="namespaces" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="memory_strategy_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="namespace_templates" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="namespaces" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
