---
title: "aws_bedrockagentcore_api_key_credential_provider"
description: "Real, generated bindings for `aws_bedrockagentcore_api_key_credential_provider`."
---

`aws_bedrockagentcore_api_key_credential_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api key credential provider"})

    		ubx.Resource(bedrockagentcore.ApiKeyCredentialProvider, "ci-runner", bedrockagentcore.ApiKeyCredentialProviderConfig{
    			Name:            "ci-runner",
    			ApiKey:          "example",
    			ApiKeyWo:        "example",
    			ApiKeyWoVersion: 1,
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiKeyCredentialProvider } from "jsr:@ubx/sdk-aws/aws/bedrockagentcore/api_key_credential_provider";

    export default stack("payments", () => {
      intent({ summary: "payments own api key credential provider" });

      resource(ApiKeyCredentialProvider, "ci-runner", {
        name: "ci-runner",
        apiKey: "example",
        apiKeyWo: "example",
        apiKeyWoVersion: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import ApiKeyCredentialProvider, ApiKeyCredentialProviderConfig

    def describe():
        ubx.intent("payments own api key credential provider")

        ubx.resource(ApiKeyCredentialProvider, "ci-runner", ApiKeyCredentialProviderConfig(
            name="ci-runner",
            api_key="example",
            api_key_wo="example",
            api_key_wo_version=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagentcore_api_key_credential_provider called
    "ci-runner", with api key example, api key wo example, api key wo
    version 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="api_key_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="api_key_wo_version" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="api_key_secret_arn" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="secret_arn" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="credential_provider_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
