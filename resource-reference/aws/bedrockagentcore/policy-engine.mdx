---
title: "aws_bedrockagentcore_policy_engine"
description: "Real, generated bindings for `aws_bedrockagentcore_policy_engine`."
---

`aws_bedrockagentcore_policy_engine` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.PolicyEngineConfig{
        Name: "example-name",
        Description: "example",
        EncryptionKeyArn: "example",
    }
    ubx.Resource(bedrockagentcore.PolicyEngine, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PolicyEngine } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/policy_engine";

    resource(PolicyEngine, "example", {
      name: "example",
      description: "example",
      encryptionKeyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        PolicyEngine,
        PolicyEngineConfig,
    )

    cfg = PolicyEngineConfig(
        name="example",
        description="example",
        encryption_key_arn="example",
    )
    ubx.resource(PolicyEngine, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_policy_engine with name,
description, encryption_key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="policy_engine_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_engine_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
