---
title: "aws_bedrockagentcore_token_vault_cmk"
description: "Real, generated bindings for `aws_bedrockagentcore_token_vault_cmk`."
---

`aws_bedrockagentcore_token_vault_cmk` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.TokenVaultCmkConfig{
        TokenVaultId: "example",
    }
    ubx.Resource(bedrockagentcore.TokenVaultCmk, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TokenVaultCmk } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/token_vault_cmk";

    resource(TokenVaultCmk, "example", {
      tokenVaultId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        TokenVaultCmk,
        TokenVaultCmkConfig,
    )

    cfg = TokenVaultCmkConfig(
        token_vault_id="example",
    )
    ubx.resource(TokenVaultCmk, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_token_vault_cmk with
token_vault_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="token_vault_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="token_vault_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
