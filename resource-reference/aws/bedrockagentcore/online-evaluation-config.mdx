---
title: "aws_bedrock_agent_core_online_evaluation_config"
description: "`aws_bedrock_agent_core_online_evaluation_config` defines which `aws_bedrock_agent_core_evaluator`s run continuously against live agent sessions as..."
---

`aws_bedrock_agent_core_online_evaluation_config` defines which `aws_bedrock_agent_core_evaluator`s run continuously against live agent sessions as they happen, real, ongoing quality scoring in production rather than only at test time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own online evaluation config"})

    		ubx.Resource(bedrockagentcore.OnlineEvaluationConfig, "example", bedrockagentcore.OnlineEvaluationConfigConfig{
    			DataSourceConfig:           map[string]any{"cloud_watch_logs": map[string]any{"log_group_names": []string{"example"}, "service_names": []string{"example"}}},
    			EvaluationExecutionRoleArn: "arn:aws:iam::123456789012:policy/example",
    			OnlineEvaluationConfigName: "example-online-evaluation-config-name",
    			Rule:                       map[string]any{"sampling_config": map[string]any{"sampling_percentage": 1}},
    			ClusteringConfig:           map[string]any{"frequencies": []string{"example"}},
    			Description:                "Managed by ubx.",
    			Evaluators:                 []map[string]any{{"evaluator_id": "example"}},
    			ExecutionStatus:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OnlineEvaluationConfig } from "@ubx/sdk-aws/aws/bedrockagentcore/online_evaluation_config";

    export default stack("example", () => {
      intent({ summary: "example own online evaluation config" });

      resource(OnlineEvaluationConfig, "example", {
        dataSourceConfig: {
          cloudWatchLogs: { logGroupNames: ["example"], serviceNames: ["example"] },
        },
        evaluationExecutionRoleArn: "arn:aws:iam::123456789012:policy/example",
        onlineEvaluationConfigName: "example-online-evaluation-config-name",
        rule: { samplingConfig: { samplingPercentage: 1 } },
        clusteringConfig: { frequencies: ["example"] },
        description: "Managed by ubx.",
        evaluators: [{ evaluatorId: "example" }],
        executionStatus: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import OnlineEvaluationConfig, OnlineEvaluationConfigConfig
    from ubx.aws.bedrockagentcore.online_evaluation_config import OnlineEvaluationConfig_DataSourceConfig, OnlineEvaluationConfig_DataSourceConfig_CloudWatchLogs, OnlineEvaluationConfig_Rule, OnlineEvaluationConfig_Rule_SamplingConfig, OnlineEvaluationConfig_ClusteringConfig, OnlineEvaluationConfig_Evaluators

    def describe():
        ubx.intent("example own online evaluation config")

        ubx.resource(OnlineEvaluationConfig, "example", OnlineEvaluationConfigConfig(
            data_source_config=OnlineEvaluationConfig_DataSourceConfig(cloud_watch_logs=OnlineEvaluationConfig_DataSourceConfig_CloudWatchLogs(log_group_names=["example"], service_names=["example"])),
            evaluation_execution_role_arn="arn:aws:iam::123456789012:policy/example",
            online_evaluation_config_name="example-online-evaluation-config-name",
            rule=OnlineEvaluationConfig_Rule(sampling_config=OnlineEvaluationConfig_Rule_SamplingConfig(sampling_percentage=1)),
            clustering_config=OnlineEvaluationConfig_ClusteringConfig(frequencies=["example"]),
            description="Managed by ubx.",
            evaluators=[OnlineEvaluationConfig_Evaluators(evaluator_id="example")],
            execution_status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_agent_core_online_evaluation_config, with data
    source config a set value, evaluation execution role arn
    arn:aws:iam::123456789012:policy/example, online evaluation
    config name example-online-evaluation-config-name, rule a set
    value, clustering config a set value, description Managed by
    ubx., evaluators a set value, execution status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="clustering_config" type="object">
  The configuration for clustering analysis of evaluation results. Optional.
  <Expandable title="properties">
    <ResponseField name="frequencies" type="list(string)" required>
      The list of frequencies at which clustering reports are generated. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_source_config" type="object" required>
  The configuration that specifies where to read agent traces for online evaluation. Required.
  <Expandable title="properties">
    <ResponseField name="cloud_watch_logs" type="object" required>
      The configuration for reading agent traces from CloudWatch logs. Required.
      <Expandable title="properties">
        <ResponseField name="log_group_names" type="list(string)" required>
          The list of CloudWatch log group names to monitor for agent traces. Required.
        </ResponseField>
        <ResponseField name="service_names" type="list(string)" required>
          The list of service names to filter traces within the specified log groups. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the online evaluation configuration. Optional.
</ResponseField>

<ResponseField name="evaluation_execution_role_arn" type="string" required>
  The Amazon Resource Name (ARN) of the IAM role that grants permissions for evaluation. Required.
</ResponseField>

<ResponseField name="evaluators" type="list(object)">
  The list of evaluators to apply during online evaluation. Optional.
  <Expandable title="properties">
    <ResponseField name="evaluator_id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="execution_status" type="string">
  Optional.
</ResponseField>

<ResponseField name="insights" type="list(object)">
  The list of insights to enable for failure analysis. Optional.
  <Expandable title="properties">
    <ResponseField name="insight_id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="online_evaluation_config_name" type="string" required>
  The name of the online evaluation configuration. Must be unique within your account. Required.
</ResponseField>

<ResponseField name="rule" type="object" required>
  The evaluation rule that defines sampling configuration, filtering criteria, and session detection settings. Required.
  <Expandable title="properties">
    <ResponseField name="filters" type="list(object)">
      The list of filters that determine which agent traces should be included in the evaluation. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="operator" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="value" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="boolean_value" type="bool">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="double_value" type="number">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="string_value" type="string">
              Provides the string to match in a rule filter for the Bedrock Agent Core online evaluation configuration, when the filter's value type is STRING, to determine which interactions are evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sampling_config" type="object" required>
      The configuration that controls what percentage of agent traces are sampled for evaluation. Required.
      <Expandable title="properties">
        <ResponseField name="sampling_percentage" type="number" required>
          The percentage of agent traces to sample for evaluation. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="session_config" type="object">
      The configuration that defines how agent sessions are detected. Optional.
      <Expandable title="properties">
        <ResponseField name="session_timeout_minutes" type="number" required>
          The number of minutes of inactivity after which an agent session is considered complete. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tags to assign to the online evaluation configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Bedrock agent core online evaluation configuration, used to organize and identify the resource by custom metadata such as environment or project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The timestamp when the online evaluation configuration was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="online_evaluation_config_arn" type="string">
  The Amazon Resource Name (ARN) of the online evaluation configuration. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="online_evaluation_config_id" type="string">
  The unique identifier of the online evaluation configuration. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="output_config" type="object">
  The configuration that specifies where evaluation results should be written. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="cloud_watch_config" type="object">
      The CloudWatch configuration for writing evaluation results. Optional.
      <Expandable title="properties">
        <ResponseField name="log_group_name" type="string">
          The CloudWatch log group name for evaluation results. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the online evaluation configuration was last updated. Computed by AWS after `ship`.
</ResponseField>
