---
title: "aws_bedrockagentcore_gateway_target"
description: "Real, generated bindings for `aws_bedrockagentcore_gateway_target`."
---

`aws_bedrockagentcore_gateway_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.GatewayTargetConfig{
        GatewayIdentifier: "example",
        Name: "example-name",
        Description: "example",
    }
    ubx.Resource(bedrockagentcore.GatewayTarget, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayTarget } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/gateway_target";

    resource(GatewayTarget, "example", {
      gatewayIdentifier: "example",
      name: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        GatewayTarget,
        GatewayTargetConfig,
    )

    cfg = GatewayTargetConfig(
        gateway_identifier="example",
        name="example",
        description="example",
    )
    ubx.resource(GatewayTarget, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_gateway_target with
gateway_identifier, name, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="gateway_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
