---
title: "aws_bedrockagentcore_oauth2_credential_provider"
description: "Real, generated bindings for `aws_bedrockagentcore_oauth2_credential_provider`."
---

`aws_bedrockagentcore_oauth2_credential_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.Oauth2CredentialProviderConfig{
        CredentialProviderVendor: "example",
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        bedrockagentcore.Oauth2CredentialProvider,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Oauth2CredentialProvider } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/oauth2_credential_provider";

    resource(Oauth2CredentialProvider, "example", {
      credentialProviderVendor: "example",
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        Oauth2CredentialProvider,
        Oauth2CredentialProviderConfig,
    )

    cfg = Oauth2CredentialProviderConfig(
        credential_provider_vendor="example",
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(Oauth2CredentialProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_oauth2_credential_provider with
credential_provider_vendor, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="credential_provider_vendor" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="client_secret_arn" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="secret_arn" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="credential_provider_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
