---
title: "aws_bedrockagentcore_gateway"
description: "Real, generated bindings for `aws_bedrockagentcore_gateway`."
---

`aws_bedrockagentcore_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrockagentcore"

    cfg := bedrockagentcore.GatewayConfig{
        AuthorizerType: "example",
        Name: "example-name",
        RoleArn: "example",
        Description: "example",
        ExceptionLevel: "example",
    }
    ubx.Resource(bedrockagentcore.Gateway, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Gateway } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/gateway";

    resource(Gateway, "example", {
      authorizerType: "example",
      name: "example",
      roleArn: "example",
      description: "example",
      exceptionLevel: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import Gateway, GatewayConfig

    cfg = GatewayConfig(
        authorizer_type="example",
        name="example",
        role_arn="example",
        description="example",
        exception_level="example",
    )
    ubx.resource(Gateway, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_gateway with authorizer_type,
name, role_arn, description, exception_level.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorizer_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="exception_level" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="protocol_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="gateway_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="gateway_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="gateway_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="protocol_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="workload_identity_details" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="workload_identity_arn" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
