---
title: "aws_bedrockagentcore_browser"
description: "Real, generated bindings for `aws_bedrockagentcore_browser`."
---

`aws_bedrockagentcore_browser` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.BrowserConfig{
        Name: "example-name",
        Description: "example",
        ExecutionRoleArn: "example",
    }
    ubx.Resource(bedrockagentcore.Browser, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Browser } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/browser";

    resource(Browser, "example", {
      name: "example",
      description: "example",
      executionRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import Browser, BrowserConfig

    cfg = BrowserConfig(
        name="example",
        description="example",
        execution_role_arn="example",
    )
    ubx.resource(Browser, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_browser with name, description,
execution_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="browser_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="browser_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
