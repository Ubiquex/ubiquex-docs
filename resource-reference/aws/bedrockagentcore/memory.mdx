---
title: "aws_bedrockagentcore_memory"
description: "Real, generated bindings for `aws_bedrockagentcore_memory`."
---

`aws_bedrockagentcore_memory` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own memory"})

    		ubx.Resource(bedrockagentcore.Memory, "ci-runner", bedrockagentcore.MemoryConfig{
    			EventExpiryDuration:    7200,
    			Name:                   "ci-runner",
    			Description:            "Managed by ubx.",
    			EncryptionKeyArn:       "arn:aws:iam::123456789012:policy/ci",
    			MemoryExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Memory } from "jsr:@ubx/sdk-aws/aws/bedrockagentcore/memory";

    export default stack("payments", () => {
      intent({ summary: "payments own memory" });

      resource(Memory, "ci-runner", {
        eventExpiryDuration: 7200,
        name: "ci-runner",
        description: "Managed by ubx.",
        encryptionKeyArn: "arn:aws:iam::123456789012:policy/ci",
        memoryExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import Memory, MemoryConfig

    def describe():
        ubx.intent("payments own memory")

        ubx.resource(Memory, "ci-runner", MemoryConfig(
            event_expiry_duration=7200,
            name="ci-runner",
            description="Managed by ubx.",
            encryption_key_arn="arn:aws:iam::123456789012:policy/ci",
            memory_execution_role_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagentcore_memory called "ci-runner", with event
    expiry duration 7200, description Managed by ubx., encryption key
    arn arn:aws:iam::123456789012:policy/ci, memory execution role
    arn arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_expiry_duration" type="number" required>
  Required.
</ResponseField>

<ResponseField name="memory_execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
