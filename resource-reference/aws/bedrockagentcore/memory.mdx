---
title: "aws_bedrockagentcore_memory"
description: "Real, generated bindings for `aws_bedrockagentcore_memory`."
---

`aws_bedrockagentcore_memory` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.MemoryConfig{
        EventExpiryDuration: 1,
        Name: "example-name",
        Description: "example",
        EncryptionKeyArn: "example",
    }
    ubx.Resource(bedrockagentcore.Memory, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Memory } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/memory";

    resource(Memory, "example", {
      eventExpiryDuration: 1,
      name: "example",
      description: "example",
      encryptionKeyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import Memory, MemoryConfig

    cfg = MemoryConfig(
        event_expiry_duration=1,
        name="example",
        description="example",
        encryption_key_arn="example",
    )
    ubx.resource(Memory, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_memory with event_expiry_duration,
name, description, encryption_key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_expiry_duration" type="number" required>
  Required.
</ResponseField>

<ResponseField name="memory_execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
