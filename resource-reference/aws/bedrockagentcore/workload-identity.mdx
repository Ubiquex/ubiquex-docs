---
title: "aws_bedrockagentcore_workload_identity"
description: "Real, generated bindings for `aws_bedrockagentcore_workload_identity`."
---

`aws_bedrockagentcore_workload_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrockagentcore"

    cfg := bedrockagentcore.WorkloadIdentityConfig{
        Name: "example-name",
        AllowedResourceOauth2ReturnUrls: []string{"example"},
    }
    ubx.Resource(bedrockagentcore.WorkloadIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WorkloadIdentity } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/workload_identity";

    resource(WorkloadIdentity, "example", {
      name: "example",
      allowedResourceOauth2ReturnUrls: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        WorkloadIdentity,
        WorkloadIdentityConfig,
    )

    cfg = WorkloadIdentityConfig(
        name="example",
        allowed_resource_oauth2_return_urls=["example"],
    )
    ubx.resource(WorkloadIdentity, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_workload_identity with name,
allowed_resource_oauth2_return_urls.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_resource_oauth2_return_urls" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workload_identity_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
