---
title: "aws_bedrockagentcore_workload_identity"
description: "Real, generated bindings for `aws_bedrockagentcore_workload_identity`."
---

`aws_bedrockagentcore_workload_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workload identity"})

    		ubx.Resource(bedrockagentcore.WorkloadIdentity, "ci-runner", bedrockagentcore.WorkloadIdentityConfig{
    			Name:                            "ci-runner",
    			AllowedResourceOauth2ReturnUrls: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkloadIdentity } from "jsr:@ubx/sdk-aws/aws/bedrockagentcore/workload_identity";

    export default stack("payments", () => {
      intent({ summary: "payments own workload identity" });

      resource(WorkloadIdentity, "ci-runner", {
        name: "ci-runner",
        allowedResourceOauth2ReturnUrls: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import WorkloadIdentity, WorkloadIdentityConfig

    def describe():
        ubx.intent("payments own workload identity")

        ubx.resource(WorkloadIdentity, "ci-runner", WorkloadIdentityConfig(
            name="ci-runner",
            allowed_resource_oauth2_return_urls=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagentcore_workload_identity called "ci-runner",
    with allowed resource oauth2 return urls a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_resource_oauth2_return_urls" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workload_identity_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
