---
title: "aws_bedrockagentcore_evaluator"
description: "Real, generated bindings for `aws_bedrockagentcore_evaluator`."
---

`aws_bedrockagentcore_evaluator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.EvaluatorConfig{
        EvaluatorName: "example-evaluator-name",
        Level: "example",
        Description: "example",
        KmsKeyArn: "example",
    }
    ubx.Resource(bedrockagentcore.Evaluator, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Evaluator } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/evaluator";

    resource(Evaluator, "example", {
      evaluatorName: "example",
      level: "example",
      description: "example",
      kmsKeyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import Evaluator, EvaluatorConfig

    cfg = EvaluatorConfig(
        evaluator_name="example",
        level="example",
        description="example",
        kms_key_arn="example",
    )
    ubx.resource(Evaluator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_evaluator with evaluator_name,
level, description, kms_key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="evaluator_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="level" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="evaluator_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="evaluator_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="locked_for_modification" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
