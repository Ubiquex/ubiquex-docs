---
title: "aws_bedrockagentcore_code_interpreter"
description: "Real, generated bindings for `aws_bedrockagentcore_code_interpreter`."
---

`aws_bedrockagentcore_code_interpreter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"

    cfg := bedrockagentcore.CodeInterpreterConfig{
        Name: "example-name",
        Description: "example",
        ExecutionRoleArn: "example",
    }
    ubx.Resource(bedrockagentcore.CodeInterpreter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CodeInterpreter } from
      "jsr:@ubx/sdk-aws/aws/bedrockagentcore/code_interpreter";

    resource(CodeInterpreter, "example", {
      name: "example",
      description: "example",
      executionRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrockagentcore import (
        CodeInterpreter,
        CodeInterpreterConfig,
    )

    cfg = CodeInterpreterConfig(
        name="example",
        description="example",
        execution_role_arn="example",
    )
    ubx.resource(CodeInterpreter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrockagentcore_code_interpreter with name,
description, execution_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="code_interpreter_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="code_interpreter_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
