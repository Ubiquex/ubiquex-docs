---
title: "aws_clean_rooms_membership"
description: "`aws_clean_rooms_membership` is the real resource an invited AWS account creates to actually join an `aws_clean_rooms_collaboration` it's been invited..."
---

`aws_clean_rooms_membership` is the real resource an invited AWS account creates to actually join an `aws_clean_rooms_collaboration` it's been invited to, distinct from the collaboration itself, which only the creating account manages directly.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	clean "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/clean"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rooms membership"})

    		ubx.Resource(clean.RoomsMembership, "ci-runner", clean.RoomsMembershipConfig{
    			CollaborationIdentifier:       "example",
    			QueryLogStatus:                "example",
    			DefaultJobResultConfiguration: map[string]any{"output_configuration": map[string]any{"s3": map[string]any{"bucket": "example"}}},
    			DefaultResultConfiguration:    map[string]any{"output_configuration": map[string]any{"s3": map[string]any{"bucket": "example"}}},
    			IsMetricsEnabled:              true,
    			JobLogStatus:                  "example",
    			PaymentConfiguration:          map[string]any{"query_compute": map[string]any{"is_responsible": true}},
    			Tags:                          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoomsMembership } from "./local-sdk/aws/sdk/typescript/aws/clean/rooms_membership.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own rooms membership" });

      resource(RoomsMembership, "ci-runner", {
        collaborationIdentifier: "example",
        queryLogStatus: "example",
        defaultJobResultConfiguration: {
          outputConfiguration: { s3: { bucket: "example" } },
        },
        defaultResultConfiguration: {
          outputConfiguration: { s3: { bucket: "example" } },
        },
        isMetricsEnabled: true,
        jobLogStatus: "example",
        paymentConfiguration: { queryCompute: { isResponsible: true } },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.clean import RoomsMembership, RoomsMembershipConfig

    def describe():
        ubx.intent("payments own rooms membership")

        ubx.resource(RoomsMembership, "ci-runner", RoomsMembershipConfig(
            collaboration_identifier="example",
            query_log_status="example",
            default_job_result_configuration={"output_configuration": {"s3": {"bucket": "example"}}},
            default_result_configuration={"output_configuration": {"s3": {"bucket": "example"}}},
            is_metrics_enabled=True,
            job_log_status="example",
            payment_configuration={"query_compute": {"is_responsible": True}},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_clean_rooms_membership, with collaboration identifier
    example, query log status example, default job result
    configuration a set value, default result configuration a set
    value, is metrics enabled true, job log status example, payment
    configuration a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="collaboration_identifier" type="string" required>
  The unique identifier of the AWS Clean Rooms collaboration that this membership is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="default_job_result_configuration" type="object">
  Specifies the default configuration for storing query results, including the S3 output location and the IAM role used to write results for jobs run by this membership. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="output_configuration" type="object" required>
      Specifies the S3 bucket (and optional key prefix) that serves as the default destination for query results when running jobs in this membership. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="s3" type="object" required>
          The S3 output configuration for the default job result configuration, specifying the Amazon S3 bucket (and optional key prefix) where analysis job results are delivered. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket" type="string" required>
              The name of the S3 bucket where the default job results are delivered, as part of the membership's default job result output configuration. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_prefix" type="string">
              The S3 key prefix under which the default analysis job result objects are stored in the specified output S3 bucket. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role_arn" type="string" required>
      The ARN of the IAM role that AWS Clean Rooms assumes to write query results to the S3 location configured in the default job result configuration. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_result_configuration" type="object">
  Specifies the default output configuration (such as the S3 bucket and IAM role) used to write query results for the membership. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="output_configuration" type="object" required>
      Specifies the Amazon S3 destination (bucket and optional key prefix) used to store the default query results for this Clean Rooms membership. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="s3" type="object" required>
          Specifies the S3 bucket and optional key prefix where query results for this Clean Rooms membership are stored. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket" type="string" required>
              The name of the S3 bucket to which the default result configuration delivers query results for this AWS Clean Rooms membership. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="key_prefix" type="string">
              The S3 object key prefix that is applied to the output files when the Clean Rooms membership's default result configuration writes query results to the specified S3 bucket. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="result_format" type="string" required>
              Specifies the file format (CSV or PARQUET) for query results written to the S3 bucket and key prefix in the membership's default result configuration. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="single_file_output" type="bool">
              Determines whether the default result configuration writes query output to a single S3 file (true) or multiple files (false). Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="role_arn" type="string">
      The ARN of the IAM role that AWS Clean Rooms assumes to write query results to the default output location configured for the membership. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="is_metrics_enabled" type="bool">
  Whether query metrics are enabled for the Clean Rooms membership, which if true enables Amazon CloudWatch metrics for queries run within the membership. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="job_log_status" type="string">
  Optional.
</ResponseField>

<ResponseField name="payment_configuration" type="object">
  Configures the payment details for the membership, including the mode that determines which member is billed for query compute in the clean room. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="job_compute" type="object">
      Specifies the payment configuration for job compute in the membership, indicating which party (the 'MemberWhoPays' value) is responsible for paying for compute jobs run under the collaboration. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="is_responsible" type="bool" required>
          Indicates whether this Clean Rooms membership is responsible for paying the compute costs for analysis jobs run in the collaboration. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="machine_learning" type="object">
      Configures which party (the member or the collaborator) is responsible for paying for machine learning operations within the AWS Clean Rooms membership, as part of the overarching payment configuration. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="model_inference" type="object">
          Specifies the payment configuration for machine learning model inference jobs in the Clean Rooms membership, including which member is responsible for associated costs. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool" required>
              Indicates whether the member associated with this Clean Rooms membership is responsible for the costs of machine learning model inference under this payment configuration. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_training" type="object">
          Defines the payment responsibility for machine learning model training in the membership's payment configuration, indicating whether the membership is responsible for the associated costs. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool" required>
              Indicates whether the membership owner is responsible for covering the costs incurred by machine learning model training in the collaboration. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="synthetic_data_generation" type="object">
          Configures synthetic data generation for Clean Rooms ML by providing an IsEnabled flag that determines whether the feature is active and thus billing for it is incurred. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool" required>
              Indicates whether the membership is responsible for the costs of synthetic data generation queries in the machine learning payment configuration. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="query_compute" type="object" required>
      This configuration block indicates whether this Clean Rooms membership is responsible for the compute costs associated with running queries in the collaboration. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="is_responsible" type="bool" required>
          Indicates whether this membership is responsible for paying the query compute costs incurred by queries in the collaboration. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="query_log_status" type="string" required>
  Specifies whether query logging is enabled for the Clean Rooms membership, with valid values ENABLED or DISABLED to control retention of query activity records. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this cleanrooms membership. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key portion of a user-defined tag attached to the AWS Clean Rooms membership, used to categorize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      A user-defined tag value attached to the AWS Clean Rooms membership, providing metadata that can be used for categorization, cost allocation, or access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AWS Clean Rooms membership. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collaboration_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS Clean Rooms collaboration that this membership is associated with. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collaboration_creator_account_id" type="string">
  The `collaboration_creator_account_id` attribute returns the AWS account ID of the account that created the collaboration to which this Clean Rooms membership belongs. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_identifier" type="string">
  The unique AWS-assigned identifier for this AWS Clean Rooms membership. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
