---
title: "aws_clean_rooms_collaboration"
description: "`aws_clean_rooms_collaboration` creates the actual secure workspace multiple AWS accounts join to analyze their combined data..."
---

`aws_clean_rooms_collaboration` creates the actual secure workspace multiple AWS accounts join to analyze their combined data, inviting partner accounts and setting the privacy-enhancing controls that govern what any one party can see of another's underlying data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	clean "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/clean"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rooms collaboration"})

    		ubx.Resource(clean.RoomsCollaboration, "example", clean.RoomsCollaborationConfig{
    			CreatorDisplayName:      "example-creator-display-name",
    			Description:             "Managed by ubx.",
    			Name:                    "example",
    			QueryLogStatus:          "example",
    			AllowedResultRegions:    []string{"example"},
    			AnalyticsEngine:         "example",
    			AutoApprovedChangeTypes: []string{"example"},
    			CreatorMemberAbilities:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoomsCollaboration } from "./local-sdk/aws/sdk/typescript/aws/clean/rooms_collaboration.ts";

    export default stack("example", () => {
      intent({ summary: "example own rooms collaboration" });

      resource(RoomsCollaboration, "example", {
        creatorDisplayName: "example-creator-display-name",
        description: "Managed by ubx.",
        name: "example",
        queryLogStatus: "example",
        allowedResultRegions: ["example"],
        analyticsEngine: "example",
        autoApprovedChangeTypes: ["example"],
        creatorMemberAbilities: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.clean import RoomsCollaboration, RoomsCollaborationConfig

    def describe():
        ubx.intent("example own rooms collaboration")

        ubx.resource(RoomsCollaboration, "example", RoomsCollaborationConfig(
            creator_display_name="example-creator-display-name",
            description="Managed by ubx.",
            name="example",
            query_log_status="example",
            allowed_result_regions=["example"],
            analytics_engine="example",
            auto_approved_change_types=["example"],
            creator_member_abilities=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_clean_rooms_collaboration called "example", with
    creator display name example-creator-display-name, description
    Managed by ubx., query log status example, allowed result regions
    a set value, analytics engine example, auto approved change types
    a set value, creator member abilities a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_result_regions" type="list(string)">
  Limits the AWS Regions where members of the collaboration can write query results, restricting result outputs to the specified regions. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="analytics_engine" type="string">
  Specifies the analytics engine for the collaboration, which must be either `CLEAN_ROOMS_SQL` (the default) or `SPARK`, and controls the query engine used by members of the collaboration. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="auto_approved_change_types" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="creator_display_name" type="string" required>
  Specifies the display name of the member who creates the collaboration, which is visible to other members in the collaboration. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="creator_member_abilities" type="list(string)">
  Defines the set of abilities (e.g., CAN_QUERY, CAN_RECEIVE_RESULTS) that are granted to the creator of the collaboration, determining what actions the creator can perform within the Clean Rooms collaboration. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="creator_mlmember_abilities" type="object">
  This object specifies the machine learning abilities granted to the creator member of the AWS Clean Rooms collaboration, determining which custom and standard ML member operations (such as receiving model output or trained models) the creator is permitted to perform. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="custom_mlmember_abilities" type="list(string)" required>
      Specifies a list of custom machine learning member ability names to be granted to the creator member of the AWS Clean Rooms collaboration, defining customized ML permissions beyond the default standard abilities. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="creator_payment_configuration" type="object">
  Determines how the collaboration creator is compensated for query compute costs, including whether the creator accepts responsibility for those costs and the number of query credits provided to collaborating members. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="job_compute" type="object">
      Specifies how the collaboration creator pays for compute resources used by members when running jobs (as opposed to interactive queries) within the clean rooms collaboration. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="is_responsible" type="bool" required>
          Specifies whether the collaboration creator is responsible for paying for job compute costs in the collaboration. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="machine_learning" type="object">
      Specifies the configuration for whether the collaboration creator is responsible for costs incurred by machine learning queries, typically by setting a status like ENABLED or DISABLED. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="model_inference" type="object">
          Configures the payment responsibility for machine learning model inference in the collaboration, indicating whether the collaboration creator is responsible for the resulting charges. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool" required>
              Indicates whether the collaboration creator is responsible for the costs incurred by machine learning model inference. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_training" type="object">
          Configuration specifying whether the collaboration creator is responsible for the costs of training machine learning models. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool" required>
              Indicates whether the creator of the AWS Clean Rooms collaboration is responsible for the cost of training machine learning models under the creator payment configuration. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="synthetic_data_generation" type="object">
          Defines the payment configuration for the synthetic data generation capability under machine learning settings, specifying how the collaboration creator is billed for using this feature in an AWS Clean Rooms collaboration. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool" required>
              Indicates whether the collaboration creator is responsible for the costs associated with synthetic data generation. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="query_compute" type="object" required>
      Specifies the payment configuration for query compute in the collaboration, determining whether the collaboration creator pays for query compute usage. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="is_responsible" type="bool" required>
          Indicates whether the creator of the AWS Clean Rooms collaboration is responsible for the query compute costs. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_encryption_metadata" type="object">
  Specifies the data encryption metadata for the collaboration, controlling whether clear text is allowed, duplicate rows are permitted, joins on columns with different names are allowed, and how null values are preserved. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="allow_cleartext" type="bool" required>
      Indicates whether encrypted tables in the collaboration are allowed to contain cleartext (unencrypted) data. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="allow_duplicates" type="bool" required>
      Specifies whether this collaboration permits query results to contain duplicate rows under the configured encryption metadata. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="allow_joins_on_columns_with_different_names" type="bool" required>
      Controls whether a query in the collaboration can join tables on columns that have different names across different members' datasets. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="preserve_nulls" type="bool" required>
      This required Boolean value determines whether nulls in the underlying data remain as nulls when encrypted in the AWS Clean Rooms collaboration, rather than being encrypted into a non-null value. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string" required>
  The user-provided description of the collaboration, which helps identify its purpose and can be updated after creation. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="is_metrics_enabled" type="bool">
  Whether the collaboration collects metrics about query activity (such as query counts and latency), enabling you to monitor and analyze usage patterns. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="job_log_status" type="string">
  Optional.
</ResponseField>

<ResponseField name="members" type="list(object)">
  Defines the AWS accounts that participate in the collaboration, each with an account ID, display name, and the abilities (such as CAN_QUERY or CAN_RECEIVE_RESULTS) granted to that member. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="account_id" type="string">
      The AWS account ID of the member being added to the Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="display_name" type="string">
      The friendly, human-readable name assigned to a member's AWS account, used to identify that member within the Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="member_abilities" type="list(string)">
      Specifies the list of allowable operations (e.g., CAN_QUERY, CAN_RECEIVE_RESULTS) that this collaboration member is permitted to perform. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mlmember_abilities" type="object">
      Specifies the ML-specific abilities for the member, such as whether they can query ML-protected data and receive results from ML queries. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_mlmember_abilities" type="list(string)">
          Specifies the list of custom machine learning (ML) member abilities granted to this member in the AWS Clean Rooms collaboration, allowing them to perform additional ML operations beyond the predefined ML member abilities. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="payment_configuration" type="object">
      Configures how the member pays for query compute in the collaboration, specifically indicating whether the member is responsible for query compute costs via the query_compute.is_responsible property. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="job_compute" type="object">
          The `job_compute` object within a member's `payment_configuration` specifies which party pays for the compute used when this member runs analysis jobs, with its `type` field indicating either `MEMBER` (the member pays) or `COLLABORATOR` (the collaboration creator pays). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool">
              Indicates whether this member is responsible for paying the compute costs for job compute queries in the AWS Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="machine_learning" type="object">
          Determines how this member pays for machine learning tasks (model training and inference) within the AWS Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="model_inference" type="object">
              Specifies the payment configuration for machine learning model inference activity, typically including the payment amount (in USD) that a collaboration member pays for each model inference operation run in the collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="is_responsible" type="bool">
                  Indicates whether this member is the payer (responsible party) for costs incurred from machine learning model inference queries in the AWS Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="model_training" type="object">
              Payment configuration for machine learning model training, indicating whether the member is responsible for the costs of model training in the AWS Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="is_responsible" type="bool">
                  Indicates whether this member is responsible for paying for the costs associated with training the machine learning model in the collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="synthetic_data_generation" type="object">
              An object that specifies whether this collaboration member enables synthetic data generation jobs and which party (the collaboration creator or the member) is responsible for paying for those jobs. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="is_responsible" type="bool">
                  Indicates whether this collaboration member is responsible for payment for synthetic data generation under the machine learning payment configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="query_compute" type="object">
          Specifies whether this collaboration member is responsible for paying the compute costs for queries run in the collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="is_responsible" type="bool">
              Indicates whether this member is responsible for paying the query compute costs incurred by the collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the collaboration, a user-defined friendly identifier that must be unique within the AWS account and can be up to 128 characters. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="query_log_status" type="string" required>
  Determines whether query logs are enabled for the collaboration, accepting the enum values ENABLED or DISABLED. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this cleanrooms collaboration. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) uniquely identifying the Clean Rooms collaboration. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collaboration_identifier" type="string">
  The unique identifier assigned by AWS to the collaboration. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
