---
title: "aws_clean_rooms_intermediate_table"
description: "`aws_clean_rooms_intermediate_table` stores the real, intermediate output of a multi-step Clean Rooms analysis..."
---

`aws_clean_rooms_intermediate_table` stores the real, intermediate output of a multi-step Clean Rooms analysis, populated by one query and then queryable by a further, real analysis rule of its own, rather than every step needing to reprocess from each member's original configured table.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clean "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/clean"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rooms intermediate table"})

    		ubx.Resource(clean.RoomsIntermediateTable, "example", clean.RoomsIntermediateTableConfig{
    			MembershipIdentifier:            "example",
    			Name:                            "example",
    			PopulationAnalysisConfiguration: map[string]any{"sql_parameters": map[string]any{"analysis_template_arn": "example"}},
    			AnalysisRules:                   []map[string]any{{"policy": map[string]any{"v1": map[string]any{"custom": map[string]any{"additional_analyses": "example"}}}}},
    			Description:                     "Managed by ubx.",
    			KmsKeyArn:                       "arn:aws:iam::123456789012:policy/example",
    			Tags:                            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoomsIntermediateTable } from "@ubx/sdk-aws/aws/clean/rooms_intermediate_table";

    export default stack("example", () => {
      intent({ summary: "example own rooms intermediate table" });

      resource(RoomsIntermediateTable, "example", {
        membershipIdentifier: "example",
        name: "example",
        populationAnalysisConfiguration: {
          sqlParameters: { analysisTemplateArn: "example" },
        },
        analysisRules: [{
          policy: { v1: { custom: { additionalAnalyses: "example" } } },
        }],
        description: "Managed by ubx.",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.clean import RoomsIntermediateTable, RoomsIntermediateTableConfig

    def describe():
        ubx.intent("example own rooms intermediate table")

        ubx.resource(RoomsIntermediateTable, "example", RoomsIntermediateTableConfig(
            membership_identifier="example",
            name="example",
            population_analysis_configuration={"sql_parameters": {"analysis_template_arn": "example"}},
            analysis_rules=[{"policy": {"v1": {"custom": {"additional_analyses": "example"}}}}],
            description="Managed by ubx.",
            kms_key_arn="arn:aws:iam::123456789012:policy/example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_clean_rooms_intermediate_table called "example", with
    membership identifier example, population analysis configuration
    a set value, analysis rules a set value, description Managed by
    ubx., kms key arn arn:aws:iam::123456789012:policy/example, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="analysis_rules" type="list(object)">
  Defines the analysis constraints that govern how members can query the intermediate table, including the allowed analysis methods and their configurations. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="policy" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="v1" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="custom" type="object">
              Defines the custom analysis rule variant within the V1 analysis rule policy, specifying which SQL analyses (e.g., SELECT) are allowed and any restrictions on analysis providers or output columns for the table. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="additional_analyses" type="string">
                  This field specifies the ARNs of additional analyses that are allowed to be run on the intermediate table under the custom analysis rule policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allowed_analyses" type="list(string)">
                  Specifies the list of allowed analysis template names (strings) that collaborators may run on the configured table under this custom analysis rule, restricting which analyses are permitted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allowed_analysis_providers" type="list(string)">
                  This list of strings defines the AWS account IDs that are authorized to act as analysis providers for the custom analysis rule, restricting which accounts can run SQL queries against the intermediate table. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allowed_result_receivers" type="list(string)">
                  Specifies the list of member account identifiers that are allowed to receive the results of queries run under this custom analysis rule policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="differential_privacy" type="object">
                  Defines the differential privacy settings for the custom analysis rule, specifying the privacy budget and noise injection mechanism applied to prevent re-identification of individual records. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="columns" type="list(object)">
                      Specifies the columns in the AWS Clean Rooms intermediate table that are protected by the differential privacy policy within this custom analysis rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the column in the configured table to which the differential privacy policy is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="disallowed_output_columns" type="list(string)">
                  Specifies the list of column names from the intermediate table that are prohibited from being included in the output of any query executed under this custom analysis rule, preventing sensitive data from being exposed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Specifies the AWS KMS key ARN used to encrypt the intermediate table, enabling customer-managed server-side encryption for the table's data. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_identifier" type="string" required>
  The unique identifier of the membership within the AWS Clean Rooms collaboration that this intermediate table belongs to. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="population_analysis_configuration" type="object" required>
  Required.
  <Expandable title="properties">
    <ResponseField name="sql_parameters" type="object">
      The sql_parameters object provides named values for the placeholders defined in the SQL query of the population analysis configuration, enabling the analysis to be customized with different parameter values at execution time. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="analysis_template_arn" type="string">
          The Amazon Resource Name (ARN) of the analysis template that the SQL parameters in this block are associated with, used to identify which template's parameters are being configured for the population analysis. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="query_string" type="string">
          The SQL query text that defines the population analysis logic for generating the intermediate table in AWS Clean Rooms. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag attached to the AWS Clean Rooms intermediate table, serving as the identifier for the tag's metadata label. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="collaboration_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS Clean Rooms collaboration in which the intermediate table is defined. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collaboration_identifier" type="string">
  The unique identifier of the AWS Clean Rooms collaboration that this intermediate table belongs to. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="intermediate_table_identifier" type="string">
  The system-generated unique identifier assigned by AWS Clean Rooms to this intermediate table, used to reference and manage the table resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS Clean Rooms membership that owns or contains the intermediate table, provided as a read-only identifier. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
