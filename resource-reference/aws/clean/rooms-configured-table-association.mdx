---
title: "aws_clean_rooms_configured_table_association"
description: "`aws_clean_rooms_configured_table_association` associates an `aws_clean_rooms_configured_table` with a specific `aws_clean_rooms_membership`..."
---

`aws_clean_rooms_configured_table_association` associates an `aws_clean_rooms_configured_table` with a specific `aws_clean_rooms_membership`, the actual grant that makes a configured table usable within one collaboration rather than just configured in the abstract.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	clean "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/clean"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rooms configured table association"})

    		ubx.Resource(clean.RoomsConfiguredTableAssociation, "example", clean.RoomsConfiguredTableAssociationConfig{
    			ConfiguredTableIdentifier:               "example",
    			MembershipIdentifier:                    "example",
    			Name:                                    "example",
    			RoleArn:                                 "arn:aws:iam::123456789012:policy/example",
    			ConfiguredTableAssociationAnalysisRules: []map[string]any{{"policy": map[string]any{"v1": map[string]any{"aggregation": map[string]any{"allowed_additional_analyses": []string{"example"}}}}}},
    			Description:                             "Managed by ubx.",
    			Tags:                                    []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoomsConfiguredTableAssociation } from "./local-sdk/aws/sdk/typescript/aws/clean/rooms_configured_table_association.ts";

    export default stack("example", () => {
      intent({ summary: "example own rooms configured table association" });

      resource(RoomsConfiguredTableAssociation, "example", {
        configuredTableIdentifier: "example",
        membershipIdentifier: "example",
        name: "example",
        roleArn: "arn:aws:iam::123456789012:policy/example",
        configuredTableAssociationAnalysisRules: [{
          policy: {
            v1: { aggregation: { allowedAdditionalAnalyses: ["example"] } },
          },
        }],
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.clean import RoomsConfiguredTableAssociation, RoomsConfiguredTableAssociationConfig

    def describe():
        ubx.intent("example own rooms configured table association")

        ubx.resource(RoomsConfiguredTableAssociation, "example", RoomsConfiguredTableAssociationConfig(
            configured_table_identifier="example",
            membership_identifier="example",
            name="example",
            role_arn="arn:aws:iam::123456789012:policy/example",
            configured_table_association_analysis_rules=[{"policy": {"v1": {"aggregation": {"allowed_additional_analyses": ["example"]}}}}],
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_clean_rooms_configured_table_association called
    "example", with configured table identifier example, membership
    identifier example, role arn
    arn:aws:iam::123456789012:policy/example, configured table
    association analysis rules a set value, description Managed by
    ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configured_table_association_analysis_rules" type="list(object)">
  Configures the analysis rules for the configured table association, specifying the permitted analysis types (e.g., list, aggregate, custom) and their policy constraints for querying the associated configured table within a collaboration. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="policy" type="object">
      This object specifies the policy for an analysis rule attached to a configured table association, defining the type of analysis (e.g., aggregation, list, or custom) along with the detailed constraints and parameters (such as aggregation columns, join conditions, and output limits) that govern queries run by collaboration members on the associated table. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="v1" type="object">
          Specifies the versioned analysis rule policy for a configured table association, containing the v1 policy that defines allowed aggregation columns, join columns, or custom SQL templates used to constrain queries in the collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="aggregation" type="object">
              Specifies the aggregation analysis rule policy parameters for the configured table association, including the allowed aggregate columns, dimension columns, join requirements, and permitted aggregation operators. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allowed_additional_analyses" type="list(string)">
                  Specifies which additional analyses (such as CORRELATION or REGRESSION) are permitted on the output of aggregation queries governed by this configured table association's aggregation analysis rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allowed_result_receivers" type="list(string)">
                  The list of collaboration member account IDs (or member ARNs) allowed to receive query results from aggregation queries run against this configured table association. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="custom" type="object">
              The custom analysis rule policy (version v1) for this AWS Clean Rooms configured table association, defining the allowed SQL operations such as joins, aggregations, and output columns that members can use when querying the associated table. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allowed_additional_analyses" type="list(string)">
                  This field specifies the additional types of analyses (e.g., SQL, Python) that are permitted to be run on the configured table when using a custom analysis rule policy, extending the set defined in `allowedAnalyses`. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allowed_result_receivers" type="list(string)">
                  Specifies the list of member account IDs that are allowed to receive query results when this custom analysis rule is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="list" type="object">
              Configures the list analysis rule for the configured table association, defining the allowed columns that members can select in list queries. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allowed_additional_analyses" type="list(string)">
                  Specifies the list of additional analysis types (such as AGGREGATE or CUSTOM) that are permitted for queries on the configured table association when the base analysis rule is of type LIST. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="allowed_result_receivers" type="list(string)">
                  Specifies the list of allowed result receivers (e.g., AWS account IDs or IAM roles) that may receive query results when a LIST analysis rule is applied to the associated configured table. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of analysis rule for the configured table association (e.g., AGGREGATION or LIST), which defines the privacy-enforcing query operations allowed on the associated table. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="configured_table_identifier" type="string" required>
  The unique identifier of the AWS Clean Rooms configured table that this association links to a membership for collaborative analysis. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  An optional free-form description of the configured table association, used to provide additional context or identity for the association within the AWS Clean Rooms membership. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_identifier" type="string" required>
  The unique identifier of the AWS Clean Rooms membership for which this configured table association is created, linking the configured table to that membership. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  A unique, user-provided name for the configured table association, used to identify and manage the association within the AWS Clean Rooms membership. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The ARN of the IAM role that AWS Clean Rooms assumes to access the underlying data referenced by the configured table when the associated table is used in analyses. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this cleanrooms collaboration. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a tag key attached to the AWS Clean Rooms configured table association, used to classify and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the configured table association, assigned by AWS when the association is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="configured_table_association_identifier" type="string">
  The unique identifier assigned by AWS to the configured table association within the membership. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
