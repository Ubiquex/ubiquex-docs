---
title: "aws_clean_rooms_ml_configured_model_algorithm_association"
description: "`aws_clean_rooms_ml_configured_model_algorithm_association` associates an `aws_clean_rooms_ml_configured_model_algorithm` with a specific collaboration..."
---

`aws_clean_rooms_ml_configured_model_algorithm_association` associates an `aws_clean_rooms_ml_configured_model_algorithm` with a specific collaboration membership, the real grant that makes a configured algorithm usable inside one collaboration.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	clean "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/clean"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rooms ml configured model algorithm association"})

    		ubx.Resource(clean.RoomsMlConfiguredModelAlgorithmAssociation, "example", clean.RoomsMlConfiguredModelAlgorithmAssociationConfig{
    			ConfiguredModelAlgorithmArn: "arn:aws:iam::123456789012:policy/example",
    			MembershipIdentifier:        "example",
    			Name:                        "example",
    			Description:                 "Managed by ubx.",
    			PrivacyConfiguration:        map[string]any{"policies": map[string]any{"trained_model_exports": map[string]any{"files_to_export": []string{"example"}}}},
    			Tags:                        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoomsMlConfiguredModelAlgorithmAssociation } from "@ubx/sdk-aws/aws/clean/rooms_ml_configured_model_algorithm_association";

    export default stack("example", () => {
      intent({
        summary: "example own rooms ml configured model algorithm association",
      });

      resource(RoomsMlConfiguredModelAlgorithmAssociation, "example", {
        configuredModelAlgorithmArn: "arn:aws:iam::123456789012:policy/example",
        membershipIdentifier: "example",
        name: "example",
        description: "Managed by ubx.",
        privacyConfiguration: {
          policies: { trainedModelExports: { filesToExport: ["example"] } },
        },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.clean import RoomsMlConfiguredModelAlgorithmAssociation, RoomsMlConfiguredModelAlgorithmAssociationConfig

    def describe():
        ubx.intent("example own rooms ml configured model algorithm association")

        ubx.resource(RoomsMlConfiguredModelAlgorithmAssociation, "example", RoomsMlConfiguredModelAlgorithmAssociationConfig(
            configured_model_algorithm_arn="arn:aws:iam::123456789012:policy/example",
            membership_identifier="example",
            name="example",
            description="Managed by ubx.",
            privacy_configuration={"policies": {"trained_model_exports": {"files_to_export": ["example"]}}},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_clean_rooms_ml_configured_model_algorithm_association
    called "example", with configured model algorithm arn
    arn:aws:iam::123456789012:policy/example, membership identifier
    example, description Managed by ubx., privacy configuration a set
    value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configured_model_algorithm_arn" type="string" required>
  The ARN of the configured model algorithm that this association links to the configured model. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  The description field is an optional, user-defined text string that describes the configured model algorithm association, allowing you to identify its purpose or context within AWS Clean Rooms ML. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_identifier" type="string" required>
  The unique identifier (UUID) of the AWS Clean Rooms membership that this configured model algorithm association is created within and scoped to. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the configured model algorithm association, which provides a required identifier for the association within AWS Clean Rooms ML. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="privacy_configuration" type="object">
  Defines the privacy policies for the configured model algorithm association, including the direct analysis privacy budget template and output configuration constraints that govern how query results are protected in the collaboration. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="policies" type="object" required>
      Required.
      <Expandable title="properties">
        <ResponseField name="trained_model_exports" type="object">
          Defines the policy for exporting trained models from the configured model algorithm association, specifying the allowed S3 destinations and any restrictions on where model artifacts may be written. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="files_to_export" type="list(string)" required>
              The list of file names that are allowed to be exported from the trained model when this trained model exports policy is applied in the privacy configuration. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_size" type="object" required>
              Limits the maximum size of a trained model export allowed by the privacy policy, preventing the export of models larger than the specified threshold. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="unit" type="string" required>
                  Required.
                </ResponseField>
                <ResponseField name="value" type="number" required>
                  The maximum size, in bytes, allowed for a trained model export as part of the privacy configuration's trained model exports policy. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="trained_model_inference_jobs" type="object">
          Specifies the privacy policy for trained model inference jobs on this configured model algorithm association, governing how inference job outputs are handled and where they may be sent. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="container_logs" type="list(object)">
              Optional.
              <Expandable title="properties">
                <ResponseField name="allowed_account_ids" type="list(string)">
                  Lists the AWS account IDs permitted to access the container logs produced during trained model inference jobs, as configured in the association's privacy policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter_pattern" type="string">
                  The filter pattern defines a pattern (e.g., a CloudWatch Logs-style filter) used to select which container log lines are captured or recorded for trained model inference jobs under this privacy policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="log_redaction_configuration" type="object">
                  Defines the redaction settings applied to container logs from trained model inference jobs, ensuring sensitive data is removed before logs are stored or processed, as part of the privacy configuration for the configured model algorithm association. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="custom_entity_config" type="object">
                      Configures custom entities for log redaction in trained model inference jobs, defining the names and regular expression patterns used to redact sensitive data from container logs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="custom_data_identifiers" type="list(string)">
                          Specifies the list of custom data identifier patterns (regular expressions) used by the log redaction configuration to detect and redact sensitive custom entities in container logs from trained model inference jobs for this configured model algorithm association. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="entities_to_redact" type="list(string)">
                      Specifies the list of sensitive data entity types that should be redacted from the container logs of trained model inference jobs in this configured model algorithm association. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="log_type" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_output_size" type="object">
              Defines the maximum output size cap for trained model inference jobs, enforcing privacy constraints by limiting the volume of data that can be returned. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="unit" type="string" required>
                  The unit of measurement for the maximum output size of a trained model inference job, with the only valid value being GB (gigabytes). Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number" required>
                  The numeric value of the maximum output size allowed for a trained model inference job, enforced by the privacy configuration's policy for the Clean Rooms ML configured model algorithm association. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="trained_models" type="object">
          Defines the trained models policy within the privacy configuration, specifying how trained models generated from the associated configured model algorithm can be accessed and used by collaboration members. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="container_logs" type="list(object)">
              Defines the list of container log configurations (each specifying a CloudWatch Logs log group ARN and a log type) that are permitted for the trained model container as part of this algorithm association's privacy policy. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allowed_account_ids" type="list(string)">
                  Defines the list of AWS account IDs permitted to access container logs for trained models, as part of the privacy configuration's trained models policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="filter_pattern" type="string">
                  Specifies a pattern (likely a CloudWatch Logs filter pattern) applied to container logs of trained models to exclude sensitive information as part of the privacy configuration in the AWS Clean Rooms ML algorithm association. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="log_redaction_configuration" type="object">
                  Configures how sensitive data in the container logs of trained models is redacted, including the patterns to match and the redaction method to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="custom_entity_config" type="object">
                      Optional; if omitted, computed by AWS.
                      <Expandable title="properties">
                        <ResponseField name="custom_data_identifiers" type="list(string)">
                          Specifies the list of custom data identifiers used by the custom entity configuration to redact matching sensitive data from the trained model's container logs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="entities_to_redact" type="list(string)">
                      Specifies the list of sensitive data entity types (e.g., personal identifiers) that are redacted from the container logs of the trained model, under the log redaction configuration of the privacy policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="log_type" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="container_metrics" type="object">
              Optional.
              <Expandable title="properties">
                <ResponseField name="noise_level" type="string" required>
                  The noise level (HIGH, MEDIUM, or LOW) that sets the amount of differential privacy noise added to container metrics for trained models, balancing privacy protection against metric utility in the Clean Rooms ML association. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="max_artifact_size" type="object">
              Sets the maximum size limit for trained model artifacts in the privacy configuration, expressed as an object with a numeric size and a unit, to prevent overly large models from being exported. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="unit" type="string" required>
                  The unit of measurement for the maximum size of a trained model artifact, such as MB or GB. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number" required>
                  Specifies the numeric component of the maximum trained model artifact size, which the privacy configuration uses to limit the size of model outputs that can be shared. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this cleanrooms-ml configured model algorithm association. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="collaboration_identifier" type="string">
  The identifier of the AWS Clean Rooms collaboration that contains this configured model algorithm association, used to scope the association to a specific collaboration environment. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="configured_model_algorithm_association_arn" type="string">
  The Amazon Resource Name (ARN) of the configured model algorithm association. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
