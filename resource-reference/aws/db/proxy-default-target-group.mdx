---
title: "aws_db_proxy_default_target_group"
description: "Real, generated bindings for `aws_db_proxy_default_target_group`."
---

`aws_db_proxy_default_target_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/db"

    cfg := db.ProxyDefaultTargetGroupConfig{
        DbProxyName: "example-db-proxy-name",
    }
    ubx.Resource(db.ProxyDefaultTargetGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProxyDefaultTargetGroup } from
      "jsr:@ubx/sdk-aws/aws/db/proxy_default_target_group";

    resource(ProxyDefaultTargetGroup, "example", {
      dbProxyName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.db import (
        ProxyDefaultTargetGroup,
        ProxyDefaultTargetGroupConfig,
    )

    cfg = ProxyDefaultTargetGroupConfig(
        db_proxy_name="example",
    )
    ubx.resource(ProxyDefaultTargetGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_db_proxy_default_target_group with db_proxy_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="db_proxy_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
