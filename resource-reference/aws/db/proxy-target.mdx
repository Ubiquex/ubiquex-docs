---
title: "aws_db_proxy_target"
description: "Real, generated bindings for `aws_db_proxy_target`."
---

`aws_db_proxy_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own proxy target"})

    		ubx.Resource(db.ProxyTarget, "ci-runner", db.ProxyTargetConfig{
    			DbProxyName:          "example-db-proxy-name",
    			TargetGroupName:      "example-target-group-name",
    			DbClusterIdentifier:  "example",
    			DbInstanceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProxyTarget } from "jsr:@ubx/sdk-aws/aws/db/proxy_target";

    export default stack("payments", () => {
      intent({ summary: "payments own proxy target" });

      resource(ProxyTarget, "ci-runner", {
        dbProxyName: "example-db-proxy-name",
        targetGroupName: "example-target-group-name",
        dbClusterIdentifier: "example",
        dbInstanceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.db import ProxyTarget, ProxyTargetConfig

    def describe():
        ubx.intent("payments own proxy target")

        ubx.resource(ProxyTarget, "ci-runner", ProxyTargetConfig(
            db_proxy_name="example-db-proxy-name",
            target_group_name="example-target-group-name",
            db_cluster_identifier="example",
            db_instance_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_db_proxy_target, with db proxy name
    example-db-proxy-name, target group name
    example-target-group-name, db cluster identifier example, db
    instance identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="db_cluster_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="db_instance_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="db_proxy_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_group_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="port" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rds_resource_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tracked_cluster_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
