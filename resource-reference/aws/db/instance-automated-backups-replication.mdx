---
title: "aws_db_instance_automated_backups_replication"
description: "Real, generated bindings for `aws_db_instance_automated_backups_replication`."
---

`aws_db_instance_automated_backups_replication` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance automated backups replication"})

    		ubx.Resource(db.InstanceAutomatedBackupsReplication, "ci-runner", db.InstanceAutomatedBackupsReplicationConfig{
    			SourceDbInstanceArn: "arn:aws:iam::123456789012:policy/ci",
    			PreSignedUrl:        "example",
    			RetentionPeriod:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceAutomatedBackupsReplication } from "jsr:@ubx/sdk-aws/aws/db/instance_automated_backups_replication";

    export default stack("payments", () => {
      intent({ summary: "payments own instance automated backups replication" });

      resource(InstanceAutomatedBackupsReplication, "ci-runner", {
        sourceDbInstanceArn: "arn:aws:iam::123456789012:policy/ci",
        preSignedUrl: "example",
        retentionPeriod: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.db import InstanceAutomatedBackupsReplication, InstanceAutomatedBackupsReplicationConfig

    def describe():
        ubx.intent("payments own instance automated backups replication")

        ubx.resource(InstanceAutomatedBackupsReplication, "ci-runner", InstanceAutomatedBackupsReplicationConfig(
            source_db_instance_arn="arn:aws:iam::123456789012:policy/ci",
            pre_signed_url="example",
            retention_period=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_db_instance_automated_backups_replication, with source
    db instance arn arn:aws:iam::123456789012:policy/ci, pre signed
    url example, retention period 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pre_signed_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="source_db_instance_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
