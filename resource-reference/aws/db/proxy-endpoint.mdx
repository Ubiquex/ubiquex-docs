---
title: "aws_db_proxy_endpoint"
description: "Real, generated bindings for `aws_db_proxy_endpoint`."
---

`aws_db_proxy_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	db "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/db"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own proxy endpoint"})

    		ubx.Resource(db.ProxyEndpoint, "ci-runner", db.ProxyEndpointConfig{
    			DbProxyEndpointName: "example-db-proxy-endpoint-name",
    			DbProxyName:         "example-db-proxy-name",
    			VpcSubnetIds:        []string{"example"},
    			Tags:                map[string]string{"managed-by": "ubx"},
    			TargetRole:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProxyEndpoint } from "jsr:@ubx/sdk-aws/aws/db/proxy_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own proxy endpoint" });

      resource(ProxyEndpoint, "ci-runner", {
        dbProxyEndpointName: "example-db-proxy-endpoint-name",
        dbProxyName: "example-db-proxy-name",
        vpcSubnetIds: ["example"],
        tags: { "managed-by": "ubx" },
        targetRole: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.db import ProxyEndpoint, ProxyEndpointConfig

    def describe():
        ubx.intent("payments own proxy endpoint")

        ubx.resource(ProxyEndpoint, "ci-runner", ProxyEndpointConfig(
            db_proxy_endpoint_name="example-db-proxy-endpoint-name",
            db_proxy_name="example-db-proxy-name",
            vpc_subnet_ids=["example"],
            tags={"managed-by": "ubx"},
            target_role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_db_proxy_endpoint, with db proxy endpoint name
    example-db-proxy-endpoint-name, db proxy name
    example-db-proxy-name, vpc subnet ids a set value, tags a set
    value, target role example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="db_proxy_endpoint_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="db_proxy_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="vpc_security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_security_group_ids" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
