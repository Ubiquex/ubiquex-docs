---
title: "aws_bedrock_automated_reasoning_policy"
description: "`aws_bedrock_automated_reasoning_policy` encodes a domain expert's own rules, an operational workflow, an HR policy..."
---

`aws_bedrock_automated_reasoning_policy` encodes a domain expert's own rules, an operational workflow, an HR policy, into a formal specification that Amazon Bedrock Guardrails checks a model's response against using real mathematical logic, not another model's judgment, to catch hallucinations and unstated assumptions with a provable, not probabilistic, guarantee.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own automated reasoning policy"})

    		ubx.Resource(bedrock.AutomatedReasoningPolicy, "ci-runner", bedrock.AutomatedReasoningPolicyConfig{
    			Name:             "ci-runner",
    			Description:      "Managed by ubx.",
    			ForceDelete:      true,
    			KmsKeyId:         "example",
    			PolicyDefinition: map[string]any{"rules": []map[string]any{{"alternate_expression": "example"}}},
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AutomatedReasoningPolicy } from "./local-sdk/aws/sdk/typescript/aws/bedrock/automated_reasoning_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own automated reasoning policy" });

      resource(AutomatedReasoningPolicy, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        forceDelete: true,
        kmsKeyId: "example",
        policyDefinition: { rules: [{ alternateExpression: "example" }] },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.bedrock import AutomatedReasoningPolicy, AutomatedReasoningPolicyConfig

    def describe():
        ubx.intent("payments own automated reasoning policy")

        ubx.resource(AutomatedReasoningPolicy, "ci-runner", AutomatedReasoningPolicyConfig(
            name="ci-runner",
            description="Managed by ubx.",
            force_delete=True,
            kms_key_id="example",
            policy_definition={"rules": [{"alternate_expression": "example"}]},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_automated_reasoning_policy called "ci-runner",
    with description Managed by ubx., force delete true, kms key id
    example, policy definition a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  An optional, human-readable explanation of the purpose or contents of the automated reasoning policy. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="force_delete" type="bool">
  Specifies whether to force delete the automated reasoning policy even if it has active resources. When false , Amazon Bedrock validates if all artifacts have been deleted (e.g. policy version, test case, test result) for a policy before deletion. When true , Amazon Bedrock will delete the policy and all its artifacts without validation. Default is false Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The KMS key with which the Policy's assets will be encrypted at rest. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_definition" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="rules" type="list(object)">
      Optional.
      <Expandable title="properties">
        <ResponseField name="alternate_expression" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="expression" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="types" type="list(object)">
      Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="values" type="list(object)">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              Provides a human-readable explanation of a specific value defined within a type in the automated reasoning policy, giving the model additional context for interpreting that value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variables" type="list(object)">
      Optional.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="type" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version" type="string">
      The policy format version. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value tags to associate with the AWS Bedrock automated reasoning policy. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  Time this policy was created Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="definition_hash" type="string">
  The hash for this version Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  The KMS key with which the Policy's assets will be encrypted at rest. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_id" type="string">
  The id of the policy Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Time this policy was last updated Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Version of the policy that was created. This will always be `DRAFT` Computed by AWS after `ship`.
</ResponseField>
