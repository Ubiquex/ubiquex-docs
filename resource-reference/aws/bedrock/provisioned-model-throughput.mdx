---
title: "aws_bedrock_provisioned_model_throughput"
description: "Real, generated bindings for `aws_bedrock_provisioned_model_throughput`."
---

`aws_bedrock_provisioned_model_throughput` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrock"

    cfg := bedrock.ProvisionedModelThroughputConfig{
        ModelArn: "example",
        ModelUnits: 1,
        ProvisionedModelName: "example-provisioned-model-name",
        CommitmentDuration: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(bedrock.ProvisionedModelThroughput, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProvisionedModelThroughput } from
      "jsr:@ubx/sdk-aws/aws/bedrock/provisioned_model_throughput";

    resource(ProvisionedModelThroughput, "example", {
      modelArn: "example",
      modelUnits: 1,
      provisionedModelName: "example",
      commitmentDuration: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrock import (
        ProvisionedModelThroughput,
        ProvisionedModelThroughputConfig,
    )

    cfg = ProvisionedModelThroughputConfig(
        model_arn="example",
        model_units=1,
        provisioned_model_name="example",
        commitment_duration="example",
        tags={"key": "value"},
    )
    ubx.resource(ProvisionedModelThroughput, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrock_provisioned_model_throughput with
model_arn, model_units, provisioned_model_name,
commitment_duration, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="commitment_duration" type="string">
  Optional.
</ResponseField>

<ResponseField name="model_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_units" type="number" required>
  Required.
</ResponseField>

<ResponseField name="provisioned_model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioned_model_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
