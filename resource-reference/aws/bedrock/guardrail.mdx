---
title: "aws_bedrock_guardrail"
description: "Real, generated bindings for `aws_bedrock_guardrail`."
---

`aws_bedrock_guardrail` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"

    cfg := bedrock.GuardrailConfig{
        BlockedInputMessaging: "example",
        BlockedOutputsMessaging: "example",
        Name: "example-name",
        Description: "example",
        KmsKeyArn: "example",
    }
    ubx.Resource(bedrock.Guardrail, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Guardrail } from
      "jsr:@ubx/sdk-aws/aws/bedrock/guardrail";

    resource(Guardrail, "example", {
      blockedInputMessaging: "example",
      blockedOutputsMessaging: "example",
      name: "example",
      description: "example",
      kmsKeyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrock import Guardrail, GuardrailConfig

    cfg = GuardrailConfig(
        blocked_input_messaging="example",
        blocked_outputs_messaging="example",
        name="example",
        description="example",
        kms_key_arn="example",
    )
    ubx.resource(Guardrail, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrock_guardrail with blocked_input_messaging,
blocked_outputs_messaging, name, description, kms_key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="blocked_input_messaging" type="string" required>
  Required.
</ResponseField>

<ResponseField name="blocked_outputs_messaging" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="guardrail_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="guardrail_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
