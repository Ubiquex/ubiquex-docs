---
title: "aws_bedrock_model_invocation_logging_configuration"
description: "Real, generated bindings for `aws_bedrock_model_invocation_logging_configuration`."
---

`aws_bedrock_model_invocation_logging_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own model invocation logging configuration"})

    		ubx.Resource(bedrock.ModelInvocationLoggingConfiguration, "ci-runner", bedrock.ModelInvocationLoggingConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ModelInvocationLoggingConfiguration } from "jsr:@ubx/sdk-aws/aws/bedrock/model_invocation_logging_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own model invocation logging configuration" });

      resource(ModelInvocationLoggingConfiguration, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import ModelInvocationLoggingConfiguration, ModelInvocationLoggingConfigurationConfig

    def describe():
        ubx.intent("payments own model invocation logging configuration")

        ubx.resource(ModelInvocationLoggingConfiguration, "ci-runner", ModelInvocationLoggingConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_model_invocation_logging_configuration, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
