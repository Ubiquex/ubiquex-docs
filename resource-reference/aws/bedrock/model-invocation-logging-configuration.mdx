---
title: "aws_bedrock_model_invocation_logging_configuration"
description: "Real, generated bindings for `aws_bedrock_model_invocation_logging_configuration`."
---

`aws_bedrock_model_invocation_logging_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"

    cfg := bedrock.ModelInvocationLoggingConfigurationConfig{
    }
    ubx.Resource(
        bedrock.ModelInvocationLoggingConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ModelInvocationLoggingConfiguration } from
      "jsr:@ubx/sdk-aws/aws/bedrock/model_invocation_logging_configuration";

    resource(ModelInvocationLoggingConfiguration, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrock import (
        ModelInvocationLoggingConfiguration,
        ModelInvocationLoggingConfigurationConfig,
    )

    cfg = ModelInvocationLoggingConfigurationConfig(
    )
    ubx.resource(ModelInvocationLoggingConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrock_model_invocation_logging_configuration.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
