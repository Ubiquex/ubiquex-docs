---
title: "aws_bedrock_custom_model"
description: "Real, generated bindings for `aws_bedrock_custom_model`."
---

`aws_bedrock_custom_model` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom model"})

    		ubx.Resource(bedrock.CustomModel, "ci-runner", bedrock.CustomModelConfig{
    			BaseModelIdentifier: "example",
    			CustomModelName:     "example-custom-model-name",
    			Hyperparameters:     map[string]string{"managed-by": "ubx"},
    			JobName:             "example-job-name",
    			RoleArn:             "arn:aws:iam::123456789012:policy/ci",
    			CustomModelKmsKeyId: "example",
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomModel } from "jsr:@ubx/sdk-aws/aws/bedrock/custom_model";

    export default stack("payments", () => {
      intent({ summary: "payments own custom model" });

      resource(CustomModel, "ci-runner", {
        baseModelIdentifier: "example",
        customModelName: "example-custom-model-name",
        hyperparameters: { "managed-by": "ubx" },
        jobName: "example-job-name",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        customModelKmsKeyId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import CustomModel, CustomModelConfig

    def describe():
        ubx.intent("payments own custom model")

        ubx.resource(CustomModel, "ci-runner", CustomModelConfig(
            base_model_identifier="example",
            custom_model_name="example-custom-model-name",
            hyperparameters={"managed-by": "ubx"},
            job_name="example-job-name",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            custom_model_kms_key_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_custom_model, with base model identifier
    example, custom model name example-custom-model-name,
    hyperparameters a set value, job name example-job-name, role arn
    arn:aws:iam::123456789012:policy/ci, custom model kms key id
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="base_model_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_model_kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="custom_model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="customization_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="hyperparameters" type="map(string)" required>
  Required.
</ResponseField>

<ResponseField name="job_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="custom_model_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="customization_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="job_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="job_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="training_metrics" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="training_loss" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="validation_metrics" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="validation_loss" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>
