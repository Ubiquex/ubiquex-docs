---
title: "aws_bedrock_guardrail_version"
description: "Real, generated bindings for `aws_bedrock_guardrail_version`."
---

`aws_bedrock_guardrail_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own guardrail version"})

    		ubx.Resource(bedrock.GuardrailVersion, "ci-runner", bedrock.GuardrailVersionConfig{
    			GuardrailArn: "arn:aws:iam::123456789012:policy/ci",
    			Description:  "Managed by ubx.",
    			SkipDestroy:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GuardrailVersion } from "jsr:@ubx/sdk-aws/aws/bedrock/guardrail_version";

    export default stack("payments", () => {
      intent({ summary: "payments own guardrail version" });

      resource(GuardrailVersion, "ci-runner", {
        guardrailArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        skipDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import GuardrailVersion, GuardrailVersionConfig

    def describe():
        ubx.intent("payments own guardrail version")

        ubx.resource(GuardrailVersion, "ci-runner", GuardrailVersionConfig(
            guardrail_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            skip_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_guardrail_version, with guardrail arn
    arn:aws:iam::123456789012:policy/ci, description Managed by ubx.,
    skip destroy true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="guardrail_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
