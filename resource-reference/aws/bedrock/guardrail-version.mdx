---
title: "aws_bedrock_guardrail_version"
description: "Real, generated bindings for `aws_bedrock_guardrail_version`."
---

`aws_bedrock_guardrail_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"

    cfg := bedrock.GuardrailVersionConfig{
        GuardrailArn: "example",
        Description: "example",
        SkipDestroy: true,
    }
    ubx.Resource(bedrock.GuardrailVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GuardrailVersion } from
      "jsr:@ubx/sdk-aws/aws/bedrock/guardrail_version";

    resource(GuardrailVersion, "example", {
      guardrailArn: "example",
      description: "example",
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrock import (
        GuardrailVersion,
        GuardrailVersionConfig,
    )

    cfg = GuardrailVersionConfig(
        guardrail_arn="example",
        description="example",
        skip_destroy=True,
    )
    ubx.resource(GuardrailVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrock_guardrail_version with guardrail_arn,
description, skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="guardrail_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
