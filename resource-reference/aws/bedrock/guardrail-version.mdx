---
title: "aws_bedrock_guardrail_version"
description: "`aws_bedrock_guardrail_version` publishes a real, immutable version of an `aws_bedrock_guardrail`'s current configuration..."
---

`aws_bedrock_guardrail_version` publishes a real, immutable version of an `aws_bedrock_guardrail`'s current configuration, letting an application pin to a specific, unchanging set of content filters and denied topics rather than the guardrail's editable draft.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own guardrail version"})

    		ubx.Resource(bedrock.GuardrailVersion, "example", bedrock.GuardrailVersionConfig{
    			GuardrailIdentifier: "example",
    			Description:         "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GuardrailVersion } from "@ubx/sdk-aws/aws/bedrock/guardrail_version";

    export default stack("example", () => {
      intent({ summary: "example own guardrail version" });

      resource(GuardrailVersion, "example", {
        guardrailIdentifier: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock.guardrail_version import GuardrailVersion, GuardrailVersionConfig

    def describe():
        ubx.intent("example own guardrail version")

        ubx.resource(GuardrailVersion, "example", GuardrailVersionConfig(
            guardrail_identifier="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_guardrail_version, with guardrail identifier
    example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Description of the Guardrail version Optional.
</ResponseField>

<ResponseField name="guardrail_identifier" type="string" required>
  Identifier (GuardrailId or GuardrailArn) for the guardrail Required.
</ResponseField>

## Output properties

<ResponseField name="guardrail_arn" type="string">
  Arn representation for the guardrail Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="guardrail_id" type="string">
  Unique id for the guardrail Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Guardrail version Computed by AWS after `ship`.
</ResponseField>
