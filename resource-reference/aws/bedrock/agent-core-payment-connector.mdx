---
title: "aws_bedrock_agent_core_payment_connector"
description: "`aws_bedrock_agent_core_payment_connector` integrates an `aws_bedrock_agent_core_payment_manager` with one real external payment provider..."
---

`aws_bedrock_agent_core_payment_connector` integrates an `aws_bedrock_agent_core_payment_manager` with one real external payment provider, Coinbase or Stripe, for instance, referencing a credential stored in AgentCore Identity so API keys and wallet secrets are never passed directly through the Payments API.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agent core payment connector"})

    		ubx.Resource(bedrock.AgentCorePaymentConnector, "ci-runner", bedrock.AgentCorePaymentConnectorConfig{
    			ConnectorName:                    "example-connector-name",
    			ConnectorType:                    "example",
    			PaymentManagerId:                 "example",
    			CredentialProviderConfigurations: []map[string]any{{"coinbase_cdp": map[string]any{"credential_provider_arn": "example"}}},
    			Description:                      "Managed by ubx.",
    			ProvisionMode:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AgentCorePaymentConnector } from "./local-sdk/aws/sdk/typescript/aws/bedrock/agent_core_payment_connector.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own agent core payment connector" });

      resource(AgentCorePaymentConnector, "ci-runner", {
        connectorName: "example-connector-name",
        connectorType: "example",
        paymentManagerId: "example",
        credentialProviderConfigurations: [{
          coinbaseCdp: { credentialProviderArn: "example" },
        }],
        description: "Managed by ubx.",
        provisionMode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.bedrock import AgentCorePaymentConnector, AgentCorePaymentConnectorConfig

    def describe():
        ubx.intent("payments own agent core payment connector")

        ubx.resource(AgentCorePaymentConnector, "ci-runner", AgentCorePaymentConnectorConfig(
            connector_name="example-connector-name",
            connector_type="example",
            payment_manager_id="example",
            credential_provider_configurations=[{"coinbase_cdp": {"credential_provider_arn": "example"}}],
            description="Managed by ubx.",
            provision_mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_agent_core_payment_connector, with connector
    name example-connector-name, connector type example, payment
    manager id example, credential provider configurations a set
    value, description Managed by ubx., provision mode example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="connector_name" type="string" required>
  The name of the payment connector Required.
</ResponseField>

<ResponseField name="connector_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="credential_provider_configurations" type="list(object)">
  The credential provider configurations for the connector. Required when ProvisionMode is MANUAL or not specified. Empty for QUICK_CREATE until provisioning completes. Optional.
  <Expandable title="properties">
    <ResponseField name="coinbase_cdp" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="credential_provider_arn" type="string">
          The ARN of the credential provider (e.g., an AWS Secrets Manager secret or IAM role) that supplies the API credentials used by the Bedrock agent to authenticate with Coinbase CDP for payment operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stripe_privy" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="credential_provider_arn" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A description of the payment connector Optional.
</ResponseField>

<ResponseField name="payment_manager_id" type="string" required>
  The identifier of the parent payment manager Required.
</ResponseField>

<ResponseField name="provision_mode" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="authorization_url" type="string">
  The URL the user must open to complete OAuth consent. Only present when ConnectorStatus is PENDING_AUTHENTICATION. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connector_created_at" type="string">
  The timestamp when the connector was created Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connector_last_updated_at" type="string">
  The timestamp when the connector was last updated Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connector_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="payment_connector_arn" type="string">
  Synthetic ARN for the payment connector (used for engine resolution) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="payment_connector_id" type="string">
  The unique identifier for the payment connector Computed by AWS after `ship`.
</ResponseField>
