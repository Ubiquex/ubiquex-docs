---
title: "aws_bedrock_agent_core_gateway"
description: "`aws_bedrock_agent_core_gateway` securely exposes and routes traffic to real tools, MCP servers, inference models, other agents..."
---

`aws_bedrock_agent_core_gateway` securely exposes and routes traffic to real tools, MCP servers, inference models, other agents, or plain HTTP endpoints, at scale, so agents discover and call tools through one governed entry point instead of each agent integrating with every tool directly.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agent core gateway"})

    		ubx.Resource(bedrock.AgentCoreGateway, "ci-runner", bedrock.AgentCoreGatewayConfig{
    			AuthorizerType:            "example",
    			Name:                      "ci-runner",
    			RoleArn:                   "arn:aws:iam::123456789012:policy/ci",
    			AuthorizerConfiguration:   map[string]any{"custom_jwtauthorizer": map[string]any{"advertised_scope_mapping": "example"}},
    			Description:               "Managed by ubx.",
    			ExceptionLevel:            "example",
    			InterceptorConfigurations: []map[string]any{{"input_configuration": map[string]any{"pass_request_headers": true}}},
    			KmsKeyArn:                 "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AgentCoreGateway } from "./local-sdk/aws/sdk/typescript/aws/bedrock/agent_core_gateway.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own agent core gateway" });

      resource(AgentCoreGateway, "ci-runner", {
        authorizerType: "example",
        name: "ci-runner",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        authorizerConfiguration: {
          customJwtauthorizer: { advertisedScopeMapping: "example" },
        },
        description: "Managed by ubx.",
        exceptionLevel: "example",
        interceptorConfigurations: [{
          inputConfiguration: { passRequestHeaders: true },
        }],
        kmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.bedrock import AgentCoreGateway, AgentCoreGatewayConfig

    def describe():
        ubx.intent("payments own agent core gateway")

        ubx.resource(AgentCoreGateway, "ci-runner", AgentCoreGatewayConfig(
            authorizer_type="example",
            name="ci-runner",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            authorizer_configuration={"custom_jwtauthorizer": {"advertised_scope_mapping": "example"}},
            description="Managed by ubx.",
            exception_level="example",
            interceptor_configurations=[{"input_configuration": {"pass_request_headers": True}}],
            kms_key_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_agent_core_gateway called "ci-runner", with
    authorizer type example, role arn
    arn:aws:iam::123456789012:policy/ci, authorizer configuration a
    set value, description Managed by ubx., exception level example,
    interceptor configurations a set value, kms key arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="authorizer_configuration" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="custom_jwtauthorizer" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="advertised_scope_mapping" type="any">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="allowed_audience" type="list(string)">
          Specifies the list of allowed audience values (the 'aud' claim) that a JWT must contain to be accepted by the custom JWT authorizer for this Agent Core Gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="allowed_clients" type="list(string)">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="allowed_scopes" type="list(string)">
          Specifies the list of JWT scopes that must be present in the token's claims for the custom JWT authorizer to allow the request through the core gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="custom_claims" type="list(object)">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="authorizing_claim_match_value" type="object">
              Specifies the value that a custom claim in the JWT token must match for the request to be authorized by the Bedrock Agent Core Gateway's custom JWT authorizer. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="claim_match_operator" type="string">
                  Determines the comparison operator (e.g., equals) used when evaluating whether a custom JWT claim's value matches the configured authorizing claim match value in the JWT authorizer of the Bedrock agent core gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="claim_match_value" type="object">
                  Defines the exact value that a custom claim in the incoming JWT token must match to authorize the request to the Bedrock agent core gateway, used by the custom JWT authorizer's custom claims configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_value_string" type="string">
                      The exact string value that the specified custom claim must match for the request to be authorized. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_value_string_list" type="list(string)">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="inbound_token_claim_name" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="inbound_token_claim_value_type" type="string">
              For the custom JWT authorizer on the Bedrock Agent Core Gateway, this field determines whether the value of the inbound token claim is interpreted as a single string or an array of strings when extracting custom claims. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="discovery_url" type="string">
          This field specifies the OIDC discovery URL (e.g., https://auth.example.com/.well-known/openid-configuration) that the custom JWT authorizer fetches to obtain the JWKS (JSON Web Key Set) used to validate the JWT tokens presented to the agent's core gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="private_endpoint" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="managed_vpc_resource" type="object">
              Defines the managed VPC configuration (VPC ID, subnet IDs, security group IDs) used by the private endpoint of the custom JWT authorizer in the Bedrock Agent Core Gateway resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="endpoint_ip_address_type" type="string">
                  Specifies the IP address type (IPv4 or IPv6) for the managed VPC endpoint used to privately access the custom JWT authorizer. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="routing_domain" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="security_group_ids" type="list(string)">
                  The security group IDs to attach to the managed VPC resources that back this private endpoint, controlling network traffic for the custom JWT authorizer. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="subnet_ids" type="list(string)">
                  Specifies the list of subnet IDs in the managed VPC used for the private endpoint of the custom JWT authorizer, determining where the endpoint resources are placed for network connectivity. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="vpc_identifier" type="string">
                  The ID of the VPC in which the managed VPC resource for the private endpoint is provisioned. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="self_managed_lattice_resource" type="object">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="resource_configuration_identifier" type="string">
                  The unique identifier of the Amazon VPC Lattice resource configuration that the custom JWT authorizer's private endpoint uses to route requests to the self-managed lattice resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="authorizer_type" type="string" required>
  Specifies the type of authorizer that the Bedrock Agent core gateway uses to authenticate and authorize incoming API requests before invoking the underlying agent. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="exception_level" type="string">
  Optional.
</ResponseField>

<ResponseField name="interceptor_configurations" type="list(object)">
  Specifies a list of interceptor configurations that attach Lambda-based hooks to the agent core gateway for monitoring, modifying, or short-circuiting requests and responses between clients and the agent. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="input_configuration" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="pass_request_headers" type="bool">
          Whether the original request headers are included and passed through to the interceptor Lambda when the agent intercepts an invocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="payload_filter" type="object">
          This object defines the payload filter for the interceptor's input configuration, specifying a matching pattern and type that determine which parts of the incoming payload are allowed through to the agent for processing. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="exclude" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="field" type="string">
                  Specifies the name of a field in the input payload that should be excluded from the payload sent to the interceptor as part of the payload filter's exclude list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="interception_points" type="list(string)">
      Specifies the agent lifecycle stages (such as PRE_PROCESSING and POST_PROCESSING) at which the interceptor Lambda function is invoked to intercept requests and responses. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="interceptor" type="object">
      Specifies the interceptor to be invoked by the core gateway, including its ARN and interception type (e.g., request or response). Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="lambda" type="object">
          Defines the Lambda function configuration used by the interceptor to hook into and process requests and responses flowing through the Bedrock agent core gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="arn" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  The ARN of the customer-managed AWS KMS key used to encrypt the Bedrock agent core gateway's data at rest, providing control over encryption. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_engine_configuration" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="arn" type="string" required>
      Required.
    </ResponseField>
    <ResponseField name="mode" type="string" required>
      Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="protocol_configuration" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="mcp" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="instructions" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="search_type" type="string">
          Determines whether the agent discovers tools from the MCP server via exact tool-name matching or semantic search. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="session_configuration" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="session_timeout_in_seconds" type="number">
              Specifies the maximum duration in seconds that an MCP session remains active before timing out, controlling the lifespan of Model Context Protocol server connections used by the agent's core gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="streaming_configuration" type="object">
          Configures streaming behavior for MCP (Model Context Protocol) communication, including whether MCP messages are streamed between the agent and the core gateway. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="enable_response_streaming" type="bool">
              This boolean enables response streaming in the MCP streaming configuration of the agent core gateway, allowing data to be delivered incrementally rather than as a single complete payload. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="supported_versions" type="list(string)">
          Specifies the list of MCP (Model Context Protocol) versions, such as '2024-11-05', that the gateway supports when communicating with connected MCP servers. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="protocol_type" type="object">
  Defines the communication protocol used by the core gateway, with supported values of 'APPLICATION' for application-to-agent traffic and 'AGENTS' for agent-to-agent traffic. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The ARN of the IAM role that the Bedrock Agent Core Gateway assumes to invoke Amazon Bedrock agents and access related AWS services. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="gateway_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="gateway_identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="gateway_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reasons" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="workload_identity_details" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="workload_identity_arn" type="string" required>
      Required.
    </ResponseField>
  </Expandable>
</ResponseField>
