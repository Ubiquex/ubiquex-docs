---
title: "aws_bedrock_agent_core_payment_credential_provider"
description: "`aws_bedrock_agent_core_payment_credential_provider` registers real, stored payment credentials that an AgentCore agent can retrieve at runtime to..."
---

`aws_bedrock_agent_core_payment_credential_provider` registers real, stored payment credentials that an AgentCore agent can retrieve at runtime to complete a transaction through an `aws_bedrock_agent_core_payment_connector` or `aws_bedrock_agent_core_payment_manager`, without the credentials being hardcoded into the agent's own logic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own agent core payment credential provider"})

    		ubx.Resource(bedrockagentcore.PaymentCredentialProvider, "example", bedrockagentcore.PaymentCredentialProviderConfig{
    			CredentialProviderVendor:   "example",
    			Name:                       "example",
    			ProviderConfigurationInput: map[string]any{"coinbase_cdp_configuration": map[string]any{"api_key_id": "example"}},
    			Tags:                       []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PaymentCredentialProvider } from "@ubx/sdk-aws/aws/bedrockagentcore/payment_credential_provider";

    export default stack("example", () => {
      intent({ summary: "example own agent core payment credential provider" });

      resource(PaymentCredentialProvider, "example", {
        credentialProviderVendor: "example",
        name: "example",
        providerConfigurationInput: {
          coinbaseCdpConfiguration: { apiKeyId: "example" },
        },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore.payment_credential_provider import PaymentCredentialProvider, PaymentCredentialProviderConfig

    def describe():
        ubx.intent("example own agent core payment credential provider")

        ubx.resource(PaymentCredentialProvider, "example", PaymentCredentialProviderConfig(
            credential_provider_vendor="example",
            name="example",
            provider_configuration_input={"coinbase_cdp_configuration": {"api_key_id": "example"}},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_agent_core_payment_credential_provider called
    "example", with credential provider vendor example, provider
    configuration input a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="credential_provider_vendor" type="string" required>
  Supported vendor types for payment providers Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Unique name for the payment credential provider Required.
</ResponseField>

<ResponseField name="provider_configuration_input" type="object">
  Provider configuration input containing secrets for creation/update Optional.
  <Expandable title="properties">
    <ResponseField name="coinbase_cdp_configuration" type="object">
      Coinbase CDP configuration with API credentials Optional.
      <Expandable title="properties">
        <ResponseField name="api_key_id" type="string" required>
          The Coinbase CDP API key ID Required.
        </ResponseField>
        <ResponseField name="api_key_secret" type="string">
          The Coinbase CDP API key secret Optional.
        </ResponseField>
        <ResponseField name="api_key_secret_config" type="object">
          A reference to a customer-provided secret stored in AWS Secrets Manager Optional.
          <Expandable title="properties">
            <ResponseField name="json_key" type="string" required>
              The JSON key within the secret that contains the credential value Required.
            </ResponseField>
            <ResponseField name="secret_id" type="string" required>
              The ID or ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="api_key_secret_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
        <ResponseField name="wallet_secret" type="string">
          The Coinbase CDP wallet secret Optional.
        </ResponseField>
        <ResponseField name="wallet_secret_config" type="object">
          A reference to a customer-provided secret stored in AWS Secrets Manager Optional.
          <Expandable title="properties">
            <ResponseField name="json_key" type="string" required>
              The JSON key within the secret that contains the credential value Required.
            </ResponseField>
            <ResponseField name="secret_id" type="string" required>
              The ID or ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="wallet_secret_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stripe_privy_configuration" type="object">
      Stripe Privy configuration with credentials Optional.
      <Expandable title="properties">
        <ResponseField name="app_id" type="string" required>
          The app ID provided by Privy Required.
        </ResponseField>
        <ResponseField name="app_secret" type="string">
          The app secret provided by Privy Optional.
        </ResponseField>
        <ResponseField name="app_secret_config" type="object">
          A reference to a customer-provided secret stored in AWS Secrets Manager Optional.
          <Expandable title="properties">
            <ResponseField name="json_key" type="string" required>
              The JSON key within the secret that contains the credential value Required.
            </ResponseField>
            <ResponseField name="secret_id" type="string" required>
              The ID or ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="app_secret_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
        <ResponseField name="authorization_id" type="string" required>
          The authorization ID for the Stripe Privy integration Required.
        </ResponseField>
        <ResponseField name="authorization_private_key" type="string">
          The authorization private key for the Stripe Privy integration Optional.
        </ResponseField>
        <ResponseField name="authorization_private_key_config" type="object">
          A reference to a customer-provided secret stored in AWS Secrets Manager Optional.
          <Expandable title="properties">
            <ResponseField name="json_key" type="string" required>
              The JSON key within the secret that contains the credential value Required.
            </ResponseField>
            <ResponseField name="secret_id" type="string" required>
              The ID or ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="authorization_private_key_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags for the payment credential provider Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="created_time" type="string">
  The timestamp when the credential provider was created Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="credential_provider_arn" type="string">
  The Amazon Resource Name (ARN) of the payment credential provider Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The timestamp when the credential provider was last updated Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provider_configuration_output" type="object">
  Provider configuration output containing secret ARNs (no raw secrets) Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="coinbase_cdp_configuration" type="object">
      Coinbase CDP configuration output with secret ARNs Optional.
      <Expandable title="properties">
        <ResponseField name="api_key_id" type="string" required>
          The Coinbase CDP API key ID Required.
        </ResponseField>
        <ResponseField name="api_key_secret_arn" type="object" required>
          Contains information about a secret in AWS Secrets Manager Required.
          <Expandable title="properties">
            <ResponseField name="secret_arn" type="string" required>
              The ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="api_key_secret_json_key" type="string">
          The JSON key within the secret that contains the API key secret value Optional.
        </ResponseField>
        <ResponseField name="api_key_secret_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
        <ResponseField name="wallet_secret_arn" type="object">
          Contains information about a secret in AWS Secrets Manager Optional.
          <Expandable title="properties">
            <ResponseField name="secret_arn" type="string" required>
              The ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="wallet_secret_json_key" type="string">
          The JSON key within the secret that contains the wallet secret value Optional.
        </ResponseField>
        <ResponseField name="wallet_secret_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="stripe_privy_configuration" type="object">
      Stripe Privy configuration output with secret ARNs Optional.
      <Expandable title="properties">
        <ResponseField name="app_id" type="string" required>
          The app ID provided by Privy Required.
        </ResponseField>
        <ResponseField name="app_secret_arn" type="object" required>
          Contains information about a secret in AWS Secrets Manager Required.
          <Expandable title="properties">
            <ResponseField name="secret_arn" type="string" required>
              The ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="app_secret_json_key" type="string">
          The JSON key within the secret that contains the app secret value Optional.
        </ResponseField>
        <ResponseField name="app_secret_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
        <ResponseField name="authorization_id" type="string" required>
          The authorization ID for the Stripe Privy integration Required.
        </ResponseField>
        <ResponseField name="authorization_private_key_arn" type="object" required>
          Contains information about a secret in AWS Secrets Manager Required.
          <Expandable title="properties">
            <ResponseField name="secret_arn" type="string" required>
              The ARN of the secret in AWS Secrets Manager Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="authorization_private_key_json_key" type="string">
          The JSON key within the secret that contains the authorization private key value Optional.
        </ResponseField>
        <ResponseField name="authorization_private_key_source" type="string">
          The source of the secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided secrets. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
