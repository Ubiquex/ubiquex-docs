---
title: "aws_bedrock_use_case_for_model_access"
description: "Real, generated bindings for `aws_bedrock_use_case_for_model_access`."
---

`aws_bedrock_use_case_for_model_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrock"

    cfg := bedrock.UseCaseForModelAccessConfig{
        FormData: "example",
    }
    ubx.Resource(bedrock.UseCaseForModelAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UseCaseForModelAccess } from
      "jsr:@ubx/sdk-aws/aws/bedrock/use_case_for_model_access";

    resource(UseCaseForModelAccess, "example", {
      formData: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrock import (
        UseCaseForModelAccess,
        UseCaseForModelAccessConfig,
    )

    cfg = UseCaseForModelAccessConfig(
        form_data="example",
    )
    ubx.resource(UseCaseForModelAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrock_use_case_for_model_access with form_data.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="form_data" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
