---
title: "aws_bedrock_use_case_for_model_access"
description: "Real, generated bindings for `aws_bedrock_use_case_for_model_access`."
---

`aws_bedrock_use_case_for_model_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own use case for model access"})

    		ubx.Resource(bedrock.UseCaseForModelAccess, "ci-runner", bedrock.UseCaseForModelAccessConfig{
    			FormData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UseCaseForModelAccess } from "jsr:@ubx/sdk-aws/aws/bedrock/use_case_for_model_access";

    export default stack("payments", () => {
      intent({ summary: "payments own use case for model access" });

      resource(UseCaseForModelAccess, "ci-runner", {
        formData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import UseCaseForModelAccess, UseCaseForModelAccessConfig

    def describe():
        ubx.intent("payments own use case for model access")

        ubx.resource(UseCaseForModelAccess, "ci-runner", UseCaseForModelAccessConfig(
            form_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_use_case_for_model_access, with form data
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="form_data" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
