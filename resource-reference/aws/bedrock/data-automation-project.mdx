---
title: "aws_bedrock_data_automation_project"
description: "`aws_bedrock_data_automation_project` configures how Bedrock Data Automation extracts structured insights, text, tables, entities..."
---

`aws_bedrock_data_automation_project` configures how Bedrock Data Automation extracts structured insights, text, tables, entities, from unstructured documents, images, audio, and video, the real container jobs run through and that an `aws_bedrock_data_automation_library` can be associated with.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data automation project"})

    		ubx.Resource(bedrock.DataAutomationProject, "example", bedrock.DataAutomationProjectConfig{
    			ProjectName:                 "example-project-name",
    			CustomOutputConfiguration:   map[string]any{"blueprints": []map[string]any{{"blueprint_arn": "example"}}},
    			KmsEncryptionContext:        "example",
    			KmsKeyId:                    "example",
    			OverrideConfiguration:       map[string]any{"audio": map[string]any{"language_configuration": map[string]any{"generative_output_language": "example"}}},
    			ProjectDescription:          "example",
    			ProjectType:                 "example",
    			StandardOutputConfiguration: map[string]any{"audio": map[string]any{"extraction": map[string]any{"category": map[string]any{"state": "example"}}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataAutomationProject } from "./local-sdk/aws/sdk/typescript/aws/bedrock/data_automation_project.ts";

    export default stack("example", () => {
      intent({ summary: "example own data automation project" });

      resource(DataAutomationProject, "example", {
        projectName: "example-project-name",
        customOutputConfiguration: { blueprints: [{ blueprintArn: "example" }] },
        kmsEncryptionContext: "example",
        kmsKeyId: "example",
        overrideConfiguration: {
          audio: { languageConfiguration: { generativeOutputLanguage: "example" } },
        },
        projectDescription: "example",
        projectType: "example",
        standardOutputConfiguration: {
          audio: { extraction: { category: { state: "example" } } },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.bedrock import DataAutomationProject, DataAutomationProjectConfig

    def describe():
        ubx.intent("example own data automation project")

        ubx.resource(DataAutomationProject, "example", DataAutomationProjectConfig(
            project_name="example-project-name",
            custom_output_configuration={"blueprints": [{"blueprint_arn": "example"}]},
            kms_encryption_context="example",
            kms_key_id="example",
            override_configuration={"audio": {"language_configuration": {"generative_output_language": "example"}}},
            project_description="example",
            project_type="example",
            standard_output_configuration={"audio": {"extraction": {"category": {"state": "example"}}}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_data_automation_project, with project name
    example-project-name, custom output configuration a set value,
    kms encryption context example, kms key id example, override
    configuration a set value, project description example, project
    type example, standard output configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="custom_output_configuration" type="object">
  Custom output configuration Optional.
  <Expandable title="properties">
    <ResponseField name="blueprints" type="list(object)">
      Specifies the list of blueprint configurations that the custom output uses to define how Bedrock Data Automation extracts and structures data from the input documents. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="blueprint_arn" type="string">
          The Amazon Resource Name (ARN) of the blueprint that defines the custom extraction schema applied to the data automation project's output configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="blueprint_stage" type="string">
          Specifies the stage of the blueprint (e.g., DEVELOPMENT or LIVE) to use for data extraction within the project's custom output configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="blueprint_version" type="string">
          Sets the version identifier of the blueprint that defines the data extraction schema for this output configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kms_encryption_context" type="any">
  KMS encryption context Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  KMS key identifier Optional.
</ResponseField>

<ResponseField name="override_configuration" type="object">
  Override configuration Optional.
  <Expandable title="properties">
    <ResponseField name="audio" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="language_configuration" type="object">
          Specifies the language (such as ENGLISH or MULTI_LANGUAGE) to apply when processing audio files in the data automation project, determining how speech is transcribed and analyzed. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="generative_output_language" type="string">
              Specifies the language code used for generative AI output when processing audio content in the project's override configuration, such as the language for summaries or insights generated from the audio. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="identify_multiple_languages" type="bool">
              Optional.
            </ResponseField>
            <ResponseField name="input_languages" type="list(string)">
              Specifies the list of languages expected in the audio input, which the data automation project uses to process and extract information from the audio. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="modality_processing" type="object">
          Specifies the audio modality processing configuration, letting you choose between automatic processing of audio data or overriding it with a custom blueprint ARN. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Specifies whether audio modality processing is enabled or disabled for this Bedrock data automation project, overriding the default audio processing configuration. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sensitive_data_configuration" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="detection_mode" type="string">
              Optional.
            </ResponseField>
            <ResponseField name="detection_scope" type="list(string)">
              The types of sensitive data (e.g., PII categories) that Bedrock Data Automation will detect in the audio content. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pii_entities_configuration" type="object">
              Specifies the configuration for detecting and redacting specific PII entity types (such as names, credit card numbers, or addresses) in audio data processed by the AWS Bedrock data automation project, enabling selective redaction of sensitive information. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="pii_entity_types" type="list(string)">
                  Specifies the list of personally identifiable information (PII) entity types to detect and redact in audio content, overriding the project's default sensitive data configuration for audio. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="redaction_mask_mode" type="string">
                  Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="document" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="modality_processing" type="object">
          Configures the document modality processing type (e.g., extraction or summarization) applied when overriding the project's default document processing settings. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              When set, determines whether the document modality processing is enabled or disabled for the project's override configuration. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sensitive_data_configuration" type="object">
          Controls how sensitive data (such as personally identifiable information) is handled when processing documents in this Bedrock Data Automation project, typically to enable or configure automatic redaction of such content in the extraction output. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="detection_mode" type="string">
              Optional.
            </ResponseField>
            <ResponseField name="detection_scope" type="list(string)">
              Defines the detection scope for sensitive data in documents by listing the specific sensitive data types (e.g., credit card numbers, bank account numbers) that should be detected, overriding any default scope set at the project level. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pii_entities_configuration" type="object">
              This object defines the PII entities configuration for document sensitive data handling, specifying which PII entity types to detect and the action to take on them (e.g., redact or mask) when processing documents in the data automation project. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="pii_entity_types" type="list(string)">
                  Specifies the list of personally identifiable information (PII) entity types (such as NAME, EMAIL, or PHONE_NUMBER) that the Bedrock data automation project should detect and apply the configured sensitive data handling to for document processing when this override is used. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="redaction_mask_mode" type="string">
                  Specifies how detected PII entities are redacted, either by replacing them with a fixed mask character or by substituting the entity type label. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="splitter" type="object">
          Configures the document splitter for the Bedrock data automation project, determining how uploaded documents are segmented (e.g., by page or semantic sections) before extraction. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Determines whether the document splitter component is enabled or disabled in the override configuration for this Bedrock data automation project. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image" type="object">
      Specifies the override settings for how image data is processed in the data automation project, including the extraction type used when extracting content from images. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="modality_processing" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Specifies whether image modality processing is enabled or disabled in the override configuration, controlling if the data automation project processes image content. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sensitive_data_configuration" type="object">
          Configures how sensitive data (such as PII) is handled during image extraction in this Bedrock Data Automation project, overriding the project-level default sensitive data settings for image modality. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="detection_mode" type="string">
              Optional.
            </ResponseField>
            <ResponseField name="detection_scope" type="list(string)">
              Optional.
            </ResponseField>
            <ResponseField name="pii_entities_configuration" type="object">
              This object configures which PII entity types are detected or redacted in images as part of the sensitive data configuration override for the Bedrock Data Automation project. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="pii_entity_types" type="list(string)">
                  Specifies the list of PII entity types (e.g., NAME, EMAIL, PHONE) to detect or redact in images when the sensitive data configuration is enabled under the image override settings. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="redaction_mask_mode" type="string">
                  Specifies how PII entities detected in images are masked during redaction, with options such as replacing them with their entity type, a generic label, a hash, or a redaction box. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="modality_routing" type="object">
      Modality routing configuration Optional.
      <Expandable title="properties">
        <ResponseField name="jpeg" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="mov" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="mp4" type="string">
          When set, this field maps the MP4 media type to a specific Amazon Bedrock Data Automation blueprint ARN for the project's override configuration, so MP4 video inputs are processed using that blueprint instead of the project's default routing. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="png" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="video" type="object">
      Overrides the default data automation configuration for video content in the project, specifying settings for the video extraction stage and splitter stage. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="modality_processing" type="object">
          Specifies the video-specific processing settings within the override configuration of the Bedrock data automation project, allowing customization of how video content is handled during data automation. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Determines whether the video modality processing override is enabled (ENABLED) or disabled (DISABLED) for the data automation project's video processing configuration. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sensitive_data_configuration" type="object">
          Configures sensitive data detection (such as PII) for video processing in this project's override configuration, letting you adjust which sensitive data rules are applied to video content. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="detection_mode" type="string">
              Optional.
            </ResponseField>
            <ResponseField name="detection_scope" type="list(string)">
              Specifies which categories of sensitive data to detect in video content when overriding the blueprint's sensitive data configuration. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pii_entities_configuration" type="object">
              Defines which PII entity types are detected in video content and the redaction action (such as BLOCK or REDACT) applied to each, as part of the sensitive data configuration in the override settings for this Bedrock Data Automation project. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="pii_entity_types" type="list(string)">
                  Determines the specific PII entity types (e.g., EMAIL, PHONE) that the data automation project will detect or act on within video content under the given override configuration. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="redaction_mask_mode" type="string">
                  Specifies how redacted PII entities in video content are masked, determining whether the output uses a placeholder or the entity type as the mask. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project_description" type="string">
  Description of the DataAutomationProject Optional.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  Name of the DataAutomationProject Required.
</ResponseField>

<ResponseField name="project_type" type="string">
  Type of the DataAutomationProject - Sync or Async Optional.
</ResponseField>

<ResponseField name="standard_output_configuration" type="object">
  Standard output configuration Optional.
  <Expandable title="properties">
    <ResponseField name="audio" type="object">
      Defines the output settings for audio generated by the data automation project, including the audio file format and sample rate. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="extraction" type="object">
          Specifies the audio extraction settings, such as the types of transcripts or analysis to include, for the project's standard output configuration in AWS Bedrock Data Automation. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="category" type="object" required>
              Required.
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  Specifies whether the audio extraction category is enabled (ENABLED) or disabled (DISABLED) in the standard output configuration for the AWS::Bedrock::DataAutomationProject resource. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type_configuration" type="object">
                  Provides category-specific configuration for the selected audio extraction type (such as transcript or sentiment), with its nested fields varying based on the category's 'type' value, to tailor how that category is extracted from the audio input. Optional. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="transcript" type="object">
                      Optional.
                      <Expandable title="properties">
                        <ResponseField name="channel_labeling" type="object">
                          Configures the labeling of audio channels in the transcript data, so that extracted transcripts can identify which audio channel each speech segment belongs to. Optional. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="state" type="string" required>
                              Specifies whether channel labeling, which identifies and separates different audio channels or speakers in transcribed audio, is enabled or disabled for this transcript extraction category configuration in the audio standard output. Required. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="speaker_labeling" type="object">
                          This object configures speaker labeling (speaker diarization) for the audio transcript, controlling whether distinct speakers are identified and labeled in the output. Optional. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="state" type="string" required>
                              Specifies the state (e.g., ENABLED or DISABLED) of speaker labeling for transcript extraction within the audio standard output configuration of the Bedrock Data Automation project. Required. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="types" type="list(string)">
                  Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="generative_field" type="object">
          In the audio standard output configuration, this object defines a custom generative field -- specified by a name and description -- that AWS Bedrock Data Automation will attempt to generate from the processed audio. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string" required>
              Specifies whether generative field extraction is enabled (e.g., ENABLED) or disabled (e.g., DISABLED) for audio in the standard output configuration of the Bedrock Data Automation project. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="types" type="list(string)">
              Specifies the list of generative field types to be produced for audio data in the standard output configuration of the AWS Bedrock data automation project. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="document" type="object">
      Defines the standard output format for document processing, including the output type (such as plain text or structured data) and optionally a data automation blueprint to use. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="extraction" type="object">
          This object defines the document extraction settings for the project's standard output configuration, controlling whether extraction is enabled and which structured data types (e.g., KEY_VALUE_PAIRS, TABLES) are extracted from documents. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bounding_box" type="object" required>
              Determines whether bounding box coordinates are extracted from documents, and which granularity of bounding boxes (such as word or line) are included in the standard output configuration. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  Determines whether bounding box extraction is enabled for documents in the standard output configuration, with accepted values typically ENABLED or DISABLED. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="granularity" type="object" required>
              Configures the granularity level for document extraction in the standard output configuration, controlling whether extracted document data is returned at the document, page, or element level for the Amazon Bedrock Data Automation project. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="types" type="list(string)">
                  Specifies the granularity levels (such as document or page) at which document extraction results are produced in the standard output configuration of the Bedrock Data Automation project. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="generative_field" type="object">
          Defines a generative field for document output in AWS Bedrock Data Automation, specifying the field name, data type, and the prompt or instructions that guide the model to generate the field's value from the document's content. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string" required>
              Specifies whether the generative field is enabled (ENABLED) or disabled (DISABLED) for the document standard output configuration in the Bedrock data automation project. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="output_format" type="object">
          Specifies the output format for documents in the standard output configuration, determining the text format preset and any additional file formats (such as CSV or JSON) for generated extraction output. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="additional_file_format" type="object" required>
              This object configures whether Amazon Bedrock Data Automation generates an additional output file format for documents (such as CSV) alongside the primary format, with a state property that enables or disables this extra file. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  Specifies whether the additional file format for document output is enabled or disabled in the standard output configuration of the Bedrock data automation project. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="text_format" type="object" required>
              This object configures the text format types (such as plain text, markdown, or HTML) that the Bedrock data automation project will output for document extraction. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="types" type="list(string)">
                  Specifies the types of text output formats (e.g., plain text or Markdown) that the Bedrock Data Automation project will generate for extracted document content. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image" type="object">
      Defines the output configuration for images processed by the data automation project, controlling how image-derived data is extracted and formatted in the standard output. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="extraction" type="object">
          Defines the extraction configuration for image files in the standard output configuration, specifying which extraction types (e.g., text extraction) are enabled for the data automation project. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bounding_box" type="object" required>
              Specifies the configuration for bounding box extraction from images in the standard output of a Bedrock Data Automation project, typically including an enable/disable setting that controls whether bounding box coordinates are generated. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  Specifies whether bounding box extraction is enabled or disabled in the image extraction standard output configuration, where the required value is typically 'ENABLED' or 'DISABLED'. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="category" type="object" required>
              Required.
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  Specifies whether the image extraction category is enabled or disabled (as `ENABLED` or `DISABLED`) for the project's standard output configuration, controlling whether results from that category are generated. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="types" type="list(string)">
                  Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="generative_field" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="state" type="string" required>
              Whether generative field extraction from images is enabled or disabled, with valid values ENABLED and DISABLED. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="types" type="list(string)">
              Lists the types of generative fields to extract from images in the standard output configuration, with valid values including OCR, Form, Table, and KeyValue. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="video" type="object">
      Specifies the standard output configuration for video files in the AWS Bedrock data automation project, controlling which extraction types (e.g., transcript, caption) are produced from videos. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="extraction" type="object">
          Configures how video files are analyzed and extracted into structured data for the standard output format, specifying the extraction types (such as text, key-value pairs, or entities) to apply to video content. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bounding_box" type="object" required>
              This configuration object controls whether bounding box annotations for objects detected in video frames are generated and included in the standard output of the Bedrock Data Automation project. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  The state of bounding box extraction for video in the standard output configuration, controlling whether it is enabled or disabled. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="category" type="object" required>
              Required mapping that defines which video extraction categories (such as transcription, text detection, or content moderation) are enabled and their configuration for the standard output of the Bedrock data automation project. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="state" type="string" required>
                  Defines whether the specified video extraction category is enabled for processing within the project's standard output configuration, typically set to 'ENABLED' or 'DISABLED'. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="types" type="list(string)">
                  The list of category types to extract from the video, controlling which categorical outputs are generated in the standard output configuration. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="generative_field" type="object">
          Configures the generative AI field extraction for video standard output, defining the specific text fields (with associated prompts) that the Data Automation project will automatically generate from video content. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string" required>
              Indicates whether the generative field for video output is enabled or disabled in the standard output configuration, with allowed values 'ENABLED' or 'DISABLED'. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="types" type="list(string)">
              Specifies the list of generative field types (for example, VIDEO_SUMMARY, VIDEO_CHAPTERS, and VIDEO_HIGHLIGHTS) that the data automation project will produce from the video as part of the standard output configuration. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to apply to this bedrock data automation project. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a tag attached to the Bedrock Data Automation project, enabling arbitrary metadata such as cost centers or ownership labels. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The creation time for this bedrock data automation project. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The last modified time for this bedrock data automation project. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_arn" type="string">
  ARN of a DataAutomationProject Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_stage" type="string">
  Stage of the Project Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The current lifecycle state of the data automation project, such as Creating, Active, Updating, Deleting, or Failed. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
