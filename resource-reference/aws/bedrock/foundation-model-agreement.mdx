---
title: "aws_bedrock_foundation_model_agreement"
description: "Real, generated bindings for `aws_bedrock_foundation_model_agreement`."
---

`aws_bedrock_foundation_model_agreement` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/bedrock"

    cfg := bedrock.FoundationModelAgreementConfig{
        ModelId: "example",
        OfferToken: "example",
    }
    ubx.Resource(bedrock.FoundationModelAgreement, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FoundationModelAgreement } from
      "jsr:@ubx/sdk-aws/aws/bedrock/foundation_model_agreement";

    resource(FoundationModelAgreement, "example", {
      modelId: "example",
      offerToken: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.bedrock import (
        FoundationModelAgreement,
        FoundationModelAgreementConfig,
    )

    cfg = FoundationModelAgreementConfig(
        model_id="example",
        offer_token="example",
    )
    ubx.resource(FoundationModelAgreement, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_bedrock_foundation_model_agreement with model_id,
offer_token.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="model_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="offer_token" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
