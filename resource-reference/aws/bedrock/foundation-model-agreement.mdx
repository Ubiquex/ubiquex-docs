---
title: "aws_bedrock_foundation_model_agreement"
description: "Real, generated bindings for `aws_bedrock_foundation_model_agreement`."
---

`aws_bedrock_foundation_model_agreement` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own foundation model agreement"})

    		ubx.Resource(bedrock.FoundationModelAgreement, "ci-runner", bedrock.FoundationModelAgreementConfig{
    			ModelId:    "example",
    			OfferToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FoundationModelAgreement } from "jsr:@ubx/sdk-aws/aws/bedrock/foundation_model_agreement";

    export default stack("payments", () => {
      intent({ summary: "payments own foundation model agreement" });

      resource(FoundationModelAgreement, "ci-runner", {
        modelId: "example",
        offerToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import FoundationModelAgreement, FoundationModelAgreementConfig

    def describe():
        ubx.intent("payments own foundation model agreement")

        ubx.resource(FoundationModelAgreement, "ci-runner", FoundationModelAgreementConfig(
            model_id="example",
            offer_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_foundation_model_agreement, with model id
    example, offer token example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="model_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="offer_token" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
