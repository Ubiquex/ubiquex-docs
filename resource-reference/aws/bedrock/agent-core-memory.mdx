---
title: "aws_bedrock_agent_core_memory"
description: "`aws_bedrock_agent_core_memory` gives an agent both short-term memory, turn-by-turn context within one session, and long-term memory..."
---

`aws_bedrock_agent_core_memory` gives an agent both short-term memory, turn-by-turn context within one session, and long-term memory, retained knowledge across sessions, so it holds context and personalization without the caller building and managing their own memory store.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own agent core memory"})

    		ubx.Resource(bedrockagentcore.Memory, "example", bedrockagentcore.MemoryConfig{
    			EventExpiryDuration:     7200,
    			Name:                    "example",
    			Description:             "Managed by ubx.",
    			EncryptionKeyArn:        "arn:aws:iam::123456789012:policy/example",
    			IndexedKeys:             []map[string]any{{"key": "example"}},
    			MemoryExecutionRoleArn:  "arn:aws:iam::123456789012:policy/example",
    			StreamDeliveryResources: map[string]any{"resources": []map[string]any{{"kinesis": map[string]any{"content_configurations": []map[string]any{{"level": "example"}}}}}},
    			Tags:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Memory } from "@ubx/sdk-aws/aws/bedrockagentcore/memory";

    export default stack("example", () => {
      intent({ summary: "example own agent core memory" });

      resource(Memory, "example", {
        eventExpiryDuration: 7200,
        name: "example",
        description: "Managed by ubx.",
        encryptionKeyArn: "arn:aws:iam::123456789012:policy/example",
        indexedKeys: [{ key: "example" }],
        memoryExecutionRoleArn: "arn:aws:iam::123456789012:policy/example",
        streamDeliveryResources: {
          resources: [{
            kinesis: { contentConfigurations: [{ level: "example" }] },
          }],
        },
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore.memory import Memory, MemoryConfig

    def describe():
        ubx.intent("example own agent core memory")

        ubx.resource(Memory, "example", MemoryConfig(
            event_expiry_duration=7200,
            name="example",
            description="Managed by ubx.",
            encryption_key_arn="arn:aws:iam::123456789012:policy/example",
            indexed_keys=[{"key": "example"}],
            memory_execution_role_arn="arn:aws:iam::123456789012:policy/example",
            stream_delivery_resources={"resources": [{"kinesis": {"content_configurations": [{"level": "example"}]}}]},
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_agent_core_memory called "example", with event
    expiry duration 7200, description Managed by ubx., encryption key
    arn arn:aws:iam::123456789012:policy/example, indexed keys a set
    value, memory execution role arn
    arn:aws:iam::123456789012:policy/example, stream delivery
    resources a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Description of the Memory resource Optional.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  ARN format Optional.
</ResponseField>

<ResponseField name="event_expiry_duration" type="number" required>
  Duration in days until memory events expire Required.
</ResponseField>

<ResponseField name="indexed_keys" type="list(object)">
  List of indexed keys for the memory Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the name of a memory key that the agent indexes in its core memory, enabling structured retrieval of stored information for the Bedrock agent. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Defines the data type of each key in the core memory's indexed_keys list (e.g., string, number), determining how that key's value is validated and interpreted when indexing and querying core memory entries. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="memory_execution_role_arn" type="string">
  ARN format Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the Memory resource Required.
</ResponseField>

<ResponseField name="stream_delivery_resources" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="resources" type="list(object)" required>
      Required.
      <Expandable title="properties">
        <ResponseField name="kinesis" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="content_configurations" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="level" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="type" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="data_stream_arn" type="string">
              The Amazon Resource Name (ARN) of the Kinesis Data Stream that serves as the destination for streaming core memory events from the Bedrock agent. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  A map of tag keys and values Optional.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The time, as an ISO 8601 string, at which this agent core memory object was created in AWS Bedrock. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="failure_reason" type="string">
  The failure reason reported by AWS Bedrock when a core memory operation (such as creating or updating the core memory) fails, providing details about why the operation was unsuccessful. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="memory_arn" type="string">
  ARN of the Memory resource Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="memory_id" type="string">
  Unique identifier for the Memory resource Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="memory_strategies" type="list(object)">
  List of memory strategies attached to this memory Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="custom_memory_strategy" type="object">
      Defines the configuration for a custom memory strategy, allowing the Bedrock agent to use a tailored memory implementation instead of a predefined strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="configuration" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="episodic_override" type="object">
              The `episodic_override` object configures custom settings for episodic memory in the Bedrock agent's custom memory strategy, including the time-to-live for retaining recent conversation sessions and flags for enabling episodic memory processing. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="consolidation" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      Specifies the text to append to the prompt used during the consolidation phase of episodic memory in the custom memory strategy, enabling customization of how the agent summarizes past interactions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      The unique identifier (such as a model ID or ARN) of the Amazon Bedrock foundation model used to consolidate episodic memory entries in the custom memory strategy's episodic override. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction" type="object">
                  Specifies the extraction configuration for the episodic memory override, which determines how the agent extracts and processes episodic memories from conversation history via a custom prompt template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      Adds a custom instruction string to the end of the prompt used during episodic memory extraction for the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      The ARN or ID of an Amazon Bedrock foundation model used to extract and summarize episodic memories from conversation history. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="reflection" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      In the aws_bedrock_agent_core_memory resource, this string is appended to the prompt used when generating reflections for episodic memories, allowing you to inject custom guidance into the reflection process. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="memory_record_schema" type="object">
                      Specifies the JSON schema object that defines the structure and validation constraints for memory records produced by the reflection step of the episodic override in a custom memory strategy for an AWS Bedrock Agent Core Memory resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="metadata_schema" type="list(object)">
                          Defines the metadata schema for memory records by specifying a list of metadata fields (such as name and type) used by the reflective episodic memory in the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="extraction_config" type="object">
                              Determines how metadata for the memory record is extracted from the input data, specifying either a foundation model prompt or a regex pattern to populate the metadata schema fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="llm_extraction_config" type="object">
                                  Configures how the LLM extracts metadata fields for the reflection memory record schema, specifying the model and prompt template to use for extraction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `llm_extraction_config` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="extraction_type" type="string">
                              Optional; if omitted, computed by AWS.
                            </ResponseField>
                            <ResponseField name="key" type="string">
                              Defines the name (key) of a metadata attribute in the schema for episodic memory records, used to structure the metadata stored with each memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="type" type="string">
                              Specifies the JSON Schema type (e.g., 'object') for the metadata schema associated with a memory record schema in the reflection configuration of an episodic override for a custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      The Amazon Bedrock foundation model ID used to generate reflective insights or summaries from episodic memory in the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="namespace_templates" type="list(string)">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                    <ResponseField name="namespaces" type="list(string)">
                      Specifies the list of namespaces that the reflection process uses to group and consolidate episodic memories within the custom memory strategy's episodic override configuration, enabling the agent to synthesize and retrieve across those namespaces. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="self_managed_configuration" type="object">
              Configures the self-managed vector store (typically Amazon OpenSearch Serverless) used by the agent's custom memory, including connection details like the collection ARN, vector index name, and field mappings for storing and retrieving conversation memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="historical_context_window_size" type="number">
                  Specifies the number of recent conversation turns retained as historical context for the self-managed memory strategy in the Bedrock agent core memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="invocation_configuration" type="object">
                  Specifies the invocation configuration for the self-managed memory strategy, including the AWS Lambda function ARN and method used to execute memory fetch and update operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="payload_delivery_bucket_name" type="string">
                      The name of the Amazon S3 bucket to which the payload from the custom memory strategy's Lambda invocation is delivered for processing. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="topic_arn" type="string">
                      The ARN of the SNS topic to which the Bedrock agent publishes memory events for a self-managed custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="trigger_conditions" type="list(object)">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="message_based_trigger" type="object">
                      Specifies the message-based trigger condition for a self-managed custom memory strategy, defining when an incoming agent conversation message triggers memory extraction or storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="message_count" type="number">
                          Specifies the number of messages that must be exchanged in the session before the message-based trigger fires and the custom memory strategy captures or updates memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="time_based_trigger" type="object">
                      The time_based_trigger specifies a recurring schedule (using rate or cron expressions) that determines when the self-managed memory extraction is automatically run, controlling how often session data is captured into the core memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="idle_session_timeout" type="number">
                          The number of seconds of session inactivity after which the time-based trigger condition fires, causing the self-managed custom memory strategy to run its memory consolidation action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="token_based_trigger" type="object">
                      Configures the token-based trigger condition for the self-managed memory strategy, determining the token count threshold at which the agent's core memory is updated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="token_count" type="number">
                          The token count threshold that, when reached in the conversation, triggers the self-managed custom memory strategy operation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="semantic_override" type="object">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="consolidation" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      Specifies the Amazon Bedrock model or inference profile ID used to consolidate semantic memories when the semantic override consolidation is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction" type="object">
                  Specifies the extraction configuration (such as prompt templates or extraction rules) used by the custom semantic memory strategy to identify and extract memory-relevant facts and relationships from conversation transcripts when semantic override is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      Appends a custom string to the prompt used during semantic extraction of agent memory, adding instructions for how memories should be derived from conversation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      The ID or Amazon Resource Name (ARN) of the foundation model used to perform semantic extraction, overriding the default model for deriving memories from conversations in the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="summary_override" type="object">
              Specifies a custom prompt and token limits that override the default summary generation for the core memory custom strategy, enabling you to control how conversation sessions are summarized into memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="consolidation" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      Specifies a custom text string that is appended to the prompt used during the consolidation step of the custom memory strategy, allowing you to influence how the agent summarizes and consolidates stored session memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      The unique identifier of the Amazon Bedrock foundation model used by the consolidation step to summarize and merge stored memory observations in a custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="user_preference_override" type="object">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="consolidation" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      The unique identifier of the Bedrock model used to consolidate and summarize session memories when the user preference override is enabled for the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction" type="object">
                  In the custom memory strategy of an AWS Bedrock Agent Core Memory, this extraction configuration defines the instruction or template that tells the agent how to parse and save user preferences from interactions, overriding the default preference extraction behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="append_to_prompt" type="string">
                      The text to append to the prompt that instructs the agent on how to extract user preferences from the conversation, providing additional customization for the user preference override in the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="model_id" type="string">
                      Specifies the Amazon Bedrock model ID used for extracting user preferences from conversation history when the custom memory strategy's user preference override is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="created_at" type="string">
          The creation timestamp of the custom memory strategy, indicating when it was first configured. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          The description of the custom memory strategy, which provides a human-readable explanation of the strategy's purpose and configuration within the Bedrock agent's memory settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="memory_record_schema" type="object">
          Defines the JSON Schema that specifies the structure and fields of each memory record stored by the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metadata_schema" type="list(object)">
              Defines the list of metadata fields (each with a field name and data type) that are stored as metadata for each memory record in the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="extraction_config" type="object">
                  Defines how the metadata fields of a memory record are populated, specifying whether extraction is performed by an AWS Lambda function or the Bedrock agent itself. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="llm_extraction_config" type="object">
                      Configures the large language model (LLM) and its prompt used to extract metadata field values from agent conversations for a custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="definition" type="string">
                          The definition is a natural-language instruction that tells the LLM exactly which metadata fields to extract from the conversation and how to populate them for the memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="llm_extraction_instruction" type="string">
                          Specifies the natural language instruction that guides the large language model to extract the metadata values from a conversation and populate the fields defined in the metadata schema of the custom memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="validation" type="object">
                          Optional; if omitted, computed by AWS.
                          <Expandable title="properties">
                            <ResponseField name="number_validation" type="object">
                              Specifies validation constraints for number-typed metadata fields extracted from memory, such as minimum and maximum allowed values, ensuring numeric extractions fall within the defined range. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="max_value" type="number">
                                  Specifies the maximum numeric value allowed for a metadata field during LLM-based extraction validation, so that any extracted number exceeding this threshold is rejected. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="min_value" type="number">
                                  Specifies the minimum numeric value permitted for a metadata field that undergoes LLM extraction and numeric validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_list_validation" type="object">
                              Optional; if omitted, computed by AWS.
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Defines the list of permitted string values that an extracted metadata field must match for LLM-based extraction validation to succeed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="max_items" type="number">
                                  Sets the maximum number of string elements permitted in a metadata value that the model extracts, so that validation fails if the extracted string list exceeds this count. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_validation" type="object">
                              Configures the validation constraints for string-type metadata fields in the custom memory record schema, including minimum and maximum length and a regular expression pattern that the extracted value must match. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  The list of permitted string values that an extracted metadata string must match for validation to pass in the LLM extraction config. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction_type" type="string">
                  Determines whether the agent extracts the metadata field's value directly from the user's input (e.g., EXTRACT) or infers it from the conversation context (e.g., INFER) when creating a memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The key identifies the name of a metadata field in the metadata schema that defines the structure of custom memory records for the agent's core memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Sets the JSON Schema data type for the metadata schema within a custom memory strategy, typically 'object' to define a structured metadata object. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          Specifies a unique name for the custom memory strategy so the agent can store and recall custom memory records within its core memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespace_templates" type="list(string)">
          Defines the namespace templates for the custom memory strategy, where each string can include dynamic placeholders like $[sessionId] to generate scoped namespaces for storing memories. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespaces" type="list(string)">
          Defines the list of namespace strings used to isolate and organize memory entries within the custom memory strategy, so each namespace acts as a separate memory domain for the agent. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Indicates whether the custom memory strategy is enabled or disabled for the Bedrock Agent's core memory, with values typically 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strategy_id" type="string">
          The unique identifier for a custom memory strategy within an agent's core memory configuration, used to reference the strategy when managing memory operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type field of a custom memory strategy specifies the concrete backend implementation (for example, an Amazon S3 table store) that the Bedrock agent will use to store and retrieve memory data. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          This read-only attribute provides the timestamp of the most recent update to the custom memory strategy within the AWS Bedrock agent core memory configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="episodic_memory_strategy" type="object">
      Configures the episodic memory strategy for the Bedrock agent, determining how past conversation histories are stored and used for recall during future interactions. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="description" type="string">
          The description field provides an optional, user-defined explanation for the episodic memory strategy, which stores and recalls past conversation context to help the agent maintain continuity and relevance in its responses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="memory_record_schema" type="object">
          For the Bedrock agent's episodic memory strategy, this object specifies the record schema that defines the fields (name and type) for each stored memory entry, enabling the agent to store and recall structured interaction data. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metadata_schema" type="list(object)">
              Defines the list of metadata attributes (each with name and type) that can accompany episodic memory records, enabling structured storage and retrieval of additional context alongside each memory entry. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="extraction_config" type="object">
                  Specifies how metadata fields in the episodic memory record's schema are automatically extracted from conversation traces, including the extraction method (e.g., a Bedrock model) and the field selection mode (e.g., ALL or SUBSET). Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="llm_extraction_config" type="object">
                      Defines the configuration for using a large language model to automatically extract metadata fields from memory records within the episodic memory strategy's metadata schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="definition" type="string">
                          Defines the natural-language instructions that guide the LLM in which metadata fields to extract from a conversation and how to populate them for an episodic memory record, following the specified metadata schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="llm_extraction_instruction" type="string">
                          A natural-language instruction that guides the LLM on which metadata fields to extract from a memory record and how to map them into the schema defined by the metadata_schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="validation" type="object">
                          The validation object specifies a regular expression pattern that the LLM-extracted metadata value must match to be considered valid for storage in the episodic memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="number_validation" type="object">
                              Defines validation rules for numeric metadata values extracted by an LLM, such as specifying minimum and maximum allowable numbers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="max_value" type="number">
                                  The upper bound for numeric validation of LLM-extracted metadata values, used to reject numbers above this maximum when validating against the memory record schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="min_value" type="number">
                                  Defines the minimum numeric value that an LLM-extracted number must meet to pass validation for the metadata schema in the episodic memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_list_validation" type="object">
                              Defines validation rules for a string-list metadata field, specifying an allowed set of strings that the extracted list must match after LLM-based extraction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Specifies the definitive list of permissible string values that the LLM-extracted metadata field must match during memory record validation, constraining the episodic memory's metadata to a controlled vocabulary. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="max_items" type="number">
                                  Sets the maximum number of items allowed in a string list value within the memory record metadata schema when the metadata is extracted and validated using LLM extraction in the episodic memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_validation" type="object">
                              Optional; if omitted, computed by AWS.
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Restricts the LLM-extracted metadata value to one of the listed string values, enforcing an allowed-values constraint during validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction_type" type="string">
                  Determines whether the metadata schema for episodic memory records is automatically populated by the agent (AUTO) or defined through custom configuration (CUSTOM), controlling how metadata fields are extracted and stored. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The key that names a single metadata attribute within the episodic memory record's metadata schema, used to identify and store custom metadata on each memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the JSON Schema type (e.g., 'OBJECT') for the metadata schema in the episodic memory strategy, determining the structure of metadata attached to each memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="namespace_templates" type="list(string)">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="namespaces" type="list(string)">
          For the Bedrock Agent Core Memory resource, this field defines the list of namespaces used by the episodic memory strategy to partition memories into distinct logical scopes for targeted storage and retrieval. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reflection_configuration" type="object">
          Configures how often the agent reflects on past episodes and the maximum number of reflection summaries to retain, enabling episodic memory to distill insights from prior interactions. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="memory_record_schema" type="object">
              Specifies the schema, as a set of named fields with associated data types, that defines the structure of memory records stored by the agent's episodic memory reflection configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="metadata_schema" type="list(object)">
                  Defines the list of field objects (name and data type) that constitute the metadata schema for each episodic memory record, allowing structured filtering of memories. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="extraction_config" type="object">
                      Optional; if omitted, computed by AWS.
                      <Expandable title="properties">
                        <ResponseField name="llm_extraction_config" type="object">
                          This object configures the LLM-based metadata extraction process for the episodic memory strategy, specifying the prompt and model settings used to extract structured metadata fields from memory record content. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="definition" type="string">
                              Defines the natural-language prompt that instructs the LLM to extract metadata fields from the conversation to populate the record's metadata schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="llm_extraction_instruction" type="string">
                              The instruction string that tells the large language model which metadata fields to extract from an episodic conversation or interaction for inclusion in the memory record schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="validation" type="object">
                              Optional; if omitted, computed by AWS.
                              <Expandable title="properties">
                                <ResponseField name="number_validation" type="object">
                                  Defines numeric validation rules (such as minimum and maximum allowed values) for a metadata field extracted by the LLM during memory reflection in the Bedrock Agent Core Memory configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `number_validation` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="string_list_validation" type="object">
                                  Optional; if omitted, computed by AWS.
                                  <Expandable title="properties">
                                    <Note>Nested `string_list_validation` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                                <ResponseField name="string_validation" type="object">
                                  Specifies the validation rules (such as pattern matching or length limits) applied to string values in the metadata extracted from memory for the LLM-based extraction configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                  <Expandable title="properties">
                                    <Note>Nested `string_validation` structure continues beyond 8 levels of depth; further properties omitted for readability.</Note>
                                  </Expandable>
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="extraction_type" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                    <ResponseField name="key" type="string">
                      The metadata field name (key) for a single entry in the memory record's metadata schema, defining which custom metadata attribute is stored for episodic memory records. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="type" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="namespace_templates" type="list(string)">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="namespaces" type="list(string)">
              Lists the namespaces that the episodic memory reflection configuration is scoped to, restricting which memory entries the agent considers when generating reflections. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          Indicates whether the episodic memory strategy is enabled or disabled for the agent's core memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strategy_id" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the episodic memory strategy, which must be set to the value 'EPISODIC_MEMORY' for the agent to use episodic memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="semantic_memory_strategy" type="object">
      Configures the semantic memory strategy for the agent's core memory, specifying how the agent stores and retrieves semantic memories using a vector index. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          The timestamp indicating when the semantic memory strategy was created. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          A user-supplied description for the semantic memory strategy that explains how the agent's core memory captures and uses semantic knowledge. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="memory_record_schema" type="object">
          Defines the JSON schema that specifies the structure of memory records stored by the semantic memory strategy, including field names and data types. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metadata_schema" type="list(object)">
              Defines the JSON schema that specifies the structure and allowed fields of the metadata that can be attached to each memory record in the semantic memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="extraction_config" type="object">
                  Defines how the metadata fields of a semantic memory record are automatically populated by a model from the conversation, such as specifying the extraction prompt and inference model. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="llm_extraction_config" type="object">
                      Configures the LLM used to automatically extract metadata from memory records, specifying the model ARN and prompt template for extraction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="definition" type="string">
                          Specifies a natural-language description that instructs the large language model how to extract metadata fields from each memory record stored in semantic memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="llm_extraction_instruction" type="string">
                          Specifies the instruction text that guides the large language model on what metadata fields to extract from the memory content to populate the metadata schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="validation" type="object">
                          Optional; if omitted, computed by AWS.
                          <Expandable title="properties">
                            <ResponseField name="number_validation" type="object">
                              Defines numeric constraints such as minimum and maximum allowed values for a number-type metadata field extracted by the LLM during semantic memory record validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="max_value" type="number">
                                  Specifies the maximum value allowed for a numeric metadata field when validating extracted values during LLM extraction for a Bedrock agent's semantic memory record schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="min_value" type="number">
                                  Defines the minimum numeric value that an extracted number field must equal or exceed to pass LLM extraction validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_list_validation" type="object">
                              Specifies validation rules for string list metadata fields extracted by the LLM during semantic memory extraction, including constraints like allowed values and maximum number of elements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Defines the fixed set of string values that the LLM-extracted metadata value must be restricted to, ensuring the extraction output matches one of the allowed options. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="max_items" type="number">
                                  Sets the maximum number of items permitted in a string list that the LLM can extract for a metadata schema field, enforcing a size limit for list values during semantic memory extraction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_validation" type="object">
                              Specifies the validation constraints for string fields in the metadata schema extracted by the LLM, enforcing rules like allowed patterns and length restrictions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  The list of permitted string values that a metadata field must match to be considered valid during LLM extraction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction_type" type="string">
                  Defines the method used to extract metadata values for semantic memory records, controlling whether metadata is parsed automatically from the record content or derived from a predefined schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  This key defines the field name (attribute name) for an entry in the metadata schema of a semantic memory record, specifying the key under which metadata values are stored in the memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the semantic memory strategy configuration, used to identify this strategy within the agent's memory setup. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespace_templates" type="list(string)">
          In the semantic memory strategy for an Amazon Bedrock agent, this list defines the namespace templates used to partition and organize memory entries into distinct semantic namespaces, so that memories can be scoped and retrieved by the generated namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespaces" type="list(string)">
          Specifies the list of vector store namespaces used to scope the storage and retrieval of semantic memory entries for this strategy in the Bedrock agent's core memory configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Specifies whether the semantic memory strategy is enabled, with valid values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strategy_id" type="string">
          The user-defined unique identifier for the semantic memory strategy, used to reference and manage this strategy within the agent's core memory configuration, such as when updating or deleting its associated memory data. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the vector store provider (such as AMAZON_BEDROCK, OPENAI, PINECONE, or REDIS) used for semantic memory in the agent's memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          The timestamp (in ISO 8601 format) indicating when the agent's core memory semantic memory strategy was last modified, useful for auditing changes to the memory settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="summary_memory_strategy" type="object">
      Configure the summary memory strategy for a Bedrock agent, specifying the summary length (short, medium, or long) and the number of recent conversation tokens to use when generating session summaries. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="memory_record_schema" type="object">
          Defines the structure of the memory records that the summary memory strategy stores, by specifying a list of fields (each with a name and data type) that make up each memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metadata_schema" type="list(object)">
              Specifies the list of metadata field definitions for memory records stored under a summary memory strategy, where each object represents a metadata attribute's name and data type. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="extraction_config" type="object">
                  Configures how the agent extracts the metadata fields defined in the parent metadata schema from conversation content when using the summary memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="llm_extraction_config" type="object">
                      Configures the large language model and its inference settings used to extract metadata fields for summary memory records from the conversation input. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="definition" type="string">
                          Specifies the natural-language prompt/instructions that guide the LLM to extract metadata fields from the conversation for inclusion in the memory record's metadata schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="llm_extraction_instruction" type="string">
                          Specifies the natural language instruction provided to the large language model to guide how it should extract and populate the metadata fields defined in the metadata schema for a memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="validation" type="object">
                          Defines validation rules for the metadata extracted by the LLM, such as requiring fields to be present or non-empty, ensuring the extracted output conforms to the memory record schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="number_validation" type="object">
                              Optional; if omitted, computed by AWS.
                              <Expandable title="properties">
                                <ResponseField name="max_value" type="number">
                                  The inclusive maximum numeric value allowed for a validated number when the LLM extraction config applies number validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="min_value" type="number">
                                  The minimum numeric value that a number in the metadata field must meet to be considered valid during LLM-based extraction for the memory record schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_list_validation" type="object">
                              Configures validation rules for extracted metadata fields that are expected to be a list of strings, specifying constraints such as minimum and maximum list length and allowed string values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Specifies the allowed values for a string list metadata field, restricting the LLM extraction to only these strings. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="max_items" type="number">
                                  Specifies the maximum number of items allowed in a string list value for the metadata schema, enforced during validation in the LLM extraction configuration for the summary memory strategy's memory record schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_validation" type="object">
                              Defines validation constraints for string fields in the extracted memory metadata, such as minimum/maximum length and regex pattern requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Defines the set of allowed string values for a string-validated metadata field in the LLM extraction configuration of the metadata schema for a summary memory strategy, so that extracted metadata values outside this list are considered invalid. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction_type" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="key" type="string">
                  The key (property name) of an entry within the metadata schema for the summary memory strategy, defining a structured metadata field that accompanies each memory record. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the JSON Schema type (such as 'object') for the metadata schema, which dictates the shape of metadata attached to memory records in the summary memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The user-defined name that uniquely identifies this summary memory strategy within the agent's core memory configuration, used to reference the strategy when storing or retrieving memories. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="namespace_templates" type="list(string)">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="namespaces" type="list(string)">
          The namespaces field specifies the memory namespaces in which the summary memory strategy stores and retrieves summaries for the agent's conversation memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Controls whether the agent's summary memory is active, with allowed values ENABLED or DISABLED. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strategy_id" type="string">
          A customer-defined unique identifier for this summary memory strategy, used to reference and manage the strategy within the agent's memory configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of conversation summary memory to use, with allowed values 'RECENT' (maintains a summary of the most recent conversation segments) or 'LONG_TERM' (maintains a comprehensive summary across the entire conversation history). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          The timestamp indicating when the summary memory strategy configuration was last updated, reflecting changes to the summary prompt template or other strategy settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user_preference_memory_strategy" type="object">
      Configures the user preference memory strategy, which enables the Bedrock agent to store and utilize user preferences across sessions for personalized responses. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="description" type="string">
          Provides a human-readable description of the user preference memory strategy that the Bedrock agent uses to retain and recall user preferences across sessions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="memory_record_schema" type="object">
          Defines the JSON schema that specifies the structure and data types of user preference memory records stored by the agent, including valid keys and value types. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="metadata_schema" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="extraction_config" type="object">
                  Defines how metadata fields are extracted from user interactions for the memory record, specifying either the agent's built-in prompt or a custom prompt to perform extraction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="llm_extraction_config" type="object">
                      Defines the settings for LLM-based extraction of metadata from user conversations, including the prompt template that instructs the model how to map conversation content to the memory schema's metadata fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="definition" type="string">
                          Defines the natural language instruction that tells the LLM what metadata to extract from the conversation for storage in the user preference memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="llm_extraction_instruction" type="string">
                          The instruction text that guides the LLM to extract user preference memory records from conversations, specifying what to identify and how to format the extracted preferences. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="validation" type="object">
                          Optional; if omitted, computed by AWS.
                          <Expandable title="properties">
                            <ResponseField name="number_validation" type="object">
                              Specifies the minimum and/or maximum allowed values for a numeric metadata field when extracted by the LLM in a user preference memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="max_value" type="number">
                                  Specifies the maximum numeric value that a metadata field extracted by the LLM must not exceed when number validation is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="min_value" type="number">
                                  The minimum numeric value allowed for a number extracted by the LLM when validating metadata fields in user preference memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_list_validation" type="object">
                              Defines allowed values and item count constraints for string list metadata fields, which the LLM extraction configuration uses to validate extracted values before storing them in the memory record's metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  Specifies the allowed values that the extracted string list must match, ensuring metadata list values are constrained to this defined set. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="max_items" type="number">
                                  Specifies the maximum number of items allowed in a string array extracted from user input for memory record creation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                            <ResponseField name="string_validation" type="object">
                              The `string_validation` object specifies the validation constraints for string-type metadata fields within the user preference memory strategy, defining allowed values or a regular expression pattern that the extraction config applies when validating LLM-extracted metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="allowed_values" type="list(string)">
                                  The list of allowed string values that the LLM extraction must choose from when populating a metadata field, enforcing that the extracted value matches one of these permitted strings for validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="extraction_type" type="string">
                  Specifies the extraction approach for metadata in the user preference memory record schema, determining how the agent populates metadata values from conversation context. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  Specifies the name of a metadata field in the memory record schema for user preference memory, defining how user preference context is stored and retrieved. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the schema type of the metadata schema used to store user preferences within the Bedrock agent core memory's user preference memory strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="namespace_templates" type="list(string)">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="namespaces" type="list(string)">
          The list of namespaces that scope user preference memory to specific application domains, allowing the agent to store and retrieve user preferences separately for each namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Determines whether the user preference memory strategy is enabled (ENABLED) or disabled (DISABLED) for the Amazon Bedrock agent. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="strategy_id" type="string">
          The unique identifier for this user preference memory strategy, which the agent uses to reference and manage how user preferences are stored and recalled during interactions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the memory strategy type as USER_PREFERENCE, which enables the agent to record and use user preferences across sessions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          The timestamp of when the user preference memory was last updated. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  Status of the Memory resource Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The read-only timestamp, in ISO 8601 format, indicating when the agent core memory was last updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
