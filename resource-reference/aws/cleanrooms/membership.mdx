---
title: "aws_cleanrooms_membership"
description: "Real, generated bindings for `aws_cleanrooms_membership`."
---

`aws_cleanrooms_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cleanrooms"

    cfg := cleanrooms.MembershipConfig{
        CollaborationId: "example",
        QueryLogStatus: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cleanrooms.Membership, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Membership } from
      "jsr:@ubx/sdk-aws/aws/cleanrooms/membership";

    resource(Membership, "example", {
      collaborationId: "example",
      queryLogStatus: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cleanrooms import Membership, MembershipConfig

    cfg = MembershipConfig(
        collaboration_id="example",
        query_log_status="example",
        tags={"key": "value"},
    )
    ubx.resource(Membership, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cleanrooms_membership with collaboration_id,
query_log_status, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="collaboration_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query_log_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="collaboration_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="collaboration_creator_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="collaboration_creator_display_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="collaboration_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="member_abilities" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
