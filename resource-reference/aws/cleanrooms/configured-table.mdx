---
title: "aws_cleanrooms_configured_table"
description: "Real, generated bindings for `aws_cleanrooms_configured_table`."
---

`aws_cleanrooms_configured_table` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cleanrooms"

    cfg := cleanrooms.ConfiguredTableConfig{
        AllowedColumns: []string{"example"},
        AnalysisMethod: "example",
        Name: "example-name",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cleanrooms.ConfiguredTable, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConfiguredTable } from
      "jsr:@ubx/sdk-aws/aws/cleanrooms/configured_table";

    resource(ConfiguredTable, "example", {
      allowedColumns: ["example"],
      analysisMethod: "example",
      name: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cleanrooms import (
        ConfiguredTable,
        ConfiguredTableConfig,
    )

    cfg = ConfiguredTableConfig(
        allowed_columns=["example"],
        analysis_method="example",
        name="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(ConfiguredTable, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cleanrooms_configured_table with allowed_columns,
analysis_method, name, description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_columns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="analysis_method" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
