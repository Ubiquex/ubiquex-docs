---
title: "aws_clean_rooms_analysis_template"
description: "`aws_clean_rooms_analysis_template` pre-approves the exact SQL query or PySpark job a collaboration will allow..."
---

`aws_clean_rooms_analysis_template` pre-approves the exact SQL query or PySpark job a collaboration will allow, agreed on in advance by collaboration members so what analysis can run is fixed and reviewed rather than left open to whatever a member decides to query later.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cleanrooms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/cleanrooms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own analysis template"})

    		ubx.Resource(cleanrooms.AnalysisTemplate, "example", cleanrooms.AnalysisTemplateConfig{
    			Format:                    "example",
    			MembershipIdentifier:      "example",
    			Name:                      "example",
    			Source:                    map[string]any{"artifacts": map[string]any{"additional_artifacts": []map[string]any{{"location": map[string]any{"bucket": "example"}}}}},
    			AnalysisParameters:        []map[string]any{{"name": "example-name"}},
    			Description:               "Managed by ubx.",
    			ErrorMessageConfiguration: map[string]any{"type": "example"},
    			Schema:                    map[string]any{"referenced_tables": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalysisTemplate } from "@ubx/sdk-aws/aws/cleanrooms/analysis_template";

    export default stack("example", () => {
      intent({ summary: "example own analysis template" });

      resource(AnalysisTemplate, "example", {
        format: "example",
        membershipIdentifier: "example",
        name: "example",
        source: {
          artifacts: { additionalArtifacts: [{ location: { bucket: "example" } }] },
        },
        analysisParameters: [{ name: "example-name" }],
        description: "Managed by ubx.",
        errorMessageConfiguration: { type: "example" },
        schema: { referencedTables: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cleanrooms import AnalysisTemplate, AnalysisTemplateConfig
    from ubx.aws.cleanrooms.analysis_template import AnalysisTemplate_Source, AnalysisTemplate_Source_Artifacts, AnalysisTemplate_Source_Artifacts_AdditionalArtifacts, AnalysisTemplate_Source_Artifacts_AdditionalArtifacts_Location, AnalysisTemplate_AnalysisParameters, AnalysisTemplate_ErrorMessageConfiguration, AnalysisTemplate_Schema

    def describe():
        ubx.intent("example own analysis template")

        ubx.resource(AnalysisTemplate, "example", AnalysisTemplateConfig(
            format="example",
            membership_identifier="example",
            name="example",
            source=AnalysisTemplate_Source(artifacts=AnalysisTemplate_Source_Artifacts(additional_artifacts=[AnalysisTemplate_Source_Artifacts_AdditionalArtifacts(location=AnalysisTemplate_Source_Artifacts_AdditionalArtifacts_Location(bucket="example"))])),
            analysis_parameters=[AnalysisTemplate_AnalysisParameters(name="example-name")],
            description="Managed by ubx.",
            error_message_configuration=AnalysisTemplate_ErrorMessageConfiguration(type="example"),
            schema=AnalysisTemplate_Schema(referenced_tables=["example"]),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_clean_rooms_analysis_template called "example", with
    format example, membership identifier example, source a set
    value, analysis parameters a set value, description Managed by
    ubx., error message configuration a set value, schema a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="analysis_parameters" type="list(object)">
  The member who can query can provide this placeholder for a literal data value in an analysis template Optional.
  <Expandable title="properties">
    <ResponseField name="default_value" type="string">
      The default value to use for the analysis parameter when a value is not provided at runtime. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Specifies the name of an analysis parameter in the analysis template, which is used as a placeholder in the template's SQL query. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Defines the SQL data type of the parameter (e.g., INT, STRING, BOOLEAN) used to enforce type-checking when the analysis template is run in a Clean Rooms collaboration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  Specifies a user-provided description for the analysis template to help identify its purpose. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="error_message_configuration" type="object">
  Specifies the custom error message (including subject and body) that AWS Clean Rooms returns to users when a query based on this analysis template is denied or fails. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="type" type="string" required>
      Indicates the type of error message delivery mechanism; when set to 'S3', error messages from the analysis template are written to an Amazon S3 bucket configured in the accompanying S3 property. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="format" type="string" required>
  Specifies the query language of the analysis template, with valid values being 'SQL' or 'PYTHON'. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_identifier" type="string" required>
  The unique identifier of the membership that owns this analysis template. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the analysis template, used to identify and reference the template within the collaboration. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="schema" type="object">
  Specifies the referenced tables (via an AnalysisSchema object) that the analysis template's SQL query is allowed to access. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="referenced_tables" type="list(string)" required>
      The list of table names in the AWS Clean Rooms collaboration that the analysis template's SQL query is permitted to reference, restricting the query's data access. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source" type="object" required>
  The source object defines the SQL query content for the analysis template, containing the query text that will be executed against the collaboration data. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="artifacts" type="object">
      The S3 URIs of the analysis artifacts (such as SQL query files) that make up the source of the Clean Rooms analysis template. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="additional_artifacts" type="list(object)">
          Specifies additional artifacts (as objects containing artifact URIs) to be included with the analysis template's source, enabling the analysis to use supporting files beyond the primary SQL query. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="location" type="object">
              Specifies the S3 location (bucket and key) of an additional artifact, such as a supporting file or script, that is part of the analysis template's source artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket" type="string">
                  The S3 bucket name that stores an additional artifact (such as a custom analysis script or supporting file) for the AWS Clean Rooms analysis template's source, as specified in the source's artifacts list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The Amazon S3 object key (file name) of an additional artifact that is part of the analysis template's source artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entry_point" type="object">
          The entry_point object specifies the S3 location of the main Python script that serves as the starting point for the analysis in an AWS Clean Rooms analysis template. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="location" type="object">
              The S3 location of the entry point query file, specified as an object with a bucket and key that identify the SQL script in Amazon S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket" type="string">
                  The name of the Amazon S3 bucket that stores the entry-point artifact (the primary analysis file) for the analysis template's source artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="key" type="string">
                  The S3 object key within the specified S3 bucket that identifies the entry point file (e.g., a SQL script) for the analysis template's source artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of the IAM role that grants AWS Clean Rooms read access to the S3 objects in the source artifacts location. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="text" type="string">
      The SQL query text that defines the analysis template's source, used as the basis for the analysis. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_metadata" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="artifacts" type="object">
      Maps artifact names to their S3 URI locations, defining the source code artifacts for the analysis template within the source_metadata block. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="additional_artifact_hashes" type="list(object)">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="sha256" type="string">
              Specifies the SHA-256 hash of an additional artifact file that is part of the source metadata for the analysis template. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entry_point_hash" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="sha256" type="string">
              The SHA256 hash of the entry point artifact (SQL script) in the analysis template's source metadata, used to verify the integrity of the source code. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="synthetic_data_parameters" type="object">
  Configures the generation of synthetic data based on the analysis template's schema, including the desired row count, for testing analysis queries without using real data. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ml_synthetic_data_parameters" type="object">
      Configures the machine learning model parameters for generating a differentially private synthetic dataset from the analysis template's query results. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="column_classification" type="object">
          Specifies the per-column classification (such as categorical or numeric) applied when generating ML synthetic data from the analysis template's underlying dataset, controlling how each column is modeled by the synthetic data generator. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="column_mapping" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="column_name" type="string">
                  The name of the source column that is assigned a classification in the column mapping for generating ML synthetic data in the analysis template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="column_type" type="string">
                  The semantic type assigned to the mapped column (for example, categorical, numeric, datetime, or free_text) that AWS Clean Rooms uses to guide the ML synthetic data generation process for the analysis template. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="is_predictive_value" type="bool">
                  Indicates whether this column is classified as a predictive feature (input variable) for the machine learning model when generating synthetic data in AWS Clean Rooms, so the service preserves its relationship to the training label. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="epsilon" type="number">
          Specifies the privacy loss budget (epsilon) for the differentially private synthetic data generation, where lower values produce more private but less accurate data. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="max_membership_inference_attack_score" type="number">
          The maximum acceptable membership inference attack score for synthetic data generated for machine learning, providing a privacy threshold to limit the risk of inferring original data membership from the synthetic dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this cleanrooms analysis template. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The user-defined key of a tag attached to the AWS Clean Rooms analysis template, used to categorize, organize, and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="analysis_template_identifier" type="string">
  The unique identifier assigned by AWS to this analysis template, returned after creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the analysis template in AWS Clean Rooms. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collaboration_arn" type="string">
  The ARN of the collaboration that this analysis template belongs to. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collaboration_identifier" type="string">
  The unique identifier of the AWS Clean Rooms collaboration that this analysis template belongs to, which is assigned by AWS and cannot be changed. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="membership_arn" type="string">
  The ARN of the AWS Clean Rooms membership associated with this analysis template, automatically assigned by AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
