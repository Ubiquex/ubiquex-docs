---
title: "aws_alb_listener_certificate"
description: "Real, generated bindings for `aws_alb_listener_certificate`."
---

`aws_alb_listener_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/alb"

    cfg := alb.ListenerCertificateConfig{
        CertificateArn: "example",
        ListenerArn: "example",
    }
    ubx.Resource(alb.ListenerCertificate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ListenerCertificate } from
      "jsr:@ubx/sdk-aws/aws/alb/listener_certificate";

    resource(ListenerCertificate, "example", {
      certificateArn: "example",
      listenerArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.alb import (
        ListenerCertificate,
        ListenerCertificateConfig,
    )

    cfg = ListenerCertificateConfig(
        certificate_arn="example",
        listener_arn="example",
    )
    ubx.resource(ListenerCertificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_alb_listener_certificate with certificate_arn,
listener_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="listener_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
