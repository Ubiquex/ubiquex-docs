---
title: "aws_alb_target_group_attachment"
description: "Real, generated bindings for `aws_alb_target_group_attachment`."
---

`aws_alb_target_group_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	alb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/alb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target group attachment"})

    		ubx.Resource(alb.TargetGroupAttachment, "ci-runner", alb.TargetGroupAttachmentConfig{
    			TargetGroupArn:   "arn:aws:iam::123456789012:policy/ci",
    			TargetId:         "example",
    			AvailabilityZone: "example",
    			Port:             1,
    			QuicServerId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetGroupAttachment } from "jsr:@ubx/sdk-aws/aws/alb/target_group_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own target group attachment" });

      resource(TargetGroupAttachment, "ci-runner", {
        targetGroupArn: "arn:aws:iam::123456789012:policy/ci",
        targetId: "example",
        availabilityZone: "example",
        port: 1,
        quicServerId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.alb import TargetGroupAttachment, TargetGroupAttachmentConfig

    def describe():
        ubx.intent("payments own target group attachment")

        ubx.resource(TargetGroupAttachment, "ci-runner", TargetGroupAttachmentConfig(
            target_group_arn="arn:aws:iam::123456789012:policy/ci",
            target_id="example",
            availability_zone="example",
            port=1,
            quic_server_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_alb_target_group_attachment, with target group arn
    arn:aws:iam::123456789012:policy/ci, target id example,
    availability zone example, port 1, quic server id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="availability_zone" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="port" type="number">
  Optional.
</ResponseField>

<ResponseField name="quic_server_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
