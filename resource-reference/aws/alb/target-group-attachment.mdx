---
title: "aws_alb_target_group_attachment"
description: "Real, generated bindings for `aws_alb_target_group_attachment`."
---

`aws_alb_target_group_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/alb"

    cfg := alb.TargetGroupAttachmentConfig{
        TargetGroupArn: "example",
        TargetId: "example",
        AvailabilityZone: "example",
        Port: 1,
    }
    ubx.Resource(alb.TargetGroupAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TargetGroupAttachment } from
      "jsr:@ubx/sdk-aws/aws/alb/target_group_attachment";

    resource(TargetGroupAttachment, "example", {
      targetGroupArn: "example",
      targetId: "example",
      availabilityZone: "example",
      port: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.alb import (
        TargetGroupAttachment,
        TargetGroupAttachmentConfig,
    )

    cfg = TargetGroupAttachmentConfig(
        target_group_arn="example",
        target_id="example",
        availability_zone="example",
        port=1,
    )
    ubx.resource(TargetGroupAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_alb_target_group_attachment with target_group_arn,
target_id, availability_zone, port.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="availability_zone" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="port" type="number">
  Optional.
</ResponseField>

<ResponseField name="quic_server_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
