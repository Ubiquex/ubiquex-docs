---
title: "aws_flow_log"
description: "Real, generated bindings for `aws_flow_log`."
---

`aws_flow_log` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	flow "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/flow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log"})

    		ubx.Resource(flow.Log, "ci-runner", flow.LogConfig{
    			DeliverCrossAccountRole: "example",
    			EniId:                   "example",
    			IamRoleArn:              "arn:aws:iam::123456789012:policy/ci",
    			LogDestinationType:      "example",
    			MaxAggregationInterval:  1,
    			RegionalNatGatewayId:    "example",
    			SubnetId:                "example",
    			Tags:                    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Log } from "jsr:@ubx/sdk-aws/aws/flow/log";

    export default stack("payments", () => {
      intent({ summary: "payments own log" });

      resource(Log, "ci-runner", {
        deliverCrossAccountRole: "example",
        eniId: "example",
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        logDestinationType: "example",
        maxAggregationInterval: 1,
        regionalNatGatewayId: "example",
        subnetId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.flow import Log, LogConfig

    def describe():
        ubx.intent("payments own log")

        ubx.resource(Log, "ci-runner", LogConfig(
            deliver_cross_account_role="example",
            eni_id="example",
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            log_destination_type="example",
            max_aggregation_interval=1,
            regional_nat_gateway_id="example",
            subnet_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_flow_log, with deliver cross account role example, eni
    id example, iam role arn arn:aws:iam::123456789012:policy/ci, log
    destination type example, max aggregation interval 1, regional
    nat gateway id example, subnet id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deliver_cross_account_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="eni_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_destination" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_destination_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="log_format" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_aggregation_interval" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regional_nat_gateway_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="traffic_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_destination" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_format" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
