---
title: "aws_wafv2_regex_pattern_set"
description: "`aws_wafv2_regex_pattern_set` defines a real set of regular expressions that an `aws_wafv2_rule_group` or `aws_wafv2_web_acl` rule can match a request..."
---

`aws_wafv2_regex_pattern_set` defines a real set of regular expressions that an `aws_wafv2_rule_group` or `aws_wafv2_web_acl` rule can match a request part against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own regex pattern set"})

    		ubx.Resource(wafv2.RegexPatternSet, "ci-runner", wafv2.RegexPatternSetConfig{
    			RegularExpressionList: []string{"example"},
    			Scope:                 "example",
    			Name:                  "ci-runner",
    			Description:           "Managed by ubx.",
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegexPatternSet } from "./local-sdk/aws/sdk/typescript/aws/wafv2/regex_pattern_set.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own regex pattern set" });

      resource(RegexPatternSet, "ci-runner", {
        regularExpressionList: ["example"],
        scope: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafv2 import RegexPatternSet, RegexPatternSetConfig

    def describe():
        ubx.intent("payments own regex pattern set")

        ubx.resource(RegexPatternSet, "ci-runner", RegexPatternSetConfig(
            regular_expression_list=["example"],
            scope="example",
            name="ci-runner",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafv2_regex_pattern_set called "ci-runner", with
    regular expression list a set value, scope example, description
    Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Description of the entity. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Name of the RegexPatternSet. Optional.
</ResponseField>

<ResponseField name="regular_expression_list" type="list(string)" required>
  The list of regular expression patterns, each as a string, that this AWS WAFv2 regex pattern set uses to match against web request content. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="scope" type="string" required>
  Use CLOUDFRONT for CloudFront RegexPatternSet, use REGIONAL for Application Load Balancer and API Gateway. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tag objects (each containing a Key and Value) that are attached to the regex pattern set for AWS resource tagging and cost allocation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key, a string that identifies the tag assigned to this WAFv2 regex pattern set, used for resource organization, categorization, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag key-value pair attached to the AWS WAFv2 Regex Pattern Set, used to store arbitrary metadata for resource identification and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  ARN of the WAF entity. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Id of the RegexPatternSet Computed by AWS after `ship`.
</ResponseField>
