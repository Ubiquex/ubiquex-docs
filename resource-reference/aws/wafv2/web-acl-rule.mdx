---
title: "aws_wafv2_web_acl_rule"
description: "Real, generated bindings for `aws_wafv2_web_acl_rule`."
---

`aws_wafv2_web_acl_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web acl rule"})

    		ubx.Resource(wafv2.WebAclRule, "ci-runner", wafv2.WebAclRuleConfig{
    			Name:      "ci-runner",
    			Priority:  1,
    			WebAclArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebAclRule } from "jsr:@ubx/sdk-aws/aws/wafv2/web_acl_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own web acl rule" });

      resource(WebAclRule, "ci-runner", {
        name: "ci-runner",
        priority: 1,
        webAclArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.wafv2 import WebAclRule, WebAclRuleConfig

    def describe():
        ubx.intent("payments own web acl rule")

        ubx.resource(WebAclRule, "ci-runner", WebAclRuleConfig(
            name="ci-runner",
            priority=1,
            web_acl_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafv2_web_acl_rule called "ci-runner", with priority
    1, web acl arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="web_acl_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
