---
title: "aws_wafv2_web_aclassociation"
description: "`aws_wafv2_web_aclassociation` attaches a real `aws_wafv2_web_acl` to a specific protected resource, an ALB, API Gateway stage, or similar..."
---

`aws_wafv2_web_aclassociation` attaches a real `aws_wafv2_web_acl` to a specific protected resource, an ALB, API Gateway stage, or similar, the resource that actually turns the ACL's rules on for that resource's traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own web aclassociation"})

    		ubx.Resource(wafv2.WebAclassociation, "ci-runner", wafv2.WebAclassociationConfig{
    			ResourceArn: "arn:aws:iam::123456789012:policy/ci",
    			WebAclarn:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { WebAclassociation } from "./local-sdk/aws/sdk/typescript/aws/wafv2/web_aclassociation.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own web aclassociation" });

      resource(WebAclassociation, "ci-runner", {
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        webAclarn: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafv2 import WebAclassociation, WebAclassociationConfig

    def describe():
        ubx.intent("payments own web aclassociation")

        ubx.resource(WebAclassociation, "ci-runner", WebAclassociationConfig(
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            web_aclarn="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafv2_web_aclassociation, with resource arn
    arn:aws:iam::123456789012:policy/ci, web aclarn example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="resource_arn" type="string" required>
  The ARN of the AWS resource (such as an Application Load Balancer, API Gateway, or Amazon CloudFront distribution) that this WAFv2 web ACL is being associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="web_aclarn" type="string" required>
  The ARN of the AWS WAF v2 web ACL to associate with the specified resource. Required. **(AI-inferred)**
</ResponseField>
