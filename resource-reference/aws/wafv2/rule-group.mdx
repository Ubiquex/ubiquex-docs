---
title: "aws_wafv2_rule_group"
description: "`aws_wafv2_rule_group` defines a real, reusable, named set of WAFv2 rules that can be referenced from one or more `aws_wafv2_web_acl`s..."
---

`aws_wafv2_rule_group` defines a real, reusable, named set of WAFv2 rules that can be referenced from one or more `aws_wafv2_web_acl`s, so the same rule logic doesn't have to be redefined in every ACL that needs it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rule group"})

    		ubx.Resource(wafv2.RuleGroup, "ci-runner", wafv2.RuleGroupConfig{
    			Capacity:             1,
    			Scope:                "example",
    			VisibilityConfig:     map[string]any{"cloud_watch_metrics_enabled": true},
    			Name:                 "ci-runner",
    			CustomResponseBodies: "example",
    			Description:          "Managed by ubx.",
    			MonetizationConfig:   map[string]any{"crypto_config": map[string]any{"payment_networks": []map[string]any{{"chain": "example"}}}},
    			Rules:                []map[string]any{{"name": "example-name"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleGroup } from "./local-sdk/aws/sdk/typescript/aws/wafv2/rule_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own rule group" });

      resource(RuleGroup, "ci-runner", {
        capacity: 1,
        scope: "example",
        visibilityConfig: { cloudWatchMetricsEnabled: true },
        name: "ci-runner",
        customResponseBodies: "example",
        description: "Managed by ubx.",
        monetizationConfig: {
          cryptoConfig: { paymentNetworks: [{ chain: "example" }] },
        },
        rules: [{ name: "example-name" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafv2 import RuleGroup, RuleGroupConfig

    def describe():
        ubx.intent("payments own rule group")

        ubx.resource(RuleGroup, "ci-runner", RuleGroupConfig(
            capacity=1,
            scope="example",
            visibility_config={"cloud_watch_metrics_enabled": True},
            name="ci-runner",
            custom_response_bodies="example",
            description="Managed by ubx.",
            monetization_config={"crypto_config": {"payment_networks": [{"chain": "example"}]}},
            rules=[{"name": "example-name"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafv2_rule_group called "ci-runner", with capacity 1,
    scope example, visibility config a set value, custom response
    bodies example, description Managed by ubx., monetization config
    a set value, rules a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity" type="number" required>
  Specifies the web request capacity units (WCUs) allocated to this rule group, which must be at least the total capacity of all rules it contains and determines whether the group can be associated with a web ACL. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="custom_response_bodies" type="any">
  Custom response key and body map. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the entity. Optional.
</ResponseField>

<ResponseField name="monetization_config" type="object">
  Configures monetization for the web ACL or rule group. Optional.
  <Expandable title="properties">
    <ResponseField name="crypto_config" type="object">
      Configures cryptocurrency payment settings. Optional.
      <Expandable title="properties">
        <ResponseField name="payment_networks" type="list(object)" required>
          List of payment network configurations. Required.
          <Expandable title="properties">
            <ResponseField name="chain" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="prices" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="amount" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="currency" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="wallet_address" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="currency_mode" type="string">
      The currency mode for monetization. Use REAL for production payments and TEST for testing with testnet currencies. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Name of the RuleGroup. Optional.
</ResponseField>

<ResponseField name="rules" type="list(object)">
  Collection of Rules. Optional.
  <Expandable title="properties">
    <ResponseField name="action" type="object">
      Defines the action (Allow, Block, Count, Challenge, or Captcha) that AWS WAF takes when a web request matches the rule's conditions. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="allow" type="object">
          Defines the Allow action for this rule, which permits matching requests to continue to subsequent rules or the web ACL's default action, and optionally includes a customRequestHandling block to insert custom headers into the allowed request. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_request_handling" type="object">
              Specifies custom request handling for the Allow action, defining HTTP headers to insert into the request that is forwarded to the origin when the rule matches. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="insert_headers" type="list(object)">
                  Defines the list of custom HTTP headers to insert into the web request when the rule's allow action is triggered, where each entry specifies a header name and value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the custom HTTP header to insert into the request when the rule's Allow action uses custom request handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the custom header to insert into the request when the rule's allow action is taken. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="block" type="object">
          Sets the rule's action to block matching web requests, optionally including a custom response body and status code to return to the client. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_response" type="object">
              Specifies the custom HTTP response to return to the client when the rule action is set to block, including the response code, headers, and body content. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="custom_response_body_key" type="string">
                  Key referencing a custom response body defined in the rule group's CustomResponseBodies map, used when the block action returns a custom response to the client. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="response_code" type="number">
                  The HTTP status code that AWS WAF returns to the client when a request matches a rule configured with the Block action and a custom response. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="response_headers" type="list(object)">
                  Specifies custom HTTP headers that are added to the response returned to the client when a request is blocked by this rule's block action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the name of a custom HTTP header that AWS WAF includes in the blocking response for this rule action. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      Specifies the value for the custom HTTP header that AWS WAF returns in the blocked response. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="captcha" type="object">
          Specifies the CAPTCHA action for the rule, which challenges the client to solve a puzzle to verify it is human, and only continues the request if the challenge is completed successfully. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_request_handling" type="object">
              Custom request handling that inserts the specified HTTP headers into the request when the CAPTCHA action is taken, i.e., after the CAPTCHA challenge is successfully verified and the request is forwarded to the origin. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="insert_headers" type="list(object)">
                  Defines a list of custom HTTP headers (each with a name and value) that AWS WAF inserts into the request that triggered the CAPTCHA action, under this rule's custom_request_handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the custom HTTP header to insert into the request when the CAPTCHA action is taken and custom request handling is configured for the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      Specifies the value of the HTTP header to insert into the request when the CAPTCHA action is invoked, as part of the custom request handling for the rule group's rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="challenge" type="object">
          Defines a 'Challenge' action that forces the client to complete a JavaScript-based silent challenge to confirm it is not a bot, optionally including custom request handling for requests that pass the challenge. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_request_handling" type="object">
              Defines how to customize the HTTP request that AWS WAF forwards to the protected resource by inserting or overriding headers when the Challenge action is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="insert_headers" type="list(object)">
                  Specifies custom headers to insert into the request when the challenge action is triggered, overriding any existing headers with the same names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header that AWS WAF inserts into the request when the challenge action is applied, as part of custom request handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the custom header that AWS WAF inserts into the request forwarded to the protected resource when the Challenge action is triggered and custom request handling is configured. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="count" type="object">
          Sets the rule action to Count, which counts matching web requests without blocking or allowing them; this object may be empty or include optional custom request handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_request_handling" type="object">
              Defines custom request handling for the Count action, allowing WAF to insert or override headers in the request before forwarding it to the protected resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="insert_headers" type="list(object)">
                  Defines the list of custom header names and values that AWS WAF inserts into the forwarded request when the rule's Count action triggers, allowing you to tag matched requests with identifying headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header to insert into the request when the rule's count action is configured with custom request handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value of the custom header to insert into the request when the rule action is Count. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="monetize" type="object">
          Configures the rule's action to monetize matching requests through AWS Marketplace, allowing them to pass while enabling charging. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="price_multiplier" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="captcha_config" type="object">
      Specifies the CAPTCHA configuration for the rule, including the immunity time after a successful CAPTCHA challenge, used when the rule's action is CAPTCHA. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="immunity_time_property" type="object">
          Specifies the immunity time (in seconds) after a successful CAPTCHA challenge, during which a client is exempt from solving further CAPTCHA challenges in this rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="immunity_time" type="number">
              The number of seconds a CAPTCHA-challenged user is allowed to stay in the scope of the rule without being challenged again. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="challenge_config" type="object">
      Defines the immunity time settings for the AWS WAF Challenge action, which controls how long a successfully challenged client is allowed to pass without facing another challenge. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="immunity_time_property" type="object">
          Defines the immunity time property for the challenge action, specifying how long a successfully challenged client is exempt from being challenged again before the challenge is re-applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="immunity_time" type="number">
              The number of seconds (1-2592000) that a client is granted immunity from subsequent challenge actions after successfully completing a challenge, as specified in the challenge config's immunity time property. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="name" type="string">
      A descriptive name that uniquely identifies this rule within the rule group, used for tracking and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="priority" type="number">
      The numeric priority of the rule, which determines the order in which WAF evaluates rules within the rule group, with lower numbers evaluated first. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rule_labels" type="list(object)">
      Specifies the labels, each with a name, that are added to matching web requests so that downstream rules, logging, or other services can identify the traffic that matched this rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the label that AWS WAF adds to matching web requests when this rule matches. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="statement" type="object">
      Defines the inspection criteria, such as a byte match, IP set, geo match, or logical combination of conditions, that AWS WAF evaluates to decide whether the rule's action should be applied to a web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="and_statement" type="object">
          Specifies a logical AND statement that combines multiple nested statements, requiring all of them to match for the rule to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="statements" type="list(any)">
              The Statements property of an And statement in an AWS WAFv2 rule group defines the list of nested statements that must all evaluate to true for the And statement to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="asn_match_statement" type="object">
          Defines a statement that matches requests originating from IP addresses belonging to specified autonomous system numbers (ASNs). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="asn_list" type="list(number)">
              Specifies the list of autonomous system numbers (ASNs) that the rule uses to match requests based on the ASN of the requesting client, with the match succeeding if the client's ASN is in this list. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="forwarded_ipconfig" type="object">
              Specifies how to derive the client IP address when the request is forwarded by a proxy or load balancer, by defining the HTTP header to inspect and the fallback behavior if that header is absent. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fallback_behavior" type="string">
                  Defines whether a request with a missing or unparseable IP address in the configured forwarded header should be treated as matching (MATCH) or not matching (NO_MATCH) the ASN match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header in the request that contains the originating IP address that AWS WAF uses for ASN matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="byte_match_statement" type="object">
          Configures the byte match statement used to match a web request against a literal string pattern, including the search string, the request component to inspect (e.g., headers, body), and any text transformations to apply. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field_to_match" type="object">
              Specifies the part of the web request to inspect (e.g., headers, query string, body, or cookies) for the byte match rule in the rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all_query_arguments" type="any">
                  Specifies that the WAF rule should inspect all query arguments in the request's URL query string for a match, rather than a single named argument. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="body" type="object">
                  Specifies the request body as the part of the request to inspect in a byte match statement, with an optional oversize handling configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF treats a request body that exceeds the size limit for inspection in a byte match statement's field-to-match body, with valid values such as CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cookies" type="object">
                  Specifies the cookie match configuration for the byte match statement, including which cookies to inspect and how to handle oversize cookie names or values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Determines which cookies in the request are inspected by the byte match statement, either all cookies, a list of specific cookie names to include, or a list of cookie names to exclude. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When set to true, this field instructs the WAF rule to match against all cookies in the request rather than a specific subset, so the byte match condition applies to every cookie header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_cookies" type="list(string)">
                          The names of cookies to exclude from the inspection, meaning that the rule inspects all cookies except those listed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_cookies" type="list(string)">
                          Specifies the list of cookie names to inspect; when this list is provided, the byte match statement only evaluates the values of these named cookies, overriding any 'all' match pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether AWS WAF matches all cookies or only those whose names match a given prefix (KEY_PREFIX) or suffix (KEY_SUFFIX). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF should handle requests when the cookies in the request exceed the size limit for inspection, with valid values of CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="header_order" type="object">
                  Specifies that the WAF should inspect the order in which HTTP headers appear in the request, enabling rules to match on header sequence rather than individual header values or names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles the request when the header-order information size exceeds the limit, with valid values CONTINUE, MATCH, and NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="headers" type="object">
                  Specifies the configuration for inspecting all or selected web request headers, including the header name pattern and whether to inspect header keys, values, or both. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Defines the set of HTTP headers to inspect in the byte match statement, either all headers or a specific list of included or excluded header names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When set, this match pattern instructs AWS WAFv2 to inspect all header names in the request, disabling any header name filtering and applying the byte match to every header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_headers" type="list(string)">
                          A list of HTTP header names that are excluded from the header inspection, so the byte match rule applies only to the headers not listed here. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_headers" type="list(string)">
                          The list of HTTP header names that the byte match statement inspects, so only those headers are considered for matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines whether the byte match statement inspects the entire header (ALL), only the header name (KEY), or only the header value (VALUE) for the matched header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles oversized header keys or values in the request when inspecting headers, with allowed values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  Specifies that the byte match statement inspects the JA3 fingerprint of the TLS client that sent the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how the rule action is determined when the client's JA3 fingerprint is unavailable, with valid values MATCH and NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  Specifies the JA4 TLS fingerprint of the client's connection to match, with a fallback behavior for clients that do not present a JA4 fingerprint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Determines how AWS WAF handles requests that do not have a JA4 fingerprint (e.g., non-TLS traffic), where the value MATCH or NO_MATCH specifies whether such requests are treated as matching the byte match statement on the JA4 fingerprint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="json_body" type="object">
                  Specifies that the byte match statement inspects the request's JSON body, using the configured match pattern, match scope, and invalid fallback behavior to locate the JSON field to match against. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="invalid_fallback_behavior" type="string">
                      Determines whether AWS WAF treats a request as matching or not matching the rule when the request body is not valid JSON. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_pattern" type="object">
                      Specifies the portion of the JSON request body to inspect in a byte match statement, either matching all JSON content or only the included JSON paths (includedPaths). Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When the 'all' property is present in the JSON body match pattern, the byte match statement inspects the entire JSON body payload rather than restricting to specific JSON paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_paths" type="list(string)">
                          A list of JSON pointer paths (e.g., '/foo/bar') that define which parts of the JSON request body are inspected when performing the byte match, so only values at those paths are evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Controls whether the byte match inspects all parts of the JSON body, only keys, or only values when a request's JSON content is evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles the JSON body when its size exceeds the inspection limit, using CONTINUE to process the body despite the size, MATCH to automatically treat the rule as matched, or NO_MATCH to automatically treat the rule as not matched. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="method" type="any">
                  When set, this indicates that the web request's HTTP method (such as GET, POST, or PUT) should be used as the part of the request that the byte match statement inspects for matches. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="any">
                  Specifies that AWS WAF should inspect the query string of the web request as the part of the request to match against. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  A single_header field match configuration that instructs the rule to inspect only the named request header, with the header name provided in the Name property. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header that the byte match statement inspects in the web request, such as User-Agent or Referer. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="single_query_argument" type="object">
                  The single_query_argument field specifies a single query string parameter name (as an object with a 'name' attribute) whose value the byte match rule inspects for the field_to_match condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name (key) of the single query argument that AWS WAF inspects, defining which query parameter's value is evaluated against the byte match statement's search string. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_fragment" type="object">
                  When this empty object is present in the field_to_match, it causes the byte match statement to inspect the URI fragment portion of the request (the part after the '#' character), indicating that the rule's pattern should be evaluated against that fragment. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how AWS WAF should treat requests that do not contain a URI fragment (the part of the URL after the '#') when evaluating the byte match on the URI fragment, typically using a value like MATCH or NO_MATCH to decide whether such requests satisfy the condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="any">
                  Specifies that the byte match statement inspects the URI path of the web request, which is the part of the URL after the host and before the query string. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="positional_constraint" type="string">
              Determines how AWS WAF searches for the specified search string within the request component, such as EXACTLY, STARTS_WITH, ENDS_WITH, CONTAINS, or CONTAINS_WORD. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pre_parse_text_transformations" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  The numeric priority that controls the order in which this pre-parse text transformation is applied relative to other pre-parse text transformations, where lower numbers indicate earlier application. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="search_string" type="string">
              The exact string that the byte match statement searches for within the inspected web request component (e.g., a URI, query string, or header) to trigger the rule's action. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="search_string_base64" type="string">
              The base64-encoded text that AWS WAF uses as the pattern to match against the request component, typically used to represent non-printable or binary search strings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="text_transformations" type="list(object)">
              Specifies the ordered list of text transformations (for example, lowercase or HTML entity decode) that AWS WAF applies to the inspected content before the byte match statement evaluates it. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  The relative order in which AWS WAF applies this text transformation to the inspected string, with lower numbers processed first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the text transformation type to apply, such as LOWERCASE, URL_DECODE, or HTML_ENTITY_DECODE, to modify the request component before evaluating the byte match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="geo_match_statement" type="object">
          Sets a rule statement that matches requests based on the country of origin of the IP address, using ISO country codes to allow, block, or count traffic from specific geographic locations. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="country_codes" type="list(string)">
              The list of two-letter ISO 3166-1 alpha-2 country codes that the rule matches requests against when using a geo match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="forwarded_ipconfig" type="object">
              Configures how AWS WAF retrieves the client IP address from a request header (e.g., X-Forwarded-For) for geo matching, including the header name and the fallback behavior when that header is absent or invalid. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fallback_behavior" type="string">
                  Determines how AWS WAF labels a request when it cannot extract a valid IP address from the forwarded header in the GeoMatchStatement, using MATCH to treat it as matching or NO_MATCH to treat it as not matching the geographic condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header (e.g., X-Forwarded-For) that contains the originating client IP address, used by the geo match statement to evaluate geography when the request passes through a proxy or load balancer instead of using the source IP. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ipset_reference_statement" type="object">
          Defines a rule statement that matches web requests by checking whether the source IP address (or forwarded IP) is present in an AWS WAF IP set referenced by its ARN. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="arn" type="string">
              The ARN of the AWS WAF V2 IP set whose IP addresses this statement matches against. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ipset_forwarded_ipconfig" type="object">
              Configures how AWS WAF handles IP addresses in forwarded headers (such as X-Forwarded-For) when evaluating a request against the referenced IP set, including the header name and fallback behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fallback_behavior" type="string">
                  Determines how AWS WAF handles a request when the forwarded IP address to inspect is not present in the configured header, with allowed values MATCH or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header (e.g., X-Forwarded-For) that contains the client IP address when the IP set reference statement uses forwarded IP configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="position" type="string">
                  Specifies which IP address to use from the forwarded-for header (e.g., X-Forwarded-For) when the IP set reference is evaluated, with allowed values FIRST, LAST, or ANY. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="label_match_statement" type="object">
          Configures a label match statement to match a web request by checking for the presence of a specific label key, with a scope selection of either LABEL_NAMESPACE to match any label in a namespace or LABEL_MATCH to match an exact label. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The fully qualified label key (e.g., 'awswaf:managed:aws:bot-control:bot:verified' or a custom label like 'myrule:Bots') that the label match statement uses to check whether a request carries that label, triggering the rule if present. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scope" type="string">
              Determines whether the label match statement compares against the label namespace (LABEL_NAMESPACE) or the exact label name (LABEL_NAME) of labels added by prior rules in the web ACL. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="not_statement" type="object">
          Defines a logical NOT statement, which inverts the result of its nested statement so that the rule matches when the nested statement does not match. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="statement" type="any">
              The single nested rule statement that this NOT statement negates, so the rule matches when that nested statement does not match. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="or_statement" type="object">
          Defines a logical OR operator that combines multiple nested WAF rule statements, causing the parent rule to match if any one of the included statements matches. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="statements" type="list(any)">
              Specifies the list of nested rule statements that are combined using an OR logical operator, so the parent statement matches if any of these child statements match. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rate_based_statement" type="object">
          Defines a rate-based rule criteria that limits the number of requests from a source IP address (or aggregated key) within a trailing time window, triggering the rule's action when the rate exceeds the configured limit. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="aggregate_key_type" type="string">
              Specifies the key type used to aggregate requests for the rate-based rule's rate calculation, such as IP, FORWARDED_IP, or CUSTOM_KEYS. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="custom_keys" type="list(object)">
              This field specifies custom keys (such as a header, cookie, query argument, or HTTP method) that AWS WAF uses to aggregate requests for rate-based rule evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="asn" type="any">
                  When present, this property specifies that the ASN (Autonomous System Number) of the client IP address should be used as a custom key for the rate-based rule, so rate limits are applied per ASN. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cookie" type="object">
                  The cookie custom key specifies the name of a cookie in the web request to use as an aggregation key for rate-based rule evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the cookie that AWS WAF uses as a custom key when aggregating requests for rate-based rate limiting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="text_transformations" type="list(object)">
                      Specifies an ordered list of text transformations (each with a Priority and Type) that are applied to the cookie value used as a custom key for rate-based aggregation, ensuring the value is normalized before it is inspected. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="priority" type="number">
                          Determines the order in which the text transformations are applied to the cookie value for this custom key, with lower numbers evaluated first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the type of text transformation (e.g., LOWERCASE, URL_DECODE) to apply to the cookie value before the rate-based rule counts matching requests, as part of the custom key's cookie text transformations. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="forwarded_ip" type="any">
                  This block defines the forwarded IP configuration for a rate-based rule, indicating that the request's IP address should be derived from the X-Forwarded-For header (or a custom header) so that rate limits apply to the original client IP behind a proxy, with options for fallback behavior and header name. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header" type="object">
                  Defines a request header to use as a custom key for rate-based rule aggregation, specifying the header name and text transformations that are applied before evaluating the rate. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header used as a custom key in the rate-based rule's aggregation, so requests are grouped by that header's value for rate limiting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="text_transformations" type="list(object)">
                      Specifies the ordered list of text transformations (each with a priority and type) applied to the header value used as a custom key for rate-based aggregation, normalizing the value before counting requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="priority" type="number">
                          Defines the order in which this text transformation is applied relative to other transformations in the same list, where lower numbers are processed first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the text transformation type (e.g., LOWERCASE, NONE) to apply to the header value used as a custom key in the rate-based statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="httpmethod" type="any">
                  This field enables the HTTP method of the request to be used as a custom key for aggregating requests in the rate-based rule's rate limit evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ip" type="any">
                  Configures the client IP address as a custom aggregation key for a rate-based rule statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  Specifies the client's JA3 TLS fingerprint as a custom key for rate-based rule aggregation, including optional text transformations for validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies the fallback behavior (MATCH or NO_MATCH) for the JA3 fingerprint custom key in a rate-based rule when the fingerprint cannot be derived from the request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  Defines a custom key for rate-based rules that uses the JA4 TLS fingerprint of the client to aggregate requests for rate limiting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how AWS WAF handles requests without a JA4 fingerprint when JA4 is used as a custom key in a rate-based rule, where 'ORIGIN' applies the rate limit based on the request origin IP and 'NO_MATCH' excludes such requests from the custom key aggregation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="label_namespace" type="object">
                  Defines a custom key for a rate-based rule that uses a label namespace as the aggregation dimension, so the rule tracks and rate-limits requests separately for each distinct namespace value (set in the nested namespace field). Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="namespace" type="string">
                      Specifies the label namespace string that acts as a custom key for the rate-based rule, causing request aggregation based on labels that contain this namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="query_argument" type="object">
                  Specifies the name of a query string argument to use as a custom key for rate-based aggregation, so that rate limits are applied per unique value of that argument. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the exact name of the query argument to use as a custom key for aggregating requests in the rate-based rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="text_transformations" type="list(object)">
                      Specifies the text transformations to apply to the value of the specified query argument before using it as a custom aggregation key for rate-based rule evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="priority" type="number">
                          Specifies the order in which this text transformation is executed among the text transformations for the query argument custom key, with lower values applied first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the text transformation type to apply to the query argument value (e.g., NONE, LOWERCASE, URL_DECODE) when using it as a custom key in the rate-based rule statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="query_string" type="object">
                  Defines the request's query string as a custom aggregation key for the rate-based rule, and can specify text transformations to normalize the query string before counting requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="text_transformations" type="list(object)">
                      Defines the ordered list of text transformations (such as lowercase, URL decode, or replace) that AWS WAF applies to the request's query string value before it is used as an aggregation key in the rate-based rule's custom key. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="priority" type="number">
                          Sets the processing order for this text transformation within the list of transformations applied to the query string component of the rate-based rule's custom key, with the lowest numerical priority running first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the text transformation type (such as NONE, LOWERCASE, or URL_DECODE) to apply to the query string key value before inspection. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="object">
                  Defines the URI path of the web request as a custom aggregation key for the rate-based rule, with optional text transformations applied to the path value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="text_transformations" type="list(object)">
                      Specifies the list of text transformations (each with a priority and transformation type) applied to the URI path value used as a custom key in the rate-based rule's custom key aggregation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="priority" type="number">
                          Specifies the processing order for the text transformations applied to the URI path, where lower values are executed first and each priority within the list must be unique. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the type of text transformation to apply to the URI path, such as LOWERCASE, URL_DECODE, or HTML_ENTITY_DECODE, when evaluating the custom key. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="evaluation_window_sec" type="number">
              The evaluation window (in seconds) over which the rate-based rule aggregates requests from the specified aggregate key, accepting values of 60, 120, 300, or 600 seconds, with a default of 60 seconds when not specified. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="forwarded_ipconfig" type="object">
              Specifies the configuration for inspecting the client IP address from a forwarded header (e.g., X-Forwarded-For) for the rate-based rule, including the header name and the fallback behavior when the header is absent. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="fallback_behavior" type="string">
                  Determines whether the rule matches or does not match a request when the IP address cannot be parsed from the forwarded IP header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="header_name" type="string">
                  The name of the HTTP header that contains the client IP address to use for rate limiting when requests are forwarded through a proxy or load balancer, as specified by the forwarded IP configuration of the rate-based statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="limit" type="number">
              The maximum number of requests permitted within the rate-based rule's aggregation window (default 5 minutes) before the rule takes action to block subsequent requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scope_down_statement" type="any">
              A nested statement that narrows the requests evaluated by the rate-based rule; only requests matching this statement are counted against the rate limit. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="regex_match_statement" type="object">
          The regex match statement defines a rule condition that inspects a selected component of a web request (such as a header, query argument, or body) using a regular expression pattern, and triggers the rule when a match is found. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field_to_match" type="object">
              Determines which part of the web request (such as a header, query string, body, or URI path) the regex pattern is matched against in this WAFv2 rule group's regex match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all_query_arguments" type="any">
                  When this flag is set, the regex match statement inspects all query arguments (query string parameters) of the web request for the pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="body" type="object">
                  Specifies that the regex pattern is evaluated against the body of the HTTP request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles a request body that exceeds the maximum inspectable size when performing regex matching on the body, with valid values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cookies" type="object">
                  Specifies the configuration for inspecting cookies in the web request as part of the regex match statement, defining which cookies to match, the scope of the match (e.g., all cookies or a specific list), and the handling of oversize cookie lists. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Specifies which cookies the regex match statement inspects in the web request, using either all cookies, a list of included cookies, or a list of excluded cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When set to true, this field configures the cookie match pattern to match all cookies in the request instead of a specific set of cookie names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_cookies" type="list(string)">
                          The list of cookie names to exclude from the regex match pattern, meaning those cookies are not inspected when evaluating the regex against cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_cookies" type="list(string)">
                          Specifies the names of cookies to include in the match pattern, so that WAF inspects only those cookies in the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether the regex pattern applies to all cookies or only to cookies whose key starts with a specified prefix or ends with a specified suffix. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles a request when a cookie name or value exceeds the size limit, with allowed values CONTINUE, MATCH, or NO_MATCH for the regex match rule inspection. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="header_order" type="object">
                  Specifies that the regex pattern should be matched against the order of request header names, with optional handling for oversized header sections. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles the request when the total size of the header order (all headers) exceeds the limit, with valid values being CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="headers" type="object">
                  Specifies the header inspection criteria for the regex match statement, including the set of headers to inspect and the handling behavior for oversized header values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      The match_pattern field specifies which HTTP headers to inspect for the regex pattern, allowing selection of all headers or a specific set of included or excluded header names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Setting this field configures the header match pattern to match all headers in the request, rather than limiting to a specific included or excluded header name list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_headers" type="list(string)">
                          Specifies the list of HTTP header names to exclude from inspection, so the regex pattern is evaluated only against the remaining headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_headers" type="list(string)">
                          Specifies the names of the headers to inspect; only these headers' values are matched against the regex pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines whether the regex pattern is applied to the entire header, just the header name (key), or just the header value in the request's header, with allowed values of ALL, KEY, or VALUE. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF should handle incoming requests when a header field exceeds the maximum header size limit, such as continuing the inspection, matching, or not matching the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  Configures the rule to use the client's JA3 fingerprint (the TLS handshake fingerprint) as the field to match against the regex pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how AWS WAF should treat a request when the client's JA3 fingerprint is unavailable (e.g., due to an incomplete TLS handshake), with valid values being MATCH or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  This field configures the request's JA4 TLS fingerprint as the content to match against the regex pattern, with a `fallback_behavior` setting that determines how the rule handles requests for which the fingerprint is unavailable. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Determines how AWS WAF should treat requests that lack a JA4 TLS fingerprint, evaluating them as either a match (MATCH) or a non-match (NO_MATCH). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="json_body" type="object">
                  Specifies the request's JSON body as the component to inspect, with options for match pattern and scope that determine which part of the JSON is evaluated against the regular expression. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="invalid_fallback_behavior" type="string">
                      When the request body is not valid JSON, this field determines whether the regex pattern is considered to match (MATCH) or not match (NO_MATCH) for the rule's evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_pattern" type="object">
                      Defines which JSON paths in the request body are examined by the regex pattern, by specifying either all JSON content (all = true) or a list of JSON Pointer paths (included_paths). Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When set to true, applies the regex pattern to the entire JSON body, matching all content. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_paths" type="list(string)">
                          Specifies the JSON paths (e.g., /foo/bar) within the request body that must be present for the regular expression pattern to be evaluated against the JSON body's content at those paths in a WAFv2 rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines the portion of the JSON body that the regex statement inspects for matches, accepting values ALL (inspect the entire body), KEY (inspect JSON keys), or VALUE (inspect JSON values). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles the request body when the JSON body being inspected for a regex match exceeds the maximum size limit, with valid values of CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="method" type="any">
                  Specifies that the regex match rule inspects the HTTP method of the web request (e.g., GET, POST). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="any">
                  Specifies that the regex pattern should be matched against the query string of the web request, which is the portion of the URL after the '?' delimiter. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  Configures the field to match as a single HTTP header, requiring the `name` of that header (for example, `Authorization`) to be specified. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header that the regular expression pattern is evaluated against, such as `User-Agent` or `Referer`. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="single_query_argument" type="object">
                  Identifies a single query string parameter by name (provided via its nested 'Name' property) as the part of the web request that the regex pattern is matched against. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the name of a single query parameter (e.g., 'username') whose value the regex pattern set is matched against. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_fragment" type="object">
                  Specifies that the regex pattern is evaluated against the URI fragment, which is the part of the request URI that follows the '#' separator, if present. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how the regex match statement handles requests that lack a URI fragment, with 'MATCH' meaning the rule matches when the fragment is absent and 'NO_MATCH' meaning it does not. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="any">
                  Specifies that the regex pattern is evaluated against the URI path of the web request, the path component that identifies the resource (e.g., /images/daily-ad.jpg). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pre_parse_text_transformations" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  Defines the order in which the text transformations are applied to the web request before the regex pattern is matched, with lower values meaning the transformation is applied earlier (and each priority must be unique within the list). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The type of text transformation (such as LOWERCASE, URL_DECODE, or NONE) applied to the web request content before the regex pattern matching is performed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="regex_string" type="string">
              The regular expression (regex) pattern that the statement uses to match against the inspected web request content, such as the body, headers, or query string. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="text_transformations" type="list(object)">
              Specifies the text transformations to apply to the web request component before the regular expression is evaluated in the regex match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  The numeric priority that determines the order in which AWS WAF applies this text transformation relative to others in the same rule, with lower numbers processed first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the text transformation type (e.g., LOWERCASE, HTML_ENTITY_DECODE, NONE) applied to the inspected component before regex pattern matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="regex_pattern_set_reference_statement" type="object">
          Defines a rule statement that references a regex pattern set to match the web request, allowing the rule to evaluate requests against the patterns defined in that set. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="arn" type="string">
              The ARN of the AWS WAFv2 regex pattern set that this statement references for matching against web requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="field_to_match" type="object">
              Defines the part of the web request (such as a header, query string, URI, or body) that the regex pattern set reference statement inspects for matches. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all_query_arguments" type="any">
                  Instructs AWS WAF to inspect all query string parameters (query arguments) of the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="body" type="object">
                  Inspects the request body of the web request, meaning the part of the request that follows the headers, against the regex pattern set. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles a request when the body to inspect exceeds the size limit, with allowed values such as CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cookies" type="object">
                  Specifies the cookies in the web request to inspect for the regex pattern set reference, including the cookie match pattern, match scope, and oversize handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Specifies which cookies to inspect in a web request, such as all cookies, a list of included cookies, or a list of excluded cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When true, the cookie match pattern matches all cookies in the web request, used by the regex pattern set reference statement's field-to-match configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_cookies" type="list(string)">
                          The `excluded_cookies` list specifies the cookie names to omit from inspection when AWS WAF evaluates cookies against the regex pattern set in this rule group's statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_cookies" type="list(string)">
                          Lists the cookie names that AWS WAF includes in the cookie match pattern, so only those named cookies are inspected by this rule statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether the regex pattern set reference statement inspects all cookies in the request (ALL) or only cookies whose names start with a specified prefix (KEY_PREFIX) as configured in the cookie match pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles oversized cookie headers in this field-to-match, with valid values CONTINUE, MATCH, or NO_MATCH, determining whether an oversized cookie is treated as a match or no match for the rule's inspection. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="header_order" type="object">
                  Configures inspection of the order of HTTP headers in the request, so the regex pattern set is evaluated against the sequence of header names rather than their values or other request components. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF treats the request when the order of headers exceeds the maximum size limit, accepting values CONTINUE, MATCH, or NO_MATCH, for the header order field match inside the regex pattern set reference statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="headers" type="object">
                  Configures how AWS WAF inspects the request's headers for the regex pattern set, defining which headers to match (e.g., all or a named set), the match scope (like all keys or values), and the oversize handling behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      This object defines the pattern for selecting which request headers the regex pattern set inspects, using either all headers, an explicit list of included header names, or an explicit list of excluded header names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Set to true to make the rule match all request headers for the header match pattern, instead of specifying included or excluded headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_headers" type="list(string)">
                          Specifies the list of header names to exclude from the match pattern, meaning the rule inspects all request headers except those listed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_headers" type="list(string)">
                          Specifies the exact header names to inspect, so only requests containing at least one of these header names are evaluated by the regex pattern set reference statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether the regex pattern set is applied to the header name (KEY), the header value (VALUE), or both (ALL) when matching against request headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Controls how the rule handles requests when the HTTP headers to be inspected exceed the 8 KB limit, with valid values CONTINUE to evaluate normally, MATCH to treat as a match, or NO_MATCH to treat as no match, applicable when the regex pattern set reference statement inspects the headers field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  Specifies the JA3 fingerprint of the client's TLS connection to use as the match target, including the required fallback behavior for requests without a JA3 fingerprint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies whether a request without a JA3 fingerprint (such as non-TLS traffic) should be treated as matching (MATCH) or not matching (NO_MATCH) the regex pattern set in this rule statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  Configures the JA4 TLS fingerprint as the web request component to inspect in the regex pattern set reference rule, including how to handle requests where the JA4 fingerprint is unavailable. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Controls whether a request that does not contain a JA4 fingerprint is treated as a match (MATCH) or no match (NO_MATCH) for the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="json_body" type="object">
                  Configures the rule to inspect the JSON body of the incoming web request, allowing the regex pattern set to be evaluated against the JSON payload with configurable match scope and pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="invalid_fallback_behavior" type="string">
                      Determines whether a request is treated as a match or non-match when the JSON body is invalid, with allowed values MATCH and NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_pattern" type="object">
                      Defines the pattern for selecting which parts of a JSON request body are inspected, either all content, a set of included JSON paths, or one exact path. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Specifies whether the regex pattern set reference statement analyzes the entire JSON body of the request (all) or only specified JSON paths (included_paths). Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_paths" type="list(string)">
                          Specifies the JSONPath expressions that define which parts of the request body to inspect against the regex pattern, so that only values at those included paths are evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether AWS WAF inspects the entire JSON body (ALL), only the keys (KEY), or only the values (VALUE) when evaluating the regex pattern set reference statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles a request body that exceeds the JSON body inspection size limit, with valid values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="method" type="any">
                  Matches the HTTP method (e.g., GET, POST) of the web request, used to define the inspection scope within a WAFv2 regex pattern set reference statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="any">
                  Configures the rule to inspect the query string portion of the web request URL when matching against the regex pattern set. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  Determines that the rule matches web requests by inspecting a single HTTP header, with the header's name provided in the nested `name` attribute. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header to inspect, such as User-Agent or Referer, for the regex pattern set reference statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="single_query_argument" type="object">
                  Configures the rule to inspect only the value of a single named query string parameter (specified in the accompanying 'Name' subfield), rather than the entire query string or all query arguments. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the name of the query argument that AWS WAF inspects for the regex pattern set reference statement (e.g., the 'foo' in `?foo=bar`). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_fragment" type="object">
                  Specifies that AWS WAF should inspect the URI fragment (the part of the request URI after the '#') when evaluating the regex pattern set reference statement in this rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Controls whether a request without a URI fragment is treated as a match (MATCH) or non-match (NO_MATCH) when inspecting the URI fragment for the regex pattern set reference. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="any">
                  Specifies that the regex pattern set should be matched against the URI path of the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pre_parse_text_transformations" type="list(object)">
              This list of text transformations is applied to the web request component before the regex pattern set reference statement's main text transformations, so the regex pattern set is evaluated against the pre-parsed, normalized content. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  Specifies the ascending priority order for applying this text transformation relative to other pre-parse text transformations in the list, where lower numbers are applied first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the transformation type (e.g., NONE, COMPRESS_WHITE_SPACE, HTML_ENTITY_DECODE, LOWERCASE, URL_DECODE) applied to the request component before regex pattern matching occurs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="text_transformations" type="list(object)">
              Defines an ordered list of text transformations (each with a priority and type, such as lowercase, HTML entity decode, or URL decode) that are applied to the inspected web request component before AWS WAF checks it against the referenced regex pattern set. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  Defines the priority of this text transformation within the rule, determining the order in which transformations are executed (lower numbers run first). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the type of text transformation to apply to the web request before evaluating it against the referenced regex pattern set, such as LOWERCASE or URL_DECODE. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="size_constraint_statement" type="object">
          A statement that checks the size of a specified request component (like a header, query string, or body) against a comparison operator and a size in bytes. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="comparison_operator" type="string">
              Specifies how the size of the inspected request component is compared to the Size value, using operators such as EQ, NE, LE, LT, GE, or GT. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="field_to_match" type="object">
              Specifies the part of the web request that AWS WAF inspects, such as a header, query string, body, or URI path, against the size constraint. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all_query_arguments" type="any">
                  When set, the size constraint statement inspects the entire query string (all query arguments combined) of the web request as a single match target. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="body" type="object">
                  Specifies that the size constraint statement inspects the body of the incoming web request, with an optional OversizeHandling property to control how oversized bodies are treated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles request body sizes that exceed the service's size limit for the body field match, with allowed values CONTINUE, MATCH, and NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cookies" type="object">
                  Specifies which cookies in the incoming web request AWS WAF inspects for the size constraint, defining the cookie name pattern to match, whether to evaluate all cookies, the cookie keys, or values, and the oversize handling behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Specifies which cookies to inspect in the request for the size constraint rule, choosing between matching all cookies, only the listed included cookies, or all cookies except the listed excluded cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Indicates that the size constraint statement should match all cookies in the web request, rather than a specified subset of cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_cookies" type="list(string)">
                          Specifies the cookie names to exclude from the size constraint check, meaning that all other cookies in the request are inspected for the size limit, while these listed cookies are ignored. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_cookies" type="list(string)">
                          Specifies the list of cookie names that are included in the size constraint check, so only these cookies are inspected for their size and all other cookies are ignored. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines whether AWS WAF inspects the size of all cookies (ALL), only the cookie names (KEY), or only the cookie values (VALUE) in the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles oversized cookie names or values when inspecting cookies for a size constraint, with possible values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="header_order" type="object">
                  Specifies the configuration for matching the order of HTTP headers in the request, including the oversize handling behavior when the header order exceeds AWS WAF's limits. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF should handle requests when the header order (the ordered list of header names) exceeds the maximum size allowed, with possible values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="headers" type="object">
                  Specifies that AWS WAF should inspect the request's headers, including the match pattern (all, included, or excluded), match scope (all, key, or value), and oversize handling behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Specifies the pattern for selecting which HTTP headers to inspect, allowing you to match all headers, a specific list of included headers, or a list of excluded headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When specified, this field configures the match pattern to match all request headers, so the size constraint applies to every header individually. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_headers" type="list(string)">
                          Specifies the names of headers to exclude from the size constraint inspection, so that the rule does not evaluate those headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_headers" type="list(string)">
                          Specifies the list of header names to include when the size constraint statement inspects HTTP headers, so that only the sizes of these headers are checked against the rule's size limits. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines whether the size constraint applies to the entire HTTP header, only the header key, or only the header value when the field to match is a request header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      For a size constraint statement inspecting request headers, this field determines how AWS WAFv2 handles a header that exceeds the maximum inspectable size, with values CONTINUE to inspect the header as-is, MATCH to treat the rule as matching, or NO_MATCH to treat it as not matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  This field makes the size constraint statement evaluate against the JA3 fingerprint of the client's TLS connection, using the fallback behavior defined in the object to handle requests without a fingerprint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how AWS WAF handles requests without a JA3 fingerprint, such as non-TLS traffic, by treating them as either matching or not matching the size constraint condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  Specifies that the size constraint statement should inspect the JA4 TLS fingerprint of the client request, including a fallback behavior for requests without one. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Determines whether the size constraint rule should match or not match a request when the client's JA4 TLS fingerprint is unavailable, based on values like MATCH or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="json_body" type="object">
                  Configures the size constraint statement to evaluate the request body parsed as JSON, checking the size of the entire JSON structure or a specific JSON property based on the nested match pattern and scope settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="invalid_fallback_behavior" type="string">
                      Specifies how AWS WAF should handle requests whose body is not valid JSON for the size constraint statement, either treating it as a match (MATCH) or not a match (NO_MATCH). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_pattern" type="object">
                      Specifies the match pattern for the JSON body inspection, either matching all JSON content or a set of included JSON pointer paths, which determines the portion of the request body examined for the size constraint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When set to an empty block (&#123;&#125;), this indicates that the size constraint statement should match all JSON fields in the request body. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_paths" type="list(string)">
                          The list of JSON paths (for example, /foo/bar) within the request body that must match for a size constraint rule to apply, when the match pattern is set to include only these paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether the size constraint inspects the entire JSON body, all keys, or all values, using the values ALL, KEY, or VALUE respectively. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles the JSON request body when it is larger than the maximum size AWS WAF can inspect, with valid values CONTINUE, MATCH, or NO_MATCH that determine whether the size constraint statement matches, does not match, or continues without matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="method" type="any">
                  Specifies that the size constraint statement applies to the HTTP method of the web request (e.g., GET, POST). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="any">
                  When set, indicates that the size constraint statement inspects the query string (the URL portion after the '?') of the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  Specifies the name of a single HTTP request header that AWS WAF inspects for the size constraint, so the size rule evaluates the length of that header's value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header (e.g., 'User-Agent', 'Referer') that the size constraint statement inspects when field_to_match is set to single_header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="single_query_argument" type="object">
                  This object selects a single HTTP query argument as the part of the request to inspect for the size constraint, and the argument name is specified in its nested `name` field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the name of the single query argument (for example, 'foo' in '?foo=bar') whose value the size constraint statement inspects for length. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_fragment" type="object">
                  Specifies that the size constraint statement matches against the URI fragment (the portion of the request URL after the '#' symbol) of the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Determines how AWS WAF treats requests lacking a URI fragment for size constraint evaluation, using values NO_MATCH or MATCH to indicate whether the condition should be considered a match. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="any">
                  Specifies that the size constraint applies to the URI path of the web request, which is the path component of the request URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pre_parse_text_transformations" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  The evaluation order for this pre-parse text transformation, where lower numbers are processed first among the transformations in the same rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  The type of preprocessing text transformation to apply to the web request before evaluating the size constraint, such as NONE, COMPRESS_WHITE_SPACE, HTML_ENTITY_DECODE, LOWERCASE, CMD_LINE, or URL_DECODE. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="size" type="number">
              The size (in bytes) to compare with the request component's size for a size constraint rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="text_transformations" type="list(object)">
              A list of text transformations (for example, lowercase, URL-decode, or compress whitespace) that AWS WAF applies to the inspected content before evaluating the size constraint. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  Within a size constraint statement's text transformations, the priority determines the execution order of each transformation, with lower values processed first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the type of text transformation to apply to the inspected value (e.g., NONE, LOWERCASE, URL_DECODE, HTML_ENTITY_DECODE, COMPRESS_WHITE_SPACE) before the size constraint rule evaluates the request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sqli_match_statement" type="object">
          Defines the SQL injection match statement for the rule, specifying the request component to inspect (such as body or query string) and the text transformations to apply when identifying SQL injection attacks. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field_to_match" type="object">
              Specifies the part of the web request (such as a header, query string, body, or URI) in which to search for SQL injection patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all_query_arguments" type="any">
                  When set, this field indicates that AWS WAF will inspect every query argument in the request's query string for the SQL injection match criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="body" type="object">
                  Specifies that the rule inspects the body of the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles the request body when it exceeds the maximum allowed size, with valid values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cookies" type="object">
                  Specifies the cookies that the SQL injection match statement inspects, including the cookie match pattern (all cookies or specific names) and the match scope (key, value) within those cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Determines which cookies are inspected for SQL injection patterns by specifying all cookies, or a list of included or excluded cookie names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When set to true, this field instructs the rule to inspect all cookies in the web request rather than a specified subset. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_cookies" type="list(string)">
                          A list of cookie names to exclude from the cookie inspection pattern, meaning the WAF rule inspects all cookies except those listed here for the SQL injection match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_cookies" type="list(string)">
                          Specifies the list of cookie names to inspect for the SQL injection match pattern, so only those cookies are checked and all others are ignored. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines whether the web ACL rule inspects all cookies or only a single, named cookie for SQL injection patterns, with a value of 'ALL' or 'KEY' (and when 'KEY', the cookie name is provided in the associated key property). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Controls AWS WAF's behavior when the cookies in the request exceed the inspection size limit, accepting CONTINUE, MATCH, or NO_MATCH values to determine how the rule handles the oversized cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="header_order" type="object">
                  Configures the rule to inspect the order in which HTTP headers appear in a request, with options for oversize handling and a bias that can require specific headers to appear in a specified order. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      In an AWS WAFv2 rule group, this setting on a header_order field-to-match for an SQLi match statement defines how WAF handles requests when the header inspection size is exceeded, accepting CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="headers" type="object">
                  Specifies the configuration for inspecting all request headers in the SQL injection match statement, defining which header names to include or exclude, whether to inspect header names, values, or both, and how to handle oversized headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Defines which headers the SQL injection match statement inspects, by either matching all headers or a specified list of included or excluded header names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Indicates that the rule matches all request headers when this empty structure is present in the match pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_headers" type="list(string)">
                          Specifies the names of headers to exclude from inspection when the rule's SQL injection match statement inspects a set of request headers, so those headers are ignored for matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_headers" type="list(string)">
                          In an aws_wafv2_rule_group's SQL injection match statement, this list specifies the names of the HTTP headers to inspect when the header match pattern is set to included_headers, restricting inspection to only those headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies which part of the header to inspect for SQL injection patterns: the entire header (ALL), only the header name/key (KEY), or only the header value (VALUE). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles a web request when the request headers exceed the 8 KB or 200-header inspection limit, allowing you to continue inspecting normally or treat the request as matching or not matching the SQL injection rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  Specifies that the rule should inspect the JA3 fingerprint of the client's TLS connection, enabling the rule group to match on the client application or bot traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      The fallback behavior to apply when a client's JA3 fingerprint is not available, with allowed values 'MATCH' and 'NO_MATCH'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  Inspects the JA4 fingerprint of the client's TLS handshake to match against the SQL injection rule's field-to-match criteria, using the provided fallback behavior for requests without a detectable fingerprint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Determines whether the rule matches when the JA4 fingerprint for the request is unavailable, with MATCH causing a match and NO_MATCH preventing a match in that fallback case. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="json_body" type="object">
                  Configures the JSON body of the incoming web request as the match target, allowing selection of matching patterns within the JSON structure and handling of oversized bodies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="invalid_fallback_behavior" type="string">
                      Determines whether an invalid JSON request body is treated as a match (MATCH) or non-match (NO_MATCH) for the SQL injection match rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_pattern" type="object">
                      Specifies which parts of the JSON request body are inspected for SQL injection patterns, either all of it or only the given included or excluded JSON paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Specifies that the entire JSON body is subject to the SQL injection match, rather than only the keys listed in `included_paths`. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_paths" type="list(string)">
                          Specifies a list of JSON paths within the request body that the SQL injection match statement will inspect, limiting evaluation to only the values at those included paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether AWS WAF inspects all JSON content, only the keys, or only the values of the JSON body for the SQL injection match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Specifies how AWS WAF handles a request whose JSON body exceeds the maximum size allowed for inspection, with allowed values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="method" type="any">
                  Specifies that the AWS WAFv2 rule should inspect the HTTP method (such as GET or POST) of the web request for SQL injection match patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="any">
                  Inspects the entire query string of the web request for SQL injection patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  Specifies that the SQL injection match statement should inspect a single request header, with the header's name provided in the nested Name field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header whose value is inspected for SQL injection patterns, for example 'User-Agent' or 'Authorization'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="single_query_argument" type="object">
                  When set, this object directs AWS WAF to inspect only a named query string argument (provided in its 'Name' child field) in the web request for SQL injection patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the query argument to inspect for SQL injection attacks. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_fragment" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how AWS WAF handles the request when the URI fragment is missing or cannot be inspected, with a value of either MATCH or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="any">
                  This setting configures AWS WAF to inspect the URI path component of the incoming HTTP request for SQL injection patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pre_parse_text_transformations" type="list(object)">
              Specifies a list of text transformations to be applied to the request body before it is parsed, used by the SQL injection match statement to normalize or decode content (e.g., decompress or base64 decode) prior to pattern matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  Specifies the order in which the pre-parse text transformations are applied for the SQL injection match statement, with lower numbers executed first and each priority needing to be unique within the list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the text transformation type (e.g., NONE, LOWERCASE, URL_DECODE, CMD_LINE, HTML_ENTITY_DECODE) applied to the request component before SQL injection parsing and matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="sensitivity_level" type="string">
              Sets the sensitivity level (LOW or HIGH) for the SQL injection match statement, determining how aggressively the rule evaluates request content for SQL injection patterns, with HIGH providing broader detection but potentially more false positives. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="text_transformations" type="list(object)">
              Defines the ordered list of text transformations (e.g., lowercase, HTML entity decode, compress white space) to apply to the inspected web request component before the SQL injection match evaluation, helping to normalize input and evade bypass attempts. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  This field sets the priority order of this text transformation relative to other transformations in the same rule, with lower values applied first. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the text transformation type (e.g., LOWERCASE, URL_DECODE, HTML_ENTITY_DECODE) applied to the inspected component before the SQL injection match statement evaluates it. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="xss_match_statement" type="object">
          Defines the cross-site scripting (XSS) match criteria for the rule, specifying which part of the web request to inspect and how to transform it before checking for malicious scripts. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field_to_match" type="object">
              Specifies the part of the web request (such as headers, query string, body, or URI path) that AWS WAF inspects for cross-site scripting patterns in this rule statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="all_query_arguments" type="any">
                  When this block is present, AWS WAF inspects all query arguments in the request for cross-site scripting (XSS) attack patterns, matching the AllQueryArguments field-to-match option. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="body" type="object">
                  Defines the request body as the inspected component for the XSS match statement, including optional configuration for how oversized request bodies are handled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles a request body that exceeds the size limit when evaluating the XSS match statement's body inspection, with valid values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="cookies" type="object">
                  Specifies the configuration for inspecting all cookies in the web request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Determines which cookies in the request are inspected by the XSS match statement, with options to match all cookies, only listed cookies, or all cookies except listed ones. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          Setting this option in the cookie match pattern causes AWS WAF to match all cookies in the web request, rather than limiting matches to a specified list of included or excluded cookies. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_cookies" type="list(string)">
                          A list of cookie names to exclude from the cookie inspection pattern, so that the XSS match statement inspects all cookies except those listed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_cookies" type="list(string)">
                          Specifies the names of cookies to include in the cookie match pattern, so only these cookies are inspected. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies which portion of the cookies to inspect in the XSS match statement -- either all cookies, only cookie keys, or only cookie values -- using the values ALL, KEY, or VALUE. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles a request when the cookies to inspect exceed the configured size limit, accepting values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="header_order" type="object">
                  Specifies that the XSS match rule inspects the order of the request's HTTP header names and values, with the nested oversize_handling setting controlling how to handle requests whose headers exceed size limits. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="oversize_handling" type="string">
                      Controls how AWS WAF handles the inspection when the request's header order list exceeds the allowable oversize limit, with valid values CONTINUE to continue, MATCH to treat the request as matching, and NO_MATCH to treat it as not matching. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="headers" type="object">
                  Specifies the configuration for inspecting the request's HTTP headers as the field to match in the XSS match statement, including which headers to inspect (via match pattern), which part of each header to evaluate (match scope), and how to handle oversized header blocks. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="match_pattern" type="object">
                      Defines which HTTP headers the WAF rule inspects for XSS attacks, either all headers or only a specific set of included or excluded header names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          When true, matches all headers in the web request for the XSS match statement's header field-to-match, rather than only the included or excluded header names. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="excluded_headers" type="list(string)">
                          Specifies the HTTP header names to exclude from XSS inspection when the rule's match statement evaluates request headers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_headers" type="list(string)">
                          Specifies the names of the headers to include for inspection in the XSS match statement's header match pattern. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Specifies whether to match the entire header, only the header name (KEY), or only the header value (VALUE) when inspecting a header in the XSS match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles oversized header values during inspection for the XSS match statement, with valid values CONTINUE, MATCH, or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja3_fingerprint" type="object">
                  This object configures matching of the JA3 fingerprint (TLS client identifier) as the field to inspect in an XSS match statement, with the Availability setting (CONSUMER or PRODUCER) determining which side's client fingerprint is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Controls whether a request that does not have a JA3 fingerprint (for example, non-TLS traffic) is treated as matching the rule or not when the rule uses JA3 fingerprint inspection in an XSS match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ja4_fingerprint" type="object">
                  Specifies that WAF should match the request based on the JA4 fingerprint extracted from the TLS client hello, using a configured fallback behavior for requests without a JA4 fingerprint. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Specifies how AWS WAF behaves when it cannot obtain the JA4 fingerprint from the client's TLS handshake, determining whether the rule matches (match) or doesn't match (no_match) such requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="json_body" type="object">
                  Specifies the JSON body of the request as the field to inspect within the XSS match statement, defining the match scope, match pattern, and handling of invalid or oversized JSON payloads. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="invalid_fallback_behavior" type="string">
                      Determines how AWS WAF handles a request whose JSON body is invalid or cannot be parsed, with values MATCH or NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="match_pattern" type="object">
                      Specifies which parts of the JSON payload (all content or a set of JSON paths) to inspect for cross-site scripting patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="all" type="any">
                          A boolean that when set to true causes the WAFv2 JSON body match pattern to match all JSON keys/values in the request body, instead of only specific included paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="included_paths" type="list(string)">
                          Specifies the JSON paths within the request body that the XSS match statement inspects, where only the fields at those paths are evaluated for cross-site scripting patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="match_scope" type="string">
                      Determines whether the XSS match rule inspects the entire JSON body (ALL) or the value associated with a specified key (KEY) when analyzing the request body as JSON in an AWS WAFv2 rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="oversize_handling" type="string">
                      Determines how AWS WAF handles a request when the JSON body exceeds the maximum inspectable size, with allowed values CONTINUE, MATCH, and NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="method" type="any">
                  Specifies that the XSS match statement should inspect the HTTP request method (e.g., GET or POST), represented as an empty object within the FieldToMatch configuration, so the rule evaluates the request method value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="query_string" type="any">
                  When set to true, this field instructs AWS WAF to inspect the query string portion of the web request (the part after the '?' in the URL) as the target for the XSS match rule statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="single_header" type="object">
                  Inspects a single named HTTP header in the web request for cross-site scripting (XSS) patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the HTTP header (for example, User-Agent) that the XSS match statement inspects for cross-site scripting attack patterns within its single-header field-to-match configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="single_query_argument" type="object">
                  Specifies that the XSS match statement should inspect only the value of a single named query argument in the web request's query string. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the query argument (query string parameter) that the XSS match statement inspects when field_to_match is set to single_query_argument. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_fragment" type="object">
                  Specifies that the inspection should examine the URI fragment component of the web request, which is the part after the '#' character, as the target for the XSS match statement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="fallback_behavior" type="string">
                      Determines whether the rule treats the request as a match or no-match when the request has no URI fragment, with allowed values MATCH and NO_MATCH. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="uri_path" type="any">
                  When set, the XSS match statement inspects only the URI path of the request, excluding query string and other components. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="pre_parse_text_transformations" type="list(object)">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  Determines the execution order of text transformations applied before XSS pattern matching in the AWS WAFv2 rule group, where lower numbers indicate higher priority and transformations are applied in ascending order. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the type of text transformation (e.g., NONE, URL_DECODE, LOWERCASE, HTML_ENTITY_DECODE, COMPRESS_WHITE_SPACE) to apply to the request content in the pre-parse phase before XSS match evaluation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="text_transformations" type="list(object)">
              A list of text transformations, each with a priority and type, to apply sequentially to the web request component (e.g., body or query string) before inspecting it for cross-site scripting (XSS) patterns, allowing normalization of malicious payloads. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="priority" type="number">
                  The priority order of the text transformation, with lower numbers evaluated first, controlling the sequence in which transformations are applied to the request. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the text transformation type to apply to the web request component before inspecting it for XSS, such as URL_DECODE, HTML_ENTITY_DECODE, LOWERCASE, or COMPRESS_WHITE_SPACE. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="visibility_config" type="object">
      Configures CloudWatch metrics and sampled request logging for the rule, enabling or disabling metric emission and sampled request collection under a specified metric name. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cloud_watch_metrics_enabled" type="bool">
          When set to true, this enables Amazon CloudWatch metrics for the individual rule within the WAFv2 rule group, allowing you to monitor the rule's request traffic and evaluations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metric_name" type="string">
          The name of the CloudWatch metric that AWS WAF automatically creates and updates for the rule, used to monitor rule activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sampled_requests_enabled" type="bool">
          Indicates whether AWS WAF captures and stores a sample of web requests that match the rule within the rule group for inspection and analysis, with a limit of 5,000 sampled requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scope" type="string" required>
  Use CLOUDFRONT for CloudFront RuleGroup, use REGIONAL for Application Load Balancer and API Gateway. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies a list of tag objects (each containing a Key and Value) to attach to this AWS WAFv2 rule group for cost allocation and resource management. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key that uniquely identifies a tag assigned to this AWS WAFv2 rule group, used for organizing and managing the rule group resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair attached to the AWS WAFv2 rule group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="visibility_config" type="object" required>
  Visibility Metric of the RuleGroup. Required.
  <Expandable title="properties">
    <ResponseField name="cloud_watch_metrics_enabled" type="bool" required>
      Indicates whether AWS WAF publishes CloudWatch metrics for the web requests processed by this rule group, allowing you to monitor rule group activity in CloudWatch. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="metric_name" type="string" required>
      The friendly name of the CloudWatch metric that AWS WAF publishes for the rule group (alphanumeric and underscores only), which must be unique within the AWS account for the Region. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sampled_requests_enabled" type="bool" required>
      When true, AWS WAF V2 retains a sample of the web requests that match the rules in this rule group, making them available for inspection via the console or API; when false, no sampled requests are stored. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  ARN of the WAF entity. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="available_labels" type="list(object)">
  Collection of Available Labels. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of a label that is available for use in the rule group, representing a label that its rules can generate or match against incoming web requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="consumed_labels" type="list(object)">
  Collection of Consumed Labels. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of a label that the rule group consumes, meaning a label the group's rules reference in `LabelMatchStatement` to match against request labels, typically formatted as `namespace:key` (e.g., `awswaf:managed:aws:bot-control:signal:bot`). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Id of the RuleGroup Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="label_namespace" type="string">
  Name of the Label. Computed by AWS after `ship`.
</ResponseField>
