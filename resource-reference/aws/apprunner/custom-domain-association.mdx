---
title: "aws_apprunner_custom_domain_association"
description: "Real, generated bindings for `aws_apprunner_custom_domain_association`."
---

`aws_apprunner_custom_domain_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom domain association"})

    		ubx.Resource(apprunner.CustomDomainAssociation, "ci-runner", apprunner.CustomDomainAssociationConfig{
    			DomainName:         "example-domain-name",
    			ServiceArn:         "arn:aws:iam::123456789012:policy/ci",
    			EnableWwwSubdomain: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomDomainAssociation } from "jsr:@ubx/sdk-aws/aws/apprunner/custom_domain_association";

    export default stack("payments", () => {
      intent({ summary: "payments own custom domain association" });

      resource(CustomDomainAssociation, "ci-runner", {
        domainName: "example-domain-name",
        serviceArn: "arn:aws:iam::123456789012:policy/ci",
        enableWwwSubdomain: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apprunner import CustomDomainAssociation, CustomDomainAssociationConfig

    def describe():
        ubx.intent("payments own custom domain association")

        ubx.resource(CustomDomainAssociation, "ci-runner", CustomDomainAssociationConfig(
            domain_name="example-domain-name",
            service_arn="arn:aws:iam::123456789012:policy/ci",
            enable_www_subdomain=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apprunner_custom_domain_association, with domain name
    example-domain-name, service arn
    arn:aws:iam::123456789012:policy/ci, enable www subdomain true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_www_subdomain" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="certificate_validation_records" type="set(object)">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="status" type="string">
    </ResponseField>
    <ResponseField name="type" type="string">
    </ResponseField>
    <ResponseField name="value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dns_target" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
