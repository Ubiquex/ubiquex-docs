---
title: "aws_apprunner_vpc_connector"
description: "Real, generated bindings for `aws_apprunner_vpc_connector`."
---

`aws_apprunner_vpc_connector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/apprunner"

    cfg := apprunner.VpcConnectorConfig{
        SecurityGroups: []string{"example"},
        Subnets: []string{"example"},
        VpcConnectorName: "example-vpc-connector-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(apprunner.VpcConnector, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VpcConnector } from
      "jsr:@ubx/sdk-aws/aws/apprunner/vpc_connector";

    resource(VpcConnector, "example", {
      securityGroups: ["example"],
      subnets: ["example"],
      vpcConnectorName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.apprunner import VpcConnector, VpcConnectorConfig

    cfg = VpcConnectorConfig(
        security_groups=["example"],
        subnets=["example"],
        vpc_connector_name="example",
        tags={"key": "value"},
    )
    ubx.resource(VpcConnector, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_apprunner_vpc_connector with security_groups,
subnets, vpc_connector_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnets" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_connector_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_connector_revision" type="number">
  Computed by AWS after `ship`.
</ResponseField>
