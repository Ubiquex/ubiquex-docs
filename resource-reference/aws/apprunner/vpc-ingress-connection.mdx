---
title: "aws_apprunner_vpc_ingress_connection"
description: "Real, generated bindings for `aws_apprunner_vpc_ingress_connection`."
---

`aws_apprunner_vpc_ingress_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc ingress connection"})

    		ubx.Resource(apprunner.VpcIngressConnection, "ci-runner", apprunner.VpcIngressConnectionConfig{
    			Name:       "ci-runner",
    			ServiceArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcIngressConnection } from "jsr:@ubx/sdk-aws/aws/apprunner/vpc_ingress_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc ingress connection" });

      resource(VpcIngressConnection, "ci-runner", {
        name: "ci-runner",
        serviceArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apprunner import VpcIngressConnection, VpcIngressConnectionConfig

    def describe():
        ubx.intent("payments own vpc ingress connection")

        ubx.resource(VpcIngressConnection, "ci-runner", VpcIngressConnectionConfig(
            name="ci-runner",
            service_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apprunner_vpc_ingress_connection called "ci-runner",
    with service arn arn:aws:iam::123456789012:policy/ci, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
