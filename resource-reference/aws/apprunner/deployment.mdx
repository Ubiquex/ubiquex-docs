---
title: "aws_apprunner_deployment"
description: "Real, generated bindings for `aws_apprunner_deployment`."
---

`aws_apprunner_deployment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apprunner"

    cfg := apprunner.DeploymentConfig{
        ServiceArn: "example",
    }
    ubx.Resource(apprunner.Deployment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Deployment } from
      "jsr:@ubx/sdk-aws/aws/apprunner/deployment";

    resource(Deployment, "example", {
      serviceArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.apprunner import Deployment, DeploymentConfig

    cfg = DeploymentConfig(
        service_arn="example",
    )
    ubx.resource(Deployment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_apprunner_deployment with service_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="operation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
