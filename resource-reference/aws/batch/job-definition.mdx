---
title: "aws_batch_job_definition"
description: "Real, generated bindings for `aws_batch_job_definition`."
---

`aws_batch_job_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own job definition"})

    		ubx.Resource(batch.JobDefinition, "ci-runner", batch.JobDefinitionConfig{
    			Name:                    "ci-runner",
    			Type:                    "example",
    			ContainerProperties:     "example",
    			DeregisterOnNewRevision: true,
    			EcsProperties:           "example",
    			NodeProperties:          "example",
    			Parameters:              map[string]string{"managed-by": "ubx"},
    			PlatformCapabilities:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobDefinition } from "jsr:@ubx/sdk-aws/aws/batch/job_definition";

    export default stack("payments", () => {
      intent({ summary: "payments own job definition" });

      resource(JobDefinition, "ci-runner", {
        name: "ci-runner",
        type: "example",
        containerProperties: "example",
        deregisterOnNewRevision: true,
        ecsProperties: "example",
        nodeProperties: "example",
        parameters: { "managed-by": "ubx" },
        platformCapabilities: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.batch import JobDefinition, JobDefinitionConfig

    def describe():
        ubx.intent("payments own job definition")

        ubx.resource(JobDefinition, "ci-runner", JobDefinitionConfig(
            name="ci-runner",
            type="example",
            container_properties="example",
            deregister_on_new_revision=True,
            ecs_properties="example",
            node_properties="example",
            parameters={"managed-by": "ubx"},
            platform_capabilities=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_batch_job_definition called "ci-runner", with type
    example, container properties example, deregister on new revision
    true, ecs properties example, node properties example, parameters
    a set value, platform capabilities a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="container_properties" type="string">
  Optional.
</ResponseField>

<ResponseField name="deregister_on_new_revision" type="bool">
  Optional.
</ResponseField>

<ResponseField name="ecs_properties" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="node_properties" type="string">
  Optional.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="platform_capabilities" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="propagate_tags" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scheduling_priority" type="number">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn_prefix" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
