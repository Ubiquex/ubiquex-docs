---
title: "aws_batch_job_definition"
description: "`aws_batch_job_definition` is the real template AWS Batch launches a job from, the container image, vCPU and memory requirements, command..."
---

`aws_batch_job_definition` is the real template AWS Batch launches a job from, the container image, vCPU and memory requirements, command, and retry strategy, submitted against an `aws_batch_job_queue` each time a job actually runs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own job definition"})

    		ubx.Resource(batch.JobDefinition, "example", batch.JobDefinitionConfig{
    			Type:                         "example",
    			ConsumableResourceProperties: map[string]any{"consumable_resource_list": []map[string]any{{"consumable_resource": "example"}}},
    			ContainerProperties:          map[string]any{"image": "example"},
    			EcsProperties:                map[string]any{"task_properties": []map[string]any{{"containers": []map[string]any{{"name": "example-name"}}}}},
    			EksProperties:                map[string]any{"pod_properties": map[string]any{"containers": []map[string]any{{"name": "example-name"}}}},
    			JobDefinitionName:            "example-job-definition-name",
    			NodeProperties:               map[string]any{"main_node": 1},
    			Parameters:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { JobDefinition } from "@ubx/sdk-aws/aws/batch/job_definition";

    export default stack("example", () => {
      intent({ summary: "example own job definition" });

      resource(JobDefinition, "example", {
        type: "example",
        consumableResourceProperties: {
          consumableResourceList: [{ consumableResource: "example" }],
        },
        containerProperties: { image: "example" },
        ecsProperties: {
          taskProperties: [{ containers: [{ name: "example-name" }] }],
        },
        eksProperties: {
          podProperties: { containers: [{ name: "example-name" }] },
        },
        jobDefinitionName: "example-job-definition-name",
        nodeProperties: { mainNode: 1 },
        parameters: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.batch import JobDefinition, JobDefinitionConfig

    def describe():
        ubx.intent("example own job definition")

        ubx.resource(JobDefinition, "example", JobDefinitionConfig(
            type="example",
            consumable_resource_properties={"consumable_resource_list": [{"consumable_resource": "example"}]},
            container_properties={"image": "example"},
            ecs_properties={"task_properties": [{"containers": [{"name": "example-name"}]}]},
            eks_properties={"pod_properties": {"containers": [{"name": "example-name"}]}},
            job_definition_name="example-job-definition-name",
            node_properties={"main_node": 1},
            parameters="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_batch_job_definition, with type example, consumable
    resource properties a set value, container properties a set
    value, ecs properties a set value, eks properties a set value,
    job definition name example-job-definition-name, node properties
    a set value, parameters example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="consumable_resource_properties" type="object">
  Defines a consumable resource, including its name and the quantity the job requires from a shared pool, so that concurrent access to the resource can be limited across jobs in the same job queue. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="consumable_resource_list" type="list(object)" required>
      The list of consumable resources that the job definition specifies, with each item identifying a resource name and the amount to be acquired from the job queue's pool of that resource before the job can run. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="consumable_resource" type="string">
          The name of the consumable resource that the job must acquire from a shared resource pool before it can run, used to enforce limits on concurrent consumption of that resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="quantity" type="number">
          The number of units of the consumable resource named in the list item that the job requires to be allocated for its execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="container_properties" type="object">
  Defines the container configuration for the job, including the Docker image, vCPU and memory requirements, command, environment variables, and other runtime settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="command" type="list(string)">
      The command to pass to the container, as an array of strings, which overrides the default command specified in the container image. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_execute_command" type="bool">
      Enables the Amazon ECS Exec feature for this container, allowing you to run interactive commands inside the running container of the Batch job. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="environment" type="list(object)">
      Defines the environment variables to pass to the container, where each object specifies a variable name and its value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          In the AWS Batch job definition's container properties, this is the environment variable name for each entry in the environment list, which is paired with a value to set inside the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value assigned to the environment variable key in the container's environment for the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ephemeral_storage" type="object">
      Specifies the size in GiB of the ephemeral storage available to the container, used for temporary data during the job. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="size_in_gi_b" type="number" required>
          Defines the size in GiB of the ephemeral storage volume mounted for use by the container in the AWS Batch job definition. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="execution_role_arn" type="string">
      The ARN of the IAM role that Amazon ECS uses to manage the container, including pulling container images from Amazon ECR and sending logs to Amazon CloudWatch. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="fargate_platform_configuration" type="object">
      Specifies the platform version to use for the Fargate container, such as 'LATEST' or a specific version like '1.4.0'. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="platform_version" type="string">
          Specifies the version of the AWS Fargate platform (e.g., LATEST or a specific version like 1.4.0) used to run the job's containers when the job definition uses Fargate resources. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="image" type="string" required>
      The Docker image used to launch the container for the Batch job, specified as a string (e.g., a public image like 'amazonlinux:2' or a private Amazon ECR image URI). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="job_role_arn" type="string">
      The Amazon Resource Name (ARN) of the IAM role that the container in the Batch job will assume to make AWS API calls. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="linux_parameters" type="object">
      Defines Linux-specific runtime parameters for the container in the job definition, such as shared memory size, tmpfs mounts, device mappings, and swap settings. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="devices" type="list(object)">
          Specifies a list of host devices to expose to the container in the job's container properties. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="container_path" type="string">
              Specifies the path inside the container where the host device is mounted for use by the container process. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="host_path" type="string">
              Specifies the absolute path of the device on the host instance that is exposed to the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="permissions" type="list(string)">
              Specifies the access permissions (such as READ, WRITE, or MKNOD) granted on a host device mounted into the container for an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="init_process_enabled" type="bool">
          Specifies whether to run an init process inside the container that reaps zombie processes and forwards signals to the main process, analogous to Docker's --init flag. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="max_swap" type="number">
          The maximum amount of swap memory (in MiB) the container can use, which is only supported for jobs that run on Fargate resources. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="shared_memory_size" type="number">
          Specifies the size (in MiB) of the /dev/shm volume for the container's shared memory, letting you increase the default shared memory available to the container. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="swappiness" type="number">
          Specifies the swappiness value (0 -- 100) for the container's Linux memory-swapping behavior, where 0 disables swapping and 100 maximizes swap aggressiveness, as part of the LinuxParameters of the AWS Batch job definition's container properties. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tmpfs" type="list(object)">
          Specifies the tmpfs mounts (container path, size in MiB, and mount options) to mount into the container's filesystem for the job's container properties. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="container_path" type="string">
              The absolute file path inside the container at which the tmpfs volume is mounted. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="mount_options" type="list(string)">
              Specifies the mount options (such as read-only, noexec, or size) that will be passed to the tmpfs mount when creating the temporary file system in the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="size" type="number">
              Specifies the size (in MiB) of the tmpfs volume mounted at the container path defined by the corresponding tmpfs entry. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log_configuration" type="object">
      Specifies the logging configuration for the container, including the log driver (such as awslogs for CloudWatch Logs), driver-specific options, and any secret options for sensitive values. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="log_driver" type="string" required>
          The logging driver used by the container to send logs, such as 'awslogs' for CloudWatch Logs, 'fluentd', or 'splunk', controlling where container output is delivered. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="options" type="any">
          A map of key-value pairs that configure the selected log driver for the container, such as setting the awslogs group or region when using the awslogs driver. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="secret_options" type="list(object)">
          This is a list of secret objects that specify sensitive data from AWS Secrets Manager or Systems Manager Parameter Store to be passed to the container's log driver configuration for the AWS Batch job definition's container log configuration. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              Specifies the name of the secret that is passed to the container's log configuration, where the corresponding secret value is retrieved from AWS Secrets Manager or Systems Manager via the sibling `valueFrom` field. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value_from" type="string">
              The ARN of the secret (for example, an AWS Secrets Manager secret) from which AWS Batch retrieves the value to pass to the log driver under the key specified by the `name` field in the same `secret_options` object. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="memory" type="number">
      Specifies the amount of memory (in MiB) to reserve for the container running in the AWS Batch job. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mount_points" type="list(object)">
      Defines the list of volumes to mount into the container, specifying the source volume name, the container path where it is mounted, and whether the mount is read-only. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="container_path" type="string">
          The absolute path inside the container where the selected source volume is mounted, for example /mnt/data. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="read_only" type="bool">
          Indicates whether the container should use the mounted volume in read-only mode, preventing writes from the container to the volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="source_volume" type="string">
          The name of the volume to mount from the host, which must match a volume name defined in the container properties' volumes attribute. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_configuration" type="object">
      Specifies whether the job's containers receive a public IP address by setting the AssignPublicIp property (ENABLED or DISABLED) in the Batch container's network configuration. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="assign_public_ip" type="string">
          Indicates whether the job's container network interface should receive a public IP address when running in a VPC, set to 'ENABLED' or 'DISABLED' (default). Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="privileged" type="bool">
      Indicates whether the container runs in privileged mode, granting it access to all host devices and elevated permissions similar to Docker's --privileged flag. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="readonly_root_filesystem" type="bool">
      When true, the container's root file system is mounted as read-only, preventing write operations to the root filesystem. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repository_credentials" type="object">
      Configures authentication for pulling a private container image by specifying the Secrets Manager secret (via its credentialsParameter property) that contains the registry credentials. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="credentials_parameter" type="string" required>
          Specifies the ARN or name of an AWS Secrets Manager secret that holds the credentials used to authenticate to a private container image repository. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resource_requirements" type="list(object)">
      Specifies a list of resource requirements for the job container, such as the amount of vCPUs, memory, or GPUs, where each item defines a type (e.g., VCPU, MEMORY, GPU) and its corresponding value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="type" type="string">
          The type of the resource requirement, which must be either MEMORY, VCPU, or GPU (if supported), indicating whether the paired value specifies the container's memory limit, number of vCPUs, or number of GPUs. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The quantity of the computing resource specified by the accompanying 'type' property (e.g., number of vCPUs or memory in MiB) for this container resource requirement in the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="runtime_platform" type="object">
      Specifies the operating system family and CPU architecture (e.g., LINUX, X86_64/ARM64) for the container, determining the runtime platform on which the job runs. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cpu_architecture" type="string">
          Specifies the CPU architecture for the container, such as X86_64 or ARM64, to select the appropriate runtime platform for the job definition's container properties. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="operating_system_family" type="string">
          Specifies the operating system family (e.g., LINUX or WINDOWS_SERVER_2019_CORE) for the container running on a Fargate runtime platform in an AWS Batch job definition. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="secrets" type="list(object)">
      Specifies the list of secrets to expose to the container, where each secret defines a name and a valueFrom that references an AWS Secrets Manager secret or an SSM Parameter Store value. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The name of the environment variable that will be injected into the container, with its value sourced from the AWS secret or SSM parameter specified in the corresponding valueFrom property. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value_from" type="string">
          Specifies the ARN of the AWS Secrets Manager secret that provides the secret value to the container environment variable defined by the corresponding Name in this secrets entry. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ulimits" type="list(object)">
      Specifies a list of ulimits to set for containers in the job, each defining a resource limit name along with its hard and soft limits for the container process. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="hard_limit" type="number">
          The hard limit value for the ulimit entry, specifying the maximum resource limit (e.g., number of open files) that the container can reach for the associated ulimit name. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the ulimit, such as `cpu` or `nofile`, that identifies the resource limit to set for the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="soft_limit" type="number">
          The soft limit for the resource specified by the ulimit name, which is the value enforced by the kernel and can be raised by a process up to the hard limit. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user" type="string">
      The user parameter specifies the user name or numeric UID to use for running the container's main process, overriding the default user specified in the container image. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="vcpus" type="number">
      Specifies the number of vCPUs to reserve for the container, which AWS Batch uses to allocate compute resources for EC2-based job definitions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="volumes" type="list(object)">
      A list of volume definitions for the container, each specifying a volume name and its host path or EFS configuration to be mounted into the container, analogous to Docker bind mounts or volumes. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="efs_volume_configuration" type="object">
          Configures the Amazon EFS file system to mount as a volume in the Batch container, including the file system ID, root directory, and encryption settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="authorization_config" type="object">
              Defines the authorization configuration for the EFS volume, including the EFS access point ID and whether to enable IAM authorization for the mount. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="access_point_id" type="string">
                  The ID of the Amazon EFS access point to use for this volume, providing an application-specific entry point into the EFS file system. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="iam" type="string">
                  Specifies whether to enable IAM authorization for the EFS volume, with allowed values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="file_system_id" type="string">
              The ID of the Amazon EFS file system to be mounted as a volume in the container, which identifies the file system used for the EFS mount point. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="root_directory" type="string">
              Sets the directory within the Amazon EFS file system that AWS Batch mounts as the root directory for the volume inside the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transit_encryption" type="string">
              Specifies whether to enable Amazon EFS transit encryption for data in transit between the container and the file system, with valid values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transit_encryption_port" type="number">
              Specifies the TCP port used for encrypted (TLS) connections to the Amazon EFS file system when transit encryption is enabled, allowing you to override the default port of 2992. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="host" type="object">
          Defines the host-side source path on the EC2 instance for the volume to be mounted into the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="source_path" type="string">
              The host path on the Amazon ECS container instance (or EC2 host) that is mounted into the container as a volume for the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the volume, used to reference it from the container's mountPoints. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_files_volume_configuration" type="object">
          This object configures an S3 files volume for the container, specifying the S3 bucket name, object key prefix, and mount point so the container can access files from S3 as a volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="access_point_arn" type="string">
              The ARN of the S3 access point that defines which S3 bucket and prefix is mounted as a volume in the container for this job definition's S3 files volume configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="file_system_arn" type="string">
              The Amazon Resource Name (ARN) of the S3 file system that is mounted as a volume in the container of the AWS Batch job definition, specified within the S3 files volume configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="root_directory" type="string">
              Sets the subdirectory within the specified S3 bucket that is mounted as the root of the volume in the container, controlling which portion of the bucket is exposed to the job's container. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transit_encryption_port" type="number">
              The port number used for TLS transit encryption when communicating with the S3 endpoint for this mounted S3 volume, typically 443 when transit encryption is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ecs_properties" type="object">
  Configuration block for the ECS-specific properties of the job definition, holding a list of task properties that define the container images, commands, volumes, and other ECS scheduler settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="task_properties" type="list(object)" required>
      Specifies a list of ECS task properties, each defining the container definitions, volumes, and other task-level settings used by AWS Batch to run a multi-container job. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="containers" type="list(object)">
          Defines the list of containers that run in the ECS task for this AWS Batch job definition, where each container specifies its Docker image, command, environment variables, and resource allocation (CPU/memory) within the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="command" type="list(string)">
              Specifies the command to pass to the container, which overrides the default command provided in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="depends_on" type="list(object)">
              Specifies dependencies of this container on other containers in the same task, using a list of objects that each contain a container name and a condition (such as START or COMPLETE) which determines when this container may start. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="condition" type="string">
                  Specifies the state (e.g., START, COMPLETED, SUCCESS, or HEALTHY) that the dependency container must reach before the current container can start. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="container_name" type="string">
                  The name of the container within the same task definition that the current container depends on, as used to define container dependencies in the dependsOn list for an Amazon ECS task in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="environment" type="list(object)">
              Specifies a list of environment variables to pass to the container, where each object contains 'name' and 'value' keys. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of an environment variable to pass to the container in the ECS task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  Specifies the value for an environment variable passed to a container in the AWS Batch job definition's ECS task properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="essential" type="bool">
              Indicates whether the container is essential to the task; if an essential container exits or fails, the entire task stops. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="firelens_configuration" type="object">
              Specifies the FireLens configuration for the container's log routing, including the log router type (fluentd or fluentbit), its options, and optional S3 configuration for delivering logs. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="options" type="any">
                  A map of key-value options that configure the FireLens log router, such as enabling the log router's own log forwarding or setting custom configuration keys for the chosen Fluentd or Fluent Bit log driver. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string">
                  Specifies the Firelens log router type for the container, which must be either 'fluentd' or 'fluentbit', in the ECS task properties of the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image" type="string">
              The Docker image used to launch the container for this task in the Batch job's ECS task definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="linux_parameters" type="object">
              `linux_parameters` specifies Linux-specific container runtime options such as kernel capabilities to add or drop, whether to run an init process, shared memory size, tmpfs mount configurations, and swap memory settings for each container in the Batch ECS task definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="devices" type="list(object)">
                  Specifies the host devices to expose to the container inside the ECS task, where each device includes a host path, optional container path, and permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_path" type="string">
                      The path inside the container where the device is mounted, making the host device accessible at that container path. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_path" type="string">
                      For each device specified in the container's Linux parameters, this field gives the absolute path of the device on the host that is passed through to the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="permissions" type="list(string)">
                      For each device in the linuxParameters.devices list, this specifies the permitted device access modes for the container, with valid values of read, write, and mknod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="init_process_enabled" type="bool">
                  When set to true, the container runs an init process as PID 1, enabling it to reap zombie processes and forward signals to the main process. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_swap" type="number">
                  The maximum amount of swap memory (in MiB) that the container can use; a value of 0 disables swap, and if omitted, the container runtime's default is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="shared_memory_size" type="number">
                  The size (in MiB) of the /dev/shm shared memory volume for the container, used to adjust the container's shared memory allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="swappiness" type="number">
                  The swappiness value (0-100, default 60) controls the kernel's swapping behavior for the container in the Linux parameters of each container in the ECS task properties of the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tmpfs" type="list(object)">
                  Defines tmpfs mounts for the container, each specified by container path, size (in MiB), and optional mount options, used to provide volatile in-memory storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_path" type="string">
                      The absolute path inside the container where the tmpfs volume is mounted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_options" type="list(string)">
                      Specifies the mount options for the tmpfs volumes in the container, such as 'nosuid', 'nodev', 'noexec', which control permissions and behavior on the temporary file system. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="size" type="number">
                      The size of the tmpfs mount for the container, specified in MiB. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="log_configuration" type="object">
              Specifies the logging configuration for the container, including the log driver (e.g., awslogs, splunk, json-file) and its options, as well as any secret options for the log driver. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="log_driver" type="string">
                  Specifies the log driver to use for the container, such as awslogs or splunk, which determines how container logs are delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="options" type="any">
                  The options map specifies the key-value configuration parameters passed to the container's log driver, such as awslogs-group, awslogs-region, and awslogs-stream-prefix for the awslogs driver. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="secret_options" type="list(object)">
                  Specifies the secret options that inject sensitive data, such as credentials, from AWS Secrets Manager or Systems Manager Parameter Store into the container's log driver configuration for the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the log driver option in the container's log configuration that is populated with the secret value referenced by the `valueFrom` field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="string">
                      The ARN or full Systems Manager parameter name of the secret (from AWS Secrets Manager or AWS Systems Manager Parameter Store) that provides the value for the corresponding secret option (named in the sibling `name` field) used by the container's log driver configuration in the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="mount_points" type="list(object)">
              Mount points for the container, specifying the volumes to mount into the container, including source volume, container path, and read-only flag, for each container defined in the ECS task properties of the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="container_path" type="string">
                  The absolute path inside the container where the named volume is mounted, as defined in the mount point configuration for a task container in the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only" type="bool">
                  Whether the container has read-only access to the volume mounted at this mount point. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="source_volume" type="string">
                  The name of the volume to mount, which must reference a volume defined in the job definition's `volumes` section. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Specifies the name of the container within the ECS task definition, used to identify the container and required to be unique among all containers in the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="privileged" type="bool">
              Specifies whether the container runs in privileged mode, granting it extended capabilities and access to host devices, analogous to Docker's --privileged flag. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="readonly_root_filesystem" type="bool">
              When true, the container's root filesystem is mounted as read-only, preventing the container from writing to any part of the root filesystem while still allowing writes to mounted volumes. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="repository_credentials" type="object">
              Specifies the private registry authentication credentials for the container, referenced by the ARN of a Secrets Manager secret or Systems Manager parameter that stores the credentials, used when pulling a private container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="credentials_parameter" type="string">
                  Specifies the ARN of an AWS Secrets Manager secret that contains the credentials for a private container registry, allowing the task to pull the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resource_requirements" type="list(object)">
              Specifies the type and amount of a resource (e.g., GPU or memory) to reserve for the container, where each item in the list contains a Type and Value pair. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="type" type="string">
                  Specifies the type of resource being requested for the container (e.g., GPU, MEMORY, or VCPU) within the resource requirements list of an AWS Batch job definition's ECS container properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The value of the resource requirement, such as the number of GPUs or the memory limit in MiB, expressed as a string. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="secrets" type="list(object)">
              Specifies a list of secrets to expose to the container, where each item references an AWS Secrets Manager secret or SSM parameter via its valueFrom ARN, allowing sensitive data to be injected as environment variables or mounted as files. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the environment variable that will be set in the container to hold the secret value from the referenced secret. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value_from" type="string">
                  The ARN or full name of the secret in AWS Secrets Manager or Systems Manager Parameter Store from which the container retrieves the secret value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="start_timeout" type="number">
              The timeout in seconds allowed for the container in the ECS task to start before the job is considered failed. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stop_timeout" type="number">
              Specifies the amount of time (in seconds) to wait for the container to stop before it is forcibly killed, with a minimum of 60 seconds and a maximum of 1800 seconds. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="ulimits" type="list(object)">
              Specifies the ulimit settings for each container in the ECS task defined in the job definition, allowing you to set resource constraints such as the maximum number of open files (nofile) or processes (nproc) with corresponding soft and hard limits. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="hard_limit" type="number">
                  The hard limit is the maximum value for the container resource limit, beyond which the system prevents any further increase, even for privileged processes. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the ulimit setting, identifying the system resource being limited for the container, such as core, cpu, data, nofile, or nproc. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="soft_limit" type="number">
                  The soft limit (lower bound) for the ulimit resource limit configured on the container, paired with the hard limit and ulimit name in the container's ulimits array. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="user" type="string">
              Specifies the user name or numeric UID (and optionally group) used to run the container's main process inside the ECS task, such as 'user' or 'uid:gid'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enable_execute_command" type="bool">
          Specifies whether to enable the Amazon ECS Exec feature, which allows you to run interactive commands directly inside the containers of the task for debugging and troubleshooting. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ephemeral_storage" type="object">
          Specifies the ephemeral storage configuration for the ECS task, allowing you to set the amount of temporary storage in GiB that will be available to the containers running in the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="size_in_gi_b" type="number">
              The amount of ephemeral storage, in GiB, to attach to each task's containers; valid values are between 21 and 200 GiB. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="execution_role_arn" type="string">
          The ARN of the IAM execution role for the ECS task, which grants the container agent permissions to pull container images from Amazon ECR and access other AWS services like Secrets Manager and CloudWatch Logs for the Batch job's containers. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ipc_mode" type="string">
          Sets the IPC mode for the containers in the task, controlling the inter-process communication namespace (e.g., 'host', 'task', or 'none'). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_configuration" type="object">
          Specifies the network configuration for the task, such as whether to assign a public IP and which security groups to use. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="assign_public_ip" type="string">
              Determines whether the task's elastic network interface is assigned a public IP address when using the awsvpc network mode, with allowed values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_mode" type="string">
          Specifies the network mode for the task's containers (e.g., 'awsvpc', 'host', 'bridge', or 'none'), controlling whether the task receives its own elastic network interface or shares the host's networking. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pid_mode" type="string">
          Sets the PID namespace mode for containers in the task, allowing either 'host' for sharing the host's PID namespace or 'task' for a dedicated PID namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="platform_version" type="string">
          Specifies the version of the ECS platform on which the task runs, such as LATEST or a specific version like 1.4.0, which affects the runtime environment and capabilities available to the container within the AWS Batch job. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="runtime_platform" type="object">
          This object specifies the operating system family and CPU architecture (e.g., LINUX, X86_64) for the container, matching the ECS task definition's RuntimePlatform parameter. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cpu_architecture" type="string">
              Specifies the CPU architecture (such as X86_64 or ARM64) for the AWS Batch job definition's ECS task runtime platform, determining whether the container runs on an Intel/AMD or ARM-based compute environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="operating_system_family" type="string">
              Specifies the operating system family (such as LINUX) for the container runtime, which must match the underlying ECS compute environment's OS. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="task_role_arn" type="string">
          Specifies the Amazon Resource Name (ARN) of the IAM task role that the ECS task in this job definition uses, allowing its containers to make AWS API calls. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="volumes" type="list(object)">
          Defines the list of data volumes for the task, where each volume has a name and either a host path or an empty-directory configuration, and can be mounted into containers via the container's mountPoints. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="efs_volume_configuration" type="object">
              Configures an Amazon EFS file system for use as a volume in a container, specifying the file system ID, root directory, transit encryption, and optional IAM authorization settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="authorization_config" type="object">
                  Specifies the Amazon EFS authorization configuration for the volume, including the EFS access point ID and whether IAM authorization is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="access_point_id" type="string">
                      The ID of the Amazon EFS access point to use when mounting this volume in the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="iam" type="string">
                      Specifies whether Amazon EFS uses IAM authorization for the file system access, with allowed values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="file_system_id" type="string">
                  The ID of the Amazon EFS file system (e.g., fs-12345678) to mount as a volume in the ECS task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="root_directory" type="string">
                  Specifies the path on the EFS file system to mount as the root directory for the task's containers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="transit_encryption" type="string">
                  Specifies whether to enable Amazon EFS transit encryption (TLS) for the mounted file system, with valid values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="transit_encryption_port" type="number">
                  Specifies the port that Amazon EFS uses for transit encryption, which is typically 2049. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="host" type="object">
              Specifies the host filesystem path on the EC2 instance that is mounted into the container for a bind-mounted volume in the AWS Batch job definition's ECS task properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="source_path" type="string">
                  Specifies the absolute path on the container instance (host) that is mounted into the container as a volume for the Batch job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              Specifies the name of the volume, which uniquely identifies it within the task properties and is referenced by the container definitions' mountPoints sourceVolume property. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="s3_files_volume_configuration" type="object">
              Configures an Amazon S3 bucket as a volume to mount into the container for an AWS Batch ECS task volume, specifying details such as the S3 bucket name, path, and mount options. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="access_point_arn" type="string">
                  The ARN of the S3 Access Point that the container uses to mount and access the S3 bucket for this volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="file_system_arn" type="string">
                  Specifies the ARN of the S3 bucket that is mounted as a volume for the container's task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="root_directory" type="string">
                  The absolute path inside the container where the S3 bucket is mounted for this volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="transit_encryption_port" type="number">
                  Specifies the network port used for encrypted transit communication with S3 (e.g., 443) when transit encryption is enabled for the S3 file volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="eks_properties" type="object">
  Specifies the Amazon EKS pod properties for this job definition, including the pod template, container specifications, volumes, and other Kubernetes-specific settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="pod_properties" type="object">
      Specifies the pod properties for an Amazon EKS-based job, including container definitions, volumes, and pod-level metadata such as service account name and labels. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="containers" type="list(object)">
          The list of containers that run in the EKS pod defined by these pod properties. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="args" type="list(string)">
              Specifies the arguments to pass to the container's entrypoint, overriding the default arguments in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="command" type="list(string)">
              Specifies the command to run in the container, which overrides the default command defined in the container image (equivalent to the Docker CMD). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="env" type="list(object)">
              List of environment variables to pass to the EKS container, each specifying a name and value for the container's environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the environment variable to set for the container in the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The value for the environment variable named in the corresponding `name` field within the same `env` list, applied to the container's pod on the EKS cluster when this AWS Batch job definition runs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image" type="string">
              The container image used to launch the EKS pod's container for the AWS Batch job. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="image_pull_policy" type="string">
              Specifies the Kubernetes image pull policy for the container, such as Always, IfNotPresent, or Never, which controls when the container image is pulled from the registry. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Specifies the name of the container within the Kubernetes pod, which must be unique within the pod and match the container name as defined in the pod spec. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resources" type="object">
              Specifies the Kubernetes compute resource requirements for the container, such as CPU and memory, defining optional limits and requests as key-value pairs. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="limits" type="any">
                  Specifies the maximum CPU and memory resources (in the form of key-value pairs like cpu and memory) that the EKS container in the Batch job definition is allowed to use. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="requests" type="any">
                  Defines the minimum amount of compute resources (e.g., CPU and memory) requested by the EKS container, specified as a map of Kubernetes resource names to quantity strings. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="security_context" type="object">
              This object specifies the Kubernetes security context for the container, including settings such as runAsUser, runAsGroup, privileged, and readOnlyRootFilesystem, which govern container-level permissions and security constraints within the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_privilege_escalation" type="bool">
                  Indicates whether a container is allowed to escalate privileges, matching the Kubernetes securityContext.allowPrivilegeEscalation setting for the containers in the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="privileged" type="bool">
                  When true, the container runs in privileged mode, granting it extended capabilities and access to host devices as defined by Kubernetes security context. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only_root_filesystem" type="bool">
                  When set to true, the container's root filesystem is mounted as read-only, preventing writes to the root filesystem and allowing writes only to mounted volumes. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="run_as_group" type="number">
                  The group ID (GID) that the container's main process runs as, overriding the default group from the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="run_as_non_root" type="bool">
                  Indicates whether the container must run as a non-root user, as defined in the Kubernetes security context for the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="run_as_user" type="number">
                  Sets the numeric user ID (UID) used to run the container's main process, overriding any image default. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="volume_mounts" type="list(object)">
              Specifies the volume mounts for each container in the EKS pod, mapping pod-defined volumes to mount paths inside the container's filesystem. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="mount_path" type="string">
                  Mount path inside the container where the referenced volume is mounted, as defined for each EKS pod container's volume mount in the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the volume to mount, which must correspond to a volume defined in the pod's volumes list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only" type="bool">
                  Specifies whether the volume mount is mounted as read-only inside the EKS container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sub_path" type="string">
                  The path within the referenced volume to mount instead of the volume's root, allowing a single volume to be shared by multiple mounts at different directories. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dns_policy" type="string">
          Sets the Kubernetes DNS policy for the EKS pod in the job, controlling how DNS resolution is handled (e.g., Default, ClusterFirst, ClusterFirstWithHostNet, or None). Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="host_network" type="bool">
          Indicates whether the pod should share the host's network namespace, allowing it to use the host network directly. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image_pull_secrets" type="list(object)">
          Specifies a list of image pull secrets (each with a name) used to authenticate with private container registries when pulling the container image for the EKS pod in the job definition. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the Kubernetes secret to use as an image pull secret for the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="init_containers" type="list(object)">
          Specifies the list of init containers that run before the main container in the EKS pod for the AWS Batch job definition. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="args" type="list(string)">
              The list of arguments passed to the init container's entrypoint command in the EKS pod specification for the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="command" type="list(string)">
              Specifies the command to execute in an init container of a Batch EKS pod, provided as a list of argument strings that overrides the container image's entrypoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="env" type="list(object)">
              Defines the list of environment variables passed to the init container of the Amazon EKS pod in the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of an environment variable to set in the init container of the EKS pod for this AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  The value of the environment variable for an init container in the EKS pod properties of the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="image" type="string">
              The container image to launch for each init container in the EKS pod of the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="image_pull_policy" type="string">
              The image pull policy for the init container, determining when Kubernetes pulls the container image (Always, IfNotPresent, or Never). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the init container, which is required and must be unique within the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="resources" type="object">
              Defines the CPU and memory resource limits and requests for the init container within the EKS pod properties of the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="limits" type="any">
                  Limits the maximum amount of CPU and memory resources each init container in the EKS pod can consume, using Kubernetes-style resource names and quantities. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="requests" type="any">
                  Specifies the minimum amount of compute resources (such as cpu and memory) that each init container in the EKS pod requires, which Kubernetes uses for scheduling and resource reservation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="security_context" type="object">
              Defines the security context for an init container in the EKS pod, including settings like runAsUser, runAsGroup, Linux capabilities, and read-only root filesystem behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="allow_privilege_escalation" type="bool">
                  For an init container defined in the EKS pod properties of the job definition, this boolean controls whether the container's processes can gain more privileges than its parent process, corresponding to the Kubernetes `allowPrivilegeEscalation` security context setting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="privileged" type="bool">
                  Indicates whether the init container runs in privileged mode within the EKS pod, granting it access to all devices and extended security capabilities as defined by the Kubernetes security context. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only_root_filesystem" type="bool">
                  Indicates whether the root filesystem of the init container is mounted as read-only, preventing any writes to the root filesystem, within the Kubernetes security context for the init container in the EKS pod properties of the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="run_as_group" type="number">
                  Sets the GID (group ID) that the init container's processes will run with, overriding the pod-level runAsGroup if specified. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="run_as_non_root" type="bool">
                  Indicates whether the init container in the EKS pod must run as a non-root user, enforcing that the container's security context prevents root execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="run_as_user" type="number">
                  Sets the numeric user ID (UID) that the init container's main process runs as, overriding the user specified in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="volume_mounts" type="list(object)">
              Defines volume mounts for an init container in an Amazon EKS pod, specifying the volume name and the mount path within the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="mount_path" type="string">
                  The absolute file system path inside the init container where the referenced volume is mounted, corresponding to the Kubernetes volumeMounts mountPath for that container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  In the EKS pod specification of an AWS Batch job definition, this is the name of the Kubernetes volume to mount into the init container, which must match a volume declared in the pod's volumes list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only" type="bool">
                  When true, the volume is mounted read-only inside the init container, preventing any write operations to that volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sub_path" type="string">
                  The subPath field specifies a subdirectory or file within the referenced volume to mount to the container's mountPath, enabling selective mounting of a portion of the volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="metadata" type="object">
          Defines the Kubernetes metadata (labels and annotations) applied to the pod running the EKS job, used to identify or group the pod within the cluster. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="annotations" type="any">
              The annotations are arbitrary key-value pairs attached to the Kubernetes pod, used to store non-identifying metadata that can be consumed by tools or AWS Batch integrations, distinct from labels which are used for pod selection. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="labels" type="any">
              Defines the Kubernetes labels (key-value pairs) to attach to the pod metadata for an Amazon EKS job definition. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="namespace" type="string">
              The Kubernetes namespace in which to run the EKS pod for the job definition. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_account_name" type="string">
          The name of the Kubernetes service account assigned to the pod, which enables IAM roles for service accounts (IRSA) to grant AWS permissions to the pod. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="share_process_namespace" type="bool">
          Enables sharing the same process namespace across all containers in the EKS pod, equivalent to the Kubernetes `shareProcessNamespace` pod setting. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="volumes" type="list(object)">
          Specifies the list of volumes available for mounting into containers in the EKS pod, defining sources like emptyDir or hostPath that correspond to the pod's volumeMounts. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="empty_dir" type="object">
              This object configures an emptyDir volume for an EKS pod in an AWS Batch job definition, creating a temporary, initially empty directory that persists only for the pod's lifetime and can optionally set the medium (e.g., memory) and size limit via nested fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="medium" type="string">
                  Specifies the storage medium for the emptyDir volume in the pod; set to 'Memory' to use a RAM-backed tmpfs volume, or omit it (empty string) to use the default ephemeral disk storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="size_limit" type="string">
                  The maximum storage size for the emptyDir volume in the EKS pod, specified as a Kubernetes quantity string such as '128Mi'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="host_path" type="object">
              Defines a hostPath volume for the EKS pod, which mounts a specified directory from the node's filesystem into the container; the nested `path` property sets the host directory path. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="path" type="string">
                  Specifies the path on the host machine that is mounted into the pod as a hostPath volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the volume, referenced by containers in their volumeMounts to mount this volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="persistent_volume_claim" type="object">
              Specifies a Kubernetes PersistentVolumeClaim (PVC) volume source for the Batch EKS pod, including the claim name and read-only flag to mount the claim into the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="claim_name" type="string">
                  Specifies the name of the Kubernetes PersistentVolumeClaim (PVC) in the pod's namespace that this volume references, which the AWS Batch EKS pod will mount. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only" type="bool">
                  When set to true, mounts the referenced PersistentVolumeClaim as read-only inside the EKS pod for this AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="secret" type="object">
              Configures a Kubernetes secret volume source for the pod, specifying the secret name and optionally the keys (items) from that secret to expose as files. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="optional" type="bool">
                  Specifies whether the referenced Kubernetes secret must exist before the pod starts; when set to true, the secret is optional and its absence does not prevent the pod from starting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="secret_name" type="string">
                  The name of the Kubernetes secret to mount as a volume in the pod, referencing a secret that must exist in the same EKS cluster namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="job_definition_name" type="string">
  Specifies the name of the job definition; if you omit it, AWS Batch generates a unique name for you. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="node_properties" type="object">
  Specifies the properties for a multi-node parallel (MNP) job, including the number of nodes, the main node index, and the container properties for each node range. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="main_node" type="number" required>
      Defines the node index (starting at 0) that is designated as the main node in a multi-node parallel job definition, which runs the coordinating task. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_range_properties" type="list(object)" required>
      Defines the per-node-range configuration for a multi-node parallel AWS Batch job, mapping a range of node indices to the container properties and instance types (and optional ECS configuration) used for those nodes. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="consumable_resource_properties" type="object">
          Defines how much of a named consumable resource (e.g., GPU) a node range consumes in a multi-node AWS Batch job definition, specifying the resource name and quantity. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="consumable_resource_list" type="list(object)">
              Defines the list of consumable resources (each with a name and quantity) that jobs in this node range will reserve and consume from the shared pool, enabling capacity-aware scheduling in multi-node AWS Batch jobs. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="consumable_resource" type="string">
                  Defines the name of a consumable resource that the job's nodes can consume, used for coordinating resource allocation across multi-node jobs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="quantity" type="number">
                  Specifies the required quantity of the named consumable resource for this node range in a multi-node parallel AWS Batch job, which the scheduler uses to limit how many jobs can concurrently consume the same shared resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="container" type="object">
          Defines the container properties (such as image, command, environment, and resource requirements) for the node range in a multi-node parallel AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="command" type="list(string)">
              Specifies the command to run in the container, passed as an array of strings that overrides the container image's default command for this AWS Batch job definition's node range. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_execute_command" type="bool">
              When enabled, allows you to use ECS Exec to start an interactive shell or run commands inside the container of a multi-node AWS Batch job, which is helpful for debugging running jobs. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="environment" type="list(object)">
              Defines the environment variables (as a list of objects with name and value keys) that are passed to the container when jobs run for this node range in a multi-node AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name (key) of an environment variable to pass to the container in this node range of the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  Specifies the string value of an environment variable (paired with its `name`) that is injected into the container running on each node within the node range of an AWS Batch multi-node job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ephemeral_storage" type="object">
              Specifies the ephemeral storage configuration for the container, defining the size in GiB of temporary storage provisioned beyond the default. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="size_in_gi_b" type="number">
                  The size, in GiB, of the ephemeral storage volume that is mounted to the container for scratch data in the multi-node batch job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="execution_role_arn" type="string">
              The Amazon Resource Name (ARN) of the IAM role that AWS Batch assumes so the container can pull images and publish logs, required for Fargate-based job definitions. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="image" type="string">
              Specifies the Docker image used to run the container for the job defined by this node range property. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="instance_type" type="string">
              Specifies the Amazon EC2 instance type to launch for the container that runs on this range of nodes in the multi-node AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="job_role_arn" type="string">
              The ARN of the IAM role that the container in a node range of a multi-node AWS Batch job can assume to make API calls to AWS services. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="linux_parameters" type="object">
              Defines Linux-specific container parameters (such as shared memory size, init process enablement, swap settings, devices, and tmpfs mounts) for the Batch container in this node range of a multi-node job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="devices" type="list(object)">
                  Specifies the list of host devices to expose to the container, including their host path, container path, and permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_path" type="string">
                      The path inside the container where the host device is exposed when mounting a device for a Linux container in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="host_path" type="string">
                      The host path of the device on the container instance, used to map a host device into the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="permissions" type="list(string)">
                      Specifies the device permissions (READ, WRITE, MKNOD) that the container is granted for the corresponding host device when the job's container runs, controlling access to that device. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="init_process_enabled" type="bool">
                  This field, when set to true, enables the init process inside the container, which forwards signals and reaps zombie processes for proper process lifecycle management. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="max_swap" type="number">
                  Specifies the total amount of swap memory (in MiB) that the container can use, with a value of 0 disabling swap memory. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="shared_memory_size" type="number">
                  Sets the size (in MiB) of the container's shared memory (/dev/shm) volume, controlling the amount of shared memory available to the container's Linux processes. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="swappiness" type="number">
                  Sets the swappiness kernel parameter (0-100) that controls how aggressively the container's kernel will swap anonymous memory pages to disk. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="tmpfs" type="list(object)">
                  Defines a list of tmpfs mounts to be mounted in the container, where each item specifies a container path, mount size in MiB, and optional mount options (e.g., 'noexec', 'size=') that control the mount behavior. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="container_path" type="string">
                      Specifies the absolute file path inside the container where the tmpfs volume is mounted, such as /run or /tmp. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="mount_options" type="list(string)">
                      Specifies the list of mount options (e.g., 'noexec', 'nosuid') to apply to the tmpfs volume in the container's Linux parameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="size" type="number">
                      The size (in MiB) of the tmpfs volume mounted at the specified container path for a container in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="log_configuration" type="object">
              Specifies the logging configuration for the container, such as the log driver (e.g., `awslogs` to send logs to Amazon CloudWatch Logs) and its options. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="log_driver" type="string">
                  Specifies the log driver for the container, such as awslogs for Amazon CloudWatch Logs or fluentd, which determines how container logs are captured and delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="options" type="any">
                  Specifies the log driver options (key-value pairs) for the container's log configuration, such as awslogs-group and awslogs-region when using the awslogs log driver. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="secret_options" type="list(object)">
                  Specifies a list of secrets from AWS Secrets Manager or AWS Systems Manager Parameter Store to be injected as environment variables into the container's log driver configuration, with each entry containing a `name` and a `valueFrom` reference. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the secret option, which identifies the key or environment variable name that the log driver will reference, as specified in the container's log configuration secret_options. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value_from" type="string">
                      The ARN of the secret in AWS Secrets Manager or the Systems Manager Parameter Store parameter that supplies the secret value for the log driver option identified by the corresponding name. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="memory" type="number">
              Sets the hard memory limit (in MiB) for the container of jobs in this node range. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="mount_points" type="list(object)">
              Defines the mount points for volumes mounted into the container, specifying the container path, source volume, and whether the mount is read-only. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="container_path" type="string">
                  The absolute path inside the container where the volume is mounted for this mount point entry. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="read_only" type="bool">
                  Specifies whether the container has read-only access to the mount point; if true, the container cannot write to the mounted volume, and if false, it can read and write. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="source_volume" type="string">
                  Specifies the source volume name that must correspond to a volume configured in the container's `volumes` list, defining which storage volume is mounted at the container's mount point path. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="privileged" type="bool">
              Indicates whether the container runs in privileged mode (Docker --privileged), which grants it elevated permissions on the host, such as access to devices and kernel capabilities. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="readonly_root_filesystem" type="bool">
              Specifies whether the container's root filesystem is mounted as read-only, preventing writes to the root filesystem. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="repository_credentials" type="object">
              Provides the credentials required to authenticate with a private container registry, referencing the AWS Secrets Manager secret that holds the repository authentication parameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="credentials_parameter" type="string">
                  Specifies the Amazon Resource Name (ARN) of the AWS Secrets Manager secret that contains the private repository credentials for authenticating to the container registry. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="resource_requirements" type="list(object)">
              Specifies the types and amounts of compute resources (such as vCPU, memory, and GPU) that the container in this node range requires for the job to run. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="type" type="string">
                  Specifies whether the resource requirement for the container in a node range applies to vCPUs (VCPU) or memory (MEMORY). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="string">
                  In the container definition for a node range in a multi-node Batch job, the string value giving the amount of the resource type (such as '2' for vCPUs or '2048' for MiB of memory) required. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="runtime_platform" type="object">
              Specifies the operating system and CPU architecture (e.g., 'LINUX' and 'X86_64') for the container, allowing the job to run on a compatible Amazon ECS or EKS compute environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="cpu_architecture" type="string">
                  Specifies the CPU architecture (x86_64 or arm64) for the container's runtime platform in the AWS Batch job definition's node range properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="operating_system_family" type="string">
                  Specifies the operating system family (e.g., LINUX or WINDOWS_SERVER_2019_CORE) that the container in this node range property should use. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="secrets" type="list(object)">
              Defines a list of sensitive data (secrets) to pass to the container as environment variables, where each entry specifies a secret name and the ARN or full name of the AWS Secrets Manager or Systems Manager Parameter Store resource from which to retrieve the value. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the environment variable that will be set in the container from the referenced secret. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value_from" type="string">
                  Specifies the ARN of an AWS Secrets Manager secret or AWS Systems Manager Parameter Store parameter whose value this container environment variable is populated from for jobs in the node range. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="ulimits" type="list(object)">
              Defines the ulimit resource limits (e.g., the maximum number of open files) applied to each container in a node range, where each entry consists of a name, a hard limit, and a soft limit. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="hard_limit" type="number">
                  The hard limit for the resource specified in the ulimit, representing the maximum value that the process can reach under the Batch container's ulimits configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="name" type="string">
                  Specifies which ulimit (resource limit) to apply to the container, such as 'nofile' or 'nproc', within the node range container ulimits list of the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="soft_limit" type="number">
                  The soft limit of the ulimit setting for the container, which defines the resource ceiling the container process may use before the kernel enforces throttling or restriction. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="user" type="string">
              Specifies the user name or numeric UID that the container process runs as inside the job's container, overriding the default user defined in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="vcpus" type="number">
              Defines the number of vCPUs to reserve for the container that runs on each node in this node range of the multi-node parallel AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="volumes" type="list(object)">
              This is the list of Docker volumes available to the container for jobs in the node range, each specifying a host source path and volume name to be mounted via the container's mount points. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="efs_volume_configuration" type="object">
                  The EFS volume configuration for a container volume, specifying the Amazon EFS file system to mount, including the file system ID, root directory, transit encryption settings, and IAM authorization options. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="authorization_config" type="object">
                      Defines the authorization details for the Amazon EFS volume, including the EFS access point ID and whether to use IAM authorization for accessing the file system. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="access_point_id" type="string">
                          Sets the EFS access point ID (fsap-...) to use for the EFS volume when mounting it in the container, as part of the authorization configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="iam" type="string">
                          Specifies whether to enable IAM authorization for the Amazon EFS volume, with valid values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="file_system_id" type="string">
                      The ID of the Amazon EFS file system that the container mounts as a volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="root_directory" type="string">
                      The path on the EFS file system to mount as the root directory inside the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="transit_encryption" type="string">
                      Specifies whether to enable encryption in transit for the Amazon EFS volume, with valid values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="transit_encryption_port" type="number">
                      Specifies the port to use when connecting to the Amazon EFS file system using TLS for transit encryption. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="host" type="object">
                  Specifies the source path on the host for a volume that is mounted as a bind mount from the container instance filesystem into the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="source_path" type="string">
                      The absolute path on the host (EC2 instance) that the container volume is mounted from, used to give the container access to a specific directory on the underlying compute resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="name" type="string">
                  The name of the volume, used as a key to reference it in the container's mountPoints so that the container can mount the defined storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_files_volume_configuration" type="object">
                  Defines the S3 source and mount settings (bucket, optional object keys/prefix, and container mount point) used to mount an Amazon S3 location as a filesystem volume inside the container for an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="access_point_arn" type="string">
                      The ARN of the S3 access point that the container mounts as an S3 file volume, enabling access to the underlying S3 bucket through that access point. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="file_system_arn" type="string">
                      Optional; if omitted, computed by AWS.
                    </ResponseField>
                    <ResponseField name="root_directory" type="string">
                      The path inside the container where the S3 file volume is mounted, defaulting to the container's root directory ('/'). Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="transit_encryption_port" type="number">
                      Specifies the network port to use for transit encryption when AWS Batch mounts this S3 volume in the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="ecs_properties" type="object">
          Specifies the ECS task properties (such as task definition, container overrides, and resource requirements) that apply to the nodes in this range of the multi-node AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="task_properties" type="list(object)">
              Defines the list of ECS task properties (each containing container definitions, volumes, and network settings) for the AWS Batch job definition's ECS properties within a node range. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="containers" type="list(object)">
                  For each node range in a multi-node AWS Batch job definition, this list defines the containers that run as part of the ECS task specified by the parent task properties, with each entry containing the container image, command, environment variables, and resource requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="command" type="list(string)">
                      Specifies the command (as an array of strings) passed to the container, overriding any default command defined in the container image; if omitted, the image's default command is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="depends_on" type="list(object)">
                      Specifies the container dependencies for a container in the task, where each entry identifies another container that must reach a given condition (such as START, COMPLETE, SUCCESS) before this container can start. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="condition" type="string">
                          The condition that determines when the container can start relative to the dependency container, such as START or COMPLETE. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="container_name" type="string">
                          Specifies the name of a container in the same ECS task that this container depends on, controlling its start or run condition when multiple containers are defined in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="environment" type="list(object)">
                      A list of environment variable objects (name and value) passed to the container when it runs, configured in the ECS task properties of a Batch job definition's node range properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the environment variable in the key-value pair that is set for the container in the ECS task configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          The value part of an environment variable (key-value pair) that is injected into the container running in the ECS task for the specified node range in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="essential" type="bool">
                      Specifies whether this container is essential to the task; if an essential container fails or exits, the entire Batch job task stops and all other containers are terminated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="firelens_configuration" type="object">
                      Configures FireLens log routing for the container, specifying the log router type (fluentd or fluentbit) and its options, to enable custom log routing to destinations. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="options" type="any">
                          A map of key-value pairs that specifies optional configuration for the Firelens log router, such as enabling ECS log metadata (`enable-ecs-log-metadata`) or providing a custom configuration file via `config-file-type` and `config-file-value`. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="type" type="string">
                          Specifies the Firelens log router type to use, either 'fluentd' or 'fluentbit', for the container's log routing. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="image" type="string">
                      Specifies the container image (Docker image URI) that the ECS container in the Batch job task will run. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="linux_parameters" type="object">
                      Specifies Linux-specific container settings such as kernel capabilities, device mappings, shared memory size, tmpfs mounts, and swap configuration for each container in the job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="devices" type="list(object)">
                          Defines a list of host devices to expose inside the container, each specifying the host path, the container path, and optionally the access permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_path" type="string">
                              The path inside the container where the host device is mounted, as part of a device mapping for Linux containers in an AWS Batch job's ECS task properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="host_path" type="string">
                              Specifies the host-side device path (e.g., `/dev/sda`) that the container's LinuxParameters exposes at the corresponding `container_path` for device access. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="permissions" type="list(string)">
                              Permissions granted to the container for the device, such as read, write, or mknod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="init_process_enabled" type="bool">
                          When set to true, this enables the init process (PID 1) to run inside the container, ensuring proper signal handling and zombie process reaping as defined by the ECS container's LinuxParameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="max_swap" type="number">
                          The maximum amount of swap memory (in MiB) the container can use, where 0 disables swap and a positive value must also specify swappiness. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="shared_memory_size" type="number">
                          Specifies the size in MiB of the container's shared memory (/dev/shm), overriding the default limit. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="swappiness" type="number">
                          Sets the swappiness behavior for the container's kernel, a value from 0 to 100 that controls how aggressively the kernel swaps process memory to disk. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="tmpfs" type="list(object)">
                          Specifies the tmpfs mounts for the container, where each entry defines the container path, the size in MiB, and optional mount options (such as ro, rw, or noexec) for the tmpfs filesystem. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="container_path" type="string">
                              Specifies the path inside the container where the tmpfs volume is mounted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="mount_options" type="list(string)">
                              Specifies the mount options (e.g., 'ro', 'nosuid', 'size=') that control the behavior of the tmpfs volume mounted in the Linux container within the ECS task of this AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="size" type="number">
                              Specifies the size of the tmpfs mount in MiB for the container's Linux parameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="log_configuration" type="object">
                      Specifies the log configuration for the container, including the log driver (e.g., awslogs, splunk) and its options, which control how container logs are sent to a logging service. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="log_driver" type="string">
                          Specifies the log driver (e.g., awslogs, fluentd, json-file) that the container should use to send log information to. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="options" type="any">
                          Specifies the key-value pairs for the container's log driver configuration (such as awslogs-group, awslogs-region, and awslogs-stream-prefix) used by the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="secret_options" type="list(object)">
                          Specifies a list of secret options that define sensitive data (such as credentials) to pass to the container's log driver, referencing AWS Secrets Manager or Systems Manager Parameter Store values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="name" type="string">
                              The name of the log driver option (e.g., awslogs-group or a custom key) that receives the secret value from the referenced secret ARN in the log configuration secret options. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="value_from" type="string">
                              The full ARN or name of an AWS Secrets Manager secret or Systems Manager parameter that supplies the secret value for this container log driver option in the AWS Batch job definition's ECS task properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="mount_points" type="list(object)">
                      Defines the container's volume mount points, specifying the source volume name, the path inside the container, and whether the mount is read-only. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="container_path" type="string">
                          The path inside the container where the volume is mounted, as specified for a mount point in the ECS task containers of a Batch job definition's node range properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="read_only" type="bool">
                          Indicates whether the container mount point is read-only, preventing write access to the mounted volume from within the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="source_volume" type="string">
                          The name of the volume in the ECS task definition that is mounted to the container at the specified container path. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of this container within the ECS task definition, used to uniquely identify it among the containers in the task and to reference it from other containers or logging configurations. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="privileged" type="bool">
                      When set to true, the container runs in privileged mode, granting it access to all host devices and extended kernel capabilities beyond standard container isolation, mirroring the Docker privileged flag in ECS task definitions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="readonly_root_filesystem" type="bool">
                      Specifies whether the container's root file system is mounted as read-only, so the container cannot write to its root filesystem. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="repository_credentials" type="object">
                      Specifies the secret that contains the credentials for pulling a container image from a private repository, typically referencing an AWS Secrets Manager or SSM Parameter Store secret. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="credentials_parameter" type="string">
                          The ARN or name of the AWS Secrets Manager secret or Systems Manager parameter that stores the private repository credentials for the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="resource_requirements" type="list(object)">
                      Specifies the resource requirements (e.g., vCPU and memory) for each container in a task, defined as a list of objects each containing a resource type and value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="type" type="string">
                          Specifies the type of additional resource the container requires, such as a GPU or an inference accelerator. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          The value (as a string) specifying the amount of the resource (such as CPU units or MiB of memory) requested for the container, matched to the corresponding resource type in the same requirement. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="secrets" type="list(object)">
                      Specifies the list of secrets to inject into the container as environment variables, referencing AWS Secrets Manager secrets or SSM Parameter Store parameters by their ARNs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the environment variable that will be set in the container to expose the secret value referenced by the corresponding valueFrom field in the ECS task properties of the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value_from" type="string">
                          The full ARN of the secret in AWS Secrets Manager or the parameter in AWS Systems Manager Parameter Store from which the container's secret value is retrieved. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="start_timeout" type="number">
                      Specifies the maximum time, in seconds, AWS Batch will wait from the moment a container is launched until it transitions to a running state, after which the job is considered failed. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="stop_timeout" type="number">
                      The number of seconds to wait for the container to stop before the system forces it to stop, applied when the container is stopped in the ECS task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="ulimits" type="list(object)">
                      Defines the list of resource limits (e.g., nofile, nproc) with soft and hard values applied to each container in the ECS task for the specified node range in a multi-node Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="hard_limit" type="number">
                          The hard limit for the ulimit, specifying the maximum resource value a container process can reach for the named resource (e.g., file size or open files). Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="name" type="string">
                          Specifies the name of the POSIX ulimit (e.g., core, cpu, memlock, nofile, nproc) to set for the container in the ECS task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="soft_limit" type="number">
                          The soft limit for a container's ulimit value, which the process can dynamically raise up to the hard limit. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="user" type="string">
                      The user name or numeric UID to run the container's main process as, overriding the default user specified in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="enable_execute_command" type="bool">
                  Enables ECS Exec (execute command) for the task's containers, allowing you to run interactive commands and debug the job's containers while they are running. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="execution_role_arn" type="string">
                  The ARN of the IAM role that the ECS task in this Batch job node range uses to make AWS API calls (e.g., pulling container images from Amazon ECR). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="ipc_mode" type="string">
                  Specifies the IPC mode of the ECS task container(s) in the Batch job definition, which can be 'host', 'task', or 'none' to control the IPC namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="pid_mode" type="string">
                  Specifies the PID (process ID) namespace mode for the containers in the task, either 'host' to share the host's PID namespace or 'task' to give each task a private namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="task_role_arn" type="string">
                  Specifies the IAM role that the ECS task container(s) can assume for AWS API calls, controlling permissions granted to the containers within the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volumes" type="list(object)">
                  Specifies the data volumes that can be mounted by containers in the ECS task, allowing persistent storage via EBS volumes or bind mounts from the host. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="efs_volume_configuration" type="object">
                      Configures an Amazon EFS volume to be mounted in the container, specifying the filesystem ID, root directory, transit encryption, and optional authorization configuration (access point or IAM). Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="authorization_config" type="object">
                          Defines the EFS authorization configuration, including the access point ID and whether IAM authorization is enabled, for the Amazon EFS volume mounted in the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="access_point_id" type="string">
                              Specifies the ID of the Amazon EFS access point to use when mounting the EFS volume for the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="iam" type="string">
                              Indicates whether IAM authorization is used to control access to the Amazon EFS volume, with allowed values ENABLED or DISABLED. Optional; if omitted, computed by AWS. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="file_system_id" type="string">
                          Specifies the Amazon EFS file system ID that the volume mounts, used in the EFS volume configuration for an ECS task in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="root_directory" type="string">
                          Specifies the path on the EFS file system to mount as the root directory for the volume when it is mounted into the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="transit_encryption" type="string">
                          Indicates whether to enable encryption for data in transit between the container and the Amazon EFS file system, with allowed values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="transit_encryption_port" type="number">
                          Specifies the port to use for EFS transit encryption when connecting to the file system, used in the volume configuration for an AWS Batch job's ECS task properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="host" type="object">
                      Specifies the host volume configuration for a volume in the AWS Batch job definition's ECS task properties, which defines the source directory on the container instance that gets mounted into the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="source_path" type="string">
                          Specifies the host filesystem path on the container instance to be mounted into the container as a volume, used as the source for a bind-mounted host volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name assigned to the volume, which is used to reference it in container mount points within the task definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="s3_files_volume_configuration" type="object">
                      Specifies the configuration for an S3 volume mount, including the S3 bucket, prefix, and optional transfer settings, for a container task in an ECS job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="access_point_arn" type="string">
                          The ARN of the S3 access point through which the container mounts the S3 bucket for this volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="file_system_arn" type="string">
                          The ARN of the Amazon S3 access point that defines the file-system endpoint for the S3 volume mounted by this task's containers. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="root_directory" type="string">
                          Specifies the root directory in the container where the S3 volume is mounted, determining the container filesystem path under which the S3 bucket's objects are made available. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="transit_encryption_port" type="number">
                          Specifies the custom port to use for encrypted data transfer (TLS/HTTPS) when the S3 volume's transit encryption is enabled, with a default of 443. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="eks_properties" type="object">
          The EKS properties for the job definition, specifying the EKS pod template and container properties used when running the job on Amazon EKS. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="pod_properties" type="object">
              Specifies the Kubernetes pod configuration for the EKS job, including its containers, init containers, volumes, host network settings, and pod metadata such as labels and annotations. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="containers" type="list(object)">
                  Defines the container specifications for the EKS pod in this node range, including image, command, arguments, environment variables, and resource limits for each container in the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="args" type="list(string)">
                      Specifies the arguments to pass to the container's entrypoint, overriding the default CMD defined in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="command" type="list(string)">
                      Specifies the entrypoint command for the EKS container, overriding the ENTRYPOINT instruction defined in the container image. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="env" type="list(object)">
                      The list of environment variables to set for the container in the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          The name of the environment variable that AWS Batch sets inside the container for the EKS pod when the job runs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          The value to assign to the environment variable specified by the 'name' field for a container in the EKS pod properties of an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="image" type="string">
                      Specifies the Docker image (with tag) that the EKS pod container runs, as defined in the Batch job definition's EKS pod properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="image_pull_policy" type="string">
                      Controls when the container runtime pulls the container image for the EKS pod, accepting Kubernetes-style values like Always, IfNotPresent, or Never. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name of the container within the EKS pod, which must match the container name defined in the pod specification and is used to identify the container inside the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="resources" type="object">
                      Specifies the CPU and memory resource requests and limits for the container, analogous to Kubernetes container resource requirements. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="limits" type="any">
                          Specifies the maximum amount of compute resources (such as cpu and memory) that the EKS container is allowed to use, expressed as a map of Kubernetes resource names to quantity values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="requests" type="any">
                          Specifies the minimum amount of CPU, memory, and other compute resources requested for the container, used by the Kubernetes scheduler to place the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="security_context" type="object">
                      It specifies the security context settings for the EKS container, such as privileged mode, read-only root filesystem, and the user and group IDs under which the container runs. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="allow_privilege_escalation" type="bool">
                          Whether a container's process is allowed to gain more privileges than its parent process (e.g., via setuid or setgid binaries), corresponding to the Kubernetes security context's allowPrivilegeEscalation setting in the AWS Batch EKS pod properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="privileged" type="bool">
                          When enabled, this flag runs the EKS container in privileged mode, giving it full access to the host node's devices and kernel capabilities. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="read_only_root_filesystem" type="bool">
                          When true, this makes the container's root filesystem read-only for the EKS pod running the Batch job, as defined by the Kubernetes security context. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_group" type="number">
                          The GID (group ID) that the container's main process should run as, matching the Kubernetes security context runAsGroup setting for EKS pods in the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_non_root" type="bool">
                          Specifies whether the container is required to run as a non-root user (with a UID other than 0), preventing it from running with root privileges. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user" type="number">
                          Sets the numeric user ID (UID) that all processes in the container run as, overriding the image's default user for this EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="volume_mounts" type="list(object)">
                      The list of volume mounts that specify which volumes to mount into the container and their mount paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mount_path" type="string">
                          Specifies the path within the container where the volume is mounted, following Kubernetes volumeMount mountPath semantics. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="name" type="string">
                          The name of the Kubernetes volume to mount into the container, which must correspond to a volume defined in the pod's volumes specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="read_only" type="bool">
                          Indicates whether the mounted volume is read-only, so the container can only read from the volume and cannot write to it. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="sub_path" type="string">
                          Specifies the sub-path inside the referenced volume to mount at the container's mount path, allowing a single subdirectory of the volume to be exposed rather than the entire volume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="dns_policy" type="string">
                  Sets the Kubernetes DNS policy for the EKS pod (e.g., 'Default', 'ClusterFirst', or 'None'), determining how DNS queries are resolved for containers in the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="host_network" type="bool">
                  When true, the pod uses the host's network namespace instead of a dedicated pod network. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="image_pull_secrets" type="list(object)">
                  Specifies a list of Kubernetes secrets (each with a `name` key) that AWS Batch uses to authenticate with private image registries when pulling the container image for the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      Specifies the name of the Kubernetes secret referenced in the imagePullSecrets list to provide credentials for pulling container images for the EKS pod in the AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="init_containers" type="list(object)">
                  Specifies a list of init containers that run before the main containers in the EKS pod, allowing pre-work such as fetching secrets or setting up permissions before the primary job container starts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="args" type="list(string)">
                      The list of command-line arguments passed to the entrypoint of each init container, matching the Kubernetes container args field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="command" type="list(string)">
                      The command to execute in the init container of the EKS pod, overriding the image's default entrypoint, specified as a list of strings. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="env" type="list(object)">
                      Defines environment variables for each init container in the EKS pod, where each object specifies a variable name and its value or a value-from source as per Kubernetes pod specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="name" type="string">
                          Specifies the name of an environment variable assigned to an init container within the EKS pod configuration of the Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="value" type="string">
                          Specifies the value assigned to the environment variable (with the key defined in the sibling `name` field) for an init container in the EKS pod properties of the AWS Batch job definition's node range. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="image" type="string">
                      Specifies the container image to use for an init container within the EKS pod properties of the node range properties in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="image_pull_policy" type="string">
                      The image pull policy for the init container in the EKS pod, controlling when Kubernetes pulls the container image, with allowed values such as Always, IfNotPresent, or Never. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The name assigned to an init container within the EKS pod specification, which must be unique among all containers in the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="resources" type="object">
                      Defines the compute resource requirements (e.g., CPU and memory requests/limits) for the init container inside the EKS pod, mirroring Kubernetes container resource specifications. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="limits" type="any">
                          Specifies the maximum amount of compute resources (such as CPU and memory) that an init container in the EKS pod is allowed to use, expressed as a key-value map of resource names to quantities (e.g., cpu: "1", memory: "512Mi"). Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="requests" type="any">
                          Specifies the minimum compute resources (such as cpu and memory) that the init container in an Amazon EKS pod must have scheduled, as part of the Batch job definition's EKS pod properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="security_context" type="object">
                      Defines the Kubernetes security context for the init container, specifying privilege and access control settings such as run-as user/group, Linux capabilities, and privileged mode. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="allow_privilege_escalation" type="bool">
                          Controls whether the init container's processes can gain more privileges than their parent (e.g., via setuid or setgid binaries), with false preventing privilege escalation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="privileged" type="bool">
                          When true, the init container runs in privileged mode with extended Linux capabilities inside the EKS pod used by the AWS Batch job definition, matching the Kubernetes pod security context field of the same name. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="read_only_root_filesystem" type="bool">
                          When set to true, this makes the init container's root filesystem read-only, preventing writes to the container's filesystem layer. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_group" type="number">
                          Specifies the Linux group ID (GID) that the init container's processes run as in the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_non_root" type="bool">
                          Specifies whether the init container must run as a non-root user, corresponding to the Kubernetes Pod security context field runAsNonRoot for EKS pods in an AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="run_as_user" type="number">
                          Specifies the numeric user ID (UID) to run the init container's process as, overriding the image's default user in the Kubernetes security context. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="volume_mounts" type="list(object)">
                      Specifies the volume mounts for the init containers in the EKS pod, defining how volumes are mounted into the container's filesystem at specific mount paths. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="mount_path" type="string">
                          The absolute path inside the init container where the volume is mounted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="name" type="string">
                          The name of the Kubernetes volume to mount into the init container, matching a volume defined in the pod's volumes list. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="read_only" type="bool">
                          In the AWS Batch job definition's EKS pod init containers, this boolean indicates whether the volume mount is mounted as read-only, mirroring the Kubernetes volumeMount readOnly field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="sub_path" type="string">
                          Specifies a sub-path inside the referenced Kubernetes volume to mount into the container, rather than the volume root. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="metadata" type="object">
                  Metadata for the Kubernetes pod, including optional labels and annotations, that are applied to the pod specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="annotations" type="any">
                      Provides the annotations, a set of key-value pairs attached to the Kubernetes pod metadata for EKS-based jobs in the batch job definition, used to attach arbitrary non-identifying metadata to the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="labels" type="any">
                      Specifies the Kubernetes labels to attach to the pod metadata for the EKS job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="namespace" type="string">
                      Specifies the Kubernetes namespace in which the EKS pod for the Batch job will run, allowing you to isolate pod resources within the cluster. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="service_account_name" type="string">
                  Specifies the Kubernetes service account name attached to the EKS pod, which determines the pod's identity for Kubernetes API access and enables IAM roles for service accounts (IRSA) to grant AWS permissions. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="share_process_namespace" type="bool">
                  Indicates whether all containers in the EKS pod share a single process namespace, so they can see and signal each other's processes. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volumes" type="list(object)">
                  Defines the list of Kubernetes-style volumes available to the EKS pod's containers, supporting types such as emptyDir, secret, configMap, and hostPath for providing storage or data to the job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="empty_dir" type="object">
                      Configures an emptyDir volume for a pod in the EKS job definition, creating a temporary directory that is initially empty and persists only for the pod's lifetime, allowing containers in the pod to share data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="medium" type="string">
                          Specifies the storage medium for the emptyDir volume in an Amazon EKS pod; set to 'Memory' to back the volume with tmpfs (RAM) or leave empty for disk-backed storage. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="size_limit" type="string">
                          The maximum storage size for the emptyDir volume in the EKS pod properties, specified as a Kubernetes quantity string (e.g., '1Gi'), which limits the amount of local storage the emptyDir volume can consume. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="host_path" type="object">
                      Defines the hostPath volume source for an EKS pod in an AWS Batch job definition, specifying the host node's directory path and optional type. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="path" type="string">
                          Specifies the host node filesystem path that is mounted into the pod for a hostPath volume in an Amazon EKS pod definition used by the AWS Batch job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="name" type="string">
                      The unique name of the Kubernetes volume within the pod, referenced by container volume mounts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="persistent_volume_claim" type="object">
                      Defines a pod volume backed by a Kubernetes PersistentVolumeClaim, including the PVC name and whether it is mounted read-only. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="claim_name" type="string">
                          The name of the Kubernetes PersistentVolumeClaim that this EKS pod volume references, which must exist in the same namespace as the pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="read_only" type="bool">
                          Indicates whether the referenced persistent volume claim (PVC) is mounted into the EKS pod as read-only, instead of the default read-write access. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="secret" type="object">
                      Configures a Kubernetes secret volume that mounts a named secret into the EKS pod, allowing the pod to access sensitive data such as credentials as files. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="optional" type="bool">
                          Indicates whether the Kubernetes secret referenced by this volume must exist before the pod starts; if true, the mount succeeds even when the secret is missing, and if false, the secret must be present. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="secret_name" type="string">
                          The name of the Kubernetes secret to mount as a volume in the EKS pod. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="instance_types" type="list(string)">
          Specifies the EC2 instance types (or instance families) allowed for the nodes in this node range, overriding the default instance types of the compute environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="target_nodes" type="string">
          Specifies the range of node indices (e.g., '0:5' or '1:') that this node range property applies to within a multi-node AWS Batch job definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="num_nodes" type="number" required>
      Defines the total number of nodes in a multi-node parallel AWS Batch job, determining the required compute resources across the node group. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="parameters" type="any">
  Specifies a map of key-value pairs that define default parameters, which can be referenced within the container's command using the `Ref::` syntax. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="platform_capabilities" type="list(string)">
  Specifies the platform capabilities (EC2, FARGATE, or FARGATE_SPOT) required for the job to run, determining whether the job uses EC2 or Fargate computing resources. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="propagate_tags" type="bool">
  Indicates whether tags from the AWS Batch job definition are propagated to the Amazon ECS task that runs the job. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_retention_policy" type="object">
  The resource_retention_policy configures the retention of compute resources after a job completes, specifying the number of completed jobs for which AWS Batch will retain resources to aid in troubleshooting. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="skip_deregister_on_update" type="bool">
      When set to true, this Boolean field tells AWS Batch not to deregister the current job definition revision when an update creates a new revision, leaving the old revision available for use. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="retry_strategy" type="object">
  Defines the retry strategy for the job, including the maximum number of retry attempts and the conditions under which a job should be retried based on its exit status. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="attempts" type="number">
      Specifies the total number of attempts (including the original) that AWS Batch will make for the job before considering it failed, with a valid range of 1 to 10, so an attempts value of N allows up to N-1 retries on failure. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="evaluate_on_exit" type="list(object)">
      Lists conditions that trigger specific actions (like retry or mark as failed) based on a job's exit code, status reason, or reason, controlling the retry behavior of the Batch job. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="action" type="string">
          The action to take when the condition in this evaluate_on_exit rule is satisfied, which is either 'RETRY' to rerun the job or 'EXIT' to end the job's execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="on_exit_code" type="string">
          The job exit code (or a range of exit codes, e.g., '1-10' or '*') that must be matched for the corresponding retry action to be taken in an AWS Batch job definition's retry strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="on_reason" type="string">
          Specifies the job's status reason string that triggers this evaluate-on-exit action when matched, such as a specific failure reason like 'CapacityError', causing the job to be retried according to the retry strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="on_status_reason" type="string">
          Specifies a status reason string to match against the job's status reason, triggering the corresponding retry action when the job exits with that status reason. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scheduling_priority" type="number">
  The scheduling priority for jobs using this job definition, which determines the order in which they are started when multiple jobs share the same job queue, with higher values given higher priority. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

<ResponseField name="timeout" type="object">
  The timeout configuration for the job definition, specifying the maximum duration (attemptDurationSeconds) that the job's container can run before it is forcibly terminated. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="attempt_duration_seconds" type="number">
      The timeout duration in seconds for a single job attempt; after this time elapses, AWS Batch terminates the attempt. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Specifies whether the job definition runs as a container job or a multinode job, using the value 'container' or 'multinode'. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="job_definition_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AWS Batch job definition. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
