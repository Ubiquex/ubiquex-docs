---
title: "aws_batch_consumable_resource"
description: "`aws_batch_consumable_resource` defines a real constraint beyond vCPU, GPU, or memory, a third-party license token pool, a database connection limit..."
---

`aws_batch_consumable_resource` defines a real constraint beyond vCPU, GPU, or memory, a third-party license token pool, a database connection limit, an API rate cap, that AWS Batch's resource-aware scheduling checks before starting a job, so a job doesn't launch, consume compute time, and then fail discovering the real resource it needed was unavailable.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	batch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/batch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own consumable resource"})

    		ubx.Resource(batch.ConsumableResource, "example", batch.ConsumableResourceConfig{
    			ResourceType:           "example",
    			TotalQuantity:          1,
    			ConsumableResourceName: "example-consumable-resource-name",
    			Tags:                   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConsumableResource } from "@ubx/sdk-aws/aws/batch/consumable_resource";

    export default stack("example", () => {
      intent({ summary: "example own consumable resource" });

      resource(ConsumableResource, "example", {
        resourceType: "example",
        totalQuantity: 1,
        consumableResourceName: "example-consumable-resource-name",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.batch import ConsumableResource, ConsumableResourceConfig

    def describe():
        ubx.intent("example own consumable resource")

        ubx.resource(ConsumableResource, "example", ConsumableResourceConfig(
            resource_type="example",
            total_quantity=1,
            consumable_resource_name="example-consumable-resource-name",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_batch_consumable_resource, with resource type example,
    total quantity 1, consumable resource name
    example-consumable-resource-name, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="consumable_resource_name" type="string">
  Name of ConsumableResource. Optional.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  Type of Consumable Resource. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

<ResponseField name="total_quantity" type="number" required>
  Total Quantity of ConsumableResource. Required.
</ResponseField>

## Output properties

<ResponseField name="available_quantity" type="number">
  Available Quantity of ConsumableResource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="consumable_resource_arn" type="string">
  ARN of the Consumable Resource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="number">
  The Unix timestamp (in seconds) when AWS Batch created the consumable resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="in_use_quantity" type="number">
  In-use Quantity of ConsumableResource. Computed by AWS after `ship`.
</ResponseField>
