---
title: "aws_batch_job_queue"
description: "Real, generated bindings for `aws_batch_job_queue`."
---

`aws_batch_job_queue` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/batch"

    cfg := batch.JobQueueConfig{
        Name: "example-name",
        Priority: 1,
        State: "example",
        SchedulingPolicyArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(batch.JobQueue, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { JobQueue } from "jsr:@ubx/sdk-aws/aws/batch/job_queue";

    resource(JobQueue, "example", {
      name: "example",
      priority: 1,
      state: "example",
      schedulingPolicyArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.batch import JobQueue, JobQueueConfig

    cfg = JobQueueConfig(
        name="example",
        priority=1,
        state="example",
        scheduling_policy_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(JobQueue, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_batch_job_queue with name, priority, state,
scheduling_policy_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scheduling_policy_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
