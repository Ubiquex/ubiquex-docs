---
title: "aws_arcregionswitch_plan"
description: "Real, generated bindings for `aws_arcregionswitch_plan`."
---

`aws_arcregionswitch_plan` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/arcregionswitch"

    cfg := arcregionswitch.PlanConfig{
        ExecutionRole: "example",
        Name: "example-name",
        RecoveryApproach: "example",
        Regions: []string{"example"},
        Description: "example",
        PrimaryRegion: "example",
    }
    ubx.Resource(arcregionswitch.Plan, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Plan } from "jsr:@ubx/sdk-aws/aws/arcregionswitch/plan";

    resource(Plan, "example", {
      executionRole: "example",
      name: "example",
      recoveryApproach: "example",
      regions: ["example"],
      description: "example",
      primaryRegion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.arcregionswitch import Plan, PlanConfig

    cfg = PlanConfig(
        execution_role="example",
        name="example",
        recovery_approach="example",
        regions=["example"],
        description="example",
        primary_region="example",
    )
    ubx.resource(Plan, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_arcregionswitch_plan with execution_role, name,
recovery_approach, regions, description, primary_region.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="execution_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="primary_region" type="string">
  Optional.
</ResponseField>

<ResponseField name="recovery_approach" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recovery_time_objective_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="regions" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
