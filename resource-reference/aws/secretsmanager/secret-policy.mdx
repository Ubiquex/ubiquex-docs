---
title: "aws_secretsmanager_secret_policy"
description: "Real, generated bindings for `aws_secretsmanager_secret_policy`."
---

`aws_secretsmanager_secret_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/secretsmanager"

    cfg := secretsmanager.SecretPolicyConfig{
        Policy: "example",
        SecretArn: "example",
        BlockPublicPolicy: true,
    }
    ubx.Resource(secretsmanager.SecretPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecretPolicy } from
      "jsr:@ubx/sdk-aws/aws/secretsmanager/secret_policy";

    resource(SecretPolicy, "example", {
      policy: "example",
      secretArn: "example",
      blockPublicPolicy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.secretsmanager import (
        SecretPolicy,
        SecretPolicyConfig,
    )

    cfg = SecretPolicyConfig(
        policy="example",
        secret_arn="example",
        block_public_policy=True,
    )
    ubx.resource(SecretPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_secretsmanager_secret_policy with policy,
secret_arn, block_public_policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="block_public_policy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
