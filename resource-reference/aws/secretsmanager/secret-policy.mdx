---
title: "aws_secretsmanager_secret_policy"
description: "Real, generated bindings for `aws_secretsmanager_secret_policy`."
---

`aws_secretsmanager_secret_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	"encoding/json"

    	secretsmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/secretsmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own secret policy"})

    		accessPolicy, _ := json.Marshal(map[string]any{
    			"Version": "2012-10-17",
    			"Statement": []map[string]any{{
    				"Effect":   "Allow",
    				"Action":   "*",
    				"Resource": "*",
    			}},
    		})

    		ubx.Resource(secretsmanager.SecretPolicy, "ci-runner", secretsmanager.SecretPolicyConfig{
    			Policy:            string(accessPolicy),
    			SecretArn:         "arn:aws:iam::123456789012:policy/ci",
    			BlockPublicPolicy: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecretPolicy } from "jsr:@ubx/sdk-aws/aws/secretsmanager/secret_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own secret policy" });

      const accessPolicy = JSON.stringify({
        Version: "2012-10-17",
        Statement: [{
          Effect: "Allow",
          Action: "*",
          Resource: "*",
        }],
      });

      resource(SecretPolicy, "ci-runner", {
        policy: accessPolicy,
        secretArn: "arn:aws:iam::123456789012:policy/ci",
        blockPublicPolicy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import json
    import ubx_sdk as ubx
    from ubx.aws.secretsmanager import SecretPolicy, SecretPolicyConfig

    def describe():
        ubx.intent("payments own secret policy")

        access_policy = json.dumps({
            "Version": "2012-10-17",
            "Statement": [{
                "Effect": "Allow",
                "Action": "*",
                "Resource": "*",
            }],
        })

        ubx.resource(SecretPolicy, "ci-runner", SecretPolicyConfig(
            policy=access_policy,
            secret_arn="arn:aws:iam::123456789012:policy/ci",
            block_public_policy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_secretsmanager_secret_policy, with policy a real,
    valid value, secret arn arn:aws:iam::123456789012:policy/ci,
    block public policy true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="block_public_policy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
