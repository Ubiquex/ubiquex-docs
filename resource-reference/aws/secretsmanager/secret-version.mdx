---
title: "aws_secretsmanager_secret_version"
description: "Real, generated bindings for `aws_secretsmanager_secret_version`."
---

`aws_secretsmanager_secret_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	secretsmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/secretsmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own secret version"})

    		ubx.Resource(secretsmanager.SecretVersion, "ci-runner", secretsmanager.SecretVersionConfig{
    			SecretId:              "example",
    			SecretBinary:          "example",
    			SecretString:          "example",
    			SecretStringWo:        "example",
    			SecretStringWoVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecretVersion } from "jsr:@ubx/sdk-aws/aws/secretsmanager/secret_version";

    export default stack("payments", () => {
      intent({ summary: "payments own secret version" });

      resource(SecretVersion, "ci-runner", {
        secretId: "example",
        secretBinary: "example",
        secretString: "example",
        secretStringWo: "example",
        secretStringWoVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.secretsmanager import SecretVersion, SecretVersionConfig

    def describe():
        ubx.intent("payments own secret version")

        ubx.resource(SecretVersion, "ci-runner", SecretVersionConfig(
            secret_id="example",
            secret_binary="example",
            secret_string="example",
            secret_string_wo="example",
            secret_string_wo_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_secretsmanager_secret_version, with secret id example,
    secret binary example, secret string example, secret string wo
    example, secret string wo version 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_binary" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="secret_string" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_string_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_string_wo_version" type="number">
  Optional.
</ResponseField>

<ResponseField name="version_stages" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="has_secret_string_wo" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_stages" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
