---
title: "aws_secretsmanager_secret_version"
description: "Real, generated bindings for `aws_secretsmanager_secret_version`."
---

`aws_secretsmanager_secret_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/secretsmanager"

    cfg := secretsmanager.SecretVersionConfig{
        SecretId: "example",
        SecretBinary: "example",
        SecretString: "example",
    }
    ubx.Resource(secretsmanager.SecretVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecretVersion } from
      "jsr:@ubx/sdk-aws/aws/secretsmanager/secret_version";

    resource(SecretVersion, "example", {
      secretId: "example",
      secretBinary: "example",
      secretString: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.secretsmanager import (
        SecretVersion,
        SecretVersionConfig,
    )

    cfg = SecretVersionConfig(
        secret_id="example",
        secret_binary="example",
        secret_string="example",
    )
    ubx.resource(SecretVersion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_secretsmanager_secret_version with secret_id,
secret_binary, secret_string.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_binary" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="secret_string" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_string_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_string_wo_version" type="number">
  Optional.
</ResponseField>

<ResponseField name="version_stages" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="has_secret_string_wo" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version_stages" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
