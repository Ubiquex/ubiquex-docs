---
title: "aws_secretsmanager_secret_rotation"
description: "Real, generated bindings for `aws_secretsmanager_secret_rotation`."
---

`aws_secretsmanager_secret_rotation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/secretsmanager"

    cfg := secretsmanager.SecretRotationConfig{
        SecretId: "example",
        ExternalSecretRotationRoleArn: "example",
        RotateImmediately: true,
    }
    ubx.Resource(secretsmanager.SecretRotation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecretRotation } from
      "jsr:@ubx/sdk-aws/aws/secretsmanager/secret_rotation";

    resource(SecretRotation, "example", {
      secretId: "example",
      externalSecretRotationRoleArn: "example",
      rotateImmediately: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.secretsmanager import (
        SecretRotation,
        SecretRotationConfig,
    )

    cfg = SecretRotationConfig(
        secret_id="example",
        external_secret_rotation_role_arn="example",
        rotate_immediately=True,
    )
    ubx.resource(SecretRotation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_secretsmanager_secret_rotation with secret_id,
external_secret_rotation_role_arn, rotate_immediately.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="external_secret_rotation_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rotate_immediately" type="bool">
  Optional.
</ResponseField>

<ResponseField name="rotation_lambda_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rotation_enabled" type="bool">
  Computed by AWS after `ship`.
</ResponseField>
