---
title: "aws_route_table_association"
description: "Real, generated bindings for `aws_route_table_association`."
---

`aws_route_table_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route"

    cfg := route.TableAssociationConfig{
        RouteTableId: "example",
        GatewayId: "example",
        SubnetId: "example",
    }
    ubx.Resource(route.TableAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TableAssociation } from
      "jsr:@ubx/sdk-aws/aws/route/table_association";

    resource(TableAssociation, "example", {
      routeTableId: "example",
      gatewayId: "example",
      subnetId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route import (
        TableAssociation,
        TableAssociationConfig,
    )

    cfg = TableAssociationConfig(
        route_table_id="example",
        gateway_id="example",
        subnet_id="example",
    )
    ubx.resource(TableAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route_table_association with route_table_id,
gateway_id, subnet_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="gateway_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
