---
title: "aws_route_table_association"
description: "Real, generated bindings for `aws_route_table_association`."
---

`aws_route_table_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own table association"})

    		ubx.Resource(route.TableAssociation, "ci-runner", route.TableAssociationConfig{
    			RouteTableId: "example",
    			GatewayId:    "example",
    			SubnetId:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TableAssociation } from "jsr:@ubx/sdk-aws/aws/route/table_association";

    export default stack("payments", () => {
      intent({ summary: "payments own table association" });

      resource(TableAssociation, "ci-runner", {
        routeTableId: "example",
        gatewayId: "example",
        subnetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route import TableAssociation, TableAssociationConfig

    def describe():
        ubx.intent("payments own table association")

        ubx.resource(TableAssociation, "ci-runner", TableAssociationConfig(
            route_table_id="example",
            gateway_id="example",
            subnet_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route_table_association, with route table id example,
    gateway id example, subnet id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="gateway_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
