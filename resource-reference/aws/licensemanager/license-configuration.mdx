---
title: "aws_licensemanager_license_configuration"
description: "Real, generated bindings for `aws_licensemanager_license_configuration`."
---

`aws_licensemanager_license_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	licensemanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/licensemanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own license configuration"})

    		ubx.Resource(licensemanager.LicenseConfiguration, "ci-runner", licensemanager.LicenseConfigurationConfig{
    			LicenseCountingType:   "example",
    			Name:                  "ci-runner",
    			Description:           "Managed by ubx.",
    			LicenseCount:          1,
    			LicenseCountHardLimit: true,
    			LicenseRules:          []string{"example"},
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LicenseConfiguration } from "jsr:@ubx/sdk-aws/aws/licensemanager/license_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own license configuration" });

      resource(LicenseConfiguration, "ci-runner", {
        licenseCountingType: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        licenseCount: 1,
        licenseCountHardLimit: true,
        licenseRules: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.licensemanager import LicenseConfiguration, LicenseConfigurationConfig

    def describe():
        ubx.intent("payments own license configuration")

        ubx.resource(LicenseConfiguration, "ci-runner", LicenseConfigurationConfig(
            license_counting_type="example",
            name="ci-runner",
            description="Managed by ubx.",
            license_count=1,
            license_count_hard_limit=True,
            license_rules=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_licensemanager_license_configuration called
    "ci-runner", with license counting type example, description
    Managed by ubx., license count 1, license count hard limit true,
    license rules a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_count" type="number">
  Optional.
</ResponseField>

<ResponseField name="license_count_hard_limit" type="bool">
  Optional.
</ResponseField>

<ResponseField name="license_counting_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="license_rules" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
