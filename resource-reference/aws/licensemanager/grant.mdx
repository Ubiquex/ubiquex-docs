---
title: "aws_licensemanager_grant"
description: "Real, generated bindings for `aws_licensemanager_grant`."
---

`aws_licensemanager_grant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	licensemanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/licensemanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own grant"})

    		ubx.Resource(licensemanager.Grant, "ci-runner", licensemanager.GrantConfig{
    			AllowedOperations: []string{"example"},
    			LicenseArn:        "arn:aws:iam::123456789012:policy/ci",
    			Name:              "ci-runner",
    			Principal:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Grant } from "jsr:@ubx/sdk-aws/aws/licensemanager/grant";

    export default stack("payments", () => {
      intent({ summary: "payments own grant" });

      resource(Grant, "ci-runner", {
        allowedOperations: ["example"],
        licenseArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        principal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.licensemanager import Grant, GrantConfig

    def describe():
        ubx.intent("payments own grant")

        ubx.resource(Grant, "ci-runner", GrantConfig(
            allowed_operations=["example"],
            license_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_licensemanager_grant called "ci-runner", with allowed
    operations a set value, license arn
    arn:aws:iam::123456789012:policy/ci, principal example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_operations" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="parent_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
