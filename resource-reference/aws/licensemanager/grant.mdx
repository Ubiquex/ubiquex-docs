---
title: "aws_licensemanager_grant"
description: "Real, generated bindings for `aws_licensemanager_grant`."
---

`aws_licensemanager_grant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/licensemanager"

    cfg := licensemanager.GrantConfig{
        AllowedOperations: []string{"example"},
        LicenseArn: "example",
        Name: "example-name",
        Principal: "example",
    }
    ubx.Resource(licensemanager.Grant, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Grant } from
      "jsr:@ubx/sdk-aws/aws/licensemanager/grant";

    resource(Grant, "example", {
      allowedOperations: ["example"],
      licenseArn: "example",
      name: "example",
      principal: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.licensemanager import Grant, GrantConfig

    cfg = GrantConfig(
        allowed_operations=["example"],
        license_arn="example",
        name="example",
        principal="example",
    )
    ubx.resource(Grant, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_licensemanager_grant with allowed_operations,
license_arn, name, principal.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allowed_operations" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="parent_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
