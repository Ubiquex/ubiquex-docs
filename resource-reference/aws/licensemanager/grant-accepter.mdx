---
title: "aws_licensemanager_grant_accepter"
description: "Real, generated bindings for `aws_licensemanager_grant_accepter`."
---

`aws_licensemanager_grant_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/licensemanager"

    cfg := licensemanager.GrantAccepterConfig{
        GrantArn: "example",
    }
    ubx.Resource(licensemanager.GrantAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GrantAccepter } from
      "jsr:@ubx/sdk-aws/aws/licensemanager/grant_accepter";

    resource(GrantAccepter, "example", {
      grantArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.licensemanager import (
        GrantAccepter,
        GrantAccepterConfig,
    )

    cfg = GrantAccepterConfig(
        grant_arn="example",
    )
    ubx.resource(GrantAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_licensemanager_grant_accepter with grant_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="grant_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="allowed_operations" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="parent_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="principal" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
