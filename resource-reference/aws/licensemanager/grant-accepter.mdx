---
title: "aws_licensemanager_grant_accepter"
description: "Real, generated bindings for `aws_licensemanager_grant_accepter`."
---

`aws_licensemanager_grant_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	licensemanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/licensemanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own grant accepter"})

    		ubx.Resource(licensemanager.GrantAccepter, "ci-runner", licensemanager.GrantAccepterConfig{
    			GrantArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GrantAccepter } from "jsr:@ubx/sdk-aws/aws/licensemanager/grant_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own grant accepter" });

      resource(GrantAccepter, "ci-runner", {
        grantArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.licensemanager import GrantAccepter, GrantAccepterConfig

    def describe():
        ubx.intent("payments own grant accepter")

        ubx.resource(GrantAccepter, "ci-runner", GrantAccepterConfig(
            grant_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_licensemanager_grant_accepter, with grant arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="grant_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="allowed_operations" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="home_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="parent_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="principal" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
