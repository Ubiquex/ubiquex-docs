---
title: "aws_ssmcontacts_contact_channel"
description: "Real, generated bindings for `aws_ssmcontacts_contact_channel`."
---

`aws_ssmcontacts_contact_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssmcontacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssmcontacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own contact channel"})

    		ubx.Resource(ssmcontacts.ContactChannel, "ci-runner", ssmcontacts.ContactChannelConfig{
    			ContactId: "example",
    			Name:      "ci-runner",
    			Type:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContactChannel } from "jsr:@ubx/sdk-aws/aws/ssmcontacts/contact_channel";

    export default stack("payments", () => {
      intent({ summary: "payments own contact channel" });

      resource(ContactChannel, "ci-runner", {
        contactId: "example",
        name: "ci-runner",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssmcontacts import ContactChannel, ContactChannelConfig

    def describe():
        ubx.intent("payments own contact channel")

        ubx.resource(ContactChannel, "ci-runner", ContactChannelConfig(
            contact_id="example",
            name="ci-runner",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssmcontacts_contact_channel called "ci-runner", with
    contact id example, type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="contact_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="activation_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
