---
title: "aws_ssmcontacts_contact_channel"
description: "Real, generated bindings for `aws_ssmcontacts_contact_channel`."
---

`aws_ssmcontacts_contact_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssmcontacts"

    cfg := ssmcontacts.ContactChannelConfig{
        ContactId: "example",
        Name: "example-name",
        Type: "example",
    }
    ubx.Resource(ssmcontacts.ContactChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContactChannel } from
      "jsr:@ubx/sdk-aws/aws/ssmcontacts/contact_channel";

    resource(ContactChannel, "example", {
      contactId: "example",
      name: "example",
      type: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssmcontacts import (
        ContactChannel,
        ContactChannelConfig,
    )

    cfg = ContactChannelConfig(
        contact_id="example",
        name="example",
        type="example",
    )
    ubx.resource(ContactChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssmcontacts_contact_channel with contact_id, name,
type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="contact_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="activation_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
