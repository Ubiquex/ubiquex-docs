---
title: "aws_ssmcontacts_plan"
description: "Real, generated bindings for `aws_ssmcontacts_plan`."
---

`aws_ssmcontacts_plan` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/ssmcontacts"

    cfg := ssmcontacts.PlanConfig{
        ContactId: "example",
    }
    ubx.Resource(ssmcontacts.Plan, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Plan } from "jsr:@ubx/sdk-aws/aws/ssmcontacts/plan";

    resource(Plan, "example", {
      contactId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssmcontacts import Plan, PlanConfig

    cfg = PlanConfig(
        contact_id="example",
    )
    ubx.resource(Plan, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssmcontacts_plan with contact_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="contact_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
