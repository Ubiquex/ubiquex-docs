---
title: "aws_glacier_vault_lock"
description: "Real, generated bindings for `aws_glacier_vault_lock`."
---

`aws_glacier_vault_lock` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glacier"

    cfg := glacier.VaultLockConfig{
        CompleteLock: true,
        Policy: "example",
        VaultName: "example-vault-name",
        IgnoreDeletionError: true,
    }
    ubx.Resource(glacier.VaultLock, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VaultLock } from
      "jsr:@ubx/sdk-aws/aws/glacier/vault_lock";

    resource(VaultLock, "example", {
      completeLock: true,
      policy: "example",
      vaultName: "example",
      ignoreDeletionError: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glacier import VaultLock, VaultLockConfig

    cfg = VaultLockConfig(
        complete_lock=True,
        policy="example",
        vault_name="example",
        ignore_deletion_error=True,
    )
    ubx.resource(VaultLock, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glacier_vault_lock with complete_lock, policy,
vault_name, ignore_deletion_error.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="complete_lock" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ignore_deletion_error" type="bool">
  Optional.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vault_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
