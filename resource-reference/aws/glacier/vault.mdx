---
title: "aws_glacier_vault"
description: "Real, generated bindings for `aws_glacier_vault`."
---

`aws_glacier_vault` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/glacier"

    cfg := glacier.VaultConfig{
        Name: "example-name",
        AccessPolicy: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(glacier.Vault, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Vault } from "jsr:@ubx/sdk-aws/aws/glacier/vault";

    resource(Vault, "example", {
      name: "example",
      accessPolicy: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glacier import Vault, VaultConfig

    cfg = VaultConfig(
        name="example",
        access_policy="example",
        tags={"key": "value"},
    )
    ubx.resource(Vault, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glacier_vault with name, access_policy, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
