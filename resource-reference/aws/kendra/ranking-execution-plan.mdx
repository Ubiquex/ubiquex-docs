---
title: "aws_kendra_ranking_execution_plan"
description: "`aws_kendra_ranking_execution_plan` provisions Kendra Intelligent Ranking..."
---

`aws_kendra_ranking_execution_plan` provisions Kendra Intelligent Ranking, a real, standalone semantic re-ranking API a caller applies to results from their own separate search system, OpenSearch or Elasticsearch, for instance, independent of using a full `aws_kendra_index`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ranking execution plan"})

    		ubx.Resource(kendra.RankingExecutionPlan, "ci-runner", kendra.RankingExecutionPlanConfig{
    			Name:          "ci-runner",
    			CapacityUnits: map[string]any{"rescore_capacity_units": 1},
    			Description:   "Managed by ubx.",
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RankingExecutionPlan } from "./local-sdk/aws/sdk/typescript/aws/kendra/ranking_execution_plan.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own ranking execution plan" });

      resource(RankingExecutionPlan, "ci-runner", {
        name: "ci-runner",
        capacityUnits: { rescoreCapacityUnits: 1 },
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.kendra import RankingExecutionPlan, RankingExecutionPlanConfig

    def describe():
        ubx.intent("payments own ranking execution plan")

        ubx.resource(RankingExecutionPlan, "ci-runner", RankingExecutionPlanConfig(
            name="ci-runner",
            capacity_units={"rescore_capacity_units": 1},
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_kendra_ranking_execution_plan called "ci-runner", with
    capacity units a set value, description Managed by ubx., tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity_units" type="object">
  Specifies the capacity configuration for the execution plan, including the number of rescore capacity units allocated to handle query processing for Amazon Kendra Intelligent Ranking. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="rescore_capacity_units" type="number" required>
      Specifies the number of rescore capacity units for the execution plan, which determines the rescoring query capacity (throughput) of the plan. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of the Amazon Kendra ranking execution plan, used to provide additional context or identification for the plan. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of kendra ranking rescore execution plan Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  List of tags Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of an arbitrary tag attached to the AWS Kendra Ranking execution plan, used for organizing, identifying, and filtering the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair assigned to the Amazon Kendra Intelligent Ranking execution plan, used to organize, identify, and manage access to the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the Kendra Ranking execution plan. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Unique ID of rescore execution plan Computed by AWS after `ship`.
</ResponseField>
