---
title: "aws_kendra_thesaurus"
description: "`aws_kendra_thesaurus` teaches an `aws_kendra_index` real synonym relationships..."
---

`aws_kendra_thesaurus` teaches an `aws_kendra_index` real synonym relationships, so a search for one term also matches documents using an equivalent term, improving recall beyond exact keyword matching.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kendra "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kendra"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own thesaurus"})

    		ubx.Resource(kendra.Thesaurus, "ci-runner", kendra.ThesaurusConfig{
    			IndexId:      "example",
    			Name:         "ci-runner",
    			RoleArn:      "arn:aws:iam::123456789012:policy/ci",
    			SourceS3Path: map[string]any{"bucket": "example"},
    			Description:  "Managed by ubx.",
    			Tags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Thesaurus } from "./local-sdk/aws/sdk/typescript/aws/kendra/thesaurus.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own thesaurus" });

      resource(Thesaurus, "ci-runner", {
        indexId: "example",
        name: "ci-runner",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        sourceS3Path: { bucket: "example" },
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.kendra import Thesaurus, ThesaurusConfig

    def describe():
        ubx.intent("payments own thesaurus")

        ubx.resource(Thesaurus, "ci-runner", ThesaurusConfig(
            index_id="example",
            name="ci-runner",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            source_s3_path={"bucket": "example"},
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_kendra_thesaurus called "ci-runner", with index id
    example, role arn arn:aws:iam::123456789012:policy/ci, source s3
    path a set value, description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description for the thesaurus. Optional.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  The identifier of the index for the thesaurus. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  A name for the thesaurus. Required.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  An IAM role that gives Amazon Kendra permissions to access the thesaurus file specified in SourceS3Path. Required.
</ResponseField>

<ResponseField name="source_s3_path" type="object" required>
  Information required to find a specific file in an Amazon S3 bucket. Required.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string" required>
      The name of the S3 bucket that contains the file. Required.
    </ResponseField>
    <ResponseField name="key" type="string" required>
      The name of the file. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs that identify or categorize the thesaurus. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to an AWS Kendra thesaurus, used to organize and identify the resource for management and billing. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag entry associated with the AWS Kendra thesaurus, used for organizing and identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the thesaurus. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The identifier of the thesaurus. Computed by AWS after `ship`.
</ResponseField>
