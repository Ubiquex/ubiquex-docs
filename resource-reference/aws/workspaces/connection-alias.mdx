---
title: "aws_workspaces_connection_alias"
description: "Real, generated bindings for `aws_workspaces_connection_alias`."
---

`aws_workspaces_connection_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/workspaces"

    cfg := workspaces.ConnectionAliasConfig{
        ConnectionString: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(workspaces.ConnectionAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionAlias } from
      "jsr:@ubx/sdk-aws/aws/workspaces/connection_alias";

    resource(ConnectionAlias, "example", {
      connectionString: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.workspaces import (
        ConnectionAlias,
        ConnectionAliasConfig,
    )

    cfg = ConnectionAliasConfig(
        connection_string="example",
        tags={"key": "value"},
    )
    ubx.resource(ConnectionAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_workspaces_connection_alias with connection_string,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_string" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
