---
title: "aws_waf_geo_match_set"
description: "Real, generated bindings for `aws_waf_geo_match_set`."
---

`aws_waf_geo_match_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/waf"

    cfg := waf.GeoMatchSetConfig{
        Name: "example-name",
    }
    ubx.Resource(waf.GeoMatchSet, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GeoMatchSet } from
      "jsr:@ubx/sdk-aws/aws/waf/geo_match_set";

    resource(GeoMatchSet, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.waf import GeoMatchSet, GeoMatchSetConfig

    cfg = GeoMatchSetConfig(
        name="example",
    )
    ubx.resource(GeoMatchSet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_waf_geo_match_set with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
