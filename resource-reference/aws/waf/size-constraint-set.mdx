---
title: "aws_waf_size_constraint_set"
description: "Real, generated bindings for `aws_waf_size_constraint_set`."
---

`aws_waf_size_constraint_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/waf"

    cfg := waf.SizeConstraintSetConfig{
        Name: "example-name",
    }
    ubx.Resource(waf.SizeConstraintSet, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SizeConstraintSet } from
      "jsr:@ubx/sdk-aws/aws/waf/size_constraint_set";

    resource(SizeConstraintSet, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.waf import (
        SizeConstraintSet,
        SizeConstraintSetConfig,
    )

    cfg = SizeConstraintSetConfig(
        name="example",
    )
    ubx.resource(SizeConstraintSet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_waf_size_constraint_set with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
