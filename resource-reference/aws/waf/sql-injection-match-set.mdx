---
title: "aws_waf_sql_injection_match_set"
description: "Real, generated bindings for `aws_waf_sql_injection_match_set`."
---

`aws_waf_sql_injection_match_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sql injection match set"})

    		ubx.Resource(waf.SqlInjectionMatchSet, "ci-runner", waf.SqlInjectionMatchSetConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SqlInjectionMatchSet } from "jsr:@ubx/sdk-aws/aws/waf/sql_injection_match_set";

    export default stack("payments", () => {
      intent({ summary: "payments own sql injection match set" });

      resource(SqlInjectionMatchSet, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.waf import SqlInjectionMatchSet, SqlInjectionMatchSetConfig

    def describe():
        ubx.intent("payments own sql injection match set")

        ubx.resource(SqlInjectionMatchSet, "ci-runner", SqlInjectionMatchSetConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_waf_sql_injection_match_set called "ci-runner", with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
