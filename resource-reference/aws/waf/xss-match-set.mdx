---
title: "aws_waf_xss_match_set"
description: "`aws_waf_xss_match_set` defines a real condition, matching cross-site-scripting-like patterns in a request part, that an `aws_waf_rule` references."
---

Like the rest of AWS WAF Classic, support for this resource ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own xss match set"})

    		ubx.Resource(waf.XssMatchSet, "example", waf.XssMatchSetConfig{
    			Name:           "example",
    			XssMatchTuples: []map[string]any{{"field_to_match": map[string]any{"data": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { XssMatchSet } from "@ubx/sdk-aws/aws/waf/xss_match_set";

    export default stack("example", () => {
      intent({ summary: "example own xss match set" });

      resource(XssMatchSet, "example", {
        name: "example",
        xssMatchTuples: [{ fieldToMatch: { data: "example" } }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.waf import XssMatchSet, XssMatchSetConfig
    from ubx.aws.waf.xss_match_set import XssMatchSet_XssMatchTuples, XssMatchSet_XssMatchTuples_FieldToMatch

    def describe():
        ubx.intent("example own xss match set")

        ubx.resource(XssMatchSet, "example", XssMatchSetConfig(
            name="example",
            xss_match_tuples=[XssMatchSet_XssMatchTuples(field_to_match=XssMatchSet_XssMatchTuples_FieldToMatch(data="example"))],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_waf_xss_match_set called "example", with xss match
    tuples a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  A friendly name or description of the XssMatchSet, which is required and cannot be changed after creation. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="xss_match_tuples" type="list(object)" required>
  Specifies the parts of the web request to inspect for cross-site scripting attacks and the text transformations to apply, where each tuple consists of a FieldToMatch and a TextTransformation. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="field_to_match" type="object">
      Specifies the part of a web request to inspect for cross-site scripting attacks, such as the URI, query string, body, or a named header, via the type and optional data subfields. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="data" type="string">
          The name of the header or query argument that AWS WAF inspects, used when the field-to-match type is HEADER or SINGLE_QUERY_ARG. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the type of web request component (such as HEADER, METHOD, QUERY_STRING, URI, or BODY) that AWS WAF inspects for cross-site scripting attacks. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="text_transformation" type="string">
      Specifies how AWS WAF transforms the text component of the web request before inspection for XSS patterns, such as converting to lowercase, decoding HTML entities, or compressing white spaces. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier assigned by AWS to the XSS match set, used to reference the set in other WAF resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
