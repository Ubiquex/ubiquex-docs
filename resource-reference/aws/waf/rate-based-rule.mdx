---
title: "aws_waf_rate_based_rule"
description: "Real, generated bindings for `aws_waf_rate_based_rule`."
---

`aws_waf_rate_based_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	waf "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/waf"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rate based rule"})

    		ubx.Resource(waf.RateBasedRule, "ci-runner", waf.RateBasedRuleConfig{
    			MetricName: "example-metric-name",
    			Name:       "ci-runner",
    			RateKey:    "example",
    			RateLimit:  1,
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RateBasedRule } from "jsr:@ubx/sdk-aws/aws/waf/rate_based_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own rate based rule" });

      resource(RateBasedRule, "ci-runner", {
        metricName: "example-metric-name",
        name: "ci-runner",
        rateKey: "example",
        rateLimit: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.waf import RateBasedRule, RateBasedRuleConfig

    def describe():
        ubx.intent("payments own rate based rule")

        ubx.resource(RateBasedRule, "ci-runner", RateBasedRuleConfig(
            metric_name="example-metric-name",
            name="ci-runner",
            rate_key="example",
            rate_limit=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_waf_rate_based_rule called "ci-runner", with metric
    name example-metric-name, rate key example, rate limit 1, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="predicates" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="data_id" type="string" required>
    </ResponseField>
    <ResponseField name="negated" type="bool" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rate_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rate_limit" type="number" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="predicates" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="data_id" type="string" required>
    </ResponseField>
    <ResponseField name="negated" type="bool" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
