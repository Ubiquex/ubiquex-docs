---
title: "aws_waf_web_acl"
description: "Real, generated bindings for `aws_waf_web_acl`."
---

`aws_waf_web_acl` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/waf"

    cfg := waf.WebAclConfig{
        MetricName: "example-metric-name",
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(waf.WebAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WebAcl } from "jsr:@ubx/sdk-aws/aws/waf/web_acl";

    resource(WebAcl, "example", {
      metricName: "example",
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.waf import WebAcl, WebAclConfig

    cfg = WebAclConfig(
        metric_name="example",
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(WebAcl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_waf_web_acl with metric_name, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metric_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
