---
title: "aws_location_place_index"
description: "Real, generated bindings for `aws_location_place_index`."
---

`aws_location_place_index` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	location "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own place index"})

    		ubx.Resource(location.PlaceIndex, "ci-runner", location.PlaceIndexConfig{
    			DataSource:  "example",
    			IndexName:   "example-index-name",
    			Description: "Managed by ubx.",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlaceIndex } from "jsr:@ubx/sdk-aws/aws/location/place_index";

    export default stack("payments", () => {
      intent({ summary: "payments own place index" });

      resource(PlaceIndex, "ci-runner", {
        dataSource: "example",
        indexName: "example-index-name",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.location import PlaceIndex, PlaceIndexConfig

    def describe():
        ubx.intent("payments own place index")

        ubx.resource(PlaceIndex, "ci-runner", PlaceIndexConfig(
            data_source="example",
            index_name="example-index-name",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_location_place_index, with data source example, index
    name example-index-name, description Managed by ubx., tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data_source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="index_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="index_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
