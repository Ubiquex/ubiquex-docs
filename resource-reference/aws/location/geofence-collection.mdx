---
title: "aws_location_geofence_collection"
description: "`aws_location_geofence_collection` creates a real collection of virtual geographic boundaries that Amazon Location Service monitors devices against..."
---

`aws_location_geofence_collection` creates a real collection of virtual geographic boundaries that Amazon Location Service monitors devices against, raising an event when a tracked device enters or leaves one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	location "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own geofence collection"})

    		ubx.Resource(location.GeofenceCollection, "example", location.GeofenceCollectionConfig{
    			CollectionName:        "example-collection-name",
    			Description:           "Managed by ubx.",
    			KmsKeyId:              "example",
    			PricingPlan:           "example",
    			PricingPlanDataSource: "example",
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GeofenceCollection } from "./local-sdk/aws/sdk/typescript/aws/location/geofence_collection.ts";

    export default stack("example", () => {
      intent({ summary: "example own geofence collection" });

      resource(GeofenceCollection, "example", {
        collectionName: "example-collection-name",
        description: "Managed by ubx.",
        kmsKeyId: "example",
        pricingPlan: "example",
        pricingPlanDataSource: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.location import GeofenceCollection, GeofenceCollectionConfig

    def describe():
        ubx.intent("example own geofence collection")

        ubx.resource(GeofenceCollection, "example", GeofenceCollectionConfig(
            collection_name="example-collection-name",
            description="Managed by ubx.",
            kms_key_id="example",
            pricing_plan="example",
            pricing_plan_data_source="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_location_geofence_collection, with collection name
    example-collection-name, description Managed by ubx., kms key id
    example, pricing plan example, pricing plan data source example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="collection_name" type="string" required>
  The name of the geofence collection, which must be unique within the AWS account and region and serves as the collection's identifier. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  An optional descriptive text that provides a human-readable overview or purpose of the geofence collection in Amazon Location Service. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The identifier (key ID or ARN) of the customer-managed AWS KMS key used to encrypt the geofence collection's data. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="pricing_plan" type="string">
  The pricing plan for the geofence collection, which determines the billing model (such as request-based usage) for geofencing operations performed on that collection. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="pricing_plan_data_source" type="string">
  This shape is deprecated since 2022-02-01: Deprecated. No longer allowed. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Location geofence collection, used to label the resource with custom metadata for identification, organization, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The unique Amazon Resource Name (ARN) assigned to the geofence collection. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="collection_arn" type="string">
  The Amazon Resource Name (ARN) of the geofence collection, which uniquely identifies it within AWS and is assigned by the service when the collection is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="create_time" type="string">
  The datetime value in ISO 8601 format. The timezone is always UTC. (YYYY-MM-DDThh:mm:ss.sssZ) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  The datetime value in ISO 8601 format. The timezone is always UTC. (YYYY-MM-DDThh:mm:ss.sssZ) Computed by AWS after `ship`.
</ResponseField>
