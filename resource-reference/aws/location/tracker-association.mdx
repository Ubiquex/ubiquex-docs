---
title: "aws_location_tracker_association"
description: "Real, generated bindings for `aws_location_tracker_association`."
---

`aws_location_tracker_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"

    cfg := location.TrackerAssociationConfig{
        ConsumerArn: "example",
        TrackerName: "example-tracker-name",
    }
    ubx.Resource(location.TrackerAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TrackerAssociation } from
      "jsr:@ubx/sdk-aws/aws/location/tracker_association";

    resource(TrackerAssociation, "example", {
      consumerArn: "example",
      trackerName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.location import (
        TrackerAssociation,
        TrackerAssociationConfig,
    )

    cfg = TrackerAssociationConfig(
        consumer_arn="example",
        tracker_name="example",
    )
    ubx.resource(TrackerAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_location_tracker_association with consumer_arn,
tracker_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="consumer_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tracker_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
