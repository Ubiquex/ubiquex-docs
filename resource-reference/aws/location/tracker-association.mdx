---
title: "aws_location_tracker_association"
description: "Real, generated bindings for `aws_location_tracker_association`."
---

`aws_location_tracker_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	location "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tracker association"})

    		ubx.Resource(location.TrackerAssociation, "ci-runner", location.TrackerAssociationConfig{
    			ConsumerArn: "arn:aws:iam::123456789012:policy/ci",
    			TrackerName: "example-tracker-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrackerAssociation } from "jsr:@ubx/sdk-aws/aws/location/tracker_association";

    export default stack("payments", () => {
      intent({ summary: "payments own tracker association" });

      resource(TrackerAssociation, "ci-runner", {
        consumerArn: "arn:aws:iam::123456789012:policy/ci",
        trackerName: "example-tracker-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.location import TrackerAssociation, TrackerAssociationConfig

    def describe():
        ubx.intent("payments own tracker association")

        ubx.resource(TrackerAssociation, "ci-runner", TrackerAssociationConfig(
            consumer_arn="arn:aws:iam::123456789012:policy/ci",
            tracker_name="example-tracker-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_location_tracker_association, with consumer arn
    arn:aws:iam::123456789012:policy/ci, tracker name
    example-tracker-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="consumer_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tracker_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
