---
title: "aws_location_tracker"
description: "Real, generated bindings for `aws_location_tracker`."
---

`aws_location_tracker` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"

    cfg := location.TrackerConfig{
        TrackerName: "example-tracker-name",
        Description: "example",
        KmsKeyId: "example",
    }
    ubx.Resource(location.Tracker, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Tracker } from "jsr:@ubx/sdk-aws/aws/location/tracker";

    resource(Tracker, "example", {
      trackerName: "example",
      description: "example",
      kmsKeyId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.location import Tracker, TrackerConfig

    cfg = TrackerConfig(
        tracker_name="example",
        description="example",
        kms_key_id="example",
    )
    ubx.resource(Tracker, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_location_tracker with tracker_name, description,
kms_key_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="position_filtering" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tracker_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tracker_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
