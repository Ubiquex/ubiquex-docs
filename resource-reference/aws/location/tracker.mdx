---
title: "aws_location_tracker"
description: "Real, generated bindings for `aws_location_tracker`."
---

`aws_location_tracker` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	location "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tracker"})

    		ubx.Resource(location.Tracker, "ci-runner", location.TrackerConfig{
    			TrackerName:       "example-tracker-name",
    			Description:       "Managed by ubx.",
    			KmsKeyId:          "example",
    			PositionFiltering: "example",
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Tracker } from "jsr:@ubx/sdk-aws/aws/location/tracker";

    export default stack("payments", () => {
      intent({ summary: "payments own tracker" });

      resource(Tracker, "ci-runner", {
        trackerName: "example-tracker-name",
        description: "Managed by ubx.",
        kmsKeyId: "example",
        positionFiltering: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.location import Tracker, TrackerConfig

    def describe():
        ubx.intent("payments own tracker")

        ubx.resource(Tracker, "ci-runner", TrackerConfig(
            tracker_name="example-tracker-name",
            description="Managed by ubx.",
            kms_key_id="example",
            position_filtering="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_location_tracker, with tracker name
    example-tracker-name, description Managed by ubx., kms key id
    example, position filtering example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="position_filtering" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tracker_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tracker_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
