---
title: "aws_location_tracker_consumer"
description: "`aws_location_tracker_consumer` links a real `aws_location_tracker` to an `aws_location_geofence_collection` as its consumer..."
---

`aws_location_tracker_consumer` links a real `aws_location_tracker` to an `aws_location_geofence_collection` as its consumer, the association that lets a tracker's incoming location updates be evaluated against that collection's geofences.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	location "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/location"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tracker consumer"})

    		ubx.Resource(location.TrackerConsumer, "ci-runner", location.TrackerConsumerConfig{
    			ConsumerArn: "arn:aws:iam::123456789012:policy/ci",
    			TrackerName: "example-tracker-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrackerConsumer } from "./local-sdk/aws/sdk/typescript/aws/location/tracker_consumer.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own tracker consumer" });

      resource(TrackerConsumer, "ci-runner", {
        consumerArn: "arn:aws:iam::123456789012:policy/ci",
        trackerName: "example-tracker-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.location import TrackerConsumer, TrackerConsumerConfig

    def describe():
        ubx.intent("payments own tracker consumer")

        ubx.resource(TrackerConsumer, "ci-runner", TrackerConsumerConfig(
            consumer_arn="arn:aws:iam::123456789012:policy/ci",
            tracker_name="example-tracker-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_location_tracker_consumer, with consumer arn
    arn:aws:iam::123456789012:policy/ci, tracker name
    example-tracker-name.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="consumer_arn" type="string" required>
  The Amazon Resource Name (ARN) of the consumer resource (such as an EventBridge rule) that will receive location updates from the tracker. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tracker_name" type="string" required>
  The name of the Amazon Location Service tracker that the consumer (such as a geofence collection) is associated with, linking the consumer to receive position updates from that tracker. Required. **(AI-inferred)**
</ResponseField>
