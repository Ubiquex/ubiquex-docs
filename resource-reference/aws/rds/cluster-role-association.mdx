---
title: "aws_rds_cluster_role_association"
description: "Real, generated bindings for `aws_rds_cluster_role_association`."
---

`aws_rds_cluster_role_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster role association"})

    		ubx.Resource(rds.ClusterRoleAssociation, "ci-runner", rds.ClusterRoleAssociationConfig{
    			DbClusterIdentifier: "example",
    			RoleArn:             "arn:aws:iam::123456789012:policy/ci",
    			FeatureName:         "example-feature-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterRoleAssociation } from "jsr:@ubx/sdk-aws/aws/rds/cluster_role_association";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster role association" });

      resource(ClusterRoleAssociation, "ci-runner", {
        dbClusterIdentifier: "example",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        featureName: "example-feature-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import ClusterRoleAssociation, ClusterRoleAssociationConfig

    def describe():
        ubx.intent("payments own cluster role association")

        ubx.resource(ClusterRoleAssociation, "ci-runner", ClusterRoleAssociationConfig(
            db_cluster_identifier="example",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            feature_name="example-feature-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_cluster_role_association, with db cluster
    identifier example, role arn arn:aws:iam::123456789012:policy/ci,
    feature name example-feature-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="db_cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="feature_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
