---
title: "aws_rds_export_task"
description: "Real, generated bindings for `aws_rds_export_task`."
---

`aws_rds_export_task` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rds"

    cfg := rds.ExportTaskConfig{
        ExportTaskIdentifier: "example",
        IamRoleArn: "example",
        KmsKeyId: "example",
        S3BucketName: "example-s3-bucket-name",
        SourceArn: "example",
        ExportOnly: []string{"example"},
        S3Prefix: "example",
    }
    ubx.Resource(rds.ExportTask, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ExportTask } from
      "jsr:@ubx/sdk-aws/aws/rds/export_task";

    resource(ExportTask, "example", {
      exportTaskIdentifier: "example",
      iamRoleArn: "example",
      kmsKeyId: "example",
      s3BucketName: "example",
      sourceArn: "example",
      exportOnly: ["example"],
      s3Prefix: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import ExportTask, ExportTaskConfig

    cfg = ExportTaskConfig(
        export_task_identifier="example",
        iam_role_arn="example",
        kms_key_id="example",
        s3_bucket_name="example",
        source_arn="example",
        export_only=["example"],
        s3_prefix="example",
    )
    ubx.resource(ExportTask, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_export_task with export_task_identifier,
iam_role_arn, kms_key_id, s3_bucket_name, source_arn,
export_only, s3_prefix.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="export_only" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="export_task_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="failure_cause" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="percent_progress" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="task_end_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="task_start_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="warning_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>
