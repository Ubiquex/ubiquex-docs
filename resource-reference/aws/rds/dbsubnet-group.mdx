---
title: "aws_rds_dbsubnet_group"
description: "`aws_rds_dbsubnet_group` defines a real set of VPC subnets that an `aws_rds_dbinstance` or `aws_rds_dbcluster` is allowed to place its network..."
---

`aws_rds_dbsubnet_group` defines a real set of VPC subnets that an `aws_rds_dbinstance` or `aws_rds_dbcluster` is allowed to place its network interfaces into.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dbsubnet group"})

    		ubx.Resource(rds.DbsubnetGroup, "example", rds.DbsubnetGroupConfig{
    			DbsubnetGroupDescription: "example",
    			SubnetIds:                []string{"example"},
    			DbsubnetGroupName:        "example-dbsubnet-group-name",
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbsubnetGroup } from "@ubx/sdk-aws/aws/rds/dbsubnet_group";

    export default stack("example", () => {
      intent({ summary: "example own dbsubnet group" });

      resource(DbsubnetGroup, "example", {
        dbsubnetGroupDescription: "example",
        subnetIds: ["example"],
        dbsubnetGroupName: "example-dbsubnet-group-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import DbsubnetGroup, DbsubnetGroupConfig
    from ubx.aws.rds.dbsubnet_group import DbsubnetGroup_Tags

    def describe():
        ubx.intent("example own dbsubnet group")

        ubx.resource(DbsubnetGroup, "example", DbsubnetGroupConfig(
            dbsubnet_group_description="example",
            subnet_ids=["example"],
            dbsubnet_group_name="example-dbsubnet-group-name",
            tags=[DbsubnetGroup_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_dbsubnet_group, with dbsubnet group description
    example, subnet ids a set value, dbsubnet group name
    example-dbsubnet-group-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dbsubnet_group_description" type="string" required>
  The description for the DB subnet group. Required.
</ResponseField>

<ResponseField name="dbsubnet_group_name" type="string">
  The name for the DB subnet group. This value is stored as a lowercase string. Constraints: + Must contain no more than 255 letters, numbers, periods, underscores, spaces, or hyphens. + Must not be default. + First character must be a letter. Example: ``mydbsubnetgroup`` Optional.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  The EC2 Subnet IDs for the DB subnet group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to assign to the DB subnet group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag key-value pair attached to the DB subnet group, used for metadata, cost allocation, and identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="dbsubnet_group_arn" type="string">
  The Amazon Resource Name (ARN) of the DB subnet group, assigned by AWS when the group is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
