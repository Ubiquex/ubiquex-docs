---
title: "aws_rds_instance_state"
description: "Real, generated bindings for `aws_rds_instance_state`."
---

`aws_rds_instance_state` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rds"

    cfg := rds.InstanceStateConfig{
        Identifier: "example",
        State: "example",
    }
    ubx.Resource(rds.InstanceState, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InstanceState } from
      "jsr:@ubx/sdk-aws/aws/rds/instance_state";

    resource(InstanceState, "example", {
      identifier: "example",
      state: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import InstanceState, InstanceStateConfig

    cfg = InstanceStateConfig(
        identifier="example",
        state="example",
    )
    ubx.resource(InstanceState, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_instance_state with identifier, state.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
