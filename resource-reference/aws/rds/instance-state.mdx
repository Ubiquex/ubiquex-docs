---
title: "aws_rds_instance_state"
description: "Real, generated bindings for `aws_rds_instance_state`."
---

`aws_rds_instance_state` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance state"})

    		ubx.Resource(rds.InstanceState, "ci-runner", rds.InstanceStateConfig{
    			Identifier: "example",
    			State:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceState } from "jsr:@ubx/sdk-aws/aws/rds/instance_state";

    export default stack("payments", () => {
      intent({ summary: "payments own instance state" });

      resource(InstanceState, "ci-runner", {
        identifier: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import InstanceState, InstanceStateConfig

    def describe():
        ubx.intent("payments own instance state")

        ubx.resource(InstanceState, "ci-runner", InstanceStateConfig(
            identifier="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_instance_state, with identifier example, state
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
