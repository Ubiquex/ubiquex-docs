---
title: "aws_rds_global_cluster"
description: "`aws_rds_global_cluster` creates a real multi-Region Aurora deployment..."
---

`aws_rds_global_cluster` creates a real multi-Region Aurora deployment, coordinating a primary `aws_rds_dbcluster` and up to five read-only secondary clusters in other Regions with fast replication and disaster-recovery failover.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own global cluster"})

    		ubx.Resource(rds.GlobalCluster, "example", rds.GlobalClusterConfig{
    			DeletionProtection:        true,
    			Engine:                    "example",
    			EngineLifecycleSupport:    "example",
    			EngineVersion:             "example",
    			GlobalClusterIdentifier:   "example",
    			SourceDbclusterIdentifier: "example",
    			StorageEncrypted:          true,
    			Tags:                      []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlobalCluster } from "@ubx/sdk-aws/aws/rds/global_cluster";

    export default stack("example", () => {
      intent({ summary: "example own global cluster" });

      resource(GlobalCluster, "example", {
        deletionProtection: true,
        engine: "example",
        engineLifecycleSupport: "example",
        engineVersion: "example",
        globalClusterIdentifier: "example",
        sourceDbclusterIdentifier: "example",
        storageEncrypted: true,
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import GlobalCluster, GlobalClusterConfig
    from ubx.aws.rds.global_cluster import GlobalCluster_Tags

    def describe():
        ubx.intent("example own global cluster")

        ubx.resource(GlobalCluster, "example", GlobalClusterConfig(
            deletion_protection=True,
            engine="example",
            engine_lifecycle_support="example",
            engine_version="example",
            global_cluster_identifier="example",
            source_dbcluster_identifier="example",
            storage_encrypted=True,
            tags=[GlobalCluster_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_global_cluster, with deletion protection true,
    engine example, engine lifecycle support example, engine version
    example, global cluster identifier example, source dbcluster
    identifier example, storage encrypted true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_protection" type="bool">
  The deletion protection setting for the new global database. The global database can't be deleted when deletion protection is enabled. Optional.
</ResponseField>

<ResponseField name="engine" type="string">
  The name of the database engine to be used for this DB cluster. Valid Values: aurora (for MySQL 5.6-compatible Aurora), aurora-mysql (for MySQL 5.7-compatible Aurora). If you specify the SourceDBClusterIdentifier property, don't specify this property. The value is inherited from the cluster. Optional.
</ResponseField>

<ResponseField name="engine_lifecycle_support" type="string">
  The life cycle type of the global cluster. You can use this setting to enroll your global cluster into Amazon RDS Extended Support. Optional.
</ResponseField>

<ResponseField name="engine_version" type="string">
  The version number of the database engine to use. If you specify the SourceDBClusterIdentifier property, don't specify this property. The value is inherited from the cluster. Optional.
</ResponseField>

<ResponseField name="global_cluster_identifier" type="string">
  The cluster identifier of the new global database cluster. This parameter is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="source_dbcluster_identifier" type="any">
  The Amazon Resource Name (ARN) to use as the primary cluster of the global database. This parameter is optional. This parameter is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="storage_encrypted" type="bool">
  The storage encryption setting for the new global database cluster. If you specify the SourceDBClusterIdentifier property, don't specify this property. The value is inherited from the cluster. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the RDS Global Cluster, used to organize and identify the cluster. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag key attached to the RDS Global Cluster, used to store custom metadata such as environment, owner, or cost center for the cluster. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="global_endpoint" type="object">
  The global endpoint of the RDS global cluster, containing the address and port used to connect to the primary (writer) instance for read/write operations. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The writer endpoint for the global database cluster. This endpoint always points to the writer DB instance in the current primary cluster. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
