---
title: "aws_rds_cluster_snapshot_copy"
description: "Real, generated bindings for `aws_rds_cluster_snapshot_copy`."
---

`aws_rds_cluster_snapshot_copy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"

    cfg := rds.ClusterSnapshotCopyConfig{
        SourceDbClusterSnapshotIdentifier: "example",
        TargetDbClusterSnapshotIdentifier: "example",
        CopyTags: true,
        DestinationRegion: "example",
    }
    ubx.Resource(rds.ClusterSnapshotCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ClusterSnapshotCopy } from
      "jsr:@ubx/sdk-aws/aws/rds/cluster_snapshot_copy";

    resource(ClusterSnapshotCopy, "example", {
      sourceDbClusterSnapshotIdentifier: "example",
      targetDbClusterSnapshotIdentifier: "example",
      copyTags: true,
      destinationRegion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import (
        ClusterSnapshotCopy,
        ClusterSnapshotCopyConfig,
    )

    cfg = ClusterSnapshotCopyConfig(
        source_db_cluster_snapshot_identifier="example",
        target_db_cluster_snapshot_identifier="example",
        copy_tags=True,
        destination_region="example",
    )
    ubx.resource(ClusterSnapshotCopy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_cluster_snapshot_copy with
source_db_cluster_snapshot_identifier,
target_db_cluster_snapshot_identifier, copy_tags,
destination_region.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="copy_tags" type="bool">
  Optional.
</ResponseField>

<ResponseField name="destination_region" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="presigned_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_accounts" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="source_db_cluster_snapshot_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_db_cluster_snapshot_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="allocated_storage" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_cluster_snapshot_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="license_model" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_encrypted" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
