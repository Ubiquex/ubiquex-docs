---
title: "aws_rds_cluster_activity_stream"
description: "Real, generated bindings for `aws_rds_cluster_activity_stream`."
---

`aws_rds_cluster_activity_stream` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rds"

    cfg := rds.ClusterActivityStreamConfig{
        KmsKeyId: "example",
        Mode: "example",
        ResourceArn: "example",
        EngineNativeAuditFieldsIncluded: true,
    }
    ubx.Resource(rds.ClusterActivityStream, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ClusterActivityStream } from
      "jsr:@ubx/sdk-aws/aws/rds/cluster_activity_stream";

    resource(ClusterActivityStream, "example", {
      kmsKeyId: "example",
      mode: "example",
      resourceArn: "example",
      engineNativeAuditFieldsIncluded: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import (
        ClusterActivityStream,
        ClusterActivityStreamConfig,
    )

    cfg = ClusterActivityStreamConfig(
        kms_key_id="example",
        mode="example",
        resource_arn="example",
        engine_native_audit_fields_included=True,
    )
    ubx.resource(ClusterActivityStream, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_cluster_activity_stream with kms_key_id, mode,
resource_arn, engine_native_audit_fields_included.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="engine_native_audit_fields_included" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kinesis_stream_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
