---
title: "aws_rds_custom_db_engine_version"
description: "Real, generated bindings for `aws_rds_custom_db_engine_version`."
---

`aws_rds_custom_db_engine_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom db engine version"})

    		ubx.Resource(rds.CustomDbEngineVersion, "ci-runner", rds.CustomDbEngineVersionConfig{
    			Engine:                                "example",
    			EngineVersion:                         "example",
    			DatabaseInstallationFilesS3BucketName: "example-database-installation-files-s3-bucket-name",
    			DatabaseInstallationFilesS3Prefix:     "example",
    			Description:                           "Managed by ubx.",
    			Filename:                              "example",
    			Manifest:                              "example",
    			ManifestHash:                          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomDbEngineVersion } from "jsr:@ubx/sdk-aws/aws/rds/custom_db_engine_version";

    export default stack("payments", () => {
      intent({ summary: "payments own custom db engine version" });

      resource(CustomDbEngineVersion, "ci-runner", {
        engine: "example",
        engineVersion: "example",
        databaseInstallationFilesS3BucketName:
          "example-database-installation-files-s3-bucket-name",
        databaseInstallationFilesS3Prefix: "example",
        description: "Managed by ubx.",
        filename: "example",
        manifest: "example",
        manifestHash: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import CustomDbEngineVersion, CustomDbEngineVersionConfig

    def describe():
        ubx.intent("payments own custom db engine version")

        ubx.resource(CustomDbEngineVersion, "ci-runner", CustomDbEngineVersionConfig(
            engine="example",
            engine_version="example",
            database_installation_files_s3_bucket_name="example-database-installation-files-s3-bucket-name",
            database_installation_files_s3_prefix="example",
            description="Managed by ubx.",
            filename="example",
            manifest="example",
            manifest_hash="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_custom_db_engine_version, with engine example,
    engine version example, database installation files s3 bucket
    name example-database-installation-files-s3-bucket-name, database
    installation files s3 prefix example, description Managed by
    ubx., filename example, manifest example, manifest hash example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="database_installation_files_s3_bucket_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="database_installation_files_s3_prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="engine" type="string" required>
  Required.
</ResponseField>

<ResponseField name="engine_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="filename" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="manifest" type="string">
  Optional.
</ResponseField>

<ResponseField name="manifest_hash" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_image_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_parameter_group_family" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="major_engine_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="manifest_computed" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
