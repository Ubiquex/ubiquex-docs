---
title: "aws_rds_cluster_endpoint"
description: "Real, generated bindings for `aws_rds_cluster_endpoint`."
---

`aws_rds_cluster_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster endpoint"})

    		ubx.Resource(rds.ClusterEndpoint, "ci-runner", rds.ClusterEndpointConfig{
    			ClusterEndpointIdentifier: "example",
    			ClusterIdentifier:         "example",
    			CustomEndpointType:        "example",
    			ExcludedMembers:           []string{"example"},
    			StaticMembers:             []string{"example"},
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterEndpoint } from "jsr:@ubx/sdk-aws/aws/rds/cluster_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster endpoint" });

      resource(ClusterEndpoint, "ci-runner", {
        clusterEndpointIdentifier: "example",
        clusterIdentifier: "example",
        customEndpointType: "example",
        excludedMembers: ["example"],
        staticMembers: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import ClusterEndpoint, ClusterEndpointConfig

    def describe():
        ubx.intent("payments own cluster endpoint")

        ubx.resource(ClusterEndpoint, "ci-runner", ClusterEndpointConfig(
            cluster_endpoint_identifier="example",
            cluster_identifier="example",
            custom_endpoint_type="example",
            excluded_members=["example"],
            static_members=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_cluster_endpoint, with cluster endpoint identifier
    example, cluster identifier example, custom endpoint type
    example, excluded members a set value, static members a set
    value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_endpoint_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_endpoint_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="excluded_members" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="static_members" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
