---
title: "aws_rds_certificate"
description: "Real, generated bindings for `aws_rds_certificate`."
---

`aws_rds_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"

    cfg := rds.CertificateConfig{
        CertificateIdentifier: "example",
    }
    ubx.Resource(rds.Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Certificate } from
      "jsr:@ubx/sdk-aws/aws/rds/certificate";

    resource(Certificate, "example", {
      certificateIdentifier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import Certificate, CertificateConfig

    cfg = CertificateConfig(
        certificate_identifier="example",
    )
    ubx.resource(Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_certificate with certificate_identifier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
