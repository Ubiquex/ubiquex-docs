---
title: "aws_rds_dbcluster_parameter_group"
description: "`aws_rds_dbcluster_parameter_group` defines a real set of cluster-level engine parameters that can be applied to an `aws_rds_dbcluster` instead of..."
---

`aws_rds_dbcluster_parameter_group` defines a real set of cluster-level engine parameters that can be applied to an `aws_rds_dbcluster` instead of hand-tuning the cluster individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dbcluster parameter group"})

    		ubx.Resource(rds.DbclusterParameterGroup, "example", rds.DbclusterParameterGroupConfig{
    			Description:                 "Managed by ubx.",
    			Family:                      "example",
    			Parameters:                  "example",
    			DbclusterParameterGroupName: "example-dbcluster-parameter-group-name",
    			Tags:                        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbclusterParameterGroup } from "@ubx/sdk-aws/aws/rds/dbcluster_parameter_group";

    export default stack("example", () => {
      intent({ summary: "example own dbcluster parameter group" });

      resource(DbclusterParameterGroup, "example", {
        description: "Managed by ubx.",
        family: "example",
        parameters: "example",
        dbclusterParameterGroupName: "example-dbcluster-parameter-group-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds.dbcluster_parameter_group import DbclusterParameterGroup, DbclusterParameterGroupConfig

    def describe():
        ubx.intent("example own dbcluster parameter group")

        ubx.resource(DbclusterParameterGroup, "example", DbclusterParameterGroupConfig(
            description="Managed by ubx.",
            family="example",
            parameters="example",
            dbcluster_parameter_group_name="example-dbcluster-parameter-group-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_dbcluster_parameter_group, with description
    Managed by ubx., family example, parameters example, dbcluster
    parameter group name example-dbcluster-parameter-group-name, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="dbcluster_parameter_group_name" type="string">
  The name of the DB cluster parameter group. Constraints: + Must not match the name of an existing DB cluster parameter group. This value is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  The description for the DB cluster parameter group. Required.
</ResponseField>

<ResponseField name="family" type="string" required>
  The DB cluster parameter group family name. A DB cluster parameter group can be associated with one and only one DB cluster parameter group family, and can be applied only to a DB cluster running a database engine and engine version compatible with that DB cluster parameter group family. *Aurora MySQL* Example: ``aurora-mysql5.7``, ``aurora-mysql8.0`` *Aurora PostgreSQL* Example: ``aurora-postgresql14`` *RDS for MySQL* Example: ``mysql8.0`` *RDS for PostgreSQL* Example: ``postgres13`` To list all of the available parameter group families for a DB engine, use the following command: ``aws rds describe-db-engine-versions --query "DBEngineVersions[].DBParameterGroupFamily" --engine &lt;engine&gt;`` For example, to list all of the available parameter group families for the Aurora PostgreSQL DB engine, use the following command: ``aws rds describe-db-engine-versions --query "DBEngineVersions[].DBParameterGroupFamily" --engine aurora-postgresql`` The output contains duplicates. The following are the valid DB engine values: + ``aurora-mysql`` + ``aurora-postgresql`` + ``mysql`` + ``postgres`` Required.
</ResponseField>

<ResponseField name="parameters" type="any" required>
  Provides a list of parameters for the DB cluster parameter group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to assign to the DB cluster parameter group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag applied to the DB cluster parameter group, used to organize and identify the resource within AWS cost allocation and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag applied to the DB cluster parameter group, used to categorize the resource for cost tracking, access control, and operational management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
