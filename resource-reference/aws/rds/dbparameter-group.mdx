---
title: "aws_rds_dbparameter_group"
description: "`aws_rds_dbparameter_group` defines a real set of instance-level engine parameters that can be applied to an `aws_rds_dbinstance` instead of..."
---

`aws_rds_dbparameter_group` defines a real set of instance-level engine parameters that can be applied to an `aws_rds_dbinstance` instead of hand-tuning the instance individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dbparameter group"})

    		ubx.Resource(rds.DbparameterGroup, "example", rds.DbparameterGroupConfig{
    			Description:          "Managed by ubx.",
    			Family:               "example",
    			DbparameterGroupName: "example-dbparameter-group-name",
    			Parameters:           "example",
    			Tags:                 []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbparameterGroup } from "@ubx/sdk-aws/aws/rds/dbparameter_group";

    export default stack("example", () => {
      intent({ summary: "example own dbparameter group" });

      resource(DbparameterGroup, "example", {
        description: "Managed by ubx.",
        family: "example",
        dbparameterGroupName: "example-dbparameter-group-name",
        parameters: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import DbparameterGroup, DbparameterGroupConfig
    from ubx.aws.rds.dbparameter_group import DbparameterGroup_Tags

    def describe():
        ubx.intent("example own dbparameter group")

        ubx.resource(DbparameterGroup, "example", DbparameterGroupConfig(
            description="Managed by ubx.",
            family="example",
            dbparameter_group_name="example-dbparameter-group-name",
            parameters="example",
            tags=[DbparameterGroup_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_dbparameter_group, with description Managed by
    ubx., family example, dbparameter group name
    example-dbparameter-group-name, parameters example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dbparameter_group_name" type="string">
  The name of the DB parameter group. Constraints: + Must be 1 to 255 letters, numbers, or hyphens. + First character must be a letter + Can't end with a hyphen or contain two consecutive hyphens If you don't specify a value for ``DBParameterGroupName`` property, a name is automatically created for the DB parameter group. This value is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  Provides the customer-specified description for this DB parameter group. Required.
</ResponseField>

<ResponseField name="family" type="string" required>
  The DB parameter group family name. A DB parameter group can be associated with one and only one DB parameter group family, and can be applied only to a DB instance running a database engine and engine version compatible with that DB parameter group family. To list all of the available parameter group families for a DB engine, use the following command: ``aws rds describe-db-engine-versions --query "DBEngineVersions[].DBParameterGroupFamily" --engine &lt;engine&gt;`` For example, to list all of the available parameter group families for the MySQL DB engine, use the following command: ``aws rds describe-db-engine-versions --query "DBEngineVersions[].DBParameterGroupFamily" --engine mysql`` The output contains duplicates. The following are the valid DB engine values: + ``aurora-mysql`` + ``aurora-postgresql`` + ``db2-ae`` + ``db2-ce`` + ``db2-se`` + ``mysql`` + ``oracle-ee`` + ``oracle-ee-cdb`` + ``oracle-se2`` + ``oracle-se2-cdb`` + ``postgres`` + ``sqlserver-ee`` + ``sqlserver-se`` + ``sqlserver-ex`` + ``sqlserver-web`` Required.
</ResponseField>

<ResponseField name="parameters" type="any">
  A mapping of parameter names and values for the parameter update. You must specify at least one parameter name and value. For more information about parameter groups, see [Working with parameter groups](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_WorkingWithParamGroups.html) in the *Amazon RDS User Guide*, or [Working with parameter groups](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/USER_WorkingWithParamGroups.html) in the *Amazon Aurora User Guide*. AWS CloudFormation doesn't support specifying an apply method for each individual parameter. The default apply method for each parameter is used. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to assign to the DB parameter group. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to attach to the DB parameter group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag assigned to the RDS DB parameter group, enabling custom metadata such as environment, owner, or cost center. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="dbparameter_group_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the DB parameter group in AWS, automatically assigned by RDS when the group is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
