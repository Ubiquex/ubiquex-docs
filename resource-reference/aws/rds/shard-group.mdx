---
title: "aws_rds_shard_group"
description: "Real, generated bindings for `aws_rds_shard_group`."
---

`aws_rds_shard_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rds"

    cfg := rds.ShardGroupConfig{
        DbClusterIdentifier: "example",
        DbShardGroupIdentifier: "example",
        MaxAcu: 1,
        ComputeRedundancy: 1,
        MinAcu: 1,
    }
    ubx.Resource(rds.ShardGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ShardGroup } from
      "jsr:@ubx/sdk-aws/aws/rds/shard_group";

    resource(ShardGroup, "example", {
      dbClusterIdentifier: "example",
      dbShardGroupIdentifier: "example",
      maxAcu: 1,
      computeRedundancy: 1,
      minAcu: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import ShardGroup, ShardGroupConfig

    cfg = ShardGroupConfig(
        db_cluster_identifier="example",
        db_shard_group_identifier="example",
        max_acu=1,
        compute_redundancy=1,
        min_acu=1,
    )
    ubx.resource(ShardGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_shard_group with db_cluster_identifier,
db_shard_group_identifier, max_acu, compute_redundancy, min_acu.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="compute_redundancy" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="db_cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="db_shard_group_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="max_acu" type="number" required>
  Required.
</ResponseField>

<ResponseField name="min_acu" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="publicly_accessible" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="compute_redundancy" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="db_shard_group_resource_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="min_acu" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="publicly_accessible" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
