---
title: "aws_rds_integration"
description: "Real, generated bindings for `aws_rds_integration`."
---

`aws_rds_integration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rds"

    cfg := rds.IntegrationConfig{
        IntegrationName: "example-integration-name",
        SourceArn: "example",
        TargetArn: "example",
        AdditionalEncryptionContext: map[string]string{"key": "value"},
        DataFilter: "example",
    }
    ubx.Resource(rds.Integration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Integration } from
      "jsr:@ubx/sdk-aws/aws/rds/integration";

    resource(Integration, "example", {
      integrationName: "example",
      sourceArn: "example",
      targetArn: "example",
      additionalEncryptionContext: { key: "value" },
      dataFilter: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rds import Integration, IntegrationConfig

    cfg = IntegrationConfig(
        integration_name="example",
        source_arn="example",
        target_arn="example",
        additional_encryption_context={"key": "value"},
        data_filter="example",
    )
    ubx.resource(Integration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rds_integration with integration_name, source_arn,
target_arn, additional_encryption_context, data_filter.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_encryption_context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="data_filter" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="integration_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_filter" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="integration_identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
