---
title: "aws_rds_reserved_instance"
description: "Real, generated bindings for `aws_rds_reserved_instance`."
---

`aws_rds_reserved_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own reserved instance"})

    		ubx.Resource(rds.ReservedInstance, "ci-runner", rds.ReservedInstanceConfig{
    			OfferingId:    "example",
    			InstanceCount: 1,
    			ReservationId: "example",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReservedInstance } from "jsr:@ubx/sdk-aws/aws/rds/reserved_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own reserved instance" });

      resource(ReservedInstance, "ci-runner", {
        offeringId: "example",
        instanceCount: 1,
        reservationId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import ReservedInstance, ReservedInstanceConfig

    def describe():
        ubx.intent("payments own reserved instance")

        ubx.resource(ReservedInstance, "ci-runner", ReservedInstanceConfig(
            offering_id="example",
            instance_count=1,
            reservation_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_reserved_instance, with offering id example,
    instance count 1, reservation id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_count" type="number">
  Optional.
</ResponseField>

<ResponseField name="offering_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reservation_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="currency_code" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_instance_class" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="duration" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="fixed_price" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lease_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="multi_az" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="offering_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="product_description" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="recurring_charges" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="recurring_charge_amount" type="number">
    </ResponseField>
    <ResponseField name="recurring_charge_frequency" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="usage_price" type="number">
  Computed by AWS after `ship`.
</ResponseField>
