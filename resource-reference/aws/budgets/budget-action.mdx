---
title: "aws_budgets_budget_action"
description: "Real, generated bindings for `aws_budgets_budget_action`."
---

`aws_budgets_budget_action` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/budgets"

    cfg := budgets.BudgetActionConfig{
        ActionType: "example",
        ApprovalModel: "example",
        BudgetName: "example-budget-name",
        ExecutionRoleArn: "example",
        NotificationType: "example",
        AccountId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(budgets.BudgetAction, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BudgetAction } from
      "jsr:@ubx/sdk-aws/aws/budgets/budget_action";

    resource(BudgetAction, "example", {
      actionType: "example",
      approvalModel: "example",
      budgetName: "example",
      executionRoleArn: "example",
      notificationType: "example",
      accountId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.budgets import BudgetAction, BudgetActionConfig

    cfg = BudgetActionConfig(
        action_type="example",
        approval_model="example",
        budget_name="example",
        execution_role_arn="example",
        notification_type="example",
        account_id="example",
        tags={"key": "value"},
    )
    ubx.resource(BudgetAction, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_budgets_budget_action with action_type,
approval_model, budget_name, execution_role_arn,
notification_type, account_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="action_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="approval_model" type="string" required>
  Required.
</ResponseField>

<ResponseField name="budget_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="notification_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="action_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
