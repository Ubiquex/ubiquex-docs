---
title: "aws_directory_service_radius_settings"
description: "Real, generated bindings for `aws_directory_service_radius_settings`."
---

`aws_directory_service_radius_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/directory"

    cfg := directory.ServiceRadiusSettingsConfig{
        AuthenticationProtocol: "example",
        DirectoryId: "example",
        DisplayLabel: "example",
        RadiusPort: 1,
        RadiusRetries: 1,
        RadiusServers: []string{"example"},
        RadiusTimeout: 1,
        SharedSecret: "example",
        UseSameUsername: true,
    }
    ubx.Resource(directory.ServiceRadiusSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceRadiusSettings } from
      "jsr:@ubx/sdk-aws/aws/directory/service_radius_settings";

    resource(ServiceRadiusSettings, "example", {
      authenticationProtocol: "example",
      directoryId: "example",
      displayLabel: "example",
      radiusPort: 1,
      radiusRetries: 1,
      radiusServers: ["example"],
      radiusTimeout: 1,
      sharedSecret: "example",
      useSameUsername: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import (
        ServiceRadiusSettings,
        ServiceRadiusSettingsConfig,
    )

    cfg = ServiceRadiusSettingsConfig(
        authentication_protocol="example",
        directory_id="example",
        display_label="example",
        radius_port=1,
        radius_retries=1,
        radius_servers=["example"],
        radius_timeout=1,
        shared_secret="example",
        use_same_username=True,
    )
    ubx.resource(ServiceRadiusSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_radius_settings with
authentication_protocol, directory_id, display_label,
radius_port, radius_retries, radius_servers, radius_timeout,
shared_secret, use_same_username.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication_protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="display_label" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="radius_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="radius_retries" type="number" required>
  Required.
</ResponseField>

<ResponseField name="radius_servers" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="radius_timeout" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="use_same_username" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
