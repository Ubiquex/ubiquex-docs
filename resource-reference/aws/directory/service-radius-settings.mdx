---
title: "aws_directory_service_radius_settings"
description: "Real, generated bindings for `aws_directory_service_radius_settings`."
---

`aws_directory_service_radius_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service radius settings"})

    		ubx.Resource(directory.ServiceRadiusSettings, "ci-runner", directory.ServiceRadiusSettingsConfig{
    			AuthenticationProtocol: "example",
    			DirectoryId:            "example",
    			DisplayLabel:           "example",
    			RadiusPort:             1,
    			RadiusRetries:          1,
    			RadiusServers:          []string{"example"},
    			RadiusTimeout:          1,
    			SharedSecret:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceRadiusSettings } from "jsr:@ubx/sdk-aws/aws/directory/service_radius_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own service radius settings" });

      resource(ServiceRadiusSettings, "ci-runner", {
        authenticationProtocol: "example",
        directoryId: "example",
        displayLabel: "example",
        radiusPort: 1,
        radiusRetries: 1,
        radiusServers: ["example"],
        radiusTimeout: 1,
        sharedSecret: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.directory import ServiceRadiusSettings, ServiceRadiusSettingsConfig

    def describe():
        ubx.intent("payments own service radius settings")

        ubx.resource(ServiceRadiusSettings, "ci-runner", ServiceRadiusSettingsConfig(
            authentication_protocol="example",
            directory_id="example",
            display_label="example",
            radius_port=1,
            radius_retries=1,
            radius_servers=["example"],
            radius_timeout=1,
            shared_secret="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_directory_service_radius_settings, with authentication
    protocol example, directory id example, display label example,
    radius port 1, radius retries 1, radius servers a set value,
    radius timeout 1, shared secret example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="authentication_protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="display_label" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="radius_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="radius_retries" type="number" required>
  Required.
</ResponseField>

<ResponseField name="radius_servers" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="radius_timeout" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="use_same_username" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
