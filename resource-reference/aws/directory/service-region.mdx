---
title: "aws_directory_service_region"
description: "Real, generated bindings for `aws_directory_service_region`."
---

`aws_directory_service_region` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service region"})

    		ubx.Resource(directory.ServiceRegion, "ci-runner", directory.ServiceRegionConfig{
    			DirectoryId: "example",
    			RegionName:  "example-region-name",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceRegion } from "jsr:@ubx/sdk-aws/aws/directory/service_region";

    export default stack("payments", () => {
      intent({ summary: "payments own service region" });

      resource(ServiceRegion, "ci-runner", {
        directoryId: "example",
        regionName: "example-region-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.directory import ServiceRegion, ServiceRegionConfig

    def describe():
        ubx.intent("payments own service region")

        ubx.resource(ServiceRegion, "ci-runner", ServiceRegionConfig(
            directory_id="example",
            region_name="example-region-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_directory_service_region, with directory id example,
    region name example-region-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="desired_number_of_domain_controllers" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="desired_number_of_domain_controllers" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
