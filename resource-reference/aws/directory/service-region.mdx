---
title: "aws_directory_service_region"
description: "Real, generated bindings for `aws_directory_service_region`."
---

`aws_directory_service_region` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/directory"

    cfg := directory.ServiceRegionConfig{
        DirectoryId: "example",
        RegionName: "example-region-name",
        DesiredNumberOfDomainControllers: 1,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(directory.ServiceRegion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceRegion } from
      "jsr:@ubx/sdk-aws/aws/directory/service_region";

    resource(ServiceRegion, "example", {
      directoryId: "example",
      regionName: "example",
      desiredNumberOfDomainControllers: 1,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import (
        ServiceRegion,
        ServiceRegionConfig,
    )

    cfg = ServiceRegionConfig(
        directory_id="example",
        region_name="example",
        desired_number_of_domain_controllers=1,
        tags={"key": "value"},
    )
    ubx.resource(ServiceRegion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_region with directory_id,
region_name, desired_number_of_domain_controllers, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="desired_number_of_domain_controllers" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="desired_number_of_domain_controllers" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
