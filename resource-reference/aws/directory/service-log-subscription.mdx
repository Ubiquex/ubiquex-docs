---
title: "aws_directory_service_log_subscription"
description: "Real, generated bindings for `aws_directory_service_log_subscription`."
---

`aws_directory_service_log_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"

    cfg := directory.ServiceLogSubscriptionConfig{
        DirectoryId: "example",
        LogGroupName: "example-log-group-name",
    }
    ubx.Resource(directory.ServiceLogSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceLogSubscription } from
      "jsr:@ubx/sdk-aws/aws/directory/service_log_subscription";

    resource(ServiceLogSubscription, "example", {
      directoryId: "example",
      logGroupName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import (
        ServiceLogSubscription,
        ServiceLogSubscriptionConfig,
    )

    cfg = ServiceLogSubscriptionConfig(
        directory_id="example",
        log_group_name="example",
    )
    ubx.resource(ServiceLogSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_log_subscription with
directory_id, log_group_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
