---
title: "aws_directory_service_shared_directory_accepter"
description: "Real, generated bindings for `aws_directory_service_shared_directory_accepter`."
---

`aws_directory_service_shared_directory_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service shared directory accepter"})

    		ubx.Resource(directory.ServiceSharedDirectoryAccepter, "ci-runner", directory.ServiceSharedDirectoryAccepterConfig{
    			SharedDirectoryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceSharedDirectoryAccepter } from "jsr:@ubx/sdk-aws/aws/directory/service_shared_directory_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own service shared directory accepter" });

      resource(ServiceSharedDirectoryAccepter, "ci-runner", {
        sharedDirectoryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.directory import ServiceSharedDirectoryAccepter, ServiceSharedDirectoryAccepterConfig

    def describe():
        ubx.intent("payments own service shared directory accepter")

        ubx.resource(ServiceSharedDirectoryAccepter, "ci-runner", ServiceSharedDirectoryAccepterConfig(
            shared_directory_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_directory_service_shared_directory_accepter, with
    shared directory id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_directory_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="method" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="notes" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_directory_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
