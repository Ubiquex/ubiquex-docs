---
title: "aws_directory_service_shared_directory_accepter"
description: "Real, generated bindings for `aws_directory_service_shared_directory_accepter`."
---

`aws_directory_service_shared_directory_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/directory"

    cfg := directory.ServiceSharedDirectoryAccepterConfig{
        SharedDirectoryId: "example",
    }
    ubx.Resource(
        directory.ServiceSharedDirectoryAccepter,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceSharedDirectoryAccepter } from
      "jsr:@ubx/sdk-aws/aws/directory/service_shared_directory_accepter";

    resource(ServiceSharedDirectoryAccepter, "example", {
      sharedDirectoryId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import (
        ServiceSharedDirectoryAccepter,
        ServiceSharedDirectoryAccepterConfig,
    )

    cfg = ServiceSharedDirectoryAccepterConfig(
        shared_directory_id="example",
    )
    ubx.resource(ServiceSharedDirectoryAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_shared_directory_accepter with
shared_directory_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_directory_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="method" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="notes" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_directory_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
