---
title: "aws_directory_service_conditional_forwarder"
description: "Real, generated bindings for `aws_directory_service_conditional_forwarder`."
---

`aws_directory_service_conditional_forwarder` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/directory"

    cfg := directory.ServiceConditionalForwarderConfig{
        DirectoryId: "example",
        DnsIps: []string{"example"},
        RemoteDomainName: "example-remote-domain-name",
    }
    ubx.Resource(
        directory.ServiceConditionalForwarder,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceConditionalForwarder } from
      "jsr:@ubx/sdk-aws/aws/directory/service_conditional_forwarder";

    resource(ServiceConditionalForwarder, "example", {
      directoryId: "example",
      dnsIps: ["example"],
      remoteDomainName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import (
        ServiceConditionalForwarder,
        ServiceConditionalForwarderConfig,
    )

    cfg = ServiceConditionalForwarderConfig(
        directory_id="example",
        dns_ips=["example"],
        remote_domain_name="example",
    )
    ubx.resource(ServiceConditionalForwarder, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_conditional_forwarder with
directory_id, dns_ips, remote_domain_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dns_ips" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="remote_domain_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
