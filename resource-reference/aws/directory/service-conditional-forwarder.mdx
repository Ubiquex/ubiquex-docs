---
title: "aws_directory_service_conditional_forwarder"
description: "Real, generated bindings for `aws_directory_service_conditional_forwarder`."
---

`aws_directory_service_conditional_forwarder` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service conditional forwarder"})

    		ubx.Resource(directory.ServiceConditionalForwarder, "ci-runner", directory.ServiceConditionalForwarderConfig{
    			DirectoryId:      "example",
    			DnsIps:           []string{"example"},
    			RemoteDomainName: "example-remote-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceConditionalForwarder } from "jsr:@ubx/sdk-aws/aws/directory/service_conditional_forwarder";

    export default stack("payments", () => {
      intent({ summary: "payments own service conditional forwarder" });

      resource(ServiceConditionalForwarder, "ci-runner", {
        directoryId: "example",
        dnsIps: ["example"],
        remoteDomainName: "example-remote-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.directory import ServiceConditionalForwarder, ServiceConditionalForwarderConfig

    def describe():
        ubx.intent("payments own service conditional forwarder")

        ubx.resource(ServiceConditionalForwarder, "ci-runner", ServiceConditionalForwarderConfig(
            directory_id="example",
            dns_ips=["example"],
            remote_domain_name="example-remote-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_directory_service_conditional_forwarder, with
    directory id example, dns ips a set value, remote domain name
    example-remote-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dns_ips" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="remote_domain_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
