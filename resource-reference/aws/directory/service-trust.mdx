---
title: "aws_directory_service_trust"
description: "Real, generated bindings for `aws_directory_service_trust`."
---

`aws_directory_service_trust` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/directory"

    cfg := directory.ServiceTrustConfig{
        DirectoryId: "example",
        RemoteDomainName: "example-remote-domain-name",
        TrustDirection: "example",
        TrustPassword: "example",
        ConditionalForwarderIpAddrs: []string{"example"},
        DeleteAssociatedConditionalForwarder: true,
    }
    ubx.Resource(directory.ServiceTrust, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceTrust } from
      "jsr:@ubx/sdk-aws/aws/directory/service_trust";

    resource(ServiceTrust, "example", {
      directoryId: "example",
      remoteDomainName: "example",
      trustDirection: "example",
      trustPassword: "example",
      conditionalForwarderIpAddrs: ["example"],
      deleteAssociatedConditionalForwarder: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import ServiceTrust, ServiceTrustConfig

    cfg = ServiceTrustConfig(
        directory_id="example",
        remote_domain_name="example",
        trust_direction="example",
        trust_password="example",
        conditional_forwarder_ip_addrs=["example"],
        delete_associated_conditional_forwarder=True,
    )
    ubx.resource(ServiceTrust, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_trust with directory_id,
remote_domain_name, trust_direction, trust_password,
conditional_forwarder_ip_addrs,
delete_associated_conditional_forwarder.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="conditional_forwarder_ip_addrs" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="delete_associated_conditional_forwarder" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="remote_domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="selective_auth" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="trust_direction" type="string" required>
  Required.
</ResponseField>

<ResponseField name="trust_password" type="string" required>
  Required.
</ResponseField>

<ResponseField name="trust_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="created_date_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="delete_associated_conditional_forwarder" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_date_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="selective_auth" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state_last_updated_date_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="trust_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="trust_state_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="trust_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
