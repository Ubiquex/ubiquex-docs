---
title: "aws_directory_service_shared_directory"
description: "Real, generated bindings for `aws_directory_service_shared_directory`."
---

`aws_directory_service_shared_directory` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service shared directory"})

    		ubx.Resource(directory.ServiceSharedDirectory, "ci-runner", directory.ServiceSharedDirectoryConfig{
    			DirectoryId: "example",
    			Method:      "example",
    			Notes:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceSharedDirectory } from "jsr:@ubx/sdk-aws/aws/directory/service_shared_directory";

    export default stack("payments", () => {
      intent({ summary: "payments own service shared directory" });

      resource(ServiceSharedDirectory, "ci-runner", {
        directoryId: "example",
        method: "example",
        notes: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.directory import ServiceSharedDirectory, ServiceSharedDirectoryConfig

    def describe():
        ubx.intent("payments own service shared directory")

        ubx.resource(ServiceSharedDirectory, "ci-runner", ServiceSharedDirectoryConfig(
            directory_id="example",
            method="example",
            notes="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_directory_service_shared_directory, with directory id
    example, method example, notes example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="method" type="string">
  Optional.
</ResponseField>

<ResponseField name="notes" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_directory_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
