---
title: "aws_directory_service_shared_directory"
description: "Real, generated bindings for `aws_directory_service_shared_directory`."
---

`aws_directory_service_shared_directory` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"

    cfg := directory.ServiceSharedDirectoryConfig{
        DirectoryId: "example",
        Method: "example",
        Notes: "example",
    }
    ubx.Resource(directory.ServiceSharedDirectory, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceSharedDirectory } from
      "jsr:@ubx/sdk-aws/aws/directory/service_shared_directory";

    resource(ServiceSharedDirectory, "example", {
      directoryId: "example",
      method: "example",
      notes: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.directory import (
        ServiceSharedDirectory,
        ServiceSharedDirectoryConfig,
    )

    cfg = ServiceSharedDirectoryConfig(
        directory_id="example",
        method="example",
        notes="example",
    )
    ubx.resource(ServiceSharedDirectory, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_directory_service_shared_directory with
directory_id, method, notes.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="method" type="string">
  Optional.
</ResponseField>

<ResponseField name="notes" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_directory_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
