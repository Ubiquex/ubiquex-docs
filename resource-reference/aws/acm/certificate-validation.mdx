---
title: "aws_acm_certificate_validation"
description: "Real, generated bindings for `aws_acm_certificate_validation`."
---

`aws_acm_certificate_validation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/acm"

    cfg := acm.CertificateValidationConfig{
        CertificateArn: "example",
        ValidationRecordFqdns: []string{"example"},
    }
    ubx.Resource(acm.CertificateValidation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CertificateValidation } from
      "jsr:@ubx/sdk-aws/aws/acm/certificate_validation";

    resource(CertificateValidation, "example", {
      certificateArn: "example",
      validationRecordFqdns: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.acm import (
        CertificateValidation,
        CertificateValidationConfig,
    )

    cfg = CertificateValidationConfig(
        certificate_arn="example",
        validation_record_fqdns=["example"],
    )
    ubx.resource(CertificateValidation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_acm_certificate_validation with certificate_arn,
validation_record_fqdns.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="validation_record_fqdns" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
