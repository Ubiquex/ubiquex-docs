---
title: "aws_acm_certificate_validation"
description: "Real, generated bindings for `aws_acm_certificate_validation`."
---

`aws_acm_certificate_validation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/acm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own certificate validation"})

    		ubx.Resource(acm.CertificateValidation, "ci-runner", acm.CertificateValidationConfig{
    			CertificateArn:        "arn:aws:iam::123456789012:policy/ci",
    			ValidationRecordFqdns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CertificateValidation } from "jsr:@ubx/sdk-aws/aws/acm/certificate_validation";

    export default stack("payments", () => {
      intent({ summary: "payments own certificate validation" });

      resource(CertificateValidation, "ci-runner", {
        certificateArn: "arn:aws:iam::123456789012:policy/ci",
        validationRecordFqdns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.acm import CertificateValidation, CertificateValidationConfig

    def describe():
        ubx.intent("payments own certificate validation")

        ubx.resource(CertificateValidation, "ci-runner", CertificateValidationConfig(
            certificate_arn="arn:aws:iam::123456789012:policy/ci",
            validation_record_fqdns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_acm_certificate_validation, with certificate arn
    arn:aws:iam::123456789012:policy/ci, validation record fqdns a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="validation_record_fqdns" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
