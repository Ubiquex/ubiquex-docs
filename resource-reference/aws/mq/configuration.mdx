---
title: "aws_mq_configuration"
description: "Real, generated bindings for `aws_mq_configuration`."
---

`aws_mq_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mq "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/mq"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own configuration"})

    		ubx.Resource(mq.Configuration, "ci-runner", mq.ConfigurationConfig{
    			Data:          "example",
    			EngineType:    "example",
    			EngineVersion: "example",
    			Name:          "ci-runner",
    			Description:   "Managed by ubx.",
    			SkipDestroy:   true,
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Configuration } from "jsr:@ubx/sdk-aws/aws/mq/configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own configuration" });

      resource(Configuration, "ci-runner", {
        data: "example",
        engineType: "example",
        engineVersion: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        skipDestroy: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.mq import Configuration, ConfigurationConfig

    def describe():
        ubx.intent("payments own configuration")

        ubx.resource(Configuration, "ci-runner", ConfigurationConfig(
            data="example",
            engine_type="example",
            engine_version="example",
            name="ci-runner",
            description="Managed by ubx.",
            skip_destroy=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_mq_configuration called "ci-runner", with data
    example, engine type example, engine version example, description
    Managed by ubx., skip destroy true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authentication_strategy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="engine_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="engine_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="authentication_strategy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="latest_revision" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
