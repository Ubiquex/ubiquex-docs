---
title: "aws_resource_groups_group"
description: "`aws_resource_groups_group` creates a real named collection of AWS resources, defined by a tag-based or CloudFormation-stack-based query..."
---

`aws_resource_groups_group` creates a real named collection of AWS resources, defined by a tag-based or CloudFormation-stack-based query, so they can be viewed, tagged, and acted on together instead of individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	resource "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resource"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own groups group"})

    		ubx.Resource(resource.GroupsGroup, "example", resource.GroupsGroupConfig{
    			Name:          "example",
    			Configuration: []map[string]any{{"parameters": []map[string]any{{"name": "example-name"}}}},
    			Description:   "Managed by ubx.",
    			ResourceQuery: map[string]any{"query": map[string]any{"resource_type_filters": []string{"example"}}},
    			Resources:     []string{"example"},
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupsGroup } from "./local-sdk/aws/sdk/typescript/aws/resource/groups_group.ts";

    export default stack("example", () => {
      intent({ summary: "example own groups group" });

      resource(GroupsGroup, "example", {
        name: "example",
        configuration: [{ parameters: [{ name: "example-name" }] }],
        description: "Managed by ubx.",
        resourceQuery: { query: { resourceTypeFilters: ["example"] } },
        resources: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.resource import GroupsGroup, GroupsGroupConfig

    def describe():
        ubx.intent("example own groups group")

        ubx.resource(GroupsGroup, "example", GroupsGroupConfig(
            name="example",
            configuration=[{"parameters": [{"name": "example-name"}]}],
            description="Managed by ubx.",
            resource_query={"query": {"resource_type_filters": ["example"]}},
            resources=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resource_groups_group called "example", with
    configuration a set value, description Managed by ubx., resource
    query a set value, resources a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration" type="list(object)">
  Specifies the configuration items that define the resource group, including the type of query (e.g., TagBased or CloudFormationStackBased) and associated parameters. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="parameters" type="list(object)">
      A list of parameter objects (each with a name and values) that configure a configuration item of the resource group, such as specifying the tag key and value for a tag-based group membership query. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="name" type="string">
          The parameter name (key) in a configuration item for an AWS Resource Group, which identifies what the corresponding values apply to (for example, a tag key or resource-specific filter used by the configuration type). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          The list of string values assigned to a parameter key in a resource group configuration item, such as the resource types or query criteria that define the group's membership. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      The resource type that the query targets, such as 'AWS::EC2::Instance', used to select which resources are included in the resource group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the resource group Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the resource group Required.
</ResponseField>

<ResponseField name="resource_query" type="object">
  Specifies the resource query that defines the membership of the group, including the query type (such as TAG_FILTERS) and the underlying query constraints (e.g., resource type filters and tag filters). Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="query" type="object">
      The query string that defines the resource selection criteria for the group, expressed using the AWS Resource Groups query syntax (either a JSON object of tag filters and resource type filters, or a CloudFormation stack identifier). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="resource_type_filters" type="list(string)">
          Limits the resources returned by the resource query to only those whose AWS resource types match one of the specified values (e.g., AWS::EC2::Instance), thereby narrowing the set of resources included in the group. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stack_identifier" type="string">
          This field specifies the Amazon Resource Name (ARN) of the CloudFormation stack whose resources are included in the resource group when the resource query type is CLOUDFORMATION_STACK. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tag_filters" type="list(object)">
          Defines the list of tag filters (each containing a tag key and optional set of tag values) that the resource query uses to determine which AWS resources are included in the resource group. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="key" type="string">
              The tag key used to filter resources when defining the group's resource query. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="values" type="list(string)">
              The list of tag values that, combined with the tag filter's key, determine which resources are included in the resource group's query results. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the type of resource query used to define the group's membership, such as TAG_FILTERS_1_0 for tag-based grouping or CLOUDFORMATION_STACK_1_0 for grouping by CloudFormation stack. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resources" type="list(string)">
  Specifies the Amazon Resource Names (ARNs) of the AWS resources to include in this resource group. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies the metadata tags to attach to the resource group, which are key-value pairs used for cost allocation, access control, and identifying the group. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the key portion of a key-value tag that can be applied to the AWS Resource Groups group for identification and cost tracking. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Resource Group ARN. Computed by AWS after `ship`.
</ResponseField>
