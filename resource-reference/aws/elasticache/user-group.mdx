---
title: "aws_elasticache_user_group"
description: "Real, generated bindings for `aws_elasticache_user_group`."
---

`aws_elasticache_user_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user group"})

    		ubx.Resource(elasticache.UserGroup, "ci-runner", elasticache.UserGroupConfig{
    			Engine:      "example",
    			UserGroupId: "example",
    			Tags:        map[string]string{"managed-by": "ubx"},
    			UserIds:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserGroup } from "jsr:@ubx/sdk-aws/aws/elasticache/user_group";

    export default stack("payments", () => {
      intent({ summary: "payments own user group" });

      resource(UserGroup, "ci-runner", {
        engine: "example",
        userGroupId: "example",
        tags: { "managed-by": "ubx" },
        userIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elasticache import UserGroup, UserGroupConfig

    def describe():
        ubx.intent("payments own user group")

        ubx.resource(UserGroup, "ci-runner", UserGroupConfig(
            engine="example",
            user_group_id="example",
            tags={"managed-by": "ubx"},
            user_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elasticache_user_group, with engine example, user
    group id example, tags a set value, user ids a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="engine" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_ids" type="set(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
