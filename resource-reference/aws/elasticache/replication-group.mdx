---
title: "aws_elasti_cache_replication_group"
description: "`aws_elasti_cache_replication_group` creates a real Redis OSS or Valkey deployment with primary and replica nodes and automatic failover..."
---

`aws_elasti_cache_replication_group` creates a real Redis OSS or Valkey deployment with primary and replica nodes and automatic failover, ElastiCache's high-availability alternative to a single-node `aws_elasti_cache_cache_cluster`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticache "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/elasticache"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own replication group"})

    		ubx.Resource(elasticache.ReplicationGroup, "example", elasticache.ReplicationGroupConfig{
    			ReplicationGroupDescription: "example",
    			AtRestEncryptionEnabled:     true,
    			AuthToken:                   "example",
    			AutoMinorVersionUpgrade:     true,
    			AutomaticFailoverEnabled:    true,
    			CacheNodeType:               "example",
    			CacheParameterGroupName:     "example-cache-parameter-group-name",
    			CacheSecurityGroupNames:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReplicationGroup } from "@ubx/sdk-aws/aws/elasticache/replication_group";

    export default stack("example", () => {
      intent({ summary: "example own replication group" });

      resource(ReplicationGroup, "example", {
        replicationGroupDescription: "example",
        atRestEncryptionEnabled: true,
        authToken: "example",
        autoMinorVersionUpgrade: true,
        automaticFailoverEnabled: true,
        cacheNodeType: "example",
        cacheParameterGroupName: "example-cache-parameter-group-name",
        cacheSecurityGroupNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elasticache import ReplicationGroup, ReplicationGroupConfig

    def describe():
        ubx.intent("example own replication group")

        ubx.resource(ReplicationGroup, "example", ReplicationGroupConfig(
            replication_group_description="example",
            at_rest_encryption_enabled=True,
            auth_token="example",
            auto_minor_version_upgrade=True,
            automatic_failover_enabled=True,
            cache_node_type="example",
            cache_parameter_group_name="example-cache-parameter-group-name",
            cache_security_group_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elasti_cache_replication_group, with replication group
    description example, at rest encryption enabled true, auth token
    example, auto minor version upgrade true, automatic failover
    enabled true, cache node type example, cache parameter group name
    example-cache-parameter-group-name, cache security group names a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="at_rest_encryption_enabled" type="bool">
  A flag that enables encryption at rest when set to true.AtRestEncryptionEnabled after the replication group is created. To enable encryption at rest on a replication group you must set AtRestEncryptionEnabled to true when you create the replication group. Optional.
</ResponseField>

<ResponseField name="auth_token" type="string">
  Reserved parameter. The password used to access a password protected server.AuthToken can be specified only on replication groups where TransitEncryptionEnabled is true. For more information. Optional.
</ResponseField>

<ResponseField name="auto_minor_version_upgrade" type="bool">
  This parameter is currently disabled. Optional.
</ResponseField>

<ResponseField name="automatic_failover_enabled" type="bool">
  Specifies whether a read-only replica is automatically promoted to read/write primary if the existing primary fails. AutomaticFailoverEnabled must be enabled for Redis (cluster mode enabled) replication groups. Optional.
</ResponseField>

<ResponseField name="cache_node_type" type="string">
  The compute and memory capacity of the nodes in the node group (shard). Optional.
</ResponseField>

<ResponseField name="cache_parameter_group_name" type="string">
  The name of the parameter group to associate with this replication group. If this argument is omitted, the default cache parameter group for the specified engine is used. Optional.
</ResponseField>

<ResponseField name="cache_security_group_names" type="list(string)">
  A list of cache security group names to associate with this replication group. Optional.
</ResponseField>

<ResponseField name="cache_subnet_group_name" type="string">
  The name of the cache subnet group to be used for the replication group. Optional.
</ResponseField>

<ResponseField name="cluster_mode" type="string">
  Enabled or Disabled. To modify cluster mode from Disabled to Enabled, you must first set the cluster mode to Compatible. Compatible mode allows your Redis OSS clients to connect using both cluster mode enabled and cluster mode disabled. After you migrate all Redis OSS clients to use cluster mode enabled, you can then complete cluster mode configuration and set the cluster mode to Enabled. For more information, see Modify cluster mode. Optional.
</ResponseField>

<ResponseField name="data_tiering_enabled" type="bool">
  Enables data tiering. Data tiering is only supported for replication groups using the r6gd node type. This parameter must be set to true when using r6gd nodes. Optional.
</ResponseField>

<ResponseField name="durability" type="string">
  The durability setting for the replication group. Valid values: default, async, sync, disabled. Enabling durability on an existing non-durable cluster or disabling durability on an existing durable cluster is not currently supported and will result in an error; specify the desired durability at create time. The resolved state is returned in EffectiveDurability. Optional.
</ResponseField>

<ResponseField name="engine" type="string">
  The name of the cache engine to be used for the clusters in this replication group. Optional.
</ResponseField>

<ResponseField name="engine_version" type="string">
  The version number of the cache engine to be used for the clusters in this replication group. To view the supported cache engine versions, use the DescribeCacheEngineVersions operation. Optional.
</ResponseField>

<ResponseField name="global_replication_group_id" type="string">
  The name of the Global datastore Optional.
</ResponseField>

<ResponseField name="ip_discovery" type="string">
  The network type you choose when creating a replication group, either ipv4 | ipv6. IPv6 is supported for workloads using Redis OSS engine version 6.2 onward or Memcached engine version 1.6.6 on all instances built on the Nitro system. Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The ID of the KMS key used to encrypt the disk on the cluster. Optional.
</ResponseField>

<ResponseField name="log_delivery_configurations" type="list(object)">
  Specifies the destination, format and type of the logs. Optional.
  <Expandable title="properties">
    <ResponseField name="destination_details" type="object">
      Specifies the concrete destination (CloudWatch Logs log group ARN or Kinesis Data Firehose delivery stream ARN) for a log delivery configuration of the ElastiCache replication group, selected based on the destination type. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cloud_watch_logs_details" type="object">
          Specifies the Amazon CloudWatch Logs destination for delivering replication group logs, including the CloudWatch Logs log group name to which logs are sent. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="log_group" type="string">
              The name of the CloudWatch Logs log group where ElastiCache delivers the replication group's engine or slow logs when log delivery is configured to CloudWatch Logs. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="kinesis_firehose_details" type="object">
          Specifies the Kinesis Data Firehose delivery stream that receives the detailed logs of the ElastiCache replication group, identified by its delivery stream name. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="delivery_stream" type="string">
              The name of the Amazon Kinesis Data Firehose delivery stream to which the replication group's logs are delivered for this log delivery configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="destination_type" type="string">
      The type of destination for ElastiCache replication group logs, which must be either 'cloudwatch-logs' or 'kinesis-firehose'. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="log_format" type="string">
      For each log delivery configuration on the ElastiCache replication group, this specifies whether the delivered log (for Redis slow logs or engine logs) is in 'text' or 'json' format. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="log_type" type="string">
      Determines which type of ElastiCache log (engine-log or slow-log) is delivered to the configured destination for the replication group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="multi_azenabled" type="bool">
  A flag indicating if you have Multi-AZ enabled to enhance fault tolerance. For more information, see Minimizing Downtime: Multi-AZ. Optional.
</ResponseField>

<ResponseField name="network_type" type="string">
  Must be either ipv4 | ipv6 | dual_stack. IPv6 is supported for workloads using Redis OSS engine version 6.2 onward or Memcached engine version 1.6.6 on all instances built on the Nitro system Optional.
</ResponseField>

<ResponseField name="node_group_configuration" type="list(object)">
  NodeGroupConfiguration is a property of the AWS::ElastiCache::ReplicationGroup resource that configures an Amazon ElastiCache (ElastiCache) Redis cluster node group. Optional.
  <Expandable title="properties">
    <ResponseField name="node_group_id" type="string">
      Assigns a unique zero-padded numeric identifier (e.g., '0001') to each node group in a cluster-mode replication group so that multiple node groups can be individually configured. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="primary_availability_zone" type="string">
      Specifies the Availability Zone for the primary node of a specific node group (shard) in a cluster-mode-enabled ElastiCache replication group, allowing per-shard placement control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="replica_availability_zones" type="list(string)">
      Specifies the list of Availability Zones (AZs) for the replica nodes in this node group (shard), one per replica, so you can control the physical placement of read replicas across AZs for high availability. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="replica_count" type="number">
      Specifies the number of replica nodes that should be created in each node group (shard) of the replication group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="slots" type="string">
      Assigns a contiguous range of hash slots (e.g., '0-4999') to the node group, determining which part of the keyspace the shard manages in a cluster-mode replication group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="notification_topic_arn" type="string">
  The Amazon Resource Name (ARN) of the Amazon Simple Notification Service (SNS) topic to which notifications are sent. Optional.
</ResponseField>

<ResponseField name="num_cache_clusters" type="number">
  The number of clusters this replication group initially has.This parameter is not used if there is more than one node group (shard). You should use ReplicasPerNodeGroup instead. Optional.
</ResponseField>

<ResponseField name="num_node_groups" type="number">
  An optional parameter that specifies the number of node groups (shards) for this Redis (cluster mode enabled) replication group. For Redis (cluster mode disabled) either omit this parameter or set it to 1. Optional.
</ResponseField>

<ResponseField name="port" type="number">
  The port number on which each member of the replication group accepts connections. Optional.
</ResponseField>

<ResponseField name="preferred_cache_cluster_azs" type="list(string)">
  A list of EC2 Availability Zones in which the replication group's clusters are created. The order of the Availability Zones in the list is the order in which clusters are allocated. The primary cluster is created in the first AZ in the list. This parameter is not used if there is more than one node group (shard). You should use NodeGroupConfiguration instead. Optional.
</ResponseField>

<ResponseField name="preferred_maintenance_window" type="string">
  Specifies the weekly time range during which maintenance on the cluster is performed. It is specified as a range in the format ddd:hh24:mi-ddd:hh24:mi (24H Clock UTC). The minimum maintenance window is a 60 minute period. Optional.
</ResponseField>

<ResponseField name="primary_cluster_id" type="string">
  The identifier of the cluster that serves as the primary for this replication group. This cluster must already exist and have a status of available. Optional.
</ResponseField>

<ResponseField name="replicas_per_node_group" type="number">
  An optional parameter that specifies the number of replica nodes in each node group (shard). Valid values are 0 to 5. **Note:** Using ReplicasPerNodeGroup with NodeGroupConfiguration results in resource replacement. For online scaling, use ReplicasPerNodeGroup alone. Optional.
</ResponseField>

<ResponseField name="replication_group_description" type="string" required>
  A user-created description for the replication group. Required.
</ResponseField>

<ResponseField name="replication_group_id" type="string">
  The replication group identifier. This parameter is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  One or more Amazon VPC security groups associated with this replication group. Optional.
</ResponseField>

<ResponseField name="snapshot_arns" type="list(string)">
  A list of Amazon Resource Names (ARN) that uniquely identify the Redis RDB snapshot files stored in Amazon S3. Optional.
</ResponseField>

<ResponseField name="snapshot_name" type="string">
  The name of a snapshot from which to restore data into the new replication group. The snapshot status changes to restoring while the new replication group is being created. Optional.
</ResponseField>

<ResponseField name="snapshot_retention_limit" type="number">
  The number of days for which ElastiCache retains automatic snapshots before deleting them. For example, if you set SnapshotRetentionLimit to 5, a snapshot that was taken today is retained for 5 days before being deleted. Optional.
</ResponseField>

<ResponseField name="snapshot_window" type="string">
  The daily time range (in UTC) during which ElastiCache begins taking a daily snapshot of your node group (shard). Optional.
</ResponseField>

<ResponseField name="snapshotting_cluster_id" type="string">
  The cluster ID that is used as the daily snapshot source for the replication group. This parameter cannot be set for Redis (cluster mode enabled) replication groups. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of cost allocation tags to be added to this resource. Tags are comma-separated key,value pairs (e.g. Key=myKey, Value=myKeyValue. You can include multiple tags as shown following: Key=myKey, Value=myKeyValue Key=mySecondKey, Value=mySecondKeyValue. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag applied to the ElastiCache replication group, which can be used to organize, track, or control access to the resource through IAM policies and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transit_encryption_enabled" type="bool">
  A flag that enables in-transit encryption when set to true. Optional.
</ResponseField>

<ResponseField name="transit_encryption_mode" type="string">
  A setting that allows you to migrate your clients to use in-transit encryption, with no downtime. When setting TransitEncryptionEnabled to true, you can set your TransitEncryptionMode to preferred in the same request, to allow both encrypted and unencrypted connections at the same time. Once you migrate all your Redis OSS clients to use encrypted connections you can modify the value to required to allow encrypted connections only. Setting TransitEncryptionMode to required is a two-step process that requires you to first set the TransitEncryptionMode to preferred, after that you can set TransitEncryptionMode to required. This process will not trigger the replacement of the replication group. Optional.
</ResponseField>

<ResponseField name="user_group_ids" type="list(string)">
  The ID of user group to associate with the replication group. Optional.
</ResponseField>

## Output properties

<ResponseField name="configuration_end_point" type="object">
  The configuration endpoint of the replication group, providing the DNS address and port for client connections. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The DNS hostname of the cache node. Optional.
    </ResponseField>
    <ResponseField name="port" type="string">
      The port number that the cache engine is listening on. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_durability" type="string">
  The resolved durability state of the replication group after resolving the default value. This is a read-only property. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="primary_end_point" type="object">
  The endpoint (hostname and port) of the primary node in the replication group, computed after creation and used by applications to connect for read/write operations. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The DNS hostname of the cache node. Optional.
    </ResponseField>
    <ResponseField name="port" type="string">
      The port number that the cache engine is listening on. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="read_end_point" type="object">
  The read endpoint of the replication group, a DNS address and port used for read-only connections that are distributed across the replica nodes in the cluster. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="addresses" type="string">
      A string containing a comma-separated list of endpoints for the primary and read-only replicas, formatted as [address1, address2, ...]. The order of the addresses maps to the order of the ports from the ReadEndPoint.Ports attribute. Optional.
    </ResponseField>
    <ResponseField name="addresses_list" type="list(string)">
      A list of endpoints for the read-only replicas. The order of the addresses maps to the order of the ports from the ReadEndPoint.Ports attribute. Optional.
    </ResponseField>
    <ResponseField name="ports" type="string">
      A string containing a comma-separated list of ports for the read-only replicas, formatted as [port1, port2, ...]. The order of the ports maps to the order of the addresses from the ReadEndPoint.Addresses attribute. Optional.
    </ResponseField>
    <ResponseField name="ports_list" type="list(string)">
      A list of ports for the read-only replicas. The order of the ports maps to the order of the addresses from the ReadEndPoint.Addresses attribute. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reader_end_point" type="object">
  The read-only endpoint (DNS address and port) automatically assigned by ElastiCache that points to the replication group's read replicas, used for distributing read traffic. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="address" type="string">
      The DNS hostname of the cache node. Optional.
    </ResponseField>
    <ResponseField name="port" type="string">
      The port number that the cache engine is listening on. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
