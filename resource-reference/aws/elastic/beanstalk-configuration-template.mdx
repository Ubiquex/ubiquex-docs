---
title: "aws_elastic_beanstalk_configuration_template"
description: "Real, generated bindings for `aws_elastic_beanstalk_configuration_template`."
---

`aws_elastic_beanstalk_configuration_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own beanstalk configuration template"})

    		ubx.Resource(elastic.BeanstalkConfigurationTemplate, "ci-runner", elastic.BeanstalkConfigurationTemplateConfig{
    			Application:       "example",
    			Name:              "ci-runner",
    			Description:       "Managed by ubx.",
    			EnvironmentId:     "example",
    			SolutionStackName: "example-solution-stack-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BeanstalkConfigurationTemplate } from "jsr:@ubx/sdk-aws/aws/elastic/beanstalk_configuration_template";

    export default stack("payments", () => {
      intent({ summary: "payments own beanstalk configuration template" });

      resource(BeanstalkConfigurationTemplate, "ci-runner", {
        application: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        environmentId: "example",
        solutionStackName: "example-solution-stack-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elastic import BeanstalkConfigurationTemplate, BeanstalkConfigurationTemplateConfig

    def describe():
        ubx.intent("payments own beanstalk configuration template")

        ubx.resource(BeanstalkConfigurationTemplate, "ci-runner", BeanstalkConfigurationTemplateConfig(
            application="example",
            name="ci-runner",
            description="Managed by ubx.",
            environment_id="example",
            solution_stack_name="example-solution-stack-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastic_beanstalk_configuration_template called
    "ci-runner", with application example, description Managed by
    ubx., environment id example, solution stack name
    example-solution-stack-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="environment_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="solution_stack_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
