---
title: "aws_elastic_beanstalk_configuration_template"
description: "`aws_elastic_beanstalk_configuration_template` is a real, reusable set of environment configuration options for an `aws_elastic_beanstalk_application`..."
---

`aws_elastic_beanstalk_configuration_template` is a real, reusable set of environment configuration options for an `aws_elastic_beanstalk_application`, instance type, scaling settings, environment variables, that an `aws_elastic_beanstalk_environment` can be created from rather than every environment being configured from scratch.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elasticbeanstalk "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/elasticbeanstalk"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own configuration template"})

    		ubx.Resource(elasticbeanstalk.ConfigurationTemplate, "example", elasticbeanstalk.ConfigurationTemplateConfig{
    			ApplicationName:     "example-application-name",
    			Description:         "Managed by ubx.",
    			EnvironmentId:       "example",
    			OptionSettings:      []map[string]any{{"namespace": "example"}},
    			PlatformArn:         "arn:aws:iam::123456789012:policy/example",
    			SolutionStackName:   "example-solution-stack-name",
    			SourceConfiguration: map[string]any{"application_name": "example-application-name", "template_name": "example-template-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationTemplate } from "@ubx/sdk-aws/aws/elasticbeanstalk/configuration_template";

    export default stack("example", () => {
      intent({ summary: "example own configuration template" });

      resource(ConfigurationTemplate, "example", {
        applicationName: "example-application-name",
        description: "Managed by ubx.",
        environmentId: "example",
        optionSettings: [{ namespace: "example" }],
        platformArn: "arn:aws:iam::123456789012:policy/example",
        solutionStackName: "example-solution-stack-name",
        sourceConfiguration: {
          applicationName: "example-application-name",
          templateName: "example-template-name",
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elasticbeanstalk import ConfigurationTemplate, ConfigurationTemplateConfig
    from ubx.aws.elasticbeanstalk.configuration_template import ConfigurationTemplate_OptionSettings, ConfigurationTemplate_SourceConfiguration

    def describe():
        ubx.intent("example own configuration template")

        ubx.resource(ConfigurationTemplate, "example", ConfigurationTemplateConfig(
            application_name="example-application-name",
            description="Managed by ubx.",
            environment_id="example",
            option_settings=[ConfigurationTemplate_OptionSettings(namespace="example")],
            platform_arn="arn:aws:iam::123456789012:policy/example",
            solution_stack_name="example-solution-stack-name",
            source_configuration=ConfigurationTemplate_SourceConfiguration(application_name="example-application-name", template_name="example-template-name"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastic_beanstalk_configuration_template, with
    application name example-application-name, description Managed by
    ubx., environment id example, option settings a set value,
    platform arn arn:aws:iam::123456789012:policy/example, solution
    stack name example-solution-stack-name, source configuration a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_name" type="string" required>
  The name of the Elastic Beanstalk application to associate with this configuration template. Required.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description for this configuration. Optional.
</ResponseField>

<ResponseField name="environment_id" type="string">
  The ID of an environment whose settings you want to use to create the configuration template. You must specify EnvironmentId if you don't specify PlatformArn, SolutionStackName, or SourceConfiguration. Optional.
</ResponseField>

<ResponseField name="option_settings" type="list(object)">
  Option values for the Elastic Beanstalk configuration, such as the instance type. If specified, these values override the values obtained from the solution stack or the source configuration template. For a complete list of Elastic Beanstalk configuration options, see [Option Values](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/command-options.html) in the AWS Elastic Beanstalk Developer Guide. Optional.
  <Expandable title="properties">
    <ResponseField name="namespace" type="string">
      The namespace that identifies the AWS service and configuration section (e.g., 'aws:autoscaling:asg') to which the option setting applies in the Elastic Beanstalk configuration template. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="option_name" type="string">
      The name of the configuration option being set within its namespace, identifying the specific Elastic Beanstalk setting (e.g., 'InstanceType' in the 'aws:autoscaling:launchconfiguration' namespace). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="resource_name" type="string">
      A unique name identifying the specific AWS resource (such as an Auto Scaling group or load balancer) within the Elastic Beanstalk environment that the configuration option applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value assigned to the Elastic Beanstalk configuration option identified by the sibling OptionName within the specified Namespace. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="platform_arn" type="string">
  The Amazon Resource Name (ARN) of the custom platform. For more information, see [Custom Platforms](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/custom-platforms.html) in the AWS Elastic Beanstalk Developer Guide. Optional.
</ResponseField>

<ResponseField name="solution_stack_name" type="string">
  The name of an Elastic Beanstalk solution stack (platform version) that this configuration uses. For example, 64bit Amazon Linux 2013.09 running Tomcat 7 Java 7. A solution stack specifies the operating system, runtime, and application server for a configuration template. It also determines the set of configuration options as well as the possible and default values. For more information, see [Supported Platforms](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/concepts.platforms.html) in the AWS Elastic Beanstalk Developer Guide. You must specify SolutionStackName if you don't specify PlatformArn, EnvironmentId, or SourceConfiguration. Use the ListAvailableSolutionStacks API to obtain a list of available solution stacks. Optional.
</ResponseField>

<ResponseField name="source_configuration" type="object">
  Specifies an existing Elastic Beanstalk configuration template (identified by application name and template name) whose settings are copied to create the new configuration template. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="application_name" type="string" required>
      The name of the application associated with the configuration. Required.
    </ResponseField>
    <ResponseField name="template_name" type="string" required>
      The name of the configuration template. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="template_name" type="string">
  The name of the configuration template Computed by AWS after `ship`.
</ResponseField>
