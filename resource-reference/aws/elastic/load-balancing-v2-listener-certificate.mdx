---
title: "aws_elastic_load_balancing_v2_listener_certificate"
description: "`aws_elastic_load_balancing_v2_listener_certificate` attaches an additional real TLS certificate to an `aws_elastic_load_balancing_v2_listener` beyond..."
---

`aws_elastic_load_balancing_v2_listener_certificate` attaches an additional real TLS certificate to an `aws_elastic_load_balancing_v2_listener` beyond its own default certificate, needed when a listener must serve multiple domain names with SNI.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own load balancing v2 listener certificate"})

    		ubx.Resource(elastic.LoadBalancingV2ListenerCertificate, "example", elastic.LoadBalancingV2ListenerCertificateConfig{
    			Certificates: []map[string]any{{"certificate_arn": "example"}},
    			ListenerArn:  "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoadBalancingV2ListenerCertificate } from "./local-sdk/aws/sdk/typescript/aws/elastic/load_balancing_v2_listener_certificate.ts";

    export default stack("example", () => {
      intent({ summary: "example own load balancing v2 listener certificate" });

      resource(LoadBalancingV2ListenerCertificate, "example", {
        certificates: [{ certificateArn: "example" }],
        listenerArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.elastic import LoadBalancingV2ListenerCertificate, LoadBalancingV2ListenerCertificateConfig

    def describe():
        ubx.intent("example own load balancing v2 listener certificate")

        ubx.resource(LoadBalancingV2ListenerCertificate, "example", LoadBalancingV2ListenerCertificateConfig(
            certificates=[{"certificate_arn": "example"}],
            listener_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastic_load_balancing_v2_listener_certificate, with
    certificates a set value, listener arn
    arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificates" type="list(object)" required>
  Specifies the list of ACM certificate ARNs to attach to the listener, allowing the load balancer to present these certificates during TLS handshakes. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="certificate_arn" type="string">
      The ARN of the certificate (from AWS Certificate Manager or IAM) to associate with the listener for TLS/SSL termination. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="listener_arn" type="string" required>
  Specifies the ARN of the Application Load Balancer or Network Load Balancer listener to which the certificate is associated. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier for the listener certificate, composed of the listener ARN and certificate ARN. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
