---
title: "aws_elastic_beanstalk_application"
description: "`aws_elastic_beanstalk_application` is the real, top-level Elastic Beanstalk container an `aws_elastic_beanstalk_application_version` and..."
---

`aws_elastic_beanstalk_application` is the real, top-level Elastic Beanstalk container an `aws_elastic_beanstalk_application_version` and `aws_elastic_beanstalk_environment` both belong to, the logical grouping for every deployed version of one caller application.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own beanstalk application"})

    		ubx.Resource(elastic.BeanstalkApplication, "example", elastic.BeanstalkApplicationConfig{
    			ApplicationName:         "example-application-name",
    			Description:             "Managed by ubx.",
    			ResourceLifecycleConfig: map[string]any{"service_role": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BeanstalkApplication } from "./local-sdk/aws/sdk/typescript/aws/elastic/beanstalk_application.ts";

    export default stack("example", () => {
      intent({ summary: "example own beanstalk application" });

      resource(BeanstalkApplication, "example", {
        applicationName: "example-application-name",
        description: "Managed by ubx.",
        resourceLifecycleConfig: { serviceRole: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.elastic import BeanstalkApplication, BeanstalkApplicationConfig

    def describe():
        ubx.intent("example own beanstalk application")

        ubx.resource(BeanstalkApplication, "example", BeanstalkApplicationConfig(
            application_name="example-application-name",
            description="Managed by ubx.",
            resource_lifecycle_config={"service_role": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastic_beanstalk_application, with application name
    example-application-name, description Managed by ubx., resource
    lifecycle config a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="application_name" type="string">
  A name for the Elastic Beanstalk application. If you don't specify a name, AWS CloudFormation generates a unique physical ID and uses that ID for the application name. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Your description of the application. Optional.
</ResponseField>

<ResponseField name="resource_lifecycle_config" type="object">
  Defines how Elastic Beanstalk manages the lifecycle of application versions, including whether to delete expired versions, how long to retain them, and the service role used for cleanup. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="service_role" type="string">
      The ARN of an IAM service role that Elastic Beanstalk has permission to assume. The ServiceRole property is required the first time that you provide a ResourceLifecycleConfig for the application. After you provide it once, Elastic Beanstalk persists the Service Role with the application, and you don't need to specify it again. You can, however, specify it in subsequent updates to change the Service Role to another value. Optional.
    </ResponseField>
    <ResponseField name="version_lifecycle_config" type="object">
      Defines the lifecycle policy for application versions, including optional max count and max age rules that control how many versions are retained and how old they may become before deletion. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_age_rule" type="object">
          This object defines the rule that automatically deletes application versions older than a specified number of days, optionally deleting their source bundles from S3, for the Elastic Beanstalk application version lifecycle configuration. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="delete_source_from_s3" type="bool">
              Set to true to delete a version's source bundle from Amazon S3 when Elastic Beanstalk deletes the application version. Optional.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Specify true to apply the rule, or false to disable it. Optional.
            </ResponseField>
            <ResponseField name="max_age_in_days" type="number">
              Specify the number of days to retain an application versions. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="max_count_rule" type="object">
          Specifies the max-count-based lifecycle rule for Elastic Beanstalk application versions, including whether the rule is enabled, the maximum number of versions to keep (MaxCount), and whether to delete the source bundle from S3. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="delete_source_from_s3" type="bool">
              Set to true to delete a version's source bundle from Amazon S3 when Elastic Beanstalk deletes the application version. Optional.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Specify true to apply the rule, or false to disable it. Optional.
            </ResponseField>
            <ResponseField name="max_count" type="number">
              Specify the maximum number of application versions to retain. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
