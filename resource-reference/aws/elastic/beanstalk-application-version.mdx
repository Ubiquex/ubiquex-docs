---
title: "aws_elastic_beanstalk_application_version"
description: "Real, generated bindings for `aws_elastic_beanstalk_application_version`."
---

`aws_elastic_beanstalk_application_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own beanstalk application version"})

    		ubx.Resource(elastic.BeanstalkApplicationVersion, "ci-runner", elastic.BeanstalkApplicationVersionConfig{
    			Application: "example",
    			Bucket:      "example",
    			Key:         "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			ForceDelete: true,
    			Process:     true,
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BeanstalkApplicationVersion } from "jsr:@ubx/sdk-aws/aws/elastic/beanstalk_application_version";

    export default stack("payments", () => {
      intent({ summary: "payments own beanstalk application version" });

      resource(BeanstalkApplicationVersion, "ci-runner", {
        application: "example",
        bucket: "example",
        key: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        forceDelete: true,
        process: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.elastic import BeanstalkApplicationVersion, BeanstalkApplicationVersionConfig

    def describe():
        ubx.intent("payments own beanstalk application version")

        ubx.resource(BeanstalkApplicationVersion, "ci-runner", BeanstalkApplicationVersionConfig(
            application="example",
            bucket="example",
            key="example",
            name="ci-runner",
            description="Managed by ubx.",
            force_delete=True,
            process=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastic_beanstalk_application_version called
    "ci-runner", with application example, bucket example, key
    example, description Managed by ubx., force delete true, process
    true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application" type="string" required>
  Required.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="force_delete" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="process" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
