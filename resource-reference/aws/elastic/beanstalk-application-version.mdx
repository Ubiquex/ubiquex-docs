---
title: "aws_elastic_beanstalk_application_version"
description: "`aws_elastic_beanstalk_application_version` is one real, specific, immutable build of an `aws_elastic_beanstalk_application`'s own code, stored in S3..."
---

`aws_elastic_beanstalk_application_version` is one real, specific, immutable build of an `aws_elastic_beanstalk_application`'s own code, stored in S3, that an `aws_elastic_beanstalk_environment` actually deploys.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elastic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elastic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own beanstalk application version"})

    		ubx.Resource(elastic.BeanstalkApplicationVersion, "ci-runner", elastic.BeanstalkApplicationVersionConfig{
    			ApplicationName: "example-application-name",
    			SourceBundle:    map[string]any{"s3_bucket": "example"},
    			Description:     "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BeanstalkApplicationVersion } from "./local-sdk/aws/sdk/typescript/aws/elastic/beanstalk_application_version.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own beanstalk application version" });

      resource(BeanstalkApplicationVersion, "ci-runner", {
        applicationName: "example-application-name",
        sourceBundle: { s3Bucket: "example" },
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.elastic import BeanstalkApplicationVersion, BeanstalkApplicationVersionConfig

    def describe():
        ubx.intent("payments own beanstalk application version")

        ubx.resource(BeanstalkApplicationVersion, "ci-runner", BeanstalkApplicationVersionConfig(
            application_name="example-application-name",
            source_bundle={"s3_bucket": "example"},
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elastic_beanstalk_application_version, with
    application name example-application-name, source bundle a set
    value, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_name" type="string" required>
  The name of the Elastic Beanstalk application that is associated with this application version. Required.
</ResponseField>

<ResponseField name="description" type="string">
  A description of this application version. Optional.
</ResponseField>

<ResponseField name="source_bundle" type="object" required>
  The source_bundle specifies the Amazon S3 bucket and key where the application version source bundle is located. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="s3_bucket" type="string" required>
      The Amazon S3 bucket where the data is located. Required.
    </ResponseField>
    <ResponseField name="s3_key" type="string" required>
      The Amazon S3 key where the data is located. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
