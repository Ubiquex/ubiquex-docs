---
title: "aws_codeguruprofiler_profiling_group"
description: "Real, generated bindings for `aws_codeguruprofiler_profiling_group`."
---

`aws_codeguruprofiler_profiling_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/codeguruprofiler"

    cfg := codeguruprofiler.ProfilingGroupConfig{
        Name: "example-name",
        ComputePlatform: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(codeguruprofiler.ProfilingGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProfilingGroup } from
      "jsr:@ubx/sdk-aws/aws/codeguruprofiler/profiling_group";

    resource(ProfilingGroup, "example", {
      name: "example",
      computePlatform: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codeguruprofiler import (
        ProfilingGroup,
        ProfilingGroupConfig,
    )

    cfg = ProfilingGroupConfig(
        name="example",
        compute_platform="example",
        tags={"key": "value"},
    )
    ubx.resource(ProfilingGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codeguruprofiler_profiling_group with name,
compute_platform, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="compute_platform" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="compute_platform" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
