---
title: "aws_savingsplans_savings_plan"
description: "Real, generated bindings for `aws_savingsplans_savings_plan`."
---

`aws_savingsplans_savings_plan` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	savingsplans "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/savingsplans"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own savings plan"})

    		ubx.Resource(savingsplans.SavingsPlan, "ci-runner", savingsplans.SavingsPlanConfig{
    			Commitment:            "example",
    			SavingsPlanOfferingId: "example",
    			PurchaseTime:          "example",
    			Tags:                  map[string]string{"managed-by": "ubx"},
    			UpfrontPaymentAmount:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SavingsPlan } from "jsr:@ubx/sdk-aws/aws/savingsplans/savings_plan";

    export default stack("payments", () => {
      intent({ summary: "payments own savings plan" });

      resource(SavingsPlan, "ci-runner", {
        commitment: "example",
        savingsPlanOfferingId: "example",
        purchaseTime: "example",
        tags: { "managed-by": "ubx" },
        upfrontPaymentAmount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.savingsplans import SavingsPlan, SavingsPlanConfig

    def describe():
        ubx.intent("payments own savings plan")

        ubx.resource(SavingsPlan, "ci-runner", SavingsPlanConfig(
            commitment="example",
            savings_plan_offering_id="example",
            purchase_time="example",
            tags={"managed-by": "ubx"},
            upfront_payment_amount="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_savingsplans_savings_plan, with commitment example,
    savings plan offering id example, purchase time example, tags a
    set value, upfront payment amount example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="commitment" type="string" required>
  Required.
</ResponseField>

<ResponseField name="purchase_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="savings_plan_offering_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="upfront_payment_amount" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="currency" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ec2_instance_family" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="end" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="offering_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="payment_option" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="product_types" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="recurring_payment_amount" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="returnable_until" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="savings_plan_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="savings_plan_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="savings_plan_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="start" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="term_duration_in_seconds" type="number">
  Computed by AWS after `ship`.
</ResponseField>
