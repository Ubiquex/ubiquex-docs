---
title: "aws_global_accelerator_listener"
description: "`aws_global_accelerator_listener` checks for real connection requests on a specific port and protocol against an `aws_global_accelerator_accelerator`..."
---

`aws_global_accelerator_listener` checks for real connection requests on a specific port and protocol against an `aws_global_accelerator_accelerator`, the entry point `aws_global_accelerator_endpoint_group`s route matching traffic from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own accelerator listener"})

    		ubx.Resource(globalaccelerator.Listener, "example", globalaccelerator.ListenerConfig{
    			AcceleratorArn: "arn:aws:iam::123456789012:policy/example",
    			PortRanges:     []map[string]any{{"from_port": 1}},
    			Protocol:       "example",
    			ClientAffinity: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Listener } from "@ubx/sdk-aws/aws/globalaccelerator/listener";

    export default stack("example", () => {
      intent({ summary: "example own accelerator listener" });

      resource(Listener, "example", {
        acceleratorArn: "arn:aws:iam::123456789012:policy/example",
        portRanges: [{ fromPort: 1 }],
        protocol: "example",
        clientAffinity: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.globalaccelerator.listener import Listener, ListenerConfig

    def describe():
        ubx.intent("example own accelerator listener")

        ubx.resource(Listener, "example", ListenerConfig(
            accelerator_arn="arn:aws:iam::123456789012:policy/example",
            port_ranges=[{"from_port": 1}],
            protocol="example",
            client_affinity="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_global_accelerator_listener, with accelerator arn
    arn:aws:iam::123456789012:policy/example, port ranges a set
    value, protocol example, client affinity example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accelerator_arn" type="string" required>
  The Amazon Resource Name (ARN) of the accelerator. Required.
</ResponseField>

<ResponseField name="client_affinity" type="string">
  Client affinity lets you direct all requests from a user to the same endpoint. Optional.
</ResponseField>

<ResponseField name="port_ranges" type="list(object)" required>
  Defines the list of port ranges (each with a `from` and `to` port) that the listener forwards traffic on. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="from_port" type="number">
      The first port in the inclusive range of ports (from_port to to_port) that the AWS Global Accelerator listener accepts and forwards traffic on. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="to_port" type="number">
      The inclusive ending port number of the range for the listener. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="protocol" type="string" required>
  The protocol for the listener. Required.
</ResponseField>

## Output properties

<ResponseField name="listener_arn" type="string">
  The Amazon Resource Name (ARN) of the listener. Computed by AWS after `ship`.
</ResponseField>
