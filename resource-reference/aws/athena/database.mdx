---
title: "aws_athena_database"
description: "Real, generated bindings for `aws_athena_database`."
---

`aws_athena_database` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database"})

    		ubx.Resource(athena.Database, "ci-runner", athena.DatabaseConfig{
    			Name:                "ci-runner",
    			Bucket:              "example",
    			Comment:             "example",
    			ExpectedBucketOwner: "example",
    			ForceDestroy:        true,
    			Properties:          map[string]string{"managed-by": "ubx"},
    			Workgroup:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Database } from "jsr:@ubx/sdk-aws/aws/athena/database";

    export default stack("payments", () => {
      intent({ summary: "payments own database" });

      resource(Database, "ci-runner", {
        name: "ci-runner",
        bucket: "example",
        comment: "example",
        expectedBucketOwner: "example",
        forceDestroy: true,
        properties: { "managed-by": "ubx" },
        workgroup: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.athena import Database, DatabaseConfig

    def describe():
        ubx.intent("payments own database")

        ubx.resource(Database, "ci-runner", DatabaseConfig(
            name="ci-runner",
            bucket="example",
            comment="example",
            expected_bucket_owner="example",
            force_destroy=True,
            properties={"managed-by": "ubx"},
            workgroup="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_athena_database called "ci-runner", with bucket
    example, comment example, expected bucket owner example, force
    destroy true, properties a set value, workgroup example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string">
  Optional.
</ResponseField>

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="properties" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workgroup" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
