---
title: "aws_athena_database"
description: "Real, generated bindings for `aws_athena_database`."
---

`aws_athena_database` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/athena"

    cfg := athena.DatabaseConfig{
        Name: "example-name",
        Bucket: "example",
        Comment: "example",
    }
    ubx.Resource(athena.Database, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Database } from "jsr:@ubx/sdk-aws/aws/athena/database";

    resource(Database, "example", {
      name: "example",
      bucket: "example",
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.athena import Database, DatabaseConfig

    cfg = DatabaseConfig(
        name="example",
        bucket="example",
        comment="example",
    )
    ubx.resource(Database, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_athena_database with name, bucket, comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bucket" type="string">
  Optional.
</ResponseField>

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="expected_bucket_owner" type="string">
  Optional.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="properties" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workgroup" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
