---
title: "aws_athena_prepared_statement"
description: "Real, generated bindings for `aws_athena_prepared_statement`."
---

`aws_athena_prepared_statement` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own prepared statement"})

    		ubx.Resource(athena.PreparedStatement, "ci-runner", athena.PreparedStatementConfig{
    			Name:           "ci-runner",
    			QueryStatement: "example",
    			Workgroup:      "example",
    			Description:    "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PreparedStatement } from "jsr:@ubx/sdk-aws/aws/athena/prepared_statement";

    export default stack("payments", () => {
      intent({ summary: "payments own prepared statement" });

      resource(PreparedStatement, "ci-runner", {
        name: "ci-runner",
        queryStatement: "example",
        workgroup: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.athena import PreparedStatement, PreparedStatementConfig

    def describe():
        ubx.intent("payments own prepared statement")

        ubx.resource(PreparedStatement, "ci-runner", PreparedStatementConfig(
            name="ci-runner",
            query_statement="example",
            workgroup="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_athena_prepared_statement called "ci-runner", with
    query statement example, workgroup example, description Managed
    by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query_statement" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workgroup" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
