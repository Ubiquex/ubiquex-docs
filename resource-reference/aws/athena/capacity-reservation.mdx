---
title: "aws_athena_capacity_reservation"
description: "Real, generated bindings for `aws_athena_capacity_reservation`."
---

`aws_athena_capacity_reservation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/athena"

    cfg := athena.CapacityReservationConfig{
        Name: "example-name",
        TargetDpus: 1,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(athena.CapacityReservation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CapacityReservation } from
      "jsr:@ubx/sdk-aws/aws/athena/capacity_reservation";

    resource(CapacityReservation, "example", {
      name: "example",
      targetDpus: 1,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.athena import (
        CapacityReservation,
        CapacityReservationConfig,
    )

    cfg = CapacityReservationConfig(
        name="example",
        target_dpus=1,
        tags={"key": "value"},
    )
    ubx.resource(CapacityReservation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_athena_capacity_reservation with name, target_dpus,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_dpus" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="allocated_dpus" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
