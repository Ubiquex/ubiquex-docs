---
title: "aws_athena_capacity_reservation"
description: "Real, generated bindings for `aws_athena_capacity_reservation`."
---

`aws_athena_capacity_reservation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own capacity reservation"})

    		ubx.Resource(athena.CapacityReservation, "ci-runner", athena.CapacityReservationConfig{
    			Name:       "ci-runner",
    			TargetDpus: 1,
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CapacityReservation } from "jsr:@ubx/sdk-aws/aws/athena/capacity_reservation";

    export default stack("payments", () => {
      intent({ summary: "payments own capacity reservation" });

      resource(CapacityReservation, "ci-runner", {
        name: "ci-runner",
        targetDpus: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.athena import CapacityReservation, CapacityReservationConfig

    def describe():
        ubx.intent("payments own capacity reservation")

        ubx.resource(CapacityReservation, "ci-runner", CapacityReservationConfig(
            name="ci-runner",
            target_dpus=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_athena_capacity_reservation called "ci-runner", with
    target dpus 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_dpus" type="number" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="allocated_dpus" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
