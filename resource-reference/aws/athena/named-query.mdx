---
title: "aws_athena_named_query"
description: "Real, generated bindings for `aws_athena_named_query`."
---

`aws_athena_named_query` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	athena "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/athena"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own named query"})

    		ubx.Resource(athena.NamedQuery, "ci-runner", athena.NamedQueryConfig{
    			Database:    "example",
    			Name:        "ci-runner",
    			Query:       "example",
    			Description: "Managed by ubx.",
    			Workgroup:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NamedQuery } from "jsr:@ubx/sdk-aws/aws/athena/named_query";

    export default stack("payments", () => {
      intent({ summary: "payments own named query" });

      resource(NamedQuery, "ci-runner", {
        database: "example",
        name: "ci-runner",
        query: "example",
        description: "Managed by ubx.",
        workgroup: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.athena import NamedQuery, NamedQueryConfig

    def describe():
        ubx.intent("payments own named query")

        ubx.resource(NamedQuery, "ci-runner", NamedQueryConfig(
            database="example",
            name="ci-runner",
            query="example",
            description="Managed by ubx.",
            workgroup="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_athena_named_query called "ci-runner", with database
    example, query example, description Managed by ubx., workgroup
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="database" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workgroup" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
