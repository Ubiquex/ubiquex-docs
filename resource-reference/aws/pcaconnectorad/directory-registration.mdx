---
title: "aws_pcaconnector_ad_directory_registration"
description: "`aws_pcaconnector_ad_directory_registration` registers a real Active Directory with AWS Private CA Connector for AD..."
---

`aws_pcaconnector_ad_directory_registration` registers a real Active Directory with AWS Private CA Connector for AD, the prerequisite step an `aws_pcaconnector_ad_connector` for that directory depends on.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pcaconnectorad "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/pcaconnectorad"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own directory registration"})

    		ubx.Resource(pcaconnectorad.DirectoryRegistration, "example", pcaconnectorad.DirectoryRegistrationConfig{
    			DirectoryId: "example",
    			Tags:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryRegistration } from "@ubx/sdk-aws/aws/pcaconnectorad/directory_registration";

    export default stack("example", () => {
      intent({ summary: "example own directory registration" });

      resource(DirectoryRegistration, "example", {
        directoryId: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pcaconnectorad import DirectoryRegistration, DirectoryRegistrationConfig

    def describe():
        ubx.intent("example own directory registration")

        ubx.resource(DirectoryRegistration, "example", DirectoryRegistrationConfig(
            directory_id="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pcaconnector_ad_directory_registration, with directory
    id example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="directory_id" type="string" required>
  The identifier of the AWS Managed Microsoft Active Directory that is registered with the Private CA Connector for Active Directory. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="any">
  Assigns metadata tags to the PCa Connector for AD directory registration, which can be used for cost allocation, access control, and resource organization. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="directory_registration_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this directory registration within AWS Private CA Connector for Active Directory. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
