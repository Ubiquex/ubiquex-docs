---
title: "aws_step_functions_state_machine_version"
description: "`aws_step_functions_state_machine_version` publishes a real, immutable version of an `aws_step_functions_state_machine`'s current definition..."
---

`aws_step_functions_state_machine_version` publishes a real, immutable version of an `aws_step_functions_state_machine`'s current definition, letting an `aws_step_functions_state_machine_alias` or a direct caller pin to a specific, unchanging version.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	step "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/step"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own functions state machine version"})

    		ubx.Resource(step.FunctionsStateMachineVersion, "example", step.FunctionsStateMachineVersionConfig{
    			StateMachineArn:        "arn:aws:iam::123456789012:policy/example",
    			Description:            "Managed by ubx.",
    			StateMachineRevisionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FunctionsStateMachineVersion } from "@ubx/sdk-aws/aws/step/functions_state_machine_version";

    export default stack("example", () => {
      intent({ summary: "example own functions state machine version" });

      resource(FunctionsStateMachineVersion, "example", {
        stateMachineArn: "arn:aws:iam::123456789012:policy/example",
        description: "Managed by ubx.",
        stateMachineRevisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.step import FunctionsStateMachineVersion, FunctionsStateMachineVersionConfig

    def describe():
        ubx.intent("example own functions state machine version")

        ubx.resource(FunctionsStateMachineVersion, "example", FunctionsStateMachineVersionConfig(
            state_machine_arn="arn:aws:iam::123456789012:policy/example",
            description="Managed by ubx.",
            state_machine_revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_step_functions_state_machine_version, with state
    machine arn arn:aws:iam::123456789012:policy/example, description
    Managed by ubx., state machine revision id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  An optional description of the state machine version, used to provide a human-readable label for the version. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="state_machine_arn" type="string" required>
  The Amazon Resource Name (ARN) of the state machine for which this version is created. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="state_machine_revision_id" type="string">
  Specifies the revision ID of the state machine to create the version from; if omitted, the version is created from the latest revision. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this state machine version, including the state machine name and version number. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
