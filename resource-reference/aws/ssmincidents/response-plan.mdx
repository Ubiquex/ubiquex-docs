---
title: "aws_ssmincidents_response_plan"
description: "Real, generated bindings for `aws_ssmincidents_response_plan`."
---

`aws_ssmincidents_response_plan` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssmincidents"

    cfg := ssmincidents.ResponsePlanConfig{
        Name: "example-name",
        ChatChannel: []string{"example"},
        DisplayName: "example-display-name",
    }
    ubx.Resource(ssmincidents.ResponsePlan, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResponsePlan } from
      "jsr:@ubx/sdk-aws/aws/ssmincidents/response_plan";

    resource(ResponsePlan, "example", {
      name: "example",
      chatChannel: ["example"],
      displayName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssmincidents import (
        ResponsePlan,
        ResponsePlanConfig,
    )

    cfg = ResponsePlanConfig(
        name="example",
        chat_channel=["example"],
        display_name="example",
    )
    ubx.resource(ResponsePlan, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssmincidents_response_plan with name, chat_channel,
display_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="chat_channel" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="engagements" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
