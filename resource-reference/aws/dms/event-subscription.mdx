---
title: "aws_dms_event_subscription"
description: "Real, generated bindings for `aws_dms_event_subscription`."
---

`aws_dms_event_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event subscription"})

    		ubx.Resource(dms.EventSubscription, "ci-runner", dms.EventSubscriptionConfig{
    			EventCategories: []string{"example"},
    			Name:            "ci-runner",
    			SnsTopicArn:     "arn:aws:iam::123456789012:policy/ci",
    			SourceType:      "example",
    			Enabled:         true,
    			SourceIds:       []string{"example"},
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventSubscription } from "jsr:@ubx/sdk-aws/aws/dms/event_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own event subscription" });

      resource(EventSubscription, "ci-runner", {
        eventCategories: ["example"],
        name: "ci-runner",
        snsTopicArn: "arn:aws:iam::123456789012:policy/ci",
        sourceType: "example",
        enabled: true,
        sourceIds: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dms import EventSubscription, EventSubscriptionConfig

    def describe():
        ubx.intent("payments own event subscription")

        ubx.resource(EventSubscription, "ci-runner", EventSubscriptionConfig(
            event_categories=["example"],
            name="ci-runner",
            sns_topic_arn="arn:aws:iam::123456789012:policy/ci",
            source_type="example",
            enabled=True,
            source_ids=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dms_event_subscription called "ci-runner", with event
    categories a set value, sns topic arn
    arn:aws:iam::123456789012:policy/ci, source type example, enabled
    true, source ids a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="event_categories" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="source_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
