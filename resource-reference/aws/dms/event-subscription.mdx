---
title: "aws_dms_event_subscription"
description: "Real, generated bindings for `aws_dms_event_subscription`."
---

`aws_dms_event_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dms"

    cfg := dms.EventSubscriptionConfig{
        EventCategories: []string{"example"},
        Name: "example-name",
        SnsTopicArn: "example",
        SourceType: "example",
        Enabled: true,
        SourceIds: []string{"example"},
    }
    ubx.Resource(dms.EventSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventSubscription } from
      "jsr:@ubx/sdk-aws/aws/dms/event_subscription";

    resource(EventSubscription, "example", {
      eventCategories: ["example"],
      name: "example",
      snsTopicArn: "example",
      sourceType: "example",
      enabled: true,
      sourceIds: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dms import (
        EventSubscription,
        EventSubscriptionConfig,
    )

    cfg = EventSubscriptionConfig(
        event_categories=["example"],
        name="example",
        sns_topic_arn="example",
        source_type="example",
        enabled=True,
        source_ids=["example"],
    )
    ubx.resource(EventSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dms_event_subscription with event_categories, name,
sns_topic_arn, source_type, enabled, source_ids.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="event_categories" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="source_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="source_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
