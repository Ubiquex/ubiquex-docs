---
title: "aws_dms_certificate"
description: "Real, generated bindings for `aws_dms_certificate`."
---

`aws_dms_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dms"

    cfg := dms.CertificateConfig{
        CertificateId: "example",
        CertificatePem: "example",
        CertificateWallet: "example",
    }
    ubx.Resource(dms.Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Certificate } from
      "jsr:@ubx/sdk-aws/aws/dms/certificate";

    resource(Certificate, "example", {
      certificateId: "example",
      certificatePem: "example",
      certificateWallet: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dms import Certificate, CertificateConfig

    cfg = CertificateConfig(
        certificate_id="example",
        certificate_pem="example",
        certificate_wallet="example",
    )
    ubx.resource(Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dms_certificate with certificate_id,
certificate_pem, certificate_wallet.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_pem" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_wallet" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="certificate_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
