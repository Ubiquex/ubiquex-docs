---
title: "aws_dms_migration_project"
description: "`aws_dms_migration_project` ties together source and target `aws_dms_data_provider`s, an instance profile..."
---

`aws_dms_migration_project` ties together source and target `aws_dms_data_provider`s, an instance profile, and transformation rules for AWS DMS Schema Conversion, the real project a caller uses to assess and convert database schemas to a new target engine before migrating the actual data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own migration project"})

    		ubx.Resource(dms.MigrationProject, "ci-runner", dms.MigrationProjectConfig{
    			Description:                           "Managed by ubx.",
    			InstanceProfileArn:                    "arn:aws:iam::123456789012:policy/ci",
    			InstanceProfileIdentifier:             "example",
    			InstanceProfileName:                   "example-instance-profile-name",
    			MigrationProjectCreationTime:          "example",
    			MigrationProjectIdentifier:            "example",
    			MigrationProjectName:                  "example-migration-project-name",
    			SchemaConversionApplicationAttributes: map[string]any{"s3_bucket_path": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MigrationProject } from "./local-sdk/aws/sdk/typescript/aws/dms/migration_project.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own migration project" });

      resource(MigrationProject, "ci-runner", {
        description: "Managed by ubx.",
        instanceProfileArn: "arn:aws:iam::123456789012:policy/ci",
        instanceProfileIdentifier: "example",
        instanceProfileName: "example-instance-profile-name",
        migrationProjectCreationTime: "example",
        migrationProjectIdentifier: "example",
        migrationProjectName: "example-migration-project-name",
        schemaConversionApplicationAttributes: { s3BucketPath: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.dms import MigrationProject, MigrationProjectConfig

    def describe():
        ubx.intent("payments own migration project")

        ubx.resource(MigrationProject, "ci-runner", MigrationProjectConfig(
            description="Managed by ubx.",
            instance_profile_arn="arn:aws:iam::123456789012:policy/ci",
            instance_profile_identifier="example",
            instance_profile_name="example-instance-profile-name",
            migration_project_creation_time="example",
            migration_project_identifier="example",
            migration_project_name="example-migration-project-name",
            schema_conversion_application_attributes={"s3_bucket_path": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dms_migration_project, with description Managed by
    ubx., instance profile arn arn:aws:iam::123456789012:policy/ci,
    instance profile identifier example, instance profile name
    example-instance-profile-name, migration project creation time
    example, migration project identifier example, migration project
    name example-migration-project-name, schema conversion
    application attributes a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The optional description of the migration project. Optional.
</ResponseField>

<ResponseField name="instance_profile_arn" type="string">
  The property describes an instance profile arn for the migration project. For read Optional.
</ResponseField>

<ResponseField name="instance_profile_identifier" type="string">
  The property describes an instance profile identifier for the migration project. For create Optional.
</ResponseField>

<ResponseField name="instance_profile_name" type="string">
  The property describes an instance profile name for the migration project. For read Optional.
</ResponseField>

<ResponseField name="migration_project_creation_time" type="string">
  The property describes a creating time of the migration project. Optional.
</ResponseField>

<ResponseField name="migration_project_identifier" type="string">
  The property describes an identifier for the migration project. It is used for describing/deleting/modifying can be name/arn Optional.
</ResponseField>

<ResponseField name="migration_project_name" type="string">
  The property describes a name to identify the migration project. Optional.
</ResponseField>

<ResponseField name="schema_conversion_application_attributes" type="object">
  The property describes schema conversion application attributes for the migration project. Optional.
  <Expandable title="properties">
    <ResponseField name="s3_bucket_path" type="string">
      The S3 bucket path (prefix) where the AWS DMS Schema Conversion application stores schema conversion artifacts (such as converted schemas and mapping rules) for the migration project. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="s3_bucket_role_arn" type="string">
      The ARN of the IAM role that AWS DMS assumes to access the S3 bucket used by the schema conversion application, enabling it to read and write schema conversion artifacts to the bucket specified by the sibling `s3_bucket_path` attribute. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_data_provider_descriptors" type="list(object)">
  The property describes source data provider descriptors for the migration project. Optional.
  <Expandable title="properties">
    <ResponseField name="data_provider_arn" type="string">
      The Amazon Resource Name (ARN) of the source data provider used to configure the migration project in AWS Database Migration Service. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data_provider_identifier" type="string">
      Specifies the unique identifier or ARN of the AWS DMS source data provider associated with this migration project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data_provider_name" type="string">
      The name of an existing AWS DMS data provider that acts as the source for this migration project, used within a source data provider descriptor. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="secrets_manager_access_role_arn" type="string">
      The ARN of the IAM role that AWS DMS assumes to access the AWS Secrets Manager secret containing the source data provider's credentials. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="secrets_manager_secret_id" type="string">
      Specifies the ARN or name of the AWS Secrets Manager secret that contains the credentials for the source database in the migration project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag that can be assigned to an AWS DMS migration project, used for categorizing, identifying, and managing the project resource within the service. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The user-defined string value of a tag applied to an AWS DMS migration project, used to store metadata such as project purpose, owner, or environment for resource management and cost tracking. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_data_provider_descriptors" type="list(object)">
  The property describes target data provider descriptors for the migration project. Optional.
  <Expandable title="properties">
    <ResponseField name="data_provider_arn" type="string">
      The ARN of the AWS DMS data provider that acts as the target endpoint for the migration project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data_provider_identifier" type="string">
      In the list of target data provider descriptors for the migration project, specifies the Amazon Resource Name (ARN) of the target data provider. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data_provider_name" type="string">
      The name of the target data provider that the DMS migration project uses as its destination, referencing an existing AWS DMS data provider resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="secrets_manager_access_role_arn" type="string">
      ARN of the IAM role that AWS DMS assumes to read the target data provider's connection credentials from AWS Secrets Manager. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="secrets_manager_secret_id" type="string">
      The secrets_manager_secret_id within a target data provider descriptor specifies the AWS Secrets Manager secret (by name or ARN) that stores the connection details for the target database endpoint used by the DMS migration project. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transformation_rules" type="string">
  The property describes transformation rules for the migration project. Optional.
</ResponseField>

## Output properties

<ResponseField name="migration_project_arn" type="string">
  The property describes an ARN of the migration project. Computed by AWS after `ship`.
</ResponseField>
