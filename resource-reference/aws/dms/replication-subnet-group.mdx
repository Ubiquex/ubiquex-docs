---
title: "aws_dms_replication_subnet_group"
description: "`aws_dms_replication_subnet_group` defines the real VPC subnets an `aws_dms_replication_instance` is actually placed into..."
---

`aws_dms_replication_subnet_group` defines the real VPC subnets an `aws_dms_replication_instance` is actually placed into, network placement a replication instance requires before it can be created.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own replication subnet group"})

    		ubx.Resource(dms.ReplicationSubnetGroup, "example", dms.ReplicationSubnetGroupConfig{
    			ReplicationSubnetGroupDescription: "example",
    			SubnetIds:                         []string{"example"},
    			ReplicationSubnetGroupIdentifier:  "example",
    			Tags:                              []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReplicationSubnetGroup } from "@ubx/sdk-aws/aws/dms/replication_subnet_group";

    export default stack("example", () => {
      intent({ summary: "example own replication subnet group" });

      resource(ReplicationSubnetGroup, "example", {
        replicationSubnetGroupDescription: "example",
        subnetIds: ["example"],
        replicationSubnetGroupIdentifier: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dms.replication_subnet_group import ReplicationSubnetGroup, ReplicationSubnetGroupConfig

    def describe():
        ubx.intent("example own replication subnet group")

        ubx.resource(ReplicationSubnetGroup, "example", ReplicationSubnetGroupConfig(
            replication_subnet_group_description="example",
            subnet_ids=["example"],
            replication_subnet_group_identifier="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dms_replication_subnet_group, with replication subnet
    group description example, subnet ids a set value, replication
    subnet group identifier example, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="replication_subnet_group_description" type="string" required>
  The description for the subnet group. Required.
</ResponseField>

<ResponseField name="replication_subnet_group_identifier" type="string">
  The name for the replication subnet group. This value is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  One or more subnet IDs to be assigned to the replication subnet group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  One or more tags to be assigned to the replication subnet group Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag (key-value pair) assigned to the DMS replication subnet group, used to categorize and manage the resource for cost allocation and operational purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
