---
title: "aws_dms_replication_subnet_group"
description: "Real, generated bindings for `aws_dms_replication_subnet_group`."
---

`aws_dms_replication_subnet_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own replication subnet group"})

    		ubx.Resource(dms.ReplicationSubnetGroup, "ci-runner", dms.ReplicationSubnetGroupConfig{
    			ReplicationSubnetGroupDescription: "example",
    			ReplicationSubnetGroupId:          "example",
    			SubnetIds:                         []string{"example"},
    			Tags:                              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReplicationSubnetGroup } from "jsr:@ubx/sdk-aws/aws/dms/replication_subnet_group";

    export default stack("payments", () => {
      intent({ summary: "payments own replication subnet group" });

      resource(ReplicationSubnetGroup, "ci-runner", {
        replicationSubnetGroupDescription: "example",
        replicationSubnetGroupId: "example",
        subnetIds: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dms import ReplicationSubnetGroup, ReplicationSubnetGroupConfig

    def describe():
        ubx.intent("payments own replication subnet group")

        ubx.resource(ReplicationSubnetGroup, "ci-runner", ReplicationSubnetGroupConfig(
            replication_subnet_group_description="example",
            replication_subnet_group_id="example",
            subnet_ids=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dms_replication_subnet_group, with replication subnet
    group description example, replication subnet group id example,
    subnet ids a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_subnet_group_description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="replication_subnet_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_subnet_group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
