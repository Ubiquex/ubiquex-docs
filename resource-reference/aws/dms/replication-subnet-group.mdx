---
title: "aws_dms_replication_subnet_group"
description: "Real, generated bindings for `aws_dms_replication_subnet_group`."
---

`aws_dms_replication_subnet_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/dms"

    cfg := dms.ReplicationSubnetGroupConfig{
        ReplicationSubnetGroupDescription: "example",
        ReplicationSubnetGroupId: "example",
        SubnetIds: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(dms.ReplicationSubnetGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ReplicationSubnetGroup } from
      "jsr:@ubx/sdk-aws/aws/dms/replication_subnet_group";

    resource(ReplicationSubnetGroup, "example", {
      replicationSubnetGroupDescription: "example",
      replicationSubnetGroupId: "example",
      subnetIds: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dms import (
        ReplicationSubnetGroup,
        ReplicationSubnetGroupConfig,
    )

    cfg = ReplicationSubnetGroupConfig(
        replication_subnet_group_description="example",
        replication_subnet_group_id="example",
        subnet_ids=["example"],
        tags={"key": "value"},
    )
    ubx.resource(ReplicationSubnetGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dms_replication_subnet_group with
replication_subnet_group_description,
replication_subnet_group_id, subnet_ids, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_subnet_group_description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="replication_subnet_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replication_subnet_group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
