---
title: "aws_organizations_delegated_administrator"
description: "Real, generated bindings for `aws_organizations_delegated_administrator`."
---

`aws_organizations_delegated_administrator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"

    cfg := organizations.DelegatedAdministratorConfig{
        AccountId: "example",
        ServicePrincipal: "example",
    }
    ubx.Resource(
        organizations.DelegatedAdministrator,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DelegatedAdministrator } from
      "jsr:@ubx/sdk-aws/aws/organizations/delegated_administrator";

    resource(DelegatedAdministrator, "example", {
      accountId: "example",
      servicePrincipal: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.organizations import (
        DelegatedAdministrator,
        DelegatedAdministratorConfig,
    )

    cfg = DelegatedAdministratorConfig(
        account_id="example",
        service_principal="example",
    )
    ubx.resource(DelegatedAdministrator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_organizations_delegated_administrator with
account_id, service_principal.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_principal" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="delegation_enabled_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="joined_method" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="joined_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
