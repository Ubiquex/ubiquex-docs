---
title: "aws_organizations_delegated_administrator"
description: "Real, generated bindings for `aws_organizations_delegated_administrator`."
---

`aws_organizations_delegated_administrator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own delegated administrator"})

    		ubx.Resource(organizations.DelegatedAdministrator, "ci-runner", organizations.DelegatedAdministratorConfig{
    			AccountId:        "example",
    			ServicePrincipal: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DelegatedAdministrator } from "jsr:@ubx/sdk-aws/aws/organizations/delegated_administrator";

    export default stack("payments", () => {
      intent({ summary: "payments own delegated administrator" });

      resource(DelegatedAdministrator, "ci-runner", {
        accountId: "example",
        servicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.organizations import DelegatedAdministrator, DelegatedAdministratorConfig

    def describe():
        ubx.intent("payments own delegated administrator")

        ubx.resource(DelegatedAdministrator, "ci-runner", DelegatedAdministratorConfig(
            account_id="example",
            service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_organizations_delegated_administrator, with account id
    example, service principal example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_principal" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="delegation_enabled_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="joined_method" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="joined_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
