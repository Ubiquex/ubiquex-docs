---
title: "aws_organizations_account"
description: "Real, generated bindings for `aws_organizations_account`."
---

`aws_organizations_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account"})

    		ubx.Resource(organizations.Account, "ci-runner", organizations.AccountConfig{
    			Email:                  "example",
    			Name:                   "ci-runner",
    			CloseOnDeletion:        true,
    			CreateGovcloud:         true,
    			IamUserAccessToBilling: "example",
    			RoleName:               "example-role-name",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Account } from "jsr:@ubx/sdk-aws/aws/organizations/account";

    export default stack("payments", () => {
      intent({ summary: "payments own account" });

      resource(Account, "ci-runner", {
        email: "example",
        name: "ci-runner",
        closeOnDeletion: true,
        createGovcloud: true,
        iamUserAccessToBilling: "example",
        roleName: "example-role-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.organizations import Account, AccountConfig

    def describe():
        ubx.intent("payments own account")

        ubx.resource(Account, "ci-runner", AccountConfig(
            email="example",
            name="ci-runner",
            close_on_deletion=True,
            create_govcloud=True,
            iam_user_access_to_billing="example",
            role_name="example-role-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_organizations_account called "ci-runner", with email
    example, close on deletion true, create govcloud true, iam user
    access to billing example, role name example-role-name, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="close_on_deletion" type="bool">
  Optional.
</ResponseField>

<ResponseField name="create_govcloud" type="bool">
  Optional.
</ResponseField>

<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="iam_user_access_to_billing" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="govcloud_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="joined_method" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="joined_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="parent_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
