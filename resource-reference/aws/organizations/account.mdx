---
title: "aws_organizations_account"
description: "Real, generated bindings for `aws_organizations_account`."
---

`aws_organizations_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"

    cfg := organizations.AccountConfig{
        Email: "example",
        Name: "example-name",
        CloseOnDeletion: true,
        CreateGovcloud: true,
    }
    ubx.Resource(organizations.Account, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Account } from
      "jsr:@ubx/sdk-aws/aws/organizations/account";

    resource(Account, "example", {
      email: "example",
      name: "example",
      closeOnDeletion: true,
      createGovcloud: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.organizations import Account, AccountConfig

    cfg = AccountConfig(
        email="example",
        name="example",
        close_on_deletion=True,
        create_govcloud=True,
    )
    ubx.resource(Account, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_organizations_account with email, name,
close_on_deletion, create_govcloud.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="close_on_deletion" type="bool">
  Optional.
</ResponseField>

<ResponseField name="create_govcloud" type="bool">
  Optional.
</ResponseField>

<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="iam_user_access_to_billing" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parent_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="govcloud_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="joined_method" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="joined_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="parent_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
