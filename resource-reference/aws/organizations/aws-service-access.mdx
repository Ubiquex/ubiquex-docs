---
title: "aws_organizations_aws_service_access"
description: "Real, generated bindings for `aws_organizations_aws_service_access`."
---

`aws_organizations_aws_service_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/organizations"

    cfg := organizations.AwsServiceAccessConfig{
        ServicePrincipal: "example",
    }
    ubx.Resource(organizations.AwsServiceAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AwsServiceAccess } from
      "jsr:@ubx/sdk-aws/aws/organizations/aws_service_access";

    resource(AwsServiceAccess, "example", {
      servicePrincipal: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.organizations import (
        AwsServiceAccess,
        AwsServiceAccessConfig,
    )

    cfg = AwsServiceAccessConfig(
        service_principal="example",
    )
    ubx.resource(AwsServiceAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_organizations_aws_service_access with
service_principal.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="service_principal" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="date_enabled" type="string">
  Computed by AWS after `ship`.
</ResponseField>
