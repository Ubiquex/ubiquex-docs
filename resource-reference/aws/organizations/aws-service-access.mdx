---
title: "aws_organizations_aws_service_access"
description: "Real, generated bindings for `aws_organizations_aws_service_access`."
---

`aws_organizations_aws_service_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own aws service access"})

    		ubx.Resource(organizations.AwsServiceAccess, "ci-runner", organizations.AwsServiceAccessConfig{
    			ServicePrincipal: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AwsServiceAccess } from "jsr:@ubx/sdk-aws/aws/organizations/aws_service_access";

    export default stack("payments", () => {
      intent({ summary: "payments own aws service access" });

      resource(AwsServiceAccess, "ci-runner", {
        servicePrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.organizations import AwsServiceAccess, AwsServiceAccessConfig

    def describe():
        ubx.intent("payments own aws service access")

        ubx.resource(AwsServiceAccess, "ci-runner", AwsServiceAccessConfig(
            service_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_organizations_aws_service_access, with service
    principal example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="service_principal" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="date_enabled" type="string">
  Computed by AWS after `ship`.
</ResponseField>
