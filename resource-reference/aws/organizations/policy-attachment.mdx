---
title: "aws_organizations_policy_attachment"
description: "Real, generated bindings for `aws_organizations_policy_attachment`."
---

`aws_organizations_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"

    cfg := organizations.PolicyAttachmentConfig{
        PolicyId: "example",
        TargetId: "example",
        SkipDestroy: true,
    }
    ubx.Resource(organizations.PolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PolicyAttachment } from
      "jsr:@ubx/sdk-aws/aws/organizations/policy_attachment";

    resource(PolicyAttachment, "example", {
      policyId: "example",
      targetId: "example",
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.organizations import (
        PolicyAttachment,
        PolicyAttachmentConfig,
    )

    cfg = PolicyAttachmentConfig(
        policy_id="example",
        target_id="example",
        skip_destroy=True,
    )
    ubx.resource(PolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_organizations_policy_attachment with policy_id,
target_id, skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
