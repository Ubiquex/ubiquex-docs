---
title: "aws_organizations_policy_attachment"
description: "Real, generated bindings for `aws_organizations_policy_attachment`."
---

`aws_organizations_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own policy attachment"})

    		ubx.Resource(organizations.PolicyAttachment, "ci-runner", organizations.PolicyAttachmentConfig{
    			PolicyId:    "example",
    			TargetId:    "example",
    			SkipDestroy: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PolicyAttachment } from "jsr:@ubx/sdk-aws/aws/organizations/policy_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own policy attachment" });

      resource(PolicyAttachment, "ci-runner", {
        policyId: "example",
        targetId: "example",
        skipDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.organizations import PolicyAttachment, PolicyAttachmentConfig

    def describe():
        ubx.intent("payments own policy attachment")

        ubx.resource(PolicyAttachment, "ci-runner", PolicyAttachmentConfig(
            policy_id="example",
            target_id="example",
            skip_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_organizations_policy_attachment, with policy id
    example, target id example, skip destroy true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
