---
title: "aws_organizations_policy"
description: "Real, generated bindings for `aws_organizations_policy`."
---

`aws_organizations_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/organizations"

    cfg := organizations.PolicyConfig{
        Content: "example",
        Name: "example-name",
        Description: "example",
        SkipDestroy: true,
    }
    ubx.Resource(organizations.Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Policy } from
      "jsr:@ubx/sdk-aws/aws/organizations/policy";

    resource(Policy, "example", {
      content: "example",
      name: "example",
      description: "example",
      skipDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.organizations import Policy, PolicyConfig

    cfg = PolicyConfig(
        content="example",
        name="example",
        description="example",
        skip_destroy=True,
    )
    ubx.resource(Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_organizations_policy with content, name,
description, skip_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
