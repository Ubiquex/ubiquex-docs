---
title: "aws_io_ttwin_maker_sync_job"
description: "`aws_io_ttwin_maker_sync_job` keeps an `aws_io_ttwin_maker_workspace`'s own entities automatically synchronized with a real, external source of truth..."
---

`aws_io_ttwin_maker_sync_job` keeps an `aws_io_ttwin_maker_workspace`'s own entities automatically synchronized with a real, external source of truth, so a digital twin doesn't drift out of date with the physical or logical system it represents.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ttwin maker sync job"})

    		ubx.Resource(io.TtwinMakerSyncJob, "ci-runner", io.TtwinMakerSyncJobConfig{
    			SyncRole:    "example",
    			SyncSource:  "example",
    			WorkspaceId: "example",
    			Tags:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TtwinMakerSyncJob } from "./local-sdk/aws/sdk/typescript/aws/io/ttwin_maker_sync_job.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own ttwin maker sync job" });

      resource(TtwinMakerSyncJob, "ci-runner", {
        syncRole: "example",
        syncSource: "example",
        workspaceId: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TtwinMakerSyncJob, TtwinMakerSyncJobConfig

    def describe():
        ubx.intent("payments own ttwin maker sync job")

        ubx.resource(TtwinMakerSyncJob, "ci-runner", TtwinMakerSyncJobConfig(
            sync_role="example",
            sync_source="example",
            workspace_id="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_ttwin_maker_sync_job, with sync role example, sync
    source example, workspace id example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="sync_role" type="string" required>
  The IAM Role that execute SyncJob. Required.
</ResponseField>

<ResponseField name="sync_source" type="string" required>
  The source of the SyncJob. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The ID of the workspace. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the SyncJob. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date_time" type="string">
  The date and time at which the AWS IoT TwinMaker sync job was created, expressed in ISO 8601 format. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="state" type="string">
  The state of SyncJob. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_date_time" type="string">
  The date and time when the sync job was last updated, in ISO 8601 format. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
