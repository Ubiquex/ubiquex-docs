---
title: "aws_io_tanalytics_pipeline"
description: "`aws_io_tanalytics_pipeline` is a real, ordered sequence of activities, filtering, enriching, transforming..."
---

`aws_io_tanalytics_pipeline` is a real, ordered sequence of activities, filtering, enriching, transforming, that processes messages from an `aws_io_tanalytics_channel` and writes the result into an `aws_io_tanalytics_datastore`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tanalytics pipeline"})

    		ubx.Resource(io.TanalyticsPipeline, "ci-runner", io.TanalyticsPipelineConfig{
    			PipelineActivities: []map[string]any{{"add_attributes": map[string]any{"name": "example-name"}}},
    			PipelineName:       "example-pipeline-name",
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TanalyticsPipeline } from "./local-sdk/aws/sdk/typescript/aws/io/tanalytics_pipeline.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own tanalytics pipeline" });

      resource(TanalyticsPipeline, "ci-runner", {
        pipelineActivities: [{ addAttributes: { name: "example-name" } }],
        pipelineName: "example-pipeline-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TanalyticsPipeline, TanalyticsPipelineConfig

    def describe():
        ubx.intent("payments own tanalytics pipeline")

        ubx.resource(TanalyticsPipeline, "ci-runner", TanalyticsPipelineConfig(
            pipeline_activities=[{"add_attributes": {"name": "example-name"}}],
            pipeline_name="example-pipeline-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tanalytics_pipeline, with pipeline activities a set
    value, pipeline name example-pipeline-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="pipeline_activities" type="list(object)" required>
  Specifies the ordered list of pipeline activities (such as filter, select attributes, math, device registry, and channel or datastore writes) that process messages as they flow through the IoT Analytics pipeline. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="add_attributes" type="object">
      Defines an AddAttributes activity, which adds the specified attributes to the message payload within an AWS IoT Analytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attributes" type="any">
          Defines the key-value pairs of attributes to add to the message in the AddAttributes pipeline activity, where each key is the attribute name and the value is the attribute value (both strings). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Specifies the name of the addAttributes pipeline activity, which uniquely identifies this activity within the AWS IoT Analytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          The name of the next activity in the pipeline to execute after this AddAttributes activity, chaining the processing steps in sequence. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="channel" type="object">
      Defines a channel activity that specifies the AWS IoT Analytics channel from which the pipeline reads messages as its source, including the activity's name, the channel name, and the next activity in the pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="channel_name" type="string">
          The name of the AWS IoTAnalytics channel from which this pipeline activity reads messages. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the channel activity within the IoT Analytics pipeline, which uniquely identifies this activity in the pipeline's activity list. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          Specifies the name of the next activity in the pipeline to execute after this channel activity, enabling the chaining of processing steps. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="datastore" type="object">
      Defines a datastore activity that writes the incoming messages to a specified AWS IoTAnalytics datastore, identified by its datastore name. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="datastore_name" type="string">
          The name of the IoT Analytics datastore to which this pipeline activity writes data. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of this datastore activity inside an AWS IoT Analytics pipeline, serving as a unique identifier for the activity within the pipeline's ordered activity list. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_registry_enrich" type="object">
      This object configures a DeviceRegistryEnrich pipeline activity that uses the AWS IoT device registry to look up device attributes by the device ID in each message and add them to the message payload. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attribute" type="string">
          The name of the message attribute that AWS IoT Analytics populates with the device registry information for the IoT thing specified in the thingName property. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the device registry enrichment activity, which serves as the unique reference identifier used by the `next` property of other pipeline activities to chain execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          The name of the next activity in the pipeline to execute after this device registry enrich activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          ARN of the IAM role that grants AWS IoT Analytics permission to read from the AWS IoT device registry to enrich messages with device metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="thing_name" type="string">
          Specifies the name of the AWS IoT thing whose registry data is retrieved and added to the message by the DeviceRegistryEnrich activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_shadow_enrich" type="object">
      This object configures a device shadow enrichment activity, which retrieves the latest device shadow state for a specified IoT thing and adds it to the message payload. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attribute" type="string">
          The name of the device shadow attribute whose value is read and added to the message payload by the DeviceShadowEnrich activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Specifies a unique name for the device shadow enrich activity within the pipeline, used to reference this activity in the pipeline's activity list. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          The name of the next pipeline activity to run after this device shadow enrichment step. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of the IAM role that AWS IoT Analytics assumes to access the device shadow data for the enrich activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="thing_name" type="string">
          The name of the AWS IoT thing whose shadow is used to enrich the message data in the device shadow enrich activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="filter" type="object">
      The filter block defines an IoT Analytics pipeline filter activity, which passes only messages that satisfy the specified filter expression on to the next pipeline activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="filter" type="string">
          The expression used by the filter activity to select which messages in the pipeline are passed to the next activity, retaining only those that satisfy the condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name that uniquely identifies the filter activity within the IoT Analytics pipeline, used to refer to this activity in the pipeline's data processing flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          Specifies the name of the next pipeline activity to execute after this Filter activity completes. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lambda" type="object">
      The lambda activity configuration in an AWS IoT Analytics pipeline, specifying the Lambda function name (lambdaName), the batch size for processing messages, and the name of the next activity to execute. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="batch_size" type="number">
          The maximum number of messages that the Lambda activity passes to its configured Lambda function in a single invocation (batch size). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lambda_name" type="string">
          The name of the AWS Lambda function that this activity invokes to process message data in the AWS IoTAnalytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the Lambda activity, which must be unique within the AWS IoT Analytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          The name of the next activity in the pipeline to execute after this Lambda activity completes. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="math" type="object">
      Specifies the math activity in the pipeline, which performs arithmetic operations on message attributes and assigns the computed value to a specified attribute for further processing. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attribute" type="string">
          The name of the IoT Analytics message attribute that will store the result of the math expression evaluated by this pipeline activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="math" type="string">
          The mathematical expression to evaluate for the Math activity, written using IoT Analytics expression syntax. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the math activity, which identifies it within the IoT Analytics pipeline for reference by other activities. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          Specifies the name of the next pipeline activity to run after this math activity in the AWS IoT Analytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="remove_attributes" type="object">
      The remove_attributes block configures a RemoveAttributes pipeline activity, which removes the specified attributes from each message processed by the AWS IoT Analytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attributes" type="list(string)">
          Specifies the list of attribute names to remove from the message payload in the RemoveAttributes pipeline activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the removeAttributes activity within the pipeline, which must be unique across all activities in the pipeline and is used by the IoT Analytics service to identify this activity when processing messages. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          Specifies the name of the next pipeline activity to execute after the 'removeAttributes' activity removes the listed attributes from the message. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="select_attributes" type="object">
      Configuration for a selectAttributes pipeline activity that creates a new message containing only the specified attributes from the original message, discarding all others before passing it to the next activity. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attributes" type="list(string)">
          Specifies the list of message attribute names to keep in the output message when the SelectAttributes pipeline activity creates a new message from the input. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the select_attributes activity, which must be unique within the pipeline and is used to identify the activity in the pipeline's activity list. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="next" type="string">
          Specifies the name of the next pipeline activity to execute after this select attributes activity in the AWS IoT Analytics pipeline. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="pipeline_name" type="string">
  The name of the AWS IoT Analytics pipeline, which serves as the pipeline identifier and must be unique within the AWS account and region. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Attaches a list of key-value tags to the IoT Analytics pipeline, which can be used for cost allocation, resource categorization, and access control via IAM policies. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag attached to the AWS IoT Analytics pipeline, which is a custom string used to categorize and identify the pipeline for management and cost allocation purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier of the IoT Analytics pipeline, which is set to the pipeline name specified in the configuration. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
