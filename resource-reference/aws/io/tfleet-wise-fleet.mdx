---
title: "aws_io_tfleet_wise_fleet"
description: "`aws_io_tfleet_wise_fleet` is a real, logical grouping of `aws_io_tfleet_wise_vehicle`s that an `aws_io_tfleet_wise_campaign` can target together..."
---

`aws_io_tfleet_wise_fleet` is a real, logical grouping of `aws_io_tfleet_wise_vehicle`s that an `aws_io_tfleet_wise_campaign` can target together rather than every vehicle being addressed individually. Part of AWS IoT FleetWise, which is no longer open to new customers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tfleet wise fleet"})

    		ubx.Resource(io.TfleetWiseFleet, "ci-runner", io.TfleetWiseFleetConfig{
    			Id:               "example",
    			SignalCatalogArn: "arn:aws:iam::123456789012:policy/ci",
    			Description:      "Managed by ubx.",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TfleetWiseFleet } from "./local-sdk/aws/sdk/typescript/aws/io/tfleet_wise_fleet.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own tfleet wise fleet" });

      resource(TfleetWiseFleet, "ci-runner", {
        id: "example",
        signalCatalogArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TfleetWiseFleet, TfleetWiseFleetConfig

    def describe():
        ubx.intent("payments own tfleet wise fleet")

        ubx.resource(TfleetWiseFleet, "ci-runner", TfleetWiseFleetConfig(
            id="example",
            signal_catalog_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tfleet_wise_fleet, with id example, signal catalog
    arn arn:aws:iam::123456789012:policy/ci, description Managed by
    ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Provides an optional human-readable description of the fleet, such as its purpose or operational context, with a maximum length of 2048 characters. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string" required>
  The user-defined unique fleet ID that identifies the fleet in AWS IoT FleetWise, which is required when creating the fleet. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="signal_catalog_arn" type="string" required>
  The ARN of the signal catalog that this fleet is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Defines a list of tags, each containing a key and value, applied to the AWS IoT FleetWise fleet to support cost allocation, access control, and resource identification. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS IoT FleetWise fleet, used to label and categorize the fleet resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key assigned to the AWS IoT FleetWise fleet, used for identifying or organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the fleet in AWS IoT FleetWise. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="creation_time" type="string">
  The timestamp of when the fleet was created in AWS IoT FleetWise, returned as an ISO 8601 formatted string. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="last_modification_time" type="string">
  The timestamp (in ISO 8601 format) of the last modification to the fleet. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
