---
title: "aws_io_t_account_audit_configuration"
description: "`aws_io_t_account_audit_configuration` turns on AWS IoT Device Defender's real, account-wide audit checks..."
---

`aws_io_t_account_audit_configuration` turns on AWS IoT Device Defender's real, account-wide audit checks, security-related misconfigurations across an account's things, certificates, and policies, that an `aws_io_t_scheduled_audit` then runs on a recurring basis.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own t account audit configuration"})

    		ubx.Resource(io.TAccountAuditConfiguration, "example", io.TAccountAuditConfigurationConfig{
    			AccountId:                             "example",
    			AuditCheckConfigurations:              map[string]any{"authenticated_cognito_role_overly_permissive_check": map[string]any{"enabled": true}},
    			RoleArn:                               "arn:aws:iam::123456789012:policy/example",
    			AuditNotificationTargetConfigurations: map[string]any{"sns": map[string]any{"enabled": true}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TAccountAuditConfiguration } from "./local-sdk/aws/sdk/typescript/aws/io/t_account_audit_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own t account audit configuration" });

      resource(TAccountAuditConfiguration, "example", {
        accountId: "example",
        auditCheckConfigurations: {
          authenticatedCognitoRoleOverlyPermissiveCheck: { enabled: true },
        },
        roleArn: "arn:aws:iam::123456789012:policy/example",
        auditNotificationTargetConfigurations: { sns: { enabled: true } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TAccountAuditConfiguration, TAccountAuditConfigurationConfig

    def describe():
        ubx.intent("example own t account audit configuration")

        ubx.resource(TAccountAuditConfiguration, "example", TAccountAuditConfigurationConfig(
            account_id="example",
            audit_check_configurations={"authenticated_cognito_role_overly_permissive_check": {"enabled": True}},
            role_arn="arn:aws:iam::123456789012:policy/example",
            audit_notification_target_configurations={"sns": {"enabled": True}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_account_audit_configuration, with account id
    example, audit check configurations a set value, role arn
    arn:aws:iam::123456789012:policy/example, audit notification
    target configurations a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="account_id" type="string" required>
  Your 12-digit account ID (used as the primary identifier for the CloudFormation resource). Required.
</ResponseField>

<ResponseField name="audit_check_configurations" type="object" required>
  Specifies which audit checks are enabled and disabled for this account. Required.
  <Expandable title="properties">
    <ResponseField name="authenticated_cognito_role_overly_permissive_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ca_certificate_expiring_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ca_certificate_key_quality_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="conflicting_client_ids_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_certificate_age_check" type="object">
      A structure containing the configName and corresponding configValue for configuring DeviceCertAgeCheck. Optional.
      <Expandable title="properties">
        <ResponseField name="configuration" type="object">
          A structure containing the configName and corresponding configValue for configuring audit checks. Optional.
          <Expandable title="properties">
            <ResponseField name="cert_age_threshold_in_days" type="string">
              The configValue for configuring audit checks. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_certificate_expiring_check" type="object">
      A structure containing the configName and corresponding configValue for configuring DeviceCertExpirationCheck. Optional.
      <Expandable title="properties">
        <ResponseField name="configuration" type="object">
          A structure containing the configName and corresponding configValue for configuring audit checks. Optional.
          <Expandable title="properties">
            <ResponseField name="cert_expiration_threshold_in_days" type="string">
              The configValue for configuring audit checks. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_certificate_key_quality_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_certificate_shared_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="intermediate_ca_revoked_for_active_device_certificates_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="io_tpolicy_potential_mis_configuration_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="iot_policy_overly_permissive_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="iot_role_alias_allows_access_to_unused_services_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="iot_role_alias_overly_permissive_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="logging_disabled_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="revoked_ca_certificate_still_active_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="revoked_device_certificate_still_active_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="unauthenticated_cognito_role_overly_permissive_check" type="object">
      The configuration for a specific audit check. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if the check is enabled. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="audit_notification_target_configurations" type="object">
  Information about the targets to which audit notifications are sent. Optional.
  <Expandable title="properties">
    <ResponseField name="sns" type="object">
      Configures the Amazon SNS topic and IAM role used by AWS IoT Device Defender to deliver account audit notifications, representing the SNS target within the audit notification target configuration map. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          True if notifications to the target are enabled. Optional.
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of the role that grants permission to send notifications to the target. Optional.
        </ResponseField>
        <ResponseField name="target_arn" type="string">
          The ARN of the target (SNS topic) to which audit notifications are sent. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The ARN of the role that grants permission to AWS IoT to access information about your devices, policies, certificates and other items as required when performing an audit. Required.
</ResponseField>
