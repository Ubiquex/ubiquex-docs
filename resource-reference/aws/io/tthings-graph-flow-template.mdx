---
title: "aws_io_tthings_graph_flow_template"
description: "`aws_io_tthings_graph_flow_template` defined a real, visual workflow connecting multiple devices and services together in AWS IoT Things Graph."
---

AWS IoT Things Graph was fully discontinued and transitioned to AWS IoT TwinMaker on November 9, 2022; its APIs are deprecated, kept only for backward compatibility, and should not be used for new work.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own tthings graph flow template"})

    		ubx.Resource(io.TthingsGraphFlowTemplate, "example", io.TthingsGraphFlowTemplateConfig{
    			Definition:                 map[string]any{"language": "example"},
    			CompatibleNamespaceVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TthingsGraphFlowTemplate } from "./local-sdk/aws/sdk/typescript/aws/io/tthings_graph_flow_template.ts";

    export default stack("example", () => {
      intent({ summary: "example own tthings graph flow template" });

      resource(TthingsGraphFlowTemplate, "example", {
        definition: { language: "example" },
        compatibleNamespaceVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TthingsGraphFlowTemplate, TthingsGraphFlowTemplateConfig

    def describe():
        ubx.intent("example own tthings graph flow template")

        ubx.resource(TthingsGraphFlowTemplate, "example", TthingsGraphFlowTemplateConfig(
            definition={"language": "example"},
            compatible_namespace_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tthings_graph_flow_template, with definition a set
    value, compatible namespace version 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="compatible_namespace_version" type="number">
  The version of the user's namespace that the flow template is compatible with, used to ensure the flow definition aligns with a specific published namespace version. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="definition" type="object" required>
  The definition object specifies the flow template's workflow via a definition document containing the GraphQL language and text that describes the nodes and connections. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="language" type="string" required>
      Specifies the language of the flow template definition; the only supported value is `GRAPHML`, the GraphML XML format used to describe the flow. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="text" type="string" required>
      The GraphQL text that defines the flow template, using AWS IoT Things Graph's data model language. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier of the flow template, as assigned by AWS IoT Things Graph upon creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
