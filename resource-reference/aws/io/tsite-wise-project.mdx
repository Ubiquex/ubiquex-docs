---
title: "aws_io_tsite_wise_project"
description: "`aws_io_tsite_wise_project` groups a real set of `aws_io_tsite_wise_asset`s and `aws_io_tsite_wise_dashboard`s inside an `aws_io_tsite_wise_portal`..."
---

`aws_io_tsite_wise_project` groups a real set of `aws_io_tsite_wise_asset`s and `aws_io_tsite_wise_dashboard`s inside an `aws_io_tsite_wise_portal`, organizing what a specific team or use case actually sees when they open the portal.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own tsite wise project"})

    		ubx.Resource(io.TsiteWiseProject, "example", io.TsiteWiseProjectConfig{
    			PortalId:           "example",
    			ProjectName:        "example-project-name",
    			AssetIds:           []string{"example"},
    			ProjectDescription: "example",
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TsiteWiseProject } from "./local-sdk/aws/sdk/typescript/aws/io/tsite_wise_project.ts";

    export default stack("example", () => {
      intent({ summary: "example own tsite wise project" });

      resource(TsiteWiseProject, "example", {
        portalId: "example",
        projectName: "example-project-name",
        assetIds: ["example"],
        projectDescription: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TsiteWiseProject, TsiteWiseProjectConfig

    def describe():
        ubx.intent("example own tsite wise project")

        ubx.resource(TsiteWiseProject, "example", TsiteWiseProjectConfig(
            portal_id="example",
            project_name="example-project-name",
            asset_ids=["example"],
            project_description="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tsite_wise_project, with portal id example, project
    name example-project-name, asset ids a set value, project
    description example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="asset_ids" type="list(string)">
  The IDs of the assets to be associated to the project. Optional.
</ResponseField>

<ResponseField name="portal_id" type="string" required>
  The ID of the portal in which to create the project. Required.
</ResponseField>

<ResponseField name="project_description" type="string">
  A description for the project. Optional.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  A friendly name for the project. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs that contain metadata for the project. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the key of a tag, a user-defined label used to identify, organize, and manage AWS IoT SiteWise projects in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag key-value pair applied to the AWS IoT SiteWise project, used for resource organization, cost allocation, and IAM access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="project_arn" type="string">
  The ARN of the project. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="project_id" type="string">
  The ID of the project. Computed by AWS after `ship`.
</ResponseField>
