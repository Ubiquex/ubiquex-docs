---
title: "aws_io_t_software_package_version"
description: "`aws_io_t_software_package_version` is one real, specific, deployable version, a set of actual files, firmware, configuration, security patches..."
---

`aws_io_t_software_package_version` is one real, specific, deployable version, a set of actual files, firmware, configuration, security patches, of an `aws_io_t_software_package`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own t software package version"})

    		ubx.Resource(io.TSoftwarePackageVersion, "example", io.TSoftwarePackageVersionConfig{
    			PackageName: "example-package-name",
    			Artifact:    map[string]any{"s3_location": map[string]any{"bucket": "example"}},
    			Attributes:  "example",
    			Description: "Managed by ubx.",
    			Recipe:      "example",
    			Sbom:        map[string]any{"s3_location": map[string]any{"bucket": "example"}},
    			Tags:        []map[string]any{{"key": "example"}},
    			VersionName: "example-version-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TSoftwarePackageVersion } from "./local-sdk/aws/sdk/typescript/aws/io/t_software_package_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own t software package version" });

      resource(TSoftwarePackageVersion, "example", {
        packageName: "example-package-name",
        artifact: { s3Location: { bucket: "example" } },
        attributes: "example",
        description: "Managed by ubx.",
        recipe: "example",
        sbom: { s3Location: { bucket: "example" } },
        tags: [{ key: "example" }],
        versionName: "example-version-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TSoftwarePackageVersion, TSoftwarePackageVersionConfig

    def describe():
        ubx.intent("example own t software package version")

        ubx.resource(TSoftwarePackageVersion, "example", TSoftwarePackageVersionConfig(
            package_name="example-package-name",
            artifact={"s3_location": {"bucket": "example"}},
            attributes="example",
            description="Managed by ubx.",
            recipe="example",
            sbom={"s3_location": {"bucket": "example"}},
            tags=[{"key": "example"}],
            version_name="example-version-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_software_package_version, with package name
    example-package-name, artifact a set value, attributes example,
    description Managed by ubx., recipe example, sbom a set value,
    tags a set value, version name example-version-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="artifact" type="object">
  The artifact location of the package version Optional.
  <Expandable title="properties">
    <ResponseField name="s3_location" type="object" required>
      The Amazon S3 location Required.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string" required>
          The S3 bucket Required.
        </ResponseField>
        <ResponseField name="key" type="string" required>
          The S3 key Required.
        </ResponseField>
        <ResponseField name="version" type="string" required>
          The S3 version Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="attributes" type="any">
  Specifies metadata attributes as key-value pairs that are attached to the software package version in AWS IoT, allowing you to add custom searchable or informational data to the version's registry entry. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  Provides a user-defined description of this AWS IoT software package version. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="package_name" type="string" required>
  The name of the AWS IoT software package that this software package version belongs to. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="recipe" type="string">
  The inline json job document associated with a software package version Optional.
</ResponseField>

<ResponseField name="sbom" type="object">
  The sbom zip archive location of the package version Optional.
  <Expandable title="properties">
    <ResponseField name="s3_location" type="object" required>
      The Amazon S3 location Required.
      <Expandable title="properties">
        <ResponseField name="bucket" type="string" required>
          The S3 bucket Required.
        </ResponseField>
        <ResponseField name="key" type="string" required>
          The S3 key Required.
        </ResponseField>
        <ResponseField name="version" type="string" required>
          The S3 version Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the key of a tag attached to the AWS IoT software package version, used to identify and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to this AWS IoT Software Package Version resource, providing arbitrary metadata for the package version. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version_name" type="string">
  The user-specified name for this version of the AWS IoT software package, which must be unique within the package; if omitted, AWS IoT/CloudFormation will generate a unique version name. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="error_reason" type="string">
  The error reason reported by AWS IoT when the software package version enters a FAILED state, providing details about why version creation or update failed. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="package_version_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AWS IoT software package version. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="sbom_validation_status" type="string">
  The validation status of the Sbom file Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The current status of the software package version, such as DRAFT, PUBLISHED, or DEPRECATED, as tracked by AWS IoT Software Package Catalog. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
