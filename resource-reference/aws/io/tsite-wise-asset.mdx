---
title: "aws_io_tsite_wise_asset"
description: "`aws_io_tsite_wise_asset` represents one real, physical piece of industrial equipment, created from an `aws_io_tsite_wise_asset_model`..."
---

`aws_io_tsite_wise_asset` represents one real, physical piece of industrial equipment, created from an `aws_io_tsite_wise_asset_model`, that an `aws_io_tsite_wise_gateway` streams real, live sensor data into.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own tsite wise asset"})

    		ubx.Resource(io.TsiteWiseAsset, "example", io.TsiteWiseAssetConfig{
    			AssetModelId:     "example",
    			AssetName:        "example-asset-name",
    			AssetDescription: "example",
    			AssetExternalId:  "example",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TsiteWiseAsset } from "@ubx/sdk-aws/aws/io/tsite_wise_asset";

    export default stack("example", () => {
      intent({ summary: "example own tsite wise asset" });

      resource(TsiteWiseAsset, "example", {
        assetModelId: "example",
        assetName: "example-asset-name",
        assetDescription: "example",
        assetExternalId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.io import TsiteWiseAsset, TsiteWiseAssetConfig

    def describe():
        ubx.intent("example own tsite wise asset")

        ubx.resource(TsiteWiseAsset, "example", TsiteWiseAssetConfig(
            asset_model_id="example",
            asset_name="example-asset-name",
            asset_description="example",
            asset_external_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tsite_wise_asset, with asset model id example,
    asset name example-asset-name, asset description example, asset
    external id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="asset_description" type="string">
  A description for the asset Optional.
</ResponseField>

<ResponseField name="asset_external_id" type="string">
  The External ID of the asset Optional.
</ResponseField>

<ResponseField name="asset_model_id" type="string" required>
  The ID of the asset model from which to create the asset. Required.
</ResponseField>

<ResponseField name="asset_name" type="string" required>
  A unique, friendly name for the asset. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs that contain metadata for the asset. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the AWS IoT SiteWise asset, used to identify and categorize the asset. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value string of a tag (key-value pair) attached to the AWS IoT SiteWise asset, used for metadata and resource categorization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="asset_arn" type="string">
  The ARN of the asset Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="asset_hierarchies" type="list(object)">
  The list of asset hierarchies that define parent-child relationships from this asset to other assets, each including the hierarchy's logical ID and the target child asset's ID. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="child_asset_id" type="string">
      The child_asset_id specifies the ID of the child asset that this asset's hierarchy references, linking the two assets in the IoT SiteWise asset hierarchy. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="external_id" type="string">
      Defines an optional external identifier for this asset hierarchy, allowing it to be referenced or matched with external systems in AWS IoT SiteWise. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="logical_id" type="string">
      The logical ID of the asset hierarchy, which must match a hierarchy logical ID defined in the asset model and uniquely identifies the parent -- child relationship between the asset and its child asset. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="asset_id" type="string">
  The ID of the asset Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="asset_properties" type="list(object)">
  The computed list of property definitions for the IoT SiteWise asset, each including the logical ID, alias, notification state, and resolved value from the asset model. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="alias" type="string">
      The alias provides an external, human-readable identifier for the asset property that can be used to map the property to data from an external data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="external_id" type="string">
      The external_id of an asset property specifies a unique, customer-supplied identifier for that property, which can be used to map or reference the property across systems. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      The unique identifier of the asset property, assigned by AWS IoT SiteWise when the asset is created, used to reference the property in API operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="logical_id" type="string">
      The logical ID of the asset property, which uniquely identifies the property within the asset and must match a property definition's logical ID in the asset model. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="notification_state" type="string">
      Indicates the current notification state (ENABLED or DISABLED) for the asset property, which controls whether value updates trigger AWS IoT SiteWise notifications. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="unit" type="string">
      The physical unit of measurement (e.g., 'Celsius', 'kPa') for a property of the asset, defining the standard in which the property's value is expressed. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
