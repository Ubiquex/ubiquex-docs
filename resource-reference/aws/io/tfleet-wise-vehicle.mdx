---
title: "aws_io_tfleet_wise_vehicle"
description: "`aws_io_tfleet_wise_vehicle` registers a real, individual vehicle running the AWS IoT FleetWise Edge Agent..."
---

`aws_io_tfleet_wise_vehicle` registers a real, individual vehicle running the AWS IoT FleetWise Edge Agent, the actual entity an `aws_io_tfleet_wise_campaign` and `aws_io_tfleet_wise_state_template` get associated with to collect data from. AWS IoT FleetWise is no longer open to new customers; existing customers can continue using it, with AWS pointing to its Guidance for Connected Mobility on AWS for equivalent new capability.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tfleet wise vehicle"})

    		ubx.Resource(io.TfleetWiseVehicle, "ci-runner", io.TfleetWiseVehicleConfig{
    			DecoderManifestArn:  "arn:aws:iam::123456789012:policy/ci",
    			ModelManifestArn:    "arn:aws:iam::123456789012:policy/ci",
    			Name:                "ci-runner",
    			AssociationBehavior: "example",
    			Attributes:          "example",
    			StateTemplates:      []map[string]any{{"identifier": "example"}},
    			Tags:                []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TfleetWiseVehicle } from "./local-sdk/aws/sdk/typescript/aws/io/tfleet_wise_vehicle.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own tfleet wise vehicle" });

      resource(TfleetWiseVehicle, "ci-runner", {
        decoderManifestArn: "arn:aws:iam::123456789012:policy/ci",
        modelManifestArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        associationBehavior: "example",
        attributes: "example",
        stateTemplates: [{ identifier: "example" }],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TfleetWiseVehicle, TfleetWiseVehicleConfig

    def describe():
        ubx.intent("payments own tfleet wise vehicle")

        ubx.resource(TfleetWiseVehicle, "ci-runner", TfleetWiseVehicleConfig(
            decoder_manifest_arn="arn:aws:iam::123456789012:policy/ci",
            model_manifest_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            association_behavior="example",
            attributes="example",
            state_templates=[{"identifier": "example"}],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tfleet_wise_vehicle called "ci-runner", with
    decoder manifest arn arn:aws:iam::123456789012:policy/ci, model
    manifest arn arn:aws:iam::123456789012:policy/ci, association
    behavior example, attributes example, state templates a set
    value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="association_behavior" type="string">
  Specifies whether to create or update the association between the vehicle and its associated fleet, with valid values being CREATE or UPDATE. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="attributes" type="any">
  A map of custom string key-value pairs that define metadata attributes for the vehicle, such as make, model, year, or fleet-specific identifiers, used by AWS IoT FleetWise to describe and manage the vehicle. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="decoder_manifest_arn" type="string" required>
  The Amazon Resource Name (ARN) of the decoder manifest that defines the signal decoding rules for this vehicle. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="model_manifest_arn" type="string" required>
  The Amazon Resource Name (ARN) of the model manifest that defines the vehicle's attributes and signals. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The unique name of the vehicle, which is used to identify it in AWS IoT FleetWise. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="state_templates" type="list(object)">
  Specifies a list of state templates that each define a named set of vehicle signals (e.g., speed, gear) to represent and report the vehicle's current state in AWS IoT FleetWise. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="identifier" type="string">
      The unique identifier that associates a state template with the vehicle, used to reference the state template within the vehicle's state template associations. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state_template_update_strategy" type="object">
      Specifies how the state template is updated, either by a periodic schedule (with a cron/rate expression and an action to take on update) or immediately when a change occurs. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="on_change" type="any">
          Enables the on-change update strategy for this state template, meaning the state template is refreshed whenever the vehicle's signal values change rather than on a periodic schedule. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="periodic" type="object">
          The periodic update strategy for a state template, indicating that the vehicle should send state updates at a fixed interval configured by its subfields. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state_template_update_rate" type="object">
              The state_template_update_rate field defines the interval (e.g., a duration such as '1d' or '1h') at which the periodic state template update strategy triggers an update for the FleetWise vehicle. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="unit" type="string">
                  The unit of time (e.g., MILLISECONDS, SECONDS, MINUTES, HOURS) for the periodic state template update rate, which determines how often the state template is updated for the vehicle. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The periodic state template update rate in seconds, which determines how often the vehicle sends state template data to AWS IoT FleetWise. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Associates a list of key-value tags with the vehicle for metadata, categorization, and access control in AWS IoT FleetWise. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag attached to the AWS IoT FleetWise vehicle, used for organizing and identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a single tag attached to the AWS IoT FleetWise vehicle, allowing you to assign arbitrary metadata to the vehicle resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the vehicle in AWS IoT FleetWise. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="creation_time" type="string">
  The timestamp (ISO 8601 format) indicating when this vehicle was created in AWS IoT FleetWise. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="last_modification_time" type="string">
  The timestamp of the most recent update to the vehicle's configuration or metadata in AWS IoT FleetWise. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
