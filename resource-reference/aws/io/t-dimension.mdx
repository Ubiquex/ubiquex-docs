---
title: "aws_io_t_dimension"
description: "`aws_io_t_dimension` scopes an `aws_io_t_security_profile`'s own behavior checks to a real, defined subset, a specific set of MQTT topics..."
---

`aws_io_t_dimension` scopes an `aws_io_t_security_profile`'s own behavior checks to a real, defined subset, a specific set of MQTT topics, for instance, rather than the profile applying uniformly across everything a device does.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own t dimension"})

    		ubx.Resource(io.TDimension, "ci-runner", io.TDimensionConfig{
    			StringValues: []string{"example"},
    			Type:         "example",
    			Name:         "ci-runner",
    			Tags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TDimension } from "./local-sdk/aws/sdk/typescript/aws/io/t_dimension.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own t dimension" });

      resource(TDimension, "ci-runner", {
        stringValues: ["example"],
        type: "example",
        name: "ci-runner",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TDimension, TDimensionConfig

    def describe():
        ubx.intent("payments own t dimension")

        ubx.resource(TDimension, "ci-runner", TDimensionConfig(
            string_values=["example"],
            type="example",
            name="ci-runner",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_dimension called "ci-runner", with string values
    a set value, type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string">
  A unique identifier for the dimension. Optional.
</ResponseField>

<ResponseField name="string_values" type="list(string)" required>
  Specifies the value or list of values for the dimension. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Metadata that can be used to manage the dimension. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The tag value, paired with its key, assigns arbitrary metadata to the AWS IoT Dimension resource for categorization, filtering, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Specifies the type of the dimension. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN (Amazon resource name) of the created dimension. Computed by AWS after `ship`.
</ResponseField>
