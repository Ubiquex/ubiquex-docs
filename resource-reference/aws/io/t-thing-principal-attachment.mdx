---
title: "aws_io_t_thing_principal_attachment"
description: "`aws_io_t_thing_principal_attachment` is the real, actual link between an `aws_io_t_thing` and its `aws_io_t_certificate`."
---

A certificate with no thing attachment can still authenticate an MQTT connection but has no thing identity behind it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own t thing principal attachment"})

    		ubx.Resource(io.TThingPrincipalAttachment, "ci-runner", io.TThingPrincipalAttachmentConfig{
    			Principal:          "example",
    			ThingName:          "example-thing-name",
    			ThingPrincipalType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TThingPrincipalAttachment } from "./local-sdk/aws/sdk/typescript/aws/io/t_thing_principal_attachment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own t thing principal attachment" });

      resource(TThingPrincipalAttachment, "ci-runner", {
        principal: "example",
        thingName: "example-thing-name",
        thingPrincipalType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TThingPrincipalAttachment, TThingPrincipalAttachmentConfig

    def describe():
        ubx.intent("payments own t thing principal attachment")

        ubx.resource(TThingPrincipalAttachment, "ci-runner", TThingPrincipalAttachmentConfig(
            principal="example",
            thing_name="example-thing-name",
            thing_principal_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_thing_principal_attachment, with principal
    example, thing name example-thing-name, thing principal type
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="principal" type="string" required>
  The ARN of the AWS IoT certificate or Amazon Cognito identity that is attached as a principal to the IoT thing. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  The name of the AWS IoT thing to which the principal (certificate or Cognito identity) is attached. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="thing_principal_type" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
