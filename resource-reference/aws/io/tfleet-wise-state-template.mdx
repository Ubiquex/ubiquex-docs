---
title: "aws_io_tfleet_wise_state_template"
description: "`aws_io_tfleet_wise_state_template` tracks a real, defined set of signals' current state on a vehicle..."
---

`aws_io_tfleet_wise_state_template` tracks a real, defined set of signals' current state on a vehicle, reported on change or on a periodic interval by the Edge Agent, distinct from an `aws_io_tfleet_wise_campaign`'s own bulk data-collection model. Part of AWS IoT FleetWise, which is no longer open to new customers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tfleet wise state template"})

    		ubx.Resource(io.TfleetWiseStateTemplate, "ci-runner", io.TfleetWiseStateTemplateConfig{
    			Name:                    "ci-runner",
    			SignalCatalogArn:        "arn:aws:iam::123456789012:policy/ci",
    			StateTemplateProperties: []string{"example"},
    			DataExtraDimensions:     []string{"example"},
    			Description:             "Managed by ubx.",
    			MetadataExtraDimensions: []string{"example"},
    			Tags:                    []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TfleetWiseStateTemplate } from "./local-sdk/aws/sdk/typescript/aws/io/tfleet_wise_state_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own tfleet wise state template" });

      resource(TfleetWiseStateTemplate, "ci-runner", {
        name: "ci-runner",
        signalCatalogArn: "arn:aws:iam::123456789012:policy/ci",
        stateTemplateProperties: ["example"],
        dataExtraDimensions: ["example"],
        description: "Managed by ubx.",
        metadataExtraDimensions: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TfleetWiseStateTemplate, TfleetWiseStateTemplateConfig

    def describe():
        ubx.intent("payments own tfleet wise state template")

        ubx.resource(TfleetWiseStateTemplate, "ci-runner", TfleetWiseStateTemplateConfig(
            name="ci-runner",
            signal_catalog_arn="arn:aws:iam::123456789012:policy/ci",
            state_template_properties=["example"],
            data_extra_dimensions=["example"],
            description="Managed by ubx.",
            metadata_extra_dimensions=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_tfleet_wise_state_template called "ci-runner", with
    signal catalog arn arn:aws:iam::123456789012:policy/ci, state
    template properties a set value, data extra dimensions a set
    value, description Managed by ubx., metadata extra dimensions a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_extra_dimensions" type="list(string)">
  Specifies a list of extra dimension signals (e.g., Vehicle.Make, Vehicle.Class) to be collected alongside the signals defined in the state template, enriching the vehicle data with additional metadata. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A brief, user-provided description of the AWS IoT FleetWise state template, which defines a set of vehicle state signals to collect. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="metadata_extra_dimensions" type="list(string)">
  Specifies a list of additional vehicle attribute names (e.g., make, model) to be included as metadata dimensions in the state template's collected data. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The required, user-defined name that identifies the state template within AWS IoT FleetWise. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="signal_catalog_arn" type="string" required>
  The Amazon Resource Name (ARN) of the AWS IoT FleetWise signal catalog that this state template is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="state_template_properties" type="list(string)" required>
  The `state_template_properties` field specifies the list of signal or property names (as strings) that define the set of vehicle data points to collect when using this state template in AWS IoT FleetWise. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of Tag objects (each with a Key and Value) that are applied to the AWS IoT FleetWise state template for resource organization and cost allocation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS IoT FleetWise state template, used for resource categorization, cost allocation, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Defines the tag value for a tag attached to the AWS IoT FleetWise state template, which can be used to categorize or identify the template. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the state template, which uniquely identifies it within AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="creation_time" type="string">
  The date and time the state template was created in AWS IoT FleetWise. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The read-only unique identifier (a UUID) that AWS IoT FleetWise assigns to this state template upon creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="last_modification_time" type="string">
  The timestamp of the most recent modification to the state template, automatically set by AWS IoT FleetWise when the resource is created or updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
