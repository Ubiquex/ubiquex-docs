---
title: "aws_io_ttwin_maker_component_type"
description: "`aws_io_ttwin_maker_component_type` is a real, reusable definition of properties and behavior, a temperature sensor's own data schema, for instance..."
---

`aws_io_ttwin_maker_component_type` is a real, reusable definition of properties and behavior, a temperature sensor's own data schema, for instance, that an `aws_io_ttwin_maker_entity` in an `aws_io_ttwin_maker_workspace` can be built from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	io "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/io"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ttwin maker component type"})

    		ubx.Resource(io.TtwinMakerComponentType, "example", io.TtwinMakerComponentTypeConfig{
    			ComponentTypeId:         "example",
    			WorkspaceId:             "example",
    			CompositeComponentTypes: "example",
    			Description:             "Managed by ubx.",
    			ExtendsFrom:             []string{"example"},
    			Functions:               "example",
    			IsSingleton:             true,
    			PropertyDefinitions:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { TtwinMakerComponentType } from "./local-sdk/aws/sdk/typescript/aws/io/ttwin_maker_component_type.ts";

    export default stack("example", () => {
      intent({ summary: "example own ttwin maker component type" });

      resource(TtwinMakerComponentType, "example", {
        componentTypeId: "example",
        workspaceId: "example",
        compositeComponentTypes: "example",
        description: "Managed by ubx.",
        extendsFrom: ["example"],
        functions: "example",
        isSingleton: true,
        propertyDefinitions: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.io import TtwinMakerComponentType, TtwinMakerComponentTypeConfig

    def describe():
        ubx.intent("example own ttwin maker component type")

        ubx.resource(TtwinMakerComponentType, "example", TtwinMakerComponentTypeConfig(
            component_type_id="example",
            workspace_id="example",
            composite_component_types="example",
            description="Managed by ubx.",
            extends_from=["example"],
            functions="example",
            is_singleton=True,
            property_definitions="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_ttwin_maker_component_type, with component type id
    example, workspace id example, composite component types example,
    description Managed by ubx., extends from a set value, functions
    example, is singleton true, property definitions example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="component_type_id" type="string" required>
  The ID of the component type. Required.
</ResponseField>

<ResponseField name="composite_component_types" type="any">
  An map of the composite component types in the component type. Each composite component type's key must be unique to this map. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the component type. Optional.
</ResponseField>

<ResponseField name="extends_from" type="list(string)">
  Specifies the parent component type to extend. Optional.
</ResponseField>

<ResponseField name="functions" type="any">
  a Map of functions in the component type. Each function's key must be unique to this map. Optional.
</ResponseField>

<ResponseField name="is_singleton" type="bool">
  A Boolean value that specifies whether an entity can have more than one component of this type. Optional.
</ResponseField>

<ResponseField name="property_definitions" type="any">
  An map of the property definitions in the component type. Each property definition's key must be unique to this map. Optional.
</ResponseField>

<ResponseField name="property_groups" type="any">
  An map of the property groups in the component type. Each property group's key must be unique to this map. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A map of key-value pairs to associate with a resource. Optional.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  The ID of the workspace that contains the component type. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the component type. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_date_time" type="string">
  The date and time when the component type was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="is_abstract" type="bool">
  A Boolean value that specifies whether the component type is abstract. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_schema_initialized" type="bool">
  A Boolean value that specifies whether the component type has a schema initializer and that the schema initializer has run. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="object">
  The status object indicates the current lifecycle state of the component type (e.g., CREATING, UPDATING, ACTIVE, ERROR) and includes error details if an operation failed. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="error" type="object">
      The error object (containing code and message) reported for this AWS IoT TwinMaker component type when its status is in an error state (e.g., after a failed create or update). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          The error code that identifies the type of failure encountered when AWS IoT TwinMaker processed this component type. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="message" type="string">
          The message of the error that caused the component type to fail, providing details about what went wrong. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      Represents the lifecycle state of the component type (e.g., CREATING, UPDATING, ACTIVE, DELETING, or ERROR) as reported in the status object returned by AWS IoT TwinMaker. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_date_time" type="string">
  The date and time when the component type was last updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
