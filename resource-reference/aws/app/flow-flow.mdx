---
title: "aws_app_flow_flow"
description: "`aws_app_flow_flow` defines one real data transfer between a source and destination, on demand or on a schedule..."
---

`aws_app_flow_flow` defines one real data transfer between a source and destination, on demand or on a schedule, using the connections an `aws_app_flow_connector_profile` establishes. It's the actual unit of work Amazon AppFlow runs, field mappings, transformations, and trigger included.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appflow "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/appflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own flow flow"})

    		ubx.Resource(appflow.Flow, "example", appflow.FlowConfig{
    			DestinationFlowConfigList: []map[string]any{{"api_version": "example"}},
    			FlowName:                  "example-flow-name",
    			SourceFlowConfig:          map[string]any{"connector_type": "example"},
    			Tasks:                     []map[string]any{{"connector_operator": map[string]any{"amplitude": "example"}}},
    			TriggerConfig:             map[string]any{"trigger_type": "example"},
    			Description:               "Managed by ubx.",
    			FlowStatus:                "example",
    			Kmsarn:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Flow } from "@ubx/sdk-aws/aws/appflow/flow";

    export default stack("example", () => {
      intent({ summary: "example own flow flow" });

      resource(Flow, "example", {
        destinationFlowConfigList: [{ apiVersion: "example" }],
        flowName: "example-flow-name",
        sourceFlowConfig: { connectorType: "example" },
        tasks: [{ connectorOperator: { amplitude: "example" } }],
        triggerConfig: { triggerType: "example" },
        description: "Managed by ubx.",
        flowStatus: "example",
        kmsarn: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appflow.flow import Flow, FlowConfig

    def describe():
        ubx.intent("example own flow flow")

        ubx.resource(Flow, "example", FlowConfig(
            destination_flow_config_list=[{"api_version": "example"}],
            flow_name="example-flow-name",
            source_flow_config={"connector_type": "example"},
            tasks=[{"connector_operator": {"amplitude": "example"}}],
            trigger_config={"trigger_type": "example"},
            description="Managed by ubx.",
            flow_status="example",
            kmsarn="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_flow_flow, with destination flow config list a set
    value, flow name example-flow-name, source flow config a set
    value, tasks a set value, trigger config a set value, description
    Managed by ubx., flow status example, kmsarn example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Description of the flow. Optional.
</ResponseField>

<ResponseField name="destination_flow_config_list" type="list(object)" required>
  List of Destination connectors of the flow. Required.
  <Expandable title="properties">
    <ResponseField name="api_version" type="string">
      Specifies the API version that the destination connector uses to interact with the target service when this flow writes data to the destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="connector_profile_name" type="string">
      The name of the connector profile that specifies the destination connector's authentication and access settings, used by AppFlow to write data to the target system. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="connector_type" type="string">
      Specifies the connector type for the destination, such as Salesforce, Amazon S3, or Snowflake, which determines how AppFlow writes data to that destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="destination_connector_properties" type="object">
      Specifies the connector-specific properties required to configure the destination, such as the Amazon S3 bucket name, prefix, and output format when the destination is S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_connector" type="object">
          Defines the destination properties for a custom connector in AppFlow, specifying the target entity name and any custom properties required to write data to the third-party application. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_properties" type="any">
              A map of custom key-value properties required by the custom connector to configure the destination when writing data to the target system. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="entity_name" type="string">
              The name of the entity (such as a table or object) in the custom connector destination that the AppFlow flow writes data to. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error_handling_config" type="object">
              Specifies how errors are handled when AppFlow writes data to the custom connector destination, including the S3 bucket and optional prefix for storing failed records and whether the flow fails on the first error. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket used to store error records when AppFlow fails to write data to the custom connector destination, as part of the error handling configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The object key prefix to apply to error records that AppFlow writes to the configured S3 bucket when processing fails for this custom connector destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  Specifies whether the flow stops processing at the first error, rather than continuing and writing error details to the configured error handling bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="id_field_names" type="list(string)">
              Specifies the list of field names that uniquely identify records in the custom connector destination, used to determine record identity for deduplication or upsert operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_operation_type" type="string">
              Specifies the write operation type (INSERT, UPDATE, UPSERT, or DELETE) that AppFlow performs on the custom connector destination when transferring data. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="event_bridge" type="object">
          Specifies the destination connector properties for Amazon EventBridge, including the name of the EventBridge event bus (the Object property) that receives the flow's data. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_handling_config" type="object">
              Configures how AppFlow handles errors when writing to the EventBridge destination, including the Amazon S3 bucket and prefix for storing error records and whether the flow fails on the first destination error. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket where AppFlow stores failed records when error handling is configured for the destination EventBridge connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  Specifies the S3 object key prefix under which error logs and failed event delivery records are stored when a flow targeting Amazon EventBridge encounters data processing errors. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  Determines whether the flow immediately fails when the first destination write error occurs, instead of skipping the failed record and continuing with the remaining data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="object" type="string">
              Specifies the name of the Amazon EventBridge event bus that this AppFlow flow targets as the destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="lookout_metrics" type="object">
          Defines the destination configuration for Amazon Lookout for Metrics, including the required `object` field that identifies the target dataset (measure set) in Lookout for Metrics. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string">
              The name of the Amazon Lookout for Metrics dataset (object) that receives the data from the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="marketo" type="object">
          Contains the destination-specific settings for Amazon AppFlow to write data to Marketo, including the required Marketo object name and optional error handling configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_handling_config" type="object">
              Specifies how errors encountered during data transfer to Marketo are handled, including the Amazon S3 bucket and prefix for storing error logs and whether to fail on the first error. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket that AppFlow uses to store error logs and failed records when handling errors for the Marketo destination connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The S3 prefix (folder path) within the configured Amazon S3 bucket where AppFlow writes error logs or failed records when transferring data to the Marketo destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  Indicates whether the Amazon AppFlow flow stops and fails on the first error encountered when writing to Marketo, rather than continuing with subsequent records. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="object" type="string">
              The Marketo object (such as Lead or Company) in the destination Marketo instance that the flow writes records to. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="redshift" type="object">
          Specifies the configuration for writing data to an Amazon Redshift destination, including the S3 staging bucket (intermediateBucketName), the target Redshift table/object name, and optional error handling settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_prefix" type="string">
              The object key prefix under which AppFlow writes staged data files in the intermediate S3 bucket before loading them into the Amazon Redshift destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error_handling_config" type="object">
              Defines how errors encountered while writing data to Amazon Redshift are handled, including options to fail the flow on the first error or write error logs to a specified S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The S3 bucket name that AppFlow uses to store error logs or failed records when the Redshift destination connector encounters errors during a flow run. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The prefix applied to the S3 bucket path where AppFlow stores error logs or failed records when writing to Redshift, allowing you to organize error files under a common folder. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  When set to true, the AppFlow flow stops immediately at the first error encountered while writing to the Redshift destination; when false, it continues processing and sends error records to the configured error-handling Amazon S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="intermediate_bucket_name" type="string">
              The name of the Amazon S3 bucket that AppFlow uses as a staging location to write data before copying it into Amazon Redshift. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object" type="string">
              The Amazon S3 object key in the intermediate bucket where the flow stages data before copying it into Redshift. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="s3" type="object">
          Configures Amazon S3 as the destination connector by specifying properties such as the target bucket name, object key prefix, and output format settings for the flow's destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_name" type="string">
              The name of the Amazon S3 bucket where AppFlow writes the data for this destination flow configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bucket_prefix" type="string">
              Specifies the object key prefix prepended to the names of files that AppFlow writes to the destination S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="s3_output_format_config" type="object">
              Configures the output file format and related settings, such as file type, prefix, and aggregation, for data written by the flow to the S3 destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="aggregation_config" type="object">
                  Specifies the aggregation settings (e.g., None or SingleFile) that control whether AppFlow combines all flow-run data into a single S3 file or writes multiple files. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="aggregation_type" type="string">
                      Specifies whether the flow's S3 destination output is aggregated into a single file (SingleFile) or kept as separate files (None, the default), directly controlling file grouping in the S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="target_file_size" type="number">
                      Specifies the target file size in megabytes for aggregated files written to Amazon S3 when aggregation is enabled in the flow destination configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  Specifies the file format (such as CSV, JSON, or Parquet) for the output files that AppFlow writes to the destination S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_config" type="object">
                  Defines the prefix type and format applied to S3 object keys (e.g., EXECUTION_ID, PATH, or a custom prefix) to organize output files in the configured S3 destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_prefix_hierarchy" type="list(string)">
                      Specifies a list of source data fields whose values are used to construct a hierarchical path prefix in the S3 destination bucket, enabling partition-based organization of flow output files. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="prefix_format" type="string">
                      Sets the timestamp resolution (YEAR, MONTH, DAY, HOUR, or MINUTE) used to generate the dynamic prefix for Amazon S3 output objects, controlling how files are grouped by time in the destination bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="prefix_type" type="string">
                      Determines whether the prefix is applied to the individual file name (FILENAME) or to the entire object key path (PATH) for Amazon S3 destinations in AppFlow. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="preserve_source_data_typing" type="bool">
                  When enabled, this boolean preserves the original data types (such as string, integer, or boolean) of source fields when AppFlow writes to the S3 destination, instead of converting them, ensuring type fidelity during the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="salesforce" type="object">
          Defines the Salesforce-specific destination properties for a flow, such as the target Salesforce object and the write operation type (e.g., INSERT, UPDATE, UPSERT), used when AppFlow writes data to Salesforce. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="data_transfer_api" type="string">
              Specifies the Salesforce Data Transfer API (e.g., REST, BULK, BULKV2) that AppFlow uses to write data to the destination Salesforce object. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error_handling_config" type="object">
              Specifies error handling for the Salesforce destination, including the Amazon S3 bucket and prefix for error records and whether the flow fails on the first error. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket where AppFlow stores error logs and failed record data when writes to the Salesforce destination fail. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The S3 object key prefix that is prepended to the filenames when AppFlow writes error handling files (such as failed records) to the specified error handling bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  When true, the AppFlow flow stops processing and fails immediately on the first record write error to Salesforce, rather than skipping the failed record and continuing with remaining records. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="id_field_names" type="list(string)">
              Specifies the list of field names in the Salesforce object that are used as external identifiers for upsert operations when writing records to the destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object" type="string">
              The Salesforce object name (e.g., Account, Contact) into which the flow writes the destination records. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_operation_type" type="string">
              Specifies the write operation type (INSERT, UPDATE, or UPSERT) that AppFlow performs when writing data to the Salesforce destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sapodata" type="object">
          This object specifies the SAPOData destination-specific settings for the AppFlow flow, such as the target SAP object path and the configuration for error and success response handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_handling_config" type="object">
              Configures error handling for the SAPOData destination connector, allowing you to specify the S3 bucket where failed records are written and whether the flow stops on the first error. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket where AppFlow writes error logs or failed records when a transfer to the SAPOData destination fails. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The Amazon S3 object key prefix under which error logs are stored for this SAPOData destination when flow runs encounter errors, as part of the error handling configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  Determines whether the flow fails on the first error encountered during data transfer to the SAPOData destination, causing the flow run to stop immediately instead of processing remaining records. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="id_field_names" type="list(string)">
              The list of field names that serve as the unique identifier (primary key) for records written to the SAPOData destination, enabling upsert and deduplication operations in the AppFlow flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object_path" type="string">
              The object path for the SAP OData entity to which data is written, such as the entity set name in the SAP OData service. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="success_response_handling_config" type="object">
              Configuration specifying the Amazon S3 bucket and optional prefix where AWS AppFlow stores the response payload for successfully processed records when using SAPOData as a destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the S3 bucket where AppFlow stores the success response payload for SAPOData destination operations when response handling is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The S3 object key prefix under which successful response files are saved when the SAPOData destination writes response data to the configured Amazon S3 bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="write_operation_type" type="string">
              Specifies the write operation type (such as INSERT, UPDATE, or UPSERT) that AppFlow uses when writing data to the SAP OData destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="snowflake" type="object">
          Configuration for Snowflake as the destination connector, including target object, intermediate bucket, and error handling settings, within an AppFlow flow's destination properties. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_prefix" type="string">
              The object key prefix applied to files staged in the intermediate S3 bucket before Amazon AppFlow loads them into the Snowflake table. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="error_handling_config" type="object">
              Configuration for handling errors when writing to Snowflake, including an S3 bucket and prefix for storing failed records and an option to fail the flow on the first error. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket where AWS AppFlow writes failed records when error handling is configured for the Snowflake destination connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  The S3 object key prefix under the error-handling bucket where AppFlow writes failed records for Snowflake destination flows. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  If set to true, the AppFlow flow fails immediately on the first error encountered while writing to Snowflake; if false, the flow continues and errors are handled according to the surrounding error handling configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="intermediate_bucket_name" type="string">
              The name of the Amazon S3 bucket used as an intermediate staging location for data before it is loaded into Snowflake. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object" type="string">
              The name of the target Snowflake table (object) where the flow writes the transferred data. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="upsolver" type="object">
          Configuration for the Upsolver destination connector, defining the target S3 bucket, optional prefix, and output format for data delivery via AppFlow. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_name" type="string">
              The name of the Amazon S3 bucket to which Upsolver writes the data flowing from AppFlow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bucket_prefix" type="string">
              The Amazon S3 object key prefix under which Upsolver writes the flow's output files, used to organize and locate exported data within the destination bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="s3_output_format_config" type="object">
              Specifies the S3 output format for Upsolver destination, including the file type, aggregation configuration, and S3 key prefix settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="aggregation_config" type="object">
                  Determines how Upsolver aggregates the data files written to Amazon S3, allowing you to specify an aggregation type such as 'None' or 'SingleFile' to control whether records are written as individual objects or combined into a single file. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="aggregation_type" type="string">
                      Specifies how Upsolver aggregates data in Amazon S3 output, with values such as 'None' for individual records or 'Aggregate' to combine records based on the configured aggregation threshold. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="target_file_size" type="number">
                      The target file size (in megabytes) that Upsolver uses to determine when to roll over aggregated records into a new output file in Amazon S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="file_type" type="string">
                  Specifies the format (such as JSON or CSV) of the data files that AppFlow writes to Amazon S3 when using Upsolver as the destination connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="prefix_config" type="object">
                  Defines the prefix (folder structure and naming) for S3 object keys when Upsolver writes output data to Amazon S3, including the prefix type and format used to organize files. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="path_prefix_hierarchy" type="list(string)">
                      Specifies the list of path prefix hierarchy components (e.g., EXECUTION_ID, SCHEMA_VERSION, CONNECTOR_ENTITY) that Amazon AppFlow uses to organize output files written by the Upsolver connector to Amazon S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="prefix_format" type="string">
                      Determines the time granularity (such as YEAR, MONTH, DAY, or HOUR) that AppFlow appends to the S3 object key prefix when delivering data to Upsolver, controlling how output files are partitioned by time. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="prefix_type" type="string">
                      Specifies whether the prefix for S3 object keys is configured as a filename prefix or a path prefix when Upsolver writes data to Amazon S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="zendesk" type="object">
          Defines the Zendesk-specific destination connector settings, such as the target Zendesk object, that AppFlow uses when writing flow records to Zendesk. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_handling_config" type="object">
              Specifies the error handling configuration (such as the Amazon S3 bucket for storing error records and whether to fail on the first error) for records that fail during transfer to the Zendesk destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="bucket_name" type="string">
                  The name of the Amazon S3 bucket where AppFlow stores error records and logs when the Zendesk destination connector fails during a flow run. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="bucket_prefix" type="string">
                  Amazon S3 bucket prefix under which error/failed records for the Zendesk destination connector are stored when error handling is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fail_on_first_error" type="bool">
                  Controls whether an Amazon AppFlow flow stops and fails on the first error encountered when writing data to Zendesk, or continues and logs errors to the configured error handling bucket. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="id_field_names" type="list(string)">
              A list of field names that serve as the unique identifier (primary key) for records written to the Zendesk destination, enabling AppFlow to upsert records correctly. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object" type="string">
              Specifies the Zendesk object (such as Ticket or User) that Amazon AppFlow writes the flow's records into as the destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="write_operation_type" type="string">
              Specifies the write operation type (INSERT, UPDATE, or UPSERT) used when writing data to the Zendesk destination in the AppFlow flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="flow_name" type="string" required>
  Name of the flow. Required.
</ResponseField>

<ResponseField name="flow_status" type="string">
  Flow activation status for Scheduled- and Event-triggered flows Optional.
</ResponseField>

<ResponseField name="kmsarn" type="string">
  The ARN of the AWS Key Management Service (AWS KMS) key that's used to encrypt your function's environment variables. If it's not provided, AWS Lambda uses a default service key. Optional.
</ResponseField>

<ResponseField name="metadata_catalog_config" type="object">
  Configurations of metadata catalog of the flow. Optional.
  <Expandable title="properties">
    <ResponseField name="glue_data_catalog" type="object">
      Trigger settings of the flow. Optional.
      <Expandable title="properties">
        <ResponseField name="database_name" type="string" required>
          A string containing the value for the tag Required.
        </ResponseField>
        <ResponseField name="role_arn" type="string" required>
          A string containing the value for the tag Required.
        </ResponseField>
        <ResponseField name="table_prefix" type="string" required>
          A string containing the value for the tag Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_flow_config" type="object" required>
  Configurations of Source connector of the flow. Required.
  <Expandable title="properties">
    <ResponseField name="api_version" type="string">
      The API version that the connector will use. Optional.
    </ResponseField>
    <ResponseField name="connector_profile_name" type="string">
      Name of connector profile Optional.
    </ResponseField>
    <ResponseField name="connector_type" type="string" required>
      Specifies the source connector type for the AppFlow flow, such as 'Salesforce' or 'S3', which determines the service from which the flow reads data. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="incremental_pull_config" type="object">
      Configuration for scheduled incremental data pull Optional.
      <Expandable title="properties">
        <ResponseField name="datetime_type_field_name" type="string">
          Name of the datetime/timestamp data type field to be used for importing incremental records from the source Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="source_connector_properties" type="object" required>
      Source connector details required to query a connector Required.
      <Expandable title="properties">
        <ResponseField name="amplitude" type="object">
          This object specifies the Amplitude source connector properties, particularly the name of the Amplitude object (for example, 'event') that the flow retrieves data from. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              The name of the Amplitude object (such as Event or User) from which the flow retrieves data. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom_connector" type="object">
          Specifies the source connector properties for a custom connector in an AppFlow flow, including the entity (or object) name to read from and any custom key-value configuration parameters required by the connector. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="custom_properties" type="any">
              A map for properties for custom connector. Optional.
            </ResponseField>
            <ResponseField name="data_transfer_api" type="object">
              Specifies the data transfer API configuration for the custom connector source, including the API name, path, and transfer type (SYNC or ASYNC) that AWS AppFlow uses to retrieve data. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string" required>
                  Specifies the name of the custom connector API operation (as defined in the connector's API definitions) that AppFlow calls to retrieve data from the source. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="type" type="string" required>
                  Specifies whether the custom connector's data transfer API is synchronous (SYNC) or asynchronous (ASYNC). Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="entity_name" type="string" required>
              The name of the entity (e.g., object, table, or API endpoint) in the custom connector from which the AppFlow flow retrieves data. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="datadog" type="object">
          Defines the Datadog source configuration for the flow, where the ApiKey and ApplicationKey fields provide the credentials needed to connect to and pull data from Datadog. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              Specifies the Datadog object name (such as an event, metric, or other Datadog data source) from which AppFlow reads data in the flow. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="dynatrace" type="object">
          Holds the source connector properties specific to a Dynatrace source, including the Dynatrace object (entity) from which data flow retrieves data. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              The Dynatrace entity type (object) from which the AppFlow flow extracts data, such as a host or application, as required by the Dynatrace source connector configuration. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="google_analytics" type="object">
          Specifies the Google Analytics source properties, including the object (report) name from which AppFlow reads data. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              Specifies the Google Analytics report (object) from which the flow retrieves data, such as 'ga_sessions' or 'ga_users'. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="infor_nexus" type="object">
          Configures the Infor Nexus source connector properties, specifically defining the object (data entity) from which the AppFlow flow retrieves data. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              For an Infor Nexus source connector in AWS AppFlow, this required field specifies the name of the Infor Nexus object (such as a business document or data entity) from which the flow reads data. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="marketo" type="object">
          Configures Marketo as a source in the flow, defining the Marketo object (e.g., Lead or Activity) from which AppFlow retrieves data. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              The name of the Marketo object (e.g., Lead, Activity) that Amazon AppFlow pulls records from as the source. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pardot" type="object">
          Configures the source connector details for a Pardot (Salesforce Pardot) source, including the Pardot object name that identifies which object's records to ingest. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              The specific Pardot object (e.g., Prospect or Campaign) that the AppFlow flow retrieves records from as its data source. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="s3" type="object">
          Specifies the S3 source connector settings in AppFlow, such as the bucket name, optional object prefix, and the format of the input files (e.g., CSV or JSON) to read from Amazon S3. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bucket_name" type="string" required>
              The name of the S3 bucket from which the AppFlow flow reads source data for ingestion. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="bucket_prefix" type="string" required>
              The object key prefix used to filter S3 objects for the source connector in the AppFlow flow. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="s3_input_format_config" type="object">
              Determines the file format (for example, CSV or JSON) that the S3 source connector expects when reading source files for the AppFlow flow. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="s3_input_file_type" type="string">
                  Specifies the file format of the source data in S3 (e.g., CSV, JSON, or Parquet) for the AppFlow flow's S3 source. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="salesforce" type="object">
          Defines the Salesforce-specific source configuration for the flow, including the Salesforce object to read from and optionally the fields to retrieve. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="data_transfer_api" type="string">
              Specifies whether AppFlow uses the Salesforce REST or Bulk API to transfer data from Salesforce for this source connection. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enable_dynamic_field_update" type="bool">
              When enabled, Amazon AppFlow updates the Salesforce source records with the new field values written to the destination during the flow run, allowing dynamic field updates on matching source records. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_deleted_records" type="bool">
              When true, includes records that have been soft-deleted in Salesforce (i.e., moved to the Recycle Bin) in the source data pulled by this AppFlow flow. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object" type="string" required>
              The name of the Salesforce object (e.g., Account, Contact) from which the AppFlow flow source reads data. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sapodata" type="object">
          This object defines the source-specific properties for the SAPOData connector in AWS AppFlow, such as the OData path representing the entity collection from which the flow retrieves data. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object_path" type="string" required>
              The object path within the SAP system (such as an OData entity or report path) that the AppFlow flow reads data from when using SAPOData as the source. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="pagination_config" type="object">
              SAP Source connector page size Optional.
              <Expandable title="properties">
                <ResponseField name="max_page_size" type="number" required>
                  Specifies the maximum number of records to retrieve in a single page when reading data from the SAPOData source in the flow. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="parallelism_config" type="object">
              SAP Source connector parallelism factor Optional.
              <Expandable title="properties">
                <ResponseField name="max_parallelism" type="number" required>
                  Specifies the maximum number of parallel requests that AppFlow can make to the SAP OData source during a flow run. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_now" type="object">
          Specifies the ServiceNow-specific source connector properties, including the ServiceNow object (e.g., Incident or Problem) that the flow retrieves data from. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              The ServiceNow object name (such as a table) whose records are retrieved by this AppFlow source connector. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="singular" type="object">
          Contains the source configuration specific to the Singular connector, such as the Singular object from which the flow reads data, when Singular is selected as the source connector. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              Specifies the Singular object (data entity) from which AppFlow reads data in the source step of the flow. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="slack" type="object">
          Configures the Slack source connector properties, defining the Slack object (e.g., 'Channel' or 'Message') that the flow reads data from. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              Specifies the name of the Slack object (e.g., a conversation or channel) that the flow will pull data from as its source. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="trendmicro" type="object">
          An object that specifies the request parameters for a Trend Micro source connector in AppFlow, including the Trend Micro object name to be used as the data source. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              Specifies the Trend Micro object name that the source connector queries to fetch records. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="veeva" type="object">
          This object defines the Veeva-specific source connector settings, including the Veeva object to retrieve data from and optionally the document type, when Veeva is the flow's source. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="document_type" type="string">
              The document type used to filter which Veeva documents are pulled by the flow, such as 'All' or a specific Veeva document type. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_all_versions" type="bool">
              When set to true, includes all historical versions of Veeva document objects in the source flow; when false, only the latest version is included. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_renditions" type="bool">
              Specifies whether to include renditions (alternate versions of documents) from Veeva in the flow. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_source_files" type="bool">
              Specifies whether AppFlow includes source files (attachment files) from the Veeva object in the data that is transferred by the flow. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="object" type="string" required>
              The name of the Veeva CRM object (e.g., Account, Contact) that the AppFlow flow reads data from as a source. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="zendesk" type="object">
          Specifies the Zendesk source connector properties for the AppFlow flow, including the Zendesk object (such as 'tickets' or 'users') from which data is retrieved. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="object" type="string" required>
              The name of the Zendesk object (such as Ticket, User, or Organization) that this flow reads data from. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  List of Tags. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value string of a tag attached to the AppFlow flow, used to store custom metadata that helps identify and manage the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tasks" type="list(object)" required>
  List of tasks for the flow. Required.
  <Expandable title="properties">
    <ResponseField name="connector_operator" type="object">
      Specifies the connector-specific operation (e.g., PROJECTION, EQUALITY, GREATER_THAN) that Amazon AppFlow applies to the source fields when executing this task. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="amplitude" type="string">
          For the Amplitude connector, specifies the operation (e.g., EQUAL_TO, GREATER_THAN, LESS_THAN, or BETWEEN) that AppFlow applies to the source data in the task. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="custom_connector" type="string">
          The operator to be applied for a custom connector in an AppFlow flow task, which is a string value defined by the custom connector's schema and controls the data transformation operation (e.g., projection, comparison) performed on the field. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="datadog" type="string">
          Selects the Datadog-specific connector operator (a string value from the set supported by AppFlow's Datadog connector) that defines the operation performed on the data for this flow task. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dynatrace" type="string">
          Specifies the Dynatrace connector operator (e.g., PROJECTION or EQUAL_TO) that this task applies to data when processing via AppFlow, as one of the enumerations supported by the Dynatrace connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="google_analytics" type="string">
          Defines the operator applied to Google Analytics records in a flow task, such as EQUAL_TO or BETWEEN, used when the flow's source or destination is Google Analytics. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="infor_nexus" type="string">
          Specifies the Infor Nexus-specific operator (e.g., PROJECTION, EQUAL_TO) applied to the source field in this AppFlow flow task's connector operator configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="marketo" type="string">
          Determines the Marketo connector-specific operation applied to the task's source field, such as EQUAL_TO, BETWEEN, or MASK_FIRST_N. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pardot" type="string">
          The connector-specific operator for the Pardot connector, which determines how the task processes source fields (e.g., PROJECTION for selection or EQUAL_TO for filtering). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3" type="string">
          Determines the S3 connector operator to apply to source fields in this AppFlow task, controlling operations such as field projection, filtering, or masking when the flow interacts with Amazon S3. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="salesforce" type="string">
          The Salesforce-specific operator to apply to the source data for this task within an AppFlow flow, such as PROJECTION, EQUAL_TO, or BETWEEN. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sapodata" type="string">
          Specifies the SAPOData operation to apply to source data, such as PROJECTION, LESS_THAN, GREATER_THAN, or BETWEEN, when the flow uses an SAPOData connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="service_now" type="string">
          The ServiceNow-specific operator for the flow task, determining the type of operation (such as PROJECTION or EQUALITY) that AppFlow performs when processing data through the ServiceNow connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="singular" type="string">
          Specifies the Singular connector operation (e.g., PROJECTION or EQUALITY) to apply to the data in this AppFlow flow task. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="slack" type="string">
          Specifies the operator (such as PROJECTION) applied to Slack data for this task when Slack is the connector type. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="trendmicro" type="string">
          Specifies the Trend Micro connector operator (e.g., PROJECTION) that determines how source data is projected or filtered in the flow task. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="veeva" type="string">
          Configures the Veeva-specific operator for the flow task, determining the operation applied to the data (e.g., PROJECTION, EQUAL_TO) when the connector is Veeva. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="zendesk" type="string">
          Specifies the Zendesk-specific operator (such as PROJECTION or EQUAL_TO) that this task uses to filter or transform Zendesk data during the AppFlow flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="destination_field" type="string">
      The destination field in the target connector (e.g., Salesforce, S3, or other AppFlow-supported service) where the mapped source data is written during task execution. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_fields" type="list(string)">
      Defines the list of source fields from the source connector that the task applies to, such as fields used for mapping, filtering, or transformation operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="task_properties" type="list(object)">
      The list of task property objects that provide task-specific configuration, such as source and destination data type mappings for map tasks or the write operation type for connector writes. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The key of a task property, which names the specific setting (e.g., SOURCE_DATA_TYPE, DESTINATION_DATA_TYPE, or CONCAT_FORMAT) that configures how the AppFlow flow task processes data. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          The value for a task property key, used to configure task behavior such as data transformation or filtering thresholds in the AppFlow flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="task_type" type="string">
      Specifies the type of transformation operation (e.g., Map, Filter, Aggregate) that this task performs on the data flowing through the AppFlow flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trigger_config" type="object" required>
  Trigger settings of the flow. Required.
  <Expandable title="properties">
    <ResponseField name="trigger_properties" type="object">
      Details required for scheduled trigger type Optional.
      <Expandable title="properties">
        <ResponseField name="data_pull_mode" type="string">
          Specifies whether the scheduled flow pulls only incremental data (Incremental) or all data (Complete) on each scheduled run. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="first_execution_from" type="number">
          The Unix timestamp (in seconds) that defines the earliest time at which the scheduled AppFlow flow's first execution can start, after which the schedule takes effect. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="flow_error_deactivation_threshold" type="number">
          The number of consecutive flow execution errors that must occur before AppFlow automatically deactivates the flow when using a scheduled trigger. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="schedule_end_time" type="number">
          The end time (as epoch seconds or timestamp) that defines the upper bound of the scheduling window for the AppFlow scheduled trigger, after which the flow no longer runs on the defined schedule. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="schedule_expression" type="string" required>
          The cron expression that defines the schedule for the scheduled trigger, such as 'cron(0 12 * * ? *)', determining when the flow runs. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="schedule_offset" type="number">
          The schedule offset, in minutes, that shifts the flow's scheduled trigger execution time relative to the schedule expression, useful for accommodating data source delays. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="schedule_start_time" type="number">
          The timestamp (in seconds since epoch) that defines the earliest time the scheduled flow can begin running, used as the start boundary for the schedule. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_zone" type="string">
          Specifies the IANA time zone (e.g., 'America/New_York') in which the scheduled trigger's cron or rate expression is evaluated; if omitted, the default is UTC. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="trigger_type" type="string" required>
      Specifies whether the AppFlow flow is started on a schedule, in response to an event, or on demand by a manual invocation, and is required in the trigger configuration. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="flow_arn" type="string">
  ARN identifier of the flow. Computed by AWS after `ship`.
</ResponseField>
