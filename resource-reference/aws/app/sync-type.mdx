---
title: "aws_app_sync_type"
description: "`aws_app_sync_type` defines a real, individual GraphQL type, in SDL or JSON format, within an `aws_app_sync_api`..."
---

`aws_app_sync_type` defines a real, individual GraphQL type, in SDL or JSON format, within an `aws_app_sync_api`, a more granular alternative to defining an API's entire schema at once.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sync type"})

    		ubx.Resource(app.SyncType, "example", app.SyncTypeConfig{
    			ApiId:      "example",
    			Definition: "example",
    			Format:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncType } from "./local-sdk/aws/sdk/typescript/aws/app/sync_type.ts";

    export default stack("example", () => {
      intent({ summary: "example own sync type" });

      resource(SyncType, "example", {
        apiId: "example",
        definition: "example",
        format: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.app import SyncType, SyncTypeConfig

    def describe():
        ubx.intent("example own sync type")

        ubx.resource(SyncType, "example", SyncTypeConfig(
            api_id="example",
            definition="example",
            format="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_sync_type, with api id example, definition
    example, format example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  The API ID. Required.
</ResponseField>

<ResponseField name="definition" type="string" required>
  The type definition, in GraphQL Schema Definition Language (SDL) format. Required.
</ResponseField>

<ResponseField name="format" type="string" required>
  The type format: SDL or JSON. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the type. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The type name. Computed by AWS after `ship`.
</ResponseField>
