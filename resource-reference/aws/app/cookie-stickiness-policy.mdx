---
title: "aws_app_cookie_stickiness_policy"
description: "Real, generated bindings for `aws_app_cookie_stickiness_policy`."
---

`aws_app_cookie_stickiness_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"

    cfg := app.CookieStickinessPolicyConfig{
        CookieName: "example-cookie-name",
        LbPort: 1,
        LoadBalancer: "example",
        Name: "example-name",
    }
    ubx.Resource(app.CookieStickinessPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CookieStickinessPolicy } from
      "jsr:@ubx/sdk-aws/aws/app/cookie_stickiness_policy";

    resource(CookieStickinessPolicy, "example", {
      cookieName: "example",
      lbPort: 1,
      loadBalancer: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.app import (
        CookieStickinessPolicy,
        CookieStickinessPolicyConfig,
    )

    cfg = CookieStickinessPolicyConfig(
        cookie_name="example",
        lb_port=1,
        load_balancer="example",
        name="example",
    )
    ubx.resource(CookieStickinessPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_app_cookie_stickiness_policy with cookie_name,
lb_port, load_balancer, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cookie_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
