---
title: "aws_app_cookie_stickiness_policy"
description: "Real, generated bindings for `aws_app_cookie_stickiness_policy`."
---

`aws_app_cookie_stickiness_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cookie stickiness policy"})

    		ubx.Resource(app.CookieStickinessPolicy, "ci-runner", app.CookieStickinessPolicyConfig{
    			CookieName:   "example-cookie-name",
    			LbPort:       1,
    			LoadBalancer: "example",
    			Name:         "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CookieStickinessPolicy } from "jsr:@ubx/sdk-aws/aws/app/cookie_stickiness_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own cookie stickiness policy" });

      resource(CookieStickinessPolicy, "ci-runner", {
        cookieName: "example-cookie-name",
        lbPort: 1,
        loadBalancer: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.app import CookieStickinessPolicy, CookieStickinessPolicyConfig

    def describe():
        ubx.intent("payments own cookie stickiness policy")

        ubx.resource(CookieStickinessPolicy, "ci-runner", CookieStickinessPolicyConfig(
            cookie_name="example-cookie-name",
            lb_port=1,
            load_balancer="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_cookie_stickiness_policy called "ci-runner", with
    cookie name example-cookie-name, lb port 1, load balancer
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cookie_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
