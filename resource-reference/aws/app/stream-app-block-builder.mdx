---
title: "aws_app_stream_app_block_builder"
description: "`aws_app_stream_app_block_builder` is the virtual machine used to create an `aws_app_stream_app_block`..."
---

`aws_app_stream_app_block_builder` is the virtual machine used to create an `aws_app_stream_app_block`, packaging installed application content and uploading it to S3 to complete the app block, the app-block equivalent of what an `aws_app_stream_image_builder` does for full images.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own stream app block builder"})

    		ubx.Resource(app.StreamAppBlockBuilder, "example", app.StreamAppBlockBuilderConfig{
    			InstanceType:    "example",
    			Name:            "example",
    			Platform:        "example",
    			VpcConfig:       map[string]any{"security_group_ids": []string{"example"}},
    			AccessEndpoints: []map[string]any{{"endpoint_type": "example"}},
    			AppBlockArns:    []string{"example"},
    			Description:     "Managed by ubx.",
    			DisplayName:     "example-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { StreamAppBlockBuilder } from "./local-sdk/aws/sdk/typescript/aws/app/stream_app_block_builder.ts";

    export default stack("example", () => {
      intent({ summary: "example own stream app block builder" });

      resource(StreamAppBlockBuilder, "example", {
        instanceType: "example",
        name: "example",
        platform: "example",
        vpcConfig: { securityGroupIds: ["example"] },
        accessEndpoints: [{ endpointType: "example" }],
        appBlockArns: ["example"],
        description: "Managed by ubx.",
        displayName: "example-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.app import StreamAppBlockBuilder, StreamAppBlockBuilderConfig

    def describe():
        ubx.intent("example own stream app block builder")

        ubx.resource(StreamAppBlockBuilder, "example", StreamAppBlockBuilderConfig(
            instance_type="example",
            name="example",
            platform="example",
            vpc_config={"security_group_ids": ["example"]},
            access_endpoints=[{"endpoint_type": "example"}],
            app_block_arns=["example"],
            description="Managed by ubx.",
            display_name="example-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_stream_app_block_builder called "example", with
    instance type example, platform example, vpc config a set value,
    access endpoints a set value, app block arns a set value,
    description Managed by ubx., display name example-display-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_endpoints" type="list(object)">
  Specifies a list of VPC interface endpoints (each with an endpoint type and VPC endpoint ID) that enable private connectivity to the AppStream 2.0 App Block Builder without traversing the public internet. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="endpoint_type" type="string">
      Specifies the type of AppStream access endpoint, such as STREAMING for the streaming traffic endpoint, for this AppBlockBuilder. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="vpce_id" type="string">
      The ID of the VPC endpoint (vpce-id) that AppStream uses to establish a private connection to the app block builder via the specified access endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="app_block_arns" type="list(string)">
  Specifies the list of ARNs of AppStream 2.0 app blocks to associate with this app block builder, providing the source applications and content used to build new app blocks. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable, optional description of the app block builder that helps identify its purpose. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="display_name" type="string">
  The display_name specifies a user-friendly name for the AppStream 2.0 app block builder that appears in the console, distinct from the immutable Name identifier. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="enable_default_internet_access" type="bool">
  Enables or disables default internet access for the AppStream 2.0 app block builder, allowing it to reach the internet via the default route when no VPC configuration is specified. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  Specifies the ARN of the IAM role applied to the AppStream 2.0 app block builder, granting it permissions to access application assets and other AWS resources needed to create app blocks. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  The Amazon EC2 instance type that the AppStream 2.0 App Block Builder uses to build and package applications. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The unique name for the AppStream 2.0 app block builder. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="platform" type="string" required>
  The operating system platform for the app block builder, such as WINDOWS_SERVER_2019 or AMAZON_LINUX2. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Assigns metadata key-value pairs to the AppStream app block builder, enabling cost tracking, resource organization, and IAM-based access control. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a user-defined tag attached to the AppStream 2.0 App Block Builder resource, used for metadata and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_config" type="object" required>
  Specifies the VPC configuration for the AppStream 2.0 app block builder, including the security group IDs and subnet IDs that define the network environment in which the builder runs. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="security_group_ids" type="list(string)">
      Specifies the list of EC2 security group IDs to associate with the AppStream 2.0 App Block Builder's elastic network interface within the configured VPC. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)">
      Specifies the list of subnet IDs in the VPC where the AppStream 2.0 app block builder will be launched, controlling which subnets are used for its elastic network interface. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AppStream 2.0 app block builder. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_time" type="string">
  The timestamp indicating when the AppStream App Block Builder was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
