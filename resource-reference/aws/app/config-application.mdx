---
title: "aws_app_config_application"
description: "`aws_app_config_application` is the top-level container in AWS AppConfig..."
---

`aws_app_config_application` is the top-level container in AWS AppConfig, the logical grouping that an `aws_app_config_environment` and `aws_app_config_configuration_profile` both attach to. AppConfig itself lets you change application behavior, feature flags, allow lists, throttling limits, in production without redeploying code.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own config application"})

    		ubx.Resource(app.ConfigApplication, "example", app.ConfigApplicationConfig{
    			Name:        "example",
    			Description: "Managed by ubx.",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigApplication } from "./local-sdk/aws/sdk/typescript/aws/app/config_application.ts";

    export default stack("example", () => {
      intent({ summary: "example own config application" });

      resource(ConfigApplication, "example", {
        name: "example",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.app import ConfigApplication, ConfigApplicationConfig

    def describe():
        ubx.intent("example own config application")

        ubx.resource(ConfigApplication, "example", ConfigApplicationConfig(
            name="example",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_config_application called "example", with
    description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description of the application. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A name for the application. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Metadata to assign to the application. Tags help organize and categorize your AWS AppConfig resources. Each tag consists of a key and an optional value, both of which you define. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key of a key-value pair attached to the AWS AppConfig application, used for metadata, cost tracking, and IAM-based access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="application_id" type="string">
  The application Id Computed by AWS after `ship`.
</ResponseField>
