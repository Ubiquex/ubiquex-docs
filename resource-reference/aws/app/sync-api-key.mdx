---
title: "aws_app_sync_api_key"
description: "`aws_app_sync_api_key` issues a key-based credential for authorizing requests against an `aws_app_sync_api`..."
---

`aws_app_sync_api_key` issues a key-based credential for authorizing requests against an `aws_app_sync_api`, one of several supported authorization modes alongside IAM, Cognito, OpenID Connect, and Lambda authorizers.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sync api key"})

    		ubx.Resource(app.SyncApiKey, "ci-runner", app.SyncApiKeyConfig{
    			ApiId:       "example",
    			Description: "Managed by ubx.",
    			Expires:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SyncApiKey } from "./local-sdk/aws/sdk/typescript/aws/app/sync_api_key.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sync api key" });

      resource(SyncApiKey, "ci-runner", {
        apiId: "example",
        description: "Managed by ubx.",
        expires: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.app import SyncApiKey, SyncApiKeyConfig

    def describe():
        ubx.intent("payments own sync api key")

        ubx.resource(SyncApiKey, "ci-runner", SyncApiKeyConfig(
            api_id="example",
            description="Managed by ubx.",
            expires=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_sync_api_key, with api id example, description
    Managed by ubx., expires 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="api_id" type="string" required>
  The unique identifier of the AppSync GraphQL API that this API key is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  An optional user-provided description that helps identify the purpose or owner of the API key. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="expires" type="number">
  The expiration time of the API key, expressed as the number of seconds since the Unix epoch. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="api_key" type="string">
  The generated API key value used for authenticating requests to the AppSync GraphQL API. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="api_key_id" type="string">
  The unique identifier automatically assigned to the API key by AWS AppSync on creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AppSync API key. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
