---
title: "aws_app_stream_stack_fleet_association"
description: "`aws_app_stream_stack_fleet_association` links an `aws_app_stream_stack` to the `aws_app_stream_fleet` that actually runs its streaming instances."
---

A stack with no fleet association has user access policies and storage configured but nothing to stream from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own stream stack fleet association"})

    		ubx.Resource(app.StreamStackFleetAssociation, "ci-runner", app.StreamStackFleetAssociationConfig{
    			FleetName: "example-fleet-name",
    			StackName: "example-stack-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { StreamStackFleetAssociation } from "./local-sdk/aws/sdk/typescript/aws/app/stream_stack_fleet_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own stream stack fleet association" });

      resource(StreamStackFleetAssociation, "ci-runner", {
        fleetName: "example-fleet-name",
        stackName: "example-stack-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.app import StreamStackFleetAssociation, StreamStackFleetAssociationConfig

    def describe():
        ubx.intent("payments own stream stack fleet association")

        ubx.resource(StreamStackFleetAssociation, "ci-runner", StreamStackFleetAssociationConfig(
            fleet_name="example-fleet-name",
            stack_name="example-stack-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_app_stream_stack_fleet_association, with fleet name
    example-fleet-name, stack name example-stack-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="fleet_name" type="string" required>
  The name of the AppStream 2.0 fleet to associate with the stack. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  The name of the AppStream stack that the fleet will be associated with. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  This computed identifier uniquely identifies the AppStream 2.0 stack-fleet association and is formed from the stack name and fleet name separated by a comma. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
