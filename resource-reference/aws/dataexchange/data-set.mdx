---
title: "aws_data_exchange_data_set"
description: "`aws_data_exchange_data_set` is the real, curated resource a data provider assembles in AWS Data Exchange, files, an API, Redshift, S3..."
---

`aws_data_exchange_data_set` is the real, curated resource a data provider assembles in AWS Data Exchange, files, an API, Redshift, S3, or Lake Formation data, that a receiver gains access to once they accept a real data grant or subscribe to a Marketplace data product built around it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataexchange "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/dataexchange"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data set"})

    		ubx.Resource(dataexchange.DataSet, "example", dataexchange.DataSetConfig{
    			AssetType:   "example",
    			Description: "Managed by ubx.",
    			Name:        "example",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataSet } from "@ubx/sdk-aws/aws/dataexchange/data_set";

    export default stack("example", () => {
      intent({ summary: "example own data set" });

      resource(DataSet, "example", {
        assetType: "example",
        description: "Managed by ubx.",
        name: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dataexchange import DataSet, DataSetConfig
    from ubx.aws.dataexchange.data_set import DataSet_Tags

    def describe():
        ubx.intent("example own data set")

        ubx.resource(DataSet, "example", DataSetConfig(
            asset_type="example",
            description="Managed by ubx.",
            name="example",
            tags=[DataSet_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_data_exchange_data_set called "example", with asset
    type example, description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="asset_type" type="string" required>
  The type of asset that is added to a data set. Required.
</ResponseField>

<ResponseField name="description" type="string" required>
  A description for the data set. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the data set. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags for the data set. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of a tag attached to the AWS Data Exchange data set, used to organize, identify, and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN for the data set. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The date and time that the data set was created, in ISO 8601 format. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier for the data set. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="origin" type="string">
  A property that defines the data set as OWNED by the account (for providers) or ENTITLED to the account (for subscribers). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time that the data set was last updated, in ISO 8601 format. Computed by AWS after `ship`.
</ResponseField>
