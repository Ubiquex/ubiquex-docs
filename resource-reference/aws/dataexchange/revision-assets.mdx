---
title: "aws_dataexchange_revision_assets"
description: "Real, generated bindings for `aws_dataexchange_revision_assets`."
---

`aws_dataexchange_revision_assets` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/dataexchange"

    cfg := dataexchange.RevisionAssetsConfig{
        DataSetId: "example",
        Comment: "example",
        Finalized: true,
    }
    ubx.Resource(dataexchange.RevisionAssets, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RevisionAssets } from
      "jsr:@ubx/sdk-aws/aws/dataexchange/revision_assets";

    resource(RevisionAssets, "example", {
      dataSetId: "example",
      comment: "example",
      finalized: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.dataexchange import (
        RevisionAssets,
        RevisionAssetsConfig,
    )

    cfg = RevisionAssetsConfig(
        data_set_id="example",
        comment="example",
        finalized=True,
    )
    ubx.resource(RevisionAssets, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_dataexchange_revision_assets with data_set_id,
comment, finalized.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="data_set_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="finalized" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="finalized" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
