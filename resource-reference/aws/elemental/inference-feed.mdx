---
title: "aws_elemental_inference_feed"
description: "`aws_elemental_inference_feed` is the top-level real resource in AWS Elemental Inference..."
---

`aws_elemental_inference_feed` is the top-level real resource in AWS Elemental Inference, a fully managed AI service that transforms live broadcast video in parallel with encoding into vertical crops and highlight clips for mobile and social platforms, TikTok, Instagram Reels, YouTube Shorts among them, in real time. A feed starts in a CREATING state and becomes AVAILABLE once ready to configure outputs against. AWS Elemental Inference reached general availability February 24, 2026, genuinely new enough that this session verified it live rather than trusting a remembered description.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elemental "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elemental"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own inference feed"})

    		ubx.Resource(elemental.InferenceFeed, "example", elemental.InferenceFeedConfig{
    			Name:    "example",
    			Outputs: []map[string]any{{"name": "example-name"}},
    			Tags:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { InferenceFeed } from "./local-sdk/aws/sdk/typescript/aws/elemental/inference_feed.ts";

    export default stack("example", () => {
      intent({ summary: "example own inference feed" });

      resource(InferenceFeed, "example", {
        name: "example",
        outputs: [{ name: "example-name" }],
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.elemental import InferenceFeed, InferenceFeedConfig

    def describe():
        ubx.intent("example own inference feed")

        ubx.resource(InferenceFeed, "example", InferenceFeedConfig(
            name="example",
            outputs=[{"name": "example-name"}],
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elemental_inference_feed called "example", with
    outputs a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="outputs" type="list(object)" required>
  Required.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="output_config" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="clipping" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="callback_metadata" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="data_source_configuration" type="object">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="fixture_id" type="string">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="cropping" type="any">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="subtitling" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="aspect_ratio" type="object">
              Optional; if omitted, computed by AWS.
              <Expandable title="properties">
                <ResponseField name="height" type="number">
                  Optional; if omitted, computed by AWS.
                </ResponseField>
                <ResponseField name="width" type="number">
                  Sets the width value of the subtitle aspect ratio, which pairs with the height to define the proportional shape of the subtitles in the output (e.g., 16 for a 16:9 aspect ratio). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="dictionary" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="language" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="profanity_filter" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_endpoints" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
