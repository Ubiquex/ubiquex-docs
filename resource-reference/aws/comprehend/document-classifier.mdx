---
title: "aws_comprehend_document_classifier"
description: "Real, generated bindings for `aws_comprehend_document_classifier`."
---

`aws_comprehend_document_classifier` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	comprehend "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/comprehend"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own document classifier"})

    		ubx.Resource(comprehend.DocumentClassifier, "ci-runner", comprehend.DocumentClassifierConfig{
    			DataAccessRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			LanguageCode:      "example",
    			Name:              "ci-runner",
    			Mode:              "example",
    			ModelKmsKeyId:     "example",
    			Tags:              map[string]string{"managed-by": "ubx"},
    			VolumeKmsKeyId:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DocumentClassifier } from "jsr:@ubx/sdk-aws/aws/comprehend/document_classifier";

    export default stack("payments", () => {
      intent({ summary: "payments own document classifier" });

      resource(DocumentClassifier, "ci-runner", {
        dataAccessRoleArn: "arn:aws:iam::123456789012:policy/ci",
        languageCode: "example",
        name: "ci-runner",
        mode: "example",
        modelKmsKeyId: "example",
        tags: { "managed-by": "ubx" },
        volumeKmsKeyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.comprehend import DocumentClassifier, DocumentClassifierConfig

    def describe():
        ubx.intent("payments own document classifier")

        ubx.resource(DocumentClassifier, "ci-runner", DocumentClassifierConfig(
            data_access_role_arn="arn:aws:iam::123456789012:policy/ci",
            language_code="example",
            name="ci-runner",
            mode="example",
            model_kms_key_id="example",
            tags={"managed-by": "ubx"},
            volume_kms_key_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_comprehend_document_classifier called "ci-runner",
    with data access role arn arn:aws:iam::123456789012:policy/ci,
    language code example, mode example, model kms key id example,
    tags a set value, volume kms key id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data_access_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="language_code" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mode" type="string">
  Optional.
</ResponseField>

<ResponseField name="model_kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="volume_kms_key_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
