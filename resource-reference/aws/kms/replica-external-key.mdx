---
title: "aws_kms_replica_external_key"
description: "Real, generated bindings for `aws_kms_replica_external_key`."
---

`aws_kms_replica_external_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own replica external key"})

    		ubx.Resource(kms.ReplicaExternalKey, "ci-runner", kms.ReplicaExternalKeyConfig{
    			PrimaryKeyArn:                  "arn:aws:iam::123456789012:policy/ci",
    			BypassPolicyLockoutSafetyCheck: true,
    			DeletionWindowInDays:           1,
    			Description:                    "Managed by ubx.",
    			KeyMaterialBase64:              "example",
    			Tags:                           map[string]string{"managed-by": "ubx"},
    			ValidTo:                        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReplicaExternalKey } from "jsr:@ubx/sdk-aws/aws/kms/replica_external_key";

    export default stack("payments", () => {
      intent({ summary: "payments own replica external key" });

      resource(ReplicaExternalKey, "ci-runner", {
        primaryKeyArn: "arn:aws:iam::123456789012:policy/ci",
        bypassPolicyLockoutSafetyCheck: true,
        deletionWindowInDays: 1,
        description: "Managed by ubx.",
        keyMaterialBase64: "example",
        tags: { "managed-by": "ubx" },
        validTo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.kms import ReplicaExternalKey, ReplicaExternalKeyConfig

    def describe():
        ubx.intent("payments own replica external key")

        ubx.resource(ReplicaExternalKey, "ci-runner", ReplicaExternalKeyConfig(
            primary_key_arn="arn:aws:iam::123456789012:policy/ci",
            bypass_policy_lockout_safety_check=True,
            deletion_window_in_days=1,
            description="Managed by ubx.",
            key_material_base64="example",
            tags={"managed-by": "ubx"},
            valid_to="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_kms_replica_external_key, with primary key arn
    arn:aws:iam::123456789012:policy/ci, bypass policy lockout safety
    check true, deletion window in days 1, description Managed by
    ubx., key material base64 example, tags a set value, valid to
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="bypass_policy_lockout_safety_check" type="bool">
  Optional.
</ResponseField>

<ResponseField name="deletion_window_in_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_material_base64" type="string">
  Optional.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="primary_key_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="valid_to" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="expiration_model" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="key_usage" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
