---
title: "aws_kms_grant"
description: "Real, generated bindings for `aws_kms_grant`."
---

`aws_kms_grant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own grant"})

    		ubx.Resource(kms.Grant, "ci-runner", kms.GrantConfig{
    			GranteePrincipal:    "example",
    			KeyId:               "example",
    			Operations:          []string{"example"},
    			Name:                "ci-runner",
    			GrantCreationTokens: []string{"example"},
    			RetireOnDelete:      true,
    			RetiringPrincipal:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Grant } from "jsr:@ubx/sdk-aws/aws/kms/grant";

    export default stack("payments", () => {
      intent({ summary: "payments own grant" });

      resource(Grant, "ci-runner", {
        granteePrincipal: "example",
        keyId: "example",
        operations: ["example"],
        name: "ci-runner",
        grantCreationTokens: ["example"],
        retireOnDelete: true,
        retiringPrincipal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.kms import Grant, GrantConfig

    def describe():
        ubx.intent("payments own grant")

        ubx.resource(Grant, "ci-runner", GrantConfig(
            grantee_principal="example",
            key_id="example",
            operations=["example"],
            name="ci-runner",
            grant_creation_tokens=["example"],
            retire_on_delete=True,
            retiring_principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_kms_grant called "ci-runner", with grantee principal
    example, key id example, operations a set value, grant creation
    tokens a set value, retire on delete true, retiring principal
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="grant_creation_tokens" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="grantee_principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="operations" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retire_on_delete" type="bool">
  Optional.
</ResponseField>

<ResponseField name="retiring_principal" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="grant_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grant_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
