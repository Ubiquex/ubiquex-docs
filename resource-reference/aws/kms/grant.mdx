---
title: "aws_kms_grant"
description: "Real, generated bindings for `aws_kms_grant`."
---

`aws_kms_grant` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kms"

    cfg := kms.GrantConfig{
        GranteePrincipal: "example",
        KeyId: "example",
        Operations: []string{"example"},
        GrantCreationTokens: []string{"example"},
        Name: "example-name",
    }
    ubx.Resource(kms.Grant, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Grant } from "jsr:@ubx/sdk-aws/aws/kms/grant";

    resource(Grant, "example", {
      granteePrincipal: "example",
      keyId: "example",
      operations: ["example"],
      grantCreationTokens: ["example"],
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kms import Grant, GrantConfig

    cfg = GrantConfig(
        grantee_principal="example",
        key_id="example",
        operations=["example"],
        grant_creation_tokens=["example"],
        name="example",
    )
    ubx.resource(Grant, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kms_grant with grantee_principal, key_id,
operations, grant_creation_tokens, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="grant_creation_tokens" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="grantee_principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="operations" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retire_on_delete" type="bool">
  Optional.
</ResponseField>

<ResponseField name="retiring_principal" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="grant_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="grant_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
