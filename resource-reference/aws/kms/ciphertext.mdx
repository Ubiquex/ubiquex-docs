---
title: "aws_kms_ciphertext"
description: "Real, generated bindings for `aws_kms_ciphertext`."
---

`aws_kms_ciphertext` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kms "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kms"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ciphertext"})

    		ubx.Resource(kms.Ciphertext, "ci-runner", kms.CiphertextConfig{
    			KeyId:              "example",
    			Context:            map[string]string{"managed-by": "ubx"},
    			Plaintext:          "example",
    			PlaintextWo:        "example",
    			PlaintextWoVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Ciphertext } from "jsr:@ubx/sdk-aws/aws/kms/ciphertext";

    export default stack("payments", () => {
      intent({ summary: "payments own ciphertext" });

      resource(Ciphertext, "ci-runner", {
        keyId: "example",
        context: { "managed-by": "ubx" },
        plaintext: "example",
        plaintextWo: "example",
        plaintextWoVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.kms import Ciphertext, CiphertextConfig

    def describe():
        ubx.intent("payments own ciphertext")

        ubx.resource(Ciphertext, "ci-runner", CiphertextConfig(
            key_id="example",
            context={"managed-by": "ubx"},
            plaintext="example",
            plaintext_wo="example",
            plaintext_wo_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_kms_ciphertext, with key id example, context a set
    value, plaintext example, plaintext wo example, plaintext wo
    version example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="plaintext" type="string">
  Optional.
</ResponseField>

<ResponseField name="plaintext_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="plaintext_wo_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="ciphertext_blob" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
