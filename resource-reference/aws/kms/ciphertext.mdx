---
title: "aws_kms_ciphertext"
description: "Real, generated bindings for `aws_kms_ciphertext`."
---

`aws_kms_ciphertext` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kms"

    cfg := kms.CiphertextConfig{
        KeyId: "example",
        Context: map[string]string{"key": "value"},
        Plaintext: "example",
    }
    ubx.Resource(kms.Ciphertext, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Ciphertext } from "jsr:@ubx/sdk-aws/aws/kms/ciphertext";

    resource(Ciphertext, "example", {
      keyId: "example",
      context: { key: "value" },
      plaintext: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kms import Ciphertext, CiphertextConfig

    cfg = CiphertextConfig(
        key_id="example",
        context={"key": "value"},
        plaintext="example",
    )
    ubx.resource(Ciphertext, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kms_ciphertext with key_id, context, plaintext.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="context" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="plaintext" type="string">
  Optional.
</ResponseField>

<ResponseField name="plaintext_wo" type="string">
  Optional.
</ResponseField>

<ResponseField name="plaintext_wo_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="ciphertext_blob" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
