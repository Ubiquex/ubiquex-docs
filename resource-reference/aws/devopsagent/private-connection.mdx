---
title: "aws_dev_ops_agent_private_connection"
description: "`aws_dev_ops_agent_private_connection` establishes a real private network path, rather than a public internet connection..."
---

`aws_dev_ops_agent_private_connection` establishes a real private network path, rather than a public internet connection, between an `aws_dev_ops_agent_agent_space` and one of its associated services, for environments that require it to stay off the public internet.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devopsagent "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/devopsagent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own ops agent private connection"})

    		ubx.Resource(devopsagent.PrivateConnection, "example", devopsagent.PrivateConnectionConfig{
    			ConnectionConfiguration: "example",
    			Name:                    "example",
    			Certificate:             "example",
    			Tags:                    []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PrivateConnection } from "@ubx/sdk-aws/aws/devopsagent/private_connection";

    export default stack("example", () => {
      intent({ summary: "example own ops agent private connection" });

      resource(PrivateConnection, "example", {
        connectionConfiguration: "example",
        name: "example",
        certificate: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devopsagent.private_connection import PrivateConnection, PrivateConnectionConfig

    def describe():
        ubx.intent("example own ops agent private connection")

        ubx.resource(PrivateConnection, "example", PrivateConnectionConfig(
            connection_configuration="example",
            name="example",
            certificate="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dev_ops_agent_private_connection called "example",
    with connection configuration example, certificate example, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate" type="string">
  Certificate for the Private Connection. Optional.
</ResponseField>

<ResponseField name="connection_configuration" type="any" required>
  The connection configuration, either SelfManaged or ServiceManaged. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Unique name for this Private Connection within the account. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the Private Connection. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_expiry_time" type="string">
  The expiry time of the certificate associated with the Private Connection. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the Private Connection. Computed by AWS after `ship`.
</ResponseField>
