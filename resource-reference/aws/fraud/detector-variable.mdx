---
title: "aws_fraud_detector_variable"
description: "`aws_fraud_detector_variable` defined one real input field, an IP address, an email domain, a transaction amount..."
---

`aws_fraud_detector_variable` defined one real input field, an IP address, an email domain, a transaction amount, used by an `aws_fraud_detector_event_type`'s model and rules. Closed to new customers along with the rest of Fraud Detector since November 7, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own detector variable"})

    		ubx.Resource(frauddetector.Variable, "example", frauddetector.VariableConfig{
    			DataSource:   "example",
    			DataType:     "example",
    			DefaultValue: "example",
    			Name:         "example",
    			Description:  "Managed by ubx.",
    			Tags:         []map[string]any{{"key": "example"}},
    			VariableType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Variable } from "@ubx/sdk-aws/aws/frauddetector/variable";

    export default stack("example", () => {
      intent({ summary: "example own detector variable" });

      resource(Variable, "example", {
        dataSource: "example",
        dataType: "example",
        defaultValue: "example",
        name: "example",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
        variableType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.frauddetector.variable import Variable, VariableConfig

    def describe():
        ubx.intent("example own detector variable")

        ubx.resource(Variable, "example", VariableConfig(
            data_source="example",
            data_type="example",
            default_value="example",
            name="example",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
            variable_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_fraud_detector_variable called "example", with data
    source example, data type example, default value example,
    description Managed by ubx., tags a set value, variable type
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_source" type="string" required>
  The source of the data. Required.
</ResponseField>

<ResponseField name="data_type" type="string" required>
  The data type. Required.
</ResponseField>

<ResponseField name="default_value" type="string" required>
  The default value for the variable when no value is received. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the variable. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags associated with this variable. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key for the AWS Fraud Detector variable, used to assign custom metadata for identifying, organizing, and managing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag assigned to the AWS Fraud Detector variable, used for organizing and identifying the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="variable_type" type="string">
  The variable type. For more information see https://docs.aws.amazon.com/frauddetector/latest/ug/create-a-variable.html#variable-types Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the variable. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  The time when the variable was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The time when the variable was last updated. Computed by AWS after `ship`.
</ResponseField>
