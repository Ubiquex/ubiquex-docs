---
title: "aws_controltower_baseline"
description: "Real, generated bindings for `aws_controltower_baseline`."
---

`aws_controltower_baseline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/controltower"

    cfg := controltower.BaselineConfig{
        BaselineIdentifier: "example",
        BaselineVersion: "example",
        TargetIdentifier: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(controltower.Baseline, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Baseline } from
      "jsr:@ubx/sdk-aws/aws/controltower/baseline";

    resource(Baseline, "example", {
      baselineIdentifier: "example",
      baselineVersion: "example",
      targetIdentifier: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.controltower import Baseline, BaselineConfig

    cfg = BaselineConfig(
        baseline_identifier="example",
        baseline_version="example",
        target_identifier="example",
        tags={"key": "value"},
    )
    ubx.resource(Baseline, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_controltower_baseline with baseline_identifier,
baseline_version, target_identifier, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="baseline_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="baseline_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="operation_identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
