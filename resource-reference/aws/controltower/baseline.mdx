---
title: "aws_controltower_baseline"
description: "Real, generated bindings for `aws_controltower_baseline`."
---

`aws_controltower_baseline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controltower "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/controltower"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own baseline"})

    		ubx.Resource(controltower.Baseline, "ci-runner", controltower.BaselineConfig{
    			BaselineIdentifier: "example",
    			BaselineVersion:    "example",
    			TargetIdentifier:   "example",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Baseline } from "jsr:@ubx/sdk-aws/aws/controltower/baseline";

    export default stack("payments", () => {
      intent({ summary: "payments own baseline" });

      resource(Baseline, "ci-runner", {
        baselineIdentifier: "example",
        baselineVersion: "example",
        targetIdentifier: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.controltower import Baseline, BaselineConfig

    def describe():
        ubx.intent("payments own baseline")

        ubx.resource(Baseline, "ci-runner", BaselineConfig(
            baseline_identifier="example",
            baseline_version="example",
            target_identifier="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_controltower_baseline, with baseline identifier
    example, baseline version example, target identifier example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="baseline_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="baseline_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="operation_identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
