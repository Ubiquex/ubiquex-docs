---
title: "aws_controltower_control"
description: "Real, generated bindings for `aws_controltower_control`."
---

`aws_controltower_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	controltower "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/controltower"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own control"})

    		ubx.Resource(controltower.Control, "ci-runner", controltower.ControlConfig{
    			ControlIdentifier: "example",
    			TargetIdentifier:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Control } from "jsr:@ubx/sdk-aws/aws/controltower/control";

    export default stack("payments", () => {
      intent({ summary: "payments own control" });

      resource(Control, "ci-runner", {
        controlIdentifier: "example",
        targetIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.controltower import Control, ControlConfig

    def describe():
        ubx.intent("payments own control")

        ubx.resource(Control, "ci-runner", ControlConfig(
            control_identifier="example",
            target_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_controltower_control, with control identifier example,
    target identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="control_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
