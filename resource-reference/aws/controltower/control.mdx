---
title: "aws_controltower_control"
description: "Real, generated bindings for `aws_controltower_control`."
---

`aws_controltower_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/controltower"

    cfg := controltower.ControlConfig{
        ControlIdentifier: "example",
        TargetIdentifier: "example",
    }
    ubx.Resource(controltower.Control, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Control } from
      "jsr:@ubx/sdk-aws/aws/controltower/control";

    resource(Control, "example", {
      controlIdentifier: "example",
      targetIdentifier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.controltower import Control, ControlConfig

    cfg = ControlConfig(
        control_identifier="example",
        target_identifier="example",
    )
    ubx.resource(Control, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_controltower_control with control_identifier,
target_identifier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="control_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
