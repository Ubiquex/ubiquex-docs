---
title: "aws_fsx_file_system"
description: "`aws_fsx_file_system` provisions a real, fully managed file system, Windows File Server, Lustre, NetApp ONTAP, or OpenZFS..."
---

`aws_fsx_file_system` provisions a real, fully managed file system, Windows File Server, Lustre, NetApp ONTAP, or OpenZFS, each a genuinely different underlying file system technology rather than variants of one engine, chosen based on the workload's own protocol and performance needs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	fsx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/fsx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own file system"})

    		ubx.Resource(fsx.FileSystem, "example", fsx.FileSystemConfig{
    			FileSystemType:        "example",
    			SubnetIds:             []string{"example"},
    			BackupId:              "example",
    			FileSystemTypeVersion: "example",
    			KmsKeyId:              "example",
    			LustreConfiguration:   map[string]any{"auto_import_policy": "example"},
    			NetworkType:           "example",
    			OntapConfiguration:    map[string]any{"deployment_type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { FileSystem } from "./local-sdk/aws/sdk/typescript/aws/fsx/file_system.ts";

    export default stack("example", () => {
      intent({ summary: "example own file system" });

      resource(FileSystem, "example", {
        fileSystemType: "example",
        subnetIds: ["example"],
        backupId: "example",
        fileSystemTypeVersion: "example",
        kmsKeyId: "example",
        lustreConfiguration: { autoImportPolicy: "example" },
        networkType: "example",
        ontapConfiguration: { deploymentType: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.fsx import FileSystem, FileSystemConfig

    def describe():
        ubx.intent("example own file system")

        ubx.resource(FileSystem, "example", FileSystemConfig(
            file_system_type="example",
            subnet_ids=["example"],
            backup_id="example",
            file_system_type_version="example",
            kms_key_id="example",
            lustre_configuration={"auto_import_policy": "example"},
            network_type="example",
            ontap_configuration={"deployment_type": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_fsx_file_system, with file system type example, subnet
    ids a set value, backup id example, file system type version
    example, kms key id example, lustre configuration a set value,
    network type example, ontap configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_id" type="string">
  The ID of an existing FSx backup to use when creating this file system, which restores the file system from that backup instead of creating a new empty one. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="file_system_type" type="string" required>
  Specifies the type of Amazon FSx file system to create, such as LUSTRE, WINDOWS, ONTAP, or OPENZFS, determining the underlying storage and feature set. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="file_system_type_version" type="string">
  Specifies the version of the file system type to use, such as the Lustre version (e.g., 2.10, 2.12) or Windows Server version (e.g., 2012_R2, 2016), for the FSx file system. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The ID or Amazon Resource Name (ARN) of the AWS KMS key used to encrypt the file system's data at rest, overriding the default service key when specified. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="lustre_configuration" type="object">
  The Lustre configuration for an FSx for Lustre file system, specifying deployment type, data repository import/export paths, and other Lustre-specific settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="auto_import_policy" type="string">
      Specifies how changes to files in the linked S3 bucket are automatically imported into the Lustre file system, with valid values NONE, NEW, and NEW_CHANGED (default is NONE). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="automatic_backup_retention_days" type="number">
      Specifies the number of days to retain automatic backups for the FSx for Lustre file system, where a value of 0 disables automatic backups. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="copy_tags_to_backups" type="bool">
      Indicates whether tags on the FSx Lustre file system are copied to its automatic and manual backups; defaults to false. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="daily_automatic_backup_start_time" type="string">
      The time of day (in UTC, 24-hour HH:MM format) when FSx for Lustre automatically creates a daily backup of the file system. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data_compression_type" type="string">
      Specifies the data compression type for the Amazon FSx for Lustre file system, either LZ4 to enable compression or NONE to disable it. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="data_read_cache_configuration" type="object">
      This nested object configures the data read cache for the FSx for Lustre file system, specifying the cache size (and optionally its type) used to accelerate read requests by caching frequently accessed data on faster storage. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="size_gi_b" type="number">
          Specifies the size in GiB of the data read cache for the FSx for Lustre file system, which caches frequently accessed data to improve read latency. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sizing_mode" type="string">
          Specifies the sizing mode (AUTOMATIC or USER_PROVISIONED) for the data read cache on an FSx for Lustre file system that uses HDD storage, controlling whether the cache size is automatically managed or manually set via the Size field. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="deployment_type" type="string">
      Determines the Lustre file system's deployment type (e.g., SCRATCH_1, SCRATCH_2, PERSISTENT_1, or PERSISTENT_2), which influences performance, data durability, and cost. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="drive_cache_type" type="string">
      Determines whether the Lustre file system uses an SSD drive cache for read operations, with valid values 'READ' or 'NONE'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="efa_enabled" type="bool">
      Specifies whether to enable Elastic Fabric Adapter (EFA) for the Amazon FSx for Lustre file system, providing low-latency and high-bandwidth networking for HPC workloads. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_path" type="string">
      Specifies the Amazon S3 directory (bucket and optional prefix) to which files are exported from the FSx for Lustre file system when linked to a data repository. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="import_path" type="string">
      Specifies the Amazon S3 path (starting with s3://) from which data is imported into the Lustre file system, such as an S3 bucket or bucket prefix used as the data repository. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="imported_file_chunk_size" type="number">
      Specifies the size in bytes of the chunks that the FSx for Lustre file system uses when importing files from its linked S3 data repository. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="metadata_configuration" type="object">
      Configures the metadata storage for the FSx for Lustre file system, allowing either automatic (AWS-managed) metadata capacity or user-provisioned metadata storage with a specified capacity. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="iops" type="number">
          Specifies the provisioned IOPS to use for the FSx for Lustre file system's metadata, required when the metadata configuration mode is set to USER_PROVISIONED. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          Controls whether the Lustre file system's metadata configuration is automatically managed (AUTOMATIC) or manually provisioned (USER_PROVISIONED, requiring an Iops value) to determine the IOPS of the metadata storage. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="per_unit_storage_throughput" type="number">
      Specifies the per-unit storage throughput (in MB/s per TiB) for the Lustre file system, which is required for persistent deployment types (PERSISTENT_1 or PERSISTENT_2) and must be one of the supported values (e.g., 50, 100, 200). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="throughput_capacity" type="number">
      Specifies the throughput capacity of the FSx for Lustre file system in megabytes per second (MB/s), which for PERSISTENT_2 deployment types must be one of 125, 250, 500, or 1000 MB/s, while PERSISTENT_1 requires a value between 50 and 200 MB/s. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="weekly_maintenance_start_time" type="string">
      Sets the day and time (in UTC, formatted as d:HH:MM, e.g., '1:04:30') when the weekly maintenance window starts for the FSx for Lustre file system, allowing AWS to perform system maintenance at that scheduled time. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_type" type="string">
  Determines whether the FSx file system is reachable over the internet ('INTERNET') or only within the VPC ('INTERNAL'). Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ontap_configuration" type="object">
  Specifies the FSx for ONTAP-specific configuration for the file system, including settings like deployment type, throughput capacity, automatic backup retention and start time, disk I/O configuration, admin password, preferred subnet, route tables, and endpoint IP address range. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="automatic_backup_retention_days" type="number">
      Specifies the number of days that automatic backups are retained before being deleted, with a range from 0 to 90 days, where 0 disables automatic backups. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="daily_automatic_backup_start_time" type="string">
      The preferred time in HH:MM UTC format for daily automatic backups of the FSx for ONTAP file system. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="deployment_type" type="string" required>
      Specifies the deployment type for the FSx for ONTAP file system, either MULTI_AZ_1 for a multi-AZ deployment or SINGLE_AZ_1 for a single-AZ deployment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disk_iops_configuration" type="object">
      Specifies the disk I/O operations per second (IOPS) configuration for the ONTAP file system's SSD storage, defining whether IOPS are automatically set (AUTOMATIC) or manually provisioned (USER_PROVISIONED) via the iops property. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="iops" type="number">
          Specifies the initial number of input/output operations per second (IOPS) to provision for the SSD storage of the Amazon FSx for ONTAP file system, determining its baseline performance level. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          Specifies the disk I/O configuration mode for the FSx for ONTAP file system, either 'AUTOMATIC' (the system sets IOPs automatically) or 'USER_PROVISIONED' (you specify the IOPs value in the same disk_iops_configuration). Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoint_ip_address_range" type="string">
      Specifies the CIDR block from which FSx for ONTAP assigns IP addresses to the file system's management and data endpoints; if omitted, Amazon FSx automatically chooses an available range. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_ipv6_address_range" type="string">
      The IPv6 CIDR block from which FSx for ONTAP selects IP addresses for the file system's management and intercluster endpoints. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="fsx_admin_password" type="string">
      The fsx_admin_password specifies the password for the NetApp ONTAP administrator (admin) user on the FSx for ONTAP file system. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hapairs" type="number">
      Specifies the number of high-availability (HA) pairs for the FSx for ONTAP file system, which determines total SSD storage capacity and compute resources, with allowed values of 1, 2, or 4 and a default of 2. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="preferred_subnet_id" type="string">
      Specifies the ID of the subnet where the FSx for ONTAP file system's primary management endpoint is placed, allowing you to choose the preferred Availability Zone for management traffic (typically used in multi-AZ deployments). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="route_table_ids" type="list(string)">
      Specifies the list of VPC route table IDs to be associated with the network interfaces (endpoints) of the FSx for ONTAP file system, controlling how traffic is routed between the file system and other resources in your VPC. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="throughput_capacity" type="number">
      Specifies the sustained throughput capacity of the FSx for ONTAP file system in megabytes per second (MB/s). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="throughput_capacity_per_hapair" type="number">
      Specifies the throughput capacity in MB/s per high-availability (HA) pair for an FSx for NetApp ONTAP file system, which determines the aggregate performance of the file system. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="weekly_maintenance_start_time" type="string">
      Specifies the preferred day and time (in `ddd:hh:mm` format, e.g., `SAT:02:00`) for the start of the weekly maintenance window for the FSx for ONTAP file system. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="open_zfsconfiguration" type="object">
  The `open_zfsconfiguration` block configures the OpenZFS-specific settings for an Amazon FSx for OpenZFS file system, including deployment type, throughput capacity, and associated options. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="automatic_backup_retention_days" type="number">
      The number of days that automatic backups of the OpenZFS file system are retained; a value of 0 disables automatic backups. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="copy_tags_to_backups" type="bool">
      Indicates whether to copy the file system's tags to each OpenZFS backup created from it. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="copy_tags_to_volumes" type="bool">
      Indicates whether to copy tags from the OpenZFS file system to volumes created within it. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="daily_automatic_backup_start_time" type="string">
      The preferred local time (in HH:MM format) at which daily automatic backups of the OpenZFS file system are started. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="deployment_type" type="string" required>
      Specifies the FSx for OpenZFS deployment type (e.g., SINGLE_AZ_1, SINGLE_AZ_2, or MULTI_AZ_1), which determines whether the file system is deployed in a single or multiple Availability Zones and impacts its availability and performance characteristics. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disk_iops_configuration" type="object">
      Specifies the disk IOPS configuration for the OpenZFS file system, including the mode (automatic or user-provisioned) and the number of IOPS to provision. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="iops" type="number">
          The number of provisioned input/output operations per second (IOPS) for the storage backing the FSx for OpenZFS file system, required when the disk I/O configuration mode is set to USER_PROVISIONED. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          Specifies whether the file system uses automatic disk I/O operations per second (IOPS) provisioning or user-provisioned IOPS, with valid values AUTOMATIC or USER_PROVISIONED. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="endpoint_ip_address_range" type="string">
      The IP address range in CIDR notation for the OpenZFS file system's management endpoint, which must be within one of the subnets specified for the file system. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="endpoint_ipv6_address_range" type="string">
      Defines the IPv6 CIDR block from which the OpenZFS file system's endpoint IP addresses are allocated, allowing you to control the address space for service endpoints. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="options" type="list(string)">
      Specifies the list of OpenZFS configuration options for the file system, where the only supported value is DELETE_CHILD_VOLUMES_AND_SNAPSHOTS, which permits the file system to be deleted together with its child volumes and snapshots. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="preferred_subnet_id" type="string">
      The preferred subnet ID where Amazon FSx for OpenZFS provisions the file system's endpoint network interface, allowing you to control the network placement of the endpoint within your VPC. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="read_cache_configuration" type="object">
      Specifies the read cache configuration for the FSx for OpenZFS file system, including the memory size (SizeGiB) allocated for the L2 ARC read cache. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="size_gi_b" type="number">
          Specifies the size of the read cache, in GiB, for the Amazon FSx for OpenZFS file system. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="sizing_mode" type="string">
          Determines how the read cache size is managed for the Amazon FSx for OpenZFS file system, with values such as NO_CACHE, USER_PROVISIONED, or PROPORTIONAL_TO_THROUGHPUT_CAPACITY. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="root_volume_configuration" type="object">
      This object defines the configuration of the root volume for an Amazon FSx for OpenZFS file system, including settings such as data compression, NFS exports, user and group quotas, and read-only status. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="copy_tags_to_snapshots" type="bool">
          Indicates whether tags on the root volume are copied to snapshots taken from that volume for an Amazon FSx for OpenZFS file system. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="data_compression_type" type="string">
          Specifies the data compression type for the OpenZFS root volume, allowed values are NONE, ZSTD, or LZ4. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="nfs_exports" type="list(object)">
          Specifies the NFS export configuration for the root volume of the OpenZFS file system, defining the clients and mount options allowed to access the volume. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="client_configurations" type="list(object)">
              Defines the list of client configurations that specify which clients (IP addresses or CIDR ranges) can access the NFS export and the NFS export options (e.g., rw, ro, sync) granted to them. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="clients" type="string">
                  Specifies the client IP address, CIDR range, or hostname that is allowed to access the NFS export defined in this client configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="options" type="list(string)">
                  Specifies the list of NFS export options (such as `rw`, `ro`, `sync`, `async`, or `no_root_squash`) that are applied to the client or client range identified in the containing `client_configurations` entry of the root volume's NFS export configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="read_only" type="bool">
          Specifies whether the root volume of the FSx for OpenZFS file system is read-only, preventing users from writing to the root volume when set to true. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="record_size_ki_b" type="number">
          The size in KiB of the data records (blocks) used for the root volume of the OpenZFS file system, which can be set to 4, 8, 16, 32, 64, or 128 and affects performance and compression efficiency. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_and_group_quotas" type="list(object)">
          Specifies the list of user and group quotas (each with a type, ID, and storage capacity quota in GiB) applied to the root volume of the OpenZFS file system. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="id" type="number">
              Numeric ID of the user or group to which the quota applies. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="storage_capacity_quota_gi_b" type="number">
              The maximum storage capacity, in GiB, that the specified user or group can consume on the root volume of the FSx for OpenZFS file system, enforced by this user/group quota. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              Specifies whether the quota applies to a user or a group, with allowed values USER or GROUP. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="route_table_ids" type="list(string)">
      Specifies the list of Amazon VPC route table IDs that the FSx for OpenZFS file system uses to route network traffic, allowing it to communicate with resources in other subnets or networks. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="throughput_capacity" type="number">
      Specifies the throughput capacity of the Amazon FSx for OpenZFS file system in megabytes per second (MB/s), determining its performance level. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="weekly_maintenance_start_time" type="string">
      Specifies the preferred day and time (in the format d:HH:MM, where d is 1-7 for Monday-Sunday) in UTC to start weekly maintenance for the FSx for OpenZFS file system. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  Specifies the list of VPC security group IDs to associate with the file system's network interfaces, controlling inbound and outbound traffic. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="storage_capacity" type="number">
  Specifies the storage capacity in gibibytes (GiB) to provision for the Amazon FSx file system, setting the total amount of storage and subject to type-specific limits and increments (e.g., Lustre requires 1,200 -- 100,000 GiB in multiples of 1,200). Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="storage_type" type="string">
  The storage type for the FSx file system, specifying whether it uses SSD or HDD storage, which affects performance and cost. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  The IDs of the subnets in which Amazon FSx creates the file system's network endpoints, with one subnet required for Lustre and two subnets (in different Availability Zones) for Windows File Server. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies a list of key-value tags to assign to the Amazon FSx file system for identifying, organizing, and managing the resource, including for cost allocation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the FSx file system, used to identify the tag for resource management and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="windows_configuration" type="object">
  Defines the Windows-specific configuration for an Amazon FSx for Windows File Server file system, including Active Directory settings, throughput capacity, and maintenance window. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="active_directory_id" type="string">
      The ID of the AWS Managed Microsoft Active Directory that this FSx for Windows file system joins, used when the file system is joined to a Managed AD server rather than a self-managed directory. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="aliases" type="list(string)">
      Specifies additional DNS aliases (CNAME) for the FSx for Windows File Server file system, enabling access via alternate domain names. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="audit_log_configuration" type="object">
      Configures the audit log delivery for the Windows file server, specifying the destination for audit logs (e.g., CloudWatch Logs or Amazon S3) and the types of file access events to audit. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="audit_log_destination" type="string">
          Specifies the Amazon Resource Name (ARN) of the destination for Windows File Server audit logs, such as a CloudWatch Logs log group or a Kinesis Data Firehose delivery stream; when omitted, FSx uses its default destination. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="file_access_audit_log_level" type="string" required>
          Specifies the level of file-access auditing to record for the Amazon FSx for Windows File Server file system, using values such as DISABLED, SUCCESS_ONLY, FAILURE_ONLY, or SUCCESS_AND_FAILURE to control which successful and/or failed file access operations are written to the audit log destination. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="file_share_access_audit_log_level" type="string" required>
          Specifies the level of file share access auditing for the FSx for Windows File Server, controlling whether success, failure, both, or no file access events are recorded in the audit log (values: DISABLED, SUCCESS_ONLY, FAILURE_ONLY, SUCCESS_AND_FAILURE). Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="automatic_backup_retention_days" type="number">
      Specifies the number of days to retain automatic backups for the Amazon FSx for Windows File Server file system; setting this to 0 disables automatic backups. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="copy_tags_to_backups" type="bool">
      If set to true, tags on the Amazon FSx for Windows File Server file system are copied to its automatic and user-initiated backups (and when tags are specified at backup creation, only those tags are copied), otherwise no tags are copied to backups. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="daily_automatic_backup_start_time" type="string">
      The preferred time of day (in HH:MM format, UTC) to start the automatic daily backup for the Windows file system. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="deployment_type" type="string">
      Specifies the deployment type for the FSx for Windows File Server, such as MULTI_AZ_1 or SINGLE_AZ_1, which determines whether the file system is deployed across multiple Availability Zones for high availability or in a single Availability Zone for cost savings. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disk_iops_configuration" type="object">
      Configures the disk I/O performance (IOPS) for an FSx for Windows File Server file system, specifying either automatic mode or a user-provided number of I/O operations per second. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="iops" type="number">
          The number of input/output operations per second (IOPS) to provision for the SSD storage of the FSx Windows file system, used when the disk I/O configuration mode is set to USER_PROVISIONED. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="mode" type="string">
          Specifies whether the disk I/O operations for the FSx for Windows file system are automatically managed by Amazon FSx (AUTOMATIC) or user-provided (USER_PROVIDED). Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fsrm_configuration" type="object">
      File Server Resource Manager (FSRM) settings for an Amazon FSx for Windows file system, including an enable flag, scheduled scan schedule, and aggregate/per-user quota limits. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="event_log_destination" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="fsrm_service_enabled" type="bool" required>
          Enables the File Server Resource Manager (FSRM) service on the FSx for Windows File Server, allowing quota management and file screening. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preferred_subnet_id" type="string">
      Specifies the ID of the subnet where AWS places the preferred file server for a MULTI_AZ_1 FSx for Windows File Server file system; when omitted, AWS automatically chooses one from the subnets associated with the file system's VPC. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="self_managed_active_directory_configuration" type="object">
      Configures the file system to join a self-managed Microsoft Active Directory domain, specifying the domain name, DNS servers, and service account credentials for directory access. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dns_ips" type="list(string)">
          The DNS IP addresses of the self-managed Active Directory domain controllers that FSx for Windows File Server uses to locate and join the domain. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="domain_join_service_account_secret" type="string">
          Specifies the ARN or name of an AWS Secrets Manager secret that holds the user name and password for the service account used to join the FSx for Windows file system to a self-managed Active Directory domain, providing an alternative to directly supplying UserName and Password fields. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="domain_name" type="string">
          The fully qualified domain name (FQDN) of the self-managed Microsoft Active Directory directory that this FSx Windows File Server will join, for example corp.example.com. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="file_system_administrators_group" type="string">
          Specifies the name of the group in the self-managed Active Directory that is granted administrative access to the Amazon FSx for Windows File Server file system. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizational_unit_distinguished_name" type="string">
          The distinguished name of the organizational unit (OU) in the self-managed Active Directory where the FSx for Windows File Server computer account is created. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="password" type="string">
          The password for the service account that Amazon FSx uses to join the self-managed Microsoft Active Directory domain. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_name" type="string">
          The user name of the service account that Amazon FSx uses to join the Windows File Server to your self-managed Microsoft Active Directory domain. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="throughput_capacity" type="number" required>
      Specifies the sustained throughput of the Amazon FSx for Windows File Server file system in megabytes per second (MB/s), determining its performance tier. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="weekly_maintenance_start_time" type="string">
      Sets the preferred weekly maintenance window start time for the FSx Windows file system, formatted as day-of-week:time (e.g., '2:04:00'). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="dnsname" type="string">
  The DNS name assigned to the Amazon FSx file system, used to mount the file system. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the Amazon FSx file system, such as fs-0123456789abcdef0. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="lustre_mount_name" type="string">
  The mount name (such as 'lustre') for the Amazon FSx for Lustre file system, used by clients to mount the file system after it is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_arn" type="string">
  The Amazon Resource Name (ARN) uniquely identifying the FSx file system in AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="root_volume_id" type="string">
  The unique identifier of the root volume of the Amazon FSx for OpenZFS file system. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
