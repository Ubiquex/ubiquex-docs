---
title: "aws_fsx_s3_access_point_attachment"
description: "Real, generated bindings for `aws_fsx_s3_access_point_attachment`."
---

`aws_fsx_s3_access_point_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/fsx"

    cfg := fsx.S3AccessPointAttachmentConfig{
        Name: "example-name",
        Type: "example",
    }
    ubx.Resource(fsx.S3AccessPointAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { S3AccessPointAttachment } from
      "jsr:@ubx/sdk-aws/aws/fsx/s3_access_point_attachment";

    resource(S3AccessPointAttachment, "example", {
      name: "example",
      type: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.fsx import (
        S3AccessPointAttachment,
        S3AccessPointAttachmentConfig,
    )

    cfg = S3AccessPointAttachmentConfig(
        name="example",
        type="example",
    )
    ubx.resource(S3AccessPointAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_fsx_s3_access_point_attachment with name, type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_access_point_alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="s3_access_point_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
