---
title: "aws_fsx_s3_access_point_attachment"
description: "Real, generated bindings for `aws_fsx_s3_access_point_attachment`."
---

`aws_fsx_s3_access_point_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fsx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/fsx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own s3 access point attachment"})

    		ubx.Resource(fsx.S3AccessPointAttachment, "ci-runner", fsx.S3AccessPointAttachmentConfig{
    			Name: "ci-runner",
    			Type: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { S3AccessPointAttachment } from "jsr:@ubx/sdk-aws/aws/fsx/s3_access_point_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own s3 access point attachment" });

      resource(S3AccessPointAttachment, "ci-runner", {
        name: "ci-runner",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.fsx import S3AccessPointAttachment, S3AccessPointAttachmentConfig

    def describe():
        ubx.intent("payments own s3 access point attachment")

        ubx.resource(S3AccessPointAttachment, "ci-runner", S3AccessPointAttachmentConfig(
            name="ci-runner",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_fsx_s3_access_point_attachment called "ci-runner",
    with type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_access_point_alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="s3_access_point_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
