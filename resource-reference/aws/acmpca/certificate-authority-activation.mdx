---
title: "aws_acmpca_certificate_authority_activation"
description: "`aws_acmpca_certificate_authority_activation` installs a real signed certificate into an `aws_acmpca_certificate_authority`..."
---

`aws_acmpca_certificate_authority_activation` installs a real signed certificate into an `aws_acmpca_certificate_authority`, the step that actually activates a subordinate (non-root) private CA so it can begin issuing its own certificates.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	acmpca "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/acmpca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own certificate authority activation"})

    		ubx.Resource(acmpca.CertificateAuthorityActivation, "example", acmpca.CertificateAuthorityActivationConfig{
    			Certificate:             "example",
    			CertificateAuthorityArn: "arn:aws:iam::123456789012:policy/example",
    			CertificateChain:        "example",
    			Status:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CertificateAuthorityActivation } from "./local-sdk/aws/sdk/typescript/aws/acmpca/certificate_authority_activation.ts";

    export default stack("example", () => {
      intent({ summary: "example own certificate authority activation" });

      resource(CertificateAuthorityActivation, "example", {
        certificate: "example",
        certificateAuthorityArn: "arn:aws:iam::123456789012:policy/example",
        certificateChain: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.acmpca import CertificateAuthorityActivation, CertificateAuthorityActivationConfig

    def describe():
        ubx.intent("example own certificate authority activation")

        ubx.resource(CertificateAuthorityActivation, "example", CertificateAuthorityActivationConfig(
            certificate="example",
            certificate_authority_arn="arn:aws:iam::123456789012:policy/example",
            certificate_chain="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_acmpca_certificate_authority_activation, with
    certificate example, certificate authority arn
    arn:aws:iam::123456789012:policy/example, certificate chain
    example, status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate" type="string" required>
  Certificate Authority certificate that will be installed in the Certificate Authority. Required.
</ResponseField>

<ResponseField name="certificate_authority_arn" type="string" required>
  Arn of the Certificate Authority. Required.
</ResponseField>

<ResponseField name="certificate_chain" type="string">
  Certificate chain for the Certificate Authority certificate. Optional.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the Certificate Authority. Optional.
</ResponseField>

## Output properties

<ResponseField name="complete_certificate_chain" type="string">
  The complete certificate chain, including the Certificate Authority certificate. Computed by AWS after `ship`.
</ResponseField>
