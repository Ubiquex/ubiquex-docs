---
title: "aws_acmpca_certificate"
description: "Real, generated bindings for `aws_acmpca_certificate`."
---

`aws_acmpca_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acmpca "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/acmpca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own certificate"})

    		ubx.Resource(acmpca.Certificate, "ci-runner", acmpca.CertificateConfig{
    			CertificateAuthorityArn:   "arn:aws:iam::123456789012:policy/ci",
    			CertificateSigningRequest: "example",
    			SigningAlgorithm:          "example",
    			ApiPassthrough:            "example",
    			TemplateArn:               "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Certificate } from "jsr:@ubx/sdk-aws/aws/acmpca/certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own certificate" });

      resource(Certificate, "ci-runner", {
        certificateAuthorityArn: "arn:aws:iam::123456789012:policy/ci",
        certificateSigningRequest: "example",
        signingAlgorithm: "example",
        apiPassthrough: "example",
        templateArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.acmpca import Certificate, CertificateConfig

    def describe():
        ubx.intent("payments own certificate")

        ubx.resource(Certificate, "ci-runner", CertificateConfig(
            certificate_authority_arn="arn:aws:iam::123456789012:policy/ci",
            certificate_signing_request="example",
            signing_algorithm="example",
            api_passthrough="example",
            template_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_acmpca_certificate, with certificate authority arn
    arn:aws:iam::123456789012:policy/ci, certificate signing request
    example, signing algorithm example, api passthrough example,
    template arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_passthrough" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_authority_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_signing_request" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="signing_algorithm" type="string" required>
  Required.
</ResponseField>

<ResponseField name="template_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_chain" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
