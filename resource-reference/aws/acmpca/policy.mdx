---
title: "aws_acmpca_policy"
description: "Real, generated bindings for `aws_acmpca_policy`."
---

`aws_acmpca_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/acmpca"

    cfg := acmpca.PolicyConfig{
        Policy: "example",
        ResourceArn: "example",
    }
    ubx.Resource(acmpca.Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Policy } from "jsr:@ubx/sdk-aws/aws/acmpca/policy";

    resource(Policy, "example", {
      policy: "example",
      resourceArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.acmpca import Policy, PolicyConfig

    cfg = PolicyConfig(
        policy="example",
        resource_arn="example",
    )
    ubx.resource(Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_acmpca_policy with policy, resource_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
