---
title: "aws_acmpca_permission"
description: "`aws_acmpca_permission` grants a real AWS service, most commonly ACM itself..."
---

`aws_acmpca_permission` grants a real AWS service, most commonly ACM itself, permission to use an `aws_acmpca_certificate_authority` to issue and renew certificates on an account's behalf.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	acmpca "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/acmpca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own permission"})

    		ubx.Resource(acmpca.Permission, "ci-runner", acmpca.PermissionConfig{
    			Actions:                 []string{"example"},
    			CertificateAuthorityArn: "arn:aws:iam::123456789012:policy/ci",
    			Principal:               "example",
    			SourceAccount:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Permission } from "./local-sdk/aws/sdk/typescript/aws/acmpca/permission.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own permission" });

      resource(Permission, "ci-runner", {
        actions: ["example"],
        certificateAuthorityArn: "arn:aws:iam::123456789012:policy/ci",
        principal: "example",
        sourceAccount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.acmpca import Permission, PermissionConfig

    def describe():
        ubx.intent("payments own permission")

        ubx.resource(Permission, "ci-runner", PermissionConfig(
            actions=["example"],
            certificate_authority_arn="arn:aws:iam::123456789012:policy/ci",
            principal="example",
            source_account="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_acmpca_permission, with actions a set value,
    certificate authority arn arn:aws:iam::123456789012:policy/ci,
    principal example, source account example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="actions" type="list(string)" required>
  The actions that the specified AWS service principal can use. Actions IssueCertificate, GetCertificate and ListPermissions must be provided. Required.
</ResponseField>

<ResponseField name="certificate_authority_arn" type="string" required>
  The Amazon Resource Name (ARN) of the Private Certificate Authority that grants the permission. Required.
</ResponseField>

<ResponseField name="principal" type="string" required>
  The AWS service or identity that receives the permission. At this time, the only valid principal is acm.amazonaws.com. Required.
</ResponseField>

<ResponseField name="source_account" type="string">
  The ID of the calling account. Optional.
</ResponseField>
