---
title: "aws_acmpca_permission"
description: "Real, generated bindings for `aws_acmpca_permission`."
---

`aws_acmpca_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/acmpca"

    cfg := acmpca.PermissionConfig{
        Actions: []string{"example"},
        CertificateAuthorityArn: "example",
        Principal: "example",
        SourceAccount: "example",
    }
    ubx.Resource(acmpca.Permission, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Permission } from
      "jsr:@ubx/sdk-aws/aws/acmpca/permission";

    resource(Permission, "example", {
      actions: ["example"],
      certificateAuthorityArn: "example",
      principal: "example",
      sourceAccount: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.acmpca import Permission, PermissionConfig

    cfg = PermissionConfig(
        actions=["example"],
        certificate_authority_arn="example",
        principal="example",
        source_account="example",
    )
    ubx.resource(Permission, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_acmpca_permission with actions,
certificate_authority_arn, principal, source_account.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="actions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="certificate_authority_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_account" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_account" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
