---
title: "aws_acmpca_certificate_authority_certificate"
description: "Real, generated bindings for `aws_acmpca_certificate_authority_certificate`."
---

`aws_acmpca_certificate_authority_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/acmpca"

    cfg := acmpca.CertificateAuthorityCertificateConfig{
        Certificate: "example",
        CertificateAuthorityArn: "example",
        CertificateChain: "example",
    }
    ubx.Resource(
        acmpca.CertificateAuthorityCertificate,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CertificateAuthorityCertificate } from
      "jsr:@ubx/sdk-aws/aws/acmpca/certificate_authority_certificate";

    resource(CertificateAuthorityCertificate, "example", {
      certificate: "example",
      certificateAuthorityArn: "example",
      certificateChain: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.acmpca import (
        CertificateAuthorityCertificate,
        CertificateAuthorityCertificateConfig,
    )

    cfg = CertificateAuthorityCertificateConfig(
        certificate="example",
        certificate_authority_arn="example",
        certificate_chain="example",
    )
    ubx.resource(CertificateAuthorityCertificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_acmpca_certificate_authority_certificate with
certificate, certificate_authority_arn, certificate_chain.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_authority_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_chain" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
