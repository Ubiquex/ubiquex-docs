---
title: "aws_acmpca_certificate_authority_certificate"
description: "Real, generated bindings for `aws_acmpca_certificate_authority_certificate`."
---

`aws_acmpca_certificate_authority_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	acmpca "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/acmpca"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own certificate authority certificate"})

    		ubx.Resource(acmpca.CertificateAuthorityCertificate, "ci-runner", acmpca.CertificateAuthorityCertificateConfig{
    			Certificate:             "example",
    			CertificateAuthorityArn: "arn:aws:iam::123456789012:policy/ci",
    			CertificateChain:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CertificateAuthorityCertificate } from "jsr:@ubx/sdk-aws/aws/acmpca/certificate_authority_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own certificate authority certificate" });

      resource(CertificateAuthorityCertificate, "ci-runner", {
        certificate: "example",
        certificateAuthorityArn: "arn:aws:iam::123456789012:policy/ci",
        certificateChain: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.acmpca import CertificateAuthorityCertificate, CertificateAuthorityCertificateConfig

    def describe():
        ubx.intent("payments own certificate authority certificate")

        ubx.resource(CertificateAuthorityCertificate, "ci-runner", CertificateAuthorityCertificateConfig(
            certificate="example",
            certificate_authority_arn="arn:aws:iam::123456789012:policy/ci",
            certificate_chain="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_acmpca_certificate_authority_certificate, with
    certificate example, certificate authority arn
    arn:aws:iam::123456789012:policy/ci, certificate chain example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_authority_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_chain" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
