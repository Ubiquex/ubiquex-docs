---
title: "aws_cloudsearch_domain_service_access_policy"
description: "Real, generated bindings for `aws_cloudsearch_domain_service_access_policy`."
---

`aws_cloudsearch_domain_service_access_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudsearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudsearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain service access policy"})

    		ubx.Resource(cloudsearch.DomainServiceAccessPolicy, "ci-runner", cloudsearch.DomainServiceAccessPolicyConfig{
    			AccessPolicy: "example",
    			DomainName:   "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainServiceAccessPolicy } from "jsr:@ubx/sdk-aws/aws/cloudsearch/domain_service_access_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own domain service access policy" });

      resource(DomainServiceAccessPolicy, "ci-runner", {
        accessPolicy: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudsearch import DomainServiceAccessPolicy, DomainServiceAccessPolicyConfig

    def describe():
        ubx.intent("payments own domain service access policy")

        ubx.resource(DomainServiceAccessPolicy, "ci-runner", DomainServiceAccessPolicyConfig(
            access_policy="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudsearch_domain_service_access_policy, with access
    policy example, domain name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
