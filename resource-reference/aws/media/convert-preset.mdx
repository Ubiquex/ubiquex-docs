---
title: "aws_media_convert_preset"
description: "`aws_media_convert_preset` saves real, reusable encoding settings for a single output within an `aws_media_convert_job_template` or job..."
---

`aws_media_convert_preset` saves real, reusable encoding settings for a single output within an `aws_media_convert_job_template` or job, letting the same output configuration, resolution, codec, bitrate, be applied across multiple jobs and overridden per job when needed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own convert preset"})

    		ubx.Resource(media.ConvertPreset, "ci-runner", media.ConvertPresetConfig{
    			SettingsJson: "example",
    			Name:         "ci-runner",
    			Category:     "example",
    			Description:  "Managed by ubx.",
    			Tags:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConvertPreset } from "./local-sdk/aws/sdk/typescript/aws/media/convert_preset.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own convert preset" });

      resource(ConvertPreset, "ci-runner", {
        settingsJson: "example",
        name: "ci-runner",
        category: "example",
        description: "Managed by ubx.",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import ConvertPreset, ConvertPresetConfig

    def describe():
        ubx.intent("payments own convert preset")

        ubx.resource(ConvertPreset, "ci-runner", ConvertPresetConfig(
            settings_json="example",
            name="ci-runner",
            category="example",
            description="Managed by ubx.",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_convert_preset called "ci-runner", with settings
    json example, category example, description Managed by ubx., tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="category" type="string">
  The new category for the preset, if you are changing it. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The new description for the preset, if you are changing it. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the preset that you are modifying. Optional.
</ResponseField>

<ResponseField name="settings_json" type="any" required>
  Specify, in JSON format, the transcoding job settings for this output preset. This specification must conform to the AWS Elemental MediaConvert job validation. For information about forming this specification, see the Remarks section later in this topic. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  Defines the key-value tags applied to the MediaConvert preset, which can be used for categorizing, organizing, and tracking costs associated with the resource. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the output preset, such as arn:aws:mediaconvert:us-west-2:123456789012 Computed by AWS after `ship`.
</ResponseField>
