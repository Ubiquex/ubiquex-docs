---
title: "aws_media_connect_flow_output"
description: "`aws_media_connect_flow_output` defines a real destination address, protocol..."
---

`aws_media_connect_flow_output` defines a real destination address, protocol, and port that an `aws_media_connect_flow` sends its ingested video to; a single flow can have up to 50 outputs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connect flow output"})

    		ubx.Resource(media.ConnectFlowOutput, "example", media.ConnectFlowOutputConfig{
    			FlowArn:                         "arn:aws:iam::123456789012:policy/example",
    			Name:                            "example",
    			CidrAllowList:                   []string{"example"},
    			Description:                     "Managed by ubx.",
    			Destination:                     "example",
    			Encryption:                      map[string]any{"role_arn": "example"},
    			MaxLatency:                      1,
    			MediaStreamOutputConfigurations: []map[string]any{{"destination_configurations": []map[string]any{{"destination_ip": "example"}}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectFlowOutput } from "./local-sdk/aws/sdk/typescript/aws/media/connect_flow_output.ts";

    export default stack("example", () => {
      intent({ summary: "example own connect flow output" });

      resource(ConnectFlowOutput, "example", {
        flowArn: "arn:aws:iam::123456789012:policy/example",
        name: "example",
        cidrAllowList: ["example"],
        description: "Managed by ubx.",
        destination: "example",
        encryption: { roleArn: "example" },
        maxLatency: 1,
        mediaStreamOutputConfigurations: [{
          destinationConfigurations: [{ destinationIp: "example" }],
        }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import ConnectFlowOutput, ConnectFlowOutputConfig

    def describe():
        ubx.intent("example own connect flow output")

        ubx.resource(ConnectFlowOutput, "example", ConnectFlowOutputConfig(
            flow_arn="arn:aws:iam::123456789012:policy/example",
            name="example",
            cidr_allow_list=["example"],
            description="Managed by ubx.",
            destination="example",
            encryption={"role_arn": "example"},
            max_latency=1,
            media_stream_output_configurations=[{"destination_configurations": [{"destination_ip": "example"}]}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_connect_flow_output called "example", with flow
    arn arn:aws:iam::123456789012:policy/example, cidr allow list a
    set value, description Managed by ubx., destination example,
    encryption a set value, max latency 1, media stream output
    configurations a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr_allow_list" type="list(string)">
  The range of IP addresses that should be allowed to initiate output requests to this flow. These IP addresses should be in the form of a Classless Inter-Domain Routing (CIDR) block; for example, 10.0.0.0/16. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the output. Optional.
</ResponseField>

<ResponseField name="destination" type="string">
  The address where you want to send the output. Optional.
</ResponseField>

<ResponseField name="encryption" type="object">
  Information about the encryption of the flow. Optional.
  <Expandable title="properties">
    <ResponseField name="algorithm" type="string">
      The type of algorithm that is used for the encryption (such as aes128, aes192, or aes256). Optional.
    </ResponseField>
    <ResponseField name="key_type" type="string">
      The type of key that is used for the encryption. If no keyType is provided, the service will use the default setting (static-key). Optional.
    </ResponseField>
    <ResponseField name="role_arn" type="string" required>
      The ARN of the role that you created during setup (when you set up AWS Elemental MediaConnect as a trusted entity). Required.
    </ResponseField>
    <ResponseField name="secret_arn" type="string" required>
      The ARN of the secret that you created in AWS Secrets Manager to store the encryption key. This parameter is required for static key encryption and is not valid for SPEKE encryption. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="flow_arn" type="string" required>
  The Amazon Resource Name (ARN), a unique identifier for any AWS resource, of the flow. Required.
</ResponseField>

<ResponseField name="max_latency" type="number">
  The maximum latency in milliseconds. This parameter applies only to RIST-based and Zixi-based streams. Optional.
</ResponseField>

<ResponseField name="media_stream_output_configurations" type="list(object)">
  The definition for each media stream that is associated with the output. Optional.
  <Expandable title="properties">
    <ResponseField name="destination_configurations" type="list(object)">
      A list of destination configurations that specify the IP address and port for sending a media stream, along with protocol-specific settings such as RTP or UDP parameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="destination_ip" type="string">
          The IP address to which the MediaConnect flow delivers the media stream output for this destination configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="destination_port" type="number">
          Specifies the destination port number to which the media stream output sends data for this destination configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="interface" type="object">
          Specifies the VPC interface configuration, including the interface name and type, that AWS Elemental MediaConnect uses to route the output media stream to its destination. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="name" type="string">
              The name of the VPC interface on the MediaConnect flow used as the egress interface for this destination configuration, which must match a VPC interface already defined on the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encoding_name" type="string">
      Specifies the encoding name for the media stream output, such as 'jxsv' or 'raw', which identifies the compression scheme used for the stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="encoding_parameters" type="object">
      Configures the encoding parameters for a media stream output, such as the compression factor and encoder profile applied to the stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="compression_factor" type="number">
          Controls the amount of compression applied to the video stream for this media stream output configuration's encoding parameters, with lower values generally yielding higher quality and higher values increasing compression. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="encoder_profile" type="string">
          Specifies the encoder profile (such as high, medium, or quality) that MediaConnect uses to encode the media stream for this flow output. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="media_stream_name" type="string">
      The name of the media stream that this output configuration sends to, matching the media stream name defined in the flow's media stream settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="min_latency" type="number">
  The minimum latency in milliseconds. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the output. This value must be unique within the current flow. Optional.
</ResponseField>

<ResponseField name="ndi_output_timecode_source" type="string">
  The timecode source for the NDI output. Optional.
</ResponseField>

<ResponseField name="ndi_program_name" type="string">
  A suffix for the names of the NDI sources that the flow creates. If a custom name isn't specified, MediaConnect uses the output name. Optional.
</ResponseField>

<ResponseField name="ndi_speed_hq_quality" type="number">
  A quality setting for the NDI Speed HQ encoder. Optional.
</ResponseField>

<ResponseField name="output_status" type="string">
  An indication of whether the output should transmit data or not. Optional.
</ResponseField>

<ResponseField name="port" type="number">
  The port to use when content is distributed to this output. Optional.
</ResponseField>

<ResponseField name="protocol" type="string">
  The protocol that is used by the source or output. Optional.
</ResponseField>

<ResponseField name="remote_id" type="string">
  The remote ID for the Zixi-pull stream. Optional.
</ResponseField>

<ResponseField name="router_integration_state" type="string">
  Optional.
</ResponseField>

<ResponseField name="router_integration_transit_encryption" type="object">
  The configuration that defines how content is encrypted during transit between the MediaConnect router and a MediaConnect flow. Optional.
  <Expandable title="properties">
    <ResponseField name="encryption_key_configuration" type="object" required>
      Configuration settings for flow transit encryption keys. Required.
      <Expandable title="properties">
        <ResponseField name="automatic" type="any">
          When set to true, AWS Elemental MediaConnect automatically generates and manages an encryption key for the router integration's transit encryption, and you do not need to specify a key ARN; when false, you must provide an explicit key ARN in the surrounding encryption key configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="secrets_manager" type="object">
          Defines the configuration for referencing an encryption key stored in AWS Secrets Manager, typically including the secret's ARN or name and the IAM role that grants MediaConnect permission to retrieve it. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="role_arn" type="string">
              The ARN of the IAM role that AWS Elemental MediaConnect assumes to access the encryption key stored in AWS Secrets Manager for transit encryption of this router integration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="secret_arn" type="string">
              The ARN of the AWS Secrets Manager secret that holds the static encryption key configured for transit encryption on this MediaConnect flow output when routed through a router integration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encryption_key_type" type="string">
      Defines the type of encryption key used for the router integration transit encryption, with valid values such as `static-key` and `srt-password`. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="smoothing_latency" type="number">
  The smoothing latency in milliseconds for RIST, RTP, and RTP-FEC streams. Optional.
</ResponseField>

<ResponseField name="stream_id" type="string">
  The stream ID that you want to use for this transport. This parameter applies only to Zixi-based streams. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Key-value pairs that can be used to tag and organize this flow output. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a user-defined tag associated with this AWS Elemental MediaConnect flow output, used for cost allocation, access control, or organizational metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_interface_attachment" type="object">
  The settings for attaching a VPC interface to an output. Optional.
  <Expandable title="properties">
    <ResponseField name="vpc_interface_name" type="string">
      The name of the VPC interface to use for this output. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="output_arn" type="string">
  The ARN of the output. Computed by AWS after `ship`.
</ResponseField>
