---
title: "aws_media_package_v2_channel_policy"
description: "`aws_media_package_v2_channel_policy` attaches a real resource-based access policy to an `aws_media_package_v2_channel`..."
---

`aws_media_package_v2_channel_policy` attaches a real resource-based access policy to an `aws_media_package_v2_channel`, controlling which principals can ingest into or manage that channel.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own package v2 channel policy"})

    		ubx.Resource(media.PackageV2ChannelPolicy, "example", media.PackageV2ChannelPolicyConfig{
    			ChannelGroupName: "example-channel-group-name",
    			ChannelName:      "example-channel-name",
    			Policy:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PackageV2ChannelPolicy } from "@ubx/sdk-aws/aws/media/package_v2_channel_policy";

    export default stack("example", () => {
      intent({ summary: "example own package v2 channel policy" });

      resource(PackageV2ChannelPolicy, "example", {
        channelGroupName: "example-channel-group-name",
        channelName: "example-channel-name",
        policy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.media import PackageV2ChannelPolicy, PackageV2ChannelPolicyConfig

    def describe():
        ubx.intent("example own package v2 channel policy")

        ubx.resource(PackageV2ChannelPolicy, "example", PackageV2ChannelPolicyConfig(
            channel_group_name="example-channel-group-name",
            channel_name="example-channel-name",
            policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_package_v2_channel_policy, with channel group
    name example-channel-group-name, channel name
    example-channel-name, policy example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="channel_group_name" type="string" required>
  The name of the MediaPackage v2 channel group that contains the channel to which this policy is attached. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="channel_name" type="string" required>
  The name of the AWS MediaPackage v2 channel that this channel policy applies to. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="policy" type="any" required>
  The JSON resource policy document that defines the permissions for accessing the MediaPackage v2 channel, typically specifying allowed principals and actions. Required. **(AI-inferred)**
</ResponseField>
