---
title: "aws_media_live_input"
description: "`aws_media_live_input` defines a real video source, its type and connection details, that an `aws_media_live_channel` encodes from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own live input"})

    		ubx.Resource(media.LiveInput, "example", media.LiveInputConfig{
    			Name:                 "example",
    			Destinations:         []map[string]any{{"network": "example"}},
    			InputDevices:         []map[string]any{{"id": "example"}},
    			InputNetworkLocation: "example",
    			InputSecurityGroups:  []string{"example"},
    			MediaConnectFlows:    []map[string]any{{"flow_arn": "example"}},
    			MulticastSettings:    map[string]any{"sources": []map[string]any{{"source_ip": "example"}}},
    			RoleArn:              "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LiveInput } from "./local-sdk/aws/sdk/typescript/aws/media/live_input.ts";

    export default stack("example", () => {
      intent({ summary: "example own live input" });

      resource(LiveInput, "example", {
        name: "example",
        destinations: [{ network: "example" }],
        inputDevices: [{ id: "example" }],
        inputNetworkLocation: "example",
        inputSecurityGroups: ["example"],
        mediaConnectFlows: [{ flowArn: "example" }],
        multicastSettings: { sources: [{ sourceIp: "example" }] },
        roleArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import LiveInput, LiveInputConfig

    def describe():
        ubx.intent("example own live input")

        ubx.resource(LiveInput, "example", LiveInputConfig(
            name="example",
            destinations=[{"network": "example"}],
            input_devices=[{"id": "example"}],
            input_network_location="example",
            input_security_groups=["example"],
            media_connect_flows=[{"flow_arn": "example"}],
            multicast_settings={"sources": [{"source_ip": "example"}]},
            role_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_live_input called "example", with destinations a
    set value, input devices a set value, input network location
    example, input security groups a set value, media connect flows a
    set value, multicast settings a set value, role arn
    arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="destinations" type="list(object)">
  Specifies the destination endpoints (e.g., RTMP URLs or RTP IP/port pairs) for a push input, defining the network locations where AWS MediaLive will listen for the incoming media stream. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="network" type="string">
      For a MediaLive input using a UDP or RTP push type, specifies the network IP address of the destination where the input stream is sent. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network_routes" type="list(object)">
      The `network_routes` list within each destination defines static routes (CIDR and gateway) that MediaLive uses to route push input traffic to the specified destination IP addresses, enabling delivery through a VPC or AWS Direct Connect. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="cidr" type="string">
          The CIDR block (e.g., '10.0.0.0/24') for a network route associated with the MediaLive input's destination, specifying the IP address range that the route applies to. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gateway" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="static_ip_address" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="stream_name" type="string">
      The stream name of the input destination, which represents the push endpoint (for example, the RTMP stream key or URL) that an upstream encoder uses to send content to this AWS Elemental MediaLive input. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="input_devices" type="list(object)">
  Specifies the MediaLive hardware input devices (such as Elemental Live or Link devices) that are attached to this input to deliver video/audio content to a MediaLive channel. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      The unique ID of the AWS Elemental MediaLive input device associated with this input. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="input_network_location" type="string">
  Specifies whether the input is located on AWS ('ON_AWS') or on-premises ('ON_PREMISES'), determining how AWS Elemental MediaLive accesses the input source. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="input_security_groups" type="list(string)">
  Specifies the input security groups (by ARN) to attach to this MediaLive input, which control the permitted IP addresses and CIDRs that can push content to it. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="media_connect_flows" type="list(object)">
  Specifies the AWS Elemental MediaConnect flows that this MediaLive input is linked to, where each flow is identified by its ARN. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="flow_arn" type="string">
      The ARN of an AWS Elemental MediaConnect flow to attach to this MediaLive input as a source. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="multicast_settings" type="object">
  Configures the source IP address and port for a MediaLive input that receives content over multicast, enabling the input to join a specific multicast group. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="sources" type="list(object)">
      Configures the list of multicast source endpoints (IP addresses and ports) that MediaLive will use to receive the input stream. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="source_ip" type="string">
          Specifies the source IP address of the multicast input stream, used by MediaLive to bind to and receive the multicast traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL of the multicast source, typically in the form of udp:// or rtp://, specifying the multicast group IP address and port from which the MediaLive input receives the stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The user-defined name you assign to this AWS Elemental MediaLive input, used for identification and management in the AWS console and API operations. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="role_arn" type="string">
  The ARN of the IAM role that AWS Elemental MediaLive assumes to access the input source, such as an S3 bucket for media files or an external pull endpoint. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="router_settings" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="destinations" type="list(object)">
      Optional.
      <Expandable title="properties">
        <ResponseField name="availability_zone_name" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encryption_type" type="string">
      For an SRT MediaLive input, this field sets the encryption type (AES128 or AES256) used for the incoming stream. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="secret_arn" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sdi_sources" type="list(string)">
  Associates the MediaLive input with the specified SDI sources (physical serial digital interface video inputs) by their identifiers, used when the input type is set to SDI. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="smpte2110_receiver_group_settings" type="object">
  Configures the SMPTE 2110 receiver group settings for the MediaLive input, specifying the number of receiver groups to provision for redundant IP-based media transport. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="smpte2110_receiver_groups" type="list(object)">
      Defines a list of SMPTE 2110 receiver groups, each specifying the multicast IP address and audio channel configuration used to receive the SMPTE 2110 stream for the input. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="sdp_settings" type="object">
          Contains the Session Description Protocol (SDP) settings, including the SDP body that describes the video and audio streams and network parameters, for the associated SMPTE 2110 receiver group in an AWS MediaLive input. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="ancillary_sdps" type="list(object)">
              Specifies the SDP content for each ancillary data stream (such as captions or timecode) in the SMPTE 2110 receiver group, with each item in the list providing the session description for one ancillary stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="media_index" type="number">
                  The index of the media stream in the main SDP that this ancillary SDP applies to, allowing multiple ancillary SDPs to target different media streams within the SMPTE-2110 receiver group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sdp_url" type="string">
                  The URL of the ancillary SDP (Session Description Protocol) file that provides additional media streams (such as audio or ancillary data) for the SMPTE 2110 receiver group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="audio_sdps" type="list(object)">
              Specifies the list of SDP (Session Description Protocol) URIs for the audio streams in the SMPTE 2110 receiver group. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="media_index" type="number">
                  The index of the audio media section within the SDP file, used to identify which audio stream to associate with the SMPTE 2110 receiver group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sdp_url" type="string">
                  Specifies the URL of the Session Description Protocol (SDP) file for an audio stream used by an SMPTE 2110 receiver group in the AWS Elemental MediaLive input. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="video_sdp" type="object">
              The Session Description Protocol (SDP) for the video stream in an SMPTE 2110 receiver group, defining the video's codec, resolution, and IP transport parameters. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="media_index" type="number">
                  The zero-based index of the video media description within the SDP, identifying which media line corresponds to the video stream for SMPTE 2110 receiver group input. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="sdp_url" type="string">
                  The URL of the SDP (Session Description Protocol) file that defines the video stream configuration within the SMPTE 2110 receiver group, which MediaLive uses to receive and parse the video input. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sources" type="list(object)">
  Specifies the source URLs (and optional credentials) from which AWS Elemental MediaLive pulls the input stream, typically used for pull-based input types such as RTMP_PULL or RTP_PULL. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="password_param" type="string">
      Specifies the name of the AWS Systems Manager (SSM) parameter that stores the password used for authenticating to the source server when the input source requires credentials. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      Specifies the URL of the media stream to pull into the MediaLive input, such as an RTMP or RTP endpoint from which the input is retrieved. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="username" type="string">
      The username used for basic authentication when an AWS Elemental MediaLive pull input connects to an upstream source server (such as an RTMP or RTP source) that requires credentials. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="srt_settings" type="object">
  Configures the SRT (Secure Reliable Transport) input settings for a MediaLive input, including the source IP address and listener port used to receive SRT media streams. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="srt_caller_sources" type="list(object)">
      Specifies a list of SRT caller sources, each defined by a hostname and port, that connect to MediaLive to deliver SRT content for this input. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="decryption" type="object">
          Specifies the decryption settings, including the encryption algorithm (AES-128 or AES-256) and the associated decryption key, used to decrypt the incoming SRT stream from this caller source. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="algorithm" type="string">
              The decryption algorithm for the SRT caller source, which must be either AES-128 or AES-256. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="passphrase_secret_arn" type="string">
              The ARN of the AWS Secrets Manager secret that stores the passphrase used to decrypt the SRT caller source stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="minimum_latency" type="number">
          The minimum latency in milliseconds for the SRT caller source, which acts as the lower bound for the SRT connection's target latency and directly influences the size of the receive buffer. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="srt_listener_address" type="string">
          The hostname or IP address of the SRT listener that this SRT caller source will connect to. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="srt_listener_port" type="string">
          The TCP port on which the MediaLive input listens for incoming SRT connections from each caller source in the SRT caller sources list. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_id" type="string">
          The SRT stream ID (stream identifier) for this caller source, which MediaLive uses to identify and receive the SRT stream from that source. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="srt_listener_settings" type="object">
      Configures the SRT listener for the input, specifying the allowed source IP address and port that MediaLive accepts incoming SRT connections from. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="decryption" type="object">
          Configures the decryption of the incoming SRT stream for this listener, typically by specifying the ARN of an AWS Secrets Manager secret that contains the decryption key. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="algorithm" type="string">
              The AES decryption algorithm (e.g., AES-128 or AES-256) used to decrypt the incoming SRT stream for this MediaLive input. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="passphrase_secret_arn" type="string">
              ARN of the AWS Secrets Manager secret containing the passphrase required to decrypt the SRT input stream. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="minimum_latency" type="number">
          Defines the minimum latency in milliseconds for the SRT listener, setting the lower bound of the configured latency window for the input. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_id" type="string">
          Specifies the SRT stream ID used by the listener to identify and associate the incoming SRT stream with the MediaLive input. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  Assigns metadata tags (key-value pairs) to the MediaLive input, which can be used for cost allocation, resource grouping, and IAM-based access control. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string">
  Defines the type of MediaLive input, such as UDP_PUSH, RTP_PUSH, RTMP_PUSH, RTMP_PULL, or URL_PULL, which determines the transfer protocol or method used to provide the source content. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="vpc" type="object">
  Configures the input to be provisioned inside a Virtual Private Cloud (VPC), including the subnets and security groups that define the input's network placement and access rules. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="security_group_ids" type="list(string)">
      The list of security group IDs associated with the VPC that the MediaLive input is attached to, controlling network access to the input. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)">
      Specifies the list of subnet IDs in the VPC where the incoming video streams for the MediaLive input will be provisioned, enabling the input's elastic network interfaces to be placed in those subnets. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AWS Elemental MediaLive input. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier assigned by AWS to the MediaLive input, used for referencing the input in other resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
