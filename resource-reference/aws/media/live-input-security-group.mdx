---
title: "aws_media_live_input_security_group"
description: "`aws_media_live_input_security_group` defines a real set of allowed CIDR ranges that an `aws_media_live_input` accepts push-based input from..."
---

`aws_media_live_input_security_group` defines a real set of allowed CIDR ranges that an `aws_media_live_input` accepts push-based input from, MediaLive's equivalent of a security group scoped to incoming video traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own live input security group"})

    		ubx.Resource(media.LiveInputSecurityGroup, "example", media.LiveInputSecurityGroupConfig{
    			Tags:           "example",
    			WhitelistRules: []map[string]any{{"cidr": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LiveInputSecurityGroup } from "@ubx/sdk-aws/aws/media/live_input_security_group";

    export default stack("example", () => {
      intent({ summary: "example own live input security group" });

      resource(LiveInputSecurityGroup, "example", {
        tags: "example",
        whitelistRules: [{ cidr: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.media import LiveInputSecurityGroup, LiveInputSecurityGroupConfig

    def describe():
        ubx.intent("example own live input security group")

        ubx.resource(LiveInputSecurityGroup, "example", LiveInputSecurityGroupConfig(
            tags="example",
            whitelist_rules=[{"cidr": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_live_input_security_group, with tags example,
    whitelist rules a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="any">
  Optional.
</ResponseField>

<ResponseField name="whitelist_rules" type="list(object)">
  Specifies the list of IPv4 CIDR blocks (with optional descriptions) that are allowed to access the input security group, controlling which source IPs can send media content. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cidr" type="string">
      The CIDR block that is allowed to send input requests to the MediaLive input security group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the MediaLive input security group. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the AWS Elemental MediaLive input security group, returned as a string when the resource is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
