---
title: "aws_media_connect_flow_entitlement"
description: "`aws_media_connect_flow_entitlement` grants a real other AWS account permission to create their own flow subscribed to this `aws_media_connect_flow`'s..."
---

`aws_media_connect_flow_entitlement` grants a real other AWS account permission to create their own flow subscribed to this `aws_media_connect_flow`'s output, the mechanism MediaConnect uses to share a live feed across account boundaries.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connect flow entitlement"})

    		ubx.Resource(media.ConnectFlowEntitlement, "ci-runner", media.ConnectFlowEntitlementConfig{
    			Description:                      "Managed by ubx.",
    			FlowArn:                          "arn:aws:iam::123456789012:policy/ci",
    			Name:                             "ci-runner",
    			Subscribers:                      []string{"example"},
    			DataTransferSubscriberFeePercent: 1,
    			Encryption:                       map[string]any{"algorithm": "example"},
    			EntitlementStatus:                "example",
    			Tags:                             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectFlowEntitlement } from "./local-sdk/aws/sdk/typescript/aws/media/connect_flow_entitlement.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own connect flow entitlement" });

      resource(ConnectFlowEntitlement, "ci-runner", {
        description: "Managed by ubx.",
        flowArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        subscribers: ["example"],
        dataTransferSubscriberFeePercent: 1,
        encryption: { algorithm: "example" },
        entitlementStatus: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import ConnectFlowEntitlement, ConnectFlowEntitlementConfig

    def describe():
        ubx.intent("payments own connect flow entitlement")

        ubx.resource(ConnectFlowEntitlement, "ci-runner", ConnectFlowEntitlementConfig(
            description="Managed by ubx.",
            flow_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            subscribers=["example"],
            data_transfer_subscriber_fee_percent=1,
            encryption={"algorithm": "example"},
            entitlement_status="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_connect_flow_entitlement called "ci-runner",
    with description Managed by ubx., flow arn
    arn:aws:iam::123456789012:policy/ci, subscribers a set value,
    data transfer subscriber fee percent 1, encryption a set value,
    entitlement status example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_transfer_subscriber_fee_percent" type="number">
  Percentage from 0-100 of the data transfer cost to be billed to the subscriber. Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  A description of the entitlement. Required.
</ResponseField>

<ResponseField name="encryption" type="object">
  Information about the encryption of the flow. Optional.
  <Expandable title="properties">
    <ResponseField name="algorithm" type="string" required>
      The type of algorithm that is used for the encryption (such as aes128, aes192, or aes256). Required.
    </ResponseField>
    <ResponseField name="constant_initialization_vector" type="string">
      A 128-bit, 16-byte hex value represented by a 32-character string, to be used with the key for encrypting content. This parameter is not valid for static key encryption. Optional.
    </ResponseField>
    <ResponseField name="device_id" type="string">
      The value of one of the devices that you configured with your digital rights management (DRM) platform key provider. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
    </ResponseField>
    <ResponseField name="key_type" type="string">
      The type of key that is used for the encryption. If no keyType is provided, the service will use the default setting (static-key). Optional.
    </ResponseField>
    <ResponseField name="region" type="string">
      The AWS Region that the API Gateway proxy endpoint was created in. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
    </ResponseField>
    <ResponseField name="resource_id" type="string">
      An identifier for the content. The service sends this value to the key server to identify the current endpoint. The resource ID is also known as the content ID. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
    </ResponseField>
    <ResponseField name="role_arn" type="string" required>
      The ARN of the role that you created during setup (when you set up AWS Elemental MediaConnect as a trusted entity). Required.
    </ResponseField>
    <ResponseField name="secret_arn" type="string">
      The ARN of the secret that you created in AWS Secrets Manager to store the encryption key. This parameter is required for static key encryption and is not valid for SPEKE encryption. Optional.
    </ResponseField>
    <ResponseField name="url" type="string">
      The URL from the API Gateway proxy that you set up to talk to your key server. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="entitlement_status" type="string">
  An indication of whether the entitlement is enabled. Optional.
</ResponseField>

<ResponseField name="flow_arn" type="string" required>
  The ARN of the flow. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the entitlement. Required.
</ResponseField>

<ResponseField name="subscribers" type="list(string)" required>
  The AWS account IDs that you want to share your content with. The receiving accounts (subscribers) will be allowed to create their own flow using your content as the source. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Key-value pairs that can be used to tag and organize this flow entitlement. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Elemental MediaConnect flow entitlement, used to organize, identify, and optionally drive cost allocation for this entitlement resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag key-value pair associated with this AWS Elemental MediaConnect flow entitlement, used for metadata, cost allocation, and resource management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="entitlement_arn" type="string">
  The ARN of the entitlement. Computed by AWS after `ship`.
</ResponseField>
