---
title: "aws_media_connect_flow"
description: "`aws_media_connect_flow` creates a real managed transport between a live video source and one or more outputs, MediaConnect's original..."
---

`aws_media_connect_flow` creates a real managed transport between a live video source and one or more outputs, MediaConnect's original, source-to-outputs architecture for ingesting, distributing, and routing live video within and beyond AWS.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connect flow"})

    		ubx.Resource(media.ConnectFlow, "example", media.ConnectFlowConfig{
    			Name:                 "example",
    			AvailabilityZone:     "example",
    			EncodingConfig:       map[string]any{"encoding_profile": "example"},
    			FlowSize:             "example",
    			Maintenance:          map[string]any{"maintenance_day": "example"},
    			MediaStreams:         []map[string]any{{"attributes": map[string]any{"fmtp": map[string]any{"channel_order": "example"}}}},
    			NdiConfig:            map[string]any{"machine_name": "example-machine-name"},
    			SourceFailoverConfig: map[string]any{"failover_mode": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectFlow } from "./local-sdk/aws/sdk/typescript/aws/media/connect_flow.ts";

    export default stack("example", () => {
      intent({ summary: "example own connect flow" });

      resource(ConnectFlow, "example", {
        name: "example",
        availabilityZone: "example",
        encodingConfig: { encodingProfile: "example" },
        flowSize: "example",
        maintenance: { maintenanceDay: "example" },
        mediaStreams: [{ attributes: { fmtp: { channelOrder: "example" } } }],
        ndiConfig: { machineName: "example-machine-name" },
        sourceFailoverConfig: { failoverMode: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import ConnectFlow, ConnectFlowConfig

    def describe():
        ubx.intent("example own connect flow")

        ubx.resource(ConnectFlow, "example", ConnectFlowConfig(
            name="example",
            availability_zone="example",
            encoding_config={"encoding_profile": "example"},
            flow_size="example",
            maintenance={"maintenance_day": "example"},
            media_streams=[{"attributes": {"fmtp": {"channel_order": "example"}}}],
            ndi_config={"machine_name": "example-machine-name"},
            source_failover_config={"failover_mode": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_connect_flow called "example", with availability
    zone example, encoding config a set value, flow size example,
    maintenance a set value, media streams a set value, ndi config a
    set value, source failover config a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="availability_zone" type="string">
  The Availability Zone that you want to create the flow in. These options are limited to the Availability Zones within the current AWS. Optional.
</ResponseField>

<ResponseField name="encoding_config" type="object">
  Configures the video encoding algorithm (such as H.264) and its specific parameters (resolution, bitrate, frame rate) applied to the MediaConnect flow's video stream for output. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="encoding_profile" type="string">
      The encoding profile to use when transcoding the NDI source to a Transport Stream. You can change this value while a flow is running. Optional.
    </ResponseField>
    <ResponseField name="video_max_bitrate" type="number">
      The maximum video bitrate to use when transcoding the NDI source to a Transport Stream. This parameter enables you to override the default video bitrate within the encoding profile's supported range. The supported range is 10,000,000 - 50,000,000 bits per second (bps). If you do not specify a value, MediaConnect uses the default value of 20,000,000 bps. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="flow_size" type="string">
  Determines the processing capacity and feature set of the flow. Set this optional parameter to LARGE if you want to enable NDI sources or outputs on the flow. Optional.
</ResponseField>

<ResponseField name="maintenance" type="object">
  The maintenance setting of a flow. Optional.
  <Expandable title="properties">
    <ResponseField name="maintenance_day" type="string" required>
      A day of a week when the maintenance will happen. Use Monday/Tuesday/Wednesday/Thursday/Friday/Saturday/Sunday. Required.
    </ResponseField>
    <ResponseField name="maintenance_start_hour" type="string" required>
      UTC time when the maintenance will happen. Use 24-hour HH:MM format. Minutes must be 00. Example: 13:00. The default value is 02:00. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="media_streams" type="list(object)">
  The media streams associated with the flow. You can associate any of these media streams with sources and outputs on the flow. Optional.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      The attributes object within a media stream specifies its encoding parameters, such as video resolution, frame rate, and bitrate, which control how the stream is encoded and transmitted. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="fmtp" type="object">
          The FMTP (Format Parameters) for this media stream, containing codec- or transport-specific parameters (as defined in SDP) that describe the stream's encoding characteristics. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="channel_order" type="string">
              The channel_order FMTP parameter defines the ordering of audio channels in the media stream, such as 'L,R' for stereo or 'L,R,C,LFE' for 5.1 surround. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="colorimetry" type="string">
              The colorimetry FMTP parameter of the media stream, which specifies the color space standard (e.g., BT.601, BT.709) for the video content. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="exact_framerate" type="string">
              Sets the exact video frame rate (in frames per second) for this media stream, which is included in the FMTP (Format-Specific Parameters) configuration that MediaConnect uses to define the stream's format. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="par" type="string">
              The pixel aspect ratio (PAR) for the media stream, expressed as a string ratio such as '16:9'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="range" type="string">
              Specifies the horizontal and vertical RTP video range (such as 'full' or 'narrow') for the FMTP attributes of this media stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="scan_mode" type="string">
              Specifies the scan mode (for example, progressive or interlaced) of the video in the media stream's FMTP parameters for an AWS MediaConnect flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="tcs" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="lang" type="string">
          Specifies the language code of the media stream (e.g., 'en-US') in RFC 5646 format, used to identify the language of the audio content. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="clock_rate" type="number">
      The RTP clock rate (in Hz) for the media stream, which MediaConnect uses to timestamp and synchronize packets for that stream (e.g., 48000 for audio, 90000 for video). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      A user-defined description for the media stream, used to identify it within an AWS Elemental MediaConnect flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="fmt" type="number">
      The RTP payload type number (0-127) that identifies the format of the media stream, such as the codec and sampling rate for audio or video. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="media_stream_id" type="number">
      The unique numeric identifier for the media stream within the MediaConnect flow, used to reference the media stream when configuring sources and outputs. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="media_stream_name" type="string">
      A unique name for the media stream within the MediaConnect flow, used to identify the stream when associating it with outputs or entitlements. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="media_stream_type" type="string">
      The type of the media stream, such as video, audio, ancillary-data, or combination, which determines how the stream is handled in the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="value" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="video_format" type="string">
      The video format of the media stream, specifying the video resolution and frame rate (e.g., '1080p') for the flow's video transport. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the flow. Required.
</ResponseField>

<ResponseField name="ndi_config" type="object">
  Specifies the configuration settings for NDI sources and outputs. Required when the flow includes NDI sources or outputs. Optional.
  <Expandable title="properties">
    <ResponseField name="machine_name" type="string">
      A prefix for the names of the NDI sources that the flow creates. If a custom name isn't specified, MediaConnect generates a unique 12-character ID as the prefix. Optional.
    </ResponseField>
    <ResponseField name="ndi_discovery_servers" type="list(object)">
      A list of up to three NDI discovery server configurations. While not required by the API, this configuration is necessary for NDI functionality to work properly. Optional.
      <Expandable title="properties">
        <ResponseField name="discovery_server_address" type="string">
          The IP address or hostname of the NDI Discovery Server used by the flow to discover NDI sources on the network. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="discovery_server_port" type="number">
          The TCP port on which the NDI discovery server listens, used to discover NDI sources for the MediaConnect flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="vpc_interface_adapter" type="string">
          Specifies the VPC interface adapter name that the NDI discovery server uses to communicate within a virtual private cloud, enabling the MediaConnect flow to connect to NDI sources on that private network. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ndi_state" type="string">
      Specifies whether the NDI (Network Device Interface) output configuration is enabled or disabled for the flow, using values ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_failover_config" type="object">
  The settings for source failover Optional.
  <Expandable title="properties">
    <ResponseField name="failover_mode" type="string">
      The type of failover you choose for this flow. MERGE combines the source streams into a single stream, allowing graceful recovery from any single-source loss. FAILOVER allows switching between different streams. Optional.
    </ResponseField>
    <ResponseField name="recovery_window" type="number">
      Search window time to look for dash-7 packets Optional.
    </ResponseField>
    <ResponseField name="source_priority" type="object">
      The priority you want to assign to a source. You can have a primary stream and a backup stream or two equally prioritized streams. Optional.
      <Expandable title="properties">
        <ResponseField name="primary_source" type="string" required>
          The name of the source you choose as the primary source for this flow. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      Determines whether automatic source failover is enabled for the MediaConnect flow; set to 'ENABLED' to allow failover to the secondary source, or 'DISABLED' to turn it off. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_monitoring_config" type="object">
  The settings for source monitoring. Optional.
  <Expandable title="properties">
    <ResponseField name="audio_monitoring_settings" type="list(object)">
      Contains the settings for audio stream metrics monitoring. Optional.
      <Expandable title="properties">
        <ResponseField name="silent_audio" type="object">
          Configures silent audio monitoring for the MediaConnect flow's source, including the duration threshold that triggers a silence alert and whether detection is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Controls whether the flow's source detects silent audio; set to ENABLED to activate monitoring or DISABLED to turn it off. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="threshold_seconds" type="number">
              The duration (in seconds) that incoming audio must remain silent before MediaConnect triggers a silent audio monitoring alarm. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="content_quality_analysis_state" type="string">
      Indicates whether content quality analysis is enabled or disabled. Optional.
    </ResponseField>
    <ResponseField name="thumbnail_state" type="string">
      The state of thumbnail monitoring. Optional.
    </ResponseField>
    <ResponseField name="video_monitoring_settings" type="list(object)">
      Contains the settings for video stream metrics monitoring. Optional.
      <Expandable title="properties">
        <ResponseField name="black_frames" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              Indicates whether black frame detection is enabled for the MediaConnect source's video monitoring, with allowed values ENABLED or DISABLED. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="threshold_seconds" type="number">
              The threshold_seconds value determines the minimum continuous duration of black frames in the video stream that must be detected before the source monitoring alert is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="frozen_frames" type="object">
          Specifies the detection settings for frozen frames in the video stream, including whether the detection is enabled and the maximum duration (in milliseconds) that a frame can remain unchanged before it is considered frozen. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="state" type="string">
              The state of frozen frame detection in the source's video monitoring settings, specifying whether the detection of frozen video frames is enabled (ENABLED) or disabled (DISABLED). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="threshold_seconds" type="number">
              Specifies the number of seconds a video frame must remain frozen before MediaConnect considers the source frozen and raises an alarm. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Key-value pairs that can be used to tag this flow. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag to associate with the MediaConnect flow, used to organize and identify the flow resource for cost allocation and access management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag attached to the MediaConnect flow, used for identifying, organizing, and managing the flow via AWS cost allocation and resource-level access controls. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_interfaces" type="list(object)">
  The VPC interfaces that you added to this flow. Optional.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The name of the VPC interface, which identifies it within the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network_interface_ids" type="list(string)">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="network_interface_type" type="string">
      Specifies the network interface type for the VPC interface, either 'EIP' to allocate an Elastic IP address or 'ANY' for a standard network interface without a public IP. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="role_arn" type="string">
      ARN of the IAM role that MediaConnect assumes to create and manage the elastic network interface in the VPC for this flow's VPC interface. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="security_group_ids" type="list(string)">
      The list of security group IDs that are associated with the VPC interface used by the MediaConnect flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnet_id" type="string">
      The ID of the subnet in which the VPC interface is created. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The key of a tag applied to a VPC interface within an AWS MediaConnect flow, used to attach identifying or organizational metadata to that interface. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="egress_ip" type="string">
  The IP address from which video will be sent to output destinations. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="flow_arn" type="string">
  The Amazon Resource Name (ARN), a unique identifier for any AWS resource, of the flow. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="flow_availability_zone" type="string">
  The Availability Zone that you want to create the flow in. These options are limited to the Availability Zones within the current AWS.(ReadOnly) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="flow_ndi_machine_name" type="string">
  A prefix for the names of the NDI sources that the flow creates.(ReadOnly) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source" type="object">
  The settings for the source of the flow. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="decryption" type="object">
      Information about the encryption of the flow. Optional.
      <Expandable title="properties">
        <ResponseField name="algorithm" type="string">
          The type of algorithm that is used for the encryption (such as aes128, aes192, or aes256). Optional.
        </ResponseField>
        <ResponseField name="constant_initialization_vector" type="string">
          A 128-bit, 16-byte hex value represented by a 32-character string, to be used with the key for encrypting content. This parameter is not valid for static key encryption. Optional.
        </ResponseField>
        <ResponseField name="device_id" type="string">
          The value of one of the devices that you configured with your digital rights management (DRM) platform key provider. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
        </ResponseField>
        <ResponseField name="key_type" type="string">
          The type of key that is used for the encryption. If no keyType is provided, the service will use the default setting (static-key). Optional.
        </ResponseField>
        <ResponseField name="region" type="string">
          The AWS Region that the API Gateway proxy endpoint was created in. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
        </ResponseField>
        <ResponseField name="resource_id" type="string">
          An identifier for the content. The service sends this value to the key server to identify the current endpoint. The resource ID is also known as the content ID. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
        </ResponseField>
        <ResponseField name="role_arn" type="string" required>
          The ARN of the role that you created during setup (when you set up AWS Elemental MediaConnect as a trusted entity). Required.
        </ResponseField>
        <ResponseField name="secret_arn" type="string">
          The ARN of the secret that you created in AWS Secrets Manager to store the encryption key. This parameter is required for static key encryption and is not valid for SPEKE encryption. Optional.
        </ResponseField>
        <ResponseField name="url" type="string">
          The URL from the API Gateway proxy that you set up to talk to your key server. This parameter is required for SPEKE encryption and is not valid for static key encryption. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      A description for the source. This value is not used or seen outside of the current AWS Elemental MediaConnect account. Optional.
    </ResponseField>
    <ResponseField name="entitlement_arn" type="string">
      The ARN of the entitlement that allows you to subscribe to content that comes from another AWS account. The entitlement is set by the content originator and the ARN is generated as part of the originator's flow. Optional.
    </ResponseField>
    <ResponseField name="gateway_bridge_source" type="object">
      The source configuration for cloud flows receiving a stream from a bridge. Optional.
      <Expandable title="properties">
        <ResponseField name="bridge_arn" type="string" required>
          The ARN of the bridge feeding this flow. Required.
        </ResponseField>
        <ResponseField name="vpc_interface_attachment" type="object">
          The settings for attaching a VPC interface to an resource. Optional.
          <Expandable title="properties">
            <ResponseField name="vpc_interface_name" type="string">
              The name of the VPC interface to use for this resource. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ingest_ip" type="string">
      The IP address that the flow will be listening on for incoming content. Optional.
    </ResponseField>
    <ResponseField name="ingest_port" type="number">
      The port that the flow will be listening on for incoming content. Optional.
    </ResponseField>
    <ResponseField name="max_bitrate" type="number">
      The smoothing max bitrate for RIST, RTP, and RTP-FEC streams. Optional.
    </ResponseField>
    <ResponseField name="max_latency" type="number">
      The maximum latency in milliseconds. This parameter applies only to RIST-based and Zixi-based streams. Optional.
    </ResponseField>
    <ResponseField name="max_sync_buffer" type="number">
      The size of the buffer (in milliseconds) to use to sync incoming source data. Optional.
    </ResponseField>
    <ResponseField name="media_stream_source_configurations" type="list(object)">
      The media stream that is associated with the source, and the parameters for that association. Optional.
      <Expandable title="properties">
        <ResponseField name="encoding_name" type="string">
          The encoding name (e.g., 'raw', 'aac', or 'nal') assigned to the media stream within the flow source's media stream source configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_configurations" type="list(object)">
          Specifies the input port and network interface for each media stream in the flow's source, enabling the flow to receive each media stream on a dedicated port and interface. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="input_port" type="number">
              The port number on the referenced interface where this input configuration receives the media stream from the upstream source. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="interface" type="object">
              This object identifies the VPC interface used for this input configuration by the flow's VPC interface name, which must correspond to a VPC interface attached to the flow's source. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="name" type="string">
                  The name of the network interface on the source device that receives the media stream for this input configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="media_stream_name" type="string">
          Specifies the friendly name of the media stream that this source configuration applies to, matching the media stream name that the upstream source encodes. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="min_latency" type="number">
      The minimum latency in milliseconds. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the source. Optional.
    </ResponseField>
    <ResponseField name="ndi_source_settings" type="object">
      This object configures the NDI (Network Device Interface) source for the flow, specifying the listening port and NDI tier (e.g., NDI or NDI|HX) that MediaConnect uses to receive the stream from an NDI device. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="source_name" type="string">
          Specifies the NDI source name that identifies the incoming NDI stream to be used as the source for the MediaConnect flow. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="protocol" type="string">
      The protocol that is used by the source. Optional.
    </ResponseField>
    <ResponseField name="router_integration_state" type="string">
      Indicates the current integration state of the source with an AWS Elemental MediaConnect router, such as ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="router_integration_transit_decryption" type="object">
      The configuration that defines how content is encrypted during transit between the MediaConnect router and a MediaConnect flow. Optional.
      <Expandable title="properties">
        <ResponseField name="encryption_key_configuration" type="object" required>
          Configuration settings for flow transit encryption keys. Required.
          <Expandable title="properties">
            <ResponseField name="automatic" type="any">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="secrets_manager" type="object">
              Specifies the AWS Secrets Manager secret that contains the encryption key used for decrypting the incoming media source's transit integration. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="role_arn" type="string">
                  The ARN of an IAM role that AWS Elemental MediaConnect assumes to access the AWS Secrets Manager secret containing the transit encryption key for decryption of the source flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="secret_arn" type="string">
                  The ARN of the AWS Secrets Manager secret that stores the decryption key for decrypting transit traffic on this MediaConnect flow source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="encryption_key_type" type="string">
          The type of encryption key (for example, 'static_key') used by the MediaConnect flow to decrypt the transit stream arriving from the router integration source. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sender_control_port" type="number">
      The port that the flow uses to send outbound requests to initiate connection with the sender for fujitsu-qos protocol. Optional.
    </ResponseField>
    <ResponseField name="sender_ip_address" type="string">
      The IP address that the flow communicates with to initiate connection with the sender for fujitsu-qos protocol. Optional.
    </ResponseField>
    <ResponseField name="source_arn" type="string">
      The ARN of the source. Optional.
    </ResponseField>
    <ResponseField name="source_ingest_port" type="string">
      The port that the flow will be listening on for incoming content.(ReadOnly) Optional.
    </ResponseField>
    <ResponseField name="source_listener_address" type="string">
      Source IP or domain name for SRT-caller protocol. Optional.
    </ResponseField>
    <ResponseField name="source_listener_port" type="number">
      Source port for SRT-caller protocol. Optional.
    </ResponseField>
    <ResponseField name="stream_id" type="string">
      The stream ID that you want to use for this transport. This parameter applies only to Zixi-based streams. Optional.
    </ResponseField>
    <ResponseField name="tags" type="list(object)">
      Key-value pairs that can be used to tag this source. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="value" type="string">
          This field specifies the value portion of a tag applied to a source of an AWS Elemental MediaConnect flow, used to attach metadata such as environment or project names to the source for identification and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpc_interface_name" type="string">
      The name of the VPC Interface this Source is configured with. Optional.
    </ResponseField>
    <ResponseField name="whitelist_cidr" type="string">
      The range of IP addresses that should be allowed to contribute content to your source. These IP addresses should be in the form of a Classless Inter-Domain Routing (CIDR) block; for example, 10.0.0.0/16. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
