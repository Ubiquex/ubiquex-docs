---
title: "aws_media_live_event_bridge_rule_template"
description: "`aws_media_live_event_bridge_rule_template` defines a real reusable EventBridge rule for a MediaLive event type, a channel alert, an input change..."
---

`aws_media_live_event_bridge_rule_template` defines a real reusable EventBridge rule for a MediaLive event type, a channel alert, an input change, a signal map alarm, and more, which can be applied consistently across resources rather than hand-configured per resource.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own live event bridge rule template"})

    		ubx.Resource(media.LiveEventBridgeRuleTemplate, "ci-runner", media.LiveEventBridgeRuleTemplateConfig{
    			EventType:       "example",
    			Name:            "ci-runner",
    			Description:     "Managed by ubx.",
    			EventTargets:    []map[string]any{{"arn": "example"}},
    			GroupIdentifier: "example",
    			Tags:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LiveEventBridgeRuleTemplate } from "./local-sdk/aws/sdk/typescript/aws/media/live_event_bridge_rule_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own live event bridge rule template" });

      resource(LiveEventBridgeRuleTemplate, "ci-runner", {
        eventType: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        eventTargets: [{ arn: "example" }],
        groupIdentifier: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import LiveEventBridgeRuleTemplate, LiveEventBridgeRuleTemplateConfig

    def describe():
        ubx.intent("payments own live event bridge rule template")

        ubx.resource(LiveEventBridgeRuleTemplate, "ci-runner", LiveEventBridgeRuleTemplateConfig(
            event_type="example",
            name="ci-runner",
            description="Managed by ubx.",
            event_targets=[{"arn": "example"}],
            group_identifier="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_live_event_bridge_rule_template called
    "ci-runner", with event type example, description Managed by
    ubx., event targets a set value, group identifier example, tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A resource's optional description. Optional.
</ResponseField>

<ResponseField name="event_targets" type="list(object)">
  Placeholder documentation for __listOfEventBridgeRuleTemplateTarget Optional.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      The Amazon Resource Name (ARN) of the target resource that the EventBridge rule invokes for this MediaLive event bridge rule template. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="event_type" type="string" required>
  The type of event to match with the rule. Required.
</ResponseField>

<ResponseField name="group_identifier" type="string">
  An eventbridge rule template group's identifier. Can be either be its id or current name. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A resource's name. Names must be unique within the scope of a resource type in a specific region. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  Represents the tags associated with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  An eventbridge rule template's ARN (Amazon Resource Name) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Placeholder documentation for __timestampIso8601 Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_id" type="string">
  An eventbridge rule template group's id. AWS provided template groups have ids that start with `aws-` Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  An eventbridge rule template's id. AWS provided templates have ids that start with `aws-` Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="identifier" type="string">
  Placeholder documentation for __string Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="modified_at" type="string">
  Placeholder documentation for __timestampIso8601 Computed by AWS after `ship`.
</ResponseField>
