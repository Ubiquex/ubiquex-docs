---
title: "aws_media_live_event_bridge_rule_template_group"
description: "`aws_media_live_event_bridge_rule_template_group` groups real `aws_media_live_event_bridge_rule_template`s together so the group can be attached to..."
---

`aws_media_live_event_bridge_rule_template_group` groups real `aws_media_live_event_bridge_rule_template`s together so the group can be attached to a MediaLive signal map or channel in one step.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own live event bridge rule template group"})

    		ubx.Resource(media.LiveEventBridgeRuleTemplateGroup, "example", media.LiveEventBridgeRuleTemplateGroupConfig{
    			Name:        "example",
    			Description: "Managed by ubx.",
    			Tags:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LiveEventBridgeRuleTemplateGroup } from "./local-sdk/aws/sdk/typescript/aws/media/live_event_bridge_rule_template_group.ts";

    export default stack("example", () => {
      intent({ summary: "example own live event bridge rule template group" });

      resource(LiveEventBridgeRuleTemplateGroup, "example", {
        name: "example",
        description: "Managed by ubx.",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import LiveEventBridgeRuleTemplateGroup, LiveEventBridgeRuleTemplateGroupConfig

    def describe():
        ubx.intent("example own live event bridge rule template group")

        ubx.resource(LiveEventBridgeRuleTemplateGroup, "example", LiveEventBridgeRuleTemplateGroupConfig(
            name="example",
            description="Managed by ubx.",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_live_event_bridge_rule_template_group called
    "example", with description Managed by ubx., tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A resource's optional description. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A resource's name. Names must be unique within the scope of a resource type in a specific region. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  Represents the tags associated with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  An eventbridge rule template group's ARN (Amazon Resource Name) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp indicating when the EventBridge rule template group was created (computed by AWS). Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  An eventbridge rule template group's id. AWS provided template groups have ids that start with `aws-` Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="identifier" type="string">
  The unique identifier assigned by AWS MediaLive to this event bridge rule template group, used to reference the group after creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="modified_at" type="string">
  The date and time when the EventBridge rule template group was last modified. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
