---
title: "aws_media_package_packaging_configuration"
description: "`aws_media_package_packaging_configuration` defines a real output packaging format, HLS, DASH, CMAF, MSS..."
---

`aws_media_package_packaging_configuration` defines a real output packaging format, HLS, DASH, CMAF, MSS, for on-demand assets within an `aws_media_package_packaging_group`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own package packaging configuration"})

    		ubx.Resource(media.PackagePackagingConfiguration, "example", media.PackagePackagingConfigurationConfig{
    			Id:               "example",
    			PackagingGroupId: "example",
    			CmafPackage:      map[string]any{"hls_manifests": []map[string]any{{"ad_markers": "example"}}},
    			DashPackage:      map[string]any{"dash_manifests": []map[string]any{{"manifest_layout": "example"}}},
    			HlsPackage:       map[string]any{"hls_manifests": []map[string]any{{"ad_markers": "example"}}},
    			MssPackage:       map[string]any{"mss_manifests": []map[string]any{{"manifest_name": "example-manifest-name"}}},
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PackagePackagingConfiguration } from "./local-sdk/aws/sdk/typescript/aws/media/package_packaging_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "example own package packaging configuration" });

      resource(PackagePackagingConfiguration, "example", {
        id: "example",
        packagingGroupId: "example",
        cmafPackage: { hlsManifests: [{ adMarkers: "example" }] },
        dashPackage: { dashManifests: [{ manifestLayout: "example" }] },
        hlsPackage: { hlsManifests: [{ adMarkers: "example" }] },
        mssPackage: { mssManifests: [{ manifestName: "example-manifest-name" }] },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import PackagePackagingConfiguration, PackagePackagingConfigurationConfig

    def describe():
        ubx.intent("example own package packaging configuration")

        ubx.resource(PackagePackagingConfiguration, "example", PackagePackagingConfigurationConfig(
            id="example",
            packaging_group_id="example",
            cmaf_package={"hls_manifests": [{"ad_markers": "example"}]},
            dash_package={"dash_manifests": [{"manifest_layout": "example"}]},
            hls_package={"hls_manifests": [{"ad_markers": "example"}]},
            mss_package={"mss_manifests": [{"manifest_name": "example-manifest-name"}]},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_package_packaging_configuration, with id
    example, packaging group id example, cmaf package a set value,
    dash package a set value, hls package a set value, mss package a
    set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cmaf_package" type="object">
  A CMAF packaging configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="encryption" type="object">
      A CMAF encryption configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="speke_key_provider" type="object" required>
          A configuration for accessing an external Secure Packager and Encoder Key Exchange (SPEKE) service that will provide encryption keys. Required.
          <Expandable title="properties">
            <ResponseField name="encryption_contract_configuration" type="object">
              The configuration to use for encrypting one or more content tracks separately for endpoints that use SPEKE 2.0. Optional.
              <Expandable title="properties">
                <ResponseField name="preset_speke20_audio" type="string" required>
                  A collection of audio encryption presets. Required.
                </ResponseField>
                <ResponseField name="preset_speke20_video" type="string" required>
                  A collection of video encryption presets. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="role_arn" type="string" required>
              An Amazon Resource Name (ARN) of an IAM role that AWS Elemental MediaPackage will assume when accessing the key provider service. Required.
            </ResponseField>
            <ResponseField name="system_ids" type="list(string)" required>
              The system IDs to include in key requests. Required.
            </ResponseField>
            <ResponseField name="url" type="string" required>
              The URL of the external key provider service. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hls_manifests" type="list(object)" required>
      A list of HLS manifest configurations. Required.
      <Expandable title="properties">
        <ResponseField name="ad_markers" type="string">
          Specifies how SCTE-35 ad markers are signaled in this HLS manifest, with valid values NONE, SCTE35_ENHANCED, or PASSTHROUGH. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="include_iframe_only_stream" type="bool">
          When enabled, this HLS manifest generated by the CMAF packaging configuration includes an I-frame-only stream (a variant containing only keyframes), which players can use for fast seek or scrubbing. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manifest_name" type="string">
          Specifies the name of the HLS manifest that is generated as part of the CMAF packaging configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="program_date_time_interval_seconds" type="number">
          Specifies the interval in seconds between EXT-X-PROGRAM-DATE-TIME tags in the HLS manifest, used for timestamp alignment. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repeat_ext_xkey" type="bool">
          When true, MediaPackage repeats the EXT-X-KEY tag for each media segment in the HLS manifests generated by this CMAF packaging configuration, rather than including it only once in the playlist. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_selection" type="object">
          The stream_selection object defines criteria such as minimum and maximum video bitrate and stream ordering that determine which video and audio tracks are included in the HLS manifest for the CMAF packaging configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_video_bits_per_second" type="number">
              This sets the maximum video bitrate threshold for selecting which video streams to include in the HLS manifest for the CMAF packaging configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min_video_bits_per_second" type="number">
              Specifies the minimum video bitrate (in bits per second) for video streams to be included in the HLS manifest of the CMAF package, so only streams with at least this bitrate are selected for output. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stream_order" type="string">
              Determines the order in which video streams appear in the HLS manifest, with allowed values such as ORIGINAL, VIDEO_BITRATE_ASCENDING, and VIDEO_BITRATE_DESCENDING. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="include_encoder_configuration_in_segments" type="bool">
      When includeEncoderConfigurationInSegments is set to true, MediaPackage places your encoder's Sequence Parameter Set (SPS), Picture Parameter Set (PPS), and Video Parameter Set (VPS) metadata in every video segment instead of in the init fragment. This lets you use different SPS/PPS/VPS settings for your assets during content playback. Optional.
    </ResponseField>
    <ResponseField name="segment_duration_seconds" type="number">
      Duration (in seconds) of each fragment. Actual fragments will be rounded to the nearest multiple of the source fragment duration. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dash_package" type="object">
  A Dynamic Adaptive Streaming over HTTP (DASH) packaging configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="dash_manifests" type="list(object)" required>
      A list of DASH manifest configurations. Required.
      <Expandable title="properties">
        <ResponseField name="manifest_layout" type="string">
          Determines the DASH manifest layout: FULL outputs a single manifest for all periods, while COMPACT outputs one manifest per period. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manifest_name" type="string">
          Specifies the name of the DASH manifest that AWS Elemental MediaPackage generates for this packaging configuration, which is used in the output URL. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_buffer_time_seconds" type="number">
          For each DASH manifest, this specifies the minimum buffer time in seconds that a media player must buffer before it can start playing the stream. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="profile" type="string">
          Specifies the DASH profile for the manifest, such as NONE, HBBTV_1_5, or DVB_DASH_2014, which determines the DASH compliance and features used in the packaging configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="scte_markers_source" type="string">
          Specifies whether SCTE-35 markers for the DASH manifest are sourced from the input manifest (MANIFEST) or from the encoded segments (SEGMENTS). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_selection" type="object">
          Defines the criteria (such as minimum and maximum video bitrates and stream ordering) used to select the video, audio, and subtitle tracks to include in the DASH manifest. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_video_bits_per_second" type="number">
              Sets the maximum video bitrate (in bits per second) used to select which video streams are included in the DASH manifest, filtering out higher-bitrate tracks. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min_video_bits_per_second" type="number">
              The minimum video bitrate in bits per second that a video stream must meet to be included in the DASH manifest's stream selection. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stream_order" type="string">
              Specifies the order in which streams are listed in the DASH manifest, with valid values ORIGINAL, VIDEO_BITRATE_ASCENDING, or VIDEO_BITRATE_DESCENDING. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="encryption" type="object">
      A Dynamic Adaptive Streaming over HTTP (DASH) encryption configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="speke_key_provider" type="object" required>
          A configuration for accessing an external Secure Packager and Encoder Key Exchange (SPEKE) service that will provide encryption keys. Required.
          <Expandable title="properties">
            <ResponseField name="encryption_contract_configuration" type="object">
              The configuration to use for encrypting one or more content tracks separately for endpoints that use SPEKE 2.0. Optional.
              <Expandable title="properties">
                <ResponseField name="preset_speke20_audio" type="string" required>
                  A collection of audio encryption presets. Required.
                </ResponseField>
                <ResponseField name="preset_speke20_video" type="string" required>
                  A collection of video encryption presets. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="role_arn" type="string" required>
              An Amazon Resource Name (ARN) of an IAM role that AWS Elemental MediaPackage will assume when accessing the key provider service. Required.
            </ResponseField>
            <ResponseField name="system_ids" type="list(string)" required>
              The system IDs to include in key requests. Required.
            </ResponseField>
            <ResponseField name="url" type="string" required>
              The URL of the external key provider service. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="include_encoder_configuration_in_segments" type="bool">
      When includeEncoderConfigurationInSegments is set to true, MediaPackage places your encoder's Sequence Parameter Set (SPS), Picture Parameter Set (PPS), and Video Parameter Set (VPS) metadata in every video segment instead of in the init fragment. This lets you use different SPS/PPS/VPS settings for your assets during content playback. Optional.
    </ResponseField>
    <ResponseField name="include_iframe_only_stream" type="bool">
      When enabled, an I-Frame only stream will be included in the output. Optional.
    </ResponseField>
    <ResponseField name="period_triggers" type="list(string)">
      A list of triggers that controls when the outgoing Dynamic Adaptive Streaming over HTTP (DASH) Media Presentation Description (MPD) will be partitioned into multiple periods. If empty, the content will not be partitioned into more than one period. If the list contains "ADS", new periods will be created where the Asset contains SCTE-35 ad markers. Optional.
    </ResponseField>
    <ResponseField name="segment_duration_seconds" type="number">
      Duration (in seconds) of each fragment. Actual fragments will be rounded to the nearest multiple of the source fragment duration. Optional.
    </ResponseField>
    <ResponseField name="segment_template_format" type="string">
      Determines the type of SegmentTemplate included in the Media Presentation Description (MPD). When set to NUMBER_WITH_TIMELINE, a full timeline is presented in each SegmentTemplate, with $Number$ media URLs. When set to TIME_WITH_TIMELINE, a full timeline is presented in each SegmentTemplate, with $Time$ media URLs. When set to NUMBER_WITH_DURATION, only a duration is included in each SegmentTemplate, with $Number$ media URLs. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hls_package" type="object">
  An HTTP Live Streaming (HLS) packaging configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="encryption" type="object">
      An HTTP Live Streaming (HLS) encryption configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="constant_initialization_vector" type="string">
          An HTTP Live Streaming (HLS) encryption configuration. Optional.
        </ResponseField>
        <ResponseField name="encryption_method" type="string">
          The encryption method to use. Optional.
        </ResponseField>
        <ResponseField name="speke_key_provider" type="object" required>
          A configuration for accessing an external Secure Packager and Encoder Key Exchange (SPEKE) service that will provide encryption keys. Required.
          <Expandable title="properties">
            <ResponseField name="encryption_contract_configuration" type="object">
              The configuration to use for encrypting one or more content tracks separately for endpoints that use SPEKE 2.0. Optional.
              <Expandable title="properties">
                <ResponseField name="preset_speke20_audio" type="string" required>
                  A collection of audio encryption presets. Required.
                </ResponseField>
                <ResponseField name="preset_speke20_video" type="string" required>
                  A collection of video encryption presets. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="role_arn" type="string" required>
              An Amazon Resource Name (ARN) of an IAM role that AWS Elemental MediaPackage will assume when accessing the key provider service. Required.
            </ResponseField>
            <ResponseField name="system_ids" type="list(string)" required>
              The system IDs to include in key requests. Required.
            </ResponseField>
            <ResponseField name="url" type="string" required>
              The URL of the external key provider service. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hls_manifests" type="list(object)" required>
      A list of HLS manifest configurations. Required.
      <Expandable title="properties">
        <ResponseField name="ad_markers" type="string">
          Determines the ad marker type to include in the HLS manifest, with valid values of none, scte35-enhanced, and passthrough, controlling how SCTE-35 ad signaling is represented in the manifest. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="include_iframe_only_stream" type="bool">
          When enabled, the HLS manifest includes an additional I-frame-only media playlist, enabling trick-play (fast-forward and rewind) for video content. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="manifest_name" type="string">
          Specifies the name of the HLS manifest file (e.g., 'index') that MediaPackage generates for the packaging configuration, which is used in the playback URL to request the manifest playlist. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="program_date_time_interval_seconds" type="number">
          Specifies the interval in seconds between EXT-X-PROGRAM-DATE-TIME tags in the HLS manifest. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repeat_ext_xkey" type="bool">
          When true, MediaPackage repeats the EXT-X-KEY tag for every media segment in the HLS manifest; when false, the tag appears only once. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_selection" type="object">
          Specifies which input video/audio streams are included in this HLS manifest, based on bitrate range and stream ordering criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_video_bits_per_second" type="number">
              Defines the maximum video bitrate (in bits per second) for streams selected into the HLS manifest, so streams with a higher bitrate are excluded. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min_video_bits_per_second" type="number">
              The minimum video bitrate (in bits per second) that a video stream must have to be included in the HLS manifest's stream selection. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stream_order" type="string">
              Specifies the order in which video streams are listed in the HLS manifest, with valid values typically 'ORIGINAL' (preserving source order), 'VIDEO_BITRATE_ASCENDING', or 'VIDEO_BITRATE_DESCENDING' (sorting by video bitrate). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="include_dvb_subtitles" type="bool">
      When enabled, MediaPackage passes through digital video broadcasting (DVB) subtitles into the output. Optional.
    </ResponseField>
    <ResponseField name="segment_duration_seconds" type="number">
      Duration (in seconds) of each fragment. Actual fragments will be rounded to the nearest multiple of the source fragment duration. Optional.
    </ResponseField>
    <ResponseField name="use_audio_rendition_group" type="bool">
      When enabled, audio streams will be placed in rendition groups in the output. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string" required>
  The ID of the PackagingConfiguration. Required.
</ResponseField>

<ResponseField name="mss_package" type="object">
  A Microsoft Smooth Streaming (MSS) PackagingConfiguration. Optional.
  <Expandable title="properties">
    <ResponseField name="encryption" type="object">
      A CMAF encryption configuration. Optional.
      <Expandable title="properties">
        <ResponseField name="speke_key_provider" type="object" required>
          A configuration for accessing an external Secure Packager and Encoder Key Exchange (SPEKE) service that will provide encryption keys. Required.
          <Expandable title="properties">
            <ResponseField name="encryption_contract_configuration" type="object">
              The configuration to use for encrypting one or more content tracks separately for endpoints that use SPEKE 2.0. Optional.
              <Expandable title="properties">
                <ResponseField name="preset_speke20_audio" type="string" required>
                  A collection of audio encryption presets. Required.
                </ResponseField>
                <ResponseField name="preset_speke20_video" type="string" required>
                  A collection of video encryption presets. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="role_arn" type="string" required>
              An Amazon Resource Name (ARN) of an IAM role that AWS Elemental MediaPackage will assume when accessing the key provider service. Required.
            </ResponseField>
            <ResponseField name="system_ids" type="list(string)" required>
              The system IDs to include in key requests. Required.
            </ResponseField>
            <ResponseField name="url" type="string" required>
              The URL of the external key provider service. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mss_manifests" type="list(object)" required>
      A list of MSS manifest configurations. Required.
      <Expandable title="properties">
        <ResponseField name="manifest_name" type="string">
          Specifies the name of the Microsoft Smooth Streaming manifest, which is used to generate the URL for requesting that manifest. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stream_selection" type="object">
          Configures which source video and audio streams are selected for the Microsoft Smooth Streaming manifest, supporting bitrate-based selection (MinVideoBitsPerSecond, MaxVideoBitsPerSecond) and stream ordering (StreamOrder). Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="max_video_bits_per_second" type="number">
              The maximum video bitrate (in bits per second) to include in the Microsoft Smooth Streaming manifest, filtering out video streams above this threshold. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="min_video_bits_per_second" type="number">
              Specifies the minimum video bitrate (in bits per second) a video stream must have to be included in this Microsoft Smooth Streaming manifest, filtering out lower-bitrate streams. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stream_order" type="string">
              Determines the order in which video streams are listed in the Microsoft Smooth Streaming manifest, with options for original order or sorting by video bitrate ascending or descending. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="segment_duration_seconds" type="number">
      Duration (in seconds) of each fragment. Actual fragments will be rounded to the nearest multiple of the source fragment duration. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="packaging_group_id" type="string" required>
  The ID of a PackagingGroup. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A collection of tags associated with a resource Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag applied to the MediaPackage packaging configuration, used for organizing, identifying, and managing access to the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag attached to the MediaPackage packaging configuration, used for metadata, cost allocation, and resource management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The ARN of the PackagingConfiguration. Computed by AWS after `ship`.
</ResponseField>
