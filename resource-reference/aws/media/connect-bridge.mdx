---
title: "aws_media_connect_bridge"
description: "`aws_media_connect_bridge` creates a real connection point between an on-premises data center and AWS..."
---

`aws_media_connect_bridge` creates a real connection point between an on-premises data center and AWS, moving live video content from the cloud to a local facility or from a local facility to the cloud, depending on the chosen bridge type.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mediaconnect "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/mediaconnect"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connect bridge"})

    		ubx.Resource(mediaconnect.Bridge, "example", mediaconnect.BridgeConfig{
    			Name:                 "example",
    			PlacementArn:         "arn:aws:iam::123456789012:policy/example",
    			Sources:              []map[string]any{{"flow_source": map[string]any{"name": "example-name"}}},
    			EgressGatewayBridge:  map[string]any{"max_bitrate": 1},
    			IngressGatewayBridge: map[string]any{"max_bitrate": 1},
    			Outputs:              []map[string]any{{"network_output": map[string]any{"name": "example-name"}}},
    			SourceFailoverConfig: map[string]any{"failover_mode": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Bridge } from "@ubx/sdk-aws/aws/mediaconnect/bridge";

    export default stack("example", () => {
      intent({ summary: "example own connect bridge" });

      resource(Bridge, "example", {
        name: "example",
        placementArn: "arn:aws:iam::123456789012:policy/example",
        sources: [{ flowSource: { name: "example-name" } }],
        egressGatewayBridge: { maxBitrate: 1 },
        ingressGatewayBridge: { maxBitrate: 1 },
        outputs: [{ networkOutput: { name: "example-name" } }],
        sourceFailoverConfig: { failoverMode: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.mediaconnect.bridge import Bridge, BridgeConfig

    def describe():
        ubx.intent("example own connect bridge")

        ubx.resource(Bridge, "example", BridgeConfig(
            name="example",
            placement_arn="arn:aws:iam::123456789012:policy/example",
            sources=[{"flow_source": {"name": "example-name"}}],
            egress_gateway_bridge={"max_bitrate": 1},
            ingress_gateway_bridge={"max_bitrate": 1},
            outputs=[{"network_output": {"name": "example-name"}}],
            source_failover_config={"failover_mode": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_connect_bridge called "example", with placement
    arn arn:aws:iam::123456789012:policy/example, sources a set
    value, egress gateway bridge a set value, ingress gateway bridge
    a set value, outputs a set value, source failover config a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="egress_gateway_bridge" type="object">
  Defines the egress gateway bridge settings for an AWS Elemental MediaConnect bridge, including the gateway instance ID and optional maximum bitrate for transmitting media flows to a remote network. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_bitrate" type="number" required>
      The maximum expected bitrate of the egress bridge. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ingress_gateway_bridge" type="object">
  The 'ingress_gateway_bridge' object configures the ingress gateway bridge settings for the MediaConnect bridge, including the maximum number of outputs it can support. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_bitrate" type="number" required>
      The maximum expected bitrate of the ingress bridge. Required.
    </ResponseField>
    <ResponseField name="max_outputs" type="number" required>
      The maximum number of outputs on the ingress bridge. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the bridge. Required.
</ResponseField>

<ResponseField name="outputs" type="list(object)">
  The outputs on this bridge. Optional.
  <Expandable title="properties">
    <ResponseField name="network_output" type="object">
      Configures the destination IP address, port, protocol, TTL, and other network parameters for sending the bridge's media output over the network. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ip_address" type="string">
          The destination IP address to which this bridge's network output sends media. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name assigned to a network output of the AWS Elemental MediaConnect bridge, used to identify that output within the bridge. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_name" type="string">
          Specifies the name of the network that this bridge network output uses, identifying the destination network for the output. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port" type="number">
          The destination port number on which the MediaConnect bridge network output sends media to the remote device. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protocol" type="string">
          Specifies the transport protocol used by the bridge's network output, such as RTP, RTP-FEC, or UDP, for sending media over IP. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ttl" type="number">
          The Time-to-Live (TTL) value for multicast packets sent from the network output, limiting the number of network hops the traffic can traverse. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="placement_arn" type="string" required>
  The placement Amazon Resource Number (ARN) of the bridge. Required.
</ResponseField>

<ResponseField name="source_failover_config" type="object">
  The settings for source failover. Optional.
  <Expandable title="properties">
    <ResponseField name="failover_mode" type="string" required>
      Specifies the failover mode of the bridge source, using FAILOVER to enable automatic failover to a redundant source or DEFAULT for standard behavior. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_priority" type="object">
      The priority you want to assign to a source. You can have a primary stream and a backup stream or two equally prioritized streams. Optional.
      <Expandable title="properties">
        <ResponseField name="primary_source" type="string">
          The name of the source you choose as the primary source for this flow. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="state" type="string">
      Indicates whether automatic failover is enabled or disabled for the bridge's source, with valid values ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sources" type="list(object)" required>
  The sources on this bridge. Required.
  <Expandable title="properties">
    <ResponseField name="flow_source" type="object">
      FlowSource is an object that configures the bridge source to use an existing MediaConnect flow as its source, specifying the flow ARN and the VPC interface attachment for the flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="flow_arn" type="string">
          The Amazon Resource Name (ARN) of the MediaConnect flow that serves as the source of the bridge source. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="flow_vpc_interface_attachment" type="object">
          Specifies the VPC interface attachment settings, including the name of the VPC interface, for this flow source of the AWS Elemental MediaConnect bridge. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="vpc_interface_name" type="string">
              The name of the VPC interface on the source MediaConnect flow that the bridge flow source attaches to for network connectivity. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          Specifies the name assigned to the flow source of the AWS MediaConnect bridge, used to identify the source within the bridge's source configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="network_source" type="object">
      Configures the network source for a source in a MediaConnect bridge, defining the multicast IP address, port, and protocol that the bridge listens on to ingest video from the network. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="multicast_ip" type="string">
          The multicast IP address of the network source that the MediaConnect bridge subscribes to. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="multicast_source_settings" type="object">
          Configures the multicast source settings for the network source on the bridge, specifying the multicast IP address and port from which the bridge receives media. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="multicast_source_ip" type="string">
              The IP address of the multicast source from which the MediaConnect bridge receives content, used in the multicast source settings of a network source. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string">
          A user-defined name that identifies this network source within the bridge's sources configuration in AWS Elemental MediaConnect. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="network_name" type="string">
          The name of the network where the bridge source is located, used to identify the network that provides the content for this MediaConnect bridge source. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="port" type="number">
          The network port on the bridge's source that receives the incoming media stream from an on-premises sender. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="protocol" type="string">
          Specifies the transport protocol used for the network source, which can be either RTP or RTP-FEC, indicating how the incoming media stream is delivered. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="bridge_arn" type="string">
  The Amazon Resource Number (ARN) of the bridge. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="bridge_state" type="string">
  The current lifecycle state of the MediaConnect bridge (e.g., ACTIVE, STANDBY, or DELETING), as reported by AWS after the bridge is created or updated. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
