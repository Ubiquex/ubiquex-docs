---
title: "aws_media_tailor_playback_configuration"
description: "`aws_media_tailor_playback_configuration` defines a real playback endpoint configuration, ad decision server settings, manifest processing rules..."
---

`aws_media_tailor_playback_configuration` defines a real playback endpoint configuration, ad decision server settings, manifest processing rules, and CDN configuration, that a player requests a personalized, ad-stitched manifest from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own tailor playback configuration"})

    		ubx.Resource(media.TailorPlaybackConfiguration, "example", media.TailorPlaybackConfigurationConfig{
    			AdDecisionServerUrl:           "example",
    			Name:                          "example",
    			VideoContentSourceUrl:         "example",
    			AdConditioningConfiguration:   map[string]any{"streaming_media_file_conditioning": "example"},
    			AdDecisionServerConfiguration: map[string]any{"http_request": map[string]any{"body": "example"}},
    			AdsPersonalizationConcurrency: map[string]any{"enable_vod_vast_parallelization": true},
    			AdsPersonalizationTimeouts:    map[string]any{"ads_request_timeout_milliseconds": 1},
    			AvailSuppression:              map[string]any{"fill_policy": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TailorPlaybackConfiguration } from "@ubx/sdk-aws/aws/media/tailor_playback_configuration";

    export default stack("example", () => {
      intent({ summary: "example own tailor playback configuration" });

      resource(TailorPlaybackConfiguration, "example", {
        adDecisionServerUrl: "example",
        name: "example",
        videoContentSourceUrl: "example",
        adConditioningConfiguration: { streamingMediaFileConditioning: "example" },
        adDecisionServerConfiguration: { httpRequest: { body: "example" } },
        adsPersonalizationConcurrency: { enableVodVastParallelization: true },
        adsPersonalizationTimeouts: { adsRequestTimeoutMilliseconds: 1 },
        availSuppression: { fillPolicy: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.media import TailorPlaybackConfiguration, TailorPlaybackConfigurationConfig

    def describe():
        ubx.intent("example own tailor playback configuration")

        ubx.resource(TailorPlaybackConfiguration, "example", TailorPlaybackConfigurationConfig(
            ad_decision_server_url="example",
            name="example",
            video_content_source_url="example",
            ad_conditioning_configuration={"streaming_media_file_conditioning": "example"},
            ad_decision_server_configuration={"http_request": {"body": "example"}},
            ads_personalization_concurrency={"enable_vod_vast_parallelization": True},
            ads_personalization_timeouts={"ads_request_timeout_milliseconds": 1},
            avail_suppression={"fill_policy": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_tailor_playback_configuration called "example",
    with ad decision server url example, video content source url
    example, ad conditioning configuration a set value, ad decision
    server configuration a set value, ads personalization concurrency
    a set value, ads personalization timeouts a set value, avail
    suppression a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="ad_conditioning_configuration" type="object">
  &lt;p&gt;The setting that indicates what conditioning MediaTailor will perform on ads that the ad decision server (ADS) returns.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="streaming_media_file_conditioning" type="string" required>
      Specifies whether to condition the streaming media file for ad stitching, with allowed values 'NONE' (no conditioning) or 'AUDIO_ONLY' (condition the audio track only). Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ad_decision_server_configuration" type="object">
  The configuration for the request to the specified Ad Decision Server URL. Optional.
  <Expandable title="properties">
    <ResponseField name="http_request" type="object" required>
      The configuration for the request to the Ad Decision Server URL. Required.
      <Expandable title="properties">
        <ResponseField name="body" type="string">
          The body of the request to the Ad Decision Server URL. The maximum length is 100,000 characters. Optional.
        </ResponseField>
        <ResponseField name="compress_request" type="string">
          The compression type of the request sent to the Ad Decision Server URL. Only the POST HTTP Method permits compression other than NONE. Optional.
        </ResponseField>
        <ResponseField name="headers" type="any">
          The headers in the request sent to the Ad Decision Server URL. The max length is 10,000 characters. Optional.
        </ResponseField>
        <ResponseField name="http_method" type="string">
          Supported HTTP Methods for the request to the Ad Decision Server URL. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ad_decision_server_url" type="string" required>
  The URL for the ad decision server (ADS). This includes the specification of static parameters and placeholders for dynamic parameters. AWS Elemental MediaTailor substitutes player-specific and session-specific parameters as needed when calling the ADS. Alternately, for testing you can provide a static VAST URL. The maximum length is 25,000 characters. Required.
</ResponseField>

<ResponseField name="ads_personalization_concurrency" type="object">
  The settings that control how many concurrent requests MediaTailor makes to the ad decision server (ADS). Optional.
  <Expandable title="properties">
    <ResponseField name="enable_vod_vast_parallelization" type="bool">
      Enables parallel processing of ADS requests in VOD workflows when the ADS returns VAST responses. The default is false. Optional.
    </ResponseField>
    <ResponseField name="max_concurrent_ads_requests" type="number">
      The maximum number of simultaneous requests that MediaTailor makes to the ADS for each manifest request. The default is 1. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ads_personalization_timeouts" type="object">
  The ad decision server (ADS) request timeouts and personalization time budgets for live, VOD, and prefetch workflows. Optional.
  <Expandable title="properties">
    <ResponseField name="ads_request_timeout_milliseconds" type="number">
      The maximum time, in milliseconds, that MediaTailor waits for a single ADS response during live or VOD playback. The default is 3000. Optional.
    </ResponseField>
    <ResponseField name="live_maximum_ads_personalization_time_milliseconds" type="number">
      The maximum total time, in milliseconds, that MediaTailor spends on ADS activity for live manifests. The default is 10000. Optional.
    </ResponseField>
    <ResponseField name="prefetch_ads_request_timeout_milliseconds" type="number">
      The maximum time, in milliseconds, that MediaTailor waits for a single ADS response during prefetch retrieval. If not set, MediaTailor uses the AdsRequestTimeoutMilliseconds value. Optional.
    </ResponseField>
    <ResponseField name="prefetch_maximum_ads_personalization_time_milliseconds" type="number">
      The maximum total time, in milliseconds, that MediaTailor spends on ADS activity during prefetch retrieval. Optional.
    </ResponseField>
    <ResponseField name="vod_maximum_ads_personalization_time_milliseconds" type="number">
      The maximum total time, in milliseconds, that MediaTailor spends on ADS activity for VOD manifests. The default is 10000. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="avail_suppression" type="object">
  The configuration for avail suppression, also known as ad suppression. For more information about ad suppression, see Ad Suppression (https://docs.aws.amazon.com/mediatailor/latest/ug/ad-behavior.html). Optional.
  <Expandable title="properties">
    <ResponseField name="fill_policy" type="string">
      Defines the policy to apply to the avail suppression mode. BEHIND_LIVE_EDGE will always use the full avail suppression policy. AFTER_LIVE_EDGE mode can be used to invoke partial ad break fills when a session starts mid-break. Valid values are FULL_AVAIL_ONLY and PARTIAL_AVAIL Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      Sets the ad suppression mode. By default, ad suppression is off and all ad breaks are filled with ads or slate. When Mode is set to BEHIND_LIVE_EDGE, ad suppression is active and MediaTailor won't fill ad breaks on or behind the ad suppression Value time in the manifest lookback window. When Mode is set to AFTER_LIVE_EDGE, ad suppression is active and MediaTailor won't fill ad breaks that are within the live edge plus the avail suppression value. Optional.
    </ResponseField>
    <ResponseField name="value" type="string">
      A live edge offset time in HH:MM:SS. MediaTailor won't fill ad breaks on or behind this time in the manifest lookback window. If Value is set to 00:00:00, it is in sync with the live edge, and MediaTailor won't fill any ad breaks on or behind the live edge. If you set a Value time, MediaTailor won't fill any ad breaks on or behind this time in the manifest lookback window. For example, if you set 00:45:00, then MediaTailor will fill ad breaks that occur within 45 minutes behind the live edge, but won't fill ad breaks on or behind 45 minutes behind the live edge. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bumper" type="object">
  The configuration for bumpers. Bumpers are short audio or video clips that play at the start or before the end of an ad break. To learn more about bumpers, see Bumpers (https://docs.aws.amazon.com/mediatailor/latest/ug/bumpers.html). Optional.
  <Expandable title="properties">
    <ResponseField name="end_url" type="string">
      The URL for the end bumper asset. Optional.
    </ResponseField>
    <ResponseField name="start_url" type="string">
      The URL for the start bumper asset. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cdn_configuration" type="object">
  The configuration for using a content delivery network (CDN), like Amazon CloudFront, for content and ad segment management. Optional.
  <Expandable title="properties">
    <ResponseField name="ad_segment_url_prefix" type="string">
      A non-default content delivery network (CDN) to serve ad segments. By default, AWS Elemental MediaTailor uses Amazon CloudFront with default cache settings as its CDN for ad segments. To set up an alternate CDN, create a rule in your CDN for the origin ads.mediatailor.&lt;region&gt;.amazonaws.com. Then specify the rule's name in this AdSegmentUrlPrefix. When AWS Elemental MediaTailor serves a manifest, it reports your CDN as the source for ad segments. Optional.
    </ResponseField>
    <ResponseField name="content_segment_url_prefix" type="string">
      A content delivery network (CDN) to cache content segments, so that content requests don't always have to go to the origin server. First, create a rule in your CDN for the content segment origin server. Then specify the rule's name in this ContentSegmentUrlPrefix. When AWS Elemental MediaTailor serves a manifest, it reports your CDN as the source for content segments. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="configuration_aliases" type="any">
  The predefined aliases for dynamic variables. Optional.
</ResponseField>

<ResponseField name="function_mapping" type="any">
  A map of event names to function identifiers for custom processing during session lifecycle events. Optional.
</ResponseField>

<ResponseField name="insertion_mode" type="string">
  The mode for ad insertion in the playback configuration, where PLAYER_SERVER directs MediaTailor to handle server-side ad insertion and CUSTOM allows the use of a custom ad insertion server. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="live_pre_roll_configuration" type="object">
  The configuration for pre-roll ad insertion. Optional.
  <Expandable title="properties">
    <ResponseField name="ad_decision_server_url" type="string">
      The URL for the ad decision server (ADS) for pre-roll ads. This includes the specification of static parameters and placeholders for dynamic parameters. AWS Elemental MediaTailor substitutes player-specific and session-specific parameters as needed when calling the ADS. Alternately, for testing, you can provide a static VAST URL. The maximum length is 25,000 characters. Optional.
    </ResponseField>
    <ResponseField name="max_duration_seconds" type="number">
      The maximum allowed duration for the pre-roll ad avail. AWS Elemental MediaTailor won't play pre-roll ads to exceed this duration, regardless of the total duration of ads that the ADS returns. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="log_configuration" type="object">
  Configures CloudWatch Logs logging for the playback configuration, including the percentage of session logs to send to CloudWatch Logs. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ads_interaction_log" type="object">
      Configures whether MediaTailor logs ad interaction events, such as clicks and impressions, to CloudWatch Logs for this playback configuration, typically via an Enabled flag within the object. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="exclude_event_types" type="list(string)">
          Indicates that MediaTailor won't emit the selected events in the logs for playback sessions that are initialized with this configuration. Optional.
        </ResponseField>
        <ResponseField name="publish_opt_in_event_types" type="list(string)">
          Indicates that MediaTailor emits RAW_ADS_RESPONSE logs for playback sessions that are initialized with this configuration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled_logging_strategies" type="list(string)">
      The method used for collecting logs from AWS Elemental MediaTailor. To configure MediaTailor to send logs directly to Amazon CloudWatch Logs, choose LEGACY_CLOUDWATCH. To configure MediaTailor to send logs to CloudWatch, which then vends the logs to your destination of choice, choose VENDED_LOGS. Supported destinations are CloudWatch Logs log group, Amazon S3 bucket, and Amazon Data Firehose stream. To use vended logs, you must configure the delivery destination in Amazon CloudWatch Optional.
    </ResponseField>
    <ResponseField name="manifest_service_interaction_log" type="object">
      This object configures whether MediaTailor logs manifest service interaction events to Amazon CloudWatch, with an `Enabled` subfield to turn this logging on or off. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="exclude_event_types" type="list(string)">
          Indicates that MediaTailor won't emit the selected events in the logs for playback sessions that are initialized with this configuration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="percent_enabled" type="number" required>
      The percentage of session logs that MediaTailor sends to your CloudWatch Logs account. For example, if your playback configuration has 1000 sessions and percentEnabled is set to 60, MediaTailor sends logs for 600 of the sessions to CloudWatch Logs. MediaTailor decides at random which of the playback configuration sessions to send logs for. If you want to view logs for a specific session, you can use the debug log mode. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="manifest_processing_rules" type="object">
  The configuration for manifest processing rules. Manifest processing rules enable customization of the personalized manifests created by MediaTailor. Optional.
  <Expandable title="properties">
    <ResponseField name="ad_marker_passthrough" type="object">
      For HLS, when set to true, MediaTailor passes through EXT-X-CUE-IN, EXT-X-CUE-OUT, and EXT-X-SPLICEPOINT-SCTE35 ad markers from the origin manifest to the MediaTailor personalized manifest. No logic is applied to these ad markers. For example, if EXT-X-CUE-OUT has a value of 60, but no ads are filled for that ad break, MediaTailor will not set the value to 0. Optional.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Enables ad marker passthrough for your configuration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The identifier for the playback configuration. Required.
</ResponseField>

<ResponseField name="personalization_threshold_seconds" type="number">
  Defines the maximum duration of underfilled ad time (in seconds) allowed in an ad break. If the duration of underfilled ad time exceeds the personalization threshold, then the personalization of the ad break is abandoned and the underlying content is shown. This feature applies to ad replacement in live and VOD streams, rather than ad insertion, because it relies on an underlying content stream. For more information about ad break behavior, including ad replacement and insertion, see Ad Behavior in AWS Elemental MediaTailor (https://docs.aws.amazon.com/mediatailor/latest/ug/ad-behavior.html). Optional.
</ResponseField>

<ResponseField name="slate_ad_url" type="string">
  The URL for a high-quality video asset to transcode and use to fill in time that's not used by ads. AWS Elemental MediaTailor shows the slate to fill in gaps in media content. Configuring the slate is optional for non-VPAID configurations. For VPAID, the slate is required because MediaTailor provides it in the slots that are designated for dynamic ad content. The slate must be a high-quality asset that contains both audio and video. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags to assign to the playback configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag keyed on the AWS MediaTailor playback configuration, providing an arbitrary string for categorizing, filtering, or cost-allocating the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="transcode_profile_name" type="string">
  The name that is used to associate this playback configuration with a custom transcode profile. This overrides the dynamic transcoding defaults of MediaTailor. Use this only if you have already set up custom profiles with the help of AWS Support. Optional.
</ResponseField>

<ResponseField name="video_content_source_url" type="string" required>
  The URL prefix for the parent manifest for the stream, minus the asset ID. The maximum length is 512 characters. Required.
</ResponseField>

## Output properties

<ResponseField name="dash_configuration" type="object">
  The configuration for DASH PUT operations. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="manifest_endpoint_prefix" type="string">
      The URL generated by MediaTailor to initiate a DASH playback session. The session uses server-side reporting. Optional.
    </ResponseField>
    <ResponseField name="mpd_location" type="string">
      The setting that controls whether MediaTailor includes the Location tag in DASH manifests. MediaTailor populates the Location tag with the URL for manifest update requests, to be used by players that don't support sticky redirects. Disable this if you have CDN routing rules set up for accessing MediaTailor manifests, and you are either using client-side reporting or your players support sticky HTTP redirects. Valid values are DISABLED and EMT_DEFAULT. The EMT_DEFAULT setting enables the inclusion of the tag and is the default value. Optional.
    </ResponseField>
    <ResponseField name="origin_manifest_type" type="string">
      The setting that controls whether MediaTailor handles manifests from the origin server as multi-period manifests or single-period manifests. If your origin server produces single-period manifests, set this to SINGLE_PERIOD. The default setting is MULTI_PERIOD. For multi-period manifests, omit this setting or set it to MULTI_PERIOD. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hls_configuration" type="object">
  The hls_configuration object is a read-only block that exposes the generated manifest endpoint prefix assigned by MediaTailor for delivering the HLS playlist for this playback configuration. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="manifest_endpoint_prefix" type="string">
      The URL that is used to initiate a playback session for devices that support Apple HLS. The session uses server-side reporting. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="playback_configuration_arn" type="string">
  The Amazon Resource Name (ARN) for the playback configuration. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="playback_endpoint_prefix" type="string">
  The URL that the player accesses to get a manifest from MediaTailor. This session will use server-side reporting. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="session_initialization_endpoint_prefix" type="string">
  The URL that the player uses to initialize a session that uses client-side reporting. Computed by AWS after `ship`.
</ResponseField>
