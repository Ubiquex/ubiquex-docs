---
title: "aws_media_connect_router_network_interface"
description: "`aws_media_connect_router_network_interface` provisions a real public or VPC-scoped network interface that an `aws_media_connect_router_input` or..."
---

`aws_media_connect_router_network_interface` provisions a real public or VPC-scoped network interface that an `aws_media_connect_router_input` or router output uses to actually send or receive traffic, the underlying network plumbing beneath the MediaConnect Router's logical inputs and outputs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connect router network interface"})

    		ubx.Resource(media.ConnectRouterNetworkInterface, "ci-runner", media.ConnectRouterNetworkInterfaceConfig{
    			Configuration: map[string]any{"public": map[string]any{"allow_rules": []map[string]any{{"cidr": "example"}}}},
    			Name:          "ci-runner",
    			RegionName:    "example-region-name",
    			Tags:          []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectRouterNetworkInterface } from "./local-sdk/aws/sdk/typescript/aws/media/connect_router_network_interface.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own connect router network interface" });

      resource(ConnectRouterNetworkInterface, "ci-runner", {
        configuration: { public: { allowRules: [{ cidr: "example" }] } },
        name: "ci-runner",
        regionName: "example-region-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import ConnectRouterNetworkInterface, ConnectRouterNetworkInterfaceConfig

    def describe():
        ubx.intent("payments own connect router network interface")

        ubx.resource(ConnectRouterNetworkInterface, "ci-runner", ConnectRouterNetworkInterfaceConfig(
            configuration={"public": {"allow_rules": [{"cidr": "example"}]}},
            name="ci-runner",
            region_name="example-region-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_connect_router_network_interface called
    "ci-runner", with configuration a set value, region name
    example-region-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration" type="object" required>
  The configuration settings for a router network interface. Required.
  <Expandable title="properties">
    <ResponseField name="public" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="allow_rules" type="list(object)">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="cidr" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vpc" type="object">
      Optional; if omitted, computed by AWS.
      <Expandable title="properties">
        <ResponseField name="security_group_ids" type="list(string)">
          The list of security group IDs to associate with the VPC network interface for this MediaConnect router, which determines the firewall rules for traffic to and from the interface. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subnet_id" type="string">
          The ID of the subnet within the customer's VPC where the MediaConnect router network interface is created, as specified in the router's configuration VPC settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the router network interface. Required.
</ResponseField>

<ResponseField name="region_name" type="string">
  The AWS Region for the router network interface. Defaults to the current region if not specified. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Key-value pairs that can be used to tag and organize this router network interface. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag attached to this MediaConnect router network interface, used to categorize and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag pair applied to the AWS MediaConnect router network interface, used for categorizing, identifying, and managing the resource within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the MediaConnect router network interface. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="associated_input_count" type="number">
  The number of router inputs associated with the network interface. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="associated_output_count" type="number">
  The number of router outputs associated with the network interface. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp when the router network interface was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the router network interface. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="network_interface_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the router network interface was last updated. Computed by AWS after `ship`.
</ResponseField>
