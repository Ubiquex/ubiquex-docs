---
title: "aws_media_package_v2_channel_group"
description: "`aws_media_package_v2_channel_group` creates a real top-level MediaPackage v2 resource that one or more `aws_media_package_v2_channel`s belong to..."
---

`aws_media_package_v2_channel_group` creates a real top-level MediaPackage v2 resource that one or more `aws_media_package_v2_channel`s belong to, providing a single, fixed egress domain shared across every channel in the group regardless of individual channel failures or upgrades.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own package v2 channel group"})

    		ubx.Resource(media.PackageV2ChannelGroup, "ci-runner", media.PackageV2ChannelGroupConfig{
    			ChannelGroupName: "example-channel-group-name",
    			Description:      "Managed by ubx.",
    			Tags:             []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PackageV2ChannelGroup } from "./local-sdk/aws/sdk/typescript/aws/media/package_v2_channel_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own package v2 channel group" });

      resource(PackageV2ChannelGroup, "ci-runner", {
        channelGroupName: "example-channel-group-name",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import PackageV2ChannelGroup, PackageV2ChannelGroupConfig

    def describe():
        ubx.intent("payments own package v2 channel group")

        ubx.resource(PackageV2ChannelGroup, "ci-runner", PackageV2ChannelGroupConfig(
            channel_group_name="example-channel-group-name",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_package_v2_channel_group, with channel group
    name example-channel-group-name, description Managed by ubx.,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="channel_group_name" type="string" required>
  The unique name for the channel group within the AWS Region, used to identify and reference this group of channels in AWS Elemental MediaPackage v2. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  &lt;p&gt;Enter any descriptive text that helps you to identify the channel group.&lt;/p&gt; Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tag objects (each containing a key and value) to attach to the channel group for organizing and identifying AWS resources. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag attached to the MediaPackage v2 channel group for metadata and resource management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag attached to the MediaPackage v2 channel group, used for metadata and resource categorization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) associated with the resource.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  &lt;p&gt;The date and time the channel group was created.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="egress_domain" type="string">
  &lt;p&gt;The output domain where the source stream should be sent. Integrate the domain with a downstream CDN (such as Amazon CloudFront) or playback device.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="modified_at" type="string">
  &lt;p&gt;The date and time the channel group was modified.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>
