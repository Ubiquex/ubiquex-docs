---
title: "aws_media_live_cloud_watch_alarm_template_group"
description: "`aws_media_live_cloud_watch_alarm_template_group` groups real `aws_media_live_cloud_watch_alarm_template`s together so the group..."
---

`aws_media_live_cloud_watch_alarm_template_group` groups real `aws_media_live_cloud_watch_alarm_template`s together so the group, rather than each template individually, can be attached to a MediaLive signal map or channel in one step.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	media "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/media"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own live cloud watch alarm template group"})

    		ubx.Resource(media.LiveCloudWatchAlarmTemplateGroup, "ci-runner", media.LiveCloudWatchAlarmTemplateGroupConfig{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			Tags:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LiveCloudWatchAlarmTemplateGroup } from "./local-sdk/aws/sdk/typescript/aws/media/live_cloud_watch_alarm_template_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own live cloud watch alarm template group" });

      resource(LiveCloudWatchAlarmTemplateGroup, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.media import LiveCloudWatchAlarmTemplateGroup, LiveCloudWatchAlarmTemplateGroupConfig

    def describe():
        ubx.intent("payments own live cloud watch alarm template group")

        ubx.resource(LiveCloudWatchAlarmTemplateGroup, "ci-runner", LiveCloudWatchAlarmTemplateGroupConfig(
            name="ci-runner",
            description="Managed by ubx.",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_media_live_cloud_watch_alarm_template_group called
    "ci-runner", with description Managed by ubx., tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A resource's optional description. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A resource's name. Names must be unique within the scope of a resource type in a specific region. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  Represents the tags associated with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  A cloudwatch alarm template group's ARN (Amazon Resource Name) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp (ISO 8601) indicating when this CloudWatch alarm template group was created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  A cloudwatch alarm template group's id. AWS provided template groups have ids that start with `aws-` Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="identifier" type="string">
  The computed unique group identifier that AWS MediaLive assigns to the CloudWatch alarm template group, matching the value returned by the CloudFormation Ref intrinsic function for this resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="modified_at" type="string">
  Indicates the most recent modification time of the CloudWatch alarm template group, returned as a string in RFC3339 timestamp format. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
