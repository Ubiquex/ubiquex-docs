---
title: "aws_game_lift_location"
description: "`aws_game_lift_location` registers a real, custom location, a caller's own data center or an unsupported AWS Region..."
---

`aws_game_lift_location` registers a real, custom location, a caller's own data center or an unsupported AWS Region, that an `aws_game_lift_fleet` running in Anywhere mode can actually be deployed to.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own location"})

    		ubx.Resource(gamelift.Location, "example", gamelift.LocationConfig{
    			LocationName: "example-location-name",
    			Tags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Location } from "@ubx/sdk-aws/aws/gamelift/location";

    export default stack("example", () => {
      intent({ summary: "example own location" });

      resource(Location, "example", {
        locationName: "example-location-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.gamelift import Location, LocationConfig
    from ubx.aws.gamelift.location import Location_Tags

    def describe():
        ubx.intent("example own location")

        ubx.resource(Location, "example", LocationConfig(
            location_name="example-location-name",
            tags=[Location_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_game_lift_location, with location name
    example-location-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="location_name" type="string" required>
  Specifies the name of the GameLift location, which is typically an AWS Region code (e.g., 'us-east-1') or a custom location name used to identify the location for game hosting. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the GameLift location, used to organize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="location_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the GameLift location, returned after the location is created and used for referencing it in IAM policies and other resources. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
