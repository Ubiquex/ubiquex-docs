---
title: "aws_gamelift_script"
description: "Real, generated bindings for `aws_gamelift_script`."
---

`aws_gamelift_script` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own script"})

    		ubx.Resource(gamelift.Script, "ci-runner", gamelift.ScriptConfig{
    			Name:    "ci-runner",
    			Tags:    map[string]string{"managed-by": "ubx"},
    			Version: "example",
    			ZipFile: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Script } from "jsr:@ubx/sdk-aws/aws/gamelift/script";

    export default stack("payments", () => {
      intent({ summary: "payments own script" });

      resource(Script, "ci-runner", {
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
        version: "example",
        zipFile: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.gamelift import Script, ScriptConfig

    def describe():
        ubx.intent("payments own script")

        ubx.resource(Script, "ci-runner", ScriptConfig(
            name="ci-runner",
            tags={"managed-by": "ubx"},
            version="example",
            zip_file="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_gamelift_script called "ci-runner", with tags a set
    value, version example, zip file example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Optional.
</ResponseField>

<ResponseField name="zip_file" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
