---
title: "aws_gamelift_build"
description: "Real, generated bindings for `aws_gamelift_build`."
---

`aws_gamelift_build` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own build"})

    		ubx.Resource(gamelift.Build, "ci-runner", gamelift.BuildConfig{
    			Name:            "ci-runner",
    			OperatingSystem: "example",
    			Tags:            map[string]string{"managed-by": "ubx"},
    			Version:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Build } from "jsr:@ubx/sdk-aws/aws/gamelift/build";

    export default stack("payments", () => {
      intent({ summary: "payments own build" });

      resource(Build, "ci-runner", {
        name: "ci-runner",
        operatingSystem: "example",
        tags: { "managed-by": "ubx" },
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.gamelift import Build, BuildConfig

    def describe():
        ubx.intent("payments own build")

        ubx.resource(Build, "ci-runner", BuildConfig(
            name="ci-runner",
            operating_system="example",
            tags={"managed-by": "ubx"},
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_gamelift_build called "ci-runner", with operating
    system example, tags a set value, version example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="operating_system" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
