---
title: "aws_game_lift_matchmaking_rule_set"
description: "`aws_game_lift_matchmaking_rule_set` defines the real, JSON-based rules, team size, skill balancing, latency requirements..."
---

`aws_game_lift_matchmaking_rule_set` defines the real, JSON-based rules, team size, skill balancing, latency requirements, that an `aws_game_lift_matchmaking_configuration` actually evaluates candidate players against to form a match.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own matchmaking rule set"})

    		ubx.Resource(gamelift.MatchmakingRuleSet, "example", gamelift.MatchmakingRuleSetConfig{
    			Name:        "example",
    			RuleSetBody: "example",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MatchmakingRuleSet } from "@ubx/sdk-aws/aws/gamelift/matchmaking_rule_set";

    export default stack("example", () => {
      intent({ summary: "example own matchmaking rule set" });

      resource(MatchmakingRuleSet, "example", {
        name: "example",
        ruleSetBody: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.gamelift import MatchmakingRuleSet, MatchmakingRuleSetConfig
    from ubx.aws.gamelift.matchmaking_rule_set import MatchmakingRuleSet_Tags

    def describe():
        ubx.intent("example own matchmaking rule set")

        ubx.resource(MatchmakingRuleSet, "example", MatchmakingRuleSetConfig(
            name="example",
            rule_set_body="example",
            tags=[MatchmakingRuleSet_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_game_lift_matchmaking_rule_set called "example", with
    rule set body example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  A unique identifier for the matchmaking rule set. Required.
</ResponseField>

<ResponseField name="rule_set_body" type="string" required>
  A collection of matchmaking rules, formatted as a JSON string. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the GameLift matchmaking rule set, used to identify and categorize the rule set for resource management and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag attached to the GameLift matchmaking rule set, used to organize and identify the rule set within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift matchmaking rule set resource and uniquely identifies it. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds. Computed by AWS after `ship`.
</ResponseField>
