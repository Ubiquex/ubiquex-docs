---
title: "aws_event_schemas_registry_policy"
description: "`aws_event_schemas_registry_policy` is a real, resource-based access policy controlling which other AWS accounts can view or use the schemas in..."
---

`aws_event_schemas_registry_policy` is a real, resource-based access policy controlling which other AWS accounts can view or use the schemas in an `aws_event_schemas_registry`, needed for sharing a schema registry across accounts.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventschemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/eventschemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own schemas registry policy"})

    		ubx.Resource(eventschemas.RegistryPolicy, "example", eventschemas.RegistryPolicyConfig{
    			Policy:       "example",
    			RegistryName: "example-registry-name",
    			RevisionId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegistryPolicy } from "@ubx/sdk-aws/aws/eventschemas/registry_policy";

    export default stack("example", () => {
      intent({ summary: "example own schemas registry policy" });

      resource(RegistryPolicy, "example", {
        policy: "example",
        registryName: "example-registry-name",
        revisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eventschemas.registry_policy import RegistryPolicy, RegistryPolicyConfig

    def describe():
        ubx.intent("example own schemas registry policy")

        ubx.resource(RegistryPolicy, "example", RegistryPolicyConfig(
            policy="example",
            registry_name="example-registry-name",
            revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_event_schemas_registry_policy, with policy example,
    registry name example-registry-name, revision id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="policy" type="any" required>
  The JSON resource-based policy document that grants or denies permissions to principals for actions on the EventBridge schema registry and its schemas. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="registry_name" type="string" required>
  The name of the EventBridge Schema Registry to which this resource-based policy is attached, identifying the registry whose access permissions the policy controls. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="revision_id" type="string">
  The revision ID of the registry policy, used for optimistic concurrency control to prevent conflicting updates (you must provide the current revision when updating the policy). Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier of the registry policy, equal to the name of the registry to which the policy is attached. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
