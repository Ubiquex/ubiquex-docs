---
title: "aws_devicefarm_device_pool"
description: "Real, generated bindings for `aws_devicefarm_device_pool`."
---

`aws_devicefarm_device_pool` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own device pool"})

    		ubx.Resource(devicefarm.DevicePool, "ci-runner", devicefarm.DevicePoolConfig{
    			Name:        "ci-runner",
    			ProjectArn:  "arn:aws:iam::123456789012:policy/ci",
    			Description: "Managed by ubx.",
    			MaxDevices:  1,
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DevicePool } from "jsr:@ubx/sdk-aws/aws/devicefarm/device_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own device pool" });

      resource(DevicePool, "ci-runner", {
        name: "ci-runner",
        projectArn: "arn:aws:iam::123456789012:policy/ci",
        description: "Managed by ubx.",
        maxDevices: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devicefarm import DevicePool, DevicePoolConfig

    def describe():
        ubx.intent("payments own device pool")

        ubx.resource(DevicePool, "ci-runner", DevicePoolConfig(
            name="ci-runner",
            project_arn="arn:aws:iam::123456789012:policy/ci",
            description="Managed by ubx.",
            max_devices=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_devicefarm_device_pool called "ci-runner", with
    project arn arn:aws:iam::123456789012:policy/ci, description
    Managed by ubx., max devices 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_devices" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
