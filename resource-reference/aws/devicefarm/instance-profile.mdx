---
title: "aws_devicefarm_instance_profile"
description: "Real, generated bindings for `aws_devicefarm_instance_profile`."
---

`aws_devicefarm_instance_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance profile"})

    		ubx.Resource(devicefarm.InstanceProfile, "ci-runner", devicefarm.InstanceProfileConfig{
    			Name:                          "ci-runner",
    			Description:                   "Managed by ubx.",
    			ExcludeAppPackagesFromCleanup: []string{"example"},
    			PackageCleanup:                true,
    			RebootAfterUse:                true,
    			Tags:                          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceProfile } from "jsr:@ubx/sdk-aws/aws/devicefarm/instance_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own instance profile" });

      resource(InstanceProfile, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        excludeAppPackagesFromCleanup: ["example"],
        packageCleanup: true,
        rebootAfterUse: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devicefarm import InstanceProfile, InstanceProfileConfig

    def describe():
        ubx.intent("payments own instance profile")

        ubx.resource(InstanceProfile, "ci-runner", InstanceProfileConfig(
            name="ci-runner",
            description="Managed by ubx.",
            exclude_app_packages_from_cleanup=["example"],
            package_cleanup=True,
            reboot_after_use=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_devicefarm_instance_profile called "ci-runner", with
    description Managed by ubx., exclude app packages from cleanup a
    set value, package cleanup true, reboot after use true, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="exclude_app_packages_from_cleanup" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="package_cleanup" type="bool">
  Optional.
</ResponseField>

<ResponseField name="reboot_after_use" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
