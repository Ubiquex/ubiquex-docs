---
title: "aws_devicefarm_upload"
description: "Real, generated bindings for `aws_devicefarm_upload`."
---

`aws_devicefarm_upload` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	devicefarm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/devicefarm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own upload"})

    		ubx.Resource(devicefarm.Upload, "ci-runner", devicefarm.UploadConfig{
    			Name:        "ci-runner",
    			ProjectArn:  "arn:aws:iam::123456789012:policy/ci",
    			Type:        "example",
    			ContentType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Upload } from "jsr:@ubx/sdk-aws/aws/devicefarm/upload";

    export default stack("payments", () => {
      intent({ summary: "payments own upload" });

      resource(Upload, "ci-runner", {
        name: "ci-runner",
        projectArn: "arn:aws:iam::123456789012:policy/ci",
        type: "example",
        contentType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.devicefarm import Upload, UploadConfig

    def describe():
        ubx.intent("payments own upload")

        ubx.resource(Upload, "ci-runner", UploadConfig(
            name="ci-runner",
            project_arn="arn:aws:iam::123456789012:policy/ci",
            type="example",
            content_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_devicefarm_upload called "ci-runner", with project arn
    arn:aws:iam::123456789012:policy/ci, type example, content type
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="content_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="category" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metadata" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
