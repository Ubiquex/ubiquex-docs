---
title: "aws_devicefarm_project"
description: "Real, generated bindings for `aws_devicefarm_project`."
---

`aws_devicefarm_project` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/devicefarm"

    cfg := devicefarm.ProjectConfig{
        Name: "example-name",
        DefaultJobTimeoutMinutes: 1,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(devicefarm.Project, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Project } from
      "jsr:@ubx/sdk-aws/aws/devicefarm/project";

    resource(Project, "example", {
      name: "example",
      defaultJobTimeoutMinutes: 1,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.devicefarm import Project, ProjectConfig

    cfg = ProjectConfig(
        name="example",
        default_job_timeout_minutes=1,
        tags={"key": "value"},
    )
    ubx.resource(Project, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_devicefarm_project with name,
default_job_timeout_minutes, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_job_timeout_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
