---
title: "aws_well_architected_lens"
description: "`aws_well_architected_lens` defines a real, custom set of best-practice questions, beyond the standard Well-Architected Framework pillars..."
---

`aws_well_architected_lens` defines a real, custom set of best-practice questions, beyond the standard Well-Architected Framework pillars, that can be applied to an `aws_well_architected_workload`'s review, for a specific industry or technology an organization wants to assess against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	well "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/well"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own architected lens"})

    		ubx.Resource(well.ArchitectedLens, "example", well.ArchitectedLensConfig{
    			Jsonstring:  "example",
    			LensVersion: "example",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ArchitectedLens } from "@ubx/sdk-aws/aws/well/architected_lens";

    export default stack("example", () => {
      intent({ summary: "example own architected lens" });

      resource(ArchitectedLens, "example", {
        jsonstring: "example",
        lensVersion: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.well import ArchitectedLens, ArchitectedLensConfig

    def describe():
        ubx.intent("example own architected lens")

        ubx.resource(ArchitectedLens, "example", ArchitectedLensConfig(
            jsonstring="example",
            lens_version="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_well_architected_lens, with jsonstring example, lens
    version example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="jsonstring" type="string">
  The JSON representation of a lens. Optional.
</ResponseField>

<ResponseField name="lens_version" type="string">
  The version of the lens. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags assigned to the lens. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="description" type="string">
  The description of the lens. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="lens_arn" type="string">
  The ARN of the lens. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="lens_id" type="string">
  The unique identifier of the lens. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The full name of the lens. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner" type="string">
  The Amazon Web Services account ID that owns the lens. Computed by AWS after `ship`.
</ResponseField>
