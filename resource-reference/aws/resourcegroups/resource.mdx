---
title: "aws_resourcegroups_resource"
description: "Real, generated bindings for `aws_resourcegroups_resource`."
---

`aws_resourcegroups_resource` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	resourcegroups "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resourcegroups"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own resource"})

    		ubx.Resource(resourcegroups.Resource, "ci-runner", resourcegroups.ResourceConfig{
    			GroupArn:    "arn:aws:iam::123456789012:policy/ci",
    			ResourceArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Resource } from "jsr:@ubx/sdk-aws/aws/resourcegroups/resource";

    export default stack("payments", () => {
      intent({ summary: "payments own resource" });

      resource(Resource, "ci-runner", {
        groupArn: "arn:aws:iam::123456789012:policy/ci",
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.resourcegroups import Resource, ResourceConfig

    def describe():
        ubx.intent("payments own resource")

        ubx.resource(Resource, "ci-runner", ResourceConfig(
            group_arn="arn:aws:iam::123456789012:policy/ci",
            resource_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_resourcegroups_resource, with group arn
    arn:aws:iam::123456789012:policy/ci, resource arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
