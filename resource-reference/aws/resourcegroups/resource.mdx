---
title: "aws_resourcegroups_resource"
description: "Real, generated bindings for `aws_resourcegroups_resource`."
---

`aws_resourcegroups_resource` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resourcegroups"

    cfg := resourcegroups.ResourceConfig{
        GroupArn: "example",
        ResourceArn: "example",
    }
    ubx.Resource(resourcegroups.Resource, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Resource } from
      "jsr:@ubx/sdk-aws/aws/resourcegroups/resource";

    resource(Resource, "example", {
      groupArn: "example",
      resourceArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.resourcegroups import Resource, ResourceConfig

    cfg = ResourceConfig(
        group_arn="example",
        resource_arn="example",
    )
    ubx.resource(Resource, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_resourcegroups_resource with group_arn,
resource_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>
