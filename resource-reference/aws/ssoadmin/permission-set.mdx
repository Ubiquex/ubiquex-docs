---
title: "aws_ssoadmin_permission_set"
description: "Real, generated bindings for `aws_ssoadmin_permission_set`."
---

`aws_ssoadmin_permission_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssoadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssoadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own permission set"})

    		ubx.Resource(ssoadmin.PermissionSet, "ci-runner", ssoadmin.PermissionSetConfig{
    			InstanceArn:     "arn:aws:iam::123456789012:policy/ci",
    			Name:            "ci-runner",
    			Description:     "Managed by ubx.",
    			RelayState:      "example",
    			SessionDuration: "example",
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PermissionSet } from "jsr:@ubx/sdk-aws/aws/ssoadmin/permission_set";

    export default stack("payments", () => {
      intent({ summary: "payments own permission set" });

      resource(PermissionSet, "ci-runner", {
        instanceArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        description: "Managed by ubx.",
        relayState: "example",
        sessionDuration: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssoadmin import PermissionSet, PermissionSetConfig

    def describe():
        ubx.intent("payments own permission set")

        ubx.resource(PermissionSet, "ci-runner", PermissionSetConfig(
            instance_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            description="Managed by ubx.",
            relay_state="example",
            session_duration="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssoadmin_permission_set called "ci-runner", with
    instance arn arn:aws:iam::123456789012:policy/ci, description
    Managed by ubx., relay state example, session duration example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="relay_state" type="string">
  Optional.
</ResponseField>

<ResponseField name="session_duration" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
