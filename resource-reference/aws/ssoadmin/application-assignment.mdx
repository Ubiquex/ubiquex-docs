---
title: "aws_ssoadmin_application_assignment"
description: "Real, generated bindings for `aws_ssoadmin_application_assignment`."
---

`aws_ssoadmin_application_assignment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssoadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssoadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application assignment"})

    		ubx.Resource(ssoadmin.ApplicationAssignment, "ci-runner", ssoadmin.ApplicationAssignmentConfig{
    			ApplicationArn: "arn:aws:iam::123456789012:policy/ci",
    			PrincipalId:    "example",
    			PrincipalType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationAssignment } from "jsr:@ubx/sdk-aws/aws/ssoadmin/application_assignment";

    export default stack("payments", () => {
      intent({ summary: "payments own application assignment" });

      resource(ApplicationAssignment, "ci-runner", {
        applicationArn: "arn:aws:iam::123456789012:policy/ci",
        principalId: "example",
        principalType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssoadmin import ApplicationAssignment, ApplicationAssignmentConfig

    def describe():
        ubx.intent("payments own application assignment")

        ubx.resource(ApplicationAssignment, "ci-runner", ApplicationAssignmentConfig(
            application_arn="arn:aws:iam::123456789012:policy/ci",
            principal_id="example",
            principal_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssoadmin_application_assignment, with application arn
    arn:aws:iam::123456789012:policy/ci, principal id example,
    principal type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
