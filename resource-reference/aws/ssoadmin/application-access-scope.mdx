---
title: "aws_ssoadmin_application_access_scope"
description: "Real, generated bindings for `aws_ssoadmin_application_access_scope`."
---

`aws_ssoadmin_application_access_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssoadmin"

    cfg := ssoadmin.ApplicationAccessScopeConfig{
        ApplicationArn: "example",
        Scope: "example",
        AuthorizedTargets: []string{"example"},
    }
    ubx.Resource(ssoadmin.ApplicationAccessScope, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ApplicationAccessScope } from
      "jsr:@ubx/sdk-aws/aws/ssoadmin/application_access_scope";

    resource(ApplicationAccessScope, "example", {
      applicationArn: "example",
      scope: "example",
      authorizedTargets: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssoadmin import (
        ApplicationAccessScope,
        ApplicationAccessScopeConfig,
    )

    cfg = ApplicationAccessScopeConfig(
        application_arn="example",
        scope="example",
        authorized_targets=["example"],
    )
    ubx.resource(ApplicationAccessScope, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssoadmin_application_access_scope with
application_arn, scope, authorized_targets.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authorized_targets" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
