---
title: "aws_ssoadmin_application_access_scope"
description: "Real, generated bindings for `aws_ssoadmin_application_access_scope`."
---

`aws_ssoadmin_application_access_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssoadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssoadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application access scope"})

    		ubx.Resource(ssoadmin.ApplicationAccessScope, "ci-runner", ssoadmin.ApplicationAccessScopeConfig{
    			ApplicationArn:    "arn:aws:iam::123456789012:policy/ci",
    			Scope:             "example",
    			AuthorizedTargets: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationAccessScope } from "jsr:@ubx/sdk-aws/aws/ssoadmin/application_access_scope";

    export default stack("payments", () => {
      intent({ summary: "payments own application access scope" });

      resource(ApplicationAccessScope, "ci-runner", {
        applicationArn: "arn:aws:iam::123456789012:policy/ci",
        scope: "example",
        authorizedTargets: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssoadmin import ApplicationAccessScope, ApplicationAccessScopeConfig

    def describe():
        ubx.intent("payments own application access scope")

        ubx.resource(ApplicationAccessScope, "ci-runner", ApplicationAccessScopeConfig(
            application_arn="arn:aws:iam::123456789012:policy/ci",
            scope="example",
            authorized_targets=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssoadmin_application_access_scope, with application
    arn arn:aws:iam::123456789012:policy/ci, scope example,
    authorized targets a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authorized_targets" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
