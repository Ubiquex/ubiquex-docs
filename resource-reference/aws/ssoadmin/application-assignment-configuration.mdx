---
title: "aws_ssoadmin_application_assignment_configuration"
description: "Real, generated bindings for `aws_ssoadmin_application_assignment_configuration`."
---

`aws_ssoadmin_application_assignment_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssoadmin "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssoadmin"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application assignment configuration"})

    		ubx.Resource(ssoadmin.ApplicationAssignmentConfiguration, "ci-runner", ssoadmin.ApplicationAssignmentConfigurationConfig{
    			ApplicationArn:     "arn:aws:iam::123456789012:policy/ci",
    			AssignmentRequired: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationAssignmentConfiguration } from "jsr:@ubx/sdk-aws/aws/ssoadmin/application_assignment_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own application assignment configuration" });

      resource(ApplicationAssignmentConfiguration, "ci-runner", {
        applicationArn: "arn:aws:iam::123456789012:policy/ci",
        assignmentRequired: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssoadmin import ApplicationAssignmentConfiguration, ApplicationAssignmentConfigurationConfig

    def describe():
        ubx.intent("payments own application assignment configuration")

        ubx.resource(ApplicationAssignmentConfiguration, "ci-runner", ApplicationAssignmentConfigurationConfig(
            application_arn="arn:aws:iam::123456789012:policy/ci",
            assignment_required=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssoadmin_application_assignment_configuration, with
    application arn arn:aws:iam::123456789012:policy/ci, assignment
    required true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="assignment_required" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
