---
title: "aws_ssoadmin_region"
description: "Real, generated bindings for `aws_ssoadmin_region`."
---

`aws_ssoadmin_region` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssoadmin"

    cfg := ssoadmin.RegionConfig{
        InstanceArn: "example",
        RegionName: "example-region-name",
    }
    ubx.Resource(ssoadmin.Region, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Region } from "jsr:@ubx/sdk-aws/aws/ssoadmin/region";

    resource(Region, "example", {
      instanceArn: "example",
      regionName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.ssoadmin import Region, RegionConfig

    cfg = RegionConfig(
        instance_arn="example",
        region_name="example",
    )
    ubx.resource(Region, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_ssoadmin_region with instance_arn, region_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="instance_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
