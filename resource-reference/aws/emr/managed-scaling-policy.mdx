---
title: "aws_emr_managed_scaling_policy"
description: "Real, generated bindings for `aws_emr_managed_scaling_policy`."
---

`aws_emr_managed_scaling_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own managed scaling policy"})

    		ubx.Resource(emr.ManagedScalingPolicy, "ci-runner", emr.ManagedScalingPolicyConfig{
    			ClusterId:                   "example",
    			ScalingStrategy:             "example",
    			UtilizationPerformanceIndex: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagedScalingPolicy } from "jsr:@ubx/sdk-aws/aws/emr/managed_scaling_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own managed scaling policy" });

      resource(ManagedScalingPolicy, "ci-runner", {
        clusterId: "example",
        scalingStrategy: "example",
        utilizationPerformanceIndex: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.emr import ManagedScalingPolicy, ManagedScalingPolicyConfig

    def describe():
        ubx.intent("payments own managed scaling policy")

        ubx.resource(ManagedScalingPolicy, "ci-runner", ManagedScalingPolicyConfig(
            cluster_id="example",
            scaling_strategy="example",
            utilization_performance_index=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_managed_scaling_policy, with cluster id example,
    scaling strategy example, utilization performance index 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scaling_strategy" type="string">
  Optional.
</ResponseField>

<ResponseField name="utilization_performance_index" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
