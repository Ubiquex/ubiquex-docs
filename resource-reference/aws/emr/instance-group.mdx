---
title: "aws_emr_instance_group"
description: "Real, generated bindings for `aws_emr_instance_group`."
---

`aws_emr_instance_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance group"})

    		ubx.Resource(emr.InstanceGroup, "ci-runner", emr.InstanceGroupConfig{
    			ClusterId:          "example",
    			InstanceType:       "example",
    			Name:               "ci-runner",
    			AutoscalingPolicy:  "example",
    			BidPrice:           "example",
    			ConfigurationsJson: "example",
    			EbsOptimized:       true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceGroup } from "jsr:@ubx/sdk-aws/aws/emr/instance_group";

    export default stack("payments", () => {
      intent({ summary: "payments own instance group" });

      resource(InstanceGroup, "ci-runner", {
        clusterId: "example",
        instanceType: "example",
        name: "ci-runner",
        autoscalingPolicy: "example",
        bidPrice: "example",
        configurationsJson: "example",
        ebsOptimized: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.emr import InstanceGroup, InstanceGroupConfig

    def describe():
        ubx.intent("payments own instance group")

        ubx.resource(InstanceGroup, "ci-runner", InstanceGroupConfig(
            cluster_id="example",
            instance_type="example",
            name="ci-runner",
            autoscaling_policy="example",
            bid_price="example",
            configurations_json="example",
            ebs_optimized=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_instance_group called "ci-runner", with cluster id
    example, instance type example, autoscaling policy example, bid
    price example, configurations json example, ebs optimized true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autoscaling_policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="bid_price" type="string">
  Optional.
</ResponseField>

<ResponseField name="cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configurations_json" type="string">
  Optional.
</ResponseField>

<ResponseField name="ebs_config" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="iops" type="number">
    </ResponseField>
    <ResponseField name="size" type="number" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
    <ResponseField name="volumes_per_instance" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ebs_optimized" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_count" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="ebs_config" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="iops" type="number">
    </ResponseField>
    <ResponseField name="size" type="number" required>
    </ResponseField>
    <ResponseField name="type" type="string" required>
    </ResponseField>
    <ResponseField name="volumes_per_instance" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_count" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="running_instance_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
