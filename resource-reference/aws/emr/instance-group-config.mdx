---
title: "aws_emr_instance_group_config"
description: "`aws_emr_instance_group_config` defines one real group of uniformly configured EC2 instances, master, core, or task, inside an `aws_emr_cluster`..."
---

`aws_emr_instance_group_config` defines one real group of uniformly configured EC2 instances, master, core, or task, inside an `aws_emr_cluster`, EMR's original instance-provisioning model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance group config"})

    		ubx.Resource(emr.InstanceGroupConfig, "ci-runner", emr.InstanceGroupConfigConfig{
    			InstanceCount:     1,
    			InstanceRole:      "example",
    			InstanceType:      "example",
    			JobFlowId:         "example",
    			Name:              "ci-runner",
    			AutoScalingPolicy: map[string]any{"constraints": map[string]any{"max_capacity": 1}},
    			BidPrice:          "example",
    			Configurations:    []map[string]any{{"classification": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceGroupConfig } from "./local-sdk/aws/sdk/typescript/aws/emr/instance_group_config.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own instance group config" });

      resource(InstanceGroupConfig, "ci-runner", {
        instanceCount: 1,
        instanceRole: "example",
        instanceType: "example",
        jobFlowId: "example",
        name: "ci-runner",
        autoScalingPolicy: { constraints: { maxCapacity: 1 } },
        bidPrice: "example",
        configurations: [{ classification: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.emr import InstanceGroupConfig, InstanceGroupConfigConfig

    def describe():
        ubx.intent("payments own instance group config")

        ubx.resource(InstanceGroupConfig, "ci-runner", InstanceGroupConfigConfig(
            instance_count=1,
            instance_role="example",
            instance_type="example",
            job_flow_id="example",
            name="ci-runner",
            auto_scaling_policy={"constraints": {"max_capacity": 1}},
            bid_price="example",
            configurations=[{"classification": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_instance_group_config called "ci-runner", with
    instance count 1, instance role example, instance type example,
    job flow id example, auto scaling policy a set value, bid price
    example, configurations a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_scaling_policy" type="object">
  The auto scaling policy for the EMR instance group, defining constraints on instance counts and CloudWatch metric-based scale-in/scale-out rules. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="constraints" type="object" required>
      Specifies the lower and upper bounds (minimum and maximum capacity) for the number of instances in the EMR instance group, which the auto scaling policy uses to determine scaling limits. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_capacity" type="number" required>
          For the EMR instance group's auto scaling policy, this required number defines the maximum allowable number of EC2 instances that the instance group can scale up to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_capacity" type="number" required>
          The minimum number of EC2 instances that the auto scaling policy for the EMR instance group will maintain, acting as the lower bound in the scaling constraints. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rules" type="list(object)" required>
      Defines the list of scaling rules (each containing a name, a CloudWatch metric-based condition, and a scaling action) that the auto scaling policy evaluates to automatically add or remove instances in the EMR instance group. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="action" type="object">
          Specifies the scaling action to take when this auto scaling rule is triggered, defining how many instances to add or remove (and optionally the market) for the instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="market" type="string">
              Specifies the market type (ON_DEMAND or SPOT) of the instances that the auto scaling rule's action adds or removes in the EMR instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="simple_scaling_policy_configuration" type="object">
              Specifies the parameters for a simple scaling action, including the scaling adjustment type (such as CHANGE_IN_CAPACITY), the amount to scale, and an optional cool-down period, used by an EMR instance group auto-scaling rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="adjustment_type" type="string">
                  Specifies how the scaling adjustment is interpreted for the simple scaling policy on an EMR instance group, such as an absolute change in instance count (CHANGE_IN_CAPACITY), a percentage change (PERCENT_CHANGE_IN_CAPACITY), or an exact target capacity (EXACT_CAPACITY). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="cool_down" type="number">
                  The amount of time, in seconds, after a scaling activity completes before any further trigger-related scaling activities can start. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="scaling_adjustment" type="number">
                  The amount by which to scale the instance group when this policy is triggered, interpreted according to the adjustment type (such as an absolute change in instance count, a percentage change, or an exact target capacity). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          The description of the scaling rule, used to document the purpose of the rule within the auto scaling policy for the EMR instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the auto scaling rule within the EMR instance group's auto scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="trigger" type="object">
          Specifies the CloudWatch alarm definition (including metric, comparison operator, threshold, and evaluation periods) that determines when the scaling rule's action is triggered. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cloud_watch_alarm_definition" type="object">
              Defines the CloudWatch alarm that triggers the scaling rule, specifying metric name, namespace, comparison operator, threshold, evaluation periods, period, statistic, unit, and dimensions. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="comparison_operator" type="string">
                  Specifies the arithmetic operation to use when comparing the specified statistic and threshold, such as GreaterThanOrEqualToThreshold or LessThanThreshold, for the CloudWatch alarm that triggers the scaling rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="dimensions" type="list(object)">
                  Specifies the CloudWatch metric dimensions (name/value pairs) that are applied to the alarm metric for this auto scaling rule's trigger. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="key" type="string">
                      Specifies the name (key) of a CloudWatch metric dimension included in the alarm definition that triggers the Amazon EMR instance group auto scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      The value assigned to the CloudWatch alarm dimension (e.g., an EMR instance group ID or cluster ID) that identifies the metric scope for the auto-scaling trigger, such as 'InstanceGroupId' or 'JobFlowId'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="evaluation_periods" type="number">
                  Specifies the number of consecutive evaluation periods during which the monitored metric must breach the specified threshold before the CloudWatch alarm triggers the auto scaling rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="metric_name" type="string">
                  The name of the CloudWatch metric (e.g., YARNMemoryAvailablePercentage or a custom metric) that the alarm definition monitors to trigger this auto scaling rule for the EMR instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="namespace" type="string">
                  The CloudWatch namespace (e.g., AWS/EMR) that contains the metric being monitored for the auto scaling policy's CloudWatch alarm trigger. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="period" type="number">
                  The period is the number of seconds between evaluation periods for the CloudWatch alarm that triggers this scaling rule, determining how often the metric is evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="statistic" type="string">
                  Specifies the statistic (such as Average, Sum, SampleCount, Minimum, Maximum, or a percentile) to apply when evaluating the CloudWatch metric for the auto scaling alarm. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="threshold" type="number">
                  The numeric value that the CloudWatch metric is compared against (using the comparison operator) to determine when the alarm triggers for the EMR instance group's auto scaling policy. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unit" type="string">
                  The unit of measure for the CloudWatch metric (e.g., Percent, Count, Bytes) that the alarm definition uses to evaluate the auto scaling trigger. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bid_price" type="string">
  Sets the maximum hourly price you are willing to pay for Spot Instances in the EMR instance group; if specified, the group uses Spot Instances with this bid price, otherwise it uses On-Demand instances. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="configurations" type="list(object)">
  Specifies a list of EMR software configurations (classifications and properties) that override the cluster-level configurations for this instance group. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="classification" type="string">
      Specifies the name of the Hadoop configuration group (such as 'core-site' or 'hdfs-site') that this configuration object applies to within the EMR instance group's configuration settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="configuration_properties" type="any">
      Specifies a key-value map of properties for the configuration classification (e.g., Hadoop settings) applied to the EMR instance group's applications. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="configurations" type="list(any)">
      The list of nested configuration objects under a configuration, allowing hierarchical application of multiple classifications and their properties to the instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_ami_id" type="string">
  Specifies a custom Amazon Machine Image (AMI) ID to use for the instances in this EMR instance group, overriding the default EMR AMI. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ebs_configuration" type="object">
  The EbsConfiguration property specifies the Elastic Block Store (EBS) volumes to attach to each instance in the Amazon EMR instance group, including whether to use EBS optimization. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ebs_block_device_configs" type="list(object)">
      Specifies the list of EBS block device configurations, each defining a volume specification and the number of volumes per instance, to attach to each instance in the EMR instance group. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="volume_specification" type="object">
          Defines the EBS volume configuration, including volume type, size in GB, and optional IOPS, for each EBS block device attached to the instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="iops" type="number">
              The IOPS (input/output operations per second) rate for the EBS volume, applicable when the volume type is io1, io2, or gp3. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="size_in_gb" type="number">
              Specifies the size, in GiB, of each EBS volume defined in the volume specification for the EMR instance group's EBS block device configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="throughput" type="number">
              Specifies the throughput in MiB/s for a gp3 EBS volume attached to an EMR instance group, used when the volume type is gp3 and VolumeSpecification throughput is provided. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="volume_type" type="string">
              Specifies the EBS volume type (e.g., gp2, gp3, io1, or standard) for each block device in the EMR instance group's EBS configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="volumes_per_instance" type="number">
          The number of EBS volumes of the specified volume specification to attach to each instance in the instance group. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ebs_optimized" type="bool">
      Indicates whether the EC2 instances in this EMR instance group are launched as EBS-optimized, providing dedicated network and disk I/O for Amazon EBS volumes. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_count" type="number" required>
  The number of EC2 instances to launch in this EMR instance group. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="instance_role" type="string" required>
  Defines the role of this instance group in the EMR cluster, which must be set to MASTER for the primary node group, or CORE or TASK for additional node groups. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  Specifies the EC2 instance type (e.g., m5.xlarge) that Amazon EMR will launch for each instance in this instance group, determining the compute and memory resources available to the group. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="job_flow_id" type="string" required>
  The ID of the EMR cluster (job flow) to which this instance group belongs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="market" type="string">
  Specifies whether the instance group uses On-Demand or Spot instances, with allowed values ON_DEMAND or SPOT. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  Specifies a user-defined name for the EMR instance group, which is used to identify the group within the cluster. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The `id` is the AWS-assigned unique identifier for the EMR instance group, used to reference the instance group within the EMR cluster. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
