---
title: "aws_emr_walworkspace"
description: "`aws_emr_walworkspace` is a real, durable, managed storage layer, outliving any single `aws_emr_cluster`..."
---

`aws_emr_walworkspace` is a real, durable, managed storage layer, outliving any single `aws_emr_cluster`, that Apache HBase write-ahead logs get written into, so a caller can point a brand-new cluster at the same S3 root directory and WAL workspace after a cluster or Availability Zone failure and recover unflushed HBase data within minutes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own walworkspace"})

    		ubx.Resource(emr.Walworkspace, "ci-runner", emr.WalworkspaceConfig{
    			Tags:             []map[string]any{{"key": "example"}},
    			WalworkspaceName: "example-walworkspace-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Walworkspace } from "./local-sdk/aws/sdk/typescript/aws/emr/walworkspace.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own walworkspace" });

      resource(Walworkspace, "ci-runner", {
        tags: [{ key: "example" }],
        walworkspaceName: "example-walworkspace-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.emr import Walworkspace, WalworkspaceConfig

    def describe():
        ubx.intent("payments own walworkspace")

        ubx.resource(Walworkspace, "ci-runner", WalworkspaceConfig(
            tags=[{"key": "example"}],
            walworkspace_name="example-walworkspace-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_walworkspace, with tags a set value, walworkspace
    name example-walworkspace-name.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of a user-defined tag attached to the EMR WAL workspace, used for resource categorization, cost tracking, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag attached to the EMR WAL workspace, used to store arbitrary metadata for resource identification, organization, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="walworkspace_name" type="string">
  The name of the emrwal container Optional.
</ResponseField>
