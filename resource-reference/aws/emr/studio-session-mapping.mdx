---
title: "aws_emr_studio_session_mapping"
description: "`aws_emr_studio_session_mapping` maps a real IAM Identity Center user or group to an `aws_emr_studio`..."
---

`aws_emr_studio_session_mapping` maps a real IAM Identity Center user or group to an `aws_emr_studio`, with an attached session policy refining exactly what that user or group can do inside the Studio.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own studio session mapping"})

    		ubx.Resource(emr.StudioSessionMapping, "ci-runner", emr.StudioSessionMappingConfig{
    			IdentityName:     "example-identity-name",
    			IdentityType:     "example",
    			SessionPolicyArn: "arn:aws:iam::123456789012:policy/ci",
    			StudioId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { StudioSessionMapping } from "./local-sdk/aws/sdk/typescript/aws/emr/studio_session_mapping.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own studio session mapping" });

      resource(StudioSessionMapping, "ci-runner", {
        identityName: "example-identity-name",
        identityType: "example",
        sessionPolicyArn: "arn:aws:iam::123456789012:policy/ci",
        studioId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.emr import StudioSessionMapping, StudioSessionMappingConfig

    def describe():
        ubx.intent("payments own studio session mapping")

        ubx.resource(StudioSessionMapping, "ci-runner", StudioSessionMappingConfig(
            identity_name="example-identity-name",
            identity_type="example",
            session_policy_arn="arn:aws:iam::123456789012:policy/ci",
            studio_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_studio_session_mapping, with identity name
    example-identity-name, identity type example, session policy arn
    arn:aws:iam::123456789012:policy/ci, studio id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="identity_name" type="string" required>
  The name of the user or group. For more information, see UserName and DisplayName in the AWS SSO Identity Store API Reference. Either IdentityName or IdentityId must be specified. Required.
</ResponseField>

<ResponseField name="identity_type" type="string" required>
  Specifies whether the identity to map to the Studio is a user or a group. Required.
</ResponseField>

<ResponseField name="session_policy_arn" type="string" required>
  The ARN of the IAM policy that defines the permissions for the EMR Studio session assigned to the mapped user or group. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="studio_id" type="string" required>
  The ID of the Amazon EMR Studio to which the user or group will be mapped. Required.
</ResponseField>
