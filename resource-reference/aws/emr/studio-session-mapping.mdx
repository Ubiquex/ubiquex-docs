---
title: "aws_emr_studio_session_mapping"
description: "Real, generated bindings for `aws_emr_studio_session_mapping`."
---

`aws_emr_studio_session_mapping` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own studio session mapping"})

    		ubx.Resource(emr.StudioSessionMapping, "ci-runner", emr.StudioSessionMappingConfig{
    			IdentityType:     "example",
    			SessionPolicyArn: "arn:aws:iam::123456789012:policy/ci",
    			StudioId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StudioSessionMapping } from "jsr:@ubx/sdk-aws/aws/emr/studio_session_mapping";

    export default stack("payments", () => {
      intent({ summary: "payments own studio session mapping" });

      resource(StudioSessionMapping, "ci-runner", {
        identityType: "example",
        sessionPolicyArn: "arn:aws:iam::123456789012:policy/ci",
        studioId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.emr import StudioSessionMapping, StudioSessionMappingConfig

    def describe():
        ubx.intent("payments own studio session mapping")

        ubx.resource(StudioSessionMapping, "ci-runner", StudioSessionMappingConfig(
            identity_type="example",
            session_policy_arn="arn:aws:iam::123456789012:policy/ci",
            studio_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_studio_session_mapping, with identity type
    example, session policy arn arn:aws:iam::123456789012:policy/ci,
    studio id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_policy_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="studio_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
