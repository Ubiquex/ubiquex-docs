---
title: "aws_emr_studio_session_mapping"
description: "Real, generated bindings for `aws_emr_studio_session_mapping`."
---

`aws_emr_studio_session_mapping` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"

    cfg := emr.StudioSessionMappingConfig{
        IdentityType: "example",
        SessionPolicyArn: "example",
        StudioId: "example",
        IdentityId: "example",
        IdentityName: "example-identity-name",
    }
    ubx.Resource(emr.StudioSessionMapping, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { StudioSessionMapping } from
      "jsr:@ubx/sdk-aws/aws/emr/studio_session_mapping";

    resource(StudioSessionMapping, "example", {
      identityType: "example",
      sessionPolicyArn: "example",
      studioId: "example",
      identityId: "example",
      identityName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.emr import (
        StudioSessionMapping,
        StudioSessionMappingConfig,
    )

    cfg = StudioSessionMappingConfig(
        identity_type="example",
        session_policy_arn="example",
        studio_id="example",
        identity_id="example",
        identity_name="example",
    )
    ubx.resource(StudioSessionMapping, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_emr_studio_session_mapping with identity_type,
session_policy_arn, studio_id, identity_id, identity_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_policy_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="studio_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
