---
title: "aws_emr_instance_fleet"
description: "Real, generated bindings for `aws_emr_instance_fleet`."
---

`aws_emr_instance_fleet` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance fleet"})

    		ubx.Resource(emr.InstanceFleet, "ci-runner", emr.InstanceFleetConfig{
    			ClusterId:              "example",
    			Name:                   "ci-runner",
    			TargetOnDemandCapacity: 1,
    			TargetSpotCapacity:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceFleet } from "jsr:@ubx/sdk-aws/aws/emr/instance_fleet";

    export default stack("payments", () => {
      intent({ summary: "payments own instance fleet" });

      resource(InstanceFleet, "ci-runner", {
        clusterId: "example",
        name: "ci-runner",
        targetOnDemandCapacity: 1,
        targetSpotCapacity: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.emr import InstanceFleet, InstanceFleetConfig

    def describe():
        ubx.intent("payments own instance fleet")

        ubx.resource(InstanceFleet, "ci-runner", InstanceFleetConfig(
            cluster_id="example",
            name="ci-runner",
            target_on_demand_capacity=1,
            target_spot_capacity=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_instance_fleet called "ci-runner", with cluster id
    example, target on demand capacity 1, target spot capacity 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_on_demand_capacity" type="number">
  Optional.
</ResponseField>

<ResponseField name="target_spot_capacity" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="provisioned_on_demand_capacity" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioned_spot_capacity" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
