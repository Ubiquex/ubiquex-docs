---
title: "aws_emr_studio"
description: "Real, generated bindings for `aws_emr_studio`."
---

`aws_emr_studio` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	emr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/emr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own studio"})

    		ubx.Resource(emr.Studio, "ci-runner", emr.StudioConfig{
    			AuthMode:                 "example",
    			DefaultS3Location:        "example",
    			EngineSecurityGroupId:    "example",
    			Name:                     "ci-runner",
    			ServiceRole:              "example",
    			SubnetIds:                []string{"example"},
    			VpcId:                    "example",
    			WorkspaceSecurityGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Studio } from "jsr:@ubx/sdk-aws/aws/emr/studio";

    export default stack("payments", () => {
      intent({ summary: "payments own studio" });

      resource(Studio, "ci-runner", {
        authMode: "example",
        defaultS3Location: "example",
        engineSecurityGroupId: "example",
        name: "ci-runner",
        serviceRole: "example",
        subnetIds: ["example"],
        vpcId: "example",
        workspaceSecurityGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.emr import Studio, StudioConfig

    def describe():
        ubx.intent("payments own studio")

        ubx.resource(Studio, "ci-runner", StudioConfig(
            auth_mode="example",
            default_s3_location="example",
            engine_security_group_id="example",
            name="ci-runner",
            service_role="example",
            subnet_ids=["example"],
            vpc_id="example",
            workspace_security_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_emr_studio called "ci-runner", with auth mode example,
    default s3 location example, engine security group id example,
    service role example, subnet ids a set value, vpc id example,
    workspace security group id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auth_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="default_s3_location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="encryption_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="engine_security_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="idp_auth_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="idp_relay_state_parameter_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_role" type="string">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workspace_security_group_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
