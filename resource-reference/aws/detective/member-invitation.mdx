---
title: "aws_detective_member_invitation"
description: "`aws_detective_member_invitation` invites a real member account to join an `aws_detective_graph`..."
---

`aws_detective_member_invitation` invites a real member account to join an `aws_detective_graph`, the resource that actually sends the invitation an account must accept before its own findings and telemetry are included in the graph's behavior analysis.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own member invitation"})

    		ubx.Resource(detective.MemberInvitation, "ci-runner", detective.MemberInvitationConfig{
    			GraphArn:                 "arn:aws:iam::123456789012:policy/ci",
    			MemberEmailAddress:       "example",
    			MemberId:                 "example",
    			DisableEmailNotification: true,
    			Message:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MemberInvitation } from "./local-sdk/aws/sdk/typescript/aws/detective/member_invitation.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own member invitation" });

      resource(MemberInvitation, "ci-runner", {
        graphArn: "arn:aws:iam::123456789012:policy/ci",
        memberEmailAddress: "example",
        memberId: "example",
        disableEmailNotification: true,
        message: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.detective import MemberInvitation, MemberInvitationConfig

    def describe():
        ubx.intent("payments own member invitation")

        ubx.resource(MemberInvitation, "ci-runner", MemberInvitationConfig(
            graph_arn="arn:aws:iam::123456789012:policy/ci",
            member_email_address="example",
            member_id="example",
            disable_email_notification=True,
            message="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_detective_member_invitation, with graph arn
    arn:aws:iam::123456789012:policy/ci, member email address
    example, member id example, disable email notification true,
    message example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="disable_email_notification" type="bool">
  When set to true, invitation emails are not sent to the member accounts. Member accounts must still accept the invitation before they are added to the behavior graph. Updating this field has no effect. Optional.
</ResponseField>

<ResponseField name="graph_arn" type="string" required>
  The ARN of the graph to which the member account will be invited Required.
</ResponseField>

<ResponseField name="member_email_address" type="string" required>
  The root email address for the account to be invited, for validation. Updating this field has no effect. Required.
</ResponseField>

<ResponseField name="member_id" type="string" required>
  The AWS account ID to be invited to join the graph as a member Required.
</ResponseField>

<ResponseField name="message" type="string">
  A message to be included in the email invitation sent to the invited account. Updating this field has no effect. Optional.
</ResponseField>
