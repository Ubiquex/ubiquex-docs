---
title: "aws_detective_organization_admin_account"
description: "Real, generated bindings for `aws_detective_organization_admin_account`."
---

`aws_detective_organization_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization admin account"})

    		ubx.Resource(detective.OrganizationAdminAccount, "ci-runner", detective.OrganizationAdminAccountConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationAdminAccount } from "jsr:@ubx/sdk-aws/aws/detective/organization_admin_account";

    export default stack("payments", () => {
      intent({ summary: "payments own organization admin account" });

      resource(OrganizationAdminAccount, "ci-runner", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.detective import OrganizationAdminAccount, OrganizationAdminAccountConfig

    def describe():
        ubx.intent("payments own organization admin account")

        ubx.resource(OrganizationAdminAccount, "ci-runner", OrganizationAdminAccountConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_detective_organization_admin_account, with account id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
