---
title: "aws_detective_invitation_accepter"
description: "Real, generated bindings for `aws_detective_invitation_accepter`."
---

`aws_detective_invitation_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/detective"

    cfg := detective.InvitationAccepterConfig{
        GraphArn: "example",
    }
    ubx.Resource(detective.InvitationAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InvitationAccepter } from
      "jsr:@ubx/sdk-aws/aws/detective/invitation_accepter";

    resource(InvitationAccepter, "example", {
      graphArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.detective import (
        InvitationAccepter,
        InvitationAccepterConfig,
    )

    cfg = InvitationAccepterConfig(
        graph_arn="example",
    )
    ubx.resource(InvitationAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_detective_invitation_accepter with graph_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="graph_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
