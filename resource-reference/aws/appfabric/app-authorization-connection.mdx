---
title: "aws_appfabric_app_authorization_connection"
description: "Real, generated bindings for `aws_appfabric_app_authorization_connection`."
---

`aws_appfabric_app_authorization_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app authorization connection"})

    		ubx.Resource(appfabric.AppAuthorizationConnection, "ci-runner", appfabric.AppAuthorizationConnectionConfig{
    			AppAuthorizationArn: "arn:aws:iam::123456789012:policy/ci",
    			AppBundleArn:        "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppAuthorizationConnection } from "jsr:@ubx/sdk-aws/aws/appfabric/app_authorization_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own app authorization connection" });

      resource(AppAuthorizationConnection, "ci-runner", {
        appAuthorizationArn: "arn:aws:iam::123456789012:policy/ci",
        appBundleArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appfabric import AppAuthorizationConnection, AppAuthorizationConnectionConfig

    def describe():
        ubx.intent("payments own app authorization connection")

        ubx.resource(AppAuthorizationConnection, "ci-runner", AppAuthorizationConnectionConfig(
            app_authorization_arn="arn:aws:iam::123456789012:policy/ci",
            app_bundle_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appfabric_app_authorization_connection, with app
    authorization arn arn:aws:iam::123456789012:policy/ci, app bundle
    arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_authorization_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="app" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tenant" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="tenant_display_name" type="string">
    </ResponseField>
    <ResponseField name="tenant_identifier" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
