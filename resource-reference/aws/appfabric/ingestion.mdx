---
title: "aws_appfabric_ingestion"
description: "Real, generated bindings for `aws_appfabric_ingestion`."
---

`aws_appfabric_ingestion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"

    cfg := appfabric.IngestionConfig{
        App: "example",
        AppBundleArn: "example",
        IngestionType: "example",
        TenantId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appfabric.Ingestion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Ingestion } from
      "jsr:@ubx/sdk-aws/aws/appfabric/ingestion";

    resource(Ingestion, "example", {
      app: "example",
      appBundleArn: "example",
      ingestionType: "example",
      tenantId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appfabric import Ingestion, IngestionConfig

    cfg = IngestionConfig(
        app="example",
        app_bundle_arn="example",
        ingestion_type="example",
        tenant_id="example",
        tags={"key": "value"},
    )
    ubx.resource(Ingestion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appfabric_ingestion with app, app_bundle_arn,
ingestion_type, tenant_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app" type="string" required>
  Required.
</ResponseField>

<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tenant_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
