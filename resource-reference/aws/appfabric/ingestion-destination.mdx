---
title: "aws_appfabric_ingestion_destination"
description: "Real, generated bindings for `aws_appfabric_ingestion_destination`."
---

`aws_appfabric_ingestion_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ingestion destination"})

    		ubx.Resource(appfabric.IngestionDestination, "ci-runner", appfabric.IngestionDestinationConfig{
    			AppBundleArn: "arn:aws:iam::123456789012:policy/ci",
    			IngestionArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IngestionDestination } from "jsr:@ubx/sdk-aws/aws/appfabric/ingestion_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own ingestion destination" });

      resource(IngestionDestination, "ci-runner", {
        appBundleArn: "arn:aws:iam::123456789012:policy/ci",
        ingestionArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appfabric import IngestionDestination, IngestionDestinationConfig

    def describe():
        ubx.intent("payments own ingestion destination")

        ubx.resource(IngestionDestination, "ci-runner", IngestionDestinationConfig(
            app_bundle_arn="arn:aws:iam::123456789012:policy/ci",
            ingestion_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appfabric_ingestion_destination, with app bundle arn
    arn:aws:iam::123456789012:policy/ci, ingestion arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
