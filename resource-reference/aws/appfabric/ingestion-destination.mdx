---
title: "aws_appfabric_ingestion_destination"
description: "Real, generated bindings for `aws_appfabric_ingestion_destination`."
---

`aws_appfabric_ingestion_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"

    cfg := appfabric.IngestionDestinationConfig{
        AppBundleArn: "example",
        IngestionArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appfabric.IngestionDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IngestionDestination } from
      "jsr:@ubx/sdk-aws/aws/appfabric/ingestion_destination";

    resource(IngestionDestination, "example", {
      appBundleArn: "example",
      ingestionArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appfabric import (
        IngestionDestination,
        IngestionDestinationConfig,
    )

    cfg = IngestionDestinationConfig(
        app_bundle_arn="example",
        ingestion_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(IngestionDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appfabric_ingestion_destination with
app_bundle_arn, ingestion_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
