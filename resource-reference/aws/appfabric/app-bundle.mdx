---
title: "aws_appfabric_app_bundle"
description: "Real, generated bindings for `aws_appfabric_app_bundle`."
---

`aws_appfabric_app_bundle` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"

    cfg := appfabric.AppBundleConfig{
        CustomerManagedKeyArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appfabric.AppBundle, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AppBundle } from
      "jsr:@ubx/sdk-aws/aws/appfabric/app_bundle";

    resource(AppBundle, "example", {
      customerManagedKeyArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appfabric import AppBundle, AppBundleConfig

    cfg = AppBundleConfig(
        customer_managed_key_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(AppBundle, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appfabric_app_bundle with customer_managed_key_arn,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="customer_managed_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
