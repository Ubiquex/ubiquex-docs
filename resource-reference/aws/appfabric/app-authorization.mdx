---
title: "aws_appfabric_app_authorization"
description: "Real, generated bindings for `aws_appfabric_app_authorization`."
---

`aws_appfabric_app_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/appfabric"

    cfg := appfabric.AppAuthorizationConfig{
        App: "example",
        AppBundleArn: "example",
        AuthType: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(appfabric.AppAuthorization, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AppAuthorization } from
      "jsr:@ubx/sdk-aws/aws/appfabric/app_authorization";

    resource(AppAuthorization, "example", {
      app: "example",
      appBundleArn: "example",
      authType: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.appfabric import (
        AppAuthorization,
        AppAuthorizationConfig,
    )

    cfg = AppAuthorizationConfig(
        app="example",
        app_bundle_arn="example",
        auth_type="example",
        tags={"key": "value"},
    )
    ubx.resource(AppAuthorization, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_appfabric_app_authorization with app,
app_bundle_arn, auth_type, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app" type="string" required>
  Required.
</ResponseField>

<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="auth_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="auth_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="persona" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
