---
title: "aws_appfabric_app_authorization"
description: "Real, generated bindings for `aws_appfabric_app_authorization`."
---

`aws_appfabric_app_authorization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app authorization"})

    		ubx.Resource(appfabric.AppAuthorization, "ci-runner", appfabric.AppAuthorizationConfig{
    			App:          "example",
    			AppBundleArn: "arn:aws:iam::123456789012:policy/ci",
    			AuthType:     "example",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppAuthorization } from "jsr:@ubx/sdk-aws/aws/appfabric/app_authorization";

    export default stack("payments", () => {
      intent({ summary: "payments own app authorization" });

      resource(AppAuthorization, "ci-runner", {
        app: "example",
        appBundleArn: "arn:aws:iam::123456789012:policy/ci",
        authType: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appfabric import AppAuthorization, AppAuthorizationConfig

    def describe():
        ubx.intent("payments own app authorization")

        ubx.resource(AppAuthorization, "ci-runner", AppAuthorizationConfig(
            app="example",
            app_bundle_arn="arn:aws:iam::123456789012:policy/ci",
            auth_type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appfabric_app_authorization, with app example, app
    bundle arn arn:aws:iam::123456789012:policy/ci, auth type
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app" type="string" required>
  Required.
</ResponseField>

<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="auth_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="auth_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="persona" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
