---
title: "aws_securityhub_standards_control_association"
description: "Real, generated bindings for `aws_securityhub_standards_control_association`."
---

`aws_securityhub_standards_control_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/securityhub"

    cfg := securityhub.StandardsControlAssociationConfig{
        AssociationStatus: "example",
        SecurityControlId: "example",
        StandardsArn: "example",
        UpdatedReason: "example",
    }
    ubx.Resource(
        securityhub.StandardsControlAssociation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { StandardsControlAssociation } from
      "jsr:@ubx/sdk-aws/aws/securityhub/standards_control_association";

    resource(StandardsControlAssociation, "example", {
      associationStatus: "example",
      securityControlId: "example",
      standardsArn: "example",
      updatedReason: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        StandardsControlAssociation,
        StandardsControlAssociationConfig,
    )

    cfg = StandardsControlAssociationConfig(
        association_status="example",
        security_control_id="example",
        standards_arn="example",
        updated_reason="example",
    )
    ubx.resource(StandardsControlAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_standards_control_association with
association_status, security_control_id, standards_arn,
updated_reason.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="association_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_control_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="standards_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="updated_reason" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
