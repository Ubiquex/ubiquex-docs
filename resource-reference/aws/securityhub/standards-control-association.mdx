---
title: "aws_securityhub_standards_control_association"
description: "Real, generated bindings for `aws_securityhub_standards_control_association`."
---

`aws_securityhub_standards_control_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own standards control association"})

    		ubx.Resource(securityhub.StandardsControlAssociation, "ci-runner", securityhub.StandardsControlAssociationConfig{
    			AssociationStatus: "example",
    			SecurityControlId: "example",
    			StandardsArn:      "arn:aws:iam::123456789012:policy/ci",
    			UpdatedReason:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StandardsControlAssociation } from "jsr:@ubx/sdk-aws/aws/securityhub/standards_control_association";

    export default stack("payments", () => {
      intent({ summary: "payments own standards control association" });

      resource(StandardsControlAssociation, "ci-runner", {
        associationStatus: "example",
        securityControlId: "example",
        standardsArn: "arn:aws:iam::123456789012:policy/ci",
        updatedReason: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import StandardsControlAssociation, StandardsControlAssociationConfig

    def describe():
        ubx.intent("payments own standards control association")

        ubx.resource(StandardsControlAssociation, "ci-runner", StandardsControlAssociationConfig(
            association_status="example",
            security_control_id="example",
            standards_arn="arn:aws:iam::123456789012:policy/ci",
            updated_reason="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_standards_control_association, with
    association status example, security control id example,
    standards arn arn:aws:iam::123456789012:policy/ci, updated reason
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="association_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_control_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="standards_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="updated_reason" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
