---
title: "aws_securityhub_action_target"
description: "Real, generated bindings for `aws_securityhub_action_target`."
---

`aws_securityhub_action_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own action target"})

    		ubx.Resource(securityhub.ActionTarget, "ci-runner", securityhub.ActionTargetConfig{
    			Description: "Managed by ubx.",
    			Identifier:  "example",
    			Name:        "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ActionTarget } from "jsr:@ubx/sdk-aws/aws/securityhub/action_target";

    export default stack("payments", () => {
      intent({ summary: "payments own action target" });

      resource(ActionTarget, "ci-runner", {
        description: "Managed by ubx.",
        identifier: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import ActionTarget, ActionTargetConfig

    def describe():
        ubx.intent("payments own action target")

        ubx.resource(ActionTarget, "ci-runner", ActionTargetConfig(
            description="Managed by ubx.",
            identifier="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_action_target called "ci-runner", with
    description Managed by ubx., identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
