---
title: "aws_securityhub_action_target"
description: "Real, generated bindings for `aws_securityhub_action_target`."
---

`aws_securityhub_action_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"

    cfg := securityhub.ActionTargetConfig{
        Description: "example",
        Identifier: "example",
        Name: "example-name",
    }
    ubx.Resource(securityhub.ActionTarget, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ActionTarget } from
      "jsr:@ubx/sdk-aws/aws/securityhub/action_target";

    resource(ActionTarget, "example", {
      description: "example",
      identifier: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        ActionTarget,
        ActionTargetConfig,
    )

    cfg = ActionTargetConfig(
        description="example",
        identifier="example",
        name="example",
    )
    ubx.resource(ActionTarget, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_action_target with description,
identifier, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
