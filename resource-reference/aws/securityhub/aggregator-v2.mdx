---
title: "aws_securityhub_aggregator_v2"
description: "Real, generated bindings for `aws_securityhub_aggregator_v2`."
---

`aws_securityhub_aggregator_v2` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own aggregator v2"})

    		ubx.Resource(securityhub.AggregatorV2, "ci-runner", securityhub.AggregatorV2Config{
    			RegionLinkingMode: "example",
    			LinkedRegions:     []string{"example"},
    			Tags:              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AggregatorV2 } from "jsr:@ubx/sdk-aws/aws/securityhub/aggregator_v2";

    export default stack("payments", () => {
      intent({ summary: "payments own aggregator v2" });

      resource(AggregatorV2, "ci-runner", {
        regionLinkingMode: "example",
        linkedRegions: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import AggregatorV2, AggregatorV2Config

    def describe():
        ubx.intent("payments own aggregator v2")

        ubx.resource(AggregatorV2, "ci-runner", AggregatorV2Config(
            region_linking_mode="example",
            linked_regions=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_aggregator_v2, with region linking mode
    example, linked regions a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="linked_regions" type="set(string)">
  The list of Regions linked to the aggregation Region. Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_linking_mode" type="string" required>
  Determines how Regions are linked: ALL_REGIONS, ALL_REGIONS_EXCEPT_SPECIFIED, or SPECIFIED_REGIONS. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="aggregation_region" type="string">
  The AWS Region where data is aggregated. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
