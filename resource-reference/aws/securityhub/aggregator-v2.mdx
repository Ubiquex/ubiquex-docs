---
title: "aws_securityhub_aggregator_v2"
description: "Real, generated bindings for `aws_securityhub_aggregator_v2`."
---

`aws_securityhub_aggregator_v2` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/securityhub"

    cfg := securityhub.AggregatorV2Config{
        RegionLinkingMode: "example",
        LinkedRegions: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(securityhub.AggregatorV2, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AggregatorV2 } from
      "jsr:@ubx/sdk-aws/aws/securityhub/aggregator_v2";

    resource(AggregatorV2, "example", {
      regionLinkingMode: "example",
      linkedRegions: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        AggregatorV2,
        AggregatorV2Config,
    )

    cfg = AggregatorV2Config(
        region_linking_mode="example",
        linked_regions=["example"],
        tags={"key": "value"},
    )
    ubx.resource(AggregatorV2, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_aggregator_v2 with region_linking_mode,
linked_regions, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="linked_regions" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region_linking_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="aggregation_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
