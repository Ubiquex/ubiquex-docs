---
title: "aws_security_hub_aggregator_v2"
description: "`aws_security_hub_aggregator_v2` configures real cross-Region and cross-account aggregation of findings and risk data under Security Hub's..."
---

`aws_security_hub_aggregator_v2` configures real cross-Region and cross-account aggregation of findings and risk data under Security Hub's next-generation (HubV2) model, the V2 counterpart to the original `aws_security_hub_finding_aggregator`. Security Hub's major relaunch, unifying findings across GuardDuty, Inspector, Macie, and Security Hub CSPM with near-real-time risk analytics, was announced at re:Invent in December 2025, roughly eight months ago.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own aggregator v2"})

    		ubx.Resource(securityhub.AggregatorV2, "example", securityhub.AggregatorV2Config{
    			LinkedRegions:     []string{"example"},
    			RegionLinkingMode: "example",
    			Tags:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AggregatorV2 } from "@ubx/sdk-aws/aws/securityhub/aggregator_v2";

    export default stack("example", () => {
      intent({ summary: "example own aggregator v2" });

      resource(AggregatorV2, "example", {
        linkedRegions: ["example"],
        regionLinkingMode: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import AggregatorV2, AggregatorV2Config

    def describe():
        ubx.intent("example own aggregator v2")

        ubx.resource(AggregatorV2, "example", AggregatorV2Config(
            linked_regions=["example"],
            region_linking_mode="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_security_hub_aggregator_v2, with linked regions a set
    value, region linking mode example, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="linked_regions" type="list(string)" required>
  The list of included Regions Required.
</ResponseField>

<ResponseField name="region_linking_mode" type="string" required>
  Indicates to link a list of included Regions Required.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with the Security Hub V2 resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="aggregation_region" type="string">
  The AWS Region to which Security Hub aggregates findings from all linked Regions in the cross-Region aggregation configuration. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="aggregator_v2_arn" type="string">
  The ARN of the AggregatorV2 being created and assigned as the unique identifier Computed by AWS after `ship`.
</ResponseField>
