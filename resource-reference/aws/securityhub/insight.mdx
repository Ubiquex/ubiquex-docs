---
title: "aws_securityhub_insight"
description: "Real, generated bindings for `aws_securityhub_insight`."
---

`aws_securityhub_insight` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"

    cfg := securityhub.InsightConfig{
        GroupByAttribute: "example",
        Name: "example-name",
    }
    ubx.Resource(securityhub.Insight, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Insight } from
      "jsr:@ubx/sdk-aws/aws/securityhub/insight";

    resource(Insight, "example", {
      groupByAttribute: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import Insight, InsightConfig

    cfg = InsightConfig(
        group_by_attribute="example",
        name="example",
    )
    ubx.resource(Insight, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_insight with group_by_attribute, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_by_attribute" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
