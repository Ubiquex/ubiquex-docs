---
title: "aws_securityhub_standards_control"
description: "Real, generated bindings for `aws_securityhub_standards_control`."
---

`aws_securityhub_standards_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"

    cfg := securityhub.StandardsControlConfig{
        ControlStatus: "example",
        StandardsControlArn: "example",
        DisabledReason: "example",
    }
    ubx.Resource(securityhub.StandardsControl, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { StandardsControl } from
      "jsr:@ubx/sdk-aws/aws/securityhub/standards_control";

    resource(StandardsControl, "example", {
      controlStatus: "example",
      standardsControlArn: "example",
      disabledReason: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        StandardsControl,
        StandardsControlConfig,
    )

    cfg = StandardsControlConfig(
        control_status="example",
        standards_control_arn="example",
        disabled_reason="example",
    )
    ubx.resource(StandardsControl, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_standards_control with control_status,
standards_control_arn, disabled_reason.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="control_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disabled_reason" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="standards_control_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="control_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="control_status_updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="disabled_reason" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="related_requirements" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="remediation_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="severity_rating" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="title" type="string">
  Computed by AWS after `ship`.
</ResponseField>
