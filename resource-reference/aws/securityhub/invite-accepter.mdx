---
title: "aws_securityhub_invite_accepter"
description: "Real, generated bindings for `aws_securityhub_invite_accepter`."
---

`aws_securityhub_invite_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"

    cfg := securityhub.InviteAccepterConfig{
        MasterId: "example",
    }
    ubx.Resource(securityhub.InviteAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InviteAccepter } from
      "jsr:@ubx/sdk-aws/aws/securityhub/invite_accepter";

    resource(InviteAccepter, "example", {
      masterId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        InviteAccepter,
        InviteAccepterConfig,
    )

    cfg = InviteAccepterConfig(
        master_id="example",
    )
    ubx.resource(InviteAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_invite_accepter with master_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="master_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invitation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
