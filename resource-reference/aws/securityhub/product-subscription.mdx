---
title: "aws_securityhub_product_subscription"
description: "Real, generated bindings for `aws_securityhub_product_subscription`."
---

`aws_securityhub_product_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"

    cfg := securityhub.ProductSubscriptionConfig{
        ProductArn: "example",
    }
    ubx.Resource(securityhub.ProductSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ProductSubscription } from
      "jsr:@ubx/sdk-aws/aws/securityhub/product_subscription";

    resource(ProductSubscription, "example", {
      productArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        ProductSubscription,
        ProductSubscriptionConfig,
    )

    cfg = ProductSubscriptionConfig(
        product_arn="example",
    )
    ubx.resource(ProductSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_product_subscription with product_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="product_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
