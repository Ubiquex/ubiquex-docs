---
title: "aws_securityhub_configuration_policy_association"
description: "Real, generated bindings for `aws_securityhub_configuration_policy_association`."
---

`aws_securityhub_configuration_policy_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own configuration policy association"})

    		ubx.Resource(securityhub.ConfigurationPolicyAssociation, "ci-runner", securityhub.ConfigurationPolicyAssociationConfig{
    			PolicyId: "example",
    			TargetId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationPolicyAssociation } from "jsr:@ubx/sdk-aws/aws/securityhub/configuration_policy_association";

    export default stack("payments", () => {
      intent({ summary: "payments own configuration policy association" });

      resource(ConfigurationPolicyAssociation, "ci-runner", {
        policyId: "example",
        targetId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import ConfigurationPolicyAssociation, ConfigurationPolicyAssociationConfig

    def describe():
        ubx.intent("payments own configuration policy association")

        ubx.resource(ConfigurationPolicyAssociation, "ci-runner", ConfigurationPolicyAssociationConfig(
            policy_id="example",
            target_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_configuration_policy_association, with
    policy id example, target id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
