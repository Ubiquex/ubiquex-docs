---
title: "aws_securityhub_configuration_policy"
description: "Real, generated bindings for `aws_securityhub_configuration_policy`."
---

`aws_securityhub_configuration_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/securityhub"

    cfg := securityhub.ConfigurationPolicyConfig{
        Name: "example-name",
        Description: "example",
    }
    ubx.Resource(securityhub.ConfigurationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConfigurationPolicy } from
      "jsr:@ubx/sdk-aws/aws/securityhub/configuration_policy";

    resource(ConfigurationPolicy, "example", {
      name: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        ConfigurationPolicy,
        ConfigurationPolicyConfig,
    )

    cfg = ConfigurationPolicyConfig(
        name="example",
        description="example",
    )
    ubx.resource(ConfigurationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_configuration_policy with name,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
