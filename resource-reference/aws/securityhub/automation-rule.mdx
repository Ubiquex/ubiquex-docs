---
title: "aws_securityhub_automation_rule"
description: "Real, generated bindings for `aws_securityhub_automation_rule`."
---

`aws_securityhub_automation_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/securityhub"

    cfg := securityhub.AutomationRuleConfig{
        Description: "example",
        RuleName: "example-rule-name",
        RuleOrder: 1,
        IsTerminal: true,
        RuleStatus: "example",
    }
    ubx.Resource(securityhub.AutomationRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AutomationRule } from
      "jsr:@ubx/sdk-aws/aws/securityhub/automation_rule";

    resource(AutomationRule, "example", {
      description: "example",
      ruleName: "example",
      ruleOrder: 1,
      isTerminal: true,
      ruleStatus: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        AutomationRule,
        AutomationRuleConfig,
    )

    cfg = AutomationRuleConfig(
        description="example",
        rule_name="example",
        rule_order=1,
        is_terminal=True,
        rule_status="example",
    )
    ubx.resource(AutomationRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_automation_rule with description,
rule_name, rule_order, is_terminal, rule_status.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="is_terminal" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_order" type="number" required>
  Required.
</ResponseField>

<ResponseField name="rule_status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_terminal" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
