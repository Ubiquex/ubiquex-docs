---
title: "aws_securityhub_standards_subscription"
description: "Real, generated bindings for `aws_securityhub_standards_subscription`."
---

`aws_securityhub_standards_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/securityhub"

    cfg := securityhub.StandardsSubscriptionConfig{
        StandardsArn: "example",
    }
    ubx.Resource(securityhub.StandardsSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { StandardsSubscription } from
      "jsr:@ubx/sdk-aws/aws/securityhub/standards_subscription";

    resource(StandardsSubscription, "example", {
      standardsArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        StandardsSubscription,
        StandardsSubscriptionConfig,
    )

    cfg = StandardsSubscriptionConfig(
        standards_arn="example",
    )
    ubx.resource(StandardsSubscription, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_standards_subscription with
standards_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="standards_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
