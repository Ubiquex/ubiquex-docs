---
title: "aws_securityhub_member"
description: "Real, generated bindings for `aws_securityhub_member`."
---

`aws_securityhub_member` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own member"})

    		ubx.Resource(securityhub.Member, "ci-runner", securityhub.MemberConfig{
    			AccountId: "example",
    			Email:     "example",
    			Invite:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Member } from "jsr:@ubx/sdk-aws/aws/securityhub/member";

    export default stack("payments", () => {
      intent({ summary: "payments own member" });

      resource(Member, "ci-runner", {
        accountId: "example",
        email: "example",
        invite: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import Member, MemberConfig

    def describe():
        ubx.intent("payments own member")

        ubx.resource(Member, "ci-runner", MemberConfig(
            account_id="example",
            email="example",
            invite=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_member, with account id example, email
    example, invite true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="email" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invite" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="master_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="member_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
