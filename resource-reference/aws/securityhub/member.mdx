---
title: "aws_securityhub_member"
description: "Real, generated bindings for `aws_securityhub_member`."
---

`aws_securityhub_member` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"

    cfg := securityhub.MemberConfig{
        AccountId: "example",
        Email: "example",
        Invite: true,
    }
    ubx.Resource(securityhub.Member, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Member } from "jsr:@ubx/sdk-aws/aws/securityhub/member";

    resource(Member, "example", {
      accountId: "example",
      email: "example",
      invite: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import Member, MemberConfig

    cfg = MemberConfig(
        account_id="example",
        email="example",
        invite=True,
    )
    ubx.resource(Member, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_member with account_id, email, invite.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="email" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invite" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="master_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="member_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
