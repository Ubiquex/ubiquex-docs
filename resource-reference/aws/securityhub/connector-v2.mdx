---
title: "aws_securityhub_connector_v2"
description: "Real, generated bindings for `aws_securityhub_connector_v2`."
---

`aws_securityhub_connector_v2` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connector v2"})

    		ubx.Resource(securityhub.ConnectorV2, "ci-runner", securityhub.ConnectorV2Config{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			KmsKeyArn:   "arn:aws:iam::123456789012:policy/ci",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectorV2 } from "jsr:@ubx/sdk-aws/aws/securityhub/connector_v2";

    export default stack("payments", () => {
      intent({ summary: "payments own connector v2" });

      resource(ConnectorV2, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import ConnectorV2, ConnectorV2Config

    def describe():
        ubx.intent("payments own connector v2")

        ubx.resource(ConnectorV2, "ci-runner", ConnectorV2Config(
            name="ci-runner",
            description="Managed by ubx.",
            kms_key_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_connector_v2 called "ci-runner", with
    description Managed by ubx., kms key arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connector_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="health" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="connector_status" type="string">
    </ResponseField>
    <ResponseField name="last_checked_at" type="string">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
