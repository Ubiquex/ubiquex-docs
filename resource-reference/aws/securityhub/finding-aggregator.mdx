---
title: "aws_securityhub_finding_aggregator"
description: "Real, generated bindings for `aws_securityhub_finding_aggregator`."
---

`aws_securityhub_finding_aggregator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/securityhub"

    cfg := securityhub.FindingAggregatorConfig{
        LinkingMode: "example",
        SpecifiedRegions: []string{"example"},
    }
    ubx.Resource(securityhub.FindingAggregator, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FindingAggregator } from
      "jsr:@ubx/sdk-aws/aws/securityhub/finding_aggregator";

    resource(FindingAggregator, "example", {
      linkingMode: "example",
      specifiedRegions: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.securityhub import (
        FindingAggregator,
        FindingAggregatorConfig,
    )

    cfg = FindingAggregatorConfig(
        linking_mode="example",
        specified_regions=["example"],
    )
    ubx.resource(FindingAggregator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_securityhub_finding_aggregator with linking_mode,
specified_regions.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="linking_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="specified_regions" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
