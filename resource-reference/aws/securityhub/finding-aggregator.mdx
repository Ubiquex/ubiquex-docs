---
title: "aws_securityhub_finding_aggregator"
description: "Real, generated bindings for `aws_securityhub_finding_aggregator`."
---

`aws_securityhub_finding_aggregator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own finding aggregator"})

    		ubx.Resource(securityhub.FindingAggregator, "ci-runner", securityhub.FindingAggregatorConfig{
    			LinkingMode:      "example",
    			SpecifiedRegions: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FindingAggregator } from "jsr:@ubx/sdk-aws/aws/securityhub/finding_aggregator";

    export default stack("payments", () => {
      intent({ summary: "payments own finding aggregator" });

      resource(FindingAggregator, "ci-runner", {
        linkingMode: "example",
        specifiedRegions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import FindingAggregator, FindingAggregatorConfig

    def describe():
        ubx.intent("payments own finding aggregator")

        ubx.resource(FindingAggregator, "ci-runner", FindingAggregatorConfig(
            linking_mode="example",
            specified_regions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_finding_aggregator, with linking mode
    example, specified regions a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="linking_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="specified_regions" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
