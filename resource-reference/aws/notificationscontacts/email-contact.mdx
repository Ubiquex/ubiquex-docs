---
title: "aws_notificationscontacts_email_contact"
description: "Real, generated bindings for `aws_notificationscontacts_email_contact`."
---

`aws_notificationscontacts_email_contact` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notificationscontacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/notificationscontacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own email contact"})

    		ubx.Resource(notificationscontacts.EmailContact, "ci-runner", notificationscontacts.EmailContactConfig{
    			EmailAddress: "example",
    			Name:         "ci-runner",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EmailContact } from "jsr:@ubx/sdk-aws/aws/notificationscontacts/email_contact";

    export default stack("payments", () => {
      intent({ summary: "payments own email contact" });

      resource(EmailContact, "ci-runner", {
        emailAddress: "example",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.notificationscontacts import EmailContact, EmailContactConfig

    def describe():
        ubx.intent("payments own email contact")

        ubx.resource(EmailContact, "ci-runner", EmailContactConfig(
            email_address="example",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_notificationscontacts_email_contact called
    "ci-runner", with email address example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="email_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
