---
title: "aws_notifications_contacts_email_contact"
description: "`aws_notifications_contacts_email_contact` registers a real email address as a contact that AWS User Notifications can deliver notifications to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notificationscontacts "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/notificationscontacts"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own email contact"})

    		ubx.Resource(notificationscontacts.EmailContact, "example", notificationscontacts.EmailContactConfig{
    			EmailAddress: "example",
    			Name:         "example",
    			Tags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EmailContact } from "@ubx/sdk-aws/aws/notificationscontacts/email_contact";

    export default stack("example", () => {
      intent({ summary: "example own email contact" });

      resource(EmailContact, "example", {
        emailAddress: "example",
        name: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.notificationscontacts import EmailContact, EmailContactConfig
    from ubx.aws.notificationscontacts.email_contact import EmailContact_Tags

    def describe():
        ubx.intent("example own email contact")

        ubx.resource(EmailContact, "example", EmailContactConfig(
            email_address="example",
            name="example",
            tags=[EmailContact_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_notifications_contacts_email_contact called "example",
    with email address example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="email_address" type="string" required>
  The email address of the contact that will be used to receive notifications from AWS services. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string" required>
  The friendly name for the email contact, used as a human-readable identifier for the contact within AWS User Notifications. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tags that are attached to the role. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Provides the string value for a tag key attached to an email contact resource in AWS Notifications Contacts, enabling custom metadata for categorization and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this email contact resource in AWS User Notifications. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="email_contact" type="object">
  The email_contact field is a computed object that returns the details of the created email contact, including its ARN and current status, from the AWS Notifications Contacts service. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="address" type="string" required>
      The email address of the contact that will receive AWS notifications. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="arn" type="string" required>
      The Amazon Resource Name (ARN) of the email contact that this resource associates with the notifications service. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="creation_time" type="string" required>
      The creation time of the email contact, stored as an ISO 8601 string, indicating when the contact was added to AWS User Notifications. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The friendly name assigned to the email contact, used to identify the contact in the AWS console and notifications. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string" required>
      Required.
    </ResponseField>
    <ResponseField name="update_time" type="string" required>
      The timestamp indicating when this email contact was last updated. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
