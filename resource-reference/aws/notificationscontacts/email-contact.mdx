---
title: "aws_notificationscontacts_email_contact"
description: "Real, generated bindings for `aws_notificationscontacts_email_contact`."
---

`aws_notificationscontacts_email_contact` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/notificationscontacts"

    cfg := notificationscontacts.EmailContactConfig{
        EmailAddress: "example",
        Name: "example-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(notificationscontacts.EmailContact, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EmailContact } from
      "jsr:@ubx/sdk-aws/aws/notificationscontacts/email_contact";

    resource(EmailContact, "example", {
      emailAddress: "example",
      name: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.notificationscontacts import (
        EmailContact,
        EmailContactConfig,
    )

    cfg = EmailContactConfig(
        email_address="example",
        name="example",
        tags={"key": "value"},
    )
    ubx.resource(EmailContact, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_notificationscontacts_email_contact with
email_address, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="email_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
