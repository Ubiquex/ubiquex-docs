---
title: "aws_fraud_detector_entity_type"
description: "`aws_fraud_detector_entity_type` categorized who or what performed a real event, a customer, a merchant..."
---

`aws_fraud_detector_entity_type` categorized who or what performed a real event, a customer, a merchant, that an `aws_fraud_detector_event_type` referenced. Closed to new customers along with the rest of Fraud Detector since November 7, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	frauddetector "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/frauddetector"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own entity type"})

    		ubx.Resource(frauddetector.EntityType, "example", frauddetector.EntityTypeConfig{
    			Name:        "example",
    			Description: "Managed by ubx.",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EntityType } from "@ubx/sdk-aws/aws/frauddetector/entity_type";

    export default stack("example", () => {
      intent({ summary: "example own entity type" });

      resource(EntityType, "example", {
        name: "example",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.frauddetector import EntityType, EntityTypeConfig
    from ubx.aws.frauddetector.entity_type import EntityType_Tags

    def describe():
        ubx.intent("example own entity type")

        ubx.resource(EntityType, "example", EntityTypeConfig(
            name="example",
            description="Managed by ubx.",
            tags=[EntityType_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_fraud_detector_entity_type called "example", with
    description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  The entity type description. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the entity type. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags associated with this entity type. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value portion of a tag attached to the AWS Fraud Detector entity type resource, enabling you to associate metadata for cost allocation, access control, or resource identification. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The entity type ARN. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  The timestamp when the entity type was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  The timestamp when the entity type was last updated. Computed by AWS after `ship`.
</ResponseField>
