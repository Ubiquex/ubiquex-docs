---
title: "aws_payment_cryptography_alias"
description: "`aws_payment_cryptography_alias` creates a real friendly name for an `aws_payment_cryptography_key`..."
---

`aws_payment_cryptography_alias` creates a real friendly name for an `aws_payment_cryptography_key`, so application code can reference the alias rather than a key ARN and keep working unchanged when the underlying key is rotated. A key can have multiple aliases, but each alias points to exactly one key at a time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	payment "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/payment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cryptography alias"})

    		ubx.Resource(payment.CryptographyAlias, "ci-runner", payment.CryptographyAliasConfig{
    			AliasName: "example-alias-name",
    			KeyArn:    "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CryptographyAlias } from "./local-sdk/aws/sdk/typescript/aws/payment/cryptography_alias.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own cryptography alias" });

      resource(CryptographyAlias, "ci-runner", {
        aliasName: "example-alias-name",
        keyArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.payment import CryptographyAlias, CryptographyAliasConfig

    def describe():
        ubx.intent("payments own cryptography alias")

        ubx.resource(CryptographyAlias, "ci-runner", CryptographyAliasConfig(
            alias_name="example-alias-name",
            key_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_payment_cryptography_alias, with alias name
    example-alias-name, key arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="alias_name" type="string" required>
  Specifies the friendly name for the AWS Payment Cryptography key alias, which must begin with 'alias/' and is used to reference a key in cryptographic operations. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="key_arn" type="string">
  The ARN of the AWS Payment Cryptography key that this alias is associated with, allowing the alias to serve as a friendly reference to the key for cryptographic operations. Optional. **(AI-inferred)**
</ResponseField>
