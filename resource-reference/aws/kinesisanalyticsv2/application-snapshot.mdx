---
title: "aws_kinesisanalyticsv2_application_snapshot"
description: "Real, generated bindings for `aws_kinesisanalyticsv2_application_snapshot`."
---

`aws_kinesisanalyticsv2_application_snapshot` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/kinesisanalyticsv2"

    cfg := kinesisanalyticsv2.ApplicationSnapshotConfig{
        ApplicationName: "example-application-name",
        SnapshotName: "example-snapshot-name",
    }
    ubx.Resource(
        kinesisanalyticsv2.ApplicationSnapshot,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ApplicationSnapshot } from
      "jsr:@ubx/sdk-aws/aws/kinesisanalyticsv2/application_snapshot";

    resource(ApplicationSnapshot, "example", {
      applicationName: "example",
      snapshotName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kinesisanalyticsv2 import (
        ApplicationSnapshot,
        ApplicationSnapshotConfig,
    )

    cfg = ApplicationSnapshotConfig(
        application_name="example",
        snapshot_name="example",
    )
    ubx.resource(ApplicationSnapshot, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kinesisanalyticsv2_application_snapshot with
application_name, snapshot_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="application_version_id" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_creation_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>
