---
title: "aws_kinesisanalyticsv2_application_snapshot"
description: "Real, generated bindings for `aws_kinesisanalyticsv2_application_snapshot`."
---

`aws_kinesisanalyticsv2_application_snapshot` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	kinesisanalyticsv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kinesisanalyticsv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application snapshot"})

    		ubx.Resource(kinesisanalyticsv2.ApplicationSnapshot, "ci-runner", kinesisanalyticsv2.ApplicationSnapshotConfig{
    			ApplicationName: "example-application-name",
    			SnapshotName:    "example-snapshot-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationSnapshot } from "jsr:@ubx/sdk-aws/aws/kinesisanalyticsv2/application_snapshot";

    export default stack("payments", () => {
      intent({ summary: "payments own application snapshot" });

      resource(ApplicationSnapshot, "ci-runner", {
        applicationName: "example-application-name",
        snapshotName: "example-snapshot-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.kinesisanalyticsv2 import ApplicationSnapshot, ApplicationSnapshotConfig

    def describe():
        ubx.intent("payments own application snapshot")

        ubx.resource(ApplicationSnapshot, "ci-runner", ApplicationSnapshotConfig(
            application_name="example-application-name",
            snapshot_name="example-snapshot-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_kinesisanalyticsv2_application_snapshot, with
    application name example-application-name, snapshot name
    example-snapshot-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="application_version_id" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_creation_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>
