---
title: "aws_kinesis_video_stream"
description: "Real, generated bindings for `aws_kinesis_video_stream`."
---

`aws_kinesis_video_stream` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/kinesis"

    cfg := kinesis.VideoStreamConfig{
        Name: "example-name",
        DataRetentionInHours: 1,
        DeviceName: "example-device-name",
    }
    ubx.Resource(kinesis.VideoStream, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VideoStream } from
      "jsr:@ubx/sdk-aws/aws/kinesis/video_stream";

    resource(VideoStream, "example", {
      name: "example",
      dataRetentionInHours: 1,
      deviceName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kinesis import VideoStream, VideoStreamConfig

    cfg = VideoStreamConfig(
        name="example",
        data_retention_in_hours=1,
        device_name="example",
    )
    ubx.resource(VideoStream, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kinesis_video_stream with name,
data_retention_in_hours, device_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data_retention_in_hours" type="number">
  Optional.
</ResponseField>

<ResponseField name="device_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="media_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
