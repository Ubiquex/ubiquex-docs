---
title: "aws_kinesis_analytics_application"
description: "Real, generated bindings for `aws_kinesis_analytics_application`."
---

`aws_kinesis_analytics_application` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/kinesis"

    cfg := kinesis.AnalyticsApplicationConfig{
        Name: "example-name",
        Code: "example",
        Description: "example",
    }
    ubx.Resource(kinesis.AnalyticsApplication, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AnalyticsApplication } from
      "jsr:@ubx/sdk-aws/aws/kinesis/analytics_application";

    resource(AnalyticsApplication, "example", {
      name: "example",
      code: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kinesis import (
        AnalyticsApplication,
        AnalyticsApplicationConfig,
    )

    cfg = AnalyticsApplicationConfig(
        name="example",
        code="example",
        description="example",
    )
    ubx.resource(AnalyticsApplication, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kinesis_analytics_application with name, code,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="code" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_application" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_update_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
