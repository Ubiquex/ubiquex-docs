---
title: "aws_kinesis_analytics_v2_application_reference_data_source"
description: "`aws_kinesis_analytics_v2_application_reference_data_source` adds a real static reference table, loaded from an S3 object..."
---

`aws_kinesis_analytics_v2_application_reference_data_source` adds a real static reference table, loaded from an S3 object, that an `aws_kinesis_analytics_v2_application`'s SQL-based code can join against incoming stream records.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	kinesis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kinesis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own analytics v2 application reference data source"})

    		ubx.Resource(kinesis.AnalyticsV2ApplicationReferenceDataSource, "ci-runner", kinesis.AnalyticsV2ApplicationReferenceDataSourceConfig{
    			ApplicationName:     "example-application-name",
    			ReferenceDataSource: map[string]any{"reference_schema": map[string]any{"record_columns": []map[string]any{{"name": "example-name"}}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalyticsV2ApplicationReferenceDataSource } from "./local-sdk/aws/sdk/typescript/aws/kinesis/analytics_v2_application_reference_data_source.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own analytics v2 application reference data source",
      });

      resource(AnalyticsV2ApplicationReferenceDataSource, "ci-runner", {
        applicationName: "example-application-name",
        referenceDataSource: {
          referenceSchema: { recordColumns: [{ name: "example-name" }] },
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.kinesis import AnalyticsV2ApplicationReferenceDataSource, AnalyticsV2ApplicationReferenceDataSourceConfig

    def describe():
        ubx.intent("payments own analytics v2 application reference data source")

        ubx.resource(AnalyticsV2ApplicationReferenceDataSource, "ci-runner", AnalyticsV2ApplicationReferenceDataSourceConfig(
            application_name="example-application-name",
            reference_data_source={"reference_schema": {"record_columns": [{"name": "example-name"}]}},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_kinesis_analytics_v2_application_reference_data_source, with
    application name example-application-name, reference data source
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_name" type="string" required>
  The name of the Kinesis Data Analytics for Apache Flink application to which the reference data source belongs. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="reference_data_source" type="object" required>
  The 'reference_data_source' property defines the reference data source for the Kinesis Data Analytics application, specifying the S3 location and bucket key, the table name, and the reference schema (columns, data types, and mappings) that describe how the application reads the reference data. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="reference_schema" type="object" required>
      The reference schema defines the column names and data types of the reference data source, enabling Kinesis Data Analytics to parse and query the reference data in SQL applications. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="record_columns" type="list(object)" required>
          Defines the list of columns in the reference data schema, specifying each column's name, SQL type, and mapping to the corresponding field in the source data. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="mapping" type="string">
              Specifies the mapping from a data element in the reference data source to this column, typically a JSON path (e.g., '$.field') for JSON record formats or a zero-based column index for CSV formats. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Specifies the unique name of a column in the in-application reference data stream schema, which must be a valid SQL identifier. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sql_type" type="string">
              Specifies the SQL data type of a column in the reference data source schema (e.g., VARCHAR(16), INTEGER, or BIGINT), as used by the SQL application to map incoming reference data records. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="record_encoding" type="string">
          Specifies the encoding of records in the reference data source, with UTF-8 being the only supported value. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="record_format" type="object" required>
          Specifies the record format (JSON or CSV) used by the reference data source, including the mapping of data elements to the schema columns. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="mapping_parameters" type="object">
              Defines the mapping parameters for the record format, specifying either CSV mapping (delimiters) or JSON mapping (record column path) for the reference data source schema. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="csvmapping_parameters" type="object">
                  Specifies the CSV mapping parameters (including column delimiter and row delimiter) that Kinesis Data Analytics uses to parse reference data source records in CSV format. Optional. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="record_column_delimiter" type="string" required>
                      Specifies the single-character delimiter (such as a comma) used to separate columns in CSV-formatted reference data records. Required. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="record_row_delimiter" type="string" required>
                      Specifies the character or string that separates one row from the next in the CSV reference data, such as a newline, to define how records are parsed. Required. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="jsonmapping_parameters" type="object">
                  Defines the JSON mapping parameters, specifically the record row path, that instructs Kinesis Data Analytics how to locate records within a JSON-encoded reference data source payload. Optional. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="record_row_path" type="string" required>
                      Specifies the JSON path to the root of the array containing the records in the input JSON, such as '$' for the top-level array or '$.data' for a nested field. Required. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="record_format_type" type="string" required>
              Specifies the record format type (such as CSV or JSON) used by the reference data source in the Kinesis Data Analytics application. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="s3_reference_data_source" type="object">
      Specifies the S3 bucket and object key (and optional IAM role ARN) that contains the reference data for the Kinesis Analytics application. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="bucket_arn" type="string" required>
          The ARN of the S3 bucket from which the reference data source is read. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="file_key" type="string" required>
          The object key (file name) in the S3 bucket that identifies the reference data file to be loaded by the Kinesis Data Analytics application. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="table_name" type="string">
      Specifies the name of the in-application table that maps to the reference data source, which can be used in SQL queries within the Kinesis Data Analytics application. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
