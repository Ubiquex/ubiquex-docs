---
title: "aws_kinesis_account_settings"
description: "Real, generated bindings for `aws_kinesis_account_settings`."
---

`aws_kinesis_account_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kinesis"

    cfg := kinesis.AccountSettingsConfig{
    }
    ubx.Resource(kinesis.AccountSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountSettings } from
      "jsr:@ubx/sdk-aws/aws/kinesis/account_settings";

    resource(AccountSettings, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kinesis import (
        AccountSettings,
        AccountSettingsConfig,
    )

    cfg = AccountSettingsConfig(
    )
    ubx.resource(AccountSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kinesis_account_settings.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
