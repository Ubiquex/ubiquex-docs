---
title: "aws_kinesis_stream_consumer"
description: "Real, generated bindings for `aws_kinesis_stream_consumer`."
---

`aws_kinesis_stream_consumer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/kinesis"

    cfg := kinesis.StreamConsumerConfig{
        Name: "example-name",
        StreamArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(kinesis.StreamConsumer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { StreamConsumer } from
      "jsr:@ubx/sdk-aws/aws/kinesis/stream_consumer";

    resource(StreamConsumer, "example", {
      name: "example",
      streamArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.kinesis import (
        StreamConsumer,
        StreamConsumerConfig,
    )

    cfg = StreamConsumerConfig(
        name="example",
        stream_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(StreamConsumer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_kinesis_stream_consumer with name, stream_arn,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stream_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
