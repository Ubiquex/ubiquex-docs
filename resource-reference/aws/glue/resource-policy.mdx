---
title: "aws_glue_resource_policy"
description: "Real, generated bindings for `aws_glue_resource_policy`."
---

`aws_glue_resource_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/glue"

    cfg := glue.ResourcePolicyConfig{
        Policy: "example",
        EnableHybrid: "example",
    }
    ubx.Resource(glue.ResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourcePolicy } from
      "jsr:@ubx/sdk-aws/aws/glue/resource_policy";

    resource(ResourcePolicy, "example", {
      policy: "example",
      enableHybrid: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import ResourcePolicy, ResourcePolicyConfig

    cfg = ResourcePolicyConfig(
        policy="example",
        enable_hybrid="example",
    )
    ubx.resource(ResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_resource_policy with policy, enable_hybrid.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enable_hybrid" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
