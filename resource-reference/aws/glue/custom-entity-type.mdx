---
title: "aws_glue_custom_entity_type"
description: "`aws_glue_custom_entity_type` defines a real, custom sensitive-data pattern, a regular expression..."
---

`aws_glue_custom_entity_type` defines a real, custom sensitive-data pattern, a regular expression, that AWS Glue's sensitive data detection can recognize during a crawl or ETL job beyond its own built-in entity types.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom entity type"})

    		ubx.Resource(glue.CustomEntityType, "ci-runner", glue.CustomEntityTypeConfig{
    			Name:         "ci-runner",
    			ContextWords: []string{"example"},
    			RegexString:  "example",
    			Tags:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomEntityType } from "./local-sdk/aws/sdk/typescript/aws/glue/custom_entity_type.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own custom entity type" });

      resource(CustomEntityType, "ci-runner", {
        name: "ci-runner",
        contextWords: ["example"],
        regexString: "example",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.glue import CustomEntityType, CustomEntityTypeConfig

    def describe():
        ubx.intent("payments own custom entity type")

        ubx.resource(CustomEntityType, "ci-runner", CustomEntityTypeConfig(
            name="ci-runner",
            context_words=["example"],
            regex_string="example",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_custom_entity_type called "ci-runner", with
    context words a set value, regex string example, tags example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="context_words" type="list(string)">
  A list of context words. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the custom entity type. Optional.
</ResponseField>

<ResponseField name="regex_string" type="string">
  A regular expression string that is used for detecting sensitive data in a custom pattern. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  Tags to associate with the custom entity type. Optional.
</ResponseField>
