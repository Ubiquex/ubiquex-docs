---
title: "aws_glue_workflow"
description: "Real, generated bindings for `aws_glue_workflow`."
---

`aws_glue_workflow` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workflow"})

    		ubx.Resource(glue.Workflow, "ci-runner", glue.WorkflowConfig{
    			Name:                 "ci-runner",
    			DefaultRunProperties: map[string]string{"managed-by": "ubx"},
    			Description:          "Managed by ubx.",
    			MaxConcurrentRuns:    1,
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Workflow } from "jsr:@ubx/sdk-aws/aws/glue/workflow";

    export default stack("payments", () => {
      intent({ summary: "payments own workflow" });

      resource(Workflow, "ci-runner", {
        name: "ci-runner",
        defaultRunProperties: { "managed-by": "ubx" },
        description: "Managed by ubx.",
        maxConcurrentRuns: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import Workflow, WorkflowConfig

    def describe():
        ubx.intent("payments own workflow")

        ubx.resource(Workflow, "ci-runner", WorkflowConfig(
            name="ci-runner",
            default_run_properties={"managed-by": "ubx"},
            description="Managed by ubx.",
            max_concurrent_runs=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_workflow called "ci-runner", with default run
    properties a set value, description Managed by ubx., max
    concurrent runs 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_run_properties" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_concurrent_runs" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
