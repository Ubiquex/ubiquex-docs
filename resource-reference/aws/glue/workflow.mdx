---
title: "aws_glue_workflow"
description: "Real, generated bindings for `aws_glue_workflow`."
---

`aws_glue_workflow` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"

    cfg := glue.WorkflowConfig{
        DefaultRunProperties: map[string]string{"key": "value"},
        Description: "example",
    }
    ubx.Resource(glue.Workflow, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Workflow } from "jsr:@ubx/sdk-aws/aws/glue/workflow";

    resource(Workflow, "example", {
      defaultRunProperties: { key: "value" },
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import Workflow, WorkflowConfig

    cfg = WorkflowConfig(
        default_run_properties={"key": "value"},
        description="example",
    )
    ubx.resource(Workflow, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_workflow with default_run_properties,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_run_properties" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_concurrent_runs" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
