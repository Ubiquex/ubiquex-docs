---
title: "aws_glue_security_configuration"
description: "Real, generated bindings for `aws_glue_security_configuration`."
---

`aws_glue_security_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"

    cfg := glue.SecurityConfigurationConfig{
        Name: "example-name",
    }
    ubx.Resource(glue.SecurityConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecurityConfiguration } from
      "jsr:@ubx/sdk-aws/aws/glue/security_configuration";

    resource(SecurityConfiguration, "example", {
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import (
        SecurityConfiguration,
        SecurityConfigurationConfig,
    )

    cfg = SecurityConfigurationConfig(
        name="example",
    )
    ubx.resource(SecurityConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_security_configuration with name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
