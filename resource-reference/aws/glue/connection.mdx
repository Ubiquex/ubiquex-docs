---
title: "aws_glue_connection"
description: "`aws_glue_connection` stores the real, actual connection details, JDBC URL, credentials, network placement..."
---

`aws_glue_connection` stores the real, actual connection details, JDBC URL, credentials, network placement, an `aws_glue_crawler` or `aws_glue_job` uses to reach a data source outside the Glue Data Catalog itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own connection"})

    		ubx.Resource(glue.Connection, "example", glue.ConnectionConfig{
    			CatalogId:       "example",
    			ConnectionInput: map[string]any{"connection_type": "example"},
    			Tags:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Connection } from "@ubx/sdk-aws/aws/glue/connection";

    export default stack("example", () => {
      intent({ summary: "example own connection" });

      resource(Connection, "example", {
        catalogId: "example",
        connectionInput: { connectionType: "example" },
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import Connection, ConnectionConfig
    from ubx.aws.glue.connection import Connection_ConnectionInput

    def describe():
        ubx.intent("example own connection")

        ubx.resource(Connection, "example", ConnectionConfig(
            catalog_id="example",
            connection_input=Connection_ConnectionInput(connection_type="example"),
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_connection, with catalog id example, connection
    input a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="catalog_id" type="string" required>
  The ID of the data catalog to create the catalog object in. Currently, this should be the AWS account ID. Required.
</ResponseField>

<ResponseField name="connection_input" type="object" required>
  Configuration object that defines the properties of an AWS Glue connection, including connection type, connection properties, and physical connection requirements for the target data store. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="athena_properties" type="any">
      Connection properties specific to the Athena compute environment. Optional.
    </ResponseField>
    <ResponseField name="authentication_configuration" type="object">
      Configures OAuth-based authentication details (such as client credentials and token endpoint) for Glue connection types that require OAuth, like Salesforce or ServiceNow. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="authentication_type" type="string" required>
          A structure containing the authentication configuration in the CreateConnection request. Required.
        </ResponseField>
        <ResponseField name="basic_authentication_credentials" type="object">
          A structure containing the authentication credentials in the CreateConnection request. Optional.
          <Expandable title="properties">
            <ResponseField name="password" type="string">
              The password used in the authentication configuration. Optional.
            </ResponseField>
            <ResponseField name="username" type="string">
              The username used in the authentication configuration. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="custom_authentication_credentials" type="any">
          A structure containing the authentication credentials in the CreateConnection request. Optional.
        </ResponseField>
        <ResponseField name="kms_key_arn" type="string">
          The Amazon Resource Name (ARN) of the KMS key used in the authentication configuration. Optional.
        </ResponseField>
        <ResponseField name="oauth2_properties" type="object">
          A structure containing the authentication credentials in the CreateConnection request. Optional.
          <Expandable title="properties">
            <ResponseField name="authorization_code_properties" type="object">
              A structure containing the authorization code used in the authentication configuration. Optional.
              <Expandable title="properties">
                <ResponseField name="authorization_code" type="string">
                  The authorization code used in the authentication configuration. Optional.
                </ResponseField>
                <ResponseField name="redirect_uri" type="string">
                  The redirect URI where the user gets redirected to by authorization server when issuing an authorization code. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="oauth2_client_application" type="object">
              The OAuth2 client app used for the connection. Optional.
              <Expandable title="properties">
                <ResponseField name="awsmanaged_client_application_reference" type="string">
                  The reference to the SaaS-side client app that is AWS managed. Optional.
                </ResponseField>
                <ResponseField name="user_managed_client_application_client_id" type="string">
                  The client application clientID if the ClientAppType is USER_MANAGED. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="oauth2_credentials" type="object">
              A structure containing the OAuth2 credentials used in the authentication configuration. Optional.
              <Expandable title="properties">
                <ResponseField name="access_token" type="string">
                  The access token used in the authentication configuration. Optional.
                </ResponseField>
                <ResponseField name="jwt_token" type="string">
                  The JSON Web Token (JWT) used when the authentication type is OAuth2. Optional.
                </ResponseField>
                <ResponseField name="refresh_token" type="string">
                  The refresh token used when the authentication type is OAuth2. Optional.
                </ResponseField>
                <ResponseField name="user_managed_client_application_client_secret" type="string">
                  The client application client secret if the client application is user managed. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="oauth2_grant_type" type="string">
              The grant type used in the authentication configuration. Optional.
            </ResponseField>
            <ResponseField name="token_url" type="string">
              The URL used in the authentication configuration. Optional.
            </ResponseField>
            <ResponseField name="token_url_parameters_map" type="any">
              A map of key-value pairs used in the authentication configuration. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="secret_arn" type="string">
          The secret manager ARN to store credentials in the CreateConnection request. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="connection_properties" type="any">
      A map of key-value pairs used as parameters for this connection. Optional.
    </ResponseField>
    <ResponseField name="connection_type" type="string" required>
      The type of the connection that needs to be created. Required.
    </ResponseField>
    <ResponseField name="description" type="string">
      A description of the connection. Optional.
    </ResponseField>
    <ResponseField name="match_criteria" type="list(string)">
      A list of criteria that can be used in selecting this connection. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the connection. Optional.
    </ResponseField>
    <ResponseField name="physical_connection_requirements" type="object">
      Specifies the VPC network configuration (subnet ID, security group IDs, and availability zone) needed to establish a physical connection to a data source such as a JDBC database. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="availability_zone" type="string">
          The availability zone where the connection is located. Optional.
        </ResponseField>
        <ResponseField name="security_group_id_list" type="list(string)">
          The security group ID list used by the connection. Optional.
        </ResponseField>
        <ResponseField name="subnet_id" type="string">
          The subnet ID used by the connection. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="python_properties" type="any">
      Connection properties specific to the Python compute environment. Optional.
    </ResponseField>
    <ResponseField name="spark_properties" type="any">
      Connection properties specific to the Spark compute environment. Optional.
    </ResponseField>
    <ResponseField name="validate_credentials" type="bool">
      A flag to validate the credentials during create connection. Default is true. Optional.
    </ResponseField>
    <ResponseField name="validate_for_compute_environments" type="list(string)">
      Specifies the list of compute environments (for example, SPARK or ATHENA) against which the connection is validated when the connection is created or updated. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  The collection of tags. Each tag element is associated with a given resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="name" type="string">
  The name of the connection. Computed by AWS after `ship`.
</ResponseField>
