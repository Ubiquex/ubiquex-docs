---
title: "aws_glue_trigger"
description: "Real, generated bindings for `aws_glue_trigger`."
---

`aws_glue_trigger` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trigger"})

    		ubx.Resource(glue.Trigger, "ci-runner", glue.TriggerConfig{
    			Name:            "ci-runner",
    			Type:            "example",
    			Description:     "Managed by ubx.",
    			Enabled:         true,
    			Schedule:        "example",
    			StartOnCreation: true,
    			Tags:            map[string]string{"managed-by": "ubx"},
    			WorkflowName:    "example-workflow-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Trigger } from "jsr:@ubx/sdk-aws/aws/glue/trigger";

    export default stack("payments", () => {
      intent({ summary: "payments own trigger" });

      resource(Trigger, "ci-runner", {
        name: "ci-runner",
        type: "example",
        description: "Managed by ubx.",
        enabled: true,
        schedule: "example",
        startOnCreation: true,
        tags: { "managed-by": "ubx" },
        workflowName: "example-workflow-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import Trigger, TriggerConfig

    def describe():
        ubx.intent("payments own trigger")

        ubx.resource(Trigger, "ci-runner", TriggerConfig(
            name="ci-runner",
            type="example",
            description="Managed by ubx.",
            enabled=True,
            schedule="example",
            start_on_creation=True,
            tags={"managed-by": "ubx"},
            workflow_name="example-workflow-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_trigger called "ci-runner", with type example,
    description Managed by ubx., enabled true, schedule example,
    start on creation true, tags a set value, workflow name
    example-workflow-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule" type="string">
  Optional.
</ResponseField>

<ResponseField name="start_on_creation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workflow_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
