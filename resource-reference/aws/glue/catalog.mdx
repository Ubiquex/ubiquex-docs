---
title: "aws_glue_catalog"
description: "Real, generated bindings for `aws_glue_catalog`."
---

`aws_glue_catalog` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog"})

    		ubx.Resource(glue.Catalog, "ci-runner", glue.CatalogConfig{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			OverwriteChildResourcePermissionsWithDefault: "example",
    			Parameters: map[string]string{"managed-by": "ubx"},
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Catalog } from "jsr:@ubx/sdk-aws/aws/glue/catalog";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog" });

      resource(Catalog, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        overwriteChildResourcePermissionsWithDefault: "example",
        parameters: { "managed-by": "ubx" },
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import Catalog, CatalogConfig

    def describe():
        ubx.intent("payments own catalog")

        ubx.resource(Catalog, "ci-runner", CatalogConfig(
            name="ci-runner",
            description="Managed by ubx.",
            overwrite_child_resource_permissions_with_default="example",
            parameters={"managed-by": "ubx"},
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_catalog called "ci-runner", with description
    Managed by ubx., overwrite child resource permissions with
    default example, parameters a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_full_table_external_data_access" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="overwrite_child_resource_permissions_with_default" type="string">
  Optional.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="allow_full_table_external_data_access" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="catalog_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
