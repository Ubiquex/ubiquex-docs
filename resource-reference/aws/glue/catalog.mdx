---
title: "aws_glue_catalog"
description: "Real, generated bindings for `aws_glue_catalog`."
---

`aws_glue_catalog` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/glue"

    cfg := glue.CatalogConfig{
        Name: "example-name",
        AllowFullTableExternalDataAccess: "example",
        Description: "example",
    }
    ubx.Resource(glue.Catalog, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Catalog } from "jsr:@ubx/sdk-aws/aws/glue/catalog";

    resource(Catalog, "example", {
      name: "example",
      allowFullTableExternalDataAccess: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import Catalog, CatalogConfig

    cfg = CatalogConfig(
        name="example",
        allow_full_table_external_data_access="example",
        description="example",
    )
    ubx.resource(Catalog, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_catalog with name,
allow_full_table_external_data_access, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_full_table_external_data_access" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="overwrite_child_resource_permissions_with_default" type="string">
  Optional.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="allow_full_table_external_data_access" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="catalog_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>
