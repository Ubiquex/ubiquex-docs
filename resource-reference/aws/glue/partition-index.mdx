---
title: "aws_glue_partition_index"
description: "Real, generated bindings for `aws_glue_partition_index`."
---

`aws_glue_partition_index` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own partition index"})

    		ubx.Resource(glue.PartitionIndex, "ci-runner", glue.PartitionIndexConfig{
    			DatabaseName: "example-database-name",
    			TableName:    "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PartitionIndex } from "jsr:@ubx/sdk-aws/aws/glue/partition_index";

    export default stack("payments", () => {
      intent({ summary: "payments own partition index" });

      resource(PartitionIndex, "ci-runner", {
        databaseName: "example-database-name",
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import PartitionIndex, PartitionIndexConfig

    def describe():
        ubx.intent("payments own partition index")

        ubx.resource(PartitionIndex, "ci-runner", PartitionIndexConfig(
            database_name="example-database-name",
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_partition_index, with database name
    example-database-name, table name example-table-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
