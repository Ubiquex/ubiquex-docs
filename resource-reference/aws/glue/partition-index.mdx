---
title: "aws_glue_partition_index"
description: "Real, generated bindings for `aws_glue_partition_index`."
---

`aws_glue_partition_index` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/glue"

    cfg := glue.PartitionIndexConfig{
        DatabaseName: "example-database-name",
        TableName: "example-table-name",
        CatalogId: "example",
    }
    ubx.Resource(glue.PartitionIndex, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PartitionIndex } from
      "jsr:@ubx/sdk-aws/aws/glue/partition_index";

    resource(PartitionIndex, "example", {
      databaseName: "example",
      tableName: "example",
      catalogId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import PartitionIndex, PartitionIndexConfig

    cfg = PartitionIndexConfig(
        database_name="example",
        table_name="example",
        catalog_id="example",
    )
    ubx.resource(PartitionIndex, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_partition_index with database_name,
table_name, catalog_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
