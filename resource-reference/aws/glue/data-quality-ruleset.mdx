---
title: "aws_glue_data_quality_ruleset"
description: "`aws_glue_data_quality_ruleset` defines a real set of data quality rules, in Glue's own DQDL language..."
---

`aws_glue_data_quality_ruleset` defines a real set of data quality rules, in Glue's own DQDL language, that can be evaluated against a table in an `aws_glue_catalog` to score completeness, accuracy, and consistency.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own data quality ruleset"})

    		ubx.Resource(glue.DataQualityRuleset, "example", glue.DataQualityRulesetConfig{
    			Name:        "example",
    			ClientToken: "example",
    			Description: "Managed by ubx.",
    			Ruleset:     "example",
    			Tags:        "example",
    			TargetTable: map[string]any{"database_name": "example-database-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataQualityRuleset } from "@ubx/sdk-aws/aws/glue/data_quality_ruleset";

    export default stack("example", () => {
      intent({ summary: "example own data quality ruleset" });

      resource(DataQualityRuleset, "example", {
        name: "example",
        clientToken: "example",
        description: "Managed by ubx.",
        ruleset: "example",
        tags: "example",
        targetTable: { databaseName: "example-database-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue.data_quality_ruleset import DataQualityRuleset, DataQualityRulesetConfig

    def describe():
        ubx.intent("example own data quality ruleset")

        ubx.resource(DataQualityRuleset, "example", DataQualityRulesetConfig(
            name="example",
            client_token="example",
            description="Managed by ubx.",
            ruleset="example",
            tags="example",
            target_table={"database_name": "example-database-name"},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_data_quality_ruleset called "example", with
    client token example, description Managed by ubx., ruleset
    example, tags example, target table a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="client_token" type="string">
  A unique token for idempotency. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the data quality ruleset. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  A unique name for the data quality ruleset. Required.
</ResponseField>

<ResponseField name="ruleset" type="string">
  A Data Quality Definition Language (DQDL) ruleset. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A map of key-value pairs to apply to this resource. Optional.
</ResponseField>

<ResponseField name="target_table" type="object">
  An object representing an AWS Glue table. Optional.
  <Expandable title="properties">
    <ResponseField name="database_name" type="string" required>
      The name of the database where the AWS Glue table exists. Required.
    </ResponseField>
    <ResponseField name="table_name" type="string" required>
      The name of the AWS Glue table. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
