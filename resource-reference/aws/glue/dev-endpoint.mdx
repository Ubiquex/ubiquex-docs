---
title: "aws_glue_dev_endpoint"
description: "Real, generated bindings for `aws_glue_dev_endpoint`."
---

`aws_glue_dev_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	glue "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dev endpoint"})

    		ubx.Resource(glue.DevEndpoint, "ci-runner", glue.DevEndpointConfig{
    			Name:                  "ci-runner",
    			RoleArn:               "arn:aws:iam::123456789012:policy/ci",
    			Arguments:             map[string]string{"managed-by": "ubx"},
    			ExtraJarsS3Path:       "example",
    			ExtraPythonLibsS3Path: "example",
    			GlueVersion:           "example",
    			NumberOfNodes:         1,
    			NumberOfWorkers:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DevEndpoint } from "jsr:@ubx/sdk-aws/aws/glue/dev_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own dev endpoint" });

      resource(DevEndpoint, "ci-runner", {
        name: "ci-runner",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        arguments: { "managed-by": "ubx" },
        extraJarsS3Path: "example",
        extraPythonLibsS3Path: "example",
        glueVersion: "example",
        numberOfNodes: 1,
        numberOfWorkers: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.glue import DevEndpoint, DevEndpointConfig

    def describe():
        ubx.intent("payments own dev endpoint")

        ubx.resource(DevEndpoint, "ci-runner", DevEndpointConfig(
            name="ci-runner",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            arguments={"managed-by": "ubx"},
            extra_jars_s3_path="example",
            extra_python_libs_s3_path="example",
            glue_version="example",
            number_of_nodes=1,
            number_of_workers=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_glue_dev_endpoint called "ci-runner", with role arn
    arn:aws:iam::123456789012:policy/ci, arguments a set value, extra
    jars s3 path example, extra python libs s3 path example, glue
    version example, number of nodes 1, number of workers 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="arguments" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="extra_jars_s3_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="extra_python_libs_s3_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="glue_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="number_of_nodes" type="number">
  Optional.
</ResponseField>

<ResponseField name="number_of_workers" type="number">
  Optional.
</ResponseField>

<ResponseField name="public_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="public_keys" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="security_configuration" type="string">
  Optional.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="worker_type" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="private_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="public_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="yarn_endpoint_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="zeppelin_remote_spark_interpreter_port" type="number">
  Computed by AWS after `ship`.
</ResponseField>
