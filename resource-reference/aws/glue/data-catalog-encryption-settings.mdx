---
title: "aws_glue_data_catalog_encryption_settings"
description: "Real, generated bindings for `aws_glue_data_catalog_encryption_settings`."
---

`aws_glue_data_catalog_encryption_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"

    cfg := glue.DataCatalogEncryptionSettingsConfig{
        CatalogId: "example",
    }
    ubx.Resource(glue.DataCatalogEncryptionSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { DataCatalogEncryptionSettings } from
      "jsr:@ubx/sdk-aws/aws/glue/data_catalog_encryption_settings";

    resource(DataCatalogEncryptionSettings, "example", {
      catalogId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import (
        DataCatalogEncryptionSettings,
        DataCatalogEncryptionSettingsConfig,
    )

    cfg = DataCatalogEncryptionSettingsConfig(
        catalog_id="example",
    )
    ubx.resource(DataCatalogEncryptionSettings, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_data_catalog_encryption_settings with
catalog_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
