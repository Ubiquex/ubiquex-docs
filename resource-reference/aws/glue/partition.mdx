---
title: "aws_glue_partition"
description: "Real, generated bindings for `aws_glue_partition`."
---

`aws_glue_partition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/glue"

    cfg := glue.PartitionConfig{
        DatabaseName: "example-database-name",
        PartitionValues: []string{"example"},
        TableName: "example-table-name",
        CatalogId: "example",
        Parameters: map[string]string{"key": "value"},
    }
    ubx.Resource(glue.Partition, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Partition } from "jsr:@ubx/sdk-aws/aws/glue/partition";

    resource(Partition, "example", {
      databaseName: "example",
      partitionValues: ["example"],
      tableName: "example",
      catalogId: "example",
      parameters: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.glue import Partition, PartitionConfig

    cfg = PartitionConfig(
        database_name="example",
        partition_values=["example"],
        table_name="example",
        catalog_id="example",
        parameters={"key": "value"},
    )
    ubx.resource(Partition, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_glue_partition with database_name,
partition_values, table_name, catalog_id, parameters.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="database_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="partition_values" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="catalog_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_accessed_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_analyzed_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
