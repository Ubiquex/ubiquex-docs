---
title: "aws_verifiedaccess_instance_logging_configuration"
description: "Real, generated bindings for `aws_verifiedaccess_instance_logging_configuration`."
---

`aws_verifiedaccess_instance_logging_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	verifiedaccess "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verifiedaccess"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance logging configuration"})

    		ubx.Resource(verifiedaccess.InstanceLoggingConfiguration, "ci-runner", verifiedaccess.InstanceLoggingConfigurationConfig{
    			VerifiedaccessInstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceLoggingConfiguration } from "jsr:@ubx/sdk-aws/aws/verifiedaccess/instance_logging_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own instance logging configuration" });

      resource(InstanceLoggingConfiguration, "ci-runner", {
        verifiedaccessInstanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.verifiedaccess import InstanceLoggingConfiguration, InstanceLoggingConfigurationConfig

    def describe():
        ubx.intent("payments own instance logging configuration")

        ubx.resource(InstanceLoggingConfiguration, "ci-runner", InstanceLoggingConfigurationConfig(
            verifiedaccess_instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_verifiedaccess_instance_logging_configuration, with
    verifiedaccess instance id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="verifiedaccess_instance_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
