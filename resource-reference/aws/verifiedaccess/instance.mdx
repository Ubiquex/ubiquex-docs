---
title: "aws_verifiedaccess_instance"
description: "Real, generated bindings for `aws_verifiedaccess_instance`."
---

`aws_verifiedaccess_instance` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	verifiedaccess "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verifiedaccess"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance"})

    		ubx.Resource(verifiedaccess.Instance, "ci-runner", verifiedaccess.InstanceConfig{
    			CidrEndpointsCustomSubdomain: "example",
    			Description:                  "Managed by ubx.",
    			FipsEnabled:                  true,
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Instance } from "jsr:@ubx/sdk-aws/aws/verifiedaccess/instance";

    export default stack("payments", () => {
      intent({ summary: "payments own instance" });

      resource(Instance, "ci-runner", {
        cidrEndpointsCustomSubdomain: "example",
        description: "Managed by ubx.",
        fipsEnabled: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.verifiedaccess import Instance, InstanceConfig

    def describe():
        ubx.intent("payments own instance")

        ubx.resource(Instance, "ci-runner", InstanceConfig(
            cidr_endpoints_custom_subdomain="example",
            description="Managed by ubx.",
            fips_enabled=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_verifiedaccess_instance, with cidr endpoints custom
    subdomain example, description Managed by ubx., fips enabled
    true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr_endpoints_custom_subdomain" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="fips_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="name_servers" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="verified_access_trust_providers" type="list(object)">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="device_trust_provider_type" type="string">
    </ResponseField>
    <ResponseField name="trust_provider_type" type="string">
    </ResponseField>
    <ResponseField name="user_trust_provider_type" type="string">
    </ResponseField>
    <ResponseField name="verified_access_trust_provider_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
