---
title: "aws_verifiedaccess_instance_trust_provider_attachment"
description: "Real, generated bindings for `aws_verifiedaccess_instance_trust_provider_attachment`."
---

`aws_verifiedaccess_instance_trust_provider_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	verifiedaccess "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verifiedaccess"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own instance trust provider attachment"})

    		ubx.Resource(verifiedaccess.InstanceTrustProviderAttachment, "ci-runner", verifiedaccess.InstanceTrustProviderAttachmentConfig{
    			VerifiedaccessInstanceId:      "example",
    			VerifiedaccessTrustProviderId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceTrustProviderAttachment } from "jsr:@ubx/sdk-aws/aws/verifiedaccess/instance_trust_provider_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own instance trust provider attachment" });

      resource(InstanceTrustProviderAttachment, "ci-runner", {
        verifiedaccessInstanceId: "example",
        verifiedaccessTrustProviderId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.verifiedaccess import InstanceTrustProviderAttachment, InstanceTrustProviderAttachmentConfig

    def describe():
        ubx.intent("payments own instance trust provider attachment")

        ubx.resource(InstanceTrustProviderAttachment, "ci-runner", InstanceTrustProviderAttachmentConfig(
            verifiedaccess_instance_id="example",
            verifiedaccess_trust_provider_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_verifiedaccess_instance_trust_provider_attachment,
    with verifiedaccess instance id example, verifiedaccess trust
    provider id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="verifiedaccess_instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="verifiedaccess_trust_provider_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
