---
title: "aws_sso_application_assignment"
description: "`aws_sso_application_assignment` grants a real user or group access to an `aws_sso_application`..."
---

`aws_sso_application_assignment` grants a real user or group access to an `aws_sso_application`, the resource that actually authorizes that identity to sign in to the customer-managed application through IAM Identity Center.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own application assignment"})

    		ubx.Resource(sso.ApplicationAssignment, "example", sso.ApplicationAssignmentConfig{
    			ApplicationArn: "arn:aws:iam::123456789012:policy/example",
    			PrincipalId:    "example",
    			PrincipalType:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApplicationAssignment } from "@ubx/sdk-aws/aws/sso/application_assignment";

    export default stack("example", () => {
      intent({ summary: "example own application assignment" });

      resource(ApplicationAssignment, "example", {
        applicationArn: "arn:aws:iam::123456789012:policy/example",
        principalId: "example",
        principalType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sso import ApplicationAssignment, ApplicationAssignmentConfig

    def describe():
        ubx.intent("example own application assignment")

        ubx.resource(ApplicationAssignment, "example", ApplicationAssignmentConfig(
            application_arn="arn:aws:iam::123456789012:policy/example",
            principal_id="example",
            principal_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sso_application_assignment, with application arn
    arn:aws:iam::123456789012:policy/example, principal id example,
    principal type example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="application_arn" type="string" required>
  The ARN of the application. Required.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  An identifier for an object in IAM Identity Center, such as a user or group Required.
</ResponseField>

<ResponseField name="principal_type" type="string" required>
  The entity type for which the assignment will be created. Required.
</ResponseField>
