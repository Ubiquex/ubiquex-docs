---
title: "aws_sso_instance_access_control_attribute_configuration"
description: "`aws_sso_instance_access_control_attribute_configuration` enables real attribute-based access control on an IAM Identity Center instance..."
---

`aws_sso_instance_access_control_attribute_configuration` enables real attribute-based access control on an IAM Identity Center instance, mapping identity attributes, department or cost-center, from the connected identity source into session tags that permission policies can reference.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sso "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/sso"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own instance access control attribute configuration"})

    		ubx.Resource(sso.InstanceAccessControlAttributeConfiguration, "example", sso.InstanceAccessControlAttributeConfigurationConfig{
    			InstanceArn:             "arn:aws:iam::123456789012:policy/example",
    			AccessControlAttributes: []map[string]any{{"key": "example"}},
    			InstanceAccessControlAttributeConfiguration: map[string]any{"access_control_attributes": []map[string]any{{"key": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InstanceAccessControlAttributeConfiguration } from "@ubx/sdk-aws/aws/sso/instance_access_control_attribute_configuration";

    export default stack("example", () => {
      intent({
        summary: "example own instance access control attribute configuration",
      });

      resource(InstanceAccessControlAttributeConfiguration, "example", {
        instanceArn: "arn:aws:iam::123456789012:policy/example",
        accessControlAttributes: [{ key: "example" }],
        instanceAccessControlAttributeConfiguration: {
          accessControlAttributes: [{ key: "example" }],
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sso.instance_access_control_attribute_configuration import InstanceAccessControlAttributeConfiguration, InstanceAccessControlAttributeConfigurationConfig

    def describe():
        ubx.intent("example own instance access control attribute configuration")

        ubx.resource(InstanceAccessControlAttributeConfiguration, "example", InstanceAccessControlAttributeConfigurationConfig(
            instance_arn="arn:aws:iam::123456789012:policy/example",
            access_control_attributes=[{"key": "example"}],
            instance_access_control_attribute_configuration={"access_control_attributes": [{"key": "example"}]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sso_instance_access_control_attribute_configuration,
    with instance arn arn:aws:iam::123456789012:policy/example,
    access control attributes a set value, instance access control
    attribute configuration a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="access_control_attributes" type="list(object)">
  The list of access control attributes that define the user attributes (such as email or department) used in attribute-based access control (ABAC) rules for the AWS SSO (IAM Identity Center) instance. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the name of the user attribute (such as email or department) that this access control attribute entry defines for use in attribute-based access control (ABAC) in IAM Identity Center. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="object">
      Represents the list of values for an access control attribute key, which AWS IAM Identity Center uses to enable attribute-based access control (ABAC) for the instance. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="source" type="list(string)">
          For a given access control attribute in this AWS SSO (IAM Identity Center) instance's ABAC configuration, the `source` field lists the identity provider attribute names (e.g., 'email') that are mapped to the attribute key and supply the values used for attribute-based access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_access_control_attribute_configuration" type="object">
  The InstanceAccessControlAttributeConfiguration property has been deprecated but is still supported for backwards compatibility purposes. We recomend that you use AccessControlAttributes property instead. Optional.
  <Expandable title="properties">
    <ResponseField name="access_control_attributes" type="list(object)" required>
      Defines the list of user attribute key-value pairs (such as email or department) that AWS SSO uses for attribute-based access control (ABAC) in the instance. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          The attribute key (name) of an access control attribute that maps to a user identity attribute (such as `email` or `username`) used by IAM Identity Center for permission management. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value" type="object">
          Holds the source configuration for the access control attribute value, where you provide one or more source strings (e.g., '$$&#123;path:user.email&#125;') that define how the attribute value is resolved from your identity source for permission boundaries. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="source" type="list(string)">
              The list of identity source attribute paths (for example, 'user.email') that define the source values for this access control attribute. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_arn" type="string" required>
  The ARN of the AWS SSO instance under which the operation will be executed. Required.
</ResponseField>
