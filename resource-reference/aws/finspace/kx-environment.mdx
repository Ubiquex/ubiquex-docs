---
title: "aws_finspace_kx_environment"
description: "Real, generated bindings for `aws_finspace_kx_environment`."
---

`aws_finspace_kx_environment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/finspace"

    cfg := finspace.KxEnvironmentConfig{
        KmsKeyId: "example",
        Name: "example-name",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(finspace.KxEnvironment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KxEnvironment } from
      "jsr:@ubx/sdk-aws/aws/finspace/kx_environment";

    resource(KxEnvironment, "example", {
      kmsKeyId: "example",
      name: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.finspace import KxEnvironment, KxEnvironmentConfig

    cfg = KxEnvironmentConfig(
        kms_key_id="example",
        name="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(KxEnvironment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_finspace_kx_environment with kms_key_id, name,
description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zones" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="infrastructure_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
