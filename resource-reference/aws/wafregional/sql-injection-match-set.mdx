---
title: "aws_wafregional_sql_injection_match_set"
description: "`aws_wafregional_sql_injection_match_set` defines a real condition, matching SQL-injection-like patterns, that an `aws_wafregional_rule` references."
---

Support for AWS WAF Classic ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafregional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafregional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sql injection match set"})

    		ubx.Resource(wafregional.SqlInjectionMatchSet, "ci-runner", wafregional.SqlInjectionMatchSetConfig{
    			Name:                    "ci-runner",
    			SqlInjectionMatchTuples: []map[string]any{{"field_to_match": map[string]any{"data": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SqlInjectionMatchSet } from "./local-sdk/aws/sdk/typescript/aws/wafregional/sql_injection_match_set.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sql injection match set" });

      resource(SqlInjectionMatchSet, "ci-runner", {
        name: "ci-runner",
        sqlInjectionMatchTuples: [{ fieldToMatch: { data: "example" } }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafregional import SqlInjectionMatchSet, SqlInjectionMatchSetConfig

    def describe():
        ubx.intent("payments own sql injection match set")

        ubx.resource(SqlInjectionMatchSet, "ci-runner", SqlInjectionMatchSetConfig(
            name="ci-runner",
            sql_injection_match_tuples=[{"field_to_match": {"data": "example"}}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafregional_sql_injection_match_set called
    "ci-runner", with sql injection match tuples a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  The required friendly name of the SQL injection match set, used to identify it within AWS WAF Regional. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="sql_injection_match_tuples" type="list(object)">
  Specifies a list of SQL injection match tuples, each containing a field to match (such as URI or query string) and a text transformation, that together determine which requests are flagged for SQL injection in this AWS WAF Regional match set. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="field_to_match" type="object">
      Identifies the specific portion of the web request (e.g., query string, header, body, or method) that the SQL injection match tuple will inspect for malicious patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="data" type="string">
          The name of the HTTP header to inspect when the field_to_match type is HEADER, as used in the SQL injection match tuple. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the part of the web request to inspect for SQL injection patterns, such as HEADER, QUERY_STRING, BODY, or URI. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="text_transformation" type="string">
      Specifies how the web request component (e.g., the query string or header) is transformed before WAF inspects it for SQL injection patterns, such as 'NONE', 'URL_DECODE', or 'BASE64_DECODE'. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique AWS-assigned identifier for this SQL injection match set. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
