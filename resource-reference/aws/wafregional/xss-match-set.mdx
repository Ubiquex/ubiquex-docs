---
title: "aws_wafregional_xss_match_set"
description: "`aws_wafregional_xss_match_set` defines a real condition, matching cross-site-scripting-like patterns, that an `aws_wafregional_rule` references."
---

Support for AWS WAF Classic ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafregional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafregional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own xss match set"})

    		ubx.Resource(wafregional.XssMatchSet, "example", wafregional.XssMatchSetConfig{
    			Name:           "example",
    			XssMatchTuples: []map[string]any{{"field_to_match": map[string]any{"data": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { XssMatchSet } from "./local-sdk/aws/sdk/typescript/aws/wafregional/xss_match_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own xss match set" });

      resource(XssMatchSet, "example", {
        name: "example",
        xssMatchTuples: [{ fieldToMatch: { data: "example" } }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafregional import XssMatchSet, XssMatchSetConfig

    def describe():
        ubx.intent("example own xss match set")

        ubx.resource(XssMatchSet, "example", XssMatchSetConfig(
            name="example",
            xss_match_tuples=[{"field_to_match": {"data": "example"}}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafregional_xss_match_set called "example", with xss
    match tuples a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  The friendly name or description of the XSS match set, required to create the resource in AWS WAF Regional. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="xss_match_tuples" type="list(object)">
  Specifies the parts of web requests to inspect for cross-site scripting (XSS) attacks, each tuple defining a field to match and a text transformation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="field_to_match" type="object">
      Specifies the part of a web request (such as a header, query string, or body) that AWS WAF Regional inspects for cross-site scripting attack patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="data" type="string">
          The value of the `field_to_match` that defines the specific part of the web request to match, such as the header name when `type` is `HEADER`; it is empty for types like `URI` or `QUERY_STRING`. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the part of the web request (for example, QUERY_STRING, URI, or BODY) that AWS WAF Regional inspects for cross-site scripting (XSS) attack patterns. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="text_transformation" type="string">
      Specifies how the text in the web request component is transformed (e.g., URL_DECODE, LOWERCASE) before the XSS match rule is evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier assigned by AWS to the WAF Regional XSS match set. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
