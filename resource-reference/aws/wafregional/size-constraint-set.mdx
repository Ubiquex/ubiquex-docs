---
title: "aws_wafregional_size_constraint_set"
description: "`aws_wafregional_size_constraint_set` defines a real condition, matching a request part's size against a threshold..."
---

`aws_wafregional_size_constraint_set` defines a real condition, matching a request part's size against a threshold, that an `aws_wafregional_rule` references. Support for AWS WAF Classic ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafregional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafregional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own size constraint set"})

    		ubx.Resource(wafregional.SizeConstraintSet, "example", wafregional.SizeConstraintSetConfig{
    			Name:            "example",
    			SizeConstraints: []map[string]any{{"comparison_operator": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SizeConstraintSet } from "./local-sdk/aws/sdk/typescript/aws/wafregional/size_constraint_set.ts";

    export default stack("example", () => {
      intent({ summary: "example own size constraint set" });

      resource(SizeConstraintSet, "example", {
        name: "example",
        sizeConstraints: [{ comparisonOperator: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafregional import SizeConstraintSet, SizeConstraintSetConfig

    def describe():
        ubx.intent("example own size constraint set")

        ubx.resource(SizeConstraintSet, "example", SizeConstraintSetConfig(
            name="example",
            size_constraints=[{"comparison_operator": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafregional_size_constraint_set called "example", with
    size constraints a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  A friendly name used to identify the size constraint set in AWS WAF Regional, which must be unique within the account. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="size_constraints" type="list(object)">
  Defines the size constraints that the set enforces, where each constraint specifies a part of the request (such as a header or body), a comparison operator, a size in bytes, and an optional text transformation. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="comparison_operator" type="string">
      Specifies the comparison operator (e.g., EQ, NE, LT, LE, GT, GE) used to compare the size of the request part against the configured threshold for this size constraint. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="field_to_match" type="object">
      Specifies the part of a web request that the size constraint inspects, such as a header, query string, body, or URI. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="data" type="string">
          The name of the HTTP header to match when the field_to_match type is HEADER; it is ignored for all other types. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          Determines which part of the web request (e.g., URI, query string, header, method, or body) the size constraint applies to, as specified by the WAF FieldToMatch type. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="size" type="number">
      Specifies the size (in bytes) of the web request component (such as a query string, header, or body) that AWS WAF Regional inspects, which is compared against the defined comparison operator for this size constraint. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="text_transformation" type="string">
      Specifies the text transformation to apply to the web request before evaluating the size constraint, such as NONE, CMD_LINE, HTML_ENTITY_DECODE, LOWERCASE, or URL_DECODE. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique AWS-assigned identifier for the WAF Regional Size Constraint Set, used to reference and manage the resource in Terraform. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
