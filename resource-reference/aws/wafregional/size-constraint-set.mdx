---
title: "aws_wafregional_size_constraint_set"
description: "Real, generated bindings for `aws_wafregional_size_constraint_set`."
---

`aws_wafregional_size_constraint_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafregional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafregional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own size constraint set"})

    		ubx.Resource(wafregional.SizeConstraintSet, "ci-runner", wafregional.SizeConstraintSetConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SizeConstraintSet } from "jsr:@ubx/sdk-aws/aws/wafregional/size_constraint_set";

    export default stack("payments", () => {
      intent({ summary: "payments own size constraint set" });

      resource(SizeConstraintSet, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.wafregional import SizeConstraintSet, SizeConstraintSetConfig

    def describe():
        ubx.intent("payments own size constraint set")

        ubx.resource(SizeConstraintSet, "ci-runner", SizeConstraintSetConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafregional_size_constraint_set called "ci-runner",
    with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
