---
title: "aws_wafregional_regex_pattern_set"
description: "`aws_wafregional_regex_pattern_set` defines a real set of regular expressions that an `aws_wafregional_rule` references as a match condition."
---

Support for AWS WAF Classic ended September 30, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	wafregional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafregional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own regex pattern set"})

    		ubx.Resource(wafregional.RegexPatternSet, "ci-runner", wafregional.RegexPatternSetConfig{
    			Name:                "ci-runner",
    			RegexPatternStrings: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegexPatternSet } from "./local-sdk/aws/sdk/typescript/aws/wafregional/regex_pattern_set.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own regex pattern set" });

      resource(RegexPatternSet, "ci-runner", {
        name: "ci-runner",
        regexPatternStrings: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.wafregional import RegexPatternSet, RegexPatternSetConfig

    def describe():
        ubx.intent("payments own regex pattern set")

        ubx.resource(RegexPatternSet, "ci-runner", RegexPatternSetConfig(
            name="ci-runner",
            regex_pattern_strings=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafregional_regex_pattern_set called "ci-runner", with
    regex pattern strings a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string" required>
  A unique, user-defined name for the regex pattern set, used to identify it in AWS WAF Regional. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="regex_pattern_strings" type="list(string)" required>
  Specifies the regular expression patterns to include in this set, where each string is a distinct pattern that AWS WAF Regional uses to match against web requests. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique identifier automatically assigned to this WAF Regional regex pattern set, used to reference it in other resources such as rules and WebACLs. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
