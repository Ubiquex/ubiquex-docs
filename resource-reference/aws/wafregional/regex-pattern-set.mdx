---
title: "aws_wafregional_regex_pattern_set"
description: "Real, generated bindings for `aws_wafregional_regex_pattern_set`."
---

`aws_wafregional_regex_pattern_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafregional "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafregional"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own regex pattern set"})

    		ubx.Resource(wafregional.RegexPatternSet, "ci-runner", wafregional.RegexPatternSetConfig{
    			Name:                "ci-runner",
    			RegexPatternStrings: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegexPatternSet } from "jsr:@ubx/sdk-aws/aws/wafregional/regex_pattern_set";

    export default stack("payments", () => {
      intent({ summary: "payments own regex pattern set" });

      resource(RegexPatternSet, "ci-runner", {
        name: "ci-runner",
        regexPatternStrings: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.wafregional import RegexPatternSet, RegexPatternSetConfig

    def describe():
        ubx.intent("payments own regex pattern set")

        ubx.resource(RegexPatternSet, "ci-runner", RegexPatternSetConfig(
            name="ci-runner",
            regex_pattern_strings=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafregional_regex_pattern_set called "ci-runner", with
    regex pattern strings a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="regex_pattern_strings" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
