---
title: "aws_open_search_service_domain"
description: "`aws_open_search_service_domain` creates a real, managed OpenSearch cluster, its data nodes, dedicated master nodes, and storage..."
---

`aws_open_search_service_domain` creates a real, managed OpenSearch cluster, its data nodes, dedicated master nodes, and storage, OpenSearch Service's original, cluster-provisioned resource, distinct from the auto-scaling `aws_open_search_serverless_collection`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	open "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/open"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own search service domain"})

    		ubx.Resource(open.SearchServiceDomain, "ci-runner", open.SearchServiceDomainConfig{
    			AccessPolicies:                "example",
    			AdvancedOptions:               "example",
    			Aimloptions:                   map[string]any{"s3_vectors_engine": map[string]any{"enabled": true}},
    			AutomatedSnapshotPauseOptions: map[string]any{"enabled": true},
    			ClusterConfig:                 map[string]any{"cold_storage_options": map[string]any{"enabled": true}},
    			CognitoOptions:                map[string]any{"enabled": true},
    			DeploymentStrategyOptions:     map[string]any{"deployment_strategy": "example"},
    			DomainEndpointOptions:         map[string]any{"custom_endpoint": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SearchServiceDomain } from "./local-sdk/aws/sdk/typescript/aws/open/search_service_domain.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own search service domain" });

      resource(SearchServiceDomain, "ci-runner", {
        accessPolicies: "example",
        advancedOptions: "example",
        aimloptions: { s3VectorsEngine: { enabled: true } },
        automatedSnapshotPauseOptions: { enabled: true },
        clusterConfig: { coldStorageOptions: { enabled: true } },
        cognitoOptions: { enabled: true },
        deploymentStrategyOptions: { deploymentStrategy: "example" },
        domainEndpointOptions: { customEndpoint: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.open import SearchServiceDomain, SearchServiceDomainConfig

    def describe():
        ubx.intent("payments own search service domain")

        ubx.resource(SearchServiceDomain, "ci-runner", SearchServiceDomainConfig(
            access_policies="example",
            advanced_options="example",
            aimloptions={"s3_vectors_engine": {"enabled": True}},
            automated_snapshot_pause_options={"enabled": True},
            cluster_config={"cold_storage_options": {"enabled": True}},
            cognito_options={"enabled": True},
            deployment_strategy_options={"deployment_strategy": "example"},
            domain_endpoint_options={"custom_endpoint": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_open_search_service_domain, with access policies
    example, advanced options example, aimloptions a set value,
    automated snapshot pause options a set value, cluster config a
    set value, cognito options a set value, deployment strategy
    options a set value, domain endpoint options a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_policies" type="any">
  IAM policy document (JSON) that defines the resource-based access policy for the OpenSearch domain, specifying which IAM principals and actions are allowed on the domain's APIs and endpoints. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="advanced_options" type="any">
  Configures advanced OpenSearch cluster options as key-value pairs, such as allowing explicit index names (rest.action.multi.allow_explicit_index) or setting the maximum number of query clauses (indices.query.bool.max_clause_count). Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="aimloptions" type="object">
  Configures AI/ML features for the Amazon OpenSearch Service domain, such as natural language query generation and integration with IAM Identity Center. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="s3_vectors_engine" type="object">
      Configures the S3 vector engine for the OpenSearch Service domain, which specifies the S3 bucket and IAM role used to store and query vector embeddings for vector search. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool" required>
          Whether to enable S3 vectors engine. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="serverless_vector_acceleration" type="object">
      Configuration for serverless vector acceleration on the OpenSearch Service domain, used to enable or disable accelerated vector search for serverless engine workloads. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether to enable serverless vector acceleration. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="automated_snapshot_pause_options" type="object">
  Configures a recurring time window during which automated snapshots are paused for the OpenSearch Service domain, preventing snapshot creation during periods of high activity or maintenance. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
      Enables or disables the automated snapshot pause feature, allowing Amazon OpenSearch Service to temporarily pause automated snapshots during high traffic or maintenance windows to reduce impact on cluster performance. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="end_time" type="string">
      The UTC time of day at which the automated snapshot pause window ends, after which Amazon OpenSearch Service resumes automated snapshot creation for the domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="start_time" type="string">
      Specifies the start time (in UTC, in HH:mm format) of the daily window during which automated snapshots are paused for the OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cluster_config" type="object">
  The cluster_config object configures the OpenSearch cluster's compute and sizing settings, including instance type, instance count, dedicated master nodes, and zone awareness for the domain. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cold_storage_options" type="object">
      Options for managing cold storage in the OpenSearch cluster, where the `Enabled` flag determines whether cold storage is turned on for the domain. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Enables or disables cold storage for the OpenSearch Service domain, allowing infrequently accessed data to be stored on lower-cost storage while remaining searchable via the cluster. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dedicated_master_count" type="number">
      Number of dedicated master nodes to deploy for the OpenSearch Service domain cluster, used when dedicated_master_enabled is true. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dedicated_master_enabled" type="bool">
      Specifies whether the OpenSearch Service domain is configured to use dedicated master nodes, which provide more stable cluster management for production workloads. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dedicated_master_type" type="string">
      Specifies the instance type for the dedicated master nodes of the OpenSearch cluster, typically a value like m5.large.search, and is required when dedicated_master_enabled is set to true. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_count" type="number">
      The number of data nodes (instances) that form the OpenSearch Service domain's cluster. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_type" type="string">
      Defines the Amazon OpenSearch Service instance type for the data nodes in the cluster, such as 'm5.large.search' or 'c5.large.search'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="multi_azwith_standby_enabled" type="bool">
      Enables or disables Multi-AZ with standby for the OpenSearch Service domain, which deploys the cluster across three Availability Zones (two active data nodes and one standby) to provide higher availability. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_options" type="list(object)">
      Configures the list of node options for the OpenSearch domain cluster, allowing you to specify dedicated coordinator nodes and their counts to optimize cluster performance and stability. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="node_config" type="object">
          Configuration for a node option in the OpenSearch domain cluster, specifying the node count and whether the option is enabled for the selected node type. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="count" type="number">
              Specifies the number of nodes of the configured node type (such as coordinator nodes) to provision for the OpenSearch domain's cluster, as defined within the node option's node configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              This boolean determines whether the specified node configuration (for example, dedicated coordinator nodes) is enabled for the OpenSearch Service domain cluster. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="node_type" type="string">
          Specifies the type of node (e.g., 'data', 'master', 'ultrawarm', or 'cold') for which the node option is configured within the OpenSearch domain's cluster configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="warm_count" type="number">
      Specifies the number of UltraWarm nodes for the OpenSearch domain, used to enable warm storage tiers in the cluster. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="warm_enabled" type="bool">
      Indicates whether UltraWarm, a low-cost storage tier for large amounts of read-only data in Amazon OpenSearch Service, is enabled for the domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="warm_type" type="string">
      Specifies the instance type for UltraWarm nodes in the OpenSearch domain's cluster configuration, enabling the UltraWarm storage tier for warmer, cheaper storage of read-only indices. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="zone_awareness_config" type="object">
      Specifies the number of Availability Zones for the domain when zone awareness is enabled, used to configure multi-AZ deployment. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="availability_zone_count" type="number">
          Specifies the number of Availability Zones (typically 2 or 3) that the OpenSearch Service domain's cluster spans when zone awareness is enabled, controlling distribution of data nodes across zones for high availability. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zone_awareness_enabled" type="bool">
      Indicates whether to enable zone awareness for the OpenSearch domain cluster, which distributes nodes across multiple Availability Zones to improve availability and fault tolerance. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="cognito_options" type="object">
  Cognito options for OpenSearch Dashboards authentication, specifying whether Cognito authentication is enabled and the associated Cognito user pool, identity pool, and IAM role used for Dashboards login. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Indicates whether authentication through Amazon Cognito is enabled for OpenSearch Dashboards (formerly Kibana) on the OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="identity_pool_id" type="string">
      The ID of the Cognito identity pool used to authenticate users to OpenSearch Dashboards. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="role_arn" type="string">
      The ARN of the IAM role that OpenSearch Service assumes to access Amazon Cognito for authentication, which is required when Cognito options are enabled on the domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_pool_id" type="string">
      The ID of the Amazon Cognito user pool used for authentication to OpenSearch Dashboards when CognitoOptions is enabled. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deployment_strategy_options" type="object">
  Specifies the deployment strategy for the OpenSearch domain, including the deployment mode (e.g., BLUE_GREEN) that controls how updates and changes are applied to the domain during configuration changes. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="deployment_strategy" type="string">
      Defines whether the OpenSearch Service domain uses a blue/green deployment (creating a new environment and swapping traffic) or an in-place deployment (applying changes directly to the existing cluster) when configuration changes are made. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_endpoint_options" type="object">
  Configures endpoint access for the domain, including whether HTTPS is enforced, the TLS security policy version, and optional custom endpoint settings such as a custom hostname and certificate. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="custom_endpoint" type="string">
      Specifies the fully qualified custom domain name (e.g., search.example.com) used as the domain's endpoint when custom endpoint is enabled. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="custom_endpoint_certificate_arn" type="string">
      The ARN of the AWS Certificate Manager (ACM) certificate that secures the custom endpoint of the OpenSearch Service domain when a custom domain endpoint is enabled. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="custom_endpoint_enabled" type="bool">
      Indicates whether a custom endpoint is enabled for the OpenSearch Service domain, allowing you to use a custom domain name instead of the default endpoint. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enforce_https" type="bool">
      Indicates whether the OpenSearch Service domain requires all traffic to use HTTPS, disabling HTTP access. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tlssecurity_policy" type="string">
      Sets the minimum TLS version for the domain's HTTPS endpoint, with valid values of 'Policy-Min-TLS-1-0-2019-07' (default, allowing TLS 1.0 and above) or 'Policy-Min-TLS-1-2-2019-07' (enforcing TLS 1.2 or higher). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="domain_name" type="string">
  A unique, user-friendly name for the OpenSearch domain, which appears in the domain's endpoint URL (e.g., https://&lt;domain-name&gt;.region.es.amazonaws.com) and must be unique within the AWS account and region. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ebsoptions" type="object">
  Configures the EBS volumes attached to the data nodes of the OpenSearch domain, including volume type, size, and IOPS settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="ebsenabled" type="bool">
      Whether EBS (Elastic Block Store) storage is enabled for the data nodes of the OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="iops" type="number">
      The provisioned IOPS (I/O operations per second) for the EBS volumes attached to the OpenSearch domain, used when the volume type is set to io1, io2, or gp3. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="throughput" type="number">
      Sets the provisioned throughput (in MiB/s) for the EBS volumes attached to the OpenSearch Service domain, used when the volume type supports specifying throughput such as gp3. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="volume_size" type="number">
      The size in GiB of the EBS volume attached to each data node for the OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="volume_type" type="string">
      Specifies the EBS volume type (e.g., gp2, gp3, io1) used for the OpenSearch Service domain's EBS storage volumes. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="encryption_at_rest_options" type="object">
  Configures encryption at rest for the OpenSearch Service domain, specifying whether it is enabled and the AWS KMS key ID used to encrypt the domain's data. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Whether encryption of data at rest is enabled for the OpenSearch Service domain, with a custom KMS key configurable via the KmsKeyId property in the same EncryptionAtRestOptions object. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kms_key_id" type="string">
      The KMS key ID used for encryption-at-rest of the OpenSearch Service domain, which must be a symmetric customer master key in the same region as the domain. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="engine_mode" type="string">
  The engine mode of the domain. Determines whether the domain runs the standard (GENERAL) engine or the optimized multi-engine (OPTIMIZED) engine. This value cannot be changed after the domain is created. Optional.
</ResponseField>

<ResponseField name="engine_version" type="string">
  The version of the OpenSearch or Elasticsearch engine that the domain runs, such as 'OpenSearch_1.0' or 'Elasticsearch_7.10'. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ipaddress_type" type="string">
  Specifies whether the OpenSearch Service domain uses IPv4 only or dual-stack networking (IPv4 and IPv6), controlling the IP address type assigned to the domain endpoints. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="log_publishing_options" type="any">
  Configures the publishing of OpenSearch Service domain logs (such as index slow logs, search slow logs, and audit logs) to a CloudWatch Logs group. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="node_to_node_encryption_options" type="object">
  Specifies settings for encrypting traffic between nodes in the Amazon OpenSearch Service domain, with an Enabled boolean that must be true to activate node-to-node encryption. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Whether node-to-node encryption is enabled for all communications between nodes in the OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="off_peak_window_options" type="object">
  Configures a daily off-peak window during which the OpenSearch Service domain can perform maintenance activities such as software updates, helping minimize impact on active traffic. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Indicates whether the off-peak maintenance window is enabled for the OpenSearch Service domain, allowing software updates to occur during a scheduled low-traffic period. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="off_peak_window" type="object">
      Specifies the daily off-peak maintenance window for the OpenSearch Service domain by defining the start time (hours and minutes) for when maintenance tasks can be performed. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="window_start_time" type="object">
          Defines the starting hour and minute (UTC) of the daily off-peak maintenance window for the OpenSearch Service domain. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="hours" type="number" required>
              The hour of the day (0-23) at which the off-peak maintenance window starts for the OpenSearch Service domain. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="minutes" type="number" required>
              The minute (0-59) of the hour at which the daily off-peak window starts for maintenance activities on the OpenSearch Service domain. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="skip_shard_migration_wait" type="bool">
  Set this to true to make Terraform skip waiting for shard migration to complete when reducing the number of data nodes in the OpenSearch domain, allowing the cluster configuration update to finish faster. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="snapshot_options" type="object">
  Configures the automated snapshot start hour (0-23) for the OpenSearch Service domain, controlling when daily snapshots are taken. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="automated_snapshot_start_hour" type="number">
      The hour in UTC at which the OpenSearch Service domain's automated daily snapshots are taken, specified as an integer from 0 to 23. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="software_update_options" type="object">
  Controls whether Amazon OpenSearch Service can automatically apply software updates to the domain, including the option to enable or disable automatic updates. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="auto_software_update_enabled" type="bool">
      Enables or disables automated software updates (patching) for the OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_latest_service_software_for_blue_green" type="bool">
      This boolean field enables or disables automatic blue/green deployment to update the OpenSearch Service domain to the latest service software. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An arbitrary set of tags (key-value pairs) for this Domain. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair attached to the Amazon OpenSearch Service domain, used for metadata organization, cost allocation, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="use_case" type="string">
  The primary use case of the domain. Determines the default configuration tuned for the workload. For GENERAL engine-mode domains, this value can be changed after creation. For OPTIMIZED engine-mode domains, this value cannot be changed after creation. Optional.
</ResponseField>

<ResponseField name="vpcoptions" type="object">
  Configures the Amazon Virtual Private Cloud (VPC) options for the OpenSearch Service domain, specifying the subnets and security groups in which the domain's endpoints will be placed. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="egress_enabled" type="bool">
      Controls whether egress traffic from the domain is routed through the customer VPC. Optional.
    </ResponseField>
    <ResponseField name="security_group_ids" type="list(string)">
      The list of security group IDs to associate with the VPC endpoint for the OpenSearch Service domain, controlling network access to the domain within the VPC. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnet_ids" type="list(string)">
      Specifies the subnet IDs within a VPC where the OpenSearch domain's VPC endpoints (for the data nodes) are placed, used when the domain is deployed inside a virtual private cloud. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="advanced_security_options" type="object">
  The advanced security options applied to the OpenSearch Service domain, including whether fine-grained access control is enabled, the internal user database setting, and the master user or SAML authentication configuration. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="anonymous_auth_disable_date" type="string">
      The date and time (in ISO 8601 format) after which anonymous access to the OpenSearch Service domain is automatically disabled, used when temporary anonymous authentication has been enabled. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="anonymous_auth_enabled" type="bool">
      Enables or disables anonymous authentication, allowing unauthenticated users to access the OpenSearch domain without credentials. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Enables fine-grained access control (advanced security) for the Amazon OpenSearch Service domain, requiring authentication and authorization for access. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="iamfederation_options" type="object">
      Specifies the IAM federation configuration for the OpenSearch Service domain, allowing IAM identities to authenticate to OpenSearch Dashboards through an external identity provider instead of using the internal user database. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Indicates whether IAM federation is enabled for the OpenSearch domain's advanced security options, allowing the domain to use IAM roles for authentication instead of internal users. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="roles_key" type="string">
          The SAML response attribute that contains the IAM role mappings used to grant federated users access to the OpenSearch Service domain. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subject_key" type="string">
          Specifies the source attribute in the IAM Identity Center (or IdP assertion) that is used to derive the OpenSearch username for federated users. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="internal_user_database_enabled" type="bool">
      Indicates whether the internal user database is enabled for fine-grained access control in the Amazon OpenSearch Service domain. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="jwtoptions" type="object">
      Configures JWT (JSON Web Token) authentication and authorization for the OpenSearch domain's advanced security, including enabling JWT and specifying the subject and roles claim keys for token validation. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether JWT-based authentication is enabled for the OpenSearch domain's advanced security options. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="public_key" type="string">
          The public key used to verify JWT tokens for JWT authentication within the OpenSearch domain's advanced security settings. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="roles_key" type="string">
          Specifies the JWT payload key whose value contains the user's roles, which OpenSearch Service uses to assign backend roles to the authenticated user. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subject_key" type="string">
          The JWT claim key (e.g., 'sub') that contains the user's subject identifier, used to authenticate and map the token to an OpenSearch user for access control. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="master_user_options" type="object">
      Specifies the master user (the administrative user) for the OpenSearch Service domain when fine-grained access control is enabled, allowing you to provide either an IAM role ARN (MasterUserARN) or a username and password (MasterUserName/MasterUserPassword). Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="master_user_arn" type="string">
          Specifies the ARN of the IAM user or role that acts as the master user for the OpenSearch domain's fine-grained access control. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="master_user_name" type="string">
          The master user name for the OpenSearch domain's internal user database, used when configuring fine-grained access control with the internal user database as the master user. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="master_user_password" type="string">
          The master user password for the OpenSearch Service domain's fine-grained access control, used in combination with the master user name to authenticate the primary administrative user. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="samloptions" type="object">
      Specifies the SAML (Security Assertion Markup Language) options for the domain's advanced security, enabling SAML-based single sign-on for OpenSearch Dashboards with settings such as identity provider metadata, subject key, and roles key. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether SAML authentication is enabled for the OpenSearch domain. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="idp" type="object">
          Configuration for the SAML identity provider, including the entity ID and the IdP metadata XML document, used to enable SAML authentication for the OpenSearch domain. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="entity_id" type="string" required>
              The unique identifier (entity ID) of the SAML identity provider (IdP) that OpenSearch Service uses for SAML-based authentication, typically the issuer URL from the IdP's SAML metadata. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metadata_content" type="string" required>
              The metadata content (XML) of the SAML identity provider used for configuring SAML authentication on the OpenSearch Service domain. Required. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="master_backend_role" type="string">
          Specifies the IAM role ARN to use as the master backend role (the master user) for the OpenSearch Service domain when SAML authentication is enabled, granting that role full access to the cluster. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="master_user_name" type="string">
          The SAML username of the master user who receives full access to the OpenSearch domain when SAML authentication is enabled. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="roles_key" type="string">
          Specifies the SAML attribute name that contains the user's roles, which are mapped to OpenSearch backend roles in the advanced security options. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="session_timeout_minutes" type="number">
          Specifies the SAML session timeout in minutes, after which an authenticated user's OpenSearch Dashboards session expires. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subject_key" type="string">
          Specifies the SAML assertion attribute (such as NameID) that maps to the user's subject identifier for authentication when SAML is enabled for the OpenSearch domain. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the OpenSearch domain, uniquely identifying the domain across AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="domain_arn" type="string">
  The Amazon Resource Name (ARN) of the OpenSearch Service domain, uniquely identifying it across AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="domain_endpoint" type="string">
  The fully qualified domain endpoint URL (e.g., https://search-&lt;domain-name&gt;-&lt;random-string&gt;.&lt;region&gt;.es.amazonaws.com) that is automatically assigned to the OpenSearch domain and can be used to access the search service. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="domain_endpoint_v2" type="string">
  This computed field contains the V2 domain endpoint (a URL) for the OpenSearch Service domain, which clients use to access the domain's search and indexing APIs. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="domain_endpoints" type="any">
  A computed map of the OpenSearch Service domain's endpoint URLs, with keys such as 'vpc' for the VPC endpoint (when deployed in a VPC) and 'public' for the public endpoint. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The name of the OpenSearch domain, which serves as the unique identifier for the domain resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="identity_center_options" type="object">
  Options for configuring Identity Center Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="enabled_apiaccess" type="bool">
      Whether Identity Center is enabled. Optional.
    </ResponseField>
    <ResponseField name="identity_center_application_arn" type="string">
      The ARN of the Identity Center application. Optional.
    </ResponseField>
    <ResponseField name="identity_center_instance_arn" type="string">
      The ARN of the Identity Center instance. Optional.
    </ResponseField>
    <ResponseField name="identity_store_id" type="string">
      The IdentityStoreId for Identity Center options. Optional.
    </ResponseField>
    <ResponseField name="roles_key" type="string">
      Roles Key Idc type values. Optional.
    </ResponseField>
    <ResponseField name="subject_key" type="string">
      Subject Key Idc type values. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="service_software_options" type="object">
  Provides the current service software version for the OpenSearch domain, including whether an update is available, the update status, and the scheduled automated update date. Computed by AWS after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="automated_update_date" type="string">
      The timestamp (ISO 8601 string) at which an available service software update will be automatically applied to the OpenSearch Service domain, as scheduled by AWS. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="cancellable" type="bool">
      A boolean that indicates whether the currently running service software update on the OpenSearch Service domain can be cancelled. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="current_version" type="string">
      The current_version field indicates the version of the OpenSearch service software currently installed on the domain, which is populated by the AWS API and cannot be set by the user. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      Contains a human-readable description of the current or latest service software update for the OpenSearch domain, as reported by the AWS service. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="new_version" type="string">
      The version of the OpenSearch service software that the domain can be updated to, when an update is available. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="optional_deployment" type="bool">
      A boolean that indicates whether the deployment of the pending service software update is optional, allowing you to defer or manually schedule the update instead of having it automatically applied. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="update_available" type="bool">
      Indicates whether a new service software update is available for the OpenSearch domain, as reported by the AWS OpenSearch Service. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="update_status" type="string">
      Indicates the status of a service software update for the OpenSearch Service domain, including states such as PENDING_UPDATE, IN_PROGRESS, COMPLETED, or NOT_AVAILABLE. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
