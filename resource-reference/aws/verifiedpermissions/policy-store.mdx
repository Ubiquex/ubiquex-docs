---
title: "aws_verifiedpermissions_policy_store"
description: "Real, generated bindings for `aws_verifiedpermissions_policy_store`."
---

`aws_verifiedpermissions_policy_store` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verifiedpermissions"

    cfg := verifiedpermissions.PolicyStoreConfig{
        DeletionProtection: "example",
        Description: "example",
    }
    ubx.Resource(verifiedpermissions.PolicyStore, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PolicyStore } from
      "jsr:@ubx/sdk-aws/aws/verifiedpermissions/policy_store";

    resource(PolicyStore, "example", {
      deletionProtection: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.verifiedpermissions import (
        PolicyStore,
        PolicyStoreConfig,
    )

    cfg = PolicyStoreConfig(
        deletion_protection="example",
        description="example",
    )
    ubx.resource(PolicyStore, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_verifiedpermissions_policy_store with
deletion_protection, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="deletion_protection" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deletion_protection" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_store_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
