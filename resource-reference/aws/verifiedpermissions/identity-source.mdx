---
title: "aws_verifiedpermissions_identity_source"
description: "Real, generated bindings for `aws_verifiedpermissions_identity_source`."
---

`aws_verifiedpermissions_identity_source` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/verifiedpermissions"

    cfg := verifiedpermissions.IdentitySourceConfig{
        PolicyStoreId: "example",
        PrincipalEntityType: "example",
    }
    ubx.Resource(verifiedpermissions.IdentitySource, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentitySource } from
      "jsr:@ubx/sdk-aws/aws/verifiedpermissions/identity_source";

    resource(IdentitySource, "example", {
      policyStoreId: "example",
      principalEntityType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.verifiedpermissions import (
        IdentitySource,
        IdentitySourceConfig,
    )

    cfg = IdentitySourceConfig(
        policy_store_id="example",
        principal_entity_type="example",
    )
    ubx.resource(IdentitySource, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_verifiedpermissions_identity_source with
policy_store_id, principal_entity_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_entity_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="principal_entity_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
