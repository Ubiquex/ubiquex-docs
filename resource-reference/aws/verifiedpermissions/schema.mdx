---
title: "aws_verifiedpermissions_schema"
description: "Real, generated bindings for `aws_verifiedpermissions_schema`."
---

`aws_verifiedpermissions_schema` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verifiedpermissions"

    cfg := verifiedpermissions.SchemaConfig{
        PolicyStoreId: "example",
    }
    ubx.Resource(verifiedpermissions.Schema, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Schema } from
      "jsr:@ubx/sdk-aws/aws/verifiedpermissions/schema";

    resource(Schema, "example", {
      policyStoreId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.verifiedpermissions import Schema, SchemaConfig

    cfg = SchemaConfig(
        policy_store_id="example",
    )
    ubx.resource(Schema, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_verifiedpermissions_schema with policy_store_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="namespaces" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
