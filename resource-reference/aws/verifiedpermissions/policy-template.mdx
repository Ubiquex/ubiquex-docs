---
title: "aws_verifiedpermissions_policy_template"
description: "Real, generated bindings for `aws_verifiedpermissions_policy_template`."
---

`aws_verifiedpermissions_policy_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/verifiedpermissions"

    cfg := verifiedpermissions.PolicyTemplateConfig{
        PolicyStoreId: "example",
        Statement: "example",
        Description: "example",
    }
    ubx.Resource(verifiedpermissions.PolicyTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PolicyTemplate } from
      "jsr:@ubx/sdk-aws/aws/verifiedpermissions/policy_template";

    resource(PolicyTemplate, "example", {
      policyStoreId: "example",
      statement: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.verifiedpermissions import (
        PolicyTemplate,
        PolicyTemplateConfig,
    )

    cfg = PolicyTemplateConfig(
        policy_store_id="example",
        statement="example",
        description="example",
    )
    ubx.resource(PolicyTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_verifiedpermissions_policy_template with
policy_store_id, statement, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="policy_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_template_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
