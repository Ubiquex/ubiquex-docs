---
title: "aws_verifiedpermissions_policy_template"
description: "Real, generated bindings for `aws_verifiedpermissions_policy_template`."
---

`aws_verifiedpermissions_policy_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	verifiedpermissions "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/verifiedpermissions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own policy template"})

    		ubx.Resource(verifiedpermissions.PolicyTemplate, "ci-runner", verifiedpermissions.PolicyTemplateConfig{
    			PolicyStoreId: "example",
    			Statement:     "example",
    			Description:   "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PolicyTemplate } from "jsr:@ubx/sdk-aws/aws/verifiedpermissions/policy_template";

    export default stack("payments", () => {
      intent({ summary: "payments own policy template" });

      resource(PolicyTemplate, "ci-runner", {
        policyStoreId: "example",
        statement: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.verifiedpermissions import PolicyTemplate, PolicyTemplateConfig

    def describe():
        ubx.intent("payments own policy template")

        ubx.resource(PolicyTemplate, "ci-runner", PolicyTemplateConfig(
            policy_store_id="example",
            statement="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_verifiedpermissions_policy_template, with policy store
    id example, statement example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="policy_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_template_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
