---
title: "aws_verifiedpermissions_policy"
description: "Real, generated bindings for `aws_verifiedpermissions_policy`."
---

`aws_verifiedpermissions_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/verifiedpermissions"

    cfg := verifiedpermissions.PolicyConfig{
        PolicyStoreId: "example",
    }
    ubx.Resource(verifiedpermissions.Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Policy } from
      "jsr:@ubx/sdk-aws/aws/verifiedpermissions/policy";

    resource(Policy, "example", {
      policyStoreId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.verifiedpermissions import Policy, PolicyConfig

    cfg = PolicyConfig(
        policy_store_id="example",
    )
    ubx.resource(Policy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_verifiedpermissions_policy with policy_store_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="created_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
