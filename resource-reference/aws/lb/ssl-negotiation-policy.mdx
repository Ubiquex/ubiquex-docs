---
title: "aws_lb_ssl_negotiation_policy"
description: "Real, generated bindings for `aws_lb_ssl_negotiation_policy`."
---

`aws_lb_ssl_negotiation_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/lb"

    cfg := lb.SslNegotiationPolicyConfig{
        LbPort: 1,
        LoadBalancer: "example",
        Name: "example-name",
        Triggers: map[string]string{"key": "value"},
    }
    ubx.Resource(lb.SslNegotiationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SslNegotiationPolicy } from
      "jsr:@ubx/sdk-aws/aws/lb/ssl_negotiation_policy";

    resource(SslNegotiationPolicy, "example", {
      lbPort: 1,
      loadBalancer: "example",
      name: "example",
      triggers: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lb import (
        SslNegotiationPolicy,
        SslNegotiationPolicyConfig,
    )

    cfg = SslNegotiationPolicyConfig(
        lb_port=1,
        load_balancer="example",
        name="example",
        triggers={"key": "value"},
    )
    ubx.resource(SslNegotiationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lb_ssl_negotiation_policy with lb_port,
load_balancer, name, triggers.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
