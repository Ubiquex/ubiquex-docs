---
title: "aws_lb_ssl_negotiation_policy"
description: "Real, generated bindings for `aws_lb_ssl_negotiation_policy`."
---

`aws_lb_ssl_negotiation_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ssl negotiation policy"})

    		ubx.Resource(lb.SslNegotiationPolicy, "ci-runner", lb.SslNegotiationPolicyConfig{
    			LbPort:       1,
    			LoadBalancer: "example",
    			Name:         "ci-runner",
    			Triggers:     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SslNegotiationPolicy } from "jsr:@ubx/sdk-aws/aws/lb/ssl_negotiation_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own ssl negotiation policy" });

      resource(SslNegotiationPolicy, "ci-runner", {
        lbPort: 1,
        loadBalancer: "example",
        name: "ci-runner",
        triggers: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lb import SslNegotiationPolicy, SslNegotiationPolicyConfig

    def describe():
        ubx.intent("payments own ssl negotiation policy")

        ubx.resource(SslNegotiationPolicy, "ci-runner", SslNegotiationPolicyConfig(
            lb_port=1,
            load_balancer="example",
            name="ci-runner",
            triggers={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lb_ssl_negotiation_policy called "ci-runner", with lb
    port 1, load balancer example, triggers a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
