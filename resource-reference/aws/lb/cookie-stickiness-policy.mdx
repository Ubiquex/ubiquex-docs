---
title: "aws_lb_cookie_stickiness_policy"
description: "Real, generated bindings for `aws_lb_cookie_stickiness_policy`."
---

`aws_lb_cookie_stickiness_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cookie stickiness policy"})

    		ubx.Resource(lb.CookieStickinessPolicy, "ci-runner", lb.CookieStickinessPolicyConfig{
    			LbPort:                 1,
    			LoadBalancer:           "example",
    			Name:                   "ci-runner",
    			CookieExpirationPeriod: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CookieStickinessPolicy } from "jsr:@ubx/sdk-aws/aws/lb/cookie_stickiness_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own cookie stickiness policy" });

      resource(CookieStickinessPolicy, "ci-runner", {
        lbPort: 1,
        loadBalancer: "example",
        name: "ci-runner",
        cookieExpirationPeriod: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lb import CookieStickinessPolicy, CookieStickinessPolicyConfig

    def describe():
        ubx.intent("payments own cookie stickiness policy")

        ubx.resource(CookieStickinessPolicy, "ci-runner", CookieStickinessPolicyConfig(
            lb_port=1,
            load_balancer="example",
            name="ci-runner",
            cookie_expiration_period=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lb_cookie_stickiness_policy called "ci-runner", with
    lb port 1, load balancer example, cookie expiration period 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cookie_expiration_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
