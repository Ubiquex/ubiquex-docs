---
title: "aws_lb_cookie_stickiness_policy"
description: "Real, generated bindings for `aws_lb_cookie_stickiness_policy`."
---

`aws_lb_cookie_stickiness_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lb"

    cfg := lb.CookieStickinessPolicyConfig{
        LbPort: 1,
        LoadBalancer: "example",
        Name: "example-name",
        CookieExpirationPeriod: 1,
    }
    ubx.Resource(lb.CookieStickinessPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CookieStickinessPolicy } from
      "jsr:@ubx/sdk-aws/aws/lb/cookie_stickiness_policy";

    resource(CookieStickinessPolicy, "example", {
      lbPort: 1,
      loadBalancer: "example",
      name: "example",
      cookieExpirationPeriod: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.lb import (
        CookieStickinessPolicy,
        CookieStickinessPolicyConfig,
    )

    cfg = CookieStickinessPolicyConfig(
        lb_port=1,
        load_balancer="example",
        name="example",
        cookie_expiration_period=1,
    )
    ubx.resource(CookieStickinessPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_lb_cookie_stickiness_policy with lb_port,
load_balancer, name, cookie_expiration_period.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cookie_expiration_period" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
