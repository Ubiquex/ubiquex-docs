---
title: "aws_lb_trust_store"
description: "Real, generated bindings for `aws_lb_trust_store`."
---

`aws_lb_trust_store` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trust store"})

    		ubx.Resource(lb.TrustStore, "ci-runner", lb.TrustStoreConfig{
    			CaCertificatesBundleS3Bucket:        "example",
    			CaCertificatesBundleS3Key:           "example",
    			Name:                                "ci-runner",
    			CaCertificatesBundleS3ObjectVersion: "example",
    			Tags:                                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrustStore } from "jsr:@ubx/sdk-aws/aws/lb/trust_store";

    export default stack("payments", () => {
      intent({ summary: "payments own trust store" });

      resource(TrustStore, "ci-runner", {
        caCertificatesBundleS3Bucket: "example",
        caCertificatesBundleS3Key: "example",
        name: "ci-runner",
        caCertificatesBundleS3ObjectVersion: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lb import TrustStore, TrustStoreConfig

    def describe():
        ubx.intent("payments own trust store")

        ubx.resource(TrustStore, "ci-runner", TrustStoreConfig(
            ca_certificates_bundle_s3_bucket="example",
            ca_certificates_bundle_s3_key="example",
            name="ci-runner",
            ca_certificates_bundle_s3_object_version="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lb_trust_store called "ci-runner", with ca
    certificates bundle s3 bucket example, ca certificates bundle s3
    key example, ca certificates bundle s3 object version example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="ca_certificates_bundle_s3_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ca_certificates_bundle_s3_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ca_certificates_bundle_s3_object_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn_suffix" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
