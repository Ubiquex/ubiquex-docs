---
title: "aws_lb_trust_store_revocation"
description: "Real, generated bindings for `aws_lb_trust_store_revocation`."
---

`aws_lb_trust_store_revocation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own trust store revocation"})

    		ubx.Resource(lb.TrustStoreRevocation, "ci-runner", lb.TrustStoreRevocationConfig{
    			RevocationsS3Bucket:        "example",
    			RevocationsS3Key:           "example",
    			TrustStoreArn:              "arn:aws:iam::123456789012:policy/ci",
    			RevocationsS3ObjectVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrustStoreRevocation } from "jsr:@ubx/sdk-aws/aws/lb/trust_store_revocation";

    export default stack("payments", () => {
      intent({ summary: "payments own trust store revocation" });

      resource(TrustStoreRevocation, "ci-runner", {
        revocationsS3Bucket: "example",
        revocationsS3Key: "example",
        trustStoreArn: "arn:aws:iam::123456789012:policy/ci",
        revocationsS3ObjectVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lb import TrustStoreRevocation, TrustStoreRevocationConfig

    def describe():
        ubx.intent("payments own trust store revocation")

        ubx.resource(TrustStoreRevocation, "ci-runner", TrustStoreRevocationConfig(
            revocations_s3_bucket="example",
            revocations_s3_key="example",
            trust_store_arn="arn:aws:iam::123456789012:policy/ci",
            revocations_s3_object_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lb_trust_store_revocation, with revocations s3 bucket
    example, revocations s3 key example, trust store arn
    arn:aws:iam::123456789012:policy/ci, revocations s3 object
    version example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revocations_s3_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="revocations_s3_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="revocations_s3_object_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="trust_store_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revocation_id" type="number">
  Computed by AWS after `ship`.
</ResponseField>
