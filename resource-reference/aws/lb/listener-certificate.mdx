---
title: "aws_lb_listener_certificate"
description: "Real, generated bindings for `aws_lb_listener_certificate`."
---

`aws_lb_listener_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own listener certificate"})

    		ubx.Resource(lb.ListenerCertificate, "ci-runner", lb.ListenerCertificateConfig{
    			CertificateArn: "arn:aws:iam::123456789012:policy/ci",
    			ListenerArn:    "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ListenerCertificate } from "jsr:@ubx/sdk-aws/aws/lb/listener_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own listener certificate" });

      resource(ListenerCertificate, "ci-runner", {
        certificateArn: "arn:aws:iam::123456789012:policy/ci",
        listenerArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lb import ListenerCertificate, ListenerCertificateConfig

    def describe():
        ubx.intent("payments own listener certificate")

        ubx.resource(ListenerCertificate, "ci-runner", ListenerCertificateConfig(
            certificate_arn="arn:aws:iam::123456789012:policy/ci",
            listener_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lb_listener_certificate, with certificate arn
    arn:aws:iam::123456789012:policy/ci, listener arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="listener_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
