---
title: "aws_codecatalyst_dev_environment"
description: "Real, generated bindings for `aws_codecatalyst_dev_environment`."
---

`aws_codecatalyst_dev_environment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codecatalyst "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codecatalyst"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dev environment"})

    		ubx.Resource(codecatalyst.DevEnvironment, "ci-runner", codecatalyst.DevEnvironmentConfig{
    			InstanceType:             "example",
    			ProjectName:              "example-project-name",
    			SpaceName:                "example-space-name",
    			Alias:                    "example",
    			InactivityTimeoutMinutes: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DevEnvironment } from "jsr:@ubx/sdk-aws/aws/codecatalyst/dev_environment";

    export default stack("payments", () => {
      intent({ summary: "payments own dev environment" });

      resource(DevEnvironment, "ci-runner", {
        instanceType: "example",
        projectName: "example-project-name",
        spaceName: "example-space-name",
        alias: "example",
        inactivityTimeoutMinutes: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codecatalyst import DevEnvironment, DevEnvironmentConfig

    def describe():
        ubx.intent("payments own dev environment")

        ubx.resource(DevEnvironment, "ci-runner", DevEnvironmentConfig(
            instance_type="example",
            project_name="example-project-name",
            space_name="example-space-name",
            alias="example",
            inactivity_timeout_minutes=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codecatalyst_dev_environment, with instance type
    example, project name example-project-name, space name
    example-space-name, alias example, inactivity timeout minutes 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="alias" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="inactivity_timeout_minutes" type="number">
  Optional.
</ResponseField>

<ResponseField name="instance_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="space_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
