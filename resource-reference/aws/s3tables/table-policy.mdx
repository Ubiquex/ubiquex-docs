---
title: "aws_s3tables_table_policy"
description: "Real, generated bindings for `aws_s3tables_table_policy`."
---

`aws_s3tables_table_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3tables "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3tables"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own table policy"})

    		ubx.Resource(s3tables.TablePolicy, "ci-runner", s3tables.TablePolicyConfig{
    			Name:           "ci-runner",
    			Namespace:      "example",
    			ResourcePolicy: "example",
    			TableBucketArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TablePolicy } from "jsr:@ubx/sdk-aws/aws/s3tables/table_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own table policy" });

      resource(TablePolicy, "ci-runner", {
        name: "ci-runner",
        namespace: "example",
        resourcePolicy: "example",
        tableBucketArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3tables import TablePolicy, TablePolicyConfig

    def describe():
        ubx.intent("payments own table policy")

        ubx.resource(TablePolicy, "ci-runner", TablePolicyConfig(
            name="ci-runner",
            namespace="example",
            resource_policy="example",
            table_bucket_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3tables_table_policy called "ci-runner", with
    namespace example, resource policy example, table bucket arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_bucket_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
