---
title: "aws_s3tables_table_bucket_policy"
description: "Real, generated bindings for `aws_s3tables_table_bucket_policy`."
---

`aws_s3tables_table_bucket_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3tables"

    cfg := s3tables.TableBucketPolicyConfig{
        ResourcePolicy: "example",
        TableBucketArn: "example",
    }
    ubx.Resource(s3tables.TableBucketPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TableBucketPolicy } from
      "jsr:@ubx/sdk-aws/aws/s3tables/table_bucket_policy";

    resource(TableBucketPolicy, "example", {
      resourcePolicy: "example",
      tableBucketArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3tables import (
        TableBucketPolicy,
        TableBucketPolicyConfig,
    )

    cfg = TableBucketPolicyConfig(
        resource_policy="example",
        table_bucket_arn="example",
    )
    ubx.resource(TableBucketPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3tables_table_bucket_policy with resource_policy,
table_bucket_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_bucket_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
