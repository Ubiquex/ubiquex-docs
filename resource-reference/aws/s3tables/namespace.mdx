---
title: "aws_s3tables_namespace"
description: "Real, generated bindings for `aws_s3tables_namespace`."
---

`aws_s3tables_namespace` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3tables"

    cfg := s3tables.NamespaceConfig{
        Namespace: "example-namespace",
        TableBucketArn: "example",
    }
    ubx.Resource(s3tables.Namespace, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Namespace } from
      "jsr:@ubx/sdk-aws/aws/s3tables/namespace";

    resource(Namespace, "example", {
      namespace: "example",
      tableBucketArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3tables import Namespace, NamespaceConfig

    cfg = NamespaceConfig(
        namespace="example",
        table_bucket_arn="example",
    )
    ubx.resource(Namespace, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3tables_namespace with namespace,
table_bucket_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="namespace" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="table_bucket_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_by" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
