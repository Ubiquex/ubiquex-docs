---
title: "aws_s3tables_table_replication"
description: "Real, generated bindings for `aws_s3tables_table_replication`."
---

`aws_s3tables_table_replication` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3tables "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3tables"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own table replication"})

    		ubx.Resource(s3tables.TableReplication, "ci-runner", s3tables.TableReplicationConfig{
    			Role:     "example",
    			TableArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TableReplication } from "jsr:@ubx/sdk-aws/aws/s3tables/table_replication";

    export default stack("payments", () => {
      intent({ summary: "payments own table replication" });

      resource(TableReplication, "ci-runner", {
        role: "example",
        tableArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3tables import TableReplication, TableReplicationConfig

    def describe():
        ubx.intent("payments own table replication")

        ubx.resource(TableReplication, "ci-runner", TableReplicationConfig(
            role="example",
            table_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3tables_table_replication, with role example, table
    arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>
