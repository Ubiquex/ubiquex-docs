---
title: "aws_s3tables_table_replication"
description: "Real, generated bindings for `aws_s3tables_table_replication`."
---

`aws_s3tables_table_replication` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3tables"

    cfg := s3tables.TableReplicationConfig{
        Role: "example",
        TableArn: "example",
    }
    ubx.Resource(s3tables.TableReplication, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TableReplication } from
      "jsr:@ubx/sdk-aws/aws/s3tables/table_replication";

    resource(TableReplication, "example", {
      role: "example",
      tableArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3tables import (
        TableReplication,
        TableReplicationConfig,
    )

    cfg = TableReplicationConfig(
        role="example",
        table_arn="example",
    )
    ubx.resource(TableReplication, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3tables_table_replication with role, table_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>
