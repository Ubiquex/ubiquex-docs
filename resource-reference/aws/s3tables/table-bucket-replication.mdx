---
title: "aws_s3tables_table_bucket_replication"
description: "Real, generated bindings for `aws_s3tables_table_bucket_replication`."
---

`aws_s3tables_table_bucket_replication` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3tables"

    cfg := s3tables.TableBucketReplicationConfig{
        Role: "example",
        TableBucketArn: "example",
    }
    ubx.Resource(s3tables.TableBucketReplication, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { TableBucketReplication } from
      "jsr:@ubx/sdk-aws/aws/s3tables/table_bucket_replication";

    resource(TableBucketReplication, "example", {
      role: "example",
      tableBucketArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3tables import (
        TableBucketReplication,
        TableBucketReplicationConfig,
    )

    cfg = TableBucketReplicationConfig(
        role="example",
        table_bucket_arn="example",
    )
    ubx.resource(TableBucketReplication, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3tables_table_bucket_replication with role,
table_bucket_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_bucket_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>
