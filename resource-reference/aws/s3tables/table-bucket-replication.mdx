---
title: "aws_s3tables_table_bucket_replication"
description: "Real, generated bindings for `aws_s3tables_table_bucket_replication`."
---

`aws_s3tables_table_bucket_replication` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3tables "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3tables"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own table bucket replication"})

    		ubx.Resource(s3tables.TableBucketReplication, "ci-runner", s3tables.TableBucketReplicationConfig{
    			Role:           "example",
    			TableBucketArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TableBucketReplication } from "jsr:@ubx/sdk-aws/aws/s3tables/table_bucket_replication";

    export default stack("payments", () => {
      intent({ summary: "payments own table bucket replication" });

      resource(TableBucketReplication, "ci-runner", {
        role: "example",
        tableBucketArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3tables import TableBucketReplication, TableBucketReplicationConfig

    def describe():
        ubx.intent("payments own table bucket replication")

        ubx.resource(TableBucketReplication, "ci-runner", TableBucketReplicationConfig(
            role="example",
            table_bucket_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3tables_table_bucket_replication, with role example,
    table bucket arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_bucket_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version_token" type="string">
  Computed by AWS after `ship`.
</ResponseField>
