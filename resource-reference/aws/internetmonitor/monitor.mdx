---
title: "aws_internetmonitor_monitor"
description: "Real, generated bindings for `aws_internetmonitor_monitor`."
---

`aws_internetmonitor_monitor` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	internetmonitor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/internetmonitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own monitor"})

    		ubx.Resource(internetmonitor.Monitor, "ci-runner", internetmonitor.MonitorConfig{
    			MonitorName:                "example-monitor-name",
    			MaxCityNetworksToMonitor:   1,
    			Resources:                  []string{"example"},
    			Status:                     "example",
    			Tags:                       map[string]string{"managed-by": "ubx"},
    			TrafficPercentageToMonitor: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Monitor } from "jsr:@ubx/sdk-aws/aws/internetmonitor/monitor";

    export default stack("payments", () => {
      intent({ summary: "payments own monitor" });

      resource(Monitor, "ci-runner", {
        monitorName: "example-monitor-name",
        maxCityNetworksToMonitor: 1,
        resources: ["example"],
        status: "example",
        tags: { "managed-by": "ubx" },
        trafficPercentageToMonitor: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.internetmonitor import Monitor, MonitorConfig

    def describe():
        ubx.intent("payments own monitor")

        ubx.resource(Monitor, "ci-runner", MonitorConfig(
            monitor_name="example-monitor-name",
            max_city_networks_to_monitor=1,
            resources=["example"],
            status="example",
            tags={"managed-by": "ubx"},
            traffic_percentage_to_monitor=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_internetmonitor_monitor, with monitor name
    example-monitor-name, max city networks to monitor 1, resources a
    set value, status example, tags a set value, traffic percentage
    to monitor 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_city_networks_to_monitor" type="number">
  Optional.
</ResponseField>

<ResponseField name="monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resources" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="traffic_percentage_to_monitor" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
