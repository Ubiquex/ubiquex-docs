---
title: "aws_doc_db_dbsubnet_group"
description: "`aws_doc_db_dbsubnet_group` defines the real VPC subnets an `aws_doc_db_dbcluster`'s instances are actually placed into..."
---

`aws_doc_db_dbsubnet_group` defines the real VPC subnets an `aws_doc_db_dbcluster`'s instances are actually placed into, required network placement before a cluster can be created.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	doc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/doc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own db dbsubnet group"})

    		ubx.Resource(doc.DbDbsubnetGroup, "ci-runner", doc.DbDbsubnetGroupConfig{
    			DbsubnetGroupDescription: "example",
    			SubnetIds:                []string{"example"},
    			DbsubnetGroupName:        "example-dbsubnet-group-name",
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbDbsubnetGroup } from "./local-sdk/aws/sdk/typescript/aws/doc/db_dbsubnet_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own db dbsubnet group" });

      resource(DbDbsubnetGroup, "ci-runner", {
        dbsubnetGroupDescription: "example",
        subnetIds: ["example"],
        dbsubnetGroupName: "example-dbsubnet-group-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.doc import DbDbsubnetGroup, DbDbsubnetGroupConfig

    def describe():
        ubx.intent("payments own db dbsubnet group")

        ubx.resource(DbDbsubnetGroup, "ci-runner", DbDbsubnetGroupConfig(
            dbsubnet_group_description="example",
            subnet_ids=["example"],
            dbsubnet_group_name="example-dbsubnet-group-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_doc_db_dbsubnet_group, with dbsubnet group description
    example, subnet ids a set value, dbsubnet group name
    example-dbsubnet-group-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="dbsubnet_group_description" type="string" required>
  The description for the subnet group. Required.
</ResponseField>

<ResponseField name="dbsubnet_group_name" type="string">
  The name for the db subnet group. This value is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  One or more subnet IDs to be assigned to the db subnet group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  One or more tags to be assigned to the db subnet group Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag attached to the Amazon DocumentDB DB subnet group, used for organizing, identifying, and managing the resource (e.g., via cost allocation or access control). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Specifies the value of a tag attached to the DocumentDB DB subnet group, used for metadata and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
