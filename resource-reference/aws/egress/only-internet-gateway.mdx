---
title: "aws_egress_only_internet_gateway"
description: "Real, generated bindings for `aws_egress_only_internet_gateway`."
---

`aws_egress_only_internet_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	egress "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/egress"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own only internet gateway"})

    		ubx.Resource(egress.OnlyInternetGateway, "ci-runner", egress.OnlyInternetGatewayConfig{
    			VpcId: "example",
    			Tags:  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OnlyInternetGateway } from "jsr:@ubx/sdk-aws/aws/egress/only_internet_gateway";

    export default stack("payments", () => {
      intent({ summary: "payments own only internet gateway" });

      resource(OnlyInternetGateway, "ci-runner", {
        vpcId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.egress import OnlyInternetGateway, OnlyInternetGatewayConfig

    def describe():
        ubx.intent("payments own only internet gateway")

        ubx.resource(OnlyInternetGateway, "ci-runner", OnlyInternetGatewayConfig(
            vpc_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_egress_only_internet_gateway, with vpc id example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
