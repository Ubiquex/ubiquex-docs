---
title: "aws_egress_only_internet_gateway"
description: "`aws_egress_only_internet_gateway` lets IPv6 resources in a VPC initiate outbound connections to the internet while blocking any inbound connection..."
---

`aws_egress_only_internet_gateway` lets IPv6 resources in a VPC initiate outbound connections to the internet while blocking any inbound connection from initiating one back, the IPv6-only, egress-only counterpart to a NAT gateway, which IPv6 doesn't use.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	egress "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/egress"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own only internet gateway"})

    		ubx.Resource(egress.OnlyInternetGateway, "ci-runner", egress.OnlyInternetGatewayConfig{
    			VpcId: "example",
    			Tags:  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { OnlyInternetGateway } from "./local-sdk/aws/sdk/typescript/aws/egress/only_internet_gateway.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own only internet gateway" });

      resource(OnlyInternetGateway, "ci-runner", {
        vpcId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.egress import OnlyInternetGateway, OnlyInternetGatewayConfig

    def describe():
        ubx.intent("payments own only internet gateway")

        ubx.resource(OnlyInternetGateway, "ci-runner", OnlyInternetGatewayConfig(
            vpc_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_egress_only_internet_gateway, with vpc id example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="tags" type="list(object)">
  Any tags assigned to the egress only internet gateway. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key portion of a tag attached to the egress-only internet gateway, used for identifying, organizing, and managing this VPC component in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The tag value (string) for a tag applied to the egress-only internet gateway, allowing you to categorize and manage this IPv6 outbound-only VPC component. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  The ID of the VPC for which to create the egress-only internet gateway. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Service Generated ID of the EgressOnlyInternetGateway Computed by AWS after `ship`.
</ResponseField>
