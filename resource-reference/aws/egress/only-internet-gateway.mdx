---
title: "aws_egress_only_internet_gateway"
description: "Real, generated bindings for `aws_egress_only_internet_gateway`."
---

`aws_egress_only_internet_gateway` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/egress"

    cfg := egress.OnlyInternetGatewayConfig{
        VpcId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(egress.OnlyInternetGateway, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OnlyInternetGateway } from
      "jsr:@ubx/sdk-aws/aws/egress/only_internet_gateway";

    resource(OnlyInternetGateway, "example", {
      vpcId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.egress import (
        OnlyInternetGateway,
        OnlyInternetGatewayConfig,
    )

    cfg = OnlyInternetGatewayConfig(
        vpc_id="example",
        tags={"key": "value"},
    )
    ubx.resource(OnlyInternetGateway, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_egress_only_internet_gateway with vpc_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
