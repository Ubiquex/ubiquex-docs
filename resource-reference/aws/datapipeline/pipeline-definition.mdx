---
title: "aws_datapipeline_pipeline_definition"
description: "Real, generated bindings for `aws_datapipeline_pipeline_definition`."
---

`aws_datapipeline_pipeline_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	datapipeline "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/datapipeline"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pipeline definition"})

    		ubx.Resource(datapipeline.PipelineDefinition, "ci-runner", datapipeline.PipelineDefinitionConfig{
    			PipelineId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PipelineDefinition } from "jsr:@ubx/sdk-aws/aws/datapipeline/pipeline_definition";

    export default stack("payments", () => {
      intent({ summary: "payments own pipeline definition" });

      resource(PipelineDefinition, "ci-runner", {
        pipelineId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.datapipeline import PipelineDefinition, PipelineDefinitionConfig

    def describe():
        ubx.intent("payments own pipeline definition")

        ubx.resource(PipelineDefinition, "ci-runner", PipelineDefinitionConfig(
            pipeline_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_datapipeline_pipeline_definition, with pipeline id
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pipeline_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
