---
title: "aws_osis_pipeline"
description: "Real, generated bindings for `aws_osis_pipeline`."
---

`aws_osis_pipeline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	osis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/osis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pipeline"})

    		ubx.Resource(osis.Pipeline, "ci-runner", osis.PipelineConfig{
    			MaxUnits:                  1,
    			MinUnits:                  1,
    			PipelineConfigurationBody: "example",
    			PipelineName:              "example-pipeline-name",
    			Tags:                      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Pipeline } from "jsr:@ubx/sdk-aws/aws/osis/pipeline";

    export default stack("payments", () => {
      intent({ summary: "payments own pipeline" });

      resource(Pipeline, "ci-runner", {
        maxUnits: 1,
        minUnits: 1,
        pipelineConfigurationBody: "example",
        pipelineName: "example-pipeline-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.osis import Pipeline, PipelineConfig

    def describe():
        ubx.intent("payments own pipeline")

        ubx.resource(Pipeline, "ci-runner", PipelineConfig(
            max_units=1,
            min_units=1,
            pipeline_configuration_body="example",
            pipeline_name="example-pipeline-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_osis_pipeline, with max units 1, min units 1, pipeline
    configuration body example, pipeline name example-pipeline-name,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="max_units" type="number" required>
  Required.
</ResponseField>

<ResponseField name="min_units" type="number" required>
  Required.
</ResponseField>

<ResponseField name="pipeline_configuration_body" type="string" required>
  Required.
</ResponseField>

<ResponseField name="pipeline_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="pipeline_role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ingest_endpoint_urls" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="pipeline_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="pipeline_role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
