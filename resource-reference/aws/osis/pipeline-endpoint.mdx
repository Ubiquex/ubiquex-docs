---
title: "aws_osis_pipeline_endpoint"
description: "Real, generated bindings for `aws_osis_pipeline_endpoint`."
---

`aws_osis_pipeline_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/osis"

    cfg := osis.PipelineEndpointConfig{
        PipelineArn: "example",
    }
    ubx.Resource(osis.PipelineEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PipelineEndpoint } from
      "jsr:@ubx/sdk-aws/aws/osis/pipeline_endpoint";

    resource(PipelineEndpoint, "example", {
      pipelineArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.osis import (
        PipelineEndpoint,
        PipelineEndpointConfig,
    )

    cfg = PipelineEndpointConfig(
        pipeline_arn="example",
    )
    ubx.resource(PipelineEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_osis_pipeline_endpoint with pipeline_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="pipeline_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
