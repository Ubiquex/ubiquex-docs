---
title: "aws_osis_pipeline_endpoint"
description: "Real, generated bindings for `aws_osis_pipeline_endpoint`."
---

`aws_osis_pipeline_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	osis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/osis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pipeline endpoint"})

    		ubx.Resource(osis.PipelineEndpoint, "ci-runner", osis.PipelineEndpointConfig{
    			PipelineArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PipelineEndpoint } from "jsr:@ubx/sdk-aws/aws/osis/pipeline_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own pipeline endpoint" });

      resource(PipelineEndpoint, "ci-runner", {
        pipelineArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.osis import PipelineEndpoint, PipelineEndpointConfig

    def describe():
        ubx.intent("payments own pipeline endpoint")

        ubx.resource(PipelineEndpoint, "ci-runner", PipelineEndpointConfig(
            pipeline_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_osis_pipeline_endpoint, with pipeline arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="pipeline_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
