---
title: "aws_rum_app_monitor"
description: "Real, generated bindings for `aws_rum_app_monitor`."
---

`aws_rum_app_monitor` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rum"

    cfg := rum.AppMonitorConfig{
        Name: "example-name",
        CwLogEnabled: true,
        Domain: "example",
    }
    ubx.Resource(rum.AppMonitor, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AppMonitor } from
      "jsr:@ubx/sdk-aws/aws/rum/app_monitor";

    resource(AppMonitor, "example", {
      name: "example",
      cwLogEnabled: true,
      domain: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rum import AppMonitor, AppMonitorConfig

    cfg = AppMonitorConfig(
        name="example",
        cw_log_enabled=True,
        domain="example",
    )
    ubx.resource(AppMonitor, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rum_app_monitor with name, cw_log_enabled, domain.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cw_log_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="domain" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_list" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="app_monitor_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cw_log_group" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
