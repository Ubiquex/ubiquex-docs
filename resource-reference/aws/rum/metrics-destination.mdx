---
title: "aws_rum_metrics_destination"
description: "Real, generated bindings for `aws_rum_metrics_destination`."
---

`aws_rum_metrics_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/rum"

    cfg := rum.MetricsDestinationConfig{
        AppMonitorName: "example-app-monitor-name",
        Destination: "example",
        DestinationArn: "example",
        IamRoleArn: "example",
    }
    ubx.Resource(rum.MetricsDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { MetricsDestination } from
      "jsr:@ubx/sdk-aws/aws/rum/metrics_destination";

    resource(MetricsDestination, "example", {
      appMonitorName: "example",
      destination: "example",
      destinationArn: "example",
      iamRoleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.rum import (
        MetricsDestination,
        MetricsDestinationConfig,
    )

    cfg = MetricsDestinationConfig(
        app_monitor_name="example",
        destination="example",
        destination_arn="example",
        iam_role_arn="example",
    )
    ubx.resource(MetricsDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_rum_metrics_destination with app_monitor_name,
destination, destination_arn, iam_role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="app_monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
