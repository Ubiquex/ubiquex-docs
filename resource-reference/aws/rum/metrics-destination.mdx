---
title: "aws_rum_metrics_destination"
description: "Real, generated bindings for `aws_rum_metrics_destination`."
---

`aws_rum_metrics_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rum "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rum"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own metrics destination"})

    		ubx.Resource(rum.MetricsDestination, "ci-runner", rum.MetricsDestinationConfig{
    			AppMonitorName: "example-app-monitor-name",
    			Destination:    "example",
    			DestinationArn: "arn:aws:iam::123456789012:policy/ci",
    			IamRoleArn:     "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MetricsDestination } from "jsr:@ubx/sdk-aws/aws/rum/metrics_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own metrics destination" });

      resource(MetricsDestination, "ci-runner", {
        appMonitorName: "example-app-monitor-name",
        destination: "example",
        destinationArn: "arn:aws:iam::123456789012:policy/ci",
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rum import MetricsDestination, MetricsDestinationConfig

    def describe():
        ubx.intent("payments own metrics destination")

        ubx.resource(MetricsDestination, "ci-runner", MetricsDestinationConfig(
            app_monitor_name="example-app-monitor-name",
            destination="example",
            destination_arn="arn:aws:iam::123456789012:policy/ci",
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rum_metrics_destination, with app monitor name
    example-app-monitor-name, destination example, destination arn
    arn:aws:iam::123456789012:policy/ci, iam role arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="iam_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
