---
title: "aws_support_app_slack_workspace_configuration"
description: "`aws_support_app_slack_workspace_configuration` authorizes a real Slack workspace for the AWS Support App..."
---

`aws_support_app_slack_workspace_configuration` authorizes a real Slack workspace for the AWS Support App, the prerequisite step before any `aws_support_app_slack_channel_configuration` within that workspace can be created.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	supportapp "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/supportapp"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own slack workspace configuration"})

    		ubx.Resource(supportapp.SlackWorkspaceConfiguration, "example", supportapp.SlackWorkspaceConfigurationConfig{
    			TeamId:    "example",
    			VersionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SlackWorkspaceConfiguration } from "@ubx/sdk-aws/aws/supportapp/slack_workspace_configuration";

    export default stack("example", () => {
      intent({ summary: "example own slack workspace configuration" });

      resource(SlackWorkspaceConfiguration, "example", {
        teamId: "example",
        versionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.supportapp import SlackWorkspaceConfiguration, SlackWorkspaceConfigurationConfig

    def describe():
        ubx.intent("example own slack workspace configuration")

        ubx.resource(SlackWorkspaceConfiguration, "example", SlackWorkspaceConfigurationConfig(
            team_id="example",
            version_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_support_app_slack_workspace_configuration, with team
    id example, version id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="team_id" type="string" required>
  The team ID in Slack, which uniquely identifies a workspace. Required.
</ResponseField>

<ResponseField name="version_id" type="string">
  An identifier used to update an existing Slack workspace configuration in AWS CloudFormation. Optional.
</ResponseField>
