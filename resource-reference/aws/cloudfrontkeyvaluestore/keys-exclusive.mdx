---
title: "aws_cloudfrontkeyvaluestore_keys_exclusive"
description: "Real, generated bindings for `aws_cloudfrontkeyvaluestore_keys_exclusive`."
---

`aws_cloudfrontkeyvaluestore_keys_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfrontkeyvaluestore"

    cfg := cloudfrontkeyvaluestore.KeysExclusiveConfig{
        KeyValueStoreArn: "example",
        MaxBatchSize: 1,
    }
    ubx.Resource(
        cloudfrontkeyvaluestore.KeysExclusive,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { KeysExclusive } from
      "jsr:@ubx/sdk-aws/aws/cloudfrontkeyvaluestore/keys_exclusive";

    resource(KeysExclusive, "example", {
      keyValueStoreArn: "example",
      maxBatchSize: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudfrontkeyvaluestore import (
        KeysExclusive,
        KeysExclusiveConfig,
    )

    cfg = KeysExclusiveConfig(
        key_value_store_arn="example",
        max_batch_size=1,
    )
    ubx.resource(KeysExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudfrontkeyvaluestore_keys_exclusive with
key_value_store_arn, max_batch_size.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="key_value_store_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="max_batch_size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="max_batch_size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="total_size_in_bytes" type="number">
  Computed by AWS after `ship`.
</ResponseField>
