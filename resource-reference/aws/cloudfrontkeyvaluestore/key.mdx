---
title: "aws_cloudfrontkeyvaluestore_key"
description: "Real, generated bindings for `aws_cloudfrontkeyvaluestore_key`."
---

`aws_cloudfrontkeyvaluestore_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfrontkeyvaluestore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfrontkeyvaluestore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key"})

    		ubx.Resource(cloudfrontkeyvaluestore.Key, "ci-runner", cloudfrontkeyvaluestore.KeyConfig{
    			Key:              "example",
    			KeyValueStoreArn: "arn:aws:iam::123456789012:policy/ci",
    			Value:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Key } from "jsr:@ubx/sdk-aws/aws/cloudfrontkeyvaluestore/key";

    export default stack("payments", () => {
      intent({ summary: "payments own key" });

      resource(Key, "ci-runner", {
        key: "example",
        keyValueStoreArn: "arn:aws:iam::123456789012:policy/ci",
        value: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfrontkeyvaluestore import Key, KeyConfig

    def describe():
        ubx.intent("payments own key")

        ubx.resource(Key, "ci-runner", KeyConfig(
            key="example",
            key_value_store_arn="arn:aws:iam::123456789012:policy/ci",
            value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfrontkeyvaluestore_key, with key example, key
    value store arn arn:aws:iam::123456789012:policy/ci, value
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="key" type="string" required>
  The key to put. Required.
</ResponseField>

<ResponseField name="key_value_store_arn" type="string" required>
  The Amazon Resource Name (ARN) of the Key Value Store. Required.
</ResponseField>

<ResponseField name="value" type="string" required>
  The value to put. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="total_size_in_bytes" type="number">
  Total size of the Key Value Store in bytes. Computed by AWS after `ship`.
</ResponseField>
