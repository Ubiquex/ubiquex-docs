---
title: "aws_amazon_mq_configuration_association"
description: "`aws_amazon_mq_configuration_association` is the real link between an `aws_amazon_mq_configuration` and the `aws_amazon_mq_broker` it applies to."
---

A configuration on its own changes nothing, this association is what actually applies a given configuration revision to a running broker.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amazon "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amazon"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mq configuration association"})

    		ubx.Resource(amazon.MqConfigurationAssociation, "ci-runner", amazon.MqConfigurationAssociationConfig{
    			Broker:        "example",
    			Configuration: map[string]any{"id": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MqConfigurationAssociation } from "./local-sdk/aws/sdk/typescript/aws/amazon/mq_configuration_association.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mq configuration association" });

      resource(MqConfigurationAssociation, "ci-runner", {
        broker: "example",
        configuration: { id: "example" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.amazon import MqConfigurationAssociation, MqConfigurationAssociationConfig

    def describe():
        ubx.intent("payments own mq configuration association")

        ubx.resource(MqConfigurationAssociation, "ci-runner", MqConfigurationAssociationConfig(
            broker="example",
            configuration={"id": "example"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amazon_mq_configuration_association, with broker
    example, configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="broker" type="string" required>
  ID of the Broker that the configuration should be applied to Required.
</ResponseField>

<ResponseField name="configuration" type="object" required>
  Specifies the Amazon MQ configuration (ID and revision) to associate with the broker in a ConfigurationAssociation resource. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="id" type="string" required>
      ID of the Configuration to apply to a Broker Required.
    </ResponseField>
    <ResponseField name="revision" type="number" required>
      Revision of the Configuration to apply to a Broker Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The ID of the ConfigurationAssociation Resource Computed by AWS after `ship`.
</ResponseField>
