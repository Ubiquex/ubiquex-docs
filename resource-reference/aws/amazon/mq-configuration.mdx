---
title: "aws_amazon_mq_configuration"
description: "`aws_amazon_mq_configuration` defines the engine-specific settings (XML for ActiveMQ..."
---

`aws_amazon_mq_configuration` defines the engine-specific settings (XML for ActiveMQ, a config file for RabbitMQ) that an `aws_amazon_mq_broker` runs with, kept as a separate, versioned resource so the same configuration can be validated and reused across brokers instead of being edited in place on each one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	amazon "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amazon"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mq configuration"})

    		ubx.Resource(amazon.MqConfiguration, "ci-runner", amazon.MqConfigurationConfig{
    			EngineType:             "example",
    			Name:                   "ci-runner",
    			AuthenticationStrategy: "example",
    			Data:                   "example",
    			Description:            "Managed by ubx.",
    			EngineVersion:          "example",
    			Tags:                   []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MqConfiguration } from "./local-sdk/aws/sdk/typescript/aws/amazon/mq_configuration.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mq configuration" });

      resource(MqConfiguration, "ci-runner", {
        engineType: "example",
        name: "ci-runner",
        authenticationStrategy: "example",
        data: "example",
        description: "Managed by ubx.",
        engineVersion: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.amazon import MqConfiguration, MqConfigurationConfig

    def describe():
        ubx.intent("payments own mq configuration")

        ubx.resource(MqConfiguration, "ci-runner", MqConfigurationConfig(
            engine_type="example",
            name="ci-runner",
            authentication_strategy="example",
            data="example",
            description="Managed by ubx.",
            engine_version="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amazon_mq_configuration called "ci-runner", with
    engine type example, authentication strategy example, data
    example, description Managed by ubx., engine version example,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="authentication_strategy" type="string">
  The authentication strategy associated with the configuration. The default is SIMPLE. Optional.
</ResponseField>

<ResponseField name="data" type="string">
  The base64-encoded XML configuration. Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the configuration. Optional.
</ResponseField>

<ResponseField name="engine_type" type="string" required>
  The type of broker engine. Note: Currently, Amazon MQ only supports ACTIVEMQ for creating and editing broker configurations. Required.
</ResponseField>

<ResponseField name="engine_version" type="string">
  The version of the broker engine. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the configuration. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Create tags when creating the configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the Amazon MQ configuration, used to identify and organize the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the Amazon MQ configuration. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  The ID of the Amazon MQ configuration. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="revision" type="string">
  The revision number of the configuration. Computed by AWS after `ship`.
</ResponseField>
