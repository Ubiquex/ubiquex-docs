---
title: "aws_codeartifact_domain_permissions_policy"
description: "Real, generated bindings for `aws_codeartifact_domain_permissions_policy`."
---

`aws_codeartifact_domain_permissions_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codeartifact "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codeartifact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own domain permissions policy"})

    		ubx.Resource(codeartifact.DomainPermissionsPolicy, "ci-runner", codeartifact.DomainPermissionsPolicyConfig{
    			Domain: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DomainPermissionsPolicy } from "jsr:@ubx/sdk-aws/aws/codeartifact/domain_permissions_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own domain permissions policy" });

      resource(DomainPermissionsPolicy, "ci-runner", {
        domain: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codeartifact import DomainPermissionsPolicy, DomainPermissionsPolicyConfig

    def describe():
        ubx.intent("payments own domain permissions policy")

        ubx.resource(DomainPermissionsPolicy, "ci-runner", DomainPermissionsPolicyConfig(
            domain="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codeartifact_domain_permissions_policy, with domain
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="domain" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_owner" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_document" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_revision" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="domain_owner" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_document" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_revision" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
