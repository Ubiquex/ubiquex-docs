---
title: "aws_event_schemas_registry"
description: "`aws_event_schemas_registry` is a real, named collection of event schemas..."
---

`aws_event_schemas_registry` is a real, named collection of event schemas, the container every `aws_event_schemas_schema` in one logical group of related events belongs to, separate from the default registry EventBridge maintains automatically.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eventschemas "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/eventschemas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own registry"})

    		ubx.Resource(eventschemas.Registry, "example", eventschemas.RegistryConfig{
    			Description:  "Managed by ubx.",
    			RegistryName: "example-registry-name",
    			Tags:         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Registry } from "@ubx/sdk-aws/aws/eventschemas/registry";

    export default stack("example", () => {
      intent({ summary: "example own registry" });

      resource(Registry, "example", {
        description: "Managed by ubx.",
        registryName: "example-registry-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eventschemas import Registry, RegistryConfig
    from ubx.aws.eventschemas.registry import Registry_Tags

    def describe():
        ubx.intent("example own registry")

        ubx.resource(Registry, "example", RegistryConfig(
            description="Managed by ubx.",
            registry_name="example-registry-name",
            tags=[Registry_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_event_schemas_registry, with description Managed by
    ubx., registry name example-registry-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description of the registry to be created. Optional.
</ResponseField>

<ResponseField name="registry_name" type="string">
  The name of the schema registry. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags associated with the resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key associated with the EventBridge Schema Registry, used to assign a custom metadata label that helps organize, identify, and filter the registry by their project, environment, or other criteria. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key attached to the EventBridge Schema Registry, used to store arbitrary metadata for resource management and filtering. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="registry_arn" type="string">
  The ARN of the registry. Computed by AWS after `ship`.
</ResponseField>
