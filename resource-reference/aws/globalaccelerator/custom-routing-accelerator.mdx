---
title: "aws_globalaccelerator_custom_routing_accelerator"
description: "Real, generated bindings for `aws_globalaccelerator_custom_routing_accelerator`."
---

`aws_globalaccelerator_custom_routing_accelerator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/globalaccelerator"

    cfg := globalaccelerator.CustomRoutingAcceleratorConfig{
        Name: "example-name",
        Enabled: true,
        IpAddressType: "example",
    }
    ubx.Resource(
        globalaccelerator.CustomRoutingAccelerator,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CustomRoutingAccelerator } from
      "jsr:@ubx/sdk-aws/aws/globalaccelerator/custom_routing_accelerator";

    resource(CustomRoutingAccelerator, "example", {
      name: "example",
      enabled: true,
      ipAddressType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.globalaccelerator import (
        CustomRoutingAccelerator,
        CustomRoutingAcceleratorConfig,
    )

    cfg = CustomRoutingAcceleratorConfig(
        name="example",
        enabled=True,
        ip_address_type="example",
    )
    ubx.resource(CustomRoutingAccelerator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_globalaccelerator_custom_routing_accelerator with
name, enabled, ip_address_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip_address_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="ip_addresses" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="dns_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hosted_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip_sets" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="ip_addresses" type="list(string)">
    </ResponseField>
    <ResponseField name="ip_family" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
