---
title: "aws_globalaccelerator_custom_routing_accelerator"
description: "Real, generated bindings for `aws_globalaccelerator_custom_routing_accelerator`."
---

`aws_globalaccelerator_custom_routing_accelerator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom routing accelerator"})

    		ubx.Resource(globalaccelerator.CustomRoutingAccelerator, "ci-runner", globalaccelerator.CustomRoutingAcceleratorConfig{
    			Name:          "ci-runner",
    			Enabled:       true,
    			IpAddressType: "example",
    			IpAddresses:   []string{"example"},
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomRoutingAccelerator } from "jsr:@ubx/sdk-aws/aws/globalaccelerator/custom_routing_accelerator";

    export default stack("payments", () => {
      intent({ summary: "payments own custom routing accelerator" });

      resource(CustomRoutingAccelerator, "ci-runner", {
        name: "ci-runner",
        enabled: true,
        ipAddressType: "example",
        ipAddresses: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.globalaccelerator import CustomRoutingAccelerator, CustomRoutingAcceleratorConfig

    def describe():
        ubx.intent("payments own custom routing accelerator")

        ubx.resource(CustomRoutingAccelerator, "ci-runner", CustomRoutingAcceleratorConfig(
            name="ci-runner",
            enabled=True,
            ip_address_type="example",
            ip_addresses=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_globalaccelerator_custom_routing_accelerator called
    "ci-runner", with enabled true, ip address type example, ip
    addresses a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attributes" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="flow_logs_enabled" type="bool">
    </ResponseField>
    <ResponseField name="flow_logs_s3_bucket" type="string">
    </ResponseField>
    <ResponseField name="flow_logs_s3_prefix" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip_address_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="ip_addresses" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="attributes" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="flow_logs_enabled" type="bool">
    </ResponseField>
    <ResponseField name="flow_logs_s3_bucket" type="string">
    </ResponseField>
    <ResponseField name="flow_logs_s3_prefix" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dns_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hosted_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ip_sets" type="list(object)">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="ip_addresses" type="list(string)">
    </ResponseField>
    <ResponseField name="ip_family" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
