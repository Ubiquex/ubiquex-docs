---
title: "aws_globalaccelerator_listener"
description: "Real, generated bindings for `aws_globalaccelerator_listener`."
---

`aws_globalaccelerator_listener` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own listener"})

    		ubx.Resource(globalaccelerator.Listener, "ci-runner", globalaccelerator.ListenerConfig{
    			AcceleratorArn: "arn:aws:iam::123456789012:policy/ci",
    			Protocol:       "example",
    			ClientAffinity: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Listener } from "jsr:@ubx/sdk-aws/aws/globalaccelerator/listener";

    export default stack("payments", () => {
      intent({ summary: "payments own listener" });

      resource(Listener, "ci-runner", {
        acceleratorArn: "arn:aws:iam::123456789012:policy/ci",
        protocol: "example",
        clientAffinity: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.globalaccelerator import Listener, ListenerConfig

    def describe():
        ubx.intent("payments own listener")

        ubx.resource(Listener, "ci-runner", ListenerConfig(
            accelerator_arn="arn:aws:iam::123456789012:policy/ci",
            protocol="example",
            client_affinity="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_globalaccelerator_listener, with accelerator arn
    arn:aws:iam::123456789012:policy/ci, protocol example, client
    affinity example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accelerator_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_affinity" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
