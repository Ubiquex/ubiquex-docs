---
title: "aws_globalaccelerator_listener"
description: "Real, generated bindings for `aws_globalaccelerator_listener`."
---

`aws_globalaccelerator_listener` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"

    cfg := globalaccelerator.ListenerConfig{
        AcceleratorArn: "example",
        Protocol: "example",
        ClientAffinity: "example",
    }
    ubx.Resource(globalaccelerator.Listener, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Listener } from
      "jsr:@ubx/sdk-aws/aws/globalaccelerator/listener";

    resource(Listener, "example", {
      acceleratorArn: "example",
      protocol: "example",
      clientAffinity: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.globalaccelerator import Listener, ListenerConfig

    cfg = ListenerConfig(
        accelerator_arn="example",
        protocol="example",
        client_affinity="example",
    )
    ubx.resource(Listener, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_globalaccelerator_listener with accelerator_arn,
protocol, client_affinity.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accelerator_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_affinity" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
