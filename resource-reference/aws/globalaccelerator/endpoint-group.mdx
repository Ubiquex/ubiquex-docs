---
title: "aws_globalaccelerator_endpoint_group"
description: "Real, generated bindings for `aws_globalaccelerator_endpoint_group`."
---

`aws_globalaccelerator_endpoint_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint group"})

    		ubx.Resource(globalaccelerator.EndpointGroup, "ci-runner", globalaccelerator.EndpointGroupConfig{
    			ListenerArn:                "arn:aws:iam::123456789012:policy/ci",
    			HealthCheckIntervalSeconds: 7200,
    			HealthCheckProtocol:        "example",
    			ThresholdCount:             1,
    			TrafficDialPercentage:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointGroup } from "jsr:@ubx/sdk-aws/aws/globalaccelerator/endpoint_group";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint group" });

      resource(EndpointGroup, "ci-runner", {
        listenerArn: "arn:aws:iam::123456789012:policy/ci",
        healthCheckIntervalSeconds: 7200,
        healthCheckProtocol: "example",
        thresholdCount: 1,
        trafficDialPercentage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.globalaccelerator import EndpointGroup, EndpointGroupConfig

    def describe():
        ubx.intent("payments own endpoint group")

        ubx.resource(EndpointGroup, "ci-runner", EndpointGroupConfig(
            listener_arn="arn:aws:iam::123456789012:policy/ci",
            health_check_interval_seconds=7200,
            health_check_protocol="example",
            threshold_count=1,
            traffic_dial_percentage=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_globalaccelerator_endpoint_group, with listener arn
    arn:aws:iam::123456789012:policy/ci, health check interval
    seconds 7200, health check protocol example, threshold count 1,
    traffic dial percentage 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="endpoint_group_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="health_check_interval_seconds" type="number">
  Optional.
</ResponseField>

<ResponseField name="health_check_path" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="health_check_port" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="health_check_protocol" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="listener_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="threshold_count" type="number">
  Optional.
</ResponseField>

<ResponseField name="traffic_dial_percentage" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_group_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="health_check_path" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="health_check_port" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
