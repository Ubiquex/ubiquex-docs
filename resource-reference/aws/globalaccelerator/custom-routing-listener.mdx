---
title: "aws_globalaccelerator_custom_routing_listener"
description: "Real, generated bindings for `aws_globalaccelerator_custom_routing_listener`."
---

`aws_globalaccelerator_custom_routing_listener` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/globalaccelerator"

    cfg := globalaccelerator.CustomRoutingListenerConfig{
        AcceleratorArn: "example",
    }
    ubx.Resource(
        globalaccelerator.CustomRoutingListener,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CustomRoutingListener } from
      "jsr:@ubx/sdk-aws/aws/globalaccelerator/custom_routing_listener";

    resource(CustomRoutingListener, "example", {
      acceleratorArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.globalaccelerator import (
        CustomRoutingListener,
        CustomRoutingListenerConfig,
    )

    cfg = CustomRoutingListenerConfig(
        accelerator_arn="example",
    )
    ubx.resource(CustomRoutingListener, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_globalaccelerator_custom_routing_listener with
accelerator_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accelerator_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
