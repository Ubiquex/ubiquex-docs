---
title: "aws_globalaccelerator_custom_routing_endpoint_group"
description: "Real, generated bindings for `aws_globalaccelerator_custom_routing_endpoint_group`."
---

`aws_globalaccelerator_custom_routing_endpoint_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"

    cfg := globalaccelerator.CustomRoutingEndpointGroupConfig{
        ListenerArn: "example",
        EndpointGroupRegion: "example",
    }
    ubx.Resource(
        globalaccelerator.CustomRoutingEndpointGroup,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CustomRoutingEndpointGroup } from
      "jsr:@ubx/sdk-aws/aws/globalaccelerator/custom_routing_endpoint_group";

    resource(CustomRoutingEndpointGroup, "example", {
      listenerArn: "example",
      endpointGroupRegion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.globalaccelerator import (
        CustomRoutingEndpointGroup,
        CustomRoutingEndpointGroupConfig,
    )

    cfg = CustomRoutingEndpointGroupConfig(
        listener_arn="example",
        endpoint_group_region="example",
    )
    ubx.resource(CustomRoutingEndpointGroup, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_globalaccelerator_custom_routing_endpoint_group
with listener_arn, endpoint_group_region.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="endpoint_group_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="listener_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_group_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
