---
title: "aws_globalaccelerator_custom_routing_endpoint_group"
description: "Real, generated bindings for `aws_globalaccelerator_custom_routing_endpoint_group`."
---

`aws_globalaccelerator_custom_routing_endpoint_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own custom routing endpoint group"})

    		ubx.Resource(globalaccelerator.CustomRoutingEndpointGroup, "ci-runner", globalaccelerator.CustomRoutingEndpointGroupConfig{
    			ListenerArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CustomRoutingEndpointGroup } from "jsr:@ubx/sdk-aws/aws/globalaccelerator/custom_routing_endpoint_group";

    export default stack("payments", () => {
      intent({ summary: "payments own custom routing endpoint group" });

      resource(CustomRoutingEndpointGroup, "ci-runner", {
        listenerArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.globalaccelerator import CustomRoutingEndpointGroup, CustomRoutingEndpointGroupConfig

    def describe():
        ubx.intent("payments own custom routing endpoint group")

        ubx.resource(CustomRoutingEndpointGroup, "ci-runner", CustomRoutingEndpointGroupConfig(
            listener_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_globalaccelerator_custom_routing_endpoint_group, with
    listener arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="endpoint_group_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="listener_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_group_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
