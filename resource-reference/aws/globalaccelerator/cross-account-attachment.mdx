---
title: "aws_globalaccelerator_cross_account_attachment"
description: "Real, generated bindings for `aws_globalaccelerator_cross_account_attachment`."
---

`aws_globalaccelerator_cross_account_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/globalaccelerator"

    cfg := globalaccelerator.CrossAccountAttachmentConfig{
        Name: "example-name",
        Principals: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        globalaccelerator.CrossAccountAttachment,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CrossAccountAttachment } from
      "jsr:@ubx/sdk-aws/aws/globalaccelerator/cross_account_attachment";

    resource(CrossAccountAttachment, "example", {
      name: "example",
      principals: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.globalaccelerator import (
        CrossAccountAttachment,
        CrossAccountAttachmentConfig,
    )

    cfg = CrossAccountAttachmentConfig(
        name="example",
        principals=["example"],
        tags={"key": "value"},
    )
    ubx.resource(CrossAccountAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_globalaccelerator_cross_account_attachment with
name, principals, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principals" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
