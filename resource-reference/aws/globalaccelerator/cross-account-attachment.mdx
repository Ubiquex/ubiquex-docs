---
title: "aws_globalaccelerator_cross_account_attachment"
description: "Real, generated bindings for `aws_globalaccelerator_cross_account_attachment`."
---

`aws_globalaccelerator_cross_account_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	globalaccelerator "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/globalaccelerator"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cross account attachment"})

    		ubx.Resource(globalaccelerator.CrossAccountAttachment, "ci-runner", globalaccelerator.CrossAccountAttachmentConfig{
    			Name:       "ci-runner",
    			Principals: []string{"example"},
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CrossAccountAttachment } from "jsr:@ubx/sdk-aws/aws/globalaccelerator/cross_account_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own cross account attachment" });

      resource(CrossAccountAttachment, "ci-runner", {
        name: "ci-runner",
        principals: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.globalaccelerator import CrossAccountAttachment, CrossAccountAttachmentConfig

    def describe():
        ubx.intent("payments own cross account attachment")

        ubx.resource(CrossAccountAttachment, "ci-runner", CrossAccountAttachmentConfig(
            name="ci-runner",
            principals=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_globalaccelerator_cross_account_attachment called
    "ci-runner", with principals a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principals" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
