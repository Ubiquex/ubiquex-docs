---
title: "aws_route53_recovery_control_routing_control"
description: "`aws_route53_recovery_control_routing_control` creates a real, individual on/off switch within an `aws_route53_recovery_control_control_panel`..."
---

`aws_route53_recovery_control_routing_control` creates a real, individual on/off switch within an `aws_route53_recovery_control_control_panel`, the resource an operator or an automated health check actually toggles during failover to redirect traffic.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53recoverycontrol "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/route53recoverycontrol"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own routing control"})

    		ubx.Resource(route53recoverycontrol.RoutingControl, "example", route53recoverycontrol.RoutingControlConfig{
    			Name:            "example",
    			ClusterArn:      "arn:aws:iam::123456789012:policy/example",
    			ControlPanelArn: "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoutingControl } from "@ubx/sdk-aws/aws/route53recoverycontrol/routing_control";

    export default stack("example", () => {
      intent({ summary: "example own routing control" });

      resource(RoutingControl, "example", {
        name: "example",
        clusterArn: "arn:aws:iam::123456789012:policy/example",
        controlPanelArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53recoverycontrol import RoutingControl, RoutingControlConfig

    def describe():
        ubx.intent("example own routing control")

        ubx.resource(RoutingControl, "example", RoutingControlConfig(
            name="example",
            cluster_arn="arn:aws:iam::123456789012:policy/example",
            control_panel_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53_recovery_control_routing_control called
    "example", with cluster arn
    arn:aws:iam::123456789012:policy/example, control panel arn
    arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_arn" type="string">
  Arn associated with Control Panel Optional.
</ResponseField>

<ResponseField name="control_panel_arn" type="string">
  The Amazon Resource Name (ARN) of the control panel. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the routing control. You can use any non-white space character in the name. Required.
</ResponseField>

## Output properties

<ResponseField name="routing_control_arn" type="string">
  The Amazon Resource Name (ARN) of the routing control. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The deployment status of the routing control. Status can be one of the following: PENDING, DEPLOYED, PENDING_DELETION. Computed by AWS after `ship`.
</ResponseField>
