---
title: "aws_internet_gateway_attachment"
description: "Real, generated bindings for `aws_internet_gateway_attachment`."
---

`aws_internet_gateway_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/internet"

    cfg := internet.GatewayAttachmentConfig{
        InternetGatewayId: "example",
        VpcId: "example",
    }
    ubx.Resource(internet.GatewayAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayAttachment } from
      "jsr:@ubx/sdk-aws/aws/internet/gateway_attachment";

    resource(GatewayAttachment, "example", {
      internetGatewayId: "example",
      vpcId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.internet import (
        GatewayAttachment,
        GatewayAttachmentConfig,
    )

    cfg = GatewayAttachmentConfig(
        internet_gateway_id="example",
        vpc_id="example",
    )
    ubx.resource(GatewayAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_internet_gateway_attachment with
internet_gateway_id, vpc_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="internet_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
