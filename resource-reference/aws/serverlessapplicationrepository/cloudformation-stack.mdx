---
title: "aws_serverlessapplicationrepository_cloudformation_stack"
description: "Real, generated bindings for `aws_serverlessapplicationrepository_cloudformation_stack`."
---

`aws_serverlessapplicationrepository_cloudformation_stack` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	serverlessapplicationrepository "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/serverlessapplicationrepository"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cloudformation stack"})

    		ubx.Resource(serverlessapplicationrepository.CloudformationStack, "ci-runner", serverlessapplicationrepository.CloudformationStackConfig{
    			ApplicationId: "example",
    			Name:          "ci-runner",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudformationStack } from "jsr:@ubx/sdk-aws/aws/serverlessapplicationrepository/cloudformation_stack";

    export default stack("payments", () => {
      intent({ summary: "payments own cloudformation stack" });

      resource(CloudformationStack, "ci-runner", {
        applicationId: "example",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.serverlessapplicationrepository import CloudformationStack, CloudformationStackConfig

    def describe():
        ubx.intent("payments own cloudformation stack")

        ubx.resource(CloudformationStack, "ci-runner", CloudformationStackConfig(
            application_id="example",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_serverlessapplicationrepository_cloudformation_stack
    called "ci-runner", with application id example, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="capabilities" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="semantic_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="capabilities" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="outputs" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="parameters" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="semantic_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
