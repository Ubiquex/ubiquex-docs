---
title: "aws_macie2_invitation_accepter"
description: "Real, generated bindings for `aws_macie2_invitation_accepter`."
---

`aws_macie2_invitation_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/macie2"

    cfg := macie2.InvitationAccepterConfig{
        AdministratorAccountId: "example",
    }
    ubx.Resource(macie2.InvitationAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { InvitationAccepter } from
      "jsr:@ubx/sdk-aws/aws/macie2/invitation_accepter";

    resource(InvitationAccepter, "example", {
      administratorAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.macie2 import (
        InvitationAccepter,
        InvitationAccepterConfig,
    )

    cfg = InvitationAccepterConfig(
        administrator_account_id="example",
    )
    ubx.resource(InvitationAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_macie2_invitation_accepter with
administrator_account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="administrator_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invitation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
