---
title: "aws_macie2_invitation_accepter"
description: "Real, generated bindings for `aws_macie2_invitation_accepter`."
---

`aws_macie2_invitation_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own invitation accepter"})

    		ubx.Resource(macie2.InvitationAccepter, "ci-runner", macie2.InvitationAccepterConfig{
    			AdministratorAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InvitationAccepter } from "jsr:@ubx/sdk-aws/aws/macie2/invitation_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own invitation accepter" });

      resource(InvitationAccepter, "ci-runner", {
        administratorAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.macie2 import InvitationAccepter, InvitationAccepterConfig

    def describe():
        ubx.intent("payments own invitation accepter")

        ubx.resource(InvitationAccepter, "ci-runner", InvitationAccepterConfig(
            administrator_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_macie2_invitation_accepter, with administrator account
    id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="administrator_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invitation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
