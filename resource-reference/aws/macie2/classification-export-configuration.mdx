---
title: "aws_macie2_classification_export_configuration"
description: "Real, generated bindings for `aws_macie2_classification_export_configuration`."
---

`aws_macie2_classification_export_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	macie2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/macie2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own classification export configuration"})

    		ubx.Resource(macie2.ClassificationExportConfiguration, "ci-runner", macie2.ClassificationExportConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClassificationExportConfiguration } from "jsr:@ubx/sdk-aws/aws/macie2/classification_export_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own classification export configuration" });

      resource(ClassificationExportConfiguration, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.macie2 import ClassificationExportConfiguration, ClassificationExportConfigurationConfig

    def describe():
        ubx.intent("payments own classification export configuration")

        ubx.resource(ClassificationExportConfiguration, "ci-runner", ClassificationExportConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_macie2_classification_export_configuration, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
