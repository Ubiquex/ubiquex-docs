---
title: "aws_route53profiles_association"
description: "Real, generated bindings for `aws_route53profiles_association`."
---

`aws_route53profiles_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53profiles"

    cfg := route53profiles.AssociationConfig{
        Name: "example-name",
        ProfileId: "example",
        ResourceId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(route53profiles.Association, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Association } from
      "jsr:@ubx/sdk-aws/aws/route53profiles/association";

    resource(Association, "example", {
      name: "example",
      profileId: "example",
      resourceId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.route53profiles import (
        Association,
        AssociationConfig,
    )

    cfg = AssociationConfig(
        name="example",
        profile_id="example",
        resource_id="example",
        tags={"key": "value"},
    )
    ubx.resource(Association, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_route53profiles_association with name, profile_id,
resource_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="profile_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
