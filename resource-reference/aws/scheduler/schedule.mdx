---
title: "aws_scheduler_schedule"
description: "Real, generated bindings for `aws_scheduler_schedule`."
---

`aws_scheduler_schedule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scheduler "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/scheduler"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own schedule"})

    		ubx.Resource(scheduler.Schedule, "ci-runner", scheduler.ScheduleConfig{
    			ScheduleExpression:         "example",
    			Name:                       "ci-runner",
    			Description:                "Managed by ubx.",
    			EndDate:                    "example",
    			KmsKeyArn:                  "arn:aws:iam::123456789012:policy/ci",
    			ScheduleExpressionTimezone: "example",
    			StartDate:                  "example",
    			State:                      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Schedule } from "jsr:@ubx/sdk-aws/aws/scheduler/schedule";

    export default stack("payments", () => {
      intent({ summary: "payments own schedule" });

      resource(Schedule, "ci-runner", {
        scheduleExpression: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        endDate: "example",
        kmsKeyArn: "arn:aws:iam::123456789012:policy/ci",
        scheduleExpressionTimezone: "example",
        startDate: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.scheduler import Schedule, ScheduleConfig

    def describe():
        ubx.intent("payments own schedule")

        ubx.resource(Schedule, "ci-runner", ScheduleConfig(
            schedule_expression="example",
            name="ci-runner",
            description="Managed by ubx.",
            end_date="example",
            kms_key_arn="arn:aws:iam::123456789012:policy/ci",
            schedule_expression_timezone="example",
            start_date="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_scheduler_schedule called "ci-runner", with schedule
    expression example, description Managed by ubx., end date
    example, kms key arn arn:aws:iam::123456789012:policy/ci,
    schedule expression timezone example, start date example, state
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action_after_completion" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="end_date" type="string">
  Optional.
</ResponseField>

<ResponseField name="group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule_expression" type="string" required>
  Required.
</ResponseField>

<ResponseField name="schedule_expression_timezone" type="string">
  Optional.
</ResponseField>

<ResponseField name="start_date" type="string">
  Optional.
</ResponseField>

<ResponseField name="state" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="action_after_completion" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
