---
title: "aws_cur_report_definition"
description: "Real, generated bindings for `aws_cur_report_definition`."
---

`aws_cur_report_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cur"

    cfg := cur.ReportDefinitionConfig{
        AdditionalSchemaElements: []string{"example"},
        Compression: "example",
        Format: "example",
        ReportName: "example-report-name",
        S3Bucket: "example",
        S3Prefix: "example",
        S3Region: "example",
        TimeUnit: "example",
        AdditionalArtifacts: []string{"example"},
        RefreshClosedReports: true,
    }
    ubx.Resource(cur.ReportDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ReportDefinition } from
      "jsr:@ubx/sdk-aws/aws/cur/report_definition";

    resource(ReportDefinition, "example", {
      additionalSchemaElements: ["example"],
      compression: "example",
      format: "example",
      reportName: "example",
      s3Bucket: "example",
      s3Prefix: "example",
      s3Region: "example",
      timeUnit: "example",
      additionalArtifacts: ["example"],
      refreshClosedReports: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cur import (
        ReportDefinition,
        ReportDefinitionConfig,
    )

    cfg = ReportDefinitionConfig(
        additional_schema_elements=["example"],
        compression="example",
        format="example",
        report_name="example",
        s3_bucket="example",
        s3_prefix="example",
        s3_region="example",
        time_unit="example",
        additional_artifacts=["example"],
        refresh_closed_reports=True,
    )
    ubx.resource(ReportDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cur_report_definition with
additional_schema_elements, compression, format, report_name,
s3_bucket, s3_prefix, s3_region, time_unit, additional_artifacts,
refresh_closed_reports.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="additional_artifacts" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="additional_schema_elements" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="compression" type="string" required>
  Required.
</ResponseField>

<ResponseField name="format" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="refresh_closed_reports" type="bool">
  Optional.
</ResponseField>

<ResponseField name="report_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="report_versioning" type="string">
  Optional.
</ResponseField>

<ResponseField name="s3_bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_prefix" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="time_unit" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
