---
title: "aws_codebuild_resource_policy"
description: "Real, generated bindings for `aws_codebuild_resource_policy`."
---

`aws_codebuild_resource_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/codebuild"

    cfg := codebuild.ResourcePolicyConfig{
        Policy: "example",
        ResourceArn: "example",
    }
    ubx.Resource(codebuild.ResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourcePolicy } from
      "jsr:@ubx/sdk-aws/aws/codebuild/resource_policy";

    resource(ResourcePolicy, "example", {
      policy: "example",
      resourceArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codebuild import (
        ResourcePolicy,
        ResourcePolicyConfig,
    )

    cfg = ResourcePolicyConfig(
        policy="example",
        resource_arn="example",
    )
    ubx.resource(ResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codebuild_resource_policy with policy,
resource_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
