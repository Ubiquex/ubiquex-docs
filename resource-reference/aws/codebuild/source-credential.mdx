---
title: "aws_codebuild_source_credential"
description: "Real, generated bindings for `aws_codebuild_source_credential`."
---

`aws_codebuild_source_credential` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source credential"})

    		ubx.Resource(codebuild.SourceCredential, "ci-runner", codebuild.SourceCredentialConfig{
    			AuthType:   "example",
    			ServerType: "example",
    			Token:      "example",
    			UserName:   "example-user-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceCredential } from "jsr:@ubx/sdk-aws/aws/codebuild/source_credential";

    export default stack("payments", () => {
      intent({ summary: "payments own source credential" });

      resource(SourceCredential, "ci-runner", {
        authType: "example",
        serverType: "example",
        token: "example",
        userName: "example-user-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codebuild import SourceCredential, SourceCredentialConfig

    def describe():
        ubx.intent("payments own source credential")

        ubx.resource(SourceCredential, "ci-runner", SourceCredentialConfig(
            auth_type="example",
            server_type="example",
            token="example",
            user_name="example-user-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codebuild_source_credential, with auth type example,
    server type example, token example, user name example-user-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auth_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="token" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
