---
title: "aws_codebuild_source_credential"
description: "Real, generated bindings for `aws_codebuild_source_credential`."
---

`aws_codebuild_source_credential` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/codebuild"

    cfg := codebuild.SourceCredentialConfig{
        AuthType: "example",
        ServerType: "example",
        Token: "example",
        UserName: "example-user-name",
    }
    ubx.Resource(codebuild.SourceCredential, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SourceCredential } from
      "jsr:@ubx/sdk-aws/aws/codebuild/source_credential";

    resource(SourceCredential, "example", {
      authType: "example",
      serverType: "example",
      token: "example",
      userName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codebuild import (
        SourceCredential,
        SourceCredentialConfig,
    )

    cfg = SourceCredentialConfig(
        auth_type="example",
        server_type="example",
        token="example",
        user_name="example",
    )
    ubx.resource(SourceCredential, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codebuild_source_credential with auth_type,
server_type, token, user_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auth_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="server_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="token" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
