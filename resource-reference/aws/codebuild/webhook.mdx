---
title: "aws_codebuild_webhook"
description: "Real, generated bindings for `aws_codebuild_webhook`."
---

`aws_codebuild_webhook` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codebuild"

    cfg := codebuild.WebhookConfig{
        ProjectName: "example-project-name",
        BranchFilter: "example",
        BuildType: "example",
    }
    ubx.Resource(codebuild.Webhook, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Webhook } from "jsr:@ubx/sdk-aws/aws/codebuild/webhook";

    resource(Webhook, "example", {
      projectName: "example",
      branchFilter: "example",
      buildType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.codebuild import Webhook, WebhookConfig

    cfg = WebhookConfig(
        project_name="example",
        branch_filter="example",
        build_type="example",
    )
    ubx.resource(Webhook, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_codebuild_webhook with project_name, branch_filter,
build_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="branch_filter" type="string">
  Optional.
</ResponseField>

<ResponseField name="build_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="manual_creation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="payload_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
