---
title: "aws_codebuild_webhook"
description: "Real, generated bindings for `aws_codebuild_webhook`."
---

`aws_codebuild_webhook` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	codebuild "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/codebuild"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own webhook"})

    		ubx.Resource(codebuild.Webhook, "ci-runner", codebuild.WebhookConfig{
    			ProjectName:    "example-project-name",
    			BranchFilter:   "example",
    			BuildType:      "example",
    			ManualCreation: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Webhook } from "jsr:@ubx/sdk-aws/aws/codebuild/webhook";

    export default stack("payments", () => {
      intent({ summary: "payments own webhook" });

      resource(Webhook, "ci-runner", {
        projectName: "example-project-name",
        branchFilter: "example",
        buildType: "example",
        manualCreation: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.codebuild import Webhook, WebhookConfig

    def describe():
        ubx.intent("payments own webhook")

        ubx.resource(Webhook, "ci-runner", WebhookConfig(
            project_name="example-project-name",
            branch_filter="example",
            build_type="example",
            manual_creation=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_codebuild_webhook, with project name
    example-project-name, branch filter example, build type example,
    manual creation true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="branch_filter" type="string">
  Optional.
</ResponseField>

<ResponseField name="build_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="manual_creation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="project_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="payload_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by AWS after `ship`.
</ResponseField>
