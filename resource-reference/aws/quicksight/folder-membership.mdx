---
title: "aws_quicksight_folder_membership"
description: "Real, generated bindings for `aws_quicksight_folder_membership`."
---

`aws_quicksight_folder_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/quicksight"

    cfg := quicksight.FolderMembershipConfig{
        FolderId: "example",
        MemberId: "example",
        MemberType: "example",
        AwsAccountId: "example",
    }
    ubx.Resource(quicksight.FolderMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FolderMembership } from
      "jsr:@ubx/sdk-aws/aws/quicksight/folder_membership";

    resource(FolderMembership, "example", {
      folderId: "example",
      memberId: "example",
      memberType: "example",
      awsAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.quicksight import (
        FolderMembership,
        FolderMembershipConfig,
    )

    cfg = FolderMembershipConfig(
        folder_id="example",
        member_id="example",
        member_type="example",
        aws_account_id="example",
    )
    ubx.resource(FolderMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_quicksight_folder_membership with folder_id,
member_id, member_type, aws_account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="folder_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
