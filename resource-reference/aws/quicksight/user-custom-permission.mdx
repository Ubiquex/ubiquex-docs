---
title: "aws_quicksight_user_custom_permission"
description: "Real, generated bindings for `aws_quicksight_user_custom_permission`."
---

`aws_quicksight_user_custom_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user custom permission"})

    		ubx.Resource(quicksight.UserCustomPermission, "ci-runner", quicksight.UserCustomPermissionConfig{
    			CustomPermissionsName: "example-custom-permissions-name",
    			UserName:              "example-user-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserCustomPermission } from "jsr:@ubx/sdk-aws/aws/quicksight/user_custom_permission";

    export default stack("payments", () => {
      intent({ summary: "payments own user custom permission" });

      resource(UserCustomPermission, "ci-runner", {
        customPermissionsName: "example-custom-permissions-name",
        userName: "example-user-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import UserCustomPermission, UserCustomPermissionConfig

    def describe():
        ubx.intent("payments own user custom permission")

        ubx.resource(UserCustomPermission, "ci-runner", UserCustomPermissionConfig(
            custom_permissions_name="example-custom-permissions-name",
            user_name="example-user-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_user_custom_permission, with custom
    permissions name example-custom-permissions-name, user name
    example-user-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="custom_permissions_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="namespace" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
