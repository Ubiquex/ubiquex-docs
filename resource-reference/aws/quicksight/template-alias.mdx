---
title: "aws_quicksight_template_alias"
description: "Real, generated bindings for `aws_quicksight_template_alias`."
---

`aws_quicksight_template_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own template alias"})

    		ubx.Resource(quicksight.TemplateAlias, "ci-runner", quicksight.TemplateAliasConfig{
    			AliasName:             "example-alias-name",
    			TemplateId:            "example",
    			TemplateVersionNumber: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TemplateAlias } from "jsr:@ubx/sdk-aws/aws/quicksight/template_alias";

    export default stack("payments", () => {
      intent({ summary: "payments own template alias" });

      resource(TemplateAlias, "ci-runner", {
        aliasName: "example-alias-name",
        templateId: "example",
        templateVersionNumber: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import TemplateAlias, TemplateAliasConfig

    def describe():
        ubx.intent("payments own template alias")

        ubx.resource(TemplateAlias, "ci-runner", TemplateAliasConfig(
            alias_name="example-alias-name",
            template_id="example",
            template_version_number=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_template_alias, with alias name
    example-alias-name, template id example, template version number
    1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alias_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="template_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="template_version_number" type="number" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
