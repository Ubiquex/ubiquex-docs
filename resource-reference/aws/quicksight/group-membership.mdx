---
title: "aws_quicksight_group_membership"
description: "Real, generated bindings for `aws_quicksight_group_membership`."
---

`aws_quicksight_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group membership"})

    		ubx.Resource(quicksight.GroupMembership, "ci-runner", quicksight.GroupMembershipConfig{
    			GroupName:  "example-group-name",
    			MemberName: "example-member-name",
    			Namespace:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupMembership } from "jsr:@ubx/sdk-aws/aws/quicksight/group_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own group membership" });

      resource(GroupMembership, "ci-runner", {
        groupName: "example-group-name",
        memberName: "example-member-name",
        namespace: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import GroupMembership, GroupMembershipConfig

    def describe():
        ubx.intent("payments own group membership")

        ubx.resource(GroupMembership, "ci-runner", GroupMembershipConfig(
            group_name="example-group-name",
            member_name="example-member-name",
            namespace="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_group_membership, with group name
    example-group-name, member name example-member-name, namespace
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="member_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
