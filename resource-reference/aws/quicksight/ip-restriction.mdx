---
title: "aws_quicksight_ip_restriction"
description: "Real, generated bindings for `aws_quicksight_ip_restriction`."
---

`aws_quicksight_ip_restriction` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ip restriction"})

    		ubx.Resource(quicksight.IpRestriction, "ci-runner", quicksight.IpRestrictionConfig{
    			Enabled:                         true,
    			IpRestrictionRuleMap:            map[string]string{"managed-by": "ubx"},
    			VpcEndpointIdRestrictionRuleMap: map[string]string{"managed-by": "ubx"},
    			VpcIdRestrictionRuleMap:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpRestriction } from "jsr:@ubx/sdk-aws/aws/quicksight/ip_restriction";

    export default stack("payments", () => {
      intent({ summary: "payments own ip restriction" });

      resource(IpRestriction, "ci-runner", {
        enabled: true,
        ipRestrictionRuleMap: { "managed-by": "ubx" },
        vpcEndpointIdRestrictionRuleMap: { "managed-by": "ubx" },
        vpcIdRestrictionRuleMap: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import IpRestriction, IpRestrictionConfig

    def describe():
        ubx.intent("payments own ip restriction")

        ubx.resource(IpRestriction, "ci-runner", IpRestrictionConfig(
            enabled=True,
            ip_restriction_rule_map={"managed-by": "ubx"},
            vpc_endpoint_id_restriction_rule_map={"managed-by": "ubx"},
            vpc_id_restriction_rule_map={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_ip_restriction, with enabled true, ip
    restriction rule map a set value, vpc endpoint id restriction
    rule map a set value, vpc id restriction rule map a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="ip_restriction_rule_map" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id_restriction_rule_map" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_id_restriction_rule_map" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
