---
title: "aws_quicksight_user"
description: "Real, generated bindings for `aws_quicksight_user`."
---

`aws_quicksight_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user"})

    		ubx.Resource(quicksight.User, "ci-runner", quicksight.UserConfig{
    			Email:        "example",
    			IdentityType: "example",
    			UserRole:     "example",
    			IamArn:       "arn:aws:iam::123456789012:policy/ci",
    			Namespace:    "example",
    			SessionName:  "example-session-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "jsr:@ubx/sdk-aws/aws/quicksight/user";

    export default stack("payments", () => {
      intent({ summary: "payments own user" });

      resource(User, "ci-runner", {
        email: "example",
        identityType: "example",
        userRole: "example",
        iamArn: "arn:aws:iam::123456789012:policy/ci",
        namespace: "example",
        sessionName: "example-session-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import User, UserConfig

    def describe():
        ubx.intent("payments own user")

        ubx.resource(User, "ci-runner", UserConfig(
            email="example",
            identity_type="example",
            user_role="example",
            iam_arn="arn:aws:iam::123456789012:policy/ci",
            namespace="example",
            session_name="example-session-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_user, with email example, identity type
    example, user role example, iam arn
    arn:aws:iam::123456789012:policy/ci, namespace example, session
    name example-session-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="email" type="string" required>
  Required.
</ResponseField>

<ResponseField name="iam_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_role" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_invitation_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
