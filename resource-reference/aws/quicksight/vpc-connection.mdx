---
title: "aws_quicksight_vpc_connection"
description: "Real, generated bindings for `aws_quicksight_vpc_connection`."
---

`aws_quicksight_vpc_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"

    cfg := quicksight.VpcConnectionConfig{
        Name: "example-name",
        RoleArn: "example",
        SecurityGroupIds: []string{"example"},
        SubnetIds: []string{"example"},
        VpcConnectionId: "example",
        AwsAccountId: "example",
        DnsResolvers: []string{"example"},
    }
    ubx.Resource(quicksight.VpcConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VpcConnection } from
      "jsr:@ubx/sdk-aws/aws/quicksight/vpc_connection";

    resource(VpcConnection, "example", {
      name: "example",
      roleArn: "example",
      securityGroupIds: ["example"],
      subnetIds: ["example"],
      vpcConnectionId: "example",
      awsAccountId: "example",
      dnsResolvers: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.quicksight import (
        VpcConnection,
        VpcConnectionConfig,
    )

    cfg = VpcConnectionConfig(
        name="example",
        role_arn="example",
        security_group_ids=["example"],
        subnet_ids=["example"],
        vpc_connection_id="example",
        aws_account_id="example",
        dns_resolvers=["example"],
    )
    ubx.resource(VpcConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_quicksight_vpc_connection with name, role_arn,
security_group_ids, subnet_ids, vpc_connection_id,
aws_account_id, dns_resolvers.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="dns_resolvers" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
