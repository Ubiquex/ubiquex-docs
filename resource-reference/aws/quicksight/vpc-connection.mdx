---
title: "aws_quicksight_vpc_connection"
description: "Real, generated bindings for `aws_quicksight_vpc_connection`."
---

`aws_quicksight_vpc_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc connection"})

    		ubx.Resource(quicksight.VpcConnection, "ci-runner", quicksight.VpcConnectionConfig{
    			Name:             "ci-runner",
    			RoleArn:          "arn:aws:iam::123456789012:policy/ci",
    			SecurityGroupIds: []string{"example"},
    			SubnetIds:        []string{"example"},
    			VpcConnectionId:  "example",
    			DnsResolvers:     []string{"example"},
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcConnection } from "jsr:@ubx/sdk-aws/aws/quicksight/vpc_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc connection" });

      resource(VpcConnection, "ci-runner", {
        name: "ci-runner",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        securityGroupIds: ["example"],
        subnetIds: ["example"],
        vpcConnectionId: "example",
        dnsResolvers: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import VpcConnection, VpcConnectionConfig

    def describe():
        ubx.intent("payments own vpc connection")

        ubx.resource(VpcConnection, "ci-runner", VpcConnectionConfig(
            name="ci-runner",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            security_group_ids=["example"],
            subnet_ids=["example"],
            vpc_connection_id="example",
            dns_resolvers=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_vpc_connection called "ci-runner", with
    role arn arn:aws:iam::123456789012:policy/ci, security group ids
    a set value, subnet ids a set value, vpc connection id example,
    dns resolvers a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="dns_resolvers" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
