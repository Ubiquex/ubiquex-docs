---
title: "aws_quicksight_refresh_schedule"
description: "Real, generated bindings for `aws_quicksight_refresh_schedule`."
---

`aws_quicksight_refresh_schedule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/quicksight"

    cfg := quicksight.RefreshScheduleConfig{
        DataSetId: "example",
        ScheduleId: "example",
        AwsAccountId: "example",
    }
    ubx.Resource(quicksight.RefreshSchedule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RefreshSchedule } from
      "jsr:@ubx/sdk-aws/aws/quicksight/refresh_schedule";

    resource(RefreshSchedule, "example", {
      dataSetId: "example",
      scheduleId: "example",
      awsAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.quicksight import (
        RefreshSchedule,
        RefreshScheduleConfig,
    )

    cfg = RefreshScheduleConfig(
        data_set_id="example",
        schedule_id="example",
        aws_account_id="example",
    )
    ubx.resource(RefreshSchedule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_quicksight_refresh_schedule with data_set_id,
schedule_id, aws_account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="data_set_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
