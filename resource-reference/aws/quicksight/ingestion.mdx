---
title: "aws_quicksight_ingestion"
description: "Real, generated bindings for `aws_quicksight_ingestion`."
---

`aws_quicksight_ingestion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/quicksight"

    cfg := quicksight.IngestionConfig{
        DataSetId: "example",
        IngestionId: "example",
        IngestionType: "example",
        AwsAccountId: "example",
    }
    ubx.Resource(quicksight.Ingestion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Ingestion } from
      "jsr:@ubx/sdk-aws/aws/quicksight/ingestion";

    resource(Ingestion, "example", {
      dataSetId: "example",
      ingestionId: "example",
      ingestionType: "example",
      awsAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.quicksight import Ingestion, IngestionConfig

    cfg = IngestionConfig(
        data_set_id="example",
        ingestion_id="example",
        ingestion_type="example",
        aws_account_id="example",
    )
    ubx.resource(Ingestion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_quicksight_ingestion with data_set_id,
ingestion_id, ingestion_type, aws_account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="data_set_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ingestion_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
