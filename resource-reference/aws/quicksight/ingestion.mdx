---
title: "aws_quicksight_ingestion"
description: "Real, generated bindings for `aws_quicksight_ingestion`."
---

`aws_quicksight_ingestion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ingestion"})

    		ubx.Resource(quicksight.Ingestion, "ci-runner", quicksight.IngestionConfig{
    			DataSetId:     "example",
    			IngestionId:   "example",
    			IngestionType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Ingestion } from "jsr:@ubx/sdk-aws/aws/quicksight/ingestion";

    export default stack("payments", () => {
      intent({ summary: "payments own ingestion" });

      resource(Ingestion, "ci-runner", {
        dataSetId: "example",
        ingestionId: "example",
        ingestionType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import Ingestion, IngestionConfig

    def describe():
        ubx.intent("payments own ingestion")

        ubx.resource(Ingestion, "ci-runner", IngestionConfig(
            data_set_id="example",
            ingestion_id="example",
            ingestion_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_ingestion, with data set id example,
    ingestion id example, ingestion type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="data_set_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ingestion_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
