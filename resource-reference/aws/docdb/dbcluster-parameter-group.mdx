---
title: "aws_doc_db_dbcluster_parameter_group"
description: "`aws_doc_db_dbcluster_parameter_group` sets real, engine-level configuration for an `aws_doc_db_dbcluster`..."
---

`aws_doc_db_dbcluster_parameter_group` sets real, engine-level configuration for an `aws_doc_db_dbcluster`, kept as its own resource so the same parameter set can be applied consistently across multiple clusters.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	docdb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/docdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dbcluster parameter group"})

    		ubx.Resource(docdb.DbclusterParameterGroup, "example", docdb.DbclusterParameterGroupConfig{
    			Description: "Managed by ubx.",
    			Family:      "example",
    			Parameters:  "example",
    			Name:        "example",
    			Tags:        []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbclusterParameterGroup } from "@ubx/sdk-aws/aws/docdb/dbcluster_parameter_group";

    export default stack("example", () => {
      intent({ summary: "example own dbcluster parameter group" });

      resource(DbclusterParameterGroup, "example", {
        description: "Managed by ubx.",
        family: "example",
        parameters: "example",
        name: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.docdb import DbclusterParameterGroup, DbclusterParameterGroupConfig
    from ubx.aws.docdb.dbcluster_parameter_group import DbclusterParameterGroup_Tags

    def describe():
        ubx.intent("example own dbcluster parameter group")

        ubx.resource(DbclusterParameterGroup, "example", DbclusterParameterGroupConfig(
            description="Managed by ubx.",
            family="example",
            parameters="example",
            name="example",
            tags=[DbclusterParameterGroup_Tags(key="example")],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_doc_db_dbcluster_parameter_group called "example",
    with description Managed by ubx., family example, parameters
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string" required>
  The description for the DB cluster parameter group. Required.
</ResponseField>

<ResponseField name="family" type="string" required>
  The DB cluster parameter group family name (e.g. docdb5.0). Required.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the DB cluster parameter group. If omitted, CloudFormation generates a unique name. The name is stored as lowercase. Optional.
</ResponseField>

<ResponseField name="parameters" type="any" required>
  An object containing key-value pairs of parameters to set for the DB cluster parameter group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a key-value tag attached to the DocumentDB cluster parameter group, used to identify and categorize the resource within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag attached to the DocumentDB cluster parameter group, which together with the tag key forms a key-value label used to categorize and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
