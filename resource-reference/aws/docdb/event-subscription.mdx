---
title: "aws_doc_db_event_subscription"
description: "`aws_doc_db_event_subscription` subscribes a real SNS topic to receive notifications about events, failovers, backups..."
---

`aws_doc_db_event_subscription` subscribes a real SNS topic to receive notifications about events, failovers, backups, occurring on an `aws_doc_db_dbcluster` and its instances.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	docdb "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/docdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own event subscription"})

    		ubx.Resource(docdb.EventSubscription, "example", docdb.EventSubscriptionConfig{
    			SnsTopicArn:      "arn:aws:iam::123456789012:policy/example",
    			Enabled:          true,
    			EventCategories:  []string{"example"},
    			SourceIds:        []string{"example"},
    			SourceType:       "example",
    			SubscriptionName: "example-subscription-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventSubscription } from "@ubx/sdk-aws/aws/docdb/event_subscription";

    export default stack("example", () => {
      intent({ summary: "example own event subscription" });

      resource(EventSubscription, "example", {
        snsTopicArn: "arn:aws:iam::123456789012:policy/example",
        enabled: true,
        eventCategories: ["example"],
        sourceIds: ["example"],
        sourceType: "example",
        subscriptionName: "example-subscription-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.docdb import EventSubscription, EventSubscriptionConfig

    def describe():
        ubx.intent("example own event subscription")

        ubx.resource(EventSubscription, "example", EventSubscriptionConfig(
            sns_topic_arn="arn:aws:iam::123456789012:policy/example",
            enabled=True,
            event_categories=["example"],
            source_ids=["example"],
            source_type="example",
            subscription_name="example-subscription-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_doc_db_event_subscription, with sns topic arn
    arn:aws:iam::123456789012:policy/example, enabled true, event
    categories a set value, source ids a set value, source type
    example, subscription name example-subscription-name.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="enabled" type="bool">
  A Boolean value; set to true to activate the subscription, set to false to create the subscription but not active it. Optional.
</ResponseField>

<ResponseField name="event_categories" type="list(string)">
  A list of event categories for a SourceType that you want to subscribe to. Optional.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string" required>
  The Amazon Resource Name (ARN) of the SNS topic created for event notification. Amazon SNS creates the ARN when you create a topic and subscribe to it. Required.
</ResponseField>

<ResponseField name="source_ids" type="list(string)">
  The list of identifiers of the event sources for which events are returned Optional.
</ResponseField>

<ResponseField name="source_type" type="string">
  The type of source that is generating the events. Optional.
</ResponseField>

<ResponseField name="subscription_name" type="string">
  The name of the subscription. Optional.
</ResponseField>
