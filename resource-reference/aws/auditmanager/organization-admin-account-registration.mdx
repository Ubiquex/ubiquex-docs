---
title: "aws_auditmanager_organization_admin_account_registration"
description: "Real, generated bindings for `aws_auditmanager_organization_admin_account_registration`."
---

`aws_auditmanager_organization_admin_account_registration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization admin account registration"})

    		ubx.Resource(auditmanager.OrganizationAdminAccountRegistration, "ci-runner", auditmanager.OrganizationAdminAccountRegistrationConfig{
    			AdminAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationAdminAccountRegistration } from "jsr:@ubx/sdk-aws/aws/auditmanager/organization_admin_account_registration";

    export default stack("payments", () => {
      intent({ summary: "payments own organization admin account registration" });

      resource(OrganizationAdminAccountRegistration, "ci-runner", {
        adminAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.auditmanager import OrganizationAdminAccountRegistration, OrganizationAdminAccountRegistrationConfig

    def describe():
        ubx.intent("payments own organization admin account registration")

        ubx.resource(OrganizationAdminAccountRegistration, "ci-runner", OrganizationAdminAccountRegistrationConfig(
            admin_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_auditmanager_organization_admin_account_registration,
    with admin account id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="admin_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="organization_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
