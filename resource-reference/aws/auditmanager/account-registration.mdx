---
title: "aws_auditmanager_account_registration"
description: "Real, generated bindings for `aws_auditmanager_account_registration`."
---

`aws_auditmanager_account_registration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/auditmanager"

    cfg := auditmanager.AccountRegistrationConfig{
        DelegatedAdminAccount: "example",
        DeregisterOnDestroy: true,
    }
    ubx.Resource(auditmanager.AccountRegistration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountRegistration } from
      "jsr:@ubx/sdk-aws/aws/auditmanager/account_registration";

    resource(AccountRegistration, "example", {
      delegatedAdminAccount: "example",
      deregisterOnDestroy: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.auditmanager import (
        AccountRegistration,
        AccountRegistrationConfig,
    )

    cfg = AccountRegistrationConfig(
        delegated_admin_account="example",
        deregister_on_destroy=True,
    )
    ubx.resource(AccountRegistration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_auditmanager_account_registration with
delegated_admin_account, deregister_on_destroy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delegated_admin_account" type="string">
  Optional.
</ResponseField>

<ResponseField name="deregister_on_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
