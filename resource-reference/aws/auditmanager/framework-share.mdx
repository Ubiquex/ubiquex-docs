---
title: "aws_auditmanager_framework_share"
description: "Real, generated bindings for `aws_auditmanager_framework_share`."
---

`aws_auditmanager_framework_share` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"

    cfg := auditmanager.FrameworkShareConfig{
        DestinationAccount: "example",
        DestinationRegion: "example",
        FrameworkId: "example",
        Comment: "example",
    }
    ubx.Resource(auditmanager.FrameworkShare, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FrameworkShare } from
      "jsr:@ubx/sdk-aws/aws/auditmanager/framework_share";

    resource(FrameworkShare, "example", {
      destinationAccount: "example",
      destinationRegion: "example",
      frameworkId: "example",
      comment: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.auditmanager import (
        FrameworkShare,
        FrameworkShareConfig,
    )

    cfg = FrameworkShareConfig(
        destination_account="example",
        destination_region="example",
        framework_id="example",
        comment="example",
    )
    ubx.resource(FrameworkShare, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_auditmanager_framework_share with
destination_account, destination_region, framework_id, comment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="destination_account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="framework_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
