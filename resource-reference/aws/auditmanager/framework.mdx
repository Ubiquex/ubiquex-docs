---
title: "aws_auditmanager_framework"
description: "Real, generated bindings for `aws_auditmanager_framework`."
---

`aws_auditmanager_framework` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/auditmanager"

    cfg := auditmanager.FrameworkConfig{
        Name: "example-name",
        ComplianceType: "example",
        Description: "example",
    }
    ubx.Resource(auditmanager.Framework, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Framework } from
      "jsr:@ubx/sdk-aws/aws/auditmanager/framework";

    resource(Framework, "example", {
      name: "example",
      complianceType: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.auditmanager import Framework, FrameworkConfig

    cfg = FrameworkConfig(
        name="example",
        compliance_type="example",
        description="example",
    )
    ubx.resource(Framework, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_auditmanager_framework with name, compliance_type,
description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="compliance_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="framework_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
