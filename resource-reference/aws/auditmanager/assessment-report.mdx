---
title: "aws_auditmanager_assessment_report"
description: "Real, generated bindings for `aws_auditmanager_assessment_report`."
---

`aws_auditmanager_assessment_report` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"

    cfg := auditmanager.AssessmentReportConfig{
        AssessmentId: "example",
        Name: "example-name",
        Description: "example",
    }
    ubx.Resource(auditmanager.AssessmentReport, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AssessmentReport } from
      "jsr:@ubx/sdk-aws/aws/auditmanager/assessment_report";

    resource(AssessmentReport, "example", {
      assessmentId: "example",
      name: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.auditmanager import (
        AssessmentReport,
        AssessmentReportConfig,
    )

    cfg = AssessmentReportConfig(
        assessment_id="example",
        name="example",
        description="example",
    )
    ubx.resource(AssessmentReport, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_auditmanager_assessment_report with assessment_id,
name, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="assessment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="author" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
