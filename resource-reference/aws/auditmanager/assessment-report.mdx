---
title: "aws_auditmanager_assessment_report"
description: "Real, generated bindings for `aws_auditmanager_assessment_report`."
---

`aws_auditmanager_assessment_report` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own assessment report"})

    		ubx.Resource(auditmanager.AssessmentReport, "ci-runner", auditmanager.AssessmentReportConfig{
    			AssessmentId: "example",
    			Name:         "ci-runner",
    			Description:  "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AssessmentReport } from "jsr:@ubx/sdk-aws/aws/auditmanager/assessment_report";

    export default stack("payments", () => {
      intent({ summary: "payments own assessment report" });

      resource(AssessmentReport, "ci-runner", {
        assessmentId: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.auditmanager import AssessmentReport, AssessmentReportConfig

    def describe():
        ubx.intent("payments own assessment report")

        ubx.resource(AssessmentReport, "ci-runner", AssessmentReportConfig(
            assessment_id="example",
            name="ci-runner",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_auditmanager_assessment_report called "ci-runner",
    with assessment id example, description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="assessment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="author" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
