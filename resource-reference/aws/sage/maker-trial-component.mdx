---
title: "aws_sage_maker_trial_component"
description: "`aws_sage_maker_trial_component` records a real, individual tracked step, a training job, processing job, or transform job..."
---

`aws_sage_maker_trial_component` records a real, individual tracked step, a training job, processing job, or transform job, within an `aws_sage_maker_experiment`, capturing its parameters, metrics, and artifacts for later comparison.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker trial component"})

    		ubx.Resource(sage.MakerTrialComponent, "example", sage.MakerTrialComponentConfig{
    			TrialComponentName: "example-trial-component-name",
    			DisplayName:        "example-display-name",
    			InputArtifacts:     "example",
    			MetadataProperties: map[string]any{"commit_id": "example"},
    			OutputArtifacts:    "example",
    			Parameters:         "example",
    			Status:             map[string]any{"message": "example"},
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerTrialComponent } from "./local-sdk/aws/sdk/typescript/aws/sage/maker_trial_component.ts";

    export default stack("example", () => {
      intent({ summary: "example own maker trial component" });

      resource(MakerTrialComponent, "example", {
        trialComponentName: "example-trial-component-name",
        displayName: "example-display-name",
        inputArtifacts: "example",
        metadataProperties: { commitId: "example" },
        outputArtifacts: "example",
        parameters: "example",
        status: { message: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerTrialComponent, MakerTrialComponentConfig

    def describe():
        ubx.intent("example own maker trial component")

        ubx.resource(MakerTrialComponent, "example", MakerTrialComponentConfig(
            trial_component_name="example-trial-component-name",
            display_name="example-display-name",
            input_artifacts="example",
            metadata_properties={"commit_id": "example"},
            output_artifacts="example",
            parameters="example",
            status={"message": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_trial_component, with trial component name
    example-trial-component-name, display name example-display-name,
    input artifacts example, metadata properties a set value, output
    artifacts example, parameters example, status a set value, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="display_name" type="string">
  The name of the component as displayed. If DisplayName isn't specified, TrialComponentName is displayed. Optional.
</ResponseField>

<ResponseField name="input_artifacts" type="any">
  The input artifacts for the component. Optional.
</ResponseField>

<ResponseField name="metadata_properties" type="object">
  Metadata properties of the tracking entity, trial, or trial component. Optional.
  <Expandable title="properties">
    <ResponseField name="commit_id" type="string">
      The commit ID for the sage maker trial component's metadata properties. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="generated_by" type="string">
      The entity this entity was generated by. Optional.
    </ResponseField>
    <ResponseField name="project_id" type="string">
      The project ID. Optional.
    </ResponseField>
    <ResponseField name="repository" type="string">
      The repository. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="output_artifacts" type="any">
  The output artifacts for the component. Optional.
</ResponseField>

<ResponseField name="parameters" type="any">
  The hyperparameters for the component. Optional.
</ResponseField>

<ResponseField name="status" type="object">
  The status of the trial component. Optional.
  <Expandable title="properties">
    <ResponseField name="message" type="string">
      If the component failed, a message describing why. Optional.
    </ResponseField>
    <ResponseField name="primary_status" type="string">
      The status of the trial component. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tags to associate with the trial component. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag applied to the SageMaker trial component, used to categorize resources for cost allocation and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag applied to the SageMaker trial component, forming a key-value pair used to organize, identify, and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trial_component_name" type="string" required>
  The name of the trial component. Must be unique in your AWS account and is not case-sensitive. Required.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  When the trial component was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  When the trial component was last modified. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="lineage_group_arn" type="string">
  The Amazon Resource Name (ARN) of the lineage group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="trial_component_arn" type="string">
  The Amazon Resource Name (ARN) of the trial component. Computed by AWS after `ship`.
</ResponseField>
