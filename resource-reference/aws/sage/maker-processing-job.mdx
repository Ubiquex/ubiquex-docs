---
title: "aws_sage_maker_processing_job"
description: "`aws_sage_maker_processing_job` runs a real, standalone data processing or model evaluation job on managed infrastructure, for data preprocessing..."
---

`aws_sage_maker_processing_job` runs a real, standalone data processing or model evaluation job on managed infrastructure, for data preprocessing, feature engineering, or post-training evaluation, either on its own or as a step invoked from within an `aws_sage_maker_pipeline`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker processing job"})

    		ubx.Resource(sage.MakerProcessingJob, "example", sage.MakerProcessingJobConfig{
    			AppSpecification:    map[string]any{"image_uri": "example"},
    			ProcessingResources: map[string]any{"cluster_config": map[string]any{"instance_count": 1}},
    			RoleArn:             "arn:aws:iam::123456789012:policy/example",
    			Environment:         "example",
    			ExperimentConfig:    map[string]any{"experiment_name": "example-experiment-name"},
    			NetworkConfig:       map[string]any{"enable_inter_container_traffic_encryption": true},
    			ProcessingInputs:    []map[string]any{{"app_managed": true}},
    			ProcessingJobName:   "example-processing-job-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerProcessingJob } from "@ubx/sdk-aws/aws/sage/maker_processing_job";

    export default stack("example", () => {
      intent({ summary: "example own maker processing job" });

      resource(MakerProcessingJob, "example", {
        appSpecification: { imageUri: "example" },
        processingResources: { clusterConfig: { instanceCount: 1 } },
        roleArn: "arn:aws:iam::123456789012:policy/example",
        environment: "example",
        experimentConfig: { experimentName: "example-experiment-name" },
        networkConfig: { enableInterContainerTrafficEncryption: true },
        processingInputs: [{ appManaged: true }],
        processingJobName: "example-processing-job-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerProcessingJob, MakerProcessingJobConfig

    def describe():
        ubx.intent("example own maker processing job")

        ubx.resource(MakerProcessingJob, "example", MakerProcessingJobConfig(
            app_specification={"image_uri": "example"},
            processing_resources={"cluster_config": {"instance_count": 1}},
            role_arn="arn:aws:iam::123456789012:policy/example",
            environment="example",
            experiment_config={"experiment_name": "example-experiment-name"},
            network_config={"enable_inter_container_traffic_encryption": True},
            processing_inputs=[{"app_managed": True}],
            processing_job_name="example-processing-job-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_processing_job, with app specification a
    set value, processing resources a set value, role arn
    arn:aws:iam::123456789012:policy/example, environment example,
    experiment config a set value, network config a set value,
    processing inputs a set value, processing job name
    example-processing-job-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_specification" type="object" required>
  Configures the processing job to run a specified Docker container image. Required.
  <Expandable title="properties">
    <ResponseField name="container_arguments" type="list(string)">
      The arguments for a container used to run a processing job. Optional.
    </ResponseField>
    <ResponseField name="container_entrypoint" type="list(string)">
      The entrypoint for a container used to run a processing job. Optional.
    </ResponseField>
    <ResponseField name="image_uri" type="string" required>
      The container image to be run by the processing job. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="environment" type="any">
  Sets the environment variables in the Docker container Optional.
</ResponseField>

<ResponseField name="experiment_config" type="object">
  Associates a SageMaker job as a trial component with an experiment and trial. Optional.
  <Expandable title="properties">
    <ResponseField name="experiment_name" type="string">
      The name of an existing experiment to associate with the trial component. Optional.
    </ResponseField>
    <ResponseField name="run_name" type="string">
      The name of the experiment run to associate with the trial component. Optional.
    </ResponseField>
    <ResponseField name="trial_component_display_name" type="string">
      The display name for the trial component. If this key isn't specified, the display name is the trial component name. Optional.
    </ResponseField>
    <ResponseField name="trial_name" type="string">
      The name of an existing trial to associate the trial component with. If not specified, a new trial is created. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_config" type="object">
  Networking options for a job, such as network traffic encryption between containers, whether to allow inbound and outbound network calls to and from containers, and the VPC subnets and security groups to use for VPC-enabled jobs. Optional.
  <Expandable title="properties">
    <ResponseField name="enable_inter_container_traffic_encryption" type="bool">
      Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt communications. Encryption provides greater security for distributed processing jobs, but the processing might take longer. Optional.
    </ResponseField>
    <ResponseField name="enable_network_isolation" type="bool">
      Whether to allow inbound and outbound network calls to and from the containers used for the processing job. Optional.
    </ResponseField>
    <ResponseField name="vpc_config" type="object">
      Specifies an Amazon Virtual Private Cloud (VPC) that your SageMaker jobs, hosted models, and compute resources have access to. You can control access to and from your resources by configuring a VPC. For more information, see https://docs.aws.amazon.com/sagemaker/latest/dg/infrastructure-give-access.html Optional.
      <Expandable title="properties">
        <ResponseField name="security_group_ids" type="list(string)" required>
          The VPC security group IDs, in the form 'sg-xxxxxxxx'. Specify the security groups for the VPC that is specified in the 'Subnets' field. Required.
        </ResponseField>
        <ResponseField name="subnets" type="list(string)" required>
          The ID of the subnets in the VPC to which you want to connect your training job or model. For information about the availability of specific instance types, see https://docs.aws.amazon.com/sagemaker/latest/dg/regions-quotas.html Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="processing_inputs" type="list(object)">
  An array of inputs configuring the data to download into the processing container. Optional.
  <Expandable title="properties">
    <ResponseField name="app_managed" type="bool">
      Indicates whether the processing input is managed by the processing application (true) rather than sourced from Amazon S3 (false). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dataset_definition" type="object">
      Defines how to generate input data for the processing job by querying Athena or Redshift and storing the result in S3, according to the specified database, SQL query, and output location. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="athena_dataset_definition" type="object">
          Configures an Athena dataset for the processing job input, specifying the Athena catalog, database, SQL query, query result output location, and format (e.g., PARQUET or CSV) that define the data to be processed. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="catalog" type="string">
              The name of the AWS Glue data catalog that contains the database and table referenced by the Athena query used in this processing job's dataset definition (e.g., 'AwsDataCatalog'). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="database" type="string">
              The name of the Athena database used to query the dataset for this processing job's dataset definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_id" type="string">
              The AWS KMS key ID that SageMaker uses to encrypt the Athena query results, which are stored in the S3 output location defined in the AthenaDatasetDefinition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="output_compression" type="string">
              Specifies the compression format (e.g., GZIP, SNAPPY) applied to the Athena query results stored in the output S3 location for this dataset definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="output_format" type="string">
              Specifies the data format (e.g., PARQUET, ORC, AVRO, JSON, TEXTFILE) in which the Athena query results are written to the output S3 location for the processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="output_s3_uri" type="string">
              The S3 URI (e.g., s3://bucket/path) where the results of the Athena query used to generate the dataset are written, as part of the At, denaDatasetDefinition for the processing job's dataset input. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="query_string" type="string">
              The SQL query executed against Athena to select the dataset used as input for the SageMaker processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="work_group" type="string">
              The name of the Athena workgroup to use for the query that reads data from Athena. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="data_distribution_type" type="string">
          Specifies how the input data is distributed across the processing job instances, either 'FullyReplicated' to give every instance a full copy of the data or 'ShardedByS3Object' to partition the S3 objects among the instances. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="input_mode" type="string">
          Specifies the input mode (File or Pipe) for the dataset definition when the processing job input is defined as a dataset, determining how data is streamed or copied to the processing container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="local_path" type="string">
          The local path on the processing instance where the dataset defined by the Athena or Redshift dataset definition is made available for use by the processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="redshift_dataset_definition" type="object">
          Configures an Amazon Redshift dataset for the SageMaker processing job input by specifying the Redshift cluster, database, user, query string, and S3 output location for query results. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cluster_id" type="string">
              The identifier of the Amazon Redshift cluster that contains the dataset to be processed by the SageMaker processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="cluster_role_arn" type="string">
              The Amazon Resource Name (ARN) of the IAM role that the Redshift cluster assumes to execute the query and write the resulting dataset to the S3 output location defined in the RedshiftDatasetDefinition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="database" type="string">
              The name of the Redshift database that contains the data to be queried for the processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="db_user" type="string">
              Specifies the Redshift database user name used to authenticate the query against the cluster for this dataset definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="kms_key_id" type="string">
              The AWS KMS key ID that Amazon SageMaker uses to encrypt the data retrieved from the Redshift query before writing it to the output S3 location. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="output_compression" type="string">
              Specifies the compression type (for example, NONE, GZIP, BZIP2, or ZSTD) applied to the output data from the Redshift query when it is stored in Amazon S3 for a SageMaker processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="output_format" type="string">
              Specifies the format (PARQUET or CSV) in which the results of the Redshift query are written to the output S3 location for this processing job's dataset definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="output_s3_uri" type="string">
              Specifies the S3 URI where the results of the Redshift query are stored, from which the SageMaker Processing job reads the dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="query_string" type="string">
              SQL query that defines the data to be fetched from Amazon Redshift for processing. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="input_name" type="string">
      The name of this processing input, which defines the input channel path inside the processing container (e.g., /opt/ml/processing/input/&lt;input_name&gt;) and must match the channel name expected by the processing script. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="s3_input" type="object">
      Specifies the S3 input source for the processing job, including the S3 URI, local path, data type (e.g., S3Prefix or ManifestFile), and input mode (File or Pipe). Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="local_path" type="string">
          The local path in the container instance where the S3 input data is made available for the processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_compression_type" type="string">
          Specifies the compression type of the S3 input data for the processing job, either 'None' for uncompressed or 'Gzip' for gzip-compressed data, which Amazon SageMaker uses to decompress the input before processing. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_data_distribution_type" type="string">
          Determines how the input data from S3 is distributed among the instances of the processing job: either 'FullyReplicated' (each instance gets a copy of the entire data) or 'ShardedByS3Key' (each instance gets a distinct subset of S3 objects). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_data_type" type="string">
          The type of S3 data source, either a prefix (S3Prefix) or a manifest file (ManifestFile), used to locate the input data for the processing job. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_input_mode" type="string">
          Specifies the input mode for the S3 data source of this processing job's input channel, either 'File' or 'Pipe', which controls how the data is streamed to the processing container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_uri" type="string">
          The S3 URI (e.g., s3://bucket/prefix) of the input data that SageMaker reads for this processing input. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="processing_job_name" type="string">
  The name of the processing job. The name must be unique within an AWS Region in the AWS account. Optional.
</ResponseField>

<ResponseField name="processing_output_config" type="object">
  Configuration for uploading output from the processing container. Optional.
  <Expandable title="properties">
    <ResponseField name="kms_key_id" type="string">
      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the processing job output. KmsKeyId can be an ID of a KMS key, ARN of a KMS key, or alias of a KMS key. The KmsKeyId is applied to all outputs. Optional.
    </ResponseField>
    <ResponseField name="outputs" type="list(object)" required>
      An array of outputs configuring the data to upload from the processing container. Required.
      <Expandable title="properties">
        <ResponseField name="app_managed" type="bool">
          When set to true, all output operations such as data encryption are managed by the processing job's application rather than by SageMaker; when false (the default), SageMaker manages these operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="feature_store_output" type="object">
          Configures the processing job's output to an Amazon SageMaker Feature Store, specifying the target feature group name and whether to append or overwrite existing records. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="feature_group_name" type="string">
              The name of the SageMaker Feature Store feature group that this processing job's output is written to and ingested into. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="output_name" type="string">
          The unique name that identifies this processing output within the SageMaker processing job's output configuration, used to reference the output in the job's specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_output" type="object">
          Configures the S3 destination for a processing job output, including the local path in the container and the S3 URI (and optionally the upload mode) where the output is uploaded. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="local_path" type="string">
              The local path inside the processing container where the output data is written before being uploaded to the configured S3 output location. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="s3_upload_mode" type="string">
              Determines whether the processing job uploads output to S3 continuously during execution ('Continuous') or only after the job completes ('EndOfJob'). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="s3_uri" type="string">
              The S3 URI specifying the bucket and prefix where the processing job writes its output data. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="processing_resources" type="object" required>
  Identifies the resources, ML compute instances, and ML storage volumes to deploy for a processing job. In distributed training, you specify more than one instance. Required.
  <Expandable title="properties">
    <ResponseField name="cluster_config" type="object" required>
      Configuration for the cluster used to run a processing job. Required.
      <Expandable title="properties">
        <ResponseField name="instance_count" type="number" required>
          The number of ML compute instances to use in the processing job. For distributed processing jobs, specify a value greater than 1. The default value is 1. Required.
        </ResponseField>
        <ResponseField name="instance_type" type="string" required>
          The ML compute instance type for the processing job. Required.
        </ResponseField>
        <ResponseField name="volume_kms_key_id" type="string">
          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume attached to the ML compute instance(s) that run the processing job. Optional.
        </ResponseField>
        <ResponseField name="volume_size_in_gb" type="number" required>
          The size of the ML storage volume in gigabytes that you want to provision. You must specify sufficient ML storage for your scenario. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your behalf. Required.
</ResponseField>

<ResponseField name="stopping_condition" type="object">
  Configures conditions under which the processing job should be stopped, such as how long the processing job has been running. After the condition is met, the processing job is stopped. Optional.
  <Expandable title="properties">
    <ResponseField name="max_runtime_in_seconds" type="number" required>
      Specifies the maximum runtime in seconds. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  (Optional) An array of key-value pairs. For more information, see Using Cost Allocation Tags(https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/cost-alloc-tags.html#allocation-whatURL) in the AWS Billing and Cost Management User Guide. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key portion of a tag that you assign to the SageMaker processing job, used to identify, organize, and control access to the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag applied to the SageMaker processing job, paired with the corresponding key to organize and identify AWS resources (e.g., for cost allocation or access control). Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="auto_mljob_arn" type="string">
  The ARN of an AutoML job associated with this processing job. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  The time at which the processing job was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="exit_message" type="string">
  An optional string, up to one KB in size, that contains metadata from the processing container when the processing job exits. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  A string, up to one KB in size, that contains the reason a processing job failed, if it failed. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The time at which the processing job was last modified. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="monitoring_schedule_arn" type="string">
  The ARN of a monitoring schedule for an endpoint associated with this processing job. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="processing_end_time" type="string">
  The time at which the processing job completed. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="processing_job_arn" type="string">
  The Amazon Resource Name (ARN) of the processing job. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="processing_job_status" type="string">
  Provides the status of a processing job. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="processing_start_time" type="string">
  The time at which the processing job started. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="training_job_arn" type="string">
  The ARN of a training job associated with this processing job Computed by AWS after `ship`.
</ResponseField>
