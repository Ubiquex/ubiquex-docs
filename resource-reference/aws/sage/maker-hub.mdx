---
title: "aws_sage_maker_hub"
description: "`aws_sage_maker_hub` creates a real private SageMaker JumpStart hub, a curated..."
---

`aws_sage_maker_hub` creates a real private SageMaker JumpStart hub, a curated, organization-scoped catalog of foundation models and pretrained models that mirrors JumpStart's own public model hub but under an account's own governance and access controls.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker hub"})

    		ubx.Resource(sage.MakerHub, "example", sage.MakerHubConfig{
    			HubDescription:    "example",
    			HubName:           "example-hub-name",
    			HubDisplayName:    "example-hub-display-name",
    			HubSearchKeywords: []string{"example"},
    			S3StorageConfig:   map[string]any{"s3_output_path": "example"},
    			Tags:              []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerHub } from "./local-sdk/aws/sdk/typescript/aws/sage/maker_hub.ts";

    export default stack("example", () => {
      intent({ summary: "example own maker hub" });

      resource(MakerHub, "example", {
        hubDescription: "example",
        hubName: "example-hub-name",
        hubDisplayName: "example-hub-display-name",
        hubSearchKeywords: ["example"],
        s3StorageConfig: { s3OutputPath: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerHub, MakerHubConfig

    def describe():
        ubx.intent("example own maker hub")

        ubx.resource(MakerHub, "example", MakerHubConfig(
            hub_description="example",
            hub_name="example-hub-name",
            hub_display_name="example-hub-display-name",
            hub_search_keywords=["example"],
            s3_storage_config={"s3_output_path": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_hub, with hub description example, hub name
    example-hub-name, hub display name example-hub-display-name, hub
    search keywords a set value, s3 storage config a set value, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="hub_description" type="string" required>
  A description of the hub. Required.
</ResponseField>

<ResponseField name="hub_display_name" type="string">
  The display name of the hub. Optional.
</ResponseField>

<ResponseField name="hub_name" type="string" required>
  The name of the hub. Required.
</ResponseField>

<ResponseField name="hub_search_keywords" type="list(string)">
  The searchable keywords for the hub. Optional.
</ResponseField>

<ResponseField name="s3_storage_config" type="object">
  The Amazon S3 storage configuration for the hub. Optional.
  <Expandable title="properties">
    <ResponseField name="s3_output_path" type="string">
      The Amazon S3 bucket prefix for hosting hub content. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Tags to associate with the hub. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the SageMaker Hub, used to identify and categorize the hub for resource management and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag attached to the SageMaker Hub, which pairs with the corresponding key to organize, identify, and manage the hub resource within your AWS environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The date and time that the hub was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hub_arn" type="string">
  The Amazon Resource Name (ARN) of the hub. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hub_status" type="string">
  The status of the hub. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The date and time that the hub was last modified. Computed by AWS after `ship`.
</ResponseField>
