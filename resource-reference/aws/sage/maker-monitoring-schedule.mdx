---
title: "aws_sage_maker_monitoring_schedule"
description: "`aws_sage_maker_monitoring_schedule` schedules a real, recurring run of a data quality, model quality, bias..."
---

`aws_sage_maker_monitoring_schedule` schedules a real, recurring run of a data quality, model quality, bias, or explainability job definition against an `aws_sage_maker_endpoint`'s live traffic, the resource that actually turns any of those job definitions into an ongoing monitor.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker monitoring schedule"})

    		ubx.Resource(sage.MakerMonitoringSchedule, "example", sage.MakerMonitoringScheduleConfig{
    			MonitoringScheduleConfig:       map[string]any{"monitoring_job_definition": map[string]any{"monitoring_app_specification": map[string]any{"image_uri": "example"}}},
    			MonitoringScheduleName:         "example-monitoring-schedule-name",
    			EndpointName:                   "example-endpoint-name",
    			FailureReason:                  "example",
    			LastMonitoringExecutionSummary: map[string]any{"creation_time": "example"},
    			MonitoringScheduleStatus:       "example",
    			Tags:                           []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerMonitoringSchedule } from "@ubx/sdk-aws/aws/sage/maker_monitoring_schedule";

    export default stack("example", () => {
      intent({ summary: "example own maker monitoring schedule" });

      resource(MakerMonitoringSchedule, "example", {
        monitoringScheduleConfig: {
          monitoringJobDefinition: {
            monitoringAppSpecification: { imageUri: "example" },
          },
        },
        monitoringScheduleName: "example-monitoring-schedule-name",
        endpointName: "example-endpoint-name",
        failureReason: "example",
        lastMonitoringExecutionSummary: { creationTime: "example" },
        monitoringScheduleStatus: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerMonitoringSchedule, MakerMonitoringScheduleConfig

    def describe():
        ubx.intent("example own maker monitoring schedule")

        ubx.resource(MakerMonitoringSchedule, "example", MakerMonitoringScheduleConfig(
            monitoring_schedule_config={"monitoring_job_definition": {"monitoring_app_specification": {"image_uri": "example"}}},
            monitoring_schedule_name="example-monitoring-schedule-name",
            endpoint_name="example-endpoint-name",
            failure_reason="example",
            last_monitoring_execution_summary={"creation_time": "example"},
            monitoring_schedule_status="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_monitoring_schedule, with monitoring
    schedule config a set value, monitoring schedule name
    example-monitoring-schedule-name, endpoint name
    example-endpoint-name, failure reason example, last monitoring
    execution summary a set value, monitoring schedule status
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="endpoint_name" type="string">
  The name of the endpoint used to run the monitoring job. Optional.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  Contains the reason a monitoring job failed, if it failed. Optional.
</ResponseField>

<ResponseField name="last_monitoring_execution_summary" type="object">
  Summary of information about monitoring job Optional.
  <Expandable title="properties">
    <ResponseField name="creation_time" type="string" required>
      The time at which the monitoring job was created. Required.
    </ResponseField>
    <ResponseField name="endpoint_name" type="string">
      The name of the endpoint used to run the monitoring job. Optional.
    </ResponseField>
    <ResponseField name="failure_reason" type="string">
      Contains the reason a monitoring job failed, if it failed. Optional.
    </ResponseField>
    <ResponseField name="last_modified_time" type="string" required>
      A timestamp that indicates the last time the monitoring job was modified. Required.
    </ResponseField>
    <ResponseField name="monitoring_execution_status" type="string" required>
      The status of the monitoring job. Required.
    </ResponseField>
    <ResponseField name="monitoring_schedule_name" type="string" required>
      The name of the monitoring schedule. Required.
    </ResponseField>
    <ResponseField name="processing_job_arn" type="string">
      The Amazon Resource Name (ARN) of the monitoring job. Optional.
    </ResponseField>
    <ResponseField name="scheduled_time" type="string" required>
      The time the monitoring job was scheduled. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="monitoring_schedule_config" type="object" required>
  The configuration object that specifies the monitoring schedule and defines the monitoring job. Required.
  <Expandable title="properties">
    <ResponseField name="monitoring_job_definition" type="object">
      Defines the monitoring job. Optional.
      <Expandable title="properties">
        <ResponseField name="baseline_config" type="object">
          Baseline configuration used to validate that the data conforms to the specified constraints and statistics. Optional.
          <Expandable title="properties">
            <ResponseField name="constraints_resource" type="object">
              The baseline constraints resource for a monitoring job. Optional.
              <Expandable title="properties">
                <ResponseField name="s3_uri" type="string">
                  The Amazon S3 URI. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="statistics_resource" type="object">
              The baseline statistics resource for a monitoring job. Optional.
              <Expandable title="properties">
                <ResponseField name="s3_uri" type="string">
                  The Amazon S3 URI. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="environment" type="any">
          Sets the environment variables in the Docker container Optional.
        </ResponseField>
        <ResponseField name="monitoring_app_specification" type="object" required>
          Container image configuration object for the monitoring job. Required.
          <Expandable title="properties">
            <ResponseField name="container_arguments" type="list(string)">
              An array of arguments for the container used to run the monitoring job. Optional.
            </ResponseField>
            <ResponseField name="container_entrypoint" type="list(string)">
              Specifies the entrypoint for a container used to run the monitoring job. Optional.
            </ResponseField>
            <ResponseField name="image_uri" type="string" required>
              The container image to be run by the monitoring job. Required.
            </ResponseField>
            <ResponseField name="post_analytics_processor_source_uri" type="string">
              The Amazon S3 URI. Optional.
            </ResponseField>
            <ResponseField name="record_preprocessor_source_uri" type="string">
              The Amazon S3 URI. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="monitoring_inputs" type="list(object)" required>
          The array of inputs for the monitoring job. Required.
          <Expandable title="properties">
            <ResponseField name="batch_transform_input" type="object">
              Specifies the batch transform input for a SageMaker monitoring job, including the S3 data source and configuration such as the data capture destination and the time after inference when data becomes available. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="data_captured_destination_s3_uri" type="string">
                  The S3 URI of the destination bucket where data captured from the batch transform job is stored, used as the input source for the monitoring job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="dataset_format" type="object">
                  Specifies the format of the input dataset (e.g., CSV, JSON) for the batch transform input to be monitored, enabling the monitoring job to correctly parse the data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="csv" type="object">
                      This object configures the CSV format for the batch transform input, including whether the first line of the dataset is treated as a header. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="header" type="bool">
                          Indicates whether the CSV file used as the batch transform input for the monitoring job contains a header row, which affects how the monitoring job interprets the dataset columns. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="json" type="object">
                      Specifies the JSON format configuration for the batch transform input dataset, including a boolean 'Line' property that indicates whether each line in the file contains a single JSON object (JSON Lines format). Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="line" type="bool">
                          Indicates whether the JSON dataset is formatted as JSON Lines (one JSON object per line) rather than a single monolithic JSON object. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="parquet" type="bool">
                      If set to true, this flag specifies that the monitoring job's batch transform input data is formatted as Apache Parquet. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="exclude_features_attribute" type="string">
                  Specifies the name of an attribute in the batch transform input dataset that contains a comma-separated list of feature names to be excluded from SageMaker monitoring analysis. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="local_path" type="string">
                  Specifies the local path in the processing container where the batch transform output files are mounted or written, which the monitoring job reads as its input. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_data_distribution_type" type="string">
                  Specifies how the monitoring batch transform job distributes input data from S3 across instances, either 'FullyReplicated' (each instance receives a full copy) or 'ShardedByS3Key' (data is sharded across instances). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_input_mode" type="string">
                  Determines how the SageMaker monitoring job reads the S3 input data for batch transform, accepting either 'File' mode (data downloaded to the instance) or 'Pipe' mode (data streamed as a FIFO pipe). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="endpoint_input" type="object">
              Configures the SageMaker endpoint that the monitoring job watches, including the endpoint's name and the local path in the container where the captured data is stored. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="endpoint_name" type="string">
                  The name of the SageMaker endpoint whose data is to be monitored by this monitoring job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="exclude_features_attribute" type="string">
                  Specifies a JSONPath expression that identifies the features (attributes) in the endpoint input to exclude from monitoring. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="local_path" type="string">
                  The local path in the monitoring container where the endpoint input data is stored or mounted for the monitoring job to access. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_data_distribution_type" type="string">
                  Controls how the captured endpoint data is distributed across the monitoring compute instances, either as 'FullyReplicated' (each instance receives a full copy of all data) or 'ShardedByS3Key' (data is partitioned by S3 key across instances). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_input_mode" type="string">
                  Specifies whether the data from the endpoint input is delivered to the monitoring job in File mode (downloaded to local disk) or Pipe mode (streamed directly), controlling how the monitoring job accesses the input data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="monitoring_output_config" type="object" required>
          The output configuration for monitoring jobs. Required.
          <Expandable title="properties">
            <ResponseField name="kms_key_id" type="string">
              The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest using Amazon S3 server-side encryption. Optional.
            </ResponseField>
            <ResponseField name="monitoring_outputs" type="list(object)" required>
              Monitoring outputs for monitoring jobs. This is where the output of the periodic monitoring jobs is uploaded. Required.
              <Expandable title="properties">
                <ResponseField name="s3_output" type="object">
                  Defines the S3 location and upload mode used to store the results of the monitoring job's output, including the S3 URI, local path, and whether outputs are uploaded continuously or at the end of the job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="local_path" type="string">
                      Sets the local path inside the monitoring job's container where the output is written before being uploaded to the Amazon S3 location defined in the S3Output. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="s3_upload_mode" type="string">
                      Determines whether the monitoring output is uploaded to the specified S3 location continuously as the job runs or only once at the end of the monitoring job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="s3_uri" type="string">
                      The S3 URI (bucket and optional prefix) where SageMaker stores the output artifacts of the monitoring job, such as statistics and constraints JSON files. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="monitoring_resources" type="object" required>
          Identifies the resources to deploy for a monitoring job. Required.
          <Expandable title="properties">
            <ResponseField name="cluster_config" type="object" required>
              Configuration for the cluster used to run model monitoring jobs. Required.
              <Expandable title="properties">
                <ResponseField name="instance_count" type="number" required>
                  The number of ML compute instances to use in the model monitoring job. For distributed processing jobs, specify a value greater than 1. The default value is 1. Required.
                </ResponseField>
                <ResponseField name="instance_type" type="string" required>
                  The ML compute instance type for the processing job. Required.
                </ResponseField>
                <ResponseField name="volume_kms_key_id" type="string">
                  The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume attached to the ML compute instance(s) that run the model monitoring job. Optional.
                </ResponseField>
                <ResponseField name="volume_size_in_gb" type="number" required>
                  The size of the ML storage volume, in gigabytes, that you want to provision. You must specify sufficient ML storage for your scenario. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="network_config" type="object">
          Networking options for a job, such as network traffic encryption between containers, whether to allow inbound and outbound network calls to and from containers, and the VPC subnets and security groups to use for VPC-enabled jobs. Optional.
          <Expandable title="properties">
            <ResponseField name="enable_inter_container_traffic_encryption" type="bool">
              Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt communications. Encryption provides greater security for distributed processing jobs, but the processing might take longer. Optional.
            </ResponseField>
            <ResponseField name="enable_network_isolation" type="bool">
              Whether to allow inbound and outbound network calls to and from the containers used for the processing job. Optional.
            </ResponseField>
            <ResponseField name="vpc_config" type="object">
              Specifies a VPC that your training jobs and hosted models have access to. Control access to and from your training and model containers by configuring the VPC. Optional.
              <Expandable title="properties">
                <ResponseField name="security_group_ids" type="list(string)" required>
                  The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified in the Subnets field. Required.
                </ResponseField>
                <ResponseField name="subnets" type="list(string)" required>
                  The ID of the subnets in the VPC to which you want to connect to your monitoring jobs. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="role_arn" type="string" required>
          The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your behalf. Required.
        </ResponseField>
        <ResponseField name="stopping_condition" type="object">
          Specifies a time limit for how long the monitoring job is allowed to run. Optional.
          <Expandable title="properties">
            <ResponseField name="max_runtime_in_seconds" type="number" required>
              The maximum runtime allowed in seconds. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="monitoring_job_definition_name" type="string">
      Name of the job definition Optional.
    </ResponseField>
    <ResponseField name="monitoring_type" type="string">
      The type of monitoring job. Optional.
    </ResponseField>
    <ResponseField name="schedule_config" type="object">
      Configuration details about the monitoring schedule. Optional.
      <Expandable title="properties">
        <ResponseField name="data_analysis_end_time" type="string">
          Analysis time in ISO duration format Optional.
        </ResponseField>
        <ResponseField name="data_analysis_start_time" type="string">
          Analysis time in ISO duration format Optional.
        </ResponseField>
        <ResponseField name="schedule_expression" type="string" required>
          A cron expression or 'NOW' that describes details about the monitoring schedule. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="monitoring_schedule_name" type="string" required>
  The name of the monitoring schedule. Required.
</ResponseField>

<ResponseField name="monitoring_schedule_status" type="string">
  The status of a schedule job. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a user-defined tag attached to the SageMaker monitoring schedule, used to organize and manage the resource for cost tracking and IAM policies. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The time at which the schedule was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  A timestamp that indicates the last time the monitoring job was modified. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="monitoring_schedule_arn" type="string">
  The Amazon Resource Name (ARN) of the monitoring schedule. Computed by AWS after `ship`.
</ResponseField>
