---
title: "aws_sage_maker_mlflow_tracking_server"
description: "`aws_sage_maker_mlflow_tracking_server` creates a real, fully managed MLflow tracking server, backed by an S3 artifact store..."
---

`aws_sage_maker_mlflow_tracking_server` creates a real, fully managed MLflow tracking server, backed by an S3 artifact store, for logging and comparing experiment runs using the open-source MLflow API rather than SageMaker's own native `aws_sage_maker_experiment` tracking.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker mlflow tracking server"})

    		ubx.Resource(sagemaker.MlflowTrackingServer, "example", sagemaker.MlflowTrackingServerConfig{
    			ArtifactStoreUri:             "example",
    			RoleArn:                      "arn:aws:iam::123456789012:policy/example",
    			TrackingServerName:           "example-tracking-server-name",
    			AutomaticModelRegistration:   true,
    			MlflowVersion:                "example",
    			Tags:                         []map[string]any{{"key": "example"}},
    			TrackingServerSize:           "example",
    			WeeklyMaintenanceWindowStart: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MlflowTrackingServer } from "@ubx/sdk-aws/aws/sagemaker/mlflow_tracking_server";

    export default stack("example", () => {
      intent({ summary: "example own maker mlflow tracking server" });

      resource(MlflowTrackingServer, "example", {
        artifactStoreUri: "example",
        roleArn: "arn:aws:iam::123456789012:policy/example",
        trackingServerName: "example-tracking-server-name",
        automaticModelRegistration: true,
        mlflowVersion: "example",
        tags: [{ key: "example" }],
        trackingServerSize: "example",
        weeklyMaintenanceWindowStart: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker.mlflow_tracking_server import MlflowTrackingServer, MlflowTrackingServerConfig

    def describe():
        ubx.intent("example own maker mlflow tracking server")

        ubx.resource(MlflowTrackingServer, "example", MlflowTrackingServerConfig(
            artifact_store_uri="example",
            role_arn="arn:aws:iam::123456789012:policy/example",
            tracking_server_name="example-tracking-server-name",
            automatic_model_registration=True,
            mlflow_version="example",
            tags=[{"key": "example"}],
            tracking_server_size="example",
            weekly_maintenance_window_start="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_mlflow_tracking_server, with artifact store
    uri example, role arn arn:aws:iam::123456789012:policy/example,
    tracking server name example-tracking-server-name, automatic
    model registration true, mlflow version example, tags a set
    value, tracking server size example, weekly maintenance window
    start example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="artifact_store_uri" type="string" required>
  The Amazon S3 URI for MLFlow Tracking Server artifacts. Required.
</ResponseField>

<ResponseField name="automatic_model_registration" type="bool">
  A flag to enable Automatic SageMaker Model Registration. Optional.
</ResponseField>

<ResponseField name="mlflow_version" type="string">
  The MLFlow Version used on the MLFlow Tracking Server. Optional.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The Amazon Resource Name (ARN) of an IAM role that enables Amazon SageMaker to perform tasks on behalf of the customer. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key part of a key-value tag attached to the SageMaker MLflow Tracking Server, used to identify and organize the resource within AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The arbitrary value for a tag attached to the SageMaker MLflow Tracking Server. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tracking_server_name" type="string" required>
  The name of the MLFlow Tracking Server. Required.
</ResponseField>

<ResponseField name="tracking_server_size" type="string">
  The size of the MLFlow Tracking Server. Optional.
</ResponseField>

<ResponseField name="weekly_maintenance_window_start" type="string">
  The start of the time window for maintenance of the MLFlow Tracking Server in UTC time. Optional.
</ResponseField>

## Output properties

<ResponseField name="tracking_server_arn" type="string">
  The Amazon Resource Name (ARN) of the MLFlow Tracking Server. Computed by AWS after `ship`.
</ResponseField>
