---
title: "aws_sage_maker_device_fleet"
description: "`aws_sage_maker_device_fleet` creates a real logical grouping of `aws_sage_maker_device`s that edge-deployed models are managed and monitored across..."
---

`aws_sage_maker_device_fleet` creates a real logical grouping of `aws_sage_maker_device`s that edge-deployed models are managed and monitored across together.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker device fleet"})

    		ubx.Resource(sage.MakerDeviceFleet, "example", sage.MakerDeviceFleetConfig{
    			DeviceFleetName: "example-device-fleet-name",
    			OutputConfig:    map[string]any{"s3_output_location": "example"},
    			RoleArn:         "arn:aws:iam::123456789012:policy/example",
    			Description:     "Managed by ubx.",
    			Tags:            []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerDeviceFleet } from "./local-sdk/aws/sdk/typescript/aws/sage/maker_device_fleet.ts";

    export default stack("example", () => {
      intent({ summary: "example own maker device fleet" });

      resource(MakerDeviceFleet, "example", {
        deviceFleetName: "example-device-fleet-name",
        outputConfig: { s3OutputLocation: "example" },
        roleArn: "arn:aws:iam::123456789012:policy/example",
        description: "Managed by ubx.",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerDeviceFleet, MakerDeviceFleetConfig

    def describe():
        ubx.intent("example own maker device fleet")

        ubx.resource(MakerDeviceFleet, "example", MakerDeviceFleetConfig(
            device_fleet_name="example-device-fleet-name",
            output_config={"s3_output_location": "example"},
            role_arn="arn:aws:iam::123456789012:policy/example",
            description="Managed by ubx.",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_device_fleet, with device fleet name
    example-device-fleet-name, output config a set value, role arn
    arn:aws:iam::123456789012:policy/example, description Managed by
    ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="description" type="string">
  Description for the edge device fleet Optional.
</ResponseField>

<ResponseField name="device_fleet_name" type="string" required>
  The name of the edge device fleet Required.
</ResponseField>

<ResponseField name="output_config" type="object" required>
  Specifies the Amazon S3 location (and optional AWS KMS key) where the device fleet's output data, such as device metrics and heartbeats, is stored. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="kms_key_id" type="string">
      The KMS key id used for encryption on the S3 bucket Optional.
    </ResponseField>
    <ResponseField name="s3_output_location" type="string" required>
      The Amazon Simple Storage (S3) bucket URI Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Role associated with the device fleet Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Associate tags with the resource Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag attached to the SageMaker device fleet, allowing you to assign arbitrary metadata for purposes like cost tracking, access control, or operational organization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
