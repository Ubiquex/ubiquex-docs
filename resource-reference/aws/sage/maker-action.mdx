---
title: "aws_sage_maker_action"
description: "`aws_sage_maker_action` records a real, tracked action, a training job run or model deployment, in SageMaker's ML Lineage Tracking graph..."
---

`aws_sage_maker_action` records a real, tracked action, a training job run or model deployment, in SageMaker's ML Lineage Tracking graph, connecting the artifacts, datasets, and models it consumed and produced so a model's full provenance can be traced later.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker action"})

    		ubx.Resource(sage.MakerAction, "example", sage.MakerActionConfig{
    			ActionName:         "example-action-name",
    			ActionType:         "example",
    			Source:             map[string]any{"source_uri": "example"},
    			Description:        "Managed by ubx.",
    			MetadataProperties: map[string]any{"commit_id": "example"},
    			Properties:         "example",
    			Status:             "example",
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerAction } from "./local-sdk/aws/sdk/typescript/aws/sage/maker_action.ts";

    export default stack("example", () => {
      intent({ summary: "example own maker action" });

      resource(MakerAction, "example", {
        actionName: "example-action-name",
        actionType: "example",
        source: { sourceUri: "example" },
        description: "Managed by ubx.",
        metadataProperties: { commitId: "example" },
        properties: "example",
        status: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerAction, MakerActionConfig

    def describe():
        ubx.intent("example own maker action")

        ubx.resource(MakerAction, "example", MakerActionConfig(
            action_name="example-action-name",
            action_type="example",
            source={"source_uri": "example"},
            description="Managed by ubx.",
            metadata_properties={"commit_id": "example"},
            properties="example",
            status="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_action, with action name
    example-action-name, action type example, source a set value,
    description Managed by ubx., metadata properties a set value,
    properties example, status example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="action_name" type="string" required>
  The name of the action. Must be unique to your account in an AWS Region. Required.
</ResponseField>

<ResponseField name="action_type" type="string" required>
  The action type. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the action. Optional.
</ResponseField>

<ResponseField name="metadata_properties" type="object">
  Specifies the metadata properties for the SageMaker action, including the source repository, commit ID, the entity that generated the action, and the project ID associated with it. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="commit_id" type="string">
      The commit ID. Optional.
    </ResponseField>
    <ResponseField name="generated_by" type="string">
      The entity this entity was generated by. Optional.
    </ResponseField>
    <ResponseField name="project_id" type="string">
      The project ID. Optional.
    </ResponseField>
    <ResponseField name="repository" type="string">
      The repository. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="properties" type="any">
  A list of properties to add to the action. Optional.
</ResponseField>

<ResponseField name="source" type="object" required>
  Specifies the source location and type that identify where this SageMaker action was defined or triggered, such as an Amazon S3 URI or a pipeline execution ARN. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="source_id" type="string">
      The ID of the source. Optional.
    </ResponseField>
    <ResponseField name="source_type" type="string">
      The type of the source. Optional.
    </ResponseField>
    <ResponseField name="source_uri" type="string" required>
      The URI of the source. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The status of the action. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of tags to apply to the action. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a user-defined tag associated with the SageMaker action, used to store arbitrary metadata such as cost centers or environment labels for managing and organizing the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the action. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  When the action was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  When the action was last modified. Computed by AWS after `ship`.
</ResponseField>
