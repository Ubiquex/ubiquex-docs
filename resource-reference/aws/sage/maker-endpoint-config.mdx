---
title: "aws_sage_maker_endpoint_config"
description: "`aws_sage_maker_endpoint_config` defines a real deployment configuration, which `aws_sage_maker_model`s to serve, instance type and count..."
---

`aws_sage_maker_endpoint_config` defines a real deployment configuration, which `aws_sage_maker_model`s to serve, instance type and count, traffic split across variants, that an `aws_sage_maker_endpoint` is created from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker endpoint config"})

    		ubx.Resource(sagemaker.EndpointConfig, "example", sagemaker.EndpointConfigConfig{
    			ProductionVariants:     []map[string]any{{"capacity_reservation_config": map[string]any{"capacity_reservation_preference": "example"}}},
    			AsyncInferenceConfig:   map[string]any{"output_config": map[string]any{"kms_key_id": "example"}},
    			DataCaptureConfig:      map[string]any{"capture_options": []map[string]any{{"capture_mode": "example"}}},
    			EnableNetworkIsolation: true,
    			EndpointConfigName:     "example-endpoint-config-name",
    			ExecutionRoleArn:       "arn:aws:iam::123456789012:policy/example",
    			ExplainerConfig:        map[string]any{"clarify_explainer_config": map[string]any{"shap_config": map[string]any{"shap_baseline_config": map[string]any{"mime_type": "example"}}}},
    			KmsKeyId:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointConfig } from "@ubx/sdk-aws/aws/sagemaker/endpoint_config";

    export default stack("example", () => {
      intent({ summary: "example own maker endpoint config" });

      resource(EndpointConfig, "example", {
        productionVariants: [{
          capacityReservationConfig: { capacityReservationPreference: "example" },
        }],
        asyncInferenceConfig: { outputConfig: { kmsKeyId: "example" } },
        dataCaptureConfig: { captureOptions: [{ captureMode: "example" }] },
        enableNetworkIsolation: true,
        endpointConfigName: "example-endpoint-config-name",
        executionRoleArn: "arn:aws:iam::123456789012:policy/example",
        explainerConfig: {
          clarifyExplainerConfig: {
            shapConfig: { shapBaselineConfig: { mimeType: "example" } },
          },
        },
        kmsKeyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker.endpoint_config import EndpointConfig, EndpointConfigConfig

    def describe():
        ubx.intent("example own maker endpoint config")

        ubx.resource(EndpointConfig, "example", EndpointConfigConfig(
            production_variants=[{"capacity_reservation_config": {"capacity_reservation_preference": "example"}}],
            async_inference_config={"output_config": {"kms_key_id": "example"}},
            data_capture_config={"capture_options": [{"capture_mode": "example"}]},
            enable_network_isolation=True,
            endpoint_config_name="example-endpoint-config-name",
            execution_role_arn="arn:aws:iam::123456789012:policy/example",
            explainer_config={"clarify_explainer_config": {"shap_config": {"shap_baseline_config": {"mime_type": "example"}}}},
            kms_key_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_endpoint_config, with production variants a
    set value, async inference config a set value, data capture
    config a set value, enable network isolation true, endpoint
    config name example-endpoint-config-name, execution role arn
    arn:aws:iam::123456789012:policy/example, explainer config a set
    value, kms key id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="async_inference_config" type="object">
  Specifies the configuration for asynchronous inference on the endpoint, including the S3 output path for results, optional notification settings, and client limits such as maximum concurrent invocations per instance. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="client_config" type="object">
      Specifies client configuration for asynchronous inference, including the maximum number of concurrent invocations per instance allowed for the endpoint. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_concurrent_invocations_per_instance" type="number">
          Sets the maximum number of concurrent invocations per instance that the asynchronous inference client can send to the model, controlling request parallelism for the endpoint. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="output_config" type="object" required>
      Configures the S3 output location (and optionally an SNS notification topic) for storing the results of asynchronous inference on this endpoint configuration. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="kms_key_id" type="string">
          Specifies the AWS KMS key ID used to encrypt the async inference output data stored in Amazon S3. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="notification_config" type="object">
          Specifies the Amazon SNS topic ARNs to receive success and error notifications for asynchronous inference, and optionally which inference response types to include in those notifications. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="error_topic" type="string">
              The ARN of the Amazon SNS topic to which SageMaker sends an error notification when an asynchronous inference request fails. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="include_inference_response_in" type="list(string)">
              Specifies a list of invocation outcomes (e.g., 'SUCCESS' or 'FAILURE') for which the inference response payload is included in the SNS notification for an asynchronous SageMaker endpoint. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="success_topic" type="string">
              SNS topic ARN to which SageMaker publishes a notification when an asynchronous inference completes successfully. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="s3_failure_path" type="string">
          The S3 URI (bucket and prefix) where SageMaker writes the failure response payload for asynchronous inference requests when the model prediction fails. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="s3_output_path" type="string">
          The S3 bucket and prefix (path) where the results of asynchronous inference requests are written. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_capture_config" type="object">
  Configures capturing of request/response data from the endpoint to an S3 location, including whether capture is enabled, sampling percentage, capture options (input/output), and content type headers for monitoring. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="capture_content_type_header" type="object">
      Specifies the lists of CSV and JSON content types that SageMaker captures for requests and responses in the endpoint's data capture configuration. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="csv_content_types" type="list(string)">
          Specifies the list of CSV content types (e.g., 'text/csv') for which Amazon SageMaker captures request and response payloads as part of the data capture configuration for an endpoint. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="json_content_types" type="list(string)">
          Specifies the list of content types (e.g., 'application/json') that are classified as JSON for the purpose of data capture on the SageMaker endpoint. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="capture_options" type="list(object)" required>
      Specifies which data flows (Request/Input and/or Response/Output) to capture for the endpoint's data capture configuration. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="capture_mode" type="string">
          Specifies whether the data capture option applies to the input, output, or both (Input, Output, or InputAndOutput) of the endpoint invocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="destination_s3_uri" type="string" required>
      The S3 URI where Amazon SageMaker stores the captured endpoint data (payloads) for the DataCaptureConfig of this endpoint configuration. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_capture" type="bool">
      Indicates whether data capture is enabled for the endpoint, which controls whether SageMaker captures request and response data for monitoring and debugging. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initial_sampling_percentage" type="number" required>
      The percentage of incoming requests (0 to 100) to sample for data capture when the endpoint is initially deployed, determining what fraction of traffic is recorded for monitoring or analysis. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kms_key_id" type="string">
      The AWS KMS key ID used to encrypt captured endpoint data at rest in the data capture configuration. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="enable_network_isolation" type="bool">
  If set to true, the endpoint runs in network isolation mode, preventing containers from accessing the internet and restricting traffic to the VPC configured for the endpoint. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="endpoint_config_name" type="string">
  The name for the endpoint configuration, which must be unique within an AWS Region in your account. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="execution_role_arn" type="string">
  The ARN of the IAM role that Amazon SageMaker assumes to access model artifacts and other AWS resources needed to serve the endpoint. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="explainer_config" type="object">
  The explainer configuration for the SageMaker endpoint, specifying the Clarify explainer settings used to generate feature attribution explanations for model predictions. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="clarify_explainer_config" type="object">
      Specifies the configuration for SageMaker Clarify to generate SHAP-based feature attributions for the endpoint, including the SHAP baseline, number of samples, and text configuration for natural language processing. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="enable_explanations" type="string">
          Determines whether SageMaker Clarify explanations are enabled for the endpoint, with valid values being 'Enabled' or 'Disabled'. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="inference_config" type="object">
          Specifies the input and output data layout (feature headers, feature attribute, label attribute, etc.) that SageMaker Clarify uses to interpret inference requests and responses when generating explainability explanations. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="content_template" type="string">
              Defines a template string that determines how feature values are placed into the inference request body for SageMaker Clarify, matching the endpoint's expected input format (e.g., with placeholders like &#123;features&#125;). Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="feature_headers" type="list(any)">
              Specifies the list of feature names for the model input, enabling SageMaker Clarify to label and interpret features when generating explainability insights for the endpoint configuration. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="feature_types" type="list(any)">
              Specifies the data type (categorical or numerical) for each feature, enabling SageMaker Clarify to apply the appropriate explanation method during inference. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="features_attribute" type="string">
              Specifies the name or JSONPath of the features attribute in the input dataset, used by SageMaker Clarify for explanations. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="label_attribute" type="string">
              The name of the attribute in the SageMaker model's inference response that contains the predicted label, used by SageMaker Clarify to properly parse outputs for bias and explainability analyses. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="label_headers" type="list(any)">
              Specifies the names of the label columns in the dataset, used by SageMaker Clarify to identify which model output headers correspond to labels during explanation generation. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="label_index" type="number">
              The index of the label column in the input data, used by the SageMaker Clarify explainer to identify the true label for multi-class classification models. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_payload_in_mb" type="number">
              Sets the maximum payload size (in MB) that SageMaker Clarify permits for the inference requests it sends to the endpoint when generating explanations. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_record_count" type="number">
              The maximum number of records from the input dataset that SageMaker Clarify's inference configuration uses to generate model explanations for the endpoint. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="probability_attribute" type="string">
              The attribute name in the model output that contains the probability for the predicted class, used by SageMaker Clarify to compute SHAP explanations. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="probability_index" type="number">
              The index of the model output that contains the probability of the positive class, used by SageMaker Clarify to interpret predictions for binary classification. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="shap_config" type="object" required>
          Specifies the SHAP (SHapley Additive exPlanations) baseline and sampling parameters for the SageMaker Clarify explainer, which are used to generate feature attribution values for model predictions at the endpoint. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="number_of_samples" type="number">
              Integer specifying how many samples to use when computing SHAP values for the model predictions, used by SageMaker Clarify to approximate the baseline distribution. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="seed" type="number">
              Sets the random seed for the SageMaker Clarify SHAP algorithm to ensure reproducible feature attribution results in the endpoint configuration's explainer config. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="shap_baseline_config" type="object" required>
              Specifies the baseline (background) dataset and its serialization format that SageMaker Clarify's SHAP algorithm uses to compute feature attribution values for endpoint predictions. Required. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="mime_type" type="string">
                  The MIME type of the SHAP baseline data (e.g., text/csv or application/json) used by SageMaker Clarify for explainability. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="shap_baseline" type="string">
                  Specifies the baseline data (as a JSON string or S3 URI) used by the SHAP algorithm for explainability in SageMaker Clarify. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="shap_baseline_uri" type="string">
                  The S3 URI of the baseline dataset used as the uniform background data for computing SHAP values in SageMaker Clarify explanations for this endpoint configuration. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="text_config" type="object">
              This object configures how SageMaker Clarify's SHAP explainer processes text features, including language and granularity settings for computing feature attribution. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="granularity" type="string" required>
                  Specifies the granularity level for text inputs used by SageMaker Clarify SHAP analysis, either 'token' or 'sentence', determining whether the text is divided into individual tokens or entire sentences for generating explanations. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="language" type="string" required>
                  Specifies the language (e.g., 'en' for English) of the text data for SageMaker Clarify SHAP analysis in the text configuration. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="use_logit" type="bool">
              If true, SageMaker Clarify transforms the model's probability output to log-odds (logit) before computing SHAP values, which is appropriate for binary classification problems. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  The AWS KMS key ARN used to encrypt data at rest on the ML storage volumes attached to the ML compute instances that host the SageMaker endpoint. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="production_variants" type="list(object)" required>
  Production variants define the list of model variants to serve at the endpoint, each specifying the model, compute instance type, and traffic weight (initial variant weight) for routing inference requests. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="capacity_reservation_config" type="object">
      Capacity reservation configuration for the production variant, specifying the Amazon EC2 capacity reservation target (such as a CapacityReservationId) to reserve compute capacity for the variant's instances. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="capacity_reservation_preference" type="string">
          Specifies the capacity reservation preference for the production variant, which can be set to 'capacity_reservation' to reserve capacity in an Availability Zone or 'none' to not use capacity reservations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ml_reservation_arn" type="string">
          The Amazon Resource Name (ARN) of the SageMaker ML instance reservation to apply to this production variant, allowing you to leverage committed-use discounts for the endpoint capacity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_startup_health_check_timeout_in_seconds" type="number">
      The timeout in seconds for the container startup health check, after which SageMaker considers the model in this production variant unhealthy if the container has not reported a healthy status. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_ssmaccess" type="bool">
      Indicates whether to enable AWS Systems Manager (SSM) access to the container running the model for this production variant, allowing remote debugging and management. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="inference_ami_version" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="initial_instance_count" type="number">
      The initial number of ML compute instances to launch for this production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initial_variant_weight" type="number">
      The initial weight assigned to a production variant, determining the proportion of inference traffic routed to this variant relative to other variants in the endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_pools" type="list(object)">
      Specifies a list of instance pools for a production variant, where each pool defines an instance type and instance count, enabling the variant to span multiple instance types for capacity and cost optimization. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string">
          The SageMaker ML compute instance type (e.g., ml.m5.xlarge) used by the corresponding instance pool in a production variant, determining the underlying compute resource that serves traffic for that pool within the endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_name_override" type="string">
          The name of the SageMaker model to serve from this instance pool, overriding the model name specified in the parent production variant so a single endpoint configuration can route traffic to different models across its instance pools. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="number">
          Indicates the priority of this instance pool within the production variant, where a lower numeric value represents a higher precedence that SageMaker uses when selecting an instance type to provision endpoint capacity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="instance_type" type="string">
      Specifies the Amazon SageMaker ML compute instance type (for example, ml.m4.xlarge) used to host this production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="managed_instance_scaling" type="object">
      Specifies settings for managed instance scaling of this production variant, including the minimum and maximum instance counts and whether managed scaling is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_instance_count" type="number">
          The maximum number of instances that the endpoint's production variant can scale out to when managed instance scaling is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_instance_count" type="number">
          The minimum number of instances that the SageMaker endpoint production variant can scale down to when managed instance scaling is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Indicates whether managed instance scaling is enabled or disabled for this production variant, with valid values 'ENABLED' or 'DISABLED'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_data_download_timeout_in_seconds" type="number">
      Specifies the maximum time in seconds permitted for SageMaker to download and extract the model artifacts from Amazon S3 onto the inference instance backing this production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="model_name" type="string">
      The name of the SageMaker model that this production variant will deploy as part of the endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="routing_config" type="object">
      Determines how incoming requests are distributed across the compute instances backing this production variant, using either a least-outstanding-requests or random routing strategy. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="routing_strategy" type="string">
          Specifies the routing strategy for the production variant, controlling how SageMaker distributes inference requests across the variant's instances, with valid values such as 'LEAST_OUTSTANDING_REQUESTS' and 'RANDOM'. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="serverless_config" type="object">
      Specifies the serverless inference configuration for this production variant, including memory size and maximum concurrency settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_concurrency" type="number">
          Sets the maximum number of concurrent invocations the serverless endpoint can handle, controlling throughput and triggering scaling for this production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="memory_size_in_mb" type="number">
          Specifies the memory size (in MB) for the serverless endpoint production variant, determining the compute resources allocated for the endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provisioned_concurrency" type="number">
          The number of provisioned concurrency execution environments to pre-warm for the serverless production variant, reducing cold start latency at an additional cost. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variant_instance_provision_timeout_in_seconds" type="number">
      Specifies the maximum time in seconds that SageMaker waits for the production variant's instances to be provisioned and become healthy before failing the endpoint deployment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="variant_name" type="string">
      A unique name for the production variant, used to identify and route traffic to the corresponding model within the SageMaker endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="volume_size_in_gb" type="number">
      Specifies the size of the Amazon EBS volume in gigabytes to attach to each SageMaker endpoint instance for the production variant, providing additional local storage for the container. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="shadow_production_variants" type="list(object)">
  Defines a list of shadow production variants, each specifying a model to be deployed alongside the primary production variants, which receives a copy of inference requests for testing and monitoring without serving live production traffic. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="capacity_reservation_config" type="object">
      The capacity reservation configuration for the shadow production variant, specifying the capacity reservation group where the endpoint instances should be launched and the reservation preference. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="capacity_reservation_preference" type="string">
          Specifies whether the shadow production variant uses a specific capacity reservation (value `capacity_reservation`) or the default capacity reservation preference (value `capacity_reservation_preference`) when allocating inference capacity. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="ml_reservation_arn" type="string">
          Specifies the Amazon Resource Name (ARN) of the SageMaker ML capacity reservation assigned to the shadow production variant's capacity reservation configuration, which allows the endpoint to use pre-purchased reserved capacity for hosting the variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="container_startup_health_check_timeout_in_seconds" type="number">
      Defines the timeout in seconds for the container startup health check of this shadow production variant, controlling how long SageMaker waits for the model container to become healthy after startup before routing shadow traffic to it. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enable_ssmaccess" type="bool">
      When enabled (true), grants AWS Systems Manager (SSM) access to the SageMaker container for this shadow variant, allowing secure remote shell access for debugging and troubleshooting; disabled by default. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="inference_ami_version" type="string">
      Specifies the version of the inference AMI (Amazon Machine Image) to use for the shadow production variant in the SageMaker endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initial_instance_count" type="number">
      The number of Amazon EC2 instances to initially launch for this shadow production variant, which handles the shadow/copied traffic for testing in the SageMaker endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initial_variant_weight" type="number">
      The initial weight (a value between 0 and 1) that determines the fraction of live inference traffic sent to this shadow production variant for testing, while the primary production variants continue to serve the actual traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="instance_pools" type="list(object)">
      Specifies the instance pools for this shadow variant, where each pool defines an instance type and the number of instances to allocate on that type, enabling the variant to use multiple instance types. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="instance_type" type="string">
          Specifies the Amazon EC2 instance type (e.g., ml.m5.large) that the instance pool of this shadow production variant will use in the SageMaker endpoint configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_name_override" type="string">
          For a SageMaker shadow production variant, this optional string overrides the variant-level model name for requests routed to that specific instance pool, allowing different models to be served by different instance pools within the same variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="number">
          The priority value of an instance pool within the shadow production variant, determining the order in which the instance pools are selected for routing and scaling inference traffic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="instance_type" type="string">
      The instance type (e.g., ml.m5.large) used to run the shadow production variant, which receives a copy of traffic for testing before it is promoted to a production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="managed_instance_scaling" type="object">
      Configures the managed instance scaling limits (such as minimum and maximum instance counts and status) for this shadow production variant, controlling how SageMaker automatically scales the instance count for the endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_instance_count" type="number">
          The maximum number of instances that SageMaker can provision for the shadow variant when auto-scaling is enabled via managed instance scaling. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="min_instance_count" type="number">
          Specifies the minimum number of instances that the shadow production variant can scale down to when managed instance scaling is enabled. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          Indicates whether Amazon SageMaker automatically manages instance scaling for the shadow production variant by adjusting instance counts based on traffic, with values ENABLED or DISABLED. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_data_download_timeout_in_seconds" type="number">
      Specifies the timeout value, in seconds, to download and extract the model data for this shadow production variant before the endpoint deployment proceeds. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="model_name" type="string">
      The name of the SageMaker model to deploy for this shadow production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="routing_config" type="object">
      Configures how inference requests are routed to instances of this shadow production variant by selecting a routing strategy (LEAST_OUTSTANDING_REQUESTS or RANDOM). Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="routing_strategy" type="string">
          For shadow production variants in a SageMaker endpoint configuration, this strategy determines how incoming traffic is distributed among the variants, either routing to the variant with the fewest in-flight requests ('LEAST_OUTSTANDING_REQUESTS') or choosing randomly ('RANDOM'). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="serverless_config" type="object">
      The serverless configuration for the shadow production variant, specifying the memory size and maximum concurrency for serverless inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="max_concurrency" type="number">
          Sets the maximum number of concurrent invocations the serverless shadow variant can handle. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="memory_size_in_mb" type="number">
          The memory size in megabytes allocated to the serverless endpoint for this shadow production variant, which determines the compute capacity and billable resources for serverless inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="provisioned_concurrency" type="number">
          Specifies the number of provisioned concurrent invocations for the serverless endpoint of the shadow production variant, which determines how many simultaneous requests it can handle for that variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="variant_instance_provision_timeout_in_seconds" type="number">
      Specifies the maximum time (in seconds) that SageMaker waits for an instance to be provisioned for a shadow production variant before considering the operation failed, controlling the provisioning timeout for that variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="variant_name" type="string">
      Specifies the unique name for a shadow production variant, which identifies the variant when an Amazon SageMaker endpoint receives shadow traffic for testing a new model. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="volume_size_in_gb" type="number">
      The size, in GB, of the Amazon EBS volume attached to the inference instance for this shadow production variant. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Defines the list of AWS tag objects (each with a Key and Value) to attach to this SageMaker endpoint configuration, which specifies the model, instance type, and settings for a real-time inference endpoint, enabling cost allocation and resource organization. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of an individual tag attached to the SageMaker endpoint configuration, used to identify and categorize the resource when paired with its corresponding value. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a tag attached to the SageMaker endpoint configuration, used to categorize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_config" type="object">
  Specifies the VPC configuration for the SageMaker endpoint, including the subnets and security groups that the endpoint will use to access network resources. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="security_group_ids" type="list(string)" required>
      The list of security group IDs that define the VPC security groups applied to the SageMaker endpoint's VPC configuration, governing network access to the model container. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnets" type="list(string)" required>
      The subnets field specifies the list of subnet IDs in the VPC where the SageMaker endpoint's network interfaces are placed. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The unique name of the SageMaker endpoint configuration, which serves as its primary identifier and is assigned by AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
