---
title: "aws_sage_maker_pipeline"
description: "`aws_sage_maker_pipeline` defines a real, orchestrated sequence of ML workflow steps, processing, training, evaluation, deployment..."
---

`aws_sage_maker_pipeline` defines a real, orchestrated sequence of ML workflow steps, processing, training, evaluation, deployment, as a directed acyclic graph, the resource SageMaker Pipelines uses to automate an ML workflow's own repeatable execution.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own maker pipeline"})

    		ubx.Resource(sage.MakerPipeline, "ci-runner", sage.MakerPipelineConfig{
    			PipelineDefinition:       "example",
    			PipelineName:             "example-pipeline-name",
    			RoleArn:                  "arn:aws:iam::123456789012:policy/ci",
    			ParallelismConfiguration: map[string]any{"max_parallel_execution_steps": 1},
    			PipelineDescription:      "example",
    			PipelineDisplayName:      "example-pipeline-display-name",
    			Tags:                     []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerPipeline } from "./local-sdk/aws/sdk/typescript/aws/sage/maker_pipeline.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own maker pipeline" });

      resource(MakerPipeline, "ci-runner", {
        pipelineDefinition: "example",
        pipelineName: "example-pipeline-name",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        parallelismConfiguration: { maxParallelExecutionSteps: 1 },
        pipelineDescription: "example",
        pipelineDisplayName: "example-pipeline-display-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerPipeline, MakerPipelineConfig

    def describe():
        ubx.intent("payments own maker pipeline")

        ubx.resource(MakerPipeline, "ci-runner", MakerPipelineConfig(
            pipeline_definition="example",
            pipeline_name="example-pipeline-name",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            parallelism_configuration={"max_parallel_execution_steps": 1},
            pipeline_description="example",
            pipeline_display_name="example-pipeline-display-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_pipeline, with pipeline definition example,
    pipeline name example-pipeline-name, role arn
    arn:aws:iam::123456789012:policy/ci, parallelism configuration a
    set value, pipeline description example, pipeline display name
    example-pipeline-display-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="parallelism_configuration" type="object">
  Defines the parallelism configuration for the SageMaker pipeline, including the maximum number of execution steps that can run in parallel. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="max_parallel_execution_steps" type="number" required>
      Maximum parallel execution steps Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="pipeline_definition" type="any" required>
  Specifies the SageMaker pipeline definition, as a JSON or YAML string that describes the pipeline's steps, parameters, and configuration. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="pipeline_description" type="string">
  The description of the Pipeline. Optional.
</ResponseField>

<ResponseField name="pipeline_display_name" type="string">
  The display name of the Pipeline. Optional.
</ResponseField>

<ResponseField name="pipeline_name" type="string" required>
  The name of the Pipeline. Required.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Role Arn Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value pairs attached to an AWS SageMaker pipeline to help organize, identify, and manage it, often used for cost allocation and access control. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the SageMaker pipeline, used to assign metadata for organizing, identifying, and managing the pipeline resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>
