---
title: "aws_sage_maker_model_package_group"
description: "`aws_sage_maker_model_package_group` creates a real, named container that groups related `aws_sage_maker_model_package` versions together..."
---

`aws_sage_maker_model_package_group` creates a real, named container that groups related `aws_sage_maker_model_package` versions together, the resource SageMaker Model Registry organizes a model's version history and approval status under.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker model package group"})

    		ubx.Resource(sage.MakerModelPackageGroup, "example", sage.MakerModelPackageGroupConfig{
    			ModelPackageGroupName:        "example-model-package-group-name",
    			ModelPackageGroupDescription: "example",
    			ModelPackageGroupPolicy:      "example",
    			Tags:                         []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerModelPackageGroup } from "@ubx/sdk-aws/aws/sage/maker_model_package_group";

    export default stack("example", () => {
      intent({ summary: "example own maker model package group" });

      resource(MakerModelPackageGroup, "example", {
        modelPackageGroupName: "example-model-package-group-name",
        modelPackageGroupDescription: "example",
        modelPackageGroupPolicy: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerModelPackageGroup, MakerModelPackageGroupConfig

    def describe():
        ubx.intent("example own maker model package group")

        ubx.resource(MakerModelPackageGroup, "example", MakerModelPackageGroupConfig(
            model_package_group_name="example-model-package-group-name",
            model_package_group_description="example",
            model_package_group_policy="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_model_package_group, with model package
    group name example-model-package-group-name, model package group
    description example, model package group policy example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="model_package_group_description" type="string">
  The description of the model package group. Optional.
</ResponseField>

<ResponseField name="model_package_group_name" type="string" required>
  The name of the model package group. Required.
</ResponseField>

<ResponseField name="model_package_group_policy" type="any">
  JSON policy document that defines a resource-based access policy for the SageMaker model package group, controlling which AWS accounts or principals can use or manage the group. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key part of an AWS key-value tag that can be attached to a SageMaker model package group for resource identification and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag key-value pair assigned to the SageMaker Model Package Group, allowing you to categorize and manage the group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The time at which the model package group was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="model_package_group_arn" type="string">
  The Amazon Resource Name (ARN) of the model package group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="model_package_group_status" type="string">
  The status of a modelpackage group job. Computed by AWS after `ship`.
</ResponseField>
