---
title: "aws_sage_maker_model_package"
description: "`aws_sage_maker_model_package` registers a real, specific version of a trained model, its container, artifacts, and inference specification..."
---

`aws_sage_maker_model_package` registers a real, specific version of a trained model, its container, artifacts, and inference specification, within an `aws_sage_maker_model_package_group`, the versioned unit an approval workflow is applied to before deployment.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sage "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker model package"})

    		ubx.Resource(sage.MakerModelPackage, "example", sage.MakerModelPackageConfig{
    			AdditionalInferenceSpecifications:      []map[string]any{{"name": "example-name"}},
    			AdditionalInferenceSpecificationsToAdd: []map[string]any{{"name": "example-name"}},
    			ApprovalDescription:                    "example",
    			CertifyForMarketplace:                  true,
    			ClientToken:                            "example",
    			CustomerMetadataProperties:             "example",
    			Domain:                                 "example",
    			DriftCheckBaselines:                    map[string]any{"bias": map[string]any{"config_file": map[string]any{"s3_uri": "example"}}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerModelPackage } from "@ubx/sdk-aws/aws/sage/maker_model_package";

    export default stack("example", () => {
      intent({ summary: "example own maker model package" });

      resource(MakerModelPackage, "example", {
        additionalInferenceSpecifications: [{ name: "example-name" }],
        additionalInferenceSpecificationsToAdd: [{ name: "example-name" }],
        approvalDescription: "example",
        certifyForMarketplace: true,
        clientToken: "example",
        customerMetadataProperties: "example",
        domain: "example",
        driftCheckBaselines: { bias: { configFile: { s3Uri: "example" } } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sage import MakerModelPackage, MakerModelPackageConfig

    def describe():
        ubx.intent("example own maker model package")

        ubx.resource(MakerModelPackage, "example", MakerModelPackageConfig(
            additional_inference_specifications=[{"name": "example-name"}],
            additional_inference_specifications_to_add=[{"name": "example-name"}],
            approval_description="example",
            certify_for_marketplace=True,
            client_token="example",
            customer_metadata_properties="example",
            domain="example",
            drift_check_baselines={"bias": {"config_file": {"s3_uri": "example"}}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sage_maker_model_package, with additional inference
    specifications a set value, additional inference specifications
    to add a set value, approval description example, certify for
    marketplace true, client token example, customer metadata
    properties example, domain example, drift check baselines a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="additional_inference_specifications" type="list(object)">
  An array of additional Inference Specification objects. Optional.
  <Expandable title="properties">
    <ResponseField name="containers" type="list(object)">
      Defines the Docker container images and their configurations (e.g., image URI, environment variables, and model data sources) that SageMaker uses for inference when this additional inference specification is selected. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="container_hostname" type="string">
          Defines the DNS hostname for this container within the SageMaker model package's inference specification, allowing other containers in a multi-container endpoint or pipeline to address it uniquely. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="environment" type="any">
          Environment variables, as key-value string pairs, to configure the container for this additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="framework" type="string">
          Specifies the machine learning framework used by the model container, such as TensorFlow, PyTorch, or MXNet, within an additional inference specification of the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="framework_version" type="string">
          The version of the machine learning framework (e.g., TensorFlow, PyTorch) used to run the model in this additional inference container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image" type="string">
          The URI of the Docker container image to use for inference when this additional inference specification is selected. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image_digest" type="string">
          Specifies the SHA256 digest of the container image, which uniquely identifies the exact image version for the additional inference specification container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_data_source" type="object">
          Specifies the S3 source for the model artifacts used by this additional inference container, with an S3DataSource object defining the S3 URI and data type (e.g., S3Prefix or S3Object) as an alternative to ModelDataUrl. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="s3_data_source" type="object">
              Defines the S3 location (S3Uri) and data type (S3DataType) for the model data source used by the container in this additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="compression_type" type="string">
                  Specifies the compression type (e.g., None or Gzip) for the model artifacts stored in the S3 location referenced by the model data source container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="model_access_config" type="object">
                  Configures access to the model data in Amazon S3, including whether to accept the end-user license agreement (EULA) required for the model. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="accept_eula" type="bool">
                      Indicates whether you accept the End User License Agreement (EULA) required to access the model data from the S3 source, such as for gated or licensed models. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="s3_data_type" type="string">
                  Specifies whether the S3 data source is a prefix (S3Prefix) that points to all objects under a location or a manifest file (ManifestFile) that explicitly lists the S3 objects to use as model data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_uri" type="string">
                  The S3 URI of the model artifacts for the container in the additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_data_url" type="string">
          The S3 URI that specifies the location of the model artifacts for the container in this additional inference specification of the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_input" type="object">
          Specifies the input configuration for the container within an additional inference specification, including the data input configuration string that describes the format and shape of input data for model inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="data_input_config" type="string">
              Specifies the data input configuration for the model container, as a JSON string that defines the input data format and shape (e.g., '&#123;"input": [1, 3, 224, 224]&#125;'), which the model uses to accept inference requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="nearest_model_name" type="string">
          Sets the model name used by SageMaker Edge Manager to identify and package the model in this additional inference specification container for deployment to edge devices. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of this additional inference specification, providing details about the variant, such as its intended use or contents. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of this additional inference specification, which must be unique within the model package and is used to identify the specification when selecting it for a target environment. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_content_types" type="list(string)">
      Specifies the list of supported MIME content types (e.g., 'text/csv', 'application/json') for the corresponding additional inference specification of the SageMaker model package, defining which input formats the model can accept for inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_realtime_inference_instance_types" type="list(string)">
      Specifies the list of SageMaker instance types (e.g., ml.m5.large) that this additional inference specification supports for real-time inference endpoints. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_response_mimetypes" type="list(string)">
      Specifies the list of MIME types that the model can return for inference responses in this additional inference specification, such as 'text/plain' or 'application/json'. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_transform_instance_types" type="list(string)">
      Specifies the types of ML compute instances supported for running batch transform jobs with this additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="additional_inference_specifications_to_add" type="list(object)">
  An array of additional Inference Specification objects. Optional.
  <Expandable title="properties">
    <ResponseField name="containers" type="list(object)">
      Defines the container definitions (e.g., Docker image, model data source, environment variables) that make up the additional inference specification being added to the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="container_hostname" type="string">
          The DNS host name for the container within the additional inference specification, used to route inference requests to the appropriate container in a multi-container endpoint. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="environment" type="any">
          A map of environment variable keys to values that are set in the container for the additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="framework" type="string">
          The machine learning framework (such as TensorFlow or PyTorch) that this container uses, used to configure the additional inference specification for the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="framework_version" type="string">
          Specifies the version of the machine learning framework (e.g., '1.8' for PyTorch) used by the container in an additional inference specification, which SageMaker uses to select the corresponding prebuilt inference image. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image" type="string">
          The URI of the Docker container image (typically an Amazon ECR repository path) that SageMaker runs for this additional inference specification of the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image_digest" type="string">
          The SHA256 digest of the Docker image for the container; when provided, it pins the image to that exact digest in the additional inference specification being added to the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_data_source" type="object">
          Specifies the S3 URI and S3 data type for the model artifacts used by this inference container in the additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="s3_data_source" type="object">
              Defines the S3 data source (e.g., S3Uri and S3DataType) for the model artifacts of a container within an additional inference specification being added to the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="compression_type" type="string">
                  For the model artifact stored in Amazon S3, this property specifies the compression type (such as Gzip or None) that SageMaker should use when downloading and decompressing the model data for the container in the additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="model_access_config" type="object">
                  Specifies access control for the model artifact in S3, primarily including whether to accept the model's end-user license agreement (EULA) via the AcceptEula flag. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="accept_eula" type="bool">
                      Indicates whether to accept the end-user license agreement (EULA) required for accessing this model data source from a third-party model provider. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="s3_data_type" type="string">
                  Determines whether the S3 location contains a manifest file (ManifestFile), a list of S3 prefixes (S3Prefix), or an augmented manifest (AugmentedManifestFile), which controls how SageMaker interprets the model data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_uri" type="string">
                  The S3 URI of the model data that SageMaker uses to load the model for this additional inference specification container. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_data_url" type="string">
          Specifies the Amazon S3 URL (e.g., s3://bucket/prefix/model.tar.gz) containing the model artifacts for a container in an additional inference specification of the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_input" type="object">
          Specifies the model input configuration, including data input shapes and format, for the model container in the additional inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="data_input_config" type="string">
              Specifies the input data configuration string (e.g., JSON describing input tensor shapes and formats) for the model container in this additional inference specification, which SageMaker uses to determine the expected input structure for inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="nearest_model_name" type="string">
          Specifies the name of the model that this container is most closely associated with, used for model lineage and tracking in the SageMaker Model Registry. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string">
      A description of the additional inference specification, providing details about the purpose or distinguishing characteristics of this secondary inference configuration for the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Specifies a unique name for this additional inference specification within the model package, used to identify the specification when deploying the model. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_content_types" type="list(string)">
      Defines the list of MIME content types (e.g., text/csv, application/json) that the additional inference specification accepts for inference requests. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_realtime_inference_instance_types" type="list(string)">
      The allowed Amazon EC2 instance types (e.g., ml.m5.large) for real-time inference when this additional inference specification is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_response_mimetypes" type="list(string)">
      Defines the supported MIME types for responses that the additional inference specification can produce. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="supported_transform_instance_types" type="list(string)">
      Specifies the list of ML compute instance types that this additional inference specification supports for running batch transform jobs in SageMaker. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="approval_description" type="string">
  A description provided for the model approval. Optional.
</ResponseField>

<ResponseField name="certify_for_marketplace" type="bool">
  Whether to certify the model package for listing on AWS Marketplace. Optional.
</ResponseField>

<ResponseField name="client_token" type="string">
  A unique token that guarantees that the call to this API is idempotent. Optional.
</ResponseField>

<ResponseField name="customer_metadata_properties" type="any">
  The metadata properties associated with the model package versions. Optional.
</ResponseField>

<ResponseField name="domain" type="string">
  The machine learning domain of the model package you specified. Optional.
</ResponseField>

<ResponseField name="drift_check_baselines" type="object">
  Represents the drift check baselines that can be used when the model monitor is set using the model package. Optional.
  <Expandable title="properties">
    <ResponseField name="bias" type="object">
      Represents the drift check bias baselines that can be used when the model monitor is set using the model package. Optional.
      <Expandable title="properties">
        <ResponseField name="config_file" type="object">
          Represents a File Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the file source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string">
              The type of content stored in the file source. Optional.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the file source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="post_training_constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pre_training_constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="explainability" type="object">
      Contains explainability metrics for a model. Optional.
      <Expandable title="properties">
        <ResponseField name="config_file" type="object">
          Represents a File Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the file source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string">
              The type of content stored in the file source. Optional.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the file source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_data_quality" type="object">
      Represents the drift check data quality baselines that can be used when the model monitor is set using the model package. Optional.
      <Expandable title="properties">
        <ResponseField name="constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="statistics" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_quality" type="object">
      Represents the drift check model quality baselines that can be used when the model monitor is set using the model package. Optional.
      <Expandable title="properties">
        <ResponseField name="constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="statistics" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="inference_specification" type="object">
  Details about inference jobs that can be run with models based on this model package. Optional.
  <Expandable title="properties">
    <ResponseField name="containers" type="list(object)" required>
      The Amazon ECR registry path of the Docker image that contains the inference code. Required.
      <Expandable title="properties">
        <ResponseField name="container_hostname" type="string">
          Sets the DNS hostname for the container within the inference pipeline, which is used to route requests to this specific container when multiple containers are defined in the inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="environment" type="any">
          Defines the environment variables to set in the inference container of the SageMaker model package, which are passed to the container at runtime to configure the inference code. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="framework" type="string">
          The name of the machine learning framework (such as TensorFlow, PyTorch, or MXNet) used by the inference container within the SageMaker model package's inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="framework_version" type="string">
          Specifies the version of the machine learning framework (such as TensorFlow or MXNet) used in the SageMaker model container for this inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image" type="string">
          The URI of the Docker container image (typically in Amazon ECR) that SageMaker uses to run inference for this model package container. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="image_digest" type="string">
          The SHA256 digest of the Docker container image, used to uniquely identify the exact image version for this inference container in the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_data_source" type="object">
          Specifies the Amazon S3 source of the model artifacts for this container within the inference specification, using an object that provides the S3 URI and optional data type. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="s3_data_source" type="object">
              Defines the Amazon S3 data source (including the S3 URI and data type such as S3Prefix or ManifestFile) from which model artifacts are loaded for this container in the SageMaker model package's inference specification. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="compression_type" type="string">
                  Specifies the compression type of the model data in the S3 bucket, such as 'None' or 'Gzip', used for the container's model data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="model_access_config" type="object">
                  Specifies the access configuration for the S3 model data source, including an AcceptEula flag that indicates whether you accept the End User License Agreement for the model. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="accept_eula" type="bool">
                      Specifies whether to accept the end-user license agreement (EULA) for the model, which is required to allow Amazon SageMaker to access and deploy the model from the specified S3 data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="s3_data_type" type="string">
                  Specifies whether the S3 data source for the model artifacts is a single object ('S3Object') or a prefix ('S3Prefix') containing the model artifacts. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_uri" type="string">
                  Specifies the S3 URI of the model artifacts in the S3 data source used by this container, from which SageMaker loads the model for inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="model_data_url" type="string">
          The Amazon S3 URL where the model artifacts (e.g., model.tar.gz) for this inference container are stored. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_input" type="object">
          Defines the input configuration for the model container, including the data input configuration (DataInputConfig) and the input mode (File or Pipe) used for inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="data_input_config" type="string">
              Defines the model input data configuration, usually a JSON string that specifies input shape, data type, and format expected by the model during inference. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="nearest_model_name" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="supported_content_types" type="list(string)" required>
      The supported MIME types for the input data. Required.
    </ResponseField>
    <ResponseField name="supported_realtime_inference_instance_types" type="list(string)">
      A list of the instance types that are used to generate inferences in real-time Optional.
    </ResponseField>
    <ResponseField name="supported_response_mimetypes" type="list(string)" required>
      The supported MIME types for the output data. Required.
    </ResponseField>
    <ResponseField name="supported_transform_instance_types" type="list(string)">
      A list of the instance types on which a transformation job can be run or on which an endpoint can be deployed. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata_properties" type="object">
  Metadata properties of the tracking entity, trial, or trial component. Optional.
  <Expandable title="properties">
    <ResponseField name="commit_id" type="string">
      The commit ID. Optional.
    </ResponseField>
    <ResponseField name="generated_by" type="string">
      The entity this entity was generated by. Optional.
    </ResponseField>
    <ResponseField name="project_id" type="string">
      The project ID metadata. Optional.
    </ResponseField>
    <ResponseField name="repository" type="string">
      The repository metadata. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_approval_status" type="string">
  The approval status of the model package. Optional.
</ResponseField>

<ResponseField name="model_card" type="object">
  The model card associated with the model package. Optional.
  <Expandable title="properties">
    <ResponseField name="model_card_content" type="string" required>
      The content of the model card. Required.
    </ResponseField>
    <ResponseField name="model_card_status" type="string" required>
      The approval status of the model card within your organization. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_metrics" type="object">
  A structure that contains model metrics reports. Optional.
  <Expandable title="properties">
    <ResponseField name="bias" type="object">
      Contains bias metrics for a model. Optional.
      <Expandable title="properties">
        <ResponseField name="post_training_report" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pre_training_report" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="report" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="explainability" type="object">
      Contains explainability metrics for a model. Optional.
      <Expandable title="properties">
        <ResponseField name="report" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_data_quality" type="object">
      Metrics that measure the quality of the input data for a model. Optional.
      <Expandable title="properties">
        <ResponseField name="constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="statistics" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="model_quality" type="object">
      Metrics that measure the quality of a model. Optional.
      <Expandable title="properties">
        <ResponseField name="constraints" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="statistics" type="object">
          Represents a Metric Source Object. Optional.
          <Expandable title="properties">
            <ResponseField name="content_digest" type="string">
              The digest of the metric source. Optional.
            </ResponseField>
            <ResponseField name="content_type" type="string" required>
              The type of content stored in the metric source. Required.
            </ResponseField>
            <ResponseField name="s3_uri" type="string" required>
              The Amazon S3 URI for the metric source. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_package_description" type="string">
  The description of the model package. Optional.
</ResponseField>

<ResponseField name="model_package_group_name" type="string">
  The name of the model package group. Optional.
</ResponseField>

<ResponseField name="model_package_name" type="string">
  The name or arn of the model package. Optional.
</ResponseField>

<ResponseField name="model_package_status_details" type="object">
  Details about the current status of the model package. Optional.
  <Expandable title="properties">
    <ResponseField name="validation_statuses" type="list(object)">
      Provides the list of validation status items, each describing the name, status (e.g., InProgress, Completed, Failed), and failure reason (if any) for a validation profile or test job associated with the model package. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="failure_reason" type="string">
          The reason why the model package validation failed, if applicable. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of a validation status item in the model package status details, identifying the specific validation job or stage (e.g., a processing job or evaluation) whose status is reported for the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of a model package validation job, with possible values InProgress, Completed, or Failed. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="model_package_version" type="number">
  The version of the model package. Optional.
</ResponseField>

<ResponseField name="sample_payload_url" type="string">
  The Amazon Simple Storage Service (Amazon S3) path where the sample payload are stored pointing to single gzip compressed tar archive. Optional.
</ResponseField>

<ResponseField name="security_config" type="object">
  An optional AWS Key Management Service key to encrypt, decrypt, and re-encrypt model package information for regulated workloads with highly sensitive data. Optional.
  <Expandable title="properties">
    <ResponseField name="kms_key_id" type="string" required>
      The AWS KMS Key ID (KMSKeyId) used for encryption of model package information. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="skip_model_validation" type="string">
  Indicates if you want to skip model validation. Optional.
</ResponseField>

<ResponseField name="source_algorithm_specification" type="object">
  Details about the algorithm that was used to create the model package. Optional.
  <Expandable title="properties">
    <ResponseField name="source_algorithms" type="list(object)" required>
      A list of algorithms that were used to create a model package. Required.
      <Expandable title="properties">
        <ResponseField name="algorithm_name" type="string">
          The name or Amazon Resource Name (ARN) of the SageMaker algorithm used to create the model data for this source algorithm entry in the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="model_data_url" type="string">
          Specifies the S3 URL (e.g., s3://bucket/prefix/model.tar.gz) where the trained model artifacts for this source algorithm are stored in SageMaker Model Package registration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source_uri" type="string">
  The URI of the source for the model package. Optional.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the SageMaker model package, used for resource identification, access control, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag attached to the SageMaker model package, providing user-defined metadata corresponding to the tag key. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="task" type="string">
  The machine learning task your model package accomplishes. Optional.
</ResponseField>

<ResponseField name="validation_specification" type="object">
  Specifies configurations for one or more transform jobs that Amazon SageMaker runs to test the model package. Optional.
  <Expandable title="properties">
    <ResponseField name="validation_profiles" type="list(object)" required>
      Specifies a list of validation profiles, each containing a profile name and a transform job definition, that define how the model package is validated. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="profile_name" type="string">
          The name of a validation profile, used to identify a specific validation job within the validation specification of an AWS SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="transform_job_definition" type="object">
          Defines the batch transform job configuration (such as input/output data sources, compute resources, and transformation settings) used to validate the SageMaker model package within a validation profile. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="batch_strategy" type="string">
              Specifies the batch strategy for the batch transform job used in model package validation, indicating whether the transform job sends each record as a single inference request (SingleRecord) or batches multiple records into one request (MultiRecord). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="environment" type="any">
              Specifies environment variables passed to the model container when the batch transform job defined in this validation profile runs to validate the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_concurrent_transforms" type="number">
              Specifies the maximum number of parallel transform jobs that SageMaker can run concurrently for this transform job definition, controlling resource usage during model validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="max_payload_in_mb" type="number">
              Specifies the maximum size of the payload, in megabytes, that a batch transform job can send to the model associated with this validation profile. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="transform_input" type="object">
              Specifies the input data source (e.g., S3 location, content type, compression type, and split type) for the batch transform job used to validate the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="compression_type" type="string">
                  Compression type for the input data of the batch transform job used in the model package validation profile, indicating whether the data is gzip-compressed or uncompressed (None). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="content_type" type="string">
                  The MIME type (e.g., text/csv or application/x-image) of the input data for the batch transform job that validates this model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="data_source" type="object">
                  Defines the location and format of the input data (e.g., S3 data source) for the batch transform job used to validate the model package within a validation profile. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="s3_data_source" type="object">
                      Defines the S3 data source, including the S3 URI and data type, for the input data used in the transform job that validates the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="s3_data_type" type="string">
                          Specifies whether the S3 input data is interpreted as a manifest file, an S3 prefix, or an augmented manifest file, determining how SageMaker resolves the data source path for the transform job's input. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                        <ResponseField name="s3_uri" type="string">
                          This field specifies the S3 URI of the input dataset used for the model validation transform job of the SageMaker model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="split_type" type="string">
                  Specifies how the input data is split for the transform job, such as Line, RecordIO, TFRecord, or None. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="transform_output" type="object">
              Specifies the Amazon S3 output location and settings for the results of a transform job used to validate a model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="accept" type="string">
                  Specifies the MIME type (e.g., 'text/csv') that declares the format of the transformed output data produced by the validation transform job. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="assemble_with" type="string">
                  Determines how the transform job's output is assembled in Amazon S3: 'None' for separate objects per record, or 'Line' to concatenate all records into a single newline-delimited file. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kms_key_id" type="string">
                  Specifies the AWS KMS key ID used to encrypt the output data of the transform job that validates the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="s3_output_path" type="string">
                  Specifies the S3 URI (bucket and prefix) where the output of the validation batch transform job is stored, used by the model package's validation profile. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="transform_resources" type="object">
              Specifies the ML compute instances (instance type and count) used for the batch transform job that validates the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="instance_count" type="number">
                  Specifies the number of ML compute instances to launch for the batch transform job during model package validation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="instance_type" type="string">
                  Specifies the ML compute instance type (e.g., ml.m5.xlarge) used by the batch transform job that validates the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="volume_kms_key_id" type="string">
                  The AWS KMS key ID used to encrypt data on the storage volume attached to the ML compute instances running the batch transform job for validating the model package. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="validation_role" type="string" required>
      The IAM roles to be used for the validation of the model package. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  The time at which the model package was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  The time at which the model package was last modified. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="model_package_arn" type="string">
  The Amazon Resource Name (ARN) of the model package group. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="model_package_status" type="string">
  The current status of the model package. Computed by AWS after `ship`.
</ResponseField>
