---
title: "aws_notifications_notification_configuration"
description: "Real, generated bindings for `aws_notifications_notification_configuration`."
---

`aws_notifications_notification_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/notifications"

    cfg := notifications.NotificationConfigurationConfig{
        Description: "example",
        Name: "example-name",
        AggregationDuration: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        notifications.NotificationConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NotificationConfiguration } from
      "jsr:@ubx/sdk-aws/aws/notifications/notification_configuration";

    resource(NotificationConfiguration, "example", {
      description: "example",
      name: "example",
      aggregationDuration: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.notifications import (
        NotificationConfiguration,
        NotificationConfigurationConfig,
    )

    cfg = NotificationConfigurationConfig(
        description="example",
        name="example",
        aggregation_duration="example",
        tags={"key": "value"},
    )
    ubx.resource(NotificationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_notifications_notification_configuration with
description, name, aggregation_duration, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aggregation_duration" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="aggregation_duration" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
