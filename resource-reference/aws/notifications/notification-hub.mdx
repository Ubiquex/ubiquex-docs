---
title: "aws_notifications_notification_hub"
description: "Real, generated bindings for `aws_notifications_notification_hub`."
---

`aws_notifications_notification_hub` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/notifications"

    cfg := notifications.NotificationHubConfig{
        NotificationHubRegion: "example",
    }
    ubx.Resource(notifications.NotificationHub, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NotificationHub } from
      "jsr:@ubx/sdk-aws/aws/notifications/notification_hub";

    resource(NotificationHub, "example", {
      notificationHubRegion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.notifications import (
        NotificationHub,
        NotificationHubConfig,
    )

    cfg = NotificationHubConfig(
        notification_hub_region="example",
    )
    ubx.resource(NotificationHub, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_notifications_notification_hub with
notification_hub_region.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="notification_hub_region" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
