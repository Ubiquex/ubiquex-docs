---
title: "aws_notifications_organizations_access"
description: "Real, generated bindings for `aws_notifications_organizations_access`."
---

`aws_notifications_organizations_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/notifications"

    cfg := notifications.OrganizationsAccessConfig{
        Enabled: true,
    }
    ubx.Resource(notifications.OrganizationsAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OrganizationsAccess } from
      "jsr:@ubx/sdk-aws/aws/notifications/organizations_access";

    resource(OrganizationsAccess, "example", {
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.notifications import (
        OrganizationsAccess,
        OrganizationsAccessConfig,
    )

    cfg = OrganizationsAccessConfig(
        enabled=True,
    )
    ubx.resource(OrganizationsAccess, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_notifications_organizations_access with enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
