---
title: "aws_vpn_gateway_attachment"
description: "Real, generated bindings for `aws_vpn_gateway_attachment`."
---

`aws_vpn_gateway_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpn"

    cfg := vpn.GatewayAttachmentConfig{
        VpcId: "example",
        VpnGatewayId: "example",
    }
    ubx.Resource(vpn.GatewayAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayAttachment } from
      "jsr:@ubx/sdk-aws/aws/vpn/gateway_attachment";

    resource(GatewayAttachment, "example", {
      vpcId: "example",
      vpnGatewayId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpn import (
        GatewayAttachment,
        GatewayAttachmentConfig,
    )

    cfg = GatewayAttachmentConfig(
        vpc_id="example",
        vpn_gateway_id="example",
    )
    ubx.resource(GatewayAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpn_gateway_attachment with vpc_id, vpn_gateway_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpn_gateway_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
