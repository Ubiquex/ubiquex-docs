---
title: "aws_vpn_gateway_attachment"
description: "Real, generated bindings for `aws_vpn_gateway_attachment`."
---

`aws_vpn_gateway_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpn "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gateway attachment"})

    		ubx.Resource(vpn.GatewayAttachment, "ci-runner", vpn.GatewayAttachmentConfig{
    			VpcId:        "example",
    			VpnGatewayId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GatewayAttachment } from "jsr:@ubx/sdk-aws/aws/vpn/gateway_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own gateway attachment" });

      resource(GatewayAttachment, "ci-runner", {
        vpcId: "example",
        vpnGatewayId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpn import GatewayAttachment, GatewayAttachmentConfig

    def describe():
        ubx.intent("payments own gateway attachment")

        ubx.resource(GatewayAttachment, "ci-runner", GatewayAttachmentConfig(
            vpc_id="example",
            vpn_gateway_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpn_gateway_attachment, with vpc id example, vpn
    gateway id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpn_gateway_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
