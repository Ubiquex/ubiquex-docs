---
title: "aws_vpn_connection_route"
description: "Real, generated bindings for `aws_vpn_connection_route`."
---

`aws_vpn_connection_route` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpn"

    cfg := vpn.ConnectionRouteConfig{
        DestinationCidrBlock: "example",
        VpnConnectionId: "example",
    }
    ubx.Resource(vpn.ConnectionRoute, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ConnectionRoute } from
      "jsr:@ubx/sdk-aws/aws/vpn/connection_route";

    resource(ConnectionRoute, "example", {
      destinationCidrBlock: "example",
      vpnConnectionId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpn import ConnectionRoute, ConnectionRouteConfig

    cfg = ConnectionRouteConfig(
        destination_cidr_block="example",
        vpn_connection_id="example",
    )
    ubx.resource(ConnectionRoute, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpn_connection_route with destination_cidr_block,
vpn_connection_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="destination_cidr_block" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpn_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
