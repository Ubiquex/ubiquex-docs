---
title: "aws_vpn_concentrator"
description: "Real, generated bindings for `aws_vpn_concentrator`."
---

`aws_vpn_concentrator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpn"

    cfg := vpn.ConcentratorConfig{
        TransitGatewayId: "example",
        Type: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpn.Concentrator, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Concentrator } from
      "jsr:@ubx/sdk-aws/aws/vpn/concentrator";

    resource(Concentrator, "example", {
      transitGatewayId: "example",
      type: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpn import Concentrator, ConcentratorConfig

    cfg = ConcentratorConfig(
        transit_gateway_id="example",
        type="example",
        tags={"key": "value"},
    )
    ubx.resource(Concentrator, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpn_concentrator with transit_gateway_id, type,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpn_concentrator_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
