---
title: "aws_vpn_gateway_route_propagation"
description: "Real, generated bindings for `aws_vpn_gateway_route_propagation`."
---

`aws_vpn_gateway_route_propagation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpn"

    cfg := vpn.GatewayRoutePropagationConfig{
        RouteTableId: "example",
        VpnGatewayId: "example",
    }
    ubx.Resource(vpn.GatewayRoutePropagation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GatewayRoutePropagation } from
      "jsr:@ubx/sdk-aws/aws/vpn/gateway_route_propagation";

    resource(GatewayRoutePropagation, "example", {
      routeTableId: "example",
      vpnGatewayId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpn import (
        GatewayRoutePropagation,
        GatewayRoutePropagationConfig,
    )

    cfg = GatewayRoutePropagationConfig(
        route_table_id="example",
        vpn_gateway_id="example",
    )
    ubx.resource(GatewayRoutePropagation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpn_gateway_route_propagation with route_table_id,
vpn_gateway_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpn_gateway_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
