---
title: "aws_eks_addon"
description: "Real, generated bindings for `aws_eks_addon`."
---

`aws_eks_addon` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own addon"})

    		ubx.Resource(eks.Addon, "ci-runner", eks.AddonConfig{
    			AddonName:                "example-addon-name",
    			ClusterName:              "example-cluster-name",
    			Preserve:                 true,
    			ResolveConflictsOnCreate: "example",
    			ResolveConflictsOnUpdate: "example",
    			ServiceAccountRoleArn:    "arn:aws:iam::123456789012:policy/ci",
    			Tags:                     map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Addon } from "jsr:@ubx/sdk-aws/aws/eks/addon";

    export default stack("payments", () => {
      intent({ summary: "payments own addon" });

      resource(Addon, "ci-runner", {
        addonName: "example-addon-name",
        clusterName: "example-cluster-name",
        preserve: true,
        resolveConflictsOnCreate: "example",
        resolveConflictsOnUpdate: "example",
        serviceAccountRoleArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eks import Addon, AddonConfig

    def describe():
        ubx.intent("payments own addon")

        ubx.resource(Addon, "ci-runner", AddonConfig(
            addon_name="example-addon-name",
            cluster_name="example-cluster-name",
            preserve=True,
            resolve_conflicts_on_create="example",
            resolve_conflicts_on_update="example",
            service_account_role_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_eks_addon, with addon name example-addon-name, cluster
    name example-cluster-name, preserve true, resolve conflicts on
    create example, resolve conflicts on update example, service
    account role arn arn:aws:iam::123456789012:policy/ci, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="addon_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="addon_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configuration_values" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="preserve" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resolve_conflicts_on_create" type="string">
  Optional.
</ResponseField>

<ResponseField name="resolve_conflicts_on_update" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_account_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="addon_version" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="configuration_values" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="modified_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
