---
title: "aws_eks_nodegroup"
description: "`aws_eks_nodegroup` provisions and manages a real group of EC2 worker nodes, patched and scaled by EKS itself..."
---

`aws_eks_nodegroup` provisions and manages a real group of EC2 worker nodes, patched and scaled by EKS itself, that join an `aws_eks_cluster` to actually run pods, an alternative to Fargate for workloads that need EC2-level control.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own nodegroup"})

    		ubx.Resource(eks.Nodegroup, "example", eks.NodegroupConfig{
    			ClusterName:        "example-cluster-name",
    			NodeRole:           "example",
    			Subnets:            []string{"example"},
    			AmiType:            "example",
    			CapacityType:       "example",
    			DiskSize:           1,
    			ForceUpdateEnabled: true,
    			InstanceTypes:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Nodegroup } from "@ubx/sdk-aws/aws/eks/nodegroup";

    export default stack("example", () => {
      intent({ summary: "example own nodegroup" });

      resource(Nodegroup, "example", {
        clusterName: "example-cluster-name",
        nodeRole: "example",
        subnets: ["example"],
        amiType: "example",
        capacityType: "example",
        diskSize: 1,
        forceUpdateEnabled: true,
        instanceTypes: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eks import Nodegroup, NodegroupConfig

    def describe():
        ubx.intent("example own nodegroup")

        ubx.resource(Nodegroup, "example", NodegroupConfig(
            cluster_name="example-cluster-name",
            node_role="example",
            subnets=["example"],
            ami_type="example",
            capacity_type="example",
            disk_size=1,
            force_update_enabled=True,
            instance_types=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_eks_nodegroup, with cluster name example-cluster-name,
    node role example, subnets a set value, ami type example,
    capacity type example, disk size 1, force update enabled true,
    instance types a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="ami_type" type="string">
  The AMI type for your node group. Optional.
</ResponseField>

<ResponseField name="capacity_type" type="string">
  The capacity type of your managed node group. Optional.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Name of the cluster to create the node group in. Required.
</ResponseField>

<ResponseField name="disk_size" type="number">
  The root device disk size (in GiB) for your node group instances. Optional.
</ResponseField>

<ResponseField name="force_update_enabled" type="bool">
  Force the update if the existing node group's pods are unable to be drained due to a pod disruption budget issue. Optional.
</ResponseField>

<ResponseField name="instance_types" type="list(string)">
  Specify the instance types for a node group. Optional.
</ResponseField>

<ResponseField name="labels" type="any">
  The Kubernetes labels to be applied to the nodes in the node group when they are created. Optional.
</ResponseField>

<ResponseField name="launch_template" type="object">
  An object representing a launch template specification for AWS EKS Nodegroup. Optional.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      The ID of the EC2 launch template that specifies the configuration (e.g., instance type, AMI, user data) for the worker nodes in the EKS node group. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the EC2 launch template to use for configuring the node group's instances. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the AWS EC2 launch template that the node group will use to launch instances. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="node_repair_config" type="object">
  The node auto repair configuration for node group. Optional.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Set this value to true to enable node auto repair for the node group. Optional.
    </ResponseField>
    <ResponseField name="max_parallel_nodes_repaired_count" type="number">
      Specify the maximum number of nodes that can be repaired concurrently or in parallel, expressed as a count of unhealthy nodes. This gives you finer-grained control over the pace of node replacements. When using this, you cannot also set MaxParallelNodesRepairedPercentage at the same time. Optional.
    </ResponseField>
    <ResponseField name="max_parallel_nodes_repaired_percentage" type="number">
      Specify the maximum number of nodes that can be repaired concurrently or in parallel, expressed as a percentage of unhealthy nodes. This gives you finer-grained control over the pace of node replacements. When using this, you cannot also set MaxParallelNodesRepairedCount at the same time. Optional.
    </ResponseField>
    <ResponseField name="max_unhealthy_node_threshold_count" type="number">
      Specify a count threshold of unhealthy nodes, above which node auto repair actions will stop. When using this, you cannot also set MaxUnhealthyNodeThresholdPercentage at the same time. Optional.
    </ResponseField>
    <ResponseField name="max_unhealthy_node_threshold_percentage" type="number">
      Specify a percentage threshold of unhealthy nodes, above which node auto repair actions will stop. When using this, you cannot also set MaxUnhealthyNodeThresholdCount at the same time. Optional.
    </ResponseField>
    <ResponseField name="node_repair_config_overrides" type="list(object)">
      Specify granular overrides for specific repair actions. These overrides control the repair action and the repair delay time before a node is considered eligible for repair. If you use this, you must specify all the values. Optional.
      <Expandable title="properties">
        <ResponseField name="min_repair_wait_time_mins" type="number">
          Sets the minimum wait time (in minutes) that an unhealthy node in the EKS managed node group must remain in a failed state before the node repair process automatically replaces it. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_monitoring_condition" type="string">
          Specifies the Kubernetes node condition name (e.g., Ready, NetworkUnavailable) that the EKS node auto-repair feature monitors to determine that a node is unhealthy and should be replaced. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_unhealthy_reason" type="string">
          The EKS node health detection reason code (such as DiskPressure, KernelDeadlock, or ReadonlyFilesystem) that identifies which unhealthy node condition this node repair configuration override applies to, allowing you to customize the automated repair behavior for that specific failure reason. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repair_action" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="node_role" type="string" required>
  The Amazon Resource Name (ARN) of the IAM role to associate with your node group. Required.
</ResponseField>

<ResponseField name="nodegroup_name" type="string">
  The unique name to give your node group. Optional.
</ResponseField>

<ResponseField name="release_version" type="string">
  The AMI version of the Amazon EKS-optimized AMI to use with your node group. Optional.
</ResponseField>

<ResponseField name="remote_access" type="object">
  An object representing a remote access configuration specification for AWS EKS Nodegroup. Optional.
  <Expandable title="properties">
    <ResponseField name="ec2_ssh_key" type="string" required>
      The ec2_ssh_key field specifies the name of an existing EC2 key pair that is used to enable SSH access to the worker nodes in the EKS nodegroup. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_security_groups" type="list(string)">
      The list of EC2 security group IDs that are allowed SSH (port 22) access to the worker nodes when remote access is enabled for the EKS node group. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="scaling_config" type="object">
  An object representing a auto scaling group specification for AWS EKS Nodegroup. Optional.
  <Expandable title="properties">
    <ResponseField name="desired_size" type="number">
      The desired number of worker nodes that the EKS node group should maintain, as part of its scaling configuration. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="max_size" type="number">
      The maximum number of nodes that the node group's Auto Scaling group can scale out to. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="min_size" type="number">
      The minimum number of worker nodes that the EKS managed node group will scale down to, setting the lower limit for the Auto Scaling group's instance count. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subnets" type="list(string)" required>
  The subnets to use for the Auto Scaling group that is created for your node group. Required.
</ResponseField>

<ResponseField name="tags" type="any">
  The metadata, as key-value pairs, to apply to the node group to assist with categorization and organization. Follows same schema as Labels for consistency. Optional.
</ResponseField>

<ResponseField name="taints" type="list(object)">
  The Kubernetes taints to be applied to the nodes in the node group when they are created. Optional.
  <Expandable title="properties">
    <ResponseField name="effect" type="string">
      The Kubernetes taint effect (e.g., NO_SCHEDULE, NO_EXECUTE, or PREFER_NO_SCHEDULE) that determines how pods are scheduled on nodes in the EKS node group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="key" type="string">
      The key of a Kubernetes taint applied to the node group, which together with the effect defines how pods are restricted from scheduling onto the nodes. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value component of a Kubernetes taint applied to the node group's nodes, paired with the taint key and effect. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_config" type="object">
  The node group update configuration. Optional.
  <Expandable title="properties">
    <ResponseField name="max_unavailable" type="number">
      The maximum number of nodes unavailable at once during a version update. Nodes will be updated in parallel. This value or maxUnavailablePercentage is required to have a value.The maximum number is 100. Optional.
    </ResponseField>
    <ResponseField name="max_unavailable_percentage" type="number">
      The maximum percentage of nodes unavailable during a version update. This percentage of nodes will be updated in parallel, up to 100 nodes at once. This value or maxUnavailable is required to have a value. Optional.
    </ResponseField>
    <ResponseField name="update_strategy" type="string">
      The configuration for the behavior to follow during an node group version update of this managed node group. You choose between two possible strategies for replacing nodes during an UpdateNodegroupVersion action. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string">
  The Kubernetes version to use for your managed nodes. Optional.
</ResponseField>

<ResponseField name="warm_pool_config" type="object">
  The warm pool configuration for the node group. Optional.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
      Enable or disable warm pool for the node group. Optional.
    </ResponseField>
    <ResponseField name="max_group_prepared_capacity" type="number">
      The maximum number of instances that are allowed to be in the warm pool. Optional.
    </ResponseField>
    <ResponseField name="min_size" type="number">
      The minimum number of instances to maintain in the warm pool. Optional.
    </ResponseField>
    <ResponseField name="pool_state" type="string">
      The desired state of warm pool instances. Optional.
    </ResponseField>
    <ResponseField name="reuse_on_scale_in" type="bool">
      Whether to return instances to the warm pool during scale-in instead of terminating them. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the EKS node group, which uniquely identifies it within AWS and is used for permissions and resource referencing. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
