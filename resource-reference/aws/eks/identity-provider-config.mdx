---
title: "aws_eks_identity_provider_config"
description: "Real, generated bindings for `aws_eks_identity_provider_config`."
---

`aws_eks_identity_provider_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eks"

    cfg := eks.IdentityProviderConfigConfig{
        ClusterName: "example-cluster-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(eks.IdentityProviderConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IdentityProviderConfig } from
      "jsr:@ubx/sdk-aws/aws/eks/identity_provider_config";

    resource(IdentityProviderConfig, "example", {
      clusterName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.eks import (
        IdentityProviderConfig,
        IdentityProviderConfigConfig,
    )

    cfg = IdentityProviderConfigConfig(
        cluster_name="example",
        tags={"key": "value"},
    )
    ubx.resource(IdentityProviderConfig, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_eks_identity_provider_config with cluster_name,
tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_provider_config_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
