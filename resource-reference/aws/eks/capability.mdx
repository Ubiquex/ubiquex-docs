---
title: "aws_eks_capability"
description: "Real, generated bindings for `aws_eks_capability`."
---

`aws_eks_capability` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eks"

    cfg := eks.CapabilityConfig{
        CapabilityName: "example-capability-name",
        ClusterName: "example-cluster-name",
        DeletePropagationPolicy: "example",
        RoleArn: "example",
        Type: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(eks.Capability, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Capability } from "jsr:@ubx/sdk-aws/aws/eks/capability";

    resource(Capability, "example", {
      capabilityName: "example",
      clusterName: "example",
      deletePropagationPolicy: "example",
      roleArn: "example",
      type: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.eks import Capability, CapabilityConfig

    cfg = CapabilityConfig(
        capability_name="example",
        cluster_name="example",
        delete_propagation_policy="example",
        role_arn="example",
        type="example",
        tags={"key": "value"},
    )
    ubx.resource(Capability, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_eks_capability with capability_name, cluster_name,
delete_propagation_policy, role_arn, type, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="capability_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="delete_propagation_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Computed by AWS after `ship`.
</ResponseField>
