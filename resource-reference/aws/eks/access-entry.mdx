---
title: "aws_eks_access_entry"
description: "Real, generated bindings for `aws_eks_access_entry`."
---

`aws_eks_access_entry` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access entry"})

    		ubx.Resource(eks.AccessEntry, "ci-runner", eks.AccessEntryConfig{
    			ClusterName:  "example-cluster-name",
    			PrincipalArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:         map[string]string{"managed-by": "ubx"},
    			Type:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessEntry } from "jsr:@ubx/sdk-aws/aws/eks/access_entry";

    export default stack("payments", () => {
      intent({ summary: "payments own access entry" });

      resource(AccessEntry, "ci-runner", {
        clusterName: "example-cluster-name",
        principalArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eks import AccessEntry, AccessEntryConfig

    def describe():
        ubx.intent("payments own access entry")

        ubx.resource(AccessEntry, "ci-runner", AccessEntryConfig(
            cluster_name="example-cluster-name",
            principal_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_eks_access_entry, with cluster name
    example-cluster-name, principal arn
    arn:aws:iam::123456789012:policy/ci, tags a set value, type
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kubernetes_groups" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="access_entry_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kubernetes_groups" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="modified_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
