---
title: "aws_eks_access_entry"
description: "Real, generated bindings for `aws_eks_access_entry`."
---

`aws_eks_access_entry` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/eks"

    cfg := eks.AccessEntryConfig{
        ClusterName: "example-cluster-name",
        PrincipalArn: "example",
        KubernetesGroups: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(eks.AccessEntry, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccessEntry } from
      "jsr:@ubx/sdk-aws/aws/eks/access_entry";

    resource(AccessEntry, "example", {
      clusterName: "example",
      principalArn: "example",
      kubernetesGroups: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.eks import AccessEntry, AccessEntryConfig

    cfg = AccessEntryConfig(
        cluster_name="example",
        principal_arn="example",
        kubernetes_groups=["example"],
        tags={"key": "value"},
    )
    ubx.resource(AccessEntry, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_eks_access_entry with cluster_name, principal_arn,
kubernetes_groups, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kubernetes_groups" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="access_entry_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kubernetes_groups" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="modified_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
