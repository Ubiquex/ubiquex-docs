---
title: "aws_eks_fargate_profile"
description: "Real, generated bindings for `aws_eks_fargate_profile`."
---

`aws_eks_fargate_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eks"

    cfg := eks.FargateProfileConfig{
        ClusterName: "example-cluster-name",
        FargateProfileName: "example-fargate-profile-name",
        PodExecutionRoleArn: "example",
        SubnetIds: []string{"example"},
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(eks.FargateProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FargateProfile } from
      "jsr:@ubx/sdk-aws/aws/eks/fargate_profile";

    resource(FargateProfile, "example", {
      clusterName: "example",
      fargateProfileName: "example",
      podExecutionRoleArn: "example",
      subnetIds: ["example"],
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.eks import FargateProfile, FargateProfileConfig

    cfg = FargateProfileConfig(
        cluster_name="example",
        fargate_profile_name="example",
        pod_execution_role_arn="example",
        subnet_ids=["example"],
        tags={"key": "value"},
    )
    ubx.resource(FargateProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_eks_fargate_profile with cluster_name,
fargate_profile_name, pod_execution_role_arn, subnet_ids, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="fargate_profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pod_execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
