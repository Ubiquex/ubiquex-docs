---
title: "aws_eks_fargate_profile"
description: "Real, generated bindings for `aws_eks_fargate_profile`."
---

`aws_eks_fargate_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	eks "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/eks"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fargate profile"})

    		ubx.Resource(eks.FargateProfile, "ci-runner", eks.FargateProfileConfig{
    			ClusterName:         "example-cluster-name",
    			FargateProfileName:  "example-fargate-profile-name",
    			PodExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
    			SubnetIds:           []string{"example"},
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FargateProfile } from "jsr:@ubx/sdk-aws/aws/eks/fargate_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own fargate profile" });

      resource(FargateProfile, "ci-runner", {
        clusterName: "example-cluster-name",
        fargateProfileName: "example-fargate-profile-name",
        podExecutionRoleArn: "arn:aws:iam::123456789012:policy/ci",
        subnetIds: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.eks import FargateProfile, FargateProfileConfig

    def describe():
        ubx.intent("payments own fargate profile")

        ubx.resource(FargateProfile, "ci-runner", FargateProfileConfig(
            cluster_name="example-cluster-name",
            fargate_profile_name="example-fargate-profile-name",
            pod_execution_role_arn="arn:aws:iam::123456789012:policy/ci",
            subnet_ids=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_eks_fargate_profile, with cluster name
    example-cluster-name, fargate profile name
    example-fargate-profile-name, pod execution role arn
    arn:aws:iam::123456789012:policy/ci, subnet ids a set value, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="fargate_profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pod_execution_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
