---
title: "aws_s3files_mount_target"
description: "Real, generated bindings for `aws_s3files_mount_target`."
---

`aws_s3files_mount_target` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3files "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3files"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mount target"})

    		ubx.Resource(s3files.MountTarget, "ci-runner", s3files.MountTargetConfig{
    			FileSystemId:  "example",
    			SubnetId:      "example",
    			IpAddressType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MountTarget } from "jsr:@ubx/sdk-aws/aws/s3files/mount_target";

    export default stack("payments", () => {
      intent({ summary: "payments own mount target" });

      resource(MountTarget, "ci-runner", {
        fileSystemId: "example",
        subnetId: "example",
        ipAddressType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3files import MountTarget, MountTargetConfig

    def describe():
        ubx.intent("payments own mount target")

        ubx.resource(MountTarget, "ci-runner", MountTargetConfig(
            file_system_id="example",
            subnet_id="example",
            ip_address_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3files_mount_target, with file system id example,
    subnet id example, ip address type example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="file_system_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ip_address_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="ipv4_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="availability_zone_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipv4_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_address" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="network_interface_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
