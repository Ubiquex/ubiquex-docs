---
title: "aws_s3files_file_system_policy"
description: "Real, generated bindings for `aws_s3files_file_system_policy`."
---

`aws_s3files_file_system_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3files"

    cfg := s3files.FileSystemPolicyConfig{
        FileSystemId: "example",
        Policy: "example",
    }
    ubx.Resource(s3files.FileSystemPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FileSystemPolicy } from
      "jsr:@ubx/sdk-aws/aws/s3files/file_system_policy";

    resource(FileSystemPolicy, "example", {
      fileSystemId: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3files import (
        FileSystemPolicy,
        FileSystemPolicyConfig,
    )

    cfg = FileSystemPolicyConfig(
        file_system_id="example",
        policy="example",
    )
    ubx.resource(FileSystemPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3files_file_system_policy with file_system_id,
policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="file_system_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
