---
title: "aws_s3files_synchronization_configuration"
description: "Real, generated bindings for `aws_s3files_synchronization_configuration`."
---

`aws_s3files_synchronization_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3files"

    cfg := s3files.SynchronizationConfigurationConfig{
        FileSystemId: "example",
        LatestVersionNumber: 1,
    }
    ubx.Resource(
        s3files.SynchronizationConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SynchronizationConfiguration } from
      "jsr:@ubx/sdk-aws/aws/s3files/synchronization_configuration";

    resource(SynchronizationConfiguration, "example", {
      fileSystemId: "example",
      latestVersionNumber: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3files import (
        SynchronizationConfiguration,
        SynchronizationConfigurationConfig,
    )

    cfg = SynchronizationConfigurationConfig(
        file_system_id="example",
        latest_version_number=1,
    )
    ubx.resource(SynchronizationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3files_synchronization_configuration with
file_system_id, latest_version_number.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="file_system_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="latest_version_number" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="latest_version_number" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
