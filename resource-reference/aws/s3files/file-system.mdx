---
title: "aws_s3files_file_system"
description: "Real, generated bindings for `aws_s3files_file_system`."
---

`aws_s3files_file_system` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/s3files"

    cfg := s3files.FileSystemConfig{
        Bucket: "example",
        RoleArn: "example",
        AcceptBucketWarning: true,
        KmsKeyId: "example",
    }
    ubx.Resource(s3files.FileSystem, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { FileSystem } from
      "jsr:@ubx/sdk-aws/aws/s3files/file_system";

    resource(FileSystem, "example", {
      bucket: "example",
      roleArn: "example",
      acceptBucketWarning: true,
      kmsKeyId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.s3files import FileSystem, FileSystemConfig

    cfg = FileSystemConfig(
        bucket="example",
        role_arn="example",
        accept_bucket_warning=True,
        kms_key_id="example",
    )
    ubx.resource(FileSystem, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_s3files_file_system with bucket, role_arn,
accept_bucket_warning, kms_key_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="accept_bucket_warning" type="bool">
  Optional.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
