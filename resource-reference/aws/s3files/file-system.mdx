---
title: "aws_s3files_file_system"
description: "Real, generated bindings for `aws_s3files_file_system`."
---

`aws_s3files_file_system` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3files "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3files"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own file system"})

    		ubx.Resource(s3files.FileSystem, "ci-runner", s3files.FileSystemConfig{
    			Bucket:              "example",
    			RoleArn:             "arn:aws:iam::123456789012:policy/ci",
    			AcceptBucketWarning: true,
    			Prefix:              "example",
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FileSystem } from "jsr:@ubx/sdk-aws/aws/s3files/file_system";

    export default stack("payments", () => {
      intent({ summary: "payments own file system" });

      resource(FileSystem, "ci-runner", {
        bucket: "example",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        acceptBucketWarning: true,
        prefix: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3files import FileSystem, FileSystemConfig

    def describe():
        ubx.intent("payments own file system")

        ubx.resource(FileSystem, "ci-runner", FileSystemConfig(
            bucket="example",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            accept_bucket_warning=True,
            prefix="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3files_file_system, with bucket example, role arn
    arn:aws:iam::123456789012:policy/ci, accept bucket warning true,
    prefix example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accept_bucket_warning" type="bool">
  Optional.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  S3 bucket ARN Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  KMS key ID for encryption Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix" type="string">
  S3 bucket prefix Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  IAM role ARN for S3 access Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="creation_time" type="string">
  Creation time Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  KMS key ID for encryption Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  File system name Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  AWS account ID of the owner Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  File system status Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_message" type="string">
  Status message Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
