---
title: "aws_vpc_ipam_organization_admin_account"
description: "Real, generated bindings for `aws_vpc_ipam_organization_admin_account`."
---

`aws_vpc_ipam_organization_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ipam organization admin account"})

    		ubx.Resource(vpc.IpamOrganizationAdminAccount, "ci-runner", vpc.IpamOrganizationAdminAccountConfig{
    			DelegatedAdminAccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpamOrganizationAdminAccount } from "jsr:@ubx/sdk-aws/aws/vpc/ipam_organization_admin_account";

    export default stack("payments", () => {
      intent({ summary: "payments own ipam organization admin account" });

      resource(IpamOrganizationAdminAccount, "ci-runner", {
        delegatedAdminAccountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import IpamOrganizationAdminAccount, IpamOrganizationAdminAccountConfig

    def describe():
        ubx.intent("payments own ipam organization admin account")

        ubx.resource(IpamOrganizationAdminAccount, "ci-runner", IpamOrganizationAdminAccountConfig(
            delegated_admin_account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_ipam_organization_admin_account, with delegated
    admin account id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delegated_admin_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_principal" type="string">
  Computed by AWS after `ship`.
</ResponseField>
