---
title: "aws_vpc_ipam_organization_admin_account"
description: "Real, generated bindings for `aws_vpc_ipam_organization_admin_account`."
---

`aws_vpc_ipam_organization_admin_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.IpamOrganizationAdminAccountConfig{
        DelegatedAdminAccountId: "example",
    }
    ubx.Resource(vpc.IpamOrganizationAdminAccount, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IpamOrganizationAdminAccount } from
      "jsr:@ubx/sdk-aws/aws/vpc/ipam_organization_admin_account";

    resource(IpamOrganizationAdminAccount, "example", {
      delegatedAdminAccountId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        IpamOrganizationAdminAccount,
        IpamOrganizationAdminAccountConfig,
    )

    cfg = IpamOrganizationAdminAccountConfig(
        delegated_admin_account_id="example",
    )
    ubx.resource(IpamOrganizationAdminAccount, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam_organization_admin_account with
delegated_admin_account_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delegated_admin_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="email" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_principal" type="string">
  Computed by AWS after `ship`.
</ResponseField>
