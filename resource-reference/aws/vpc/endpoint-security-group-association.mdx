---
title: "aws_vpc_endpoint_security_group_association"
description: "Real, generated bindings for `aws_vpc_endpoint_security_group_association`."
---

`aws_vpc_endpoint_security_group_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint security group association"})

    		ubx.Resource(vpc.EndpointSecurityGroupAssociation, "ci-runner", vpc.EndpointSecurityGroupAssociationConfig{
    			SecurityGroupId:           "example",
    			VpcEndpointId:             "example",
    			ReplaceDefaultAssociation: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointSecurityGroupAssociation } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_security_group_association";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint security group association" });

      resource(EndpointSecurityGroupAssociation, "ci-runner", {
        securityGroupId: "example",
        vpcEndpointId: "example",
        replaceDefaultAssociation: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointSecurityGroupAssociation, EndpointSecurityGroupAssociationConfig

    def describe():
        ubx.intent("payments own endpoint security group association")

        ubx.resource(EndpointSecurityGroupAssociation, "ci-runner", EndpointSecurityGroupAssociationConfig(
            security_group_id="example",
            vpc_endpoint_id="example",
            replace_default_association=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_security_group_association, with security
    group id example, vpc endpoint id example, replace default
    association true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="replace_default_association" type="bool">
  Optional.
</ResponseField>

<ResponseField name="security_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
