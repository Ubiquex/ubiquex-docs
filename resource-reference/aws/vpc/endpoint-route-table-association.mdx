---
title: "aws_vpc_endpoint_route_table_association"
description: "Real, generated bindings for `aws_vpc_endpoint_route_table_association`."
---

`aws_vpc_endpoint_route_table_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint route table association"})

    		ubx.Resource(vpc.EndpointRouteTableAssociation, "ci-runner", vpc.EndpointRouteTableAssociationConfig{
    			RouteTableId:  "example",
    			VpcEndpointId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointRouteTableAssociation } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_route_table_association";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint route table association" });

      resource(EndpointRouteTableAssociation, "ci-runner", {
        routeTableId: "example",
        vpcEndpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointRouteTableAssociation, EndpointRouteTableAssociationConfig

    def describe():
        ubx.intent("payments own endpoint route table association")

        ubx.resource(EndpointRouteTableAssociation, "ci-runner", EndpointRouteTableAssociationConfig(
            route_table_id="example",
            vpc_endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_route_table_association, with route table
    id example, vpc endpoint id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
