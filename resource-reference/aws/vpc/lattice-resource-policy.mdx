---
title: "aws_vpc_lattice_resource_policy"
description: "`aws_vpc_lattice_resource_policy` attaches a real resource-based policy to an `aws_vpc_lattice_service_network`..."
---

`aws_vpc_lattice_resource_policy` attaches a real resource-based policy to an `aws_vpc_lattice_service_network`, controlling which principals or accounts can associate their own VPCs or services with it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lattice resource policy"})

    		ubx.Resource(vpc.LatticeResourcePolicy, "ci-runner", vpc.LatticeResourcePolicyConfig{
    			Policy:      "example",
    			ResourceArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LatticeResourcePolicy } from "./local-sdk/aws/sdk/typescript/aws/vpc/lattice_resource_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own lattice resource policy" });

      resource(LatticeResourcePolicy, "ci-runner", {
        policy: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.vpc import LatticeResourcePolicy, LatticeResourcePolicyConfig

    def describe():
        ubx.intent("payments own lattice resource policy")

        ubx.resource(LatticeResourcePolicy, "ci-runner", LatticeResourcePolicyConfig(
            policy="example",
            resource_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_lattice_resource_policy, with policy example,
    resource arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="policy" type="any" required>
  The JSON policy document that defines the permissions for the VPC Lattice resource, such as a service network or service, controlling which principals can access the resource. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  The Amazon Resource Name (ARN) of the VPC Lattice resource (such as a service or service network) to which this resource policy is attached. Required. **(AI-inferred)**
</ResponseField>
