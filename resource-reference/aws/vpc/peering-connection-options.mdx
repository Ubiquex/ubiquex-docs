---
title: "aws_vpc_peering_connection_options"
description: "Real, generated bindings for `aws_vpc_peering_connection_options`."
---

`aws_vpc_peering_connection_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.PeeringConnectionOptionsConfig{
        VpcPeeringConnectionId: "example",
    }
    ubx.Resource(vpc.PeeringConnectionOptions, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PeeringConnectionOptions } from
      "jsr:@ubx/sdk-aws/aws/vpc/peering_connection_options";

    resource(PeeringConnectionOptions, "example", {
      vpcPeeringConnectionId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        PeeringConnectionOptions,
        PeeringConnectionOptionsConfig,
    )

    cfg = PeeringConnectionOptionsConfig(
        vpc_peering_connection_id="example",
    )
    ubx.resource(PeeringConnectionOptions, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_peering_connection_options with
vpc_peering_connection_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_peering_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
