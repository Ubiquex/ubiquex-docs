---
title: "aws_vpc_peering_connection_options"
description: "Real, generated bindings for `aws_vpc_peering_connection_options`."
---

`aws_vpc_peering_connection_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own peering connection options"})

    		ubx.Resource(vpc.PeeringConnectionOptions, "ci-runner", vpc.PeeringConnectionOptionsConfig{
    			VpcPeeringConnectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PeeringConnectionOptions } from "jsr:@ubx/sdk-aws/aws/vpc/peering_connection_options";

    export default stack("payments", () => {
      intent({ summary: "payments own peering connection options" });

      resource(PeeringConnectionOptions, "ci-runner", {
        vpcPeeringConnectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import PeeringConnectionOptions, PeeringConnectionOptionsConfig

    def describe():
        ubx.intent("payments own peering connection options")

        ubx.resource(PeeringConnectionOptions, "ci-runner", PeeringConnectionOptionsConfig(
            vpc_peering_connection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_peering_connection_options, with vpc peering
    connection id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_peering_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
