---
title: "aws_vpc_ipam_resource_discovery"
description: "Real, generated bindings for `aws_vpc_ipam_resource_discovery`."
---

`aws_vpc_ipam_resource_discovery` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.IpamResourceDiscoveryConfig{
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpc.IpamResourceDiscovery, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IpamResourceDiscovery } from
      "jsr:@ubx/sdk-aws/aws/vpc/ipam_resource_discovery";

    resource(IpamResourceDiscovery, "example", {
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        IpamResourceDiscovery,
        IpamResourceDiscoveryConfig,
    )

    cfg = IpamResourceDiscoveryConfig(
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(IpamResourceDiscovery, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam_resource_discovery with description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_resource_discovery_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
