---
title: "aws_vpc_route_server_endpoint"
description: "Real, generated bindings for `aws_vpc_route_server_endpoint`."
---

`aws_vpc_route_server_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.RouteServerEndpointConfig{
        RouteServerId: "example",
        SubnetId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpc.RouteServerEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RouteServerEndpoint } from
      "jsr:@ubx/sdk-aws/aws/vpc/route_server_endpoint";

    resource(RouteServerEndpoint, "example", {
      routeServerId: "example",
      subnetId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        RouteServerEndpoint,
        RouteServerEndpointConfig,
    )

    cfg = RouteServerEndpointConfig(
        route_server_id="example",
        subnet_id="example",
        tags={"key": "value"},
    )
    ubx.resource(RouteServerEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_route_server_endpoint with route_server_id,
subnet_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="eni_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="eni_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_endpoint_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
