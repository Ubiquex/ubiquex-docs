---
title: "aws_vpc_route_server_endpoint"
description: "Real, generated bindings for `aws_vpc_route_server_endpoint`."
---

`aws_vpc_route_server_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own route server endpoint"})

    		ubx.Resource(vpc.RouteServerEndpoint, "ci-runner", vpc.RouteServerEndpointConfig{
    			RouteServerId: "example",
    			SubnetId:      "example",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RouteServerEndpoint } from "jsr:@ubx/sdk-aws/aws/vpc/route_server_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own route server endpoint" });

      resource(RouteServerEndpoint, "ci-runner", {
        routeServerId: "example",
        subnetId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import RouteServerEndpoint, RouteServerEndpointConfig

    def describe():
        ubx.intent("payments own route server endpoint")

        ubx.resource(RouteServerEndpoint, "ci-runner", RouteServerEndpointConfig(
            route_server_id="example",
            subnet_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_route_server_endpoint, with route server id
    example, subnet id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="eni_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="eni_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_endpoint_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
