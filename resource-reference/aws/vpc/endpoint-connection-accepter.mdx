---
title: "aws_vpc_endpoint_connection_accepter"
description: "Real, generated bindings for `aws_vpc_endpoint_connection_accepter`."
---

`aws_vpc_endpoint_connection_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint connection accepter"})

    		ubx.Resource(vpc.EndpointConnectionAccepter, "ci-runner", vpc.EndpointConnectionAccepterConfig{
    			VpcEndpointId:        "example",
    			VpcEndpointServiceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointConnectionAccepter } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_connection_accepter";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint connection accepter" });

      resource(EndpointConnectionAccepter, "ci-runner", {
        vpcEndpointId: "example",
        vpcEndpointServiceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointConnectionAccepter, EndpointConnectionAccepterConfig

    def describe():
        ubx.intent("payments own endpoint connection accepter")

        ubx.resource(EndpointConnectionAccepter, "ci-runner", EndpointConnectionAccepterConfig(
            vpc_endpoint_id="example",
            vpc_endpoint_service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_connection_accepter, with vpc endpoint id
    example, vpc endpoint service id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_endpoint_service_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
