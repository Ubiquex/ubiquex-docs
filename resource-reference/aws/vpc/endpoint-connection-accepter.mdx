---
title: "aws_vpc_endpoint_connection_accepter"
description: "Real, generated bindings for `aws_vpc_endpoint_connection_accepter`."
---

`aws_vpc_endpoint_connection_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.EndpointConnectionAccepterConfig{
        VpcEndpointId: "example",
        VpcEndpointServiceId: "example",
    }
    ubx.Resource(vpc.EndpointConnectionAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointConnectionAccepter } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_connection_accepter";

    resource(EndpointConnectionAccepter, "example", {
      vpcEndpointId: "example",
      vpcEndpointServiceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        EndpointConnectionAccepter,
        EndpointConnectionAccepterConfig,
    )

    cfg = EndpointConnectionAccepterConfig(
        vpc_endpoint_id="example",
        vpc_endpoint_service_id="example",
    )
    ubx.resource(EndpointConnectionAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_connection_accepter with
vpc_endpoint_id, vpc_endpoint_service_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_endpoint_service_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
