---
title: "aws_vpc_block_public_access_exclusion"
description: "Real, generated bindings for `aws_vpc_block_public_access_exclusion`."
---

`aws_vpc_block_public_access_exclusion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own block public access exclusion"})

    		ubx.Resource(vpc.BlockPublicAccessExclusion, "ci-runner", vpc.BlockPublicAccessExclusionConfig{
    			InternetGatewayExclusionMode: "example",
    			SubnetId:                     "example",
    			Tags:                         map[string]string{"managed-by": "ubx"},
    			VpcId:                        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BlockPublicAccessExclusion } from "jsr:@ubx/sdk-aws/aws/vpc/block_public_access_exclusion";

    export default stack("payments", () => {
      intent({ summary: "payments own block public access exclusion" });

      resource(BlockPublicAccessExclusion, "ci-runner", {
        internetGatewayExclusionMode: "example",
        subnetId: "example",
        tags: { "managed-by": "ubx" },
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import BlockPublicAccessExclusion, BlockPublicAccessExclusionConfig

    def describe():
        ubx.intent("payments own block public access exclusion")

        ubx.resource(BlockPublicAccessExclusion, "ci-runner", BlockPublicAccessExclusionConfig(
            internet_gateway_exclusion_mode="example",
            subnet_id="example",
            tags={"managed-by": "ubx"},
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_block_public_access_exclusion, with internet
    gateway exclusion mode example, subnet id example, tags a set
    value, vpc id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="internet_gateway_exclusion_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
