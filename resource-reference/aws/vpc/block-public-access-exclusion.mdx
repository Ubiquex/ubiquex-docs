---
title: "aws_vpc_block_public_access_exclusion"
description: "Real, generated bindings for `aws_vpc_block_public_access_exclusion`."
---

`aws_vpc_block_public_access_exclusion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.BlockPublicAccessExclusionConfig{
        InternetGatewayExclusionMode: "example",
        SubnetId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpc.BlockPublicAccessExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BlockPublicAccessExclusion } from
      "jsr:@ubx/sdk-aws/aws/vpc/block_public_access_exclusion";

    resource(BlockPublicAccessExclusion, "example", {
      internetGatewayExclusionMode: "example",
      subnetId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        BlockPublicAccessExclusion,
        BlockPublicAccessExclusionConfig,
    )

    cfg = BlockPublicAccessExclusionConfig(
        internet_gateway_exclusion_mode="example",
        subnet_id="example",
        tags={"key": "value"},
    )
    ubx.resource(BlockPublicAccessExclusion, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_block_public_access_exclusion with
internet_gateway_exclusion_mode, subnet_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="internet_gateway_exclusion_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
