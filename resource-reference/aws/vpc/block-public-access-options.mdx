---
title: "aws_vpc_block_public_access_options"
description: "Real, generated bindings for `aws_vpc_block_public_access_options`."
---

`aws_vpc_block_public_access_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own block public access options"})

    		ubx.Resource(vpc.BlockPublicAccessOptions, "ci-runner", vpc.BlockPublicAccessOptionsConfig{
    			InternetGatewayBlockMode: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BlockPublicAccessOptions } from "jsr:@ubx/sdk-aws/aws/vpc/block_public_access_options";

    export default stack("payments", () => {
      intent({ summary: "payments own block public access options" });

      resource(BlockPublicAccessOptions, "ci-runner", {
        internetGatewayBlockMode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import BlockPublicAccessOptions, BlockPublicAccessOptionsConfig

    def describe():
        ubx.intent("payments own block public access options")

        ubx.resource(BlockPublicAccessOptions, "ci-runner", BlockPublicAccessOptionsConfig(
            internet_gateway_block_mode="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_block_public_access_options, with internet gateway
    block mode example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="internet_gateway_block_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="aws_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
