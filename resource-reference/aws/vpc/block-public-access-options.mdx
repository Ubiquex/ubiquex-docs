---
title: "aws_vpc_block_public_access_options"
description: "Real, generated bindings for `aws_vpc_block_public_access_options`."
---

`aws_vpc_block_public_access_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.BlockPublicAccessOptionsConfig{
        InternetGatewayBlockMode: "example",
    }
    ubx.Resource(vpc.BlockPublicAccessOptions, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BlockPublicAccessOptions } from
      "jsr:@ubx/sdk-aws/aws/vpc/block_public_access_options";

    resource(BlockPublicAccessOptions, "example", {
      internetGatewayBlockMode: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        BlockPublicAccessOptions,
        BlockPublicAccessOptionsConfig,
    )

    cfg = BlockPublicAccessOptionsConfig(
        internet_gateway_block_mode="example",
    )
    ubx.resource(BlockPublicAccessOptions, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_block_public_access_options with
internet_gateway_block_mode.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="internet_gateway_block_mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="aws_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="aws_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
