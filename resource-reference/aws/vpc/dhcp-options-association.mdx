---
title: "aws_vpc_dhcp_options_association"
description: "Real, generated bindings for `aws_vpc_dhcp_options_association`."
---

`aws_vpc_dhcp_options_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dhcp options association"})

    		ubx.Resource(vpc.DhcpOptionsAssociation, "ci-runner", vpc.DhcpOptionsAssociationConfig{
    			DhcpOptionsId: "example",
    			VpcId:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DhcpOptionsAssociation } from "jsr:@ubx/sdk-aws/aws/vpc/dhcp_options_association";

    export default stack("payments", () => {
      intent({ summary: "payments own dhcp options association" });

      resource(DhcpOptionsAssociation, "ci-runner", {
        dhcpOptionsId: "example",
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import DhcpOptionsAssociation, DhcpOptionsAssociationConfig

    def describe():
        ubx.intent("payments own dhcp options association")

        ubx.resource(DhcpOptionsAssociation, "ci-runner", DhcpOptionsAssociationConfig(
            dhcp_options_id="example",
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_dhcp_options_association, with dhcp options id
    example, vpc id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="dhcp_options_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
