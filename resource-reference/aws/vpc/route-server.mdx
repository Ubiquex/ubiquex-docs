---
title: "aws_vpc_route_server"
description: "Real, generated bindings for `aws_vpc_route_server`."
---

`aws_vpc_route_server` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own route server"})

    		ubx.Resource(vpc.RouteServer, "ci-runner", vpc.RouteServerConfig{
    			AmazonSideAsn:         1,
    			PersistRoutesDuration: 7200,
    			Tags:                  map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RouteServer } from "jsr:@ubx/sdk-aws/aws/vpc/route_server";

    export default stack("payments", () => {
      intent({ summary: "payments own route server" });

      resource(RouteServer, "ci-runner", {
        amazonSideAsn: 1,
        persistRoutesDuration: 7200,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import RouteServer, RouteServerConfig

    def describe():
        ubx.intent("payments own route server")

        ubx.resource(RouteServer, "ci-runner", RouteServerConfig(
            amazon_side_asn=1,
            persist_routes_duration=7200,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_route_server, with amazon side asn 1, persist
    routes duration 7200, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="amazon_side_asn" type="number" required>
  Required.
</ResponseField>

<ResponseField name="persist_routes" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="persist_routes_duration" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_notifications_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="persist_routes" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="sns_notifications_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
