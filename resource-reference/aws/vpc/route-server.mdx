---
title: "aws_vpc_route_server"
description: "Real, generated bindings for `aws_vpc_route_server`."
---

`aws_vpc_route_server` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.RouteServerConfig{
        AmazonSideAsn: 1,
        PersistRoutes: "example",
        PersistRoutesDuration: 1,
    }
    ubx.Resource(vpc.RouteServer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RouteServer } from
      "jsr:@ubx/sdk-aws/aws/vpc/route_server";

    resource(RouteServer, "example", {
      amazonSideAsn: 1,
      persistRoutes: "example",
      persistRoutesDuration: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import RouteServer, RouteServerConfig

    cfg = RouteServerConfig(
        amazon_side_asn=1,
        persist_routes="example",
        persist_routes_duration=1,
    )
    ubx.resource(RouteServer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_route_server with amazon_side_asn,
persist_routes, persist_routes_duration.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="amazon_side_asn" type="number" required>
  Required.
</ResponseField>

<ResponseField name="persist_routes" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="persist_routes_duration" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_notifications_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="persist_routes" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="sns_notifications_enabled" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sns_topic_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
