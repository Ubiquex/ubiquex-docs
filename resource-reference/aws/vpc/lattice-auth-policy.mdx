---
title: "aws_vpc_lattice_auth_policy"
description: "`aws_vpc_lattice_auth_policy` attaches a real IAM-based authorization policy to an `aws_vpc_lattice_service`..."
---

`aws_vpc_lattice_auth_policy` attaches a real IAM-based authorization policy to an `aws_vpc_lattice_service`, controlling which principals are allowed to call it, independent of network-level `aws_vpc_lattice_service_network_vpc_association` reachability.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lattice auth policy"})

    		ubx.Resource(vpc.LatticeAuthPolicy, "ci-runner", vpc.LatticeAuthPolicyConfig{
    			Policy:             "example",
    			ResourceIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LatticeAuthPolicy } from "./local-sdk/aws/sdk/typescript/aws/vpc/lattice_auth_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own lattice auth policy" });

      resource(LatticeAuthPolicy, "ci-runner", {
        policy: "example",
        resourceIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.vpc import LatticeAuthPolicy, LatticeAuthPolicyConfig

    def describe():
        ubx.intent("payments own lattice auth policy")

        ubx.resource(LatticeAuthPolicy, "ci-runner", LatticeAuthPolicyConfig(
            policy="example",
            resource_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_lattice_auth_policy, with policy example, resource
    identifier example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="policy" type="any" required>
  The JSON policy document that defines the authentication and authorization rules for the associated VPC Lattice service or service network, specifying allowed principals, actions, and conditions. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_identifier" type="string" required>
  The resource identifier specifies the ID or ARN of the VPC Lattice service or service network that this auth policy is associated with. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="state" type="string">
  Returns the current state of the VPC Lattice auth policy, indicating whether the policy is active or inactive. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
