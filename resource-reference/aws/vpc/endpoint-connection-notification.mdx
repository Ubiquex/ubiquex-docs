---
title: "aws_vpc_endpoint_connection_notification"
description: "Real, generated bindings for `aws_vpc_endpoint_connection_notification`."
---

`aws_vpc_endpoint_connection_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint connection notification"})

    		ubx.Resource(vpc.EndpointConnectionNotification, "ci-runner", vpc.EndpointConnectionNotificationConfig{
    			ConnectionEvents:          []string{"example"},
    			ConnectionNotificationArn: "arn:aws:iam::123456789012:policy/ci",
    			VpcEndpointId:             "example",
    			VpcEndpointServiceId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointConnectionNotification } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_connection_notification";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint connection notification" });

      resource(EndpointConnectionNotification, "ci-runner", {
        connectionEvents: ["example"],
        connectionNotificationArn: "arn:aws:iam::123456789012:policy/ci",
        vpcEndpointId: "example",
        vpcEndpointServiceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointConnectionNotification, EndpointConnectionNotificationConfig

    def describe():
        ubx.intent("payments own endpoint connection notification")

        ubx.resource(EndpointConnectionNotification, "ci-runner", EndpointConnectionNotificationConfig(
            connection_events=["example"],
            connection_notification_arn="arn:aws:iam::123456789012:policy/ci",
            vpc_endpoint_id="example",
            vpc_endpoint_service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_connection_notification, with connection
    events a set value, connection notification arn
    arn:aws:iam::123456789012:policy/ci, vpc endpoint id example, vpc
    endpoint service id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_events" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="connection_notification_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="vpc_endpoint_service_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="notification_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
