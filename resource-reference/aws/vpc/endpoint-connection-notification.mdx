---
title: "aws_vpc_endpoint_connection_notification"
description: "Real, generated bindings for `aws_vpc_endpoint_connection_notification`."
---

`aws_vpc_endpoint_connection_notification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.EndpointConnectionNotificationConfig{
        ConnectionEvents: []string{"example"},
        ConnectionNotificationArn: "example",
        VpcEndpointId: "example",
        VpcEndpointServiceId: "example",
    }
    ubx.Resource(vpc.EndpointConnectionNotification, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointConnectionNotification } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_connection_notification";

    resource(EndpointConnectionNotification, "example", {
      connectionEvents: ["example"],
      connectionNotificationArn: "example",
      vpcEndpointId: "example",
      vpcEndpointServiceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        EndpointConnectionNotification,
        EndpointConnectionNotificationConfig,
    )

    cfg = EndpointConnectionNotificationConfig(
        connection_events=["example"],
        connection_notification_arn="example",
        vpc_endpoint_id="example",
        vpc_endpoint_service_id="example",
    )
    ubx.resource(EndpointConnectionNotification, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_connection_notification with
connection_events, connection_notification_arn, vpc_endpoint_id,
vpc_endpoint_service_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_events" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="connection_notification_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="vpc_endpoint_service_id" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="notification_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
