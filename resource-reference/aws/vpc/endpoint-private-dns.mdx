---
title: "aws_vpc_endpoint_private_dns"
description: "Real, generated bindings for `aws_vpc_endpoint_private_dns`."
---

`aws_vpc_endpoint_private_dns` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.EndpointPrivateDnsConfig{
        PrivateDnsEnabled: true,
        VpcEndpointId: "example",
    }
    ubx.Resource(vpc.EndpointPrivateDns, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointPrivateDns } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_private_dns";

    resource(EndpointPrivateDns, "example", {
      privateDnsEnabled: true,
      vpcEndpointId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        EndpointPrivateDns,
        EndpointPrivateDnsConfig,
    )

    cfg = EndpointPrivateDnsConfig(
        private_dns_enabled=True,
        vpc_endpoint_id="example",
    )
    ubx.resource(EndpointPrivateDns, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_private_dns with private_dns_enabled,
vpc_endpoint_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="private_dns_enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
