---
title: "aws_vpc_security_group_vpc_association"
description: "Real, generated bindings for `aws_vpc_security_group_vpc_association`."
---

`aws_vpc_security_group_vpc_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security group vpc association"})

    		ubx.Resource(vpc.SecurityGroupVpcAssociation, "ci-runner", vpc.SecurityGroupVpcAssociationConfig{
    			SecurityGroupId: "example",
    			VpcId:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityGroupVpcAssociation } from "jsr:@ubx/sdk-aws/aws/vpc/security_group_vpc_association";

    export default stack("payments", () => {
      intent({ summary: "payments own security group vpc association" });

      resource(SecurityGroupVpcAssociation, "ci-runner", {
        securityGroupId: "example",
        vpcId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import SecurityGroupVpcAssociation, SecurityGroupVpcAssociationConfig

    def describe():
        ubx.intent("payments own security group vpc association")

        ubx.resource(SecurityGroupVpcAssociation, "ci-runner", SecurityGroupVpcAssociationConfig(
            security_group_id="example",
            vpc_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_security_group_vpc_association, with security
    group id example, vpc id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
