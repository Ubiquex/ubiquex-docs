---
title: "aws_vpc_route_server_peer"
description: "Real, generated bindings for `aws_vpc_route_server_peer`."
---

`aws_vpc_route_server_peer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.RouteServerPeerConfig{
        PeerAddress: "example",
        RouteServerEndpointId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpc.RouteServerPeer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RouteServerPeer } from
      "jsr:@ubx/sdk-aws/aws/vpc/route_server_peer";

    resource(RouteServerPeer, "example", {
      peerAddress: "example",
      routeServerEndpointId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import RouteServerPeer, RouteServerPeerConfig

    cfg = RouteServerPeerConfig(
        peer_address="example",
        route_server_endpoint_id="example",
        tags={"key": "value"},
    )
    ubx.resource(RouteServerPeer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_route_server_peer with peer_address,
route_server_endpoint_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="peer_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_endpoint_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_eni_address" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_eni_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="route_server_peer_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="subnet_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
