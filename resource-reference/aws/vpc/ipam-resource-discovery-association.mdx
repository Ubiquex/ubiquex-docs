---
title: "aws_vpc_ipam_resource_discovery_association"
description: "Real, generated bindings for `aws_vpc_ipam_resource_discovery_association`."
---

`aws_vpc_ipam_resource_discovery_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.IpamResourceDiscoveryAssociationConfig{
        IpamId: "example",
        IpamResourceDiscoveryId: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(
        vpc.IpamResourceDiscoveryAssociation,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IpamResourceDiscoveryAssociation } from
      "jsr:@ubx/sdk-aws/aws/vpc/ipam_resource_discovery_association";

    resource(IpamResourceDiscoveryAssociation, "example", {
      ipamId: "example",
      ipamResourceDiscoveryId: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        IpamResourceDiscoveryAssociation,
        IpamResourceDiscoveryAssociationConfig,
    )

    cfg = IpamResourceDiscoveryAssociationConfig(
        ipam_id="example",
        ipam_resource_discovery_id="example",
        tags={"key": "value"},
    )
    ubx.resource(IpamResourceDiscoveryAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam_resource_discovery_association with
ipam_id, ipam_resource_discovery_id, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ipam_resource_discovery_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
