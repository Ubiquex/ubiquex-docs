---
title: "aws_vpc_endpoint_service_private_dns_verification"
description: "Real, generated bindings for `aws_vpc_endpoint_service_private_dns_verification`."
---

`aws_vpc_endpoint_service_private_dns_verification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.EndpointServicePrivateDnsVerificationConfig{
        ServiceId: "example",
        WaitForVerification: true,
    }
    ubx.Resource(
        vpc.EndpointServicePrivateDnsVerification,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointServicePrivateDnsVerification } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_service_private_dns_verification";

    resource(EndpointServicePrivateDnsVerification, "example", {
      serviceId: "example",
      waitForVerification: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        EndpointServicePrivateDnsVerification,
        EndpointServicePrivateDnsVerificationConfig,
    )

    cfg = EndpointServicePrivateDnsVerificationConfig(
        service_id="example",
        wait_for_verification=True,
    )
    ubx.resource(
        EndpointServicePrivateDnsVerification,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_service_private_dns_verification with
service_id, wait_for_verification.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="wait_for_verification" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
