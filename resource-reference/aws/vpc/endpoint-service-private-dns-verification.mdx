---
title: "aws_vpc_endpoint_service_private_dns_verification"
description: "Real, generated bindings for `aws_vpc_endpoint_service_private_dns_verification`."
---

`aws_vpc_endpoint_service_private_dns_verification` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint service private dns verification"})

    		ubx.Resource(vpc.EndpointServicePrivateDnsVerification, "ci-runner", vpc.EndpointServicePrivateDnsVerificationConfig{
    			ServiceId:           "example",
    			WaitForVerification: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointServicePrivateDnsVerification } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_service_private_dns_verification";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint service private dns verification" });

      resource(EndpointServicePrivateDnsVerification, "ci-runner", {
        serviceId: "example",
        waitForVerification: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointServicePrivateDnsVerification, EndpointServicePrivateDnsVerificationConfig

    def describe():
        ubx.intent("payments own endpoint service private dns verification")

        ubx.resource(EndpointServicePrivateDnsVerification, "ci-runner", EndpointServicePrivateDnsVerificationConfig(
            service_id="example",
            wait_for_verification=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_service_private_dns_verification, with
    service id example, wait for verification true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="wait_for_verification" type="bool">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
