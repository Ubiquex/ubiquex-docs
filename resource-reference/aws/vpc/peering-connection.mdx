---
title: "aws_vpc_peering_connection"
description: "Real, generated bindings for `aws_vpc_peering_connection`."
---

`aws_vpc_peering_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own peering connection"})

    		ubx.Resource(vpc.PeeringConnection, "ci-runner", vpc.PeeringConnectionConfig{
    			PeerVpcId:  "example",
    			VpcId:      "example",
    			AutoAccept: true,
    			Tags:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PeeringConnection } from "jsr:@ubx/sdk-aws/aws/vpc/peering_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own peering connection" });

      resource(PeeringConnection, "ci-runner", {
        peerVpcId: "example",
        vpcId: "example",
        autoAccept: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import PeeringConnection, PeeringConnectionConfig

    def describe():
        ubx.intent("payments own peering connection")

        ubx.resource(PeeringConnection, "ci-runner", PeeringConnectionConfig(
            peer_vpc_id="example",
            vpc_id="example",
            auto_accept=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_peering_connection, with peer vpc id example, vpc
    id example, auto accept true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auto_accept" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_owner_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_vpc_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="accept_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_owner_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
