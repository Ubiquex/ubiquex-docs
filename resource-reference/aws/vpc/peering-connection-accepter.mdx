---
title: "aws_vpc_peering_connection_accepter"
description: "Real, generated bindings for `aws_vpc_peering_connection_accepter`."
---

`aws_vpc_peering_connection_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.PeeringConnectionAccepterConfig{
        VpcPeeringConnectionId: "example",
        AutoAccept: true,
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpc.PeeringConnectionAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PeeringConnectionAccepter } from
      "jsr:@ubx/sdk-aws/aws/vpc/peering_connection_accepter";

    resource(PeeringConnectionAccepter, "example", {
      vpcPeeringConnectionId: "example",
      autoAccept: true,
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        PeeringConnectionAccepter,
        PeeringConnectionAccepterConfig,
    )

    cfg = PeeringConnectionAccepterConfig(
        vpc_peering_connection_id="example",
        auto_accept=True,
        tags={"key": "value"},
    )
    ubx.resource(PeeringConnectionAccepter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_peering_connection_accepter with
vpc_peering_connection_id, auto_accept, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="auto_accept" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_peering_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="accept_status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="peer_owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="peer_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="peer_vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
