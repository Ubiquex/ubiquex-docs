---
title: "aws_vpc_endpoint_service_allowed_principal"
description: "Real, generated bindings for `aws_vpc_endpoint_service_allowed_principal`."
---

`aws_vpc_endpoint_service_allowed_principal` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.EndpointServiceAllowedPrincipalConfig{
        PrincipalArn: "example",
        VpcEndpointServiceId: "example",
    }
    ubx.Resource(vpc.EndpointServiceAllowedPrincipal, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointServiceAllowedPrincipal } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_service_allowed_principal";

    resource(EndpointServiceAllowedPrincipal, "example", {
      principalArn: "example",
      vpcEndpointServiceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        EndpointServiceAllowedPrincipal,
        EndpointServiceAllowedPrincipalConfig,
    )

    cfg = EndpointServiceAllowedPrincipalConfig(
        principal_arn="example",
        vpc_endpoint_service_id="example",
    )
    ubx.resource(EndpointServiceAllowedPrincipal, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_service_allowed_principal with
principal_arn, vpc_endpoint_service_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_service_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
