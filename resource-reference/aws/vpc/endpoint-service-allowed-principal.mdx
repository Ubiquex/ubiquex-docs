---
title: "aws_vpc_endpoint_service_allowed_principal"
description: "Real, generated bindings for `aws_vpc_endpoint_service_allowed_principal`."
---

`aws_vpc_endpoint_service_allowed_principal` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint service allowed principal"})

    		ubx.Resource(vpc.EndpointServiceAllowedPrincipal, "ci-runner", vpc.EndpointServiceAllowedPrincipalConfig{
    			PrincipalArn:         "arn:aws:iam::123456789012:policy/ci",
    			VpcEndpointServiceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointServiceAllowedPrincipal } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_service_allowed_principal";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint service allowed principal" });

      resource(EndpointServiceAllowedPrincipal, "ci-runner", {
        principalArn: "arn:aws:iam::123456789012:policy/ci",
        vpcEndpointServiceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointServiceAllowedPrincipal, EndpointServiceAllowedPrincipalConfig

    def describe():
        ubx.intent("payments own endpoint service allowed principal")

        ubx.resource(EndpointServiceAllowedPrincipal, "ci-runner", EndpointServiceAllowedPrincipalConfig(
            principal_arn="arn:aws:iam::123456789012:policy/ci",
            vpc_endpoint_service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_service_allowed_principal, with principal
    arn arn:aws:iam::123456789012:policy/ci, vpc endpoint service id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_service_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
