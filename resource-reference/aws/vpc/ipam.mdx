---
title: "aws_vpc_ipam"
description: "Real, generated bindings for `aws_vpc_ipam`."
---

`aws_vpc_ipam` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.IpamConfig{
        Cascade: true,
        Description: "example",
    }
    ubx.Resource(vpc.Ipam, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Ipam } from "jsr:@ubx/sdk-aws/aws/vpc/ipam";

    resource(Ipam, "example", {
      cascade: true,
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import Ipam, IpamConfig

    cfg = IpamConfig(
        cascade=True,
        description="example",
    )
    ubx.resource(Ipam, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam with cascade, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cascade" type="bool">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="enable_private_gua" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metered_account" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tier" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_resource_discovery_association_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_resource_discovery_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metered_account" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="private_default_scope_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="public_default_scope_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
