---
title: "aws_vpc_lattice_service_network_resource_association"
description: "`aws_vpc_lattice_service_network_resource_association` associates a real `aws_vpc_lattice_resource_configuration` with..."
---

`aws_vpc_lattice_service_network_resource_association` associates a real `aws_vpc_lattice_resource_configuration` with an `aws_vpc_lattice_service_network`, making that non-service resource reachable to everything else connected to the network.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lattice service network resource association"})

    		ubx.Resource(vpc.LatticeServiceNetworkResourceAssociation, "ci-runner", vpc.LatticeServiceNetworkResourceAssociationConfig{
    			PrivateDnsEnabled:       true,
    			ResourceConfigurationId: "example",
    			ServiceNetworkId:        "example",
    			Tags:                    []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { LatticeServiceNetworkResourceAssociation } from "./local-sdk/aws/sdk/typescript/aws/vpc/lattice_service_network_resource_association.ts";

    export default stack("payments", () => {
      intent({
        summary: "payments own lattice service network resource association",
      });

      resource(LatticeServiceNetworkResourceAssociation, "ci-runner", {
        privateDnsEnabled: true,
        resourceConfigurationId: "example",
        serviceNetworkId: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.vpc import LatticeServiceNetworkResourceAssociation, LatticeServiceNetworkResourceAssociationConfig

    def describe():
        ubx.intent("payments own lattice service network resource association")

        ubx.resource(LatticeServiceNetworkResourceAssociation, "ci-runner", LatticeServiceNetworkResourceAssociationConfig(
            private_dns_enabled=True,
            resource_configuration_id="example",
            service_network_id="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_lattice_service_network_resource_association, with
    private dns enabled true, resource configuration id example,
    service network id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="private_dns_enabled" type="bool">
  When enabled, this generates a private DNS hostname for the associated resource within the VPC Lattice service network, allowing it to be accessed via DNS resolution from associated VPCs. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="resource_configuration_id" type="string">
  The unique identifier of the resource configuration (such as a VPC or AWS service) to associate with the service network. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="service_network_id" type="string">
  The unique ID of the VPC Lattice service network that this resource association attaches the specified resource to. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Specifies a list of key-value tag objects to attach to the VPC Lattice service network resource association, which can be used for resource identification, cost allocation, and access control. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the VPC Lattice service network resource association, used to organize and identify the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag key-value pair attached to a VPC Lattice service network resource association, used to provide user-defined metadata for identifying or managing the association. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) uniquely identifying this service network resource association. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique AWS-assigned identifier for the VPC Lattice service network resource association. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
