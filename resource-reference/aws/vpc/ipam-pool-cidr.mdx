---
title: "aws_vpc_ipam_pool_cidr"
description: "Real, generated bindings for `aws_vpc_ipam_pool_cidr`."
---

`aws_vpc_ipam_pool_cidr` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.IpamPoolCidrConfig{
        IpamPoolId: "example",
        Cidr: "example",
        NetmaskLength: 1,
    }
    ubx.Resource(vpc.IpamPoolCidr, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IpamPoolCidr } from
      "jsr:@ubx/sdk-aws/aws/vpc/ipam_pool_cidr";

    resource(IpamPoolCidr, "example", {
      ipamPoolId: "example",
      cidr: "example",
      netmaskLength: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import IpamPoolCidr, IpamPoolCidrConfig

    cfg = IpamPoolCidrConfig(
        ipam_pool_id="example",
        cidr="example",
        netmask_length=1,
    )
    ubx.resource(IpamPoolCidr, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam_pool_cidr with ipam_pool_id, cidr,
netmask_length.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="cidr" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_pool_cidr_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
