---
title: "aws_vpc_network_performance_metric_subscription"
description: "Real, generated bindings for `aws_vpc_network_performance_metric_subscription`."
---

`aws_vpc_network_performance_metric_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.NetworkPerformanceMetricSubscriptionConfig{
        Destination: "example",
        Source: "example",
        Metric: "example",
        Statistic: "example",
    }
    ubx.Resource(
        vpc.NetworkPerformanceMetricSubscription,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { NetworkPerformanceMetricSubscription } from
      "jsr:@ubx/sdk-aws/aws/vpc/network_performance_metric_subscription";

    resource(NetworkPerformanceMetricSubscription, "example", {
      destination: "example",
      source: "example",
      metric: "example",
      statistic: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        NetworkPerformanceMetricSubscription,
        NetworkPerformanceMetricSubscriptionConfig,
    )

    cfg = NetworkPerformanceMetricSubscriptionConfig(
        destination="example",
        source="example",
        metric="example",
        statistic="example",
    )
    ubx.resource(
        NetworkPerformanceMetricSubscription,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_network_performance_metric_subscription with
destination, source, metric, statistic.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metric" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="statistic" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="period" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
