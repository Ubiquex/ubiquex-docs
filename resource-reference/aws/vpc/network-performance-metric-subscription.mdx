---
title: "aws_vpc_network_performance_metric_subscription"
description: "Real, generated bindings for `aws_vpc_network_performance_metric_subscription`."
---

`aws_vpc_network_performance_metric_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network performance metric subscription"})

    		ubx.Resource(vpc.NetworkPerformanceMetricSubscription, "ci-runner", vpc.NetworkPerformanceMetricSubscriptionConfig{
    			Destination: "example",
    			Source:      "example",
    			Metric:      "example",
    			Statistic:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkPerformanceMetricSubscription } from "jsr:@ubx/sdk-aws/aws/vpc/network_performance_metric_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own network performance metric subscription" });

      resource(NetworkPerformanceMetricSubscription, "ci-runner", {
        destination: "example",
        source: "example",
        metric: "example",
        statistic: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import NetworkPerformanceMetricSubscription, NetworkPerformanceMetricSubscriptionConfig

    def describe():
        ubx.intent("payments own network performance metric subscription")

        ubx.resource(NetworkPerformanceMetricSubscription, "ci-runner", NetworkPerformanceMetricSubscriptionConfig(
            destination="example",
            source="example",
            metric="example",
            statistic="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_network_performance_metric_subscription, with
    destination example, source example, metric example, statistic
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="metric" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

<ResponseField name="statistic" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="period" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
