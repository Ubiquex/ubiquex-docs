---
title: "aws_vpc_ipam_scope"
description: "Real, generated bindings for `aws_vpc_ipam_scope`."
---

`aws_vpc_ipam_scope` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.IpamScopeConfig{
        IpamId: "example",
        Description: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(vpc.IpamScope, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IpamScope } from "jsr:@ubx/sdk-aws/aws/vpc/ipam_scope";

    resource(IpamScope, "example", {
      ipamId: "example",
      description: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import IpamScope, IpamScopeConfig

    cfg = IpamScopeConfig(
        ipam_id="example",
        description="example",
        tags={"key": "value"},
    )
    ubx.resource(IpamScope, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam_scope with ipam_id, description, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ipam_scope_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_default" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="pool_count" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
