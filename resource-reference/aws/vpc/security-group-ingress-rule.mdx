---
title: "aws_vpc_security_group_ingress_rule"
description: "Real, generated bindings for `aws_vpc_security_group_ingress_rule`."
---

`aws_vpc_security_group_ingress_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security group ingress rule"})

    		ubx.Resource(vpc.SecurityGroupIngressRule, "ci-runner", vpc.SecurityGroupIngressRuleConfig{
    			IpProtocol:                "example",
    			SecurityGroupId:           "example",
    			CidrIpv4:                  "example",
    			CidrIpv6:                  "example",
    			Description:               "Managed by ubx.",
    			FromPort:                  1,
    			PrefixListId:              "example",
    			ReferencedSecurityGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityGroupIngressRule } from "jsr:@ubx/sdk-aws/aws/vpc/security_group_ingress_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own security group ingress rule" });

      resource(SecurityGroupIngressRule, "ci-runner", {
        ipProtocol: "example",
        securityGroupId: "example",
        cidrIpv4: "example",
        cidrIpv6: "example",
        description: "Managed by ubx.",
        fromPort: 1,
        prefixListId: "example",
        referencedSecurityGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import SecurityGroupIngressRule, SecurityGroupIngressRuleConfig

    def describe():
        ubx.intent("payments own security group ingress rule")

        ubx.resource(SecurityGroupIngressRule, "ci-runner", SecurityGroupIngressRuleConfig(
            ip_protocol="example",
            security_group_id="example",
            cidr_ipv4="example",
            cidr_ipv6="example",
            description="Managed by ubx.",
            from_port=1,
            prefix_list_id="example",
            referenced_security_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_security_group_ingress_rule, with ip protocol
    example, security group id example, cidr ipv4 example, cidr ipv6
    example, description Managed by ubx., from port 1, prefix list id
    example, referenced security group id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="cidr_ipv4" type="string">
  Optional.
</ResponseField>

<ResponseField name="cidr_ipv6" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="from_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="ip_protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="prefix_list_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="referenced_security_group_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="to_port" type="number">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_rule_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
