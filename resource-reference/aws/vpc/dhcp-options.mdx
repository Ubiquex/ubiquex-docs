---
title: "aws_vpc_dhcp_options"
description: "Real, generated bindings for `aws_vpc_dhcp_options`."
---

`aws_vpc_dhcp_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dhcp options"})

    		ubx.Resource(vpc.DhcpOptions, "ci-runner", vpc.DhcpOptionsConfig{
    			DomainName:                    "example-domain-name",
    			DomainNameServers:             []string{"example"},
    			Ipv6AddressPreferredLeaseTime: "example",
    			NetbiosNameServers:            []string{"example"},
    			NetbiosNodeType:               "example",
    			NtpServers:                    []string{"example"},
    			Tags:                          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DhcpOptions } from "jsr:@ubx/sdk-aws/aws/vpc/dhcp_options";

    export default stack("payments", () => {
      intent({ summary: "payments own dhcp options" });

      resource(DhcpOptions, "ci-runner", {
        domainName: "example-domain-name",
        domainNameServers: ["example"],
        ipv6AddressPreferredLeaseTime: "example",
        netbiosNameServers: ["example"],
        netbiosNodeType: "example",
        ntpServers: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import DhcpOptions, DhcpOptionsConfig

    def describe():
        ubx.intent("payments own dhcp options")

        ubx.resource(DhcpOptions, "ci-runner", DhcpOptionsConfig(
            domain_name="example-domain-name",
            domain_name_servers=["example"],
            ipv6_address_preferred_lease_time="example",
            netbios_name_servers=["example"],
            netbios_node_type="example",
            ntp_servers=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_dhcp_options, with domain name
    example-domain-name, domain name servers a set value, ipv6
    address preferred lease time example, netbios name servers a set
    value, netbios node type example, ntp servers a set value, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="domain_name_servers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipv6_address_preferred_lease_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="netbios_name_servers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="netbios_node_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="ntp_servers" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
