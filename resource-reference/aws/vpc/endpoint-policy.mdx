---
title: "aws_vpc_endpoint_policy"
description: "Real, generated bindings for `aws_vpc_endpoint_policy`."
---

`aws_vpc_endpoint_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own endpoint policy"})

    		ubx.Resource(vpc.EndpointPolicy, "ci-runner", vpc.EndpointPolicyConfig{
    			VpcEndpointId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EndpointPolicy } from "jsr:@ubx/sdk-aws/aws/vpc/endpoint_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own endpoint policy" });

      resource(EndpointPolicy, "ci-runner", {
        vpcEndpointId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import EndpointPolicy, EndpointPolicyConfig

    def describe():
        ubx.intent("payments own endpoint policy")

        ubx.resource(EndpointPolicy, "ci-runner", EndpointPolicyConfig(
            vpc_endpoint_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_endpoint_policy, with vpc endpoint id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
