---
title: "aws_vpc_endpoint_policy"
description: "Real, generated bindings for `aws_vpc_endpoint_policy`."
---

`aws_vpc_endpoint_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.EndpointPolicyConfig{
        VpcEndpointId: "example",
        Policy: "example",
    }
    ubx.Resource(vpc.EndpointPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointPolicy } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_policy";

    resource(EndpointPolicy, "example", {
      vpcEndpointId: "example",
      policy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import EndpointPolicy, EndpointPolicyConfig

    cfg = EndpointPolicyConfig(
        vpc_endpoint_id="example",
        policy="example",
    )
    ubx.resource(EndpointPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_policy with vpc_endpoint_id, policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
