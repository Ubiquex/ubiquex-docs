---
title: "aws_vpc_route_server_propagation"
description: "Real, generated bindings for `aws_vpc_route_server_propagation`."
---

`aws_vpc_route_server_propagation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"

    cfg := vpc.RouteServerPropagationConfig{
        RouteServerId: "example",
        RouteTableId: "example",
    }
    ubx.Resource(vpc.RouteServerPropagation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RouteServerPropagation } from
      "jsr:@ubx/sdk-aws/aws/vpc/route_server_propagation";

    resource(RouteServerPropagation, "example", {
      routeServerId: "example",
      routeTableId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        RouteServerPropagation,
        RouteServerPropagationConfig,
    )

    cfg = RouteServerPropagationConfig(
        route_server_id="example",
        route_table_id="example",
    )
    ubx.resource(RouteServerPropagation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_route_server_propagation with route_server_id,
route_table_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route_server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="route_table_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
