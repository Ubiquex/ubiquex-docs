---
title: "aws_vpc"
description: "An isolated virtual network."
---

`aws_vpc` creates a real, isolated virtual network, the real
foundation every other real networked resource, subnets, security
groups, route tables, gets placed inside.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc"})

    		ubx.Resource(vpc.Vpc, "ci-runner", vpc.VpcConfig{
    			AssignGeneratedIpv6CidrBlock: true,
    			EnableDnsSupport:             true,
    			InstanceTenancy:              "example",
    			Ipv4IpamPoolId:               "example",
    			Ipv4NetmaskLength:            1,
    			Ipv6IpamPoolId:               "example",
    			Ipv6NetmaskLength:            1,
    			Tags:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Vpc } from "jsr:@ubx/sdk-aws/aws/vpc/vpc";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc" });

      resource(Vpc, "ci-runner", {
        assignGeneratedIpv6CidrBlock: true,
        enableDnsSupport: true,
        instanceTenancy: "example",
        ipv4IpamPoolId: "example",
        ipv4NetmaskLength: 1,
        ipv6IpamPoolId: "example",
        ipv6NetmaskLength: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import Vpc, VpcConfig

    def describe():
        ubx.intent("payments own vpc")

        ubx.resource(Vpc, "ci-runner", VpcConfig(
            assign_generated_ipv6_cidr_block=True,
            enable_dns_support=True,
            instance_tenancy="example",
            ipv4_ipam_pool_id="example",
            ipv4_netmask_length=1,
            ipv6_ipam_pool_id="example",
            ipv6_netmask_length=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc, with assign generated ipv6 cidr block true,
    enable dns support true, instance tenancy example, ipv4 ipam pool
    id example, ipv4 netmask length 1, ipv6 ipam pool id example,
    ipv6 netmask length 1, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr_block" type="string" required>
  The real IP address range for this VPC, in CIDR notation.
</ResponseField>

<ResponseField name="enable_dns_support" type="bool" default="true">
  Whether real DNS resolution works inside this VPC.
</ResponseField>

<ResponseField name="enable_dns_hostnames" type="bool" default="false">
  Whether instances get real, public DNS hostnames.
</ResponseField>

<ResponseField name="instance_tenancy" type="string" default="default">
  `default` (shared hardware) or `dedicated`.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Real, arbitrary tags.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The VPC's own real identifier.
</ResponseField>

<ResponseField name="arn" type="string">
  The VPC's real ARN, computed, known only after ship.
</ResponseField>

<ResponseField name="default_route_table_id" type="string">
  The real, automatically-created default route table.
</ResponseField>

<ResponseField name="default_security_group_id" type="string">
  The real, automatically-created default security group.
</ResponseField>

<ResponseField name="default_network_acl_id" type="string">
  The real, automatically-created default network ACL.
</ResponseField>

<ResponseField name="main_route_table_id" type="string">
  The real ID of the main route table.
</ResponseField>

## See also

- [aws_subnet](/resource-reference/aws/vpc/subnet)
