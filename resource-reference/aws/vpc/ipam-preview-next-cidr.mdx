---
title: "aws_vpc_ipam_preview_next_cidr"
description: "Real, generated bindings for `aws_vpc_ipam_preview_next_cidr`."
---

`aws_vpc_ipam_preview_next_cidr` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ipam preview next cidr"})

    		ubx.Resource(vpc.IpamPreviewNextCidr, "ci-runner", vpc.IpamPreviewNextCidrConfig{
    			IpamPoolId:      "example",
    			DisallowedCidrs: []string{"example"},
    			NetmaskLength:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpamPreviewNextCidr } from "jsr:@ubx/sdk-aws/aws/vpc/ipam_preview_next_cidr";

    export default stack("payments", () => {
      intent({ summary: "payments own ipam preview next cidr" });

      resource(IpamPreviewNextCidr, "ci-runner", {
        ipamPoolId: "example",
        disallowedCidrs: ["example"],
        netmaskLength: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import IpamPreviewNextCidr, IpamPreviewNextCidrConfig

    def describe():
        ubx.intent("payments own ipam preview next cidr")

        ubx.resource(IpamPreviewNextCidr, "ci-runner", IpamPreviewNextCidrConfig(
            ipam_pool_id="example",
            disallowed_cidrs=["example"],
            netmask_length=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_ipam_preview_next_cidr, with ipam pool id example,
    disallowed cidrs a set value, netmask length 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disallowed_cidrs" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="cidr" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
