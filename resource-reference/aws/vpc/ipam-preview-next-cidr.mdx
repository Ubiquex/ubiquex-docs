---
title: "aws_vpc_ipam_preview_next_cidr"
description: "Real, generated bindings for `aws_vpc_ipam_preview_next_cidr`."
---

`aws_vpc_ipam_preview_next_cidr` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.IpamPreviewNextCidrConfig{
        IpamPoolId: "example",
        DisallowedCidrs: []string{"example"},
        NetmaskLength: 1,
    }
    ubx.Resource(vpc.IpamPreviewNextCidr, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IpamPreviewNextCidr } from
      "jsr:@ubx/sdk-aws/aws/vpc/ipam_preview_next_cidr";

    resource(IpamPreviewNextCidr, "example", {
      ipamPoolId: "example",
      disallowedCidrs: ["example"],
      netmaskLength: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        IpamPreviewNextCidr,
        IpamPreviewNextCidrConfig,
    )

    cfg = IpamPreviewNextCidrConfig(
        ipam_pool_id="example",
        disallowed_cidrs=["example"],
        netmask_length=1,
    )
    ubx.resource(IpamPreviewNextCidr, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_ipam_preview_next_cidr with ipam_pool_id,
disallowed_cidrs, netmask_length.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="disallowed_cidrs" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="cidr" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
