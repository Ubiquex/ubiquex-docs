---
title: "aws_vpc_endpoint_subnet_association"
description: "Real, generated bindings for `aws_vpc_endpoint_subnet_association`."
---

`aws_vpc_endpoint_subnet_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/vpc"

    cfg := vpc.EndpointSubnetAssociationConfig{
        SubnetId: "example",
        VpcEndpointId: "example",
    }
    ubx.Resource(vpc.EndpointSubnetAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EndpointSubnetAssociation } from
      "jsr:@ubx/sdk-aws/aws/vpc/endpoint_subnet_association";

    resource(EndpointSubnetAssociation, "example", {
      subnetId: "example",
      vpcEndpointId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.vpc import (
        EndpointSubnetAssociation,
        EndpointSubnetAssociationConfig,
    )

    cfg = EndpointSubnetAssociationConfig(
        subnet_id="example",
        vpc_endpoint_id="example",
    )
    ubx.resource(EndpointSubnetAssociation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_vpc_endpoint_subnet_association with subnet_id,
vpc_endpoint_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subnet_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vpc_endpoint_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
