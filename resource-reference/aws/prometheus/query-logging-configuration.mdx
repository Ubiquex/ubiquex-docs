---
title: "aws_prometheus_query_logging_configuration"
description: "Real, generated bindings for `aws_prometheus_query_logging_configuration`."
---

`aws_prometheus_query_logging_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/prometheus"

    cfg := prometheus.QueryLoggingConfigurationConfig{
        WorkspaceId: "example",
    }
    ubx.Resource(
        prometheus.QueryLoggingConfiguration,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { QueryLoggingConfiguration } from
      "jsr:@ubx/sdk-aws/aws/prometheus/query_logging_configuration";

    resource(QueryLoggingConfiguration, "example", {
      workspaceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.prometheus import (
        QueryLoggingConfiguration,
        QueryLoggingConfigurationConfig,
    )

    cfg = QueryLoggingConfigurationConfig(
        workspace_id="example",
    )
    ubx.resource(QueryLoggingConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_prometheus_query_logging_configuration with
workspace_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
