---
title: "aws_prometheus_resource_policy"
description: "Real, generated bindings for `aws_prometheus_resource_policy`."
---

`aws_prometheus_resource_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/prometheus"

    cfg := prometheus.ResourcePolicyConfig{
        PolicyDocument: "example",
        WorkspaceId: "example",
        RevisionId: "example",
    }
    ubx.Resource(prometheus.ResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourcePolicy } from
      "jsr:@ubx/sdk-aws/aws/prometheus/resource_policy";

    resource(ResourcePolicy, "example", {
      policyDocument: "example",
      workspaceId: "example",
      revisionId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.prometheus import (
        ResourcePolicy,
        ResourcePolicyConfig,
    )

    cfg = ResourcePolicyConfig(
        policy_document="example",
        workspace_id="example",
        revision_id="example",
    )
    ubx.resource(ResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_prometheus_resource_policy with policy_document,
workspace_id, revision_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
