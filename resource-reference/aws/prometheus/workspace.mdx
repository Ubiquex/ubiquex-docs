---
title: "aws_prometheus_workspace"
description: "Real, generated bindings for `aws_prometheus_workspace`."
---

`aws_prometheus_workspace` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/prometheus"

    cfg := prometheus.WorkspaceConfig{
        Alias: "example",
        KmsKeyArn: "example",
    }
    ubx.Resource(prometheus.Workspace, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Workspace } from
      "jsr:@ubx/sdk-aws/aws/prometheus/workspace";

    resource(Workspace, "example", {
      alias: "example",
      kmsKeyArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.prometheus import Workspace, WorkspaceConfig

    cfg = WorkspaceConfig(
        alias="example",
        kms_key_arn="example",
    )
    ubx.resource(Workspace, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_prometheus_workspace with alias, kms_key_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="alias" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prometheus_endpoint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
