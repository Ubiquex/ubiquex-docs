---
title: "aws_prometheus_alert_manager_definition"
description: "Real, generated bindings for `aws_prometheus_alert_manager_definition`."
---

`aws_prometheus_alert_manager_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/prometheus"

    cfg := prometheus.AlertManagerDefinitionConfig{
        Definition: "example",
        WorkspaceId: "example",
    }
    ubx.Resource(prometheus.AlertManagerDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AlertManagerDefinition } from
      "jsr:@ubx/sdk-aws/aws/prometheus/alert_manager_definition";

    resource(AlertManagerDefinition, "example", {
      definition: "example",
      workspaceId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.prometheus import (
        AlertManagerDefinition,
        AlertManagerDefinitionConfig,
    )

    cfg = AlertManagerDefinitionConfig(
        definition="example",
        workspace_id="example",
    )
    ubx.resource(AlertManagerDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_prometheus_alert_manager_definition with
definition, workspace_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="definition" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
