---
title: "aws_prometheus_alert_manager_definition"
description: "Real, generated bindings for `aws_prometheus_alert_manager_definition`."
---

`aws_prometheus_alert_manager_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	prometheus "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/prometheus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own alert manager definition"})

    		ubx.Resource(prometheus.AlertManagerDefinition, "ci-runner", prometheus.AlertManagerDefinitionConfig{
    			Definition:  "example",
    			WorkspaceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AlertManagerDefinition } from "jsr:@ubx/sdk-aws/aws/prometheus/alert_manager_definition";

    export default stack("payments", () => {
      intent({ summary: "payments own alert manager definition" });

      resource(AlertManagerDefinition, "ci-runner", {
        definition: "example",
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.prometheus import AlertManagerDefinition, AlertManagerDefinitionConfig

    def describe():
        ubx.intent("payments own alert manager definition")

        ubx.resource(AlertManagerDefinition, "ci-runner", AlertManagerDefinitionConfig(
            definition="example",
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_prometheus_alert_manager_definition, with definition
    example, workspace id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="definition" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
