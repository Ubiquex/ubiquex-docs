---
title: "aws_prometheus_rule_group_namespace"
description: "Real, generated bindings for `aws_prometheus_rule_group_namespace`."
---

`aws_prometheus_rule_group_namespace` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	prometheus "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/prometheus"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rule group namespace"})

    		ubx.Resource(prometheus.RuleGroupNamespace, "ci-runner", prometheus.RuleGroupNamespaceConfig{
    			Data:        "example",
    			Name:        "ci-runner",
    			WorkspaceId: "example",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleGroupNamespace } from "jsr:@ubx/sdk-aws/aws/prometheus/rule_group_namespace";

    export default stack("payments", () => {
      intent({ summary: "payments own rule group namespace" });

      resource(RuleGroupNamespace, "ci-runner", {
        data: "example",
        name: "ci-runner",
        workspaceId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.prometheus import RuleGroupNamespace, RuleGroupNamespaceConfig

    def describe():
        ubx.intent("payments own rule group namespace")

        ubx.resource(RuleGroupNamespace, "ci-runner", RuleGroupNamespaceConfig(
            data="example",
            name="ci-runner",
            workspace_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_prometheus_rule_group_namespace called "ci-runner",
    with data example, workspace id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
