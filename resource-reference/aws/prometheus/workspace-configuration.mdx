---
title: "aws_prometheus_workspace_configuration"
description: "Real, generated bindings for `aws_prometheus_workspace_configuration`."
---

`aws_prometheus_workspace_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/prometheus"

    cfg := prometheus.WorkspaceConfigurationConfig{
        WorkspaceId: "example",
        OutOfOrderTimeWindowInSeconds: 1,
        RetentionPeriodInDays: 1,
    }
    ubx.Resource(prometheus.WorkspaceConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { WorkspaceConfiguration } from
      "jsr:@ubx/sdk-aws/aws/prometheus/workspace_configuration";

    resource(WorkspaceConfiguration, "example", {
      workspaceId: "example",
      outOfOrderTimeWindowInSeconds: 1,
      retentionPeriodInDays: 1,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.prometheus import (
        WorkspaceConfiguration,
        WorkspaceConfigurationConfig,
    )

    cfg = WorkspaceConfigurationConfig(
        workspace_id="example",
        out_of_order_time_window_in_seconds=1,
        retention_period_in_days=1,
    )
    ubx.resource(WorkspaceConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_prometheus_workspace_configuration with
workspace_id, out_of_order_time_window_in_seconds,
retention_period_in_days.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="out_of_order_time_window_in_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period_in_days" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_query_offset_in_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="out_of_order_time_window_in_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period_in_days" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_query_offset_in_seconds" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>
