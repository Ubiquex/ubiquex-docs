---
title: "aws_prometheus_scraper"
description: "Real, generated bindings for `aws_prometheus_scraper`."
---

`aws_prometheus_scraper` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/prometheus"

    cfg := prometheus.ScraperConfig{
        ScrapeConfiguration: "example",
        Alias: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(prometheus.Scraper, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Scraper } from
      "jsr:@ubx/sdk-aws/aws/prometheus/scraper";

    resource(Scraper, "example", {
      scrapeConfiguration: "example",
      alias: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.prometheus import Scraper, ScraperConfig

    cfg = ScraperConfig(
        scrape_configuration="example",
        alias="example",
        tags={"key": "value"},
    )
    ubx.resource(Scraper, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_prometheus_scraper with scrape_configuration,
alias, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="alias" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scrape_configuration" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
