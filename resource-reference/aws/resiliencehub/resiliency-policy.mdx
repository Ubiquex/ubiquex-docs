---
title: "aws_resiliencehub_resiliency_policy"
description: "Real, generated bindings for `aws_resiliencehub_resiliency_policy`."
---

`aws_resiliencehub_resiliency_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/resiliencehub"

    cfg := resiliencehub.ResiliencyPolicyConfig{
        Name: "example-name",
        Tier: "example",
        DataLocationConstraint: "example",
        Description: "example",
    }
    ubx.Resource(resiliencehub.ResiliencyPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResiliencyPolicy } from
      "jsr:@ubx/sdk-aws/aws/resiliencehub/resiliency_policy";

    resource(ResiliencyPolicy, "example", {
      name: "example",
      tier: "example",
      dataLocationConstraint: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.resiliencehub import (
        ResiliencyPolicy,
        ResiliencyPolicyConfig,
    )

    cfg = ResiliencyPolicyConfig(
        name="example",
        tier="example",
        data_location_constraint="example",
        description="example",
    )
    ubx.resource(ResiliencyPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_resiliencehub_resiliency_policy with name, tier,
data_location_constraint, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="data_location_constraint" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_location_constraint" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="estimated_cost_tier" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
