---
title: "aws_networkfirewall_firewall_transit_gateway_attachment_accepter"
description: "Real, generated bindings for `aws_networkfirewall_firewall_transit_gateway_attachment_accepter`."
---

`aws_networkfirewall_firewall_transit_gateway_attachment_accepter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkfirewall "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkfirewall"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own firewall transit gateway attachment accepter"})

    		ubx.Resource(networkfirewall.FirewallTransitGatewayAttachmentAccepter, "ci-runner", networkfirewall.FirewallTransitGatewayAttachmentAccepterConfig{
    			TransitGatewayAttachmentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FirewallTransitGatewayAttachmentAccepter } from "jsr:@ubx/sdk-aws/aws/networkfirewall/firewall_transit_gateway_attachment_accepter";

    export default stack("payments", () => {
      intent({
        summary: "payments own firewall transit gateway attachment accepter",
      });

      resource(FirewallTransitGatewayAttachmentAccepter, "ci-runner", {
        transitGatewayAttachmentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkfirewall import FirewallTransitGatewayAttachmentAccepter, FirewallTransitGatewayAttachmentAccepterConfig

    def describe():
        ubx.intent("payments own firewall transit gateway attachment accepter")

        ubx.resource(FirewallTransitGatewayAttachmentAccepter, "ci-runner", FirewallTransitGatewayAttachmentAccepterConfig(
            transit_gateway_attachment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    aws_networkfirewall_firewall_transit_gateway_attachment_accepter,
    with transit gateway attachment id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
