---
title: "aws_iot_indexing_configuration"
description: "Real, generated bindings for `aws_iot_indexing_configuration`."
---

`aws_iot_indexing_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iot"

    cfg := iot.IndexingConfigurationConfig{
    }
    ubx.Resource(iot.IndexingConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { IndexingConfiguration } from
      "jsr:@ubx/sdk-aws/aws/iot/indexing_configuration";

    resource(IndexingConfiguration, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iot import (
        IndexingConfiguration,
        IndexingConfigurationConfig,
    )

    cfg = IndexingConfigurationConfig(
    )
    ubx.resource(IndexingConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iot_indexing_configuration.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
