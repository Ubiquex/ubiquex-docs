---
title: "aws_iot_thing_principal_attachment"
description: "Real, generated bindings for `aws_iot_thing_principal_attachment`."
---

`aws_iot_thing_principal_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own thing principal attachment"})

    		ubx.Resource(iot.ThingPrincipalAttachment, "ci-runner", iot.ThingPrincipalAttachmentConfig{
    			Principal: "example",
    			Thing:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ThingPrincipalAttachment } from "jsr:@ubx/sdk-aws/aws/iot/thing_principal_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own thing principal attachment" });

      resource(ThingPrincipalAttachment, "ci-runner", {
        principal: "example",
        thing: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import ThingPrincipalAttachment, ThingPrincipalAttachmentConfig

    def describe():
        ubx.intent("payments own thing principal attachment")

        ubx.resource(ThingPrincipalAttachment, "ci-runner", ThingPrincipalAttachmentConfig(
            principal="example",
            thing="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_thing_principal_attachment, with principal
    example, thing example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thing" type="string" required>
  Required.
</ResponseField>

<ResponseField name="thing_principal_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thing_principal_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
