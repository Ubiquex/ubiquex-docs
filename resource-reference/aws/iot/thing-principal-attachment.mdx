---
title: "aws_iot_thing_principal_attachment"
description: "Real, generated bindings for `aws_iot_thing_principal_attachment`."
---

`aws_iot_thing_principal_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iot"

    cfg := iot.ThingPrincipalAttachmentConfig{
        Principal: "example",
        Thing: "example",
        ThingPrincipalType: "example",
    }
    ubx.Resource(iot.ThingPrincipalAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ThingPrincipalAttachment } from
      "jsr:@ubx/sdk-aws/aws/iot/thing_principal_attachment";

    resource(ThingPrincipalAttachment, "example", {
      principal: "example",
      thing: "example",
      thingPrincipalType: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iot import (
        ThingPrincipalAttachment,
        ThingPrincipalAttachmentConfig,
    )

    cfg = ThingPrincipalAttachmentConfig(
        principal="example",
        thing="example",
        thing_principal_type="example",
    )
    ubx.resource(ThingPrincipalAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iot_thing_principal_attachment with principal,
thing, thing_principal_type.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thing" type="string" required>
  Required.
</ResponseField>

<ResponseField name="thing_principal_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thing_principal_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
