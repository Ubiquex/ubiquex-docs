---
title: "aws_iot_role_alias"
description: "Real, generated bindings for `aws_iot_role_alias`."
---

`aws_iot_role_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own role alias"})

    		ubx.Resource(iot.RoleAlias, "ci-runner", iot.RoleAliasConfig{
    			Alias:              "example",
    			RoleArn:            "arn:aws:iam::123456789012:policy/ci",
    			CredentialDuration: 7200,
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoleAlias } from "jsr:@ubx/sdk-aws/aws/iot/role_alias";

    export default stack("payments", () => {
      intent({ summary: "payments own role alias" });

      resource(RoleAlias, "ci-runner", {
        alias: "example",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        credentialDuration: 7200,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import RoleAlias, RoleAliasConfig

    def describe():
        ubx.intent("payments own role alias")

        ubx.resource(RoleAlias, "ci-runner", RoleAliasConfig(
            alias="example",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            credential_duration=7200,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_role_alias, with alias example, role arn
    arn:aws:iam::123456789012:policy/ci, credential duration 7200,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="credential_duration" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
