---
title: "aws_iot_thing"
description: "Real, generated bindings for `aws_iot_thing`."
---

`aws_iot_thing` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own thing"})

    		ubx.Resource(iot.Thing, "ci-runner", iot.ThingConfig{
    			Name:          "ci-runner",
    			Attributes:    map[string]string{"managed-by": "ubx"},
    			ThingTypeName: "example-thing-type-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Thing } from "jsr:@ubx/sdk-aws/aws/iot/thing";

    export default stack("payments", () => {
      intent({ summary: "payments own thing" });

      resource(Thing, "ci-runner", {
        name: "ci-runner",
        attributes: { "managed-by": "ubx" },
        thingTypeName: "example-thing-type-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import Thing, ThingConfig

    def describe():
        ubx.intent("payments own thing")

        ubx.resource(Thing, "ci-runner", ThingConfig(
            name="ci-runner",
            attributes={"managed-by": "ubx"},
            thing_type_name="example-thing-type-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_thing called "ci-runner", with attributes a set
    value, thing type name example-thing-type-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="attributes" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thing_type_name" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="default_client_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
