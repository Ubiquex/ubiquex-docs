---
title: "aws_iot_certificate"
description: "Real, generated bindings for `aws_iot_certificate`."
---

`aws_iot_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own certificate"})

    		ubx.Resource(iot.Certificate, "ci-runner", iot.CertificateConfig{
    			Active: true,
    			CaPem:  "example",
    			Csr:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Certificate } from "jsr:@ubx/sdk-aws/aws/iot/certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own certificate" });

      resource(Certificate, "ci-runner", {
        active: true,
        caPem: "example",
        csr: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import Certificate, CertificateConfig

    def describe():
        ubx.intent("payments own certificate")

        ubx.resource(Certificate, "ci-runner", CertificateConfig(
            active=True,
            ca_pem="example",
            csr="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_certificate, with active true, ca pem example, csr
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="active" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="ca_pem" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_pem" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="csr" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ca_certificate_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_pem" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="private_key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="public_key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
