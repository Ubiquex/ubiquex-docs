---
title: "aws_iot_certificate"
description: "Real, generated bindings for `aws_iot_certificate`."
---

`aws_iot_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"

    cfg := iot.CertificateConfig{
        Active: true,
        CaPem: "example",
        CertificatePem: "example",
    }
    ubx.Resource(iot.Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Certificate } from
      "jsr:@ubx/sdk-aws/aws/iot/certificate";

    resource(Certificate, "example", {
      active: true,
      caPem: "example",
      certificatePem: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iot import Certificate, CertificateConfig

    cfg = CertificateConfig(
        active=True,
        ca_pem="example",
        certificate_pem="example",
    )
    ubx.resource(Certificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iot_certificate with active, ca_pem,
certificate_pem.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="active" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="ca_pem" type="string">
  Optional.
</ResponseField>

<ResponseField name="certificate_pem" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="csr" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ca_certificate_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="certificate_pem" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="private_key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="public_key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
