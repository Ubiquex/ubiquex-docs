---
title: "aws_iot_logging_options"
description: "Real, generated bindings for `aws_iot_logging_options`."
---

`aws_iot_logging_options` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own logging options"})

    		ubx.Resource(iot.LoggingOptions, "ci-runner", iot.LoggingOptionsConfig{
    			DefaultLogLevel: "example",
    			RoleArn:         "arn:aws:iam::123456789012:policy/ci",
    			DisableAllLogs:  true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoggingOptions } from "jsr:@ubx/sdk-aws/aws/iot/logging_options";

    export default stack("payments", () => {
      intent({ summary: "payments own logging options" });

      resource(LoggingOptions, "ci-runner", {
        defaultLogLevel: "example",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        disableAllLogs: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import LoggingOptions, LoggingOptionsConfig

    def describe():
        ubx.intent("payments own logging options")

        ubx.resource(LoggingOptions, "ci-runner", LoggingOptionsConfig(
            default_log_level="example",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            disable_all_logs=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_logging_options, with default log level example,
    role arn arn:aws:iam::123456789012:policy/ci, disable all logs
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_log_level" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disable_all_logs" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
