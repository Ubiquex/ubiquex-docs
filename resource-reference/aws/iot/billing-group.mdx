---
title: "aws_io_t_billing_group"
description: "`aws_io_t_billing_group` organizes real `aws_io_t_thing`s for cost allocation and tracking purposes..."
---

`aws_io_t_billing_group` organizes real `aws_io_t_thing`s for cost allocation and tracking purposes, kept separate from an `aws_io_t_thing_group`'s own operational grouping.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own t billing group"})

    		ubx.Resource(iot.BillingGroup, "example", iot.BillingGroupConfig{
    			BillingGroupName:       "example-billing-group-name",
    			BillingGroupProperties: map[string]any{"billing_group_description": "example"},
    			Tags:                   []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BillingGroup } from "@ubx/sdk-aws/aws/iot/billing_group";

    export default stack("example", () => {
      intent({ summary: "example own t billing group" });

      resource(BillingGroup, "example", {
        billingGroupName: "example-billing-group-name",
        billingGroupProperties: { billingGroupDescription: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot.billing_group import BillingGroup, BillingGroupConfig

    def describe():
        ubx.intent("example own t billing group")

        ubx.resource(BillingGroup, "example", BillingGroupConfig(
            billing_group_name="example-billing-group-name",
            billing_group_properties={"billing_group_description": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_billing_group, with billing group name
    example-billing-group-name, billing group properties a set value,
    tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="billing_group_name" type="string">
  The name that uniquely identifies the IoT billing group, used to group devices for consolidated billing and usage tracking. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="billing_group_properties" type="object">
  Contains properties of the billing group, such as a description field (BillingGroupDescription) for identifying the group's purpose. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="billing_group_description" type="string">
      A user-defined, free-form description of the AWS IoT billing group, used to provide additional context about the group's purpose. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      The string value of a tag assigned to the AWS IoT Billing Group, used to store arbitrary metadata about the billing group. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the IoT billing group, uniquely identifying it across AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique name of the AWS IoT billing group, which serves as the resource's identifier in AWS IoT. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
