---
title: "aws_iot_authorizer"
description: "Real, generated bindings for `aws_iot_authorizer`."
---

`aws_iot_authorizer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"

    cfg := iot.AuthorizerConfig{
        AuthorizerFunctionArn: "example",
        Name: "example-name",
        EnableCachingForHttp: true,
        SigningDisabled: true,
    }
    ubx.Resource(iot.Authorizer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Authorizer } from "jsr:@ubx/sdk-aws/aws/iot/authorizer";

    resource(Authorizer, "example", {
      authorizerFunctionArn: "example",
      name: "example",
      enableCachingForHttp: true,
      signingDisabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iot import Authorizer, AuthorizerConfig

    cfg = AuthorizerConfig(
        authorizer_function_arn="example",
        name="example",
        enable_caching_for_http=True,
        signing_disabled=True,
    )
    ubx.resource(Authorizer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iot_authorizer with authorizer_function_arn, name,
enable_caching_for_http, signing_disabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorizer_function_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_caching_for_http" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="signing_disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="token_key_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="token_signing_public_keys" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
