---
title: "aws_iot_authorizer"
description: "Real, generated bindings for `aws_iot_authorizer`."
---

`aws_iot_authorizer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorizer"})

    		ubx.Resource(iot.Authorizer, "ci-runner", iot.AuthorizerConfig{
    			AuthorizerFunctionArn:  "arn:aws:iam::123456789012:policy/ci",
    			Name:                   "ci-runner",
    			EnableCachingForHttp:   true,
    			SigningDisabled:        true,
    			Status:                 "example",
    			Tags:                   map[string]string{"managed-by": "ubx"},
    			TokenKeyName:           "example-token-key-name",
    			TokenSigningPublicKeys: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Authorizer } from "jsr:@ubx/sdk-aws/aws/iot/authorizer";

    export default stack("payments", () => {
      intent({ summary: "payments own authorizer" });

      resource(Authorizer, "ci-runner", {
        authorizerFunctionArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        enableCachingForHttp: true,
        signingDisabled: true,
        status: "example",
        tags: { "managed-by": "ubx" },
        tokenKeyName: "example-token-key-name",
        tokenSigningPublicKeys: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import Authorizer, AuthorizerConfig

    def describe():
        ubx.intent("payments own authorizer")

        ubx.resource(Authorizer, "ci-runner", AuthorizerConfig(
            authorizer_function_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            enable_caching_for_http=True,
            signing_disabled=True,
            status="example",
            tags={"managed-by": "ubx"},
            token_key_name="example-token-key-name",
            token_signing_public_keys={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_authorizer called "ci-runner", with authorizer
    function arn arn:aws:iam::123456789012:policy/ci, enable caching
    for http true, signing disabled true, status example, tags a set
    value, token key name example-token-key-name, token signing
    public keys a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorizer_function_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enable_caching_for_http" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="signing_disabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="token_key_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="token_signing_public_keys" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
