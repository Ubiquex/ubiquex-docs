---
title: "aws_io_t_policy_principal_attachment"
description: "`aws_io_t_policy_principal_attachment` is the real, actual grant attaching an `aws_io_t_policy` to a specific certificate or other principal."
---

A policy with nothing attached to it authorizes no real device's connection.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own t policy principal attachment"})

    		ubx.Resource(iot.PolicyPrincipalAttachment, "example", iot.PolicyPrincipalAttachmentConfig{
    			PolicyName: "example-policy-name",
    			Principal:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PolicyPrincipalAttachment } from "@ubx/sdk-aws/aws/iot/policy_principal_attachment";

    export default stack("example", () => {
      intent({ summary: "example own t policy principal attachment" });

      resource(PolicyPrincipalAttachment, "example", {
        policyName: "example-policy-name",
        principal: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot.policy_principal_attachment import PolicyPrincipalAttachment, PolicyPrincipalAttachmentConfig

    def describe():
        ubx.intent("example own t policy principal attachment")

        ubx.resource(PolicyPrincipalAttachment, "example", PolicyPrincipalAttachmentConfig(
            policy_name="example-policy-name",
            principal="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_policy_principal_attachment, with policy name
    example-policy-name, principal example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="policy_name" type="string" required>
  The name of the AWS IoT policy that will be attached to the specified principal (such as a certificate or Amazon Cognito identity) in the AWS::IoT::PolicyPrincipalAttachment resource. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="principal" type="string" required>
  The ARN of the IoT principal (e.g., a certificate ARN or Cognito identity) to which the policy is attached. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  This computed identifier uniquely represents the attachment between the IoT policy and the principal (certificate ARN or Cognito identity), typically composed of the policy name and the target principal ARN. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
