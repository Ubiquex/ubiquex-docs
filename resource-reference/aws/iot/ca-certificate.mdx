---
title: "aws_iot_ca_certificate"
description: "Real, generated bindings for `aws_iot_ca_certificate`."
---

`aws_iot_ca_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ca certificate"})

    		ubx.Resource(iot.CaCertificate, "ci-runner", iot.CaCertificateConfig{
    			Active:                     true,
    			AllowAutoRegistration:      true,
    			CaCertificatePem:           "example",
    			CertificateMode:            "example",
    			Tags:                       map[string]string{"managed-by": "ubx"},
    			VerificationCertificatePem: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CaCertificate } from "jsr:@ubx/sdk-aws/aws/iot/ca_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own ca certificate" });

      resource(CaCertificate, "ci-runner", {
        active: true,
        allowAutoRegistration: true,
        caCertificatePem: "example",
        certificateMode: "example",
        tags: { "managed-by": "ubx" },
        verificationCertificatePem: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import CaCertificate, CaCertificateConfig

    def describe():
        ubx.intent("payments own ca certificate")

        ubx.resource(CaCertificate, "ci-runner", CaCertificateConfig(
            active=True,
            allow_auto_registration=True,
            ca_certificate_pem="example",
            certificate_mode="example",
            tags={"managed-by": "ubx"},
            verification_certificate_pem="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_ca_certificate, with active true, allow auto
    registration true, ca certificate pem example, certificate mode
    example, tags a set value, verification certificate pem example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="active" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="allow_auto_registration" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="ca_certificate_pem" type="string" required>
  Required.
</ResponseField>

<ResponseField name="certificate_mode" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="verification_certificate_pem" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="customer_version" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="generation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="validity" type="object">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="not_after" type="string">
    </ResponseField>
    <ResponseField name="not_before" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
