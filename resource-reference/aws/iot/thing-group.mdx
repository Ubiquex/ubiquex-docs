---
title: "aws_io_t_thing_group"
description: "`aws_io_t_thing_group` is a real, static or dynamic (query-based) collection of `aws_io_t_thing`s..."
---

`aws_io_t_thing_group` is a real, static or dynamic (query-based) collection of `aws_io_t_thing`s, the actual grouping an `aws_io_t_job` or `aws_io_t_fleet_metric` targets rather than every operation addressing individual things one at a time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own thing group"})

    		ubx.Resource(iot.ThingGroup, "example", iot.ThingGroupConfig{
    			ParentGroupName:      "example-parent-group-name",
    			QueryString:          "example",
    			Tags:                 []map[string]any{{"key": "example"}},
    			ThingGroupName:       "example-thing-group-name",
    			ThingGroupProperties: map[string]any{"attribute_payload": map[string]any{"attributes": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ThingGroup } from "@ubx/sdk-aws/aws/iot/thing_group";

    export default stack("example", () => {
      intent({ summary: "example own thing group" });

      resource(ThingGroup, "example", {
        parentGroupName: "example-parent-group-name",
        queryString: "example",
        tags: [{ key: "example" }],
        thingGroupName: "example-thing-group-name",
        thingGroupProperties: { attributePayload: { attributes: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import ThingGroup, ThingGroupConfig
    from ubx.aws.iot.thing_group import ThingGroup_Tags, ThingGroup_ThingGroupProperties, ThingGroup_ThingGroupProperties_AttributePayload

    def describe():
        ubx.intent("example own thing group")

        ubx.resource(ThingGroup, "example", ThingGroupConfig(
            parent_group_name="example-parent-group-name",
            query_string="example",
            tags=[ThingGroup_Tags(key="example")],
            thing_group_name="example-thing-group-name",
            thing_group_properties=ThingGroup_ThingGroupProperties(attribute_payload=ThingGroup_ThingGroupProperties_AttributePayload(attributes="example")),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_io_t_thing_group, with parent group name
    example-parent-group-name, query string example, tags a set
    value, thing group name example-thing-group-name, thing group
    properties a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="parent_group_name" type="string">
  The name of the parent thing group to which this thing group belongs, establishing a hierarchical relationship in AWS IoT. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="query_string" type="string">
  The query string that defines the dynamic thing group membership criteria, selecting which AWS IoT things are included in the group. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  An array of key-value pairs to apply to this resource. Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key (name) of a tag assigned to the AWS IoT Thing Group, used to organize and identify the group in AWS. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value for a tag attached to the AWS IoT Thing Group resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="thing_group_name" type="string">
  The user-provided name for this IoT Thing Group, which must be unique within the AWS account; if omitted, CloudFormation or AWS IoT generates a unique name. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="thing_group_properties" type="object">
  The thing group properties object, which specifies the thing group's description and attribute payload (key-value pairs) for the AWS IoT Thing Group. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="attribute_payload" type="object">
      The attribute payload defining the thing group's attributes, containing a map of attribute names to values and a boolean merge flag to determine whether to append or replace existing attributes. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="attributes" type="any">
          Specifies a map of key-value pairs that define the attributes of the IoT thing group, used for describing and grouping things in AWS IoT. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="thing_group_description" type="string">
      The thing group description provides a user-defined text string that describes the purpose or contents of the IoT thing group in the AWS IoT registry. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the IoT thing group. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the IoT Thing Group, which is the thing group name as assigned by AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
