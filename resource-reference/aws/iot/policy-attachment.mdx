---
title: "aws_iot_policy_attachment"
description: "Real, generated bindings for `aws_iot_policy_attachment`."
---

`aws_iot_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"

    cfg := iot.PolicyAttachmentConfig{
        Policy: "example",
        Target: "example",
    }
    ubx.Resource(iot.PolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PolicyAttachment } from
      "jsr:@ubx/sdk-aws/aws/iot/policy_attachment";

    resource(PolicyAttachment, "example", {
      policy: "example",
      target: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iot import (
        PolicyAttachment,
        PolicyAttachmentConfig,
    )

    cfg = PolicyAttachmentConfig(
        policy="example",
        target="example",
    )
    ubx.resource(PolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iot_policy_attachment with policy, target.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
