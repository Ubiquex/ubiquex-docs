---
title: "aws_cloudwatch_contributor_insight_rule"
description: "Real, generated bindings for `aws_cloudwatch_contributor_insight_rule`."
---

`aws_cloudwatch_contributor_insight_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.ContributorInsightRuleConfig{
        RuleDefinition: "example",
        RuleName: "example-rule-name",
        RuleState: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudwatch.ContributorInsightRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ContributorInsightRule } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/contributor_insight_rule";

    resource(ContributorInsightRule, "example", {
      ruleDefinition: "example",
      ruleName: "example",
      ruleState: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        ContributorInsightRule,
        ContributorInsightRuleConfig,
    )

    cfg = ContributorInsightRuleConfig(
        rule_definition="example",
        rule_name="example",
        rule_state="example",
        tags={"key": "value"},
    )
    ubx.resource(ContributorInsightRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_contributor_insight_rule with
rule_definition, rule_name, rule_state, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_definition" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rule_state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
