---
title: "aws_cloudwatch_contributor_insight_rule"
description: "Real, generated bindings for `aws_cloudwatch_contributor_insight_rule`."
---

`aws_cloudwatch_contributor_insight_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own contributor insight rule"})

    		ubx.Resource(cloudwatch.ContributorInsightRule, "ci-runner", cloudwatch.ContributorInsightRuleConfig{
    			RuleDefinition: "example",
    			RuleName:       "example-rule-name",
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContributorInsightRule } from "jsr:@ubx/sdk-aws/aws/cloudwatch/contributor_insight_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own contributor insight rule" });

      resource(ContributorInsightRule, "ci-runner", {
        ruleDefinition: "example",
        ruleName: "example-rule-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import ContributorInsightRule, ContributorInsightRuleConfig

    def describe():
        ubx.intent("payments own contributor insight rule")

        ubx.resource(ContributorInsightRule, "ci-runner", ContributorInsightRuleConfig(
            rule_definition="example",
            rule_name="example-rule-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_contributor_insight_rule, with rule
    definition example, rule name example-rule-name, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_definition" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rule_state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
