---
title: "aws_cloudwatch_event_connection"
description: "Real, generated bindings for `aws_cloudwatch_event_connection`."
---

`aws_cloudwatch_event_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event connection"})

    		ubx.Resource(cloudwatch.EventConnection, "ci-runner", cloudwatch.EventConnectionConfig{
    			AuthorizationType: "example",
    			Name:              "ci-runner",
    			Description:       "Managed by ubx.",
    			KmsKeyIdentifier:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventConnection } from "jsr:@ubx/sdk-aws/aws/cloudwatch/event_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own event connection" });

      resource(EventConnection, "ci-runner", {
        authorizationType: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        kmsKeyIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import EventConnection, EventConnectionConfig

    def describe():
        ubx.intent("payments own event connection")

        ubx.resource(EventConnection, "ci-runner", EventConnectionConfig(
            authorization_type="example",
            name="ci-runner",
            description="Managed by ubx.",
            kms_key_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_event_connection called "ci-runner", with
    authorization type example, description Managed by ubx., kms key
    identifier example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorization_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
