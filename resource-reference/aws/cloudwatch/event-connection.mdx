---
title: "aws_cloudwatch_event_connection"
description: "Real, generated bindings for `aws_cloudwatch_event_connection`."
---

`aws_cloudwatch_event_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.EventConnectionConfig{
        AuthorizationType: "example",
        Name: "example-name",
        Description: "example",
        KmsKeyIdentifier: "example",
    }
    ubx.Resource(cloudwatch.EventConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventConnection } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/event_connection";

    resource(EventConnection, "example", {
      authorizationType: "example",
      name: "example",
      description: "example",
      kmsKeyIdentifier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        EventConnection,
        EventConnectionConfig,
    )

    cfg = EventConnectionConfig(
        authorization_type="example",
        name="example",
        description="example",
        kms_key_identifier="example",
    )
    ubx.resource(EventConnection, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_event_connection with
authorization_type, name, description, kms_key_identifier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="authorization_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="secret_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
