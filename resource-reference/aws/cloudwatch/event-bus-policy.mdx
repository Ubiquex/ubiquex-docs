---
title: "aws_cloudwatch_event_bus_policy"
description: "Real, generated bindings for `aws_cloudwatch_event_bus_policy`."
---

`aws_cloudwatch_event_bus_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.EventBusPolicyConfig{
        Policy: "example",
        EventBusName: "example-event-bus-name",
    }
    ubx.Resource(cloudwatch.EventBusPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventBusPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/event_bus_policy";

    resource(EventBusPolicy, "example", {
      policy: "example",
      eventBusName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        EventBusPolicy,
        EventBusPolicyConfig,
    )

    cfg = EventBusPolicyConfig(
        policy="example",
        event_bus_name="example",
    )
    ubx.resource(EventBusPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_event_bus_policy with policy,
event_bus_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="event_bus_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
