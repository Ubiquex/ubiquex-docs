---
title: "aws_cloudwatch_log_destination_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_destination_policy`."
---

`aws_cloudwatch_log_destination_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogDestinationPolicyConfig{
        AccessPolicy: "example",
        DestinationName: "example-destination-name",
        ForceUpdate: true,
    }
    ubx.Resource(cloudwatch.LogDestinationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogDestinationPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_destination_policy";

    resource(LogDestinationPolicy, "example", {
      accessPolicy: "example",
      destinationName: "example",
      forceUpdate: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogDestinationPolicy,
        LogDestinationPolicyConfig,
    )

    cfg = LogDestinationPolicyConfig(
        access_policy="example",
        destination_name="example",
        force_update=True,
    )
    ubx.resource(LogDestinationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_destination_policy with
access_policy, destination_name, force_update.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_update" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
