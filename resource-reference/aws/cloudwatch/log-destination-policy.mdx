---
title: "aws_cloudwatch_log_destination_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_destination_policy`."
---

`aws_cloudwatch_log_destination_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log destination policy"})

    		ubx.Resource(cloudwatch.LogDestinationPolicy, "ci-runner", cloudwatch.LogDestinationPolicyConfig{
    			AccessPolicy:    "example",
    			DestinationName: "example-destination-name",
    			ForceUpdate:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogDestinationPolicy } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_destination_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log destination policy" });

      resource(LogDestinationPolicy, "ci-runner", {
        accessPolicy: "example",
        destinationName: "example-destination-name",
        forceUpdate: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogDestinationPolicy, LogDestinationPolicyConfig

    def describe():
        ubx.intent("payments own log destination policy")

        ubx.resource(LogDestinationPolicy, "ci-runner", LogDestinationPolicyConfig(
            access_policy="example",
            destination_name="example-destination-name",
            force_update=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_destination_policy, with access policy
    example, destination name example-destination-name, force update
    true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="access_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_update" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
