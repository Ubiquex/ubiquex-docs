---
title: "aws_cloudwatch_log_account_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_account_policy`."
---

`aws_cloudwatch_log_account_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log account policy"})

    		ubx.Resource(cloudwatch.LogAccountPolicy, "ci-runner", cloudwatch.LogAccountPolicyConfig{
    			PolicyDocument:    "example",
    			PolicyName:        "example-policy-name",
    			PolicyType:        "example",
    			Scope:             "example",
    			SelectionCriteria: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogAccountPolicy } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_account_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log account policy" });

      resource(LogAccountPolicy, "ci-runner", {
        policyDocument: "example",
        policyName: "example-policy-name",
        policyType: "example",
        scope: "example",
        selectionCriteria: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogAccountPolicy, LogAccountPolicyConfig

    def describe():
        ubx.intent("payments own log account policy")

        ubx.resource(LogAccountPolicy, "ci-runner", LogAccountPolicyConfig(
            policy_document="example",
            policy_name="example-policy-name",
            policy_type="example",
            scope="example",
            selection_criteria="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_account_policy, with policy document
    example, policy name example-policy-name, policy type example,
    scope example, selection criteria example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string">
  Optional.
</ResponseField>

<ResponseField name="selection_criteria" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
