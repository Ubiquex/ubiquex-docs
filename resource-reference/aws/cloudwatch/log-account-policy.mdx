---
title: "aws_cloudwatch_log_account_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_account_policy`."
---

`aws_cloudwatch_log_account_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogAccountPolicyConfig{
        PolicyDocument: "example",
        PolicyName: "example-policy-name",
        PolicyType: "example",
        Scope: "example",
        SelectionCriteria: "example",
    }
    ubx.Resource(cloudwatch.LogAccountPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogAccountPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_account_policy";

    resource(LogAccountPolicy, "example", {
      policyDocument: "example",
      policyName: "example",
      policyType: "example",
      scope: "example",
      selectionCriteria: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogAccountPolicy,
        LogAccountPolicyConfig,
    )

    cfg = LogAccountPolicyConfig(
        policy_document="example",
        policy_name="example",
        policy_type="example",
        scope="example",
        selection_criteria="example",
    )
    ubx.resource(LogAccountPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_account_policy with policy_document,
policy_name, policy_type, scope, selection_criteria.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string">
  Optional.
</ResponseField>

<ResponseField name="selection_criteria" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
