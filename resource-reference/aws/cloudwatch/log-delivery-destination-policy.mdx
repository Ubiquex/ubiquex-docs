---
title: "aws_cloudwatch_log_delivery_destination_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_delivery_destination_policy`."
---

`aws_cloudwatch_log_delivery_destination_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.LogDeliveryDestinationPolicyConfig{
        DeliveryDestinationName: "example-delivery-destination-name",
        DeliveryDestinationPolicy: "example",
    }
    ubx.Resource(
        cloudwatch.LogDeliveryDestinationPolicy,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogDeliveryDestinationPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_delivery_destination_policy";

    resource(LogDeliveryDestinationPolicy, "example", {
      deliveryDestinationName: "example",
      deliveryDestinationPolicy: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogDeliveryDestinationPolicy,
        LogDeliveryDestinationPolicyConfig,
    )

    cfg = LogDeliveryDestinationPolicyConfig(
        delivery_destination_name="example",
        delivery_destination_policy="example",
    )
    ubx.resource(LogDeliveryDestinationPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_delivery_destination_policy with
delivery_destination_name, delivery_destination_policy.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delivery_destination_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="delivery_destination_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
