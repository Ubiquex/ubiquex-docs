---
title: "aws_cloudwatch_log_delivery_destination_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_delivery_destination_policy`."
---

`aws_cloudwatch_log_delivery_destination_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log delivery destination policy"})

    		ubx.Resource(cloudwatch.LogDeliveryDestinationPolicy, "ci-runner", cloudwatch.LogDeliveryDestinationPolicyConfig{
    			DeliveryDestinationName:   "example-delivery-destination-name",
    			DeliveryDestinationPolicy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogDeliveryDestinationPolicy } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_delivery_destination_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log delivery destination policy" });

      resource(LogDeliveryDestinationPolicy, "ci-runner", {
        deliveryDestinationName: "example-delivery-destination-name",
        deliveryDestinationPolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogDeliveryDestinationPolicy, LogDeliveryDestinationPolicyConfig

    def describe():
        ubx.intent("payments own log delivery destination policy")

        ubx.resource(LogDeliveryDestinationPolicy, "ci-runner", LogDeliveryDestinationPolicyConfig(
            delivery_destination_name="example-delivery-destination-name",
            delivery_destination_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_delivery_destination_policy, with
    delivery destination name example-delivery-destination-name,
    delivery destination policy example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delivery_destination_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="delivery_destination_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
