---
title: "aws_cloudwatch_log_metric_filter"
description: "Real, generated bindings for `aws_cloudwatch_log_metric_filter`."
---

`aws_cloudwatch_log_metric_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.LogMetricFilterConfig{
        LogGroupName: "example-log-group-name",
        Name: "example-name",
        Pattern: "example",
        ApplyOnTransformedLogs: true,
    }
    ubx.Resource(cloudwatch.LogMetricFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogMetricFilter } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_metric_filter";

    resource(LogMetricFilter, "example", {
      logGroupName: "example",
      name: "example",
      pattern: "example",
      applyOnTransformedLogs: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogMetricFilter,
        LogMetricFilterConfig,
    )

    cfg = LogMetricFilterConfig(
        log_group_name="example",
        name="example",
        pattern="example",
        apply_on_transformed_logs=True,
    )
    ubx.resource(LogMetricFilter, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_metric_filter with log_group_name,
name, pattern, apply_on_transformed_logs.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="apply_on_transformed_logs" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="pattern" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="apply_on_transformed_logs" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
