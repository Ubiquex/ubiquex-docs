---
title: "aws_cloudwatch_log_metric_filter"
description: "Real, generated bindings for `aws_cloudwatch_log_metric_filter`."
---

`aws_cloudwatch_log_metric_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log metric filter"})

    		ubx.Resource(cloudwatch.LogMetricFilter, "ci-runner", cloudwatch.LogMetricFilterConfig{
    			LogGroupName: "example-log-group-name",
    			Name:         "ci-runner",
    			Pattern:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogMetricFilter } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_metric_filter";

    export default stack("payments", () => {
      intent({ summary: "payments own log metric filter" });

      resource(LogMetricFilter, "ci-runner", {
        logGroupName: "example-log-group-name",
        name: "ci-runner",
        pattern: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogMetricFilter, LogMetricFilterConfig

    def describe():
        ubx.intent("payments own log metric filter")

        ubx.resource(LogMetricFilter, "ci-runner", LogMetricFilterConfig(
            log_group_name="example-log-group-name",
            name="ci-runner",
            pattern="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_metric_filter called "ci-runner", with
    log group name example-log-group-name, pattern example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="apply_on_transformed_logs" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="pattern" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="apply_on_transformed_logs" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
