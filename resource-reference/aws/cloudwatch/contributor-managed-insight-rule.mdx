---
title: "aws_cloudwatch_contributor_managed_insight_rule"
description: "Real, generated bindings for `aws_cloudwatch_contributor_managed_insight_rule`."
---

`aws_cloudwatch_contributor_managed_insight_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own contributor managed insight rule"})

    		ubx.Resource(cloudwatch.ContributorManagedInsightRule, "ci-runner", cloudwatch.ContributorManagedInsightRuleConfig{
    			ResourceArn:  "arn:aws:iam::123456789012:policy/ci",
    			TemplateName: "example-template-name",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContributorManagedInsightRule } from "jsr:@ubx/sdk-aws/aws/cloudwatch/contributor_managed_insight_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own contributor managed insight rule" });

      resource(ContributorManagedInsightRule, "ci-runner", {
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        templateName: "example-template-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import ContributorManagedInsightRule, ContributorManagedInsightRuleConfig

    def describe():
        ubx.intent("payments own contributor managed insight rule")

        ubx.resource(ContributorManagedInsightRule, "ci-runner", ContributorManagedInsightRuleConfig(
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            template_name="example-template-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_contributor_managed_insight_rule, with
    resource arn arn:aws:iam::123456789012:policy/ci, template name
    example-template-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="template_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rule_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
