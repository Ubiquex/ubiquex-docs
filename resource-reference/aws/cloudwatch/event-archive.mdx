---
title: "aws_cloudwatch_event_archive"
description: "Real, generated bindings for `aws_cloudwatch_event_archive`."
---

`aws_cloudwatch_event_archive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event archive"})

    		ubx.Resource(cloudwatch.EventArchive, "ci-runner", cloudwatch.EventArchiveConfig{
    			EventSourceArn:   "arn:aws:iam::123456789012:policy/ci",
    			Name:             "ci-runner",
    			Description:      "Managed by ubx.",
    			EventPattern:     "example",
    			KmsKeyIdentifier: "example",
    			RetentionDays:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventArchive } from "jsr:@ubx/sdk-aws/aws/cloudwatch/event_archive";

    export default stack("payments", () => {
      intent({ summary: "payments own event archive" });

      resource(EventArchive, "ci-runner", {
        eventSourceArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
        description: "Managed by ubx.",
        eventPattern: "example",
        kmsKeyIdentifier: "example",
        retentionDays: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import EventArchive, EventArchiveConfig

    def describe():
        ubx.intent("payments own event archive")

        ubx.resource(EventArchive, "ci-runner", EventArchiveConfig(
            event_source_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
            description="Managed by ubx.",
            event_pattern="example",
            kms_key_identifier="example",
            retention_days=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_event_archive called "ci-runner", with
    event source arn arn:aws:iam::123456789012:policy/ci, description
    Managed by ubx., event pattern example, kms key identifier
    example, retention days 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_pattern" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_identifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_days" type="number">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
