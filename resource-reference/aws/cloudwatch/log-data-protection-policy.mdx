---
title: "aws_cloudwatch_log_data_protection_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_data_protection_policy`."
---

`aws_cloudwatch_log_data_protection_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log data protection policy"})

    		ubx.Resource(cloudwatch.LogDataProtectionPolicy, "ci-runner", cloudwatch.LogDataProtectionPolicyConfig{
    			LogGroupName:   "example-log-group-name",
    			PolicyDocument: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogDataProtectionPolicy } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_data_protection_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log data protection policy" });

      resource(LogDataProtectionPolicy, "ci-runner", {
        logGroupName: "example-log-group-name",
        policyDocument: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogDataProtectionPolicy, LogDataProtectionPolicyConfig

    def describe():
        ubx.intent("payments own log data protection policy")

        ubx.resource(LogDataProtectionPolicy, "ci-runner", LogDataProtectionPolicyConfig(
            log_group_name="example-log-group-name",
            policy_document="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_data_protection_policy, with log group
    name example-log-group-name, policy document example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
