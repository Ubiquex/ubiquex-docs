---
title: "aws_cloudwatch_log_s3_table_integration_source"
description: "Real, generated bindings for `aws_cloudwatch_log_s3_table_integration_source`."
---

`aws_cloudwatch_log_s3_table_integration_source` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.LogS3TableIntegrationSourceConfig{
        IntegrationArn: "example",
    }
    ubx.Resource(
        cloudwatch.LogS3TableIntegrationSource,
        "example",
        cfg,
    )
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogS3TableIntegrationSource } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_s3_table_integration_source";

    resource(LogS3TableIntegrationSource, "example", {
      integrationArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogS3TableIntegrationSource,
        LogS3TableIntegrationSourceConfig,
    )

    cfg = LogS3TableIntegrationSourceConfig(
        integration_arn="example",
    )
    ubx.resource(LogS3TableIntegrationSource, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_s3_table_integration_source with
integration_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="integration_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
