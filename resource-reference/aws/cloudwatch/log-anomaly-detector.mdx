---
title: "aws_cloudwatch_log_anomaly_detector"
description: "Real, generated bindings for `aws_cloudwatch_log_anomaly_detector`."
---

`aws_cloudwatch_log_anomaly_detector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.LogAnomalyDetectorConfig{
        Enabled: true,
        LogGroupArnList: []string{"example"},
        AnomalyVisibilityTime: 1,
        DetectorName: "example-detector-name",
    }
    ubx.Resource(cloudwatch.LogAnomalyDetector, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogAnomalyDetector } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_anomaly_detector";

    resource(LogAnomalyDetector, "example", {
      enabled: true,
      logGroupArnList: ["example"],
      anomalyVisibilityTime: 1,
      detectorName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogAnomalyDetector,
        LogAnomalyDetectorConfig,
    )

    cfg = LogAnomalyDetectorConfig(
        enabled=True,
        log_group_arn_list=["example"],
        anomaly_visibility_time=1,
        detector_name="example",
    )
    ubx.resource(LogAnomalyDetector, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_anomaly_detector with enabled,
log_group_arn_list, anomaly_visibility_time, detector_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="anomaly_visibility_time" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="detector_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="evaluation_frequency" type="string">
  Optional.
</ResponseField>

<ResponseField name="filter_pattern" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="log_group_arn_list" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="anomaly_visibility_time" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
