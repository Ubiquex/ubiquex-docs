---
title: "aws_cloudwatch_log_anomaly_detector"
description: "Real, generated bindings for `aws_cloudwatch_log_anomaly_detector`."
---

`aws_cloudwatch_log_anomaly_detector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log anomaly detector"})

    		ubx.Resource(cloudwatch.LogAnomalyDetector, "ci-runner", cloudwatch.LogAnomalyDetectorConfig{
    			Enabled:             true,
    			LogGroupArnList:     []string{"example"},
    			DetectorName:        "example-detector-name",
    			EvaluationFrequency: "example",
    			FilterPattern:       "example",
    			KmsKeyId:            "example",
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogAnomalyDetector } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_anomaly_detector";

    export default stack("payments", () => {
      intent({ summary: "payments own log anomaly detector" });

      resource(LogAnomalyDetector, "ci-runner", {
        enabled: true,
        logGroupArnList: ["example"],
        detectorName: "example-detector-name",
        evaluationFrequency: "example",
        filterPattern: "example",
        kmsKeyId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogAnomalyDetector, LogAnomalyDetectorConfig

    def describe():
        ubx.intent("payments own log anomaly detector")

        ubx.resource(LogAnomalyDetector, "ci-runner", LogAnomalyDetectorConfig(
            enabled=True,
            log_group_arn_list=["example"],
            detector_name="example-detector-name",
            evaluation_frequency="example",
            filter_pattern="example",
            kms_key_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_anomaly_detector, with enabled true,
    log group arn list a set value, detector name
    example-detector-name, evaluation frequency example, filter
    pattern example, kms key id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="anomaly_visibility_time" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="detector_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="evaluation_frequency" type="string">
  Optional.
</ResponseField>

<ResponseField name="filter_pattern" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="log_group_arn_list" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="anomaly_visibility_time" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
