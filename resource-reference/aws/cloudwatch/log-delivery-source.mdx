---
title: "aws_cloudwatch_log_delivery_source"
description: "Real, generated bindings for `aws_cloudwatch_log_delivery_source`."
---

`aws_cloudwatch_log_delivery_source` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log delivery source"})

    		ubx.Resource(cloudwatch.LogDeliverySource, "ci-runner", cloudwatch.LogDeliverySourceConfig{
    			LogType:     "example",
    			Name:        "ci-runner",
    			ResourceArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogDeliverySource } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_delivery_source";

    export default stack("payments", () => {
      intent({ summary: "payments own log delivery source" });

      resource(LogDeliverySource, "ci-runner", {
        logType: "example",
        name: "ci-runner",
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogDeliverySource, LogDeliverySourceConfig

    def describe():
        ubx.intent("payments own log delivery source")

        ubx.resource(LogDeliverySource, "ci-runner", LogDeliverySourceConfig(
            log_type="example",
            name="ci-runner",
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_delivery_source called "ci-runner",
    with log type example, resource arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="log_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
