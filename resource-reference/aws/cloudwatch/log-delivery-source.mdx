---
title: "aws_cloudwatch_log_delivery_source"
description: "Real, generated bindings for `aws_cloudwatch_log_delivery_source`."
---

`aws_cloudwatch_log_delivery_source` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.LogDeliverySourceConfig{
        LogType: "example",
        Name: "example-name",
        ResourceArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudwatch.LogDeliverySource, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogDeliverySource } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_delivery_source";

    resource(LogDeliverySource, "example", {
      logType: "example",
      name: "example",
      resourceArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogDeliverySource,
        LogDeliverySourceConfig,
    )

    cfg = LogDeliverySourceConfig(
        log_type="example",
        name="example",
        resource_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(LogDeliverySource, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_delivery_source with log_type, name,
resource_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="log_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
