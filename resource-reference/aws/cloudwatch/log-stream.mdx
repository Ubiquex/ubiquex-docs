---
title: "aws_cloudwatch_log_stream"
description: "Real, generated bindings for `aws_cloudwatch_log_stream`."
---

`aws_cloudwatch_log_stream` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogStreamConfig{
        LogGroupName: "example-log-group-name",
        Name: "example-name",
    }
    ubx.Resource(cloudwatch.LogStream, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogStream } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_stream";

    resource(LogStream, "example", {
      logGroupName: "example",
      name: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import LogStream, LogStreamConfig

    cfg = LogStreamConfig(
        log_group_name="example",
        name="example",
    )
    ubx.resource(LogStream, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_stream with log_group_name, name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
