---
title: "aws_cloudwatch_log_storage_tier_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_storage_tier_policy`."
---

`aws_cloudwatch_log_storage_tier_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogStorageTierPolicyConfig{
        StorageTier: "example",
    }
    ubx.Resource(cloudwatch.LogStorageTierPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogStorageTierPolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_storage_tier_policy";

    resource(LogStorageTierPolicy, "example", {
      storageTier: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogStorageTierPolicy,
        LogStorageTierPolicyConfig,
    )

    cfg = LogStorageTierPolicyConfig(
        storage_tier="example",
    )
    ubx.resource(LogStorageTierPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_storage_tier_policy with
storage_tier.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_tier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
