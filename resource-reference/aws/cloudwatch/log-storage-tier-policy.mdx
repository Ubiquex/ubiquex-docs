---
title: "aws_cloudwatch_log_storage_tier_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_storage_tier_policy`."
---

`aws_cloudwatch_log_storage_tier_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log storage tier policy"})

    		ubx.Resource(cloudwatch.LogStorageTierPolicy, "ci-runner", cloudwatch.LogStorageTierPolicyConfig{
    			StorageTier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogStorageTierPolicy } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_storage_tier_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log storage tier policy" });

      resource(LogStorageTierPolicy, "ci-runner", {
        storageTier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogStorageTierPolicy, LogStorageTierPolicyConfig

    def describe():
        ubx.intent("payments own log storage tier policy")

        ubx.resource(LogStorageTierPolicy, "ci-runner", LogStorageTierPolicyConfig(
            storage_tier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_storage_tier_policy, with storage tier
    example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="storage_tier" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
