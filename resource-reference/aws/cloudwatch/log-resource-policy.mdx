---
title: "aws_cloudwatch_log_resource_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_resource_policy`."
---

`aws_cloudwatch_log_resource_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.LogResourcePolicyConfig{
        PolicyDocument: "example",
        PolicyName: "example-policy-name",
        ResourceArn: "example",
    }
    ubx.Resource(cloudwatch.LogResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogResourcePolicy } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_resource_policy";

    resource(LogResourcePolicy, "example", {
      policyDocument: "example",
      policyName: "example",
      resourceArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogResourcePolicy,
        LogResourcePolicyConfig,
    )

    cfg = LogResourcePolicyConfig(
        policy_document="example",
        policy_name="example",
        resource_arn="example",
    )
    ubx.resource(LogResourcePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_resource_policy with
policy_document, policy_name, resource_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_scope" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="revision_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
