---
title: "aws_cloudwatch_log_subscription_filter"
description: "Real, generated bindings for `aws_cloudwatch_log_subscription_filter`."
---

`aws_cloudwatch_log_subscription_filter` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log subscription filter"})

    		ubx.Resource(cloudwatch.LogSubscriptionFilter, "ci-runner", cloudwatch.LogSubscriptionFilterConfig{
    			DestinationArn:   "arn:aws:iam::123456789012:policy/ci",
    			FilterPattern:    "example",
    			LogGroupName:     "example-log-group-name",
    			Name:             "ci-runner",
    			Distribution:     "example",
    			EmitSystemFields: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogSubscriptionFilter } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_subscription_filter";

    export default stack("payments", () => {
      intent({ summary: "payments own log subscription filter" });

      resource(LogSubscriptionFilter, "ci-runner", {
        destinationArn: "arn:aws:iam::123456789012:policy/ci",
        filterPattern: "example",
        logGroupName: "example-log-group-name",
        name: "ci-runner",
        distribution: "example",
        emitSystemFields: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogSubscriptionFilter, LogSubscriptionFilterConfig

    def describe():
        ubx.intent("payments own log subscription filter")

        ubx.resource(LogSubscriptionFilter, "ci-runner", LogSubscriptionFilterConfig(
            destination_arn="arn:aws:iam::123456789012:policy/ci",
            filter_pattern="example",
            log_group_name="example-log-group-name",
            name="ci-runner",
            distribution="example",
            emit_system_fields=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_subscription_filter called "ci-runner",
    with destination arn arn:aws:iam::123456789012:policy/ci, filter
    pattern example, log group name example-log-group-name,
    distribution example, emit system fields a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="apply_on_transformed_logs" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="destination_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="distribution" type="string">
  Optional.
</ResponseField>

<ResponseField name="emit_system_fields" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="filter_pattern" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="apply_on_transformed_logs" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
