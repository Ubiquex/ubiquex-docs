---
title: "aws_cloudwatch_event_endpoint"
description: "Real, generated bindings for `aws_cloudwatch_event_endpoint`."
---

`aws_cloudwatch_event_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.EventEndpointConfig{
        Name: "example-name",
        Description: "example",
        RoleArn: "example",
    }
    ubx.Resource(cloudwatch.EventEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventEndpoint } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/event_endpoint";

    resource(EventEndpoint, "example", {
      name: "example",
      description: "example",
      roleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        EventEndpoint,
        EventEndpointConfig,
    )

    cfg = EventEndpointConfig(
        name="example",
        description="example",
        role_arn="example",
    )
    ubx.resource(EventEndpoint, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_event_endpoint with name, description,
role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
