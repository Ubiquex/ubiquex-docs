---
title: "aws_cloudwatch_event_endpoint"
description: "Real, generated bindings for `aws_cloudwatch_event_endpoint`."
---

`aws_cloudwatch_event_endpoint` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event endpoint"})

    		ubx.Resource(cloudwatch.EventEndpoint, "ci-runner", cloudwatch.EventEndpointConfig{
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			RoleArn:     "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventEndpoint } from "jsr:@ubx/sdk-aws/aws/cloudwatch/event_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own event endpoint" });

      resource(EventEndpoint, "ci-runner", {
        name: "ci-runner",
        description: "Managed by ubx.",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import EventEndpoint, EventEndpointConfig

    def describe():
        ubx.intent("payments own event endpoint")

        ubx.resource(EventEndpoint, "ci-runner", EventEndpointConfig(
            name="ci-runner",
            description="Managed by ubx.",
            role_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_event_endpoint called "ci-runner", with
    description Managed by ubx., role arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="endpoint_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
