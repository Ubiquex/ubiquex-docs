---
title: "aws_cloudwatch_log_delivery_destination"
description: "Real, generated bindings for `aws_cloudwatch_log_delivery_destination`."
---

`aws_cloudwatch_log_delivery_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogDeliveryDestinationConfig{
        Name: "example-name",
        DeliveryDestinationType: "example",
        OutputFormat: "example",
    }
    ubx.Resource(cloudwatch.LogDeliveryDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogDeliveryDestination } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_delivery_destination";

    resource(LogDeliveryDestination, "example", {
      name: "example",
      deliveryDestinationType: "example",
      outputFormat: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogDeliveryDestination,
        LogDeliveryDestinationConfig,
    )

    cfg = LogDeliveryDestinationConfig(
        name="example",
        delivery_destination_type="example",
        output_format="example",
    )
    ubx.resource(LogDeliveryDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_delivery_destination with name,
delivery_destination_type, output_format.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="delivery_destination_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="output_format" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="delivery_destination_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
