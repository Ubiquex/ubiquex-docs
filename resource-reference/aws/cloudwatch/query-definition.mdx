---
title: "aws_cloudwatch_query_definition"
description: "Real, generated bindings for `aws_cloudwatch_query_definition`."
---

`aws_cloudwatch_query_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.QueryDefinitionConfig{
        Name: "example-name",
        QueryString: "example",
        LogGroupNames: []string{"example"},
    }
    ubx.Resource(cloudwatch.QueryDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { QueryDefinition } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/query_definition";

    resource(QueryDefinition, "example", {
      name: "example",
      queryString: "example",
      logGroupNames: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        QueryDefinition,
        QueryDefinitionConfig,
    )

    cfg = QueryDefinitionConfig(
        name="example",
        query_string="example",
        log_group_names=["example"],
    )
    ubx.resource(QueryDefinition, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_query_definition with name,
query_string, log_group_names.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_names" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="query_definition_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
