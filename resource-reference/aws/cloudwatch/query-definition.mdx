---
title: "aws_cloudwatch_query_definition"
description: "Real, generated bindings for `aws_cloudwatch_query_definition`."
---

`aws_cloudwatch_query_definition` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own query definition"})

    		ubx.Resource(cloudwatch.QueryDefinition, "ci-runner", cloudwatch.QueryDefinitionConfig{
    			Name:          "ci-runner",
    			QueryString:   "example",
    			LogGroupNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QueryDefinition } from "jsr:@ubx/sdk-aws/aws/cloudwatch/query_definition";

    export default stack("payments", () => {
      intent({ summary: "payments own query definition" });

      resource(QueryDefinition, "ci-runner", {
        name: "ci-runner",
        queryString: "example",
        logGroupNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import QueryDefinition, QueryDefinitionConfig

    def describe():
        ubx.intent("payments own query definition")

        ubx.resource(QueryDefinition, "ci-runner", QueryDefinitionConfig(
            name="ci-runner",
            query_string="example",
            log_group_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_query_definition called "ci-runner", with
    query string example, log group names a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_names" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="query_string" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="query_definition_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
