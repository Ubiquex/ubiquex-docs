---
title: "aws_cloudwatch_event_api_destination"
description: "Real, generated bindings for `aws_cloudwatch_event_api_destination`."
---

`aws_cloudwatch_event_api_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event api destination"})

    		ubx.Resource(cloudwatch.EventApiDestination, "ci-runner", cloudwatch.EventApiDestinationConfig{
    			ConnectionArn:                "arn:aws:iam::123456789012:policy/ci",
    			HttpMethod:                   "example",
    			InvocationEndpoint:           "example",
    			Name:                         "ci-runner",
    			Description:                  "Managed by ubx.",
    			InvocationRateLimitPerSecond: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventApiDestination } from "jsr:@ubx/sdk-aws/aws/cloudwatch/event_api_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own event api destination" });

      resource(EventApiDestination, "ci-runner", {
        connectionArn: "arn:aws:iam::123456789012:policy/ci",
        httpMethod: "example",
        invocationEndpoint: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        invocationRateLimitPerSecond: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import EventApiDestination, EventApiDestinationConfig

    def describe():
        ubx.intent("payments own event api destination")

        ubx.resource(EventApiDestination, "ci-runner", EventApiDestinationConfig(
            connection_arn="arn:aws:iam::123456789012:policy/ci",
            http_method="example",
            invocation_endpoint="example",
            name="ci-runner",
            description="Managed by ubx.",
            invocation_rate_limit_per_second=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_event_api_destination called "ci-runner",
    with connection arn arn:aws:iam::123456789012:policy/ci, http
    method example, invocation endpoint example, description Managed
    by ubx., invocation rate limit per second 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="connection_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="http_method" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invocation_endpoint" type="string" required>
  Required.
</ResponseField>

<ResponseField name="invocation_rate_limit_per_second" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
