---
title: "aws_cloudwatch_event_permission"
description: "Real, generated bindings for `aws_cloudwatch_event_permission`."
---

`aws_cloudwatch_event_permission` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.EventPermissionConfig{
        Principal: "example",
        StatementId: "example",
        Action: "example",
        EventBusName: "example-event-bus-name",
    }
    ubx.Resource(cloudwatch.EventPermission, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventPermission } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/event_permission";

    resource(EventPermission, "example", {
      principal: "example",
      statementId: "example",
      action: "example",
      eventBusName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        EventPermission,
        EventPermissionConfig,
    )

    cfg = EventPermissionConfig(
        principal="example",
        statement_id="example",
        action="example",
        event_bus_name="example",
    )
    ubx.resource(EventPermission, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_event_permission with principal,
statement_id, action, event_bus_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="action" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_bus_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="principal" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="statement_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
