---
title: "aws_cloudwatch_alarm_mute_rule"
description: "Real, generated bindings for `aws_cloudwatch_alarm_mute_rule`."
---

`aws_cloudwatch_alarm_mute_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.AlarmMuteRuleConfig{
        Name: "example-name",
        Description: "example",
        ExpireDate: "example",
    }
    ubx.Resource(cloudwatch.AlarmMuteRule, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AlarmMuteRule } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/alarm_mute_rule";

    resource(AlarmMuteRule, "example", {
      name: "example",
      description: "example",
      expireDate: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        AlarmMuteRule,
        AlarmMuteRuleConfig,
    )

    cfg = AlarmMuteRuleConfig(
        name="example",
        description="example",
        expire_date="example",
    )
    ubx.resource(AlarmMuteRule, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_alarm_mute_rule with name, description,
expire_date.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="expire_date" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="start_date" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="mute_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
