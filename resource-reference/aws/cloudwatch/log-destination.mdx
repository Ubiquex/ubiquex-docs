---
title: "aws_cloudwatch_log_destination"
description: "Real, generated bindings for `aws_cloudwatch_log_destination`."
---

`aws_cloudwatch_log_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogDestinationConfig{
        Name: "example-name",
        RoleArn: "example",
        TargetArn: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(cloudwatch.LogDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogDestination } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_destination";

    resource(LogDestination, "example", {
      name: "example",
      roleArn: "example",
      targetArn: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogDestination,
        LogDestinationConfig,
    )

    cfg = LogDestinationConfig(
        name="example",
        role_arn="example",
        target_arn="example",
        tags={"key": "value"},
    )
    ubx.resource(LogDestination, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_destination with name, role_arn,
target_arn, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="target_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
