---
title: "aws_cloudwatch_log_transformer"
description: "Real, generated bindings for `aws_cloudwatch_log_transformer`."
---

`aws_cloudwatch_log_transformer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.LogTransformerConfig{
        LogGroupArn: "example",
    }
    ubx.Resource(cloudwatch.LogTransformer, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { LogTransformer } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/log_transformer";

    resource(LogTransformer, "example", {
      logGroupArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        LogTransformer,
        LogTransformerConfig,
    )

    cfg = LogTransformerConfig(
        log_group_arn="example",
    )
    ubx.resource(LogTransformer, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_log_transformer with log_group_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="log_group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
