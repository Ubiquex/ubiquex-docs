---
title: "aws_cloudwatch_log_transformer"
description: "Real, generated bindings for `aws_cloudwatch_log_transformer`."
---

`aws_cloudwatch_log_transformer` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log transformer"})

    		ubx.Resource(cloudwatch.LogTransformer, "ci-runner", cloudwatch.LogTransformerConfig{
    			LogGroupArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogTransformer } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_transformer";

    export default stack("payments", () => {
      intent({ summary: "payments own log transformer" });

      resource(LogTransformer, "ci-runner", {
        logGroupArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogTransformer, LogTransformerConfig

    def describe():
        ubx.intent("payments own log transformer")

        ubx.resource(LogTransformer, "ci-runner", LogTransformerConfig(
            log_group_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_transformer, with log group arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="log_group_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
