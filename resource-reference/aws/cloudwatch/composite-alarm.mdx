---
title: "aws_cloudwatch_composite_alarm"
description: "Real, generated bindings for `aws_cloudwatch_composite_alarm`."
---

`aws_cloudwatch_composite_alarm` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.CompositeAlarmConfig{
        AlarmName: "example-alarm-name",
        AlarmRule: "example",
        ActionsEnabled: true,
        AlarmActions: []string{"example"},
    }
    ubx.Resource(cloudwatch.CompositeAlarm, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { CompositeAlarm } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/composite_alarm";

    resource(CompositeAlarm, "example", {
      alarmName: "example",
      alarmRule: "example",
      actionsEnabled: true,
      alarmActions: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        CompositeAlarm,
        CompositeAlarmConfig,
    )

    cfg = CompositeAlarmConfig(
        alarm_name="example",
        alarm_rule="example",
        actions_enabled=True,
        alarm_actions=["example"],
    )
    ubx.resource(CompositeAlarm, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_composite_alarm with alarm_name,
alarm_rule, actions_enabled, alarm_actions.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="actions_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="alarm_actions" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="alarm_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="alarm_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="alarm_rule" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="insufficient_data_actions" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="ok_actions" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
