---
title: "aws_cloudwatch_dashboard"
description: "Real, generated bindings for `aws_cloudwatch_dashboard`."
---

`aws_cloudwatch_dashboard` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dashboard"})

    		ubx.Resource(cloudwatch.Dashboard, "ci-runner", cloudwatch.DashboardConfig{
    			DashboardBody: "example",
    			DashboardName: "example-dashboard-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Dashboard } from "jsr:@ubx/sdk-aws/aws/cloudwatch/dashboard";

    export default stack("payments", () => {
      intent({ summary: "payments own dashboard" });

      resource(Dashboard, "ci-runner", {
        dashboardBody: "example",
        dashboardName: "example-dashboard-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import Dashboard, DashboardConfig

    def describe():
        ubx.intent("payments own dashboard")

        ubx.resource(Dashboard, "ci-runner", DashboardConfig(
            dashboard_body="example",
            dashboard_name="example-dashboard-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_dashboard, with dashboard body example,
    dashboard name example-dashboard-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dashboard_body" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dashboard_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="dashboard_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
