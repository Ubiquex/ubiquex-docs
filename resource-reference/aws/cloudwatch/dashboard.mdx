---
title: "aws_cloudwatch_dashboard"
description: "Real, generated bindings for `aws_cloudwatch_dashboard`."
---

`aws_cloudwatch_dashboard` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/cloudwatch"

    cfg := cloudwatch.DashboardConfig{
        DashboardBody: "example",
        DashboardName: "example-dashboard-name",
    }
    ubx.Resource(cloudwatch.Dashboard, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { Dashboard } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/dashboard";

    resource(Dashboard, "example", {
      dashboardBody: "example",
      dashboardName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import Dashboard, DashboardConfig

    cfg = DashboardConfig(
        dashboard_body="example",
        dashboard_name="example",
    )
    ubx.resource(Dashboard, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_dashboard with dashboard_body,
dashboard_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="dashboard_body" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dashboard_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="dashboard_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
