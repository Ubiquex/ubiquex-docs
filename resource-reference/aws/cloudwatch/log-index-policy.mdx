---
title: "aws_cloudwatch_log_index_policy"
description: "Real, generated bindings for `aws_cloudwatch_log_index_policy`."
---

`aws_cloudwatch_log_index_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own log index policy"})

    		ubx.Resource(cloudwatch.LogIndexPolicy, "ci-runner", cloudwatch.LogIndexPolicyConfig{
    			LogGroupName:   "example-log-group-name",
    			PolicyDocument: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LogIndexPolicy } from "jsr:@ubx/sdk-aws/aws/cloudwatch/log_index_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own log index policy" });

      resource(LogIndexPolicy, "ci-runner", {
        logGroupName: "example-log-group-name",
        policyDocument: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import LogIndexPolicy, LogIndexPolicyConfig

    def describe():
        ubx.intent("payments own log index policy")

        ubx.resource(LogIndexPolicy, "ci-runner", LogIndexPolicyConfig(
            log_group_name="example-log-group-name",
            policy_document="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_log_index_policy, with log group name
    example-log-group-name, policy document example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
