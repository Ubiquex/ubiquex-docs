---
title: "aws_cloudwatch_otel_enrichment"
description: "Real, generated bindings for `aws_cloudwatch_otel_enrichment`."
---

`aws_cloudwatch_otel_enrichment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"

    cfg := cloudwatch.OtelEnrichmentConfig{
    }
    ubx.Resource(cloudwatch.OtelEnrichment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OtelEnrichment } from
      "jsr:@ubx/sdk-aws/aws/cloudwatch/otel_enrichment";

    resource(OtelEnrichment, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.cloudwatch import (
        OtelEnrichment,
        OtelEnrichmentConfig,
    )

    cfg = OtelEnrichmentConfig(
    )
    ubx.resource(OtelEnrichment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_cloudwatch_otel_enrichment.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
