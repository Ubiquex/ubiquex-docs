---
title: "aws_cloudwatch_otel_enrichment"
description: "Real, generated bindings for `aws_cloudwatch_otel_enrichment`."
---

`aws_cloudwatch_otel_enrichment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudwatch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudwatch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own otel enrichment"})

    		ubx.Resource(cloudwatch.OtelEnrichment, "ci-runner", cloudwatch.OtelEnrichmentConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OtelEnrichment } from "jsr:@ubx/sdk-aws/aws/cloudwatch/otel_enrichment";

    export default stack("payments", () => {
      intent({ summary: "payments own otel enrichment" });

      resource(OtelEnrichment, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudwatch import OtelEnrichment, OtelEnrichmentConfig

    def describe():
        ubx.intent("payments own otel enrichment")

        ubx.resource(OtelEnrichment, "ci-runner", OtelEnrichmentConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudwatch_otel_enrichment, with .
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
