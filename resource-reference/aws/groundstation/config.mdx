---
title: "aws_ground_station_config"
description: "`aws_ground_station_config` defines one real, specific piece of a satellite contact, antenna downlink parameters, tracking, dataflow endpoint details..."
---

`aws_ground_station_config` defines one real, specific piece of a satellite contact, antenna downlink parameters, tracking, dataflow endpoint details, that an `aws_ground_station_mission_profile` combines several of into a complete contact plan.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	groundstation "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/groundstation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own station config"})

    		ubx.Resource(groundstation.Config, "example", groundstation.ConfigConfig{
    			ConfigData: map[string]any{"antenna_downlink_config": map[string]any{"spectrum_config": map[string]any{"bandwidth": map[string]any{"units": "example"}}}},
    			Name:       "example",
    			Tags:       []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Config } from "@ubx/sdk-aws/aws/groundstation/config";

    export default stack("example", () => {
      intent({ summary: "example own station config" });

      resource(Config, "example", {
        configData: {
          antennaDownlinkConfig: {
            spectrumConfig: { bandwidth: { units: "example" } },
          },
        },
        name: "example",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.groundstation.config import Config, ConfigConfig

    def describe():
        ubx.intent("example own station config")

        ubx.resource(Config, "example", ConfigConfig(
            config_data={"antenna_downlink_config": {"spectrum_config": {"bandwidth": {"units": "example"}}}},
            name="example",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ground_station_config called "example", with config
    data a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config_data" type="object" required>
  The required config data that defines this Ground Station config, containing one of the supported configuration types (such as antenna downlink, antenna uplink, antenna downlink demod decode, tracking, dataflow endpoint, or uplink echo) along with its specific parameters. Required. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="antenna_downlink_config" type="object">
      Defines the receive (downlink) configuration for the Ground Station antenna, specifying parameters such as frequency, bandwidth, and polarization to capture satellite data. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="spectrum_config" type="object">
          Defines the spectral characteristics of the downlink signal, including center frequency, bandwidth, and polarization, used by the Ground Station to receive data from a satellite. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bandwidth" type="object">
              The bandwidth of the downlink signal spectrum, specified as an object with a numeric value and a unit (GHz, MHz, or kHz), defining the width of the frequency range to receive. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="units" type="string">
                  The unit of measurement for the downlink bandwidth (e.g., MHz or GHz) in the spectrum configuration of the antenna downlink for the Ground Station config. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The numeric magnitude of the downlink spectrum bandwidth, which is interpreted along with the separate units field to determine the actual bandwidth in hertz (e.g., if units is 'MHz', the bandwidth in Hz equals this value times 1e6). Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="center_frequency" type="object">
              Specifies the center frequency of the downlink signal for the antenna's receive spectrum, used to tune the Ground Station receiver. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="units" type="string">
                  Specifies the unit of measurement for the center frequency, either 'GHz' or 'MHz'. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The center frequency value in hertz for the downlink spectrum, used within the antenna downlink config's spectrum config. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="polarization" type="string">
              The polarization mode (RIGHT_HAND or LEFT_HAND) for the downlink spectrum in the antenna downlink config, specifying the electromagnetic wave orientation used for receiving the signal. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="antenna_downlink_demod_decode_config" type="object">
      Defines the demodulation and decode configuration for an antenna downlink, including the demodulation settings and decode configuration (typically specified as JSON) used to process the received satellite signal. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="decode_config" type="object">
          Specifies how the downlink signal is decoded, providing a decode configuration consisting of an unvalidated JSON payload that defines the decoding parameters for the Ground Station antenna downlink demodulation and decode processing. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unvalidated_json" type="string">
              Provides the unvalidated JSON that defines the decode configuration for the antenna downlink, specifying how the received signal is demodulated and decoded. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="demodulation_config" type="object">
          Configures the demodulation scheme (e.g., BPSK or QPSK) and processing options used by the antenna downlink demodulation and decode chain. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="unvalidated_json" type="string">
              Specifies the demodulation configuration as a raw JSON string, containing the parameters for demodulating the received signal in the Ground Station antenna downlink. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="spectrum_config" type="object">
          Configures the center frequency and bandwidth of the downlink spectrum to be processed for this demodulation and decoding step. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="bandwidth" type="object">
              Defines the bandwidth of the downlink spectrum signal, specifying the numeric value and its units (e.g., MHz or kHz) used in the antenna downlink demodulate/decode configuration. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="units" type="string">
                  The unit of measure for the bandwidth value (such as MHz or GHz) in the spectrum configuration for the antenna downlink demodulation and decoding. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The value is the numeric magnitude of the frequency bandwidth (in the unit given by the sibling `units` field) for the downlink demodulation decode spectrum config, used to set the demodulator's bandwidth for the received signal. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="center_frequency" type="object">
              Specifies the center frequency of the downlink signal spectrum for antenna demodulation and decoding, represented as a frequency object with a numeric value and a unit (e.g., Hz, MHz, GHz). Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="units" type="string">
                  Specifies the unit of measurement for the center frequency value, such as GHz, MHz, or kHz. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The numeric value of the center frequency, expressed in the units set by the parent center_frequency object, used for the downlink demodulation and decoding of the antenna signal. Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="polarization" type="string">
              Specifies the polarization of the downlink signal, which can be 'RIGHT_HAND' or 'LEFT_HAND'. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="antenna_uplink_config" type="object">
      Defines the uplink spectrum settings and target satellite ARN for an antenna uplink configuration in AWS Ground Station. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="spectrum_config" type="object">
          Specifies the frequency and polarization (e.g., RIGHT_HAND or LEFT_HAND) for the uplink radio signal transmitted from the ground station antenna. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="center_frequency" type="object">
              Specifies the center frequency of the uplink signal, containing a numeric value and a unit (GHz or MHz), used for the antenna uplink spectrum configuration. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="units" type="string">
                  The unit of measurement for the uplink center frequency value, such as GHz or MHz. Optional. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="value" type="number">
                  The numeric value of the center frequency for the antenna uplink spectrum, interpreted in the units defined by the sibling `units` field (e.g., GHz or MHz). Optional. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="polarization" type="string">
              Specifies the polarization of the uplink signal, which can be either RIGHT_HAND or LEFT_HAND. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="target_eirp" type="object">
          Specifies the target effective isotropic radiated power (EIRP) for the uplink transmission, defined as an object containing the numeric value and its units (e.g., dBW). Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="units" type="string">
              The unit of measurement for the target effective isotropic radiated power (EIRP) of the uplink, typically dBW. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="value" type="number">
              The target effective isotropic radiated power (EIRP) in decibel-watts (dBW) for the ground station's uplink antenna beam. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transmit_disabled" type="bool">
          Determines whether the antenna's uplink transmission is disabled; when set to true, the uplink transmitter is turned off so no signal is sent. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dataflow_endpoint_config" type="object">
      Provides the dataflow endpoint configuration for the Ground Station config, identifying the named endpoint (with its socket address and port) to use for data exchange. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dataflow_endpoint_name" type="string">
          The name of the dataflow endpoint that this Ground Station configuration references, identifying where data is sent or received for the dataflow endpoint config. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="dataflow_endpoint_region" type="string">
          Specifies the AWS region in which the dataflow endpoint is located. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="s3_recording_config" type="object">
      Specifies the Amazon S3 bucket and prefix where the Ground Station service stores recorded antenna data when this config is used for S3 recording. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="bucket_arn" type="string">
          The ARN of the S3 bucket where AWS Ground Station stores the recorded data for the S3 recording config. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="prefix" type="string">
          The S3 key prefix under which recorded data files are stored in the bucket, allowing you to organize output files. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="role_arn" type="string">
          The ARN of the IAM role that AWS Ground Station assumes to write antenna downlink data to the associated S3 bucket. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="telemetry_sink_config" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="telemetry_sink_data" type="object" required>
          The telemetry_sink_data object specifies the dataflow endpoint ARN to which the Ground Station telemetry sink forwards satellite telemetry data. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="kinesis_data_stream_data" type="object">
              Specifies the Kinesis Data Stream that serves as the telemetry sink destination, defining where Ground Station telemetry data is delivered for the telemetry sink configuration. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="kinesis_data_stream_arn" type="string" required>
                  The Amazon Resource Name (ARN) of the Kinesis data stream that receives telemetry data from the ground station config. Required. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="kinesis_role_arn" type="string" required>
                  The ARN of the IAM role that AWS Ground Station assumes to write telemetry data to the specified Kinesis Data Stream. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="telemetry_sink_type" type="string" required>
          Specifies the type of telemetry sink used to deliver ground station telemetry data, distinguishing between sink types such as S3 bucket storage and agent-based streaming. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tracking_config" type="object">
      Defines the tracking behavior for the ground station antenna, including the autotrack setting that controls whether automatic satellite tracking is required, preferred, or removed. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="autotrack" type="string">
          Sets the autotrack mode for the ground station antenna, determining whether automatic satellite tracking is disabled (NO_CONTACT), preferred (PREFERRED), or required (REQUIRED) during a contact. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uplink_echo_config" type="object">
      Configures the uplink echo feature for the ground station config, which echoes the uplink signal back for testing and includes the antenna uplink config ARN and an enabled flag. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="antenna_uplink_config_arn" type="string">
          The Amazon Resource Name (ARN) of the antenna uplink config whose transmitted signal is echoed back for the uplink echo function in this Ground Station config. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          When true, enables the Ground Station antenna to receive and downlink echoes of the uplink signal for testing or observation purposes. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The user-defined name assigned to this AWS Ground Station config, used to identify it within the service. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the AWS Ground Station Config resource, used to label and categorize the configuration with custom metadata such as environment or purpose. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a key-value tag attached to the AWS Ground Station Config resource, used to categorize or identify the configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this AWS Ground Station config. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="type" type="string">
  The type of the Ground Station config, such as 'antenna-downlink' or 'tracking', which is automatically derived from the configuration data provided in the resource. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
