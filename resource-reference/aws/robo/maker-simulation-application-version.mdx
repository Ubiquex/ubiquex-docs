---
title: "aws_robo_maker_simulation_application_version"
description: "`aws_robo_maker_simulation_application_version` published a real, immutable version of an `aws_robo_maker_simulation_application`."
---

This describes the now fully discontinued AWS RoboMaker service, which ended support September 10, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	robo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/robo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker simulation application version"})

    		ubx.Resource(robo.MakerSimulationApplicationVersion, "example", robo.MakerSimulationApplicationVersionConfig{
    			Application:       "example",
    			CurrentRevisionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerSimulationApplicationVersion } from "./local-sdk/aws/sdk/typescript/aws/robo/maker_simulation_application_version.ts";

    export default stack("example", () => {
      intent({ summary: "example own maker simulation application version" });

      resource(MakerSimulationApplicationVersion, "example", {
        application: "example",
        currentRevisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.robo import MakerSimulationApplicationVersion, MakerSimulationApplicationVersionConfig

    def describe():
        ubx.intent("example own maker simulation application version")

        ubx.resource(MakerSimulationApplicationVersion, "example", MakerSimulationApplicationVersionConfig(
            application="example",
            current_revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_robo_maker_simulation_application_version, with
    application example, current revision id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application" type="string" required>
  The name or Amazon Resource Name (ARN) of the RoboMaker simulation application for which this version is created. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="current_revision_id" type="string">
  The revision ID of robot application. Optional.
</ResponseField>

## Output properties

<ResponseField name="application_version" type="string">
  The version number (e.g., '1', '2') assigned to the newly created RoboMaker simulation application version, returned by AWS after creation. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) assigned by AWS to the newly created simulation application version, uniquely identifying it within the account and region. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
