---
title: "aws_robo_maker_robot_application_version"
description: "`aws_robo_maker_robot_application_version` published a real, immutable version of an `aws_robo_maker_robot_application`."
---

This describes the now fully discontinued AWS RoboMaker service, which ended support September 10, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	robo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/robo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own maker robot application version"})

    		ubx.Resource(robo.MakerRobotApplicationVersion, "ci-runner", robo.MakerRobotApplicationVersionConfig{
    			Application:       "example",
    			CurrentRevisionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerRobotApplicationVersion } from "./local-sdk/aws/sdk/typescript/aws/robo/maker_robot_application_version.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own maker robot application version" });

      resource(MakerRobotApplicationVersion, "ci-runner", {
        application: "example",
        currentRevisionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.robo import MakerRobotApplicationVersion, MakerRobotApplicationVersionConfig

    def describe():
        ubx.intent("payments own maker robot application version")

        ubx.resource(MakerRobotApplicationVersion, "ci-runner", MakerRobotApplicationVersionConfig(
            application="example",
            current_revision_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_robo_maker_robot_application_version, with application
    example, current revision id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application" type="string" required>
  The Amazon Resource Name (ARN) or name of the AWS RoboMaker robot application for which this version is created. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="current_revision_id" type="string">
  The revision ID of robot application. Optional.
</ResponseField>

## Output properties

<ResponseField name="application_version" type="string">
  The version of the robot application, automatically generated by AWS RoboMaker when the version is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="arn" type="string">
  The unique Amazon Resource Name (ARN) that identifies this RoboMaker robot application version. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
