---
title: "aws_robo_maker_fleet"
description: "`aws_robo_maker_fleet` created a real logical grouping of physical robots that `aws_robo_maker_robot_application`s were deployed to together in AWS..."
---

`aws_robo_maker_fleet` created a real logical grouping of physical robots that `aws_robo_maker_robot_application`s were deployed to together in AWS RoboMaker. AWS RoboMaker is now fully discontinued: AWS ended support on September 10, 2025, a date nearly a year in the past, steering customers toward AWS Batch for containerized simulation workloads instead; the project's own GitHub organization was archived in July 2026. This resource describes a service no longer operable rather than a current one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	robo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/robo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own maker fleet"})

    		ubx.Resource(robo.MakerFleet, "ci-runner", robo.MakerFleetConfig{
    			Name: "ci-runner",
    			Tags: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerFleet } from "./local-sdk/aws/sdk/typescript/aws/robo/maker_fleet.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own maker fleet" });

      resource(MakerFleet, "ci-runner", {
        name: "ci-runner",
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.robo import MakerFleet, MakerFleetConfig

    def describe():
        ubx.intent("payments own maker fleet")

        ubx.resource(MakerFleet, "ci-runner", MakerFleetConfig(
            name="ci-runner",
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_robo_maker_fleet called "ci-runner", with tags
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="name" type="string">
  The name of the fleet. Optional.
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the RoboMaker fleet, automatically assigned by AWS when the fleet is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
