---
title: "aws_robo_maker_simulation_application"
description: "`aws_robo_maker_simulation_application` defined a real simulation environment, its code and rendering engine bundle..."
---

`aws_robo_maker_simulation_application` defined a real simulation environment, its code and rendering engine bundle, that AWS RoboMaker ran robotics applications against virtually before deploying to physical hardware. This describes the now fully discontinued AWS RoboMaker service, which ended support September 10, 2025.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	robo "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/robo"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own maker simulation application"})

    		ubx.Resource(robo.MakerSimulationApplication, "example", robo.MakerSimulationApplicationConfig{
    			RobotSoftwareSuite:      map[string]any{"name": "example-name"},
    			SimulationSoftwareSuite: map[string]any{"name": "example-name"},
    			Name:                    "example",
    			CurrentRevisionId:       "example",
    			Environment:             "example",
    			RenderingEngine:         map[string]any{"name": "example-name"},
    			Sources:                 []map[string]any{{"architecture": "example"}},
    			Tags:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MakerSimulationApplication } from "@ubx/sdk-aws/aws/robo/maker_simulation_application";

    export default stack("example", () => {
      intent({ summary: "example own maker simulation application" });

      resource(MakerSimulationApplication, "example", {
        robotSoftwareSuite: { name: "example-name" },
        simulationSoftwareSuite: { name: "example-name" },
        name: "example",
        currentRevisionId: "example",
        environment: "example",
        renderingEngine: { name: "example-name" },
        sources: [{ architecture: "example" }],
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.robo import MakerSimulationApplication, MakerSimulationApplicationConfig

    def describe():
        ubx.intent("example own maker simulation application")

        ubx.resource(MakerSimulationApplication, "example", MakerSimulationApplicationConfig(
            robot_software_suite={"name": "example-name"},
            simulation_software_suite={"name": "example-name"},
            name="example",
            current_revision_id="example",
            environment="example",
            rendering_engine={"name": "example-name"},
            sources=[{"architecture": "example"}],
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_robo_maker_simulation_application called "example",
    with robot software suite a set value, simulation software suite
    a set value, current revision id example, environment example,
    rendering engine a set value, sources a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="current_revision_id" type="string">
  The current revision id. Optional.
</ResponseField>

<ResponseField name="environment" type="string">
  The URI of the Docker image for the robot application. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the simulation application. Optional.
</ResponseField>

<ResponseField name="rendering_engine" type="object">
  Information about a rendering engine. Optional.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The name of the rendering engine. Required.
    </ResponseField>
    <ResponseField name="version" type="string" required>
      The version of the rendering engine. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="robot_software_suite" type="object" required>
  Information about a robot software suite. Required.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The name of the robot software suite. Required.
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the robot software suite. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="simulation_software_suite" type="object" required>
  Information about a simulation software suite. Required.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      The name of the simulation software suite. Required.
    </ResponseField>
    <ResponseField name="version" type="string">
      The version of the simulation software suite. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sources" type="list(object)">
  The sources of the simulation application. Optional.
  <Expandable title="properties">
    <ResponseField name="architecture" type="string">
      The target processor architecture (X86_64 or ARM64) for the simulation application source bundle, used to match the application with the correct robot simulator architecture. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="s3_bucket" type="string">
      The name of the Amazon S3 bucket where the robot application source bundle is stored for this simulation application. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="s3_key" type="string">
      The S3 object key of the robot application bundle stored in the bucket specified by the source's s3_bucket, which is a required component of the source configuration for the simulation application. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  A key-value pair to associate with a resource. Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this RoboMaker simulation application. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
