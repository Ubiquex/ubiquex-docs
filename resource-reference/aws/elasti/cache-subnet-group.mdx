---
title: "aws_elasti_cache_subnet_group"
description: "`aws_elasti_cache_subnet_group` defines the real VPC subnets an `aws_elasti_cache_cache_cluster` or `aws_elasti_cache_replication_group`'s nodes..."
---

`aws_elasti_cache_subnet_group` defines the real VPC subnets an `aws_elasti_cache_cache_cluster` or `aws_elasti_cache_replication_group`'s nodes are actually placed into, required network placement before either can be created.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	elasti "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/elasti"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cache subnet group"})

    		ubx.Resource(elasti.CacheSubnetGroup, "ci-runner", elasti.CacheSubnetGroupConfig{
    			Description:          "Managed by ubx.",
    			SubnetIds:            []string{"example"},
    			CacheSubnetGroupName: "example-cache-subnet-group-name",
    			Tags:                 []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { CacheSubnetGroup } from "./local-sdk/aws/sdk/typescript/aws/elasti/cache_subnet_group.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own cache subnet group" });

      resource(CacheSubnetGroup, "ci-runner", {
        description: "Managed by ubx.",
        subnetIds: ["example"],
        cacheSubnetGroupName: "example-cache-subnet-group-name",
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.elasti import CacheSubnetGroup, CacheSubnetGroupConfig

    def describe():
        ubx.intent("payments own cache subnet group")

        ubx.resource(CacheSubnetGroup, "ci-runner", CacheSubnetGroupConfig(
            description="Managed by ubx.",
            subnet_ids=["example"],
            cache_subnet_group_name="example-cache-subnet-group-name",
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_elasti_cache_subnet_group, with description Managed by
    ubx., subnet ids a set value, cache subnet group name
    example-cache-subnet-group-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="cache_subnet_group_name" type="string">
  The name for the cache subnet group. This value is stored as a lowercase string. Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  The description for the cache subnet group. Required.
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)" required>
  The EC2 subnet IDs for the cache subnet group. Required.
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value tag objects to associate with the ElastiCache subnet group, used for organizing, identifying, and allocating costs for the resource. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The tag key assigned to the ElastiCache subnet group, used to categorize and manage the resource through AWS tagging features. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Defines the value of a user-defined tag assigned to the ElastiCache subnet group, used for metadata and resource organization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
