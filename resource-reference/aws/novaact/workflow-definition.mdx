---
title: "aws_nova_act_workflow_definition"
description: "`aws_nova_act_workflow_definition` defines a real, automated browser workflow, combining natural language instructions with Python code..."
---

`aws_nova_act_workflow_definition` defines a real, automated browser workflow, combining natural language instructions with Python code, for Amazon Nova Act, an AWS service for building and deploying AI agents that reliably automate production UI workflows, form-filling, search and extraction, shopping and booking, and QA testing, at scale.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	novaact "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/novaact"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own workflow definition"})

    		ubx.Resource(novaact.WorkflowDefinition, "example", novaact.WorkflowDefinitionConfig{
    			Name:         "example",
    			Description:  "Managed by ubx.",
    			ExportConfig: map[string]any{"s3_bucket_name": "example-s3-bucket-name"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkflowDefinition } from "@ubx/sdk-aws/aws/novaact/workflow_definition";

    export default stack("example", () => {
      intent({ summary: "example own workflow definition" });

      resource(WorkflowDefinition, "example", {
        name: "example",
        description: "Managed by ubx.",
        exportConfig: { s3BucketName: "example-s3-bucket-name" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.novaact import WorkflowDefinition, WorkflowDefinitionConfig
    from ubx.aws.novaact.workflow_definition import WorkflowDefinition_ExportConfig

    def describe():
        ubx.intent("example own workflow definition")

        ubx.resource(WorkflowDefinition, "example", WorkflowDefinitionConfig(
            name="example",
            description="Managed by ubx.",
            export_config=WorkflowDefinition_ExportConfig(s3_bucket_name="example-s3-bucket-name"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_nova_act_workflow_definition called "example", with
    description Managed by ubx., export config a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  An optional description of the workflow definition's purpose and functionality. Optional.
</ResponseField>

<ResponseField name="export_config" type="object">
  Configuration settings for exporting workflow execution data and logs to Amazon S3. Optional.
  <Expandable title="properties">
    <ResponseField name="s3_bucket_name" type="string" required>
      The name of the Amazon S3 bucket for exporting workflow data. Required.
    </ResponseField>
    <ResponseField name="s3_key_prefix" type="string">
      An optional prefix for Amazon S3 object keys to organize exported data. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the workflow definition. Must be unique within your account and region. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the workflow definition. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp when the workflow definition was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The current status of the workflow definition. Computed by AWS after `ship`.
</ResponseField>
