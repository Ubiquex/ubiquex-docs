---
title: "aws_quick_sight_theme"
description: "`aws_quick_sight_theme` defines a real, reusable set of colors, fonts, and layout options that can be applied to dashboards in QuickSight/Amazon Quick..."
---

`aws_quick_sight_theme` defines a real, reusable set of colors, fonts, and layout options that can be applied to dashboards in QuickSight/Amazon Quick, so visual styling stays consistent without configuring it dashboard by dashboard.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sight theme"})

    		ubx.Resource(quick.SightTheme, "ci-runner", quick.SightThemeConfig{
    			AwsAccountId:       "example",
    			BaseThemeId:        "example",
    			Configuration:      map[string]any{"data_color_palette": map[string]any{"colors": []string{"example"}}},
    			Name:               "ci-runner",
    			ThemeId:            "example",
    			Permissions:        []map[string]any{{"actions": []string{"example"}}},
    			Tags:               []map[string]any{{"key": "example"}},
    			VersionDescription: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightTheme } from "./local-sdk/aws/sdk/typescript/aws/quick/sight_theme.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sight theme" });

      resource(SightTheme, "ci-runner", {
        awsAccountId: "example",
        baseThemeId: "example",
        configuration: { dataColorPalette: { colors: ["example"] } },
        name: "ci-runner",
        themeId: "example",
        permissions: [{ actions: ["example"] }],
        tags: [{ key: "example" }],
        versionDescription: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.quick import SightTheme, SightThemeConfig

    def describe():
        ubx.intent("payments own sight theme")

        ubx.resource(SightTheme, "ci-runner", SightThemeConfig(
            aws_account_id="example",
            base_theme_id="example",
            configuration={"data_color_palette": {"colors": ["example"]}},
            name="ci-runner",
            theme_id="example",
            permissions=[{"actions": ["example"]}],
            tags=[{"key": "example"}],
            version_description="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_theme called "ci-runner", with aws account
    id example, base theme id example, configuration a set value,
    theme id example, permissions a set value, tags a set value,
    version description example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string" required>
  The AWS account ID where the QuickSight theme is created, scoping the theme resource to that specific account. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="base_theme_id" type="string" required>
  The ID of the base theme that this theme inherits its default styles from, such as MIDNIGHT or CLASSIC. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="configuration" type="object" required>
  &lt;p&gt;The theme configuration. This configuration contains all of the display properties for a theme.&lt;/p&gt; Required.
  <Expandable title="properties">
    <ResponseField name="data_color_palette" type="object">
      &lt;p&gt;The theme colors that are used for data colors in charts. The colors description is a hexadecimal color code that consists of six alphanumerical characters, prefixed with &lt;code&gt;#&lt;/code&gt;, for example #37BFF5. &lt;/p&gt; Optional.
      <Expandable title="properties">
        <ResponseField name="colors" type="list(string)">
          &lt;p&gt;The hexadecimal codes for the colors.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="empty_fill_color" type="string">
          &lt;p&gt;The hexadecimal code of a color that applies to charts where a lack of data is highlighted.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="min_max_gradient" type="list(string)">
          &lt;p&gt;The minimum and maximum hexadecimal codes that describe a color gradient. &lt;/p&gt; Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sheet" type="object">
      &lt;p&gt;The theme display options for sheets. &lt;/p&gt; Optional.
      <Expandable title="properties">
        <ResponseField name="tile" type="object">
          &lt;p&gt;Display options related to tiles on a sheet.&lt;/p&gt; Optional.
          <Expandable title="properties">
            <ResponseField name="border" type="object">
              &lt;p&gt;The display options for tile borders for visuals.&lt;/p&gt; Optional.
              <Expandable title="properties">
                <ResponseField name="show" type="bool">
                  &lt;p&gt;The option to enable display of borders for visuals.&lt;/p&gt; Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="tile_layout" type="object">
          &lt;p&gt;The display options for the layout of tiles on a sheet.&lt;/p&gt; Optional.
          <Expandable title="properties">
            <ResponseField name="gutter" type="object">
              &lt;p&gt;The display options for gutter spacing between tiles on a sheet.&lt;/p&gt; Optional.
              <Expandable title="properties">
                <ResponseField name="show" type="bool">
                  &lt;p&gt;This Boolean value controls whether to display a gutter space between sheet tiles. &lt;/p&gt; Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="margin" type="object">
              &lt;p&gt;The display options for margins around the outside edge of sheets.&lt;/p&gt; Optional.
              <Expandable title="properties">
                <ResponseField name="show" type="bool">
                  &lt;p&gt;This Boolean value controls whether to display sheet margins.&lt;/p&gt; Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="typography" type="object">
      Specifies the typography configuration for the theme, including the font families that can be used in visualizations. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="font_families" type="list(object)">
          Specifies the list of font families used in the theme's typography, where each item is an object that identifies a font by name for use in QuickSight visuals. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="font_family" type="string">
              Specifies the name of a font family (such as 'Arial' or 'Roboto') to apply to text in the Amazon QuickSight theme's typography settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="uicolor_palette" type="object">
      &lt;p&gt;The theme colors that apply to UI and to charts, excluding data colors. The colors description is a hexadecimal color code that consists of six alphanumerical characters, prefixed with &lt;code&gt;#&lt;/code&gt;, for example #37BFF5. For more information, see &lt;a href="https://docs.aws.amazon.com/quicksight/latest/user/themes-in-quicksight.html"&gt;Using Themes in Amazon QuickSight&lt;/a&gt; in the &lt;i&gt;Amazon QuickSight User Guide.&lt;/i&gt; &lt;/p&gt; Optional.
      <Expandable title="properties">
        <ResponseField name="accent" type="string">
          &lt;p&gt;This color is that applies to selected states and buttons.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="accent_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any text or other elements that appear over the accent color.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="danger" type="string">
          &lt;p&gt;The color that applies to error messages.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="danger_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any text or other elements that appear over the error color.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="dimension" type="string">
          &lt;p&gt;The color that applies to the names of fields that are identified as dimensions.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="dimension_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any text or other elements that appear over the dimension color.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="measure" type="string">
          &lt;p&gt;The color that applies to the names of fields that are identified as measures.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="measure_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any text or other elements that appear over the measure color.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="primary_background" type="string">
          &lt;p&gt;The background color that applies to visuals and other high emphasis UI.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="primary_foreground" type="string">
          &lt;p&gt;The color of text and other foreground elements that appear over the primary background regions, such as grid lines, borders, table banding, icons, and so on.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="secondary_background" type="string">
          &lt;p&gt;The background color that applies to the sheet background and sheet controls.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="secondary_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any sheet title, sheet control text, or UI that appears over the secondary background.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="success" type="string">
          &lt;p&gt;The color that applies to success messages, for example the check mark for a successful download.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="success_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any text or other elements that appear over the success color.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="warning" type="string">
          &lt;p&gt;This color that applies to warning and informational messages.&lt;/p&gt; Optional.
        </ResponseField>
        <ResponseField name="warning_foreground" type="string">
          &lt;p&gt;The foreground color that applies to any text or other elements that appear over the warning color.&lt;/p&gt; Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The display name of the QuickSight theme, used to identify it in the QuickSight console and API. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  Specifies the list of access permissions for the theme, where each item grants a principal (user, group, or role ARN) a set of allowed actions such as quicksight:DescribeTheme and quicksight:UpdateTheme. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="actions" type="list(string)">
      Specifies the list of QuickSight actions (e.g., 'quicksight:DescribeTheme', 'quicksight:UpdateTheme') that the principal is allowed to perform on the theme. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="principal" type="string">
      The IAM identity (ARN or name) that is granted the specified actions on the Amazon QuickSight theme. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value tag pairs to attach to the QuickSight theme for resource management and categorization. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag attached to the QuickSight theme, used to categorize and manage the theme resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag assigned to the Amazon QuickSight theme, used to store arbitrary metadata such as environment or owner. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="theme_id" type="string" required>
  The unique identifier for the theme, which must be unique within an AWS account and is used to reference the theme in other QuickSight resources. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="version_description" type="string">
  Specifies the description of the theme version, providing context about the changes or purpose of this iteration of the QuickSight theme. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) of the theme.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  &lt;p&gt;The date and time that the theme was created.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  &lt;p&gt;The date and time that the theme was last updated.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="version" type="object">
  &lt;p&gt;A version of a theme.&lt;/p&gt; Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="arn" type="string">
      &lt;p&gt;The Amazon Resource Name (ARN) of the resource.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="base_theme_id" type="string">
      &lt;p&gt;The Amazon QuickSight-defined ID of the theme that a custom theme inherits from. All themes initially inherit from a default Amazon QuickSight theme.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="configuration" type="object">
      &lt;p&gt;The theme configuration. This configuration contains all of the display properties for a theme.&lt;/p&gt; Optional.
      <Expandable title="properties">
        <ResponseField name="data_color_palette" type="object">
          &lt;p&gt;The theme colors that are used for data colors in charts. The colors description is a hexadecimal color code that consists of six alphanumerical characters, prefixed with &lt;code&gt;#&lt;/code&gt;, for example #37BFF5. &lt;/p&gt; Optional.
          <Expandable title="properties">
            <ResponseField name="colors" type="list(string)">
              &lt;p&gt;The hexadecimal codes for the colors.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="empty_fill_color" type="string">
              &lt;p&gt;The hexadecimal code of a color that applies to charts where a lack of data is highlighted.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="min_max_gradient" type="list(string)">
              &lt;p&gt;The minimum and maximum hexadecimal codes that describe a color gradient. &lt;/p&gt; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="sheet" type="object">
          &lt;p&gt;The theme display options for sheets. &lt;/p&gt; Optional.
          <Expandable title="properties">
            <ResponseField name="tile" type="object">
              &lt;p&gt;Display options related to tiles on a sheet.&lt;/p&gt; Optional.
              <Expandable title="properties">
                <ResponseField name="border" type="object">
                  &lt;p&gt;The display options for tile borders for visuals.&lt;/p&gt; Optional.
                  <Expandable title="properties">
                    <ResponseField name="show" type="bool">
                      &lt;p&gt;The option to enable display of borders for visuals.&lt;/p&gt; Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="tile_layout" type="object">
              &lt;p&gt;The display options for the layout of tiles on a sheet.&lt;/p&gt; Optional.
              <Expandable title="properties">
                <ResponseField name="gutter" type="object">
                  &lt;p&gt;The display options for gutter spacing between tiles on a sheet.&lt;/p&gt; Optional.
                  <Expandable title="properties">
                    <ResponseField name="show" type="bool">
                      &lt;p&gt;This Boolean value controls whether to display a gutter space between sheet tiles. &lt;/p&gt; Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="margin" type="object">
                  &lt;p&gt;The display options for margins around the outside edge of sheets.&lt;/p&gt; Optional.
                  <Expandable title="properties">
                    <ResponseField name="show" type="bool">
                      &lt;p&gt;This Boolean value controls whether to display sheet margins.&lt;/p&gt; Optional.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="typography" type="object">
          Defines the typography configuration for the QuickSight theme, including the font families and variants used for text elements in visuals and the interface. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="font_families" type="list(object)">
              Specifies a list of font family objects, each containing the name of a font family (e.g., 'Arial', 'Times New Roman') to be used in the theme's typography. Optional. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="font_family" type="string">
                  The name of the font family (e.g., 'Arial' or 'Times New Roman') that is applied to the QuickSight theme's typography configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="uicolor_palette" type="object">
          &lt;p&gt;The theme colors that apply to UI and to charts, excluding data colors. The colors description is a hexadecimal color code that consists of six alphanumerical characters, prefixed with &lt;code&gt;#&lt;/code&gt;, for example #37BFF5. For more information, see &lt;a href="https://docs.aws.amazon.com/quicksight/latest/user/themes-in-quicksight.html"&gt;Using Themes in Amazon QuickSight&lt;/a&gt; in the &lt;i&gt;Amazon QuickSight User Guide.&lt;/i&gt; &lt;/p&gt; Optional.
          <Expandable title="properties">
            <ResponseField name="accent" type="string">
              &lt;p&gt;This color is that applies to selected states and buttons.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="accent_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any text or other elements that appear over the accent color.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="danger" type="string">
              &lt;p&gt;The color that applies to error messages.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="danger_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any text or other elements that appear over the error color.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="dimension" type="string">
              &lt;p&gt;The color that applies to the names of fields that are identified as dimensions.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="dimension_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any text or other elements that appear over the dimension color.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="measure" type="string">
              &lt;p&gt;The color that applies to the names of fields that are identified as measures.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="measure_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any text or other elements that appear over the measure color.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="primary_background" type="string">
              &lt;p&gt;The background color that applies to visuals and other high emphasis UI.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="primary_foreground" type="string">
              &lt;p&gt;The color of text and other foreground elements that appear over the primary background regions, such as grid lines, borders, table banding, icons, and so on.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="secondary_background" type="string">
              &lt;p&gt;The background color that applies to the sheet background and sheet controls.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="secondary_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any sheet title, sheet control text, or UI that appears over the secondary background.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="success" type="string">
              &lt;p&gt;The color that applies to success messages, for example the check mark for a successful download.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="success_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any text or other elements that appear over the success color.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="warning" type="string">
              &lt;p&gt;This color that applies to warning and informational messages.&lt;/p&gt; Optional.
            </ResponseField>
            <ResponseField name="warning_foreground" type="string">
              &lt;p&gt;The foreground color that applies to any text or other elements that appear over the warning color.&lt;/p&gt; Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_time" type="string">
      &lt;p&gt;The date and time that this theme version was created.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="description" type="string">
      &lt;p&gt;The description of the theme.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="errors" type="list(object)">
      &lt;p&gt;Errors associated with the theme.&lt;/p&gt; Optional.
      <Expandable title="properties">
        <ResponseField name="message" type="string">
          The error message describing why a specific theme version could not be created or updated in Amazon QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of error that occurred during the creation or update of a QuickSight theme version, indicating the nature of the failure. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="status" type="string">
      Status of the theme version, representing the current state of creation or update (e.g., CREATION_IN_PROGRESS, CREATION_SUCCESSFUL, CREATION_FAILED, UPDATE_IN_PROGRESS, UPDATE_SUCCESSFUL, UPDATE_FAILED). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="version_number" type="number">
      &lt;p&gt;The version number of the theme.&lt;/p&gt; Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
