---
title: "aws_quick_sight_data_set"
description: "`aws_quick_sight_data_set` defines a real, queryable dataset, built from one or more `aws_quick_sight_data_source`s with its own joins..."
---

`aws_quick_sight_data_set` defines a real, queryable dataset, built from one or more `aws_quick_sight_data_source`s with its own joins, calculated fields, and row-level security, that dashboards and analyses in QuickSight/Amazon Quick are built against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sight data set"})

    		ubx.Resource(quick.SightDataSet, "example", quick.SightDataSetConfig{
    			Name:                       "example",
    			AwsAccountId:               "example",
    			ColumnGroups:               []map[string]any{{"geo_spatial_column_group": map[string]any{"name": "example-name"}}},
    			ColumnLevelPermissionRules: []map[string]any{{"column_names": []string{"example"}}},
    			DataPrepConfiguration:      map[string]any{"destination_table_map": "example"},
    			DataSetId:                  "example",
    			DataSetRefreshProperties:   map[string]any{"failure_configuration": map[string]any{"email_alert": map[string]any{"alert_status": "example"}}},
    			DataSetUsageConfiguration:  map[string]any{"disable_use_as_direct_query_source": true},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightDataSet } from "@ubx/sdk-aws/aws/quick/sight_data_set";

    export default stack("example", () => {
      intent({ summary: "example own sight data set" });

      resource(SightDataSet, "example", {
        name: "example",
        awsAccountId: "example",
        columnGroups: [{ geoSpatialColumnGroup: { name: "example-name" } }],
        columnLevelPermissionRules: [{ columnNames: ["example"] }],
        dataPrepConfiguration: { destinationTableMap: "example" },
        dataSetId: "example",
        dataSetRefreshProperties: {
          failureConfiguration: { emailAlert: { alertStatus: "example" } },
        },
        dataSetUsageConfiguration: { disableUseAsDirectQuerySource: true },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quick import SightDataSet, SightDataSetConfig

    def describe():
        ubx.intent("example own sight data set")

        ubx.resource(SightDataSet, "example", SightDataSetConfig(
            name="example",
            aws_account_id="example",
            column_groups=[{"geo_spatial_column_group": {"name": "example-name"}}],
            column_level_permission_rules=[{"column_names": ["example"]}],
            data_prep_configuration={"destination_table_map": "example"},
            data_set_id="example",
            data_set_refresh_properties={"failure_configuration": {"email_alert": {"alert_status": "example"}}},
            data_set_usage_configuration={"disable_use_as_direct_query_source": True},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_data_set called "example", with aws
    account id example, column groups a set value, column level
    permission rules a set value, data prep configuration a set
    value, data set id example, data set refresh properties a set
    value, data set usage configuration a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string">
  The AWS account ID that owns the data set, defaulting to the account used to make the request if not specified. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="column_groups" type="list(object)">
  &lt;p&gt;Groupings of columns that work together in certain Amazon QuickSight features. Currently, only geospatial hierarchy is supported.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="geo_spatial_column_group" type="object">
      Defines a geospatial column group in a QuickSight dataset, specifying which columns are treated as geographic data and the country code used for map rendering. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="columns" type="list(string)">
          The list of column names that are grouped together to form a geographical hierarchy (e.g., country, state, city) for mapping in QuickSight visualizations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="country_code" type="string">
          Specifies the two-letter country code (e.g., US, GB) that defines the geographic hierarchy and mapping for the columns in this geospatial column group in the QuickSight dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the geospatial column group, which uniquely identifies the group within the QuickSight data set. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="column_level_permission_rules" type="list(object)">
  &lt;p&gt;A set of one or more definitions of a &lt;code&gt; &lt;a href="https://docs.aws.amazon.com/quicksight/latest/APIReference/API_ColumnLevelPermissionRule.html"&gt;ColumnLevelPermissionRule&lt;/a&gt; &lt;/code&gt;.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="column_names" type="list(string)">
      The list of column names that this column-level permission rule applies to, controlling which columns are visible to the users or groups specified in the rule. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="principals" type="list(string)">
      The list of Amazon QuickSight user or group principal ARNs that this column-level permission rule applies to, granting them access to the specified columns. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_prep_configuration" type="object">
  Optional.
  <Expandable title="properties">
    <ResponseField name="destination_table_map" type="any" required>
      Determines the destination table names for the data set after the data prep transformations are applied, mapping each source table to its corresponding output table within the QuickSight data set. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="source_table_map" type="any" required>
      Defines a mapping of physical table IDs to their corresponding source table definitions, used during data preparation for the QuickSight dataset. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="transform_step_map" type="any" required>
      Defines the map of data preparation transform steps for the QuickSight dataset, where each entry maps a user-defined step name to a TransformStep object specifying the transform action (such as filter, project, or rename) to apply. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_set_id" type="string">
  The user-defined unique identifier for the QuickSight dataset within an AWS Region; if omitted, QuickSight generates one automatically. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="data_set_refresh_properties" type="object">
  &lt;p&gt;The refresh properties of a dataset.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="failure_configuration" type="object">
      Specifies the retry behavior when a QuickSight dataset refresh fails, defining the maximum number of retry attempts and the wait interval between retries. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="email_alert" type="object">
          Configures email notifications to specified recipients when a dataset refresh fails. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="alert_status" type="string">
              Indicates whether the email alert for data set refresh failure is enabled or disabled, with allowed values such as ENABLED or DISABLED. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="refresh_configuration" type="object">
      &lt;p&gt;The refresh configuration of a dataset.&lt;/p&gt; Optional.
      <Expandable title="properties">
        <ResponseField name="incremental_refresh" type="object" required>
          &lt;p&gt;The incremental refresh configuration for a dataset.&lt;/p&gt; Required.
          <Expandable title="properties">
            <ResponseField name="lookback_window" type="object" required>
              &lt;p&gt;The lookback window setup of an incremental refresh configuration.&lt;/p&gt; Required.
              <Expandable title="properties">
                <ResponseField name="column_name" type="string" required>
                  &lt;p&gt;The name of the lookback window column.&lt;/p&gt; Required.
                </ResponseField>
                <ResponseField name="size" type="number" required>
                  &lt;p&gt;The lookback window column size.&lt;/p&gt; Required.
                </ResponseField>
                <ResponseField name="size_unit" type="string" required>
                  Specifies the unit of measurement for the lookback window size (e.g., DAY or HOUR), which determines how much historical data is included in the incremental refresh window. Required. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_set_usage_configuration" type="object">
  &lt;p&gt;The usage configuration to apply to child datasets that reference this dataset as a source.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="disable_use_as_direct_query_source" type="bool">
      &lt;p&gt;An option that controls whether a child dataset of a direct query can use this dataset as a source.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="disable_use_as_imported_source" type="bool">
      &lt;p&gt;An option that controls whether a child dataset that's stored in QuickSight can use this dataset as a source.&lt;/p&gt; Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dataset_parameters" type="list(object)">
  &lt;p&gt;The parameter declarations of the dataset.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="date_time_dataset_parameter" type="object">
      Defines a date-time dataset parameter in Amazon QuickSight, specifying its ID, name, value type (single or multi-valued), optional time granularity, and default values for filtering dataset content. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="default_values" type="object">
          Specifies the default values for a date-time dataset parameter, which is a container that holds a list of timestamp strings to apply when the parameter is not explicitly provided. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="static_values" type="list(string)">
              Defines the list of static default date-time values (as strings) for a date time dataset parameter, used when no dynamic default value is provided for the QuickSight data set parameter. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="id" type="string">
          The unique identifier for the date-time dataset parameter within the QuickSight dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The unique name that identifies this date-time dataset parameter, used to reference the parameter within the dataset's queries and when configuring parameter controls. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="time_granularity" type="string">
          Specifies the granularity of the date-time dataset parameter (e.g., YEAR, MONTH, DAY, HOUR), which controls how the date-time values are interpreted or aggregated in QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value_type" type="string">
          Indicates the value type of the date-time dataset parameter, which can be 'SINGLE' for a single date-time value or 'MULTI_VALUED' for multiple date-time values, controlling how the parameter is used in dataset filters and queries. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="decimal_dataset_parameter" type="object">
      Defines a decimal (numeric) parameter for the QuickSight dataset, specifying its name and list of numeric values. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="default_values" type="object">
          Defines the default decimal values for the decimal dataset parameter, which are used when no parameter value is supplied during dataset or analysis operations. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="static_values" type="list(number)">
              The list of static decimal numbers used as the default values for this decimal dataset parameter when no value is provided by the user. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="id" type="string">
          The unique identifier for the decimal dataset parameter, used to reference this parameter within the QuickSight dataset and in dataset operations such as custom SQL or filters. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          A unique name that identifies this decimal dataset parameter within the dataset, which can be referenced in custom SQL or other dataset configurations. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value_type" type="string">
          Specifies whether the decimal dataset parameter accepts a single value or multiple values, using the enum values SINGLE_VALUED or MULTI_VALUED. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="integer_dataset_parameter" type="object">
      Defines an integer parameter for the QuickSight dataset, including its name, value type (single or multi-valued), and default values. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="default_values" type="object">
          The list of default integer values for the integer dataset parameter, used when a value is not supplied during dataset refresh or query runtime. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="static_values" type="list(number)">
              This field specifies a list of integer values that serve as the static default values for an integer dataset parameter in an Amazon QuickSight data set. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the integer dataset parameter, used to reference and bind the parameter value within the dataset's custom SQL or calculated fields. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value_type" type="string">
          Specifies whether the integer dataset parameter accepts a single value or multiple values, with valid values being SINGLE_VALUED or MULTI_VALUED. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="string_dataset_parameter" type="object">
      Defines a string dataset parameter for a QuickSight dataset, specifying its name and list of string values used in queries or filters. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="default_values" type="object">
          Specifies the default string values for this string dataset parameter, which are used when the parameter is not provided at analysis runtime. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="static_values" type="list(string)">
              Defines a list of static default values for a string dataset parameter, used as the parameter's default when no value is supplied at runtime. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by AWS.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name that identifies this QuickSight dataset string parameter, used to reference the parameter in dataset custom SQL (for example, $&#123;param_name&#125;) and dataset expressions. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="value_type" type="string">
          Specifies whether the string dataset parameter permits a single value ('SINGLE_VALUED') or multiple values ('MULTI_VALUED'). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="field_folders" type="any">
  Configures field folders, a map of folder names to objects listing the dataset columns that should be grouped under each folder, enabling organized browsing of fields in QuickSight. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="folder_arns" type="list(string)">
  &lt;p&gt;When you create the dataset, Amazon QuickSight adds the dataset to these folders.&lt;/p&gt; Optional.
</ResponseField>

<ResponseField name="import_mode" type="string">
  The import mode for the dataset, which determines whether data is stored in SPICE (in-memory) for fast analytics or queried directly from the underlying data source; valid values are SPICE and DIRECT_QUERY. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="ingestion_wait_policy" type="object">
  &lt;p&gt;Wait policy to use when creating/updating dataset. Default is to wait for SPICE ingestion to finish with timeout of 36 hours.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="ingestion_wait_time_in_hours" type="number">
      &lt;p&gt;The maximum time (in hours) to wait for Ingestion to complete. Default timeout is 36 hours. Applicable only when DataSetImportMode mode is set to SPICE and WaitForSpiceIngestion is set to true.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="wait_for_spice_ingestion" type="bool">
      &lt;p&gt;Wait for SPICE ingestion to finish to mark dataset creation/update successful. Default (true). Applicable only when DataSetImportMode mode is set to SPICE.&lt;/p&gt; Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="logical_table_map" type="any">
  Maps logical table names to their definitions, enabling custom data views over physical tables with transformations, joins, and filters. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  &lt;p&gt;The display name for the dataset.&lt;/p&gt; Optional.
</ResponseField>

<ResponseField name="performance_configuration" type="object">
  Defines performance settings for the dataset, specifically a list of unique key configurations used to optimize query performance. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="unique_keys" type="list(object)">
      Specifies a list of unique key configurations for the QuickSight dataset's performance configuration, where each unique key defines one or more column names that uniquely identify each row, enabling incremental data refresh and performance optimization. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="column_names" type="list(string)">
          The list of column names that together form a unique key for the dataset, used by QuickSight to identify unique rows for performance optimization and incremental data refresh. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  &lt;p&gt;A list of resource permissions on the dataset.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(string)">
      Specifies the list of QuickSight actions (e.g., 'quicksight:DescribeDataSet', 'quicksight:UpdateDataSet', 'quicksight:DeleteDataSet') allowed for the associated principal on the data set. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="principal" type="string">
      The ARN of the principal (user, group, or role) that the permissions entry grants or revokes actions on the dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="physical_table_map" type="any">
  Defines the physical tables backing the dataset, mapping each physical table identifier to its source definition (relational table, custom SQL, or S3 source). Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="row_level_permission_data_set" type="object">
  &lt;p&gt;Information about a dataset that contains permissions for row-level security (RLS). The permissions dataset maps fields to users or groups. For more information, see &lt;a href="https://docs.aws.amazon.com/quicksight/latest/user/restrict-access-to-a-data-set-using-row-level-security.html"&gt;Using Row-Level Security (RLS) to Restrict Access to a Dataset&lt;/a&gt; in the &lt;i&gt;Amazon QuickSight User Guide&lt;/i&gt;.&lt;/p&gt; &lt;p&gt;The option to deny permissions by setting &lt;code&gt;PermissionPolicy&lt;/code&gt; to &lt;code&gt;DENY_ACCESS&lt;/code&gt; is not supported for new RLS datasets.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="arn" type="string" required>
      &lt;p&gt;The Amazon Resource Name (ARN) of the dataset that contains permissions for RLS.&lt;/p&gt; Required.
    </ResponseField>
    <ResponseField name="format_version" type="string">
      The format version of the row-level security dataset, where VERSION_1 is the legacy format and VERSION_2 is the current format supporting additional features such as multiple role hierarchies. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="namespace" type="string">
      &lt;p&gt;The namespace associated with the dataset that contains permissions for RLS.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="permission_policy" type="string" required>
      Specifies whether the row-level permission dataset grants or denies access to the underlying data, with valid values of GRANT_ACCESS or DENY_ACCESS. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      Specifies whether the row-level permission dataset is currently enabled (ENABLED) or disabled (DISABLED) for the QuickSight data set. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="row_level_permission_tag_configuration" type="object">
  &lt;p&gt;The configuration of tags on a dataset to set row-level security. &lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="status" type="string">
      Indicates whether row-level security tag rules are enabled (ENABLED) or disabled (DISABLED) for the QuickSight dataset. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tag_rule_configurations" type="list(list(string))">
      &lt;p&gt;A list of tag configuration rules to apply to a dataset. All tag configurations have the OR condition. Tags within each tile will be joined (AND). At least one rule in this structure must have all tag values assigned to it to apply Row-level security (RLS) to the dataset.&lt;/p&gt; Optional.
    </ResponseField>
    <ResponseField name="tag_rules" type="list(object)" required>
      &lt;p&gt;A set of rules associated with row-level security, such as the tag names and columns that they are assigned to.&lt;/p&gt; Required.
      <Expandable title="properties">
        <ResponseField name="column_name" type="string">
          The name of the dataset column that this row-level permission tag rule uses to map tag values to rows for access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="match_all_value" type="string">
          The value that, when the tag key's value equals this string, grants access to all rows in the dataset (bypassing row-level permission filtering). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tag_key" type="string">
          Defines the tag key that QuickSight matches against a user's tag values to enforce row-level security for the dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="tag_multi_value_delimiter" type="string">
          The delimiter used to separate multiple values within a single tag rule's tag value for row-level permission tag configuration in this QuickSight dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="semantic_model_configuration" type="object">
  Configures the dataset's semantic model properties, providing lists of configured and unconfigured semantic model property configurations that map dataset columns to semantic types for Amazon QuickSight Q. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="semantic_metadata" type="list(object)">
      The semantic_metadata list defines the semantic entities and their field mappings for the QuickSight semantic model configuration, enabling natural language query and other semantic features on the data set. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="custom_instructions" type="list(object)">
          Specifies a list of custom instruction objects that provide additional business context and guidance to QuickSight's semantic model, enhancing natural language query interpretation for the data set. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="inline_custom_instruction" type="object">
              Contains the inline text of a custom instruction attached to the dataset's semantic metadata, providing explicit guidance to the QuickSight semantic model for interpreting or using the data. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="instruction_text" type="string">
                  The actual textual content of an inline custom instruction for QuickSight's semantic model, providing specific guidance on how data should be interpreted or processed in the data set. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="uploaded_document_metadata" type="object">
                  For an inline custom instruction in the QuickSight data set's semantic model, this object stores the metadata of a document uploaded to Amazon S3 that contains the instruction's content. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="name" type="string">
                      The name of the uploaded document that provides custom instruction content for the QuickSight data set's semantic model. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="object">
          The description field inside semantic_metadata provides a human-readable explanation of the semantic metadata entry, describing the purpose or meaning of the semantic type assigned to a data path in the QuickSight data set's semantic model. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="text" type="string">
              Specifies the free-form text description for a semantic metadata item (such as a column) in the QuickSight dataset's semantic model, providing human-readable business context. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="table_map" type="any">
      A map of table names to semantic model table definitions, configuring how each table in the QuickSight dataset is modeled for semantic model features such as natural language querying (Q) by defining measures, dimensions, and hierarchies. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  &lt;p&gt;Contains a map of the key-value pairs for the resource tag or tags assigned to the dataset.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Specifies the key of a tag applied to the QuickSight data set, enabling resource identification, categorization, and access control. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      Value of a tag applied to the Amazon QuickSight data set, used to categorize or filter the resource for management, access control, or cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="use_as" type="string">
  Determines whether the dataset is used as a visual or as a query in AWS QuickSight. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) of the resource.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="consumed_spice_capacity_in_bytes" type="number">
  &lt;p&gt;The amount of SPICE capacity used by this dataset. This is 0 if the dataset isn't imported into SPICE.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  &lt;p&gt;The time that this dataset was created.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  &lt;p&gt;The last time that this dataset was updated.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="output_columns" type="list(object)">
  &lt;p&gt;The list of columns after all transforms. These columns are available in templates, analyses, and dashboards.&lt;/p&gt; Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      A description of the output column, providing additional context about the data contained in that column within the QuickSight dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the output column, which defines the column heading in the resulting dataset schema for Amazon QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sub_type" type="string">
      The sub data type of the output column in an Amazon QuickSight data set, which further refines the column's data type, for instance indicating that a string column carries geospatial data. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      Specifies the data type of the output column (e.g., STRING, INTEGER, DECIMAL, DATETIME) in the QuickSight dataset output schema. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>
