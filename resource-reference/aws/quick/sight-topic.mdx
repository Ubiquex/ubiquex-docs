---
title: "aws_quick_sight_topic"
description: "`aws_quick_sight_topic` defines a real natural-language topic, an `aws_quick_sight_data_set` plus curated field synonyms and calculations..."
---

`aws_quick_sight_topic` defines a real natural-language topic, an `aws_quick_sight_data_set` plus curated field synonyms and calculations, that QuickSight/Amazon Quick's Q&A feature answers plain-English questions against.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sight topic"})

    		ubx.Resource(quick.SightTopic, "ci-runner", quick.SightTopicConfig{
    			Name:               "ci-runner",
    			AwsAccountId:       "example",
    			ConfigOptions:      map[string]any{"qbusiness_insights_enabled": true},
    			CustomInstructions: map[string]any{"custom_instructions_string": "example"},
    			DataSets:           []map[string]any{{"calculated_fields": []map[string]any{{"aggregation": "example"}}}},
    			Description:        "Managed by ubx.",
    			FolderArns:         []string{"example"},
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightTopic } from "./local-sdk/aws/sdk/typescript/aws/quick/sight_topic.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sight topic" });

      resource(SightTopic, "ci-runner", {
        name: "ci-runner",
        awsAccountId: "example",
        configOptions: { qbusinessInsightsEnabled: true },
        customInstructions: { customInstructionsString: "example" },
        dataSets: [{ calculatedFields: [{ aggregation: "example" }] }],
        description: "Managed by ubx.",
        folderArns: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.quick import SightTopic, SightTopicConfig

    def describe():
        ubx.intent("payments own sight topic")

        ubx.resource(SightTopic, "ci-runner", SightTopicConfig(
            name="ci-runner",
            aws_account_id="example",
            config_options={"qbusiness_insights_enabled": True},
            custom_instructions={"custom_instructions_string": "example"},
            data_sets=[{"calculated_fields": [{"aggregation": "example"}]}],
            description="Managed by ubx.",
            folder_arns=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_topic called "ci-runner", with aws account
    id example, config options a set value, custom instructions a set
    value, data sets a set value, description Managed by ubx., folder
    arns a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string">
  The AWS account ID that owns the QuickSight topic; if omitted, defaults to the account where the CloudFormation stack is running. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="config_options" type="object">
  The config_options property specifies topic-level configuration settings for an Amazon QuickSight topic, such as whether Q Business Insights is enabled. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="qbusiness_insights_enabled" type="bool">
      Enables Amazon Q Business insights for the topic, allowing natural-language queries to surface relevant insights from the topic's data model. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_instructions" type="object">
  &lt;p&gt;Instructions that provide additional guidance and context for response generation.&lt;/p&gt; Optional.
  <Expandable title="properties">
    <ResponseField name="custom_instructions_string" type="string" required>
      &lt;p&gt;A text field for providing additional guidance or context for response generation.&lt;/p&gt; Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_sets" type="list(object)">
  Configures the datasets that constitute the QuickSight topic, where each list item provides metadata (e.g., dataset ARN and column details) for a dataset included in the topic. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="calculated_fields" type="list(object)">
      Specifies the calculated fields (computed fields/expressions) defined for each dataset in the QuickSight topic, allowing formula-based transformations of the underlying data. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="aggregation" type="string">
          Specifies the default aggregation (such as SUM or AVERAGE) applied to the calculated field when it is used in visuals in the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="allowed_aggregations" type="list(string)">
          Specifies the list of aggregation functions (such as SUM, AVERAGE, MIN, MAX, COUNT, etc.) that are permitted on this calculated field within the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="calculated_field_description" type="string">
          Provides an optional user-defined description that explains the purpose or business meaning of the calculated field within the QuickSight topic's dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="calculated_field_name" type="string">
          The calculated field name is a unique, human-readable string that identifies the calculated field within the dataset, enabling references to it from other parts of the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="calculated_field_synonyms" type="list(string)">
          Specifies alternative names or synonyms for the calculated field so that QuickSight can match user natural-language queries to the field. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="cell_value_synonyms" type="list(object)">
          Defines a list of synonym mappings for cell values in this calculated field, allowing QuickSight to match alternative names for the same value when users ask natural language questions. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cell_value" type="string">
              The exact string value in the dataset that is represented by a list of synonyms, enabling QuickSight to group equivalent cell values under a single canonical term. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="synonyms" type="list(string)">
              Specifies the list of alternate terms that map to a specific cell value within a calculated field, enabling QuickSight's natural language queries to interpret those terms as referring to that value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="column_data_role" type="string">
          Specifies the data role of the calculated field in the QuickSight topic, indicating whether it is a dimension (DIMENSION) or a measure (MEASURE). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="comparative_order" type="object">
          Defines the comparison and ranking rules for this calculated field, including whether to use the default sort order, treat larger or smaller values as better, or specify an explicit list of values to control how they are ordered in QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="specifed_order" type="list(string)">
              Specifies the ordered list of discrete string values that define the custom comparative sort order for the calculated field within the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="treat_undefined_specified_values" type="string">
              For a calculated field's comparative ordering, this setting decides whether values that are not in the specified list are treated as the smallest (LEAST) or largest (MOST) when sorting. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use_ordering" type="string">
              Controls whether the comparative ordering settings (including custom column order and sort direction) defined in the parent comparative order object are applied to the calculated field. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_formatting" type="object">
          Configures the default formatting options (such as number, date, or currency settings) that determine how the calculated field's values are displayed in QuickSight visuals. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="display_format" type="string">
              Specifies how the calculated field's values are formatted for display in QuickSight, such as AUTO, NUMBER, CURRENCY, PERCENT, or DATE. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_format_options" type="object">
              Provides the detailed formatting options (e.g., decimal places, currency prefix/suffix, or date time patterns) that define how the calculated field's values are rendered under its default display format. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="blank_cell_format" type="string">
                  Specifies the text to display in cells that have no value for this calculated field in the QuickSight topic dataset, overriding the default blank cell representation. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="currency_symbol" type="string">
                  The currency symbol (e.g., '$' or '€') that QuickSight prepends to numeric values when the calculated field's default formatting uses a currency display format. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="date_format" type="string">
                  Specifies the format pattern (e.g., 'yyyy-MM-dd') that controls how date values are displayed for the calculated field when using the date display format, within the default formatting options of a dataset in an Amazon QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="decimal_separator" type="string">
                  Specifies the decimal separator (e.g., COMMA or PERIOD) to use when displaying the calculated field's formatted numeric values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fraction_digits" type="number">
                  Specifies the number of digits to display after the decimal point for numeric values in the calculated field's default formatting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="grouping_separator" type="string">
                  Specifies the grouping separator (e.g., comma, period, or space) used to group digits in the default numeric display format of the calculated field within the QuickSight topic's dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_format" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="prefix" type="string">
                      Specifies the text prefix (e.g., '-' or '(') that is prepended to negative numbers in the display format for this calculated field, when a custom negative format is defined. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="suffix" type="string">
                      The suffix string displayed at the end of a negative number in the calculated field's default number formatting, such as a closing parenthesis for accounting-style negative values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="prefix" type="string">
                  The prefix is a string prepended to the displayed value of a calculated field (e.g., a currency symbol like '$') when using a custom display format in QuickSight topics. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="suffix" type="string">
                  Specifies the text to append to the formatted value in a QuickSight calculated field's display format, such as a currency symbol or percentage sign. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unit_scaler" type="string">
                  Determines the scaling factor (e.g., thousands, millions, billions) applied to the display of numeric values for the calculated field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="use_blank_cell_format" type="bool">
                  Determines whether blank cells are formatted according to the blank cell format specified in the display format options for the calculated field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="use_grouping" type="bool">
                  When set to true, this option applies digit grouping separators (such as commas) to the numeric values of the calculated field in QuickSight displays. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disable_indexing" type="bool">
          When true, disables semantic search indexing for this calculated field in the QuickSight Topic, so it won't appear in natural language search or autocomplete but remains fully usable in analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="expression" type="string">
          The formula or expression used to define the calculated field's value within the QuickSight topic dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_included_in_topic" type="bool">
          Specifies whether the calculated field is included in the QuickSight topic, making it available for end users to use in their analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="never_aggregate_in_filter" type="bool">
          Specifies whether this calculated field should not be aggregated when used in a filter, giving raw values instead of summarized results. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="non_additive" type="bool">
          Specifies whether the calculated field is non-additive, meaning it cannot be summed across multiple records (for example, ratios or distinct counts) and requires special aggregation handling. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="not_allowed_aggregations" type="list(string)">
          Specifies a list of aggregate functions (such as SUM, AVERAGE, MIN, MAX) that are not allowed to be used with this calculated field when it is included in QuickSight analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="semantic_type" type="object">
          Specifies the semantic type for the calculated field, including its data type and optionally additional sub-type properties (such as country code), which controls how QuickSight interprets the field's values in analysis. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="falsey_cell_value" type="string">
              Specifies the cell value that QuickSight treats as false (falsey) for this calculated field's semantic type, enabling boolean interpretation of non-standard values such as '0' or 'No'. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="falsey_cell_value_synonyms" type="list(string)">
              Specifies a list of alternative string values that are treated as synonyms for the falsey cell value of this semantic type, so that any of these values in a cell are interpreted as false when evaluating the calculated field. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sub_type_name" type="string">
              Specifies a finer-grained classification for the calculated field's semantic type, such as 'YEAR' or 'MONTH' for a datetime semantic type, to refine how QuickSight interprets and displays the field. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="truthy_cell_value" type="string">
              Specifies the string value (e.g., 'true', 'yes', or '1') that QuickSight should treat as the truthy/true representation when interpreting values of this calculated field's semantic type. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="truthy_cell_value_synonyms" type="list(string)">
              Defines additional string values (besides the primary truthy cell value) that should be treated as true when interpreting this calculated field as a boolean semantic type in Amazon QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_name" type="string">
              Sets the semantic type name (e.g., YEAR, QUARTER, MONTH) that defines how QuickSight interprets the calculated field's values, typically enabling date-aware grouping or formatting within the topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_parameters" type="any">
              Defines key-value parameters for the semantic type of a calculated field, allowing customization such as date format or numeric precision. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="time_granularity" type="string">
          Specifies the time granularity (e.g., YEAR, MONTH, DAY) for the calculated field, determining how date/time values are grouped or aggregated in QuickSight analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="columns" type="list(object)">
      Defines the list of columns from each dataset that are included in the QuickSight topic, specifying their attributes and semantic roles (e.g., dimension or measure) for analysis. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="aggregation" type="string">
          Defines the default aggregation (such as SUM, AVERAGE, MIN, MAX) applied to this column within the QuickSight topic, which influences how it is aggregated in analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="allowed_aggregations" type="list(string)">
          Specifies the list of aggregate functions (e.g., SUM, AVERAGE, COUNT, MIN, MAX) that are allowed to be applied to this column in QuickSight topic analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="cell_value_synonyms" type="list(object)">
          Defines a list of synonym mappings for individual cell values in this column, allowing QuickSight to recognize alternative terms users may type when asking natural-language questions about these data values. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="cell_value" type="string">
              Specifies the actual column value that the associated synonyms refer to, allowing QuickSight to recognize alternative names for that cell value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="synonyms" type="list(string)">
              A list of alternative names (synonyms) for a specific cell value in the column, used to map those synonyms to the actual value for QuickSight natural language querying. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="column_data_role" type="string">
          Specifies whether the column in the QuickSight topic dataset acts as a dimension or a measure for analytical purposes. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="column_description" type="string">
          Provides descriptive text for a column within a QuickSight topic dataset, giving end users context about the column's meaning and purpose during data exploration. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="column_friendly_name" type="string">
          Specifies a human-readable display name for the column, used in place of the physical column name in QuickSight audiences. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="column_name" type="string">
          The name of the column in the dataset, used to identify this column when defining semantic rules and relationships in the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="column_synonyms" type="list(string)">
          Defines alternate names (synonyms) for the column, allowing users to reference it with those terms in QuickSight natural language queries. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="comparative_order" type="object">
          Defines the custom comparative order (e.g., an explicit ordering of values like high/medium/low) for this column in the QuickSight topic, controlling how its categories are sorted and compared in visualizations. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="specifed_order" type="list(string)">
              A list of column values that defines the exact sequence in which the column's values are ordered or displayed in QuickSight when the comparative order is set to a specified (custom) order. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="treat_undefined_specified_values" type="string">
              Determines whether values not in the comparative order's specified values are treated as least or most when sorting the column. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="use_ordering" type="string">
              Indicates whether the column's comparative ordering uses a custom-defined order from the parent 'ColumnOrder' list, with false meaning the default ordering is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_formatting" type="object">
          Specifies the default display formatting for the column, such as number, currency, date, or string formatting rules applied when the column is used in visuals. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="display_format" type="string">
              Determines the default display format for the column's values, controlling how numbers, currencies, percentages, or dates are rendered in QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="display_format_options" type="object">
              Defines format-specific configuration options (such as decimal places, negative value styling, or date/time patterns) that accompany the chosen display format for a column in the QuickSight topic's default formatting. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="blank_cell_format" type="string">
                  The custom string that is displayed in the column's cells when the value is null or blank, for example 'N/A'. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="currency_symbol" type="string">
                  The currency symbol (e.g., '$', '€') used when the column's display format is set to currency in QuickSight topic datasets. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="date_format" type="string">
                  Sets the custom date pattern (e.g., 'yyyy-MM-dd' or 'MM/dd/yyyy') used to display date values for the column when the default formatting is applied. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="decimal_separator" type="string">
                  Specifies the character used as the decimal separator when displaying numeric values in the topic column, such as a period or comma. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="fraction_digits" type="number">
                  Specifies the number of decimal places to display for numeric values when the column's default display format uses a fraction-based numeric format. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="grouping_separator" type="string">
                  Specifies the character used to group digits in numeric values (e.g., the thousands separator) when the column's default formatting is displayed in a QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="negative_format" type="object">
                  Specifies the prefix and suffix used to format negative numbers in the column when the display format is a numeric type, such as displaying them with a minus sign or surrounding them in parentheses. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="prefix" type="string">
                      Specifies the prefix string (e.g., '-') that is placed before negative values when this display format is applied in QuickSight. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="suffix" type="string">
                      Specifies the suffix string (e.g., ')' or ' CR') that is appended after a negative number when the negative format is applied to the column's display formatting in a QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="prefix" type="string">
                  Specifies the custom string that is placed before the displayed value of a column (e.g., a currency symbol or unit label) when the column's default display format is applied in an Amazon QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="suffix" type="string">
                  The text that appears immediately after the formatted column value in QuickSight, used to append units (e.g., 'kg') or other suffixes to numeric displays. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="unit_scaler" type="string">
                  Configures the unit scale applied to numeric values in the column's display format, allowing values to be shown as thousands, millions, billions, or trillions (e.g., scaling 1,500,000 to 1.5M). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="use_blank_cell_format" type="bool">
                  Indicates whether to apply the configured blank cell format to empty cells in the column's display output. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="use_grouping" type="bool">
                  Specifies whether grouping separators (such as thousands separators) are displayed for numeric values in the QuickSight topic column's default formatting. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="disable_indexing" type="bool">
          When set to true, this column is excluded from the topic's index, preventing it from being used in auto-suggestions or natural language query answering in QuickSight Q. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="is_included_in_topic" type="bool">
          Indicates whether the column is included in the QuickSight topic, making it available for question answering and dashboard building. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="never_aggregate_in_filter" type="bool">
          When set to true, this column will never be aggregated when used in a filter within the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="non_additive" type="bool">
          When true, indicates that the column is non-additive, meaning it cannot be aggregated by summing across dimension values in QuickSight (e.g., ratios or percentages). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="not_allowed_aggregations" type="list(string)">
          Specifies the list of aggregation functions (such as SUM, AVERAGE, MIN, MAX, COUNT) that are explicitly disallowed for this column in QuickSight topic-based visuals, preventing those aggregations from being applied to the column. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="semantic_type" type="object">
          Defines the semantic type of a column in a QuickSight topic dataset, such as whether it acts as a dimension, measure, date, or geographic field, which determines how QuickSight auto-aggregates and displays it in analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="falsey_cell_value" type="string">
              Specifies the custom string value that QuickSight treats as a falsey (null/false) representation when applying the column's semantic type to its data. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="falsey_cell_value_synonyms" type="list(string)">
              Specifies a list of string values that QuickSight should interpret as false (falsey) when applying the semantic type to the column, so that those cell values are treated as boolean false in analyses. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="sub_type_name" type="string">
              Specifies the sub-type classification of the column's semantic type in the QuickSight topic, refining the semantic type to a more specific format such as a date granularity (e.g., 'MONTH' or 'YEAR') or fiscal period. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="truthy_cell_value" type="string">
              Specifies the string value in the dataset column that QuickSight should interpret as TRUE when the column's semantic type is Boolean, enabling non-standard truthy indicators like 'Y' or 'Yes' to be mapped to a true condition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="truthy_cell_value_synonyms" type="list(string)">
              Specifies a list of string values that are treated as true (synonyms for the truthy cell value) when interpreting a column's semantic type in a QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_name" type="string">
              Specifies the semantic type name (such as 'DIMENSION', 'MEASURE', or 'DATETIME') that defines how the column is interpreted by the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_parameters" type="any">
              Configuration key-value pairs that customize how QuickSight interprets the column's semantic type, such as date format or geographic spatial settings. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="time_granularity" type="string">
          Specifies the time granularity (such as DAY, MONTH, YEAR) used to aggregate date/time values for this column in the QuickSight topic's dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="data_aggregation" type="object">
      Configures the default data aggregation behavior for a dataset in the QuickSight topic, specifying the column to aggregate and the aggregation function (e.g., SUM, AVERAGE, or COUNT) used for semantic queries in QuickSight Q. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dataset_row_date_granularity" type="string">
          Specifies the date granularity (e.g., DAY, MONTH, YEAR) used to aggregate date fields in the dataset rows for the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="default_date_column_name" type="string">
          Specifies the default date column name for the dataset within the QuickSight topic, which QuickSight uses when performing date-based aggregations without an explicit date column. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="dataset_arn" type="string">
      The Amazon Resource Name (ARN) of the QuickSight dataset that this topic data set references, linking the topic to a specific dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dataset_description" type="string">
      Provides a human-readable description for the dataset, helping topic users understand its content and purpose within the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dataset_name" type="string">
      Specifies the name of the QuickSight dataset that this topic's data set configuration references, linking the topic to a specific dataset in your account. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="filters" type="list(object)">
      Defines the filter rules that constrain which rows or columns of the data set are available in the QuickSight topic, enabling row-level or column-level security and data scoping. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="category_filter" type="object">
          Specifies a category filter configuration for a QuickSight topic dataset, used to filter rows based on categorical field values such as specific category strings or a list of categories. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="category_filter_function" type="string">
              Specifies the match function (such as EXACT, CONTAINS, STARTS_WITH, or ENDS_WITH) used by this QuickSight topic category filter to evaluate field values. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="category_filter_type" type="string">
              Specifies whether the category filter is a custom filter (CUSTOM_FILTER) or a namespace filter (NAMESPACE_FILTER). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="constant" type="object">
              Defines the fixed list of category values used by the category filter to include or exclude matching data in the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="collective_constant" type="object">
                  Specifies a collective constant for a category filter in a QuickSight topic data set, containing a list of values used to filter categorical data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="value_list" type="list(string)">
                      The list of values that define the collective constant used in the category filter, specifying the set of categories to include or exclude in the filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="constant_type" type="string">
                  Specifies whether the category filter's constant is a single value (SINGULAR) or a range of values (RANGE), controlling how the constant value is interpreted. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="singular_constant" type="string">
                  Specifies a single category value used as the constant in the filter condition, matching items exactly equal to that value. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="inverse" type="bool">
              When true, inverts the category filter so that it matches all categories except those specified in the filter's category values, rather than the ones that match them. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="date_range_filter" type="object">
          Configures a date range filter on a field in a QuickSight topic dataset, specifying a time interval (start and end dates) that determines which data rows are included. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="constant" type="object">
              Specifies the start and end date values that define the range for filtering records in the dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="constant_type" type="string">
                  Specifies the type of constant for the date range filter, either SINGULAR (a single date) or RANGE (a start and end date). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_constant" type="object">
                  This object defines the lower and upper bound dates for the date range filter in the QuickSight topic's dataset filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="maximum" type="string">
                      The upper-bound (end) date of the date range constant for the date range filter, provided as a string. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="minimum" type="string">
                      This string value sets the minimum (lower bound) date of the range constant applied by a date range filter to a dataset in an Amazon QuickSight topic, defining the earliest date considered for the filtered data. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="inclusive" type="bool">
              Indicates whether the boundary dates of the date range filter are included in the filtered results (true) or excluded (false). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="filter_class" type="string">
          Specifies whether the filter is an enforced value filter or a conditional value filter within the QuickSight topic's data set filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter_description" type="string">
          A plain-text description that explains the purpose or effect of the filter applied to the dataset in the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter_name" type="string">
          The unique name that identifies this filter within its parent data set in the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter_synonyms" type="list(string)">
          Specifies alternative names (synonyms) for the filter, which QuickSight users can use in natural language queries to refer to this filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="filter_type" type="string">
          Specifies the type of filter (e.g., CATEGORY_FILTER, NUMERIC_RANGE_FILTER, DATE_RANGE_FILTER) applied to a dataset in the QuickSight topic, determining which nested filter configuration is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="numeric_equality_filter" type="object">
          Configures a numeric equality filter for a QuickSight topic dataset, retaining only rows where the specified numeric field's value equals a given constant or parameter. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="aggregation" type="string">
              Specifies the aggregation function (such as SUM, AVERAGE, MIN, MAX, or COUNT) to apply to the numeric field before the equality filter condition is evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="constant" type="object">
              Specifies the singular numeric value or numeric range used as the equal-to condition in the numeric equality filter, containing the constant type and the decimal value(s) to match. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="constant_type" type="string">
                  Specifies the constant type for the numeric equality filter, which must be set to SINGULAR to indicate a single numeric value is used for comparison. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="singular_constant" type="string">
                  The singular_constant defines the single numeric value that the numeric_equality_filter matches against the topic's dataset field. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="numeric_range_filter" type="object">
          Defines the numeric range filter settings for a dataset filter in the QuickSight topic, including the column to filter, the minimum and maximum values, and whether the boundaries are inclusive. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="aggregation" type="string">
              Specifies the aggregation function (such as SUM, AVERAGE, or COUNT) to apply to the numeric field before the numeric range filter condition is evaluated. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="constant" type="object">
              Defines the constant numeric interval (minimum and maximum) used by the numeric range filter to restrict the topic dataset values. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="constant_type" type="string">
                  Indicates the constant type for the numeric range filter, with the value RANGE used to define a lower and upper bound for the filtered numeric values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="range_constant" type="object">
                  Defines the lower and upper bound values (minimum and maximum) for a numeric range filter constant in a QuickSight topic dataset, restricting the data to values within that range. Optional; if omitted, computed by AWS. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="maximum" type="string">
                      Specifies the upper bound value of the numeric range for the range constant in a numeric range filter applied to a QuickSight topic data set. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="minimum" type="string">
                      The lower bound (minimum) value of the numeric range filter constant, specifying the start of the range to filter on. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="inclusive" type="bool">
              Indicates whether the numeric range filter includes the boundary values (true) or excludes them (false). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="operand_field_name" type="string">
          Specifies the name of the field in the dataset that the filter operates on (the field whose values are used as the operand for the filter condition). Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="relative_date_filter" type="object">
          This object configures a relative date filter for a dataset within an Amazon QuickSight topic, specifying how data should be filtered based on a date range relative to a reference date (such as the current date), e.g., last 30 days or this month. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="constant" type="object">
              Defines the constant date value and its type (e.g., singular or range) used by the relative date filter to resolve the filter's date reference within the QuickSight topic dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="constant_type" type="string">
                  Specifies whether the constant for the relative date filter is a single value (SINGULAR) or a range of values (RANGE). Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="singular_constant" type="string">
                  The singular constant value (e.g., a specific ISO date or a keyword like 'TODAY') that defines the reference point for evaluating the relative date filter in a QuickSight topic dataset. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="relative_date_filter_function" type="string">
              The relative date function (such as PREVIOUS, NEXT, THIS, or YEAR_TO_DATE) that defines how the relative date filter calculates the date range to include in the QuickSight topic filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="time_granularity" type="string">
              Specifies the date granularity (e.g., DAY, MONTH, YEAR) used by the relative date filter to compute the relative time range in the QuickSight topic's data set filter. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="named_entities" type="list(object)">
      Defines the named entities (semantic business concepts) within each dataset of the QuickSight topic, which are used by Amazon QuickSight Q for natural language question answering. Optional; if omitted, computed by AWS. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="definition" type="list(object)">
          Specifies the list of named entity definitions that map dataset fields to the semantic properties, roles, and types of a named entity in a QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="field_name" type="string">
              The name of the dataset field that this named entity refers to, used to link the entity's semantic definition to a specific column in the data source. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metric" type="object">
              Defines a metric-based named entity by specifying the aggregation (e.g., SUM, AVERAGE, COUNT) and the dataset field it applies to, enabling quantitative measures in a QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="aggregation" type="string">
                  Specifies the aggregation function (e.g., SUM, AVERAGE, COUNT) applied to the metric in the relevant Amazon QuickSight topic's named entity definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
                <ResponseField name="aggregation_function_parameters" type="any">
                  Specifies the named parameters required by the metric's aggregation function (for example, the percentile value for PERCENTILE) as a map of parameter names to values. Optional; if omitted, computed by AWS. **(AI-inferred)**
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="property_name" type="string">
              The physical name of the dataset property (column) that this named entity definition references, used when the named entity is defined directly from a property. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="property_role" type="string">
              Determines whether the property is treated as a dimension (categorical) or a measure (numeric) in the QuickSight topic's named entity definition. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="property_usage" type="string">
              Specifies the semantic usage of a property in a QuickSight topic's named entity definition, indicating whether the property acts as a DIMENSION (categorical attribute), MEASURE (numeric metric), or INHERIT (use the dataset's default property usage). Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entity_description" type="string">
          A human-readable description of the named entity, used to clarify the entity's business meaning within the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="entity_name" type="string">
          Specifies the name of a named entity within a dataset of a QuickSight topic, which represents a business concept for analysis. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="entity_synonyms" type="list(string)">
          A list of alternate names or aliases for a named entity, used to match user natural language queries to the entity in Amazon QuickSight Topics. Optional; if omitted, computed by AWS. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="semantic_entity_type" type="object">
          Defines the semantic classification of the named entity, such as whether it acts as a dimension or a measure, along with an optional subtype for finer categorization within the QuickSight topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="sub_type_name" type="string">
              Specifies a secondary classification that further refines the primary semantic entity type (defined by type_name) for the named entity in an Amazon QuickSight topic, enabling more granular categorization of the underlying data asset. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_name" type="string">
              Sets the semantic type name for the named entity (e.g., 'QUANTITATIVE' or 'DIMENSION'), defining how QuickSight interprets and analyzes the field in the topic. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type_parameters" type="any">
              Defines a map of key-value pairs that customize the semantic entity type for a named entity in a QuickSight topic, for example by setting a date format for a DATETIME type or an aggregation method for a measure type. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  The description of the QuickSight topic, providing user-friendly context about the topic's purpose or contents. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="folder_arns" type="list(string)">
  The Amazon Resource Names (ARNs) of the folders that this QuickSight topic is associated with. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The display name for the QuickSight topic, which is a human-readable label shown in the QuickSight interface. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  A list of key-value tag pairs to attach to the QuickSight Topic for metadata management, resource organization, and access control. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      This is the key of a tag assigned to the QuickSight topic, which together with the tag value forms a metadata label used for resource identification, access control, and cost allocation. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value part of a tag key-value pair attached to the Amazon QuickSight topic, used for metadata management like cost allocation or environment identification. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="topic_id" type="string">
  The unique identifier for the QuickSight topic, used to reference the topic in other resources and API operations; if omitted, AWS generates a default topic ID. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="user_experience_version" type="string">
  Specifies the user experience version for the QuickSight topic, which controls the feature set and UI, such as NEW_UX for the latest experience. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) of the QuickSight topic, which uniquely identifies the topic within AWS. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
