---
title: "aws_quick_sight_approval_policy"
description: "`aws_quick_sight_approval_policy` defines a real rule governing when an `aws_quick_sight_action_connector`'s write actions require human approval..."
---

`aws_quick_sight_approval_policy` defines a real rule governing when an `aws_quick_sight_action_connector`'s write actions require human approval before they execute versus running automatically, part of Amazon Quick's own agentic-action governance.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sight approval policy"})

    		ubx.Resource(quick.SightApprovalPolicy, "ci-runner", quick.SightApprovalPolicyConfig{
    			Actions:        []string{"example"},
    			ApplicableTo:   map[string]any{"type": "example"},
    			ApprovalGroups: []string{"example"},
    			AssetTypes:     []string{"example"},
    			Name:           "ci-runner",
    			PolicyId:       "example",
    			Description:    "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightApprovalPolicy } from "./local-sdk/aws/sdk/typescript/aws/quick/sight_approval_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sight approval policy" });

      resource(SightApprovalPolicy, "ci-runner", {
        actions: ["example"],
        applicableTo: { type: "example" },
        approvalGroups: ["example"],
        assetTypes: ["example"],
        name: "ci-runner",
        policyId: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.quick import SightApprovalPolicy, SightApprovalPolicyConfig

    def describe():
        ubx.intent("payments own sight approval policy")

        ubx.resource(SightApprovalPolicy, "ci-runner", SightApprovalPolicyConfig(
            actions=["example"],
            applicable_to={"type": "example"},
            approval_groups=["example"],
            asset_types=["example"],
            name="ci-runner",
            policy_id="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_approval_policy called "ci-runner", with
    actions a set value, applicable to a set value, approval groups a
    set value, asset types a set value, policy id example,
    description Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="actions" type="list(any)" required>
  List of governed actions a policy applies to. Required.
</ResponseField>

<ResponseField name="applicable_to" type="object" required>
  Scoping: who the policy applies to. GROUP: `groupArns` required (one or more group ARNs). Required.
  <Expandable title="properties">
    <ResponseField name="group_arns" type="list(string)">
      Required when type = GROUP. One or more group ARNs. Optional.
    </ResponseField>
    <ResponseField name="type" type="any" required>
      Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="approval_groups" type="list(string)" required>
  List of approval group ARNs (e.g. QuickSight group ARNs). At least one approval group is required; the upper bound is enforced per-account at the service layer via the configurable approver-group limit. Required.
</ResponseField>

<ResponseField name="asset_types" type="list(any)" required>
  List of asset types a policy applies to. At least one asset type is required. Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  The unique identifier for a QuickSight approval policy that governs whether asset publishing requires approval before the asset is shared. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="policy_arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies the QuickSight approval policy, used as a reference in IAM policies and API operations. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>
