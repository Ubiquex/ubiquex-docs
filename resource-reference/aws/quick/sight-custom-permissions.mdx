---
title: "aws_quick_sight_custom_permissions"
description: "`aws_quick_sight_custom_permissions` defines a real, named set of restricted capabilities, disabling the ability to share dashboards, for example..."
---

`aws_quick_sight_custom_permissions` defines a real, named set of restricted capabilities, disabling the ability to share dashboards, for example, that can be assigned to specific QuickSight/Amazon Quick users or groups, layered on top of standard Author or Reader permissions.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sight custom permissions"})

    		ubx.Resource(quick.SightCustomPermissions, "ci-runner", quick.SightCustomPermissionsConfig{
    			AwsAccountId:          "example",
    			CustomPermissionsName: "example-custom-permissions-name",
    			Capabilities:          map[string]any{"access_apps_native_data_store": "example"},
    			Tags:                  []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightCustomPermissions } from "./local-sdk/aws/sdk/typescript/aws/quick/sight_custom_permissions.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sight custom permissions" });

      resource(SightCustomPermissions, "ci-runner", {
        awsAccountId: "example",
        customPermissionsName: "example-custom-permissions-name",
        capabilities: { accessAppsNativeDataStore: "example" },
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.quick import SightCustomPermissions, SightCustomPermissionsConfig

    def describe():
        ubx.intent("payments own sight custom permissions")

        ubx.resource(SightCustomPermissions, "ci-runner", SightCustomPermissionsConfig(
            aws_account_id="example",
            custom_permissions_name="example-custom-permissions-name",
            capabilities={"access_apps_native_data_store": "example"},
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_custom_permissions, with aws account id
    example, custom permissions name example-custom-permissions-name,
    capabilities a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string" required>
  The AWS account ID in which the QuickSight custom permissions profile is created and associated with this resource. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="capabilities" type="object">
  Specifies the capabilities to enable or disable for the QuickSight custom permissions profile, with each key (e.g., CREATE_AND_UPDATE_DASHBOARDS, ADD_OR_RUN_ANALYSIS) mapped to either 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="access_apps_native_data_store" type="string">
      Controls whether the user is granted the ACCESS_APPS_NATIVE_DATA_STORE capability, which permits access to the native data store used by QuickSight applications like QuickSight Q. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="action" type="string">
      Specifies a specific QuickSight capability action (such as EXPORT_TO_CSV or CREATE_AND_UPDATE_DATASETS) that the custom permissions set allows. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="add_or_run_anomaly_detection_for_analyses" type="string">
      Controls whether users in the custom permissions profile can add or run anomaly detection on analyses in Amazon QuickSight, with values typically being ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="amazon_bedrock_arsaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="amazon_bedrock_fsaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="amazon_bedrock_krsaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="amazon_sthree_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="analysis" type="string">
      Sets the analysis permission level for the QuickSight custom permissions profile, controlling which analysis actions (e.g., read, create, update) users with these permissions can perform. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="approve_flow_share_requests" type="string">
      Specifies whether users granted this custom permission level are allowed to approve requests to share QuickSight flows. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="apps" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="asana_action" type="string">
      Controls the Asana action capability, determining whether users with this custom permissions profile can connect to and use Asana as a data source, with allowed values 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="automate" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="bamboo_hraction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="box_agent_action" type="string">
      Sets the resource status for the box_agent_action capability, controlling whether users with this custom permissions profile can perform Box agent actions in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="build_calculated_field_with_q" type="string">
      Specifies whether users are allowed to build calculated fields using Amazon QuickSight Q, with a value of either 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="canva_agent_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="chat_agent" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="comprehend_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="comprehend_medical_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="confluence_action" type="string">
      Controls whether the Confluence integration action is enabled or disabled for these custom permissions, by setting the value to 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_amazon_bedrock_arsaction" type="string">
      Specifies whether the custom permissions include the CREATE_AND_UPDATE_AMAZON_BEDROCK_ARS_ACTION capability, which allows a user to create and update Amazon Bedrock ARS actions in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_amazon_bedrock_fsaction" type="string">
      Determines whether the custom permission allows users to create and update Amazon Bedrock FS actions in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_amazon_bedrock_krsaction" type="string">
      Determines whether QuickSight custom permissions allow or deny the creation and update of Amazon Bedrock KRS actions, with the value set to 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_amazon_sthree_action" type="string">
      Sets the state (ENABLED or DISABLED) for the Amazon QuickSight custom permission that controls whether users can create and update Amazon S3 data sources. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_apps" type="string">
      Specifies whether users with this custom permission can create and update QuickSight apps; set to 'DENY' to restrict this capability, or 'ALLOW' to permit it. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_asana_action" type="string">
      Specifies whether users with this custom permission can create and update Asana actions in Amazon QuickSight, such as triggering Asana tasks from dashboard insights. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_bamboo_hraction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_box_agent_action" type="string">
      Controls whether the user can create and update Box agent actions in QuickSight, with an allowed value of 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_canva_agent_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_comprehend_action" type="string">
      Specifies whether the user has permission to create and update Amazon Comprehend actions in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_comprehend_medical_action" type="string">
      Controls whether the QuickSight custom permission allows or denies the create and update action for Amazon Comprehend Medical, with values typically 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_confluence_action" type="string">
      Determines whether users can create and update Confluence data source connections, typically set to ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_dashboard_email_reports" type="string">
      Specifies whether users with these custom permissions can create and update dashboard email reports, with valid values 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_data_sources" type="string">
      Specifies whether this custom permissions profile permits or denies the 'CREATE_AND_UPDATE_DATA_SOURCES' capability, controlling whether assigned QuickSight users can create and update data sources. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_datasets" type="string">
      Determines whether the user can create and update datasets in Amazon QuickSight, with the value set to either 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_fact_set_action" type="string">
      Determines whether users with this custom permission can create and update QuickSight fact sets, accepting 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_generic_httpaction" type="string">
      This field sets the permission status for the 'create and update generic HTTP action' capability within the QuickSight custom permissions profile, using a string value such as 'ENABLED' or 'DISABLED' to indicate whether users can create and update these HTTP actions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_github_action" type="string">
      Indicates whether the QuickSight custom permissions profile includes the CREATE_AND_UPDATE_GITHUB_ACTION capability, allowing users to create and update GitHub actions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_google_calendar_action" type="string">
      Indicates whether the custom permissions set includes the CREATE_AND_UPDATE_GOOGLE_CALENDAR_ACTION capability, allowing QuickSight users with these permissions to create and update Google Calendar actions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_hubspot_action" type="string">
      This field defines the permission setting (e.g., ALLOW or DENY) for the QuickSight capability to create and update HubSpot actions within custom permissions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_hugging_face_action" type="string">
      Determines whether the custom permissions allow users to create and update Hugging Face actions in Amazon QuickSight, using a string value that enables or disables this capability. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_intercom_action" type="string">
      Specifies whether the custom permission allows ('ALLOW') or denies ('DENY') the user's ability to create and update Intercom actions in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_jira_action" type="string">
      Determines whether the custom permissions grant or deny the ability to create and update Jira actions in Amazon QuickSight, typically using the values 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_knowledge_bases" type="string">
      This string, with allowed values 'ALLOW' or 'DENY', controls whether a user with these custom permissions can create and update knowledge bases in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_linear_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_mcpaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_monday_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_msexchange_action" type="string">
      Specifies whether the custom permission allows users to create and update Microsoft Exchange data sources in Amazon QuickSight, typically with a value of 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_msteams_action" type="string">
      This field specifies whether the Create and update Microsoft Teams action capability is enabled or disabled for the custom permissions, allowing or denying users the ability to create and update Microsoft Teams actions in Amazon QuickSight (with allowed values 'ENABLED' or 'DISABLED'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_new_relic_action" type="string">
      Controls whether the custom permissions include the CREATE_AND_UPDATE_NEW_RELIC_ACTION capability, granting users the ability to create and update New Relic actions in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_notion_action" type="string">
      Defines the permission action (ALLOW or DENY) for the QuickSight capability that enables creating and updating Notions (AI-powered natural language narratives) in the QuickSight console. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_one_drive_action" type="string">
      Controls whether the custom permissions profile allows users to create and update OneDrive actions, with a typical string value of 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_open_apiaction" type="string">
      Controls whether the custom permissions profile grants the ability to create and update open API actions in Amazon QuickSight, corresponding to the CREATE_AND_UPDATE_OPEN_APIAction capability. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_pager_duty_action" type="string">
      Controls whether users with this custom permissions profile can create and update PagerDuty actions, which are used to set up PagerDuty notifications for QuickSight alerts. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_salesforce_action" type="string">
      This capability key specifies whether users can create and update Salesforce actions in QuickSight, typically set to either 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_sand_pglobal_energy_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_sand_pgmiaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_sapbill_of_material_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_sapbusiness_partner_action" type="string">
      Specifies whether users can create and update SAP Business Partner actions in Amazon QuickSight, accepting 'ALLOW' or 'DENY' as the permission value within the custom permissions capabilities. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_sapmaterial_stock_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_sapphysical_inventory_action" type="string">
      Determines whether a user can create and update SAP physical inventory actions in QuickSight, typically set to 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_sapproduct_master_data_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_service_now_action" type="string">
      Determines whether this custom permission grants the ability to create and update ServiceNow actions (such as data source connections) in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_share_point_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="create_and_update_slack_action" type="string">
      Controls whether users with this custom permissions profile can create and update Slack actions, which configure how QuickSight reports and dashboards are delivered to Slack channels. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_smartsheet_action" type="string">
      Determines whether the QuickSight user is allowed to create and update Smartsheet data source datasets, with a value of ALLOW or DENY. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_textract_action" type="string">
      Determines whether users granted this custom permission can create and update Amazon Textract actions, with the value specified as 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_themes" type="string">
      Specifies whether users with this custom permissions profile are allowed to create and update themes in QuickSight, with accepted values 'ENABLED' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_threshold_alerts" type="string">
      Specifies whether users assigned to this custom permission can create and update threshold alerts in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_and_update_zendesk_action" type="string">
      Controls whether the custom permission set enables the ability to create and update Zendesk actions within Amazon QuickSight, as defined in the capabilities block. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_chat_agents" type="string">
      Controls whether users with this custom permissions profile can create and manage chat agents (Amazon QuickSight Q generative AI assistants) in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_dashboard_executive_summary_with_q" type="string">
      Toggles the QuickSight capability that lets a user generate an executive summary for a dashboard using QuickSight Q (natural language queries), with the value indicating whether the capability is enabled or disabled. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_shared_folders" type="string">
      Controls whether the QuickSight principal with this custom permissions set is allowed to create shared folders, where the value must be 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_spaces" type="string">
      This field specifies whether the custom permissions profile permits the user to create spaces in Amazon QuickSight, which are used to organize and manage QuickSight assets and users. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="create_spicedataset" type="string">
      Determines whether principals with this custom permission are allowed to create SPICE datasets, with a value of 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="dashboard" type="string">
      Specifies the permission level for dashboards in this QuickSight custom permissions profile, accepting values such as READ_ONLY, READ_WRITE, or DENY to control user access to dashboards. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="edit_visual_with_q" type="string">
      Controls whether users with this custom permissions profile can use Amazon QuickSight Q to edit visuals, typically set to ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_to_csv" type="string">
      This field indicates whether the custom permissions profile allows (ENABLED) or denies (DISABLED) the ability to export QuickSight data to CSV files. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_to_csv_in_scheduled_reports" type="string">
      Enables or disables the ability for users with this custom permissions profile to export data to CSV in QuickSight scheduled reports, using ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_to_excel" type="string">
      Specifies whether users assigned to this custom permissions profile may export dashboard or analysis data to Excel, with the value set to 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_to_excel_in_scheduled_reports" type="string">
      Specifies whether the export-to-Excel in scheduled reports capability is enabled (ENABLED) or disabled (DISABLED) for users granted these custom permissions in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_to_pdf" type="string">
      Determines whether users assigned these custom permissions can export QuickSight dashboards to PDF, with allowed values 'ENABLED' or 'DENIED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="export_to_pdf_in_scheduled_reports" type="string">
      The export_to_pdf_in_scheduled_reports capability specifies whether users with this custom permissions profile can export scheduled reports to PDF, with the string value typically being 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="extension" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="fact_set_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="flow" type="string">
      This field specifies whether the user is allowed or denied the Flow capability in Amazon QuickSight, with accepted values 'ALLOW' and 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="generic_httpaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="github_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="google_calendar_action" type="string">
      Controls whether QuickSight users can perform Google Calendar actions, such as creating, updating, or deleting calendar events, with allowed values ENABLED, DISABLED, or ALERT. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hubspot_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="hugging_face_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="include_content_in_scheduled_reports_email" type="string">
      This string value toggles whether the QuickSight custom permission grants users the ability to include report content (such as charts or tables) in the emails automatically generated for scheduled reports. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="intercom_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="invoke_apps_aiinference" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="jira_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="knowledge_base" type="string">
      Controls whether users with these custom permissions can access and use Amazon Q knowledge bases in QuickSight, typically set to 'ENABLED' or 'DENIED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="linear_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="manage_shared_folders" type="string">
      Controls whether users with this custom permission can manage shared folders in Amazon QuickSight, with a value of either ALLOW or DENY. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mcpaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="monday_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="msexchange_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="msteams_action" type="string">
      Determines whether users with this custom permission are granted or denied the ability to use Microsoft Teams actions in QuickSight, with allowed values `GRANT` and `DENY`. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="new_relic_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="notion_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="one_drive_action" type="string">
      Specifies whether OneDrive actions (e.g., exporting dashboards or reports to OneDrive) are enabled or disabled for the user, with valid values ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="open_apiaction" type="string">
      Specifies whether the principal has permission to use QuickSight's OpenAPI actions, which allow programmatic access to QuickSight's REST API operations. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pager_duty_action" type="string">
      Controls whether users are allowed to perform PagerDuty actions (such as sending alerts to PagerDuty), with values 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="perform_flow_ui_task" type="string">
      Determines whether a QuickSight user can perform UI tasks in the Flow feature (a visual data preparation experience), with typical values set to ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="print_reports" type="string">
      This field maps to the 'PRINT_REPORTS' capability in Amazon QuickSight, determining whether users with these custom permissions are allowed to print reports, with values like 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="publish_without_approval" type="string">
      Specifies whether users with this custom permissions profile can publish QuickSight dashboards without requiring an approval process. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rename_shared_folders" type="string">
      Specifies whether users assigned this custom permissions profile are allowed to rename shared folders in Amazon QuickSight (e.g., using 'ALLOW' or 'DENY'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="research" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="salesforce_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="sand_pglobal_energy_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="sand_pgmiaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="sapbill_of_material_action" type="string">
      Capability that allows or denies the SAP Bill of Material action, controlling whether users with these custom permissions can perform bill-of-material operations on SAP data sources (allowed values: ALLOW, DENY). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sapbusiness_partner_action" type="string">
      Determines whether users with these custom permissions are allowed to perform SAP Business Partner actions in Amazon QuickSight, with the value set to 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sapmaterial_stock_action" type="string">
      Controls whether users with these custom permissions are allowed to perform the SAP material stock action in Amazon QuickSight, typically by setting the value to ALLOW or DENY. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sapphysical_inventory_action" type="string">
      Controls whether users with these custom permissions can perform the physical inventory action (such as counting or posting inventory) against SAP data sources in Amazon QuickSight, with allowed values `ALLOW` or `DENY`. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sapproduct_master_data_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="service_now_action" type="string">
      Determines which ServiceNow actions are permitted (CREATE_UPDATE or READ) for a QuickSight custom permissions profile. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_amazon_bedrock_arsaction" type="string">
      Determines whether the custom permissions include the SHARE_AMAZON_BEDROCK_ACTION capability, granting users the ability to share Amazon Bedrock actions (such as AI-generated insights) with other QuickSight users. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_amazon_bedrock_fsaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_amazon_bedrock_krsaction" type="string">
      Controls whether users with this custom permission can share Amazon Bedrock KRS actions in QuickSight, with possible values 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_amazon_sthree_action" type="string">
      Determines whether users assigned these custom permissions are allowed to share QuickSight assets directly to Amazon S3, with a value of 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_analyses" type="string">
      Specifies whether the custom permission allows sharing analyses, with value ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_apps" type="string">
      Determines whether users with this custom permissions profile are allowed to share QuickSight apps (e.g., Q topics, stories, and other app assets) with other users, with string values ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_asana_action" type="string">
      Sets the state ('ALLOW' or 'DENY') of the SHARE_ASANA_ACTION capability, controlling whether QuickSight users with these custom permissions can share analyses/dashboards to Asana. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_bamboo_hraction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_box_agent_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_canva_agent_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_chat_agents" type="string">
      Controls whether the user is allowed to share QuickSight assets (such as dashboards) with QuickSight Chat Agents, with values typically 'ENABLED' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_comprehend_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_comprehend_medical_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_confluence_action" type="string">
      Controls whether the capability to share QuickSight dashboards with Confluence is enabled, using the status value ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_dashboards" type="string">
      Determines whether this Amazon QuickSight custom permissions profile allows users to share dashboards with other users. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_data_sources" type="string">
      Controls whether users with this custom permissions profile are allowed to share QuickSight data sources, with the value typically set to 'ENABLED' or 'DENIED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_datasets" type="string">
      Controls whether users with this custom permissions profile can share datasets with other QuickSight users, accepting values 'ENABLED' or 'DENIED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_fact_set_action" type="string">
      Determines whether users with these custom permissions can share fact sets, with value 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_generic_httpaction" type="string">
      Controls whether users with this custom permissions profile are allowed to share QuickSight dashboards or analyses via a generic HTTP action, such as a custom URL or webhook. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_github_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_google_calendar_action" type="string">
      Defines whether users assigned this custom permission are allowed to share QuickSight assets to Google Calendar, with the string value typically being 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_hubspot_action" type="string">
      Controls whether sharing QuickSight assets to HubSpot is allowed or denied, with a value of either 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_hugging_face_action" type="string">
      Controls whether a user can share Hugging Face actions (such as ML models or integrations) within Amazon QuickSight custom permissions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_intercom_action" type="string">
      Controls whether the custom permission set grants the ability to share QuickSight analyses or dashboards via Intercom, with a value expected to be 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_jira_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_knowledge_bases" type="string">
      Controls whether the custom permissions include the SHARE_KNOWLEDGE_BASES capability, which grants users permission to share Amazon QuickSight knowledge bases. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_linear_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_mcpaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_monday_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_msexchange_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_msteams_action" type="string">
      Controls whether the QuickSight custom permissions allow users to share dashboards and analyses to Microsoft Teams, corresponding to the SHARE_MSTEAMS_ACTION capability. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_new_relic_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_notion_action" type="string">
      Determines whether the custom permissions include the ability to share QuickSight notebooks with other users (i.e., the SHARE_NOTEBOOKS capability). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_one_drive_action" type="string">
      Specifies whether users with this custom permission can share QuickSight dashboards and analyses via OneDrive, with valid values 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_open_apiaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_pager_duty_action" type="string">
      Specifies whether users with these custom permissions can share QuickSight dashboards by sending them to PagerDuty, typically set to 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_point_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_salesforce_action" type="string">
      Determines whether users with these custom permissions can share QuickSight dashboards and analyses to Salesforce, with allowed values 'DENY' and 'ALLOW'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_sand_pglobal_energy_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_sand_pgmiaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_sapbill_of_material_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_sapbusiness_partner_action" type="string">
      Controls the custom permission for the share-with-SAP-Business-Partner action, where the string value (e.g., ENABLED or DISABLED) determines whether QuickSight users can share assets with SAP Business Partner accounts. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_sapmaterial_stock_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_sapphysical_inventory_action" type="string">
      Controls whether users with these custom permissions may share SAP physical inventory actions, typically set to 'ENABLED' or 'DISABLED' to allow or deny the action. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_sapproduct_master_data_action" type="string">
      This field represents the 'SHARE_SAP_PRODUCT_MASTER_DATA_ACTION' capability in Amazon QuickSight custom permissions, which controls whether users with these permissions can share SAP product master data. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_service_now_action" type="string">
      Controls whether the SHARE_SERVICE_NOW_ACTION custom permission capability is enabled or disabled, allowing a user to share QuickSight dashboards through a ServiceNow action. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_share_point_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_slack_action" type="string">
      Controls whether users with these custom permissions can share QuickSight dashboards to Slack, where the string value indicates the allowed or denied status (e.g., 'ENABLED' or 'DISABLED'). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_smartsheet_action" type="string">
      Sets the ShareSmartsheetAction capability to either 'ENABLED' or 'DISABLED', determining whether users with these custom permissions can share Smartsheet data sources in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_spaces" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="share_textract_action" type="string">
      Indicates whether sharing of Amazon Textract actions is enabled ('ENABLED') or disabled ('DISABLED') for this custom permissions profile in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="share_zendesk_action" type="string">
      Determines whether the user can share dashboards with Zendesk, with the value typically set to 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="slack_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="smartsheet_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="space" type="string">
      Controls whether users with this custom permission can create and manage QuickSight spaces, with allowed values 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscribe_dashboard_email_reports" type="string">
      This field enables or disables the 'SUBSCRIBE_DASHBOARD_EMAIL_REPORTS' capability, granting or denying users the ability to subscribe to dashboard email reports. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="textract_action" type="string">
      Specifies whether the Amazon Textract action is enabled or disabled for the custom permissions profile, controlling whether users can use Textract to extract text from documents in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="topic" type="string">
      Controls whether the QuickSight Q Topics capability is allowed or denied for the custom permissions, determining users' ability to create and manage topics. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_agent_web_search" type="string">
      Controls whether the QuickSight user is permitted to enable agent web search, allowing Amazon Q to incorporate external web results when generating insights and answers. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_amazon_bedrock_arsaction" type="string">
      Determines the permission state for users to invoke Amazon Bedrock actions within QuickSight, typically set to 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_amazon_bedrock_fsaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_amazon_bedrock_krsaction" type="string">
      Controls whether users with these QuickSight custom permissions can use the Amazon Bedrock KRS action, with the string value typically set to 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_amazon_sthree_action" type="string">
      Controls whether the QuickSight custom permissions profile enables the UseAmazonS3Action capability, which allows or denies users access to Amazon S3 actions such as using S3 data sources, typically set to ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_asana_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_bamboo_hraction" type="string">
      Controls whether the custom permission set authorizes the use of BambooHR actions in Amazon QuickSight, typically with a value of 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_bedrock_models" type="string">
      This field controls whether the QuickSight custom permissions profile allows the use of Amazon Bedrock models for generative BI, corresponding to the 'USE_BEDROCK_MODELS' capability in the capabilities list. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_box_agent_action" type="string">
      Sets the ALLOW or DENY state for the USE_BOX_AGENT_ACTION capability, controlling whether users with this custom permissions profile can use Box agent actions in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_canva_agent_action" type="string">
      Controls whether the custom permission set includes the ability to use the Canva agent action in Amazon QuickSight, enabling users to leverage Canva's design features for creating or editing visuals. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_comprehend_action" type="string">
      Specifies whether the custom permission set includes the capability to use Amazon Comprehend actions in QuickSight, such as running text analysis or other natural language processing features on data. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_comprehend_medical_action" type="string">
      Sets the status of the QuickSight capability 'USE_COMPREHEND_MEDICAL_ACTION' in a custom permissions profile, controlling whether users are allowed or denied the ability to use Amazon Comprehend Medical actions in QuickSight, with valid values 'ALLOW' and 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_confluence_action" type="string">
      This string field sets the `USE_CONFLUENCE_ACTION` capability in the QuickSight custom permissions, controlling whether users with these permissions can use Confluence actions. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_fact_set_action" type="string">
      Specifies whether users with this custom permissions profile are allowed to use fact sets in QuickSight, corresponding to the `UseFactSetAction` capability. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_generic_httpaction" type="string">
      Whether the custom permissions profile allows users to use generic HTTP actions (such as connecting to a custom HTTP endpoint as a data source) in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_github_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_google_calendar_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_hubspot_action" type="string">
      Specifies whether the Use HubSpot action capability is enabled or disabled for the custom permissions profile, controlling if users can perform HubSpot-specific actions in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_hugging_face_action" type="string">
      Determines whether users assigned to these custom permissions are allowed to use Hugging Face actions in Amazon QuickSight, such as running Hugging Face machine learning models, by setting the USE_HUGGING_FACE_ACTION capability to ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_intercom_action" type="string">
      Whether the custom permission set includes the USE_INTERCOM_ACTION capability, which grants users the ability to use the Intercom action in Amazon QuickSight analyses and dashboards. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_jira_action" type="string">
      Controls whether the user can use the Jira integration action in Amazon QuickSight, with values 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_linear_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_mcpaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_monday_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_msexchange_action" type="string">
      Specifies whether users with these custom permissions can use Microsoft Exchange actions (such as connecting to Microsoft Exchange as a data source) in Amazon QuickSight, typically set to 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_msteams_action" type="string">
      Controls whether the custom permission allows the use of Microsoft Teams actions (e.g., sharing insights or sending dashboard updates to Teams) in Amazon QuickSight, with values typically 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_new_relic_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_notion_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_one_drive_action" type="string">
      Controls whether users with these custom permissions can use the OneDrive action (for example, exporting dashboards to OneDrive) by setting this capability to ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_open_apiaction" type="string">
      Specifies whether the user or group with these custom permissions is allowed to use QuickSight OpenAPI actions to programmatically perform operations on QuickSight resources such as creating or updating dashboards and data sources. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_pager_duty_action" type="string">
      Determines whether users with these custom permissions can use the PagerDuty action in QuickSight, with a value of 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_salesforce_action" type="string">
      Sets the USE_SALESFORCE_ACTION capability to enabled or disabled, controlling whether users can perform Salesforce-related actions in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_sand_pglobal_energy_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_sand_pgmiaction" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_sapbill_of_material_action" type="string">
      The `use_sapbill_of_material_action` key in the `capabilities` map sets the permission status (e.g., 'ALLOW' or 'DENY') for the QuickSight SAP Bill of Materials (BOM) action when defining custom permissions for a QuickSight principal. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_sapbusiness_partner_action" type="string">
      Controls whether the SAP Business Partner action is enabled or disabled for users assigned these QuickSight custom permissions, where the value is typically 'ENABLED' or 'DISABLED'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_sapmaterial_stock_action" type="string">
      Sets the custom permission capability to either allow or deny the use of the SAP Material Stock action, with allowed values ENABLED or DISABLED. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_sapphysical_inventory_action" type="string">
      Determines whether the SAP Physical Inventory action is allowed or denied for the QuickSight custom permissions capabilities, typically set to 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_sapproduct_master_data_action" type="string">
      Determines whether the custom permission allows or denies use of the SAP product master data action in Amazon QuickSight, typically with values like 'ALLOW' or 'DENY'. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_service_now_action" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="use_share_point_action" type="string">
      Controls whether users with this custom permissions profile can use the SharePoint action in QuickSight, such as exporting or sharing content to SharePoint. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_slack_action" type="string">
      Determines whether the `USE_SLACK_ACTION` capability is enabled in the custom permissions, allowing users to use Slack actions to share QuickSight dashboards and reports. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_smartsheet_action" type="string">
      Controls whether users with this custom permissions profile are allowed to use Smartsheet as a data source in Amazon QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_textract_action" type="string">
      Indicates whether the custom permissions set includes the QuickSight `USE_TEXT_EXTRACTOR` capability, which grants users the ability to use Amazon Textract actions to extract text from images. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_zendesk_action" type="string">
      Determines whether the USE_ZENDESK_ACTION capability is restricted (denied) by the QuickSight custom permissions, affecting users' ability to use Zendesk actions in QuickSight. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="view_account_spicecapacity" type="string">
      Specifies the state (e.g., ENABLED or DISABLED) of the View account SPICE capacity capability in QuickSight custom permissions, controlling whether users can view the account's SPICE capacity usage. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="zendesk_action" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="custom_permissions_name" type="string" required>
  The unique name for the custom permissions profile, used to identify and attach it to QuickSight users or groups within the account. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags field attaches key-value metadata to the QuickSight custom permissions resource, enabling cost allocation, resource categorization, and IAM-based access control. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a user-defined tag attached to this QuickSight custom permissions resource for identification and organization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value portion of a tag key-value pair assigned to the QuickSight custom permissions resource, used for metadata and resource organization. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) that uniquely identifies this QuickSight custom permissions resource, automatically assigned by AWS when the resource is created. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
