---
title: "aws_quick_sight_action_connector"
description: "`aws_quick_sight_action_connector` connects a real external system, a SaaS application or internal API..."
---

`aws_quick_sight_action_connector` connects a real external system, a SaaS application or internal API, to Amazon Quick (the AI-workspace product that Amazon QuickSight was renamed into, first as Amazon Quick Suite on October 9, 2025 and shortened to Amazon Quick in 2026), letting its agents take real write actions in that system rather than only read data from it. The underlying CloudFormation namespace remains `AWS::QuickSight::*` despite the product rename.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own sight action connector"})

    		ubx.Resource(quick.SightActionConnector, "example", quick.SightActionConnectorConfig{
    			ActionConnectorId:    "example",
    			AuthenticationConfig: map[string]any{"authentication_metadata": map[string]any{"api_key_connection_metadata": map[string]any{"api_key": "example"}}},
    			AwsAccountId:         "example",
    			Name:                 "example",
    			Type:                 "example",
    			Description:          "Managed by ubx.",
    			Permissions:          []map[string]any{{"actions": []string{"example"}}},
    			Tags:                 []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightActionConnector } from "@ubx/sdk-aws/aws/quick/sight_action_connector";

    export default stack("example", () => {
      intent({ summary: "example own sight action connector" });

      resource(SightActionConnector, "example", {
        actionConnectorId: "example",
        authenticationConfig: {
          authenticationMetadata: {
            apiKeyConnectionMetadata: { apiKey: "example" },
          },
        },
        awsAccountId: "example",
        name: "example",
        type: "example",
        description: "Managed by ubx.",
        permissions: [{ actions: ["example"] }],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quick import SightActionConnector, SightActionConnectorConfig

    def describe():
        ubx.intent("example own sight action connector")

        ubx.resource(SightActionConnector, "example", SightActionConnectorConfig(
            action_connector_id="example",
            authentication_config={"authentication_metadata": {"api_key_connection_metadata": {"api_key": "example"}}},
            aws_account_id="example",
            name="example",
            type="example",
            description="Managed by ubx.",
            permissions=[{"actions": ["example"]}],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_action_connector called "example", with
    action connector id example, authentication config a set value,
    aws account id example, type example, description Managed by
    ubx., permissions a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="action_connector_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authentication_config" type="object" required>
  Required.
  <Expandable title="properties">
    <ResponseField name="authentication_metadata" type="object" required>
      Required.
      <Expandable title="properties">
        <ResponseField name="api_key_connection_metadata" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="api_key" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="base_endpoint" type="string">
              The base endpoint URL of the external API service that the QuickSight action connector communicates with when using API key authentication. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="email" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="authorization_code_grant_metadata" type="object">
          Contains the OAuth 2.0 authorization code grant parameters (such as authorization and token endpoints, client ID, client secret, and scopes) used by QuickSight to authenticate an action connector to an external service. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="authorization_code_grant_credentials_details" type="object">
              Contains the OAuth 2.0 client ID and client secret (or a reference to a Secrets Manager secret storing them) used by the QuickSight action connector during the authorization code grant token exchange in its authentication flow. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="authorization_code_grant_details" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="authorization_endpoint" type="string">
                      The URL of the OAuth 2.0 authorization server's authorization endpoint to which users are redirected to grant permissions during the authorization code grant flow for the QuickSight action connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="client_id" type="string">
                      The OAuth 2.0 client ID issued to the QuickSight action connector by the external authorization server, used as the public client identifier during the authorization code grant flow to obtain access tokens. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="client_secret" type="string">
                      The client secret for the OAuth authorization code grant flow, used by the QuickSight action connector to authenticate with the external service. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="token_endpoint" type="string">
                      The URL of the OAuth 2.0 token endpoint used in the authorization code grant flow to exchange an authorization code for an access token. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="authorization_code_grant_credentials_source" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="base_endpoint" type="string">
              The base endpoint (base URL) of the OAuth authorization server to which Amazon QuickSight sends authorization code grant requests for the action connector's authentication metadata. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="redirect_url" type="string">
              Specifies the redirect (callback) URL to which the user is redirected after authorizing the OAuth 2.0 authorization code grant, enabling the QuickSight action connector to exchange the authorization code for tokens. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="basic_auth_connection_metadata" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="base_endpoint" type="string">
              The base endpoint (root URL) of the external service that this basic authentication connection targets, used as the base for all API requests made through the connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="password" type="string">
              Specifies the password for HTTP basic authentication to the external endpoint that this QuickSight action connector is configured to call. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="username" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="client_credentials_grant_metadata" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="base_endpoint" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
            <ResponseField name="client_credentials_details" type="object">
              This object contains the client credentials (e.g., client ID and client secret) and other details needed to perform the OAuth 2.0 client-credentials grant for authenticating the QuickSight action connector to the external service. Optional; if omitted, computed by AWS. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="client_credentials_grant_details" type="object">
                  Optional; if omitted, computed by AWS.
                  <Expandable title="properties">
                    <ResponseField name="client_id" type="string">
                      The OAuth client identifier used in the client credentials grant flow to authenticate the QuickSight action connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="client_secret" type="string">
                      The client secret used to authenticate the application when requesting an access token via the OAuth 2.0 client credentials grant for the QuickSight action connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="token_endpoint" type="string">
                      Defines the OAuth 2.0 token endpoint URL that the QuickSight action connector calls with the client credentials grant to obtain an access token for authenticating to the external API. Optional; if omitted, computed by AWS. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="client_credentials_source" type="string">
              Optional; if omitted, computed by AWS.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="iam_connection_metadata" type="object">
          Optional; if omitted, computed by AWS.
          <Expandable title="properties">
            <ResponseField name="role_arn" type="string">
              The ARN of the IAM role that QuickSight assumes to authenticate with the external service when using IAM connection metadata for this action connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="none_connection_metadata" type="object">
          The `noneConnectionMetadata` object is an empty placeholder used within the authentication metadata when the authentication type is set to `none`, indicating that no connection credentials are required for the action connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="base_endpoint" type="string">
              The base endpoint URL that the QuickSight action connector targets when no authentication method (NoneConnectionMetadata) is used. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="authentication_type" type="string" required>
      Specifies the authentication mechanism (such as basic authentication, API key, or OAuth 2.0) that QuickSight uses to authenticate the action connector when invoking the external endpoint. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="aws_account_id" type="string" required>
  The AWS account ID where the QuickSight action connector is deployed, used to scope the resource within the correct account. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="permissions" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="actions" type="list(string)">
      Specifies the list of Amazon QuickSight actions (e.g., "quicksight:DescribeDashboard", "quicksight:UpdateDashboard") that the principal is allowed to perform on the QuickSight resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="principal" type="string">
      The ARN of the principal (IAM user, group, role, or QuickSight user/group) that the corresponding actions are being granted to for this QuickSight action connector. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(object)">
  Optional.
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
    <ResponseField name="value" type="string">
      Optional; if omitted, computed by AWS.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of the QuickSight action connector, identifying the external system or service it integrates with (e.g., ServiceNow, Salesforce, or similar supported platforms), required when defining the connector. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="vpc_connection_arn" type="string">
  The ARN of the VPC connection that the QuickSight action connector uses to securely access resources within a private VPC. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled_actions" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
