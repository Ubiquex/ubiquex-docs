---
title: "aws_quick_sight_vpcconnection"
description: "`aws_quick_sight_vpcconnection` creates a real, persistent elastic network interface into a VPC that an `aws_quick_sight_data_source` can route through..."
---

`aws_quick_sight_vpcconnection` creates a real, persistent elastic network interface into a VPC that an `aws_quick_sight_data_source` can route through to reach a database that isn't publicly reachable.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	quick "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quick"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sight vpcconnection"})

    		ubx.Resource(quick.SightVpcconnection, "ci-runner", quick.SightVpcconnectionConfig{
    			Name:               "ci-runner",
    			AvailabilityStatus: "example",
    			AwsAccountId:       "example",
    			DnsResolvers:       []string{"example"},
    			RoleArn:            "arn:aws:iam::123456789012:policy/ci",
    			SecurityGroupIds:   []string{"example"},
    			SubnetIds:          []string{"example"},
    			Tags:               []map[string]any{{"key": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SightVpcconnection } from "./local-sdk/aws/sdk/typescript/aws/quick/sight_vpcconnection.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own sight vpcconnection" });

      resource(SightVpcconnection, "ci-runner", {
        name: "ci-runner",
        availabilityStatus: "example",
        awsAccountId: "example",
        dnsResolvers: ["example"],
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        securityGroupIds: ["example"],
        subnetIds: ["example"],
        tags: [{ key: "example" }],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.quick import SightVpcconnection, SightVpcconnectionConfig

    def describe():
        ubx.intent("payments own sight vpcconnection")

        ubx.resource(SightVpcconnection, "ci-runner", SightVpcconnectionConfig(
            name="ci-runner",
            availability_status="example",
            aws_account_id="example",
            dns_resolvers=["example"],
            role_arn="arn:aws:iam::123456789012:policy/ci",
            security_group_ids=["example"],
            subnet_ids=["example"],
            tags=[{"key": "example"}],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quick_sight_vpcconnection called "ci-runner", with
    availability status example, aws account id example, dns
    resolvers a set value, role arn
    arn:aws:iam::123456789012:policy/ci, security group ids a set
    value, subnet ids a set value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="availability_status" type="string">
  Indicates the current availability status of the QuickSight VPC connection, using values such as AVAILABLE, UNAVAILABLE, or PARTIALLY_AVAILABLE, which reflects whether the connection is ready for use in SPICE data ingestion. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="aws_account_id" type="string">
  The AWS account ID where the VPC connection is being created, allowing you to specify a different account than the one the credentials belong to. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="dns_resolvers" type="list(string)">
  Specifies the IP addresses of the private DNS resolvers used to resolve domain names for resources in the VPC connection. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The display name for the QuickSight VPC connection, used to identify it in the QuickSight console and APIs. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="role_arn" type="string">
  The ARN of the IAM role that grants Amazon QuickSight the permissions required to access the VPC and its resources. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="security_group_ids" type="list(string)">
  Specifies the list of security group IDs associated with the VPC connection that control network access to the Amazon QuickSight VPC resources. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="subnet_ids" type="list(string)">
  The list of subnet IDs within the VPC where the QuickSight VPC connection is deployed, used to route network traffic for the connection. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="tags" type="list(object)">
  The tags to assign to the QuickSight VPC connection, consisting of key-value pairs that help identify, organize, and manage the connection within AWS. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="key" type="string">
      The key of a tag assigned to the QuickSight VPC connection, used to categorize and manage the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="string">
      The value of a tag attached to this AWS QuickSight VPC connection, used to assign metadata or categorize the resource. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpcconnection_id" type="string">
  Specifies a custom, user-defined identifier for the QuickSight VPC connection, unique per AWS account and Region, which AWS uses to refer to the connection instead of generating one automatically. Optional. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  &lt;p&gt;The Amazon Resource Name (ARN) of the VPC connection.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_time" type="string">
  &lt;p&gt;The time that the VPC connection was created.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_time" type="string">
  &lt;p&gt;The time that the VPC connection was last updated.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="network_interfaces" type="list(object)">
  &lt;p&gt;A list of network interfaces.&lt;/p&gt; Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="availability_zone" type="string">
      The Availability Zone where the network interface used by the QuickSight VPC connection is deployed. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="error_message" type="string">
      The error message returned by AWS when a network interface associated with the QuickSight VPC connection is in an error state, providing details about why that interface is unhealthy. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="network_interface_id" type="string">
      The AWS-generated ID of the network interface that QuickSight creates and attaches to the VPC connection to route traffic to resources in your VPC. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="status" type="string">
      The current provisioning state of the network interface created in the customer's VPC for the QuickSight VPC connection, such as whether it is available, creating, or deleting. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subnet_id" type="string">
      The ID of the subnet where the QuickSight VPC connection's network interface is created, enabling QuickSight to reach resources within that subnet. Optional; if omitted, computed by AWS. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The current lifecycle status of the QuickSight VPC connection, such as CREATION_IN_PROGRESS, CREATION_SUCCESSFUL, CREATION_FAILED, UPDATE_IN_PROGRESS, UPDATE_SUCCESSFUL, or UPDATE_FAILED (read-only). Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="vpcid" type="string">
  &lt;p&gt;The Amazon EC2 VPC ID associated with the VPC connection.&lt;/p&gt; Computed by AWS after `ship`.
</ResponseField>
