---
title: "aws_fms_resource_set"
description: "Real, generated bindings for `aws_fms_resource_set`."
---

`aws_fms_resource_set` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/fms"

    cfg := fms.ResourceSetConfig{
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(fms.ResourceSet, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ResourceSet } from
      "jsr:@ubx/sdk-aws/aws/fms/resource_set";

    resource(ResourceSet, "example", {
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.fms import ResourceSet, ResourceSetConfig

    cfg = ResourceSetConfig(
        tags={"key": "value"},
    )
    ubx.resource(ResourceSet, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_fms_resource_set with tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
