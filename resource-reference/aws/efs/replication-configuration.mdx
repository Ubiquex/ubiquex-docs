---
title: "aws_efs_replication_configuration"
description: "Real, generated bindings for `aws_efs_replication_configuration`."
---

`aws_efs_replication_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	efs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/efs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own replication configuration"})

    		ubx.Resource(efs.ReplicationConfiguration, "ci-runner", efs.ReplicationConfigurationConfig{
    			SourceFileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReplicationConfiguration } from "jsr:@ubx/sdk-aws/aws/efs/replication_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own replication configuration" });

      resource(ReplicationConfiguration, "ci-runner", {
        sourceFileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.efs import ReplicationConfiguration, ReplicationConfigurationConfig

    def describe():
        ubx.intent("payments own replication configuration")

        ubx.resource(ReplicationConfiguration, "ci-runner", ReplicationConfigurationConfig(
            source_file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_efs_replication_configuration, with source file system
    id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_file_system_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="original_source_file_system_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_file_system_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_file_system_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>
