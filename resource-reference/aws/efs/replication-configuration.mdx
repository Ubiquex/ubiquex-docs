---
title: "aws_efs_replication_configuration"
description: "Real, generated bindings for `aws_efs_replication_configuration`."
---

`aws_efs_replication_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/efs"

    cfg := efs.ReplicationConfigurationConfig{
        SourceFileSystemId: "example",
    }
    ubx.Resource(efs.ReplicationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ReplicationConfiguration } from
      "jsr:@ubx/sdk-aws/aws/efs/replication_configuration";

    resource(ReplicationConfiguration, "example", {
      sourceFileSystemId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.efs import (
        ReplicationConfiguration,
        ReplicationConfigurationConfig,
    )

    cfg = ReplicationConfigurationConfig(
        source_file_system_id="example",
    )
    ubx.resource(ReplicationConfiguration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_efs_replication_configuration with
source_file_system_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_file_system_id" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="original_source_file_system_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_file_system_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_file_system_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>
