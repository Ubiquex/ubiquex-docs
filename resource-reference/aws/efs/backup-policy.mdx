---
title: "aws_efs_backup_policy"
description: "Real, generated bindings for `aws_efs_backup_policy`."
---

`aws_efs_backup_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/efs"

    cfg := efs.BackupPolicyConfig{
        FileSystemId: "example",
    }
    ubx.Resource(efs.BackupPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { BackupPolicy } from
      "jsr:@ubx/sdk-aws/aws/efs/backup_policy";

    resource(BackupPolicy, "example", {
      fileSystemId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.efs import BackupPolicy, BackupPolicyConfig

    cfg = BackupPolicyConfig(
        file_system_id="example",
    )
    ubx.resource(BackupPolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_efs_backup_policy with file_system_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="file_system_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
