---
title: "aws_efs_backup_policy"
description: "Real, generated bindings for `aws_efs_backup_policy`."
---

`aws_efs_backup_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	efs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/efs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backup policy"})

    		ubx.Resource(efs.BackupPolicy, "ci-runner", efs.BackupPolicyConfig{
    			FileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackupPolicy } from "jsr:@ubx/sdk-aws/aws/efs/backup_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own backup policy" });

      resource(BackupPolicy, "ci-runner", {
        fileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.efs import BackupPolicy, BackupPolicyConfig

    def describe():
        ubx.intent("payments own backup policy")

        ubx.resource(BackupPolicy, "ci-runner", BackupPolicyConfig(
            file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_efs_backup_policy, with file system id example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="file_system_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
