---
title: "aws_ecrpublic_repository"
description: "Real, generated bindings for `aws_ecrpublic_repository`."
---

`aws_ecrpublic_repository` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecrpublic "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecrpublic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own repository"})

    		ubx.Resource(ecrpublic.Repository, "ci-runner", ecrpublic.RepositoryConfig{
    			RepositoryName: "example-repository-name",
    			ForceDestroy:   true,
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Repository } from "jsr:@ubx/sdk-aws/aws/ecrpublic/repository";

    export default stack("payments", () => {
      intent({ summary: "payments own repository" });

      resource(Repository, "ci-runner", {
        repositoryName: "example-repository-name",
        forceDestroy: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecrpublic import Repository, RepositoryConfig

    def describe():
        ubx.intent("payments own repository")

        ubx.resource(Repository, "ci-runner", RepositoryConfig(
            repository_name="example-repository-name",
            force_destroy=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecrpublic_repository, with repository name
    example-repository-name, force destroy true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="repository_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="repository_uri" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
