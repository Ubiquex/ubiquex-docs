---
title: "aws_sqs_queue_policy"
description: "Real, generated bindings for `aws_sqs_queue_policy`."
---

`aws_sqs_queue_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/sqs"

    cfg := sqs.QueuePolicyConfig{
        Policy: "example",
        QueueUrl: "example",
    }
    ubx.Resource(sqs.QueuePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { QueuePolicy } from
      "jsr:@ubx/sdk-aws/aws/sqs/queue_policy";

    resource(QueuePolicy, "example", {
      policy: "example",
      queueUrl: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.sqs import QueuePolicy, QueuePolicyConfig

    cfg = QueuePolicyConfig(
        policy="example",
        queue_url="example",
    )
    ubx.resource(QueuePolicy, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_sqs_queue_policy with policy, queue_url.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="queue_url" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
