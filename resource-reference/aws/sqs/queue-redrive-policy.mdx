---
title: "aws_sqs_queue_redrive_policy"
description: "Real, generated bindings for `aws_sqs_queue_redrive_policy`."
---

`aws_sqs_queue_redrive_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sqs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sqs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own queue redrive policy"})

    		ubx.Resource(sqs.QueueRedrivePolicy, "ci-runner", sqs.QueueRedrivePolicyConfig{
    			QueueUrl:      "example",
    			RedrivePolicy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QueueRedrivePolicy } from "jsr:@ubx/sdk-aws/aws/sqs/queue_redrive_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own queue redrive policy" });

      resource(QueueRedrivePolicy, "ci-runner", {
        queueUrl: "example",
        redrivePolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sqs import QueueRedrivePolicy, QueueRedrivePolicyConfig

    def describe():
        ubx.intent("payments own queue redrive policy")

        ubx.resource(QueueRedrivePolicy, "ci-runner", QueueRedrivePolicyConfig(
            queue_url="example",
            redrive_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sqs_queue_redrive_policy, with queue url example,
    redrive policy example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="queue_url" type="string" required>
  Required.
</ResponseField>

<ResponseField name="redrive_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
