---
title: "aws_iam_account_alias"
description: "Real, generated bindings for `aws_iam_account_alias`."
---

`aws_iam_account_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.AccountAliasConfig{
        AccountAlias: "example",
    }
    ubx.Resource(iam.AccountAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AccountAlias } from
      "jsr:@ubx/sdk-aws/aws/iam/account_alias";

    resource(AccountAlias, "example", {
      accountAlias: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import AccountAlias, AccountAliasConfig

    cfg = AccountAliasConfig(
        account_alias="example",
    )
    ubx.resource(AccountAlias, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_account_alias with account_alias.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="account_alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
