---
title: "aws_iam_group_policies_exclusive"
description: "Real, generated bindings for `aws_iam_group_policies_exclusive`."
---

`aws_iam_group_policies_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group policies exclusive"})

    		ubx.Resource(iam.GroupPoliciesExclusive, "ci-runner", iam.GroupPoliciesExclusiveConfig{
    			GroupName:   "example-group-name",
    			PolicyNames: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupPoliciesExclusive } from "jsr:@ubx/sdk-aws/aws/iam/group_policies_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own group policies exclusive" });

      resource(GroupPoliciesExclusive, "ci-runner", {
        groupName: "example-group-name",
        policyNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import GroupPoliciesExclusive, GroupPoliciesExclusiveConfig

    def describe():
        ubx.intent("payments own group policies exclusive")

        ubx.resource(GroupPoliciesExclusive, "ci-runner", GroupPoliciesExclusiveConfig(
            group_name="example-group-name",
            policy_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_group_policies_exclusive, with group name
    example-group-name, policy names a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_names" type="set(string)" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
