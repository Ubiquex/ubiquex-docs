---
title: "aws_iam_group_membership"
description: "Real, generated bindings for `aws_iam_group_membership`."
---

`aws_iam_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group membership"})

    		ubx.Resource(iam.GroupMembership, "ci-runner", iam.GroupMembershipConfig{
    			Group: "example",
    			Name:  "ci-runner",
    			Users: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupMembership } from "jsr:@ubx/sdk-aws/aws/iam/group_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own group membership" });

      resource(GroupMembership, "ci-runner", {
        group: "example",
        name: "ci-runner",
        users: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import GroupMembership, GroupMembershipConfig

    def describe():
        ubx.intent("payments own group membership")

        ubx.resource(GroupMembership, "ci-runner", GroupMembershipConfig(
            group="example",
            name="ci-runner",
            users=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_group_membership called "ci-runner", with group
    example, users a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="users" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
