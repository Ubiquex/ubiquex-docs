---
title: "aws_iam_group_membership"
description: "Real, generated bindings for `aws_iam_group_membership`."
---

`aws_iam_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.GroupMembershipConfig{
        Group: "example",
        Name: "example-name",
        Users: []string{"example"},
    }
    ubx.Resource(iam.GroupMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GroupMembership } from
      "jsr:@ubx/sdk-aws/aws/iam/group_membership";

    resource(GroupMembership, "example", {
      group: "example",
      name: "example",
      users: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import GroupMembership, GroupMembershipConfig

    cfg = GroupMembershipConfig(
        group="example",
        name="example",
        users=["example"],
    )
    ubx.resource(GroupMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_group_membership with group, name, users.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="users" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
