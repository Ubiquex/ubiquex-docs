---
title: "aws_iam_user_policies_exclusive"
description: "Real, generated bindings for `aws_iam_user_policies_exclusive`."
---

`aws_iam_user_policies_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.UserPoliciesExclusiveConfig{
        PolicyNames: []string{"example"},
        UserName: "example-user-name",
    }
    ubx.Resource(iam.UserPoliciesExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserPoliciesExclusive } from
      "jsr:@ubx/sdk-aws/aws/iam/user_policies_exclusive";

    resource(UserPoliciesExclusive, "example", {
      policyNames: ["example"],
      userName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        UserPoliciesExclusive,
        UserPoliciesExclusiveConfig,
    )

    cfg = UserPoliciesExclusiveConfig(
        policy_names=["example"],
        user_name="example",
    )
    ubx.resource(UserPoliciesExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_user_policies_exclusive with policy_names,
user_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_names" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
