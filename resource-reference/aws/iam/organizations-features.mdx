---
title: "aws_iam_organizations_features"
description: "Real, generated bindings for `aws_iam_organizations_features`."
---

`aws_iam_organizations_features` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.OrganizationsFeaturesConfig{
        EnabledFeatures: []string{"example"},
    }
    ubx.Resource(iam.OrganizationsFeatures, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OrganizationsFeatures } from
      "jsr:@ubx/sdk-aws/aws/iam/organizations_features";

    resource(OrganizationsFeatures, "example", {
      enabledFeatures: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        OrganizationsFeatures,
        OrganizationsFeaturesConfig,
    )

    cfg = OrganizationsFeaturesConfig(
        enabled_features=["example"],
    )
    ubx.resource(OrganizationsFeatures, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_organizations_features with enabled_features.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="enabled_features" type="set(string)" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
