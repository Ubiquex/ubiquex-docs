---
title: "aws_iam_virtual_mfa_device"
description: "Real, generated bindings for `aws_iam_virtual_mfa_device`."
---

`aws_iam_virtual_mfa_device` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own virtual mfa device"})

    		ubx.Resource(iam.VirtualMfaDevice, "ci-runner", iam.VirtualMfaDeviceConfig{
    			VirtualMfaDeviceName: "example-virtual-mfa-device-name",
    			Path:                 "/ci/",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VirtualMfaDevice } from "jsr:@ubx/sdk-aws/aws/iam/virtual_mfa_device";

    export default stack("payments", () => {
      intent({ summary: "payments own virtual mfa device" });

      resource(VirtualMfaDevice, "ci-runner", {
        virtualMfaDeviceName: "example-virtual-mfa-device-name",
        path: "/ci/",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import VirtualMfaDevice, VirtualMfaDeviceConfig

    def describe():
        ubx.intent("payments own virtual mfa device")

        ubx.resource(VirtualMfaDevice, "ci-runner", VirtualMfaDeviceConfig(
            virtual_mfa_device_name="example-virtual-mfa-device-name",
            path="/ci/",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_virtual_mfa_device, with virtual mfa device name
    example-virtual-mfa-device-name, path /ci/, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="path" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="virtual_mfa_device_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="base_32_string_seed" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enable_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="qr_code_png" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="serial_number" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>
