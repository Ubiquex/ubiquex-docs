---
title: "aws_iam_virtual_mfa_device"
description: "Real, generated bindings for `aws_iam_virtual_mfa_device`."
---

`aws_iam_virtual_mfa_device` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.VirtualMfaDeviceConfig{
        VirtualMfaDeviceName: "example-virtual-mfa-device-name",
        Path: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(iam.VirtualMfaDevice, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { VirtualMfaDevice } from
      "jsr:@ubx/sdk-aws/aws/iam/virtual_mfa_device";

    resource(VirtualMfaDevice, "example", {
      virtualMfaDeviceName: "example",
      path: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        VirtualMfaDevice,
        VirtualMfaDeviceConfig,
    )

    cfg = VirtualMfaDeviceConfig(
        virtual_mfa_device_name="example",
        path="example",
        tags={"key": "value"},
    )
    ubx.resource(VirtualMfaDevice, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_virtual_mfa_device with
virtual_mfa_device_name, path, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="path" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="virtual_mfa_device_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="base_32_string_seed" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enable_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="qr_code_png" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="serial_number" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>
