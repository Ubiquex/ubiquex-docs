---
title: "aws_iam_user_policy_attachments_exclusive"
description: "Real, generated bindings for `aws_iam_user_policy_attachments_exclusive`."
---

`aws_iam_user_policy_attachments_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.UserPolicyAttachmentsExclusiveConfig{
        PolicyArns: []string{"example"},
        UserName: "example-user-name",
    }
    ubx.Resource(iam.UserPolicyAttachmentsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserPolicyAttachmentsExclusive } from
      "jsr:@ubx/sdk-aws/aws/iam/user_policy_attachments_exclusive";

    resource(UserPolicyAttachmentsExclusive, "example", {
      policyArns: ["example"],
      userName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        UserPolicyAttachmentsExclusive,
        UserPolicyAttachmentsExclusiveConfig,
    )

    cfg = UserPolicyAttachmentsExclusiveConfig(
        policy_arns=["example"],
        user_name="example",
    )
    ubx.resource(UserPolicyAttachmentsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_user_policy_attachments_exclusive with
policy_arns, user_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
