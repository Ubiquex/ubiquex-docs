---
title: "aws_iam_group"
description: "Real, generated bindings for `aws_iam_group`."
---

`aws_iam_group` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group"})

    		ubx.Resource(iam.Group, "ci-runner", iam.GroupConfig{
    			Name: "ci-runner",
    			Path: "/ci/",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Group } from "jsr:@ubx/sdk-aws/aws/iam/group";

    export default stack("payments", () => {
      intent({ summary: "payments own group" });

      resource(Group, "ci-runner", {
        name: "ci-runner",
        path: "/ci/",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import Group, GroupConfig

    def describe():
        ubx.intent("payments own group")

        ubx.resource(Group, "ci-runner", GroupConfig(
            name="ci-runner",
            path="/ci/",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_group called "ci-runner", with path /ci/.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="path" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="unique_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
