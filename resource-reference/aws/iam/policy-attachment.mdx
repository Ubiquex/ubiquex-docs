---
title: "aws_iam_policy_attachment"
description: "Real, generated bindings for `aws_iam_policy_attachment`."
---

`aws_iam_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.PolicyAttachmentConfig{
        Name: "example-name",
        PolicyArn: "example",
        Groups: []string{"example"},
        Roles: []string{"example"},
    }
    ubx.Resource(iam.PolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { PolicyAttachment } from
      "jsr:@ubx/sdk-aws/aws/iam/policy_attachment";

    resource(PolicyAttachment, "example", {
      name: "example",
      policyArn: "example",
      groups: ["example"],
      roles: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        PolicyAttachment,
        PolicyAttachmentConfig,
    )

    cfg = PolicyAttachmentConfig(
        name="example",
        policy_arn="example",
        groups=["example"],
        roles=["example"],
    )
    ubx.resource(PolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_policy_attachment with name, policy_arn,
groups, roles.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="groups" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="roles" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="users" type="set(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
