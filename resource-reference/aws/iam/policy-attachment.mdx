---
title: "aws_iam_policy_attachment"
description: "Real, generated bindings for `aws_iam_policy_attachment`."
---

`aws_iam_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own policy attachment"})

    		ubx.Resource(iam.PolicyAttachment, "ci-runner", iam.PolicyAttachmentConfig{
    			Name:      "ci-runner",
    			PolicyArn: "arn:aws:iam::123456789012:policy/ci",
    			Groups:    []string{"example"},
    			Roles:     []string{"example"},
    			Users:     []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PolicyAttachment } from "jsr:@ubx/sdk-aws/aws/iam/policy_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own policy attachment" });

      resource(PolicyAttachment, "ci-runner", {
        name: "ci-runner",
        policyArn: "arn:aws:iam::123456789012:policy/ci",
        groups: ["example"],
        roles: ["example"],
        users: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import PolicyAttachment, PolicyAttachmentConfig

    def describe():
        ubx.intent("payments own policy attachment")

        ubx.resource(PolicyAttachment, "ci-runner", PolicyAttachmentConfig(
            name="ci-runner",
            policy_arn="arn:aws:iam::123456789012:policy/ci",
            groups=["example"],
            roles=["example"],
            users=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_policy_attachment called "ci-runner", with policy
    arn arn:aws:iam::123456789012:policy/ci, groups a set value,
    roles a set value, users a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="groups" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="roles" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="users" type="set(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
