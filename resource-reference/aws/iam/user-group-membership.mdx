---
title: "aws_iam_user_group_membership"
description: "Real, generated bindings for `aws_iam_user_group_membership`."
---

`aws_iam_user_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user group membership"})

    		ubx.Resource(iam.UserGroupMembership, "ci-runner", iam.UserGroupMembershipConfig{
    			Groups: []string{"example"},
    			User:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserGroupMembership } from "jsr:@ubx/sdk-aws/aws/iam/user_group_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own user group membership" });

      resource(UserGroupMembership, "ci-runner", {
        groups: ["example"],
        user: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import UserGroupMembership, UserGroupMembershipConfig

    def describe():
        ubx.intent("payments own user group membership")

        ubx.resource(UserGroupMembership, "ci-runner", UserGroupMembershipConfig(
            groups=["example"],
            user="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_user_group_membership, with groups a set value,
    user example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="groups" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
