---
title: "aws_iam_user_group_membership"
description: "Real, generated bindings for `aws_iam_user_group_membership`."
---

`aws_iam_user_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.UserGroupMembershipConfig{
        Groups: []string{"example"},
        User: "example",
    }
    ubx.Resource(iam.UserGroupMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserGroupMembership } from
      "jsr:@ubx/sdk-aws/aws/iam/user_group_membership";

    resource(UserGroupMembership, "example", {
      groups: ["example"],
      user: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        UserGroupMembership,
        UserGroupMembershipConfig,
    )

    cfg = UserGroupMembershipConfig(
        groups=["example"],
        user="example",
    )
    ubx.resource(UserGroupMembership, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_user_group_membership with groups, user.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="groups" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
