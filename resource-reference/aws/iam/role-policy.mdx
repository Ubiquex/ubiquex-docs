---
title: "aws_iam_role_policy"
description: "`aws_iam_role_policy` attaches a real inline IAM policy directly to an `aws_iam_role`, embedded in the role itself rather than existing as a separate..."
---

`aws_iam_role_policy` attaches a real inline IAM policy directly to an `aws_iam_role`, embedded in the role itself rather than existing as a separate, reusable `aws_iam_policy` that could also be attached elsewhere.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own role policy"})

    		ubx.Resource(iam.RolePolicy, "ci-runner", iam.RolePolicyConfig{
    			PolicyName:     "example-policy-name",
    			RoleName:       "example-role-name",
    			PolicyDocument: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RolePolicy } from "./local-sdk/aws/sdk/typescript/aws/iam/role_policy.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own role policy" });

      resource(RolePolicy, "ci-runner", {
        policyName: "example-policy-name",
        roleName: "example-role-name",
        policyDocument: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.iam import RolePolicy, RolePolicyConfig

    def describe():
        ubx.intent("payments own role policy")

        ubx.resource(RolePolicy, "ci-runner", RolePolicyConfig(
            policy_name="example-policy-name",
            role_name="example-role-name",
            policy_document="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_role_policy, with policy name example-policy-name,
    role name example-role-name, policy document example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="policy_document" type="any">
  The policy document. You must provide policies in JSON format in IAM. However, for CFN templates formatted in YAML, you can provide the policy in JSON or YAML format. CFN always converts a YAML policy to JSON format before submitting it to IAM. The [regex pattern](https://docs.aws.amazon.com/http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following: + Any printable ASCII character ranging from the space character (``\u0020``) through the end of the ASCII character range + The printable characters in the Basic Latin and Latin-1 Supplement character set (through ``\u00FF``) + The special characters tab (``\u0009``), line feed (``\u000A``), and carriage return (``\u000D``) Optional.
</ResponseField>

<ResponseField name="policy_name" type="string" required>
  The name of the policy document. This parameter allows (through its [regex pattern](https://docs.aws.amazon.com/http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: _+=,.@- Required.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  The name of the role to associate the policy with. This parameter allows (through its [regex pattern](https://docs.aws.amazon.com/http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: _+=,.@- Required.
</ResponseField>
