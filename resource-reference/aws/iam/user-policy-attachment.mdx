---
title: "aws_iam_user_policy_attachment"
description: "Real, generated bindings for `aws_iam_user_policy_attachment`."
---

`aws_iam_user_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user policy attachment"})

    		ubx.Resource(iam.UserPolicyAttachment, "ci-runner", iam.UserPolicyAttachmentConfig{
    			PolicyArn: "arn:aws:iam::123456789012:policy/ci",
    			User:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPolicyAttachment } from "jsr:@ubx/sdk-aws/aws/iam/user_policy_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own user policy attachment" });

      resource(UserPolicyAttachment, "ci-runner", {
        policyArn: "arn:aws:iam::123456789012:policy/ci",
        user: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import UserPolicyAttachment, UserPolicyAttachmentConfig

    def describe():
        ubx.intent("payments own user policy attachment")

        ubx.resource(UserPolicyAttachment, "ci-runner", UserPolicyAttachmentConfig(
            policy_arn="arn:aws:iam::123456789012:policy/ci",
            user="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_user_policy_attachment, with policy arn
    arn:aws:iam::123456789012:policy/ci, user example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
