---
title: "aws_iam_user_policy_attachment"
description: "Real, generated bindings for `aws_iam_user_policy_attachment`."
---

`aws_iam_user_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.UserPolicyAttachmentConfig{
        PolicyArn: "example",
        User: "example",
    }
    ubx.Resource(iam.UserPolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserPolicyAttachment } from
      "jsr:@ubx/sdk-aws/aws/iam/user_policy_attachment";

    resource(UserPolicyAttachment, "example", {
      policyArn: "example",
      user: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        UserPolicyAttachment,
        UserPolicyAttachmentConfig,
    )

    cfg = UserPolicyAttachmentConfig(
        policy_arn="example",
        user="example",
    )
    ubx.resource(UserPolicyAttachment, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_user_policy_attachment with policy_arn, user.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
