---
title: "aws_iam_saml_provider"
description: "Real, generated bindings for `aws_iam_saml_provider`."
---

`aws_iam_saml_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.SamlProviderConfig{
        Name: "example-name",
        SamlMetadataDocument: "example",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(iam.SamlProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SamlProvider } from
      "jsr:@ubx/sdk-aws/aws/iam/saml_provider";

    resource(SamlProvider, "example", {
      name: "example",
      samlMetadataDocument: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import SamlProvider, SamlProviderConfig

    cfg = SamlProviderConfig(
        name="example",
        saml_metadata_document="example",
        tags={"key": "value"},
    )
    ubx.resource(SamlProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_saml_provider with name,
saml_metadata_document, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="saml_metadata_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="saml_provider_uuid" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="valid_until" type="string">
  Computed by AWS after `ship`.
</ResponseField>
