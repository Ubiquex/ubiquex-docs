---
title: "aws_iam_role_policies_exclusive"
description: "Real, generated bindings for `aws_iam_role_policies_exclusive`."
---

`aws_iam_role_policies_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own role policies exclusive"})

    		ubx.Resource(iam.RolePoliciesExclusive, "ci-runner", iam.RolePoliciesExclusiveConfig{
    			PolicyNames: []string{"example"},
    			RoleName:    "example-role-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RolePoliciesExclusive } from "jsr:@ubx/sdk-aws/aws/iam/role_policies_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own role policies exclusive" });

      resource(RolePoliciesExclusive, "ci-runner", {
        policyNames: ["example"],
        roleName: "example-role-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import RolePoliciesExclusive, RolePoliciesExclusiveConfig

    def describe():
        ubx.intent("payments own role policies exclusive")

        ubx.resource(RolePoliciesExclusive, "ci-runner", RolePoliciesExclusiveConfig(
            policy_names=["example"],
            role_name="example-role-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_role_policies_exclusive, with policy names a set
    value, role name example-role-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="policy_names" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  Required.
</ResponseField>

## Output properties


