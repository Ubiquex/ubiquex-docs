---
title: "aws_iam_role_policies_exclusive"
description: "Real, generated bindings for `aws_iam_role_policies_exclusive`."
---

`aws_iam_role_policies_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.RolePoliciesExclusiveConfig{
        PolicyNames: []string{"example"},
        RoleName: "example-role-name",
    }
    ubx.Resource(iam.RolePoliciesExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RolePoliciesExclusive } from
      "jsr:@ubx/sdk-aws/aws/iam/role_policies_exclusive";

    resource(RolePoliciesExclusive, "example", {
      policyNames: ["example"],
      roleName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        RolePoliciesExclusive,
        RolePoliciesExclusiveConfig,
    )

    cfg = RolePoliciesExclusiveConfig(
        policy_names=["example"],
        role_name="example",
    )
    ubx.resource(RolePoliciesExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_role_policies_exclusive with policy_names,
role_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_names" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
