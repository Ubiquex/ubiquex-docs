---
title: "aws_iam_group_policy_attachments_exclusive"
description: "Real, generated bindings for `aws_iam_group_policy_attachments_exclusive`."
---

`aws_iam_group_policy_attachments_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.GroupPolicyAttachmentsExclusiveConfig{
        GroupName: "example-group-name",
        PolicyArns: []string{"example"},
    }
    ubx.Resource(iam.GroupPolicyAttachmentsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { GroupPolicyAttachmentsExclusive } from
      "jsr:@ubx/sdk-aws/aws/iam/group_policy_attachments_exclusive";

    resource(GroupPolicyAttachmentsExclusive, "example", {
      groupName: "example",
      policyArns: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        GroupPolicyAttachmentsExclusive,
        GroupPolicyAttachmentsExclusiveConfig,
    )

    cfg = GroupPolicyAttachmentsExclusiveConfig(
        group_name="example",
        policy_arns=["example"],
    )
    ubx.resource(GroupPolicyAttachmentsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_group_policy_attachments_exclusive with
group_name, policy_arns.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_arns" type="set(string)" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
