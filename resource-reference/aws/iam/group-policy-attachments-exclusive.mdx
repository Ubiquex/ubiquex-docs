---
title: "aws_iam_group_policy_attachments_exclusive"
description: "Real, generated bindings for `aws_iam_group_policy_attachments_exclusive`."
---

`aws_iam_group_policy_attachments_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group policy attachments exclusive"})

    		ubx.Resource(iam.GroupPolicyAttachmentsExclusive, "ci-runner", iam.GroupPolicyAttachmentsExclusiveConfig{
    			GroupName:  "example-group-name",
    			PolicyArns: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupPolicyAttachmentsExclusive } from "jsr:@ubx/sdk-aws/aws/iam/group_policy_attachments_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own group policy attachments exclusive" });

      resource(GroupPolicyAttachmentsExclusive, "ci-runner", {
        groupName: "example-group-name",
        policyArns: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import GroupPolicyAttachmentsExclusive, GroupPolicyAttachmentsExclusiveConfig

    def describe():
        ubx.intent("payments own group policy attachments exclusive")

        ubx.resource(GroupPolicyAttachmentsExclusive, "ci-runner", GroupPolicyAttachmentsExclusiveConfig(
            group_name="example-group-name",
            policy_arns=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_group_policy_attachments_exclusive, with group
    name example-group-name, policy arns a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_arns" type="set(string)" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
