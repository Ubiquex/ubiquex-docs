---
title: "aws_iam_service_linked_role"
description: "Real, generated bindings for `aws_iam_service_linked_role`."
---

`aws_iam_service_linked_role` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.ServiceLinkedRoleConfig{
        AwsServiceName: "example-aws-service-name",
        CustomSuffix: "example",
        Description: "example",
    }
    ubx.Resource(iam.ServiceLinkedRole, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceLinkedRole } from
      "jsr:@ubx/sdk-aws/aws/iam/service_linked_role";

    resource(ServiceLinkedRole, "example", {
      awsServiceName: "example",
      customSuffix: "example",
      description: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        ServiceLinkedRole,
        ServiceLinkedRoleConfig,
    )

    cfg = ServiceLinkedRoleConfig(
        aws_service_name="example",
        custom_suffix="example",
        description="example",
    )
    ubx.resource(ServiceLinkedRole, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_service_linked_role with aws_service_name,
custom_suffix, description.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_suffix" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="path" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="unique_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
