---
title: "aws_iam_service_linked_role"
description: "Real, generated bindings for `aws_iam_service_linked_role`."
---

`aws_iam_service_linked_role` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service linked role"})

    		ubx.Resource(iam.ServiceLinkedRole, "ci-runner", iam.ServiceLinkedRoleConfig{
    			AwsServiceName: "example-aws-service-name",
    			CustomSuffix:   "example",
    			Description:    "Managed by ubx.",
    			Tags:           map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceLinkedRole } from "jsr:@ubx/sdk-aws/aws/iam/service_linked_role";

    export default stack("payments", () => {
      intent({ summary: "payments own service linked role" });

      resource(ServiceLinkedRole, "ci-runner", {
        awsServiceName: "example-aws-service-name",
        customSuffix: "example",
        description: "Managed by ubx.",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import ServiceLinkedRole, ServiceLinkedRoleConfig

    def describe():
        ubx.intent("payments own service linked role")

        ubx.resource(ServiceLinkedRole, "ci-runner", ServiceLinkedRoleConfig(
            aws_service_name="example-aws-service-name",
            custom_suffix="example",
            description="Managed by ubx.",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_service_linked_role, with aws service name
    example-aws-service-name, custom suffix example, description
    Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="aws_service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="custom_suffix" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="create_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="path" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="unique_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
