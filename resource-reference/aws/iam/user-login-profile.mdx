---
title: "aws_iam_user_login_profile"
description: "Real, generated bindings for `aws_iam_user_login_profile`."
---

`aws_iam_user_login_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.UserLoginProfileConfig{
        User: "example",
        PasswordLength: 1,
        PasswordResetRequired: true,
    }
    ubx.Resource(iam.UserLoginProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { UserLoginProfile } from
      "jsr:@ubx/sdk-aws/aws/iam/user_login_profile";

    resource(UserLoginProfile, "example", {
      user: "example",
      passwordLength: 1,
      passwordResetRequired: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        UserLoginProfile,
        UserLoginProfileConfig,
    )

    cfg = UserLoginProfileConfig(
        user="example",
        password_length=1,
        password_reset_required=True,
    )
    ubx.resource(UserLoginProfile, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_user_login_profile with user, password_length,
password_reset_required.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="password_length" type="number">
  Optional.
</ResponseField>

<ResponseField name="password_reset_required" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pgp_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="encrypted_password" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_fingerprint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="password" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="password_reset_required" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
