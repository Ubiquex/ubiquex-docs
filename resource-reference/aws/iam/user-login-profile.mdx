---
title: "aws_iam_user_login_profile"
description: "Real, generated bindings for `aws_iam_user_login_profile`."
---

`aws_iam_user_login_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user login profile"})

    		ubx.Resource(iam.UserLoginProfile, "ci-runner", iam.UserLoginProfileConfig{
    			User:           "example",
    			PasswordLength: 1,
    			PgpKey:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserLoginProfile } from "jsr:@ubx/sdk-aws/aws/iam/user_login_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own user login profile" });

      resource(UserLoginProfile, "ci-runner", {
        user: "example",
        passwordLength: 1,
        pgpKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import UserLoginProfile, UserLoginProfileConfig

    def describe():
        ubx.intent("payments own user login profile")

        ubx.resource(UserLoginProfile, "ci-runner", UserLoginProfileConfig(
            user="example",
            password_length=1,
            pgp_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_user_login_profile, with user example, password
    length 1, pgp key example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="password_length" type="number">
  Optional.
</ResponseField>

<ResponseField name="password_reset_required" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pgp_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="encrypted_password" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_fingerprint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="password" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="password_reset_required" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
