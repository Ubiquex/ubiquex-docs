---
title: "aws_iam_user"
description: "Real, generated bindings for `aws_iam_user`."
---

`aws_iam_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.UserConfig{
        Name: "example-name",
        ForceDestroy: true,
        Path: "example",
    }
    ubx.Resource(iam.User, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { User } from "jsr:@ubx/sdk-aws/aws/iam/user";

    resource(User, "example", {
      name: "example",
      forceDestroy: true,
      path: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import User, UserConfig

    cfg = UserConfig(
        name="example",
        force_destroy=True,
        path="example",
    )
    ubx.resource(User, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_user with name, force_destroy, path.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="force_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="path" type="string">
  Optional.
</ResponseField>

<ResponseField name="permissions_boundary" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="unique_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
