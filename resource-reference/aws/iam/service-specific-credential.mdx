---
title: "aws_iam_service_specific_credential"
description: "Real, generated bindings for `aws_iam_service_specific_credential`."
---

`aws_iam_service_specific_credential` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.ServiceSpecificCredentialConfig{
        ServiceName: "example-service-name",
        UserName: "example-user-name",
        CredentialAgeDays: 1,
        Status: "example",
    }
    ubx.Resource(iam.ServiceSpecificCredential, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { ServiceSpecificCredential } from
      "jsr:@ubx/sdk-aws/aws/iam/service_specific_credential";

    resource(ServiceSpecificCredential, "example", {
      serviceName: "example",
      userName: "example",
      credentialAgeDays: 1,
      status: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        ServiceSpecificCredential,
        ServiceSpecificCredentialConfig,
    )

    cfg = ServiceSpecificCredentialConfig(
        service_name="example",
        user_name="example",
        credential_age_days=1,
        status="example",
    )
    ubx.resource(ServiceSpecificCredential, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_service_specific_credential with service_name,
user_name, credential_age_days, status.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="credential_age_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="create_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_credential_alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_credential_secret" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_password" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_specific_credential_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_user_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>
