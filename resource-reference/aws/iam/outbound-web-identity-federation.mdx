---
title: "aws_iam_outbound_web_identity_federation"
description: "Real, generated bindings for `aws_iam_outbound_web_identity_federation`."
---

`aws_iam_outbound_web_identity_federation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own outbound web identity federation"})

    		ubx.Resource(iam.OutboundWebIdentityFederation, "ci-runner", iam.OutboundWebIdentityFederationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OutboundWebIdentityFederation } from "jsr:@ubx/sdk-aws/aws/iam/outbound_web_identity_federation";

    export default stack("payments", () => {
      intent({ summary: "payments own outbound web identity federation" });

      resource(OutboundWebIdentityFederation, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import OutboundWebIdentityFederation, OutboundWebIdentityFederationConfig

    def describe():
        ubx.intent("payments own outbound web identity federation")

        ubx.resource(OutboundWebIdentityFederation, "ci-runner", OutboundWebIdentityFederationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_outbound_web_identity_federation, with .
    ```
  </Tab>
</Tabs>

## Input properties

This resource type has no settable input properties.

## Output properties


<ResponseField name="issuer_identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>
