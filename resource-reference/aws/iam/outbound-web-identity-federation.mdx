---
title: "aws_iam_outbound_web_identity_federation"
description: "Real, generated bindings for `aws_iam_outbound_web_identity_federation`."
---

`aws_iam_outbound_web_identity_federation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.OutboundWebIdentityFederationConfig{
    }
    ubx.Resource(iam.OutboundWebIdentityFederation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OutboundWebIdentityFederation } from
      "jsr:@ubx/sdk-aws/aws/iam/outbound_web_identity_federation";

    resource(OutboundWebIdentityFederation, "example", {
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        OutboundWebIdentityFederation,
        OutboundWebIdentityFederationConfig,
    )

    cfg = OutboundWebIdentityFederationConfig(
    )
    ubx.resource(OutboundWebIdentityFederation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_outbound_web_identity_federation.
    ```
  </Tab>
</Tabs>

## Input properties

This resource type has no settable input properties.

## Output properties


<ResponseField name="issuer_identifier" type="string">
  Computed by AWS after `ship`.
</ResponseField>
