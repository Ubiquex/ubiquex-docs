---
title: "aws_iam_openid_connect_provider"
description: "Real, generated bindings for `aws_iam_openid_connect_provider`."
---

`aws_iam_openid_connect_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.OpenidConnectProviderConfig{
        ClientIdList: []string{"example"},
        Url: "example",
        Tags: map[string]string{"key": "value"},
        ThumbprintList: []string{"example"},
    }
    ubx.Resource(iam.OpenidConnectProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { OpenidConnectProvider } from
      "jsr:@ubx/sdk-aws/aws/iam/openid_connect_provider";

    resource(OpenidConnectProvider, "example", {
      clientIdList: ["example"],
      url: "example",
      tags: { key: "value" },
      thumbprintList: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        OpenidConnectProvider,
        OpenidConnectProviderConfig,
    )

    cfg = OpenidConnectProviderConfig(
        client_id_list=["example"],
        url="example",
        tags={"key": "value"},
        thumbprint_list=["example"],
    )
    ubx.resource(OpenidConnectProvider, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_openid_connect_provider with client_id_list,
url, tags, thumbprint_list.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="client_id_list" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thumbprint_list" type="list(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thumbprint_list" type="list(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
