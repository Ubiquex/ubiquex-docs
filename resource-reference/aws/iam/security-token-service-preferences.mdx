---
title: "aws_iam_security_token_service_preferences"
description: "Real, generated bindings for `aws_iam_security_token_service_preferences`."
---

`aws_iam_security_token_service_preferences` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security token service preferences"})

    		ubx.Resource(iam.SecurityTokenServicePreferences, "ci-runner", iam.SecurityTokenServicePreferencesConfig{
    			GlobalEndpointTokenVersion: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityTokenServicePreferences } from "jsr:@ubx/sdk-aws/aws/iam/security_token_service_preferences";

    export default stack("payments", () => {
      intent({ summary: "payments own security token service preferences" });

      resource(SecurityTokenServicePreferences, "ci-runner", {
        globalEndpointTokenVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import SecurityTokenServicePreferences, SecurityTokenServicePreferencesConfig

    def describe():
        ubx.intent("payments own security token service preferences")

        ubx.resource(SecurityTokenServicePreferences, "ci-runner", SecurityTokenServicePreferencesConfig(
            global_endpoint_token_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_security_token_service_preferences, with global
    endpoint token version example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="global_endpoint_token_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
