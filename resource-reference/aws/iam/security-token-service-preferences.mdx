---
title: "aws_iam_security_token_service_preferences"
description: "Real, generated bindings for `aws_iam_security_token_service_preferences`."
---

`aws_iam_security_token_service_preferences` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.SecurityTokenServicePreferencesConfig{
        GlobalEndpointTokenVersion: "example",
    }
    ubx.Resource(iam.SecurityTokenServicePreferences, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SecurityTokenServicePreferences } from
      "jsr:@ubx/sdk-aws/aws/iam/security_token_service_preferences";

    resource(SecurityTokenServicePreferences, "example", {
      globalEndpointTokenVersion: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        SecurityTokenServicePreferences,
        SecurityTokenServicePreferencesConfig,
    )

    cfg = SecurityTokenServicePreferencesConfig(
        global_endpoint_token_version="example",
    )
    ubx.resource(SecurityTokenServicePreferences, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_security_token_service_preferences with
global_endpoint_token_version.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="global_endpoint_token_version" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
