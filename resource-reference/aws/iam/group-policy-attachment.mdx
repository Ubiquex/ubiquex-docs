---
title: "aws_iam_group_policy_attachment"
description: "Real, generated bindings for `aws_iam_group_policy_attachment`."
---

`aws_iam_group_policy_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group policy attachment"})

    		ubx.Resource(iam.GroupPolicyAttachment, "ci-runner", iam.GroupPolicyAttachmentConfig{
    			Group:     "example",
    			PolicyArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupPolicyAttachment } from "jsr:@ubx/sdk-aws/aws/iam/group_policy_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own group policy attachment" });

      resource(GroupPolicyAttachment, "ci-runner", {
        group: "example",
        policyArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import GroupPolicyAttachment, GroupPolicyAttachmentConfig

    def describe():
        ubx.intent("payments own group policy attachment")

        ubx.resource(GroupPolicyAttachment, "ci-runner", GroupPolicyAttachmentConfig(
            group="example",
            policy_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_group_policy_attachment, with group example,
    policy arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="group" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
