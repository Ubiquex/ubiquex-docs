---
title: "aws_iam_access_key"
description: "Real, generated bindings for `aws_iam_access_key`."
---

`aws_iam_access_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access key"})

    		ubx.Resource(iam.AccessKey, "ci-runner", iam.AccessKeyConfig{
    			User:   "example",
    			PgpKey: "example",
    			Status: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessKey } from "jsr:@ubx/sdk-aws/aws/iam/access_key";

    export default stack("payments", () => {
      intent({ summary: "payments own access key" });

      resource(AccessKey, "ci-runner", {
        user: "example",
        pgpKey: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import AccessKey, AccessKeyConfig

    def describe():
        ubx.intent("payments own access key")

        ubx.resource(AccessKey, "ci-runner", AccessKeyConfig(
            user="example",
            pgp_key="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_access_key, with user example, pgp key example,
    status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pgp_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="user" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="create_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encrypted_secret" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encrypted_ses_smtp_password_v4" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_fingerprint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="secret" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ses_smtp_password_v4" type="string">
  Computed by AWS after `ship`.
</ResponseField>
