---
title: "aws_iam_role_policy_attachments_exclusive"
description: "Real, generated bindings for `aws_iam_role_policy_attachments_exclusive`."
---

`aws_iam_role_policy_attachments_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"

    cfg := iam.RolePolicyAttachmentsExclusiveConfig{
        PolicyArns: []string{"example"},
        RoleName: "example-role-name",
    }
    ubx.Resource(iam.RolePolicyAttachmentsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { RolePolicyAttachmentsExclusive } from
      "jsr:@ubx/sdk-aws/aws/iam/role_policy_attachments_exclusive";

    resource(RolePolicyAttachmentsExclusive, "example", {
      policyArns: ["example"],
      roleName: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        RolePolicyAttachmentsExclusive,
        RolePolicyAttachmentsExclusiveConfig,
    )

    cfg = RolePolicyAttachmentsExclusiveConfig(
        policy_arns=["example"],
        role_name="example",
    )
    ubx.resource(RolePolicyAttachmentsExclusive, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_role_policy_attachments_exclusive with
policy_arns, role_name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
