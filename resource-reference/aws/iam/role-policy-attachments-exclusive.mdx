---
title: "aws_iam_role_policy_attachments_exclusive"
description: "Real, generated bindings for `aws_iam_role_policy_attachments_exclusive`."
---

`aws_iam_role_policy_attachments_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own role policy attachments exclusive"})

    		ubx.Resource(iam.RolePolicyAttachmentsExclusive, "ci-runner", iam.RolePolicyAttachmentsExclusiveConfig{
    			PolicyArns: []string{"example"},
    			RoleName:   "example-role-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RolePolicyAttachmentsExclusive } from "jsr:@ubx/sdk-aws/aws/iam/role_policy_attachments_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own role policy attachments exclusive" });

      resource(RolePolicyAttachmentsExclusive, "ci-runner", {
        policyArns: ["example"],
        roleName: "example-role-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import RolePolicyAttachmentsExclusive, RolePolicyAttachmentsExclusiveConfig

    def describe():
        ubx.intent("payments own role policy attachments exclusive")

        ubx.resource(RolePolicyAttachmentsExclusive, "ci-runner", RolePolicyAttachmentsExclusiveConfig(
            policy_arns=["example"],
            role_name="example-role-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_role_policy_attachments_exclusive, with policy
    arns a set value, role name example-role-name.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="policy_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="role_name" type="string" required>
  Required.
</ResponseField>

## Output properties

This resource type has no computed output properties.
