---
title: "aws_iam_account_password_policy"
description: "Real, generated bindings for `aws_iam_account_password_policy`."
---

`aws_iam_account_password_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account password policy"})

    		ubx.Resource(iam.AccountPasswordPolicy, "ci-runner", iam.AccountPasswordPolicyConfig{
    			AllowUsersToChangePassword: true,
    			MinimumPasswordLength:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountPasswordPolicy } from "jsr:@ubx/sdk-aws/aws/iam/account_password_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own account password policy" });

      resource(AccountPasswordPolicy, "ci-runner", {
        allowUsersToChangePassword: true,
        minimumPasswordLength: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import AccountPasswordPolicy, AccountPasswordPolicyConfig

    def describe():
        ubx.intent("payments own account password policy")

        ubx.resource(AccountPasswordPolicy, "ci-runner", AccountPasswordPolicyConfig(
            allow_users_to_change_password=True,
            minimum_password_length=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_account_password_policy, with allow users to
    change password true, minimum password length 1.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="allow_users_to_change_password" type="bool">
  Optional.
</ResponseField>

<ResponseField name="hard_expiry" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_password_age" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="minimum_password_length" type="number">
  Optional.
</ResponseField>

<ResponseField name="password_reuse_prevention" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_lowercase_characters" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_numbers" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_symbols" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_uppercase_characters" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="expire_passwords" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hard_expiry" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_password_age" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="password_reuse_prevention" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_lowercase_characters" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_numbers" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_symbols" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="require_uppercase_characters" type="bool">
  Optional; if omitted, computed by AWS.
</ResponseField>
