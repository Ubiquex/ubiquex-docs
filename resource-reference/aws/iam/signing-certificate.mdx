---
title: "aws_iam_signing_certificate"
description: "Real, generated bindings for `aws_iam_signing_certificate`."
---

`aws_iam_signing_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/iam"

    cfg := iam.SigningCertificateConfig{
        CertificateBody: "example",
        UserName: "example-user-name",
        Status: "example",
    }
    ubx.Resource(iam.SigningCertificate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SigningCertificate } from
      "jsr:@ubx/sdk-aws/aws/iam/signing_certificate";

    resource(SigningCertificate, "example", {
      certificateBody: "example",
      userName: "example",
      status: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.iam import (
        SigningCertificate,
        SigningCertificateConfig,
    )

    cfg = SigningCertificateConfig(
        certificate_body="example",
        user_name="example",
        status="example",
    )
    ubx.resource(SigningCertificate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_iam_signing_certificate with certificate_body,
user_name, status.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="certificate_body" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="certificate_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
