---
title: "aws_iam_signing_certificate"
description: "Real, generated bindings for `aws_iam_signing_certificate`."
---

`aws_iam_signing_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own signing certificate"})

    		ubx.Resource(iam.SigningCertificate, "ci-runner", iam.SigningCertificateConfig{
    			CertificateBody: "example",
    			UserName:        "example-user-name",
    			Status:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SigningCertificate } from "jsr:@ubx/sdk-aws/aws/iam/signing_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own signing certificate" });

      resource(SigningCertificate, "ci-runner", {
        certificateBody: "example",
        userName: "example-user-name",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import SigningCertificate, SigningCertificateConfig

    def describe():
        ubx.intent("payments own signing certificate")

        ubx.resource(SigningCertificate, "ci-runner", SigningCertificateConfig(
            certificate_body="example",
            user_name="example-user-name",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_signing_certificate, with certificate body
    example, user name example-user-name, status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_body" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="certificate_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
