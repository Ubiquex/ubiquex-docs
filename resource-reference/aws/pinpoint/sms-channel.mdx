---
title: "aws_pinpoint_sms_channel"
description: "Real, generated bindings for `aws_pinpoint_sms_channel`."
---

`aws_pinpoint_sms_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/pinpoint"

    cfg := pinpoint.SmsChannelConfig{
        ApplicationId: "example",
        Enabled: true,
        SenderId: "example",
    }
    ubx.Resource(pinpoint.SmsChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { SmsChannel } from
      "jsr:@ubx/sdk-aws/aws/pinpoint/sms_channel";

    resource(SmsChannel, "example", {
      applicationId: "example",
      enabled: true,
      senderId: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.pinpoint import SmsChannel, SmsChannelConfig

    cfg = SmsChannelConfig(
        application_id="example",
        enabled=True,
        sender_id="example",
    )
    ubx.resource(SmsChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_pinpoint_sms_channel with application_id, enabled,
sender_id.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sender_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="short_code" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="promotional_messages_per_second" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transactional_messages_per_second" type="number">
  Computed by AWS after `ship`.
</ResponseField>
