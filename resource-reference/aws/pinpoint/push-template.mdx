---
title: "aws_pinpoint_push_template"
description: "`aws_pinpoint_push_template` creates a real, reusable push-notification template shared across an `aws_pinpoint_app`'s push channels, ADM, APNs, Baidu..."
---

`aws_pinpoint_push_template` creates a real, reusable push-notification template shared across an `aws_pinpoint_app`'s push channels, ADM, APNs, Baidu, GCM, subject to the same October 30, 2026 Pinpoint retirement.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own push template"})

    		ubx.Resource(pinpoint.PushTemplate, "ci-runner", pinpoint.PushTemplateConfig{
    			TemplateName:         "example-template-name",
    			Adm:                  map[string]any{"action": "example"},
    			Apns:                 map[string]any{"action": "example"},
    			Baidu:                map[string]any{"action": "example"},
    			Default:              map[string]any{"action": "example"},
    			DefaultSubstitutions: "example",
    			Gcm:                  map[string]any{"action": "example"},
    			Tags:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { PushTemplate } from "./local-sdk/aws/sdk/typescript/aws/pinpoint/push_template.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own push template" });

      resource(PushTemplate, "ci-runner", {
        templateName: "example-template-name",
        adm: { action: "example" },
        apns: { action: "example" },
        baidu: { action: "example" },
        default: { action: "example" },
        defaultSubstitutions: "example",
        gcm: { action: "example" },
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.pinpoint import PushTemplate, PushTemplateConfig

    def describe():
        ubx.intent("payments own push template")

        ubx.resource(PushTemplate, "ci-runner", PushTemplateConfig(
            template_name="example-template-name",
            adm={"action": "example"},
            apns={"action": "example"},
            baidu={"action": "example"},
            default={"action": "example"},
            default_substitutions="example",
            gcm={"action": "example"},
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpoint_push_template, with template name
    example-template-name, adm a set value, apns a set value, baidu a
    set value, default a set value, default substitutions example,
    gcm a set value, tags example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="adm" type="object">
  Defines the Amazon Device Messenger (ADM) push notification settings, such as the action, title, body, and URL, used for delivering messages to Fire OS devices. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Specifies the action that occurs when a user taps the push notification delivered via ADM, with valid values OPEN_APP, DEEP_LINK, or URL. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="body" type="string">
      The message body text that is sent to Amazon Device Messaging (ADM) endpoints as the push notification content. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_icon_url" type="string">
      For Amazon Device Messenger (ADM) push notifications, this is the URL of the icon image to display in the notification. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_url" type="string">
      The URL of the image to display in the push notification sent to ADM (Amazon Device Messenger) endpoints. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="small_image_icon_url" type="string">
      The URL of the small icon image to display in the status bar or as a small icon in notifications sent through Amazon Device Messaging (ADM) from this push template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sound" type="string">
      For Amazon Device Messaging (ADM) push notifications, this field specifies the name of the sound file to play when the message is received. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="title" type="string">
      This field specifies the title of the push notification that Amazon Pinpoint sends to Amazon Device Messaging (ADM) endpoints using this template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      Specifies the URL to open when a user taps the push notification delivered through Amazon Device Messaging (ADM). Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="apns" type="object">
  The Apple Push Notification Service (APNs) settings for this push template, defining the notification content and behavior for iOS devices. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      The action to take when a user taps the notification sent via APNs; valid values are OPEN_APP, DEEP_LINK, or URL. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="body" type="string">
      Specifies the message body (the text content) of the push notification sent to Apple devices through the APNs channel. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="media_url" type="string">
      The URL of the image, video, or other media file to include in the push notification delivered to Apple Push Notification Service (APNS) endpoints. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sound" type="string">
      The name of the sound file to play when an APNs push notification is received, such as 'default' or a custom sound file. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="title" type="string">
      The title of the push notification message sent to Apple Push Notification Service (APNS) endpoints. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The URL to be opened when an iOS user taps the APNS push notification, applicable when the message action is set to URL. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="baidu" type="object">
  Specifies the push notification settings for the Baidu channel, including message body, title, action, and other platform-specific options for Android devices in China. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Specifies the action to take when a user taps a push notification sent via the Baidu channel, with supported values including `OPEN_APP`, `DEEP_LINK`, and `URL`. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="body" type="string">
      The message body of the push notification delivered through the Baidu channel (Baidu Cloud Push) for Android devices in China, used in the push template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_icon_url" type="string">
      Specifies the URL of a large image icon to display for Baidu push notifications when using this template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_url" type="string">
      The image_url field specifies the URL of the image to display in the Baidu push notification. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="small_image_icon_url" type="string">
      The URL of the small icon image that appears in notifications delivered through the Baidu channel for this push template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sound" type="string">
      The sound to play on the device when the Baidu push notification is delivered. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="title" type="string">
      The title text for the Baidu push notification message, which appears as the message's headline or subject. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The URL that the Baidu push notification opens in the app when tapped, used when the notification Action is set to open a URL rather than the app or a deep link. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default" type="object">
  The default message data that applies to all message types and platforms, including the message title, body, and other push notification settings. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Specifies the action to occur if a recipient taps the push notification, such as opening the app, opening a deep link, or opening a URL. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="body" type="string">
      Sets the body text of the push notification message, which is the main content displayed to the user when the notification is delivered. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sound" type="string">
      Specifies the sound file name or audio resource to play when the app receives the push notification, interpreted according to the mobile platform (for example, an iOS sound filename or an Android notification sound resource). Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="title" type="string">
      The default title of the push notification, used when no platform-specific override is provided. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      In the default push notification template for an AWS Pinpoint push template, this URL specifies the web address that is opened when the user taps the notification, typically used when the notification's action is set to 'URL'. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="default_substitutions" type="string">
  A JSON string defining the default key-value pairs that map message template variables to their default substitution values for the push notification template. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="gcm" type="object">
  Specifies the push notification template settings for Android devices via the Google Cloud Messaging (GCM) channel, including message content, action, and Android-specific delivery options. Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="action" type="string">
      Determines the action that occurs when a user taps a push notification sent via GCM (Android), with possible values OPEN_APP, DEEP_LINK, and URL. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="body" type="string">
      The message body of the push notification delivered to GCM (Google/Firebase Cloud Messaging) recipients. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_icon_url" type="string">
      The URL of the image icon to display in the push notification for the GCM (Google) channel. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="image_url" type="string">
      The URL of the image to display in the push notification for the GCM (Firebase Cloud Messaging) channel of the PushTemplate. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="small_image_icon_url" type="string">
      The URL of the small icon image displayed for Google Cloud Messaging (GCM) push notifications in the AWS Pinpoint push template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="sound" type="string">
      Specifies the sound to play when the push notification is received on an Android device, either as 'default' or the file name of a sound resource bundled with the app. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="title" type="string">
      The title of the push notification sent to GCM (Google Cloud Messaging / Firebase Cloud Messaging) recipients for this Pinpoint push template. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The URL to open when a recipient taps the push notification delivered to Android devices through the GCM/FCM channel. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="any">
  Assigns metadata to the Amazon Pinpoint push notification template in the form of key-value pairs that can be used to categorize and manage the template. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="template_description" type="string">
  A description of the push notification template, used to identify the template in the Amazon Pinpoint console. Optional. **(AI-inferred)**
</ResponseField>

<ResponseField name="template_name" type="string" required>
  The name of the push notification template, which uniquely identifies it within your AWS account and must be unique among all templates in your Amazon Pinpoint project. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  The Amazon Resource Name (ARN) uniquely identifying the push notification template in AWS Pinpoint. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="string">
  The id is the name of the push template, which uniquely identifies it within the AWS account. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
