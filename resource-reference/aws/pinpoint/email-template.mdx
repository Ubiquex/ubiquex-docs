---
title: "aws_pinpoint_email_template"
description: "Real, generated bindings for `aws_pinpoint_email_template`."
---

`aws_pinpoint_email_template` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws-go/aws/pinpoint"

    cfg := pinpoint.EmailTemplateConfig{
        TemplateName: "example-template-name",
        Tags: map[string]string{"key": "value"},
    }
    ubx.Resource(pinpoint.EmailTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EmailTemplate } from
      "jsr:@ubx/sdk-aws/aws/pinpoint/email_template";

    resource(EmailTemplate, "example", {
      templateName: "example",
      tags: { key: "value" },
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.pinpoint import EmailTemplate, EmailTemplateConfig

    cfg = EmailTemplateConfig(
        template_name="example",
        tags={"key": "value"},
    )
    ubx.resource(EmailTemplate, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_pinpoint_email_template with template_name, tags.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="template_name" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
