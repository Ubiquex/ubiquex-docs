---
title: "aws_pinpoint_gcm_channel"
description: "Real, generated bindings for `aws_pinpoint_gcm_channel`."
---

`aws_pinpoint_gcm_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own gcm channel"})

    		ubx.Resource(pinpoint.GcmChannel, "ci-runner", pinpoint.GcmChannelConfig{
    			ApplicationId:               "example",
    			ApiKey:                      "example",
    			DefaultAuthenticationMethod: "example",
    			Enabled:                     true,
    			ServiceJson:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GcmChannel } from "jsr:@ubx/sdk-aws/aws/pinpoint/gcm_channel";

    export default stack("payments", () => {
      intent({ summary: "payments own gcm channel" });

      resource(GcmChannel, "ci-runner", {
        applicationId: "example",
        apiKey: "example",
        defaultAuthenticationMethod: "example",
        enabled: true,
        serviceJson: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pinpoint import GcmChannel, GcmChannelConfig

    def describe():
        ubx.intent("payments own gcm channel")

        ubx.resource(GcmChannel, "ci-runner", GcmChannelConfig(
            application_id="example",
            api_key="example",
            default_authentication_method="example",
            enabled=True,
            service_json="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpoint_gcm_channel, with application id example, api
    key example, default authentication method example, enabled true,
    service json example.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="api_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="default_authentication_method" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_json" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
