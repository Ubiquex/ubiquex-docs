---
title: "aws_pinpoint_email_configuration_set_event_destination"
description: "`aws_pinpoint_email_configuration_set_event_destination` routes real email sending events, opens, bounces, complaints..."
---

`aws_pinpoint_email_configuration_set_event_destination` routes real email sending events, opens, bounces, complaints, from an `aws_pinpoint_email_configuration_set` to a destination such as CloudWatch or Kinesis Data Firehose, subject to the same October 30, 2026 Pinpoint retirement.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only aws --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-aws/sdk/go => ./local-sdk/aws/sdk/go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own email configuration set event destination"})

    		ubx.Resource(pinpoint.EmailConfigurationSetEventDestination, "example", pinpoint.EmailConfigurationSetEventDestinationConfig{
    			ConfigurationSetName: "example-configuration-set-name",
    			EventDestinationName: "example-event-destination-name",
    			EventDestination:     map[string]any{"matching_event_types": []string{"example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only aws --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { EmailConfigurationSetEventDestination } from "./local-sdk/aws/sdk/typescript/aws/pinpoint/email_configuration_set_event_destination.ts";

    export default stack("example", () => {
      intent({ summary: "example own email configuration set event destination" });

      resource(EmailConfigurationSetEventDestination, "example", {
        configurationSetName: "example-configuration-set-name",
        eventDestinationName: "example-event-destination-name",
        eventDestination: { matchingEventTypes: ["example"] },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only aws --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/aws/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.aws.pinpoint import EmailConfigurationSetEventDestination, EmailConfigurationSetEventDestinationConfig

    def describe():
        ubx.intent("example own email configuration set event destination")

        ubx.resource(EmailConfigurationSetEventDestination, "example", EmailConfigurationSetEventDestinationConfig(
            configuration_set_name="example-configuration-set-name",
            event_destination_name="example-event-destination-name",
            event_destination={"matching_event_types": ["example"]},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpoint_email_configuration_set_event_destination,
    with configuration set name example-configuration-set-name, event
    destination name example-event-destination-name, event
    destination a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="configuration_set_name" type="string" required>
  The name of the Amazon Pinpoint configuration set that this event destination is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="event_destination" type="object">
  Defines the event destination for the email configuration set, specifying where Amazon Pinpoint publishes email event data (such as deliveries, bounces, and complaints) based on matching event types and the chosen destination type (e.g., CloudWatch Logs, Kinesis Data Firehose, or SNS). Optional. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="cloud_watch_destination" type="object">
      Configures the CloudWatch Logs destination for the event destination, including dimension configurations that define how email event metrics are grouped and published to CloudWatch. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="dimension_configurations" type="list(object)">
          Specifies the list of dimension configurations that define how Pinpoint adds custom dimensions (with a dimension name, value source, and default value) to the CloudWatch metrics when event destinations publish events to CloudWatch. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="default_dimension_value" type="string">
              The default value to use for the CloudWatch metric dimension when the source event does not provide a value for the dimension configuration, so the metric always has a dimension value. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dimension_name" type="string">
              The name of the CloudWatch dimension to attach to the event destination's metrics for this dimension configuration. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="dimension_value_source" type="string">
              Specifies the source of the dimension value (e.g., MESSAGE_TYPE or EVENT_TYPE) that determines which email event metadata is used to populate the CloudWatch metric dimension. Optional; if omitted, computed by AWS. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool">
      Determines whether the event destination is active, controlling whether Amazon Pinpoint delivers event data (such as sends, bounces, or complaints) to the associated destination like an SNS topic, CloudWatch Logs, or Kinesis Firehose. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="kinesis_firehose_destination" type="object">
      Configures the Amazon Kinesis Data Firehose delivery stream settings that Amazon Pinpoint uses to send event data when this event destination is triggered. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="delivery_stream_arn" type="string" required>
          The ARN of the Amazon Kinesis Data Firehose delivery stream that Amazon Pinpoint publishes event data to for this event destination. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="iam_role_arn" type="string" required>
          The ARN of the IAM role that Amazon Pinpoint assumes to send event data to the specified Kinesis Firehose delivery stream. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="matching_event_types" type="list(string)" required>
      Specifies the email event types (such as OPEN, CLICK, BOUNCE, COMPLAINT, DELIVERY, REJECT, or RENDER_FAILURE) that trigger this event destination to process events. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pinpoint_destination" type="object">
      Specifies the Amazon Pinpoint application ID (ApplicationId) that will receive event notifications from this configuration set. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="application_arn" type="string">
          The Amazon Resource Name (ARN) of the Amazon Pinpoint application (project) to which the email configuration set's event destination sends event data. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sns_destination" type="object">
      Specifies the Amazon SNS topic (via its TopicArn) that receives email event notifications from this event destination. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="topic_arn" type="string" required>
          The ARN of the Amazon SNS topic where the event destination publishes email event notifications for the Amazon Pinpoint configuration set. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="event_destination_name" type="string" required>
  The name of the event destination, which uniquely identifies it within the Amazon Pinpoint email configuration set. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
