---
title: "aws_pinpoint_event_stream"
description: "Real, generated bindings for `aws_pinpoint_event_stream`."
---

`aws_pinpoint_event_stream` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"

    cfg := pinpoint.EventStreamConfig{
        ApplicationId: "example",
        DestinationStreamArn: "example",
        RoleArn: "example",
    }
    ubx.Resource(pinpoint.EventStream, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EventStream } from
      "jsr:@ubx/sdk-aws/aws/pinpoint/event_stream";

    resource(EventStream, "example", {
      applicationId: "example",
      destinationStreamArn: "example",
      roleArn: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.pinpoint import EventStream, EventStreamConfig

    cfg = EventStreamConfig(
        application_id="example",
        destination_stream_arn="example",
        role_arn="example",
    )
    ubx.resource(EventStream, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_pinpoint_event_stream with application_id,
destination_stream_arn, role_arn.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_stream_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
