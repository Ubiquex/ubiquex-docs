---
title: "aws_pinpoint_event_stream"
description: "`aws_pinpoint_event_stream` streams a real `aws_pinpoint_app`'s app and campaign events to a Kinesis stream or Firehose delivery stream..."
---

`aws_pinpoint_event_stream` streams a real `aws_pinpoint_app`'s app and campaign events to a Kinesis stream or Firehose delivery stream, subject to the same October 30, 2026 Pinpoint retirement as the rest of its analytics surface.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/v2/aws/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own event stream"})

    		ubx.Resource(pinpoint.EventStream, "example", pinpoint.EventStreamConfig{
    			ApplicationId:        "example",
    			DestinationStreamArn: "arn:aws:iam::123456789012:policy/example",
    			RoleArn:              "arn:aws:iam::123456789012:policy/example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventStream } from "@ubx/sdk-aws/aws/pinpoint/event_stream";

    export default stack("example", () => {
      intent({ summary: "example own event stream" });

      resource(EventStream, "example", {
        applicationId: "example",
        destinationStreamArn: "arn:aws:iam::123456789012:policy/example",
        roleArn: "arn:aws:iam::123456789012:policy/example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pinpoint.event_stream import EventStream, EventStreamConfig

    def describe():
        ubx.intent("example own event stream")

        ubx.resource(EventStream, "example", EventStreamConfig(
            application_id="example",
            destination_stream_arn="arn:aws:iam::123456789012:policy/example",
            role_arn="arn:aws:iam::123456789012:policy/example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpoint_event_stream, with application id example,
    destination stream arn arn:aws:iam::123456789012:policy/example,
    role arn arn:aws:iam::123456789012:policy/example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  The unique identifier (ID) of the Amazon Pinpoint application that the event stream is associated with. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="destination_stream_arn" type="string" required>
  The ARN of the Amazon Kinesis Data Stream or Amazon Data Firehose delivery stream that receives the Pinpoint event stream data. Required. **(AI-inferred)**
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  The ARN of the IAM role that authorizes Amazon Pinpoint to write event data to the configured Amazon Kinesis stream. Required. **(AI-inferred)**
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  The application ID of the Amazon Pinpoint application that this event stream is associated with. Computed by AWS after `ship`. **(AI-inferred)**
</ResponseField>
