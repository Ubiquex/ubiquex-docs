---
title: "aws_pinpoint_email_channel"
description: "Real, generated bindings for `aws_pinpoint_email_channel`."
---

`aws_pinpoint_email_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"

    cfg := pinpoint.EmailChannelConfig{
        ApplicationId: "example",
        FromAddress: "example",
        Identity: "example",
        ConfigurationSet: "example",
        Enabled: true,
    }
    ubx.Resource(pinpoint.EmailChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { EmailChannel } from
      "jsr:@ubx/sdk-aws/aws/pinpoint/email_channel";

    resource(EmailChannel, "example", {
      applicationId: "example",
      fromAddress: "example",
      identity: "example",
      configurationSet: "example",
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.pinpoint import EmailChannel, EmailChannelConfig

    cfg = EmailChannelConfig(
        application_id="example",
        from_address="example",
        identity="example",
        configuration_set="example",
        enabled=True,
    )
    ubx.resource(EmailChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_pinpoint_email_channel with application_id,
from_address, identity, configuration_set, enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="configuration_set" type="string">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="from_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity" type="string" required>
  Required.
</ResponseField>

<ResponseField name="orchestration_sending_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="messages_per_second" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
