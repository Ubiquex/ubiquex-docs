---
title: "aws_pinpoint_adm_channel"
description: "Real, generated bindings for `aws_pinpoint_adm_channel`."
---

`aws_pinpoint_adm_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own adm channel"})

    		ubx.Resource(pinpoint.AdmChannel, "ci-runner", pinpoint.AdmChannelConfig{
    			ApplicationId: "example",
    			ClientId:      "example",
    			ClientSecret:  "example",
    			Enabled:       true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AdmChannel } from "jsr:@ubx/sdk-aws/aws/pinpoint/adm_channel";

    export default stack("payments", () => {
      intent({ summary: "payments own adm channel" });

      resource(AdmChannel, "ci-runner", {
        applicationId: "example",
        clientId: "example",
        clientSecret: "example",
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pinpoint import AdmChannel, AdmChannelConfig

    def describe():
        ubx.intent("payments own adm channel")

        ubx.resource(AdmChannel, "ci-runner", AdmChannelConfig(
            application_id="example",
            client_id="example",
            client_secret="example",
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpoint_adm_channel, with application id example,
    client id example, client secret example, enabled true.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
