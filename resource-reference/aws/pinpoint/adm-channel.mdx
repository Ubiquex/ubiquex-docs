---
title: "aws_pinpoint_adm_channel"
description: "Real, generated bindings for `aws_pinpoint_adm_channel`."
---

`aws_pinpoint_adm_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    import "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"

    cfg := pinpoint.AdmChannelConfig{
        ApplicationId: "example",
        ClientId: "example",
        ClientSecret: "example",
        Enabled: true,
    }
    ubx.Resource(pinpoint.AdmChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { AdmChannel } from
      "jsr:@ubx/sdk-aws/aws/pinpoint/adm_channel";

    resource(AdmChannel, "example", {
      applicationId: "example",
      clientId: "example",
      clientSecret: "example",
      enabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from ubx.aws.pinpoint import AdmChannel, AdmChannelConfig

    cfg = AdmChannelConfig(
        application_id="example",
        client_id="example",
        client_secret="example",
        enabled=True,
    )
    ubx.resource(AdmChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision aws_pinpoint_adm_channel with application_id,
client_id, client_secret, enabled.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="client_secret" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties


<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
