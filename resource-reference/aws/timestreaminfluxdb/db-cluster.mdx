---
title: "aws_timestreaminfluxdb_db_cluster"
description: "Real, generated bindings for `aws_timestreaminfluxdb_db_cluster`."
---

`aws_timestreaminfluxdb_db_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	timestreaminfluxdb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/timestreaminfluxdb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own db cluster"})

    		ubx.Resource(timestreaminfluxdb.DbCluster, "ci-runner", timestreaminfluxdb.DbClusterConfig{
    			DbInstanceType:             "example",
    			Name:                       "ci-runner",
    			VpcSecurityGroupIds:        []string{"example"},
    			VpcSubnetIds:               []string{"example"},
    			AllocatedStorage:           1,
    			Bucket:                     "example",
    			DbParameterGroupIdentifier: "example",
    			Organization:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DbCluster } from "jsr:@ubx/sdk-aws/aws/timestreaminfluxdb/db_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own db cluster" });

      resource(DbCluster, "ci-runner", {
        dbInstanceType: "example",
        name: "ci-runner",
        vpcSecurityGroupIds: ["example"],
        vpcSubnetIds: ["example"],
        allocatedStorage: 1,
        bucket: "example",
        dbParameterGroupIdentifier: "example",
        organization: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.timestreaminfluxdb import DbCluster, DbClusterConfig

    def describe():
        ubx.intent("payments own db cluster")

        ubx.resource(DbCluster, "ci-runner", DbClusterConfig(
            db_instance_type="example",
            name="ci-runner",
            vpc_security_group_ids=["example"],
            vpc_subnet_ids=["example"],
            allocated_storage=1,
            bucket="example",
            db_parameter_group_identifier="example",
            organization="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_timestreaminfluxdb_db_cluster called "ci-runner", with
    db instance type example, vpc security group ids a set value, vpc
    subnet ids a set value, allocated storage 1, bucket example, db
    parameter group identifier example, organization example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allocated_storage" type="number">
  The amount of storage to allocate for your DB storage type in GiB (gibibytes). This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group). Optional.
</ResponseField>

<ResponseField name="bucket" type="string">
  Name of the initial InfluxDB bucket. All InfluxDB data is stored in a bucket. A bucket combines the concept of a database and a retention period (the duration of time that each data point persists). A bucket belongs to an organization. Along with organization, username, and password, this argument will be stored in the secret referred to by the influx_auth_parameters_secret_arn attribute. This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group). Optional.
</ResponseField>

<ResponseField name="db_instance_type" type="string" required>
  The Timestream for InfluxDB DB instance type to run InfluxDB on. Required.
</ResponseField>

<ResponseField name="db_parameter_group_identifier" type="string">
  The ID of the DB parameter group to assign to your DB cluster. DB parameter groups specify how the database is configured. For example, DB parameter groups can specify the limit for query concurrency. Optional.
</ResponseField>

<ResponseField name="db_storage_type" type="string">
  The Timestream for InfluxDB DB storage type to read and write InfluxDB data. You can choose between 3 different types of provisioned Influx IOPS included storage according to your workloads requirements: Influx IO Included 3000 IOPS, Influx IO Included 12000 IOPS, Influx IO Included 16000 IOPS. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="deployment_type" type="string">
  Specifies the type of cluster to create. This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="failover_mode" type="string">
  Specifies the behavior of failure recovery when the primary node of the cluster fails. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_delivery_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="s3_configuration" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="bucket_name" type="string" required>
        </ResponseField>
        <ResponseField name="enabled" type="bool" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_schedule" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="preferred_maintenance_window" type="string" required>
    </ResponseField>
    <ResponseField name="timezone" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name that uniquely identifies the DB cluster when interacting with the Amazon Timestream for InfluxDB API and CLI commands. This name will also be a prefix included in the endpoint. DB cluster names must be unique per customer and per region. Required.
</ResponseField>

<ResponseField name="network_type" type="string">
  Specifies whether the networkType of the Timestream for InfluxDB cluster is IPV4, which can communicate over IPv4 protocol only, or DUAL, which can communicate over both IPv4 and IPv6 protocols. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="organization" type="string">
  Name of the initial organization for the initial admin user in InfluxDB. An InfluxDB organization is a workspace for a group of users. Along with bucket, username, and password, this argument will be stored in the secret referred to by the influx_auth_parameters_secret_arn attribute. This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group). Optional.
</ResponseField>

<ResponseField name="password" type="string">
  Password of the initial admin user created in InfluxDB. This password will allow you to access the InfluxDB UI to perform various administrative tasks and also use the InfluxDB CLI to create an operator token. Along with bucket, username, and organization, this argument will be stored in the secret referred to by the influx_auth_parameters_secret_arn attribute. This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group) as the AWS API rejects it. Optional.
</ResponseField>

<ResponseField name="port" type="number">
  The port number on which InfluxDB accepts connections. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="publicly_accessible" type="bool">
  Configures the Timestream for InfluxDB cluster with a public IP to facilitate access. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="username" type="string">
  Username of the initial admin user created in InfluxDB. Must start with a letter and can't end with a hyphen or contain two consecutive hyphens. This username will allow you to access the InfluxDB UI to perform various administrative tasks and also use the InfluxDB CLI to create an operator token. Along with bucket, organization, and password, this argument will be stored in the secret referred to by the influx_auth_parameters_secret_arn attribute. This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group). Optional.
</ResponseField>

<ResponseField name="vpc_security_group_ids" type="set(string)" required>
  A list of VPC security group IDs to associate with the Timestream for InfluxDB cluster. Required.
</ResponseField>

<ResponseField name="vpc_subnet_ids" type="set(string)" required>
  A list of VPC subnet IDs to associate with the DB cluster. Provide at least two VPC subnet IDs in different availability zones when deploying with a Multi-AZ standby. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_storage_type" type="string">
  The Timestream for InfluxDB DB storage type to read and write InfluxDB data. You can choose between 3 different types of provisioned Influx IOPS included storage according to your workloads requirements: Influx IO Included 3000 IOPS, Influx IO Included 12000 IOPS, Influx IO Included 16000 IOPS. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="deployment_type" type="string">
  Specifies the type of cluster to create. This field is forbidden for InfluxDB V3 clusters (when using an InfluxDB V3 db parameter group). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="endpoint" type="string">
  The endpoint used to connect to InfluxDB. The default InfluxDB port is 8086. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine_type" type="string">
  The database engine type of the DB cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failover_mode" type="string">
  Specifies the behavior of failure recovery when the primary node of the cluster fails. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="influx_auth_parameters_secret_arn" type="string">
  The Amazon Resource Name (ARN) of the AWS Secrets Manager secret containing the initial InfluxDB authorization parameters. For InfluxDB V2 clusters, the secret value is a JSON formatted key-value pair holding InfluxDB authorization values: organization, bucket, username, and password. For InfluxDB V3 clusters, the secret contains the InfluxDB admin token. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="log_delivery_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="s3_configuration" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="bucket_name" type="string" required>
        </ResponseField>
        <ResponseField name="enabled" type="bool" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_schedule" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="preferred_maintenance_window" type="string" required>
    </ResponseField>
    <ResponseField name="timezone" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_type" type="string">
  Specifies whether the networkType of the Timestream for InfluxDB cluster is IPV4, which can communicate over IPv4 protocol only, or DUAL, which can communicate over both IPv4 and IPv6 protocols. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="port" type="number">
  The port number on which InfluxDB accepts connections. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="publicly_accessible" type="bool">
  Configures the Timestream for InfluxDB cluster with a public IP to facilitate access. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reader_endpoint" type="string">
  The endpoint used to connect to the Timestream for InfluxDB cluster for read-only operations. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
