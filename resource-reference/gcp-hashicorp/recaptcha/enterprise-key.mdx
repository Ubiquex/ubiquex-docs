---
title: "google_recaptcha_enterprise_key"
description: "Real, generated bindings for `google_recaptcha_enterprise_key`."
---

`google_recaptcha_enterprise_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	recaptcha "github.com/ubiquex/ubx-sdk-google/sdk/go/google/recaptcha"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own enterprise key"})

    		ubx.Resource(recaptcha.EnterpriseKey, "ci-runner", recaptcha.EnterpriseKeyConfig{
    			DisplayName: "example-display-name",
    			Labels:      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnterpriseKey } from "jsr:@ubx/sdk-google/google/recaptcha/enterprise_key";

    export default stack("payments", () => {
      intent({ summary: "payments own enterprise key" });

      resource(EnterpriseKey, "ci-runner", {
        displayName: "example-display-name",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.recaptcha import EnterpriseKey, EnterpriseKeyConfig

    def describe():
        ubx.intent("payments own enterprise key")

        ubx.resource(EnterpriseKey, "ci-runner", EnterpriseKeyConfig(
            display_name="example-display-name",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_recaptcha_enterprise_key, with display name
    example-display-name, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="android_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_all_package_names" type="bool">
    </ResponseField>
    <ResponseField name="allowed_package_names" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Human-readable display name of this key. Modifiable by user. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ios_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_all_bundle_ids" type="bool">
    </ResponseField>
    <ResponseField name="allowed_bundle_ids" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  See [Creating and managing labels](https://cloud.google.com/recaptcha-enterprise/docs/labels). **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field `effective_labels` for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  The project for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="testing_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="testing_challenge" type="string">
    </ResponseField>
    <ResponseField name="testing_score" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="waf_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="waf_feature" type="string" required>
    </ResponseField>
    <ResponseField name="waf_service" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_all_domains" type="bool">
    </ResponseField>
    <ResponseField name="allow_amp_traffic" type="bool">
    </ResponseField>
    <ResponseField name="allowed_domains" type="list(string)">
    </ResponseField>
    <ResponseField name="challenge_security_preference" type="string">
    </ResponseField>
    <ResponseField name="challenge_settings" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="action_settings" type="set(object)">
          <Expandable title="properties">
            <ResponseField name="action" type="string" required>
            </ResponseField>
            <ResponseField name="score_threshold" type="number" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_settings" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="score_threshold" type="number" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="integration_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="android_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_all_package_names" type="bool">
    </ResponseField>
    <ResponseField name="allowed_package_names" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  The timestamp corresponding to the creation of this Key. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ios_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_all_bundle_ids" type="bool">
    </ResponseField>
    <ResponseField name="allowed_bundle_ids" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The resource id for the Key, which is the same as the Site Key itself. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  The project for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="testing_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="testing_challenge" type="string">
    </ResponseField>
    <ResponseField name="testing_score" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="waf_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="waf_feature" type="string" required>
    </ResponseField>
    <ResponseField name="waf_service" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="web_settings" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_all_domains" type="bool">
    </ResponseField>
    <ResponseField name="allow_amp_traffic" type="bool">
    </ResponseField>
    <ResponseField name="allowed_domains" type="list(string)">
    </ResponseField>
    <ResponseField name="challenge_security_preference" type="string">
    </ResponseField>
    <ResponseField name="challenge_settings" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="action_settings" type="set(object)">
          <Expandable title="properties">
            <ResponseField name="action" type="string" required>
            </ResponseField>
            <ResponseField name="score_threshold" type="number" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="default_settings" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="score_threshold" type="number" required>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="integration_type" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>
