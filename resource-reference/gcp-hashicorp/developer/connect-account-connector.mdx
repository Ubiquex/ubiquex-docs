---
title: "google_developer_connect_account_connector"
description: "Real, generated bindings for `google_developer_connect_account_connector`."
---

`google_developer_connect_account_connector` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	developer "github.com/ubiquex/ubx-sdk-google/sdk/go/google/developer"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connect account connector"})

    		ubx.Resource(developer.ConnectAccountConnector, "ci-runner", developer.ConnectAccountConnectorConfig{
    			AccountConnectorId: "example",
    			Location:           "example",
    			Annotations:        map[string]string{"managed-by": "ubx"},
    			Etag:               "example",
    			Labels:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectAccountConnector } from "jsr:@ubx/sdk-google/google/developer/connect_account_connector";

    export default stack("payments", () => {
      intent({ summary: "payments own connect account connector" });

      resource(ConnectAccountConnector, "ci-runner", {
        accountConnectorId: "example",
        location: "example",
        annotations: { "managed-by": "ubx" },
        etag: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.developer import ConnectAccountConnector, ConnectAccountConnectorConfig

    def describe():
        ubx.intent("payments own connect account connector")

        ubx.resource(ConnectAccountConnector, "ci-runner", ConnectAccountConnectorConfig(
            account_connector_id="example",
            location="example",
            annotations={"managed-by": "ubx"},
            etag="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_developer_connect_account_connector, with account
    connector id example, location example, annotations a set value,
    etag example, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_connector_id" type="string" required>
  The ID to use for the AccountConnector, which will become the final component of the AccountConnector's resource name. Its format should adhere to https://google.aip.dev/122#resource-id-segments Names must be unique per-project per-location. Required.
</ResponseField>

<ResponseField name="annotations" type="map(string)">
  Allows users to store small amounts of arbitrary data. **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field 'effective_annotations' for all of the annotations present on the resource. Optional.
</ResponseField>

<ResponseField name="custom_oauth_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="auth_uri" type="string" required>
    </ResponseField>
    <ResponseField name="client_id" type="string" required>
    </ResponseField>
    <ResponseField name="client_secret" type="string" required>
    </ResponseField>
    <ResponseField name="host_uri" type="string" required>
    </ResponseField>
    <ResponseField name="pkce_disabled" type="bool">
    </ResponseField>
    <ResponseField name="scm_provider" type="string" required>
    </ResponseField>
    <ResponseField name="scopes" type="list(string)" required>
    </ResponseField>
    <ResponseField name="server_version" type="string">
    </ResponseField>
    <ResponseField name="service_directory_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="service" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ssl_ca_certificate" type="string">
    </ResponseField>
    <ResponseField name="token_uri" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  This checksum is computed by the server based on the value of other fields, and may be sent on update and delete requests to ensure the client has an up-to-date value before proceeding. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="provider_oauth_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="scopes" type="list(string)" required>
    </ResponseField>
    <ResponseField name="system_provider_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proxy_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The timestamp when the accountConnector was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="custom_oauth_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="auth_uri" type="string" required>
    </ResponseField>
    <ResponseField name="client_id" type="string" required>
    </ResponseField>
    <ResponseField name="client_secret" type="string" required>
    </ResponseField>
    <ResponseField name="host_uri" type="string" required>
    </ResponseField>
    <ResponseField name="pkce_disabled" type="bool">
    </ResponseField>
    <ResponseField name="scm_provider" type="string" required>
    </ResponseField>
    <ResponseField name="scopes" type="list(string)" required>
    </ResponseField>
    <ResponseField name="server_version" type="string">
    </ResponseField>
    <ResponseField name="service_directory_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="service" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ssl_ca_certificate" type="string">
    </ResponseField>
    <ResponseField name="token_uri" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_annotations" type="map(string)">
  All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the accountConnector, in the format 'projects/&#123;project&#125;/locations/&#123;location&#125;/accountConnectors/&#123;account_connector_id&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="oauth_start_uri" type="string">
  Start OAuth flow by clicking on this URL. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="provider_oauth_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="scopes" type="list(string)" required>
    </ResponseField>
    <ResponseField name="system_provider_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="proxy_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the accountConnector was updated. Computed by GCP after `ship`.
</ResponseField>
