---
title: "google_bigquery_capacity_commitment"
description: "Real, generated bindings for `google_bigquery_capacity_commitment`."
---

`google_bigquery_capacity_commitment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own capacity commitment"})

    		ubx.Resource(bigquery.CapacityCommitment, "ci-runner", bigquery.CapacityCommitmentConfig{
    			Plan:                            "example",
    			SlotCount:                       1,
    			CapacityCommitmentId:            "example",
    			Edition:                         "example",
    			EnforceSingleAdminProjectPerOrg: "example",
    			Location:                        "example",
    			RenewalPlan:                     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CapacityCommitment } from "jsr:@ubx/sdk-google/google/bigquery/capacity_commitment";

    export default stack("payments", () => {
      intent({ summary: "payments own capacity commitment" });

      resource(CapacityCommitment, "ci-runner", {
        plan: "example",
        slotCount: 1,
        capacityCommitmentId: "example",
        edition: "example",
        enforceSingleAdminProjectPerOrg: "example",
        location: "example",
        renewalPlan: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import CapacityCommitment, CapacityCommitmentConfig

    def describe():
        ubx.intent("payments own capacity commitment")

        ubx.resource(CapacityCommitment, "ci-runner", CapacityCommitmentConfig(
            plan="example",
            slot_count=1,
            capacity_commitment_id="example",
            edition="example",
            enforce_single_admin_project_per_org="example",
            location="example",
            renewal_plan="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_capacity_commitment, with plan example,
    slot count 1, capacity commitment id example, edition example,
    enforce single admin project per org example, location example,
    renewal plan example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity_commitment_id" type="string">
  The optional capacity commitment ID. Capacity commitment name will be generated automatically if this field is empty. This field must only contain lower case alphanumeric characters or dashes. The first and last character cannot be a dash. Max length is 64 characters. NOTE: this ID won't be kept if the capacity commitment is split or merged. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="edition" type="string">
  The edition type. Valid values are STANDARD, ENTERPRISE, ENTERPRISE_PLUS Optional.
</ResponseField>

<ResponseField name="enforce_single_admin_project_per_org" type="string">
  If true, fail the request if another project in the organization has a capacity commitment. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  The geographic location where the transfer config should reside. Examples: US, EU, asia-northeast1. The default value is US. Optional.
</ResponseField>

<ResponseField name="plan" type="string" required>
  Capacity commitment plan. Valid values are at https://cloud.google.com/bigquery/docs/reference/reservations/rpc/google.cloud.bigquery.reservation.v1#commitmentplan Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="renewal_plan" type="string">
  The plan this capacity commitment is converted to after commitmentEndTime passes. Once the plan is changed, committed period is extended according to commitment plan. Only applicable for some commitment plans. Optional.
</ResponseField>

<ResponseField name="slot_count" type="number" required>
  Number of slots in this commitment. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="commitment_end_time" type="string">
  The start of the current commitment period. It is applicable only for ACTIVE capacity commitments. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="commitment_start_time" type="string">
  The start of the current commitment period. It is applicable only for ACTIVE capacity commitments. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the capacity commitment, e.g., projects/myproject/locations/US/capacityCommitments/123 Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  State of the commitment Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
