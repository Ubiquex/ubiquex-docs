---
title: "google_bigquery_analytics_hub_listing"
description: "Real, generated bindings for `google_bigquery_analytics_hub_listing`."
---

`google_bigquery_analytics_hub_listing` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own analytics hub listing"})

    		ubx.Resource(bigquery.AnalyticsHubListing, "ci-runner", bigquery.AnalyticsHubListingConfig{
    			DataExchangeId:           "example",
    			DisplayName:              "example-display-name",
    			ListingId:                "example",
    			Location:                 "example",
    			AllowOnlyMetadataSharing: true,
    			Categories:               []string{"example"},
    			DeleteCommercial:         true,
    			Description:              "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalyticsHubListing } from "jsr:@ubx/sdk-google/google/bigquery/analytics_hub_listing";

    export default stack("payments", () => {
      intent({ summary: "payments own analytics hub listing" });

      resource(AnalyticsHubListing, "ci-runner", {
        dataExchangeId: "example",
        displayName: "example-display-name",
        listingId: "example",
        location: "example",
        allowOnlyMetadataSharing: true,
        categories: ["example"],
        deleteCommercial: true,
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import AnalyticsHubListing, AnalyticsHubListingConfig

    def describe():
        ubx.intent("payments own analytics hub listing")

        ubx.resource(AnalyticsHubListing, "ci-runner", AnalyticsHubListingConfig(
            data_exchange_id="example",
            display_name="example-display-name",
            listing_id="example",
            location="example",
            allow_only_metadata_sharing=True,
            categories=["example"],
            delete_commercial=True,
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_analytics_hub_listing, with data exchange
    id example, display name example-display-name, listing id
    example, location example, allow only metadata sharing true,
    categories a set value, delete commercial true, description
    Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allow_only_metadata_sharing" type="bool">
  If true, the listing is only available to get the resource metadata. Listing is non subscribable. Optional.
</ResponseField>

<ResponseField name="bigquery_dataset" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dataset" type="string" required>
    </ResponseField>
    <ResponseField name="effective_replicas" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="location" type="string">
        </ResponseField>
        <ResponseField name="primary_state" type="string">
        </ResponseField>
        <ResponseField name="replica_state" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_locations" type="set(string)">
    </ResponseField>
    <ResponseField name="selected_resources" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="routine" type="string">
        </ResponseField>
        <ResponseField name="table" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="categories" type="list(string)">
  Categories of the listing. Up to two categories are allowed. Optional.
</ResponseField>

<ResponseField name="data_exchange_id" type="string" required>
  The ID of the data exchange. Must contain only Unicode letters, numbers (0-9), underscores (_). Should not use characters that require URL-escaping, or characters outside of ASCII, spaces. Required.
</ResponseField>

<ResponseField name="data_provider" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="primary_contact" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="delete_commercial" type="bool">
  If the listing is commercial then this field must be set to true, otherwise a failure is thrown. This acts as a safety guard to avoid deleting commercial listings accidentally. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Short description of the listing. The description must not contain Unicode non-characters and C0 and C1 control codes except tabs (HT), new lines (LF), carriage returns (CR), and page breaks (FF). Optional.
</ResponseField>

<ResponseField name="discovery_type" type="string">
  Specifies the type of discovery on the discovery page. Cannot be set for a restricted listing. Note that this does not control the visibility of the exchange/listing which is defined by IAM permission. Possible values: ["DISCOVERY_TYPE_PRIVATE", "DISCOVERY_TYPE_PUBLIC"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Human-readable display name of the listing. The display name must contain only Unicode letters, numbers (0-9), underscores (_), dashes (-), spaces ( ), ampersands (&) and can't start or end with spaces. Required.
</ResponseField>

<ResponseField name="documentation" type="string">
  Documentation describing the listing. Optional.
</ResponseField>

<ResponseField name="icon" type="string">
  Base64 encoded image representing the listing. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="listing_id" type="string" required>
  The ID of the listing. Must contain only Unicode letters, numbers (0-9), underscores (_). Should not use characters that require URL-escaping, or characters outside of ASCII, spaces. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The name of the location this data exchange listing. Required.
</ResponseField>

<ResponseField name="log_linked_dataset_query_user_email" type="bool">
  If true, subscriber email logging is enabled and all queries on the linked dataset will log the email address of the querying user. Once enabled, this setting cannot be turned off. Optional.
</ResponseField>

<ResponseField name="primary_contact" type="string">
  Email or URL of the primary point of contact of the listing. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="publisher" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="primary_contact" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="pubsub_topic" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="data_affinity_regions" type="set(string)">
    </ResponseField>
    <ResponseField name="topic" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="request_access" type="string">
  Email or URL of the request access of the listing. Subscribers can use this reference to request access. Optional.
</ResponseField>

<ResponseField name="restricted_export_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
    </ResponseField>
    <ResponseField name="restrict_direct_table_access" type="bool">
    </ResponseField>
    <ResponseField name="restrict_query_result" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="bigquery_dataset" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dataset" type="string" required>
    </ResponseField>
    <ResponseField name="effective_replicas" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="location" type="string">
        </ResponseField>
        <ResponseField name="primary_state" type="string">
        </ResponseField>
        <ResponseField name="replica_state" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="replica_locations" type="set(string)">
    </ResponseField>
    <ResponseField name="selected_resources" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="routine" type="string">
        </ResponseField>
        <ResponseField name="table" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="commercial_info" type="list(object)">
  Commercial info contains the information about the commercial data products associated with the listing. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="cloud_marketplace" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="commercial_state" type="string">
        </ResponseField>
        <ResponseField name="service" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_provider" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="primary_contact" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="discovery_type" type="string">
  Specifies the type of discovery on the discovery page. Cannot be set for a restricted listing. Note that this does not control the visibility of the exchange/listing which is defined by IAM permission. Possible values: ["DISCOVERY_TYPE_PRIVATE", "DISCOVERY_TYPE_PUBLIC"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the listing. e.g. "projects/myproject/locations/US/dataExchanges/123/listings/456" Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="publisher" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
    </ResponseField>
    <ResponseField name="primary_contact" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="pubsub_topic" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="data_affinity_regions" type="set(string)">
    </ResponseField>
    <ResponseField name="topic" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="restricted_export_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool">
    </ResponseField>
    <ResponseField name="restrict_direct_table_access" type="bool">
    </ResponseField>
    <ResponseField name="restrict_query_result" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  Current state of the listing. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
