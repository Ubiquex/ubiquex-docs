---
title: "google_bigquery_reservation_assignment"
description: "Real, generated bindings for `google_bigquery_reservation_assignment`."
---

`google_bigquery_reservation_assignment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own reservation assignment"})

    		ubx.Resource(bigquery.ReservationAssignment, "ci-runner", bigquery.ReservationAssignmentConfig{
    			Assignee:    "example",
    			JobType:     "example",
    			Reservation: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReservationAssignment } from "jsr:@ubx/sdk-google/google/bigquery/reservation_assignment";

    export default stack("payments", () => {
      intent({ summary: "payments own reservation assignment" });

      resource(ReservationAssignment, "ci-runner", {
        assignee: "example",
        jobType: "example",
        reservation: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import ReservationAssignment, ReservationAssignmentConfig

    def describe():
        ubx.intent("payments own reservation assignment")

        ubx.resource(ReservationAssignment, "ci-runner", ReservationAssignmentConfig(
            assignee="example",
            job_type="example",
            reservation="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_reservation_assignment, with assignee
    example, job type example, reservation example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="assignee" type="string" required>
  The resource which will use the reservation. E.g. projects/myproject, folders/123, organizations/456. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="job_type" type="string" required>
  Types of job, which could be specified when using the reservation. Possible values: JOB_TYPE_UNSPECIFIED, PIPELINE, QUERY, CONTINUOUS Required.
</ResponseField>

<ResponseField name="location" type="string">
  The location for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="principal" type="string">
  Optional. Represents the principal for this assignment. If not empty, jobs run by this principal will utilize the associated reservation. Otherwise, jobs will fall back to using the reservation assigned to the project, folder, or organization (in that order). If no reservation is assigned at any of these levels, on-demand capacity will be used. The supported formats are: * 'principal://goog/subject/USER_EMAIL_ADDRESS' for users, * 'principal://iam.googleapis.com/projects/-/serviceAccounts/SA_EMAIL_ADDRESS' for service accounts, * 'principal://iam.googleapis.com/projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/POOL_ID/subject/SUBJECT_ID' for workload identity pool identities. * The special value 'unknown_or_deleted_user' represents principals which cannot be read from the user info service, for example deleted users. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reservation" type="string" required>
  The reservation for the resource Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  The location for the resource Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the assignment. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  Assignment will remain in PENDING state if no active capacity commitment is present. It will become ACTIVE when some capacity commitment becomes active. Possible values: STATE_UNSPECIFIED, PENDING, ACTIVE Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
