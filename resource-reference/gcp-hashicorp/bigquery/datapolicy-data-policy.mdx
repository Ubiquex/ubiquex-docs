---
title: "google_bigquery_datapolicy_data_policy"
description: "Real, generated bindings for `google_bigquery_datapolicy_data_policy`."
---

`google_bigquery_datapolicy_data_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigquery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigquery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own datapolicy data policy"})

    		ubx.Resource(bigquery.DatapolicyDataPolicy, "ci-runner", bigquery.DatapolicyDataPolicyConfig{
    			DataPolicyId:   "example",
    			DataPolicyType: "example",
    			Location:       "example",
    			PolicyTag:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatapolicyDataPolicy } from "jsr:@ubx/sdk-google/google/bigquery/datapolicy_data_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own datapolicy data policy" });

      resource(DatapolicyDataPolicy, "ci-runner", {
        dataPolicyId: "example",
        dataPolicyType: "example",
        location: "example",
        policyTag: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigquery import DatapolicyDataPolicy, DatapolicyDataPolicyConfig

    def describe():
        ubx.intent("payments own datapolicy data policy")

        ubx.resource(DatapolicyDataPolicy, "ci-runner", DatapolicyDataPolicyConfig(
            data_policy_id="example",
            data_policy_type="example",
            location="example",
            policy_tag="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigquery_datapolicy_data_policy, with data policy
    id example, data policy type example, location example, policy
    tag example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_masking_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="predefined_expression" type="string">
    </ResponseField>
    <ResponseField name="routine" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_policy_id" type="string" required>
  User-assigned (human readable) ID of the data policy that needs to be unique within a project. Used as &#123;dataPolicyId&#125; in part of the resource name. Required.
</ResponseField>

<ResponseField name="data_policy_type" type="string" required>
  The enrollment level of the service. Possible values: ["COLUMN_LEVEL_SECURITY_POLICY", "DATA_MASKING_POLICY"] Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The name of the location of the data policy. Required.
</ResponseField>

<ResponseField name="policy_tag" type="string" required>
  Policy tag resource name, in the format of projects/&#123;project_number&#125;/locations/&#123;locationId&#125;/taxonomies/&#123;taxonomyId&#125;/policyTags/&#123;policyTag_id&#125;. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="data_masking_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="predefined_expression" type="string">
    </ResponseField>
    <ResponseField name="routine" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Resource name of this data policy, in the format of projects/&#123;project_number&#125;/locations/&#123;locationId&#125;/dataPolicies/&#123;dataPolicyId&#125;. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
