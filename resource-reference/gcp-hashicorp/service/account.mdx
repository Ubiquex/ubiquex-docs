---
title: "google_service_account"
description: "Real, generated bindings for `google_service_account`."
---

`google_service_account` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account"})

    		ubx.Resource(service.Account, "ci-runner", service.AccountConfig{
    			AccountId:                 "example",
    			CreateIgnoreAlreadyExists: true,
    			Description:               "Managed by ubx.",
    			Disabled:                  true,
    			DisplayName:               "example-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Account } from "jsr:@ubx/sdk-google/google/service/account";

    export default stack("payments", () => {
      intent({ summary: "payments own account" });

      resource(Account, "ci-runner", {
        accountId: "example",
        createIgnoreAlreadyExists: true,
        description: "Managed by ubx.",
        disabled: true,
        displayName: "example-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import Account, AccountConfig

    def describe():
        ubx.intent("payments own account")

        ubx.resource(Account, "ci-runner", AccountConfig(
            account_id="example",
            create_ignore_already_exists=True,
            description="Managed by ubx.",
            disabled=True,
            display_name="example-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_account, with account id example, create
    ignore already exists true, description Managed by ubx., disabled
    true, display name example-display-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string" required>
  The account id that is used to generate the service account email address and a stable unique id. It is unique within a project, must be 6-30 characters long, and match the regular expression [a-z]([-a-z0-9]*[a-z0-9]) to comply with RFC1035. Changing this forces a new service account to be created. Required.
</ResponseField>

<ResponseField name="create_ignore_already_exists" type="bool">
  If set to true, skip service account creation if a service account with the same email already exists. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A text description of the service account. Must be less than or equal to 256 UTF-8 bytes. Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  Whether the service account is disabled. Defaults to false Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  The display name for the service account. Can be updated without creating a new resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project that the service account will be created in. Defaults to the provider project configuration. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="email" type="string">
  The e-mail address of the service account. This value should be referenced from any google_iam_policy data sources that would grant the service account privileges. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="member" type="string">
  The Identity of the service account in the form 'serviceAccount:&#123;email&#125;'. This value is often used to refer to the service account in order to grant IAM permissions. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The fully-qualified name of the service account. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project that the service account will be created in. Defaults to the provider project configuration. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unique_id" type="string">
  The unique id of the service account. Computed by GCP after `ship`.
</ResponseField>
