---
title: "google_service_networking_vpc_service_controls"
description: "Real, generated bindings for `google_service_networking_vpc_service_controls`."
---

`google_service_networking_vpc_service_controls` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own networking vpc service controls"})

    		ubx.Resource(service.NetworkingVpcServiceControls, "ci-runner", service.NetworkingVpcServiceControlsConfig{
    			Enabled: true,
    			Network: "example",
    			Service: "example",
    			Project: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkingVpcServiceControls } from "jsr:@ubx/sdk-google/google/service/networking_vpc_service_controls";

    export default stack("payments", () => {
      intent({ summary: "payments own networking vpc service controls" });

      resource(NetworkingVpcServiceControls, "ci-runner", {
        enabled: true,
        network: "example",
        service: "example",
        project: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import NetworkingVpcServiceControls, NetworkingVpcServiceControlsConfig

    def describe():
        ubx.intent("payments own networking vpc service controls")

        ubx.resource(NetworkingVpcServiceControls, "ci-runner", NetworkingVpcServiceControlsConfig(
            enabled=True,
            network="example",
            service="example",
            project="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_networking_vpc_service_controls, with
    enabled true, network example, service example, project example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="enabled" type="bool" required>
  Desired VPC Service Controls state service producer VPC network, as described at the top of this page. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="network" type="string" required>
  The network that the consumer is using to connect with services. Required.
</ResponseField>

<ResponseField name="project" type="string">
  The id of the Google Cloud project containing the consumer network. Optional.
</ResponseField>

<ResponseField name="service" type="string" required>
  The service that is managing peering connectivity for a service producer's organization. For Google services that support this functionality, this value is 'servicenetworking.googleapis.com'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
