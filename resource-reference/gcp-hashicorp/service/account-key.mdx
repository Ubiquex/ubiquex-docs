---
title: "google_service_account_key"
description: "Real, generated bindings for `google_service_account_key`."
---

`google_service_account_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account key"})

    		ubx.Resource(service.AccountKey, "ci-runner", service.AccountKeyConfig{
    			ServiceAccountId: "example",
    			Keepers:          map[string]string{"managed-by": "ubx"},
    			KeyAlgorithm:     "example",
    			PrivateKeyType:   "example",
    			PublicKeyData:    "example",
    			PublicKeyType:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountKey } from "jsr:@ubx/sdk-google/google/service/account_key";

    export default stack("payments", () => {
      intent({ summary: "payments own account key" });

      resource(AccountKey, "ci-runner", {
        serviceAccountId: "example",
        keepers: { "managed-by": "ubx" },
        keyAlgorithm: "example",
        privateKeyType: "example",
        publicKeyData: "example",
        publicKeyType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import AccountKey, AccountKeyConfig

    def describe():
        ubx.intent("payments own account key")

        ubx.resource(AccountKey, "ci-runner", AccountKeyConfig(
            service_account_id="example",
            keepers={"managed-by": "ubx"},
            key_algorithm="example",
            private_key_type="example",
            public_key_data="example",
            public_key_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_account_key, with service account id
    example, keepers a set value, key algorithm example, private key
    type example, public key data example, public key type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="keepers" type="map(string)">
  Arbitrary map of values that, when changed, will trigger recreation of resource. Optional.
</ResponseField>

<ResponseField name="key_algorithm" type="string">
  The algorithm used to generate the key, used only on create. KEY_ALG_RSA_2048 is the default algorithm. Valid values are: "KEY_ALG_RSA_1024", "KEY_ALG_RSA_2048". Optional.
</ResponseField>

<ResponseField name="private_key_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="public_key_data" type="string">
  A field that allows clients to upload their own public key. If set, use this public key data to create a service account key for given service account. Please note, the expected format for this field is a base64 encoded X509_PEM. Optional.
</ResponseField>

<ResponseField name="public_key_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  The ID of the parent service account of the key. This can be a string in the format &#123;ACCOUNT&#125; or projects/&#123;PROJECT_ID&#125;/serviceAccounts/&#123;ACCOUNT&#125;, where &#123;ACCOUNT&#125; is the email address or unique id of the service account. If the &#123;ACCOUNT&#125; syntax is used, the project will be inferred from the provider's configuration. Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name used for this key pair Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="private_key" type="string">
  The private key in JSON format, base64 encoded. This is what you normally get as a file when creating service account keys through the CLI or web console. This is only populated when creating a new key. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="public_key" type="string">
  The public key, base64 encoded Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="valid_after" type="string">
  The key can be used after this timestamp. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="valid_before" type="string">
  The key can be used before this timestamp. A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>
