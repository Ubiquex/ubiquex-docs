---
title: "google_service_directory_endpoint"
description: "Real, generated bindings for `google_service_directory_endpoint`."
---

`google_service_directory_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory endpoint"})

    		ubx.Resource(service.DirectoryEndpoint, "ci-runner", service.DirectoryEndpointConfig{
    			EndpointId: "example",
    			Service:    "example",
    			Address:    "example",
    			Metadata:   map[string]string{"managed-by": "ubx"},
    			Network:    "example",
    			Port:       1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryEndpoint } from "jsr:@ubx/sdk-google/google/service/directory_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own directory endpoint" });

      resource(DirectoryEndpoint, "ci-runner", {
        endpointId: "example",
        service: "example",
        address: "example",
        metadata: { "managed-by": "ubx" },
        network: "example",
        port: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import DirectoryEndpoint, DirectoryEndpointConfig

    def describe():
        ubx.intent("payments own directory endpoint")

        ubx.resource(DirectoryEndpoint, "ci-runner", DirectoryEndpointConfig(
            endpoint_id="example",
            service="example",
            address="example",
            metadata={"managed-by": "ubx"},
            network="example",
            port=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_directory_endpoint, with endpoint id
    example, service example, address example, metadata a set value,
    network example, port 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="address" type="string">
  IPv4 or IPv6 address of the endpoint. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_id" type="string" required>
  The Resource ID must be 1-63 characters long, including digits, lowercase letters or the hyphen character. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  Metadata for the endpoint. This data can be consumed by service clients. The entire metadata dictionary may contain up to 512 characters, spread across all key-value pairs. Metadata that goes beyond any these limits will be rejected. Optional.
</ResponseField>

<ResponseField name="network" type="string">
  The URL to the network, such as projects/PROJECT_NUMBER/locations/global/networks/NETWORK_NAME. Optional.
</ResponseField>

<ResponseField name="port" type="number">
  Port that the endpoint is running on, must be in the range of [0, 65535]. If unspecified, the default is 0. Optional.
</ResponseField>

<ResponseField name="service" type="string" required>
  The resource name of the service that this endpoint provides. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name for the endpoint in the format 'projects/*/locations/*/namespaces/*/services/*/endpoints/*'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
