---
title: "google_service_directory_namespace_iam_policy"
description: "Real, generated bindings for `google_service_directory_namespace_iam_policy`."
---

`google_service_directory_namespace_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	service "github.com/ubiquex/ubx-sdk-google/sdk/go/google/service"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own directory namespace iam policy"})

    		ubx.Resource(service.DirectoryNamespaceIamPolicy, "ci-runner", service.DirectoryNamespaceIamPolicyConfig{
    			Name:       "ci-runner",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DirectoryNamespaceIamPolicy } from "jsr:@ubx/sdk-google/google/service/directory_namespace_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own directory namespace iam policy" });

      resource(DirectoryNamespaceIamPolicy, "ci-runner", {
        name: "ci-runner",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.service import DirectoryNamespaceIamPolicy, DirectoryNamespaceIamPolicyConfig

    def describe():
        ubx.intent("payments own directory namespace iam policy")

        ubx.resource(DirectoryNamespaceIamPolicy, "ci-runner", DirectoryNamespaceIamPolicyConfig(
            name="ci-runner",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_service_directory_namespace_iam_policy called
    "ci-runner", with policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
