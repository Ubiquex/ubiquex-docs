---
title: "google_vector_search_index"
description: "Real, generated bindings for `google_vector_search_index`."
---

`google_vector_search_index` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vector "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vector"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own search index"})

    		ubx.Resource(vector.SearchIndex, "ci-runner", vector.SearchIndexConfig{
    			CollectionId: "example",
    			IndexField:   "example",
    			IndexId:      "example",
    			Location:     "example",
    			Description:  "Managed by ubx.",
    			DisplayName:  "example-display-name",
    			FilterFields: []string{"example"},
    			Labels:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SearchIndex } from "jsr:@ubx/sdk-google/google/vector/search_index";

    export default stack("payments", () => {
      intent({ summary: "payments own search index" });

      resource(SearchIndex, "ci-runner", {
        collectionId: "example",
        indexField: "example",
        indexId: "example",
        location: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        filterFields: ["example"],
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vector import SearchIndex, SearchIndexConfig

    def describe():
        ubx.intent("payments own search index")

        ubx.resource(SearchIndex, "ci-runner", SearchIndexConfig(
            collection_id="example",
            index_field="example",
            index_id="example",
            location="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            filter_fields=["example"],
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vector_search_index, with collection id example,
    index field example, index id example, location example,
    description Managed by ubx., display name example-display-name,
    filter fields a set value, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="collection_id" type="string" required>
  The ID of the parent Collection. Required.
</ResponseField>

<ResponseField name="dedicated_infrastructure" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="autoscaling_spec" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_replica_count" type="number">
        </ResponseField>
        <ResponseField name="min_replica_count" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dense_scann" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="feature_norm_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  User-specified description of the index Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  User-specified display name of the index Optional.
</ResponseField>

<ResponseField name="distance_metric" type="string">
  Distance metric used for indexing. If not specified, will default to 'DOT_PRODUCT'. Possible values: ["DOT_PRODUCT", "COSINE_DISTANCE"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="filter_fields" type="list(string)">
  The fields to push into the index to enable fast ANN inline filtering. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="index_field" type="string" required>
  The collection schema field to index. Required.
</ResponseField>

<ResponseField name="index_id" type="string" required>
  ID of the Index to create. The id must be 1-63 characters long, and comply with [RFC1035](https://www.ietf.org/rfc/rfc1035.txt). Specifically, it must be 1-63 characters long and match the regular expression '[a-z](?:[-a-z0-9]&#123;0,61&#125;[a-z0-9])?'. Required.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="store_fields" type="list(string)">
  The fields to push into the index to enable inline data retrieval. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  [Output only] Create time stamp Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="dedicated_infrastructure" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="autoscaling_spec" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="max_replica_count" type="number">
        </ResponseField>
        <ResponseField name="min_replica_count" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="mode" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dense_scann" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="feature_norm_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="distance_metric" type="string">
  Distance metric used for indexing. If not specified, will default to 'DOT_PRODUCT'. Possible values: ["DOT_PRODUCT", "COSINE_DISTANCE"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. name of resource Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  [Output only] Update time stamp Computed by GCP after `ship`.
</ResponseField>
