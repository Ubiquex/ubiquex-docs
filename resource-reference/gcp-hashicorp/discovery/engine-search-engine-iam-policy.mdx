---
title: "google_discovery_engine_search_engine_iam_policy"
description: "Real, generated bindings for `google_discovery_engine_search_engine_iam_policy`."
---

`google_discovery_engine_search_engine_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine search engine iam policy"})

    		ubx.Resource(discovery.EngineSearchEngineIamPolicy, "ci-runner", discovery.EngineSearchEngineIamPolicyConfig{
    			CollectionId: "example",
    			EngineId:     "example",
    			PolicyData:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineSearchEngineIamPolicy } from "jsr:@ubx/sdk-google/google/discovery/engine_search_engine_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own engine search engine iam policy" });

      resource(EngineSearchEngineIamPolicy, "ci-runner", {
        collectionId: "example",
        engineId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineSearchEngineIamPolicy, EngineSearchEngineIamPolicyConfig

    def describe():
        ubx.intent("payments own engine search engine iam policy")

        ubx.resource(EngineSearchEngineIamPolicy, "ci-runner", EngineSearchEngineIamPolicyConfig(
            collection_id="example",
            engine_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_search_engine_iam_policy, with
    collection id example, engine id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="collection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="engine_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
