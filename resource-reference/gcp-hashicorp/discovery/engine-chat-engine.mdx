---
title: "google_discovery_engine_chat_engine"
description: "Real, generated bindings for `google_discovery_engine_chat_engine`."
---

`google_discovery_engine_chat_engine` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine chat engine"})

    		ubx.Resource(discovery.EngineChatEngine, "ci-runner", discovery.EngineChatEngineConfig{
    			CollectionId:     "example",
    			DataStoreIds:     []string{"example"},
    			DisplayName:      "example-display-name",
    			EngineId:         "example",
    			Location:         "example",
    			IndustryVertical: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineChatEngine } from "jsr:@ubx/sdk-google/google/discovery/engine_chat_engine";

    export default stack("payments", () => {
      intent({ summary: "payments own engine chat engine" });

      resource(EngineChatEngine, "ci-runner", {
        collectionId: "example",
        dataStoreIds: ["example"],
        displayName: "example-display-name",
        engineId: "example",
        location: "example",
        industryVertical: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineChatEngine, EngineChatEngineConfig

    def describe():
        ubx.intent("payments own engine chat engine")

        ubx.resource(EngineChatEngine, "ci-runner", EngineChatEngineConfig(
            collection_id="example",
            data_store_ids=["example"],
            display_name="example-display-name",
            engine_id="example",
            location="example",
            industry_vertical="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_chat_engine, with collection id
    example, data store ids a set value, display name
    example-display-name, engine id example, location example,
    industry vertical example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="chat_engine_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="agent_creation_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="business" type="string">
        </ResponseField>
        <ResponseField name="default_language_code" type="string" required>
        </ResponseField>
        <ResponseField name="location" type="string">
        </ResponseField>
        <ResponseField name="time_zone" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="allow_cross_region" type="bool">
    </ResponseField>
    <ResponseField name="dialogflow_agent_to_link" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="collection_id" type="string" required>
  The collection ID. Required.
</ResponseField>

<ResponseField name="common_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="company_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data_store_ids" type="list(string)" required>
  The data stores associated with this engine. Multiple DataStores in the same Collection can be associated here. All listed DataStores must be 'SOLUTION_TYPE_CHAT'. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The display name of the engine. Should be human readable. UTF-8 encoded string with limit of 1024 characters. Required.
</ResponseField>

<ResponseField name="engine_id" type="string" required>
  The ID to use for chat engine. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="industry_vertical" type="string">
  The industry vertical that the chat engine registers. Vertical on Engine has to match vertical of the DataStore linked to the engine. Default value: "GENERIC" Possible values: ["GENERIC"] Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="chat_engine_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="agent_creation_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="business" type="string">
        </ResponseField>
        <ResponseField name="default_language_code" type="string" required>
        </ResponseField>
        <ResponseField name="location" type="string">
        </ResponseField>
        <ResponseField name="time_zone" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="allow_cross_region" type="bool">
    </ResponseField>
    <ResponseField name="dialogflow_agent_to_link" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="chat_engine_metadata" type="list(object)">
  Additional information of the Chat Engine. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="dialogflow_agent" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="common_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="company_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Timestamp the Engine was created at. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The unique full resource name of the chat engine. Values are of the format 'projects/&#123;project&#125;/locations/&#123;location&#125;/collections/&#123;collection_id&#125;/engines/&#123;engine_id&#125;'. This field must be a UTF-8 encoded string with a length limit of 1024 characters. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Timestamp the Engine was last updated. Computed by GCP after `ship`.
</ResponseField>
