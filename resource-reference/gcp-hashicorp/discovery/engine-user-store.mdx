---
title: "google_discovery_engine_user_store"
description: "Real, generated bindings for `google_discovery_engine_user_store`."
---

`google_discovery_engine_user_store` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	discovery "github.com/ubiquex/ubx-sdk-google/sdk/go/google/discovery"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own engine user store"})

    		ubx.Resource(discovery.EngineUserStore, "ci-runner", discovery.EngineUserStoreConfig{
    			Location:                       "example",
    			DefaultLicenseConfig:           "example",
    			EnableExpiredLicenseAutoUpdate: true,
    			EnableLicenseAutoRegister:      true,
    			UserStoreId:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EngineUserStore } from "jsr:@ubx/sdk-google/google/discovery/engine_user_store";

    export default stack("payments", () => {
      intent({ summary: "payments own engine user store" });

      resource(EngineUserStore, "ci-runner", {
        location: "example",
        defaultLicenseConfig: "example",
        enableExpiredLicenseAutoUpdate: true,
        enableLicenseAutoRegister: true,
        userStoreId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.discovery import EngineUserStore, EngineUserStoreConfig

    def describe():
        ubx.intent("payments own engine user store")

        ubx.resource(EngineUserStore, "ci-runner", EngineUserStoreConfig(
            location="example",
            default_license_config="example",
            enable_expired_license_auto_update=True,
            enable_license_auto_register=True,
            user_store_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_discovery_engine_user_store, with location example,
    default license config example, enable expired license auto
    update true, enable license auto register true, user store id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_license_config" type="string">
  The resource name of the default license config assigned to users created in this user store. Format: 'projects/&#123;project&#125;/locations/&#123;location&#125;/licenseConfigs/&#123;license_config&#125;'. If 'enableLicenseAutoRegister' is true, new users will automatically register under the default subscription. If the default license config doesn't have remaining license seats left, new users will not be assigned with license. Optional.
</ResponseField>

<ResponseField name="enable_expired_license_auto_update" type="bool">
  Whether to enable automatic license update for users with expired licenses in this user store. If enabled, users with expired licenses will automatically be updated to the default subscription if there are remaining license seats. Optional.
</ResponseField>

<ResponseField name="enable_license_auto_register" type="bool">
  Whether to enable automatic license registration for new users created in this user store. If enabled, new users will automatically register under the default subscription. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The geographic location where the data store should reside. The value can only be one of "global", "us" and "eu". Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_store_id" type="string">
  The ID of the user store. Currently only accepts "default_user_store". Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The unique full resource name of the user store. Values are of the format 'projects/&#123;project&#125;/locations/&#123;location&#125;/userStores/&#123;user_store_id&#125;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
