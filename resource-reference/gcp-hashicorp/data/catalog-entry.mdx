---
title: "google_data_catalog_entry"
description: "Real, generated bindings for `google_data_catalog_entry`."
---

`google_data_catalog_entry` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	data "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog entry"})

    		ubx.Resource(data.CatalogEntry, "ci-runner", data.CatalogEntryConfig{
    			EntryGroup:          "example",
    			EntryId:             "example",
    			Description:         "Managed by ubx.",
    			DisplayName:         "example-display-name",
    			Schema:              "example",
    			Type:                "example",
    			UserSpecifiedSystem: "example",
    			UserSpecifiedType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogEntry } from "jsr:@ubx/sdk-google/google/data/catalog_entry";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog entry" });

      resource(CatalogEntry, "ci-runner", {
        entryGroup: "example",
        entryId: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        schema: "example",
        type: "example",
        userSpecifiedSystem: "example",
        userSpecifiedType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data import CatalogEntry, CatalogEntryConfig

    def describe():
        ubx.intent("payments own catalog entry")

        ubx.resource(CatalogEntry, "ci-runner", CatalogEntryConfig(
            entry_group="example",
            entry_id="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            schema="example",
            type="example",
            user_specified_system="example",
            user_specified_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_data_catalog_entry, with entry group example, entry
    id example, description Managed by ubx., display name
    example-display-name, schema example, type example, user
    specified system example, user specified type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Entry description, which can consist of several sentences or paragraphs that describe entry contents. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display information such as title and description. A short name to identify the entry, for example, "Analytics Data - Jan 2011". Optional.
</ResponseField>

<ResponseField name="entry_group" type="string" required>
  The name of the entry group this entry is in. Required.
</ResponseField>

<ResponseField name="entry_id" type="string" required>
  The id of the entry to create. Required.
</ResponseField>

<ResponseField name="gcs_fileset_spec" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="file_patterns" type="list(string)" required>
    </ResponseField>
    <ResponseField name="sample_gcs_file_specs" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="file_path" type="string">
        </ResponseField>
        <ResponseField name="size_bytes" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="linked_resource" type="string">
  The resource this metadata entry refers to. For Google Cloud Platform resources, linkedResource is the full name of the resource. For example, the linkedResource for a table resource from BigQuery is: //bigquery.googleapis.com/projects/projectId/datasets/datasetId/tables/tableId Output only when Entry is of type in the EntryType enum. For entries with userSpecifiedType, this field is optional and defaults to an empty string. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="schema" type="string">
  Schema of the entry (e.g. BigQuery, GoogleSQL, Avro schema), as a json string. An entry might not have any schema attached to it. See https://cloud.google.com/data-catalog/docs/reference/rest/v1/projects.locations.entryGroups.entries#schema for what fields this schema can contain. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the entry. Only used for Entries with types in the EntryType enum. Currently, only FILESET enum value is allowed. All other entries created through Data Catalog must use userSpecifiedType. Possible values: ["FILESET"] Optional.
</ResponseField>

<ResponseField name="user_specified_system" type="string">
  This field indicates the entry's source system that Data Catalog does not integrate with. userSpecifiedSystem strings must begin with a letter or underscore and can only contain letters, numbers, and underscores; are case insensitive; must be at least 1 character and at most 64 characters long. Optional.
</ResponseField>

<ResponseField name="user_specified_type" type="string">
  Entry type if it does not fit any of the input-allowed values listed in EntryType enum above. When creating an entry, users should check the enum values first, if nothing matches the entry to be created, then provide a custom value, for example "my_special_type". userSpecifiedType strings must begin with a letter or underscore and can only contain letters, numbers, and underscores; are case insensitive; must be at least 1 character and at most 64 characters long. Optional.
</ResponseField>

## Output properties

<ResponseField name="bigquery_date_sharded_spec" type="list(object)">
  Specification for a group of BigQuery tables with name pattern [prefix]YYYYMMDD. Context: https://cloud.google.com/bigquery/docs/partitioned-tables#partitioning_versus_sharding. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="dataset" type="string">
    </ResponseField>
    <ResponseField name="shard_count" type="number">
    </ResponseField>
    <ResponseField name="table_prefix" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="bigquery_table_spec" type="list(object)">
  Specification that applies to a BigQuery table. This is only valid on entries of type TABLE. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="table_source_type" type="string">
    </ResponseField>
    <ResponseField name="table_spec" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="grouped_entry" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="view_spec" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="view_query" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="gcs_fileset_spec" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="file_patterns" type="list(string)" required>
    </ResponseField>
    <ResponseField name="sample_gcs_file_specs" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="file_path" type="string">
        </ResponseField>
        <ResponseField name="size_bytes" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="integrated_system" type="string">
  This field indicates the entry's source system that Data Catalog integrates with, such as BigQuery or Pub/Sub. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="linked_resource" type="string">
  The resource this metadata entry refers to. For Google Cloud Platform resources, linkedResource is the full name of the resource. For example, the linkedResource for a table resource from BigQuery is: //bigquery.googleapis.com/projects/projectId/datasets/datasetId/tables/tableId Output only when Entry is of type in the EntryType enum. For entries with userSpecifiedType, this field is optional and defaults to an empty string. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The Data Catalog resource name of the entry in URL format. Example: projects/&#123;project_id&#125;/locations/&#123;location&#125;/entryGroups/&#123;entryGroupId&#125;/entries/&#123;entryId&#125;. Note that this Entry and its child resources may not actually be stored in the location in this name. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
