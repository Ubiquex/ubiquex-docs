---
title: "google_data_catalog_tag_template_iam_policy"
description: "Real, generated bindings for `google_data_catalog_tag_template_iam_policy`."
---

`google_data_catalog_tag_template_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	data "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog tag template iam policy"})

    		ubx.Resource(data.CatalogTagTemplateIamPolicy, "ci-runner", data.CatalogTagTemplateIamPolicyConfig{
    			PolicyData:  "example",
    			TagTemplate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogTagTemplateIamPolicy } from "jsr:@ubx/sdk-google/google/data/catalog_tag_template_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog tag template iam policy" });

      resource(CatalogTagTemplateIamPolicy, "ci-runner", {
        policyData: "example",
        tagTemplate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data import CatalogTagTemplateIamPolicy, CatalogTagTemplateIamPolicyConfig

    def describe():
        ubx.intent("payments own catalog tag template iam policy")

        ubx.resource(CatalogTagTemplateIamPolicy, "ci-runner", CatalogTagTemplateIamPolicyConfig(
            policy_data="example",
            tag_template="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_data_catalog_tag_template_iam_policy, with policy
    data example, tag template example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tag_template" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
