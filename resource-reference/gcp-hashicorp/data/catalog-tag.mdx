---
title: "google_data_catalog_tag"
description: "Real, generated bindings for `google_data_catalog_tag`."
---

`google_data_catalog_tag` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	data "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog tag"})

    		ubx.Resource(data.CatalogTag, "ci-runner", data.CatalogTagConfig{
    			Template: "example",
    			Column:   "example",
    			Parent:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogTag } from "jsr:@ubx/sdk-google/google/data/catalog_tag";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog tag" });

      resource(CatalogTag, "ci-runner", {
        template: "example",
        column: "example",
        parent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data import CatalogTag, CatalogTagConfig

    def describe():
        ubx.intent("payments own catalog tag")

        ubx.resource(CatalogTag, "ci-runner", CatalogTagConfig(
            template="example",
            column="example",
            parent="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_data_catalog_tag, with template example, column
    example, parent example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="column" type="string">
  Resources like Entry can have schemas associated with them. This scope allows users to attach tags to an individual column based on that schema. For attaching a tag to a nested column, use '.' to separate the column names. Example: 'outer_column.inner_column' Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fields" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="bool_value" type="bool">
    </ResponseField>
    <ResponseField name="display_name" type="string">
    </ResponseField>
    <ResponseField name="double_value" type="number">
    </ResponseField>
    <ResponseField name="enum_value" type="string">
    </ResponseField>
    <ResponseField name="field_name" type="string" required>
    </ResponseField>
    <ResponseField name="order" type="number">
    </ResponseField>
    <ResponseField name="string_value" type="string">
    </ResponseField>
    <ResponseField name="timestamp_value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string">
  The name of the parent this tag is attached to. This can be the name of an entry or an entry group. If an entry group, the tag will be attached to all entries in that group. Optional.
</ResponseField>

<ResponseField name="template" type="string" required>
  The resource name of the tag template that this tag uses. Example: projects/&#123;project_id&#125;/locations/&#123;location&#125;/tagTemplates/&#123;tagTemplateId&#125; This field cannot be modified after creation. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fields" type="set(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="bool_value" type="bool">
    </ResponseField>
    <ResponseField name="display_name" type="string">
    </ResponseField>
    <ResponseField name="double_value" type="number">
    </ResponseField>
    <ResponseField name="enum_value" type="string">
    </ResponseField>
    <ResponseField name="field_name" type="string" required>
    </ResponseField>
    <ResponseField name="order" type="number">
    </ResponseField>
    <ResponseField name="string_value" type="string">
    </ResponseField>
    <ResponseField name="timestamp_value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the tag in URL format. Example: projects/&#123;project_id&#125;/locations/&#123;location&#125;/entrygroups/&#123;entryGroupId&#125;/entries/&#123;entryId&#125;/tags/&#123;tag_id&#125; or projects/&#123;project_id&#125;/locations/&#123;location&#125;/entrygroups/&#123;entryGroupId&#125;/tags/&#123;tag_id&#125; where tag_id is a system-generated identifier. Note that this Tag may not actually be stored in the location in this name. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="template_displayname" type="string">
  The display name of the tag template. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
