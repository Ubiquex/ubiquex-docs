---
title: "google_data_catalog_taxonomy_iam_policy"
description: "Real, generated bindings for `google_data_catalog_taxonomy_iam_policy`."
---

`google_data_catalog_taxonomy_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	data "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own catalog taxonomy iam policy"})

    		ubx.Resource(data.CatalogTaxonomyIamPolicy, "ci-runner", data.CatalogTaxonomyIamPolicyConfig{
    			PolicyData: "example",
    			Taxonomy:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CatalogTaxonomyIamPolicy } from "jsr:@ubx/sdk-google/google/data/catalog_taxonomy_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own catalog taxonomy iam policy" });

      resource(CatalogTaxonomyIamPolicy, "ci-runner", {
        policyData: "example",
        taxonomy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data import CatalogTaxonomyIamPolicy, CatalogTaxonomyIamPolicyConfig

    def describe():
        ubx.intent("payments own catalog taxonomy iam policy")

        ubx.resource(CatalogTaxonomyIamPolicy, "ci-runner", CatalogTaxonomyIamPolicyConfig(
            policy_data="example",
            taxonomy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_data_catalog_taxonomy_iam_policy, with policy data
    example, taxonomy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="taxonomy" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
