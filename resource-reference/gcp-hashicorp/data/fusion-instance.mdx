---
title: "google_data_fusion_instance"
description: "Real, generated bindings for `google_data_fusion_instance`."
---

`google_data_fusion_instance` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	data "github.com/ubiquex/ubx-sdk-google/sdk/go/google/data"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fusion instance"})

    		ubx.Resource(data.FusionInstance, "ci-runner", data.FusionInstanceConfig{
    			Name:                        "ci-runner",
    			Type:                        "example",
    			DataprocServiceAccount:      "example",
    			Description:                 "Managed by ubx.",
    			DisplayName:                 "example-display-name",
    			EnableRbac:                  true,
    			EnableStackdriverLogging:    true,
    			EnableStackdriverMonitoring: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FusionInstance } from "jsr:@ubx/sdk-google/google/data/fusion_instance";

    export default stack("payments", () => {
      intent({ summary: "payments own fusion instance" });

      resource(FusionInstance, "ci-runner", {
        name: "ci-runner",
        type: "example",
        dataprocServiceAccount: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        enableRbac: true,
        enableStackdriverLogging: true,
        enableStackdriverMonitoring: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.data import FusionInstance, FusionInstanceConfig

    def describe():
        ubx.intent("payments own fusion instance")

        ubx.resource(FusionInstance, "ci-runner", FusionInstanceConfig(
            name="ci-runner",
            type="example",
            dataproc_service_account="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            enable_rbac=True,
            enable_stackdriver_logging=True,
            enable_stackdriver_monitoring=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_data_fusion_instance called "ci-runner", with type
    example, dataproc service account example, description Managed by
    ubx., display name example-display-name, enable rbac true, enable
    stackdriver logging true, enable stackdriver monitoring true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accelerators" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="accelerator_type" type="string" required>
    </ResponseField>
    <ResponseField name="state" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="crypto_key_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key_reference" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dataproc_service_account" type="string">
  User-managed service account to set on Dataproc when Cloud Data Fusion creates Dataproc to run data processing pipelines. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of the instance. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Display name for an instance. Optional.
</ResponseField>

<ResponseField name="enable_rbac" type="bool">
  Option to enable granular role-based access control. Optional.
</ResponseField>

<ResponseField name="enable_stackdriver_logging" type="bool">
  Option to enable Stackdriver Logging. Optional.
</ResponseField>

<ResponseField name="enable_stackdriver_monitoring" type="bool">
  Option to enable Stackdriver Monitoring. Optional.
</ResponseField>

<ResponseField name="event_publish_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="topic" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The resource labels for instance to use to annotate any related underlying resources, such as Compute Engine VMs. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="maintenance_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="maintenance_window" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="recurring_time_window" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="recurrence" type="string" required>
            </ResponseField>
            <ResponseField name="window" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="end_time" type="string" required>
                </ResponseField>
                <ResponseField name="start_time" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The ID of the instance or a fully qualified identifier for the instance. Required.
</ResponseField>

<ResponseField name="network_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string">
    </ResponseField>
    <ResponseField name="ip_allocation" type="string">
    </ResponseField>
    <ResponseField name="network" type="string">
    </ResponseField>
    <ResponseField name="private_service_connect_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="effective_unreachable_cidr_block" type="string">
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
        </ResponseField>
        <ResponseField name="unreachable_cidr_block" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="options" type="map(string)">
  Map of additional options used to configure the behavior of Data Fusion instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="patch_revision" type="string">
  Current patch revision of the Data Fusion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="private_instance" type="bool">
  Specifies whether the Data Fusion instance should be private. If set to true, all Data Fusion nodes will have private IP addresses and will not be able to access the public internet. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region of the Data Fusion instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  A map of resource manager tags. Resource manager tag keys and values have the same definition as resource manager tags. Keys must be in the format tagKeys/&#123;tag_key_id&#125;, and values are in the format tagValues/&#123;tag_value_id&#125;. The field is ignored (both PUT & PATCH) when empty. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Represents the type of Data Fusion instance. Each type is configured with the default settings for processing and memory. - BASIC: Basic Data Fusion instance. In Basic type, the user will be able to create data pipelines using point and click UI. However, there are certain limitations, such as fewer number of concurrent pipelines, no support for streaming pipelines, etc. - ENTERPRISE: Enterprise Data Fusion instance. In Enterprise type, the user will have more features available, such as support for streaming pipelines, higher number of concurrent pipelines, etc. - DEVELOPER: Developer Data Fusion instance. In Developer type, the user will have all features available but with restrictive capabilities. This is to help enterprises design and develop their data ingestion and integration pipelines at low cost. Possible values: ["BASIC", "ENTERPRISE", "DEVELOPER"] Required.
</ResponseField>

<ResponseField name="version" type="string">
  Current version of the Data Fusion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string">
  Name of the zone in which the Data Fusion instance will be created. Only DEVELOPER instances use this field. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="accelerators" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="accelerator_type" type="string" required>
    </ResponseField>
    <ResponseField name="state" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="api_endpoint" type="string">
  Endpoint on which the REST APIs is accessible. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  The time the instance was created in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="crypto_key_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key_reference" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="event_publish_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="topic" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gcs_bucket" type="string">
  Cloud Storage bucket generated by Data Fusion in the customer project. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="maintenance_events" type="list(object)">
  The maintenance events for this instance. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="end_time" type="string">
    </ResponseField>
    <ResponseField name="start_time" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="maintenance_policy" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="maintenance_window" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="recurring_time_window" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="recurrence" type="string" required>
            </ResponseField>
            <ResponseField name="window" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="end_time" type="string" required>
                </ResponseField>
                <ResponseField name="start_time" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="network_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="connection_type" type="string">
    </ResponseField>
    <ResponseField name="ip_allocation" type="string">
    </ResponseField>
    <ResponseField name="network" type="string">
    </ResponseField>
    <ResponseField name="private_service_connect_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="effective_unreachable_cidr_block" type="string">
        </ResponseField>
        <ResponseField name="network_attachment" type="string">
        </ResponseField>
        <ResponseField name="unreachable_cidr_block" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="options" type="map(string)">
  Map of additional options used to configure the behavior of Data Fusion instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="p4_service_account" type="string">
  P4 service account for the customer project. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="patch_revision" type="string">
  Current patch revision of the Data Fusion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region of the Data Fusion instance. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_endpoint" type="string">
  Endpoint on which the Data Fusion UI and REST APIs are accessible. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of this Data Fusion instance. - CREATING: Instance is being created - RUNNING: Instance is running and ready for requests - FAILED: Instance creation failed - DELETING: Instance is being deleted - UPGRADING: Instance is being upgraded - RESTARTING: Instance is being restarted Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_message" type="string">
  Additional information about the current state of this Data Fusion instance if available. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="tenant_project_id" type="string">
  The name of the tenant project. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time the instance was last updated in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="version" type="string">
  Current version of the Data Fusion. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="zone" type="string">
  Name of the zone in which the Data Fusion instance will be created. Only DEVELOPER instances use this field. Optional; if omitted, computed by GCP.
</ResponseField>
