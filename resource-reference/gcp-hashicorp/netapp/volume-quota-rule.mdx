---
title: "google_netapp_volume_quota_rule"
description: "Real, generated bindings for `google_netapp_volume_quota_rule`."
---

`google_netapp_volume_quota_rule` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	netapp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/netapp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own volume quota rule"})

    		ubx.Resource(netapp.VolumeQuotaRule, "ci-runner", netapp.VolumeQuotaRuleConfig{
    			DiskLimitMib: 1,
    			Name:         "ci-runner",
    			Type:         "example",
    			VolumeName:   "example-volume-name",
    			Description:  "Managed by ubx.",
    			Labels:       map[string]string{"managed-by": "ubx"},
    			Location:     "example",
    			Target:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VolumeQuotaRule } from "jsr:@ubx/sdk-google/google/netapp/volume_quota_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own volume quota rule" });

      resource(VolumeQuotaRule, "ci-runner", {
        diskLimitMib: 1,
        name: "ci-runner",
        type: "example",
        volumeName: "example-volume-name",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        location: "example",
        target: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.netapp import VolumeQuotaRule, VolumeQuotaRuleConfig

    def describe():
        ubx.intent("payments own volume quota rule")

        ubx.resource(VolumeQuotaRule, "ci-runner", VolumeQuotaRuleConfig(
            disk_limit_mib=1,
            name="ci-runner",
            type="example",
            volume_name="example-volume-name",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            location="example",
            target="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_netapp_volume_quota_rule called "ci-runner", with
    disk limit mib 1, type example, volume name example-volume-name,
    description Managed by ubx., labels a set value, location
    example, target example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description for the quota rule. Optional.
</ResponseField>

<ResponseField name="disk_limit_mib" type="number" required>
  The maximum allowed capacity in MiB. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs of the quota rule. Example: '&#123; "owner": "Bob", "department": "finance", "purpose": "testing" &#125;'. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string">
  Loction of the quotaRule. QuotaRules are child resources of volumes and live in the same location. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name of the quotaRule. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target" type="string">
  The quota rule applies to the specified user or group. Valid targets for volumes with NFS protocol enabled: - UNIX UID for individual user quota - UNIX GID for individual group quota Valid targets for volumes with SMB protocol enabled: - Windows SID for individual user quota Leave empty for default quotas Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Types of Quota Rule. Possible values: ["INDIVIDUAL_USER_QUOTA", "INDIVIDUAL_GROUP_QUOTA", "DEFAULT_USER_QUOTA", "DEFAULT_GROUP_QUOTA"] Required.
</ResponseField>

<ResponseField name="volume_name" type="string" required>
  Name of the volume to create the quotaRule in. Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Create time of the quota rule. A timestamp in RFC3339 UTC "Zulu" format. Examples: "2023-06-22T09:13:01.617Z". Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the quota rule. Possible Values : [STATE_UNSPECIFIED, CREATING, UPDATING, READY, DELETING, ERROR] Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state_details" type="string">
  State details of the quota rule Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
