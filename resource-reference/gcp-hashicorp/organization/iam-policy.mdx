---
title: "google_organization_iam_policy"
description: "Real, generated bindings for `google_organization_iam_policy`."
---

`google_organization_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	organization "github.com/ubiquex/ubx-sdk-google/sdk/go/google/organization"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iam policy"})

    		ubx.Resource(organization.IamPolicy, "ci-runner", organization.IamPolicyConfig{
    			OrgId:      "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamPolicy } from "jsr:@ubx/sdk-google/google/organization/iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own iam policy" });

      resource(IamPolicy, "ci-runner", {
        orgId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.organization import IamPolicy, IamPolicyConfig

    def describe():
        ubx.intent("payments own iam policy")

        ubx.resource(IamPolicy, "ci-runner", IamPolicyConfig(
            org_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_organization_iam_policy, with org id example,
    policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The numeric ID of the organization in which you want to manage the audit logging config. Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
