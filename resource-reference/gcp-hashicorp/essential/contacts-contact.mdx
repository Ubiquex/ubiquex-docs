---
title: "google_essential_contacts_contact"
description: "Real, generated bindings for `google_essential_contacts_contact`."
---

`google_essential_contacts_contact` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	essential "github.com/ubiquex/ubx-sdk-google/sdk/go/google/essential"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own contacts contact"})

    		ubx.Resource(essential.ContactsContact, "ci-runner", essential.ContactsContactConfig{
    			Email:                             "example",
    			LanguageTag:                       "example",
    			NotificationCategorySubscriptions: []string{"example"},
    			Parent:                            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContactsContact } from "jsr:@ubx/sdk-google/google/essential/contacts_contact";

    export default stack("payments", () => {
      intent({ summary: "payments own contacts contact" });

      resource(ContactsContact, "ci-runner", {
        email: "example",
        languageTag: "example",
        notificationCategorySubscriptions: ["example"],
        parent: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.essential import ContactsContact, ContactsContactConfig

    def describe():
        ubx.intent("payments own contacts contact")

        ubx.resource(ContactsContact, "ci-runner", ContactsContactConfig(
            email="example",
            language_tag="example",
            notification_category_subscriptions=["example"],
            parent="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_essential_contacts_contact, with email example,
    language tag example, notification category subscriptions a set
    value, parent example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="email" type="string" required>
  The email address to send notifications to. This does not need to be a Google account. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="language_tag" type="string" required>
  The preferred language for notifications, as a ISO 639-1 language code. See Supported languages for a list of supported languages. Required.
</ResponseField>

<ResponseField name="notification_category_subscriptions" type="list(string)" required>
  The categories of notifications that the contact will receive communications for. Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The resource to save this contact for. Format: organizations/&#123;organization_id&#125;, folders/&#123;folder_id&#125; or projects/&#123;project_id&#125; Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The identifier for the contact. Format: &#123;resourceType&#125;/&#123;resource_id&#125;/contacts/&#123;contact_id&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
