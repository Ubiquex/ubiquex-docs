---
title: "google_agent_identity_auth_provider"
description: "Real, generated bindings for `google_agent_identity_auth_provider`."
---

`google_agent_identity_auth_provider` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	agent "github.com/ubiquex/ubx-sdk-google/sdk/go/google/agent"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity auth provider"})

    		ubx.Resource(agent.IdentityAuthProvider, "ci-runner", agent.IdentityAuthProviderConfig{
    			AuthProviderId: "example",
    			Location:       "example",
    			AllowedScopes:  []string{"example"},
    			BlockedScopes:  []string{"example"},
    			Description:    "Managed by ubx.",
    			Labels:         map[string]string{"managed-by": "ubx"},
    			WorkloadIds:    []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityAuthProvider } from "jsr:@ubx/sdk-google/google/agent/identity_auth_provider";

    export default stack("payments", () => {
      intent({ summary: "payments own identity auth provider" });

      resource(IdentityAuthProvider, "ci-runner", {
        authProviderId: "example",
        location: "example",
        allowedScopes: ["example"],
        blockedScopes: ["example"],
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        workloadIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.agent import IdentityAuthProvider, IdentityAuthProviderConfig

    def describe():
        ubx.intent("payments own identity auth provider")

        ubx.resource(IdentityAuthProvider, "ci-runner", IdentityAuthProviderConfig(
            auth_provider_id="example",
            location="example",
            allowed_scopes=["example"],
            blocked_scopes=["example"],
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            workload_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_agent_identity_auth_provider, with auth provider id
    example, location example, allowed scopes a set value, blocked
    scopes a set value, description Managed by ubx., labels a set
    value, workload ids a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allowed_scopes" type="list(string)">
  List of scopes that are allowed to be requested for this auth_provider. If this list is non-empty, only scopes within this list may be requested. If this list is empty, all scopes may be requested. Scopes appearing in 'blocked_scopes' are disallowed even if they appear in 'allowed_scopes'. The number of allowed scopes is limited to 200. Optional.
</ResponseField>

<ResponseField name="auth_provider_id" type="string" required>
  The ID to use for the AuthProvider, which will become the final segment of the AuthProvider's resource name. This value should be 1-63 characters, and valid characters are /a-z-/. The first character must be a lowercase letter, and the last character must be a lowercase letter or a number. Required.
</ResponseField>

<ResponseField name="auth_provider_type_params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="api_key" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="api_key" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ge_auth_provider" type="list(object)">
    </ResponseField>
    <ResponseField name="three_legged_oauth" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="authorization_url" type="string">
        </ResponseField>
        <ResponseField name="client_id" type="string">
        </ResponseField>
        <ResponseField name="client_secret" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo_version" type="string">
        </ResponseField>
        <ResponseField name="default_continue_uri" type="string">
        </ResponseField>
        <ResponseField name="enable_pkce" type="bool">
        </ResponseField>
        <ResponseField name="redirect_url" type="string">
        </ResponseField>
        <ResponseField name="token_url" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="two_legged_oauth" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
        </ResponseField>
        <ResponseField name="client_secret" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo_version" type="string">
        </ResponseField>
        <ResponseField name="token_url" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="blocked_scopes" type="list(string)">
  List of scopes that are blocked from being requested for this auth_provider. If a scope appears in this list, it will not be requested, even if it also appears in 'allowed_scopes'. 'blocked_scopes' takes precedence over 'allowed_scopes'. The number of blocked scopes is limited to 200. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Description of the resource. Must be less than 256 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key value pairs **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workload_ids" type="list(string)">
  Input only. Represents the workload identity in IAM 'principal://' format of the agent(s) that will use this AuthProvider. Example: 'principal://agents.global.org-$&#123;ORG_ID&#125;.system.id.goog/resources/aiplatform/projects/&#123;PROJECT_ID&#125;/locations/&#123;LOCATIONS&#125;/reasoningEngines/&#123;ID&#125;' Optional.
</ResponseField>

## Output properties

<ResponseField name="auth_provider_type_params" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="api_key" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="api_key" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ge_auth_provider" type="list(object)">
    </ResponseField>
    <ResponseField name="three_legged_oauth" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="authorization_url" type="string">
        </ResponseField>
        <ResponseField name="client_id" type="string">
        </ResponseField>
        <ResponseField name="client_secret" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo_version" type="string">
        </ResponseField>
        <ResponseField name="default_continue_uri" type="string">
        </ResponseField>
        <ResponseField name="enable_pkce" type="bool">
        </ResponseField>
        <ResponseField name="redirect_url" type="string">
        </ResponseField>
        <ResponseField name="token_url" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="two_legged_oauth" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
        </ResponseField>
        <ResponseField name="client_secret" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo" type="string">
        </ResponseField>
        <ResponseField name="client_secret_wo_version" type="string">
        </ResponseField>
        <ResponseField name="token_url" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  [Output only] Create time stamp Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deleted" type="bool">
  This is set to true if the auth_provider is deleted. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="expire_time" type="string">
  The time when the auth_provider will expire. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The full resource name of the auth_provider. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/authProviders/&#123;auth_provider&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the auth_provider. Possible values: ENABLED DISABLED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  [Output only] Update time stamp Computed by GCP after `ship`.
</ResponseField>
