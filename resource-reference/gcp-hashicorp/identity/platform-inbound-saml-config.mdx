---
title: "google_identity_platform_inbound_saml_config"
description: "Real, generated bindings for `google_identity_platform_inbound_saml_config`."
---

`google_identity_platform_inbound_saml_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	identity "github.com/ubiquex/ubx-sdk-google/sdk/go/google/identity"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own platform inbound saml config"})

    		ubx.Resource(identity.PlatformInboundSamlConfig, "ci-runner", identity.PlatformInboundSamlConfigConfig{
    			DisplayName: "example-display-name",
    			Name:        "ci-runner",
    			Enabled:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PlatformInboundSamlConfig } from "jsr:@ubx/sdk-google/google/identity/platform_inbound_saml_config";

    export default stack("payments", () => {
      intent({ summary: "payments own platform inbound saml config" });

      resource(PlatformInboundSamlConfig, "ci-runner", {
        displayName: "example-display-name",
        name: "ci-runner",
        enabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.identity import PlatformInboundSamlConfig, PlatformInboundSamlConfigConfig

    def describe():
        ubx.intent("payments own platform inbound saml config")

        ubx.resource(PlatformInboundSamlConfig, "ci-runner", PlatformInboundSamlConfigConfig(
            display_name="example-display-name",
            name="ci-runner",
            enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_identity_platform_inbound_saml_config called
    "ci-runner", with display name example-display-name, enabled
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Human friendly display name. Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  If this config allows users to sign in with the provider. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="idp_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="idp_certificates" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="x509_certificate" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="idp_entity_id" type="string" required>
    </ResponseField>
    <ResponseField name="sign_request" type="bool">
    </ResponseField>
    <ResponseField name="sso_url" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the InboundSamlConfig resource. Must start with 'saml.' and can only have alphanumeric characters, hyphens, underscores or periods. The part after 'saml.' must also start with a lowercase letter, end with an alphanumeric character, and have at least 2 characters. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sp_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="callback_uri" type="string">
    </ResponseField>
    <ResponseField name="sp_certificates" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="x509_certificate" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sp_entity_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="idp_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="idp_certificates" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="x509_certificate" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="idp_entity_id" type="string" required>
    </ResponseField>
    <ResponseField name="sign_request" type="bool">
    </ResponseField>
    <ResponseField name="sso_url" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="sp_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="callback_uri" type="string">
    </ResponseField>
    <ResponseField name="sp_certificates" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="x509_certificate" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sp_entity_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
