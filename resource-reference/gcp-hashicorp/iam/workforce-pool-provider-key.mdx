---
title: "google_iam_workforce_pool_provider_key"
description: "Real, generated bindings for `google_iam_workforce_pool_provider_key`."
---

`google_iam_workforce_pool_provider_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workforce pool provider key"})

    		ubx.Resource(iam.WorkforcePoolProviderKey, "ci-runner", iam.WorkforcePoolProviderKeyConfig{
    			KeyId:           "example",
    			Location:        "example",
    			ProviderId:      "example",
    			Use:             "example",
    			WorkforcePoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkforcePoolProviderKey } from "jsr:@ubx/sdk-google/google/iam/workforce_pool_provider_key";

    export default stack("payments", () => {
      intent({ summary: "payments own workforce pool provider key" });

      resource(WorkforcePoolProviderKey, "ci-runner", {
        keyId: "example",
        location: "example",
        providerId: "example",
        use: "example",
        workforcePoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import WorkforcePoolProviderKey, WorkforcePoolProviderKeyConfig

    def describe():
        ubx.intent("payments own workforce pool provider key")

        ubx.resource(WorkforcePoolProviderKey, "ci-runner", WorkforcePoolProviderKeyConfig(
            key_id="example",
            location="example",
            provider_id="example",
            use="example",
            workforce_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_workforce_pool_provider_key, with key id
    example, location example, provider id example, use example,
    workforce pool id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_data" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="format" type="string">
    </ResponseField>
    <ResponseField name="key" type="string">
    </ResponseField>
    <ResponseField name="key_spec" type="string" required>
    </ResponseField>
    <ResponseField name="not_after_time" type="string">
    </ResponseField>
    <ResponseField name="not_before_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="key_id" type="string" required>
  The ID to use for the key, which becomes the final component of the resource name. This value must be 4-32 characters, and may contain the characters [a-z0-9-]. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the resource. Required.
</ResponseField>

<ResponseField name="provider_id" type="string" required>
  The ID of the provider. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="use" type="string" required>
  The purpose of the key. Possible values: ["ENCRYPTION"] Required.
</ResponseField>

<ResponseField name="workforce_pool_id" type="string" required>
  The ID of the workforce pool. Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expire_time" type="string">
  The time after which the key will be permanently deleted and cannot be recovered. Note that the key may get purged before this time if the total limit of keys per provider is exceeded. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_data" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="format" type="string">
    </ResponseField>
    <ResponseField name="key" type="string">
    </ResponseField>
    <ResponseField name="key_spec" type="string" required>
    </ResponseField>
    <ResponseField name="not_after_time" type="string">
    </ResponseField>
    <ResponseField name="not_before_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the key. Format: 'locations/&#123;location&#125;/workforcePools/&#123;workforcePoolId&#125;/providers/&#123;providerId&#125;/keys/&#123;keyId&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the key. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
