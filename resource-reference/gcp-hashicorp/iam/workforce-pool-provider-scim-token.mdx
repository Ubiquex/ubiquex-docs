---
title: "google_iam_workforce_pool_provider_scim_token"
description: "Real, generated bindings for `google_iam_workforce_pool_provider_scim_token`."
---

`google_iam_workforce_pool_provider_scim_token` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workforce pool provider scim token"})

    		ubx.Resource(iam.WorkforcePoolProviderScimToken, "ci-runner", iam.WorkforcePoolProviderScimTokenConfig{
    			Location:        "example",
    			ProviderId:      "example",
    			ScimTenantId:    "example",
    			ScimTokenId:     "example",
    			WorkforcePoolId: "example",
    			DisplayName:     "example-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkforcePoolProviderScimToken } from "jsr:@ubx/sdk-google/google/iam/workforce_pool_provider_scim_token";

    export default stack("payments", () => {
      intent({ summary: "payments own workforce pool provider scim token" });

      resource(WorkforcePoolProviderScimToken, "ci-runner", {
        location: "example",
        providerId: "example",
        scimTenantId: "example",
        scimTokenId: "example",
        workforcePoolId: "example",
        displayName: "example-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import WorkforcePoolProviderScimToken, WorkforcePoolProviderScimTokenConfig

    def describe():
        ubx.intent("payments own workforce pool provider scim token")

        ubx.resource(WorkforcePoolProviderScimToken, "ci-runner", WorkforcePoolProviderScimTokenConfig(
            location="example",
            provider_id="example",
            scim_tenant_id="example",
            scim_token_id="example",
            workforce_pool_id="example",
            display_name="example-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_workforce_pool_provider_scim_token, with
    location example, provider id example, scim tenant id example,
    scim token id example, workforce pool id example, display name
    example-display-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  A user-specified display name for the scim token. Cannot exceed 32 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the resource. Required.
</ResponseField>

<ResponseField name="provider_id" type="string" required>
  The ID of the Provider. Required.
</ResponseField>

<ResponseField name="scim_tenant_id" type="string" required>
  The ID of the SCIM Tenant. Required.
</ResponseField>

<ResponseField name="scim_token_id" type="string" required>
  The ID to use for the SCIM Token, which becomes the final component of the resource name. This value should be 4-32 characters and follow the pattern: '([a-z]([a-z0-9\\-]&#123;2,30&#125;[a-z0-9]))'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workforce_pool_id" type="string" required>
  The ID of the Workforce Pool. Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the scim token. Format: 'locations/&#123;location&#125;/workforcePools/&#123;workforce_pool&#125;/providers/&#123;workforce_pool_provider&#125;/scimTenants/&#123;scim_tenant_id&#125;/tokens/&#123;scim_token_id&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="security_token" type="string">
  The token string provided to the IdP for authentication and will be set only during creation. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the scim token. * ACTIVE: The token is active and may be used to provision users and groups. * DELETED: The token is soft-deleted. Soft-deleted tokens are permanently deleted after approximately 30 days. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
