---
title: "google_iam_workforce_pool_provider_scim_tenant"
description: "Real, generated bindings for `google_iam_workforce_pool_provider_scim_tenant`."
---

`google_iam_workforce_pool_provider_scim_tenant` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workforce pool provider scim tenant"})

    		ubx.Resource(iam.WorkforcePoolProviderScimTenant, "ci-runner", iam.WorkforcePoolProviderScimTenantConfig{
    			Location:        "example",
    			ProviderId:      "example",
    			ScimTenantId:    "example",
    			WorkforcePoolId: "example",
    			ClaimMapping:    map[string]string{"managed-by": "ubx"},
    			Description:     "Managed by ubx.",
    			DisplayName:     "example-display-name",
    			HardDelete:      true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkforcePoolProviderScimTenant } from "jsr:@ubx/sdk-google/google/iam/workforce_pool_provider_scim_tenant";

    export default stack("payments", () => {
      intent({ summary: "payments own workforce pool provider scim tenant" });

      resource(WorkforcePoolProviderScimTenant, "ci-runner", {
        location: "example",
        providerId: "example",
        scimTenantId: "example",
        workforcePoolId: "example",
        claimMapping: { "managed-by": "ubx" },
        description: "Managed by ubx.",
        displayName: "example-display-name",
        hardDelete: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import WorkforcePoolProviderScimTenant, WorkforcePoolProviderScimTenantConfig

    def describe():
        ubx.intent("payments own workforce pool provider scim tenant")

        ubx.resource(WorkforcePoolProviderScimTenant, "ci-runner", WorkforcePoolProviderScimTenantConfig(
            location="example",
            provider_id="example",
            scim_tenant_id="example",
            workforce_pool_id="example",
            claim_mapping={"managed-by": "ubx"},
            description="Managed by ubx.",
            display_name="example-display-name",
            hard_delete=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_workforce_pool_provider_scim_tenant, with
    location example, provider id example, scim tenant id example,
    workforce pool id example, claim mapping a set value, description
    Managed by ubx., display name example-display-name, hard delete
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="claim_mapping" type="map(string)">
  Maps BYOID claims to SCIM claims. This is a required field for new SCIM Tenants being created. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A user-specified description of the provider. Cannot exceed 256 characters. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  A user-specified display name for the scim tenant. Cannot exceed 32 characters. Optional.
</ResponseField>

<ResponseField name="hard_delete" type="bool">
  Deletes the SCIM tenant immediately. This operation cannot be undone. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the resource. Required.
</ResponseField>

<ResponseField name="provider_id" type="string" required>
  The ID of the provider. Required.
</ResponseField>

<ResponseField name="scim_tenant_id" type="string" required>
  The ID to use for the SCIM tenant, which becomes the final component of the resource name. This value must be 4-32 characters, and may contain the characters [a-z0-9-]. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workforce_pool_id" type="string" required>
  The ID of the workforce pool. Required.
</ResponseField>

## Output properties

<ResponseField name="base_uri" type="string">
  Represents the base URI as defined in [RFC 7644, Section 1.3](https://datatracker.ietf.org/doc/html/rfc7644#section-1.3). Clients must use this as the root address for managing resources under the tenant. Format: https://iamscim.googleapis.com/&#123;version&#125;/&#123;tenant_id&#125;/ Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the scim tenant. Format: 'locations/&#123;location&#125;/workforcePools/&#123;workforce_pool&#125;/providers/&#123;workforce_pool_provider&#125;/scimTenants/&#123;scim_tenant_id&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="purge_time" type="string">
  The timestamp that represents the time when the SCIM tenant is purged. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="service_agent" type="string">
  Service Agent created by SCIM Tenant API. SCIM tokens created under this tenant will be attached to this service agent. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the scim tenant. * ACTIVE: The scim tenant is active and may be used to validate authentication credentials. * DELETED: The scim tenant is soft-deleted. Soft-deleted scim tenants are permanently deleted after approximately 30 days. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
