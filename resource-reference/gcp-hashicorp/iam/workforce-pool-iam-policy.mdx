---
title: "google_iam_workforce_pool_iam_policy"
description: "Real, generated bindings for `google_iam_workforce_pool_iam_policy`."
---

`google_iam_workforce_pool_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iam "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iam"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workforce pool iam policy"})

    		ubx.Resource(iam.WorkforcePoolIamPolicy, "ci-runner", iam.WorkforcePoolIamPolicyConfig{
    			PolicyData:      "example",
    			WorkforcePoolId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkforcePoolIamPolicy } from "jsr:@ubx/sdk-google/google/iam/workforce_pool_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own workforce pool iam policy" });

      resource(WorkforcePoolIamPolicy, "ci-runner", {
        policyData: "example",
        workforcePoolId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iam import WorkforcePoolIamPolicy, WorkforcePoolIamPolicyConfig

    def describe():
        ubx.intent("payments own workforce pool iam policy")

        ubx.resource(WorkforcePoolIamPolicy, "ci-runner", WorkforcePoolIamPolicyConfig(
            policy_data="example",
            workforce_pool_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iam_workforce_pool_iam_policy, with policy data
    example, workforce pool id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="workforce_pool_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
