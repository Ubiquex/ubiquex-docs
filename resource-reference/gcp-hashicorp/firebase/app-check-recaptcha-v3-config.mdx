---
title: "google_firebase_app_check_recaptcha_v3_config"
description: "Real, generated bindings for `google_firebase_app_check_recaptcha_v3_config`."
---

`google_firebase_app_check_recaptcha_v3_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	firebase "github.com/ubiquex/ubx-sdk-google/sdk/go/google/firebase"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app check recaptcha v3 config"})

    		ubx.Resource(firebase.AppCheckRecaptchaV3Config, "ci-runner", firebase.AppCheckRecaptchaV3ConfigConfig{
    			AppId:      "example",
    			SiteSecret: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppCheckRecaptchaV3Config } from "jsr:@ubx/sdk-google/google/firebase/app_check_recaptcha_v3_config";

    export default stack("payments", () => {
      intent({ summary: "payments own app check recaptcha v3 config" });

      resource(AppCheckRecaptchaV3Config, "ci-runner", {
        appId: "example",
        siteSecret: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.firebase import AppCheckRecaptchaV3Config, AppCheckRecaptchaV3ConfigConfig

    def describe():
        ubx.intent("payments own app check recaptcha v3 config")

        ubx.resource(AppCheckRecaptchaV3Config, "ci-runner", AppCheckRecaptchaV3ConfigConfig(
            app_id="example",
            site_secret="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_firebase_app_check_recaptcha_v3_config, with app id
    example, site secret example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  The ID of an [Web App](https://firebase.google.com/docs/reference/firebase-management/rest/v1beta1/projects.webApps#WebApp.FIELDS.app_id). Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="site_secret" type="string" required>
  The site secret used to identify your service for reCAPTCHA v3 verification. For security reasons, this field will never be populated in any response. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_ttl" type="string">
  Specifies the duration for which App Check tokens exchanged from reCAPTCHA V3 artifacts will be valid. If unset, a default value of 1 hour is assumed. Must be between 30 minutes and 7 days, inclusive. A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s". Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The relative resource name of the reCAPTCHA V3 configuration object Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="site_secret_set" type="bool">
  Whether the siteSecret was previously set. Since we will never return the siteSecret field, this field is the only way to find out whether it was previously set. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_ttl" type="string">
  Specifies the duration for which App Check tokens exchanged from reCAPTCHA V3 artifacts will be valid. If unset, a default value of 1 hour is assumed. Must be between 30 minutes and 7 days, inclusive. A duration in seconds with up to nine fractional digits, ending with 's'. Example: "3.5s". Optional; if omitted, computed by GCP.
</ResponseField>
