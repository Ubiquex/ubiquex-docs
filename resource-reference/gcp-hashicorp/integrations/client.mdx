---
title: "google_integrations_client"
description: "Real, generated bindings for `google_integrations_client`."
---

`google_integrations_client` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	integrations "github.com/ubiquex/ubx-sdk-google/sdk/go/google/integrations"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own client"})

    		ubx.Resource(integrations.Client, "ci-runner", integrations.ClientConfig{
    			Location:                 "example",
    			CreateSampleIntegrations: true,
    			RunAsServiceAccount:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Client } from "jsr:@ubx/sdk-google/google/integrations/client";

    export default stack("payments", () => {
      intent({ summary: "payments own client" });

      resource(Client, "ci-runner", {
        location: "example",
        createSampleIntegrations: true,
        runAsServiceAccount: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.integrations import Client, ClientConfig

    def describe():
        ubx.intent("payments own client")

        ubx.resource(Client, "ci-runner", ClientConfig(
            location="example",
            create_sample_integrations=True,
            run_as_service_account="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_integrations_client, with location example, create
    sample integrations true, run as service account example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cloud_kms_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="key_version" type="string">
    </ResponseField>
    <ResponseField name="kms_location" type="string" required>
    </ResponseField>
    <ResponseField name="kms_project_id" type="string">
    </ResponseField>
    <ResponseField name="kms_ring" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_sample_integrations" type="bool">
  Indicates if sample integrations should be created along with provisioning. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Location in which client needs to be provisioned. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="run_as_service_account" type="string">
  User input run-as service account, if empty, will bring up a new default service account. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="cloud_kms_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="key_version" type="string">
    </ResponseField>
    <ResponseField name="kms_location" type="string" required>
    </ResponseField>
    <ResponseField name="kms_project_id" type="string">
    </ResponseField>
    <ResponseField name="kms_ring" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
