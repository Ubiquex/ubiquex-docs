---
title: "google_edgenetwork_interconnect_attachment"
description: "Real, generated bindings for `google_edgenetwork_interconnect_attachment`."
---

`google_edgenetwork_interconnect_attachment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	edgenetwork "github.com/ubiquex/ubx-sdk-google/sdk/go/google/edgenetwork"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own interconnect attachment"})

    		ubx.Resource(edgenetwork.InterconnectAttachment, "ci-runner", edgenetwork.InterconnectAttachmentConfig{
    			Interconnect:             "example",
    			InterconnectAttachmentId: "example",
    			Location:                 "example",
    			Network:                  "example",
    			VlanId:                   1,
    			Zone:                     "example",
    			Description:              "Managed by ubx.",
    			Labels:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { InterconnectAttachment } from "jsr:@ubx/sdk-google/google/edgenetwork/interconnect_attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own interconnect attachment" });

      resource(InterconnectAttachment, "ci-runner", {
        interconnect: "example",
        interconnectAttachmentId: "example",
        location: "example",
        network: "example",
        vlanId: 1,
        zone: "example",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.edgenetwork import InterconnectAttachment, InterconnectAttachmentConfig

    def describe():
        ubx.intent("payments own interconnect attachment")

        ubx.resource(InterconnectAttachment, "ci-runner", InterconnectAttachmentConfig(
            interconnect="example",
            interconnect_attachment_id="example",
            location="example",
            network="example",
            vlan_id=1,
            zone="example",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_edgenetwork_interconnect_attachment, with
    interconnect example, interconnect attachment id example,
    location example, network example, vlan id 1, zone example,
    description Managed by ubx., labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A free-text description of the resource. Max length 1024 characters. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="interconnect" type="string" required>
  The ID of the underlying interconnect that this attachment's traffic will traverse through. Required.
</ResponseField>

<ResponseField name="interconnect_attachment_id" type="string" required>
  A unique ID that identifies this interconnect attachment. Required.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels associated with this resource. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The Google Cloud region to which the target Distributed Cloud Edge zone belongs. Required.
</ResponseField>

<ResponseField name="mtu" type="number">
  IP (L3) MTU value of the virtual edge cloud. Default value is '1500'. Possible values are: '1500', '9000'. Optional.
</ResponseField>

<ResponseField name="network" type="string" required>
  The ID of the network to which this interconnect attachment belongs. Must be of the form: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/zones/&#123;&#123;zone&#125;&#125;/networks/&#123;&#123;network_id&#125;&#125;' Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vlan_id" type="number" required>
  VLAN ID provided by user. Must be site-wise unique. Required.
</ResponseField>

<ResponseField name="zone" type="string" required>
  The name of the target Distributed Cloud Edge zone. Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time when the resource was created. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: '2014-10-02T15:01:23Z' and '2014-10-02T15:01:23.045123456Z'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The canonical name of this resource, with format 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/zones/&#123;&#123;zone&#125;&#125;/interconnectAttachments/&#123;&#123;interconnect_attachment_id&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time when the resource was last updated. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: '2014-10-02T15:01:23Z' and '2014-10-02T15:01:23.045123456Z'. Computed by GCP after `ship`.
</ResponseField>
