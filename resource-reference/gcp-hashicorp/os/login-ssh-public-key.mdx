---
title: "google_os_login_ssh_public_key"
description: "Real, generated bindings for `google_os_login_ssh_public_key`."
---

`google_os_login_ssh_public_key` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	os "github.com/ubiquex/ubx-sdk-google/sdk/go/google/os"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own login ssh public key"})

    		ubx.Resource(os.LoginSshPublicKey, "ci-runner", os.LoginSshPublicKeyConfig{
    			Key:                "example",
    			User:               "example",
    			ExpirationTimeUsec: "example",
    			Project:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LoginSshPublicKey } from "jsr:@ubx/sdk-google/google/os/login_ssh_public_key";

    export default stack("payments", () => {
      intent({ summary: "payments own login ssh public key" });

      resource(LoginSshPublicKey, "ci-runner", {
        key: "example",
        user: "example",
        expirationTimeUsec: "example",
        project: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.os import LoginSshPublicKey, LoginSshPublicKeyConfig

    def describe():
        ubx.intent("payments own login ssh public key")

        ubx.resource(LoginSshPublicKey, "ci-runner", LoginSshPublicKeyConfig(
            key="example",
            user="example",
            expiration_time_usec="example",
            project="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_os_login_ssh_public_key, with key example, user
    example, expiration time usec example, project example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expiration_time_usec" type="string">
  An expiration time in microseconds since epoch. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key" type="string" required>
  Public key text in SSH format, defined by RFC4253 section 6.6. Required.
</ResponseField>

<ResponseField name="project" type="string">
  The project ID of the Google Cloud Platform project. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user" type="string" required>
  The user email. Required.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="fingerprint" type="string">
  The SHA-256 fingerprint of the SSH public key. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
