---
title: "google_billing_account_iam_policy"
description: "Real, generated bindings for `google_billing_account_iam_policy`."
---

`google_billing_account_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	billing "github.com/ubiquex/ubx-sdk-google/sdk/go/google/billing"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account iam policy"})

    		ubx.Resource(billing.AccountIamPolicy, "ci-runner", billing.AccountIamPolicyConfig{
    			BillingAccountId: "example",
    			PolicyData:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountIamPolicy } from "jsr:@ubx/sdk-google/google/billing/account_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own account iam policy" });

      resource(AccountIamPolicy, "ci-runner", {
        billingAccountId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.billing import AccountIamPolicy, AccountIamPolicyConfig

    def describe():
        ubx.intent("payments own account iam policy")

        ubx.resource(AccountIamPolicy, "ci-runner", AccountIamPolicyConfig(
            billing_account_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_billing_account_iam_policy, with billing account id
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="billing_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
