---
title: "google_kms_crypto_key_version"
description: "Real, generated bindings for `google_kms_crypto_key_version`."
---

`google_kms_crypto_key_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	kms "github.com/ubiquex/ubx-sdk-google/sdk/go/google/kms"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own crypto key version"})

    		ubx.Resource(kms.CryptoKeyVersion, "ci-runner", kms.CryptoKeyVersionConfig{
    			CryptoKey: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CryptoKeyVersion } from "jsr:@ubx/sdk-google/google/kms/crypto_key_version";

    export default stack("payments", () => {
      intent({ summary: "payments own crypto key version" });

      resource(CryptoKeyVersion, "ci-runner", {
        cryptoKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import CryptoKeyVersion, CryptoKeyVersionConfig

    def describe():
        ubx.intent("payments own crypto key version")

        ubx.resource(CryptoKeyVersion, "ci-runner", CryptoKeyVersionConfig(
            crypto_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_crypto_key_version, with crypto key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="crypto_key" type="string" required>
  The name of the cryptoKey associated with the CryptoKeyVersions. Format: ''projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/keyRings/&#123;&#123;keyring&#125;&#125;/cryptoKeys/&#123;&#123;cryptoKey&#125;&#125;'' Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="external_protection_level_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ekm_connection_key_path" type="string">
    </ResponseField>
    <ResponseField name="external_key_uri" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the CryptoKeyVersion. Note: you can only specify this field to manually 'ENABLE' or 'DISABLE' the CryptoKeyVersion, otherwise the value of this field is always retrieved automatically. Possible values: ["PENDING_GENERATION", "ENABLED", "DISABLED", "DESTROYED", "DESTROY_SCHEDULED", "PENDING_IMPORT", "IMPORT_FAILED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="algorithm" type="string">
  The CryptoKeyVersionAlgorithm that this CryptoKeyVersion supports. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="attestation" type="list(object)">
  Statement that was generated and signed by the HSM at key creation time. Use this statement to verify attributes of the key as stored on the HSM, independently of Google. Only provided for key versions with protectionLevel HSM. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="cert_chains" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="cavium_certs" type="list(string)">
        </ResponseField>
        <ResponseField name="google_card_certs" type="list(string)">
        </ResponseField>
        <ResponseField name="google_partition_certs" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="content" type="string">
    </ResponseField>
    <ResponseField name="external_protection_level_options" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="ekm_connection_key_path" type="string">
        </ResponseField>
        <ResponseField name="external_key_uri" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="format" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="external_protection_level_options" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ekm_connection_key_path" type="string">
    </ResponseField>
    <ResponseField name="external_key_uri" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="generate_time" type="string">
  The time this CryptoKeyVersion key material was generated Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name for this CryptoKeyVersion. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="protection_level" type="string">
  The ProtectionLevel describing how crypto operations are performed with this CryptoKeyVersion. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the CryptoKeyVersion. Note: you can only specify this field to manually 'ENABLE' or 'DISABLE' the CryptoKeyVersion, otherwise the value of this field is always retrieved automatically. Possible values: ["PENDING_GENERATION", "ENABLED", "DISABLED", "DESTROYED", "DESTROY_SCHEDULED", "PENDING_IMPORT", "IMPORT_FAILED"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
