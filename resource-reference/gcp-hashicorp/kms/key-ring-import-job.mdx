---
title: "google_kms_key_ring_import_job"
description: "Real, generated bindings for `google_kms_key_ring_import_job`."
---

`google_kms_key_ring_import_job` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	kms "github.com/ubiquex/ubx-sdk-google/sdk/go/google/kms"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key ring import job"})

    		ubx.Resource(kms.KeyRingImportJob, "ci-runner", kms.KeyRingImportJobConfig{
    			ImportJobId:     "example",
    			ImportMethod:    "example",
    			KeyRing:         "example",
    			ProtectionLevel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyRingImportJob } from "jsr:@ubx/sdk-google/google/kms/key_ring_import_job";

    export default stack("payments", () => {
      intent({ summary: "payments own key ring import job" });

      resource(KeyRingImportJob, "ci-runner", {
        importJobId: "example",
        importMethod: "example",
        keyRing: "example",
        protectionLevel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.kms import KeyRingImportJob, KeyRingImportJobConfig

    def describe():
        ubx.intent("payments own key ring import job")

        ubx.resource(KeyRingImportJob, "ci-runner", KeyRingImportJobConfig(
            import_job_id="example",
            import_method="example",
            key_ring="example",
            protection_level="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_kms_key_ring_import_job, with import job id
    example, import method example, key ring example, protection
    level example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="import_job_id" type="string" required>
  It must be unique within a KeyRing and match the regular expression [a-zA-Z0-9_-]&#123;1,63&#125; Required.
</ResponseField>

<ResponseField name="import_method" type="string" required>
  The wrapping method to be used for incoming key material. Possible values: ["RSA_OAEP_3072_SHA1_AES_256", "RSA_OAEP_4096_SHA1_AES_256", "RSA_OAEP_3072_SHA256_AES_256", "RSA_OAEP_4096_SHA256_AES_256", "RSA_OAEP_3072_SHA256", "RSA_OAEP_4096_SHA256"] Required.
</ResponseField>

<ResponseField name="key_ring" type="string" required>
  The KeyRing that this import job belongs to. Format: ''projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/keyRings/&#123;&#123;keyRing&#125;&#125;''. Required.
</ResponseField>

<ResponseField name="protection_level" type="string" required>
  The protection level of the ImportJob. This must match the protectionLevel of the versionTemplate on the CryptoKey you attempt to import into. Possible values: ["SOFTWARE", "HSM", "EXTERNAL"] Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="attestation" type="list(object)">
  Statement that was generated and signed by the key creator (for example, an HSM) at key creation time. Use this statement to verify attributes of the key as stored on the HSM, independently of Google. Only present if the chosen ImportMethod is one with a protection level of HSM. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="content" type="string">
    </ResponseField>
    <ResponseField name="format" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expire_time" type="string">
  The time at which this resource is scheduled for expiration and can no longer be used. This is in RFC3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name for this ImportJob in the format projects/*/locations/*/keyRings/*/importJobs/*. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="public_key" type="list(object)">
  The public key with which to wrap key material prior to import. Only returned if state is 'ACTIVE'. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="pem" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the ImportJob, indicating if it can be used. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
