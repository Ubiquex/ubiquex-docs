---
title: "google_backup_dr_backup_plan_association"
description: "Real, generated bindings for `google_backup_dr_backup_plan_association`."
---

`google_backup_dr_backup_plan_association` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	backup "github.com/ubiquex/ubx-sdk-google/sdk/go/google/backup"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own dr backup plan association"})

    		ubx.Resource(backup.DrBackupPlanAssociation, "ci-runner", backup.DrBackupPlanAssociationConfig{
    			BackupPlan:              "example",
    			BackupPlanAssociationId: "example",
    			Location:                "example",
    			Resource:                "example",
    			ResourceType:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DrBackupPlanAssociation } from "jsr:@ubx/sdk-google/google/backup/dr_backup_plan_association";

    export default stack("payments", () => {
      intent({ summary: "payments own dr backup plan association" });

      resource(DrBackupPlanAssociation, "ci-runner", {
        backupPlan: "example",
        backupPlanAssociationId: "example",
        location: "example",
        resource: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.backup import DrBackupPlanAssociation, DrBackupPlanAssociationConfig

    def describe():
        ubx.intent("payments own dr backup plan association")

        ubx.resource(DrBackupPlanAssociation, "ci-runner", DrBackupPlanAssociationConfig(
            backup_plan="example",
            backup_plan_association_id="example",
            location="example",
            resource="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_backup_dr_backup_plan_association, with backup plan
    example, backup plan association id example, location example,
    resource example, resource type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_plan" type="string" required>
  The BP with which resource needs to be created Note: - A Backup Plan configured for 'compute.googleapis.com/Instance', can only protect instance type resources. - A Backup Plan configured for 'compute.googleapis.com/Disk' can be used to protect both standard Disks and Regional Disks resources. - A Backup Plan configured for 'file.googleapis.com/Instance' can only protect Filestore instances. - A Backup Plan configured for 'sqladmin.googleapis.com/Instance' can only protect Cloud SQL instances. Required.
</ResponseField>

<ResponseField name="backup_plan_association_id" type="string" required>
  The id of backupplan association Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the backupplan association Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource" type="string" required>
  The resource for which BPA needs to be created Required.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The resource type of workload on which backupplan is applied. Examples include, "compute.googleapis.com/Instance", "compute.googleapis.com/Disk", "compute.googleapis.com/RegionDisk", and "file.googleapis.com/Instance" Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time when the instance was created Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="data_source" type="string">
  Resource name of data source which will be used as storage location for backups taken Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of backup plan association resource created Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rules_config_info" type="list(object)">
  Message for rules config info Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="last_backup_error" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="code" type="number">
        </ResponseField>
        <ResponseField name="message" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="last_backup_state" type="string">
    </ResponseField>
    <ResponseField name="last_successful_backup_consistency_time" type="string">
    </ResponseField>
    <ResponseField name="rule_id" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time when the instance was updated. Computed by GCP after `ship`.
</ResponseField>
