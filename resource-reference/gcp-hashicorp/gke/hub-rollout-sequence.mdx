---
title: "google_gke_hub_rollout_sequence"
description: "Real, generated bindings for `google_gke_hub_rollout_sequence`."
---

`google_gke_hub_rollout_sequence` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	gke "github.com/ubiquex/ubx-sdk-google/sdk/go/google/gke"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub rollout sequence"})

    		ubx.Resource(gke.HubRolloutSequence, "ci-runner", gke.HubRolloutSequenceConfig{
    			RolloutSequenceId:      "example",
    			DisplayName:            "example-display-name",
    			Labels:                 map[string]string{"managed-by": "ubx"},
    			MinControlPlaneVersion: "example",
    			MinNodeVersion:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubRolloutSequence } from "jsr:@ubx/sdk-google/google/gke/hub_rollout_sequence";

    export default stack("payments", () => {
      intent({ summary: "payments own hub rollout sequence" });

      resource(HubRolloutSequence, "ci-runner", {
        rolloutSequenceId: "example",
        displayName: "example-display-name",
        labels: { "managed-by": "ubx" },
        minControlPlaneVersion: "example",
        minNodeVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.gke import HubRolloutSequence, HubRolloutSequenceConfig

    def describe():
        ubx.intent("payments own hub rollout sequence")

        ubx.resource(HubRolloutSequence, "ci-runner", HubRolloutSequenceConfig(
            rollout_sequence_id="example",
            display_name="example-display-name",
            labels={"managed-by": "ubx"},
            min_control_plane_version="example",
            min_node_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_gke_hub_rollout_sequence, with rollout sequence id
    example, display name example-display-name, labels a set value,
    min control plane version example, min node version example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_upgrade_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="rollout_creation_scope" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="upgrade_types" type="set(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="display_name" type="string">
  Human readable display name of the Rollout Sequence. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ignored_clusters_selector" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="label_selector" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels for this Rollout Sequence. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="min_control_plane_version" type="string">
  Minimum control plane version that the clusters in the sequence should be upgraded to. Setting this field will cause the creation of a rollout to the specified version. Any rollout of the same type already running on the first stage of the sequence will be cancelled to allow for the creation of the new rollout. Should be a valid [semantic version](https://semver.org/). Version aliases are supported, as described in the [cluster version docs](https://docs.cloud.google.com/kubernetes-engine/versioning#specifying_cluster_version). Note that the 'latest' and '-' aliases are not supported for this field. Supported formats: '1.X', '1.X.Y', '1.X.Y-gke.N'. Optional.
</ResponseField>

<ResponseField name="min_node_version" type="string">
  Minimum node version that the clusters in the sequence should be upgraded to. Setting this field will cause the creation of a rollout to the specified version. Any rollout of the same type already running on the first stage of the sequence will be cancelled to allow for the creation of the new rollout. Should be a valid [semantic version](https://semver.org/). Version aliases are supported, as described in the [cluster version docs](https://docs.cloud.google.com/kubernetes-engine/versioning#specifying_cluster_version). Note that the 'latest' and '-' aliases are not supported for this field. Supported formats: '1.X', '1.X.Y', '1.X.Y-gke.N'. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rollout_sequence_id" type="string" required>
  The user-provided identifier of the RolloutSequence. Required.
</ResponseField>

<ResponseField name="stages" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cluster_selector" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="label_selector" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fleet_projects" type="list(string)" required>
    </ResponseField>
    <ResponseField name="soak_duration" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="auto_upgrade_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="rollout_creation_scope" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="upgrade_types" type="set(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  The timestamp at which the Rollout Sequence was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="delete_time" type="string">
  The timestamp at the Rollout Sequence was deleted. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  etag of the Rollout Sequence. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ignored_clusters_selector" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="label_selector" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  The full resource name of the RolloutSequence. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="operational_state" type="list(object)">
  The operational state of the rollout sequence. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="stages" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="cluster_selector" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="label_selector" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="fleet_projects" type="list(string)" required>
    </ResponseField>
    <ResponseField name="soak_duration" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_control_plane_version" type="string">
  The current target control plane version. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="target_node_version" type="string">
  The current target node version. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  Google-generated UUID for this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp at which the Rollout Sequence was last updated. Computed by GCP after `ship`.
</ResponseField>
