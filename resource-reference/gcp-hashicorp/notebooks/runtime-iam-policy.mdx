---
title: "google_notebooks_runtime_iam_policy"
description: "Real, generated bindings for `google_notebooks_runtime_iam_policy`."
---

`google_notebooks_runtime_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	notebooks "github.com/ubiquex/ubx-sdk-google/sdk/go/google/notebooks"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own runtime iam policy"})

    		ubx.Resource(notebooks.RuntimeIamPolicy, "ci-runner", notebooks.RuntimeIamPolicyConfig{
    			PolicyData:  "example",
    			RuntimeName: "example-runtime-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuntimeIamPolicy } from "jsr:@ubx/sdk-google/google/notebooks/runtime_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own runtime iam policy" });

      resource(RuntimeIamPolicy, "ci-runner", {
        policyData: "example",
        runtimeName: "example-runtime-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.notebooks import RuntimeIamPolicy, RuntimeIamPolicyConfig

    def describe():
        ubx.intent("payments own runtime iam policy")

        ubx.resource(RuntimeIamPolicy, "ci-runner", RuntimeIamPolicyConfig(
            policy_data="example",
            runtime_name="example-runtime-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_notebooks_runtime_iam_policy, with policy data
    example, runtime name example-runtime-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="runtime_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
