---
title: "google_healthcare_fhir_store_iam_policy"
description: "Real, generated bindings for `google_healthcare_fhir_store_iam_policy`."
---

`google_healthcare_fhir_store_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	healthcare "github.com/ubiquex/ubx-sdk-google/sdk/go/google/healthcare"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fhir store iam policy"})

    		ubx.Resource(healthcare.FhirStoreIamPolicy, "ci-runner", healthcare.FhirStoreIamPolicyConfig{
    			FhirStoreId: "example",
    			PolicyData:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FhirStoreIamPolicy } from "jsr:@ubx/sdk-google/google/healthcare/fhir_store_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own fhir store iam policy" });

      resource(FhirStoreIamPolicy, "ci-runner", {
        fhirStoreId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.healthcare import FhirStoreIamPolicy, FhirStoreIamPolicyConfig

    def describe():
        ubx.intent("payments own fhir store iam policy")

        ubx.resource(FhirStoreIamPolicy, "ci-runner", FhirStoreIamPolicyConfig(
            fhir_store_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_healthcare_fhir_store_iam_policy, with fhir store
    id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="fhir_store_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
