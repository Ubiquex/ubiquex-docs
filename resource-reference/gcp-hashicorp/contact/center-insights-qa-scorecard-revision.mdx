---
title: "google_contact_center_insights_qa_scorecard_revision"
description: "Real, generated bindings for `google_contact_center_insights_qa_scorecard_revision`."
---

`google_contact_center_insights_qa_scorecard_revision` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	contact "github.com/ubiquex/ubx-sdk-google/sdk/go/google/contact"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own center insights qa scorecard revision"})

    		ubx.Resource(contact.CenterInsightsQaScorecardRevision, "ci-runner", contact.CenterInsightsQaScorecardRevisionConfig{
    			Location:    "example",
    			QaScorecard: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CenterInsightsQaScorecardRevision } from "jsr:@ubx/sdk-google/google/contact/center_insights_qa_scorecard_revision";

    export default stack("payments", () => {
      intent({ summary: "payments own center insights qa scorecard revision" });

      resource(CenterInsightsQaScorecardRevision, "ci-runner", {
        location: "example",
        qaScorecard: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.contact import CenterInsightsQaScorecardRevision, CenterInsightsQaScorecardRevisionConfig

    def describe():
        ubx.intent("payments own center insights qa scorecard revision")

        ubx.resource(CenterInsightsQaScorecardRevision, "ci-runner", CenterInsightsQaScorecardRevisionConfig(
            location="example",
            qa_scorecard="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_contact_center_insights_qa_scorecard_revision, with
    location example, qa scorecard example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="qa_scorecard" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="qa_scorecard_revision_id" type="string">
  A unique ID for the new QaScorecardRevision. This ID will become the final component of the QaScorecardRevision's resource name. If no ID is specified this resource will get the latest revision on the given scorecard. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="alternate_ids" type="list(string)">
  Alternative IDs for this revision of the scorecard, e.g., 'latest'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  The timestamp that the revision was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The name of the scorecard revision. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/qaScorecards/&#123;qa_scorecard&#125;/revisions/&#123;revision&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="qa_scorecard_revision_id" type="string">
  A unique ID for the new QaScorecardRevision. This ID will become the final component of the QaScorecardRevision's resource name. If no ID is specified this resource will get the latest revision on the given scorecard. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="snapshot" type="list(object)">
  A QaScorecard represents a collection of questions to be scored during analysis. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="create_time" type="string">
    </ResponseField>
    <ResponseField name="description" type="string">
    </ResponseField>
    <ResponseField name="display_name" type="string">
    </ResponseField>
    <ResponseField name="is_default" type="bool">
    </ResponseField>
    <ResponseField name="name" type="string">
    </ResponseField>
    <ResponseField name="source" type="string">
    </ResponseField>
    <ResponseField name="update_time" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  State of the scorecard revision, indicating whether it's ready to be used in analysis. Possible values: EDITABLE TRAINING TRAINING_FAILED READY DELETING TRAINING_CANCELLED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
