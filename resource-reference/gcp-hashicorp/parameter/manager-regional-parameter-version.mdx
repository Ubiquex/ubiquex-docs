---
title: "google_parameter_manager_regional_parameter_version"
description: "Real, generated bindings for `google_parameter_manager_regional_parameter_version`."
---

`google_parameter_manager_regional_parameter_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	parameter "github.com/ubiquex/ubx-sdk-google/sdk/go/google/parameter"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager regional parameter version"})

    		ubx.Resource(parameter.ManagerRegionalParameterVersion, "ci-runner", parameter.ManagerRegionalParameterVersionConfig{
    			Parameter:          "example",
    			ParameterData:      "example",
    			ParameterVersionId: "example",
    			Disabled:           true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerRegionalParameterVersion } from "jsr:@ubx/sdk-google/google/parameter/manager_regional_parameter_version";

    export default stack("payments", () => {
      intent({ summary: "payments own manager regional parameter version" });

      resource(ManagerRegionalParameterVersion, "ci-runner", {
        parameter: "example",
        parameterData: "example",
        parameterVersionId: "example",
        disabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.parameter import ManagerRegionalParameterVersion, ManagerRegionalParameterVersionConfig

    def describe():
        ubx.intent("payments own manager regional parameter version")

        ubx.resource(ManagerRegionalParameterVersion, "ci-runner", ManagerRegionalParameterVersionConfig(
            parameter="example",
            parameter_data="example",
            parameter_version_id="example",
            disabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_parameter_manager_regional_parameter_version, with
    parameter example, parameter data example, parameter version id
    example, disabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="disabled" type="bool">
  The current state of Regional Parameter Version. This field is only applicable for updating Regional Parameter Version. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parameter" type="string" required>
  Parameter Manager Regional Parameter resource. Required.
</ResponseField>

<ResponseField name="parameter_data" type="string" required>
  The Regional Parameter data. Required.
</ResponseField>

<ResponseField name="parameter_version_id" type="string" required>
  Version ID of the Regional Parameter Version Resource. This must be unique within the Regional Parameter. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which the Regional Parameter Version was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_version" type="string">
  The resource name of the Cloud KMS CryptoKeyVersion used to decrypt regional parameter version payload. Format 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/keyRings/&#123;&#123;key_ring&#125;&#125;/cryptoKeys/&#123;&#123;crypto_key&#125;&#125;/cryptoKeyVersions/&#123;&#123;crypto_key_version&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="location" type="string">
  Location of Parameter Manager Regional parameter resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the Regional Parameter Version. Format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/parameters/&#123;&#123;parameter_id&#125;&#125;/versions/&#123;&#123;parameter_version_id&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time at which the Regional Parameter Version was updated. Computed by GCP after `ship`.
</ResponseField>
