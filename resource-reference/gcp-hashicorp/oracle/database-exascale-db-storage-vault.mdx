---
title: "google_oracle_database_exascale_db_storage_vault"
description: "Real, generated bindings for `google_oracle_database_exascale_db_storage_vault`."
---

`google_oracle_database_exascale_db_storage_vault` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	oracle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/oracle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own database exascale db storage vault"})

    		ubx.Resource(oracle.DatabaseExascaleDbStorageVault, "ci-runner", oracle.DatabaseExascaleDbStorageVaultConfig{
    			DisplayName:              "example-display-name",
    			ExascaleDbStorageVaultId: "example",
    			Location:                 "example",
    			DeletionProtection:       true,
    			ExadataInfrastructure:    "example",
    			Labels:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DatabaseExascaleDbStorageVault } from "jsr:@ubx/sdk-google/google/oracle/database_exascale_db_storage_vault";

    export default stack("payments", () => {
      intent({ summary: "payments own database exascale db storage vault" });

      resource(DatabaseExascaleDbStorageVault, "ci-runner", {
        displayName: "example-display-name",
        exascaleDbStorageVaultId: "example",
        location: "example",
        deletionProtection: true,
        exadataInfrastructure: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.oracle import DatabaseExascaleDbStorageVault, DatabaseExascaleDbStorageVaultConfig

    def describe():
        ubx.intent("payments own database exascale db storage vault")

        ubx.resource(DatabaseExascaleDbStorageVault, "ci-runner", DatabaseExascaleDbStorageVaultConfig(
            display_name="example-display-name",
            exascale_db_storage_vault_id="example",
            location="example",
            deletion_protection=True,
            exadata_infrastructure="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_oracle_database_exascale_db_storage_vault, with
    display name example-display-name, exascale db storage vault id
    example, location example, deletion protection true, exadata
    infrastructure example, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Whether or not to allow Terraform to destroy the instance. Unless this field is set to false in Terraform state, a terraform destroy or terraform apply that would delete the instance will fail. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The display name for the ExascaleDbStorageVault. The name does not have to be unique within your project. The name must be 1-255 characters long and can only contain alphanumeric characters. Required.
</ResponseField>

<ResponseField name="exadata_infrastructure" type="string">
  The Exadata Infrastructure resource on which ExascaleDbStorageVault resource is created. In the format: projects/&#123;project&#125;/locations/&#123;region&#125;/cloudExadataInfrastructures/&#123;cloud_extradata_infrastructure&#125; Optional.
</ResponseField>

<ResponseField name="exascale_db_storage_vault_id" type="string" required>
  The ID of the ExascaleDbStorageVault to create. This value is restricted to (^[a-z]([a-z0-9-]&#123;0,61&#125;[a-z0-9])?$) and must be a maximum of 63 characters in length. The value must start with a letter and end with a letter or a number. Required.
</ResponseField>

<ResponseField name="gcp_oracle_zone" type="string">
  The GCP Oracle zone where Oracle ExascaleDbStorageVault is hosted. Example: us-east4-b-r2. If not specified, the system will pick a zone based on availability. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The labels or tags associated with the ExascaleDbStorageVault. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="properties" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="additional_flash_cache_percent" type="number">
    </ResponseField>
    <ResponseField name="attached_shape_attributes" type="list(string)">
    </ResponseField>
    <ResponseField name="available_shape_attributes" type="list(string)">
    </ResponseField>
    <ResponseField name="exascale_db_storage_details" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="available_size_gbs" type="number">
        </ResponseField>
        <ResponseField name="total_size_gbs" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oci_uri" type="string">
    </ResponseField>
    <ResponseField name="ocid" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
    <ResponseField name="time_zone" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="id" type="string">
        </ResponseField>
        <ResponseField name="version" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vm_cluster_count" type="number">
    </ResponseField>
    <ResponseField name="vm_cluster_ids" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The date and time when the ExascaleDbStorageVault was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="entitlement_id" type="string">
  The ID of the subscription entitlement associated with the ExascaleDbStorageVault. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="gcp_oracle_zone" type="string">
  The GCP Oracle zone where Oracle ExascaleDbStorageVault is hosted. Example: us-east4-b-r2. If not specified, the system will pick a zone based on availability. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the ExascaleDbStorageVault. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/exascaleDbStorageVaults/&#123;exascale_db_storage_vault&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="properties" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="additional_flash_cache_percent" type="number">
    </ResponseField>
    <ResponseField name="attached_shape_attributes" type="list(string)">
    </ResponseField>
    <ResponseField name="available_shape_attributes" type="list(string)">
    </ResponseField>
    <ResponseField name="exascale_db_storage_details" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="available_size_gbs" type="number">
        </ResponseField>
        <ResponseField name="total_size_gbs" type="number" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="oci_uri" type="string">
    </ResponseField>
    <ResponseField name="ocid" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
    <ResponseField name="time_zone" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="id" type="string">
        </ResponseField>
        <ResponseField name="version" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="vm_cluster_count" type="number">
    </ResponseField>
    <ResponseField name="vm_cluster_ids" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
