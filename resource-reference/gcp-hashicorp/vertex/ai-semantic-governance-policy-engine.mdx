---
title: "google_vertex_ai_semantic_governance_policy_engine"
description: "Real, generated bindings for `google_vertex_ai_semantic_governance_policy_engine`."
---

`google_vertex_ai_semantic_governance_policy_engine` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vertex "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vertex"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ai semantic governance policy engine"})

    		ubx.Resource(vertex.AiSemanticGovernancePolicyEngine, "ci-runner", vertex.AiSemanticGovernancePolicyEngineConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiSemanticGovernancePolicyEngine } from "jsr:@ubx/sdk-google/google/vertex/ai_semantic_governance_policy_engine";

    export default stack("payments", () => {
      intent({ summary: "payments own ai semantic governance policy engine" });

      resource(AiSemanticGovernancePolicyEngine, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vertex import AiSemanticGovernancePolicyEngine, AiSemanticGovernancePolicyEngineConfig

    def describe():
        ubx.intent("payments own ai semantic governance policy engine")

        ubx.resource(AiSemanticGovernancePolicyEngine, "ci-runner", AiSemanticGovernancePolicyEngineConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vertex_ai_semantic_governance_policy_engine, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region of the SemanticGovernancePolicyEngine, e.g. 'us-central1'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time the SemanticGovernancePolicyEngine was created, in RFC3339 UTC "Zulu" format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ip_address" type="string">
  The IP address allocated for the SGPE's managed PSC endpoint. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the SemanticGovernancePolicyEngine, in the form 'projects/&#123;project&#125;/locations/&#123;region&#125;/semanticGovernancePolicyEngine'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="psc_forwarding_rule" type="string">
  The Private Service Connect forwarding rule URI for the SGPE's managed endpoint. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="psc_service_attachment" type="string">
  The Private Service Connect service attachment URI for the SGPE's managed endpoint. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  The region of the SemanticGovernancePolicyEngine, e.g. 'us-central1'. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the SemanticGovernancePolicyEngine. One of: STATE_UNSPECIFIED, PROVISIONING, ACTIVE, FAILED, DEPROVISIONING, INACTIVE. 'FAILED' indicates provisioning did not succeed; recover by destroying the resource (deprovision) or re-applying (re-provision). Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The time the SemanticGovernancePolicyEngine was last updated, in RFC3339 UTC "Zulu" format. Computed by GCP after `ship`.
</ResponseField>
