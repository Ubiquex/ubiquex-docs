---
title: "google_vertex_ai_deployment_resource_pool"
description: "Real, generated bindings for `google_vertex_ai_deployment_resource_pool`."
---

`google_vertex_ai_deployment_resource_pool` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vertex "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vertex"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ai deployment resource pool"})

    		ubx.Resource(vertex.AiDeploymentResourcePool, "ci-runner", vertex.AiDeploymentResourcePoolConfig{
    			Name:   "ci-runner",
    			Region: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiDeploymentResourcePool } from "jsr:@ubx/sdk-google/google/vertex/ai_deployment_resource_pool";

    export default stack("payments", () => {
      intent({ summary: "payments own ai deployment resource pool" });

      resource(AiDeploymentResourcePool, "ci-runner", {
        name: "ci-runner",
        region: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vertex import AiDeploymentResourcePool, AiDeploymentResourcePoolConfig

    def describe():
        ubx.intent("payments own ai deployment resource pool")

        ubx.resource(AiDeploymentResourcePool, "ci-runner", AiDeploymentResourcePoolConfig(
            name="ci-runner",
            region="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vertex_ai_deployment_resource_pool called
    "ci-runner", with region example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="dedicated_resources" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="autoscaling_metric_specs" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="metric_name" type="string" required>
        </ResponseField>
        <ResponseField name="target" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="machine_spec" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
        </ResponseField>
        <ResponseField name="machine_type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_replica_count" type="number">
    </ResponseField>
    <ResponseField name="min_replica_count" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource name of deployment resource pool. The maximum length is 63 characters, and valid characters are '/^[a-z]([a-z0-9-]&#123;0,61&#125;[a-z0-9])?$/'. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region of deployment resource pool. eg us-central1 Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="dedicated_resources" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="autoscaling_metric_specs" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="metric_name" type="string" required>
        </ResponseField>
        <ResponseField name="target" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="machine_spec" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="accelerator_count" type="number">
        </ResponseField>
        <ResponseField name="accelerator_type" type="string">
        </ResponseField>
        <ResponseField name="machine_type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_replica_count" type="number">
    </ResponseField>
    <ResponseField name="min_replica_count" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
