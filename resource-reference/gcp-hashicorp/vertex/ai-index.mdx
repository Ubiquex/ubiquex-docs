---
title: "google_vertex_ai_index"
description: "Real, generated bindings for `google_vertex_ai_index`."
---

`google_vertex_ai_index` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	vertex "github.com/ubiquex/ubx-sdk-google/sdk/go/google/vertex"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ai index"})

    		ubx.Resource(vertex.AiIndex, "ci-runner", vertex.AiIndexConfig{
    			DisplayName:       "example-display-name",
    			Description:       "Managed by ubx.",
    			IndexUpdateMethod: "example",
    			Labels:            map[string]string{"managed-by": "ubx"},
    			Region:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AiIndex } from "jsr:@ubx/sdk-google/google/vertex/ai_index";

    export default stack("payments", () => {
      intent({ summary: "payments own ai index" });

      resource(AiIndex, "ci-runner", {
        displayName: "example-display-name",
        description: "Managed by ubx.",
        indexUpdateMethod: "example",
        labels: { "managed-by": "ubx" },
        region: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.vertex import AiIndex, AiIndexConfig

    def describe():
        ubx.intent("payments own ai index")

        ubx.resource(AiIndex, "ci-runner", AiIndexConfig(
            display_name="example-display-name",
            description="Managed by ubx.",
            index_update_method="example",
            labels={"managed-by": "ubx"},
            region="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_vertex_ai_index, with display name
    example-display-name, description Managed by ubx., index update
    method example, labels a set value, region example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the Index. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The display name of the Index. The name can be up to 128 characters long and can consist of any UTF-8 characters. Required.
</ResponseField>

<ResponseField name="encryption_spec" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="index_update_method" type="string">
  The update method to use with this Index. The value must be the followings. If not set, BATCH_UPDATE will be used by default. * BATCH_UPDATE: user can call indexes.patch with files on Cloud Storage of datapoints to update. * STREAM_UPDATE: user can call indexes.upsertDatapoints/DeleteDatapoints to update the Index and the updates will be applied in corresponding DeployedIndexes in nearly real-time. Optional.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The labels with user-defined metadata to organize your Indexes. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="algorithm_config" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="brute_force_config" type="list(object)">
            </ResponseField>
            <ResponseField name="tree_ah_config" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="leaf_node_embedding_count" type="number">
                </ResponseField>
                <ResponseField name="leaf_nodes_to_search_percent" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="approximate_neighbors_count" type="number">
        </ResponseField>
        <ResponseField name="dimensions" type="number" required>
        </ResponseField>
        <ResponseField name="distance_measure_type" type="string">
        </ResponseField>
        <ResponseField name="feature_norm_type" type="string">
        </ResponseField>
        <ResponseField name="shard_size" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="contents_delta_uri" type="string">
    </ResponseField>
    <ResponseField name="is_complete_overwrite" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="region" type="string">
  The region of the index. eg us-central1 Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The timestamp of when the Index was created in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deployed_indexes" type="list(object)">
  The pointers to DeployedIndexes created from this Index. An Index can be only deleted if all its DeployedIndexes had been undeployed first. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="deployed_index_id" type="string">
    </ResponseField>
    <ResponseField name="index_endpoint" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="encryption_spec" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="kms_key_name" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="etag" type="string">
  Used to perform consistent read-modify-write updates. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="index_stats" type="list(object)">
  Stats of the index resource. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="shards_count" type="number">
    </ResponseField>
    <ResponseField name="vectors_count" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="algorithm_config" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="brute_force_config" type="list(object)">
            </ResponseField>
            <ResponseField name="tree_ah_config" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="leaf_node_embedding_count" type="number">
                </ResponseField>
                <ResponseField name="leaf_nodes_to_search_percent" type="number">
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="approximate_neighbors_count" type="number">
        </ResponseField>
        <ResponseField name="dimensions" type="number" required>
        </ResponseField>
        <ResponseField name="distance_measure_type" type="string">
        </ResponseField>
        <ResponseField name="feature_norm_type" type="string">
        </ResponseField>
        <ResponseField name="shard_size" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="contents_delta_uri" type="string">
    </ResponseField>
    <ResponseField name="is_complete_overwrite" type="bool">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata_schema_uri" type="string">
  Points to a YAML file stored on Google Cloud Storage describing additional information about the Index, that is specific to it. Unset if the Index does not have any additional information. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the Index. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp of when the Index was last updated in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Computed by GCP after `ship`.
</ResponseField>
