---
title: "google_beyondcorp_security_gateway_application_iam_policy"
description: "Real, generated bindings for `google_beyondcorp_security_gateway_application_iam_policy`."
---

`google_beyondcorp_security_gateway_application_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	beyondcorp "github.com/ubiquex/ubx-sdk-google/sdk/go/google/beyondcorp"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security gateway application iam policy"})

    		ubx.Resource(beyondcorp.SecurityGatewayApplicationIamPolicy, "ci-runner", beyondcorp.SecurityGatewayApplicationIamPolicyConfig{
    			ApplicationId:     "example",
    			PolicyData:        "example",
    			SecurityGatewayId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityGatewayApplicationIamPolicy } from "jsr:@ubx/sdk-google/google/beyondcorp/security_gateway_application_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own security gateway application iam policy" });

      resource(SecurityGatewayApplicationIamPolicy, "ci-runner", {
        applicationId: "example",
        policyData: "example",
        securityGatewayId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.beyondcorp import SecurityGatewayApplicationIamPolicy, SecurityGatewayApplicationIamPolicyConfig

    def describe():
        ubx.intent("payments own security gateway application iam policy")

        ubx.resource(SecurityGatewayApplicationIamPolicy, "ci-runner", SecurityGatewayApplicationIamPolicyConfig(
            application_id="example",
            policy_data="example",
            security_gateway_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_beyondcorp_security_gateway_application_iam_policy,
    with application id example, policy data example, security
    gateway id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="security_gateway_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
