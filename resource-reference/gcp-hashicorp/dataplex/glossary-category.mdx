---
title: "google_dataplex_glossary_category"
description: "Real, generated bindings for `google_dataplex_glossary_category`."
---

`google_dataplex_glossary_category` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataplex "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dataplex"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own glossary category"})

    		ubx.Resource(dataplex.GlossaryCategory, "ci-runner", dataplex.GlossaryCategoryConfig{
    			Location:    "example",
    			Parent:      "example",
    			CategoryId:  "example",
    			Description: "Managed by ubx.",
    			DisplayName: "example-display-name",
    			GlossaryId:  "example",
    			Labels:      map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GlossaryCategory } from "jsr:@ubx/sdk-google/google/dataplex/glossary_category";

    export default stack("payments", () => {
      intent({ summary: "payments own glossary category" });

      resource(GlossaryCategory, "ci-runner", {
        location: "example",
        parent: "example",
        categoryId: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        glossaryId: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataplex import GlossaryCategory, GlossaryCategoryConfig

    def describe():
        ubx.intent("payments own glossary category")

        ubx.resource(GlossaryCategory, "ci-runner", GlossaryCategoryConfig(
            location="example",
            parent="example",
            category_id="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            glossary_id="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataplex_glossary_category, with location example,
    parent example, category id example, description Managed by ubx.,
    display name example-display-name, glossary id example, labels a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="category_id" type="string">
  The category id for creation. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  The user-mutable description of the GlossaryCategory. Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  User friendly display name of the GlossaryCategory. This is user-mutable. This will be same as the categoryId, if not specified. Optional.
</ResponseField>

<ResponseField name="glossary_id" type="string">
  The glossary id for creation. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  User-defined labels for the GlossaryCategory. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location where the glossary category should reside. Required.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The immediate parent of the GlossaryCategory in the resource-hierarchy. It can either be a Glossary or a Category. Format: projects/&#123;projectId&#125;/locations/&#123;locationId&#125;/glossaries/&#123;glossaryId&#125; OR projects/&#123;projectId&#125;/locations/&#123;locationId&#125;/glossaries/&#123;glossaryId&#125;/categories/&#123;categoryId&#125; Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which the GlossaryCategory was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the GlossaryCategory. Format: projects/&#123;projectId&#125;/locations/&#123;locationId&#125;/glossaries/&#123;glossaryId&#125;/categories/&#123;categoryId&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="uid" type="string">
  System generated unique id for the GlossaryCategory. This ID will be different if the GlossaryCategory is deleted and re-created with the same name. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  The time at which the GlossaryCategory was last updated. Computed by GCP after `ship`.
</ResponseField>
