---
title: "google_chronicle_data_table_row"
description: "Real, generated bindings for `google_chronicle_data_table_row`."
---

`google_chronicle_data_table_row` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own data table row"})

    		ubx.Resource(chronicle.DataTableRow, "ci-runner", chronicle.DataTableRowConfig{
    			DataTableId:   "example",
    			Instance:      "example",
    			Location:      "example",
    			Values:        []string{"example"},
    			RowTimeToLive: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DataTableRow } from "jsr:@ubx/sdk-google/google/chronicle/data_table_row";

    export default stack("payments", () => {
      intent({ summary: "payments own data table row" });

      resource(DataTableRow, "ci-runner", {
        dataTableId: "example",
        instance: "example",
        location: "example",
        values: ["example"],
        rowTimeToLive: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import DataTableRow, DataTableRowConfig

    def describe():
        ubx.intent("payments own data table row")

        ubx.resource(DataTableRow, "ci-runner", DataTableRowConfig(
            data_table_id="example",
            instance="example",
            location="example",
            values=["example"],
            row_time_to_live="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_data_table_row, with data table id
    example, instance example, location example, values a set value,
    row time to live example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_table_id" type="string" required>
  The ID of the parent DataTable. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The Chronicle instance ID. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The GCP location of the Chronicle instance. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="row_time_to_live" type="string">
  User-provided TTL of the data table row. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="values" type="list(string)" required>
  All column values for a single row. The values should be in the same order as the columns of the data tables. Required.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  DataTableRow create time Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="data_table_row" type="string">
  The ID of the DataTable Row. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the data table row. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/dataTables/&#123;data_table_id&#125;/dataTableRows/&#123;data_table_row&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  DataTableRow update time Computed by GCP after `ship`.
</ResponseField>
