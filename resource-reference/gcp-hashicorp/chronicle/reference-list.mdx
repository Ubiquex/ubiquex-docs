---
title: "google_chronicle_reference_list"
description: "Real, generated bindings for `google_chronicle_reference_list`."
---

`google_chronicle_reference_list` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own reference list"})

    		ubx.Resource(chronicle.ReferenceList, "ci-runner", chronicle.ReferenceListConfig{
    			Description:     "Managed by ubx.",
    			Instance:        "example",
    			Location:        "example",
    			ReferenceListId: "example",
    			SyntaxType:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ReferenceList } from "jsr:@ubx/sdk-google/google/chronicle/reference_list";

    export default stack("payments", () => {
      intent({ summary: "payments own reference list" });

      resource(ReferenceList, "ci-runner", {
        description: "Managed by ubx.",
        instance: "example",
        location: "example",
        referenceListId: "example",
        syntaxType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import ReferenceList, ReferenceListConfig

    def describe():
        ubx.intent("payments own reference list")

        ubx.resource(ReferenceList, "ci-runner", ReferenceListConfig(
            description="Managed by ubx.",
            instance="example",
            location="example",
            reference_list_id="example",
            syntax_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_reference_list, with description Managed
    by ubx., instance example, location example, reference list id
    example, syntax type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string" required>
  Required. A user-provided description of the reference list. Required.
</ResponseField>

<ResponseField name="entries" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="value" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The unique identifier for the Chronicle instance, which is the same as the customer ID. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the resource. This is the geographical region where the Chronicle instance resides, such as "us" or "europe-west2". Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reference_list_id" type="string" required>
  Required. The ID to use for the reference list. This is also the display name for the reference list. It must satisfy the following requirements: - Starts with letter. - Contains only letters, numbers and underscore. - Has length &lt; 256. - Must be unique. Required.
</ResponseField>

<ResponseField name="scope_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="reference_list_scope" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="scope_names" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="syntax_type" type="string" required>
  Possible values: REFERENCE_LIST_SYNTAX_TYPE_PLAIN_TEXT_STRING REFERENCE_LIST_SYNTAX_TYPE_REGEX REFERENCE_LIST_SYNTAX_TYPE_CIDR Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="display_name" type="string">
  Output only. The unique display name of the reference list. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="entries" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="value" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Output only. The resource name of the reference list. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/referenceLists/&#123;reference_list&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="revision_create_time" type="string">
  Output only. The timestamp when the reference list was last updated. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="rule_associations_count" type="number">
  Output only. The count of self-authored rules using the reference list. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="rules" type="list(string)">
  Output only. The resource names for the associated self-authored Rules that use this reference list. This is returned only when the view is REFERENCE_LIST_VIEW_FULL. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="scope_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="reference_list_scope" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="scope_names" type="list(string)">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
