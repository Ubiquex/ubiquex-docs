---
title: "google_chronicle_environment"
description: "Real, generated bindings for `google_chronicle_environment`."
---

`google_chronicle_environment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment"})

    		ubx.Resource(chronicle.Environment, "ci-runner", chronicle.EnvironmentConfig{
    			Contact:           "example",
    			ContactEmails:     "example",
    			ContactPhone:      "example",
    			Description:       "Managed by ubx.",
    			DisplayName:       "example-display-name",
    			Instance:          "example",
    			Location:          "example",
    			RetentionDuration: 7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Environment } from "jsr:@ubx/sdk-google/google/chronicle/environment";

    export default stack("payments", () => {
      intent({ summary: "payments own environment" });

      resource(Environment, "ci-runner", {
        contact: "example",
        contactEmails: "example",
        contactPhone: "example",
        description: "Managed by ubx.",
        displayName: "example-display-name",
        instance: "example",
        location: "example",
        retentionDuration: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import Environment, EnvironmentConfig

    def describe():
        ubx.intent("payments own environment")

        ubx.resource(Environment, "ci-runner", EnvironmentConfig(
            contact="example",
            contact_emails="example",
            contact_phone="example",
            description="Managed by ubx.",
            display_name="example-display-name",
            instance="example",
            location="example",
            retention_duration=7200,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_environment, with contact example,
    contact emails example, contact phone example, description
    Managed by ubx., display name example-display-name, instance
    example, location example, retention duration 7200.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aliases_json" type="string">
  Environment nicknames. Optional.
</ResponseField>

<ResponseField name="contact" type="string" required>
  MAX_NAME_LENGTH = 256 Name of the contact for the environment. Required.
</ResponseField>

<ResponseField name="contact_emails" type="string" required>
  MAX_NAME_LENGTH = 256 Email of the contact for the environment. Multiple emails can be sepereated with the ';' character. Required.
</ResponseField>

<ResponseField name="contact_phone" type="string" required>
  MAX_NAME_LENGTH = 256 Phone number of the contact for the environment. Required.
</ResponseField>

<ResponseField name="data_access_scopes_json" type="string">
  data access scopes. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_protection" type="bool">
  Whether Terraform will be prevented from destroying the environment. Deleting an environment will remove all its data and all playbooks, environments, integrations instances, reports and agents related to the environment. Once you delete an environment, it cannot be reversed. Deleting environments via terraform destroy or terraform apply will only succeed if this field is false in the Terraform state. Optional.
</ResponseField>

<ResponseField name="description" type="string" required>
  MAX_NAME_LENGTH = 256 Description of the environment. Required.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Name of the environment MAX_NAME_LENGTH = 256 Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="retention_duration" type="number" required>
  Environment data retention in months. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment_id" type="string">
  Id of the environment record. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The unique name(ID) of the Environment. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/environments/&#123;environment&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
