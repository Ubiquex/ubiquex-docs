---
title: "google_chronicle_rule_deployment"
description: "Real, generated bindings for `google_chronicle_rule_deployment`."
---

`google_chronicle_rule_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own rule deployment"})

    		ubx.Resource(chronicle.RuleDeployment, "ci-runner", chronicle.RuleDeploymentConfig{
    			Instance:     "example",
    			Location:     "example",
    			Rule:         "example",
    			Alerting:     true,
    			Archived:     true,
    			Enabled:      true,
    			RunFrequency: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleDeployment } from "jsr:@ubx/sdk-google/google/chronicle/rule_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own rule deployment" });

      resource(RuleDeployment, "ci-runner", {
        instance: "example",
        location: "example",
        rule: "example",
        alerting: true,
        archived: true,
        enabled: true,
        runFrequency: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import RuleDeployment, RuleDeploymentConfig

    def describe():
        ubx.intent("payments own rule deployment")

        ubx.resource(RuleDeployment, "ci-runner", RuleDeploymentConfig(
            instance="example",
            location="example",
            rule="example",
            alerting=True,
            archived=True,
            enabled=True,
            run_frequency="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_rule_deployment, with instance example,
    location example, rule example, alerting true, archived true,
    enabled true, run frequency example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alerting" type="bool">
  Whether detections resulting from this deployment should be considered alerts. Optional.
</ResponseField>

<ResponseField name="archived" type="bool">
  The archive state of the rule deployment. Cannot be set to true unless enabled is set to false i.e. archiving requires a two-step process: first, disable the rule by setting 'enabled' to false, then set 'archive' to true. If set to true, alerting will automatically be set to false. If currently set to true, enabled, alerting, and run_frequency cannot be updated. Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Whether the rule is currently deployed continuously against incoming data. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The unique identifier for the Chronicle instance, which is the same as the customer ID. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the resource. This is the geographical region where the Chronicle instance resides, such as "us" or "europe-west2". Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="rule" type="string" required>
  The Rule ID of the rule. Required.
</ResponseField>

<ResponseField name="run_frequency" type="string">
  The run frequency of the rule deployment. Possible values: LIVE HOURLY DAILY Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="archive_time" type="string">
  Output only. The timestamp when the rule deployment archive state was last set to true. If the rule deployment's current archive state is not set to true, the field will be empty. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="consumer_rules" type="list(string)">
  Output only. The names of the associated/chained consumer rules. Rules are considered consumers of this rule if their rule text explicitly filters on this rule's ruleid. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/rules/&#123;rule&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="execution_state" type="string">
  The execution state of the rule deployment. Possible values: DEFAULT LIMITED PAUSED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="last_alert_status_change_time" type="string">
  Output only. The timestamp when the rule deployment alert state was lastly changed. This is filled regardless of the current alert state.E.g. if the current alert status is false, this timestamp will be the timestamp when the alert status was changed to false. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the rule deployment. Note that RuleDeployment is a child of the overall Rule, not any individual revision, so the resource ID segment for the Rule resource must not reference a specific revision. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/rules/&#123;rule&#125;/deployment Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="producer_rules" type="list(string)">
  Output only. The names of the associated/chained producer rules. Rules are considered producers for this rule if this rule explicitly filters on their ruleid. Format: projects/&#123;project&#125;/locations/&#123;location&#125;/instances/&#123;instance&#125;/rules/&#123;rule&#125; Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
