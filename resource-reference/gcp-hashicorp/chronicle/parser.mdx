---
title: "google_chronicle_parser"
description: "Real, generated bindings for `google_chronicle_parser`."
---

`google_chronicle_parser` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	chronicle "github.com/ubiquex/ubx-sdk-google/sdk/go/google/chronicle"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own parser"})

    		ubx.Resource(chronicle.Parser, "ci-runner", chronicle.ParserConfig{
    			Instance:             "example",
    			Location:             "example",
    			Logtype:              "example",
    			Cbn:                  "example",
    			ValidatedOnEmptyLogs: true,
    			ValidationSkipped:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Parser } from "jsr:@ubx/sdk-google/google/chronicle/parser";

    export default stack("payments", () => {
      intent({ summary: "payments own parser" });

      resource(Parser, "ci-runner", {
        instance: "example",
        location: "example",
        logtype: "example",
        cbn: "example",
        validatedOnEmptyLogs: true,
        validationSkipped: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.chronicle import Parser, ParserConfig

    def describe():
        ubx.intent("payments own parser")

        ubx.resource(Parser, "ci-runner", ParserConfig(
            instance="example",
            location="example",
            logtype="example",
            cbn="example",
            validated_on_empty_logs=True,
            validation_skipped=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_chronicle_parser, with instance example, location
    example, logtype example, cbn example, validated on empty logs
    true, validation skipped true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cbn" type="string">
  if the parser is built using config documentation: https://cloud.google.com/chronicle/docs/preview/parser-extensions/parsing-overview Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="logtype" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="low_code" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="field_extractors" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="append_repeated_fields" type="bool">
        </ResponseField>
        <ResponseField name="extractors" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="destination_path" type="string">
            </ResponseField>
            <ResponseField name="field_path" type="string">
            </ResponseField>
            <ResponseField name="precondition_op" type="string">
            </ResponseField>
            <ResponseField name="precondition_path" type="string">
            </ResponseField>
            <ResponseField name="precondition_value" type="string">
            </ResponseField>
            <ResponseField name="value" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="log_format" type="string">
        </ResponseField>
        <ResponseField name="preprocess_config" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="grok_regex" type="string">
            </ResponseField>
            <ResponseField name="target" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transformed_cbn_snippet" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="validated_on_empty_logs" type="bool">
  Flag to bypass parser validation when no logs are found. If enabled, the parser won't be be rejected during the validation phase when no logs are found. Optional.
</ResponseField>

<ResponseField name="validation_skipped" type="bool">
  If true, bypasses parser validation. If enabled, the parser won't be rejected during the validation phase and validation will be skipped. Optional.
</ResponseField>

<ResponseField name="version_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="auto_upgrade_disabled" type="bool" required>
    </ResponseField>
    <ResponseField name="latest_parser" type="string">
    </ResponseField>
    <ResponseField name="latest_parser_version" type="string">
    </ResponseField>
    <ResponseField name="rollback_available" type="bool">
    </ResponseField>
    <ResponseField name="version" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="changelogs" type="list(object)">
  Changelogs of a parser. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="entries" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="change_message" type="string">
        </ResponseField>
        <ResponseField name="create_time" type="string">
        </ResponseField>
        <ResponseField name="deleted" type="bool">
        </ResponseField>
        <ResponseField name="parser_version" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="create_time" type="string">
  Time at which parser was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="creator" type="list(object)">
  Information about the creator of the parser. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="author" type="string">
    </ResponseField>
    <ResponseField name="customer" type="string">
    </ResponseField>
    <ResponseField name="source" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dynamic_parsing_config" type="string">
  Dynamic parsing config applied over the parser, if any. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="low_code" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="field_extractors" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="append_repeated_fields" type="bool">
        </ResponseField>
        <ResponseField name="extractors" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="destination_path" type="string">
            </ResponseField>
            <ResponseField name="field_path" type="string">
            </ResponseField>
            <ResponseField name="precondition_op" type="string">
            </ResponseField>
            <ResponseField name="precondition_path" type="string">
            </ResponseField>
            <ResponseField name="precondition_value" type="string">
            </ResponseField>
            <ResponseField name="value" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="log_format" type="string">
        </ResponseField>
        <ResponseField name="preprocess_config" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="grok_regex" type="string">
            </ResponseField>
            <ResponseField name="target" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="transformed_cbn_snippet" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="log" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  name of the parser resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="parser" type="string">
  Output only. The server-generated ID of the parser. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="parser_extension" type="string">
  Extension applied over the parser, if any. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="release_stage" type="string">
  The release stage of the parser After internal validations the prebuilt parser will directly start as Release Candidate. The release_stage of prebuilt parsers are changed after every release cycle: The prebuilt Release Candidate parser is promoted as Release parser. The existing prebuilt Release parser is moved to Rollback state. and existing prebuilt rollback parser is moved to Archived. In case of custom parser: When the customer submits a validation passed custom parser it starts as Release state. And existing one is moved to Rollback stage. And the existing rollback is moved to Archived. In case a release or release candidate parser is found faulty, the parser is marked FAULTY, if it is release parser then rollback candidate is moved to release. Possible values: RELEASE RELEASE_CANDIDATE ROLLBACK_CANDIDATE ARCHIVED FAULTY ARCHIVED_IN_USE Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the parser Possible values: ACTIVE INACTIVE Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  The type of the parser Possible values: CUSTOM PREBUILT Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="validation_report" type="string">
  The Validation report generated during parser validation. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="validation_stage" type="string">
  The validation stage of the parser When a customer submits a new parser for validation, it starts with a new stage. When parser is picked for validation, it changes to Validation state. If validation failed it is marked as failed, and existing failed is moved to delete_candidate stage. If passes it is moved to passed stage. If customer opts to submit it, the parser is moved to Release State. Possible values: NEW VALIDATING PASSED FAILED DELETE_CANDIDATE INTERNAL_ERROR VALIDATION_SKIPPED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="version_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="auto_upgrade_disabled" type="bool" required>
    </ResponseField>
    <ResponseField name="latest_parser" type="string">
    </ResponseField>
    <ResponseField name="latest_parser_version" type="string">
    </ResponseField>
    <ResponseField name="rollback_available" type="bool">
    </ResponseField>
    <ResponseField name="version" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
