---
title: "google_container_analysis_note_iam_policy"
description: "Real, generated bindings for `google_container_analysis_note_iam_policy`."
---

`google_container_analysis_note_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	container "github.com/ubiquex/ubx-sdk-google/sdk/go/google/container"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own analysis note iam policy"})

    		ubx.Resource(container.AnalysisNoteIamPolicy, "ci-runner", container.AnalysisNoteIamPolicyConfig{
    			Note:       "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnalysisNoteIamPolicy } from "jsr:@ubx/sdk-google/google/container/analysis_note_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own analysis note iam policy" });

      resource(AnalysisNoteIamPolicy, "ci-runner", {
        note: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.container import AnalysisNoteIamPolicy, AnalysisNoteIamPolicyConfig

    def describe():
        ubx.intent("payments own analysis note iam policy")

        ubx.resource(AnalysisNoteIamPolicy, "ci-runner", AnalysisNoteIamPolicyConfig(
            note="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_container_analysis_note_iam_policy, with note
    example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="note" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
