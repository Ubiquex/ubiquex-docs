---
title: "google_iap_agent_registry_mcp_server_iam_policy"
description: "Real, generated bindings for `google_iap_agent_registry_mcp_server_iam_policy`."
---

`google_iap_agent_registry_mcp_server_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	iap "github.com/ubiquex/ubx-sdk-google/sdk/go/google/iap"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own agent registry mcp server iam policy"})

    		ubx.Resource(iap.AgentRegistryMcpServerIamPolicy, "ci-runner", iap.AgentRegistryMcpServerIamPolicyConfig{
    			McpServerId: "example",
    			PolicyData:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AgentRegistryMcpServerIamPolicy } from "jsr:@ubx/sdk-google/google/iap/agent_registry_mcp_server_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own agent registry mcp server iam policy" });

      resource(AgentRegistryMcpServerIamPolicy, "ci-runner", {
        mcpServerId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.iap import AgentRegistryMcpServerIamPolicy, AgentRegistryMcpServerIamPolicyConfig

    def describe():
        ubx.intent("payments own agent registry mcp server iam policy")

        ubx.resource(AgentRegistryMcpServerIamPolicy, "ci-runner", AgentRegistryMcpServerIamPolicyConfig(
            mcp_server_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_iap_agent_registry_mcp_server_iam_policy, with mcp
    server id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="mcp_server_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
