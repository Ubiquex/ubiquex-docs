---
title: "google_tags_tag_key_iam_policy"
description: "Real, generated bindings for `google_tags_tag_key_iam_policy`."
---

`google_tags_tag_key_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tags "github.com/ubiquex/ubx-sdk-google/sdk/go/google/tags"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag key iam policy"})

    		ubx.Resource(tags.TagKeyIamPolicy, "ci-runner", tags.TagKeyIamPolicyConfig{
    			PolicyData: "example",
    			TagKey:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagKeyIamPolicy } from "jsr:@ubx/sdk-google/google/tags/tag_key_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own tag key iam policy" });

      resource(TagKeyIamPolicy, "ci-runner", {
        policyData: "example",
        tagKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.tags import TagKeyIamPolicy, TagKeyIamPolicyConfig

    def describe():
        ubx.intent("payments own tag key iam policy")

        ubx.resource(TagKeyIamPolicy, "ci-runner", TagKeyIamPolicyConfig(
            policy_data="example",
            tag_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_tags_tag_key_iam_policy, with policy data example,
    tag key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tag_key" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
