---
title: "google_tags_tag_binding"
description: "Real, generated bindings for `google_tags_tag_binding`."
---

`google_tags_tag_binding` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	tags "github.com/ubiquex/ubx-sdk-google/sdk/go/google/tags"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own tag binding"})

    		ubx.Resource(tags.TagBinding, "ci-runner", tags.TagBindingConfig{
    			Parent:   "example",
    			TagValue: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TagBinding } from "jsr:@ubx/sdk-google/google/tags/tag_binding";

    export default stack("payments", () => {
      intent({ summary: "payments own tag binding" });

      resource(TagBinding, "ci-runner", {
        parent: "example",
        tagValue: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.tags import TagBinding, TagBindingConfig

    def describe():
        ubx.intent("payments own tag binding")

        ubx.resource(TagBinding, "ci-runner", TagBindingConfig(
            parent="example",
            tag_value="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_tags_tag_binding, with parent example, tag value
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="parent" type="string" required>
  The full resource name of the resource the TagValue is bound to. E.g. //cloudresourcemanager.googleapis.com/projects/123 Required.
</ResponseField>

<ResponseField name="tag_value" type="string" required>
  The TagValue of the TagBinding. Must be either in id format 'tagValues/&#123;tag-value-id&#125;', or namespaced format '&#123;parent-id&#125;/&#123;tag-key-short-name&#125;/&#123;tag-value-short-name&#125;'. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The generated id for the TagBinding. This is a string of the form 'tagBindings/&#123;full-resource-name&#125;/&#123;tag-value-name&#125;' or 'tagBindings/&#123;full-resource-name&#125;/&#123;tag-key-name&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
