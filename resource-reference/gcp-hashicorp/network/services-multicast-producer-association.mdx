---
title: "google_network_services_multicast_producer_association"
description: "Real, generated bindings for `google_network_services_multicast_producer_association`."
---

`google_network_services_multicast_producer_association` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own services multicast producer association"})

    		ubx.Resource(network.ServicesMulticastProducerAssociation, "ci-runner", network.ServicesMulticastProducerAssociationConfig{
    			Location:                       "example",
    			MulticastDomainActivation:      "example",
    			MulticastProducerAssociationId: "example",
    			Network:                        "example",
    			Description:                    "Managed by ubx.",
    			Labels:                         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServicesMulticastProducerAssociation } from "jsr:@ubx/sdk-google/google/network/services_multicast_producer_association";

    export default stack("payments", () => {
      intent({ summary: "payments own services multicast producer association" });

      resource(ServicesMulticastProducerAssociation, "ci-runner", {
        location: "example",
        multicastDomainActivation: "example",
        multicastProducerAssociationId: "example",
        network: "example",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import ServicesMulticastProducerAssociation, ServicesMulticastProducerAssociationConfig

    def describe():
        ubx.intent("payments own services multicast producer association")

        ubx.resource(ServicesMulticastProducerAssociation, "ci-runner", ServicesMulticastProducerAssociationConfig(
            location="example",
            multicast_domain_activation="example",
            multicast_producer_association_id="example",
            network="example",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_services_multicast_producer_association,
    with location example, multicast domain activation example,
    multicast producer association id example, network example,
    description Managed by ubx., labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional text description of the multicast producer association. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels as key-value pairs **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122. Required.
</ResponseField>

<ResponseField name="multicast_domain_activation" type="string" required>
  The resource name of the multicast domain activation that is in the same zone as this multicast producer association. Use the following format: // 'projects/*/locations/*/multicastDomainActivations/*'. Required.
</ResponseField>

<ResponseField name="multicast_producer_association_id" type="string" required>
  A unique name for the multicast producer association. The name is restricted to letters, numbers, and hyphen, with the first character a letter, and the last a letter or a number. The name must not exceed 48 characters. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  The resource name of the multicast producer VPC network. Use following format: 'projects/&#123;project&#125;/locations/global/networks/&#123;network&#125;'. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  [Output only] The timestamp when the multicast producer association was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  Identifier. The resource name of the multicast producer association. Use the following format: 'projects/*/locations/*/multicastProducerAssociations/*'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="list(object)">
  The multicast resource's state. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="unique_id" type="string">
  [Output only] The Google-generated UUID for the resource. This value is unique across all multicast producer association resources. If a producer association is deleted and another with the same name is created, the new producer association is assigned a different unique_id. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="update_time" type="string">
  [Output only] The timestamp when the Multicast Producer Association was most recently updated. Computed by GCP after `ship`.
</ResponseField>
