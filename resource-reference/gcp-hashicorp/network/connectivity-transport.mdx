---
title: "google_network_connectivity_transport"
description: "Real, generated bindings for `google_network_connectivity_transport`."
---

`google_network_connectivity_transport` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connectivity transport"})

    		ubx.Resource(network.ConnectivityTransport, "ci-runner", network.ConnectivityTransportConfig{
    			Name:             "ci-runner",
    			Network:          "example",
    			Region:           "example",
    			RemoteProfile:    "example",
    			AdvertisedRoutes: []string{"example"},
    			Description:      "Managed by ubx.",
    			Labels:           map[string]string{"managed-by": "ubx"},
    			MtuLimit:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectivityTransport } from "jsr:@ubx/sdk-google/google/network/connectivity_transport";

    export default stack("payments", () => {
      intent({ summary: "payments own connectivity transport" });

      resource(ConnectivityTransport, "ci-runner", {
        name: "ci-runner",
        network: "example",
        region: "example",
        remoteProfile: "example",
        advertisedRoutes: ["example"],
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
        mtuLimit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import ConnectivityTransport, ConnectivityTransportConfig

    def describe():
        ubx.intent("payments own connectivity transport")

        ubx.resource(ConnectivityTransport, "ci-runner", ConnectivityTransportConfig(
            name="ci-runner",
            network="example",
            region="example",
            remote_profile="example",
            advertised_routes=["example"],
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
            mtu_limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_connectivity_transport called "ci-runner",
    with network example, region example, remote profile example,
    advertised routes a set value, description Managed by ubx.,
    labels a set value, mtu limit 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="advertised_routes" type="list(string)">
  List of IP Prefixes that will be advertised to the remote provider. Both IPv4 and IPv6 addresses are supported. Optional.
</ResponseField>

<ResponseField name="bandwidth" type="string">
  Bandwidth of the Transport. This must be one of the supported bandwidths for the remote profile. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Optional labels in key:value format. For more information about labels, see [Requirements for labels](https://cloud.google.com/resource-manager/docs/creating-managing-labels#requirements). **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="mtu_limit" type="number">
  [Output only] The maximum transmission unit (MTU) of a packet that can be sent over this transport. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource, see google.aip.dev/122 for resource naming. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  Resource URL of the Network that will be peered with this Transport. This field must be provided during resource creation and cannot be changed. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="provided_activation_key" type="string">
  Key used for establishing a connection with the remote transport. This key can only be provided if the profile supports an INPUT key flow and the resource is in the PENDING_KEY state. Optional.
</ResponseField>

<ResponseField name="region" type="string" required>
  The region of this resource. This is required to construct the resource name, but is not sent to the API since the region is already contained in the parent field. Required.
</ResponseField>

<ResponseField name="remote_account_id" type="string">
  The user supplied account id for the CSP associated with the remote profile. Optional.
</ResponseField>

<ResponseField name="remote_profile" type="string" required>
  Resource URL of the remoteTransportProfile that this Transport is connecting to. Required.
</ResponseField>

<ResponseField name="stack_type" type="string">
  IP version stack for the established connectivity. Possible values: ["IPV4_IPV6", "IPV4_ONLY"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="bandwidth" type="string">
  Bandwidth of the Transport. This must be one of the supported bandwidths for the remote profile. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="generated_activation_key" type="string">
  [Output only] Google-generated activation key. This is only output if the selected profile supports an OUTPUT key flow. Inputting this to the provider is only valid while the resource is in a PENDING_KEY state. Once the provider has accepted the key, the resource will move to the CONFIGURING state. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="peering_network" type="string">
  VPC Network URI that was created for the VPC Peering connection to the provided 'network'. If VPC Peering is disconnected, this can be used to re-establish. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="stack_type" type="string">
  IP version stack for the established connectivity. Possible values: ["IPV4_IPV6", "IPV4_ONLY"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="state" type="string">
  [Output Only] State of the underlying connectivity. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
