---
title: "google_network_security_gateway_security_policy_rule"
description: "Real, generated bindings for `google_network_security_gateway_security_policy_rule`."
---

`google_network_security_gateway_security_policy_rule` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security gateway security policy rule"})

    		ubx.Resource(network.SecurityGatewaySecurityPolicyRule, "ci-runner", network.SecurityGatewaySecurityPolicyRuleConfig{
    			BasicProfile:          "example",
    			Enabled:               true,
    			GatewaySecurityPolicy: "example",
    			Location:              "example",
    			Name:                  "ci-runner",
    			Priority:              1,
    			SessionMatcher:        "example",
    			ApplicationMatcher:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityGatewaySecurityPolicyRule } from "jsr:@ubx/sdk-google/google/network/security_gateway_security_policy_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own security gateway security policy rule" });

      resource(SecurityGatewaySecurityPolicyRule, "ci-runner", {
        basicProfile: "example",
        enabled: true,
        gatewaySecurityPolicy: "example",
        location: "example",
        name: "ci-runner",
        priority: 1,
        sessionMatcher: "example",
        applicationMatcher: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityGatewaySecurityPolicyRule, SecurityGatewaySecurityPolicyRuleConfig

    def describe():
        ubx.intent("payments own security gateway security policy rule")

        ubx.resource(SecurityGatewaySecurityPolicyRule, "ci-runner", SecurityGatewaySecurityPolicyRuleConfig(
            basic_profile="example",
            enabled=True,
            gateway_security_policy="example",
            location="example",
            name="ci-runner",
            priority=1,
            session_matcher="example",
            application_matcher="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_gateway_security_policy_rule
    called "ci-runner", with basic profile example, enabled true,
    gateway security policy example, location example, priority 1,
    session matcher example, application matcher example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_matcher" type="string">
  CEL expression for matching on L7/application level criteria. Optional.
</ResponseField>

<ResponseField name="basic_profile" type="string" required>
  Profile which tells what the primitive action should be. Possible values are: * ALLOW * DENY. Possible values: ["BASIC_PROFILE_UNSPECIFIED", "ALLOW", "DENY"] Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Free-text description of the resource. Optional.
</ResponseField>

<ResponseField name="enabled" type="bool" required>
  Whether the rule is enforced. Required.
</ResponseField>

<ResponseField name="gateway_security_policy" type="string" required>
  The name of the gatewat security policy this rule belongs to. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location of the gateway security policy. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the resource. ame is the full resource name so projects/&#123;project&#125;/locations/&#123;location&#125;/gatewaySecurityPolicies/&#123;gateway_security_policy&#125;/rules/&#123;rule&#125; rule should match the pattern: (^a-z?$). Required.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Priority of the rule. Lower number corresponds to higher precedence. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="session_matcher" type="string" required>
  CEL expression for matching on session criteria. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tls_inspection_enabled" type="bool">
  Flag to enable TLS inspection of traffic matching on. Can only be true if the parent GatewaySecurityPolicy references a TLSInspectionConfig. Optional.
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The timestamp when the resource was created. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z" Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Server-defined URL of this resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the resource was updated. A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z". Computed by GCP after `ship`.
</ResponseField>
