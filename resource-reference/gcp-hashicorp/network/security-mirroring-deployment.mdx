---
title: "google_network_security_mirroring_deployment"
description: "Real, generated bindings for `google_network_security_mirroring_deployment`."
---

`google_network_security_mirroring_deployment` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	network "github.com/ubiquex/ubx-sdk-google/sdk/go/google/network"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security mirroring deployment"})

    		ubx.Resource(network.SecurityMirroringDeployment, "ci-runner", network.SecurityMirroringDeploymentConfig{
    			ForwardingRule:           "example",
    			Location:                 "example",
    			MirroringDeploymentGroup: "example",
    			MirroringDeploymentId:    "example",
    			Description:              "Managed by ubx.",
    			Labels:                   map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityMirroringDeployment } from "jsr:@ubx/sdk-google/google/network/security_mirroring_deployment";

    export default stack("payments", () => {
      intent({ summary: "payments own security mirroring deployment" });

      resource(SecurityMirroringDeployment, "ci-runner", {
        forwardingRule: "example",
        location: "example",
        mirroringDeploymentGroup: "example",
        mirroringDeploymentId: "example",
        description: "Managed by ubx.",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.network import SecurityMirroringDeployment, SecurityMirroringDeploymentConfig

    def describe():
        ubx.intent("payments own security mirroring deployment")

        ubx.resource(SecurityMirroringDeployment, "ci-runner", SecurityMirroringDeploymentConfig(
            forwarding_rule="example",
            location="example",
            mirroring_deployment_group="example",
            mirroring_deployment_id="example",
            description="Managed by ubx.",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_network_security_mirroring_deployment, with
    forwarding rule example, location example, mirroring deployment
    group example, mirroring deployment id example, description
    Managed by ubx., labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  User-provided description of the deployment. Used as additional context for the deployment. Optional.
</ResponseField>

<ResponseField name="forwarding_rule" type="string" required>
  The regional forwarding rule that fronts the mirroring collectors, for example: 'projects/123456789/regions/us-central1/forwardingRules/my-rule'. See https://google.aip.dev/124. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  Labels are key/value pairs that help to organize and filter resources. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string" required>
  The cloud location of the deployment, e.g. 'us-central1-a' or 'asia-south1-b'. Required.
</ResponseField>

<ResponseField name="mirroring_deployment_group" type="string" required>
  The deployment group that this deployment is a part of, for example: 'projects/123456789/locations/global/mirroringDeploymentGroups/my-dg'. See https://google.aip.dev/124. Required.
</ResponseField>

<ResponseField name="mirroring_deployment_id" type="string" required>
  The ID to use for the new deployment, which will become the final component of the deployment's resource name. Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The timestamp when the resource was created. See https://google.aip.dev/148#timestamps. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of this deployment, for example: 'projects/123456789/locations/us-central1-a/mirroringDeployments/my-dep'. See https://google.aip.dev/122 for more details. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reconciling" type="bool">
  The current state of the resource does not match the user's intended state, and the system is working to reconcile them. This part of the normal operation (e.g. linking a new association to the parent group). See https://google.aip.dev/128. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the deployment. See https://google.aip.dev/216. Possible values: STATE_UNSPECIFIED ACTIVE CREATING DELETING OUT_OF_SYNC DELETE_FAILED Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The timestamp when the resource was most recently updated. See https://google.aip.dev/148#timestamps. Computed by GCP after `ship`.
</ResponseField>
