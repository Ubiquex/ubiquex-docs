---
title: "google_sql_provision_script"
description: "Real, generated bindings for `google_sql_provision_script`."
---

`google_sql_provision_script` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sql "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sql"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provision script"})

    		ubx.Resource(sql.ProvisionScript, "ci-runner", sql.ProvisionScriptConfig{
    			Instance:       "example",
    			Script:         "example",
    			Database:       "example",
    			DeletionPolicy: "example",
    			Description:    "Managed by ubx.",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProvisionScript } from "jsr:@ubx/sdk-google/google/sql/provision_script";

    export default stack("payments", () => {
      intent({ summary: "payments own provision script" });

      resource(ProvisionScript, "ci-runner", {
        instance: "example",
        script: "example",
        database: "example",
        deletionPolicy: "example",
        description: "Managed by ubx.",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sql import ProvisionScript, ProvisionScriptConfig

    def describe():
        ubx.intent("payments own provision script")

        ubx.resource(ProvisionScript, "ci-runner", ProvisionScriptConfig(
            instance="example",
            script="example",
            database="example",
            deletion_policy="example",
            description="Managed by ubx.",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sql_provision_script, with instance example, script
    example, database example, deletion policy example, description
    Managed by ubx..
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="database" type="string">
  The name of the database to which Terraform connects. Changing this forces Terraform to connect to the new database and run the script. This argument is required for Postgres instances. It's optional for MySQL, but some of your queries may require a database. You can create and use a database in the script or explicitly reference a google_sql_database. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  The deletion policy for the resources created by the script. The default is "ABANDON". It must be "ABANDON" to allow Terraform to abandon the resources. If you want to delete resources, add statements in the script such as "drop … if exists". Optional.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the provision script. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The name of the Cloud SQL instance. Changing this forces the script to be run on the new instance. Required.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="script" type="string" required>
  The SQL script to provision database resources. Its execution time limit is 30 s. Changing this forces the script to be rerun. Make sure the script is idempotent. You can use statements like "create if not exists …" or "if not exists (select …) then … end if" to prevent existence-related errors. If it's not possible to make a statement idempotent, you can run it once and then remove it from this script. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>
