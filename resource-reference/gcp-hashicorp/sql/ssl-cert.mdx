---
title: "google_sql_ssl_cert"
description: "Real, generated bindings for `google_sql_ssl_cert`."
---

`google_sql_ssl_cert` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	sql "github.com/ubiquex/ubx-sdk-google/sdk/go/google/sql"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ssl cert"})

    		ubx.Resource(sql.SslCert, "ci-runner", sql.SslCertConfig{
    			CommonName: "example-common-name",
    			Instance:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SslCert } from "jsr:@ubx/sdk-google/google/sql/ssl_cert";

    export default stack("payments", () => {
      intent({ summary: "payments own ssl cert" });

      resource(SslCert, "ci-runner", {
        commonName: "example-common-name",
        instance: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.sql import SslCert, SslCertConfig

    def describe():
        ubx.intent("payments own ssl cert")

        ubx.resource(SslCert, "ci-runner", SslCertConfig(
            common_name="example-common-name",
            instance="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_sql_ssl_cert, with common name example-common-name,
    instance example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="common_name" type="string" required>
  The common name to be used in the certificate to identify the client. Constrained to [a-zA-Z.-_ ]+. Changing this forces a new resource to be created. Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance" type="string" required>
  The name of the Cloud SQL instance. Changing this forces a new resource to be created. Required.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="cert" type="string">
  The actual certificate data for this client certificate. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="cert_serial_number" type="string">
  The serial number extracted from the certificate data. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  The time when the certificate was created in RFC 3339 format, for example 2012-11-15T16:19:00.094Z. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="expiration_time" type="string">
  The time when the certificate expires in RFC 3339 format, for example 2012-11-15T16:19:00.094Z. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="private_key" type="string">
  The private key associated with the client certificate. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="server_ca_cert" type="string">
  The CA cert of the server this client cert was generated from. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="sha1_fingerprint" type="string">
  The SHA1 Fingerprint of the certificate. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
