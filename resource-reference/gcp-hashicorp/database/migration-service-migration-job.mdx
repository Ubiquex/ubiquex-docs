---
title: "google_database_migration_service_migration_job"
description: "Real, generated bindings for `google_database_migration_service_migration_job`."
---

`google_database_migration_service_migration_job` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	database "github.com/ubiquex/ubx-sdk-google/sdk/go/google/database"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own migration service migration job"})

    		ubx.Resource(database.MigrationServiceMigrationJob, "ci-runner", database.MigrationServiceMigrationJobConfig{
    			Destination:    "example",
    			MigrationJobId: "example",
    			Source:         "example",
    			Type:           "example",
    			DisplayName:    "example-display-name",
    			DumpPath:       "example",
    			DumpType:       "example",
    			Labels:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MigrationServiceMigrationJob } from "jsr:@ubx/sdk-google/google/database/migration_service_migration_job";

    export default stack("payments", () => {
      intent({ summary: "payments own migration service migration job" });

      resource(MigrationServiceMigrationJob, "ci-runner", {
        destination: "example",
        migrationJobId: "example",
        source: "example",
        type: "example",
        displayName: "example-display-name",
        dumpPath: "example",
        dumpType: "example",
        labels: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.database import MigrationServiceMigrationJob, MigrationServiceMigrationJobConfig

    def describe():
        ubx.intent("payments own migration service migration job")

        ubx.resource(MigrationServiceMigrationJob, "ci-runner", MigrationServiceMigrationJobConfig(
            destination="example",
            migration_job_id="example",
            source="example",
            type="example",
            display_name="example-display-name",
            dump_path="example",
            dump_type="example",
            labels={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_database_migration_service_migration_job, with
    destination example, migration job id example, source example,
    type example, display name example-display-name, dump path
    example, dump type example, labels a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="desired_state" type="string">
  The desired state of the migration job. If set to 'RUNNING', the migration job will be started. Possible values: ["NOT_STARTED", "RUNNING"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destination" type="string" required>
  The name of the destination connection profile resource in the form of projects/&#123;project&#125;/locations/&#123;location&#125;/connectionProfiles/&#123;destinationConnectionProfile&#125;. Required.
</ResponseField>

<ResponseField name="display_name" type="string">
  The migration job display name. Optional.
</ResponseField>

<ResponseField name="dump_flags" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dump_flags" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dump_path" type="string">
  The path to the dump file in Google Cloud Storage, in the format: (gs://[BUCKET_NAME]/[OBJECT_NAME]). This field and the "dump_flags" field are mutually exclusive. Optional.
</ResponseField>

<ResponseField name="dump_type" type="string">
  The type of the data dump. Supported for MySQL to CloudSQL for MySQL migrations only. Possible values: ["LOGICAL", "PHYSICAL"] Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="labels" type="map(string)">
  The resource labels for migration job to use to annotate any related underlying resources such as Compute Engine VMs. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field 'effective_labels' for all of the labels present on the resource. Optional.
</ResponseField>

<ResponseField name="location" type="string">
  The location where the migration job should reside. Optional.
</ResponseField>

<ResponseField name="migration_job_id" type="string" required>
  The ID of the migration job. Required.
</ResponseField>

<ResponseField name="objects_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="source_objects_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="object_configs" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="object_identifier" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="database" type="string">
                </ResponseField>
                <ResponseField name="schema" type="string">
                </ResponseField>
                <ResponseField name="table" type="string">
                </ResponseField>
                <ResponseField name="type" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="objects_selection_type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dump_parallel_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="postgres_homogeneous_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="is_native_logical" type="bool" required>
    </ResponseField>
    <ResponseField name="max_additional_subscriptions" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reverse_ssh_connectivity" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="vm" type="string">
    </ResponseField>
    <ResponseField name="vm_ip" type="string">
    </ResponseField>
    <ResponseField name="vm_port" type="number">
    </ResponseField>
    <ResponseField name="vpc" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source" type="string" required>
  The name of the source connection profile resource in the form of projects/&#123;project&#125;/locations/&#123;location&#125;/connectionProfiles/&#123;sourceConnectionProfile&#125;. Required.
</ResponseField>

<ResponseField name="static_ip_connectivity" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="stop_on_warnings" type="bool">
  If set to true, will stop the Terraform apply if there are validation warnings. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of the migration job. Possible values: ["ONE_TIME", "CONTINUOUS"] Required.
</ResponseField>

<ResponseField name="vpc_peering_connectivity" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="vpc" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Output only. The timestamp when the resource was created. A timestamp in RFC3339 UTC 'Zulu' format, accurate to nanoseconds. Example: '2014-10-02T15:01:23.045123456Z'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="desired_state" type="string">
  The desired state of the migration job. If set to 'RUNNING', the migration job will be started. Possible values: ["NOT_STARTED", "RUNNING"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="dump_flags" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dump_flags" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
        <ResponseField name="value" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_labels" type="map(string)">
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="error" type="list(object)">
  Output only. The error details in case of state FAILED. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="code" type="number">
    </ResponseField>
    <ResponseField name="details" type="list(map(string))">
    </ResponseField>
    <ResponseField name="message" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of this migration job resource in the form of projects/&#123;project&#125;/locations/&#123;location&#125;/migrationJobs/&#123;migrationJob&#125;. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="objects_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="source_objects_config" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="object_configs" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="object_identifier" type="list(object)">
              <Expandable title="properties">
                <ResponseField name="database" type="string">
                </ResponseField>
                <ResponseField name="schema" type="string">
                </ResponseField>
                <ResponseField name="table" type="string">
                </ResponseField>
                <ResponseField name="type" type="string" required>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="objects_selection_type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="performance_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="dump_parallel_level" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="phase" type="string">
  The current migration job phase. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="postgres_homogeneous_config" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="is_native_logical" type="bool" required>
    </ResponseField>
    <ResponseField name="max_additional_subscriptions" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="reverse_ssh_connectivity" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="vm" type="string">
    </ResponseField>
    <ResponseField name="vm_ip" type="string">
    </ResponseField>
    <ResponseField name="vm_port" type="number">
    </ResponseField>
    <ResponseField name="vpc" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  The current migration job state. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="static_ip_connectivity" type="list(object)">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="terraform_labels" type="map(string)">
  The combination of labels configured directly on the resource and default labels configured on the provider. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_peering_connectivity" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="vpc" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
