---
title: "google_cloud_ids_endpoint"
description: "Real, generated bindings for `google_cloud_ids_endpoint`."
---

`google_cloud_ids_endpoint` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	cloud "github.com/ubiquex/ubx-sdk-google/sdk/go/google/cloud"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ids endpoint"})

    		ubx.Resource(cloud.IdsEndpoint, "ci-runner", cloud.IdsEndpointConfig{
    			Location:         "example",
    			Name:             "ci-runner",
    			Network:          "example",
    			Severity:         "example",
    			Description:      "Managed by ubx.",
    			ThreatExceptions: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdsEndpoint } from "jsr:@ubx/sdk-google/google/cloud/ids_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own ids endpoint" });

      resource(IdsEndpoint, "ci-runner", {
        location: "example",
        name: "ci-runner",
        network: "example",
        severity: "example",
        description: "Managed by ubx.",
        threatExceptions: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.cloud import IdsEndpoint, IdsEndpointConfig

    def describe():
        ubx.intent("payments own ids endpoint")

        ubx.resource(IdsEndpoint, "ci-runner", IdsEndpointConfig(
            location="example",
            name="ci-runner",
            network="example",
            severity="example",
            description="Managed by ubx.",
            threat_exceptions=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_cloud_ids_endpoint called "ci-runner", with
    location example, network example, severity example, description
    Managed by ubx., threat exceptions a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of the endpoint. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string" required>
  The location for the endpoint. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the endpoint in the format projects/&#123;project_id&#125;/locations/&#123;locationId&#125;/endpoints/&#123;endpointId&#125;. Required.
</ResponseField>

<ResponseField name="network" type="string" required>
  Name of the VPC network that is connected to the IDS endpoint. This can either contain the VPC network name itself (like "src-net") or the full URL to the network (like "projects/&#123;project_id&#125;/global/networks/src-net"). Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="severity" type="string" required>
  The minimum alert severity level that is reported by the endpoint. Possible values: ["INFORMATIONAL", "LOW", "MEDIUM", "HIGH", "CRITICAL"] Required.
</ResponseField>

<ResponseField name="threat_exceptions" type="list(string)">
  Configuration for threat IDs excluded from generating alerts. Limit: 99 IDs. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  Creation timestamp in RFC 3339 text format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="endpoint_forwarding_rule" type="string">
  URL of the endpoint's network address to which traffic is to be sent by Packet Mirroring. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="endpoint_ip" type="string">
  Internal IP address of the endpoint's network entry point. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  Last update timestamp in RFC 3339 text format. Computed by GCP after `ship`.
</ResponseField>
