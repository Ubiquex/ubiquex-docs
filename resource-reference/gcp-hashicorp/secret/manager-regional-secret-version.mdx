---
title: "google_secret_manager_regional_secret_version"
description: "Real, generated bindings for `google_secret_manager_regional_secret_version`."
---

`google_secret_manager_regional_secret_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secret "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secret"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager regional secret version"})

    		ubx.Resource(secret.ManagerRegionalSecretVersion, "ci-runner", secret.ManagerRegionalSecretVersionConfig{
    			Secret:             "example",
    			SecretData:         "example",
    			Enabled:            true,
    			IsSecretDataBase64: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerRegionalSecretVersion } from "jsr:@ubx/sdk-google/google/secret/manager_regional_secret_version";

    export default stack("payments", () => {
      intent({ summary: "payments own manager regional secret version" });

      resource(ManagerRegionalSecretVersion, "ci-runner", {
        secret: "example",
        secretData: "example",
        enabled: true,
        isSecretDataBase64: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secret import ManagerRegionalSecretVersion, ManagerRegionalSecretVersionConfig

    def describe():
        ubx.intent("payments own manager regional secret version")

        ubx.resource(ManagerRegionalSecretVersion, "ci-runner", ManagerRegionalSecretVersionConfig(
            secret="example",
            secret_data="example",
            enabled=True,
            is_secret_data_base64=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secret_manager_regional_secret_version, with secret
    example, secret data example, enabled true, is secret data base64
    true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/secret_manager_regional_regional_secret_version.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enabled" type="bool">
  The current state of the regional secret version. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_secret_data_base64" type="bool">
  If set to 'true', the secret data is expected to be base64-encoded string and would be sent as is. Optional.
</ResponseField>

<ResponseField name="secret" type="string" required>
  Secret Manager regional secret resource. Required.
</ResponseField>

<ResponseField name="secret_data" type="string" required>
  The secret data. Must be no larger than 64KiB. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which the regional secret version was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="customer_managed_encryption" type="list(object)">
  The customer-managed encryption configuration of the regional secret. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="kms_key_version_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/secret_manager_regional_regional_secret_version.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destroy_time" type="string">
  The time at which the regional secret version was destroyed. Only present if state is DESTROYED. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Location of Secret Manager regional secret resource. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the regional secret version. Format: 'projects/&#123;&#123;project&#125;&#125;/locations/&#123;&#123;location&#125;&#125;/secrets/&#123;&#123;secret_id&#125;&#125;/versions/&#123;&#123;version&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string">
  The version of the Regional Secret. Computed by GCP after `ship`.
</ResponseField>
