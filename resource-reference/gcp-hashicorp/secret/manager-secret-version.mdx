---
title: "google_secret_manager_secret_version"
description: "Real, generated bindings for `google_secret_manager_secret_version`."
---

`google_secret_manager_secret_version` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secret "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secret"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own manager secret version"})

    		ubx.Resource(secret.ManagerSecretVersion, "ci-runner", secret.ManagerSecretVersionConfig{
    			Secret:              "example",
    			Enabled:             true,
    			IsSecretDataBase64:  true,
    			SecretData:          "example",
    			SecretDataWo:        "example",
    			SecretDataWoVersion: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ManagerSecretVersion } from "jsr:@ubx/sdk-google/google/secret/manager_secret_version";

    export default stack("payments", () => {
      intent({ summary: "payments own manager secret version" });

      resource(ManagerSecretVersion, "ci-runner", {
        secret: "example",
        enabled: true,
        isSecretDataBase64: true,
        secretData: "example",
        secretDataWo: "example",
        secretDataWoVersion: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secret import ManagerSecretVersion, ManagerSecretVersionConfig

    def describe():
        ubx.intent("payments own manager secret version")

        ubx.resource(ManagerSecretVersion, "ci-runner", ManagerSecretVersionConfig(
            secret="example",
            enabled=True,
            is_secret_data_base64=True,
            secret_data="example",
            secret_data_wo="example",
            secret_data_wo_version=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secret_manager_secret_version, with secret example,
    enabled true, is secret data base64 true, secret data example,
    secret data wo example, secret data wo version 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/secret_manager_secret_version.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="enabled" type="bool">
  The current state of the SecretVersion. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_secret_data_base64" type="bool">
  If set to 'true', the secret data is expected to be base64-encoded string and would be sent as is. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="secret" type="string" required>
  Secret Manager secret resource Required.
</ResponseField>

<ResponseField name="secret_data" type="string">
  The secret data. Must be no larger than 64KiB. Optional.
</ResponseField>

<ResponseField name="secret_data_wo" type="string">
  The secret data. Must be no larger than 64KiB. For more info see [updating write-only arguments](https://cloud.google.com/docs/providers/google/guides/using_write_only_arguments.html#updating-write-only-arguments) Optional.
</ResponseField>

<ResponseField name="secret_data_wo_version" type="number">
  Triggers update of secret data write-only. For more info see [updating write-only arguments](https://cloud.google.com/docs/providers/google/guides/using_write_only_arguments.html#updating-write-only-arguments) Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="create_time" type="string">
  The time at which the Secret was created. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  This field uses a custom implementation please refer to documentation under /hashicorp/terraform-provider-google-beta/website/docs/r/secret_manager_secret_version.html.markdown for specifics Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="destroy_time" type="string">
  The time at which the Secret was destroyed. Only present if state is DESTROYED. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The resource name of the SecretVersion. Format: 'projects/&#123;&#123;project&#125;&#125;/secrets/&#123;&#123;secret_id&#125;&#125;/versions/&#123;&#123;version&#125;&#125;' Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  The ID of the project in which the resource belongs. If it is not provided, the provider project is used Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="version" type="string">
  The version of the Secret. Computed by GCP after `ship`.
</ResponseField>
