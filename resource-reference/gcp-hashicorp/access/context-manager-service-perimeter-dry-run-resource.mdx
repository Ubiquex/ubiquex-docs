---
title: "google_access_context_manager_service_perimeter_dry_run_resource"
description: "Real, generated bindings for `google_access_context_manager_service_perimeter_dry_run_resource`."
---

`google_access_context_manager_service_perimeter_dry_run_resource` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	access "github.com/ubiquex/ubx-sdk-google/sdk/go/google/access"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager service perimeter dry run resource"})

    		ubx.Resource(access.ContextManagerServicePerimeterDryRunResource, "ci-runner", access.ContextManagerServicePerimeterDryRunResourceConfig{
    			PerimeterName: "example-perimeter-name",
    			Resource:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerServicePerimeterDryRunResource } from "jsr:@ubx/sdk-google/google/access/context_manager_service_perimeter_dry_run_resource";

    export default stack("payments", () => {
      intent({
        summary: "payments own context manager service perimeter dry run resource",
      });

      resource(ContextManagerServicePerimeterDryRunResource, "ci-runner", {
        perimeterName: "example-perimeter-name",
        resource: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerServicePerimeterDryRunResource, ContextManagerServicePerimeterDryRunResourceConfig

    def describe():
        ubx.intent("payments own context manager service perimeter dry run resource")

        ubx.resource(ContextManagerServicePerimeterDryRunResource, "ci-runner", ContextManagerServicePerimeterDryRunResourceConfig(
            perimeter_name="example-perimeter-name",
            resource="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    google_access_context_manager_service_perimeter_dry_run_resource,
    with perimeter name example-perimeter-name, resource example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="perimeter_name" type="string" required>
  The name of the Service Perimeter to add this resource to. Required.
</ResponseField>

<ResponseField name="resource" type="string" required>
  A GCP resource that is inside of the service perimeter. Currently only projects are allowed. Format: projects/&#123;project_number&#125; Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="access_policy_id" type="string">
  The name of the Access Policy this resource belongs to. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  The perimeter etag is internally used to prevent overwriting the list of perimeter resources on PATCH calls. It is retrieved from the same GET perimeter API call that's used to get the current list of resources. The resource to add or remove is merged into that list and then this etag is sent with the PATCH call along with the updated resource list. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
