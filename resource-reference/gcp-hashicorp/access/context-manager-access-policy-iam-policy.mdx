---
title: "google_access_context_manager_access_policy_iam_policy"
description: "Real, generated bindings for `google_access_context_manager_access_policy_iam_policy`."
---

`google_access_context_manager_access_policy_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	access "github.com/ubiquex/ubx-sdk-google/sdk/go/google/access"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager access policy iam policy"})

    		ubx.Resource(access.ContextManagerAccessPolicyIamPolicy, "ci-runner", access.ContextManagerAccessPolicyIamPolicyConfig{
    			Name:       "ci-runner",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerAccessPolicyIamPolicy } from "jsr:@ubx/sdk-google/google/access/context_manager_access_policy_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own context manager access policy iam policy" });

      resource(ContextManagerAccessPolicyIamPolicy, "ci-runner", {
        name: "ci-runner",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerAccessPolicyIamPolicy, ContextManagerAccessPolicyIamPolicyConfig

    def describe():
        ubx.intent("payments own context manager access policy iam policy")

        ubx.resource(ContextManagerAccessPolicyIamPolicy, "ci-runner", ContextManagerAccessPolicyIamPolicyConfig(
            name="ci-runner",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_access_context_manager_access_policy_iam_policy
    called "ci-runner", with policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
