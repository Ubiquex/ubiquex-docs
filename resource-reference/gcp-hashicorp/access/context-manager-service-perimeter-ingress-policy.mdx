---
title: "google_access_context_manager_service_perimeter_ingress_policy"
description: "Real, generated bindings for `google_access_context_manager_service_perimeter_ingress_policy`."
---

`google_access_context_manager_service_perimeter_ingress_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	access "github.com/ubiquex/ubx-sdk-google/sdk/go/google/access"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own context manager service perimeter ingress policy"})

    		ubx.Resource(access.ContextManagerServicePerimeterIngressPolicy, "ci-runner", access.ContextManagerServicePerimeterIngressPolicyConfig{
    			Perimeter: "example",
    			Title:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ContextManagerServicePerimeterIngressPolicy } from "jsr:@ubx/sdk-google/google/access/context_manager_service_perimeter_ingress_policy";

    export default stack("payments", () => {
      intent({
        summary: "payments own context manager service perimeter ingress policy",
      });

      resource(ContextManagerServicePerimeterIngressPolicy, "ci-runner", {
        perimeter: "example",
        title: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.access import ContextManagerServicePerimeterIngressPolicy, ContextManagerServicePerimeterIngressPolicyConfig

    def describe():
        ubx.intent("payments own context manager service perimeter ingress policy")

        ubx.resource(ContextManagerServicePerimeterIngressPolicy, "ci-runner", ContextManagerServicePerimeterIngressPolicyConfig(
            perimeter="example",
            title="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create
    google_access_context_manager_service_perimeter_ingress_policy,
    with perimeter example, title example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ingress_from" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="identities" type="list(string)">
    </ResponseField>
    <ResponseField name="identity_type" type="string">
    </ResponseField>
    <ResponseField name="sources" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="access_level" type="string">
        </ResponseField>
        <ResponseField name="psc_endpoint" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="forwarding_rule" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ingress_to" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="operations" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="method_selectors" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="method" type="string">
            </ResponseField>
            <ResponseField name="permission" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resources" type="list(string)">
    </ResponseField>
    <ResponseField name="roles" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="perimeter" type="string" required>
  The name of the Service Perimeter to add this resource to. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="title" type="string">
  Human readable title. Must be unique within the perimeter. Does not affect behavior. Optional.
</ResponseField>

## Output properties

<ResponseField name="access_policy_id" type="string">
  The name of the Access Policy this resource belongs to. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="etag" type="string">
  The perimeter etag is internally used to prevent overwriting the list of policies on PATCH calls. It is retrieved from the same GET perimeter API call that's used to get the current list of policies. The policy defined in this resource is added or removed from that list, and then this etag is sent with the PATCH call along with the updated policies. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ingress_from" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="identities" type="list(string)">
    </ResponseField>
    <ResponseField name="identity_type" type="string">
    </ResponseField>
    <ResponseField name="sources" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="access_level" type="string">
        </ResponseField>
        <ResponseField name="psc_endpoint" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="forwarding_rule" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="resource" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ingress_to" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="operations" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="method_selectors" type="list(object)">
          <Expandable title="properties">
            <ResponseField name="method" type="string">
            </ResponseField>
            <ResponseField name="permission" type="string">
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="service_name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="resources" type="list(string)">
    </ResponseField>
    <ResponseField name="roles" type="list(string)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
