---
title: "google_apigee_target_server"
description: "Real, generated bindings for `google_apigee_target_server`."
---

`google_apigee_target_server` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target server"})

    		ubx.Resource(apigee.TargetServer, "ci-runner", apigee.TargetServerConfig{
    			EnvId:       "example",
    			Host:        "example",
    			Name:        "ci-runner",
    			Port:        1,
    			Description: "Managed by ubx.",
    			IsEnabled:   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetServer } from "jsr:@ubx/sdk-google/google/apigee/target_server";

    export default stack("payments", () => {
      intent({ summary: "payments own target server" });

      resource(TargetServer, "ci-runner", {
        envId: "example",
        host: "example",
        name: "ci-runner",
        port: 1,
        description: "Managed by ubx.",
        isEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import TargetServer, TargetServerConfig

    def describe():
        ubx.intent("payments own target server")

        ubx.resource(TargetServer, "ci-runner", TargetServerConfig(
            env_id="example",
            host="example",
            name="ci-runner",
            port=1,
            description="Managed by ubx.",
            is_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_target_server called "ci-runner", with env
    id example, host example, port 1, description Managed by ubx., is
    enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable description of this TargetServer. Optional.
</ResponseField>

<ResponseField name="env_id" type="string" required>
  The Apigee environment group associated with the Apigee environment, in the format 'organizations/&#123;&#123;org_name&#125;&#125;/environments/&#123;&#123;env_name&#125;&#125;'. Required.
</ResponseField>

<ResponseField name="host" type="string" required>
  The host name this target connects to. Value must be a valid hostname as described by RFC-1123. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="is_enabled" type="bool">
  Enabling/disabling a TargetServer is useful when TargetServers are used in load balancing configurations, and one or more TargetServers need to taken out of rotation periodically. Defaults to true. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The resource id of this reference. Values must match the regular expression [\w\s-.]+. Required.
</ResponseField>

<ResponseField name="port" type="number" required>
  The port number this target connects to on the given host. Value must be between 1 and 65535, inclusive. Required.
</ResponseField>

<ResponseField name="protocol" type="string">
  Immutable. The protocol used by this TargetServer. Possible values: ["HTTP", "HTTP2", "GRPC_TARGET", "GRPC", "EXTERNAL_CALLOUT"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="s_sl_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ciphers" type="list(string)">
    </ResponseField>
    <ResponseField name="client_auth_enabled" type="bool">
    </ResponseField>
    <ResponseField name="common_name" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="value" type="string">
        </ResponseField>
        <ResponseField name="wildcard_match" type="bool">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="enforce" type="bool">
    </ResponseField>
    <ResponseField name="ignore_validation_errors" type="bool">
    </ResponseField>
    <ResponseField name="key_alias" type="string">
    </ResponseField>
    <ResponseField name="key_store" type="string">
    </ResponseField>
    <ResponseField name="protocols" type="list(string)">
    </ResponseField>
    <ResponseField name="trust_store" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="protocol" type="string">
  Immutable. The protocol used by this TargetServer. Possible values: ["HTTP", "HTTP2", "GRPC_TARGET", "GRPC", "EXTERNAL_CALLOUT"] Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="s_sl_info" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="ciphers" type="list(string)">
    </ResponseField>
    <ResponseField name="client_auth_enabled" type="bool">
    </ResponseField>
    <ResponseField name="common_name" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="value" type="string">
        </ResponseField>
        <ResponseField name="wildcard_match" type="bool">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="bool" required>
    </ResponseField>
    <ResponseField name="enforce" type="bool">
    </ResponseField>
    <ResponseField name="ignore_validation_errors" type="bool">
    </ResponseField>
    <ResponseField name="key_alias" type="string">
    </ResponseField>
    <ResponseField name="key_store" type="string">
    </ResponseField>
    <ResponseField name="protocols" type="list(string)">
    </ResponseField>
    <ResponseField name="trust_store" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
