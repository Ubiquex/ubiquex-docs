---
title: "google_apigee_keystores_aliases_self_signed_cert"
description: "Real, generated bindings for `google_apigee_keystores_aliases_self_signed_cert`."
---

`google_apigee_keystores_aliases_self_signed_cert` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own keystores aliases self signed cert"})

    		ubx.Resource(apigee.KeystoresAliasesSelfSignedCert, "ci-runner", apigee.KeystoresAliasesSelfSignedCertConfig{
    			Alias:              "example",
    			Environment:        "example",
    			Keystore:           "example",
    			OrgId:              "example",
    			SigAlg:             "example",
    			CertValidityInDays: 1,
    			KeySize:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeystoresAliasesSelfSignedCert } from "jsr:@ubx/sdk-google/google/apigee/keystores_aliases_self_signed_cert";

    export default stack("payments", () => {
      intent({ summary: "payments own keystores aliases self signed cert" });

      resource(KeystoresAliasesSelfSignedCert, "ci-runner", {
        alias: "example",
        environment: "example",
        keystore: "example",
        orgId: "example",
        sigAlg: "example",
        certValidityInDays: 1,
        keySize: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import KeystoresAliasesSelfSignedCert, KeystoresAliasesSelfSignedCertConfig

    def describe():
        ubx.intent("payments own keystores aliases self signed cert")

        ubx.resource(KeystoresAliasesSelfSignedCert, "ci-runner", KeystoresAliasesSelfSignedCertConfig(
            alias="example",
            environment="example",
            keystore="example",
            org_id="example",
            sig_alg="example",
            cert_validity_in_days=1,
            key_size="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_keystores_aliases_self_signed_cert, with
    alias example, environment example, keystore example, org id
    example, sig alg example, cert validity in days 1, key size
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alias" type="string" required>
  Alias for the key/certificate pair. Values must match the regular expression [\w\s-.]&#123;1,255&#125;. This must be provided for all formats except selfsignedcert; self-signed certs may specify the alias in either this parameter or the JSON body. Required.
</ResponseField>

<ResponseField name="cert_validity_in_days" type="number">
  Validity duration of certificate, in days. Accepts positive non-zero value. Defaults to 365. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="environment" type="string" required>
  The Apigee environment name Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="key_size" type="string">
  Key size. Default and maximum value is 2048 bits. Optional.
</ResponseField>

<ResponseField name="keystore" type="string" required>
  The Apigee keystore name associated in an Apigee environment Required.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  The Apigee Organization name associated with the Apigee environment Required.
</ResponseField>

<ResponseField name="sig_alg" type="string" required>
  Signature algorithm to generate private key. Valid values are SHA512withRSA, SHA384withRSA, and SHA256withRSA Required.
</ResponseField>

<ResponseField name="subject" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="common_name" type="string">
    </ResponseField>
    <ResponseField name="country_code" type="string">
    </ResponseField>
    <ResponseField name="email" type="string">
    </ResponseField>
    <ResponseField name="locality" type="string">
    </ResponseField>
    <ResponseField name="org" type="string">
    </ResponseField>
    <ResponseField name="org_unit" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subject_alternative_dns_names" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="subject_alternative_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="certs_info" type="list(object)">
  Chain of certificates under this alias. Computed by GCP after `ship`.
  <Expandable title="properties">
    <ResponseField name="cert_info" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="basic_constraints" type="string">
        </ResponseField>
        <ResponseField name="expiry_date" type="string">
        </ResponseField>
        <ResponseField name="is_valid" type="string">
        </ResponseField>
        <ResponseField name="issuer" type="string">
        </ResponseField>
        <ResponseField name="public_key" type="string">
        </ResponseField>
        <ResponseField name="serial_number" type="string">
        </ResponseField>
        <ResponseField name="sig_alg_name" type="string">
        </ResponseField>
        <ResponseField name="subject" type="string">
        </ResponseField>
        <ResponseField name="subject_alternative_names" type="list(string)">
        </ResponseField>
        <ResponseField name="valid_from" type="string">
        </ResponseField>
        <ResponseField name="version" type="number">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="subject" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="common_name" type="string">
    </ResponseField>
    <ResponseField name="country_code" type="string">
    </ResponseField>
    <ResponseField name="email" type="string">
    </ResponseField>
    <ResponseField name="locality" type="string">
    </ResponseField>
    <ResponseField name="org" type="string">
    </ResponseField>
    <ResponseField name="org_unit" type="string">
    </ResponseField>
    <ResponseField name="state" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="subject_alternative_dns_names" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="subject_alternative_name" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string">
  Optional.Type of Alias Computed by GCP after `ship`.
</ResponseField>
