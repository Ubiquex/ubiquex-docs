---
title: "google_apigee_environment_addons_config"
description: "Real, generated bindings for `google_apigee_environment_addons_config`."
---

`google_apigee_environment_addons_config` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment addons config"})

    		ubx.Resource(apigee.EnvironmentAddonsConfig, "ci-runner", apigee.EnvironmentAddonsConfigConfig{
    			EnvId:            "example",
    			AnalyticsEnabled: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentAddonsConfig } from "jsr:@ubx/sdk-google/google/apigee/environment_addons_config";

    export default stack("payments", () => {
      intent({ summary: "payments own environment addons config" });

      resource(EnvironmentAddonsConfig, "ci-runner", {
        envId: "example",
        analyticsEnabled: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EnvironmentAddonsConfig, EnvironmentAddonsConfigConfig

    def describe():
        ubx.intent("payments own environment addons config")

        ubx.resource(EnvironmentAddonsConfig, "ci-runner", EnvironmentAddonsConfigConfig(
            env_id="example",
            analytics_enabled=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_environment_addons_config, with env id
    example, analytics enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="analytics_enabled" type="bool">
  Flag to enable/disable Analytics. Optional.
</ResponseField>

<ResponseField name="env_id" type="string" required>
  The Apigee environment group associated with the Apigee environment, in the format 'organizations/&#123;&#123;org_name&#125;&#125;/environments/&#123;&#123;env_name&#125;&#125;'. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
