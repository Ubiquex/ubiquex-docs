---
title: "google_apigee_environment_iam_policy"
description: "Real, generated bindings for `google_apigee_environment_iam_policy`."
---

`google_apigee_environment_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	apigee "github.com/ubiquex/ubx-sdk-google/sdk/go/google/apigee"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own environment iam policy"})

    		ubx.Resource(apigee.EnvironmentIamPolicy, "ci-runner", apigee.EnvironmentIamPolicyConfig{
    			EnvId:      "example",
    			OrgId:      "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EnvironmentIamPolicy } from "jsr:@ubx/sdk-google/google/apigee/environment_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own environment iam policy" });

      resource(EnvironmentIamPolicy, "ci-runner", {
        envId: "example",
        orgId: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.apigee import EnvironmentIamPolicy, EnvironmentIamPolicyConfig

    def describe():
        ubx.intent("payments own environment iam policy")

        ubx.resource(EnvironmentIamPolicy, "ci-runner", EnvironmentIamPolicyConfig(
            env_id="example",
            org_id="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_apigee_environment_iam_policy, with env id example,
    org id example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="env_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="org_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
