---
title: "google_privateca_certificate_template_iam_policy"
description: "Real, generated bindings for `google_privateca_certificate_template_iam_policy`."
---

`google_privateca_certificate_template_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	privateca "github.com/ubiquex/ubx-sdk-google/sdk/go/google/privateca"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own certificate template iam policy"})

    		ubx.Resource(privateca.CertificateTemplateIamPolicy, "ci-runner", privateca.CertificateTemplateIamPolicyConfig{
    			CertificateTemplate: "example",
    			PolicyData:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CertificateTemplateIamPolicy } from "jsr:@ubx/sdk-google/google/privateca/certificate_template_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own certificate template iam policy" });

      resource(CertificateTemplateIamPolicy, "ci-runner", {
        certificateTemplate: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.privateca import CertificateTemplateIamPolicy, CertificateTemplateIamPolicyConfig

    def describe():
        ubx.intent("payments own certificate template iam policy")

        ubx.resource(CertificateTemplateIamPolicy, "ci-runner", CertificateTemplateIamPolicyConfig(
            certificate_template="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_privateca_certificate_template_iam_policy, with
    certificate template example, policy data example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_template" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
