---
title: "google_project_default_service_accounts"
description: "Real, generated bindings for `google_project_default_service_accounts`."
---

`google_project_default_service_accounts` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	project "github.com/ubiquex/ubx-sdk-google/sdk/go/google/project"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own default service accounts"})

    		ubx.Resource(project.DefaultServiceAccounts, "ci-runner", project.DefaultServiceAccountsConfig{
    			Action:        "example",
    			Project:       "example",
    			RestorePolicy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefaultServiceAccounts } from "jsr:@ubx/sdk-google/google/project/default_service_accounts";

    export default stack("payments", () => {
      intent({ summary: "payments own default service accounts" });

      resource(DefaultServiceAccounts, "ci-runner", {
        action: "example",
        project: "example",
        restorePolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.project import DefaultServiceAccounts, DefaultServiceAccountsConfig

    def describe():
        ubx.intent("payments own default service accounts")

        ubx.resource(DefaultServiceAccounts, "ci-runner", DefaultServiceAccountsConfig(
            action="example",
            project="example",
            restore_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_project_default_service_accounts, with action
    example, project example, restore policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="action" type="string" required>
  The action to be performed in the default service accounts. Valid values are: DEPRIVILEGE, DELETE, DISABLE. Note that DEPRIVILEGE action will ignore the REVERT configuration in the restore_policy. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string" required>
  The project ID where service accounts are created. Required.
</ResponseField>

<ResponseField name="restore_policy" type="string">
  The action to be performed in the default service accounts on the resource destroy. Valid values are NONE, REVERT and REVERT_AND_IGNORE_FAILURE. It is applied for any action but in the DEPRIVILEGE. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="service_accounts" type="map(string)">
  The Service Accounts changed by this resource. It is used for revert the action on the destroy. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
