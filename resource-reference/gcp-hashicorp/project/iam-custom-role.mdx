---
title: "google_project_iam_custom_role"
description: "Real, generated bindings for `google_project_iam_custom_role`."
---

`google_project_iam_custom_role` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	project "github.com/ubiquex/ubx-sdk-google/sdk/go/google/project"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iam custom role"})

    		ubx.Resource(project.IamCustomRole, "ci-runner", project.IamCustomRoleConfig{
    			Permissions: []string{"example"},
    			RoleId:      "example",
    			Title:       "example",
    			Description: "Managed by ubx.",
    			Stage:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IamCustomRole } from "jsr:@ubx/sdk-google/google/project/iam_custom_role";

    export default stack("payments", () => {
      intent({ summary: "payments own iam custom role" });

      resource(IamCustomRole, "ci-runner", {
        permissions: ["example"],
        roleId: "example",
        title: "example",
        description: "Managed by ubx.",
        stage: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.project import IamCustomRole, IamCustomRoleConfig

    def describe():
        ubx.intent("payments own iam custom role")

        ubx.resource(IamCustomRole, "ci-runner", IamCustomRoleConfig(
            permissions=["example"],
            role_id="example",
            title="example",
            description="Managed by ubx.",
            stage="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_project_iam_custom_role, with permissions a set
    value, role id example, title example, description Managed by
    ubx., stage example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  A human-readable description for the role. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="permissions" type="set(string)" required>
  The names of the permissions this role grants when bound in an IAM policy. At least one permission must be specified. Required.
</ResponseField>

<ResponseField name="project" type="string">
  The project that the service account will be created in. Defaults to the provider project configuration. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_id" type="string" required>
  The camel case role id to use for this role. Cannot contain - characters. Required.
</ResponseField>

<ResponseField name="stage" type="string">
  The current launch stage of the role. Defaults to GA. Optional.
</ResponseField>

<ResponseField name="title" type="string" required>
  A human-readable title for the role. Required.
</ResponseField>

## Output properties

<ResponseField name="deleted" type="bool">
  The current deleted state of the role. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the role in the format projects/&#123;&#123;project&#125;&#125;/roles/&#123;&#123;role_id&#125;&#125;. Like id, this field can be used as a reference in other resources such as IAM role bindings. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  The project that the service account will be created in. Defaults to the provider project configuration. Optional; if omitted, computed by GCP.
</ResponseField>
