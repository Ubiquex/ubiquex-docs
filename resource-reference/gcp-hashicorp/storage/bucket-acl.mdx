---
title: "google_storage_bucket_acl"
description: "Real, generated bindings for `google_storage_bucket_acl`."
---

`google_storage_bucket_acl` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket acl"})

    		ubx.Resource(storage.BucketAcl, "ci-runner", storage.BucketAclConfig{
    			Bucket:        "example",
    			DefaultAcl:    "example",
    			PredefinedAcl: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketAcl } from "jsr:@ubx/sdk-google/google/storage/bucket_acl";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket acl" });

      resource(BucketAcl, "ci-runner", {
        bucket: "example",
        defaultAcl: "example",
        predefinedAcl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import BucketAcl, BucketAclConfig

    def describe():
        ubx.intent("payments own bucket acl")

        ubx.resource(BucketAcl, "ci-runner", BucketAclConfig(
            bucket="example",
            default_acl="example",
            predefined_acl="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_bucket_acl, with bucket example, default
    acl example, predefined acl example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  The name of the bucket it applies to. Required.
</ResponseField>

<ResponseField name="default_acl" type="string">
  Configure this ACL to be the default ACL. Optional.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="predefined_acl" type="string">
  The canned GCS ACL to apply. Must be set if role_entity is not. Optional.
</ResponseField>

<ResponseField name="role_entity" type="list(string)">
  List of role/entity pairs in the form ROLE:entity. See GCS Bucket ACL documentation for more details. Must be set if predefined_acl is not. Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="role_entity" type="list(string)">
  List of role/entity pairs in the form ROLE:entity. See GCS Bucket ACL documentation for more details. Must be set if predefined_acl is not. Optional; if omitted, computed by GCP.
</ResponseField>
