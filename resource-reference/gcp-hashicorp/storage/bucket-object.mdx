---
title: "google_storage_bucket_object"
description: "Real, generated bindings for `google_storage_bucket_object`."
---

`google_storage_bucket_object` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket object"})

    		ubx.Resource(storage.BucketObject, "ci-runner", storage.BucketObjectConfig{
    			Bucket:             "example",
    			Name:               "ci-runner",
    			CacheControl:       "example",
    			ContentDisposition: "example",
    			ContentEncoding:    "example",
    			ContentLanguage:    "example",
    			DetectMd5hash:      "example",
    			EventBasedHold:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketObject } from "jsr:@ubx/sdk-google/google/storage/bucket_object";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket object" });

      resource(BucketObject, "ci-runner", {
        bucket: "example",
        name: "ci-runner",
        cacheControl: "example",
        contentDisposition: "example",
        contentEncoding: "example",
        contentLanguage: "example",
        detectMd5hash: "example",
        eventBasedHold: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import BucketObject, BucketObjectConfig

    def describe():
        ubx.intent("payments own bucket object")

        ubx.resource(BucketObject, "ci-runner", BucketObjectConfig(
            bucket="example",
            name="ci-runner",
            cache_control="example",
            content_disposition="example",
            content_encoding="example",
            content_language="example",
            detect_md5hash="example",
            event_based_hold=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_bucket_object called "ci-runner", with
    bucket example, cache control example, content disposition
    example, content encoding example, content language example,
    detect md5hash example, event based hold true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket" type="string" required>
  The name of the containing bucket. Required.
</ResponseField>

<ResponseField name="cache_control" type="string">
  Cache-Control directive to specify caching behavior of object data. If omitted and object is accessible to all anonymous users, the default will be public, max-age=3600 Optional.
</ResponseField>

<ResponseField name="content" type="string">
  Data as string to be uploaded. Must be defined if source is not. Note: The content field is marked as sensitive. To view the raw contents of the object, please define an output. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="content_disposition" type="string">
  Content-Disposition of the object data. Optional.
</ResponseField>

<ResponseField name="content_encoding" type="string">
  Content-Encoding of the object data. Optional.
</ResponseField>

<ResponseField name="content_language" type="string">
  Content-Language of the object data. Optional.
</ResponseField>

<ResponseField name="content_type" type="string">
  Content-Type of the object data. Defaults to "application/octet-stream" or "text/plain; charset=utf-8". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="contexts" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="custom" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="create_time" type="string">
        </ResponseField>
        <ResponseField name="key" type="string" required>
        </ResponseField>
        <ResponseField name="update_time" type="string">
        </ResponseField>
        <ResponseField name="value" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="customer_encryption" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="encryption_algorithm" type="string">
    </ResponseField>
    <ResponseField name="encryption_key" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="detect_md5hash" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_based_hold" type="bool">
  Whether an object is under event-based hold. Event-based hold is a way to retain objects until an event occurs, which is signified by the hold's release (i.e. this value is set to false). After being released (set to false), such objects will be subject to bucket-level retention (if any). Optional.
</ResponseField>

<ResponseField name="force_empty_content_type" type="bool">
  Flag to set empty Content-Type. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  Resource name of the Cloud KMS key that will be used to encrypt the object. Overrides the object metadata's kmsKeyName value, if any. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="metadata" type="map(string)">
  User-provided metadata, in key/value pairs. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the object. If you're interpolating the name of this object, see output_name instead. Required.
</ResponseField>

<ResponseField name="retention" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="mode" type="string" required>
    </ResponseField>
    <ResponseField name="retain_until_time" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source" type="string">
  A path to the data you want to upload. Must be defined if content is not. Optional.
</ResponseField>

<ResponseField name="source_md5hash" type="string">
  User-provided md5hash, Base 64 MD5 hash of the object data. Optional.
</ResponseField>

<ResponseField name="storage_class" type="string">
  The StorageClass of the new bucket object. Supported values include: MULTI_REGIONAL, REGIONAL, NEARLINE, COLDLINE, ARCHIVE. If not provided, this defaults to the bucket's default storage class or to a standard class. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="temporary_hold" type="bool">
  Whether an object is under temporary hold. While this flag is set to true, the object is protected against deletion and overwrites. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="content" type="string">
  Data as string to be uploaded. Must be defined if source is not. Note: The content field is marked as sensitive. To view the raw contents of the object, please define an output. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="content_type" type="string">
  Content-Type of the object data. Defaults to "application/octet-stream" or "text/plain; charset=utf-8". Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="contexts" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="custom" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="create_time" type="string">
        </ResponseField>
        <ResponseField name="key" type="string" required>
        </ResponseField>
        <ResponseField name="update_time" type="string">
        </ResponseField>
        <ResponseField name="value" type="string" required>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="crc32c" type="string">
  Base 64 CRC32 hash of the uploaded data. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="customer_encryption" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="encryption_algorithm" type="string">
    </ResponseField>
    <ResponseField name="encryption_key" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="generation" type="number">
  The content generation of this object. Used for object versioning and soft delete. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kms_key_name" type="string">
  Resource name of the Cloud KMS key that will be used to encrypt the object. Overrides the object metadata's kmsKeyName value, if any. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="md5hash" type="string">
  Base 64 MD5 hash of the uploaded data. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="md5hexhash" type="string">
  Hex value of md5hash Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="media_link" type="string">
  A url reference to download this object. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="output_name" type="string">
  The name of the object. Use this field in interpolations with google_storage_object_acl to recreate google_storage_object_acl resources when your google_storage_bucket_object is recreated. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="retention" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="mode" type="string" required>
    </ResponseField>
    <ResponseField name="retain_until_time" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="self_link" type="string">
  A url reference to this object. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="storage_class" type="string">
  The StorageClass of the new bucket object. Supported values include: MULTI_REGIONAL, REGIONAL, NEARLINE, COLDLINE, ARCHIVE. If not provided, this defaults to the bucket's default storage class or to a standard class. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
