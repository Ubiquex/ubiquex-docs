---
title: "google_storage_anywhere_cache"
description: "Real, generated bindings for `google_storage_anywhere_cache`."
---

`google_storage_anywhere_cache` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	storage "github.com/ubiquex/ubx-sdk-google/sdk/go/google/storage"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own anywhere cache"})

    		ubx.Resource(storage.AnywhereCache, "ci-runner", storage.AnywhereCacheConfig{
    			Bucket:          "example",
    			Zone:            "example",
    			AdmissionPolicy: "example",
    			IngestOnWrite:   true,
    			Ttl:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AnywhereCache } from "jsr:@ubx/sdk-google/google/storage/anywhere_cache";

    export default stack("payments", () => {
      intent({ summary: "payments own anywhere cache" });

      resource(AnywhereCache, "ci-runner", {
        bucket: "example",
        zone: "example",
        admissionPolicy: "example",
        ingestOnWrite: true,
        ttl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.storage import AnywhereCache, AnywhereCacheConfig

    def describe():
        ubx.intent("payments own anywhere cache")

        ubx.resource(AnywhereCache, "ci-runner", AnywhereCacheConfig(
            bucket="example",
            zone="example",
            admission_policy="example",
            ingest_on_write=True,
            ttl="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_storage_anywhere_cache, with bucket example, zone
    example, admission policy example, ingest on write true, ttl
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="admission_policy" type="string">
  The cache admission policy dictates whether a block should be inserted upon a cache miss. Note: "admit-on-second-miss" is deprecated and will fallback to "admit-on-first-miss". Default value: "admit-on-first-miss" Possible values: ["admit-on-first-miss", "admit-on-second-miss"] Optional.
</ResponseField>

<ResponseField name="bucket" type="string" required>
  A reference to Bucket resource Required.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ingest_on_write" type="bool">
  Whether or not the cache ingests data as the data is written to the bucket. Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="ttl" type="string">
  The TTL of all cache entries in whole seconds. e.g., "7200s". It defaults to '86400s' Optional.
</ResponseField>

<ResponseField name="zone" type="string" required>
  The zone in which the cache instance needs to be created. For example, 'us-central1-a.' Required.
</ResponseField>

## Output properties

<ResponseField name="anywhere_cache_id" type="string">
  The ID of the Anywhere cache instance. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="create_time" type="string">
  The creation time of the cache instance in RFC 3339 format. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="pending_update" type="bool">
  True if the cache instance has an active Update long-running operation. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  The current state of the cache instance. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="update_time" type="string">
  The modification time of the cache instance metadata in RFC 3339 format. Computed by GCP after `ship`.
</ResponseField>
