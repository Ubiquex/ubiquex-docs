---
title: "google_bigtable_app_profile"
description: "Real, generated bindings for `google_bigtable_app_profile`."
---

`google_bigtable_app_profile` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	bigtable "github.com/ubiquex/ubx-sdk-google/sdk/go/google/bigtable"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app profile"})

    		ubx.Resource(bigtable.AppProfile, "ci-runner", bigtable.AppProfileConfig{
    			AppProfileId:                  "example",
    			Description:                   "Managed by ubx.",
    			IgnoreWarnings:                true,
    			Instance:                      "example",
    			MultiClusterRoutingClusterIds: []string{"example"},
    			MultiClusterRoutingUseAny:     true,
    			RowAffinity:                   true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppProfile } from "jsr:@ubx/sdk-google/google/bigtable/app_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own app profile" });

      resource(AppProfile, "ci-runner", {
        appProfileId: "example",
        description: "Managed by ubx.",
        ignoreWarnings: true,
        instance: "example",
        multiClusterRoutingClusterIds: ["example"],
        multiClusterRoutingUseAny: true,
        rowAffinity: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.bigtable import AppProfile, AppProfileConfig

    def describe():
        ubx.intent("payments own app profile")

        ubx.resource(AppProfile, "ci-runner", AppProfileConfig(
            app_profile_id="example",
            description="Managed by ubx.",
            ignore_warnings=True,
            instance="example",
            multi_cluster_routing_cluster_ids=["example"],
            multi_cluster_routing_use_any=True,
            row_affinity=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_bigtable_app_profile, with app profile id example,
    description Managed by ubx., ignore warnings true, instance
    example, multi cluster routing cluster ids a set value, multi
    cluster routing use any true, row affinity true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_profile_id" type="string" required>
  The unique name of the app profile in the form '[_a-zA-Z0-9][-_.a-zA-Z0-9]*'. Required.
</ResponseField>

<ResponseField name="data_boost_isolation_read_only" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="compute_billing_owner" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="description" type="string">
  Long form description of the use case for this app profile. Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="ignore_warnings" type="bool">
  If true, ignore safety checks when deleting/updating the app profile. Optional.
</ResponseField>

<ResponseField name="instance" type="string">
  The name of the instance to create the app profile within. Optional.
</ResponseField>

<ResponseField name="multi_cluster_routing_cluster_ids" type="list(string)">
  The set of clusters to route to. The order is ignored; clusters will be tried in order of distance. If left empty, all clusters are eligible. Optional.
</ResponseField>

<ResponseField name="multi_cluster_routing_use_any" type="bool">
  If true, read/write requests are routed to the nearest cluster in the instance, and will fail over to the nearest cluster that is available in the event of transient errors or delays. Clusters in a region are considered equidistant. Choosing this option sacrifices read-your-writes consistency to improve availability. Optional.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="row_affinity" type="bool">
  Must be used with multi-cluster routing. If true, then this app profile will use row affinity sticky routing. With row affinity, Bigtable will route single row key requests based on the row key, rather than randomly. Instead, each row key will be assigned to a cluster by Cloud Bigtable, and will stick to that cluster. Choosing this option improves read-your-writes consistency for most requests under most circumstances, without sacrificing availability. Consistency is not guaranteed, as requests may still fail over between clusters in the event of errors or latency. Optional.
</ResponseField>

<ResponseField name="single_cluster_routing" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_transactional_writes" type="bool">
    </ResponseField>
    <ResponseField name="cluster_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="standard_isolation" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="priority" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="data_boost_isolation_read_only" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="compute_billing_owner" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="deletion_policy" type="string">
  Whether Terraform will be prevented from destroying the instance. Defaults to "DELETE". When a 'terraform destroy' or 'terraform apply' would delete the instance, the command will fail if this field is set to "PREVENT" in Terraform state. When set to "ABANDON", the command will remove the resource from Terraform management without updating or deleting the resource in the API. When set to "DELETE", deleting the resource is allowed. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The unique name of the requested app profile. Values are of the form 'projects/&lt;project&gt;/instances/&lt;instance&gt;/appProfiles/&lt;appProfileId&gt;'. Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="single_cluster_routing" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="allow_transactional_writes" type="bool">
    </ResponseField>
    <ResponseField name="cluster_id" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="standard_isolation" type="list(object)">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="priority" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
