---
title: "google_dataproc_autoscaling_policy_iam_policy"
description: "Real, generated bindings for `google_dataproc_autoscaling_policy_iam_policy`."
---

`google_dataproc_autoscaling_policy_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	dataproc "github.com/ubiquex/ubx-sdk-google/sdk/go/google/dataproc"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own autoscaling policy iam policy"})

    		ubx.Resource(dataproc.AutoscalingPolicyIamPolicy, "ci-runner", dataproc.AutoscalingPolicyIamPolicyConfig{
    			PolicyData: "example",
    			PolicyId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AutoscalingPolicyIamPolicy } from "jsr:@ubx/sdk-google/google/dataproc/autoscaling_policy_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own autoscaling policy iam policy" });

      resource(AutoscalingPolicyIamPolicy, "ci-runner", {
        policyData: "example",
        policyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.dataproc import AutoscalingPolicyIamPolicy, AutoscalingPolicyIamPolicyConfig

    def describe():
        ubx.intent("payments own autoscaling policy iam policy")

        ubx.resource(AutoscalingPolicyIamPolicy, "ci-runner", AutoscalingPolicyIamPolicyConfig(
            policy_data="example",
            policy_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_dataproc_autoscaling_policy_iam_policy, with policy
    data example, policy id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
