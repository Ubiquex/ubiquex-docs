---
title: "google_biglake_iceberg_table_iam_policy"
description: "Real, generated bindings for `google_biglake_iceberg_table_iam_policy`."
---

`google_biglake_iceberg_table_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	biglake "github.com/ubiquex/ubx-sdk-google/sdk/go/google/biglake"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own iceberg table iam policy"})

    		ubx.Resource(biglake.IcebergTableIamPolicy, "ci-runner", biglake.IcebergTableIamPolicyConfig{
    			Catalog:    "example",
    			Name:       "ci-runner",
    			Namespace:  "example",
    			PolicyData: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IcebergTableIamPolicy } from "jsr:@ubx/sdk-google/google/biglake/iceberg_table_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own iceberg table iam policy" });

      resource(IcebergTableIamPolicy, "ci-runner", {
        catalog: "example",
        name: "ci-runner",
        namespace: "example",
        policyData: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.biglake import IcebergTableIamPolicy, IcebergTableIamPolicyConfig

    def describe():
        ubx.intent("payments own iceberg table iam policy")

        ubx.resource(IcebergTableIamPolicy, "ci-runner", IcebergTableIamPolicyConfig(
            catalog="example",
            name="ci-runner",
            namespace="example",
            policy_data="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_biglake_iceberg_table_iam_policy called
    "ci-runner", with catalog example, namespace example, policy data
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="catalog" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="namespace" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
