---
title: "google_secure_source_manager_repository_iam_policy"
description: "Real, generated bindings for `google_secure_source_manager_repository_iam_policy`."
---

`google_secure_source_manager_repository_iam_policy` -- real, typed bindings generated directly from the
`hashicorp/google` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	secure "github.com/ubiquex/ubx-sdk-google/sdk/go/google/secure"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own source manager repository iam policy"})

    		ubx.Resource(secure.SourceManagerRepositoryIamPolicy, "ci-runner", secure.SourceManagerRepositoryIamPolicyConfig{
    			PolicyData:   "example",
    			RepositoryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SourceManagerRepositoryIamPolicy } from "jsr:@ubx/sdk-google/google/secure/source_manager_repository_iam_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own source manager repository iam policy" });

      resource(SourceManagerRepositoryIamPolicy, "ci-runner", {
        policyData: "example",
        repositoryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.secure import SourceManagerRepositoryIamPolicy, SourceManagerRepositoryIamPolicyConfig

    def describe():
        ubx.intent("payments own source manager repository iam policy")

        ubx.resource(SourceManagerRepositoryIamPolicy, "ci-runner", SourceManagerRepositoryIamPolicyConfig(
            policy_data="example",
            repository_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_secure_source_manager_repository_iam_policy, with
    policy data example, repository id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="policy_data" type="string" required>
  Required.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="repository_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="etag" type="string">
  Computed by GCP after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="location" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="project" type="string">
  Optional; if omitted, computed by GCP.
</ResponseField>
