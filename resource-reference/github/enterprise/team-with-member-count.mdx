---
title: "github_enterprise_team_with_member_count"
description: "`github_enterprise_team_with_member_count` is a team defined once at the enterprise level and assignable across up to 15,000 organizations within..."
---

`github_enterprise_team_with_member_count` is a team defined once at the enterprise level and assignable across up to 15,000 organizations within it, rather than being...

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	enterprise "github.com/ubiquex/ubx-sdk-github/sdk/go/github/enterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own team with member count"})

    		ubx.Resource(enterprise.TeamWithMemberCount, "example", enterprise.TeamWithMemberCountConfig{
    			Enterprise: "example",
    			Name:       "example",
    			TeamSlug:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TeamWithMemberCount } from "@ubx/sdk-github/github/enterprise/team_with_member_count";

    export default stack("example", () => {
      intent({ summary: "example own team with member count" });

      resource(TeamWithMemberCount, "example", {
        enterprise: "example",
        name: "example",
        teamSlug: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.enterprise import TeamWithMemberCount, TeamWithMemberCountConfig

    def describe():
        ubx.intent("example own team with member count")

        ubx.resource(TeamWithMemberCount, "example", TeamWithMemberCountConfig(
            enterprise="example",
            name="example",
            team_slug="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_enterprise_team_with_member_count called "example",
    with enterprise example, team slug example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  A description of the team. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="group_id" type="string">
  The ID of the IdP group to assign team membership with. You can get this value from the [REST API endpoints for SCIM](https://docs.github.com/enterprise-cloud@latest/rest/scim#list-provisioned-scim-groups-for-an-enterprise). Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the team. Required.
</ResponseField>

<ResponseField name="notification_setting" type="string">
  The notification setting the team is set to. The options are: * `notifications_enabled` - team members receive notifications when the team is @mentioned. * `notifications_disabled` - no one receives notifications. Default: `notifications_enabled` Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="organization_selection_type" type="string">
  Specifies which organizations in the enterprise should have access to this team. Can be one of `disabled`, `selected`, or `all`. `disabled`: The team is not assigned to any organizations. This is the default when you create a new team. `selected`: The team is assigned to specific organizations. You can then use the [add organization assignments API](https://docs.github.com/enterprise-cloud@latest/rest/enterprise-teams/enterprise-team-organizations#add-organization-assignments) endpoint. `all`: The team is assigned to all current and future organizations in the enterprise. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="sync_to_organizations" type="string">
  Retired: this field is no longer supported. Whether the enterprise team should be reflected in each organization. This value cannot be set. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="team_slug" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  A description of the team. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="group_id" type="string">
  The ID of the IdP group to assign team membership with. You can get this value from the [REST API endpoints for SCIM](https://docs.github.com/enterprise-cloud@latest/rest/scim#list-provisioned-scim-groups-for-an-enterprise). Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="group_name" type="string">
  Retired: this field will not be returned with GHEC enterprise teams. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="html_url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="members_count" type="number">
  The number of members in the enterprise team. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="members_url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="notification_setting" type="string">
  The notification setting the team is set to. The options are: * `notifications_enabled` - team members receive notifications when the team is @mentioned. * `notifications_disabled` - no one receives notifications. Default: `notifications_enabled` Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="organization_selection_type" type="string">
  Specifies which organizations in the enterprise should have access to this team. Can be one of `disabled`, `selected`, or `all`. `disabled`: The team is not assigned to any organizations. This is the default when you create a new team. `selected`: The team is assigned to specific organizations. You can then use the [add organization assignments API](https://docs.github.com/enterprise-cloud@latest/rest/enterprise-teams/enterprise-team-organizations#add-organization-assignments) endpoint. `all`: The team is assigned to all current and future organizations in the enterprise. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="slug" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="sync_to_organizations" type="string">
  Retired: this field is no longer supported. Whether the enterprise team should be reflected in each organization. This value cannot be set. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>
