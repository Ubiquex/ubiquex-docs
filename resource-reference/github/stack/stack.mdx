---
title: "github_stack"
description: "`github_stack` records where one pull request sits inside a real stack -- a chain of smaller, dependent pull requests..."
---

`github_stack` records where one pull request sits inside a real stack -- a chain of smaller, dependent pull requests, each showing only its own layer's diff instead of one large change. Stacks merge from the bottom up, and any contiguous run of pull requests starting from the lowest unmerged one can be merged together at once.

Real, typed bindings generated directly from the real GitHub REST API OpenAPI specification, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	stack "github.com/ubiquex/ubx-sdk-github/sdk/go/github/stack"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own stack"})

    		ubx.Resource(stack.Stack, "ci-runner", stack.StackConfig{
    			Owner:        "example",
    			PullRequests: []string{"example"},
    			Repo:         "example",
    			StackNumber:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Stack } from "./local-sdk/github/sdk/typescript/github/stack/stack.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own stack" });

      resource(Stack, "ci-runner", {
        owner: "example",
        pullRequests: ["example"],
        repo: "example",
        stackNumber: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.stack import Stack, StackConfig

    def describe():
        ubx.intent("payments own stack")

        ubx.resource(Stack, "ci-runner", StackConfig(
            owner="example",
            pull_requests=["example"],
            repo="example",
            stack_number="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_stack, with owner example, pull requests a set
    value, repo example, stack number example.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="pull_requests" type="list(number)" required>
  An ordered list of pull request numbers forming the stack from bottom to top. Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="stack_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="base" type="object">
  Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="ref" type="string" required>
      The git reference (branch, tag, or commit SHA) to use as the base for the stack. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="node_id" type="string">
  The GraphQL node ID of the GitHub stack. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="number" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="open" type="bool">
  Whether the stack has any open pull request. False when all pull requests are merged or closed. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>
