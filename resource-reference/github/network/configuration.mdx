---
title: "github_network_configuration"
description: "`github_network_configuration` is the real, organization- or enterprise-level construct that governs private networking for GitHub-hosted compute..."
---

`github_network_configuration` is the real, organization- or enterprise-level construct that governs private networking for GitHub-hosted compute -- controlling outbound access, routing runner traffic through a private Azure VNet, and optionally a real failover subnet if the primary one becomes unavailable.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-github/sdk/go/github/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own configuration"})

    		ubx.Resource(network.Configuration, "ci-runner", network.ConfigurationConfig{
    			Name:                   "ci-runner",
    			NetworkConfigurationId: "example",
    			NetworkSettingsIds:     []string{"example"},
    			Org:                    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Configuration } from "./local-sdk/github/sdk/typescript/github/network/configuration.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own configuration" });

      resource(Configuration, "ci-runner", {
        name: "ci-runner",
        networkConfigurationId: "example",
        networkSettingsIds: ["example"],
        org: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.network import Configuration, ConfigurationConfig

    def describe():
        ubx.intent("payments own configuration")

        ubx.resource(Configuration, "ci-runner", ConfigurationConfig(
            name="ci-runner",
            network_configuration_id="example",
            network_settings_ids=["example"],
            org="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_network_configuration called "ci-runner", with
    network configuration id example, network settings ids a set
    value, org example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="compute_service" type="string">
  The hosted compute service to use for the network configuration. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="failover_network_enabled" type="bool">
  Indicates whether the failover network resource is enabled. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="failover_network_settings_ids" type="list(string)">
  A list of identifiers of the failover network settings resources to use for the network configuration. Exactly one resource identifier must be specified in the list. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the network configuration. Must be between 1 and 100 characters and may only contain upper and lowercase letters a-z, numbers 0-9, '.', '-', and '_'. Required.
</ResponseField>

<ResponseField name="network_configuration_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="network_settings_ids" type="list(string)" required>
  A list of identifiers of the network settings resources to use for the network configuration. Exactly one resource identifier must be specified in the list. Required.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="compute_service" type="string">
  The hosted compute service to use for the network configuration. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="created_on" type="string">
  The time at which the network configuration was created, in ISO 8601 format. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="failover_network_enabled" type="bool">
  Indicates whether the failover network resource is enabled. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="failover_network_settings_ids" type="list(string)">
  A list of identifiers of the failover network settings resources to use for the network configuration. Exactly one resource identifier must be specified in the list. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="id" type="string">
  The unique identifier of the network configuration. Computed by GitHub after `ship`.
</ResponseField>
