---
title: "github_blob"
description: "`github_blob` is a raw git object holding one file's own content at a specific point in history..."
---

`github_blob` is a raw git object holding one file's own content at a specific point in history, addressed by its own SHA rather than a filename or path -- the lowest-level building block a git tree assembles into a real directory structure.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	blob "github.com/ubiquex/ubx-sdk-github/sdk/go/github/blob"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own blob"})

    		ubx.Resource(blob.Blob, "ci-runner", blob.BlobConfig{
    			Content: "example",
    			FileSha: "example",
    			Owner:   "example",
    			Repo:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Blob } from "./local-sdk/github/sdk/typescript/github/blob/blob.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own blob" });

      resource(Blob, "ci-runner", {
        content: "example",
        fileSha: "example",
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.blob import Blob, BlobConfig

    def describe():
        ubx.intent("payments own blob")

        ubx.resource(Blob, "ci-runner", BlobConfig(
            content="example",
            file_sha="example",
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_blob, with content example, file sha example, owner
    example, repo example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="content" type="string" required>
  The new blob's content. Required.
</ResponseField>

<ResponseField name="encoding" type="string">
  The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="file_sha" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="encoding" type="string">
  The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="highlighted_content" type="string">
  The blob's content rendered with syntax highlighting, when requested. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="node_id" type="string">
  The blob's GraphQL node ID. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="sha" type="string">
  The SHA-1 hash identifying this blob's content. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="size" type="number">
  The size of the blob's content, in bytes. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  The API URL for retrieving this blob. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
