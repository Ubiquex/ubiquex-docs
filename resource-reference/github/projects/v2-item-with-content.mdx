---
title: "github_projects_v2_item_with_content"
description: "A complete, runnable github_projects_v2_item_with_content program, in every SDK language."
---

`github_projects_v2_item_with_content` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	projects "github.com/ubiquex/ubx-sdk-github/sdk/go/github/projects"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own v2 item with content"})

    		ubx.Resource(projects.V2ItemWithContent, "ci-runner", projects.V2ItemWithContentConfig{
    			ItemId:        "example",
    			Org:           "example",
    			ProjectNumber: "example",
    			Type:          "example",
    			Number:        1,
    			Owner:         "example",
    			Repo:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { V2ItemWithContent } from "./local-sdk/github/sdk/typescript/github/projects/v2_item_with_content.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own v2 item with content" });

      resource(V2ItemWithContent, "ci-runner", {
        itemId: "example",
        org: "example",
        projectNumber: "example",
        type: "example",
        number: 1,
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.projects import V2ItemWithContent, V2ItemWithContentConfig

    def describe():
        ubx.intent("payments own v2 item with content")

        ubx.resource(V2ItemWithContent, "ci-runner", V2ItemWithContentConfig(
            item_id="example",
            org="example",
            project_number="example",
            type="example",
            number=1,
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_projects_v2_item_with_content, with item id
    example, org example, project number example, type example,
    number 1, owner example, repo example.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="id" type="number">
  The unique identifier of the issue or pull request to add to the project. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="item_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="number" type="number">
  The issue or pull request number. Optional.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="owner" type="string">
  The repository owner login. Optional.
</ResponseField>

<ResponseField name="project_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string">
  The repository name. Optional.
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of item to add to the project. Must be either Issue or PullRequest. Required.
</ResponseField>

## Output properties

<ResponseField name="archived_at" type="string">
  The time when the item was archived. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="content" type="any">
  The content of the item, which varies by content type. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="content_type" type="string">
  The type of content tracked in a project item Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The time when the item was created. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="creator" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the avatar image for the user who created the item. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the creator of the project item, if available. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to retrieve events for this creator user via the GitHub REST API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The URL to the follower list of the user (the creator of the project item). This is a standard GitHub API field, typically formatted as a URL template. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL template for the list of users that the creator follows, with a placeholder for the target user if checking a specific follow relationship. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL to the user's gists list, optionally with a gist ID to access a specific gist. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The Gravatar identifier for the user, used to construct an avatar image URL. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The HTML URL of the creator's GitHub profile page. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The unique identifier of the user who created the Projects v2 item. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the creator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the creator (user or bot) of the project item. This field is optional. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The node_id of the creator, a unique identifier assigned by GitHub for use with the GraphQL API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The API URL to list the organizations that the creator belongs to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the GitHub API endpoint that returns the events received by the creator (the user) of the Projects v2 item. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the user's repositories. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Whether the creator is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template for listing repositories starred by this user. Includes `&#123;owner&#125;` and `&#123;repo&#125;` placeholders for accessing a specific starred repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The URL to retrieve the subscriptions of the creator (user), typically in the format https://api.github.com/users/&#123;username&#125;/subscriptions. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Required.
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The GitHub profile URL of the creator of the project v2 item. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      The user's membership type in the organization, either 'public' or 'private'. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fields" type="list(any)">
  The fields and values associated with this item. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  The unique identifier of the issue or pull request to add to the project. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="item_url" type="string">
  The API URL of this item. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="node_id" type="string">
  The node ID of the project item. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="project_url" type="string">
  The API URL of the project that contains this item. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The time when the item was last updated. Computed by GitHub after `ship`.
</ResponseField>
