---
title: "github_copilot_space_resource"
description: "`github_copilot_space_resource` is one real piece of content -- a repository, a file, a pull request, a note -- added into a Copilot Space..."
---

`github_copilot_space_resource` is one real piece of content -- a repository, a file, a pull request, a note -- added into a Copilot Space, the individual unit a space's own context is actually built from.

Real, typed bindings generated directly from the real GitHub REST API OpenAPI specification, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	copilot "github.com/ubiquex/ubx-sdk-github/sdk/go/github/copilot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own space resource"})

    		ubx.Resource(copilot.SpaceResource, "ci-runner", copilot.SpaceResourceConfig{
    			Metadata:        "example",
    			Org:             "example",
    			ResourceType:    "example",
    			SpaceNumber:     "example",
    			SpaceResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { SpaceResource } from "./local-sdk/github/sdk/typescript/github/copilot/space_resource.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own space resource" });

      resource(SpaceResource, "ci-runner", {
        metadata: "example",
        org: "example",
        resourceType: "example",
        spaceNumber: "example",
        spaceResourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.copilot import SpaceResource, SpaceResourceConfig

    def describe():
        ubx.intent("payments own space resource")

        ubx.resource(SpaceResource, "ci-runner", SpaceResourceConfig(
            metadata="example",
            org="example",
            resource_type="example",
            space_number="example",
            space_resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_copilot_space_resource, with metadata example, org
    example, resource type example, space number example, space
    resource id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="metadata" type="any" required>
  Resource-specific metadata. Required.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  The type of resource to create. Required.
</ResponseField>

<ResponseField name="space_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="space_resource_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="copilot_chat_attachment_id" type="number">
  The ID of the associated chat attachment, if any. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The date and time the resource was created. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  The unique identifier of the resource. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time the resource was last updated. Computed by GitHub after `ship`.
</ResponseField>
