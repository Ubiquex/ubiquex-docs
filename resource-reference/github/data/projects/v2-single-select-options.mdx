---
title: "github_projects_v2_single_select_options"
description: "`github_projects_v2_single_select_options` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_projects_v2_single_select_options` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	projects "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/projects"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_projects_v2_single_select_options"})

    		ubx.Data(projects.V2SingleSelectOptions, "example", projects.V2SingleSelectOptionsConfig{
    			FieldId:       1,
    			ProjectNumber: 1,
    			Username:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V2SingleSelectOptions } from "@ubx/sdk-github/github/data/projects/v2_single_select_options";

    export default stack("example", () => {
      intent({ summary: "look up github_projects_v2_single_select_options" });

      data(V2SingleSelectOptions, "example", {
        fieldId: 1,
        projectNumber: 1,
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.projects import V2SingleSelectOptions, V2SingleSelectOptionsConfig

    def describe():
        ubx.intent("look up github_projects_v2_single_select_options")

        ubx.data(V2SingleSelectOptions, "example", V2SingleSelectOptionsConfig(
            field_id=1,
            project_number=1,
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="field_id" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="project_number" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="username" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="configuration" type="object">
  Configuration for iteration fields. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="duration" type="number">
      The duration of the iteration in days. Optional lookup argument.
    </ResponseField>
    <ResponseField name="iterations" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="start_day" type="number">
      The day of the week when the iteration starts. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_at" type="string">
  The time when the field was created. Returned by the lookup.
</ResponseField>

<ResponseField name="data_type" type="string">
  The field's data type. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="number">
  The unique identifier of the field. Returned by the lookup.
</ResponseField>

<ResponseField name="issue_field_id" type="number">
  The ID of the issue field. Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the field. Returned by the lookup.
</ResponseField>

<ResponseField name="node_id" type="string">
  The node ID of the field. Returned by the lookup.
</ResponseField>

<ResponseField name="options" type="list(object)">
  The options available for single select fields. Returned by the lookup.
</ResponseField>

<ResponseField name="project_url" type="string">
  The API URL of the project that contains the field. Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The time when the field was last updated. Returned by the lookup.
</ResponseField>
