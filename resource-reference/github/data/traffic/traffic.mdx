---
title: "github_traffic"
description: "`github_traffic` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_traffic` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	traffic "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/traffic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_traffic"})

    		ubx.Data(traffic.Traffic, "example", traffic.TrafficConfig{
    			Owner: "example",
    			Repo:  "example",
    			Per:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Traffic } from "@ubx/sdk-github/github/data/traffic/traffic";

    export default stack("example", () => {
      intent({ summary: "look up github_traffic" });

      data(Traffic, "example", {
        owner: "example",
        repo: "example",
        per: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.traffic import Traffic, TrafficConfig

    def describe():
        ubx.intent("look up github_traffic")

        ubx.data(Traffic, "example", TrafficConfig(
            owner="example",
            repo="example",
            per="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="per" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="clones" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="uniques" type="number">
  Returned by the lookup.
</ResponseField>
