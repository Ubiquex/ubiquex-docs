---
title: "github_branche"
description: "`github_branche` lists the real branches in a repository, each one's own name and the commit it currently points to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	branche "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/branche"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_branche"})

    		ubx.Data(branche.Branche, "example", branche.BrancheConfig{
    			Branch:  "example",
    			Owner:   "example",
    			Repo:    "example",
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Branche } from "@ubx/sdk-github/github/data/branche/branche";

    export default stack("example", () => {
      intent({ summary: "look up github_branche" });

      data(Branche, "example", {
        branch: "example",
        owner: "example",
        repo: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.branche import Branche, BrancheConfig

    def describe():
        ubx.intent("look up github_branche")

        ubx.data(Branche, "example", BrancheConfig(
            branch="example",
            owner="example",
            repo="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="branch" type="string" required>
  The real branch name this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
