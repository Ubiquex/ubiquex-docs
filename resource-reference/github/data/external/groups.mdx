---
title: "github_external_groups"
description: "`github_external_groups` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_external_groups` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	external "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/external"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_external_groups"})

    		ubx.Data(external.Groups, "example", external.GroupsConfig{
    			Org:         "example",
    			DisplayName: "example-display-name",
    			Page:        1,
    			PerPage:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Groups } from "@ubx/sdk-github/github/data/external/groups";

    export default stack("example", () => {
      intent({ summary: "look up github_external_groups" });

      data(Groups, "example", {
        org: "example",
        displayName: "example-display-name",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.external import Groups, GroupsConfig

    def describe():
        ubx.intent("look up github_external_groups")

        ubx.data(Groups, "example", GroupsConfig(
            org="example",
            display_name="example-display-name",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="display_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="groups" type="list(object)">
  An array of external groups available to be mapped to a team Returned by the lookup.
</ResponseField>
