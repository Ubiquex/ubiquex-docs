---
title: "github_topic_search_result_item"
description: "`github_topic_search_result_item` looks up one real match from a GitHub topic search."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	topic "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/topic"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_topic_search_result_item"})

    		ubx.Data(topic.SearchResultItem, "example", topic.SearchResultItemConfig{
    			Q:       "example",
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SearchResultItem } from "@ubx/sdk-github/github/data/topic/search_result_item";

    export default stack("example", () => {
      intent({ summary: "look up github_topic_search_result_item" });

      data(SearchResultItem, "example", {
        q: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.topic import SearchResultItem, SearchResultItemConfig

    def describe():
        ubx.intent("look up github_topic_search_result_item")

        ubx.data(SearchResultItem, "example", SearchResultItemConfig(
            q="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="q" type="string" required>
  The real search query string. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="incomplete_results" type="bool">
  Whether the search timed out before scanning every possible match, making the result set incomplete. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="items" type="list(object)">
  The list of results for this page of the response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
