---
title: "github_team_membership"
description: "`github_team_membership` looks up one real person's own already-established place inside a team -- member or maintainer."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_team_membership"})

    		ubx.Data(team.Membership, "example", team.MembershipConfig{
    			TeamId:   1,
    			Username: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Membership } from "./local-sdk/github/sdk/typescript/github/data/team/membership.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_team_membership" });

      data(Membership, "example", {
        teamId: 1,
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.team import Membership, MembershipConfig

    def describe():
        ubx.intent("look up github_team_membership")

        ubx.data(Membership, "example", MembershipConfig(
            team_id=1,
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="team_id" type="number" required>
  The real, numeric ID of the team. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="username" type="string" required>
  The real account's own username. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="role" type="string">
  The role of the user in the team. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the user's membership in the team. Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
