---
title: "github_deprecation"
description: "`github_deprecation` reads a real, published notice of an upcoming REST API deprecation -- what's changing and when."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	deprecation "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/deprecation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_deprecation"})

    		ubx.Data(deprecation.Deprecation, "example", deprecation.DeprecationConfig{
    			Enterprise: "example",
    			Version:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Deprecation } from "./local-sdk/github/sdk/typescript/github/data/deprecation/deprecation.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_deprecation" });

      data(Deprecation, "example", {
        enterprise: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.deprecation import Deprecation, DeprecationConfig

    def describe():
        ubx.intent("look up github_deprecation")

        ubx.data(Deprecation, "example", DeprecationConfig(
            enterprise="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  The real version identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="registration_deprecates_at" type="string">
  The date after which this runner version can no longer register. Null if no schedule is set. Returned by the lookup.
</ResponseField>

<ResponseField name="runner_version" type="string">
  The runner version string. Returned by the lookup.
</ResponseField>

<ResponseField name="runtime_deprecates_at" type="string">
  The date after which jobs will no longer be dispatched to runners on this version. Returned by the lookup.
</ResponseField>
