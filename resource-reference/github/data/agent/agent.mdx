---
title: "github_agent"
description: "`github_agent` looks up a real, registered Copilot coding agent instance and its current status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	agent "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/agent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_agent"})

    		ubx.Data(agent.Agent, "example", agent.AgentConfig{
    			Direction:  "example",
    			IsArchived: true,
    			Page:       1,
    			PerPage:    1,
    			Since:      "example",
    			Sort:       "example",
    			State:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Agent } from "./local-sdk/github/sdk/typescript/github/data/agent/agent.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_agent" });

      data(Agent, "example", {
        direction: "example",
        isArchived: true,
        page: 1,
        perPage: 1,
        since: "example",
        sort: "example",
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.agent import Agent, AgentConfig

    def describe():
        ubx.intent("look up github_agent")

        ubx.data(Agent, "example", AgentConfig(
            direction="example",
            is_archived=True,
            page=1,
            per_page=1,
            since="example",
            sort="example",
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="is_archived" type="bool">
  Whether the repository is currently archived, read-only. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="since" type="string">
  Only return results updated at or after this real timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tasks" type="list(object)">
  List of tasks Returned by the lookup.
</ResponseField>

<ResponseField name="total_active_count" type="number">
  Total count of active (non-archived) tasks Returned by the lookup.
</ResponseField>

<ResponseField name="total_archived_count" type="number">
  Total count of archived tasks Returned by the lookup.
</ResponseField>
