---
title: "github_personal_access_token"
description: "`github_personal_access_token` looks up a real, organization-approved fine-grained personal access token and its own current scope and status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	personal "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/personal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_personal_access_token"})

    		ubx.Data(personal.AccessToken, "example", personal.AccessTokenConfig{
    			Org:     "example",
    			PatId:   1,
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { AccessToken } from "./local-sdk/github/sdk/typescript/github/data/personal/access_token.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_personal_access_token" });

      data(AccessToken, "example", {
        org: "example",
        patId: 1,
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.personal import AccessToken, AccessTokenConfig

    def describe():
        ubx.intent("look up github_personal_access_token")

        ubx.data(AccessToken, "example", AccessTokenConfig(
            org="example",
            pat_id=1,
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pat_id" type="number" required>
  The real, unique identifier of this fine-grained personal access token. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
