---
title: "github_personal_access_token_request"
description: "`github_personal_access_token_request` looks up a real, pending fine-grained personal access token request awaiting an organization owner's approval."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personal "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/personal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_personal_access_token_request"})

    		ubx.Data(personal.AccessTokenRequest, "example", personal.AccessTokenRequestConfig{
    			Org:          "example",
    			PatRequestId: 1,
    			Page:         1,
    			PerPage:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessTokenRequest } from "@ubx/sdk-github/github/data/personal/access_token_request";

    export default stack("example", () => {
      intent({ summary: "look up github_personal_access_token_request" });

      data(AccessTokenRequest, "example", {
        org: "example",
        patRequestId: 1,
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.personal import AccessTokenRequest, AccessTokenRequestConfig

    def describe():
        ubx.intent("look up github_personal_access_token_request")

        ubx.data(AccessTokenRequest, "example", AccessTokenRequestConfig(
            org="example",
            pat_request_id=1,
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="pat_request_id" type="number" required>
  The real, unique identifier of this personal access token approval request. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
