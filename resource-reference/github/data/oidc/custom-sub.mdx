---
title: "github_oidc_custom_sub"
description: "`github_oidc_custom_sub` reads a repository's own real, custom OIDC subject claim template used when Actions requests a cloud provider's own OIDC token."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oidc "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/oidc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_oidc_custom_sub"})

    		ubx.Data(oidc.CustomSub, "example", oidc.CustomSubConfig{
    			Org: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomSub } from "@ubx/sdk-github/github/data/oidc/custom_sub";

    export default stack("example", () => {
      intent({ summary: "look up github_oidc_custom_sub" });

      data(CustomSub, "example", {
        org: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.oidc import CustomSub, CustomSubConfig

    def describe():
        ubx.intent("look up github_oidc_custom_sub")

        ubx.data(CustomSub, "example", CustomSubConfig(
            org="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="include_claim_keys" type="list(string)">
  Array of unique strings. Each claim key can only contain alphanumeric characters and underscores. Returned by the lookup.
</ResponseField>

<ResponseField name="use_immutable_subject" type="bool">
  Whether to opt in to the immutable OIDC subject claim format for the organization. When `true`, new OIDC tokens will use a stable, repository-ID-based `sub` claim instead of the name-based format. Returned by the lookup.
</ResponseField>
