---
title: "github_oidc_custom_sub_repo"
description: "`github_oidc_custom_sub_repo` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_oidc_custom_sub_repo` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oidc "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/oidc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_oidc_custom_sub_repo"})

    		ubx.Data(oidc.CustomSubRepo, "example", oidc.CustomSubRepoConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CustomSubRepo } from "@ubx/sdk-github/github/data/oidc/custom_sub_repo";

    export default stack("example", () => {
      intent({ summary: "look up github_oidc_custom_sub_repo" });

      data(CustomSubRepo, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.oidc import CustomSubRepo, CustomSubRepoConfig

    def describe():
        ubx.intent("look up github_oidc_custom_sub_repo")

        ubx.data(CustomSubRepo, "example", CustomSubRepoConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="include_claim_keys" type="list(string)">
  Array of unique strings. Each claim key can only contain alphanumeric characters and underscores. Returned by the lookup.
</ResponseField>

<ResponseField name="sub_claim_prefix" type="string">
  The current `sub` claim prefix for this repository. Returned by the lookup.
</ResponseField>

<ResponseField name="use_default" type="bool">
  Whether to use the default template or not. If `true`, the `include_claim_keys` field is ignored. Returned by the lookup.
</ResponseField>

<ResponseField name="use_immutable_subject" type="bool">
  Whether the repository has opted in to the immutable OIDC subject claim format. When `true`, OIDC tokens will use a stable, repository-ID-based `sub` claim. If not set at the repository level, falls back to the organization-level setting. Returned by the lookup.
</ResponseField>
