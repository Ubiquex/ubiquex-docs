---
title: "github_deployment"
description: "`github_deployment` looks up a real, already-created request to deploy a specific ref -- a branch, a SHA, a tag."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deployment "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/deployment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_deployment"})

    		ubx.Data(deployment.Deployment, "example", deployment.DeploymentConfig{
    			DeploymentId: 1,
    			Owner:        "example",
    			Repo:         "example",
    			Page:         1,
    			PerPage:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Deployment } from "@ubx/sdk-github/github/data/deployment/deployment";

    export default stack("example", () => {
      intent({ summary: "look up github_deployment" });

      data(Deployment, "example", {
        deploymentId: 1,
        owner: "example",
        repo: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.deployment import Deployment, DeploymentConfig

    def describe():
        ubx.intent("look up github_deployment")

        ubx.data(Deployment, "example", DeploymentConfig(
            deployment_id=1,
            owner="example",
            repo="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="deployment_id" type="number" required>
  The real, numeric ID of this deployment. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
