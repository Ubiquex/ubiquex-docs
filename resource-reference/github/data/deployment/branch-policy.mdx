---
title: "github_deployment_branch_policy"
description: "`github_deployment_branch_policy` looks up a real, already-configured restriction on which branches are allowed to deploy to a given environment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	deployment "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/deployment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_deployment_branch_policy"})

    		ubx.Data(deployment.BranchPolicy, "example", deployment.BranchPolicyConfig{
    			EnvironmentName: "example-environment-name",
    			Owner:           "example",
    			Repo:            "example",
    			Page:            1,
    			PerPage:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { BranchPolicy } from "./local-sdk/github/sdk/typescript/github/data/deployment/branch_policy.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_deployment_branch_policy" });

      data(BranchPolicy, "example", {
        environmentName: "example-environment-name",
        owner: "example",
        repo: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.deployment import BranchPolicy, BranchPolicyConfig

    def describe():
        ubx.intent("look up github_deployment_branch_policy")

        ubx.data(BranchPolicy, "example", BranchPolicyConfig(
            environment_name="example-environment-name",
            owner="example",
            repo="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="environment_name" type="string" required>
  The real name of the deployment environment. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="branch_policies" type="list(object)">
  The real branch name patterns allowed to deploy to this environment. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The number of deployment branch policies for the environment. Returned by the lookup.
</ResponseField>
