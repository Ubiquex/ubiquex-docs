---
title: "github_dismissal_request_response"
description: "`github_dismissal_request_response` looks up the real decision recorded on a dismissal request -- approved or denied, and by whom."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	dismissal "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/dismissal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_dismissal_request_response"})

    		ubx.Data(dismissal.RequestResponse, "example", dismissal.RequestResponseConfig{
    			AlertNumber: 1,
    			Owner:       "example",
    			Repo:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { RequestResponse } from "./local-sdk/github/sdk/typescript/github/data/dismissal/request_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_dismissal_request_response" });

      data(RequestResponse, "example", {
        alertNumber: 1,
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.dismissal import RequestResponse, RequestResponseConfig

    def describe():
        ubx.intent("look up github_dismissal_request_response")

        ubx.data(RequestResponse, "example", RequestResponseConfig(
            alert_number=1,
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="alert_number" type="number" required>
  The real, repository-scoped number identifying this alert. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The date and time the dismissal request was created. Returned by the lookup.
</ResponseField>

<ResponseField name="data" type="list(object)">
  Data describing the dismissal request metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="expires_at" type="string">
  The date and time the dismissal request will expire. Returned by the lookup.
</ResponseField>

<ResponseField name="html_url" type="string">
  The URL to view the dismissal request in a browser. Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="number">
  The unique identifier of the dismissal request. Returned by the lookup.
</ResponseField>

<ResponseField name="number" type="number">
  The number uniquely identifying the dismissal request within its repository. Returned by the lookup.
</ResponseField>

<ResponseField name="organization" type="object">
  The organization associated with the repository the dismissal request is for. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="id" type="number">
      The ID of the organization. Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the organization. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="repository" type="object">
  The repository the dismissal request is for. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="full_name" type="string">
      The full name of the repository the dismissal request is for. Optional lookup argument.
    </ResponseField>
    <ResponseField name="id" type="number">
      The ID of the repository the dismissal request is for. Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the repository the dismissal request is for. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="request_type" type="string">
  The type of request. Returned by the lookup.
</ResponseField>

<ResponseField name="requester" type="object">
  The user who requested the dismissal request. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="actor_id" type="number">
      The ID of the GitHub user who requested the dismissal request. Optional lookup argument.
    </ResponseField>
    <ResponseField name="actor_name" type="string">
      The name of the GitHub user who requested the dismissal request. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="requester_comment" type="string">
  The comment the requester provided when creating the dismissal request. Returned by the lookup.
</ResponseField>

<ResponseField name="resource_identifier" type="string">
  The unique identifier for the request type of the dismissal request. Returned by the lookup.
</ResponseField>

<ResponseField name="responses" type="list(object)">
  The responses to the dismissal request. Returned by the lookup.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the dismissal request. Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
