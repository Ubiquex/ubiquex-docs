---
title: "github_dismissal_request"
description: "`github_dismissal_request` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_dismissal_request` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dismissal "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/dismissal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_dismissal_request"})

    		ubx.Data(dismissal.Request, "example", dismissal.RequestConfig{
    			Enterprise:       "example",
    			OrganizationName: "example-organization-name",
    			Page:             1,
    			PerPage:          1,
    			RequestStatus:    "example",
    			Requester:        "example",
    			Reviewer:         "example",
    			TimePeriod:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Request } from "@ubx/sdk-github/github/data/dismissal/request";

    export default stack("example", () => {
      intent({ summary: "look up github_dismissal_request" });

      data(Request, "example", {
        enterprise: "example",
        organizationName: "example-organization-name",
        page: 1,
        perPage: 1,
        requestStatus: "example",
        requester: "example",
        reviewer: "example",
        timePeriod: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.dismissal import Request, RequestConfig

    def describe():
        ubx.intent("look up github_dismissal_request")

        ubx.data(Request, "example", RequestConfig(
            enterprise="example",
            organization_name="example-organization-name",
            page=1,
            per_page=1,
            request_status="example",
            requester="example",
            reviewer="example",
            time_period="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="organization_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="request_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="requester" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="reviewer" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="time_period" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
