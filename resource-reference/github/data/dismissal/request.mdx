---
title: "github_dismissal_request"
description: "`github_dismissal_request` looks up a real, submitted request to dismiss a secret scanning alert or similar finding, awaiting or already given a decision."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dismissal "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/dismissal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_dismissal_request"})

    		ubx.Data(dismissal.Request, "example", dismissal.RequestConfig{
    			Enterprise:       "example",
    			OrganizationName: "example-organization-name",
    			Page:             1,
    			PerPage:          1,
    			RequestStatus:    "example",
    			Requester:        "example",
    			Reviewer:         "example",
    			TimePeriod:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Request } from "@ubx/sdk-github/github/data/dismissal/request";

    export default stack("example", () => {
      intent({ summary: "look up github_dismissal_request" });

      data(Request, "example", {
        enterprise: "example",
        organizationName: "example-organization-name",
        page: 1,
        perPage: 1,
        requestStatus: "example",
        requester: "example",
        reviewer: "example",
        timePeriod: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.dismissal import Request, RequestConfig

    def describe():
        ubx.intent("look up github_dismissal_request")

        ubx.data(Request, "example", RequestConfig(
            enterprise="example",
            organization_name="example-organization-name",
            page=1,
            per_page=1,
            request_status="example",
            requester="example",
            reviewer="example",
            time_period="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="organization_name" type="string">
  The real name of the organization this result belongs to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="request_status" type="string">
  The real, current status of this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="requester" type="string">
  The real user who submitted this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="reviewer" type="string">
  The real user or team responsible for reviewing this request. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="time_period" type="string">
  The real time window the returned statistics are aggregated over. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
