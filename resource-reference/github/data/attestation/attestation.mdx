---
title: "github_attestation"
description: "`github_attestation` looks up a real, already-issued cryptographically signed record establishing where and how a software artifact was built."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	attestation "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/attestation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_attestation"})

    		ubx.Data(attestation.Attestation, "example", attestation.AttestationConfig{
    			Org:           "example",
    			After:         "example",
    			Before:        "example",
    			PerPage:       1,
    			PredicateType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Attestation } from "@ubx/sdk-github/github/data/attestation/attestation";

    export default stack("example", () => {
      intent({ summary: "look up github_attestation" });

      data(Attestation, "example", {
        org: "example",
        after: "example",
        before: "example",
        perPage: 1,
        predicateType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.attestation import Attestation, AttestationConfig

    def describe():
        ubx.intent("look up github_attestation")

        ubx.data(Attestation, "example", AttestationConfig(
            org="example",
            after="example",
            before="example",
            per_page=1,
            predicate_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Only return results after this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Only return results before this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="predicate_type" type="string">
  The real, in-toto predicate type this attestation makes a claim under. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
