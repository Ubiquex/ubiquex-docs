---
title: "github_protected_branch_pull_request_review"
description: "`github_protected_branch_pull_request_review` reads a protected branch's own required pull request review settings -- how many approvals..."
---

`github_protected_branch_pull_request_review` reads a protected branch's own required pull request review settings -- how many approvals, whether stale reviews are dismissed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	protected "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/protected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_protected_branch_pull_request_review"})

    		ubx.Data(protected.BranchPullRequestReview, "example", protected.BranchPullRequestReviewConfig{
    			Branch: "example",
    			Owner:  "example",
    			Repo:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { BranchPullRequestReview } from "./local-sdk/github/sdk/typescript/github/data/protected/branch_pull_request_review.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_protected_branch_pull_request_review" });

      data(BranchPullRequestReview, "example", {
        branch: "example",
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.protected import BranchPullRequestReview, BranchPullRequestReviewConfig

    def describe():
        ubx.intent("look up github_protected_branch_pull_request_review")

        ubx.data(BranchPullRequestReview, "example", BranchPullRequestReviewConfig(
            branch="example",
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="branch" type="string" required>
  The real branch name this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="bypass_pull_request_allowances" type="object">
  Allow specific users, teams, or apps to bypass pull request requirements. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="apps" type="list(object)">
      The list of apps allowed to bypass pull request requirements. Optional lookup argument.
    </ResponseField>
    <ResponseField name="teams" type="list(object)">
      The list of teams allowed to bypass pull request requirements. Optional lookup argument.
    </ResponseField>
    <ResponseField name="users" type="list(object)">
      The list of users allowed to bypass pull request requirements. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="dismiss_stale_reviews" type="bool">
  Whether new commits automatically dismiss existing pull request approvals. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dismissal_restrictions" type="object">
  The real users and teams allowed to dismiss a pull request review. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="apps" type="list(object)">
      The list of apps with review dismissal access. Optional lookup argument.
    </ResponseField>
    <ResponseField name="teams" type="list(object)">
      The list of teams with review dismissal access. Optional lookup argument.
    </ResponseField>
    <ResponseField name="teams_url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="url" type="string">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="users" type="list(object)">
      The list of users with review dismissal access. Optional lookup argument.
    </ResponseField>
    <ResponseField name="users_url" type="string">
      Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="require_code_owner_reviews" type="bool">
  Whether an approving review from a code owner is required before merging. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="require_last_push_approval" type="bool">
  Whether the most recent push must be approved by someone other than the person who pushed it. Returned by the lookup.
</ResponseField>

<ResponseField name="required_approving_review_count" type="number">
  The real number of approving reviews required before merging. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
