---
title: "github_protected_branch_admin_enforced"
description: "`github_protected_branch_admin_enforced` reads whether a protected branch's own rules also apply to repository administrators..."
---

`github_protected_branch_admin_enforced` reads whether a protected branch's own rules also apply to repository administrators, or whether admins can bypass them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	protected "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/protected"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_protected_branch_admin_enforced"})

    		ubx.Data(protected.BranchAdminEnforced, "example", protected.BranchAdminEnforcedConfig{
    			Branch: "example",
    			Owner:  "example",
    			Repo:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BranchAdminEnforced } from "@ubx/sdk-github/github/data/protected/branch_admin_enforced";

    export default stack("example", () => {
      intent({ summary: "look up github_protected_branch_admin_enforced" });

      data(BranchAdminEnforced, "example", {
        branch: "example",
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.protected import BranchAdminEnforced, BranchAdminEnforcedConfig

    def describe():
        ubx.intent("look up github_protected_branch_admin_enforced")

        ubx.data(BranchAdminEnforced, "example", BranchAdminEnforcedConfig(
            branch="example",
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="branch" type="string" required>
  The real branch name this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enabled" type="bool">
  Whether this feature is currently enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
