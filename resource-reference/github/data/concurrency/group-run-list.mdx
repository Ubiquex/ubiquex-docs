---
title: "github_concurrency_group_run_list"
description: "`github_concurrency_group_run_list` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_concurrency_group_run_list` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	concurrency "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/concurrency"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_concurrency_group_run_list"})

    		ubx.Data(concurrency.GroupRunList, "example", concurrency.GroupRunListConfig{
    			Owner:   "example",
    			Repo:    "example",
    			RunId:   1,
    			After:   "example",
    			Before:  "example",
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupRunList } from "@ubx/sdk-github/github/data/concurrency/group_run_list";

    export default stack("example", () => {
      intent({ summary: "look up github_concurrency_group_run_list" });

      data(GroupRunList, "example", {
        owner: "example",
        repo: "example",
        runId: 1,
        after: "example",
        before: "example",
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.concurrency import GroupRunList, GroupRunListConfig

    def describe():
        ubx.intent("look up github_concurrency_group_run_list")

        ubx.data(GroupRunList, "example", GroupRunListConfig(
            owner="example",
            repo="example",
            run_id=1,
            after="example",
            before="example",
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="run_id" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="concurrency_groups" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of concurrency groups this workflow run participates in, derived from the run's configuration. This count is not filtered by whether the run currently holds or is waiting in each group, so it can include groups whose `group_members` array is empty (for example, when the run has already released its lease in that group). Returned by the lookup.
</ResponseField>
