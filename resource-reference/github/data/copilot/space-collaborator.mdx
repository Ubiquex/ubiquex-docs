---
title: "github_copilot_space_collaborator"
description: "`github_copilot_space_collaborator` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_copilot_space_collaborator` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	copilot "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/copilot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_copilot_space_collaborator"})

    		ubx.Data(copilot.SpaceCollaborator, "example", copilot.SpaceCollaboratorConfig{
    			Org:         "example",
    			SpaceNumber: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SpaceCollaborator } from "@ubx/sdk-github/github/data/copilot/space_collaborator";

    export default stack("example", () => {
      intent({ summary: "look up github_copilot_space_collaborator" });

      data(SpaceCollaborator, "example", {
        org: "example",
        spaceNumber: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.copilot import SpaceCollaborator, SpaceCollaboratorConfig

    def describe():
        ubx.intent("look up github_copilot_space_collaborator")

        ubx.data(SpaceCollaborator, "example", SpaceCollaboratorConfig(
            org="example",
            space_number=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="space_number" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="collaborators" type="list(any)">
  The list of collaborators for this Copilot Space. Returned by the lookup.
</ResponseField>
