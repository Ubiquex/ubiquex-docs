---
title: "github_license"
description: "`github_license` looks up one of GitHub's own real, published open source license templates."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	license "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/license"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_license"})

    		ubx.Data(license.License, "example", license.LicenseConfig{
    			License: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { License } from "@ubx/sdk-github/github/data/license/license";

    export default stack("example", () => {
      intent({ summary: "look up github_license" });

      data(License, "example", {
        license: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.license import License, LicenseConfig

    def describe():
        ubx.intent("look up github_license")

        ubx.data(License, "example", LicenseConfig(
            license="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="license" type="string" required>
  The real, detected open source license for this repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="body" type="string">
  The real, free-text body content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="conditions" type="list(string)">
  The real conditions this license or ruleset imposes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="featured" type="bool">
  Whether GitHub features this license prominently in its own license picker. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="html_url" type="string">
  The real, browsable web URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="implementation" type="string">
  Real guidance on how to apply this license. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="key" type="string">
  The real, unique key identifying this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="limitations" type="list(string)">
  The real limitations this license places on use. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="node_id" type="string">
  The real, opaque GraphQL node identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="permissions" type="list(string)">
  The real set of permissions granted here. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="spdx_id" type="string">
  The real SPDX identifier for this license. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
