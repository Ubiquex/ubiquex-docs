---
title: "github_server_statistics"
description: "`github_server_statistics` reads real, aggregated usage statistics for a GitHub Enterprise Server instance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	server "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/server"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_server_statistics"})

    		ubx.Data(server.Statistics, "example", server.StatisticsConfig{
    			EnterpriseOrOrg: "example",
    			DateEnd:         "example",
    			DateStart:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Statistics } from "./local-sdk/github/sdk/typescript/github/data/server/statistics.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_server_statistics" });

      data(Statistics, "example", {
        enterpriseOrOrg: "example",
        dateEnd: "example",
        dateStart: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.server import Statistics, StatisticsConfig

    def describe():
        ubx.intent("look up github_server_statistics")

        ubx.data(Statistics, "example", StatisticsConfig(
            enterprise_or_org="example",
            date_end="example",
            date_start="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="date_end" type="string">
  The latest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="date_start" type="string">
  The earliest real date to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="enterprise_or_org" type="string" required>
  The real enterprise or organization slug this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
