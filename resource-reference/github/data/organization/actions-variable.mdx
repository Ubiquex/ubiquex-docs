---
title: "github_organization_actions_variable"
description: "`github_organization_actions_variable` looks up a real, already-defined Actions configuration variable set at the organization level..."
---

`github_organization_actions_variable` looks up a real, already-defined Actions configuration variable set at the organization level, readable by every repository underneath through the `vars` context.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_organization_actions_variable"})

    		ubx.Data(organization.ActionsVariable, "example", organization.ActionsVariableConfig{
    			Org:     "example",
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ActionsVariable } from "@ubx/sdk-github/github/data/organization/actions_variable";

    export default stack("example", () => {
      intent({ summary: "look up github_organization_actions_variable" });

      data(ActionsVariable, "example", {
        org: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.organization import ActionsVariable, ActionsVariableConfig

    def describe():
        ubx.intent("look up github_organization_actions_variable")

        ubx.data(ActionsVariable, "example", ActionsVariableConfig(
            org="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="variables" type="list(object)">
  The real Actions configuration variables included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
