---
title: "github_organization_full"
description: "`github_organization_full` looks up the complete, real record of an organization -- every setting and plan detail attached to it."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_organization_full"})

    		ubx.Data(organization.Full, "example", organization.FullConfig{
    			Org: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Full } from "./local-sdk/github/sdk/typescript/github/data/organization/full.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_organization_full" });

      data(Full, "example", {
        org: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.organization import Full, FullConfig

    def describe():
        ubx.intent("look up github_organization_full")

        ubx.data(Full, "example", FullConfig(
            org="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="advanced_security_enabled_for_new_repositories" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether GitHub Advanced Security is enabled for new repositories and repositories transferred to this organization. This field is only visible to organization owners or members of a team with the security manager role. Returned by the lookup.
</ResponseField>

<ResponseField name="archived_at" type="string">
  The real timestamp this object was archived, if it has been. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="avatar_url" type="string">
  The real URL of this account's own profile picture. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="billing_email" type="string">
  The real email address billing notices are sent to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="blog" type="string">
  The real, user-specified blog or website URL on this profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="collaborators" type="number">
  The number of collaborators on private repositories. This field may be null if the number of private repositories is over 50,000. Returned by the lookup.
</ResponseField>

<ResponseField name="company" type="string">
  The real, user-specified company name on this profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="default_repository_branch" type="string">
  The default branch for repositories created in this organization. Returned by the lookup.
</ResponseField>

<ResponseField name="default_repository_permission" type="string">
  The real, default permission level newly created repositories grant to organization members. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="dependabot_alerts_enabled_for_new_repositories" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether Dependabot alerts are automatically enabled for new repositories and repositories transferred to this organization. This field is only visible to organization owners or members of a team with the security manager role. Returned by the lookup.
</ResponseField>

<ResponseField name="dependabot_security_updates_enabled_for_new_repositories" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether Dependabot security updates are automatically enabled for new repositories and repositories transferred to this organization. This field is only visible to organization owners or members of a team with the security manager role. Returned by the lookup.
</ResponseField>

<ResponseField name="dependency_graph_enabled_for_new_repositories" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether dependency graph is automatically enabled for new repositories and repositories transferred to this organization. This field is only visible to organization owners or members of a team with the security manager role. Returned by the lookup.
</ResponseField>

<ResponseField name="deploy_keys_enabled_for_repositories" type="bool">
  Controls whether or not deploy keys may be added and used for repositories in the organization. Returned by the lookup.
</ResponseField>

<ResponseField name="description" type="string">
  The real, free-text description set on this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="disk_usage" type="number">
  The real disk space this account's own repositories currently consume, in kilobytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="display_commenter_full_name_setting_enabled" type="bool">
  Whether commenters' own full names are shown alongside their username. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="email" type="string">
  The real, public email address on this profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="events_url" type="string">
  The real API URL for this object's own events. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="followers" type="number">
  The real number of accounts following this user. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="following" type="number">
  The real number of accounts this user follows. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="has_organization_projects" type="bool">
  Whether organization-level Projects are enabled for repositories in this organization. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="has_repository_projects" type="bool">
  Whether repository-level Projects are enabled for this repository. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hooks_url" type="string">
  The real API URL for this object's own webhooks. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="html_url" type="string">
  The real, browsable web URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="id" type="number">
  The real, numeric, globally unique identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="is_verified" type="bool">
  Whether this organization's own domain has been verified. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="issues_url" type="string">
  The real API URL template for this object's own issues. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="location" type="string">
  The real, physical or logical location associated with this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="login" type="string">
  The real account's own username. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_allowed_repository_creation_type" type="string">
  Which real repository visibility levels members are allowed to create. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_change_repo_visibility" type="bool">
  Whether members are allowed to change a repository's own visibility. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_internal_repositories" type="bool">
  Whether members are allowed to create internal repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_pages" type="bool">
  Whether members are allowed to publish GitHub Pages sites. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_private_pages" type="bool">
  Whether members are allowed to publish private GitHub Pages sites. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_private_repositories" type="bool">
  Whether members are allowed to create private repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_public_pages" type="bool">
  Whether members are allowed to publish public GitHub Pages sites. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_public_repositories" type="bool">
  Whether members are allowed to create public repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_repositories" type="bool">
  Whether members are allowed to create repositories at all. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_create_teams" type="bool">
  Whether members are allowed to create teams. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_delete_issues" type="bool">
  Whether members are allowed to delete issues. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_delete_repositories" type="bool">
  Whether members are allowed to delete repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_fork_private_repositories" type="bool">
  Whether members are allowed to fork private repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_invite_outside_collaborators" type="bool">
  Whether members are allowed to invite outside collaborators. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_can_view_dependency_insights" type="bool">
  Whether members are allowed to view dependency insights. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="members_url" type="string">
  The real API URL template for this team's own members. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="node_id" type="string">
  The real, opaque GraphQL node identifier for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="owned_private_repos" type="number">
  The real number of private repositories this account owns. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="plan" type="object">
  The real, current billing plan for this account. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="filled_seats" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="private_repos" type="number" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="seats" type="number">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="space" type="number" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="private_gists" type="number">
  The real number of private gists this account owns. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_gists" type="number">
  The real number of public gists this account owns. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_members_url" type="string">
  The real API URL template for an organization's own public members. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_repos" type="number">
  The real number of public repositories this account owns. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="readers_can_create_discussions" type="bool">
  Whether read-only members are allowed to start new discussions. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repos_url" type="string">
  The real API URL for this account's own repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="secret_scanning_enabled_for_new_repositories" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether secret scanning is automatically enabled for new repositories and repositories transferred to this organization. This field is only visible to organization owners or members of a team with the security manager role. Returned by the lookup.
</ResponseField>

<ResponseField name="secret_scanning_push_protection_custom_link" type="string">
  An optional URL string to display to contributors who are blocked from pushing a secret. Returned by the lookup.
</ResponseField>

<ResponseField name="secret_scanning_push_protection_custom_link_enabled" type="bool">
  Whether a custom link is shown to contributors who are blocked from pushing a secret by push protection. Returned by the lookup.
</ResponseField>

<ResponseField name="secret_scanning_push_protection_enabled_for_new_repositories" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether secret scanning push protection is automatically enabled for new repositories and repositories transferred to this organization. This field is only visible to organization owners or members of a team with the security manager role. Returned by the lookup.
</ResponseField>

<ResponseField name="secret_scanning_validity_checks_enabled" type="bool">
  **Endpoint closing down notice.** Please use [code security configurations](https://docs.github.com/enterprise-cloud@latest/rest/code-security/configurations) instead. Whether secret scanning automatic validity checks on supported partner tokens is enabled for all repositories under this organization. Returned by the lookup.
</ResponseField>

<ResponseField name="total_private_repos" type="number">
  The real total number of private repositories this account owns. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="twitter_username" type="string">
  The real, user-specified X/Twitter handle on this profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="two_factor_requirement_enabled" type="bool">
  Whether this organization requires every member to have two-factor authentication enabled. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The real timestamp this object was last updated. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="web_commit_signoff_required" type="bool">
  Whether every commit made through GitHub's own web interface must include a Signed-off-by trailer. **(AI-inferred)** Returned by the lookup.
</ResponseField>
