---
title: "github_organization_dependabot_secret"
description: "`github_organization_dependabot_secret` looks up the real metadata of an organization-level Dependabot secret -- never its own decrypted value."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-github/sdk/go/github/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dependabot secret"})

    		ubx.Resource(organization.DependabotSecret, "example", organization.DependabotSecretConfig{
    			Org:                   "example",
    			SecretName:            "example-secret-name",
    			Visibility:            "example",
    			EncryptedValue:        "example",
    			KeyId:                 "example",
    			SelectedRepositoryIds: []any{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DependabotSecret } from "./local-sdk/github/sdk/typescript/github/organization/dependabot_secret.ts";

    export default stack("example", () => {
      intent({ summary: "example own dependabot secret" });

      resource(DependabotSecret, "example", {
        org: "example",
        secretName: "example-secret-name",
        visibility: "example",
        encryptedValue: "example",
        keyId: "example",
        selectedRepositoryIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.organization import DependabotSecret, DependabotSecretConfig

    def describe():
        ubx.intent("example own dependabot secret")

        ubx.resource(DependabotSecret, "example", DependabotSecretConfig(
            org="example",
            secret_name="example-secret-name",
            visibility="example",
            encrypted_value="example",
            key_id="example",
            selected_repository_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_organization_dependabot_secret, with org example,
    secret name example-secret-name, visibility example, encrypted
    value example, key id example, selected repository ids a set
    value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="secrets" type="list(object)">
  The real Actions or Dependabot secrets included in this result -- names and metadata only, never a decrypted value. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
