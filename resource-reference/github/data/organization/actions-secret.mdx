---
title: "github_organization_actions_secret"
description: "`github_organization_actions_secret` looks up the real metadata of an organization-level Actions secret -- never its own decrypted value."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-github/sdk/go/github/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own actions secret"})

    		ubx.Resource(organization.ActionsSecret, "example", organization.ActionsSecretConfig{
    			EncryptedValue:        "example",
    			KeyId:                 "example",
    			Org:                   "example",
    			SecretName:            "example-secret-name",
    			Visibility:            "example",
    			SelectedRepositoryIds: []any{1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ActionsSecret } from "./local-sdk/github/sdk/typescript/github/organization/actions_secret.ts";

    export default stack("example", () => {
      intent({ summary: "example own actions secret" });

      resource(ActionsSecret, "example", {
        encryptedValue: "example",
        keyId: "example",
        org: "example",
        secretName: "example-secret-name",
        visibility: "example",
        selectedRepositoryIds: [1],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.organization import ActionsSecret, ActionsSecretConfig

    def describe():
        ubx.intent("example own actions secret")

        ubx.resource(ActionsSecret, "example", ActionsSecretConfig(
            encrypted_value="example",
            key_id="example",
            org="example",
            secret_name="example-secret-name",
            visibility="example",
            selected_repository_ids=[1],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_organization_actions_secret, with encrypted value
    example, key id example, org example, secret name
    example-secret-name, visibility example, selected repository ids
    a set value.
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="secrets" type="list(object)">
  The real Actions or Dependabot secrets included in this result -- names and metadata only, never a decrypted value. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
