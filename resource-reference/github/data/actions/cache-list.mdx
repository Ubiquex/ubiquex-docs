---
title: "github_actions_cache_list"
description: "`github_actions_cache_list` lists the real Actions caches currently stored for a repository, each entry's own key, size, and last-accessed time."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_cache_list"})

    		ubx.Data(actions.CacheList, "example", actions.CacheListConfig{
    			Owner:     "example",
    			Repo:      "example",
    			Direction: "example",
    			Key:       "example",
    			Page:      1,
    			PerPage:   1,
    			Ref:       "example",
    			Sort:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { CacheList } from "./local-sdk/github/sdk/typescript/github/data/actions/cache_list.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_cache_list" });

      data(CacheList, "example", {
        owner: "example",
        repo: "example",
        direction: "example",
        key: "example",
        page: 1,
        perPage: 1,
        ref: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.actions import CacheList, CacheListConfig

    def describe():
        ubx.intent("look up github_actions_cache_list")

        ubx.data(CacheList, "example", CacheListConfig(
            owner="example",
            repo="example",
            direction="example",
            key="example",
            page=1,
            per_page=1,
            ref="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string">
  The real, unique key identifying this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ref" type="string">
  The real git reference (a branch or tag name) this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actions_caches" type="list(object)">
  Array of caches Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Total number of caches Returned by the lookup.
</ResponseField>
