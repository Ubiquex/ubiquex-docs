---
title: "github_actions_cache_list"
description: "`github_actions_cache_list` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_cache_list` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_cache_list"})

    		ubx.Data(actions.CacheList, "example", actions.CacheListConfig{
    			Owner:     "example",
    			Repo:      "example",
    			Direction: "example",
    			Key:       "example",
    			Page:      1,
    			PerPage:   1,
    			Ref:       "example",
    			Sort:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CacheList } from "@ubx/sdk-github/github/data/actions/cache_list";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_cache_list" });

      data(CacheList, "example", {
        owner: "example",
        repo: "example",
        direction: "example",
        key: "example",
        page: 1,
        perPage: 1,
        ref: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import CacheList, CacheListConfig

    def describe():
        ubx.intent("look up github_actions_cache_list")

        ubx.data(CacheList, "example", CacheListConfig(
            owner="example",
            repo="example",
            direction="example",
            key="example",
            page=1,
            per_page=1,
            ref="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ref" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actions_caches" type="list(object)">
  Array of caches Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Total number of caches Returned by the lookup.
</ResponseField>
