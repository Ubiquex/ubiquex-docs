---
title: "github_actions_artifact_and_log_retention_response"
description: "`github_actions_artifact_and_log_retention_response` reads how many days Actions keeps a repository's own build artifacts and workflow logs before..."
---

`github_actions_artifact_and_log_retention_response` reads how many days Actions keeps a repository's own build artifacts and workflow logs before deleting them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_artifact_and_log_retention_response"})

    		ubx.Data(actions.ArtifactAndLogRetentionResponse, "example", actions.ArtifactAndLogRetentionResponseConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ArtifactAndLogRetentionResponse } from "@ubx/sdk-github/github/data/actions/artifact_and_log_retention_response";

    export default stack("example", () => {
      intent({
        summary: "look up github_actions_artifact_and_log_retention_response",
      });

      data(ArtifactAndLogRetentionResponse, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import ArtifactAndLogRetentionResponse, ArtifactAndLogRetentionResponseConfig

    def describe():
        ubx.intent("look up github_actions_artifact_and_log_retention_response")

        ubx.data(ArtifactAndLogRetentionResponse, "example", ArtifactAndLogRetentionResponseConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="days" type="number">
  The number of days artifacts and logs are retained Returned by the lookup.
</ResponseField>

<ResponseField name="maximum_allowed_days" type="number">
  The maximum number of days that can be configured Returned by the lookup.
</ResponseField>
