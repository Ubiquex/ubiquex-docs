---
title: "github_actions_hosted_runner_custom_image"
description: "`github_actions_hosted_runner_custom_image` looks up a real, organization-uploaded VM image available for provisioning a GitHub-hosted runner..."
---

`github_actions_hosted_runner_custom_image` looks up a real, organization-uploaded VM image available for provisioning a GitHub-hosted runner, in place of one of GitHub's own curated images.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_hosted_runner_custom_image"})

    		ubx.Data(actions.HostedRunnerCustomImage, "example", actions.HostedRunnerCustomImageConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HostedRunnerCustomImage } from "@ubx/sdk-github/github/data/actions/hosted_runner_custom_image";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_hosted_runner_custom_image" });

      data(HostedRunnerCustomImage, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import HostedRunnerCustomImage, HostedRunnerCustomImageConfig

    def describe():
        ubx.intent("look up github_actions_hosted_runner_custom_image")

        ubx.data(HostedRunnerCustomImage, "example", HostedRunnerCustomImageConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="images" type="list(object)">
  The real VM images available for provisioning a GitHub-hosted runner. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
