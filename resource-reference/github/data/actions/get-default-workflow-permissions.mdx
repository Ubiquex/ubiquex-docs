---
title: "github_actions_get_default_workflow_permissions"
description: "`github_actions_get_default_workflow_permissions` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_get_default_workflow_permissions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_get_default_workflow_permissions"})

    		ubx.Data(actions.GetDefaultWorkflowPermissions, "example", actions.GetDefaultWorkflowPermissionsConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GetDefaultWorkflowPermissions } from "@ubx/sdk-github/github/data/actions/get_default_workflow_permissions";

    export default stack("example", () => {
      intent({
        summary: "look up github_actions_get_default_workflow_permissions",
      });

      data(GetDefaultWorkflowPermissions, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import GetDefaultWorkflowPermissions, GetDefaultWorkflowPermissionsConfig

    def describe():
        ubx.intent("look up github_actions_get_default_workflow_permissions")

        ubx.data(GetDefaultWorkflowPermissions, "example", GetDefaultWorkflowPermissionsConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="can_approve_pull_request_reviews" type="bool">
  Whether GitHub Actions can approve pull requests. Enabling this can be a security risk. Returned by the lookup.
</ResponseField>

<ResponseField name="default_workflow_permissions" type="string">
  The default workflow permissions granted to the GITHUB_TOKEN when running workflows. Returned by the lookup.
</ResponseField>
