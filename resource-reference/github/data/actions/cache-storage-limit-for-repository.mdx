---
title: "github_actions_cache_storage_limit_for_repository"
description: "`github_actions_cache_storage_limit_for_repository` reads the repository-level cap on total Actions cache storage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_cache_storage_limit_for_repository"})

    		ubx.Data(actions.CacheStorageLimitForRepository, "example", actions.CacheStorageLimitForRepositoryConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CacheStorageLimitForRepository } from "@ubx/sdk-github/github/data/actions/cache_storage_limit_for_repository";

    export default stack("example", () => {
      intent({
        summary: "look up github_actions_cache_storage_limit_for_repository",
      });

      data(CacheStorageLimitForRepository, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import CacheStorageLimitForRepository, CacheStorageLimitForRepositoryConfig

    def describe():
        ubx.intent("look up github_actions_cache_storage_limit_for_repository")

        ubx.data(CacheStorageLimitForRepository, "example", CacheStorageLimitForRepositoryConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_cache_size_gb" type="number">
  The maximum total cache size for this repository, in gigabytes. Returned by the lookup.
</ResponseField>
