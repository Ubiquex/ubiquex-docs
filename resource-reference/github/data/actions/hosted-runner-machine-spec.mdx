---
title: "github_actions_hosted_runner_machine_spec"
description: "`github_actions_hosted_runner_machine_spec` lists the real hardware configurations (CPU, memory..."
---

`github_actions_hosted_runner_machine_spec` lists the real hardware configurations (CPU, memory, storage) available for provisioning a GitHub-hosted runner.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_hosted_runner_machine_spec"})

    		ubx.Data(actions.HostedRunnerMachineSpec, "example", actions.HostedRunnerMachineSpecConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { HostedRunnerMachineSpec } from "./local-sdk/github/sdk/typescript/github/data/actions/hosted_runner_machine_spec.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_hosted_runner_machine_spec" });

      data(HostedRunnerMachineSpec, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.actions import HostedRunnerMachineSpec, HostedRunnerMachineSpecConfig

    def describe():
        ubx.intent("look up github_actions_hosted_runner_machine_spec")

        ubx.data(HostedRunnerMachineSpec, "example", HostedRunnerMachineSpecConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="machine_specs" type="list(object)">
  The real hardware machine specs available for provisioning a GitHub-hosted runner. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
