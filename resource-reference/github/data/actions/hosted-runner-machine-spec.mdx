---
title: "github_actions_hosted_runner_machine_spec"
description: "`github_actions_hosted_runner_machine_spec` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_hosted_runner_machine_spec` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_hosted_runner_machine_spec"})

    		ubx.Data(actions.HostedRunnerMachineSpec, "example", actions.HostedRunnerMachineSpecConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HostedRunnerMachineSpec } from "@ubx/sdk-github/github/data/actions/hosted_runner_machine_spec";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_hosted_runner_machine_spec" });

      data(HostedRunnerMachineSpec, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import HostedRunnerMachineSpec, HostedRunnerMachineSpecConfig

    def describe():
        ubx.intent("look up github_actions_hosted_runner_machine_spec")

        ubx.data(HostedRunnerMachineSpec, "example", HostedRunnerMachineSpecConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="machine_specs" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Returned by the lookup.
</ResponseField>
