---
title: "github_actions_hosted_runner_limits"
description: "`github_actions_hosted_runner_limits` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_hosted_runner_limits` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_hosted_runner_limits"})

    		ubx.Data(actions.HostedRunnerLimits, "example", actions.HostedRunnerLimitsConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HostedRunnerLimits } from "@ubx/sdk-github/github/data/actions/hosted_runner_limits";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_hosted_runner_limits" });

      data(HostedRunnerLimits, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import HostedRunnerLimits, HostedRunnerLimitsConfig

    def describe():
        ubx.intent("look up github_actions_hosted_runner_limits")

        ubx.data(HostedRunnerLimits, "example", HostedRunnerLimitsConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="public_ips" type="object">
  Provides details of static public IP limits for GitHub-hosted Hosted Runners Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="current_usage" type="number" required>
      The current number of static public IP addresses in use by Hosted Runners. Required lookup argument.
    </ResponseField>
    <ResponseField name="maximum" type="number" required>
      The maximum number of static public IP addresses that can be used for Hosted Runners. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
