---
title: "github_actions_cache_retention_limit_for_enterprise"
description: "`github_actions_cache_retention_limit_for_enterprise` reads the enterprise-level policy controlling how long an Actions cache entry can sit unused..."
---

`github_actions_cache_retention_limit_for_enterprise` reads the enterprise-level policy controlling how long an Actions cache entry can sit unused before eviction.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_cache_retention_limit_for_enterprise"})

    		ubx.Data(actions.CacheRetentionLimitForEnterprise, "example", actions.CacheRetentionLimitForEnterpriseConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { CacheRetentionLimitForEnterprise } from "./local-sdk/github/sdk/typescript/github/data/actions/cache_retention_limit_for_enterprise.ts";

    export default stack("example", () => {
      intent({
        summary: "look up github_actions_cache_retention_limit_for_enterprise",
      });

      data(CacheRetentionLimitForEnterprise, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.actions import CacheRetentionLimitForEnterprise, CacheRetentionLimitForEnterpriseConfig

    def describe():
        ubx.intent("look up github_actions_cache_retention_limit_for_enterprise")

        ubx.data(CacheRetentionLimitForEnterprise, "example", CacheRetentionLimitForEnterpriseConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_cache_retention_days" type="number">
  For repositories & organizations in an enterprise, the maximum duration, in days, for which caches in a repository may be retained. Returned by the lookup.
</ResponseField>
