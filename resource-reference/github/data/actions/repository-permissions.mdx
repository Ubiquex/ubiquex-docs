---
title: "github_actions_repository_permissions"
description: "`github_actions_repository_permissions` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_repository_permissions` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_repository_permissions"})

    		ubx.Data(actions.RepositoryPermissions, "example", actions.RepositoryPermissionsConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RepositoryPermissions } from "@ubx/sdk-github/github/data/actions/repository_permissions";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_repository_permissions" });

      data(RepositoryPermissions, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import RepositoryPermissions, RepositoryPermissionsConfig

    def describe():
        ubx.intent("look up github_actions_repository_permissions")

        ubx.data(RepositoryPermissions, "example", RepositoryPermissionsConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="allowed_actions" type="string">
  The permissions policy that controls the actions and reusable workflows that are allowed to run. Returned by the lookup.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Whether GitHub Actions is enabled on the repository. Returned by the lookup.
</ResponseField>

<ResponseField name="selected_actions_url" type="string">
  The API URL to use to get or set the actions and reusable workflows that are allowed to run, when `allowed_actions` is set to `selected`. Returned by the lookup.
</ResponseField>

<ResponseField name="sha_pinning_required" type="bool">
  Whether actions must be pinned to a full-length commit SHA. Returned by the lookup.
</ResponseField>
