---
title: "github_actions_fork_pr_contributor_approval"
description: "`github_actions_fork_pr_contributor_approval` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_fork_pr_contributor_approval` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_fork_pr_contributor_approval"})

    		ubx.Data(actions.ForkPrContributorApproval, "example", actions.ForkPrContributorApprovalConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ForkPrContributorApproval } from "@ubx/sdk-github/github/data/actions/fork_pr_contributor_approval";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_fork_pr_contributor_approval" });

      data(ForkPrContributorApproval, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import ForkPrContributorApproval, ForkPrContributorApprovalConfig

    def describe():
        ubx.intent("look up github_actions_fork_pr_contributor_approval")

        ubx.data(ForkPrContributorApproval, "example", ForkPrContributorApprovalConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approval_policy" type="string">
  The policy that controls when fork PR workflows require approval from a maintainer. Returned by the lookup.
</ResponseField>
