---
title: "github_actions_fork_pr_contributor_approval"
description: "`github_actions_fork_pr_contributor_approval` reads the policy controlling which contributors from a forked pull request require manual approval before..."
---

`github_actions_fork_pr_contributor_approval` reads the policy controlling which contributors from a forked pull request require manual approval before their workflow runs are allowed to execute.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_fork_pr_contributor_approval"})

    		ubx.Data(actions.ForkPrContributorApproval, "example", actions.ForkPrContributorApprovalConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { ForkPrContributorApproval } from "./local-sdk/github/sdk/typescript/github/data/actions/fork_pr_contributor_approval.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_fork_pr_contributor_approval" });

      data(ForkPrContributorApproval, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.actions import ForkPrContributorApproval, ForkPrContributorApprovalConfig

    def describe():
        ubx.intent("look up github_actions_fork_pr_contributor_approval")

        ubx.data(ForkPrContributorApproval, "example", ForkPrContributorApprovalConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="approval_policy" type="string">
  The policy that controls when fork PR workflows require approval from a maintainer. Returned by the lookup.
</ResponseField>
