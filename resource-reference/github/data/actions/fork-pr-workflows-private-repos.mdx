---
title: "github_actions_fork_pr_workflows_private_repos"
description: "`github_actions_fork_pr_workflows_private_repos` reads whether workflows triggered by a fork's own pull request are permitted to run at all on..."
---

`github_actions_fork_pr_workflows_private_repos` reads whether workflows triggered by a fork's own pull request are permitted to run at all on a private repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_fork_pr_workflows_private_repos"})

    		ubx.Data(actions.ForkPrWorkflowsPrivateRepos, "example", actions.ForkPrWorkflowsPrivateReposConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ForkPrWorkflowsPrivateRepos } from "@ubx/sdk-github/github/data/actions/fork_pr_workflows_private_repos";

    export default stack("example", () => {
      intent({ summary: "look up github_actions_fork_pr_workflows_private_repos" });

      data(ForkPrWorkflowsPrivateRepos, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import ForkPrWorkflowsPrivateRepos, ForkPrWorkflowsPrivateReposConfig

    def describe():
        ubx.intent("look up github_actions_fork_pr_workflows_private_repos")

        ubx.data(ForkPrWorkflowsPrivateRepos, "example", ForkPrWorkflowsPrivateReposConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="require_approval_for_fork_pr_workflows" type="bool">
  Whether workflows triggered by pull requests from forks require approval from a repository administrator to run. Returned by the lookup.
</ResponseField>

<ResponseField name="run_workflows_from_fork_pull_requests" type="bool">
  Whether workflows triggered by pull requests from forks are allowed to run on private repositories. Returned by the lookup.
</ResponseField>

<ResponseField name="send_secrets_and_variables" type="bool">
  Whether to make secrets and variables available to workflows triggered by pull requests from forks. Returned by the lookup.
</ResponseField>

<ResponseField name="send_write_tokens_to_workflows" type="bool">
  Whether GitHub Actions can create pull requests or submit approving pull request reviews from a workflow triggered by a fork pull request. Returned by the lookup.
</ResponseField>
