---
title: "github_actions_cache_storage_limit_for_enterprise"
description: "`github_actions_cache_storage_limit_for_enterprise` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_actions_cache_storage_limit_for_enterprise` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	actions "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/actions"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_actions_cache_storage_limit_for_enterprise"})

    		ubx.Data(actions.CacheStorageLimitForEnterprise, "example", actions.CacheStorageLimitForEnterpriseConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CacheStorageLimitForEnterprise } from "@ubx/sdk-github/github/data/actions/cache_storage_limit_for_enterprise";

    export default stack("example", () => {
      intent({
        summary: "look up github_actions_cache_storage_limit_for_enterprise",
      });

      data(CacheStorageLimitForEnterprise, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.actions import CacheStorageLimitForEnterprise, CacheStorageLimitForEnterpriseConfig

    def describe():
        ubx.intent("look up github_actions_cache_storage_limit_for_enterprise")

        ubx.data(CacheStorageLimitForEnterprise, "example", CacheStorageLimitForEnterpriseConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="max_cache_size_gb" type="number">
  For repositories & organizations in an enterprise, the maximum size limit for the sum of all caches in a repository, in gigabytes. Returned by the lookup.
</ResponseField>
