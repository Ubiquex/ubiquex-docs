---
title: "github_root"
description: "`github_root` reads the real root of the GitHub REST API -- the top-level map of URL templates every other real endpoint hangs off of."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	root "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/root"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_root"})

    		ubx.Data(root.Root, "example", root.RootConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Root } from "@ubx/sdk-github/github/data/root/root";

    export default stack("example", () => {
      intent({ summary: "look up github_root" });

      data(Root, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.root import Root, RootConfig

    def describe():
        ubx.intent("look up github_root")

        ubx.data(Root, "example", RootConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="authorizations_url" type="string">
  The real API URL for the authenticated user's own OAuth authorizations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="code_search_url" type="string">
  The real API URL template for GitHub's own code search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="commit_search_url" type="string">
  The real API URL template for GitHub's own commit search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_authorizations_html_url" type="string">
  The real web URL for the authenticated user's own OAuth authorizations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_repositories_url" type="string">
  The real API URL template for the authenticated user's own repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_url" type="string">
  The real API URL for the authenticated user's own profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="emails_url" type="string">
  The real API URL for the authenticated user's own email addresses. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="emojis_url" type="string">
  The real API URL for GitHub's own available emoji. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="events_url" type="string">
  The real API URL for this object's own events. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="feeds_url" type="string">
  The real API URL for GitHub's own available Atom feeds. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="followers_url" type="string">
  The real API URL for this user's own followers. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="following_url" type="string">
  The real API URL template for this user's own followed accounts. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="gists_url" type="string">
  The real API URL template for this user's own gists. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="hub_url" type="string">
  The real API URL for GitHub's own legacy PubSubHubbub hub. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="issue_search_url" type="string">
  The real API URL template for GitHub's own issue search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="issues_url" type="string">
  The real API URL template for this object's own issues. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="keys_url" type="string">
  The real API URL template for a repository's own deploy keys. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="label_search_url" type="string">
  The real API URL template for GitHub's own label search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="notifications_url" type="string">
  The real API URL template for a repository's own notification subscriptions. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_repositories_url" type="string">
  The real API URL template for an organization's own repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_teams_url" type="string">
  The real API URL for an organization's own teams. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="organization_url" type="string">
  The real API URL for the organization this object belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="public_gists_url" type="string">
  The real API URL template for a user's own public gists. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="rate_limit_url" type="string">
  The real API URL for checking the caller's own current rate limit. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repository_search_url" type="string">
  The real API URL template for GitHub's own repository search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repository_url" type="string">
  The real API URL for the repository this object belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="starred_gists_url" type="string">
  The real API URL template for a user's own starred gists. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="starred_url" type="string">
  The real API URL template for a user's own starred repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="topic_search_url" type="string">
  The real API URL template for GitHub's own topic search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_organizations_url" type="string">
  The real API URL template for a user's own organizations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_repositories_url" type="string">
  The real API URL template for a user's own repositories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_search_url" type="string">
  The real API URL template for GitHub's own user search. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_url" type="string">
  The real API URL template for looking up a user by login. **(AI-inferred)** Returned by the lookup.
</ResponseField>
