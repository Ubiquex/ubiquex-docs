---
title: "github_ruleset"
description: "`github_ruleset` looks up a real, already-defined repository or organization ruleset and its own current rules."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	ruleset "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/ruleset"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_ruleset"})

    		ubx.Data(ruleset.Ruleset, "example", ruleset.RulesetConfig{
    			Enterprise: "example",
    			RulesetId:  1,
    			Page:       1,
    			PerPage:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Ruleset } from "./local-sdk/github/sdk/typescript/github/data/ruleset/ruleset.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_ruleset" });

      data(Ruleset, "example", {
        enterprise: "example",
        rulesetId: 1,
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.ruleset import Ruleset, RulesetConfig

    def describe():
        ubx.intent("look up github_ruleset")

        ubx.data(Ruleset, "example", RulesetConfig(
            enterprise="example",
            ruleset_id=1,
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ruleset_id" type="number" required>
  The real, numeric ID of this ruleset. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
