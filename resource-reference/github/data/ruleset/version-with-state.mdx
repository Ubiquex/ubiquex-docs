---
title: "github_ruleset_version_with_state"
description: "`github_ruleset_version_with_state` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_ruleset_version_with_state` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ruleset "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/ruleset"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_ruleset_version_with_state"})

    		ubx.Data(ruleset.VersionWithState, "example", ruleset.VersionWithStateConfig{
    			Enterprise: "example",
    			RulesetId:  1,
    			VersionId:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { VersionWithState } from "@ubx/sdk-github/github/data/ruleset/version_with_state";

    export default stack("example", () => {
      intent({ summary: "look up github_ruleset_version_with_state" });

      data(VersionWithState, "example", {
        enterprise: "example",
        rulesetId: 1,
        versionId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.ruleset import VersionWithState, VersionWithStateConfig

    def describe():
        ubx.intent("look up github_ruleset_version_with_state")

        ubx.data(VersionWithState, "example", VersionWithStateConfig(
            enterprise="example",
            ruleset_id=1,
            version_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="ruleset_id" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version_id" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
