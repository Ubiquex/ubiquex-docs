---
title: "github_configuration"
description: "`github_configuration` looks up a real, enterprise-level security configuration and its own current rollout status across member organizations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	configuration "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/configuration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_configuration"})

    		ubx.Data(configuration.Configuration, "example", configuration.ConfigurationConfig{
    			ConfigurationId: 1,
    			Enterprise:      "example",
    			After:           "example",
    			Before:          "example",
    			PerPage:         1,
    			Status:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Configuration } from "./local-sdk/github/sdk/typescript/github/data/configuration/configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_configuration" });

      data(Configuration, "example", {
        configurationId: 1,
        enterprise: "example",
        after: "example",
        before: "example",
        perPage: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.configuration import Configuration, ConfigurationConfig

    def describe():
        ubx.intent("look up github_configuration")

        ubx.data(Configuration, "example", ConfigurationConfig(
            configuration_id=1,
            enterprise="example",
            after="example",
            before="example",
            per_page=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Only return results after this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Only return results before this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="configuration_id" type="number" required>
  The real, numeric ID of this configuration. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
