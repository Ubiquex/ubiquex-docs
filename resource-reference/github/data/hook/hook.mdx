---
title: "github_hook"
description: "`github_hook` looks up a real, already-configured repository webhook -- which events it's subscribed to and where it delivers them."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	hook "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/hook"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_hook"})

    		ubx.Data(hook.Hook, "example", hook.HookConfig{
    			Cursor:  "example",
    			PerPage: 1,
    			Status:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Hook } from "./local-sdk/github/sdk/typescript/github/data/hook/hook.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_hook" });

      data(Hook, "example", {
        cursor: "example",
        perPage: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.hook import Hook, HookConfig

    def describe():
        ubx.intent("look up github_hook")

        ubx.data(Hook, "example", HookConfig(
            cursor="example",
            per_page=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cursor" type="string">
  An opaque pagination cursor from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  The real, current status of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
