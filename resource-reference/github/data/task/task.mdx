---
title: "github_task"
description: "`github_task` looks up one real repository's own already-recorded result within a broader Multi-Repository Variant Analysis run -- its analysis status..."
---

`github_task` looks up one real repository's own already-recorded result within a broader Multi-Repository Variant Analysis run -- its analysis status, its result count, and the reason it failed if it did.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	task "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/task"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_task"})

    		ubx.Data(task.Task, "example", task.TaskConfig{
    			TaskId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Task } from "./local-sdk/github/sdk/typescript/github/data/task/task.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_task" });

      data(Task, "example", {
        taskId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.task import Task, TaskConfig

    def describe():
        ubx.intent("look up github_task")

        ubx.data(Task, "example", TaskConfig(
            task_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="task_id" type="string" required>
  The real, unique identifier of this task. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
