---
title: "github_gist"
description: "`github_gist` looks up a real gist -- a real, shareable snippet of one or more files, versioned like a small repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	gist "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/gist"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_gist"})

    		ubx.Data(gist.Gist, "example", gist.GistConfig{
    			Page:    1,
    			PerPage: 1,
    			Since:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Gist } from "./local-sdk/github/sdk/typescript/github/data/gist/gist.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_gist" });

      data(Gist, "example", {
        page: 1,
        perPage: 1,
        since: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.gist import Gist, GistConfig

    def describe():
        ubx.intent("look up github_gist")

        ubx.data(Gist, "example", GistConfig(
            page=1,
            per_page=1,
            since="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="since" type="string">
  Only return results updated at or after this real timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
