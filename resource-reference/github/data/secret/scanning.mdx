---
title: "github_secret_scanning"
description: "`github_secret_scanning` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_secret_scanning` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	secret "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/secret"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_secret_scanning"})

    		ubx.Data(secret.Scanning, "example", secret.ScanningConfig{
    			Enterprise:         "example",
    			After:              "example",
    			Assignee:           "example",
    			Before:             "example",
    			Direction:          "example",
    			ExcludeProviders:   "example",
    			ExcludeSecretTypes: "example",
    			HideSecret:         true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Scanning } from "@ubx/sdk-github/github/data/secret/scanning";

    export default stack("example", () => {
      intent({ summary: "look up github_secret_scanning" });

      data(Scanning, "example", {
        enterprise: "example",
        after: "example",
        assignee: "example",
        before: "example",
        direction: "example",
        excludeProviders: "example",
        excludeSecretTypes: "example",
        hideSecret: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.secret import Scanning, ScanningConfig

    def describe():
        ubx.intent("look up github_secret_scanning")

        ubx.data(Scanning, "example", ScanningConfig(
            enterprise="example",
            after="example",
            assignee="example",
            before="example",
            direction="example",
            exclude_providers="example",
            exclude_secret_types="example",
            hide_secret=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="assignee" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="exclude_providers" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="exclude_secret_types" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="hide_secret" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="included_metadata" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_bypassed" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_multi_repo" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_publicly_leaked" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="owner_email_hash" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="providers" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resolution" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="secret_type" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="validity" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
