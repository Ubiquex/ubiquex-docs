---
title: "github_budget"
description: "`github_budget` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_budget` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	budget "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/budget"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_budget"})

    		ubx.Data(budget.Budget, "example", budget.BudgetConfig{
    			Enterprise: "example",
    			Page:       1,
    			PerPage:    1,
    			Scope:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Budget } from "@ubx/sdk-github/github/data/budget/budget";

    export default stack("example", () => {
      intent({ summary: "look up github_budget" });

      data(Budget, "example", {
        enterprise: "example",
        page: 1,
        perPage: 1,
        scope: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.budget import Budget, BudgetConfig

    def describe():
        ubx.intent("look up github_budget")

        ubx.data(Budget, "example", BudgetConfig(
            enterprise="example",
            page=1,
            per_page=1,
            scope="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="scope" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="budgets" type="list(object)">
  Array of budget objects for the enterprise Returned by the lookup.
</ResponseField>

<ResponseField name="effective_budget" type="object">
  Effective user-level budget details returned when the response is scoped with the `user` query parameter. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="budget_amount" type="number" required>
      The budget amount for the effective budget. Required lookup argument.
    </ResponseField>
    <ResponseField name="consumed_amount" type="number" required>
      The consumed amount for the specified user within the effective budget. Required lookup argument.
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the effective budget. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="has_next_page" type="bool">
  Indicates if there are more pages of results available (maps to hasNextPage from billing platform) Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Total number of budgets matching the query Returned by the lookup.
</ResponseField>

<ResponseField name="user" type="string">
  Returned by the lookup.
</ResponseField>
