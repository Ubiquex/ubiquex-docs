---
title: "github_workflow"
description: "`github_workflow` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_workflow` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workflow "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/workflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_workflow"})

    		ubx.Data(workflow.Workflow, "example", workflow.WorkflowConfig{
    			Owner:   "example",
    			Repo:    "example",
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Workflow } from "@ubx/sdk-github/github/data/workflow/workflow";

    export default stack("example", () => {
      intent({ summary: "look up github_workflow" });

      data(Workflow, "example", {
        owner: "example",
        repo: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.workflow import Workflow, WorkflowConfig

    def describe():
        ubx.intent("look up github_workflow")

        ubx.data(Workflow, "example", WorkflowConfig(
            owner="example",
            repo="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="total_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="workflows" type="list(object)">
  Returned by the lookup.
</ResponseField>
