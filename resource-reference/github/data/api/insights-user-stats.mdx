---
title: "github_api_insights_user_stats"
description: "`github_api_insights_user_stats` reads real, aggregated REST API usage statistics broken down by individual user, from API Insights."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_api_insights_user_stats"})

    		ubx.Data(api.InsightsUserStats, "example", api.InsightsUserStatsConfig{
    			MinTimestamp:       "example",
    			Org:                "example",
    			UserId:             "example",
    			ActorNameSubstring: "example",
    			Direction:          "example",
    			MaxTimestamp:       "example",
    			Page:               1,
    			PerPage:            1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsUserStats } from "@ubx/sdk-github/github/data/api/insights_user_stats";

    export default stack("example", () => {
      intent({ summary: "look up github_api_insights_user_stats" });

      data(InsightsUserStats, "example", {
        minTimestamp: "example",
        org: "example",
        userId: "example",
        actorNameSubstring: "example",
        direction: "example",
        maxTimestamp: "example",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.api import InsightsUserStats, InsightsUserStatsConfig

    def describe():
        ubx.intent("look up github_api_insights_user_stats")

        ubx.data(InsightsUserStats, "example", InsightsUserStatsConfig(
            min_timestamp="example",
            org="example",
            user_id="example",
            actor_name_substring="example",
            direction="example",
            max_timestamp="example",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="actor_name_substring" type="string">
  Restricts results to actors whose own login contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_timestamp" type="string">
  The latest real timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="min_timestamp" type="string" required>
  The earliest real timestamp to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="list(string)">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="user_id" type="string" required>
  The real, numeric ID of the user this result refers to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
