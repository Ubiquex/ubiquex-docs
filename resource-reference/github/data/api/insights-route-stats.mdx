---
title: "github_api_insights_route_stats"
description: "`github_api_insights_route_stats` reads real, aggregated REST API usage statistics broken down by endpoint route..."
---

`github_api_insights_route_stats` reads real, aggregated REST API usage statistics broken down by endpoint route, for an enterprise with API Insights enabled.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_api_insights_route_stats"})

    		ubx.Data(api.InsightsRouteStats, "example", api.InsightsRouteStatsConfig{
    			ActorId:           1,
    			ActorType:         "example",
    			MinTimestamp:      "example",
    			Org:               "example",
    			ApiRouteSubstring: "example",
    			Direction:         "example",
    			MaxTimestamp:      "example",
    			Page:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { InsightsRouteStats } from "./local-sdk/github/sdk/typescript/github/data/api/insights_route_stats.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_api_insights_route_stats" });

      data(InsightsRouteStats, "example", {
        actorId: 1,
        actorType: "example",
        minTimestamp: "example",
        org: "example",
        apiRouteSubstring: "example",
        direction: "example",
        maxTimestamp: "example",
        page: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.api import InsightsRouteStats, InsightsRouteStatsConfig

    def describe():
        ubx.intent("look up github_api_insights_route_stats")

        ubx.data(InsightsRouteStats, "example", InsightsRouteStatsConfig(
            actor_id=1,
            actor_type="example",
            min_timestamp="example",
            org="example",
            api_route_substring="example",
            direction="example",
            max_timestamp="example",
            page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="actor_id" type="number" required>
  The real, numeric ID of the actor that performed this action. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="actor_type" type="string" required>
  Whether the actor that performed this action was a user, an app, or another kind of identity. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="api_route_substring" type="string">
  Restricts results to API routes whose own path contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_timestamp" type="string">
  The latest real timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="min_timestamp" type="string" required>
  The earliest real timestamp to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="list(string)">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
