---
title: "github_api_insights_route_stats"
description: "`github_api_insights_route_stats` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_api_insights_route_stats` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_api_insights_route_stats"})

    		ubx.Data(api.InsightsRouteStats, "example", api.InsightsRouteStatsConfig{
    			ActorId:           1,
    			ActorType:         "example",
    			MinTimestamp:      "example",
    			Org:               "example",
    			ApiRouteSubstring: "example",
    			Direction:         "example",
    			MaxTimestamp:      "example",
    			Page:              1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsRouteStats } from "@ubx/sdk-github/github/data/api/insights_route_stats";

    export default stack("example", () => {
      intent({ summary: "look up github_api_insights_route_stats" });

      data(InsightsRouteStats, "example", {
        actorId: 1,
        actorType: "example",
        minTimestamp: "example",
        org: "example",
        apiRouteSubstring: "example",
        direction: "example",
        maxTimestamp: "example",
        page: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.api import InsightsRouteStats, InsightsRouteStatsConfig

    def describe():
        ubx.intent("look up github_api_insights_route_stats")

        ubx.data(InsightsRouteStats, "example", InsightsRouteStatsConfig(
            actor_id=1,
            actor_type="example",
            min_timestamp="example",
            org="example",
            api_route_substring="example",
            direction="example",
            max_timestamp="example",
            page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="actor_id" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="actor_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="api_route_substring" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="max_timestamp" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="min_timestamp" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="list(string)">
  Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
