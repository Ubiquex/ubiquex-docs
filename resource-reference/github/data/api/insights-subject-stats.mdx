---
title: "github_api_insights_subject_stats"
description: "`github_api_insights_subject_stats` reads real, aggregated REST API usage statistics broken down by the actor (user, app, or token) making the calls."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	api "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/api"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_api_insights_subject_stats"})

    		ubx.Data(api.InsightsSubjectStats, "example", api.InsightsSubjectStatsConfig{
    			MinTimestamp:         "example",
    			Org:                  "example",
    			Direction:            "example",
    			MaxTimestamp:         "example",
    			Page:                 1,
    			PerPage:              1,
    			Sort:                 []string{"example"},
    			SubjectNameSubstring: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightsSubjectStats } from "@ubx/sdk-github/github/data/api/insights_subject_stats";

    export default stack("example", () => {
      intent({ summary: "look up github_api_insights_subject_stats" });

      data(InsightsSubjectStats, "example", {
        minTimestamp: "example",
        org: "example",
        direction: "example",
        maxTimestamp: "example",
        page: 1,
        perPage: 1,
        sort: ["example"],
        subjectNameSubstring: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.api import InsightsSubjectStats, InsightsSubjectStatsConfig

    def describe():
        ubx.intent("look up github_api_insights_subject_stats")

        ubx.data(InsightsSubjectStats, "example", InsightsSubjectStatsConfig(
            min_timestamp="example",
            org="example",
            direction="example",
            max_timestamp="example",
            page=1,
            per_page=1,
            sort=["example"],
            subject_name_substring="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="max_timestamp" type="string">
  The latest real timestamp to include in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="min_timestamp" type="string" required>
  The earliest real timestamp to include in the results. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="list(string)">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="subject_name_substring" type="string">
  Restricts results to subjects whose own name contains this substring. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
