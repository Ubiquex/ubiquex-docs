---
title: "github_stubbed"
description: "`github_stubbed` is a paginated placeholder shape carried in the real API schema with no further real, distinguishing field content beyond pagination..."
---

`github_stubbed` is a paginated placeholder shape carried in the real API schema with no further real, distinguishing field content beyond pagination -- not a business-domain lookup like the rest of this reference.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	stubbed "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/stubbed"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_stubbed"})

    		ubx.Data(stubbed.Stubbed, "example", stubbed.StubbedConfig{
    			Page:    1,
    			PerPage: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Stubbed } from "@ubx/sdk-github/github/data/stubbed/stubbed";

    export default stack("example", () => {
      intent({ summary: "look up github_stubbed" });

      data(Stubbed, "example", {
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.stubbed import Stubbed, StubbedConfig

    def describe():
        ubx.intent("look up github_stubbed")

        ubx.data(Stubbed, "example", StubbedConfig(
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
