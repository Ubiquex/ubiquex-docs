---
title: "github_thread_subscription"
description: "`github_thread_subscription` reads whether the authenticated user is currently subscribed to a specific notification thread."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	thread "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/thread"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_thread_subscription"})

    		ubx.Data(thread.Subscription, "example", thread.SubscriptionConfig{
    			ThreadId: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Subscription } from "./local-sdk/github/sdk/typescript/github/data/thread/subscription.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_thread_subscription" });

      data(Subscription, "example", {
        threadId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.thread import Subscription, SubscriptionConfig

    def describe():
        ubx.intent("look up github_thread_subscription")

        ubx.data(Subscription, "example", SubscriptionConfig(
            thread_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="thread_id" type="number" required>
  The real, numeric ID of this notification thread. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="created_at" type="string">
  The real timestamp this object was created. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="ignored" type="bool">
  Whether this alert or dependency has been marked as ignored. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="reason" type="string">
  The real reason this notification, invitation, or event exists. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repository_url" type="string">
  The real API URL for the repository this object belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="subscribed" type="bool">
  Whether the authenticated user is currently subscribed to this thread or repository. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="thread_url" type="string">
  The real API URL for this notification's own thread. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
