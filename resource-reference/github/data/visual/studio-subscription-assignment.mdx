---
title: "github_visual_studio_subscription_assignment"
description: "`github_visual_studio_subscription_assignment` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_visual_studio_subscription_assignment` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	visual "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/visual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_visual_studio_subscription_assignment"})

    		ubx.Data(visual.StudioSubscriptionAssignment, "example", visual.StudioSubscriptionAssignmentConfig{
    			Enterprise:      "example",
    			IsUnmatchedOnly: true,
    			Page:            1,
    			PerPage:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { StudioSubscriptionAssignment } from "@ubx/sdk-github/github/data/visual/studio_subscription_assignment";

    export default stack("example", () => {
      intent({ summary: "look up github_visual_studio_subscription_assignment" });

      data(StudioSubscriptionAssignment, "example", {
        enterprise: "example",
        isUnmatchedOnly: true,
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.visual import StudioSubscriptionAssignment, StudioSubscriptionAssignmentConfig

    def describe():
        ubx.intent("look up github_visual_studio_subscription_assignment")

        ubx.data(StudioSubscriptionAssignment, "example", StudioSubscriptionAssignmentConfig(
            enterprise="example",
            is_unmatched_only=True,
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="is_unmatched_only" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="total_count" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="visual_studio_subscriptions" type="list(object)">
  Returned by the lookup.
</ResponseField>
