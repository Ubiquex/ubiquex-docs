---
title: "github_feed"
description: "`github_feed` reads the real Atom feed URLs available for a user or repository's own activity -- what a feed reader would subscribe to."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	feed "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/feed"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_feed"})

    		ubx.Data(feed.Feed, "example", feed.FeedConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Feed } from "@ubx/sdk-github/github/data/feed/feed";

    export default stack("example", () => {
      intent({ summary: "look up github_feed" });

      data(Feed, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.feed import Feed, FeedConfig

    def describe():
        ubx.intent("look up github_feed")

        ubx.data(Feed, "example", FeedConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="_links" type="object">
  A real map of related API and web URLs for this object. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="current_user" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_user_actor" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_user_organization" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_user_organizations" type="list(object)">
      Optional lookup argument.
    </ResponseField>
    <ResponseField name="current_user_public" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="repository_discussions" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="repository_discussions_category" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="security_advisories" type="object">
      Hypermedia Link with Type Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="timeline" type="object" required>
      Hypermedia Link with Type Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="user" type="object" required>
      Hypermedia Link with Type Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          Required lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="current_user_actor_url" type="string">
  The real API URL for the authenticated user's own event feed. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_organization_url" type="string">
  The real API URL template for the authenticated user's own organizations. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_organization_urls" type="list(string)">
  The real API URLs for each organization the authenticated user belongs to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_public_url" type="string">
  The real API URL for the authenticated user's own public profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="current_user_url" type="string">
  The real API URL for the authenticated user's own profile. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="repository_discussions_category_url" type="string">
  A feed of discussions for a given repository and category. Returned by the lookup.
</ResponseField>

<ResponseField name="repository_discussions_url" type="string">
  A feed of discussions for a given repository. Returned by the lookup.
</ResponseField>

<ResponseField name="security_advisories_url" type="string">
  The real API URL for this account's own security advisories. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="timeline_url" type="string">
  The real API URL for this object's own event timeline. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="user_url" type="string">
  The real API URL template for looking up a user by login. **(AI-inferred)** Returned by the lookup.
</ResponseField>
