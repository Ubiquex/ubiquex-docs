---
title: "github_org"
description: "`github_org` looks up a real organization -- its own name, billing plan, and member/repository counts..."
---

`github_org` looks up a real organization -- its own name, billing plan, and member/repository counts, the compact shape returned alongside other resources.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	org "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/org"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_org"})

    		ubx.Data(org.Org, "example", org.OrgConfig{
    			Org:     "example",
    			After:   "example",
    			Before:  "example",
    			Include: "example",
    			Order:   "example",
    			PerPage: 1,
    			Phrase:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Org } from "./local-sdk/github/sdk/typescript/github/data/org/org.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_org" });

      data(Org, "example", {
        org: "example",
        after: "example",
        before: "example",
        include: "example",
        order: "example",
        perPage: 1,
        phrase: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.org import Org, OrgConfig

    def describe():
        ubx.intent("look up github_org")

        ubx.data(Org, "example", OrgConfig(
            org="example",
            after="example",
            before="example",
            include="example",
            order="example",
            per_page=1,
            phrase="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Only return results after this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Only return results before this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  The real organization this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="phrase" type="string">
  The real search query string, in audit log search syntax. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
