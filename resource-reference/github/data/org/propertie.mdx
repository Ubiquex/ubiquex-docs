---
title: "github_org_propertie"
description: "`github_org_propertie` looks up a real, enterprise-level custom property definition available for organizations to apply."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	org "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/org"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_org_propertie"})

    		ubx.Data(org.Propertie, "example", org.PropertieConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Propertie } from "@ubx/sdk-github/github/data/org/propertie";

    export default stack("example", () => {
      intent({ summary: "look up github_org_propertie" });

      data(Propertie, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.org import Propertie, PropertieConfig

    def describe():
        ubx.intent("look up github_org_propertie")

        ubx.data(Propertie, "example", PropertieConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
