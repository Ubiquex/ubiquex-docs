---
title: "github_code_scanning"
description: "`github_code_scanning` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_code_scanning` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_code_scanning"})

    		ubx.Data(code.Scanning, "example", code.ScanningConfig{
    			Enterprise: "example",
    			After:      "example",
    			Assignees:  "example",
    			Before:     "example",
    			Direction:  "example",
    			Page:       1,
    			PerPage:    1,
    			Sort:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Scanning } from "@ubx/sdk-github/github/data/code/scanning";

    export default stack("example", () => {
      intent({ summary: "look up github_code_scanning" });

      data(Scanning, "example", {
        enterprise: "example",
        after: "example",
        assignees: "example",
        before: "example",
        direction: "example",
        page: 1,
        perPage: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.code import Scanning, ScanningConfig

    def describe():
        ubx.intent("look up github_code_scanning")

        ubx.data(Scanning, "example", ScanningConfig(
            enterprise="example",
            after="example",
            assignees="example",
            before="example",
            direction="example",
            page=1,
            per_page=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="assignees" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="direction" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  State of a code scanning alert. Optional lookup argument.
</ResponseField>

<ResponseField name="tool_guid" type="string">
  The GUID of the tool used to generate the code scanning analysis, if provided in the uploaded SARIF data. Optional lookup argument.
</ResponseField>

<ResponseField name="tool_name" type="string">
  The name of the tool used to generate the code scanning analysis. Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
