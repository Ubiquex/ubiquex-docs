---
title: "github_code_quality_setup"
description: "`github_code_quality_setup` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_code_quality_setup` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_code_quality_setup"})

    		ubx.Data(code.QualitySetup, "example", code.QualitySetupConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QualitySetup } from "@ubx/sdk-github/github/data/code/quality_setup";

    export default stack("example", () => {
      intent({ summary: "look up github_code_quality_setup" });

      data(QualitySetup, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.code import QualitySetup, QualitySetupConfig

    def describe():
        ubx.intent("look up github_code_quality_setup")

        ubx.data(QualitySetup, "example", QualitySetupConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="ai_findings_option" type="string">
  The AI findings configuration for the repository. Returned by the lookup.
</ResponseField>

<ResponseField name="languages" type="list(string)">
  Languages to be analyzed. Returned by the lookup.
</ResponseField>

<ResponseField name="runner_label" type="string">
  Runner label to be used if the runner type is labeled. Returned by the lookup.
</ResponseField>

<ResponseField name="runner_type" type="string">
  Runner type to be used. Returned by the lookup.
</ResponseField>

<ResponseField name="schedule" type="string">
  The frequency of the periodic analysis. Returned by the lookup.
</ResponseField>

<ResponseField name="state" type="string">
  Code quality setup has been configured or not. Returned by the lookup.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Timestamp of latest configuration update. Returned by the lookup.
</ResponseField>
