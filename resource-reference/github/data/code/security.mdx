---
title: "github_code_security"
description: "`github_code_security` looks up a real, already-configured bundle of security features -- Dependabot alerts, CodeQL code scanning..."
---

`github_code_security` looks up a real, already-configured bundle of security features -- Dependabot alerts, CodeQL code scanning, secret protection -- as a single named, centrally-managed policy.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_code_security"})

    		ubx.Data(code.Security, "example", code.SecurityConfig{
    			Enterprise: "example",
    			After:      "example",
    			Before:     "example",
    			PerPage:    1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Security } from "./local-sdk/github/sdk/typescript/github/data/code/security.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_code_security" });

      data(Security, "example", {
        enterprise: "example",
        after: "example",
        before: "example",
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.code import Security, SecurityConfig

    def describe():
        ubx.intent("look up github_code_security")

        ubx.data(Security, "example", SecurityConfig(
            enterprise="example",
            after="example",
            before="example",
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Only return results after this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Only return results before this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
