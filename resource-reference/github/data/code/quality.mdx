---
title: "github_code_quality"
description: "`github_code_quality` reads a repository's own real, current code quality analysis summary."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_code_quality"})

    		ubx.Data(code.Quality, "example", code.QualityConfig{
    			Owner:     "example",
    			Repo:      "example",
    			After:     "example",
    			Before:    "example",
    			Direction: "example",
    			PerPage:   1,
    			State:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Quality } from "@ubx/sdk-github/github/data/code/quality";

    export default stack("example", () => {
      intent({ summary: "look up github_code_quality" });

      data(Quality, "example", {
        owner: "example",
        repo: "example",
        after: "example",
        before: "example",
        direction: "example",
        perPage: 1,
        state: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.code import Quality, QualityConfig

    def describe():
        ubx.intent("look up github_code_quality")

        ubx.data(Quality, "example", QualityConfig(
            owner="example",
            repo="example",
            after="example",
            before="example",
            direction="example",
            per_page=1,
            state="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="after" type="string">
  Only return results after this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="before" type="string">
  Only return results before this real cursor or timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="state" type="string">
  The real, current state of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
