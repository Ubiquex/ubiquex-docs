---
title: "github_code_search_result_item"
description: "`github_code_search_result_item` looks up one real match from a GitHub code search -- the file, repository, and matching text fragment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_code_search_result_item"})

    		ubx.Data(code.SearchResultItem, "example", code.SearchResultItemConfig{
    			Q:       "example",
    			Order:   "example",
    			Page:    1,
    			PerPage: 1,
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { SearchResultItem } from "./local-sdk/github/sdk/typescript/github/data/code/search_result_item.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_code_search_result_item" });

      data(SearchResultItem, "example", {
        q: "example",
        order: "example",
        page: 1,
        perPage: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.code import SearchResultItem, SearchResultItemConfig

    def describe():
        ubx.intent("look up github_code_search_result_item")

        ubx.data(SearchResultItem, "example", SearchResultItemConfig(
            q="example",
            order="example",
            page=1,
            per_page=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="order" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="q" type="string" required>
  The real search query string. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="incomplete_results" type="bool">
  Whether the search timed out before scanning every possible match, making the result set incomplete. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="items" type="list(object)">
  The list of results for this page of the response. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
