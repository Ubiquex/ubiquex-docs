---
title: "github_code_of_conduct"
description: "`github_code_of_conduct` looks up one of GitHub's own real, published code-of-conduct templates (Contributor Covenant and similar) available for..."
---

`github_code_of_conduct` looks up one of GitHub's own real, published code-of-conduct templates (Contributor Covenant and similar) available for a repository to adopt.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_code_of_conduct"})

    		ubx.Data(code.OfConduct, "example", code.OfConductConfig{
    			Key: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { OfConduct } from "./local-sdk/github/sdk/typescript/github/data/code/of_conduct.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_code_of_conduct" });

      data(OfConduct, "example", {
        key: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.code import OfConduct, OfConductConfig

    def describe():
        ubx.intent("look up github_code_of_conduct")

        ubx.data(OfConduct, "example", OfConductConfig(
            key="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="key" type="string" required>
  The real, unique key identifying this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="body" type="string">
  The real, free-text body content of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="html_url" type="string">
  The real, browsable web URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
