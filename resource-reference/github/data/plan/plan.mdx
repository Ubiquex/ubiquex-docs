---
title: "github_plan"
description: "`github_plan` reads a user or organization's own real current GitHub billing plan -- its name, seat count, and private repository allowance."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	plan "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/plan"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_plan"})

    		ubx.Data(plan.Plan, "example", plan.PlanConfig{
    			PlanId:    1,
    			Direction: "example",
    			Page:      1,
    			PerPage:   1,
    			Sort:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Plan } from "@ubx/sdk-github/github/data/plan/plan";

    export default stack("example", () => {
      intent({ summary: "look up github_plan" });

      data(Plan, "example", {
        planId: 1,
        direction: "example",
        page: 1,
        perPage: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.plan import Plan, PlanConfig

    def describe():
        ubx.intent("look up github_plan")

        ubx.data(Plan, "example", PlanConfig(
            plan_id=1,
            direction="example",
            page=1,
            per_page=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="plan_id" type="number" required>
  The real, unique identifier of this billing plan. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
