---
title: "github_scim_enterprise_user_response"
description: "`github_scim_enterprise_user_response` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_scim_enterprise_user_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scim "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/scim"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_scim_enterprise_user_response"})

    		ubx.Data(scim.EnterpriseUserResponse, "example", scim.EnterpriseUserResponseConfig{
    			Enterprise: "example",
    			Count:      1,
    			Filter:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EnterpriseUserResponse } from "@ubx/sdk-github/github/data/scim/enterprise_user_response";

    export default stack("example", () => {
      intent({ summary: "look up github_scim_enterprise_user_response" });

      data(EnterpriseUserResponse, "example", {
        enterprise: "example",
        count: 1,
        filter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.scim import EnterpriseUserResponse, EnterpriseUserResponseConfig

    def describe():
        ubx.intent("look up github_scim_enterprise_user_response")

        ubx.data(EnterpriseUserResponse, "example", EnterpriseUserResponseConfig(
            enterprise="example",
            count=1,
            filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="count" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="items_per_page" type="number">
  Number of objects per page Returned by the lookup.
</ResponseField>

<ResponseField name="resources" type="list(object)">
  Information about each provisioned account. Returned by the lookup.
</ResponseField>

<ResponseField name="schemas" type="list(string)">
  The URIs that are used to indicate the namespaces of the list SCIM schemas. Returned by the lookup.
</ResponseField>

<ResponseField name="start_index" type="number">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_results" type="number">
  Number of results found Returned by the lookup.
</ResponseField>
