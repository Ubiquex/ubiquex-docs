---
title: "github_enterprise_role"
description: "`github_enterprise_role` looks up a real, custom enterprise-level permission role defined for an enterprise's own owners to assign."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	enterprise "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/enterprise"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_enterprise_role"})

    		ubx.Data(enterprise.Role, "example", enterprise.RoleConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Role } from "./local-sdk/github/sdk/typescript/github/data/enterprise/role.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_enterprise_role" });

      data(Role, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.enterprise import Role, RoleConfig

    def describe():
        ubx.intent("look up github_enterprise_role")

        ubx.data(Role, "example", RoleConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="roles" type="list(object)">
  The list of enterprise roles available to the enterprise. Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of enterprise roles available to the enterprise. Returned by the lookup.
</ResponseField>
