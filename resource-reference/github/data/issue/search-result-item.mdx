---
title: "github_issue_search_result_item"
description: "`github_issue_search_result_item` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_issue_search_result_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	issue "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/issue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_issue_search_result_item"})

    		ubx.Data(issue.SearchResultItem, "example", issue.SearchResultItemConfig{
    			Q:              "example",
    			AdvancedSearch: "example",
    			Order:          "example",
    			Page:           1,
    			PerPage:        1,
    			Sort:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SearchResultItem } from "@ubx/sdk-github/github/data/issue/search_result_item";

    export default stack("example", () => {
      intent({ summary: "look up github_issue_search_result_item" });

      data(SearchResultItem, "example", {
        q: "example",
        advancedSearch: "example",
        order: "example",
        page: 1,
        perPage: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.issue import SearchResultItem, SearchResultItemConfig

    def describe():
        ubx.intent("look up github_issue_search_result_item")

        ubx.data(SearchResultItem, "example", SearchResultItemConfig(
            q="example",
            advanced_search="example",
            order="example",
            page=1,
            per_page=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="advanced_search" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="q" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="incomplete_results" type="bool">
  Returned by the lookup.
</ResponseField>

<ResponseField name="items" type="list(object)">
  Returned by the lookup.
</ResponseField>

<ResponseField name="lexical_fallback_reason" type="list(string)">
  When a semantic or hybrid search falls back to lexical search, this field contains the reasons for the fallback. Only present when a fallback occurred. Returned by the lookup.
</ResponseField>

<ResponseField name="search_type" type="string">
  Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Returned by the lookup.
</ResponseField>
