---
title: "github_issue"
description: "`github_issue` looks up a real, already-filed unit of work -- a bug report, a feature idea -- tracked inside a repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	issue "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/issue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_issue"})

    		ubx.Data(issue.Issue, "example", issue.IssueConfig{
    			Owner:     "example",
    			Repo:      "example",
    			Direction: "example",
    			Page:      1,
    			PerPage:   1,
    			Since:     "example",
    			Sort:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Issue } from "./local-sdk/github/sdk/typescript/github/data/issue/issue.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_issue" });

      data(Issue, "example", {
        owner: "example",
        repo: "example",
        direction: "example",
        page: 1,
        perPage: 1,
        since: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.issue import Issue, IssueConfig

    def describe():
        ubx.intent("look up github_issue")

        ubx.data(Issue, "example", IssueConfig(
            owner="example",
            repo="example",
            direction="example",
            page=1,
            per_page=1,
            since="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="direction" type="string">
  The sort direction, asc or desc. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="since" type="string">
  Only return results updated at or after this real timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
