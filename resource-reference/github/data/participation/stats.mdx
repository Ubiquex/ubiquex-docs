---
title: "github_participation_stats"
description: "`github_participation_stats` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_participation_stats` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	participation "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/participation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_participation_stats"})

    		ubx.Data(participation.Stats, "example", participation.StatsConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Stats } from "@ubx/sdk-github/github/data/participation/stats";

    export default stack("example", () => {
      intent({ summary: "look up github_participation_stats" });

      data(Stats, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.participation import Stats, StatsConfig

    def describe():
        ubx.intent("look up github_participation_stats")

        ubx.data(Stats, "example", StatsConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="all" type="list(number)">
  Returned by the lookup.
</ResponseField>
