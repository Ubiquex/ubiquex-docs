---
title: "github_participation_stats"
description: "`github_participation_stats` reads a repository's own real weekly commit count history, for both all contributors and the repository owner alone."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	participation "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/participation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_participation_stats"})

    		ubx.Data(participation.Stats, "example", participation.StatsConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Stats } from "./local-sdk/github/sdk/typescript/github/data/participation/stats.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_participation_stats" });

      data(Stats, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.participation import Stats, StatsConfig

    def describe():
        ubx.intent("look up github_participation_stats")

        ubx.data(Stats, "example", StatsConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="all" type="list(number)">
  Whether every matching item is included, rather than a filtered subset. **(AI-inferred)** Returned by the lookup.
</ResponseField>
