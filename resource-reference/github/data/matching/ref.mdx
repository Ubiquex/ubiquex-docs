---
title: "github_matching_ref"
description: "`github_matching_ref` looks up every real git reference -- branch head or tag -- whose name matches a given prefix..."
---

`github_matching_ref` looks up every real git reference -- branch head or tag -- whose name matches a given prefix, built on the same real ref object `github_git_ref` represents individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	matching "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/matching"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_matching_ref"})

    		ubx.Data(matching.Ref, "example", matching.RefConfig{
    			Owner: "example",
    			Ref:   "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Ref } from "./local-sdk/github/sdk/typescript/github/data/matching/ref.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_matching_ref" });

      data(Ref, "example", {
        owner: "example",
        ref: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.matching import Ref, RefConfig

    def describe():
        ubx.intent("look up github_matching_ref")

        ubx.data(Ref, "example", RefConfig(
            owner="example",
            ref="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="ref" type="string" required>
  The real git reference (a branch or tag name) this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
