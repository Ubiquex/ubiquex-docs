---
title: "github_content_file"
description: "`github_content_file` looks up the real contents and metadata of one specific file in a repository at a specific ref."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	content "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/content"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_content_file"})

    		ubx.Data(content.File, "example", content.FileConfig{
    			Owner: "example",
    			Repo:  "example",
    			Ref:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { File } from "@ubx/sdk-github/github/data/content/file";

    export default stack("example", () => {
      intent({ summary: "look up github_content_file" });

      data(File, "example", {
        owner: "example",
        repo: "example",
        ref: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.content import File, FileConfig

    def describe():
        ubx.intent("look up github_content_file")

        ubx.data(File, "example", FileConfig(
            owner="example",
            repo="example",
            ref="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="ref" type="string">
  The real git reference (a branch or tag name) this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="_links" type="object">
  A real map of related API and web URLs for this object. **(AI-inferred)** Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="git" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="html" type="string" required>
      Required lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string" required>
      Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="content" type="string">
  The real, base64-encoded content of this file. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="download_url" type="string">
  The real URL for downloading this file's raw content directly. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="encoding" type="string">
  The real encoding used for this file's own content field, typically base64. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="git_url" type="string">
  The real git:// protocol URL for cloning this repository. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="html_url" type="string">
  The real, browsable web URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="path" type="string">
  The real file path within the repository. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="sha" type="string">
  The real, full 40-character SHA-1 hash identifying this git object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="size" type="number">
  The real size of this repository or file, in kilobytes. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="submodule_git_url" type="string">
  The real git URL of the submodule this entry points to, if it is one. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="target" type="string">
  The real object this reference or symlink points to. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="type" type="string">
  The real, discriminating type of this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="url" type="string">
  The real, canonical API URL for this object. **(AI-inferred)** Returned by the lookup.
</ResponseField>
