---
title: "github_check_automated_security_fixes"
description: "`github_check_automated_security_fixes` reads whether Dependabot's own automated security fix pull requests are currently enabled for a repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	check "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/check"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_check_automated_security_fixes"})

    		ubx.Data(check.AutomatedSecurityFixes, "example", check.AutomatedSecurityFixesConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AutomatedSecurityFixes } from "@ubx/sdk-github/github/data/check/automated_security_fixes";

    export default stack("example", () => {
      intent({ summary: "look up github_check_automated_security_fixes" });

      data(AutomatedSecurityFixes, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.check import AutomatedSecurityFixes, AutomatedSecurityFixesConfig

    def describe():
        ubx.intent("look up github_check_automated_security_fixes")

        ubx.data(AutomatedSecurityFixes, "example", AutomatedSecurityFixesConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="enabled" type="bool">
  Whether Dependabot security updates are enabled for the repository. Returned by the lookup.
</ResponseField>

<ResponseField name="paused" type="bool">
  Whether Dependabot security updates are paused for the repository. Returned by the lookup.
</ResponseField>
