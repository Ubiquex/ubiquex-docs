---
title: "github_check_suite"
description: "`github_check_suite` looks up the real, already-created collection of every check run one GitHub App created for a single commit."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	check "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/check"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_check_suite"})

    		ubx.Data(check.Suite, "example", check.SuiteConfig{
    			Owner:     "example",
    			Ref:       "example",
    			Repo:      "example",
    			AppId:     1,
    			CheckName: "example-check-name",
    			Page:      1,
    			PerPage:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Suite } from "./local-sdk/github/sdk/typescript/github/data/check/suite.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_check_suite" });

      data(Suite, "example", {
        owner: "example",
        ref: "example",
        repo: "example",
        appId: 1,
        checkName: "example-check-name",
        page: 1,
        perPage: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.check import Suite, SuiteConfig

    def describe():
        ubx.intent("look up github_check_suite")

        ubx.data(Suite, "example", SuiteConfig(
            owner="example",
            ref="example",
            repo="example",
            app_id=1,
            check_name="example-check-name",
            page=1,
            per_page=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="number">
  The real, numeric ID of the GitHub App this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="check_name" type="string">
  The real name of the status check. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="owner" type="string" required>
  The real account (user or organization) that owns the repository. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ref" type="string" required>
  The real git reference (a branch or tag name) this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  The real repository's own name, without its owner. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="check_suites" type="list(object)">
  The real check suites included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  The total number of results matching the request, across every page. **(AI-inferred)** Returned by the lookup.
</ResponseField>
