---
title: "github_rule_suites"
description: "`github_rule_suites` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_rule_suites` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rule "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/rule"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_rule_suites"})

    		ubx.Data(rule.Suites, "example", rule.SuitesConfig{
    			Org:             "example",
    			ActorName:       "example-actor-name",
    			EvaluateStatus:  "example",
    			Page:            1,
    			PerPage:         1,
    			Ref:             "example",
    			RepositoryName:  "example-repository-name",
    			RuleSuiteResult: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Suites } from "@ubx/sdk-github/github/data/rule/suites";

    export default stack("example", () => {
      intent({ summary: "look up github_rule_suites" });

      data(Suites, "example", {
        org: "example",
        actorName: "example-actor-name",
        evaluateStatus: "example",
        page: 1,
        perPage: 1,
        ref: "example",
        repositoryName: "example-repository-name",
        ruleSuiteResult: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.rule import Suites, SuitesConfig

    def describe():
        ubx.intent("look up github_rule_suites")

        ubx.data(Suites, "example", SuitesConfig(
            org="example",
            actor_name="example-actor-name",
            evaluate_status="example",
            page=1,
            per_page=1,
            ref="example",
            repository_name="example-repository-name",
            rule_suite_result="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="actor_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="evaluate_status" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="org" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="ref" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="repository_name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="rule_suite_result" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="time_period" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
