---
title: "github_get_budget_user_states"
description: "`github_get_budget_user_states` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_get_budget_user_states` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_get_budget_user_states"})

    		ubx.Data(get.BudgetUserStates, "example", get.BudgetUserStatesConfig{
    			BudgetId:            "example",
    			Enterprise:          "example",
    			Page:                1,
    			PerPage:             1,
    			SortOrder:           "example",
    			ThresholdLowerBound: 1,
    			ThresholdUpperBound: 1,
    			User:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BudgetUserStates } from "@ubx/sdk-github/github/data/get/budget_user_states";

    export default stack("example", () => {
      intent({ summary: "look up github_get_budget_user_states" });

      data(BudgetUserStates, "example", {
        budgetId: "example",
        enterprise: "example",
        page: 1,
        perPage: 1,
        sortOrder: "example",
        thresholdLowerBound: 1,
        thresholdUpperBound: 1,
        user: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.get import BudgetUserStates, BudgetUserStatesConfig

    def describe():
        ubx.intent("look up github_get_budget_user_states")

        ubx.data(BudgetUserStates, "example", BudgetUserStatesConfig(
            budget_id="example",
            enterprise="example",
            page=1,
            per_page=1,
            sort_order="example",
            threshold_lower_bound=1,
            threshold_upper_bound=1,
            user="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="budget_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_order" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="threshold_lower_bound" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="threshold_upper_bound" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="user" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="has_next_page" type="bool">
  Indicates if there are more pages of results available. Returned by the lookup.
</ResponseField>

<ResponseField name="total_count" type="number">
  Total number of user state entries matching the query. Returned by the lookup.
</ResponseField>

<ResponseField name="user_states" type="list(object)">
  Per-user state entries for the budget. Returned by the lookup.
</ResponseField>
