---
title: "github_get_license_sync_status"
description: "`github_get_license_sync_status` reads the real, current status of an enterprise's own license usage sync with GitHub."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_get_license_sync_status"})

    		ubx.Data(get.LicenseSyncStatus, "example", get.LicenseSyncStatusConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseSyncStatus } from "@ubx/sdk-github/github/data/get/license_sync_status";

    export default stack("example", () => {
      intent({ summary: "look up github_get_license_sync_status" });

      data(LicenseSyncStatus, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.get import LicenseSyncStatus, LicenseSyncStatusConfig

    def describe():
        ubx.intent("look up github_get_license_sync_status")

        ubx.data(LicenseSyncStatus, "example", LicenseSyncStatusConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="server_instances" type="list(object)">
  The real GitHub Enterprise Server instances included in this result. **(AI-inferred)** Returned by the lookup.
</ResponseField>
