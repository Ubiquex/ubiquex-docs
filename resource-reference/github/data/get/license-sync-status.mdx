---
title: "github_get_license_sync_status"
description: "`github_get_license_sync_status` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_get_license_sync_status` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_get_license_sync_status"})

    		ubx.Data(get.LicenseSyncStatus, "example", get.LicenseSyncStatusConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LicenseSyncStatus } from "@ubx/sdk-github/github/data/get/license_sync_status";

    export default stack("example", () => {
      intent({ summary: "look up github_get_license_sync_status" });

      data(LicenseSyncStatus, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.get import LicenseSyncStatus, LicenseSyncStatusConfig

    def describe():
        ubx.intent("look up github_get_license_sync_status")

        ubx.data(LicenseSyncStatus, "example", LicenseSyncStatusConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="server_instances" type="list(object)">
  Returned by the lookup.
</ResponseField>
