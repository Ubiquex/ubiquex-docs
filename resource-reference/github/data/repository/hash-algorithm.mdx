---
title: "github_repository_hash_algorithm"
description: "`github_repository_hash_algorithm` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_repository_hash_algorithm` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	repository "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/repository"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_repository_hash_algorithm"})

    		ubx.Data(repository.HashAlgorithm, "example", repository.HashAlgorithmConfig{
    			Owner: "example",
    			Repo:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HashAlgorithm } from "@ubx/sdk-github/github/data/repository/hash_algorithm";

    export default stack("example", () => {
      intent({ summary: "look up github_repository_hash_algorithm" });

      data(HashAlgorithm, "example", {
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.repository import HashAlgorithm, HashAlgorithmConfig

    def describe():
        ubx.intent("look up github_repository_hash_algorithm")

        ubx.data(HashAlgorithm, "example", HashAlgorithmConfig(
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="owner" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="repo" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="hash_algorithm" type="string">
  The Git hash algorithm used by this repository. Returned by the lookup.
</ResponseField>
