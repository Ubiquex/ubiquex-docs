---
title: "github_propertie"
description: "`github_propertie` looks up a real, organization-defined custom property value already set on a specific repository."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	propertie "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/propertie"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_propertie"})

    		ubx.Data(propertie.Propertie, "example", propertie.PropertieConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { Propertie } from "./local-sdk/github/sdk/typescript/github/data/propertie/propertie.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_propertie" });

      data(Propertie, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.propertie import Propertie, PropertieConfig

    def describe():
        ubx.intent("look up github_propertie")

        ubx.data(Propertie, "example", PropertieConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  The real enterprise account this result belongs to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
