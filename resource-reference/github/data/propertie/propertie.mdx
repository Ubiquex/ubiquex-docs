---
title: "github_propertie"
description: "`github_propertie` is a real, live lookup against GitHub, generated from github's own real schema -- read-only, never created or destroyed."
---

`github_propertie` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against GitHub's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	propertie "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/propertie"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_propertie"})

    		ubx.Data(propertie.Propertie, "example", propertie.PropertieConfig{
    			Enterprise: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Propertie } from "@ubx/sdk-github/github/data/propertie/propertie";

    export default stack("example", () => {
      intent({ summary: "look up github_propertie" });

      data(Propertie, "example", {
        enterprise: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.data.propertie import Propertie, PropertieConfig

    def describe():
        ubx.intent("look up github_propertie")

        ubx.data(Propertie, "example", PropertieConfig(
            enterprise="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="enterprise" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
