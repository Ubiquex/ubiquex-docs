---
title: "github_codespaces_user_public_key"
description: "`github_codespaces_user_public_key` reads the real public key used to encrypt a user-level Codespaces secret's value before it's sent to GitHub."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	codespaces "github.com/ubiquex/ubx-sdk-github/sdk/go/github/data/codespaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up github_codespaces_user_public_key"})

    		ubx.Data(codespaces.UserPublicKey, "example", codespaces.UserPublicKeyConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { UserPublicKey } from "./local-sdk/github/sdk/typescript/github/data/codespaces/user_public_key.ts";

    export default stack("example", () => {
      intent({ summary: "look up github_codespaces_user_public_key" });

      data(UserPublicKey, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.data.codespaces import UserPublicKey, UserPublicKeyConfig

    def describe():
        ubx.intent("look up github_codespaces_user_public_key")

        ubx.data(UserPublicKey, "example", UserPublicKeyConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="key" type="string">
  The Base64 encoded public key. Returned by the lookup.
</ResponseField>

<ResponseField name="key_id" type="string">
  The identifier for the key. Returned by the lookup.
</ResponseField>
