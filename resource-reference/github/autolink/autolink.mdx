---
title: "github_autolink"
description: "`github_autolink` configures a pattern that automatically converts references to an external system -- a JIRA issue..."
---

`github_autolink` configures a pattern that automatically converts references to an external system -- a JIRA issue, a Zendesk ticket -- into a real clickable link anywhere that pattern appears in a repository's issues, pull requests, and commit messages, without needing to write the full URL out every time.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	autolink "github.com/ubiquex/ubx-sdk-github/sdk/go/github/autolink"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own autolink"})

    		ubx.Resource(autolink.Autolink, "example", autolink.AutolinkConfig{
    			AutolinkId:  "example",
    			KeyPrefix:   "example",
    			Owner:       "example",
    			Repo:        "example",
    			UrlTemplate: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Autolink } from "./local-sdk/github/sdk/typescript/github/autolink/autolink.ts";

    export default stack("example", () => {
      intent({ summary: "example own autolink" });

      resource(Autolink, "example", {
        autolinkId: "example",
        keyPrefix: "example",
        owner: "example",
        repo: "example",
        urlTemplate: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.autolink import Autolink, AutolinkConfig

    def describe():
        ubx.intent("example own autolink")

        ubx.resource(Autolink, "example", AutolinkConfig(
            autolink_id="example",
            key_prefix="example",
            owner="example",
            repo="example",
            url_template="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_autolink, with autolink id example, key prefix
    example, owner example, repo example, url template example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autolink_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="is_alphanumeric" type="bool">
  Whether this autolink reference matches alphanumeric characters. If true, the `&lt;num&gt;` parameter of the `url_template` matches alphanumeric characters `A-Z` (case insensitive), `0-9`, and `-`. If false, this autolink reference only matches numeric characters. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="key_prefix" type="string" required>
  This prefix appended by certain characters will generate a link any time it is found in an issue, pull request, or commit. Required.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="url_template" type="string" required>
  The URL must contain `&lt;num&gt;` for the reference number. `&lt;num&gt;` matches different characters depending on the value of `is_alphanumeric`. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="number">
  The autolink reference's numeric ID. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="is_alphanumeric" type="bool">
  Whether this autolink reference matches alphanumeric characters. If true, the `&lt;num&gt;` parameter of the `url_template` matches alphanumeric characters `A-Z` (case insensitive), `0-9`, and `-`. If false, this autolink reference only matches numeric characters. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Timestamp of the autolink reference's last update. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
