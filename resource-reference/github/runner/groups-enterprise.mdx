---
title: "github_runner_groups_enterprise"
description: "`github_runner_groups_enterprise` is a named collection of self-hosted GitHub Actions runners managed at the enterprise level and shared across member..."
---

`github_runner_groups_enterprise` is a named collection of self-hosted GitHub Actions runners managed at the enterprise level and shared across member organizations -- which organizations and workflows may use the group, and whether it's restricted to specific workflow files. Enterprises use runner groups to control which teams can run jobs on which hardware, rather than every organization managing its own separate runner pool.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	runner "github.com/ubiquex/ubx-sdk-github/sdk/go/github/runner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own groups enterprise"})

    		ubx.Resource(runner.GroupsEnterprise, "example", runner.GroupsEnterpriseConfig{
    			Enterprise:              "example",
    			Name:                    "example",
    			RunnerGroupId:           "example",
    			Runners:                 []any{1},
    			SelectedOrganizationIds: []any{1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupsEnterprise } from "@ubx/sdk-github/github/runner/groups_enterprise";

    export default stack("example", () => {
      intent({ summary: "example own groups enterprise" });

      resource(GroupsEnterprise, "example", {
        enterprise: "example",
        name: "example",
        runnerGroupId: "example",
        runners: [1],
        selectedOrganizationIds: [1],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.runner import GroupsEnterprise, GroupsEnterpriseConfig

    def describe():
        ubx.intent("example own groups enterprise")

        ubx.resource(GroupsEnterprise, "example", GroupsEnterpriseConfig(
            enterprise="example",
            name="example",
            runner_group_id="example",
            runners=[1],
            selected_organization_ids=[1],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_runner_groups_enterprise called "example", with
    enterprise example, runner group id example, runners a set value,
    selected organization ids a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="allows_public_repositories" type="bool">
  Whether the runner group can be used by `public` repositories. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the runner group. Required.
</ResponseField>

<ResponseField name="network_configuration_id" type="string">
  The identifier of a hosted compute network configuration. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="restricted_to_workflows" type="bool">
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="runner_group_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="runners" type="list(number)">
  List of runner IDs to add to the runner group. Optional.
</ResponseField>

<ResponseField name="selected_organization_ids" type="list(number)">
  List of organization IDs that can access the runner group. Optional.
</ResponseField>

<ResponseField name="selected_workflows" type="list(string)">
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="visibility" type="string">
  Visibility of a runner group. You can select all organizations or select individual organization. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="allows_public_repositories" type="bool">
  Whether the runner group can be used by `public` repositories. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="default" type="bool">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="hosted_runners_url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="network_configuration_id" type="string">
  The identifier of a hosted compute network configuration. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="restricted_to_workflows" type="bool">
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="runners_url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="selected_organizations_url" type="string">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="selected_workflows" type="list(string)">
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="visibility" type="string">
  Visibility of a runner group. You can select all organizations or select individual organization. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="workflow_restrictions_read_only" type="bool">
  If `true`, the `restricted_to_workflows` and `selected_workflows` fields cannot be modified. Computed by GitHub after `ship`.
</ResponseField>
