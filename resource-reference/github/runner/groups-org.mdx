---
title: "github_runner_groups_org"
description: "A complete, runnable github_runner_groups_org program, in every SDK language."
---

`github_runner_groups_org` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	runner "github.com/ubiquex/ubx-sdk-github/sdk/go/github/runner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own groups org"})

    		ubx.Resource(runner.GroupsOrg, "ci-runner", runner.GroupsOrgConfig{
    			Name:                  "ci-runner",
    			Org:                   "example",
    			RunnerGroupId:         "example",
    			Runners:               []string{"example"},
    			SelectedRepositoryIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupsOrg } from "./local-sdk/github/sdk/typescript/github/runner/groups_org.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own groups org" });

      resource(GroupsOrg, "ci-runner", {
        name: "ci-runner",
        org: "example",
        runnerGroupId: "example",
        runners: ["example"],
        selectedRepositoryIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.runner import GroupsOrg, GroupsOrgConfig

    def describe():
        ubx.intent("payments own groups org")

        ubx.resource(GroupsOrg, "ci-runner", GroupsOrgConfig(
            name="ci-runner",
            org="example",
            runner_group_id="example",
            runners=["example"],
            selected_repository_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_runner_groups_org called "ci-runner", with org
    example, runner group id example, runners a set value, selected
    repository ids a set value.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="allows_public_repositories" type="bool">
  Whether the runner group can be used by `public` repositories. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the runner group. Required.
</ResponseField>

<ResponseField name="network_configuration_id" type="string">
  The identifier of a hosted compute network configuration. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="restricted_to_workflows" type="bool">
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="runner_group_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="runners" type="list(number)">
  List of runner IDs to add to the runner group. Optional.
</ResponseField>

<ResponseField name="selected_repository_ids" type="list(number)">
  List of repository IDs that can access the runner group. Optional.
</ResponseField>

<ResponseField name="selected_workflows" type="list(string)">
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="visibility" type="string">
  Visibility of a runner group. You can select all repositories, select individual repositories, or limit access to private repositories. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="allows_public_repositories" type="bool">
  Whether the runner group can be used by `public` repositories. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="default" type="bool">
  Whether this runner group is the default group for the organization. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="hosted_runners_url" type="string">
  The API URL for accessing the hosted runners associated with this runner group. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="inherited" type="bool">
  Indicates whether the runner group is inherited from an enterprise account. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="inherited_allows_public_repositories" type="bool">
  Whether the runner group inherits the organization's setting for allowing public repositories. This computed value is determined by GitHub and reflects the inherited state of `allows_public_repositories`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="network_configuration_id" type="string">
  The identifier of a hosted compute network configuration. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="restricted_to_workflows" type="bool">
  If `true`, the runner group will be restricted to running only the workflows specified in the `selected_workflows` array. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="runners_url" type="string">
  The API URL to fetch the list of runners belonging to this organization runner group. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="selected_repositories_url" type="string">
  Link to the selected repositories resource for this runner group. Not present unless visibility was set to `selected` Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="selected_workflows" type="list(string)">
  List of workflows the runner group should be allowed to run. This setting will be ignored unless `restricted_to_workflows` is set to `true`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="visibility" type="string">
  Visibility of a runner group. You can select all repositories, select individual repositories, or limit access to private repositories. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="workflow_restrictions_read_only" type="bool">
  If `true`, the `restricted_to_workflows` and `selected_workflows` fields cannot be modified. Computed by GitHub after `ship`.
</ResponseField>
