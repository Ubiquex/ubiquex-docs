---
title: "github_code_scanning_sarifs_status"
description: "`github_code_scanning_sarifs_status` reports whether an uploaded SARIF results file has finished processing -- code scanning expects one such upload..."
---

`github_code_scanning_sarifs_status` reports whether an uploaded SARIF results file has finished processing -- code scanning expects one such upload per analysis, and this is the real, honest way to confirm a given upload actually succeeded rather than assuming it did.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scanning sarifs status"})

    		ubx.Resource(code.ScanningSarifsStatus, "ci-runner", code.ScanningSarifsStatusConfig{
    			CommitSha:   "example",
    			Owner:       "example",
    			Ref:         "example",
    			Repo:        "example",
    			Sarif:       "example",
    			SarifId:     "example",
    			CheckoutUri: "example",
    			StartedAt:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScanningSarifsStatus } from "./local-sdk/github/sdk/typescript/github/code/scanning_sarifs_status.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own scanning sarifs status" });

      resource(ScanningSarifsStatus, "ci-runner", {
        commitSha: "example",
        owner: "example",
        ref: "example",
        repo: "example",
        sarif: "example",
        sarifId: "example",
        checkoutUri: "example",
        startedAt: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.code import ScanningSarifsStatus, ScanningSarifsStatusConfig

    def describe():
        ubx.intent("payments own scanning sarifs status")

        ubx.resource(ScanningSarifsStatus, "ci-runner", ScanningSarifsStatusConfig(
            commit_sha="example",
            owner="example",
            ref="example",
            repo="example",
            sarif="example",
            sarif_id="example",
            checkout_uri="example",
            started_at="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_code_scanning_sarifs_status, with commit sha
    example, owner example, ref example, repo example, sarif example,
    sarif id example, checkout uri example, started at example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="checkout_uri" type="string">
  The base directory used in the analysis, as it appears in the SARIF file. This property is used to convert file paths from absolute to relative, so that alerts can be mapped to their correct location in the repository. Optional.
</ResponseField>

<ResponseField name="commit_sha" type="string" required>
  The SHA of the commit to which the analysis you are uploading relates. Required.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="ref" type="string" required>
  The full Git reference, formatted as `refs/heads/&lt;branch name&gt;`, `refs/tags/&lt;tag&gt;`, `refs/pull/&lt;number&gt;/merge`, or `refs/pull/&lt;number&gt;/head`. Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="sarif" type="string" required>
  A Base64 string representing the SARIF file to upload. You must first compress your SARIF file using [`gzip`](http://www.gnu.org/software/gzip/manual/gzip.html) and then translate the contents of the file into a Base64 encoding string. For more information, see "[SARIF support for code scanning](https://docs.github.com/code-security/secure-coding/sarif-support-for-code-scanning)." Required.
</ResponseField>

<ResponseField name="sarif_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="started_at" type="string">
  The time that the analysis run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Optional.
</ResponseField>

<ResponseField name="tool_name" type="string">
  The name of the tool used to generate the code scanning analysis. If this parameter is not used, the tool name defaults to "API". If the uploaded SARIF contains a tool GUID, this will be available for filtering using the `tool_guid` parameter of operations such as `GET /repos/&#123;owner&#125;/&#123;repo&#125;/code-scanning/alerts`. Optional.
</ResponseField>

<ResponseField name="validate" type="bool">
  Whether the SARIF file will be validated according to the code scanning specifications. This parameter is intended to help integrators ensure that the uploaded SARIF files are correctly rendered by code scanning. Optional.
</ResponseField>

## Output properties

<ResponseField name="analyses_url" type="string">
  The REST API URL for getting the analyses associated with the upload. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="errors" type="list(string)">
  Any errors that ocurred during processing of the delivery. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="processing_status" type="string">
  `pending` files have not yet been processed, while `complete` means results from the SARIF have been stored. `failed` files have either not been processed at all, or could only be partially processed. Computed by GitHub after `ship`.
</ResponseField>
