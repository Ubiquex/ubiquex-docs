---
title: "github_code_scanning_variant_analysis"
description: "A complete, runnable github_code_scanning_variant_analysis program, in every SDK language."
---

`github_code_scanning_variant_analysis` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scanning variant analysis"})

    		ubx.Resource(code.ScanningVariantAnalysis, "ci-runner", code.ScanningVariantAnalysisConfig{
    			CodeqlVariantAnalysisId: "example",
    			Language:                "example",
    			Owner:                   "example",
    			QueryPack:               "example",
    			Repo:                    "example",
    			Repositories:            []string{"example"},
    			RepositoryLists:         []string{"example"},
    			RepositoryOwners:        []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScanningVariantAnalysis } from "./local-sdk/github/sdk/typescript/github/code/scanning_variant_analysis.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own scanning variant analysis" });

      resource(ScanningVariantAnalysis, "ci-runner", {
        codeqlVariantAnalysisId: "example",
        language: "example",
        owner: "example",
        queryPack: "example",
        repo: "example",
        repositories: ["example"],
        repositoryLists: ["example"],
        repositoryOwners: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.code import ScanningVariantAnalysis, ScanningVariantAnalysisConfig

    def describe():
        ubx.intent("payments own scanning variant analysis")

        ubx.resource(ScanningVariantAnalysis, "ci-runner", ScanningVariantAnalysisConfig(
            codeql_variant_analysis_id="example",
            language="example",
            owner="example",
            query_pack="example",
            repo="example",
            repositories=["example"],
            repository_lists=["example"],
            repository_owners=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_code_scanning_variant_analysis, with codeql variant
    analysis id example, language example, owner example, query pack
    example, repo example, repositories a set value, repository lists
    a set value, repository owners a set value.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="codeql_variant_analysis_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="language" type="string" required>
  The language targeted by the CodeQL query Required.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="query_pack" type="string" required>
  A Base64-encoded tarball containing a CodeQL query and all its dependencies Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repositories" type="list(string)">
  List of repository names (in the form `owner/repo-name`) to run the query against. Precisely one property from `repositories`, `repository_lists` and `repository_owners` is required. Optional.
</ResponseField>

<ResponseField name="repository_lists" type="list(string)">
  List of repository lists to run the query against. Precisely one property from `repositories`, `repository_lists` and `repository_owners` is required. Optional.
</ResponseField>

<ResponseField name="repository_owners" type="list(string)">
  List of organization or user names whose repositories the query should be run against. Precisely one property from `repositories`, `repository_lists` and `repository_owners` is required. Optional.
</ResponseField>

## Output properties

<ResponseField name="actions_workflow_run_id" type="number">
  The GitHub Actions workflow run used to execute this variant analysis. This is only available if the workflow run has started. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="actor" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the actor's avatar image. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the actor who triggered or is associated with the code scanning variant analysis. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL at which the actor's public events can be retrieved. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The URL to the GitHub followers list for the actor, as returned by the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL template for listing the users this actor is following, with an optional &#123;/other_user&#125; placeholder for a specific user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL to the actor's gists list in the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The unique hash identifying the user's Gravatar image, used to generate the avatar URL via Gravatar service. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to the GitHub profile of the actor (user or bot) that triggered the variant analysis. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The numeric identifier of the user or bot account that triggered the code scanning variant analysis. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username (login) of the actor who initiated the variant analysis. This field is required. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL node identifier for the actor (user or bot) that triggered the variant analysis. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The URL to the organizations endpoint for this actor, listing the organizations to which the actor belongs. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the actor's received events. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the GitHub API endpoint that lists repositories owned by the actor. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Whether the actor is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template to list the repositories starred by the actor. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The API endpoint URL for the subscriptions of the actor (user or bot) that triggered the code scanning variant analysis. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Required.
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The GitHub API URL for the actor associated with the code scanning variant analysis. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      The user's view type, which indicates whether the user account is publicly visible or private. Possible values are 'public' and 'private'. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="completed_at" type="string">
  The date and time at which the variant analysis was completed, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Will be null if the variant analysis has not yet completed or this information is not available. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="controller_repo" type="object">
  A GitHub repository. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="archive_url" type="string" required>
      A template for the API URL to download the repository as an archive. Required.
    </ResponseField>
    <ResponseField name="assignees_url" type="string" required>
      A template for the API URL to list the available assignees for issues in the repository. Required.
    </ResponseField>
    <ResponseField name="blobs_url" type="string" required>
      A template for the API URL to create or retrieve a raw Git blob in the repository. Required.
    </ResponseField>
    <ResponseField name="branches_url" type="string" required>
      A template for the API URL to get information about branches in the repository. Required.
    </ResponseField>
    <ResponseField name="collaborators_url" type="string" required>
      A template for the API URL to get information about collaborators of the repository. Required.
    </ResponseField>
    <ResponseField name="comments_url" type="string" required>
      A template for the API URL to get information about comments on the repository. Required.
    </ResponseField>
    <ResponseField name="commits_url" type="string" required>
      A template for the API URL to get information about commits on the repository. Required.
    </ResponseField>
    <ResponseField name="compare_url" type="string" required>
      A template for the API URL to compare two commits or refs. Required.
    </ResponseField>
    <ResponseField name="contents_url" type="string" required>
      A template for the API URL to get the contents of the repository. Required.
    </ResponseField>
    <ResponseField name="contributors_url" type="string" required>
      A template for the API URL to list the contributors to the repository. Required.
    </ResponseField>
    <ResponseField name="deployments_url" type="string" required>
      The API URL to list the deployments of the repository. Required.
    </ResponseField>
    <ResponseField name="description" type="string" required>
      The repository description. Required.
    </ResponseField>
    <ResponseField name="downloads_url" type="string" required>
      The API URL to list the downloads on the repository. Required.
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The API URL to list the events of the repository. Required.
    </ResponseField>
    <ResponseField name="fork" type="bool" required>
      Whether the repository is a fork. Required.
    </ResponseField>
    <ResponseField name="forks_url" type="string" required>
      The API URL to list the forks of the repository. Required.
    </ResponseField>
    <ResponseField name="full_name" type="string" required>
      The full, globally unique, name of the repository. Required.
    </ResponseField>
    <ResponseField name="git_commits_url" type="string" required>
      A template for the API URL to get information about Git commits of the repository. Required.
    </ResponseField>
    <ResponseField name="git_refs_url" type="string" required>
      A template for the API URL to get information about Git refs of the repository. Required.
    </ResponseField>
    <ResponseField name="git_tags_url" type="string" required>
      A template for the API URL to get information about Git tags of the repository. Required.
    </ResponseField>
    <ResponseField name="hooks_url" type="string" required>
      The API URL to list the hooks on the repository. Required.
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to view the repository on GitHub.com. Required.
    </ResponseField>
    <ResponseField name="id" type="number" required>
      A unique identifier of the repository. Required.
    </ResponseField>
    <ResponseField name="issue_comment_url" type="string" required>
      A template for the API URL to get information about issue comments on the repository. Required.
    </ResponseField>
    <ResponseField name="issue_events_url" type="string" required>
      A template for the API URL to get information about issue events on the repository. Required.
    </ResponseField>
    <ResponseField name="issues_url" type="string" required>
      A template for the API URL to get information about issues on the repository. Required.
    </ResponseField>
    <ResponseField name="keys_url" type="string" required>
      A template for the API URL to get information about deploy keys on the repository. Required.
    </ResponseField>
    <ResponseField name="labels_url" type="string" required>
      A template for the API URL to get information about labels of the repository. Required.
    </ResponseField>
    <ResponseField name="languages_url" type="string" required>
      The API URL to get information about the languages of the repository. Required.
    </ResponseField>
    <ResponseField name="merges_url" type="string" required>
      The API URL to merge branches in the repository. Required.
    </ResponseField>
    <ResponseField name="milestones_url" type="string" required>
      A template for the API URL to get information about milestones of the repository. Required.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the repository. Required.
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL identifier of the repository. Required.
    </ResponseField>
    <ResponseField name="notifications_url" type="string" required>
      A template for the API URL to get information about notifications on the repository. Required.
    </ResponseField>
    <ResponseField name="owner" type="object" required>
      A GitHub user. Required.
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string" required>
          The URL of the avatar image for the repository owner. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address of the owner of the controller repository. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events_url" type="string" required>
          The URL to the events of the owner (user or organization). This field is always present and points to the owner's public events list. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string" required>
          The URL to retrieve the list of followers for the owner of the controller repository's GitHub account. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string" required>
          The URL template for the list of users that this repository owner follows. It includes a placeholder that can be replaced with a specific username to check a follow relationship. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string" required>
          The API URL for the gists belonging to the repository owner. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string" required>
          The Gravatar ID for the owner of the controller repository. This string identifies the owner's Gravatar image and is always present. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string" required>
          The GitHub profile URL (HTML) for the owner (user or organization) of the controller repository used in code scanning variant analysis. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number" required>
          Required.
        </ResponseField>
        <ResponseField name="login" type="string" required>
          The username or organization name of the GitHub account that owns the repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the owner (user or organization) that controls the repository used for the code scanning variant analysis. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The node ID of the owner of the controller repository. This is a globally unique identifier for the owner (user or organization) in GitHub's GraphQL API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string" required>
          The URL to fetch the organizations that the owner belongs to, as provided in the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string" required>
          The URL to the GitHub API endpoint for events received by the owner (user or organization). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string" required>
          The URL to the API endpoint that lists all repositories belonging to the owner of the controller repository. This is a standard GitHub API field for user or organization objects. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool" required>
          Indicates whether the repository owner is a GitHub site administrator. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="starred_url" type="string" required>
          A URL template for listing repositories starred by this owner, containing `&#123;owner&#125;&#123;/repo&#125;` placeholders. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string" required>
          The URL to the GitHub API endpoint for subscriptions of the repository owner (the owner of the controller repository). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of account that owns the repository. In the GitHub API, this is typically 'User' or 'Organization'. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The URL of the GitHub account (user or organization) that owns the controller repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private" type="bool" required>
      Whether the repository is private. Required.
    </ResponseField>
    <ResponseField name="pulls_url" type="string" required>
      A template for the API URL to get information about pull requests on the repository. Required.
    </ResponseField>
    <ResponseField name="releases_url" type="string" required>
      A template for the API URL to get information about releases on the repository. Required.
    </ResponseField>
    <ResponseField name="stargazers_url" type="string" required>
      The API URL to list the stargazers on the repository. Required.
    </ResponseField>
    <ResponseField name="statuses_url" type="string" required>
      A template for the API URL to get information about statuses of a commit. Required.
    </ResponseField>
    <ResponseField name="subscribers_url" type="string" required>
      The API URL to list the subscribers on the repository. Required.
    </ResponseField>
    <ResponseField name="subscription_url" type="string" required>
      The API URL to subscribe to notifications for this repository. Required.
    </ResponseField>
    <ResponseField name="tags_url" type="string" required>
      The API URL to get information about tags on the repository. Required.
    </ResponseField>
    <ResponseField name="teams_url" type="string" required>
      The API URL to list the teams on the repository. Required.
    </ResponseField>
    <ResponseField name="trees_url" type="string" required>
      A template for the API URL to create or retrieve a raw Git tree of the repository. Required.
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL to get more information about the repository from the GitHub API. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_at" type="string">
  The date and time at which the variant analysis was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="failure_reason" type="string">
  The reason for a failure of the variant analysis. This is only available if the variant analysis has failed. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  The ID of the variant analysis. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="query_language" type="string">
  The language targeted by the CodeQL query Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="query_pack_url" type="string">
  The download url for the query pack. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="scanned_repositories" type="list(object)">
  The list of repositories that were scanned as part of the GitHub code scanning variant analysis. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="analysis_status" type="string">
      The current status of the variant analysis for a repository. Possible values are pending, in_progress, succeeded, failed, canceled, and timed_out. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="artifact_size_in_bytes" type="number">
      The exact size in bytes of the analysis artifact associated with this scanned repository. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="failure_message" type="string">
      The error message indicating why the analysis failed for this repository, if applicable. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repository" type="object">
      The GitHub repository that was scanned as part of the variant analysis. Optional; if omitted, computed by GitHub. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="full_name" type="string">
          The full name of the repository, formatted as `owner/name` (e.g., `octocat/Hello-World`). Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number">
          The unique numeric identifier of the GitHub repository. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by GitHub.
        </ResponseField>
        <ResponseField name="private" type="bool">
          Indicates whether the repository is private (true) or public (false). Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="stargazers_count" type="number">
          The number of users who have starred the repository. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          The timestamp of the last update to the repository, in ISO 8601 format. This indicates when the repository's metadata or content was last modified. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="result_count" type="number">
      The number of code scanning results detected in this repository for the variant analysis. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="skipped_repositories" type="object">
  Information about repositories that were skipped from processing. This information is only available to the user that initiated the variant analysis. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="access_mismatch_repos" type="object" required>
      An object containing a repository count and a list of repositories that were skipped because the analyzed access token does not have permission to access them. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="repositories" type="list(object)" required>
          A list of repositories that were skipped. This list may not include all repositories that were skipped. This is only available when the repository was found and the user has access to it. Required.
          <Expandable title="properties">
            <ResponseField name="full_name" type="string">
              The full name of the repository in the format `owner/repo`, for example `octocat/Hello-World`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="number">
              The unique numeric identifier of a repository that was skipped because of an access mismatch. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the repository that was skipped due to an access mismatch. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="private" type="bool">
              True if the repository is private; false if the repository is public. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stargazers_count" type="number">
              The total number of stargazers (users who starred) the repository has on GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="updated_at" type="string">
              The date and time when the repository was last updated, in ISO 8601 format. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="repository_count" type="number" required>
          The total number of repositories that were skipped for this reason. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="no_codeql_db_repos" type="object" required>
      An object containing details about repositories that were skipped from the variant analysis due to no CodeQL database being available. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="repositories" type="list(object)" required>
          A list of repositories that were skipped. This list may not include all repositories that were skipped. This is only available when the repository was found and the user has access to it. Required.
          <Expandable title="properties">
            <ResponseField name="full_name" type="string">
              The repository's full name in the format 'owner/repo', identifying a skipped repository with no CodeQL database. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="number">
              The unique numeric identifier of the repository that was skipped because it has no CodeQL database. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the repository that was skipped because it does not have a CodeQL database. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="private" type="bool">
              Indicates whether the repository is private (true) or public (false). Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stargazers_count" type="number">
              The number of users who have starred the repository. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="updated_at" type="string">
              The date and time at which the repository was last updated. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="repository_count" type="number" required>
          The total number of repositories that were skipped for this reason. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="not_found_repos" type="object" required>
      An object describing repositories that could not be found during the code scanning variant analysis, and were therefore skipped. This is a required part of the skipped repositories summary. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="repository_count" type="number" required>
          The total number of repositories that were skipped for this reason. Required.
        </ResponseField>
        <ResponseField name="repository_full_names" type="list(string)" required>
          A list of full repository names that were skipped. This list may not include all repositories that were skipped. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="over_limit_repos" type="object" required>
      An object containing details about repositories that were skipped because they exceeded the repository limit. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="repositories" type="list(object)" required>
          A list of repositories that were skipped. This list may not include all repositories that were skipped. This is only available when the repository was found and the user has access to it. Required.
          <Expandable title="properties">
            <ResponseField name="full_name" type="string">
              The full name of the repository, in the format `owner/repo` (e.g., `octocat/Hello-World`). Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="number">
              The unique numeric identifier of the repository. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the repository that was skipped because it exceeded the repository limit for the variant analysis. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="private" type="bool">
              Indicates whether the repository is private (true) or public (false). Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="stargazers_count" type="number">
              The number of stargazers (stars) that the repository has received. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="updated_at" type="string">
              Optional; if omitted, computed by GitHub.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="repository_count" type="number" required>
          The total number of repositories that were skipped for this reason. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The current status of the variant analysis, which can be 'in_progress', 'succeeded', 'failed', or 'cancelled'. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time at which the variant analysis was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Computed by GitHub after `ship`.
</ResponseField>
