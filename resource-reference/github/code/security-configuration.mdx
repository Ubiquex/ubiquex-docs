---
title: "github_code_security_configuration"
description: "`github_code_security_configuration` bundles real security features -- Dependabot alerts, CodeQL code scanning, secret protection -- into one named..."
---

`github_code_security_configuration` bundles real security features -- Dependabot alerts, CodeQL code scanning, secret protection -- into one named, centrally-managed policy an organization can apply across many repositories at once, instead of enabling each feature repository by repository.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	code "github.com/ubiquex/ubx-sdk-github/sdk/go/github/code"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own security configuration"})

    		ubx.Resource(code.SecurityConfiguration, "example", code.SecurityConfigurationConfig{
    			ConfigurationId:  "example",
    			Enterprise:       "example",
    			Name:             "example",
    			CodeSecurity:     "example",
    			SecretProtection: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityConfiguration } from "@ubx/sdk-github/github/code/security_configuration";

    export default stack("example", () => {
      intent({ summary: "example own security configuration" });

      resource(SecurityConfiguration, "example", {
        configurationId: "example",
        enterprise: "example",
        name: "example",
        codeSecurity: "example",
        secretProtection: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.code import SecurityConfiguration, SecurityConfigurationConfig

    def describe():
        ubx.intent("example own security configuration")

        ubx.resource(SecurityConfiguration, "example", SecurityConfigurationConfig(
            configuration_id="example",
            enterprise="example",
            name="example",
            code_security="example",
            secret_protection="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_code_security_configuration called "example", with
    configuration id example, enterprise example, code security
    example, secret protection example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="advanced_security" type="string">
  The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret Protection features. &gt; [!WARNING] &gt; `code_security` and `secret_protection` are deprecated values for this field. Prefer the individual `code_security` and `secret_protection` fields to set the status of these features. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="code_scanning_default_setup" type="string">
  The enablement status of code scanning default setup Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="code_scanning_default_setup_options" type="object">
  Feature options for code scanning default setup Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="runner_label" type="string">
      The label of the runner to use for code scanning default setup when runner_type is 'labeled'. Optional.
    </ResponseField>
    <ResponseField name="runner_type" type="string">
      Whether to use labeled runners or standard GitHub runners. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="code_scanning_delegated_alert_dismissal" type="string">
  The enablement status of code scanning delegated alert dismissal Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="code_scanning_options" type="object">
  Security Configuration feature options for code scanning Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="allow_advanced" type="bool">
      Whether to allow repos which use advanced setup Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="code_security" type="string">
  The enablement status of GitHub Code Security features. Optional.
</ResponseField>

<ResponseField name="configuration_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="dependabot_alerts" type="string">
  The enablement status of Dependabot alerts Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependabot_security_updates" type="string">
  The enablement status of Dependabot security updates Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependency_graph" type="string">
  The enablement status of Dependency Graph Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependency_graph_autosubmit_action" type="string">
  The enablement status of Automatic dependency submission Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependency_graph_autosubmit_action_options" type="object">
  Feature options for Automatic dependency submission Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="labeled_runners" type="bool">
      Whether to use runners labeled with 'dependency-submission' or standard GitHub runners. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A description of the code security configuration Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="enforcement" type="string">
  The enforcement status for a security configuration Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the code security configuration. Must be unique within the enterprise. Required.
</ResponseField>

<ResponseField name="private_vulnerability_reporting" type="string">
  The enablement status of private vulnerability reporting Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_protection" type="string">
  The enablement status of GitHub Secret Protection features. Optional.
</ResponseField>

<ResponseField name="secret_scanning" type="string">
  The enablement status of secret scanning Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_delegated_alert_dismissal" type="string">
  The enablement status of secret scanning delegated alert dismissal Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_extended_metadata" type="string">
  The enablement status of secret scanning extended metadata Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_generic_secrets" type="string">
  The enablement status of Copilot secret scanning Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_non_provider_patterns" type="string">
  The enablement status of secret scanning non provider patterns Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_push_protection" type="string">
  The enablement status of secret scanning push protection Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_validity_checks" type="string">
  The enablement status of secret scanning validity checks Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="advanced_security" type="string">
  The enablement status of GitHub Advanced Security features. `enabled` will enable both Code Security and Secret Protection features. &gt; [!WARNING] &gt; `code_security` and `secret_protection` are deprecated values for this field. Prefer the individual `code_security` and `secret_protection` fields to set the status of these features. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="code_scanning_default_setup" type="string">
  The enablement status of code scanning default setup Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="code_scanning_default_setup_options" type="object">
  Feature options for code scanning default setup Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="runner_label" type="string">
      The label of the runner to use for code scanning default setup when runner_type is 'labeled'. Optional.
    </ResponseField>
    <ResponseField name="runner_type" type="string">
      Whether to use labeled runners or standard GitHub runners. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="code_scanning_delegated_alert_dismissal" type="string">
  The enablement status of code scanning delegated alert dismissal Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="code_scanning_options" type="object">
  Security Configuration feature options for code scanning Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="allow_advanced" type="bool">
      Whether to allow repos which use advanced setup Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp at which the code security configuration was created, automatically set by the system. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="dependabot_alerts" type="string">
  The enablement status of Dependabot alerts Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependabot_delegated_alert_dismissal" type="string">
  The enablement status of Dependabot delegated alert dismissal Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="dependabot_security_updates" type="string">
  The enablement status of Dependabot security updates Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependency_graph" type="string">
  The enablement status of Dependency Graph Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependency_graph_autosubmit_action" type="string">
  The enablement status of Automatic dependency submission Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="dependency_graph_autosubmit_action_options" type="object">
  Feature options for Automatic dependency submission Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="labeled_runners" type="bool">
      Whether to use runners labeled with 'dependency-submission' or standard GitHub runners. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  A description of the code security configuration Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="enforcement" type="string">
  The enforcement status for a security configuration Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="html_url" type="string">
  The URL of the configuration Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  The ID of the code security configuration Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="private_vulnerability_reporting" type="string">
  The enablement status of private vulnerability reporting Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning" type="string">
  The enablement status of secret scanning Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_delegated_alert_dismissal" type="string">
  The enablement status of secret scanning delegated alert dismissal Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_delegated_bypass" type="string">
  The enablement status of secret scanning delegated bypass Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="secret_scanning_delegated_bypass_options" type="object">
  Feature options for secret scanning delegated bypass Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="reviewers" type="list(object)">
      The bypass reviewers for secret scanning delegated bypass Optional.
      <Expandable title="properties">
        <ResponseField name="mode" type="string">
          The mode of the reviewer, which must be either `ALWAYS` or `EXEMPT`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reviewer_id" type="number">
          The ID of the GitHub user or team that serves as a reviewer for delegated secret scanning bypass requests. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="reviewer_type" type="string">
          Type of the reviewer for the delegated bypass. Valid values are `TEAM` or `ROLE`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="security_configuration_id" type="number">
          The ID of the code security configuration that contains these secret scanning delegated bypass reviewers. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secret_scanning_extended_metadata" type="string">
  The enablement status of secret scanning extended metadata Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_generic_secrets" type="string">
  The enablement status of Copilot secret scanning Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_non_provider_patterns" type="string">
  The enablement status of secret scanning non provider patterns Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_push_protection" type="string">
  The enablement status of secret scanning push protection Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="secret_scanning_validity_checks" type="string">
  The enablement status of secret scanning validity checks Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="target_type" type="string">
  The type of the code security configuration. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of when the code security configuration was last updated, in ISO 8601 format. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  The URL of the configuration Computed by GitHub after `ship`.
</ResponseField>
