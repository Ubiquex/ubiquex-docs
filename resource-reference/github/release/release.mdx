---
title: "github_release"
description: "`github_release` packages one real point in a repository's history -- built on a specific git tag -- with release notes and binary files..."
---

`github_release` packages one real point in a repository's history -- built on a specific git tag -- with release notes and binary files, the deployable, citable unit a repository's own users actually download and install.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	release "github.com/ubiquex/ubx-sdk-github/sdk/go/github/release"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own release"})

    		ubx.Resource(release.Release, "example", release.ReleaseConfig{
    			Owner:                  "example",
    			Repo:                   "example",
    			Tag:                    "example",
    			TagName:                "example-tag-name",
    			Name:                   "example",
    			DiscussionCategoryName: "example-discussion-category-name",
    			GenerateReleaseNotes:   true,
    			MakeLatest:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Release } from "./local-sdk/github/sdk/typescript/github/release/release.ts";

    export default stack("example", () => {
      intent({ summary: "example own release" });

      resource(Release, "example", {
        owner: "example",
        repo: "example",
        tag: "example",
        tagName: "example-tag-name",
        name: "example",
        discussionCategoryName: "example-discussion-category-name",
        generateReleaseNotes: true,
        makeLatest: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.release import Release, ReleaseConfig

    def describe():
        ubx.intent("example own release")

        ubx.resource(Release, "example", ReleaseConfig(
            owner="example",
            repo="example",
            tag="example",
            tag_name="example-tag-name",
            name="example",
            discussion_category_name="example-discussion-category-name",
            generate_release_notes=True,
            make_latest="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_release called "example", with owner example,
    repo example, tag example, tag name example-tag-name, discussion
    category name example-discussion-category-name, generate release
    notes true, make latest example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="body" type="string">
  Text describing the contents of the tag. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="discussion_category_name" type="string">
  If specified, a discussion of the specified category is created and linked to the release. The value must be a category that already exists in the repository. For more information, see "[Managing categories for discussions in your repository](https://docs.github.com/discussions/managing-discussions-for-your-community/managing-categories-for-discussions-in-your-repository)." Optional.
</ResponseField>

<ResponseField name="draft" type="bool">
  `true` to create a draft (unpublished) release, `false` to create a published one. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="generate_release_notes" type="bool">
  Whether to automatically generate the name and body for this release. If `name` is specified, the specified name will be used; otherwise, a name will be automatically generated. If `body` is specified, the body will be pre-pended to the automatically generated notes. Optional.
</ResponseField>

<ResponseField name="make_latest" type="string">
  Specifies whether this release should be set as the latest release for the repository. Drafts and prereleases cannot be set as latest. Defaults to `true` for newly published releases. `legacy` specifies that the latest release should be determined based on the release creation date and higher semantic version. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the release. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="prerelease" type="bool">
  `true` to identify the release as a prerelease. `false` to identify the release as a full release. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="tag" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="tag_name" type="string" required>
  The name of the tag. Required.
</ResponseField>

<ResponseField name="target_commitish" type="string">
  Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="assets" type="list(object)">
  The list of release assets (files) attached to the GitHub release. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="browser_download_url" type="string">
      The URL to download the release asset directly in a browser. This is the web-based link that users can visit to download the file without using the API. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="content_type" type="string">
      The MIME type of the release asset, e.g., application/zip or application/x-gzip. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_at" type="string">
      The date and time when the GitHub release asset was created, formatted as an ISO 8601 timestamp. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="digest" type="string">
      The cryptographic digest (typically SHA-256) of the release asset file, used to verify its integrity. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="download_count" type="number">
      Number of times this release asset has been downloaded. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number">
      The unique identifier for the release asset, as assigned by GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="label" type="string">
      The user-defined label for the release asset, which is displayed in the GitHub UI; may be null if no label is set. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the release asset, typically the filename. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string">
      The GraphQL node ID of the release asset. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="size" type="number">
      The size of the release asset in bytes. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string">
      The state of the release asset, either `uploaded` or `open`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      The timestamp of when the release asset was last updated. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="uploader" type="object">
      The GitHub user who uploaded the release asset. This is a user object containing standard GitHub user fields such as login, id, and avatar_url. Optional; if omitted, computed by GitHub. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string">
          The URL for the uploader's avatar image. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address of the user who uploaded the release asset. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events_url" type="string">
          The URL to the events endpoint for the uploader user, providing access to the user's public events. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string">
          The API URL to the uploader's list of followers on GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string">
          The URL to the GitHub API endpoint that lists the users this uploader is following. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string">
          The URL to the user's gists. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string">
          The Gravatar ID for the user, which is the MD5 hash of the user's email address. It is used to construct the user's avatar URL via Gravatar. In current API responses, this field is often an empty string. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string">
          The URL to the uploader's GitHub profile page. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number">
          The unique numeric identifier for the GitHub user who uploaded the release asset. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="login" type="string">
          The GitHub username of the user who uploaded the release asset. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The display name of the user who uploaded the release asset. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string">
          The unique identifier for the uploader's user object in GitHub's GraphQL API. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string">
          The API URL to fetch the list of organizations the uploader belongs to, typically in the form https://api.github.com/users/&#123;username&#125;/orgs. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string">
          The URL to the received events endpoint for the uploader user, providing a list of events that this user has received. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string">
          The API endpoint URL that lists the repositories owned by the uploader user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool">
          Whether the uploader is a site administrator on GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          Optional; if omitted, computed by GitHub.
        </ResponseField>
        <ResponseField name="starred_url" type="string">
          The URL to retrieve the list of repositories starred by this uploader user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string">
          The API endpoint URL for the uploader user's GitHub subscriptions, typically pointing to a list of repositories the user is subscribed to. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of GitHub user account that uploaded the asset. Typically 'User' or 'Bot'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string">
          The GitHub API URL for the uploader user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          Optional; if omitted, computed by GitHub.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="url" type="string">
      The API URL for the release asset. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="assets_url" type="string">
  The API URL for the assets associated with this GitHub release. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="author" type="object">
  The author for this release. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the avatar image for the GitHub user who authored the release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the user who authored the GitHub release. This field is optional and may be empty if the author's email is not publicly available or not exposed by the API. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to the author's public events on GitHub, providing a link to their recent activity. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The GitHub API URL for the release author's list of followers. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL template for the list of users followed by the author, with placeholders for the username and an optional other user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The API URL for the author's gists, typically formatted as https://api.github.com/users/&#123;username&#125;/gists&#123;/gist_id&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The Gravatar ID of the release author, used to construct their avatar image URL. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL of the author's GitHub profile page. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The unique identifier for the GitHub user account of the release's author. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user who authored the release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the GitHub user who authored the release. This field is optional and may be empty if the user has not set a name in their GitHub profile. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL node ID for the release author, used as a unique identifier in GitHub's GraphQL API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The API endpoint URL that lists the organizations the release author belongs to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The GitHub API URL for events received by the release author. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the author's public repositories on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Indicates whether the author is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      The timestamp of when the release author starred the associated repository, in ISO 8601 format. This field is optional and may be absent if the author has not starred the repository. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template for the list of repositories starred by the author. It may contain placeholders such as &#123;owner&#125; and &#123;repo&#125; in the GitHub API format. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The URL to the GitHub API endpoint for the subscriptions of the release author. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub account that authored the release, such as 'User' or 'Bot'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The API URL for the GitHub user who authored the release. For example, https://api.github.com/users/octocat. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      The user's view type, either 'PUBLIC' or 'PRIVATE', indicating whether the author is a public or private user. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="body" type="string">
  Text describing the contents of the tag. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="body_html" type="string">
  The rendered HTML version of the release body or description. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="body_text" type="string">
  The full body text of the GitHub release, typically containing release notes or a description. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp of when the release was created, in ISO 8601 format (e.g., 2023-01-01T00:00:00Z). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="discussion_url" type="string">
  The URL of the release discussion. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="draft" type="bool">
  `true` to create a draft (unpublished) release, `false` to create a published one. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="html_url" type="string">
  The GitHub web URL where the release can be viewed. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="number">
  The unique numeric identifier for the release, automatically assigned by GitHub when the release is created. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="immutable" type="bool">
  Whether or not the release is immutable. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="mentions_count" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the release. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="node_id" type="string">
  The GraphQL Node ID of the GitHub release. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="prerelease" type="bool">
  `true` to identify the release as a prerelease. `false` to identify the release as a full release. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="published_at" type="string">
  The timestamp of when the release was published. This field is computed and cannot be set by the user. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="reactions" type="object">
  An object summarizing reactions to the release, mapping each reaction type (e.g., +1, -1, laugh, hooray) to the count received. This field is automatically computed and read-only. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="confused" type="number" required>
      The number of users who reacted with the 'confused' emoji to this release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="eyes" type="number" required>
      The number of 'eyes' emoji reactions on the release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="heart" type="number" required>
      The count of heart reactions (❤️) on the release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hooray" type="number" required>
      The number of 'hooray' reactions (🎉) on the GitHub release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="laugh" type="number" required>
      The number of users who reacted with the 'laugh' (😂) emoji on this GitHub release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rocket" type="number" required>
      The count of rocket (🚀) reactions on the GitHub release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_count" type="number" required>
      The total count of reactions on the release. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL to the API endpoint that returns the reactions for this GitHub release. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tarball_url" type="string">
  The URL to download the tarball source archive for this release. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="target_commitish" type="string">
  Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of the last update to the release, in ISO 8601 format (e.g., '2023-01-15T12:34:56Z'). This value is set by the system and cannot be modified directly. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="upload_url" type="string">
  The URL endpoint for uploading assets to this GitHub release. It is a computed, read-only value and typically includes a placeholder for the asset name. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  The API URL that points to this release on GitHub (e.g., https://api.github.com/repos/&#123;owner&#125;/&#123;repo&#125;/releases/&#123;id&#125;). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="zipball_url" type="string">
  The URL of the zip archive of the source code for this release. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
