---
title: "github_release_asset"
description: "`github_release_asset` is one real file attached to a release beyond the zip and tarball archives GitHub generates automatically -- up to 1,000..."
---

`github_release_asset` is one real file attached to a release beyond the zip and tarball archives GitHub generates automatically -- up to 1,000 additional files per release, each under 2 GiB, for shipping compiled binaries or other build output alongside the source.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	release "github.com/ubiquex/ubx-sdk-github/sdk/go/github/release"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own asset"})

    		ubx.Resource(release.Asset, "example", release.AssetConfig{
    			AssetId:   "example",
    			Owner:     "example",
    			ReleaseId: "example",
    			Repo:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Asset } from "./local-sdk/github/sdk/typescript/github/release/asset.ts";

    export default stack("example", () => {
      intent({ summary: "example own asset" });

      resource(Asset, "example", {
        assetId: "example",
        owner: "example",
        releaseId: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.release import Asset, AssetConfig

    def describe():
        ubx.intent("example own asset")

        ubx.resource(Asset, "example", AssetConfig(
            asset_id="example",
            owner="example",
            release_id="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_release_asset, with asset id example, owner
    example, release id example, repo example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="asset_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="release_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="browser_download_url" type="string">
  The browser-friendly URL for downloading the release asset. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="content_type" type="string">
  The MIME type of the release asset, as reported by GitHub for the uploaded file. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp of when the release asset was created, set by the API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="digest" type="string">
  The SHA-256 digest of the release asset file, computed by GitHub after upload. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="download_count" type="number">
  The number of times the release asset has been downloaded. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="number">
  The unique identifier for this release asset, assigned by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="label" type="string">
  The custom label assigned to the release asset, as returned by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The file name of the asset. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="node_id" type="string">
  The unique identifier for the release asset, used as a GraphQL node ID. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="size" type="number">
  The size of the release asset file in bytes. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="state" type="string">
  State of the release asset. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of the last update to the release asset, in ISO 8601 format (e.g., 2013-02-27T19:35:32Z). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="uploader" type="object">
  The uploader for this release asset. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the GitHub user's avatar image. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the uploader, if publicly available. May be null if the user has not made their email public. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The API URL that lists events performed by this user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The URL to the GitHub API endpoint that lists the followers of the uploader user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL template to list the users that this uploader follows, with a placeholder for the target user (e.g., &#123;/other_user&#125;). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL to the user's gists on GitHub, typically in the form 'https://api.github.com/users/&#123;username&#125;/gists&#123;/gist_id&#125;'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The Gravatar hash of the uploader user, used to construct their avatar image URL. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The GitHub profile URL for the user who uploaded the release asset. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Required.
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user who uploaded the release asset. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the user who uploaded the release asset. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL node ID of the user who uploaded the release asset. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The URL to the organizations endpoint for the user who uploaded the release asset. This endpoint returns the list of organizations the uploader belongs to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the GitHub API endpoint that lists events received by the uploader user (the user who uploaded the release asset). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the list of repositories belonging to the user who uploaded the release asset. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Indicates whether the uploader is a GitHub site administrator (e.g., on GitHub.com or GitHub Enterprise). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template for listing the uploader's starred GitHub repositories, with placeholders &#123;owner&#125; and &#123;repo&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The URL to the GitHub subscriptions of the user who uploaded the release asset. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the uploader account, either 'User' or 'Organization'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL of the uploader's GitHub profile. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="url" type="string">
  The API URL for the release asset on GitHub, used for programmatic access. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
