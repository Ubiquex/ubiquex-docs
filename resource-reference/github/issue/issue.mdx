---
title: "github_issue"
description: "A complete, runnable github_issue program, in every SDK language."
---

`github_issue` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	issue "github.com/ubiquex/ubx-sdk-github/sdk/go/github/issue"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own issue"})

    		ubx.Resource(issue.Issue, "ci-runner", issue.IssueConfig{
    			IssueNumber: "example",
    			Owner:       "example",
    			Repo:        "example",
    			Title:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Issue } from "./local-sdk/github/sdk/typescript/github/issue/issue.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own issue" });

      resource(Issue, "ci-runner", {
        issueNumber: "example",
        owner: "example",
        repo: "example",
        title: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.issue import Issue, IssueConfig

    def describe():
        ubx.intent("payments own issue")

        ubx.resource(Issue, "ci-runner", IssueConfig(
            issue_number="example",
            owner="example",
            repo="example",
            title="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_issue, with issue number example, owner example,
    repo example, title example.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="assignee" type="string">
  Login for the user that this issue should be assigned to. _NOTE: Only users with push access can set the assignee for new issues. The assignee is silently dropped otherwise. **This field is closing down.**_ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="assignees" type="list(string)">
  Logins for Users to assign to this issue. _NOTE: Only users with push access can set assignees for new issues. Assignees are silently dropped otherwise._ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="body" type="string">
  The contents of the issue. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="issue_field_values" type="list(object)">
  An array of issue field values to set on this issue. Each field value must include the field ID and the value to set. Issue fields are only available for organization-owned repositories with the feature enabled. Field values are silently dropped otherwise. Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="field_id" type="number">
      The unique identifier of the project field that this value is associated with. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="any">
      The value assigned to the custom issue field. The type is dynamic and depends on the custom field's definition (e.g., text, number, date, or selection). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="issue_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="labels" type="list(any)">
  Labels to associate with this issue. _NOTE: Only users with push access can set labels for new issues. Labels are silently dropped otherwise._ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="milestone" type="any">
  The milestone associated with the issue, if any. This field is computed and read-only; it cannot be set from the client. Optional; if omitted, computed by GitHub. **(AI-inferred)**
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="title" type="any" required>
  The title of the issue. Required.
</ResponseField>

<ResponseField name="type" type="string">
  The name of the issue type to associate with this issue. _NOTE: Only users with push access can set the type for new issues. The type is silently dropped otherwise._ Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="active_lock_reason" type="string">
  The reason the issue is locked, if applicable. Possible values are `off-topic`, `too heated`, `resolved`, and `spam`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="assignee" type="string">
  Login for the user that this issue should be assigned to. _NOTE: Only users with push access can set the assignee for new issues. The assignee is silently dropped otherwise. **This field is closing down.**_ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="assignees" type="list(string)">
  Logins for Users to assign to this issue. _NOTE: Only users with push access can set assignees for new issues. Assignees are silently dropped otherwise._ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="author_association" type="string">
  How the author is associated with the repository. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="body" type="string">
  The contents of the issue. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="body_html" type="string">
  The HTML-rendered version of the issue body. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="body_text" type="string">
  The plain-text version of the issue body, with Markdown removed. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="closed_at" type="string">
  The timestamp when the issue was closed, in RFC3339 format (e.g., 2023-01-01T00:00:00Z). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="closed_by" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the avatar image for the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the user who closed the issue. This is only present if the user has a public email associated with their GitHub account; otherwise it is null. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to fetch the events associated with the user who closed the issue. This is a standard GitHub API user attribute, providing a link to the user's public events. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The URL to the followers list of the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL to retrieve the list of users that this user follows. This is a template URL that can include the placeholder &#123;/other_user&#125; to check if a specific user is followed. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL to access the gists of the user who closed the issue, via the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The unique Gravatar ID for the user, used to generate avatar image URLs. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The HTML URL of the GitHub profile for the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The unique numeric identifier of the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The username of the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the user who closed the issue, as provided by the user's GitHub profile. This field is optional and may be absent if the user has not set a name. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL node ID of the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The URL to the GitHub API endpoint listing the organizations that this user belongs to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the user's received events feed, for the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      URL to the GitHub API endpoint listing the repositories owned by the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Indicates whether the user is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template for the user's starred repositories, with placeholders for &#123;owner&#125; and &#123;repo&#125; parameters. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The URL to view the subscriptions of the user who closed the issue. This field is part of the user object returned in the `closed_by` field of an issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub account that closed the issue, such as 'User' or 'Bot'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The REST API URL for the user who closed the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Indicates the user's profile visibility type, either 'public' or 'private'. In the context of the user who closed the issue. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="comments" type="number">
  The number of comments on the GitHub issue. This value is computed and provided by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="comments_url" type="string">
  The API endpoint URL that returns the comments for this issue. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The creation timestamp of the GitHub issue, set by the provider and returned as a string in ISO 8601 format (e.g., 2023-01-01T12:00:00Z). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="draft" type="bool">
  A boolean indicating whether the issue is a draft. Draft issues are hidden from non-collaborators and are not ready for review. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="events_url" type="string">
  The URL to retrieve the events associated with this issue, such as labeling, closing, or reopening. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="html_url" type="string">
  The URL to view the issue on GitHub's website. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="issue_dependencies_summary" type="object">
  Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="blocked_by" type="number" required>
      The number of issues that are blocking this issue, i.e., the count of dependencies that must be resolved before this issue can be completed. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="blocking" type="number" required>
      The number of issues whose implementation is blocked by this issue, based on declared issue dependencies. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_blocked_by" type="number" required>
      The total number of issues that this issue is blocked by. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_blocking" type="number" required>
      Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="issue_field_values" type="list(object)">
  An array of issue field values to set on this issue. Each field value must include the field ID and the value to set. Issue fields are only available for organization-owned repositories with the feature enabled. Field values are silently dropped otherwise. Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="field_id" type="number">
      The unique identifier of the project field that this value is associated with. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="value" type="any">
      The value assigned to the custom issue field. The type is dynamic and depends on the custom field's definition (e.g., text, number, date, or selection). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="labels" type="list(any)">
  Labels to associate with this issue. _NOTE: Only users with push access can set labels for new issues. Labels are silently dropped otherwise._ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="labels_url" type="string">
  The API endpoint URL for querying the labels of this issue. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="locked" type="bool">
  Indicates whether the issue is locked for comments, meaning only users with write access can comment. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="milestone" type="any">
  The milestone associated with the issue, if any. This field is computed and read-only; it cannot be set from the client. Optional; if omitted, computed by GitHub. **(AI-inferred)**
</ResponseField>

<ResponseField name="node_id" type="string">
  The Node ID of the issue, used by GitHub's GraphQL API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="number" type="number">
  Number uniquely identifying the issue within its repository Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="parent_issue_url" type="string">
  URL to get the parent issue of this issue, if it is a sub-issue Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="performed_via_github_app" type="object">
  GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user accounts and granted access to specific repositories. They come with granular permissions and built-in webhooks. GitHub apps are first class actors within GitHub. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="client_id" type="string">
      The client ID of the GitHub App that performed the action. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      The ISO 8601 timestamp of when the GitHub App was created. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string" required>
      The description of the GitHub App that performed the action. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events" type="list(string)" required>
      The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation. Required.
    </ResponseField>
    <ResponseField name="external_url" type="string" required>
      The URL of the GitHub App's homepage or external reference site. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to the GitHub App's public page on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Unique identifier of the GitHub app Required.
    </ResponseField>
    <ResponseField name="installations_count" type="number">
      The number of installations associated with the GitHub app. Only returned when the integration is requesting details about itself. Optional.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the GitHub app Required.
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL global node ID for the issue, usable as an identifier in GraphQL queries. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="owner" type="object" required>
      The user or organization that owns the GitHub app. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string">
          The URL of the avatar image for the owner (user or organization) of the GitHub App that performed the action. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="created_at" type="string">
          The timestamp (as a string in ISO 8601 format) of when the GitHub App owner account was created. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="description" type="string">
          The description of the organization that owns the GitHub App. This field is only populated when the owner is an organization. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          Optional; if omitted, computed by GitHub.
        </ResponseField>
        <ResponseField name="events_url" type="string">
          The URL to the events for this GitHub user or organization. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string">
          The URL to the list of followers of the owner (user or organization) of the GitHub App that performed the issue. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string">
          The URL template for the list of users this owner is following, with a placeholder for the username (e.g., https://api.github.com/users/&#123;username&#125;/following). Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string">
          The URL to fetch the owner's public gists, following the standard GitHub API URL format for user gists. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string">
          The Gravatar ID of the GitHub App owner, used to construct the owner's avatar URL from Gravatar. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string">
          The URL to the GitHub profile of the app owner. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number">
          The unique numeric identifier of the user or organization that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="login" type="string">
          The GitHub username of the owner of the GitHub App that performed the action. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The display name of the user or organization that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string">
          The GraphQL node ID of the user or organization that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string">
          The URL to retrieve the list of organizations associated with this GitHub App owner, as defined by the GitHub API. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string">
          The URL to the user's received events endpoint on GitHub, containing the user's incoming events. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string">
          The URL to the list of repositories belonging to this owner (user or organization). Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool">
          Indicates whether the owner is a site administrator for the GitHub instance (e.g., on GitHub Enterprise). Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="slug" type="string">
          The slug of the owner of the GitHub App that performed the action on the issue. For users and organizations, this is the lowercased, URL-friendly identifier (e.g., 'octocat'). Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          Optional; if omitted, computed by GitHub.
        </ResponseField>
        <ResponseField name="starred_url" type="string">
          The URL to the list of repositories starred by this owner (user or organization) of the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string">
          The API endpoint URL for the owner's subscriptions, indicating resources the owner follows or is subscribed to on GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of GitHub account that owns the app, either `User` or `Organization`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          Optional; if omitted, computed by GitHub.
        </ResponseField>
        <ResponseField name="url" type="string">
          The API URL for the GitHub account (user or organization) that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          The user's profile view type, either 'public' or 'private'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="website_url" type="string">
          The website URL of the user or organization that owns the GitHub App which performed the action on the issue. Optional; if omitted, computed by GitHub. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="permissions" type="object" required>
      The set of permissions for the GitHub app Required.
      <Expandable title="properties">
        <ResponseField name="checks" type="string">
          The permission level for the Checks API. Set to 'read' or 'write' to grant the GitHub App access. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="contents" type="string">
          The permission level granted to the GitHub App for accessing repository contents. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="deployments" type="string">
          The permission level for deployments granted to the GitHub App. Allowed values are 'read', 'write', or 'none'. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="issues" type="string">
          The permission level for issues granted to the GitHub App that performed the issue. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="metadata" type="string">
          The permission level for metadata access granted to the GitHub App, typically 'read' or 'write'. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="slug" type="string">
      The slug name of the GitHub app Optional.
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      The timestamp of when the GitHub App was last updated, in ISO 8601 format. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="pinned_comment" type="object">
  Comments provide a way for people to collaborate on an issue. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="author_association" type="string">
      How the author is associated with the repository. Optional.
    </ResponseField>
    <ResponseField name="body" type="string">
      Contents of the issue comment Optional.
    </ResponseField>
    <ResponseField name="body_html" type="string">
      The HTML-rendered version of the comment's body content. This field is only present if the comment body has been processed by GitHub's markdown renderer. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="body_text" type="string">
      The plain-text body of the pinned comment, with any Markdown or HTML formatting removed. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      The timestamp indicating when the pinned comment was created. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to view the pinned comment on the GitHub website. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Unique identifier of the issue comment Required.
    </ResponseField>
    <ResponseField name="issue_url" type="string" required>
      The URL of the issue to which this comment is pinned. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="minimized" type="object">
      Details about why an issue comment was minimized. Optional.
      <Expandable title="properties">
        <ResponseField name="reason" type="string" required>
          The reason the comment was minimized. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      Required.
    </ResponseField>
    <ResponseField name="performed_via_github_app" type="object">
      GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user accounts and granted access to specific repositories. They come with granular permissions and built-in webhooks. GitHub apps are first class actors within GitHub. Optional.
      <Expandable title="properties">
        <ResponseField name="client_id" type="string">
          The client ID of the GitHub App that performed the action. This is the OAuth client ID for the app, present when the action was performed via a GitHub App. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="created_at" type="string" required>
          Required.
        </ResponseField>
        <ResponseField name="description" type="string" required>
          The description of the GitHub App that performed the action. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events" type="list(string)" required>
          The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation. Required.
        </ResponseField>
        <ResponseField name="external_url" type="string" required>
          The URL of the GitHub App's website or external homepage. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string" required>
          The HTML URL for the GitHub App that performed the action. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number" required>
          Unique identifier of the GitHub app Required.
        </ResponseField>
        <ResponseField name="installations_count" type="number">
          The number of installations associated with the GitHub app. Only returned when the integration is requesting details about itself. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the GitHub app Required.
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The GraphQL node identifier of the GitHub App that performed the action. This is a globally unique identifier for the app resource. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="owner" type="object" required>
          The user or organization that owns the GitHub App that performed the action. Required. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="avatar_url" type="string">
              The URL of the avatar image for the owner (user or organization) of the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="created_at" type="string">
              The date and time when the owner (user or organization) of the GitHub App was created. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by GitHub.
            </ResponseField>
            <ResponseField name="email" type="string">
              Email address of the user or organization that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="events_url" type="string">
              The URL to retrieve events for the owner (user or organization) of the GitHub App, as defined in GitHub's user/org representation. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="followers_url" type="string">
              The URL to retrieve the list of followers of this GitHub user or organization. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="following_url" type="string">
              The URL template to list the users that this user follows. It includes a placeholder for the username parameter. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="gists_url" type="string">
              The URL to the gists of the owner, typically in the form https://api.github.com/users/&#123;owner&#125;/gists&#123;/gist_id&#125;. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="gravatar_id" type="string">
              The Gravatar hash for the user, used to construct their avatar URL. May be empty or deprecated in favor of avatar_url. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="html_url" type="string">
              The GitHub profile URL for the account (user or organization) that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="number">
              The numeric ID of the user or organization that owns the GitHub App. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="login" type="string">
              The username or organization name that owns the GitHub App, used as a unique identifier for the owner. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by GitHub.
            </ResponseField>
            <ResponseField name="node_id" type="string">
              The GraphQL node ID of the owner of the GitHub App that performed the action. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="organizations_url" type="string">
              The URL to the API endpoint that lists the organizations this user (the owner of the GitHub App) belongs to. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="received_events_url" type="string">
              The URL to the received events endpoint for this user, which lists events received by the user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="repos_url" type="string">
              The URL to the GitHub API endpoint that lists repositories owned by this user or organization. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="site_admin" type="bool">
              Whether the owner (user or organization) has GitHub site administrator privileges. `true` if site admin, `false` otherwise. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="slug" type="string">
              Optional; if omitted, computed by GitHub.
            </ResponseField>
            <ResponseField name="starred_at" type="string">
              Optional; if omitted, computed by GitHub.
            </ResponseField>
            <ResponseField name="starred_url" type="string">
              The URL template for listing repositories starred by the owner of the GitHub App. For example: `https://api.github.com/users/&#123;owner&#125;/starred&#123;/owner&#125;&#123;/repo&#125;`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subscriptions_url" type="string">
              The API URL to the subscriptions of the owner (user or organization) associated with the GitHub app. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of GitHub account that owns the GitHub App, typically either 'User' or 'Organization'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="updated_at" type="string">
              The timestamp of the last update to the owner (user or organization) of the GitHub App that performed the action. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string">
              The API URL for the owner (user or organization) of the GitHub App that performed the comment. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_view_type" type="string">
              The user view type of the GitHub App owner, indicating whether the account is a public or private user. Possible values are 'public' and 'private'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="website_url" type="string">
              The URL of the website associated with the user or organization that owns the GitHub app. Optional; if omitted, computed by GitHub. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="permissions" type="object" required>
          The set of permissions for the GitHub app Required.
          <Expandable title="properties">
            <ResponseField name="checks" type="string">
              The level of access granted to the GitHub App for checks, indicating the app's permission to read or write check runs and check suites. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="contents" type="string">
              The access level granted to the GitHub App for repository contents. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="deployments" type="string">
              The level of access granted to the GitHub App for deployments. Can be 'read' or 'write'. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="issues" type="string">
              The permission level the GitHub App has for issues, as defined in the GitHub API. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="metadata" type="string">
              The level of access granted for the 'metadata' permission of the GitHub App. Possible values are 'read', 'write', or 'none' if not explicitly granted. Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="slug" type="string">
          The slug name of the GitHub app Optional.
        </ResponseField>
        <ResponseField name="updated_at" type="string" required>
          The timestamp indicating when the GitHub App was last updated, typically in ISO 8601 format. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pin" type="object">
      Context around who pinned an issue comment and when it was pinned. Optional.
      <Expandable title="properties">
        <ResponseField name="pinned_at" type="string" required>
          The timestamp indicating when the comment was pinned. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pinned_by" type="object" required>
          A GitHub user. Required.
          <Expandable title="properties">
            <ResponseField name="avatar_url" type="string" required>
              The URL of the GitHub user's avatar image for the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="email" type="string">
              The email address of the user who pinned the comment. This field is optional and may be empty if the user has not made their email public. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="events_url" type="string" required>
              The URL to the events of the user who pinned the comment, as provided by the GitHub API. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="followers_url" type="string" required>
              The URL to retrieve the followers list of the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="following_url" type="string" required>
              The API URL that lists the users that this user follows. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="gists_url" type="string" required>
              The URL template to fetch the user's gists. This field is required, meaning it is always present in the user object. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="gravatar_id" type="string" required>
              A hash of the user's email used to fetch their Gravatar avatar. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="html_url" type="string" required>
              The URL to the user's profile page on GitHub. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="id" type="number" required>
              The unique identifier of the GitHub user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="login" type="string" required>
              The GitHub username of the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="name" type="string">
              The display name of the user who pinned the comment. Optional. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="node_id" type="string" required>
              The GraphQL node ID of the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="organizations_url" type="string" required>
              The URL to the organizations list for the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="received_events_url" type="string" required>
              The URL to retrieve events received by the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="repos_url" type="string" required>
              The URL to the list of repositories owned by the user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="site_admin" type="bool" required>
              Whether the user who pinned the comment is a GitHub site administrator. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="starred_at" type="string">
              Optional.
            </ResponseField>
            <ResponseField name="starred_url" type="string" required>
              The URL template for the user's starred repositories, including placeholders for owner and repo (e.g., https://api.github.com/users/&#123;username&#125;/starred&#123;/owner&#125;&#123;/repo&#125;). Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="subscriptions_url" type="string" required>
              The URL to the API endpoint listing the repositories and issues that this user is subscribed to. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="type" type="string" required>
              The type of the GitHub account that pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="url" type="string" required>
              The URL of the GitHub user who pinned the comment. Required. **(AI-inferred)**
            </ResponseField>
            <ResponseField name="user_view_type" type="string">
              Indicates whether the user's GitHub profile is public or private (e.g., 'public' or 'private'). Optional. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="reactions" type="object">
      The object summarizing reactions to the pinned comment, including total_count and per-type counts for reaction types such as +1, -1, laugh, confused, heart, hooray, rocket, and eyes. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="confused" type="number" required>
          The number of confused reactions (the 😕 emoji) on the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="eyes" type="number" required>
          The number of eyes (👀) reactions on this pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="heart" type="number" required>
          The number of heart reactions on the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="hooray" type="number" required>
          The number of hooray reactions on this comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="laugh" type="number" required>
          The number of 'laugh' reactions (the 😆 emoji) on the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rocket" type="number" required>
          The number of rocket (🚀) reactions on the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="total_count" type="number" required>
          The total number of reactions (of all types) on the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The API URL where this reaction can be accessed. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      The timestamp of when the pinned comment was last updated (in RFC3339 format). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      URL for the issue comment Required.
    </ResponseField>
    <ResponseField name="user" type="object" required>
      A GitHub user. Required.
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string" required>
          The URL of the user's avatar image. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address of the user who authored the pinned comment. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events_url" type="string" required>
          The URL template for the user's public events endpoint, typically in the form 'https://api.github.com/users/&#123;username&#125;/events&#123;/privacy&#125;'. It is used to list events performed by the user. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string" required>
          The API URL to list the followers of this user. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string" required>
          URL template for the list of users this user is following, with a placeholder for the username (e.g., https://api.github.com/users/&#123;username&#125;/following). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string" required>
          A URL template that can be used to list or access the user's gists (e.g., https://api.github.com/users/&#123;user&#125;/gists&#123;/gist_id&#125;). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string" required>
          The user's Gravatar ID, an MD5 hash of the user's email address, used to construct the user's Gravatar avatar URL. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string" required>
          The URL to the user's GitHub profile. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number" required>
          Required.
        </ResponseField>
        <ResponseField name="login" type="string" required>
          The GitHub username of the user who authored the pinned comment. This field is always present and uniquely identifies the user. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The display name of the user, if provided. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The GraphQL node identifier of the user who authored the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string" required>
          The API URL to list the organizations that this user belongs to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string" required>
          The URL to retrieve the list of events received by this user, per the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string" required>
          The URL to access the list of repositories owned by this user. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool" required>
          True if the user is a site administrator, false otherwise. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="starred_url" type="string" required>
          The URL template for the list of repositories starred by this user. In the GitHub API, this is typically a template URI that requires expansion of placeholders such as &#123;owner&#125; and &#123;repo&#125;. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string" required>
          The URL to the user's subscriptions, where the user's subscribed repositories can be accessed. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of user account (e.g., 'User' or 'Organization') associated with the pinned comment. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The API URL of the user, providing a link to the user resource in the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="pull_request" type="object">
  The pull_request object is present when the issue is also a pull request, and contains metadata about the pull request such as its URL and merge status. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="diff_url" type="string" required>
      The URL to the diff for this pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to view the pull request on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="merged_at" type="string">
      The timestamp of when the pull request was merged, if applicable. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="patch_url" type="string" required>
      The URL to the patch file for this pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL of the pull request. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="reactions" type="object">
  The reactions summary for the issue, containing counts of user reactions by type (e.g., thumbs up, thumbs down, laugh, hooray, confused, heart, rocket, eyes). Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="confused" type="number" required>
      The number of 'confused' reactions on the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="eyes" type="number" required>
      The number of 'eyes' reactions (the :eyes: emoji) on the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="heart" type="number" required>
      The number of heart (❤️) reactions on the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hooray" type="number" required>
      The number of reactions with the 'hooray' emoji on the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="laugh" type="number" required>
      The number of laugh reactions (😂) on the GitHub issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rocket" type="number" required>
      The number of rocket reactions (🚀) on the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_count" type="number" required>
      The total number of reactions on the issue's reaction summary. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL of the reaction resource, pointing to the API endpoint for this reaction. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="repository" type="object">
  A repository on GitHub. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="allow_auto_merge" type="bool">
      Whether to allow Auto-merge to be used on pull requests. Optional.
    </ResponseField>
    <ResponseField name="allow_forking" type="bool">
      Whether to allow forking this repo Optional.
    </ResponseField>
    <ResponseField name="allow_merge_commit" type="bool">
      Whether to allow merge commits for pull requests. Optional.
    </ResponseField>
    <ResponseField name="allow_rebase_merge" type="bool">
      Whether to allow rebase merges for pull requests. Optional.
    </ResponseField>
    <ResponseField name="allow_squash_merge" type="bool">
      Whether to allow squash merges for pull requests. Optional.
    </ResponseField>
    <ResponseField name="allow_update_branch" type="bool">
      Whether or not a pull request head branch that is behind its base branch can always be updated even if it is not required to be up to date before merging. Optional.
    </ResponseField>
    <ResponseField name="anonymous_access_enabled" type="bool">
      Whether anonymous git access is enabled for this repository Optional.
    </ResponseField>
    <ResponseField name="archive_url" type="string" required>
      The URL template for downloading the repository's source code archive, with placeholders for archive format and reference (e.g., zipball and ref). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="archived" type="bool" required>
      Whether the repository is archived. Required.
    </ResponseField>
    <ResponseField name="assignees_url" type="string" required>
      The API URL template for managing repository assignees, with an optional placeholder for a specific assignee. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="blobs_url" type="string" required>
      The URL template for accessing git blobs in the repository, with a placeholder for the blob SHA (e.g., `http://api.github.com/repos/&#123;owner&#125;/&#123;repo&#125;/git/blobs&#123;/sha&#125;`). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="branches_url" type="string" required>
      The URL to access the list of branches in this repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="clone_url" type="string" required>
      The URL used to clone the repository, typically in HTTPS or SSH format. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="code_search_index_status" type="object">
      The status of the code search index for this repository Optional.
      <Expandable title="properties">
        <ResponseField name="lexical_commit_sha" type="string">
          The SHA of the commit that the lexical code search index is based on. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="lexical_search_ok" type="bool">
          Indicates whether the repository's lexical search index is currently functional, allowing lexical code search to work. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="collaborators_url" type="string" required>
      The URL template to access the list of collaborators for the repository. In the GitHub API, this typically takes the form: https://api.github.com/repos/&#123;owner&#125;/&#123;repo&#125;/collaborators&#123;/collaborator&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="comments_url" type="string" required>
      The API URL to retrieve the list of comments on this issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="commits_url" type="string" required>
      The URL template for the commits in the repository associated with this issue, often including a placeholder for a commit SHA. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="compare_url" type="string" required>
      The URL template for comparing commits in the repository, typically in the form of `https://api.github.com/repos/&#123;owner&#125;/&#123;repo&#125;/compare/&#123;base&#125;...&#123;head&#125;`. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="contents_url" type="string" required>
      The API URL template for accessing the contents of the repository, as defined by GitHub's REST API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="contributors_url" type="string" required>
      The URL to access the list of contributors for this repository via the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      The creation timestamp of the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="default_branch" type="string" required>
      The default branch of the repository. Required.
    </ResponseField>
    <ResponseField name="delete_branch_on_merge" type="bool">
      Whether to delete head branches when pull requests are merged Optional.
    </ResponseField>
    <ResponseField name="deployments_url" type="string" required>
      The API endpoint URL for accessing the list of deployments for this repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string" required>
      The description of the GitHub repository associated with the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="disabled" type="bool" required>
      Returns whether or not this repository disabled. Required.
    </ResponseField>
    <ResponseField name="downloads_url" type="string" required>
      The URL to the repository's downloads endpoint, which provides access to the downloads associated with the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to retrieve events for the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="fork" type="bool" required>
      Indicates whether the repository is a fork of another repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="forks" type="number" required>
      The number of times this repository has been forked. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="forks_count" type="number" required>
      The total number of times the repository has been forked. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="forks_url" type="string" required>
      The URL to list the forks of the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="full_name" type="string" required>
      The full name of the repository, including the owner and repository name (e.g., 'octocat/hello-world'). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="git_commits_url" type="string" required>
      URL template for the git commits of the repository, with a placeholder for the commit SHA, e.g., `https://api.github.com/repos/&#123;owner&#125;/&#123;repo&#125;/git/commits&#123;/sha&#125;`. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="git_refs_url" type="string" required>
      The URL to access the git references (e.g., branches and tags) for the repository. This URL can be used to list or retrieve refs via the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="git_tags_url" type="string" required>
      The API endpoint URL for accessing the repository's git tags. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="git_url" type="string" required>
      The URL to the Git repository associated with the GitHub repository, typically in the format git://github.com/owner/repository.git, used for cloning and remote operations. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="has_discussions" type="bool">
      Whether discussions are enabled. Optional.
    </ResponseField>
    <ResponseField name="has_downloads" type="bool" required>
      Whether downloads are enabled. Required.
    </ResponseField>
    <ResponseField name="has_issues" type="bool" required>
      Whether issues are enabled. Required.
    </ResponseField>
    <ResponseField name="has_pages" type="bool" required>
      Whether the repository has GitHub Pages enabled. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="has_projects" type="bool" required>
      Whether projects are enabled. Required.
    </ResponseField>
    <ResponseField name="has_pull_requests" type="bool">
      Whether pull requests are enabled. Optional.
    </ResponseField>
    <ResponseField name="has_wiki" type="bool" required>
      Whether the wiki is enabled. Required.
    </ResponseField>
    <ResponseField name="homepage" type="string" required>
      The URL of the repository's homepage. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hooks_url" type="string" required>
      The URL to the repository's webhooks API endpoint, used to manage hooks for the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL of the repository on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Unique identifier of the repository Required.
    </ResponseField>
    <ResponseField name="is_template" type="bool">
      Whether this repository acts as a template that can be used to generate new repositories. Optional.
    </ResponseField>
    <ResponseField name="issue_comment_url" type="string" required>
      The URL to the issue comments endpoint for this repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="issue_events_url" type="string" required>
      The API URL template for accessing issue events in this repository, typically containing placeholders for event numbers. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="issues_url" type="string" required>
      The API URL to the repository's issues endpoint, which may include a placeholder for a specific issue number. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="keys_url" type="string" required>
      The URL endpoint for the repository's deploy keys, with an optional template placeholder for a specific key ID. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="labels_url" type="string" required>
      The API endpoint URL for accessing the labels of the repository, possibly containing a templated placeholder (e.g., '/labels&#123;/name&#125;') for a specific label name. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="language" type="string" required>
      The primary programming language of the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="languages_url" type="string" required>
      The URL to retrieve the list of programming languages used in the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="license" type="object" required>
      License Simple Required.
      <Expandable title="properties">
        <ResponseField name="html_url" type="string">
          The URL to the HTML page for this license on GitHub. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="key" type="string" required>
          The SPDX license key for the repository's license (e.g., MIT, Apache-2.0, GPL-3.0). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The full name of the license, such as 'MIT License' or 'Apache License 2.0'. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The unique identifier for this license in GitHub's GraphQL API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="spdx_id" type="string" required>
          The SPDX license identifier for the repository's license, such as 'MIT' or 'Apache-2.0'. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The API URL for the license information on GitHub. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="master_branch" type="string">
      The name of the repository's master branch. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="merge_commit_message" type="string">
      The default value for a merge commit message. - `PR_TITLE` - default to the pull request's title. - `PR_BODY` - default to the pull request's body. - `BLANK` - default to a blank commit message. Optional.
    </ResponseField>
    <ResponseField name="merge_commit_title" type="string">
      The default value for a merge commit title. - `PR_TITLE` - default to the pull request's title. - `MERGE_MESSAGE` - default to the classic title for a merge message (e.g., Merge pull request #123 from branch-name). Optional.
    </ResponseField>
    <ResponseField name="merges_url" type="string" required>
      The URL template for the merge endpoint, used to merge branches in this repository via the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="milestones_url" type="string" required>
      The URL template for accessing milestones in the repository, with a placeholder for the specific milestone number (e.g., `.../milestones&#123;/number&#125;`). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="mirror_url" type="string" required>
      The URL of the original repository if this repository is a mirror of another source. This is typically set for repositories that are mirrored from an external Git repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The name of the repository. Required.
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The unique global node identifier (GraphQL ID) for the repository associated with the issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="notifications_url" type="string" required>
      The URL to access notifications for this repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="open_issues" type="number" required>
      The number of open issues currently in the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="open_issues_count" type="number" required>
      The number of open issues in the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="owner" type="object" required>
      A GitHub user. Required.
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string" required>
          The URL of the avatar image for the repository owner (a user or organization). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address of the repository owner. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events_url" type="string" required>
          The URL to the events endpoint for the repository owner (user or organization). This is used to fetch events associated with the owner. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string" required>
          The API URL to retrieve the list of followers of the repository owner. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string" required>
          The URL template for the list of users the repository owner follows, typically in the format `https://api.github.com/users/&#123;owner&#125;/following&#123;?other_user&#125;`. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string" required>
          The URL for the owner's gists, with a placeholder for the gist ID when applicable. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string" required>
          The Gravatar ID (hash) for the repository owner's avatar, used to construct the Gravatar image URL. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string" required>
          The URL to the GitHub profile page for the repository owner (user or organization). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The unique numeric identifier for the repository owner (a user or organization). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="login" type="string" required>
          The username (for a user) or organization name (for an organization) that owns the repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The globally unique GraphQL node ID for the repository owner, used to identify the user or organization in GitHub's GraphQL API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string" required>
          The API endpoint URL to list the organizations that this user belongs to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string" required>
          The URL to retrieve events received by the repository owner (e.g., via the GitHub API). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string" required>
          The API URL for the list of repositories owned by this user or organization, e.g., https://api.github.com/users/octocat/repos. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool" required>
          Indicates whether the user is a site administrator of GitHub. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          The ISO 8601 timestamp of when the repository owner starred the repository, if applicable. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_url" type="string" required>
          URL to the owner's list of starred repositories, as returned by the GitHub API. This is a template URL that can include placeholders for specifying a particular repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string" required>
          The API endpoint for the repository owner's subscriptions, typically in the form of https://api.github.com/users/&#123;owner&#125;/subscriptions. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of GitHub account that owns the repository: either 'User' for an individual account or 'Organization' for an organization account. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The URL of the repository owner. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="permissions" type="object">
      An object indicating the permission levels the authenticated user has on this repository, typically including flags such as admin, push, and pull. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="admin" type="bool" required>
          Whether the current user has admin permissions on the repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="maintain" type="bool">
          Whether the user has 'maintain' permission for the repository. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="pull" type="bool" required>
          Whether the authenticated user has permission to pull (clone/fetch) the repository's contents. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="push" type="bool" required>
          Indicates whether the authenticated user has push (write) access to the repository. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="triage" type="bool">
          Indicates whether the user has triage permission on the repository. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="private" type="bool" required>
      Whether the repository is private or public. Required.
    </ResponseField>
    <ResponseField name="pull_request_creation_policy" type="string">
      The policy controlling who can create pull requests: all or collaborators_only. Optional.
    </ResponseField>
    <ResponseField name="pulls_url" type="string" required>
      The API endpoint URL for pull requests in this repository, with a template placeholder for the pull request number. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="pushed_at" type="string" required>
      The timestamp of the last push to the repository, in ISO 8601 format (e.g., '2023-01-01T12:00:00Z'). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="releases_url" type="string" required>
      The URL to the releases page of the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="size" type="number" required>
      The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0. Required.
    </ResponseField>
    <ResponseField name="squash_merge_commit_message" type="string">
      The default value for a squash merge commit message: - `PR_BODY` - default to the pull request's body. - `COMMIT_MESSAGES` - default to the branch's commit messages. - `BLANK` - default to a blank commit message. Optional.
    </ResponseField>
    <ResponseField name="squash_merge_commit_title" type="string">
      The default value for a squash merge commit title: - `PR_TITLE` - default to the pull request's title. - `COMMIT_OR_PR_TITLE` - default to the commit's title (if only one commit) or the pull request's title (when more than one commit). Optional.
    </ResponseField>
    <ResponseField name="ssh_url" type="string" required>
      The SSH URL for accessing the repository over SSH, used for Git operations. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stargazers_count" type="number" required>
      The total number of users who have starred the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="stargazers_url" type="string" required>
      The URL to the GitHub API endpoint that lists the users who have starred (stargazers) this repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      The ISO 8601 timestamp of when the repository was starred by the authenticated user. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="statuses_url" type="string" required>
      URL template to access commit statuses for the repository; replace &#123;sha&#125; with a commit SHA to list statuses for that commit. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscribers_url" type="string" required>
      The URL to retrieve the list of users subscribed to the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscription_url" type="string" required>
      The API endpoint URL to manage the authenticated user's subscription to this repository's notifications. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="svn_url" type="string" required>
      The URL to access this repository via Subversion (SVN). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="tags_url" type="string" required>
      The API URL to list the tags of the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="teams_url" type="string" required>
      The URL to the teams endpoint for this repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="temp_clone_token" type="string">
      A temporary clone token that allows cloning the repository without authentication for a limited time. It is typically provided only for private repositories and expires after a short period. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="topics" type="list(string)">
      List of topics (tags) associated with the repository that contains the issue. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="trees_url" type="string" required>
      URL template for accessing the repository's git trees. Replace the `&#123;sha&#125;` placeholder with a commit SHA to retrieve a specific tree. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      The timestamp of the last update to the repository, in ISO 8601 format. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL of the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="use_squash_pr_title_as_default" type="bool">
      Whether a squash merge commit can use the pull request title as default. **This property is closing down. Please use `squash_merge_commit_title` instead. Optional.
    </ResponseField>
    <ResponseField name="visibility" type="string">
      The repository visibility: public, private, or internal. Optional.
    </ResponseField>
    <ResponseField name="watchers" type="number" required>
      The number of users watching (subscribed to) the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="watchers_count" type="number" required>
      The number of users watching the repository. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="web_commit_signoff_required" type="bool">
      Whether to require contributors to sign off on web-based commits Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="repository_url" type="string">
  The URL of the repository in which the issue is located. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="state" type="string">
  State of the issue; either 'open' or 'closed' Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="state_reason" type="string">
  The reason for the current state Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="sub_issues_summary" type="object">
  Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="completed" type="number" required>
      The number of sub-issues that have been completed, as tracked in the sub-issues summary for this GitHub issue. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="percent_completed" type="number" required>
      The overall completion percentage of the sub-issues in this summary, as a number between 0 and 100. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total" type="number" required>
      The total number of sub-issues associated with this issue, as reported in the sub-issues summary. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="timeline_url" type="string">
  The URL to retrieve the timeline of events for this issue via the GitHub API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="type" type="string">
  The name of the issue type to associate with this issue. _NOTE: Only users with push access can set the type for new issues. The type is silently dropped otherwise._ Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp when the issue was last updated, in ISO 8601 format (e.g., 2023-01-15T10:30:00Z). This field is computed and read-only. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  URL for the issue Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="user" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the user's avatar image. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The publicly visible email address associated with the user's GitHub profile, if they have set one. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to list the events performed by the user via the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The URL to the user's followers list on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL to list the GitHub users this user is following. It may include a template such as &#123;/other_user&#125; to check the following relationship for a specific user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL template for the user's gists. In the GitHub API, this is typically 'https://api.github.com/users/&#123;username&#125;/gists&#123;/gist_id&#125;', allowing access to the user's list of gists or a specific gist. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The unique identifier for the user's Gravatar image, used to construct the user's avatar URL. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to the user's GitHub profile page. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Required.
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user. This is a unique handle that identifies the user on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The full display name of the user associated with the GitHub issue, as set in their GitHub profile. May be empty if the user has not provided a name. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The unique identifier for the user in GitHub's GraphQL API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The API endpoint URL to retrieve the list of organizations that the user is a member of. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      URL to the user's received events feed, as provided by the GitHub API. This endpoint lists events involving the user, such as issue and pull request activity. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the user's public repositories on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Indicates whether the user is a site administrator of GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template for the user's starred repositories, typically in the form https://api.github.com/users/&#123;user&#125;/starred&#123;/owner&#125;&#123;/repo&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The API URL for the user's subscriptions, typically pointing to a list of repositories the user is subscribed to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub account, either "User" for a personal account or "Organization" for an organization account. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The GitHub API URL for this user, typically in the form https://api.github.com/users/&#123;username&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
