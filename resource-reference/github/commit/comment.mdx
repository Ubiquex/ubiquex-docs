---
title: "github_commit_comment"
description: "A complete, runnable github_commit_comment program, in every SDK language."
---

`github_commit_comment` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	commit "github.com/ubiquex/ubx-sdk-github/sdk/go/github/commit"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own comment"})

    		ubx.Resource(commit.Comment, "ci-runner", commit.CommentConfig{
    			Body:      "example",
    			CommentId: "example",
    			CommitSha: "example",
    			Owner:     "example",
    			Repo:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Comment } from "./local-sdk/github/sdk/typescript/github/commit/comment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own comment" });

      resource(Comment, "ci-runner", {
        body: "example",
        commentId: "example",
        commitSha: "example",
        owner: "example",
        repo: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.commit import Comment, CommentConfig

    def describe():
        ubx.intent("payments own comment")

        ubx.resource(Comment, "ci-runner", CommentConfig(
            body="example",
            comment_id="example",
            commit_sha="example",
            owner="example",
            repo="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_commit_comment, with body example, comment id
    example, commit sha example, owner example, repo example.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="body" type="string" required>
  The contents of the comment. Required.
</ResponseField>

<ResponseField name="comment_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="commit_sha" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="line" type="number">
  **Closing down notice**. Use **position** parameter instead. Line number in the file to comment on. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="path" type="string">
  Relative path of the file to comment on. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="position" type="number">
  Line index in the diff to comment on. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="author_association" type="string">
  How the author is associated with the repository. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="commit_id" type="string">
  The SHA of the commit that this comment is associated with. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp of when the commit comment was created, in ISO 8601 format (e.g., 2025-01-01T12:00:00Z). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="html_url" type="string">
  The HTML URL of the commit comment. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="number">
  Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="line" type="number">
  **Closing down notice**. Use **position** parameter instead. Line number in the file to comment on. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="node_id" type="string">
  The unique identifier assigned to the commit comment by GitHub's GraphQL API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="path" type="string">
  Relative path of the file to comment on. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="position" type="number">
  Line index in the diff to comment on. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="reactions" type="object">
  The reactions summary for the commit comment, containing counts for each reaction type (e.g., +1, -1, laugh, hooray, confused, heart, rocket, eyes), a total count, and a URL to fetch detailed reaction information. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="confused" type="number" required>
      Number of confused reactions on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="eyes" type="number" required>
      Number of eye reactions (👀) on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="heart" type="number" required>
      The number of heart (❤️) reactions on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hooray" type="number" required>
      The number of hooray reactions on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="laugh" type="number" required>
      The number of laugh reactions on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rocket" type="number" required>
      The count of rocket reactions (🚀) on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_count" type="number" required>
      The total number of reactions of all types on the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The API URL that returns the reactions for the commit comment. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of when the commit comment was last updated. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  The API URL that points to this commit comment. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="user" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the user's avatar image. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the user who authored the commit comment, if the user has made it public. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to the user's events on GitHub, typically in the form https://api.github.com/users/&#123;username&#125;/events. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      URL to access the list of followers for this GitHub user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL (template) for fetching the list of users this user follows, as specified in the GitHub API user object. This field is always present for a user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The API URL for the user's gists, typically of the form https://api.github.com/users/&#123;username&#125;/gists&#123;/gist_id&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The Gravatar ID for the user, used to construct their avatar image URL. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to the user's GitHub profile page. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Required.
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user who created the commit comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the user associated with the commit comment. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The node_id is the unique identifier for the user in GitHub's GraphQL API. This field is always present. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The URL to fetch the list of organizations the user belongs to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL for the user's received events, which lists the events that the user has received. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the user's public repositories on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Whether the user is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL to list the repositories starred by the user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The API URL for the user's subscriptions, which lists repositories the user is watching. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub user account that authored the commit comment, such as "User" for a normal account or "Bot" for a bot account. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The GitHub API URL for the user, such as https://api.github.com/users/octocat. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
