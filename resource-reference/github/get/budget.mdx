---
title: "github_get_budget"
description: "`github_get_budget` reads back one organization- or enterprise-level spending budget -- its own amount..."
---

`github_get_budget` reads back one organization- or enterprise-level spending budget -- its own amount, its own alert thresholds -- part of GitHub's real billing API for programmatically managing the full lifecycle of a budget rather than only viewing usage after the fact.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-github/sdk/go/github/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own budget"})

    		ubx.Resource(get.Budget, "example", get.BudgetConfig{
    			BudgetAlerting:      map[string]any{"alert_recipients": []string{"example"}, "will_alert": true},
    			BudgetAmount:        1,
    			BudgetId:            "example",
    			BudgetScope:         "example",
    			BudgetType:          "example",
    			Enterprise:          "example",
    			PreventFurtherUsage: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Budget } from "./local-sdk/github/sdk/typescript/github/get/budget.ts";

    export default stack("example", () => {
      intent({ summary: "example own budget" });

      resource(Budget, "example", {
        budgetAlerting: { alertRecipients: ["example"], willAlert: true },
        budgetAmount: 1,
        budgetId: "example",
        budgetScope: "example",
        budgetType: "example",
        enterprise: "example",
        preventFurtherUsage: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.get import Budget, BudgetConfig
    from ubx.github.get.budget import Budget_BudgetAlerting

    def describe():
        ubx.intent("example own budget")

        ubx.resource(Budget, "example", BudgetConfig(
            budget_alerting=Budget_BudgetAlerting(alert_recipients=["example"], will_alert=True),
            budget_amount=1,
            budget_id="example",
            budget_scope="example",
            budget_type="example",
            enterprise="example",
            prevent_further_usage=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_get_budget, with budget alerting a set value,
    budget amount 1, budget id example, budget scope example, budget
    type example, enterprise example, prevent further usage true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="budget_alerting" type="object" required>
  Required.
  <Expandable title="properties">
    <ResponseField name="alert_recipients" type="list(string)" required>
      Array of user login names who will receive alerts. Rejected for user-scope as alerting is always disabled for them. Required.
    </ResponseField>
    <ResponseField name="will_alert" type="bool" required>
      Whether alerts are enabled for this budget. Rejected for user-scope as alerting is always disabled for them. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="budget_amount" type="number" required>
  The budget amount in whole dollars. For license-based products, this represents the number of licenses. Required.
</ResponseField>

<ResponseField name="budget_entity_name" type="string">
  The name of the entity to apply the budget to Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="budget_product_sku" type="string">
  A single product or SKU that will be covered in the budget Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_scope" type="string" required>
  The scope of the budget. - `enterprise`: Apply the budget to the entire enterprise. - `organization`: Apply the budget to a specific organization in the enterprise. - `repository`: Apply the budget to a specific repository. - `cost_center`: Apply the budget to a specific cost center. - `multi_user_customer`: Apply a universal budget to all users in the enterprise. - `multi_user_cost_center`: Apply a universal budget to all users in a cost center. - `user`: Apply the budget to a single user. `user`, `multi_user_customer`, and `multi_user_cost_center` scopes are only supported when `budget_product_sku` is `ai_credits` or `premium_requests`. Required.
</ResponseField>

<ResponseField name="budget_type" type="string" required>
  The type of pricing model used by the budget. Determines how `budget_product_sku` is interpreted. - `BundlePricing`: Covers all AI credit SKUs. Set `budget_product_sku` to `ai_credits`. - `ProductPricing`: Covers all SKUs that belong to a product. Set `budget_product_sku` to a product such as `actions` or `packages`. - `SkuPricing`: Covers a single, specific SKU. Set `budget_product_sku` to a SKU such as `actions_linux`. Required.
</ResponseField>

<ResponseField name="enterprise" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="prevent_further_usage" type="bool" required>
  Whether to prevent additional spending once the budget is exceeded. For `user` and `multi_user_customer` scopes, this must be `true`. Required.
</ResponseField>

<ResponseField name="user" type="string">
  The username of the user for `user` scope budgets. This field is required when `budget_scope` is `user`. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="budget_entity_name" type="string">
  The name of the entity to apply the budget to Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_product_sku" type="string">
  A single product or SKU that will be covered in the budget Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="id" type="string">
  ID of the budget. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="user" type="string">
  The username of the user for `user` scope budgets. This field is required when `budget_scope` is `user`. Optional; if omitted, computed by GitHub.
</ResponseField>
