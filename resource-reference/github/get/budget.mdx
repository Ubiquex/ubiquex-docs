---
title: "github_get_budget"
description: "`github_get_budget` reads back one organization- or enterprise-level spending budget -- its own amount..."
---

`github_get_budget` reads back one organization- or enterprise-level spending budget -- its own amount, its own alert thresholds -- part of GitHub's real billing API for programmatically managing the full lifecycle of a budget rather than only viewing usage after the fact.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-github/sdk/go/github/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own budget"})

    		ubx.Resource(get.Budget, "example", get.BudgetConfig{
    			BudgetAlerting:      map[string]any{"alert_recipients": []string{"example"}},
    			BudgetAmount:        1,
    			BudgetId:            "example",
    			BudgetScope:         "example",
    			BudgetType:          "example",
    			Enterprise:          "example",
    			PreventFurtherUsage: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Budget } from "@ubx/sdk-github/github/get/budget";

    export default stack("example", () => {
      intent({ summary: "example own budget" });

      resource(Budget, "example", {
        budgetAlerting: { alertRecipients: ["example"] },
        budgetAmount: 1,
        budgetId: "example",
        budgetScope: "example",
        budgetType: "example",
        enterprise: "example",
        preventFurtherUsage: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.github.get import Budget, BudgetConfig

    def describe():
        ubx.intent("example own budget")

        ubx.resource(Budget, "example", BudgetConfig(
            budget_alerting={"alert_recipients": ["example"]},
            budget_amount=1,
            budget_id="example",
            budget_scope="example",
            budget_type="example",
            enterprise="example",
            prevent_further_usage=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_get_budget, with budget alerting a set value,
    budget amount 1, budget id example, budget scope example, budget
    type example, enterprise example, prevent further usage true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="budget_alerting" type="object">
  Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="alert_recipients" type="list(string)">
      Array of user login names who will receive alerts. Rejected for user-scope as alerting is always disabled for them. Optional.
    </ResponseField>
    <ResponseField name="will_alert" type="bool">
      Whether alerts are enabled for this budget. Rejected for user-scope as alerting is always disabled for them. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="budget_amount" type="number">
  The budget amount in whole dollars. For license-based products, this represents the number of licenses. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_entity_name" type="string">
  The name of the entity to apply the budget to Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="budget_product_sku" type="string">
  A single product or SKU that will be covered in the budget Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_scope" type="string">
  The scope of the budget for this organization. - `organization`: Apply the budget to the organization. - `repository`: Apply the budget to a specific repository in the organization. - `multi_user_customer`: Apply a universal budget to all users in the organization. - `user`: Apply the budget to a single user in the organization. `user` and `multi_user_customer` scopes are only supported when `budget_product_sku` is `ai_credits` or `premium_requests`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_type" type="string">
  The type of pricing model used by the budget. Determines how `budget_product_sku` is interpreted. - `BundlePricing`: Covers all AI credit SKUs. Set `budget_product_sku` to `ai_credits`. - `ProductPricing`: Covers all SKUs that belong to a product. Set `budget_product_sku` to a product such as `actions` or `packages`. - `SkuPricing`: Covers a single, specific SKU. Set `budget_product_sku` to a SKU such as `actions_linux`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="prevent_further_usage" type="bool">
  Whether to prevent additional spending once the budget is exceeded. For `user` and `multi_user_customer` scopes, this must be `true`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="user" type="string">
  The username of the user for `user` scope budgets. This field is required when `budget_scope` is `user`. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="budget_alerting" type="object">
  Optional; if omitted, computed by GitHub.
  <Expandable title="properties">
    <ResponseField name="alert_recipients" type="list(string)">
      Array of user login names who will receive alerts. Rejected for user-scope as alerting is always disabled for them. Optional.
    </ResponseField>
    <ResponseField name="will_alert" type="bool">
      Whether alerts are enabled for this budget. Rejected for user-scope as alerting is always disabled for them. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="budget_amount" type="number">
  The budget amount in whole dollars. For license-based products, this represents the number of licenses. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_entity_name" type="string">
  The name of the entity to apply the budget to Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_product_sku" type="string">
  A single product or SKU that will be covered in the budget Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_scope" type="string">
  The scope of the budget for this organization. - `organization`: Apply the budget to the organization. - `repository`: Apply the budget to a specific repository in the organization. - `multi_user_customer`: Apply a universal budget to all users in the organization. - `user`: Apply the budget to a single user in the organization. `user` and `multi_user_customer` scopes are only supported when `budget_product_sku` is `ai_credits` or `premium_requests`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="budget_type" type="string">
  The type of pricing model used by the budget. Determines how `budget_product_sku` is interpreted. - `BundlePricing`: Covers all AI credit SKUs. Set `budget_product_sku` to `ai_credits`. - `ProductPricing`: Covers all SKUs that belong to a product. Set `budget_product_sku` to a product such as `actions` or `packages`. - `SkuPricing`: Covers a single, specific SKU. Set `budget_product_sku` to a SKU such as `actions_linux`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="id" type="string">
  ID of the budget. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="prevent_further_usage" type="bool">
  Whether to prevent additional spending once the budget is exceeded. For `user` and `multi_user_customer` scopes, this must be `true`. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="user" type="string">
  The username of the user for `user` scope budgets. This field is required when `budget_scope` is `user`. Optional; if omitted, computed by GitHub.
</ResponseField>
