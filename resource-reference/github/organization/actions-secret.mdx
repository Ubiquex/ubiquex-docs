---
title: "github_organization_actions_secret"
description: "`github_organization_actions_secret` creates or updates an encrypted secret available to GitHub Actions workflows across repositories within..."
---

`github_organization_actions_secret` creates or updates an encrypted secret available to GitHub Actions workflows across repositories within an organization, scoped to a caller-chosen selection of them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-github/sdk/go/github/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own actions secret"})

    		ubx.Resource(organization.ActionsSecret, "example", organization.ActionsSecretConfig{
    			EncryptedValue:        "example",
    			KeyId:                 "example",
    			Org:                   "example",
    			SecretName:            "example-secret-name",
    			Visibility:            "example",
    			SelectedRepositoryIds: []any{1},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ActionsSecret } from "./local-sdk/github/sdk/typescript/github/organization/actions_secret.ts";

    export default stack("example", () => {
      intent({ summary: "example own actions secret" });

      resource(ActionsSecret, "example", {
        encryptedValue: "example",
        keyId: "example",
        org: "example",
        secretName: "example-secret-name",
        visibility: "example",
        selectedRepositoryIds: [1],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.organization import ActionsSecret, ActionsSecretConfig

    def describe():
        ubx.intent("example own actions secret")

        ubx.resource(ActionsSecret, "example", ActionsSecretConfig(
            encrypted_value="example",
            key_id="example",
            org="example",
            secret_name="example-secret-name",
            visibility="example",
            selected_repository_ids=[1],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_organization_actions_secret, with encrypted value
    example, key id example, org example, secret name
    example-secret-name, visibility example, selected repository ids
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="encrypted_value" type="string" required>
  Value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages) using the public key retrieved from the [Get an organization public key](https://docs.github.com/enterprise-cloud@latest/rest/actions/secrets#get-an-organization-public-key) endpoint. Required.
</ResponseField>

<ResponseField name="key_id" type="string" required>
  ID of the key you used to encrypt the secret. Required.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="secret_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="selected_repository_ids" type="list(number)">
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can manage the list of selected repositories using the [List selected repositories for an organization secret](https://docs.github.com/enterprise-cloud@latest/rest/actions/secrets#list-selected-repositories-for-an-organization-secret), [Set selected repositories for an organization secret](https://docs.github.com/enterprise-cloud@latest/rest/actions/secrets#set-selected-repositories-for-an-organization-secret), and [Remove selected repository from an organization secret](https://docs.github.com/enterprise-cloud@latest/rest/actions/secrets#remove-selected-repository-from-an-organization-secret) endpoints. Optional.
</ResponseField>

<ResponseField name="visibility" type="string" required>
  Which type of organization repositories have access to the organization secret. `selected` means only the repositories specified by `selected_repository_ids` can access the secret. Required.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The date and time at which the variable was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The name of the secret. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="selected_repositories_url" type="string">
  API URL listing the repositories this organization-level Actions variable is selectively exposed to. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time at which the variable was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
