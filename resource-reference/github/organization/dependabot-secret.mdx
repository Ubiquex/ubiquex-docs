---
title: "github_organization_dependabot_secret"
description: "`github_organization_dependabot_secret` creates or updates an encrypted secret available to Dependabot version updates across repositories within..."
---

`github_organization_dependabot_secret` creates or updates an encrypted secret available to Dependabot version updates across repositories within an organization, scoped to a caller-chosen selection of them.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-github/sdk/go/github/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own dependabot secret"})

    		ubx.Resource(organization.DependabotSecret, "example", organization.DependabotSecretConfig{
    			Org:                   "example",
    			SecretName:            "example-secret-name",
    			Visibility:            "example",
    			EncryptedValue:        "example",
    			KeyId:                 "example",
    			SelectedRepositoryIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { DependabotSecret } from "./local-sdk/github/sdk/typescript/github/organization/dependabot_secret.ts";

    export default stack("example", () => {
      intent({ summary: "example own dependabot secret" });

      resource(DependabotSecret, "example", {
        org: "example",
        secretName: "example-secret-name",
        visibility: "example",
        encryptedValue: "example",
        keyId: "example",
        selectedRepositoryIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.organization import DependabotSecret, DependabotSecretConfig

    def describe():
        ubx.intent("example own dependabot secret")

        ubx.resource(DependabotSecret, "example", DependabotSecretConfig(
            org="example",
            secret_name="example-secret-name",
            visibility="example",
            encrypted_value="example",
            key_id="example",
            selected_repository_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_organization_dependabot_secret, with org example,
    secret name example-secret-name, visibility example, encrypted
    value example, key id example, selected repository ids a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="encrypted_value" type="string">
  Value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages) using the public key retrieved from the [Get an organization public key](https://docs.github.com/enterprise-cloud@latest/rest/dependabot/secrets#get-an-organization-public-key) endpoint. Optional.
</ResponseField>

<ResponseField name="key_id" type="string">
  ID of the key you used to encrypt the secret. Optional.
</ResponseField>

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="secret_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="selected_repository_ids" type="list(any)">
  An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can manage the list of selected repositories using the [List selected repositories for an organization secret](https://docs.github.com/enterprise-cloud@latest/rest/dependabot/secrets#list-selected-repositories-for-an-organization-secret), [Set selected repositories for an organization secret](https://docs.github.com/enterprise-cloud@latest/rest/dependabot/secrets#set-selected-repositories-for-an-organization-secret), and [Remove selected repository from an organization secret](https://docs.github.com/enterprise-cloud@latest/rest/dependabot/secrets#remove-selected-repository-from-an-organization-secret) endpoints. Optional.
</ResponseField>

<ResponseField name="visibility" type="string" required>
  Which type of organization repositories have access to the organization secret. `selected` means only the repositories specified by `selected_repository_ids` can access the secret. Required.
</ResponseField>

## Output properties

<ResponseField name="created_at" type="string">
  The date and time at which the variable was created, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Computed by GitHub after `ship`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="name" type="string">
  The name of the secret. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="selected_repositories_url" type="string">
  API URL listing the repositories this organization-level Actions variable is selectively exposed to. Computed by GitHub after `ship`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="updated_at" type="string">
  The date and time at which the variable was last updated, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ. Computed by GitHub after `ship`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
