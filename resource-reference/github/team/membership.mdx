---
title: "github_team_membership"
description: "`github_team_membership` records one real person's own place inside a team -- member or maintainer -- which is what actually determines whether they..."
---

`github_team_membership` records one real person's own place inside a team -- member or maintainer -- which is what actually determines whether they inherit that team's own repository permissions and get included in its notifications and review requests.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-github/sdk/go/github/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own membership"})

    		ubx.Resource(team.Membership, "example", team.MembershipConfig{
    			Org:      "example",
    			TeamId:   "example",
    			TeamSlug: "example",
    			Username: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Membership } from "./local-sdk/github/sdk/typescript/github/team/membership.ts";

    export default stack("example", () => {
      intent({ summary: "example own membership" });

      resource(Membership, "example", {
        org: "example",
        teamId: "example",
        teamSlug: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.team import Membership, MembershipConfig

    def describe():
        ubx.intent("example own membership")

        ubx.resource(Membership, "example", MembershipConfig(
            org="example",
            team_id="example",
            team_slug="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_team_membership, with org example, team id example,
    team slug example, username example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="org" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="role" type="string">
  The role that this user should have in the team. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="team_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="team_slug" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="username" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="role" type="string">
  The role that this user should have in the team. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="state" type="string">
  The state of the user's membership in the team. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="url" type="string">
  The API URL for the team membership. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
