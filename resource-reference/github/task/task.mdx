---
title: "github_task"
description: "`github_task` is one real repository's own result within a broader Multi-Repository Variant Analysis run -- its analysis status..."
---

`github_task` is one real repository's own result within a broader Multi-Repository Variant Analysis run -- its analysis status, its result count once finished, and the reason it failed if it did -- the per-repository record a variant analysis run is actually made up of.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	task "github.com/ubiquex/ubx-sdk-github/sdk/go/github/task"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own task"})

    		ubx.Resource(task.Task, "ci-runner", task.TaskConfig{
    			Owner:             "example",
    			Prompt:            "example",
    			Repo:              "example",
    			TaskId:            "example",
    			BaseRef:           "example",
    			CreatePullRequest: true,
    			CustomAgent:       "example",
    			HeadRef:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Task } from "./local-sdk/github/sdk/typescript/github/task/task.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own task" });

      resource(Task, "ci-runner", {
        owner: "example",
        prompt: "example",
        repo: "example",
        taskId: "example",
        baseRef: "example",
        createPullRequest: true,
        customAgent: "example",
        headRef: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.task import Task, TaskConfig

    def describe():
        ubx.intent("payments own task")

        ubx.resource(Task, "ci-runner", TaskConfig(
            owner="example",
            prompt="example",
            repo="example",
            task_id="example",
            base_ref="example",
            create_pull_request=True,
            custom_agent="example",
            head_ref="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_task, with owner example, prompt example, repo
    example, task id example, base ref example, create pull request
    true, custom agent example, head ref example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="base_ref" type="string">
  Base ref for new branch/PR Optional.
</ResponseField>

<ResponseField name="create_pull_request" type="bool">
  Whether to create a PR. Optional.
</ResponseField>

<ResponseField name="custom_agent" type="string">
  Optional identifier for a custom agent to use for this task. Use the custom agent's filename without the extension - for example, for a `.github/agents/performance-optimizer.agent.md` custom agent, use `performance-optimizer`. Optional.
</ResponseField>

<ResponseField name="head_ref" type="string">
  Head ref for existing branch/PR. If provided with `base_ref`, the agent looks up open PR context for `head_ref` targeting `base_ref` and commits to `head_ref` instead of creating a new branch. Optional.
</ResponseField>

<ResponseField name="model" type="string">
  The model to use for this task. The allowed models may change over time and depend on the user's GitHub Copilot plan and organization policies. Currently supported values: `claude-sonnet-4.6`, `claude-opus-4.6`, `gpt-5.2-codex`, `gpt-5.3-codex`, `gpt-5.4`, `claude-sonnet-4.5`, `claude-opus-4.5` Optional.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="prompt" type="string" required>
  The user's prompt for the agent Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="task_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
