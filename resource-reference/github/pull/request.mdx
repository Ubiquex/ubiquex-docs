---
title: "github_pull_request"
description: "A complete, runnable github_pull_request program, in every SDK language."
---

`github_pull_request` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	pull "github.com/ubiquex/ubx-sdk-github/sdk/go/github/pull"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own request"})

    		ubx.Resource(pull.Request, "ci-runner", pull.RequestConfig{
    			Base:       "example",
    			Head:       "example",
    			Owner:      "example",
    			PullNumber: "example",
    			Repo:       "example",
    			HeadRepo:   "example",
    			Issue:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Request } from "./local-sdk/github/sdk/typescript/github/pull/request.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own request" });

      resource(Request, "ci-runner", {
        base: "example",
        head: "example",
        owner: "example",
        pullNumber: "example",
        repo: "example",
        headRepo: "example",
        issue: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.pull import Request, RequestConfig

    def describe():
        ubx.intent("payments own request")

        ubx.resource(Request, "ci-runner", RequestConfig(
            base="example",
            head="example",
            owner="example",
            pull_number="example",
            repo="example",
            head_repo="example",
            issue=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_pull_request, with base example, head example,
    owner example, pull number example, repo example, head repo
    example, issue 1.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="base" type="string" required>
  The name of the branch you want the changes pulled into. This should be an existing branch on the current repository. You cannot submit a pull request to one repository that requests a merge to a base of another repository. Required.
</ResponseField>

<ResponseField name="body" type="string">
  The contents of the pull request. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="draft" type="bool">
  Indicates whether the pull request is a draft. See "[Draft Pull Requests](https://docs.github.com/articles/about-pull-requests#draft-pull-requests)" in the GitHub Help documentation to learn more. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="head" type="string" required>
  The name of the branch where your changes are implemented. For cross-repository pull requests in the same network, namespace `head` with a user like this: `username:branch`. Required.
</ResponseField>

<ResponseField name="head_repo" type="string">
  The name of the repository where the changes in the pull request were made. This field is required for cross-repository pull requests if both repositories are owned by the same organization. Optional.
</ResponseField>

<ResponseField name="issue" type="number">
  An issue in the repository to convert to a pull request. The issue title, body, and comments will become the title, body, and comments on the new pull request. Required unless `title` is specified. Optional.
</ResponseField>

<ResponseField name="maintainer_can_modify" type="bool">
  Indicates whether [maintainers can modify](https://docs.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="pull_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="title" type="string">
  The title of the new pull request. Required unless `issue` is specified. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="_links" type="object">
  The hypermedia links associated with the pull request, including URLs for related resources such as the pull request itself, its HTML page, comments, and status checks. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="comments" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to access the comments for this pull request via the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="commits" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to the pull request's commits list. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="html" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to the HTML page for the pull request on GitHub. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="issue" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL of the issue associated with this pull request, as specified in the GitHub API link object. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="review_comment" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to the review comment resource. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="review_comments" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to the review comments for this pull request. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="self" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL of the pull request, used as the self link in the hypermedia response. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="statuses" type="object" required>
      Hypermedia Link Required.
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL of the statuses endpoint for this pull request. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="active_lock_reason" type="string">
  The reason a pull request is currently locked, if any. Possible values include 'off-topic', 'too heated', 'resolved', and 'spam'. This field is null when the pull request is not locked. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="additions" type="number">
  The total number of lines added in the pull request's diff, as computed by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="assignee" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the assignee's avatar image on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the user assigned to the pull request, if publicly visible. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to the events of the user assigned to the pull request, following the GitHub API user object format. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The URL to the GitHub API endpoint that returns the list of followers for the assignee. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The API URL to list the users this user is following. This is a template URL that includes an optional `&#123;/other_user&#125;` placeholder to query a specific user's following relationship. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL to the assignee's list of gists on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The Gravatar ID of the assignee, used to fetch the user's avatar image from Gravatar. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The HTML URL to the GitHub profile of the assignee. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The numeric identifier of the GitHub user assigned to the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user assigned to the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the user assigned to the pull request. This field is optional and may be null if the user has not set a name. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The global node ID of the user assigned to this pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The API URL that lists the organizations to which the user (assignee) belongs. This is a standard field on GitHub user objects. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL of the GitHub API endpoint that lists events received by the assignee user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to list the repositories owned by the assignee user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Indicates whether the assignee is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      URL template for listing the assignee's starred repositories, with placeholders for &#123;owner&#125; and &#123;repo&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The API URL to list repositories that the assignee is subscribed to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the assignee's GitHub account, such as 'User' or 'Bot'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The GitHub API URL for the user assigned to the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Specifies the user's view type, indicating whether the user is a public or private user. Possible values: 'public', 'private'. Optional. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="assignees" type="list(object)">
  A list of GitHub user objects representing the users assigned to the pull request. Each object contains standard user attributes such as login, id, and node_id. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string">
      The URL of the assignee's avatar image. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the GitHub user assigned to this pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string">
      The URL to the events for this GitHub user, who is assigned to the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string">
      The API endpoint URL to retrieve the list of followers for this GitHub user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string">
      The URL template for the GitHub API endpoint that lists the users this assignee follows. Typically in the form `https://api.github.com/users/&#123;username&#125;/following`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string">
      The URL to the assignee user's gists, typically a template containing an optional gist ID placeholder (e.g., https://api.github.com/users/&#123;user&#125;/gists&#123;/gist_id&#125;). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string">
      The user's Gravatar ID, a hash used to construct the user's avatar image URL. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string">
      The HTML URL to the user's GitHub profile page. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number">
      The unique numeric identifier for the GitHub user assigned to the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string">
      The GitHub username of the user assigned to the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The full name of the GitHub user assigned to this pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string">
      The GraphQL node identifier for the assignee (a GitHub user). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string">
      The URL to the list of organizations to which the assignee user belongs. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string">
      The URL to retrieve the events received by the assignee, as provided by the GitHub API for user objects. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string">
      URL to the API endpoint that lists the repositories owned by the assignee. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool">
      Indicates whether the assignee is a GitHub site administrator (true) or not (false). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
    <ResponseField name="starred_url" type="string">
      The URL template for listing the GitHub repositories starred by the assignee user, as defined in the GitHub API user object. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string">
      The URL to the assignee's subscriptions. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of GitHub account assigned to the pull request, such as 'User' or 'Bot'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The GitHub API URL for the user assigned to the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="author_association" type="string">
  How the author is associated with the repository. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="auto_merge" type="object">
  The status of auto merging a pull request. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="commit_message" type="string" required>
      Commit message for the merge commit. Required.
    </ResponseField>
    <ResponseField name="commit_title" type="string" required>
      Title for the merge commit message. Required.
    </ResponseField>
    <ResponseField name="enabled_by" type="object" required>
      A GitHub user. Required.
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string" required>
          The URL of the avatar image for the user who enabled auto-merge on the pull request. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          Email address of the user who enabled auto-merge. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events_url" type="string" required>
          The URL to the user's events, as defined by the GitHub API. For example: https://api.github.com/users/&#123;username&#125;/events&#123;/privacy&#125;. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string" required>
          The URL to retrieve the list of followers of the user via the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string" required>
          The URL template to list the users this user follows, with an optional placeholder for another user's username. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string" required>
          The gists API URL for the user who enabled auto-merge, typically formatted as https://api.github.com/users/&#123;username&#125;/gists&#123;/gist_id&#125;. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string" required>
          The unique identifier for the user's Gravatar image, used to construct the user's avatar URL. In this context, it identifies the user who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string" required>
          The HTML URL to the GitHub profile of the user who enabled auto-merge on the pull request. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The numeric identifier of the user who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="login" type="string" required>
          The GitHub username of the user who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the user who enabled auto-merge for the pull request. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The unique identifier for the user who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string" required>
          The URL to the list of organizations that the user belongs to, as provided by the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string" required>
          The URL to the received events endpoint for the user who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string" required>
          The API URL that provides access to the list of repositories owned by the user. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool" required>
          Whether the user is a site administrator of GitHub. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="starred_url" type="string" required>
          The URL template to list repositories starred by the user. This field is part of the user object for the person who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string" required>
          The API URL that returns the list of repositories or threads the user (who enabled auto-merge) is subscribed to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of GitHub account that enabled auto-merge for the pull request. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The GitHub API URL of the user who enabled auto-merge. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          The type of user view for the user who enabled auto-merge. Possible values are 'public' or 'private'. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="merge_method" type="string" required>
      The merge method to use. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="body" type="string">
  The contents of the pull request. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="changed_files" type="number">
  The number of files that have been changed in this pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="closed_at" type="string">
  The timestamp of when the pull request was closed, or null if the pull request is still open. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="comments" type="number">
  Total number of comments on the pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="comments_url" type="string">
  The URL to retrieve comments for this pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="commits" type="number">
  The number of commits contained in the pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="commits_url" type="string">
  The URL to retrieve the list of commits made in the pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp at which the pull request was created, automatically set by the GitHub API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="deletions" type="number">
  The number of lines deleted in the pull request's diff. This is computed by GitHub and reflects the total deletions across all changed files. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="diff_url" type="string">
  The URL to the diff file for this pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="draft" type="bool">
  Indicates whether the pull request is a draft. See "[Draft Pull Requests](https://docs.github.com/articles/about-pull-requests#draft-pull-requests)" in the GitHub Help documentation to learn more. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="html_url" type="string">
  The URL to the pull request's page on GitHub, typically used to open the pull request in a web browser. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="id" type="number">
  The unique numeric identifier assigned to the pull request by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="issue_url" type="string">
  The GitHub API URL for the issue associated with this pull request. Since pull requests are issues, this URL points to the pull request's issue endpoint. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="labels" type="list(object)">
  Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="color" type="string">
      The label's color, represented as a six-character hex code (e.g., 'ffffff') without the leading '#'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="default" type="bool">
      Indicates whether the label is a default label assigned by GitHub (e.g., bug, enhancement) rather than a custom label created by the repository owner. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="description" type="string">
      A brief description of the label, explaining its meaning or purpose. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number">
      The unique numeric identifier of the label attached to the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the label applied to the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string">
      The GraphQL global node ID for the label. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The API URL for this label. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="locked" type="bool">
  Indicates whether the pull request conversation is locked, restricting commenting to users with write access. This is a read-only value set by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="maintainer_can_modify" type="bool">
  Indicates whether [maintainers can modify](https://docs.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="merge_commit_sha" type="string">
  The SHA of the merge commit created when the pull request is merged. This value is computed by GitHub and is only populated after a successful merge. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="mergeable" type="bool">
  Whether the pull request is mergeable, based on absence of merge conflicts and passing of required status checks. This is computed and reflects the current state of the pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="mergeable_state" type="string">
  The mergeability state of the pull request, such as clean, unstable, blocked, draft, or unknown. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="merged" type="bool">
  Indicates whether the pull request has been merged (true) or not (false). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="merged_at" type="string">
  The timestamp (in RFC3339 format) of when the pull request was merged, or null if it has not been merged. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="merged_by" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the avatar image for the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the user who merged the pull request, if publicly available. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to retrieve events for this user, typically in the form https://api.github.com/users/&#123;username&#125;/events&#123;/privacy&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      URL to the followers list for the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL template to retrieve the list of users this GitHub user is following, with an optional placeholder to check a specific user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The API URL to the list of gists for the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The Gravatar ID (hash) for the user's avatar, typically derived from their email address, used to fetch the profile image from Gravatar. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to the GitHub profile page of the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The unique numeric identifier of the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the user who merged this pull request. This field is optional and may be empty if the user has not provided a name. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL node ID of the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The URL to the GitHub API endpoint listing the organizations to which the user belongs. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the received events for the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the user's repositories. This is the API endpoint that returns a list of repositories for the merged_by user, as defined in GitHub's user object. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Whether the user is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL template for the user's starred repositories. It is used to construct the API endpoint to list the repositories starred by this user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      URL to the GitHub API endpoint listing repositories that this user is subscribed to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub account that merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The URL for the user who merged the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="milestone" type="object">
  A collection of related issues and pull requests. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="closed_at" type="string" required>
      The timestamp of when the milestone was closed, in ISO 8601 format. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="closed_issues" type="number" required>
      The number of issues associated with this milestone that are currently closed. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      The timestamp of when the milestone was created, in ISO 8601 format (e.g., YYYY-MM-DDTHH:MM:SSZ). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="creator" type="object" required>
      A GitHub user. Required.
      <Expandable title="properties">
        <ResponseField name="avatar_url" type="string" required>
          The URL of the avatar image for the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="email" type="string">
          The public email address of the user who created the milestone, if available. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="events_url" type="string" required>
          The URL to retrieve the user's public events, optionally filtered by privacy (e.g., /users/&#123;user&#125;/events&#123;/privacy&#125;). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="followers_url" type="string" required>
          The URL to the GitHub API endpoint listing the followers of the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="following_url" type="string" required>
          A URL template to access the list of users this user follows, with an optional placeholder for another user (e.g., /users/&#123;username&#125;/following&#123;/other_user&#125;). Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gists_url" type="string" required>
          The API URL to the user's list of gists. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="gravatar_id" type="string" required>
          The Gravatar ID (hash) of the user who created the milestone. This ID is derived from the user's email address and can be used to construct the user's Gravatar avatar URL. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="html_url" type="string" required>
          The URL to the GitHub profile page of the milestone creator. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The numeric identifier of the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="login" type="string" required>
          The GitHub username of the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="name" type="string">
          The display name of the user who created the milestone, if provided. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="node_id" type="string" required>
          The GraphQL node ID of the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="organizations_url" type="string" required>
          The URL to the user's organizations endpoint, which lists the organizations the user belongs to. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="received_events_url" type="string" required>
          The URL to the received events for the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="repos_url" type="string" required>
          URL to the endpoint listing the user's repositories, as defined by the GitHub API. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="site_admin" type="bool" required>
          Whether the user is a GitHub site administrator. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="starred_at" type="string">
          Optional.
        </ResponseField>
        <ResponseField name="starred_url" type="string" required>
          The URL to access the list of repositories starred by this user. It may include template placeholders like &#123;owner&#125; and &#123;repo&#125; for filtering. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="subscriptions_url" type="string" required>
          The URL to list the repositories the milestone creator is watching. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of the GitHub account that created the milestone, such as 'User', 'Bot', or 'Organization'. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="url" type="string" required>
          The GitHub API URL for the user who created the milestone. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="user_view_type" type="string">
          The type of GitHub user account, indicating whether the account is public or private. Possible values are 'public' and 'private'. Optional. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="description" type="string" required>
      The description of the milestone associated with the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="due_on" type="string" required>
      The due date of the milestone, in ISO 8601 format (e.g., YYYY-MM-DDTHH:MM:SSZ). This field is required, so it always has a value. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to view the milestone on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Required.
    </ResponseField>
    <ResponseField name="labels_url" type="string" required>
      The URL for the labels endpoint of this milestone, which lists the labels associated with it. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The node ID of the milestone in the GitHub GraphQL API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="number" type="number" required>
      The number of the milestone. Required.
    </ResponseField>
    <ResponseField name="open_issues" type="number" required>
      The number of open issues associated with this milestone. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="state" type="string" required>
      The state of the milestone. Required.
    </ResponseField>
    <ResponseField name="title" type="string" required>
      The title of the milestone. Required.
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      The timestamp of the last modification to the milestone associated with the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The API URL for the milestone. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="node_id" type="string">
  The GraphQL node ID for the pull request, used to identify the resource in the GitHub GraphQL API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="number" type="number">
  Number uniquely identifying the pull request within its repository. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="patch_url" type="string">
  The URL to the patch file for this pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="rebaseable" type="bool">
  Whether the pull request can be rebased. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="requested_reviewers" type="list(object)">
  List of users and/or teams that have been requested to review the pull request. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string">
      The URL of the avatar image for the requested reviewer. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
    <ResponseField name="events_url" type="string">
      The URL to the events list for the user who is a requested reviewer on the pull request. In the GitHub API, this is typically `https://api.github.com/users/&#123;username&#125;/events`. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string">
      The URL to retrieve the list of followers of the requested reviewer user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string">
      The API URL to the list of users that this user follows, with a template placeholder for another user (e.g., https://api.github.com/users/&#123;username&#125;/following&#123;/other_user&#125;). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string">
      The API URL to the gists of the requested reviewer user. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string">
      The unique identifier for the user's Gravatar, used to construct their avatar image URL. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string">
      The GitHub profile URL for the requested reviewer. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
    <ResponseField name="login" type="string">
      The GitHub username of the requested reviewer. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the team requested as a reviewer. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string">
      The unique identifier for the requested reviewer (user or team) in GitHub's GraphQL API. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string">
      The URL to access the list of organizations that the requested reviewer belongs to. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string">
      The URL to the received events for the user (e.g., https://api.github.com/users/&#123;username&#125;/received_events). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string">
      The URL to the GitHub user's repositories. This is the standard user object field indicating the API endpoint for the user's public repositories. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool">
      Indicates whether the requested reviewer is a GitHub site administrator. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
    <ResponseField name="starred_url" type="string">
      The URL template to list repositories starred by the user, with placeholders for owner and repo. Typically in the form https://api.github.com/user/starred&#123;/owner&#125;&#123;/repo&#125;. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string">
      The API URL to the user's subscriptions endpoint, which lists the repositories the user is subscribed to. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the requested reviewer, which can be either 'User' or 'Team'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The API URL for the requested reviewer (user or team) on the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="requested_teams" type="list(object)">
  Teams that have been requested to review this pull request. Each item contains team details such as id, name, slug, and permissions. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="description" type="string">
      The description of the team that is requested for review on the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="enterprise_id" type="number">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
    <ResponseField name="html_url" type="string">
      The URL to the team's page on GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number">
      The unique identifier assigned to the team in GitHub. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="ldap_dn" type="string">
      The LDAP distinguished name (DN) of the team that has been requested to review the pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="members_url" type="string">
      URL to retrieve the members of this team, with a placeholder for a specific member. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the team that is requested for review on this pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string">
      The GraphQL node ID of the team. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="notification_setting" type="string">
      The team's notification setting, indicating whether notifications are enabled or disabled. Possible values: 'notifications_enabled', 'notifications_disabled'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organization_id" type="number">
      The unique identifier of the organization that owns the team requested for review on this pull request. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="permission" type="string">
      The level of access the team has to the repository (e.g., 'pull', 'push', 'admin'). Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="privacy" type="string">
      The privacy level of the team. For GitHub teams, this can be either 'secret' or 'closed'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repositories_url" type="string">
      The API URL that points to the list of repositories belonging to this requested team. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="slug" type="string">
      The slug of the team, a URL-friendly identifier used in the GitHub API to reference the team. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of the requested team, either 'enterprise' or 'organization'. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string">
      The API URL for the team, used to retrieve team details from the GitHub API. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="review_comment_url" type="string">
  The URL to the review comments for this pull request, provided by the GitHub API. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="review_comments" type="number">
  The number of review comments on the pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="review_comments_url" type="string">
  The API endpoint URL for the list of review comments on this pull request. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="stack" type="object">
  The stack information associated with a pull request. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="base" type="object" required>
      The pull request's base branch section, containing details such as the branch name, commit SHA, and repository information. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="ref" type="string" required>
          The base ref of the stack this pull request belongs to. Required.
        </ResponseField>
        <ResponseField name="sha" type="string" required>
          The base SHA of the stack this pull request belongs to. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="number">
      The ID of the stack that this pull request belongs to. Optional.
    </ResponseField>
    <ResponseField name="number" type="number">
      The number of the stack that this pull request belongs to. Optional.
    </ResponseField>
    <ResponseField name="position" type="number">
      The one-based position of this pull request within the stack, where 1 is the bottom of the stack. Optional.
    </ResponseField>
    <ResponseField name="size" type="number">
      The total number of pull requests in the stack. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="state" type="string">
  State of this Pull Request. Either `open` or `closed`. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="statuses_url" type="string">
  The API URL to query the commit statuses associated with the pull request's head commit. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="title" type="string">
  The title of the new pull request. Required unless `issue` is specified. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of the last update to the pull request, in ISO 8601 format (e.g., 2025-04-01T12:34:56Z). Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  The API URL of the pull request, e.g., `https://api.github.com/repos/&#123;owner&#125;/&#123;repo&#125;/pulls/&#123;number&#125;`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="user" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the user's avatar image. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      The email address of the GitHub user, if publicly available. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to the events of the GitHub user, typically in the form https://api.github.com/users/&#123;username&#125;/events&#123;/privacy&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The API endpoint URL to retrieve the list of followers of this user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL endpoint that retrieves the list of users that this user is following. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL template for accessing the user's gists, typically in the format https://api.github.com/users/&#123;username&#125;/gists&#123;/gist_id&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The user's Gravatar ID, used to construct the avatar URL. May be an empty string if the user has no Gravatar associated. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The URL to the user's GitHub profile page. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The unique numeric identifier for the user on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username of the user associated with the pull request. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The full name of the user as provided on their GitHub profile. This field is optional and may be null if the user has not set a name. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The node ID for the user, which is a global identifier used with GitHub's GraphQL API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      URL to the list of organizations this user is a member of, as provided by the GitHub API (typically https://api.github.com/users/&#123;username&#125;/orgs). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the GitHub API endpoint that lists events received by the user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The URL to the GitHub API endpoint listing the user's repositories. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Indicates whether the user is a GitHub site administrator. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The URL to the list of repositories starred by the user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The URL to retrieve the subscriptions of the user via the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub account, typically 'User' or 'Organization'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The API URL for the GitHub user, e.g., https://api.github.com/users/&#123;username&#125;. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
