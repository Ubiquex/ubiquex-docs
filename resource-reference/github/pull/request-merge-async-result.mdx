---
title: "github_pull_request_merge_async_result"
description: "`github_pull_request_merge_async_result` is the real, polled outcome of a pull request merge that GitHub processes asynchronously rather than..."
---

`github_pull_request_merge_async_result` is the real, polled outcome of a pull request merge that GitHub processes asynchronously rather than completing inline -- checked after the fact to confirm whether the merge actually succeeded.

Real, typed bindings generated directly from the real GitHub REST API OpenAPI specification, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	pull "github.com/ubiquex/ubx-sdk-github/sdk/go/github/pull"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own request merge async result"})

    		ubx.Resource(pull.RequestMergeAsyncResult, "ci-runner", pull.RequestMergeAsyncResultConfig{
    			Owner:         "example",
    			PullNumber:    "example",
    			Repo:          "example",
    			Uuid:          "example",
    			CommitMessage: "example",
    			CommitTitle:   "example",
    			MergeAction:   "example",
    			MergeMethod:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RequestMergeAsyncResult } from "./local-sdk/github/sdk/typescript/github/pull/request_merge_async_result.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own request merge async result" });

      resource(RequestMergeAsyncResult, "ci-runner", {
        owner: "example",
        pullNumber: "example",
        repo: "example",
        uuid: "example",
        commitMessage: "example",
        commitTitle: "example",
        mergeAction: "example",
        mergeMethod: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.pull import RequestMergeAsyncResult, RequestMergeAsyncResultConfig

    def describe():
        ubx.intent("payments own request merge async result")

        ubx.resource(RequestMergeAsyncResult, "ci-runner", RequestMergeAsyncResultConfig(
            owner="example",
            pull_number="example",
            repo="example",
            uuid="example",
            commit_message="example",
            commit_title="example",
            merge_action="example",
            merge_method="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_pull_request_merge_async_result, with owner
    example, pull number example, repo example, uuid example, commit
    message example, commit title example, merge action example,
    merge method example.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="commit_message" type="string">
  Extra detail to append to automatic commit message. Optional.
</ResponseField>

<ResponseField name="commit_title" type="string">
  Title for the automatic commit message. Optional.
</ResponseField>

<ResponseField name="merge_action" type="string">
  The action that will be taken to merge the pull request. `direct_merge` merges the pull request directly without using a merge queue; `merge_queue` adds the pull request to a merge queue; `default` selects the most appropriate option. Optional.
</ResponseField>

<ResponseField name="merge_method" type="string">
  The merge method to use. Optional.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="pull_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="sha" type="string">
  SHA that pull request head must match to allow merge. If not provided, the current head of the PR at the time of the request will be used; if the PR is pushed in between the merge being requested and being executed, the merge will be cancelled. Optional.
</ResponseField>

<ResponseField name="uuid" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="details" type="object">
  Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="expected_head_sha" type="string">
      The SHA of the head commit expected by the merge operation, used to verify the branch hasn't changed before merging. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="merge_action" type="string">
      The merge strategy or action used for the pull request merge operation, indicating how the changes are combined. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="merge_method" type="string">
      The method used to merge the pull request, as recorded in the result details of an asynchronous merge operation. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="message" type="string">
      Optional; if omitted, computed by GitHub.
    </ResponseField>
    <ResponseField name="sha" type="string">
      The SHA of the merge commit created when the pull request was merged asynchronously. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="uuid" type="string">
      A UUID string that uniquely identifies the asynchronous pull request merge operation. Optional; if omitted, computed by GitHub. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="string">
  The current state of the asynchronous merge operation for the pull request. Possible values are `pending`, `merged`, `enqueued`, and `failed`. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>
