---
title: "github_pull_request_review_comment"
description: "A complete, runnable github_pull_request_review_comment program, in every SDK language."
---

`github_pull_request_review_comment` -- real, typed bindings generated directly from
the real GitHub REST API OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only github --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-github/sdk/go => ./local-sdk/github/sdk/go
    package main

    import (
    	pull "github.com/ubiquex/ubx-sdk-github/sdk/go/github/pull"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own request review comment"})

    		ubx.Resource(pull.RequestReviewComment, "ci-runner", pull.RequestReviewCommentConfig{
    			Body:       "example",
    			CommentId:  "example",
    			CommitId:   "example",
    			Owner:      "example",
    			Path:       "/ci/",
    			PullNumber: "example",
    			Repo:       "example",
    			InReplyTo:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only github --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { RequestReviewComment } from "./local-sdk/github/sdk/typescript/github/pull/request_review_comment.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own request review comment" });

      resource(RequestReviewComment, "ci-runner", {
        body: "example",
        commentId: "example",
        commitId: "example",
        owner: "example",
        path: "/ci/",
        pullNumber: "example",
        repo: "example",
        inReplyTo: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only github --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/github/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.github.pull import RequestReviewComment, RequestReviewCommentConfig

    def describe():
        ubx.intent("payments own request review comment")

        ubx.resource(RequestReviewComment, "ci-runner", RequestReviewCommentConfig(
            body="example",
            comment_id="example",
            commit_id="example",
            owner="example",
            path="/ci/",
            pull_number="example",
            repo="example",
            in_reply_to=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create github_pull_request_review_comment, with body example,
    comment id example, commit id example, owner example, path /ci/,
    pull number example, repo example, in reply to 1.
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="body" type="string" required>
  The text of the review comment. Required.
</ResponseField>

<ResponseField name="comment_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="commit_id" type="string" required>
  The SHA of the commit needing a comment. Not using the latest commit SHA may render your comment outdated if a subsequent commit modifies the line you specify as the `position`. Required.
</ResponseField>

<ResponseField name="in_reply_to" type="number">
  The ID of the review comment to reply to. To find the ID of a review comment with ["List review comments on a pull request"](#list-review-comments-on-a-pull-request). When specified, all parameters other than `body` in the request body are ignored. Optional.
</ResponseField>

<ResponseField name="line" type="number">
  **Required unless using `subject_type:file`**. The line of the blob in the pull request diff that the comment applies to. For a multi-line comment, the last line of the range that your comment applies to. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="owner" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="path" type="string" required>
  The relative path to the file that necessitates a comment. Required.
</ResponseField>

<ResponseField name="position" type="number">
  **This parameter is closing down. Use `line` instead**. The position in the diff where you want to add a review comment. Note this value is not the same as the line number in the file. The position value equals the number of lines down from the first "@@" hunk header in the file you want to add a comment. The line just below the "@@" line is position 1, the next line is position 2, and so on. The position in the diff continues to increase through lines of whitespace and additional hunks until the beginning of a new file. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="pull_number" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="repo" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="side" type="string">
  In a split diff view, the side of the diff that the pull request's changes appear on. Can be `LEFT` or `RIGHT`. Use `LEFT` for deletions that appear in red. Use `RIGHT` for additions that appear in green or unchanged lines that appear in white and are shown for context. For a multi-line comment, side represents whether the last line of the comment range is a deletion or addition. For more information, see "[Diff view options](https://docs.github.com/articles/about-comparing-branches-in-pull-requests#diff-view-options)" in the GitHub Help documentation. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="start_line" type="number">
  **Required when using multi-line comments unless using `in_reply_to`**. The `start_line` is the first line in the pull request diff that your multi-line comment applies to. To learn more about multi-line comments, see "[Commenting on a pull request](https://docs.github.com/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="start_side" type="string">
  **Required when using multi-line comments unless using `in_reply_to`**. The `start_side` is the starting side of the diff that the comment applies to. Can be `LEFT` or `RIGHT`. To learn more about multi-line comments, see "[Commenting on a pull request](https://docs.github.com/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation. See `side` in this table for additional context. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="subject_type" type="string">
  The level at which the comment is targeted. Optional; if omitted, computed by GitHub.
</ResponseField>

## Output properties

<ResponseField name="_links" type="object">
  Hypermedia links to related resources for this pull request review comment, such as the comment's API endpoint, HTML view, and associated pull request. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="html" type="object" required>
      The HTML URL of the pull request review comment, represented as a link object with an href field. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to access the pull request review comment in the GitHub web interface. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pull_request" type="object" required>
      The link to the pull request associated with this review comment, containing the API URL (href) to the pull request. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL to the pull request that this review comment is associated with. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="self" type="object" required>
      The URL to this pull request review comment itself. Required. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="href" type="string" required>
          The URL of the pull request review comment. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="author_association" type="string">
  How the author is associated with the repository. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="body_html" type="string">
  The rendered HTML version of the comment body. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="body_text" type="string">
  The plain-text version of the pull request review comment body, as computed by GitHub. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="created_at" type="string">
  The timestamp of when the pull request review comment was created, in RFC3339 format. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="diff_hunk" type="string">
  The diff of the line that the comment refers to. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="html_url" type="string">
  HTML URL for the pull request review comment. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="id" type="number">
  The ID of the pull request review comment. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="in_reply_to_id" type="number">
  The comment ID to reply to. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="line" type="number">
  **Required unless using `subject_type:file`**. The line of the blob in the pull request diff that the comment applies to. For a multi-line comment, the last line of the range that your comment applies to. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="node_id" type="string">
  The node ID of the pull request review comment. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="original_commit_id" type="string">
  The SHA of the original commit to which the comment applies. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="original_line" type="number">
  The line of the blob to which the comment applies. The last line of the range for a multi-line comment Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="original_position" type="number">
  The index of the original line in the diff to which the comment applies. This field is closing down; use `original_line` instead. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="original_start_line" type="number">
  The first line of the range for a multi-line comment. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="position" type="number">
  **This parameter is closing down. Use `line` instead**. The position in the diff where you want to add a review comment. Note this value is not the same as the line number in the file. The position value equals the number of lines down from the first "@@" hunk header in the file you want to add a comment. The line just below the "@@" line is position 1, the next line is position 2, and so on. The position in the diff continues to increase through lines of whitespace and additional hunks until the beginning of a new file. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="pull_request_review_id" type="number">
  The ID of the pull request review to which the comment belongs. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="pull_request_url" type="string">
  URL for the pull request that the review comment belongs to. Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="reactions" type="object">
  An object containing reaction summary counts (such as +1, -1, laugh, hooray, confused, heart, rocket, eyes) and the total count for this review comment, reflecting how users have reacted to it. Computed by GitHub after `ship`. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="confused" type="number" required>
      The number of confused reactions on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="eyes" type="number" required>
      The number of eye reactions (👀) on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="heart" type="number" required>
      The count of heart (❤️) reactions on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="hooray" type="number" required>
      The number of hooray reactions on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="laugh" type="number" required>
      The number of laugh reactions (😂) on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="rocket" type="number" required>
      The number of rocket reactions (🚀) on the review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="total_count" type="number" required>
      The total number of reactions on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The API URL for the reactions resource on the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="side" type="string">
  In a split diff view, the side of the diff that the pull request's changes appear on. Can be `LEFT` or `RIGHT`. Use `LEFT` for deletions that appear in red. Use `RIGHT` for additions that appear in green or unchanged lines that appear in white and are shown for context. For a multi-line comment, side represents whether the last line of the comment range is a deletion or addition. For more information, see "[Diff view options](https://docs.github.com/articles/about-comparing-branches-in-pull-requests#diff-view-options)" in the GitHub Help documentation. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="start_line" type="number">
  **Required when using multi-line comments unless using `in_reply_to`**. The `start_line` is the first line in the pull request diff that your multi-line comment applies to. To learn more about multi-line comments, see "[Commenting on a pull request](https://docs.github.com/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="start_side" type="string">
  **Required when using multi-line comments unless using `in_reply_to`**. The `start_side` is the starting side of the diff that the comment applies to. Can be `LEFT` or `RIGHT`. To learn more about multi-line comments, see "[Commenting on a pull request](https://docs.github.com/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation. See `side` in this table for additional context. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="subject_type" type="string">
  The level at which the comment is targeted. Optional; if omitted, computed by GitHub.
</ResponseField>

<ResponseField name="updated_at" type="string">
  The timestamp of the last update to the review comment. Computed by GitHub after `ship`. **(AI-inferred)**
</ResponseField>

<ResponseField name="url" type="string">
  URL for the pull request review comment Computed by GitHub after `ship`.
</ResponseField>

<ResponseField name="user" type="object">
  A GitHub user. Computed by GitHub after `ship`.
  <Expandable title="properties">
    <ResponseField name="avatar_url" type="string" required>
      The URL of the user's avatar image on GitHub. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="email" type="string">
      Email address of the user who authored the review comment. This field is optional and may be empty if the user's email is not public. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="events_url" type="string" required>
      The URL to retrieve the events performed by this user, with optional placeholder for privacy filtering (e.g., https://api.github.com/users/&#123;username&#125;/events&#123;/privacy&#125;). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="followers_url" type="string" required>
      The API URL for the user's followers, as defined by the GitHub REST API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="following_url" type="string" required>
      The URL template for the API endpoint that lists users this user follows. May include a placeholder for optionally specifying a target user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gists_url" type="string" required>
      The URL template for the user's gists, which may include a placeholder for a specific gist ID (e.g., https://api.github.com/users/&#123;user&#125;/gists&#123;/gist_id&#125;). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="gravatar_id" type="string" required>
      The user's Gravatar ID, a hash used to generate the avatar image URL. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="html_url" type="string" required>
      The GitHub profile URL for the user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="id" type="number" required>
      Required.
    </ResponseField>
    <ResponseField name="login" type="string" required>
      The GitHub username (login) of the user associated with the review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      The display name of the user who authored the pull request review comment. Optional. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="node_id" type="string" required>
      The GraphQL node ID for the user associated with the pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="organizations_url" type="string" required>
      The URL to the list of organizations that the user belongs to. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="received_events_url" type="string" required>
      The URL to the user's received events in the GitHub API. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="repos_url" type="string" required>
      The API URL to the user's list of repositories. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="site_admin" type="bool" required>
      Whether the user is a site administrator (i.e., has admin rights on the GitHub instance). Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="starred_at" type="string">
      Optional.
    </ResponseField>
    <ResponseField name="starred_url" type="string" required>
      The API endpoint URL that lists repositories starred by the user. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="subscriptions_url" type="string" required>
      The API endpoint URL for the user's subscriptions, e.g., https://api.github.com/users/&#123;username&#125;/subscriptions. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of GitHub account, such as 'User' or 'Bot'. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="url" type="string" required>
      The API URL for the user associated with this pull request review comment. Required. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="user_view_type" type="string">
      Optional.
    </ResponseField>
  </Expandable>
</ResponseField>
