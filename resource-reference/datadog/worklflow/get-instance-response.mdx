---
title: "datadog_worklflow_get_instance_response"
description: "`datadog_worklflow_get_instance_response` is a real, running or completed instance of a Datadog Workflow Automation workflow -- one real execution of..."
---

`datadog_worklflow_get_instance_response` is a real, running or completed instance of a Datadog Workflow Automation workflow -- one real execution of a `datadog_get_workflow_response` definition, with its own real status and step-by-step results (the misspelled "worklflow" reflects Datadog's own real, live wire name, not a transcription error here).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	worklflow "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/worklflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own get instance response"})

    		ubx.Resource(worklflow.GetInstanceResponse, "example", worklflow.GetInstanceResponseConfig{
    			InstanceId: "example",
    			WorkflowId: "example",
    			Meta:       map[string]any{"payload": map[string]any{"key": "value"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GetInstanceResponse } from "@ubx/sdk-datadog/datadog/worklflow/get_instance_response";

    export default stack("example", () => {
      intent({ summary: "example own get instance response" });

      resource(GetInstanceResponse, "example", {
        instanceId: "example",
        workflowId: "example",
        meta: { payload: { key: "value" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.worklflow import GetInstanceResponse, GetInstanceResponseConfig

    def describe():
        ubx.intent("example own get instance response")

        ubx.resource(GetInstanceResponse, "example", GetInstanceResponseConfig(
            instance_id="example",
            workflow_id="example",
            meta={"payload": {"key": "value"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_worklflow_get_instance_response, with instance id
    example, workflow id example, meta a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="instance_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information for creating a workflow instance. Optional.
  <Expandable title="properties">
    <ResponseField name="payload" type="map(any)">
      The input parameters to the workflow. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="workflow_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="data" type="object">
  The data of the instance response. Computed by Datadog after `ship`.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      The attributes of the instance response data. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          The id of the instance. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
