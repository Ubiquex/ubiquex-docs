---
title: "datadog_entity_integration_config_response"
description: "`datadog_entity_integration_config_response` is real, per-entity configuration for how Datadog's Software Catalog links a service..."
---

`datadog_entity_integration_config_response` is real, per-entity configuration for how Datadog's Software Catalog links a service, or other cataloged entity, to an external integration (a repository, a dashboard, an on-call schedule) it references.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	entity "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/entity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own integration config response"})

    		ubx.Resource(entity.IntegrationConfigResponse, "example", entity.IntegrationConfigResponseConfig{
    			Data:          map[string]any{"attributes": map[string]any{"config": map[string]any{"key": "value"}}},
    			IntegrationId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IntegrationConfigResponse } from "@ubx/sdk-datadog/datadog/entity/integration_config_response";

    export default stack("example", () => {
      intent({ summary: "example own integration config response" });

      resource(IntegrationConfigResponse, "example", {
        data: { attributes: { config: { key: "value" } } },
        integrationId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.entity.integration_config_response import IntegrationConfigResponse, IntegrationConfigResponseConfig

    def describe():
        ubx.intent("example own integration config response")

        ubx.resource(IntegrationConfigResponse, "example", IntegrationConfigResponseConfig(
            data={"attributes": {"config": {"key": "value"}}},
            integration_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_entity_integration_config_response, with data a
    set value, integration id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  JSON:API resource object used in a request to create or update an entity integration configuration. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes used to create or update an entity integration configuration. Required.
      <Expandable title="properties">
        <ResponseField name="config" type="map(any)" required>
          Integration-specific configuration payload. The shape of this object depends on the integration identified by the path parameter. For `github`, the object must contain an `enabled_repos` array. For `jira`, it must contain an `enabled_projects` array. For `pagerduty`, it must contain an `accounts` array. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      JSON:API resource type for the entity integration configuration create or update request. Always `entity_integration_config_requests`. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="integration_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
