---
title: "datadog_monitor_config_policy_response"
description: "`datadog_monitor_config_policy_response` is a real, org-wide policy constraining how monitors may be configured -- for example..."
---

`datadog_monitor_config_policy_response` is a real, org-wide policy constraining how monitors may be configured -- for example, requiring every monitor to carry a specific tag -- enforced across all monitors created in the organization, not just one.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own config policy response"})

    		ubx.Resource(monitor.ConfigPolicyResponse, "example", monitor.ConfigPolicyResponseConfig{
    			Data:     map[string]any{"attributes": map[string]any{"policy": map[string]any{"tag_key": "example"}}},
    			PolicyId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigPolicyResponse } from "@ubx/sdk-datadog/datadog/monitor/config_policy_response";

    export default stack("example", () => {
      intent({ summary: "example own config policy response" });

      resource(ConfigPolicyResponse, "example", {
        data: { attributes: { policy: { tagKey: "example" } } },
        policyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.monitor.config_policy_response import ConfigPolicyResponse, ConfigPolicyResponseConfig

    def describe():
        ubx.intent("example own config policy response")

        ubx.resource(ConfigPolicyResponse, "example", ConfigPolicyResponseConfig(
            data={"attributes": {"policy": {"tag_key": "example"}}},
            policy_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_monitor_config_policy_response, with data a set
    value, policy id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  A monitor configuration policy data. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Policy and policy type for a monitor configuration policy. Required.
      <Expandable title="properties">
        <ResponseField name="policy" type="object" required>
          Configuration for the policy. Required.
          <Expandable title="properties">
            <ResponseField name="tag_key" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="tag_key_required" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="valid_tag_values" type="list(string)">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="policy_type" type="string" required>
          The monitor configuration policy type. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Monitor configuration policy resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
