---
title: "datadog_incident_responders_response"
description: "`datadog_incident_responders_response` reads the real responders currently assigned to an incident."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_responders_response"})

    		ubx.Data(incident.RespondersResponse, "example", incident.RespondersResponseConfig{
    			IncidentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RespondersResponse } from "@ubx/sdk-datadog/datadog/data/incident/responders_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_responders_response" });

      data(RespondersResponse, "example", {
        incidentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import RespondersResponse, RespondersResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_responders_response")

        ubx.data(RespondersResponse, "example", RespondersResponseConfig(
            incident_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string" required>
  The real, unique identifier of this incident. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of incident responders. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included related resources. Returned by the lookup.
</ResponseField>
