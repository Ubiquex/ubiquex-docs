---
title: "datadog_incident_org_settings_response"
description: "`datadog_incident_org_settings_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_org_settings_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_org_settings_response"})

    		ubx.Data(incident.OrgSettingsResponse, "example", incident.OrgSettingsResponseConfig{
    			IncidentTypeId: "example",
    			Include:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrgSettingsResponse } from "@ubx/sdk-datadog/datadog/data/incident/org_settings_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_org_settings_response" });

      data(OrgSettingsResponse, "example", {
        incidentTypeId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import OrgSettingsResponse, OrgSettingsResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_org_settings_response")

        ubx.data(OrgSettingsResponse, "example", OrgSettingsResponseConfig(
            incident_type_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_type_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Incident org settings data in a response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an incident org settings resource in a response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="created" type="string" required>
          Timestamp when the settings were created. Required lookup argument.
        </ResponseField>
        <ResponseField name="modified" type="string" required>
          Timestamp when the settings were last modified. Required lookup argument.
        </ResponseField>
        <ResponseField name="settings" type="map(any)" required>
          The settings configuration for an incident org settings resource. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The org settings identifier. Required lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships for an incident org settings resource. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="incident_type" type="object">
          Relationship to an incident type. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Relationship to incident type object. Required lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Incident org settings resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
