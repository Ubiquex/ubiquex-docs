---
title: "datadog_incident_handles_response"
description: "`datadog_incident_handles_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_handles_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_handles_response"})

    		ubx.Data(incident.HandlesResponse, "example", incident.HandlesResponseConfig{
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HandlesResponse } from "@ubx/sdk-datadog/datadog/data/incident/handles_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_handles_response" });

      data(HandlesResponse, "example", {
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import HandlesResponse, HandlesResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_handles_response")

        ubx.data(HandlesResponse, "example", HandlesResponseConfig(
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of incident handle data objects returned in a list response. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included related resources Returned by the lookup.
</ResponseField>
