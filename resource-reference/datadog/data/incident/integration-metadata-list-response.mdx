---
title: "datadog_incident_integration_metadata_list_response"
description: "`datadog_incident_integration_metadata_list_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_integration_metadata_list_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_integration_metadata_list_response"})

    		ubx.Data(incident.IntegrationMetadataListResponse, "example", incident.IntegrationMetadataListResponseConfig{
    			IncidentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegrationMetadataListResponse } from "@ubx/sdk-datadog/datadog/data/incident/integration_metadata_list_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_incident_integration_metadata_list_response",
      });

      data(IntegrationMetadataListResponse, "example", {
        incidentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import IntegrationMetadataListResponse, IntegrationMetadataListResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_integration_metadata_list_response")

        ubx.data(IntegrationMetadataListResponse, "example", IntegrationMetadataListResponseConfig(
            incident_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of incident integration metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included related resources that the user requested. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  The metadata object containing pagination metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Pagination properties. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="next_offset" type="number">
          The index of the first element in the next page of results. Equal to page size added to the current offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          The index of the first element in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          Maximum size of pages to return. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
