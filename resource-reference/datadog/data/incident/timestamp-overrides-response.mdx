---
title: "datadog_incident_timestamp_overrides_response"
description: "`datadog_incident_timestamp_overrides_response` reads the real, manually overridden timestamps on an incident's own timeline."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_timestamp_overrides_response"})

    		ubx.Data(incident.TimestampOverridesResponse, "example", incident.TimestampOverridesResponseConfig{
    			IncidentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { TimestampOverridesResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/incident/timestamp_overrides_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_timestamp_overrides_response" });

      data(TimestampOverridesResponse, "example", {
        incidentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import TimestampOverridesResponse, TimestampOverridesResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_timestamp_overrides_response")

        ubx.data(TimestampOverridesResponse, "example", TimestampOverridesResponseConfig(
            incident_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string" required>
  The real, unique identifier of this incident. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of timestamp overrides. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included related resources. Returned by the lookup.
</ResponseField>
