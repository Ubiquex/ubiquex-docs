---
title: "datadog_incident_user_defined_roles_response"
description: "`datadog_incident_user_defined_roles_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_user_defined_roles_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_user_defined_roles_response"})

    		ubx.Data(incident.UserDefinedRolesResponse, "example", incident.UserDefinedRolesResponseConfig{
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserDefinedRolesResponse } from "@ubx/sdk-datadog/datadog/data/incident/user_defined_roles_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_user_defined_roles_response" });

      data(UserDefinedRolesResponse, "example", {
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import UserDefinedRolesResponse, UserDefinedRolesResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_user_defined_roles_response")

        ubx.data(UserDefinedRolesResponse, "example", UserDefinedRolesResponseConfig(
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of incident user-defined role data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included resources for an incident user-defined role response. Returned by the lookup.
</ResponseField>
