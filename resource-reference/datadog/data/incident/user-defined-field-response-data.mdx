---
title: "datadog_incident_user_defined_field_response_data"
description: "`datadog_incident_user_defined_field_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_user_defined_field_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_user_defined_field_response_data"})

    		ubx.Data(incident.UserDefinedFieldResponseData, "example", incident.UserDefinedFieldResponseDataConfig{
    			Include:        "example",
    			IncludeDeleted: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UserDefinedFieldResponseData } from "@ubx/sdk-datadog/datadog/data/incident/user_defined_field_response_data";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_incident_user_defined_field_response_data",
      });

      data(UserDefinedFieldResponseData, "example", {
        include: "example",
        includeDeleted: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import UserDefinedFieldResponseData, UserDefinedFieldResponseDataConfig

    def describe():
        ubx.intent("look up datadog_incident_user_defined_field_response_data")

        ubx.data(UserDefinedFieldResponseData, "example", UserDefinedFieldResponseDataConfig(
            include="example",
            include_deleted=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_deleted" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of user-defined field objects. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata for the user-defined field list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="offset" type="number">
      The offset of the current page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="size" type="number">
      The total number of items in the current page. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
