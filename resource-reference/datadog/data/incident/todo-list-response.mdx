---
title: "datadog_incident_todo_list_response"
description: "`datadog_incident_todo_list_response` reads the real checklist of to-do items attached to an incident."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_todo_list_response"})

    		ubx.Data(incident.TodoListResponse, "example", incident.TodoListResponseConfig{
    			IncidentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { TodoListResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/incident/todo_list_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_todo_list_response" });

      data(TodoListResponse, "example", {
        incidentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import TodoListResponse, TodoListResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_todo_list_response")

        ubx.data(TodoListResponse, "example", TodoListResponseConfig(
            incident_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string" required>
  The real, unique identifier of this incident. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of incident todos. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included related resources that the user requested. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  The metadata object containing pagination metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Pagination properties. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="next_offset" type="number">
          The index of the first element in the next page of results. Equal to page size added to the current offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          The index of the first element in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          Maximum size of pages to return. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
