---
title: "datadog_incident_notification_rule_array"
description: "`datadog_incident_notification_rule_array` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_notification_rule_array` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_notification_rule_array"})

    		ubx.Data(incident.NotificationRuleArray, "example", incident.NotificationRuleArrayConfig{
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationRuleArray } from "@ubx/sdk-datadog/datadog/data/incident/notification_rule_array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_notification_rule_array" });

      data(NotificationRuleArray, "example", {
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import NotificationRuleArray, NotificationRuleArrayConfig

    def describe():
        ubx.intent("look up datadog_incident_notification_rule_array")

        ubx.data(NotificationRuleArray, "example", NotificationRuleArrayConfig(
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The `NotificationRuleArray` `data`. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Related objects that are included in the response. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Pagination metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="next_offset" type="number">
          The offset for the next page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          The current offset in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          The number of results returned per page. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
