---
title: "datadog_incident_notification_template_array"
description: "`datadog_incident_notification_template_array` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_notification_template_array` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_notification_template_array"})

    		ubx.Data(incident.NotificationTemplateArray, "example", incident.NotificationTemplateArrayConfig{
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationTemplateArray } from "@ubx/sdk-datadog/datadog/data/incident/notification_template_array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_notification_template_array" });

      data(NotificationTemplateArray, "example", {
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import NotificationTemplateArray, NotificationTemplateArrayConfig

    def describe():
        ubx.intent("look up datadog_incident_notification_template_array")

        ubx.data(NotificationTemplateArray, "example", NotificationTemplateArrayConfig(
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The `NotificationTemplateArray` `data`. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Related objects that are included in the response. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total number of notification templates. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total number of notification templates matching the filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
