---
title: "datadog_incident_rule_data_response"
description: "`datadog_incident_rule_data_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_incident_rule_data_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_incident_rule_data_response"})

    		ubx.Data(incident.RuleDataResponse, "example", incident.RuleDataResponseConfig{
    			IncidentTypeUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RuleDataResponse } from "@ubx/sdk-datadog/datadog/data/incident/rule_data_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_incident_rule_data_response" });

      data(RuleDataResponse, "example", {
        incidentTypeUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.incident import RuleDataResponse, RuleDataResponseConfig

    def describe():
        ubx.intent("look up datadog_incident_rule_data_response")

        ubx.data(RuleDataResponse, "example", RuleDataResponseConfig(
            incident_type_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_type_uuid" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of incident rules. Returned by the lookup.
</ResponseField>
