---
title: "datadog_ownership_settings_response"
description: "`datadog_ownership_settings_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_ownership_settings_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ownership "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/ownership"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_ownership_settings_response"})

    		ubx.Data(ownership.SettingsResponse, "example", ownership.SettingsResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SettingsResponse } from "@ubx/sdk-datadog/datadog/data/ownership/settings_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_ownership_settings_response" });

      data(SettingsResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.ownership import SettingsResponse, SettingsResponseConfig

    def describe():
        ubx.intent("look up datadog_ownership_settings_response")

        ubx.data(SettingsResponse, "example", SettingsResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  The data wrapper for an ownership settings response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of the ownership settings response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="auto_tag" type="bool" required>
          Whether automatic ownership tagging is enabled. Required lookup argument.
        </ResponseField>
        <ResponseField name="confidence_level" type="string" required>
          The ownership confidence level. Required lookup argument.
        </ResponseField>
        <ResponseField name="version" type="number" required>
          The current version of the ownership settings. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The identifier of the ownership settings resource. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the ownership settings resource. The value should always be `ownership_settings`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
