---
title: "datadog_ownership_untagged_findings_response"
description: "`datadog_ownership_untagged_findings_response` reads real findings for resources with no ownership tag assigned at all."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	ownership "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/ownership"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_ownership_untagged_findings_response"})

    		ubx.Data(ownership.UntaggedFindingsResponse, "example", ownership.UntaggedFindingsResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { UntaggedFindingsResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/ownership/untagged_findings_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_ownership_untagged_findings_response" });

      data(UntaggedFindingsResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.ownership import UntaggedFindingsResponse, UntaggedFindingsResponseConfig

    def describe():
        ubx.intent("look up datadog_ownership_untagged_findings_response")

        ubx.data(UntaggedFindingsResponse, "example", UntaggedFindingsResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  The data wrapper for an ownership untagged findings response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The counts of findings without a team tag by ownership confidence. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="high_confidence" type="number" required>
          The number of high confidence findings without a team tag. Required lookup argument.
        </ResponseField>
        <ResponseField name="low_confidence" type="number" required>
          The number of low confidence findings without a team tag. Required lookup argument.
        </ResponseField>
        <ResponseField name="medium_confidence" type="number" required>
          The number of medium confidence findings without a team tag. Required lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number" required>
          The total number of findings without a team tag. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The identifier of the ownership untagged findings resource. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the ownership untagged findings resource. The value should always be `ownership_untagged_findings`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
