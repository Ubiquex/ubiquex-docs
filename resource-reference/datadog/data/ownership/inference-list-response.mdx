---
title: "datadog_ownership_inference_list_response"
description: "`datadog_ownership_inference_list_response` lists Datadog's own real, inferred ownership assignments for Software Catalog entities."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ownership "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/ownership"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_ownership_inference_list_response"})

    		ubx.Data(ownership.InferenceListResponse, "example", ownership.InferenceListResponseConfig{
    			ResourceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InferenceListResponse } from "@ubx/sdk-datadog/datadog/data/ownership/inference_list_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_ownership_inference_list_response" });

      data(InferenceListResponse, "example", {
        resourceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.ownership import InferenceListResponse, InferenceListResponseConfig

    def describe():
        ubx.intent("look up datadog_ownership_inference_list_response")

        ubx.data(InferenceListResponse, "example", InferenceListResponseConfig(
            resource_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="resource_id" type="string" required>
  The real, unique identifier of this resource. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The data wrapper for the ownership inferences collection response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of the ownership inferences collection response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="items" type="list(object)" required>
          The list of inferences for a resource, with one inference per owner type. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The resource identifier associated with the returned inferences. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the ownership inferences collection resource. The value should always be `ownership_inferences`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
