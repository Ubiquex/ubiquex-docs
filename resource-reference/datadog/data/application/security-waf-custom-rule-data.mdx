---
title: "datadog_application_security_waf_custom_rule_data"
description: "`datadog_application_security_waf_custom_rule_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_application_security_waf_custom_rule_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/application"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_application_security_waf_custom_rule_data"})

    		ubx.Data(application.SecurityWafCustomRuleData, "example", application.SecurityWafCustomRuleDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityWafCustomRuleData } from "@ubx/sdk-datadog/datadog/data/application/security_waf_custom_rule_data";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_application_security_waf_custom_rule_data",
      });

      data(SecurityWafCustomRuleData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.application import SecurityWafCustomRuleData, SecurityWafCustomRuleDataConfig

    def describe():
        ubx.intent("look up datadog_application_security_waf_custom_rule_data")

        ubx.data(SecurityWafCustomRuleData, "example", SecurityWafCustomRuleDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The WAF custom rule data. Returned by the lookup.
</ResponseField>
