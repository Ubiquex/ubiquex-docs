---
title: "datadog_application_security_service_resource"
description: "`datadog_application_security_service_resource` looks up a real service as tracked by Application Security Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/application"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_application_security_service_resource"})

    		ubx.Data(application.SecurityServiceResource, "example", application.SecurityServiceResourceConfig{
    			ServiceFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SecurityServiceResource } from "@ubx/sdk-datadog/datadog/data/application/security_service_resource";

    export default stack("example", () => {
      intent({ summary: "look up datadog_application_security_service_resource" });

      data(SecurityServiceResource, "example", {
        serviceFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.application import SecurityServiceResource, SecurityServiceResourceConfig

    def describe():
        ubx.intent("look up datadog_application_security_service_resource")

        ubx.data(SecurityServiceResource, "example", SecurityServiceResourceConfig(
            service_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="service_filter" type="string" required>
  A real filter expression restricting results to matching services. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The list of services matching the requested name. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata returned alongside the list of services. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="num_services_with_appsec" type="number" required>
      The number of services with Application Security Management (Threats) enabled. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
