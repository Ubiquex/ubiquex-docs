---
title: "datadog_report_schedule_list_response"
description: "`datadog_report_schedule_list_response` lists the real schedules configured to regenerate a report."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	report "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/report"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_report_schedule_list_response"})

    		ubx.Data(report.ScheduleListResponse, "example", report.ScheduleListResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ScheduleListResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/report/schedule_list_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_report_schedule_list_response" });

      data(ScheduleListResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.report import ScheduleListResponse, ScheduleListResponseConfig

    def describe():
        ubx.intent("look up datadog_report_schedule_list_response")

        ubx.data(ScheduleListResponse, "example", ScheduleListResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The list of report schedules. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Related resources included with the report schedules, such as authors and rendered resources. Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  Pagination links for navigating a report schedule list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="first" type="string">
      Link to the first page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last" type="string">
      Link to the last page, or `null` if it is unavailable. Optional lookup argument.
    </ResponseField>
    <ResponseField name="next" type="string">
      Link to the next page, or `null` if it is unavailable. Optional lookup argument.
    </ResponseField>
    <ResponseField name="prev" type="string">
      Link to the previous page, or `null` if it is unavailable. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string">
      Link to the current page. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for a paginated report schedule list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Offset and limit pagination metadata for a report schedule list response. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_offset" type="number">
          The first offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_offset" type="number">
          The last offset when the total count is known, or `null` if it is unavailable. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          The maximum number of schedules returned. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_offset" type="number">
          The next offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          The current offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_offset" type="number">
          The previous offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          The total number of matching schedules. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          The pagination type. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
