---
title: "datadog_degradation_array"
description: "`datadog_degradation_array` lists real, detected performance degradations -- services or endpoints whose latency, error rate..."
---

`datadog_degradation_array` lists real, detected performance degradations -- services or endpoints whose latency, error rate, or other health signal has measurably worsened, each tracked as its own entity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	degradation "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/degradation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_degradation_array"})

    		ubx.Data(degradation.Array, "example", degradation.ArrayConfig{
    			Include: "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Array } from "@ubx/sdk-datadog/datadog/data/degradation/array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_degradation_array" });

      data(Array, "example", {
        include: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.degradation import Array, ArrayConfig

    def describe():
        ubx.intent("look up datadog_degradation_array")

        ubx.data(Array, "example", ArrayConfig(
            include="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of degradation data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  The included related resources of a degradation. Client must explicitly request these resources by name in the `include` query parameter. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Offset-based pagination schema. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_offset" type="number">
          Integer representing the offset to fetch the first page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_offset" type="number">
          Integer representing the offset to fetch the last page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Integer representing the number of elements to be returned in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_offset" type="number">
          Integer representing the index of the first element in the next page of results. Equal to page size added to the current offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          Integer representing the index of the first element in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_offset" type="number">
          Integer representing the index of the first element in the previous page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Integer representing the total number of elements available. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          The pagination type used for offset-based pagination. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
