---
title: "datadog_degradation_template_array"
description: "`datadog_degradation_template_array` lists the real, reusable templates for defining how a new performance degradation gets detected and tracked."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	degradation "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/degradation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_degradation_template_array"})

    		ubx.Data(degradation.TemplateArray, "example", degradation.TemplateArrayConfig{
    			PageId:  "example",
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TemplateArray } from "@ubx/sdk-datadog/datadog/data/degradation/template_array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_degradation_template_array" });

      data(TemplateArray, "example", {
        pageId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.degradation import TemplateArray, TemplateArrayConfig

    def describe():
        ubx.intent("look up datadog_degradation_template_array")

        ubx.data(TemplateArray, "example", TemplateArrayConfig(
            page_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_id" type="string" required>
  The real, unique identifier of this status page. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of degradation template data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  The included related resources of a degradation template. Client must explicitly request these resources by name in the `include` query parameter. Returned by the lookup.
</ResponseField>
