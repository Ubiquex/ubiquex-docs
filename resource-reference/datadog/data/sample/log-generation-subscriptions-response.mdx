---
title: "datadog_sample_log_generation_subscriptions_response"
description: "`datadog_sample_log_generation_subscriptions_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_sample_log_generation_subscriptions_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sample "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/sample"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_sample_log_generation_subscriptions_response"})

    		ubx.Data(sample.LogGenerationSubscriptionsResponse, "example", sample.LogGenerationSubscriptionsResponseConfig{
    			EndTimestamp:   "example",
    			StartTimestamp: "example",
    			Status:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LogGenerationSubscriptionsResponse } from "@ubx/sdk-datadog/datadog/data/sample/log_generation_subscriptions_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_sample_log_generation_subscriptions_response",
      });

      data(LogGenerationSubscriptionsResponse, "example", {
        endTimestamp: "example",
        startTimestamp: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.sample import LogGenerationSubscriptionsResponse, LogGenerationSubscriptionsResponseConfig

    def describe():
        ubx.intent("look up datadog_sample_log_generation_subscriptions_response")

        ubx.data(LogGenerationSubscriptionsResponse, "example", LogGenerationSubscriptionsResponseConfig(
            end_timestamp="example",
            start_timestamp="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end_timestamp" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start_timestamp" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Filter that controls whether to return only active subscriptions or every subscription on record. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The list of sample log generation subscriptions. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata returned alongside a list of sample log generation subscriptions. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="total_subscriptions" type="number" required>
      The total number of subscriptions matching the request, irrespective of pagination. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
