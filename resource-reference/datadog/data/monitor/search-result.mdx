---
title: "datadog_monitor_search_result"
description: "`datadog_monitor_search_result` searches for real monitors by name, tag, or status."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_monitor_search_result"})

    		ubx.Data(monitor.SearchResult, "example", monitor.SearchResultConfig{
    			Page:    1,
    			PerPage: 1,
    			Query:   "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { SearchResult } from "./local-sdk/datadog/sdk/typescript/datadog/data/monitor/search_result.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_monitor_search_result" });

      data(SearchResult, "example", {
        page: 1,
        perPage: 1,
        query: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.monitor import SearchResult, SearchResultConfig

    def describe():
        ubx.intent("look up datadog_monitor_search_result")

        ubx.data(SearchResult, "example", SearchResultConfig(
            page=1,
            per_page=1,
            query="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  The real query string evaluated against the underlying data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="counts" type="object">
  The counts of monitors per different criteria. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="muted" type="list(object)">
      Search facets. Optional lookup argument.
    </ResponseField>
    <ResponseField name="status" type="list(object)">
      Search facets. Optional lookup argument.
    </ResponseField>
    <ResponseField name="tag" type="list(object)">
      Search facets. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="list(object)">
      Search facets. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="metadata" type="object">
  Metadata about the response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="number">
      The page to start paginating from. Returned by the lookup.
    </ResponseField>
    <ResponseField name="page_count" type="number">
      The number of pages. Returned by the lookup.
    </ResponseField>
    <ResponseField name="per_page" type="number">
      The number of monitors to return per page. Returned by the lookup.
    </ResponseField>
    <ResponseField name="total_count" type="number">
      The total number of monitors. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="monitors" type="list(object)">
  The list of found monitors. Returned by the lookup.
</ResponseField>
