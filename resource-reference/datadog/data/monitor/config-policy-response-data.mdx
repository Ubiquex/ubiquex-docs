---
title: "datadog_monitor_config_policy_response_data"
description: "`datadog_monitor_config_policy_response_data` looks up a real Monitor configuration policy -- an org-wide governance rule constraining how monitors can..."
---

`datadog_monitor_config_policy_response_data` looks up a real Monitor configuration policy -- an org-wide governance rule constraining how monitors can be configured.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_monitor_config_policy_response_data"})

    		ubx.Data(monitor.ConfigPolicyResponseData, "example", monitor.ConfigPolicyResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ConfigPolicyResponseData } from "./local-sdk/datadog/sdk/typescript/datadog/data/monitor/config_policy_response_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_monitor_config_policy_response_data" });

      data(ConfigPolicyResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.monitor import ConfigPolicyResponseData, ConfigPolicyResponseDataConfig

    def describe():
        ubx.intent("look up datadog_monitor_config_policy_response_data")

        ubx.data(ConfigPolicyResponseData, "example", ConfigPolicyResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An array of monitor configuration policies. Returned by the lookup.
</ResponseField>
