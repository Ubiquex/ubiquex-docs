---
title: "datadog_monitor_notification_rule_list_response"
description: "`datadog_monitor_notification_rule_list_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_monitor_notification_rule_list_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_monitor_notification_rule_list_response"})

    		ubx.Data(monitor.NotificationRuleListResponse, "example", monitor.NotificationRuleListResponseConfig{
    			Filters: "example",
    			Include: "example",
    			Page:    1,
    			PerPage: 1,
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationRuleListResponse } from "@ubx/sdk-datadog/datadog/data/monitor/notification_rule_list_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_monitor_notification_rule_list_response",
      });

      data(NotificationRuleListResponse, "example", {
        filters: "example",
        include: "example",
        page: 1,
        perPage: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.monitor import NotificationRuleListResponse, NotificationRuleListResponseConfig

    def describe():
        ubx.intent("look up datadog_monitor_notification_rule_list_response")

        ubx.data(NotificationRuleListResponse, "example", NotificationRuleListResponseConfig(
            filters="example",
            include="example",
            page=1,
            per_page=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filters" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of monitor notification rules. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of objects related to the monitor notification rules. Returned by the lookup.
</ResponseField>
