---
title: "datadog_monitor_group_search_result"
description: "`datadog_monitor_group_search_result` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_monitor_group_search_result` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	monitor "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/monitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_monitor_group_search_result"})

    		ubx.Data(monitor.GroupSearchResult, "example", monitor.GroupSearchResultConfig{
    			Page:    1,
    			PerPage: 1,
    			Query:   "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GroupSearchResult } from "@ubx/sdk-datadog/datadog/data/monitor/group_search_result";

    export default stack("example", () => {
      intent({ summary: "look up datadog_monitor_group_search_result" });

      data(GroupSearchResult, "example", {
        page: 1,
        perPage: 1,
        query: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.monitor import GroupSearchResult, GroupSearchResultConfig

    def describe():
        ubx.intent("look up datadog_monitor_group_search_result")

        ubx.data(GroupSearchResult, "example", GroupSearchResultConfig(
            page=1,
            per_page=1,
            query="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="per_page" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="counts" type="object">
  The counts of monitor groups per different criteria. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="status" type="list(object)">
      Search facets. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="list(object)">
      Search facets. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="groups" type="list(object)">
  The list of found monitor groups. Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="object">
  Metadata about the response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="number">
      The page to start paginating from. Returned by the lookup.
    </ResponseField>
    <ResponseField name="page_count" type="number">
      The number of pages. Returned by the lookup.
    </ResponseField>
    <ResponseField name="per_page" type="number">
      The number of monitors to return per page. Returned by the lookup.
    </ResponseField>
    <ResponseField name="total_count" type="number">
      The total number of monitors. Returned by the lookup.
    </ResponseField>
  </Expandable>
</ResponseField>
