---
title: "datadog_dataset_response"
description: "`datadog_dataset_response` looks up a real, custom dataset uploaded to Datadog."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dataset "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/dataset"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_dataset_response"})

    		ubx.Data(dataset.Response, "example", dataset.ResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Response } from "@ubx/sdk-datadog/datadog/data/dataset/response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_dataset_response" });

      data(Response, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.dataset import Response, ResponseConfig

    def describe():
        ubx.intent("look up datadog_dataset_response")

        ubx.data(Response, "example", ResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The list of datasets returned in response. Returned by the lookup.
</ResponseField>
