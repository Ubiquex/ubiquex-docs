---
title: "datadog_synthetics_global_variable"
description: "`datadog_synthetics_global_variable` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_global_variable` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_global_variable"})

    		ubx.Data(synthetics.GlobalVariable, "example", synthetics.GlobalVariableConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GlobalVariable } from "@ubx/sdk-datadog/datadog/data/synthetics/global_variable";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_global_variable" });

      data(GlobalVariable, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import GlobalVariable, GlobalVariableConfig

    def describe():
        ubx.intent("look up datadog_synthetics_global_variable")

        ubx.data(GlobalVariable, "example", GlobalVariableConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="variables" type="list(object)">
  Array of Synthetic global variables. Returned by the lookup.
</ResponseField>
