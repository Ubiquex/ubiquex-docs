---
title: "datadog_synthetics_api_multistep_subtest_data"
description: "`datadog_synthetics_api_multistep_subtest_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_api_multistep_subtest_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_api_multistep_subtest_data"})

    		ubx.Data(synthetics.ApiMultistepSubtestData, "example", synthetics.ApiMultistepSubtestDataConfig{
    			PublicId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiMultistepSubtestData } from "@ubx/sdk-datadog/datadog/data/synthetics/api_multistep_subtest_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_api_multistep_subtest_data" });

      data(ApiMultistepSubtestData, "example", {
        publicId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import ApiMultistepSubtestData, ApiMultistepSubtestDataConfig

    def describe():
        ubx.intent("look up datadog_synthetics_api_multistep_subtest_data")

        ubx.data(ApiMultistepSubtestData, "example", ApiMultistepSubtestDataConfig(
            public_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="public_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of API tests that can be added as subtests. Returned by the lookup.
</ResponseField>
