---
title: "datadog_synthetics_test_details_without_steps"
description: "`datadog_synthetics_test_details_without_steps` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_test_details_without_steps` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_test_details_without_steps"})

    		ubx.Data(synthetics.TestDetailsWithoutSteps, "example", synthetics.TestDetailsWithoutStepsConfig{
    			PageNumber: 1,
    			PageSize:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestDetailsWithoutSteps } from "@ubx/sdk-datadog/datadog/data/synthetics/test_details_without_steps";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_test_details_without_steps" });

      data(TestDetailsWithoutSteps, "example", {
        pageNumber: 1,
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import TestDetailsWithoutSteps, TestDetailsWithoutStepsConfig

    def describe():
        ubx.intent("look up datadog_synthetics_test_details_without_steps")

        ubx.data(TestDetailsWithoutSteps, "example", TestDetailsWithoutStepsConfig(
            page_number=1,
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_number" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tests" type="list(object)">
  Array of Synthetic tests configuration. Returned by the lookup.
</ResponseField>
