---
title: "datadog_synthetics_downtime_data"
description: "`datadog_synthetics_downtime_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_downtime_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_downtime_data"})

    		ubx.Data(synthetics.DowntimeData, "example", synthetics.DowntimeDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DowntimeData } from "@ubx/sdk-datadog/datadog/data/synthetics/downtime_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_downtime_data" });

      data(DowntimeData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import DowntimeData, DowntimeDataConfig

    def describe():
        ubx.intent("look up datadog_synthetics_downtime_data")

        ubx.data(DowntimeData, "example", DowntimeDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of Synthetics downtime objects. Returned by the lookup.
</ResponseField>
