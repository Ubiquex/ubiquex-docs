---
title: "datadog_synthetics_test_version_change_data"
description: "`datadog_synthetics_test_version_change_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_test_version_change_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_test_version_change_data"})

    		ubx.Data(synthetics.TestVersionChangeData, "example", synthetics.TestVersionChangeDataConfig{
    			PublicId:          "example",
    			LastVersionNumber: 1,
    			Limit:             1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestVersionChangeData } from "@ubx/sdk-datadog/datadog/data/synthetics/test_version_change_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_test_version_change_data" });

      data(TestVersionChangeData, "example", {
        publicId: "example",
        lastVersionNumber: 1,
        limit: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import TestVersionChangeData, TestVersionChangeDataConfig

    def describe():
        ubx.intent("look up datadog_synthetics_test_version_change_data")

        ubx.data(TestVersionChangeData, "example", TestVersionChangeDataConfig(
            public_id="example",
            last_version_number=1,
            limit=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="last_version_number" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="public_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of version change records. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata for a version history response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="next_last_version_number" type="number">
      The version number to use as the `last_version_number` query parameter to fetch the next page. `null` indicates there are no more pages. Optional lookup argument.
    </ResponseField>
    <ResponseField name="retention_period_in_days" type="number">
      The number of days that version history is retained. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
