---
title: "datadog_synthetics_browser_test_result_short"
description: "`datadog_synthetics_browser_test_result_short` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_browser_test_result_short` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_browser_test_result_short"})

    		ubx.Data(synthetics.BrowserTestResultShort, "example", synthetics.BrowserTestResultShortConfig{
    			PublicId: "example",
    			FromTs:   1,
    			ProbeDc:  []string{"example"},
    			ToTs:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BrowserTestResultShort } from "@ubx/sdk-datadog/datadog/data/synthetics/browser_test_result_short";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_browser_test_result_short" });

      data(BrowserTestResultShort, "example", {
        publicId: "example",
        fromTs: 1,
        probeDc: ["example"],
        toTs: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import BrowserTestResultShort, BrowserTestResultShortConfig

    def describe():
        ubx.intent("look up datadog_synthetics_browser_test_result_short")

        ubx.data(BrowserTestResultShort, "example", BrowserTestResultShortConfig(
            public_id="example",
            from_ts=1,
            probe_dc=["example"],
            to_ts=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from_ts" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="probe_dc" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="public_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="to_ts" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="last_timestamp_fetched" type="number">
  Timestamp of the latest browser test run. Returned by the lookup.
</ResponseField>

<ResponseField name="results" type="list(object)">
  Result of the latest browser test run. Returned by the lookup.
</ResponseField>
