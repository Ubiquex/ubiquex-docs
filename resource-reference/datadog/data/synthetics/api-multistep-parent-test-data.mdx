---
title: "datadog_synthetics_api_multistep_parent_test_data"
description: "`datadog_synthetics_api_multistep_parent_test_data` looks up the real, parent configuration of a multistep API test -- a synthetic test made of several..."
---

`datadog_synthetics_api_multistep_parent_test_data` looks up the real, parent configuration of a multistep API test -- a synthetic test made of several ordered HTTP steps.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_api_multistep_parent_test_data"})

    		ubx.Data(synthetics.ApiMultistepParentTestData, "example", synthetics.ApiMultistepParentTestDataConfig{
    			PublicId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ApiMultistepParentTestData } from "./local-sdk/datadog/sdk/typescript/datadog/data/synthetics/api_multistep_parent_test_data.ts";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_synthetics_api_multistep_parent_test_data",
      });

      data(ApiMultistepParentTestData, "example", {
        publicId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import ApiMultistepParentTestData, ApiMultistepParentTestDataConfig

    def describe():
        ubx.intent("look up datadog_synthetics_api_multistep_parent_test_data")

        ubx.data(ApiMultistepParentTestData, "example", ApiMultistepParentTestDataConfig(
            public_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="public_id" type="string" required>
  The real, public identifier of this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of parent tests that include this subtest. Returned by the lookup.
</ResponseField>
