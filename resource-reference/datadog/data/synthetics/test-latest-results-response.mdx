---
title: "datadog_synthetics_test_latest_results_response"
description: "`datadog_synthetics_test_latest_results_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_test_latest_results_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_test_latest_results_response"})

    		ubx.Data(synthetics.TestLatestResultsResponse, "example", synthetics.TestLatestResultsResponseConfig{
    			PublicId: "example",
    			DeviceId: []string{"example"},
    			FromTs:   1,
    			ProbeDc:  []string{"example"},
    			RunType:  "example",
    			Status:   "example",
    			ToTs:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TestLatestResultsResponse } from "@ubx/sdk-datadog/datadog/data/synthetics/test_latest_results_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_synthetics_test_latest_results_response",
      });

      data(TestLatestResultsResponse, "example", {
        publicId: "example",
        deviceId: ["example"],
        fromTs: 1,
        probeDc: ["example"],
        runType: "example",
        status: "example",
        toTs: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import TestLatestResultsResponse, TestLatestResultsResponseConfig

    def describe():
        ubx.intent("look up datadog_synthetics_test_latest_results_response")

        ubx.data(TestLatestResultsResponse, "example", TestLatestResultsResponseConfig(
            public_id="example",
            device_id=["example"],
            from_ts=1,
            probe_dc=["example"],
            run_type="example",
            status="example",
            to_ts=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="device_id" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="from_ts" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="probe_dc" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="public_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="run_type" type="string">
  The type of run for a Synthetic test result. Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Status of a Synthetic test result. Optional lookup argument.
</ResponseField>

<ResponseField name="to_ts" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of Synthetic test result summaries. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of included related resources, such as the test definition. Returned by the lookup.
</ResponseField>
