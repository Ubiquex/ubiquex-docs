---
title: "datadog_synthetics_poll_test_results_response"
description: "`datadog_synthetics_poll_test_results_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_synthetics_poll_test_results_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_synthetics_poll_test_results_response"})

    		ubx.Data(synthetics.PollTestResultsResponse, "example", synthetics.PollTestResultsResponseConfig{
    			ResultIds: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PollTestResultsResponse } from "@ubx/sdk-datadog/datadog/data/synthetics/poll_test_results_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_synthetics_poll_test_results_response" });

      data(PollTestResultsResponse, "example", {
        resultIds: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.synthetics import PollTestResultsResponse, PollTestResultsResponseConfig

    def describe():
        ubx.intent("look up datadog_synthetics_poll_test_results_response")

        ubx.data(PollTestResultsResponse, "example", PollTestResultsResponseConfig(
            result_ids="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="result_ids" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of Synthetic test results. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of included related resources, such as the test definition. Returned by the lookup.
</ResponseField>
