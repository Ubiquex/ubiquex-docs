---
title: "datadog_oauth_scopes_restriction_response"
description: "`datadog_oauth_scopes_restriction_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_oauth_scopes_restriction_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	oauth "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/oauth"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_oauth_scopes_restriction_response"})

    		ubx.Data(oauth.ScopesRestrictionResponse, "example", oauth.ScopesRestrictionResponseConfig{
    			ClientUuid: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScopesRestrictionResponse } from "@ubx/sdk-datadog/datadog/data/oauth/scopes_restriction_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_oauth_scopes_restriction_response" });

      data(ScopesRestrictionResponse, "example", {
        clientUuid: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.oauth import ScopesRestrictionResponse, ScopesRestrictionResponseConfig

    def describe():
        ubx.intent("look up datadog_oauth_scopes_restriction_response")

        ubx.data(ScopesRestrictionResponse, "example", ScopesRestrictionResponseConfig(
            client_uuid="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="client_uuid" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object of an OAuth2 client scopes restriction response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an OAuth2 client scopes restriction. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="required_permission_scopes" type="list(string)" required>
          Permission scopes automatically required for this client (for example, mobile-app permission scopes). Returns `null` when no scopes are required. Required lookup argument.
        </ResponseField>
        <ResponseField name="scopes_restriction" type="object" required>
          Allowlist of OIDC and permission scopes enforced for the OAuth2 client. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="oidc_scopes" type="list(string)" required>
              OIDC scopes the client is restricted to. Required lookup argument.
            </ResponseField>
            <ResponseField name="permission_scopes" type="list(string)" required>
              Datadog permission scopes the client is restricted to. Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      UUID of the OAuth2 client this restriction applies to. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      JSON:API resource type for an OAuth2 client scopes restriction. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
