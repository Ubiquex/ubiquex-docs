---
title: "datadog_playlist_data"
description: "`datadog_playlist_data` looks up a real, ordered sequence of Datadog dashboards that auto-cycles on a timer -- commonly used to drive a wall-mounted..."
---

`datadog_playlist_data` looks up a real, ordered sequence of Datadog dashboards that auto-cycles on a timer -- commonly used to drive a wall-mounted status display through a rotating set of dashboards.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	playlist "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/playlist"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_playlist_data"})

    		ubx.Data(playlist.Data, "example", playlist.DataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Data } from "./local-sdk/datadog/sdk/typescript/datadog/data/playlist/data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_playlist_data" });

      data(Data, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.playlist import Data, DataConfig

    def describe():
        ubx.intent("look up datadog_playlist_data")

        ubx.data(Data, "example", DataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of playlist data objects. Returned by the lookup.
</ResponseField>
