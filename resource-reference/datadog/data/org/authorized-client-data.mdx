---
title: "datadog_org_authorized_client_data"
description: "`datadog_org_authorized_client_data` looks up a real OAuth client authorized for the organization."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	org "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/org"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_org_authorized_client_data"})

    		ubx.Data(org.AuthorizedClientData, "example", org.AuthorizedClientDataConfig{
    			Filter:  "example",
    			Include: "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuthorizedClientData } from "@ubx/sdk-datadog/datadog/data/org/authorized_client_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_org_authorized_client_data" });

      data(AuthorizedClientData, "example", {
        filter: "example",
        include: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.org import AuthorizedClientData, AuthorizedClientDataConfig

    def describe():
        ubx.intent("look up datadog_org_authorized_client_data")

        ubx.data(AuthorizedClientData, "example", AuthorizedClientDataConfig(
            filter="example",
            include="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of org authorized client data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Object describing meta attributes of response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination object. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total count. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total count of elements matched by the filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
