---
title: "datadog_rule_based_view_response"
description: "`datadog_rule_based_view_response` reads a real, saved view whose own membership is computed dynamically from a rule rather than a fixed list."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rule "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/rule"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_rule_based_view_response"})

    		ubx.Data(rule.BasedViewResponse, "example", rule.BasedViewResponseConfig{
    			To:                                   1,
    			Framework:                            "example",
    			IncludeRulesWithoutFindings:          true,
    			IsCustom:                             true,
    			Query:                                "example",
    			QueryFindingsWithoutFrameworkVersion: true,
    			Version:                              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BasedViewResponse } from "@ubx/sdk-datadog/datadog/data/rule/based_view_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_rule_based_view_response" });

      data(BasedViewResponse, "example", {
        to: 1,
        framework: "example",
        includeRulesWithoutFindings: true,
        isCustom: true,
        query: "example",
        queryFindingsWithoutFrameworkVersion: true,
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.rule import BasedViewResponse, BasedViewResponseConfig

    def describe():
        ubx.intent("look up datadog_rule_based_view_response")

        ubx.data(BasedViewResponse, "example", BasedViewResponseConfig(
            to=1,
            framework="example",
            include_rules_without_findings=True,
            is_custom=True,
            query="example",
            query_findings_without_framework_version=True,
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="framework" type="string">
  The real compliance or security framework this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_rules_without_findings" type="bool">
  Whether rules with no matching findings are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="is_custom" type="bool">
  Whether this object is custom, rather than provided by Datadog. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  The real query string evaluated against the underlying data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query_findings_without_framework_version" type="bool">
  Whether findings are queried without pinning to a specific compliance framework version. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="to" type="number" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string">
  The real version identifier for this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data envelope for the rule-based view response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the rule-based view. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="count" type="number" required>
          Total number of rules in the view. Required lookup argument.
        </ResponseField>
        <ResponseField name="rules" type="list(object)" required>
          List of rules in the rule-based view. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier of the rule-based view document. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the resource. The value should always be `rule_based_view`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
