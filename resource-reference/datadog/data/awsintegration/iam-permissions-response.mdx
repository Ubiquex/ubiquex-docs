---
title: "datadog_awsintegration_iam_permissions_response"
description: "`datadog_awsintegration_iam_permissions_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_awsintegration_iam_permissions_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	awsintegration "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/awsintegration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_awsintegration_iam_permissions_response"})

    		ubx.Data(awsintegration.IamPermissionsResponse, "example", awsintegration.IamPermissionsResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IamPermissionsResponse } from "@ubx/sdk-datadog/datadog/data/awsintegration/iam_permissions_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_awsintegration_iam_permissions_response",
      });

      data(IamPermissionsResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.awsintegration import IamPermissionsResponse, IamPermissionsResponseConfig

    def describe():
        ubx.intent("look up datadog_awsintegration_iam_permissions_response")

        ubx.data(IamPermissionsResponse, "example", IamPermissionsResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  AWS Integration IAM Permissions response data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      AWS Integration IAM Permissions response attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="permissions" type="list(string)" required>
          List of AWS IAM permissions required for the integration. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The `AWSIntegrationIamPermissionsResponseData` `id`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      The `AWSIntegrationIamPermissionsResponseData` `type`. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
