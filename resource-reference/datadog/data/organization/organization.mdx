---
title: "datadog_organization"
description: "`datadog_organization` looks up a real organization's own account record."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	organization "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/organization"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_organization"})

    		ubx.Data(organization.Organization, "example", organization.OrganizationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Organization } from "./local-sdk/datadog/sdk/typescript/datadog/data/organization/organization.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_organization" });

      data(Organization, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.organization import Organization, OrganizationConfig

    def describe():
        ubx.intent("look up datadog_organization")

        ubx.data(Organization, "example", OrganizationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="orgs" type="list(object)">
  Array of organization objects. Returned by the lookup.
</ResponseField>
