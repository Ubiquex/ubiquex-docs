---
title: "datadog_restriction_query_role"
description: "`datadog_restriction_query_role` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_restriction_query_role` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	restriction "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/restriction"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_restriction_query_role"})

    		ubx.Data(restriction.QueryRole, "example", restriction.QueryRoleConfig{
    			RestrictionQueryId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { QueryRole } from "@ubx/sdk-datadog/datadog/data/restriction/query_role";

    export default stack("example", () => {
      intent({ summary: "look up datadog_restriction_query_role" });

      data(QueryRole, "example", {
        restrictionQueryId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.restriction import QueryRole, QueryRoleConfig

    def describe():
        ubx.intent("look up datadog_restriction_query_role")

        ubx.data(QueryRole, "example", QueryRoleConfig(
            restriction_query_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="restriction_query_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of roles. Returned by the lookup.
</ResponseField>
