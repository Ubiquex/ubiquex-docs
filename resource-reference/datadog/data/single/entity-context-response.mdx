---
title: "datadog_single_entity_context_response"
description: "`datadog_single_entity_context_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_single_entity_context_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	single "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/single"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_single_entity_context_response"})

    		ubx.Data(single.EntityContextResponse, "example", single.EntityContextResponseConfig{
    			Id:   "example",
    			AsOf: "example",
    			From: "example",
    			To:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityContextResponse } from "@ubx/sdk-datadog/datadog/data/single/entity_context_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_single_entity_context_response" });

      data(EntityContextResponse, "example", {
        id: "example",
        asOf: "example",
        from: "example",
        to: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.single import EntityContextResponse, EntityContextResponseConfig

    def describe():
        ubx.intent("look up datadog_single_entity_context_response")

        ubx.data(EntityContextResponse, "example", EntityContextResponseConfig(
            id="example",
            as_of="example",
            from_="example",
            to="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="as_of" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="from" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="to" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  A single entity returned by the entity context endpoint. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of an entity context entry, grouping all the historical revisions of the entity. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="revisions" type="list(object)" required>
          The historical revisions of the entity, ordered chronologically. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the entity. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the entity. Reflects the underlying entity kind from the entity context store (for example, `siem_entity_identity` for identities). Defaults to `entity` when the kind is unknown. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
