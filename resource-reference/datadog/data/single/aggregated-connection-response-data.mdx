---
title: "datadog_single_aggregated_connection_response_data"
description: "`datadog_single_aggregated_connection_response_data` reads real, aggregated network connection data for one device, in Network Device Monitoring."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	single "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/single"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_single_aggregated_connection_response_data"})

    		ubx.Data(single.AggregatedConnectionResponseData, "example", single.AggregatedConnectionResponseDataConfig{
    			From:    1,
    			GroupBy: "example",
    			Limit:   1,
    			Query:   "example",
    			Tags:    "example",
    			To:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AggregatedConnectionResponseData } from "./local-sdk/datadog/sdk/typescript/datadog/data/single/aggregated_connection_response_data.ts";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_single_aggregated_connection_response_data",
      });

      data(AggregatedConnectionResponseData, "example", {
        from: 1,
        groupBy: "example",
        limit: 1,
        query: "example",
        tags: "example",
        to: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.single import AggregatedConnectionResponseData, AggregatedConnectionResponseDataConfig

    def describe():
        ubx.intent("look up datadog_single_aggregated_connection_response_data")

        ubx.data(AggregatedConnectionResponseData, "example", AggregatedConnectionResponseDataConfig(
            from_=1,
            group_by="example",
            limit=1,
            query="example",
            tags="example",
            to=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from" type="number">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="string">
  The real field results are grouped by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  The real query string evaluated against the underlying data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  The real tags applied to this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="to" type="number">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of aggregated connection objects. Returned by the lookup.
</ResponseField>
