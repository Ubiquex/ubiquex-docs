---
title: "datadog_single_aggregated_connection_response_data"
description: "`datadog_single_aggregated_connection_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_single_aggregated_connection_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	single "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/single"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_single_aggregated_connection_response_data"})

    		ubx.Data(single.AggregatedConnectionResponseData, "example", single.AggregatedConnectionResponseDataConfig{
    			From:    1,
    			GroupBy: "example",
    			Limit:   1,
    			Query:   "example",
    			Tags:    "example",
    			To:      1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AggregatedConnectionResponseData } from "@ubx/sdk-datadog/datadog/data/single/aggregated_connection_response_data";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_single_aggregated_connection_response_data",
      });

      data(AggregatedConnectionResponseData, "example", {
        from: 1,
        groupBy: "example",
        limit: 1,
        query: "example",
        tags: "example",
        to: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.single import AggregatedConnectionResponseData, AggregatedConnectionResponseDataConfig

    def describe():
        ubx.intent("look up datadog_single_aggregated_connection_response_data")

        ubx.data(AggregatedConnectionResponseData, "example", AggregatedConnectionResponseDataConfig(
            from_=1,
            group_by="example",
            limit=1,
            query="example",
            tags="example",
            to=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="group_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="to" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of aggregated connection objects. Returned by the lookup.
</ResponseField>
