---
title: "datadog_active_billing_dimensions_response"
description: "`datadog_active_billing_dimensions_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_active_billing_dimensions_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	active "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/active"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_active_billing_dimensions_response"})

    		ubx.Data(active.BillingDimensionsResponse, "example", active.BillingDimensionsResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { BillingDimensionsResponse } from "@ubx/sdk-datadog/datadog/data/active/billing_dimensions_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_active_billing_dimensions_response" });

      data(BillingDimensionsResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.active import BillingDimensionsResponse, BillingDimensionsResponseConfig

    def describe():
        ubx.intent("look up datadog_active_billing_dimensions_response")

        ubx.data(BillingDimensionsResponse, "example", BillingDimensionsResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  Active billing dimensions data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      List of active billing dimensions. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="month" type="string">
          Datetime in ISO-8601 format, UTC, precise to hour: `[YYYY-MM-DDThh]`. Optional lookup argument.
        </ResponseField>
        <ResponseField name="values" type="list(string)">
          List of active billing dimensions. Example: `[infra_host, apm_host, serverless_infra]`. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Unique ID of the response. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Type of active billing dimensions data. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
