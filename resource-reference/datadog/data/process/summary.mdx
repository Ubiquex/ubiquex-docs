---
title: "datadog_process_summary"
description: "`datadog_process_summary` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_process_summary` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	process "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/process"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_process_summary"})

    		ubx.Data(process.Summary, "example", process.SummaryConfig{
    			From:   1,
    			Search: "example",
    			Tags:   "example",
    			To:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Summary } from "@ubx/sdk-datadog/datadog/data/process/summary";

    export default stack("example", () => {
      intent({ summary: "look up datadog_process_summary" });

      data(Summary, "example", {
        from: 1,
        search: "example",
        tags: "example",
        to: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.process import Summary, SummaryConfig

    def describe():
        ubx.intent("look up datadog_process_summary")

        ubx.data(Summary, "example", SummaryConfig(
            from_=1,
            search="example",
            tags="example",
            to=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="to" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of process summary objects. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Paging attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="after" type="string">
          The cursor used to get the next results, if any. To make the next request, use the same parameters with the addition of the `page[cursor]`. Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          Number of results returned. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
