---
title: "datadog_case_count_response"
description: "`datadog_case_count_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_case_count_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	case_ "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/case_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_case_count_response"})

    		ubx.Data(case_.CountResponse, "example", case_.CountResponseConfig{
    			GroupBys:    "example",
    			Limit:       1,
    			QueryFilter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CountResponse } from "@ubx/sdk-datadog/datadog/data/case_/count_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_case_count_response" });

      data(CountResponse, "example", {
        groupBys: "example",
        limit: 1,
        queryFilter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.case_ import CountResponse, CountResponseConfig

    def describe():
        ubx.intent("look up datadog_case_count_response")

        ubx.data(CountResponse, "example", CountResponseConfig(
            group_bys="example",
            limit=1,
            query_filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_bys" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query_filter" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object containing the count results, including per-field group breakdowns. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for the count response, including the total count and optional facet breakdowns. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="groups" type="list(object)" required>
          List of facet groups, one per field specified in `group_bys`. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Count response identifier. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Count resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
