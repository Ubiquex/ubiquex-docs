---
title: "datadog_case"
description: "`datadog_case` looks up a real case in Case Management -- a tracked unit of investigative or operational work."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	case_ "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/case_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_case"})

    		ubx.Data(case_.Case, "example", case_.CaseConfig{
    			Filter: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Case } from "@ubx/sdk-datadog/datadog/data/case/case";

    export default stack("example", () => {
      intent({ summary: "look up datadog_case" });

      data(Case, "example", {
        filter: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.case import Case, CaseConfig

    def describe():
        ubx.intent("look up datadog_case")

        ubx.data(Case, "example", CaseConfig(
            filter="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Cases response data Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Cases response metadata Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination metadata Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="current" type="number">
          Current page number Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          Number of cases in current page Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Total number of pages Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
