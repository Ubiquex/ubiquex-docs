---
title: "datadog_workflow_instance_list_item"
description: "`datadog_workflow_instance_list_item` reads one real, individual run of a Workflow Automation workflow."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	workflow "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/workflow"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_workflow_instance_list_item"})

    		ubx.Data(workflow.InstanceListItem, "example", workflow.InstanceListItemConfig{
    			WorkflowId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { InstanceListItem } from "./local-sdk/datadog/sdk/typescript/datadog/data/workflow/instance_list_item.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_workflow_instance_list_item" });

      data(InstanceListItem, "example", {
        workflowId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.workflow import InstanceListItem, InstanceListItemConfig

    def describe():
        ubx.intent("look up datadog_workflow_instance_list_item")

        ubx.data(InstanceListItem, "example", InstanceListItemConfig(
            workflow_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="workflow_id" type="string" required>
  The real, unique identifier of this workflow. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of workflow instances. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata about the instances list Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Page information for the list instances response. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          The total count of items. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
