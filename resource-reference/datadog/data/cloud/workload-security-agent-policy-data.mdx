---
title: "datadog_cloud_workload_security_agent_policy_data"
description: "`datadog_cloud_workload_security_agent_policy_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_cloud_workload_security_agent_policy_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloud "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/cloud"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_cloud_workload_security_agent_policy_data"})

    		ubx.Data(cloud.WorkloadSecurityAgentPolicyData, "example", cloud.WorkloadSecurityAgentPolicyDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { WorkloadSecurityAgentPolicyData } from "@ubx/sdk-datadog/datadog/data/cloud/workload_security_agent_policy_data";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_cloud_workload_security_agent_policy_data",
      });

      data(WorkloadSecurityAgentPolicyData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.cloud import WorkloadSecurityAgentPolicyData, WorkloadSecurityAgentPolicyDataConfig

    def describe():
        ubx.intent("look up datadog_cloud_workload_security_agent_policy_data")

        ubx.data(WorkloadSecurityAgentPolicyData, "example", WorkloadSecurityAgentPolicyDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of Agent policy objects Returned by the lookup.
</ResponseField>
