---
title: "datadog_environment"
description: "`datadog_environment` looks up a real environment tag value (such as `prod` or `staging`) reporting into the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	environment "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/environment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_environment"})

    		ubx.Data(environment.Environment, "example", environment.EnvironmentConfig{
    			Name:   "example",
    			DdEnv:  "example",
    			Key:    "example",
    			Limit:  1,
    			Offset: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Environment } from "./local-sdk/datadog/sdk/typescript/datadog/data/environment/environment.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_environment" });

      data(Environment, "example", {
        name: "example",
        ddEnv: "example",
        key: "example",
        limit: 1,
        offset: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.environment import Environment, EnvironmentConfig

    def describe():
        ubx.intent("look up datadog_environment")

        ubx.data(Environment, "example", EnvironmentConfig(
            name="example",
            dd_env="example",
            key="example",
            limit=1,
            offset=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dd_env" type="string">
  The real `env` tag value this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string">
  The real, unique key identifying this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="name" type="string">
  The real, given name of this object. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="offset" type="number">
  The number of matching results to skip before returning the rest, for offset-based pagination. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of environments. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata for environments. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination metadata for environments list responses. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total number of items. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total number of items matching the filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
