---
title: "datadog_budget"
description: "`datadog_budget` looks up a real Cloud Cost Management budget -- a configured spend limit tracked against actual cloud cost."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	budget "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/budget"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_budget"})

    		ubx.Data(budget.Budget, "example", budget.BudgetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Budget } from "./local-sdk/datadog/sdk/typescript/datadog/data/budget/budget.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_budget" });

      data(Budget, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.budget import Budget, BudgetConfig

    def describe():
        ubx.intent("look up datadog_budget")

        ubx.data(Budget, "example", BudgetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The `BudgetArray` `data`. Returned by the lookup.
</ResponseField>
