---
title: "datadog_google_chat_delegated_user_response"
description: "`datadog_google_chat_delegated_user_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_google_chat_delegated_user_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	google "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/google"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_google_chat_delegated_user_response"})

    		ubx.Data(google.ChatDelegatedUserResponse, "example", google.ChatDelegatedUserResponseConfig{
    			OrganizationBindingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChatDelegatedUserResponse } from "@ubx/sdk-datadog/datadog/data/google/chat_delegated_user_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_google_chat_delegated_user_response" });

      data(ChatDelegatedUserResponse, "example", {
        organizationBindingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.google import ChatDelegatedUserResponse, ChatDelegatedUserResponseConfig

    def describe():
        ubx.intent("look up datadog_google_chat_delegated_user_response")

        ubx.data(ChatDelegatedUserResponse, "example", ChatDelegatedUserResponseConfig(
            organization_binding_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_binding_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Google Chat delegated user data from a response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Google Chat delegated user attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="display_name" type="string">
          The delegated user's display name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="email" type="string">
          The delegated user's email address. Optional lookup argument.
        </ResponseField>
        <ResponseField name="features" type="list(string)">
          The list of features enabled for the delegated user. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The ID of the delegated user. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Google Chat delegated user resource type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
