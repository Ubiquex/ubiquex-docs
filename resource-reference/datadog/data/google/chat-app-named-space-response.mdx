---
title: "datadog_google_chat_app_named_space_response"
description: "`datadog_google_chat_app_named_space_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_google_chat_app_named_space_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	google "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/google"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_google_chat_app_named_space_response"})

    		ubx.Data(google.ChatAppNamedSpaceResponse, "example", google.ChatAppNamedSpaceResponseConfig{
    			DomainName:       "example-domain-name",
    			SpaceDisplayName: "example-space-display-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ChatAppNamedSpaceResponse } from "@ubx/sdk-datadog/datadog/data/google/chat_app_named_space_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_google_chat_app_named_space_response" });

      data(ChatAppNamedSpaceResponse, "example", {
        domainName: "example-domain-name",
        spaceDisplayName: "example-space-display-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.google import ChatAppNamedSpaceResponse, ChatAppNamedSpaceResponseConfig

    def describe():
        ubx.intent("look up datadog_google_chat_app_named_space_response")

        ubx.data(ChatAppNamedSpaceResponse, "example", ChatAppNamedSpaceResponseConfig(
            domain_name="example-domain-name",
            space_display_name="example-space-display-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="domain_name" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="space_display_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Google Chat space data from a response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Google Chat space attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="display_name" type="string">
          Google space display name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="organization_binding_id" type="string">
          Organization binding ID. Optional lookup argument.
        </ResponseField>
        <ResponseField name="resource_name" type="string">
          Google space resource name. Optional lookup argument.
        </ResponseField>
        <ResponseField name="space_uri" type="string">
          Google space URI. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The ID of the Google Chat space. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Google Chat space resource type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
