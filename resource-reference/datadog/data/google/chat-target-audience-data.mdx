---
title: "datadog_google_chat_target_audience_data"
description: "`datadog_google_chat_target_audience_data` reads the real target audience (Chat spaces/users) configured for a Google Chat notification."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	google "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/google"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_google_chat_target_audience_data"})

    		ubx.Data(google.ChatTargetAudienceData, "example", google.ChatTargetAudienceDataConfig{
    			OrganizationBindingId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ChatTargetAudienceData } from "./local-sdk/datadog/sdk/typescript/datadog/data/google/chat_target_audience_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_google_chat_target_audience_data" });

      data(ChatTargetAudienceData, "example", {
        organizationBindingId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.google import ChatTargetAudienceData, ChatTargetAudienceDataConfig

    def describe():
        ubx.intent("look up datadog_google_chat_target_audience_data")

        ubx.data(ChatTargetAudienceData, "example", ChatTargetAudienceDataConfig(
            organization_binding_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="organization_binding_id" type="string" required>
  The real, unique identifier of this organization binding. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of Google Chat target audiences. Returned by the lookup.
</ResponseField>
