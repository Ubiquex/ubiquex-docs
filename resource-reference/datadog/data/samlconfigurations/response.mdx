---
title: "datadog_samlconfigurations_response"
description: "`datadog_samlconfigurations_response` reads the real SAML single sign-on configuration for the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	samlconfigurations "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/samlconfigurations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_samlconfigurations_response"})

    		ubx.Data(samlconfigurations.Response, "example", samlconfigurations.ResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Response } from "./local-sdk/datadog/sdk/typescript/datadog/data/samlconfigurations/response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_samlconfigurations_response" });

      data(Response, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.samlconfigurations import Response, ResponseConfig

    def describe():
        ubx.intent("look up datadog_samlconfigurations_response")

        ubx.data(Response, "example", ResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of SAML configurations. An organization has at most one SAML configuration. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Resources related to the SAML configurations, such as the default roles. Returned by the lookup.
</ResponseField>
