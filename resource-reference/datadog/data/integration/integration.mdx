---
title: "datadog_integration"
description: "`datadog_integration` looks up a real, installed integration on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	integration "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/integration"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_integration"})

    		ubx.Data(integration.Integration, "example", integration.IntegrationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Integration } from "@ubx/sdk-datadog/datadog/data/integration/integration";

    export default stack("example", () => {
      intent({ summary: "look up datadog_integration" });

      data(Integration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.integration import Integration, IntegrationConfig

    def describe():
        ubx.intent("look up datadog_integration")

        ubx.data(Integration, "example", IntegrationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of integration objects. Returned by the lookup.
</ResponseField>
