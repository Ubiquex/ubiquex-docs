---
title: "datadog_validate_apikey_response"
description: "`datadog_validate_apikey_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_validate_apikey_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	validate "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/validate"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_validate_apikey_response"})

    		ubx.Data(validate.ApikeyResponse, "example", validate.ApikeyResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApikeyResponse } from "@ubx/sdk-datadog/datadog/data/validate/apikey_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_validate_apikey_response" });

      data(ApikeyResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.validate import ApikeyResponse, ApikeyResponseConfig

    def describe():
        ubx.intent("look up datadog_validate_apikey_response")

        ubx.data(ApikeyResponse, "example", ApikeyResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="status" type="string">
  Status of the validation. Always `ok` when both the API key and the application key are valid. Returned by the lookup.
</ResponseField>
