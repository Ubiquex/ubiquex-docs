---
title: "datadog_validate_v2_response"
description: "`datadog_validate_v2_response` reads the real result of validating a request against the v2 API."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	validate "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/validate"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_validate_v2_response"})

    		ubx.Data(validate.V2Response, "example", validate.V2ResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { V2Response } from "@ubx/sdk-datadog/datadog/data/validate/v2_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_validate_v2_response" });

      data(V2Response, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.validate import V2Response, V2ResponseConfig

    def describe():
        ubx.intent("look up datadog_validate_v2_response")

        ubx.data(V2Response, "example", V2ResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  Data object containing the API key validation result. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the API key validation response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="api_key_id" type="string" required>
          The UUID of the API key. Required lookup argument.
        </ResponseField>
        <ResponseField name="api_key_scopes" type="list(string)" required>
          List of scope names associated with the API key. Required lookup argument.
        </ResponseField>
        <ResponseField name="valid" type="bool" required>
          Whether the API key is valid. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The UUID of the organization associated with the API key. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type for the API key validation response. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
