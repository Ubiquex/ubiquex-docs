---
title: "datadog_schedule_on_call_responders_included"
description: "`datadog_schedule_on_call_responders_included` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_schedule_on_call_responders_included` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	schedule "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/schedule"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_schedule_on_call_responders_included"})

    		ubx.Data(schedule.OnCallRespondersIncluded, "example", schedule.OnCallRespondersIncludedConfig{
    			ScheduleId: "example",
    			Include:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OnCallRespondersIncluded } from "@ubx/sdk-datadog/datadog/data/schedule/on_call_responders_included";

    export default stack("example", () => {
      intent({ summary: "look up datadog_schedule_on_call_responders_included" });

      data(OnCallRespondersIncluded, "example", {
        scheduleId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.schedule import OnCallRespondersIncluded, OnCallRespondersIncludedConfig

    def describe():
        ubx.intent("look up datadog_schedule_on_call_responders_included")

        ubx.data(OnCallRespondersIncluded, "example", OnCallRespondersIncludedConfig(
            schedule_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="schedule_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The main data object representing a schedule's on-call responders lookup, including relationships and metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes for a schedule's on-call responders lookup. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="scheduled_at" type="string">
          The timestamp the responders were resolved at. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Unique identifier of this on-call responders lookup. Optional lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships for a schedule's on-call responders lookup, including the schedule and its responder groups. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="responders" type="object">
          Defines the list of per-position (previous, current, next) responder groups for the schedule. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              Array of references to the responder groups included in the response. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="schedule" type="object">
          Defines the relationship to the schedule this on-call responders lookup was performed for. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object">
              Represents a reference to the schedule this on-call responders lookup was performed for. Optional lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Represents the resource type for a schedule's grouped on-call responders across the previous, current, and next positions. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="included" type="list(object)">
  Related resources referenced in the responder groups' relationships, such as shifts, schedules, and users. Returned by the lookup.
</ResponseField>
