---
title: "datadog_auth_nmappings_response"
description: "`datadog_auth_nmappings_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_auth_nmappings_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auth "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/auth"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_auth_nmappings_response"})

    		ubx.Data(auth.NmappingsResponse, "example", auth.NmappingsResponseConfig{
    			Filter:       "example",
    			ResourceType: "example",
    			Sort:         "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NmappingsResponse } from "@ubx/sdk-datadog/datadog/data/auth/nmappings_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_auth_nmappings_response" });

      data(NmappingsResponse, "example", {
        filter: "example",
        resourceType: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.auth import NmappingsResponse, NmappingsResponseConfig

    def describe():
        ubx.intent("look up datadog_auth_nmappings_response")

        ubx.data(NmappingsResponse, "example", NmappingsResponseConfig(
            filter="example",
            resource_type="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="resource_type" type="string">
  The type of resource being mapped to. Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Sorting options for AuthN Mappings. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of returned AuthN Mappings. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Included data in the AuthN Mapping response. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Object describing meta attributes of response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination object. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total count. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total count of elements matched by the filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
