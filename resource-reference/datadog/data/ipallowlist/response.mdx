---
title: "datadog_ipallowlist_response"
description: "`datadog_ipallowlist_response` reads the real IP addresses allow-listed for accessing the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ipallowlist "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/ipallowlist"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_ipallowlist_response"})

    		ubx.Data(ipallowlist.Response, "example", ipallowlist.ResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Response } from "@ubx/sdk-datadog/datadog/data/ipallowlist/response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_ipallowlist_response" });

      data(Response, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.ipallowlist import Response, ResponseConfig

    def describe():
        ubx.intent("look up datadog_ipallowlist_response")

        ubx.data(Response, "example", ResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  IP allowlist data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of the IP allowlist. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="enabled" type="bool">
          Whether the IP allowlist logic is enabled or not. Optional lookup argument.
        </ResponseField>
        <ResponseField name="entries" type="list(object)">
          Array of entries in the IP allowlist. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The unique identifier of the org. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      IP allowlist type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
