---
title: "datadog_twilio_integration_account_response_data"
description: "`datadog_twilio_integration_account_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_twilio_integration_account_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	twilio "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/twilio"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_twilio_integration_account_response_data"})

    		ubx.Data(twilio.IntegrationAccountResponseData, "example", twilio.IntegrationAccountResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IntegrationAccountResponseData } from "@ubx/sdk-datadog/datadog/data/twilio/integration_account_response_data";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_twilio_integration_account_response_data",
      });

      data(IntegrationAccountResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.twilio import IntegrationAccountResponseData, IntegrationAccountResponseDataConfig

    def describe():
        ubx.intent("look up datadog_twilio_integration_account_response_data")

        ubx.data(IntegrationAccountResponseData, "example", IntegrationAccountResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of Twilio integration accounts. Returned by the lookup.
</ResponseField>
