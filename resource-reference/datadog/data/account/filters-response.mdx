---
title: "datadog_account_filters_response"
description: "`datadog_account_filters_response` reads the real, available filters for scoping a query to specific cloud accounts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	account "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/account"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_account_filters_response"})

    		ubx.Data(account.FiltersResponse, "example", account.FiltersResponseConfig{
    			CloudAccountId: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FiltersResponse } from "@ubx/sdk-datadog/datadog/data/account/filters_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_account_filters_response" });

      data(FiltersResponse, "example", {
        cloudAccountId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.account import FiltersResponse, FiltersResponseConfig

    def describe():
        ubx.intent("look up datadog_account_filters_response")

        ubx.data(FiltersResponse, "example", FiltersResponseConfig(
            cloud_account_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="cloud_account_id" type="number" required>
  The real, unique identifier of this cloud account. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The account filters for a cloud account. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for the account filters of a cloud account. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="account_filters" type="object">
          The account filtering configuration. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="excluded_accounts" type="list(string)">
              The AWS account IDs to be excluded from your billing dataset. This field is used when `include_new_accounts` is `true`. Optional lookup argument.
            </ResponseField>
            <ResponseField name="include_new_accounts" type="bool">
              Whether or not to automatically include new member accounts by default in your billing dataset. Optional lookup argument.
            </ResponseField>
            <ResponseField name="included_accounts" type="list(string)">
              The AWS account IDs to be included in your billing dataset. This field is used when `include_new_accounts` is `false`. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="account_id" type="string">
          The cloud account ID. Optional lookup argument.
        </ResponseField>
        <ResponseField name="cloud" type="string">
          The cloud provider of the account, for example `aws`, `aws_cur2`, or `oci`. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The ID of the cloud account. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of account filters. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
