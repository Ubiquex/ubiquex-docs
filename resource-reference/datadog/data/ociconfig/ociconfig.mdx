---
title: "datadog_ociconfig"
description: "`datadog_ociconfig` looks up a real Oracle Cloud Infrastructure (OCI) integration configuration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	ociconfig "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/ociconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_ociconfig"})

    		ubx.Data(ociconfig.Ociconfig, "example", ociconfig.OciconfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Ociconfig } from "./local-sdk/datadog/sdk/typescript/datadog/data/ociconfig/ociconfig.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_ociconfig" });

      data(Ociconfig, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.ociconfig import Ociconfig, OciconfigConfig

    def describe():
        ubx.intent("look up datadog_ociconfig")

        ubx.data(Ociconfig, "example", OciconfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An OCI config. Returned by the lookup.
</ResponseField>
