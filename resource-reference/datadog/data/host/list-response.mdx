---
title: "datadog_host_list_response"
description: "`datadog_host_list_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_host_list_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	host "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/host"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_host_list_response"})

    		ubx.Data(host.ListResponse, "example", host.ListResponseConfig{
    			Count:                 1,
    			Filter:                "example",
    			From:                  1,
    			IncludeHostsMetadata:  true,
    			IncludeMutedHostsData: true,
    			SortDir:               "example",
    			SortField:             "example",
    			Start:                 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListResponse } from "@ubx/sdk-datadog/datadog/data/host/list_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_host_list_response" });

      data(ListResponse, "example", {
        count: 1,
        filter: "example",
        from: 1,
        includeHostsMetadata: true,
        includeMutedHostsData: true,
        sortDir: "example",
        sortField: "example",
        start: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.host import ListResponse, ListResponseConfig

    def describe():
        ubx.intent("look up datadog_host_list_response")

        ubx.data(ListResponse, "example", ListResponseConfig(
            count=1,
            filter="example",
            from_=1,
            include_hosts_metadata=True,
            include_muted_hosts_data=True,
            sort_dir="example",
            sort_field="example",
            start=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="count" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="from" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_hosts_metadata" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="include_muted_hosts_data" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_dir" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_field" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="start" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="host_list" type="list(object)">
  Array of hosts. Returned by the lookup.
</ResponseField>

<ResponseField name="total_matching" type="number">
  Number of host matching the query. Returned by the lookup.
</ResponseField>

<ResponseField name="total_returned" type="number">
  Number of host returned. Returned by the lookup.
</ResponseField>
