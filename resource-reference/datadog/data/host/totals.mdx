---
title: "datadog_host_totals"
description: "`datadog_host_totals` reads real, aggregated host counts across the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	host "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/host"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_host_totals"})

    		ubx.Data(host.Totals, "example", host.TotalsConfig{
    			From: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Totals } from "./local-sdk/datadog/sdk/typescript/datadog/data/host/totals.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_host_totals" });

      data(Totals, "example", {
        from: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.host import Totals, TotalsConfig

    def describe():
        ubx.intent("look up datadog_host_totals")

        ubx.data(Totals, "example", TotalsConfig(
            from_=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from" type="number">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="total_active" type="number">
  Total number of active host (UP and ???) reporting to Datadog. Returned by the lookup.
</ResponseField>

<ResponseField name="total_up" type="number">
  Number of host that are UP and reporting to Datadog. Returned by the lookup.
</ResponseField>
