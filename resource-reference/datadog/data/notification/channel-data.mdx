---
title: "datadog_notification_channel_data"
description: "`datadog_notification_channel_data` looks up a real notification channel configured for delivering alerts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	notification "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/notification"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_notification_channel_data"})

    		ubx.Data(notification.ChannelData, "example", notification.ChannelDataConfig{
    			UserId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ChannelData } from "./local-sdk/datadog/sdk/typescript/datadog/data/notification/channel_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_notification_channel_data" });

      data(ChannelData, "example", {
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.notification import ChannelData, ChannelDataConfig

    def describe():
        ubx.intent("look up datadog_notification_channel_data")

        ubx.data(ChannelData, "example", ChannelDataConfig(
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of this user. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of notification channel data objects. Returned by the lookup.
</ResponseField>
