---
title: "datadog_notification_rule"
description: "`datadog_notification_rule` looks up a real rule controlling how a specific Datadog Team is notified -- which channel that team's own alerts route to..."
---

`datadog_notification_rule` looks up a real rule controlling how a specific Datadog Team is notified -- which channel that team's own alerts route to, scoped to the team rather than to a single monitor.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	notification "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/notification"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_notification_rule"})

    		ubx.Data(notification.Rule, "example", notification.RuleConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Rule } from "./local-sdk/datadog/sdk/typescript/datadog/data/notification/rule.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_notification_rule" });

      data(Rule, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.notification import Rule, RuleConfig

    def describe():
        ubx.intent("look up datadog_notification_rule")

        ubx.data(Rule, "example", RuleConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The real, primary payload of this response. **(AI-inferred)** Returned by the lookup.
</ResponseField>
