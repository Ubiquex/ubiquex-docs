---
title: "datadog_notification_rule_response"
description: "`datadog_notification_rule_response` looks up a real rule controlling how a specific Datadog Team is notified -- which channel that team's own alerts..."
---

`datadog_notification_rule_response` looks up a real rule controlling how a specific Datadog Team is notified -- which channel that team's own alerts route to, scoped to the team rather than to a single monitor.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	notification "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/notification"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_notification_rule_response"})

    		ubx.Data(notification.RuleResponse, "example", notification.RuleResponseConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { RuleResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/notification/rule_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_notification_rule_response" });

      data(RuleResponse, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.notification import RuleResponse, RuleResponseConfig

    def describe():
        ubx.intent("look up datadog_notification_rule_response")

        ubx.data(RuleResponse, "example", RuleResponseConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Notification rules allow full control over notifications generated by the various Datadog security products. They allow users to define the conditions under which a notification should be generated (based on rule severities, rule types, rule tags, and so on), and the targets to notify. A notification rule is composed of a rule ID, a rule type, and the rule attributes. All fields are required. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the notification rule. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="number" required>
          Date as Unix timestamp in milliseconds. Required lookup argument.
        </ResponseField>
        <ResponseField name="created_by" type="object" required>
          User creating or modifying a rule. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="handle" type="string">
              The user handle. Optional lookup argument.
            </ResponseField>
            <ResponseField name="name" type="string">
              The user name. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool" required>
          Field used to enable or disable the rule. Required lookup argument.
        </ResponseField>
        <ResponseField name="modified_at" type="number" required>
          Date as Unix timestamp in milliseconds. Required lookup argument.
        </ResponseField>
        <ResponseField name="modified_by" type="object" required>
          User creating or modifying a rule. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="handle" type="string">
              The user handle. Optional lookup argument.
            </ResponseField>
            <ResponseField name="name" type="string">
              The user name. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Name of the notification rule. Required lookup argument.
        </ResponseField>
        <ResponseField name="selectors" type="object" required>
          Selectors are used to filter security issues for which notifications should be generated. Users can specify rule severities, rule types, a query to filter security issues on tags and attributes, and the trigger source. Only the trigger_source field is required. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="query" type="string">
              The query is composed of one or several key:value pairs, which can be used to filter security issues on tags and attributes. Optional lookup argument.
            </ResponseField>
            <ResponseField name="rule_types" type="list(string)">
              Security rule types used as filters in security rules. Optional lookup argument.
            </ResponseField>
            <ResponseField name="severities" type="list(string)">
              The security rules severities to consider. Optional lookup argument.
            </ResponseField>
            <ResponseField name="trigger_source" type="string" required>
              The type of security issues on which the rule applies. Notification rules based on security signals need to use the trigger source "security_signals", while notification rules based on security vulnerabilities need to use the trigger source "security_findings". Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="targets" type="list(string)" required>
          List of recipients to notify when a notification rule is triggered. Many different target types are supported, such as email addresses, Slack channels, and PagerDuty services. The appropriate integrations need to be properly configured to send notifications to the specified targets. Required lookup argument.
        </ResponseField>
        <ResponseField name="time_aggregation" type="number">
          Time aggregation period (in seconds) is used to aggregate the results of the notification rule evaluation. Results are aggregated over a selected time frame using a rolling window, which updates with each new evaluation. Notifications are only sent for new issues discovered during the window. Time aggregation is only available for vulnerability-based notification rules. When omitted or set to 0, no aggregation is done. Optional lookup argument.
        </ResponseField>
        <ResponseField name="version" type="number" required>
          Version of the notification rule. It is updated when the rule is modified. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The ID of a notification rule. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The rule type associated to notification rules. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
