---
title: "datadog_llmobs_patterns_clustered_points_response"
description: "`datadog_llmobs_patterns_clustered_points_response` reads the real data points LLM Observability's automatic clustering grouped together as one pattern."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_patterns_clustered_points_response"})

    		ubx.Data(llmobs.PatternsClusteredPointsResponse, "example", llmobs.PatternsClusteredPointsResponseConfig{
    			TopicId:   "example",
    			PageSize:  1,
    			PageToken: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { PatternsClusteredPointsResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/llmobs/patterns_clustered_points_response.ts";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_llmobs_patterns_clustered_points_response",
      });

      data(PatternsClusteredPointsResponse, "example", {
        topicId: "example",
        pageSize: 1,
        pageToken: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import PatternsClusteredPointsResponse, PatternsClusteredPointsResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_patterns_clustered_points_response")

        ubx.data(PatternsClusteredPointsResponse, "example", PatternsClusteredPointsResponseConfig(
            topic_id="example",
            page_size=1,
            page_token="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_token" type="string">
  An opaque pagination token from a previous response, requesting the next page of results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="topic_id" type="string" required>
  The real, unique identifier of this topic. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object of an Agent Observability patterns clustered points response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an Agent Observability patterns clustered points response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="next_page_token" type="string" required>
          Pagination token for the next page of points. Null if there are no more pages. Required lookup argument.
        </ResponseField>
        <ResponseField name="points" type="list(object)" required>
          List of clustered points. Required lookup argument.
        </ResponseField>
        <ResponseField name="topic_id" type="string" required>
          Identifier of the topic the points belong to. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Identifier of the topic the points belong to. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of an Agent Observability patterns clustered points response. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
