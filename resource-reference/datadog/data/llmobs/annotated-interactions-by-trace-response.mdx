---
title: "datadog_llmobs_annotated_interactions_by_trace_response"
description: "`datadog_llmobs_annotated_interactions_by_trace_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_annotated_interactions_by_trace_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_annotated_interactions_by_trace_response"})

    		ubx.Data(llmobs.AnnotatedInteractionsByTraceResponse, "example", llmobs.AnnotatedInteractionsByTraceResponseConfig{
    			ContentIds: []string{"example"},
    			Limit:      1,
    			Offset:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AnnotatedInteractionsByTraceResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/annotated_interactions_by_trace_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_llmobs_annotated_interactions_by_trace_response",
      });

      data(AnnotatedInteractionsByTraceResponse, "example", {
        contentIds: ["example"],
        limit: 1,
        offset: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import AnnotatedInteractionsByTraceResponse, AnnotatedInteractionsByTraceResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_annotated_interactions_by_trace_response")

        ubx.data(AnnotatedInteractionsByTraceResponse, "example", AnnotatedInteractionsByTraceResponseConfig(
            content_ids=["example"],
            limit=1,
            offset=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="content_ids" type="list(string)" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offset" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object for the cross-queue annotated interactions response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the cross-queue annotated interactions response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="annotated_interactions" type="list(object)" required>
          List of annotated interactions across all queues for the requested content IDs. Required lookup argument.
        </ResponseField>
        <ResponseField name="total_count" type="number" required>
          Total number of annotated interactions matching the query. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Opaque identifier for the response object. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type for cross-queue annotated interactions lookup. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
