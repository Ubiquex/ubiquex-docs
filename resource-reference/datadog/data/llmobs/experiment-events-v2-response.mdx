---
title: "datadog_llmobs_experiment_events_v2_response"
description: "`datadog_llmobs_experiment_events_v2_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_experiment_events_v2_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_experiment_events_v2_response"})

    		ubx.Data(llmobs.ExperimentEventsV2Response, "example", llmobs.ExperimentEventsV2ResponseConfig{
    			ExperimentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ExperimentEventsV2Response } from "@ubx/sdk-datadog/datadog/data/llmobs/experiment_events_v2_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_experiment_events_v2_response" });

      data(ExperimentEventsV2Response, "example", {
        experimentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import ExperimentEventsV2Response, ExperimentEventsV2ResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_experiment_events_v2_response")

        ubx.data(ExperimentEventsV2Response, "example", ExperimentEventsV2ResponseConfig(
            experiment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="experiment_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  JSON:API data object for an experiment events response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an experiment events response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="spans" type="list(object)" required>
          Experiment spans, each enriched with their associated evaluation metrics. Required lookup argument.
        </ResponseField>
        <ResponseField name="summary_metrics" type="list(object)" required>
          Experiment-level summary evaluation metrics (not tied to individual spans). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Identifier for this events resource. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type for an experiment events collection. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination cursor metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="after" type="string">
      Cursor for the next page of results. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
