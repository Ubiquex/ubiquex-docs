---
title: "datadog_llmobs_project_data_response"
description: "`datadog_llmobs_project_data_response` looks up a real LLM Observability project -- the top-level grouping for an application's own traces..."
---

`datadog_llmobs_project_data_response` looks up a real LLM Observability project -- the top-level grouping for an application's own traces, experiments, and datasets.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_project_data_response"})

    		ubx.Data(llmobs.ProjectDataResponse, "example", llmobs.ProjectDataResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ProjectDataResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/llmobs/project_data_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_project_data_response" });

      data(ProjectDataResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import ProjectDataResponse, ProjectDataResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_project_data_response")

        ubx.data(ProjectDataResponse, "example", ProjectDataResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of projects. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination cursor metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="after" type="string">
      Cursor for the next page of results. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
