---
title: "datadog_llmobs_patterns_run_status_response"
description: "`datadog_llmobs_patterns_run_status_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_patterns_run_status_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_patterns_run_status_response"})

    		ubx.Data(llmobs.PatternsRunStatusResponse, "example", llmobs.PatternsRunStatusResponseConfig{
    			ConfigId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatternsRunStatusResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/patterns_run_status_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_patterns_run_status_response" });

      data(PatternsRunStatusResponse, "example", {
        configId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import PatternsRunStatusResponse, PatternsRunStatusResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_patterns_run_status_response")

        ubx.data(PatternsRunStatusResponse, "example", PatternsRunStatusResponseConfig(
            config_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object of an Agent Observability patterns run status response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an Agent Observability patterns run status. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="string" required>
          Timestamp when the run was created. Required lookup argument.
        </ResponseField>
        <ResponseField name="progress" type="list(object)" required>
          List of step-by-step progress entries for a patterns run. Required lookup argument.
        </ResponseField>
        <ResponseField name="status" type="string" required>
          Overall status of the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="step" type="string" required>
          The current step of the run. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The ID of the patterns run. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of an Agent Observability patterns run status. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
