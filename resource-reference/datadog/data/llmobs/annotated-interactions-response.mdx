---
title: "datadog_llmobs_annotated_interactions_response"
description: "`datadog_llmobs_annotated_interactions_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_annotated_interactions_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_annotated_interactions_response"})

    		ubx.Data(llmobs.AnnotatedInteractionsResponse, "example", llmobs.AnnotatedInteractionsResponseConfig{
    			QueueId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AnnotatedInteractionsResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/annotated_interactions_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_annotated_interactions_response" });

      data(AnnotatedInteractionsResponse, "example", {
        queueId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import AnnotatedInteractionsResponse, AnnotatedInteractionsResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_annotated_interactions_response")

        ubx.data(AnnotatedInteractionsResponse, "example", AnnotatedInteractionsResponseConfig(
            queue_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="queue_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object for annotated interactions. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes containing the list of annotated interactions. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="annotated_interactions" type="list(object)" required>
          List of interactions with their annotations. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The annotation queue ID. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type for annotated interactions. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
