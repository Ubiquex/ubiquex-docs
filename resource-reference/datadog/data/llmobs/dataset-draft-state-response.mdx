---
title: "datadog_llmobs_dataset_draft_state_response"
description: "`datadog_llmobs_dataset_draft_state_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_dataset_draft_state_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_dataset_draft_state_response"})

    		ubx.Data(llmobs.DatasetDraftStateResponse, "example", llmobs.DatasetDraftStateResponseConfig{
    			DatasetId: "example",
    			ProjectId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatasetDraftStateResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/dataset_draft_state_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_dataset_draft_state_response" });

      data(DatasetDraftStateResponse, "example", {
        datasetId: "example",
        projectId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import DatasetDraftStateResponse, DatasetDraftStateResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_dataset_draft_state_response")

        ubx.data(DatasetDraftStateResponse, "example", DatasetDraftStateResponseConfig(
            dataset_id="example",
            project_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="project_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object for an Agent Observability dataset draft state. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an Agent Observability dataset draft state. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="drafting_since" type="string" required>
          Timestamp when the dataset draft session started. Required lookup argument.
        </ResponseField>
        <ResponseField name="user" type="object" required>
          User information associated with a dataset draft state. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="email" type="string">
              Email address of the user. Optional lookup argument.
            </ResponseField>
            <ResponseField name="handle" type="string">
              Handle of the user. Optional lookup argument.
            </ResponseField>
            <ResponseField name="icon" type="string">
              Icon for the user. Optional lookup argument.
            </ResponseField>
            <ResponseField name="id" type="string" required>
              Unique identifier of the user holding the draft lock. Required lookup argument.
            </ResponseField>
            <ResponseField name="name" type="string">
              Display name of the user. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier of the dataset draft state. Matches the dataset ID. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of an Agent Observability dataset draft state. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
