---
title: "datadog_llmobs_annotation_queue_label_schema_response"
description: "`datadog_llmobs_annotation_queue_label_schema_response` reads the real label schema (the set of labels reviewers can apply) configured for..."
---

`datadog_llmobs_annotation_queue_label_schema_response` reads the real label schema (the set of labels reviewers can apply) configured for an annotation queue.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_annotation_queue_label_schema_response"})

    		ubx.Data(llmobs.AnnotationQueueLabelSchemaResponse, "example", llmobs.AnnotationQueueLabelSchemaResponseConfig{
    			QueueId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AnnotationQueueLabelSchemaResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/llmobs/annotation_queue_label_schema_response.ts";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_llmobs_annotation_queue_label_schema_response",
      });

      data(AnnotationQueueLabelSchemaResponse, "example", {
        queueId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import AnnotationQueueLabelSchemaResponse, AnnotationQueueLabelSchemaResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_annotation_queue_label_schema_response")

        ubx.data(AnnotationQueueLabelSchemaResponse, "example", AnnotationQueueLabelSchemaResponseConfig(
            queue_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="queue_id" type="string" required>
  The real, unique identifier of this queue. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object for an annotation queue label schema. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an annotation queue label schema. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="annotation_schema" type="object" required>
          Schema defining the labels for an annotation queue. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="label_schemas" type="list(object)" required>
              List of label schema definitions. Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier of the annotation queue. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of an Agent Observability annotation queue. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
