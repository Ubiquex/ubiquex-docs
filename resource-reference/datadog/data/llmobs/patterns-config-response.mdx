---
title: "datadog_llmobs_patterns_config_response"
description: "`datadog_llmobs_patterns_config_response` reads the real configuration controlling how LLM Observability clusters traces into patterns and topics."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_patterns_config_response"})

    		ubx.Data(llmobs.PatternsConfigResponse, "example", llmobs.PatternsConfigResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatternsConfigResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/patterns_config_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_patterns_config_response" });

      data(PatternsConfigResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import PatternsConfigResponse, PatternsConfigResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_patterns_config_response")

        ubx.data(PatternsConfigResponse, "example", PatternsConfigResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  Data object of an Agent Observability patterns configuration. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an Agent Observability patterns configuration. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="account_id" type="string">
          Integration account ID for a bring-your-own-model configuration. Optional lookup argument.
        </ResponseField>
        <ResponseField name="created_at" type="string" required>
          Timestamp when the configuration was created. Required lookup argument.
        </ResponseField>
        <ResponseField name="evp_query" type="string" required>
          Query that selects the spans the patterns run analyzes. Required lookup argument.
        </ResponseField>
        <ResponseField name="hierarchy_depth" type="number" required>
          Depth of the topic hierarchy to generate. Required lookup argument.
        </ResponseField>
        <ResponseField name="integration_provider" type="string">
          Integration provider for a bring-your-own-model configuration. Optional lookup argument.
        </ResponseField>
        <ResponseField name="model_name" type="string">
          Model name for a bring-your-own-model configuration. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Name of the configuration. Required lookup argument.
        </ResponseField>
        <ResponseField name="num_records" type="number" required>
          Maximum number of records to process for the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="sampling_ratio" type="number" required>
          Fraction of matching spans to sample for the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="scope" type="string" required>
          Scope of the configuration. Required lookup argument.
        </ResponseField>
        <ResponseField name="template" type="string">
          Template used to guide topic generation. Optional lookup argument.
        </ResponseField>
        <ResponseField name="updated_at" type="string" required>
          Timestamp when the configuration was last updated. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier of the configuration. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of an Agent Observability patterns configuration. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
