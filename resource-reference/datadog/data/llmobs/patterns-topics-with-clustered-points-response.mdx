---
title: "datadog_llmobs_patterns_topics_with_clustered_points_response"
description: "`datadog_llmobs_patterns_topics_with_clustered_points_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_patterns_topics_with_clustered_points_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_patterns_topics_with_clustered_points_response"})

    		ubx.Data(llmobs.PatternsTopicsWithClusteredPointsResponse, "example", llmobs.PatternsTopicsWithClusteredPointsResponseConfig{
    			ConfigId:       "example",
    			IncludeMetrics: true,
    			RunId:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PatternsTopicsWithClusteredPointsResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/patterns_topics_with_clustered_points_response";

    export default stack("example", () => {
      intent({
        summary:
          "look up datadog_llmobs_patterns_topics_with_clustered_points_response",
      });

      data(PatternsTopicsWithClusteredPointsResponse, "example", {
        configId: "example",
        includeMetrics: true,
        runId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import PatternsTopicsWithClusteredPointsResponse, PatternsTopicsWithClusteredPointsResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_patterns_topics_with_clustered_points_response")

        ubx.data(PatternsTopicsWithClusteredPointsResponse, "example", PatternsTopicsWithClusteredPointsResponseConfig(
            config_id="example",
            include_metrics=True,
            run_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="include_metrics" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="run_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object of an Agent Observability patterns topics-with-clustered-points response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an Agent Observability patterns topics-with-clustered-points response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="completed_at" type="string">
          Timestamp when the run completed. Null if the run has not completed. Optional lookup argument.
        </ResponseField>
        <ResponseField name="config_id" type="string" required>
          Identifier of the configuration that produced the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="config_snapshot" type="object">
          Snapshot of the configuration used for a patterns run. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="account_id" type="string">
              Integration account ID used for a bring-your-own-model run. Optional lookup argument.
            </ResponseField>
            <ResponseField name="evp_query" type="string">
              Query that selected the spans for the run. Optional lookup argument.
            </ResponseField>
            <ResponseField name="hierarchy_depth" type="number">
              Depth of the topic hierarchy generated. Optional lookup argument.
            </ResponseField>
            <ResponseField name="integration_provider" type="string">
              Integration provider used for a bring-your-own-model run. Optional lookup argument.
            </ResponseField>
            <ResponseField name="model_name" type="string">
              Model name used for a bring-your-own-model run. Optional lookup argument.
            </ResponseField>
            <ResponseField name="num_records" type="number">
              Maximum number of records processed for the run. Optional lookup argument.
            </ResponseField>
            <ResponseField name="sampling_ratio" type="number">
              Fraction of matching spans sampled for the run. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="created_at" type="string" required>
          Timestamp when the run was created. Required lookup argument.
        </ResponseField>
        <ResponseField name="previous_run_id" type="string" required>
          Identifier of the run that completed immediately before this one. Empty if none. Required lookup argument.
        </ResponseField>
        <ResponseField name="run_id" type="string" required>
          Identifier of the run that produced the topics. Required lookup argument.
        </ResponseField>
        <ResponseField name="topics" type="list(object)" required>
          List of discovered topics with their clustered points. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Identifier of the run the topics belong to. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of an Agent Observability patterns topics-with-clustered-points response. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
