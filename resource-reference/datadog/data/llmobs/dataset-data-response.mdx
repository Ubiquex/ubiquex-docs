---
title: "datadog_llmobs_dataset_data_response"
description: "`datadog_llmobs_dataset_data_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_llmobs_dataset_data_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_dataset_data_response"})

    		ubx.Data(llmobs.DatasetDataResponse, "example", llmobs.DatasetDataResponseConfig{
    			ProjectId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DatasetDataResponse } from "@ubx/sdk-datadog/datadog/data/llmobs/dataset_data_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_dataset_data_response" });

      data(DatasetDataResponse, "example", {
        projectId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import DatasetDataResponse, DatasetDataResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_dataset_data_response")

        ubx.data(DatasetDataResponse, "example", DatasetDataResponseConfig(
            project_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="project_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of datasets. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination cursor metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="after" type="string">
      Cursor for the next page of results. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
