---
title: "datadog_llmobs_patterns_runs_response"
description: "`datadog_llmobs_patterns_runs_response` lists the real LLM Observability pattern-clustering runs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	llmobs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/llmobs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_llmobs_patterns_runs_response"})

    		ubx.Data(llmobs.PatternsRunsResponse, "example", llmobs.PatternsRunsResponseConfig{
    			ConfigId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { PatternsRunsResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/llmobs/patterns_runs_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_llmobs_patterns_runs_response" });

      data(PatternsRunsResponse, "example", {
        configId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.llmobs import PatternsRunsResponse, PatternsRunsResponseConfig

    def describe():
        ubx.intent("look up datadog_llmobs_patterns_runs_response")

        ubx.data(PatternsRunsResponse, "example", PatternsRunsResponseConfig(
            config_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="config_id" type="string" required>
  The real, unique identifier of this configuration. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object of an Agent Observability patterns runs response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an Agent Observability patterns runs response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="runs" type="list(object)" required>
          List of patterns runs. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Identifier of the configuration the runs belong to. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type of a list of Agent Observability patterns runs. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
