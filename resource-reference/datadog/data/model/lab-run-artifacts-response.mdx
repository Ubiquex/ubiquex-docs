---
title: "datadog_model_lab_run_artifacts_response"
description: "`datadog_model_lab_run_artifacts_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_model_lab_run_artifacts_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	model "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/model"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_model_lab_run_artifacts_response"})

    		ubx.Data(model.LabRunArtifactsResponse, "example", model.LabRunArtifactsResponseConfig{
    			RunId: 1,
    			Path:  "/example/",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LabRunArtifactsResponse } from "@ubx/sdk-datadog/datadog/data/model/lab_run_artifacts_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_model_lab_run_artifacts_response" });

      data(LabRunArtifactsResponse, "example", {
        runId: 1,
        path: "/example/",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.model import LabRunArtifactsResponse, LabRunArtifactsResponseConfig

    def describe():
        ubx.intent("look up datadog_model_lab_run_artifacts_response")

        ubx.data(LabRunArtifactsResponse, "example", LabRunArtifactsResponseConfig(
            run_id=1,
            path="/example/",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="path" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="run_id" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  A run artifacts JSON:API resource object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Artifact listing for a Model Lab run. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="files" type="list(object)" required>
          The list of artifact files and directories. Required lookup argument.
        </ResponseField>
        <ResponseField name="path_in_project" type="string" required>
          The path of the run's artifacts relative to the project's artifact root. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the artifacts resource. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type for a run artifacts resource. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
