---
title: "datadog_model_lab_facet_keys_response"
description: "`datadog_model_lab_facet_keys_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_model_lab_facet_keys_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	model "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/model"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_model_lab_facet_keys_response"})

    		ubx.Data(model.LabFacetKeysResponse, "example", model.LabFacetKeysResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LabFacetKeysResponse } from "@ubx/sdk-datadog/datadog/data/model/lab_facet_keys_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_model_lab_facet_keys_response" });

      data(LabFacetKeysResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.model import LabFacetKeysResponse, LabFacetKeysResponseConfig

    def describe():
        ubx.intent("look up datadog_model_lab_facet_keys_response")

        ubx.data(LabFacetKeysResponse, "example", LabFacetKeysResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  A facet keys JSON:API resource object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Available facet key names for filtering resources. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="metrics" type="list(string)" required>
          The list of available metric facet keys. Required lookup argument.
        </ResponseField>
        <ResponseField name="parameters" type="list(string)" required>
          The list of available parameter facet keys. Required lookup argument.
        </ResponseField>
        <ResponseField name="tags" type="list(string)" required>
          The list of available tag facet keys. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the facet keys resource. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type for a facet keys resource. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
