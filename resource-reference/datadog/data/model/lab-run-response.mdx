---
title: "datadog_model_lab_run_response"
description: "`datadog_model_lab_run_response` looks up a real Model Lab run -- one execution of an experiment within a Model Lab project."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	model "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/model"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_model_lab_run_response"})

    		ubx.Data(model.LabRunResponse, "example", model.LabRunResponseConfig{
    			RunId: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { LabRunResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/model/lab_run_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_model_lab_run_response" });

      data(LabRunResponse, "example", {
        runId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.model import LabRunResponse, LabRunResponseConfig

    def describe():
        ubx.intent("look up datadog_model_lab_run_response")

        ubx.data(LabRunResponse, "example", LabRunResponseConfig(
            run_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="run_id" type="number" required>
  The real, unique identifier of this run. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  A Model Lab run JSON:API resource object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of a Model Lab run. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="completed_at" type="string">
          The date and time the run completed. Optional lookup argument.
        </ResponseField>
        <ResponseField name="created_at" type="string" required>
          The date and time the run was created. Required lookup argument.
        </ResponseField>
        <ResponseField name="deleted_at" type="string">
          The date and time the run was soft-deleted. Optional lookup argument.
        </ResponseField>
        <ResponseField name="descendant_match" type="bool" required>
          Whether a descendant run matched the applied filters. Required lookup argument.
        </ResponseField>
        <ResponseField name="description" type="string" required>
          A description of the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="duration" type="number">
          The duration of the run in seconds. Optional lookup argument.
        </ResponseField>
        <ResponseField name="external_url" type="string">
          An optional external URL associated with the run. Optional lookup argument.
        </ResponseField>
        <ResponseField name="has_children" type="bool" required>
          Whether the run has child runs. Required lookup argument.
        </ResponseField>
        <ResponseField name="is_pinned" type="bool" required>
          Whether the run is pinned by the current user. Required lookup argument.
        </ResponseField>
        <ResponseField name="metric_summaries" type="list(object)" required>
          Summary statistics for metrics recorded during the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="mlflow_artifact_location" type="string" required>
          The MLflow artifact storage location for this run. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="owner_id" type="string">
          The UUID of the run owner. Optional lookup argument.
        </ResponseField>
        <ResponseField name="params" type="list(object)" required>
          The list of parameters used for the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="project_id" type="number" required>
          The ID of the project this run belongs to. Required lookup argument.
        </ResponseField>
        <ResponseField name="started_at" type="string" required>
          The date and time the run started. Required lookup argument.
        </ResponseField>
        <ResponseField name="status" type="string" required>
          The status of a Model Lab run. Required lookup argument.
        </ResponseField>
        <ResponseField name="tags" type="list(object)" required>
          The list of tags associated with the run. Required lookup argument.
        </ResponseField>
        <ResponseField name="updated_at" type="string" required>
          The date and time the run was last updated. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the run. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type for a Model Lab run resource. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
