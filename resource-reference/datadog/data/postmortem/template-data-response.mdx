---
title: "datadog_postmortem_template_data_response"
description: "`datadog_postmortem_template_data_response` looks up a real postmortem template used when documenting an incident after resolution."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	postmortem "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/postmortem"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_postmortem_template_data_response"})

    		ubx.Data(postmortem.TemplateDataResponse, "example", postmortem.TemplateDataResponseConfig{
    			Sort: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TemplateDataResponse } from "@ubx/sdk-datadog/datadog/data/postmortem/template_data_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_postmortem_template_data_response" });

      data(TemplateDataResponse, "example", {
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.postmortem import TemplateDataResponse, TemplateDataResponseConfig

    def describe():
        ubx.intent("look up datadog_postmortem_template_data_response")

        ubx.data(TemplateDataResponse, "example", TemplateDataResponseConfig(
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of postmortem template data objects. Returned by the lookup.
</ResponseField>
