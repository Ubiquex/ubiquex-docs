---
title: "datadog_team_link"
description: "`datadog_team_link` looks up a real, external link attached to a Datadog team, such as a runbook or dashboard URL."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_team_link"})

    		ubx.Data(team.Link, "example", team.LinkConfig{
    			TeamId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Link } from "./local-sdk/datadog/sdk/typescript/datadog/data/team/link.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_team_link" });

      data(Link, "example", {
        teamId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.team import Link, LinkConfig

    def describe():
        ubx.intent("look up datadog_team_link")

        ubx.data(Link, "example", LinkConfig(
            team_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="team_id" type="string" required>
  The real, unique identifier of this team. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Team links response data Returned by the lookup.
</ResponseField>
