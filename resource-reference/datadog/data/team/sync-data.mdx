---
title: "datadog_team_sync_data"
description: "`datadog_team_sync_data` reads the real synchronization status of a Datadog team against its own external identity source (e.g. an IdP group)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_team_sync_data"})

    		ubx.Data(team.SyncData, "example", team.SyncDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SyncData } from "@ubx/sdk-datadog/datadog/data/team/sync_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_team_sync_data" });

      data(SyncData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.team import SyncData, SyncDataConfig

    def describe():
        ubx.intent("look up datadog_team_sync_data")

        ubx.data(SyncData, "example", SyncDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of team sync configurations Returned by the lookup.
</ResponseField>
