---
title: "datadog_team_connection"
description: "`datadog_team_connection` looks up a real link connecting a Datadog team to an external system."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_team_connection"})

    		ubx.Data(team.Connection, "example", team.ConnectionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Connection } from "./local-sdk/datadog/sdk/typescript/datadog/data/team/connection.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_team_connection" });

      data(Connection, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.team import Connection, ConnectionConfig

    def describe():
        ubx.intent("look up datadog_team_connection")

        ubx.data(Connection, "example", ConnectionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of team connections. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Connections response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Page-based pagination metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_number" type="number">
          The first page number. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_number" type="number">
          The last page number. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_number" type="number">
          The next page number. Optional lookup argument.
        </ResponseField>
        <ResponseField name="number" type="number">
          The current page number. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_number" type="number">
          The previous page number. Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          The page size. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Total connections matching request. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          Pagination type. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
