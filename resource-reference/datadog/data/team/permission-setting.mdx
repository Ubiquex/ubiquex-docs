---
title: "datadog_team_permission_setting"
description: "`datadog_team_permission_setting` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_team_permission_setting` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_team_permission_setting"})

    		ubx.Data(team.PermissionSetting, "example", team.PermissionSettingConfig{
    			TeamId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PermissionSetting } from "@ubx/sdk-datadog/datadog/data/team/permission_setting";

    export default stack("example", () => {
      intent({ summary: "look up datadog_team_permission_setting" });

      data(PermissionSetting, "example", {
        teamId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.team import PermissionSetting, PermissionSettingConfig

    def describe():
        ubx.intent("look up datadog_team_permission_setting")

        ubx.data(PermissionSetting, "example", PermissionSettingConfig(
            team_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="team_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Team permission settings response data Returned by the lookup.
</ResponseField>
