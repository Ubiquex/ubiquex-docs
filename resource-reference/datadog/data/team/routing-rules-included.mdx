---
title: "datadog_team_routing_rules_included"
description: "`datadog_team_routing_rules_included` reads the real routing rules included alongside a team lookup, controlling how alerts get routed to that team."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	team "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/team"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_team_routing_rules_included"})

    		ubx.Data(team.RoutingRulesIncluded, "example", team.RoutingRulesIncludedConfig{
    			TeamId:  "example",
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { RoutingRulesIncluded } from "./local-sdk/datadog/sdk/typescript/datadog/data/team/routing_rules_included.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_team_routing_rules_included" });

      data(RoutingRulesIncluded, "example", {
        teamId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.team import RoutingRulesIncluded, RoutingRulesIncludedConfig

    def describe():
        ubx.intent("look up datadog_team_routing_rules_included")

        ubx.data(RoutingRulesIncluded, "example", RoutingRulesIncludedConfig(
            team_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="team_id" type="string" required>
  The real, unique identifier of this team. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Represents the top-level data object for team routing rules, containing the ID, relationships, and resource type. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Specifies the unique identifier of this team routing rules record. Optional lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Specifies relationships for team routing rules, including rule references. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="rules" type="object">
          Holds references to a set of routing rules in a relationship. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              An array of references to the routing rules associated with this team. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Team routing rules resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="included" type="list(object)">
  Provides related routing rules or other included resources. Returned by the lookup.
</ResponseField>
