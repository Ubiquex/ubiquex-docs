---
title: "datadog_recommendation_document"
description: "`datadog_recommendation_document` reads a real, system-generated recommendation."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	recommendation "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/recommendation"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_recommendation_document"})

    		ubx.Data(recommendation.Document, "example", recommendation.DocumentConfig{
    			Service:     "example",
    			BypassCache: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Document } from "./local-sdk/datadog/sdk/typescript/datadog/data/recommendation/document.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_recommendation_document" });

      data(Document, "example", {
        service: "example",
        bypassCache: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.recommendation import Document, DocumentConfig

    def describe():
        ubx.intent("look up datadog_recommendation_document")

        ubx.data(Document, "example", DocumentConfig(
            service="example",
            bypass_cache="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="bypass_cache" type="string">
  Whether the request bypasses any cached result and reads live data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="service" type="string" required>
  The real service name this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  JSON:API resource object for SPA Recommendation. Includes type, optional ID, and resource attributes with structured recommendations. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the SPA Recommendation resource. Contains recommendations for both driver and executor components. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="confidence_level" type="number">
          The confidence level of the recommendation, expressed as a value between 0.0 (low confidence) and 1.0 (high confidence). Optional lookup argument.
        </ResponseField>
        <ResponseField name="driver" type="object" required>
          Resource recommendation for a single Spark component (driver or executor). Contains estimation data used to patch Spark job specs. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="estimation" type="object" required>
              Recommended resource values for a Spark driver or executor, derived from recent real usage metrics. Used by SPA to propose more efficient pod sizing. Required lookup argument.
              <Expandable title="properties">
                <ResponseField name="cpu" type="object">
                  CPU usage statistics derived from historical Spark job metrics. Provides multiple estimates so users can choose between conservative and cost-saving risk profiles. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="max" type="number">
                      Maximum CPU usage observed for the job, expressed in millicores. This represents the upper bound of usage. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="p75" type="number">
                      75th percentile of CPU usage (millicores). Represents a cost-saving configuration while covering most workloads. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="p95" type="number">
                      95th percentile of CPU usage (millicores). Balances performance and cost, providing a safer margin than p75. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ephemeral_storage" type="number">
                  Recommended ephemeral storage allocation (in MiB). Derived from job temporary storage patterns. Optional lookup argument.
                </ResponseField>
                <ResponseField name="heap" type="number">
                  Recommended JVM heap size (in MiB). Optional lookup argument.
                </ResponseField>
                <ResponseField name="memory" type="number">
                  Recommended total memory allocation (in MiB). Includes both heap and overhead. Optional lookup argument.
                </ResponseField>
                <ResponseField name="overhead" type="number">
                  Recommended JVM overhead (in MiB). Computed as total memory - heap. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="executor" type="object" required>
          Resource recommendation for a single Spark component (driver or executor). Contains estimation data used to patch Spark job specs. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="estimation" type="object" required>
              Recommended resource values for a Spark driver or executor, derived from recent real usage metrics. Used by SPA to propose more efficient pod sizing. Required lookup argument.
              <Expandable title="properties">
                <ResponseField name="cpu" type="object">
                  CPU usage statistics derived from historical Spark job metrics. Provides multiple estimates so users can choose between conservative and cost-saving risk profiles. Optional lookup argument.
                  <Expandable title="properties">
                    <ResponseField name="max" type="number">
                      Maximum CPU usage observed for the job, expressed in millicores. This represents the upper bound of usage. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="p75" type="number">
                      75th percentile of CPU usage (millicores). Represents a cost-saving configuration while covering most workloads. Optional lookup argument.
                    </ResponseField>
                    <ResponseField name="p95" type="number">
                      95th percentile of CPU usage (millicores). Balances performance and cost, providing a safer margin than p75. Optional lookup argument.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
                <ResponseField name="ephemeral_storage" type="number">
                  Recommended ephemeral storage allocation (in MiB). Derived from job temporary storage patterns. Optional lookup argument.
                </ResponseField>
                <ResponseField name="heap" type="number">
                  Recommended JVM heap size (in MiB). Optional lookup argument.
                </ResponseField>
                <ResponseField name="memory" type="number">
                  Recommended total memory allocation (in MiB). Includes both heap and overhead. Optional lookup argument.
                </ResponseField>
                <ResponseField name="overhead" type="number">
                  Recommended JVM overhead (in MiB). Computed as total memory - heap. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Resource identifier for the recommendation. Optional in responses. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      JSON:API resource type for Spark Pod Autosizing recommendations. Identifies the Recommendation resource returned by SPA. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
