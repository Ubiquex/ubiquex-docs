---
title: "datadog_custom_ruleset"
description: "`datadog_custom_ruleset` looks up a real, custom set of detection rules grouped together as one ruleset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	custom "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/custom"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_custom_ruleset"})

    		ubx.Data(custom.Ruleset, "example", custom.RulesetConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Ruleset } from "@ubx/sdk-datadog/datadog/data/custom/ruleset";

    export default stack("example", () => {
      intent({ summary: "look up datadog_custom_ruleset" });

      data(Ruleset, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.custom import Ruleset, RulesetConfig

    def describe():
        ubx.intent("look up datadog_custom_ruleset")

        ubx.data(Ruleset, "example", RulesetConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The list of custom rulesets. Returned by the lookup.
</ResponseField>
