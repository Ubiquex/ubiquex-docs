---
title: "datadog_custom_destination_response_definition"
description: "`datadog_custom_destination_response_definition` looks up a real Custom Destination -- a configured forwarding target for logs leaving Datadog."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	custom "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/custom"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_custom_destination_response_definition"})

    		ubx.Data(custom.DestinationResponseDefinition, "example", custom.DestinationResponseDefinitionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DestinationResponseDefinition } from "@ubx/sdk-datadog/datadog/data/custom/destination_response_definition";

    export default stack("example", () => {
      intent({ summary: "look up datadog_custom_destination_response_definition" });

      data(DestinationResponseDefinition, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.custom import DestinationResponseDefinition, DestinationResponseDefinitionConfig

    def describe():
        ubx.intent("look up datadog_custom_destination_response_definition")

        ubx.data(DestinationResponseDefinition, "example", DestinationResponseDefinitionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of custom destinations. Returned by the lookup.
</ResponseField>
