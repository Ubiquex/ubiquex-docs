---
title: "datadog_container_image_item"
description: "`datadog_container_image_item` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_container_image_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_container_image_item"})

    		ubx.Data(container.ImageItem, "example", container.ImageItemConfig{
    			GroupBy: "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ImageItem } from "@ubx/sdk-datadog/datadog/data/container/image_item";

    export default stack("example", () => {
      intent({ summary: "look up datadog_container_image_item" });

      data(ImageItem, "example", {
        groupBy: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.container import ImageItem, ImageItemConfig

    def describe():
        ubx.intent("look up datadog_container_image_item")

        ubx.data(ImageItem, "example", ImageItemConfig(
            group_by="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="group_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of Container Image objects. Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  Pagination links. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="first" type="string">
      Link to the first page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last" type="string">
      Link to the last page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="next" type="string">
      Link to the next page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="prev" type="string">
      Link to previous page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string">
      Link to current page. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Paging attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cursor" type="string">
          The cursor used to get the current results, if any. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Number of results returned Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_cursor" type="string">
          The cursor used to get the next results, if any. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_cursor" type="string">
          The cursor used to get the previous results, if any. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Total number of records that match the query. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of Container Image pagination. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
