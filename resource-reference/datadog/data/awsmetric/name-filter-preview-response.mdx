---
title: "datadog_awsmetric_name_filter_preview_response"
description: "`datadog_awsmetric_name_filter_preview_response` previews the real AWS CloudWatch metric names a given filter would collect."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	awsmetric "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/awsmetric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_awsmetric_name_filter_preview_response"})

    		ubx.Data(awsmetric.NameFilterPreviewResponse, "example", awsmetric.NameFilterPreviewResponseConfig{
    			AwsAccountConfigId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NameFilterPreviewResponse } from "@ubx/sdk-datadog/datadog/data/awsmetric/name_filter_preview_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_awsmetric_name_filter_preview_response" });

      data(NameFilterPreviewResponse, "example", {
        awsAccountConfigId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.awsmetric import NameFilterPreviewResponse, NameFilterPreviewResponseConfig

    def describe():
        ubx.intent("look up datadog_awsmetric_name_filter_preview_response")

        ubx.data(NameFilterPreviewResponse, "example", NameFilterPreviewResponseConfig(
            aws_account_config_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_config_id" type="string" required>
  The real, unique identifier of this AWS account configuration. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  AWS metric name filter preview response data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      AWS metric name filter preview response attributes. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(object)" required>
          The list of namespaces affected by the previewed metric name filters. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique Datadog ID of the AWS Account Integration Config. To get the config ID for an account, use the [List all AWS integrations](https://docs.datadoghq.com/api/latest/aws-integration/#list-all-aws-integrations) endpoint and query by AWS Account ID. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The `AWSMetricNameFilterPreviewResponseData` `type`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
