---
title: "datadog_domain_allowlist_response"
description: "`datadog_domain_allowlist_response` reads the real, allowed email domains configured for the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	domain "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/domain"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_domain_allowlist_response"})

    		ubx.Data(domain.AllowlistResponse, "example", domain.AllowlistResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AllowlistResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/domain/allowlist_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_domain_allowlist_response" });

      data(AllowlistResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.domain import AllowlistResponse, AllowlistResponseConfig

    def describe():
        ubx.intent("look up datadog_domain_allowlist_response")

        ubx.data(AllowlistResponse, "example", AllowlistResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  The email domain allowlist response for an org. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      The details of the email domain allowlist. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="domains" type="list(string)">
          The list of domains in the email domain allowlist. Optional lookup argument.
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Whether the email domain allowlist is enabled for the org. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The unique identifier of the org. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Email domain allowlist allowlist type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
