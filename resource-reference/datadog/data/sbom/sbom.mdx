---
title: "datadog_sbom"
description: "`datadog_sbom` looks up a real Software Bill of Materials (SBOM)."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sbom "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/sbom"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_sbom"})

    		ubx.Data(sbom.Sbom, "example", sbom.SbomConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Sbom } from "@ubx/sdk-datadog/datadog/data/sbom/sbom";

    export default stack("example", () => {
      intent({ summary: "look up datadog_sbom" });

      data(Sbom, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.sbom import Sbom, SbomConfig

    def describe():
        ubx.intent("look up datadog_sbom")

        ubx.data(Sbom, "example", SbomConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of assets SBOMs. Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  The JSON:API links related to pagination. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="first" type="string" required>
      First page link. Required lookup argument.
    </ResponseField>
    <ResponseField name="last" type="string" required>
      Last page link. Required lookup argument.
    </ResponseField>
    <ResponseField name="next" type="string">
      Next page link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="previous" type="string">
      Previous page link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string" required>
      Request link. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  The metadata related to this request. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="count" type="number" required>
      Number of entities included in the response. Required lookup argument.
    </ResponseField>
    <ResponseField name="token" type="string" required>
      The token that identifies the request. Required lookup argument.
    </ResponseField>
    <ResponseField name="total" type="number" required>
      Total number of entities across all pages. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
