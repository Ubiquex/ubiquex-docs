---
title: "datadog_fleet_tracers_response"
description: "`datadog_fleet_tracers_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_fleet_tracers_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fleet "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/fleet"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_fleet_tracers_response"})

    		ubx.Data(fleet.TracersResponse, "example", fleet.TracersResponseConfig{
    			AgentKey:       "example",
    			PageNumber:     1,
    			PageSize:       1,
    			SortAttribute:  "example",
    			SortDescending: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TracersResponse } from "@ubx/sdk-datadog/datadog/data/fleet/tracers_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_fleet_tracers_response" });

      data(TracersResponse, "example", {
        agentKey: "example",
        pageNumber: 1,
        pageSize: 1,
        sortAttribute: "example",
        sortDescending: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.fleet import TracersResponse, TracersResponseConfig

    def describe():
        ubx.intent("look up datadog_fleet_tracers_response")

        ubx.data(TracersResponse, "example", TracersResponseConfig(
            agent_key="example",
            page_number=1,
            page_size=1,
            sort_attribute="example",
            sort_descending=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="agent_key" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="page_number" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_attribute" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_descending" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The response data containing status and tracers array. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the fleet tracers response containing the list of tracers. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="tracers" type="list(object)">
          Array of tracers matching the query criteria. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Status identifier. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for the list of tracers response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="total_filtered_count" type="number">
      Total number of tracers matching the filter criteria across all pages. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
