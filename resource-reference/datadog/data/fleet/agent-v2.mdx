---
title: "datadog_fleet_agent_v2"
description: "`datadog_fleet_agent_v2` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_fleet_agent_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fleet "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/fleet"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_fleet_agent_v2"})

    		ubx.Data(fleet.AgentV2, "example", fleet.AgentV2Config{
    			Filter:         "example",
    			PageNumber:     1,
    			PageSize:       1,
    			SortAttribute:  "example",
    			SortDescending: true,
    			Tags:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentV2 } from "@ubx/sdk-datadog/datadog/data/fleet/agent_v2";

    export default stack("example", () => {
      intent({ summary: "look up datadog_fleet_agent_v2" });

      data(AgentV2, "example", {
        filter: "example",
        pageNumber: 1,
        pageSize: 1,
        sortAttribute: "example",
        sortDescending: true,
        tags: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.fleet import AgentV2, AgentV2Config

    def describe():
        ubx.intent("look up datadog_fleet_agent_v2")

        ubx.data(AgentV2, "example", AgentV2Config(
            filter="example",
            page_number=1,
            page_size=1,
            sort_attribute="example",
            sort_descending=True,
            tags="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_number" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_attribute" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort_descending" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tags" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of agents matching the query criteria. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for the v2 list of agents, including pagination information. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination details for the v2 list of agents. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total number of agents in the fleet, regardless of any filter. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total number of agents matching the current filter criteria. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
