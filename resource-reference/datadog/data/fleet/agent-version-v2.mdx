---
title: "datadog_fleet_agent_version_v2"
description: "`datadog_fleet_agent_version_v2` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_fleet_agent_version_v2` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fleet "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/fleet"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_fleet_agent_version_v2"})

    		ubx.Data(fleet.AgentVersionV2, "example", fleet.AgentVersionV2Config{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentVersionV2 } from "@ubx/sdk-datadog/datadog/data/fleet/agent_version_v2";

    export default stack("example", () => {
      intent({ summary: "look up datadog_fleet_agent_version_v2" });

      data(AgentVersionV2, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.fleet import AgentVersionV2, AgentVersionV2Config

    def describe():
        ubx.intent("look up datadog_fleet_agent_version_v2")

        ubx.data(AgentVersionV2, "example", AgentVersionV2Config(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of available agent versions. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for the v2 list of agent versions. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination details for the v2 list of agent versions. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total number of available agent versions. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
