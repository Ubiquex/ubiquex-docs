---
title: "datadog_fleet_deployment_v2"
description: "`datadog_fleet_deployment_v2` looks up a real Fleet Automation deployment -- a rollout of an Agent version or configuration across a set of hosts."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	fleet "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/fleet"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_fleet_deployment_v2"})

    		ubx.Data(fleet.DeploymentV2, "example", fleet.DeploymentV2Config{
    			Ascending:  true,
    			Filter:     "example",
    			PageNumber: 1,
    			PageSize:   1,
    			Sort:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { DeploymentV2 } from "./local-sdk/datadog/sdk/typescript/datadog/data/fleet/deployment_v2.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_fleet_deployment_v2" });

      data(DeploymentV2, "example", {
        ascending: true,
        filter: "example",
        pageNumber: 1,
        pageSize: 1,
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.fleet import DeploymentV2, DeploymentV2Config

    def describe():
        ubx.intent("look up datadog_fleet_deployment_v2")

        ubx.data(DeploymentV2, "example", DeploymentV2Config(
            ascending=True,
            filter="example",
            page_number=1,
            page_size=1,
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ascending" type="bool">
  Whether results are sorted in ascending order. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_number" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  The number of results per page, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of deployments matching the query criteria. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for the v2 list of deployments, including pagination information. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination details for the v2 list of deployments. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total number of deployments available across all pages. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total number of deployments matching the current filter query. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
