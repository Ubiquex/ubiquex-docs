---
title: "datadog_fleet_schedule_v2_response"
description: "`datadog_fleet_schedule_v2_response` looks up a real Fleet Automation schedule controlling when a deployment rolls out."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	fleet "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/fleet"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_fleet_schedule_v2_response"})

    		ubx.Data(fleet.ScheduleV2Response, "example", fleet.ScheduleV2ResponseConfig{
    			Id: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ScheduleV2Response } from "./local-sdk/datadog/sdk/typescript/datadog/data/fleet/schedule_v2_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_fleet_schedule_v2_response" });

      data(ScheduleV2Response, "example", {
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.fleet import ScheduleV2Response, ScheduleV2ResponseConfig

    def describe():
        ubx.intent("look up datadog_fleet_schedule_v2_response")

        ubx.data(ScheduleV2Response, "example", ScheduleV2ResponseConfig(
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  A fleet upgrade schedule resource in the v2 API response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of a fleet schedule in the v2 API response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          RFC3339 timestamp when the schedule was created. Optional lookup argument.
        </ResponseField>
        <ResponseField name="created_by" type="string">
          User handle of the person who created the schedule. Optional lookup argument.
        </ResponseField>
        <ResponseField name="is_default" type="bool">
          Whether this is the default schedule for the organization. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          Human-readable name for the schedule. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_run" type="string">
          RFC3339 timestamp of the next scheduled maintenance window start time. Absent when the next run time cannot be computed. Optional lookup argument.
        </ResponseField>
        <ResponseField name="notification_rule" type="object">
          Notification configuration attached to a schedule. Included when available. If the notification rule cannot be retrieved, this field is omitted and the schedule is still returned. If the notification rule is retrieved but its handles cannot be resolved, it is still included with an empty `handles` array. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="handles" type="list(string)">
              Notification handles (for example, Slack channels or PagerDuty integrations). Optional lookup argument.
            </ResponseField>
            <ResponseField name="tags" type="list(string)">
              Tags associated with the notification rule. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="query" type="string">
          Query used to filter and select target hosts for scheduled deployments. Optional lookup argument.
        </ResponseField>
        <ResponseField name="rule" type="object">
          Defines the recurrence pattern for the schedule. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="days_of_week" type="list(string)">
              Days of the week when the schedule triggers. Valid values are "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun". Optional lookup argument.
            </ResponseField>
            <ResponseField name="interval" type="number">
              Interval between schedule runs in weeks. 1 means the schedule runs every week on the specified days. Higher values repeat every N weeks. Optional lookup argument.
            </ResponseField>
            <ResponseField name="maintenance_window_duration" type="number">
              Duration of the maintenance window in minutes. Optional lookup argument.
            </ResponseField>
            <ResponseField name="start_maintenance_window" type="string">
              Start time of the maintenance window in 24-hour clock format (HHMM). Deployments are triggered at this time on the specified days. Optional lookup argument.
            </ResponseField>
            <ResponseField name="timezone" type="string">
              Timezone in IANA Time Zone Database format. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the schedule. - `active`: The schedule is active and will create deployments according to its recurrence rule. - `inactive`: The schedule is inactive and will not create any deployments. Optional lookup argument.
        </ResponseField>
        <ResponseField name="updated_at" type="string">
          RFC3339 timestamp when the schedule was last updated. Optional lookup argument.
        </ResponseField>
        <ResponseField name="updated_by" type="string">
          User handle of the person who last updated the schedule. Optional lookup argument.
        </ResponseField>
        <ResponseField name="version_to_latest" type="number">
          Number of major versions behind the latest to target for upgrades. - 0: Always upgrade to the latest version. - 1: Upgrade to latest minus 1 major version. - 2: Upgrade to latest minus 2 major versions. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier for the schedule. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of schedule resource. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
