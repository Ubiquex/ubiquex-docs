---
title: "datadog_scorecard_list_response_data"
description: "`datadog_scorecard_list_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_scorecard_list_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	scorecard "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/scorecard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_scorecard_list_response_data"})

    		ubx.Data(scorecard.ListResponseData, "example", scorecard.ListResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListResponseData } from "@ubx/sdk-datadog/datadog/data/scorecard/list_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_scorecard_list_response_data" });

      data(ListResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.scorecard import ListResponseData, ListResponseDataConfig

    def describe():
        ubx.intent("look up datadog_scorecard_list_response_data")

        ubx.data(ListResponseData, "example", ListResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of scorecards. Returned by the lookup.
</ResponseField>
