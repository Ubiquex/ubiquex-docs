---
title: "datadog_billing_dimensions_mapping_body_item"
description: "`datadog_billing_dimensions_mapping_body_item` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_billing_dimensions_mapping_body_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	billing "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/billing"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_billing_dimensions_mapping_body_item"})

    		ubx.Data(billing.DimensionsMappingBodyItem, "example", billing.DimensionsMappingBodyItemConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DimensionsMappingBodyItem } from "@ubx/sdk-datadog/datadog/data/billing/dimensions_mapping_body_item";

    export default stack("example", () => {
      intent({ summary: "look up datadog_billing_dimensions_mapping_body_item" });

      data(DimensionsMappingBodyItem, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.billing import DimensionsMappingBodyItem, DimensionsMappingBodyItemConfig

    def describe():
        ubx.intent("look up datadog_billing_dimensions_mapping_body_item")

        ubx.data(DimensionsMappingBodyItem, "example", DimensionsMappingBodyItemConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Billing dimensions mapping data. Returned by the lookup.
</ResponseField>
