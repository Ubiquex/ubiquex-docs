---
title: "datadog_ruleset_resp_data"
description: "`datadog_ruleset_resp_data` looks up a real, custom set of detection rules grouped together as one ruleset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ruleset "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/ruleset"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_ruleset_resp_data"})

    		ubx.Data(ruleset.RespData, "example", ruleset.RespDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RespData } from "@ubx/sdk-datadog/datadog/data/ruleset/resp_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_ruleset_resp_data" });

      data(RespData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.ruleset import RespData, RespDataConfig

    def describe():
        ubx.intent("look up datadog_ruleset_resp_data")

        ubx.data(RespData, "example", RespDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The `RulesetRespArray` `data`. Returned by the lookup.
</ResponseField>
