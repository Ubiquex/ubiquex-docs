---
title: "datadog_sast_ruleset_data"
description: "`datadog_sast_ruleset_data` looks up a real static application security testing (SAST) ruleset."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sast "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/sast"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_sast_ruleset_data"})

    		ubx.Data(sast.RulesetData, "example", sast.RulesetDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RulesetData } from "@ubx/sdk-datadog/datadog/data/sast/ruleset_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_sast_ruleset_data" });

      data(RulesetData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.sast import RulesetData, RulesetDataConfig

    def describe():
        ubx.intent("look up datadog_sast_ruleset_data")

        ubx.data(RulesetData, "example", RulesetDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The list of SAST rulesets returned in the response. Returned by the lookup.
</ResponseField>
