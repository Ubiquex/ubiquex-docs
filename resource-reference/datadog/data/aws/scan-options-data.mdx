---
title: "datadog_aws_scan_options_data"
description: "`datadog_aws_scan_options_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_aws_scan_options_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	aws "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/aws"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_aws_scan_options_data"})

    		ubx.Data(aws.ScanOptionsData, "example", aws.ScanOptionsDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ScanOptionsData } from "@ubx/sdk-datadog/datadog/data/aws/scan_options_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_aws_scan_options_data" });

      data(ScanOptionsData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.aws import ScanOptionsData, ScanOptionsDataConfig

    def describe():
        ubx.intent("look up datadog_aws_scan_options_data")

        ubx.data(ScanOptionsData, "example", ScanOptionsDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of AWS scan options. Returned by the lookup.
</ResponseField>
