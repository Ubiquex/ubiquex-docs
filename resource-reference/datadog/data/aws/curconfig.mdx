---
title: "datadog_aws_curconfig"
description: "`datadog_aws_curconfig` looks up a real AWS Cost and Usage Report (CUR) configuration feeding Cloud Cost Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	aws "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/aws"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_aws_curconfig"})

    		ubx.Data(aws.Curconfig, "example", aws.CurconfigConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Curconfig } from "./local-sdk/datadog/sdk/typescript/datadog/data/aws/curconfig.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_aws_curconfig" });

      data(Curconfig, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.aws import Curconfig, CurconfigConfig

    def describe():
        ubx.intent("look up datadog_aws_curconfig")

        ubx.data(Curconfig, "example", CurconfigConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An AWS CUR config. Returned by the lookup.
</ResponseField>
