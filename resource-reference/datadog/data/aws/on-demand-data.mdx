---
title: "datadog_aws_on_demand_data"
description: "`datadog_aws_on_demand_data` reads real, on-demand (unreserved) AWS spend data in Cloud Cost Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	aws "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/aws"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_aws_on_demand_data"})

    		ubx.Data(aws.OnDemandData, "example", aws.OnDemandDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { OnDemandData } from "./local-sdk/datadog/sdk/typescript/datadog/data/aws/on_demand_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_aws_on_demand_data" });

      data(OnDemandData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.aws import OnDemandData, OnDemandDataConfig

    def describe():
        ubx.intent("look up datadog_aws_on_demand_data")

        ubx.data(OnDemandData, "example", OnDemandDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of on demand tasks. Returned by the lookup.
</ResponseField>
