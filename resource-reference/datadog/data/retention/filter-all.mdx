---
title: "datadog_retention_filter_all"
description: "`datadog_retention_filter_all` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_retention_filter_all` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	retention "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/retention"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_retention_filter_all"})

    		ubx.Data(retention.FilterAll, "example", retention.FilterAllConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FilterAll } from "@ubx/sdk-datadog/datadog/data/retention/filter_all";

    export default stack("example", () => {
      intent({ summary: "look up datadog_retention_filter_all" });

      data(FilterAll, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.retention import FilterAll, FilterAllConfig

    def describe():
        ubx.intent("look up datadog_retention_filter_all")

        ubx.data(FilterAll, "example", FilterAllConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of retention filters objects. Returned by the lookup.
</ResponseField>
