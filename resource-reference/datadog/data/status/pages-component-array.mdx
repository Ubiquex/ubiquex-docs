---
title: "datadog_status_pages_component_array"
description: "`datadog_status_pages_component_array` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_status_pages_component_array` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	status "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/status"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_status_pages_component_array"})

    		ubx.Data(status.PagesComponentArray, "example", status.PagesComponentArrayConfig{
    			PageId:  "example",
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PagesComponentArray } from "@ubx/sdk-datadog/datadog/data/status/pages_component_array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_status_pages_component_array" });

      data(PagesComponentArray, "example", {
        pageId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.status import PagesComponentArray, PagesComponentArrayConfig

    def describe():
        ubx.intent("look up datadog_status_pages_component_array")

        ubx.data(PagesComponentArray, "example", PagesComponentArrayConfig(
            page_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of component data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  The included related resources of a component. Client must explicitly request these resources by name in the `include` query parameter. Returned by the lookup.
</ResponseField>
