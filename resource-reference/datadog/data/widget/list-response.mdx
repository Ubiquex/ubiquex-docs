---
title: "datadog_widget_list_response"
description: "`datadog_widget_list_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_widget_list_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	widget "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/widget"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_widget_list_response"})

    		ubx.Data(widget.ListResponse, "example", widget.ListResponseConfig{
    			ExperienceType: "example",
    			Sort:           "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListResponse } from "@ubx/sdk-datadog/datadog/data/widget/list_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_widget_list_response" });

      data(ListResponse, "example", {
        experienceType: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.widget import ListResponse, ListResponseConfig

    def describe():
        ubx.intent("look up datadog_widget_list_response")

        ubx.data(ListResponse, "example", ListResponseConfig(
            experience_type="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="experience_type" type="string" required>
  Widget experience types that differentiate between the products using the specific widget. Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of widget resources. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of user resources related to the widgets. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata about the search results. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="created_by_anyone_total" type="number">
      Total number of widgets created by anyone. Optional lookup argument.
    </ResponseField>
    <ResponseField name="created_by_you_total" type="number">
      Total number of widgets created by the current user. Optional lookup argument.
    </ResponseField>
    <ResponseField name="favorited_by_you_total" type="number">
      Total number of widgets favorited by the current user. Optional lookup argument.
    </ResponseField>
    <ResponseField name="filtered_total" type="number">
      Total number of widgets matching the current filter criteria. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
