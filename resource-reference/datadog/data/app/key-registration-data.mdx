---
title: "datadog_app_key_registration_data"
description: "`datadog_app_key_registration_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_app_key_registration_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	app "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/app"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_app_key_registration_data"})

    		ubx.Data(app.KeyRegistrationData, "example", app.KeyRegistrationDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { KeyRegistrationData } from "@ubx/sdk-datadog/datadog/data/app/key_registration_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_app_key_registration_data" });

      data(KeyRegistrationData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.app import KeyRegistrationData, KeyRegistrationDataConfig

    def describe():
        ubx.intent("look up datadog_app_key_registration_data")

        ubx.data(KeyRegistrationData, "example", KeyRegistrationDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An array of app key registrations. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  The definition of `ListAppKeyRegistrationsResponseMeta` object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="total" type="number">
      The total number of app key registrations. Optional lookup argument.
    </ResponseField>
    <ResponseField name="total_filtered" type="number">
      The total number of app key registrations that match the specified filters. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
