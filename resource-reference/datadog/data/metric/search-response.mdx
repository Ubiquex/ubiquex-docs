---
title: "datadog_metric_search_response"
description: "`datadog_metric_search_response` searches for real custom metrics by name."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	metric "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/metric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_metric_search_response"})

    		ubx.Data(metric.SearchResponse, "example", metric.SearchResponseConfig{
    			Q: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SearchResponse } from "@ubx/sdk-datadog/datadog/data/metric/search_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_metric_search_response" });

      data(SearchResponse, "example", {
        q: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.metric import SearchResponse, SearchResponseConfig

    def describe():
        ubx.intent("look up datadog_metric_search_response")

        ubx.data(SearchResponse, "example", SearchResponseConfig(
            q="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="q" type="string" required>
  The real search query string. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="results" type="object">
  Search result. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="metrics" type="list(string)">
      List of metrics that match the search query. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
