---
title: "datadog_metric_asset_response_included"
description: "`datadog_metric_asset_response_included` reads a real, related item included alongside a metric asset lookup, such as its own tag configuration or usage."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	metric "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/metric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_metric_asset_response_included"})

    		ubx.Data(metric.AssetResponseIncluded, "example", metric.AssetResponseIncludedConfig{
    			MetricName: "example-metric-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AssetResponseIncluded } from "./local-sdk/datadog/sdk/typescript/datadog/data/metric/asset_response_included.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_metric_asset_response_included" });

      data(AssetResponseIncluded, "example", {
        metricName: "example-metric-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.metric import AssetResponseIncluded, AssetResponseIncludedConfig

    def describe():
        ubx.intent("look up datadog_metric_asset_response_included")

        ubx.data(AssetResponseIncluded, "example", AssetResponseIncludedConfig(
            metric_name="example-metric-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Metric assets response data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="id" type="string" required>
      The metric name for this resource. Required lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships to assets related to the metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="dashboards" type="object">
          An object containing the list of dashboards that can be referenced in the `included` data. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              A list of dashboards that can be referenced in the `included` data. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="monitors" type="object">
          A object containing the list of monitors that can be referenced in the `included` data. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              A list of monitors that can be referenced in the `included` data. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="notebooks" type="object">
          An object containing the list of notebooks that can be referenced in the `included` data. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              A list of notebooks that can be referenced in the `included` data. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="slos" type="object">
          An object containing a list of SLOs that can be referenced in the `included` data. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              A list of SLOs that can be referenced in the `included` data. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The metric resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of objects related to the metric assets. Returned by the lookup.
</ResponseField>
