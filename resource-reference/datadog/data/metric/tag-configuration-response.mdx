---
title: "datadog_metric_tag_configuration_response"
description: "`datadog_metric_tag_configuration_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_metric_tag_configuration_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	metric "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/metric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_metric_tag_configuration_response"})

    		ubx.Data(metric.TagConfigurationResponse, "example", metric.TagConfigurationResponseConfig{
    			MetricName: "example-metric-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TagConfigurationResponse } from "@ubx/sdk-datadog/datadog/data/metric/tag_configuration_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_metric_tag_configuration_response" });

      data(TagConfigurationResponse, "example", {
        metricName: "example-metric-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.metric import TagConfigurationResponse, TagConfigurationResponseConfig

    def describe():
        ubx.intent("look up datadog_metric_tag_configuration_response")

        ubx.data(TagConfigurationResponse, "example", TagConfigurationResponseConfig(
            metric_name="example-metric-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="metric_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Object for a single metric tag configuration. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Object containing the definition of a metric tag configuration attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="aggregations" type="list(object)">
          Deprecated. You no longer need to configure specific time and space aggregations for Metrics Without Limits. Optional lookup argument.
        </ResponseField>
        <ResponseField name="created_at" type="string">
          Timestamp when the tag configuration was created. Optional lookup argument.
        </ResponseField>
        <ResponseField name="exclude_tags_mode" type="bool">
          When set to true, the configuration will exclude the configured tags and include any other submitted tags. When set to false, the configuration will include the configured tags and exclude any other submitted tags. Defaults to false. Requires `tags` property. Optional lookup argument.
        </ResponseField>
        <ResponseField name="include_percentiles" type="bool">
          Toggle to include or exclude percentile aggregations for distribution metrics. Only present when the `metric_type` is `distribution`. Optional lookup argument.
        </ResponseField>
        <ResponseField name="metric_type" type="string">
          The metric's type. Optional lookup argument.
        </ResponseField>
        <ResponseField name="modified_at" type="string">
          Timestamp when the tag configuration was last modified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="tags" type="list(string)">
          List of tag keys on which to group. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The metric name for this resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships for a metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="metric_volumes" type="object">
          Relationship to a metric volume included in the response. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object">
              Relationship data for a metric volume. Optional lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      The metric tag configuration resource type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
