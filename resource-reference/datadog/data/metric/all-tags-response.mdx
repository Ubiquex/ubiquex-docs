---
title: "datadog_metric_all_tags_response"
description: "`datadog_metric_all_tags_response` reads the real, complete set of tags submitted on a custom metric."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	metric "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/metric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_metric_all_tags_response"})

    		ubx.Data(metric.AllTagsResponse, "example", metric.AllTagsResponseConfig{
    			MetricName: "example-metric-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AllTagsResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/metric/all_tags_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_metric_all_tags_response" });

      data(AllTagsResponse, "example", {
        metricName: "example-metric-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.metric import AllTagsResponse, AllTagsResponseConfig

    def describe():
        ubx.intent("look up datadog_metric_all_tags_response")

        ubx.data(AllTagsResponse, "example", AllTagsResponseConfig(
            metric_name="example-metric-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="metric_name" type="string" required>
  The real name of the metric this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Object for a single metric's indexed and ingested tags. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Object containing the definition of a metric's indexed and ingested tags. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="ingested_tags" type="list(string)">
          List of ingested tags that are not indexed. Optional lookup argument.
        </ResponseField>
        <ResponseField name="tags" type="list(string)">
          List of indexed tags. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The metric name for this resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      The metric resource type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
