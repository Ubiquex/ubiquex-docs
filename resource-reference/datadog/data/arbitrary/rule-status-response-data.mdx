---
title: "datadog_arbitrary_rule_status_response_data"
description: "`datadog_arbitrary_rule_status_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_arbitrary_rule_status_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	arbitrary "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/arbitrary"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_arbitrary_rule_status_response_data"})

    		ubx.Data(arbitrary.RuleStatusResponseData, "example", arbitrary.RuleStatusResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RuleStatusResponseData } from "@ubx/sdk-datadog/datadog/data/arbitrary/rule_status_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_arbitrary_rule_status_response_data" });

      data(RuleStatusResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.arbitrary import RuleStatusResponseData, RuleStatusResponseDataConfig

    def describe():
        ubx.intent("look up datadog_arbitrary_rule_status_response_data")

        ubx.data(RuleStatusResponseData, "example", RuleStatusResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Processing status for a custom allocation rule. Returned by the lookup.
</ResponseField>
