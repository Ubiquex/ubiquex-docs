---
title: "datadog_csm_host_facet_info_response"
description: "`datadog_csm_host_facet_info_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_csm_host_facet_info_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	csm "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/csm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_csm_host_facet_info_response"})

    		ubx.Data(csm.HostFacetInfoResponse, "example", csm.HostFacetInfoResponseConfig{
    			Facet:  "example",
    			Query:  "example",
    			Search: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { HostFacetInfoResponse } from "@ubx/sdk-datadog/datadog/data/csm/host_facet_info_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_csm_host_facet_info_response" });

      data(HostFacetInfoResponse, "example", {
        facet: "example",
        query: "example",
        search: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.csm import HostFacetInfoResponse, HostFacetInfoResponseConfig

    def describe():
        ubx.intent("look up datadog_csm_host_facet_info_response")

        ubx.data(HostFacetInfoResponse, "example", HostFacetInfoResponseConfig(
            facet="example",
            query="example",
            search="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="facet" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="search" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The data wrapper for a facet info response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of a facet info response, containing the value distribution for the requested facet. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="items" type="list(object)" required>
          The list of facet value entries for the current page. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The identifier of the facet. Required lookup argument.
    </ResponseField>
    <ResponseField name="meta" type="object" required>
      Metadata for the facet info response. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number" required>
          The total number of distinct values for this facet. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type for facet info resources. The value should always be `facet_info`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
