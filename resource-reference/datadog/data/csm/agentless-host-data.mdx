---
title: "datadog_csm_agentless_host_data"
description: "`datadog_csm_agentless_host_data` looks up a real host scanned by Cloud Security Management's agentless scanning, no Agent installed."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	csm "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/csm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_csm_agentless_host_data"})

    		ubx.Data(csm.AgentlessHostData, "example", csm.AgentlessHostDataConfig{
    			Page:  1,
    			Query: "example",
    			Size:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AgentlessHostData } from "@ubx/sdk-datadog/datadog/data/csm/agentless_host_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_csm_agentless_host_data" });

      data(AgentlessHostData, "example", {
        page: 1,
        query: "example",
        size: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.csm import AgentlessHostData, AgentlessHostDataConfig

    def describe():
        ubx.intent("look up datadog_csm_agentless_host_data")

        ubx.data(AgentlessHostData, "example", AgentlessHostDataConfig(
            page=1,
            query="example",
            size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  The real query string evaluated against the underlying data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="size" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The list of agentless hosts for the current page. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata for a CSM settings list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page_index" type="number" required>
      The current page index (zero-based). Required lookup argument.
    </ResponseField>
    <ResponseField name="page_size" type="number" required>
      The number of resources returned per page. Required lookup argument.
    </ResponseField>
    <ResponseField name="total_filtered" type="number" required>
      The total number of resources matching the filter criteria. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
