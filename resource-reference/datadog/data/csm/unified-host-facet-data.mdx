---
title: "datadog_csm_unified_host_facet_data"
description: "`datadog_csm_unified_host_facet_data` reads the real facet values (filterable attributes) available across Cloud Security Management's unified host view."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	csm "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/csm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_csm_unified_host_facet_data"})

    		ubx.Data(csm.UnifiedHostFacetData, "example", csm.UnifiedHostFacetDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { UnifiedHostFacetData } from "./local-sdk/datadog/sdk/typescript/datadog/data/csm/unified_host_facet_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_csm_unified_host_facet_data" });

      data(UnifiedHostFacetData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.csm import UnifiedHostFacetData, UnifiedHostFacetDataConfig

    def describe():
        ubx.intent("look up datadog_csm_unified_host_facet_data")

        ubx.data(UnifiedHostFacetData, "example", UnifiedHostFacetDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The list of available facets for unified hosts. Returned by the lookup.
</ResponseField>
