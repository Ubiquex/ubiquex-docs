---
title: "datadog_control_notification_settings_response"
description: "`datadog_control_notification_settings_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_control_notification_settings_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	control "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_control_notification_settings_response"})

    		ubx.Data(control.NotificationSettingsResponse, "example", control.NotificationSettingsResponseConfig{
    			DetectionType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NotificationSettingsResponse } from "@ubx/sdk-datadog/datadog/data/control/notification_settings_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_control_notification_settings_response" });

      data(NotificationSettingsResponse, "example", {
        detectionType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.control import NotificationSettingsResponse, NotificationSettingsResponseConfig

    def describe():
        ubx.intent("look up datadog_control_notification_settings_response")

        ubx.data(NotificationSettingsResponse, "example", NotificationSettingsResponseConfig(
            detection_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detection_type" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  A control notification settings resource. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of a governance control's notification settings. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="event_settings" type="list(object)" required>
          The notification settings for each supported event type on the control. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The detection type the notification settings apply to. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Control notification settings resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
