---
title: "datadog_campaign_response_data"
description: "`datadog_campaign_response_data` looks up a real Case Management campaign -- a bulk, tracked initiative grouping many related cases."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	campaign "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/campaign"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_campaign_response_data"})

    		ubx.Data(campaign.ResponseData, "example", campaign.ResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ResponseData } from "./local-sdk/datadog/sdk/typescript/datadog/data/campaign/response_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_campaign_response_data" });

      data(ResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.campaign import ResponseData, ResponseDataConfig

    def describe():
        ubx.intent("look up datadog_campaign_response_data")

        ubx.data(ResponseData, "example", ResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of campaigns. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for scores response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="count" type="number" required>
      Number of entities in this response. Required lookup argument.
    </ResponseField>
    <ResponseField name="limit" type="number" required>
      Pagination limit. Required lookup argument.
    </ResponseField>
    <ResponseField name="offset" type="number" required>
      Pagination offset. Required lookup argument.
    </ResponseField>
    <ResponseField name="total" type="number" required>
      Total number of entities available. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
