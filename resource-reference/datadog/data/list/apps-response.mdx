---
title: "datadog_list_apps_response"
description: "`datadog_list_apps_response` lists the real App Builder apps built on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	list "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/list"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_list_apps_response"})

    		ubx.Data(list.AppsResponse, "example", list.AppsResponseConfig{
    			Limit: 1,
    			Page:  1,
    			Sort:  []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AppsResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/list/apps_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_list_apps_response" });

      data(AppsResponse, "example", {
        limit: 1,
        page: 1,
        sort: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.list import AppsResponse, AppsResponseConfig

    def describe():
        ubx.intent("look up datadog_list_apps_response")

        ubx.data(AppsResponse, "example", AppsResponseConfig(
            limit=1,
            page=1,
            sort=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="limit" type="number">
  The maximum number of results to return. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="page" type="number">
  The page number of results to fetch, for a paginated response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="list(string)">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of app definitions. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Data on the version of the app that was published. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Information on the total number of apps, to be used for pagination. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          The total number of apps under the Datadog organization, disregarding any filters applied. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          The total number of apps that match the specified filters. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
