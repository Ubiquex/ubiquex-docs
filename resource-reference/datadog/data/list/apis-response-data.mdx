---
title: "datadog_list_apis_response_data"
description: "`datadog_list_apis_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_list_apis_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	list "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/list"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_list_apis_response_data"})

    		ubx.Data(list.ApisResponseData, "example", list.ApisResponseDataConfig{
    			Query: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApisResponseData } from "@ubx/sdk-datadog/datadog/data/list/apis_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_list_apis_response_data" });

      data(ApisResponseData, "example", {
        query: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.list import ApisResponseData, ApisResponseDataConfig

    def describe():
        ubx.intent("look up datadog_list_apis_response_data")

        ubx.data(ApisResponseData, "example", ApisResponseDataConfig(
            query="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of API items. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for `ListAPIsResponse`. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Pagination metadata information for `ListAPIsResponse`. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="limit" type="number">
          Number of items in the current page. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          Offset for pagination. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_count" type="number">
          Total number of items. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
