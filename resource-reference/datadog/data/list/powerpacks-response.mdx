---
title: "datadog_list_powerpacks_response"
description: "`datadog_list_powerpacks_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_list_powerpacks_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	list "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/list"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_list_powerpacks_response"})

    		ubx.Data(list.PowerpacksResponse, "example", list.PowerpacksResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PowerpacksResponse } from "@ubx/sdk-datadog/datadog/data/list/powerpacks_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_list_powerpacks_response" });

      data(PowerpacksResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.list import PowerpacksResponse, PowerpacksResponseConfig

    def describe():
        ubx.intent("look up datadog_list_powerpacks_response")

        ubx.data(PowerpacksResponse, "example", PowerpacksResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of powerpack definitions. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of objects related to the users. Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  Links attributes. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="first" type="string">
      Link to last page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last" type="string">
      Link to first page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="next" type="string">
      Link for the next set of results. Optional lookup argument.
    </ResponseField>
    <ResponseField name="prev" type="string">
      Link for the previous set of results. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string">
      Link to current page. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Powerpack response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Powerpack response pagination metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_offset" type="number">
          The first offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_offset" type="number">
          The last offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Pagination limit. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_offset" type="number">
          The next offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          The offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_offset" type="number">
          The previous offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Total results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          Offset type. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
