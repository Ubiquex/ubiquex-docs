---
title: "datadog_list_application_keys_response"
description: "`datadog_list_application_keys_response` lists the real application keys configured on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	list "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/list"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_list_application_keys_response"})

    		ubx.Data(list.ApplicationKeysResponse, "example", list.ApplicationKeysResponseConfig{
    			Filter:  "example",
    			Include: "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ApplicationKeysResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/list/application_keys_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_list_application_keys_response" });

      data(ApplicationKeysResponse, "example", {
        filter: "example",
        include: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.list import ApplicationKeysResponse, ApplicationKeysResponseConfig

    def describe():
        ubx.intent("look up datadog_list_application_keys_response")

        ubx.data(ApplicationKeysResponse, "example", ApplicationKeysResponseConfig(
            filter="example",
            include="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  A real filter expression restricting which results are returned. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Sorting options Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of application keys. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of objects related to the application key. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information related to the application key response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="max_allowed_per_user" type="number">
      Max allowed number of application keys per user. Optional lookup argument.
    </ResponseField>
    <ResponseField name="page" type="object">
      Additional information related to the application key response. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_filtered_count" type="number">
          Total filtered application key count. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
