---
title: "datadog_list_relation_catalog_response"
description: "`datadog_list_relation_catalog_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_list_relation_catalog_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	list "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/list"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_list_relation_catalog_response"})

    		ubx.Data(list.RelationCatalogResponse, "example", list.RelationCatalogResponseConfig{
    			Include:           "example",
    			IncludeDiscovered: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RelationCatalogResponse } from "@ubx/sdk-datadog/datadog/data/list/relation_catalog_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_list_relation_catalog_response" });

      data(RelationCatalogResponse, "example", {
        include: "example",
        includeDiscovered: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.list import RelationCatalogResponse, RelationCatalogResponseConfig

    def describe():
        ubx.intent("look up datadog_list_relation_catalog_response")

        ubx.data(RelationCatalogResponse, "example", RelationCatalogResponseConfig(
            include="example",
            include_discovered=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Supported include types for relations. Optional lookup argument.
</ResponseField>

<ResponseField name="include_discovered" type="bool">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of relation responses Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  List relation response included entities. Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  List relation response links. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="next" type="string">
      Next link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="previous" type="string">
      Previous link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string">
      Current link. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Relation response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="count" type="number">
      Total relations count. Optional lookup argument.
    </ResponseField>
    <ResponseField name="include_count" type="number">
      Total included data count. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
