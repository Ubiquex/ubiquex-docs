---
title: "datadog_list_connections_response"
description: "`datadog_list_connections_response` lists the real integration connections configured for App Builder / Workflow Automation actions."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	list "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/list"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_list_connections_response"})

    		ubx.Data(list.ConnectionsResponse, "example", list.ConnectionsResponseConfig{
    			Entity: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConnectionsResponse } from "@ubx/sdk-datadog/datadog/data/list/connections_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_list_connections_response" });

      data(ConnectionsResponse, "example", {
        entity: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.list import ConnectionsResponse, ConnectionsResponseConfig

    def describe():
        ubx.intent("look up datadog_list_connections_response")

        ubx.data(ConnectionsResponse, "example", ConnectionsResponseConfig(
            entity="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity" type="string" required>
  The real entity this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The data object containing the resource type and attributes for the list connections response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of the list connections response, containing the collection of data source connections. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="connections" type="list(object)">
          The list of data source connections configured for the entity. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Unique identifier for the list connections response resource. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      List connections response resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
