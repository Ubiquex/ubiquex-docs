---
title: "datadog_service_access_token"
description: "`datadog_service_access_token` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_service_access_token` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_service_access_token"})

    		ubx.Data(service.AccessToken, "example", service.AccessTokenConfig{
    			ServiceAccountId: "example",
    			Filter:           "example",
    			Sort:             "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccessToken } from "@ubx/sdk-datadog/datadog/data/service/access_token";

    export default stack("example", () => {
      intent({ summary: "look up datadog_service_access_token" });

      data(AccessToken, "example", {
        serviceAccountId: "example",
        filter: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.service import AccessToken, AccessTokenConfig

    def describe():
        ubx.intent("look up datadog_service_access_token")

        ubx.data(AccessToken, "example", AccessTokenConfig(
            service_account_id="example",
            filter="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="service_account_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Sorting options Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of access tokens. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information related to the access token response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination information. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_filtered_count" type="number">
          Total filtered access token count. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
