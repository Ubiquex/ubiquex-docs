---
title: "datadog_service_now_template_data"
description: "`datadog_service_now_template_data` looks up a real ServiceNow ticket template configured in Datadog's own integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_service_now_template_data"})

    		ubx.Data(service.NowTemplateData, "example", service.NowTemplateDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { NowTemplateData } from "./local-sdk/datadog/sdk/typescript/datadog/data/service/now_template_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_service_now_template_data" });

      data(NowTemplateData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.service import NowTemplateData, NowTemplateDataConfig

    def describe():
        ubx.intent("look up datadog_service_now_template_data")

        ubx.data(NowTemplateData, "example", NowTemplateDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of ServiceNow template data objects Returned by the lookup.
</ResponseField>
