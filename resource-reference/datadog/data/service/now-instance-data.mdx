---
title: "datadog_service_now_instance_data"
description: "`datadog_service_now_instance_data` looks up a real, connected ServiceNow instance configured in Datadog's own integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_service_now_instance_data"})

    		ubx.Data(service.NowInstanceData, "example", service.NowInstanceDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NowInstanceData } from "@ubx/sdk-datadog/datadog/data/service/now_instance_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_service_now_instance_data" });

      data(NowInstanceData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.service import NowInstanceData, NowInstanceDataConfig

    def describe():
        ubx.intent("look up datadog_service_now_instance_data")

        ubx.data(NowInstanceData, "example", NowInstanceDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of ServiceNow instance data objects Returned by the lookup.
</ResponseField>
