---
title: "datadog_service_list"
description: "`datadog_service_list` lists the real services registered in Service Catalog."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_service_list"})

    		ubx.Data(service.List, "example", service.ListConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { List } from "@ubx/sdk-datadog/datadog/data/service/list";

    export default stack("example", () => {
      intent({ summary: "look up datadog_service_list" });

      data(List, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.service import List, ListConfig

    def describe():
        ubx.intent("look up datadog_service_list")

        ubx.data(List, "example", ListConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  A single data item in the service list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of a service list entry, containing metadata and a list of service names. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="metadata" type="list(object)">
          A list of metadata items associated with the service. Optional lookup argument.
        </ResponseField>
        <ResponseField name="services" type="list(string)">
          A list of service names. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The unique identifier of the service. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Services list resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
