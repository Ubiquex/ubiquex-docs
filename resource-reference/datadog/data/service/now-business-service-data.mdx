---
title: "datadog_service_now_business_service_data"
description: "`datadog_service_now_business_service_data` reads a real ServiceNow business service, surfaced through Datadog's ServiceNow integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_service_now_business_service_data"})

    		ubx.Data(service.NowBusinessServiceData, "example", service.NowBusinessServiceDataConfig{
    			InstanceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { NowBusinessServiceData } from "@ubx/sdk-datadog/datadog/data/service/now_business_service_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_service_now_business_service_data" });

      data(NowBusinessServiceData, "example", {
        instanceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.service import NowBusinessServiceData, NowBusinessServiceDataConfig

    def describe():
        ubx.intent("look up datadog_service_now_business_service_data")

        ubx.data(NowBusinessServiceData, "example", NowBusinessServiceDataConfig(
            instance_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="instance_id" type="string" required>
  The real, unique identifier of this instance. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of ServiceNow business service data objects Returned by the lookup.
</ResponseField>
