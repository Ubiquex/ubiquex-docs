---
title: "datadog_service_level_objective"
description: "`datadog_service_level_objective` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_service_level_objective` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_service_level_objective"})

    		ubx.Data(service.LevelObjective, "example", service.LevelObjectiveConfig{
    			Ids:          "example",
    			IsDeleted:    true,
    			Limit:        1,
    			MetricsQuery: "example",
    			Offset:       1,
    			Query:        "example",
    			TagsQuery:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { LevelObjective } from "@ubx/sdk-datadog/datadog/data/service/level_objective";

    export default stack("example", () => {
      intent({ summary: "look up datadog_service_level_objective" });

      data(LevelObjective, "example", {
        ids: "example",
        isDeleted: true,
        limit: 1,
        metricsQuery: "example",
        offset: 1,
        query: "example",
        tagsQuery: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.service import LevelObjective, LevelObjectiveConfig

    def describe():
        ubx.intent("look up datadog_service_level_objective")

        ubx.data(LevelObjective, "example", LevelObjectiveConfig(
            ids="example",
            is_deleted=True,
            limit=1,
            metrics_query="example",
            offset=1,
            query="example",
            tags_query="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ids" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="is_deleted" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="metrics_query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offset" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tags_query" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of service level objective objects. Returned by the lookup.
</ResponseField>

<ResponseField name="errors" type="list(string)">
  An array of error messages. Each endpoint documents how/whether this field is used. Returned by the lookup.
</ResponseField>

<ResponseField name="metadata" type="object">
  The metadata object containing additional information about the list of SLOs. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      The object containing information about the pages of the list of SLOs. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          The total number of resources that could be retrieved ignoring the parameters and filters in the request. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          The total number of resources that match the parameters and filters in the request. This attribute can be used by a client to determine the total number of pages. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
