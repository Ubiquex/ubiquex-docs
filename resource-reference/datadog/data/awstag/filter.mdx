---
title: "datadog_awstag_filter"
description: "`datadog_awstag_filter` looks up a real tag filter controlling which AWS resources Datadog's own AWS integration collects from."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	awstag "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/awstag"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_awstag_filter"})

    		ubx.Data(awstag.Filter, "example", awstag.FilterConfig{
    			AccountId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Filter } from "@ubx/sdk-datadog/datadog/data/awstag/filter";

    export default stack("example", () => {
      intent({ summary: "look up datadog_awstag_filter" });

      data(Filter, "example", {
        accountId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.awstag import Filter, FilterConfig

    def describe():
        ubx.intent("look up datadog_awstag_filter")

        ubx.data(Filter, "example", FilterConfig(
            account_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string" required>
  The real, unique identifier of the account this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="filters" type="list(object)">
  An array of tag filters. Returned by the lookup.
</ResponseField>
