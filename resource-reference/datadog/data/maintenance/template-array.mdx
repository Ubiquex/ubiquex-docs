---
title: "datadog_maintenance_template_array"
description: "`datadog_maintenance_template_array` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_maintenance_template_array` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_maintenance_template_array"})

    		ubx.Data(maintenance.TemplateArray, "example", maintenance.TemplateArrayConfig{
    			PageId:  "example",
    			Include: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TemplateArray } from "@ubx/sdk-datadog/datadog/data/maintenance/template_array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_maintenance_template_array" });

      data(TemplateArray, "example", {
        pageId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.maintenance import TemplateArray, TemplateArrayConfig

    def describe():
        ubx.intent("look up datadog_maintenance_template_array")

        ubx.data(TemplateArray, "example", TemplateArrayConfig(
            page_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of maintenance template data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  The included related resources of a maintenance template. Client must explicitly request these resources by name in the `include` query parameter. Returned by the lookup.
</ResponseField>
