---
title: "datadog_maintenance_array"
description: "`datadog_maintenance_array` lists real, scheduled maintenance windows inside Cloud Security Management -- each suppressing posture/vulnerability..."
---

`datadog_maintenance_array` lists real, scheduled maintenance windows inside Cloud Security Management -- each suppressing posture/vulnerability findings for a defined scope and time range during planned changes.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	maintenance "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/maintenance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_maintenance_array"})

    		ubx.Data(maintenance.Array, "example", maintenance.ArrayConfig{
    			Include: "example",
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Array } from "@ubx/sdk-datadog/datadog/data/maintenance/array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_maintenance_array" });

      data(Array, "example", {
        include: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.maintenance import Array, ArrayConfig

    def describe():
        ubx.intent("look up datadog_maintenance_array")

        ubx.data(Array, "example", ArrayConfig(
            include="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of maintenance data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  The included related resources of a maintenance. Client must explicitly request these resources by name in the `include` query parameter. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Offset-based pagination schema. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_offset" type="number">
          Integer representing the offset to fetch the first page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_offset" type="number">
          Integer representing the offset to fetch the last page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Integer representing the number of elements to be returned in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_offset" type="number">
          Integer representing the index of the first element in the next page of results. Equal to page size added to the current offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          Integer representing the index of the first element in the results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_offset" type="number">
          Integer representing the index of the first element in the previous page of results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Integer representing the total number of elements available. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          The pagination type used for offset-based pagination. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
