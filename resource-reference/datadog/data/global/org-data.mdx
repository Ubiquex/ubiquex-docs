---
title: "datadog_global_org_data"
description: "`datadog_global_org_data` looks up a real, top-level organization record spanning a multi-org account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	global "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/global"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_global_org_data"})

    		ubx.Data(global.OrgData, "example", global.OrgDataConfig{
    			UserHandle: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OrgData } from "@ubx/sdk-datadog/datadog/data/global_/org_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_global_org_data" });

      data(OrgData, "example", {
        userHandle: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.global_ import OrgData, OrgDataConfig

    def describe():
        ubx.intent("look up datadog_global_org_data")

        ubx.data(OrgData, "example", OrgDataConfig(
            user_handle="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_handle" type="string" required>
  The real handle of the user this object is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Organizations across regions for the authenticated user. Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  Pagination links. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="next" type="string">
      Link to the next page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="prev" type="string">
      Link to the previous page. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string">
      Link to the current page. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Paging attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cursor" type="string">
          The cursor used to get the current results, if any. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Number of results returned. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_cursor" type="string">
          The cursor used to get the next results, if any. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_cursor" type="string">
          The cursor used to get the previous results, if any. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          Type of global orgs pagination. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
