---
title: "datadog_data_deletion_response_item"
description: "`datadog_data_deletion_response_item` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_data_deletion_response_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	data "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/data"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_data_deletion_response_item"})

    		ubx.Data(data.DeletionResponseItem, "example", data.DeletionResponseItemConfig{
    			NextPage: "example",
    			PageSize: 1,
    			Product:  "example",
    			Query:    "example",
    			Status:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DeletionResponseItem } from "@ubx/sdk-datadog/datadog/data/data/deletion_response_item";

    export default stack("example", () => {
      intent({ summary: "look up datadog_data_deletion_response_item" });

      data(DeletionResponseItem, "example", {
        nextPage: "example",
        pageSize: 1,
        product: "example",
        query: "example",
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.data import DeletionResponseItem, DeletionResponseItemConfig

    def describe():
        ubx.intent("look up datadog_data_deletion_response_item")

        ubx.data(DeletionResponseItem, "example", DeletionResponseItemConfig(
            next_page="example",
            page_size=1,
            product="example",
            query="example",
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="next_page" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="status" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The list of data deletion requests that matches the query. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  The metadata of the data deletion response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="count_product" type="map(number)">
      The total deletion requests created by product. Optional lookup argument.
    </ResponseField>
    <ResponseField name="count_status" type="map(number)">
      The total deletion requests created by status. Optional lookup argument.
    </ResponseField>
    <ResponseField name="next_page" type="string">
      The next page when searching deletion requests created in the current organization. Optional lookup argument.
    </ResponseField>
    <ResponseField name="product" type="string">
      The product of the deletion request. Optional lookup argument.
    </ResponseField>
    <ResponseField name="request_status" type="string">
      The status of the executed request. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
