---
title: "datadog_item_api_payload_data"
description: "`datadog_item_api_payload_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_item_api_payload_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	item "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/item"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_item_api_payload_data"})

    		ubx.Data(item.ApiPayloadData, "example", item.ApiPayloadDataConfig{
    			DatastoreId: "example",
    			Filter:      "example",
    			ItemKey:     "example",
    			Sort:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ApiPayloadData } from "@ubx/sdk-datadog/datadog/data/item/api_payload_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_item_api_payload_data" });

      data(ApiPayloadData, "example", {
        datastoreId: "example",
        filter: "example",
        itemKey: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.item import ApiPayloadData, ApiPayloadDataConfig

    def describe():
        ubx.intent("look up datadog_item_api_payload_data")

        ubx.data(ApiPayloadData, "example", ApiPayloadDataConfig(
            datastore_id="example",
            filter="example",
            item_key="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="datastore_id" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="item_key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of datastore items with their content and metadata. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional metadata about a collection of datastore items, including pagination and schema information. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination information for a collection of datastore items. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="has_more" type="bool">
          Whether there are additional pages of items beyond the current page. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_count" type="number">
          The total number of items in the datastore, ignoring any filters. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          The total number of items that match the current filter criteria. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="schema" type="object">
      Schema information about the datastore, including its primary key and field definitions. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="fields" type="list(object)">
          An array describing the columns available in this datastore. Optional lookup argument.
        </ResponseField>
        <ResponseField name="primary_key" type="string">
          The name of the primary key column for this datastore. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
