---
title: "datadog_default_rulesets_per_language_response"
description: "`datadog_default_rulesets_per_language_response` reads Datadog's own real, default static-analysis rulesets available per programming language."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	default_ "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/default_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_default_rulesets_per_language_response"})

    		ubx.Data(default_.RulesetsPerLanguageResponse, "example", default_.RulesetsPerLanguageResponseConfig{
    			Language: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { RulesetsPerLanguageResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/default_/rulesets_per_language_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_default_rulesets_per_language_response" });

      data(RulesetsPerLanguageResponse, "example", {
        language: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.default_ import RulesetsPerLanguageResponse, RulesetsPerLanguageResponseConfig

    def describe():
        ubx.intent("look up datadog_default_rulesets_per_language_response")

        ubx.data(RulesetsPerLanguageResponse, "example", RulesetsPerLanguageResponseConfig(
            language="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="language" type="string" required>
  The real programming language this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The primary data object in the default rulesets per language response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of the default rulesets per language response, containing the list of default ruleset names. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="rulesets" type="list(string)" required>
          The list of default ruleset names for the specified programming language. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The language identifier used as the resource identifier. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Default rulesets per language resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
