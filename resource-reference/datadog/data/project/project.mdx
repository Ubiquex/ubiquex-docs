---
title: "datadog_project"
description: "`datadog_project` looks up a real project -- a saved workspace grouping related notebooks, dashboards, or investigations."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	project "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/project"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_project"})

    		ubx.Data(project.Project, "example", project.ProjectConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Project } from "./local-sdk/datadog/sdk/typescript/datadog/data/project/project.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_project" });

      data(Project, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.project import Project, ProjectConfig

    def describe():
        ubx.intent("look up datadog_project")

        ubx.data(Project, "example", ProjectConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Projects response data. Returned by the lookup.
</ResponseField>
