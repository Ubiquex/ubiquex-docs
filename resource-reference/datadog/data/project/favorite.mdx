---
title: "datadog_project_favorite"
description: "`datadog_project_favorite` reads whether a real project has been marked a favorite by the current user."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	project "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/project"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_project_favorite"})

    		ubx.Data(project.Favorite, "example", project.FavoriteConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Favorite } from "@ubx/sdk-datadog/datadog/data/project/favorite";

    export default stack("example", () => {
      intent({ summary: "look up datadog_project_favorite" });

      data(Favorite, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.project import Favorite, FavoriteConfig

    def describe():
        ubx.intent("look up datadog_project_favorite")

        ubx.data(Favorite, "example", FavoriteConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of project favorites. Returned by the lookup.
</ResponseField>
