---
title: "datadog_okta_account_response_data"
description: "`datadog_okta_account_response_data` looks up a real Okta account connected via Datadog's own Okta integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	okta "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/okta"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_okta_account_response_data"})

    		ubx.Data(okta.AccountResponseData, "example", okta.AccountResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountResponseData } from "@ubx/sdk-datadog/datadog/data/okta/account_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_okta_account_response_data" });

      data(AccountResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.okta import AccountResponseData, AccountResponseDataConfig

    def describe():
        ubx.intent("look up datadog_okta_account_response_data")

        ubx.data(AccountResponseData, "example", AccountResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of Okta accounts. Returned by the lookup.
</ResponseField>
