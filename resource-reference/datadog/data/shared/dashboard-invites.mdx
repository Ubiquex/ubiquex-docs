---
title: "datadog_shared_dashboard_invites"
description: "`datadog_shared_dashboard_invites` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_shared_dashboard_invites` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shared "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/shared"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_shared_dashboard_invites"})

    		ubx.Data(shared.DashboardInvites, "example", shared.DashboardInvitesConfig{
    			Token:      "example",
    			PageNumber: 1,
    			PageSize:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { DashboardInvites } from "@ubx/sdk-datadog/datadog/data/shared/dashboard_invites";

    export default stack("example", () => {
      intent({ summary: "look up datadog_shared_dashboard_invites" });

      data(DashboardInvites, "example", {
        token: "example",
        pageNumber: 1,
        pageSize: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.shared import DashboardInvites, DashboardInvitesConfig

    def describe():
        ubx.intent("look up datadog_shared_dashboard_invites")

        ubx.data(DashboardInvites, "example", DashboardInvitesConfig(
            token="example",
            page_number=1,
            page_size=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="page_number" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="page_size" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="token" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="any">
  An object or list of objects containing the information for an invitation to a shared dashboard. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata returned by the API. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Object containing the total count of invitations across all pages Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          The total number of invitations on this shared board, across all pages. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
