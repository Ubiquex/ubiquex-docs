---
title: "datadog_security_monitoring_dataset_response"
description: "`datadog_security_monitoring_dataset_response` looks up a real Security Monitoring dataset -- an index of logs scanned by detection rules."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_security_monitoring_dataset_response"})

    		ubx.Data(security.MonitoringDatasetResponse, "example", security.MonitoringDatasetResponseConfig{
    			DatasetId: "example",
    			Version:   1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { MonitoringDatasetResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/security/monitoring_dataset_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_security_monitoring_dataset_response" });

      data(MonitoringDatasetResponse, "example", {
        datasetId: "example",
        version: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.security import MonitoringDatasetResponse, MonitoringDatasetResponseConfig

    def describe():
        ubx.intent("look up datadog_security_monitoring_dataset_response")

        ubx.data(MonitoringDatasetResponse, "example", MonitoringDatasetResponseConfig(
            dataset_id="example",
            version=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="dataset_id" type="string" required>
  The real, unique identifier of this dataset. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="version" type="number" required>
  The real version identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The data wrapper of a dataset response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of a Cloud SIEM dataset. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="string" required>
          The creation timestamp of the dataset, in ISO 8601 format. Required lookup argument.
        </ResponseField>
        <ResponseField name="created_by_handle" type="string" required>
          The Datadog handle of the user who created the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="created_by_name" type="string" required>
          The display name of the user who created the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="definition" type="object" required>
          The definition of the dataset. The shape depends on the value of `data_source`. Use `reference_table` or `managed_resource` for a referential dataset, or one of the event platform sources (for example `logs`, `audit`, `events`, `spans`, `rum`) for an event platform dataset. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="columns" type="list(object)">
              For event platform datasets, the list of columns exposed by the dataset. Optional lookup argument.
            </ResponseField>
            <ResponseField name="data_source" type="string" required>
              The data source backing this dataset definition. Required lookup argument.
            </ResponseField>
            <ResponseField name="indexes" type="list(string)">
              For event platform datasets, the list of indexes to query. Optional lookup argument.
            </ResponseField>
            <ResponseField name="name" type="string" required>
              The unique name of the dataset. Must start with a lowercase letter and contain only lowercase letters, digits, and underscores (max 255 characters). Required lookup argument.
            </ResponseField>
            <ResponseField name="query_filter" type="string">
              For referential datasets, an optional filter expression applied to the table. Optional lookup argument.
            </ResponseField>
            <ResponseField name="search" type="object">
              The search clause applied to an event platform dataset. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="query" type="string" required>
                  The search query expression. Required lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="storage" type="string">
              Storage tier the dataset reads from. Applies to event platform datasets. Optional lookup argument.
            </ResponseField>
            <ResponseField name="table_name" type="string">
              For referential datasets, the name of the underlying table. Optional lookup argument.
            </ResponseField>
            <ResponseField name="time_window" type="object">
              An optional time window that overrides the default query time range. Optional lookup argument.
              <Expandable title="properties">
                <ResponseField name="from" type="number">
                  Inclusive start of the time window, in milliseconds since the Unix epoch. Optional lookup argument.
                </ResponseField>
                <ResponseField name="to" type="number">
                  Exclusive end of the time window, in milliseconds since the Unix epoch. Optional lookup argument.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string" required>
          The description of the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="id" type="string" required>
          The UUID of the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="is_default" type="bool" required>
          Whether the dataset is an out-of-the-box dataset provided by Datadog. Required lookup argument.
        </ResponseField>
        <ResponseField name="is_deprecated" type="bool" required>
          Whether the dataset is marked as deprecated. Required lookup argument.
        </ResponseField>
        <ResponseField name="modified_at" type="string" required>
          The timestamp of the last modification of the dataset, in ISO 8601 format. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unique name of the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="updated_by_handle" type="string" required>
          The Datadog handle of the user who last updated the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="updated_by_name" type="string" required>
          The display name of the user who last updated the dataset. Required lookup argument.
        </ResponseField>
        <ResponseField name="version" type="number" required>
          The current version of the dataset. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The UUID of the dataset. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of resource for a dataset response. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
