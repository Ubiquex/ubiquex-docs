---
title: "datadog_security_monitoring_suppression"
description: "`datadog_security_monitoring_suppression` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_security_monitoring_suppression` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_security_monitoring_suppression"})

    		ubx.Data(security.MonitoringSuppression, "example", security.MonitoringSuppressionConfig{
    			Query: "example",
    			Sort:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitoringSuppression } from "@ubx/sdk-datadog/datadog/data/security/monitoring_suppression";

    export default stack("example", () => {
      intent({ summary: "look up datadog_security_monitoring_suppression" });

      data(MonitoringSuppression, "example", {
        query: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.security import MonitoringSuppression, MonitoringSuppressionConfig

    def describe():
        ubx.intent("look up datadog_security_monitoring_suppression")

        ubx.data(MonitoringSuppression, "example", MonitoringSuppressionConfig(
            query="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="query" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  The sort parameters used for querying suppression rules. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of suppressions objects. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for the suppression list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="page_number" type="number">
          Current page number. Optional lookup argument.
        </ResponseField>
        <ResponseField name="page_size" type="number">
          Current page size. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_count" type="number">
          Total count of suppressions. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
