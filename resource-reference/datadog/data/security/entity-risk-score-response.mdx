---
title: "datadog_security_entity_risk_score_response"
description: "`datadog_security_entity_risk_score_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_security_entity_risk_score_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_security_entity_risk_score_response"})

    		ubx.Data(security.EntityRiskScoreResponse, "example", security.EntityRiskScoreResponseConfig{
    			EntityId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { EntityRiskScoreResponse } from "@ubx/sdk-datadog/datadog/data/security/entity_risk_score_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_security_entity_risk_score_response" });

      data(EntityRiskScoreResponse, "example", {
        entityId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.security import EntityRiskScoreResponse, EntityRiskScoreResponseConfig

    def describe():
        ubx.intent("look up datadog_security_entity_risk_score_response")

        ubx.data(EntityRiskScoreResponse, "example", EntityRiskScoreResponseConfig(
            entity_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="entity_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  An entity risk score containing security risk assessment information Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of an entity risk score. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="account_ids" type="list(string)" required>
          Cloud account IDs associated with the entity. Required lookup argument.
        </ResponseField>
        <ResponseField name="config_risks" type="object" required>
          Configuration risks associated with the entity Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="has_identity_risk" type="bool" required>
              Whether the entity has identity risks Required lookup argument.
            </ResponseField>
            <ResponseField name="has_misconfiguration" type="bool" required>
              Whether the entity has misconfigurations Required lookup argument.
            </ResponseField>
            <ResponseField name="has_privileged_role" type="bool" required>
              Whether the entity has privileged roles Required lookup argument.
            </ResponseField>
            <ResponseField name="is_privileged" type="bool" required>
              Whether the entity has privileged access Required lookup argument.
            </ResponseField>
            <ResponseField name="is_production" type="bool" required>
              Whether the entity is in a production environment Required lookup argument.
            </ResponseField>
            <ResponseField name="is_publicly_accessible" type="bool" required>
              Whether the entity is publicly accessible Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entity_metadata" type="object" required>
          Metadata about the entity from cloud providers Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="account_id" type="string">
              Cloud account ID (AWS) Optional lookup argument.
            </ResponseField>
            <ResponseField name="environments" type="list(string)" required>
              Environment tags associated with the entity Required lookup argument.
            </ResponseField>
            <ResponseField name="mitre_tactics" type="list(string)" required>
              MITRE ATT&CK tactics detected Required lookup argument.
            </ResponseField>
            <ResponseField name="mitre_techniques" type="list(string)" required>
              MITRE ATT&CK techniques detected Required lookup argument.
            </ResponseField>
            <ResponseField name="project_id" type="string">
              Cloud project ID (GCP) Optional lookup argument.
            </ResponseField>
            <ResponseField name="services" type="list(string)" required>
              Services associated with the entity Required lookup argument.
            </ResponseField>
            <ResponseField name="sources" type="list(string)" required>
              Data sources that detected this entity Required lookup argument.
            </ResponseField>
            <ResponseField name="subscription_id" type="string">
              Cloud subscription ID (Azure) Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="entity_name" type="string">
          Human-readable name of the entity. Optional lookup argument.
        </ResponseField>
        <ResponseField name="entity_providers" type="list(string)" required>
          Cloud providers associated with the entity. Required lookup argument.
        </ResponseField>
        <ResponseField name="entity_roles" type="list(string)">
          Roles associated with the entity. Optional lookup argument.
        </ResponseField>
        <ResponseField name="entity_sub_types" type="list(string)" required>
          Sub-types associated with the entity. Required lookup argument.
        </ResponseField>
        <ResponseField name="entity_type" type="string">
          Type of the entity (for example, aws_iam_user, aws_ec2_instance). Optional lookup argument.
        </ResponseField>
        <ResponseField name="entity_types" type="list(string)">
          All types associated with the entity. Optional lookup argument.
        </ResponseField>
        <ResponseField name="first_detected" type="number" required>
          Timestamp when the entity was first detected (Unix milliseconds). Required lookup argument.
        </ResponseField>
        <ResponseField name="last_activity_title" type="string" required>
          Title of the most recent signal detected for this entity. Required lookup argument.
        </ResponseField>
        <ResponseField name="last_detected" type="number" required>
          Timestamp when the entity was last detected (Unix milliseconds). Required lookup argument.
        </ResponseField>
        <ResponseField name="risk_score" type="number" required>
          Current risk score for the entity. Required lookup argument.
        </ResponseField>
        <ResponseField name="risk_score_evolution" type="number" required>
          Change in risk score compared to previous period. Required lookup argument.
        </ResponseField>
        <ResponseField name="severity" type="string" required>
          Severity level based on risk score Required lookup argument.
        </ResponseField>
        <ResponseField name="signals_detected" type="number" required>
          Number of security signals detected for this entity. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier for the entity Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
