---
title: "datadog_security_monitoring_content_pack_state_data"
description: "`datadog_security_monitoring_content_pack_state_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_security_monitoring_content_pack_state_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_security_monitoring_content_pack_state_data"})

    		ubx.Data(security.MonitoringContentPackStateData, "example", security.MonitoringContentPackStateDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitoringContentPackStateData } from "@ubx/sdk-datadog/datadog/data/security/monitoring_content_pack_state_data";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_security_monitoring_content_pack_state_data",
      });

      data(MonitoringContentPackStateData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.security import MonitoringContentPackStateData, MonitoringContentPackStateDataConfig

    def describe():
        ubx.intent("look up datadog_security_monitoring_content_pack_state_data")

        ubx.data(MonitoringContentPackStateData, "example", MonitoringContentPackStateDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of content pack states. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for content pack states. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="cloud_siem_index_incorrect" type="bool" required>
      Whether the Cloud SIEM index configuration is incorrect for the organization. Required lookup argument.
    </ResponseField>
    <ResponseField name="retention_months" type="number">
      The number of months that standard logs are retained for organizations on the standalone_indexed` pricing model. This field is omitted for other pricing models. Optional lookup argument.
    </ResponseField>
    <ResponseField name="sku" type="string" required>
      The Cloud SIEM pricing model (SKU) for the organization. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
