---
title: "datadog_security_monitoring_signal_suggested_action"
description: "`datadog_security_monitoring_signal_suggested_action` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_security_monitoring_signal_suggested_action` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_security_monitoring_signal_suggested_action"})

    		ubx.Data(security.MonitoringSignalSuggestedAction, "example", security.MonitoringSignalSuggestedActionConfig{
    			SignalId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { MonitoringSignalSuggestedAction } from "@ubx/sdk-datadog/datadog/data/security/monitoring_signal_suggested_action";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_security_monitoring_signal_suggested_action",
      });

      data(MonitoringSignalSuggestedAction, "example", {
        signalId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.security import MonitoringSignalSuggestedAction, MonitoringSignalSuggestedActionConfig

    def describe():
        ubx.intent("look up datadog_security_monitoring_signal_suggested_action")

        ubx.data(MonitoringSignalSuggestedAction, "example", MonitoringSignalSuggestedActionConfig(
            signal_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="signal_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of suggested actions for a security signal. Returned by the lookup.
</ResponseField>
