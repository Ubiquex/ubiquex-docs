---
title: "datadog_security_filter_version"
description: "`datadog_security_filter_version` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_security_filter_version` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_security_filter_version"})

    		ubx.Data(security.FilterVersion, "example", security.FilterVersionConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FilterVersion } from "@ubx/sdk-datadog/datadog/data/security/filter_version";

    export default stack("example", () => {
      intent({ summary: "look up datadog_security_filter_version" });

      data(FilterVersion, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.security import FilterVersion, FilterVersionConfig

    def describe():
        ubx.intent("look up datadog_security_filter_version")

        ubx.data(FilterVersion, "example", FilterVersionConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of historical security filter configurations, ordered from the most recent to the oldest. Returned by the lookup.
</ResponseField>
