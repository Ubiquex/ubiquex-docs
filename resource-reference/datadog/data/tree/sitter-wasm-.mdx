---
title: "datadog_tree_sitter_wasm"
description: "`datadog_tree_sitter_wasm` reads a real, compiled Tree-sitter WebAssembly grammar module used for source-code parsing."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tree "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/tree"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_tree_sitter_wasm"})

    		ubx.Data(tree.SitterWasm, "example", tree.SitterWasmConfig{
    			File: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SitterWasm } from "@ubx/sdk-datadog/datadog/data/tree/sitter_wasm_";

    export default stack("example", () => {
      intent({ summary: "look up datadog_tree_sitter_wasm" });

      data(SitterWasm, "example", {
        file: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.tree import SitterWasm, SitterWasmConfig

    def describe():
        ubx.intent("look up datadog_tree_sitter_wasm")

        ubx.data(SitterWasm, "example", SitterWasmConfig(
            file="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="file" type="string" required>
  The real file this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

_No computed result fields._
