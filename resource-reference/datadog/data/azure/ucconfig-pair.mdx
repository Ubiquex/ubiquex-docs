---
title: "datadog_azure_ucconfig_pair"
description: "`datadog_azure_ucconfig_pair` looks up a real Azure usage-and-cost configuration pair feeding Cloud Cost Management."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	azure "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/azure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_azure_ucconfig_pair"})

    		ubx.Data(azure.UcconfigPair, "example", azure.UcconfigPairConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UcconfigPair } from "@ubx/sdk-datadog/datadog/data/azure/ucconfig_pair";

    export default stack("example", () => {
      intent({ summary: "look up datadog_azure_ucconfig_pair" });

      data(UcconfigPair, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.azure import UcconfigPair, UcconfigPairConfig

    def describe():
        ubx.intent("look up datadog_azure_ucconfig_pair")

        ubx.data(UcconfigPair, "example", UcconfigPairConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An Azure config pair. Returned by the lookup.
</ResponseField>
