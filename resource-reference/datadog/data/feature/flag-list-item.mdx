---
title: "datadog_feature_flag_list_item"
description: "`datadog_feature_flag_list_item` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_feature_flag_list_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	feature "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/feature"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_feature_flag_list_item"})

    		ubx.Data(feature.FlagListItem, "example", feature.FlagListItemConfig{
    			IsArchived: true,
    			Key:        "example",
    			Limit:      1,
    			Offset:     1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FlagListItem } from "@ubx/sdk-datadog/datadog/data/feature/flag_list_item";

    export default stack("example", () => {
      intent({ summary: "look up datadog_feature_flag_list_item" });

      data(FlagListItem, "example", {
        isArchived: true,
        key: "example",
        limit: 1,
        offset: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.feature import FlagListItem, FlagListItemConfig

    def describe():
        ubx.intent("look up datadog_feature_flag_list_item")

        ubx.data(FlagListItem, "example", FlagListItemConfig(
            is_archived=True,
            key="example",
            limit=1,
            offset=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="is_archived" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="key" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="offset" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of feature flags. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Pagination metadata for feature flags. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination metadata for feature flags list responses. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total number of items. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total number of items matching the filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
