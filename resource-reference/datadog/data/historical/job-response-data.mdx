---
title: "datadog_historical_job_response_data"
description: "`datadog_historical_job_response_data` reads the real, current status of a historical backfill job."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	historical "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/historical"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_historical_job_response_data"})

    		ubx.Data(historical.JobResponseData, "example", historical.JobResponseDataConfig{
    			Sort: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { JobResponseData } from "./local-sdk/datadog/sdk/typescript/datadog/data/historical/job_response_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_historical_job_response_data" });

      data(JobResponseData, "example", {
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.historical import JobResponseData, JobResponseDataConfig

    def describe():
        ubx.intent("look up datadog_historical_job_response_data")

        ubx.data(JobResponseData, "example", JobResponseDataConfig(
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="sort" type="string">
  The field results are sorted by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array containing the list of historical jobs. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata about the list of jobs. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="total_count" type="number">
      Number of jobs in the list. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
