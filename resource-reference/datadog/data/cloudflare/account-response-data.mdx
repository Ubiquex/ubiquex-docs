---
title: "datadog_cloudflare_account_response_data"
description: "`datadog_cloudflare_account_response_data` looks up a real Cloudflare account connected via Datadog's own Cloudflare integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudflare "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/cloudflare"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_cloudflare_account_response_data"})

    		ubx.Data(cloudflare.AccountResponseData, "example", cloudflare.AccountResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountResponseData } from "@ubx/sdk-datadog/datadog/data/cloudflare/account_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_cloudflare_account_response_data" });

      data(AccountResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.cloudflare import AccountResponseData, AccountResponseDataConfig

    def describe():
        ubx.intent("look up datadog_cloudflare_account_response_data")

        ubx.data(AccountResponseData, "example", AccountResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The JSON:API data schema. Returned by the lookup.
</ResponseField>
