---
title: "datadog_user_override_identity_provider_data"
description: "`datadog_user_override_identity_provider_data` reads a real, manual override of which identity provider a specific user authenticates through."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	user "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/user"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_user_override_identity_provider_data"})

    		ubx.Data(user.OverrideIdentityProviderData, "example", user.OverrideIdentityProviderDataConfig{
    			UserId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { OverrideIdentityProviderData } from "./local-sdk/datadog/sdk/typescript/datadog/data/user/override_identity_provider_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_user_override_identity_provider_data" });

      data(OverrideIdentityProviderData, "example", {
        userId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.user import OverrideIdentityProviderData, OverrideIdentityProviderDataConfig

    def describe():
        ubx.intent("look up datadog_user_override_identity_provider_data")

        ubx.data(OverrideIdentityProviderData, "example", OverrideIdentityProviderDataConfig(
            user_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_id" type="string" required>
  The real, unique identifier of this user. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of user identity provider override data objects. Returned by the lookup.
</ResponseField>
