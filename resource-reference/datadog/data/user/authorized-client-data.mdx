---
title: "datadog_user_authorized_client_data"
description: "`datadog_user_authorized_client_data` looks up a real OAuth client a user has authorized."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	user "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/user"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_user_authorized_client_data"})

    		ubx.Data(user.AuthorizedClientData, "example", user.AuthorizedClientDataConfig{
    			OrgAuthorizedClientId: "example",
    			Sort:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { AuthorizedClientData } from "./local-sdk/datadog/sdk/typescript/datadog/data/user/authorized_client_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_user_authorized_client_data" });

      data(AuthorizedClientData, "example", {
        orgAuthorizedClientId: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.user import AuthorizedClientData, AuthorizedClientDataConfig

    def describe():
        ubx.intent("look up datadog_user_authorized_client_data")

        ubx.data(AuthorizedClientData, "example", AuthorizedClientDataConfig(
            org_authorized_client_id="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="org_authorized_client_id" type="string" required>
  The real, unique identifier of this organization-authorized OAuth client. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Field to sort user authorizations by. Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  List of user authorized client data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Object describing meta attributes of response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination object. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="total_count" type="number">
          Total count. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total_filtered_count" type="number">
          Total count of elements matched by the filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
