---
title: "datadog_user_response_included_item"
description: "`datadog_user_response_included_item` reads a real, related item included alongside a user lookup."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	user "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/user"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_user_response_included_item"})

    		ubx.Data(user.ResponseIncludedItem, "example", user.ResponseIncludedItemConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ResponseIncludedItem } from "./local-sdk/datadog/sdk/typescript/datadog/data/user/response_included_item.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_user_response_included_item" });

      data(ResponseIncludedItem, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.user import ResponseIncludedItem, ResponseIncludedItemConfig

    def describe():
        ubx.intent("look up datadog_user_response_included_item")

        ubx.data(ResponseIncludedItem, "example", ResponseIncludedItemConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  User object returned by the API. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of user object returned by the API. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          The ISO 8601 timestamp of when the user account was created. Optional lookup argument.
        </ResponseField>
        <ResponseField name="disabled" type="bool">
          Whether the user account is deactivated. Disabled users cannot log in. Optional lookup argument.
        </ResponseField>
        <ResponseField name="email" type="string">
          The email address of the user, used for login and notifications. Optional lookup argument.
        </ResponseField>
        <ResponseField name="handle" type="string">
          The unique handle (username) of the user, typically matching their email prefix. Optional lookup argument.
        </ResponseField>
        <ResponseField name="icon" type="string">
          URL of the user's profile icon, typically a Gravatar URL derived from the email address. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_login_time" type="string">
          The ISO 8601 timestamp of the user's most recent login, or null if the user has never logged in. Returned by the lookup.
        </ResponseField>
        <ResponseField name="mfa_enabled" type="bool">
          Whether multi-factor authentication (MFA) is enabled for the user's account. Returned by the lookup.
        </ResponseField>
        <ResponseField name="modified_at" type="string">
          The ISO 8601 timestamp of when the user account was last modified. Optional lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string">
          The full display name of the user as shown in the Datadog UI. Optional lookup argument.
        </ResponseField>
        <ResponseField name="service_account" type="bool">
          Whether this is a service account rather than a human user. Service accounts are used for programmatic API access. Optional lookup argument.
        </ResponseField>
        <ResponseField name="status" type="string">
          The current status of the user account (for example, `Active`, `Pending`, or `Disabled`). Optional lookup argument.
        </ResponseField>
        <ResponseField name="title" type="string">
          The job title of the user (for example, "Senior Engineer" or "Product Manager"). Optional lookup argument.
        </ResponseField>
        <ResponseField name="uuid" type="string">
          The globally unique identifier (UUID) of the user. Returned by the lookup.
        </ResponseField>
        <ResponseField name="verified" type="bool">
          Whether the user's email address has been verified. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      ID of the user. Optional lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships of the user object returned by the API. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="org" type="object">
          Relationship to an organization. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Relationship to organization object. Required lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="other_orgs" type="object">
          Relationship to organizations. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)" required>
              Relationships to organization objects. Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="other_users" type="object">
          Relationship to users. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)" required>
              Relationships to user objects. Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="roles" type="object">
          Relationship to roles. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)">
              An array containing type and the unique identifier of a role. Optional lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      Users resource type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="included" type="list(object)">
  Array of objects related to the user. Returned by the lookup.
</ResponseField>
