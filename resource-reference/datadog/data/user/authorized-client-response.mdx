---
title: "datadog_user_authorized_client_response"
description: "`datadog_user_authorized_client_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_user_authorized_client_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	user "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/user"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_user_authorized_client_response"})

    		ubx.Data(user.AuthorizedClientResponse, "example", user.AuthorizedClientResponseConfig{
    			UserAuthorizedClientId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AuthorizedClientResponse } from "@ubx/sdk-datadog/datadog/data/user/authorized_client_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_user_authorized_client_response" });

      data(AuthorizedClientResponse, "example", {
        userAuthorizedClientId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.user import AuthorizedClientResponse, AuthorizedClientResponseConfig

    def describe():
        ubx.intent("look up datadog_user_authorized_client_response")

        ubx.data(AuthorizedClientResponse, "example", AuthorizedClientResponseConfig(
            user_authorized_client_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="user_authorized_client_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data object representing a user authorized client. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of a user authorized client. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="string" required>
          The date and time this authorization was created. Required lookup argument.
        </ResponseField>
        <ResponseField name="disabled" type="bool" required>
          Whether the user has disabled this authorization. Required lookup argument.
        </ResponseField>
        <ResponseField name="last_exercised" type="string" required>
          The date and time this authorization was last exercised. Required lookup argument.
        </ResponseField>
        <ResponseField name="modified_at" type="string" required>
          The date and time this authorization was last modified. Required lookup argument.
        </ResponseField>
        <ResponseField name="org_disabled" type="bool" required>
          Whether the organization has disabled this authorization. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the user authorized client. Required lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object" required>
      Relationships for a user authorized client. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="oauth2_client" type="object" required>
          Relationship to the OAuth2 client that was authorized. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Data identifying the OAuth2 client that was authorized. Required lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scopes" type="object" required>
          Relationship to the scopes granted to the OAuth2 client. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="list(object)" required>
              List of scope relationship data objects. Required lookup argument.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="user" type="object" required>
          Relationship to the user who granted this authorization. Required lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Data identifying the user who granted this authorization. Required lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The resource type for user authorized clients. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
