---
title: "datadog_commitments_list_item"
description: "`datadog_commitments_list_item` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_commitments_list_item` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	commitments "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/commitments"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_commitments_list_item"})

    		ubx.Data(commitments.ListItem, "example", commitments.ListItemConfig{
    			End:            1,
    			Product:        "example",
    			Provider:       "example",
    			Start:          1,
    			CommitmentType: "example",
    			FilterBy:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListItem } from "@ubx/sdk-datadog/datadog/data/commitments/list_item";

    export default stack("example", () => {
      intent({ summary: "look up datadog_commitments_list_item" });

      data(ListItem, "example", {
        end: 1,
        product: "example",
        provider: "example",
        start: 1,
        commitmentType: "example",
        filterBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.commitments import ListItem, ListItemConfig

    def describe():
        ubx.intent("look up datadog_commitments_list_item")

        ubx.data(ListItem, "example", ListItemConfig(
            end=1,
            product="example",
            provider="example",
            start=1,
            commitment_type="example",
            filter_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="commitment_type" type="string">
  Type of commitment. ri for Reserved Instances, sp for Savings Plans. Optional lookup argument.
</ResponseField>

<ResponseField name="end" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Cloud provider for commitment programs. Required lookup argument.
</ResponseField>

<ResponseField name="start" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="commitments" type="list(object)">
  Array of commitment items. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for a commitments list response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="committed_spend_unit" type="object">
      Unit metadata for a numeric metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="family" type="string" required>
          The unit family (for example, percentage or money). Required lookup argument.
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The unit identifier. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unit name (for example, percent or dollar). Required lookup argument.
        </ResponseField>
        <ResponseField name="plural" type="string" required>
          The plural form of the unit name. Required lookup argument.
        </ResponseField>
        <ResponseField name="scale_factor" type="number" required>
          The scale factor for the unit. Required lookup argument.
        </ResponseField>
        <ResponseField name="short_name" type="string" required>
          The abbreviated unit name (for example, % or $). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
