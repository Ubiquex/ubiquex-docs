---
title: "datadog_commitments_savings_timeseries_response"
description: "`datadog_commitments_savings_timeseries_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_commitments_savings_timeseries_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	commitments "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/commitments"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_commitments_savings_timeseries_response"})

    		ubx.Data(commitments.SavingsTimeseriesResponse, "example", commitments.SavingsTimeseriesResponseConfig{
    			End:      1,
    			Product:  "example",
    			Provider: "example",
    			Start:    1,
    			FilterBy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { SavingsTimeseriesResponse } from "@ubx/sdk-datadog/datadog/data/commitments/savings_timeseries_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_commitments_savings_timeseries_response",
      });

      data(SavingsTimeseriesResponse, "example", {
        end: 1,
        product: "example",
        provider: "example",
        start: 1,
        filterBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.commitments import SavingsTimeseriesResponse, SavingsTimeseriesResponseConfig

    def describe():
        ubx.intent("look up datadog_commitments_savings_timeseries_response")

        ubx.data(SavingsTimeseriesResponse, "example", SavingsTimeseriesResponseConfig(
            end=1,
            product="example",
            provider="example",
            start=1,
            filter_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Cloud provider for commitment programs. Required lookup argument.
</ResponseField>

<ResponseField name="start" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="actual_cost" type="object">
  A timeseries metric containing timestamps, series values, and optional unit metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="series" type="map(list(number))" required>
      Timeseries data as a map of series names to their corresponding value arrays. Required lookup argument.
    </ResponseField>
    <ResponseField name="times" type="list(number)" required>
      Unix timestamps in seconds for the timeseries data points. Required lookup argument.
    </ResponseField>
    <ResponseField name="unit" type="object">
      Unit metadata for a numeric metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="family" type="string" required>
          The unit family (for example, percentage or money). Required lookup argument.
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The unit identifier. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unit name (for example, percent or dollar). Required lookup argument.
        </ResponseField>
        <ResponseField name="plural" type="string" required>
          The plural form of the unit name. Required lookup argument.
        </ResponseField>
        <ResponseField name="scale_factor" type="number" required>
          The scale factor for the unit. Required lookup argument.
        </ResponseField>
        <ResponseField name="short_name" type="string" required>
          The abbreviated unit name (for example, % or $). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="effective_savings_rate" type="object">
  A timeseries metric containing timestamps, series values, and optional unit metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="series" type="map(list(number))" required>
      Timeseries data as a map of series names to their corresponding value arrays. Required lookup argument.
    </ResponseField>
    <ResponseField name="times" type="list(number)" required>
      Unix timestamps in seconds for the timeseries data points. Required lookup argument.
    </ResponseField>
    <ResponseField name="unit" type="object">
      Unit metadata for a numeric metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="family" type="string" required>
          The unit family (for example, percentage or money). Required lookup argument.
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The unit identifier. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unit name (for example, percent or dollar). Required lookup argument.
        </ResponseField>
        <ResponseField name="plural" type="string" required>
          The plural form of the unit name. Required lookup argument.
        </ResponseField>
        <ResponseField name="scale_factor" type="number" required>
          The scale factor for the unit. Required lookup argument.
        </ResponseField>
        <ResponseField name="short_name" type="string" required>
          The abbreviated unit name (for example, % or $). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="on_demand_equivalent_cost" type="object">
  A timeseries metric containing timestamps, series values, and optional unit metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="series" type="map(list(number))" required>
      Timeseries data as a map of series names to their corresponding value arrays. Required lookup argument.
    </ResponseField>
    <ResponseField name="times" type="list(number)" required>
      Unix timestamps in seconds for the timeseries data points. Required lookup argument.
    </ResponseField>
    <ResponseField name="unit" type="object">
      Unit metadata for a numeric metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="family" type="string" required>
          The unit family (for example, percentage or money). Required lookup argument.
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The unit identifier. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unit name (for example, percent or dollar). Required lookup argument.
        </ResponseField>
        <ResponseField name="plural" type="string" required>
          The plural form of the unit name. Required lookup argument.
        </ResponseField>
        <ResponseField name="scale_factor" type="number" required>
          The scale factor for the unit. Required lookup argument.
        </ResponseField>
        <ResponseField name="short_name" type="string" required>
          The abbreviated unit name (for example, % or $). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="realized_savings" type="object">
  A timeseries metric containing timestamps, series values, and optional unit metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="series" type="map(list(number))" required>
      Timeseries data as a map of series names to their corresponding value arrays. Required lookup argument.
    </ResponseField>
    <ResponseField name="times" type="list(number)" required>
      Unix timestamps in seconds for the timeseries data points. Required lookup argument.
    </ResponseField>
    <ResponseField name="unit" type="object">
      Unit metadata for a numeric metric. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="family" type="string" required>
          The unit family (for example, percentage or money). Required lookup argument.
        </ResponseField>
        <ResponseField name="id" type="number" required>
          The unit identifier. Required lookup argument.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unit name (for example, percent or dollar). Required lookup argument.
        </ResponseField>
        <ResponseField name="plural" type="string" required>
          The plural form of the unit name. Required lookup argument.
        </ResponseField>
        <ResponseField name="scale_factor" type="number" required>
          The scale factor for the unit. Required lookup argument.
        </ResponseField>
        <ResponseField name="short_name" type="string" required>
          The abbreviated unit name (for example, % or $). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
