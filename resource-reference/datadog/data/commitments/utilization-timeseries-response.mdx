---
title: "datadog_commitments_utilization_timeseries_response"
description: "`datadog_commitments_utilization_timeseries_response` reads the real, time-series utilization rate of cloud commitments over time."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	commitments "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/commitments"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_commitments_utilization_timeseries_response"})

    		ubx.Data(commitments.UtilizationTimeseriesResponse, "example", commitments.UtilizationTimeseriesResponseConfig{
    			End:            1,
    			Product:        "example",
    			Provider:       "example",
    			Start:          1,
    			CommitmentType: "example",
    			FilterBy:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UtilizationTimeseriesResponse } from "@ubx/sdk-datadog/datadog/data/commitments/utilization_timeseries_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_commitments_utilization_timeseries_response",
      });

      data(UtilizationTimeseriesResponse, "example", {
        end: 1,
        product: "example",
        provider: "example",
        start: 1,
        commitmentType: "example",
        filterBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.commitments import UtilizationTimeseriesResponse, UtilizationTimeseriesResponseConfig

    def describe():
        ubx.intent("look up datadog_commitments_utilization_timeseries_response")

        ubx.data(UtilizationTimeseriesResponse, "example", UtilizationTimeseriesResponseConfig(
            end=1,
            product="example",
            provider="example",
            start=1,
            commitment_type="example",
            filter_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="commitment_type" type="string">
  Type of commitment. ri for Reserved Instances, sp for Savings Plans. Optional lookup argument.
</ResponseField>

<ResponseField name="end" type="number" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="string">
  The real field results are filtered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  The real Datadog product this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Cloud provider for commitment programs. Required lookup argument.
</ResponseField>

<ResponseField name="start" type="number" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="series" type="map(list(number))">
  Timeseries data as a map of series names to their corresponding value arrays. Returned by the lookup.
</ResponseField>

<ResponseField name="times" type="list(number)">
  Unix timestamps in seconds for the timeseries data points. Returned by the lookup.
</ResponseField>

<ResponseField name="unit" type="object">
  Unit metadata for a numeric metric. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="family" type="string" required>
      The unit family (for example, percentage or money). Required lookup argument.
    </ResponseField>
    <ResponseField name="id" type="number" required>
      The unit identifier. Required lookup argument.
    </ResponseField>
    <ResponseField name="name" type="string" required>
      The unit name (for example, percent or dollar). Required lookup argument.
    </ResponseField>
    <ResponseField name="plural" type="string" required>
      The plural form of the unit name. Required lookup argument.
    </ResponseField>
    <ResponseField name="scale_factor" type="number" required>
      The scale factor for the unit. Required lookup argument.
    </ResponseField>
    <ResponseField name="short_name" type="string" required>
      The abbreviated unit name (for example, % or $). Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
