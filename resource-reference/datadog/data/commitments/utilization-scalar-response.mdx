---
title: "datadog_commitments_utilization_scalar_response"
description: "`datadog_commitments_utilization_scalar_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_commitments_utilization_scalar_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	commitments "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/commitments"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_commitments_utilization_scalar_response"})

    		ubx.Data(commitments.UtilizationScalarResponse, "example", commitments.UtilizationScalarResponseConfig{
    			End:            1,
    			Product:        "example",
    			Provider:       "example",
    			Start:          1,
    			CommitmentType: "example",
    			FilterBy:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { UtilizationScalarResponse } from "@ubx/sdk-datadog/datadog/data/commitments/utilization_scalar_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_commitments_utilization_scalar_response",
      });

      data(UtilizationScalarResponse, "example", {
        end: 1,
        product: "example",
        provider: "example",
        start: 1,
        commitmentType: "example",
        filterBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.commitments import UtilizationScalarResponse, UtilizationScalarResponseConfig

    def describe():
        ubx.intent("look up datadog_commitments_utilization_scalar_response")

        ubx.data(UtilizationScalarResponse, "example", UtilizationScalarResponseConfig(
            end=1,
            product="example",
            provider="example",
            start=1,
            commitment_type="example",
            filter_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="commitment_type" type="string">
  Type of commitment. ri for Reserved Instances, sp for Savings Plans. Optional lookup argument.
</ResponseField>

<ResponseField name="end" type="number" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Cloud provider for commitment programs. Required lookup argument.
</ResponseField>

<ResponseField name="start" type="number" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="columns" type="list(object)">
  Array of scalar columns in the response. Returned by the lookup.
</ResponseField>

<ResponseField name="product_breakdown" type="list(object)">
  Array of per-product utilization breakdown entries. Returned by the lookup.
</ResponseField>
