---
title: "datadog_commitments_on_demand_hotspots_scalar_response"
description: "`datadog_commitments_on_demand_hotspots_scalar_response` reads real, aggregated on-demand spend hotspots not covered by any existing commitment."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	commitments "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/commitments"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_commitments_on_demand_hotspots_scalar_response"})

    		ubx.Data(commitments.OnDemandHotspotsScalarResponse, "example", commitments.OnDemandHotspotsScalarResponseConfig{
    			End:      1,
    			Product:  "example",
    			Provider: "example",
    			Start:    1,
    			FilterBy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { OnDemandHotspotsScalarResponse } from "@ubx/sdk-datadog/datadog/data/commitments/on_demand_hotspots_scalar_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_commitments_on_demand_hotspots_scalar_response",
      });

      data(OnDemandHotspotsScalarResponse, "example", {
        end: 1,
        product: "example",
        provider: "example",
        start: 1,
        filterBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.commitments import OnDemandHotspotsScalarResponse, OnDemandHotspotsScalarResponseConfig

    def describe():
        ubx.intent("look up datadog_commitments_on_demand_hotspots_scalar_response")

        ubx.data(OnDemandHotspotsScalarResponse, "example", OnDemandHotspotsScalarResponseConfig(
            end=1,
            product="example",
            provider="example",
            start=1,
            filter_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end" type="number" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="string">
  The real field results are filtered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  The real Datadog product this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Cloud provider for commitment programs. Required lookup argument.
</ResponseField>

<ResponseField name="start" type="number" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="columns" type="list(object)">
  Array of scalar columns in the response. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for the on-demand hot-spots scalar response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="on_demand_filters" type="string" required>
      Active on-demand filters applied to the response. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total" type="list(object)">
  Array of scalar columns in the response. Returned by the lookup.
</ResponseField>
