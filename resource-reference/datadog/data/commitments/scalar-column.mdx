---
title: "datadog_commitments_scalar_column"
description: "`datadog_commitments_scalar_column` reads one real, aggregated column of commitment data."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	commitments "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/commitments"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_commitments_scalar_column"})

    		ubx.Data(commitments.ScalarColumn, "example", commitments.ScalarColumnConfig{
    			End:      1,
    			Product:  "example",
    			Provider: "example",
    			Start:    1,
    			FilterBy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { ScalarColumn } from "./local-sdk/datadog/sdk/typescript/datadog/data/commitments/scalar_column.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_commitments_scalar_column" });

      data(ScalarColumn, "example", {
        end: 1,
        product: "example",
        provider: "example",
        start: 1,
        filterBy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.commitments import ScalarColumn, ScalarColumnConfig

    def describe():
        ubx.intent("look up datadog_commitments_scalar_column")

        ubx.data(ScalarColumn, "example", ScalarColumnConfig(
            end=1,
            product="example",
            provider="example",
            start=1,
            filter_by="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="end" type="number" required>
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="filter_by" type="string">
  The real field results are filtered by. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="product" type="string" required>
  The real Datadog product this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="provider" type="string" required>
  Cloud provider for commitment programs. Required lookup argument.
</ResponseField>

<ResponseField name="start" type="number" required>
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="columns" type="list(object)">
  Array of scalar columns in the response. Returned by the lookup.
</ResponseField>
