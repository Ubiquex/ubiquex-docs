---
title: "datadog_awsevent_bridge_account_configuration"
description: "`datadog_awsevent_bridge_account_configuration` looks up a real AWS EventBridge source configuration feeding events into Datadog."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	awsevent "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/awsevent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_awsevent_bridge_account_configuration"})

    		ubx.Data(awsevent.BridgeAccountConfiguration, "example", awsevent.BridgeAccountConfigurationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { BridgeAccountConfiguration } from "./local-sdk/datadog/sdk/typescript/datadog/data/awsevent/bridge_account_configuration.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_awsevent_bridge_account_configuration" });

      data(BridgeAccountConfiguration, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.awsevent import BridgeAccountConfiguration, BridgeAccountConfigurationConfig

    def describe():
        ubx.intent("look up datadog_awsevent_bridge_account_configuration")

        ubx.data(BridgeAccountConfiguration, "example", BridgeAccountConfigurationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="accounts" type="list(object)">
  List of accounts with their event sources. Returned by the lookup.
</ResponseField>

<ResponseField name="is_installed" type="bool">
  True if the EventBridge sub-integration is enabled for your organization. Returned by the lookup.
</ResponseField>
