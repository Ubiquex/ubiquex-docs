---
title: "datadog_network_health_insight"
description: "`datadog_network_health_insight` reads a real, surfaced insight about network health from Network Device Monitoring."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	network "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/network"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_network_health_insight"})

    		ubx.Data(network.HealthInsight, "example", network.HealthInsightConfig{
    			From: "example",
    			To:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { HealthInsight } from "./local-sdk/datadog/sdk/typescript/datadog/data/network/health_insight.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_network_health_insight" });

      data(HealthInsight, "example", {
        from: "example",
        to: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.network import HealthInsight, HealthInsightConfig

    def describe():
        ubx.intent("look up datadog_network_health_insight")

        ubx.data(HealthInsight, "example", HealthInsightConfig(
            from_="example",
            to="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="from" type="string">
  The real, inclusive start of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="to" type="string">
  The real, inclusive end of the time range this request covers. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of network health insights returned for the query window. Returned by the lookup.
</ResponseField>
