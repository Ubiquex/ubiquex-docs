---
title: "datadog_observability_pipeline_data"
description: "`datadog_observability_pipeline_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_observability_pipeline_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	observability "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/observability"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_observability_pipeline_data"})

    		ubx.Data(observability.PipelineData, "example", observability.PipelineDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PipelineData } from "@ubx/sdk-datadog/datadog/data/observability/pipeline_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_observability_pipeline_data" });

      data(PipelineData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.observability import PipelineData, PipelineDataConfig

    def describe():
        ubx.intent("look up datadog_observability_pipeline_data")

        ubx.data(PipelineData, "example", PipelineDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The `schema` `data`. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata about the response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="total_count" type="number">
      The total number of pipelines. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
