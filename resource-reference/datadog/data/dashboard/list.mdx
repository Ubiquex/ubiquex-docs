---
title: "datadog_dashboard_list"
description: "`datadog_dashboard_list` looks up a real dashboard list -- a named, curated grouping of dashboards."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_dashboard_list"})

    		ubx.Data(dashboard.List, "example", dashboard.ListConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { List } from "./local-sdk/datadog/sdk/typescript/datadog/data/dashboard/list.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_dashboard_list" });

      data(List, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.dashboard import List, ListConfig

    def describe():
        ubx.intent("look up datadog_dashboard_list")

        ubx.data(List, "example", ListConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="dashboard_lists" type="list(object)">
  List of all your dashboard lists. Returned by the lookup.
</ResponseField>
