---
title: "datadog_governance_insight_data"
description: "`datadog_governance_insight_data` reads a real Governance insight -- a surfaced finding or recommendation about the account's own posture."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	governance "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/governance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_governance_insight_data"})

    		ubx.Data(governance.InsightData, "example", governance.InsightDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { InsightData } from "@ubx/sdk-datadog/datadog/data/governance/insight_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_governance_insight_data" });

      data(InsightData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.governance import InsightData, InsightDataConfig

    def describe():
        ubx.intent("look up datadog_governance_insight_data")

        ubx.data(InsightData, "example", InsightDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An array of governance insight resources. Returned by the lookup.
</ResponseField>
