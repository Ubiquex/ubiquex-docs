---
title: "datadog_governance_control_detection_data"
description: "`data_datadog_governance_control_detection_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only..."
---

`data_datadog_governance_control_detection_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	governance "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/governance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_governance_control_detection_data"})

    		ubx.Data(governance.ControlDetectionData, "example", governance.ControlDetectionDataConfig{
    			DetectionType: "example",
    			Sort:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlDetectionData } from "@ubx/sdk-datadog/datadog/data/governance/control_detection_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_governance_control_detection_data" });

      data(ControlDetectionData, "example", {
        detectionType: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.governance import ControlDetectionData, ControlDetectionDataConfig

    def describe():
        ubx.intent("look up datadog_governance_control_detection_data")

        ubx.data(ControlDetectionData, "example", ControlDetectionDataConfig(
            detection_type="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detection_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of governance control detection resources. Returned by the lookup.
</ResponseField>
