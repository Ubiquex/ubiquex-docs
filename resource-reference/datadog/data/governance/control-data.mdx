---
title: "datadog_governance_control_data"
description: "`datadog_governance_control_data` looks up a real Governance control -- a defined compliance or policy check."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	governance "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/governance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_governance_control_data"})

    		ubx.Data(governance.ControlData, "example", governance.ControlDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlData } from "@ubx/sdk-datadog/datadog/data/governance/control_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_governance_control_data" });

      data(ControlData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.governance import ControlData, ControlDataConfig

    def describe():
        ubx.intent("look up datadog_governance_control_data")

        ubx.data(ControlData, "example", ControlDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An array of governance control resources. Returned by the lookup.
</ResponseField>
