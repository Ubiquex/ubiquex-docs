---
title: "datadog_governance_control_detections_response"
description: "`datadog_governance_control_detections_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_governance_control_detections_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	governance "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/governance"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_governance_control_detections_response"})

    		ubx.Data(governance.ControlDetectionsResponse, "example", governance.ControlDetectionsResponseConfig{
    			DetectionType: "example",
    			Sort:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ControlDetectionsResponse } from "@ubx/sdk-datadog/datadog/data/governance/control_detections_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_governance_control_detections_response" });

      data(ControlDetectionsResponse, "example", {
        detectionType: "example",
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.governance import ControlDetectionsResponse, ControlDetectionsResponseConfig

    def describe():
        ubx.intent("look up datadog_governance_control_detections_response")

        ubx.data(ControlDetectionsResponse, "example", ControlDetectionsResponseConfig(
            detection_type="example",
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="detection_type" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of governance control detection resources. Returned by the lookup.
</ResponseField>
