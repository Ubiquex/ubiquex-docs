---
title: "datadog_licenses_list_response"
description: "`datadog_licenses_list_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_licenses_list_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	licenses "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/licenses"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_licenses_list_response"})

    		ubx.Data(licenses.ListResponse, "example", licenses.ListResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ListResponse } from "@ubx/sdk-datadog/datadog/data/licenses/list_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_licenses_list_response" });

      data(ListResponse, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.licenses import ListResponse, ListResponseConfig

    def describe():
        ubx.intent("look up datadog_licenses_list_response")

        ubx.data(ListResponse, "example", ListResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  The data object in a licenses list response, containing the list of SPDX licenses. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes of the licenses list response, containing the array of SPDX licenses. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="licenses" type="list(object)" required>
          The list of SPDX licenses returned by the API. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier for this licenses list response. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type identifier for license list responses. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
