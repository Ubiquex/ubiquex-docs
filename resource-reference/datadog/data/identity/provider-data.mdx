---
title: "datadog_identity_provider_data"
description: "`datadog_identity_provider_data` looks up a real identity provider (IdP) connected to the account for single sign-on."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	identity "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/identity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_identity_provider_data"})

    		ubx.Data(identity.ProviderData, "example", identity.ProviderDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ProviderData } from "@ubx/sdk-datadog/datadog/data/identity/provider_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_identity_provider_data" });

      data(ProviderData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.identity import ProviderData, ProviderDataConfig

    def describe():
        ubx.intent("look up datadog_identity_provider_data")

        ubx.data(ProviderData, "example", ProviderDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  List of organization identity provider data objects. Returned by the lookup.
</ResponseField>
