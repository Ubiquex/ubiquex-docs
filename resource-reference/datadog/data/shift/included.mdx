---
title: "datadog_shift_included"
description: "`datadog_shift_included` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_shift_included` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shift "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/shift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_shift_included"})

    		ubx.Data(shift.Included, "example", shift.IncludedConfig{
    			ScheduleId: "example",
    			Include:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Included } from "@ubx/sdk-datadog/datadog/data/shift/included";

    export default stack("example", () => {
      intent({ summary: "look up datadog_shift_included" });

      data(Included, "example", {
        scheduleId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.shift import Included, IncludedConfig

    def describe():
        ubx.intent("look up datadog_shift_included")

        ubx.data(Included, "example", IncludedConfig(
            schedule_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="schedule_id" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data for an on-call shift. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes for an on-call shift. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="end" type="string">
          The end time of the shift. Optional lookup argument.
        </ResponseField>
        <ResponseField name="start" type="string">
          The start time of the shift. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The `ShiftData` `id`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships for an on-call shift. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="user" type="object">
          Defines the relationship between a shift and the user who is working that shift. Optional lookup argument.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Represents a reference to the user assigned to this shift, containing the user's ID and resource type. Required lookup argument.
              <Expandable title="properties">
                <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Indicates that the resource is of type 'shifts'. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="included" type="list(object)">
  The `Shift` `included`. Returned by the lookup.
</ResponseField>
