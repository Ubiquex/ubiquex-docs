---
title: "datadog_sourcemap_file_response"
description: "`datadog_sourcemap_file_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_sourcemap_file_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sourcemap "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/sourcemap"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_sourcemap_file_response"})

    		ubx.Data(sourcemap.FileResponse, "example", sourcemap.FileResponseConfig{
    			Filename: "example",
    			Service:  "example",
    			Version:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { FileResponse } from "@ubx/sdk-datadog/datadog/data/sourcemap/file_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_sourcemap_file_response" });

      data(FileResponse, "example", {
        filename: "example",
        service: "example",
        version: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.sourcemap import FileResponse, FileResponseConfig

    def describe():
        ubx.intent("look up datadog_sourcemap_file_response")

        ubx.data(FileResponse, "example", FileResponseConfig(
            filename="example",
            service="example",
            version="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="filename" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="service" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="version" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  JavaScript source map file data object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of a JavaScript source map file. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="file" type="string" required>
          The name of the minified JavaScript file. Required lookup argument.
        </ResponseField>
        <ResponseField name="mappings" type="string" required>
          The Base64 VLQ encoded string that maps positions in the minified file to positions in the original source files. Required lookup argument.
        </ResponseField>
        <ResponseField name="minified_line_lengths" type="list(number)" required>
          List of character counts for each line in the minified file. Required lookup argument.
        </ResponseField>
        <ResponseField name="names" type="list(any)" required>
          List of symbol names referenced in the mappings. Required lookup argument.
        </ResponseField>
        <ResponseField name="source_root" type="string" required>
          The root path prepended to source file paths. Required lookup argument.
        </ResponseField>
        <ResponseField name="sources" type="list(string)" required>
          List of original source file paths. Required lookup argument.
        </ResponseField>
        <ResponseField name="sources_content" type="list(string)" required>
          List of original source file contents corresponding to the paths in `sources`. Required lookup argument.
        </ResponseField>
        <ResponseField name="version" type="number" required>
          The version of the source map format (typically 3). Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The unique identifier of the source map file, typically the path to the file. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The resource type for source map file objects. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
