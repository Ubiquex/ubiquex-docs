---
title: "datadog_logs_pipelines_order"
description: "`datadog_logs_pipelines_order` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_logs_pipelines_order` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_logs_pipelines_order"})

    		ubx.Data(logs.PipelinesOrder, "example", logs.PipelinesOrderConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { PipelinesOrder } from "@ubx/sdk-datadog/datadog/data/logs/pipelines_order";

    export default stack("example", () => {
      intent({ summary: "look up datadog_logs_pipelines_order" });

      data(PipelinesOrder, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.logs import PipelinesOrder, PipelinesOrderConfig

    def describe():
        ubx.intent("look up datadog_logs_pipelines_order")

        ubx.data(PipelinesOrder, "example", PipelinesOrderConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="pipeline_ids" type="list(string)">
  Ordered Array of `&lt;PIPELINE_ID&gt;` strings, the order of pipeline IDs in the array define the overall Pipelines order for Datadog. Returned by the lookup.
</ResponseField>
