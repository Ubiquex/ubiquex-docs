---
title: "datadog_logs_indexes_order"
description: "`datadog_logs_indexes_order` reads the real, evaluation order of an account's own Logs indexes."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_logs_indexes_order"})

    		ubx.Data(logs.IndexesOrder, "example", logs.IndexesOrderConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { IndexesOrder } from "./local-sdk/datadog/sdk/typescript/datadog/data/logs/indexes_order.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_logs_indexes_order" });

      data(IndexesOrder, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.logs import IndexesOrder, IndexesOrderConfig

    def describe():
        ubx.intent("look up datadog_logs_indexes_order")

        ubx.data(IndexesOrder, "example", IndexesOrderConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="index_names" type="list(string)">
  Array of strings identifying by their name(s) the index(es) of your organization. Logs are tested against the query filter of each index one by one, following the order of the array. Logs are eventually stored in the first matching index. Returned by the lookup.
</ResponseField>
