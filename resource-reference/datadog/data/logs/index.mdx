---
title: "datadog_logs_index"
description: "`datadog_logs_index` looks up a real Logs index -- a configured bucket controlling retention and filtering for a subset of ingested logs."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_logs_index"})

    		ubx.Data(logs.Index, "example", logs.IndexConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Index } from "@ubx/sdk-datadog/datadog/data/logs/index";

    export default stack("example", () => {
      intent({ summary: "look up datadog_logs_index" });

      data(Index, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.logs import Index, IndexConfig

    def describe():
        ubx.intent("look up datadog_logs_index")

        ubx.data(Index, "example", IndexConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="indexes" type="list(object)">
  Array of Log index configurations. Returned by the lookup.
</ResponseField>
