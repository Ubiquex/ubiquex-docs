---
title: "datadog_logs_archive_order"
description: "`datadog_logs_archive_order` reads the real, evaluation order of an account's own Logs archives."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	logs "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/logs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_logs_archive_order"})

    		ubx.Data(logs.ArchiveOrder, "example", logs.ArchiveOrderConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ArchiveOrder } from "@ubx/sdk-datadog/datadog/data/logs/archive_order";

    export default stack("example", () => {
      intent({ summary: "look up datadog_logs_archive_order" });

      data(ArchiveOrder, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.logs import ArchiveOrder, ArchiveOrderConfig

    def describe():
        ubx.intent("look up datadog_logs_archive_order")

        ubx.data(ArchiveOrder, "example", ArchiveOrderConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  The definition of an archive order. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes associated with the archive order. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="archive_ids" type="list(string)" required>
          An ordered array of `&lt;ARCHIVE_ID&gt;` strings, the order of archive IDs in the array define the overall archives order for Datadog. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type of the archive order definition. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
