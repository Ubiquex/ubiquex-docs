---
title: "datadog_confluent_account_response_data"
description: "`datadog_confluent_account_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_confluent_account_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	confluent "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/confluent"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_confluent_account_response_data"})

    		ubx.Data(confluent.AccountResponseData, "example", confluent.AccountResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { AccountResponseData } from "@ubx/sdk-datadog/datadog/data/confluent/account_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_confluent_account_response_data" });

      data(AccountResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.confluent import AccountResponseData, AccountResponseDataConfig

    def describe():
        ubx.intent("look up datadog_confluent_account_response_data")

        ubx.data(AccountResponseData, "example", AccountResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  The Confluent account. Returned by the lookup.
</ResponseField>
