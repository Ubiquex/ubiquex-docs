---
title: "datadog_tag_indexing_rule_exemption_response"
description: "`datadog_tag_indexing_rule_exemption_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_tag_indexing_rule_exemption_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tag "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/tag"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_tag_indexing_rule_exemption_response"})

    		ubx.Data(tag.IndexingRuleExemptionResponse, "example", tag.IndexingRuleExemptionResponseConfig{
    			MetricName: "example-metric-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IndexingRuleExemptionResponse } from "@ubx/sdk-datadog/datadog/data/tag/indexing_rule_exemption_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_tag_indexing_rule_exemption_response" });

      data(IndexingRuleExemptionResponse, "example", {
        metricName: "example-metric-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.tag import IndexingRuleExemptionResponse, IndexingRuleExemptionResponseConfig

    def describe():
        ubx.intent("look up datadog_tag_indexing_rule_exemption_response")

        ubx.data(IndexingRuleExemptionResponse, "example", IndexingRuleExemptionResponseConfig(
            metric_name="example-metric-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="metric_name" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  A tag indexing rule exemption resource object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of a tag indexing rule exemption. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="created_at" type="string">
          Timestamp when the exemption was created. Returned by the lookup.
        </ResponseField>
        <ResponseField name="created_by_handle" type="string">
          Handle of the user who created the exemption. Returned by the lookup.
        </ResponseField>
        <ResponseField name="kind" type="string">
          Discriminates between an explicit exemption (`exemption`) and a pre-existing legacy tag configuration acting as an implicit exclusion (`legacy_tag_configuration`). Optional lookup argument.
        </ResponseField>
        <ResponseField name="reason" type="string">
          The reason the metric is exempt from tag indexing rules. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The metric name, used as the resource ID. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      The tag indexing rule exemption resource type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
