---
title: "datadog_tag_rules_list_response"
description: "`datadog_tag_rules_list_response` lists the real tag rules configured on the account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	tag "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/tag"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_tag_rules_list_response"})

    		ubx.Data(tag.RulesListResponse, "example", tag.RulesListResponseConfig{
    			Include:         "example",
    			IncludeDeleted:  true,
    			IncludeDisabled: true,
    			TsEnd:           1,
    			TsStart:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { RulesListResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/tag/rules_list_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_tag_rules_list_response" });

      data(RulesListResponse, "example", {
        include: "example",
        includeDeleted: true,
        includeDisabled: true,
        tsEnd: 1,
        tsStart: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.tag import RulesListResponse, RulesListResponseConfig

    def describe():
        ubx.intent("look up datadog_tag_rules_list_response")

        ubx.data(RulesListResponse, "example", RulesListResponseConfig(
            include="example",
            include_deleted=True,
            include_disabled=True,
            ts_end=1,
            ts_start=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="string">
  A related resource to include alongside a tag rule in the response. Currently the only supported value is `score`. Optional lookup argument.
</ResponseField>

<ResponseField name="include_deleted" type="bool">
  Whether deleted items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="include_disabled" type="bool">
  Whether disabled items are included in the results. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ts_end" type="number">
  The real, inclusive end of the time range this request covers, as a Unix timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="ts_start" type="number">
  The real, inclusive start of the time range this request covers, as a Unix timestamp. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of tag rule data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Related resources fetched alongside the primary tag rules. Populated when an `include` query parameter is supplied. Returned by the lookup.
</ResponseField>
