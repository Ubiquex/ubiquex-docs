---
title: "datadog_tag_to_hosts"
description: "`datadog_tag_to_hosts` reads the real hosts currently carrying a given tag."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	tag "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/tag"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_tag_to_hosts"})

    		ubx.Data(tag.ToHosts, "example", tag.ToHostsConfig{
    			Source: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ToHosts } from "@ubx/sdk-datadog/datadog/data/tag/to_hosts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_tag_to_hosts" });

      data(ToHosts, "example", {
        source: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.tag import ToHosts, ToHostsConfig

    def describe():
        ubx.intent("look up datadog_tag_to_hosts")

        ubx.data(ToHosts, "example", ToHostsConfig(
            source="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="source" type="string">
  The real source this object was derived or ingested from. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="tags" type="map(list(string))">
  A mapping of tags to host names Returned by the lookup.
</ResponseField>
