---
title: "datadog_teams_response"
description: "`datadog_teams_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_teams_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	teams "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/teams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_teams_response"})

    		ubx.Data(teams.Response, "example", teams.ResponseConfig{
    			Include: []string{"example"},
    			Sort:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Response } from "@ubx/sdk-datadog/datadog/data/teams/response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_teams_response" });

      data(Response, "example", {
        include: ["example"],
        sort: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.teams import Response, ResponseConfig

    def describe():
        ubx.intent("look up datadog_teams_response")

        ubx.data(Response, "example", ResponseConfig(
            include=["example"],
            sort="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include" type="list(string)">
  Optional lookup argument.
</ResponseField>

<ResponseField name="sort" type="string">
  Specifies the order of the returned teams Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Teams response data Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  Resources related to the team Returned by the lookup.
</ResponseField>

<ResponseField name="links" type="object">
  Teams response links. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="first" type="string">
      First link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="last" type="string">
      Last link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="next" type="string">
      Next link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="prev" type="string">
      Previous link. Optional lookup argument.
    </ResponseField>
    <ResponseField name="self" type="string">
      Current link. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Teams response metadata. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="pagination" type="object">
      Teams response metadata. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="first_offset" type="number">
          The first offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="last_offset" type="number">
          The last offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Pagination limit. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_offset" type="number">
          The next offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="offset" type="number">
          The offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="prev_offset" type="number">
          The previous offset. Optional lookup argument.
        </ResponseField>
        <ResponseField name="total" type="number">
          Total results. Optional lookup argument.
        </ResponseField>
        <ResponseField name="type" type="string">
          Offset type. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
