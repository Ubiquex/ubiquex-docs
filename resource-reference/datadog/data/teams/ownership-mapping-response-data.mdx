---
title: "datadog_teams_ownership_mapping_response_data"
description: "`datadog_teams_ownership_mapping_response_data` reads the real mapping of Software Catalog entities to the teams that own them."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	teams "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/teams"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_teams_ownership_mapping_response_data"})

    		ubx.Data(teams.OwnershipMappingResponseData, "example", teams.OwnershipMappingResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { OwnershipMappingResponseData } from "./local-sdk/datadog/sdk/typescript/datadog/data/teams/ownership_mapping_response_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_teams_ownership_mapping_response_data" });

      data(OwnershipMappingResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.teams import OwnershipMappingResponseData, OwnershipMappingResponseDataConfig

    def describe():
        ubx.intent("look up datadog_teams_ownership_mapping_response_data")

        ubx.data(OwnershipMappingResponseData, "example", OwnershipMappingResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  A list of teams ownership mappings. Returned by the lookup.
</ResponseField>
