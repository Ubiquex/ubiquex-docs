---
title: "datadog_attachment_array"
description: "`datadog_attachment_array` lists the real file attachments on an object such as an incident or case."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	attachment "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/attachment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_attachment_array"})

    		ubx.Data(attachment.Array, "example", attachment.ArrayConfig{
    			IncidentId: "example",
    			Include:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Array } from "@ubx/sdk-datadog/datadog/data/attachment/array";

    export default stack("example", () => {
      intent({ summary: "look up datadog_attachment_array" });

      data(Array, "example", {
        incidentId: "example",
        include: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.attachment import Array, ArrayConfig

    def describe():
        ubx.intent("look up datadog_attachment_array")

        ubx.data(Array, "example", ArrayConfig(
            incident_id="example",
            include="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="incident_id" type="string" required>
  The real, unique identifier of this incident. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="include" type="string">
  Which real, additional related data to include alongside this response. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of attachment data objects. Returned by the lookup.
</ResponseField>

<ResponseField name="included" type="list(object)">
  A list of related objects included in the response. Returned by the lookup.
</ResponseField>
