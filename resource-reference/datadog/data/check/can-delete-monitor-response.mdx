---
title: "datadog_check_can_delete_monitor_response"
description: "`datadog_check_can_delete_monitor_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_check_can_delete_monitor_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	check "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/check"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_check_can_delete_monitor_response"})

    		ubx.Data(check.CanDeleteMonitorResponse, "example", check.CanDeleteMonitorResponseConfig{
    			MonitorIds: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CanDeleteMonitorResponse } from "@ubx/sdk-datadog/datadog/data/check/can_delete_monitor_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_check_can_delete_monitor_response" });

      data(CanDeleteMonitorResponse, "example", {
        monitorIds: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.check import CanDeleteMonitorResponse, CanDeleteMonitorResponseConfig

    def describe():
        ubx.intent("look up datadog_check_can_delete_monitor_response")

        ubx.data(CanDeleteMonitorResponse, "example", CanDeleteMonitorResponseConfig(
            monitor_ids=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="monitor_ids" type="list(number)" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Wrapper object with the list of monitor IDs. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ok" type="list(number)">
      An array of Monitor IDs that can be safely deleted. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="errors" type="map(list(string))">
  A mapping of Monitor ID to strings denoting where it's used. Returned by the lookup.
</ResponseField>
