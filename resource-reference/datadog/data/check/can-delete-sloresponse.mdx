---
title: "datadog_check_can_delete_sloresponse"
description: "`datadog_check_can_delete_sloresponse` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_check_can_delete_sloresponse` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	check "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/check"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_check_can_delete_sloresponse"})

    		ubx.Data(check.CanDeleteSloresponse, "example", check.CanDeleteSloresponseConfig{
    			Ids: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { CanDeleteSloresponse } from "@ubx/sdk-datadog/datadog/data/check/can_delete_sloresponse";

    export default stack("example", () => {
      intent({ summary: "look up datadog_check_can_delete_sloresponse" });

      data(CanDeleteSloresponse, "example", {
        ids: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.check import CanDeleteSloresponse, CanDeleteSloresponseConfig

    def describe():
        ubx.intent("look up datadog_check_can_delete_sloresponse")

        ubx.data(CanDeleteSloresponse, "example", CanDeleteSloresponseConfig(
            ids="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="ids" type="string" required>
  Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  An array of service level objective objects. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="ok" type="list(string)">
      An array of SLO IDs that can be safely deleted. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="errors" type="map(string)">
  A mapping of SLO id to it's current usages. Returned by the lookup.
</ResponseField>
