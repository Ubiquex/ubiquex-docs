---
title: "datadog_deployment_gate_response_data"
description: "`datadog_deployment_gate_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_deployment_gate_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deployment "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/deployment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_deployment_gate_response_data"})

    		ubx.Data(deployment.GateResponseData, "example", deployment.GateResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { GateResponseData } from "@ubx/sdk-datadog/datadog/data/deployment/gate_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_deployment_gate_response_data" });

      data(GateResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.deployment import GateResponseData, GateResponseDataConfig

    def describe():
        ubx.intent("look up datadog_deployment_gate_response_data")

        ubx.data(GateResponseData, "example", GateResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of deployment gates. Returned by the lookup.
</ResponseField>

<ResponseField name="meta" type="object">
  Metadata for a list of deployment gates response. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="page" type="object">
      Pagination information for a list of deployment gates. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cursor" type="string">
          The cursor used for the current page. Optional lookup argument.
        </ResponseField>
        <ResponseField name="next_cursor" type="string">
          The cursor to use to fetch the next page. This is absent when there are no more pages. Optional lookup argument.
        </ResponseField>
        <ResponseField name="size" type="number">
          The number of results per page. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
