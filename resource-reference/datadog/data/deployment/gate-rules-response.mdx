---
title: "datadog_deployment_gate_rules_response"
description: "`datadog_deployment_gate_rules_response` reads the real rules configured on a Deployment Gate."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	deployment "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/deployment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_deployment_gate_rules_response"})

    		ubx.Data(deployment.GateRulesResponse, "example", deployment.GateRulesResponseConfig{
    			GateId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { GateRulesResponse } from "./local-sdk/datadog/sdk/typescript/datadog/data/deployment/gate_rules_response.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_deployment_gate_rules_response" });

      data(GateRulesResponse, "example", {
        gateId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.deployment import GateRulesResponse, GateRulesResponseConfig

    def describe():
        ubx.intent("look up datadog_deployment_gate_rules_response")

        ubx.data(GateRulesResponse, "example", GateRulesResponseConfig(
            gate_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="gate_id" type="string" required>
  The real, unique identifier of this deployment gate. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  Data for a list of deployment rules. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes of the response for listing deployment rules. Required lookup argument.
      <Expandable title="properties">
        <ResponseField name="rules" type="list(object)">
          The list of deployment rules. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      Unique identifier of the deployment rule. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      List deployment rule resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
