---
title: "datadog_playlists_session_data"
description: "`datadog_playlists_session_data` reads the real, current playback session of a Playlist -- which dashboard is showing now and where the auto-cycle..."
---

`datadog_playlists_session_data` reads the real, current playback session of a Playlist -- which dashboard is showing now and where the auto-cycle is in its own rotation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	playlists "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/playlists"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_playlists_session_data"})

    		ubx.Data(playlists.SessionData, "example", playlists.SessionDataConfig{
    			PlaylistId: 1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { SessionData } from "./local-sdk/datadog/sdk/typescript/datadog/data/playlists/session_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_playlists_session_data" });

      data(SessionData, "example", {
        playlistId: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.playlists import SessionData, SessionDataConfig

    def describe():
        ubx.intent("look up datadog_playlists_session_data")

        ubx.data(SessionData, "example", SessionDataConfig(
            playlist_id=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="playlist_id" type="number" required>
  The real, unique identifier of this playlist. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  Array of playlist session data objects. Returned by the lookup.
</ResponseField>
