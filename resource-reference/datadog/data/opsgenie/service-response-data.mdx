---
title: "datadog_opsgenie_service_response_data"
description: "`datadog_opsgenie_service_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_opsgenie_service_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opsgenie "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/opsgenie"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_opsgenie_service_response_data"})

    		ubx.Data(opsgenie.ServiceResponseData, "example", opsgenie.ServiceResponseDataConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ServiceResponseData } from "@ubx/sdk-datadog/datadog/data/opsgenie/service_response_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_opsgenie_service_response_data" });

      data(ServiceResponseData, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.opsgenie import ServiceResponseData, ServiceResponseDataConfig

    def describe():
        ubx.intent("look up datadog_opsgenie_service_response_data")

        ubx.data(ServiceResponseData, "example", ServiceResponseDataConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  An array of Opsgenie services. Returned by the lookup.
</ResponseField>
