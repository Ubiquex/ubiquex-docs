---
title: "datadog_table_row_resource_data"
description: "`datadog_table_row_resource_data` reads one real row within a table-widget result."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	table "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/table"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_table_row_resource_data"})

    		ubx.Data(table.RowResourceData, "example", table.RowResourceDataConfig{
    			Id:    "example",
    			RowId: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { RowResourceData } from "@ubx/sdk-datadog/datadog/data/table/row_resource_data";

    export default stack("example", () => {
      intent({ summary: "look up datadog_table_row_resource_data" });

      data(RowResourceData, "example", {
        id: "example",
        rowId: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.table import RowResourceData, RowResourceDataConfig

    def describe():
        ubx.intent("look up datadog_table_row_resource_data")

        ubx.data(RowResourceData, "example", RowResourceDataConfig(
            id="example",
            row_id=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="id" type="string" required>
  The real, unique identifier for this object. **(AI-inferred)** Required lookup argument.
</ResponseField>

<ResponseField name="row_id" type="list(string)" required>
  The real, unique identifier of this row. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  The rows. Returned by the lookup.
</ResponseField>
