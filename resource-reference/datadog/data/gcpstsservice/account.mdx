---
title: "datadog_gcpstsservice_account"
description: "`datadog_gcpstsservice_account` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_gcpstsservice_account` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gcpstsservice "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/gcpstsservice"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_gcpstsservice_account"})

    		ubx.Data(gcpstsservice.Account, "example", gcpstsservice.AccountConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Account } from "@ubx/sdk-datadog/datadog/data/gcpstsservice/account";

    export default stack("example", () => {
      intent({ summary: "look up datadog_gcpstsservice_account" });

      data(Account, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.gcpstsservice import Account, AccountConfig

    def describe():
        ubx.intent("look up datadog_gcpstsservice_account")

        ubx.data(Account, "example", AccountConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="list(object)">
  Array of GCP STS enabled service accounts. Returned by the lookup.
</ResponseField>
