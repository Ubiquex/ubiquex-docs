---
title: "datadog_microsoft_teams_tenant_based_handle_info_response_data"
description: "`datadog_microsoft_teams_tenant_based_handle_info_response_data` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_microsoft_teams_tenant_based_handle_info_response_data` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	microsoft "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/microsoft"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_microsoft_teams_tenant_based_handle_info_response_data"})

    		ubx.Data(microsoft.TeamsTenantBasedHandleInfoResponseData, "example", microsoft.TeamsTenantBasedHandleInfoResponseDataConfig{
    			Name:     "example",
    			TenantId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { TeamsTenantBasedHandleInfoResponseData } from "@ubx/sdk-datadog/datadog/data/microsoft/teams_tenant_based_handle_info_response_data";

    export default stack("example", () => {
      intent({
        summary:
          "look up datadog_microsoft_teams_tenant_based_handle_info_response_data",
      });

      data(TeamsTenantBasedHandleInfoResponseData, "example", {
        name: "example",
        tenantId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.microsoft import TeamsTenantBasedHandleInfoResponseData, TeamsTenantBasedHandleInfoResponseDataConfig

    def describe():
        ubx.intent("look up datadog_microsoft_teams_tenant_based_handle_info_response_data")

        ubx.data(TeamsTenantBasedHandleInfoResponseData, "example", TeamsTenantBasedHandleInfoResponseDataConfig(
            name="example",
            tenant_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="name" type="string">
  Optional lookup argument.
</ResponseField>

<ResponseField name="tenant_id" type="string">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  An array of tenant-based handles. Returned by the lookup.
</ResponseField>
