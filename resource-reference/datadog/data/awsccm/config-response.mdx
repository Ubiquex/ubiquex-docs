---
title: "datadog_awsccm_config_response"
description: "`datadog_awsccm_config_response` reads the real AWS Cloud Cost Management configuration for an AWS account."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	awsccm "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/awsccm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_awsccm_config_response"})

    		ubx.Data(awsccm.ConfigResponse, "example", awsccm.ConfigResponseConfig{
    			AwsAccountConfigId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ConfigResponse } from "@ubx/sdk-datadog/datadog/data/awsccm/config_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_awsccm_config_response" });

      data(ConfigResponse, "example", {
        awsAccountConfigId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.awsccm import ConfigResponse, ConfigResponseConfig

    def describe():
        ubx.intent("look up datadog_awsccm_config_response")

        ubx.data(ConfigResponse, "example", ConfigResponseConfig(
            aws_account_config_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="aws_account_config_id" type="string" required>
  The real, unique identifier of this AWS account configuration. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  AWS CCM Config response data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      AWS CCM Config response attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="data_export_configs" type="list(object)">
          List of data export configurations for Cost and Usage Reports. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Unique Datadog ID of the AWS Account Integration Config. To get the config ID for an account, use the [List all AWS integrations](https://docs.datadoghq.com/api/latest/aws-integration/#list-all-aws-integrations) endpoint and query by AWS Account ID. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      AWS CCM Config resource type. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
