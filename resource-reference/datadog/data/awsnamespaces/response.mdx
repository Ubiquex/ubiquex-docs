---
title: "datadog_awsnamespaces_response"
description: "`datadog_awsnamespaces_response` reads the real AWS CloudWatch namespaces available for metric collection through Datadog's own AWS integration."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	awsnamespaces "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/awsnamespaces"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_awsnamespaces_response"})

    		ubx.Data(awsnamespaces.Response, "example", awsnamespaces.ResponseConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { Response } from "@ubx/sdk-datadog/datadog/data/awsnamespaces/response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_awsnamespaces_response" });

      data(Response, "example", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.awsnamespaces import Response, ResponseConfig

    def describe():
        ubx.intent("look up datadog_awsnamespaces_response")

        ubx.data(Response, "example", ResponseConfig(
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

_This lookup takes no arguments._

## Result properties

<ResponseField name="data" type="object">
  AWS Namespaces response data. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      AWS Namespaces response attributes. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="namespaces" type="list(string)" required>
          AWS CloudWatch namespace. Required lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The `AWSNamespacesResponseData` `id`. Required lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The `AWSNamespacesResponseData` `type`. Required lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
