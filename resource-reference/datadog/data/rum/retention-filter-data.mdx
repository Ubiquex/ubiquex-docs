---
title: "datadog_rum_retention_filter_data"
description: "`datadog_rum_retention_filter_data` looks up a real RUM retention filter controlling what share of matching RUM events get retained."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	rum "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/rum"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_rum_retention_filter_data"})

    		ubx.Data(rum.RetentionFilterData, "example", rum.RetentionFilterDataConfig{
    			AppId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { RetentionFilterData } from "./local-sdk/datadog/sdk/typescript/datadog/data/rum/retention_filter_data.ts";

    export default stack("example", () => {
      intent({ summary: "look up datadog_rum_retention_filter_data" });

      data(RetentionFilterData, "example", {
        appId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.data.rum import RetentionFilterData, RetentionFilterDataConfig

    def describe():
        ubx.intent("look up datadog_rum_retention_filter_data")

        ubx.data(RetentionFilterData, "example", RetentionFilterDataConfig(
            app_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="app_id" type="string" required>
  The real, unique identifier of the app this result is scoped to. **(AI-inferred)** Required lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="list(object)">
  A list of RUM retention filters. Returned by the lookup.
</ResponseField>
