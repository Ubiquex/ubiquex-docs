---
title: "datadog_get_io_cindicator_response"
description: "`datadog_get_io_cindicator_response` is a real, live lookup against Datadog, generated from datadog's own real schema -- read-only, never created or destroyed."
---

`datadog_get_io_cindicator_response` is a real, live data source: `ubx.Data`/`data`/`ubx.data` (per language) executes this lookup at resolve time, against Datadog's own real API, and returns whatever it finds. It is never created, modified, or destroyed by ubx -- see [docs/schema.md's own "Amendment: data sources"] for the real wire shape this compiles to (`data_sources[]`, never `resources[]`).

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_get_io_cindicator_response"})

    		ubx.Data(get.IoCindicatorResponse, "example", get.IoCindicatorResponseConfig{
    			Indicator:            "example",
    			IncludeTriageHistory: true,
    			Ocsf:                 true,
    			TriageHistoryLimit:   1,
    			TriageHistoryOffset:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { IoCindicatorResponse } from "@ubx/sdk-datadog/datadog/data/get/io_cindicator_response";

    export default stack("example", () => {
      intent({ summary: "look up datadog_get_io_cindicator_response" });

      data(IoCindicatorResponse, "example", {
        indicator: "example",
        includeTriageHistory: true,
        ocsf: true,
        triageHistoryLimit: 1,
        triageHistoryOffset: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.get import IoCindicatorResponse, IoCindicatorResponseConfig

    def describe():
        ubx.intent("look up datadog_get_io_cindicator_response")

        ubx.data(IoCindicatorResponse, "example", IoCindicatorResponseConfig(
            indicator="example",
            include_triage_history=True,
            ocsf=True,
            triage_history_limit=1,
            triage_history_offset=1,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="include_triage_history" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="indicator" type="string" required>
  Required lookup argument.
</ResponseField>

<ResponseField name="ocsf" type="bool">
  Optional lookup argument.
</ResponseField>

<ResponseField name="triage_history_limit" type="number">
  Optional lookup argument.
</ResponseField>

<ResponseField name="triage_history_offset" type="number">
  Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  IoC indicator response data object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of the get indicator response. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="data" type="object">
          An indicator of compromise with extended context from your environment. Optional lookup argument.
          <Expandable title="properties">
            <Note>Nested `data` structure omitted (recursive or deeply nested).</Note>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      Unique identifier for the response. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Response type identifier. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
