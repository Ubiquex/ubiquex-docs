---
title: "datadog_get_resource_evaluation_filters_response"
description: "`datadog_get_resource_evaluation_filters_response` reads the real filters controlling which cloud resources get evaluated against a compliance rule."
---

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	get "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/data/get"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "look up datadog_get_resource_evaluation_filters_response"})

    		ubx.Data(get.ResourceEvaluationFiltersResponse, "example", get.ResourceEvaluationFiltersResponseConfig{
    			AccountId:     "example",
    			CloudProvider: "example",
    			SkipCache:     true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { data, intent, stack } from "@ubx/sdk";
    import { ResourceEvaluationFiltersResponse } from "@ubx/sdk-datadog/datadog/data/get/resource_evaluation_filters_response";

    export default stack("example", () => {
      intent({
        summary: "look up datadog_get_resource_evaluation_filters_response",
      });

      data(ResourceEvaluationFiltersResponse, "example", {
        accountId: "example",
        cloudProvider: "example",
        skipCache: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.data.get import ResourceEvaluationFiltersResponse, ResourceEvaluationFiltersResponseConfig

    def describe():
        ubx.intent("look up datadog_get_resource_evaluation_filters_response")

        ubx.data(ResourceEvaluationFiltersResponse, "example", ResourceEvaluationFiltersResponseConfig(
            account_id="example",
            cloud_provider="example",
            skip_cache=True,
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
</Tabs>

## Lookup arguments

<ResponseField name="account_id" type="string">
  The real, unique identifier of the account this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="cloud_provider" type="string">
  The real cloud provider (AWS, Azure, GCP) this result is scoped to. **(AI-inferred)** Optional lookup argument.
</ResponseField>

<ResponseField name="skip_cache" type="bool">
  Whether the request bypasses any cached result and reads live data. **(AI-inferred)** Optional lookup argument.
</ResponseField>

## Result properties

<ResponseField name="data" type="object">
  The definition of `GetResourceFilterResponseData` object. Returned by the lookup.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      Attributes of a resource filter. Optional lookup argument.
      <Expandable title="properties">
        <ResponseField name="cloud_provider" type="map(map(list(string)))" required>
          A map of cloud provider names (e.g., "aws", "gcp", "azure") to a map of account/resource IDs and their associated tag filters. Required lookup argument.
        </ResponseField>
        <ResponseField name="uuid" type="string">
          The UUID of the resource filter. Optional lookup argument.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string">
      The `data` `id`. Optional lookup argument.
    </ResponseField>
    <ResponseField name="type" type="string">
      Constant string to identify the request type. Optional lookup argument.
    </ResponseField>
  </Expandable>
</ResponseField>
