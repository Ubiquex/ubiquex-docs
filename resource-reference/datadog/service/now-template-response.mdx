---
title: "datadog_service_now_template_response"
description: "`datadog_service_now_template_response` is a real, saved template controlling how Datadog creates a ServiceNow ticket when it integrates with..."
---

`datadog_service_now_template_response` is a real, saved template controlling how Datadog creates a ServiceNow ticket when it integrates with ServiceNow -- the ServiceNow counterpart to `datadog_jira_issue_template_response`.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	service "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/service"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own now template response"})

    		ubx.Resource(service.NowTemplateResponse, "example", service.NowTemplateResponseConfig{
    			Data:       map[string]any{"attributes": map[string]any{"handle_name": "example-handle-name"}},
    			TemplateId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { NowTemplateResponse } from "./local-sdk/datadog/sdk/typescript/datadog/service/now_template_response.ts";

    export default stack("example", () => {
      intent({ summary: "example own now template response" });

      resource(NowTemplateResponse, "example", {
        data: { attributes: { handleName: "example-handle-name" } },
        templateId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.service import NowTemplateResponse, NowTemplateResponseConfig

    def describe():
        ubx.intent("example own now template response")

        ubx.resource(NowTemplateResponse, "example", NowTemplateResponseConfig(
            data={"attributes": {"handle_name": "example-handle-name"}},
            template_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_service_now_template_response, with data a set
    value, template id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Data object for creating a ServiceNow template Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for creating a ServiceNow template Required.
      <Expandable title="properties">
        <ResponseField name="assignment_group_id" type="string">
          The ID of the assignment group Optional.
        </ResponseField>
        <ResponseField name="business_service_id" type="string">
          The ID of the business service Optional.
        </ResponseField>
        <ResponseField name="fields_mapping" type="map(string)">
          Custom field mappings for the template Optional.
        </ResponseField>
        <ResponseField name="handle_name" type="string" required>
          The handle name of the template Required.
        </ResponseField>
        <ResponseField name="instance_id" type="string" required>
          The ID of the ServiceNow instance Required.
        </ResponseField>
        <ResponseField name="servicenow_tablename" type="string" required>
          The name of the destination ServiceNow table Required.
        </ResponseField>
        <ResponseField name="user_id" type="string">
          The ID of the user Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type identifier for ServiceNow template resources Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="template_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
