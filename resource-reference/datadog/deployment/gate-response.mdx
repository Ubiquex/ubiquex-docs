---
title: "datadog_deployment_gate_response"
description: "`datadog_deployment_gate_response` is a real Datadog Deployment Gate -- an automated check (based on real monitors, SLOs..."
---

`datadog_deployment_gate_response` is a real Datadog Deployment Gate -- an automated check (based on real monitors, SLOs, or error rates) wired into a CI/CD pipeline that blocks or allows a deployment from proceeding based on live service health.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deployment "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/deployment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own gate response"})

    		ubx.Resource(deployment.GateResponse, "example", deployment.GateResponseConfig{
    			Data: map[string]any{"attributes": map[string]any{"env": "example"}},
    			Id:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GateResponse } from "@ubx/sdk-datadog/datadog/deployment/gate_response";

    export default stack("example", () => {
      intent({ summary: "example own gate response" });

      resource(GateResponse, "example", {
        data: { attributes: { env: "example" } },
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.deployment import GateResponse, GateResponseConfig

    def describe():
        ubx.intent("example own gate response")

        ubx.resource(GateResponse, "example", GateResponseConfig(
            data={"attributes": {"env": "example"}},
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_deployment_gate_response, with data a set value,
    id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Parameters for creating a deployment gate. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Parameters for creating a deployment gate. Required.
      <Expandable title="properties">
        <ResponseField name="dry_run" type="bool">
          Whether this gate is run in dry-run mode. Optional.
        </ResponseField>
        <ResponseField name="env" type="string" required>
          The environment of the deployment gate. Required.
        </ResponseField>
        <ResponseField name="identifier" type="string">
          The identifier of the deployment gate. Optional.
        </ResponseField>
        <ResponseField name="service" type="string" required>
          The service of the deployment gate. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Deployment gate resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
