---
title: "datadog_deployment_rule_response"
description: "`datadog_deployment_rule_response` is a real rule governing Datadog's deployment tracking -- how Datadog Deployment Visibility identifies and..."
---

`datadog_deployment_rule_response` is a real rule governing Datadog's deployment tracking -- how Datadog Deployment Visibility identifies and correlates a deployment event from CI/CD data with the services and infrastructure it affects.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	deployment "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/deployment"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own rule response"})

    		ubx.Resource(deployment.RuleResponse, "example", deployment.RuleResponseConfig{
    			GateId: "example",
    			Id:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuleResponse } from "@ubx/sdk-datadog/datadog/deployment/rule_response";

    export default stack("example", () => {
      intent({ summary: "example own rule response" });

      resource(RuleResponse, "example", {
        gateId: "example",
        id: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.deployment import RuleResponse, RuleResponseConfig

    def describe():
        ubx.intent("example own rule response")

        ubx.resource(RuleResponse, "example", RuleResponseConfig(
            gate_id="example",
            id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_deployment_rule_response, with gate id example, id
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object">
  Parameters for creating a deployment rule. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Parameters for creating a deployment rule. Required.
      <Expandable title="properties">
        <ResponseField name="dry_run" type="bool">
          Whether this rule is run in dry-run mode. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the deployment rule. Required.
        </ResponseField>
        <ResponseField name="options" type="object" required>
          Options for deployment rule response representing either faulty deployment detection or monitor options. Required.
          <Expandable title="properties">
            <ResponseField name="allowed_resources" type="list(string)">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="duration" type="number">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="excluded_resources" type="list(string)">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="query" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string" required>
          The type of the deployment rule (faulty_deployment_detection or monitor). Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Deployment rule resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="gate_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
