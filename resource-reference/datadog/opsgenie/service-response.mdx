---
title: "datadog_opsgenie_service_response"
description: "`datadog_opsgenie_service_response` is a real Opsgenie service connected through Datadog's Opsgenie integration -- lets a Datadog monitor page..."
---

`datadog_opsgenie_service_response` is a real Opsgenie service connected through Datadog's Opsgenie integration -- lets a Datadog monitor page an Opsgenie service directly, for organizations running Opsgenie as their on-call tool alongside Datadog.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	opsgenie "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/opsgenie"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own service response"})

    		ubx.Resource(opsgenie.ServiceResponse, "example", opsgenie.ServiceResponseConfig{
    			Data:                 map[string]any{"attributes": map[string]any{"name": "example-name"}},
    			IntegrationServiceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceResponse } from "./local-sdk/datadog/sdk/typescript/datadog/opsgenie/service_response.ts";

    export default stack("example", () => {
      intent({ summary: "example own service response" });

      resource(ServiceResponse, "example", {
        data: { attributes: { name: "example-name" } },
        integrationServiceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.opsgenie import ServiceResponse, ServiceResponseConfig

    def describe():
        ubx.intent("example own service response")

        ubx.resource(ServiceResponse, "example", ServiceResponseConfig(
            data={"attributes": {"name": "example-name"}},
            integration_service_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_opsgenie_service_response, with data a set value,
    integration service id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Opsgenie service data for a create request. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The Opsgenie service attributes for a create request. Required.
      <Expandable title="properties">
        <ResponseField name="custom_url" type="string">
          The custom URL for a custom region. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name for the Opsgenie service. Required.
        </ResponseField>
        <ResponseField name="opsgenie_api_key" type="string" required>
          The Opsgenie API key for your Opsgenie service. Required.
        </ResponseField>
        <ResponseField name="region" type="string" required>
          The region for the Opsgenie service. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Opsgenie service resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="integration_service_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
