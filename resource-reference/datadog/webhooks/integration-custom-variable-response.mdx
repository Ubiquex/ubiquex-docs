---
title: "datadog_webhooks_integration_custom_variable_response"
description: "Real, generated bindings for `datadog_webhooks_integration_custom_variable_response`."
---

`datadog_webhooks_integration_custom_variable_response` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/webhooks"
    )

    cfg := webhooks.IntegrationCustomVariableResponseConfig{
        CustomVariableName: "example-custom-variable-name",
        IsSecret: true,
        Name: "example-name",
        Value: "example",
    }
    ubx.Resource(webhooks.IntegrationCustomVariableResponse, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { IntegrationCustomVariableResponse } from "./local-sdk/datadog/sdk/typescript/datadog/webhooks/integration_custom_variable_response.ts";

    resource(IntegrationCustomVariableResponse, "example", {
      customVariableName: "example-custom-variable-name",
      isSecret: true,
      name: "example-name",
      value: "example",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.webhooks import (
        IntegrationCustomVariableResponse,
        IntegrationCustomVariableResponseConfig,
    )

    cfg = IntegrationCustomVariableResponseConfig(
        custom_variable_name="example-custom-variable-name",
        is_secret=True,
        name="example-name",
        value="example",
    )
    ubx.resource(IntegrationCustomVariableResponse, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_webhooks_integration_custom_variable_response
with custom_variable_name, is_secret, name, value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="custom_variable_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="is_secret" type="bool" required>
  Make custom variable is secret or not. If the custom variable is secret, the value is not returned in the response payload. Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the variable. It corresponds with `&lt;CUSTOM_VARIABLE_NAME&gt;`. Required.
</ResponseField>

<ResponseField name="value" type="string" required>
  Value of the custom variable. Required.
</ResponseField>

## Output properties


