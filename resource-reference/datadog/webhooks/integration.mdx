---
title: "datadog_webhooks_integration"
description: "Real, generated bindings for `datadog_webhooks_integration`."
---

`datadog_webhooks_integration` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/webhooks"
    )

    cfg := webhooks.IntegrationConfig{
        Name: "example-name",
        Url: "example",
        WebhookName: "example-webhook-name",
    }
    ubx.Resource(webhooks.Integration, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Integration } from "./local-sdk/datadog/sdk/typescript/datadog/webhooks/integration.ts";

    resource(Integration, "example", {
      name: "example-name",
      url: "example",
      webhookName: "example-webhook-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.webhooks import Integration, IntegrationConfig

    cfg = IntegrationConfig(
        name="example-name",
        url="example",
        webhook_name="example-webhook-name",
    )
    ubx.resource(Integration, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_webhooks_integration with name, url,
webhook_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="custom_headers" type="string">
  If `null`, uses no header. If given a JSON payload, these will be headers attached to your webhook. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="encode_as" type="string">
  Encoding type. Can be given either `json` or `form`. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the webhook. It corresponds with `&lt;WEBHOOK_NAME&gt;`. Learn more on how to use it in [monitor notifications](https://docs.datadoghq.com/monitors/notify). Required.
</ResponseField>

<ResponseField name="payload" type="string">
  If `null`, uses the default payload. If given a JSON payload, the webhook returns the payload specified by the given payload. [Webhooks variable usage](https://docs.datadoghq.com/integrations/webhooks/#usage). Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="url" type="string" required>
  URL of the webhook. Required.
</ResponseField>

<ResponseField name="webhook_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="custom_headers" type="string">
  If `null`, uses no header. If given a JSON payload, these will be headers attached to your webhook. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="encode_as" type="string">
  Encoding type. Can be given either `json` or `form`. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="payload" type="string">
  If `null`, uses the default payload. If given a JSON payload, the webhook returns the payload specified by the given payload. [Webhooks variable usage](https://docs.datadoghq.com/integrations/webhooks/#usage). Optional; if omitted, computed by Datadog.
</ResponseField>
