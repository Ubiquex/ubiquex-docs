---
title: "datadog_google_chat_target_audience_response"
description: "`datadog_google_chat_target_audience_response` is the real, allowed audience configuration for a Google Chat integration -- which real Google Workspace..."
---

`datadog_google_chat_target_audience_response` is the real, allowed audience configuration for a Google Chat integration -- which real Google Workspace domain/audience Datadog is authorized to post Chat messages to, part of Google Chat's own webhook authentication model.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	google "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/google"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own chat target audience response"})

    		ubx.Resource(google.ChatTargetAudienceResponse, "example", google.ChatTargetAudienceResponseConfig{
    			Data:                  map[string]any{"attributes": map[string]any{"audience_id": "example", "audience_name": "example-audience-name"}, "type": "example"},
    			OrganizationBindingId: "example",
    			TargetAudienceId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ChatTargetAudienceResponse } from "@ubx/sdk-datadog/datadog/google/chat_target_audience_response";

    export default stack("example", () => {
      intent({ summary: "example own chat target audience response" });

      resource(ChatTargetAudienceResponse, "example", {
        data: {
          attributes: {
            audienceId: "example",
            audienceName: "example-audience-name",
          },
          type: "example",
        },
        organizationBindingId: "example",
        targetAudienceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.google import ChatTargetAudienceResponse, ChatTargetAudienceResponseConfig
    from ubx.datadog.google.chat_target_audience_response import ChatTargetAudienceResponse_Data, ChatTargetAudienceResponse_Data_Attributes

    def describe():
        ubx.intent("example own chat target audience response")

        ubx.resource(ChatTargetAudienceResponse, "example", ChatTargetAudienceResponseConfig(
            data=ChatTargetAudienceResponse_Data(attributes=ChatTargetAudienceResponse_Data_Attributes(audience_id="example", audience_name="example-audience-name"), type="example"),
            organization_binding_id="example",
            target_audience_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_google_chat_target_audience_response, with data a
    set value, organization binding id example, target audience id
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Data for a create target audience request. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for creating a Google Chat target audience. Required.
      <Expandable title="properties">
        <ResponseField name="audience_id" type="string" required>
          The audience ID. Required.
        </ResponseField>
        <ResponseField name="audience_name" type="string" required>
          The audience name. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Google Chat target audience resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="organization_binding_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="target_audience_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
