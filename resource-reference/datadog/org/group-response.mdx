---
title: "datadog_org_group_response"
description: "`datadog_org_group_response` is a real group of Datadog child organizations under a parent org -- the structural unit Datadog's multi-org management..."
---

`datadog_org_group_response` is a real group of Datadog child organizations under a parent org -- the structural unit Datadog's multi-org management features (shared policy, centralized billing visibility) operate on for enterprises running many separate Datadog orgs.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	org "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/org"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own group response"})

    		ubx.Resource(org.GroupResponse, "example", org.GroupResponseConfig{
    			Data:       map[string]any{"attributes": map[string]any{"name": "example-name"}},
    			OrgGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupResponse } from "@ubx/sdk-datadog/datadog/org/group_response";

    export default stack("example", () => {
      intent({ summary: "example own group response" });

      resource(GroupResponse, "example", {
        data: { attributes: { name: "example-name" } },
        orgGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.org import GroupResponse, GroupResponseConfig

    def describe():
        ubx.intent("example own group response")

        ubx.resource(GroupResponse, "example", GroupResponseConfig(
            data={"attributes": {"name": "example-name"}},
            org_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_org_group_response, with data a set value, org
    group id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Data for creating an org group. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for creating an org group. Required.
      <Expandable title="properties">
        <ResponseField name="name" type="string" required>
          The name of the org group. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Org groups resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="org_group_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
