---
title: "datadog_pager_duty_service_name"
description: "Real, generated bindings for `datadog_pager_duty_service_name`."
---

`datadog_pager_duty_service_name` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/pager"
    )

    cfg := pager.DutyServiceNameConfig{
        ServiceKey: "example",
        ServiceName: "example-service-name",
    }
    ubx.Resource(pager.DutyServiceName, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { DutyServiceName } from "./local-sdk/datadog/sdk/typescript/datadog/pager/duty_service_name.ts";

    resource(DutyServiceName, "example", {
      serviceKey: "example",
      serviceName: "example-service-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.pager import (
        DutyServiceName,
        DutyServiceNameConfig,
    )

    cfg = DutyServiceNameConfig(
        service_key="example",
        service_name="example-service-name",
    )
    ubx.resource(DutyServiceName, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_pager_duty_service_name with service_key,
service_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="service_key" type="string" required>
  Your service key in PagerDuty. Required.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Your service name associated with a service key in PagerDuty. Required.
</ResponseField>

## Output properties


