---
title: "datadog_user_response"
description: "Real, generated bindings for `datadog_user_response`."
---

`datadog_user_response` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/user"
    )

    cfg := user.ResponseConfig{
        UserHandle: "example",
        AccessRole: "example",
        Disabled: true,
    }
    ubx.Resource(user.Response, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { Response } from "./local-sdk/datadog/sdk/typescript/datadog/user/response.ts";

    resource(Response, "example", {
      userHandle: "example",
      accessRole: "example",
      disabled: true,
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.user import Response, ResponseConfig

    cfg = ResponseConfig(
        user_handle="example",
        access_role="example",
        disabled=True,
    )
    ubx.resource(Response, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_user_response with user_handle, access_role,
disabled.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_role" type="string">
  The access role of the user. Options are **st** (standard user), **adm** (admin user), or **ro** (read-only user). Optional.
</ResponseField>

<ResponseField name="disabled" type="bool">
  The new disabled status of the user. Optional.
</ResponseField>

<ResponseField name="email" type="string">
  The new email of the user. Optional.
</ResponseField>

<ResponseField name="handle" type="string">
  The user handle, must be a valid email. Optional.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the user. Optional.
</ResponseField>

<ResponseField name="user_handle" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="icon" type="string">
  Gravatar icon associated to the user. Computed by Datadog after `ship`.
</ResponseField>

<ResponseField name="user" type="object">
  Create, edit, and disable users. Computed by Datadog after `ship`.
  <Expandable title="properties">
    <ResponseField name="access_role" type="string">
      The access role of the user. Options are **st** (standard user), **adm** (admin user), or **ro** (read-only user). Optional.
    </ResponseField>
    <ResponseField name="disabled" type="bool">
      The new disabled status of the user. Optional.
    </ResponseField>
    <ResponseField name="email" type="string">
      The new email of the user. Optional.
    </ResponseField>
    <ResponseField name="handle" type="string">
      The user handle, must be a valid email. Optional.
    </ResponseField>
    <ResponseField name="icon" type="string">
      Gravatar icon associated to the user. Computed by Datadog after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the user. Optional.
    </ResponseField>
    <ResponseField name="verified" type="bool">
      Whether or not the user logged in Datadog at least once. Computed by Datadog after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="verified" type="bool">
  Whether or not the user logged in Datadog at least once. Computed by Datadog after `ship`.
</ResponseField>
