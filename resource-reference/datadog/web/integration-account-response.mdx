---
title: "datadog_web_integration_account_response"
description: "`datadog_web_integration_account_response` is a real, connected account for one of Datadog's simpler..."
---

`datadog_web_integration_account_response` is a real, connected account for one of Datadog's simpler, credential-based web integrations -- the general shape behind an integration that only needs an account identifier and a secret, rather than a full OAuth flow.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	web "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/web"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own integration account response"})

    		ubx.Resource(web.IntegrationAccountResponse, "example", web.IntegrationAccountResponseConfig{
    			AccountId:       "example",
    			Data:            map[string]any{"attributes": map[string]any{"name": "example-name", "secrets": map[string]any{"key": "value"}, "settings": map[string]any{"key": "value"}}, "type": "example"},
    			IntegrationName: "example-integration-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IntegrationAccountResponse } from "@ubx/sdk-datadog/datadog/web/integration_account_response";

    export default stack("example", () => {
      intent({ summary: "example own integration account response" });

      resource(IntegrationAccountResponse, "example", {
        accountId: "example",
        data: {
          attributes: {
            name: "example-name",
            secrets: { key: "value" },
            settings: { key: "value" },
          },
          type: "example",
        },
        integrationName: "example-integration-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.web import IntegrationAccountResponse, IntegrationAccountResponseConfig
    from ubx.datadog.web.integration_account_response import IntegrationAccountResponse_Data, IntegrationAccountResponse_Data_Attributes

    def describe():
        ubx.intent("example own integration account response")

        ubx.resource(IntegrationAccountResponse, "example", IntegrationAccountResponseConfig(
            account_id="example",
            data=IntegrationAccountResponse_Data(attributes=IntegrationAccountResponse_Data_Attributes(name="example-name", secrets={"key": "value"}, settings={"key": "value"}), type="example"),
            integration_name="example-integration-name",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_web_integration_account_response, with account id
    example, data a set value, integration name
    example-integration-name.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="account_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="data" type="object" required>
  Data object for creating a web integration account. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes object for creating a web integration account. Required.
      <Expandable title="properties">
        <ResponseField name="name" type="string" required>
          A human-readable name for the account. Must be unique among accounts of the same integration. Required.
        </ResponseField>
        <ResponseField name="secrets" type="map(any)" required>
          Integration-specific secrets. The shape of this object varies by integration. Secrets are write-only and never returned by the API. Required.
        </ResponseField>
        <ResponseField name="settings" type="map(any)" required>
          Integration-specific settings. The shape of this object varies by integration. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Account resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="integration_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
