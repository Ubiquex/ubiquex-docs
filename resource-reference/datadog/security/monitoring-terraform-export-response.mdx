---
title: "datadog_security_monitoring_terraform_export_response"
description: "`datadog_security_monitoring_terraform_export_response` is a real, generated Terraform representation of an organization's own Cloud SIEM detection..."
---

`datadog_security_monitoring_terraform_export_response` is a real, generated Terraform representation of an organization's own Cloud SIEM detection rule configuration -- lets a team export their live security-monitoring setup as real, reviewable infrastructure-as-code.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	security "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/security"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own monitoring terraform export response"})

    		ubx.Resource(security.MonitoringTerraformExportResponse, "example", security.MonitoringTerraformExportResponseConfig{
    			Data:         map[string]any{"attributes": map[string]any{"resource_json": map[string]any{"key": "value"}}},
    			ResourceId:   "example",
    			ResourceType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MonitoringTerraformExportResponse } from "@ubx/sdk-datadog/datadog/security/monitoring_terraform_export_response";

    export default stack("example", () => {
      intent({ summary: "example own monitoring terraform export response" });

      resource(MonitoringTerraformExportResponse, "example", {
        data: { attributes: { resourceJson: { key: "value" } } },
        resourceId: "example",
        resourceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.security import MonitoringTerraformExportResponse, MonitoringTerraformExportResponseConfig

    def describe():
        ubx.intent("example own monitoring terraform export response")

        ubx.resource(MonitoringTerraformExportResponse, "example", MonitoringTerraformExportResponseConfig(
            data={"attributes": {"resource_json": {"key": "value"}}},
            resource_id="example",
            resource_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_security_monitoring_terraform_export_response,
    with data a set value, resource id example, resource type
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  The convert request data object. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for the convert request. Required.
      <Expandable title="properties">
        <ResponseField name="resource_json" type="map(any)" required>
          The resource attributes as a JSON object, matching the structure returned by the corresponding Datadog API (for example, the attributes of a suppression rule). Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="id" type="string" required>
      The ID of the resource being converted. Required.
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type. Always `convert_resource`. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="resource_type" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
