---
title: "datadog_application_key_response"
description: "`datadog_application_key_response` works alongside an API key to authorize programmatic access to Datadog's own API."
---

Unlike an API key, an application key is tied to the user who created it and carries that user's own permissions -- it's what a script or integration uses to act on Datadog data on that user's behalf, not what an agent uses to submit it.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	application "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/application"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own key response"})

    		ubx.Resource(application.KeyResponse, "example", application.KeyResponseConfig{
    			Key:  "example",
    			Name: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyResponse } from "./local-sdk/datadog/sdk/typescript/datadog/application/key_response.ts";

    export default stack("example", () => {
      intent({ summary: "example own key response" });

      resource(KeyResponse, "example", {
        key: "example",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.application import KeyResponse, KeyResponseConfig

    def describe():
        ubx.intent("example own key response")

        ubx.resource(KeyResponse, "example", KeyResponseConfig(
            key="example",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_application_key_response called "example", with
    key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="key" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="name" type="string">
  Name of an application key. Optional.
</ResponseField>

## Output properties

<ResponseField name="application_key" type="object">
  An application key with its associated metadata. Computed by Datadog after `ship`.
  <Expandable title="properties">
    <ResponseField name="hash" type="string">
      Hash of an application key. Computed by Datadog after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of an application key. Optional.
    </ResponseField>
    <ResponseField name="owner" type="string">
      Owner of an application key. Computed by Datadog after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hash" type="string">
  Hash of an application key. Computed by Datadog after `ship`.
</ResponseField>

<ResponseField name="owner" type="string">
  Owner of an application key. Computed by Datadog after `ship`.
</ResponseField>
