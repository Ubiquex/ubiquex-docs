---
title: "datadog_application_key_response"
description: "Real, generated bindings for `datadog_application_key_response`."
---

`datadog_application_key_response` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/application"
    )

    cfg := application.KeyResponseConfig{
        Key: "example",
        Name: "example-name",
    }
    ubx.Resource(application.KeyResponse, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { KeyResponse } from "./local-sdk/datadog/sdk/typescript/datadog/application/key_response.ts";

    resource(KeyResponse, "example", {
      key: "example",
      name: "example-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.application import (
        KeyResponse,
        KeyResponseConfig,
    )

    cfg = KeyResponseConfig(
        key="example",
        name="example-name",
    )
    ubx.resource(KeyResponse, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_application_key_response with key, name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="key" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="name" type="string">
  Name of an application key. Optional.
</ResponseField>

## Output properties

<ResponseField name="application_key" type="object">
  An application key with its associated metadata. Computed by Datadog after `ship`.
  <Expandable title="properties">
    <ResponseField name="hash" type="string">
      Hash of an application key. Computed by Datadog after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of an application key. Optional.
    </ResponseField>
    <ResponseField name="owner" type="string">
      Owner of an application key. Computed by Datadog after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hash" type="string">
  Hash of an application key. Computed by Datadog after `ship`.
</ResponseField>

<ResponseField name="owner" type="string">
  Owner of an application key. Computed by Datadog after `ship`.
</ResponseField>
