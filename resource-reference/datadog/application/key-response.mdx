---
title: "datadog_application_key_response"
description: "A complete, runnable datadog_application_key_response program, in every SDK language."
---

`datadog_application_key_response` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	application "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/application"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key response"})

    		ubx.Resource(application.KeyResponse, "ci-runner", application.KeyResponseConfig{
    			Key:  "example",
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyResponse } from "./local-sdk/datadog/sdk/typescript/datadog/application/key_response.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own key response" });

      resource(KeyResponse, "ci-runner", {
        key: "example",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.application import KeyResponse, KeyResponseConfig

    def describe():
        ubx.intent("payments own key response")

        ubx.resource(KeyResponse, "ci-runner", KeyResponseConfig(
            key="example",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_application_key_response called "ci-runner", with
    key example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="key" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="name" type="string">
  Name of an application key. Optional.
</ResponseField>

## Output properties

<ResponseField name="application_key" type="object">
  An application key with its associated metadata. Computed by Datadog after `ship`.
  <Expandable title="properties">
    <ResponseField name="hash" type="string">
      Hash of an application key. Computed by Datadog after `ship`.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of an application key. Optional.
    </ResponseField>
    <ResponseField name="owner" type="string">
      Owner of an application key. Computed by Datadog after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="hash" type="string">
  Hash of an application key. Computed by Datadog after `ship`.
</ResponseField>

<ResponseField name="owner" type="string">
  Owner of an application key. Computed by Datadog after `ship`.
</ResponseField>
