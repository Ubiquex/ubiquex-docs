---
title: "datadog_application_security_policy_response"
description: "`datadog_application_security_policy_response` is a policy governing Datadog Application Security Management (ASM) behavior for a service -- which..."
---

`datadog_application_security_policy_response` is a policy governing Datadog Application Security Management (ASM) behavior for a service -- which protections (in-app WAF, threat detection) are active and how ASM should respond when it detects an attack.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	application "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/application"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own security policy response"})

    		ubx.Resource(application.SecurityPolicyResponse, "example", application.SecurityPolicyResponseConfig{
    			Data:     map[string]any{"attributes": map[string]any{"based_on": "example", "description": "example", "name": "example-name"}, "type": "example"},
    			PolicyId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityPolicyResponse } from "@ubx/sdk-datadog/datadog/application/security_policy_response";

    export default stack("example", () => {
      intent({ summary: "example own security policy response" });

      resource(SecurityPolicyResponse, "example", {
        data: {
          attributes: {
            basedOn: "example",
            description: "example",
            name: "example-name",
          },
          type: "example",
        },
        policyId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.application import SecurityPolicyResponse, SecurityPolicyResponseConfig
    from ubx.datadog.application.security_policy_response import SecurityPolicyResponse_Data, SecurityPolicyResponse_Data_Attributes

    def describe():
        ubx.intent("example own security policy response")

        ubx.resource(SecurityPolicyResponse, "example", SecurityPolicyResponseConfig(
            data=SecurityPolicyResponse_Data(attributes=SecurityPolicyResponse_Data_Attributes(based_on="example", description="example", name="example-name"), type="example"),
            policy_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_application_security_policy_response, with data a
    set value, policy id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Object for a single WAF policy. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Create a new WAF policy. Required.
      <Expandable title="properties">
        <ResponseField name="based_on" type="string" required>
          When creating a new policy, clone the policy indicated by this identifier. Required.
        </ResponseField>
        <ResponseField name="description" type="string" required>
          Description of the WAF policy. Required.
        </ResponseField>
        <ResponseField name="is_default" type="bool">
          Make this policy the default policy. The default policy is applied to every service not specifically assigned to another policy. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the WAF policy. Required.
        </ResponseField>
        <ResponseField name="protection_presets" type="list(string)">
          Presets enabled on this policy. Optional.
        </ResponseField>
        <ResponseField name="rules" type="list(object)">
          Rule overrides applied by the policy. Optional.
          <Expandable title="properties">
            <ResponseField name="blocking" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="extended_data_collection" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="id" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="rulesets" type="list(object)">
          Deprecated: Ruleset overrides. Use `protectionPresets` instead. Optional.
          <Expandable title="properties">
            <ResponseField name="blocking" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="enabled" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="id" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="scope" type="list(object)">
          The scope of the WAF policy. Optional.
          <Expandable title="properties">
            <ResponseField name="env" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="service" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="version" type="number">
          Version of the WAF ruleset maintained by Datadog used by this policy. 0 is the default value. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The type of the resource. The value should always be `policy`. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="policy_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
