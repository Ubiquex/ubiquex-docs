---
title: "datadog_form_response"
description: "`datadog_form_response` is a real, structured form definition used inside Datadog Workflow Automation and App Builder -- the real fields, layout..."
---

`datadog_form_response` is a real, structured form definition used inside Datadog Workflow Automation and App Builder -- the real fields, layout, and validation a user-facing intake form (used to kick off a workflow or gather structured input) is built from.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	form "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/form"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own response"})

    		ubx.Resource(form.Response, "example", form.ResponseConfig{
    			Data:   map[string]any{"attributes": map[string]any{"name": "example-name"}},
    			FormId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Response } from "./local-sdk/datadog/sdk/typescript/datadog/form/response.ts";

    export default stack("example", () => {
      intent({ summary: "example own response" });

      resource(Response, "example", {
        data: { attributes: { name: "example-name" } },
        formId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.form import Response, ResponseConfig

    def describe():
        ubx.intent("example own response")

        ubx.resource(Response, "example", ResponseConfig(
            data={"attributes": {"name": "example-name"}},
            form_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_form_response, with data a set value, form id
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  The data for creating a form. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      The attributes for creating a form. Required.
      <Expandable title="properties">
        <ResponseField name="anonymous" type="bool">
          Whether the form accepts anonymous submissions. Optional.
        </ResponseField>
        <ResponseField name="data_definition" type="object" required>
          A JSON Schema definition that describes the form's data fields. Required.
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              A description shown to form respondents. Optional.
            </ResponseField>
            <ResponseField name="properties" type="map(any)">
              A map of field names to their JSON Schema definitions. Optional.
            </ResponseField>
            <ResponseField name="required" type="list(string)">
              List of field names that must be answered. Optional.
            </ResponseField>
            <ResponseField name="title" type="string">
              The title of the form schema. Optional.
            </ResponseField>
            <ResponseField name="type" type="string">
              The root schema type. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          The description of the form. Optional.
        </ResponseField>
        <ResponseField name="idp_survey" type="bool">
          Whether the form is an IDP survey. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the form. Required.
        </ResponseField>
        <ResponseField name="single_response" type="bool">
          Whether each user can only submit one response. Optional.
        </ResponseField>
        <ResponseField name="ui_definition" type="object" required>
          UI configuration for rendering form fields, including widget overrides, field ordering, and themes. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The resource type for a form. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="form_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
