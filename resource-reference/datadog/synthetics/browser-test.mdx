---
title: "datadog_synthetics_browser_test"
description: "A complete, runnable datadog_synthetics_browser_test program, in every SDK language."
---

`datadog_synthetics_browser_test` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own browser test"})

    		ubx.Resource(synthetics.BrowserTest, "ci-runner", synthetics.BrowserTestConfig{
    			Config:    map[string]any{"assertions": []map[string]any{{"code": "example"}}},
    			Locations: []string{"example"},
    			Message:   "example",
    			Name:      "ci-runner",
    			Options:   map[string]any{"accept_self_signed": true},
    			Type:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { BrowserTest } from "./local-sdk/datadog/sdk/typescript/datadog/synthetics/browser_test.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own browser test" });

      resource(BrowserTest, "ci-runner", {
        config: { assertions: [{ code: "example" }] },
        locations: ["example"],
        message: "example",
        name: "ci-runner",
        options: { acceptSelfSigned: true },
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.synthetics import BrowserTest, BrowserTestConfig

    def describe():
        ubx.intent("payments own browser test")

        ubx.resource(BrowserTest, "ci-runner", BrowserTestConfig(
            config={"assertions": [{"code": "example"}]},
            locations=["example"],
            message="example",
            name="ci-runner",
            options={"accept_self_signed": True},
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_synthetics_browser_test called "ci-runner", with
    config a set value, locations a set value, message example,
    options a set value, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config" type="object" required>
  Configuration object for a Synthetic browser test. Required.
  <Expandable title="properties">
    <ResponseField name="assertions" type="list(object)" required>
      Array of assertions used for the test. Required.
      <Expandable title="properties">
        <ResponseField name="code" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="operator" type="string">
          The comparison operator used in the assertion. Allowed values include: contains, doesNotContain, is, isNot, lessThan, lessThanOrEqual, moreThan, moreThanOrEqual, matches, doesNotMatch, validates, isInMoreThan, isInLessThan, doesNotExist, isUndefined. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="property" type="string">
          Specifies the property of the response to evaluate, such as a header name when using header assertions. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="target" type="any">
          The expected value to compare against when evaluating the assertion. The data type depends on the assertion property being validated. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="timings_scope" type="string">
          Specifies which timings are included in the assertion: `all` includes all timings, `withoutDNS` excludes DNS resolution timing. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="type" type="string">
          Specifies the kind of assertion to apply, such as 'statusCode', 'body', 'header', or 'responseTime'. The allowed values in this context are: body, header, statusCode, certificate, responseTime, property, recordEvery, recordSome, tlsVersion, minTlsVersion, latency, packetLossPercentage, packetsReceived, networkHop, receivedMessage, grpcHealthcheckStatus, grpcMetadata, grpcProto, connection, multiNetworkHop, jitter, mcpToolNameLength, mcpToolCount. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="config_variables" type="list(object)">
      Array of variables used for the test. Optional.
      <Expandable title="properties">
        <ResponseField name="example" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="name" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="pattern" type="string">
          Regular expression pattern used to generate random values for the config variable during test execution. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="secure" type="bool">
          Whether the config variable is a secure (hidden) variable. When set to true, the variable's value is not displayed in the UI or API responses. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of configuration variable. Allowed values are: `global`, `text`, `email`. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="request" type="object" required>
      Object describing the Synthetic test request. Required.
      <Expandable title="properties">
        <ResponseField name="allow_insecure" type="bool">
          Allows loading insecure content for an HTTP request in a multistep test step. Optional.
        </ResponseField>
        <ResponseField name="basic_auth" type="object">
          Object to handle basic authentication when performing the test. Optional.
          <Expandable title="properties">
            <ResponseField name="access_key" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="access_token_url" type="string">
              The URL used to obtain an OAuth access token during authentication for the synthetic request. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="add_claims" type="object">
              Map of custom claims to add to the authentication request, where each key is the claim name and the corresponding value is the claim value. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
              <Expandable title="properties">
                <ResponseField name="exp" type="bool">
                  Optional; if omitted, computed by Datadog.
                </ResponseField>
                <ResponseField name="iat" type="bool">
                  If true, includes the 'issued at' (iat) claim in the JWT token used for basic authentication. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="algorithm" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="audience" type="string">
              The audience for the OAuth2 token, used when the basic auth type is set to 'oauth2'. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="client_id" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="client_secret" type="string">
              The client secret used for OAuth client authentication in the request's basic_auth configuration. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="domain" type="string">
              The domain for NTLM authentication in the basic auth configuration. Used when the auth type is set to 'ntlm'. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="expires_in" type="number">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="header" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="password" type="string">
              The password to use for basic authentication against the requested endpoint. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="payload" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="region" type="string">
              The AWS region to use for SigV4 authentication. Only relevant when the basic authentication type is set to `sigv4`. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="resource" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="scope" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="secret" type="string">
              The password or secret used for basic authentication against the endpoint. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="secret_key" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="service_name" type="string">
              The service name to use for NTLM authentication within the basic auth configuration. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="session_token" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="token_api_authentication" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="token_prefix" type="string">
              The prefix (scheme) to prepend to the token in the Authorization header, e.g., 'Bearer'. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="type" type="string">
              The type of basic authentication to use for the request. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="username" type="string">
              The username used for basic authentication in the Synthetics browser test request. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="workstation" type="string">
              The workstation name for NTLM authentication. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="body" type="string">
          Body to include in the test. Optional.
        </ResponseField>
        <ResponseField name="body_type" type="string">
          Type of the request body. Optional.
        </ResponseField>
        <ResponseField name="call_type" type="string">
          The type of call to perform. Used by gRPC steps (`healthcheck`, `unary`) and MCP steps (`init`, `tool_list`, `tool_call`). Valid values depend on the parent step's `subtype`. Optional.
        </ResponseField>
        <ResponseField name="certificate" type="object">
          Client certificate to use when performing the test request. Optional.
          <Expandable title="properties">
            <ResponseField name="cert" type="object">
              Define a request certificate. Optional.
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  Content of the certificate or key. Optional.
                </ResponseField>
                <ResponseField name="filename" type="string">
                  File name for the certificate or key. Optional.
                </ResponseField>
                <ResponseField name="updated_at" type="string">
                  Date of update of the certificate or key, ISO format. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="key" type="object">
              Define a request certificate. Optional.
              <Expandable title="properties">
                <ResponseField name="content" type="string">
                  Content of the certificate or key. Optional.
                </ResponseField>
                <ResponseField name="filename" type="string">
                  File name for the certificate or key. Optional.
                </ResponseField>
                <ResponseField name="updated_at" type="string">
                  Date of update of the certificate or key, ISO format. Optional.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="certificate_domains" type="list(string)">
          By default, the client certificate is applied on the domain of the starting URL for browser tests. If you want your client certificate to be applied on other domains instead, add them in `certificateDomains`. Optional.
        </ResponseField>
        <ResponseField name="check_certificate_revocation" type="bool">
          Check for certificate revocation. Optional.
        </ResponseField>
        <ResponseField name="compressed_json_descriptor" type="string">
          A protobuf JSON descriptor that needs to be gzipped first then base64 encoded. Optional.
        </ResponseField>
        <ResponseField name="compressed_proto_file" type="string">
          A protobuf file that needs to be gzipped first then base64 encoded. Optional.
        </ResponseField>
        <ResponseField name="disable_aia_intermediate_fetching" type="bool">
          Disable fetching intermediate certificates from AIA. Optional.
        </ResponseField>
        <ResponseField name="dns_server" type="string">
          DNS server to use for DNS tests. Optional.
        </ResponseField>
        <ResponseField name="dns_server_port" type="any">
          DNS server port to use for DNS tests. Optional.
        </ResponseField>
        <ResponseField name="files" type="list(object)">
          Files to be used as part of the request in the test. Only valid if `bodyType` is `multipart/form-data`. Optional.
          <Expandable title="properties">
            <ResponseField name="bucket_key" type="string">
              The object key (path) of the file within the S3 bucket to be uploaded for the browser test. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="content" type="string">
              The base64-encoded content of the file to be uploaded in the browser test request. The maximum length of the string is 3,145,728 characters (3 MB). Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="encoding" type="string">
              Encoding of the file content, such as 'base64'. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="name" type="string">
              The name of the file to be sent in the request, used as the filename in multipart form data. Must be at most 1500 characters. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="original_file_name" type="string">
              The original filename of the file to be uploaded in the browser test step. Must be a string with a maximum length of 1500 characters. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="size" type="number">
              The size of the file, in bytes. The value must be between 1 and 3,145,728 bytes (3 MB). Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="type" type="string">
              The MIME type (content type) of the uploaded file, such as 'application/json' or 'image/png'. Must be at most 1500 characters. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="follow_redirects" type="bool">
          Specifies whether or not the request follows redirects. Optional.
        </ResponseField>
        <ResponseField name="form" type="map(string)">
          Form to be used as part of the request in the test. Only valid if `bodyType` is `multipart/form-data`. Optional.
        </ResponseField>
        <ResponseField name="headers" type="map(string)">
          Headers to include when performing the test. Optional.
        </ResponseField>
        <ResponseField name="host" type="string">
          Host name to perform the test with. Optional.
        </ResponseField>
        <ResponseField name="http_version" type="string">
          HTTP version to use for a Synthetic test. Optional.
        </ResponseField>
        <ResponseField name="ignore_certificate_validation" type="bool">
          For SSL tests, whether the test should ignore certificate validation. Optional.
        </ResponseField>
        <ResponseField name="is_message_base64_encoded" type="bool">
          Whether the message is base64 encoded. Optional.
        </ResponseField>
        <ResponseField name="mcp_protocol_version" type="string">
          The MCP protocol version used by the step. See https://modelcontextprotocol.io/specification. Optional.
        </ResponseField>
        <ResponseField name="message" type="string">
          Message to send for UDP or WebSocket tests. Optional.
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          Metadata to include when performing the gRPC test. Optional.
        </ResponseField>
        <ResponseField name="method" type="string">
          Either the HTTP method/verb to use or a gRPC method available on the service set in the `service` field. Required if `subtype` is `HTTP` or if `subtype` is `grpc` and `callType` is `unary`. Optional.
        </ResponseField>
        <ResponseField name="no_saving_response_body" type="bool">
          Determines whether or not to save the response body. Optional.
        </ResponseField>
        <ResponseField name="number_of_packets" type="number">
          Number of pings to use per test. Optional.
        </ResponseField>
        <ResponseField name="persist_cookies" type="bool">
          Persist cookies across redirects. Optional.
        </ResponseField>
        <ResponseField name="port" type="any">
          Port to use when performing the test. Optional.
        </ResponseField>
        <ResponseField name="proxy" type="object">
          The proxy to perform the test. Optional.
          <Expandable title="properties">
            <ResponseField name="headers" type="map(string)">
              Headers to include when performing the test. Optional.
            </ResponseField>
            <ResponseField name="url" type="string" required>
              URL of the proxy to perform the test. Required.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="query" type="any">
          Query to use for the test. Optional.
        </ResponseField>
        <ResponseField name="servername" type="string">
          For SSL tests, it specifies on which server you want to initiate the TLS handshake, allowing the server to present one of multiple possible certificates on the same IP address and TCP port number. Optional.
        </ResponseField>
        <ResponseField name="service" type="string">
          The gRPC service on which you want to perform the gRPC call. Optional.
        </ResponseField>
        <ResponseField name="should_track_hops" type="bool">
          Turns on a traceroute probe to discover all gateways along the path to the host destination. Optional.
        </ResponseField>
        <ResponseField name="timeout" type="number">
          Timeout in seconds for the test. Optional.
        </ResponseField>
        <ResponseField name="tool_args" type="map(any)">
          Arguments to pass to the MCP tool. Free-form object whose shape depends on the tool. Used when `callType` is `tool_call`. Optional.
        </ResponseField>
        <ResponseField name="tool_name" type="string">
          The name of the MCP tool to call. Required when `callType` is `tool_call`. Optional.
        </ResponseField>
        <ResponseField name="url" type="string">
          URL to perform the test with. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="set_cookie" type="string">
      Cookies to be used for the request, using the [Set-Cookie](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie) syntax. Optional.
    </ResponseField>
    <ResponseField name="variables" type="list(object)">
      Array of variables used for the test steps. Optional.
      <Expandable title="properties">
        <ResponseField name="example" type="string">
          The example value for the variable. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the variable, used to reference this variable in the browser test configuration. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="pattern" type="string">
          Regular expression pattern used to generate random values for the variable. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="secure" type="bool">
          Set to true to mark the variable as secure, preventing its value from being displayed in test results and logs. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the variable used in the browser test. Valid values are `element`, `email`, `global`, and `text`. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="locations" type="list(string)" required>
  Array of locations used to run the test. Required.
</ResponseField>

<ResponseField name="message" type="string" required>
  Notification message associated with the test. Message can either be text or an empty string. Required.
</ResponseField>

<ResponseField name="monitor_id" type="number">
  The associated monitor ID. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the test. Required.
</ResponseField>

<ResponseField name="options" type="object" required>
  Object describing the extra options for a Synthetic test. Required.
  <Expandable title="properties">
    <ResponseField name="accept_self_signed" type="bool">
      For SSL tests, whether or not the test should allow self signed certificates. Optional.
    </ResponseField>
    <ResponseField name="allow_insecure" type="bool">
      Allows loading insecure content for an HTTP request in an API test. Optional.
    </ResponseField>
    <ResponseField name="blocked_request_patterns" type="list(string)">
      Array of URL patterns to block. Optional.
    </ResponseField>
    <ResponseField name="capture_network_payloads" type="bool">
      Capture HTTP request/response headers and bodies for Fetch/XHR calls made during browser tests. Optional.
    </ResponseField>
    <ResponseField name="check_certificate_revocation" type="bool">
      For SSL tests, whether or not the test should fail on revoked certificate in stapled OCSP. Optional.
    </ResponseField>
    <ResponseField name="ci" type="object">
      CI/CD options for a Synthetic test. Optional.
      <Expandable title="properties">
        <ResponseField name="execution_rule" type="string" required>
          Execution rule for a Synthetic test. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="device_ids" type="list(string)">
      For browser test, array with the different device IDs used to run the test. Optional.
    </ResponseField>
    <ResponseField name="disable_aia_intermediate_fetching" type="bool">
      For SSL tests, whether or not the test should disable fetching intermediate certificates from AIA. Optional.
    </ResponseField>
    <ResponseField name="disable_cors" type="bool">
      Whether or not to disable CORS mechanism. Optional.
    </ResponseField>
    <ResponseField name="disable_csp" type="bool">
      Disable Content Security Policy for browser tests. Optional.
    </ResponseField>
    <ResponseField name="enable_profiling" type="bool">
      Enable profiling for browser tests. Optional.
    </ResponseField>
    <ResponseField name="enable_security_testing" type="bool">
      Enable security testing for browser tests. Security testing is not available anymore. This field is deprecated and won't be used. Optional.
    </ResponseField>
    <ResponseField name="follow_redirects" type="bool">
      For API HTTP test, whether or not the test should follow redirects. Optional.
    </ResponseField>
    <ResponseField name="http_version" type="string">
      HTTP version to use for a Synthetic test. Optional.
    </ResponseField>
    <ResponseField name="ignore_certificate_validation" type="bool">
      For SSL tests, whether the test should ignore certificate validation. Optional.
    </ResponseField>
    <ResponseField name="ignore_server_certificate_error" type="bool">
      Ignore server certificate error for browser tests. Optional.
    </ResponseField>
    <ResponseField name="initial_navigation_timeout" type="number">
      Timeout before declaring the initial step as failed (in seconds) for browser tests. Optional.
    </ResponseField>
    <ResponseField name="min_failure_duration" type="number">
      Minimum amount of time in failure required to trigger an alert. Optional.
    </ResponseField>
    <ResponseField name="min_location_failed" type="number">
      Minimum number of locations in failure required to trigger an alert. Optional.
    </ResponseField>
    <ResponseField name="monitor_name" type="string">
      The monitor name is used for the alert title as well as for all monitor dashboard widgets and SLOs. Optional.
    </ResponseField>
    <ResponseField name="monitor_options" type="object">
      Object containing the options for a Synthetic test as a monitor (for example, renotification). Optional.
      <Expandable title="properties">
        <ResponseField name="escalation_message" type="string">
          Message to include in the escalation notification. Optional.
        </ResponseField>
        <ResponseField name="notification_preset_name" type="string">
          The name of the preset for the notification for the monitor. Optional.
        </ResponseField>
        <ResponseField name="renotify_interval" type="number">
          Time interval before renotifying if the test is still failing (in minutes). Optional.
        </ResponseField>
        <ResponseField name="renotify_occurrences" type="number">
          The number of times to renotify if the test is still failing. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="monitor_priority" type="number">
      Integer from 1 (high) to 5 (low) indicating alert severity. Optional.
    </ResponseField>
    <ResponseField name="no_screenshot" type="bool">
      Prevents saving screenshots of the steps. Optional.
    </ResponseField>
    <ResponseField name="restricted_roles" type="list(string)">
      A list of role identifiers that can be pulled from the Roles API, for restricting read and write access. This field is deprecated. Use the restriction policies API to manage permissions. Optional.
    </ResponseField>
    <ResponseField name="retry" type="object">
      Object describing the retry strategy to apply to a Synthetic test. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Number of times a test needs to be retried before marking a location as failed. Defaults to 0. Optional.
        </ResponseField>
        <ResponseField name="interval" type="number">
          Time interval between retries (in milliseconds). Defaults to 300ms. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="rum_settings" type="object">
      The RUM data collection settings for the Synthetic browser test. **Note:** There are 3 ways to format RUM settings: `&#123; isEnabled: false &#125;` RUM data is not collected. `&#123; isEnabled: true &#125;` RUM data is collected from the Synthetic test's default application. `&#123; isEnabled: true, applicationId: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx", clientTokenId: 12345 &#125;` RUM data is collected using the specified application. Optional.
      <Expandable title="properties">
        <ResponseField name="application_id" type="string">
          RUM application ID used to collect RUM data for the browser test. Optional.
        </ResponseField>
        <ResponseField name="client_token_id" type="number">
          RUM application API key ID used to collect RUM data for the browser test. Optional.
        </ResponseField>
        <ResponseField name="is_enabled" type="bool" required>
          Determines whether RUM data is collected during test runs. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Object containing timeframes and timezone used for advanced scheduling. Optional.
      <Expandable title="properties">
        <ResponseField name="timeframes" type="list(object)" required>
          Array containing objects describing the scheduling pattern to apply to each day. Required.
          <Expandable title="properties">
            <ResponseField name="day" type="number">
              The day of the week (1=Monday, 7=Sunday) that this timeframe applies to. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="from" type="string">
              The start time of the scheduling timeframe, formatted as HH:mm in 24-hour time. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="to" type="string">
              The end time of the scheduling time window. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="timezone" type="string" required>
          Timezone in which the timeframe is based. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tick_every" type="number">
      The frequency at which to run the Synthetic test (in seconds). Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="public_id" type="string">
  The public ID of the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="status" type="string">
  Define whether you want to start (`live`) or pause (`paused`) a Synthetic test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  Array of steps for the test. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="allow_failure" type="bool">
      When set to true, failures in this step are allowed and do not fail the entire test. This is useful for steps that may be conditional or non-critical. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="always_execute" type="bool">
      Boolean that indicates whether the step should always be executed, even if a previous step fails. If set to false, the step will be skipped when a preceding step fails. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="exit_if_succeed" type="bool">
      If set to `true`, the test stops and is marked as successful when this step succeeds. Defaults to `false`. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="is_critical" type="bool">
      Whether the step is critical. If set to `true`, a failure of this step causes the entire synthetic test to fail. If `false`, the step can fail without failing the test. Defaults to `false`. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="no_screenshot" type="bool">
      When set to true, screenshots are not taken for this step. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="params" type="any">
      A dynamic object containing step-specific configuration, such as the element to interact with, text to input, or assertion details, depending on the step type in a Datadog synthetic browser test. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="public_id" type="string">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="timeout" type="number">
      The maximum time, in milliseconds, to wait for the step to complete before failing. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of step in the browser test, determining the action to be performed. Allowed values include click, typeText, hover, wait, assertCurrentUrl, assertElementPresent, and others. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(string)">
  Array of tags attached to the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="type" type="string" required>
  Type of the Synthetic test, `browser`. Required.
</ResponseField>

## Output properties

<ResponseField name="monitor_id" type="number">
  The associated monitor ID. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="public_id" type="string">
  The public ID of the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="status" type="string">
  Define whether you want to start (`live`) or pause (`paused`) a Synthetic test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  Array of steps for the test. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="allow_failure" type="bool">
      When set to true, failures in this step are allowed and do not fail the entire test. This is useful for steps that may be conditional or non-critical. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="always_execute" type="bool">
      Boolean that indicates whether the step should always be executed, even if a previous step fails. If set to false, the step will be skipped when a preceding step fails. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="exit_if_succeed" type="bool">
      If set to `true`, the test stops and is marked as successful when this step succeeds. Defaults to `false`. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="is_critical" type="bool">
      Whether the step is critical. If set to `true`, a failure of this step causes the entire synthetic test to fail. If `false`, the step can fail without failing the test. Defaults to `false`. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="no_screenshot" type="bool">
      When set to true, screenshots are not taken for this step. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="params" type="any">
      A dynamic object containing step-specific configuration, such as the element to interact with, text to input, or assertion details, depending on the step type in a Datadog synthetic browser test. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="public_id" type="string">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="timeout" type="number">
      The maximum time, in milliseconds, to wait for the step to complete before failing. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of step in the browser test, determining the action to be performed. Allowed values include click, typeText, hover, wait, assertCurrentUrl, assertElementPresent, and others. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(string)">
  Array of tags attached to the test. Optional; if omitted, computed by Datadog.
</ResponseField>
