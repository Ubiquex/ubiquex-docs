---
title: "datadog_synthetics_mobile_test"
description: "A complete, runnable datadog_synthetics_mobile_test program, in every SDK language."
---

`datadog_synthetics_mobile_test` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mobile test"})

    		ubx.Resource(synthetics.MobileTest, "ci-runner", synthetics.MobileTestConfig{
    			Config:  map[string]any{"initial_application_arguments": map[string]string{"key": "value"}},
    			Message: "example",
    			Name:    "ci-runner",
    			Options: map[string]any{"device_ids": []string{"example"}},
    			Type:    "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { MobileTest } from "./local-sdk/datadog/sdk/typescript/datadog/synthetics/mobile_test.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own mobile test" });

      resource(MobileTest, "ci-runner", {
        config: { initialApplicationArguments: { key: "value" } },
        message: "example",
        name: "ci-runner",
        options: { deviceIds: ["example"] },
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.synthetics import MobileTest, MobileTestConfig

    def describe():
        ubx.intent("payments own mobile test")

        ubx.resource(MobileTest, "ci-runner", MobileTestConfig(
            config={"initial_application_arguments": {"key": "value"}},
            message="example",
            name="ci-runner",
            options={"device_ids": ["example"]},
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_synthetics_mobile_test called "ci-runner", with
    config a set value, message example, options a set value, type
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="config" type="object" required>
  Configuration object for a Synthetic mobile test. Required.
  <Expandable title="properties">
    <ResponseField name="initial_application_arguments" type="map(string)">
      Initial application arguments for a mobile test. Optional.
    </ResponseField>
    <ResponseField name="variables" type="list(object)">
      Array of variables used for the test steps. Optional.
      <Expandable title="properties">
        <ResponseField name="example" type="string">
          Sample value for the variable, used as an example in the test configuration. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="id" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="name" type="string">
          The name of the variable, used to reference it within the test configuration. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="pattern" type="string">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="secure" type="bool">
          Whether the variable is a secure variable, meaning its value is encrypted and hidden in the UI. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="type" type="string">
          The type of the Synthetics mobile test variable. Valid values are `global` (references a global variable), `text` (a literal text value), and `email` (a generated email address). Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="device_ids" type="list(string)">
  Array with the different device IDs used to run the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="message" type="string" required>
  Notification message associated with the test. Required.
</ResponseField>

<ResponseField name="monitor_id" type="number">
  The associated monitor ID. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the test. Required.
</ResponseField>

<ResponseField name="options" type="object" required>
  Object describing the extra options for a Synthetic test. Required.
  <Expandable title="properties">
    <ResponseField name="allow_application_crash" type="bool">
      A boolean to set if an application crash would mark the test as failed. Optional.
    </ResponseField>
    <ResponseField name="bindings" type="list(object)">
      Array of bindings used for the mobile test. Optional.
      <Expandable title="properties">
        <ResponseField name="principals" type="list(string)">
          List of principals (users, groups, or roles) that this binding applies to. Each principal is a string identifier such as a user email or a group ID. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="relation" type="string">
          The type of access granted to the bound role. Allowed values are `editor` (full edit access) and `viewer` (read-only access). Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="ci" type="object">
      CI/CD options for a Synthetic test. Optional.
      <Expandable title="properties">
        <ResponseField name="execution_rule" type="string" required>
          Execution rule for a Synthetic test. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="default_step_timeout" type="number">
      The default timeout for steps in the test (in seconds). Optional.
    </ResponseField>
    <ResponseField name="device_ids" type="list(string)" required>
      For mobile test, array with the different device IDs used to run the test. Required.
    </ResponseField>
    <ResponseField name="disable_auto_accept_alert" type="bool">
      A boolean to disable auto accepting alerts. Optional.
    </ResponseField>
    <ResponseField name="min_failure_duration" type="number">
      Minimum amount of time in failure required to trigger an alert. Optional.
    </ResponseField>
    <ResponseField name="mobile_application" type="object" required>
      Mobile application for mobile synthetics test. Required.
      <Expandable title="properties">
        <ResponseField name="application_id" type="string" required>
          Application ID of the mobile application. Required.
        </ResponseField>
        <ResponseField name="reference_id" type="string" required>
          Reference ID of the mobile application. Required.
        </ResponseField>
        <ResponseField name="reference_type" type="string" required>
          Reference type for the mobile application for a mobile synthetics test. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="monitor_name" type="string">
      The monitor name is used for the alert title as well as for all monitor dashboard widgets and SLOs. Optional.
    </ResponseField>
    <ResponseField name="monitor_options" type="object">
      Object containing the options for a Synthetic test as a monitor (for example, renotification). Optional.
      <Expandable title="properties">
        <ResponseField name="escalation_message" type="string">
          Message to include in the escalation notification. Optional.
        </ResponseField>
        <ResponseField name="notification_preset_name" type="string">
          The name of the preset for the notification for the monitor. Optional.
        </ResponseField>
        <ResponseField name="renotify_interval" type="number">
          Time interval before renotifying if the test is still failing (in minutes). Optional.
        </ResponseField>
        <ResponseField name="renotify_occurrences" type="number">
          The number of times to renotify if the test is still failing. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="monitor_priority" type="number">
      Integer from 1 (high) to 5 (low) indicating alert severity. Optional.
    </ResponseField>
    <ResponseField name="no_screenshot" type="bool">
      A boolean set to not take a screenshot for the step. Optional.
    </ResponseField>
    <ResponseField name="restricted_roles" type="list(string)">
      A list of role identifiers that can be pulled from the Roles API, for restricting read and write access. This field is deprecated. Use the restriction policies API to manage permissions. Optional.
    </ResponseField>
    <ResponseField name="retry" type="object">
      Object describing the retry strategy to apply to a Synthetic test. Optional.
      <Expandable title="properties">
        <ResponseField name="count" type="number">
          Number of times a test needs to be retried before marking a location as failed. Defaults to 0. Optional.
        </ResponseField>
        <ResponseField name="interval" type="number">
          Time interval between retries (in milliseconds). Defaults to 300ms. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="scheduling" type="object">
      Object containing timeframes and timezone used for advanced scheduling. Optional.
      <Expandable title="properties">
        <ResponseField name="timeframes" type="list(object)" required>
          Array containing objects describing the scheduling pattern to apply to each day. Required.
          <Expandable title="properties">
            <ResponseField name="day" type="number">
              The day of the week for this scheduling timeframe, where 1 is Monday and 7 is Sunday. Must be between 1 and 7. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="from" type="string">
              Start time of the time frame during which the test is scheduled to run. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="to" type="string">
              The end time of the scheduling timeframe, formatted as HH:MM. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="timezone" type="string" required>
          Timezone in which the timeframe is based. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tick_every" type="number" required>
      The frequency at which to run the Synthetic test (in seconds). Required.
    </ResponseField>
    <ResponseField name="verbosity" type="number">
      The level of verbosity for the mobile test. This field can not be set by a user. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="public_id" type="string">
  The public ID of the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="status" type="string">
  Define whether you want to start (`live`) or pause (`paused`) a Synthetic test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  Array of steps for the test. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="allow_failure" type="bool">
      Determines if the mobile test step can fail without marking the entire test as failed. When set to true, a failure in this step will not fail the overall test. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="has_new_step_element" type="bool">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="is_critical" type="bool">
      A boolean indicating whether this step is critical. If a critical step fails, the entire test is considered failed; non-critical steps do not cause failure on their own. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the step in the Datadog Synthetics mobile test. Must be at most 1500 characters. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="no_screenshot" type="bool">
      Whether to disable the screenshot capture for this step. Set to true to prevent Datadog from taking a screenshot during this step. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="params" type="object">
      Optional; if omitted, computed by Datadog.
      <Expandable title="properties">
        <ResponseField name="check" type="string">
          The type of check to perform on the target value. Allowed values: equals, notEquals, contains, notContains, startsWith, notStartsWith, greater, lower, greaterEquals, lowerEquals, matchRegex, between, isEmpty, notIsEmpty. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="delay" type="number">
          Delay in milliseconds to wait before executing the step. Must be between 0 and 5000. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="direction" type="string">
          Specifies the direction of the mobile gesture (e.g., swipe), with allowed values: up, down, left, right. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="element" type="object">
          Optional; if omitted, computed by Datadog.
          <Expandable title="properties">
            <ResponseField name="context" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="context_type" type="string">
              Specifies whether the element is in a native or web context. Allowed values are 'native' and 'web'. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="element_description" type="string">
              A description of the mobile UI element targeted by the step. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="multi_locator" type="any">
              A list of alternative locators for the element, allowing the synthetics mobile test to use any of these locators to find the element during test execution. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="relative_position" type="object">
              Optional; if omitted, computed by Datadog.
              <Expandable title="properties">
                <ResponseField name="x" type="number">
                  The x-coordinate of the element's relative position within the parent context, used to define the element's location for interaction in the mobile test step. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
                <ResponseField name="y" type="number">
                  The y-coordinate of the element's relative position, used to define the vertical offset from the reference element. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="text_content" type="string">
              The text content of the on-screen element used to identify and interact with it in the mobile test step. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="user_locator" type="object">
              Optional; if omitted, computed by Datadog.
              <Expandable title="properties">
                <ResponseField name="fail_test_on_cannot_locate" type="bool">
                  If true, the mobile test step is marked as failed when the specified user locator fails to locate the element. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
                <ResponseField name="values" type="list(object)">
                  Optional; if omitted, computed by Datadog.
                  <Expandable title="properties">
                    <ResponseField name="type" type="string">
                      The type of locator used to identify a mobile element. Allowed values: accessibility-id, id, ios-predicate-string, ios-class-chain, xpath. Optional; if omitted, computed by Datadog.

                      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      A single string value in the user locator 'values' list, used to identify the mobile element during the test step. Optional; if omitted, computed by Datadog.

                      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="view_name" type="string">
              The name of the view (screen) in the mobile application where the element is located. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="max_scrolls" type="number">
          The maximum number of scroll actions to perform during this mobile test step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="positions" type="list(object)">
          Optional; if omitted, computed by Datadog.
          <Expandable title="properties">
            <ResponseField name="x" type="number">
              The x-coordinate (horizontal position) of the point on the screen, in pixels. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="y" type="number">
              The y-coordinate of the position, representing the vertical location on the screen. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subtest_public_id" type="string">
          The public ID of the subtest to run in this step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="value" type="any">
          The value associated with a parameter for a step in the mobile test. The type is dynamic and can vary based on the specific step and parameter. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="variable" type="object">
          Optional; if omitted, computed by Datadog.
          <Expandable title="properties">
            <ResponseField name="example" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="with_enter" type="bool">
          Whether to press the Enter key after inputting text in the step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="x" type="number">
          The horizontal (x) coordinate for the interaction point in the mobile test step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="y" type="number">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_id" type="string">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="timeout" type="number">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of step to perform in the mobile test. Possible values include assertElementContent, assertScreenContains, assertScreenLacks, doubleTap, extractVariable, flick, openDeeplink, playSubTest, pressBack, restartApplication, rotate, scroll, scrollToElement, tap, toggleWiFi, typeText, and wait. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(string)">
  Array of tags attached to the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="type" type="string" required>
  Type of the Synthetic test, `mobile`. Required.
</ResponseField>

## Output properties

<ResponseField name="device_ids" type="list(string)">
  Array with the different device IDs used to run the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="monitor_id" type="number">
  The associated monitor ID. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="public_id" type="string">
  The public ID of the test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="status" type="string">
  Define whether you want to start (`live`) or pause (`paused`) a Synthetic test. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="steps" type="list(object)">
  Array of steps for the test. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="allow_failure" type="bool">
      Determines if the mobile test step can fail without marking the entire test as failed. When set to true, a failure in this step will not fail the overall test. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="has_new_step_element" type="bool">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="is_critical" type="bool">
      A boolean indicating whether this step is critical. If a critical step fails, the entire test is considered failed; non-critical steps do not cause failure on their own. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name of the step in the Datadog Synthetics mobile test. Must be at most 1500 characters. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="no_screenshot" type="bool">
      Whether to disable the screenshot capture for this step. Set to true to prevent Datadog from taking a screenshot during this step. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
    <ResponseField name="params" type="object">
      Optional; if omitted, computed by Datadog.
      <Expandable title="properties">
        <ResponseField name="check" type="string">
          The type of check to perform on the target value. Allowed values: equals, notEquals, contains, notContains, startsWith, notStartsWith, greater, lower, greaterEquals, lowerEquals, matchRegex, between, isEmpty, notIsEmpty. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="delay" type="number">
          Delay in milliseconds to wait before executing the step. Must be between 0 and 5000. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="direction" type="string">
          Specifies the direction of the mobile gesture (e.g., swipe), with allowed values: up, down, left, right. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="element" type="object">
          Optional; if omitted, computed by Datadog.
          <Expandable title="properties">
            <ResponseField name="context" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="context_type" type="string">
              Specifies whether the element is in a native or web context. Allowed values are 'native' and 'web'. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="element_description" type="string">
              A description of the mobile UI element targeted by the step. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="multi_locator" type="any">
              A list of alternative locators for the element, allowing the synthetics mobile test to use any of these locators to find the element during test execution. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="relative_position" type="object">
              Optional; if omitted, computed by Datadog.
              <Expandable title="properties">
                <ResponseField name="x" type="number">
                  The x-coordinate of the element's relative position within the parent context, used to define the element's location for interaction in the mobile test step. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
                <ResponseField name="y" type="number">
                  The y-coordinate of the element's relative position, used to define the vertical offset from the reference element. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="text_content" type="string">
              The text content of the on-screen element used to identify and interact with it in the mobile test step. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="user_locator" type="object">
              Optional; if omitted, computed by Datadog.
              <Expandable title="properties">
                <ResponseField name="fail_test_on_cannot_locate" type="bool">
                  If true, the mobile test step is marked as failed when the specified user locator fails to locate the element. Optional; if omitted, computed by Datadog.

                  **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                </ResponseField>
                <ResponseField name="values" type="list(object)">
                  Optional; if omitted, computed by Datadog.
                  <Expandable title="properties">
                    <ResponseField name="type" type="string">
                      The type of locator used to identify a mobile element. Allowed values: accessibility-id, id, ios-predicate-string, ios-class-chain, xpath. Optional; if omitted, computed by Datadog.

                      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                    </ResponseField>
                    <ResponseField name="value" type="string">
                      A single string value in the user locator 'values' list, used to identify the mobile element during the test step. Optional; if omitted, computed by Datadog.

                      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="view_name" type="string">
              The name of the view (screen) in the mobile application where the element is located. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
        <ResponseField name="max_scrolls" type="number">
          The maximum number of scroll actions to perform during this mobile test step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="positions" type="list(object)">
          Optional; if omitted, computed by Datadog.
          <Expandable title="properties">
            <ResponseField name="x" type="number">
              The x-coordinate (horizontal position) of the point on the screen, in pixels. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
            <ResponseField name="y" type="number">
              The y-coordinate of the position, representing the vertical location on the screen. Optional; if omitted, computed by Datadog.

              **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="subtest_public_id" type="string">
          The public ID of the subtest to run in this step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="value" type="any">
          The value associated with a parameter for a step in the mobile test. The type is dynamic and can vary based on the specific step and parameter. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="variable" type="object">
          Optional; if omitted, computed by Datadog.
          <Expandable title="properties">
            <ResponseField name="example" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="name" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="with_enter" type="bool">
          Whether to press the Enter key after inputting text in the step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="x" type="number">
          The horizontal (x) coordinate for the interaction point in the mobile test step. Optional; if omitted, computed by Datadog.

          **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
        </ResponseField>
        <ResponseField name="y" type="number">
          Optional; if omitted, computed by Datadog.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="public_id" type="string">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="timeout" type="number">
      Optional; if omitted, computed by Datadog.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of step to perform in the mobile test. Possible values include assertElementContent, assertScreenContains, assertScreenLacks, doubleTap, extractVariable, flick, openDeeplink, playSubTest, pressBack, restartApplication, rotate, scroll, scrollToElement, tap, toggleWiFi, typeText, and wait. Optional; if omitted, computed by Datadog.

      **AI-inferred** -- not sourced from the real provider schema; generated by ubx's own description pipeline and never independently verified against the real provider.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(string)">
  Array of tags attached to the test. Optional; if omitted, computed by Datadog.
</ResponseField>
