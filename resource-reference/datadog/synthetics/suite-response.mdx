---
title: "datadog_synthetics_suite_response"
description: "`datadog_synthetics_suite_response` is a real, named group of Synthetic Monitoring tests run and reported on together -- lets a team organize related..."
---

`datadog_synthetics_suite_response` is a real, named group of Synthetic Monitoring tests run and reported on together -- lets a team organize related synthetic checks (all the tests for one user journey, for example) as one unit.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own suite response"})

    		ubx.Resource(synthetics.SuiteResponse, "example", synthetics.SuiteResponseConfig{
    			Data:     map[string]any{"attributes": map[string]any{"name": "example-name", "options": map[string]any{"alerting_threshold": 1}, "tests": []map[string]any{{"alerting_criticality": "example"}}, "type": "example"}, "type": "example"},
    			PublicId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SuiteResponse } from "@ubx/sdk-datadog/datadog/synthetics/suite_response";

    export default stack("example", () => {
      intent({ summary: "example own suite response" });

      resource(SuiteResponse, "example", {
        data: {
          attributes: {
            name: "example-name",
            options: { alertingThreshold: 1 },
            tests: [{ alertingCriticality: "example" }],
            type: "example",
          },
          type: "example",
        },
        publicId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.synthetics import SuiteResponse, SuiteResponseConfig
    from ubx.datadog.synthetics.suite_response import SuiteResponse_Data, SuiteResponse_Data_Attributes, SuiteResponse_Data_Attributes_Options, SuiteResponse_Data_Attributes_Tests

    def describe():
        ubx.intent("example own suite response")

        ubx.resource(SuiteResponse, "example", SuiteResponseConfig(
            data=SuiteResponse_Data(attributes=SuiteResponse_Data_Attributes(name="example-name", options=SuiteResponse_Data_Attributes_Options(alerting_threshold=1), tests=[SuiteResponse_Data_Attributes_Tests(alerting_criticality="example")], type="example"), type="example"),
            public_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_synthetics_suite_response, with data a set value,
    public id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Data object for creating or editing a Synthetic test suite. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Object containing details about a Synthetic suite. Required.
      <Expandable title="properties">
        <ResponseField name="message" type="string">
          Notification message associated with the suite. Optional.
        </ResponseField>
        <ResponseField name="monitor_id" type="number">
          The associated monitor ID. Computed by Datadog after `ship`.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Name of the suite. Required.
        </ResponseField>
        <ResponseField name="options" type="object" required>
          Object describing the extra options for a Synthetic suite. Required.
          <Expandable title="properties">
            <ResponseField name="alerting_threshold" type="number">
              Percentage of critical tests failure needed for a suite to fail. Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="public_id" type="string">
          The public ID for the test. Computed by Datadog after `ship`.
        </ResponseField>
        <ResponseField name="tags" type="list(string)">
          Array of tags attached to the suite. Optional.
        </ResponseField>
        <ResponseField name="tests" type="list(object)" required>
          Array of Synthetic tests included in the suite. Required.
          <Expandable title="properties">
            <ResponseField name="alerting_criticality" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="public_id" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="type" type="string" required>
          Type of the Synthetic suite, `suite`. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Type for the Synthetics suites responses, `suites`. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="public_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
