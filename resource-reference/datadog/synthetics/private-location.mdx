---
title: "datadog_synthetics_private_location"
description: "Real, generated bindings for `datadog_synthetics_private_location`."
---

`datadog_synthetics_private_location` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/synthetics"
    )

    cfg := synthetics.PrivateLocationConfig{
        Description: "example",
        LocationId: "example",
        Name: "example-name",
        Tags: []string{"example"},
    }
    ubx.Resource(synthetics.PrivateLocation, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { PrivateLocation } from "./local-sdk/datadog/sdk/typescript/datadog/synthetics/private_location.ts";

    resource(PrivateLocation, "example", {
      description: "example",
      locationId: "example",
      name: "example-name",
      tags: ["example"],
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.synthetics import (
        PrivateLocation,
        PrivateLocationConfig,
    )

    cfg = PrivateLocationConfig(
        description="example",
        location_id="example",
        name="example-name",
        tags=["example"],
    )
    ubx.resource(PrivateLocation, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_synthetics_private_location with description,
location_id, name, tags.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string" required>
  Description of the private location. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Unique identifier of the private location. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="location_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="metadata" type="object">
  Object containing metadata about the private location. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="restricted_roles" type="list(string)">
      A list of role identifiers that can be pulled from the Roles API, for restricting read and write access. This field is deprecated. Use the restriction policies API to manage permissions. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string" required>
  Name of the private location. Required.
</ResponseField>

<ResponseField name="secrets" type="object">
  Secrets for the private location. Only present in the response when creating the private location. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="authentication" type="object">
      Authentication part of the secrets. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Access key for the private location. Computed by Datadog after `ship`.
        </ResponseField>
        <ResponseField name="key" type="string">
          Secret access key for the private location. Computed by Datadog after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="config_decryption" type="object">
      Private key for the private location. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Private key for the private location. Computed by Datadog after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="tags" type="list(string)" required>
  Array of tags attached to the private location. Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Unique identifier of the private location. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="metadata" type="object">
  Object containing metadata about the private location. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="restricted_roles" type="list(string)">
      A list of role identifiers that can be pulled from the Roles API, for restricting read and write access. This field is deprecated. Use the restriction policies API to manage permissions. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secrets" type="object">
  Secrets for the private location. Only present in the response when creating the private location. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="authentication" type="object">
      Authentication part of the secrets. Optional.
      <Expandable title="properties">
        <ResponseField name="id" type="string">
          Access key for the private location. Computed by Datadog after `ship`.
        </ResponseField>
        <ResponseField name="key" type="string">
          Secret access key for the private location. Computed by Datadog after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="config_decryption" type="object">
      Private key for the private location. Optional.
      <Expandable title="properties">
        <ResponseField name="key" type="string">
          Private key for the private location. Computed by Datadog after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
