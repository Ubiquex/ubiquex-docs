---
title: "datadog_dashboard_list"
description: "`datadog_dashboard_list` is a named collection used to organize dashboards so a team can search, browse..."
---

`datadog_dashboard_list` is a named collection used to organize dashboards so a team can search, browse, and share a related set of them instead of hunting through every dashboard in the organization individually.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dashboard "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/dashboard"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own list"})

    		ubx.Resource(dashboard.List, "example", dashboard.ListConfig{
    			ListId: "example",
    			Name:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { List } from "@ubx/sdk-datadog/datadog/dashboard/list";

    export default stack("example", () => {
      intent({ summary: "example own list" });

      resource(List, "example", {
        listId: "example",
        name: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.dashboard import List, ListConfig

    def describe():
        ubx.intent("example own list")

        ubx.resource(List, "example", ListConfig(
            list_id="example",
            name="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_dashboard_list called "example", with list id
    example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="author" type="object">
  Object describing the creator of the shared element. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="email" type="string">
      Email of the creator. Optional.
    </ResponseField>
    <ResponseField name="handle" type="string">
      Handle of the creator. Optional.
    </ResponseField>
    <ResponseField name="name" type="string">
      Name of the creator. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="created" type="string">
  Date of creation of the dashboard list. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="dashboard_count" type="number">
  The number of dashboards in the list. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="id" type="number">
  The ID of the dashboard list. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="is_favorite" type="bool">
  Whether or not the list is in the favorites. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="list_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="modified" type="string">
  Date of last edition of the dashboard list. Optional; if omitted, computed by Datadog.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the dashboard list. Required.
</ResponseField>

<ResponseField name="type" type="string">
  The type of dashboard list. Optional; if omitted, computed by Datadog.
</ResponseField>
