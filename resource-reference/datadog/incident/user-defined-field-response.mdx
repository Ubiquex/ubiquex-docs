---
title: "datadog_incident_user_defined_field_response"
description: "`datadog_incident_user_defined_field_response` is a real, custom field an organization adds to its own incidents beyond Datadog's built-in fields..."
---

`datadog_incident_user_defined_field_response` is a real, custom field an organization adds to its own incidents beyond Datadog's built-in fields -- a way to capture org-specific data (a customer-impact tier, an internal tracking ID) on every incident.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	incident "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/incident"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own user defined field response"})

    		ubx.Resource(incident.UserDefinedFieldResponse, "example", incident.UserDefinedFieldResponseConfig{
    			Data:    map[string]any{"attributes": map[string]any{"name": "example-name", "type": 1}, "relationships": map[string]any{"incident_type": map[string]any{"data": map[string]any{"id": "example", "type": "example"}}}, "type": "example"},
    			FieldId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserDefinedFieldResponse } from "@ubx/sdk-datadog/datadog/incident/user_defined_field_response";

    export default stack("example", () => {
      intent({ summary: "example own user defined field response" });

      resource(UserDefinedFieldResponse, "example", {
        data: {
          attributes: { name: "example-name", type: 1 },
          relationships: {
            incidentType: { data: { id: "example", type: "example" } },
          },
          type: "example",
        },
        fieldId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.incident import UserDefinedFieldResponse, UserDefinedFieldResponseConfig
    from ubx.datadog.incident.user_defined_field_response import UserDefinedFieldResponse_Data, UserDefinedFieldResponse_Data_Attributes, UserDefinedFieldResponse_Data_Relationships, UserDefinedFieldResponse_Data_Relationships_IncidentType, UserDefinedFieldResponse_Data_Relationships_IncidentType_Data

    def describe():
        ubx.intent("example own user defined field response")

        ubx.resource(UserDefinedFieldResponse, "example", UserDefinedFieldResponseConfig(
            data=UserDefinedFieldResponse_Data(attributes=UserDefinedFieldResponse_Data_Attributes(name="example-name", type=1), relationships=UserDefinedFieldResponse_Data_Relationships(incident_type=UserDefinedFieldResponse_Data_Relationships_IncidentType(data=UserDefinedFieldResponse_Data_Relationships_IncidentType_Data(id="example", type="example"))), type="example"),
            field_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_incident_user_defined_field_response, with data a
    set value, field id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Data for creating an incident user-defined field. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for creating an incident user-defined field. Required.
      <Expandable title="properties">
        <ResponseField name="category" type="string">
          The section in which the field appears: "what_happened" or "why_it_happened". When null, the field appears in the Attributes section. Optional.
        </ResponseField>
        <ResponseField name="collected" type="string">
          The lifecycle stage at which the app prompts users to fill out this field. Cannot be set on required fields. Optional.
        </ResponseField>
        <ResponseField name="default_value" type="string">
          The default value for the field. Must be one of the valid values when valid_values is set. Optional.
        </ResponseField>
        <ResponseField name="display_name" type="string">
          The human-readable name shown in the UI. Defaults to a formatted version of the name if not provided. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The unique identifier of the field. Must start with a letter or digit and contain only letters, digits, underscores, or periods. Required.
        </ResponseField>
        <ResponseField name="ordinal" type="string">
          A decimal string representing the field's display order in the UI. Optional.
        </ResponseField>
        <ResponseField name="required" type="bool">
          When true, users must fill out this field on incidents. Optional.
        </ResponseField>
        <ResponseField name="tag_key" type="string">
          For metric tag-type fields only, the metric tag key that powers the autocomplete options. Optional.
        </ResponseField>
        <ResponseField name="type" type="number" required>
          The data type of the field. 1=dropdown, 2=multiselect, 3=textbox, 4=textarray, 5=metrictag, 6=autocomplete, 7=number, 8=datetime. Required.
        </ResponseField>
        <ResponseField name="valid_values" type="list(object)">
          The list of allowed values for dropdown and multiselect fields. Limited to 1000 values. Optional.
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="display_name" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="short_description" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="value" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="relationships" type="object" required>
      Relationships for creating an incident user-defined field. Required.
      <Expandable title="properties">
        <ResponseField name="incident_type" type="object" required>
          Relationship to an incident type. Required.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Relationship to incident type object. Required.
              <Expandable title="properties">
                <ResponseField name="id" type="string" required>
                  The incident type's ID. Required.
                </ResponseField>
                <ResponseField name="type" type="string" required>
                  Incident type resource type. Required.
                </ResponseField>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The incident user defined fields type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="field_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
