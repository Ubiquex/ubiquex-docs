---
title: "datadog_campaign_response"
description: "`datadog_campaign_response` is a real, structured initiative inside Datadog's Security Inbox / vulnerability remediation workflow -- a grouped set of..."
---

`datadog_campaign_response` is a real, structured initiative inside Datadog's Security Inbox / vulnerability remediation workflow -- a grouped set of findings assigned to a team with a real, tracked goal (fix these vulnerabilities by this date), not an individual finding itself.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	campaign "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/campaign"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own response"})

    		ubx.Resource(campaign.Response, "example", campaign.ResponseConfig{
    			CampaignId: "example",
    			Data:       map[string]any{"attributes": map[string]any{"key": "example", "name": "example-name", "owner_id": "example", "rule_ids": []string{"example"}, "start_date": "example"}, "type": "example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Response } from "@ubx/sdk-datadog/datadog/campaign/response";

    export default stack("example", () => {
      intent({ summary: "example own response" });

      resource(Response, "example", {
        campaignId: "example",
        data: {
          attributes: {
            key: "example",
            name: "example-name",
            ownerId: "example",
            ruleIds: ["example"],
            startDate: "example",
          },
          type: "example",
        },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.campaign import Response, ResponseConfig
    from ubx.datadog.campaign.response import Response_Data, Response_Data_Attributes

    def describe():
        ubx.intent("example own response")

        ubx.resource(Response, "example", ResponseConfig(
            campaign_id="example",
            data=Response_Data(attributes=Response_Data_Attributes(key="example", name="example-name", owner_id="example", rule_ids=["example"], start_date="example"), type="example"),
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_campaign_response, with campaign id example, data
    a set value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="campaign_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="data" type="object" required>
  Data for creating a new campaign. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for creating a new campaign. Required.
      <Expandable title="properties">
        <ResponseField name="description" type="string">
          The description of the campaign. Optional.
        </ResponseField>
        <ResponseField name="due_date" type="string">
          The due date of the campaign. Optional.
        </ResponseField>
        <ResponseField name="entity_scope" type="string">
          Entity scope query to filter entities for this campaign. Optional.
        </ResponseField>
        <ResponseField name="guidance" type="string">
          Guidance for the campaign. Optional.
        </ResponseField>
        <ResponseField name="key" type="string" required>
          The unique key for the campaign. Required.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the campaign. Required.
        </ResponseField>
        <ResponseField name="owner_id" type="string" required>
          The UUID of the campaign owner. Required.
        </ResponseField>
        <ResponseField name="rule_ids" type="list(string)" required>
          Array of rule IDs associated with this campaign. Required.
        </ResponseField>
        <ResponseField name="start_date" type="string" required>
          The start date of the campaign. Required.
        </ResponseField>
        <ResponseField name="status" type="string">
          The status of the campaign. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type for campaigns. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
