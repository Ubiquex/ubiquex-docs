---
title: "datadog_severity_modifier_rule_response"
description: "`datadog_severity_modifier_rule_response` is a real rule that adjusts a security finding's or incident's severity based on real context (a critical..."
---

`datadog_severity_modifier_rule_response` is a real rule that adjusts a security finding's or incident's severity based on real context (a critical asset, a specific environment) -- lets an organization's own risk context override a detection's default severity.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	severity "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/severity"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own modifier rule response"})

    		ubx.Resource(severity.ModifierRuleResponse, "example", severity.ModifierRuleResponseConfig{
    			Data:   map[string]any{"attributes": map[string]any{"action": map[string]any{"description": "example"}, "name": "example-name", "rule": map[string]any{"finding_types": []string{"example"}}}, "type": "example"},
    			RuleId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ModifierRuleResponse } from "@ubx/sdk-datadog/datadog/severity/modifier_rule_response";

    export default stack("example", () => {
      intent({ summary: "example own modifier rule response" });

      resource(ModifierRuleResponse, "example", {
        data: {
          attributes: {
            action: { description: "example" },
            name: "example-name",
            rule: { findingTypes: ["example"] },
          },
          type: "example",
        },
        ruleId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.severity import ModifierRuleResponse, ModifierRuleResponseConfig
    from ubx.datadog.severity.modifier_rule_response import ModifierRuleResponse_Data, ModifierRuleResponse_Data_Attributes, ModifierRuleResponse_Data_Attributes_Action, ModifierRuleResponse_Data_Attributes_Rule

    def describe():
        ubx.intent("example own modifier rule response")

        ubx.resource(ModifierRuleResponse, "example", ModifierRuleResponseConfig(
            data=ModifierRuleResponse_Data(attributes=ModifierRuleResponse_Data_Attributes(action=ModifierRuleResponse_Data_Attributes_Action(description="example"), name="example-name", rule=ModifierRuleResponse_Data_Attributes_Rule(finding_types=["example"])), type="example"),
            rule_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_severity_modifier_rule_response, with data a set
    value, rule id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  The data object for a severity modifier rule create or update request. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes for creating or updating a severity modifier rule. Required.
      <Expandable title="properties">
        <ResponseField name="action" type="object" required>
          The action to take when a severity modifier rule matches a finding. This is a discriminated union on `type`: `set` assigns a fixed severity, while `shift` moves the severity up or down by one rank. A severity modifier rule's `rule.query` must not filter on `@severity` or on the `@severity_details.user_adjusted.*` namespace. Use `@severity_details.adjusted.value` instead, which reflects the severity before user-defined adjustments. Required.
          <Expandable title="properties">
            <ResponseField name="description" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="severity" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="severity_delta" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="type" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="enabled" type="bool">
          Whether the severity modifier rule is enabled. Optional.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          The name of the severity modifier rule. Required.
        </ResponseField>
        <ResponseField name="rule" type="object" required>
          Defines the scope of findings to which the automation rule applies. Required.
          <Expandable title="properties">
            <ResponseField name="finding_types" type="list(string)" required>
              The list of security finding types that the automation rule applies to. Required.
            </ResponseField>
            <ResponseField name="query" type="string">
              A search query to further filter the findings matched by this rule. The `@workflow.*` namespace and `@status` fields are not permitted. For a reference of available fields, see the [Security Findings schema documentation](https://docs.datadoghq.com/security/guide/findings-schema/). Optional.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      The JSON:API type for severity modifier rules. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rule_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
