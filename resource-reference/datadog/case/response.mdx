---
title: "datadog_case_response"
description: "`datadog_case_response` is a real case inside Datadog Case Management -- a trackable unit of work (a security incident..."
---

`datadog_case_response` is a real case inside Datadog Case Management -- a trackable unit of work (a security incident, an operational issue) with real status, assignee, priority, and linked evidence, the same real primitive that powers Datadog's own Cloud SIEM and App Security investigation workflows.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	case_ "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/case_"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own response"})

    		ubx.Resource(case_.Response, "example", case_.ResponseConfig{
    			CaseId: "example",
    			Data:   map[string]any{"attributes": map[string]any{"title": "example"}},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Response } from "./local-sdk/datadog/sdk/typescript/datadog/case/response.ts";

    export default stack("example", () => {
      intent({ summary: "example own response" });

      resource(Response, "example", {
        caseId: "example",
        data: { attributes: { title: "example" } },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.case import Response, ResponseConfig

    def describe():
        ubx.intent("example own response")

        ubx.resource(Response, "example", ResponseConfig(
            case_id="example",
            data={"attributes": {"title": "example"}},
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_case_response, with case id example, data a set
    value.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="case_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="data" type="object" required>
  Case creation data Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Case creation attributes Required.
      <Expandable title="properties">
        <ResponseField name="custom_attributes" type="map(object)">
          Case custom attributes Optional.
          <Expandable title="properties">
            <ResponseField name="is_multi" type="bool">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="type" type="string">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
            <ResponseField name="value" type="any">
              Optional; if omitted, computed by Datadog.
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="description" type="string">
          A description of this case response. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="priority" type="string">
          The priority for this case response. Optional. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="status_name" type="string">
          Status of the case. Must be one of the existing statuses for the case's type. Optional.
        </ResponseField>
        <ResponseField name="title" type="string" required>
          The title for this case response. Required. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="type_id" type="string" required>
          The type ID for this case response. Required. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="relationships" type="object">
      Relationships formed with the case on creation Optional.
      <Expandable title="properties">
        <ResponseField name="assignee" type="object">
          Relationship to user. Optional.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Relationship to user object. Required.
              <Expandable title="properties">
                <Note>`data` recurs here (its own type contains itself); see the `data` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="project" type="object" required>
          Relationship to project. Required.
          <Expandable title="properties">
            <ResponseField name="data" type="object" required>
              Relationship to project object. Required.
              <Expandable title="properties">
                <Note>`data` recurs here (its own type contains itself); see the `data` field's own properties above for the repeating pattern.</Note>
              </Expandable>
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      JSON:API resource type for cases. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
