---
title: "datadog_slocorrection_response"
description: "A complete, runnable datadog_slocorrection_response program, in every SDK language."
---

`datadog_slocorrection_response` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.
Every tab below is a complete, runnable program, not a fragment, real
enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    package main

    import (
    	slocorrection "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/slocorrection"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own response"})

    		ubx.Resource(slocorrection.Response, "ci-runner", slocorrection.ResponseConfig{
    			SloCorrectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { intent, resource, stack } from "@ubx/sdk";
    import { Response } from "./local-sdk/datadog/sdk/typescript/datadog/slocorrection/response.ts";

    export default stack("payments", () => {
      intent({ summary: "payments own response" });

      resource(Response, "ci-runner", {
        sloCorrectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    import ubx_sdk as ubx
    from ubx.datadog.slocorrection import Response, ResponseConfig

    def describe():
        ubx.intent("payments own response")

        ubx.resource(Response, "ci-runner", ResponseConfig(
            slo_correction_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_slocorrection_response, with slo correction id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data" type="object">
  The data object associated with the SLO correction to be created. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      The attribute object associated with the SLO correction to be created. Exactly one of `slo_id` or `slo_query` must be provided. Optional.
      <Expandable title="properties">
        <ResponseField name="category" type="string" required>
          Category the SLO correction belongs to. Required.
        </ResponseField>
        <ResponseField name="description" type="string">
          Description of the correction being made. Optional.
        </ResponseField>
        <ResponseField name="duration" type="number">
          Length of time (in seconds) for a specified `rrule` recurring SLO correction. Optional.
        </ResponseField>
        <ResponseField name="end" type="number">
          Ending time of the correction in epoch seconds. Optional.
        </ResponseField>
        <ResponseField name="rrule" type="string">
          The recurrence rules as defined in the iCalendar RFC 5545. The supported rules for SLO corrections are `FREQ`, `INTERVAL`, `COUNT`, `UNTIL` and `BYDAY`. Optional.
        </ResponseField>
        <ResponseField name="slo_id" type="string">
          ID of the single SLO that this correction applies to. Optional.
        </ResponseField>
        <ResponseField name="slo_query" type="string">
          Query that matches the SLOs this correction applies to. The query uses the [Events search syntax](https://docs.datadoghq.com/events/explorer/searching/) and can filter SLOs by SLO tags. Optional.
        </ResponseField>
        <ResponseField name="start" type="number" required>
          Starting time of the correction in epoch seconds. Required.
        </ResponseField>
        <ResponseField name="timezone" type="string">
          The timezone to display in the UI for the correction times (defaults to "UTC"). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      SLO correction resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="slo_correction_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

## Output properties

<ResponseField name="data" type="object">
  The data object associated with the SLO correction to be created. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object">
      The attribute object associated with the SLO correction to be created. Exactly one of `slo_id` or `slo_query` must be provided. Optional.
      <Expandable title="properties">
        <ResponseField name="category" type="string" required>
          Category the SLO correction belongs to. Required.
        </ResponseField>
        <ResponseField name="description" type="string">
          Description of the correction being made. Optional.
        </ResponseField>
        <ResponseField name="duration" type="number">
          Length of time (in seconds) for a specified `rrule` recurring SLO correction. Optional.
        </ResponseField>
        <ResponseField name="end" type="number">
          Ending time of the correction in epoch seconds. Optional.
        </ResponseField>
        <ResponseField name="rrule" type="string">
          The recurrence rules as defined in the iCalendar RFC 5545. The supported rules for SLO corrections are `FREQ`, `INTERVAL`, `COUNT`, `UNTIL` and `BYDAY`. Optional.
        </ResponseField>
        <ResponseField name="slo_id" type="string">
          ID of the single SLO that this correction applies to. Optional.
        </ResponseField>
        <ResponseField name="slo_query" type="string">
          Query that matches the SLOs this correction applies to. The query uses the [Events search syntax](https://docs.datadoghq.com/events/explorer/searching/) and can filter SLOs by SLO tags. Optional.
        </ResponseField>
        <ResponseField name="start" type="number" required>
          Starting time of the correction in epoch seconds. Required.
        </ResponseField>
        <ResponseField name="timezone" type="string">
          The timezone to display in the UI for the correction times (defaults to "UTC"). Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      SLO correction resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>
