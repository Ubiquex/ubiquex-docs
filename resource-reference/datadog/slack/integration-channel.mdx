---
title: "datadog_slack_integration_channel"
description: "Real, generated bindings for `datadog_slack_integration_channel`."
---

`datadog_slack_integration_channel` -- real, typed bindings generated directly from
the real Datadog OpenAPI specification, in every SDK language.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    // ubx sdk gen --only datadog --lang go --out ./local-sdk
    // go.mod: replace github.com/ubiquex/ubx-sdk-datadog/sdk/go => ./local-sdk/datadog/sdk/go
    import (
        ubx "github.com/ubiquex/ubx-sdk-go/runtime"
        "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/slack"
    )

    cfg := slack.IntegrationChannelConfig{
        AccountName: "example-account-name",
        ChannelName: "example-channel-name",
    }
    ubx.Resource(slack.IntegrationChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    // ubx sdk gen --only datadog --lang ts --out ./local-sdk
    import { IntegrationChannel } from "./local-sdk/datadog/sdk/typescript/datadog/slack/integration_channel.ts";

    resource(IntegrationChannel, "example", {
      accountName: "example-account-name",
      channelName: "example-channel-name",
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # ubx sdk gen --only datadog --lang py --out ./local-sdk
    # export PYTHONPATH=./local-sdk/datadog/sdk/python:$PYTHONPATH
    from ubx.datadog.slack import (
        IntegrationChannel,
        IntegrationChannelConfig,
    )

    cfg = IntegrationChannelConfig(
        account_name="example-account-name",
        channel_name="example-channel-name",
    )
    ubx.resource(IntegrationChannel, "example", cfg)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Provision datadog_slack_integration_channel with account_name,
channel_name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="channel_name" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>

<ResponseField name="display" type="object">
  Configuration options for what is shown in an alert event message. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="message" type="bool">
      Show the main body of the alert event. Optional.
    </ResponseField>
    <ResponseField name="mute_buttons" type="bool">
      Show interactive buttons to mute the alerting monitor. Optional.
    </ResponseField>
    <ResponseField name="notified" type="bool">
      Show the list of @-handles in the alert event. Optional.
    </ResponseField>
    <ResponseField name="snapshot" type="bool">
      Show the alert event's snapshot image. Optional.
    </ResponseField>
    <ResponseField name="tags" type="bool">
      Show the scopes on which the monitor alerted. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Your channel name. Optional; if omitted, computed by Datadog.
</ResponseField>

## Output properties

<ResponseField name="display" type="object">
  Configuration options for what is shown in an alert event message. Optional; if omitted, computed by Datadog.
  <Expandable title="properties">
    <ResponseField name="message" type="bool">
      Show the main body of the alert event. Optional.
    </ResponseField>
    <ResponseField name="mute_buttons" type="bool">
      Show interactive buttons to mute the alerting monitor. Optional.
    </ResponseField>
    <ResponseField name="notified" type="bool">
      Show the list of @-handles in the alert event. Optional.
    </ResponseField>
    <ResponseField name="snapshot" type="bool">
      Show the alert event's snapshot image. Optional.
    </ResponseField>
    <ResponseField name="tags" type="bool">
      Show the scopes on which the monitor alerted. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="name" type="string">
  Your channel name. Optional; if omitted, computed by Datadog.
</ResponseField>
