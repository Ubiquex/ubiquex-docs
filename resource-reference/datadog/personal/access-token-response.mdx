---
title: "datadog_personal_access_token_response"
description: "`datadog_personal_access_token_response` is a real, user-scoped Datadog API credential -- unlike an application key (tied to a user but usable org-wide..."
---

`datadog_personal_access_token_response` is a real, user-scoped Datadog API credential -- unlike an application key (tied to a user but usable org-wide within that user's permissions), a personal access token is explicitly scoped to specific real permissions the user grants it at creation.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	personal "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/personal"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own access token response"})

    		ubx.Resource(personal.AccessTokenResponse, "example", personal.AccessTokenResponseConfig{
    			Data:    map[string]any{"attributes": map[string]any{"expires_at": "example", "name": "example-name", "scopes": []string{"example"}}, "type": "example"},
    			TokenId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessTokenResponse } from "@ubx/sdk-datadog/datadog/personal/access_token_response";

    export default stack("example", () => {
      intent({ summary: "example own access token response" });

      resource(AccessTokenResponse, "example", {
        data: {
          attributes: {
            expiresAt: "example",
            name: "example-name",
            scopes: ["example"],
          },
          type: "example",
        },
        tokenId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.personal import AccessTokenResponse, AccessTokenResponseConfig
    from ubx.datadog.personal.access_token_response import AccessTokenResponse_Data, AccessTokenResponse_Data_Attributes

    def describe():
        ubx.intent("example own access token response")

        ubx.resource(AccessTokenResponse, "example", AccessTokenResponseConfig(
            data=AccessTokenResponse_Data(attributes=AccessTokenResponse_Data_Attributes(expires_at="example", name="example-name", scopes=["example"]), type="example"),
            token_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_personal_access_token_response, with data a set
    value, token id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  Object used to create an access token. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes used to create an access token. Required.
      <Expandable title="properties">
        <ResponseField name="expires_at" type="string" required>
          Expiration date of the access token. Must be at least 24 hours in the future. Required.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Name of the access token. Required.
        </ResponseField>
        <ResponseField name="scopes" type="list(string)" required>
          Array of scopes to grant the access token. Required.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string" required>
      Personal access tokens resource type. Required.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="token_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
