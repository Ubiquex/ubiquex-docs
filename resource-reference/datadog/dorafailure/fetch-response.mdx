---
title: "datadog_dorafailure_fetch_response"
description: "`datadog_dorafailure_fetch_response` is the real, queried change-failure-rate half of Datadog's DORA metrics -- how often a deployment led to..."
---

`datadog_dorafailure_fetch_response` is the real, queried change-failure-rate half of Datadog's DORA metrics -- how often a deployment led to a real incident or rollback, computed alongside `datadog_doradeployment_fetch_response`'s own deployment-frequency data.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dorafailure "github.com/ubiquex/ubx-sdk-datadog/sdk/go/datadog/dorafailure"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("example", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "example own fetch response"})

    		ubx.Resource(dorafailure.FetchResponse, "example", dorafailure.FetchResponseConfig{
    			Data:      map[string]any{"attributes": map[string]any{"from": "example"}},
    			FailureId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FetchResponse } from "@ubx/sdk-datadog/datadog/dorafailure/fetch_response";

    export default stack("example", () => {
      intent({ summary: "example own fetch response" });

      resource(FetchResponse, "example", {
        data: { attributes: { from: "example" } },
        failureId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.datadog.dorafailure.fetch_response import FetchResponse, FetchResponseConfig

    def describe():
        ubx.intent("example own fetch response")

        ubx.resource(FetchResponse, "example", FetchResponseConfig(
            data={"attributes": {"from": "example"}},
            failure_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("example", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create datadog_dorafailure_fetch_response, with data a set value,
    failure id example.
    ```
  </Tab>
</Tabs>

## Properties

<ResponseField name="data" type="object" required>
  The JSON:API data. Required.
  <Expandable title="properties">
    <ResponseField name="attributes" type="object" required>
      Attributes to get a list of incidents. Required.
      <Expandable title="properties">
        <ResponseField name="from" type="string">
          Minimum timestamp for requested events. Optional.
        </ResponseField>
        <ResponseField name="limit" type="number">
          Maximum number of events in the response. Optional.
        </ResponseField>
        <ResponseField name="query" type="string">
          Search query with event platform syntax. Optional.
        </ResponseField>
        <ResponseField name="sort" type="string">
          Sort order (prefixed with `-` for descending). Optional.
        </ResponseField>
        <ResponseField name="to" type="string">
          Maximum timestamp for requested events. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="type" type="string">
      The definition of `DORAListFailuresRequestDataType` object. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="failure_id" type="string" required>
  path parameter, not part of the API's own resource representation Required.
</ResponseField>
