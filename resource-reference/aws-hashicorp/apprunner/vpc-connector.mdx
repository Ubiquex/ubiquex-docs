---
title: "aws_apprunner_vpc_connector"
description: "Real, generated bindings for `aws_apprunner_vpc_connector`."
---

`aws_apprunner_vpc_connector` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	apprunner "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/apprunner"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vpc connector"})

    		ubx.Resource(apprunner.VpcConnector, "ci-runner", apprunner.VpcConnectorConfig{
    			SecurityGroups:   []string{"example"},
    			Subnets:          []string{"example"},
    			VpcConnectorName: "example-vpc-connector-name",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VpcConnector } from "jsr:@ubx/sdk-aws/aws/apprunner/vpc_connector";

    export default stack("payments", () => {
      intent({ summary: "payments own vpc connector" });

      resource(VpcConnector, "ci-runner", {
        securityGroups: ["example"],
        subnets: ["example"],
        vpcConnectorName: "example-vpc-connector-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.apprunner import VpcConnector, VpcConnectorConfig

    def describe():
        ubx.intent("payments own vpc connector")

        ubx.resource(VpcConnector, "ci-runner", VpcConnectorConfig(
            security_groups=["example"],
            subnets=["example"],
            vpc_connector_name="example-vpc-connector-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_apprunner_vpc_connector, with security groups a set
    value, subnets a set value, vpc connector name
    example-vpc-connector-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_groups" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnets" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_connector_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpc_connector_revision" type="number">
  Computed by AWS after `ship`.
</ResponseField>
