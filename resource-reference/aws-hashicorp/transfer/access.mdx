---
title: "aws_transfer_access"
description: "Real, generated bindings for `aws_transfer_access`."
---

`aws_transfer_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	"encoding/json"

    	transfer "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/transfer"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access"})

    		accessPolicy, _ := json.Marshal(map[string]any{
    			"Version": "2012-10-17",
    			"Statement": []map[string]any{{
    				"Effect":   "Allow",
    				"Action":   "*",
    				"Resource": "*",
    			}},
    		})

    		ubx.Resource(transfer.Access, "ci-runner", transfer.AccessConfig{
    			ExternalId:        "example",
    			ServerId:          "example",
    			HomeDirectory:     "example",
    			HomeDirectoryType: "example",
    			Policy:            string(accessPolicy),
    			Role:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Access } from "jsr:@ubx/sdk-aws/aws/transfer/access";

    export default stack("payments", () => {
      intent({ summary: "payments own access" });

      const accessPolicy = JSON.stringify({
        Version: "2012-10-17",
        Statement: [{
          Effect: "Allow",
          Action: "*",
          Resource: "*",
        }],
      });

      resource(Access, "ci-runner", {
        externalId: "example",
        serverId: "example",
        homeDirectory: "example",
        homeDirectoryType: "example",
        policy: accessPolicy,
        role: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import json
    import ubx_sdk as ubx
    from ubx.aws.transfer import Access, AccessConfig

    def describe():
        ubx.intent("payments own access")

        access_policy = json.dumps({
            "Version": "2012-10-17",
            "Statement": [{
                "Effect": "Allow",
                "Action": "*",
                "Resource": "*",
            }],
        })

        ubx.resource(Access, "ci-runner", AccessConfig(
            external_id="example",
            server_id="example",
            home_directory="example",
            home_directory_type="example",
            policy=access_policy,
            role="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_transfer_access, with external id example, server id
    example, home directory example, home directory type example,
    policy a real, valid value, role example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="external_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="home_directory" type="string">
  Optional.
</ResponseField>

<ResponseField name="home_directory_mappings" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="entry" type="string" required>
    </ResponseField>
    <ResponseField name="target" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="home_directory_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string">
  Optional.
</ResponseField>

<ResponseField name="posix_profile" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="gid" type="number" required>
    </ResponseField>
    <ResponseField name="secondary_gids" type="set(number)">
    </ResponseField>
    <ResponseField name="uid" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role" type="string">
  Optional.
</ResponseField>

<ResponseField name="server_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="home_directory_mappings" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="entry" type="string" required>
    </ResponseField>
    <ResponseField name="target" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="posix_profile" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="gid" type="number" required>
    </ResponseField>
    <ResponseField name="secondary_gids" type="set(number)">
    </ResponseField>
    <ResponseField name="uid" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
