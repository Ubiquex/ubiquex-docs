---
title: "aws_directory_service_log_subscription"
description: "Real, generated bindings for `aws_directory_service_log_subscription`."
---

`aws_directory_service_log_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	directory "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/directory"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service log subscription"})

    		ubx.Resource(directory.ServiceLogSubscription, "ci-runner", directory.ServiceLogSubscriptionConfig{
    			DirectoryId:  "example",
    			LogGroupName: "example-log-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceLogSubscription } from "jsr:@ubx/sdk-aws/aws/directory/service_log_subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own service log subscription" });

      resource(ServiceLogSubscription, "ci-runner", {
        directoryId: "example",
        logGroupName: "example-log-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.directory import ServiceLogSubscription, ServiceLogSubscriptionConfig

    def describe():
        ubx.intent("payments own service log subscription")

        ubx.resource(ServiceLogSubscription, "ci-runner", ServiceLogSubscriptionConfig(
            directory_id="example",
            log_group_name="example-log-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_directory_service_log_subscription, with directory id
    example, log group name example-log-group-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="directory_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="log_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
