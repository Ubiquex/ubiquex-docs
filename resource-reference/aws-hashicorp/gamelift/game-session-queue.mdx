---
title: "aws_gamelift_game_session_queue"
description: "Real, generated bindings for `aws_gamelift_game_session_queue`."
---

`aws_gamelift_game_session_queue` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	gamelift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/gamelift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own game session queue"})

    		ubx.Resource(gamelift.GameSessionQueue, "ci-runner", gamelift.GameSessionQueueConfig{
    			Name:               "ci-runner",
    			CustomEventData:    "example",
    			Destinations:       []string{"example"},
    			NotificationTarget: "example",
    			Tags:               map[string]string{"managed-by": "ubx"},
    			TimeoutInSeconds:   7200,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GameSessionQueue } from "jsr:@ubx/sdk-aws/aws/gamelift/game_session_queue";

    export default stack("payments", () => {
      intent({ summary: "payments own game session queue" });

      resource(GameSessionQueue, "ci-runner", {
        name: "ci-runner",
        customEventData: "example",
        destinations: ["example"],
        notificationTarget: "example",
        tags: { "managed-by": "ubx" },
        timeoutInSeconds: 7200,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.gamelift import GameSessionQueue, GameSessionQueueConfig

    def describe():
        ubx.intent("payments own game session queue")

        ubx.resource(GameSessionQueue, "ci-runner", GameSessionQueueConfig(
            name="ci-runner",
            custom_event_data="example",
            destinations=["example"],
            notification_target="example",
            tags={"managed-by": "ubx"},
            timeout_in_seconds=7200,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_gamelift_game_session_queue called "ci-runner", with
    custom event data example, destinations a set value, notification
    target example, tags a set value, timeout in seconds 7200.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="custom_event_data" type="string">
  Optional.
</ResponseField>

<ResponseField name="destinations" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="notification_target" type="string">
  Optional.
</ResponseField>

<ResponseField name="player_latency_policy" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="maximum_individual_player_latency_milliseconds" type="number" required>
    </ResponseField>
    <ResponseField name="policy_duration_seconds" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeout_in_seconds" type="number">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="player_latency_policy" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="maximum_individual_player_latency_milliseconds" type="number" required>
    </ResponseField>
    <ResponseField name="policy_duration_seconds" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
