---
title: "aws_load_balancer_backend_server_policy"
description: "Real, generated bindings for `aws_load_balancer_backend_server_policy`."
---

`aws_load_balancer_backend_server_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	load "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/load"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own balancer backend server policy"})

    		ubx.Resource(load.BalancerBackendServerPolicy, "ci-runner", load.BalancerBackendServerPolicyConfig{
    			InstancePort:     1,
    			LoadBalancerName: "example-load-balancer-name",
    			PolicyNames:      []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BalancerBackendServerPolicy } from "jsr:@ubx/sdk-aws/aws/load/balancer_backend_server_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own balancer backend server policy" });

      resource(BalancerBackendServerPolicy, "ci-runner", {
        instancePort: 1,
        loadBalancerName: "example-load-balancer-name",
        policyNames: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.load import BalancerBackendServerPolicy, BalancerBackendServerPolicyConfig

    def describe():
        ubx.intent("payments own balancer backend server policy")

        ubx.resource(BalancerBackendServerPolicy, "ci-runner", BalancerBackendServerPolicyConfig(
            instance_port=1,
            load_balancer_name="example-load-balancer-name",
            policy_names=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_load_balancer_backend_server_policy, with instance
    port 1, load balancer name example-load-balancer-name, policy
    names a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="load_balancer_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="policy_names" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
