---
title: "aws_iam_organizations_features"
description: "Real, generated bindings for `aws_iam_organizations_features`."
---

`aws_iam_organizations_features` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organizations features"})

    		ubx.Resource(iam.OrganizationsFeatures, "ci-runner", iam.OrganizationsFeaturesConfig{
    			EnabledFeatures: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationsFeatures } from "jsr:@ubx/sdk-aws/aws/iam/organizations_features";

    export default stack("payments", () => {
      intent({ summary: "payments own organizations features" });

      resource(OrganizationsFeatures, "ci-runner", {
        enabledFeatures: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import OrganizationsFeatures, OrganizationsFeaturesConfig

    def describe():
        ubx.intent("payments own organizations features")

        ubx.resource(OrganizationsFeatures, "ci-runner", OrganizationsFeaturesConfig(
            enabled_features=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_organizations_features, with enabled features a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="enabled_features" type="set(string)" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
