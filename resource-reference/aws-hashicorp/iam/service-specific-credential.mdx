---
title: "aws_iam_service_specific_credential"
description: "Real, generated bindings for `aws_iam_service_specific_credential`."
---

`aws_iam_service_specific_credential` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own service specific credential"})

    		ubx.Resource(iam.ServiceSpecificCredential, "ci-runner", iam.ServiceSpecificCredentialConfig{
    			ServiceName:       "example-service-name",
    			UserName:          "example-user-name",
    			CredentialAgeDays: 1,
    			Status:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ServiceSpecificCredential } from "jsr:@ubx/sdk-aws/aws/iam/service_specific_credential";

    export default stack("payments", () => {
      intent({ summary: "payments own service specific credential" });

      resource(ServiceSpecificCredential, "ci-runner", {
        serviceName: "example-service-name",
        userName: "example-user-name",
        credentialAgeDays: 1,
        status: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import ServiceSpecificCredential, ServiceSpecificCredentialConfig

    def describe():
        ubx.intent("payments own service specific credential")

        ubx.resource(ServiceSpecificCredential, "ci-runner", ServiceSpecificCredentialConfig(
            service_name="example-service-name",
            user_name="example-user-name",
            credential_age_days=1,
            status="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_service_specific_credential, with service name
    example-service-name, user name example-user-name, credential age
    days 1, status example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="credential_age_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="status" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="create_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="expiration_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_credential_alias" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_credential_secret" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_password" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_specific_credential_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="service_user_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>
