---
title: "aws_iam_openid_connect_provider"
description: "Real, generated bindings for `aws_iam_openid_connect_provider`."
---

`aws_iam_openid_connect_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own openid connect provider"})

    		ubx.Resource(iam.OpenidConnectProvider, "ci-runner", iam.OpenidConnectProviderConfig{
    			ClientIdList: []string{"example"},
    			Url:          "example",
    			Tags:         map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OpenidConnectProvider } from "jsr:@ubx/sdk-aws/aws/iam/openid_connect_provider";

    export default stack("payments", () => {
      intent({ summary: "payments own openid connect provider" });

      resource(OpenidConnectProvider, "ci-runner", {
        clientIdList: ["example"],
        url: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import OpenidConnectProvider, OpenidConnectProviderConfig

    def describe():
        ubx.intent("payments own openid connect provider")

        ubx.resource(OpenidConnectProvider, "ci-runner", OpenidConnectProviderConfig(
            client_id_list=["example"],
            url="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_openid_connect_provider, with client id list a set
    value, url example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="client_id_list" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thumbprint_list" type="list(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thumbprint_list" type="list(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
