---
title: "aws_iam_user_ssh_key"
description: "Real, generated bindings for `aws_iam_user_ssh_key`."
---

`aws_iam_user_ssh_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user ssh key"})

    		ubx.Resource(iam.UserSshKey, "ci-runner", iam.UserSshKeyConfig{
    			Encoding:  "example",
    			PublicKey: "example",
    			Username:  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserSshKey } from "jsr:@ubx/sdk-aws/aws/iam/user_ssh_key";

    export default stack("payments", () => {
      intent({ summary: "payments own user ssh key" });

      resource(UserSshKey, "ci-runner", {
        encoding: "example",
        publicKey: "example",
        username: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import UserSshKey, UserSshKeyConfig

    def describe():
        ubx.intent("payments own user ssh key")

        ubx.resource(UserSshKey, "ci-runner", UserSshKeyConfig(
            encoding="example",
            public_key="example",
            username="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_user_ssh_key, with encoding example, public key
    example, username example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="encoding" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="public_key" type="string" required>
  Required.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="username" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="fingerprint" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ssh_public_key_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
