---
title: "aws_iam_account_alias"
description: "Real, generated bindings for `aws_iam_account_alias`."
---

`aws_iam_account_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account alias"})

    		ubx.Resource(iam.AccountAlias, "ci-runner", iam.AccountAliasConfig{
    			AccountAlias: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountAlias } from "jsr:@ubx/sdk-aws/aws/iam/account_alias";

    export default stack("payments", () => {
      intent({ summary: "payments own account alias" });

      resource(AccountAlias, "ci-runner", {
        accountAlias: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import AccountAlias, AccountAliasConfig

    def describe():
        ubx.intent("payments own account alias")

        ubx.resource(AccountAlias, "ci-runner", AccountAliasConfig(
            account_alias="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_account_alias, with account alias example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_alias" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
