---
title: "aws_iam_saml_provider"
description: "Real, generated bindings for `aws_iam_saml_provider`."
---

`aws_iam_saml_provider` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own saml provider"})

    		ubx.Resource(iam.SamlProvider, "ci-runner", iam.SamlProviderConfig{
    			Name:                 "ci-runner",
    			SamlMetadataDocument: "example",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SamlProvider } from "jsr:@ubx/sdk-aws/aws/iam/saml_provider";

    export default stack("payments", () => {
      intent({ summary: "payments own saml provider" });

      resource(SamlProvider, "ci-runner", {
        name: "ci-runner",
        samlMetadataDocument: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import SamlProvider, SamlProviderConfig

    def describe():
        ubx.intent("payments own saml provider")

        ubx.resource(SamlProvider, "ci-runner", SamlProviderConfig(
            name="ci-runner",
            saml_metadata_document="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_saml_provider called "ci-runner", with saml
    metadata document example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="saml_metadata_document" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="saml_provider_uuid" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="valid_until" type="string">
  Computed by AWS after `ship`.
</ResponseField>
