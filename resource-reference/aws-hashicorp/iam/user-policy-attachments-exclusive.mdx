---
title: "aws_iam_user_policy_attachments_exclusive"
description: "Real, generated bindings for `aws_iam_user_policy_attachments_exclusive`."
---

`aws_iam_user_policy_attachments_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iam "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iam"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user policy attachments exclusive"})

    		ubx.Resource(iam.UserPolicyAttachmentsExclusive, "ci-runner", iam.UserPolicyAttachmentsExclusiveConfig{
    			PolicyArns: []string{"example"},
    			UserName:   "example-user-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UserPolicyAttachmentsExclusive } from "jsr:@ubx/sdk-aws/aws/iam/user_policy_attachments_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own user policy attachments exclusive" });

      resource(UserPolicyAttachmentsExclusive, "ci-runner", {
        policyArns: ["example"],
        userName: "example-user-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iam import UserPolicyAttachmentsExclusive, UserPolicyAttachmentsExclusiveConfig

    def describe():
        ubx.intent("payments own user policy attachments exclusive")

        ubx.resource(UserPolicyAttachmentsExclusive, "ci-runner", UserPolicyAttachmentsExclusiveConfig(
            policy_arns=["example"],
            user_name="example-user-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iam_user_policy_attachments_exclusive, with policy
    arns a set value, user name example-user-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="policy_arns" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="user_name" type="string" required>
  Required.
</ResponseField>

## Output properties


