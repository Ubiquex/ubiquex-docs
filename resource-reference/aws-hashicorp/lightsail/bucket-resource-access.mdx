---
title: "aws_lightsail_bucket_resource_access"
description: "Real, generated bindings for `aws_lightsail_bucket_resource_access`."
---

`aws_lightsail_bucket_resource_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own bucket resource access"})

    		ubx.Resource(lightsail.BucketResourceAccess, "ci-runner", lightsail.BucketResourceAccessConfig{
    			BucketName:   "example-bucket-name",
    			ResourceName: "example-resource-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BucketResourceAccess } from "jsr:@ubx/sdk-aws/aws/lightsail/bucket_resource_access";

    export default stack("payments", () => {
      intent({ summary: "payments own bucket resource access" });

      resource(BucketResourceAccess, "ci-runner", {
        bucketName: "example-bucket-name",
        resourceName: "example-resource-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import BucketResourceAccess, BucketResourceAccessConfig

    def describe():
        ubx.intent("payments own bucket resource access")

        ubx.resource(BucketResourceAccess, "ci-runner", BucketResourceAccessConfig(
            bucket_name="example-bucket-name",
            resource_name="example-resource-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_bucket_resource_access, with bucket name
    example-bucket-name, resource name example-resource-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
