---
title: "aws_lightsail_lb_certificate"
description: "Real, generated bindings for `aws_lightsail_lb_certificate`."
---

`aws_lightsail_lb_certificate` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lightsail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lightsail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own lb certificate"})

    		ubx.Resource(lightsail.LbCertificate, "ci-runner", lightsail.LbCertificateConfig{
    			LbName: "example-lb-name",
    			Name:   "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LbCertificate } from "jsr:@ubx/sdk-aws/aws/lightsail/lb_certificate";

    export default stack("payments", () => {
      intent({ summary: "payments own lb certificate" });

      resource(LbCertificate, "ci-runner", {
        lbName: "example-lb-name",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lightsail import LbCertificate, LbCertificateConfig

    def describe():
        ubx.intent("payments own lb certificate")

        ubx.resource(LbCertificate, "ci-runner", LbCertificateConfig(
            lb_name="example-lb-name",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lightsail_lb_certificate called "ci-runner", with lb
    name example-lb-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="domain_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="lb_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subject_alternative_names" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="domain_validation_records" type="set(object)">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="domain_name" type="string">
    </ResponseField>
    <ResponseField name="resource_record_name" type="string">
    </ResponseField>
    <ResponseField name="resource_record_type" type="string">
    </ResponseField>
    <ResponseField name="resource_record_value" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="subject_alternative_names" type="set(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="support_code" type="string">
  Computed by AWS after `ship`.
</ResponseField>
