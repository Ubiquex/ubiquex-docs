---
title: "aws_lambda_invocation"
description: "Real, generated bindings for `aws_lambda_invocation`."
---

`aws_lambda_invocation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own invocation"})

    		ubx.Resource(lambda.Invocation, "ci-runner", lambda.InvocationConfig{
    			FunctionName:   "example-function-name",
    			Input:          "example",
    			LifecycleScope: "example",
    			Qualifier:      "example",
    			TenantId:       "example",
    			TerraformKey:   "example",
    			Triggers:       map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Invocation } from "jsr:@ubx/sdk-aws/aws/lambda/invocation";

    export default stack("payments", () => {
      intent({ summary: "payments own invocation" });

      resource(Invocation, "ci-runner", {
        functionName: "example-function-name",
        input: "example",
        lifecycleScope: "example",
        qualifier: "example",
        tenantId: "example",
        terraformKey: "example",
        triggers: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import Invocation, InvocationConfig

    def describe():
        ubx.intent("payments own invocation")

        ubx.resource(Invocation, "ci-runner", InvocationConfig(
            function_name="example-function-name",
            input="example",
            lifecycle_scope="example",
            qualifier="example",
            tenant_id="example",
            terraform_key="example",
            triggers={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_invocation, with function name
    example-function-name, input example, lifecycle scope example,
    qualifier example, tenant id example, terraform key example,
    triggers a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="input" type="string" required>
  Required.
</ResponseField>

<ResponseField name="lifecycle_scope" type="string">
  Optional.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tenant_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="terraform_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="triggers" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="result" type="string">
  Computed by AWS after `ship`.
</ResponseField>
