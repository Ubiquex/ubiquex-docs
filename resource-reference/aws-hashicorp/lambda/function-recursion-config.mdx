---
title: "aws_lambda_function_recursion_config"
description: "Real, generated bindings for `aws_lambda_function_recursion_config`."
---

`aws_lambda_function_recursion_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own function recursion config"})

    		ubx.Resource(lambda.FunctionRecursionConfig, "ci-runner", lambda.FunctionRecursionConfigConfig{
    			FunctionName:  "example-function-name",
    			RecursiveLoop: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FunctionRecursionConfig } from "jsr:@ubx/sdk-aws/aws/lambda/function_recursion_config";

    export default stack("payments", () => {
      intent({ summary: "payments own function recursion config" });

      resource(FunctionRecursionConfig, "ci-runner", {
        functionName: "example-function-name",
        recursiveLoop: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import FunctionRecursionConfig, FunctionRecursionConfigConfig

    def describe():
        ubx.intent("payments own function recursion config")

        ubx.resource(FunctionRecursionConfig, "ci-runner", FunctionRecursionConfigConfig(
            function_name="example-function-name",
            recursive_loop="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_function_recursion_config, with function name
    example-function-name, recursive loop example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="recursive_loop" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
