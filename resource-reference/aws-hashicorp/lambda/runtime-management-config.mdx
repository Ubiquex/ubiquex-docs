---
title: "aws_lambda_runtime_management_config"
description: "Real, generated bindings for `aws_lambda_runtime_management_config`."
---

`aws_lambda_runtime_management_config` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lambda "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/lambda"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own runtime management config"})

    		ubx.Resource(lambda.RuntimeManagementConfig, "ci-runner", lambda.RuntimeManagementConfigConfig{
    			FunctionName:      "example-function-name",
    			Qualifier:         "example",
    			RuntimeVersionArn: "arn:aws:iam::123456789012:policy/ci",
    			UpdateRuntimeOn:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RuntimeManagementConfig } from "jsr:@ubx/sdk-aws/aws/lambda/runtime_management_config";

    export default stack("payments", () => {
      intent({ summary: "payments own runtime management config" });

      resource(RuntimeManagementConfig, "ci-runner", {
        functionName: "example-function-name",
        qualifier: "example",
        runtimeVersionArn: "arn:aws:iam::123456789012:policy/ci",
        updateRuntimeOn: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.lambda_ import RuntimeManagementConfig, RuntimeManagementConfigConfig

    def describe():
        ubx.intent("payments own runtime management config")

        ubx.resource(RuntimeManagementConfig, "ci-runner", RuntimeManagementConfigConfig(
            function_name="example-function-name",
            qualifier="example",
            runtime_version_arn="arn:aws:iam::123456789012:policy/ci",
            update_runtime_on="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_lambda_runtime_management_config, with function name
    example-function-name, qualifier example, runtime version arn
    arn:aws:iam::123456789012:policy/ci, update runtime on example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="function_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="qualifier" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="runtime_version_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="update_runtime_on" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="function_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
