---
title: "aws_rds_export_task"
description: "Real, generated bindings for `aws_rds_export_task`."
---

`aws_rds_export_task` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own export task"})

    		ubx.Resource(rds.ExportTask, "ci-runner", rds.ExportTaskConfig{
    			ExportTaskIdentifier: "example",
    			IamRoleArn:           "arn:aws:iam::123456789012:policy/ci",
    			KmsKeyId:             "example",
    			S3BucketName:         "example-s3-bucket-name",
    			SourceArn:            "arn:aws:iam::123456789012:policy/ci",
    			ExportOnly:           []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ExportTask } from "jsr:@ubx/sdk-aws/aws/rds/export_task";

    export default stack("payments", () => {
      intent({ summary: "payments own export task" });

      resource(ExportTask, "ci-runner", {
        exportTaskIdentifier: "example",
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        kmsKeyId: "example",
        s3BucketName: "example-s3-bucket-name",
        sourceArn: "arn:aws:iam::123456789012:policy/ci",
        exportOnly: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import ExportTask, ExportTaskConfig

    def describe():
        ubx.intent("payments own export task")

        ubx.resource(ExportTask, "ci-runner", ExportTaskConfig(
            export_task_identifier="example",
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            kms_key_id="example",
            s3_bucket_name="example-s3-bucket-name",
            source_arn="arn:aws:iam::123456789012:policy/ci",
            export_only=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_export_task, with export task identifier example,
    iam role arn arn:aws:iam::123456789012:policy/ci, kms key id
    example, s3 bucket name example-s3-bucket-name, source arn
    arn:aws:iam::123456789012:policy/ci, export only a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="export_only" type="list(string)">
  Optional.
</ResponseField>

<ResponseField name="export_task_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="kms_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_bucket_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="s3_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="failure_cause" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="percent_progress" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_prefix" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="source_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="task_end_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="task_start_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="warning_message" type="string">
  Computed by AWS after `ship`.
</ResponseField>
