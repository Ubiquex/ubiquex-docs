---
title: "aws_rds_cluster_snapshot_copy"
description: "Real, generated bindings for `aws_rds_cluster_snapshot_copy`."
---

`aws_rds_cluster_snapshot_copy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster snapshot copy"})

    		ubx.Resource(rds.ClusterSnapshotCopy, "ci-runner", rds.ClusterSnapshotCopyConfig{
    			SourceDbClusterSnapshotIdentifier: "example",
    			TargetDbClusterSnapshotIdentifier: "example",
    			CopyTags:                          true,
    			DestinationRegion:                 "example",
    			KmsKeyId:                          "example",
    			PresignedUrl:                      "example",
    			SharedAccounts:                    []string{"example"},
    			Tags:                              map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterSnapshotCopy } from "jsr:@ubx/sdk-aws/aws/rds/cluster_snapshot_copy";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster snapshot copy" });

      resource(ClusterSnapshotCopy, "ci-runner", {
        sourceDbClusterSnapshotIdentifier: "example",
        targetDbClusterSnapshotIdentifier: "example",
        copyTags: true,
        destinationRegion: "example",
        kmsKeyId: "example",
        presignedUrl: "example",
        sharedAccounts: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import ClusterSnapshotCopy, ClusterSnapshotCopyConfig

    def describe():
        ubx.intent("payments own cluster snapshot copy")

        ubx.resource(ClusterSnapshotCopy, "ci-runner", ClusterSnapshotCopyConfig(
            source_db_cluster_snapshot_identifier="example",
            target_db_cluster_snapshot_identifier="example",
            copy_tags=True,
            destination_region="example",
            kms_key_id="example",
            presigned_url="example",
            shared_accounts=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_cluster_snapshot_copy, with source db cluster
    snapshot identifier example, target db cluster snapshot
    identifier example, copy tags true, destination region example,
    kms key id example, presigned url example, shared accounts a set
    value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="copy_tags" type="bool">
  Optional.
</ResponseField>

<ResponseField name="destination_region" type="string">
  Optional.
</ResponseField>

<ResponseField name="kms_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="presigned_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="shared_accounts" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="source_db_cluster_snapshot_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="target_db_cluster_snapshot_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="allocated_storage" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="db_cluster_snapshot_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="engine_version" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="license_model" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_encrypted" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="storage_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
