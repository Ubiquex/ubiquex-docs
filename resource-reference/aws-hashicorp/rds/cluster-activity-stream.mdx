---
title: "aws_rds_cluster_activity_stream"
description: "Real, generated bindings for `aws_rds_cluster_activity_stream`."
---

`aws_rds_cluster_activity_stream` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	rds "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/rds"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster activity stream"})

    		ubx.Resource(rds.ClusterActivityStream, "ci-runner", rds.ClusterActivityStreamConfig{
    			KmsKeyId:                        "example",
    			Mode:                            "example",
    			ResourceArn:                     "arn:aws:iam::123456789012:policy/ci",
    			EngineNativeAuditFieldsIncluded: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterActivityStream } from "jsr:@ubx/sdk-aws/aws/rds/cluster_activity_stream";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster activity stream" });

      resource(ClusterActivityStream, "ci-runner", {
        kmsKeyId: "example",
        mode: "example",
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        engineNativeAuditFieldsIncluded: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.rds import ClusterActivityStream, ClusterActivityStreamConfig

    def describe():
        ubx.intent("payments own cluster activity stream")

        ubx.resource(ClusterActivityStream, "ci-runner", ClusterActivityStreamConfig(
            kms_key_id="example",
            mode="example",
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            engine_native_audit_fields_included=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_rds_cluster_activity_stream, with kms key id example,
    mode example, resource arn arn:aws:iam::123456789012:policy/ci,
    engine native audit fields included true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="engine_native_audit_fields_included" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_key_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="mode" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kinesis_stream_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
