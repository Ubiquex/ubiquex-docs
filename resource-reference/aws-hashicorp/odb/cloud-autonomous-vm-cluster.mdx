---
title: "aws_odb_cloud_autonomous_vm_cluster"
description: "Real, generated bindings for `aws_odb_cloud_autonomous_vm_cluster`."
---

`aws_odb_cloud_autonomous_vm_cluster` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cloud autonomous vm cluster"})

    		ubx.Resource(odb.CloudAutonomousVmCluster, "ci-runner", odb.CloudAutonomousVmClusterConfig{
    			AutonomousDataStorageSizeInTbs:  1,
    			CpuCoreCountPerNode:             1,
    			DbServers:                       []string{"example"},
    			DisplayName:                     "example-display-name",
    			MemoryPerOracleComputeUnitInGbs: 1,
    			ScanListenerPortNonTls:          1,
    			ScanListenerPortTls:             1,
    			TotalContainerDatabases:         1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { CloudAutonomousVmCluster } from "jsr:@ubx/sdk-aws/aws/odb/cloud_autonomous_vm_cluster";

    export default stack("payments", () => {
      intent({ summary: "payments own cloud autonomous vm cluster" });

      resource(CloudAutonomousVmCluster, "ci-runner", {
        autonomousDataStorageSizeInTbs: 1,
        cpuCoreCountPerNode: 1,
        dbServers: ["example"],
        displayName: "example-display-name",
        memoryPerOracleComputeUnitInGbs: 1,
        scanListenerPortNonTls: 1,
        scanListenerPortTls: 1,
        totalContainerDatabases: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.odb import CloudAutonomousVmCluster, CloudAutonomousVmClusterConfig

    def describe():
        ubx.intent("payments own cloud autonomous vm cluster")

        ubx.resource(CloudAutonomousVmCluster, "ci-runner", CloudAutonomousVmClusterConfig(
            autonomous_data_storage_size_in_tbs=1,
            cpu_core_count_per_node=1,
            db_servers=["example"],
            display_name="example-display-name",
            memory_per_oracle_compute_unit_in_gbs=1,
            scan_listener_port_non_tls=1,
            scan_listener_port_tls=1,
            total_container_databases=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_odb_cloud_autonomous_vm_cluster, with autonomous data
    storage size in tbs 1, cpu core count per node 1, db servers a
    set value, display name example-display-name, memory per oracle
    compute unit in gbs 1, scan listener port non tls 1, scan
    listener port tls 1, total container databases 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="autonomous_data_storage_size_in_tbs" type="number" required>
  The data storage size allocated for Autonomous Databases in the Autonomous VM cluster, in TB. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_arn" type="string">
  The unique identifier of the Exadata infrastructure for this VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_id" type="string">
  Exadata infrastructure id. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cpu_core_count_per_node" type="number" required>
  The number of CPU cores enabled per node in the Autonomous VM cluster. Required.
</ResponseField>

<ResponseField name="db_servers" type="set(string)" required>
  The database servers in the Autonomous VM cluster. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="description" type="string">
  The description of the Autonomous VM cluster. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The display name of the Autonomous VM cluster. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="is_mtls_enabled_vm_cluster" type="bool">
  Indicates whether mutual TLS (mTLS) authentication is enabled for the Autonomous VM cluster. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_model" type="string">
  The license model for the Autonomous VM cluster. Valid values are LICENSE_INCLUDED or BRING_YOUR_OWN_LICENSE . Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maintenance_window" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="days_of_week" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hours_of_day" type="set(number)">
    </ResponseField>
    <ResponseField name="lead_time_in_weeks" type="number">
    </ResponseField>
    <ResponseField name="months" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preference" type="string" required>
    </ResponseField>
    <ResponseField name="weeks_of_month" type="set(number)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="memory_per_oracle_compute_unit_in_gbs" type="number" required>
  The amount of memory allocated per Oracle Compute Unit, in GB. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="odb_network_arn" type="string">
  The unique identifier of the ODB network for the VM cluster. This member is required. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="odb_network_id" type="string">
  The unique identifier of the ODB network associated with this Autonomous VM Cluster. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scan_listener_port_non_tls" type="number" required>
  The SCAN listener port for non-TLS (TCP) protocol. The default is 1521. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="scan_listener_port_tls" type="number" required>
  The SCAN listener port for TLS (TCP) protocol. The default is 2484. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="time_zone" type="string">
  The time zone of the Autonomous VM cluster. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_container_databases" type="number" required>
  The total number of Autonomous Container Databases that can be created with the allocated local storage. Changing this will force terraform to create new resource. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="autonomous_data_storage_percentage" type="number">
  The progress of the current operation on the Autonomous VM cluster, as a percentage. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="available_autonomous_data_storage_size_in_tbs" type="number">
  The available data storage space for Autonomous Databases in the Autonomous VM cluster, in TB. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="available_container_databases" type="number">
  The number of Autonomous CDBs that you can create with the currently available storage. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="available_cpus" type="number">
  The number of CPU cores available for allocation to Autonomous Databases Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_arn" type="string">
  The unique identifier of the Exadata infrastructure for this VM cluster. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="cloud_exadata_infrastructure_id" type="string">
  Exadata infrastructure id. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="compute_model" type="string">
  The compute model of the Autonomous VM cluster: ECPU or OCPU. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cpu_core_count" type="number">
  The total number of CPU cores in the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cpu_percentage" type="number">
  The percentage of total CPU cores currently in use in the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_at" type="string">
  The date and time when the Autonomous VM cluster was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_storage_size_in_gbs" type="number">
  The total data storage allocated to the Autonomous VM cluster, in GB. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="data_storage_size_in_tbs" type="number">
  The total data storage allocated to the Autonomous VM cluster, in TB. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="domain" type="string">
  The domain name of the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="exadata_storage_in_tbs_lowest_scaled_value" type="number">
  The minimum value to which you can scale down the Exadata storage, in TB. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hostname" type="string">
  The hostname of the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="is_mtls_enabled_vm_cluster" type="bool">
  Indicates whether mutual TLS (mTLS) authentication is enabled for the Autonomous VM cluster. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="license_model" type="string">
  The license model for the Autonomous VM cluster. Valid values are LICENSE_INCLUDED or BRING_YOUR_OWN_LICENSE . Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="maintenance_window" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="days_of_week" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="hours_of_day" type="set(number)">
    </ResponseField>
    <ResponseField name="lead_time_in_weeks" type="number">
    </ResponseField>
    <ResponseField name="months" type="set(object)">
      <Expandable title="properties">
        <ResponseField name="name" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="preference" type="string" required>
    </ResponseField>
    <ResponseField name="weeks_of_month" type="set(number)">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="max_acds_lowest_scaled_value" type="number">
  The minimum value to which you can scale down the maximum number of Autonomous CDBs. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="memory_size_in_gbs" type="number">
  The total amount of memory allocated to the Autonomous VM cluster, in gigabytes(GB). Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="node_count" type="number">
  The number of database server nodes in the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="non_provisionable_autonomous_container_databases" type="number">
  The number of Autonomous CDBs that can't be provisioned because of resource constraints. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_resource_anchor_name" type="string">
  The name of the OCI resource anchor associated with this Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_url" type="string">
  The URL for accessing the OCI console page for this Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="ocid" type="string">
  The Oracle Cloud Identifier (OCID) of the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="odb_network_arn" type="string">
  The unique identifier of the ODB network for the VM cluster. This member is required. Changing this will create a new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="odb_network_id" type="string">
  The unique identifier of the ODB network associated with this Autonomous VM Cluster. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="odb_node_storage_size_in_gbs" type="number">
  The local node storage allocated to the Autonomous VM cluster, in gigabytes (GB) Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="percent_progress" type="number">
  The progress of the current operation on the Autonomous VM cluster, as a percentage. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisionable_autonomous_container_databases" type="number">
  The number of Autonomous CDBs that can be provisioned in the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioned_autonomous_container_databases" type="number">
  The number of Autonomous CDBs currently provisioned in the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioned_cpus" type="number">
  The number of CPUs provisioned in the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="reclaimable_cpus" type="number">
  The number of CPU cores that can be reclaimed from terminated or scaled-down Autonomous Databases. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reserved_cpus" type="number">
  The number of CPU cores reserved for system operations and redundancy. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="shape" type="string">
  The shape of the Exadata infrastructure for the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the Autonomous VM cluster. Possible values include CREATING, AVAILABLE , UPDATING , DELETING , DELETED , FAILED Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Additional information about the current status of the Autonomous VM cluster. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="time_database_ssl_certificate_expires" type="string">
  The expiration date and time of the database SSL certificate. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="time_ords_certificate_expires" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="time_zone" type="string">
  The time zone of the Autonomous VM cluster. Changing this will force terraform to create new resource. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
