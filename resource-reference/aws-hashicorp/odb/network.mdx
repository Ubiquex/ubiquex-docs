---
title: "aws_odb_network"
description: "Real, generated bindings for `aws_odb_network`."
---

`aws_odb_network` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	odb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/odb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network"})

    		ubx.Resource(odb.Network, "ci-runner", odb.NetworkConfig{
    			AvailabilityZoneId: "example",
    			BackupSubnetCidr:   "example",
    			ClientSubnetCidr:   "example",
    			DisplayName:        "example-display-name",
    			S3Access:           "example",
    			ZeroEtlAccess:      "example",
    			CustomDomainName:   "example-custom-domain-name",
    			DefaultDnsPrefix:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Network } from "jsr:@ubx/sdk-aws/aws/odb/network";

    export default stack("payments", () => {
      intent({ summary: "payments own network" });

      resource(Network, "ci-runner", {
        availabilityZoneId: "example",
        backupSubnetCidr: "example",
        clientSubnetCidr: "example",
        displayName: "example-display-name",
        s3Access: "example",
        zeroEtlAccess: "example",
        customDomainName: "example-custom-domain-name",
        defaultDnsPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.odb import Network, NetworkConfig

    def describe():
        ubx.intent("payments own network")

        ubx.resource(Network, "ci-runner", NetworkConfig(
            availability_zone_id="example",
            backup_subnet_cidr="example",
            client_subnet_cidr="example",
            display_name="example-display-name",
            s3_access="example",
            zero_etl_access="example",
            custom_domain_name="example-custom-domain-name",
            default_dns_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_odb_network, with availability zone id example, backup
    subnet cidr example, client subnet cidr example, display name
    example-display-name, s3 access example, zero etl access example,
    custom domain name example-custom-domain-name, default dns prefix
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="availability_zone" type="string">
  The name of the Availability Zone (AZ) where the odb network is located. Changing this will force terraform to create new resource Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="availability_zone_id" type="string" required>
  The AZ ID of the AZ where the ODB network is located. Changing this will force terraform to create new resource. Required.
</ResponseField>

<ResponseField name="backup_subnet_cidr" type="string" required>
  The CIDR range of the backup subnet for the ODB network. Changing this will force terraform to create new resource. Constraints: - Must not overlap with the CIDR range of the client subnet. - Must not overlap with the CIDR ranges of the VPCs that are connected to the ODB network. - Must not use the following CIDR ranges that are reserved by OCI: - 100.106.0.0/16 and 100.107.0.0/16 - 169.254.0.0/16 - 224.0.0.0 - 239.255.255.255 - 240.0.0.0 - 255.255.255.255 Required.
</ResponseField>

<ResponseField name="client_subnet_cidr" type="string" required>
  The CIDR notation for the network resource. Changing this will force terraform to create new resource. Constraints: - Must not overlap with the CIDR range of the backup subnet. - Must not overlap with the CIDR ranges of the VPCs that are connected to the ODB network. - Must not use the following CIDR ranges that are reserved by OCI: - 100.106.0.0/16 and 100.107.0.0/16 - 169.254.0.0/16 - 224.0.0.0 - 239.255.255.255 - 240.0.0.0 - 255.255.255.255 Required.
</ResponseField>

<ResponseField name="cross_region_s3_restore_sources_access" type="set(string)">
  The list of regions enabled for cross-region restore in the ODB network. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="custom_domain_name" type="string">
  The name of the custom domain that the network is located. custom_domain_name and default_dns_prefix both can't be given. Optional.
</ResponseField>

<ResponseField name="default_dns_prefix" type="string">
  The default DNS prefix for the network resource. Changing this will force terraform to create new resource. Optional.
</ResponseField>

<ResponseField name="delete_associated_resources" type="bool">
  If set to true deletes associated OCI resources. Default false. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  The user-friendly name for the odb network. Changing this will force terraform to create a new resource. Required.
</ResponseField>

<ResponseField name="kms_access" type="string">
  Specifies the configuration for Amazon KMS access from the ODB network. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="kms_policy_document" type="string">
  Specifies the endpoint policy for Amazon KMS access from the ODB network. Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_access" type="string" required>
  Specifies the configuration for Amazon S3 access from the ODB network. Required.
</ResponseField>

<ResponseField name="s3_policy_document" type="string">
  Specifies the endpoint policy for Amazon S3 access from the ODB network. Optional.
</ResponseField>

<ResponseField name="sts_access" type="string">
  Specifies the configuration for Amazon STS access from the ODB network. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sts_policy_document" type="string">
  Specifies the endpoint policy for Amazon STS access from the ODB network. Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zero_etl_access" type="string" required>
  Specifies the configuration for Zero-ETL access from the ODB network. Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="availability_zone" type="string">
  The name of the Availability Zone (AZ) where the odb network is located. Changing this will force terraform to create new resource Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="created_at" type="string">
  The date and time when the ODB network was created. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cross_region_s3_restore_sources_access" type="set(string)">
  The list of regions enabled for cross-region restore in the ODB network. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="delete_associated_resources" type="bool">
  If set to true deletes associated OCI resources. Default false. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ec2_placement_group_ids" type="list(string)">
  A list of EC2 placement group IDs associated with the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="kms_access" type="string">
  Specifies the configuration for Amazon KMS access from the ODB network. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="managed_services" type="list(object)">
  The managed services configuration for the ODB network. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="cross_region_s3_restore_sources_access" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="ipv4_addresses" type="set(string)">
        </ResponseField>
        <ResponseField name="region" type="string">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="kms_access" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string">
        </ResponseField>
        <ResponseField name="ipv4_addresses" type="set(string)">
        </ResponseField>
        <ResponseField name="kms_policy_document" type="string">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="managed_s3_backup_access" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="ipv4_addresses" type="set(string)">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="managed_service_ipv4_cidrs" type="set(string)">
    </ResponseField>
    <ResponseField name="resource_gateway_arn" type="string">
    </ResponseField>
    <ResponseField name="s3_access" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string">
        </ResponseField>
        <ResponseField name="ipv4_addresses" type="set(string)">
        </ResponseField>
        <ResponseField name="s3_policy_document" type="string">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="service_network_arn" type="string">
    </ResponseField>
    <ResponseField name="service_network_endpoint" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="vpc_endpoint_id" type="string">
        </ResponseField>
        <ResponseField name="vpc_endpoint_type" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="sts_access" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="domain_name" type="string">
        </ResponseField>
        <ResponseField name="ipv4_addresses" type="set(string)">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
        <ResponseField name="sts_policy_document" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="zero_etl_access" type="list(object)">
      <Expandable title="properties">
        <ResponseField name="cidr" type="string">
        </ResponseField>
        <ResponseField name="status" type="string">
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="oci_dns_forwarding_configs" type="list(object)">
  The DNS resolver endpoint in OCI for forwarding DNS queries for the ociPrivateZone domain. Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="domain_name" type="string">
    </ResponseField>
    <ResponseField name="oci_dns_listener_ip" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="oci_network_anchor_id" type="string">
  The unique identifier of the OCI network anchor for the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_network_anchor_url" type="string">
  The URL of the OCI network anchor for the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_resource_anchor_name" type="string">
  The name of the OCI resource anchor for the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_vcn_id" type="string">
  The unique identifier Oracle Cloud ID (OCID) of the OCI VCN for the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="oci_vcn_url" type="string">
  The URL of the OCI VCN for the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="peered_cidrs" type="set(string)">
  The list of CIDR ranges from the peered VPC that are allowed access to the ODB network. Please refer odb network peering documentation. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="percent_progress" type="number">
  The amount of progress made on the current operation on the ODB network, expressed as a percentage. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the network resource. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="status_reason" type="string">
  Additional information about the current status of the ODB network. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="sts_access" type="string">
  Specifies the configuration for Amazon STS access from the ODB network. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
