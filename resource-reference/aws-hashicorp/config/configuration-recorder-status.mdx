---
title: "aws_config_configuration_recorder_status"
description: "Real, generated bindings for `aws_config_configuration_recorder_status`."
---

`aws_config_configuration_recorder_status` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	config "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/config"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own configuration recorder status"})

    		ubx.Resource(config.ConfigurationRecorderStatus, "ci-runner", config.ConfigurationRecorderStatusConfig{
    			IsEnabled: true,
    			Name:      "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConfigurationRecorderStatus } from "jsr:@ubx/sdk-aws/aws/config/configuration_recorder_status";

    export default stack("payments", () => {
      intent({ summary: "payments own configuration recorder status" });

      resource(ConfigurationRecorderStatus, "ci-runner", {
        isEnabled: true,
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.config import ConfigurationRecorderStatus, ConfigurationRecorderStatusConfig

    def describe():
        ubx.intent("payments own configuration recorder status")

        ubx.resource(ConfigurationRecorderStatus, "ci-runner", ConfigurationRecorderStatusConfig(
            is_enabled=True,
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_config_configuration_recorder_status called
    "ci-runner", with is enabled true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="is_enabled" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
