---
title: "aws_ram_sharing_with_organization"
description: "Real, generated bindings for `aws_ram_sharing_with_organization`."
---

`aws_ram_sharing_with_organization` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ram "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ram"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sharing with organization"})

    		ubx.Resource(ram.SharingWithOrganization, "ci-runner", ram.SharingWithOrganizationConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SharingWithOrganization } from "jsr:@ubx/sdk-aws/aws/ram/sharing_with_organization";

    export default stack("payments", () => {
      intent({ summary: "payments own sharing with organization" });

      resource(SharingWithOrganization, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ram import SharingWithOrganization, SharingWithOrganizationConfig

    def describe():
        ubx.intent("payments own sharing with organization")

        ubx.resource(SharingWithOrganization, "ci-runner", SharingWithOrganizationConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ram_sharing_with_organization, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
