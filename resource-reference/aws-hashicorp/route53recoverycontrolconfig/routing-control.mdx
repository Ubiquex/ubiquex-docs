---
title: "aws_route53recoverycontrolconfig_routing_control"
description: "Real, generated bindings for `aws_route53recoverycontrolconfig_routing_control`."
---

`aws_route53recoverycontrolconfig_routing_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	route53recoverycontrolconfig "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/route53recoverycontrolconfig"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own routing control"})

    		ubx.Resource(route53recoverycontrolconfig.RoutingControl, "ci-runner", route53recoverycontrolconfig.RoutingControlConfig{
    			ClusterArn: "arn:aws:iam::123456789012:policy/ci",
    			Name:       "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RoutingControl } from "jsr:@ubx/sdk-aws/aws/route53recoverycontrolconfig/routing_control";

    export default stack("payments", () => {
      intent({ summary: "payments own routing control" });

      resource(RoutingControl, "ci-runner", {
        clusterArn: "arn:aws:iam::123456789012:policy/ci",
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.route53recoverycontrolconfig import RoutingControl, RoutingControlConfig

    def describe():
        ubx.intent("payments own routing control")

        ubx.resource(RoutingControl, "ci-runner", RoutingControlConfig(
            cluster_arn="arn:aws:iam::123456789012:policy/ci",
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_route53recoverycontrolconfig_routing_control called
    "ci-runner", with cluster arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="control_panel_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="control_panel_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
