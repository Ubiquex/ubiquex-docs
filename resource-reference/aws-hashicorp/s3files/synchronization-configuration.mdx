---
title: "aws_s3files_synchronization_configuration"
description: "Real, generated bindings for `aws_s3files_synchronization_configuration`."
---

`aws_s3files_synchronization_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3files "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3files"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own synchronization configuration"})

    		ubx.Resource(s3files.SynchronizationConfiguration, "ci-runner", s3files.SynchronizationConfigurationConfig{
    			FileSystemId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SynchronizationConfiguration } from "jsr:@ubx/sdk-aws/aws/s3files/synchronization_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own synchronization configuration" });

      resource(SynchronizationConfiguration, "ci-runner", {
        fileSystemId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3files import SynchronizationConfiguration, SynchronizationConfigurationConfig

    def describe():
        ubx.intent("payments own synchronization configuration")

        ubx.resource(SynchronizationConfiguration, "ci-runner", SynchronizationConfigurationConfig(
            file_system_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3files_synchronization_configuration, with file
    system id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="expiration_data_rule" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="days_after_last_access" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="file_system_id" type="string" required>
  File system ID Required.
</ResponseField>

<ResponseField name="import_data_rule" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="prefix" type="string" required>
    </ResponseField>
    <ResponseField name="size_less_than" type="number" required>
    </ResponseField>
    <ResponseField name="trigger" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="latest_version_number" type="number">
  Latest version number for optimistic locking Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="expiration_data_rule" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="days_after_last_access" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="import_data_rule" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="prefix" type="string" required>
    </ResponseField>
    <ResponseField name="size_less_than" type="number" required>
    </ResponseField>
    <ResponseField name="trigger" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="latest_version_number" type="number">
  Latest version number for optimistic locking Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
