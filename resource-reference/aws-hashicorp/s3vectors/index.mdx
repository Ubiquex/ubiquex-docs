---
title: "aws_s3vectors_index"
description: "Real, generated bindings for `aws_s3vectors_index`."
---

`aws_s3vectors_index` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3vectors "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3vectors"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own index"})

    		ubx.Resource(s3vectors.Index, "ci-runner", s3vectors.IndexConfig{
    			DataType:         "example",
    			Dimension:        1,
    			DistanceMetric:   "example",
    			IndexName:        "example-index-name",
    			VectorBucketName: "example-vector-bucket-name",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Index } from "jsr:@ubx/sdk-aws/aws/s3vectors/index";

    export default stack("payments", () => {
      intent({ summary: "payments own index" });

      resource(Index, "ci-runner", {
        dataType: "example",
        dimension: 1,
        distanceMetric: "example",
        indexName: "example-index-name",
        vectorBucketName: "example-vector-bucket-name",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3vectors import Index, IndexConfig

    def describe():
        ubx.intent("payments own index")

        ubx.resource(Index, "ci-runner", IndexConfig(
            data_type="example",
            dimension=1,
            distance_metric="example",
            index_name="example-index-name",
            vector_bucket_name="example-vector-bucket-name",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3vectors_index, with data type example, dimension 1,
    distance metric example, index name example-index-name, vector
    bucket name example-vector-bucket-name, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="data_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="dimension" type="number" required>
  Required.
</ResponseField>

<ResponseField name="distance_metric" type="string" required>
  Required.
</ResponseField>

<ResponseField name="encryption_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
    </ResponseField>
    <ResponseField name="sse_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="index_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="metadata_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="non_filterable_metadata_keys" type="set(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vector_bucket_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="creation_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="encryption_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="kms_key_arn" type="string">
    </ResponseField>
    <ResponseField name="sse_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="index_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="metadata_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="non_filterable_metadata_keys" type="set(string)" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
