---
title: "aws_workspacesweb_network_settings"
description: "Real, generated bindings for `aws_workspacesweb_network_settings`."
---

`aws_workspacesweb_network_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own network settings"})

    		ubx.Resource(workspacesweb.NetworkSettings, "ci-runner", workspacesweb.NetworkSettingsConfig{
    			SecurityGroupIds: []string{"example"},
    			SubnetIds:        []string{"example"},
    			VpcId:            "example",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NetworkSettings } from "jsr:@ubx/sdk-aws/aws/workspacesweb/network_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own network settings" });

      resource(NetworkSettings, "ci-runner", {
        securityGroupIds: ["example"],
        subnetIds: ["example"],
        vpcId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import NetworkSettings, NetworkSettingsConfig

    def describe():
        ubx.intent("payments own network settings")

        ubx.resource(NetworkSettings, "ci-runner", NetworkSettingsConfig(
            security_group_ids=["example"],
            subnet_ids=["example"],
            vpc_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_network_settings, with security group
    ids a set value, subnet ids a set value, vpc id example, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="subnet_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="vpc_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="associated_portal_arns" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="network_settings_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
