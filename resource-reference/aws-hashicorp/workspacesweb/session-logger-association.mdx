---
title: "aws_workspacesweb_session_logger_association"
description: "Real, generated bindings for `aws_workspacesweb_session_logger_association`."
---

`aws_workspacesweb_session_logger_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workspacesweb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workspacesweb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own session logger association"})

    		ubx.Resource(workspacesweb.SessionLoggerAssociation, "ci-runner", workspacesweb.SessionLoggerAssociationConfig{
    			PortalArn:        "arn:aws:iam::123456789012:policy/ci",
    			SessionLoggerArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SessionLoggerAssociation } from "jsr:@ubx/sdk-aws/aws/workspacesweb/session_logger_association";

    export default stack("payments", () => {
      intent({ summary: "payments own session logger association" });

      resource(SessionLoggerAssociation, "ci-runner", {
        portalArn: "arn:aws:iam::123456789012:policy/ci",
        sessionLoggerArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workspacesweb import SessionLoggerAssociation, SessionLoggerAssociationConfig

    def describe():
        ubx.intent("payments own session logger association")

        ubx.resource(SessionLoggerAssociation, "ci-runner", SessionLoggerAssociationConfig(
            portal_arn="arn:aws:iam::123456789012:policy/ci",
            session_logger_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workspacesweb_session_logger_association, with portal
    arn arn:aws:iam::123456789012:policy/ci, session logger arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="portal_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="session_logger_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
