---
title: "aws_networkmonitor_probe"
description: "Real, generated bindings for `aws_networkmonitor_probe`."
---

`aws_networkmonitor_probe` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmonitor "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmonitor"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own probe"})

    		ubx.Resource(networkmonitor.Probe, "ci-runner", networkmonitor.ProbeConfig{
    			Destination:     "example",
    			MonitorName:     "example-monitor-name",
    			Protocol:        "example",
    			SourceArn:       "arn:aws:iam::123456789012:policy/ci",
    			DestinationPort: 1,
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Probe } from "jsr:@ubx/sdk-aws/aws/networkmonitor/probe";

    export default stack("payments", () => {
      intent({ summary: "payments own probe" });

      resource(Probe, "ci-runner", {
        destination: "example",
        monitorName: "example-monitor-name",
        protocol: "example",
        sourceArn: "arn:aws:iam::123456789012:policy/ci",
        destinationPort: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmonitor import Probe, ProbeConfig

    def describe():
        ubx.intent("payments own probe")

        ubx.resource(Probe, "ci-runner", ProbeConfig(
            destination="example",
            monitor_name="example-monitor-name",
            protocol="example",
            source_arn="arn:aws:iam::123456789012:policy/ci",
            destination_port=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmonitor_probe, with destination example,
    monitor name example-monitor-name, protocol example, source arn
    arn:aws:iam::123456789012:policy/ci, destination port 1, tags a
    set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="destination" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_port" type="number">
  Optional.
</ResponseField>

<ResponseField name="monitor_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="packet_size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="protocol" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="address_family" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="packet_size" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="probe_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpc_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
