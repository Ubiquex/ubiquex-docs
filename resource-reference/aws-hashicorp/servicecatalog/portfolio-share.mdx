---
title: "aws_servicecatalog_portfolio_share"
description: "Real, generated bindings for `aws_servicecatalog_portfolio_share`."
---

`aws_servicecatalog_portfolio_share` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicecatalog "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicecatalog"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own portfolio share"})

    		ubx.Resource(servicecatalog.PortfolioShare, "ci-runner", servicecatalog.PortfolioShareConfig{
    			PortfolioId:       "example",
    			PrincipalId:       "example",
    			Type:              "example",
    			AcceptLanguage:    "example",
    			SharePrincipals:   true,
    			ShareTagOptions:   true,
    			WaitForAcceptance: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PortfolioShare } from "jsr:@ubx/sdk-aws/aws/servicecatalog/portfolio_share";

    export default stack("payments", () => {
      intent({ summary: "payments own portfolio share" });

      resource(PortfolioShare, "ci-runner", {
        portfolioId: "example",
        principalId: "example",
        type: "example",
        acceptLanguage: "example",
        sharePrincipals: true,
        shareTagOptions: true,
        waitForAcceptance: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicecatalog import PortfolioShare, PortfolioShareConfig

    def describe():
        ubx.intent("payments own portfolio share")

        ubx.resource(PortfolioShare, "ci-runner", PortfolioShareConfig(
            portfolio_id="example",
            principal_id="example",
            type="example",
            accept_language="example",
            share_principals=True,
            share_tag_options=True,
            wait_for_acceptance=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicecatalog_portfolio_share, with portfolio id
    example, principal id example, type example, accept language
    example, share principals true, share tag options true, wait for
    acceptance true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="accept_language" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="portfolio_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="principal_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="share_principals" type="bool">
  Optional.
</ResponseField>

<ResponseField name="share_tag_options" type="bool">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="wait_for_acceptance" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="accepted" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
