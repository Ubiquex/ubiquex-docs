---
title: "aws_shield_subscription"
description: "Real, generated bindings for `aws_shield_subscription`."
---

`aws_shield_subscription` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	shield "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/shield"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own subscription"})

    		ubx.Resource(shield.Subscription, "ci-runner", shield.SubscriptionConfig{
    			SkipDestroy: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Subscription } from "jsr:@ubx/sdk-aws/aws/shield/subscription";

    export default stack("payments", () => {
      intent({ summary: "payments own subscription" });

      resource(Subscription, "ci-runner", {
        skipDestroy: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.shield import Subscription, SubscriptionConfig

    def describe():
        ubx.intent("payments own subscription")

        ubx.resource(Subscription, "ci-runner", SubscriptionConfig(
            skip_destroy=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_shield_subscription, with skip destroy true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_renew" type="string">
  Whether to automatically renew the subscription when it expires. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="auto_renew" type="string">
  Whether to automatically renew the subscription when it expires. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
