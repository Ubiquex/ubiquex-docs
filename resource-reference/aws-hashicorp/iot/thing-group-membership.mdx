---
title: "aws_iot_thing_group_membership"
description: "Real, generated bindings for `aws_iot_thing_group_membership`."
---

`aws_iot_thing_group_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	iot "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/iot"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own thing group membership"})

    		ubx.Resource(iot.ThingGroupMembership, "ci-runner", iot.ThingGroupMembershipConfig{
    			ThingGroupName:       "example-thing-group-name",
    			ThingName:            "example-thing-name",
    			OverrideDynamicGroup: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ThingGroupMembership } from "jsr:@ubx/sdk-aws/aws/iot/thing_group_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own thing group membership" });

      resource(ThingGroupMembership, "ci-runner", {
        thingGroupName: "example-thing-group-name",
        thingName: "example-thing-name",
        overrideDynamicGroup: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.iot import ThingGroupMembership, ThingGroupMembershipConfig

    def describe():
        ubx.intent("payments own thing group membership")

        ubx.resource(ThingGroupMembership, "ci-runner", ThingGroupMembershipConfig(
            thing_group_name="example-thing-group-name",
            thing_name="example-thing-name",
            override_dynamic_group=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_iot_thing_group_membership, with thing group name
    example-thing-group-name, thing name example-thing-name, override
    dynamic group true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="override_dynamic_group" type="bool">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="thing_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="thing_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
