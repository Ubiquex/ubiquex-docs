---
title: "aws_wafv2_api_key"
description: "Real, generated bindings for `aws_wafv2_api_key`."
---

`aws_wafv2_api_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	wafv2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/wafv2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own api key"})

    		ubx.Resource(wafv2.ApiKey, "ci-runner", wafv2.ApiKeyConfig{
    			Scope:        "example",
    			TokenDomains: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ApiKey } from "jsr:@ubx/sdk-aws/aws/wafv2/api_key";

    export default stack("payments", () => {
      intent({ summary: "payments own api key" });

      resource(ApiKey, "ci-runner", {
        scope: "example",
        tokenDomains: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.wafv2 import ApiKey, ApiKeyConfig

    def describe():
        ubx.intent("payments own api key")

        ubx.resource(ApiKey, "ci-runner", ApiKeyConfig(
            scope="example",
            token_domains=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_wafv2_api_key, with scope example, token domains a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scope" type="string" required>
  Specifies whether this is for an AWS CloudFront distribution or for a regional application. Valid values are CLOUDFRONT or REGIONAL. Required.
</ResponseField>

<ResponseField name="token_domains" type="set(string)" required>
  The domains that you want to be able to use the API key with, for example example.com. Maximum of 5 domains. Required.
</ResponseField>

## Output properties

<ResponseField name="api_key" type="string">
  The API key value. This is sensitive and not included in responses. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
