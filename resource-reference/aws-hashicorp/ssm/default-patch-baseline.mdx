---
title: "aws_ssm_default_patch_baseline"
description: "Real, generated bindings for `aws_ssm_default_patch_baseline`."
---

`aws_ssm_default_patch_baseline` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ssm "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ssm"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own default patch baseline"})

    		ubx.Resource(ssm.DefaultPatchBaseline, "ci-runner", ssm.DefaultPatchBaselineConfig{
    			BaselineId:      "example",
    			OperatingSystem: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DefaultPatchBaseline } from "jsr:@ubx/sdk-aws/aws/ssm/default_patch_baseline";

    export default stack("payments", () => {
      intent({ summary: "payments own default patch baseline" });

      resource(DefaultPatchBaseline, "ci-runner", {
        baselineId: "example",
        operatingSystem: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ssm import DefaultPatchBaseline, DefaultPatchBaselineConfig

    def describe():
        ubx.intent("payments own default patch baseline")

        ubx.resource(DefaultPatchBaseline, "ci-runner", DefaultPatchBaselineConfig(
            baseline_id="example",
            operating_system="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ssm_default_patch_baseline, with baseline id example,
    operating system example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="baseline_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="operating_system" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
