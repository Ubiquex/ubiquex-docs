---
title: "aws_mailmanager_traffic_policy"
description: "Real, generated bindings for `aws_mailmanager_traffic_policy`."
---

`aws_mailmanager_traffic_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	mailmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/mailmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own traffic policy"})

    		ubx.Resource(mailmanager.TrafficPolicy, "ci-runner", mailmanager.TrafficPolicyConfig{
    			DefaultAction:       "example",
    			Name:                "ci-runner",
    			MaxMessageSizeBytes: 1,
    			Tags:                map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TrafficPolicy } from "jsr:@ubx/sdk-aws/aws/mailmanager/traffic_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own traffic policy" });

      resource(TrafficPolicy, "ci-runner", {
        defaultAction: "example",
        name: "ci-runner",
        maxMessageSizeBytes: 1,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.mailmanager import TrafficPolicy, TrafficPolicyConfig

    def describe():
        ubx.intent("payments own traffic policy")

        ubx.resource(TrafficPolicy, "ci-runner", TrafficPolicyConfig(
            default_action="example",
            name="ci-runner",
            max_message_size_bytes=1,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_mailmanager_traffic_policy called "ci-runner", with
    default action example, max message size bytes 1, tags a set
    value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="default_action" type="string" required>
  Required.
</ResponseField>

<ResponseField name="max_message_size_bytes" type="number">
  Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="created_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_updated_timestamp" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
