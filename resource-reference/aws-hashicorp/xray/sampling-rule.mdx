---
title: "aws_xray_sampling_rule"
description: "Real, generated bindings for `aws_xray_sampling_rule`."
---

`aws_xray_sampling_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	xray "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/xray"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sampling rule"})

    		ubx.Resource(xray.SamplingRule, "ci-runner", xray.SamplingRuleConfig{
    			FixedRate:     1,
    			Host:          "example",
    			HttpMethod:    "example",
    			Priority:      1,
    			ReservoirSize: 1,
    			ResourceArn:   "arn:aws:iam::123456789012:policy/ci",
    			ServiceName:   "example-service-name",
    			ServiceType:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SamplingRule } from "jsr:@ubx/sdk-aws/aws/xray/sampling_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own sampling rule" });

      resource(SamplingRule, "ci-runner", {
        fixedRate: 1,
        host: "example",
        httpMethod: "example",
        priority: 1,
        reservoirSize: 1,
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        serviceName: "example-service-name",
        serviceType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.xray import SamplingRule, SamplingRuleConfig

    def describe():
        ubx.intent("payments own sampling rule")

        ubx.resource(SamplingRule, "ci-runner", SamplingRuleConfig(
            fixed_rate=1,
            host="example",
            http_method="example",
            priority=1,
            reservoir_size=1,
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            service_name="example-service-name",
            service_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_xray_sampling_rule, with fixed rate 1, host example,
    http method example, priority 1, reservoir size 1, resource arn
    arn:aws:iam::123456789012:policy/ci, service name
    example-service-name, service type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attributes" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="fixed_rate" type="number" required>
  Required.
</ResponseField>

<ResponseField name="host" type="string" required>
  Required.
</ResponseField>

<ResponseField name="http_method" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="priority" type="number" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="reservoir_size" type="number" required>
  Required.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="rule_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="service_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="url_path" type="string" required>
  Required.
</ResponseField>

<ResponseField name="version" type="number" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
