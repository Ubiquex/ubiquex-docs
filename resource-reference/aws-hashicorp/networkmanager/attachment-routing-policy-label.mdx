---
title: "aws_networkmanager_attachment_routing_policy_label"
description: "Real, generated bindings for `aws_networkmanager_attachment_routing_policy_label`."
---

`aws_networkmanager_attachment_routing_policy_label` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	networkmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/networkmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attachment routing policy label"})

    		ubx.Resource(networkmanager.AttachmentRoutingPolicyLabel, "ci-runner", networkmanager.AttachmentRoutingPolicyLabelConfig{
    			AttachmentId:       "example",
    			CoreNetworkId:      "example",
    			RoutingPolicyLabel: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AttachmentRoutingPolicyLabel } from "jsr:@ubx/sdk-aws/aws/networkmanager/attachment_routing_policy_label";

    export default stack("payments", () => {
      intent({ summary: "payments own attachment routing policy label" });

      resource(AttachmentRoutingPolicyLabel, "ci-runner", {
        attachmentId: "example",
        coreNetworkId: "example",
        routingPolicyLabel: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.networkmanager import AttachmentRoutingPolicyLabel, AttachmentRoutingPolicyLabelConfig

    def describe():
        ubx.intent("payments own attachment routing policy label")

        ubx.resource(AttachmentRoutingPolicyLabel, "ci-runner", AttachmentRoutingPolicyLabelConfig(
            attachment_id="example",
            core_network_id="example",
            routing_policy_label="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_networkmanager_attachment_routing_policy_label, with
    attachment id example, core network id example, routing policy
    label example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attachment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="core_network_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="routing_policy_label" type="string" required>
  Required.
</ResponseField>

## Output properties


