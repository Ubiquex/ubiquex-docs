---
title: "aws_ec2_transit_gateway_prefix_list_reference"
description: "Real, generated bindings for `aws_ec2_transit_gateway_prefix_list_reference`."
---

`aws_ec2_transit_gateway_prefix_list_reference` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ec2 "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ec2"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own transit gateway prefix list reference"})

    		ubx.Resource(ec2.TransitGatewayPrefixListReference, "ci-runner", ec2.TransitGatewayPrefixListReferenceConfig{
    			PrefixListId:               "example",
    			TransitGatewayRouteTableId: "example",
    			Blackhole:                  true,
    			TransitGatewayAttachmentId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TransitGatewayPrefixListReference } from "jsr:@ubx/sdk-aws/aws/ec2/transit_gateway_prefix_list_reference";

    export default stack("payments", () => {
      intent({ summary: "payments own transit gateway prefix list reference" });

      resource(TransitGatewayPrefixListReference, "ci-runner", {
        prefixListId: "example",
        transitGatewayRouteTableId: "example",
        blackhole: true,
        transitGatewayAttachmentId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ec2 import TransitGatewayPrefixListReference, TransitGatewayPrefixListReferenceConfig

    def describe():
        ubx.intent("payments own transit gateway prefix list reference")

        ubx.resource(TransitGatewayPrefixListReference, "ci-runner", TransitGatewayPrefixListReferenceConfig(
            prefix_list_id="example",
            transit_gateway_route_table_id="example",
            blackhole=True,
            transit_gateway_attachment_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ec2_transit_gateway_prefix_list_reference, with prefix
    list id example, transit gateway route table id example,
    blackhole true, transit gateway attachment id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="blackhole" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix_list_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_route_table_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="prefix_list_owner_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
