---
title: "aws_sagemaker_image_version"
description: "Real, generated bindings for `aws_sagemaker_image_version`."
---

`aws_sagemaker_image_version` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sagemaker "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sagemaker"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own image version"})

    		ubx.Resource(sagemaker.ImageVersion, "ci-runner", sagemaker.ImageVersionConfig{
    			BaseImage:       "example",
    			ImageName:       "example-image-name",
    			Aliases:         []string{"example"},
    			Horovod:         true,
    			JobType:         "example",
    			MlFramework:     "example",
    			Processor:       "example",
    			ProgrammingLang: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ImageVersion } from "jsr:@ubx/sdk-aws/aws/sagemaker/image_version";

    export default stack("payments", () => {
      intent({ summary: "payments own image version" });

      resource(ImageVersion, "ci-runner", {
        baseImage: "example",
        imageName: "example-image-name",
        aliases: ["example"],
        horovod: true,
        jobType: "example",
        mlFramework: "example",
        processor: "example",
        programmingLang: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sagemaker import ImageVersion, ImageVersionConfig

    def describe():
        ubx.intent("payments own image version")

        ubx.resource(ImageVersion, "ci-runner", ImageVersionConfig(
            base_image="example",
            image_name="example-image-name",
            aliases=["example"],
            horovod=True,
            job_type="example",
            ml_framework="example",
            processor="example",
            programming_lang="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sagemaker_image_version, with base image example,
    image name example-image-name, aliases a set value, horovod true,
    job type example, ml framework example, processor example,
    programming lang example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aliases" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="base_image" type="string" required>
  Required.
</ResponseField>

<ResponseField name="horovod" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="job_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="ml_framework" type="string">
  Optional.
</ResponseField>

<ResponseField name="processor" type="string">
  Optional.
</ResponseField>

<ResponseField name="programming_lang" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="release_notes" type="string">
  Optional.
</ResponseField>

<ResponseField name="vendor_guidance" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="container_image" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="image_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="version" type="number">
  Computed by AWS after `ship`.
</ResponseField>
