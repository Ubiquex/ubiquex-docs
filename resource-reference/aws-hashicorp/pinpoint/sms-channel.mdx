---
title: "aws_pinpoint_sms_channel"
description: "Real, generated bindings for `aws_pinpoint_sms_channel`."
---

`aws_pinpoint_sms_channel` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	pinpoint "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/pinpoint"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sms channel"})

    		ubx.Resource(pinpoint.SmsChannel, "ci-runner", pinpoint.SmsChannelConfig{
    			ApplicationId: "example",
    			Enabled:       true,
    			SenderId:      "example",
    			ShortCode:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SmsChannel } from "jsr:@ubx/sdk-aws/aws/pinpoint/sms_channel";

    export default stack("payments", () => {
      intent({ summary: "payments own sms channel" });

      resource(SmsChannel, "ci-runner", {
        applicationId: "example",
        enabled: true,
        senderId: "example",
        shortCode: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.pinpoint import SmsChannel, SmsChannelConfig

    def describe():
        ubx.intent("payments own sms channel")

        ubx.resource(SmsChannel, "ci-runner", SmsChannelConfig(
            application_id="example",
            enabled=True,
            sender_id="example",
            short_code="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_pinpoint_sms_channel, with application id example,
    enabled true, sender id example, short code example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="sender_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="short_code" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="promotional_messages_per_second" type="number">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="transactional_messages_per_second" type="number">
  Computed by AWS after `ship`.
</ResponseField>
