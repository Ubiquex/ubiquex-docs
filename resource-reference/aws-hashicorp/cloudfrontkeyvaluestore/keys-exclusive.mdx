---
title: "aws_cloudfrontkeyvaluestore_keys_exclusive"
description: "Real, generated bindings for `aws_cloudfrontkeyvaluestore_keys_exclusive`."
---

`aws_cloudfrontkeyvaluestore_keys_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfrontkeyvaluestore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfrontkeyvaluestore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own keys exclusive"})

    		ubx.Resource(cloudfrontkeyvaluestore.KeysExclusive, "ci-runner", cloudfrontkeyvaluestore.KeysExclusiveConfig{
    			KeyValueStoreArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeysExclusive } from "jsr:@ubx/sdk-aws/aws/cloudfrontkeyvaluestore/keys_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own keys exclusive" });

      resource(KeysExclusive, "ci-runner", {
        keyValueStoreArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfrontkeyvaluestore import KeysExclusive, KeysExclusiveConfig

    def describe():
        ubx.intent("payments own keys exclusive")

        ubx.resource(KeysExclusive, "ci-runner", KeysExclusiveConfig(
            key_value_store_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfrontkeyvaluestore_keys_exclusive, with key value
    store arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="key_value_store_arn" type="string" required>
  The Amazon Resource Name (ARN) of the Key Value Store. Required.
</ResponseField>

<ResponseField name="max_batch_size" type="number">
  Maximum resource key values pairs that you wills update in a single API request. AWS has a default quota of 50 keys or a 3 MB payload, whichever is reached first Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_key_value_pair" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="value" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="max_batch_size" type="number">
  Maximum resource key values pairs that you wills update in a single API request. AWS has a default quota of 50 keys or a 3 MB payload, whichever is reached first Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_key_value_pair" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="key" type="string" required>
    </ResponseField>
    <ResponseField name="value" type="string" required>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total_size_in_bytes" type="number">
  Total size of the Key Value Store in bytes. Computed by AWS after `ship`.
</ResponseField>
