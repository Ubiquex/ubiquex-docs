---
title: "aws_cognito_user"
description: "Real, generated bindings for `aws_cognito_user`."
---

`aws_cognito_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cognito "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cognito"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user"})

    		ubx.Resource(cognito.User, "ci-runner", cognito.UserConfig{
    			UserPoolId:             "example",
    			Username:               "example",
    			Attributes:             map[string]string{"managed-by": "ubx"},
    			ClientMetadata:         map[string]string{"managed-by": "ubx"},
    			DesiredDeliveryMediums: []string{"example"},
    			Enabled:                true,
    			ForceAliasCreation:     true,
    			MessageAction:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "jsr:@ubx/sdk-aws/aws/cognito/user";

    export default stack("payments", () => {
      intent({ summary: "payments own user" });

      resource(User, "ci-runner", {
        userPoolId: "example",
        username: "example",
        attributes: { "managed-by": "ubx" },
        clientMetadata: { "managed-by": "ubx" },
        desiredDeliveryMediums: ["example"],
        enabled: true,
        forceAliasCreation: true,
        messageAction: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cognito import User, UserConfig

    def describe():
        ubx.intent("payments own user")

        ubx.resource(User, "ci-runner", UserConfig(
            user_pool_id="example",
            username="example",
            attributes={"managed-by": "ubx"},
            client_metadata={"managed-by": "ubx"},
            desired_delivery_mediums=["example"],
            enabled=True,
            force_alias_creation=True,
            message_action="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cognito_user, with user pool id example, username
    example, attributes a set value, client metadata a set value,
    desired delivery mediums a set value, enabled true, force alias
    creation true, message action example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="attributes" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="client_metadata" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="desired_delivery_mediums" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="force_alias_creation" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="message_action" type="string">
  Optional.
</ResponseField>

<ResponseField name="password" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="temporary_password" type="string">
  Optional.
</ResponseField>

<ResponseField name="user_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="username" type="string" required>
  Required.
</ResponseField>

<ResponseField name="validation_data" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="creation_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="last_modified_date" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="mfa_setting_list" type="set(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="preferred_mfa_setting" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="sub" type="string">
  Computed by AWS after `ship`.
</ResponseField>
