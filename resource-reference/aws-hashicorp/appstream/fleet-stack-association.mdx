---
title: "aws_appstream_fleet_stack_association"
description: "Real, generated bindings for `aws_appstream_fleet_stack_association`."
---

`aws_appstream_fleet_stack_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appstream "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appstream"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own fleet stack association"})

    		ubx.Resource(appstream.FleetStackAssociation, "ci-runner", appstream.FleetStackAssociationConfig{
    			FleetName: "example-fleet-name",
    			StackName: "example-stack-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FleetStackAssociation } from "jsr:@ubx/sdk-aws/aws/appstream/fleet_stack_association";

    export default stack("payments", () => {
      intent({ summary: "payments own fleet stack association" });

      resource(FleetStackAssociation, "ci-runner", {
        fleetName: "example-fleet-name",
        stackName: "example-stack-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appstream import FleetStackAssociation, FleetStackAssociationConfig

    def describe():
        ubx.intent("payments own fleet stack association")

        ubx.resource(FleetStackAssociation, "ci-runner", FleetStackAssociationConfig(
            fleet_name="example-fleet-name",
            stack_name="example-stack-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appstream_fleet_stack_association, with fleet name
    example-fleet-name, stack name example-stack-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="fleet_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
