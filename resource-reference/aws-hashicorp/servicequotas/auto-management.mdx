---
title: "aws_servicequotas_auto_management"
description: "Real, generated bindings for `aws_servicequotas_auto_management`."
---

`aws_servicequotas_auto_management` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	servicequotas "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/servicequotas"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own auto management"})

    		ubx.Resource(servicequotas.AutoManagement, "ci-runner", servicequotas.AutoManagementConfig{
    			OptInLevel:      "example",
    			OptInType:       "example",
    			ExclusionList:   map[string]string{"managed-by": "ubx"},
    			NotificationArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AutoManagement } from "jsr:@ubx/sdk-aws/aws/servicequotas/auto_management";

    export default stack("payments", () => {
      intent({ summary: "payments own auto management" });

      resource(AutoManagement, "ci-runner", {
        optInLevel: "example",
        optInType: "example",
        exclusionList: { "managed-by": "ubx" },
        notificationArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.servicequotas import AutoManagement, AutoManagementConfig

    def describe():
        ubx.intent("payments own auto management")

        ubx.resource(AutoManagement, "ci-runner", AutoManagementConfig(
            opt_in_level="example",
            opt_in_type="example",
            exclusion_list={"managed-by": "ubx"},
            notification_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_servicequotas_auto_management, with opt in level
    example, opt in type example, exclusion list a set value,
    notification arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="exclusion_list" type="map(list(string))">
  Optional.
</ResponseField>

<ResponseField name="notification_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="opt_in_level" type="string" required>
  Required.
</ResponseField>

<ResponseField name="opt_in_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
