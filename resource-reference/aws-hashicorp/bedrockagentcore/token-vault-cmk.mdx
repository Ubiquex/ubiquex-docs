---
title: "aws_bedrockagentcore_token_vault_cmk"
description: "Real, generated bindings for `aws_bedrockagentcore_token_vault_cmk`."
---

`aws_bedrockagentcore_token_vault_cmk` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrockagentcore "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrockagentcore"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own token vault cmk"})

    		ubx.Resource(bedrockagentcore.TokenVaultCmk, "ci-runner", bedrockagentcore.TokenVaultCmkConfig{})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TokenVaultCmk } from "jsr:@ubx/sdk-aws/aws/bedrockagentcore/token_vault_cmk";

    export default stack("payments", () => {
      intent({ summary: "payments own token vault cmk" });

      resource(TokenVaultCmk, "ci-runner", {});
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrockagentcore import TokenVaultCmk, TokenVaultCmkConfig

    def describe():
        ubx.intent("payments own token vault cmk")

        ubx.resource(TokenVaultCmk, "ci-runner", TokenVaultCmkConfig(
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrockagentcore_token_vault_cmk, with .
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="kms_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="key_type" type="string" required>
    </ResponseField>
    <ResponseField name="kms_key_arn" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="token_vault_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="kms_configuration" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="key_type" type="string" required>
    </ResponseField>
    <ResponseField name="kms_key_arn" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="token_vault_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
