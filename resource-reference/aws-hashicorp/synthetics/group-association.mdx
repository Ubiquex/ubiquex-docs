---
title: "aws_synthetics_group_association"
description: "Real, generated bindings for `aws_synthetics_group_association`."
---

`aws_synthetics_group_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	synthetics "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/synthetics"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own group association"})

    		ubx.Resource(synthetics.GroupAssociation, "ci-runner", synthetics.GroupAssociationConfig{
    			CanaryArn: "arn:aws:iam::123456789012:policy/ci",
    			GroupName: "example-group-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { GroupAssociation } from "jsr:@ubx/sdk-aws/aws/synthetics/group_association";

    export default stack("payments", () => {
      intent({ summary: "payments own group association" });

      resource(GroupAssociation, "ci-runner", {
        canaryArn: "arn:aws:iam::123456789012:policy/ci",
        groupName: "example-group-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.synthetics import GroupAssociation, GroupAssociationConfig

    def describe():
        ubx.intent("payments own group association")

        ubx.resource(GroupAssociation, "ci-runner", GroupAssociationConfig(
            canary_arn="arn:aws:iam::123456789012:policy/ci",
            group_name="example-group-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_synthetics_group_association, with canary arn
    arn:aws:iam::123456789012:policy/ci, group name
    example-group-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="canary_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="group_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="group_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
