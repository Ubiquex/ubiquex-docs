---
title: "aws_opensearch_authorize_vpc_endpoint_access"
description: "Real, generated bindings for `aws_opensearch_authorize_vpc_endpoint_access`."
---

`aws_opensearch_authorize_vpc_endpoint_access` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	opensearch "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/opensearch"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authorize vpc endpoint access"})

    		ubx.Resource(opensearch.AuthorizeVpcEndpointAccess, "ci-runner", opensearch.AuthorizeVpcEndpointAccessConfig{
    			Account:    "example",
    			DomainName: "example-domain-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AuthorizeVpcEndpointAccess } from "jsr:@ubx/sdk-aws/aws/opensearch/authorize_vpc_endpoint_access";

    export default stack("payments", () => {
      intent({ summary: "payments own authorize vpc endpoint access" });

      resource(AuthorizeVpcEndpointAccess, "ci-runner", {
        account: "example",
        domainName: "example-domain-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.opensearch import AuthorizeVpcEndpointAccess, AuthorizeVpcEndpointAccessConfig

    def describe():
        ubx.intent("payments own authorize vpc endpoint access")

        ubx.resource(AuthorizeVpcEndpointAccess, "ci-runner", AuthorizeVpcEndpointAccessConfig(
            account="example",
            domain_name="example-domain-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_opensearch_authorize_vpc_endpoint_access, with account
    example, domain name example-domain-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="domain_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="authorized_principal" type="list(object)">
  Computed by AWS after `ship`.
  <Expandable title="properties">
    <ResponseField name="principal" type="string">
    </ResponseField>
    <ResponseField name="principal_type" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
