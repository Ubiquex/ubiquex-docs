---
title: "aws_vpc_ipam_pool_cidr_allocation"
description: "Real, generated bindings for `aws_vpc_ipam_pool_cidr_allocation`."
---

`aws_vpc_ipam_pool_cidr_allocation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ipam pool cidr allocation"})

    		ubx.Resource(vpc.IpamPoolCidrAllocation, "ci-runner", vpc.IpamPoolCidrAllocationConfig{
    			IpamPoolId:      "example",
    			Description:     "Managed by ubx.",
    			DisallowedCidrs: []string{"example"},
    			Tags:            map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IpamPoolCidrAllocation } from "jsr:@ubx/sdk-aws/aws/vpc/ipam_pool_cidr_allocation";

    export default stack("payments", () => {
      intent({ summary: "payments own ipam pool cidr allocation" });

      resource(IpamPoolCidrAllocation, "ci-runner", {
        ipamPoolId: "example",
        description: "Managed by ubx.",
        disallowedCidrs: ["example"],
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import IpamPoolCidrAllocation, IpamPoolCidrAllocationConfig

    def describe():
        ubx.intent("payments own ipam pool cidr allocation")

        ubx.resource(IpamPoolCidrAllocation, "ci-runner", IpamPoolCidrAllocationConfig(
            ipam_pool_id="example",
            description="Managed by ubx.",
            disallowed_cidrs=["example"],
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_ipam_pool_cidr_allocation, with ipam pool id
    example, description Managed by ubx., disallowed cidrs a set
    value, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cidr" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="disallowed_cidrs" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_pool_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="cidr" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ipam_pool_allocation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="netmask_length" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_owner" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="resource_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
