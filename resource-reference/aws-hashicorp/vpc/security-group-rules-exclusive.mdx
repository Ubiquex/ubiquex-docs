---
title: "aws_vpc_security_group_rules_exclusive"
description: "Real, generated bindings for `aws_vpc_security_group_rules_exclusive`."
---

`aws_vpc_security_group_rules_exclusive` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpc "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpc"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own security group rules exclusive"})

    		ubx.Resource(vpc.SecurityGroupRulesExclusive, "ci-runner", vpc.SecurityGroupRulesExclusiveConfig{
    			EgressRuleIds:   []string{"example"},
    			IngressRuleIds:  []string{"example"},
    			SecurityGroupId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecurityGroupRulesExclusive } from "jsr:@ubx/sdk-aws/aws/vpc/security_group_rules_exclusive";

    export default stack("payments", () => {
      intent({ summary: "payments own security group rules exclusive" });

      resource(SecurityGroupRulesExclusive, "ci-runner", {
        egressRuleIds: ["example"],
        ingressRuleIds: ["example"],
        securityGroupId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpc import SecurityGroupRulesExclusive, SecurityGroupRulesExclusiveConfig

    def describe():
        ubx.intent("payments own security group rules exclusive")

        ubx.resource(SecurityGroupRulesExclusive, "ci-runner", SecurityGroupRulesExclusiveConfig(
            egress_rule_ids=["example"],
            ingress_rule_ids=["example"],
            security_group_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpc_security_group_rules_exclusive, with egress rule
    ids a set value, ingress rule ids a set value, security group id
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="egress_rule_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="ingress_rule_ids" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="security_group_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
