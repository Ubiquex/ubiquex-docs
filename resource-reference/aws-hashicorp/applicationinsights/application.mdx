---
title: "aws_applicationinsights_application"
description: "Real, generated bindings for `aws_applicationinsights_application`."
---

`aws_applicationinsights_application` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	applicationinsights "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/applicationinsights"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own application"})

    		ubx.Resource(applicationinsights.Application, "ci-runner", applicationinsights.ApplicationConfig{
    			ResourceGroupName:  "example-resource-group-name",
    			AutoConfigEnabled:  true,
    			AutoCreate:         true,
    			CweMonitorEnabled:  true,
    			GroupingType:       "example",
    			OpsCenterEnabled:   true,
    			OpsItemSnsTopicArn: "arn:aws:iam::123456789012:policy/ci",
    			Tags:               map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Application } from "jsr:@ubx/sdk-aws/aws/applicationinsights/application";

    export default stack("payments", () => {
      intent({ summary: "payments own application" });

      resource(Application, "ci-runner", {
        resourceGroupName: "example-resource-group-name",
        autoConfigEnabled: true,
        autoCreate: true,
        cweMonitorEnabled: true,
        groupingType: "example",
        opsCenterEnabled: true,
        opsItemSnsTopicArn: "arn:aws:iam::123456789012:policy/ci",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.applicationinsights import Application, ApplicationConfig

    def describe():
        ubx.intent("payments own application")

        ubx.resource(Application, "ci-runner", ApplicationConfig(
            resource_group_name="example-resource-group-name",
            auto_config_enabled=True,
            auto_create=True,
            cwe_monitor_enabled=True,
            grouping_type="example",
            ops_center_enabled=True,
            ops_item_sns_topic_arn="arn:aws:iam::123456789012:policy/ci",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_applicationinsights_application, with resource group
    name example-resource-group-name, auto config enabled true, auto
    create true, cwe monitor enabled true, grouping type example, ops
    center enabled true, ops item sns topic arn
    arn:aws:iam::123456789012:policy/ci, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_config_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="auto_create" type="bool">
  Optional.
</ResponseField>

<ResponseField name="cwe_monitor_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="grouping_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ops_center_enabled" type="bool">
  Optional.
</ResponseField>

<ResponseField name="ops_item_sns_topic_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
