---
title: "aws_appautoscaling_scheduled_action"
description: "Real, generated bindings for `aws_appautoscaling_scheduled_action`."
---

`aws_appautoscaling_scheduled_action` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appautoscaling "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appautoscaling"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own scheduled action"})

    		ubx.Resource(appautoscaling.ScheduledAction, "ci-runner", appautoscaling.ScheduledActionConfig{
    			Name:              "ci-runner",
    			ResourceId:        "example",
    			ScalableDimension: "example",
    			Schedule:          "example",
    			ServiceNamespace:  "example",
    			EndTime:           "example",
    			StartTime:         "example",
    			Timezone:          "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ScheduledAction } from "jsr:@ubx/sdk-aws/aws/appautoscaling/scheduled_action";

    export default stack("payments", () => {
      intent({ summary: "payments own scheduled action" });

      resource(ScheduledAction, "ci-runner", {
        name: "ci-runner",
        resourceId: "example",
        scalableDimension: "example",
        schedule: "example",
        serviceNamespace: "example",
        endTime: "example",
        startTime: "example",
        timezone: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appautoscaling import ScheduledAction, ScheduledActionConfig

    def describe():
        ubx.intent("payments own scheduled action")

        ubx.resource(ScheduledAction, "ci-runner", ScheduledActionConfig(
            name="ci-runner",
            resource_id="example",
            scalable_dimension="example",
            schedule="example",
            service_namespace="example",
            end_time="example",
            start_time="example",
            timezone="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appautoscaling_scheduled_action called "ci-runner",
    with resource id example, scalable dimension example, schedule
    example, service namespace example, end time example, start time
    example, timezone example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="end_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scalable_dimension" type="string" required>
  Required.
</ResponseField>

<ResponseField name="scalable_target_action" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="max_capacity" type="string">
    </ResponseField>
    <ResponseField name="min_capacity" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="schedule" type="string" required>
  Required.
</ResponseField>

<ResponseField name="service_namespace" type="string" required>
  Required.
</ResponseField>

<ResponseField name="start_time" type="string">
  Optional.
</ResponseField>

<ResponseField name="timezone" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="scalable_target_action" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="max_capacity" type="string">
    </ResponseField>
    <ResponseField name="min_capacity" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
