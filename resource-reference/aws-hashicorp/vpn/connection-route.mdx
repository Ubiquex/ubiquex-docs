---
title: "aws_vpn_connection_route"
description: "Real, generated bindings for `aws_vpn_connection_route`."
---

`aws_vpn_connection_route` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpn "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own connection route"})

    		ubx.Resource(vpn.ConnectionRoute, "ci-runner", vpn.ConnectionRouteConfig{
    			DestinationCidrBlock: "example",
    			VpnConnectionId:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ConnectionRoute } from "jsr:@ubx/sdk-aws/aws/vpn/connection_route";

    export default stack("payments", () => {
      intent({ summary: "payments own connection route" });

      resource(ConnectionRoute, "ci-runner", {
        destinationCidrBlock: "example",
        vpnConnectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpn import ConnectionRoute, ConnectionRouteConfig

    def describe():
        ubx.intent("payments own connection route")

        ubx.resource(ConnectionRoute, "ci-runner", ConnectionRouteConfig(
            destination_cidr_block="example",
            vpn_connection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpn_connection_route, with destination cidr block
    example, vpn connection id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="destination_cidr_block" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="vpn_connection_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
