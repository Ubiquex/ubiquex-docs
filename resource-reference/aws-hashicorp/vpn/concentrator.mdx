---
title: "aws_vpn_concentrator"
description: "Real, generated bindings for `aws_vpn_concentrator`."
---

`aws_vpn_concentrator` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	vpn "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/vpn"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own concentrator"})

    		ubx.Resource(vpn.Concentrator, "ci-runner", vpn.ConcentratorConfig{
    			TransitGatewayId: "example",
    			Type:             "example",
    			Tags:             map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Concentrator } from "jsr:@ubx/sdk-aws/aws/vpn/concentrator";

    export default stack("payments", () => {
      intent({ summary: "payments own concentrator" });

      resource(Concentrator, "ci-runner", {
        transitGatewayId: "example",
        type: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.vpn import Concentrator, ConcentratorConfig

    def describe():
        ubx.intent("payments own concentrator")

        ubx.resource(Concentrator, "ci-runner", ConcentratorConfig(
            transit_gateway_id="example",
            type="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_vpn_concentrator, with transit gateway id example,
    type example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="transit_gateway_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="transit_gateway_attachment_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="vpn_concentrator_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
