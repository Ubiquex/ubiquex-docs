---
title: "aws_quicksight_folder_membership"
description: "Real, generated bindings for `aws_quicksight_folder_membership`."
---

`aws_quicksight_folder_membership` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	quicksight "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/quicksight"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own folder membership"})

    		ubx.Resource(quicksight.FolderMembership, "ci-runner", quicksight.FolderMembershipConfig{
    			FolderId:   "example",
    			MemberId:   "example",
    			MemberType: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FolderMembership } from "jsr:@ubx/sdk-aws/aws/quicksight/folder_membership";

    export default stack("payments", () => {
      intent({ summary: "payments own folder membership" });

      resource(FolderMembership, "ci-runner", {
        folderId: "example",
        memberId: "example",
        memberType: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.quicksight import FolderMembership, FolderMembershipConfig

    def describe():
        ubx.intent("payments own folder membership")

        ubx.resource(FolderMembership, "ci-runner", FolderMembershipConfig(
            folder_id="example",
            member_id="example",
            member_type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_quicksight_folder_membership, with folder id example,
    member id example, member type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="folder_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="member_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="aws_account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
