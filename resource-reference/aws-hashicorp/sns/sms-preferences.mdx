---
title: "aws_sns_sms_preferences"
description: "Real, generated bindings for `aws_sns_sms_preferences`."
---

`aws_sns_sms_preferences` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sns "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sns"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own sms preferences"})

    		ubx.Resource(sns.SmsPreferences, "ci-runner", sns.SmsPreferencesConfig{
    			DefaultSenderId:                   "example",
    			DefaultSmsType:                    "example",
    			DeliveryStatusIamRoleArn:          "arn:aws:iam::123456789012:policy/ci",
    			DeliveryStatusSuccessSamplingRate: "example",
    			UsageReportS3Bucket:               "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SmsPreferences } from "jsr:@ubx/sdk-aws/aws/sns/sms_preferences";

    export default stack("payments", () => {
      intent({ summary: "payments own sms preferences" });

      resource(SmsPreferences, "ci-runner", {
        defaultSenderId: "example",
        defaultSmsType: "example",
        deliveryStatusIamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        deliveryStatusSuccessSamplingRate: "example",
        usageReportS3Bucket: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sns import SmsPreferences, SmsPreferencesConfig

    def describe():
        ubx.intent("payments own sms preferences")

        ubx.resource(SmsPreferences, "ci-runner", SmsPreferencesConfig(
            default_sender_id="example",
            default_sms_type="example",
            delivery_status_iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            delivery_status_success_sampling_rate="example",
            usage_report_s3_bucket="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sns_sms_preferences, with default sender id example,
    default sms type example, delivery status iam role arn
    arn:aws:iam::123456789012:policy/ci, delivery status success
    sampling rate example, usage report s3 bucket example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="default_sender_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="default_sms_type" type="string">
  Optional.
</ResponseField>

<ResponseField name="delivery_status_iam_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="delivery_status_success_sampling_rate" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="monthly_spend_limit" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="usage_report_s3_bucket" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="monthly_spend_limit" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
