---
title: "aws_dynamodb_kinesis_streaming_destination"
description: "Real, generated bindings for `aws_dynamodb_kinesis_streaming_destination`."
---

`aws_dynamodb_kinesis_streaming_destination` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dynamodb "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dynamodb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own kinesis streaming destination"})

    		ubx.Resource(dynamodb.KinesisStreamingDestination, "ci-runner", dynamodb.KinesisStreamingDestinationConfig{
    			StreamArn: "arn:aws:iam::123456789012:policy/ci",
    			TableName: "example-table-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KinesisStreamingDestination } from "jsr:@ubx/sdk-aws/aws/dynamodb/kinesis_streaming_destination";

    export default stack("payments", () => {
      intent({ summary: "payments own kinesis streaming destination" });

      resource(KinesisStreamingDestination, "ci-runner", {
        streamArn: "arn:aws:iam::123456789012:policy/ci",
        tableName: "example-table-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dynamodb import KinesisStreamingDestination, KinesisStreamingDestinationConfig

    def describe():
        ubx.intent("payments own kinesis streaming destination")

        ubx.resource(KinesisStreamingDestination, "ci-runner", KinesisStreamingDestinationConfig(
            stream_arn="arn:aws:iam::123456789012:policy/ci",
            table_name="example-table-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dynamodb_kinesis_streaming_destination, with stream
    arn arn:aws:iam::123456789012:policy/ci, table name
    example-table-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="approximate_creation_date_time_precision" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stream_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="table_name" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="approximate_creation_date_time_precision" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
