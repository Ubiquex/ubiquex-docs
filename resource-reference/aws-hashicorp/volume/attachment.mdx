---
title: "aws_volume_attachment"
description: "Real, generated bindings for `aws_volume_attachment`."
---

`aws_volume_attachment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	volume "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/volume"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own attachment"})

    		ubx.Resource(volume.Attachment, "ci-runner", volume.AttachmentConfig{
    			DeviceName:                  "example-device-name",
    			InstanceId:                  "example",
    			VolumeId:                    "example",
    			ForceDetach:                 true,
    			SkipDestroy:                 true,
    			StopInstanceBeforeDetaching: true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Attachment } from "jsr:@ubx/sdk-aws/aws/volume/attachment";

    export default stack("payments", () => {
      intent({ summary: "payments own attachment" });

      resource(Attachment, "ci-runner", {
        deviceName: "example-device-name",
        instanceId: "example",
        volumeId: "example",
        forceDetach: true,
        skipDestroy: true,
        stopInstanceBeforeDetaching: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.volume import Attachment, AttachmentConfig

    def describe():
        ubx.intent("payments own attachment")

        ubx.resource(Attachment, "ci-runner", AttachmentConfig(
            device_name="example-device-name",
            instance_id="example",
            volume_id="example",
            force_detach=True,
            skip_destroy=True,
            stop_instance_before_detaching=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_volume_attachment, with device name
    example-device-name, instance id example, volume id example,
    force detach true, skip destroy true, stop instance before
    detaching true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="device_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="force_detach" type="bool">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="instance_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="stop_instance_before_detaching" type="bool">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="volume_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
