---
title: "aws_workmail_user"
description: "Real, generated bindings for `aws_workmail_user`."
---

`aws_workmail_user` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	workmail "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/workmail"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own user"})

    		ubx.Resource(workmail.User, "ci-runner", workmail.UserConfig{
    			DisplayName:    "example-display-name",
    			Email:          "example",
    			Name:           "ci-runner",
    			OrganizationId: "example",
    			City:           "example",
    			Company:        "example",
    			Country:        "example",
    			Department:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { User } from "jsr:@ubx/sdk-aws/aws/workmail/user";

    export default stack("payments", () => {
      intent({ summary: "payments own user" });

      resource(User, "ci-runner", {
        displayName: "example-display-name",
        email: "example",
        name: "ci-runner",
        organizationId: "example",
        city: "example",
        company: "example",
        country: "example",
        department: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.workmail import User, UserConfig

    def describe():
        ubx.intent("payments own user")

        ubx.resource(User, "ci-runner", UserConfig(
            display_name="example-display-name",
            email="example",
            name="ci-runner",
            organization_id="example",
            city="example",
            company="example",
            country="example",
            department="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_workmail_user called "ci-runner", with display name
    example-display-name, email example, organization id example,
    city example, company example, country example, department
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="city" type="string">
  City where the user is located. Optional.
</ResponseField>

<ResponseField name="company" type="string">
  Company associated with the user. Optional.
</ResponseField>

<ResponseField name="country" type="string">
  Country where the user is located. Optional.
</ResponseField>

<ResponseField name="department" type="string">
  Department associated with the user. Optional.
</ResponseField>

<ResponseField name="display_name" type="string" required>
  Display name of the user. Required.
</ResponseField>

<ResponseField name="email" type="string" required>
  Primary email address used to register the user with WorkMail. Required.
</ResponseField>

<ResponseField name="first_name" type="string">
  First name of the user. Optional.
</ResponseField>

<ResponseField name="hidden_from_global_address_list" type="bool">
  Whether to hide the user from the global address list. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_provider_user_id" type="string">
  User ID from IAM Identity Center associated with the user. Optional.
</ResponseField>

<ResponseField name="initials" type="string">
  Initials of the user. Optional.
</ResponseField>

<ResponseField name="job_title" type="string">
  Job title of the user. Optional.
</ResponseField>

<ResponseField name="last_name" type="string">
  Last name of the user. Optional.
</ResponseField>

<ResponseField name="name" type="string" required>
  Username of the user. Required.
</ResponseField>

<ResponseField name="office" type="string">
  Office where the user is located. Optional.
</ResponseField>

<ResponseField name="organization_id" type="string" required>
  Identifier of the WorkMail organization where the user is managed. Required.
</ResponseField>

<ResponseField name="password" type="string">
  Password to set for the user. Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="street" type="string">
  Street address of the user. Optional.
</ResponseField>

<ResponseField name="telephone" type="string">
  Telephone number of the user. Optional.
</ResponseField>

<ResponseField name="user_role" type="string">
  Role assigned to the user. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="zip_code" type="string">
  ZIP or postal code of the user. Optional.
</ResponseField>

## Output properties

<ResponseField name="disabled_date" type="string">
  Timestamp when the user was disabled from WorkMail use. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="enabled_date" type="string">
  Timestamp when the user was enabled for WorkMail use. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="hidden_from_global_address_list" type="bool">
  Whether to hide the user from the global address list. Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="identity_provider_identity_store_id" type="string">
  Identity store ID from IAM Identity Center associated with the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="mailbox_deprovisioned_date" type="string">
  Timestamp when the mailbox was removed for the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="mailbox_provisioned_date" type="string">
  Timestamp when the mailbox was created for the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="state" type="string">
  Current WorkMail state of the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_id" type="string">
  Identifier of the user. Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="user_role" type="string">
  Role assigned to the user. Optional; if omitted, computed by AWS.
</ResponseField>
