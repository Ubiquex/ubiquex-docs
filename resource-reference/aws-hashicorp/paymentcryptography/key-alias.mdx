---
title: "aws_paymentcryptography_key_alias"
description: "Real, generated bindings for `aws_paymentcryptography_key_alias`."
---

`aws_paymentcryptography_key_alias` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	paymentcryptography "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/paymentcryptography"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own key alias"})

    		ubx.Resource(paymentcryptography.KeyAlias, "ci-runner", paymentcryptography.KeyAliasConfig{
    			AliasName: "example-alias-name",
    			KeyArn:    "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { KeyAlias } from "jsr:@ubx/sdk-aws/aws/paymentcryptography/key_alias";

    export default stack("payments", () => {
      intent({ summary: "payments own key alias" });

      resource(KeyAlias, "ci-runner", {
        aliasName: "example-alias-name",
        keyArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.paymentcryptography import KeyAlias, KeyAliasConfig

    def describe():
        ubx.intent("payments own key alias")

        ubx.resource(KeyAlias, "ci-runner", KeyAliasConfig(
            alias_name="example-alias-name",
            key_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_paymentcryptography_key_alias, with alias name
    example-alias-name, key arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="alias_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="key_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
