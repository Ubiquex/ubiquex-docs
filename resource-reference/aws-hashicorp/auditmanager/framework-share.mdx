---
title: "aws_auditmanager_framework_share"
description: "Real, generated bindings for `aws_auditmanager_framework_share`."
---

`aws_auditmanager_framework_share` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own framework share"})

    		ubx.Resource(auditmanager.FrameworkShare, "ci-runner", auditmanager.FrameworkShareConfig{
    			DestinationAccount: "example",
    			DestinationRegion:  "example",
    			FrameworkId:        "example",
    			Comment:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FrameworkShare } from "jsr:@ubx/sdk-aws/aws/auditmanager/framework_share";

    export default stack("payments", () => {
      intent({ summary: "payments own framework share" });

      resource(FrameworkShare, "ci-runner", {
        destinationAccount: "example",
        destinationRegion: "example",
        frameworkId: "example",
        comment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.auditmanager import FrameworkShare, FrameworkShareConfig

    def describe():
        ubx.intent("payments own framework share")

        ubx.resource(FrameworkShare, "ci-runner", FrameworkShareConfig(
            destination_account="example",
            destination_region="example",
            framework_id="example",
            comment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_auditmanager_framework_share, with destination account
    example, destination region example, framework id example,
    comment example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="destination_account" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="framework_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
