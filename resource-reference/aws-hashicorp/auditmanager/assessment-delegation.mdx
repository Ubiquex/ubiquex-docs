---
title: "aws_auditmanager_assessment_delegation"
description: "Real, generated bindings for `aws_auditmanager_assessment_delegation`."
---

`aws_auditmanager_assessment_delegation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own assessment delegation"})

    		ubx.Resource(auditmanager.AssessmentDelegation, "ci-runner", auditmanager.AssessmentDelegationConfig{
    			AssessmentId: "example",
    			ControlSetId: "example",
    			RoleArn:      "arn:aws:iam::123456789012:policy/ci",
    			RoleType:     "example",
    			Comment:      "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AssessmentDelegation } from "jsr:@ubx/sdk-aws/aws/auditmanager/assessment_delegation";

    export default stack("payments", () => {
      intent({ summary: "payments own assessment delegation" });

      resource(AssessmentDelegation, "ci-runner", {
        assessmentId: "example",
        controlSetId: "example",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        roleType: "example",
        comment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.auditmanager import AssessmentDelegation, AssessmentDelegationConfig

    def describe():
        ubx.intent("payments own assessment delegation")

        ubx.resource(AssessmentDelegation, "ci-runner", AssessmentDelegationConfig(
            assessment_id="example",
            control_set_id="example",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            role_type="example",
            comment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_auditmanager_assessment_delegation, with assessment id
    example, control set id example, role arn
    arn:aws:iam::123456789012:policy/ci, role type example, comment
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="assessment_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="control_set_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="role_type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="delegation_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
