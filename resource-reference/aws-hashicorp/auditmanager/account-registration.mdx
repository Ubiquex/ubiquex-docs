---
title: "aws_auditmanager_account_registration"
description: "Real, generated bindings for `aws_auditmanager_account_registration`."
---

`aws_auditmanager_account_registration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	auditmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/auditmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own account registration"})

    		ubx.Resource(auditmanager.AccountRegistration, "ci-runner", auditmanager.AccountRegistrationConfig{
    			DelegatedAdminAccount: "example",
    			DeregisterOnDestroy:   true,
    			KmsKey:                "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccountRegistration } from "jsr:@ubx/sdk-aws/aws/auditmanager/account_registration";

    export default stack("payments", () => {
      intent({ summary: "payments own account registration" });

      resource(AccountRegistration, "ci-runner", {
        delegatedAdminAccount: "example",
        deregisterOnDestroy: true,
        kmsKey: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.auditmanager import AccountRegistration, AccountRegistrationConfig

    def describe():
        ubx.intent("payments own account registration")

        ubx.resource(AccountRegistration, "ci-runner", AccountRegistrationConfig(
            delegated_admin_account="example",
            deregister_on_destroy=True,
            kms_key="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_auditmanager_account_registration, with delegated
    admin account example, deregister on destroy true, kms key
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="delegated_admin_account" type="string">
  Optional.
</ResponseField>

<ResponseField name="deregister_on_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="kms_key" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>
