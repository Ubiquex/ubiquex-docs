---
title: "aws_securityhub_standards_control"
description: "Real, generated bindings for `aws_securityhub_standards_control`."
---

`aws_securityhub_standards_control` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	securityhub "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/securityhub"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own standards control"})

    		ubx.Resource(securityhub.StandardsControl, "ci-runner", securityhub.StandardsControlConfig{
    			ControlStatus:       "example",
    			StandardsControlArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { StandardsControl } from "jsr:@ubx/sdk-aws/aws/securityhub/standards_control";

    export default stack("payments", () => {
      intent({ summary: "payments own standards control" });

      resource(StandardsControl, "ci-runner", {
        controlStatus: "example",
        standardsControlArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.securityhub import StandardsControl, StandardsControlConfig

    def describe():
        ubx.intent("payments own standards control")

        ubx.resource(StandardsControl, "ci-runner", StandardsControlConfig(
            control_status="example",
            standards_control_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_securityhub_standards_control, with control status
    example, standards control arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="control_status" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disabled_reason" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="standards_control_arn" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="control_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="control_status_updated_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="description" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="disabled_reason" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="related_requirements" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="remediation_url" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="severity_rating" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="title" type="string">
  Computed by AWS after `ship`.
</ResponseField>
