---
title: "aws_organizations_policy"
description: "Real, generated bindings for `aws_organizations_policy`."
---

`aws_organizations_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	organizations "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/organizations"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own policy"})

    		ubx.Resource(organizations.Policy, "ci-runner", organizations.PolicyConfig{
    			Content:     "example",
    			Name:        "ci-runner",
    			Description: "Managed by ubx.",
    			SkipDestroy: true,
    			Tags:        map[string]string{"managed-by": "ubx"},
    			Type:        "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Policy } from "jsr:@ubx/sdk-aws/aws/organizations/policy";

    export default stack("payments", () => {
      intent({ summary: "payments own policy" });

      resource(Policy, "ci-runner", {
        content: "example",
        name: "ci-runner",
        description: "Managed by ubx.",
        skipDestroy: true,
        tags: { "managed-by": "ubx" },
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.organizations import Policy, PolicyConfig

    def describe():
        ubx.intent("payments own policy")

        ubx.resource(Policy, "ci-runner", PolicyConfig(
            content="example",
            name="ci-runner",
            description="Managed by ubx.",
            skip_destroy=True,
            tags={"managed-by": "ubx"},
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_organizations_policy called "ci-runner", with content
    example, description Managed by ubx., skip destroy true, tags a
    set value, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="type" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
