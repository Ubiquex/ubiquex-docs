---
title: "aws_fis_target_account_configuration"
description: "Real, generated bindings for `aws_fis_target_account_configuration`."
---

`aws_fis_target_account_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	fis "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/fis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own target account configuration"})

    		ubx.Resource(fis.TargetAccountConfiguration, "ci-runner", fis.TargetAccountConfigurationConfig{
    			AccountId:            "example",
    			ExperimentTemplateId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { TargetAccountConfiguration } from "jsr:@ubx/sdk-aws/aws/fis/target_account_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own target account configuration" });

      resource(TargetAccountConfiguration, "ci-runner", {
        accountId: "example",
        experimentTemplateId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.fis import TargetAccountConfiguration, TargetAccountConfigurationConfig

    def describe():
        ubx.intent("payments own target account configuration")

        ubx.resource(TargetAccountConfiguration, "ci-runner", TargetAccountConfigurationConfig(
            account_id="example",
            experiment_template_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_fis_target_account_configuration, with account id
    example, experiment template id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="experiment_template_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="description" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
