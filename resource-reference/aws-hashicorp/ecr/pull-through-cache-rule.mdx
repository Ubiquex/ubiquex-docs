---
title: "aws_ecr_pull_through_cache_rule"
description: "Real, generated bindings for `aws_ecr_pull_through_cache_rule`."
---

`aws_ecr_pull_through_cache_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecr "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecr"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own pull through cache rule"})

    		ubx.Resource(ecr.PullThroughCacheRule, "ci-runner", ecr.PullThroughCacheRuleConfig{
    			EcrRepositoryPrefix:      "example",
    			UpstreamRegistryUrl:      "example",
    			CredentialArn:            "arn:aws:iam::123456789012:policy/ci",
    			CustomRoleArn:            "arn:aws:iam::123456789012:policy/ci",
    			UpstreamRepositoryPrefix: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { PullThroughCacheRule } from "jsr:@ubx/sdk-aws/aws/ecr/pull_through_cache_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own pull through cache rule" });

      resource(PullThroughCacheRule, "ci-runner", {
        ecrRepositoryPrefix: "example",
        upstreamRegistryUrl: "example",
        credentialArn: "arn:aws:iam::123456789012:policy/ci",
        customRoleArn: "arn:aws:iam::123456789012:policy/ci",
        upstreamRepositoryPrefix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecr import PullThroughCacheRule, PullThroughCacheRuleConfig

    def describe():
        ubx.intent("payments own pull through cache rule")

        ubx.resource(PullThroughCacheRule, "ci-runner", PullThroughCacheRuleConfig(
            ecr_repository_prefix="example",
            upstream_registry_url="example",
            credential_arn="arn:aws:iam::123456789012:policy/ci",
            custom_role_arn="arn:aws:iam::123456789012:policy/ci",
            upstream_repository_prefix="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecr_pull_through_cache_rule, with ecr repository
    prefix example, upstream registry url example, credential arn
    arn:aws:iam::123456789012:policy/ci, custom role arn
    arn:aws:iam::123456789012:policy/ci, upstream repository prefix
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="credential_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="custom_role_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="ecr_repository_prefix" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="upstream_registry_url" type="string" required>
  Required.
</ResponseField>

<ResponseField name="upstream_repository_prefix" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="registry_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
