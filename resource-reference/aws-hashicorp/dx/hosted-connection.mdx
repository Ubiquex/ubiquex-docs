---
title: "aws_dx_hosted_connection"
description: "Real, generated bindings for `aws_dx_hosted_connection`."
---

`aws_dx_hosted_connection` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	dx "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/dx"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hosted connection"})

    		ubx.Resource(dx.HostedConnection, "ci-runner", dx.HostedConnectionConfig{
    			Bandwidth:      "example",
    			ConnectionId:   "example",
    			Name:           "ci-runner",
    			OwnerAccountId: "example",
    			Vlan:           1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HostedConnection } from "jsr:@ubx/sdk-aws/aws/dx/hosted_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own hosted connection" });

      resource(HostedConnection, "ci-runner", {
        bandwidth: "example",
        connectionId: "example",
        name: "ci-runner",
        ownerAccountId: "example",
        vlan: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.dx import HostedConnection, HostedConnectionConfig

    def describe():
        ubx.intent("payments own hosted connection")

        ubx.resource(HostedConnection, "ci-runner", HostedConnectionConfig(
            bandwidth="example",
            connection_id="example",
            name="ci-runner",
            owner_account_id="example",
            vlan=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_dx_hosted_connection called "ci-runner", with
    bandwidth example, connection id example, owner account id
    example, vlan 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="bandwidth" type="string" required>
  Required.
</ResponseField>

<ResponseField name="connection_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="owner_account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="vlan" type="number" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="aws_device" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="connection_region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="has_logical_redundancy" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="jumbo_frame_capable" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="lag_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="loa_issue_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="location" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="partner_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provider_name" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="state" type="string">
  Computed by AWS after `ship`.
</ResponseField>
