---
title: "aws_redshiftserverless_usage_limit"
description: "Real, generated bindings for `aws_redshiftserverless_usage_limit`."
---

`aws_redshiftserverless_usage_limit` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshiftserverless "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshiftserverless"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own usage limit"})

    		ubx.Resource(redshiftserverless.UsageLimit, "ci-runner", redshiftserverless.UsageLimitConfig{
    			Amount:       1,
    			ResourceArn:  "arn:aws:iam::123456789012:policy/ci",
    			UsageType:    "example",
    			BreachAction: "example",
    			Period:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { UsageLimit } from "jsr:@ubx/sdk-aws/aws/redshiftserverless/usage_limit";

    export default stack("payments", () => {
      intent({ summary: "payments own usage limit" });

      resource(UsageLimit, "ci-runner", {
        amount: 1,
        resourceArn: "arn:aws:iam::123456789012:policy/ci",
        usageType: "example",
        breachAction: "example",
        period: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshiftserverless import UsageLimit, UsageLimitConfig

    def describe():
        ubx.intent("payments own usage limit")

        ubx.resource(UsageLimit, "ci-runner", UsageLimitConfig(
            amount=1,
            resource_arn="arn:aws:iam::123456789012:policy/ci",
            usage_type="example",
            breach_action="example",
            period="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshiftserverless_usage_limit, with amount 1,
    resource arn arn:aws:iam::123456789012:policy/ci, usage type
    example, breach action example, period example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="amount" type="number" required>
  Required.
</ResponseField>

<ResponseField name="breach_action" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="period" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="usage_type" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
