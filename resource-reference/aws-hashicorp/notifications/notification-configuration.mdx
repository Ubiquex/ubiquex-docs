---
title: "aws_notifications_notification_configuration"
description: "Real, generated bindings for `aws_notifications_notification_configuration`."
---

`aws_notifications_notification_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/notifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own notification configuration"})

    		ubx.Resource(notifications.NotificationConfiguration, "ci-runner", notifications.NotificationConfigurationConfig{
    			Description: "Managed by ubx.",
    			Name:        "ci-runner",
    			Tags:        map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { NotificationConfiguration } from "jsr:@ubx/sdk-aws/aws/notifications/notification_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own notification configuration" });

      resource(NotificationConfiguration, "ci-runner", {
        description: "Managed by ubx.",
        name: "ci-runner",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.notifications import NotificationConfiguration, NotificationConfigurationConfig

    def describe():
        ubx.intent("payments own notification configuration")

        ubx.resource(NotificationConfiguration, "ci-runner", NotificationConfigurationConfig(
            description="Managed by ubx.",
            name="ci-runner",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_notifications_notification_configuration called
    "ci-runner", with description Managed by ubx., tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="aggregation_duration" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="description" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="aggregation_duration" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
