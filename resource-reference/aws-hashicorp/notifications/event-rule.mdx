---
title: "aws_notifications_event_rule"
description: "Real, generated bindings for `aws_notifications_event_rule`."
---

`aws_notifications_event_rule` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	notifications "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/notifications"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own event rule"})

    		ubx.Resource(notifications.EventRule, "ci-runner", notifications.EventRuleConfig{
    			EventType:                    "example",
    			NotificationConfigurationArn: "arn:aws:iam::123456789012:policy/ci",
    			Regions:                      []string{"example"},
    			Source:                       "example",
    			EventPattern:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EventRule } from "jsr:@ubx/sdk-aws/aws/notifications/event_rule";

    export default stack("payments", () => {
      intent({ summary: "payments own event rule" });

      resource(EventRule, "ci-runner", {
        eventType: "example",
        notificationConfigurationArn: "arn:aws:iam::123456789012:policy/ci",
        regions: ["example"],
        source: "example",
        eventPattern: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.notifications import EventRule, EventRuleConfig

    def describe():
        ubx.intent("payments own event rule")

        ubx.resource(EventRule, "ci-runner", EventRuleConfig(
            event_type="example",
            notification_configuration_arn="arn:aws:iam::123456789012:policy/ci",
            regions=["example"],
            source="example",
            event_pattern="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_notifications_event_rule, with event type example,
    notification configuration arn
    arn:aws:iam::123456789012:policy/ci, regions a set value, source
    example, event pattern example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="event_pattern" type="string">
  Optional.
</ResponseField>

<ResponseField name="event_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="notification_configuration_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="regions" type="set(string)" required>
  Required.
</ResponseField>

<ResponseField name="source" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>
