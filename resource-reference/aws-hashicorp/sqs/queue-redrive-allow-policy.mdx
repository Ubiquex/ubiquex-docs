---
title: "aws_sqs_queue_redrive_allow_policy"
description: "Real, generated bindings for `aws_sqs_queue_redrive_allow_policy`."
---

`aws_sqs_queue_redrive_allow_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	sqs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/sqs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own queue redrive allow policy"})

    		ubx.Resource(sqs.QueueRedriveAllowPolicy, "ci-runner", sqs.QueueRedriveAllowPolicyConfig{
    			QueueUrl:           "example",
    			RedriveAllowPolicy: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { QueueRedriveAllowPolicy } from "jsr:@ubx/sdk-aws/aws/sqs/queue_redrive_allow_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own queue redrive allow policy" });

      resource(QueueRedriveAllowPolicy, "ci-runner", {
        queueUrl: "example",
        redriveAllowPolicy: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.sqs import QueueRedriveAllowPolicy, QueueRedriveAllowPolicyConfig

    def describe():
        ubx.intent("payments own queue redrive allow policy")

        ubx.resource(QueueRedriveAllowPolicy, "ci-runner", QueueRedriveAllowPolicyConfig(
            queue_url="example",
            redrive_allow_policy="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_sqs_queue_redrive_allow_policy, with queue url
    example, redrive allow policy example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="queue_url" type="string" required>
  Required.
</ResponseField>

<ResponseField name="redrive_allow_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
