---
title: "aws_secretsmanager_secret_rotation"
description: "Real, generated bindings for `aws_secretsmanager_secret_rotation`."
---

`aws_secretsmanager_secret_rotation` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	secretsmanager "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/secretsmanager"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own secret rotation"})

    		ubx.Resource(secretsmanager.SecretRotation, "ci-runner", secretsmanager.SecretRotationConfig{
    			SecretId:          "example",
    			RotateImmediately: true,
    			RotationLambdaArn: "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SecretRotation } from "jsr:@ubx/sdk-aws/aws/secretsmanager/secret_rotation";

    export default stack("payments", () => {
      intent({ summary: "payments own secret rotation" });

      resource(SecretRotation, "ci-runner", {
        secretId: "example",
        rotateImmediately: true,
        rotationLambdaArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.secretsmanager import SecretRotation, SecretRotationConfig

    def describe():
        ubx.intent("payments own secret rotation")

        ubx.resource(SecretRotation, "ci-runner", SecretRotationConfig(
            secret_id="example",
            rotate_immediately=True,
            rotation_lambda_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_secretsmanager_secret_rotation, with secret id
    example, rotate immediately true, rotation lambda arn
    arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rotate_immediately" type="bool">
  Optional.
</ResponseField>

<ResponseField name="rotation_lambda_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="rotation_rules" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="automatically_after_days" type="number">
    </ResponseField>
    <ResponseField name="duration" type="string">
    </ResponseField>
    <ResponseField name="schedule_expression" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="secret_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="rotation_enabled" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="rotation_rules" type="list(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="automatically_after_days" type="number">
    </ResponseField>
    <ResponseField name="duration" type="string">
    </ResponseField>
    <ResponseField name="schedule_expression" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
