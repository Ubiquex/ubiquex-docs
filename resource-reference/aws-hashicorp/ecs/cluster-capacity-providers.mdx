---
title: "aws_ecs_cluster_capacity_providers"
description: "Real, generated bindings for `aws_ecs_cluster_capacity_providers`."
---

`aws_ecs_cluster_capacity_providers` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ecs "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/ecs"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own cluster capacity providers"})

    		ubx.Resource(ecs.ClusterCapacityProviders, "ci-runner", ecs.ClusterCapacityProvidersConfig{
    			ClusterName:       "example-cluster-name",
    			CapacityProviders: []string{"example"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ClusterCapacityProviders } from "jsr:@ubx/sdk-aws/aws/ecs/cluster_capacity_providers";

    export default stack("payments", () => {
      intent({ summary: "payments own cluster capacity providers" });

      resource(ClusterCapacityProviders, "ci-runner", {
        clusterName: "example-cluster-name",
        capacityProviders: ["example"],
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.ecs import ClusterCapacityProviders, ClusterCapacityProvidersConfig

    def describe():
        ubx.intent("payments own cluster capacity providers")

        ubx.resource(ClusterCapacityProviders, "ci-runner", ClusterCapacityProvidersConfig(
            cluster_name="example-cluster-name",
            capacity_providers=["example"],
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_ecs_cluster_capacity_providers, with cluster name
    example-cluster-name, capacity providers a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="capacity_providers" type="set(string)">
  Optional.
</ResponseField>

<ResponseField name="cluster_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="default_capacity_provider_strategy" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="base" type="number">
    </ResponseField>
    <ResponseField name="capacity_provider" type="string" required>
    </ResponseField>
    <ResponseField name="weight" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="default_capacity_provider_strategy" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="base" type="number">
    </ResponseField>
    <ResponseField name="capacity_provider" type="string" required>
    </ResponseField>
    <ResponseField name="weight" type="number">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
