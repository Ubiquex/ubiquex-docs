---
title: "aws_bedrock_evaluation_job"
description: "Real, generated bindings for `aws_bedrock_evaluation_job`."
---

`aws_bedrock_evaluation_job` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own evaluation job"})

    		ubx.Resource(bedrock.EvaluationJob, "ci-runner", bedrock.EvaluationJobConfig{
    			JobName:                 "example-job-name",
    			RoleArn:                 "arn:aws:iam::123456789012:policy/ci",
    			CustomerEncryptionKeyId: "example",
    			JobDescription:          "example",
    			SkipDestroy:             true,
    			Tags:                    map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { EvaluationJob } from "jsr:@ubx/sdk-aws/aws/bedrock/evaluation_job";

    export default stack("payments", () => {
      intent({ summary: "payments own evaluation job" });

      resource(EvaluationJob, "ci-runner", {
        jobName: "example-job-name",
        roleArn: "arn:aws:iam::123456789012:policy/ci",
        customerEncryptionKeyId: "example",
        jobDescription: "example",
        skipDestroy: true,
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import EvaluationJob, EvaluationJobConfig

    def describe():
        ubx.intent("payments own evaluation job")

        ubx.resource(EvaluationJob, "ci-runner", EvaluationJobConfig(
            job_name="example-job-name",
            role_arn="arn:aws:iam::123456789012:policy/ci",
            customer_encryption_key_id="example",
            job_description="example",
            skip_destroy=True,
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_evaluation_job, with job name
    example-job-name, role arn arn:aws:iam::123456789012:policy/ci,
    customer encryption key id example, job description example, skip
    destroy true, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties


<ResponseField name="application_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="customer_encryption_key_id" type="string">
  Optional.
</ResponseField>

<ResponseField name="job_description" type="string">
  Optional.
</ResponseField>

<ResponseField name="job_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="skip_destroy" type="bool">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties


<ResponseField name="application_type" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="created_at" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="failure_messages" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="job_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="job_type" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="last_modified_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
