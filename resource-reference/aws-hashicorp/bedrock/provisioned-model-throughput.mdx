---
title: "aws_bedrock_provisioned_model_throughput"
description: "Real, generated bindings for `aws_bedrock_provisioned_model_throughput`."
---

`aws_bedrock_provisioned_model_throughput` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	bedrock "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/bedrock"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own provisioned model throughput"})

    		ubx.Resource(bedrock.ProvisionedModelThroughput, "ci-runner", bedrock.ProvisionedModelThroughputConfig{
    			ModelArn:             "arn:aws:iam::123456789012:policy/ci",
    			ModelUnits:           1,
    			ProvisionedModelName: "example-provisioned-model-name",
    			CommitmentDuration:   "example",
    			Tags:                 map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { ProvisionedModelThroughput } from "jsr:@ubx/sdk-aws/aws/bedrock/provisioned_model_throughput";

    export default stack("payments", () => {
      intent({ summary: "payments own provisioned model throughput" });

      resource(ProvisionedModelThroughput, "ci-runner", {
        modelArn: "arn:aws:iam::123456789012:policy/ci",
        modelUnits: 1,
        provisionedModelName: "example-provisioned-model-name",
        commitmentDuration: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.bedrock import ProvisionedModelThroughput, ProvisionedModelThroughputConfig

    def describe():
        ubx.intent("payments own provisioned model throughput")

        ubx.resource(ProvisionedModelThroughput, "ci-runner", ProvisionedModelThroughputConfig(
            model_arn="arn:aws:iam::123456789012:policy/ci",
            model_units=1,
            provisioned_model_name="example-provisioned-model-name",
            commitment_duration="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_bedrock_provisioned_model_throughput, with model arn
    arn:aws:iam::123456789012:policy/ci, model units 1, provisioned
    model name example-provisioned-model-name, commitment duration
    example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="commitment_duration" type="string">
  Optional.
</ResponseField>

<ResponseField name="model_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="model_units" type="number" required>
  Required.
</ResponseField>

<ResponseField name="provisioned_model_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="provisioned_model_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
