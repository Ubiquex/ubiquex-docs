---
title: "aws_appfabric_ingestion"
description: "Real, generated bindings for `aws_appfabric_ingestion`."
---

`aws_appfabric_ingestion` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	appfabric "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/appfabric"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own ingestion"})

    		ubx.Resource(appfabric.Ingestion, "ci-runner", appfabric.IngestionConfig{
    			App:           "example",
    			AppBundleArn:  "arn:aws:iam::123456789012:policy/ci",
    			IngestionType: "example",
    			TenantId:      "example",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Ingestion } from "jsr:@ubx/sdk-aws/aws/appfabric/ingestion";

    export default stack("payments", () => {
      intent({ summary: "payments own ingestion" });

      resource(Ingestion, "ci-runner", {
        app: "example",
        appBundleArn: "arn:aws:iam::123456789012:policy/ci",
        ingestionType: "example",
        tenantId: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.appfabric import Ingestion, IngestionConfig

    def describe():
        ubx.intent("payments own ingestion")

        ubx.resource(Ingestion, "ci-runner", IngestionConfig(
            app="example",
            app_bundle_arn="arn:aws:iam::123456789012:policy/ci",
            ingestion_type="example",
            tenant_id="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_appfabric_ingestion, with app example, app bundle arn
    arn:aws:iam::123456789012:policy/ci, ingestion type example,
    tenant id example, tags a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app" type="string" required>
  Required.
</ResponseField>

<ResponseField name="app_bundle_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="ingestion_type" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tenant_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
