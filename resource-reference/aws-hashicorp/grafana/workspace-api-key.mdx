---
title: "aws_grafana_workspace_api_key"
description: "Real, generated bindings for `aws_grafana_workspace_api_key`."
---

`aws_grafana_workspace_api_key` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	grafana "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/grafana"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workspace api key"})

    		ubx.Resource(grafana.WorkspaceApiKey, "ci-runner", grafana.WorkspaceApiKeyConfig{
    			KeyName:       "example-key-name",
    			KeyRole:       "example",
    			SecondsToLive: 7200,
    			WorkspaceId:   "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkspaceApiKey } from "jsr:@ubx/sdk-aws/aws/grafana/workspace_api_key";

    export default stack("payments", () => {
      intent({ summary: "payments own workspace api key" });

      resource(WorkspaceApiKey, "ci-runner", {
        keyName: "example-key-name",
        keyRole: "example",
        secondsToLive: 7200,
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.grafana import WorkspaceApiKey, WorkspaceApiKeyConfig

    def describe():
        ubx.intent("payments own workspace api key")

        ubx.resource(WorkspaceApiKey, "ci-runner", WorkspaceApiKeyConfig(
            key_name="example-key-name",
            key_role="example",
            seconds_to_live=7200,
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_grafana_workspace_api_key, with key name
    example-key-name, key role example, seconds to live 7200,
    workspace id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="key_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="seconds_to_live" type="number" required>
  Required.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="key" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
