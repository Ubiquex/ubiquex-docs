---
title: "aws_grafana_workspace_service_account"
description: "Real, generated bindings for `aws_grafana_workspace_service_account`."
---

`aws_grafana_workspace_service_account` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	grafana "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/grafana"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own workspace service account"})

    		ubx.Resource(grafana.WorkspaceServiceAccount, "ci-runner", grafana.WorkspaceServiceAccountConfig{
    			GrafanaRole: "example",
    			Name:        "ci-runner",
    			WorkspaceId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { WorkspaceServiceAccount } from "jsr:@ubx/sdk-aws/aws/grafana/workspace_service_account";

    export default stack("payments", () => {
      intent({ summary: "payments own workspace service account" });

      resource(WorkspaceServiceAccount, "ci-runner", {
        grafanaRole: "example",
        name: "ci-runner",
        workspaceId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.grafana import WorkspaceServiceAccount, WorkspaceServiceAccountConfig

    def describe():
        ubx.intent("payments own workspace service account")

        ubx.resource(WorkspaceServiceAccount, "ci-runner", WorkspaceServiceAccountConfig(
            grafana_role="example",
            name="ci-runner",
            workspace_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_grafana_workspace_service_account called "ci-runner",
    with grafana role example, workspace id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="grafana_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="workspace_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="service_account_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
