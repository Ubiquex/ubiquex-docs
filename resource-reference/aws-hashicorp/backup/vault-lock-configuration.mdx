---
title: "aws_backup_vault_lock_configuration"
description: "Real, generated bindings for `aws_backup_vault_lock_configuration`."
---

`aws_backup_vault_lock_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own vault lock configuration"})

    		ubx.Resource(backup.VaultLockConfiguration, "ci-runner", backup.VaultLockConfigurationConfig{
    			BackupVaultName:   "example-backup-vault-name",
    			ChangeableForDays: 1,
    			MaxRetentionDays:  1,
    			MinRetentionDays:  1,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { VaultLockConfiguration } from "jsr:@ubx/sdk-aws/aws/backup/vault_lock_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own vault lock configuration" });

      resource(VaultLockConfiguration, "ci-runner", {
        backupVaultName: "example-backup-vault-name",
        changeableForDays: 1,
        maxRetentionDays: 1,
        minRetentionDays: 1,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import VaultLockConfiguration, VaultLockConfigurationConfig

    def describe():
        ubx.intent("payments own vault lock configuration")

        ubx.resource(VaultLockConfiguration, "ci-runner", VaultLockConfigurationConfig(
            backup_vault_name="example-backup-vault-name",
            changeable_for_days=1,
            max_retention_days=1,
            min_retention_days=1,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_vault_lock_configuration, with backup vault
    name example-backup-vault-name, changeable for days 1, max
    retention days 1, min retention days 1.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="backup_vault_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="changeable_for_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="max_retention_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="min_retention_days" type="number">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="backup_vault_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
