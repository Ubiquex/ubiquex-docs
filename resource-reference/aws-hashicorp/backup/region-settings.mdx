---
title: "aws_backup_region_settings"
description: "Real, generated bindings for `aws_backup_region_settings`."
---

`aws_backup_region_settings` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	backup "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/backup"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region settings"})

    		ubx.Resource(backup.RegionSettings, "ci-runner", backup.RegionSettingsConfig{
    			ResourceTypeOptInPreference: map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionSettings } from "jsr:@ubx/sdk-aws/aws/backup/region_settings";

    export default stack("payments", () => {
      intent({ summary: "payments own region settings" });

      resource(RegionSettings, "ci-runner", {
        resourceTypeOptInPreference: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.backup import RegionSettings, RegionSettingsConfig

    def describe():
        ubx.intent("payments own region settings")

        ubx.resource(RegionSettings, "ci-runner", RegionSettingsConfig(
            resource_type_opt_in_preference={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_backup_region_settings, with resource type opt in
    preference a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type_management_preference" type="map(bool)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type_opt_in_preference" type="map(bool)" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="resource_type_management_preference" type="map(bool)">
  Optional; if omitted, computed by AWS.
</ResponseField>
