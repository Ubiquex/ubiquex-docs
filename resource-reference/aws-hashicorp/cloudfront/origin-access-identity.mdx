---
title: "aws_cloudfront_origin_access_identity"
description: "Real, generated bindings for `aws_cloudfront_origin_access_identity`."
---

`aws_cloudfront_origin_access_identity` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	cloudfront "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/cloudfront"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own origin access identity"})

    		ubx.Resource(cloudfront.OriginAccessIdentity, "ci-runner", cloudfront.OriginAccessIdentityConfig{
    			Comment: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OriginAccessIdentity } from "jsr:@ubx/sdk-aws/aws/cloudfront/origin_access_identity";

    export default stack("payments", () => {
      intent({ summary: "payments own origin access identity" });

      resource(OriginAccessIdentity, "ci-runner", {
        comment: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.cloudfront import OriginAccessIdentity, OriginAccessIdentityConfig

    def describe():
        ubx.intent("payments own origin access identity")

        ubx.resource(OriginAccessIdentity, "ci-runner", OriginAccessIdentityConfig(
            comment="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_cloudfront_origin_access_identity, with comment
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="comment" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="caller_reference" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="cloudfront_access_identity_path" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="etag" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="iam_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="s3_canonical_user_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>
