---
title: "aws_amplify_backend_environment"
description: "Real, generated bindings for `aws_amplify_backend_environment`."
---

`aws_amplify_backend_environment` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own backend environment"})

    		ubx.Resource(amplify.BackendEnvironment, "ci-runner", amplify.BackendEnvironmentConfig{
    			AppId:           "example",
    			EnvironmentName: "example-environment-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { BackendEnvironment } from "jsr:@ubx/sdk-aws/aws/amplify/backend_environment";

    export default stack("payments", () => {
      intent({ summary: "payments own backend environment" });

      resource(BackendEnvironment, "ci-runner", {
        appId: "example",
        environmentName: "example-environment-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.amplify import BackendEnvironment, BackendEnvironmentConfig

    def describe():
        ubx.intent("payments own backend environment")

        ubx.resource(BackendEnvironment, "ci-runner", BackendEnvironmentConfig(
            app_id="example",
            environment_name="example-environment-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amplify_backend_environment, with app id example,
    environment name example-environment-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="deployment_artifacts" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="environment_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="deployment_artifacts" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stack_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>
