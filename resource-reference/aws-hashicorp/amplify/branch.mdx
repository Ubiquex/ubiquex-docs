---
title: "aws_amplify_branch"
description: "Real, generated bindings for `aws_amplify_branch`."
---

`aws_amplify_branch` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	amplify "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/amplify"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own branch"})

    		ubx.Resource(amplify.Branch, "ci-runner", amplify.BranchConfig{
    			AppId:                 "example",
    			BranchName:            "example-branch-name",
    			BackendEnvironmentArn: "arn:aws:iam::123456789012:policy/ci",
    			BasicAuthCredentials:  "example",
    			Description:           "Managed by ubx.",
    			EnableAutoBuild:       true,
    			EnableBasicAuth:       true,
    			EnableNotification:    true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Branch } from "jsr:@ubx/sdk-aws/aws/amplify/branch";

    export default stack("payments", () => {
      intent({ summary: "payments own branch" });

      resource(Branch, "ci-runner", {
        appId: "example",
        branchName: "example-branch-name",
        backendEnvironmentArn: "arn:aws:iam::123456789012:policy/ci",
        basicAuthCredentials: "example",
        description: "Managed by ubx.",
        enableAutoBuild: true,
        enableBasicAuth: true,
        enableNotification: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.amplify import Branch, BranchConfig

    def describe():
        ubx.intent("payments own branch")

        ubx.resource(Branch, "ci-runner", BranchConfig(
            app_id="example",
            branch_name="example-branch-name",
            backend_environment_arn="arn:aws:iam::123456789012:policy/ci",
            basic_auth_credentials="example",
            description="Managed by ubx.",
            enable_auto_build=True,
            enable_basic_auth=True,
            enable_notification=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_amplify_branch, with app id example, branch name
    example-branch-name, backend environment arn
    arn:aws:iam::123456789012:policy/ci, basic auth credentials
    example, description Managed by ubx., enable auto build true,
    enable basic auth true, enable notification true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="app_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="backend_environment_arn" type="string">
  Optional.
</ResponseField>

<ResponseField name="basic_auth_credentials" type="string">
  Optional.
</ResponseField>

<ResponseField name="branch_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="enable_auto_build" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_basic_auth" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_notification" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_performance_mode" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_pull_request_preview" type="bool">
  Optional.
</ResponseField>

<ResponseField name="enable_skew_protection" type="bool">
  Optional.
</ResponseField>

<ResponseField name="environment_variables" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="framework" type="string">
  Optional.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="pull_request_environment_name" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="stage" type="string">
  Optional.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="ttl" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="associated_resources" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="custom_domains" type="list(string)">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="destination_branch" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="display_name" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="source_branch" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Optional; if omitted, computed by AWS.
</ResponseField>
