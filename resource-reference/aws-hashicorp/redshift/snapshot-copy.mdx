---
title: "aws_redshift_snapshot_copy"
description: "Real, generated bindings for `aws_redshift_snapshot_copy`."
---

`aws_redshift_snapshot_copy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own snapshot copy"})

    		ubx.Resource(redshift.SnapshotCopy, "ci-runner", redshift.SnapshotCopyConfig{
    			ClusterIdentifier:     "example",
    			DestinationRegion:     "example",
    			SnapshotCopyGrantName: "example-snapshot-copy-grant-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SnapshotCopy } from "jsr:@ubx/sdk-aws/aws/redshift/snapshot_copy";

    export default stack("payments", () => {
      intent({ summary: "payments own snapshot copy" });

      resource(SnapshotCopy, "ci-runner", {
        clusterIdentifier: "example",
        destinationRegion: "example",
        snapshotCopyGrantName: "example-snapshot-copy-grant-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import SnapshotCopy, SnapshotCopyConfig

    def describe():
        ubx.intent("payments own snapshot copy")

        ubx.resource(SnapshotCopy, "ci-runner", SnapshotCopyConfig(
            cluster_identifier="example",
            destination_region="example",
            snapshot_copy_grant_name="example-snapshot-copy-grant-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_snapshot_copy, with cluster identifier
    example, destination region example, snapshot copy grant name
    example-snapshot-copy-grant-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="destination_region" type="string" required>
  Required.
</ResponseField>

<ResponseField name="manual_snapshot_retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="snapshot_copy_grant_name" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="manual_snapshot_retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="retention_period" type="number">
  Optional; if omitted, computed by AWS.
</ResponseField>
