---
title: "aws_redshift_authentication_profile"
description: "Real, generated bindings for `aws_redshift_authentication_profile`."
---

`aws_redshift_authentication_profile` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own authentication profile"})

    		ubx.Resource(redshift.AuthenticationProfile, "ci-runner", redshift.AuthenticationProfileConfig{
    			AuthenticationProfileContent: "example",
    			AuthenticationProfileName:    "example-authentication-profile-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AuthenticationProfile } from "jsr:@ubx/sdk-aws/aws/redshift/authentication_profile";

    export default stack("payments", () => {
      intent({ summary: "payments own authentication profile" });

      resource(AuthenticationProfile, "ci-runner", {
        authenticationProfileContent: "example",
        authenticationProfileName: "example-authentication-profile-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import AuthenticationProfile, AuthenticationProfileConfig

    def describe():
        ubx.intent("payments own authentication profile")

        ubx.resource(AuthenticationProfile, "ci-runner", AuthenticationProfileConfig(
            authentication_profile_content="example",
            authentication_profile_name="example-authentication-profile-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_authentication_profile, with authentication
    profile content example, authentication profile name
    example-authentication-profile-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="authentication_profile_content" type="string" required>
  Required.
</ResponseField>

<ResponseField name="authentication_profile_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
