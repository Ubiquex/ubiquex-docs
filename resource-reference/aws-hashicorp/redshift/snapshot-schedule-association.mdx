---
title: "aws_redshift_snapshot_schedule_association"
description: "Real, generated bindings for `aws_redshift_snapshot_schedule_association`."
---

`aws_redshift_snapshot_schedule_association` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redshift "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/redshift"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own snapshot schedule association"})

    		ubx.Resource(redshift.SnapshotScheduleAssociation, "ci-runner", redshift.SnapshotScheduleAssociationConfig{
    			ClusterIdentifier:  "example",
    			ScheduleIdentifier: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { SnapshotScheduleAssociation } from "jsr:@ubx/sdk-aws/aws/redshift/snapshot_schedule_association";

    export default stack("payments", () => {
      intent({ summary: "payments own snapshot schedule association" });

      resource(SnapshotScheduleAssociation, "ci-runner", {
        clusterIdentifier: "example",
        scheduleIdentifier: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.redshift import SnapshotScheduleAssociation, SnapshotScheduleAssociationConfig

    def describe():
        ubx.intent("payments own snapshot schedule association")

        ubx.resource(SnapshotScheduleAssociation, "ci-runner", SnapshotScheduleAssociationConfig(
            cluster_identifier="example",
            schedule_identifier="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_redshift_snapshot_schedule_association, with cluster
    identifier example, schedule identifier example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="cluster_identifier" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="schedule_identifier" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
