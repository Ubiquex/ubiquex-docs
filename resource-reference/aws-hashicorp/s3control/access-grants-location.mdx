---
title: "aws_s3control_access_grants_location"
description: "Real, generated bindings for `aws_s3control_access_grants_location`."
---

`aws_s3control_access_grants_location` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access grants location"})

    		ubx.Resource(s3control.AccessGrantsLocation, "ci-runner", s3control.AccessGrantsLocationConfig{
    			IamRoleArn:    "arn:aws:iam::123456789012:policy/ci",
    			LocationScope: "example",
    			Tags:          map[string]string{"managed-by": "ubx"},
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessGrantsLocation } from "jsr:@ubx/sdk-aws/aws/s3control/access_grants_location";

    export default stack("payments", () => {
      intent({ summary: "payments own access grants location" });

      resource(AccessGrantsLocation, "ci-runner", {
        iamRoleArn: "arn:aws:iam::123456789012:policy/ci",
        locationScope: "example",
        tags: { "managed-by": "ubx" },
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3control import AccessGrantsLocation, AccessGrantsLocationConfig

    def describe():
        ubx.intent("payments own access grants location")

        ubx.resource(AccessGrantsLocation, "ci-runner", AccessGrantsLocationConfig(
            iam_role_arn="arn:aws:iam::123456789012:policy/ci",
            location_scope="example",
            tags={"managed-by": "ubx"},
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_access_grants_location, with iam role arn
    arn:aws:iam::123456789012:policy/ci, location scope example, tags
    a set value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="iam_role_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="location_scope" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags" type="map(string)">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="access_grants_location_arn" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="access_grants_location_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="tags_all" type="map(string)">
  Computed by AWS after `ship`.
</ResponseField>
