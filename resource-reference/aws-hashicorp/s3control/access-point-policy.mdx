---
title: "aws_s3control_access_point_policy"
description: "Real, generated bindings for `aws_s3control_access_point_policy`."
---

`aws_s3control_access_point_policy` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	"encoding/json"

    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own access point policy"})

    		accessPolicy, _ := json.Marshal(map[string]any{
    			"Version": "2012-10-17",
    			"Statement": []map[string]any{{
    				"Effect":   "Allow",
    				"Action":   "*",
    				"Resource": "*",
    			}},
    		})

    		ubx.Resource(s3control.AccessPointPolicy, "ci-runner", s3control.AccessPointPolicyConfig{
    			AccessPointArn: "arn:aws:iam::123456789012:policy/ci",
    			Policy:         string(accessPolicy),
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AccessPointPolicy } from "jsr:@ubx/sdk-aws/aws/s3control/access_point_policy";

    export default stack("payments", () => {
      intent({ summary: "payments own access point policy" });

      const accessPolicy = JSON.stringify({
        Version: "2012-10-17",
        Statement: [{
          Effect: "Allow",
          Action: "*",
          Resource: "*",
        }],
      });

      resource(AccessPointPolicy, "ci-runner", {
        accessPointArn: "arn:aws:iam::123456789012:policy/ci",
        policy: accessPolicy,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import json
    import ubx_sdk as ubx
    from ubx.aws.s3control import AccessPointPolicy, AccessPointPolicyConfig

    def describe():
        ubx.intent("payments own access point policy")

        access_policy = json.dumps({
            "Version": "2012-10-17",
            "Statement": [{
                "Effect": "Allow",
                "Action": "*",
                "Resource": "*",
            }],
        })

        ubx.resource(AccessPointPolicy, "ci-runner", AccessPointPolicyConfig(
            access_point_arn="arn:aws:iam::123456789012:policy/ci",
            policy=access_policy,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_access_point_policy, with access point arn
    arn:aws:iam::123456789012:policy/ci, policy a real, valid value.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_point_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="has_public_access_policy" type="bool">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
