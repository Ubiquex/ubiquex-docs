---
title: "aws_s3control_multi_region_access_point_routes"
description: "Real, generated bindings for `aws_s3control_multi_region_access_point_routes`."
---

`aws_s3control_multi_region_access_point_routes` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	s3control "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/s3control"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own multi region access point routes"})

    		ubx.Resource(s3control.MultiRegionAccessPointRoutes, "ci-runner", s3control.MultiRegionAccessPointRoutesConfig{
    			Mrap: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MultiRegionAccessPointRoutes } from "jsr:@ubx/sdk-aws/aws/s3control/multi_region_access_point_routes";

    export default stack("payments", () => {
      intent({ summary: "payments own multi region access point routes" });

      resource(MultiRegionAccessPointRoutes, "ci-runner", {
        mrap: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.s3control import MultiRegionAccessPointRoutes, MultiRegionAccessPointRoutesConfig

    def describe():
        ubx.intent("payments own multi region access point routes")

        ubx.resource(MultiRegionAccessPointRoutes, "ci-runner", MultiRegionAccessPointRoutesConfig(
            mrap="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_s3control_multi_region_access_point_routes, with mrap
    example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="mrap" type="string" required>
  Required.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string" required>
    </ResponseField>
    <ResponseField name="region" type="string" required>
    </ResponseField>
    <ResponseField name="traffic_dial_percentage" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="account_id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="route" type="set(object)">
  Optional; if omitted, computed by AWS.
  <Expandable title="properties">
    <ResponseField name="bucket" type="string" required>
    </ResponseField>
    <ResponseField name="region" type="string" required>
    </ResponseField>
    <ResponseField name="traffic_dial_percentage" type="number" required>
    </ResponseField>
  </Expandable>
</ResponseField>
