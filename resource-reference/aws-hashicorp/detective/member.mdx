---
title: "aws_detective_member"
description: "Real, generated bindings for `aws_detective_member`."
---

`aws_detective_member` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own member"})

    		ubx.Resource(detective.Member, "ci-runner", detective.MemberConfig{
    			AccountId:                "example",
    			EmailAddress:             "example",
    			GraphArn:                 "arn:aws:iam::123456789012:policy/ci",
    			DisableEmailNotification: true,
    			Message:                  "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Member } from "jsr:@ubx/sdk-aws/aws/detective/member";

    export default stack("payments", () => {
      intent({ summary: "payments own member" });

      resource(Member, "ci-runner", {
        accountId: "example",
        emailAddress: "example",
        graphArn: "arn:aws:iam::123456789012:policy/ci",
        disableEmailNotification: true,
        message: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.detective import Member, MemberConfig

    def describe():
        ubx.intent("payments own member")

        ubx.resource(Member, "ci-runner", MemberConfig(
            account_id="example",
            email_address="example",
            graph_arn="arn:aws:iam::123456789012:policy/ci",
            disable_email_notification=True,
            message="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_detective_member, with account id example, email
    address example, graph arn arn:aws:iam::123456789012:policy/ci,
    disable email notification true, message example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="account_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="disable_email_notification" type="bool">
  Optional.
</ResponseField>

<ResponseField name="email_address" type="string" required>
  Required.
</ResponseField>

<ResponseField name="graph_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="message" type="string">
  Optional.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="administrator_id" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="disabled_reason" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="invited_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="status" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="updated_time" type="string">
  Computed by AWS after `ship`.
</ResponseField>

<ResponseField name="volume_usage_in_bytes" type="string">
  Computed by AWS after `ship`.
</ResponseField>
