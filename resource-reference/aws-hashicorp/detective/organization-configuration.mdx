---
title: "aws_detective_organization_configuration"
description: "Real, generated bindings for `aws_detective_organization_configuration`."
---

`aws_detective_organization_configuration` -- real, typed bindings generated directly from the
`hashicorp/aws` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	detective "github.com/ubiquex/ubx-sdk-aws/sdk/go/aws/detective"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own organization configuration"})

    		ubx.Resource(detective.OrganizationConfiguration, "ci-runner", detective.OrganizationConfigurationConfig{
    			AutoEnable: true,
    			GraphArn:   "arn:aws:iam::123456789012:policy/ci",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { OrganizationConfiguration } from "jsr:@ubx/sdk-aws/aws/detective/organization_configuration";

    export default stack("payments", () => {
      intent({ summary: "payments own organization configuration" });

      resource(OrganizationConfiguration, "ci-runner", {
        autoEnable: true,
        graphArn: "arn:aws:iam::123456789012:policy/ci",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.aws.detective import OrganizationConfiguration, OrganizationConfigurationConfig

    def describe():
        ubx.intent("payments own organization configuration")

        ubx.resource(OrganizationConfiguration, "ci-runner", OrganizationConfigurationConfig(
            auto_enable=True,
            graph_arn="arn:aws:iam::123456789012:policy/ci",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create aws_detective_organization_configuration, with auto enable
    true, graph arn arn:aws:iam::123456789012:policy/ci.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="auto_enable" type="bool" required>
  Required.
</ResponseField>

<ResponseField name="graph_arn" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by AWS.
</ResponseField>

<ResponseField name="region" type="string">
  Region where this resource will be [managed](https://docs.aws.amazon.com/general/latest/gr/rande.html#regional-endpoints). Defaults to the Region set in the [provider configuration](https://registry.terraform.io/providers/hashicorp/aws/latest/docs#aws-configuration-reference). Optional; if omitted, computed by AWS.
</ResponseField>
