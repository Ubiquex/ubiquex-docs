---
title: "azurerm_federated_identity_credential"
description: "Real, generated bindings for `azurerm_federated_identity_credential`."
---

`azurerm_federated_identity_credential` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	federated "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/federated"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own identity credential"})

    		ubx.Resource(federated.IdentityCredential, "ci-runner", federated.IdentityCredentialConfig{
    			Audience:               []string{"example"},
    			Issuer:                 "example",
    			Name:                   "ci-runner",
    			Subject:                "example",
    			UserAssignedIdentityId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { IdentityCredential } from "jsr:@ubx/sdk-azure/azurerm/federated/identity_credential";

    export default stack("payments", () => {
      intent({ summary: "payments own identity credential" });

      resource(IdentityCredential, "ci-runner", {
        audience: ["example"],
        issuer: "example",
        name: "ci-runner",
        subject: "example",
        userAssignedIdentityId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.federated import IdentityCredential, IdentityCredentialConfig

    def describe():
        ubx.intent("payments own identity credential")

        ubx.resource(IdentityCredential, "ci-runner", IdentityCredentialConfig(
            audience=["example"],
            issuer="example",
            name="ci-runner",
            subject="example",
            user_assigned_identity_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_federated_identity_credential called "ci-runner",
    with audience a set value, issuer example, subject example, user
    assigned identity id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="audience" type="list(string)" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="issuer" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="subject" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="user_assigned_identity_id" type="string" required>
  Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
