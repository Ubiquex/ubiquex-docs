---
title: "azurerm_postgresql_flexible_server_virtual_endpoint"
description: "Real, generated bindings for `azurerm_postgresql_flexible_server_virtual_endpoint`."
---

`azurerm_postgresql_flexible_server_virtual_endpoint` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	postgresql "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/postgresql"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own flexible server virtual endpoint"})

    		ubx.Resource(postgresql.FlexibleServerVirtualEndpoint, "ci-runner", postgresql.FlexibleServerVirtualEndpointConfig{
    			Name:            "ci-runner",
    			ReplicaServerId: "example",
    			SourceServerId:  "example",
    			Type:            "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { FlexibleServerVirtualEndpoint } from "jsr:@ubx/sdk-azure/azurerm/postgresql/flexible_server_virtual_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own flexible server virtual endpoint" });

      resource(FlexibleServerVirtualEndpoint, "ci-runner", {
        name: "ci-runner",
        replicaServerId: "example",
        sourceServerId: "example",
        type: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.postgresql import FlexibleServerVirtualEndpoint, FlexibleServerVirtualEndpointConfig

    def describe():
        ubx.intent("payments own flexible server virtual endpoint")

        ubx.resource(FlexibleServerVirtualEndpoint, "ci-runner", FlexibleServerVirtualEndpointConfig(
            name="ci-runner",
            replica_server_id="example",
            source_server_id="example",
            type="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_postgresql_flexible_server_virtual_endpoint called
    "ci-runner", with replica server id example, source server id
    example, type example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the Virtual Endpoint Required.
</ResponseField>

<ResponseField name="replica_server_id" type="string" required>
  The Resource ID of the *Replica* Postgres Flexible Server this should be associated with Required.
</ResponseField>

<ResponseField name="source_server_id" type="string" required>
  The Resource ID of the *Source* Postgres Flexible Server this should be associated with Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="type" type="string" required>
  The type of Virtual Endpoint Required.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
