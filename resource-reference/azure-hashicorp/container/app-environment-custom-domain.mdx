---
title: "azurerm_container_app_environment_custom_domain"
description: "Real, generated bindings for `azurerm_container_app_environment_custom_domain`."
---

`azurerm_container_app_environment_custom_domain` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app environment custom domain"})

    		ubx.Resource(container.AppEnvironmentCustomDomain, "ci-runner", container.AppEnvironmentCustomDomainConfig{
    			CertificateBlobBase64:     "example",
    			CertificatePassword:       "example",
    			ContainerAppEnvironmentId: "example",
    			DnsSuffix:                 "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppEnvironmentCustomDomain } from "jsr:@ubx/sdk-azure/azurerm/container/app_environment_custom_domain";

    export default stack("payments", () => {
      intent({ summary: "payments own app environment custom domain" });

      resource(AppEnvironmentCustomDomain, "ci-runner", {
        certificateBlobBase64: "example",
        certificatePassword: "example",
        containerAppEnvironmentId: "example",
        dnsSuffix: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.container import AppEnvironmentCustomDomain, AppEnvironmentCustomDomainConfig

    def describe():
        ubx.intent("payments own app environment custom domain")

        ubx.resource(AppEnvironmentCustomDomain, "ci-runner", AppEnvironmentCustomDomainConfig(
            certificate_blob_base64="example",
            certificate_password="example",
            container_app_environment_id="example",
            dns_suffix="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_container_app_environment_custom_domain, with
    certificate blob base64 example, certificate password example,
    container app environment id example, dns suffix example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="certificate_blob_base64" type="string" required>
  The Custom Domain Certificate Private Key as a base64 encoded PFX or PEM. Required.
</ResponseField>

<ResponseField name="certificate_password" type="string" required>
  The Custom Domain Certificate password. Required.
</ResponseField>

<ResponseField name="container_app_environment_id" type="string" required>
  The Container App Managed Environment ID to configure this Custom Domain on. Required.
</ResponseField>

<ResponseField name="dns_suffix" type="string" required>
  The Custom Domain DNS suffix for this Container App Environment. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
