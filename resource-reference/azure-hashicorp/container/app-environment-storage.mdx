---
title: "azurerm_container_app_environment_storage"
description: "Real, generated bindings for `azurerm_container_app_environment_storage`."
---

`azurerm_container_app_environment_storage` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	container "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/container"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own app environment storage"})

    		ubx.Resource(container.AppEnvironmentStorage, "ci-runner", container.AppEnvironmentStorageConfig{
    			AccessMode:                "example",
    			ContainerAppEnvironmentId: "example",
    			Name:                      "ci-runner",
    			ShareName:                 "example-share-name",
    			AccessKey:                 "example",
    			AccountName:               "example-account-name",
    			NfsServerUrl:              "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { AppEnvironmentStorage } from "jsr:@ubx/sdk-azure/azurerm/container/app_environment_storage";

    export default stack("payments", () => {
      intent({ summary: "payments own app environment storage" });

      resource(AppEnvironmentStorage, "ci-runner", {
        accessMode: "example",
        containerAppEnvironmentId: "example",
        name: "ci-runner",
        shareName: "example-share-name",
        accessKey: "example",
        accountName: "example-account-name",
        nfsServerUrl: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.container import AppEnvironmentStorage, AppEnvironmentStorageConfig

    def describe():
        ubx.intent("payments own app environment storage")

        ubx.resource(AppEnvironmentStorage, "ci-runner", AppEnvironmentStorageConfig(
            access_mode="example",
            container_app_environment_id="example",
            name="ci-runner",
            share_name="example-share-name",
            access_key="example",
            account_name="example-account-name",
            nfs_server_url="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_container_app_environment_storage called
    "ci-runner", with access mode example, container app environment
    id example, share name example-share-name, access key example,
    account name example-account-name, nfs server url example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="access_key" type="string">
  The Storage Account Access Key. Optional.
</ResponseField>

<ResponseField name="access_mode" type="string" required>
  The access mode to connect this storage to the Container App. Possible values include `ReadOnly` and `ReadWrite`. Required.
</ResponseField>

<ResponseField name="account_name" type="string">
  The Azure Storage Account in which the Share to be used is located. Optional.
</ResponseField>

<ResponseField name="container_app_environment_id" type="string" required>
  The ID of the Container App Environment to which this storage belongs. Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  The name for this Storage. Required.
</ResponseField>

<ResponseField name="nfs_server_url" type="string">
  Optional.
</ResponseField>

<ResponseField name="share_name" type="string" required>
  The name of the Azure Storage Share to use. Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
