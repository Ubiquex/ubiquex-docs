---
title: "azurerm_virtual_hub_bgp_connection"
description: "Real, generated bindings for `azurerm_virtual_hub_bgp_connection`."
---

`azurerm_virtual_hub_bgp_connection` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own hub bgp connection"})

    		ubx.Resource(virtual.HubBgpConnection, "ci-runner", virtual.HubBgpConnectionConfig{
    			Name:                       "ci-runner",
    			PeerAsn:                    1,
    			PeerIp:                     "example",
    			VirtualHubId:               "example",
    			VirtualNetworkConnectionId: "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { HubBgpConnection } from "jsr:@ubx/sdk-azure/azurerm/virtual/hub_bgp_connection";

    export default stack("payments", () => {
      intent({ summary: "payments own hub bgp connection" });

      resource(HubBgpConnection, "ci-runner", {
        name: "ci-runner",
        peerAsn: 1,
        peerIp: "example",
        virtualHubId: "example",
        virtualNetworkConnectionId: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.virtual import HubBgpConnection, HubBgpConnectionConfig

    def describe():
        ubx.intent("payments own hub bgp connection")

        ubx.resource(HubBgpConnection, "ci-runner", HubBgpConnectionConfig(
            name="ci-runner",
            peer_asn=1,
            peer_ip="example",
            virtual_hub_id="example",
            virtual_network_connection_id="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_virtual_hub_bgp_connection called "ci-runner",
    with peer asn 1, peer ip example, virtual hub id example, virtual
    network connection id example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="peer_asn" type="number" required>
  Required.
</ResponseField>

<ResponseField name="peer_ip" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="virtual_hub_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="virtual_network_connection_id" type="string">
  Optional.
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
