---
title: "azurerm_virtual_desktop_application"
description: "Real, generated bindings for `azurerm_virtual_desktop_application`."
---

`azurerm_virtual_desktop_application` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	virtual "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/virtual"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own desktop application"})

    		ubx.Resource(virtual.DesktopApplication, "ci-runner", virtual.DesktopApplicationConfig{
    			ApplicationGroupId:        "example",
    			CommandLineArgumentPolicy: "example",
    			Name:                      "ci-runner",
    			Path:                      "/ci/",
    			CommandLineArguments:      "example",
    			Description:               "Managed by ubx.",
    			IconIndex:                 1,
    			ShowInPortal:              true,
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { DesktopApplication } from "jsr:@ubx/sdk-azure/azurerm/virtual/desktop_application";

    export default stack("payments", () => {
      intent({ summary: "payments own desktop application" });

      resource(DesktopApplication, "ci-runner", {
        applicationGroupId: "example",
        commandLineArgumentPolicy: "example",
        name: "ci-runner",
        path: "/ci/",
        commandLineArguments: "example",
        description: "Managed by ubx.",
        iconIndex: 1,
        showInPortal: true,
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.virtual import DesktopApplication, DesktopApplicationConfig

    def describe():
        ubx.intent("payments own desktop application")

        ubx.resource(DesktopApplication, "ci-runner", DesktopApplicationConfig(
            application_group_id="example",
            command_line_argument_policy="example",
            name="ci-runner",
            path="/ci/",
            command_line_arguments="example",
            description="Managed by ubx.",
            icon_index=1,
            show_in_portal=True,
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_virtual_desktop_application called "ci-runner",
    with application group id example, command line argument policy
    example, path /ci/, command line arguments example, description
    Managed by ubx., icon index 1, show in portal true.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="application_group_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="command_line_argument_policy" type="string" required>
  Required.
</ResponseField>

<ResponseField name="command_line_arguments" type="string">
  Optional.
</ResponseField>

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="friendly_name" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="icon_index" type="number">
  Optional.
</ResponseField>

<ResponseField name="icon_path" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="path" type="string" required>
  Required.
</ResponseField>

<ResponseField name="show_in_portal" type="bool">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="friendly_name" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="icon_path" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
