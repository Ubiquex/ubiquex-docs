---
title: "azurerm_lb_probe"
description: "Real, generated bindings for `azurerm_lb_probe`."
---

`azurerm_lb_probe` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	lb "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/lb"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own probe"})

    		ubx.Resource(lb.Probe, "ci-runner", lb.ProbeConfig{
    			LoadbalancerId:    "example",
    			Name:              "ci-runner",
    			Port:              1,
    			IntervalInSeconds: 7200,
    			NumberOfProbes:    1,
    			ProbeThreshold:    1,
    			Protocol:          "example",
    			RequestPath:       "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { Probe } from "jsr:@ubx/sdk-azure/azurerm/lb/probe";

    export default stack("payments", () => {
      intent({ summary: "payments own probe" });

      resource(Probe, "ci-runner", {
        loadbalancerId: "example",
        name: "ci-runner",
        port: 1,
        intervalInSeconds: 7200,
        numberOfProbes: 1,
        probeThreshold: 1,
        protocol: "example",
        requestPath: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.lb import Probe, ProbeConfig

    def describe():
        ubx.intent("payments own probe")

        ubx.resource(Probe, "ci-runner", ProbeConfig(
            loadbalancer_id="example",
            name="ci-runner",
            port=1,
            interval_in_seconds=7200,
            number_of_probes=1,
            probe_threshold=1,
            protocol="example",
            request_path="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_lb_probe called "ci-runner", with loadbalancer id
    example, port 1, interval in seconds 7200, number of probes 1,
    probe threshold 1, protocol example, request path example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="interval_in_seconds" type="number">
  Optional.
</ResponseField>

<ResponseField name="loadbalancer_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="number_of_probes" type="number">
  Optional.
</ResponseField>

<ResponseField name="port" type="number" required>
  Required.
</ResponseField>

<ResponseField name="probe_threshold" type="number">
  Optional.
</ResponseField>

<ResponseField name="protocol" type="string">
  Optional.
</ResponseField>

<ResponseField name="request_path" type="string">
  Optional.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="load_balancer_rules" type="set(string)">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
