---
title: "azurerm_storage_mover_source_endpoint"
description: "Real, generated bindings for `azurerm_storage_mover_source_endpoint`."
---

`azurerm_storage_mover_source_endpoint` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	storage "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/storage"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own mover source endpoint"})

    		ubx.Resource(storage.MoverSourceEndpoint, "ci-runner", storage.MoverSourceEndpointConfig{
    			Host:           "example",
    			Name:           "ci-runner",
    			StorageMoverId: "example",
    			Description:    "Managed by ubx.",
    			Export:         "example",
    			NfsVersion:     "example",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { MoverSourceEndpoint } from "jsr:@ubx/sdk-azure/azurerm/storage/mover_source_endpoint";

    export default stack("payments", () => {
      intent({ summary: "payments own mover source endpoint" });

      resource(MoverSourceEndpoint, "ci-runner", {
        host: "example",
        name: "ci-runner",
        storageMoverId: "example",
        description: "Managed by ubx.",
        export: "example",
        nfsVersion: "example",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.storage import MoverSourceEndpoint, MoverSourceEndpointConfig

    def describe():
        ubx.intent("payments own mover source endpoint")

        ubx.resource(MoverSourceEndpoint, "ci-runner", MoverSourceEndpointConfig(
            host="example",
            name="ci-runner",
            storage_mover_id="example",
            description="Managed by ubx.",
            export="example",
            nfs_version="example",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_storage_mover_source_endpoint called "ci-runner",
    with host example, storage mover id example, description Managed
    by ubx., export example, nfs version example.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="description" type="string">
  Optional.
</ResponseField>

<ResponseField name="export" type="string">
  Optional.
</ResponseField>

<ResponseField name="host" type="string" required>
  Required.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="nfs_version" type="string">
  Optional.
</ResponseField>

<ResponseField name="storage_mover_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
    <ResponseField name="update" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
