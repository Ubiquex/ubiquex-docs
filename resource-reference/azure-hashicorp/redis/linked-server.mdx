---
title: "azurerm_redis_linked_server"
description: "Real, generated bindings for `azurerm_redis_linked_server`."
---

`azurerm_redis_linked_server` -- real, typed bindings generated directly from the
`hashicorp/azurerm` provider schema, in every SDK language.

Every tab below is a complete, runnable program, not a fragment,
real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	redis "github.com/ubiquex/ubx-sdk-azure/sdk/go/azurerm/redis"
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own linked server"})

    		ubx.Resource(redis.LinkedServer, "ci-runner", redis.LinkedServerConfig{
    			LinkedRedisCacheId:       "example",
    			LinkedRedisCacheLocation: "example",
    			ResourceGroupName:        "example-resource-group-name",
    			ServerRole:               "example",
    			TargetRedisCacheName:     "example-target-redis-cache-name",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { LinkedServer } from "jsr:@ubx/sdk-azure/azurerm/redis/linked_server";

    export default stack("payments", () => {
      intent({ summary: "payments own linked server" });

      resource(LinkedServer, "ci-runner", {
        linkedRedisCacheId: "example",
        linkedRedisCacheLocation: "example",
        resourceGroupName: "example-resource-group-name",
        serverRole: "example",
        targetRedisCacheName: "example-target-redis-cache-name",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.azurerm.redis import LinkedServer, LinkedServerConfig

    def describe():
        ubx.intent("payments own linked server")

        ubx.resource(LinkedServer, "ci-runner", LinkedServerConfig(
            linked_redis_cache_id="example",
            linked_redis_cache_location="example",
            resource_group_name="example-resource-group-name",
            server_role="example",
            target_redis_cache_name="example-target-redis-cache-name",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create azurerm_redis_linked_server, with linked redis cache id
    example, linked redis cache location example, resource group name
    example-resource-group-name, server role example, target redis
    cache name example-target-redis-cache-name.
    ```
  </Tab>
</Tabs>

## Input properties

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="linked_redis_cache_id" type="string" required>
  Required.
</ResponseField>

<ResponseField name="linked_redis_cache_location" type="string" required>
  Required.
</ResponseField>

<ResponseField name="resource_group_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="server_role" type="string" required>
  Required.
</ResponseField>

<ResponseField name="target_redis_cache_name" type="string" required>
  Required.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>

## Output properties

<ResponseField name="geo_replicated_primary_host_name" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="id" type="string">
  Optional; if omitted, computed by Azure.
</ResponseField>

<ResponseField name="name" type="string">
  Computed by Azure after `ship`.
</ResponseField>

<ResponseField name="timeouts" type="object">
  Optional; if omitted, computed by Azure.
  <Expandable title="properties">
    <ResponseField name="create" type="string">
    </ResponseField>
    <ResponseField name="delete" type="string">
    </ResponseField>
    <ResponseField name="read" type="string">
    </ResponseField>
  </Expandable>
</ResponseField>
