---
title: "google_compute_region_instance_group_manager"
description: "`google_compute_region_instance_group_manager` is the regional counterpart to a zonal instance group manager -- managing a managed instance group whose..."
---

`google_compute_region_instance_group_manager` is the regional counterpart to a zonal instance group manager -- managing a managed instance group whose real instances are spread across multiple zones in one region for higher availability.

Real, typed bindings generated directly from real Google Discovery Documents, in every SDK language -- every tab below is a complete, runnable program, not a fragment, real enough to save and run exactly as shown.

## Example

<Tabs>
  <Tab title="Go">
    ```go
    package main

    import (
    	ubx "github.com/ubiquex/ubx-sdk-go/runtime"
    	compute "github.com/ubiquex/ubx-sdk-google/sdk/go/google/compute"
    )

    func main() {
    	ubx.Main(ubx.Stack("payments", func() {
    		ubx.Intent(ubx.IntentInfo{Summary: "payments own region instance group manager"})

    		ubx.Resource(compute.RegionInstanceGroupManager, "ci-runner", compute.RegionInstanceGroupManagerConfig{
    			Name: "ci-runner",
    		})
    	}))
    }
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import { intent, resource, stack } from "@ubx/sdk";
    import { RegionInstanceGroupManager } from "jsr:@ubx/sdk-google/google/compute/region_instance_group_manager";

    export default stack("payments", () => {
      intent({ summary: "payments own region instance group manager" });

      resource(RegionInstanceGroupManager, "ci-runner", {
        name: "ci-runner",
      });
    });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import ubx_sdk as ubx
    from ubx.google.compute import RegionInstanceGroupManager, RegionInstanceGroupManagerConfig

    def describe():
        ubx.intent("payments own region instance group manager")

        ubx.resource(RegionInstanceGroupManager, "ci-runner", RegionInstanceGroupManagerConfig(
            name="ci-runner",
        ))

    if __name__ == "__main__":
        ubx.run("payments", describe)
    ```
  </Tab>
  <Tab title="Markdown">
    ```
    Create google_compute_region_instance_group_manager called
    "ci-runner", with .
    ```
  </Tab>
</Tabs>

<Note>
Fields marked **(AI-inferred)** below were not sourced from the real provider schema -- they were generated by ubx's own description pipeline and have not been independently verified against the real provider.
</Note>

## Input properties

<ResponseField name="all_instances_config" type="object">
  The computed configuration applied to all instances in this regional instance group manager. This field is output-only and reflects the current all-instances configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="properties" type="object">
      Represents the change that you want to make to the instance properties. Optional.
      <Expandable title="properties">
        <ResponseField name="labels" type="map(string)">
          The label key-value pairs that you want to patch onto the instance. Optional.
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          The metadata key-value pairs that you want to patch onto the instance. For more information, see Project and instance metadata. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_healing_policies" type="list(object)">
  The autohealing policy for this managed instance group. You can specify only one value. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="health_check" type="string">
      The health check resource URL (self-link) that determines instance health for auto-healing. Can be specified as a full URL, a self-link, or a name reference. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initial_delay_sec" type="number">
      The initial delay, in seconds, before an instance is considered available for auto-healing. This is the time to wait after an instance is created or restarted before checking its health for auto-healing purposes. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="base_instance_name" type="string">
  The base instance name is a prefix that you want to attach to the names of all VMs in a MIG. The maximum character length is 58 and the name must comply with RFC1035 format. When a VM is created in the group, the MIG appends a hyphen and a random four-character string to the base instance name. If you want the MIG to assign sequential numbers instead of a random string, then end the base instance name with a hyphen followed by one or more hash symbols. The hash symbols indicate the number of digits. For example, a base instance name of "vm-###" results in "vm-001" as a VM name. @pattern [a-z](([-a-z0-9]&#123;0,57&#125;)|([-a-z0-9]&#123;0,51&#125;-#&#123;1,10&#125;(\\[[0-9]&#123;1,10&#125;\\])?)) Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. The creation timestamp for this managed instance group inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="current_actions" type="object">
  The current actions on instances in the group, providing counts of pending operations (e.g., creating, deleting, restarting, recreating). This is a computed attribute that reflects the live state of the managed instance group. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="abandoning" type="number">
      Output only. The total number of instances in the managed instance group that are scheduled to be abandoned. Abandoning an instance removes it from the managed instance group without deleting it. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="creating" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be created or are currently being created. If the group fails to create any of these instances, it tries again until it creates the instance successfully. If you have disabled creation retries, this field will not be populated; instead, the creatingWithoutRetries field will be populated. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="creating_without_retries" type="number">
      Output only. The number of instances that the managed instance group will attempt to create. The group attempts to create each instance only once. If the group fails to create any of these instances, it decreases the group's targetSize value accordingly. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="deleting" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be deleted or are currently being deleted. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="none" type="number">
      Output only. The number of instances in the managed instance group that are running and have no scheduled actions. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="recreating" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be recreated or are currently being being recreated. Recreating an instance deletes the existing root persistent disk and creates a new disk from the image that is defined in the instance template. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="refreshing" type="number">
      Output only. The number of instances in the managed instance group that are being reconfigured with properties that do not require a restart or a recreate action. For example, setting or removing target pools for the instance. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="restarting" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be restarted or are currently being restarted. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="resuming" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be resumed or are currently being resumed. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="starting" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be started or are currently being started. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="stopping" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be stopped or are currently being stopped. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="suspending" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be suspended or are currently being suspended. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="verifying" type="number">
      Output only. The number of instances in the managed instance group that are being verified. See the managedInstances[].currentAction property in the listManagedInstances method documentation. Computed by GCP after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="distribution_policy" type="object">
  The distribution policy for the regional instance group manager, defining how instances are distributed across zones in the region. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="target_shape" type="string">
      The distribution shape to which the group converges either proactively or on resize events (depending on the value set inupdatePolicy.instanceRedistributionType). Optional.
    </ResponseField>
    <ResponseField name="zones" type="list(object)">
      Zones where the regional managed instance group will create and manage its instances. By default, a regional MIG doesn't automatically select an AI zone to create instances, even if an AI zone is available in the specified region. To create instances in an AI zone in the selected region, you must explicitly specify it in the distribution policy together with the other preferred zones. Optional.
      <Expandable title="properties">
        <ResponseField name="zone" type="string">
          Specifies the zone to which instances are distributed as part of the distribution policy for this regional managed instance group. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. This field may be used in optimistic locking. It will be ignored when inserting an InstanceGroupManager. An up-to-date fingerprint must be provided in order to update the InstanceGroupManager, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve an InstanceGroupManager. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. A unique identifier for this resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_flexibility_policy" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_selections" type="map(object)">
      Named instance selections configuring properties that the group will use when creating new VMs. Optional.
      <Expandable title="properties">
        <ResponseField name="machine_types" type="list(string)">
          List of machine types that can be used for this instance selection. The instance group manager can choose any of these types when provisioning instances according to the flexibility policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rank" type="number">
          The priority rank of this instance selection, where lower numbers have higher priority. Must be between 0 and 5. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_group" type="string">
  Output only. The URL of the Instance Group resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_lifecycle_policy" type="object">
  The lifecycle policy that determines how the managed instance group handles instance failures, including whether to restart or replace instances. This field is computed and output-only, reflecting the effective policy applied to the group. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="default_action_on_failure" type="string">
      The action that a MIG performs on a failed VM. If the value of the onFailedHealthCheck field is `DEFAULT_ACTION`, then the same action also applies to the VMs on which your application fails a health check. Valid values are - REPAIR (default): MIG automatically repairs a failed VM by recreating it. For more information, see About repairing VMs in a MIG. - DO_NOTHING: MIG does not repair a failed VM. Optional.
    </ResponseField>
    <ResponseField name="force_update_on_repair" type="string">
      A bit indicating whether to forcefully apply the group's latest configuration when repairing a VM. Valid options are: - NO (default): If configuration updates are available, they are not forcefully applied during repair. Instead, configuration updates are applied according to the group's update policy. - YES: If configuration updates are available, they are applied during repair. Optional.
    </ResponseField>
    <ResponseField name="on_failed_health_check" type="string">
      The action that a MIG performs on an unhealthy VM. A VM is marked as unhealthy when the application running on that VM fails a health check. Valid values are: - DEFAULT_ACTION (default): MIG uses the same action configured for instanceLifecyclePolicy.defaultActionOnFailure field. - REPAIR: MIG automatically repairs an unhealthy VM by recreating it. - DO_NOTHING: MIG doesn't repair an unhealthy VM. For more information, see About repairing VMs in a MIG. Optional.
    </ResponseField>
    <ResponseField name="on_repair" type="object">
      Configuration for VM repairs in the MIG. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_changing_zone" type="string">
          Specifies whether the MIG can change a VM's zone during a repair. Valid values are: - NO (default): MIG cannot change a VM's zone during a repair. - YES: MIG can select a different zone for the VM during a repair. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_template" type="string">
  The URL of the instance template that is specified for this managed instance group. The group uses this template to create all new instances in the managed instance group. The templates for existing instances in the group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="list_managed_instances_results" type="string">
  Pagination behavior of the listManagedInstances API method for this managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the managed instance group. The name must be 1-63 characters long, and comply withRFC1035. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="named_ports" type="list(object)">
  [Output Only] Named ports configured on the Instance Groups complementary to this Instance Group Manager. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The symbolic name of the named port, which acts as the key in the named port mapping to a port number. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The port number for this named port. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] The URL of theregion where the managed instance group resides (for regional resources). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_policies" type="object">
  The resource policies attached to the instances in this regional managed instance group. This field is output-only and reflects the current policies applied to the group's instances. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="workload_policy" type="string">
      The URL of the workload policy that is specified for this managed instance group. It can be a full or partial URL. For example, the following are all valid URLs to a workload policy: - https://www.googleapis.com/compute/v1/projects/project/regions/region/resourcePolicies/resourcePolicy - projects/project/regions/region/resourcePolicies/resourcePolicy - regions/region/resourcePolicies/resourcePolicy Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. The URL for this managed instance group. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="standby_policy" type="object">
  The standby policy for the managed instance group, which defines settings for standby instances, such as the initial delay before a standby instance is stopped. This field is output-only and populated by the Google Compute Engine API. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="initial_delay_sec" type="number">
      Specifies the number of seconds that the MIG should wait to suspend or stop a VM after that VM was created. The initial delay gives the initialization script the time to prepare your VM for a quick scale out. The value of initial delay must be between 0 and 3600 seconds. The default value is 0. Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      Defines how a MIG resumes or starts VMs from a standby pool when the group scales out. The default mode is `MANUAL`. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_policy" type="object">
  The stateful policy configuration for the regional instance group manager. This optional block defines which disks and metadata are preserved for instances during group operations such as Rolling Update or Instance Regeneration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="preserved_state" type="object">
      Configuration of preserved resources. Optional.
      <Expandable title="properties">
        <ResponseField name="disks" type="map(object)">
          Disks created on the instances that will be preserved on instance delete, update, etc. This map is keyed with the device names of the disks. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Specifies whether the stateful disk should be automatically deleted when the instance is deleted. Accepts 'true' or 'false'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="external_ips" type="map(object)">
          External network IPs assigned to the instances that will be preserved on instance delete, update, etc. This map is keyed with the network interface name. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Controls whether the associated external IP is automatically deleted when the instance is deleted, or whether it is preserved. In a stateful preserved state configuration, this determines the lifecycle of the external IP for the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="internal_ips" type="map(object)">
          Internal network IPs assigned to the instances that will be preserved on instance delete, update, etc. This map is keyed with the network interface name. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Controls whether the internal IP address is automatically deleted when its managed instance is deleted. Set to 'true' to delete the IP address automatically, or 'false' to preserve it for reuse. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  The current status of the managed instance group, including stability and the state of managed instances. This is a computed field that provides read-only information about the group's runtime state. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="all_instances_config" type="object">
      The status of the all-instances configuration for this regional instance group manager, including the fingerprint and the current state of the configuration applied to all instances. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="current_revision" type="string">
          Output only. Current all-instances configuration revision. This value is in RFC3339 text format. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="effective" type="bool">
          Output only. A bit indicating whether this configuration has been applied to all managed instances in the group. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="applied_accelerator_topologies" type="list(object)">
      Output only. The accelerator topology applied to this MIG. Currently only one accelerator topology is supported. Computed by GCP after `ship`.
      <Expandable title="properties">
        <ResponseField name="accelerator_topology" type="string">
          The accelerator topology currently applied to this regional instance group manager. This may differ from the topology specified in the instance template, as it reflects the actual state. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current state of the accelerator topology applied to the instance group. Possible values are: ACTIVATING, ACTIVE, DEACTIVATING, FAILED, INCOMPLETE, and REACTIVATING. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state_details" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="error" type="object">
              The error object contains details about a failure that occurred while applying an accelerator topology to the instance group, including the error code and message. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="errors" type="list(object)">
                  A list of errors that occurred while applying accelerator topologies to the instance group. Each error object contains a code and message describing the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                    <ResponseField name="error_details" type="list(object)">
                      A list of objects containing detailed information about the errors that occurred while applying accelerator topologies to the instance group. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="error_info" type="object">
                          Optional; if omitted, computed by GCP.
                          <Expandable title="properties">
                            <ResponseField name="domain" type="string">
                              The domain associated with the error, typically indicating the service or API where the error originated (e.g., 'compute.googleapis.com'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metadatas" type="map(string)">
                              A map of key-value pairs containing additional error information that can help diagnose the issue. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="reason" type="string">
                              The reason for the error, as defined by the error domain. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="help" type="object">
                          The help information associated with the error, containing links to relevant documentation and guidance for resolving the issue. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="links" type="list(object)">
                              A list of links that provide additional helpful information about the error. Each link includes a URL and an optional description. Optional; if omitted, computed by GCP. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="description" type="string">
                                  The description of the help link provided for the error. This text explains the purpose of the linked resource, such as a documentation page. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="url" type="string">
                                  The URL of the help resource associated with the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="localized_message" type="object">
                          A localized error message that provides a human-readable version of the error in a specific language, along with the locale identifier. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="locale" type="string">
                              The locale (e.g., 'en-US') for the localized error message, indicating the language in which the message is provided. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="message" type="string">
                              The human-readable localized message describing the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="quota_info" type="object">
                          Information about a quota violation related to the error, including the quota metric, limit, and current usage. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="dimensions" type="map(string)">
                              A map of dimension names to string values that identifies the specific quota that was exceeded, such as region or resource type. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="future_limit" type="number">
                              The projected quota limit that would be in effect if the operation succeeds, used in quota error details to indicate the limit after a change. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit" type="number">
                              The quota limit for the metric that triggered the quota error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit_name" type="string">
                              The name of the specific quota limit that was exceeded, as reported in the quota error details. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metric_name" type="string">
                              The name of the quota metric that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="rollout_status" type="string">
                              The current rollout status of the applied accelerator topologies. Possible values are `IN_PROGRESS` and `ROLLOUT_STATUS_UNSPECIFIED`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="location" type="string">
                      The field or path in the resource configuration that caused the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A human-readable error message describing the reason for the failure within the applied accelerator topology state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaler" type="string">
      Output only. The URL of theAutoscaler that targets this instance group manager. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="bulk_instance_operation" type="object">
      Bulk instance operation is the creation of VMs in a MIG when the targetSizePolicy.mode is set to BULK. Optional.
      <Expandable title="properties">
        <ResponseField name="in_progress" type="bool">
          Output only. Informs whether bulk instance operation is in progress. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="last_progress_check" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="error" type="object">
              Output only. Errors encountered during bulk instance operation. Computed by GCP after `ship`.
              <Expandable title="properties">
                <ResponseField name="errors" type="list(object)">
                  [Output Only] The array of errors encountered while processing this operation. Optional.
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      The machine-readable code identifying the type of error that occurred. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="error_details" type="list(object)">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="error_info" type="object">
                          Optional; if omitted, computed by GCP.
                          <Expandable title="properties">
                            <ResponseField name="domain" type="string">
                              The logical grouping to which the error belongs, such as "googleapis.com" or "gdata". Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metadatas" type="map(string)">
                              Additional structured details about the error, as key-value pairs. The keys and values are service-defined and provide context such as the affected component or reason for the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="reason" type="string">
                              The reason for the error, typically a machine-readable identifier for the failure cause. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="help" type="object">
                          Optional; if omitted, computed by GCP.
                          <Expandable title="properties">
                            <ResponseField name="links" type="list(object)">
                              A list of links to related documentation or help resources for the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="description" type="string">
                                  A human-readable description of the help link. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="url" type="string">
                                  The URL of the help resource linked from the error detail. This is the target of the help link. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="localized_message" type="object">
                          The localized error message, containing the locale (e.g., 'en-US') and the message text in that locale. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="locale" type="string">
                              The locale (language code) of the localized error message. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="message" type="string">
                              The localized error message text, as provided in the language/locale of the localized_message block. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="quota_info" type="object">
                          The quota_info block provides details about the quota limit that was exceeded or insufficient, which contributed to the operation error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="dimensions" type="map(string)">
                              A map of quota dimension names to their values (e.g., region, project) that define the scope of the quota that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="future_limit" type="number">
                              The quota limit that will be in effect after the operation completes, used to show the projected quota if the operation were to succeed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit" type="number">
                              The maximum allowed value for the quota metric. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit_name" type="string">
                              The name of the quota limit that was exceeded, as reported in the error details. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metric_name" type="string">
                              The name of the quota metric that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="rollout_status" type="string">
                              The status of the rollout operation that encountered the quota error. Possible values are IN_PROGRESS and ROLLOUT_STATUS_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="location" type="string">
                      The location where the error occurred, typically a field name or resource identifier associated with the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A human-readable error message describing the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              Output only. Timestamp of the last progress check of bulk instance operation. Timestamp is in RFC3339 text format. Computed by GCP after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_instance_statuses" type="object">
      The list of instance statuses and the number of instances in this managed instance group that have the status. For more information about how to interpret each status check the instance lifecycle documentation. Currently only shown for TPU MIGs. Optional.
      <Expandable title="properties">
        <ResponseField name="deprovisioning" type="number">
          Output only. The number of instances in the managed instance group that have DEPROVISIONING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="non_existent" type="number">
          Output only. The number of instances that have not been created yet or have been deleted. Includes only instances that would be shown in the listManagedInstances method and not all instances that have been deleted in the lifetime of the MIG. Does not include FlexStart instances that are waiting for the resources availability, they are considered as 'pending'. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="pending" type="number">
          Output only. The number of instances in the managed instance group that have PENDING status, that is FlexStart instances that are waiting for resources. Instances that do not exist because of the other reasons are counted as 'non_existent'. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="pending_stop" type="number">
          Output only. The number of instances in the managed instance group that have PENDING_STOP status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="provisioning" type="number">
          Output only. The number of instances in the managed instance group that have PROVISIONING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="repairing" type="number">
          Output only. The number of instances in the managed instance group that have REPAIRING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="running" type="number">
          Output only. The number of instances in the managed instance group that have RUNNING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="staging" type="number">
          Output only. The number of instances in the managed instance group that have STAGING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="stopped" type="number">
          Output only. The number of instances in the managed instance group that have STOPPED status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="stopping" type="number">
          Output only. The number of instances in the managed instance group that have STOPPING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="suspended" type="number">
          Output only. The number of instances in the managed instance group that have SUSPENDED status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="suspending" type="number">
          Output only. The number of instances in the managed instance group that have SUSPENDING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="terminated" type="number">
          Output only. The number of instances in the managed instance group that have TERMINATED status. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_stable" type="bool">
      Output only. A bit indicating whether the managed instance group is in a stable state. A stable state means that: none of the instances in the managed instance group is currently undergoing any type of change (for example, creation, restart, or deletion); no future changes are scheduled for instances in the managed instance group; and the managed instance group itself is not being modified. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="stateful" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="has_stateful_config" type="bool">
          Output only. A bit indicating whether the managed instance group has stateful configuration, that is, if you have configured any items in a stateful policy or in per-instance configs. The group might report that it has no stateful configuration even when there is still some preserved state on a managed instance, for example, if you have deleted all PICs but not yet applied those deletions. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="per_instance_configs" type="object">
          A list of statuses for each per-instance configuration in the regional instance group manager, containing the configuration ID and its current state. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="all_effective" type="bool">
              Output only. A bit indicating if all of the group's per-instance configurations (listed in the output of a listPerInstanceConfigs API call) have status EFFECTIVE or there are no per-instance-configs. Computed by GCP after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version_target" type="object">
      A map of version names to the target number of instances for each version in the regional instance group manager. This indicates the intended distribution of instances among the configured versions. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="is_reached" type="bool">
          Output only. A bit indicating whether version target has been reached in this managed instance group, i.e. all instances are in their target version. Instances' target version are specified byversion field on Instance Group Manager. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_pools" type="list(string)">
  The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically apply to all of the instances in the managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_size" type="number">
  The target number of running instances for this managed instance group. You can reduce this number by using the instanceGroupManager deleteInstances or abandonInstances methods. Resizing the group also changes this number. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_size_policy" type="object">
  The target_size_policy block defines the scaling limits for the managed instance group, specifying the minimum and maximum number of instances. This field is computed when not explicitly set, and the API determines the effective policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      The mode of target size policy based on which the MIG creates its VMs individually or all at once. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_stopped_size" type="number">
  The target number of stopped instances for this managed instance group. This number changes when you: - Stop instance using the stopInstances method or start instances using the startInstances method. - Manually change the targetStoppedSize using the update method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_suspended_size" type="number">
  The target number of suspended instances for this managed instance group. This number changes when you: - Suspend instance using the suspendInstances method or resume instances using the resumeInstances method. - Manually change the targetSuspendedSize using the update method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="update_policy" type="object">
  The update policy for the managed instance group, defining how instances are updated (e.g., rolling updates). This field is computed and reflects the current policy applied by the API. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="instance_redistribution_type" type="string">
      The instance redistribution policy for regional managed instance groups. Valid values are: - PROACTIVE (default): The group attempts to maintain an even distribution of VM instances across zones in the region. - NONE: For non-autoscaled groups, proactive redistribution is disabled. Optional.
    </ResponseField>
    <ResponseField name="max_surge" type="object">
      Encapsulates numeric value that can be either absolute or relative. Optional.
      <Expandable title="properties">
        <ResponseField name="calculated" type="number">
          Output only. Absolute value of VM instances calculated based on the specific mode. - If the value is fixed, then the calculated value is equal to the fixed value. - If the value is a percent, then the calculated value is percent/100 * targetSize. For example, the calculated value of a 80% of a managed instance group with 150 instances would be (80/100 * 150) = 120 VM instances. If there is a remainder, the number is rounded. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="fixed" type="number">
          Specifies a fixed number of VM instances. This must be a positive integer. Optional.
        </ResponseField>
        <ResponseField name="percent" type="number">
          Specifies a percentage of instances between 0 to 100%, inclusive. For example, specify 80 for 80%. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_unavailable" type="object">
      Encapsulates numeric value that can be either absolute or relative. Optional.
      <Expandable title="properties">
        <ResponseField name="calculated" type="number">
          Output only. Absolute value of VM instances calculated based on the specific mode. - If the value is fixed, then the calculated value is equal to the fixed value. - If the value is a percent, then the calculated value is percent/100 * targetSize. For example, the calculated value of a 80% of a managed instance group with 150 instances would be (80/100 * 150) = 120 VM instances. If there is a remainder, the number is rounded. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="fixed" type="number">
          Specifies a fixed number of VM instances. This must be a positive integer. Optional.
        </ResponseField>
        <ResponseField name="percent" type="number">
          Specifies a percentage of instances between 0 to 100%, inclusive. For example, specify 80 for 80%. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="minimal_action" type="string">
      Minimal action to be taken on an instance. Use this option to minimize disruption as much as possible or to apply a more disruptive action than is necessary. - To limit disruption as much as possible, set the minimal action toREFRESH. If your update requires a more disruptive action, Compute Engine performs the necessary action to execute the update. - To apply a more disruptive action than is strictly necessary, set the minimal action to RESTART or REPLACE. For example, Compute Engine does not need to restart a VM to change its metadata. But if your application reads instance metadata only when a VM is restarted, you can set the minimal action to RESTART in order to pick up metadata changes. Optional.
    </ResponseField>
    <ResponseField name="most_disruptive_allowed_action" type="string">
      Most disruptive action that is allowed to be taken on an instance. You can specify either NONE to forbid any actions,REFRESH to avoid restarting the VM and to limit disruption as much as possible. RESTART to allow actions that can be applied without instance replacing or REPLACE to allow all possible actions. If the Updater determines that the minimal update action needed is more disruptive than most disruptive allowed action you specify it will not perform the update at all. Optional.
    </ResponseField>
    <ResponseField name="replacement_method" type="string">
      What action should be used to replace instances. See minimal_action.REPLACE Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of update process. You can specify either PROACTIVE so that the MIG automatically updates VMs to the latest configurations orOPPORTUNISTIC so that you can select the VMs that you want to update. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="versions" type="list(object)">
  Specifies the instance templates used by this managed instance group to create instances. Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance group. This field overrides the top-level instanceTemplate field. Read more about therelationships between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all remaining instances. For more information, read aboutcanary updates. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_template" type="string">
      The URL (self_link) of the instance template that defines the configuration for this version of the managed instance group. This template is used to create instances when the group scales or updates to this version. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="target_size" type="object">
      The target size for this version, specified either as a fixed number of instances or as a percentage of the group's total size. This controls how many instances are assigned to this version when using multiple versions. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="calculated" type="number">
          The number of instances that are created from this version based on the autoscaler's calculated capacity. This value is computed automatically and cannot be set. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fixed" type="number">
          The fixed number of instances that this version should maintain. This is used when specifying a non-percentage target size. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="percent" type="number">
          The percentage of instances in the group that should be running this version. Must be between 0 and 100. Only valid when updating the group. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. The URL of azone where the managed instance group is located (for zonal resources). Optional; if omitted, computed by GCP.
</ResponseField>

## Output properties

<ResponseField name="all_instances_config" type="object">
  The computed configuration applied to all instances in this regional instance group manager. This field is output-only and reflects the current all-instances configuration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="properties" type="object">
      Represents the change that you want to make to the instance properties. Optional.
      <Expandable title="properties">
        <ResponseField name="labels" type="map(string)">
          The label key-value pairs that you want to patch onto the instance. Optional.
        </ResponseField>
        <ResponseField name="metadata" type="map(string)">
          The metadata key-value pairs that you want to patch onto the instance. For more information, see Project and instance metadata. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="auto_healing_policies" type="list(object)">
  The autohealing policy for this managed instance group. You can specify only one value. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="health_check" type="string">
      The health check resource URL (self-link) that determines instance health for auto-healing. Can be specified as a full URL, a self-link, or a name reference. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="initial_delay_sec" type="number">
      The initial delay, in seconds, before an instance is considered available for auto-healing. This is the time to wait after an instance is created or restarted before checking its health for auto-healing purposes. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="base_instance_name" type="string">
  The base instance name is a prefix that you want to attach to the names of all VMs in a MIG. The maximum character length is 58 and the name must comply with RFC1035 format. When a VM is created in the group, the MIG appends a hyphen and a random four-character string to the base instance name. If you want the MIG to assign sequential numbers instead of a random string, then end the base instance name with a hyphen followed by one or more hash symbols. The hash symbols indicate the number of digits. For example, a base instance name of "vm-###" results in "vm-001" as a VM name. @pattern [a-z](([-a-z0-9]&#123;0,57&#125;)|([-a-z0-9]&#123;0,51&#125;-#&#123;1,10&#125;(\\[[0-9]&#123;1,10&#125;\\])?)) Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="creation_timestamp" type="string">
  Output only. The creation timestamp for this managed instance group inRFC3339 text format. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="current_actions" type="object">
  The current actions on instances in the group, providing counts of pending operations (e.g., creating, deleting, restarting, recreating). This is a computed attribute that reflects the live state of the managed instance group. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="abandoning" type="number">
      Output only. The total number of instances in the managed instance group that are scheduled to be abandoned. Abandoning an instance removes it from the managed instance group without deleting it. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="creating" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be created or are currently being created. If the group fails to create any of these instances, it tries again until it creates the instance successfully. If you have disabled creation retries, this field will not be populated; instead, the creatingWithoutRetries field will be populated. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="creating_without_retries" type="number">
      Output only. The number of instances that the managed instance group will attempt to create. The group attempts to create each instance only once. If the group fails to create any of these instances, it decreases the group's targetSize value accordingly. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="deleting" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be deleted or are currently being deleted. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="none" type="number">
      Output only. The number of instances in the managed instance group that are running and have no scheduled actions. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="recreating" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be recreated or are currently being being recreated. Recreating an instance deletes the existing root persistent disk and creates a new disk from the image that is defined in the instance template. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="refreshing" type="number">
      Output only. The number of instances in the managed instance group that are being reconfigured with properties that do not require a restart or a recreate action. For example, setting or removing target pools for the instance. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="restarting" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be restarted or are currently being restarted. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="resuming" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be resumed or are currently being resumed. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="starting" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be started or are currently being started. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="stopping" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be stopped or are currently being stopped. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="suspending" type="number">
      Output only. The number of instances in the managed instance group that are scheduled to be suspended or are currently being suspended. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="verifying" type="number">
      Output only. The number of instances in the managed instance group that are being verified. See the managedInstances[].currentAction property in the listManagedInstances method documentation. Computed by GCP after `ship`.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="description" type="string">
  An optional description of this resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="distribution_policy" type="object">
  The distribution policy for the regional instance group manager, defining how instances are distributed across zones in the region. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="target_shape" type="string">
      The distribution shape to which the group converges either proactively or on resize events (depending on the value set inupdatePolicy.instanceRedistributionType). Optional.
    </ResponseField>
    <ResponseField name="zones" type="list(object)">
      Zones where the regional managed instance group will create and manage its instances. By default, a regional MIG doesn't automatically select an AI zone to create instances, even if an AI zone is available in the specified region. To create instances in an AI zone in the selected region, you must explicitly specify it in the distribution policy together with the other preferred zones. Optional.
      <Expandable title="properties">
        <ResponseField name="zone" type="string">
          Specifies the zone to which instances are distributed as part of the distribution policy for this regional managed instance group. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="fingerprint" type="string">
  Fingerprint of this resource. This field may be used in optimistic locking. It will be ignored when inserting an InstanceGroupManager. An up-to-date fingerprint must be provided in order to update the InstanceGroupManager, otherwise the request will fail with error412 conditionNotMet. To see the latest fingerprint, make a get() request to retrieve an InstanceGroupManager. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="id" type="string">
  Output only. A unique identifier for this resource type. The server generates this identifier. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_flexibility_policy" type="object">
  Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_selections" type="map(object)">
      Named instance selections configuring properties that the group will use when creating new VMs. Optional.
      <Expandable title="properties">
        <ResponseField name="machine_types" type="list(string)">
          List of machine types that can be used for this instance selection. The instance group manager can choose any of these types when provisioning instances according to the flexibility policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="rank" type="number">
          The priority rank of this instance selection, where lower numbers have higher priority. Must be between 0 and 5. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_group" type="string">
  Output only. The URL of the Instance Group resource. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="instance_lifecycle_policy" type="object">
  The lifecycle policy that determines how the managed instance group handles instance failures, including whether to restart or replace instances. This field is computed and output-only, reflecting the effective policy applied to the group. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="default_action_on_failure" type="string">
      The action that a MIG performs on a failed VM. If the value of the onFailedHealthCheck field is `DEFAULT_ACTION`, then the same action also applies to the VMs on which your application fails a health check. Valid values are - REPAIR (default): MIG automatically repairs a failed VM by recreating it. For more information, see About repairing VMs in a MIG. - DO_NOTHING: MIG does not repair a failed VM. Optional.
    </ResponseField>
    <ResponseField name="force_update_on_repair" type="string">
      A bit indicating whether to forcefully apply the group's latest configuration when repairing a VM. Valid options are: - NO (default): If configuration updates are available, they are not forcefully applied during repair. Instead, configuration updates are applied according to the group's update policy. - YES: If configuration updates are available, they are applied during repair. Optional.
    </ResponseField>
    <ResponseField name="on_failed_health_check" type="string">
      The action that a MIG performs on an unhealthy VM. A VM is marked as unhealthy when the application running on that VM fails a health check. Valid values are: - DEFAULT_ACTION (default): MIG uses the same action configured for instanceLifecyclePolicy.defaultActionOnFailure field. - REPAIR: MIG automatically repairs an unhealthy VM by recreating it. - DO_NOTHING: MIG doesn't repair an unhealthy VM. For more information, see About repairing VMs in a MIG. Optional.
    </ResponseField>
    <ResponseField name="on_repair" type="object">
      Configuration for VM repairs in the MIG. Optional.
      <Expandable title="properties">
        <ResponseField name="allow_changing_zone" type="string">
          Specifies whether the MIG can change a VM's zone during a repair. Valid values are: - NO (default): MIG cannot change a VM's zone during a repair. - YES: MIG can select a different zone for the VM during a repair. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="instance_template" type="string">
  The URL of the instance template that is specified for this managed instance group. The group uses this template to create all new instances in the managed instance group. The templates for existing instances in the group do not change unless you run recreateInstances, runapplyUpdatesToInstances, or set the group'supdatePolicy.type to PROACTIVE. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="kind" type="string">
  Output only. The resource type, which is alwayscompute#instanceGroupManager for managed instance groups. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="list_managed_instances_results" type="string">
  Pagination behavior of the listManagedInstances API method for this managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="name" type="string">
  The name of the managed instance group. The name must be 1-63 characters long, and comply withRFC1035. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="named_ports" type="list(object)">
  [Output Only] Named ports configured on the Instance Groups complementary to this Instance Group Manager. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      The symbolic name of the named port, which acts as the key in the named port mapping to a port number. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="port" type="number">
      The port number for this named port. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="region" type="string">
  Output only. [Output Only] The URL of theregion where the managed instance group resides (for regional resources). Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="resource_policies" type="object">
  The resource policies attached to the instances in this regional managed instance group. This field is output-only and reflects the current policies applied to the group's instances. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="workload_policy" type="string">
      The URL of the workload policy that is specified for this managed instance group. It can be a full or partial URL. For example, the following are all valid URLs to a workload policy: - https://www.googleapis.com/compute/v1/projects/project/regions/region/resourcePolicies/resourcePolicy - projects/project/regions/region/resourcePolicies/resourcePolicy - regions/region/resourcePolicies/resourcePolicy Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="satisfies_pzi" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="satisfies_pzs" type="bool">
  Output only. Reserved for future use. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="self_link" type="string">
  Output only. The URL for this managed instance group. The server defines this URL. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="standby_policy" type="object">
  The standby policy for the managed instance group, which defines settings for standby instances, such as the initial delay before a standby instance is stopped. This field is output-only and populated by the Google Compute Engine API. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="initial_delay_sec" type="number">
      Specifies the number of seconds that the MIG should wait to suspend or stop a VM after that VM was created. The initial delay gives the initialization script the time to prepare your VM for a quick scale out. The value of initial delay must be between 0 and 3600 seconds. The default value is 0. Optional.
    </ResponseField>
    <ResponseField name="mode" type="string">
      Defines how a MIG resumes or starts VMs from a standby pool when the group scales out. The default mode is `MANUAL`. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="stateful_policy" type="object">
  The stateful policy configuration for the regional instance group manager. This optional block defines which disks and metadata are preserved for instances during group operations such as Rolling Update or Instance Regeneration. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="preserved_state" type="object">
      Configuration of preserved resources. Optional.
      <Expandable title="properties">
        <ResponseField name="disks" type="map(object)">
          Disks created on the instances that will be preserved on instance delete, update, etc. This map is keyed with the device names of the disks. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Specifies whether the stateful disk should be automatically deleted when the instance is deleted. Accepts 'true' or 'false'. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="external_ips" type="map(object)">
          External network IPs assigned to the instances that will be preserved on instance delete, update, etc. This map is keyed with the network interface name. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Controls whether the associated external IP is automatically deleted when the instance is deleted, or whether it is preserved. In a stateful preserved state configuration, this determines the lifecycle of the external IP for the instance. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="internal_ips" type="map(object)">
          Internal network IPs assigned to the instances that will be preserved on instance delete, update, etc. This map is keyed with the network interface name. Optional.
          <Expandable title="properties">
            <ResponseField name="auto_delete" type="string">
              Controls whether the internal IP address is automatically deleted when its managed instance is deleted. Set to 'true' to delete the IP address automatically, or 'false' to preserve it for reuse. Optional; if omitted, computed by GCP. **(AI-inferred)**
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="status" type="object">
  The current status of the managed instance group, including stability and the state of managed instances. This is a computed field that provides read-only information about the group's runtime state. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="all_instances_config" type="object">
      The status of the all-instances configuration for this regional instance group manager, including the fingerprint and the current state of the configuration applied to all instances. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="current_revision" type="string">
          Output only. Current all-instances configuration revision. This value is in RFC3339 text format. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="effective" type="bool">
          Output only. A bit indicating whether this configuration has been applied to all managed instances in the group. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="applied_accelerator_topologies" type="list(object)">
      Output only. The accelerator topology applied to this MIG. Currently only one accelerator topology is supported. Computed by GCP after `ship`.
      <Expandable title="properties">
        <ResponseField name="accelerator_topology" type="string">
          The accelerator topology currently applied to this regional instance group manager. This may differ from the topology specified in the instance template, as it reflects the actual state. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state" type="string">
          The current state of the accelerator topology applied to the instance group. Possible values are: ACTIVATING, ACTIVE, DEACTIVATING, FAILED, INCOMPLETE, and REACTIVATING. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="state_details" type="object">
          Optional; if omitted, computed by GCP.
          <Expandable title="properties">
            <ResponseField name="error" type="object">
              The error object contains details about a failure that occurred while applying an accelerator topology to the instance group, including the error code and message. Optional; if omitted, computed by GCP. **(AI-inferred)**
              <Expandable title="properties">
                <ResponseField name="errors" type="list(object)">
                  A list of errors that occurred while applying accelerator topologies to the instance group. Each error object contains a code and message describing the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      Optional; if omitted, computed by GCP.
                    </ResponseField>
                    <ResponseField name="error_details" type="list(object)">
                      A list of objects containing detailed information about the errors that occurred while applying accelerator topologies to the instance group. Optional; if omitted, computed by GCP. **(AI-inferred)**
                      <Expandable title="properties">
                        <ResponseField name="error_info" type="object">
                          Optional; if omitted, computed by GCP.
                          <Expandable title="properties">
                            <ResponseField name="domain" type="string">
                              The domain associated with the error, typically indicating the service or API where the error originated (e.g., 'compute.googleapis.com'). Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metadatas" type="map(string)">
                              A map of key-value pairs containing additional error information that can help diagnose the issue. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="reason" type="string">
                              The reason for the error, as defined by the error domain. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="help" type="object">
                          The help information associated with the error, containing links to relevant documentation and guidance for resolving the issue. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="links" type="list(object)">
                              A list of links that provide additional helpful information about the error. Each link includes a URL and an optional description. Optional; if omitted, computed by GCP. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="description" type="string">
                                  The description of the help link provided for the error. This text explains the purpose of the linked resource, such as a documentation page. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="url" type="string">
                                  The URL of the help resource associated with the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="localized_message" type="object">
                          A localized error message that provides a human-readable version of the error in a specific language, along with the locale identifier. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="locale" type="string">
                              The locale (e.g., 'en-US') for the localized error message, indicating the language in which the message is provided. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="message" type="string">
                              The human-readable localized message describing the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="quota_info" type="object">
                          Information about a quota violation related to the error, including the quota metric, limit, and current usage. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="dimensions" type="map(string)">
                              A map of dimension names to string values that identifies the specific quota that was exceeded, such as region or resource type. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="future_limit" type="number">
                              The projected quota limit that would be in effect if the operation succeeds, used in quota error details to indicate the limit after a change. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit" type="number">
                              The quota limit for the metric that triggered the quota error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit_name" type="string">
                              The name of the specific quota limit that was exceeded, as reported in the quota error details. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metric_name" type="string">
                              The name of the quota metric that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="rollout_status" type="string">
                              The current rollout status of the applied accelerator topologies. Possible values are `IN_PROGRESS` and `ROLLOUT_STATUS_UNSPECIFIED`. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="location" type="string">
                      The field or path in the resource configuration that caused the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A human-readable error message describing the reason for the failure within the applied accelerator topology state. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              Optional; if omitted, computed by GCP.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="autoscaler" type="string">
      Output only. The URL of theAutoscaler that targets this instance group manager. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="bulk_instance_operation" type="object">
      Bulk instance operation is the creation of VMs in a MIG when the targetSizePolicy.mode is set to BULK. Optional.
      <Expandable title="properties">
        <ResponseField name="in_progress" type="bool">
          Output only. Informs whether bulk instance operation is in progress. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="last_progress_check" type="object">
          Optional.
          <Expandable title="properties">
            <ResponseField name="error" type="object">
              Output only. Errors encountered during bulk instance operation. Computed by GCP after `ship`.
              <Expandable title="properties">
                <ResponseField name="errors" type="list(object)">
                  [Output Only] The array of errors encountered while processing this operation. Optional.
                  <Expandable title="properties">
                    <ResponseField name="code" type="string">
                      The machine-readable code identifying the type of error that occurred. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="error_details" type="list(object)">
                      Optional; if omitted, computed by GCP.
                      <Expandable title="properties">
                        <ResponseField name="error_info" type="object">
                          Optional; if omitted, computed by GCP.
                          <Expandable title="properties">
                            <ResponseField name="domain" type="string">
                              The logical grouping to which the error belongs, such as "googleapis.com" or "gdata". Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metadatas" type="map(string)">
                              Additional structured details about the error, as key-value pairs. The keys and values are service-defined and provide context such as the affected component or reason for the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="reason" type="string">
                              The reason for the error, typically a machine-readable identifier for the failure cause. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="help" type="object">
                          Optional; if omitted, computed by GCP.
                          <Expandable title="properties">
                            <ResponseField name="links" type="list(object)">
                              A list of links to related documentation or help resources for the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                              <Expandable title="properties">
                                <ResponseField name="description" type="string">
                                  A human-readable description of the help link. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                                <ResponseField name="url" type="string">
                                  The URL of the help resource linked from the error detail. This is the target of the help link. Optional; if omitted, computed by GCP. **(AI-inferred)**
                                </ResponseField>
                              </Expandable>
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="localized_message" type="object">
                          The localized error message, containing the locale (e.g., 'en-US') and the message text in that locale. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="locale" type="string">
                              The locale (language code) of the localized error message. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="message" type="string">
                              The localized error message text, as provided in the language/locale of the localized_message block. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                        <ResponseField name="quota_info" type="object">
                          The quota_info block provides details about the quota limit that was exceeded or insufficient, which contributed to the operation error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                          <Expandable title="properties">
                            <ResponseField name="dimensions" type="map(string)">
                              A map of quota dimension names to their values (e.g., region, project) that define the scope of the quota that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="future_limit" type="number">
                              The quota limit that will be in effect after the operation completes, used to show the projected quota if the operation were to succeed. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit" type="number">
                              The maximum allowed value for the quota metric. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="limit_name" type="string">
                              The name of the quota limit that was exceeded, as reported in the error details. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="metric_name" type="string">
                              The name of the quota metric that was exceeded. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                            <ResponseField name="rollout_status" type="string">
                              The status of the rollout operation that encountered the quota error. Possible values are IN_PROGRESS and ROLLOUT_STATUS_UNSPECIFIED. Optional; if omitted, computed by GCP. **(AI-inferred)**
                            </ResponseField>
                          </Expandable>
                        </ResponseField>
                      </Expandable>
                    </ResponseField>
                    <ResponseField name="location" type="string">
                      The location where the error occurred, typically a field name or resource identifier associated with the error. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                    <ResponseField name="message" type="string">
                      A human-readable error message describing the failure. Optional; if omitted, computed by GCP. **(AI-inferred)**
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
            <ResponseField name="timestamp" type="string">
              Output only. Timestamp of the last progress check of bulk instance operation. Timestamp is in RFC3339 text format. Computed by GCP after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="current_instance_statuses" type="object">
      The list of instance statuses and the number of instances in this managed instance group that have the status. For more information about how to interpret each status check the instance lifecycle documentation. Currently only shown for TPU MIGs. Optional.
      <Expandable title="properties">
        <ResponseField name="deprovisioning" type="number">
          Output only. The number of instances in the managed instance group that have DEPROVISIONING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="non_existent" type="number">
          Output only. The number of instances that have not been created yet or have been deleted. Includes only instances that would be shown in the listManagedInstances method and not all instances that have been deleted in the lifetime of the MIG. Does not include FlexStart instances that are waiting for the resources availability, they are considered as 'pending'. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="pending" type="number">
          Output only. The number of instances in the managed instance group that have PENDING status, that is FlexStart instances that are waiting for resources. Instances that do not exist because of the other reasons are counted as 'non_existent'. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="pending_stop" type="number">
          Output only. The number of instances in the managed instance group that have PENDING_STOP status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="provisioning" type="number">
          Output only. The number of instances in the managed instance group that have PROVISIONING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="repairing" type="number">
          Output only. The number of instances in the managed instance group that have REPAIRING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="running" type="number">
          Output only. The number of instances in the managed instance group that have RUNNING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="staging" type="number">
          Output only. The number of instances in the managed instance group that have STAGING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="stopped" type="number">
          Output only. The number of instances in the managed instance group that have STOPPED status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="stopping" type="number">
          Output only. The number of instances in the managed instance group that have STOPPING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="suspended" type="number">
          Output only. The number of instances in the managed instance group that have SUSPENDED status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="suspending" type="number">
          Output only. The number of instances in the managed instance group that have SUSPENDING status. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="terminated" type="number">
          Output only. The number of instances in the managed instance group that have TERMINATED status. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="is_stable" type="bool">
      Output only. A bit indicating whether the managed instance group is in a stable state. A stable state means that: none of the instances in the managed instance group is currently undergoing any type of change (for example, creation, restart, or deletion); no future changes are scheduled for instances in the managed instance group; and the managed instance group itself is not being modified. Computed by GCP after `ship`.
    </ResponseField>
    <ResponseField name="stateful" type="object">
      Optional.
      <Expandable title="properties">
        <ResponseField name="has_stateful_config" type="bool">
          Output only. A bit indicating whether the managed instance group has stateful configuration, that is, if you have configured any items in a stateful policy or in per-instance configs. The group might report that it has no stateful configuration even when there is still some preserved state on a managed instance, for example, if you have deleted all PICs but not yet applied those deletions. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="per_instance_configs" type="object">
          A list of statuses for each per-instance configuration in the regional instance group manager, containing the configuration ID and its current state. Optional. **(AI-inferred)**
          <Expandable title="properties">
            <ResponseField name="all_effective" type="bool">
              Output only. A bit indicating if all of the group's per-instance configurations (listed in the output of a listPerInstanceConfigs API call) have status EFFECTIVE or there are no per-instance-configs. Computed by GCP after `ship`.
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="version_target" type="object">
      A map of version names to the target number of instances for each version in the regional instance group manager. This indicates the intended distribution of instances among the configured versions. Optional. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="is_reached" type="bool">
          Output only. A bit indicating whether version target has been reached in this managed instance group, i.e. all instances are in their target version. Instances' target version are specified byversion field on Instance Group Manager. Computed by GCP after `ship`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_pools" type="list(string)">
  The URLs for all TargetPool resources to which instances in theinstanceGroup field are added. The target pools automatically apply to all of the instances in the managed instance group. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_size" type="number">
  The target number of running instances for this managed instance group. You can reduce this number by using the instanceGroupManager deleteInstances or abandonInstances methods. Resizing the group also changes this number. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_size_policy" type="object">
  The target_size_policy block defines the scaling limits for the managed instance group, specifying the minimum and maximum number of instances. This field is computed when not explicitly set, and the API determines the effective policy. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="mode" type="string">
      The mode of target size policy based on which the MIG creates its VMs individually or all at once. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="target_stopped_size" type="number">
  The target number of stopped instances for this managed instance group. This number changes when you: - Stop instance using the stopInstances method or start instances using the startInstances method. - Manually change the targetStoppedSize using the update method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="target_suspended_size" type="number">
  The target number of suspended instances for this managed instance group. This number changes when you: - Suspend instance using the suspendInstances method or resume instances using the resumeInstances method. - Manually change the targetSuspendedSize using the update method. Optional; if omitted, computed by GCP.
</ResponseField>

<ResponseField name="update_policy" type="object">
  The update policy for the managed instance group, defining how instances are updated (e.g., rolling updates). This field is computed and reflects the current policy applied by the API. Optional; if omitted, computed by GCP. **(AI-inferred)**
  <Expandable title="properties">
    <ResponseField name="instance_redistribution_type" type="string">
      The instance redistribution policy for regional managed instance groups. Valid values are: - PROACTIVE (default): The group attempts to maintain an even distribution of VM instances across zones in the region. - NONE: For non-autoscaled groups, proactive redistribution is disabled. Optional.
    </ResponseField>
    <ResponseField name="max_surge" type="object">
      Encapsulates numeric value that can be either absolute or relative. Optional.
      <Expandable title="properties">
        <ResponseField name="calculated" type="number">
          Output only. Absolute value of VM instances calculated based on the specific mode. - If the value is fixed, then the calculated value is equal to the fixed value. - If the value is a percent, then the calculated value is percent/100 * targetSize. For example, the calculated value of a 80% of a managed instance group with 150 instances would be (80/100 * 150) = 120 VM instances. If there is a remainder, the number is rounded. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="fixed" type="number">
          Specifies a fixed number of VM instances. This must be a positive integer. Optional.
        </ResponseField>
        <ResponseField name="percent" type="number">
          Specifies a percentage of instances between 0 to 100%, inclusive. For example, specify 80 for 80%. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="max_unavailable" type="object">
      Encapsulates numeric value that can be either absolute or relative. Optional.
      <Expandable title="properties">
        <ResponseField name="calculated" type="number">
          Output only. Absolute value of VM instances calculated based on the specific mode. - If the value is fixed, then the calculated value is equal to the fixed value. - If the value is a percent, then the calculated value is percent/100 * targetSize. For example, the calculated value of a 80% of a managed instance group with 150 instances would be (80/100 * 150) = 120 VM instances. If there is a remainder, the number is rounded. Computed by GCP after `ship`.
        </ResponseField>
        <ResponseField name="fixed" type="number">
          Specifies a fixed number of VM instances. This must be a positive integer. Optional.
        </ResponseField>
        <ResponseField name="percent" type="number">
          Specifies a percentage of instances between 0 to 100%, inclusive. For example, specify 80 for 80%. Optional.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="minimal_action" type="string">
      Minimal action to be taken on an instance. Use this option to minimize disruption as much as possible or to apply a more disruptive action than is necessary. - To limit disruption as much as possible, set the minimal action toREFRESH. If your update requires a more disruptive action, Compute Engine performs the necessary action to execute the update. - To apply a more disruptive action than is strictly necessary, set the minimal action to RESTART or REPLACE. For example, Compute Engine does not need to restart a VM to change its metadata. But if your application reads instance metadata only when a VM is restarted, you can set the minimal action to RESTART in order to pick up metadata changes. Optional.
    </ResponseField>
    <ResponseField name="most_disruptive_allowed_action" type="string">
      Most disruptive action that is allowed to be taken on an instance. You can specify either NONE to forbid any actions,REFRESH to avoid restarting the VM and to limit disruption as much as possible. RESTART to allow actions that can be applied without instance replacing or REPLACE to allow all possible actions. If the Updater determines that the minimal update action needed is more disruptive than most disruptive allowed action you specify it will not perform the update at all. Optional.
    </ResponseField>
    <ResponseField name="replacement_method" type="string">
      What action should be used to replace instances. See minimal_action.REPLACE Optional.
    </ResponseField>
    <ResponseField name="type" type="string">
      The type of update process. You can specify either PROACTIVE so that the MIG automatically updates VMs to the latest configurations orOPPORTUNISTIC so that you can select the VMs that you want to update. Optional.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="versions" type="list(object)">
  Specifies the instance templates used by this managed instance group to create instances. Each version is defined by an instanceTemplate and aname. Every version can appear at most once per instance group. This field overrides the top-level instanceTemplate field. Read more about therelationships between these fields. Exactly one version must leave thetargetSize field unset. That version will be applied to all remaining instances. For more information, read aboutcanary updates. Optional; if omitted, computed by GCP.
  <Expandable title="properties">
    <ResponseField name="instance_template" type="string">
      The URL (self_link) of the instance template that defines the configuration for this version of the managed instance group. This template is used to create instances when the group scales or updates to this version. Optional; if omitted, computed by GCP. **(AI-inferred)**
    </ResponseField>
    <ResponseField name="name" type="string">
      Optional; if omitted, computed by GCP.
    </ResponseField>
    <ResponseField name="target_size" type="object">
      The target size for this version, specified either as a fixed number of instances or as a percentage of the group's total size. This controls how many instances are assigned to this version when using multiple versions. Optional; if omitted, computed by GCP. **(AI-inferred)**
      <Expandable title="properties">
        <ResponseField name="calculated" type="number">
          The number of instances that are created from this version based on the autoscaler's calculated capacity. This value is computed automatically and cannot be set. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="fixed" type="number">
          The fixed number of instances that this version should maintain. This is used when specifying a non-percentage target size. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
        <ResponseField name="percent" type="number">
          The percentage of instances in the group that should be running this version. Must be between 0 and 100. Only valid when updating the group. Optional; if omitted, computed by GCP. **(AI-inferred)**
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="zone" type="string">
  Output only. The URL of azone where the managed instance group is located (for zonal resources). Optional; if omitted, computed by GCP.
</ResponseField>
