{
 "runbook": "write-artifacts",
 "target": "aws",
 "started": "2026-08-31",
 "updated": "2026-08-31",
 "hops": [
  {
   "id": "write-field-descriptions",
   "status": "in_progress",
   "note": "UBI-231. Scoped narrower than write-artifacts' full three-check coverage: this hop tracks missing_field_description only. Real, live-recomputed missing_field_description before batch 1: 613. Unlike gcp, aws has no common_aws_fields.py dictionary tier -- every field here is individually authored, checked against real AWS CloudFormation resource reference documentation for anything uncommon rather than guessed. aws is a long tail: 208 resources across roughly 90 real services, most services carrying only one or two gapped resources -- batched by service, largest first, expect this to run slower per field than gcp's clusters did.",
   "batches": [
    {
     "batch": 1,
     "service": "bedrockagentcore",
     "scope": "14 resources, 73 depth-0 fields -- the single largest AWS service by resource count",
     "wires_written": 14,
     "fields_written": 73,
     "gaps_before": 613,
     "gaps_after": 540,
     "note": "one field (aws_bedrock_agent_core_gateway_rule.description) recovered verbatim from this resource's own currently published page -- real, pre-existing AI-inferred text never migrated into descriptions.json, preserved rather than rewritten. The rest checked against AWS's own official CloudFormation resource reference (Gateway, GatewayTarget, Harness, PolicyEngine, Runtime) before writing.",
     "commit": "PENDING",
     "pr": "PENDING"
    }
   ]
  }
 ]
}
